# Cycle-097 resume notes (post batch-30 meta-phase)

**SESSION RESTART REQUIRED before cycle-097.** The batch-30 meta-phase (post-cycle-096 finalize, aggregating cycles 094/095/096) enacted role-spec changes under `.claude/agents/`. The parent orchestrator must restart the Claude Code session before cycle-097 begins so the new agent definitions are loaded (per friction-ledger `new-agent-defs-need-session-restart`). The restart also resets the primary conversation context (subsuming the retired per-meta `/compact` step — do NOT run a separate compaction).

## Agent-defs that changed (and why a restart is needed)

1. **`.claude/agents/integrator-finalize.md`** — step-5b rewritten. The graded-stack linters are now LIVE under `tools/graded-stack-lint/` (built c094), so the prior "not-yet-built no-op" branch is dropped. Finalize now runs `python3 tools/graded-stack-lint/graded_stack_lint.py --json` on the LANDED tree every cycle, gates on (i) any NEW `rank_violation` beyond the tracked baseline-exception set (baseline fully discharged at c096 → `rank_violations == 0`, so ANY violation is new and blocks) and (ii) a newly-orphaned node, records the `totals` block + the `rank_violations` cycle-over-cycle trend in `integrator-signals.md`, and exempts `cites-evidence` L0-range `depends-on` edges from slug-resolution + rank-check. The high `untyped`/`detritus` mass during the P1 rollout is explicitly NOT a block. (Resolves OQ `graded-stack-finalize-json-wiring-role-spec` + blesses OQ `cites-evidence-l0-edge-linter-slug-resolution-exemption`.)

2. **`.claude/agents/lifter.md`** — gained a graded-stack typed-edge + rank-invariant §Discipline bullet. The lifter is the cascade-execution + lazy-tail-typing producer (it re-types edges in firm-flip cascades, e.g. c095-D2, and typed the O1 lazy-tail node c096-D3) but was missed in the pre-c094 GRADED-STACK kickoff codification (commit `ac4fa0a`) that gave harvester/lowering-verifier/layer-intro-author/abstractor the matching bullet. The new bullet: a re-anchor/firm-flip/lazy-tail-typing the lifter lands carries `rank:`+`edges:` frontmatter satisfying `rank(u) ≤ min over depends-on deps`; confirm all `depends-on` deps read ≥ `firm` on disk before flipping a node firm (the hard-gate-new invariant — baseline now 0, so any new violation blocks at finalize); mark each authored edge `depends-on`/`reference` (root-edge `reference`; `cites-evidence` L0 ranges exempt); the honest residual gate stays if a dep is still `rough-in`. (Resolves friction `graded-stack-kickoff-codification-missed-the-lifter-role-spec`.)

No other `.claude/agents/` files, `skills/`, or CLAUDE.md changed this batch.

## Cycle-097 active head (the batch-31 LEAD)

Reshaped into `scaffolding/priorities.md` (CYCLE-097 / batch-31 active head). The LEAD is item 1 **`graded-stack-p2-slice-deletion-tranche`** — the dominant remaining graded-stack phase: absorb the 9 Phase-1 slices' worked-example content into concept/theme homes citing L0 directly, repoint the firm/concept citations off slice line-anchors (HARD blockers: `polynomial_recurrence_step:119-160` ×5, `arnoldi_step` heavy anchors, the `plane_rotation_stream :72-108`/`:73-108` off-by-one — reconcile FIRST), migrate the ~6 pending-lift OQs, mint `roadmap_goal` chapters for not-yet-describable homes, THEN delete the slices + `spec/index.md` rows + SUMMARY entries. Completion criterion (mechanical): the reachability GC shows `spec/slices/*` unreachable + all load-bearing claims reachable via non-slice paths + rank invariant holds with zero slice nodes. On completion: retire the `annotated-and-retained` carve-out + skill `phase-1-slice-reduction-audit`. The c096 planner explicitly deferred this as too large for a batch-closing cycle — sequence it as its OWN multi-dispatch campaign (absorb → repoint → verify-GC-unreachable → delete), NOT bundled with forward-frontier work.

Plus item 2 (batch-31 land-clean residues: `domain_energy_reduce.md:377` mwn stale-rough-in; any goal-flow residues; the `L2/index.md:112,121` normalize_B-gate phrasing) and item 3 (the standing bottom-up vocabulary + 5-driver→L4 completeness frontier, resumes once P2 is sequenced).
