# Integrator → planner signals

Append-only running ledger. The integrator appends a section at the **top** after each cycle's integration commit (newest first). The next cycle's `cycle-planner` reads the most recent ~3 entries as input to dispatch planning.

**User directive (2026-05-27):** the integrator should have a channel to write information used by the planner about next/unblocked/resolution/etc options implied by the integration. This file is that channel.

**Format** (per-cycle section):

```markdown
## cycle-<n> — <ISO-timestamp>

### Unblocked
- <one-line item per now-tractable priority / question> — <citation: priority slug or open-question slug>

### New dependencies
- <one-line edge that landed this cycle> — <citation: report / commit>

### Resolution implications
- <open-question slug> — <answered | partially-answered | needs-more> — <one-line how this cycle's landings bear on it>

### Suggested next dispatches
- (`<agent>`, `<scope>`) — <one-line rationale>

### Wave-conflict observations
- <one-line case where dispatches conflicted at integration; how the integrator resolved>

### Integration-tooling friction
- <one-line case where the integrator hit a gap that better tooling would close>
```

**Discipline:**

- Integrator appends each cycle (prepended at top — newest first).
- Cycle-planner reads top ~3 entries.
- Keep file under ~500 lines; entries older than 10 cycles archive to `scaffolding/integrator-signals-archive/cycle-<n>-<n+9>.md`.
- No other agent writes here. (If meta-phase needs to annotate, append a `<!-- meta-phase: ... -->` HTML comment to the relevant section.)

---

## cycle-013 — 2026-05-28T200000Z

**Meta-batch context**: **FIRST primary cycle of meta-batch-3** under the 3:1 meta cadence (cycles 013/014/015 form batch-3; the meta-phase fires after **cycle-015** finalize, NOT this cycle). Cycle counter does not reset at batch boundaries (batch-2 = 010/011/012 closed at the cycle-012 meta-phase commit `ba3cd0b`). Cycle-013 landed: the **FIRST live partly-constructive→firm promotion** (eigsolve-mutation-rotation Sub-pattern B) + the **first partly-constructive ENTRY** (divfree-projector L1) + L3 chebyshev partial-obstruction + L4 chebyshev rough-in + 2 new firm L1>L0 themes (orthogonalize + chebyshev-smoother mutation-rotation) + first L2-L1 chapter (chebyshev-iteration-fusion) + new partly-constructive eigsolve-convergence-reason-mapping sub-theme + L0 bundle-6 #3 (linalg-orthog-file; 18 chapters) + phase-1 corpus annotated-reduction 10/10 COMPLETE (removals stay 8/10).

<!-- meta-phase: this is the batch-3-OPENING integrator-signals section. The partly-constructive-status signals + citation-line-drift signal + discovery→authoring scope-stretch signal below are flagged for the cycle-015 meta-phase aggregation. -->

### Unblocked

- **`combinator-miner` on `chebyshev-l4-wrapper-iteration-vocabulary-reconcile`** unblocked (the L4 chebyshev firming condition) — the L4 `chebyshev` entry landed `rough-in` because its `forM_`/`foldM` iteration combinators are un-anchored at L4 and compete with the firm `iterate-while` family; re-expressing them via `iterate_while_pure`/`iterate-while-with-prev` with step-count predicates (OR anchoring them as firm L4 rows) flips the entry rough-in → firm and moves it into the "Firm at L4" cohort (count 3 → 4). Citation: OQ `chebyshev-l4-wrapper-iteration-vocabulary-reconcile` (cycle-013) + STAGING row 3.
- **`lowering-verifier` on `divfree-weakdiv-sign-convention-l0-verify`** unblocked (the divfree-projector firming condition) — the partly-constructive `L1/divfree-projector` entry's idempotence law + divergence-free output depend on the unverified `WeakDiv ≈ GᵀM` sign reading; a verify-citation-range audit on the MFEM-vendored `MixedVectorWeakDivergenceIntegrator` resolves the three-way irrotational/subtract/additive contradiction and flips partly-constructive → firm. Citation: OQ `divfree-weakdiv-sign-convention-l0-verify` + `divfree-projector-l1-l0-lowering-verifier-followup` (cycle-013) + STAGING row 2.
- **`lowering-verifier` on `eigsolve-convergence-reason-mapping-promotion`** unblocked-but-gated — the single global gate covering all 8 partly-constructive diverged rows of the new sub-theme (downstream of the parent Sub-pattern B's `EPSGetConvergedReason` gate); the lowering-verifier may UNBLOCK without ENACTING. Citation: OQ `eigsolve-convergence-reason-mapping-promotion` (cycle-013) + STAGING row 10.
- **`lowering-verifier` on `orthogonalize-mutation-rotation-lowering-verifier-audit` + `chebyshev-lowering-themes-lowering-verifier-followup`** unblocked — standard exhaustiveness audits for the 3 new firm lowering themes landed this cycle (orthogonalize 3-variant-loop-structure + chebyshev L1>L0 four-sub-pattern + chebyshev L2>L1 per-degree-step fusion). Citation: OQs (cycle-013) + STAGING rows 6, 7.
- **`harvester` on `l4-preconditioning-framework-promotion`** unblocked (the `cg_preconditioning_framework` REMOVABLE condition) — batch-4 annotated-reduced the slice to `partially-absorbed` but RETAINED §L4/§L4-v0.2/§L4-v0.3 as sole-source load-bearing; a future firm `L4/preconditioning-framework` (or `L4/ksp-solve`) lift would transcribe those sections + let the ~10 concept-page citations re-point, making the slice removable (not just stub-reduced). Citation: OQ `l4-preconditioning-framework-promotion` (cycle-013) + STAGING row 5.
- **`layer-intro-author` bundle-author on `l0-bundle-6-candidates` #2 `linalg-rap-file`** unblocked — bundle-6 #3 (`linalg-orthog-file`) landed this cycle; #2 `linalg-rap-file` is the next ranked candidate (suggested chapter outline + single-rank-reading caveat carried in the OQ). Citation: OQ `l0-bundle-6-candidates` (cycle-013 discovery-update) + STAGING row 9.
- **`layer-intro-author` on `chebyshev-l3-l4-layer-intro-refresh`** unblocked — the L3/L4 index intros need a narrative refresh now that the chebyshev rows (L3 partial-obstruction + L4 rough-in) landed. Citation: OQ `chebyshev-l3-l4-layer-intro-refresh` (cycle-013) + STAGING row 3.
- **`same-layer-cross-cutter` on `chebyshev-phase1-slice-reduction`** unblocked — the L3+L4 chebyshev rows landed, so `book/src/spec/slices/chebyshev.md` is now fully lifted and eligible for a reduction audit (with a concept-extension-preservation note). Citation: OQ `chebyshev-phase1-slice-reduction` (cycle-013) + STAGING row 3.
- **dedicated citation-re-anchor dispatch on `krylov-step-typed-wrapper-dissolution-cg-md-citation-sweep`** unblocked-but-small — the lifter cleaned the line-20/220 "no L3 row" residuals + 1 dangling `cg.md` pointer this cycle, but the theme still has dangling `cg.md` pointers at lines 98/109/126/200/204/210/218/231/233 needing a sweep to the firm `L3-L2/krylov-step-body-identity.md`. Citation: OQ `krylov-step-typed-wrapper-dissolution-cg-md-citation-sweep` (cycle-013) + STAGING row 8.
- **(carry-forward, still unblocked-but-large) `lifter`/`abstractor` on `gmres.md §L4 v0.6→v0.7 self-rotation`** — would firm both the cycle-008 GMRES theme + the cycle-011 FGMRES sister theme. Not selected cycle-013.
- **(carry-forward, large) `harvester` on NLEPS at L1+** — cycle-009 OQ; carry-forward.

### New dependencies

- **eigsolve-mutation-rotation PROMOTED partly-constructive → firm (cycle-013)** — the FIRST live such promotion; Sub-pattern B's `LinearSolveFailed` materialisation caveat dropped after applying the GetConverged-forwarder snippet fix (`ksp.hpp:41`/`:64`) + Sub-pattern A `SolveInternal` attribution + ncv-clamp citation refinements; theme now `firm (structural)`.
- **`L1/divfree-projector` partly-constructive ENTRY (cycle-013)** — NEW L1 operator; `ksp_solve`+`apply_linop`+`axpy` deps; idempotence law contingent on the WeakDiv-sign OQ; the first L1 entry to ENTER partly-constructive (complements the eigsolve EXIT).
- **L3 chebyshev partial-obstruction + L4 chebyshev rough-in (cycle-013)** — the chebyshev lowering chain extends to L3 (body lifts, inner-k-recurrence + outer-pc_it-sweep are sequential obstructions) + L4 (firm body, rough-in wrapper); non-adjacent L3↔L1 identity annotated in-line, no `L3-L1/`/`L3-L2/` directory.
- **L1>L0 cohort 7 → 9 themes (cycle-013)** — +`orthogonalize-mutation-rotation` (firm; 3 variant loop-structures) +`chebyshev-smoother-mutation-rotation` (firm; 4 sub-patterns + transpose-alias sub-rule) +`eigsolve-convergence-reason-mapping` (partly-constructive sibling sub-theme; 8 diverged-row EigStatus map) + eigsolve promotion.
- **L2-L1 Part 0 → 1 firm chapter (cycle-013)** — `chebyshev-iteration-fusion`; the FIRST real chapter under the previously-skeleton L2-L1 Part; the index `## Theme list` placeholder displaced by the firm theme table.
- **L0 bundle-6 #3 `linalg-orthog-file` (cycle-013)** — `palace/linalg/orthog.hpp` header-only overview; L0 now 18 chapters; authored via a discovery→authoring scope stretch (safety-gated).
- **4 concept pages touched (cycle-013)** — 3 plane-rotation canonical-pointer repoints (`plane-rotation-stream`/`givens_generate`/`givens_apply` → `plane_rotation_stream`) + `orthogonalization.md` full-file rewrite aligned to the firm L1 "does not normalize output" contract.

### Resolution implications

- **`eigsolve-getconverged-forwarder-fix-and-gated-promotion`** (cycle-012 GATED) — **resolved/enacted** (`open` → `answered`; the gated cycle-013 abstractor applied the edits + dropped the partly-constructive caveat; theme now firm). The FIRST live partly-constructive→firm promotion.
- **`l1-divfree-projector-promotion`** (cycle-012 HEADLINE) — **partially-resolved** (the L1 entry landed, but as `partly-constructive` not firm; firm promotion gated on `divfree-weakdiv-sign-convention-l0-verify`).
- **`l3-l4-chebyshev-rows-eligible`** (cycle-012) — **resolved** (`open` → `answered`; both rows landed — L3 partial-obstruction + L4 rough-in).
- **`orthogonalize-mutation-rotation-l1-l0-theme`** + **`chebyshev-l1-l0-and-l2-l1-lowering-themes`** (cycle-012) — both **resolved** (`open` → `answered`; the 3 firm themes landed incl. the first L2-L1 chapter).
- **`phase-1-corpus-reduction-batch-4-remaining-slices`** (cycle-012) — **resolved** (`open` → `answered`; both final slices annotated-reduced; annotated-reduction 10/10 COMPLETE — but REMOVALS stay 8/10, 2 retained; the removable condition for cg_preconditioning_framework is the new OQ `l4-preconditioning-framework-promotion`).
- **`plane-rotation-concept-page-canonical-pointer-repoint`** (cycle-012) — **resolved** (`open` → `answered`; 3 repoints applied; the 4th mermaid-edge candidate routed to `dependency-map-orthog-plane-rotation-stale-edge-prune`).
- **`krylov-step-theme-body-no-l3-row-drift-cycle-013`** (cycle-012) — **resolved** (`open` → `answered`; line-20/220 residuals struck + cg.md pointer re-anchored; theme-wide cg.md sweep routed to `krylov-step-typed-wrapper-dissolution-cg-md-citation-sweep`).
- **`concepts-orthogonalization-coefficient-normalisation-drift`** (cycle-012) — **resolved** (`open` → `answered`; full-file rewrite of `orthogonalization.md`).
- **`l0-bundle-6-candidates`** (cycle-009) — **partially-answered update** (#3 `linalg-orthog-file` landed; #2 `linalg-rap-file` next-ranked).
- **`krylov-step-l3-identity-in-form-audit`** (answered_in cycle-006) — **NOT re-touched** (the cycle-013 lifter firmed its historical "no L3 row needed" disposition tail; finalize did NOT double-close or re-open it per the informational STAGING note).
- **Cycle-015 meta-phase (batch-3 aggregation) fires after cycle-015 finalize** per 3:1 cadence — NOT this cycle.

### Suggested next dispatches

- **(combinator-miner)** `chebyshev-l4-wrapper-iteration-vocabulary-reconcile` — the L4 chebyshev rough-in→firm condition; reconcile `forM_`/`foldM` against the firm `iterate-while` family. **follow_up_agent in report 3's META is `combinator-miner`.**
- **(lowering-verifier)** `divfree-weakdiv-sign-convention-l0-verify` — the divfree-projector partly-constructive→firm condition; verify-citation-range on `MixedVectorWeakDivergenceIntegrator`.
- **(lowering-verifier)** the 3 new-firm-theme exhaustiveness audits (orthogonalize-mutation-rotation + chebyshev-smoother-mutation-rotation + chebyshev-iteration-fusion) + the `eigsolve-convergence-reason-mapping-promotion` global gate.
- **(harvester)** `l4-preconditioning-framework-promotion` — the firm-L4 lift that makes `cg_preconditioning_framework` removable (not just stub-reduced).
- **(layer-intro-author bundle-author)** `linalg-rap-file` (bundle-6 #2) + `chebyshev-l3-l4-layer-intro-refresh`.
- **(same-layer-cross-cutter)** `chebyshev-phase1-slice-reduction` — the now-fully-lifted chebyshev.md slice reduction audit.
- **(GATED to cycle-015 meta-phase)** `partly-constructive-to-firm-promotion-route-ratification` + `divfree-projector-status-adjudication` + `partial-obstruction-status-codification` — three status-mechanism methodology items aggregated from this cycle's heavy partly-constructive/partial-obstruction exercise.

### Wave-conflict observations

- **11-report dispatch — largest wave to date** (prior max 9, cycle-011). All 11 applied as-is at integration; zero rework loops; cap 12 honored.
- **`book/src/SUMMARY.md` touched ~6 times** (L3+L4 chebyshev, orthogonalize-mutation-rotation, chebyshev-smoother-mutation-rotation + chebyshev-iteration-fusion, linalg-orthog-file, eigsolve-convergence-reason-mapping) + the finalize divfree-projector build-repair. **One stale-anchor re-anchor**: report 10's SUMMARY Change-4 used the repairer's `bicgstab-iteration` anchor, which was stale post-orthogonalize-landing — re-anchored to the current `eigsolve-mutation-rotation`+`orthogonalize-mutation-rotation` adjacency, sibling placed directly under parent. Clean re-read-disk adaptation, no merge conflict.
- **`book/src/L1-L0/index.md` touched 4 times** (eigsolve-convergence-reason-mapping, orthogonalize, chebyshev-smoother, eigsolve promotion) at distinct rows; firm-themes-grouped-above-obstructions ordering preserved across the in-cycle landings.
- **`book/src/L2-L1/index.md` first-firm-row placeholder displacement**: the `## Theme list` carried a fenced `(empty — Phase B skeleton.)` placeholder (a fenced code block, not the bare line the cycle-006 precedent describes) — displaced (not appended-below) by the firm theme table per the `index-placeholder-displacement-on-first-firm-row` gate.
- **`scaffolding/open-questions.md` touched 11 times** at distinct ranges; append-before-Dropped convention held; zero collisions.
- **Serial-apply ordering honoured for cross-report link dependencies**: report 6 (orthogonalize-mutation-rotation theme) landed before report 11 (concepts/orthogonalization rewrite linking it) and report 9 (L0 linalg-orthog-file linking the firm L1/orthogonalize MGS range) — all downstream links resolved at build time. No dangling-link build break from in-cycle ordering.
- **No deferrals, no rejections, no rework loops.** Clean-run streak continues (cycles 005–013 — nine consecutive clean cycles under the split integrator), modulo the finalize SUMMARY-registration build-repair.

### Integration-tooling friction

- **SIGNAL FOR cycle-015 META-PHASE — divfree SUMMARY-registration build-break from an INCORRECT staging-row gate assertion**: report 2's per-report integrator Wrote `book/src/L1/divfree-projector.md` but recorded `summary-md-registration: n/a (L1 operator chapters are not individually SUMMARY-registered)`. **That assertion is FALSE** — every L1 operator chapter from `axpy` through `chebyshev-smoother` IS individually SUMMARY-registered, and mdBook errors on any `book/src/` markdown absent from SUMMARY. The build broke (`L1/divfree-projector.md wasn't included in SUMMARY.md`); finalize repaired it surgically. The per-report SUMMARY-registration gate carried a wrong heuristic for the L1 Part (it conflated L1 with the genuinely-not-registered cases). Better tooling: a per-report gate that greps SUMMARY for the sibling-Part registration pattern before deciding `n/a`, OR a finalize pre-commit `mdbook build --dry` that the per-report integrators can't skip. Recurrence-1 for this specific mislabel; candidate friction-ledger entry.
- **SIGNAL FOR cycle-015 META-PHASE — partly-constructive status heavily exercised in a single cycle (4 instances)**: eigsolve EXIT (the first live promotion to firm), divfree ENTRY (the first adjudicated-into entry), eigsolve-convergence-reason-mapping (new partly-constructive sub-theme), chebyshev L4 rough-in. The cycle-012-codified mechanism is in active use across all its modes. The **promotion-route ratification OQ** (`partly-constructive-to-firm-promotion-route-ratification`) and the **divfree status-adjudication OQ** are both routed to the cycle-015 meta-phase. The per-report integrators correctly handled the EXIT (flagged deliberate decision + OQ) and the ENTRY (status adjudication diverging from the harvester's argued firm) without blocking — the mechanism is working, but the ratification of the promotion ROUTE (theme `## Status` gate option-b vs an invariant per-line audit) is a conscious methodology judgment the meta-phase should make.
- **SIGNAL FOR cycle-015 META-PHASE — recurring citation line-drift (off-by-1-3 lines) in ~6 reports**: eigsolve, divfree, chebyshev, orthogonalize, L0-bundle, krylov-step all had repairer-applied citation-line-offset corrections (mostly codemap-ground-truth values 1 line off the producer's). The critic's skill-uptake-survey repeatedly noted that `verify-citation-range` was NOT self-invoked by the producers. 3-cycle-window candidate friction pattern (the producers cite from memory/initial-read rather than re-verifying against the codemap before emitting). Candidate: a producer-side soft-gate or a stronger skill-uptake nudge.
- **SIGNAL FOR cycle-015 META-PHASE — discovery→authoring plan-kind scope stretch**: report 9 was dispatched as an L0-bundle discovery/ranking task but ALSO authored a full firm L0 chapter (`linalg-orthog-file`). The integrator safety-gated it (codemap MGS-range verify + file-non-existence collision check + SUMMARY anchor) and applied the authored chapter on its merits rather than as a ranking note. Plan-kind boundary observation: discovery dispatches occasionally over-produce; the safety-gate held, but whether discovery dispatches SHOULD author is a plan-kind-consistency question for the meta-phase. (Report 11's cross-cutter→concept-rewrite is a parallel but distinct kind-stretch, also safety-gated/applied.)

---

## cycle-012 — 2026-05-28T072500Z

**Meta-batch context**: **THIRD / FINAL primary cycle of meta-batch-2** under the 3:1 meta cadence (cycles 010/011/012 form batch-2). **The meta-phase fires after THIS cycle-012 finalize commit**, aggregating evidence across the full 3-cycle batch (NOT run by this finalize — the orchestrator dispatches it separately). Cycle counter does not reset at batch boundaries. Cycle-012 landed: 2 new firm L1 ops (orthogonalize + chebyshev-smoother; L1 firm 8→10) + 1 new firm L2 (chebyshev-iteration; L2 firm 1→2, first L2 growth since cycle-005) + 2 lowering-verifier audits both resolved + cycle-009 eigsolve OQ cluster fully closed across the batch + 4 concept pages corrected/extended + L3 index refresh + L4 index SUPERSEDED drift cleaned + phase-1 corpus reduction batch-3 (cumulative 8/10).

<!-- meta-phase: this is the batch-2-closing integrator-signals section. The 10 enumerated forward signals below in §Integration-tooling friction + §Wave-conflict observations are the meta-phase's PRIMARY aggregation input. -->

### Unblocked

- **GATED `abstractor` on `eigsolve-getconverged-forwarder-fix-and-gated-promotion`** unblocked-but-GATED — the cycle-012 lowering-verifier audit of `eigsolve-mutation-rotation` returned confirms-with-refinement and **UNBLOCKS but does NOT enact** the Sub-pattern B partly-constructive → fully-firm promotion. The cycle-013 abstractor must FIRST apply audit Edit 2 (GetConverged public forwarder on `BaseKspSolver` mirroring `GetRelTol()` at `ksp.hpp:64`; `opInv->GetConverged()` is not on `ComplexKspSolver`'s public surface) + audit Edit 3 (Sub-pattern A attribution: switch+abort live in `ArpackEigenvalueSolver::SolveInternal`, not `SetWhichEigenpairs`); ONLY THEN drop the `partly-constructive` caveat. The `## Status` line was LEFT UNCHANGED this cycle. Citation: OQ `eigsolve-getconverged-forwarder-fix-and-gated-promotion` (cycle-012) + STAGING row 3.
- **`harvester` on `l1-divfree-projector-promotion`** unblocked (HEADLINE) — cycle-012 batch-3 reduced `divfree.md`; 6 firm entries cite the slice as load-bearing evidence; strong harvester candidate to lift the divergence-free projector to a firm L1 entry, which would unblock further divfree reduction. Citation: OQ `l1-divfree-projector-promotion` (cycle-012) + STAGING row 8.
- **`harvester` on L3 + L4 chebyshev rows** unblocked — cycle-012 landed firm `L1/chebyshev-smoother` + `L2/chebyshev-iteration`, but full `chebyshev.md` slice reduction remains gated on the L3 (partial-obstruction) + L4 (`ChebOp<E,S>` monadic) rows per the identity-lowerings-still-require-both-L-levels invariant. Citation: OQ `l3-l4-chebyshev-rows-eligible` (cycle-012) + STAGING row 2.
- **`abstractor` on `orthogonalize-mutation-rotation` + `chebyshev-smoother-mutation-rotation` / `chebyshev-iteration-fusion` lowering themes** unblocked — the firm L1/L2 operators landed cycle-012; their forward lowering themes (L1>L0 + L2>L1) are unauthored abstractor candidates analogous to the firm `ksp-solve-mutation-rotation` / `axpby-mutation-rotation` themes. Citation: OQs `orthogonalize-mutation-rotation-l1-l0-theme` + `chebyshev-l1-l0-and-l2-l1-lowering-themes` (cycle-012) + STAGING rows 1, 2.
- **`same-layer-cross-cutter` on phase-1 corpus reduction batch-4** unblocked — batch-3 landed (8/10 reduced); the final 2 slices remain: `cg_preconditioning_framework` (priority #5; likely overlaps `L1/ksp_solve` + `L4/krylov-step` Form A + the chebyshev consumer pattern) + `sparse_triangular_solve` (priority #7; expected out-of-scope-obstruction). Citation: OQ `phase-1-corpus-reduction-batch-4-remaining-slices` (cycle-012) + STAGING row 8.
- **`layer-intro-author` on `plane-rotation-concept-page-canonical-pointer-repoint`** unblocked (HEADLINE) — cycle-012 batch-3 reduced the orthog plane-rotation sub-slice + plane_rotation_stream; 3 firm concept pages still cite the orthog slice as canonical and need re-pointing to the surviving canonical surfaces. Citation: OQ `plane-rotation-concept-page-canonical-pointer-repoint` (cycle-012) + STAGING row 8.
- **`lifter` on `krylov-step-theme-body-no-l3-row-drift-cycle-013`** unblocked-but-small — the cycle-012 lifter cleaned the L4/index.md:40 SUPERSEDED cross-reference, but the theme-body lines 20 + 220 in `krylov-step-typed-wrapper-dissolution.md` still phrase the cycle-006 "no L3 row needed" conclusion as live (internally reconciled by the line-218 SUPERSEDED annotation). Low-cost re-anchor. Citation: OQ `krylov-step-theme-body-no-l3-row-drift-cycle-013` (cycle-012) + STAGING row 7.
- **`lifter` / `harvester-NEP` on `eigsolve-nep-coordinate-convention-empirical-witness`** unblocked-but-low-priority — the cycle-012 SLEPc-NEP audit verdict is source-read-confirmed but empirically-unwitnessed (no `test-eigensolver.cpp` NEP case); routes a future upgrade when a NEP test case lands or a `SlepcNEPSolver` L0 entry firms up. Citation: OQ `eigsolve-nep-coordinate-convention-empirical-witness` (cycle-012, priority low) + STAGING row 4.
- **(carry-forward, still unblocked-but-large) `lifter` / `abstractor` on `gmres.md §L4 v0.6 → v0.7 self-rotation`** — would firm both the cycle-008 GMRES theme + cycle-011 FGMRES sister theme. Not selected cycle-012.
- **(carry-forward) `layer-intro-author` on L0 bundle-6 candidates #2 + #3** — cycle-009 OQ; #1 landed cycle-011; #2 + #3 still open. Not selected cycle-012.
- **(carry-forward, large) `harvester` on NLEPS at L1+** — cycle-009 OQ; carry-forward.

### New dependencies

- **L1 firm cohort 8 → 10 (cycle-012)** — `orthogonalize` (Gram-Schmidt; `dot`+`axpy` deps only; MGS/CGS/CGS2 + `dot_op` variant axes; all-three-levels absorption under residual-axis disclosure; Householder scoped out) + `chebyshev-smoother` (first fixed-degree polynomial-action L1 op; `apply_linop` direct + opaque `spectrum_estimate` deps; RATIFIED KEEP-FIRM). Firm header `(8)` → `(10)`.
- **L2 firm cohort 1 → 2 (cycle-012)** — `chebyshev-iteration` (explicit degree-`order` three-term recurrence unfolding the L1 closed-form action into `apply_linop`/`axpby`/`scal`/`elementwise_product`; the concrete L2 entry behind `L2/krylov-step` variant-axis 3; RATIFIED KEEP-FIRM in lockstep with the L1 entry). **First L2 growth since cycle-005** — directly addresses the priority #17 lower-layer-shared-vocabulary signal flagged across cycles 010/011.
- **eigsolve-mutation-rotation lowering-verifier audit (cycle-012)** — appended a machine-readable `verified_against:` YAML record; verdict confirms-with-refinement; promotion GATED to cycle-013 (see §Unblocked). New OQ `eigsolve-getconverged-forwarder-fix-and-gated-promotion`.
- **SLEPc-NEP coordinate-convention audit (cycle-012)** — resolved-with-refinement; two-mechanism finding (NEP solves un-scaled directly; gamma/delta dead store); §5 prose refined + `## Verified-against` section appended; carry-forward citation fix `arpack.cpp:387` → `:383` at `eigsolve.md:116` + `:222`.
- **4 concept pages corrected/extended (cycle-012)** — nrm2 stability-claim correction + state-stratification four-stratum / derived-view-hoisting control-flow-boundary / negative-result-slice partial-positive extensions.
- **L3 index Semantics overlay refreshed (cycle-012)** — names all 8 firm L3 operators grouped by kind; matvec (apply_linop) parenthetical reconcile.
- **L4 index SUPERSEDED drift cleaned (cycle-012)** — `L4/index.md:40` re-anchored; the integrator-signals carry-forward flag chain (cycle-010 wave-1 pass-2 META Issue 1, re-flagged cycle-011) is **RESOLVED this cycle** (see §Integration-tooling friction signal 8).
- **Phase-1 corpus reduction batch-3 (cycle-012)** — 3 reductions (orthog plane-rotation sub-slice fully reduced + plane_rotation_stream partial + divfree partial); cumulative 8/10; **first intra-corpus-redundancy verdict** (plane_rotation_stream + orthog sub-slice are the same algorithm).

### Resolution implications

- **`l1-orthogonalize-promotion-from-arnoldi-step-and-orthog`** (cycle-010 HIGH) — **resolved** (`open` → `answered`; via `book/src/L1/orthogonalize.md`).
- **`l1-l2-chebyshev-smoother-and-iteration-firm-row-promotion`** (cycle-011) — **resolved** (`open` → `answered`; via `L1/chebyshev-smoother.md` + `L2/chebyshev-iteration.md`).
- **`eigsolve-slepc-nep-coordinate-convention-audit`** (cycle-011) — **resolved-with-refinement** (`open` → `answered`). **Cycle-009 eigsolve OQ cluster now fully closed across cycles 010/011/012.**
- **`l3-index-semantics-overlay-blas1-cohort-prose-refresh`** + **`l3-index-matvec-naming-vs-apply_linop-slug`** (cycle-011) — both **resolved** (`open` → `answered`; via L3/index.md refresh).
- **`concepts-nrm2-stability-claim-correction`** (cycle-011 entry + cycle-003 duplicate) — both **resolved** (`open` → `answered`; via concepts/nrm2.md correction).
- **`concepts-state-stratification-four-stratum-extension`** + **`concepts-derived-view-hoisting-control-flow-boundary-extension`** + **`concepts-negative-result-slice-partial-positive-sub-pattern-extension`** (cycle-011) — all **resolved** (`open` → `answered`; via concept-page extensions).
- **`eigsolve-mutation-rotation` Sub-pattern B promotion** — **UNBLOCKED but GATED to cycle-013** (NOT enacted; `## Status` partly-constructive caveat retained). New OQ `eigsolve-getconverged-forwarder-fix-and-gated-promotion`.
- **L4/index.md:40 SUPERSEDED-text drift** (carry-forward flag chain, cycle-010/011) — **resolved** by the cycle-012 lifter; residual theme-body line-20/220 drift routed to OQ `krylov-step-theme-body-no-l3-row-drift-cycle-013`.
- **17 new OQs opened + 8 closed/resolved cycle-012**; net ledger growth modest given the batch-closing cleanup emphasis.
- **Cycle-012 meta-phase batch-2 aggregation fires after this finalize commit** per 3:1 cadence.

### Suggested next dispatches

- **(GATED cycle-013 — highest-priority)** `abstractor` on `eigsolve-getconverged-forwarder-fix-and-gated-promotion` — applies audit Edits 2+3, then drops the partly-constructive caveat per the theme's own promotion gate (b). The dispatch prompt MUST state the cycle number explicitly as cycle-013 (see §Integration-tooling friction signal 8 — per-report cycle-mislabel).
- **(HEADLINE harvester)** `l1-divfree-projector-promotion` — 6 firm entries cite the now-reduced divfree slice; landing unblocks further divfree reduction.
- **(harvester)** L3 + L4 chebyshev rows (`l3-l4-chebyshev-rows-eligible`) — unblock full `chebyshev.md` reduction per identity-lowerings-both-levels.
- **(abstractor)** `orthogonalize-mutation-rotation` (L1>L0) + `chebyshev-smoother-mutation-rotation` / `chebyshev-iteration-fusion` lowering themes.
- **(same-layer-cross-cutter)** phase-1 corpus reduction batch-4 — final 2 slices (cg_preconditioning_framework + sparse_triangular_solve).
- **(HEADLINE layer-intro-author)** `plane-rotation-concept-page-canonical-pointer-repoint` — 3 firm concept pages still cite the orthog slice as canonical.
- **(lifter, small)** `krylov-step-theme-body-no-l3-row-drift-cycle-013` — theme-body line-20/220 re-anchor.
- **(lifter, large carry-forward)** `gmres.md §L4 v0.6 → v0.7 self-rotation` — firms both GMRES + FGMRES sister themes.
- **(layer-intro-author)** L0 bundle-6 candidates #2 + #3 — carry-forward.
- **(abstractor / lifter)** `slepc-convergence-reason-lift-sub-theme` — carry-forward from cycle-011.
- **(harvester)** `spectrum_estimate` L1 rough-in (`spectrum_estimate-l1-rough-in-opacity`) — shares the matrix-weighted-norm cohort.
- **(harvester, large)** NLEPS at L1+ — carry-forward.
- **CRITICAL cycle-012 meta-phase batch-2 aggregation targets** (batch-2 = cycles 010/011/012 — see the 10 enumerated signals in §Wave-conflict observations + §Integration-tooling friction below):
  - **`l3-l1-directory-naming-structure-policy` closure DECISION** — cumulative in-line identity-rotation count now ~9+ (BLAS-1 cohort all in-line); exceeds the revisit threshold. Meta-phase DECISION item: codify the in-line convention via a `layer-intro-author` role-spec edit OR introduce a `book/src/L3-L1/` directory and back-fill the in-line annotations into thin theme files. The former is the recommended default per the cohort's empirical convergence.
  - **`phase-1-slice-reduction-audit` skill promotion at recurrence-3 + severity escalation** — STRONG promotion candidate; cycle-012 batch-3 carried a HIGH-severity line-map defect (END-but-not-START boundary verification → mis-scoped sub-slice). The mitigation is mechanical and machine-replayable across all 3 batch instances.
  - **Skill-uptake-survey gap CYCLE-WIDE** — all 8 cycle-012 reports lacked explicit `verify-citation-range` / `classify-variant-axis` / `verify-refinement-surface` invocation; 3-cycle strengthening pattern; friction-ledger / skill-uptake codification candidate.
  - **Write-authority phase-boundary violation (layer-intro-author, recurrence-1)** — prompt-guard candidate.
  - **Per-report-integrator cycle-mislabeling (NEW, recurrence-1)** — integrator-per-report prompt guard candidate (always state the cycle number explicitly in the dispatch prompt).
  - **`partly-constructive-lowering-theme-status` at recurrence-2/3** — codification candidate (eigsolve theme authored cycle-011, audited cycle-012, promotion-gated to cycle-013).
  - **`negative-anchor-citation-pattern` at recurrence-2/3** — codification candidate (eigsolve constructive cases).
  - **`lifter-scope-content-correction-boundary` at recurrence-2/3** — clarification candidate (eigsolve OQ-cluster + cycle-012 audits doing L0-evidence-driven prose corrections).
  - **`dispatch-prompt-framing-drift` at recurrence-3** — friction-ledger candidate; the cycle-012 PLANNER itself cited `palace/eigensolver/slepc.cpp` (wrong; correct is `palace/linalg/slepc.cpp`); orchestrator corrected in briefs. Same family as cycle-010 (eps.cpp/feast.cpp) + cycle-011 (Solver<OperType>).
  - **`mcp-codemap-permission-denied-across-batch-1` friction-ledger resolution** — MCP codemap used routinely cycle-012 with 0 permission-denied; pattern stable; meta-phase enacts the ledger entry resolution (integrator only signals per CLAUDE.md §Write-authority partition).
  - **L1 cohort frontmatter divergence cleanup** — carry-forward (matrix-weighted-norm no frontmatter; bilinear-form 8-field frontmatter; no normalization happened cycle-012).
  - **L2 cohort growth signal ADDRESSED** — chebyshev-iteration is the first L2 growth since cycle-005; the cycle-010/011 priority #17 signal is partially discharged.

### Wave-conflict observations

1. **8-report single-wave dispatch.** All 8 applied as-is at integration; zero rework loops; no deferrals/rejections.
2. **`scaffolding/open-questions.md` touched 8 times** at distinct line ranges with zero collisions; append-before-Dropped convention held. **Pattern reaches recurrence-8** (cycles 005–012).
3. **`book/src/L1/index.md` touched twice** (orthogonalize then chebyshev): the chebyshev landing re-read disk and adapted the Firm-count transition to `(9)→(10)` instead of the proposed `(8)→(9)`, placing rows after the `orthogonalize` rows (which did not exist when the harvester wrote the proposed-change). Clean re-read-disk adaptation, no merge conflict — validates the per-report re-read-before-edit discipline.
4. **`book/src/SUMMARY.md` touched twice**; index files (L1 ×2, L2 ×1, L3 ×1, L4 ×1) each re-read disk before edit; no simultaneous-index collision needing re-merge beyond the L1 Firm-count adaptation. **Per-report integrator cycle-mislabeling (NEW, recurrence-1)**: report #3's integrator mis-filed its staging row to a `reports/cycle-013-integrator-staging/` directory; the orchestrator corrected (relocated the row to cycle-012 STAGING, removed the misplaced directory, fixed backward `cycle-013` refs in `book/src/L1-L0/eigsolve-mutation-rotation.md` + the OQ `opened_at`); forward-references to the GATED cycle-013 abstractor follow-up intentionally retained. Finalize verified via `grep -rn cycle-013 book/src/ scaffolding/open-questions.md` that the only remaining cycle-013 refs are forward-references to the gated dispatch (4 refs: the krylov-step-theme-body OQ slug, the batch-4 scheduling note, the eigsolve gated-promotion OQ routing, the eigsolve-mutation-rotation OQ body).
5. **Write-authority phase-boundary violation (layer-intro-author, recurrence-1)**: report #6 (concept-corrections) wrote 4 edits directly to `book/` during the dispatch phase; critic caught (HIGH), repairer reverted to HEAD (Option A), integrator-per-report applied normally from proposed-changes blocks. Caught + repaired within-cycle before integration — clean-run streak preserved at the integration boundary (8 consecutive clean integration cycles 005–012), but a phase-discipline recurrence worth a meta-phase prompt-guard.
6. **No deferrals, no rejections, no rework loops.** All 8 reports applied as-is.

### Integration-tooling friction

The 10 enumerated forward signals for meta-phase batch-2 aggregation (cycles 010/011/012). These are the meta-phase's primary input.

1. **Write-authority phase-boundary violation (recurrence-1)** — concept-corrections layer-intro-author (report #6) wrote to `book/` during the dispatch phase; critic caught, repairer reverted (Option A), integrator re-applied. **Meta-phase candidate: layer-intro-author prompt-level guard** (explicit "do NOT touch `book/` — emit proposed-changes blocks only" reinforcement in the agent spec).
2. **`phase-1-slice-reduction-audit` skill candidate at recurrence-3 + severity escalation** — the batch-3 audit verified the END boundary but not the START, causing a HIGH-severity line-map defect (mis-scoped the orthog plane-rotation sub-slice as starting at 311/317 when it starts at 225; repaired pre-apply by the repairer correcting to the full text-anchored 225-376 span). STRONG skill-promotion candidate for meta-phase batch-2; the mitigation is mechanical ("verify a sub-slice's START boundary, not just its END; reduction header notes should use section-heading text-anchors, not absolute line numbers") and machine-replayable across all 3 batch instances.
3. **Skill-uptake-survey gap CYCLE-WIDE** — all 8 cycle-012 reports lacked explicit `verify-citation-range` / `classify-variant-axis` / `verify-refinement-surface` skill invocation despite applicability; the slepc-nep critic explicitly noted the `:387` citation drift would likely have been caught by `verify-citation-range`. Strengthening 3-cycle pattern (cycle-010 + cycle-011 + cycle-012). Friction-ledger / skill-uptake aggregation candidate.
4. **Partly-constructive lowering theme-status at recurrence-2/3** — the eigsolve theme (cycle-011 authored, cycle-012 audited; promotion gated to cycle-013). Codification candidate (same gate-mechanism as the cycle-010 lifter's L1-constructive option (b)).
5. **Negative-anchor citation pattern at recurrence-2/3** — the eigsolve constructive cases (LinearSolveFailed cycle-010, EigResult.iterations cycle-011, the cycle-012 audit's negative anchors). Codification candidate; distinguishable from per-operator obstruction-theme negative-anchors.
6. **Lifter-scope content-correction boundary at recurrence-2/3** — the eigsolve OQ-cluster (cycle-011) + cycle-012 audits doing L0-evidence-driven prose corrections (the SLEPc-NEP §5 refinement + the `:387`→`:383` carry-forward fixes). Clarification candidate (codify as in-scope allowance OR formalize the abstractor-reread re-routing path).
7. **Dispatch-prompt framing drift at recurrence-3** — the cycle-012 PLANNER itself cited `palace/eigensolver/slepc.cpp` (wrong; the correct path is `palace/linalg/slepc.cpp`); the orchestrator corrected in the briefs. Same family as cycle-010 (eps.cpp/feast.cpp non-existent) + cycle-011 (Solver<OperType>-as-direct-solver). Friction-ledger candidate at recurrence-3.
8. **Per-report integrator cycle-mislabeling (NEW, recurrence-1)** — report #3's per-report integrator mis-filed its staging row to a `reports/cycle-013-integrator-staging/` directory (mislabeled the cycle); the orchestrator corrected. **Possible integrator-per-report prompt guard candidate**: the dispatch prompt should always state the cycle number explicitly so the per-report integrator writes to the correct staging directory. ALSO in this signal slot: the **L4/index.md:40 SUPERSEDED carry-forward flag chain** (cycle-010 wave-1 pass-2 META Issue 1, re-flagged cycle-011 lines 58/91/150/184 of the prior integrator-signals state) is **RESOLVED** by the cycle-012 lifter (report #7) — recorded here per the STAGING row 7 note (per-report integrator cannot write integrator-signals; finalize-only authority); the residual theme-body line-20/220 drift is routed to OQ `krylov-step-theme-body-no-l3-row-drift-cycle-013`.
9. **`l3-l1-directory-naming-structure-policy`** — cumulative in-line identity-rotation count now ~9+ (BLAS-1 cohort all in-line; exceeds the revisit threshold of 6). Meta-phase **DECISION** item: codify the in-line convention OR introduce a `book/src/L3-L1/` directory.
10. **L3/L4 chebyshev rows + L1/divfree-projector + orthogonalize-mutation-rotation theme** — cycle-013 harvester/abstractor candidates (unblock further slice reductions). Enumerated in §Suggested next dispatches.

**Additional housekeeping note**: `scaffolding/integrator-signals.md` is now ~900 lines (over the ~500-line soft cap). Per the file's own discipline ("entries older than 10 cycles archive"), no archiving is yet due — cycles 002–012 are all within the 10-cycle window. Once cycle-013 lands, cycle-002/003 era entries become archive-eligible to `scaffolding/integrator-signals-archive/`. Flagging for the cycle-013 finalize (or meta-phase) to action.

---

## cycle-011 — 2026-05-28T010000Z

**Meta-batch context**: **second primary cycle of meta-batch-2** under the 3:1 meta cadence (cycles 010/011/012 form batch-2; meta-phase fires after cycle-012 finalize, NOT after this cycle). Cycle counter does not reset at batch boundaries. Cycle-011 enacted priority #20 continuation (identity-lowering-both-levels-backfill BLAS-1 cohort closure) as wave-1 lead work + priority #19 second instance (phase-1-corpus-reduction batch-2) as wave-2 closing work + 4 eigsolve-related cycle-009/010 OQs in 2 wave-2 dispatches (eigsolve-mutation-rotation L1>L0 theme + cycle-009 OQ cluster closure) + first L4>L3 rough-in sister-theme (FGMRES) + L0 bundle-6 candidate #1 (linalg-solver-file). Priority #13 closed out-of-band by cycle-planner at cycle start.

### Unblocked

- **`harvester` / `cross-layer-cross-cutter` / `meta-phase` on `l3-l1-directory-naming-structure-policy` closure** unblocked — cumulative in-line identity-rotation count reaches **7** (apply_linop + axpy + axpby + axpbypcz + dot + nrm2 + scal) — **exceeds** the cycle-010 OQ's earlier-stated revisit threshold of 6. Cycle-012 meta-phase is the natural decision point: codify in-line convention via `layer-intro-author` role-spec edit, vs back-fill 7 in-line annotations into thin `book/src/L3-L1/` directory theme files. Strong candidate for closure cycle-012 meta-phase. Citation: STAGING rows 1-4 + integrator merge of `l3-l1-identity-in-form-annotation-policy-formalization` into the cycle-010 OQ.
- **`abstractor` / `lifter` on `scal-mutation-rotation` L1>L0 theme** unblocked — cycle-011 wave-1 pass-4 surfaced new OQ `scal-mutation-rotation-l1-l0-theme`; analogous to existing firm `axpby-mutation-rotation` / `axpbypcz-mutation-rotation` themes; routable to future abstractor / lifter dispatch.
- **`layer-intro-author` on `l3-index-semantics-overlay-blas1-cohort-prose-refresh`** unblocked-but-small — cycle-011 wave-1 pass-4 surfaced new OQ for L3 index `## Semantics (overlay)` prose at `book/src/L3/index.md:11-15`; the prose lists only "matvec, axpy, dot, nrm2 as field operations" but does not literally name `scal` / `axpby` / `axpbypcz` / `apply_linop` (now fully reflected in the dep-map table below). Smallest-cost cycle-012 cleanup.
- **`layer-intro-author` / `same-layer-cross-cutter` on `concepts-nrm2-stability-claim-correction`** unblocked — cycle-011 wave-1 pass-3 surfaced new concrete-correction OQ for `book/src/concepts/nrm2.md:8-9` false stability claim that contradicts the L1 entry's authoritative finding; routable to future layer-intro-author / cross-cutter dispatch.
- **`lifter` / `lowering-verifier` / `harvester-NEP` on `eigsolve-slepc-nep-coordinate-convention-audit`** unblocked — cycle-011 wave-2 pass-7 repairer surfaced new OQ; concrete follow-up audit on SLEPc-NEP coordinate-convention gap (gamma computed at SetOperators :1645-1651, :1711-1719 but not applied at GetEigenvalue :1554-1560; "NEP gamma = 1" reading is wrong). Routable to cycle-012+ lifter / lowering-verifier / harvester-NEP dispatch on slepc.cpp:1554-1719.
- **`abstractor` / `lifter` on `slepc-convergence-reason-lift-sub-theme`** unblocked — cycle-011 wave-2 pass-6 surfaced sub-theme candidate covering the full SLEPc `EPSConvergedReason` → `EigStatus` mapping table (including `EPS_DIVERGED_BREAKDOWN` / `EPS_DIVERGED_SYMMETRY_LOST` → `LinearSolveFailed`); relates_to firm parent theme + cycle-009 `eigsolve-iteration-count-result-field` (resolved cycle-011).
- **`lowering-verifier` on `eigsolve-mutation-rotation-lowering-verifier-followup`** unblocked — cycle-011 wave-2 pass-6 surfaced per-line lowering-verifier audit OQ; deferred from structural-level recognition of the firm theme; routable to cycle-012+ lowering-verifier dispatch to walk each backend body and confirm the 4 sub-pattern rewrites are exhaustive at the per-step level.
- **`cross-layer-cross-cutter` (L2) / `abstractor` (L4) on `eigsolve-driver-side-double-solve-composition`** unblocked — cycle-011 wave-2 pass-6 surfaced new OQ for driver-side `eigensolver.cpp:377-407` higher-level composition pattern (linear eigensolve's result threaded as initial-condition for QuasiNewton refinement); flagged out-of-scope at L1; routable to future same-layer-cross-cutter at L2 or abstractor at L4 to formalise the bind-shape composition pattern.
- **`same-layer-cross-cutter` on phase-1 corpus reduction batch-3** unblocked — cycle-011 wave-2 pass-9 batch-2 landed; OQ `phase-1-corpus-reduction-remaining-7-slices` (cycle-010) now has 4 remaining slices: divfree + cg_preconditioning_framework + plane_rotation_stream + sparse_triangular_solve. Audit template established and machine-replayable across both batches.
- **`harvester` on `l1-l2-chebyshev-smoother-and-iteration-firm-row-promotion`** unblocked — cycle-011 wave-2 pass-9 surfaced new OQ for the chebyshev.md reduction-blocker firm-row promotion; landing would unblock further reduction of `chebyshev.md` (currently retains L1/L2/L3/L4 sections pending the firm-row promotion).
- **`layer-intro-author` / `same-layer-cross-cutter` on 3 concept-page extension OQs** unblocked — cycle-011 wave-2 pass-9 surfaced 3 new concept-page-extension OQs: `concepts-state-stratification-four-stratum-extension` + `concepts-derived-view-hoisting-control-flow-boundary-extension` + `concepts-negative-result-slice-partial-positive-sub-pattern-extension`. All routable to future layer-intro-author / cross-cutter dispatch.
- **`harvester` on `l1-orthogonalize`** unblocked-but-gated (carry-forward) — cycle-010 wave-2 pass-8 OQ; cycle-011 wave-2 pass-9 amendment notes the orthog slice is now reduced (2-slice-blocker status from arnoldi_step.md + orthog.md); landing would unblock further reduction of remaining `arnoldi_step.md` + `orthog.md` content. Priority-weight upgrade for batch-2-or-3 harvester.
- **`lifter` on `book/src/L4/index.md:40` SUPERSEDED-text drift** still unblocked-but-small (carry-forward) — cycle-010 OQ; not selected by cycle-011 planner. Smallest-cost cycle-012 cleanup.
- **`abstractor` (or `lifter`) on upstream `gmres.md §L4 v0.6 → v0.7 self-rotation`** still unblocked-but-large (carry-forward) — cycle-008 OQ; would firm both cycle-008 GMRES theme + cycle-011 FGMRES sister theme.
- **`layer-intro-author` on L0 bootstrap bundle-6 candidates #2 + #3** still unblocked (carry-forward) — cycle-009 OQ `l0-bundle-6-candidates` partially-answered cycle-011 (candidate #1 landed); #2 + #3 still open.
- **`harvester` on NLEPS at L1+** still unblocked-but-large (carry-forward) — cycle-009 OQ.

### New dependencies

- **L3 BLAS-1 cohort closure (wave-1 passes 1-4, cycle-011)** — **7 firm L3 operators landed in one cycle** as 4 cohort-bundle harvester dispatches: `apply_linop` (pass 1; first primitive-flavored L3 backfill), `axpy` + `axpby` + `axpbypcz` (pass 2; linear-update cohort), `dot` + `nrm2` (pass 3; reduction cohort with same-layer L3 dependency `nrm2(x) = √dot(x, x)`), `scal` (pass 4; closes cohort). L3 firm-operator count: 1 → 8. **Fully closes the BLAS-1 cohort portion of OQ `l3-backfill-apply-linop-and-blas1-cohort`** (HIGH CONFIDENCE recommendations from cycle-010 cross-layer-cross-cutter audit); priority #20 second target fully met. All 8 L3 entries follow the cycle-010 wave-1 L3/krylov-step.md structural template (frontmatter, section order, identity-in-form annotation, subsumption-chain preservation). L3 cohort frontmatter uniformly with-frontmatter at 8/8.
- **L4>L3 `fgmres-inner-loop-iterate-while-migration` rough-in (wave-2 pass 5, cycle-011)** — **first L4>L3 rough-in sister-theme in the artifact**; sibling to cycle-008 firm-rough-in `gmres-inner-loop-iterate-while-migration`. FGMRES variant-axis collapses (`pc_side = RIGHT`, `flexible = true`); textually-identical `:823-828` break-site finding; 6 applicability conditions (5 inherited from sibling + 1 FGMRES-specific). L4>L3 layer transitions from 2-theme to 3-theme status (krylov-step-typed-wrapper-dissolution firm + gmres-inner-loop-iterate-while-migration rough-in + fgmres-inner-loop-iterate-while-migration rough-in). Both rough-in themes pending upstream `gmres.md §L4 v0.6→v0.7` self-rotation; firm-promotion of `check_stop_into_carry` remains deferred per `nleps-spec-gap-as-check-stop-into-carry-reuse-blocker`. **Closes cycle-010 OQ `fgmres-inner-loop-iterate-while-migration-lifter-candidate`** (status `open` → `answered-by-rough-in-theme`).
- **L1>L0 `eigsolve-mutation-rotation` theme (wave-2 pass 6, cycle-011)** — **second L1>L0 mutation-rotation theme for a structured opaque primary argument** (after cycle-008 firm `ksp-solve-mutation-rotation`). **First firm-structural-but-partly-constructive theme in artifact** — Sub-pattern B's `LinearSolveFailed` materialisation is partly-constructive pending upstream Palace refactor OR lowering-verifier audit; the rest is `firm structural`. 4-sub-pattern decomposition (A setup / B inner-solve mutation-rotation / C result-status flow / D teardown); exhaustively cites 10 `opInv->Mult` callsites across ARPACK (4) + NLEPS (1) + SLEPc shell (5). L1>L0 layer transitions from 4-theme/2-obstruction to 5-theme/2-obstruction status.
- **Cycle-009 eigsolve OQ cluster fully closed (wave-2 pass 7, cycle-011)** — 7 in-place edits to `book/src/L1/eigsolve.md` + 6 in-place edits to `scaffolding/open-questions.md` + 1 auto-promoted new OQ. Resolves 3 of the 4 cycle-009 eigsolve OQs in one report (the 4th was already resolved cycle-010 via partial-answer): `eigsolve-scaling-coordinate-convention` (rewrote §5 from incorrect convention-(a) to correct convention-(b) with 5 backend-specific un-scaling citations) + `eigsolve-initial-space-axis-placement` (eps.hpp:122 method-on-eigensolver-value + driver-side call-sites + ordering invariant SetOperators → SetInitialSpace → Solve) + `eigsolve-iteration-count-result-field` (added `iterations : Int` field as L1-constructive with negative-anchor citation set parallel to cycle-010 LinearSolveFailed callout pattern). New follow-up OQ `eigsolve-slepc-nep-coordinate-convention-audit` opened by repairer. L1/eigsolve.md status: `rough-in (test-coverage-bounded, cycle-009; LinearSolveFailed-constructive resolved cycle-010; variant-axis & result-record inventory closed cycle-011)`.
- **L0 chapter count 16 → 17 (wave-2 pass 8, cycle-011)** — `linalg-solver-file` lands as L0 bundle-6 candidate #1. Closes cycle-009 OQ `l0-bundle-6-candidates` (status `open` → `partially-answered`; #2 + #3 still open). Chapter corrects dispatch-prompt framing: `Solver<OperType>` is type-axis root of ALL Palace solvers (not direct-solver-only base); 8 concrete subclass families enumerated.
- **Phase-1 corpus reduction batch-2 (wave-2 pass 9, cycle-011)** — 3 slices reduced: orthog.md (L0→L1 Gram-Schmidt body replaced with reduced stub); chebyshev.md (Consumers / Open-questions / Concept-references condensed; L1/L2/L3/L4 sections retained pending firm-row promotion); polynomial_recurrence_step.md (**first negative-result slice audited** — verdict `blocked / minimal reduction; the slice IS the artifact`; 2 narrow forward-pointers added; load-bearing distinction catalog retained verbatim). Cumulative slice-corpus coverage 6 of 10; 4 remaining for batch-3+.
- **Priority #13 closed out-of-band by cycle-planner at cycle start** — cycle-010 wave-2 pass-5 surfaced routing OQ `priority-13-now-landed-as-matrix-weighted-norm` (status `routing`); cycle-011 cycle-planner read OQs alongside priorities at cycle start and executed the close. **First instance of cycle-planner enacting an integrator-flagged priority close out-of-band** (typical pattern is meta-phase enactment).

### Resolution implications

- **`fgmres-inner-loop-iterate-while-migration-lifter-candidate`** (cycle-010) — **resolved** (`open` → `answered-by-rough-in-theme`; resolved cycle-011 via wave-2 pass-5 lifter rough-in landing; disposition analogous to cycle-008 GMRES rough-in).
- **`eigsolve-scaling-coordinate-convention`** (cycle-009) — **resolved** (`open` → `resolved`; resolved cycle-011 via wave-2 pass-7 lifter §5 rewrite with 5 backend-specific un-scaling citations).
- **`eigsolve-initial-space-axis-placement`** (cycle-009) — **resolved** (`open` → `resolved`; resolved cycle-011 via wave-2 pass-7 lifter with eps.hpp:122 evidence).
- **`eigsolve-iteration-count-result-field`** (cycle-009) — **resolved** (`open` → `resolved`; resolved cycle-011 via wave-2 pass-7 lifter with L1-constructive `iterations` field + negative-anchor callout pattern). **Cycle-009 4-OQ eigsolve cluster fully closed**.
- **`l0-bundle-6-candidates`** (cycle-009) — **partially-answered** (status flipped `open` → `partially-answered`; `partial_answer_at: cycle-011`). Wave-2 pass-8 landed candidate #1 (linalg-solver-file); #2 + #3 still open.
- **`l3-backfill-apply-linop-and-blas1-cohort`** (cycle-010) — **fully met for BLAS-1 portion** via cycle-011 wave-1 cohort closure (4 dispatches; 7 firm L3 entries). The OQ scope was specifically `apply_linop` + 6-entry BLAS-1 cohort; the 7th entry `scal` was the closing entry. Priority #20 second target fully met.
- **`l3-l1-directory-naming-structure-policy`** (cycle-010) — **status updates × 4 partial-precedent paragraphs + 1 merge** from cycle-011 wave-1; cumulative in-line identity-rotation count now at **7** (exceeds the OQ's earlier-stated revisit threshold of 6); merged the report-proposed sibling OQ `l3-l1-identity-in-form-annotation-policy-formalization` into this existing OQ per integrator's policy-merge discretion. **Strong candidate for cycle-012 meta-phase closure decision.**
- **`l1-orthogonalize-promotion-from-arnoldi-step-and-orthog`** (cycle-010) — **amendment** with cycle-011 batch-2 update (2-slice-blocker status: arnoldi_step.md + orthog.md; 5-firm-entry citation count; priority-weight upgrade for batch-2-or-3 harvester).
- **12 new OQs opened cycle-011** (pass 1: 1; pass 3: 1; pass 4: 2; pass 6: 3; pass 7: 1 — opened by repairer; pass 9: 5). **4 resolutions + 1 partial-answer flip**. **Net ledger change: +7 OQs.** Cycle-011 emphasized OQ closure (4 resolutions) over OQ generation (12 new vs cycle-010's 18 new) — first cycle to see ≥4 OQ resolutions.
- **Cycle-012 meta-phase batch-2 aggregation** fires after cycle-012 finalize per 3:1 cadence. Critical aggregation targets surfaced this cycle (see §Suggested next dispatches below).

### Suggested next dispatches

- **(CYCLE-012 meta-phase candidate — highest-priority)** close cycle-010 OQ `l3-l1-directory-naming-structure-policy` — cumulative in-line identity-rotation count = 7 exceeds threshold of 6; codify in-line convention via `layer-intro-author` role-spec edit OR introduce `book/src/L3-L1/` directory and back-fill 7 in-line annotations into thin theme files. The former is the recommended default per the cohort's empirical convergence; the latter is a non-trivial migration.
- **(CYCLE-012 candidate — same-layer-cross-cutter)** phase-1 corpus reduction batch-3 — 4 remaining slices (divfree + cg_preconditioning_framework + plane_rotation_stream + sparse_triangular_solve); per cycle-010 OQ `phase-1-corpus-reduction-remaining-7-slices` priority order; batch-size suggestion 2-4 slices. Cycle-011 batch-2 verdicts available as precedent (especially polynomial_recurrence_step.md negative-result-slice verdict).
- (`lifter`, `gmres.md §L4 v0.6 → v0.7 self-rotation`) — large dispatch carry-forward; would firm both cycle-008 GMRES theme + cycle-011 FGMRES sister theme.
- **(CYCLE-012 smallest-cost) (`lifter`, `book/src/L4/index.md:40` SUPERSEDED-text drift)** — cycle-010 wave-1 pass-2 META Issue 1; single-edit cleanup pointing at the cycle-010 wave-1 pass-1 L3 backfill enactment. Carry-forward from cycle-010.
- (`harvester`, `l1-orthogonalize`) — small candidate per cycle-010 wave-2 pass-8 routing OQ; landing unblocks further `arnoldi_step.md` + `orthog.md` reduction. Cycle-011 wave-2 pass-9 amendment notes 2-slice-blocker status and priority-weight upgrade.
- (`harvester` / `cross-layer-cross-cutter`, L2 cohort growth — priority #17) — no L2 entries added cycle-011 despite priority #17 (lower-layer-shared-vocabulary). Cycle-012 candidate.
- (`layer-intro-author`, L0 bundle-6 candidates #2 + #3) — carry-forward from cycle-009 OQ; #1 landed cycle-011.
- (`lowering-verifier`, `eigsolve-mutation-rotation-lowering-verifier-followup`) — per-line audit of the cycle-011 firm-structural-but-partly-constructive theme; same approach as cycle-008 `ksp-solve-mutation-rotation` firm-promotion which deferred per-line verification.
- (`abstractor` / `lifter`, `slepc-convergence-reason-lift-sub-theme`) — sub-theme covering the full SLEPc `EPSConvergedReason` → `EigStatus` mapping; relates_to firm parent theme + cycle-009 `eigsolve-iteration-count-result-field` (resolved cycle-011).
- (`lifter` / `lowering-verifier` / `harvester-NEP`, `eigsolve-slepc-nep-coordinate-convention-audit`) — concrete follow-up audit on SLEPc-NEP coordinate-convention gap; opened by cycle-011 repairer.
- (`harvester`, `scal-mutation-rotation` L1>L0 theme) — concrete-gap OQ for the missing firm `book/src/L1-L0/scal-mutation-rotation.md` theme; analogous to existing firm `axpby-mutation-rotation` / `axpbypcz-mutation-rotation`.
- (`layer-intro-author`, `l3-index-semantics-overlay-blas1-cohort-prose-refresh`) — concrete-refresh OQ for L3 index `## Semantics (overlay)` prose at `book/src/L3/index.md:11-15`.
- (`layer-intro-author` / `same-layer-cross-cutter`, `concepts-nrm2-stability-claim-correction`) — concrete-correction OQ for `book/src/concepts/nrm2.md:8-9` false stability claim.
- (`harvester`, NLEPS at L1+) — large multi-cycle dispatch; carry-forward; would unblock several downstream eigsolve-related OQs.
- **CRITICAL cycle-012 meta-phase batch-2 aggregation targets** (batch-2 = cycles 010/011/012):
  - **`l3-l1-directory-naming-structure-policy` closure decision** — count = 7 exceeds threshold; strong candidate for cycle-012 meta-phase closure decision (codify in-line OR introduce thin `book/src/L3-L1/` directory).
  - **`negative-anchor-citation-pattern` at recurrence-2** (cycle-010 lifter LinearSolveFailed + cycle-011 lifter EigResult.iterations) — codification candidate; distinguishable from existing per-operator obstruction-theme negative-anchor pattern. Same negative-anchor-at-per-status-variant-granularity pattern at recurrence-2.
  - **`partly-constructive-lowering-theme-status` at recurrence-2** (cycle-010 lifter's L1-constructive option (b) + cycle-011 abstractor's eigsolve-mutation-rotation Sub-pattern B; same gate-mechanism: structural recognition with deferred materialisation pending upstream Palace refactor OR lowering-verifier audit) — codification candidate.
  - **`lifter-scope-content-correction-boundary` at recurrence-2** (cycle-010 lifter L0-evidence-driven prose tightening + cycle-011 lifter Edit 3 §5 rewrite from incorrect convention-(a) to correct convention-(b); both borderline content-correction tensions, defensible by L0 evidence) — clarification candidate; codify as in-scope allowance or formalise abstractor-reread re-routing path.
  - **`dispatch-prompt-framing-drift` at recurrence-2** (cycle-010 planner cited eps.cpp/feast.cpp (non-existent) + cycle-011 cycle-planner brief framed `Solver<OperType>` as direct-solver-only base; both corrected by MCP localization + source-truth reading; pattern is the cycle-planner's source-file framing summary diverging from source truth on linalg/* file overviews) — friction-ledger candidate.
  - **`skill-uptake-survey gap` continuing** — telemetry pattern across multiple recent dispatches (cycle-010 wave-2 pass-6 + cycle-010 wave-2 pass-7 + cycle-011 wave-2 pass-7 + cycle-011 wave-2 pass-8); codify as friction-ledger entry candidate.
  - **Coordination gap**: wave-2 abstractor's eigsolve L1>L0 theme references older `EigResult` field list (no `iterations`); wave-2 lifter added `iterations` field to L1/eigsolve.md. Co-existence structurally OK (mdBook does not cross-validate inter-chapter field consistency); cycle-012 lifter / lowering-verifier pass should reconcile.
  - **L1 cohort frontmatter divergence cleanup** continuing — L1 cohort still mixed post-cycle-011 (no frontmatter normalization happened cycle-011); future-normalization candidate carry-forward from cycle-010.
  - **`phase-1-slice-reduction-audit` skill candidate** — pre-existing entry at `scaffolding/skill-candidates.md`; cycle-011 batch-2 reinforces the case for cycle-012 meta-phase batch-2 promotion (the first-instance template from cycle-010 batch-1 + the second-instance template from cycle-011 batch-2 are detailed and machine-replayable across both instances).
  - **`phase-1-corpus-audit-line-range-arithmetic-brittleness` friction at recurrence-2** — cycle-010 batch-1 + cycle-011 batch-2 both showed the same drift pattern; mitigation (H2 enumeration via `grep -n` before line-range arithmetic) was successfully applied both cycles; cycle-012 meta-phase batch-2 codifies as `addressed` status when aggregating this 2-cycle pattern.
  - **`mcp-codemap-permission-denied-across-batch-1` friction-ledger entry resolution** — cycle-011 dispatches used MCP routinely with 0 permission-denied; pattern stable post-cycle-010 MCP pilot SUCCESS; cycle-012 meta-phase enacts the ledger entry resolution (integrator only signals per CLAUDE.md §Write-authority partition).
  - **Index-placeholder displacement pattern formalization** — now 5 instances cumulative through cycle-010; cycle-011 had 0 instances (L3/index.md was already populated post-cycle-010 wave-1); cycle-012 meta-phase formalization recommended.
  - **`variant-absorption-vs-instance-counting-policy` codification** — cycle-009 OQ; cycle-011 wave-2 pass-5 FGMRES rough-in is the second data point (GMRES + FGMRES absorbed at L1 via single `ksp_solve` slice; counted as 2 distinct themes at L4>L3); strong cycle-012 meta-phase codification target.
  - **L2 cohort growth signal** — no L2 entries added cycle-011 despite priority #17 (lower-layer-shared-vocabulary); cycle-012 candidate for either targeted dispatch OR re-balancing priority weights.
  - **Per-slice retroactive-budget recurrence-2 on eigsolve** — cycle-010 + cycle-011 both hit; if cycle-012 lifter / lowering-verifier on eigsolve adds another retroactive-budget burst, it would trip the ≥3 block-and-defer threshold. Cycle-012+ planner should consider whether eigsolve has reached a settling point that warrants no further retroactive backfill until firm-promotion.

### Wave-conflict observations

- **4-wave-1 + 5-wave-2 split** (9 dispatches total) — **largest dispatch shape under the 12-cap to date** (cycle-008 was 5+2 = 7; cycle-010 was 5+3 = 8; cycle-011 is 4+5 = 9). All 9 reports applied as-is at integration; zero rework loops. Continues the validation of 4+N / 5+N wave shape under split integrator.
- **`scaffolding/open-questions.md` touched 9 times** at distinct line ranges (passes 1-9 all promoted/updated OQs). Zero collisions; append-before-Dropped convention held cleanly per-OQ YAML status authority. **Pattern reaches recurrence-7** (cycles 005/006/007/008/009/010/011).
- **`book/src/SUMMARY.md` touched 5 times** across the 9 staging entries (passes 1/2/3/4/5/6/8 — 7 inserts across 5 of the 9 staging entries; passes 7+9 not touching). 7-cycle pattern continues.
- **`book/src/L3/index.md` touched 4 times** (passes 1/2/3/4) with sibling-coordination clean across the 4 wave-1 BLAS-1 cohort dispatches; each pass re-read disk before edits to see prior staging entries' landings. Largest single-cycle touch-count for L3/index.md.
- **`book/src/L1/eigsolve.md` touched 1 time across 7 edits** (pass 7) — 7 in-place serial edits with anchors matched exactly on disk; the §Algebraic-laws §5 rewrite is the most substantive change (content correction rather than re-anchor per the lifter-scope content-correction boundary tension flag).
- **MCP codemap used routinely** in multiple dispatches with **0 permission-denied** across all uses — pattern stable post-cycle-010 MCP pilot SUCCESS. Friction-ledger entry `mcp-codemap-permission-denied-across-batch-1` remains resolution-candidate for cycle-012 meta-phase enactment.
- **Coordination gap on eigsolve between wave-2 abstractor and wave-2 lifter**: wave-2 abstractor's `eigsolve-mutation-rotation` L1>L0 theme references the older `EigResult` field list (no `iterations`); wave-2 lifter on the eigsolve-OQ-cluster adds `iterations` to L1/eigsolve.md. **Co-existence is structurally OK** (mdBook does not cross-validate inter-chapter field consistency; the wave-2 abstractor's Sub-pattern C is structurally general enough to accommodate `iterations` materialisation without text change), but cycle-012 lifter / lowering-verifier pass should reconcile.
- **First firm-structural-but-partly-constructive theme** — pass-6 abstractor's eigsolve-mutation-rotation theme establishes the pattern (Sub-pattern B `LinearSolveFailed` materialisation partly-constructive pending upstream Palace refactor); recurrence-2 of partly-constructive pattern from cycle-010 lifter's L1-constructive option (b); pattern at recurrence-2 — codification candidate for cycle-012 meta-phase.
- **First L4>L3 rough-in sister-theme** — pass-5 lifter's FGMRES theme is sibling to cycle-008 GMRES; sister-theme vs parameterised-theme decision recorded as reversible discipline note (not OQ-grade); pattern at recurrence-1 of cycle-008's sibling shape; routes to cycle-012 meta-phase `variant-absorption-vs-instance-counting-policy` codification at recurrence-2 data points.
- **First cycle-planner enacting integrator-flagged priority close out-of-band** — priority #13 closed at cycle-011 cycle-planner; typical pattern is meta-phase enactment. Cycle-012 meta-phase pattern note.
- **No index-placeholder displacement** — L3/index.md was already populated post-cycle-010 wave-1; the 4 wave-1 BLAS-1 cohort dispatches all append after existing rows. **Pattern of 5 displacements through cycle-010 holds steady at 5 cumulative** (no cycle-011 instances).
- **No deferrals, no rejections, no rework loops.** All 9 reports applied as-is. **Clean-run streak continues** (cycles 005 / 006 / 007 / 008 / 009 / 010 / 011 — **seven consecutive clean cycles** since split integrator).

### Integration-tooling friction

- **1 mechanical build-repair applied** (surgical-minimal) — fixed broken link `[L1-L0/](../L1-L0/)` → `[L1-L0/](../L1-L0/index.md)` at `book/src/L3/scal.md:53`; the per-report integrator did not catch the directory-link rendering by mdbook-linkcheck2 (mdbook itself accepts directory-only links; linkcheck2 rejects them). **First in-cycle linkcheck2 directory-link failure** — minor friction pattern for cycle-012 meta-phase consideration (route via per-report safety-net gate `directory-link-rejected-by-linkcheck2` candidate).
- **MCP codemap usage stable** — multiple cycle-011 dispatches used MCP routinely (wave-2 abstractor + wave-2 lifter cluster + wave-1 BLAS-1 reduction-cohort search_text verification); **0 permission-denied** across all uses. Friction-ledger entry `mcp-codemap-permission-denied-across-batch-1` is resolution-candidate for cycle-012 meta-phase enactment.
- **per-report `integrated_at:` write-authority drift: 0 recurrences** — **sixth consecutive clean cycle** (cycle-006 enacted; cycle-007/008/009/010/011 zero recurrences). Friction-ledger entry `integrated-at-write-authority-drift` `resolved` per cycle-009 meta-phase enactment continues to hold.
- **OQ-policy-merge discretion exercised once** (wave-1 pass-3 reduction-cohort dispatch) — the report proposed a new OQ slug `l3-l1-identity-in-form-annotation-policy-formalization`; per integrator's policy-merge discretion this was merged into the existing cycle-010 OQ `l3-l1-directory-naming-structure-policy` (structurally the same underlying policy question). Recorded explicitly in the BLAS-1 reduction-cohort partial-precedent paragraph for full integrator-finalize visibility. Pattern continues integrator-finalize's policy-merge-vs-new-OQ discretion.

---

## cycle-010 — 2026-05-27T230802Z

**Meta-batch context**: **first primary cycle of meta-batch-2** under the 3:1 meta cadence (cycles 010/011/012 form batch-2; meta-phase fires after cycle-012 finalize, NOT after this cycle). Cycle counter does not reset at batch boundaries; batch-1 closed at cycle-009 meta-phase commit `eac5007`. Cycle-010 enacted priority #20 first target (identity-lowering-both-levels-backfill) as wave-1 lead work + priority #17 first targets (lower-layer-shared-vocabulary) as wave-2 lead work + priority #19 first instance (phase-1-corpus-reduction-audit) as wave-2 closing work.

### Unblocked

- **`harvester` / `cross-layer-cross-cutter` on the 4 cycle-010 wave-1 pass-2 audit OQs** unblocked — cycle-010 wave-1 pass 2 audit promoted 4 routing OQs for cycle-011+ planner: `l3-backfill-apply-linop-and-blas1-cohort` (HIGH CONFIDENCE; harvester dispatch for `apply_linop` L3 + 6-entry BLAS-1 cohort backfill — second-target priority #20), `ksp-solve-l2-promotion-non-identity-substantive-gap` (MEDIUM CONFIDENCE; separate priority candidate, out-of-priority-20-scope), `l3-l1-directory-naming-structure-policy` (policy question for layer-intro-author / cycle-planner), `l3-vocabulary-inventory-gap` (latent pattern — L3 index advertises vocabulary not yet populated). Citation: cycle-010 STAGING row 2 §"Open questions promoted" + 4 OQs at `scaffolding/open-questions.md` (cycle-010).
- **`lifter` on `book/src/L4/index.md:40` SUPERSEDED-text drift** unblocked-but-small — cycle-010 wave-1 pass 2 META Issue 1 flagged `book/src/L4/index.md:40` carrying cycle-006-era SUPERSEDED text "the kernel body's primitive sequence is identity-in-form, so no intermediate L3 `krylov-step` row is needed". Wave-1 pass 1 enacted the substantive supersession (added `book/src/L3/krylov-step.md`); the index drift is cross-reference cleanup that the wave-1 pass-1 dispatch did not touch (out-of-scope per dispatch instructions; flagged for finalize/future lifter). Smallest-cost cycle-011 dispatch.
- **`abstractor` on `eigsolve-mutation-rotation` L1>L0 theme** unblocked — cycle-010 wave-2 pass 7 lifter annotated `LinearSolveFailed` as L1-constructive with 10 negative-anchor citations; the materialising L1>L0 theme is the natural cycle-011+ follow-on (sister-theme to `ksp-solve-mutation-rotation` per the L1 eigsolve §L1-vs-L0 forward-pointers across 5 chapter loci). Plumbs the case via inner-solver coupling refactor per cycle-009 OQ option (c) deferred reading.
- **`cycle-planner` to close priority #13** — cycle-010 wave-2 pass 5 surfaced routing OQ `priority-13-now-landed-as-matrix-weighted-norm` (status `routing`). Per write-authority partition, `scaffolding/priorities.md` is meta-phase + cycle-planner co-edit. Cycle-011 cycle-planner reads OQs alongside priorities and will execute the close.
- **`lifter` cluster on 3 remaining cycle-009 eigsolve OQs** unblocked — `eigsolve-scaling-coordinate-convention`, `eigsolve-initial-space-axis-placement`, `eigsolve-iteration-count-result-field` remain `open`. Pass 7 explicitly out-of-scope per "one theme per invocation"; cycle-011 candidate cluster (or roll into `eigsolve-mutation-rotation` abstractor when that fires).
- **`same-layer-cross-cutter` on next phase-1 corpus reduction batch** unblocked — cycle-010 wave-2 pass 8 first-instance landed; OQ `phase-1-corpus-reduction-remaining-7-slices` enumerates priority order (orthog → chebyshev → polynomial_recurrence_step → divfree → cg_preconditioning_framework → plane_rotation_stream → sparse_triangular_solve) with batch-size suggestion 2-4 slices per dispatch. Audit template established and machine-replayable.
- **`harvester` on `l1-orthogonalize` candidate** unblocked-but-gated — cycle-010 wave-2 pass 8 surfaced OQ `l1-orthogonalize-promotion-from-arnoldi-step-and-orthog` recording promotion criterion under unimplemented-Palace-stub policy (small AND simplifies higher forms). Landing would unblock further reduction of `arnoldi_step.md` + `orthog.md`. Variant-axis `gs_orthog` + MPI-collective shape table is unique evidence.
- **`harvester` on NLEPS at L1+** still unblocked-but-large — carried forward from cycle-009 signals. Cycle-010 wave-2 #6 MCP pilot SUCCESS validates that the dispatch can use MCP localization tools cleanly; this lowers (but does not eliminate) the cost of the NLEPS dispatch.
- **`abstractor` (or `lifter`) on upstream `gmres.md §L4 v0.6 → v0.7 self-rotation`** still unblocked-but-large — carried forward. Cycle-010 wave-2 #6 surfaced sibling routing OQ `fgmres-inner-loop-iterate-while-migration-lifter-candidate` with explicit lifter-before-harvester sequencing directive for cycle-011 planner (lifter on the cycle-008 GMRES migration theme — re-anchor against firm `gmres.md §L4 v0.7` — before harvester on `book/src/L4/check-stop-into-carry.md`).
- **`layer-intro-author` on L0 bootstrap bundle 6** still unblocked (deferred from cycle-010 planner per capacity) — cycle-009 OQ `l0-bundle-6-candidates` carried forward.

### New dependencies

- **L3 `krylov-step` firm (wave-1 pass 1, cycle-010)** — **first firm L3 operator in the artifact** (~105 lines). Identity-lowering backfill enacting CLAUDE.md §Methodology invariants new bullet "Identity-lowerings still require both L levels" (codified cycle-009 meta-phase). Six variant axes (problem-type / spectrum-target / spectral-transformation / scaling — using L3 vocabulary; corresponds to L4 / L2 variant axes via the identity lowering); three algebraic laws + seven non-laws; full Signature + Semantics + Evidence + L3-vs-L4 + L3-vs-L2 sections. **L3 layer transitions from placeholder-only to 1-firm-operator status.** Cycle-006 verdict "no L3 row needed for krylov-step" formally SUPERSEDED via SUPERSEDED-annotation at `book/src/L4-L3/krylov-step-typed-wrapper-dissolution.md:218`. Krylov-step chain now occupies all 6 layer/lowering positions explicitly: L4 firm > L4>L3 firm > L3 firm > L3>L2 firm > L2 firm > L1 ksp_solve firm > L1>L0 ksp-solve-mutation-rotation firm.
- **L1 `matrix-weighted-norm` rough-in (wave-2 pass 3, cycle-010)** — test-coverage-bounded; L1 energy-norm primitive `α = ‖x‖_B = √(xᴴ B x)` for SPD `B`; ~110 lines; 12 algebraic laws + 5 explicit non-laws; 2 orthogonal variant axes (element-type, output-arg-vs-return-value) + 2 collapsed/absorbed axes (parallel-wrapper, operator-representation). L0 anchor `palace/linalg/operator.hpp:372-374` + `cpp:599-619`. **Second test-coverage-bounded L1 rough-in** (after `eigsolve` cycle-009).
- **L1 `bilinear-form` rough-in (wave-2 pass 4, cycle-010)** — lower-layer-shared-vocabulary; `bilinear_form(x, M, y) = xᴴ M y` matrix-weighted inner product for arbitrary linear `M` (no SPD requirement); ~430 lines; **first cycle-010 citation-validity FAIL repaired** (conjugation-handedness mis-read in draft; 9 coordinated repairer edits; false OQ closed in-CYCLE). 4 variant axes (precision-mode, output-arg-pattern, M-symmetry-property, parallel-wrapper) + 2 collapsed/absorbed axes. **Third L1 rough-in landed cycle-010** alongside cycle-009 eigsolve.
- **L1 cohort post-cycle-010**: **8 firm + 3 rough-in (eigsolve, matrix-weighted-norm, bilinear-form) + 6 rough-in (obstruction)**. Cycle-008 OQ `matrix-weighted-norm-and-bilinear-form-l1-rough-ins` both halves landed (status `partially-answered`; SpectralNorm + L1>L0 themes residuals tracked).
- **L1/index.md restructure** — new rough-ins added to existing "Rough-in (test-coverage-bounded)" subsection (introduced cycle-009 with eigsolve); dep-map widened with 2 new rows.
- **Phase 1 slice corpus reduction first instance (wave-2 pass 8, cycle-010)** — 3 slices reduced: `gmres.md` (1144 → 671 lines, -42%); `cg.md` (506 → 165 lines, -67%); `arnoldi_step.md` (330 → 302 lines, -8%). **Net 842 lines removed from the audited slice subset.** First concrete realization of cycle-009-meta-batch-1-closure user-directive "Phase 1 corpus reduces as material is lifted" (priority #19). Stub headers consistently point at firm layered entries; unique material retained verbatim (gmres L4 v0.2-v0.6 self-rotation history; cg L4 v0.5 first-iteration-unrolling derivation + ratification; arnoldi_step L1/L2/L3/L4 sections unique relative to firm krylov-step chain). Audit template established and machine-replayable.
- **Index-placeholder displacement** — pass 1 displaced `L3/index.md` placeholder `(empty — Phase B skeleton.)` with first firm dep-map row. **Fifth such displacement total** (cycle-006 L4/index + cycle-006 L4-L3/index + cycle-007 L3-L2/index + cycle-008 L1-L0/index + cycle-010 L3/index). Pattern stable across 5 instances. Meta-phase formalization carry-over from batch-1 signals still pending.
- **L3 entry frontmatter precedent** — pass 1 L3 `krylov-step.md` introduces YAML frontmatter (layer / operator / firmness / lowers_to / lifts_from / variant_axes; 6 fields). **First L_n entry to carry frontmatter in the artifact** (other than L4>L3 / L1>L0 themes' `verified_against:` blocks).
- **L1 cohort frontmatter divergence** — pass 4 `bilinear-form.md` landed with 8-field frontmatter (layer / operator / firmness / lowers_to / lifts_from / depends_on / variant_axes); pass 3 `matrix-weighted-norm.md` landed without frontmatter per existing L1 cohort convention. **L1 cohort now mixed** post-cycle-010; future-normalization candidate (either backfill all L1 entries to frontmatter, or strip L3 + bilinear-form frontmatter to match cohort norm). Forwarded to cycle-012 meta-phase batch consideration.

### Resolution implications

- **`matrix-weighted-norm-and-bilinear-form-l1-rough-ins`** (cycle-008) — **partially-answered** (status flipped `open` → `partially-answered`; `last_revisited: cycle-010`). Both halves landed cycle-010 (matrix-weighted-norm wave-2 pass 3; bilinear-form wave-2 pass 4); residuals tracked: (a) SpectralNorm power-iteration sibling harvest; (b) L1>L0 lowering themes for both operators (`matrix-weighted-norm-mutation-rotation` / `bilinear-form-mutation-rotation`).
- **`nrm2-B-weighted-energy-norm-harvest`** (cycle-003) — **partially-answered** (status flipped `open` → `partially-answered`; `last_revisited: cycle-010`). Cycle-010 wave-2 pass 5 verified duplicate-of-`matrix-weighted-norm` via 6 identity claims (same L0 anchor / closed-form / SPD precondition / dependencies / variant axis / callsite cohort); merge-and-rename verdict; no separate L1 file landed; naming-axis residual tracked via new OQ `matrix-weighted-norm-naming-sweep`.
- **`eigsolve-linear-solve-failed-status-anchor`** (cycle-009) — **partially-answered** (status flipped `open` → `partially-answered`; `partial_answer_at: cycle-010`). Cycle-010 wave-2 pass 7 lifter adopted option (b): keep + annotate `LinearSolveFailed` as L1-constructed with 10 negative-anchor citations. Materialising `eigsolve-mutation-rotation` L1>L0 theme deferred to cycle-010+ abstractor (the case will be lowered via inner-solver coupling refactor when the theme lands).
- **`nleps-spec-gap-as-check-stop-into-carry-reuse-blocker`** (cycle-009) — held `open` with `last_revisited: cycle-010`. Cycle-010 wave-2 pass 6 MCP-pilot retry confirmed FGMRES sister-algorithm match (lower-edge reading); cross-references new `variant-absorption-vs-instance-counting-policy` (cycle-009) confirming two cycles of corroborating evidence for batch-2 meta-phase codification.
- **18 new OQs opened cycle-010** (passes 2: 4 routing; 3: 5 follow-ups + naming sweep; 4: 3 follow-ups; 5: 1 routing; 6: 1 routing; 7: 0; 8: 4 routing/follow-up); **3 partial-answer flips**. **Net ledger change: +15 OQs.** Net positive but reflects priority #19's audit dispatch (4 routing OQs) + priority #20's audit dispatch (4 routing OQs) + priority #17's two harvester dispatches (8 follow-up OQs).
- **Cycle-012 meta-phase batch-2 aggregation** fires after cycle-012 finalize per 3:1 cadence. Critical aggregation targets surfaced this cycle (see §Suggested next dispatches below).

### Suggested next dispatches

- **(CYCLE-011 highest-priority follow-on to priority #20)** (`harvester` / `cross-layer-cross-cutter`, **L3 `apply_linop` + BLAS-1 cohort backfill**) — per cycle-010 wave-1 pass-2 audit's HIGH CONFIDENCE recommendation. 6-entry BLAS-1 cohort (axpy / scal / dot / nrm2 / axpby / axpbypcz) candidates with identity-lowering shape; bundle dispatch recommended. Closes priority #20 second-target work.
- **(CYCLE-011 smallest-cost)** (`lifter`, `book/src/L4/index.md:40` SUPERSEDED-text drift) — per cycle-010 wave-1 pass-2 META Issue 1. Single-edit cleanup pointing at the cycle-010 wave-1 pass-1 L3 backfill enactment.
- **(CYCLE-011 cycle-planner)** close priority #13 per routing OQ `priority-13-now-landed-as-matrix-weighted-norm` — landing-as-matrix-weighted-norm move or remove.
- (`abstractor`, `eigsolve-mutation-rotation` L1>L0 theme) — materialising theme for `LinearSolveFailed` case + inner-solver coupling refactor; sister-theme to `ksp-solve-mutation-rotation` (cycle-008).
- (`lifter` cluster, 3 remaining cycle-009 eigsolve OQs) — `eigsolve-scaling-coordinate-convention`, `eigsolve-initial-space-axis-placement`, `eigsolve-iteration-count-result-field`. May be rolled into the `eigsolve-mutation-rotation` abstractor's scope.
- (`same-layer-cross-cutter`, **next phase-1 corpus reduction batch**) — per OQ `phase-1-corpus-reduction-remaining-7-slices`; suggested first batch orthog → chebyshev → polynomial_recurrence_step (3 slices).
- (`lifter`, FGMRES on cycle-008 `gmres-inner-loop-iterate-while-migration` theme) — per cycle-010 wave-2 pass-6 routing OQ; lifter-before-harvester sequencing directive: re-anchor against firm `gmres.md §L4 v0.7` before any harvester on `book/src/L4/check-stop-into-carry.md`.
- (`abstractor` or `lifter`, `gmres.md §L4 v0.6 → v0.7 self-rotation`) — large dispatch; unlocks the FGMRES lifter above.
- (`layer-intro-author`, L0 bootstrap bundle 6) — deferred from cycle-010 planner; carry-forward from cycle-009 OQ `l0-bundle-6-candidates`.
- (`harvester`, `NLEPS at L1+`) — large multi-cycle dispatch; auto-promotes `check_stop_into_carry` per cycle-008 criterion if it lands.
- (`harvester`, `l1-orthogonalize`) — small candidate per cycle-010 wave-2 pass-8 routing OQ; landing unblocks further `arnoldi_step.md` + `orthog.md` reduction.
- **CRITICAL cycle-012 meta-phase batch-2 aggregation targets** (batch-2 = cycles 010/011/012):
  - **MCP codemap rollout completion** — friction-ledger entry `mcp-codemap-permission-denied-across-batch-1` is **resolution-candidate** (cycle-010 wave-2 #6 pilot SUCCESS: 14 calls, 0 permission-denied). Meta-phase enacts the ledger entry update (integrator only signals per CLAUDE.md §Write-authority partition).
  - **`planner-side-deduplication-by-L0-anchor` friction pattern at recurrence-1** — cycle-N OQ filed at site A; cycle-N+5 OQ filed at site B; both name same operator; cycle-N+7 wave dispatches both. Meta-phase: deduplicate-by-L0-anchor in OQ ledger as part of meta-phase aggregation.
  - **`dispatch-brief-drift-planner-reads-stale-file-inventory` friction at recurrence-1** — cycle-010 planner cited `eps.cpp`/`feast.cpp` (non-existent); MCP `list_files` corrected on first call. If recurs cycle-011, promote to friction-ledger.
  - **`phase-1-corpus-audit-line-range-arithmetic-brittleness` friction at recurrence-1** — pass 8 first-instance audit drifted 1-2 lines from actual H2 boundaries; repair corrected via `grep -n "^## "` enumeration. Pre-existing skill candidate `phase-1-slice-reduction-audit` would prescribe the fix step.
  - **`negative-anchor-citation-at-per-status-variant-granularity` pattern at recurrence-1** — pass 7 first systematic use; distinguishable from existing per-operator obstruction-theme pattern. Watch for recurrence-2 on cycle-009 eigsolve `eigsolve-iteration-count-result-field` OQ (similar negative-anchor evidence about iteration-counter exposure) or any cycle-011 sum-typed-status lifter / abstractor.
  - **L1 cohort frontmatter divergence cleanup** — mixed cohort post-cycle-010 (matrix-weighted-norm no frontmatter; bilinear-form with frontmatter; cycle-010-wave-1 L3 entry also with frontmatter). Future-normalization candidate.
  - **`test-coverage-bounded-rough-in-nomenclature` methodology question** — 3 instances now (eigsolve cycle-009, matrix-weighted-norm cycle-010, bilinear-form cycle-010 with named gate `lower-layer-shared-vocabulary` but same gate-mechanism). Cycle-012 codification decision.
  - **`localize-then-read` skill candidate** — pass 6 critic-flagged; repair marked `not-needed` at recurrence-1. Cycle-012 meta-phase consideration if MCP-codemap pattern recurs cycle-011.
  - **`phase-1-slice-reduction-audit` skill candidate** — pre-existing entry at `scaffolding/skill-candidates.md:114-115` (filed by pass-8 critic per META §skill-uptake-survey); cycle-012 meta-phase batch-2 promotion consideration (the cycle-010 first-instance template is detailed and machine-replayable).
  - **OQ-to-resolution latency** — cycle-009 OQ `eigsolve-linear-solve-failed-status-anchor` resolved within 1 cycle (cycle-010 lifter dispatch) via the harvester-recommended option (b). Pattern strengthens the cycle-007/008/009 trend of OQ-to-resolution latency at ≤2 cycles for option-named OQs.
  - **Index-placeholder displacement pattern formalization** — now 5 instances; batch-1 signals already carried this candidate forward; cycle-012 meta-phase formalization recommended.

### Wave-conflict observations

- **5-wave-1 + 3-wave-2 split** — second-largest dispatch shape under the 12-cap (cycle-008 was 5+2 = 7; cycle-010 is 5+3 = 8). All 8 reports applied as-is at integration; zero rework loops. Continues the validation of 5+N wave shape under split integrator.
- **`scaffolding/open-questions.md` touched 7 times** at distinct line ranges across passes 2/3/4/5/6/7/8 (pass 1 had no promotion). Zero collisions; append-before-Dropped convention held cleanly per-OQ YAML status authority. **Pattern reaches recurrence-6** (cycles 005/006/007/008/009/010).
- **`book/src/SUMMARY.md` touched 3 times** (passes 1 L3 + 3 L1 + 4 L1) with zero collisions. 6-cycle pattern stable: cycle-005 (5/6) / cycle-006 (4/5) / cycle-007 (5/6) / cycle-008 (3/7) / cycle-009 (3/4) / cycle-010 (3/8).
- **`book/src/L1/index.md` touched 2 times** (passes 3 + 4) with sibling-coordination clean: pass 3 added matrix-weighted-norm row + cohort bullet + Queued line update; pass 4 added bilinear-form row + cohort bullet + Queued line retirement. Pass-3 staging row explicitly noted the coordination posture for pass 4 to read; integrator-per-report serial-re-read discipline held.
- **First L1 cohort frontmatter divergence** — pass 3 (matrix-weighted-norm) no frontmatter (existing cohort convention); pass 4 (bilinear-form) 8-field frontmatter (cycle-010 wave-1 L3 precedent); pass 1 L3 `krylov-step.md` first L_n entry with frontmatter ever. Mixed L1 cohort; future-normalization candidate forwarded to cycle-012 meta-phase batch.
- **First cycle-010 citation-validity FAIL repaired cleanly** — pass 4 bilinear-form's conjugation-handedness mis-read; 9 coordinated repairer edits. Demonstrates the repair pipeline handles substantive citation errors at full chapter scope; methodology-signal for meta-phase batch (validates the repair-then-integrate pipeline under load).
- **Index-placeholder displacement** applied once (pass 1 `L3/index.md`). **Fifth such displacement total** (cycle-006 L4/index + cycle-006 L4-L3/index + cycle-007 L3-L2/index + cycle-008 L1-L0/index + cycle-010 L3/index). Pattern stable across 5 instances; cycle-012 meta-phase formalization candidate continues to carry forward.
- **First inspection-only-AND-no-OQ-augmentation dispatch followed by audit-led OQ shower** — pass 6 (MCP-codemap-pilot retry on `check_stop_into_carry`) is the second cycle in a row to see inspection-only with full-OQ-output payload (pass 6 promoted 1 cycle-009-status-update + 1 new routing OQ; pass 2 promoted 4 new routing OQs; pass 8 promoted 4 new routing OQs). **Cycle-010 had 3 inspection-only / audit-led dispatches** (passes 2, 6, 8) — first cycle to see ≥3 such dispatches in a single wave-1+wave-2 cycle.
- **First cross-cycle dispatch-brief drift correction via MCP localization** — pass 6's MCP `list_files` corrected the cycle-010 planner's `eps.cpp`/`feast.cpp` references (non-existent in Palace; correct files are `arpack.cpp` / `slepc.cpp` / `nleps.cpp`). The drift-correction was cheap-and-visible thanks to MCP enablement; pre-MCP era would have required the dispatch to re-prompt the planner or work around the drift. **MCP pilot SUCCESS payload signal** — going forward, MCP localization makes planner-brief drift correctable in the dispatch rather than at planning.
- **No deferrals, no rejections, no rework loops.** All 8 reports applied as-is. Clean-run streak continues (cycle-005 / 006 / 007 / 008 / 009 / 010 — **six consecutive clean cycles** since split integrator).

### Integration-tooling friction

- **MCP codemap pilot SUCCESS** (cycle-010 wave-2 #6) — 14 tool calls (`list_files` × 2, `search_text` × 7, `get_file_subtree` × 1, `read_range` × 4), **0 permission-denied**. First post-cycle-009-meta-phase pilot under commit `ceb87da` enablement (option (a) per cycle-009-meta ask resolution). **Friction-ledger entry `mcp-codemap-permission-denied-across-batch-1` is resolution-candidate** for cycle-012 meta-phase enactment (integrator only signals; meta-phase enacts ledger entry updates per CLAUDE.md §Write-authority partition). Going forward, MCP localization should be preferred for C++ source-localization in combinator-miner / cross-cutter / lowering-verifier audit dispatches.
- **per-report `integrated_at:` write-authority drift: 0 recurrences** — **fifth consecutive clean cycle** (cycle-006 enacted; cycle-007/008/009/010 zero recurrences). Friction-ledger entry `integrated-at-write-authority-drift` `resolved` per cycle-009 meta-phase enactment continues to hold.
- **abstractor write-authority drift (cycle-008 first occurrence): 0 recurrences cycle-010** — **second consecutive cycle of zero recurrences** after cycle-009. Single-instance evidence; meta-phase cycle-009 decision was close-as-one-off; cycle-012 meta-phase batch-2 evidence-base now expands to 3-cycle clean record post-enactment.
- **legacy log/cycle-010.md name collision handled** — pre-layered-era `log/cycle-010.md` (2026-05-24 `forward orthog [L0→L1] — pass`; 212 bytes) renamed to `log/cycle-010-legacy.md` per cycle-005..009 precedent. Cycle-011..172 legacy files remain unchanged pending bulk-rename meta-phase candidate (carry-over from batch-1 signals).
- **`planner-side-deduplication-by-L0-anchor` friction at recurrence-1** (cycle-010 wave-2 pass 5) — cycle-003 OQ filed for `nrm2_B-weighted-energy-norm`; cycle-008 OQ filed for `matrix-weighted-norm` (with `nrm2_weighted` candidate naming); cycle-010 wave dispatched both, and pass 5 verified duplication via 6 identity claims. Possible meta-phase improvement: deduplicate-by-L0-anchor in OQ ledger as part of meta-phase aggregation. Below friction-ledger threshold (recurrence-1); cycle-012 meta-phase watch-item.
- **`dispatch-brief-drift-planner-reads-stale-file-inventory` friction at recurrence-1** (cycle-010 wave-2 pass 6) — cycle-010 planner cited `eps.cpp` / `feast.cpp` (non-existent); MCP `list_files` corrected on first invocation. MCP localization makes the correction cheap-and-visible; below friction-ledger threshold. Cycle-012 meta-phase watch-item — if recurrence-2 fires in cycle-011, promote to friction-ledger.
- **`phase-1-corpus-audit-line-range-arithmetic-brittleness` friction at recurrence-1** (cycle-010 wave-2 pass 8) — first-instance audit drifted 1-2 lines from actual `## H2` boundaries across all 3 slices; repair systematically corrected the drift via `grep -n "^## "` enumeration. Cycle-012 meta-phase watch-item — if recurrence-2 fires in cycle-011 slice-reduction audits, promote to friction-ledger with addressed-by skill-uptake (the proposed `phase-1-slice-reduction-audit` skill would prescribe the `grep -n` enumeration step).
- **`negative-anchor-citation-at-per-status-variant-granularity` pattern at recurrence-1** (cycle-010 wave-2 pass 7) — first systematic use of negative-anchor citations at finer-grained variant within an operator's result-record (`LinearSolveFailed` constructive case in `EigStatus` sum-type; 10 sites cited demonstrating the *absence* of `GetConverged()` querying). Distinguishable from existing per-operator obstruction-theme negative-anchor pattern (`book/src/L1-L0/minres-iteration.md` / `book/src/L1-L0/bicgstab-iteration.md`). Cycle-012 meta-phase watch-item — if recurrence-2 fires on (a) cycle-009 sibling OQ `eigsolve-iteration-count-result-field`, or (b) any cycle-011 sum-typed-status dispatch, promote to friction-ledger or skill-candidates.
- **L1 cohort frontmatter divergence cleanup candidate** — pass-3 matrix-weighted-norm landed without frontmatter; pass-4 bilinear-form landed with 8-field frontmatter; pass-1 wave-1 L3 krylov-step is the cycle-010 frontmatter precedent. Mixed L1 cohort post-cycle-010. Cycle-012 meta-phase consideration: either backfill all L1 entries to frontmatter (consistency with L3 + bilinear-form precedents) OR strip the L3 + bilinear-form frontmatter to match the existing L1 + L2 + L4 cohort norm (consistency with non-frontmatter convention). Either resolution is acceptable; the mixed state is the bug.
- **`localize-then-read` skill candidate observed** (cycle-010 wave-2 #6 critic-flagged; repair marked `not-needed` at recurrence-1) — the MCP server-instructions' "use list_files / get_file_subtree / get_symbol_def / get_call_sites / list_dependencies / search_text to localize before reading; read_range is the only source-returning tool" guidance matched how the dispatch naturally flowed. Cycle-012 meta-phase batch-2 aggregation should consider for promotion if (a) the MCP-codemap pilot pattern recurs in cycle-011 dispatches with the same shape, or (b) the cycle-012 meta-phase batch evidence supports default-accept under the low-bar policy.
- **`phase-1-slice-reduction-audit` skill candidate already filed** at `scaffolding/skill-candidates.md:114-115` (filed by pass-8 critic per META §skill-uptake-survey) — pre-existing entry; cycle-012 meta-phase batch-2 aggregation should consider for promotion (the cycle-010 first-instance template is detailed and machine-replayable; the audit per slice is moderately expensive per dispatch caveat 6 — promotion would help cycle-011+ replay).
- **First firm L3 operator landed** — methodology milestone; enacts CLAUDE.md §Methodology invariants new bullet "Identity-lowerings still require both L levels" (codified cycle-009 meta-phase). L3 layer transitions from placeholder-only to 1-firm-operator status. Cycle-006 verdict formally SUPERSEDED. Cycle-011+ candidate cluster: `apply_linop` L3 + 6-entry BLAS-1 cohort.
- **First phase-1 corpus reduction landed** — methodology milestone; first concrete realization of cycle-009-meta-batch-1-closure user-directive "Phase 1 corpus reduces as material is lifted" (priority #19). 3 slices reduced by 842 lines; audit template established and machine-replayable for cycle-011+ batches.
- **OQ-to-resolution latency: cycle-009 OQ resolved within 1 cycle** (cycle-010 wave-2 pass 7 lifter via the cycle-009 harvester-recommended option (b)). Pattern strengthens the cycle-007/008/009 trend.
- **No new safety-net gates needed.** Cycle-005..010 gate set held cleanly across 8 wave-mates with 5+3 wave shape. 3 gate hits cycle-wide (1 index-placeholder displacement applied-discretionarily; 2 priorities.md authority guard correctly redirecting to OQ promotion; 1 retroactive-budget per-slice below ≥3 block threshold). No global-budget retroactive-budget hit. Zero edge-label drift. Zero forward-edge claims without surface. Zero variant-axis-missing. Zero append-on-missing-slug. Zero bookkeeping-incomplete.

---

## cycle-009 — 2026-05-27T200036Z

**Meta-batch context**: **third and final primary cycle of meta-batch-1** under the 3:1 meta cadence (cycles 007/008/009 form batch-1; **meta-phase fires immediately after this finalize commit lands**; `/compact` after meta-phase per post-meta-compactification user directive). Cycle counter does not reset at batch boundaries — cycles 010/011/012 form batch-2.

### Unblocked

- **`harvester` / `lifter` / `lowering-verifier` on the four `eigsolve` firm-promotion follow-up OQs** unblocked — cycle-009 wave-1 pass 3 landed `eigsolve` as rough-in (test-coverage-bounded); the four follow-up OQs (`eigsolve-linear-solve-failed-status-anchor`, `eigsolve-scaling-coordinate-convention`, `eigsolve-initial-space-axis-placement`, `eigsolve-iteration-count-result-field`) are individually tractable as small lifter / lowering-verifier dispatches and collectively gate firm promotion. The `LinearSolveFailed` status anchor question is the highest-priority of the four (constructively-introduced sum-type case with no L0 anchor). Citation: cycle-009 STAGING row 3 §"Open questions promoted" + the 4 OQs at `scaffolding/open-questions.md` (cycle-009).
- **`layer-intro-author` on L0 bootstrap bundle 6** unblocked — cycle-009 wave-1 pass 2 (L0 bundle 5) opened OQ `l0-bundle-6-candidates` enumerating remaining candidates: `linalg-solver-file` (highest priority — file-level overview of `palace/linalg/solver.{hpp,cpp}`, closes file-overview gap on the four `linalg/` anchor files), `tests-as-semantic-supplement` (pending placement decision per OQ `tests-as-semantic-supplement-l0-vs-concepts-decision`), `mutable-workspace-pattern` Category-5 expansion (if cycle-008+ work surfaces new workspace-pattern variants). Citation: open questions `l0-bundle-6-candidates` + `tests-as-semantic-supplement-l0-vs-concepts-decision` (cycle-009).
- **`harvester` on NLEPS at L1+** unblocked-but-large — cycle-009 wave-1 pass 4 (combinator-miner survey) identified NLEPS Quasi-Newton inner loop at `reference/palace/palace/linalg/nleps.cpp:589-647` as **the natural second consumer** for `check_stop_into_carry` (same 3-condition stop shape with `{Converged, Diverged, MaxIt}` reason set). NLEPS is a non-linear eigenvalue problem driver (sibling-tier to GMRES, not a sub-component); promoting it to L1+ is a multi-cycle effort (`nleps.cpp` is 952 lines including deflation, Armijo backtracking, line search). If NLEPS L1+ slice lands, the `check_stop_into_carry` helper auto-promotes per cycle-008 promotion criterion. Citation: open question `nleps-spec-gap-as-check-stop-into-carry-reuse-blocker` (cycle-009).
- **`harvester` on `matrix-weighted-norm` + `bilinear-form` L1 rough-ins** still unblocked (deferred from cycle-009 planner per capacity) — cycle-008 OQ `matrix-weighted-norm-and-bilinear-form-l1-rough-ins` carried forward. Candidates: `nrm2_weighted :: (x, B) → √(xᴴ B x)`, `dot_bilinear :: (x, M, y) → xᴴ M y`, `power_iterate :: (A, x_init) → λ_dom`. Lower priority than `eigsolve` firm-promotion follow-ups (less direct test coverage).
- **`abstractor` (or `lifter`) on upstream `gmres.md §L4` v0.6 → v0.7 self-rotation** still unblocked-but-large — cycle-008 wave-1 pass 5 landed `gmres-inner-loop-iterate-while-migration` as a rough-in L4>L3 theme whose firming requires the upstream gmres.md re-render. Deferred to cycle-010+; gmres.md is one of the most-elaborate slice docs.
- **Cycle-009 meta-phase batch-1 aggregation fires next** — aggregation across cycles 007/008/009 per 3:1 cadence. **Critical aggregation targets** enumerated in batch CYCLE.md §"Meta-batch-1 closure summary".

### New dependencies

- **L3>L2 `krylov-step-body-identity` (firm, promoted cycle-009 wave-1 lifter via status-inheritance from `firm-rough-in`)** — first across-cycle status-inheritance promotion in the artifact. Status promotion was mechanical (5 in-place edits consolidating the inheritance flip + 1 dep-map row update); no signature, applicability-condition, or LHS/RHS shape changes. Downstream effect: **krylov-step lowering chain fully firm** — L4 `krylov-step` (cycle-006) > L4>L3 `krylov-step-typed-wrapper-dissolution` (cycle-008) > L3 rendering (in the L4>L3 theme) > L3>L2 `krylov-step-body-identity` (cycle-009) > L2 `krylov-step` (cycle-005). **First fully-firm cross-layer chain in the artifact**.
- **L1 `eigsolve` (rough-in test-coverage-bounded, cycle-009 wave-1)** — **second constructed-operator gate at L1** composing against `ksp_solve` (the inner linear solver is itself a constructed-operator absorption). **First multi-level constructed-operator composition in the firm+rough-in L1 vocabulary**. Introduces 4 preserved variant axes + 3 collapsed axes + 3 out-of-scope items. New "Rough-in (test-coverage-bounded)" L1 cohort subsection added to `book/src/L1/index.md` between Firm bullet list and "Rough-in (obstruction)" subsection (cohort-purity preserving — Firm count unchanged at 8). L1 dep-map widened with new `eigsolve` row depending on `ksp_solve` (direct), `apply_linop` (direct), and `dot`/`nrm2`/`axpy`/`axpby` (transitive). Introduces the **partial-convergence** semantic (`PartialConverged` status) as a distinguishing feature relative to `ksp_solve`'s soft-fail.
- **2 new L0 chapters (bundle 5, cycle-009 wave-1)**: `mpi-globalsum-and-collectives` (File overviews — `palace/utils/communication.hpp`), `preconditioner-classes-overview` (Overload-sets-and-class-interfaces — 7-class survey). **L0 chapter count 14 → 16**. Backlinks: `mpi-globalsum-and-collectives` references `par-types-single-rank-reading` + `mfem-vector-types` + `transparent-vs-load-bearing-tricks`; `preconditioner-classes-overview` references `mfem-wrapper-solver` + `apply-linop-overload-set` + `kspsolver-base-class`. Both chapters use `(forward-target)` annotation convention per cycle-008 bundle-4 precedent.
- **First `Rough-in (test-coverage-bounded)` L1 cohort subsection** — first instance of this cohort-status category in any layer's index. Distinct from `Rough-in (obstruction)` (no Palace L0 anchor, harvester promotion gated on Palace gaining implementation) — `Rough-in (test-coverage-bounded)` has Palace L0 anchor + L1 form but reduced algebraic-law confidence pending dedicated tests or expanded literature anchoring. Cycle-010+ meta-phase consideration: should this cohort subsection convention be back-applied to L0/L2/L3/L4 indexes where applicable?

### Resolution implications

- **`l0-bundle-5-candidates`** (cycle-008) — **closed** (status flipped `open` → `answered`; `answered_at: cycle-009`). 2 of 3 candidates landed (mpi-globalsum-and-collectives, preconditioner-classes-overview); third candidate `tests-as-semantic-supplement` deferred via new OQ `tests-as-semantic-supplement-l0-vs-concepts-decision` pending L0-vs-concepts placement decision.
- **`eigsolve-l1-operator-rough-in-candidate`** (cycle-008) — **partially-answered** (status flipped `open` → `partially-answered`; `partial_answer_at: cycle-009`). Cycle-009 wave-1 pass 3 harvester landed `eigsolve` as rough-in (test-coverage-bounded); firm-promotion follow-up tracked via 4 new OQs. **Deliberate departure** from binary `open|answered` to preserve firm-promotion tracking — precedent: cycle-007 `iterate-while-l3-rendering-trajectory-accumulation-gap` (multiple verdict statuses).
- **1 OQ closed cycle-009 + 1 partially-answered + 11 new opens = net +10 OQs**. Net positive but reflects deliberate "carry the work as recorded follow-ups" pattern: combinator-miner inspection-only dispatch contributes 5 OQs (its entire output is OQs — no book/ edits); eigsolve rough-in's firm-promotion follow-ups contribute 4 OQs (well-scoped individual lifter / lowering-verifier candidates); L0 bundle 5 contributes 2 OQs (bundle-6 forward-routing + tests-as-semantic-supplement placement).
- **`abstractor-write-authority-violation-cycle-008`** (cycle-008, critical-for-meta) — **NO RECURRENCE cycle-009**. All 4 cycle-009 dispatches held write-authority discipline cleanly. Meta-phase will judge whether single-instance evidence warrants role-spec wording-prominence boost or treats as one-off.
- **Cycle-009 meta-phase batch-1 aggregation** fires next — see §Suggested next dispatches below + batch CYCLE.md for full target list.

### Suggested next dispatches

- **(CYCLE-010 highest-priority)** (`lifter` or `lowering-verifier`, `eigsolve-linear-solve-failed-status-anchor`) — small dispatch closing the constructively-introduced `LinearSolveFailed` sum-type case question. Options per source OQ: (a) drop the case (collapse to `MaxIterReached`); (b) accept constructive introduction with "constructed by the L1 form" annotation; (c) require L1>L0 lowering theme to plumb the case via inner-solver coupling refactor. Recommendation per harvester: keep + mark `unconfirmed` until L1>L0 theme harvested. Smallest-cost of the 4 eigsolve follow-up OQs.
- (`lifter`, `eigsolve-scaling-coordinate-convention`) — coordinate-system decision for `EigResult.eigenvalues` under `ScaleType::NORM_2`. Affects every downstream L2 / L4 consumer.
- (`lifter`, `eigsolve-initial-space-axis-placement`) — per-call EigControl field vs construction-bound EigSolver field. Affects `EigSolver[problem]` opaque-type composition shape.
- (`lifter`, `eigsolve-iteration-count-result-field`) — whether `EigResult` carries `iterations` field analogous to `SolveResult.iterations`. Re-evaluate during firm-promotion.
- (`layer-intro-author`, `L0 bootstrap bundle 6`) — per cycle-009 OQ `l0-bundle-6-candidates`. `linalg-solver-file` (highest priority; closes file-overview gap), `tests-as-semantic-supplement` (pending placement decision), `mutable-workspace-pattern` Category-5 expansion. Continues priority #10.
- (`harvester`, `matrix-weighted-norm` / `bilinear-form` L1 rough-ins) — cycle-008 OQ carried forward; cycle-009 planner deferred per capacity. Rough-in only.
- (`abstractor` or `lifter`, `gmres.md §L4 v0.6 → v0.7 self-rotation`) — large dispatch firming cycle-008 rough-in L4>L3 `gmres-inner-loop-iterate-while-migration`. Cycle-010+ candidate.
- (`harvester`, `NLEPS at L1+`) — large multi-cycle dispatch; if it lands, auto-promotes `check_stop_into_carry` helper per cycle-008 promotion criterion. Cycle-010+ candidate.
- **CRITICAL cycle-009 meta-phase aggregation targets** (batch-1: aggregates evidence across cycles 007/008/009):
  - OQ `abstractor-write-authority-violation-cycle-008` — pattern-watching aggregated across batch-1; **single-instance evidence** (cycle-008 only; cycle-009 no recurrence). Decision: role-spec wording-prominence boost (if treat as latent pattern) vs close as one-off.
  - OQ `variant-absorption-vs-instance-counting-policy` (cycle-009) — policy codification for "second slice needs it" language across speculative-combinator promotion criteria.
  - OQ `combinator-miner-authority-defer-verdict-status-edit-scope` (cycle-009) — role-spec authority question for combinator-miner: should `defer` verdicts permit upstream §Status-block updates?
  - OQ `tests-as-semantic-supplement-l0-vs-concepts-decision` (cycle-009) — placement decision for the deferred bundle-5 candidate.
  - OQ `vocabulary-cohort-middle-slot-cross-layer-adaptation` (cycle-008, suggested slug) — L4 middle-slot adaptation precedent for L2/L3 (where same-layer rough-ins may also be absent).
  - OQ `dep-map-lowers-to-column-back-application` (cycle-008, suggested slug) — L4 dep-map `Lowers to` column split back-application to L1/L2/L3.
  - Open-questions.md section-drift cleanup (cycle-008 + cycle-009 carry-over observation).
  - **NEW**: `Rough-in (test-coverage-bounded)` L1 cohort subsection convention — should this back-apply to L0/L2/L3/L4 indexes?
  - **NEW (USER-RAISED MID-CYCLE-009 2026-05-27)**: `~/.claude/settings.json` notification hook (configured during cycle-008 post-cycle-008 work) is firing in situations where Claude is not asking the user a question. **Meta-phase should examine the hook trigger conditions and propose refinement.**
  - **Index-placeholder displacement pattern** (4 instances stable across cycles 006-008; cycle-009 had zero) — formalization candidate.
  - **Per-report `integrated_at:` write-authority drift** — friction-ledger entry `integrated-at-write-authority-drift` markable `addressed` (cycles 007/008/009 zero recurrences = 4 consecutive clean cycles since cycle-006 enactment).
- **MCP codemap rollout decision** — fires cycle-009 meta-phase per user directive (persistent permission-denied across batch-1).

### Wave-conflict observations

- **First pure-wave-1 cycle since cycle-005** (cycle-006: 4+1; cycle-007: 5+1; cycle-008: 5+2; cycle-009: 4+0). Cycle-009's planner deliberately did not stage wave-2 — priority items all wave-1-tractable; eigsolve's L1/index restructure was bundled into the wave-1 harvester per cohort-restructure convention. Validates pure-wave-1 cycles remain available under split integrator + 12-cap.
- **SUMMARY.md touched 3 times** (passes 2 ×2 L0 + pass 3 L1) with zero collisions. 5-cycle pattern stable: cycle-005 (5/6) / cycle-006 (4/5) / cycle-007 (5/6) / cycle-008 (3/7) / cycle-009 (3/4).
- **open-questions.md touched 4 times this cycle** (passes 2/3/4) at 4 distinct line ranges. Zero collisions; append-before-Dropped convention held cleanly per-OQ YAML status authority. Cycle-008 carry-over drift observation routes to cycle-009 meta-phase aggregation.
- **First across-cycle status-inheritance promotion** — pass 1 lifter promoted L3>L2 `krylov-step-body-identity` from `firm-rough-in` → plain `firm`. The cycle-007 in-cycle inheritance-pattern + cycle-008 upstream firming + cycle-009 downstream promotion forms the first complete cross-cycle inheritance precedent.
- **First cross-cycle abstractor-criterion → combinator-miner-verdict round-trip** — pass 4 combinator-miner programmatically surveyed cycle-008 wave-1 pass 5 abstractor's "defer until a second slice needs it" criterion, producing the verdict `defer`. First instance of this dispatch pattern (abstractor sets promotion criterion → combinator-miner runs survey → verdict propagates as OQ closure or new OQ). **Methodology signal for meta-phase**: the criterion-and-survey roundtrip is a clean two-cycle pattern that supports the speculative-combinator-with-defer-criterion methodology — but it requires explicit policy on what "second slice needs it" means (cycle-009 OQ `variant-absorption-vs-instance-counting-policy`).
- **First inspection-only dispatch with zero `book/` edits** (cycle-009 pass 4 combinator-miner verdict defer). All output is via OQs (6 promoted). Validates the inspection-only audit dispatch pattern (precedent: cycle-006 wave-2 lowering-verifier audit also had zero book/ edits but contributed OQ-augmentations; cycle-009 pass 4 is the first dispatch with zero book/ edits AND zero OQ-augmentations-on-existing — purely new-OQ output).
- **First cohort-purity-preserving L1/index restructure** — pass 3 harvester added new "Rough-in (test-coverage-bounded)" subsection between Firm bullet list (unchanged) and "Rough-in (obstruction)" subsection (unchanged). The Firm-cohort count remains 8 unchanged; the new subsection introduces a separate cohort category. Cohort-purity preservation is a methodology pattern worth flagging — cycle-009 meta-phase consideration.
- **Zero index-placeholder displacement this cycle** — first cycle since cycle-006 without one. The pattern remains stable across the 4 prior instances (cycle-006 L4/index + cycle-006 L4-L3/index + cycle-007 L3-L2/index + cycle-008 L1-L0/index); cycle-009 meta-phase formalization candidate (carry-over from cycle-007/008 signals).
- **No deferrals, no rejections, no rework loops.** All 4 reports applied as-is. Clean-run streak continues (cycles 005 / 006 / 007 / 008 / 009 — five consecutive clean cycles since split integrator).

### Integration-tooling friction

- **NEW USER-RAISED (mid-cycle-009 2026-05-27)**: `~/.claude/settings.json` notification hook (configured during cycle-008 post-cycle-008 work) is firing in situations where Claude is not asking the user a question. **Meta-phase should examine the hook trigger conditions and propose refinement.** Recorded for batch-1 meta-phase visibility. Routes to cycle-009 meta-phase enactment via direct `.claude/settings.json` edit or skill `update-config` proposal.
- **per-report `integrated_at:` write-authority drift: 0 recurrences** — **fourth consecutive clean cycle** (cycle-006 enacted; cycle-007/008/009 zero recurrences). Friction-ledger entry `integrated-at-write-authority-drift` markable `addressed` at cycle-009 meta-phase aggregation.
- **abstractor write-authority drift (cycle-008 first occurrence): 0 recurrences cycle-009** — single-instance evidence after one full primary cycle of pattern-watching. Meta-phase decision: role-spec wording-prominence boost (if treat as latent pattern) vs close as one-off.
- **legacy log/cycle-009.md name collision** — pre-layered-era `log/cycle-009.md` (2026-05-24 `back gmres — revise`) existed; finalize renamed it to `log/cycle-009-legacy.md` per cycle-005/006/007/008 precedent. **Cycle-010 onwards: no legacy collision expected** (cycle-008 integrator-signals flagged cycle-010+ as the cutoff). Pattern still working as designed; meta-phase bulk-rename candidate (cycle-006 / 007 / 008 / 009 carry-over signal) — alternative (a) once layered-era cycle-N numbers reach the post-collision range.
- **MCP codemap pilot remained permission-denied across batch-1** — no new pilot any cycle in batch-1 (cycle-007 first pilot result carried forward unchanged through cycle-008 + cycle-009). **Rollout decision fires cycle-009 meta-phase** per user directive. Cycle-010 planner reads meta-phase output to decide whether MCP tools become preferred for C++ source-localization.
- **Combinator-miner authority-scope question (cycle-009 pass 4)** — OQ `combinator-miner-authority-defer-verdict-status-edit-scope`. The cycle-009 dispatch dropped a candidate §Status-block edit on the cycle-008 upstream theme per repairer's Finding 4 (technically outside combinator-miner's stated authority). Routes to meta-phase for codification (strict reading preserves clean role boundaries but creates paperwork tax; relaxed reading more efficient but blurs role authority).
- **Inspection-only dispatch OQ-yield pattern** (cycle-009 pass 4) — combinator-miner survey produced 6 OQs as its entire output (zero book/ edits). High OQ-yield is expected for inspection-only dispatches but worth noting as a load-bearing aspect of the inspection-only pattern: integrator-per-report must promote all OQs cleanly + integrator-finalize must aggregate them into integrator-signals + cycle-record without confusing them with substantive-landing OQs. Pattern held cleanly this cycle.
- **No new safety-net gates needed.** Cycle-005/006/007/008/009 gate set held cleanly across 4 wave-mates with pure wave-1 ordering. **Zero gate hits cycle-009** (cleanest since cycle-005). Zero retroactive-budget hits (no retroactive sweeps; pure forward-frontier work). Zero edge-label drift. Zero forward-edge claims without surface. Zero variant-axis-missing. Zero index-placeholder displacements (no relevant placeholders remain).

---

## cycle-008 — 2026-05-27T183515Z

**Meta-batch context**: second primary cycle of meta-batch-1 under the 3:1 meta cadence (cycles 007/008/009 form batch-1; meta-phase fires after cycle-009 finalize, NOT after this cycle; compactification likewise deferred).

### Unblocked

- **`lifter` (small, mechanical) or `integrator-per-report` (mechanical inheritance update) on `book/src/L3-L2/krylov-step-body-identity.md`** unblocked (CYCLE-009 mechanical follow-up) — cycle-008 wave-1 lifter (pass 2) promoted upstream `book/src/L4-L3/krylov-step-typed-wrapper-dissolution.md` from `rough-in` to `firm`. Per status-inheritance convention recorded at `scaffolding/open-questions.md:1225`, the downstream L3>L2 theme's `firm-rough-in` status (where the `rough-in` qualifier inherited the upstream's `rough-in`) is now auto-eligible for mechanical promotion to plain `firm`. Single-file edit: `Status:` cell + brief inheritance-note paragraph. Citation: cycle-008 STAGING row 2 §"Cycle-009 integrator-signals follow-up flagged here".
- **`layer-intro-author` on L0 bootstrap bundle 5** unblocked — cycle-008 wave-1 pass 3 (L0 bundle 4) opened OQ `l0-bundle-5-candidates` enumerating remaining candidates: `mpi-globalsum-and-collectives` (Conventions), `tests-as-semantic-supplement` (Conventions — methodology cross-reference), `preconditioner-classes-overview` (Overload-sets-and-class-interfaces). `linalg-vector-file` and `linalg-iterative-file` and `linalg-operator-file` already firm. Citation: open question `l0-bundle-5-candidates` (cycle-008).
- **`harvester` on `eigsolve` L1 rough-in** unblocked — cycle-008 L0 bundle 4 landed `eigensolver-wrapper.md` which is the L0 anchor for an L1 `eigsolve` operator (the eigenmode-solver analog of cycle-007's `ksp_solve`). Test-coverage constraint flagged: `eps.hpp` is a real abstract base but instantiation tests are sparser than Krylov-solver tests; harvester should pre-check `reference/palace/test/unit/` for eigensolver tests before promotion. Citation: open question `eigsolve-l1-operator-rough-in-candidate` (cycle-008).
- **`harvester` on `matrix-weighted-norm` and `bilinear-form` L1 rough-ins** unblocked — surfaced by cycle-008 L0 bundle 4 dispatch (`nrm2_weighted`, `dot_bilinear`, `power_iterate` candidates per OQ `matrix-weighted-norm-and-bilinear-form-l1-rough-ins`). These extend the BLAS-1-like L1 cohort with bilinear/quadratic-form-aware analogs needed by AMS / curl-curl projector / multigrid level recurrences (per roadmap shared-infrastructure section). Lower priority than `eigsolve` (less direct test coverage to verify); rough-in only.
- **`abstractor` (or `lifter`) on upstream `gmres.md §L4` v0.6 → v0.7 self-rotation** unblocked-but-deferred — cycle-008 wave-1 pass 5 landed `gmres-inner-loop-iterate-while-migration` as a rough-in L4>L3 theme whose firming requires the upstream gmres.md to be re-rendered with the inner Arnoldi loop expressed via `iterate_while`. Per the report's caveat 1, this is a downstream dependency on existing slice-vertical content (`book/src/spec/slices/gmres.md`) which is preserved-as-raw-material. Routing the upstream rotation as a cycle-009+ dispatch (`abstractor` or `lifter`) is feasible but not a single-cycle effort — gmres.md is one of the most-elaborate slice docs. Lower priority than the mechanical L3>L2 promotion above; not blocking.
- **MCP codemap rollout decision** scheduled — deferred to cycle-009 meta-phase per user directive (cycle-008 ran no new pilot; cycle-007 pilot result `permission-denied` carried forward unchanged). Cycle-009 planner does NOT yet treat MCP tools as preferred for C++ source-localization.

### New dependencies

- **L4>L3 `krylov-step-typed-wrapper-dissolution` (firm, promoted cycle-008 wave-1 from rough-in)** — first L4>L3 theme promoted to firm via a lifter dispatch. The promotion landed: two-form pruned/unpruned sketch + L3-side Law-1 image reduction rule + new Condition 5 + 10-citation `verified_against:` YAML block (trailing-YAML placement per L1>L0 precedent). Downstream effect: cycle-007's L3>L2 `krylov-step-body-identity` (status `firm-rough-in`) is now eligible for status-inheritance promotion to plain `firm`.
- **L1>L0 `ksp-solve-mutation-rotation` (firm, cycle-008 wave-1)** — first L1>L0 theme for a constructed-operator-absorption operator. 4 sub-patterns (A: workspace allocation; B: initial-guess threading; C: statistics-counter mutation; D: convergence-warning `Mpi::Warning` logging) × {CG, GMRES, FGMRES} outer-loop variants. Decomposes into firm sister-theme primitives ([`apply-linop-mutation-rotation`](../L1-L0/apply-linop-mutation-rotation.md), [`axpby-mutation-rotation`](../L1-L0/axpby-mutation-rotation.md), [`axpbypcz-mutation-rotation`](../L1-L0/axpbypcz-mutation-rotation.md)) per-step. Mutation-rotation cohort grows from 3 themes (axpby + apply-linop + axpbypcz) to 4 themes (+ ksp-solve). Displaced `(empty — Phase B skeleton.)` placeholder in `L1-L0/index.md` with first firm dep-map table.
- **L4>L3 `gmres-inner-loop-iterate-while-migration` (rough-in, cycle-008 wave-1)** — second L4>L3 theme. Cohort grows from 1 firm theme to 1 firm + 1 rough-in (2 themes total). Rough-in dependency on upstream `gmres.md §L4` self-rotation.
- **14 L0 reference-note chapters total** (cycle-005 bundle 1 = 6 + cycle-006 bundle 2 = 2 + cycle-007 bundle 3 = 3 + cycle-008 bundle 4 = 3). 3 new chapters this cycle: `eigensolver-wrapper` (Overload-sets-and-class-interfaces), `par-types-single-rank-reading` (Conventions), `linalg-operator-file` (File overviews). Backlinks: `eigensolver-wrapper` references `kspsolver-base-class` / `apply-linop-overload-set` / `mutable-workspace-pattern`; `par-types-single-rank-reading` references `mfem-vector-types` / `apply-linop-overload-set` / `transparent-vs-load-bearing-tricks`; `linalg-operator-file` references `apply-linop-overload-set` / `mfem-vector-types` / `mutable-workspace-pattern` / `par-types-single-rank-reading`. Sibling-new bidirectional references resolve mutually.
- **L4 Vocabulary cohort subsection landed (cycle-008 wave-2)** — second instance of the cohort-subsection template (first was cycle-004 L1 precedent). L4-specific template adaptation: middle slot uses L4>L3 cross-layer themes rather than rough-in same-layer operators (L4 has no rough-in operators currently). Routes to cycle-009+ meta-phase for precedent-setting consideration under suggested slug `vocabulary-cohort-middle-slot-cross-layer-adaptation`.
- **L4 dep-map column split landed (cycle-008 wave-2)** — `L4/index.md` dep-map widened 4→5 columns with new `Lowers to` column splitting cross-layer L4>L3 theme references out of the previously-overloaded `Dependencies` cell. L4-specific in this dispatch; could be back-applied to L1/L2/L3 dep-maps for consistency (routes to cycle-009+ under suggested slug `dep-map-lowers-to-column-back-application`).
- **L1/index.md cross-link in Status column** — first instance of a cross-layer markdown link living in the dep-map Status column rather than the Dependencies column (the `ksp_solve` row's Status cell now embeds `(L1>L0: [ksp-solve-mutation-rotation](...), cycle-008)`). Cycle-008 wave-2 pass 6 explicit caveat: novel cross-column shape; structural fix (separate `Lowering` column for L1/L2/L3 dep-maps) is the alternative path if rendering becomes awkward, but build verified the parenthetical-in-Status form renders cleanly.

### Resolution implications

- **`iterate-while-l3-rendering-trajectory-accumulation-gap`** (cycle-006) — **closed** (status flipped `open` → `answered`; `answered_at: cycle-008`). Cycle-008 wave-1 lifter (pass 2) landed the substantive §3.8-citation patch + new Condition 5 + two-form sketch + 10-citation `verified_against:` YAML block. **First closure of a 2-cycle-carried OQ via direct lifter-driven artifact landing.** Significant structural milestone: validates the wave-2 audit-only → wave-1 lifter follow-up pattern across cycle-006 → cycle-007 → cycle-008.
- **`eigensolver-wrapper-l0-bundle-4-candidate`** (cycle-007) — **closed**. Cycle-008 wave-1 pass 3 authored `book/src/L0/eigensolver-wrapper.md` covering all three concrete branches (RCI / shell-matrix / direct-Newton), the 22 virtuals on `EigenvalueSolver`, the `palace/models/modeeigensolver.cpp:1029-1047` dispatch site, and the workspace pattern Category-3 backlinks. A follow-up OQ `eigsolve-l1-operator-rough-in-candidate` carries forward the L1 harvester routing.
- **`gmres-inner-loop-iterate-while-migration`** (cycle-007) — **status flipped to `answered-by-rough-in-theme`**. Cycle-008 wave-1 pass 5 authored the rough-in L4>L3 theme covering migrated L4 form + L3 form + 4-axis variant pass-through. Theme is rough-in because dependent on an upstream gmres.md §L4 self-rotation that has not been authored; firming requires a subsequent lifter dispatch on `gmres.md §L4`. Three follow-up design caveats recorded inline in the new theme (alternative-combinator choice; `check_stop_into_carry` helper-promotion decision; Form-B confirmation) — routed to cycle-008+ lifter / harvester / combinator-miner respectively.
- **`l1-intro-refresh-after-constructed-operator-gate`** (cycle-007) — **closed**. Cycle-008 wave-2 pass 6 added closing-the-loop pointer at motif 4 + dep-map `ksp_solve` row Status-cell annotation + Working Notes bullet. Originally-asked-for Context paragraph proved unnecessary (current intro is correct; cohort/motif self-explanatory) — surgical-3-edit closure rather than broader rewrite.
- **`l4-layer-intro-refresh-unblocked-by-first-firm-row`** (cycle-006) — **closed**. Cycle-008 wave-2 pass 7 landed: Semantics-overlay placeholder replaced with grounded 4-motif overlay; new `## Vocabulary cohort` subsection (template adapted for L4); dep-map widened 4→5 columns. Two-cycle-carried OQ (cycle-006 opened, cycle-007 deferred, cycle-008 closed).
- **`ksp-solve-mutation-rotation-l1-l0-theme`** (cycle-007) — **closed at finalize** (pass-4 abstractor authored the L1>L0 theme but missed flipping OQ status; finalize completed the closure with explicit closure paragraph naming the 4 sub-patterns).
- **5 OQs closed cycle-008 + 1 missed-and-finalize-closed = 6 total**. 4 new OQs opened (1 critical-for-meta + 3 routine). **Net ledger change: −2 OQs.** First net reduction in several cycles.
- **`abstractor-write-authority-violation-cycle-008`** (cycle-008, critical-for-meta) — **open**. Pattern-watching for cycle-009 meta-phase: if the write-authority violation pattern recurs in cycle-009, role-spec wording at `.claude/agents/abstractor.md:23` needs more prominent placement; if single-instance, treat as a one-off and close at meta-aggregation. Three sub-questions enumerated: role-spec wording prominence (top vs middle); pre-dispatch fence-header priming for opus-class agents; integrator-per-report safety gate to detect uncommitted-but-modified `book/` files at dispatch start.
- **Cycle-009 meta-phase batch aggregation** fires after cycle-009 finalize per 3:1 cadence. Critical OQ `abstractor-write-authority-violation-cycle-008` is the highest-priority meta-batch-1 aggregation target; other cycle-008 observations (vocabulary-cohort middle-slot template-shape adaptation, dep-map `Lowers to` column back-application, append-before-Dropped open-questions section drift, plain-text-when-anchor-created-same-dispatch convention question) also surface for batch consideration.

### Suggested next dispatches

- **(CYCLE-009 mechanical follow-up)** (`lifter` or `integrator-per-report`, `book/src/L3-L2/krylov-step-body-identity.md` status promotion) — promote `firm-rough-in` → plain `firm` via status-inheritance now that upstream L4>L3 `krylov-step-typed-wrapper-dissolution` firmed cycle-008. Single `Status:` cell edit + brief inheritance-note paragraph. Smallest-cost cycle-009 dispatch.
- (`layer-intro-author`, `L0 bootstrap bundle 5`) — candidates per cycle-008 OQ `l0-bundle-5-candidates`: `mpi-globalsum-and-collectives` (Conventions), `tests-as-semantic-supplement` (Conventions — methodology cross-reference), `preconditioner-classes-overview` (Overload-sets-and-class-interfaces). `linalg-vector-file` / `linalg-iterative-file` / `linalg-operator-file` are all firm. Continues priority #10.
- (`harvester`, `eigsolve @ L1`) — per cycle-008 OQ `eigsolve-l1-operator-rough-in-candidate`. L0 anchor `eigensolver-wrapper.md` firm cycle-008; test-coverage constraint flagged (pre-check `reference/palace/test/unit/` for eigensolver tests before promotion). Rough-in or firm depending on test evidence.
- (`harvester`, `matrix-weighted-norm` / `bilinear-form` L1 rough-ins) — per cycle-008 OQ `matrix-weighted-norm-and-bilinear-form-l1-rough-ins`. Candidates: `nrm2_weighted :: (x, B) → √(xᴴ B x)`, `dot_bilinear :: (x, M, y) → xᴴ M y`, `power_iterate :: (A, x_init) → λ_dom`. Rough-in only; extends BLAS-1-like cohort with bilinear-aware analogs for AMS / curl-curl projector / multigrid.
- (`abstractor` or `lifter`, `gmres.md §L4 v0.6 → v0.7 self-rotation`) — upstream rotation that would firm cycle-008's rough-in L4>L3 `gmres-inner-loop-iterate-while-migration` theme. Large dispatch (gmres.md is among the most-elaborate slice docs); cycle-009+ candidate at higher priority than the matrix-weighted-norm rough-ins.
- (`lifter` or `combinator-miner`, `check_stop_into_carry` promotion decision) — recorded in `gmres-inner-loop-iterate-while-migration.md` §Status caveat (b): promotion criterion is "defer until a second slice needs it." If cycle-009 finds another GMRES-family slice (FGMRES, IRBL, IDR(s)) with the same pattern, promote; else defer.
- (`same-layer-cross-cutter` or `meta-phase`, `open-questions.md section-drift cleanup`) — observation from cycle-008 pass 4: new `open` OQs appended immediately before `## Dropped` end up structurally inside `## Answered` section. Per-OQ YAML frontmatter is the authoritative status source, so this is not blocking, but a future cleanup pass could re-sort entries into their correct sections or formalize that new OQs append at the tail. Cycle-009+ meta-phase candidate.
- **CRITICAL cycle-009 meta-phase aggregation targets**:
  - OQ `abstractor-write-authority-violation-cycle-008` — pattern-watching; if recurs cycle-009, role-spec at `.claude/agents/abstractor.md:23` needs prominence boost.
  - OQ `vocabulary-cohort-middle-slot-cross-layer-adaptation` (suggested slug; recorded in pass-7 caveat 2 not yet promoted as standalone) — decide whether L4's middle-slot adaptation is one-off or a precedent for L2/L3 (where same-layer rough-ins may also be absent).
  - OQ `dep-map-lowers-to-column-back-application` (suggested slug; recorded in pass-7 caveat 3 not yet promoted) — decide whether L1/L2/L3 dep-maps should also split into `Dependencies` + `Lowers to` columns for consistency.
  - L4>L3 plain-text-when-anchor-created-same-dispatch convention question (suggested via pass-5 staging note) — friction-ledger entry `rough-in-rows-must-be-plain-text-when-anchor-missing` could be tightened to "plain-text iff anchor not in same dispatch" without harm; conservative form is also fine.
- **MCP codemap rollout decision** — deferred to cycle-009 meta-phase per user directive. Cycle-009 planner does NOT yet treat MCP tools as preferred.

### Wave-conflict observations

- **Second wave-1 + wave-2 multi-dispatch ordering under split integrator** (cycle-006 first single wave-2; cycle-007 first single wave-2; cycle-008 first **two-dispatch** wave-2). 5-wave-1 + 2-wave-2 splits cleanly; wave-2's L1/index refresh + L4/index refresh both depend on wave-1 landings (L1>L0 ksp-solve theme for the L1 motif-4 closing + L4>L3 typed-wrapper-dissolution firm-promotion for the L4 Vocabulary cohort middle slot) which materialized in the same cycle. **Validates 5+2 wave shape for split-integrator under the 12-cap raised mid-cycle-006.**
- **SUMMARY.md again a convergence point** (3 of 7 dispatches edited it — passes 3, 4, 5). Zero collisions across 4 surgical inserts (L0 Part: 3 lines bundled; L1>L0 Part: 1 line; L4>L3 Part: 1 line). Per-report serial re-read discipline held cleanly. Pattern continues from cycle-005 (5/6) / cycle-006 (4/5) / cycle-007 (5/6).
- **open-questions.md touched 6 times this cycle** (passes 2, 3, 4, 5, 6, 7) at 6 distinct line ranges. Zero collisions; convention of appending new OQs before `## Dropped` (structurally within `## Answered` section) held cleanly per-OQ YAML status authority. Drift observation surfaced (recommended cleanup pass routed to cycle-009 meta-phase).
- **Index-placeholder displacement pattern applied once** (cycle-008 pass 4: L1-L0/index.md placeholder → first firm dep-map table). Fourth such displacement total (cycle-006 L4/index + cycle-006 L4-L3/index + cycle-007 L3-L2/index + cycle-008 L1-L0/index). **Pattern stable across 4 instances; cycle-009 meta-phase formalization candidate** (carry-over from cycle-007 wave-conflict observations).
- **First write-authority violation under split integrator** — pass 4 abstractor wrote directly to `book/` during dispatch (3 files: `L1-L0/ksp-solve-mutation-rotation.md` + `L1-L0/index.md` + `SUMMARY.md`). Repairer executed Option A clean discipline restoration: `git checkout --` to revert + `rm` to remove directly-created theme file + co-locate as report supporting doc + rewrite CYCLE.md into canonical proposed-changes blocks. Re-applied cleanly via canonical pipeline. **Critical OQ `abstractor-write-authority-violation-cycle-008` promoted for cycle-009 meta-phase pattern-watching.** Single-instance ≠ pattern, but the role-spec wording-prominence question is real (role spec wording is at `.claude/agents/abstractor.md:23`, not top-of-file).
- **First L4>L3 rough-in → firm promotion via lifter** — pass 2 promoted `krylov-step-typed-wrapper-dissolution`. First time the lifter role has been used as the promotion vehicle for a multi-cycle-carried theme. Lifter ate the work the cycle-007 wave-2 lowering-verifier audit-only dispatch flagged. Validates the audit-only → lifter sequence as a clean two-cycle promotion pattern.
- **First multi-OQ-closure cycle under split integrator** — 6 OQs closed (5 by per-report dispatches + 1 by finalize) in a single cycle. Previous cycles closed ≤2 per cycle.
- **No deferrals, no rejections, no rework loops.** All 7 reports applied as-is. Continues the clean-run streak (cycle-005 / 006 / 007 / 008).

### Integration-tooling friction

- **mdbook-linkcheck2-fails-on-rough-in-anchor-missing recurrence check: NEGATIVE** — cycle-008 wave-1 pass 5 correctly used plain-text-with-rough-in-parenthetical form for the `gmres-inner-loop-iterate-while-migration` row in `L4-L3/index.md` even though the anchor file was created in the same dispatch (per the report author's conservative reading of cycle-006 friction-ledger discipline). Wave-2 pass 7's italic-suffix annotation `*(rough-in; landed cycle-008 wave-2)*` for the same theme in `L4/index.md` Vocabulary cohort + dep-map `Lowers to` column used markdown-link form because the anchor file existed at write-time. **Convention question surfaced**: discipline could be tightened to "plain-text iff anchor not in same dispatch" without harm; routes to cycle-009 meta-phase.
- **per-report `integrated_at:` write-authority drift: 0 recurrences** — all 7 per-report dispatches deferred correctly to finalize per meta-phase-enacted role-spec clarification (cycle-006 enacted; cycle-007 zero recurrences; cycle-008 zero recurrences). Friction-ledger entry `integrated-at-write-authority-drift` is markable `addressed` at cycle-009 meta with 3-cycle clean record.
- **NEW: abstractor write-authority drift (different pattern than `integrated_at:`)** — cycle-008 wave-1 pass 4 abstractor wrote directly to `book/` during dispatch (not just `integrated_at:` frontmatter — actual artifact files). Repairer executed Option A discipline restoration successfully, but the pattern is new and warrants meta-phase aggregation. **Single-instance this cycle; if cycle-009 sees a recurrence, prominence boost on role-spec write-authority section is recommended.**
- **legacy log/cycle-008.md name collision** — pre-layered-era `log/cycle-008.md` (2026-05-24 `back gmres — revise`) existed; finalize renamed it to `log/cycle-008-legacy.md` per cycle-005/006/007 precedent. Pattern still working as designed; meta-phase bulk-rename candidate (cycle-006 / 007 carry-over signal — alternative (a)) once cycle-N collisions become predictable noise. Cycle-009 also has a legacy `log/cycle-009.md` waiting; cycle-010+ does not.
- **MCP codemap pilot remained permission-denied** — no new pilot this cycle (cycle-007 result carried forward unchanged). **Rollout decision deferred to cycle-009 meta-phase** per user directive. Cycle-009 planner does NOT yet treat MCP tools as preferred for C++ source-localization.
- **L4 dep-map column-split convention** — cycle-008 wave-2 pass 7 split `L4/index.md` dep-map's `Dependencies` cell into `Dependencies` + `Lowers to` for the L4-specific case. Routes to cycle-009+ for back-application decision to L1/L2/L3 dep-maps. Mechanical work if decided; observation only this cycle.
- **L1/index.md cross-link in Status column novelty** — cycle-008 wave-2 pass 6 embedded a markdown link in the `ksp_solve` row Status cell (parenthetical pointing at the L1>L0 theme). First instance of cross-layer cross-link in the Status column rather than Dependencies column. Build verified clean render; structural fix (separate `Lowering` column) is the alternative path if rendering becomes awkward in future, but no immediate friction.
- **No new safety-net gates needed.** Cycle-005/006/007/008 gate set held cleanly across 7 wave-mates with 5+2 wave-1/wave-2 ordering. Zero retroactive-budget hits (no retroactive sweeps this cycle — forward-frontier + polish refresh). Zero edge-label drift. Zero forward-edge claims without surface. Zero variant-axis-missing. One index-placeholder displacement (applied-discretionarily; cycle-006 precedent + cycle-009 meta-phase formalization recommended — 4-instance carry-over).

---

## cycle-007 — 2026-05-27T171702Z

**Meta-batch context**: first primary cycle of meta-batch-1 under the new 3:1 meta cadence (cycles 007/008/009 form batch-1; meta-phase fires after cycle-009 finalize, NOT after this cycle).

### Unblocked

- **`lifter` on `book/src/L4-L3/krylov-step-typed-wrapper-dissolution.md`** unblocked (CYCLE-008 PRIORITY) — cycle-007 wave-2 lowering-verifier delivered verdict (c) closing the iterate-while L3 trajectory-collapse gap conceptually. The substantive patch needs lifter authority: apply (a) Change 2 `verified_against:` 10-citation block (trailing-YAML-block placement per L1-L0 precedent `apply-linop-mutation-rotation.md:353` / `axpby-mutation-rotation.md:173`); (b) Change 3 substantive §3.8-citation patch at §"What the L3 form for iterate_while looks like" — cite Law 1 + `concepts/derived-view-hoisting.md` §"Worked example: CG residual norm", replace 9-line code-block sketch with two-form sketch (pruned vs unpruned), add Condition 5 to §"Applicability conditions". Low-cost single-file edit. Upon landing, OQ `iterate-while-l3-rendering-trajectory-accumulation-gap` becomes closeable; cycle-007's L3>L2 `krylov-step-body-identity` firm-rough-in auto-promotes to plain `firm` via status-inheritance. Citation: cycle-007 wave-2 STAGING row + OQ verdict.
- **`abstractor` on `ksp_solve @ L1>L0`** unblocked — first L1 entry with a structured opaque primary argument (`KspSolver<OperType>*` typed pointer). Theme would document the constructed-operator-absorption + workspace-pattern lowering rotation. Citation: open question `ksp-solve-mutation-rotation-l1-l0-theme` (cycle-007).
- **`layer-intro-author` on L1/index.md refresh** unblocked — `ksp_solve` introduced Vocabulary cohort 7→8 + new Semantics motif 4 (Constructed-operator absorption). L1 intro Context bullets, Semantics motifs, and Working Notes already updated by report #2; a follow-up dispatch could refresh the cross-cutting framing now that the cohort has reached 8 firm entries spanning two distinct motif categories. Citation: open question `l1-intro-refresh-after-constructed-operator-gate` (cycle-007).
- **`same-layer-cross-cutter` on 5 L0 chapters carrying stale forward-declaration italic notes** unblocked — cycle-007 L1 thinning sweep flagged stale `*Forward-declared; L1 pages will be thinned to reference this chapter in the cycle-006 retroactive-thinning sweep (priority #11).*` notes in 5 L0 chapters (now post-sweep). Targets: `output-arg-vs-receiver.md:36`, `mfem-vector-types.md:42`, `linalg-free-functions.md:47`, `transparent-vs-load-bearing-tricks.md:34`, `apply-linop-overload-set.md:55`. Bundlable into one short housekeeping dispatch. Citation: cycle-007 report #3 STAGING notes.
- **`layer-intro-author` on L0 bootstrap bundle 4** unblocked — eigensolver-wrapper candidate identified by cycle-007 report #1 OQs. Citation: open question `eigensolver-wrapper-l0-bundle-4-candidate` (cycle-007).
- **`abstractor` on GMRES-inner-loop iterate-while migration** unblocked — both iterate-while L4 anchors now firm; GMRES inner Arnoldi loop's predicate-in-body pattern is a natural migration target. Citation: open question `gmres-inner-loop-iterate-while-migration` (cycle-007).
- **MCP codemap rollout decision** scheduled — deferred to cycle-009 meta-phase per user directive (cycle-007 pilot result: permission-denied; rollout not actionable until cycle-009 meta-phase aggregates evidence). Cycle-008 planner does NOT yet treat MCP tools as preferred for C++ source-localization.

### New dependencies

- **L4 `iterate_while` (firm) + `iterate_while_with_prev` (firm)** depend on 4 concept-page entries each (`solve-monad`, `derived-view-hoisting`, `convergence-test`, `first-iteration-unrolling`). Second instance of the concept-deps-on-L4-rows convention established cycle-006 (krylov-step). The `l4-row-vs-concept-dependency-convention` OQ (cycle-006) now has 3 L4 firm chapters following the pattern; meta-phase / future L4 harvesters can either ratify the convention or shift to L4-rows-must-depend-on-L4-rows.
- **L4 `krylov-step` dep-map row's Dependencies cell extended** to list `iterate_while` + `iterate_while_with_prev` as 2 new L4-row dependencies (Form A body folds via iterate_while; Form B body uses iterate_while_with_prev). Re-anchors cycle-006's cross-row dependency from concept-pages to L4-rows for the iterate-while case specifically. **First L4-row-on-L4-row dependency edge in the artifact.**
- **L3>L2 `krylov-step-body-identity` (firm-rough-in)** depends on L4 `krylov-step` (firm, cycle-006), L4>L3 `krylov-step-typed-wrapper-dissolution` (rough-in, cycle-006), L2 `krylov-step` (firm, cycle-005), plus 5 concept pages. **First L3>L2 theme** in the artifact; first instance of the `firm-rough-in` status pattern (inherits upstream theme's `rough-in` status until the upstream firms).
- **L1 `ksp_solve` (firm)** depends on L0 `kspsolver-base-class.md` (cycle-006), L0 `ksp-factory-file.md` (cycle-005), and concept pages `solve-monad`, `solver-as-operator`, `constructed-operators`, `variant-absorption`, `constructed-operator-factory`, `ksp_solve`. **First L1 operator with a structured opaque primary argument** (`KspSolver<OperType>*` typed pointer). Introduces the Constructed-operator absorption motif at L1 (Semantics motif 4).
- **11 L0 reference-note chapters total** (cycle-005 bundle 1 = 6 + cycle-006 bundle 2 = 2 + cycle-007 bundle 3 = 3). New L0 grouping rows distributed across Conventions / File overviews / Overload sets and class interfaces (the 3 new chapters split across two of the three groupings).
- **L4 firm cohort growth 1→3** (`krylov-step` cycle-006 + `iterate_while` cycle-007 + `iterate_while_with_prev` cycle-007). Vocabulary-cohort subsection template at L4 is now eligible (3 firms ≥ threshold per cycle-004 L1 precedent); the `l4-layer-intro-refresh-unblocked-by-first-firm-row` OQ (cycle-006) now has expanded scope.

### Resolution implications

- **`iterate-while-l4-anchor-missing`** (cycle-006) — **answered**. Both L4 anchor files landed cycle-007 wave-1 report #4 with firm typing, 3+2 variant axes, and chapter shape matching the cycle-006 krylov-step precedent. Closure recorded in OQ frontmatter.
- **`krylov-step-body-identity-theme-pending-cycle-007`** (cycle-006) — **closed**. Theme authored at `book/src/L3-L2/krylov-step-body-identity.md` with `empirical-match` justification kind; first L3>L2 firm-rough-in entry; ratifies cycle-006 audit verdict. Status auto-promotes to plain `firm` when upstream L4>L3 theme firms.
- **`l1-ksp-solve-firm-up-anchor-ready`** (cycle-006) — **answered**. L1 entry `book/src/L1/ksp_solve.md` landed cycle-007 wave-1 report #2 with 3 exposed + 1 collapsed variant axis, Constructed-operator absorption motif registration, and firm-up of the L0-anchor → concept-page → L1 chain.
- **`mfemwrappersolver-l0-coverage-candidate`** (cycle-006) — **answered**. L0 chapter `book/src/L0/mfem-wrapper-solver.md` landed cycle-007 wave-1 report #1 covering the preconditioner-side adapter class. Now anchored.
- **`iterate-while-l3-rendering-trajectory-accumulation-gap`** (cycle-006) — **partially-answered, NOT closed** (user directive). Wave-2 lowering-verifier delivered verdict (c): the gap is closeable by citing `derived-view-hoisting.md` §3.8 + adding a new Condition 5 + replacing the single-readout sketch with a two-form pruned/unpruned sketch. The body-augmentation paragraphs (cycle-007 update + cycle-007 wave-2 verdict) record the conceptual closure rationale. **Closure proper is gated on the cycle-008+ lifter dispatch landing the substantive §3.8-citation patch** at `book/src/L4-L3/krylov-step-typed-wrapper-dissolution.md`. Final status flip from `open` → `closed` happens at that lifter's per-report integration.
- **Priority #11 retroactive-L1-context-thinning** — **substantively progressed** (8 of 7 firm L1 chapters swept this cycle; the only firm L1 chapter NOT swept is `ksp_solve` itself, which is newly-firm cycle-007 and authored post-sweep with thin Context section by construction). ~55% net Context-section shrink across the 7 swept chapters per repairer's recount.
- **`l4-row-vs-concept-dependency-convention`** (cycle-006) — **needs-more**. 3 L4 firm chapters now use concept-page deps (krylov-step cycle-006, iterate-while + iterate-while-with-prev cycle-007). Plus first L4-row-on-L4-row dep edge landed (krylov-step → iterate-while). Convention seems stable; meta-phase ratification appropriate for cycle-009 meta.

### Suggested next dispatches

- **(CYCLE-008 PRIORITY)** (`lifter`, `book/src/L4-L3/krylov-step-typed-wrapper-dissolution.md`) — apply wave-2 audit's Change 2 (`verified_against:` block) + Change 3 (substantive §3.8-citation patch + Condition 5 + two-form sketch). After landing, OQ `iterate-while-l3-rendering-trajectory-accumulation-gap` becomes closeable. A `rough-in` → `firm` status promotion would naturally subsume both Changes 2+3 AND cascade-promote cycle-007's L3>L2 `krylov-step-body-identity` from `firm-rough-in` → `firm` via status-inheritance.
- (`abstractor`, `ksp_solve @ L1>L0`) — first L1>L0 theme for a constructed-operator-absorption operator. Closes OQ `ksp-solve-mutation-rotation-l1-l0-theme` (cycle-007).
- (`layer-intro-author`, `L1/index.md refresh post-ksp_solve`) — refresh after ksp_solve introduced Constructed-operator-absorption motif (motif 4) + Vocabulary cohort 7→8. Closes OQ `l1-intro-refresh-after-constructed-operator-gate` (cycle-007).
- (`same-layer-cross-cutter`, `5 L0 chapters with stale forward-declaration notes`) — bundle into one short housekeeping dispatch. Targets: `output-arg-vs-receiver.md:36`, `mfem-vector-types.md:42`, `linalg-free-functions.md:47`, `transparent-vs-load-bearing-tricks.md:34`, `apply-linop-overload-set.md:55`. The notes flag completed work (cycle-007 L1 thinning landed); rewording or removal is mechanical.
- (`layer-intro-author`, `L0 bootstrap bundle 4`) — eigensolver-wrapper candidate per OQ `eigensolver-wrapper-l0-bundle-4-candidate` (cycle-007). Continues priority #10. Other remaining candidates from cycle-006 backlog: `mpi-globalsum-and-collectives`, `par-types-single-rank-reading`, `linalg-operator-file`, `tests-as-semantic-supplement`.
- (`abstractor`, `GMRES-inner-loop iterate-while migration`) — both iterate-while L4 anchors now firm; GMRES inner Arnoldi loop's predicate-in-body pattern is a natural migration target. Per OQ `gmres-inner-loop-iterate-while-migration` (cycle-007).
- (`layer-intro-author`, `L4/index.md refresh post-3-firm-cohort`) — Vocabulary-cohort subsection template at L4 now eligible (3 firms ≥ threshold per cycle-004 L1 precedent). The `l4-layer-intro-refresh-unblocked-by-first-firm-row` OQ (cycle-006) has expanded scope.
- (`lowering-verifier` or `abstractor`, `iterate_while log-effect-vs-trajectory channel`) — orthogonal follow-up per new OQ `iterate-while-log-effect-vs-trajectory-channel` (cycle-007). Lower-priority than the §3.8-citation patch; not blocking. Surfaces during meta-phase methodology review of the L4 monad surface (cycle-009 meta consideration if no per-cycle dispatcher picks it up earlier).
- (`same-layer-cross-cutter`, `concepts/index.md full Kind-classification audit`) — cycle-006 OQ `concepts-index-kind-classification-full-audit`. Bounded scope (42 rows × short pages). Not blocking; still queued from cycle-006 signals.
- **MCP codemap rollout decision** — deferred to cycle-009 meta-phase per user directive. Cycle-008 planner does NOT yet treat MCP tools as preferred.

### Wave-conflict observations

- **Second wave-1 + wave-2 dispatch ordering under split integrator** (cycle-006 first; cycle-007 second). Wave-2 lowering-verifier depended on wave-1 harvester's iterate-while L4 chapters. Per-report serial dispatch order honoured (STAGING.md rows 1-5 then row 6). The OQ `iterate-while-l3-rendering-trajectory-accumulation-gap` was augmented by both wave-1 dispatch 4 and wave-2 dispatch 6; per-report integrator correctly extended-rather-than-overwrote the pre-existing paragraph. **Validates extend-pattern for OQ body augmentations across in-cycle wave-1+wave-2 mate-pairs.**
- **L4 dep-map promotion (rough-in → firm) coupled with row-extension** worked cleanly. Wave-1 dispatch 4's edits both (a) promoted 2 rough-in rows to firm and (b) extended the existing `krylov-step` row's Dependencies cell. Both changes self-contained in the same per-report apply; no inter-row collision risk.
- **SUMMARY.md again a convergence point** (5 of 6 dispatches edited it — same pattern cycle-005). Per-report serial dispatch + literal-anchor insert discipline → zero collisions across 6 inserts (L0 Part: 3; L1 Part: 1; L4 Part: 2; L3>L2 Part: 1).
- **Index-placeholder displacement pattern (cycle-006 precedent) applied once** — wave-1 dispatch 5 displaced the `(empty — Phase B skeleton.)` placeholder in `book/src/L3-L2/index.md` with the first firm-rough-in row. Third such displacement total (cycle-006 L4/index.md + cycle-006 L4-L3/index.md + cycle-007 L3-L2/index.md). **Methodology question status (cycle-006 raised)**: meta-phase should formalize whether per-report-integrator authority covers index-placeholder displacement or whether it stays discretionary. Recommend formalizing — three uses, zero hiccups.
- **First in-cycle status inheritance** — L3>L2 `krylov-step-body-identity` declared `firm-rough-in` to inherit upstream L4>L3 theme's `rough-in` status. **First instance of cross-edge status inheritance in the artifact.** Auto-promotes to plain `firm` when the cycle-008+ lifter firms the upstream theme. Pattern worth documenting in meta-phase as a layered-stack status convention.
- **No deferrals, no rejections, no rework loops.** All 6 reports applied as-is.

### Integration-tooling friction

- **mdbook-linkcheck2-fails-on-rough-in-anchor-missing recurrence check: NEGATIVE** — cycle-007 wave-1 dispatch 4 correctly promoted the rough-in rows to firm rows WITH anchor files created (rather than leaving rough-in rows pointing at missing files). Meta-phase-enacted role-spec discipline (rough-in rows must use plain-text names, not links, when anchor file does not exist; abstractor.md role spec) held this cycle. Friction-ledger entry can stay `addressed`.
- **per-report `integrated_at:` write-authority drift: 0 recurrences** — all 6 per-report dispatches deferred correctly to finalize per meta-phase-enacted role-spec clarification (`.claude/agents/integrator-per-report.md` Process / "What you DO NOT do" sections). Friction-ledger entry `integrated-at-write-authority-drift` may be markable `addressed` at cycle-009 meta.
- **legacy log/cycle-007.md name collision** — pre-layered-era `log/cycle-007.md` (2026-05-24 `forward gmres [L1→L2] — revise`) existed; finalize renamed it to `log/cycle-007-legacy.md` per cycle-005/006 precedent. Pattern still working as designed; meta-phase candidate for bulk-rename pass (alternative (a) from cycle-006 signals) once cycle-N collisions become predictable noise.
- **MCP codemap pilot permission-denied** — cycle-007 wave-1 dispatch 4 was the designated MCP codemap pilot per priority #16 step (e). The sub-session was unable to invoke `mcp__palace-codemap__*` tools (configured at repo root `.mcp.json` per `ab73d37`). Fallback to vanilla Grep/Read worked correctly; dispatch landed successfully. **Rollout decision deferred to cycle-009 meta-phase** per user directive. Cycle-008/009 dispatches may opportunistically pilot if permission is granted.
- **No new safety-net gates needed.** Cycle-005/006/007 gate set held cleanly across 6 wave-mates with one wave-1+wave-2 ordering. Zero retroactive-budget hits even with the largest cross-slice retroactive sweep this cycle (7 L1 chapters). Zero edge-label drift. Zero forward-edge claims without surface. Zero variant-axis-missing. One index-placeholder displacement (applied-discretionarily; cycle-006 precedent + meta-phase formalization recommended).

---

## cycle-006 — 2026-05-27T090849Z

### Unblocked

- **`iterate_while` / `iterate_while_with_prev` L4 anchor harvest** unblocked — both names are used as load-bearing vocabulary in cycle-006's L4 `krylov-step` entry (Form A/B bodies) and surfaced doubly via (a) the wave-1 harvester's caveat 2 and (b) the wave-2 abstractor's rough-in L4 dep-map row proposals. Cycle-007 candidate: `harvester` on the L4 loop-combinator family. Anchors needed to lift the cycle-006 defanged-to-plain-text dep-map entries back to firm `[name](./name.md)` links. Citation: open question `iterate-while-l4-anchor-missing` (cycle-006).
- **`krylov-step` L3>L2 body-identity theme** unblocked — cycle-006 wave-2 abstractor's audit confirmed-with-refinement the cycle-005 identity-in-form claim for the L3>L2 body rewrite. A short cycle-007 `abstractor` dispatch can author `book/src/L3-L2/krylov-step-body-identity.md` (one-line theme with `empirical-match` justification). Low-cost; slottable alongside the `iterate_while` harvester. Citation: open question `krylov-step-body-identity-theme-pending-cycle-007` (cycle-006).
- **`retroactive-L1-context-thinning` sweep (priority #11)** unblocked — threshold of ≥6 L0 reference-note chapters is now met (8 chapters total post-bundle-2). Cycle-007 candidate: `layer-intro-author` per-operator or single-sweep on the 7 L1 entries (`axpy`, `dot`, `nrm2`, `axpby`, `scal`, `apply_linop`, `axpbypcz`) to replace inline L0-interpretation prose with cross-references to L0 chapters. Distinct from cycle-006's scalar-promotion-specific thinning (priority #9 progressed); priority #11 is the broader L0-interpretation thinning.
- **`mfemwrappersolver-l0-coverage-candidate` L0 bundle-3+ chapter** unblocked — flagged by cycle-006 wave-1 L0 bundle-2 as a future L0 reference-note targeting the preconditioner-side construction surface. Cycle-007+ `layer-intro-author` candidate. Citation: open question `mfemwrappersolver-l0-coverage-candidate` (cycle-006).
- **`l1-ksp-solve` L1 harvest** unblocked — both concept-page and L0-anchor entry points now exist (`kspsolver-base-class` L0 chapter landed cycle-006; `solve-monad` concept already exists). Cycle-007+ `harvester` candidate. Citation: open question `l1-ksp-solve-firm-up-anchor-ready` (cycle-006).
- **MCP codemap reintegration (priority #16)** scheduled post-meta-phase — orchestration-layer work, not a planner dispatch. See mid-cycle directive commit `f661039`.

### New dependencies

- **L4 `krylov-step` (firm) depends on 5 concept-page entries** (`state-stratification`, `solve-monad`, `first-iteration-unrolling`, `derived-view-hoisting`, `convergence-test`) — concept-deps-on-L4-rows convention used as a first-instance precedent; future L4 rows can follow this pattern or, if cycle-007+ decides L4-rows-must-depend-on-L4-rows, the cycle-006 entry's link targets re-anchor without content rewrite. Citation: open question `l4-row-vs-concept-dependency-convention` (cycle-006).
- **L4>L3 `krylov-step-typed-wrapper-dissolution` (firm theme) depends on L4 `krylov-step` (LHS) and L2 `krylov-step` (RHS via L3>L2 identity-in-form)** — first L4>L3 theme; first cross-Part lowering surface in the layered stack to date.
- **8 L0 reference-note chapters total** (cycle-005 bundle 1 = 6 + cycle-006 bundle 2 = 2) — L0 reference-notes overlay is now substantial enough to support the priority #11 retroactive-thinning sweep. Future cross-cutter and lowering-verifier dispatches can target these L0 chapters as anchor sources alongside the citation evidence.
- **L4 dep-map now has 2 rough-in rows beyond the first firm** (`iterate_while`, `iterate_while_with_prev`) — vocabulary cohort pattern (firm + rough-in mixed) reaches L4 for the first time. The cycle-004 L1 Vocabulary-cohort subsection template (from `layer-intro-author` role spec) may want to be invoked on L4 once `iterate_while` firms in cycle-007+ (cycle-006 OQ `l4-layer-intro-refresh-unblocked-by-first-firm-row` tracks this).

### Resolution implications

- **`krylov-step-l3-identity-in-form-audit`** (cycle-005) — **answered**. Cycle-006 wave-2 abstractor audit verdict: **confirms-with-refinement**. The cycle-002 framing "L2>L3 step-body lift is identity-in-form" is confirmed; refined to "L4>L3>L2 step-body chain is identity-in-form on the kernel body's primitive sequence; the L4>L3 hop is non-identity at the wrapper level but the body's dataflow chain survives both hops textually unchanged." See closure-note OQ `krylov-step-l3-identity-in-form-audit-closure-cycle-006`.
- **`krylov-step-l3-row-contingency`** (cycle-006 wave-1) — **answered**. Same audit resolves: the contingency triggered by "non-identity rotation at L3 body" did not fire; the defensive L4 entry's "Lowers to" wording (L4>L3>L2 chain with no interposed L3 row) stands as-is.
- **`krylov-step-dual-placement-l2-l4-routing`** (cycle-005) — **answered**. L4 firm landed (`book/src/L4/krylov-step.md`); L4>L3 theme firm landed (`book/src/L4-L3/krylov-step-typed-wrapper-dissolution.md`); L3>L2 confirmed identity-in-form via audit; no intermediate L3 row promoted. The three named cycle-006 follow-up dispatches (primary harvester, secondary abstractor, tertiary deferrable layer-intro-author) were enacted as wave-1 harvester + wave-2 abstractor; the tertiary deferrable layer-intro-author L4 dep-map refresh is now bounded by the cycle-006 entries and not yet executed (cycle-007 candidate via `l4-layer-intro-refresh-unblocked-by-first-firm-row` OQ).
- **`scalar-promotion-retroactive-l1-thinning`** (cycle-005) — **answered for the 4 L1 entries**. Cycle-006 wave-1 layer-intro-author retroactive-thinned `axpy.md`, `axpby.md`, `axpbypcz.md`, `scal.md` to replace inline scalar-promotion prose with `concepts/scalar-promotion` backlinks. Tail item: `concepts/axpby.md` and `concepts/axpbypcz.md` concept pages not authored, so the concept-pages can't backlink back yet (cycle-007+ via `concepts-axpby-axpbypcz-pages-absent` OQ).
- **Pre-existing `concepts/index.md` duplicate rows** (cycle-005 integrator-signals item) — **answered**. Cycle-006 wave-1 same-layer-cross-cutter deduped both pairs (`complex-from-real-lift` pure copy-paste; `solver-as-operator` `layer-pattern`-kept / `primitive`-deleted).
- **`l4-row-vs-concept-dependency-convention`** (cycle-006) — **needs-more**. The cycle-006 L4 `krylov-step` entry uses concept-page links as deps; convention to be ratified or rejected by meta-phase / future L4 harvester dispatches.

### Suggested next dispatches

- (`harvester`, `iterate_while @ L4`) — closes `iterate-while-l4-anchor-missing` OQ; lifts the cycle-006 defanged plain-text rough-in rows back to firm `[name](./name.md)` linkable entries. Cycle-006's two rough-in dep-map rows in `book/src/L4/index.md` provide signature sketches the harvester can adopt or refine. Likely co-bundle with `iterate_while_with_prev @ L4` as a single dispatch (variant axis is the `PrevCarry` parameter, degenerate when `PrevCarry = ()`).
- (`abstractor`, `krylov-step-body-identity @ L3>L2`) — short single-theme dispatch; one-line `empirical-match` justification per the cycle-006 audit's verdict. Authors `book/src/L3-L2/krylov-step-body-identity.md`. Closes `krylov-step-body-identity-theme-pending-cycle-007` OQ. Low-cost (~half a dispatch budget); slottable alongside the `iterate_while` harvester for symmetric completion of the krylov-step lowering chain.
- (`layer-intro-author`, `retroactive-L1-context-thinning sweep`) — priority #11 now eligible (8 L0 chapters ≥ 6 threshold). Per-operator or single sweep over 7 L1 entries. Distinct from cycle-006's scalar-promotion-specific thinning; broader L0-interpretation thinning (Context sections to L0 chapter references).
- (`layer-intro-author`, `L0 bootstrap bundle 3`) — priority #10 continuation. Remaining candidate chapters: `mpi-globalsum-and-collectives`, `par-types-single-rank-reading`, `mutable-workspace-pattern`, `linalg-operator-file`, `linalg-iterative-file`, `mfem-wrapper-solver` (per cycle-006 OQ `mfemwrappersolver-l0-coverage-candidate`), `tests-as-semantic-supplement`. Cycle-007 planner's call on bundling.
- (`harvester`, `l1-ksp-solve @ L1`) — both concept-page (`solve-monad`) and L0-anchor (cycle-006 `kspsolver-base-class.md`) entry points now exist. Cycle-007+ forward-frontier candidate. Closes `l1-ksp-solve-firm-up-anchor-ready` OQ.
- (`lowering-verifier`, `iterate_while L3 trajectory-accumulation reconciliation`) — flagged by cycle-006 wave-2 abstractor's §"What the L3 form for `iterate_while` looks like" (repairer-deferred substantive rotation decision). Resolves the L4 trajectory `[readout]` vs L3 single-readout gap. Routes to either cycle-007 `lowering-verifier` or folds into the `iterate_while @ L4` harvester dispatch. Citation: open question `iterate-while-l3-rendering-trajectory-accumulation-gap` (cycle-006).
- (`same-layer-cross-cutter`, `concepts/index.md full Kind-classification audit`) — cycle-006 OQ `concepts-index-kind-classification-full-audit`. Bounded scope (42 rows × short pages). Not blocking; cycle-007+ candidate for housekeeping budget.
- **MCP codemap reintegration** (priority #16, post-meta-phase) — orchestration-layer work scheduled per mid-cycle directive commit `f661039`. Not a planner dispatch. Sequence: (a) binary verify/rebuild against current rmcp + tree-sitter deps; (b) `cargo test` smoke against `reference/palace/`; (c) confirm `.claude/mcp.json` registration; (d) update 5 role specs to reference codemap tools as preferred C++ source-localization; (e) pilot on one cycle-007 harvester dispatch; (f) instrument tool-call count vs vanilla baseline; (g) surface results to user before broad rollout.

### Wave-conflict observations

- **First wave-1 + wave-2 dispatch ordering under split integrator** worked cleanly. The wave-2 abstractor depended on wave-1 harvester's L4 entry (its "Lowers to" L4>L3 chain references the abstractor's theme; its L4 dep-map row is appended after the wave-1 firm row). Per-report serial dispatch order honoured (STAGING.md rows 1-4 then row 5). The L4 dep-map at wave-2's edit time already had wave-1's firm row, so wave-2's two rough-in appends went after it cleanly. **Validates the per-report serial-dispatch design at wave-mate-dependency boundaries**, not just across independent wave-1 mates.
- **Index-placeholder displacement convention established cycle-006** — when the first real entry lands under a Part that still carries the "(empty — Phase B skeleton.)" placeholder, the per-report integrator discretionarily replaces the placeholder with the first real table row. Pattern applied twice cycle-006: wave-1 harvester on L4/index.md; wave-2 abstractor on L4-L3/index.md (the latter explicitly cited the former as precedent in its STAGING row notes). **Methodology question for meta-phase**: formalize "first-real-entry placeholder displacement" as a per-report-integrator authority or leave as discretionary practice?
- **No deferrals, no rejections, no rework loops.** All 5 reports `ready` post-repair and applied as-is. Cycle-005's clean run repeats cycle-006.

### Integration-tooling friction

- **mdbook-linkcheck2-fails-on-rough-in-anchor-missing** — NEW friction this cycle. The cycle-006 wave-2 abstractor's two rough-in L4 dep-map rows used `[iterate_while](./iterate_while.md)` markdown links, but those files don't exist yet (rough-in status; cycle-007 OQ `iterate-while-l4-anchor-missing` tracks the anchor pending). mdbook's `linkcheck2` renderer treated these as **errors** (not warnings) and failed the build. **Finalize repair**: defanged to plain-text `iterate_while (rough-in; no anchor yet)` with annotation. **Pattern observation**: rough-in dep-map rows that reference yet-to-exist files should NOT use markdown link syntax. Two candidate resolutions for meta-phase: (a) document this constraint in `layer-intro-author` / `abstractor` role specs ("rough-in rows must use plain-text names, not links, when the anchor file does not exist"); (b) configure mdbook to downgrade missing-link errors to warnings (less surgical; affects all link-checking). Cycle-006 used (a)-equivalent surgical fix at finalize; meta-phase candidate friction-ledger entry.
- **per-report `integrated_at:` write-authority drift** — per-report dispatch #1 (harvester krylov-step L4) set `integrated_at: 2026-05-27T09:00:00Z` in its CYCLE.md frontmatter at per-report integration time, outside CLAUDE.md write-authority partition (which assigns `integrated_at` touches to integrator-finalize). The other 4 per-report dispatches deferred correctly. Finalize overwrote with the actual finalize timestamp (`2026-05-27T09:08:49Z`). **Meta-phase candidate**: role-spec clarification in `.claude/agents/integrator-per-report.md` "Process" or "What you DO NOT do" section to explicitly call out `integrated_at:` is finalize's domain (and add to the staging-log notes that per-report should defer the timestamp).
- **legacy log/cycle-006.md name collision** — pre-layered-era `log/cycle-006.md` (2026-05-24 `back gmres — revise`) existed; would have been overwritten by the layered-era cycle-006.md write. Finalize renamed it to `log/cycle-006-legacy.md` (mirroring the cycle-005 `cycle-005-legacy.md` precedent). **Pattern observation**: legacy slice-era cycle-NNN.md files in `log/` will collide with future layered-era cycle-NNN.md writes for any N ≤ 172. Two candidate resolutions for meta-phase: (a) bulk-rename all pre-pilot-1 cycle-N.md files to `cycle-N-legacy.md` in one pass; (b) handle as encountered (cycle-005 + cycle-006 precedent — works fine but adds a small finalize step). Currently following pattern (b); not blocking.
- **No new safety-net gates needed.** Cycle-005's gate set held cleanly across 5 wave-mates under split integrator. Zero retroactive-budget hits (4 L1 retroactive-thinning edits all at per-slice = 1 per operator, well below threshold). Zero edge-label drift. Zero forward-edge claims without surface (wave-1+wave-2 ordering correctly resolved the krylov-step L4 / L4>L3 mutual reference within-cycle). Zero variant-axis-missing.

---

## cycle-005 — 2026-05-27T070424Z

### Unblocked

- **`krylov-step` L4 dual-placement (3 cycle-006 dispatches)** unblocked — cycle-005 landed `krylov-step` firm at L2 (`book/src/L2/krylov-step.md`); the cross-cutter recommendation routes the L4 row + L4>L3 lowering theme to cycle-006. Three named follow-ups: (a) primary `harvester` on `krylov-step @ L4`; (b) secondary `abstractor` on L4>L3 lowering theme; (c) tertiary deferrable `layer-intro-author` on L4 dep-map. Citation: open question `krylov-step-dual-placement-l2-l4-routing` (cycle-005).
- **`L0 bootstrap bundle 2` (priority #10 continuation)** unblocked — cycle-005 landed bundle 1 (6 chapters + L0/index.md re-framing). Bundle 2 candidates per priority #10 backlog: `apply_linop` overload-set page, `kspsolver-base-class` page, additional file-overview chapters. Routes to `layer-intro-author` next cycle.
- **`scalar-promotion` retroactive L1 thinning** unblocked — the concept page now exists at `book/src/concepts/scalar-promotion.md`; cycle-006+ retroactive backlinking from the 4 L1 entries (axpy, axpby, axpbypcz, scal) can replace ~600 words of per-operator prose with one-line concept backlinks. Citation: open question `scalar-promotion-retroactive-l1-thinning` (cycle-005).
- **`apply-linop` cross-family mutation-rotation theme** unblocked — cycle-005 abstractor on `axpbypcz-mutation-rotation` surfaced the open question `scalar-promotion-mutation-rotation-cross-family-theme` (shared structural pattern across axpy/axpby/axpbypcz/scal mutation-rotation themes). Routes to abstractor in cycle-006+.
- **`axpbypcz` sub-pattern B full-corpus audit** unblocked — open question `axpbypcz-sub-pattern-B-defined-not-used-corpus-audit` names a discrete cycle-006+ harvester or lowering-verifier dispatch.

### New dependencies

- **`krylov-step` (L2 firm) depends on 7 L1 firm operators**: `apply_linop`, `axpy`, `axpby`, `axpbypcz`, `scal`, `dot`, `nrm2`. All firm post-cycle-004; the L2 firm-up was the natural next step. Planner: future L2 operators sharing this vocabulary subset are now unblocked.
- **`scalar-promotion` concept centralises the typing rule across 4 L1 operators**: the per-operator clauses in `axpy.md`, `axpby.md`, `axpbypcz.md`, `scal.md` are now superseded by one canonical concept page. Retroactive-thinning is a follow-up, not load-bearing.
- **`apply-linop-mutation-rotation` (L1>L0)** is the second mutation-rotation theme after `axpby-mutation-rotation`; companion `axpbypcz-mutation-rotation` is the third. Three mutation-rotation themes form a cohort — recognizable cross-family pattern.
- **First `algebraic`+`structural` mixed-justification sub-rule landed** (`axpbypcz-mutation-rotation` γ==0). Tooling implications routed to cross-layer-cross-cutter via `mixed-justification-sub-rule-methodology` OQ.
- **L0 reference-notes overlay precedent** — L0 is now a 2-cohort layer (citations + reference notes); the 6 L0 chapter pages co-exist with the L0=citations baseline. The 4 conventions pages (output-arg-vs-receiver, transparent-vs-load-bearing-tricks, etc.) are referenced FROM L1>L0 themes; the 2 file-overview pages (linalg-vector-file, ksp-factory-file) supply file-scoped context for citation-validity audits.

### Resolution implications

- **`krylov-step-speculative-l1-promotion-decision`** — **answered**. Decision: NOT to promote any of the 6 cycle-004 speculative L1 operators. Decision artifact at `scaffolding/decisions/2026-05-27-krylov-step-speculative-l1-promotion.md`. Reaffirms the unimplemented-Palace-components policy: the L2 `krylov-step` cleanly absorbs the algorithmic variation without speculation-promotion needed.
- **`scalar-promotion-typing-rule`** — **answered** (cycle-002 → cycle-005). The concept page at `book/src/concepts/scalar-promotion.md` lifts the per-operator clauses; retroactive thinning is the follow-up.
- **`L1-index-refresh` triggering for L2**: now that L2 has its first firm operator (`krylov-step`), L2 index-refresh threshold (≥3 firm L2 operators) is NOT yet met — refresh blocked on more L2 work. Cycle-005 dispatch #1 promoted `L2-layer-intro-refresh-for-named-compositions` as a tracking OQ for when threshold hits.
- **`apply-linop-lowering-theme-scope`** (cycle-004) — **answered** by cycle-005 abstractor on `apply-linop-mutation-rotation` (5 sub-patterns A-E land as predicted).
- **`axpbypcz-mutation-rotation-abstractor-target`** (cycle-004) — **answered** by cycle-005 abstractor.
- **`mfem-as-l0-substrate`** (cycle-004 meta-phase ask) — **answered upstream** by user directive 2026-05-27 (commit `fb8239a`). Cycle-005's krylov-step decision artifact records the policy application in practice.
- **`cycle-005-L0-bootstrap` priority #10** — **partially-answered** (bundle 1 of N landed). Continues cycle-006+.

### Suggested next dispatches

- (`harvester`, `krylov-step @ L4`) — primary cycle-006 dispatch per cross-cutter recommendation. Requires `iterate_while`, `state-stratification`, `solve-monad` L4 vocabulary; may co-bundle with L4 dep-map updates.
- (`abstractor`, `L4>L3 lowering theme for krylov-step (krylov-step-as-iterate-while)`) — secondary; can be parallel with the L4 harvester if planner prefers wave.
- (`layer-intro-author`, `L4 dep-map refresh`) — tertiary deferrable; only if cycle-006 actually populates L4 with multiple operators.
- (`layer-intro-author`, `L0 bootstrap bundle 2`) — priority #10 continuation; candidates include `apply_linop` overload-set page, `kspsolver-base-class` page, additional file-overview chapters per bundle planning.
- (`cross-layer-cross-cutter`, `mixed-justification-sub-rule methodology audit`) — first instance of `algebraic`+`structural` sub-rule (axpbypcz γ==0); needs cross-cutter or meta-phase review to decide if it's a new pattern or specific to this theme.
- (`layer-intro-author`, `scalar-promotion retroactive L1 thinning`) — 4 L1 operators backlink to concept page; estimated ~600 words savings.
- (`harvester` or `cross-layer-cross-cutter`, `apply_linop cross-family mutation-rotation theme`) — `scalar-promotion-mutation-rotation-cross-family-theme` OQ; mid-priority.
- (`harvester` or `lowering-verifier`, `axpbypcz sub-pattern B corpus audit`) — `axpbypcz-sub-pattern-B-defined-not-used-corpus-audit` OQ; bounded scope.
- (`same-layer-cross-cutter` or `problems/`, `concepts/index.md duplicate-rows housekeeping`) — `complex-from-real-lift` rows 70-71 and `solver-as-operator` rows 98-99 (pre-existing; surfaced by dispatch #6). Drive-by fix; low-cost.

### Wave-conflict observations

- **SUMMARY.md was the load-bearing convergence point** — 5 of 6 dispatches edited it (L2 + 2× L1>L0 + L0 bundle (6 rows + heading rename) + concepts/scalar-promotion). Per-report serial dispatch order + the "surgical insert preserving append-points" discipline (introduced by dispatch #1's notes and propagated through subsequent dispatch notes) meant **zero collisions**. Each per-report integrator re-read SUMMARY.md fresh and inserted at literal-string anchors. **Validates the per-report serial-dispatch design at 5 SUMMARY-writers** (cycle-004 was 5 writers under single-pass integrator; cycle-005 confirms split-integrator handles it identically).
- **L1>L0 alphabetical ordering self-resolved** — `apply-linop-mutation-rotation` and `axpbypcz-mutation-rotation` independently picked positions relative to existing `axpby-mutation-rotation`; interleaved correctly via per-report serial dispatch (axpbypcz first → apply-linop second, by dispatch order).
- **No deferrals, no rejections, no rework loops.** All 6 reports `ready` post-repair and applied as-is.
- **Cross-reference fix `bicgstab-iteration.md :53-56 → :53-57` folded into L0-bootstrap dispatch** rather than handled as a separate finalize-repair — per dispatch instructions, the fix was directly relevant to L0 bootstrap consistency (the new `ksp-factory-file.md` cites the same range; consistent reference range across `minres-iteration`, `bicgstab-iteration`, `ksp-factory-file`). This pattern (route a small cross-reference fix into the most-related dispatch rather than the finalize) is a useful template: keeps finalize purely housekeeping, keeps the fix's rationale co-located with the relevant content surface.

### Integration-tooling friction

- **`new-agent-defs-need-session-restart`** — first-cycle-under-split-integrator friction. The `integrator-per-report` and `integrator-finalize` agent defs were added in commit `ccc5082`; the session that wrote that commit did NOT see them in the cached agent registry, and the first per-report dispatch attempted before restart returned "Agent type not found". Restart required. **Status `addressed-by-restart`** — but if there's a way to invalidate the cached agent list mid-session (or auto-invalidate on `.claude/agents/` write), that would close it cleanly. Routes to meta-phase as a friction-ledger candidate.
- **Split integrator design VALIDATED on first cycle.** Six per-report dispatches each had bounded scope (one report's proposed-changes + the artifact files that report touched + the staging-log append). The split (vs. cycle-001..004's single-pass integrator that handled all reports + commit in one dispatch) means each per-report dispatch's context budget is bounded by ONE report's scope, not the cycle's total. **No per-report context-bound friction observed.** STAGING.md format usability also PASS — aggregating gate-totals, files-touched, and OQ counts for this finalize was mechanical (read STAGING.md, sum columns). No format changes proposed.
- **SUMMARY-chapter-registration-auto-fix applied discretionarily ONCE** — concepts/scalar-promotion is outside the literal gate scope (gate targets `book/src/L<n>/<slug>.md`), but the existing SUMMARY.md pattern registers nearly all concept pages (~35 entries between lines 64-104). Per-report integrator #6 chose to register for discoverability. **Methodology question for meta-phase**: extend the gate spec to include `book/src/concepts/<slug>.md` (most concepts register), or formalise "discretionary auto-fix" as a per-report-integrator authority? Currently undocumented practice; cycle-005 establishes precedent.
- **Pre-existing `concepts/index.md` duplicate rows** (`complex-from-real-lift` lines 70-71, `solver-as-operator` lines 98-99) NOT addressed by any cycle-005 dispatch — out of scope for each per-report dispatch's edit set. Flagged by dispatch #6 critic + repairer for cycle-006 housekeeping. **Routing this to cycle-planner**: cycle-006 drive-by fix or `problems/` filing.
- **No new gates needed.** Cycle-004's gate set held cleanly for 6 wave-mates under split-integrator. Zero retroactive-budget hits (global aggregate confirmed 0 by this finalize). Zero variant-axis-missing (6 axes / 4 sub-patterns / 3 axes correctly absorbed across the 3 multi-variant reports). Zero edge-label drift. Zero forward-edge claims without surface.

---

## cycle-004 — 2026-05-27T005952Z

### Unblocked

- **`krylov-step` L2 harvester promotion** now fully unblocked — `apply_linop` landed firm at L1 in this cycle. Priority #5 in the planner's bootstrap-L1-vocabulary becomes the natural next forward-frontier work. Citation: `book/src/L1/apply_linop.md`; combinator-miner rough-in at `book/src/L2/index.md`.
- **`apply-linop-mutation-rotation` L1>L0 theme** unblocked — the harvester's own Open question #2 flagged this as substantially larger than `axpby-mutation-rotation` (representation-axis caveats, transpose-mode specialisations, accumulating-form fusion, parallel-wrapper). Routes to abstractor. Citation: open question `apply-linop-lowering-theme-scope` (cycle-004).
- **`axpbypcz-mutation-rotation` L1>L0 theme** unblocked — companion to existing `axpby-mutation-rotation`; first L1>L0 theme to mix structural-rebind with algebraic-constant-folding (the `γ == 0` sub-rule). Routes to abstractor. Citation: open question `axpbypcz-mutation-rotation-abstractor-target` (cycle-004).
- **Concepts sweep over `book/src/concepts/`** unblocked — cycle-004 dot rewrite establishes the pattern template; cycle-005 same-layer-cross-cutter can replay against `concepts/axpy.md`, `concepts/nrm2.md`, `concepts/orthogonalization.md`, etc. Citation: open question `concepts-sweep-cycle-005-candidate` (cycle-004), bundles cycle-003 `concepts-pre-layered-era-sweep`.
- **`nrm2_B` energy-norm L1 harvest** unblocked — depends on `apply_linop` (now firm) and `dot` (firm cycle-002). Citation: open question `nrm2-B-weighted-energy-norm-harvest` (cycle-003).

### New dependencies

- **`apply_linop` is the L2 `krylov-step` gate** — the L2 row's dep list (`apply_linop`, `axpy`, `dot`, `nrm2`) is now fully populated at L1 firm tier. Planner: cycle-005 L2 harvester dispatch is no longer blocked by an L1 vocabulary gap.
- **`axpbypcz` subsumes both `axpby` and `axpy`** as L1 siblings — three-way subsumption chain `axpy ≺ axpby ≺ axpbypcz` recorded as algebraic laws (not dep-map edges). Planner: future L2 fusion patterns over coefficient-update lines should consult the `axpbypcz` Law 12 chained-collapse pattern.
- **Two obstruction L1>L0 themes coexist** — `minres-iteration` and `bicgstab-iteration` are the first themes with `justification kind: obstruction`. New theme category introduced this cycle; tooling implications routed to meta-phase (friction-ledger candidate `advertised-but-unimplemented-krylov-solvers`).
- **`scal` subsumption of `axpby` (β=0)** — formalised as algebraic law; both stay in L1 dep-map as siblings.

### Resolution implications

- **`axpby-axpbypcz-next-harvest`** — **answered**. Both halves now firm at L1 (cycle-003 axpby, cycle-004 axpbypcz).
- **`axpbypcz-l1-harvest`** — **answered** by cycle-004 harvester. Mirror of axpby decision; 12 laws; 1 internal-L0 control-flow axis explicitly non-L1.
- **`scal-primitive-l1-harvest`** — **answered** by cycle-004 harvester. Module-axiom laws + scalar-promotion sub-axis.
- **`l1-index-refresh`** + **`l1-index-refresh-trigger-met`** — both **answered** by cycle-004 layer-intro-author refresh. New "Vocabulary cohort" subsection pattern proposed for meta-phase promotion across L_n intros.
- **`concepts-dot-return-type-correction`** + **`concepts-dot-dotc-and-inverted-conjugation`** + **`dot-backpointer-staleness-after-rewrite`** + **`dot-blas-heritage-framing-salvage`** — all **answered** by cycle-004 concepts/dot rewrite + L1/dot back-pointer softening.
- **`scalar-promotion-typing-rule`** — **needs-more**. Now visible across `axpy`, `dot`, `axpby`, `axpbypcz`, `scal` (5 operators stating the same per-operator clause). Well past any reasonable threshold for promotion above per-operator prose. Cycle-planner should escalate as a high-priority dispatch (`layer-intro-author` or new role) for cycle-005 or cycle-006.
- **`concepts-page-authorship-role-scope`** — **needs-more**. Cycle-004 confirmed `layer-intro-author` can handle concept-page rewrites in practice; meta-phase to decide whether to (a) explicitly broaden the role spec or (b) add a new `concept-page-author` role. Cycle-004 follows the cycle-003 precedent.

### Suggested next dispatches

- (`harvester`, `krylov-step @ L2 firm`) — now unblocked; cycle-002 combinator-miner rough-in awaits promotion. L1 vocabulary fully gates this (all four deps `apply_linop`, `axpy`, `dot`, `nrm2` are firm).
- (`abstractor`, `apply-linop-mutation-rotation @ L1>L0`) — harvester flagged the lowering theme will be substantially larger than `axpby-mutation-rotation` (representation-axis + transpose-mode + accumulate-mode + parallel-wrapper). Closes open question `apply-linop-lowering-theme-scope`.
- (`abstractor`, `axpbypcz-mutation-rotation @ L1>L0`) — companion to existing `axpby-mutation-rotation`; introduces the `γ == 0` algebraic-sub-rule as first instance of algebraic-constant-folding inside L1>L0. Closes `axpbypcz-mutation-rotation-abstractor-target`.
- (`cross-layer-cross-cutter`, `krylov-step layer placement`) — cycle-002 open question; can co-bundle with the L2 firm-up to ensure the L2/L4 dual placement decision is made coherently.
- (`meta-phase`, `mfem-as-l0-substrate-policy ask item`) — surfaces to human: should MFEM be admitted as L0 substrate for the MINRES/BiCGStab obstruction themes (and the future `Householder QR` work)? Routes to meta-phase under `ask` decision-kind.
- (`harvester`, `Householder QR @ L1`) — Shared Infrastructure roadmap item; structurally-distinct variant of MGS/CGS/CGS2. Cycle-005 may attempt as harvester with abstractor-obstruction fallback (cycle-004 MINRES precedent says: grep first).
- (`harvester`, `Jacobi smoother @ L1`) — Shared Infrastructure roadmap item; depends on a "diagonal-preconditioner apply" intermediate.
- (`same-layer-cross-cutter`, `book/src/concepts/ sweep`) — cycle-004 dot rewrite is the pattern template; cycle-005 sweep over remaining concepts pages can surface analogous defects.

### Wave-conflict observations

- **Wave-1 of cycle-004 was 7 parallel dispatches** with substantial overlap on L1/index.md (9 row appends from 5 wave-mates) and SUMMARY.md (5 chapter-line appends from 5 wave-mates). **Zero structural conflicts** at integration. **POSITIVE signal that the parallel-when-in-doubt philosophy is working at scale.** The same pattern as cycle-003 (2 wave-mates appending to same files) generalises cleanly to 5 wave-mates. Each row was distinct; the planner's per-row anchor merge plan was unnecessary at integration time — direct dep-map row appends in dep-map row order plus alphabetical SUMMARY ordering Just Worked. Planner cycle-005 can mark **same-file row-level edits as PARALLEL by default** even at higher wave-size.
- **SUMMARY.md L1>L0 Part — alphabetical anchor merge**. Both MINRES and BiCGStab independently proposed `append-after axpby-mutation-rotation`. Planner pre-resolved alphabetically (`bicgstab-iteration` then `minres-iteration`); integrator applied both as adjacent lines. Zero friction at integration.
- **L1/dot.md two-writer pseudo-conflict**. Only `concepts-dot-rewrite` writes to `book/src/L1/dot.md` (a 1-line softening edit at line 17). No other report writes it. Listed in planner conflict analysis but resolved at design time, not integration.
- **L1/index.md two layouts in flight**. The `L1-index-refresh` report rewrote the intro structure (new Context bullets, Semantics, new Vocabulary-cohort subsection) while three harvesters proposed new dep-map rows. Integrator merged: refreshed-intro-prose + dep-map verbatim from refresh + 9 new rows appended (3 firm + 6 rough-in obstruction). Clean composition; the dep-map-preserved-verbatim discipline in the refresh report was load-bearing for this.

### Integration-tooling friction

- **No new gates needed**. Cycle-003's gate set held cleanly for 7 wave-mates: zero retroactive-budget hits, zero edge-label drift, zero forward-edge claims without surface, zero variant-axis-missing on multi-variant operators (apply_linop's 3+1 collapsed axis was correctly classified by the report; axpbypcz's 2+1 internal-L0 axis was correctly classified). H1→H2 normalisation not needed (no reports introduced H1 headings on existing pages). Append-by-slug fallback not needed (no slug typos).
- **Obstruction-theme category needs a tooling decision** at meta-phase: the new `justification kind: obstruction` is unprecedented in `book/src/L1-L0/`. Whether future cross-layer-cross-cutter consumers should treat obstruction themes differently (e.g., skip evidence-walking, surface as "anticipated work") is an open methodology question. Routes to `scaffolding/friction-ledger.md` via meta-phase.
- **Subagent-skipped-Edit pattern recurred** (cycle-002 cycle-planner haiku skipped Edit; cycle-004 abstractor (BiCGStab) skipped Edit despite explicit parent-pre-creates-skeleton workflow). Pattern crossed from haiku to opus tier. Routes to meta-phase as a methodology / prompt-engineering item; tracked under open question `subagent-skips-edit-on-explicit-instruction`.

---

## cycle-003 — 2026-05-27T002354Z

### Unblocked

- **L1 layer-intro refresh** is now tractable — the L1 dep-map has 4 firm operators (`axpy`, `dot`, `nrm2`, `axpby`), passing the ≥3-operator threshold from pilot-1's `l1-index-refresh`. Routes to `layer-intro-author`. Citation: open question `l1-index-refresh-trigger-met` (cycle-003).
- **`concepts/dot.md` rewrite** unblocked: the cycle-003 cross-cutter surfaced three concrete contradictions with evidence, and the L1 `dot.md` is the authoritative target for alignment. Routes to `layer-intro-author` (closest existing fit; meta-phase scope-question pending). Citation: open question `concepts-page-authorship-role-scope`, priority #4.
- **`scal` L1 harvest** unblocked: referenced in `axpby` laws 2/3 as a forthcoming primitive; independently appears in `linalg::Normalize` (`vector.hpp:262-270`) and CG's update lines. Small primitive, no blockers. Citation: open question `scal-primitive-l1-harvest`.
- **`axpbypcz` L1 harvest** unblocked: the `axpby` firm landing + the cycle-003 lowering-verifier audit (which confirmed the `vector.cpp:756` internal `AXPBY+Add` composition) provide the L1 anchor. Citation: open question `axpbypcz-l1-harvest`, closes the second half of `axpby-axpbypcz-next-harvest`.
- **`krylov-step` L2 harvester promotion** approaches tractable: L1 vocabulary now has 4 firm operators (`apply_linop` still missing); priority #5 depends on bootstrap-L1-vocabulary item #1. Once `apply_linop` lands, krylov-step harvester can proceed with stable L1 deps.

### New dependencies

- **`nrm2` depends on `dot` at L1** — `nrm2(x) = √dot(x, x)` (algebraic law 8; the L0 form is literal one-line composition). Planner: future `nrm2` edits should not race with `dot` edits in the same wave. Citation: `book/src/L1/nrm2.md` §Dependencies; commit cycle-003.
- **`axpby` subsumes `axpy` (not depends on)** — siblings at L1 dep-map; L1>L0 lowering theme `axpby-mutation-rotation` covers `axpy`'s three sub-patterns as β=1 specialisation. Citation: `book/src/L1/axpby.md` §Dependencies + `scaffolding/decisions/axpby-as-primitive.md`.
- **`axpby-mutation-rotation` theme is now `verified_against:`-stamped** — future `cross-layer-cross-cutter` queries can rely on the per-citation YAML block to consume verdicts. Citation: `book/src/L1-L0/axpby-mutation-rotation.md` §Verified-against (cycle-003 append).

### Resolution implications

- **`axpby-axpy-scal-decomposition-decision`** — **answered**. Cycle-003 harvester chose fused primitive; rationale recorded in `scaffolding/decisions/axpby-as-primitive.md`. The `axpby-mutation-rotation` theme requires no retraction (already assumed fused form).
- **`axpby-lowering-verifier-audit`** — **partially-answered**. Cycle-003 lowering-verifier audited all 8 cited L0 ranges (all `supports`); coverage verdict `partially-supported` with ~25 uncited corpus sites and 3 defined-not-used L0 forms enumerated. Theme content correct; exhaustive corpus indexing deferred to a future cycle (see new open question `axpby-corpus-coverage-exhaustive-indexing`).
- **`concepts-dot-return-type-correction`** + **`concepts-dot-dotc-and-inverted-conjugation`** — **needs-more**. Cycle-003 cross-cutter confirmed all three contradictions concretely (return-type, non-existent `Dotc`, bogus `vector.cpp:142-178` citation) and routed to cycle-004 `layer-intro-author` for the rewrite. The questions remain open until the cycle-004 rewrite lands.
- **`l1-index-refresh`** — **needs-more**. The threshold (≥3 firm L1 operators) is now met (4 firm). New open question `l1-index-refresh-trigger-met` (cycle-003) names the actionable dispatch.
- **`scalar-promotion-typing-rule`** — **needs-more**. Now visible across `axpy`, `dot`, `axpby` (cycle-003 harvester counts three operators stating the same per-operator clause); the typing-rule lift is approaching threshold for promotion above per-operator prose. Cycle-planner may want to escalate priority.

### Suggested next dispatches

- (`layer-intro-author`, `rewrite concepts/dot.md to align with L1/dot.md`) — closes the three cycle-003 contradictions; cycle-004 dispatch the cross-cutter explicitly routed. Bundle with the L1 layer-intro refresh below for one role's two outputs.
- (`layer-intro-author`, `refresh book/src/L1/index.md intro + dep-map prose now that 4 firm operators exist`) — `l1-index-refresh-trigger-met` (cycle-003) names this dispatch. Low-medium scope; can co-bundle with the `concepts/dot.md` rewrite under the same role invocation.
- (`harvester`, `scal @ L1`) — small primitive; referenced in `axpby` laws 2/3; closes cycle-003 open question `scal-primitive-l1-harvest`. Forward-frontier work; closes a pending cosmetic-update obligation on `axpby.md`.
- (`harvester`, `apply_linop @ L1`) — bootstrap-L1-vocabulary priority #1; gates `krylov-step` harvester (#5) and `nrm2_B` (cycle-003 open question `nrm2-B-weighted-energy-norm-harvest`). High-value forward-frontier work; substantial L0 surface (`mfem::Operator::Mult`, `palace::ParOperator::Mult`, `linalg::Operator`) — may want subdivision (cycle-planner to assess).
- (`harvester` or `slice-author`, `MINRES @ L0→L1`) — Shared Infrastructure priority #8 (user directive 2026-05-27: shared infra raised above per-solver pipelines). Roadmap §Shared infrastructure / Krylov solvers; symmetric-indefinite three-term recurrence. New ground; substantial L0 surface — likely candidate for two-step harvest (operator-level dispatch first, then L1 form).

### Wave-conflict observations

- **`book/src/L1/index.md` row-anchor case.** nrm2 and axpby harvesters both edited the L1 dep-map in the same wave. Original nrm2 REPORT proposed a full-file replacement (would have silently overwritten axpby's row-replacement edit); cycle-003 repairer caught this pre-integration and rewrote nrm2's edit as `append-after dot row`. At integration time the two edits were non-overlapping at the row level — planner's "sequential" call was over-cautious. Useful signal: cycle-planner can mark same-file row-level edits as PARALLEL when the rows differ. Integrator action: applied both edits to the dep-map cleanly (axpby row-replaced; nrm2 row appended after dot).
- **`book/src/SUMMARY.md` anchor-line case.** Both harvesters wanted to append a new chapter entry immediately after the existing `- [dot](./L1/dot.md)` line under "L1 — Mutation-Lifted Forms" Part. Two chapter entries appended in sequence — auto-resolved cleanly by chaining (nrm2 first, axpby second; matching dep-map row order). Integrator action: applied both lines in one Edit. Useful signal: SUMMARY.md anchor-collisions where both wave-mates simply add lines are zero-friction at integration; planner can mark these PARALLEL by default.

### Integration-tooling friction

- **`verified_against:` YAML-in-prose embedding** — the cycle-003 lowering-verifier's `verified_against:` block is YAML inside an mdBook chapter, with no fenced code block delimiter; downstream parsers (`cross-layer-cross-cutter`) are expected to extract by leading-keyword scan. No spec exists in `scaffolding/` or `.claude/agents/` for this convention. Routes to meta-phase: decide (a) fenced code block, (b) explicit channel-format spec, or (c) sidecar `.yaml` file. Cycle-003 integrator landed the YAML as proposed (per repairer/critic acceptance); flagging here for meta-phase tooling decisions. Citation: open question `lowering-verifier-yaml-in-prose-channel-format` (cycle-003).
- **No other integration-tooling friction observed** this cycle. All four reports' proposed-changes blocks parsed cleanly (one `edit:` with new-file content, two `append-after:` with explicit anchors, one in-place row-replacement). No safety-net gate hits. Build rebuild ran clean. The user-directive philosophy (parallel-when-in-doubt, conflict-as-signal) worked as designed on its first cycle.

---
