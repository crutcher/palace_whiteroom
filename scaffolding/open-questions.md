# Open questions ledger

**This is an INTAKE channel, not a holding pen** (user directive 2026-05-28). Any agent appends a question here when it surfaces. It is **not** where open work accumulates indefinitely — actionable questions are *migrated into the plan* (`scaffolding/priorities.md`, the project's ongoing fan-out-ranked work plan); resolved/stale/duplicate ones are *closed* to the index below; genuinely-blocked ones are *kept, compacted, with their trigger condition*.

**Maintenance protocol (meta-phase owns this):**
- Between meta-phases the ledger is **append-only** (any agent appends; `integrator-per-report` promotes per-report open questions).
- At **every meta-phase**, the meta-phase runs the unification pass (see `.claude/agents/meta-phase.md` §OQ-ledger unification): triage each `Open` entry against the firm artifact → **close** (resolved/stale/duplicate), **migrate** (actionable → `priorities.md` backlog, ranked by fan-out impact), or **keep** (deferred/contingent, one-liner + trigger). Methodology/policy questions only the human can resolve are flagged `routes to meta-phase / human` and surfaced as an `ask`.
- `cycle-planner` reads this ledger each primary cycle for fresh-intake plan candidates; it does not edit it.

**Last unified:** 2026-06-02 (batch-18 meta-phase, post-cycle-060; cycles 058/059/060). Closed to the Closed index: the verbose c058 (D1/D3/D4) + c059 (D1/D2/D3) + c060 (D1/D2) per-report New-intake blocks compacted into the new "Closed by the batch-18 meta-phase" subsection (full prose in git; slug strings preserved). **Batch-18 decisions enacted:** (1) **batch-18 arc ASSESSED — the two-combinator MAP/FOLD spine descended cleanly + closed out** (`fold_solve` firm L4 + L4>L3 + L3 `partial-obstruction` + L3>L2, NO L2 floor absent-by-warrant; `map_solve` permanent single-witness finding; all 5 pipelines characterized vs the spine; eigenmode SPINE-COMPLETE) — GO, batch-19 frontier reshaped (the lead = the `weak_form_term` FE-vocabulary cohort, pull-gated); (2) **8 formal closes** — `map_solve` single-witness (c058 D3), eigenmode SPINE-COMPLETE (c059 D3), `eigenmode-hybrid-refine_solve` non-actionable (c059 D3), L2/fold_solve NO-FLOOR-WARRANT (c060 D2 RESOLVED-BY-WARRANT — `L2/fold_solve` added to the STOP-PROPOSING negative list), + 4 c059/c060 resolved-on-landing OQs; (3) **friction `index-table-status-cell-drifts-when-theme-file-promoted` flipped `addressed`→`resolved`** (c058 D4 L1/L1-L0 audit CONFIRM-CLEAN 68/68 closed the open half; the promotion-time guard held; no recurrence-2); (4) **`L3_L2_firm` cycle-record `counts_after` stale-vehicle-count note CODIFIED** as a finalize-side bookkeeping convention (align the jsonl field with the consolidated `L3-L2/index` Vocabulary-cohort tally, currently 6 — the stale 13 was the pre-refactor vehicle count); (5) **NO role-spec / agent-def change → NO session restart before cycle-061.** ~12 OQs closed / 0 migrated (all batch-18 actionable items already landed; the batch-19 frontier is reshaped from existing deferred/plan items) / ~3 kept-deferred (folded into the solve_family/fold_solve cohort one-liners). Prior: 2026-06-02 (batch-17 meta-phase, post-cycle-057; cycles 055/056/057; 1005 → 795 lines). Closed to the Closed index: the verbose c055 D1–D8 + c056 D1/D2 + c057 D1/D2/D3/D4 per-report New-intake blocks (~210 lines) compacted into the new "Closed by the batch-17 meta-phase" subsection. **Batch-17 decisions enacted:** (1) **batch-17 arc ASSESSED** — the solver test-load matured the two-combinator MAP/FOLD spine + completed the FE-assembly sub-spine L1>L0 edge without forcing the spine; continue as the batch-18 frontier (GO, plan reshaped to CYCLE-058 active head); (2) **two-combinator MAP/FOLD factoring + §3.7 `iterate_while` shared-parent RATIFIED (GO, no third abstraction)** — `solve_family` (map) + `fold_solve` (fold) are the two §3.7-family specializations, distinguished by whether the step carries state; (3) **`map_solve` superset — keep-probing ONE non-driven pipeline then retire-as-single-witness** (GO, low-priority; migrated to plan c058 #3); (4) **index-table-status-cell-drift — promotion-time guard CODIFIED** as a `.claude/agents/lifter.md` producer bullet + a sharpened `.claude/agents/layer-intro-author.md` count-owner survey bullet — friction `index-table-status-cell-drifts-when-theme-file-promoted` (recurrence-1, addressed) — **agent-def edits → SESSION RESTART required before c058**; (5) **skill `disciplined-cross-pipeline-combinator-mining-gate` CONFIRMED working** (no refinement; gate worked end-to-end c055/c056/c057); (6) **tool-tag-leak hazard NO-GO** on a producer-side guard (single c055 occurrence, finalize net sufficient; report-only, not ledgered). ~16 OQs closed / 4 migrated (fold_solve, weak_form_term, map_solve-probe, L1/L1-L0-audit) / ~6 kept-deferred (folded into 6 cohort one-liners). Prior: 2026-06-02 (batch-16 meta-phase, post-cycle-054; cycles 052/053/054). Closed to the Closed index: the verbose batch-16 New-intake blocks (the c052 D1–D6 + c053 D1/D2/D3 + c054 D1/D2 per-report integration appends — the D4-coordination items RESOLVED at c052 finalize; the spine-coverage / single-witness / mine-now findings RESOLVED by the batch's own progress: `firm-l2-l3-surface-is-combinator-complete-for-in-layer-conciseness` (D5 negative, confirmed), `electrostatic-outer-terminal-sweep...` / `solve-family-combinator-confirmed-2-of-n-mine-now` (mined as `solve_family` c054 D1), `capacitance-reduction-may-be-gram-variant-axis-extension` (confirmed + landed on `gram` c053 D2), `electrostatic-solver-probe...single-witness...` (gate DISCHARGED c053 D1), `fe-assemble-slug-collision-with-bilinear-form` (resolved-in-report); full prose in git). **Batch-16 decisions enacted:** (1) **batch-16 arc ASSESSED** — refactor-pass-complete (c052) → solver-test-load productively underway (c053 probes → c054 `solve_family` mined + `fe_assemble` firm); the batch-14 "L4-complete / strategic-pivot ASK" framing STAYS RETIRED; the solver-test-load has legs as the batch-17 frontier (GO, plan reshaped); (2) **batch-17 frontier DECIDED** — lead = the `solve_family` propagation (full L4 entry + 2 specializations + the `L4-L3/solve-family-map-dissolution` theme); then the FE-assembly continuation (`eliminate_rhs`+`eliminate_essential_bc`, the `weak_form_term` cohort deferred); then the `map_solve` superset + driven/transient 3rd-probe (fold-vs-map guarded); then the gram-consuming solver-postprocess reduction (all migrated to the CYCLE-055 plan); (3) **libCEED-boundary classification RATIFIED (GO)** — the innermost element-local quadrature kernel (`integ->Assemble`-built `CeedOperator`, `bilinearform.cpp:67-70`; COO→CSR `CeedOperatorFullAssemble`, `libceed/operator.cpp:455-490`) is `obstruction (opaque-library-ownership)` — a DEEPER-boundary sibling of the HYPRE/SLEPc cases (Palace owns the orchestration: the integrator-fold `AddSubOperator` at `bilinearform.cpp:77`, the PA/FA dispatch, BC-elimination; only the leaf kernel is library-owned); `fe_assemble` stays FIRM (the fold quantifies over `A` opaquely); enactment (a thin theme annotation) migrated to the plan; (4) **skill `disciplined-cross-pipeline-combinator-mining-gate` PROMOTED** (the single-witness→2nd-probe→discharge→mine gate, default-accept) + friction `disciplined-cross-pipeline-combinator-mining-procedure-unskilled` (recurrence-1, addressed); (5) **NO role-spec / agent-def change → NO session restart before cycle-055.** ~21 OQs closed / ~9 migrated / ~3 kept-deferred. Prior: 2026-06-01 (batch-15 meta-phase, post-cycle-051). Closed to the Closed index: the verbose batch-15 New-intake blocks (the c050/c051 per-report D-section status-record OQs — the demotion-enactment-input verdicts, the count-provenance guards, the D1–D5 apply-status records, the 3-way line-266 co-edit records — all RESOLVED at c051 integration; full prose in git). **Batch-15 decisions enacted:** (1) **`collapsed-leaf-disposition-convention-cohort-wide` RATIFIED — reduce-to-specialization-stub** (NOT full-delete: preserves per-operator unique L0 anchors + variant-axis row + live inbound consumer links; removes the duplicated rectangular semantics; `nrm2` → consumer-stub; information-non-lossy) → migrated to plan CYCLE-052 #1 (`refactor-pass-leaf-chapter-reduce-to-stub`); (2) **batch-16 frontier ASSESSED** — redirect items 2b (continued in-layer combinator-miner abstraction, replace-and-propagate) + 3 (solvers as low-priority test-load) both GO, sequenced after 2a (leaf-chapter stub enactment, the lead) → migrated to plan CYCLE-052 #2/#3; (3) the `linear-combination-fork-OQs-superseded-by-2026-06-01-redirect` batch-12 keep-leaf-floor-(b) fork OQs CLOSED-as-superseded-by-redirect; (4) 3 new friction patterns + 1 recurrence increment (skill `deleted-slug-inbound-live-link-sweep` PROMOTED; clean-tree-gate recurrence-4 RE-WEIGHED → NO-GO; finalize re-pin convention NO-GO; cohort-denominator audit-first precedent recorded). NO role-spec change → **NO session restart before cycle-052.** ~17 OQs closed / 2 migrated (leaf-chapter disposition + the bounded narrative micro-sweep, folded into CYCLE-052 #1) / ~3 kept-deferred (`l4-propagation-depth-linear-combination` low-priority flag rides item 2b; the divfree `Mult` doc-inversion + the `cg-initial-residual-quirk` upstream-confirmation stay trigger-gated). Prior: 2026-06-01 (batch-14 meta-phase, post-cycle-048; 801 → 701 lines). Closed to the Closed index: the c046 New-intake blocks (L4/L4>L3 coverage survey, erasure-scope concept page, residual-L2>L1-gap audit) + the verbose c047/c048 per-report integration-disposition blocks (~135 lines) compacted into the new "Closed by the batch-14 meta-phase" subsection. **Batch-14 decisions enacted:** (1) **L4 frontier ASSESSED substantially-complete** (L4 firm 4→6 `ksp_solve`/`eigsolve` caps; L4>L3 firm 3→6; outer-driver vocab 3→4) — near-term L4 consolidation (the `l4-native-combinator-denominator-completeness-survey` + an L4 width/depth audit) migrated to the plan; the strategic **"is the layer-construction phase done / pivot to downstream burn-component effort?"** question surfaced as an **ASK to the human** (scope-direction, not a methodology tweak); (2) **`outcome-sum-one-row-vs-per-cap-specialisation` RATIFIED (b)**: KEEP the landed one-canonical-`Outcome` + per-cap `*Outcome`-rows shape (outer-driver vocab count = 4, NOT re-collapsed); polymorphic `Outcome α` declined (no law-unification benefit); the contingent `eigoutcome-vs-polymorphic-outcome-count-dependency` RESOLVED at 4; (3) **`residual-l2-l1-gap-audit` CLOSED** (both gaps themed c047, L2>L1 firm 19→21). NEW friction codified: `cross-report-forward-reference-slug-divergence` (canonical-slug-for-sibling-forward-reference convention; role-spec bullets in cycle-planner/harvester/abstractor — **session restart needed before c049**). ~22 OQs closed / 1 migrated (denominator survey) / 4 kept-deferred. Prior: 2026-06-01 (batch-13 meta-phase, post-cycle-045). Closed to the Closed index: the 12 batch-13 New-intake entries (c043/c044/c045) compacted into the new "Closed by the batch-13 meta-phase" subsection + the long-orphaned **cycle-020/021 New-intake residual block (39 entries)** finally compacted (it was never drained by the batch-5/6 passes; every entry is resolved-superseded — the whole eigsolve chain L1/L2/L3 + L3>L2 is now firm — or trigger-gated-and-mirrored in the top deferred-contingent sections; full prose in git). **Batch-13 decisions enacted:** (1) FOUR-ROOT erasure-scope taxonomy RATIFIED + the 3 predecessor OQ slugs UNIFIED + `concepts/erasure-scope.md` page GO (migrated to plan Backlog High `erasure-scope-taxonomy-concept-page`); (2) `l3-l2-rotation-theme-coverage-gap` RE-DENOMINATED 17-of-17-applicable (`apply_linop` no-L2-by-design); (3) `chebyshev-floor-cohort-count-reconciliation` resolved (12-of-12-applicable + naming-exception); (4) `normalize-fused-composite-no-fold-parent-sub-shape` resolved (third §Vocabulary-cohort sub-shape named); (5) both substantive L3>L2 rotations CLOSED (`remaining-substantive-l3-l2-rotations-chebyshev-eigsolve` both halves landed). NEW friction codified: `index-dual-registration-row-and-own-bullet-vs-consolidated-tally` (producer role-spec bullets) + `floor-landing-implies-same-cycle-adjacent-entry-reanchor` (cycle-planner dispatch-design note). Next frontier reshaped in the plan: L4/L4>L3 climb (L4 lowest incomplete layer). ~16 OQs closed / 1 migrated (concept page) / ~4 kept-deferred. Prior: 2026-06-01 (batch-12 meta-phase, post-cycle-042). Closed to the Closed index: the cycle-040/041/042 D-section sprawl (~225 lines of per-report integrator OQ appends) compacted into the new "Closed by the batch-12 meta-phase" subsection. **Five load-bearing batch-12 decisions enacted:** (1) leaf-vs-fold fork `dot-l2-leaf-floor-vs-fold-only-design` RATIFIED (b) leaf-floor cohort-wide (`nrm2` carved out as consumer-not-member); (2) theme-slug convention RATIFIED `-leaf-identity`/`-body-identity` (2 c041 `-fold-specialization` outliers + 1 underscore rename → c043 lifter sweep); (3) directive RENAMED `l2-floor-under-l3-blas1-cohort`→`l2-floor-under-l3-leaf-cohort`; (4) cycle-043 consolidated lifter sweep MIGRATED to plan (8 stale-L3/citation/slug OQs folded); (5) foundation-first directive ASSESSED working (L2-floor 0→8-of-13, L3>L2 2→10-of-18 in 2 cycles) + the held axpy-family UNBLOCKED. Also compacted the stale cycle-034 D1/D2 intake sections. ~33 OQs closed / 4 migrated / ~7 kept-deferred. NEW friction `co-dispatched-harvesters-reach-contradictory-design-conclusions` (addressed via the audit-then-adjudicate precedent); `parallel-blind-shared-index-count-divergence` batch-12 confirmation (held clean at 7-wide + 11-wide). Prior: 2026-05-31 (batch-11 meta-phase, post-cycle-039). Closed to the Closed index: the c036 (A) L3-cohort tracker FULLY CLOSED (6-of-6: assemble-diagonal/jacobi-smoother c037, reciprocal/elementwise_product/divfree-projector c038, normalize c039; L3 firm 9→15) + 6 parallel integrator-action count-bump flags reconciled to ONE consolidated tally (D3-count-ownership convention) + floquet AddMult-aliasing audit resolved (c039 D2 re-anchor) + L3 fourth-obstruction-profile fold + 3 plain-text-forward-ref staleness closes + the codemap +1-drift data point consumed into the friction-ledger. 6 genuinely-open one-liners kept deferred. Methodology signals codified: `parallel-blind-shared-index-count-divergence` (NEW, addressed — D3-count-ownership convention into cycle-planner/harvester/layer-intro-author specs); `cycle-planner-stale-priorities-line-recruitment` CLOSED→`addressed` (batch-11 3-of-3 clean under opus+paste-inline-evidence — the held haiku→opus ASK answered by the 2026-05-31 blanket Opus-4.8 upgrade) + its sibling `cycle-planner-reproposes-already-landed-work` closed→`addressed`; `codemap-read-range-plus-one-drift-on-brace-boundary` recurrence-5 (NEW boundary, catch-and-correct held). Prior: 2026-05-31 (batch-10 meta-phase, post-cycle-036). Closed to the Closed index: 8 batch-10 RESOLVED dispositions (c034 D1+D2 lifts/audits; c035 D1+D2+D3 chebyshev-cite-tighten + CG-quirk-lift + floquet-apply_linop-MATCH; c036 D1+D2 floquet L1 harvest + L3-cohort-growth audit settlement). 2 methodology signals ENACTED: `cycle-planner-stale-priorities-line-recruitment` escalation 3→6 recurrence (post-batch-9 codification insufficient; batch-10 paste-inline-evidence strengthening + skill steps 5/6 + STOP-PROPOSING NEGATIVE LIST recorded in plan); `firm-chapter-body-authored-outside-proposed-changes-fence` recurrence-3 verdict NO-GO on further intervention (steady-state detect+repair acceptable). 1 carry-forward formally CLOSED-BLOCKED-RETIRED (`polynomial-smoother-l2-combinator-from-jacobi-and-chebyshev` — richardson-as-third-sibling-doesn't-exist in Palace). 3 new low-priority deferred-contingent one-liners (floquet dead-code real-vector instantiation; floquet AddMult aliasing audit; chebyshev-smoother-applyorder0 citation-tighten sibling). Prior: 2026-05-30 (batch-9 meta-phase, post-cycle-033; 947 → 850 lines); batch-8 meta-phase post-cycle-030 (1075 → 822 lines); batch-7 meta-phase post-cycle-027 (~1075 → ~775 lines).

Three live sections: **Open — migrated to the plan** (pointers) · **Open — deferred / contingent** (waiting on a trigger) · **Closed (index)**.

---

## Open — migrated to the plan

These are genuinely-open AND actionable; they are tracked in `scaffolding/priorities.md` (the plan), ranked by fan-out impact. The pointer stays here so the ledger remains a complete index of open issues; the plan is the owner.

**Stub homes materialized 2026-05-28:** most of these now have claim-free `stub` entries in the artifact (L2 `inner_product`/`orthogonalize`/`incremental-least-squares`/`ksp_solve`; L2-L1 `inner-product-fold-specialization`; L1-L0 `dot`/`nrm2`/`scal`/`matrix-weighted-norm`-mutation-rotation; L1 `assemble-diagonal`). The constituent OQ slugs below remain the refinement tracking; the work is now "firm the stub in place."

- **l2-named-composition-lifts** → plan Backlog (Medium; `orthogonalize` firmed cycle-019; remaining: `incremental-least-squares` stub→firm). Carries `L2-layer-intro-refresh-for-named-compositions` (now part of a low-fan-out L2-index-prose-refresh cohort).
- ~~**ksp-solve-l2-promotion-non-identity-substantive-gap**~~ — **RESOLVED cycle-021** (Closed index). L2 `ksp_solve` outer-driver firm.
- ~~**l3-vocabulary-inventory-gap**~~ — **RESOLVED cycle-028** (Closed index; ALL 4 leaves done: `gemv`/`ksp_solve`/`eigsolve`/`trsv`). The `trsv` leaf resolved-by-obstruction c028 (Palace has NO standalone trsv; HYPRE-internal / external direct-solver / 2×2-block only), with the c029 `triangular-solve-obstruction` L1>L0 obstruction theme giving the resolved leaf its citable home. Parent plan item CLOSED.
- ~~**blas1-l1-l0-lowering-theme-gap**~~ — **CLOSED 8/8 cycle-022** (Closed index; `axpbypcz-mutation-rotation` rough-in→firm closed the floor).
- **matrix-weighted-norm + bilinear-form firm-promotion** → plan Backlog (Medium). Mutation-rotation theme + variant-axis/test-coverage gates. (Constituent OQs: `matrix-weighted-norm-mutation-rotation-l1-l0-theme`, `matrix-weighted-norm-mixed-element-type-variant`, `bilinear-form-real-vector-coverage-gap`, `bilinear-form-variant-axis-test-coverage`.)
- **normalize-l1-primitive-harvest** → plan Backlog (Medium). (Constituent OQs: `normalize-as-fused-l1-primitive`, `normalize-and-normalize-b-weighted-l1-candidates`.)
- **diagonal-extraction-l1** → plan Backlog (Medium). Harvest `AssembleDiagonal` as an operator-to-data L1 primitive. (OQ `assemblediagonal-is-not-apply-linop-variant`.)
- **rough-in-naming-residue-l0-sweep** → plan Backlog (Low/hygiene). (Constituent OQs: `matrix-weighted-norm-naming-sweep`, `bilinear-form-slug-name-coordination`, `dependency-map-orthog-plane-rotation-stale-edge-prune`, `negative-result-slice-examples-reciprocal-membership`.)
- ~~**cg-initial-residual-quirk-palace-bug-flag-lift**~~ — **LIFT LANDED cycle-035 D2** (annotation in firm L1>L0 theme `book/src/L1-L0/ksp-solve-mutation-rotation.md` CG Sub-pattern B; `(b·b)^{1/4}` recognition-rule + 2 new `Citations:` rows for `iterative.cpp:398-411` + `vector.hpp:257-260`; theme stays `firm`). Sub-OQ retained: **cg-initial-residual-quirk-upstream-confirmation-pending** — bug-vs-intentional classification needs Palace maintainer answer (file upstream issue / `git blame` line 408 introduction commit); out-of-scope for this project to resolve. *Trigger:* an upstream confirmation surfaces, or a future cycle decides to file/track the issue. (Pre-narrowing OQ `cg-initial-residual-quirk-palace-bug-flag-lift-path` — lift portion closed; only upstream-confirmation portion remains.)
- ~~**floquet-correction-operator-construction-variants**~~ — **apply_linop dimension RESOLVED cycle-035 D3 (NEGATIVE finding)**: `FloquetCorrSolver::Mult`/`::AddMult` (`palace/linalg/floquetcorrection.cpp:72-85`) match `apply-linop-mutation-rotation` sub-patterns A and D bit-for-bit; the existing `apply-linop-overload-set.md:33` non-exhaustive caveat ("Other operator-shaped types in Palace … all implement the same interface; the overload-set shape is uniform") already accommodates constructed-operator-gate classes (`FloquetCorrSolver` does NOT inherit from `Operator`/`ComplexOperator` — it is a standalone class that exposes the same overload-set surface, in the same boat as `DivFreeSolver`). **NO extension to `apply-linop-mutation-rotation` or `apply-linop-overload-set` is needed.** See `reports/2026-05-31T141500Z-cross-layer-cross-cutter-floquet-operator-construction-variants/CYCLE.md`. The coverage gap is at a DIFFERENT tier — see migrated plan candidate below.
- **floquet-correction-l1-gate-harvest** → plan Backlog candidate (Medium fan-out; flagged for integrator-finalize migration to `priorities.md`), opened cycle-035 D3. NEW intake from the D3 cross-layer-cross-cutter observation: no firm L1 `floquet_correction` operator / `L1-L0/floquet-correction-mutation-rotation` theme exists; the gate is structurally isomorphic to the firm `divfree-projector` (third firm instance of `nested-constructed-operator-gate`, sibling to `divfree-projector` + `eigsolve`). Route: `harvester` (NOT abstractor-first — construction + apply sites fully present in firm Palace source, no negative-anchor concern). Template: `divfree-projector` L1 entry + `divfree-projector-mutation-rotation` L1>L0 theme port directly. Closure: `FloquetCorrector[N_nd, N_rt]` carrying `M : LinearOperator[N_rt, N_rt]` (RT mass), `Cross : LinearOperator[N_nd, N_rt]` ([kp ×] matrix realization), `ksp : Solver[M]` (inner CG + JacobiSmoother). L1 signature: `floquet_correction :: (F: FloquetCorrector[N_nd, N_rt], x: Field[N_nd]) -> Field[N_rt]` with `floquet_correction(F, x) = F.M⁻¹ · F.Cross · x`. Lowering theme: sub-pattern A for `Mult`, sub-pattern D for `AddMult`. Variant axes: element-type (`<ComplexVector>` only-instantiated — likely deliberate-real-omission scope-out per grep-verified absence of `<Vector>` instantiation; harvester makes final call). Fan-out: 4 AddMult call sites (3 in `palace/drivers/drivensolver.cpp:212, 336, 468` + 1 in `palace/drivers/eigensolver.cpp:454`); fan-out via concept-page upgrade (`nested-constructed-operator-gate` 2 firm → 3 firm). Cost: small (~half of divfree-projector — no `bdr_eff` boundary, no complex-vs-real branching, no `Mult(VecType &y)` in-place form). Plan-tag `nested-constructed-operator-gate-instance-3`. See `reports/2026-05-31T141500Z-cross-layer-cross-cutter-floquet-operator-construction-variants/CYCLE.md` §Recommendation item 2.
- ~~**inner_product L2 firm operator** + **L2-L1/inner-product-fold-specialization theme**~~ — **RESOLVED cycle-019** (Closed index; were plan Now #1/#2). The `linear-combination-fold-specialization-theme-followups` were assessed non-blocking (caveats already in that theme; see deferred).
- ~~**gmres.md §L4 v0.6→v0.7 self-rotation**~~ — **RESOLVED cycle-020.** **NLEPS at L1+** — **5-atom NEP-interior cohort COMPLETE cycle-024** (Closed index). REMAINING migrated-to-plan: the two NEP-interior L1>L0 themes (`nleps_jacobian_action`, `nleps_eigenvalue_correction`) → plan active-head #1 (Backlog High `nleps-interior-atom-l1-l0-themes`).
- ~~**lu_solve L1 dense-solve primitive**~~ — **DONE cycle-022** (Closed index; L1 `lu_solve` firm, unblocked `deflate`/`gram`).
- ~~**deflate/gram L2 combinator**~~ — **`gram` firm + `deflate` partly-constructive cycle-022** (Closed index). The `deflate` bare-Galerkin-core promotion gate is now **deferred/contingent** in the plan (`deflate-galerkin-core-promotion`) — gated on a positive bare-Gram-solve site outside the Schur wrapping (triple-referenced). (OQs `deflate-single-algorithm-concentration-scope-review`, `deflate-composition-lowering-galerkin-core-promotion-gate-shared-with-l2-deflate-stays-open`.)
- ~~**L3-entry citation-drift sweep**~~ — **DONE cycle-022** (Closed index; L3 `ksp_solve` `:464→:463`/`:564→:563` + inner-product-fold `operator.cpp :624`/`:634`/`:616` all re-anchored by the cycle-022 lifter sweep).
- **eigsolve L2>L1 + concept page** → plan active-head #2/#3 (Backlog High `eigsolve-l2-l1-and-concept`). The L2>L1 spectral-transform-composition theme + the absent `concepts/eigsolve` page — the only remaining eigsolve-chain authoring gaps (L1/L2/L3 all firm). (OQs `eigsolve-l2-l1-spectral-transform-composition-lowering-theme-needed`, `concepts-eigsolve-page-still-absent`.)
- **cycle-043 LIFTER-SWEEP (batch-12 migration)** → plan `## CYCLE-043 active head` #1 (`cycle-043-l2-floor-stale-l3-lifter-sweep`). Re-anchor the stale-L3 "no interposed L2 entry / no L3-L2 theme" assertions across `reciprocal`/`assemble-diagonal`/`jacobi-smoother`/`divfree-projector` (D3 `elementwise_product` reconciled inline) + the `book/src/L1/assemble-diagonal.md:111` `:172`→`:174` + `L3/elementwise_product.md:166` sibling-gloss fixes + the slug renames (`nrm2`/`scal` `-fold-specialization`→`-leaf-identity`, `elementwise_product-body-identity`→hyphen). (Folds the 8 cycle-042 stale-L3 / citation-drift / slug OQs — see Closed index batch-12 subsection.)
- **axpy-family + normalize + chebyshev L2 floors (batch-12 migration; fork UNBLOCKED)** → plan `## CYCLE-043 active head` #3/#4 (`l2-floor-under-l3-leaf-cohort`, final slices). The held `axpy`/`axpby`/`axpbypcz` arity-family (now unblocked by the (b)-leaf-floor ratification) + `normalize` + `chebyshev` — the last 5 of the 13-entry L2-floor cohort. (OQ `arity-family-leaf-floors-output-aliasing-axis-is-the-folds` rides as an authoring note.)
- **L2-INDEX-FORK-GENERALIZATION (batch-12 migration)** → plan `## CYCLE-043 active head` #2 (`l2-index-fork-ratification-touch`). The one-line `book/src/L2/index.md` §"Fold-cohort boundary" generalization + Working-Notes flip to "RATIFIED (b)" + cohort-heading cohort-neutral rename. Route `layer-intro-author` (meta-phase does not write `book/`).

> *Note (batch-12/13 era pointers above are historical — all landed cycles 043–045; retained as the migration trail, not live work. The live migrated items are the batch-16 block below.)*

> *Note (batch-16 migration pointers — `solve-family-propagation`, `fe-assembly-thread-continuation`, libCEED-boundary enactment, `map-solve-superset-probe`, `gram-consuming-solver-postprocess-reduction` — all LANDED or resolved across cycles 055/056/057: solve_family propagated full L4 + firm L4>L3 dissolution; all 3 FE-assembly ops firm + the rotation theme firm + the libCEED obstruction themed; `map_solve` disciplined-deferred at 1 witness; gram-postprocess CLOSED as genuinely solver-specific. Retained as the migration trail in git; the live migrated items are the batch-17 block below.)*

**Batch-17 migration (post-cycle-057; the CYCLE-058 / batch-18 frontier):**
- **`fold_solve` full L4 entry + L4>L3 lowering (the batch-18 LEAD-if-gate-fires)** → plan `## CYCLE-058` #1 (`fold-solve-promotion`). The transient state-threaded FOLD combinator (c057 D4 rough-in dep-map row in `book/src/L4/index.md`), the fold-sibling of `solve_family`'s independent MAP. The ≥2-fold-witness gate IS met (transient + SweepAdaptive's offline greedy phase, both confirmed fold-family members c057 D3) — author the §3.7-`iterate_while`-child entry (NOT a new parent abstraction, per the batch-17-ratified shared-parent decision) + its L4>L3 lowering (per-step body = `ode->Step` opaque-library obstruction). The c058 miner decides whether `fold_solve` is one combinator parameterized over the schedule-source OR the greedy sub-shape is distinct. fan-out: MEDIUM-HIGH. (OQ `fold-solve-second-witness-gate`; the §3.7-parent question `fold-solve-solve-family-share-iterate-while-parent` RATIFIED + closed.)
- **`weak_form_term` FE-vocabulary cohort** → plan `## CYCLE-058` #2 (`weak-form-term-fe-cohort`). The genuinely-new FE differential-operator vocabulary the now-firm FE-assembly sub-spine folds over opaquely (∇/identity/curl/div + boundary integrators). Clean-gated: author term forms only as a pipeline pull-up first needs a non-diffusion term, NOT speculatively. fan-out: MEDIUM. (OQs `fe-assemble-weak-form-term-cohort-enumeration` + `fe-assemble-weak-form-term-cohort-width-after-firm-flip`, unified; pointer retained in §Open—deferred/contingent → FE-assembly sub-spine siblings.)
- **`map_solve` 2nd-pipeline probe (then retire-as-single-witness if no discharge)** → plan `## CYCLE-058` #3 (`map-solve-second-pipeline-probe`). ONE cheap observation-first probe of a non-driven multi-operator sweep for a 2nd operator-varying-MAP witness; discharge LICENSES batch-19 `map_solve.md` authoring, non-discharge RECORDS `map_solve` as a permanent single-witness spine-coverage finding. Do NOT author from 1 witness. Cite the `disciplined-cross-pipeline-combinator-mining-gate` skill. fan-out: LOW-gated. (OQ `sweepadaptive-is-rom-fold-map-solve-stays-single-witness` resolved the SweepAdaptive candidate; the superset stays at 1 witness.)
- **L1 / L1-L0 index-table-staleness audit** → plan `## CYCLE-058` #4 (`l1-l1-l0-index-staleness-audit`). The highest-in-place-promotion-churn next-audit candidate for the index-cell drift class (per c056 D2); CONFIRM-CLEAN expected (mirrors c056 D2's L3-L2/L2-L1 result). Closes the open half of friction `index-table-status-cell-drifts-when-theme-file-promoted` (the promotion-time guard codified this batch prevents NEW drift; this audit checks historical residue). fan-out: LOW/hygiene. (OQ `l1-l1-l0-tables-next-index-staleness-audit-candidate`.)

## Open — deferred / contingent

Genuinely open but **not yet actionable** — each waits on a stated trigger (an upstream change, a not-yet-firm dependency, a "when-relevant" survey, or a downstream dispatch that will consume it). Promoted into the plan by a future meta-phase once its trigger fires.

### BLAS-1 / linear-update family
- `dot-reduction-tree-determinism-survey` — survey Palace bit-determinism claims for dot uses. *Trigger:* a deterministic-reduction solver variant becomes relevant.
- `axpby-corpus-coverage-exhaustive-indexing` — exhaustive enumeration of ~25 uncited axpy-shaped sites + 3 defined-not-used L0 forms. *Trigger:* L1 vocabulary fully firm / a consumer needs the full set.
- `scal-bit-determinism-fusion` — scal law-4 two-pass-vs-fused rounding (non-load-bearing today). *Trigger:* a deterministic-reduction solver upgrades it to load-bearing.
- `scalar-promotion-mutation-rotation-cross-family-theme` — first-class L1>L0 scalar-promotion theme across the BLAS-1 family. *Trigger:* a cycle decides to surface scalar-promotion recognition as its own theme.
- `axpbypcz-member-method-body-survey` — read the member-form `AXPBYPCZ` γ==0 branch (L1 algebra unaffected). *Trigger:* a lowering-verifier needs the member body.
- `axpbypcz-gamma-asymmetric-branching-rationale` — confirm γ-only L0 branching is by-design vs incidental. *Trigger:* combinator-miner / cross-cutter pass.
- `axpbypcz-sub-pattern-B-defined-not-used-corpus-audit` — full-tree caller audit of sub-pattern B. *Trigger:* lowering-verifier exhaustive audit.
- `reciprocal-upstream-and-safe-variant` (folds `safe-reciprocal-threshold-l1-candidacy` + `mfem-vector-reciprocal-upstream-body-investigation`, both cycle-034 D1) — a speculative zero-guarded `safe_reciprocal(x,ε)` L1 candidate (no Palace L0 anchor) + the upstream `mfem::Vector::Reciprocal()` NaN-on-zero/device-kernel body investigation (out-of-scope vendored upstream). *Trigger:* a consumer-side zero-guard need or an upstream Palace kernel addition.

### nrm2-mutation-rotation lowering family (opened_at: cycle-019, opened_by: abstractor)
The `nrm2-mutation-rotation` L1>L0 theme firmed up (stub→firm) cycle-019 — four-stage `Dot→MPI_Allreduce→std::abs→std::sqrt` chain; 3 surface forms (free-function template / MFEM method / `ErrorIndicator` wrapper); element-type variant axis collapses to one L1 operator; `std::abs` guard classified.
- `nrm2-std-abs-defensive-guard-classification` — **RESOLVED by the cycle-019 firm theme.** Verdict: load-bearing numerical defensive guard (domain-safety / non-negativity for `sqrt`, never `NaN`), a no-op in exact arithmetic; NOT a transparent performance trick. Consistent with the already-landed `L0/transparent-vs-load-bearing-tricks.md:22` "Defensive non-negativity guard" worked example for `linalg::Norml2` (no new L0-page subsection needed). *Meta-phase:* close on the migrated-to-plan constituent (§Open — migrated to the plan, `blas1-l1-l0-lowering-theme-gap`).
- `nrm2-lowering-theme-deliverables` — **ADDRESSED by the cycle-019 firm theme.** Delivered the four-stage chain, the three surface forms, the variant-axis collapse, the abs-guard classification, and the MPI-collective reintroduction; the B-weighted overload boundary is marked and deferred to `matrix-weighted-norm-mutation-rotation`. *Meta-phase:* close on the migrated-to-plan constituent.
- `nrm2-mutation-rotation-dot-stub-collective-double-statement-recheck` — sub-pattern A forward-references the still-`stub` [`dot-mutation-rotation`](../book/src/L1-L0/dot-mutation-rotation.md) as the inherited MPI-collective sub-theme (kept as a live link; the stub file exists). When `dot-mutation-rotation` firms up, re-check `nrm2`'s sub-pattern A for any double-statement of the `LocalDot`/`Allreduce` collective chain. *Trigger:* a lifter consuming the firmed `dot-mutation-rotation` theme (lifter work, not abstractor).
- `nrm2-mutation-rotation-verified-against-audit` — the `verified_against:` block (per-citation `verdict`/`audited_at`, per the axpby-theme convention) is deferred to a `lowering-verifier` cycle (its authorship, not the abstractor's). The theme is `firm` on its structural content; the audit confirms surface-form exhaustiveness — in particular whether any un-cited `Norml2` overload or caller surface exists beyond sub-patterns A/B/C. *Trigger:* a lowering-verifier dispatch on the theme.
- `dot-mutation-rotation-verified-against-audit` (opened_at: cycle-020, opened_by: abstractor) — the `dot-mutation-rotation` L1>L0 theme firmed up (stub→firm) cycle-020; the standard `verified_against:` block (per-citation `verdict`/`audited_at`, per the axpby/nrm2-theme convention) is deferred to a `lowering-verifier` cycle (its authorship, not the abstractor's). The theme is `firm` on its structural content (the `Mpi::GlobalSum ∘ LocalDot` two-step, the three surface forms A/B/C, the conjugate-pair re-order `xᴴ y = conj(yᴴ x)`); the audit confirms surface-form exhaustiveness — in particular whether any un-cited fourth `Dot` overload exists beyond the three sub-patterns and whether the reduction-tree table matches the L0 bodies. Non-status-reducing (the theme is firm). Pairs with the conjugate-pair caller-classification refinement `inner-product-conjugate-pair-reorder-caller-classification`. *Trigger:* a lowering-verifier dispatch on `L1-L0/dot-mutation-rotation`.

### apply_linop / operator family
- `apply-linop-lowering-verifier-audit-cohort` (folds `apply-linop-workspace-tensor-reading-at-L0` + `apply-linop-preconditioner-application-coverage` + `apply-linop-complex-wrapper-operator-lifting` + `apply-linop-complex-operator-default-impls-of-hermitian-transpose`) — pending lowering-verifier audits on the `apply-linop-mutation-rotation` theme (workspace mention-and-erase; preconditioner-hierarchy coverage; `ComplexWrapperOperator` four-block; `MultHermitianTranspose`/`MultTranspose` defaults). *Trigger:* a lowering-verifier dispatch on the theme.
- `addmult-decomposition-bit-equivalence` — add the matrix-free fp-sum-order load-bearing caveat to `apply-linop-mutation-rotation`. *Trigger:* small abstractor/lifter edit (promote to plan if it blocks a consumer).
- `mfem-add-alias-safety` — audit MFEM `Vector::Add` aliasing semantics (upstream). *Trigger:* lowering-verifier / cross-layer pass on the axpbypcz alias claim.

### Krylov / iterate-while / NLEPS family (mostly gated on NLEPS landing at L1+)
- `nleps-spec-gap-as-check-stop-into-carry-reuse-blocker` — NLEPS is the firm-promotion blocker for `check_stop_into_carry`. *Trigger:* NLEPS lands (plan active #4).
- `check-stop-into-carry-parameterization-over-stop-condition` — parameterized helper form. *Trigger:* `check_stop_into_carry` promotion (NLEPS).
- `iterate-while-witness-alternative-combinator-design` — witness-vs-carry design choice. *Trigger:* concurrent with NLEPS promotion.
- `standalone-iterate-while-l4-l3-theme-pending` — standalone dissolution theme. *Trigger:* a second iterate-while-using L4>L3 theme lands.
- `gmres-givens-stream-as-step-kernel-borderline` — revisit krylov-step inclusion of the Givens-stream. *Trigger:* `incremental-least-squares` firmed at L2 (plan backlog).
- `l4-v01-v06-self-rotation-history-lift-target-decision` — lift-target for the gmres.md L4 self-rotation history. *Trigger:* gmres §L4 self-rotation (plan active #3).

### eigsolve / SLEPc family (gated on upstream)
- `eigsolve-convergence-reason-mapping-promotion` — partly-constructive → firm. *Trigger:* Palace reads `EPSGetConvergedReason` (currently only prints via `EPSConvergedReasonView`).
- `eigsolve-convergence-reason-mapping-slepc-enum-upstream-confirm` — confirm per-version SLEPc enum set + table PEP rows. *Trigger:* lowering-verifier upstream pass.
- `eigsolve-driver-side-double-solve-composition` — linear-eigensolve→QuasiNewton bind at L2/L4. *Trigger:* an L2/L4 eigsolve dispatch.
- `eigsolve-nep-coordinate-convention-empirical-witness` — empirical witness for the NEP convention (source-read-confirmed only). *Trigger:* a test-eigensolver NEP case or a SlepcNEP L0 entry.
- `eigsolve-mutation-rotation-embedded-audit-yaml-resolution-marker` — append the resolved-cycle-013 marker to the embedded audit YAML (low-pri). *Trigger:* a cleanup dispatch.

### chebyshev / preconditioning family
- `spectrum_estimate-l1-rough-in-opacity` — opaque `SpectralNorm` power-iteration dependency. *Trigger:* harvester on `SpectralNorm` (plan carried-forward).
- `chebyshev-l4-l3-dedicated-theme-file` — optional thin L4>L3 chebyshev theme (in-line annotation suffices for now). *Trigger:* a lowering-verifier wants a dedicated anchor.
- `chebyshev-l3-l4-layer-intro-refresh` — refresh L3/L4 index narrative for the chebyshev rows. *Trigger:* a layer-intro-author dispatch.
- `l4-preconditioning-framework-promotion` — firm L4 lift to unblock `cg_preconditioning_framework` slice removal. *Trigger:* a harvester L4 preconditioning-framework lift.

### orthogonalize / concepts / citation hygiene
- `orthogonalize-mutation-rotation-audit-confirmed-rom-consumer-residual` — ROM greedy-loop prior-w discard unaudited. *Trigger:* a lowering-verifier ROM pass.
- `orthogonalize-mutation-rotation-l2-krylov-step-lift-notes` — reverse-lift working notes for a downstream L2 krylov-step lift. *Trigger:* a lifter consuming the theme.
- `concepts-orthogonalization-spec-slices-link-survival` — keep the orthog-slice path-anchor alive if later stub-reduced. *Trigger:* orthog.md stub-reduction.
- `concepts-sequential-obstruction-variant-absorption-drift-spot-check` — spot-check two concept pages vs firm L1. *Trigger:* a same-layer-cross-cutter dispatch.
- `concepts-sequential-obstruction-opaque-library-marker-distinction` (c045 D1) — note in `concepts/sequential-obstruction.md` that a sequential-obstruction marker can be rooted in opaque-library-ownership (no Palace recurrence to render) vs a Palace-authored renderable-then-erased recurrence. The sibling `concepts/erasure-scope.md` page landed c046 D2 (it gives the opaque-library root its cross-cutting home); the in-`sequential-obstruction.md` fold-in was NOT performed there. *Trigger:* a future `concepts/sequential-obstruction.md` touch (layer-intro-author / same-layer-cross-cutter) — fold the marker-root distinction in then, cross-referencing `concepts/erasure-scope.md`.
- `erasure-scope-l3-l2-index-line-anchor-drift-risk` (c046 D2) — `concepts/erasure-scope.md` carries plain-text source-locator references into `book/src/L3-L2/index.md` (§Erasure-scope-taxonomy / §Vocabulary-cohort, by section name — stable; the line numbers are a c046 convenience locator). Low-severity bookkeeping caveat, not a build risk (linkcheck does not parse plain-text line locators). *Trigger:* a future recompaction / re-anchor of `L3-L2/index.md` §Working-Notes — re-confirm or drop the line locators on the concept page then.
- `concepts-axpby-page-unauthored` (c043 D4) — no `book/src/concepts/axpby.md` exists (only `concepts/axpy.md`); candidate cross-cut covering the arity-floor cohort, below the clearly-implied-stub bar (single forward-ref). *Trigger:* a layer-intro-author concept-page dispatch covering the arity-floor cohort, OR a second converging reference to `concepts/axpby`.
- `plane-rotation-givens-l0-citation-range-reconcile` — givens_* concept pages cite stale gmres.cpp + off-by-one vs `plane_rotation_stream.md`. *Trigger:* a verify-citation-range pass.
- `slice-pages-l2-l3-accuracy-audit` — verify cg.md/gmres.md slices accurately describe dot L2/L3 usage. *Trigger:* a same-layer-cross-cutter on those slices.
- `axpy-test-linkages-deferred` — add test-linkage entries if axpy is exercised via integration tests. *Trigger:* a test-linkage agent dispatch.
- `mfem-wrapper-solver-l4-complex-from-real-lift-backref` — add the back-ref once `complex-from-real-lift` is firmed at L4. *Trigger:* L4 complex-from-real-lift firming.
- `l0-reference-note-citations-grep-vs-read-discipline` — re-read grep-verified ranges if a future audit extracts algebraic detail. *Trigger:* such an audit consumes those ranges.
- `iterative-file-helper-citation-granularity` — per-overload helper enumeration if the small-dense kernel becomes load-bearing. *Trigger:* that.

### Routes to meta-phase (methodology codification) — ALL FOUR ADJUDICATED batch-5 meta-phase (cycle-021); see Closed index
The 2026-05-28-unification's four parked methodology questions were all adjudicated by the batch-5 meta-phase (cycle-021):
- `variant-absorption-vs-instance-counting-policy` — **CLOSED-resolved** (Closed index): addressed by the cycle-018 parametric-family mode, confirmed concordant-and-complementary by the cycle-019 + cycle-021 live exercises.
- `combinator-miner-authority-defer-verdict-status-edit-scope` — **CLOSED-resolved** (Closed index): moot under the universal dispatch-phase write-guard — combinator-miner is proposal-only (emits a rough-in dep-map row; never edits status / emits a binding `defer` verdict); promotion/deferral is the downstream harvester's call.
- `test-coverage-bounded-rough-in-nomenclature` — **CLOSED-resolved** (Closed index): codified in CLAUDE.md §Methodology invariants as a first-class rough-in qualifier `rough-in (test-coverage-bounded)`.
- `partial-obstruction-status-codification` — **CLOSED-resolved** (Closed index): codified in CLAUDE.md §Methodology invariants as a first-class L3 status `partial-obstruction`.

### fem / FE-space family
*(opened_at: cycle-019, opened_by: layer-intro-author — from the `fespace-file` L0 anchor)*
- `fem-libceed-basis-restriction-l0-anchor` — proposed follow-up L0 anchor for `palace/fem/libceed/{basis,restriction}.{hpp,cpp}` (the MFEM→libCEED basis/restriction bridge: `ceed::InitBasis` / `ceed::InitRestriction`, `fespace.cpp:158,167`). Forward-referenced as plain text by BOTH `fespace-file` and `fem-libceed-operator-file`; ranks next after `fespace-file` per the producer's scope-coverage judgment. Companion deferred regions on the same trigger: quadrature-rule selection (`fem::DefaultIntegrationOrder::Get`, `fem/integrator.hpp`) and geometric factors (`GetCeedGeomFactorData`, `fem/mesh.hpp`). *Trigger:* an L0 anchor dispatch on the libCEED basis/restriction bridge (planner flagged as a possible cycle-020 defer).
- `l0-index-summary-file-overview-drift-libceed-operator` — navigation-completeness gap: `fem-libceed-operator-file` is registered in `SUMMARY.md:103` but was never given a matching bullet in the `book/src/L0/index.md` "File overviews" cohort (cycle-016 landing). Independently confirmed by the cycle-019 critic. Not a build error (the chapter is reachable via `SUMMARY.md`); the `fespace-file` insert (cycle-019) correctly did NOT backfill it (one-chapter-per-invocation scope). *Trigger:* an index-maintenance / backfill pass (integrator stub-or-backfill judgment, or a layer-intro-author index dispatch).
- `fespace-tvector-lvector-workspace-prose-treatment` — `GetTVector` / `GetLVector` / `GetLVector2` templated workspace accessors (`fespace.hpp:151-184`) are cited in the `fespace-file` evidence but given no prose treatment; they are `mutable-workspace-pattern` instances on the element-type axis (`ComplexVector` vs `.Real()`), boilerplate at L0. *Trigger:* a future L1 operator on FE-space apply needing them (warrants a one-line note then).

### assemble-diagonal / diagonal-preconditioning family
*(opened_at: cycle-019, opened_by: harvester — from the firm L1 `assemble-diagonal` harvest)*
- `assemblediagonal-is-not-apply-linop-variant` — **RESOLVED by this harvest** (resolution-anchor: `book/src/L1/assemble-diagonal.md`, firm cycle-019). The deliberate exclusion-marker is now answered: `assemble_diagonal` lands as its own firm L1 entry with the operator/data divide stated explicitly (`apply_linop :: (A, x) -> y` is operator-and-vector-to-vector; `assemble_diagonal :: (A) -> Tensor[N]` is operator-to-data, consuming no vector — the result is a property of `A` alone). Recorded here so the **meta-phase** closes/migrates it out of the active ledger (close authority is meta-phase per CLAUDE.md §Write-authority partition); the migration target is "done", not the plan.
- `assemble-diagonal-mutation-rotation` — the L1>L0 lowering theme for `assemble_diagonal` is not yet authored (abstractor). Must narrate forward from L1 `d = assemble_diagonal(A)` to the L0 `A.AssembleDiagonal(diag)` output-arg mutation, absorbing: (i) destination sizing / `diag = 0.0` zero-init; (ii) the representation split (Hypre CSR exact read vs libCEED element-local accumulation vs `ParOperator` abs-prolongation-transpose `|P|ᵀ dₗ`); (iii) the Dirichlet `DiagonalPolicy` BC post-step; (iv) the matrix-free high-order-Nedelec approximation as a load-bearing assembly-order property. **Backlog migration** — pair with the existing `diagonal-extraction-l1` plan item. *Trigger:* an abstractor L1>L0 dispatch on `assemble_diagonal` (now-firm L1 anchor ready).
- `assemble-diagonal-reciprocal-elementwise-product-l1-primitives` — the diagonal-preconditioner apply completes via `mfem::Vector::Reciprocal` (`dinv.Reciprocal()`) + an element-wise product (`Apply(dinv, x, y)`, `palace/linalg/jacobi.cpp:104`); both referenced as **plain text** in the `assemble-diagonal` entry (forthcoming `reciprocal` / `elementwise_product` L1 primitives, not yet authored — singletons, not stub-materialized this pass). Harvesting them would let the Jacobi/Chebyshev preconditioner-apply be expressed as a clean L1 composition `y = elementwise_product(reciprocal(assemble_diagonal(A)), x)`. **Backlog migration** — candidate plan items. *Trigger:* a harvester dispatch on `reciprocal` / `elementwise_product` (or a consumer needing the clean composition).
- `assemble-diagonal-mfem-real-path-upstream` — the real-operator `AssembleDiagonal` resolves into vendored MFEM via `using Operator = mfem::Operator` (`palace/linalg/operator.hpp:21`); Palace overrides it only on its own subclasses (`HypreCSRMatrix`, `ParOperator`, libCEED `Operator`). Per CLAUDE.md "Many symbols resolve into upstream libraries", the L1 entry cites Palace call sites + overrides, not the MFEM base virtual's internals. The exact semantics of `mfem::Operator::AssembleDiagonal` for any Palace real-operator that does NOT override it (and inherits the MFEM default) is out-of-scope evidence — does NOT block firm status (all surfaced smoother consumers call into overriding subclasses). *Trigger:* a future cross-layer pass should confirm no real-operator consumer relies on the MFEM default behaviour.
- `l1-index-fifth-motif-operator-to-data-introspection` — the L1 `index.md` "Semantics (overlay)" enumerates four recurring semantic motifs; `assemble_diagonal` introduces a fifth — **operator-to-data introspection** (an operator-argument primitive returning operator-intrinsic data rather than the operator's action). Flagged by the harvester per scope (intro edits are layer-intro-author's job). *Trigger:* a layer-intro-author dispatch refreshing the L1 index motif list.

### FE-assembly sub-spine siblings (opened_at: cycle-053/054, opened_by: abstractor/harvester — from the FE-assembly thread)
*(The FE-assembly thread opened c053 D3 / `fe_assemble` firm c054 D2. The next-step continuation — `eliminate_rhs`/`eliminate_essential_bc` + the libCEED-boundary annotation + the lifter cleanup — is MIGRATED to the plan (CYCLE-055 #2). These siblings stay deferred until a solver actually needs them.)*
- `fe-space-l1-form-untouched` (c053 D3) — `FiniteElementSpace` has an L0 chapter (`L0/fespace-file.md`) but no L1 form; the `fe_assemble` thread takes a space as an opaque parameter. A full FE-assembly spine eventually needs an L1 FE-space form (mesh + element-collection + dof-map). *Trigger:* an FE-space pull-up downstream of the FE-assembly sequencing — a solver needs the FE-space as more than an opaque parameter.
- `discrete-linear-operator-interpolation-sibling` (c053 D3) — `DiscreteLinearOperator` (`bilinearform.cpp:203-282`) is the interpolation variant of the assembly fold (interpolator basis, transpose-also production, dof-multiplicity averaging); sketch as a `fe_interpolate` sibling reusing the c053 thread's fold structure. *Trigger:* a solver pull-up needs interpolation operators (e.g. a multigrid prolongation).
- `fe-assemble-weak-form-term-cohort-enumeration` (c054 D2) + `fe-assemble-weak-form-term-cohort-width-after-firm-flip` (c057 D2, unified here) — **MIGRATED to the plan** (CYCLE-058 active #2 `weak-form-term-fe-cohort`): the `weak_form_term` `(coefficient, differential-operator)` cohort (∇/identity/curl/div + boundary integrators, `integrator.hpp:39-130`) is the genuinely-new FE vocabulary; `fe_assemble` + the now-firm `fe-operator-assemble-mutation-rotation` theme fold over it opaquely. The two slugs are the same width concern (one pre-firm-flip, one post-) — unified. Clean-gated: author term forms only as a pipeline pull-up first NEEDS a non-diffusion (mass / curl-curl / div-div / boundary) term, NOT speculatively. The plan owns it; pointer retained here as the index.
- `fe-assemble-rectangular-and-multilevel-axes` (c054 D2) — the firm `fe_assemble` signature is the square single-operator case; the rectangular `fe_assemble(trial,test,terms)` (`bilinearform.cpp:42-46`) + the multigrid-hierarchy `Assemble(FiniteElementSpaceHierarchy,...)` overload (`bilinearform.cpp:155-160`) are sibling sub-axes. *Trigger:* a multigrid-preconditioned solver pull-up needs per-level / interpolation assembly.
- `c052-d4-preexisting-non-link-stale-refs-out-of-scope` (c052 D4) — two pre-existing NON-build-breaking stale prose code-spans surfaced during the c052 refactor: `book/src/L2/index.md:107` cites the removed slice `spec/slices/chebyshev.md:354-362` (cycle-015 historical narrative); `book/src/L3/linear_combination.md:162` cites the cycle-051-deleted `L3-L2/axpy-body-identity.md` in §Evidence. Bare code-spans, not `](...)` links → linkcheck2-safe. *Trigger:* a future lifter/layer-intro-author touch of the respective §Working-Notes / §Evidence prose retires them.

### solve_family / fold_solve / solver-driver spine family (opened_at: cycle-055–060, opened_by: harvester/abstractor/cross-layer-cross-cutter — the batch-17/18 solver-test-load; kept-deferred carries)
*(The batch-17/18 solver-test-load matured AND descended the two-combinator MAP/FOLD spine. `solve_family` (MAP) + `fold_solve` (FOLD) are both firm with their L4/L4>L3 descent; `fold_solve` additionally has its L3 (`partial-obstruction`) + L3>L2 image, with NO L2 floor (absent-by-warrant). `map_solve` is a permanent single-witness spine-coverage finding; all 5 pipelines are characterized vs the spine (batch-18 meta-phase, post-c060). The §3.7-`iterate_while`-shared-parent was RATIFIED batch-17 (no third parent abstraction). The carries below are the not-yet-actionable trigger-gated residuals.)*
- `fold-solve-greedy-schedule-source-generalization` (c058 D1) — the SweepAdaptive greedy state-generated-schedule form is the `schedule-source` VARIANT AXIS of firm `L4/fold_solve.md` (ONE combinator); whether the greedy form warrants a dedicated chapter is a size-judgment. The no-L2-floor warrant carries to it (same opaque per-step leaf, `drivensolver.cpp:389`). *Trigger:* a batch-19+ downstream PROM consumer of the greedy march, OR a size-judgment dispatch.
- `fold-solve-time-step-body-cosmetic-residue-cohort` (c059 D1 + c060 D2, folded) — two LOW-fan-out cosmetic residues on `book/src/L3-L2/fold-solve-time-step-body.md`: (a) slug `fold-solve-time-step-body` describes "per-step body" but the substantive content is the outer-sweep erasure (slug-stability argues against an in-flight rename); (b) `:82` "sole act `ode->Step`" is literally imprecise (`:409`/`:412` also save/restore `dt`, a guard; warrant unaffected). *Trigger:* an opportunistic low-priority lifter touch on that theme — NOT a standalone dispatch.
- `solve-family-firm-on-structure-vs-test-coverage` (c055 D1+D2 cohort — folds `solve-family-status-firm-on-positive-structure-vs-test-coverage-bounded` + `solve-family-map-dissolution-firm-on-structure-vs-lhs-test-coverage`) — the `L4/solve_family.md` cap landed `rough-in (test-coverage-bounded)`; its concatenation-homomorphism (law 1) + operator-capture-hoist (law 2) are firm-on-positive-structure, but the **independence** law (no hidden cross-element `KspSolver`-reuse state / `RHS`-buffer-aliasing) is test-unconfirmed. The firm L4>L3 `solve-family-map-dissolution` theme is firm-on-structure regardless (the dissolution shape holds even if independence is demoted). *Trigger:* a `lowering-verifier` `KspSolver`-statefulness / `RHS`-buffer-aliasing pass — would promote the cap rough-in→firm on the firm-on-positive-structure escape (the dissolution theme stays firm either way). Did NOT run in batch-17.
- `time-step-op-opaque-mfem-integrator-boundary` (c057 D4) — the eventual `fold_solve` per-step body bottoms out in `ode->Step(sol,t,dt)` (`timeoperator.cpp:410`) → an MFEM/SUNDIALS-owned `mfem::ODESolver` (`obstruction (opaque-library-ownership)` sub-kind). Palace owns the outer time sweep + `sol`-threading, NOT the single integration step. *Trigger:* the firm `fold_solve` lowering needs the integrator's per-step semantics (e.g. SDIRK23's implicit solve) — log as an upstream MFEM OQ then; cite Palace's *call*, not MFEM internals.
- `fe-bc-elimination-l1-l0-theme-split-vs-fold` (c055 D3/D4 + c057 D2 cohort — folds `eliminate-rhs-mutation-rotation-l1-l0-theme-unauthored` + `eliminate-essential-bc-l1-l0-lowering-re-anchor` + `eliminate-rhs-mutation-rotation-sibling-stub-candidate`) — the now-firm `fe-operator-assemble-mutation-rotation` theme folds both BC-elimination legs' L0 narration **inline** (fully cited; no dead reference; firm-flip not blocked). Whether to split the two legs into dedicated sibling L1>L0 themes (`eliminate-rhs-mutation-rotation` + `eliminate-essential-bc-mutation-rotation`, anticipated by the firm operators' §Downward-to-L0 + the `eliminate_rhs.md` frontmatter `lowers_to:` slug) is an **abstractor decision, out of lifter scope**. Stub-materialization candidate (≥2 converging plain-text refs already). *Trigger:* an abstractor decides to split (LOW fan-out), OR the stub-materialization bar recurs (a 3rd+ converging reference / a consumer needing the live link). See plan Backlog Low.
- `fe-assembly-obstruction-verdict-vocabulary-and-boundary-precision` (c055 D5 cohort — folds `boundary-anchor-verdict-flavor-vs-negative-anchor-reconciliation` + `operatorcootocsr-palace-vs-libceed-ownership-fine-line`) — `fe-assemble-libceed-boundary-obstruction.md` introduced a `verdict: boundary-anchor` flavor (a positive-but-library-owned site, distinct from `triangular-solve-obstruction`'s `negative-anchor` for an *absence*); and the COO→CSR Palace-vs-libCEED split has a fine line at `OperatorCOOtoCSR` `:487-499` (the shell/leaf split is robust regardless). Both non-blocking — the theme is internally consistent. *Trigger:* a `lowering-verifier` obstruction-corpus pass — normalize the verdict vocabulary across the obstruction themes + settle the COO→CSR fine line by reading `OperatorCOOtoCSR`'s body.
- `fe-assemble-anchor-form-and-count-presentation` (c055 D6/D7 cohort — folds `fe-operator-assemble-addsuboperator-range-vs-pinpoint-convention` + `l1-fe-sub-spine-vs-main-cohort-unified-firm-count-renumber`) — the `AddSubOperator` accumulation is cited as a pinpoint (`:77`/`:97`) in the theme but as a range (`:71-77`/`:91-97`) in `fe_assemble.md` §Context (both correct, different granularities — flagged so a verifier does not read it as drift); and `L1/index.md` carries a split firm count (26 main cohort + 3 FE sub-spine = 29 grand total) that a future refresh could unify by folding the FE bullets into the main cohort. Both non-gating presentation choices. *Trigger:* a `lowering-verifier` cross-check of the FE-assembly cohort anchors / a `layer-intro-author` L1-index refresh.
- `fe-bc-dof-set-and-set-subvector-concept-pages` (c055 D3/D4 cohort — folds `set-subvector-essential-dof-mask-concept-page` + `dof-set-concept-page`) — `eliminate_rhs`/`eliminate_essential_bc` reference `set_subvector` (general write-mask, shared with `divfree-projector`'s `set_subvector_zero`) + `DofSet[N]` (essential-true-dof index subset) as **concepts**, not firm L1 spine operators; neither has a concept page. NOT required for any firm land (both are thin; gate no law). *Trigger:* a `layer-intro-author` concept-page dispatch covering the BC-application cohort, OR a 2nd+ converging reference to either (`book/src/concepts/set-subvector.md` + `book/src/concepts/dof-set.md` would be the homes).

### orthogonalize L2 composition family
*(opened_at: cycle-019, opened_by: harvester — from the firm L2 `orthogonalize` composition harvest)*
- `orthogonalize-composition-lowering-l2-l1-theme` — the `L2-L1/orthogonalize-composition-lowering` theme is not yet authored (abstractor). Must narrate forward L2→L1 how the named composition lowers into the firm L1 leaf: variant-dispatch on `op.variant`, and which L0 summation / reduction order each lowered variant pins (the load-bearing content of the reduction-tree and variant-agreement non-laws). Raw material: the firm L1 leaf `orthogonalize` (cycle-012) + its `L1-L0/orthogonalize-mutation-rotation` theme (firm cycle-013). Referenced as **plain text** in the firm L2 entry (the chapter does not yet exist; singleton forward-ref, not stub-materialized this pass). **Backlog migration** candidate — recommend the cycle-019+ planner queue it. *Trigger:* an abstractor L2>L1 dispatch on `orthogonalize` (now-firm L2 anchor ready).
- `orthogonalize-l2-record-vs-l1-tuple-naming` — the L2 entry uses the record form `{ residual: Tensor[N], coeffs: Tensor[m] }` to match `krylov-step`'s `op.orthog` call-shape (the consumer reads `.residual` / `.coeffs`); the firm L1 leaf uses the bare tuple `(w', H)`. Same value-pair; the record naming is the L2 composition-surface convention (cf. `krylov-step`'s `{ state, outputs }`). Flagged in case a cross-cutter prefers tuple-uniformity across the L1/L2 boundary. *Trigger:* a same-layer-cross-cutter / cross-layer-cross-cutter pass on the L1↔L2 result-shape convention.
- `orthogonalize-inner-product-constituent-tightening` — the L2 entry's `project` stage batches inner products that are a use-site of the rough-in L2 `inner_product` fold. Once `inner_product` firms (it is a sibling stub in the same `L2/index.md` dep-map), the `project` stage could cite `inner_product` as the inner-product constituent rather than `dot` directly. NOT done here (one-operator discipline + `inner_product` is still rough-in). The relationship is **constituent, not parent** — `orthogonalize` is NOT an instance of `inner_product`. *Trigger:* `inner_product` firms (then a lifter / cross-cutter tightens the `project`-stage citation).
- `L2-layer-intro-refresh-for-named-compositions` — the L2 `index.md` Working Notes (`L2/index.md:41`) state the cycle-005 firm-up "did **not** introduce a new L2 entry for `orthogonalize`… that remains a candidate for a future harvester invocation." With this entry landed (firm cycle-019), that note is now **stale** and the dep-map row has flipped stub→firm. Updating the Working-Notes prose is layer-intro-author territory (not in scope for the harvest); flagging for a refresh alongside the `incremental-least-squares` lift mentioned in the same note. *Trigger:* a layer-intro-author dispatch refreshing the L2 index Working Notes.

### divfree Mult doc-comment fidelity (opened_at: cycle-019, opened_by: cross-layer-cross-cutter)
- `divfree-mult-doc-irrotational-vs-divfree-stale` — **RESOLVED-by-this-report; closure-ready, meta-phase to close + migrate to the Closed index.** Carry-forward OQ (originated L1 entry cycle-013, re-surfaced cycle-016, tracked only via the plan `priorities.md:27` + `cycle-019-resume-notes.md:88`, NOT previously in this ledger). A cross-layer-cross-cutter dispatch (`reports/2026-05-29T023000Z-cross-layer-cross-cutter-divfree-doc/`) adjudicated it **(a) resolve** with firsthand L0 verification. **Disposition: answered — Palace-internal stale per-method doc-comment.** `palace/linalg/divfree.hpp:64-66` ("the irrotational portion … satisfying ∇ × y = 0") is **inverted**: in the Helmholtz/Hodge decomposition `y = y_divfree + Grad·ψ` the irrotational (`Grad·ψ`) component is the part the projector *removes*; the comment names the removed part where it should name the divergence-free remainder. The authoritative L0 site is the **class** doc `palace/linalg/divfree.hpp:28-31` ("projection onto a divergence-free space satisfying `Gᵀ M x = 0`"), realised by the implementation `palace/linalg/divfree.cpp:155-190`; a third independent L0 witness is the inline impl comment `palace/linalg/divfree.cpp:176` ("Compute the irrotational portion of y and subtract."). No artifact defect — the firm entries already pin the correct divergence-free semantics (`book/src/L1/divfree-projector.md:145-150`, `book/src/L1-L0/divfree-projector-mutation-rotation.md:460-468`, the latter sharpened this cycle to record the resolution self-contained). **No re-anchor, no lifter follow-up; upstream comment-fix is out of project scope.** *Trigger:* meta-phase unify-pass — move to the Closed index and flip the `priorities.md:27` plan item to resolved.

### combinator-miner parametric-family mode — first live exercise (opened_at: cycle-019, opened_by: combinator-miner)
*(from `reports/2026-05-29T023000Z-combinator-miner-parametric-family/` — the FIRST live exercise of the cycle-018-enacted parametric/variadic-family detection mode. PROPOSAL / mode-validation report; emitted NO `book/` mutation — the `inner_product` L2 rough-in row already stands from cycle-018. Two of the items below are **methodology-agenda intake for the next meta-phase**; the rest are harvester-input characterization for the in-flight `inner_product` harvester.)*

- `inner-product-fold-family-mode-characterization` — **harvester-input (feeds the in-flight cycle-019 `inner_product` L2 harvester, integration #7 next).** The parametric-family mode independently surfaced the `inner_product` reduce-to-scalar fold as ONE family across three structured parameter axes — **conjugation convention** (`dot` Hermitian `conj(x[i])·y[i]` vs `tdot`/`TransposeDot` bilinear `x[i]·y[i]`), **element-type** (real `Vector` fold lifted componentwise to complex `ComplexVector` over (Re,Im), `vector.cpp:674-685`), **weight presence** (`M=I` plain vs pre-applied `M`) — plus a fourth **diagonal-degeneration** value (`y=x` → norm-squared, the `nrm2`/`matrix-weighted-norm` *consumer*, NOT a fold member). The **fold-law membership test** (the evidence the cohort is one family, not a cluster): split-additivity / concatenation-homomorphism `inner_product (x₁++x₂)(y₁++y₂) = inner_product x₁ y₁ + inner_product x₂ y₂` — a monoid homomorphism `(length-concat,++) → (Scalar,+)`, mirroring the firm `linear_combination` concatenation-homomorphism (`book/src/L2/linear_combination.md:117-124`). Over-unification guards (REQUIRED, all three hold): do NOT merge with `linear_combination` (reduce-to-`Tensor[N]`, different result-type/combining-step/fold-axis — boundary already drawn `L2/index.md:25-26`); do NOT subsume `nrm2`/`matrix-weighted-norm` (`√ ∘ inner_product` consumers); do NOT subsume the smoother cohort (operator-action, not a fold — see the mode-gap item below). **Post-repair**, the M-weighted member's L0 form is `yᴴ A x` (Palace's `Dot(comm,x,A,y)` builds `Ax=A·x` then `Dot(comm,Ax,y)`, and `Dot` conjugates its SECOND operand `y`; `operator.cpp:621-628`, `vector.cpp:263-267`, `vector.hpp:246`), matching the standing stub `book/src/L2/inner_product.md:7` — the harvester inherits the corrected math directly. Conjugation-pinning is the hard must-resolve-before-firm (already tracked, see next item). *Trigger:* the in-flight `inner_product` L2 harvester folds this characterization into its firm entry (no separate dispatch needed — it is the next integration this cycle).
- `inner-product-tdot-uncalled-evidentiary-weight` — `tdot`/`ComplexVector::TransposeDot` (`vector.hpp:112` decl + `vector.cpp:269-274` def) has **zero call sites** in the Palace tree (independently confirmed by the critic's `search_text TransposeDot` over `palace/**` → only the decl+def lines). It is a real, defined kernel and a legitimate conjugation-axis value (the unconjugated bilinear form), but its evidentiary weight is **type-API-surface only, not behavioral**. Combinator-miner recommends keeping it as a family member with a "no Palace call site" caveat (cf. `bilinear-form`'s narrow-coverage note) rather than demoting it to a documented-but-not-instantiated axis value. **Question for the harvester:** cite `tdot` as firm (matching `book/src/L1/dot.md` which treats it as a co-defined operator) or rough-in? *Trigger:* the `inner_product` harvester decides the citation tier (same in-flight dispatch).
- `inner-product-weighted-split-additivity-whole-vector-only` — the split-additivity homomorphism holds elementwise for the plain/conjugate members, but for `inner_product_M` the *per-block* split requires `M` block-diagonal w.r.t. the split; the *whole-vector* form always holds (one fold over the M-applied operand). The harvester should state the law at whole-vector granularity to avoid over-claiming a per-block tiling law for the weighted member. *Trigger:* the `inner_product` harvester firm-up (same in-flight dispatch).
- **Conjugation-pinning** — already tracked by the active plan OQ `inner-product-harvester-formalization-and-conjugation-pinning` (§Open — migrated to the plan, plan **Now #1/#2**). This report RAISES it to a hard must-resolve-before-firm and supplies the resolved L0 reading (`yᴴ A x` for the weighted member; the L1 `dot.md:34,43` deliberate arg-1-conjugate re-order is an intentional L1 mutation-rotation, NOT a Palace-internal contradiction — the report self-corrected to that framing, critic-affirmed). No new OQ slug; the existing plan item owns it. *Trigger:* already plan-active.

### Routes to meta-phase — combinator-miner parametric-family mode-gap (opened_at: cycle-019, opened_by: combinator-miner)
*(methodology-agenda intake for the NEXT meta-phase to adjudicate; mirror into `priorities.md` §Next-meta-phase methodology agenda. This is the load-bearing mode-validation finding from the first live exercise — feeds friction-ledger `combinator-miner-arity-blind-parametric-family-detection` resolution.)*
- `combinator-miner-nonfold-parametric-family-no-positive-channel` (Qualification B — the load-bearing finding) — **the parametric/variadic-family mode performed to spec on the fold case (validated: it independently surfaced the `inner_product` cohort as one candidate, catching a fold-family that arity-blind same-shape counting would see only as 3 too-thin leaves — `tdot` is 0 call sites), but it found a genuine spec gap.** The broad scan turned up a strong parametric cohort the mode's spec does NOT cover: the **smoother / constructed-operator-action family** — `JacobiSmoother` (`palace/linalg/jacobi.hpp:19`), `ChebyshevSmoother` + `ChebyshevSmoother1stKind` (`palace/linalg/chebyshev.hpp:23,86`), `DistRelaxationSmoother` (`palace/linalg/distrelaxation.hpp:30`), all `: public Solver<OperType>`, all templated `<Operator>`/`<ComplexOperator>` (element-type axis), all implementing one `Mult(x,y)` fixed-cost relaxation action parameterized by a relaxation kernel (Jacobi diagonal-scale / Chebyshev polynomial / hybrid DistRelaxation). This is unmistakably a **parametric family along element-type + relaxation-kernel** — BUT it is `Tensor[N] → Tensor[N]` **operator-action-shaped, NOT a fold** (no `-> Scalar`/`-> Tensor[N]` reduction, no combining-step identity, no concatenation-homomorphism). The mode's "if you cannot state a unifying fold/parametric law it is NOT a parametric family" guard *correctly excludes* it from a fold-candidate proposal — but the spec gives **no positive channel** for "this IS a structured parametric family, just not a fold one." Non-blocking here (no row lost; the cohort is already partly captured by `book/src/L1/chebyshev-smoother.md` + the `nested-constructed-operator-gate` concept), but a future non-fold parametric cohort with no existing capture could fall through the crack. **Recommendation for the meta-phase:** extend the parametric-family mode (or add a sibling note) to recognize **constructed-operator-action families** (parametric over kernel + element-type, unified by a shared `Solver<OperType>::Mult` action contract `y' = relax(op, x, y, initial_guess)` rather than a fold-law) as a distinct, reportable family class. Without this the mode is fold-complete but parametric-family-incomplete. *Routes to:* next meta-phase methodology adjudication.
- `variant-absorption-vs-instance-counting-policy` confirmation — the existing meta-phase-agenda OQ (§Routes to meta-phase, line ~95) asked whether the cycle-018 parametric-family mode addresses the variant-absorption-vs-instance-counting question. **This first live exercise CONFIRMS it does, with a refinement worth recording:** the mode is *concordant and complementary* with same-shape instance-counting (both point at the same L2 `inner_product` fold — cycle-018's same-shape pass already cleared the ≥3-instance bar and landed the rough-in), and the parametric-family mode's **distinctive deliverable is the fold-law membership-test + axis taxonomy, even when same-shape already surfaced the row** — i.e. it earns its keep as a *characterizer*, not only as a *surfacer*. The meta-phase can use this to close `variant-absorption-vs-instance-counting-policy` (the existing slug; close authority is meta-phase per §Write-authority partition — I only append this confirmation as intake). *Routes to:* next meta-phase (close `variant-absorption-vs-instance-counting-policy` with this confirmation; record the characterizer-deliverable refinement in the friction-ledger `combinator-miner-arity-blind-parametric-family-detection` resolution).

### inner_product L2 fold family (opened_at: cycle-019, opened_by: harvester)
*(from the firm L2 `inner_product` harvest — `reports/2026-05-29T024500Z-harvester-inner-product-l2/`, stub→firm. Conjugation pinned arg-1 `xᴴ y` / `xᴴ M y` to match the L1 `dot`/`bilinear-form` leaves; the arg-2-conjugated Palace `Dot(comm,x,y)=yᴴ x` source reconciled as the deliberate L1 mutation-rotation re-order — not a Palace-internal contradiction; critic ratified the conjugation-pinning crux as sound. 3 proposed-changes blocks landed: full firm rewrite of `book/src/L2/inner_product.md`, dep-map row flip `L2/index.md:26` rough-in→firm, SUMMARY de-stub.)*
- `inner-product-harvester-formalization-and-conjugation-pinning` — **RESOLVED-by-this-entry; meta-phase to close + flip the plan item.** This was the highest-fan-out plan head item (plan **Now #1/#2**), owning the conjugation-pinning hard-must-resolve. **Disposition: answered.** The L2 `inner_product` entry pins **arg-1-conjugated** (`xᴴ y`, weighted `xᴴ M y`) to stay consistent with both L1 leaves (`dot.md:34,43`, `bilinear-form.md:19,63`); the value-level re-order vs Palace's source/doc `yᴴ x` (arg-2 conjugated; `vector.hpp:109,242,246`, `operator.hpp:386,391`, kernel bodies `vector.cpp:263-267,674-685`, weighted `operator.cpp:621-628`) is documented in the entry §"Conjugation convention (pinned)" as complex-conjugate-of and forward-handed to the L2>L1 lowering theme to map onto the L0 call (high→low discipline). Palace verified self-consistent (the prior combinator-miner "contradiction" reframed as representation-vs-Palace, critic-affirmed). *Trigger:* meta-phase unify-pass — move to the Closed index and flip the `priorities.md` Now #1/#2 plan item to resolved.
- `inner-product-fold-sibling-candidate` — **RESOLVED-by-this-entry; meta-phase to close.** The sibling-fold candidate question ("is `inner_product` a genuine fold sibling of `linear_combination`, distinct and not-to-be-merged?") is settled: the firm entry draws the two-sided boundary (`inner_product` reduce-to-`Scalar` collapsing length axis `N` via `(length-concat,++)→(Scalar,+)`; `linear_combination` reduce-to-`Tensor[N]` folding the term axis, keeping `N`; different result-type/combining-step/homomorphism/laws — no bridge identity claimed), reciprocated at `linear_combination.md` §"Sibling fold: dot is not subsumed". The fold is now firm. *Trigger:* meta-phase unify-pass — move to the Closed index.
- `inner-product-fold-specialization-l2-l1-theme` — the `L2-L1/inner-product-fold-specialization` theme is not yet authored (abstractor — cycle-019 dispatch #2, the NEXT integration this cycle). Must narrate forward L2→L1 how the L2 fold lowers into the L1 leaves: conjugation/weight dispatch (Hermitian→`dot`, unconjugated→`tdot`, weighted→`bilinear-form`); element-type dispatch (real Hypre `hypre_SeqVectorInnerProd` kernel vs complex four-real-dot lift); the value-level conjugation re-order `xᴴ y ↔ yᴴ x` mapped onto the L0 call; and which L0 reduction tree each lowered call pins (the load-bearing content of the IEEE reduction-tree non-law). Referenced **plain-text** in the firm L2 entry (the chapter exists as a stub at `book/src/L2-L1/inner-product-fold-specialization.md`; the NEXT report firms it — NOT stub-materialized this pass per dispatch). *Trigger:* the in-flight cycle-019 L2>L1 abstractor dispatch on `inner-product-fold-specialization` (integration #8 this cycle; now-firm L2 anchor ready).
- `inner-product-tdot-member-status-citation-tier` — `tdot` is carried as the unconjugated conjugation-axis **member** with a **type-API-surface-only** evidentiary caveat: `ComplexVector::TransposeDot` (`vector.hpp:112` decl + `vector.cpp:269-274` def) has zero Palace call sites (critic-confirmed `search_text TransposeDot` → only decl+def). The entry's overall status is `firm` (fold structure + two behaviorally-exercised axis values `dot`/weighted); only `tdot`'s behavioral weight is API-only. The harvester kept it a full firm-member (matching `dot.md`'s co-defined-operator treatment) with the caveat at member granularity rather than demoting to a documented-but-not-instantiated axis value. If a later audit prefers the demote-to-axis-value posture, that is a member-level refinement, not an entry-level status change. *Trigger:* a lowering-verifier / cross-cutter audit of the `tdot` member behavioral coverage (non-blocking; shares the `bilinear-form` narrow-coverage caveat).
- `inner-product-weighted-split-additivity-whole-vector-only` — the split-additivity / concatenation-homomorphism law (entry law 2) holds elementwise/per-block for the plain/conjugate members; for `inner_product_M` the per-block split requires `M` block-diagonal w.r.t. the split, so the law is stated at **whole-vector granularity** for the weighted member to avoid over-claiming a per-block tiling law. Recorded in the entry §"Algebraic laws" law 2 + §"Variant axes". No further action; carried for visibility. *(Note: a same-slug intake row exists in the wave-1 combinator-miner group above — this is the harvester's confirmation that the entry states the law at whole-vector level as recommended; meta-phase may dedup.)* *Trigger:* none (informational; meta-phase dedup with the wave-1 row).
- `inner-product-empirical-match-complex-weighted-untested` — the real-vector inner product has a direct value-asserting unit test (`test/unit/test-vector.cpp:206-207`, `1·4+2·5+3·6=32`); the complex/Hermitian and M-weighted members have **no dedicated value-asserting unit test** — grounded by direct source-transcription (kernels + weighted free-function + SPD-realness assertion) plus verified live call sites (`iterative.cpp:395`, `nleps.cpp:487,492`, `boundarymodeoperator.cpp:85,90`). The firm-without-full-test bar follows the `chebyshev-iteration` / `linear_combination` precedent (source-transcription confidence + integration coverage). *Trigger:* a `find-tests-for-region` / lowering-verifier pass if a complex/weighted value-assertion test surfaces or is wanted.
- `L2-layer-intro-refresh-for-fold-cohort` — the L2 `index.md` §"Semantics (overlay)" primitive list (`axpy, dot, matvec, gemv, trsv, scal, nrm2`) predates the L2 fold cohort; with `inner_product` + `linear_combination` both now firm, the overlay could note the two reduce-to-X fold siblings (reduce-to-`Scalar` / reduce-to-`Tensor[N]`) as first-class L2 forms. Layer-intro-author territory (out of harvester scope). *(Note: pairs with `L2-layer-intro-refresh-for-named-compositions` from the wave-2 `orthogonalize` harvest above — same L2 index Working-Notes / overlay refresh; meta-phase may fold into one layer-intro flag.)* *Trigger:* a layer-intro-author dispatch refreshing the L2 index overlay + Working Notes.

### inner-product-fold-specialization L2>L1 theme family (opened_at: cycle-019, opened_by: abstractor)
*(from the firm L2>L1 `inner-product-fold-specialization` theme — `reports/2026-05-29T024500Z-abstractor-inner-product-fold/`, stub→firm. Dispatch #2 / integration #8 — the LAST cycle-019 report. Lowers the now-firm L2 `inner_product` fold into its L1 leaves (`dot` Hermitian / `tdot` unconjugated / `bilinear-form` M-weighted) by a conjugation-convention / element-type / weight-presence dispatch; the headline crux — the value-level `xᴴ y ↔ yᴴ x` conjugate-pair re-order — PASSED critique as sound and L0-verified. 3 proposed-changes blocks landed: full firm rewrite of `book/src/L2-L1/inner-product-fold-specialization.md`, firm theme row appended to `L2-L1/index.md:14`, SUMMARY de-stub at `:49`. Resolves the forward pointer `inner-product-fold-specialization-l2-l1-theme` (line 141 above) — the wave-2 abstractor dispatch it pointed at.)*
- `inner-product-fold-specialization-l2-l1-theme` — **RESOLVED-by-this-theme; meta-phase to close** (was the forward pointer at line 141, opened by the #7 harvester). The L2>L1 theme is now authored + firm: it narrates forward L2→L1 the conjugation/weight dispatch (Hermitian→`dot`, unconjugated→`tdot`, weighted→`bilinear-form`), the element-type dispatch (real Hypre `hypre_SeqVectorInnerProd` vs complex four-real-dot lift), the value-level conjugate-pair re-order `xᴴ y = conj(yᴴ x)` mapped onto the L0 call (operand-swap `linalg::Dot(comm,y,x)` or outer-`conj`), and the per-call pinned reduction trees (the load-bearing IEEE-non-law content the L2 entry deferred). All 11 L0 ranges critic-re-verified. *Trigger:* meta-phase unify-pass — move to the Closed index (dedup against the line-141 forward pointer).
- `inner-product-fold-specialization-lowering-verifier-audit` — the standard lowering-verifier follow-up: a per-line audit confirming the dispatch rule + the conjugate-pair re-order rule + the summation-order table against the L0 source, attaching a `verified_against:` block. Non-status-reducing (the theme is firm). Pairs with the conjugate-pair caller-classification refinement below. *Trigger:* a `lowering-verifier` dispatch on `L2-L1/inner-product-fold-specialization`.
- `inner-product-conjugate-pair-reorder-caller-classification` — the theme records the operand-swap form (`linalg::Dot(comm,y,x)`) and the outer-conj form (`conj(linalg::Dot(comm,x,y))`) as equivalent recoveries of the L2 `xᴴ y`; a full caller audit classifying every Palace `linalg::Dot` site as "real-projected (re-order invisible)" vs "full-complex (re-order observable, e.g. the non-Hermitian cross-coupling `boundarymodeoperator.cpp:90`)" would tighten the re-order story to per-site precision (small, not blocking). The bit-determinism half is covered by the existing OQ `dot-reduction-tree-determinism-survey`. *Trigger:* a `lowering-verifier` / `same-layer-cross-cutter` follow-up.
- `inner-product-weighted-member-two-stage-reduction-tree` — the weighted dispatch arm's bit-reproduction requires pinning BOTH the M-application reduction (the operator's internal SpMV/quadrature tree, opaque behind `apply_linop`) AND the subsequent dot reduction; the theme's summation-order table records this as a two-stage tree but does NOT enumerate the M-apply tree (it is `apply_linop`-internal + matrix-free-representation-dependent). A full weighted-member bit-reproduction story would pull the M-apply tree from the `apply-linop-mutation-rotation` theme. The abstractor noted this is **tracked under the existing OQ `apply-linop-lowering-verifier-audit-cohort`** (no new tracking intended) — recorded here as a cross-reference, not a duplicate. *Trigger:* folds into `apply-linop-lowering-verifier-audit-cohort`.
- `linear-combination-fold-specialization-theme-followups` — **abstractor recommends RESOLVABLE on the linear_combination side; meta-phase to assess** (this is an existing plan-migrated item — line 32 above pairs it with the inner_product head item). The wave-2 abstractor reviewed the cycle-018 sibling theme's three live follow-ups (its lifting note, its no-dedicated-test caveat, its arity-≥4 chunking note) and found NONE small/in-scope for the inner-product theme: the lifting note is mirrored (inner-product working-note); the no-test caveat is orthogonal (the linear-combination free functions lack a test, whereas the real `inner_product` HAS one at `test/unit/test-vector.cpp:206-207` — the complex/weighted-test gap belongs to `L2/inner_product`, tracked at `inner-product-empirical-match-complex-weighted-untested` above); the arity-≥4 note has no analogue (inner_product dispatches on conjugation/weight, not arity). The inner-product theme consumes NONE of them. *Trigger:* meta-phase unify-pass — assess closing/migrating the linear_combination follow-ups on their own side (caveats already in that theme, not blockers).
- `inner-product-fold-specialization-operator-cpp-inline-anchor-drift` (opened_at: cycle-020, opened_by: lowering-verifier) — the cycle-020 `lowering-verifier` audit of the firm `L2-L1/inner-product-fold-specialization` theme found it **fully-supported / keep firm** (every dispatch arm, the conjugate-pair re-order, and the summation-order table are semantically faithful to the verified Palace bodies; the verdict block is appended to the chapter as a `verified_against:` yaml block, integration #7 cycle-020), **with three genuine inline-anchor drifts** the theme's prose asserts as verified but `read_range` places 1-2 lines off: (1) §"The weighted-member workspace" real-`Operator` `Ax` allocation `operator.cpp:623` → **`:624`**; (2) same section `ComplexOperator` `Ax` `operator.cpp:632` → **`:634`**; (3) §"diagonal degeneration" + §Verified-against SPD assertion range `operator.cpp:615-616` → **`:616`** (single line; `:615` is the preceding `std::complex<double> dot = Dot(comm, Bx, x)` statement). The wide enclosing ranges (`:598-617`, `:621-628`, `:631-638`) are NOT in error — only the pinpoint inline anchors drifted. **NOT a status reduction** (the dispatch structure + named L0 bodies are correct; only the exact line offsets drifted; in-scope per `lifter-scope-content-correction-boundary`). The SPD comment is NOT in this list — the live theme already pins it at `:612` (verified; an earlier framing of the report listed a `:611`→`:612` drift but `:611` appears nowhere in the committed file — the repairer dropped that phantom item). Mirrors the cycle-012 SLEPc-NEP `:387`→`:383` precedent (both a report-anchor fix and an integrator carry-forward). *Trigger:* a `lifter` dispatch on `book/src/L2-L1/inner-product-fold-specialization.md` (or an integrator carry-forward touch) correcting the three inline anchors `:623`→`:624`, `:632`→`:634`, `:615-616`→`:616`.

### cycle-046/047/048 New-intake — COMPACTED to the Closed index (batch-14 meta-phase)
The verbose cycle-046 New-intake blocks (L4/L4>L3 coverage survey, erasure-scope concept page, residual-L2>L1-gap audit) + the cycle-047/048 per-report integration-disposition blocks are compacted by the batch-14 meta-phase into **§Closed (index) → "Closed by the batch-14 meta-phase"** (resolved/landed → one-line dispositions) and the kept-deferred carries below. Full prose in git history (the "git history is the historical record" methodology); slug strings preserved as cross-reference anchors. The one low-severity carry from the c046 erasure-scope block (`erasure-scope-l3-l2-index-line-anchor-drift-risk`) is kept in **Open — deferred / contingent → orthogonalize / concepts / citation hygiene**. The `l4-orthogonalize-cap-marginal-defer` (R5) carry is already mirrored by the deferred `l4-orthogonalize-arnoldi-step-monad-surface-unauthored`.

## Closed (index)

One line per slug; full prose in git history. Slugs preserved as cross-reference anchors.

### Resolved / answered in earlier cycles (compacted from the former `## Answered` section)

- `axpby-axpbypcz-next-harvest` — answered cycle-004 — both halves firm at L1; axpbypcz with subsumption chain.
- `axpbypcz-l1-harvest` — answered cycle-004 — firm L1 axpbypcz, 12 laws, two variant axes.
- `scal-primitive-l1-harvest` — answered cycle-004 — firm L1 scal, nine module-axiom laws.
- `l1-index-refresh` — answered cycle-004 — L1 index refreshed (Context/Semantics/Vocabulary-cohort).
- `l1-index-refresh-trigger-met` — answered cycle-004 — refresh landed once firm-operator trigger met.
- `concepts-dot-return-type-correction` — answered cycle-004 — element-type return rule corrected (concepts/dot.md).
- `concepts-dot-dotc-and-inverted-conjugation` — answered cycle-004 — non-existent linalg::Dotc removed; conjugation polarity fixed.
- `dot-backpointer-staleness-after-rewrite` — answered cycle-004 — stale warning replaced with clean back-pointer.
- `dot-blas-heritage-framing-salvage` — answered cycle-004 — BLAS-1 heritage framing kept while specifics corrected.
- `krylov-step-dual-placement-l2-l4-routing` — answered cycle-005 — L2 + L4 with lowering edge.
- `krylov-step-naming-reuse-vs-disambiguation` — answered cycle-005 — same-slug-different-layer; deferred to L4 harvester.
- `krylov-step-l3-identity-in-form-audit` — answered cycle-006 — confirmed-with-refinement by abstractor audit.
- `state-stratification-as-l4-concept-or-l4-row` — answered cycle-005 — flagged to L4 harvester/cycle-planner.
- `scalar-promotion-retroactive-l1-thinning` — answered cycle-005 — routed as priority; four L1 entries thinned.
- `scalar-promotion-l4-calculus-formalisation` — answered cycle-005 — deferred as L4-calculus-design work.
- `l4-row-vs-concept-dependency-convention` — answered cycle-006 — convention honoured by entry content.
- `iterate-while-l4-anchor-missing` — answered cycle-007 — iterate_while + iterate_while_with_prev firmed as L4 rows.
- `krylov-step-l3-row-contingency` — answered cycle-006 — contingency does not fire (abstractor confirms-with-refinement).
- `l4-layer-intro-refresh-unblocked-by-first-firm-row` — answered cycle-008 — L4 intro grounded in three firm operators.
- `concepts-index-kind-classification-full-audit` — answered cycle-006 — 40-row Kind-audit routed as bounded dispatch.
- `same-layer-cross-cutter-cycle-md-write-failure` — answered cycle-006 — routed to meta-phase; no content loss.
- `concepts-index-auxiliary-kind-usage-review` — answered cycle-006 — recorded as future concept-sweep review item.
- `concepts-axpby-axpbypcz-pages-absent` — answered cycle-006 — concept-page authoring routed as cycle-007+ candidate.
- `open-questions-ledger-backreference-audit` — answered cycle-006 — routed to future meta-phase/layer-intro pass (superseded by this unification).
- `krylov-step-l3-identity-in-form-audit-closure-cycle-006` — answered cycle-006 — closure-note resolving two parent OQs.
- `krylov-step-body-identity-theme-pending-cycle-007` — answered cycle-007 — theme authored (L3-L2/krylov-step-body-identity.md).
- `iterate-while-l3-rendering-trajectory-accumulation-gap` — answered cycle-008 — verdict-(c) collapse-rule + Condition 5 applied.
- `ksp-solve-concept-page-signature-update` — answered cycle-007 — concept page defers to L1 SolveResult signature.
- `ksp-solve-mutation-rotation-l1-l0-theme` — answered cycle-008 — first firm L1>L0 constructed-operator theme.
- `l1-intro-refresh-after-constructed-operator-gate` — answered cycle-008 — three surgical L1 index edits.
- `gmres-inner-loop-iterate-while-migration` — answered-by-rough-in-theme cycle-008 — L4>L3 migration theme (rough-in).
- `iterate-while-pure-promotion-decision` — answered cycle-007 — defer; keep sugar inside iterate-while.
- `iterate-while-log-effect-vs-trajectory-channel` — answered cycle-007 — routed to cycle-008+ verifier/abstractor.
- `eigsolve-l1-operator-rough-in-candidate` — answered (partially) cycle-009 — eigsolve landed rough-in (now firm).
- `matrix-weighted-norm-and-bilinear-form-l1-rough-ins` — answered (partially) cycle-010 — both rough-ins landed; firm gates → plan.
- `l0-bundle-5-candidates` — answered cycle-009 — bundle 5 landed 2 of 3; remainder → bundle 6.
- `tests-as-semantic-supplement-l0-vs-concepts-decision` — answered cycle-009 — placement routed to planner/meta-phase.
- `l0-bundle-6-candidates` — answered (partially) cycle-011/016 — bundle-6 ongoing (next candidate fespace → plan #5).
- `eigsolve-linear-solve-failed-status-anchor` — answered cycle-010 — LinearSolveFailed kept L1-constructive with negative anchors.
- `eigsolve-scaling-coordinate-convention` — resolved cycle-011 — un-scaled eigenvalues per un-scale-at-accessor.
- `eigsolve-initial-space-axis-placement` — resolved cycle-011 — initial_space kept in EigControl per-call.
- `eigsolve-iteration-count-result-field` — resolved cycle-011 — added iterations:Int as L1-constructive field.
- `l1-orthogonalize-promotion-from-arnoldi-step-and-orthog` — answered cycle-012 — firm L1/orthogonalize landed.
- `l3-index-matvec-naming-vs-apply_linop-slug` — answered cycle-012 — adopted matvec (apply_linop) parenthetical form.
- `concepts-nrm2-stability-claim-correction` — answered cycle-012 — false scaled-summation bullet replaced (concepts/nrm2.md).
- `l3-index-semantics-overlay-blas1-cohort-prose-refresh` — answered cycle-012 — overlay reframed; BLAS-1 cohort named.
- `eigsolve-slepc-nep-coordinate-convention-audit` — answered cycle-012 — resolved-with-refinement (NEP solves un-scaled).
- `orthog-plane-rotation-stream-sub-slice-batch-3-joint-audit` — answered cycle-012 — sub-slice reduced to stub; invariant hoisted.
- `l1-l2-chebyshev-smoother-and-iteration-firm-row-promotion` — answered cycle-012 — both rows firm.
- `concepts-state-stratification-four-stratum-extension` — answered cycle-012 — four-stratum split added.
- `concepts-derived-view-hoisting-control-flow-boundary-extension` — answered cycle-012 — control-flow-boundary example added.
- `concepts-negative-result-slice-partial-positive-sub-pattern-extension` — answered cycle-012 — partial-positive subsection added.
- `concepts-orthogonalization-coefficient-normalisation-drift` — answered cycle-013 — concept page aligned with firm L1 contract.
- `krylov-step-typed-wrapper-dissolution-cg-md-citation-sweep` — answered cycle-014 — 8 dangling cg.md pointers re-anchored.
- `krylov-step-l3-identity-in-form-audit-already-answered-note` — informational — note: already answered cycle-006; do not double-close.
- `l3-krylov-step-cg-md-citation-sweep` — answered cycle-015 — 5 dangling cg.md pointers re-anchored to terminal firm homes.
- `chebyshev-anchor-element-kernel-and-mult2-carry-forward-sweep` — resolved cycle-015 — 7 verified citation corrections across two anchor entries.
- `divfree-projector-partly-constructive-to-firm-enactment` — resolved cycle-015 — 5 firming edits; status flipped firm.
- `partly-constructive-entry-mechanism-validated-eigsolve-convergence-reason-mapping` — answered cycle-016 — ENTRY case validated; stays partly-constructive.
- `chebyshev-slice-l4-full-removal` — resolved cycle-015 — slice §L4 re-pointed; chebyshev.md removed (corpus 9/10).
- `l3-chebyshev-downward-prose-iterate-while-refresh` — resolved cycle-016 — stale L3 prose → iterate_while_pure.
- `l4-krylov-step-cg-md-citation-sweep` — answered cycle-016 — 7 dangling cg.md pointers re-anchored.
- `l2-krylov-step-cg-md-citation-sweep` — answered cycle-016 — 12 dangling cg.md pointers re-anchored + 1 CheckDot drift.
- `bundle-6-l0-libceed-operator-file-next-candidate` — resolved cycle-016 — fem-libceed-operator-file authored.
- `l4-chebyshev-residual-formm-foldm-prose-cleanup` — resolved cycle-016 — three stale forM_/foldM prose sites refreshed.
- `divfree-l1-entry-apply-close-and-reltol-line-drift` — resolved cycle-017 — 11 surgical citation corrections in firm L1 entry.
- `divfree-closure-nesting-constructed-gate-carrying-constructed-gate` — answered cycle-017 — premise refuted; divfree is third (not first) gate-carrying instance.
- `l3-l2-body-identity-cg-md-citation-sweep` — resolved cycle-017 — 3 dangling cg.md provenance pointers re-anchored.
- `l3-chebyshev-sibling-formm-foldm-prose-sweep` — resolved cycle-017 — all five sibling mentions refreshed; zero residual.
- `blas1-variadic-linear-combination-fold-unification` — resolved cycle-018 — prong-b (linear_combination firm) + prong-a (combinator-miner parametric-family mode, cycle-018 meta) both enacted.
- `linear-combination-harvester-formalization` — resolved cycle-018 — firm L2 chapter + L2>L1 lowering theme landed.
- `inner-product-fold-sibling-candidate` — answered cycle-018 — inner_product rough-in L2 row landed (harvester/pinning → plan #1/#2).
- `nested-constructed-operator-gate-concept-and-divfree-correction` — resolved cycle-018 — concept page authored + divfree "first" claims corrected.

### Closed by the batch-5 meta-phase (cycles 019/020/021; 2026-05-29)

- `divfree-mult-doc-irrotational-vs-divfree-stale` — resolved cycle-019 — Palace-internal stale per-method doc-comment (`divfree.hpp:64-66` inverted); authoritative L0 is class-doc `divfree.hpp:28-31` + impl `divfree.cpp:155-190,176`; no artifact defect (firm entries pin correct divfree semantics); upstream comment-fix out of scope.
- `inner-product-harvester-formalization-and-conjugation-pinning` — resolved cycle-019 — L2 `inner_product` firm, conjugation PINNED arg-1 `xᴴ y`/`xᴴ M y`; was plan Now #1/#2; answer `book/src/L2/inner_product.md`.
- `inner-product-fold-sibling-candidate` — resolved cycle-019 — firm entry draws the two-sided `inner_product`(reduce-to-Scalar)/`linear_combination`(reduce-to-Tensor[N]) boundary; reciprocated at `linear_combination.md`.
- `inner-product-fold-specialization-l2-l1-theme` — resolved cycle-019 — `L2-L1/inner-product-fold-specialization.md` firm (conjugation/weight + element-type dispatch + conjugate-pair re-order + per-call reduction trees).
- `assemblediagonal-is-not-apply-linop-variant` — resolved cycle-019 — `assemble_diagonal` lands its own firm L1 entry with the operator-to-data divide stated; answer `book/src/L1/assemble-diagonal.md`.
- `nrm2-std-abs-defensive-guard-classification` — resolved cycle-019 — load-bearing defensive non-negativity guard (domain-safety for `sqrt`), not a transparent trick; consistent with `L0/transparent-vs-load-bearing-tricks.md:22`.
- `nrm2-lowering-theme-deliverables` — resolved cycle-019 — `nrm2-mutation-rotation` firm: four-stage chain + 3 surface forms + variant collapse + abs-guard + MPI-collective.
- `assemble-diagonal-mutation-rotation` (theme-authoring) — resolved cycle-020 — `assemble-diagonal-mutation-rotation` L1>L0 theme firm (4 sub-patterns + complex-split sub-rule + matrix-free non-law).
- `gmres-inner-loop-iterate-while-migration` — resolved cycle-020 (supersedes the cycle-008 `answered-by-rough-in-theme` Closed-index line) — landed firm (slice §L4 v0.7, option (a) `check_stop_into_carry` witness-into-carry hoist).
- `inner-product-conjugate-pair-reorder-caller-classification` — resolved cycle-020 — full `linalg::Dot` caller census (44+20 sites; `conjugation_caller_inventory:` in `inner-product-fold-specialization.md` §Condition 5); bare-`dot` convention load-bearing in exactly one algorithm (SLEPc-NEP, 4 sites). Corroborated cycle-021 by Sub-pattern D.
- `l3-l2-ksp-solve-outer-driver-theme-warranted-gated-on-l2-promotion` — resolved cycle-021 — `book/src/L3-L2/ksp-solve-outer-driver.md` firm (substantive non-identity driver rotation; complement of `krylov-step-body-identity`); gate satisfied by L2 `ksp_solve` stub→firm same cycle.
- `ksp-solve-l2-promotion-non-identity-substantive-gap` — resolved cycle-021 — L2 `ksp_solve` outer-driver firm; resolves the maturity-gradient inversion (firm cycle-020 L3 above an L2 stub); answer `book/src/L2/ksp_solve.md`.
- `fgmres-inner-loop-iterate-while-migration-lifter-candidate` — resolved cycle-021 — terminally closed 5-batch carry-forward (cycle-010→021); `L4-L3/fgmres-inner-loop-iterate-while-migration.md` firm against the firm gmres sibling (no new self-rotation).
- `fgmres-inner-loop-iterate-while-migration-firm-against-gmres-sibling` — resolved cycle-021 — the cycle-020 trigger; enacted by the cycle-021 fgmres firm lifter dispatch (dedup with the carry-forward above).
- `orthog-hpp-localdot-globalsum-unfused-dot-surface` — resolved cycle-021 — Sub-pattern D (hook-routed `LocalDot` + batched `Mpi::GlobalSum`) added to `dot-mutation-rotation.md` + bypass-surface note on `inner-product-fold-specialization.md`; first unweighted-observable `dot` use outside the SLEPc-NEP cohort.
- `orthog-hpp-localdot-globalsum-unweighted-inner-product-surface` — resolved cycle-021 — dedup of the cycle-020 coverage-gap flag with the cycle-021 Sub-pattern D resolution above.
- `variant-absorption-vs-instance-counting-policy` — resolved cycle-021 meta-phase — addressed by the cycle-018 combinator-miner parametric-family mode, CONFIRMED concordant-and-complementary with same-shape instance-counting by the cycle-019 first live exercise (inner_product) + cycle-021 deflate/gram exercise; the mode's distinctive deliverable is the fold-law membership-test + axis taxonomy (a characterizer, not only a surfacer). See friction-ledger `combinator-miner-arity-blind-parametric-family-detection`.
- `combinator-miner-authority-defer-verdict-status-edit-scope` — resolved cycle-021 meta-phase — moot under the universal dispatch-phase write-guard: combinator-miner is proposal-only (emits a rough-in dep-map row; never edits status / emits a binding defer verdict); promotion/deferral is the downstream harvester's call.
- `test-coverage-bounded-rough-in-nomenclature` — resolved cycle-021 meta-phase — codified in CLAUDE.md §Methodology invariants as first-class rough-in qualifier `rough-in (test-coverage-bounded)` (structure firm; laws test/literature-gated).
- `partial-obstruction-status-codification` — resolved cycle-021 meta-phase — codified in CLAUDE.md §Methodology invariants as first-class L3 status `partial-obstruction` (body lifts; loop is a witnessed sequential-obstruction).
- `combinator-miner-nonfold-parametric-family-no-positive-channel` — resolved cycle-021 meta-phase — Qualification-B mode-gap CLOSED: combinator-miner gains a non-fold "constructed-operator-action family" reportable class (parametric over kernel + element-type, unified by a shared `Solver<OperType>::Mult` action contract, not a fold-law). See friction-ledger `combinator-miner-arity-blind-parametric-family-detection`.

### Closed by the batch-6 meta-phase (cycles 022/023/024; 2026-05-29)

Landing-record + carry-forward OQs from the New-intake section, closed here (full prose in git history; slugs preserved as cross-reference anchors). The two major cohort completions (NEP-interior atoms + eigsolve L1→L2→L3 chain) account for most of these.

*Eigsolve chain (L1→L2→L3 complete):*
- `eigsolve-l1-firm-landed-chain-step-1-done-l2-entry-unblocked` — resolved cycle-022 — L1 `eigsolve` rough-in(test-coverage-bounded)→firm via law-confidence re-eval (syntactic identities on positive source); chain step 1.
- `eigsolve-l1-rough-in-to-firm` — resolved cycle-022 — the step-1 plan candidate (dedup of the above).
- `eigsolve-l2-firm-landed-chain-step-2-done-l3-backfill-unblocked` — resolved cycle-023 — L2 `eigsolve` firm (shift-invert spectral-transform composition `apply_linop(M) ▷ ksp_solve((K−σM)⁻¹)`); chain step 2; answer `book/src/L2/eigsolve.md`.
- `eigsolve-l3-stub-materialized-cycle-024-backfill-refines-in-place` — resolved cycle-023/024 — L3 `eigsolve` stub (c023) refined in place to `partial-obstruction` (c024); answer `book/src/L3/eigsolve.md`.
- `eigsolve-l3-backfill-partial-obstruction-landed-cycle-024-chain-step-3-done` — resolved cycle-024 — chain step 3; eigen-iteration loop is a witnessed sequential-obstruction (opaque SLEPc `EPSSolve` / ARPACK `naupd` RCI); 2nd L3 partial-obstruction + 1st opaque-library one.
- `l3-eigsolve-blocked-on-l1-firm-and-l2-entry` — resolved cycle-024 — the strict prerequisite chain (steps 1/2/3) all DONE; was the migrated-to-plan blocker.
- `l3-eigsolve-linear-evp-has-no-krylov-step-kernel-analog` — resolved (CONFIRMED) cycle-024 — the cycle-021 structural prediction held; no Palace-authored linear-EVP eigsolve kernel/driver pair (the `ApplyOp` body is an `apply_linop ▷ ksp_solve` composition of firm L3 leaves).
- `eigsolve-l3-backfill-still-blocked-predicted-partial-obstruction` — resolved cycle-024 — the c022 forward-flag; prediction realized.
- `eigsolve-firm-source-read-confirmed-empirically-unwitnessed-residual-caveat` — kept (low-pri, source-read-confirmed/empirically-unwitnessed caveat does NOT gate firm; the dedicated `test-eigensolver.cpp` is out of project write-scope) — folds into the existing deferred `eigsolve-nep-coordinate-convention-empirical-witness`; no new tracking.

*NEP-interior atom cohort (5 firm L1 atoms + L1>L0 cohort):*
- `nleps-deflated-residual-l1-firm-landed` — resolved cycle-022 — `book/src/L1/nleps_deflated_residual.md` firm (deflation extension of `apply_nonlinear_pencil`).
- `nleps-deflated-residual-l1` — resolved cycle-022 — the plan candidate (dedup).
- `nleps-deflated-solve-firm-landed-deflate-promotion-gate-stays-open` — resolved cycle-023 — `book/src/L1/nleps_deflated_solve.md` firm (Schur-complement deflated block solve); CONFIRMS `deflate` partly-constructive (gate stays open).
- `nleps-deflated-solve-l1` / `nleps-deflated-solve-is-next-fan-out-ordered-nleps-piece-and-l2-deflate-gram-positive-site` — resolved cycle-023 — the next-piece plan flag; landed.
- `nleps-jacobian-action-firm-landed-cycle-024` — resolved cycle-024 — `book/src/L1/nleps_jacobian_action.md` firm (T'(λ)-derivative-pencil action; divided-difference A2' a load-bearing non-law).
- `nleps-eigenvalue-correction-firm-landed-cycle-024-nep-cohort-complete` — resolved cycle-024 — `book/src/L1/nleps_eigenvalue_correction.md` firm (δλ Rayleigh-functional correction); 5th/FINAL NEP atom; cohort COMPLETE.
- `nleps-interior-atoms-remaining-jacobian-action-and-eigenvalue-correction` / `nleps-interior-atoms-jacobian-action-half-resolved-eigenvalue-correction-remains` — resolved cycle-024 — both halves landed; supersedes the cycle-021 `nleps-deferred-l1-primitives-carry-forward`.
- `nleps-deferred-l1-primitives-carry-forward` — resolved cycle-024 — all 4 deferred pieces (+ `apply_nonlinear_pencil` c021) landed firm.

*NLEPS L1>L0 cohort (deflation + bare-pencil complete):*
- `lu-solve-mutation-rotation-l1-l0-landed-firm-cycle-023` / `lu-solve-mutation-rotation-l1-l0-theme-needed` — resolved cycle-023 — `book/src/L1-L0/lu-solve-mutation-rotation.md` firm (2 sub-patterns; load-bearing factorization-kernel axis).
- `nleps-deflated-residual-mutation-rotation-l1-l0-landed-firm-cycle-023` / `nleps-deflated-residual-l1-l0-lowering-theme-needed` — resolved cycle-023 — `book/src/L1-L0/nleps-deflated-residual-mutation-rotation.md` firm (3 sub-patterns).
- `nleps-deflated-solve-l1-l0-lowering-theme-firm-landed-cycle-024` / `nleps-deflated-solve-l1-l0-lowering-theme` — resolved cycle-024 — `book/src/L1-L0/nleps-deflated-solve-mutation-rotation.md` firm (enacts the c023 OQ).
- `apply-nonlinear-pencil-mutation-rotation-l1-l0-landed-firm-cycle-024-nleps-l1-l0-cohort-complete` — resolved cycle-024 — `book/src/L1-L0/apply-nonlinear-pencil-mutation-rotation.md` firm; the LAST plain-text-forward-referenced NLEPS L1>L0 leaf; cohort COMPLETE. (Citation note: `nleps.cpp:810-811` confirmed correct — the critic off-by-one finding was itself off by one.)
- `nleps-deflated-residual-l1-l0-interior-leaf-themes-still-forward-referenced` — resolved cycle-024 — both interior leaves (`apply_nonlinear_pencil`, `lu_solve` L1>L0) now firm.
- `nleps-l1-l0-lowering-cohort-deflation-themes-complete-pencil-leaf-remains` — resolved cycle-024 — pencil leaf landed; cohort complete.

*L1 `lu_solve` + BLAS-1 floor:*
- `lu-solve-l1-firm-landed-unblocks-deflate-gram` / `deflate-needs-small-dense-lu-solve-primitive` / `lu-solve-l1-dense-primitive` — resolved cycle-022 — `book/src/L1/lu_solve.md` firm (small-dense `k×k` pivoted direct solve); unblocked `deflate`/`gram`.
- `axpbypcz-mutation-rotation-callsite-correction-and-firm-RESOLVED` / `axpbypcz-mutation-rotation-callsite-correction-and-firm` — resolved cycle-022 — `book/src/L1-L0/axpbypcz-mutation-rotation.md` rough-in→firm (3 callsite corrections + range fix; D/B defined-not-used).
- `blas1-l1-l0-lowering-floor-CLOSED-8-of-8-axpbypcz-firm` / `blas1-l1-l0-lowering-floor-7-of-8-axpbypcz-remains` / `blas1-l1-l0-lowering-theme-gap` — resolved cycle-022 — BLAS-1 L1>L0 floor CLOSED 8/8; answer `book/src/L1-L0/index.md`.
- `axpbypcz-mutation-rotation-firm-direction-of-definition-clean` — closed cycle-022 — informational affirmation (forward-narrated; no deliverable).

*L2/L2>L1 deflation cohort:*
- `gram-l2-firm-landed-unblocks-deflate-firm-and-nleps-deflation-lowering` — resolved cycle-022 — `book/src/L2/gram.md` firm (all-pairs `inner_product` fold → `Matrix[k,k]`).
- `deflate-l2-partly-constructive-landed-promotion-gates-on-positive-galerkin-site` / `deflate-project-oblique-core-vs-nleps-schur-modification` / `deflate-vs-orthogonalize-over-unification-guard` — resolved cycle-022 — `book/src/L2/deflate.md` partly-constructive (Schur-form firm; bare-Galerkin core constructive). The shared Galerkin-core gate continues (deferred/contingent plan item).
- `orthogonalize-composition-lowering-l2-l1-theme-FIRM-LANDED` / `orthogonalize-composition-lowering-l2-l1-theme` — resolved cycle-022 — `book/src/L2-L1/orthogonalize-composition-lowering.md` firm (MGS/CGS/CGS2 variant-dispatch).
- `orthogonalize-composition-lowering-audited-fully-supported-firm-stays-cycle-023` — resolved cycle-023 — 17-citation `verified_against:` audit; stays firm.
- `gram-fold-specialization-l2-l1-theme-firm-landed-cycle-024` / `gram-l2-l1-lowering-theme-double-dot-loop-fusion` — resolved cycle-024 — `book/src/L2-L1/gram-fold-specialization.md` firm (matrix-valued sibling of inner-product-fold-specialization).
- `deflate-composition-lowering-l2-l1-theme-partly-constructive-landed-cycle-024` / `deflate-l2-l1-lowering-theme-needed` — resolved cycle-024 — `book/src/L2-L1/deflate-composition-lowering.md` partly-constructive; NLEPS-deflation L2>L1 pair COMPLETE.
- `nleps-deflation-lowering-chain-substantially-anchored-post-cycle-022` / `nleps-deflation-subspace-projection-combinator-deflate-gram` — resolved cycle-022/024 — the deflate/gram combinator landed (rough-in c021 → gram firm + deflate partly-constructive c022; L2>L1 pair c024).

*Citation-drift fixes (mechanical re-anchors; targets stay firm):*
- `l3-ksp-solve-citation-drift-463-563-correction` — resolved cycle-022 — `:464→:463`/`:564→:563` re-anchored in `book/src/L3/ksp_solve.md`.
- `inner-product-fold-specialization-operator-cpp-inline-anchor-drift` — resolved cycle-022 — `operator.cpp :623→:624`/`:632→:634`/`:615-616→:616` re-anchored; enacts the theme's embedded `audit_caveat`.
- `dot-mutation-rotation-subpattern-d-stale-orthog-hpp-34-anchor-should-be-35` / `dot-mutation-rotation-subpattern-d-citation-fix` / `dot-mutation-rotation-subpattern-d-orthog-anchor-fix-RESOLVED-cycle-024` — resolved cycle-024 — `orthog.hpp:34→:35` re-anchored in `book/src/L1-L0/dot-mutation-rotation.md` Sub-pattern D + `verified_against:` block; stays firm.
- `orthogonalize-mutation-rotation-l1-l0-theme-should-cite-dot-subpattern-d-DISCHARGED-ON-L2L1-SIDE` — resolved-on-L2>L1-side cycle-022 — the L2>L1 theme cites Sub-pattern D; the L1>L0-side residual folds into the still-queued `orthogonalize-mutation-rotation` theme (deferred).

*Layer-intro / index-prose (informational; layer-intro-author follow-ups migrated to plan):*
- `l1-semantic-motif-taxonomy-expanded-to-six-apply-linop-motif-7-watch` — resolved cycle-023 — L1 motif taxonomy 4→6 (operator-introspection + coordinate-space dense direct algebra); motif-7 a future-watch trigger.
- `l2-index-working-note-staleness-l3-ksp-solve-on-disk` / `L2-layer-intro-refresh-for-named-compositions` / `L2-layer-intro-refresh-for-fold-cohort` — resolved cycle-022 — L2 Part-intro refresh (dropped stale L3-not-on-disk clause; live-linked firm L3 `ksp_solve`).
- `lu-solve-layer-intro-count-refresh-and-fifth-motif` / `eigsolve-firm-stale-cycle-009-narrative-bullet-routes-to-layer-intro-author` / `l1-index-fifth-motif-operator-to-data-introspection` — migrated to plan (cycle-025 active-head #5, L1/L2/L3 index cohort-prose refresh).
- `eigsolve-l2-l1-spectral-transform-composition-lowering-theme-needed` / `concepts-eigsolve-page-still-absent` — migrated to plan (cycle-025 active-head #2/#3, `eigsolve-l2-l1-and-concept`).
- `nleps-jacobian-action-mutation-rotation-l1-l0-lowering-theme` / `nleps-eigenvalue-correction-mutation-rotation-l1-l0-lowering-theme` — migrated to plan (cycle-025 active-head #1, `nleps-interior-atom-l1-l0-themes`).

*Audit follow-ups (migrated to plan — cycle-025 active-head #4 / Medium-tier audits):*
- `apply-nonlinear-pencil-mutation-rotation-lowering-verifier-audit-followup` / `deflate-composition-lowering-mutation-rotation-lowering-verifier-audit-followup` / `gram-fold-specialization-l2-gram-forward-reference-closure-followup` / `orthogonalize-composition-lowering-three-way-delegation-boundary-audit` — migrated to plan (batch-6-firm-theme lowering-verifier audits).
- `lu-solve-mutation-rotation-lowering-verifier-audit-and-lu-solve-citation-tightening` / `eigsolve-l3-l2-body-identity-audit-anchor-optional-followup` — deferred/contingent (optional audits; trigger = a lowering-verifier dispatch on the named theme).

*Lower-priority kept (deferred/contingent — non-actionable, trigger-gated):*
- `gram-percell-dot-vs-fused-matmul-tree-loadbearing` — kept — per-cell-dot vs fused-matmul-tree is a load-bearing recording in the firm theme; informational.
- `gram-l2-coverage-caveat-single-gram-build-site` / `lu-solve-real-element-type-variant-permitted-but-unwitnessed` / `lu-solve-adjacent-future-leaves-prolongate-and-real-variant` — kept — single-site / unwitnessed-variant coverage caveats; trigger = a second site / consumer surfaces (the `prolongate`/`basis_expand` L1 leaf is a candidate when the deflation/ROM L2 cohort needs it).
- `eigsolve-l4-surface-solve-monad-unauthored-future-dispatch` — kept (deferred/contingent, in plan) — speculative L4 surface, below the clearly-implied-stub bar.

### Closed by the batch-7 meta-phase (cycles 025/026/027; 2026-05-29)

Landing-record + audit-discharge + citation-hygiene dispositions appended by the per-report integrators across batch-7 (append-only between meta-phases), now compacted here (full prose in git history; slugs preserved as cross-reference anchors). Batch-7 was a high-yield cohort-completion + citation-hygiene batch: 3 clean cycles, 1 deferral (D5 → plan c028), 0 rejections.

*Eigsolve chain completion (L2>L1 + concept page + audit; the cohort is now fully complete + audited cross batch-6/7):*
- `eigsolve-l2-l1-spectral-transform-composition-lowering-theme-needed` — resolved cycle-025 — `book/src/L2-L1/eigsolve-spectral-transform-composition.md` firm (the L2>L1 edge, last authoring gap); the migrated-to-plan `:323` clause discharged.
- `concepts-eigsolve-page-still-absent` — resolved cycle-025 — `book/src/concepts/eigsolve.md` created (cross-cutting nav home; introduces `EigSolver[problem]`); the migrated-to-plan `:323` clause + the `eigsolve-l2-l1-and-concept` plan item FULLY discharged (both halves landed c025).
- `eigsolve-l2-entry-lowers-from-pending-forward-reference-upgrade` — resolved cycle-026 — `L2/eigsolve.md:163` "pending" forward-ref upgraded to live link (D7 lifter).
- `concepts-eigsolve-chain-entries-live-link-upgrade-followup` — resolved cycle-026 — the three chain entries' stale "`concepts/eigsolve` does not yet exist" prose upgraded to live links (D7 lifter).
- `gram-md-forward-ref-text-refresh-to-name-gram-fold-specialization` — resolved cycle-026 — `gram.md` "(forthcoming)" prose upgraded to live `gram-fold-specialization` link (D7 lifter).
- `eigsolve-spectral-transform-composition-lowering-verifier-audit-followup` — resolved cycle-026 — fully-supported; additive `verified_against:` (15 entries) landed (D6c); stays firm. Completes the 3-theme c025-new audit cohort.

*NEP-interior L1>L0 cohort completion + audit (5/5 complete):*
- `nleps-jacobian-action-mutation-rotation-l1-l0-lowering-theme` — resolved cycle-025 — `book/src/L1-L0/nleps-jacobian-action-mutation-rotation.md` firm; the migrated-to-plan `:324` clause discharged.
- `nleps-eigenvalue-correction-mutation-rotation-l1-l0-lowering-theme` — resolved cycle-025 — `book/src/L1-L0/nleps-eigenvalue-correction-mutation-rotation.md` firm (closes the 5/5 NEP-interior L1>L0 cohort); `:324` clause discharged.
- `nleps-jacobian-action-mutation-rotation-lowering-verifier-audit-followup` — resolved cycle-026 — fully-supported; additive `verified_against:` (24 entries) landed (D6a); stays firm.
- `nleps-eigenvalue-correction-mutation-rotation-lowering-verifier-audit-followup` — resolved cycle-026 — fully-supported; additive `verified_against:` (19 entries) landed (D6b); stays firm.

*L1 vocabulary growth (normalize + back_solve; both cohorts complete):*
- `normalize-as-fused-l1-primitive` / `normalize-and-normalize-b-weighted-l1-candidates` — resolved cycle-026 — decided-YES; L1 `normalize` firm (`linalg::Normalize`; B-weighted as in-chapter rough-in note); closes `normalize-l1-primitive-harvest`.
- `ls-update-column-l1-leaf` — resolved cycle-027 — landed firm under the renamed slug `back_solve` (small-dense triangular back-solve `y=back_solve(R,s)`; renamed-in-repair to resolve the `ls_update_column` slug collision); answer `book/src/L1/back_solve.md`.
- `incremental-least-squares-as-future-L2-firstclass-entry` — resolved cycle-026 — `book/src/L2/incremental-least-squares.md` stub→firm; `l2-named-composition-lifts` cohort COMPLETE 2/2 (with `orthogonalize` c019).
- `gmres-givens-stream-as-step-kernel-borderline` — resolved cycle-026 (NEGATIVE) — the Givens running-QR stream is a DISTINCT named L2 composition, NOT a `krylov-step` axis (small-dense `O(j)`, field-dim-independent); recorded in `incremental-least-squares.md`.
- `l2-ksp-solve-materialise-iterate-incremental-least-squares-cite-tightening` — resolved cycle-027 — the gated `ksp_solve.md:123` cite-tightening ENACTED (D6 lifter; plain-text→live-link + queued→firm + K.y→V.y/Z.y correction-shape).

*L1>L0 firm themes + matrix-weighted-norm:*
- `matrix-weighted-norm-mutation-rotation-l1-l0-theme` — resolved cycle-026 — `book/src/L1-L0/matrix-weighted-norm-mutation-rotation.md` stub→firm (energy norm `√(xᴴBx)`→`linalg::Norml2`); the `:26`-line constituent clause discharged. (The L1 OPERATOR entry stays `rough-in (test-coverage-bounded)` — its own gate, migrated to plan c028 #4.)
- `matrix-weighted-norm-mutation-rotation-lowering-verifier-audit-followup` — resolved cycle-027 — additive `verified_against:` (19 entries) landed; stays firm.
- `normalize-mutation-rotation` — resolved cycle-027 — `book/src/L1-L0/normalize-mutation-rotation.md` firm (the c026-forward-referenced theme); closes `normalize-l1-primitive-harvest` end-to-end.

*L0 naming-residue / index-prose sweeps:*
- `matrix-weighted-norm-naming-sweep` / `dependency-map-orthog-plane-rotation-stale-edge-prune` / `negative-result-slice-examples-reciprocal-membership` — resolved cycle-026 — L0/dependency-map/negative-result-slice repoints landed (D5 layer-intro-author naming-residue sweep).
- `bilinear-form-workspace-category-4-mislabel` — resolved cycle-027 (for the named/dispatched sites) — `:33`/`:73`/`:80` + `matrix-weighted-norm.md:9` relabelled Category-2→Category-1 (D2 lifter); residual `:22`/`:87` split to a NEW OQ (migrated to plan c028 #2).
- `givens-concept-page-source-cite-staleness-gmres-md-should-be-iterative-cpp` — resolved cycle-027 — `concepts/givens.md:29` re-anchored `gmres.md`→`iterative.cpp:634-640` (D2 lifter Correction 3).
- `lu-solve-layer-intro-count-refresh-and-fifth-motif` / `eigsolve-firm-stale-cycle-009-narrative-bullet-routes-to-layer-intro-author` / `l1-index-fifth-motif-operator-to-data-introspection` — resolved cycle-025 — the `:322`-line cohort fully discharged (D9 index refresh: motif-5/6 already-on-disk; cycle-009 bullet historical-marked; `Firm (19)` cohort split present).

*Citation-drift re-anchors (mechanical; targets stay firm):*
- `nleps-eigenvalue-correction-l1-entry-two-anchor-reanchor` — resolved cycle-026 — `nleps_eigenvalue_correction.md` `:596→:590` (−6) + `:709→:712` (+3) re-anchored (D1 lifter).
- `nleps-jacobian-action-l1-entry-six-anchor-reanchor` — resolved cycle-026 — six deflation-block `+1` codemap-drift anchors re-anchored (D1 lifter); the entry + drift-free theme now agree on-disk.
- `vector-cpp-667-mfem-assert-citation-drift-to-668-sibling-sweep` — resolved cycle-026 — `:667→:668` swept across `inner_product.md:360` + `inner-product-fold-specialization.md` (4 sites; D1 lifter); BLAS-1 inner-product/gram cohort all `:668`.

*Batch-6-firm-theme lowering-verifier audits (the `:327` line, fully discharged this batch):*
- `apply-nonlinear-pencil-mutation-rotation-lowering-verifier-audit-followup` — resolved cycle-025 — fully-supported; 21-entry `verified_against:` (D5); stays firm. The `:810` no-shift verdict holds.
- `gram-fold-specialization-l2-gram-forward-reference-closure-followup` — resolved cycle-025 — forward-reference closure CONFIRMED; 13-entry `verified_against:` + `:667→:668`/`:613→:614` anchor-precision (D7); stays firm.
- `orthogonalize-composition-lowering-three-way-delegation-boundary-audit` — resolved cycle-025 — cleanly-partitioned (3 themes cite shared `orthog.hpp` ranges for disjoint facets, no contradiction); 100-citation re-scan clean; verdict-only, no mutation (D8).
- `deflate-composition-lowering-mutation-rotation-lowering-verifier-audit-followup` — RE-SCOPED cycle-025 — audit half discharged (40 ok, 19-entry `verified_against:`); the shared bare-Galerkin-core promotion gate STAYS OPEN → re-scoped to the deferred/contingent `deflate-galerkin-core-promotion` plan item (trigger = a positive bare-Gram-solve site). Stays partly-constructive.

*Cosmetic / opportunistic trims (kept deferred — do NOT dispatch standalone; co-schedule with any touch on the named file):*
- `orthogonalize-composition-lowering-stale-good-direction-34-parenthetical-trim` — kept — drop the now-obsolete "(should be `:35`)" note next time a lifter touches the theme.


### Closed by the batch-8 meta-phase (cycles 028/029/030; 2026-05-30)

- `incremental-least-squares-composition-lowering-theme-deferred-needs-back-solve-reanchor` — RESOLVED c028 D1 — `book/src/L2-L1/incremental-least-squares-composition-lowering.md` landed firm; terminal-back-solve re-pointed at firm `back_solve` leaf, `trsv`↔`back_solve` reconciled (general `trsv` plain-text forward note).
- `linalg-operator-file-category-mislabel-residual-lines-22-87` — RESOLVED c028 D2 — `book/src/L0/linalg-operator-file.md:22,:87` Category-2→Category-1 relabel; file fully internally consistent (all 5 sites now read "Category 1").
- `l2-incremental-least-squares-self-description-still-says-queued-after-firming` — RESOLVED c028 D2 — `book/src/L2/incremental-least-squares.md:13` stale "queued" qualifier dropped (entry has been `status:firm` since c026).
- `normalize-mutation-rotation-lowering-verifier-audit` — RESOLVED c028 D3 — fully-supported on the firm unweighted core; 16-row `verified_against:` block appended to `book/src/L1-L0/normalize-mutation-rotation.md`; F1 row (does-not-support on the rough-in `normalize_B` note) routed to a follow-up that landed c029.
- `back-solve-lowering-verifier-audit` — RESOLVED c028 D4 — fully-supported; 18-row `verified_against:` block appended to `book/src/L1/back_solve.md`; theme stays firm (firm-on-positive-structure).
- `incremental-least-squares-composition-lowering-verifier-audit` — RESOLVED c028 D5 — fully-supported; 22-row `verified_against:` block appended to `book/src/L2-L1/incremental-least-squares-composition-lowering.md`; theme stays firm; flagged the leading-`"` channel-format hazard (codified c030 batch-8 meta as `verified-against-note-no-leading-quote-of-either-kind`).
- `matrix-weighted-norm-mixed-element-type-variant` — NARROWED c028 D6 (STAYS OPEN; constituent of plan Backlog `matrix-weighted-norm + bilinear-form firm-promotion`) — element-type axis now shape-witnessed by `test/unit/test-orthog.cpp`; residual is the named-entry-point √+SPD-guard test (`linalg::Norml2(comm,x,B,Bx)` at the exact entry point); stay `rough-in (test-coverage-bounded)`.
- `matrix-weighted-norm-and-bilinear-form-stay-rough-in-with-sharpened-per-operator-gates-c028` — RESOLVED-as-adjudication c028 D6 (STAYS OPEN as plan Backlog item with sharpened gates) — both L1 operators STAY rough-in; matrix-weighted-norm gate narrowed to named-entry-point √+SPD-guard test; bilinear-form gate sharpened to (i) author L1>L0 theme AND (ii) test of `linalg::Dot(comm,x,A,y)` covering Hermitian + non-symmetric cases.
- `bilinear-form-mutation-rotation-l1-l0-theme-needed-c028` — RESOLVED c029 D2 — `book/src/L1-L0/bilinear-form-mutation-rotation.md` landed firm; the L1 `bilinear-form` operator stays `rough-in (test-coverage-bounded)` per its own independent gate.
- `l3-vocabulary-inventory-gap-trsv-leaf` — RESOLVED-BY-OBSTRUCTION c028 D7 (`harvester-trsv-l1-localization`; closes the LAST leaf of plan-Backlog `l3-vocabulary-inventory-gap`; parent plan item fully resolved). Palace has NO standalone `trsv` primitive; triangular solves are HYPRE-internal / external direct-solver / 2×2 block — the FIRST opaque-library-ownership obstruction in the cohort. Skill `establish-negative-finding-exhaustiveness` promoted c030 from this dispatch's pattern.
- `triangular-solve-obstruction-l1-l0-theme-needed-c028` — RESOLVED c029 D3 — `book/src/L1-L0/triangular-solve-obstruction.md` landed firm; the FIRST opaque-library-ownership L1>L0 obstruction (HYPRE relax-type sites + external direct-solver wrappers as negative anchors). Codified the obstruction sub-kind distinction c030 batch-8 meta (CLAUDE.md invariant + abstractor role-spec).
- `back-solve-law-6-leaf-prose-tightening-c029` — closed-stale c030 batch-8 meta — superseded by the c030 D1 audit's Finding A ("+1-line brace-placement shift" is byte-identical) which routes the broader narrative repair to c031 (plan-active-#2); this fold-the-prose-tightening note is subsumed by the larger narrative correction.
- `back-solve-mutation-rotation-cycle-030-verified-against-audit-c029` — RESOLVED c030 D1 — 22-row additive `verified_against:` block landed (1 `partially-supports` on the FGMRES Sub-pattern B narrative defect, routed to c031; 21 `supports`); theme stays firm.
- `bilinear-form-mutation-rotation-cycle-030-verified-against-audit-c029` — RESOLVED c030 D2 — 19-row additive `verified_against:` block landed, fully-supported; theme stays firm; YAML leading-quote-of-either-kind hazard caught + codified.
- `triangular-solve-obstruction-l1-l0-theme-LANDED-c029` — informational/closure c029 D3 — records the landing; dispositions migrated to the c028 OQs above.
- `ls-update-column-l2-l1-theme-plain-text-ref-upgrade-to-live-link-c029` — RESOLVED-PARTIAL c030 D6 — 3 of 6 mechanical plain-text refs upgraded to live links; the 3 sibling `ls_update_column-mutation-rotation` mentions with adjacent stale "forthcoming" framing routed to c031 (plan-active-#3 bounded prose rework).
- `ls-update-column-mutation-rotation-l1l0-theme-forthcoming-c029` — RESOLVED c030 D1 — `book/src/L1-L0/ls-update-column-mutation-rotation.md` landed firm; GMRES restart-cycle L1>L0 cohort COMPLETE (producer + consumer fully lowered).
- `normalize_B-note-says-no-fused-B-Normalize-but-uncalled-fused-operator-exists` — RESOLVED c029 D3 — `book/src/L1-L0/normalize-mutation-rotation.md` + `book/src/L1/normalize.md` rewritten "no fused" → "exists but uncalled"; `normalize_B` promotion gate tightened (mere existence is insufficient — positive callsite required); gate STAYS open.
- `normalize-mutation-rotation-verified-against-row-466-469-stale-after-c029-prose-correction` — RESOLVED c030 D5 — F1 row at on-disk location `:481-484` (the slug's `:466-469` was a stale line-ref) verdict `does-not-support` → `supports`; completes the c028→c029→c030 metadata-refresh chain.
- `ls-update-column-mutation-rotation-l1l0-theme-forthcoming-c029-RESOLVED-c030` — informational closure marker — duplicate of the c029 forward-ref close above.
- `back-solve-mutation-rotation-cycle-030-verified-against-audit-c029-RESOLVED-c030` — informational closure marker — duplicate of the c030 D1 close above.
- `bilinear-form-mutation-rotation-cycle-030-verified-against-audit-c029-RESOLVED-c030` — informational closure marker — duplicate of the c030 D2 close above.
- `ls_update_column-cycle-030-verified-against-audit` — RESOLVED c030 D4 — 25-row independent-verifier `verified_against:` block landed on `book/src/L1/ls-update-column.md`; dual-block convention (c029 21-row producer self-verify + c030 25-row independent) operating as designed; leaf stays firm.
- `normalize-mutation-rotation-verified-against-row-466-469-stale-after-c029-prose-correction-c030-RESOLVED` — informational closure marker — duplicate of the c030 D5 close above.
- `ls-update-column-l2-l1-theme-plain-text-ref-upgrade-to-live-link-c029-RESOLVED-c030` — informational closure marker — duplicate of the c029 partial-resolution close above.

#### Carry-open deferred minor follow-ups (c029 abstractor low-priority notes)

- `back-solve-mutation-rotation-reduction-order-section-promotion-stylistic-c029` — DEFERRED (stylistic; trigger: a future lifter chapter-touch).
- `back-solve-mutation-rotation-l2-l1-incremental-least-squares-boundary-c029` — DEFERRED (low-priority cohort-boundary note from the c029 abstractor; trigger: a future L2>L1 cite-tightening pass).
- `bilinear-form-l0-surface-comment-callout-polish-c029` — DEFERRED (stylistic L0-surface comment callout; trigger: a future lifter on bilinear-form-MR).
- `back-solve-mutation-rotation-empty-cycle-j-minus-one-reachability-c029` — DEFERRED (low-priority empty-cycle reachability note; trigger: a future lowering-verifier exhaustive audit).
- `bilinear-form-l1-entry-upstream-variant-axis-coverage-gaps-c029-tracking` — DEFERRED (tracking-only; constituent of plan Backlog `matrix-weighted-norm + bilinear-form firm-promotion`).
- `bilinear-form-l2-weighted-inner-product-reduction-combinator-c029` — DEFERRED (combinator-mining candidate; trigger: a future combinator-miner dispatch with the inner-product family in scope).

_Three c031-routed items (`back-solve-mutation-rotation-sub-pattern-b-brace-placement-narrative-correction-c030`, `ls-update-column-mutation-rotation-l2l1-incremental-least-squares-composition-lowering-face-1-plain-text-to-live-link-c030`, `sparse-triangular-solve-slice-reduction-after-l1l0-theme-lands`) are NOT in the deferred-minor list above — they are plan-active picks for cycle-031 (see `scaffolding/priorities.md` §Now — picks #2, #3, #5)._
### Resolved by the 2026-05-28 unification pass (artifact landed earlier; ledger never pruned)

- `axpy-l1-l0-three-subpatterns` — resolved — axpby-mutation-rotation Sub-patterns A/B/C + linear-combination-fold-specialization; axpy subsumed.
- `axpby-axpy-scal-decomposition-decision` — answered cycle-003 — fused-primitive chosen (decisions/axpby-as-primitive.md).
- `scalar-promotion-typing-rule` — resolved cycle-005 — concepts/scalar-promotion.md formalizes the real⊑complex typing rule.
- `nrm2-B-weighted-energy-norm-harvest` — resolved cycle-010 — matrix-weighted-norm.md lands ‖x‖_B (firm-promotion gate → plan).
- `axpby-lowering-verifier-audit` — resolved cycle-003/014 — axpby-mutation-rotation §Verified-against audited.
- `axpbypcz-internal-sub-pattern-A` — resolved cycle-005 — axpbypcz-mutation-rotation Sub-pattern A documents the AXPBY+z.Add internal lowering.
- `axpbypcz-gamma-recognition-is-syntactic-not-semantic` — stale — informational downstream-lowering note; no deliverable.
- `fused-update-chained-collapse-combinator-mining` — resolved cycle-018 — L2/linear_combination laws capture the fused-update fold.
- `subsumption-chain-cross-cutting-concept` — resolved cycle-018 — L2/linear_combination:154-156 formalizes scal≺axpy≺axpby≺axpbypcz.
- `apply-linop-lowering-theme-scope` — resolved cycle-005 — apply-linop-mutation-rotation firm (sub-patterns A–E).
- `addmult-as-more-primitive-form-in-some-subclasses` — resolved — theme records SumOperator::Mult→AddMult + BaseProductOperator inversion.
- `apply-linop-sum-operator-mult-via-addmult-reuse` — resolved — theme records the Mult-via-AddMult reuse note (:163-165).
- `krylov-step-layer-placement` — resolved — firm at L2/L3/L4 with lowering edges.
- `krylov-step-naming-and-borderline-cases` — resolved — firm chapter kept the name with role-vs-family rationale.
- `krylov-step-harvester-deliverables` — resolved cycle-006 — krylov-step harvested firm at L2.
- `l2-dep-map-format-vs-l1` — resolved — L2 index uses the 4-column table matching L1.
- `L2-named-compositions-have-no-single-L0-citation` — stale — recognized L2-named-composition norm; informational.
- `krylov-step-naming-stretches-to-chebyshev` — resolved cycle-012 — chebyshev has its own L2 entry (chebyshev-iteration).
- `krylov-step-theme-body-no-l3-row-drift-cycle-013` — resolved cycle-017 — dissolution theme lines 20/218/220 SUPERSEDED + re-anchored to firm L3/krylov-step.
- `krylov-step-speculative-l1-promotion-decision` — answered cycle-005 — NO promotion (decisions/2026-05-27-krylov-step-speculative-l1-promotion.md).
- `concepts-page-authorship-role-scope` — resolved cycle-003 — layer-intro-author broadened to concepts/.
- `concepts-pre-layered-era-sweep` — resolved cycle-012 — concept-corrections sweep landed.
- `concepts-page-word-count-discipline` — stale — never enforced; concept pages carry inline structure.
- `concepts-sweep-cycle-005-candidate` — duplicate → `concepts-pre-layered-era-sweep`.
- `layer-intro-refresh-thresholds-l2-l3-l4` — stale — resolved by practice; all layer indices populated.
- `vocabulary-cohort-subsection-as-layer-intro-pattern` — stale — pattern adopted in layer intros.
- `lowering-verifier-yaml-in-prose-channel-format` — resolved cycle-003 — fenced ```yaml codified in lowering-verifier.md.
- `subagent-skips-edit-on-explicit-instruction` — resolved cycle-018 — dispatch-phase write-guard across all 8 specialized specs.
- `cycle-planner-grep-before-harvester` — resolved cycle-012 — MCP-first localization codified.
- `minres-mfem-as-l0-substrate-policy` — resolved 2026-05-28 (user) — obstruction stands; Palace aborts on the MINRES/BiCGStab enum (ksp.cpp:53-57), so document "not implemented in Palace", do NOT re-anchor to vendored `mfem::MINRESSolver` (cite Palace; match-not-extend).
- `bicgstab-mfem-reanchor-policy` — duplicate → `minres-mfem-as-l0-substrate-policy` (resolved 2026-05-28).
- `bicgstab-enum-intent` — stale — unanswerable without upstream; obstruction theme stands.
- `advertised-but-unimplemented-krylov-solvers-friction` — resolved — friction-ledger `advertised-but-unimplemented-krylov-solvers`.
- `shared-infra-priorities-rescope-after-obstruction` — resolved — roadmap/plan mark MINRES/BiCGStab `[stub]`.
- `minres-bicgstab-signature-sketches-not-contracts` — stale — obstruction themes stand; speculative sigs are scaffolding.
- `mfemwrappersolver-l0-coverage-candidate` — resolved cycle-007 — L0/mfem-wrapper-solver.md authored.
- `l1-ksp-solve-firm-up-anchor-ready` — resolved cycle-007 — L1/ksp_solve.md firm.
- `mutable-workspace-category-4-split-decision` — stale — split never warranted; Category 4 stays cohorted.
- `plane-rotation-concept-page-canonical-pointer-repoint` — resolved cycle-013 — 3 concept cross-refs repointed to plane_rotation_stream.
- `l1-divfree-projector-promotion` — resolved cycle-015 — L1/divfree-projector.md firm.
- `mixed-justification-sub-rule-methodology` — stale — framing ratified in-place in axpbypcz-mutation-rotation.
- `eigensolver-wrapper-l0-bundle-4-candidate` — answered cycle-008 — L0/eigensolver-wrapper.md authored.
- `l3-backfill-apply-linop-and-blas1-cohort` — resolved cycle-011 — all 8 L3 entries exist; BLAS-1 cohort closed.
- `l3-l1-directory-naming-structure-policy` — resolved cycle-012 — in-line non-adjacent-identity convention codified (no L3-L1/ dir).
- `priority-13-now-landed-as-matrix-weighted-norm` — resolved cycle-010 — routing OQ; matrix-weighted-norm landed.
- `slepc-convergence-reason-lift-sub-theme` — resolved — eigsolve-convergence-reason-mapping.md is the reason→EigStatus table.
- `eigsolve-mutation-rotation-lowering-verifier-followup` — resolved cycle-012 — audited (confirms-with-refinement).
- `orthogonalize-mutation-rotation-l1-l0-theme` — resolved cycle-013 — orthogonalize-mutation-rotation.md firm.
- `chebyshev-slice-rho_0-coefficient-correction` — stale — chebyshev.md slice removed cycle-015; firm entries correct.
- `l3-l4-chebyshev-rows-eligible` — resolved cycle-013/015 — L3/chebyshev (partial-obstruction) + L4/chebyshev (firm) landed.
- `chebyshev-l1-l0-and-l2-l1-lowering-themes` — resolved cycle-013 — chebyshev-smoother-mutation-rotation + chebyshev-iteration-fusion firm.
- `eigsolve-getconverged-forwarder-fix-and-gated-promotion` — resolved cycle-013 — forwarder + promotion landed; theme firm (structural).
- `partly-constructive-to-firm-promotion-route-ratification` — resolved cycle-015 — ratified by meta-phase (validated-by-use).
- `divfree-projector-l1-l0-lowering-verifier-followup` — resolved cycle-016 — divfree-projector-mutation-rotation firm; WeakDiv sign positively anchored.
- `divfree-projector-status-adjudication` — resolved cycle-015 — promoted partly-constructive→firm.
- `chebyshev-l4-wrapper-iteration-vocabulary-reconcile` — resolved cycle-015 — L4/chebyshev firm via iterate_while_pure folds.
- `chebyshev-phase1-slice-reduction` — resolved cycle-015 — chebyshev.md slice removed.
- `chebyshev-lowering-themes-lowering-verifier-followup` — resolved cycle-014 — both themes carry verified_against audit blocks.
- `chebyshev-dead-code-complex-transpose-kernels` — resolved cycle-014 — recorded as recognition rules in the theme.
- `jacobi-mutation-rotation-dead-code-complex-transpose-kernel-lowering-verifier-audit` — **resolved cycle-034 D2** (lowering-verifier audit) — jacobi `:61-69` confirmed template-dead (`Apply<true>` zero call sites; `MultTranspose` self-aliases to `Mult`); chebyshev `:101-110` + `:150-159` confirmed template-dead (`ApplyOrder0<true>` / `ApplyOrderK<true>` zero hits; both `MultTranspose2` self-alias to `Mult2`); axpby `ComplexVector::Subtract` is **not** a transpose kernel — it is a defined-not-used member-API recognition rule (planner-scope conflated two distinct dead-code shapes), the axpby theme's existing recognition-rule framing is correct as-stated. Three caveats all present and accurate; one minor citation-hygiene observation on chebyshev `:150-159` (overshoots by 4 lines on end) filed as new low-priority OQ below. Audit report: `reports/2026-05-30T220500Z-lowering-verifier-dead-code-complex-transpose-kernel-audit/CYCLE.md`.

_(cycle-034 D1/D2 intake sections compacted by the batch-12 meta-phase: `chebyshev-smoother-mutation-rotation-applyorderk-true-citation-tighten` resolved c035 D1; `reciprocal-l1-l0-mutation-rotation-theme` + `elementwise-product-l1-l0-mutation-rotation-theme` resolved c034 D1; `reciprocal-elementwise-product-mr-dead-code-transpose-consumer-branch` is a durable provenance pointer, closed c037 D3 — all in the Closed index. The two genuinely-deferred items `safe-reciprocal-threshold-l1-candidacy` + `mfem-vector-reciprocal-upstream-body-investigation` folded into the deferred-contingent §BLAS-1/linear-update family above as a cohort line.)_

---

## New intake (post-2026-05-28 unification; append-only until next meta-phase)

### cycle-020 / cycle-021 New-intake residual — COMPACTED to the Closed index (batch-13 meta-phase)

The 39 cycle-020/021 New-intake entries that lived here (appended by the batch-5/6 integrators, never drained by the batch-5/6 unification passes — they sat as an orphaned residual block) are **compacted by the batch-13 meta-phase**. Verified at compaction: every entry is either (a) **resolved-superseded** — the whole eigsolve chain L1/L2/L3 + the L3>L2 `eigsolve-opaque-eigen-iteration` theme are now firm (`l3-eigsolve-blocked-on-l1-firm-and-l2-entry`, `l3-eigsolve-linear-evp-has-no-krylov-step-kernel-analog` already in the Closed index at the batch-5/6 subsections), the BLAS-1 / inner-product / nleps-deflation cohort all landed, the gmres/fgmres L4>L3 themes both firm; or (b) **trigger-gated and already mirrored** in the top **Open — deferred / contingent** sections (the eigsolve/SLEPc, apply_linop, Krylov/iterate-while/NLEPS, orthogonalize/concepts families). Full prose is in git history (the "git history is the historical record" methodology). The slug strings remain valid cross-reference anchors via the Closed-index subsections above + the deferred-contingent families above. No live information lost: the genuinely-still-open deferreds are the top-of-file deferred-contingent one-liners; the resolved ones are in the Closed index.


**Batch-6 New-intake compacted (cycle-024 meta-phase unification).** The cycle-022/023/024 landing-record + carry-forward New-intake entries (72 slugs) were verbose append-only prose; the meta-phase compacted them per the unification protocol — every slug now lives as a one-line disposition in **§Closed (index) → "Closed by the batch-6 meta-phase"** (resolved/landed) or was migrated to the plan (`scaffolding/priorities.md` cycle-025 active-head + Backlog) or kept as a deferred-contingent one-liner there. Full prose preserved in git history (the "git history is the historical record" methodology). The slug strings are retained as cross-reference anchors in the Closed index. The cycle-020/021 entries above this marker remain (most already migrated/closed by the batch-5 pass; the residual deferred ones are trigger-gated). Next batch's New-intake appends below this line (append-only until the batch-7 meta-phase).

### Batch-7 per-report dispositions — unified into the Closed index + the plan (cycle-027 meta-phase)

The detailed clause-scoped dispositions the per-report integrators appended across cycles 025/026/027 (append-only between meta-phases) are **unified here by the cycle-027 meta-phase**. The fully-resolved ones are compacted into the **Closed (index) → "Closed by the batch-7 meta-phase"** subsection above (full prose in git history; slugs preserved there as cross-reference anchors). The codemap-drift methodology signal is addressed; the genuinely-open items are migrated to the plan or kept deferred below.

**Addressed this meta-phase (was Open methodology signal):**
- `codemap-read-range-plus-one-drift-on-brace-boundary` — **ADDRESSED cycle-027 meta-phase.** The `palace-codemap` `read_range` +1 drift on comment+`{`-brace boundaries (detected c025, worked-corrected c026/c027, confined to the `nleps.cpp` deflation block + `operator.cpp:601`) is now a friction-ledger entry (recurrence-4, `addressed`) with the enacted fix: a "codemap is localization-only; `citecheck`/on-disk is the citation source of truth" sub-bullet in all 5 citing producer/auditor specs. HARD per-report `--anchor` gate stays no-go (needs CYCLE.md anchor-token channel-format change, ask-class); escalate on recurrence-5 or a new-boundary occurrence. (Full prose: friction-ledger `codemap-read-range-plus-one-drift-on-brace-boundary`.)

**Open — migrated to the plan (c028 active head; pointer-only, the plan owns them):**
- `incremental-least-squares-composition-lowering-theme-deferred-needs-back-solve-reanchor` — OPEN → **plan c028 active #1.** The cycle-027 D5 deferral (`needs-revision`): the L2>L1 `incremental-least-squares-composition-lowering` theme must re-anchor its terminal-back-solve fan-down to the now-firm `back_solve` leaf (NOT `ls_update_column`, which is reserved for the distinct column-streaming step), reconcile `trsv`↔`back_solve`, optionally harvest the `ls_update_column` column-streaming leaf, then promote rough-in→firm. Clean lifter-promotion task. Co-keyed with `l2-named-composition-lifts`.
- `linalg-operator-file-category-mislabel-residual-lines-22-87` — OPEN → **plan c028 active #2.** Two further same-file sites (`:22`, `:87`) carry the Category-2→Category-1 workspace mislabel that the c027 D2 pass fixed at `:33`/`:73`/`:80` + `matrix-weighted-norm.md:9`; relabel them (Evidence `mutable-workspace-pattern.md:128-129` assigns both to Category 1) so the file is fully internally consistent. Mechanical 2-site label correction.
- `l2-incremental-least-squares-self-description-still-says-queued-after-firming` — OPEN → **plan c028 active #2.** The firm L2 producer entry `incremental-least-squares.md:13` still self-describes as "queued" despite `status: firm`; drop the stale qualifier (the consumer side `ksp_solve.md:123` was fixed c027 D6). Bounded prose-correction per `lifter-scope-content-correction-boundary`.
- `matrix-weighted-norm-mixed-element-type-variant` — OPEN → **plan c028 active #4** (the L1 `matrix-weighted-norm` rough-in→firm test-coverage/variant-axis gate; paired with the forthcoming `bilinear-form-mutation-rotation` theme — shared real-`B`-on-complex-`x` plumbing, decide the element-type-variant policy across the pair). Co-keyed with the plan Backlog `matrix-weighted-norm + bilinear-form firm-promotion`.

**Open — deferred / contingent (kept; promote when the trigger fires):**
- `constructed-solver-opaque-type-generic-concept-candidate` — deferred (cross-cutter watch). `EigSolver[problem]` is the SECOND consumer of the construction-bound-solver-as-operator pattern (after `ksp_solve`'s `KSP`); a dedicated generic `constructed-solver-opaque-type` concept is NOT warranted at two consumers (`solver-as-operator` covers it). *Trigger:* a THIRD consumer (e.g. a transient `TimeIntegrator[problem]` or driven-solver opaque value).
- `no-l4-eigsolve-entry-yet` / `eigsolve-l4-surface-solve-monad-unauthored-future-dispatch` — deferred (= the `eigsolve-l4-cap` future dispatch; already a deferred/contingent plan item). The eigsolve chain is authoring-complete L1→L2→L3 + concept page; the L4 cap (`book/src/L4/eigsolve.md`) is unauthored, below the clearly-implied-stub bar. *Trigger:* a second L4 consumer or a firm L4-eigsolve anchor; gated on scheduling vs lower-vocabulary priority.
- `orthogonalize-composition-lowering-stale-good-direction-34-parenthetical-trim` — deferred (cosmetic; do NOT dispatch standalone). Drop the now-obsolete "(should be `:35`)" note in the theme's `verified_against:` boundary-2 entry next time a lifter touches `orthogonalize-composition-lowering.md` for any reason.
- `eigsolve-l2-lowers-from-live-link` / `gram-md-forward-ref-live-link` / `eigsolve-chain-entries-concepts-live-link` — RESOLVED cycle-026 (the c025-flagged cross-ref live-link upgrades all LANDED via the c026 D7 lifter; indexed in the batch-7 Closed-index subsection above). No residual.

### Batch-9 per-report dispositions — unified into the Closed index + the plan (cycle-033 meta-phase)

The cycle-031/032/033 per-report integrator OQ appends (append-only between meta-phases) are **unified here by the cycle-033 meta-phase**. RESOLVED dispositions are compacted into the **Closed (index) → "Closed by the batch-9 meta-phase"** subsection below (full prose in git history; slugs preserved as cross-reference anchors). Methodology signals are codified in the friction-ledger + CLAUDE.md + skills (see Enacted changes section of this batch's meta-phase report). The genuinely-open items are migrated to the plan (`scaffolding/priorities.md` cycle-034 active head + Backlog) or kept as deferred-contingent one-liners below.

**Addressed this meta-phase (was Open methodology signal):**
- `negative-result-slice-canonical-instance-blocks-reduction` — **ADDRESSED cycle-033 meta-phase.** Codified as friction-ledger entry (4-cycle pattern: cycles 013/022/028/031); CLAUDE.md §Methodology invariants "Phase 1 corpus reduces as material is lifted" amended with the canonical-instance carve-out (≥2 concept-page references → retained-by-design); `skills/phase-1-slice-reduction-audit/SKILL.md` step 7 + Discipline bullet added. (Full prose: friction-ledger `negative-result-slice-canonical-instance-blocks-reduction`.)
- `cycle-planner-pre-dispatch-existence-check-of-target-artifact` — **ADDRESSED cycle-033 meta-phase.** Promoted to the standalone friction-ledger entry `cycle-planner-stale-priorities-line-recruitment` (recurrence-3 across batch-9: c031 file-existence-staleness / c032 deliverable-presence-staleness / c033 working precedent); skill `verify-dispatch-scope-not-already-discharged` promoted as the canonical four-step procedure; `.claude/agents/cycle-planner.md` §Discipline ENFORCEMENT bullet wiring the deeper check as MANDATORY pre-dispatch. (Full prose: friction-ledger + skill + role-spec.)

**Open — migrated to the plan (c034 active head; pointer-only, the plan owns them):**
- `reciprocal-l1-l0-mutation-rotation-theme` + `elementwise-product-l1-l0-mutation-rotation-theme` — OPEN → **plan c034 active #1 (composite candidate).** Possibly composite single theme — the two L1 leaves share the in-place-receiver-overwrite L0 mutation shape and differ only in the scalar kernel (precedent: `ksp-solve-mutation-rotation` thin-theme co-authoring). Fan-out: medium-high (both leaves are consumed across the diagonal-preconditioner cohort: assemble-diagonal + jacobi-smoother + chebyshev-smoother). Routes `abstractor`.
- `jacobi-mutation-rotation-dead-code-complex-transpose-kernel-lowering-verifier-audit` — OPEN → **plan c034 active #2.** Thin verdict-only sweep; same family as chebyshev sibling dead-code kernels + axpby sibling. Could be unified into a single lowering-verifier audit across the smoother-cohort dead-code complex-transpose kernels (jacobi `:61-69` + chebyshev `:101-110, :150-159` + axpby `ComplexVector::Subtract` forms). Fan-out: low (audit confirmation; no chapter-body changes expected). Routes `lowering-verifier`.

**Open — deferred / contingent (kept; promote when the trigger fires):**
- `polynomial-smoother-l2-combinator-from-jacobi-and-chebyshev` — deferred (combinator-miner candidate; awaits a third firm sibling). Jacobi (c032 firm) + Chebyshev (c008 firm) are the two-sibling speculation; a third L1 sibling (e.g. a literature-anchored Richardson-iteration smoother) is needed to firm the L2 polynomial_smoother combinator past two-case coincidence. *Trigger:* a third firm smoother L1 lands.
- `jacobi-fixed-damping-mode-consumer-coverage` — deferred (low-priority variant-axis audit). `jacobi_smoother` fixed-damping mode (`ω ∉ {0.0, 1.0}`) is source-witnessed but has zero Palace consumer sites; possible dead-code variant. *Trigger:* a same-layer-cross-cutter or cross-layer-cross-cutter pass on the jacobi entry.
- `reciprocal-l1-mfem-upstream-behaviour-pinning` — deferred (out-of-focus per CLAUDE.md upstream-citation policy). The real-`mfem::Vector::Reciprocal` resolves into upstream MFEM; treated as given. *Trigger:* a future consumer surfaces a behaviour-sensitive claim depending on the precise MFEM implementation (NaN/Inf propagation, device-kernel specifics).
- `elementwise-product-apply-linop-diagonal-operator-round-trip-law-9-cross-reference` — deferred (informational; cross-operator identity). The law `apply_linop(DiagonalOperator(d), x) = elementwise_product(d, x)` should surface in a future `assemble-diagonal` editing pass. *Trigger:* an `assemble-diagonal` lifter/same-layer-cross-cutter touch (low-fan-out hygiene).
- `incremental-ls-l2-entry-historical-forthcoming-quote-sync-after-l2-currency-pass` — deferred (cosmetic; do NOT dispatch standalone). If a future L2-side prose-currency pass eventually rewrites the L2 `incremental-least-squares.md:278-285` non-law text to drop "forthcoming" framing, then four historical-quote sites at `incremental-least-squares-composition-lowering.md:15/:145/:204/:541` would want a synchronised currency pass. *Trigger:* L2-side currency change firing.
- `normalize_B-note-says-no-fused-B-Normalize-but-uncalled-fused-operator-exists` — deferred (precision-fix on non-firm `normalize_B` rough-in note; does NOT gate the firm core). The theme's `normalize_B` note claims Palace has no fused B-Normalize free function; in fact it exists at `palace/linalg/operator.hpp:377-384` but is *uncalled* (zero callsites). Strengthens (does NOT change) the `normalize_B`-stays-rough-in verdict. *Trigger:* a follow-up abstractor/lifter touch on `book/src/L1-L0/normalize-mutation-rotation.md` + `book/src/L1/normalize.md` correcting "does not exist" → "exists but uncalled."
- `variant-axis-vs-distinct-primitive-decision` (potential skill) — deferred (methodology-shape candidate). The c033 `elementwise_product` decision-record establishes that conjugation may sit as a sub-axis on a complex element-type rather than splitting into two primitives, when the law overlap is large (8 of 10 laws shared). Convention differs from `dot`/`tdot` where the laws genuinely diverge. *Trigger:* a ≥1-more occurrence of the same decision question → promote to friction-ledger or skill.

### Closed by the batch-9 meta-phase (cycles 031/032/033; 2026-05-30)

- `ls-update-column-mutation-rotation-cycle-031-verified-against-audit-c030` — resolved cycle-031 — 33-row `verified_against:` block landed (all `supports`); GMRES-restart-cycle L1>L0 cohort end-to-end firm status stands.
- `back-solve-mutation-rotation-sub-pattern-b-brace-placement-narrative-correction-c030` — resolved cycle-031 — lifter narrative repair (5 edits + F1 row flip) corrected wrong "+1-line brace-placement shift" to "byte-for-byte identical, +179-line file offset"; theme stays firm.
- `incremental-ls-composition-lowering-residual-forthcoming-mentions-c032` — resolved cycle-032 — 4 "forthcoming"→"firm" qualifier flips landed at `:114`/`:276`/`:300`/`:306`; 4 historical-quote sites correctly preserved.
- `sparse_triangular_solve-slice-reduction-candidacy-c029-on-disk` — resolved cycle-031 — verdict **DEFER** (retain in full, `annotated-and-retained`); slice is canonical instance for 3 concept pages; Phase-1 corpus tally stays 9/10; reciprocal cross-link to `triangular-solve-obstruction` added. Precedent: `polynomial_recurrence_step`. Canonical-instance carve-out codified this batch (CLAUDE.md + skill step 7).
- `jacobi-smoother-mutation-rotation-l1-l0` — resolved cycle-033 — firm L1>L0 theme `jacobi-smoother-mutation-rotation` landed report-1; 4 sub-patterns A/B/C/D, syntactic identities on Palace source.
- `reciprocal-and-elementwise-product-l1-primitives` — resolved cycle-033 — both firm L1 leaves landed reports-2/3; closes the stub-or-harvest decision via harvester preference per CLAUDE.md `Lower-level shared vocabulary takes priority`.
- `jacobi-smoother-mutation-rotation-reciprocal-elementwise-product-live-link-upgrade` — resolved cycle-033 — 4 in-cycle live-link upgrades landed by `integrator-finalize` per the skill; closes in one cycle.
- `reciprocal-l1-index-md-firm-count-d3-second-bump` — resolved cycle-033 — D3 per-report integrator bumped `book/src/L1/index.md` Firm count 24→25 and extended heading prose tail; D2 and D3 coordination cleanly serialized.
- `elementwise-product-conjugation-variant-axis-vs-distinct-primitive-decision-record` — resolved-by-design cycle-033 — verdict landed: one primitive with conjugation sub-axis on complex element-type; durable methodology-decision record (8-of-10 laws shared; precedent differs from `dot`/`tdot`). Promoted to deferred-contingent `variant-axis-vs-distinct-primitive-decision` (potential skill if recurs).
- `negative-result-slice-canonical-instance-blocks-reduction` — addressed cycle-033 meta-phase — friction-ledger entry codified; CLAUDE.md §Phase-1-corpus invariant amended with canonical-instance carve-out; `phase-1-slice-reduction-audit` skill step 7 + Discipline bullet added.
- `cycle-planner-pre-dispatch-existence-check-of-target-artifact` — addressed cycle-033 meta-phase — promoted to friction-ledger `cycle-planner-stale-priorities-line-recruitment` (recurrence-3); skill `verify-dispatch-scope-not-already-discharged` promoted; cycle-planner role-spec ENFORCEMENT bullet wiring four-step deliverable-presence check.

### Closed by the batch-10 meta-phase (cycles 034/035/036; 2026-05-31)

- `reciprocal-l1-l0-mutation-rotation-theme` — resolved cycle-034 D1 — landed firm as Sub-pattern A in the composite L1>L0 theme `reciprocal-elementwise-product-mutation-rotation` (757 lines, 40 citations clean). Per c033-suggested thin-theme co-authoring precedent.
- `elementwise-product-l1-l0-mutation-rotation-theme` — resolved cycle-034 D1 — landed firm as Sub-pattern B in the same composite theme. Composite-vs-split decision: composite.
- `jacobi-mutation-rotation-dead-code-complex-transpose-kernel-lowering-verifier-audit` — resolved cycle-034 D2 — verdict-only audit landed clean; all three sub-verdicts confirm caveats are correctly recorded (jacobi `:61-69` supports; chebyshev `:101-110` supports + `:150-159` supports-with-cite-tighten-followup; axpby `ComplexVector::Subtract` does-not-support-planner-mischaracterization).
- `chebyshev-smoother-mutation-rotation-applyorderk-true-citation-tighten` — resolved cycle-035 D1 — surgical cite-precision fix landed; `:150-159` → `:147-155` (3 edits, theme stays firm).
- `cg-initial-residual-quirk-palace-bug-flag-lift-path` — resolved cycle-035 D2 (lift portion) — additive recognition-rule annotation lifted into firm L1>L0 theme `ksp-solve-mutation-rotation` CG Sub-pattern B (`Norml2`-vs-`Dot` initial-residual asymmetry; theme stays firm). Narrower upstream-confirmation sub-OQ retained as out-of-scope (see deferred).
- `floquet-correction-operator-construction-variants` (apply_linop dimension) — resolved cycle-035 D3 (NEGATIVE finding) — `FloquetCorrSolver::Mult` / `::AddMult` (`floquetcorrection.cpp:72-85`) match `apply-linop-mutation-rotation` sub-patterns A/D bit-for-bit; `apply-linop-overload-set.md:33` non-exhaustive caveat already accommodates constructed-operator-gate classes. NO extension to apply-linop-mutation-rotation / apply-linop-overload-set needed.
- `floquet-correction-l1-gate-harvest` — resolved cycle-036 D1 — firm L1 `floquet-correction.md` (402 lines, 6th constructed-operator gate; 96/96 citecheck clean) + firm L1>L0 `floquet-correction-mutation-rotation.md` (525 lines, 4 sub-patterns A/B/C/D) + concept `nested-constructed-operator-gate` firm-instances 2→3 (second three-deep chain floquet → ksp → jacobi-smoother).
- `nested-constructed-operator-gate-second-three-deep-chain-codified` — resolved-on-landing cycle-036 D1 — tracker entry; the concept-page upgrade landed alongside the firm L1 floquet operator.
- `l3-cohort-growth-audit-c036-verdict` — resolved cycle-036 D2 — cycle-010 deferred-audit at `book/src/L3/index.md:38` SETTLED with the (A)/(B)/(C) classification: 6 firm (A) identity-in-form L3-backfill candidates (`assemble-diagonal`, `jacobi-smoother`, `reciprocal`, `elementwise_product`, `normalize`, `divfree-projector`); 2 L1-promotion-gated (A) (`matrix-weighted-norm`, `bilinear-form`); 3 substantive (B) (`orthogonalize`, `chebyshev-smoother`, `apply_nonlinear_pencil`); 7-operator (C) NEGATIVE LIST (`lu_solve`, `back_solve`, `ls-update-column`, 4 NLEPS atoms). Supersedes `l3-vocabulary-inventory-gap` + `l3-backfill-apply-linop-and-blas1-cohort`. The (C) NEGATIVE LIST is the anti-recurrence data feed for `verify-dispatch-scope-not-already-discharged` skill step 5.
- `cycle-planner-stale-priorities-line-recruitment` — strengthened cycle-036 meta-phase (was c033-addressed) — recurrence 3 → 6 status `escalating`; batch-10 paste-inline-evidence amendment to cycle-planner role-spec + skill steps 5/6 + STOP-PROPOSING NEGATIVE LIST in priorities.md. Swap-to-opus stays HELD; surfaced as ASK for recurrence-7 trigger.
- `firm-chapter-body-authored-outside-proposed-changes-fence` — NO-GO verdict cycle-036 meta-phase — recurrence-3; detect+repair pipeline held; further intervention not warranted at current cadence.
- `polynomial-smoother-l2-combinator-from-jacobi-and-chebyshev` — closed BLOCKED-RETIRED cycle-036 meta-phase — Palace has no third Richardson sibling; out of scope per unimplemented-Palace-components policy.

### Closed by the batch-11 meta-phase (cycles 037/038/039; 2026-05-31)

- `l3-cohort-growth-audit-c036-verdict` — **FULLY CLOSED (A)-cohort cycle-039** — the six (A) identity-in-form L3 backfills all landed firm: `assemble-diagonal`+`jacobi-smoother` (c037), `reciprocal`+`elementwise_product`+`divfree-projector` (c038), `normalize` (c039); L3 firm 9→15. Residual tracker outside (A): the L1-promotion-gated pair (`matrix-weighted-norm`/`bilinear-form`) + the (B) substantive candidates (`orthogonalize`/`chebyshev-smoother`/`apply_nonlinear_pencil`) carried into the plan Backlog. The 6 per-portion closure notes (assemble-diagonal/jacobi-smoother/reciprocal/elementwise_product/divfree-projector/normalize) all fold here.
- `floquet-corrector-addmult-aliasing-applicability-audit` — resolved cycle-039 D2 — the c038 D4 partially-supports AddMult-aliasing row re-anchored from the thin wrapper `ksp.cpp:297` to the true gated mechanism `CgSolver::Mult` (`iterative.cpp:361`, else-branch `:384-385` `r=b; x=0.0`) + the `SetInitialGuess(0)` precondition (`floquetcorrection.cpp:61`); all 6 mention sites re-framed, verified_against upgraded to all-supports, theme stays firm.
- `l3-index-fourth-obstruction-profile-obstruction-carrying-by-reference` — resolved cycle-039 D3 — §Semantics-overlay gained shape (d) obstruction-carrying-by-reference (divfree-projector exemplar; carries inner ksp_solve's sequential-obstruction by reference, authors none of its own); "Three firm shapes" → "Four firm shapes".
- `l3-index-semantics-overlay-constructed-operator-gate-sub-family` — resolved cycle-039 D3 — folded into the §Semantics-overlay refresh alongside the fourth-obstruction-profile fold (constructed-operator-gate sub-family named; obstruction-carrying vs obstruction-free gates distinguished).
- `l3-index-firm-count-bump-assemble-diagonal` / `l3-index-firm-count-bump-jacobi-smoother` / `l3-index-working-notes-firm-count-refresh-c038-reciprocal` / `-elementwise-product` / `-divfree-projector` / `l3-index-working-notes-firm-count-refresh-c039-normalize` — all RESOLVED cycle-039 D3 — the six parallel integrator-action count-bump flags reconciled to ONE authoritative consolidated tally (15 firm + 2 partial-obstruction) under the D3-count-ownership convention; per-cycle running snapshots labeled superseded. Friction-ledger `parallel-blind-shared-index-count-divergence` (addressed) codifies the convention that avoided the divergence in c039.
- `assemble-diagonal-l3-reciprocal-elementwise-product-plain-text-forward-refs` / `l3-reciprocal-plain-text-forward-refs-elementwise-product` / `l3-elementwise-product-plain-text-forward-refs-normalize-divfree` — closed-stale cycle-039 — the (A) cohort targets (`reciprocal`/`elementwise_product`/`normalize`/`divfree-projector` L3) all landed by c039, so the plain-text forward-refs were live-link-upgraded at apply time (`upgrade-plain-text-ref-to-live-link-when-target-on-disk`) where the target was on disk; no residual plain-text-to-firm-target refs remain in the cohort.
- `floquet-corrector-addmult-aliasing-codemap-read-range-plus-one-drift-carry-forward` — closed-consumed cycle-039 meta-phase — the `iterative.cpp:360`-vs-on-disk-`:361` +1 drift data point recorded into friction-ledger `codemap-read-range-plus-one-drift-on-brace-boundary` (recurrence-5, NEW boundary, catch-and-correct held, status stays `addressed`). No per-cycle action.
- `reciprocal-elementwise-product-mr-dead-code-transpose-consumer-branch` — closed cycle-037 D3 (re-confirmation note) — second independent dead-code confirmation; status UNCHANGED (`needs-more (informational)`); durable provenance pointer carried at the c034 D1 OQ. No new OQ opened.

### Closed by the batch-12 meta-phase (cycles 040/041/042; 2026-06-01)

**The five load-bearing batch-12 decisions (all the cycle-041/042 D-section OQs resolve here):**
- `dot-l2-leaf-floor-vs-fold-only-design` — **RATIFIED (b) leaf-floor cohort-wide, cycle-042 meta-phase.** Each firm L3 leaf gets a same-named L2 floor (CLAUDE.md "both L levels"); floor defers fusion to the fold-parent; the +4-file delta is below the duplication-explosion bar; consistent with `axpby-as-primitive`. Evidence: cycle-042 D1 cross-cutter audit. Standing convention in `priorities.md` §Methodology. Closes the canonical fork OQ + all its riders below.
- `l2-no-dot-leaf-floor-but-fold-is-the-l2-surface` / `nrm2-l2-floor-rides-...` / `scal-leaf-vs-linear-combination-fold-realization-fork` / `l2-leaf-floor-distinctness-rests-on-methodological-not-algebraic-axis` — closed → folded into the fork ratification. **`nrm2` carved out** (consumer-not-member, fork-invariant on membership).
- `l2-reciprocal-outside-the-leaf-vs-fold-fork` / `l2-elementwise-product-fork-independent-design-final` / `l2-assemble-diagonal-outside-the-leaf-vs-fold-fork` / `l2-jacobi-smoother-outside-the-leaf-vs-fold-fork` / `divfree-projector-l2-floor-is-moderate-not-thin` / `assemble-diagonal-themes-fork-independent-not-under-blas1-fork` — closed-informational. Fork-independence data points; the 5 cycle-042 standalone floors are design-final regardless (no fold-parent). Confirmed by the ratification: the fork touches ONLY fold-parented leaves (`dot`/`scal`).
- `arity-family-leaf-floors-output-aliasing-axis-is-the-folds` — migrated to plan (c043 active head #3 authoring note: the axpy-family floors note the aliasing axis is the fold's).
- `l2-scal-fold-parent-frontmatter-field-convention` — closed-deferred: the `fold_parent:` frontmatter field is kept as-authored (a low-stakes convention signal); no cohort-wide mandate enacted (prose membership is the primary record). Drop or keep per-entry without status impact.
- `l2-reciprocal-l2l1-theme-slug-naming` / `l2-elementwise-product-underscore-vs-hyphen-filename-wrinkle` / `l2-assemble-diagonal-directive-scope-extension-blas1-to-operator-to-data` / `l2-floor-under-l3-blas1-cohort-directive-rename-candidate` — **RESOLVED cycle-042 meta-phase.** Theme-slug convention RATIFIED `-leaf-identity`/`-body-identity`; the 2 cycle-041 `-fold-specialization` outliers + the `elementwise_product-body-identity` underscore rename via the c043 LIFTER-SWEEP. Directive RENAMED `l2-floor-under-l3-blas1-cohort` → `l2-floor-under-l3-leaf-cohort`. Operator-chapter filenames keep existing spelling.
- `l2-elementwise-product-floor-candidate` / `l2-jacobi-smoother-elementwise-product-floor-candidate` — closed-resolved cycle-042: the `elementwise_product` + `reciprocal` L2 floors landed c042 (D3/D2); the jacobi-smoother apply now rests on present elementwise floors (dep-map re-point folds into the c043 lifter sweep gloss-refresh).

**cycle-042 count-ownership + forward-ref + theme-landing dispositions (all resolved-by-D11 / resolved-by-landing):**
- `l2-reciprocal-count-ownership-deferred-to-D11` / `l2-elementwise-product-count-ownership-deferred-to-D11` / `l2-assemble-diagonal-count-ownership-deferred-to-D11` / `l2-jacobi-smoother-count-ownership-deferred-to-D11` / `l2-divfree-projector-count-ownership-deferred-to-D11` — RESOLVED cycle-042 D11 (layer-intro-author sole count-owner reconciled the L2/index tally to 17 firm + 1 partly-constructive + the §Vocabulary-cohort standalone-floor sub-list; finalize confirmed clean, count-ownership partition held at 11-wide). Friction-ledger `parallel-blind-shared-index-count-divergence` (batch-12 confirmation).
- `l2-elementwise-product-l2l1-theme-not-yet-authored` / `l2-assemble-diagonal-l2l1-theme-not-yet-authored` / `l2-jacobi-smoother-l2l1-apply-identity-theme-not-yet-authored` / `l2-scal-fusion-l3-l2-scal-themes-plain-text-forward-ref-upgrade` — RESOLVED cycle-042 (the D7–D10 abstractor dispatches landed the L2>L1 + L3>L2 themes for all 5 standalone floors; L3>L2 5-of-18 → 10-of-18; forward-refs live-link-upgraded at apply time where the target was on disk).
- `assemble-diagonal-load-bearing-approximate-diagonal-non-law-preserved-both-edges` — RESOLVED cycle-042 D7 (satisfied as authored; the matrix-free high-order-Nedelec non-law preserved across both `assemble-diagonal` theme edges; lowering-verifier confirm deferred).

**cycle-040 dispositions:**
- `l3-index-semantics-overlay-taxonomy-and-partial-obstruction-count-refresh` / `l3-index-fifth-obstruction-profile-fused-composite-obstruction-free` / `l3-index-working-notes-stale-snapshot-compaction-candidate` — DISCHARGED cycle-040 D2 (L3/index §Semantics-overlay refreshed to 15 firm + 3 partial-obstruction with `orthogonalize` as the third; fifth obstruction-free profile folded; single authoritative tally established, prior snapshots labeled SUPERSEDED).
- `floquet-mutation-rotation-m-block-comment-citation-over-extension` / `chebyshev-smoother-mutation-rotation-applyorder0-true-citation-tighten-sibling` / `chebyshev-smoother-mutation-rotation-applyorderk-true-citation-tighten` — DISCHARGED cycle-040 D3 + c035 D1 (the lifter citation-tightens; `:25-26`→`:25`, `:101-110`→`:102-110`, `:150-159`→`:147-155`; all citecheck `--anchor` verified).
- `concepts-sequential-obstruction-variant-conditional-sub-shape` / `orthogonalize-mgs-variant-split-obstruction-sub-shape-naming` — kept-deferred (the `concepts/sequential-obstruction.md` named variant-conditional sub-shape write-up; out of D2 index-refresh scope). *Trigger:* a same-layer-cross-cutter / layer-intro-author concept-page touch.
- `l4-orthogonalize-arnoldi-step-monad-surface-unauthored` — kept-deferred (speculative L4 `orthogonalize` Arnoldi-step-monad sketch; below the clearly-implied-stub bar). *Trigger:* an abstractor L4 dispatch when a second L4 consumer materializes.

**cycle-043 lifter-sweep targets (MIGRATED to plan — c043 active head #1 LIFTER-SWEEP):**
- `l3-reciprocal-stale-no-interposed-l2-entry-lifter-reanchor` / `l3-assemble-diagonal-stale-no-interposed-l2-entry-lifter-reanchor` / `l3-jacobi-smoother-stale-no-interposed-l2-entry-lifter-reanchor` / `l3-divfree-projector-stale-no-interposed-l2-entry-lifter-reanchor` (the directive-named consolidation OQ) / `l3-elementwise-product-sibling-gloss-staleness-at-166` / `assemble-diagonal-l3-stale-lowers-to-folded-into-cycle-043-lifter-sweep` / `l1-assemble-diagonal-absmulttranspose-citation-drift-172-vs-174` / `l3-dot-lowers-to-non-adjacent-l1-wants-reanchor-to-new-l2-floor` — all MIGRATED into the single c043 LIFTER-SWEEP plan item (re-anchor the stale-L3 "no interposed L2 entry / no L3-L2 theme" assertions across reciprocal/assemble-diagonal/jacobi-smoother/divfree-projector + the two cross-file citation fixes + the slug renames). Pointer in §Open — migrated to the plan.

**count-ownership notes resolved-by-D7 (cycle-041):**
- `l2-index-vocabulary-cohort-firm-at-l2-list-add-dot-floor` / `-add-nrm2-floor` / `l2-index-intro-third-category-identity-in-form-floor-leaves` / `l2-index-intro-third-category-and-firm-count-add-scal-floor` / `l2-blas1-floor-cohort-completeness-planner-confirm` / `l2-normalize-as-fused-l2-primitive-inherited` — RESOLVED cycle-041 D7 (the L2/index §Vocabulary-cohort firm-list + third-category motif naming + tally landed; `normalize` L2 floor migrated to plan c043 active head #4).

### Batch-10 per-report dispositions — unified into the Closed index + the plan (cycle-036 meta-phase)

The cycle-034/035/036 per-report integrator OQ appends (append-only between meta-phases) are **unified here by the cycle-036 meta-phase**. RESOLVED dispositions are compacted into the **Closed (index) → "Closed by the batch-10 meta-phase"** subsection below. Methodology signals are codified in the friction-ledger + skills + cycle-planner role-spec (see Enacted changes section of the batch-10 meta-phase report `reports/2026-05-31T233000Z-meta-phase-cycle-036/CYCLE.md`). Genuinely-open items are kept as deferred-contingent one-liners below.

**Addressed this meta-phase (was Open methodology signal):**
- `cycle-planner-stale-priorities-line-recruitment` (recurrence-3-within-batch-10) — **STRENGTHENED cycle-036 meta-phase.** The batch-9 codification proved insufficient; the batch-10 enactment adds paste-inline-evidence requirement (role-spec bullet AMENDED), skill steps 5 (STOP-PROPOSING NEGATIVE LIST consult) + 6 (audit-first vs reflexive-harvest framing), and the STOP-PROPOSING NEGATIVE LIST recorded prominently in priorities.md Backlog. Friction-ledger entry status `addressed` → `escalating`, recurrence 3 → 6. Swap-to-opus stays HELD; surfaced as a meta-phase ASK for the recurrence-7 trigger.
- `firm-chapter-body-authored-outside-proposed-changes-fence` (recurrence-3) — **NO-GO verdict cycle-036 meta-phase.** Detect+repair pipeline held cleanly; producer-side bullets exist and are reminder-dependent like all reminder-class mitigations; steady-state cost (~1 critic-fail + 1 repair per occurrence, ~1 cycle in ~12) is bounded. Re-open ONLY on recurrence-4 within ≤6 cycles.

**Closed this meta-phase (BLOCKED-RETIRED):**
- `polynomial-smoother-l2-combinator-from-jacobi-and-chebyshev` — closed cycle-036 meta-phase as BLOCKED-RETIRED. Palace exposes no standalone Richardson smoother; the 3-sibling combinator-miner pattern needs 3 source-witnessed siblings, and Palace has 2 (Jacobi + Chebyshev). Out of scope per CLAUDE.md unimplemented-Palace-components policy. Updated in `priorities.md` §Deferred-contingent as closed-BLOCKED-RETIRED.

**Kept — deferred / contingent (genuinely open one-liners):**
- `chebyshev-smoother-mutation-rotation-applyorder0-true-citation-tighten-sibling` — opened cycle-035 D1. Low-priority citation hygiene: the chebyshev-smoother-mutation-rotation theme's first dead-code complex-transpose kernel is cited as `:101-110`; tight dead `else`-block body is `:102-110` (1-line overshoot on start). citecheck with `--anchor 'else'` passes; not a citecheck-failing drift. *Trigger:* a future lifter / same-layer-cross-cutter touch on `book/src/L1-L0/chebyshev-smoother-mutation-rotation.md`.
- `floquet-correction-real-vector-instantiation-dead-code` — opened cycle-036 D1. Constructor `if constexpr` real branches at `floquetcorrection.cpp:35-38, 53-56` textually present but unreachable under sole `<ComplexVector>` instantiation; captured as deliberate scope-out in theme §Sub-pattern D + L1 §Context. *Trigger:* upstream Palace adds `<Vector>` instantiation OR same-layer-cross-cutter survey of `if constexpr`-real-branch dead-code patterns.
- `cg-initial-residual-quirk-upstream-confirmation-pending` — narrower sub-OQ retained from c035 D2 closure; out-of-scope. *Trigger:* upstream Palace issue filed or `git blame iterative.cpp:408`.
- _(closed cycle-039 D2: `floquet-corrector-addmult-aliasing-applicability-audit` — see Closed index, batch-11 subsection.)_

### Batch-11/12 per-report dispositions — unified into the Closed index + the plan (batch-11/12 meta-phases)

The cycle-037–042 per-report integrator OQ appends were unified by the batch-11/12 meta-phases (RESOLVED dispositions in the **Closed by the batch-11/12 meta-phase** subsections above; methodology signals in the friction-ledger + role-specs). Genuinely-still-open one-liners kept below as deferred-contingent.

**Kept — deferred / contingent (genuinely open one-liners):**
- `jacobi-smoother-l4-no-entry-verdict-carried-by-analogy` — opened cycle-037 D2. The "no standalone L4 entry" verdict for `jacobi-smoother` rests on analogy to the firm `apply_linop`/`ksp_solve` constructed-operator-gate L4 verdicts, not a discrete audit. *Trigger:* a future cross-layer-cross-cutter pass if L4 constructed-operator-gate policy is revisited (likely folds into the batch-14 `l4-l3-coverage-and-l4-expansion` survey).
- `normalize_B-l3-l1-promotion-gated` — opened cycle-039 D1. The B-weighted `normalize_B` is NOT L3-dispatchable until `matrix-weighted-norm` promotes from `rough-in (test-coverage-bounded)` at L1. *Trigger:* L1 promotion of `matrix-weighted-norm`.
- `l1-normalize-frontmatter-firmness-field-absent` — opened cycle-039 D1 (minor convention-harmonization). `book/src/L1/normalize.md` has no YAML `firmness:` field (status is in §Status prose, authoritative). *Trigger:* any future producer touch on the file.
- `concepts-sequential-obstruction-variant-conditional-sub-shape` (folds `orthogonalize-mgs-variant-split-obstruction-sub-shape-naming`) — opened cycle-040 D1/D2. A named **variant-conditional** sequential-obstruction sub-shape (`orthogonalize` MGS canonical instance). *Trigger:* the batch-14 `erasure-scope-taxonomy-concept-page` dispatch — fold in alongside it (it touches the same obstruction-concept family).
- `l4-orthogonalize-arnoldi-step-monad-surface-unauthored` — opened cycle-040 D1. Speculative L4 `orthogonalize` Arnoldi-step-monad sketch; below the clearly-implied-stub bar. *Trigger:* an abstractor L4 dispatch when a second L4 consumer materializes (likely surfaces under the batch-14 L4 climb).

## Closed by the batch-13 meta-phase (cycles 043/044/045; 2026-06-01)

**The foundation campaign completed + the four-root erasure-scope taxonomy ratified (all c043/c044/c045 New-intake OQs resolve here):**
- `chebyshev-floor-cohort-count-reconciliation` — resolved cycle-045 meta-phase — **count-correct-to-12-of-12-applicable + naming-exception**: `chebyshev`'s L2 floor is already present as the non-same-named firm `book/src/L2/chebyshev-iteration.md` (c012); a same-named `L2/chebyshev.md` pointer would be pure naming bureaucracy with zero coherence gain (the L3 reader already finds `chebyshev` floored). The cohort is COMPLETE.
- `normalize-fused-composite-no-fold-parent-sub-shape` — resolved cycle-045 meta-phase — accepted as the **third thin-identity sub-shape** (fused-composite-of-two-same-layer-floors, no fold-parent; `normalize = nrm2 ∘ scal`), distinct from c041 fold-parented-leaf + c042 standalone-leaf/-gate. Already the third §Vocabulary-cohort sub-group in `book/src/L2/index.md`; no further action.
- `l2-floor-directive-slug-rename-book-chapter-body-residual` + `l2-floor-directive-slug-rename-scaffolding-residual-sweep` — resolved cycle-044/045 — c044 D1 swept all `book/` chapter-body occurrences to ZERO; the batch-13 meta-phase swept the live cross-references in `priorities.md` (`l2-floor-under-l3-blas1-cohort` → `-leaf-cohort`); historical-narrative + rename-event-record + append-only (`cycle-record.jsonl`/`integrator-signals.md`) occurrences intentionally left as the record.
- `l3-{axpy,axpby,axpbypcz,normalize}-lowers-to-staleness-after-l2-floor` (+ their D6/D7/D8/D10 converging-signal appends) — resolved cycle-044 D1 — the consolidated lifter sweep re-anchored all four L3 entries L3>L1 → L3>L2>L1 onto the now-present floors + `-body-identity` themes; the cross-cycle stale-assertion window closed.
- `l3-leaf-cohort-l2-floor-reanchor-deferred-from-c043` — resolved cycle-045 meta-phase — the process recommendation codified as friction-ledger `floor-landing-implies-same-cycle-adjacent-entry-reanchor` + a cycle-planner dispatch-design §Discipline bullet (couple a floor landing with its adjacent-entry re-anchor in the same cycle; c045 D1/D2 bundled-into-dispatch is the working precedent).
- `l3-index-audit-block-citation-drift` — resolved cycle-044 D1.ii — the index-wide `L3/index.md:NN` audit-block re-pin (the 037-040 L3 operator entries' `:39`/`:41`/`:44`/`:47` drift to the live `:45-49`) swept in the c044 consolidated lifter sweep.
- `verify-slug-rename-completeness` (skill-candidate) — **PROMOTED cycle-045 meta-phase** — `skills/verify-slug-rename-completeness/SKILL.md` (grep → map → confirm-zero-residual + substring-collision / not-renamed-sibling guard; companion to `audit-slug-meaning-before-coordinated-cross-report-rename`). The c043 D1 sweep is the working precedent.
- `chebyshev-smoother-l3-candidate-subsumed-closed` — resolved cycle-044 D2 (opened-and-resolved) — SUBSUMED / NO-LAND: `chebyshev-smoother` (L1 slug) is the same operator whose L3 iteration-rotation is the firm L3 `chebyshev` (c013); the L1/L2/L3/L4 slug-name asymmetry (`chebyshev-smoother`/`chebyshev-iteration`/`chebyshev`) is benign-and-intentional (each layer names in its own vocabulary). Removed one (B)-candidate from the substantive-L3 frontier.
- `remaining-substantive-l3-l2-rotations-chebyshev-eigsolve` — resolved cycle-045 — BOTH halves landed: `eigsolve` (a) via D1 (`eigsolve-opaque-eigen-iteration`), `chebyshev` (b) via D2 (`chebyshev-nested-recurrence`). The substantive L3>L2 frontier is complete.
- `l3-l2-chebyshev-substantive-theme-vs-in-line-decision` — resolved cycle-045 D2 — dedicated theme WARRANTED + LANDED (`book/src/L3-L2/chebyshev-nested-recurrence.md`): the in-line non-adjacent-identity convention covers only IDENTITY edges; chebyshev's two nested `iterate_while_pure_L3` recurrences are SUBSTANTIVE loop-erasure, so a dedicated theme is warranted (the body stays identity-in-form in-line).
- `substantive-l3-l2-erasure-scope-taxonomy` (c044) + `l3-l2-substantive-erasure-scope-taxonomy` (c045 D1) + `l3-l2-erasure-scope-taxonomy-FOUR-root-complete-ratify-plus-concepts-page` (c045 D3) — **UNIFIED + resolved cycle-045 meta-phase** — these THREE were the same question at successive corner-counts (two / three / four roots). The **FOUR-ROOT erasure-scope taxonomy RATIFIED**: (1) unconditional-single-loop (`ksp-solve-outer-driver`), (2) variant-conditional-single-loop (`orthogonalize-variant-split`), (3) unconditional-nested-double-loop (`chebyshev-nested-recurrence`), (4) opaque-library (`eigsolve-opaque-eigen-iteration`). The `concepts/erasure-scope.md` page is **GO** → migrated to plan Backlog High `erasure-scope-taxonomy-concept-page` (layer-intro-author; meta-phase does not write `book/`). Canonical write-up already in `book/src/L3-L2/index.md` §Working-Notes (c045 D3).
- `l3-l2-coverage-gap-denominator-reconciliation-17-of-18-vs-17-of-17-applicable` — resolved cycle-045 meta-phase — **RE-DENOMINATED to 17-of-17-applicable** (option (a)): the 18th L3 entry `apply_linop` is no-L2-by-design (`book/src/L3/apply_linop.md:146` — lowers directly L3 → L1, no L2 RHS for an L3>L2 theme to target), so it is not an applicable denominator member. The `l3-l2-rotation-theme-coverage-gap` is effectively closed.

**Kept — deferred / contingent (batch-13; moved to the top-of-file deferred-contingent sections):**
- `concepts-sequential-obstruction-opaque-library-marker-distinction` (c045 D1) + `concepts-axpby-page-unauthored` (c043 D4) — both moved to the top **Open — deferred / contingent → orthogonalize / concepts / citation hygiene** family (the former folds into the `erasure-scope-taxonomy-concept-page` dispatch; the latter awaits a second converging concept-page reference).


## Closed by the batch-14 meta-phase (cycles 046/047/048; 2026-06-01)

**The L4 frontier was advanced to substantially-complete (L4 firm 4→6, L4>L3 firm 3→6, L4 outer-driver vocab 3→4) + the residual-L2>L1 floor hygiene closed (L2>L1 firm 19→21). All c046/c047/c048 New-intake + per-report integration-disposition OQs resolve here:**

*L4 frontier / coverage survey (c046 D1 survey → c047/c048 landings):*
- `l4-l3-coverage-and-l4-expansion` — **NOT closed; lead-frontier plan item, re-scoped by the batch-14 meta-phase** (the L4 frontier is now substantially complete; the item carries forward for the batch-14 next-direction reshape — see plan).
- `iterate-while-l4-l3-standalone-theme-warranted-lifter-vs-abstractor` — resolved cycle-047 D1+D2 (route-b: abstractor authored both dedicated L4>L3 themes `iterate-while-dissolution` + `iterate-while-with-prev-dissolution`; both firm L4 caps re-anchored). Subsumes/closes `standalone-iterate-while-l4-l3-theme-pending` (trigger moot).
- `iterate-while-l3-rendering-trajectory-accumulation-gap` — resolved cycle-047 D1 (trajectory-drop is the §3.8-pruned image of Law 1's unpruned ground form, not a gap; two consumer-selected L3 images + the collapse rule between them).
- `iterate-while-with-prev-lowers-to-reanchor-to-dedicated-dissolution-theme` — resolved cycle-047 D2 (authored the with-prev theme + the three re-anchors).
- `l4-ksp-solve-eigsolve-caps-gated-on-solve-monad-outer-driver-vocabulary` — **WHOLE OQ resolved cycle-048** (anchor-half discharged c047 D5 `solve-monad` vocabulary; ksp_solve cap-half c048 D1; eigsolve cap-half c048 D2). `book/src/L4/{ksp_solve,eigsolve}.md` both firm.
- `solve-monad-l4-row-firm-maturity-straddle` — resolved cycle-048 D1 (the backing `L4/ksp_solve` page firms the `solve_loop`/`restart_cycle` rows; the `Outcome` eigsolve-extension straddle discharged by the `EigOutcome` clean-addition).
- `ksp-solve-driver-dissolution-l4-l3-theme-landed` — completed cycle-048 D3 (`book/src/L4-L3/ksp-solve-driver-dissolution.md` firm; the R2 cap's dedicated driver-half rotation).
- `ksp-solve-driver-dissolution-slug-reconciliation` — resolved cycle-048 (D1's working slug `ksp-solve-outer-driver-dissolution` reconciled to the canonical `ksp-solve-driver-dissolution`; zero residual). The friction is codified as `cross-report-forward-reference-slug-divergence` (friction-ledger; role-spec bullets this batch).
- `eigsolve-l4-l3-in-line-by-design-no-dedicated-theme` — recorded/closed cycle-048 (no dedicated `L4-L3/eigsolve-*` theme; in-line marker-erasure by design over an opaque-library obstruction, parallel to `chebyshev`'s in-line-by-design L4>L3). No follow-up.

*outer-driver Outcome-convention RATIFICATION (the batch-14 meta-phase decision-of-record):*
- `outcome-sum-one-row-vs-per-cap-specialisation` + `eigoutcome-vs-polymorphic-outcome-count-dependency` — **UNIFIED + RATIFIED (b) by the batch-14 meta-phase: KEEP the landed one-canonical-`Outcome` + per-cap `*Outcome`-rows shape** (the c048-landed surface: `Outcome = Continue | Done Bool` + `EigOutcome = Continue | Done EigStatus` as DISTINCT L4 dep-map rows; outer-driver vocab count = **4**, NOT re-collapsed). Deciding factor: the `EigOutcome` `PartialConverged Int` arm (`0 < k < requested`, no `ksp_solve` analog) carries a payload that a single polymorphic `Outcome α` would have to thread as a type parameter without unifying any laws — the per-cap concrete sums are the honest, lower-ceremony shape and they keep each cap's variant-axis self-contained. The polymorphic-`Outcome α` alternative is **declined** (no law-unification benefit; would force a parameter on every `solve_loop`/`restart_cycle` signature for zero shared-rewrite gain). The contingent count-dependency (4↔3) is therefore RESOLVED at 4 — no follow-up −1 adjustment to the `L4/index.md` tally. Standing convention going forward: a new iterative-solve cap with a richer termination sum registers its OWN `*Outcome` L4 row as a clean-addition extension of the canonical `Outcome`, not a contradiction.

*residual-L2>L1-gap floor hygiene (c046 D3 census → c047 D3/D4 landings):*
- `residual-l2-l1-gap-audit` — **CLOSED cycle-047** (both census gaps themed: `ksp-solve-outer-driver-unfold` D3 + `krylov-step-kernel-defusion` D4; L2>L1 firm 19→21).
- `ksp-solve-l2-l1-theme-gap` — closed cycle-047 D3 (`book/src/L2-L1/ksp-solve-outer-driver-unfold.md` firm; the downward driver edge now themed, matching the upward `L3-L2/ksp-solve-outer-driver`).
- `krylov-step-l2-l1-theme-gap` — closed cycle-047 D4 (`book/src/L2-L1/krylov-step-kernel-defusion.md` firm; resolves the dangling `L2/krylov-step.md:121` forward-ref's satisfiability).
- `residual-l2-l1-gap-audit-planner-undercount` — resolved cycle-047 (the plan used the census's gap-set of 2, dispatched both; the dispatch-vs-census undercount-of-1 is moot).
- `residual-l2-l1-gap-audit-ksp-solve-edge-mislabel` — closed-in-critique cycle-046 (in the report's favor — `ksp_solve`'s L2>L1 is a genuine missing-dedicated-theme gap, not an edge-mislabel).

*erasure-scope concept page (c046 D2):*
- `erasure-scope-taxonomy-concept-page` — landed cycle-046 D2 (`book/src/concepts/erasure-scope.md` firm, kind `layer-pattern`); plan Backlog High item DISCHARGED.
- `erasure-scope-kind-classification` — decided/closed at integration cycle-046 (`layer-pattern`, per the literal `concepts/index.md` Kind-column definitions; decision-of-record so a later width pass does not re-litigate).
- `l3-l2-rotation-theme-coverage-gap` — answered/COMPLETE (17-of-17-applicable, re-denominated batch-13; the erasure-scope page is its conceptual yield).

**Kept — deferred / contingent (batch-14; moved to the top-of-file deferred-contingent sections):**
- `l4-orthogonalize-cap-marginal-defer` (c046 R5) — the only remaining L4-cap candidate, deferred-marginal (gated on a non-existent firm L4 Arnoldi consumer); mirrors the deferred `l4-orthogonalize-arnoldi-step-monad-surface-unauthored`. *Trigger:* a firm L4 `arnoldi_step` / GMRES Arnoldi driver materializes.
- `l4-native-combinator-denominator-completeness-survey` (c046 + c047 D5 touched) — definitional: whether the L4 native-combinator denominator includes the outer-driver coordination vocabulary (`solve_loop`/`restart_cycle`/`Outcome`/`EigOutcome` — monad-coordination, arguably distinct from the pure kernel-fold combinator denominator). Migrated to the plan as a candidate next-direction survey (batch-14 meta-phase — see plan Backlog `l4-native-combinator-denominator-completeness-survey`). *Trigger:* the cycle-049 planner picks the survey (a low-cost L4 consolidation pass).
- `l4-near-exhaustion-assessment-batch-14` (c048) — **ASSESSED by the batch-14 meta-phase**: the L4 frontier IS substantially complete; the near-term direction (L4 consolidation/audit + the denominator survey) is migrated to the plan, and the strategic "is the layer-construction phase done / do we pivot to the downstream burn-component effort?" question is surfaced as an **ASK to the human** (see the batch-14 meta-phase CYCLE.md §ask). *Trigger:* the human's scope-direction answer.
- `erasure-scope-l3-l2-index-line-anchor-drift-risk` (c046 D2) — moved to the top **Open — deferred / contingent → orthogonalize / concepts / citation hygiene** family.



## Batch-15 per-report dispositions — unified into the Closed index + the plan (cycle-051 meta-phase)

The cycle-049/050/051 per-report integrator OQ appends (append-only between meta-phases; ~215 lines of refactor-pass enactment status-records, demotion-worklist inputs, count-provenance guards, and D1–D5 apply-status records) are **unified here by the batch-15 meta-phase**. The refactor-pass theme-demotion is COMPLETE (16-of-17 degenerate pairs demoted, `divfree-projector-leaf-identity` KEPT), so nearly all of these are RESOLVED at c051 integration → compacted into the **"Closed by the batch-15 meta-phase"** subsection below (full prose in git history; slugs preserved as cross-reference anchors). The genuinely-open items are migrated to the plan or kept deferred below.

**Open — migrated to the plan (CYCLE-052 active head; pointer-only, the plan owns them):**
- `collapsed-leaf-disposition-convention-cohort-wide` / `leaf-chapter-disposition-remains-meta-gated-after-cycle-051-theme-demotion` (folds the D2 `inner-product-cohort-collapse-demotion-l3-propagation-one-batch` leaf-disposition sub-question) → **RATIFIED reduce-to-specialization-stub** by the batch-15 meta-phase → plan **CYCLE-052 #1** (`refactor-pass-leaf-chapter-reduce-to-stub`). The convention: each fold-family leaf chapter reduces to a thin specialization-stub (live link up to the combinator + retained unique L0 anchors + variant-axis row + deferred semantics); `nrm2` → consumer-stub; full-delete declined (would lose unique L0 citations + break live inbound consumer links).
- `l2-index-and-operator-chapter-historical-narrative-and-stale-future-tense-micro-sweep` + `linear-combination-home-residual-future-tense-sweep` → plan **CYCLE-052 #1** (co-scheduled bounded narrative micro-sweep: the non-build-breaking stale-future-tense / demoted-slug code-span residuals in `L2/index.md` `:118`/`:121`/`:123`, `L3/linear_combination.md` §Arity-specializations/§Status/§Dependencies/§Evidence, `L3-L2/index.md` cohort bullets — all bare code-spans, `linkcheck2`-safe).

**Open — deferred / contingent (kept; promote when the trigger fires):**
- `l4-propagation-depth-linear-combination` (c049 D1) — express the L4/L3 update-primitive group through `linear_combination` where it reads cleanly ("flag, don't force"); the krylov-step basis-correction-sum is clean, the chebyshev in-place aliasing may read better as a named specialization. Rides plan **CYCLE-052 #2** (continued in-layer combinator abstraction) as a low-priority note. *Trigger:* a batch-16+ harvester/abstractor picks up the L4 propagation note. (Scope fence: do NOT rewrite `dot`/`nrm2` L4 refs through `linear_combination` — sibling `inner_product` cohort.)

### Closed by the batch-15 meta-phase (cycles 049/050/051; 2026-06-01)

The refactor pass under the 2026-06-01 VOCABULARY-SHIFT REDIRECT. All RESOLVED at c051 integration unless noted. Slugs preserved as cross-reference anchors; full prose in git.

- `linear-combination-fork-OQs-superseded-by-2026-06-01-redirect` (+ `scal-leaf-vs-linear-combination-fold-realization-fork` + the `L2/index.md` §Working-Notes fork entry) — **closed-superseded-by-redirect** — the batch-12 keep-leaf-floor-(b) ratification is overturned by combinator-as-entry (the fork's option (a)); the inverted `linear_combination`/`inner_product` entries are now authoritative.
- `nrm2-consumer-not-member-must-survive-cycle-050` / `degenerate-lowering-d1-d2-reconciliation-before-cycle-050-enactment` — **closed-as-agreement** — D2+D3 agreed `nrm2` = do-NOT-merge CONSUMER (not a fold member); c051 D3 demoted it as a consumer-stub, the `nrm2-mutation-rotation` L1>L0 four-stage expansion KEPT (not a smell). Reflected in the ratified leaf-disposition (`nrm2` → consumer-stub).
- `degenerate-lowering-cohort-is-18-not-12-cycle-050-must-cover-all` / `degenerate-cohort-denominator-18-to-17-correction-after-divfree-leaf-keep` — **closed** — the cohort denominator settled at 17 (c049 D3: 12→18; c050 D8 verify-body: 18→17, `divfree-projector-leaf-identity` KEEP). Recorded as friction `refactor-cohort-denominator-under-scoped-by-planner-corrected-by-audit`.
- `degenerate-lowering-demotion-worklist-cycle-050-consumable` / `inner-product-cohort-collapse-demotion-l3-propagation-one-batch` / `divfree-jacobi-verify-body-verdicts-c051-demotion-enactment-input` — **closed** — D3's §A/§B/§C worklist + D8's per-pair verdicts (3 DEMOTE-OK / 1 KEEP) were the c050/c051 enactment inputs; the 16-of-17 demotion sweep COMPLETED c051. (The D2 leaf-disposition sub-question migrated to the plan, above.)
- `l3-linear-combination-leaf-re-expression-cycle-051` / `l3-linear-combination-downward-to-l2-demotion-home-cycle-051` / `l3-inner-product-leaf-re-expression-cycle-051` (+ the c050 `l3-{linear-combination,inner-product}` authoring/downward-note OQs) — **closed** — the 2 L3 combinators landed firm c050; the 5 L3 BLAS-1 leaves re-expressed through them c051.
- `divfree-l3-l2-demotion-must-keep-l2-floor-and-l2-l1-fusion-reachable` — **closed (constraint honored)** — c051 D4 demoted `divfree-projector-body-identity` (L3>L2) to an in-line note while keeping the L2 floor + the KEPT `divfree-projector-leaf-identity` (L2>L1) reachable via 2 direct live links from `L3/divfree-projector` (the one genuine step-4 `Grad->AddMult` fusion rotation not orphaned).
- the 4 `{assemble-diagonal,elementwise-product,reciprocal,normalize}-degenerate-theme-demotion-d7-count-reconciliation` — **closed** — resolved by c050 D7's count-owner landing (L3 17 firm, L3-L2 13, L2-L1 17).
- `c050-firm-theme-count-drop-is-vehicle-change-not-coverage-regression` — **closed (provenance recorded)** — the L3>L2 17→5 + L2>L1 21→10 drops across c050+c051 are a vehicle-change (theme file → in-line note / combinator re-expression), NOT coverage regression; recorded so a future census does not mis-flag it.
- the c051 D1–D5 apply-status records (`divfree-projector-leaf-identity-three-way-co-edit-line-266-serial-ordering`, `dot-family-cross-dispatch-dangler-and-cohort-log-line-drift-d5-finalize-checks`, `nrm2-consumer-demotion-d5-tally-and-l2-index-historical-narrative-refresh`, `nrm2-leaf-identity-d3-apply-status-and-divfree-body-identity-cross-dispatch-dangler`, `d4-jacobi-divfree-demotion-applied-divfree-body-identity-dangler-resolved-and-d5-count`, `d5-consolidated-count-applied-refactor-pass-theme-demotion-sweep-complete`) — **closed** — all applied clean; the 15-deletion dangling-link coordination resolved via distinct-substring narrowing (friction `multi-deletion-cross-dispatch-dangling-link-coordination`, skill `deleted-slug-inbound-live-link-sweep` PROMOTED); the OOB index-prose line-drift is non-load-bearing (friction `mass-deletion-shrinks-index-prose-line-references-drift-oob`, NO-GO on a re-pin convention); the final dead-link grep for all deleted slugs returned ZERO.
- `inner-product-fold-specialization-citation-drift-cycle-050-firming` — **kept-noted** — the 3 minor anchor drifts (`operator.cpp` Ax `:623→:624`/`:632→:634`; SPD assert `:615-616→:616`) are a citation-correction touch (NOT a status reduction; the KEEP-firm verdict stands); opportunistically co-schedulable on any future lifter touch of `inner-product-fold-specialization`. *Trigger:* a lifter pass touches that theme.



### Closed by the batch-16 meta-phase (cycles 052/053/054; 2026-06-02)

The verbose c052 (D1–D6) + c053 (D1/D2/D3) + c054 (D1/D2) per-report integration-append blocks are compacted here (full prose in git history; slug strings preserved as cross-reference anchors). Live carries went to **Open — migrated to the plan → Batch-16 migration** (the 5 batch-17 frontier items) and **Open — deferred / contingent → FE-assembly sub-spine siblings** (the 5 trigger-gated siblings).

**c052 refactor-pass-completion D-coordination + provenance (all RESOLVED at c052 finalize — D4 reconciled the indexes):**
- `collapsed-leaf-disposition-convention-cohort-wide` — CLOSED for the fold family (reduce-to-stub applied uniformly across all 12 fold-family leaf chapters; convention SETTLED).
- `l2-linear-combination-leaf-stub-stale-pinpoint-normalize-micro-sweep` / `l2-leaf-stub-fold-parent-to-specialization-of-frontmatter-rename-d4-coordination` / `l3-index-row-tense-collapses-to-collapsed-d4-coordination` / `inner-product-family-index-narrative-member-vs-consumer-d4-coordination` — D4-coordination micro-sweep items, reconciled at c052 (member-vs-consumer distinction preserved: `dot`=specialization, `nrm2`=consumer).
- `c052-d4-no-count-delta-verify-at-finalize` / `c052-d4-stale-l3-axpy-pinpoints-reconciled-to-reduced-stub` / `l3-index-obstruction-spectrum-leaf-wording-unchanged-correct` / `l3-scal-stub-dropped-plain-text-forward-refs-nrm2-and-scal-mutation-rotation` / `nrm2-stub-retains-normalize-consumed-floor-facts-provenance` — provenance-only / reconciled-at-apply; no residual.
- `firm-l2-l3-surface-is-combinator-complete-for-in-layer-conciseness` (D5 NEGATIVE) — confirmed: the firm L2/L3 BLAS/projector/smoother surface has no un-mined in-layer combinator; the next combinator comes from solver test-load material. `orthogonalize-deflate-specialization-edge-is-cross-cutter-not-combinator` (D5 caveat) — standing-answer note (the edge is a cross-link, not a combinator).
- `batch-16-frontier-signal-solver-test-load-is-next-combinator-material` (D5+D6 convergence) — the frontier moved off the saturated spine onto solver material; ASSESSED + GO this meta-phase (the batch-17 frontier is the solver-test-load continuation).

**c052 D6 / c053 / c054 solver-test-load findings (RESOLVED by the batch's own progress):**
- `electrostatic-outer-terminal-sweep-needs-solve-family-combinator` (D6) — action half SUPERSEDED by c053 D1 (2nd witness) → mined as `solve_family` c054 D1. `electrostatic-solver-probe-findings-are-single-witness-generality-unverified` (D6 gate) — DISCHARGED c053 D1 (magnetostatic 2-of-N).
- `capacitance-reduction-may-be-gram-variant-axis-extension` (D6) — CONFIRMED + LANDED c053 D2 (capacitance + inductance = `gram` on the `B`-weighted axis; 2 solver witnesses onto `book/src/L2/gram.md`). `gram-b-weighted-axis-cross-set-still-witness-less` / `gram-weighted-witness-real-path-conjugation-vacuous-here` (D2 caveats) — coverage-granularity / forward-compat notes; non-actionable.
- `solve-family-combinator-confirmed-2-of-n-mine-now` (c053 D1) — MINED c054 D1 (`solve_family` fixed-operator rough-in row at `book/src/L4/index.md`; L4 outer-driver rows 4→5). `solve-sweep-shared-operator-capture-invariant-needs-driven-transient-check` (c053 D1) — refined into the c054 superset/transient OQs (migrated to plan). `inductance-capacitance-reduction-now-2-witness-gram-hypothesis` (c053 D1) — SUPERSEDED by c053 D2 (convergent observation; D2 is the live thread).
- `fe-assemble-slug-collision-with-bilinear-form` (c053 D3) — RESOLVED-in-report (the FE-assembly thread uses `fe_assemble`, NOT `bilinear-form`; disambiguation recorded in `L1/index.md`).

**Migrated to the plan (CYCLE-055 / batch-17 — see Open — migrated to the plan):** `solve-family-general-operator-rhs-superset-probe` + `solve-family-transient-fold-vs-map-over-unification-guard` (c054 D1) → `map-solve-superset-probe`; `solver-postprocess-reduction-consumes-gram-distinct-dispatch` (c053 D2) → `gram-consuming-solver-postprocess-reduction`; `fe-assembly-thread-scope-and-sequencing` (c053 D3) + `fe-assemble-bc-elimination-siblings-deferred` (c054 D2) → `fe-assembly-thread-continuation`; `fe-assemble-libceed-boundary-classification` (c053 D3 / c054 D2) → RATIFIED `obstruction (opaque-library-ownership)`, enactment migrated; `fe-assemble-theme-reanchor-to-firm-lhs` + `fe-assemble-theme-addsuboperator-citation-drift` + `fe-assemble-l1-index-cohort-header-stale` (c054 D2) → the batch-17 lifter/layer-intro cleanup within `fe-assembly-thread-continuation`.

**Kept deferred (see Open — deferred / contingent → FE-assembly sub-spine siblings):** `fe-space-l1-form-untouched`, `discrete-linear-operator-interpolation-sibling`, `fe-assemble-weak-form-term-cohort-enumeration`, `fe-assemble-rectangular-and-multilevel-axes` (c053 D3 / c054 D2) + `c052-d4-preexisting-non-link-stale-refs-out-of-scope` (c052 D4).

### Closed by the batch-17 meta-phase (cycles 055/056/057; 2026-06-02)

The verbose cycle-055/056/057 per-report New-intake blocks (the D1–D8 c055 + D1/D2 c056 + D1/D2/D3/D4 c057 integration appends) are compacted here by the batch-17 meta-phase per the unification protocol. Full prose in git history (the "git history is the historical record" methodology); slug strings preserved as cross-reference anchors. The genuinely-still-open trigger-gated residuals are kept as the cohort one-liners in **Open — deferred / contingent → solve_family / fold_solve / solver-driver spine family** + the FE-assembly sub-spine siblings section; the actionable promotions are migrated to the plan (CYCLE-058 active head).

**Closed — resolved / answered / ratified this batch:**
- `solve-family-l3-no-entry-warrant-record` (c057 D1) — **closed-as-NO-ENTRY** (the negative warrant IS the recorded outcome). No `L3/solve_family` chapter, no `L3-L2/solve-family-step-fold-dissolution` theme: the firm L4>L3 `solve-family-map-dissolution` is the authoritative L3-form home; the family loop carries no `sequential-obstruction` so an L3 mirror is the 2026-06-01-redirect anti-mirror smell. Added to the plan STOP-PROPOSING negative list. *Re-open trigger:* a witnessed fixed-operator family sweep whose family loop carries a genuine cross-member sequential-obstruction (would be `fold_solve`, not `solve_family`).
- `fold-solve-solve-family-share-iterate-while-parent` (c057 D4) — **closed-RATIFIED** by the batch-17 meta-phase (decision 2): the strawman §3.7 `iterate_while` family IS the shared parent of `solve_family` (map) + `fold_solve` (fold); no third parent abstraction is authored (a new parent would add vocabulary without simplifying — the mine-and-strand anti-pattern). The map-vs-fold distinction is the load-bearing axis.
- `sweepadaptive-is-rom-fold-map-solve-stays-single-witness` (c057 D3) — **closed-resolved.** SweepAdaptive is a reduced-order-model FOLD (state-derived loop bound + state-accumulated basis), NOT a 2nd operator-varying-MAP witness; the online fast-sweep is a map over the FROZEN ROM (counting it = a scope-boundary violation). `map_solve` stays at 1 witness; SweepAdaptive advances `fold_solve` toward its gate. The `map_solve` keep-probing-then-retire decision is migrated to the plan (c058 #3).
- `solve-family-shape-classification-fold-vs-map-and-map-solve-superset-deferred` (c056 D1) — **closed-superseded** by the c057 D3 resolution + the migrated `map-solve-second-pipeline-probe` plan item. The three pipelines classify into driven=operator-varying-MAP (1 witness) / transient=state-threaded-FOLD / eigenmode=opaque-single-solve.
- `eliminate-essential-bc-l1-co-dispatch-sibling` (c055 D3) — **closed-resolved-by-D4** (`eliminate_essential_bc` landed firm c055 alongside `eliminate_rhs`; the operator+RHS BC-treatment pair complete).
- `eliminate-essential-bc-fe-assemble-sibling-fe_assemble-md-citation-drift` (c055 D4) + `fe-assemble-laplaceoperator-citation-drift-215-vs-216` (c055 D7) — **closed-resolved** by the c056 D3 lifter (`fe_assemble.md` `:215-217`→`:216-217` corrected at both `:147` + `:257`, verified on-disk this meta-phase).
- `l4-l3-fgmres-firmness-prose-vs-table-divergence` (c055 D7) — **closed-resolved-by-D8** (the L4-L3 table cells were the stale surface; D8 flipped krylov-step/gmres/fgmres cells firm + the consolidated tally 6→7; the L4-index prose was always correct; the two surfaces now agree).
- `index-table-status-cell-drifts-when-theme-file-promoted` (c055 D8) + `index-table-status-cell-drift-CLOSED-for-L3-L2-and-L2-L1-tables` (c056 D2) + `index-consistency-guard-prefer-lightweight-promotion-time-over-finalize-sweep` (c056 D2) — **closed-ADDRESSED.** Codified as friction-ledger `index-table-status-cell-drifts-when-theme-file-promoted` (recurrence-1, addressed) + the promotion-time guard role-spec edits (lifter + layer-intro-author). c056 D2 confirmed L3-L2/L2-L1 tables CLEAN (drift is promotion-in-place-specific). The L1/L1-L0 historical-residue audit is migrated to the plan (c058 #4).
- `solve-family-map-no-sequential-obstruction-vs-per-solve-fold` (c055 D2) — **closed-recorded-anchor** (a deliberate structural-contrast note, not an open question; consumed by the c057 D1 NO-ENTRY warrant + the firm dissolution theme's §"What does NOT change").
- `boundary-anchor-verdict-flavor` / `set-subvector-essential-dof-mask-concept-page` / `dof-set-concept-page` / `fe-operator-assemble-addsuboperator-range-vs-pinpoint-convention` / `l1-fe-sub-spine-vs-main-cohort-unified-firm-count-renumber` / `operatorcootocsr-palace-vs-libceed-ownership-fine-line` (c055 D4/D5/D6/D7) + `eliminate-rhs-mutation-rotation-l1-l0-theme-unauthored` / `eliminate-essential-bc-l1-l0-lowering-re-anchor` (c055 D3/D4) + `eliminate-rhs-mutation-rotation-sibling-stub-candidate` / `fe-assemble-weak-form-term-cohort-width-after-firm-flip` / `time-step-op-opaque-mfem-integrator-boundary` (c057 D2/D4) — **kept-deferred** (folded into the cohort one-liners under Open—deferred/contingent: `fe-bc-elimination-l1-l0-theme-split-vs-fold`, `fe-assembly-obstruction-verdict-vocabulary-and-boundary-precision`, `fe-assemble-anchor-form-and-count-presentation`, `fe-bc-dof-set-and-set-subvector-concept-pages`, `solve-family-firm-on-structure-vs-test-coverage`, `time-step-op-opaque-mfem-integrator-boundary`). `weak_form_term` cohort migrated to the plan (c058 #2).

### Closed by the batch-18 meta-phase (cycles 058/059/060; 2026-06-02)

The verbose cycle-058/059/060 per-report New-intake blocks (the c058 D1/D3/D4 + c059 D1/D2/D3 + c060 D1/D2 integration appends) are compacted here by the batch-18 meta-phase per the unification protocol. Full prose in git history (the "git history is the historical record" methodology); slug strings preserved as cross-reference anchors. The batch's headline: the two-combinator MAP/FOLD spine descended cleanly — `fold_solve` is firm at L4 + L4>L3 + L3 (`partial-obstruction`) + L3>L2, with the L2 floor correctly absent-by-warrant (anti-mirror at the floor); `map_solve` is recorded a permanent single-witness spine-coverage finding; all 5 pipelines are characterized vs the spine. The genuinely-still-open trigger-gated residuals are kept as the cohort one-liners in **Open — deferred / contingent → solve_family / fold_solve / solver-driver spine family**.

**Closed — resolved / confirmed / recorded this batch:**
- `map-solve-second-pipeline-probe-NON-DISCHARGE` (c058 D3) — **closed-as-FINDING (formal close).** All NON-driven pipelines probed for a 2nd operator-VARYING-MAP witness: none. Electrostatic (`electrostaticsolver.cpp:36` `SetOperators(*K,*K)` BEFORE the terminal loop `:60`) + magnetostatic (`magnetostaticsolver.cpp:36` BEFORE the surface-current loop `:65`) are FIXED-operator MAP members of `solve_family`; eigenmode ksp is a single opaque SLEPc EPS solve; transient is a state-threaded FOLD. The driven `Sweep` (`drivensolver.cpp:180` `SetOperators` INSIDE the ω-loop) is the SOLE operator-varying-MAP witness. **`map_solve` is recorded a PERMANENT single-witness spine-coverage finding** — no `book/src/L4/map_solve.md` authored (consistent with batch-17 meta decision 3 + `disciplined-cross-pipeline-combinator-mining-gate` step 1); `solve_family` stays the firm cross-pipeline combinator with the driven case riding it as the operator-varying scope-boundary superset annotation. *Re-open trigger:* a genuine 2nd operator-varying-MAP witness from a NON-driven pipeline (none in Palace's current 5-pipeline feature set).
- `l1-l1l0-index-status-cell-staleness-audit-CONFIRM-CLEAN` (c058 D4) — **closed-CONFIRM-CLEAN.** 68/68 status cells in `book/src/L1/index.md` (36) + `book/src/L1-L0/index.md` (32) agree with each linked chapter's `## Status` line (mirrors c056 D2's 16/16 on L3-L2/L2-L1). Confirms the drift class is promotion-in-place-specific historical residue that did NOT accumulate in L1/L1-L0. Closes the L1/L1-L0 half (the open half) of friction `index-table-status-cell-drifts-when-theme-file-promoted` → friction status flips to `resolved` this meta-phase.
- `obstruction-theme-index-cell-umbrella-vs-rough-in-obstruction-sub-tier-wording` (c058 D4) — **closed-NO-ACTION.** The `minres-iteration`/`bicgstab-iteration` L1-L0 index cells say `obstruction` (umbrella) vs chapter `rough-in — sketched as obstruction` / `rough-in (obstruction)` — a MATCH under the established coarse-cell/fine-chapter convention. The only variance is the two chapters phrasing the sub-tier differently from each other; not worth a uniform convention change. No-action (the coarse-cell convention is intentional).
- `eigenmode-outer-machinery-SPINE-COMPLETE-no-combinator-witness` (c059 D3) — **closed-SPINE-COMPLETE (formal close).** The eigenmode driver OUTER machinery (`eigensolver.cpp:32-477`) yields NO new cleanly-describable spine vocabulary above firm `L3/eigsolve`/`L4/eigsolve`: one opaque `eigen->Solve()` (`:367`, not a family map / not a state-fold) + a post-processing READOUT map (`:425-471`) over the converged eigenpair set (all constituents either solver-specific scalar maps or already-firm shared ops — `apply_linop(Curl)` B-recovery, `PostOperator`/`ErrorIndicator` pipeline-generic post-processing). Per the mining-gate step 2, eigenmode is OUT of the family/fold-solve domain. Closes the `solve_family` §Status "eigenmode unprobed" item + the `fold_solve` implicit eigenmode-coverage gap. *Re-open trigger:* a Palace re-architecture exposing an eigenmode-internal solve family / state-threaded eigen-march (none today).
- `eigenmode-hybrid-two-phase-refine-single-witness-refine_solve-candidate` (c059 D3) — **closed-as-FINDING (NON-ACTIONABLE).** The eigenmode HYBRID nonlinear path (`eigensolver.cpp:377-406`) runs a SECOND solve via `QuasiNewtonSolver` (`std::move(eigen)` + `num_conv` seed `:379-381`; second `Solve()` `:404`) — a sequential "solve → refine-with-second-solver" handoff. Single intra-pipeline witness, no 2nd-pipeline candidate → spine-coverage finding, not a mineable combinator; structurally it is two sequential applications of the SAME `EigenvalueSolver::Solve` cap (`QuasiNewtonSolver` is another `EigenvalueSolver`), already cap-covered by `L4/eigsolve` applied twice with a seed-handoff. **No `refine_solve` combinator warranted.** *Re-open trigger:* a genuine 2nd-pipeline "solve → refine-with-second-solver" witness (none today).
- `fold-solve-l3-entry-vs-dissolution-home` (opened c058 D1) — **closed-RESOLVED (verdict L3-ENTRY, c059 D1).** Standalone `book/src/L3/fold_solve.md` (`partial-obstruction`) + L3>L2 `book/src/L3-L2/fold-solve-time-step-body.md` landed. The decisive distinction from `solve_family`'s NO-ENTRY (c057): `fold_solve`'s time-sweep loop carries TWO genuine obstructions — a carry-threading `sequential-obstruction` (step n's in-place write to `sol` IS step n+1's input) + an opaque-library per-step leaf (`ode->Step`, `timeoperator.cpp:410`) — the `chebyshev`/`eigsolve` body-lifts-loop-doesn't shape, so the L3 form earns a standing iteration-rotation home.
- `fold-solve-l4-index-vocabulary-cohort-firmness-split-refresh` (opened c058 D1) — **closed-RESOLVED (c059 D2).** `book/src/L4/index.md` prose refreshed: §Vocabulary-cohort count 6→7 firm + the batch-17 two-combinator MAP/FOLD framing; `fold_solve` firm bullet added; §Active-frontier thread-opener re-stated firm with the now-live `../L3/fold_solve.md` link. Count-owner discipline honored (firm count from chapter `## Status`, not index cells; the already-firm `:82` TABLE row not re-touched).
- `l3-index-sixth-obstruction-profile-shape-f-combined-carry-threading-opaque-per-step` (opened c059 D1) — **closed-RESOLVED (c060 D1).** `book/src/L3/index.md:15` §Semantics-overlay spectrum prose synced to the authoritative `:64` tally: "Five → Six" non-trivial-obstruction shapes (new shape (f): combined carry-threading `sequential-obstruction` + opaque-per-step-leaf on a Palace-authored L3-rendered sweep), "three → four" `partial-obstruction`. The `:64` tally (already current at 17 firm + 4 `partial-obstruction`) was NOT touched; the two loci now read consistently.
- `fold-solve-l2-floor-no-warrant-descent-complete` (c060 D2) — **closed-RESOLVED-BY-WARRANT (formal close; descent complete, NO L2 floor owed).** `book/src/L2/fold_solve.md` absence is correct by warrant, NOT a gap: `fold_solve`'s per-step body is the opaque MFEM `ode->Step` integrator leaf (`timeoperator.cpp:410`, a thin forwarder) with NO L2 composition — unlike `eigsolve` whose per-step body opens to `apply_linop ▷ ksp_solve`. The substantive L3>L2 content is the **outer-sweep erasure** (carry-threaded iteration → an L2 fold-by-role modeled on `L2/eigsolve.md`'s RHS shape), NOT a body-composition rotation. The warrant is consistent across all three places (the `L3/fold_solve.md` frontmatter + §Downward/§Dependencies/§"L3 vs L2 distinction"; the `L3-L2/fold-solve-time-step-body.md` §"L2 form (RHS)"/§"opaque per-step leaf"/§"What this lowering does NOT cover"/§Verified-against; and the on-disk ABSENT `L2/fold_solve.md`). Anti-mirror discipline at the L2 floor (redirect §1d smell): a degenerate `L2/fold_solve` mirror would be the smell; absence-by-warrant is correct. **Do NOT author `book/src/L2/fold_solve.md`** (added to the plan STOP-PROPOSING negative list). *Re-open trigger:* a witnessed `fold_solve` per-step body that decomposes into visible L2 primitives (none — the integrator leaf is opaque-library-owned).

**Kept deferred / contingent (see Open — deferred / contingent → solve_family / fold_solve / solver-driver spine family):**
- `fold-solve-greedy-schedule-source-generalization` (c058 D1) — the driven-PROM SweepAdaptive state-generated-schedule form is recorded as the `schedule-source` VARIANT AXIS of firm `L4/fold_solve.md` (ONE combinator, not a distinct entry). Whether the greedy form warrants a dedicated chapter is a size-judgment deferred to a downstream PROM consumer pull. The no-L2-floor warrant carries to this superset (same opaque per-step leaf, `drivensolver.cpp:389`). *Trigger:* a batch-19+ downstream PROM consumer / a size-judgment dispatch.
- `fold-solve-time-step-body-slug-underdescribes-outer-sweep-erasure-content` (c059 D1) + `fold-solve-time-step-body-sole-act-phrasing-cosmetic-residue` (c060 D2) — two LOW-fan-out cosmetic residues on `book/src/L3-L2/fold-solve-time-step-body.md`: (a) the slug describes "per-step **body** lowers to L2" but the substantive content is the outer-sweep erasure (slug-stability + planner canonical-slug authority argue against an in-flight rename); (b) `:82` calls `TimeOperator::Step` "sole act `ode->Step`" but `:409`/`:412` also save/restore `dt` (a parameter-preservation guard; warrant unaffected — cosmetic precision nit only). *Trigger:* a future low-priority lifter touch on that theme (co-schedulable, opportunistic) — NOT a standalone dispatch (in-artifact text, no measurable fan-out).
- `l3-index-working-notes-stale-snapshot-compaction-candidate` (re-noted c060 D1) — the `book/src/L3/index.md:60`/`:62`/`:63` §Working-Notes superseded-snapshot bullets are historical per-cycle landing narratives explicitly labeled superseded; standing compaction candidate (discharged-once c040 D2). *Trigger:* a layer-intro-author L3-index refresh that opts to compact the Working-Notes.

### cycle-061 New-intake (append-only until the batch-19 meta-phase)

- `driven-affine-frequency-operator-as-operator-valued-linear-combination` (c061 D3 cross-layer-cross-cutter, `reports/2026-06-02T075145Z-cross-layer-cross-cutter-driven-transient-outer-machinery-probe/CYCLE.md`) — **LICENSE-FUTURE (batch-19, low-priority, pull-gated).** The driven per-ω operator assembly `A = (1·K + iω·C − ω²·M + A2)` (`drivensolver.cpp:176-177`) is a **scalar-weighted sum of fixed operators**: `GetSystemMatrix` (`spaceoperator.cpp:522-528`) ≡ `BuildParSumOperator({a0,a1,a2,1},{K,C,M,A2})` whose body (`rap.cpp:765-786`) is `sum->AddOperator(ops[i], coeff[i])` for `coeff[i]!=0`. This is the **operator-domain image of the firm tensor-domain `linear_combination` fold** (`L3/linear_combination.md:34-37`) — `{K,C,M}` assembled ONCE before the loop (`drivensolver.cpp:92-94`), only the scalar weights ω-vary → an **affine-in-ω fixed-basis operator family**. Disposition: land `assemble_frequency_operator` as a **driven single-pipeline specialization** that re-expresses THROUGH an **operand-category-extended `linear_combination`** (variant axis `tensor-operand | operator-operand`), replace-and-propagate NOT a mirrored fold (2026-06-01 anti-mirror). **Witness count: the *fold* is firm-by-extension (tensor-operand BLAS-1 cohort); the *driven specialization* is single-pipeline by design** (transient bakes excitation into the captured op; electro/magnetostatic capture a single fixed `K`; eigenmode is opaque `eigen->Solve`) — per `disciplined-cross-pipeline-combinator-mining-gate` step 1, record as single-witness, do NOT route a 2nd-pipeline discharge probe (none will come). Also SHARPENS the `map_solve` scope-boundary (`solve_family.md:65,90,163`): the per-ω operator rebuild that scopes driven out of `solve_family` IS this affine-operator-family evaluation. Caveat: `A2` is an ω-dependent operand (not just coefficient, `drivensolver.cpp:174`) — the literal `BuildParSumOperator` shape absorbs it as a coefficient-1 4th basis term; the "affine family" abstraction holds modulo the `A2` ω-dependence (a harvester decides absorb-vs-note-separately). *Trigger:* a batch-19 FE-assembly / `weak_form_term` cohort dispatch that pulls on operator-sum vocabulary (co-schedulable), OR a standalone low-priority harvester on the driven `A`-assembly L1>L0.
- `driven-transient-outer-machinery-spine-complete-except-affine-operator-assembly` (c061 D3) — **RECORDED (spine-complete-or-solver-specific).** Beyond the one candidate above, the driven/transient OUTER machinery yields NO new shared spine vocabulary: (Region 2) driven S-parameter/port postprocessing + `B = -1/(iω)Curl·E` recovery (`drivensolver.cpp:202-216`) is solver-specific complex-frequency physics, the per-element `map_solve` consumer already accounted-for (`solve_family.md:111`); (Region 3) transient coefficient/excitation setup (`transientsolver.cpp:30-36`) is already-spine-covered — `delta_t`/`n_step` = the `fold_solve` fixed-`[Time]` schedule (`fold_solve.md:63`), `dJdt_coef` captured ONCE into the `TimeOperator` op-stratum (`fold_solve.md:61`), `J_coef(t)` a per-step postprocess input only (`:104`); (Region 4) transient post-march `GetE`/`GetB` collection (`transientsolver.cpp:98-99`) is the `fold_solve` consumed trajectory (`fold_solve.md:64,90`). Spine-finding: what these regions can't say in shared vocabulary is genuinely solver-specific, not a missing combinator. *No re-open trigger* (characterization complete; subsumes the driven/transient half of the batch-17 3rd-probe item).

#### weak-form-term-concept-page-reconsideration-on-second-consumer
opened_at: cycle-061
opened_by: harvester
status: open (deferred / contingent)

`weak_form_term` (firm c061 D1, `book/src/L1/weak_form_term.md`) authored **no concept page** — it has exactly one downstream consumer (`fe_assemble`) at L1, below the ≥2-consumer / genuine-cross-cutting bar for a `book/src/concepts/` page. **Reconsider promoting** the `(coefficient, differential-operator)` factorization + the differential-operator de-Rham-position table (`Gradient`/H1, `Identity`/any, `Curl`/H(curl), `Divergence`/H(div)) to a concept page **if a future pipeline pull adds a second independent consumer** of the term abstraction — e.g. an L2 assembly combinator that folds terms, or a mass-term pull that references the differential-operator axis from a distinct site. Two named pending-pull sibling variants on the differential-operator axis are the likeliest second-consumer triggers: **`Identity`/mass** (`Mass`/`VectorFEMassIntegrator`; pervasive across eigenmode/driven/transient/ROM pipelines — the most-likely next pull; authored as a specialization note under `weak_form_term` per the redirect's combinator-primary discipline, NOT a new standalone entry) and **`Divergence`/div-div** (`DivDivIntegrator`; **no in-scope solver-K witness** — codemap search over `palace/models/*.cpp` returns no `AddDomainIntegrator<DivDivIntegrator>`; recorded as a possible spine-coverage finding, not a gap to fill speculatively). *Trigger:* a batch-19+ pipeline pull that materializes a second `weak_form_term` consumer (a mass-term solver-K/M witness, or an L2 term-folding combinator). Not parked — a plan candidate if the second consumer materializes.

#### l1-index-fe-assembly-sub-spine-count-prose-refresh-3-to-4
opened_at: cycle-061
opened_by: harvester
status: open (deferred / contingent — layer-intro-author domain)

The `book/src/L1/index.md` §"Firm (FE-assembly sub-spine — 3; opened cycle-053, completed cycle-055)" subsection-header **count prose** reads "3" members (`fe_assemble` + `eliminate_essential_bc` + `eliminate_rhs`); `weak_form_term` (firm c061 D1) is a **4th** sub-spine member (the term abstraction the fold quantifies over). Likewise the §Vocabulary-cohort header **grand-total tally** ("29 firm grand total incl. the FE-assembly sub-spine" / "bringing the L1 firm grand total to **29**") should refresh **29→30**. The c061 D1 harvester registered its own dep-map row + its own cohort bullet (the per-operator dual-registration it owns) but flagged the **consolidated subsection-header count prose** ("FE-assembly sub-spine — 3" → "4") + the **§Vocabulary-cohort grand-total** ("29" → "30") for the **layer-intro-author** — these live in the §Vocabulary-cohort header prose (layer-intro-author's domain), not the per-operator bullet the harvester owns. *Trigger:* a layer-intro-author L1-index refresh (co-schedulable / opportunistic — in-artifact count prose, low fan-out).

### cycle-062 New-intake (append-only until the batch-19 meta-phase)

#### driven-affine-frequency-operator-license-ENACTED-c062
opened_at: cycle-062
opened_by: integrator-per-report
status: closed-RESOLVED-BY-LANDING

The c061 D3 LICENSE-FUTURE candidate `driven-affine-frequency-operator-as-operator-valued-linear-combination` (above, c061 New-intake) is **ENACTED this cycle**: harvester:2026-06-02T083220Z (cycle-062 D3) landed `book/src/L1/assemble_frequency_operator.md` (firm) + `book/src/L1-L0/assemble-frequency-operator-rotation.md` (firm L1>L0) as the **operator-operand specialization of `linear_combination`** (the operand-category variant axis `tensor-operand | operator-operand` was extended onto `book/src/L2/linear_combination.md` + `book/src/L3/linear_combination.md` — replace-and-propagate, NOT a mirrored fold). The three caveats from the c061 license are settled as **stated facts in the firm entry, not open questions**: (a) **affine modulo A2** — recorded as the `parameter-dependent-operand` stationarity axis-point + the affine-as-a-whole non-law (A2 = ω-dependent operand carrying coeff 1); (b) **single-pipeline-by-design** — landed as a single-pipeline specialization with NO 2nd-pipeline discharge owed (the fold generality comes from the tensor-operand BLAS-1 cohort); (c) **coeff-type overload** — the driven `<ComplexOperator>` path resolves to the complex-coeff `BuildParSumOperator` overload (`rap.cpp:833`; declared `rap.hpp:238`; instantiations `rap.cpp:971-977`), noted in the Evidence list (both overloads share the identical fold shape, every structural/algebraic claim holds verbatim). The c061 intake item above may be marked resolved at the batch-19 meta-phase unify pass. *No re-open trigger* (landing complete).

#### assemble-frequency-operator-map-solve-scope-boundary-cross-ref-refresh
opened_at: cycle-062
opened_by: integrator-per-report
status: open (deferred / contingent — cross-layer-cross-cutter / layer-intro-author domain)

The firm `book/src/L1/assemble_frequency_operator.md` (c062 D3) NAMES the driven `map_solve_over_(operator,rhs)_family` superset's per-element operator as an affine-in-ω operator-operand `linear_combination`. `book/src/L4/solve_family.md` already records the driven scope-out / `map_solve` scope boundary (`:65,90,163` per the c061 D3 finding) but does NOT yet cite `assemble_frequency_operator` by name as the per-element operator that rebuild produces. An in-place cross-reference refresh on `solve_family.md` (to cite `assemble_frequency_operator` as the named per-ω operator of the driven `map_solve` superset) would tighten the boundary narrative — explicitly flagged out-of-scope by the c062 D3 one-operator dispatch. *Trigger:* a future cross-layer-cross-cutter pass on the driven `solve_family` scope boundary, OR a layer-intro-author `solve_family` refresh (co-schedulable / opportunistic — in-artifact cross-ref, low fan-out).

#### l1-index-fe-assembly-sub-spine-count-prose-refresh-3-to-4 — RESOLVED-BY-LANDING-c062-D2
opened_at: cycle-061
opened_by: integrator-per-report
status: closed-RESOLVED-BY-LANDING

Resolution of the c061 New-intake OQ `l1-index-fe-assembly-sub-spine-count-prose-refresh-3-to-4` (above). The cycle-062 D2 layer-intro-author count-owner dispatch (layer-intro-author:2026-06-02T083220Z) **landed both header-prose refreshes** on `book/src/L1/index.md`: (1) the §Vocabulary-cohort grand-total header (`26 main; 29 firm grand total` → `27 main; 31 firm grand total`, folding in c061's `weak_form_term` AND c062 D3's new firm `assemble_frequency_operator`), and (2) the §"Firm (FE-assembly sub-spine)" subsection header (`— 3` → `— 4`, adding the firm `weak_form_term` term-type as the 4th sub-spine member). The OQ's stated target (`29→30` for the c061 weak_form_term lag) is **subsumed** — the c062 refresh went directly to **31** because D3's `assemble_frequency_operator` landed the same cycle (29→30→31 collapsed into one refresh). Arithmetic independently re-verified by the c062 D2 critic via both routes (27 main + 4 FE-assembly = 31; 30 in-table dep-map firm rows after D3 + 1 off-table `fe_assemble` = 31). *No re-open trigger* (count-prose now current with the firm tally).

#### l1-index-fe-assemble-needs-dep-map-row-for-self-summing-table
opened_at: cycle-062
opened_by: integrator-per-report
status: open (deferred / contingent — layer-intro-author / harvester domain; clean future fix)

Surfaced by the c062 D2 count-owner dispatch (carried as its §Open-questions caveat). `book/src/L1/fe_assemble.md` is **firm** but carries **no dep-map row** in `book/src/L1/index.md` — it is narrated only in the §"Firm (FE-assembly sub-spine)" subsection prose. Consequence: the in-table firm-row count (30 after c062 D3) does NOT self-sum to the §Vocabulary-cohort grand total (31); the +1 reconciliation (`30 dep-map firm rows + 1 off-table fe_assemble = 31`) is carried in the header prose narrative instead. The **clean fix** to make the dep-map table self-summing (table-firm-row-count == grand total) is to add a `fe_assemble` dep-map row — out of the c062 D2 count-owner scope (the count-owner refreshes the consolidated tally + cohort narrative prose, not the addition of a new table row). *Trigger:* a future layer-intro-author L1-index dep-map pass, OR a harvester touching `fe_assemble` (co-schedulable / opportunistic — in-artifact table hygiene, low fan-out; cosmetic, the grand total is already correct).
