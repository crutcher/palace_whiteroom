# Cycle-118 resume notes — SESSION RESTART REQUIRED before c118

**Written by:** the SCHEDULED batch-37 meta-phase (post-cycle-117 finalize; aggregates cycles 115/116/117), 2026-06-06.

## Why a restart is required

This meta-phase edited **2 `.claude/agents/*` role-specs**. Custom agent definitions are loaded at session start, so the parent orchestrator must **restart the Claude Code session before cycle-118** for the new definitions to take effect.

## Role-spec changes (the restart-triggering edits)

Both edits codify ONE new discipline (friction-ledger `new-summary-kind-grouping-placeholder-link-duplicate-file-build-break`, GO this meta-phase) — the c117 finalize build-repair pattern moved from finalize-time to producer/per-report-time:

1. **`.claude/agents/integrator-per-report.md`** — new bullet (in the per-report safety-net auto-fix list, before the graded-resolution-ladder bullet): **a per-report integrator that opens a NEW SUMMARY by-kind grouping MUST create its navigational-container group-intro stub in the same landing** — never placeholder-link an existing page (mdBook treats a twice-linked file as a `Duplicate file` build break). Record `applied-discretionarily` rationale `group-intro-stub-created`.

2. **`.claude/agents/layer-intro-author.md`** — new bullet (in the §By-kind sub-chapter grouping section): **opening a NEW kind grouping ⇒ author its group-intro page (or a navigational-container stub) in the SAME landing**, never placeholder-link the Part `index.md`.

## What did NOT change

- **No new agent role** (no High-cascade authoring).
- **No cycle-structure change.**
- **No CLAUDE.md / memory edit** this meta-phase (the two new directives were already codified by the out-of-band meta-phase post-c115; this scheduled meta-phase only carries the standing-duty work + the batch-38 plan reshape).
- **No `book/` content/frontmatter change** beyond the two meta-phase-owned methodology mirrors (`goal-flow.md` batch-37 arc close, `resolution-ladder.md` RE9/RE10 note) — the `build_mesh` grounding edge is MIGRATED to the c118 plan (item-2), NOT enacted here (it is a `book/` content edit, an integrator/producer dispatch).

## Do NOT

- Do NOT emit a `/compact` step — the restart resets primary context (user directive 2026-05-29; the restart subsumes compaction).

## Batch-38 / cycle-118 frontier (from the reshaped `scaffolding/priorities.md` CYCLE-118 head)

The MESH→FE_SPACE SUBSTRATE LOWERING + GROUNDING campaign:
1. `mesh-fe-space-l1l0-theme-cohort` (LEAD, HIGH) — the 3 L1>L0 construction-rotation themes (`build-mesh`/`fe-space-hierarchy`/`interpolator`).
2. `build-mesh-lifecycle-grounding-edge` (HIGH) — the faithful `feature/lifecycle.L1 → L1/build_mesh` composes edge (§2f GROUND; grounds `build_mesh` off detritus).
3. `waveguide-mode-reduce-l4-verb-home` (MEDIUM) — promotes the waveguide-mode column rough-in→firm.
4. `record-definition-cohort-c117` + kind-grouping-fold (LOW/MEDIUM) — `Mesh`/`FiniteElementSpaceHierarchy`/`WaveguideModeTable` record homes + fold-vs-standalone for the Mesh-construction grouping.

Standing: RE9/RE10 (`fe_space_hierarchy`/`interpolator`) baseline-excepted; RE1-RE8 unchanged.
