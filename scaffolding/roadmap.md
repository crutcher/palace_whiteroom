# Roadmap

A coarse map of what this project intends to cover, what's currently in flight, and what's done. The roadmap is **deliberately abstract**: a few dozen items, not a granular task list. It exists so the README's *Relative Progress* section can report proportional coverage with real denominators.

**Relationship to the plan** (user directive 2026-05-28): the granular, prioritized work backlog lives in `scaffolding/priorities.md` (**the plan**) — that is where open-questions/friction migrate and where the planner picks dispatches. This roadmap is the *coverage/goals map* plus the **fan-out impact model** (see §Intermediate-tier `impact_score`, and §How proportional coverage is computed) that **ranks** the plan's backlog by downstream impact. Use this file to judge how much a candidate unblocks; use the plan to decide what to dispatch.

**Lifecycle.**

- The Meta-Critic reviews this file during every meta-cycle. Items move between `not-started` (`[ ]`), `in-progress` (`[~]`), and `done` (`[x]`) based on what landed in the window. New items are added as scope clarifies; items are not removed without a meta-review note.
- The README builder reads this file to compute the *Relative Progress* coverage report.
- Changes to this file are committed as part of the meta-cycle enactment (or as immediate follow-ups).

Status legend:

- `[x]` — landed; at least one slice at L4 (for solver/algorithm components) or the procedure/concept is in production (for methodology components).
- `[~]` — in flight; slice exists at L1/L2/L3 but not at L4, OR the methodology component exists but is being refined.
- `[ ]` — not started.

## Shared infrastructure (PRIORITY — user directive 2026-05-27)

Components that underlie all five solver pipelines. **Raised above per-solver pipelines** per user directive: shared infrastructure is the leverage point — every per-solver pipeline depends on it. Krylov + smoothers + projections + FE assembly all gate per-solver coverage. The remaining unstarted shared-infrastructure items (MINRES, BiCGStab, Householder QR, Jacobi, SGS, ILU, AMS, multigrid V-cycle, curl-curl projector, FE assembly, boundary conditions) should be prioritised in cycle-planner dispatch selection over individual per-solver work.

**Per the unimplemented-Palace-components policy (user directive 2026-05-27, see CLAUDE.md §Scope):** Shared Infra items that are enum-only stubs in Palace (MINRES + BiCGStab confirmed cycle-004; possibly others — see priority #13 discovery item) are NOT direct implementation targets. They land as L1>L0 obstruction themes; their literature-anchored L1 forms may inform higher-form abstraction (L2 combinators like `krylov-step`); speculative operators are promoted only when small AND simplifying higher-form semantics. Status notation: items marked `[stub]` are enum-only-with-MFEM_ABORT (documented obstruction); items marked `[?stub]` haven't been grepped yet; items with normal `[ ]` are believed implemented in Palace.

## Per-solver pipelines (5 solvers)

A solver pipeline is *covered* when its driver algorithm has at least one slice at L4 AND all its support primitives are at least at L2. None of the five pipelines is fully covered yet — the Krylov-only footprint is shared across all five and is largely done, but per-pipeline FE assembly + boundary conditions + post-processing are not yet touched.

- [ ] **Electrostatic** — Poisson-like (∇·(ε∇φ) = ρ); SPD; uses CG + (eventually) geometric multigrid. **Solver test-load probes cycle-052/053**: the electrostatic outer terminal-sweep exhibits a **fixed-operator parametric solve-sweep** (same `K`, re-solve over a family of RHS/terminal-excitations) — `electrostaticsolver.cpp:111-137`; its capacitance reduction is the `B`-weighted `gram` variant (landed as a witness on `book/src/L2/gram.md` cycle-053 D2, NOT a new operator).
- [ ] **Magnetostatic** — curl-curl (∇×(μ⁻¹∇×A) = J); uses GMRES + (eventually) AMS. **2nd-pipeline solve-sweep probe cycle-053 (D1) DISCHARGED the single-witness gate**: magnetostatic exhibits the SAME outer parametric solve-sweep shape (`magnetostaticsolver.cpp:110-152`), confirming the **fixed-operator solve-family combinator is mineable at 2-of-N** (electrostatic + magnetostatic) — the highest-fan-out batch-16 frontier item, queued for the cycle-054 combinator-miner dispatch. Its inductance reduction is the `B`-weighted `gram` variant (2nd `gram` solver witness, landed cycle-053 D2). **Scope caveat**: driven BREAKS shared-operator-capture (`drivensolver.cpp:176/:180` `SetOperators`-inside-loop) — the combinator is **fixed-operator-only**, with the general `map_solve_over_(operator,rhs)_family` as the superset for driven/transient.
- [ ] **Eigenmode** — generalized eigenvalue (∇×(μ⁻¹∇×E) = ω²εE); uses ARPACK/FEAST + shift-invert. **NLEPS interior atom landed firm cycle-021**: `book/src/L1/apply_nonlinear_pencil.md` (the `r = T(λ)·v` for `T(λ) = K + λC + λ²M + A2(λ)` residual-apply read from positive `QuasiNewtonSolver::GetResidualNorm` site; nonlinearity localised in the opaque `A2` closure — `apply_linop`-of-the-NEP-loop). **Cycle-022 NEP vocabulary buildup**: `nleps_deflated_residual` landed firm (`book/src/L1/nleps_deflated_residual.md` — the deflation extension of `apply_nonlinear_pencil` in the quasi-Newton loop, `r = T(λ)·(vv + X·(λI−H)⁻¹·vv₂)` / `r₂ = Xᴴ·vv`; `k=0` degenerates to `apply_nonlinear_pencil + nrm2`) + `lu_solve` landed firm (`book/src/L1/lu_solve.md` — small-dense `k×k` direct solve, the HIGH-fan-out Gram-coordinate primitive shared across eigensolver/ROM paths). 3 NLEPS L1 pieces still carried (deflated-solve → Jacobian → eigenvalue-correction; deflated-solve `nleps.cpp:504-537` is the next fan-out-ranked harvest, also the positive site firming the constructive `deflate` core). **Eigsolve prerequisite chain step 1 DONE cycle-022**: `book/src/L1/eigsolve.md` rough-in (test-coverage-bounded) → **firm** (law-confidence re-eval — the laws are positive-source syntactic identities, the cycle-009 convergence-semantics rough-in premise was over-stated; `apply_linop` firm-on-positive-structure precedent). The **L2 `eigsolve` entry is now UNBLOCKED** (chain step 2). **L3 `eigsolve` backfill STAYS BLOCKED** until the L2 entry exists (chain step 3; the linear-EVP has no Palace-authored kernel/driver pair — predicted `partial-obstruction`, the eigen-iteration is opaque-library-owned). **Cycles 023→025 — NEP-interior + eigsolve-chain COMPLETION**: cycle-023 landed `nleps_deflated_solve` firm (L1 16→17) + L2 `eigsolve` firm (chain step 2) + a materialized L3 `eigsolve` stub; cycle-024 landed `nleps_jacobian_action` + `nleps_eigenvalue_correction` firm (**L1 firm 17→19 — NEP-interior atom cohort COMPLETE 5/5**: `apply_nonlinear_pencil`/`nleps_deflated_residual`/`nleps_deflated_solve`/`nleps_jacobian_action`/`nleps_eigenvalue_correction`) + refined the L3 `eigsolve` stub→`partial-obstruction` (**chain step 3 DONE**; confirms the cycle-021 `l3-eigsolve-linear-evp-has-no-krylov-step-kernel-analog` prediction — eigen-iteration opaque-library-owned, SLEPc `EPSSolve` `slepc.cpp:694` / ARPACK `naupd` `arpack.cpp:318`). **Cycle-025 — both cohorts CLOSED**: the **NEP-interior L1>L0 lowering cohort is COMPLETE 5/5** (`nleps-jacobian-action-mutation-rotation` + `nleps-eigenvalue-correction-mutation-rotation` landed firm, joining apply-nonlinear-pencil/nleps-deflated-residual/nleps-deflated-solve mutation-rotations; the full per-step quasi-Newton chain is lowered L1>L0 end-to-end) AND the **eigsolve L1→L2→L3→L2>L1→concept chain is FULLY COMPLETE** (the firm L2>L1 `eigsolve-spectral-transform-composition` edge + the `concepts/eigsolve` navigational home both landed; the plan item `eigsolve-l2-l1-and-concept` is FULLY discharged). Remaining Eigenmode work shifts to the OUTER eigenpair-extraction / restart machinery + FE assembly, not the interior atoms.
- [ ] **Driven** (frequency-domain) — Helmholtz with complex shifts; uses GMRES with complex arithmetic. **Cycle-036 D1 landed firm `floquet_correction` L1 + `floquet-correction-mutation-rotation` L1>L0** (closes the c035 D3 coverage-gap): `book/src/L1/floquet-correction.md` is the sixth constructed-operator gate at L1 (after `ksp_solve`, `eigsolve`, `chebyshev-smoother`, `divfree-projector`, `jacobi-smoother`) and the **third firm instance** of `nested-constructed-operator-gate` (closure carries `F.ksp : Solver[F.M_RT]` with an inner `JacobiSmoother` preconditioner — gate transitively three-deep `floquet → ksp → jacobi-smoother`); 4 AddMult consumer sites (`palace/drivers/drivensolver.cpp:212,336,468` + `palace/drivers/eigensolver.cpp:454`); firm-on-positive-structure; structurally isomorphic to but strictly thinner than `divfree-projector` (no boundary-zeroing, no gradient correction, no empty-boundary nullspace pin). The L1>L0 theme decomposes into 4 sub-patterns A/B/C/D (Mult+AddMult surface + RHS-buffer-recycling closure construction + element-type scope-out). Driven-solver Floquet-periodic apply surface now firm-lowered end-to-end at L1/L1>L0. **Cycle-069: the driven ASSEMBLE-half reaches L4** — `assemble_frequency_operator` PROMOTED FIRM L4 (`book/src/L4/assemble_frequency_operator.md`, the per-ω system-operator weighted-sum assembly `A(ω) = K + iω·C − ω²·M` through `L4/linear_combination`'s operator-operand corner). The driven SOLVE-half (the `map_solve`-over-(operator,rhs)-family — driven BREAKS shared-operator-capture per the line-30 scope caveat) remains the **one open pipeline-half**, routed to the batch-21 meta-phase (OQ `driven-solve-half-l4-completeness-vs-map-solve-single-witness-stop`). **Cycle-070 (batch-22 position 1/3, LEAD): the DRIVEN SOLVE-half reaches L4 — the LAST OPEN pipeline-half is CLOSED; the whole assemble+solve deliverable now reaches L4 across all 5 pipelines.** **D1 (harvester): `frequency_sweep` PROMOTED FIRM L4** (`book/src/L4/frequency_sweep.md` — the driven pipeline's solve-half single-witness L4 form, the **operator-VARYING per-ω sweep** that re-solves through the firm `L4/assemble_frequency_operator` at each swept frequency; the batch-21-meta-phase-decided LIFT [verdict (a)] — authors the driven feature's OWN single-witness L4 form while the shared `map_solve` generalization stays BARRED at the ≥2-witness STOP; the completeness directive governs upward-to-L4, orthogonal to the shared-combinator-mining stop). **D2 (abstractor): `frequency-sweep-dissolution` LANDED FIRM L4>L3** (`book/src/L4-L3/frequency-sweep-dissolution.md` — the coupled L4>L3 lowering of the operator-varying sweep; L4>L3 firm-theme tally 9→10). **L4 firm 13→14** (`frequency_sweep`); **L4>L3 firm themes 9→10** (`frequency-sweep-dissolution`). **THE ASSEMBLE+SOLVE DELIVERABLE NOW REACHES L4 ACROSS ALL 5 PIPELINES** (the driven solve-half — the one remaining gap after c069's assemble-half close — is now closed). D4/D5 (lifters): `L3/dot`+`L3/nrm2` LEAF stale-no-L4→live-link re-anchor (ENACTS OQ `l3-dot-nrm2-stale-no-l4-entry-lines-need-reanchor`) + the `black-box-vs-accelerated-kernels` concept page `fe_assemble` L1→L4 link upgrade (ENACTS OQ `l4-fe-assemble-absent-forward-ref-for-blackbox-kernel-page`). D3 (layer-intro-author): the NEW feature-surface spine OPENED — see §Feature-surface spine (entry points) below.
- [ ] **Transient** (time-domain) — ODE integrator over time stepping.

## Feature-surface spine (entry points) — added cycle-070 (USER DIRECTIVE 2026-06-02)

A **TOP-DOWN composition-root spine** parallel to the bottom-up vocabulary spine: Palace's high-level **entry-point features** (what Palace is *written for*) get dedicated **feature-surface chapters** (a NEW chapter kind — composition-root, presented per-feature at L4 + L1 + L0; L2/L3 only where a decomposition reshapes). Run in PARALLEL with the vocabulary spine (co-equal standing goal, interleaved by fan-out — NOT a replacement frontier). Full def: project memory `project_feature_surface_spine`; the new mdBook Part is `book/src/feature/`.

**Feature set (all four cohorts):**
- [x] **(1) 5 simulation drivers — DRIVER-COLUMN HALF COMPLETE 5-of-5 (cycle-073).** electrostatic / magnetostatic / eigenmode / driven / transient. **Cycle-073 (batch-23 position 1/3; D2/D3/D4 layer-intro-author): the remaining 3 per-driver columns LANDED — `driven` + `transient` + `eigenmode`, each at L4/L1/L0** (`book/src/feature/{driven,transient,eigenmode}.{L4,L1,L0}.md`, 9 new files, all status `seed`): the 5-driver leaf-column set is now COMPLETE. **D2 (driven, COHORT OWNER):** config → `fe_assemble`/`assemble_frequency_operator` → `frequency_sweep` (the firm operator-varying solve-half) → S-parameter reduction (plain-text forward-ref `sparameter_reduce`/`sparameter_response` — the un-authored output-product constituent, keeps the column `seed`) → S-params out; D2 SOLE-owned the `feature/index.md` matrix (+3 rows, leaf drivers grouped before lifecycle ROOT) + the `SUMMARY.md` `# Feature surfaces` block rows for all three new columns (within-column high→low, the deliberate non-alpha exception). **D3 (transient, FOLD-pipeline):** the second-order-in-time wave system; the single `fe_assemble` matrix down-link stands for a **thrice-applied** K/C/M assemble-fold. **D4 (eigenmode):** the minimal `assemble ×3 ▷ eigsolve ▷ readout-map` (the single-black-box-kernel shape, NO `solve_family`/`fold_solve`); the `eigenfrequency-qfactor` output-product down-link stays plain-text forward-ref (keeps the column `seed` on that one non-firm constituent; both solve-side constituents ARE firm). **All 5 driver columns now registered in both `feature/index.md` matrix and `SUMMARY.md` `# Feature surfaces` Part with all cross-links live.** **Cycle-070 (D3 layer-intro-author): the spine OPENED with its FIRST EXEMPLAR column** — the NEW top-level Part `# Feature surfaces — entry points` (`book/src/feature/index.md` overview + feature×level matrix) + the **electrostatic** exemplar at L4/L1/L0 (`book/src/feature/electrostatic.{L4,L1,L0}.md`): the composition root config → `fe_assemble` (assemble K once) → `solve_family` (fixed-operator corner) → capacitance reduction (Vⱼᵀ K Vᵢ) → capacitance-out. status `seed (exemplar)` — first instance of the composition-root chapter kind, authored ahead of role-spec codification (batch-22 meta-phase codifies; two standing OQs `feature-surface-kind-adapted-check-codification` + `feature-surface-part-path-layout-and-within-column-level-ordering-ratification` carried for the meta-phase). Stage-3 (capacitance reduction) rests on rough-in L1 primitives (`matrix-weighted-norm` test-coverage-bounded + `bilinear-form`), correct for a seed. **Cycle-072 (batch-22 position 3/3, D1 layer-intro-author): the 2nd per-driver exemplar LANDED — `magnetostatic` at L4/L1/L0** (`book/src/feature/magnetostatic.{L4,L1,L0}.md`, status `seed`): the composition root config → `fe_assemble` → `solve_family` (fixed-operator corner, the **2nd `solve_family` witness**) → operator-weighted-Gram inductance reduction → **inductance** output. D1 SOLE-owned the `feature/index.md` matrix + the `SUMMARY.md` `# Feature surfaces` block rows for BOTH new columns (magnetostatic + lifecycle). Surfaces the **`gram_reduce` 2-witness combinator-mine candidate** (electrostatic + magnetostatic share the operator-weighted-Gram reduction Vⱼᵀ·Op·Vᵢ — OQ `shared-l4-energy-form-reduction-combinator-gram-reduce-two-witness-mine`). Remaining 3 driver columns (eigenmode/driven/transient) carried as the parallel frontier, plain-text forward-refs from the lifecycle root.
- [~] **(2) top-level lifecycle** — `main`→`BaseSolver`: config→mesh→assemble→solve→postprocess→output. **Cycle-072 (D2 layer-intro-author): the lifecycle-root META-FEATURE column LANDED at L4/L1/L0** (`book/src/feature/lifecycle.{L4,L1,L0}.md`, status `seed`; **c074 D5 normalized the token from `seed (composition-root)` → bare `seed` per the batch-22 uniform token; c074 D4 live-linked all 5 per-driver branches** — 5-branch navigation now complete): the spine **ROOT** that all 5 per-driver columns hang off (the first **meta-feature** — its constituents are other feature columns, not only vocabulary). Composition: config→mesh-build → `dispatch (problem_type)` selecting ONE per-driver column → the **AMR estimate-mark-refine outer fold** expressed as `fold_solve`'s **state-generated `schedule-source`** form (the 2nd state-generated `fold_solve` witness, after the driven-PROM SweepAdaptive; carry `{mesh, indicators, ntdof, err}`; `BaseSolver::SolveEstimateMarkRefine`, `palace/drivers/basesolver.cpp:153-276`). Composes DOWN to the per-driver columns (electrostatic + magnetostatic live-linked on disk; eigenmode/driven/transient plain-text). Surfaces the `boundarymode-is-sixth-problemtype-branch` reconcile OQ (the `switch` has 6 branches incl. `BOUNDARYMODE`) + the `fold_solve` state-generated-schedule-source 2nd-witness lifter OQ.
- [x] **(3) output/postprocess products — COHORT COMPLETE 5-of-5 (cycle-078).** S-params / capacitance / inductance / eigenfreq+Q / energy-fields. **BATCH-26 NET (cycles 082/083/084): TWO output-product reduce verbs PROMOTED to FIRM — L4 firm 14→16 main / 18→20 grand.** **Cycle-082 (batch-26 position 1): `eigenfreq_qfactor_reduce` PROMOTED `rough-in (test-coverage-bounded)` → `firm`** (the FIRST reduce-verb firm promotion) via the firm-on-positive-structure / syntactic-identity escape — its laws are syntactic identities over the two now-firm folded per-mode primitives (`participation_ratio` c077 + `eigenvalue-untransform` c080) + positive assembly source; discharged gate-(b) OQ `eigenfreq-qfactor-reduce-firm-needs-assembly-test` BY-AUDIT (no out-of-scope assembly test needed). The cross-layer-cross-cutter survey CONFIRMED the 5-driver→L4 picture (all 5 drivers + boundary-mode + lifecycle reach L4 on BOTH assemble-half AND solve-half; columns sit at `seed` ONLY because their stage-3 output-product reductions are rough-in/seed). **Cycle-083 (batch-26 position 2): `sparameter_reduce` PROMOTED `rough-in (test-coverage-bounded)` → `firm`** (the SECOND reduce-verb firm promotion, SAME escape) — laws are syntactic identities / closed-form arithmetic over the now-firm folded projection primitive `port_projection` (c077) + the positive `MeasureSParameter` assembly source; resolved the A1 half of cohort OQ `output-product-reduce-verb-test-coverage-bounded-promotion-route` BY-AUDIT. **The in-scope reduce-verb law-confidence route is now EXHAUSTED for the two verbs whose folded primitives are ALL firm (A1/A2 both firm); A3 `gram_reduce` + A4 `domain_energy_reduce` remain foundation-gated behind the `matrix-weighted-norm` √-entry-point ~30-file cascade (batch-25 NO-GO; the convergent blocker for the whole remaining reduce-verb tail — a batch-26 meta-phase weigh).** Both promoted-verb columns (`sparameters`, `eigenfrequency-qfactor`) STAY `seed` — the per-column promotion RULE is held verbatim pending the batch-26 meta-phase USER DIRECTIVE `feature-column-promotion-break-the-seed-deadlock` (a column should promote on its OWN composition + directly-owned constituents; cross-linked sibling columns are references, not blockers — two firm verbs now concretely demonstrate the current sibling-blocks-promotion deadlock). **Cycle-084 (batch-26 position 3, the LAST primary cycle before the batch-26 meta-phase; land-clean discipline): a SINGLE clean lifter hygiene dispatch corrected two stale `eigenfreq_qfactor_reduce` maturity references in `book/src/L4/domain_energy_reduce.md`** (now firm c082; the chapter called it rough-in), incl. the load-bearing §Status gating-logic re-narration (`domain_energy_reduce` stays rough-in because its OWN folded `matrix-weighted-norm` energy form is rough-in, NOT because firm siblings are). **ZERO status/count change c084.** **Cycle-079 (batch-25 position 1): BOTH c075 reduce verbs' 2nd (test-coverage) gate DISCHARGED via existing-test citation (batch-24 meta-phase decision (e)) — `sparameter_reduce` + `eigenfreq_qfactor_reduce` both `rough-in` → `rough-in (test-coverage-bounded)`; `sparameter_reduce` gate-b also recorded already-discharged via firm `port_projection` (c077). The energy-fields verb `domain_energy_reduce` was AUTHORED at `rough-in` (`book/src/L4/domain_energy_reduce.md`, on a combinator-miner DISTINCT-VERB-WARRANTED confirm-probe) — resolves the energy-fields column's plain-text forward-refs to live links; closes OQ `domain_energy_reduce-l4-verb-needs-authoring`. L4 reduce-family now 4 AUTHORED verbs; L4 rough-in 1→2; both coupled columns (`sparameters`, `eigenfrequency-qfactor`) STAY `seed`; NO firm-count change. Both reduce verbs' SECOND gate to FULL `firm` is now narrowed: `sparameter_reduce` needs the assembly-fold test; `eigenfreq_qfactor_reduce` needs the eigenvalue-un-transform L1 primitive + an assembly test (successor OQ `eigenfreq-qfactor-reduce-firm-needs-l1-eigenvalue-untransform-primitive`); `domain_energy_reduce` needs the `matrix-weighted-norm` energy-form firm + a per-domain participation test (the `matrix-weighted-norm` test-coverage gate is incidentally addressable via `test-domainpostoperator.cpp` `GetElectricFieldEnergy` — a strong next-cycle candidate).** **Cycle-078 (batch-24 position 3/3, the LAST primary cycle before the batch-24 meta-phase; D1 layer-intro-author): the FINAL output-product column LANDED — `energy-fields`, at L4/L1/L0** (`book/src/feature/energy-fields.{L4,L1,L0}.md`, 3 new files, status `seed`, alpha-within-kind between `eigenfrequency-qfactor` and `inductance`): the per-domain field-energy table (per-domain E/H field energies + participation ratios `energyᵢ/E_total`). Its reduction composes the **firm** L1 `participation_ratio` (c077 D4) over the domain-restricted SPD energy form `½⟨field, M_i field⟩` (`matrix-weighted-norm`-squared, rough-in), minting a NEW L4 reduction verb **`domain_energy_reduce`** (`rough-in`; the per-domain `(energyᵢ, pᵢ)` map) — **referenced as plain-text code-spans only; the `L4/domain_energy_reduce.md` verb file is NOT authored (a follow-up; OQ `domain_energy_reduce-l4-verb-needs-authoring`)**. The verb is the **per-domain sibling of the per-mode `eigenfreq_qfactor_reduce`** (both reduce-to-scalar-TABLE, rank-1; NOT a `gram_reduce` family-PAIR grid — the c074 D6 do-NOT-over-unify guard honored). **NOTE — energy-fields is driver-AGNOSTIC** (it consumes a *single* solution field from *any* field-bearing driver, not a per-driver solution family): it BREAKS the 1:1 output-product↔driver convention the other 4 output-product columns follow (OQ `energy-fields-driver-agnostic-not-per-driver-stage3`, routed to the batch-24 meta-phase). D1 is COHORT OWNER this cycle — SOLE-owned the shared `feature/index.md` matrix + `SUMMARY.md` `# Feature surfaces` rows for BOTH new columns (energy-fields output-product + boundary-mode driver-leaf), and applied a consistency touch to the "one reduction verb each" prose (the rank-1 shape now carries two verbs). **Cycle-075 (batch-23 position 3/3, the LAST primary cycle before the batch-23 meta-phase): TWO MORE output-product columns LANDED — `sparameters` + `eigenfrequency-qfactor`, each at L4/L1/L0** (`book/src/feature/{sparameters,eigenfrequency-qfactor}.{L4,L1,L0}.md`, 6 new files, status `seed`) **+ each got its OWN NEW L4 reduction verb** (the c074 D6 closed-negative `gram_reduce`-non-subsume routing honored — neither is a `gram_reduce` specialization): **`sparameter_reduce`** (`book/src/L4/sparameter_reduce.md`, `rough-in` — the driven per-port port-projection → S-matrix, reduce-to-matrix LINEAR-projection; D6 authored the chapter, D1 the dep-map row/SUMMARY/cohort-note) + **`eigenfreq_qfactor_reduce`** (`book/src/L4/eigenfreq_qfactor_reduce.md`, `rough-in` — the eigenmode per-mode `(f,Q)` scalar-ratio, reduce-to-scalar-TABLE; D3 authored chapter+row+SUMMARY). **The L4 algebra-of-folds now has 3 reduce-shapes:** reduce-to-matrix BILINEAR-Gram (`gram_reduce`) / reduce-to-matrix LINEAR-projection (`sparameter_reduce`) / reduce-to-scalar-TABLE per-mode (`eigenfreq_qfactor_reduce`). **D2 (cohort owner)** registered BOTH new columns in the `feature/index.md` matrix (under `*output products*`, with the §cohort prose rewritten to the **3-reduction-shape taxonomy**) + the `SUMMARY.md` `# Feature surfaces` block (within-column high→low). **Both new verbs `rough-in`, double-gated** on their L1 homes (the per-port projection L1 home for `sparameter_reduce` — OQ `sparameter-reduce-l1-port-projection-home`; the participation-ratio L1 primitive for `eigenfreq_qfactor_reduce` — OQ `participation-ratio-l1-primitive-as-eigenfreq-qfactor-firming-route`) + dedicated tests; **both columns `seed`, promotion coupled to their verb firming.** **Cycle-077 (batch-24 position 2/3): BOTH verb first-gates DISCHARGED — each reduce verb now has a firm L1 home.** D5 (harvester) landed firm L1 `port_projection` (`book/src/L1/port_projection.md` — the per-port linear functional `⟨s, E⟩`, OWN-verb verdict, NOT a `dot`/`bilinear-form` specialization; firms `sparameter_reduce` **gate-b**; closes OQ `sparameter-reduce-l1-port-projection-home`); D4 (combinator-miner) landed firm L1 `participation_ratio` (`book/src/L1/participation_ratio.md` — the shared `½X|I|²/E` energy-quotient of the eigenmode κ/EPR/surface-dielectric participations; firms `eigenfreq_qfactor_reduce` **gate-a**; closes OQ `participation-ratio-l1-primitive-as-eigenfreq-qfactor-firming-route`). **Both reduce verbs STAY `rough-in`** — each remains double-gated, the SECOND gate is a dedicated reduction test (OQs `sparameter-reduce-status-promotion-double-gated` + `eigenfreq-qfactor-reduce-status-promotion-double-gated`, both open, fold into the `gram-reduce` standing-gate family); both columns stay `seed`. **Cycle-074 (batch-23 position 2/3): the FIRST output-product columns LANDED — `capacitance` + `inductance`, each at L4/L1/L0** (`book/src/feature/{capacitance,inductance}.{L4,L1,L0}.md`, 6 new files, status `seed`): composition-roots that COMPOSE the firm-track L4 `gram_reduce` over the electrostatic/magnetostatic driver columns (capacitance `w=1` voltage; inductance `w=1/(IᵢIⱼ)` current-normalized). D2 (cohort owner) registered both in the `feature/index.md` matrix (with `*output products*` sub-header) + `SUMMARY.md` `# Feature surfaces` (6 rows, after the 5 leaf drivers, before lifecycle ROOT, within-column high→low). **D1 closed the `gram_reduce` replace-and-propagate** (electrostatic.L4 + magnetostatic.L4 §reduction now read "the rough-in-track L4 `gram_reduce` reduction" — the mine-and-strand gap closed). **D6 CLOSED-NEGATIVE the `gram_reduce` 3rd-witness probe** (`L4/gram_reduce.md` §Specialization now records eigenmode-Q + S-params both NON-MATCH → the **`gram_reduce` 2-witness boundary is now closed-negative**; future S-param/eigenfreq+Q output-product columns route to author their OWN reduction verbs, NOT broadening `gram_reduce`). **Cycle-073 UNBLOCKED** (now consumed): the shared operator-weighted symmetric-Gram reduction is firm-track at L4 as `gram_reduce` (`book/src/L4/gram_reduce.md`, `rough-in (test-coverage-bounded)`; unifies capacitance `Vⱼᵀ·K·Vᵢ` + inductance `Vⱼᵀ·M·Vᵢ`). **Remaining 3 output-product columns** (S-params / eigenfreq+Q / energy-fields) gate on their OWN reduction verbs per D6 (no shared-combinator path); still plain-text forward-refs from the driven/eigenmode columns. **Cycle-118 (batch-38 position 1/3, D5 combinator-miner): the 6th output-product reduce verb LANDED FIRM — `waveguide_mode_reduce` (`book/src/L4/waveguide_mode_reduce.md`, the field-carrying propagation-mode member of the output-product reduce-verb algebra; L4 Data-algebra firm 21→22; authored as a DISTINCT sibling of `eigenfreq_qfactor_reduce`, non-unify guard closed-negative) → the `waveguide-mode` output-product column PROMOTED rough-in→FIRM at L4+L1 (own-composition rule; `feature_root: seed` KEPT) — output-product reduce-verb cohort now 6-of-6 with firm reduce verbs at L4.
- [x] **(4) wave-port / boundary-mode — LANDED (cycle-078).** `boundarymodesolver`. **Cycle-078 (batch-24 position 3/3; D2 layer-intro-author): the boundary-mode driver-leaf column LANDED — the 6th driver, at L4/L1/L0** (`book/src/feature/boundary-mode.{L4,L1,L0}.md`, 3 new files, status `seed`, **alpha-FIRST in the driver-leaf grouping** — boundary < driven < eigenmode < electrostatic < magnetostatic < transient): the composition root for `BoundaryModeSolver` (the 6th `ProblemType` dispatch branch, `palace/main.cpp:276-278`). Body = a 2D-submesh-extraction preface (`ExtractBoundary2DSubmesh`: `CreateFromBoundary` → 3D→2D projection) + the **SAME opaque-library black-box eigen-iteration corner as the `eigenmode` driver** (the `ModeEigenSolver` GEP is opaque-library-owned; cross-referenced to `eigenmode` as the 2nd clean witness, distinguished by the 2D-submesh preface). Both solve-side constituents (`fe_assemble`, `eigsolve`) firm at L4+L1; stays `seed` because the stage-3 waveguide-mode readout has no dedicated output-product column yet (forward-ref). D2 added its alpha-FIRST bullet to `feature/driver-leaf.md` ("5 drivers"→"6 drivers"); its index/SUMMARY rows were added by D1 (cohort owner). The `boundarymode-is-sixth-problemtype-branch` reconcile OQ is settled-by-landing (the column IS the 6th branch). Two forward-ref OQs carried: `boundary-mode-2d-submesh-extraction-preface-vocabulary-home` (the submesh-extraction preface vocabulary), `boundary-mode-waveguide-output-product-column-needs-home` (the stage-3 product — RESOLVED: the `waveguide-mode` column landed c117 seed/rough-in and PROMOTED FIRM c118 D5 via the `waveguide_mode_reduce` verb).

## Synthesis — synthesized-library implementation view (USER DIRECTIVE 2026-06-07; added cycle-136, batch-44 LEAD)

A new top-level Part **`# Synthesis`** placed immediately BEFORE `# Feature surfaces` in `SUMMARY.md`: the spec's surface rendered **as a synthesized implementation library** in the L4 pseudo-language (5 libraries + index). The implementation VIEW — links to the authoritative L4/semantics/concepts defs (does NOT restate); navigational-containers (reference-class edges only, no `rank:`, no `depends-on`; correctly classify `expected_unreachable_outside_dag`, NOT detritus). Full def: project memory `project_synthesis_section_directive`; the new mdBook Part is `book/src/synthesis/`.

- [x] **Synthesis section COMPLETE + FULLY CORRESPONDENCE-AUDITED cycle-138 (batch-44 CLOSE) — 6 of 6 chapters bodied, whole Part rendered-def↔L4 correspondence-audited.** **Cycle-138 (batch-44 position 3/3, BATCH-CLOSING):** the lowering-verifier completed the Synthesis rendered-def-vs-L4 correspondence audit over the LAST-pull groups (`coordination` + `drivers` + `types`; c137 covered `iteration` + `data-algebra`) — top-level verdict FULLY-SUPPORTED, AUDIT-CLASS (NO book mutation), modulo 2 gated non-blocking residuals (`l4-eigsolve-initial-state-vs-initial-eig-state-seed-inconsistency` upstream-L4 self-inconsistency → abstractor; `synthesis-types-iodata-omits-units-field` 5-of-6-field completeness → layer-intro-author shell pass). The harvester refreshed the stale `cg_solve` Form-B worked-example in `book/src/L4/krylov-step.md:192-197` (the stale `iterate_while_with_prev` call → canonical boot/init/steady/cont arg order + record returns; a within-`firm` body-fidelity fix, no node maturity/edge moved). The batch-44 full-hygiene sweep (cross-layer-cross-cutter, once-per-batch cadence) returned a CLEAN BILL: `rank_violations==0`, `unresolved_depends_on_targets==0`, all 6 synthesis chapters `reference`-class-only (0 blocking edges, correct GC classification = `expected_unreachable_outside_dag`, NOT detritus), 3 `realizes-kernel-api` reference-class edges, 3 `#extern` boundaries, 0 `$`-sigil-fence leaks, DIRECTIVE-1 MPI boundary clean. **The `# Synthesis` Part is now COMPLETE and whole-Part correspondence-audited — batch-44 (the LEAD direction) closes substantively complete.** **Cycle-137 (batch-44 position 2/3, the MIDDLE):** the deferred `drivers` library body LANDED (the topologically-last 6th chapter), completing the Part 6/6. `synthesis/drivers.md` filled stub-shell→rendered implementation VIEW: 13 composition defs (6 sim drivers: electrostatic/magnetostatic/driven/transient/eigenmode/boundary_mode + 6 output products: capacitance/inductance/sparameters/eigenfrequency_qfactor/energy_fields/waveguide_mode + the lifecycle ROOT) + 6 IoData-projection-view config type aliases, composing the calculus libraries below, lifted from the Feature spine; reference-class edges only (21 frontmatter `reference:` edges, 0 `depends-on`). A lowering-verifier correspondence audit of the rendered synthesis defs vs the L4 chapter bodies ran AUDIT-CLASS (FULLY-SUPPORTED verdict, NO book mutation). Finalize NORMALIZED the index 5-library matrix: the `iteration` / `data-algebra` / `coordination` rows still read `stub (Wave 2)` (stale since c136) → flipped to `navigational (rendered)` to match their c136-bodied chapters + the c137 `drivers` row (resolves OQs `synthesis-index-per-library-status-cell-rendered-completeness-convention` + `synthesis-coordination-chapter-status-seed-token-reconciliation-c136`). All 6 chapters classify as `expected_unreachable_outside_dag` (correct navigational-container disposition, NOT detritus). **Cycle-136 (batch-44 position 1/3, the LEAD):** the `# Synthesis` Part landed with index + all 6 library shells + SUMMARY wiring, then the 3 calculus libraries (`iteration` / `data-algebra` / `coordination`) filled with rendered def bodies in one wide multi-dispatch (Wave-1 shell + Wave-2 bodies, same cycle). **Chapters:** `index` (Part overview) + `types` (shared cross-cutting record defs — IoData/OpParams/SimState rendered) + `iteration` (iterate_while/krylov-step/chebyshev) + `data-algebra` (13 firm op defs incl. the DIRECTIVE-3 dual surface: `#extern assemble_term` kernel-API + the inline `mk_matrix_free_operator` kernel-impl; + the `sharding-decompose-reduce` rank-0 roadmap_goal note, DIRECTIVE-1 boundary held) + `coordination` (preconditioning-framework/ksp_solve/eigsolve/solve_family/frequency_sweep/fold_solve, + EigState in-chapter type block resolving the D1 OQ) — ALL WITH BODIES. **`drivers` remains a stub shell** (`status: stub`; body deferred to a later batch-44 cycle — lifts the 5 sim drivers + lifecycle ROOT + output products from the Feature spine, composing the calculus libraries). The 6 chapters all built clean and classify as reference-reachable navigational-containers. The status-token inconsistency across the three Wave-2 chapters (`navigational-container`/`stub`/`seed`) was NORMALIZED at finalize: a filled implementation-VIEW library chapter carries NO `status:` field (matching the index/types reference convention — kind-only, no resolution claim); `drivers` keeps `status: stub` as a deferred shell.

**GRADED-STACK SNAPSHOT — CYCLE-139 (BATCH-45 / 1/3, the OPENER): the WIDE ALL-FRONTS fan-out OPENED — and the KEY DELIVERABLE-PRESENCE FINDING reshapes the arc: TWO of the four directed fronts (1 geometric-multigrid, 2 AMR) were ALREADY SUBSTANTIALLY LANDED at batch-39, so the substantive batch-45 forward content is fronts 3+4 + the shared-core finding + the synthesis follow-ups.** ALL graded-stack totals HELD EXACTLY vs c138 by design (c139's book edits are all within-chapter content advances — D2 `lanczos_step` in-place sharpening / D3 `sharding-decompose-reduce` rank-0 extension via `reference`-class solve-root edges only / D4 `eigsolve-impl` `verified_against:` audit-block append / D5 5-file synthesis content-fidelity fixes — no node maturity flip, no `depends-on` edge, no rank move): `files=392` (HELD), `typed=331` (HELD), `untyped=61` (HELD), `roots=45` (HELD), `reachable=163` (HELD), `reference_reachable=247` (HELD), `rank_violations=0` (HELD — gating clean; baseline fully discharged so ANY violation would be NEW; none), `unresolved_depends_on_targets=0` (HELD), `promotion_frontier=12` (HELD), `detritus=123` (HELD — D3's 3 NEW firm solve-root `reference`-class edges point INTO already-reachable firm nodes; reference edges from a rank-0 node add no reachability mass, so detritus does NOT move), `true_detritus=51` (HELD), `expected_unreachable_outside_dag=54` (HELD). Both step-5b block-conditions PASS (rank_violations==0 held; NO newly-orphaned node — reachability identical). `rank_violations` trend …→0 (c136)→0 (c137)→0 (c138)→0 (c139). Post-cycle-139 (batch-45 position 1/3 — the FIRST / OPENER primary cycle of meta-batch-45; cycles 139/140/141; **the batch-45 meta-phase fires AFTER cycle-141's finalize, aggregating 139/140/141 as a SEPARATE dispatch/commit**; cycle counter does NOT reset). BATCH-45 DIRECTION = open ALL FOUR gated fronts at once (USER DECISION 2026-06-07; `project_batch45_direction_open_all_gated_fronts`) — geometric-multigrid + AMR + eigsolve-impl + sharding-math-further, one wide shared-exploration fan-out; standing gates held (DIRECTIVE-1 MPI OUT, DIRECTIVE-3 kernel-API/impl, no-forced-rectangular-pull-up); maintenance floor reverts to surround. **HEADLINE — the OPENER's planner-reshape finding: fronts 1 (geometric-multigrid: `feature/geometric-multigrid-preconditioner.{L4,L1}` firm + `L1/multigrid-relaxation-smoother` firm kernel-impl, RE9/RE1/RE5/RE7/RE10 ALL discharged batch-39) and 2 (AMR: `L1-L0/amr-estimate-mark-refine` firm + `L1/flux_recovery_estimate`/`L1/dorfler_mark` firm) are ALREADY SUBSTANTIALLY LANDED — forcing new V-cycle/AMR vocab nodes would be a rectangular pull-up; the residual forward room is the SHARED-CORE mine + the two genuinely-open fronts.** **D1 (`combinator-miner`, `iterate-while-basis-extension-shared-core`):** clean NO-COMBINATOR finding (verdict (b), NO book mutation) — the shared iteration substrate is ALREADY lifted as two firm combinators (`iterate_while_L3` driver + `correction_step` body); the 3 carry-shapes genuinely diverge; `extend_while` candidate rejected as identity-in-named-terms. OQ pair recorded (the durable negative verdict + an AMR-refinement-set-growth `iterate_while_L3`-rendering watch-item for when the AMR consumer lands). **D2 (`abstractor`, `lanczos-step-toward-promotion`):** 5 in-place edits to `book/src/L3/lanczos_step.md` (§Signature operator-value spelling / §Status two-arm gate / §Relationship cross-ref / §Evidence + banner citation-drift fixes); `lanczos_step` STAYS rank-0 `roadmap_goal` (redirect-correct floor — arm-A positive-structure promotion is structurally UNSATISFIABLE from `palace/` since MINRES is an enum-only-stub with empty L0; the live path is arm-B blocking-consumer wiring); so `eigsolve-impl`'s `roadmap_goal → stub` promotion did NOT fire this cycle. **FINALIZE BUILD-REPAIR (step-5c): the D2 §Signature edit re-introduced a `$`-sigil signature in an INDENTED (4-space) pseudocode block (`lanczos_step :: LinOp[(S: ...), $S] -> ...`), which KaTeX ate (`class="katex"` appeared inside the `<pre>` in `L3/lanczos_step.html`); the surgical repair CONVERTED the indented block to a fenced ` ```text ` block per the standing convention (project memory `project_katex_dollar_sigil_fence_requirement`); rebuild → step-5c assertion PASS (0 `class="katex"` inside any `<pre>` across all built HTML).** **D3 (`abstractor`, `sharding-decompose-reduce-solve-generalization-sketch`):** extended `book/src/L4/sharding-decompose-reduce.md` (STAYS rank-0 `roadmap_goal`) with the additive-Schwarz SOLVE-generalization; 3 NEW firm solve roots (`L4/ksp_solve`/`L4/fold_solve`/`L4/krylov-step`) added under `reference:` ONLY (NO `depends-on`, so no `rank(firm)=3 > 0` violation); MPI cited-as-deferred (DIRECTIVE-1 held); recorded as an honest config-conditional NON-law (solve-case recovery STRICTLY WEAKER than the reduce-case — no free homomorphic-solve law). **D4 (`lowering-verifier`, `eigsolve-impl-realizes-kernel-api-reaudit-lanczos`):** audit-class FULLY-SUPPORTED; appended 8 `verified_against:` Hermitian-arm entries to `book/src/L3/eigsolve-impl.md` (195→227 lines, YAML round-trips clean — single top key, 16 entries); `realizes-kernel-api` stays `reference`-class, kernel-api `L3/eigsolve` stays `partial-obstruction` undowngraded. **D5 (`layer-intro-author`, `synthesis-residual-content-fidelity-followups`):** 3 content-fidelity fixes across 5 files — `L4/iterate-while-with-prev.md` (stale `cg_solve` call refreshed) + `L4/eigsolve.md` (7× `initial_state→initial_eig_state`) + `L4/index.md` (2 eigsolve-cap occurrences) + `synthesis/coordination.md` (stale NOTE re-phrased) + `synthesis/types.md` (`units : Units` added); 3 parent c138 OQs DISCHARGED (DISCHARGED-c139 notes appended; the meta-phase CLOSE-RESOLVES the 3 parent sections + retires their Backlog-Low migration lines at the batch-45 unify). **D6 (`cross-layer-cross-cutter`, MAINTENANCE FLOOR — the once-per-batch full-hygiene sweep, placed at the OPENER to ground the wide arc's linter baseline):** audit-class CLEAN BILL (NO book mutation) — all 12 graded-stack baseline fields reproduce on-disk, both hard invariants hold, 3 `realizes-kernel-api` edges reference-class, DIRECTIVE-1 boundary intact. `cargo make book` EXIT 0; **Step-5c KaTeX `$`-sigil assertion PASS** (after the 1 build-repair above); 1 build-repair (the lanczos_step fence conversion), 0 within-finalize consistency fixes; only the pre-existing benign unclosed-HTML-tag / KaTeX-bracket WARNs in untouched files. 6/6 staging rows == 6 dispatched-ready (120th consecutive clean staging). 6 reports applied clean. **THE CARRY to c140/c141 + the BATCH-45 META (fires after c141, aggregating 139/140/141):** (1) front-3 (eigsolve-impl) did NOT promote — `lanczos_step` STAYS `roadmap_goal` (arm-A positive-structure unsatisfiable in `palace/`; the live path is arm-B blocking-consumer wiring); c140/c141 may carry an `eigsolve-impl` `roadmap_goal → stub` promotion ONLY if a positive grounding surfaces (it will NOT from MINRES). (2) the all-fronts reshaping disposition — two of the four directed fronts were already complete (the human RATIFIED the intent-honoring plan 2026-06-08), so the substantive batch-45 forward content is fronts 3+4 (eigsolve-impl advanced-but-gated; sharding-math solve-generalization sketched) + the D1 shared-core finding + the synthesis follow-ups; the batch-45 meta should render this. (3) D5 flagged the meta-phase to CLOSE-RESOLVE 3 parent OQ sections + retire their Backlog-Low migration lines at the batch-45 unify pass. (4) NO `.claude/agents/` changes from this finalize → NO session restart needed before c140 (no meta-phase fires until after c141). The in-scope FEATURE-SURFACE SPINE remains L4-COMPLETE.

**GRADED-STACK SNAPSHOT — CYCLE-138 (BATCH-44 / 3/3, BATCH-CLOSING): the `# Synthesis` Part is COMPLETE and whole-Part rendered-def↔L4 CORRESPONDENCE-AUDITED — batch-44 (the SYNTHESIS LEAD direction) closes substantively complete.** ALL graded-stack totals HELD EXACTLY vs c137 by design (c138's only book edit is a within-`firm` body-fidelity fix to `L4/krylov-step.md` — no node maturity/edge/rank moved; the other 2 reports are audit-class with no book mutation): `files=392` (HELD), `typed=331` (HELD), `untyped=61` (HELD), `roots=45` (HELD), `reachable=163` (HELD), `reference_reachable=247` (HELD), `rank_violations=0` (HELD — gating clean; baseline fully discharged so ANY violation would be NEW; none), `unresolved_depends_on_targets=0` (HELD), `promotion_frontier=12` (HELD), `detritus=123` (HELD), `true_detritus=51` (HELD), `expected_unreachable_outside_dag=54` (HELD — all 6 synthesis chapters classify here, the correct navigational-container disposition). Both step-5b block-conditions PASS (rank_violations==0 held; NO newly-orphaned node). `rank_violations` trend …→0 (c135)→0 (c136)→0 (c137)→0 (c138). Post-cycle-138 (batch-44 position 3/3 — the THIRD / BATCH-CLOSING primary cycle of meta-batch-44; cycles 136/137/138; **the batch-44 meta-phase fires AFTER this cycle-138 finalize, aggregating 136/137/138 as a SEPARATE dispatch/commit**; cycle counter does NOT reset). BATCH-44 DIRECTION = the SYNTHESIS section (USER DIRECTIVE 2026-06-07; `project_synthesis_section_directive`) is the LEAD; the wind-to-maintenance floor (`project_batch44_direction_wind_to_maintenance`) is the steady-state surround. **HEADLINE — the `# Synthesis` Part is COMPLETE (6/6 chapters bodied) AND now whole-Part correspondence-audited (c137 covered iteration+data-algebra; c138 covered coordination+drivers+types) — the LEAD direction closes substantively complete.** **D1 (`harvester`, `l4-krylov-step-cg-solve-worked-example-refresh`):** within-`firm` body fidelity — refreshed the stale `cg_solve` Form-B worked-example in `book/src/L4/krylov-step.md:192-197` to the canonical `iterate_while_with_prev` boot/init/steady/cont arg order + record returns; `krylov-step` stays `firm`, no dep-map/SUMMARY edit. **D2 (`lowering-verifier`, `synthesis-rendered-def-vs-l4-correspondence-audit-coordination-drivers-types`):** AUDIT-CLASS, top-level verdict FULLY-SUPPORTED — NO `## Proposed changes` to `book/`, NO build relevance; 2 NEW OQ promotions only (`l4-eigsolve-initial-state-vs-initial-eig-state-seed-inconsistency`, `synthesis-types-iodata-omits-units-field`); completes the Synthesis correspondence coverage modulo those 2 gated non-blocking residuals. **D3 (`cross-layer-cross-cutter`, MAINTENANCE FLOOR — once-per-batch full-hygiene sweep):** clean-bill, AUDIT-CLASS (NO book/scaffolding mutation) — `rank_violations==0`, `unresolved_depends_on_targets==0`, all 6 synthesis chapters `reference`-class-only (0 blocking edges, correct GC = `expected_unreachable_outside_dag`), 3 `realizes-kernel-api` reference-class edges, 3 `#extern` boundaries, 0 `$`-sigil-fence leaks, DIRECTIVE-1 MPI boundary clean. `cargo make book` EXIT 0; **Step-5c KaTeX `$`-sigil collision assertion PASS** (`class="katex"` inside any `<pre>` = 0 across all built HTML; the edit lands inside a pre-existing ` ```text ` fence); only the pre-existing benign KaTeX/markdown-bracket WARNs in untouched files; ZERO build-repairs; ZERO within-finalize consistency fixes. 3/3 staging rows == 3 dispatched-ready (119th consecutive clean staging). 3 reports applied clean. **THE STRONG TEE-UP for the BATCH-44 META (fires next, aggregating 136/137/138):** (1) the SYNTHESIS LEAD direction is substantively complete — the `# Synthesis` Part is 6/6 bodied + whole-Part correspondence-audited; the meta should render the Synthesis-complete disposition + the role-spec codification of the synthesis-chapter kind (`layer-intro-author` shell + `abstractor`/`harvester` per-op renders + `lowering-verifier` correspondence-audit duty). (2) 5 NEW OQs across the batch await meta migration: `iterate-while-with-prev-evidence-prose-stale-cg-call-shape` (c138 D1 secondary stale occurrence), `l4-eigsolve-initial-state-vs-initial-eig-state-seed-inconsistency` (c138 D2 → abstractor), `synthesis-types-iodata-omits-units-field` (c138 D2 → shell pass), plus the c138 DISCHARGES of three OQs (`synthesis-l4-krylov-step-worked-example-cg-solve-stale-vs-iterate-while-with-prev-signature`, `synthesis-correspondence-audit-coverage-coordination-drivers-types-next-pull`, `synthesis-edges-next-batch-maintenance-floor-audit`) for the meta to close/migrate. (3) a FRESH forward-direction §CENTRAL ASK is warranted — the SYNTHESIS LEAD is complete and the in-scope spine remains L4-COMPLETE; with the wind-to-maintenance floor `project_batch44_direction_wind_to_maintenance` as the standing default, surface whether the human wants a new substantive direction or to settle into the maintenance steady-state.

**GRADED-STACK SNAPSHOT — CYCLE-137 (BATCH-44 / 2/3, the MIDDLE): the `# Synthesis` Part is COMPLETE — the synthesized-library implementation view, 6-of-6 chapters bodied (`drivers` body landed).** Counts HELD vs c136 by design (the `drivers` chapter was a stub-shell→body fill, NOT a new file; no node maturity/edge moved): `files=392` (HELD), `typed=331` (HELD), `untyped=61` (HELD), `roots=45` (HELD), `reachable=163` (HELD), `reference_reachable=247` (HELD), `rank_violations=0` (HELD — gating clean; baseline fully discharged so ANY violation would be NEW; none), `unresolved_depends_on_targets=0` (HELD), `promotion_frontier=12`, `detritus=123` (HELD — NO synthesis chapter is detritus), `true_detritus=51` (HELD), `expected_unreachable_outside_dag=54` (HELD — all 6 synthesis chapters classify here, the correct navigational-container disposition; `synthesis/iteration` is additionally reference-reachable-inbound). Both step-5b block-conditions PASS (rank_violations==0 held; NO newly-orphaned node). `rank_violations` trend …→0 (c135)→0 (c136)→0 (c137). Post-cycle-137 (batch-44 position 2/3 — the MIDDLE primary cycle of meta-batch-44; cycles 136/137/138; the batch-44 meta-phase fires AFTER cycle-138's finalize; cycle counter does NOT reset). **HEADLINE — the `# Synthesis` Part is COMPLETE (6/6 chapters bodied): index + types + 3 filled calculus libraries (iteration/data-algebra/coordination) + the now-filled `drivers` library; the deferred 6th chapter is closed.** Finalize NORMALIZED the index 5-library matrix (the 3 calculus-library rows still read the stale `stub (Wave 2)`; flipped to `navigational (rendered)`); a lowering-verifier rendered-def-vs-L4 correspondence audit ran clean (FULLY-SUPPORTED, NO book mutation). `cargo make book` EXIT 0; KaTeX `$`-sigil collision did NOT recur (`class="katex"` inside any `<pre>` = 0). **CARRY to c138 + the batch-44 meta:** the Synthesis section is now substantively complete; c138 is likely consolidation/maintenance (per-operator synthesized-def deepening + lowering-verifier correspondence audits may continue; the maintenance floor is the steady-state surround); the batch-44 meta should render the Synthesis-complete disposition + the role-spec codification of the synthesis-chapter kind.

**GRADED-STACK SNAPSHOT — CYCLE-136 (BATCH-44 / 1/3, the LEAD / OPENER): the `# Synthesis` Part STOOD UP — the synthesized-library implementation view, 5-of-6 chapters with bodies (`drivers` deferred). Counts moved by the +6 new synthesis chapters (all navigational-containers, NOT detritus): `files=386→392` (+6 = the 6 synthesis chapters), `typed=325→331`, `untyped=61` (HELD), `roots=45` (HELD), `reachable=163` (HELD), `reference_reachable=247` (HELD), `rank_violations=0` (HELD — gating clean; baseline fully discharged so ANY violation would be NEW; none), `unresolved_depends_on_targets=0` (HELD), `promotion_frontier=11→13`, `detritus=123` (HELD — NO synthesis chapter is detritus; all 6 classify `expected_unreachable_outside_dag`, the correct navigational-container disposition), `true_detritus=51` (HELD), `expected_unreachable_outside_dag` includes all 6 synthesis chapters; `synthesis/iteration` is additionally reference-reachable-inbound. Post-cycle-136 (batch-44 position 1/3 — the LEAD/OPENER of meta-batch-44; cycles 136/137/138; the batch-44 meta-phase fires AFTER cycle-138's finalize; cycle counter does NOT reset). BATCH-44 DIRECTION = the SYNTHESIS section (USER DIRECTIVE 2026-06-07; `project_synthesis_section_directive`) is the LEAD; the wind-to-maintenance floor (`project_batch44_direction_wind_to_maintenance`) is the steady-state surround. **HEADLINE — the `# Synthesis` Part is STOOD UP: index + types + 3 filled calculus libraries (iteration/data-algebra/coordination) + drivers stub shell, SUMMARY-wired before `# Feature surfaces`; DIRECTIVE-3 dual-surface intact (kernel-API `#extern` + kernel-impl inline render); DIRECTIVE-1 boundary held (sharding stays a rank-0 roadmap_goal note); `rank_violations=0` held; `cargo make book` EXIT 0; KaTeX `$`-sigil collision did NOT recur (`class="katex"` inside any `<pre>` = 0).** Finalize NORMALIZED the cross-chapter status-token inconsistency (filled VIEW chapters → no `status:` field, matching index/types; `drivers` stays `status: stub`). The maintenance-floor sweep (c136 cross-layer-cross-cutter) was a clean-bill audit. **CARRY to c137/c138:** fill the `drivers` library body (the deferred 6th chapter — lift the 5 drivers + lifecycle ROOT + output products from the Feature spine); the per-operator synthesized-def rendering may deepen (abstractor/harvester own per-op renders per the directive); maintenance floor is the steady-state surround.

**GRADED-STACK SNAPSHOT — CYCLE-135 (BATCH-43 / 3/3, BATCH-CLOSING): the gated (C) sharding-MATH exploration is DISCHARGED — a THIN CONSOLIDATION + maintenance batch-closing cycle. ALL graded-stack totals HELD EXACTLY vs c134 by design (c135 added only a `reference`-class navigational dep-map row + an `open-questions.md` append — no node/edge): `files=386, typed=325, untyped=61, roots=45, reachable=163, reference_reachable=247, rank_violations=0, unresolved_depends_on_targets=0, promotion_frontier=11, detritus=123, true_detritus=51, detritus_reference_reachable_re11_cohort=72, rank_histogram.roadmap_goal=4, expected_unreachable_outside_dag=48`. Post-cycle-135 (batch-43 position 3/3 — the THIRD / BATCH-CLOSING primary cycle of meta-batch-43; cycles 133/134/135; the batch-43 meta-phase fires AFTER this cycle-135 finalize, aggregating all three as a SEPARATE dispatch/commit; cycle counter does NOT reset). BATCH-43 DIRECTION = open the DEFERRED sharding-MATH gate (USER DECISION 2026-06-07; `project_batch43_direction_sharding_math_gate`), GATED-FIRST behind a hard spine-non-destabilization probe; MPI/distributed STAYS OUT (DIRECTIVE-1). **HEADLINE — THE GATED (C) SHARDING-MATH EXPLORATION IS DISCHARGED across batch-43: WAVE-1 hard gate CLEAR on both arms (c133) → WAVE-2 sketch landed non-destabilizingly, `rank_violations=0` held (c134) → navigational L4-index dep-map row added (c135); DIRECTIVE-1 held throughout.** **D1 (`layer-intro-author`, `l4-index-sharding-row`): the L4-index navigational mirror.** APPLIED one navigational `roadmap_goal` dep-map row for `sharding-decompose-reduce` into `book/src/L4/index.md`'s "Data-algebra combinators & named verbs" table, alpha-positioned between `nrm2` and `sparameter_reduce` (mirroring the c134 SUMMARY placement + the `mk_matrix_free_operator` rank-0 precedent at `index.md:119`). The row is `reference`-class — the L4 index page is `kind: navigational-container (layer index)` (reference-edges-only, no `rank:`), so it adds NO typed-graph `depends-on` edge and the counts do NOT move. The signature cell's Haskell list-comprehension bar is ESCAPED `\|` (the c135-repaired table-render defect; matches `index.md:130/135`). Resolved 2 OQs (`sharding-decompose-reduce-l4-index-roadmap-goal-listing` → YES; `sharding-decompose-reduce-summary-group-placement` → RATIFIED). **D2 (`cross-layer-cross-cutter`): MAINTENANCE FLOOR standing hygiene** — clean-bill, the c134 re-baselined disposition HELD EXACTLY; AUDIT-CLASS (NO book mutation; touched only `scaffolding/open-questions.md` append); 3 `realizes-kernel-api` kernel-impl edges stay `reference`-class; semantic surface clean; the new `L4/sharding-decompose-reduce` node is the EXPECTED single detritus item — a rank-0 `roadmap_goal` reference-EMITTING leaf (`true_detritus`/`no_typed_edges`; NOT a member of the reference-reached RE11 §2g cohort of 72); RE4 consumer-gated (premise holds), RE11 premises hold; DIRECTIVE-1 boundary held across the whole batch; promoted 4 batch-43-meta TEE-UP findings (`sharding-decompose-reduce-formal-RE-disposition-re11-extend-vs-re12`, `maintenance-floor-exception-ledger-rebaseline-disposition-c135`, `directive-1-mpi-sharding-boundary-held-batch43-confirmation`, `maintenance-floor-hygiene-sweep-cadence-per-batch-vs-per-cycle`). `cargo make book` EXIT 0 (`Build Done in 92.65 s`); **THE c135-REPAIRED TABLE-RENDER DEFECT VERIFIED FIXED** — the new row renders as a well-formed 5-cell (5-column) `<td>` row in `book/book/html/L4/index.html` (the escaped `\|` did NOT split it into an extra column); **KaTeX `$`-sigil collision did NOT recur** (`class="katex"` inside any `<pre>` = 0 across all built HTML); only the pre-existing benign KaTeX/markdown-bracket WARNs in untouched `concepts/plane-rotation-stream.md` / `concepts/step-outputs.md`; ZERO build-repairs. **THE STRONG TEE-UP for the BATCH-43 META (fires next, aggregating 133/134/135):** (1) the gated (C) sharding-MATH exploration is DISCHARGED (gate cleared both arms c133 + sketch landed c134 + navigational row c135, DIRECTIVE-1 held throughout); (2) the new node's FORMAL RE-disposition is META-OWNED — decide EXTEND RE11 §2g (to cover reference-EMITTING `roadmap_goal` leaves) vs MINT a fresh RE12 (the node is in `true_detritus`/`no_typed_edges`, NOT the reference-reached cohort of 72) + re-baseline the held-baseline-exceptions counts to the c135 disposition; (3) a FRESH forward-direction §CENTRAL ASK is warranted — THIRD consecutive batch reaching "substantive in-scope forward work substantially exhausted" (batch-41 capstone → batch-42 polish → batch-43 sharding-gate-discharged); surface (E) wind-to-maintenance (thrice-reinforced default) vs a new substantive direction the human wants; (4) methodology observation — whether the every-cycle full-hygiene sweep should move to per-batch cadence with a lightweight per-cycle two-invariant tripwire (`rank_violations` + newly-orphaned-node) now the artifact is in steady-state maintenance; the in-scope FEATURE-SURFACE SPINE remains L4-COMPLETE.**

**GRADED-STACK SNAPSHOT — CYCLE-134 (BATCH-43 / 2/3, MIDDLE): the batch-43 LEAD WAVE-2 sharding-MATH decomposition-abstraction SKETCH LANDED — a NEW rank-0 `roadmap_goal` L4 chapter `book/src/L4/sharding-decompose-reduce.md` (`subdomain_reduce = reduce ∘ restrict-to-block`), the sharding-as-decomposition-abstraction MATH, composing the firm reduce roots BY NAME via `reference`-class edges ONLY. THE HARD TRIPWIRE PASSED ON THE LANDED TREE: `rank_violations=0`, `unresolved_depends_on_targets=0` — the new rank-0 node lands reference-reachable-only (the 5 firm roots are `reference`-class edges, NO `depends-on` into the rank-0 node, so no `rank(firm)=3 > rank(roadmap_goal)=0` violation manufactured). RE-BASELINED counts (the c133 `files=385` snapshot is SUPERSEDED): `files=386` (+1 = the new chapter), `typed=325`, `untyped=61`, `roots=45`, `reachable=163` (HELD — the new node is reference-reachable-only, NOT hard-reachable, BY DESIGN), `reference_reachable=247` (HELD — reaches roots via `reference`-class edges only), `rank_violations=0`, `unresolved_depends_on_targets=0`, `promotion_frontier=11`, `detritus=123` (+1 vs c133's 122 — the new reference-reachable `roadmap_goal` node, RE11-cohort-class per §2g; the escalate-guard does NOT fire — the climb is FULLY accounted by the deliberate-reference-only-reachable new node), `true_detritus=51`, `rank_histogram.roadmap_goal=4` (was 3 — the new rank-0 node). Post-cycle-134 (batch-43 position 2/3 — the MIDDLE primary cycle of meta-batch-43; cycles 133/134/135; the batch-43 meta-phase fires AFTER cycle-135's finalize, aggregating all three; cycle counter does NOT reset). BATCH-43 DIRECTION = open the DEFERRED sharding-MATH gate (USER DECISION 2026-06-07; `project_batch43_direction_sharding_math_gate`), GATED-FIRST behind a hard spine-non-destabilization probe (cleared c133); MPI/distributed STAYS OUT (DIRECTIVE-1). **HEADLINE — the WAVE-2 sketch LANDED non-destabilizingly; the gate-CLEAR exploration is substantially complete.** **D1 (`abstractor`, the LEAD): the WAVE-2 sketch.** The new `roadmap_goal` chapter authors the sub-domain decomposition-as-restriction-and-compose math (`subdomain_reduce = reduce ∘ restrict-to-block`), composing the EXISTING firm reduce roots (`inner_product`/`linear_combination`/`gram`/`gram_reduce`/`domain_energy_reduce`) BY NAME via `reference`-class edges ONLY (the hard tripwire from both c133 probe arms — directly verified via the rank linter, not assumed). DIRECTIVE-1 honored on disk: the chapter cites the MPI/distributed mechanism (`geodata.cpp:262`, `:3230-3242`; `rap.hpp:24`; `rap.cpp:116-126`) as the deferred-future realization ONLY, NOT lifted; the active content is the MATH of decomposition. Pseudocode FENCED in ```text blocks — the KaTeX `$`-sigil collision did NOT recur (verified: `grep 'class="katex"'` on the rendered chapter returns 0 hits; 2 `<pre>` code blocks rendered). 3 forward-direction OQs promoted (`sharding-decompose-reduce-l4-index-roadmap-goal-listing` for the L4 index dep-map row; `sharding-decompose-reduce-summary-group-placement`; `sharding-decompose-reduce-solve-generalization-promotion-pull` — the solve-case generalization recorded as open intent, promotion-pull = a domain-decomposition-preconditioner consumer). **D2 (`cross-layer-cross-cutter`): MAINTENANCE FLOOR standing hygiene** — clean-bill, RE-BASELINE CONFIRMED: the c133-baseline forecast (`files→386`, roadmap_goal 3→4, `rank_violations=0`/`unresolved=0` held) CONFIRMED on disk by the D1 landing; all 3 `realizes-kernel-api` kernel-impl edges stay `reference`-class; semantic surface clean; RE4 consumer-gated (premise holds), RE11 premises hold + the new node joins the RE11 deliberate-reference-only-reachable cohort BY DESIGN (formal RE11-narrowing/new-RE disposition deferred to the batch-43 meta). `cargo make book` EXIT 0 (`Build Done in 92.80 s`); the new chapter rendered (45KB HTML, 0 KaTeX spans); only the pre-existing benign KaTeX/markdown-bracket WARNs in untouched `concepts/plane-rotation-stream.md` / `concepts/step-outputs.md`; ZERO build-repairs. **THE CARRY to c135 (the BATCH-CLOSING cycle) + the BATCH-43 META (fires after c135):** (1) with the WAVE-2 sketch landed and the gate-CLEAR exploration substantially complete, c135 is likely a thin consolidation + maintenance cycle (deepen the sketch's working context, OR a maintenance-floor-only cycle); the batch-43 meta renders the sharding-math-exploration disposition + surfaces the next §CENTRAL ASK; (2) the new node is RE11-cohort-class (deliberate reference-only-reachable per §2g) — the batch-43 meta owns the formal RE-set disposition (match to RE11 or ratify a new RE) + the held-baseline-exceptions re-baseline; (3) the DIRECTIVE-1 MPI boundary held — only the MATH lifted; the MPI mechanism cited as deferred-future ONLY (the Dörfler cross-rank bisection stays the deferred OQ).**

**GRADED-STACK SNAPSHOT — CYCLE-133 (BATCH-43 / 1/3, OPENER): the batch-43 LEAD WAVE-1 sharding-MATH non-destabilization HARD GATE is CLEAR on BOTH arms — WAVE-2 (`sharding-math-decomposition-abstraction-sketch`, roadmap_goal-class) is GREENLIT for c134. AUDIT-ONLY cycle — 3 audit-class reports, ZERO book mutation; ALL graded-stack totals HELD EXACTLY vs c127..c132 (`files=385, typed=324, untyped=61, roots=45, reachable=163, reference_reachable=247, rank_violations=0, unresolved_depends_on_targets=0, promotion_frontier=10, detritus=122, true_detritus=50`) — by design (no node touched). Post-cycle-133 (batch-43 position 1/3 — the OPENER / FIRST primary cycle of meta-batch-43; cycles 133/134/135; the batch-43 meta-phase fires AFTER cycle-135's finalize, aggregating all three; cycle counter does NOT reset). BATCH-43 DIRECTION = open the DEFERRED sharding-MATH gate (USER DECISION 2026-06-07; `project_batch43_direction_sharding_math_gate`), GATED-FIRST behind a hard spine-non-destabilization probe (this c133 LEAD WAVE-1); MPI/distributed STAYS OUT (DIRECTIVE-1). **HEADLINE — the WAVE-1 HARD GATE returned CLEAR on both arms.** **D1 (cross-layer-cross-cutter, the LEAD): the VERTICAL/cross-layer arm.** Verdict: gate-CLEAR-for-roadmap_goal-sketch (SPLIT-leaning-CLEAR). The specific Dörfler cross-rank bisection candidate = NO-CLEAR (pure MPI collective; stays the deferred OQ `dorfler-cross-rank-bisection-distributed-note-deferred`, NOT lifted); the general decomposition-reduce abstraction = CLEAR (firm `domain_energy_reduce` is the non-destabilizing precedent; a roadmap_goal-class restrict/compose sketch composing firm roots via `reference`-class edges leaves every firm node firm, `rank_violations=0` held by construction). **D2 (same-layer-cross-cutter): the LATERAL/same-layer arm.** Verdict: ALL-GREEN, ZERO-RED — no firm L4/L3/L2 reduce/fold combinator must re-root; the firm reduce primitives (`inner_product`, `linear_combination`, `gram`/`gram_reduce`, `domain_energy_reduce`) ALREADY carry the decomposition abstraction's mathematical core (the split/concatenation monoid-homomorphism over the index set), so a partition-of-the-index-set restriction is a DERIVED `reference`-class consumer (`reduce ∘ restrict`), NOT a re-root. This DISCHARGES-GREEN the D1 contingency → COMBINED gate disposition = CLEAR. **D3 (cross-layer-cross-cutter): MAINTENANCE FLOOR standing hygiene** — clean-bill, baseline HELD EXACTLY on all eleven gate counts (§2g escalate-guard does NOT fire); all 3 `realizes-kernel-api` kernel-impl edges confirmed `reference`-class on disk; semantic surface no stale path/anchor drift; RE4 consumer-gated (premise holds), RE11 premises hold. `cargo make book` EXIT 0; mdbook-linkcheck2 clean (only the pre-existing benign KaTeX bracket WARNs in untouched `concepts/plane-rotation-stream.md` / `concepts/step-outputs.md`); ZERO build-repairs; NO vocabulary count change. **THE CARRY to c134 + the BATCH-43 META (fires after c135):** (1) WAVE-2 is GREENLIT — the c134 planner dispatches `sharding-math-decomposition-abstraction-sketch` (abstractor, roadmap_goal-class, **reference-class edges to firm roots ONLY** — a mistyped `depends-on` from a firm node to the rank-0 abstraction would manufacture a `rank(firm)=3 > rank(roadmap_goal)=0` violation, the RED outcome the gate forbids; the rank-linter would catch it); the partition-of-unity precondition is a stated hypothesis (not unconditional law) + the IEEE-754 reduction-tree-pinning stays deferred; (2) the RE-BASELINE caveat (OQ `maintenance-floor-baseline-re-baseline-on-sharding-sketch-landing`): if WAVE-2 lands reference-class roadmap_goal nodes this batch the held-baseline counts WILL MOVE BY DESIGN — the next maintenance pass / the batch-43 meta must re-baseline against the batch-43 disposition, matching each new node to RE11 or a new RE; (3) the DIRECTIVE-1 MPI boundary held — no MPI/distributed content entered the artifact (`linalg/rap.*`, `utils/geodata.*`, `utils/communication.hpp` collectives, the Dörfler cross-rank bisection cited as deferred mechanism ONLY).**

**GRADED-STACK SNAPSHOT — CYCLE-132 (BATCH-42 / 3/3, BATCH-CLOSING): the §1.2.2 / CLOSURE-SIGNATURE POLISH PASS is COMPLETE — the §1.2.2-R operator-VALUE-codomain CONVERT axis is FULLY EXHAUSTED including the intro-prose tail. ALL graded-stack totals HELD vs c127/c128/c129/c130/c131 (`files=385, typed=324, untyped=61, roots=45, reachable=163, reference_reachable=247, rank_violations=0, unresolved_depends_on_targets=0, promotion_frontier=10, detritus=122, true_detritus=50`) — by design: NO node maturity/edge changed (pure prose-fidelity re-anchor). Post-cycle-132 (batch-42 position 3/3 — the THIRD/BATCH-CLOSING primary cycle of meta-batch-42; cycles 130/131/132; the batch-42 meta-phase fires AFTER this finalize, aggregating all three; cycle counter does NOT reset). **HEADLINE — the polish pass is DONE: the §1.2.2-R operator-VALUE-codomain CONVERT axis is FULLY exhausted incl. the last intro-prose residual.** **D1 (lifter)** applied ONE prose-fidelity CONVERT to the intro-prose operator-value mention in `book/src/L4-L3/fe-assemble-fold-dissolution.md:3` (`LinearOperator[N,N]` → `LinOp[(N: ...), $N]`, mirroring the file's own already-converted `:30`/`:37` signature codomains) — the last intro-prose residual the c130 (15-site) + c131 (4-site) fenced-signature sweeps did not reach. The deferred OWNER'S-CALL site (ii) `mk-matrix-free-operator-dissolution.md:151` derived-product square-op `LinOp[(N: ...), $N]` was recorded NO-CHANGE (dual-spelling intentional, §1.2.2-R-compliant, critic-cleared). **The operator-VALUE-codomain axis is now EXHAUSTED end-to-end:** `grep -rnE '\-> *LinearOperator\['` over `book/src/{L4,L3,L2}` + the `L4-L3`/`L3-L2`/`L2-L1` lowering dirs returns **0 hits** (confirmed at finalize). `cargo make book` EXIT 0; mdbook-linkcheck2 clean — the `:3` re-spell introduces no cross-file links; ZERO build-repairs. NO vocabulary count change this cycle. **THE CARRY to the BATCH-42 META (fires next, aggregating 130/131/132):** (1) the §1.2.2 / closure-signature POLISH PASS is COMPLETE — the §1.2.2-R operator-VALUE-codomain axis is FULLY exhausted (re-grep CLEAN); recommend the meta (a) formally MARK the axis COMPLETE, (b) CLOSE the closure-signature OQ cohort (`closure-signature-1.2.2-R-operator-value-CONVERT-axis-fully-exhausted-incl-intro-prose-tail`, `closure-signature-1.2.2-R-operator-value-codomain-axis-exhausted`, `closure-signature-cohort-sweep-1.2.2-R-scope-gate`, the 2 benign-style follow-ups), (c) RENDER the wind-to-maintenance disposition (the human-chosen bounded polish pass is now done; the maintenance floor is the steady-state surround), (d) consider SURFACING a fresh forward-direction §CENTRAL ASK to the human; (2) the in-scope FEATURE-SURFACE SPINE remains L4-COMPLETE.**

**GRADED-STACK SNAPSHOT — CYCLE-131 (BATCH-42 / 2/3): the §1.2.2-R OPERATOR-VALUE-CODOMAIN AXIS is now EXHAUSTED/COMPLETE — a single-dispatch residual codomain spelling-fidelity sweep finishing the cohort the c130 OPENER opened. ALL graded-stack totals HELD vs c127/c128/c129/c130 (`files=385, typed=324, untyped=61, roots=45, reachable=163, reference_reachable=247, rank_violations=0, unresolved_depends_on_targets=0, promotion_frontier=10, detritus=122, true_detritus=50`) — by design: NO node maturity/edge changed (pure prose/signature codomain-spelling fidelity). Post-cycle-131 (batch-42 position 2/3 — the SECOND primary cycle of meta-batch-42; cycles 130/131/132; the batch-42 meta-phase fires AFTER cycle-132's finalize, aggregating all three; cycle counter does NOT reset). **HEADLINE — the §1.2.2-R operator-VALUE-codomain CONVERT axis is EXHAUSTED.** **D1 (lifter)** applied the pinned §1.2.2-R ruling (`semantics/index.md:97-104`) to the 4 residual calculus-codomain sites the c130 cohort sweep did not reach: the L2 combinator constructor codomain in `book/src/L2/matrix-free-operator-apply.md:72` (`-> LinearOperator[(N: ...)]` → `-> LinOp[(N: ...), $N]`) + its `book/src/L2/index.md:143` dep-map mirror row + two result-codomain prose sites in `book/src/L4/assemble_frequency_operator.md` (`:137`, `:146`) lagging the already-bracketed signature codomain at `:99`. The deliberate carve-outs (rank-1 `{K,C,M}` record FIELDS, law-prose / conceptual-noun mentions) KEPT per §1.2.2-R clause 2. **The operator-VALUE-codomain axis is now EXHAUSTED of calculus-level opaque smells:** `grep -rnE '\-> *LinearOperator\['` over `book/src/{L4,L3,L2}` + lowering dirs returns **0 hits**. `cargo make book` EXIT 0; mdbook-linkcheck2 clean — the L2/index.md dep-map mirror row renders + links resolve; ZERO build-repairs. NO vocabulary count change this cycle. **THE CARRY to c132 + the BATCH-42 META (fires after c132, aggregating 130/131/132):** (1) the §1.2.2-R operator-VALUE-codomain axis is EXHAUSTED/COMPLETE — recommend the meta CONFIRM with the arrow-codomain grep + formally mark it COMPLETE + close the related OQs (`closure-signature-1.2.2-R-operator-value-codomain-axis-exhausted` EXHAUSTION finding; the c130-carried `closure-signature-cohort-sweep-1.2.2-R-scope-gate` now has zero in-scope residual convert sites); (2) the polish pass is nearly done; the in-scope spine remains L4-COMPLETE — the maintenance floor is the steady-state surround.**

**GRADED-STACK SNAPSHOT — CYCLE-130 (BATCH-42 OPENER / 1/3): the §1.2.2 / CLOSURE-SIGNATURE POLISH PASS BEGINS — a clean 3-front consolidation wave on the calculus surface + the inner-product anchor-stability, ALL prose/BNF/anchor-fidelity, NO node maturity moved. ALL graded-stack totals HELD vs c127/c128/c129 (`files=385, typed=324, untyped=61, roots=45, reachable=163, reference_reachable=247, rank_violations=0, unresolved_depends_on_targets=0, promotion_frontier=10, detritus=122, true_detritus=50`) — by design: NO node maturity/edge changed (D1 = BNF/semantic-surface prose; D2 = signature-spelling re-spell; D3 = anchor-fidelity hygiene). Post-cycle-130 (batch-42 position 1/3 — the OPENER / FIRST primary cycle of meta-batch-42; cycles 130/131/132; the batch-42 meta-phase fires AFTER cycle-132's finalize, aggregating all three; cycle counter does NOT reset). **HEADLINE — the user-chosen §1.2.2 / closure-signature POLISH PASS opens** (USER DECISION 2026-06-07 answering the batch-41 §CENTRAL ASK: the in-scope spine is L4-COMPLETE; the user chose the bounded calculus-surface consolidation over wind-to-maintenance and over the gated sharding-math). **D1 (layer-intro-author)** added the §1.3 `e ::=` **`op-with-params { … ; λ(x: τ_in). e } : Op[τ_in → τ_out]`** operator-VALUE introducer production + pinned the **`##### 1.2.2-R` operator-VALUE spelling RULING block** on `book/src/semantics/index.md` (the cohort-sweep scope-gate: CONVERT-if-opaque-calculus-codomain vs KEEP-rank-1-flat-dof + a discriminator) — the BNF half of the closure-signature introduction-form is now DISCHARGED end-to-end. **D2 (lifter, WAVE-2 dep D1)** executed the **§1.2.2-R codomain cohort sweep — 15 opaque `LinearOperator[…]` → bracketed `LinOp[(N: ...), $N]` / `Op[Tensor[(N: ...)] → Tensor[(N: ...)]]`** edits across `L4-L3/fe-assemble-fold-dissolution.md` (2) + `L4/fe_assemble.md` (7) + `L4-L3/mk-matrix-free-operator-dissolution.md` (4) + `L4/frequency_sweep.md` (1) + the `L4-L3/index.md` dep-map mirror row (1); documented KEEP sites preserved (intro-prose monoid carriers + bare-word conceptual nouns). **D3 (layer-intro-author)** ran the **count-owner anchor-stability sweep** — shortened the 2 long `inner_product` Part-A section-anchors in BOTH `L2/inner_product.md` + `L3/inner_product.md` (`## Specializations`, `## Consumer: nrm2 and matrix-weighted-norm`) and re-pointed **66 inbound `#fragment` links across 18 files** (28 `#specializations` + 38 `#consumer-nrm2-and-matrix-weighted-norm`) via a deterministic tree-wide `replace_all`; the out-of-scope `L4/inner_product.md` sibling long-headings DELIBERATELY left untouched (zero inbound links target them). `cargo make book` EXIT 0; mdbook-linkcheck2 clean — D3's 66-inbound-fragment anchor re-point built with ZERO dangling-fragment errors (the load-bearing safety net for the anchor sweep). NO vocabulary count change this cycle. **THE CARRY to c131/c132 + the BATCH-42 META (fires after c132, aggregating 130/131/132):** (1) batch-42 = the §1.2.2/closure-signature POLISH PASS; c131/c132 continue the polish + maintenance floor; (2) OQ to CLOSE — `closure-signature-op-with-params-bnf-promotion` BNF half DISCHARGED c130 D1 → the meta CLOSEs the parent `closure-signature-introduction-form-into-bnf-and-role-discipline-bullet` FULLY (both halves now landed); (3) OQs OPEN — `closure-signature-cohort-sweep-1.2.2-R-scope-gate` (residual convert/keep sites incl. the `divfree-projector` illustrative-prose keep-site) + 2 benign-style OQs (`fe-assemble-fold-dissolution-intro-prose-monoid-carrier-codomain-consistency`, `mk-matrix-free-dissolution-codomain-spelling-Op-vs-LinOp-uniformity`).**

**GRADED-STACK SNAPSHOT — CYCLE-129 (BATCH-41 BATCH-CLOSING / 3/3): the CLOSURE-RETURNING-SIGNATURE CONVENTION is now COMPLETE END-TO-END — §1.3.1 pinned (operator-transformer/-constructor adjudication) + the non-compliant cohort SWEPT to the bracketed `LinOp[...]` spelling + 2 stale maturity-tokens corrected. ALL graded-stack totals HELD vs c127/c128 (`files=385, typed=324, untyped=61, roots=45, reachable=163, reference_reachable=247, rank_violations=0, unresolved_depends_on_targets=0, promotion_frontier=10, detritus=122, true_detritus=50`) — by design: NO RE fired, NO node maturity/edge changed (D1/D2 = semantic-surface prose + signature-spelling re-spell + 2 stale-prose maturity-token corrections; the underlying nodes were ALREADY firm on disk, so the tokens were stale-prose NOT status flips). Post-cycle-129 (batch-41 position 3/3 — the BATCH-CLOSING / THIRD primary cycle of meta-batch-41; cycles 127/128/129; the batch-41 meta-phase fires AFTER this finalize, aggregating all three; cycle counter does NOT reset). **HEADLINE — the high-order closure-returning-signature convention is COMPLETE.** This light consolidation/cleanup cycle closed the c128-surfaced loose ends. **D1 (layer-intro-author)** pinned the operator-transformer-codomain adjudication into semantics §1.3.1 (`book/src/semantics/index.md` — bracketed `Op[...]`/`LinOp[...]` operator-value codomains = already compliant; opaque `LinearOperator[...]` type-application = the non-compliant smell, re-spell-not-wrap; appended a ruling bullet + a "Grouping" column + a third opaque-`LinearOperator[N,N]` row to the §1.3.1 table), RESOLVING OQ `oq-highorder-operator-transformer-codomain-convention`. **D2 (lifter)** swept the 7 opaque `LinearOperator[...]` codomain sites to the bracketed `LinOp[(N: ...), $N]` form across `L4/assemble_frequency_operator.md` (4 sites), `L4/fe_assemble.md` (3 sites) + the 2 narrative `L4/index.md` rows (:61 eliminate_bc reconcile, :62 fe_assemble), plus 2 evidenced stale-token corrections — `mk_matrix_free_operator` `roadmap_goal`→`firm` (c127) in `fe_assemble.md:16,164` and `boundary-mode.L4` `rough-in`→`firm` in `lifecycle.L4.md:72`. RESOLVES OQ `highorder-signature-noncompliant-cohort-c129-lifter-sweep` + the two stale-token OQs `fe-assemble-stale-mk-matrix-free-roadmap-goal-token` + `lifecycle-l4-stale-boundary-mode-rough-in-token`. **DELIBERATE within-chapter dual-spelling PRESERVED** (NOT fixed): the plain operator-VALUE record fields stay rank-1 `LinearOperator[N, N]` — routed to the META-owned whole-book §1.2.2 compliance cohort sweep OQ `closure-signature-l4-constructor-restatement-compliance-cohort-sweep`. NO vocabulary count change this cycle. **THE CARRY to the BATCH-41 META (fires next):** (1) the ASK-2 "B" L4-COMPLETE CAPSTONE VERDICT (from c128 D3) + the "wind the in-scope spine to MAINTENANCE" recommendation — the central batch-41-meta judgment; (2) the closure-signature convention is COMPLETE (§1.3.1 c128 + transformer adjudication pinned c129 + cohort swept c129) → the meta decides the 2 DEFERRED items (promote the `op-with-params {...}` introduction form into §1.3 BNF + add a harvester/abstractor closure-returning-signature discipline bullet — OQ `closure-signature-introduction-form-into-bnf-and-role-discipline-bullet`); (3) the meta-owned whole-book L4-constructor §1.2.2 compliance-cohort sweep OQ `closure-signature-l4-constructor-restatement-compliance-cohort-sweep` (D2 left the deliberate dual-spelling); (4) the OQs RESOLVED this batch for the meta to CLOSE.**

**GRADED-STACK SNAPSHOT — CYCLE-128 (BATCH-41 MIDDLE / 2/3): the USER-DIRECTIVE (2026-06-07) CLOSURE-RETURNING-SIGNATURE CONVENTION LANDS as semantics §1.3.1 + the ASK-2 "B" CAPSTONE — the in-scope FEATURE-SURFACE SPINE is L4-COMPLETE. ALL graded-stack totals HELD vs c127 (`files=385, typed=324, untyped=61, roots=45, reachable=163, reference_reachable=247, rank_violations=0, unresolved_depends_on_targets=0, promotion_frontier=10, detritus=122, true_detritus=50`) — by design: NO RE fired, no status/rank/edge changed (D1/D4 = semantic-surface prose + 3 surgical signature-spelling edits; D2/D3 = read-only audits appending only OQs). Post-cycle-128 (batch-41 position 2/3 — the MIDDLE primary cycle of meta-batch-41; cycles 127/128/129; the batch-41 meta-phase fires AFTER c129 finalize, aggregating all three; cycle counter does NOT reset). **HEADLINE-1 — the L4 calculus's high-order closure-returning signatures get a managed semantic convention.** A mid-flight USER DIRECTIVE (2026-06-07) established that the L4 calculus is high-order: closure-returning signatures use paren-grouping `foo -> (bar -> baz)` and/or the operator-value spelling `Op[τ_in → τ_out]`. **D1 (layer-intro-author)** codified this as **semantics §1.3.1 "Closure-returning signatures"** (`book/src/semantics/index.md`, a new `#### 1.3.1` subsection — the active semantic-management surface per `project_semantic_consolidation_surface`), resolved the long-standing :518 working-note operator-body gap via §1.3.1, and applied the exemplar fix: `L4/mk_matrix_free_operator`'s codomain `LinearOperator (Tensor[(N: ...)])` → `Op[Tensor[(N: ...)] → Tensor[(N: ...)]]` (operator-value form), with the lockstep fix on `feature/matrix-free-operator.L4`. **Finalize applied the two remaining lockstep consistency fixes** to keep the spelling uniform across the now-fixed cap: the `L4/index.md:119` dep-map MIRROR row + D4's new L2-pointer signature mention in `L2/matrix-free-operator-apply.md` (both `LinearOperator (...)` → `Op[...]`). **HEADLINE-2 — the ASK-2 "B" CAPSTONE: the in-scope FEATURE-SURFACE SPINE is L4-COMPLETE.** **D3 (cross-layer-cross-cutter)** ran the 5-driver L4-completeness audit (the ASK-2 "B" capstone): ALL 5 drivers (electrostatic / magnetostatic / eigenmode / driven / transient) + the lifecycle ROOT PASS — every composition stage names already-firm constituents BY NAME (12 named constituents verified firm on disk). The two non-PASS constituents are GENUINELY-TRACKED opaque-library kernels, NOT gaps: eigenmode `eigsolve` (SLEPc/ARPACK opaque, tracked RE11) + the transient per-step ODE body (`obstruction (opaque-library-ownership)` quantified-over by the firm `fold_solve`). NO GAP. The audit recommends DEFER / "wind the in-scope spine to MAINTENANCE" — the central input for the batch-41 meta's ASK-2 verdict. **D4 (lifter)** de-staled `L2/matrix-free-operator-apply`'s speculative L4-placeholder to a firm USE+LINK pointer (discharges OQ `matrix-free-operator-apply-l4-placeholder-now-stale`). **D2 (cross-layer-cross-cutter)** ran a read-only high-order-signature compliance audit (17 ops; 4 non-compliant → c129 lifter-sweep OQ `highorder-signature-noncompliant-cohort-c129-lifter-sweep` + the operator-transformer-codomain adjudication OQ `oq-highorder-operator-transformer-codomain-convention`). NO vocabulary count change this cycle.

**GRADED-STACK SNAPSHOT — CYCLE-127 (BATCH-41 OPENER / 1/3): ASK-2 "A" DEEPENS THE CONSTRUCTIVE-KERNEL LAYER — THE L4 CAP `mk_matrix_free_operator` FIRM-FLIPS (roadmap_goal→firm) AND THE matrix-free FEATURE COLUMN LANDS FIRM AT L1+L4, so the matrix-free / burn-GPU backend-lowering surface now spans `L1`(substrate ops FIRM + impl FIRM) → `L2`(combinator FIRM) → `L4`(cap FIRM + feature-column FIRM) ALL FIRM; `rank_violations` HELD 0; `unresolved_depends_on_targets` HELD 0; NO newly-orphaned node. `reachable` 157→163 (+6: the firmed cap + the 2 firm feature-column files + 3 libceed substrate ops climbing OUT of the reference-only-reachable RE11 cohort into HARD-reachable); `reference_reachable` 244→247 (+3); `detritus` 129→122 (−7); `true_detritus` 53→50 (−3); `roots` 43→45 (+2: the 2 new feature-surface columns are root markers); `files` 386→385 (net: D4 −4 deletions + 3 new files = −1); `promotion_frontier` 11→10. Post-cycle-127 (batch-41 position 1/3 — the OPENING primary cycle of meta-batch-41; cycles 127/128/129; the batch-41 meta-phase fires AFTER c129 finalize, aggregating 127/128/129; cycle counter does NOT reset). **HEADLINE — RE11 libceed-substrate sub-cohort GROUNDED.** The batch-40 RE11 promotion condition (a faithful `depends-on` consumer naming the 4 libceed substrate ops by name — the ASK-2 "A" deepen-the-layer work) FIRED this cycle: (1) **D1** firm-flipped the L4 cap `mk_matrix_free_operator` (roadmap_goal→firm, resting on the firm `L2/matrix-free-operator-apply` depends-on dep) AND added the **firm `feature/matrix-free-operator.{L1,L4}` backend-lowering feature column** — the L1 column’s 4 blocking `depends-on (composes)` edges name `element_restrict`/`basis_apply`/`quad_point_contract`/`geom_factor_build` BY NAME (a feature-surface-root-reaching faithful consumer); (2) **D2** added the firm L4>L3 theme `mk-matrix-free-operator-dissolution` (the matrix-free constructive-INTERIOR dissolution; 5 `depends-on` edges to the L2 combinator + 4 substrate ops). The 4 substrate ops + the cap + the L2 combinator are NO LONGER in the detritus list (hard-reachable). **L4 firm count +1** (`mk_matrix_free_operator` flipped roadmap_goal→firm) **+ the firm L4 feature-column file**; **L2 firm count −2** (D4 deleted `L2/dot` + `L2/nrm2`, folding both into the firm `inner_product` combinator — combinator-primary RE-style elimination; same at L3, deleting `L3/dot` + `L3/nrm2`) — the L2 dep-map is now **17 firm + 1 partly-constructive = 18 rows** (D5 prose-reconcile to the post-D4 self-summing count). D3 surgically realigned the L2 combinator’s `quad_point_contract` inline render (drop run-time `Q`, output axis `C→C’`); finalize applied the IDENTICAL faithful-render fix to D2’s theme :168 (the D3-flagged same-drift follow-up). NEW L4>L3 theme `mk-matrix-free-operator-dissolution` (L4>L3 theme tally 11→12). `cargo make book` EXIT 0; mdbook-linkcheck2 clean (D4’s 4 deletions all swept, the D1↔D2 cross-references resolve). 5 dispatches, ALL applied clean (5/5 staging rows == 5 dispatched-ready). **The matrix-free / burn-GPU backend-lowering surface is now FULLY FIRM end-to-end** — the ASK-2 "A" constructive-kernel build advances from "spine spans firm-impl→firm-combinator→roadmap_goal-cap" (c126) to "spine spans ALL-FIRM with a firm composition-root feature column."

**GRADED-STACK SNAPSHOT — CYCLE-126 (BATCH-40 CLOSER / 3/3): THE MATRIX-FREE / burn-GPU BACKEND-LOWERING SURFACE NOW SPANS L1(impl FIRM)→L2(combinator FIRM)→L4(constructor `roadmap_goal`); `rank_violations` HELD 0; `unresolved_depends_on_targets` HELD 0; `reachable` HELD 157 (no orphaning); `reference_reachable` 243→244 (post-cycle-126, batch-40 position 3/3 — THE BATCH-CLOSING primary cycle of meta-batch-40; cycles 124/125/126; the batch-40 meta-phase fires AFTER this finalize, aggregating 124/125/126; cycle counter does NOT reset).** **HEADLINE — ASK-2 "A" first L4 step lands: the L4 backend-lowering operator-CONSTRUCTOR entry point `L4/mk_matrix_free_operator` is created as a claim-free `roadmap_goal` (rank 0) with its pull-chain wired `reference`-class (`lowers-to` the firm `L2/matrix-free-operator-apply` apply-chain + `constructs-via`/`pulled-by` the firm spine consumer `L4/fe_assemble` reaching a feature root), so `rank_violations: 0` holds — a firm→roadmap_goal `depends-on` would have VIOLATED well-foundedness (`rank(fe_assemble)=firm > rank(mk)=0`); correctly reference-class. The node is reference-reachable via `L4/fe_assemble` (a deliberate §2g/RE11-style reference-only-reachable cohort member — it appears in `detritus_reference_reachable_re11_cohort`, NOT `true_detritus`; the `[garbage?]` on the depends-on-only mark is EXPECTED for a rank-0 node whose pull-chain is reference-class by requirement). 2 dispatches, BOTH applied clean (2/2 staging rows == 2 dispatched-ready; 107th consecutive clean staging). DELTAS: (1) **the L4 `roadmap_goal` cap `mk_matrix_free_operator`** (`book/src/L4/mk_matrix_free_operator.md` created; SUMMARY + L4/index alpha-inserted between `linear_combination` and `nrm2`; `fe_assemble` frontmatter+§Lowers-to wired the pull-chain) — the matrix-free / burn-GPU backend-lowering surface now spans the full stack: `L1/libceed-quadrature-kernel-impl` FIRM (c125) → `L2/matrix-free-operator-apply` FIRM (c125) → `L4/mk_matrix_free_operator` `roadmap_goal` (c126, the operator-CONSTRUCTOR surface; promotes when the dedicated L4 backend-lowering feature surface lands, batch-41 "A"). Rank-0 cap does NOT bump the L4 firm count. (2) **the now-firm kernel-impl empirical-match audit DISCHARGED** — `L1/libceed-quadrature-kernel-impl`'s `verified_against:` block upgraded `empirical-anchor-confirmed-deferred` → `empirical-match` (`test-libceed.cpp:284`, owed since c124 now the impl is firm c125 D1) + a NEW `test-libceed.cpp:328-377` apply-level harness row (the `TestCeedOperatorMult` + assembled-matrix matches) — the DIRECTIVE-3 impl↔API correspondence the lowering-verifier owed; audit-only, NO status/rank/edge change (the chapter stays firm, the `realizes-kernel-api` + `realizes-leaf` edges stay reference-class, the four `composes` depends-on deps unchanged); the kernel-api obstruction surface `L1-L0/fe-assemble-libceed-boundary-obstruction.md` stays UNTOUCHED (`status: obstruction`/`opaque-library-ownership`), DIRECTIVE-3 integrity preserved. **BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook + linkcheck2 0.12.0) EXIT 0, NO finalize build-repair (the new L4 chapter + SUMMARY/index alpha-inserts + the `fe_assemble` reference-class pull-chain + the `verified_against:` metadata upgrade ALL resolve clean; 0 dead links); only the pre-existing benign `Potential incomplete link` / KaTeX-`[k]`/`[j+1]` WARNs in unrelated files. **STEP-5b graded-stack linters on the LANDED tree:** **`rank_violations: 0`** (baseline fully discharged c096 → ANY violation NEW + BLOCK; NONE — GATE PASSES; the new L4 rank-0 cap's pull-chain edges are `reference`-class by requirement, so no firm→roadmap_goal depends-on exists) + **NO newly-orphaned node** (`reachable` HELD 157 — the new cap is reference-reachable, not a previously-reachable node gone dark; the audit-only D2 changed no edges) + **`unresolved_depends_on_targets: 0`** (HELD; no deletions, no frontmatter-edge class). TRUE CUMULATIVE (re-measured by integrator-finalize on the landed tree): `files=386 (+1 the new L4 cap), typed=325 (+1), untyped=61 (HELD), roots=43 (HELD), reachable=157 (HELD), reference_reachable=244 (243→244, +1 — the new cap reference-reachable via fe_assemble), rank_violations=0 (HELD), unresolved_depends_on_targets=0 (HELD), promotion_frontier=11, detritus=129 (true_detritus=53 [HELD], detritus_no_typed_edges_pre_p1_artifact=108, detritus_with_typed_edges_stronger_signal=21, detritus_reference_reachable_re11_cohort=76 [+1 the new cap], stronger_signal_reference_reachable=14, stronger_signal_true_detritus=7), expected_unreachable_outside_dag=48, rank_histogram={firm:224, roadmap_goal:4 (+1 the cap), typed-no-rank:84, rough-in:4, partly-constructive:3, obstruction:2, partial-obstruction:4}`. **TREND:** `rank_violations` HELD 0 (22 c094 → 0 c096 → … → 0 c124 → 0 c125 → 0 c126); `unresolved_depends_on_targets` HELD 0 (c123→…→c126); `reachable` 157 (c124→c125→c126 HELD); `reference_reachable` 235 (c124) → 243 (c125) → 244 (c126); `true_detritus` 53 (c125→c126 HELD). **BATCH-40 MEASURABLE ARC (124→126):** **L1 firm 43→47** (the 4 libCEED substrate ops), **L2 firm 22→23** (the matrix-free combinator), **new L4 `roadmap_goal` cap** (`mk_matrix_free_operator`), **RE3 FIRED + RE6 DISCHARGED + RE11 GROUNDED** (all c124), **the libCEED constructive-kernel substrate sub-spine COMPLETE**, **the matrix-free/burn-GPU backend-lowering surface now L1(impl firm)→L2(combinator firm)→L4(constructor roadmap_goal)**; `libceed-quadrature-kernel-impl` rough-in→firm (c125, kernel-impl kind) + its empirical-match audit discharged (c126). **THE CARRY to the BATCH-40 META (fires next, a SEPARATE dispatch aggregating 124/125/126) — FULL BATCH-40 CARRYOVER:** **(1) OQs to CLOSE** (meta unify-authority): `mk_matrix_free_operator-l4-backend-lowering-placeholder` (c126 D1 — a RESOLUTION MARKER was appended to the OQ ledger this cycle; the actual header-close is meta unify-authority), `batch-37-era-stale-design-l4-calculus-path-drift-sweep` (count→0, swept c125 D3), `libceed-substrate-rough-in-to-firm-flip-and-45-to-47-tally-followup` (discharged c125 D1 via the firm-flip marker). **(2) RE dispositions for `scaffolding/graded-stack-baseline-exceptions.md`** (meta write-territory; the per-report integrators FLAGGED but did NOT touch the file across all of batch-40): RE3 FIRED + RE11 (eigsolve-impl/lanczos_step) GROUNDED + RE6 DISCHARGED (all c124, via the nleps-deflated-eigensolve consumer + the arity-leaf elimination); RE4 still consumer-gated. **(3) the L2/index prose-vs-dep-map-row firm-count gap** (c125 D2 — prose ~23 firm vs ~19–20 dep-map TABLE rows; a count-reconcile hygiene candidate). **(4) the c124 "deleted-slug frontmatter-edge sweep" process note** — codify into the destructive-refactor checklist for `combinator-miner` + `integrator-per-report` (the gap that produced c124's 2 build-repairs; did NOT recur c125/c126 since no deletions). **(5) BATCH-41 forward direction (ASK-2 "A then B"):** A = deepen the constructive-kernel / matrix-free layer (the element-local rank-tensor / matrix-free assembly build, the burn-relevant column); B = the 5-driver L4-completeness audit capstone; D (P1 edge-typing / true-detritus sweep) folded in opportunistically; C (sharding-math) deferred/gated; E (maintenance) the fallback. The batch-40 meta should reshape `priorities.md` into the batch-41 head per this.** ---  **GRADED-STACK SNAPSHOT — CYCLE-125 (BATCH-40 MIDDLE): THE libCEED CONSTRUCTIVE-KERNEL SUBSTRATE SUB-SPINE IS NOW FULLY FIRM + A 2ND FAITHFUL SUBSTRATE CONSUMER (the new firm L2 combinator `matrix-free-operator-apply`, the matrix-free / burn-GPU backend-lowering surface) GROUNDS IT FURTHER; `rank_violations` HELD 0; `unresolved_depends_on_targets` HELD 0; `reachable` HELD 157 (no orphaning); `reference_reachable` 235→243 (post-cycle-125, batch-40 position 2/3 — THE MIDDLE primary cycle of meta-batch-40; cycles 124/125/126; the batch-40 meta-phase fires AFTER cycle-126's finalize, aggregating 124/125/126; cycle counter does NOT reset).** **HEADLINE — the constructive-kernel layer (ASK-2 "A": finish the constructive-kernel build) advances. 3 dispatches, ALL applied clean (3/3 staging rows == 3 dispatched-ready; 106th consecutive clean staging). DELTAS: (1) **L1 firm 45→47** — the two libCEED substrate ops `L1/element_restrict` + `L1/geom_factor_build` flipped rough-in→FIRM (sole blocking dep `concepts/element-local-tensor` firm on disk c124 D5), COMPLETING the substrate sub-spine (`element_restrict` + `basis_apply` + `quad_point_contract` + `geom_factor_build` all firm; tally 33 main + 4 FE-assembly + 5 FE-space + 1 Mesh-construction + 4 libCEED-substrate = 47). (2) **the libCEED kernel-IMPL node `L1/libceed-quadrature-kernel-impl` flipped rough-in→FIRM** (all four `depends-on (composes)` substrate deps now firm; `rank(impl) ≤ min(deps) = firm`; the `realizes-kernel-api` edge to the kept obstruction surface `L1-L0/fe-assemble-libceed-boundary-obstruction.md` stays `reference`-class — the kernel-api obstruction surface UNTOUCHED, still `status: obstruction`/`opaque-library-ownership`, DIRECTIVE-3 integrity preserved). (3) **L2 firm 22→23** — a NEW firm L2 constructive-kernel combinator `book/src/L2/matrix-free-operator-apply.md` landed (the matrix-free / burn-GPU backend-lowering surface; a 2ND faithful `depends-on (composes)` substrate consumer, further grounding it) + a new L2 by-kind group `Constructive-kernel compositions` + its group-intro `constructive-kernel-compositions-intro.md`, both SUMMARY-registered. The L2↔L1 rotation is identity-in-named-terms (a degenerate-lowering smell) → NO L2-L1 theme authored, resolved as in-line "Downward to L1" + a `reference`-class `lifts-kernel-impl` edge. (4) **GMG cross-ref hygiene** — `L1/multigrid-relaxation-smoother.md:113` re-pointed the last stale `design/l4_calculus.md §1.2.2` ref → live `semantics/index.md §1.2.1` (double-correction: path AND section). **STALE-PATH SWEEP COMPLETE: `grep -rn 'design/l4_calculus' book/src/` is now 0 (was 1 — the batch-37-era drift residual CLEARED; OQ `batch-37-era-stale-design-l4-calculus-path-drift-sweep` flagged for batch-40 meta CLOSE).** Two NEGATIVE-finding combinator-miner records (V-cycle do-not-mine — single instance gmg.cpp:172; GMG-smoother-L3-home already-covered). **BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook + linkcheck2 0.12.0) EXIT 0, NO finalize build-repair (the new L2 chapter + group-intro + their SUMMARY/index inserts + the firm-flips + the stale-path re-point ALL resolve clean; 0 dead links); only the pre-existing benign `Potential incomplete link` / KaTeX-adjacent WARNs in unrelated files. **STEP-5b graded-stack linters on the LANDED tree:** **`rank_violations: 0`** (baseline fully discharged c096 → ANY violation NEW + BLOCK; NONE — GATE PASSES; the substrate firm-flips rest on the firm `element-local-tensor`, the kernel-impl on its four-firm substrate, the new L2 combinator on its four-firm substrate constituents) + **NO newly-orphaned node** (`reachable` HELD 157 — no previously-reachable node went dark; the firm-flips are promotions of already-reachable nodes) + **`unresolved_depends_on_targets: 0`** (HELD; no deleted-slug frontmatter-edge class this cycle — no deletions). TRUE CUMULATIVE (re-measured by integrator-finalize on the landed tree): `files=385 (+2: the new L2 chapter + group-intro), typed=324 (+2), untyped=61 (HELD), roots=43 (HELD), reachable=157 (HELD), reference_reachable=243 (235→243, +8 — the new firm L2 chapter + substrate navigational reach), rank_violations=0 (HELD), unresolved_depends_on_targets=0 (HELD), promotion_frontier=10, detritus=128 (true_detritus=53; detritus_no_typed_edges_pre_p1_artifact=107, detritus_with_typed_edges_stronger_signal=21, detritus_reference_reachable_re11_cohort=75, stronger_signal_reference_reachable=14, stronger_signal_true_detritus=7), expected_unreachable_outside_dag=48, rank_histogram={firm:224, roadmap_goal:3, typed-no-rank:84, rough-in:4, partly-constructive:3, obstruction:2, partial-obstruction:4}`. **TREND:** `rank_violations` HELD 0 (22 c094 → 0 c096 → … → 0 c123 → 0 c124 → 0 c125); `unresolved_depends_on_targets` HELD 0 (c123→c124→c125); `reachable` 158 (c123) → 157 (c124) → 157 (c125, HELD); `reference_reachable` 235 (c124) → 243 (c125). **RE DISPOSITION (carried, owed to the batch-40 meta):** RE3/RE11/RE6 dispositions (FIRED/GROUNDED/DISCHARGED at c124) STILL need the batch-40 meta to record in `scaffolding/graded-stack-baseline-exceptions.md` (meta write-territory; the per-report integrators FLAGGED but did NOT touch the file). **NOTES-FOR-META:** (a) the **L2/index prose-vs-dep-map-row firm-count gap** (prose ~23 firm vs ~19–20 firm dep-map TABLE rows — a pre-existing count-reconcile hygiene candidate, surfaced by D2; the prose narrative count and the dep-map table count have diverged); (b) the owed lowering-verifier re-audit of the now-firm kernel-impl empirical-match (the DIRECTIVE-3 impl-realizes-API correspondence on the FIRM impl); (c) the speculative L4 `mk_matrix_free_operator` held as OQ `mk_matrix_free_operator-l4-backend-lowering-placeholder` for c126/batch-41 (land as roadmap_goal once the L4 backend-lowering feature surface provides the pull); (d) the c124-flagged "deleted-slug frontmatter-edge sweep" process note for the destructive-refactor checklist (no deletions this cycle, so it did NOT recur — carried for the checklist codification). **COUNTS: firm-count flips this cycle** — L1 firm **45→47** (`element_restrict` + `geom_factor_build`); the `libceed-quadrature-kernel-impl` rough-in→firm (kernel-impl kind, tracked separately); L2 firm **22→23** (`matrix-free-operator-apply`); the substrate sub-spine COMPLETE. SLICE CORPUS: 0. 3 reports applied clean; zero deferrals, zero rejections, zero per-report gate-hits, ZERO finalize build-repairs. **THE CARRY to c126 (batch-40 position 3/3, THE BATCH-CLOSING cycle) + the batch-40 meta: (1) UPDATE `graded-stack-baseline-exceptions.md` for RE3/RE11/RE6 (meta write-territory, carried from c124); (2) the L2/index prose-vs-dep-map-row count reconcile (note (a)); (3) the owed lowering-verifier kernel-impl empirical-match re-audit (note (b)); (4) the speculative L4 `mk_matrix_free_operator` placeholder (note (c), c126/batch-41); (5) CLOSE the `batch-37-era-stale-design-l4-calculus-path-drift-sweep` + `libceed-substrate-rough-in-to-firm-flip...` OQs (both discharged this cycle); (6) ASK-2 forward direction — the constructive-kernel substrate layer + the first matrix-free L2 combinator are now firm; c126 advances toward the 5-driver L4-completeness audit capstone ("B").** ---  **GRADED-STACK SNAPSHOT — CYCLE-124 (BATCH-40 OPENER): RE3 FIRED + RE11 GROUNDED + RE6 DISCHARGED + THE libCEED CONSTRUCTIVE-KERNEL SUBSTRATE FIRMED (L1 firm 43→45 + 2 rough-in substrate ops + the kernel-impl rough-in + the element-local-tensor record page) + the nleps-deflated-eigensolve L3 composition-root; `rank_violations` HELD 0; `unresolved_depends_on_targets` 2→0 (a finalize build-repair); ASK-1 `--reference-reachable` tier now reported (reference_reachable=235); residual RE set after this cycle = RE3 FIRED + RE11 GROUNDED + RE6 DISCHARGED (post-cycle-124, batch-40 position 1/3 — THE OPENER/FIRST primary cycle of meta-batch-40; cycles 124/125/126; the batch-40 meta-phase fires AFTER cycle-126's finalize, aggregating 124/125/126; cycle counter does NOT reset).** **HEADLINE — the FIRST cycle of batch-40 (ASK-2 "A then B": finish the constructive-kernel layer THEN the 5-driver L4-completeness capstone). 7 dispatches, ALL applied clean (7/7 staging rows == 7 dispatched-ready; 105th consecutive clean staging). (D1, harvester, `deflate-nleps-consumer`) landed `book/src/L3/nleps-deflated-eigensolve.md` — the NEW L3 composition-root (rank `roadmap_goal` BY DESIGN, §(h) capped by the rank-0 `eigsolve-impl` seed) whose faithful blocking `depends-on (composes)` edges (a) FIRE **RE3** (the `deflate → L2/gram` faithful constituent edge becomes reachable through a built consumer) and (b) GROUND **`L3/eigsolve-impl` + `L3/lanczos_step`** off the RE11 reference-only-reachable cohort (this consumer is the first faithful `depends-on` consumer of `eigsolve-impl` direct + `lanczos_step` transitive via the `folds` edge). (D2, lowering-verifier, `eigsolve-impl-correspondence`) audited D1's wiring — appended a fresh `verified_against:` block to the consumer chapter (5 entries) + one entry into `eigsolve-impl`'s existing block (→8); CONFIRMED the `realizes-kernel-api` edges stay `reference`-class and the consumer's `depends-on` edges are faithful (edge-integrity PASS + consumer-faithfulness PASS); no promotions/new edges. (D3, harvester, `basis-apply-quad-contract`) PROMOTED `L1/basis_apply` + `L1/quad_point_contract` roadmap_goal→**FIRM** (firm-on-positive-structure escape; syntactic-identity laws on read-not-constructed positive libCEED source) + the stale `design/l4_calculus.md §1.2.1` → live `semantics/index.md §1.2.1` path fix + repaired EvalMode/apply-QFunction pinpoints. (D4, harvester, `element-restrict-geom-factor`) PROMOTED `L1/element_restrict` + `L1/geom_factor_build` roadmap_goal→**ROUGH-IN** (the honest one-rank climb; well-foundedness caps them at rough-in while their shape home `concepts/element-local-tensor` firms this same wave — a `depends-on (shape-vocabulary)` edge) + the same stale-path fix + re-anchored build-QFunction citations. (D5, layer-intro-author, `element-local-tensor`) CREATED `book/src/concepts/element-local-tensor.md` FIRM (the `[E,L]`/`[E,P,C]`/`[E,P,G]` element-local rank-tensor shape-family record-definition home; resolves D3's firm live-links + D4's rough-in bare-slug forward-refs) + PROMOTED `L1/libceed-quadrature-kernel-impl` roadmap_goal→**ROUGH-IN** (4 depends-on (composes) deps = 2 firm + 2 rough-in → min=rough-in, well-foundedness caps it; `realizes-kernel-api`/`realizes-leaf` reference edges UNTOUCHED, DIRECTIVE-3 integrity preserved) + the new semantic §1.2.3 "Named axes of fixed meaning (the element-local family)" (USE+LINK per the SEMANTIC CONSOLIDATION discipline — states the convention, links the record page, does NOT restate the axis table) + SOLE-OWNED L1/index consolidated tally (grand-total 43→45 firm + new libCEED-substrate sub-spine; cohort header "— 4 roadmap_goal" → "DRAINED — 2 firm + 2 rough-in"). (D6, combinator-miner, `re6-arity-refactor`) **DISCHARGED RE6** — the 8 `linear_combination` arity-leaf standalone nodes (`scal`/`axpy`/`axpby`/`axpbypcz` at BOTH L2 and L3) ELIMINATED (delete-not-ground, the higher-value disposition): per-arity unique-L0 anchors (incl. the load-bearing γ==0 collapse `vector.cpp:749-751` + α==1.0 fast-path `vector.cpp:702-712` + the L3 scal live-consumer sites) FOLDED INTO the combinator §arity-specializations at each layer, the 8 standalone chapters `git rm`'d, SUMMARY + L2/L3 index dep-maps DE-REGISTERED, ~90 inbound links re-pointed to `linear_combination.md#arity-specializations`. (D7, layer-intro-author, `gmg-hygiene`) cheap-hygiene bundle (zero maturity/GC/rank impact): 4 new `reference`-class `L2/correction_step` down-links on the GMG L4/L1 + multigrid-relaxation-smoother (2 as EXPLICIT downward-annotation NON-edges) + 3 stale `ido==99` cite corrections `:330-333`→`:331-334`.** **BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook + linkcheck2 0.12.0) EXIT 0 AFTER TWO surgical finalize build-repairs — **two STALE FRONTMATTER `depends-on` edges to deleted RE6 leaf slugs that D6's body/index re-point sweep MISSED** (`L3/normalize depends-on L3/scal` and `L3/orthogonalize depends-on L3/axpy`); these are frontmatter typed edges, lint-INVISIBLE to linkcheck2 (so the build was clean) but SURFACED by the graded-stack linter as `unresolved_depends_on_targets: 2`; repaired by re-pointing both to the surviving consolidation target `L3/linear_combination` (both `normalize` firm + `orthogonalize` partial-obstruction rest faithfully on firm `linear_combination` — well-foundedness HOLDS), exactly parallel to D6's ~90 body re-points, NOT new authoring; `unresolved` 2→0 after repair. All other touched files linkcheck-clean (D5's new concepts page + SUMMARY insert, D1's new L3 chapter + SUMMARY insert, D6's de-registration + ~90 re-points, the 8 deletions — 0 dead links, 0 surviving links to any deleted leaf); only the pre-existing benign `Potential incomplete link` / KaTeX-adjacent WARNs in unrelated files. **STEP-5b graded-stack linters on the LANDED tree (ASK-1 `--reference-reachable` tier now active):** **`rank_violations: 0`** (baseline fully discharged c096 → ANY violation NEW + BLOCK; NONE — GATE PASSES; the firm libCEED substrate ops rest on firm/L0-ground deps, the rough-in ops + kernel-impl rest on the now-firm element-local-tensor, the nleps consumer roadmap_goal rests-on-anything vacuously) + **NO newly-orphaned node** (the RE6 deletions are INTENTIONAL node removals, not orphanings; the RE3/RE11-grounding + substrate nodes are new/promoted this cycle, not previously-reachable-gone-dark) + **`unresolved_depends_on_targets: 0`** (2→0 after the finalize repair). TRUE CUMULATIVE (re-measured by integrator-finalize on the landed tree): `files=383 (−6 net: +2 new [nleps-deflated-eigensolve, element-local-tensor] −8 RE6 deletions), typed=322, untyped=61 (HELD), roots=43 (HELD), reachable=157, reference_reachable=235 (ASK-1 tier), rank_violations=0 (HELD), unresolved_depends_on_targets=0, promotion_frontier=13, detritus=127 (true_detritus=59; detritus_no_typed_edges_pre_p1_artifact=107, detritus_with_typed_edges_stronger_signal=20, detritus_reference_reachable_re11_cohort=68), expected_unreachable_outside_dag=47, rank_histogram={firm:220, roadmap_goal:3, typed-no-rank:83, rough-in:7, partly-constructive:3, obstruction:2, partial-obstruction:4}`. **TREND:** `rank_violations` HELD 0 (22 c094 → 0 c096 → … → 0 c123 → 0 c124); `unresolved_depends_on_targets` 0 (c123) → 2 (D6 frontmatter-edge miss) → 0 (finalize repaired); `reachable` 158 (c123) → 157 (c124); `reference_reachable` now reported (235) per ASK-1/cca2fa8. **RE DISPOSITION (the central batch-40-meta signal):** **RE3 FIRED** (deflate→gram constituent reachable through the built nleps consumer); **RE11 GROUNDED** (`eigsolve-impl` direct + `lanczos_step` transitive now have a faithful `depends-on` consumer); **RE6 DISCHARGED** (8 arity-leaf nodes eliminated into the combinator). The batch-40 META MUST update `scaffolding/graded-stack-baseline-exceptions.md` (meta write-territory) to mark RE3 + the eigsolve-impl/lanczos_step RE11 rows + RE6 per the rebuilt graph (the per-report integrators FLAGGED but did NOT touch the baseline-exceptions file). **COUNTS: firm-count flips this cycle** — L1 firm **43→45** (`basis_apply` + `quad_point_contract`); +2 L1 rough-in substrate ops (`element_restrict`, `geom_factor_build`); `libceed-quadrature-kernel-impl` roadmap_goal→rough-in (kernel-impl kind, tracked separately); +1 firm concepts record page (`element-local-tensor`); +1 new L3 roadmap_goal composition-root (`nleps-deflated-eigensolve`); **−8 L2/L3 standalone arity-leaf nodes (RE6 elimination)**. SLICE CORPUS: 0. 7 reports applied clean; zero deferrals, zero rejections, zero per-report gate-hits, TWO finalize build-repairs (the RE6 frontmatter-edge re-points). **THE CARRY to c125 (batch-40 position 2/3) + the batch-40 meta: (1) UPDATE `graded-stack-baseline-exceptions.md` for RE3/RE11/RE6 (meta write-territory); (2) the 45→47 firm flip (OQ `libceed-substrate-rough-in-to-firm-flip-and-45-to-47-tally-followup` — D4's 2 rough-in substrate ops + the kernel-impl qualify for rough-in→firm now that the shape home is firm on disk, a c125 cross-report rank-propagation pick); (3) the `batch-37-era-stale-design-l4-calculus-path-drift-sweep` OQ (a meta `grep -rn 'design/l4_calculus' book/src` enumeration); (4) the `interpolator-backward-reference-note-trim-target-unidentified` OQ (next planner specifies the file:line or confirms moot); (5) the `inner-product-family-re-style-elimination-candidate` OQ (the RE6-style dot/nrm2 follow-on); (6) the optional D6 non-blocking stale bare-code prose-mention readability sweep (`grep -rn 'book/src/L[23]/\(scal\|axpy\|axpby\|axpbypcz\)\.md' book/src`); (7) ASK-2 forward direction — having finished the constructive-kernel substrate layer this cycle, the matrix-free assembly / element-local rank-tensor build then the 5-driver L4-completeness audit capstone.** ---  **GRADED-STACK SNAPSHOT — CYCLE-123 (BATCH-39 CLOSING): RE2+RE8 DISCHARGED VIA A REAL DEPENDS-ON REACHABILITY FLIP (the krylov-iteration infrastructure column) + AMR NAVIGATIONAL HYGIENE LANDED + the RefinementData record-definition page; `rank_violations` HELD 0; `unresolved_depends_on_targets` HELD 0; the REFERENCE-EDGE-LIVENESS scheme question now has a CLEAN CONTRASTING DATA CLASS (depends-on flip vs reference-only-reachable); residual RE set = RE3 + RE6 (post-cycle-123, batch-39 position 3/3 — THE BATCH-CLOSING/THIRD primary cycle of meta-batch-39; cycles 121/122/123; the batch-39 meta-phase fires AFTER this finalize, aggregating 121/122/123; cycle counter does NOT reset).** **HEADLINE — the THIRD and FINAL cycle of the 2026-06-07 RE-SCOPE batch; the L3 iteration-views consumer + the carried-over AMR navigational cleanup + the record-definition home. 4 dispatches, ALL applied clean (4/4 staging rows == 4 dispatched-ready; 104th consecutive clean staging). (D2, layer-intro-author, `krylov-iteration-column`) landed the KRYLOV-ITERATION INFRASTRUCTURE FEATURE COLUMN at L4+L1 (`book/src/feature/krylov-iteration.{L4,L1}.md`, `feature_root: seed`, `rank: rough-in`; the L3 iteration-rotation composition-root) — its blocking `depends-on (composes)` edges to `L3/krylov-step` + `L3/fold_solve` + `L3/orthogonalize` produced a GENUINE depends-on root→node REACHABILITY FLIP that DISCHARGES RE2 (`L3/orthogonalize`) + RE8 (`L3/krylov-step`, `L3/fold_solve`) — all three confirmed REACHABLE on the LANDED tree (out of the STRONGER/detritus lists); mechanically DISTINCT from the c122 reference-only-reachable cohort. The column landed `rough-in` (NOT firm): well-foundedness CAPS it — its blocking L3 deps are `partial-obstruction` (≈2.5), so `firm` would VIOLATE `rank(u) ≤ min deps` — the firm-vs-rough-in adjudication vs the GMG firm-on-positive-structure precedent (where the blocking dep was firm) is OQ `krylov-iteration-rough-in-vs-firm-over-partial-obstruction-iteration-views`, routed to the batch-39 meta (NOT silently resolved). `L3/eigsolve-impl` + `L3/lanczos_step` correctly STAYED detritus (the column's `reference` edges to them gained NO liveness — the correct second data class). (D1, layer-intro-author, `amr-estimate-mark-group-intro`) landed the DEFERRED-FROM-c122 AMR navigational hygiene: created `book/src/L1/amr-estimate-mark-intro.md` (a `navigational-container (group intro)`, NO `rank:`, `reference`-only to the 2 AMR verbs) + re-nested the 2 flat AMR verbs under the `AMR estimate / mark vocabulary` SUMMARY grouping + de-staled the `index.md` dep-map group header (`**Rough-in (AMR estimate/mark vocabulary)**` → `**AMR estimate/mark vocabulary**`; both verb rows already `firm`) — CLOSES OQ `amr-estimate-mark-group-intro-needs-authoring`. (D4, layer-intro-author, `refinement-data-concepts-page`) landed `book/src/concepts/RefinementData.md` FIRM (the AMR config record-definition page, ≥2-consumer bar met by `dorfler_mark` + `amr-estimate-mark-refine` + the lifecycle.L4 AMR fold) + re-pointed the consumers' θ-field / `RefineConfig` references to the new page (struct cite `:97-119`/`:96-125` → `:97-154`) — CLOSES OQ `record-RefinementData-needs-concept-definition-home`. (D3, same-layer-cross-cutter, `correction-step-wider-propagate`) PURE OBSERVATION (no book edit): CLOSED 2 OQs (`correction-step-wider-replace-and-propagate-set-l1-and-feature-column` c122-opened + `correction-step-replace-and-propagate-scope` c121-opened, transitive) + promoted the NEW OQ `correction-step-l4-reference-edge-adds-to-reference-only-reachable-liveness-evidence` flagging the candidate L4 `feature/geometric-multigrid-preconditioner.L4 → L2/correction_step` reference down-link as COMPLEMENTARY reference-only-reachable meta-evidence (the candidate edge is NOT enacted this cycle — surfaced for a future producer dispatch).** **BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook + linkcheck2 0.12.0) EXIT 0, NO finalize build-repair (D1's group-intro re-nest + D4's RefinementData page + D1's link to it + the new krylov-iteration column SUMMARY/index entries ALL resolve clean; 0 dead links); only the pre-existing benign `Potential incomplete link` / HTML-tag-in-prose WARNs in unrelated files (+ one benign `<key>` definition-list HTML-tag WARN in the new `concepts/RefinementData.md`, NOT a linkcheck failure). **STEP-5b graded-stack linters on the LANDED tree:** **`rank_violations: 0`** (baseline fully discharged c096 → ANY violation NEW + BLOCK; NONE — GATE PASSES; the krylov-iteration column landed `rough-in` PRECISELY to keep `rank(u) ≤ min deps` over its partial-obstruction L3 deps; RefinementData firm rests only on `cites-evidence` L0-ground depends-on, vacuous) + **NO newly-orphaned node** (no previously-reachable node went dark; the RE2/RE8 nodes FLIPPED reachable — a gain, not a loss; `L3/eigsolve-impl`+`L3/lanczos_step` stayed detritus as designed). TRUE CUMULATIVE (re-measured by integrator-finalize on the landed tree): `files=389 (+4), typed=328 (+4), untyped=61 (HELD), roots=43 (+2 the krylov-iteration L4/L1 columns carry feature_root: seed), reachable=158, rank_violations=0 (HELD), unresolved_depends_on_targets=0 (HELD), promotion_frontier=14, detritus=132, rank_histogram={firm:225, roadmap_goal:7, typed-no-rank:83, rough-in:4, partly-constructive:3, obstruction:2, partial-obstruction:4}`. **⟢ THE TWO BATCH-39-META HEADLINE ITEMS:** **(a) the REFERENCE-EDGE-LIVENESS SCHEME QUESTION (c122 headline, persists, NOW WITH A CLEAN CONTRASTING DATA CLASS):** firm/roadmap_goal nodes reachable from roots ONLY via `reference`-class edges (combinator-primary specialization-notes, `realizes-kernel-api`, kernel-impl realizes, the c123-D3 candidate L4 `correction_step` down-link) are flagged `[GARBAGE*]` by the depends-on-only GC — do reference-class edges to root-reachable nodes count toward liveness? The c123-D2 RE2/RE8 discharge gave a clean CONTRASTING data class (a REAL depends-on reachability flip — krylov-step/fold_solve/orthogonalize → REACHABLE) mechanically distinct from the reference-only-reachable cohort (eigsolve-impl/lanczos_step stayed detritus; the correction_step candidate would stay detritus too). The batch-39 meta now has BOTH classes side-by-side to adjudicate the accounting decision. **(b) the ROUGH-IN-vs-FIRM-OVER-PARTIAL-OBSTRUCTION question (c123-D2 OQ `krylov-iteration-rough-in-vs-firm-over-partial-obstruction-iteration-views`):** the krylov-iteration column landed `rough-in` (well-foundedness caps it — its blocking L3 deps are `partial-obstruction`) vs the GMG firm-on-positive-structure precedent (where the blocking dep was firm) — meta adjudication. **TREND:** `rank_violations` HELD 0 (22 c094 → 0 c096 → … → 0 c121 → 0 c122 → 0 c123); `unresolved_depends_on_targets` HELD 0 (c122→c123); `reachable` 150 (c122) → 158 (c123, RE2/RE8 flip + new column nodes). **RE-DISCHARGE STATE (the batch-39 close):** **RE2 + RE8 DISCHARGED this cycle** via the krylov-iteration column's real depends-on reachability flip (distinct from the reference-only-reachable cohort). **CUMULATIVE BATCH-39 RE DISCHARGES:** RE10 (c121) + RE9/RE1/RE5/RE7 GROUNDED (c121-c122 GMG) + RE2/RE8 (c123). **RESIDUAL RE SET = RE3** (deflate/NLEPS, consumer-gated — lifts when its NLEPS-deflated-eigensolve/preconditioner consumer surfaces) **+ RE6** (axpy-family arity leaves — the combinator-arity-notes refactor). The batch-36→38 "RE set = permanent faithful floor" framing is fully superseded: under DIRECTIVE-2 the in-scope RE set was a DISCHARGE TARGET, and across batch-39 the lift-through campaign discharged/grounded 8 of the 10, leaving 2 genuinely consumer-/refactor-gated residuals. **COUNTS: firm-count flips this cycle** — +1 firm concepts page (`RefinementData`); the krylov-iteration column landed `rough-in` (Infrastructure kind, tracked separately; +2 GC roots → 43); 0 main-cohort firm promotions (D2 rough-in, D1 navigational+hygiene, D4 concepts page, D3 observation-only). SLICE CORPUS: 0. 4 reports applied clean (4/4 staging rows == 4 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur; 104th consecutive clean staging); zero deferrals, zero rejections, zero per-report gate-hits, ZERO finalize build-repairs. **THIS IS THE BATCH-CLOSING FINALIZE OF META-BATCH-39; THE BATCH-39 META-PHASE FIRES NEXT (a SEPARATE dispatch, aggregating 121/122/123). THE CARRY to the batch-39 meta: (1) the REFERENCE-EDGE-LIVENESS SCHEME QUESTION — now adjudicable with BOTH data classes (depends-on flip [c123-D2] vs reference-only-reachable [c122 cohort + c123-D3 correction_step candidate]); (2) the ROUGH-IN-vs-FIRM-over-partial-obstruction adjudication (OQ `krylov-iteration-...`); (3) the residual RE3 (deflate/NLEPS, consumer-gated) + RE6 (axpy-arity-notes refactor) — the only 2 remaining baseline-exceptions after batch-39's 8 discharges; (4) the candidate L4 `correction_step` reference down-link + the 2 L1 downward annotations (a future producer dispatch, per the closed `correction-step-wider-...` OQ); (5) the carried optional `eigsolve-impl-reference-uplink-to-krylov-iteration-column` cosmetic uplink.** ---  **GRADED-STACK SNAPSHOT — CYCLE-122: THE LIFT-THROUGH CAMPAIGN CONTINUES — CONSUMER-WIRING + AMR-VERB HARVEST + GMG FIRM; `unresolved_depends_on_targets` 6→0; `rank_violations` HELD 0; the REFERENCE-EDGE-LIVENESS SCHEME QUESTION surfaces as the headline batch-39-meta item (`detritus` ≈123→136 entirely from reference-only-reachable firm/root nodes; post-cycle-122, batch-39 position 2/3 — the SECOND primary cycle of meta-batch-39; cycles 121/122/123; the batch-39 meta-phase fires AFTER cycle-123's finalize, aggregating 121/122/123; cycle counter does NOT reset).** **HEADLINE — the SECOND cycle of the 2026-06-07 RE-SCOPE; the c121 fan-out's consumer-wiring + grounding follow-through. 7 dispatches, ALL applied clean. (D4, harvester, `libceed-substrate-ops`) landed the 4 shared-substrate ops `element_restrict`/`basis_apply`/`quad_point_contract`/`geom_factor_build` (all rank-0 `roadmap_goal`, the G/Gᵀ · B/Bᵀ · D · geometry-factor stages of the matrix-free FE contraction) — the libceed-quadrature-kernel-impl's `unresolved_depends_on_targets` 6→2; the consumer's stale "Speculative L1 operators" prose re-anchored to live links. (D6, lowering-verifier, `libceed+eigsolve-kernel-api-audit`) appended `verified_against:` structured-audit YAML blocks to `L1/libceed-quadrature-kernel-impl` (8 entries) + `L3/eigsolve-impl` (7 entries) — the DIRECTIVE-3 impl-realizes-API correspondence audit; both `realizes-kernel-api` edges confirmed `reference`-class, both kernel-api statuses preserved (`obstruction`/`partial-obstruction`). (D5, lowering-verifier, `smoother-kernel-api-audit`) appended an 8-entry `verified_against:` block to `L1/multigrid-relaxation-smoother` + 2 carry-forward off-by-one citation corrections (distrelaxation.cpp `:103→:102`, `:121-152→:121-151`). (D3, harvester, `correction_step`) PROMOTED `L2/correction_step` rough-in→**FIRM** (the `y + B·(x − A·y)` step-kernel combinator; depends-on `L1/apply_linop` + `L1/axpby`) + REPLACE-AND-PROPAGATE into `L2/chebyshev-iteration` (re-expressed as `correction_step` with `B = p_order(D⁻¹A)`) + `L2/jacobi-smoother` (the `B`-slot, `B = ω·D⁻¹`); L2 firm 21→22; 3 c121 correction_step OQs SETTLED. (D7, layer-intro-author, `gmg-promotion-eval`) PROMOTED the GEOMETRIC-MULTIGRID PRECONDITIONER column rough-in→**FIRM** at L4+L1 (`feature_root: seed` KEPT) + the central faithful edge re-type: `L3/chebyshev` + `L2/jacobi-smoother` `depends-on (composes)` → `reference` (sibling iteration-VIEWS, not blocking deps — the smoother's own depends-on points at the firm `L1/chebyshev-smoother`) — RE1 re-stated (L2/L3 iteration-views absorbed-below-spine like RE5/RE7). (D1, harvester, `flux_recovery_estimate`) landed `L1/flux_recovery_estimate` FIRM (the ZZ flux-recovery a-posteriori AMR estimate verb; depends-on ksp_solve/apply_linop/nrm2) — `unresolved` 2→1. (D2, harvester, `dorfler_mark`) landed `L1/dorfler_mark` FIRM (the Dörfler bulk-marking verb) — `unresolved` 1→**0** + FIRED the `L1-L0/amr-estimate-mark-refine` THEME FIRM-FLIP rough-in→firm (both L1 endpoints now firm; the MFEM-opaque refine leg stays a documented obstruction sub-leaf that does NOT gate, per the theme's own stated two-endpoint gate). Both AMR verbs registered FLAT in SUMMARY (the `amr-estimate-mark-intro.md` by-kind group-intro is DEFERRED to a c123 layer-intro-author pick — confirmed NO dangling group link, `grep -c amr-estimate-mark-intro SUMMARY.md = 0`).** **BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook + linkcheck2 0.12.0) EXIT 0, NO finalize build-repair (the FLAT AMR-verb SUMMARY fallback + the 4 substrate cohort entries + correction_step alpha-insert + the GMG firm flip all resolve clean; 0 dead links); only the pre-existing benign `Potential incomplete link` / `j+1` KaTeX-adjacent WARNs in unrelated `concepts/plane-rotation-stream.md` + `concepts/step-outputs.md` (NOT cycle-122 files). **STEP-5b graded-stack linters on the LANDED tree:** **`rank_violations: 0`** (baseline fully discharged c096 → ANY violation NEW + BLOCK; NONE — GATE PASSES; correction_step/flux_recovery_estimate/dorfler_mark firm rest on firm deps; the 4 substrate roadmap_goals rest-on-anything vacuously; the GMG firm flip is well-founded — all 5 depends-on constituents firm, the 2 demoted edges now reference-class) + **NO newly-orphaned node** (no previously-reachable node went dark — the firm/root nodes flagged this cycle are reachable-via-reference, NOT previously-depends-on-reachable-now-gone). TRUE CUMULATIVE (re-measured by integrator-finalize on the landed tree): `files=385 (+7), typed=324 (+7), untyped=61 (HELD), roots=41 (HELD), reachable=150, rank_violations=0 (HELD), unresolved_depends_on_targets=0 (6→0; ALL libceed substrate + both AMR verbs resolved), promotion_frontier=12, detritus=136 (detritus_no_typed_edges_pre_p1_artifact=108, detritus_with_typed_edges_stronger_signal=28, expected_unreachable_outside_dag=47), rank_histogram={firm:224, roadmap_goal:7, typed-no-rank:82, rough-in:2, partly-constructive:3, obstruction:2, partial-obstruction:4}`. **⟢ THE HEADLINE BATCH-39-META ITEM — THE REFERENCE-EDGE-LIVENESS SCHEME QUESTION.** Multiple genuinely-firm c122 nodes (`correction_step`, `flux_recovery_estimate`, `dorfler_mark`, the `amr-estimate-mark-refine` theme, the 4 libceed substrate `roadmap_goal`s, the GMG-re-typed `L3/chebyshev` + `L2/jacobi-smoother` iteration-views) are flagged `[GARBAGE*]` by the **depends-on-only** reachability GC because they reach the feature roots ONLY via `reference`-class edges (the combinator-primary specialization-note edges, the `realizes-kernel-api` edges, the kernel-impl realizes edges). `detritus` climbed ≈123→136 across the cycle (127→128 [D3 correction_step]→134 [D7 GMG re-type]→135 [D1 flux]→136 [D2 dorfler+AMR theme], per the staging rows' linter runs). **This is NOT a new rank violation (HELD 0) and NOT a per-report defect — it is a genuine SCHEME QUESTION the batch-39 meta-phase MUST adjudicate: do `reference`-class edges to firm / root-reachable nodes count toward liveness?** Under the current depends-on-only mark-sweep, the combinator-primary model (specializations `reference` their combinator; an entry's specializations don't `depends-on` it) + the DIRECTIVE-3 dual-surface model (`realizes-kernel-api`/realizes edges are deliberately free `reference`-class so the impl doesn't block on the opaque API) systematically produce firm nodes that are correctly-modelled-but-GC-unreachable. The accounting decision is the batch-39-meta's headline. **TREND:** `rank_violations` HELD 0 (22 c094 → 0 c096 → … → 0 c120 → 0 c121 → 0 c122); `unresolved_depends_on_targets` 6 (c121) → **0 (c122)**; `detritus` 123 (c121) → 136 (c122, +13 from the reference-edge-liveness accounting, NOT new defects). **RE-DISCHARGE DELTAS (the central signal for the c123 RE-recheck + the batch-39 meta):** **`unresolved_depends_on_targets` 6→0** (all 4 libceed substrate ops + both AMR verbs now resolve to live files). **RE1 RE-STATED** (GMG firm; the L2/L3 chebyshev/jacobi iteration-views are now reference-class sibling-views absorbed-below-spine, like RE5/RE7 after the faithful re-type — a classification correction, NOT a regression; the firm grounding is the `L1/chebyshev-smoother` chain). **c122 OQ-resolutions:** 3 c121 `correction_step` OQs SETTLED (one-vs-two-operator → one conjugated combinator; divfree-projector-borderline → kept out of core roster; replace-and-propagate-scope → L2 leg done, wider set is a new c123 OQ); the `amr-estimate-mark-refine-theme-firmness-gate` DISCHARGED (both endpoints firm → theme flipped); the `FiniteElementSpaceHierarchy` 2nd-FIRM-consumer trigger now literally SATISFIED by the GMG firm flip (folds into the existing promote-watch-wording-reconcile, operationally inert — page already firm on disk). **COUNTS: firm-count flips this cycle** — +2 L1 firm (`flux_recovery_estimate`, `dorfler_mark` — the AMR estimate/mark vocabulary group, tracked separately from the 43-member main grand total per the new cohort heading); +1 L2 firm (`correction_step`, 21→22); GMG column rough-in→FIRM (Infrastructure kind, tracked separately); the `amr-estimate-mark-refine` L1>L0 theme rough-in→firm; +4 L1 roadmap_goal (libceed substrate). SLICE CORPUS: 0. 7 reports applied clean (7/7 staging rows == 7 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur; 103rd consecutive clean staging); zero deferrals, zero rejections, zero per-report gate-hits, ZERO finalize build-repairs. **THE CARRY to c123 (batch-39 position 3/3, THE BATCH-CLOSING cycle): (1) the REFERENCE-EDGE-LIVENESS SCHEME QUESTION — the batch-39-meta headline adjudication (does a `reference`-class edge to a firm/root-reachable node count toward liveness? the combinator-primary + DIRECTIVE-3-dual-surface models systematically produce reference-only-reachable firm nodes); (2) the DEFERRED `amr-estimate-mark-intro.md` by-kind group-intro authoring + the SUMMARY re-nest of the 2 flat AMR verbs + the index.md "Rough-in (AMR estimate/mark vocabulary)" header rename — one coordinated c123 layer-intro-author follow-up (OQ `amr-estimate-mark-group-intro-needs-authoring`); (3) the wider correction_step replace-and-propagate set (L1 multigrid-relaxation-smoother / GMG V-cycle column / distributive-relaxation — a c123 same-layer-cross-cutter, OQ `correction-step-wider-replace-and-propagate-set-l1-and-feature-column`); (4) the remaining RE set (RE2/RE3/RE6/RE8) gated on eigsolve-impl consumers / combinator-arity-notes / the L3 iteration-views column; (5) 2 new record-definition home OQs (`RefinementData`, `element-local-tensor` gated on the libceed substrate firm flip); (6) the libceed-substrate element-local rank-tensor L1 vocabulary front (batch-39-meta scheduling pick — would firm all 4 substrate ops + the consumer).** ---  **GRADED-STACK SNAPSHOT — CYCLE-121: THE LIFT-THROUGH CAMPAIGN OPENER — THE WIDE ALL-FRONTS FAN-OUT THAT BREAKS THE BATCH-36/37/38 PLATEAU (`reachable` 139→156 (+17, the largest single-cycle reachability jump since the campaign began), `rank_violations` HELD 0, STRONGER GARBAGE SIGNAL 27→22 (−5); post-cycle-121, batch-39 position 1/3 — THE OPENING primary cycle of meta-batch-39; cycles 121/122/123; the batch-39 meta-phase fires AFTER cycle-123's finalize, aggregating 121/122/123; cycle counter does NOT reset).** **HEADLINE — the FIRST cycle of the 2026-06-07 RE-SCOPE (DIRECTIVE-1/2/3). 9 dispatches, ALL applied clean, the wide all-fronts fan-out the post-consolidation directive called for. The LEAD (D1, layer-intro-author) landed the GEOMETRIC-MULTIGRID PRECONDITIONER feature column at L4+L1 (`book/src/feature/geometric-multigrid-preconditioner.{L4,L1}.md`, rank rough-in; a NEW 4th feature sub-kind "Infrastructure / shared-substrate columns" with its group-intro `feature/infrastructure.md`) — THE highest-fan-out lift-through consumer, which GROUNDS RE9/RE1/RE5/RE7 by composing `fe_space_hierarchy`(RE9)/chebyshev+jacobi-smoother(RE1)/normalize(RE5)/reciprocal(RE7) as firm `depends-on` constituents. D3 (harvester) landed `L1/multigrid-relaxation-smoother` FIRM (the GS-SSOR/triangular-solve kernel-IMPL, DIRECTIVE-3 dual-surface; its kernel-API is the repositioned `L1-L0/triangular-solve-obstruction` with the new `kernel-api` role-label) — made LIVE intra-cycle by D1's hard `depends-on`. D8 (layer-intro-author) DISCHARGED RE10: grounded `L1/interpolator` via two faithful `depends-on (uses)` edges from `L1/divfree-projector` (discrete-`Grad`, `divfree.cpp:117`) + `L4/waveguide_mode_reduce` (discrete-curl `Bz`, `boundarymodesolver.cpp:319-323`) → interpolator + its construction-rotation theme flipped OFF the STRONGER garbage list. D2 (layer-intro-author) landed the `concepts/FiniteElementSpaceHierarchy.md` record-definition page (firm) + trimmed the `fe_space_hierarchy` in-chapter §Record-definition to a back-link (the ≥2-consumer promotion under the live "≥2 consumers, not ≥2 FIRM" rule). D9 (layer-intro-author) cleaned the c118-surfaced waveguide-mode `.L0`/index maturity drift (rough-in→firm prose; feature-column firm count 12→13; `feature_root: seed` KEPT). D4 (abstractor) + D5 (abstractor) opened the DIRECTIVE-3 constructive-kernel frontier: `L1/libceed-quadrature-kernel-impl` (roadmap_goal; the matrix-free FE Gᵀ Bᵀ D B G contraction realizing the libCEED quadrature kernel-API) + `L3/eigsolve-impl` + `L3/lanczos_step` (roadmap_goal; the constructive Krylov-Schur/Lanczos loop realizing the SLEPc EPS kernel-API) — all three INTENTIONALLY land as grounded-future `[GARBAGE*]` (their only inbound is the free `realizes-kernel-api` reference edge; awaiting c122 consumers + the D6-routed shared-substrate ops). D6 (combinator-miner) landed the `L2/correction_step` rough-in dep-map row (the `y + B·(x − A·y)` shared smoother/V-cycle core, Palace-named at gmg.cpp:174-176 / distrelaxation.cpp:104). D7 (abstractor) opened the AMR front (DIRECTIVE-2 grounded consumer-(2)): `L1-L0/amr-estimate-mark-refine.md` theme (rough-in; ZZ flux-recovery estimate / Dörfler bulk mark / MFEM-opaque refine obstruction) + 2 rough-in L1 verb rows (`dorfler_mark`, `flux_recovery_estimate`).** **BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook + linkcheck2 0.12.0) EXIT 0 AFTER ONE surgical finalize build-repair — two dead `[lowering-verifier](../../README.md)` links in the new `book/src/L3/eigsolve-impl.md` (D5; the root `README.md` was DELETED in the batch-31 cleanup, and `lowering-verifier` is a pipeline AGENT ROLE with no in-book chapter target) were DE-LINKED to plain inline-code (the de-link fallback, NOT stub-creation — an agent role is not an implied spec component); all other touched files linkcheck-clean (the 9 new chapters + their SUMMARY/index inserts all resolve); only the pre-existing benign `Potential incomplete link` / `j+1` KaTeX-adjacent WARNs remain. **STEP-5b graded-stack linters on the LANDED tree:** **`rank_violations: 0`** (baseline fully discharged c096 → ANY violation would be NEW + BLOCK; NONE — GATE PASSES; every new firm/rough-in node rests on ≥-its-rank deps, the 3 roadmap_goals rest-on-anything vacuously) + **NO newly-orphaned node** (`reachable` CLIMBED 139→156; the 3 intentional `[GARBAGE*]` roadmap_goal kernel-impls are grounded-future per DIRECTIVE-3 + `feedback_gc_ground_dont_remove_future_deps`, NOT orphaned) + **`unresolved_depends_on_targets: 6`** (the declared-future substrate deps: 4 libceed substrate ops `element_restrict`/`basis_apply`/`quad_point_contract`/`geom_factor_build` + 2 AMR verbs `dorfler_mark`/`flux_recovery_estimate` — all WARNING-non-strict, the graded-stack-tracked declared-future-dep state, linter exit 0). TRUE CUMULATIVE (re-measured by integrator-finalize on the landed tree): `files=378 (+9), typed=317 (+9), untyped=61 (HELD), roots=41 (+2 the GMG L4/L1 columns carry feature_root: seed), reachable=156 (+17), rank_violations=0 (HELD), unresolved_depends_on_targets=6, promotion_frontier=11, detritus=123 (−9; detritus_no_typed_edges_pre_p1_artifact=101, detritus_with_typed_edges_stronger_signal=22, expected_unreachable_outside_dag=47)`. **TREND:** `rank_violations` HELD 0 (22 c094 → 0 c096 → … → 0 c119 → 0 c120 → 0 c121); `reachable` 36 → … → 139 → 139 → **156 (+17, plateau BROKEN)**; STRONGER 27 → **22 (−5: RE10 interpolator+theme off + smoother/neighbors live, partly offset by +3 intentional roadmap_goal kernel-impls landing in STRONGER)**. **RE-DISCHARGE DELTAS (the central signal for the c122 RE-recheck + the batch-39 meta):** **RE10 = DISCHARGED** (`L1/interpolator` LIVE; inbound: divfree-projector, multigrid-relaxation-smoother, waveguide_mode_reduce + the construction-rotation theme — the c117 RE10 premise falsified at c120 D1, now closed). **RE9/RE1/RE5/RE7 = GROUNDED** (the GMG column composes `fe_space_hierarchy`/chebyshev+jacobi-smoother/normalize/reciprocal as firm `depends-on` — `fe_space_hierarchy` is now LIVE via both GMG levels; the RE set is no longer a permanent floor but a discharge target per DIRECTIVE-2, RE-set re-check is the c122 planner's standing job). **CONSTRUCTIVE-KERNEL FRONTIER OPENED** (3 roadmap_goal kernel-impls awaiting c122 consumers + D6 shared-substrate harvest). **COUNTS: firm-count flips this cycle** — +1 L1 firm (`multigrid-relaxation-smoother`, kernel-impl kind — tracked SEPARATELY from the main-cohort firm tally per its OQ); +1 firm concepts page (`FiniteElementSpaceHierarchy`); feature-column firm 12→13 (waveguide-mode drift cleanup, D9); GMG column rough-in (Infrastructure kind, tracked separately); 3 new roadmap_goal nodes; +1 L2 rough-in row (`correction_step`); +1 L1>L0 rough-in theme (`amr-estimate-mark-refine`) + 2 L1 rough-in verb rows. SLICE CORPUS: 0. 9 reports applied clean (9/9 staging rows == 9 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur; 102nd consecutive clean staging); zero deferrals, zero rejections, zero per-report gate-hits, ONE finalize build-repair (the README de-link). **THE CARRY to c122 (batch-39 position 2/3): (1) WIRE THE CONSUMERS — the c122 forward frontier is the constructive-kernel consumers + the D6-routed shared-substrate ops (`element_restrict`/`basis_apply`/`quad_point_contract`/`geom_factor_build` for libceed; the eigsolve-impl consumer for RE3/RE8) that FIRE the roadmap_goal grounding triggers + the remaining RE promotion conditions; (2) HARVEST the AMR verbs (`dorfler_mark`, `flux_recovery_estimate`) to firm the amr-estimate-mark-refine theme; (3) RE-RECHECK — the standing RE1-RE10 baseline re-measure (RE10 discharged, RE9/RE1/RE5/RE7 grounded by GMG; the c122 planner re-runs the linter `--show-inbound` to authoritatively confirm); (4) the GMG rough-in→firm promotion gate (smoother-leg-gated) + the `FiniteElementSpaceHierarchy` promote-watch reconcile (GMG landed rough-in, not the watch's "2nd FIRM consumer" wording — a meta-phase reconcile); (5) the DIRECTIVE-3 `realizes-kernel-api` correspondence-audit cohort (lowering-verifier: smoother/libceed/eigsolve-impl); (6) MultigridConfig + vcycle-recursive-combinator mining candidates.** ---  **GRADED-STACK SNAPSHOT — CYCLE-120: THE BATCH-CLOSING PLATEAU-CONFIRMATION PRE-META AUDIT — OBSERVATION-ONLY, NO ARTIFACT CHANGE (ALL totals HELD vs c119; `reachable` 139, `rank_violations` HELD 0; post-cycle-120, batch-38 position 3/3 — THE BATCH-CLOSING (THIRD/FINAL) primary cycle of meta-batch-38; cycles 118/119/120; the batch-38 meta-phase fires AFTER this finalize, aggregating 118/119/120; cycle counter does NOT reset).** **HEADLINE — a single OBSERVATION-ONLY plateau-confirmation pre-meta audit (the c115 D1 precedent). (D1, cross-layer-cross-cutter, `plateau-probe`) `## Proposed changes` = None; independently re-derived the batch-38 terminal-state on BOTH graded-stack axes without trusting the planner or the finalizes. VERDICT: PLATEAU LARGELY CONFIRMED — Axis-1 (rank/well-foundedness) clean (`rank_violations=0`); the `promotion_frontier` (6) is entirely obstruction-/demand-gated; the in-scope feature + vocabulary frontier has NO coverage hole (all 5 drivers + lifecycle + 6 output products + boundary-mode columns present; mesh/fe_space/fe-assembly vocabulary all homed); STRONGER=27 maps EXHAUSTIVELY (27/27) to the ratified RE1-RE10 reachability baseline-exception set; the in-scope feature frontier is closed by c117/c118. The sweep surfaced 2 STRUCTURED FINDINGS for the batch-38 meta-phase (both routed to the OQ-ledger, NOT applied as artifact changes this cycle): FINDING-1 — a MISSED §2f GROUND edge for RE10 (`L1/interpolator`): the c117 RE10-ratification premise ("no faithful inbound consumer yet") is FALSIFIED — `L4/waveguide_mode_reduce` (firm c118, reachable) genuinely consumes the discrete-curl interpolator for `Bz = curl(Et)/(iω)` (`boundarymodesolver.cpp:319-323`), AND `L1/divfree-projector` (firm, reachable) consumes the discrete-`Grad` interpolator (`divfree.cpp:117`); both already prose-document the consumption but neither carries a `depends-on` edge — grounding either DISCHARGES RE10 (+2 reachable). Caveat: the `waveguide_mode_reduce → interpolator` L4→L1 altitude-crossing edge convention needs layer-intro-author confirmation before authoring (a meta-phase decision, NOT a this-cycle apply). The asymmetry RE9-correct/RE10-falsified IS the finding (RE9 `fe_space_hierarchy` re-checked, premise HOLDS — stays baseline-excepted). FINDING-2 — consistency drift: the c118 D5 `waveguide-mode` column promotion (rough-in→firm at L1/L4) left `feature/waveguide-mode.L0.md` still `rank: rough-in` + stale "sole seed output-product column" prose in `feature/index.md` + `feature/output-product.md` (the index-cell drift the OWN-COMPOSITION mechanics warn against; `feature_root: seed` is CORRECTLY KEPT — the drift is the maturity prose, not the root marker). Both are LOW-fan-out c121 cleanup picks.** **BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook + linkcheck2 0.12.0) EXIT 0 — NO artifact edits landed this cycle (observation-only), so NO build-repair; only the pre-existing benign `Potential incomplete link` WARNs (markdown-table / KaTeX false positives). **STEP-5b graded-stack linters on the LANDED tree:** **`rank_violations: 0`** (baseline fully discharged c096 → ANY violation would be NEW + BLOCK; NONE — GATE PASSES) + **NO newly-orphaned node** (`reachable` HELD 139) + **`unresolved_depends_on_targets: 0`** (HELD). TRUE CUMULATIVE (re-measured by integrator-finalize on the landed tree) — ALL HELD vs c119: `files=369, typed=308, untyped=61 (HELD), roots=39 (HELD), reachable=139 (HELD), rank_violations=0 (HELD), unresolved_depends_on_targets=0 (HELD), promotion_frontier=6 (HELD), detritus=132 (HELD; detritus_no_typed_edges_pre_p1_artifact=105, detritus_with_typed_edges_stronger_signal=27, expected_unreachable_outside_dag=46)`. Exactly the expected outcome for an observation-only cycle (no artifact mutation). **TREND:** `rank_violations` HELD 0 (22 c094 → 0 c096 → … → 0 c118 → 0 c119 → 0 c120); `reachable` 36 → … → 139 → 139 → **139 (HELD)**. **RE disposition:** NO RE change this cycle (observation-only) — but FINDING-1 RECOMMENDS the batch-38 meta DISCHARGE RE10 via a c121 grounding dispatch (modulo the L4→L1 altitude-crossing-edge-convention confirmation); RE9 re-confirmed correct. STRONGER HELD 27. 1 report applied clean (1/1 staging rows == 1 dispatched-ready; 101st consecutive clean staging); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs. **COUNTS: no firm-count status flips this cycle** — D1 observation-only (no authored vocabulary, no node promotion, no edge); layer tallies unchanged; SLICE CORPUS: 0. **THIS IS THE BATCH-CLOSING FINALIZE OF META-BATCH-38; THE BATCH-38 META-PHASE FIRES NEXT (a SEPARATE dispatch, aggregating 118/119/120). THE CARRY: (1) PLATEAU CONFIRMED on both axes (exhaustion-OF-CURRENT-SCOPE re-confirmed, c115/c119 precedent); (2) FINDING-1 (the missed RE10 §2f GROUND edge — migrate a c121 grounding pick, modulo the altitude-crossing-edge-convention confirm); (3) FINDING-2 (the waveguide-mode `.L0`/index consistency drift — a c121 mechanical cleanup); (4) the carried items: `FiniteElementSpaceHierarchy` promote-watch (unfired), the linter-maintenance ask-class `tools/` bundle (incl. `--show-stronger`), the producer report-frontmatter YAML-hygiene flag, the `divfree-projector.md` bare-basename citecheck AMBIG (disambiguate to `book/src/L1/divfree-projector.md` when the c121 RE10 grounding dispatch authors the edge).**

**GRADED-STACK SNAPSHOT — CYCLE-119: THE HONEST GROUNDING / HYGIENE TAIL — PLATEAU SIGNAL (ALL totals HELD vs c118; `reachable` 139, `rank_violations` HELD 0; post-cycle-119, batch-38 position 2/3 — the MIDDLE primary cycle of meta-batch-38; cycles 118/119/120; the batch-38 meta-phase fires AFTER cycle-120's finalize, aggregating 118/119/120; cycle counter does NOT reset).** **HEADLINE — the batch-38 substantive frontier (the mesh→fe_space substrate lowering + grounding campaign) was consumed at c118; c119 is the honest grounding + citation-hygiene tail. 2 dispatches, both applied clean. (D1, layer-intro-author, `lifecycle.L4-ground-edge`) the §2f GROUND edge `feature/lifecycle.L4 →depends-on(composes)→ L1/build_mesh` (3 surgical edits on `book/src/feature/lifecycle.L4.md`) — HONEST-TYPING of the L4 lifecycle sibling, NOT a flip: `build_mesh` was ALREADY reachable via `feature/lifecycle.L1 → L1/build_mesh` (c118 D4), so this honest-types the L4 sibling too, completing `build_mesh`'s grounding at BOTH siblings (L1 + L4); well-foundedness firm(3)→firm(3) HOLDS; `reachable` HELD 139 (no node flipped). (D2, lowering-verifier, `interpolator-citation-hygiene`) citation over-range `interpolator.cpp:282-310 → :282-306` at 4 sites across `book/src/L1-L0/interpolator-construction-rotation.md` + `book/src/L1/interpolator.md` + a `verified_against:` block append (the point-list `InterpolateFunction` body closes at `:306`; `:282-310` over-ran by 4 lines) — pure citation-range correction, NO edge/status/node change; rank + reachability UNCHANGED.** **BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook + linkcheck2 0.12.0) EXIT 0 — no new files, no SUMMARY change → no insert; only the pre-existing benign `Potential incomplete link` WARNs (markdown-table / KaTeX false positives); NO finalize build-repair. **STEP-5b graded-stack linters on the LANDED tree:** **`rank_violations: 0`** (baseline fully discharged c096 → ANY violation would be NEW + BLOCK; NONE — GATE PASSES) + **NO newly-orphaned node** (`reachable` HELD 139) + **`unresolved_depends_on_targets: 0`** (HELD). TRUE CUMULATIVE (re-measured by integrator-finalize on the landed tree) — ALL HELD vs c118: `files=369, typed=308, untyped=61 (HELD), roots=39 (HELD), reachable=139 (HELD), rank_violations=0 (HELD), unresolved_depends_on_targets=0 (HELD), promotion_frontier=6 (HELD), detritus=132 (HELD; detritus_no_typed_edges_pre_p1_artifact=105, detritus_with_typed_edges_stronger_signal=27, expected_unreachable_outside_dag=46)`. Exactly the expected outcome for an honest-grounding + citation-hygiene cycle. **TREND:** `rank_violations` HELD 0 (22 c094 → 0 c096 → … → 0 c117 → 0 c118 → 0 c119); `reachable` 36 → … → 136 → 139 → **139 (HELD)**. **RE disposition:** NO RE change — `build_mesh` was DISCHARGED at c118 D4; D1 honest-types the second (L4) sibling, no RE moves; RE9/RE10 HELD (theme grounds HOME only). STRONGER HELD 27. 2 reports applied clean (2/2 staging rows == 2 dispatched-ready; 100th consecutive clean staging); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs. **COUNTS: no firm-count status flips this cycle** — D1 honest-typing (frontmatter edge ADD + prose links), D2 citation-only; layer tallies unchanged; SLICE CORPUS: 0. **THE CENTRAL CARRY to the batch-38 meta-phase (fires after c120, aggregating 118/119/120): the PLATEAU SIGNAL — batch-38's substantive frontier was consumed c118, c119 cleared the honest grounding/hygiene tail, and the remaining in-scope movement is TRIGGER-GATED grounding + hygiene (no un-gated forward frontier under the current clean-gate scope; re-confirms the c115 plateau-probe verdict, exhaustion-OF-CURRENT-SCOPE, not terminal). Plus the carried ask-class linter-maintenance items (incl `--show-stronger` per-node attribution), the `FiniteElementSpaceHierarchy` promote-watch, the producer report-frontmatter YAML-hygiene flag (c118 interpolator report's unquoted-colon `scope:` line), and the citecheck-misses-range-END methodology note D2 surfaced (`--anchor`/`--scan` don't catch a range-END over-run; only an on-disk close-brace read does).**

**GRADED-STACK P1 TYPED-EDGE CAMPAIGN — BATCH-36 CLOSING CYCLE: THE FE-ASSEMBLE CLUSTER + dot/nrm2/scal THEMES GROUNDED (`reachable` 124→132 (+8), `rank_violations` HELD 0, STRONGER GARBAGE SIGNAL 24→23; post-cycle-114, batch-36 position 3/3 — THE BATCH-CLOSING primary cycle of meta-batch-36; cycles 112/113/114; the batch-36 meta-phase fires NEXT-AFTER THIS finalize, as a SEPARATE dispatch aggregating 112/113/114; cycle counter does NOT reset).** **HEADLINE — two FRONTMATTER-ONLY grounding dispatches landed the c113-routed `L1/weak_form_term` GROUNDABLE candidate + a faithful L1-op→theme sweep. (D1, layer-intro-author, `fe-assemble-cluster-ground`) authored canonical `edges:` on `book/src/L1/fe_assemble.md` (migrate firmness/lowers_to/depends_on/variant_axes → `rank: firm` + `composes`→{`weak_form_term`,`fe_space`} + `lowers-to`→`fe-operator-assemble-mutation-rotation` + `reference`→`bilinear-form`) and from-scratch on `book/src/L1/fe_space.md` (`rank: firm` + `composes`→`fe_collection` + `cites-evidence`→`palace/fem/fespace.hpp:67-75` + `lowers-to`→`fe-space-construction-rotation` + 4 `reference`) → +5 reachable: 3 direct nodes (`weak_form_term`, `fe_space`, `fe_collection`) + 2 transitive L1>L0 themes (`fe-assemble-libceed-boundary-obstruction` via `weak_form_term`'s `lowers_to`, `fe-space-construction-rotation` via `fe_space`'s new `lowers-to`). (D2, layer-intro-author, `l1-theme-grounding-sweep`) upgraded the L1>L0 theme edge on `book/src/L1/{dot,nrm2,scal}.md` from `reference` → `depends-on (kind: lowers-to)` (the c108 §5 L1-op→theme convention) → +3 reachable (the `dot`/`nrm2`/`scal` mutation-rotation themes). **MEASUREMENT-FRAMING (carried to the batch-36 meta-phase, OQ `l1l0-theme-grounding-projection-correction`):** flipping an EDGE-UNTYPED-DETRITUS theme reachable drops edge-untyped detritus (`detritus_no_typed_edges_pre_p1_artifact`), NOT the STRONGER subset; D2's −3 cleared edge-untyped detritus, D1's −5 mixed (weak_form_term was STRONGER → −1 STRONGER, the rest edge-untyped). **BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook + linkcheck2) EXIT 0 — all 5 touched `book/` files frontmatter-only with on-disk edge targets → linkcheck2-clean; no new file → no SUMMARY/index insert; only the pre-existing benign `Potential incomplete link` WARNs (markdown-table / KaTeX false positives); NO finalize build-repair. **STEP-5b graded-stack linters on the LANDED tree:** **`rank_violations: 0`** (baseline fully discharged c096 → ANY violation would be NEW + BLOCK; NONE — GATE PASSES; all source nodes `rank: firm`, all `composes`/`lowers-to` targets on-disk ≥-firm) + **NO newly-orphaned node** (`reachable` CLIMBED 124→132) + **`unresolved_depends_on_targets: 0`** (HELD). TRUE CUMULATIVE (re-measured by integrator-finalize on the landed tree): `files=355, typed=295, untyped=60 (HELD), roots=36, reachable=132 (+8), rank_violations=0 (HELD), unresolved_depends_on_targets=0 (HELD), promotion_frontier=8, detritus=127 (−8; detritus_no_typed_edges_pre_p1_artifact=104, detritus_with_typed_edges_stronger_signal=23, expected_unreachable_outside_dag=44)`. **TREND:** `rank_violations` HELD 0 (22 c094 → 0 c096 → … → 0 c113 → 0 c114); `reachable` 36 → 81 → 88 → 95 → 102 → 107 → 119 → 122 → 123 → 124 → **132**. **BATCH-36 ARC: reachable 122→132 over c112/c113/c114 (+10), rank_violations HELD 0 throughout** (c112 typed the L3 reduce/orthogonalize/linear_combination mid-nodes, +1 via `L2/nrm2`; c113 grounded `set_subvector_zero`'s theme + audited the STRONGER GARBAGE SIGNAL into the RE6-RE8 recs; c114 grounded the FE-assemble cluster + the dot/nrm2/scal themes). **REPAIR PHASE: both reports applied clean** (per the staging log; no repair-phase warnings carried to finalize). 2 reports applied clean (2/2 staging rows == 2 dispatched-ready; 95th consecutive clean staging / 109th consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs. **3 OQs in the c114 intake routed to the batch-36 meta-phase:** `fe_collection-own-constituents-future-pass`, `fe_space-deferred-siblings-still-ungrounded`, `l1l0-theme-grounding-projection-correction` (the measurement-framing correction). **COUNTS: no firm-count status flips this cycle** — both dispatches frontmatter-only edge-grounding (no new authored vocabulary, no node promotion); layer tallies unchanged; SLICE CORPUS: 0. The measurable movement is entirely on the reachability axis. **THE BATCH-36 META-PHASE FIRES AFTER THIS FINALIZE (aggregating 112/113/114) — main job: RATIFY the RE6-RE8 reachability baseline-exceptions** (RE6 axpy-family+`scal`→`linear_combination` 6 nodes [MUST incl arity-1 `scal`]; RE7 diagonal-preconditioner cluster incl `L3/jacobi-smoother` 4 nodes [RE7-vs-RE1-extension id-split judgment]; RE8 unconsumed L3 iteration-views `fold_solve`/`krylov-step` 2 nodes [RE8-vs-RE2-extension id-split judgment]); + assess `stale-pre-c108-rank-direction-error-prose-on-L1-ops` (mostly resolved — c113/c114 worked it; dot/nrm2/scal had no stale prose); + the hygiene-only L1-op→theme exclusions (`normalize`/`reciprocal`/`elementwise_product` — RE5 garbage ops, themes can't be flipped reachable by op-grounding); + codify the measurement-framing clarification.** **BATCH-38 OPENER — CYCLE-118: THE MESH→FE_SPACE SUBSTRATE LOWERING + GROUNDING CAMPAIGN (post-cycle-118, batch-38 position 1/3).** The 3 new mesh→fe_space substrate L1 ops (homed c117) get their **L1>L0 construction-rotation theme HOMES** (all firm): `book/src/L1-L0/build-mesh-construction-rotation.md` (D1), `fe-space-hierarchy-construction-rotation.md` (D2), `interpolator-construction-rotation.md` (D3, GSLIB `obstruction (opaque-library-ownership)` sub-note). `build_mesh` GROUNDED off detritus by D4 (faithful `feature/lifecycle.L1 → L1/build_mesh` composes edge, the GROUND-don't-remove disposition migrated to plan by the batch-37 meta) → `reachable` 136→139 (+3: build_mesh + its now-reachable construction-rotation theme + the new `waveguide_mode_reduce` verb/concept pages), `detritus` 129→132, STRONGER 24→27 (+2 the D2/D3 themes whose ops `fe_space_hierarchy`/`interpolator` stay RE9/RE10 baseline-excepted — EXPECTED, not a regression), `rank_violations` HELD 0, `unresolved` HELD 0. Two new `concepts/` record-definition pages landed firm: `concepts/mesh.md` (`Mesh`), `concepts/WaveguideModeTable.md` (D6). `fe_space_hierarchy`/`interpolator` ops stay baseline-excepted RE9/RE10 (theme grounds the HOME only, does not force op inbound-reachability).

**GRADED-STACK P1 TYPED-EDGE CAMPAIGN — PIVOT TO AUDIT-FIRST GROUNDING (the c112 F1 re-baseline finding); the `set-subvector-zero` L1>L0 theme grounded via the c108 §5 L1-op→theme convention + the STRONGER GARBAGE SIGNAL set fully dispositioned; `reachable` 123→124 (+1, D2's faithful L1-op→theme ground), `rank_violations` HELD 0, STRONGER GARBAGE SIGNAL 25→24; post-cycle-113, batch-36 position 2/3 — THE MIDDLE primary cycle of meta-batch-36; cycles 112/113/114; the batch-36 meta-phase fires AFTER cycle-114's finalize aggregating 112/113/114; cycle counter does NOT reset).** **HEADLINE — PIVOT off blind lazy-tail typing (acting on the c112 F1 finding that the lazy-tail `untyped` count does NOT decrement for legacy-edged files) to AUDIT-FIRST grounding. (D1, cross-layer-cross-cutter, OBSERVATION-ONLY — no `book/` mutation) audited the 13 un-baseline-excepted STRONGER GARBAGE SIGNAL members → 1 GROUNDABLE (`L1/weak_form_term` via the faithful `L1/fe_assemble → L1/weak_form_term` edge + companion `fe_assemble→fe_space`/`fe_space→fe_collection`; routed as a c114 grounding-dispatch candidate) + 12 baseline-exception recommendations for the batch-36 meta-phase: RE6 (axpy-family `L2/{axpy,axpby,axpbypcz}`+`L3/{axpy,axpby,axpbypcz}` absorbed into the reachable `linear_combination` combinator, 6 nodes — MUST incl arity-1 `scal`), RE7 (`L2/elementwise_product`+`L3/elementwise_product`+`L3/assemble-diagonal`+`L3/jacobi-smoother` diagonal-preconditioner cluster, 4 nodes — RE7-vs-RE1-extension id-split judgment), RE8 (`L3/fold_solve`+`L3/krylov-step` unconsumed iteration-views, 2 nodes — RE8-vs-RE2-extension id-split judgment); appended 5 OQ sections. (D2, layer-intro-author) applied the one already-confirmed faithful grounding edge: upgraded `book/src/L1/set_subvector_zero.md` frontmatter edge to `L1-L0/set-subvector-zero-mutation-rotation` from `reference` → `depends-on (kind: lowers-to)` (the c108 §5 L1-op→theme asymmetric convention; the op is reachable via `feature/eigenmode.L4 → L3/divfree-projector → L1/set_subvector_zero`, theme `rank: firm` so `rank(op=3) ≤ rank(theme=3)` holds) + corrected stale pre-c108 "rank-direction error" prose in 3 locations → the theme flips out of STRONGER GARBAGE SIGNAL → `reachable` 123→124 (+1), `detritus` 136→135 (−1). This closes the residual half of `set-subvector-zero-cluster-reachability` (op grounded c107, theme grounded here). **BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook + linkcheck2) EXIT 0 — D2's frontmatter+prose edit has on-disk edge targets → linkcheck2-clean; D1 wrote no `book/` artifact; no new file → no SUMMARY/index insert; only the pre-existing benign `Potential incomplete link` WARNs (markdown-table / KaTeX false positives); NO finalize build-repair. **STEP-5b graded-stack linters on the LANDED tree:** **`rank_violations: 0`** (baseline fully discharged c096 → ANY violation would be NEW + BLOCK; NONE — GATE PASSES; D2's edge firm-op rank 3 → firm-theme rank 3; D1 authored no edges) + **NO newly-orphaned node** (`reachable` CLIMBED 123→124) + **`unresolved_depends_on_targets: 0`** (HELD). TRUE CUMULATIVE (re-measured by integrator-finalize on the landed tree): `files=355, typed=295, untyped=60 (HELD), roots=36, reachable=124 (+1), rank_violations=0 (HELD), unresolved_depends_on_targets=0 (HELD), promotion_frontier=8, detritus=135 (−1; detritus_no_typed_edges_pre_p1_artifact=111, detritus_with_typed_edges_stronger_signal=24, expected_unreachable_outside_dag=44)`. **TREND:** `rank_violations` HELD 0 (22 c094 → 0 c096 → … → 0 c112 → 0 c113); `reachable` 36 → 81 → 88 → 95 → 102 → 107 → 119 → 122 → 123 → **124**. **REPAIR PHASE DID RUN on D1 ONLY** (one cross-reference-integrity warning — canonical token — repaired before integration; D1 observation-only so the repair touched prose/OQ-routing, not any edge). 2 reports applied clean (2/2 staging rows == 2 dispatched-ready; 94th consecutive clean staging / 108th consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits. **6+ OQs in the c113 intake routed to the batch-36 meta-phase:** the 5 appended this cycle (`...RE6`, `...RE7`, `...RE8`, `weak_form_term-groundable-...`, `RE7-cluster-completeness-add-L3-jacobi-smoother`) + the carried-open c112 set (`re2-shadows-orthogonalize-variant-split-theme` etc.) + the candidate friction `stale-pre-c108-rank-direction-error-prose-on-L1-ops`. **COUNTS: no firm-count status flips this cycle** — D2 is a single edge upgrade, D1 observation-only (no new authored vocabulary, no node promotion); layer tallies unchanged; SLICE CORPUS: 0. The measurable movement is entirely on the reachability axis. **THE IMPORTANT CARRY to the batch-36 meta-phase (fires after c114):** (1) the RE6-RE8 baseline-exception RATIFICATION batch (corrected enumeration 13 = 1 GROUNDABLE + 6 RE6 + 4 RE7 + 2 RE8); (2) the c114 grounding-dispatch candidate `L1/weak_form_term`; (3) the candidate friction `stale-pre-c108-rank-direction-error-prose-on-L1-ops` → a high-fan-out systematic L1-op→theme grounding sweep for c114 (`normalize`/`reciprocal`/`elementwise_product`/`scal` likely carry the same stale prose + un-upgraded `reference` edge, each a faithful +1). **NEXT: cycle-114 (batch-36 position 3/3, the BATCH-CLOSING cycle) — candidates: the `L1/weak_form_term` grounding + the systematic L1-op→theme grounding sweep; the batch-36 meta-phase fires after its finalize, aggregating 112/113/114.**

**GRADED-STACK P1 TYPED-EDGE CAMPAIGN — L3 LAZY-TAIL MID-NODE TYPING (`orthogonalize`/`nrm2` + `scal`/`linear_combination`); `reachable` 122→123 (+1, the faithful `L3/nrm2 → L2/nrm2` RE5 transitive-ground), `rank_violations` HELD 0, STRONGER GARBAGE SIGNAL 26→25; post-cycle-112, batch-36 position 1/3 — THE OPENING primary cycle of meta-batch-36; cycles 112/113/114; the batch-36 meta-phase fires AFTER cycle-114's finalize aggregating 112/113/114; cycle counter does NOT reset).** **HEADLINE — two FRONTMATTER-ONLY typed-edge migrations in the LEAD `graded-stack-lazy-tail-typing`. (D1, layer-intro-author, THE LEAD) typed `rank:`+`edges:` on `book/src/L3/orthogonalize.md` (`rank: partial-obstruction` + `obstruction_resolution: firm`; HELD GARBAGE — RE2 honored, only outbound edges authored, no forced inbound depender) + `book/src/L3/nrm2.md` (`rank: firm`); the genuine adjacent-layer `L3/nrm2 → L2/nrm2` lowers-to `depends-on` edge from the already-reachable `L3/nrm2` GROUNDS the previously-unreachable `L2/nrm2` → `reachable` 122→123 (+1), `detritus` 137→136 (−1). RE5 transitive-grounding, NOT a manufactured flip. (D2, layer-intro-author, FRONTMATTER-ONLY) typed `rank: firm`+`edges:` (bare-slug surface mirroring `L3/dot`) on `book/src/L3/scal.md` + `book/src/L3/linear_combination.md` — ZERO standalone delta (both carried legacy `lowers_to`/`lifts_from`, shim-counted typed before the edit; `untyped` HELD), a representation upgrade legacy→canonical `edges:`, NOT a typed-count change. **CROSS-CUTTING SIGNAL F1 (carried to the batch-36 meta-phase): the lazy-tail `untyped` count does NOT decrement for files with pre-existing legacy `lowers_to`/`lifts_from` (the linter auto-migrates them); the remaining `untyped`-60 tail is mostly truly-edge-less pages (L0 ground-truth, frozen meta-reviews, methodology/design/SUMMARY) NOT L3 mid-nodes — the campaign's `untyped` expectation needs re-baselining. The measurable lazy-tail value is the representation upgrade + faithful adjacent-layer grounds (like D1's +1), not the `untyped` count.** **BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook + linkcheck2) EXIT 0 — all 4 touched files frontmatter-only with on-disk edge targets → linkcheck2-clean; only the pre-existing benign KaTeX/incomplete-link WARNs (135, markdown-table/KaTeX false positives); NO finalize build-repair. **STEP-5b graded-stack linters on the LANDED tree:** **`rank_violations: 0`** (baseline fully discharged c096 → ANY violation would be NEW + BLOCK; NONE — GATE PASSES) + **NO newly-orphaned node** (`reachable` CLIMBED 122→123) + **`unresolved_depends_on_targets: 0`** (HELD). TRUE CUMULATIVE (re-measured by integrator-finalize on the landed tree): `files=355, typed=295, untyped=60 (HELD), roots=36, reachable=123 (+1), rank_violations=0 (HELD), unresolved_depends_on_targets=0 (HELD), promotion_frontier=8, detritus=136 (−1; detritus_no_typed_edges_pre_p1_artifact=111, detritus_with_typed_edges_stronger_signal=25, expected_unreachable_outside_dag=44)`. **TREND:** `rank_violations` HELD 0 (22 c094 → 0 c096 → … → 0 c111 → 0 c112); `reachable` 36 → 81 → 88 → 95 → 102 → 107 → 119 → 122 → **123**. **REPAIR PHASE DID RUN on D1 ONLY** (one edge-label-fidelity warning — a mis-stated PRECEDENT in the rationale prose, NOT an edge defect — fixed in-place pre-integration; the `edges:` blocks left untouched; D2 all-pass clean). 2 reports applied clean (2/2 staging rows == 2 dispatched-ready — the cycle-018 staging-completeness gap did NOT recur; 93rd consecutive clean staging / 107th consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits. **5 OQs promoted to the batch-36 meta-phase:** `re2-shadows-orthogonalize-variant-split-theme`, `lazy-tail-untyped-no-decrement-for-legacy-edged-files` (the F1 finding), `obstruction-resolution-firm-linter-keying-untested`, `L3-scal-reachable-via-normalize-grounding`, `linter-legacy-shim-line-citation-527-532-not-546-547`. **COUNTS: no firm-count status flips this cycle** — both dispatches frontmatter-only (no new authored vocabulary, no node promotion); layer tallies unchanged; SLICE CORPUS: 0. The measurable movement is entirely on the reachability axis. **NEXT: cycles 113/114 continue the lazy-tail/grounding work; the batch-36 meta-phase (a SEPARATE dispatch, fires after cycle-114's finalize, aggregating 112/113/114) triages the routed findings incl. F1's untyped-re-baselining.**

**GRADED-STACK P1 TYPED-EDGE CAMPAIGN — ORTHOGONALIZE-CHAIN GROUNDING + AXPY L1>L0 THEME SCHEME-TYPING (the c110-flagged lazy-tail mid-node tranche; `reachable` 119→122 (+3), STRONGER GARBAGE SIGNAL HOLDS 26; post-cycle-111, batch-35 position 3/3 — THE BATCH-CLOSING primary cycle of meta-batch-35; cycles 109/110/111; the batch-35 meta-phase fires NEXT-AFTER THIS finalize, as a SEPARATE dispatch aggregating 109/110/111; cycle counter does NOT reset).** **HEADLINE — the carried c110 mid-node tranche (`L2/orthogonalize` typing flips `orthogonalize-composition-lowering`) GROUNDED, plus the c110-filed `l1-l0-axpy-family-themes-need-scheme-frontmatter` RESOLVED. (D1, layer-intro-author, THE LEAD) the orthogonalize chain was reachable AT `L2/orthogonalize` (c110 grounded it via the `L4/krylov-step →composes→ L2/orthogonalize` edge) but its lowering home `L2-L1/orthogonalize-composition-lowering` + the `L1/orthogonalize` op + the `L1-L0/orthogonalize-mutation-rotation` theme were untyped and DEAD. From-scratch `edges:` blocks on `book/src/L2/orthogonalize.md` (`rank: firm` + 3 `depends-on` [`L1/orthogonalize`, `L1/dot`, `L1/axpy`] + `lowers-to → L2-L1/orthogonalize-composition-lowering` + 3 `reference`) and `book/src/L1/orthogonalize.md` (`rank: firm` + 4 `cites-evidence` L0 `depends-on` + `lowers-to → L1-L0/orthogonalize-mutation-rotation` + 4 `reference`) ground the chain down to L0 → `reachable` 119→122 (+3: `L1/orthogonalize`, `L2-L1/orthogonalize-composition-lowering`, `L1-L0/orthogonalize-mutation-rotation` flip in). (D2, layer-intro-author, FRONTMATTER-ONLY scheme hygiene) from-scratch `edges:` blocks on `book/src/L1-L0/{axpby,axpbypcz}-mutation-rotation.md` (`rank: firm` + `cites-evidence` L0 `depends-on` + `reference` to the L1 ops); reachability-NEUTRAL (both themes already reachable via inbound `reference` edges from `L1/axpby`/`L1/axpbypcz`/`L1/axpy`, confirmed by `--show-inbound`). The producer correctly DECLINED a dispatch-suggested `reference: L1-L0/dot-mutation-rotation` (don't-manufacture discipline — grep of both bodies returns nothing). **TRUE CUMULATIVE (both applied): `reachable` 119→122 (+3, all D1; D2 reachability-neutral), `rank_violations` HELD 0, `untyped` HELD 60, `detritus` 140→137 (−3), STRONGER GARBAGE SIGNAL HOLDS 26 (the −3 cleared WEAKER `[garbage?]` untyped-detritus, NOT the stronger bucket).** **ROUTED FINDINGS (for the batch-35 meta-phase — THE BATCH CLOSES HERE):** (i) the `L3/orthogonalize` sub-chain has no faithful reachable depender (`l3-orthogonalize-sub-chain-no-faithful-reachable-depender`, present at open-questions.md:1494); (ii) the normalize/reciprocal internal-utility chain (routing for the meta-phase); (iii) the chebyshev/jacobi preconditioner-leg baseline-exception ratification (carried from c110, absorbed-below-column, the c107 BC/divfree pattern); (iv) the gram + incremental-least-squares routings. **FRICTION RECURRENCE NOTE (for the meta-phase):** the c110-flagged candidate `parallel-dispatch-reachability-measurement-contamination` did NOT recur this cycle — c111's per-report integrators correctly isolated each dispatch's delta (D1 measured its own +3 standalone; D2 confirmed reachability-neutral; finalize re-measured the authoritative cumulative on the landed tree). The discipline HELD this cycle. 2 reports applied clean (2/2 staging rows == 2 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur — NINETY-SECOND consecutive clean staging / 106th consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs. **REPAIR PHASE DID RUN** (on D1 only — one low-severity garbage-bucket-wording WARNING fixed in-place; the `edges:` blocks left untouched; D2 was an all-pass clean critic-set ready). **BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook + linkcheck2) EXIT 0 — all 4 touched `book/` files are frontmatter-only edits, every edge target resolves on-disk → linkcheck2-clean; no new file → no SUMMARY/index insert; only the pre-existing benign KaTeX/bracket-in-prose `Potential incomplete link` WARNs (markdown-table false positives, NOT link errors); NO finalize build-repair needed. **STEP-5b graded-stack linters on the LANDED tree:** **`rank_violations: 0`** (`[]` — baseline fully discharged c096 → ANY violation would be NEW and BLOCK; there are NONE — GATE PASSES) + **NO newly-orphaned node** (`reachable` CLIMBED 119→122) + **`unresolved_depends_on_targets: 0`** (HELD). Totals: `files=355, typed=295, untyped=60 (HELD), roots=36, reachable=122 (was 119, +3), rank_violations=0, unresolved_depends_on_targets=0, promotion_frontier=8, detritus=137 (was 140, −3; STRONGER GARBAGE SIGNAL HOLDS 26, edge-untyped artifact 114→111)`. **rank_violations cycle-over-cycle trend: 22 (c094) → 1 (c095) → 0 (c096) → … → 0 (c109) → 0 (c110) → 0 (c111). reachable trend across the campaign: 36 (c105-end, pre-fix) → 81 (batch-33 meta) → 88 (c106) → 95 (c107) → 102 (c108) → 107 (c109) → 119 (c110) → 122 (c111). BATCH-35 ARC: reachable 102→122 over c109/c110/c111 (+20), rank_violations HELD 0 throughout.** **CITECHECK NOTE (non-blocking):** the per-report integrators noted AMBIG/MISS/OOB citecheck warnings — ALL on report PROSE / OQ-prose / linter-tool-source references, NONE in any applied `edges:` block; no prose-claim text lands in `book/` (frontmatter-only edits), so all are non-blocking and recorded-not-repaired. **CARRY-FORWARD body note (non-blocking, out of D2's frontmatter-only scope):** a pre-existing body-prose mislabel in `axpby-mutation-rotation.md:25-26` (labels `vector.cpp:739-743` as an "AXPBYPCZ member form"; on-disk `739-743` is the `AXPBY` overload) — flagged + deferred by producer + critic to a future harvester/lowering-verifier body pass; the frontmatter `cites-evidence` comments authored here use the accurate on-disk labels. **CAMPAIGN STATE: P0/P2/P3 DONE; P1 typed-edge campaign — WAVE-3 record-rescue + BC/divfree + L1/L0 lowering-chain tails GROUNDED, the 4 on-spine L2-L1 themes grounded (c109), the reduce-to-scalar chain + axpby/axpbypcz themes grounded (c110), and this cycle the orthogonalize chain grounded down to L0 + the axpy L1>L0 themes scheme-typed (`reachable` 36→122 over the campaign). RESIDUAL P1 (for the batch-35 meta-phase to triage/route): the `L3/orthogonalize` sub-chain no-faithful-depender finding, the normalize/reciprocal internal-utility chain, the chebyshev/jacobi preconditioner-leg baseline-exception, the gram/incremental-least-squares routings, the latent linter-reader block-mapping-misparse bug (still latent, batch-34 NO-GO), and the lazy-untyped tail. THE BATCH-35 META-PHASE FIRES AFTER THIS FINALIZE (aggregating 109/110/111).** **COUNTS: no firm-count status flips this cycle** — D1's 2 from-scratch `edges:` blocks + D2's 2 from-scratch `edges:` blocks are all frontmatter-only (no new authored vocabulary, no node promotion). Layer tallies unchanged: L1 firm 33 main / 40 grand, L4 firm 21 main / 25 grand, L4>L3 11, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 34, methodology 4, feature spine 11 FIRM / 1 seed, L4 reduce-family 4 verbs ALL firm. **SLICE CORPUS: 0.** The measurable movement is entirely on the reachability axis (`reachable` 119→122, the orthogonalize chain grounded, axpy L1>L0 themes scheme-typed, STRONGER GARBAGE SIGNAL HOLDS 26). **NEXT: the batch-35 meta-phase (a SEPARATE dispatch, fires after this finalize, aggregating 109/110/111) triages the routed findings; then cycle-112 opens batch-36.**

**GRADED-STACK P1 TYPED-EDGE CAMPAIGN — REDUCE/ORTHOGONALIZE/CHEBYSHEV COHORT GROUNDING PASS + AXPY-FAMILY TYPING (the batch-35 LEAD's Group-B next tranche + the lazy-tail BLAS-leaf typing; `reachable` 107→119 (+12), STRONGER GARBAGE SIGNAL 34→26; post-cycle-110, batch-35 position 2/3 — the MIDDLE primary cycle of meta-batch-35; cycles 109/110/111; the batch-35 meta-phase fires NEXT-AFTER cycle-111's finalize, aggregating 109/110/111; cycle counter does NOT reset).** **HEADLINE — the c109 Group-B finding (`l2-reduce-orthogonalize-cohort-itself-unreachable-blocks-theme-grounding`) RESOLVED by a single faithful host edit. (D1, layer-intro-author, THE LEAD) the reduce-to-scalar chain (`inner_product`/`nrm2`/`dot`) was typed correctly DOWNWARD (L4→L3→L2) but DEAD AT THE TOP — nothing reachable pointed `depends-on` into `L4/dot`/`L4/nrm2`/`L4/inner_product`. The §(g) GROUND-don't-remove faithful fix: add 3 `composes` `depends-on` edges from the already-reachable consumer `L4/krylov-step` (reachable ← `L4/ksp_solve` ← driven/electrostatic/magnetostatic.L4) into the reduce/orthogonalize verbs its body GENUINELY calls (`L4/dot`, `L4/nrm2`, `L2/orthogonalize`). ONE host edit cascades the entire reduce-to-scalar chain reachable + grounds the orthogonalize leg → D1-isolation `reachable` 107→117 (+10). (D2, layer-intro-author) the lazy-tail BLAS-leaf typing: `rank: firm` + typed `edges:` blocks prepended onto `book/src/L1/{axpy,axpby,axpbypcz}.md` (which previously carried NO frontmatter), mirroring the `scal`/`apply_linop`/`set_subvector_zero` convention (blocking `depends-on` = the rank-terminal POSITIVE L0 source via `cites-evidence` + the `lowers-to` edge to the L1>L0 theme); standalone +2 `reachable` (the `axpby`/`axpbypcz` mutation-rotation themes flip; both LEFT detritus before). **TRUE CUMULATIVE (both applied): `reachable` 107→119 (+12: D1 +10 reduce chain, D2 +2 axpby/axpbypcz themes), `rank_violations` HELD 0, `untyped` HELD 60 (the linter prose-`## Status` rank fallback already ranked the BLAS leaves firm — the win was Axis-2 reachability, not Axis-1 untyped), `detritus` 152→140 (−12), STRONGER GARBAGE SIGNAL 34→26 (−8).** **ROUTED FINDINGS (for the batch-35 meta-phase):** the chebyshev/jacobi preconditioner leg recommended for a baseline-exception (absorbed-below-column, the c107 BC/divfree pattern); `gram_reduce`→`inner_product` is a SIBLING reference NOT a `composes` edge (decline confirmed); the lazy-tail mid-node typing of `L2/orthogonalize`/`L3/orthogonalize`/`L3/nrm2` is the next mechanical tranche that would flip `orthogonalize-composition-lowering`. **MEASUREMENT-HYGIENE FRICTION (for the meta-phase):** both parallel D1/D2 dispatches initially MISreported reachability because each measured the linter with the OTHER's not-yet-reverted edits in the working tree (apply→lint→revert contamination across parallel layer-intro-author dispatches); both caught by the critics + fixed by the repairers — a candidate friction-ledger entry `parallel-dispatch-reachability-measurement-contamination`. 2 reports applied clean (2/2 staging rows == 2 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur — NINETY-FIRST consecutive clean staging / 105th consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs. **REPAIR PHASE DID RUN** (2 reports, each with 2 low/medium measurement-misattribution warnings fixed in-place — the producer headline reachability figures corrected to the standalone/cumulative truth; the `edges:` blocks left untouched). **BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook + linkcheck2) EXIT 0 — all 4 touched `book/` files are frontmatter-only edits, every edge target resolves on-disk → linkcheck2-clean; no new file → no SUMMARY/index insert; only the pre-existing benign KaTeX/bracket-in-prose `Potential incomplete link` WARNs (markdown-table false positives, NOT link errors); NO finalize build-repair needed. **STEP-5b graded-stack linters on the LANDED tree:** **`rank_violations: 0`** (`[]` — baseline fully discharged c096 → ANY violation would be NEW and BLOCK; there are NONE — GATE PASSES) + **NO newly-orphaned node** (`reachable` CLIMBED 107→119) + **`unresolved_depends_on_targets: 0`** (HELD). Totals: `files=355, typed=295, untyped=60 (HELD), roots=36, reachable=119 (was 107, +12), rank_violations=0, unresolved_depends_on_targets=0, promotion_frontier=8, detritus=140 (was 152, −12; STRONGER GARBAGE SIGNAL 34→26, edge-untyped artifact 118→114)`. `rank_histogram: {firm:201, typed-no-rank:80, rough-in:5, partly-constructive:3, obstruction:2, partial-obstruction:4}`. **rank_violations cycle-over-cycle trend: 22 (c094) → 1 (c095) → 0 (c096) → … → 0 (c108) → 0 (c109) → 0 (c110). reachable trend across the campaign: 36 (c105-end, pre-fix) → 81 (batch-33 meta) → 88 (c106) → 95 (c107) → 102 (c108) → 107 (c109) → 119 (c110).** **CITECHECK NOTE (non-blocking):** the per-report integrators noted AMBIG/MISS/OOB citecheck warnings — ALL on report PROSE / OQ-prose / linter-tool-source references (e.g. `graded_stack_lint.py:425-437` is a reference to the linter tool's OWN source, not a Palace L0 citation), NONE in any applied `edges:` block; no prose-claim text lands in `book/` (frontmatter-only edits), so all are non-blocking and recorded-not-repaired. **CAMPAIGN STATE: P0/P2/P3 DONE; P1 typed-edge campaign — WAVE-3 record-rescue + BC/divfree + L1/L0 lowering-chain tails GROUNDED, the 4 on-spine L2-L1 themes grounded (c109), and this cycle the reduce-to-scalar chain + orthogonalize leg + axpby/axpbypcz themes grounded (`reachable` 36→119 over the campaign). RESIDUAL P1: the chebyshev/jacobi preconditioner-leg baseline-exception, the gram/incremental-least-squares routings, the `L2/orthogonalize`/`L3/orthogonalize`/`L3/nrm2` lazy-tail mid-node typing (the next mechanical tranche), the latent linter-reader block-mapping-misparse bug (still latent, batch-34 NO-GO), and the lazy-untyped tail. The batch-35 meta-phase fires after cycle-111's finalize (aggregating 109/110/111).** **COUNTS: no firm-count status flips this cycle** — D1's 3 edges + D2's 3 BLAS-leaf typings are all frontmatter-only (no new authored vocabulary, no node promotion). Layer tallies unchanged: L1 firm 33 main / 40 grand, L4 firm 21 main / 25 grand, L4>L3 11, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 34, methodology 4, feature spine 11 FIRM / 1 seed, L4 reduce-family 4 verbs ALL firm. **SLICE CORPUS: 0.** The measurable movement is entirely on the reachability axis (`reachable` 107→119, the reduce-to-scalar chain + orthogonalize leg + 2 BLAS themes grounded, STRONGER GARBAGE SIGNAL 34→26). **NEXT: cycle-111 (batch-35 position 3/3, the BATCH-CLOSING cycle) — candidates: the `L2/orthogonalize`/`L3/orthogonalize`/`L3/nrm2` lazy-tail mid-node typing (flips `orthogonalize-composition-lowering`), the gram/incremental-least-squares routings; the batch-35 meta-phase fires after its finalize, aggregating 109/110/111.**

**GRADED-STACK P1 TYPED-EDGE CAMPAIGN — L2-L1 LOWERING-THEME-COHORT GROUNDING PASS (the batch-35 LEAD; the operator→theme edge-convention gap GROUNDED for the 4 on-spine themes; `reachable` 102→107, STRONGER GARBAGE SIGNAL 35→34; post-cycle-109, batch-35 position 1/3 — the OPENING primary cycle of meta-batch-35; cycles 109/110/111; the batch-35 meta-phase fires NEXT-AFTER cycle-111's finalize, aggregating 109/110/111; cycle counter does NOT reset).** **HEADLINE — the batch-34-meta-migrated LEAD `graded-stack-l2-l1-theme-cohort-grounding` landed as a bounded one-edge-per-theme GROUNDING pass over the L2-L1 lowering-theme cohort, applying the §(g) GROUND-don't-remove disposition as FAITHFUL-PATH-OR-FINDING. (D1, layer-intro-author, THE LEAD) the L2-L1 lowering themes stayed `[garbage?]` because the `lowers-to` edge convention points operator→operator and never operator→theme, so a lowering theme had no inbound `depends-on` from a reachable node; the faithful fix is a `lowers-to`-kind `depends-on` edge `L2/<op> → L2-L1/<theme>` on each host op (mirroring the c108 `L2/divfree-projector` block-mapping precedent). On-disk verification confirmed the planner's FAITHFUL-PATH-OR-FINDING split EXACTLY: 4 Group-A themes (host L2 op itself reachable) GROUNDED this cycle — all 4 flipped OUT of `[garbage?]` (`eigsolve-spectral-transform-composition ← L2/eigsolve`, `ksp-solve-outer-driver-unfold ← L2/ksp_solve`, `krylov-step-kernel-defusion ← L2/krylov-step`, `linear-combination-fold-specialization ← L2/linear_combination`); 1 cheap faithful edge-lay (`inner_product → inner-product-fold-specialization`) typed correctly but does NOT flip (host `L2/inner_product` is itself `[GARBAGE*]`); 5 Group-B themes (host L2 op itself unreachable: `chebyshev-iteration-fusion`/`gram-fold-specialization`/`incremental-least-squares-composition-lowering`/`orthogonalize-composition-lowering`/`inner-product-fold-specialization`) ROUTED as a structured finding (OQ `l2-reduce-orthogonalize-cohort-itself-unreachable-blocks-theme-grounding`); `deflate`/`deflate-composition-lowering` NOT TOUCHED (demand-gated FRONTIER member, STOP-PROPOSING). The 5 edits are FRONTMATTER-ONLY (3 ADD/UPGRADE on existing blocks + 1 from-scratch `edges:` block on `L2/krylov-step` + 1 cheap edge-lay). **`reachable` 102→107 (+5; EXCEEDS the predicted +4 because authoring `L2/krylov-step`'s `edges:` block from scratch also makes `L2/krylov-step` ITSELF a typed-and-reachable node), STRONGER GARBAGE SIGNAL 35→34, `detritus` 157→152 (−5), `rank_violations` HELD 0, `untyped` HELD 60.** Faithfulness — every `lowers-to` was confirmed REAL from the theme/host chapter prose before typing (the 4 theme-chapter heads + `krylov-step.md:96` for the 7 firm L1 leaves). **The next-tranche blocker (the Group-B finding): the L2 reduce/orthogonalize/chebyshev cohort is itself off-spine** — its only inbound `depends-on` come from L3 ops that are themselves off-spine (`L3/dot`, `L3/inner_product`, `L3/chebyshev`, `L3/orthogonalize`, `L3/gram`, …); a faithful `L2/<op> →lowers-to→ theme` edge from an unreachable op cannot flip the theme. Grounding the L2 reduce/orthogonalize/chebyshev cohort (tracing up through the unreachable L3 reduce/iteration cohort) is the recommended next tranche — the meta-phase makes the ground-from-column-vs-absorbed-detritus call (the c107 disposition pattern; recommended bias: GROUND from the column where a faithful path exists, the reduce verbs being genuine constituents of the solve/postprocess columns). 1 report applied clean (1/1 staging rows == 1 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur — NINETIETH consecutive clean staging / 104th consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs. **REPAIR PHASE DID RUN this cycle** (one low-severity rank-invariant warning repaired in-place by rationale-only prose softening — the `edges:` blocks were left untouched; the 3 typed-no-rank L1 BLAS leaves axpy/axpby/axpbypcz make their inbound edges hold vacuously). **BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook + linkcheck2) EXIT 0 — all 5 touched `book/` files are frontmatter-only edits, every edge target resolves on-disk → linkcheck2-clean; no new file → no SUMMARY/index insert; only the pre-existing benign KaTeX/bracket-in-prose `Potential incomplete link` WARNs (markdown-table false positives, NOT link errors); NO finalize build-repair needed. **STEP-5b graded-stack linters on the LANDED tree:** **`rank_violations: 0`** (`[]` — baseline fully discharged c096 → ANY violation would be NEW and BLOCK; there are NONE — GATE PASSES) + **NO newly-orphaned node** (`reachable` CLIMBED 102→107) + **`unresolved_depends_on_targets: 0`** (HELD). Totals: `files=355, typed=295, untyped=60 (HELD), roots=36, reachable=107 (was 102, +5), rank_violations=0, unresolved_depends_on_targets=0, promotion_frontier=8, detritus=152 (was 157, −5; STRONGER GARBAGE SIGNAL 35→34, edge-untyped artifact 122→118)`. `rank_histogram: {firm:201, typed-no-rank:80, rough-in:5, partly-constructive:3, obstruction:2, partial-obstruction:4}`. **rank_violations cycle-over-cycle trend: 22 (c094) → 1 (c095) → 0 (c096) → … → 0 (c107) → 0 (c108) → 0 (c109). reachable trend across the campaign: 36 (c105-end, pre-fix) → 81 (batch-33 meta) → 88 (c106) → 95 (c107) → 102 (c108) → 107 (c109).** **CITECHECK NOTE (non-blocking):** the per-report integrator noted 2 `AMBIG` citecheck warnings on bare-basenames in the report's §Faithfulness-confirmations PROSE (`eigsolve.md:171`, `krylov-step.md:96`) — these are report-prose bare-basename ambiguities; NO prose-claim text lands in `book/` (frontmatter-only edits), so they are non-blocking and recorded-not-repaired. **CAMPAIGN STATE: P0/P2/P3 DONE; P1 typed-edge campaign — the WAVE-3 record-rescue + BC/divfree lowering-chain tails are GROUNDED (`reachable` 36→102 over the prior campaign), and this cycle's L2-L1 theme-cohort LEAD grounded the 4 on-spine themes (`reachable` 102→107). RESIDUAL P1: the L2 reduce/orthogonalize/chebyshev cohort grounding (the Group-B next-tranche; the meta-phase makes the ground-vs-absorbed-detritus call), the cheap inner_product edge already laid for when that cohort grounds, the latent linter-reader block-mapping-misparse bug (still latent, batch-34 NO-GO), and the lazy-untyped tail. The batch-35 meta-phase fires after cycle-111's finalize (aggregating 109/110/111).** **COUNTS: no firm-count status flips this cycle** — D1's 5 edits are all frontmatter-only (no new authored vocabulary, no node promotion; `L2/krylov-step` was authored only at the frontmatter/`edges:` level — its body pre-existed). Layer tallies unchanged: L1 firm 33 main / 40 grand, L4 firm 21 main / 25 grand, L4>L3 11, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 34, methodology 4, feature spine 11 FIRM / 1 seed, L4 reduce-family 4 verbs ALL firm. **SLICE CORPUS: 0.** The measurable movement is entirely on the reachability axis (`reachable` 102→107, 4 themes + `L2/krylov-step` grounded, STRONGER GARBAGE SIGNAL 35→34). **NEXT: cycle-110 (batch-35 position 2/3) — the natural HIGH-fan-out LEAD is the L2 reduce/orthogonalize/chebyshev cohort grounding (the Group-B next-tranche from this cycle's finding), tracing up through the unreachable L3 reduce/iteration cohort; once an L2 reduce/orthogonalize op is itself reachable, its already-laid (inner_product) or to-be-laid theme edge flips it automatically.**

**GRADED-STACK P1 TYPED-EDGE CAMPAIGN — LOWERING-CHAIN-LIVENESS GROUNDING PASS (the carried L1-BC-op-tail/divfree lowering-chain gap RESOLVED by grounding the L1/L0 lowering homes; `reachable` 95→102, the lowering-chain tail is now GROUNDED; post-cycle-108, batch-34 position 3/3 — the BATCH-CLOSING primary cycle of meta-batch-34; cycles 106/107/108; the batch-34 meta-phase fires NEXT-AFTER this finalize, aggregating 106/107/108).** **HEADLINE — the ONE carried follow-up (`lowering-chain-liveness-not-propagated-to-l1-ops`, c107 D1) is RESOLVED by a SYSTEMATIC `lowers-to` GROUNDING pass down the BC + divfree lowering chains. (D1, THE LEAD, MEDIUM) typed `edges:` blocks on 8 pre-scheme lowering-chain chapters — BC chain: `bc-elimination-post-composition-dissolution`, `eliminate_essential_bc`, `essential_dofs`, `essential-dofs-construction-rotation`; divfree chain: `L2/divfree-projector` (had NO frontmatter), `L1/divfree-projector`, `divfree-projector-leaf-identity`, `divfree-projector-mutation-rotation` — grounding the L1/L0 lowering homes from their already-reachable L4/L3 sources. **`reachable` 95→102 (+7 nodes rescued), 0 regression, `rank_violations` HELD 0.** Faithful — D1 CAUGHT a would-be over-edge (`essential_dofs` is a consumed operand reaching root via its own `essential-dofs-construction-rotation` theme, NOT a dissolution lowering target — no forced edge), the faithful-path-or-finding discipline. This resolved OQ `lowering-chain-liveness-not-propagated-to-l1-ops` grounded-and-rescued and ROUTED 2 findings to the batch-34 meta-phase (the residual ~10 L2-L1 theme-cohort reachability gap + a scheme-doc clarification). (D2, LOW) typed `concepts/counter-update.md` as a `firm` NODE (the c107-D2 deferral): `untyped` 61→60. Honestly NOT forced reachable (reference-only edges, no consumer carries a typed `depends-on: concepts/counter-update`); the EXPECTED critic-reproduced outcome (no `depends-on` manufactured to chase reachability). This resolved OQ `concepts-counter-update-needs-node-rank-and-depends-on-edges`. 2 reports applied clean (2/2 staging rows == 2 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur — EIGHTY-NINTH consecutive clean staging / 103rd consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs, NO repair phase. **BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook + linkcheck2) EXIT 0 — all 9 touched `book/` files are frontmatter-only edits, every edge target resolves on-disk → linkcheck2-clean; no new file → no SUMMARY/index insert; only the pre-existing benign KaTeX/bracket-in-prose `Potential incomplete link` WARNs (markdown-table false positives, NOT link errors); NO finalize build-repair needed. **STEP-5b graded-stack linters on the LANDED tree:** **`rank_violations: 0`** (baseline fully discharged c096 → ANY violation would be NEW and BLOCK; there are NONE — GATE PASSES; D1's grounding edges are firm→firm or firm→untyped-non-node lowering homes, no promotion flip to gate) + **NO newly-orphaned node** (`reachable` HELD/CLIMBED to 102) + **`unresolved_depends_on_targets: 0`** (HELD). Totals: `files=355, typed=295, untyped=60 (was 61, −1), roots=36, reachable=102 (was 95), rank_violations=0, unresolved_depends_on_targets=0, promotion_frontier=8, detritus=157 (was 156 post-D1; D2's counter-update +1 is a typed-but-unreached node, NOT a regression)`. **DETRITUS-SEMANTICS CAVEAT (NOT a regression):** the `detritus` total mixes THREE dispositions — genuine-garbage + typed-non-node (reference-only, no liveness by construction) + typed-but-not-yet-reached NODE (D2's `counter-update` is the latter: a `firm` node honestly not forced live). 0 nodes lost reachability (`reachable` climbed 95→102). One of the carried findings recommends a linter `totals`-split to separate these three signals so the GC health read is unambiguous. **rank_violations cycle-over-cycle trend: 22 (c094) → 1 (c095) → 0 (c096) → … → 0 (c106) → 0 (c107) → 0 (c108). reachable trend across the WAVE-3/grounding campaign: 36 (c105-end, pre-fix) → 81 (batch-33 meta) → 88 (c106) → 95 (c107) → 102 (c108)** — the c105 linter-fix made it measurable, then c106/c107/c108 grounded the record + BC + divfree + lowering-chain nodes. **CAMPAIGN STATE: P0/P2/P3 DONE; P1 typed-edge campaign — WAVE-3 RECORD-RESCUE + the BC/divfree LOWERING-CHAIN tail are now GROUNDED (the record set + BC + divfree + L1/L0 lowering homes are all root-reachable; `reachable` 36→102 over the campaign); the strict-zero concept-page lazy-tail + the `counter-update` node-typing are DONE. RESIDUAL P1 routed to the batch-34 meta-phase: the ~10 L2-L1 theme-cohort reachability gap (identical operator→operator-not-operator→theme cause), a scheme-doc clarification (lowering-theme reachability-vs-well-foundedness), the linter `totals`-split recommendation, and the grounding-disposition codification (grounding-vs-route-vs-baseline-exception) + the lazy-untyped tail. The batch-34 meta-phase fires after THIS cycle-108 finalize (aggregating 106/107/108).** **COUNTS: no firm-count status flips this cycle** — D1's 8 lowering-chain edge typings + D2's 1 node typing are all frontmatter-only (no new authored vocabulary, no node promotion). Layer tallies unchanged: L1 firm 33 main / 40 grand, L4 firm 21 main / 25 grand, L4>L3 11, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 34, methodology 4, feature spine 11 FIRM / 1 seed, L4 reduce-family 4 verbs ALL firm. **SLICE CORPUS: 0.** The measurable movement is entirely on the reachability axis (`reachable` 95→102, 7 lowering-chain nodes grounded) + the typed-edge axis (`untyped` 61→60). **NEXT: the batch-34 meta-phase fires after this finalize, aggregating cycles 106/107/108 — it weighs the routed findings (L2-L1 theme-cohort reachability gap, scheme-doc clarification, linter totals-split, grounding-disposition codification) and the carried latent linter-reader bug.**

**GRADED-STACK P1 TYPED-EDGE CAMPAIGN — WAVE-3 RECORD-RESCUE TRANCHE COMPLETED BY GROUNDING (the BC-elimination + divfree absorbed clusters GROUNDED from the feature-spine roots; `reachable` 88→95, the WAVE-3 record-rescue tranche is now DONE; post-cycle-107, batch-34 position 2/3 — the MIDDLE primary cycle of meta-batch-34; cycles 106/107/108; the batch-34 meta-phase fires NEXT-AFTER-108 as a SEPARATE dispatch aggregating 106/107/108 — this finalize runs NO meta-phase housekeeping).** **HEADLINE — the carried WAVE-3 dofset/BC-cluster reachability gap is RESOLVED by GROUNDING, per a NEW 2026-06-05 user directive: when the reachability-GC surfaces an unreachable node that is a genuine future/absorbed dependency of a goal node, GROUND it into the goal node with an honestly-typed `depends-on` edge rather than removing it / filing it as detritus. (D1, THE LEAD, HIGH) audited the BC/divfree cluster, found it firm-but-absorbed-below-column (the c106-recommended `column→eliminate_bc` edge would have been an UNFAITHFUL over-link — the driver columns compose `fe_assemble`, BC-elimination is a SEPARABLE post-composition absorbed into `models/`-level operator construction per `L4/fe_assemble.md:121` + `palace/models/{laplaceoperator.cpp:240,252,modeeigensolver.cpp:570-611}` + `palace/linalg/rap.cpp:140-143,181`), and GROUNDED it with 3 honestly-typed, citation-grounded (critic-verified faithful) `depends-on` edges: `L4/fe_assemble →(absorbed-post-composition)→ L4/eliminate_bc`; `feature/eigenmode.L4 →(constrains-eigvec)→ L3/divfree-projector`; `L3/divfree-projector →(uses)→ {L1/set_subvector_zero, concepts/set_subvector_zero}`. **reachable 88→95 (+7 nodes rescued: `eliminate_bc`, `dofset`, `L3/divfree-projector`, `L2/divfree-projector`, `L1/set_subvector_zero`, `concepts/set_subvector_zero`, `L4-L3/bc-elimination-post-composition-dissolution` [rode along transitively]), 0 regressions, `rank_violations` HELD 0.** This resolved BOTH carried OQs (`bc-driver-column-eliminate-bc-edge-gap-blocks-dofset-rescue` + `set-subvector-zero-cluster-reachability-not-rescued-by-reference-backlink`) grounded-and-rescued. Net WAVE-3 record-rescue arc: 5 of 6 records rescued c106 (`sim-state`/`krylov`/`step-outputs`/`prev-carry`/`solve-result`), the 6th (`dofset`) + the divfree leg grounded c107 — **the record-rescue tranche is COMPLETE.** (D2, LOW) typed 15 of the 16 strict-zero cluster-B/C non-node concept pages with the batch-33-ratified `reference`-only encoding (`untyped` 76→61, −15; `reachable` HELD 95; `rank_violations` HELD 0); the 16th (`concepts/counter-update`) DEFERRED as a node needing `rank:` (OQ `concepts-counter-update-needs-node-rank-and-depends-on-edges`, routed to batch-34). 2 reports applied clean (2/2 staging rows == 2 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur — EIGHTY-EIGHTH consecutive clean staging / 102nd consecutive clean split-integrator cycle); zero deferrals (D2's `counter-update` was a deliberate planned single-page deferral, captured by its OQ — not a report deferral), zero rejections, zero gate-hits, zero finalize build-repairs, NO repair phase. **BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook + linkcheck2) EXIT 0 (~93s) — all 18 touched `book/` files are frontmatter-only edits, every edge target resolves on-disk → linkcheck2-clean; no new file → no SUMMARY/index insert; only the 3 pre-existing benign KaTeX `Potential incomplete link` WARNs in `design/l4_calculus.md`; NO finalize build-repair needed. **STEP-5b graded-stack linters on the LANDED tree:** **`rank_violations: 0`** (baseline fully discharged c096 → ANY violation would be NEW and BLOCK; there are NONE — GATE PASSES; the 3 new D1 grounding edges are firm→firm or firm→untyped-non-node, no promotion flip to gate) + **NO newly-orphaned node** (`reachable` HELD 95 across D2) + **`unresolved_depends_on_targets: 0`** (HELD). Totals: `files=355, typed=294, untyped=61 (was 76, −15), roots=36, reachable=95 (was 88), rank_violations=0, unresolved_depends_on_targets=0, promotion_frontier=8, detritus=163 (was 149 post-D1; see RECLASSIFICATION caveat)`. **DETRITUS-RISE CAVEAT (NOT a regression):** detritus 149→163 (+14) is PURELY the untyped→typed-non-node reclassification — the 14 newly-typed `concepts/*` pages carry no liveness by construction (a non-node asserts no blocking dependency) and join the counted-detritus set, the same disposition as the pre-existing `concepts/{dot,nrm2,scal,apply_linop,axpy}` reference-only pages; 0 nodes lost reachability (`reachable` held 95), verified by git-stash set-diff of the `detritus` JSON node-sets. Report the detritus number with this reclassification caveat so it is not misread as backsliding. **rank_violations cycle-over-cycle trend: 22 (c094) → 1 (c095) → 0 (c096) → … → 0 (c105) → 0 (c106) → 0 (c107). reachable trend over the WAVE-3 campaign: 36 (c105-end, pre-fix) → 81 (batch-33 meta) → 88 (c106) → 95 (c107).** **CARRIED FOLLOW-UP routed to the batch-34 meta-phase:** the **L1-BC-op-tail lowering-chain-liveness gap** — `L1/divfree-projector` did NOT flip reachable (an edge-untypedness artifact: `L2/divfree-projector.md` is a PRE-SCHEME chapter with no `edges:` block, so the L2→L1 mark-sweep dead-ends; same root cause as the L1 BC-op tail + the `L1-L0/set-subvector-zero-mutation-rotation` untyped legacy edges). Routed as OQ `lowering-chain-liveness-not-propagated-to-l1-ops`. **CAMPAIGN STATE: P0/P2/P3 DONE; P1 typed-edge campaign — WAVE-3 RECORD-RESCUE TRANCHE COMPLETE (all 6 internal records + the BC/divfree absorbed clusters now root-reachable; reachable 36→95 over the campaign) + the strict-zero concept-page lazy-tail sub-target 15/16 DONE. REMAINING P1: the `counter-update` node-typing + the L1-BC-op-tail lowering-chain-liveness gap (both routed to the batch-34 meta-phase) + the lazy-untyped tail. The batch-34 meta-phase fires after cycle-108's finalize (aggregating 106/107/108).** **COUNTS: no firm-count status flips this cycle** — D1's 3 grounding edges + D2's 15 non-node typings are all frontmatter-only (no new authored vocabulary, no node promotion). Layer tallies unchanged: L1 firm 33 main / 40 grand, L4 firm 21 main / 25 grand, L4>L3 11, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 34, methodology 4, feature spine 11 FIRM / 1 seed, L4 reduce-family 4 verbs ALL firm. **SLICE CORPUS: 0.** The measurable movement is entirely on the reachability axis (`reachable` 88→95, 7 absorbed-cluster nodes grounded-and-rescued) + the typed-edge axis (`untyped` 76→61). **NEXT: cycle-108 (batch-34 position 3/3, the BATCH-CLOSING cycle) — candidates: the `counter-update` node-typing, the L1-op-tail lowering-edge typing, or remaining lazy-untyped-tail work; the batch-34 meta-phase fires after its finalize.**

**GRADED-STACK P1 TYPED-EDGE CAMPAIGN — WAVE-3 OP-CHAPTER `uses-record` TYPING TRANCHE (the reachability axis is now MEASURABLE; 5 records rescued, `reachable` 36→88, `unresolved_depends_on_targets` 21→0; post-cycle-106, batch-34 position 1/3 — the OPENING primary cycle of meta-batch-34; cycles 106/107/108; the batch-34 meta-phase fires NEXT-AFTER-108 as a SEPARATE dispatch aggregating 106/107/108 — this finalize runs NO meta-phase housekeeping).** **HEADLINE — the batch-33 meta-phase fixed its MUST-DO #1 (the `parse_frontmatter` block-mapping-edge bug that made reachability invisible), and this cycle is the FIRST to exploit it: the WAVE-3 op-chapter typing tranche landed across 5 clean reports. (D1, THE LEAD) `L4/ksp_solve` migrated to a typed `edges:` block + `L4/krylov-step` `edges:` block authored FROM SCRATCH (it had NO frontmatter) → `krylov-step` now ROOT-REACHABLE (`← L4/ksp_solve, L3/krylov-step`), 5 internal records RESCUED from GC-garbage via op-chapter `uses-record` edges (`sim-state`/`krylov`/`step-outputs`/`prev-carry`/`solve-result`), `L4/iterate-while` rescued as a bonus, detritus 163→156. (D2) `L4/solve_family` + `L4/fold_solve` migrated (sim-state rescued, op-params inbound climbed to 5 sources; `fold_solve→solve_family` a DELIBERATE `reference` contrast-sibling carrying no liveness). (D3) `L4/eliminate_bc` migrated + the stale `concepts/DofSet.md`→`concepts/dofset.md` record-home prose retargeted (the c103-D2 nonexistent-page flag dropped) — FAITHFUL-PATH-OR-FINDING: `dofset` STAYS unreachable because NO feature column links to `L4/eliminate_bc`; D3 declined to force an unfaithful column→eliminate_bc edge, routed OQ `bc-driver-column-eliminate-bc-edge-gap-blocks-dofset-rescue` (THE ONE CARRIED FOLLOW-UP — the 6th record). (D4, LOW) `concepts/set_subvector_zero` `reference` back-link de-staled (doubly-stale `reference: []` + false comment removed; bidirectional now) — a reference creates no reachability (cluster stays detritus, OQ `set-subvector-zero-cluster-reachability-not-rescued-by-reference-backlink` routes the sub-question into the same dofset/eliminate_bc follow-up). (D5) the 18-host lazy-tail legacy frontmatter reclassified into typed `edges:` blocks → `unresolved_depends_on_targets` 21→0 (combined with D2's solve_family migration), `--strict` EXIT 0 — all 21 were false-positives from un-migrated legacy frontmatter (36 distinct re-encoded targets all resolve on-disk). `rank_violations` HELD at 0 throughout, NO newly-orphaned node, `untyped` 77→76 (D4's typing −1).** 5 reports applied clean (5/5 staging rows == 5 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the EIGHTY-SEVENTH consecutive clean staging / 101st consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs, NO repair phase. **BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook + linkcheck2) EXIT 0 (~93s) — all 24 touched `book/` files are frontmatter-only edits (+ D3 §Record-definition prose retarget), every edge target resolves on-disk → linkcheck2-clean; no new file → no SUMMARY/index insert; only the pre-existing benign KaTeX WARNs; NO finalize build-repair needed. **STEP-5b graded-stack linters on the LANDED tree:** **`rank_violations: 0`** (baseline fully discharged c096 → ANY violation would be NEW and BLOCK; there are NONE — GATE PASSES; every WAVE-3 chapter rank: firm on all-firm depends-on targets) + **NO newly-orphaned node** + **`unresolved_depends_on_targets: 0`** (THE CAMPAIGN GOAL — 21→0, `--strict` EXIT 0). Totals: `files=355, typed=279, untyped=76 (was 77, −1), roots=36, reachable=88 (was 36 at c105-end PRE the batch-33 linter fix), rank_violations=0, unresolved_depends_on_targets=0, promotion_frontier=8, detritus=156 (was 163 at the report's c105 baseline)`. **rank_violations cycle-over-cycle trend: 22 (c094) → 1 (c095) → 0 (c096) → … → 0 (c104) → 0 (c105) → 0 (c106). reachable trend: 36 (c105-end, pre-fix) → 81 (batch-33 meta) → 88 (c106).** **LATENT linter-reader bug routed (D5 OQ `graded-stack-lint-block-mapping-misparse-on-legacy-edge-prose-colon`):** the parser mis-reads legacy `:`-in-qualifier items as block-mapping dicts; D5's migration removed the trigger for the 18 files but the bug is latent until the next un-migrated `:`-bearing legacy item — a fix-reader-vs-rely-on-migration decision for the batch-34 meta-phase. **CAMPAIGN STATE: P0/P2/P3 DONE; P1 typed-edge campaign — WAVE-3 OP-CHAPTER TYPING tranche LANDED (5 WAVE-3 chapters migrated, `krylov-step` authored, 5 of the 6 records rescued, reachable 36→88) + the lazy-tail `unresolved_depends_on_targets` sub-target COMPLETE (21→0). REMAINING P1: the dofset/eliminate_bc column-edge follow-up (the 6th record — add `feature/{electrostatic,magnetostatic,eigenmode}.L4 →composes eliminate_bc`) + remaining un-migrated WAVE-3 op-chapters + the lazy-untyped tail. The batch-34 meta-phase fires after cycle-108's finalize (aggregating 106/107/108) and should weigh the latent linter-reader bug + the "integrator-signals stale at cycle-019" planner false-read (finalize IS still appending — chain unbroken).** **COUNTS: no firm-count status flips this cycle** — all 5 reports were frontmatter edge-typing migrations (+ D3 prose retarget), not new authored vocabulary (`L4/krylov-step` was authored only at the frontmatter/edges level; its body pre-existed). Layer tallies unchanged: L1 firm 33 main / 40 grand, L4 firm 21 main / 25 grand, L4>L3 11, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 34, methodology 4, feature spine 11 FIRM / 1 seed, L4 reduce-family 4 verbs ALL firm. **SLICE CORPUS: 0.** The measurable movement is entirely on the reachability axis (`reachable` 36→88, `detritus` 163→156, 5 records rescued, `unresolved_depends_on_targets` 21→0). **NEXT: cycle-107 (batch-34 position 2/3) continues the WAVE-3 frontier — the dofset/eliminate_bc column-edge follow-up is the natural HIGH-fan-out LEAD.**

---

**GRADED-STACK P1 TYPED-EDGE CAMPAIGN — CONTENT TAIL + BATCH-CLOSING (energy-fields linked → ALL 12 config-input columns carry a `uses-record` edge + `set-subvector-zero-mutation-rotation` L1>L0 theme authored + citation hygiene; untyped held 77; post-cycle-105, batch-33 position 3/3 — the BATCH-CLOSING primary cycle of meta-batch-33; cycles 103/104/105; the batch-33 meta-phase fires NEXT, after this finalize, as a SEPARATE dispatch aggregating 103/104/105 — this finalize runs NO meta-phase housekeeping).** **HEADLINE — the P1 campaign's CONTENT TAIL landed across 4 clean reports: (D1) the `energy-fields.L4` column gained the 12th-and-final `depends-on (kind: uses-record)` → `config-record` edge (FOLD decision; PostprocessConfig is a read-only IoData sub-record projection, no new concept page; ALL 12 config-input feature columns now carry the edge — OQ `energy-fields-config-and-domaindata-records-need-concept-pages` closed); (D3) 4 bare `main.cpp:NNN` citations in `config-record.md` disambiguated to `palace/main.cpp:NNN` (the c103/c104-carried AMBIG residue — citecheck now 31 ok / 0 failing); (D4) a NEW firm L1>L0 theme `set-subvector-zero-mutation-rotation` authored (the coupled other half of the c104 `set_subvector_zero` landing) + the L1 entry's 4 `(forthcoming)` forward-refs de-staled to live links; (D2) a confirmed NO-OP — the c104 critic's `±1 prose-citation drift` on `op-params`/`sim-state` was itself a CODEMAP read_range `+1` false-positive on a comment/declaration boundary, the prose was always correct, OQ `record-concept-prose-citation-pm1-drift` resolved as a false-positive. The linter `untyped` count HELD at 77 (the new theme is typed-from-start: `files` 354→355, `typed` 277→278), `rank_violations` HELD at 0, NO newly-orphaned node. THE L1>L0 THEME TALLY +1.** 4 reports applied clean (4/4 staging rows == 4 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the EIGHTY-SIXTH consecutive clean staging / 100TH consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs. **(SUPERSEDED SUMMARY of the c104 second-tranche, retained for the batch arc:) the 6 record-concept pages (`krylov`/`op-params`/`sim-state`/`step-outputs`/`prev-carry`/`solve-result`) typed FIRM (closing the c103 carry-forward), 1 NEW firm L1 entry `set_subvector_zero` (the last homeless-primitive leg resolved), 12 feature-column `depends-on (kind: uses-record)` edges that make `config-record` (11 columns) + `op-params` (transient) ROOT-REACHABLE — 2 of 8 graded-stack records RESCUED from GC-garbage — and 2 prose-drift fixes. The linter `untyped` count dropped 78 → 77, `rank_violations` HELD at 0, NO newly-orphaned node.** 4 reports applied clean (4/4 staging rows == 4 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the EIGHTY-FIFTH consecutive clean staging / NINETY-NINTH consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs. **WHAT LANDED:** **D1 (layer-intro-author):** the 6 record-concept pages typed `rank: firm`/`kind: record` (`cites-evidence` `depends-on` → L0 `iterative.{hpp,cpp}`; the 3 constructive records point at per-field positive backing ranges, a by-design struct-granularity exception, critic-verified); closes OQ `graded-stack-six-record-concept-pages-need-frontmatter`. **D3 (harvester):** `book/src/L1/set_subvector_zero.md` CREATED — the `s=0.0` `SetSubVector` index-set vector-zeroing primitive (the diagonal 0/1 projector `Z_idx = I − P_idx`, vector-side BC-cleanup atom, `rank: firm` firm-on-positive-structure escape; 3 `cites-evidence` `depends-on` → L0 `vector.cpp:461-474`/`:476-492`/`vector.hpp:220-221`; repairer had repaired 2 build-critical warnings pre-integration — a dangling `depends-on` to the forthcoming L1>L0 theme retyped to the L0 cites + a dangling `reference` to the speculative L3 seed removed — so well-foundedness holds + build dangling-free); SUMMARY + L1-index dep-map alpha-after `scal`; `concepts/trsv` + `gemv_basis` repointed + §Disposition; resolves all 3 legs of OQ `concept-primitive-without-L1-home-trsv-set_subvector_zero-gemv_basis`. **D2 (layer-intro-author):** 12 `depends-on (kind: uses-record)` edges (11 columns → `config-record` + `transient.L4` → `op-params`) + 4 reciprocal `reference` back-refs in `config-record.md` (now 11 columns); `energy-fields.L4` DELIBERATELY NOT LINKED (no `PostprocessConfig`/`DomainData` concept pages, 0 danglers); opened HIGH OQ `solve-record-reachability-needs-op-chapter-uses-record-edges` + MEDIUM OQ `energy-fields-config-and-domaindata-records-need-concept-pages`. **D4 (lifter):** de-staled the `eliminate_rhs` cohort bullet in `L1/index.md` (re-anchored to the live firm theme `fe-operator-assemble-mutation-rotation`) + slug-fixed `incremental-least-squares` (`givens-rotation` → live `givens`); resolves 2 pre-existing c103 LOW OQs. **BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook 0.5.1 + linkcheck2) EXIT 0, ~92s — the new `set_subvector_zero.md` (SUMMARY-wired + L1-index-registered) + 6 record-page prepends + 12 feature-column edge inserts all linkcheck2-clean; `energy-fields` deliberately unlinked (0 dangling); the finalize-side L1-index firm-count narration edit re-built clean; only the 4 pre-existing benign KaTeX WARNs; NO finalize build-repair needed. **STEP-5b graded-stack linters on the LANDED tree:** **`rank_violations: 0`** (baseline fully discharged c096 → ANY violation would be NEW and BLOCK; there are NONE — GATE PASSES; all new ranked nodes rest on `cites-evidence`-to-L0 or firm targets, well-foundedness holds for every `depends-on` edge) + **NO newly-orphaned node**. Totals: `files=354 (was 353, +1 set_subvector_zero), typed=277 (was 275, +2), untyped=77 (was 78, −1), roots=36, reachable=36, rank_violations=0, promotion_frontier=8, unresolved_depends_on_targets=35, detritus=228`. **rank_violations cycle-over-cycle trend: 22 (c094) → 1 (c095) → 0 (c096) → … → 0 (c103) → 0 (c104).** The `untyped` net −1 (not the predicted −6) is because 5 of the 6 record pages were ALREADY counted `typed` at c103-end via `reference`-only frontmatter — D1's firm-typing flipped only 1 out of `untyped`; `set_subvector_zero` registers as a new typed file (raises files+typed, not untyped). Monotone-non-increasing, gate respected; all 6 + `set_subvector_zero` confirmed `untyped=False, rank=3.0`. **NEW LINTER GAP SURFACED THIS CYCLE (informational, exit unaffected, routed to meta/tools): the linter's hand-rolled `parse_frontmatter` does NOT parse the multi-line block-mapping `- target: X` / `  kind: Y` edge form (only the inline-flow `{target: X, kind: Y}`) — block-mapping `depends-on` targets store as malformed `"target: X"` strings the reachability mark cannot traverse, so `config-record`/`op-params` STILL read `garbage?` in `--show-inbound` despite their correct on-disk `uses-record` edges. The content is RIGHT; the linter just can't see block-mapping edges. PRE-EXISTING + uniform (affects every block-mapping edge incl. c103's), NOT a new orphan; compounds with the c103-flagged `is_likely_outside_dag` + `uses-record-kind` gaps.** **CAMPAIGN STATE: P0 DONE; P3 DONE; P2 slice-deletion DONE (corpus 9→0, c097/098/099); P1 typed-edge campaign SECOND TRANCHE landed (record pages typed firm + `set_subvector_zero` homed + first feature-column `uses-record` edges; untyped 78→77; `config-record`/`op-params` root-reachable). REMAINING P1: WAVE-3 op-chapter `uses-record` typing — the 6 internal solve/BC records (`sim-state`/`krylov`/`step-outputs`/`prev-carry`/`solve-result`/`dofset`) reach the GC roots ONLY via op-chapter edges, and the L4 solve/BC op chapters still carry pre-scheme frontmatter (`krylov-step` has none) — the natural cycle-105 LEAD (OQ `solve-record-reachability-needs-op-chapter-uses-record-edges`, HIGH); plus the broader operator/theme edge-typing tail; the meta-phase owns campaign sequencing.** **COUNTS: L1 firm 32 main / 39 grand → 33 main / 40 grand** (+1 `set_subvector_zero`, the 33rd main-cohort firm member; on-disk dep-map firm-row count = 40 confirmed; finalize is count-owner, updated the `L1/index.md` narration — D3 deferred it); no other vocabulary status flips (record-page typing + 1 new L1 leaf + edge-typing metadata + prose-drift): L4 firm 21 main / 25 grand, L4>L3 11, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 34, methodology 4, feature spine 11 FIRM / 1 seed, L4 reduce-family 4 verbs ALL firm. **SLICE CORPUS: 0** (deleted c097/098/099). **NEXT: cycle-105 (batch-33 position 3/3, BATCH-CLOSING) runs the standing forward frontier — the natural LEAD is the WAVE-3 op-chapter `uses-record` typing that rescues the remaining 6 internal records; the batch-33 meta-phase fires after cycle-105's finalize (aggregating 103/104/105) and should UNIFY the node-status convention divergence, RATIFY the container convention into graded-stack-scheme.md, route the linter gaps to tools (now including the block-mapping-edge parser gap surfaced this cycle + the c103 outside-dag/uses-record gaps), and confirm the P1-edge-typing role home.**

---

**GRADED-STACK P1 TYPED-EDGE CAMPAIGN — FIRST TRANCHE (concepts substrate + containers typed; untyped 142 → 78; post-cycle-103, batch-33 position 1/3 — the FIRST primary cycle of meta-batch-33; cycles 103/104/105; the batch-33 meta-phase fires AFTER cycle-105, NOT this cycle — this finalize runs NO meta-phase housekeeping).** **HEADLINE — the meta-phase-owned graded-stack typed-edge campaign (priorities item 0, standing duty) LED this cycle: its FIRST P1 tranche landed — ~45 concept pages + 2 concept-infra pages + 35 layer-index/group-intro container pages got typed `edges:` frontmatter + 1 NEW record-concept page (`dofset`) authored as a fully-typed node. The linter `untyped` count dropped 142 → 78 (−64), `rank_violations` HELD at 0.** 8 reports applied clean (8/8 staging rows == 8 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the EIGHTY-FOURTH consecutive clean staging / NINETY-EIGHTH consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs. **WHAT TYPED (D1–D5 + D7 layer-intro-author):** **D7:** `book/src/concepts/dofset.md` CREATED — the `DofSet[N]` record-definition home (rank: firm, kind: record, typed `edges:`, SUMMARY-wired alpha between derived-view-hoisting and dot), the ONLY new ranked node besides config-record + closes 3 c055-cohort OQs. **D1 (cluster A, 16 pages):** `reference`-only typed edges on the blas1/operator-application/lift concept pages. **D2 (cluster B):** the ONE node `config-record` typed `rank: firm` + 3 `cites-evidence` L0 depends-on edges; its 16 non-node siblings got NO frontmatter (the §5 strict-zero encoding). **D3 (cluster C, 12 pages):** `reference`-only typed edges on the gmres/givens/obstruction/disposition meta-pages. **D4 (infra reconcile):** `concepts/index.md` + `concepts/dependency-map.md` typed `kind: navigational-container` (no rank, reference-only); the dependency-map's 3 Mermaid sub-graphs re-derived to `-.->|ref|` (c101 over-asserted blocking dependence among non-node pages); the `dofset` node + member-table row wired. **D5 (35 container pages):** all 8 layer indices + 4 lowering indices + feature Part index + 3 feature-group pages + 23 group-intro pages typed `kind: navigational-container` (no rank, reference-only). **CONTENT TAIL (D6 + D8):** **D6 (abstractor):** the `eliminate_rhs` L1>L0 leg FOLDED into the firm `fe-operator-assemble-mutation-rotation` theme (new anchored §"The `eliminate_rhs` leg (folded here)" + a dangling `lowers_to:` edge repointed to the real firm theme + 6 de-stale `(forthcoming)` sites across L1/L4/L4-L3); closes OQ `eliminate-rhs-mutation-rotation-l1-l0-half-forthcoming-vs-already-folded`. **D8 (layer-intro-author):** 2 §Vocabulary-cohort prose bullets added to `L4/index.md` (`eliminate_bc` + `preconditioning-framework`); closes OQ `vocabulary-cohort-bullets-missing-for-precond-framework-and-eliminate-bc`. **BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook 0.5.1 + linkcheck2) EXIT 0, ~92s — the ~70 frontmatter prepends + the new dofset.md (SUMMARY-wired) + the re-derived dependency-map Mermaid all co-landed link-safe (dofset.html rendered → SUMMARY link + index member-row + dep-map prose link all resolved; D7 created dofset before D4 referenced it); only the 4 pre-existing benign KaTeX `Potential incomplete link` WARNs in `design/l4_calculus.md`; NO finalize build-repair needed. **STEP-5b graded-stack linters on the LANDED tree:** **`rank_violations: 0`** (baseline fully discharged c096 → ANY violation would be NEW and BLOCK; there are NONE — GATE PASSES; the only 2 NEW ranked nodes `dofset` + `config-record` both rest on `cites-evidence` L0 edges, well-foundedness vacuously holds) + **NO newly-orphaned node** (no node removed; new nodes ADDED). Totals: `files=353 (was 352, +1 dofset), typed=275 (was 210, +65), untyped=78 (was 142, −64), roots=36, reachable=36, rank_violations=0, promotion_frontier=8, unresolved_depends_on_targets=34, detritus=226 (of which detritus_with_typed_edges_stronger_signal=63 = the EXPECTED linter-noise: the 23 group-intros + concepts/dependency-map read as detritus because `is_likely_outside_dag` does NOT yet recognize `kind: navigational-container` — informational, exit stays 0, fix routed to meta-phase/tools)`. **rank_violations cycle-over-cycle trend: 22 (c094) → 1 (c095) → 0 (c096) → … → 0 (c101) → 0 (c103).** **CAMPAIGN STATE: P0 DONE; P3 DONE; P2 slice-deletion DONE (corpus 9→0, c097/098/099); P1 typed-edge campaign FIRST TRANCHE landed (concepts substrate + containers typed, untyped 142→78). REMAINING P1: ~6 record-concept pages still untyped (krylov/op-params/sim-state/step-outputs/prev-carry/solve-result — the follow-on record-page tranche) + the broader operator/theme/feature-column edge-typing tail; the meta-phase owns campaign sequencing.** **COUNTS UNCHANGED from c102** (this cycle is edge-typing-metadata + content-tail prose, no vocabulary status flips; dofset is a record-concept page, config-record gained a rank but was already on disk): L1 firm 32 main / 39 grand, L4 firm 21 main / 25 grand, L4>L3 11, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 34 (+1 dofset), methodology 4, feature spine 11 FIRM / 1 seed, L4 reduce-family 4 verbs ALL firm. **SLICE CORPUS: 0** (deleted c097/098/099). **NEXT: cycle-104 (batch-33 position 2/3) runs the standing forward frontier (campaign P1 continuation candidates: the 6-record-page tranche, then operator/theme edge-typing); the batch-33 meta-phase fires after cycle-105's finalize (aggregating 103/104/105) and should UNIFY the node-status convention divergence (D1/D3 `reference`-only-block vs D2 strict-zero-frontmatter on non-node pages — OQs `graded-stack-concept-node-status-convention` / `concept-non-node-frontmatter-encoding-reference-only-vs-empty`), RATIFY the container `kind: navigational-container` convention into graded-stack-scheme.md, route the 2 linter-gaps to tools (outside-dag recognition of group-intros/dependency-map; config-record+dofset reachability via `kind: uses-record` from consuming columns), and confirm the P1-edge-typing role home (6 of 8 dispatches were layer-intro-author doing bulk frontmatter authoring — planner-flagged role-fit friction).**

---

**GRADED-STACK P2 SLICE-DELETION CAMPAIGN — SECOND TRANCHE (2 more slices deleted + unreachable; slice corpus 5 → 3; post-cycle-098, batch-31 position 2/3 — the SECOND primary cycle of meta-batch-31; cycles 097/098/099; the batch-31 meta-phase fires AFTER cycle-099, NOT this cycle — this finalize runs NO meta-phase housekeeping).** **HEADLINE — the P2 slice-deletion campaign's SECOND tranche landed: `orthog` + `polynomial_recurrence_step` BOTH DELETED + made fully unreachable from any GC root, their material verified-already-firm-homed (no-op absorbs) and ALL inbound markdown links repointed. The slice corpus shrank 5 → 3.** 3 reports applied clean (3/3 staging rows == 3 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the SEVENTY-NINTH consecutive clean staging / NINETY-THIRD consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs. **THE 2 DELETIONS (D1 + D2 same-layer-cross-cutter slice-deletes, each verified-no-op absorb + repoint):** **D1:** `orthog` slice DELETED (verified-no-op absorb — its MPI-collective-shape + L1-invariant content fully firm-homed already) + 2 original cites repointed (PC-1 `concepts/gemv_basis.md`, PC-2 `L1/orthogonalize.md`) PLUS 8 inbound markdown links the original sweep MISSED — found by the critic, added by the repairer (PC-4 a–f): `L0/mpi-globalsum-and-collectives.md:69,:105`, `concepts/orthogonalization.md:77`, `concepts/gmres.md:23`, `concepts/sequential-obstruction.md:48`, and the 4 dangling `./orthog.md` sibling links inside the SURVIVING `spec/slices/arnoldi_step.md:67,:95,:115,:144` (repointed to `concepts/orthogonalization.md`, arnoldi_step survives for c099). **D2:** `polynomial_recurrence_step` slice DELETED + a SURGICAL polynomial-clause drop at `L2/krylov-step.md:7` (the cg/gmres/chebyshev/arnoldi clauses PRESERVED byte-exact for the c099 krylov-trio sub-campaign) + R2/R3/R9 anchor repoints (`L2/krylov-step.md:142`, `L2/index.md:135`, `L3/krylov-step.md:200` → `concepts/negative-result-slice.md` + `L4/chebyshev.md` §Semantics) + R4 mermaid edge removal + R5 `negative-result-slice.md:46,66` delink + R7/R8 removal of BOTH the orthog AND polynomial rows from `SUMMARY.md` + `spec/index.md` (the D1↔D2 co-application serialization caveat — D1 deleted orthog's file, D2 removed both orphaned nav rows). **THE WITHIN-FILE LAND-CLEAN (D3 lifter):** `book/src/L4/domain_energy_reduce.md:313-316` §Status closing parenthetical re-anchored — dropped the post-c095 FALSIFIED "gram_reduce STAYS rough-in because bilinear-form is still rough-in" maturity assertion (both firm on disk since c095), recast as the permanent rank-1-vs-rank-2 / single-field-vs-family-PAIR SHAPE distinction; RESOLVES the OQ `domain_energy_reduce-313-gram_reduce-bilinear-form-c095-stale-rough-in-narration`. **THE BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook + linkcheck2) EXIT 0, ~92s — the 2 slice deletions + all ~10 inbound repoints + the SUMMARY/index/mermaid removals all co-landed link-safe (verified: no dangling `[..](..)` to `orthog.md`/`polynomial_recurrence_step.md`; no plain-text slice anchor survives; no SUMMARY entry points at a deleted file; the 3 surviving slice rows cg/gmres/arnoldi_step intact); only pre-existing benign KaTeX `Potential incomplete link` WARNs in `design/l4_calculus.md`; NO finalize build-repair needed. **STEP-5b graded-stack linters on the LANDED tree** (`python3 tools/graded-stack-lint/graded_stack_lint.py --json`): **`rank_violations: 0`** (was 0 at c096/c097; baseline-exceptions discharged at c096 → ANY violation would be NEW and block — there are NONE; GATE PASSES) + **NO newly-orphaned node** (the 2 deleted slices were `reference`-class detritus leaves in the pre-P1 detritus mass, an INTENDED deletion not an orphaning — the other step-5b block condition also clears). `rank_histogram: {firm: 192, rough-in: 7, partly-constructive: 3, obstruction: 2, partial-obstruction: 4}`; `files=354 (was 356 — the 2 deleted slices), typed=208, untyped=146 (was 148 — the 2 deleted slice nodes left the untyped tail), roots=36, promotion_frontier=10, detritus=172, unresolved_depends_on_targets=35`. **rank_violations cycle-over-cycle trend: 22 (c094) → 1 (c095) → 0 (c096) → 0 (c097) → 0 (c098).** The slice-node set in the linter output is now EXACTLY the 3 survivors (cg/gmres/arnoldi_step). The high untyped/detritus mass is P2 mid-campaign (3 slices still present) — informational, NOT a block. **CAMPAIGN STATE: P0 DONE; P3 DONE; P1 DONE (rank-violation criterion MET = 0); P2 slice-deletion SECOND TRANCHE landed (orthog + polynomial deleted + unreachable; slice corpus 5 → 3). REMAINING P2 (c099): the krylov trio `cg`/`gmres`/`arnoldi_step` — the deeply-interwoven ~30-anchor single-coordinated-owner sub-campaign (NOT 3 parallel dispatches). Completion criterion = the reachability GC shows `spec/slices/*` empty; ON COMPLETION at c099 → retire the `annotated-and-retained` carve-out + skill `phase-1-slice-reduction-audit`.** **COUNTS UNCHANGED from c097** (the slice corpus is Phase-1 raw material, NOT layered-vocabulary counts; D3 is a within-file maturity-NARRATION re-anchor with `rank: firm` already on disk, no node status flip): L1 firm 32 main / 39 grand, L4 firm 20 main / 24 grand, L4>L3 10, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 33, methodology 4, feature spine 11 FIRM / 1 seed, L4 reduce-family 4 verbs ALL firm. **SLICE CORPUS: 5 → 3** (`spec/slices/`: `arnoldi_step`, `cg`, `gmres` remain — the c099 krylov-trio sub-campaign). **OQs (per-report intake, RECOMMENDED for the batch-31 meta unify to CLOSE — per-report integrators have no OQ-close authority; finalize RECORDS the recommendation, the meta-phase closes):** `domain_energy_reduce-313-gram_reduce-bilinear-form-c095-stale-rough-in-narration` (D3, resolved-by-re-anchor), `orthog-slice-substantive-absorb-framing-was-a-verified-no-op` (D1 — records the stale "substantive MPI-collective-shape absorb" tranche framing as a VERIFIED NO-OP), and `polynomial-recurrence-step-slice-absorb-verified-no-op-and-campaign-state-5to3-slices` (D2). **STILL-OPEN (for c099 + downstream):** the arnoldi_step end-bound citation-divergence `:73-109`/`:73-120`/`:73-118` (to reconcile when the trio repoints), and the c098-D3 sibling cross-file `bilinear-form`-c095-firm-flip residue-class sweep (other files co-mentioning gram_reduce+rough-in — out of D3's within-file scope, for the batch-31 §Intake→plan migration). **NEXT: cycle-099 (batch-31 position 3/3) executes the krylov-trio sub-campaign as ONE single-coordinated-owner dispatch (the `cg.md:27-141` L4-v0.5 cg-unrolling material may be the one genuinely-unlifted datum needing a firm home or `roadmap_goal` before cg deletes); the batch-31 meta-phase fires after cycle-099's finalize (aggregating 097/098/099) and should close the recommended-resolved OQs above + retire the carve-out/skill on campaign completion.**

**BATCH-32 CLOSE — HYGIENE CYCLE; THE IN-SCOPE STACK IS SUBSTANTIALLY L4-COMPLETE FOR BACKEND-LOWERING AND THE CLEAN-GATED FORWARD FRONTIER IS SUBSTANTIALLY EXHAUSTED (post-cycle-102, batch-32 position 3/3 — the BATCH-CLOSING cycle of meta-batch-32; cycles 100/101/102; the batch-32 meta-phase fires AFTER this cycle-102 finalize as a SEPARATE dispatch aggregating 100/101/102 — this finalize ran NO meta-phase housekeeping).** **HEADLINE — cycle-102 is a pure-hygiene cycle (no node promotions, no new `depends-on` edges, no coverage movement): the L4 `index.md` §Vocabulary-cohort firm-count narration was corrected from the two-landings-stale `(19 + 4 outer-driver)` to the on-disk-authoritative `(21 + 4 outer-driver)` (the lag was `preconditioning-framework` c096 + `eliminate_bc` c101) with two narration sentences added, and 5 inline-prose citations in `L4-L3/fe-assemble-fold-dissolution.md` were full-path-disambiguated (`integrator.hpp:58-61` AMBIG + `libceed/operator.cpp:455` MISS → `palace/fem/...`, both now citecheck `[ok]`).** 2 reports applied clean (2/2 staging rows == 2 dispatched-ready — the cycle-018 staging-completeness gap did NOT recur; both all-pass clean, no repair phase ran); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs. **THE BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook + linkcheck2) EXIT 0, ~92s — prose/citation-text edits only (the 5 repointed citations are inline-code backticks, not markdown links, so `linkcheck2` cannot dangle on them); only the pre-existing benign KaTeX `Potential incomplete link` WARNs in `design/l4_calculus.md`; NO finalize build-repair needed. Finalize citecheck on both touched files clean (`fe-assemble-fold-dissolution.md` 16 ok / 0 failing — the 2 pre-existing AMBIG/MISS flags resolved; `L4/index.md` 45 ok / 0 failing — the staging-row AMBIG flags were over the report's evidence shorthand, not the landed file). **STEP-5b graded-stack linters on the LANDED tree** (`python3 tools/graded-stack-lint/graded_stack_lint.py --json`): **`rank_violations: 0`** (baseline fully discharged at c096 → ANY violation would be NEW and block — there are NONE; GATE PASSES) + **NO newly-orphaned node** (this cycle adds no nodes and deletes none — text refreshes only). Totals: `files=352, typed=210, untyped=142, roots=36, reachable=36, rank_violations=0, promotion_frontier=8, unresolved_depends_on_targets=35, detritus=174`. **rank_violations cycle-over-cycle trend held flat across all of batch-32: 0 (c100) → 0 (c101) → 0 (c102)** (full arc: 22 c094 → 1 c095 → 0 c096 → 0 c097 → 0 c098 → 0 c099 → 0 c100 → 0 c101 → 0 c102). **THE STRATEGIC SIGNAL FOR THE BATCH-32 META-PHASE — THE CLEAN-GATED FORWARD FRONTIER IS SUBSTANTIALLY EXHAUSTED:** the in-scope stack is now L4-complete for backend-lowering (all 5 drivers + FE-assembly + 5 output-products + BC-elimination reach firm L4); the 8 remaining `promotion_frontier` members are ALL obstruction-/demand-gated, NOT clean-gated picks — `L1-L0/{bicgstab,minres}-iteration` (enum-only-stub obstructions), `L1-L0/eigsolve-convergence-reason-mapping` + `L2/deflate` + `L2-L1/deflate-composition-lowering` (opaque-library / demand-gated), `feature/boundary-mode.{L4,L1,L0}` (demand-gated feature column). **The batch-32 meta-phase should re-assess what the NEXT batch's LEAD even IS** — candidates: the meta-phase-owned full graded-stack edge-typing campaign (priorities item-0 standing duty; c101's D2 typing was LIGHT only); dead orchestrator-era skills retirement (5 skills); the `eliminate-rhs-mutation-rotation` L1>L0 leg; the `record-DofSet` definition home; the missing §Vocabulary-cohort bullets; OR a genuinely new frontier direction. **BATCH-32 ARC (cycles 100/101/102):** c100 firmed two L1>L0 lowering floors (`apply-linop` matvec floor + `ksp-solve` driver floor) + produced the L4 completeness matrix + swept the last class-B slice residue; c101 CLOSED the BC-elimination genuine hole (firm L4 `eliminate_bc` cap + firm `bc-elimination-post-composition-dissolution` L4>L3, tally 10→11) + refreshed concepts index/dep-map; c102 hygiene (firm-count + citation paths). Build green every cycle; rank_violations held at 0 across all three. **COUNTS UNCHANGED from c101** (hygiene cycle — no node status flip): L4 firm 21 main / 25 grand (the narration NOW matches; no NEW firm node landed c102 — the count was already 21 on disk from c101's `eliminate_bc`), L4>L3 11, all other layer-vocabulary counts as c101. **OQs (per-report intake, RECOMMENDED for the batch-32 meta unify):** `vocabulary-cohort-bullets-missing-for-precond-framework-and-eliminate-bc` (D1, OPENED — the §Vocabulary-cohort prose bullets for the two newly-counted chapters are still missing; the count-owner refresh corrected the COUNT but the per-chapter bullets are a follow-up layer-intro-author/harvester-on-touch task). **STILL-OPEN carry-forward for the batch-32 meta-phase to unify** (across 100/101/102): `record-DofSet-needs-definition-home` (≥2 signature consumers, no `concepts/<record>.md` home; unify with the c055 `dof-set-concept-page` cohort), `eliminate-rhs-mutation-rotation-l1-l0-half-forthcoming-vs-already-folded` (the RHS-side L1>L0 leg disposition), `vocabulary-cohort-bullets-missing-for-precond-framework-and-eliminate-bc` (this cycle), and the DOUBLY-stale memory `project_l4_is_backend_lowering_target` (its named hole "FE-assembly/FE-space cohort stranded at L1" is falsified twice over — assemble-half closed c068, BC-half closed c101; memory edits are out of finalize scope, RECORDED for the meta-phase). **STANDING meta-phase-owned campaigns:** the full graded-stack edge-typing pass (D2 c101's typing was LIGHT only — the meta-phase-owned authoritative typed-graph campaign is unstarted); dead orchestrator-era skills retirement (5 skills); the 2 ASK items (`.env.example`; BOOTSTRAP/MIGRATION — already compacted to history-stubs at batch-31). **NEXT: the batch-32 meta-phase fires after this finalize (aggregating 100/101/102) — it should unify the carry-forward OQs, update the doubly-stale memory, and pick (or re-assess) the batch-33 LEAD now that the clean-gated forward frontier is substantially exhausted.**

---

**GRADED-STACK P2 SLICE-DELETION CAMPAIGN — COMPLETE; THE PHASE-1 SLICE CORPUS IS FULLY LIFTED + DELETED (slice corpus 3 → 0; post-cycle-099, batch-31 position 3/3 — the BATCH-CLOSING cycle of meta-batch-31; cycles 097/098/099; the batch-31 meta-phase fires AFTER this cycle-099 finalize as a SEPARATE dispatch aggregating 097/098/099 — this finalize ran NO meta-phase housekeeping).** **HEADLINE — the P2 slice-deletion campaign is DONE: the krylov trio `cg`/`gmres`/`arnoldi_step` DELETED + made fully unreachable, `book/src/spec/index.md` DELETED, the SUMMARY `# Phase 1 corpus` Part removed, and the `spec/slices/` directory is now EMPTY. The Phase-1 slice corpus is fully lifted + deleted (9 → 0 across cycles 097/098/099).** 3 reports applied clean (3/3 staging rows == 3 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the EIGHTIETH consecutive clean staging / NINETY-FOURTH consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs. **THE CAMPAIGN-CLOSING WORK (D1 harvester + D2 harvester + D3 lifter):** **D1:** the cg slice's one genuinely-unlifted datum — the `cg.md:27-141` v0.5 first-iteration-unrolling worked example — ABSORBED into `book/src/L4/krylov-step.md` as a NEW firm `### Worked example — CG Form B (v0.5 first-iteration-unrolling)` subsection (CG-concrete `cg_first_step`/`cg_steady_step` typed bodies + `cg_solve` driver via `iterate_while_with_prev` + `forget_beta_prev` projection + v0.4↔v0.5 equivalence + pcg variant + L0-ground prose); re-anchored that file's dangling `cg.md:*` slice-pointers (§Semantics:82, §Status:152, §Evidence:171). Rank stays 0 (firm-on-positive-structure — syntactic L4-self-rotation identities on the `CgSolver::Mult` read closure; the L0 ground is a rank-exempt `cites-evidence` edge). **D2 (the campaign completion):** repointed ALL 31 inbound Class-A markdown links + the Class-B plain-text hub mentions across the whole krylov hub (L2/L3/L4-L3/L3-L2/L1/L1-L0/L0/concepts), reconciled the arnoldi plane-rotation end-bound (`incremental-least-squares-composition-lowering.md:112` `iterative.cpp:73-118 → :73-109`), DELETED the 3 slice files `cg`/`gmres`/`arnoldi_step`, removed the SUMMARY `# Phase 1 corpus` section (header + Index parent link + 3 slice children), removed the now-dangling `introduction.md:23` `[Specification → Slice Status]` nav bullet, and DELETED `book/src/spec/index.md` (corpus empty, producer option-(a) full removal). The per-report integrator confirmed BOTH inbound-link sweeps return ZERO on the REAL applied tree (`grep -rnE '\]\([^)]*slices/(cg|gmres|arnoldi_step)\.md'` → 0; `grep -rnoE '\]\([^)]*spec/index\.md\)'` → 0). **D3 (lifter):** verified clean no-op — the cycle-095 `bilinear-form`-firm-flip residue is confined to the krylov hub (`L2/index.md:89` re-confirmed NON-stale on disk); zero genuinely-stale instances outside the hub; bookkeeping-only. **THE BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook + linkcheck2) EXIT 0 — 33 modified + 4 deleted (3 slices + `spec/index.md`) all co-landed link-safe (finalize re-verified BOTH inbound-link sweeps → ZERO; `# Phase 1 corpus` cleanly gone from SUMMARY with no orphaned structure; the new `L4/krylov-step.md` Worked-example subsection builds; `spec/slices/` empty; `spec/` now holds only the empty `slices/` dir — harmless to git + mdBook); only the pre-existing 101 benign KaTeX `Potential incomplete link` WARNs in `design/l4_calculus.md`; NO finalize build-repair needed. **STEP-5b graded-stack linters on the LANDED tree** (`python3 tools/graded-stack-lint/graded_stack_lint.py --json`): **`rank_violations: 0`** (baseline fully discharged at c096 → ANY violation would be NEW and block — there are NONE; GATE PASSES) + **NO newly-orphaned node** (the 3 deleted slices were `reference`-class detritus leaves in the pre-P1 detritus mass, an INTENDED deletion not an orphaning — the other step-5b block condition also clears). **THE SLICE-NODE COUNT IN THE LINTER OUTPUT IS NOW ZERO** (the reachability-GC detritus from `spec/slices/*` has collapsed to zero). Totals: `files=350 (was 354 at c098 — the 3 deleted slices + `spec/index.md`), typed=208, untyped=142 (was 146 — the 3 deleted slice nodes left the untyped tail), roots=36, reachable=36, promotion_frontier=10, detritus=172, unresolved_depends_on_targets=35`. **rank_violations cycle-over-cycle trend: 22 (c094) → 1 (c095) → 0 (c096) → 0 (c097) → 0 (c098) → 0 (c099).** **CAMPAIGN STATE: P0 DONE; P3 DONE; P1 DONE (rank-violation criterion MET = 0); P2 slice-deletion DONE — the Phase-1 slice corpus is EMPTY (9 → 0). THE WHOLE GRADED-STACK CAMPAIGN (priorities item-0) IS COMPLETE.** **CAMPAIGN-COMPLETE TRIGGER for the batch-31 meta-phase (a CLAUDE.md + skills/ write-authority = meta-phase enactment, NOT this finalize's job — RECORDED for the meta-phase to ENACT):** the Phase-1 corpus is empty → **retire the `annotated-and-retained` carve-out** (CLAUDE.md §Methodology-invariants "Phase 1 corpus reduces") **+ the skill `phase-1-slice-reduction-audit`** (per METHODOLOGY-GRADED-STACK.md §6/§7). **COUNTS UNCHANGED** (the slice corpus is Phase-1 raw material, NOT layered-vocabulary counts; D1's absorb is a NEW firm subsection on an already-firm L4 chapter, no node status flip): L1 firm 32 main / 39 grand, L4 firm 20 main / 24 grand, L4>L3 10, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 33, methodology 4, feature spine 11 FIRM / 1 seed, L4 reduce-family 4 verbs ALL firm. **SLICE CORPUS: 3 → 0** (`spec/slices/` EMPTY; `book/src/spec/` holds only the empty `slices/` dir). **OQs (per-report intake, RECOMMENDED for the batch-31 meta unify to CLOSE — per-report integrators have no OQ-close authority; finalize RECORDS the recommendation, the meta-phase closes):** `cg-slice-27to141-fully-homed-clear-to-delete-and-evidence-pointer-residue-class-B` (D1), `krylov-trio-slice-corpus-3to0-campaign-complete-retire-carveout-and-skill` (D2 — the CAMPAIGN-COMPLETE trigger), `krylov-trio-class-B-plaintext-mention-residue-batch31-cleanup` (D2 — the residual ~50 plain-text Class-B slice-range provenance mentions, stale-but-harmless meta-reviews-convention kind), `bilinear-form-c095-residue-sweep-clean-noop-and-L2-index-89-confirmed-non-stale` (D3). **STILL-OPEN for the batch-31 meta-phase:** the `non-book-artifact-orphan-review` user directive (already in open-questions.md); the residual ~50 Class-B plain-text mentions (dedicated batch-32 cleanup vs accept-as-historical); + the OQs recommended-for-close across cycles 097/098/099 (the slice-deletion OQ closures, the arnoldi end-bound reconcile, the domain_energy_reduce residues, the orthog no-op-framing). **NEXT: the batch-31 meta-phase fires after this finalize (aggregating 097/098/099) — it should close the recommended-resolved OQs, enact the campaign-complete trigger (retire the carve-out + skill), and pick the batch-32 LEAD now that the graded-stack campaign is fully discharged.**

---

**GRADED-STACK P2 SLICE-DELETION CAMPAIGN — FIRST TRANCHE (4 of 9 slices deleted + unreachable; post-cycle-097, batch-31 position 1/3 — the FIRST primary cycle of meta-batch-31; cycles 097/098/099; the batch-31 meta-phase fires AFTER cycle-099, NOT this cycle — this finalize runs NO meta-phase housekeeping).** **HEADLINE — the deferred P2 slice-deletion tranche (the batch-31 LEAD) LAUNCHED: 4 of the 9 Phase-1 `book/src/spec/slices/*` slices DELETED + made fully unreachable from any GC root, their material absorbed into firm homes and ~18 inbound markdown links repointed. The slice corpus shrank 9 → 5.** This is ALSO the FIRST cycle after the batch-30 meta-phase session restart enacted finalize step-5b (the finalize-runs-linters build-gate companion to `cargo make book`); step-5b ran on the landed tree this cycle. 6 reports applied clean (6/6 staging rows == 6 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the SEVENTY-EIGHTH consecutive clean staging / NINETY-SECOND consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs. **THE 4 DELETIONS (D1–D4 same-layer-cross-cutter slice-deletes, each absorb-into-firm-home + repoint):** **D1:** `cg_preconditioning_framework` slice DELETED + 7 concept-page links repointed onto the firm `book/src/L4/preconditioning-framework.md` (the c096-authored firm chapter — the P2 vocabulary gap that unblocked this deletion). **D2:** `divfree` slice DELETED + 3 `book/src/L1/` re-anchors (ksp_solve :131 parenthetical + Evidence bullet → `divfree-projector.md`; the slice was a no-op absorb — the firm L1 homes already carried the positive L0 `divfree.cpp:175`). **D3:** `sparse_triangular_solve` slice DELETED + 3 absorbed L0 findings folded into `book/src/L1-L0/triangular-solve-obstruction.md` §(d) + 7 L0 anchors appended to the Verified-against list + 3 concept repoints + the firm-theme self-link collapsed + the `annotated-and-retained` framing retired (theme declared sole home). **D4:** `plane_rotation_stream` slice DELETED + the L3 replay-prefix worked example absorbed into `book/src/concepts/sequential-obstruction.md` §"Givens-stream replay-prefix (GMRES least-squares update)" re-anchored DIRECTLY to L0 `iterative.cpp:634-640` (+3 absorbed sub-analyses) + 5 concept repoints. **THE INDEX/SUMMARY/MERMAID CLEANUP (D5 layer-intro-author, the per-slice GC completion):** D5 removed the 4 deleted-slice rows from `book/src/SUMMARY.md` + `book/src/spec/index.md` and the snake_case `divfree`/`cg_preconditioning_framework`/`plane_rotation_stream` mermaid `-->` edges from `book/src/concepts/dependency-map.md` — making the 4 deleted slices FULLY unreachable from any root over any edge (the campaign's per-slice mark-sweep completion). The 5 SURVIVOR rows (`arnoldi_step`, `cg`, `gmres`, `orthog`, `polynomial_recurrence_step`) + their index/SUMMARY/mermaid references INTENTIONALLY left intact for c098/c099. **THE WITHIN-FILE LAND-CLEAN (D6 lifter):** `book/src/L4/domain_energy_reduce.md` 3 within-file matrix-weighted-norm maturity re-anchors (`:377`/`:268`/`:374` — `rough-in (test-coverage-bounded)` → `firm` under the firm-on-positive-structure escape, both folded primitives firm: participation_ratio c077 + matrix-weighted-norm c091) — discharges the c096-opened OQ `domain_energy_reduce-377-mwn-stale-rough-in-residue`. **THE BUILD + STEP-5b LANDED RESULT:** `cargo make book` (mdbook + linkcheck2) EXIT 0, ~92s — the 4 slice deletions + ~18 inbound repoints + the SUMMARY/index/mermaid removals all co-landed link-safe (no dangling `[..](..)` to a deleted slice; no SUMMARY entry points at a deleted file; the surviving 5 slices' mentions of the deleted 4 were all bare-backtick inline-code, build-safe — confirmed against the build); only pre-existing benign KaTeX `Potential incomplete link` WARNs in `design/l4_calculus.md`; NO finalize build-repair needed. **STEP-5b graded-stack linters on the LANDED tree** (`python3 tools/graded-stack-lint/graded_stack_lint.py --json`): **`rank_violations: 0`** (was 0 at c096; baseline-exceptions discharged at c096 → any violation would be NEW and block — there are NONE; GATE PASSES) + **NO newly-orphaned node** (the 4 deleted slices were `reference`-class detritus leaves in the pre-P1 detritus mass, an INTENDED deletion not an orphaning — the other step-5b block condition also clears). `rank_histogram: {firm: 192, rough-in: 7, partly-constructive: 3, obstruction: 2, partial-obstruction: 4}`; `files=356 (was 360 — the 4 deleted slices), typed=208, untyped=148 (was 152 — the 4 deleted slice nodes left the untyped tail), roots=36, promotion_frontier=10, detritus=172, unresolved_depends_on_targets=35`. The slice-node set in the linter output is now EXACTLY the 5 survivors (the 4 deleted are gone). The high untyped/detritus mass is P2 mid-campaign (5 slices still present) — informational, NOT a block. **CAMPAIGN STATE: P0 DONE; P3 DONE; P1 DONE (rank-violation criterion MET = 0); P2 slice-deletion FIRST TRANCHE landed (4 of 9 deleted + unreachable; slice corpus 9 → 5). REMAINING P2 (c098/c099): the krylov trio `cg`/`gmres`/`arnoldi_step` is the deeply-interwoven ~30-anchor sub-campaign; plus `orthog` (MPI-collective-shape absorb) + `polynomial_recurrence_step` (5-axis-table absorb, Cohort-A-adjacent). Completion criterion = the reachability GC shows `spec/slices/*` empty.** **COUNTS UNCHANGED from c096** (the slice corpus is Phase-1 raw material, NOT layered-vocabulary counts — no L1/L4/L2/L3/concept/feature count changed; D6 is a within-file maturity-NARRATION re-anchor with `rank: firm` already on disk, no node status flip): L1 firm 32 main / 39 grand, L4 firm 20 main / 24 grand, L4>L3 10, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 33, methodology 4, feature spine 11 FIRM / 1 seed, L4 reduce-family 4 verbs ALL firm. **SLICE CORPUS: 9 → 5** (`spec/slices/`: `arnoldi_step`, `cg`, `gmres`, `orthog`, `polynomial_recurrence_step` remain). **OQs (per-report intake, RECOMMENDED for the batch-31 meta unify to CLOSE — per-report integrators have no OQ-close authority; finalize RECORDS the recommendation, the meta-phase closes):** `l4-preconditioning-framework-promotion` (D1, resolved-by-c096), `plane-rotation-givens-l0-citation-range-reconcile` (D4, resolved-by-deletion), `domain_energy_reduce-377-mwn-stale-rough-in-residue` (D6, resolved-by-re-anchor), and the two retired sparse-trisolve slice OQs `sparse-trisolve-rename-to-sparse-direct-solver-wrapper` + `sparse-trisolve-mfem-superlu-factor-allgatherv-family` (D3, resolved-by-obstruction/out-of-scope). **NEW follow-ups (per-report intake):** `domain_energy_reduce-313-gram_reduce-bilinear-form-c095-stale-rough-in-narration` (D6, gram_reduce-cohort land-clean) + a still-open end-bound citation-divergence sub-note in `arnoldi_step`/`polynomial_recurrence_step`/`composition-lowering` (D4 critic Issue 3, for a downstream verify-citation-range pass — distinct from the closed reconcile OQ). **NEXT: cycle-098 (batch-31 position 2/3) continues P2 — the krylov-trio sub-campaign and/or `orthog`+`polynomial_recurrence_step`; the batch-31 meta-phase fires after cycle-099's finalize (aggregating 097/098/099) and should close the 5 recommended-resolved OQs above.**

---

**GRADED-STACK CAMPAIGN MECHANICAL COMPLETION (typed frontier) — rank_violations 0; P2 first tranche landed; the BATCH-CLOSING cycle (post-cycle-096, batch-30 position 3/3 — the LAST primary cycle of meta-batch-30; cycles 094/095/096; the batch-30 meta-phase fires AFTER this finalize).** **HEADLINE — the graded-stack rank-violation criterion is now MET for the WHOLE typed subset: `rank_violations` 22 (c094) → 1 (c095) → 0 (c096). The residual O1 was discharged two ways at once — D3 typed `solve-family-map-dissolution` `rank: firm` (clears the genuine edge by construction), and D4's `read_status_line` `tools/` parse-bug fix retired the prose-fallback FALSE-POSITIVE class so the histogram is now ACCURATE.** 5 reports applied clean (5/5 staging rows == 5 dispatched-ready; no repair phase — all 5 critics passed clean; the cycle-018 staging-completeness gap did NOT recur for the SEVENTY-SEVENTH consecutive clean staging / NINETY-FIRST consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs. **THE P2 FIRST TRANCHE (D1, the genuine-gap author):** **D1 (layer-intro-author):** the NEW firm L4 chapter `book/src/L4/preconditioning-framework.md` (the P2 campaign vocabulary gap — the capability-typed preconditioner-binding composition root; `rank: firm`, typed `edges: depends-on [L4/ksp_solve (caps-the-binding), reference/palace/.../ksp.cpp:276-293 (cites-evidence)]`, concept refs as `reference`) + repointed the two `capability-typing.md` slice refs (:26, :55) to the new chapter §"Capability typing" + alpha-positioned SUMMARY row (between `ksp_solve` and `solve_family`, Outer-driver kind-group) + L4/index dep-map row (alpha between `Outcome` and `restart_cycle`). **L4 firm +1 (19→20 main).** **O1 DISCHARGE + THE PARSE FIX (D3 + D4, the campaign mechanical-completion):** **D3 (lifter):** typed `book/src/L4-L3/solve-family-map-dissolution.md` (had NO frontmatter) with `rank: firm` + a typed `edges:` block (`depends-on: solve_family, ksp_solve, ksp-solve-driver-dissolution, L3/ksp_solve`; 6 `reference` edges) — pure edge-typing, no body/maturity change; clears O1 by construction (all 4 depends-on endpoints firm on disk → invariant holds). **D4 (layer-intro-author, `tools/`-only):** rewrote `read_status_line` in `graded_stack_lint.py` to the leading-inline-code-token rule (blob-scan removed; `_STATUS_TOKENS` ordered longest-/most-qualified-first) + a NEW regression fixture (`fixture/.../prose_firm_provenance.md` + `widget.L4.md` wiring) + README parse-rule paragraph — retires the `read_status_line` prose-fallback FALSE-POSITIVE class (the ~20 untyped-node maturity mis-reads that masked firm nodes as rough-in/partly-constructive/obstruction). **THE WITHIN-FILE STALE-RESIDUE LAND-CLEAN (D2 + D5):** **D2 (layer-intro-author):** the `methodology/resolution-ladder.md` worked-example repair — the c094 FALSIFIED forward-prediction (gram_reduce held-back-by-rough-in-bilinear-form) re-told as the COMPLETED c091/c095 two-wave cascade discharge (§heading "cycles 088–095 cascade, completed"; DAG surfaces gram_reduce's second off-diagonal `bilinear-form` leaf); resolves the resolution-ladder.md half of OQ `bilinear-form-firm-flip-stale-narration-in-meta-owned-methodology-pages` (the goal-flow.md half stays OPEN — meta-phase-owned, batch-30 refresh). **D5 (lifter):** `matrix-weighted-norm-mutation-rotation.md` within-theme stale-residue fix (2 sites: :5 opening prose + :317 §Speculative-L1 clause, both `rough-in` → `firm`/`firm, promoted c091`; theme's own `## Status` deliberately untouched). **THE LANDED LINTER RESULT (the campaign's mechanical-completion confirmation):** `python3 tools/graded-stack-lint/graded_stack_lint.py --json` on the LANDED state — **`rank_violations: 0`** (was 1 at c095, 22 at c094); **`rank_histogram: {firm: 192, rough-in: 7, partly-constructive: 3, obstruction: 2, partial-obstruction: 4}` (now ACCURATE — D4's fix retired the prose-fallback mis-reads)**; `files=360, typed=208, untyped=152, roots=36, promotion_frontier=10, detritus=172 (no_typed_edges_pre_p1=110 + with_typed_edges=62), unresolved_depends_on_targets=35` (the high untyped/detritus counts are P2 mid-campaign — the standing reachability-GC detritus sweep; informational, NOT a build-gate failure — there is NO new rank violation and NO newly-orphaned node). **CAMPAIGN STATE: P0 DONE; P3 DONE; P1 DONE (frontier-first typed; rank-violation criterion MET = 0 for the typed subset; the prose-fallback false-positive class RETIRED via the parse fix); P2 FIRST TRANCHE landed (the preconditioning-framework vocabulary gap authored firm). The DEFERRED P2 slice-deletion tranche is the batch-31 LEAD candidate — the 9 Phase-1 slices under `book/src/spec/slices/` + their `spec/index.md` rows + SUMMARY entries + the ~30 slice-line-anchor repoints; completion criterion = the reachability GC shows `spec/slices/*` unreachable (currently in the pre-P1 detritus mass).** **COUNTS: L4 firm 19→20 main (preconditioning-framework — NEW firm node).** All other layer-vocabulary counts UNCHANGED from c095 (L1 firm 32 main / 39 grand, L4>L3 10, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 33, methodology 4, feature spine 11 FIRM / 1 seed, L4 reduce-family 4 verbs ALL firm). Build clean (`cargo make book` + linkcheck2 EXIT 0, ~92s; the NEW L4 chapter `preconditioning-framework.html` built + all its internal links resolve under linkcheck2; NO build-repair needed; only pre-existing benign KaTeX `Potential incomplete link` WARNs in `design/l4_calculus.md`). **BASELINE-EXCEPTIONS LEDGER: TRACKED-OPEN-1 (O1) now DISCHARGED-by-c096-D3 → the burn-down moves 1 tracked → 0 tracked; the typed subset is CLEAN.** **OQs (per-report intake): CLOSED-by-this-cycle (to mark resolved at the batch-30 meta unify):** `l4-preconditioning-framework-promotion` (D1), `graded-stack-lint-read-status-line-token-priority-bug` (D4), `matrix-weighted-norm-mutation-rotation-within-theme-stale-rough-in-residue` (D5), and the resolution-ladder.md half of `bilinear-form-firm-flip-stale-narration-in-meta-owned-methodology-pages` (D2, PARTIALLY-RESOLVED — goal-flow.md half stays open). **NEW (batch-31 land-clean):** `domain_energy_reduce-377-mwn-stale-rough-in-residue` (D5's cross-file grep-guard surfaced one CLEAN cross-file stale operator-maturity assertion at `book/src/L4/domain_energy_reduce.md:377`, flagged-not-fixed, out of the one-theme scope); plus a benign 2nd-consumer watch `record-OpBinding-may-need-concept-page` (D1). **NEXT: the batch-30 meta-phase fires after THIS finalize (aggregating 094/095/096) — it should mark the closed OQs, decide the batch-31 LEAD (the P2 slice-deletion tranche), refresh the `goal-flow.md` stale worked-example half, and decide the carried P1 finalize-runs-linters build-gate wiring (`graded-stack-finalize-json-wiring-role-spec`) now that rank_violations=0 makes the gate cheap to enforce.**

**bilinear-form FIRM-FLIP CASCADE LANDED + P1 FRONTIER-FIRST EDGE-TYPING (post-cycle-095, batch-30 position 2/3 — the SECOND primary cycle of meta-batch-30; cycles 094/095/096; the batch-30 meta-phase fires AFTER cycle-096).** **HEADLINE — the `bilinear-form` firm-flip cascade (the batch-30 LEAD, priorities item-1) AND the graded-stack P1 frontier-first edge-typing (priorities item-0) composed in ONE 7-dispatch cycle; the rank linter got its FIRST live validation — rank violations dropped 22 → 1 on the landed state — and the convergent foundation-blocker tail (`matrix-weighted-norm` c091 → `bilinear-form` c095) is now worked off.** 7 reports applied clean (7/7 staging rows == 7 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the SEVENTY-SIXTH consecutive clean staging / NINETIETH consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs. **THE CASCADE (D1→D3→D4, serial dependency chain):** **D1 (harvester):** `bilinear-form` rough-in → **firm** (firm-on-positive-structure escape, the c092 DISCHARGE flipped) + the campaign's FIRST HARD-gate-new typed `edges:` block (`depends-on: dot, apply_linop, matrix-weighted-norm`, rank-gate PASS firm-over-firm) + `L1/index.md` count-owner (**L1 firm 31→32 main / 38→39 grand**; new BLAS-1 alpha-position bullet; empty test-coverage-bounded sub-list retired). **D2 (lifter):** the whole-book `bilinear-form` firm-flip cross-ref re-anchor (14 sites / 11 files; theme `## Status` VERDICTs untouched per HARD constraint). **D3 (lowering-verifier):** `gram_reduce` DISCHARGE → **firm** (both folded gates discharged — matrix-weighted-norm c091 + bilinear-form c095) + typed `edges:` [direct-dep set] + first `verified_against:` block; **L4 firm 18→19 main / 22→23 grand; L4 rough-in (test-coverage-bounded) cohort 1→0 (EMPTY)**. **D4 (layer-intro-author):** the 4 feature columns `capacitance`/`inductance`/`electrostatic`/`magnetostatic` × {L4,L1,L0} seed → **firm** under OWN-COMPOSITION (the planner's outcome-(a) forward-reference MATERIALIZED — gram_reduce firm on disk, no (b)-fallback revert) + `feature/index.md` SOLE-owner chapter-kind split rewritten to **11 firm / 1 seed** + 2 group-intros; **feature spine 7→11 FIRM / 5→1 seed** (the lone non-firm column is `boundary-mode`, now honestly typed `rank: rough-in`). **THE P1 FRONTIER-FIRST EDGE-TYPING (D5/D6, the graded-stack campaign):** **D5 (layer-intro-author):** the P1 feature-root closure typing — 25 files; the 7 NON-cascade feature columns + the spine-ROOT got the scheme §3 `feature_root: seed` + `rank:` SPLIT + typed `edges:` migration (`composes:`→`depends-on`, `l0_ground_truth:`→`cites-evidence`, sibling columns + `lifts_to:`→`reference`); the lifecycle ROOT is **firm** under OWN-COMPOSITION; **the reachability-GC root set is now FULLY TYPED**. **D6 (cross-layer-cross-cutter):** the P1 high-fan-out vocabulary-frontier edge-typing — 17 files; ~15 high-fan-out vocab ops got NEW `rank: firm` + typed `edges:` (ad-hoc lowers_to/lifts_from/consumes blocks replaced; cross-layer identity-view edges classified `reference` to avoid importing partial-obstruction ranks) + `L4/index.md` (count 18→19 + cell flips) + `L4/solve_family.md:154` re-anchor (3 stale c080-NO-GO-HELD claims overturned — DISCHARGES the D3-promoted OQ `solve-family-154-stale-column-gate-narrative-post-c091-c095` in-artifact). **D7 (same-layer-cross-cutter, scaffolding-only):** authored `scaffolding/graded-stack-baseline-exceptions.md` (the tracked exception ledger; the predicted 22→21→1 end-state REALIZED on disk, residual == O1). **THE 22 → 1 RANK-VIOLATION DROP (the rank linter's first live validation; BASELINE RECORD, not gated — the §8 finalize-runs-linters wiring is still OQ `graded-stack-finalize-json-wiring-role-spec`):** the cascade (D1/D3/D4) cleared ~10 GENUINE firm-rests-on-rough-in violations; the P1 typed-frontmatter retyping (D6/D5/D4) cleared ~11 `read_status_line`-parse-bug FALSE POSITIVES; leaving exactly 1 genuine residual **O1 = `L4/solve_family → L4-L3/solve-family-map-dissolution (rough-in (test-coverage-bounded))`** — a mechanical c096 lazy-tail typing item (type `solve-family-map-dissolution` `rank: firm`, both endpoints firm on disk → invariant holds immediately). **This is the campaign's mechanical completion criterion MET for the typed subset.** LANDED linter totals: `files=359, typed=207, untyped=152, roots=36, rank_violations=1 (was 22), unresolved_depends_on_targets=34, promotion_frontier=22, detritus=171` (the high untyped/detritus counts are P2 mid-campaign — informational, not gating; `python3 tools/graded-stack-lint/graded_stack_lint.py --json`). **CAMPAIGN STATE: P0 DONE; P3 DONE; P1 LAUNCHED (frontier-first — the GC root set + the high-fan-out vocabulary frontier are TYPED; rank-violation criterion MET for the typed subset); P2 (Phase-1 slice corpus finalize/migrate-to-`roadmap_goal`) is the dominant remaining phase, QUEUED for c096+.** **COUNTS: L1 firm 31→32 main / 38→39 grand (bilinear-form); L4 firm 18→19 main / 22→23 grand (gram_reduce) + L4 rough-in (tcb) 1→0 EMPTY; feature spine 7→11 FIRM / 5→1 seed.** All other layer-vocabulary counts UNCHANGED from c094 (L4>L3 10, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 33, methodology 4, L4 reduce-family 4 verbs ALL firm now). **The P1 typing pass added `rank:`/`edges:` frontmatter to ~40 files WITHOUT changing underlying on-disk maturity** (the `rank:` token RECORDS firmness; only bilinear-form/gram_reduce/the-4-columns are genuine promotions). Build clean (`cargo make book` + linkcheck2 EXIT 0, ~92s; mdbook tolerated the new `edges:`/`rank:`/`feature_root:` frontmatter keys cleanly — ignores unknown keys; linkcheck2 zero dead links across the ~40 status-label changes; NO build-repair needed; only pre-existing benign KaTeX/bracket "Potential incomplete link" WARNs). **OQs (9 NEW by per-report intake, 0 closed/opened by finalize):** **(LOAD-BEARING for the batch-30 meta-phase)** `bilinear-form-firm-flip-stale-narration-in-meta-owned-methodology-pages` (D2 — the c094 `resolution-ladder.md` worked example + `goal-flow.md:263` are now FALSIFIED by this cascade; a reader-facing meta-OWNED page has a stale worked example; CONTENT-repair not build-repair so NOT fixed here — couples with `goal-flow-refresh-two-health-invariants-and-typing-audit-campaign`); `graded-stack-lint-read-status-line-token-priority-bug` (D6, corroborated D7 — the `## Status` blob-scan reads rough-in/stub before firm, 12 instances; the typed `rank:` token retires the prose fallback; a `tools/` fix BEFORE wiring the finalize build-gate); `cites-evidence-l0-edge-linter-slug-resolution-exemption` (D4/D5/D7 shared — the `kind: cites-evidence` edges target Palace ranges not book slugs, linters must exempt); `matrix-weighted-norm-mutation-rotation-within-theme-stale-rough-in-residue` (D2 — a c096 mwn land-clean follow-up); plus the cross-report dedup'd shared ones. **NEXT: cycle-096 (batch-30 position 3/3, the LAST primary cycle before the batch-30 meta-phase) should close O1 (mechanical) + advance P2 (Phase-1 slice migration); the batch-30 meta-phase fires after cycle-096's finalize (aggregating 094/095/096) and should address the stale meta-owned worked example, the `read_status_line` `tools/` fix, the cites-evidence linter-exemption convention, AND the carried c094 P1 edge-home fork + finalize-runs-linters wiring.**

**Open-all-feature-fronts WIDE WAVE (post-cycle-117, batch-37 position 3/3 — the BATCH-CLOSING cycle; cycles 115/116/117; the batch-37 meta-phase fires AFTER this c117 finalize, aggregating all three).** **HEADLINE — the user directive B `open-all-feature-fronts-simultaneously` (2026-06-06; memory `project_open_all_feature_fronts_simultaneously`) LANDED its wide all-fronts fan-out: 5 dispatches, ALL applied clean, opening the previously demand-gated feature/mesh fronts together so their shared mesh→fe_space substrate is lifted once.** **D1 (layer-intro-author): the `waveguide-mode` 6th OUTPUT-PRODUCT feature column LANDED** (`book/src/feature/waveguide-mode.{L4,L1,L0}.md`, status `seed`/rank rough-in — the boundary-mode readout's reduction product; output-product cohort 5→6) + sole-owned `feature/index.md` + `output-product.md` + `SUMMARY.md`. **D2 (layer-intro-author): the `boundary-mode` driver-leaf column PROMOTED rank rough-in→FIRM** (×3 chapter bodies, `feature_root: seed` KEPT as the permanent GC-root marker; live waveguide-mode sibling cross-links) — its own-readout gate cleared by the waveguide-mode landing; both-land coupling with D1 RESOLVED. **D3 (harvester): new firm L1 op `build_mesh :: Config → Mesh`** (`book/src/L1/build_mesh.md`, firm-on-positive-structure; the single-machine geometric substrate under all 5 solver pipelines; in-chapter `Mesh` record definition) + a NEW `Mesh & FE-space construction` kind-grouping. **D4 (layer-intro-author): new firm L1 op `fe_space_hierarchy`** (the `AddLevel`-fold p-multigrid combinator composing firm `fe_space` + `fe_collection`; 3 `fe_space.md` re-anchors; FE-space sub-spine 4→5; OWNS the consolidated **L1 firm count 40→43 main**). **D5 (harvester): new firm L1 op `interpolator`** (the de-Rham discrete grid-transfer operator, firm on the discrete piece + a GSLIB `obstruction (opaque-library-ownership)` sibling note; carries NO dangling `depends-on` edge — the load-bearing reference-demotion for the forthcoming L1>L0 theme). **The mesh→fe_space substrate is now homed** (mesh-wrapper front opened, FE-space deferred siblings landed, boundary-mode/waveguide-mode boundary-mode front opened). **MEASURABLE DELTAS: L1 firm 40→43 main (+`build_mesh` +`fe_space_hierarchy` +`interpolator`); FE-space sub-spine 4→5; NEW `Mesh & FE-space construction` kind-grouping (1 member); feature spine output-product cohort 5→6 (+`waveguide-mode` seed); `boundary-mode` driver-leaf rank rough-in→FIRM.** **GRADED-STACK (landed-tree linter): `rank_violations` HELD 0, `unresolved_depends_on_targets` HELD 0 (D5's reference-demotion VERIFIED); `files` 356→363 (+7: 6 new chapters + the `mesh-construction-intro.md` build-repair group-intro stub), `roots` 36→39 (+3 waveguide-mode feature chapters — first root-set movement since the spine settled), `reachable` 133→136 (+3 waveguide-mode reachable from roots), `detritus` 126→129 (+3: the 3 NEW firm L1 ops await an inbound feature/higher `depends-on` consumer — the GC-ground-don't-remove case, NEW nodes NOT newly-orphaned, NOT a block), STRONGER 23→24 (+`fe_space_hierarchy`), `untyped` HELD 61.** Build clean after ONE finalize build-repair (the new kind-grouping linked the `./L1/index.md` placeholder → duplicate-file SUMMARY error; repaired by creating the `mesh-construction-intro.md` group-intro stub + repointing — the preferred stub-creation repair per the integrator-finalize role-spec step-5, partially resolving D4's deferred `build-mesh-fe-space-kind-grouping-fold-residual-c117`). **OQs:** 14 promoted across the 5 reports (3 record-definition homes — `Mesh`/`FiniteElementSpaceHierarchy`/`WaveguideModeTable`; 3 forthcoming L1>L0 construction-rotation themes — `build-mesh`/`fe-space-hierarchy`/`interpolator`; the `waveguide-mode-reduce-needs-l4-verb-home` seed→firm gate; etc.). RESOLVED-BY-FINALIZE `fe-space-front-l1-count-owner-reconciliation-c117` (D5 landed `interpolator` firm at the FE-space sub-spine → grand total stays 43). **CARRY-FORWARDS for the batch-37 meta-phase:** (i) **NO RE1-RE8 reachability baseline-exception auto-discharged** — the new firm L1 ops are new substrate awaiting consumers, NOT a feature column consuming a previously-stranded L3 iteration-view; the RE set is UNCHANGED; (ii) **ground the 3 new detritus firm L1 ops** (`build_mesh`/`fe_space_hierarchy`/`interpolator`) once a feature column `depends-on` them (GC-ground-don't-remove); (iii) close `named-shape-groups-general-rule-restatement-cohort-extent` (fully swept Tier A+B+C, c116); (iv) the `waveguide-mode` rough-in→firm gate (`waveguide-mode-reduce-needs-l4-verb-home`); (v) the kind-grouping fold residual (the group-intro now EXISTS; the fold-into-FE-space-sub-spine question is open).

**Graded-stack campaign LAUNCH (post-cycle-094, batch-30 position 1/3 — the FIRST primary cycle of meta-batch-30; cycles 094/095/096; the batch-30 meta-phase fires AFTER cycle-096).** **HEADLINE — the graded-stack two-axis artifact-health infrastructure (priorities item-0, the 2026-06-04 GRADED RESOLUTION LADDER + FEATURE-ROOT REACHABILITY directive; full spec `METHODOLOGY-GRADED-STACK.md`) LANDED at its P0 + P3 milestones: the canonical machine-readable scheme page, the two `tools/` linters, and the reader-facing two-axis ladder page all shipped this cycle; the artifact-wide edge-typing audit (P1) and slice migration (P2) remain queued for c095+.** 3 reports applied clean (3/3 staging rows == 3 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the SEVENTY-FIFTH consecutive clean staging / EIGHTY-NINTH consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs. **D1 (layer-intro-author, build-relevant): `book/src/methodology/graded-stack-scheme.md` LANDED** — the canonical machine-readable convention page (the node-status `rank:` token + maturity→ladder mapping table `roadmap_goal=0 < stub=1 < rough-in=2 < firm=3` with sub-ranks; the typed `edges:` block grammar `depends-on` vs `reference`; the `feature_root: seed` root marker; the migration mapping + un-fronted-file node-status + authoring checklist) + `SUMMARY.md` `# Methodology` registration (anchored after `Goal & Flow`). The page is itself OUTSIDE the subject DAG (§2d — carries no `rank:`/`edges:` frontmatter, makes no operator/concept/rotation claims). **D3 (layer-intro-author, build-relevant): `book/src/methodology/resolution-ladder.md` LANDED** — the reader-facing NON-AUTHORITATIVE two-axis mirror (Axis 1 resolution + well-foundedness: the rank-0..3 ladder + the `rank(u) ≤ min over depends-on deps of rank(v)` invariant + the cycles-088–091 matrix-weighted-norm upward-rank-propagation worked example + the `roadmap_goal` rank-0 chapter + `stub` vs `roadmap_goal`; Axis 2 reachability/liveness: feature-surfaces-as-GC-roots + mark-sweep detritus/orphaned-intent unification + OWN-COMPOSITION-from-root-marker; the shared typed-edge substrate) + `SUMMARY.md` registration inserted BETWEEN `Goal & Flow` and `Graded-stack scheme` (named final order: Overview → Goal & Flow → Resolution ladder & reachability → Graded-stack scheme). The forward-link `./graded-stack-scheme.md` resolves under linkcheck2. **D2 (layer-intro-author, NOT build-relevant — `tools/` deliverable): the two graded-stack linters LANDED under `tools/graded-stack-lint/`** — `graded_stack_lint.py` (rank check + reachability GC) + `README.md` + `requirements.txt` + `fixture/`; written directly to `tools/` write-authority territory (NOT through proposed-changes), no `book/` edits. **VALIDATION — the rank linter empirically REDISCOVERED the project's hand-tracked firm-rests-on-rough-in cascade: the 22 rank violations it reports ARE the `L2/normalize→L1/normalize`, `L3/dot→L2/inner_product`, `L4/gram_reduce(2.5)→L1/bilinear-form`, `feature/energy-fields.L4→L1/matrix-weighted-norm(2.5)` cascade that priorities item-1 (`bilinear-form-firm-flip-and-cascade-wave`, the batch-30 LEAD) is queued to discharge** — strong independent evidence the linter is correct. The 22 are NOT acted on this cycle (that is P1/item-1 by scope). **BASELINE linter totals (recorded, not gated — the linters JUST landed this cycle):** `files=359, typed=207, untyped=152, roots=36, rank_violations=22, unresolved_depends_on_targets=11, promotion_frontier=30, reachable=77, detritus=136 (no_typed_edges=102 + with_typed_edges=34), expected_unreachable_outside_dag=21` (`python3 tools/graded-stack-lint/graded_stack_lint.py --json`, exit=1 reflecting the 22 known-cascade violations). **CAMPAIGN STATE: P0 (scheme page + both linters) DONE; P3 (reader-facing ladder page) DONE; P1 (artifact-wide edge-typing audit — gated on the `graded-stack-edge-home-fork-p1-cost` decision) + P2 (Phase-1 slice corpus finalize/migrate-to-`roadmap_goal`) QUEUED for c095+.** **COUNTS: methodology chapters 2→4** (added `graded-stack-scheme` + `resolution-ladder`); all layer-vocabulary counts UNCHANGED from c093 (L1 firm 31 main / 38 grand, L4 firm 18 main / 22 grand, L4 rough-in 0 plain / 1 test-coverage-bounded `gram_reduce`, feature spine 7 FIRM / 5 seed, L4>L3 10, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 33). Build clean (`cargo make book` + linkcheck2 EXIT 0, ~93s; both new methodology pages render + their forward/back cross-links resolve; only pre-existing benign KaTeX `[unit]` `Potential incomplete link` WARNs in `design/l4_calculus.md` + concept pages; NO build-repair needed). **OQs PROMOTED (10 across the 3 reports):** `graded-stack-edge-home-fork-p1-cost` (the DECISION-POINT setting the entire P1 audit cost — per-chapter `edges:` frontmatter vs index-table-parse vs hybrid; routed to batch-30 meta / human), `graded-stack-finalize-json-wiring-role-spec` (the §8 finalize-runs-linters `--json` wiring — meta-phase write-authority), `graded-stack-feature-root-frontmatter-split`, `graded-stack-linter-categorical-root-rule-p1-sync`, `graded-stack-obstruction-resolution-encoding-parser-coordination`, `graded-stack-index-and-concept-node-status`, `graded-stack-unresolved-target-prose-as-slug-p1-reclassify`, `goal-flow-refresh-two-health-invariants-and-typing-audit-campaign`, `roadmap-goal-unbuilt-frontier-SUMMARY-grouping-deferred`, `graded-stack-finalize-json-wiring-role-spec`. **NEXT: cycle-095 (batch-30 position 2/3) continues item-1 (`bilinear-form-firm-flip-and-cascade-wave`, the LEAD — would discharge ~half the 22 rank violations) and/or P1 (pending the edge-home-fork decision); the batch-30 meta-phase fires after cycle-096's finalize (aggregating 094/095/096) and should decide the P1 edge-home fork + the finalize-runs-linters wiring.**

**Spine surface-firming state-advance (post-cycle-092, batch-29 position 2/3 — the SECOND primary cycle of meta-batch-29; cycles 091/092/093; the batch-29 meta-phase fires AFTER cycle-093).** **NO MEASURABLE COUNT CHANGE this cycle — a law-confidence STATE ADVANCE, not a count flip.** The cycle-092 LEAD was a SCOPED `lowering-verifier` dischargeability probe on `bilinear-form` (the c088/c089 `matrix-weighted-norm` cheap-probe pattern), verdict **DISCHARGE**: the firm-on-positive-structure escape (CLAUDE.md §Methodology invariants, the `rough-in (test-coverage-bounded)` bullet) APPLIES — laws 1-6 (`bilinear-form.md:182-201`) are syntactic read-offs over firm `dot` + firm `apply_linop` (NO inner-product-norm theorem content — materially cleaner than `matrix-weighted-norm`, which needed two probes c088+c089 for its norm-axiom theorems); laws 7-8 (`:205-220`) are M-symmetry-CONDITIONAL with both witnesses on-disk (`boundarymodeoperator.cpp:85` Hermitian `Bttr` / `:90` non-Hermitian `Atn`), law 8's SPD positivity inherited from the firm `matrix-weighted-norm` sibling (c091); the narrow-variant-axis-coverage gate is REDUNDANT (the one shape it names — real-`M`-real-`y` `xᵀ M y` — is not surfaced by Palace at all). **The verb STAYS `rough-in` by design** (frontmatter `firmness: rough-in` UNFLIPPED, §Status retains the `rough-in (lower-layer-shared-vocabulary, cycle-010-wave-1)` token) — the probe is the gate-TEST; the firm flip is a SEPARATE gated wave. **§Status narrowed to record the discharge + a 9-entry `verified_against:` block appended** (8 supports + 1 partially-supports — 4 L0 anchors + 3 firm-constituent chapters + 2 law-group self-citations). **CONSEQUENCE — the bilinear-form law-confidence gate is now DISCHARGED; the firm flip + the high-fan-out cascade (`gram_reduce` firm re-judgment → 4 columns `capacitance`/`inductance`/`electrostatic`/`magnetostatic` seed→firm unblock + ~30-file re-anchor) is QUEUED as the c093/batch-30 `bilinear-form-firm-flip-and-cascade-wave` candidate** — this sets up a potential SECOND consecutive cascade wave (the foundation-blocker tail is being worked off systematically; the same cheap-probe→firm-wave arc that discharged `matrix-weighted-norm` across batches 25-29). **COUNTS UNCHANGED from c091: L1 firm 31 main / 38 grand, L4 firm 18 main / 22 grand, L4 rough-in 0 plain / 1 test-coverage-bounded (`gram_reduce`), feature spine 7 FIRM / 5 seed, L4>L3 10, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 33, methodology 2.** Build clean (`cargo make book` EXIT 0; one file changed — `book/src/L1/bilinear-form.md` §Status + `verified_against:`; zero dead links; only pre-existing benign KaTeX `[unit]` WARNs). Repair FIRED pre-integration (one prose-only OOB citation misattribution fixed — `matrix-weighted-norm.md:251-257` → `bilinear-form.md:251-257`; the proposed-changes block was clean; the per-report integrator also corrected one in-edit L0 anchor `:386-394` → `:385-394` to match the report's own verified line-range). **OQs:** PROMOTED `bilinear-form-firm-flip-and-cascade-wave` (the c093/batch-30 cascade-wave candidate, stale-consumer cluster enumerated for the lifter).

**Spine surface-firming tally (post-cycle-091, batch-29 position 1/3 — the FIRST primary cycle of meta-batch-29; cycles 091/092/093; the batch-29 meta-phase fires AFTER cycle-093).** **HEADLINE — the batch-29 LEAD LANDED CLEAN: the `matrix-weighted-norm` firm-flip-and-cascade wave, the long-held √-foundation-blocker finally DISCHARGED.** After the cheap-probe→GO arc across batches 25-28 (NO-GO-HELD for the heavy whole-cascade wave through batch-27; both norm-axiom law-sides discharged structure-side c088 + FP-side c089; the batch-28 meta-phase judged gate-(a) the missing 4-arg `Norml2(comm,x,B,Bx)` sqrt-entry-point test REDUNDANT under the firm-on-positive-structure escape and GO'd the firm flip as the batch-29 LEAD), a 4-dispatch structural wave (D1→D2→D3→D4) flipped `matrix-weighted-norm` to firm and propagated the cascade across ~30 files. **D1 (harvester):** `matrix-weighted-norm` `rough-in (test-coverage-bounded)` → **firm** (firm-on-positive-structure escape, both math-sides discharged, gate-(a) redundant). **L1 firm 30→31 main / 37→38 grand**; the c080 count-reconciliation note DISCHARGED. **D2 (lifter):** 12-file / 21-block consumer re-anchor of stale `matrix-weighted-norm` VERB labels →firm across the vocabulary-spine/L0/L1-L0 themes; `bilinear-form` PRESERVED at rough-in everywhere. **D3 (lowering-verifier):** `domain_energy_reduce` `rough-in` → **firm** (the per-DOMAIN realization of the per-MODE `eigenfreq_qfactor_reduce` c082 disposition — both folded primitives now firm L1: matrix-weighted-norm c091 + participation_ratio c077; the firm-on-positive-structure escape); **L4 firm 17→18 main / 21→22 grand; L4 rough-in 1→0 (cohort EMPTY)**. `gram_reduce` STAYS `rough-in (test-coverage-bounded)` — its diagonal matrix-weighted-norm primitive firmed, but its off-diagonal `bilinear-form` primitive is the **sole residual gate**. **D4 (layer-intro-author):** the `energy-fields` output-product column `seed` → **firm** (×3 levels; own composition all-firm — domain_energy_reduce c091 + participation_ratio c077). **Feature spine columns 6→7 FIRM / 6→5 seed.** The 4 columns `capacitance`/`inductance`/`electrostatic`/`magnetostatic` STAY seed on the `gram_reduce`→`bilinear-form` residual gate; `boundary-mode` stays seed on its unhomed waveguide readout. **THE NEXT FOUNDATION GATE for the driver/output-product unblock is `bilinear-form` firming** — when a future cycle firms `bilinear-form`, `gram_reduce` clears and electrostatic/magnetostatic/capacitance/inductance all become unblockable (a batch-29 forward-frontier candidate). **MEASURABLE DELTAS: L1 firm 30→31 main / 37→38 grand; L4 firm 17→18 main / 21→22 grand, L4 rough-in 1→0; feature spine columns 6→7 FIRM / 6→5 seed; L4 reduce-family 4 verbs — `eigenfreq_qfactor_reduce` + `sparameter_reduce` + `domain_energy_reduce` FIRM, `gram_reduce` test-coverage-bounded (residual `bilinear-form` gate).** All other layer-vocabulary counts UNCHANGED from c090 (L4>L3 10, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 33, methodology 2). Build clean (`cargo make book` EXIT 0; zero dead links; one finalize build-repair — a symmetric stale-label cleanup the D4 report omitted: `magnetostatic.L4.md:41` stage-3 paragraph still called matrix-weighted-norm a "rough-in L1 building block" and attributed gram_reduce's rough-in to BOTH constituents, contradicting the SAME file's firm-c091 narration at lines 56/65/69 + its electrostatic.L4 twin which D4 DID fix; finalize applied the symmetric fix mirroring electrostatic verbatim-in-pattern — a friction signal that D4's discretionary line-56 fix had a line-41 twin gap). **The codified whole-book-grep discipline HELD** (the per-report integrators caught/fixed cascade residue — e.g. D4's discretionary magnetostatic.L4 line-56 symmetry fix; finalize caught only the one residual line-41 symmetric twin). **OQs:** PROMOTED `goal-flow-mwn-firm-flip-cascade-refresh-stale-rough-in-refs` (D2 intake — `methodology/goal-flow.md` is meta-phase-owned, stale mwn rough-in refs flagged for the batch-29 meta-phase goal-flow refresh).

**Spine surface-firming tally (post-cycle-086, batch-27 position 2/3 — the SECOND primary cycle after the batch-26 meta-phase; the batch-27 meta-phase fires AFTER cycle-087).** **HEADLINE — a THIRD firm-on-positive-structure FIRM PROMOTION this batch arc: the L4 solve-family combinator `solve_family` PROMOTED `rough-in (test-coverage-bounded)` → `firm` via the SAME firm-on-positive-structure / syntactic-identity escape that promoted `eigenfreq_qfactor_reduce` (c082) + `sparameter_reduce` (c083) — the in-scope law-confidence escape route now EXTENDS BEYOND reduce verbs to the solve-family combinator.** The element-independence law (no cross-element state across the solve-family corners) is read off the const `BaseKspSolver::Mult` body (`palace/linalg/ksp.cpp:297-310`), so the laws are syntactic identities over positive source — no axiom smuggled. **L4 firm 16→17 main / 20→21 grand; L4 rough-in (test-coverage-bounded) 1→0** (`solve_family` was the sole test-coverage-bounded L4 entry; `domain_energy_reduce` plain-rough-in remains the only L4 rough-in). **D1** (lowering-verifier, COUNT OWNER): the verb promotion (`solve_family.md` frontmatter `rough-in (test-coverage-bounded)`→`firm` + `## Status` firm re-narration + a fresh `verified_against:` block of 8 `supports` entries) + `L4/index.md` count-owner refresh (per-operator maturity cell `*(rough-in (test-coverage-bounded); cycle-055 D1)*`→`*(firm; cycle-086 D1)*` + §Vocabulary-cohort firm tally `(16 + 4 outer-driver)`→`(17 + 4 outer-driver)`). **D2** (lifter, consumer re-anchor; ZERO column flip): propagated the `solve_family` maturity WORD `rough-in (test-coverage-bounded)`→`firm` to the 4 consumer files (`gram_reduce.md` consumes-row + dep-map row, `electrostatic.L4.md` + `magnetostatic.L4.md` composes-row/prose/dep-map) + NARROWED the electrostatic/magnetostatic §Status gate from TWO own-constituent rough-in gates to ONE (`gram_reduce` only) + cleaned the `solve_family.md` §Evidence/§Provenance stale-after-promotion lines (closes OQ `solve-family-md-stale-evidence-provenance-lines-after-firm-promotion`). **CRITICALLY: NO feature column flipped — `electrostatic` + `magnetostatic` STAY `seed`.** Firming `solve_family` discharges 1 of their 2 own-constituent gates; the SECOND (`gram_reduce`, `rough-in (test-coverage-bounded)`, folding plain-rough-in `matrix-weighted-norm` + `bilinear-form`) STILL gates them — convergently blocked on the `matrix-weighted-norm` √-cascade NO-GO-HELD. **MEASURABLE DELTAS: L4 firm 16→17 main / 20→21 grand (+`solve_family`); L4 rough-in (test-coverage-bounded) 1→0; feature spine columns UNCHANGED at 6 FIRM / 6 seed (NO column flip this cycle).** All other layer-vocabulary counts UNCHANGED from c085 (L1 firm 30 main / 37 grand, L4>L3 10, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 33 + `record` Kind, methodology 2; L4 reduce-family 4 verbs — `eigenfreq_qfactor_reduce` + `sparameter_reduce` FIRM, `gram_reduce` test-coverage-bounded, `domain_energy_reduce` rough-in). Build clean (`cargo make book` EXIT 0; no SUMMARY/dead-link change; only pre-existing benign KaTeX WARNs). **OQs:** PROMOTED `solve-family-firmed-discharges-one-of-two-electrostatic-magnetostatic-column-gates` (D1; the 1-of-2-gates finding, for the batch-27 meta-phase); CLOSED `solve-family-md-stale-evidence-provenance-lines-after-firm-promotion` (D2 resolved). **CARRY-FORWARDS for the cycle-087 planner + batch-27 meta-phase:** (i) **the 1-of-2-gates finding** — electrostatic/magnetostatic now gate ONLY on `gram_reduce` (the gate narrowed); the convergent blocker is the `matrix-weighted-norm` √-cascade (NO-GO-HELD) which now blocks `gram_reduce` → electrostatic/magnetostatic/capacitance/inductance columns AND `domain_energy_reduce`/energy-fields — accumulating downstream demand the meta-phase should re-weigh; (ii) the **firm-token-choice question** (carried from c085, still open — `firm` vs a feature-specific composition-root token); (iii) **c087 is the land-clean cycle before the batch-27 meta-phase.**

**Spine surface-firming tally (post-cycle-085, batch-27 position 1/3 — the FIRST primary cycle after the batch-26 meta-phase; the batch-27 meta-phase fires AFTER cycle-087).** **HEADLINE — the batch-27 LEAD: the all-12-column FEATURE-SURFACE SPINE re-evaluation under the new OWN-COMPOSITION column-promotion rule (USER DIRECTIVE 2026-06-03 `feature-column-promotion-break-the-seed-deadlock`, enacted by the batch-26 meta-phase; CLAUDE.md §Extraction-goal; memory `project_feature_column_promotion_rule`) LANDED — 6 feature columns PROMOTED `seed` → `firm`, the FIRST feature columns ever off the terminal `seed` state.** The `eigenmode`↔`eigenfrequency-qfactor` mutual-blocking deadlock is BROKEN (each named the other as its non-firm blocker; under OWN-COMPOSITION a column promotes on its own composition + directly-owned constituents, cross-linked sibling columns are references NOT blockers). **PROMOTED seed→firm (6 columns × 3 levels = 18 frontmatter flips):** `eigenmode` + `driven` + `transient` (driver-leaf; own solve/assemble combinators firm) + `eigenfrequency-qfactor` + `sparameters` (output-product; own reduce verbs `eigenfreq_qfactor_reduce` c082 + `sparameter_reduce` c083 firm) + `lifecycle` (spine-ROOT; own driver-agnostic composition mesh-build + firm `fold_solve` adaptive fold; the per-driver dispatch is over sibling columns, references not blockers). **STAY seed (6 columns, on genuine OWN-constituent gates):** `electrostatic` + `magnetostatic` (own `solve_family` rough-in + `gram_reduce` reduce-verb rough-in) / `capacitance` + `inductance` (own `gram_reduce` reduce-verb rough-in) / `energy-fields` (own `domain_energy_reduce` rough-in + folded `matrix-weighted-norm` rough-in) / `boundary-mode` (own waveguide-mode readout unhomed — no output-product column yet). The 5-driver→L4 picture was AFFIRMED-COMPLETE (the batch-26 cross-layer survey); this validates the spine-promotion MECHANISM (a composition-root promotes when its own composition is firm). **MEASURABLE DELTAS: feature spine columns 0→6 FIRM / 6 seed (was 12 columns all `seed`).** ALL layer-vocabulary counts UNCHANGED from c084 (this is a status-token-flip + prose-re-authoring cycle, NO new files, NO SUMMARY.md change: L1 firm 30 main / 37 grand, L4 firm 16 main / 20 grand, L4 rough-in 1 + 1 test-coverage-bounded, L4>L3 10, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 33 + `record` Kind, methodology 2; L4 reduce-family 4 verbs — `eigenfreq_qfactor_reduce` + `sparameter_reduce` FIRM, `gram_reduce` test-coverage-bounded, `domain_energy_reduce` rough-in). **D1** (layer-intro-author, driver-leaf + SOLE-owner `feature/index.md`): 6 driver-leaf columns re-evaluated — eigenmode/driven/transient FLIP (9 frontmatter), electrostatic/magnetostatic/boundary-mode STAY seed (own-constituent gate prose); `feature/index.md` §Chapter-kind narrative re-narrated to the 6-firm/6-seed cohort. **D2** (layer-intro-author, output-product): eigenfrequency-qfactor + sparameters FLIP (6 frontmatter + sibling-relabel dep-map rows + deadlock-clause retirement), capacitance/inductance/energy-fields STAY seed (own reduce-verb gate prose). **D3** (layer-intro-author, spine-ROOT): lifecycle FLIP (3 frontmatter + §Status OWN-COMPOSITION re-authoring). Build clean (`cargo make book` EXIT 0; the index 6-firm/6-seed enumeration is internally consistent with on-disk column tokens; no SUMMARY/dead-link change). **OQs:** PROMOTED `feature-column-firm-token-choice-batch-27-meta-phase` (D1; whether `firm` is the right promoted token for a composition-root or a feature-specific token — flagged by ALL 3 dispatches) + `waveguide-mode-output-product-column-would-promote-boundary-mode` + `electrostatic-magnetostatic-stay-seed-overrides-priorities-1-expectation` + `output-product-stay-seed-columns-gated-on-reduce-verb-firming` (D2; gram_reduce firming jointly unblocks capacitance+inductance, domain_energy_reduce+matrix-weighted-norm unblock energy-fields). **CARRY-FORWARDS for the cycle-086 planner + batch-27 meta-phase:** (i) **the firm-token-choice question** (`firm` vs a feature-specific promoted token for composition-roots) — flagged by all 3 dispatches, a batch-27 meta-phase decision; (ii) the **solve-family-list-homomorphism-law-confidence-pass** fresh candidate (would unblock electrostatic+magnetostatic by firming `solve_family` — the planner appended it to priorities.md); (iii) the **waveguide-mode demand-gated boundary-mode promotion** candidate (an output-product column for the waveguide-mode readout would promote boundary-mode); (iv) the **convergent stay-seed gates**: `solve_family` rough-in blocks electrostatic/magnetostatic; `gram_reduce` rough-in blocks capacitance/inductance; the `matrix-weighted-norm` √-entry-point cascade (NO-GO-HELD) blocks the `domain_energy_reduce` reduce-verb tail → energy-fields.

**Spine surface-firming tally (post-cycle-083, batch-26 position 2/3 — the cycle BEFORE the batch-26 meta-phase; the column build-out is COMPLETE, the frontier is the highest-fan-out bottom-up vocabulary + opportunistic seed-firming via lowering-verifier law-confidence passes).** **HEADLINE — the SECOND reduce-verb FIRM PROMOTION: the L4 verb `sparameter_reduce` PROMOTED `rough-in (test-coverage-bounded)` → `firm` via the SAME firm-on-positive-structure / syntactic-identity escape that promoted `eigenfreq_qfactor_reduce` in c082 — its laws are syntactic identities / closed-form arithmetic over the now-firm folded projection primitive `port_projection` (c077) + the positive `MeasureSParameter` assembly source (`postoperator.cpp:1246-1309`); the per-port projection cache holds the projection verbatim, so no axiom is smuggled (decisive contrast with the c080 `matrix-weighted-norm` ruling, where the outer √ was NOT positively covered). L4 firm 15→16 main / 19→20 grand; L4 rough-in UNCHANGED (1 + 1 test-coverage-bounded — `domain_energy_reduce` rough-in + `gram_reduce` test-coverage-bounded both remain).** This is the in-scope seed-firming route the batch-25 meta-phase identified (a `lowering-verifier` law-confidence pass on a both-primitives-firm verb), resolving the A1 half of OQ `output-product-reduce-verb-test-coverage-bounded-promotion-route`. **D1** (lowering-verifier, COUNT OWNER): the verb promotion (`sparameter_reduce.md` frontmatter + `## Status` body + fresh `verified_against:` block) + `L4/index.md` count refresh (firm 15→16, dep-map status cell line 107 → `firm`, reduce-to-matrix prose note line 79 → `*(firm, c083)*`; §57 rough-in count UNCHANGED — `sparameter_reduce` was never a §57 bullet) + the coupled `sparameters.{L4,L1,L0}` feature-column verb-status-token refresh (all `(rough-in)`→`(firm, c083)`; the column STAYS `seed` and the per-column promotion-RULE prose was deliberately LEFT verbatim, pending the batch-26 meta-phase `feature-column-promotion-break-the-seed-deadlock` directive) + a stale `bilinear-form`→`port_projection` (c077) citation correction in `sparameters.L0.md:28`. **D2** (lifter, HYGIENE, zero status/count change): `eigenmode.L4.md` stale `(its eigenfreq_qfactor_reduce verb is rough-in)` clause synced to firm (c082) — pure maturity-word hygiene, the column stays `seed`. **MEASURABLE DELTAS: L4 firm 15→16 main / 19→20 grand (+`sparameter_reduce`); L4 rough-in UNCHANGED.** All other layer-vocabulary counts UNCHANGED from c082 (L1 firm 30 main / 37 grand, L4>L3 10, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 33 + `record` Kind, methodology 2; feature spine 13 columns by-kind-grouped, all `seed`; L4 reduce-family 4 verbs — `eigenfreq_qfactor_reduce` + `sparameter_reduce` now FIRM, `gram_reduce` test-coverage-bounded, `domain_energy_reduce` rough-in). **OQs:** RESOLVED-BY-AUDIT the A1 half of `output-product-reduce-verb-test-coverage-bounded-promotion-route` (sparameter_reduce → firm); no new OQs opened by finalize. **BATCH-26 REDUCE-VERB STATUS: the in-scope law-confidence route is now EXHAUSTED for the two verbs whose folded primitives are ALL firm (`eigenfreq_qfactor_reduce` c082, `sparameter_reduce` c083 — confirming the batch-25 "seed-surface firming ceiling" is CONDITIONAL, not absolute); the remaining A3 `gram_reduce` + A4 `domain_energy_reduce` are both foundation-gated behind the `matrix-weighted-norm` √-entry-point trigger-gated cascade (their energy/Gram forms are still rough-in) — a batch-26 meta-phase weigh.** **CARRY-FORWARDS for the cycle-084 planner + batch-26 meta-phase:** (i) **THE USER DIRECTIVE 2026-06-03 `feature-column-promotion-break-the-seed-deadlock`** (open-questions.md USER DIRECTIVE section; memory `project_feature_column_promotion_rule`) — the batch-26 meta-phase MUST enact it: amend the column-promotion convention in CLAUDE.md §Extraction-goal + the `layer-intro-author` role-spec §FEATURE-SURFACE (a column promotes on its OWN composition + directly-owned constituents; cross-linked sibling columns are references, NOT blockers), queue the all-13-column re-evaluation as the batch-27 lead, and SESSION-RESTART after the `.claude/agents/` edit; (ii) the in-scope reduce-verb law-confidence route is EXHAUSTED for A1/A2 — A3 `gram_reduce` + A4 `domain_energy_reduce` need the `matrix-weighted-norm` √-entry-point cascade (meta-phase weigh); (iii) the two **(D) stale-pointer findings for the meta-phase ledger-unification** (orthogonalize family) still pending the unify-pass.

**Spine surface-firming tally (post-cycle-081, batch-25 position 3/3 — the LAST primary cycle of batch-25; the batch-25 meta-phase fires AFTER this finalize, aggregating 079/080/081).** **HEADLINE — a SINGLE clean hygiene dispatch (land-clean discipline for the last pre-meta cycle): the c080 D3-staleness clause in the `eigenfrequency-qfactor` feature column was CLEARED; ZERO firm-count / status change.** **D1** (lifter) cleared the now-stale "the eigenvalue-un-transform has no firm L1 entry" claim in `book/src/feature/eigenfrequency-qfactor.{L4,L1}.md` (7 edits): dropped the stale claim, live-linked the now-firm L1 `eigenvalue-untransform` (c080), flipped two stale dep-map cells `rough-in`→`firm`, and re-anchored the column's `seed`-rationale onto the sole remaining gate-(b) (the eigenpair→`(f,Q)` assembly test). Closed OQ-1016 (`eigenfrequency-qfactor-L4-column-promotion-coupled-to-D2-untransform-firming`). The residual gate-(b) lives on at OQ-1013 (`eigenfreq-qfactor-reduce-firm-needs-assembly-test`, OPEN, out of write-scope). **MEASURABLE DELTAS: NONE** — both columns stay `seed`, verb `eigenfreq_qfactor_reduce` stays `rough-in (test-coverage-bounded)`; all layer-vocabulary counts UNCHANGED from c080 (L1 firm 30 main / 37 grand, L4 firm 14, L4 rough-in 5, L4>L3 10, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 33 + `record` Kind, methodology 2; feature spine 13 columns by-kind-grouped, all `seed`; L4 reduce-family 4 verbs). **BATCH-25 NET (079/080/081):** L1 firm **+1** (`eigenvalue-untransform`, c080); both c075 reduce verbs (`sparameter_reduce`, `eigenfreq_qfactor_reduce`) promoted to `rough-in (test-coverage-bounded)` (c079) with `eigenfreq_qfactor_reduce` gate-(a) DISCHARGED (c080); `matrix-weighted-norm` √-entry-point warrant SHARPENED (rough-in, c080); one NEW rough-in L4 verb `domain_energy_reduce` authored (c079). **CARRY-FORWARDS routed to the BATCH-25 META-PHASE** (explicitly meta-phase questions, NOT cycle-082 plan items yet): (i) **seed-surface firming ceiling** — the cycle-081 planner found the eigenpair→(f,Q) assembly test (gate-(b)) CANNOT be discharged via cite-existing-tests (no positive assembly test in the corpus — only round-trip-invariance tests); this recurs across all three reduce verbs' assembly gates; the meta-phase should assess whether the seed surface is at its in-scope firming ceiling (remaining gates need out-of-write-scope new tests); (ii) **`matrix-weighted-norm` √-entry-point full firm** would cascade a ~30-file re-anchor sweep — meta-phase weighs "dedicate a cascade cycle" vs "stay bounded"; (iii) **`cycle-record.jsonl:209` blank line** (pre-existing, predates batch-25; all rows otherwise parse) — possible meta-phase cleanup; (iv) **`domain-field-energy-participation-guard-inconsistency`** source-observation (electric numerator-guard vs magnetic denominator-guard asymmetry in `MeasureDomainFieldEnergy`, c079 D3 intake) — flagged by two planners now; the meta-phase decides if it crosses the `problems/` bar given the aggregated 079/080/081 view.

**Spine surface-firming tally (post-cycle-080, batch-25 position 2 — the SECOND primary cycle after the batch-24 meta-phase; the column build-out is COMPLETE, the frontier is FIRMING the seed surface; the batch-25 meta-phase fires AFTER cycle-081).** **HEADLINE — a NEW firm L1 primitive `eigenvalue-untransform` LANDED, discharging gate-(a) of the L4 verb `eigenfreq_qfactor_reduce` (both folded scalar maps now firm L1); L1 firm 29→30 main / 36→37 grand (+1).** **D2** (harvester) authored `book/src/L1/eigenvalue-untransform.md` — the per-mode eigenvalue→ω un-transform scalar map (`ω=√μ` linear-EVP / `ω=λ/i` quadratic-EVP, keyed on the structural predicate `!C && !has_A2`; in-chapter `EvpDegree` §Record definition; firm via the firm-on-positive-structure escape). This is the SECOND per-mode scalar building block the `eigenfreq_qfactor_reduce` fold folds (the first being `participation_ratio`, c077); with both folded maps now firm L1, the verb's **gate-(a) is DISCHARGED**. The verb itself STAYS `rough-in (test-coverage-bounded)` (NOT promoted to firm) — its remaining gate-(b) is the eigenpair→`(f,Q)` assembly test (open + out of project write-scope; OQ `eigenfreq-qfactor-reduce-firm-needs-assembly-test`). **D1** (lowering-verifier) sharpened the `matrix-weighted-norm` 2nd-gate warrant: the SPD radicand `⟨E,M E⟩`+`½` is now positively test-covered (`test-domainpostoperator.cpp`), but the outer `√` at the named entry point `linalg::Norml2` is NOT (firm-on-positive-structure escape RULED OUT); the token STAYS `rough-in (test-coverage-bounded)` (**+0 firm** — a warrant-sharpening + a `verified_against:` block, NOT a promotion). Coupled re-anchor of `domain_energy_reduce` (its energy-form constituent's radicand is now test-covered; gate-(a) PARTIALLY advanced, not discharged). **D3** (lifter) prose hygiene: `sparameters.L1.md` 6 stale `bilinear-form` down-link refs repointed to the firm L1 `port_projection` (dep-map cell `rough-in`→`firm`; column STAYS `seed`, now gated on the still-rough-in `sparameter_reduce`); `eigenfrequency-qfactor.L4.md` two internally-contradictory stale Status paragraphs reconciled. **MEASURABLE DELTAS: L1 firm 29→30 main / 36→37 grand (+`eigenvalue-untransform`).** All other layer-vocabulary counts UNCHANGED from c079 (L4 firm 14, L4 rough-in 5, L4>L3 10, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 33 + `record` Kind, methodology 2; feature spine 13 columns by-kind-grouped, all `seed`; L4 reduce-family 4 verbs — `gram_reduce`/`sparameter_reduce`/`eigenfreq_qfactor_reduce` all test-coverage-bounded + `domain_energy_reduce` rough-in). **OQs:** RESOLVED `eigenvalue-untransform-l1-primitive`, `eigenfreq-qfactor-reduce-firm-needs-l1-eigenvalue-untransform-primitive`; PARTIALLY-ADVANCED `matrix-weighted-norm-and-bilinear-form-stay-rough-in-with-sharpened-per-operator-gates-c028` + `domain_energy_reduce-promotion-double-gated`; OPENED `eigenfreq-qfactor-reduce-firm-needs-assembly-test` + two coupled-column promotion-gate follow-ups (`eigenfrequency-qfactor-L4-column-promotion-coupled-to-D2-untransform-firming`, `sparameters-L1-column-promotion-coupled-to-sparameter-reduce-firming`). **CARRY-FORWARDS for the cycle-081 planner:** (i) **D3-staleness clause** — `book/src/feature/eigenfrequency-qfactor.L4.md` still contains a now-stale "the eigenvalue-un-transform has no firm L1 entry" clause (D3's reconciled prose, written before D2's firm `eigenvalue-untransform` landed the SAME cycle); a trivial next-cycle prose touch should tighten it to "the un-transform IS now firm L1 `eigenvalue-untransform` (c080); the residual `firm`-blocker is gate-(b) the assembly test" (low fan-out, mechanical; NOT a build error — was NOT tightened this cycle since it sits inside a `seed`-gate-narration the per-report integrator correctly deferred); (ii) **`eigenfreq_qfactor_reduce` gate-(b)** (the eigenpair→(f,Q) assembly test) is the residual gate to firming the verb — OQ `eigenfreq-qfactor-reduce-firm-needs-assembly-test`; whether an assembly-confidence `lowering-verifier` pass could discharge it in-scope is a batch-25 meta-phase question; (iii) **`matrix-weighted-norm` √-entry-point** is the residual gate; a full firm promotion would cascade a ~30-file re-anchor sweep — NOT triggered this cycle.

**Spine surface-firming tally (post-cycle-079, batch-25 position 1 — the FIRST primary cycle after the batch-24 meta-phase; the column build-out is COMPLETE, the frontier is now FIRMING the seed surface).** **HEADLINE — BOTH c075 output-product reduce verbs' 2nd (test-coverage) gate DISCHARGED via existing-test citation + a NEW L4 verb `domain_energy_reduce` authored at `rough-in`.** Both `sparameter_reduce` + `eigenfreq_qfactor_reduce` promoted `rough-in` → **`rough-in (test-coverage-bounded)`** by citing the existing Palace postprocess unit tests (`test/unit/test-postoperator.cpp`, `test/unit/test-postoperatorcsv.cpp`) as L0-equivalent semantic documentation — the route explicitly sanctioned by the **batch-24 meta-phase decision (e)** (cite existing postprocess tests, do NOT author new). The tests assert the reduction-OUTPUT dimensional/round-trip invariants over the `Measurement` cache (`c.S` for sparameter, `mode_port_kappa`/`participation_ratio` for eigenfreq-qfactor); the field-assembly path is NOT directly exercised, so the verbs are test-coverage-bounded rough-in, NOT firm. `sparameter_reduce` also recorded its **gate-b** already-discharged out-of-band by the firm L1 `port_projection` (c077). **Both coupled feature columns (`sparameters`, `eigenfrequency-qfactor`) STAY `seed`** (a constituent still not firm — by their own stated rule). **NEW L4 verb `domain_energy_reduce` authored at `rough-in`** (`book/src/L4/domain_energy_reduce.md`, the per-domain `(energyᵢ, pᵢ)` energy-table reduction; per-DOMAIN sibling of the per-mode `eigenfreq_qfactor_reduce`, both reduce-to-scalar-TABLE rank-1) on a combinator-miner DISTINCT-VERB-WARRANTED confirm-probe (the redirect's do-not-mint-vocabulary-that-does-not-simplify check) — resolves the energy-fields column's plain-text forward-refs to live links; carries an in-chapter `## Record definition` for the single-consumer input record `DomainOpMap` (≥2-consumer promote-watch opened). **MEASURABLE DELTAS: L4 rough-in 1→2** (+`domain_energy_reduce`; the L4 reduce-family is now 4 verbs: `gram_reduce` + `sparameter_reduce` + `eigenfreq_qfactor_reduce` + `domain_energy_reduce`); **both c075 reduce verbs `rough-in` → `rough-in (test-coverage-bounded)`** (a maturity-qualifier upgrade, NOT a firm promotion). **NO firm-count change** (qualifier upgrades + 1 new rough-in chapter; firm UNCHANGED). All other layer-vocabulary counts UNCHANGED from c078 (L1 firm 29 main / 36 grand, L4 firm 14, L4>L3 10, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 33 + `record` Kind, methodology 2; feature spine 13 columns by-kind-grouped, all `seed`). **OQs:** RESOLVED `sparameter-reduce-status-promotion-double-gated`, `eigenfreq-qfactor-reduce-status-promotion-double-gated`, `domain_energy_reduce-l4-verb-needs-authoring`, the confirm-probe provenance marker; OPENED successor `eigenfreq-qfactor-reduce-firm-needs-l1-eigenvalue-untransform-primitive` + the `DomainOpMap` ≥2-consumer promote-watch. **CARRY-FORWARDS for the next planner:** (i) the `sparameters.L1.md` PROSE down-link repoint (`:39,60,64` `bilinear-form`→`port_projection`) was DEFERRED (one-theme-per-invocation) — route as a next-cycle `layer-intro-author`/`lifter` follow-up; (ii) the `eigenfrequency-qfactor.L4.md` pre-existing Status opening paragraph (~line 68) still carries the old κ-participation "rough-in" reasoning (the appended paragraph carries the corrected narration) — minor staleness, flag for next-cycle prose cleanup; (iii) **`matrix-weighted-norm`'s test-coverage gate is incidentally addressable via `test-domainpostoperator.cpp` (`GetElectricFieldEnergy` positive SI-energy assertion) — a STRONG next-cycle candidate** that would firm `domain_energy_reduce`'s constituent gate (surfaced by the c079 planner).

**Spine column tally (post-cycle-078): COLUMN BUILD-OUT COMPLETE — 13 columns on disk, all BY-KIND-GROUPED** — 6 driver columns (boundary-mode / driven / eigenmode / electrostatic / magnetostatic / transient) + 5 output-product columns (capacitance / eigenfrequency-qfactor / energy-fields / inductance / sparameters) + 1 lifecycle ROOT (spine-ROOT). **Cycle-078 (batch-24 position 3/3) closed out the column build-out:** the FINAL output-product column (`energy-fields`, output-product cohort 4→5, driver-AGNOSTIC) + the boundary-mode driver-leaf (driver cohort 5→6) + an output-product↔driver reciprocal cross-link wiring pass (D3 lifter, across `feature/{driven,eigenmode,electrostatic,magnetostatic}.L4.md`). **The FEATURE-SURFACE SPINE is now COMPLETE on the column count** — every driver-leaf + output-product + the spine-ROOT is authored at `seed`, all in by-kind groupings. **NO firm-count change** (these are `seed` feature columns + a wiring pass; no layer-vocabulary status changed). The energy-fields column is **driver-agnostic** (breaks the 1:1 output-product↔driver convention — OQ for the batch-24 meta-phase). The minted `domain_energy_reduce` L4 verb is referenced but its verb file is NOT yet authored (a follow-up; OQ). **The next forward frontier (post-column-build-out) is the batch-24 meta-phase's to assess** (reduce-verb 2nd-gate firming, verb-file authoring, driver-agnostic-convention reconcile, or a different direction). **Spine column tally (post-cycle-076): 10 columns on disk, now BY-KIND-GROUPED** — 5 driver columns (electrostatic / magnetostatic / eigenmode / driven / transient) + 4 output-product columns (capacitance / inductance / sparameters / eigenfrequency-qfactor) + 1 lifecycle ROOT. **L4 reduce-family now 3 verbs:** `gram_reduce` (bilinear-Gram, `rough-in (test-coverage-bounded)`) + `sparameter_reduce` (linear-projection, `rough-in`, c075) + `eigenfreq_qfactor_reduce` (per-mode scalar-table, `rough-in`, c075); **as of cycle-077 the two new c075 verbs each have a firm L1 home (the first of their two gates)** — `sparameter_reduce`→firm `port_projection` (gate-b), `eigenfreq_qfactor_reduce`→firm `participation_ratio` (gate-a) — but **stay `rough-in`** pending the SECOND gate (a dedicated reduction test). **L1 firm count: 27→29 main-cohort / 34→36 grand total (cycle-077, +`participation_ratio` +`port_projection`)** — incl. 4 FE-assembly + 3 FE-space sub-spine; the dep-map table holds 36 firm rows on-table. **Cycle-077 also landed the FIRST record-definition concept pages (USER DIRECTIVE 2026-06-03 directive-2, the record-definition obligation):** 7 new `book/src/concepts/` pages (`op-params`, `sim-state`, `krylov`, `step-outputs`, `prev-carry`, `solve-result`, `config-record`) + a NEW `record` Kind in the concepts Kind-legend (concepts pages 26→33). The `record` Kind value is in-use-now-pending **batch-24 meta-phase ratification** (OQ `concepts-record-kind-needs-meta-ratification`). **STRUCTURAL MILESTONE — Cycle-076 (batch-24 position 1/3, LEAD): the FEATURE-PART BY-KIND REORG LANDED (USER DIRECTIVE 2026-06-03 directive-1, enacted).** The flat 10-column `# Feature surfaces` Part is now nested into **3 by-kind sub-chapter groupings**, each headed by a NEW group-intro page: **spine-ROOT (lifecycle)** (`book/src/feature/spine-root.md`, 1 column, nested FIRST — the spine reads top-down) / **driver-leaf** (`book/src/feature/driver-leaf.md`, 5 columns alpha-within-kind) / **output-product** (`book/src/feature/output-product.md`, 4 columns alpha-within-kind). `SUMMARY.md` `# Feature surfaces` block nested under the 3 group parents; `feature/index.md` matrix re-sorted into the 3 groupings with bold group-header rows; the **within-column high→low (L4→L1→L0) deliberate exception PRESERVED** in every group/row. **PURE-STRUCTURAL — ZERO count/status/citation/column-body changes** (all layer-vocabulary + spine counts UNCHANGED from c075). Closes OQ `feature-part-by-kind-nesting-output-product-cohort-grouping`. New feature columns (energy-fields, wave-port/boundary-mode) now land directly in their by-kind grouping (alpha-within-kind), not flat-appended. The companion directive-2 (struct-definition-obligation coverage gap) was codified into the role-specs by the batch-23 meta-phase (CLAUDE.md §Methodology-invariants "the record-definition obligation").

## Shared infrastructure — detail

Detail rows for the prioritised section above.

### Krylov solvers

- [x] **CG** — preconditioned conjugate gradient (cg slice at L4; v0.2 + preconditioned variant).
- [x] **GMRES / FGMRES** — restarted with `pc_side × gs_orthog × flexible` variants at L4 (gmres slice). **L4>L3 migration themes both firm as of cycle-021**: `gmres-inner-loop-iterate-while-migration` (firm cycle-020) + `fgmres-inner-loop-iterate-while-migration` (firm cycle-021 — closes the 5-batch carry-forward `fgmres-inner-loop-iterate-while-migration-lifter-candidate`, cycle-010→021; the FGMRES theme adds the two variant-axis collapses `pc_side=RIGHT`/`flexible=true` + the per-iteration `Z[j]` workspace over the firm gmres rotation). L4>L3 firm cohort 2→3; rough-in cohort 1→0.
- [stub] **MINRES** — symmetric-indefinite three-term recurrence. Palace ships enum (`KspType::MINRES`) + JSON parser entry but routes to `MFEM_ABORT` at `palace/linalg/ksp.cpp:53-56`. L1>L0 obstruction theme landed cycle-004 (`book/src/L1-L0/minres-iteration.md`). NOT a direct implementation target per the unimplemented-stub policy; L1 form available as guidance for `krylov-step` at L2.
- [stub] **BiCGStab** — non-symmetric short-recurrence. Same enum-only-with-MFEM_ABORT pattern as MINRES. L1>L0 obstruction theme landed cycle-004 (`book/src/L1-L0/bicgstab-iteration.md`). NOT a direct implementation target.

**L1>L0 lowering floor — BOTH key mutation-rotation themes firm as of cycle-100** (two lowering-verifier firm-promotions, firm-on-positive-structure / syntactic-identity escape): the **matvec lowering floor** `book/src/L1-L0/apply-linop-mutation-rotation.md` (`rough-in` → **firm**; 5 sub-patterns; rests on `L1/apply_linop` firm + L0 ground) and the **driver lowering floor** `book/src/L1-L0/ksp-solve-mutation-rotation.md` (`rough-in` → **firm**; 4 sub-patterns; CG axpby/dot/converged + GMRES Arnoldi/Givens; per-step `apply-linop` is a recognition `reference` edge — rank invariant holds at firm/firm). Both `rank_violations`-clean. The ksp-solve promotion also fixed 2 pre-existing `verified_against:` YAML defects (unquoted mid-string colons) as bounded make-machine-readable repairs.

### Orthogonalisation

- [x] **MGS / CGS / CGS2** — at L4 (orthog slice; residual-axis disclosure for L2 primitive-sequence divergence). **Firm L1 operator `orthogonalize` landed cycle-012** (`book/src/L1/orthogonalize.md`; closes the HIGH-priority cycle-010 blocking OQ `l1-orthogonalize-promotion-from-arnoldi-step-and-orthog`; `dot`+`axpy` deps only; all-three-levels (a/b/c) absorption under residual-axis disclosure; Householder explicitly scoped out per the unimplemented-component policy).
- [?stub] **Householder QR** — sibling slice; structurally-distinct variant per `book/src/concepts/variant-absorption.md`. Stub-status unverified — pre-grep `palace/utils/labels.hpp` + orthog selection points before harvester dispatch (per priority #13 enum-discovery sweep).

### Smoothers and preconditioners

- [x] **Chebyshev (1st-kind and 4th-kind)** — at L4 (chebyshev slice; constructed-operator variant absorption). **Firm L1 operator `chebyshev-smoother` + firm L2 operator `chebyshev-iteration` landed cycle-012** (`book/src/L1/chebyshev-smoother.md` + `book/src/L2/chebyshev-iteration.md`; closes cycle-011 OQ `l1-l2-chebyshev-smoother-and-iteration-firm-row-promotion`; ratified KEEP-FIRM by the cycle-012 integrator — bounded fixed-degree polynomial *action* with closed-form coefficients + live multigrid-integration coverage, distinct from the `eigsolve` literature-inferred rough-in precedent; `rho_0 = delta/theta` per source). **L3 + L4 Chebyshev rows landed cycle-013**: `book/src/L3/chebyshev.md` (`partial-obstruction` — body lifts to global tensor-field, inner `k`-recurrence + outer `pc_it`-sweep witnessed sequential obstructions per Phillips & Fischer 2022) + `book/src/L4/chebyshev.md` (**PROMOTED `rough-in`→`firm` cycle-015** — the `apply` body's two un-anchored sequential obstructions [`forM_` outer `pc_it` sweep, `foldM` inner `k`-recurrence] were re-anchored onto nested `iterate_while_pure` folds with step-count predicates, reusing the canonical firm `iterate-while` family per the cycle-014 combinator-miner REUSE verdict; OQ `chebyshev-l4-firm-via-iterate-while-reanchor` resolved; **L4 firm 3→4, rough-in cohort → 0**). **Lowering themes landed cycle-013**: `book/src/L1-L0/chebyshev-smoother-mutation-rotation.md` (firm) + `book/src/L2-L1/chebyshev-iteration-fusion.md` (firm — **first chapter under the L2-L1 Part**). **Phase-1 `chebyshev.md` slice FULLY REMOVED cycle-015** (`git rm`; 18 inbound citations re-pointed onto the firm L1/L4 cohort; corpus removals 8/10 → 9/10).
- [~] **Jacobi / damped Jacobi** — point-wise. **Firm L1 operator `jacobi-smoother` landed cycle-032** (`book/src/L1/jacobi-smoother.md`; the FIRST diagonal-preconditioner-apply primitive in the L1 cohort; `y = (ω·D⁻¹) ⊙ x` Jacobi smoother / damped Jacobi / pre-smoother for Chebyshev / block-Jacobi base / multigrid coarsening; 6 algebraic laws + 4 explicit non-laws + 2 variant axes (element-type Real/Complex + damping-mode `omega == 0.0` adaptive-fixed-point vs explicit `omega > 0.0`); firm-on-positive-structure per `chebyshev-smoother` precedent — read in full at `palace/linalg/jacobi.{cpp,hpp}` with 68 citations + 13 live cross-links; consumes the firm assemble-diagonal primitive at setup; the constructed-operator solve-gate is a thin elementwise product. **Cycle-033 batch-9 closing**: firm L1>L0 theme `jacobi-smoother-mutation-rotation` landed (`book/src/L1-L0/jacobi-smoother-mutation-rotation.md` — 4 sub-patterns A/B/C/D, 33 citations clean, the diagonal-preconditioner-apply lowering; lowers cleanly to the cycle-033-landed `reciprocal` + `elementwise_product` L1 primitives + the firm `assemble-diagonal-mutation-rotation` setup chain) — closes OQ `jacobi-smoother-mutation-rotation-l1-l0`. The L1>L0 mutation-rotation cohort now end-to-end-lowers the diagonal-preconditioner-apply gate. The `polynomial-smoother` L2 combinator candidacy from `jacobi`+`chebyshev`+`richardson` remains routed cycle-034+ (awaits a third firm sibling — `richardson`).
- [?stub] **Symmetric Gauss-Seidel** — element-wise; genuinely sequential at L3 (predicted obstruction). Stub-status unverified.
- [?stub] **ILU(0) / ILU(k)** — incomplete LU factorisations. Stub-status unverified.
- [ ] **AMS** (auxiliary-space Maxwell, Hiptmair-Xu) — Palace's multigrid-equivalent for curl-curl.
- [ ] **Geometric multigrid V-cycle** — driver + level-recurrence + restriction/prolongation.

### Projections and auxiliary operators

- [~] **Divergence-free projection** (divfree slice) — at L3, L4 in flight. (Status unchanged through meta-18; no L4 landing in cycles 80-85.) **Slice partially reduced cycle-012** (phase-1 corpus reduction batch-3; transparent-optimization list dropped, load-bearing claims retained verbatim; 6 firm entries cite the slice as load-bearing evidence). **`L1/divfree-projector` landed cycle-013 as `partly-constructive`** (`book/src/L1/divfree-projector.md`; structure firm, but the idempotence law `P∘P=P` + divergence-free output characterization depended on the then-unverified `WeakDiv ≈ GᵀM` sign reading). **PROMOTED partly-constructive→`firm` cycle-015** — the `WeakDiv ≈ −GᵀM` sign is positively anchored in **Palace-owned** source (`palace/fem/integrator.hpp:217` documenting `a(u,v) = -(Q u, grad v)` + the explicit `-1.0` coefficient at `palace/fem/integ/mixedvecgrad.cpp:202`, contrast sibling `:142` without it), so the idempotence sub-law is evidence-grounded (cycle-014 lowering-verifier UNBLOCKED, cycle-015 abstractor ENACTED; OQ `divfree-weakdiv-sign-convention-l0-verify` resolved). **This is the FIRST full `partly-constructive` ENTRY→EXIT lifecycle** (ENTRY cycle-013, UNBLOCK cycle-014, EXIT-firm cycle-015) — validates the cycle-012-codified transient-gate mechanism. `ksp_solve`+`apply_linop`+`axpy` deps. **L1 firm 10 → 11.** **L1>L0 lowering theme `divfree-projector-mutation-rotation` landed firm cycle-016** (`book/src/L1-L0/divfree-projector-mutation-rotation.md`; 4 sub-patterns A/B/C/D [in-place Mult apply + scratch threading / copy-then-apply out-of-place wrapper / constructor closure-field materialisation / VecType real/complex element-type variant]; load-bearing `WeakDiv = -Gᵀ` sign positively anchored; first L1>L0 mutation-rotation whose closure carries another constructed-operator gate `P.ksp` as a sub-field — OQ `divfree-closure-nesting-constructed-gate-carrying-constructed-gate`). **L1>L0 themes 10 → 11.**
- [ ] **Curl-curl projector** — predicted similar-pattern to divfree.

### FE assembly

**THREAD OPENED cycle-053 (abstractor D3); FIRST FIRM ENTRY cycle-054 (harvester D2).** The MFEM-equivalent FE-assembly sub-spine is no longer untouched: `book/src/L1-L0/fe-operator-assemble-mutation-rotation.md` landed cycle-053 as a **rough-in L1>L0 thread-opener** (`## Status: rough-in` BY DESIGN) — an observation-first sketch of the "assemble a global FE operator from a list of weak-form terms" rewrite into Palace's build-up-then-assemble machinery. Key insight codemap-verified honest: **BilinearForm is a fold over integrators** `K = Σ_i A(term_i)` (`palace/fem/bilinearform.cpp:28-107`; `palace/models/laplaceoperator.cpp:184` `GetStiffnessMatrix`). **Cycle-054 (harvester D2): `fe_assemble` PROMOTED to FIRM L1** (`book/src/L1/fe_assemble.md`) — the **first firm FE-assembly operator, opening the FE sub-spine**. It is the integrator-fold `K = Σ_i A(term_i)` over an opaque per-term assembly `A`; 4 algebraic laws stated over opaque `A`; 3 variant axes (assembly-representation PA/FA, term-position domain/boundary, trial-test-coincidence square/rectangular). Slug-collision held distinct from the BLAS-2 `bilinear-form` (`xᴴMy` reduction) via a load-bearing `## Slug-collision` section. The L1>L0 dep-map LHS is now firm `fe_assemble` (live link) with corrected `AddSubOperator` anchors `:71-77`/`:91-97` in the dep-map row (the theme BODY still cites the old +2-drift `:73-75`/`:93-95` — a propose-only deferral to a batch-17 lifter, OQ `fe-assemble-theme-addsuboperator-citation-drift`). Deferred speculative operators `eliminate_essential_bc` / `eliminate_rhs` / `weak_form_term` stay **rough-in** (plain-text forward-refs in `book/src/L1/index.md`; await harvester promotion). OQs routed to the batch-16 meta-phase: FE-assembly thread scope/sequencing, libCEED matrix-materialization boundary classification (transitive-firm-leaf vs opaque-library-ownership vs tensor-contraction-respine), the `weak_form_term` cohort enumeration, the BC-elimination siblings, the rectangular/multilevel axes, the theme re-anchor-to-firm-LHS, and the L1-index cohort-header staleness. **L1 firm +1** (`fe_assemble`); L1>L0 themes unchanged (the thread-opener stays rough-in; the firm entry lowers via it). **Cycle-055 (D3/D4/D5/D6): the FE sub-spine advanced — `eliminate_rhs` + `eliminate_essential_bc` both PROMOTED FIRM L1 (all 3 FE-assembly operators now firm), the libCEED matrix-materialization boundary CLASSIFIED `obstruction (opaque-library-ownership)`** (`book/src/L1-L0/fe-assemble-libceed-boundary-obstruction.md` — settles the c053 boundary OQ per the batch-16 meta-phase ratification: libCEED owns the element-local quadrature kernel + COO numerical materialization, Palace owns the fold/dispatch/COO→CSR-shuffle/BC-elimination; `fe_assemble` STAYS firm — the obstruction is a strict sub-term below the fold's opaque leaf `A(term_i)`), and the `fe-operator-assemble-mutation-rotation` thread-opener was re-anchored to the firm `fe_assemble` LHS (D6 lifter; theme STAYS rough-in pending the eliminate_* elimination-leg re-anchors). **L1 firm +2 cycle-055** (`eliminate_rhs` + `eliminate_essential_bc`); **L1>L0 +1 obstruction annotation** (libCEED boundary). The residual `fe_assemble.md:147` citation drift `:215-217`→`:216-217` (1 place, integrator-confirmed) is a deferred OQ for a future lifter — NOT a build defect. **Cycle-057 (lifter D2): `fe-operator-assemble-mutation-rotation` PROMOTED rough-in→FIRM L1>L0** — now that all 3 LHS operators (`fe_assemble` + `eliminate_rhs` + `eliminate_essential_bc`) are firm, the FE-assembly mutation-rotation theme is firm (2 BC-elimination-leg re-anchors to the firm `eliminate_essential_bc`/`eliminate_rhs` live links + 3 citation drift fixes `:225-253`→`:225-252`/`:236`→`:238`/new `:247` + a stale-back-ref fix; the libCEED-boundary OQ re-anchored to the settled obstruction annotation; the index-cell anti-drift guard applied — theme `## Status` + frontmatter + `L1-L0/index.md` row all flipped to firm in one report). **The FE-assembly sub-spine L1>L0 edge is now COMPLETE**: 3 firm L1 operators + the firm L1>L0 rotation theme + the libCEED obstruction boundary. **L1>L0 firm themes +1** (`fe-operator-assemble-mutation-rotation`). The genuinely-new FE differential-operator vocabulary (`weak_form_term` cohort) is the remaining FE-assembly width, deferred as a batch-18 candidate. **Cycle-061 (batch-19, position 1/3; D1 harvester + D2 abstractor): the `weak_form_term` cohort LANDED — the genuinely-new FE differential-operator vocabulary, pull-driven by the magnetostatic curl-curl term.** **`weak_form_term` PROMOTED FIRM L1** (`book/src/L1/weak_form_term.md` — the `(coefficient, differential-operator)` pair naming one weak-form contribution `a(u,v) = (Q·𝒟u, 𝒟v)`; `𝒟` the differential-operator variant axis, `Q` the material coefficient; it is the **element type of `fe_assemble`'s opaque term list** — 2 opaque-`WeakFormTerm` rough-in notes in `fe_assemble.md` re-anchored to live links, replace-and-propagate per the vocabulary-shift redirect; grounded in `palace/models/laplaceoperator.cpp:184-194` Gradient/DiffusionIntegrator/epsilon_func + `palace/models/curlcurloperator.cpp:170-181` Curl/CurlCurlIntegrator/muinv_func). **`weak-form-term-rotation` LANDED FIRM L1>L0** (`book/src/L1-L0/weak-form-term-rotation.md` — the L1 `{coefficient, diff_op}` pair → L0 `AddDomainIntegrator<T>(Q)` template-type + runtime-arg dispatch rewrite; identity-lowers/kernel-opaque split; 2 grounded cases Gradient/DiffusionIntegrator + Curl/CurlCurlIntegrator; mass/div-div pending-pull). **L1 firm 29→30** (`weak_form_term`); **L1>L0 firm themes +1** (`weak-form-term-rotation`); the **FE-assembly sub-spine grows 3→4 firm L1 operators**. NOTE: the `L1/index.md` §Vocabulary-cohort header prose count ("FE-assembly sub-spine 3→4"; grand total "29→30") lags — the dep-map TABLE tally is correct (harvester applied its own row+bullet per the dual-registration partition); only the prose-header count is stale, flagged for a future layer-intro-author touch (OQ `l1-index-fe-assembly-sub-spine-count-prose-refresh-3-to-4`). **D3 (cross-layer-cross-cutter, observation-only): driven/transient OUTER machinery probed — ONE batch-19 LICENSE-FUTURE candidate** `assemble_frequency_operator` (the operator-domain image of the firm tensor-domain `linear_combination`; the driven per-ω `A = a0·K + a1·C + a2·M + A2` scalar-weighted sum of fixed operators via `BuildParSumOperator`, `drivensolver.cpp:176-177` → `spaceoperator.cpp:522-528` → `rap.cpp:780-786`; single positive witness, pull-gated, anti-mirror disposition = extend the EXISTING firm `linear_combination` operand-category axis rather than mint a new slug) + the **driven/transient outer machinery RECORDED spine-complete-except-affine-operator-assembly** (RHS/excitation eval + post-step field collection + transient coefficient setup all solver-specific or already-spine-covered). **Cycle-062 (batch-19, position 2/3; D1+D3 harvester + D2 layer-intro-author): the c061 D3 LICENSE-FUTURE candidate ENACTED + the `weak_form_term` mass axis grounded + the L1/index count refreshed.** **`assemble_frequency_operator` PROMOTED FIRM L1** (`book/src/L1/assemble_frequency_operator.md` — the driven per-ω system-operator assembly `A = a0·K + a1·C + a2·M + A2`; landed as the **operator-operand specialization THROUGH `linear_combination`, NOT a mirrored `operator_linear_combination` fold**, per the 2026-06-01 anti-mirror/replace-and-propagate discipline) + **`assemble-frequency-operator-rotation` LANDED FIRM L1>L0** (`book/src/L1-L0/assemble-frequency-operator-rotation.md`). The enactment **extended `linear_combination`'s operand-category variant axis (tensor-operand | operator-operand) at BOTH L2 AND L3** (`book/src/L2/linear_combination.md` + `book/src/L3/linear_combination.md` — surgical axis-point additions, the fold NOT re-derived; replace-and-propagate of the c061 D3 license). It is **single-pipeline-by-design driven** (one positive witness, affine-modulo-A2, coeff-type-overload — all settled as stated facts, not open caveats) and **sharpens the `map_solve` scope-boundary** (the per-ω operator of the driven `map_solve` superset; `solve_family.md` should cite it by name — deferred cross-ref OQ `assemble-frequency-operator-map-solve-scope-boundary-cross-ref-refresh`, cross-layer-cross-cutter/layer-intro-author domain). **D1 (harvester): `weak_form_term` mass/Identity axis GROUNDED 2-of-4 → 3-of-4** (in-place — `VectorFEMassIntegrator` @ `spaceoperator.cpp:278`; no count change, status stays firm). **D2 (layer-intro-author): `L1/index.md` header-prose count refreshed** — L1 firm grand total **29→31** (27 main + 4 FE-assembly; the c061 prose-lag OQ closed-by-landing, going directly to 31 because `assemble_frequency_operator` landed the same cycle), FE-assembly sub-spine **3→4** (firm `weak_form_term` as 4th member). **L1 firm 30→31** (`assemble_frequency_operator`); **L1>L0 firm themes +1** (`assemble-frequency-operator-rotation`); `linear_combination` gained the **operand-category variant axis at L2+L3**; `weak_form_term` mass axis grounded 3-of-4; the **FE-assembly sub-spine is now 4 firm L1 operators**. NOTE: a NEW cosmetic intake OQ `l1-index-fe-assemble-needs-dep-map-row-for-self-summing-table` (firm `fe_assemble` carries no dep-map row, so the in-table firm-row count [30 after the `assemble_frequency_operator` row] reconciles to the grand total [31] via a +1 off-table header-prose note rather than self-summing; grand total is correct, clean future fix = add a `fe_assemble` dep-map row). **Cycle-063 (batch-19, position 3/3 — close-out/hygiene cycle; NO new firm operators/themes): the FE-assembly cohort's upward-descent is SETTLED-BY-WARRANT + the cosmetic self-summing-table fix + the map_solve↔assemble_frequency_operator scope cross-ref firmed.** **D1 (abstractor, record-only): `fe_assemble` declines an L2 entry — NO-ENTRY-BY-WARRANT** (`L2/fe_assemble` is a degenerate mirror on BOTH anti-mirror axes: a no-carry concatenation-homomorphism fold + an opaque-libCEED per-term leaf `A`; the vocabulary does not shift upward, so per the 2026-06-01 anti-mirror discipline there is NO L2 entry — recorded as OQ `l2-fe-assemble-NO-ENTRY-by-warrant`, routed to the batch-19 meta-phase for formal close + STOP-PROPOSING-negative-list addition, paralleling the c060 D2 `L2/fold_solve` no-floor-warrant; single reopen condition = a future Palace-owned L2 tensor-contraction respine of the libCEED leaf). NO `book/` write. **D2 (lifter): `book/src/L4/solve_family.md` map_solve scope-note re-anchored** — the firm per-ω operator now named `assemble_frequency_operator` via live link `../L1/assemble_frequency_operator.md` (the deferred c062 cross-ref OQ `assemble-frequency-operator-map-solve-scope-boundary-cross-ref-refresh` RESOLVED-BY-LANDING; the reciprocal L1→L4 cross-ref already present, so the link is now mutual; `solve_family` status/laws/axes untouched, no index-cell update owed). **D3 (layer-intro-author): `book/src/L1/index.md` self-summing-table fix** — added the `fe_assemble` dep-map row (in-table firm count 30→31, now self-sums to the grand total with NO off-table +1 note; the cosmetic c062 OQ `l1-index-fe-assemble-needs-dep-map-row-for-self-summing-table` RESOLVED-BY-LANDING), updated the reconciliation note ("31 in-table, all firm rows on-table"), + appended a §Working-Notes cycle-063 bullet recording the 3 NO-ENTRY upward-propagation warrants (`fe_assemble`, `weak_form_term`, `assemble_frequency_operator`) routing their FORMAL close to the batch-19 meta-phase. **NO count delta** (grand total stays 31 — `fe_assemble` was always counted; only its dep-map row was added so the table now self-sums). **The FE-assembly + frequency-operator cohort's upward propagation is SETTLED: 4 firm L1 operators + the affine-operator-assembly driven sibling, the L1>L0 edge COMPLETE, and the L2 floor declined-by-warrant (anti-mirror) — `fe_assemble` upward-descent COMPLETE-BY-WARRANT (NO L2 entry).** For the batch-19 meta-phase: mark `L2/fe_assemble` NO-ENTRY RESOLVED-BY-WARRANT + add `L2/fe_assemble` to the planner STOP-PROPOSING negative list (alongside the batch-18 `L2/fold_solve`); the `weak_form_term`-own-L2 disposition is flagged-forward (not settled this cycle).

- [~] **Sparse-assembly patterns** — partial-assembly vs full-assembly vs matrix-free. **Thread opened cycle-053** via `fe-operator-assemble-mutation-rotation` (PA/FA variant axis named in §Applicability-conditions); **firm `fe_assemble` L1 operator landed cycle-054** (integrator-fold `K = Σ_i A(term_i)`; PA/FA assembly-representation as a declared variant axis).
- [~] **Mesh + FE-space construction** — quadrature points, basis tables, geometric-factor computation. **FE-SPACE/MESH-CONSTRUCTION L1 FRONT OPENED cycle-064 (batch-20 position 1/3, the user's resolved strategic steer — the batch-19 meta-phase's `open-FE-space-mesh-construction-L1-front` ASK).** **`fe_space` PROMOTED FIRM L1** (`book/src/L1/fe_space.md` — the prime FE-space-construction entry; `fe_space :: (mesh: Mesh, collection: FECollection) → FiniteElementSpace[N]`, the typed identity carrying true-dof count `N = GetTrueVSize()`; the **shared substrate under all 5 solver pipelines** — consumed-by `fe_assemble`/`weak_form_term`/`eliminate_essential_bc`/`eliminate_rhs`; **de-Rham family variant axis** H1 (VALUE) / H(curl) (ND) / H(div) (RT) / L2 (INTEGRAL) ↔ `H1_/ND_/RT_/L2_FECollection`, all 4 witnessed at `palace/models/spaceoperator.cpp:47/49/51` (ND/H1/RT) + `:72-75` (2-D L2-curl); L0 variadic ctor `palace/fem/fespace.hpp:67-75` forwarding into `mfem::ParFiniteElementSpace`, `GetTrueVSize` `:96`, MFEM-forwarders `:93-103`, coarse-seed `palace/fem/multigrid.hpp:89-90`; firm-on-positive-structure, no-dedicated-test caveat non-gating per `fe_assemble`/`apply_linop` precedent; **NO `dof_map` mirror** — the MFEM-owned-read-as-given dof structure is an identity-in-named-terms smell, deliberately not minted). **`fe-space-construction-rotation` LANDED FIRM L1>L0** (`book/src/L1-L0/fe-space-construction-rotation.md` — LHS L1 `fe_space` → RHS L0 `FiniteElementSpace` ctor; construction-lowers / dof-bookkeeping-MFEM-owned split; 4 de-Rham rewrite cases H1/ND/RT/L2 with per-case L0 instantiation sites). **L4-index-author front-shell**: `book/src/L1/index.md` gains a NEW "FE-space sub-spine — 1" subsection (the cohort home for `fe_space`, distinct from the FE-assembly sub-spine — `fe_space` CONSTRUCTS the space, FE-assembly FOLDS over it; distinct vocabulary fronts) + grand-total count **31→32** (27 main + 4 FE-assembly + 1 FE-space). **L1 firm 31→32** (`fe_space`); **L1>L0 firm themes +1** (`fe-space-construction-rotation`). **FE-space sub-spine backlog (the batch-20 continuing frontier; fan-out-ranked deferred siblings, all named-not-authored):** `fe_collection` (#2), `essential_dofs` (#3 — straddles the MFEM-owned-boundary), `fe_space_hierarchy` (#4), + sibling-pull-gated `BuildDiscreteInterpolator` / `BuildProlongationAtLevel`. The opaque-parameter fan-out re-anchor (`fe_assemble`/`weak_form_term`/`eliminate_essential_bc`/`eliminate_rhs` re-anchor to firm `fe_space` in a later replace-and-propagate pass) is now UNBLOCKED. **CITATION-TOOLING NOTE for the batch-20 meta-phase:** a `palace-codemap` `read_range` ±1 line-drift on `fespace.hpp` (reported `:66-74` vs the on-disk-verified `:67-75`) cost a citation-reconciliation round this cycle; the artifact is consistent at `:67-75` (verified directly against the on-disk file: line 67=template, 68=signature, 75=closing brace), but the codemap-vs-disk drift is a friction signal for the meta-phase's assessment. **Cycle-065 (batch-20 position 2/3): the FE-space sub-spine grew to its 2nd member + the opaque-parameter re-anchor ENACTED.** **`fe_collection` PROMOTED FIRM L1** (`book/src/L1/fe_collection.md` — the p-multigrid FE-collection order schedule; `fe_collection :: (p, dim, mg_max_levels, coarsening, mat_lor, family) → [FECollection]`, the ordered list of per-level collections that `ConstructFiniteElementSpaceHierarchy` feeds one-per-level into `fe_space`; producer→consumer across the `[FECollection]` boundary; firm-on-positive-structure; 3 variant axes (de-Rham family / coarsening policy / LOR basis) + 6 laws; `mat_lor: Bool` signature param added per repair; `palace/fem/multigrid.hpp:22-73`). **`fe-collection-construction-rotation` LANDED FIRM L1>L0** (`book/src/L1-L0/fe-collection-construction-rotation.md` — LHS L1 `fe_collection` schedule value → RHS L0 `ConstructFECollections` template body; 5-piece forward rewrite; 3 variant-axis rewrite cases; `std::reverse` load-bearing reorganization; MFEM-owned-read-as-given boundary non-gating). **Opaque-parameter re-anchor ENACTED (D1 lifter, replace-and-propagate complete for the front-opening):** the 4 consumer L1 entries (`fe_assemble`/`weak_form_term`/`eliminate_essential_bc`/`eliminate_rhs`) now cross-ref firm `fe_space` via live links over the true-dof axis N (`= space.GetTrueVSize()`, `palace/fem/fespace.hpp:96`); all 4 stay firm (pure cross-ref firming, no status flip). **L1 firm 32→33** (`fe_collection`); **L1>L0 firm themes +1** (`fe-collection-construction-rotation`); the **FE-space sub-spine grows 1→2 firm L1 operators** (`fe_space` + `fe_collection`). **FE-space sub-spine backlog now:** `essential_dofs` (#3 — straddles the MFEM-owned-boundary), `fe_space_hierarchy` (#4), + the L1>L0-theme re-anchor follow-on (OQ `fe-space-opaque-param-l1-l0-theme-reanchor-to-firm-fe-space` — the 4 consumers' L1>L0 THEMES still need re-anchoring to firm `fe_space`, distinct from this operator-surface pass). **CITATION-TOOLING NOTE (recurred c065):** the `palace-codemap` ±1 line-drift recurred this cycle (caught on `multigrid.hpp`/`fespace.hpp`; all ranges verified on-disk before landing) — a corroborating signal for the batch-20 meta-phase's tooling-friction assessment. **Cycle-066 (batch-20 position 3/3, the LAST primary cycle before the batch-20 meta-phase): the FE-space sub-spine CLOSED its batch-20 frontier at 3 members + the consumer re-anchor COMPLETED at the theme layer.** **`essential_dofs` PROMOTED FIRM L1** (`book/src/L1/essential_dofs.md` — the boundary-condition true-dof-set member of the FE-space sub-spine; sub-spine DAG `fe_collection` ▷ `fe_space` ▷ `essential_dofs`; two variant axes (attribute-wildcard explicit-list vs `[-1]`; per-level-hierarchy application); firm-on-positive-structure; RESOLVES the c064 `fe-space-essential-dofs-straddles-mfem-owned-boundary` straddle OQ toward a self-standing entry — WARRANT=YES, contradicting the c064 "lean noted-property" guess). **`essential-dofs-construction-rotation` LANDED FIRM L1>L0** (`book/src/L1-L0/essential-dofs-construction-rotation.md` — LHS L1 `essential_dofs` → RHS L0 dbc block `palace/fem/multigrid.hpp:92-101`; construction-head-lowers / dof-resolution-tail-MFEM-owned split). **The `fe_space`-consumer re-anchor COMPLETED at the THEME layer (D3 lifter, replace-and-propagate now done at BOTH operator + theme layers):** `fe-operator-assemble-mutation-rotation` + `weak-form-term-rotation` re-anchored to firm `fe_space` (the c065 D1 operator-surface pass + this c066 theme-layer pass close the replace-and-propagate end-to-end); PLUS the `multigrid.hpp:22-72`→`:22-73` close-brace citation-hygiene fix at all 3 `fe_space.md` loci; 2 OQs closed (`fe-space-opaque-param-l1-l0-theme-reanchor-to-firm-fe-space` + `multigrid-hpp-template-close-line-citation-hygiene`). **L1 firm 33→34** (`essential_dofs`); **L1>L0 firm themes +1** (`essential-dofs-construction-rotation`); the **FE-space sub-spine grows 2→3 firm L1 operators** (`fe_collection` ▷ `fe_space` ▷ `essential_dofs`). **FE-space sub-spine backlog now:** `fe_space_hierarchy` (#4, lower fan-out), + the `eliminate_*` DofSet→`essential_dofs` cross-ref replace-and-propagate follow-on, + the `fe-space-construction-rotation` forward-ref→live-link upgrade (the new c066 D3 OQ). **CITATION-TOOLING NOTE (recurred c066 — STRONG, 3-of-3 batch-20):** the `palace-codemap` ±1 line-drift recurred AGAIN (the `multigrid.hpp:22-72`→`:22-73` close-brace off-by-one D3 fixed; citecheck `--anchor` does NOT catch it — only on-disk hand-Read does); 3-of-3 batch-20 FE-source cycles, a STRONG corroborated signal for the batch-20 meta-phase (likely a friction-ledger entry + a localization-preference note: prefer on-disk `Read` over codemap `read_range` for FE-source brace-boundary citations). **Cycle-067 (batch-21 position 1/3, LEAD cycle): the FE-space sub-spine TAIL CLEANUP landed (no new operator) + the FE-cohort→L4 lift frontier OPENED via a survey.** D1 (lifter) finished the two carried follow-on cleanups: `eliminate_essential_bc.md` prose shape-contract bullet now names its constructor `[essential_dofs](./essential_dofs.md)` (the `eliminate_*` DofSet→`essential_dofs` cross-ref follow-on), and `fe_space.md:39,149` upgraded the two `fe-space-construction-rotation` forward-refs to live links (the c066 D3 forward-ref→live-link OQ) — all three entries stay firm, no count change. D2 (cross-layer-cross-cutter) ran an OBSERVATION-ONLY FE-cohort→L4 lift survey (no `book/` write) classifying the FE-cohort members three-way (black-box kernel / kept-named-abstraction / accelerated-kernel) and emitting a fan-out-ranked c068 dispatch list (4 OQs promoted): rank-1 `fe_assemble` L4 assemble-fold combinator (the batch-21 frontier opener), rank-2 `assemble_frequency_operator` via `linear_combination` (GATED on the `L4/index.md:66` 13-of-18 "no-L4-by-design" sub-finding being partially wrong — combinators + kept-named-abstractions DO rise to L4), ranks 3-4 the Dirichlet-BC post-compositions, deferred construction-inputs. **No count change this cycle** (D1 cross-ref/live-link firming only; D2 observation-only). **FE-space sub-spine backlog now:** `fe_space_hierarchy` (#4, lower fan-out) carried; the batch-21 frontier shifts UPWARD to FE-cohort→L4 lift (D2 survey ranking). **Cycle-068 (batch-21 position 2/3): the FE-cohort→L4 lift frontier LANDED its rank-1 opener + the two BLAS-1 data-algebra combinators ROSE to L4 — `+4 firm L4 entries`, L4 firm 7→10, the assemble-half of the deliverable now has an L4 surface.** **D1 (harvester): `fe_assemble` PROMOTED FIRM L4** (`book/src/L4/fe_assemble.md` — the **assemble-fold combinator** `fe_assemble space terms = foldr (\t acc -> assemble_term space t + acc) zero terms = sum (map (assemble_term space) terms)`; capture the FE space once `readonly`, fold the immutable `[WeakFormTerm]` list by the opaque per-term libCEED leaf `assemble_term`, sum the contributions into `K`; the **concatenation-homomorphism `foldr` producing a sum** — the homomorphic sibling of `solve_family`'s map, distinct from `fold_solve`'s carry-threaded fold; the opaque per-term leaf rises as a **black-box-kernel `readonly` input** [`black-box-vs-accelerated-kernels` case 1], lifting the `fe-assemble-libceed-boundary-obstruction`; firm-on-positive-structure, ≥2-witness mining-gate met with 3 witnesses; the rank-1 FE-cohort→L4 lift opening the **assemble-half** of the deliverable's L4 surface, directive-1). **D2 (abstractor): `fe-assemble-fold-dissolution` LANDED FIRM L4>L3** (`book/src/L4-L3/fe-assemble-fold-dissolution.md` — the substantive L4>L3 dissolution narrating the `foldr`/sum collapse to the L3 explicit accumulating composite-operator build [`for`-over-integrators + `AddSubOperator`], space-capture-once → `BilinearForm space(...)`-outside-the-loop, per-term leaf bottoming out in the `obstruction (opaque-library-ownership)` boundary; **DISSOLUTION-HOME verdict — NO interposed `L3/fe_assemble` entry** [the `solve_family` NO-ENTRY shape: the term fold lifts, no `sequential-obstruction`]; homomorphic sibling of `solve-family-map-dissolution`, NOT the carry-threaded `fold-solve-time-step-dissolution`; L4-L3 firm-theme tally 8→9). **D3 (combinator-miner): `linear_combination` + `inner_product` PROMOTED FIRM L4** (`book/src/L4/linear_combination.md` — the scalar-weighted-tensor-sum combinator `Σᵢ aᵢ·tᵢ`, arity leaves `scal`/`axpy`/`axpby`/`axpbypcz` as accelerated-kernel notes stopped low; `book/src/L4/inner_product.md` — the reduce-to-scalar inner-product combinator `α = ⟨x,y⟩`, conjugation/element-type/weight specializations `dot`/`tdot`/`bilinear_form` as notes) — the **two BLAS-1 data-algebra combinators that rise to L4 regardless as feature-surface verbs the backend wants** [`black-box-vs-accelerated-kernels` §"The combinators rise regardless"; L4>L3 identity-in-form on the body, the in-line-marker route — no dedicated theme files], correcting the `L4/index.md:66` "13-of-18 no-L4-by-design" blanket to the **per-case** disposition (combinators rise; kept named abstractions `dot`/`nrm2` rise alongside as next-pull verbs; pure accelerated kernels stay low). **L4 firm 7→10** (`fe_assemble` + `linear_combination` + `inner_product`); **L4>L3 firm themes +1** (`fe-assemble-fold-dissolution`, tally 8→9). **Unblocked for c069:** rank-2 `assemble_frequency_operator` → L4 *through* `linear_combination`'s operator-operand corner; ranks 3-4 the `eliminate_*` BC post-compositions → L4; the `L4/dot` + `L4/nrm2` named-verb next-pull through `L4/inner_product`. **Carried-cosmetic:** the `L3/linear_combination` + `L3/inner_product` "no L4" lines are now stale (the combinators DID rise) — a thin re-anchor pass (OQ `l3-data-algebra-combinators-stale-no-l4-reanchor`). **Cycle-069 (batch-21 position 3/3, the LAST primary cycle before the batch-21 meta-phase): the rank-2 FE-cohort→L4 lift LANDED + the kept named verbs ROSE + the L3/L1 stale-pointer/citation-drift hygiene CLOSED — `+3 firm L4 entries`, L4 firm 10→13; the DRIVEN pipeline's ASSEMBLE-half now reaches L4.** **D1 (harvester): `assemble_frequency_operator` PROMOTED FIRM L4** (`book/src/L4/assemble_frequency_operator.md` — the driven per-ω system-operator assembly verb, the **operator-operand specialization of `L4/linear_combination`** through its operator-operand corner [`A(ω) = K + iω·C − ω²·M`, the frequency-parametrized weighted sum of the pre-assembled stiffness/damping/mass operators]; firm-on-positive-structure, 3 variant axes declared+disposed; opens the **assemble-half of the DRIVEN pipeline at L4** [directive-1] — the c068 D2-survey rank-2 item, unblocked by `linear_combination` rising c068). **D2 (harvester): `dot` + `nrm2` PROMOTED FIRM L4** (`book/src/L4/dot.md` — the Hermitian/symmetric inner-product named verb, specialization of `L4/inner_product` at `M = I`; `book/src/L4/nrm2.md` — the Euclidean-norm named verb, CONSUMER of `L4/inner_product` at the diagonal under `√∘abs`, NOT a fold member) — the **two kept named abstractions that rise as feature-surface verbs** [`black-box-vs-accelerated-kernels` case 2; the c068 D3 next-pull verbs], tied to `inner_product` below; D2 the sole L4/index count-owner, tally `(10+4)`→`(13+4)`. **D3 (lifter): `L3/linear_combination` (3 loci) + `L3/inner_product` (2 loci)** stale "no L4 entry"→live-link re-anchor (ENACTS the c068 OQ `l3-data-algebra-combinators-stale-no-l4-reanchor`; both L3 entries stay firm, no status flip; the c068 combinators-rose carried-cosmetic now CLOSED). **D4 (lifter): `L1/fe_assemble` (4 loci)** witness-line-drift citation re-anchor (`laplaceoperator.cpp:191-192`→`:193-196`, `curlcurloperator.cpp:179-181`→`:180-181`, §Evidence pinpoints; ENACTS the c068 OQ `fe-assemble-l1-cap-weak-form-term-witness-line-drift-reanchor`, bringing the firm L1 cap into agreement with the c068 D2 theme; recurrence-6 `codemap-read-range-plus-one-drift-on-brace-boundary` boundary drift, mechanically corrected). **L4 firm 10→13** (`assemble_frequency_operator` + `dot` + `nrm2`); all other layers UNCHANGED from c068 (L1 firm 34, L2 firm 21+1pc, L2>L1 21, L3 firm 17+4po, L3>L2 6, L4>L3 9, L0 22, FE-assembly sub-spine 4, FE-space sub-spine 3, concepts 26, methodology 2). **Per-pipeline L4-completeness picture (assemble-half):** the FE-assembly fold (`fe_assemble` c068) + the driven per-ω weighted-sum assembly (`assemble_frequency_operator` c069) both reach L4 — the assemble-half of the deliverable's L4 surface is materially advanced. **The DRIVEN SOLVE-half remains the ONE OPEN pipeline-half** (the `map_solve`-over-(operator,rhs)-family single-witness STOP vs the L4-completeness directive — OQ `driven-solve-half-l4-completeness-vs-map-solve-single-witness-stop`, user-steered "let the meta-phase decide"; routed to the batch-21 meta-phase). **Unblocked for c070+:** ranks 3-4 the `eliminate_*` BC post-compositions → L4 once their primitives rise; the `L3/dot` + `L3/nrm2` LEAF stale-no-L4 re-anchor (OQ `l3-dot-nrm2-stale-no-l4-entry-lines-need-reanchor`, a c070+ follow-on, distinct from D3's combinator re-anchor).
- [ ] **Operator composition** — sum/product/transpose of FE operators.
- [~] **Boundary conditions** — essential (Dirichlet), natural (Neumann), periodic. **Separable BC-elimination named cycle-053** (`eliminate_essential_bc` / `eliminate_rhs` speculative rough-ins); **both PROMOTED FIRM L1 cycle-055** (`book/src/L1/eliminate_rhs.md` `b' = b − K·x_bc` essential-row pinning per diagonal policy + `book/src/L1/eliminate_essential_bc.md` square-operator essential-Dirichlet pin, zero rows/cols + diagonal-policy `DIAG_ONE`/`DIAG_ZERO` set; both firm-on-positive-structure, separable post-compositions AFTER `fe_assemble`, NOT part of the assembly fold). The L1>L0 elimination-leg theme re-anchors (sibling of `fe-operator-assemble-mutation-rotation`) are a future lifter pass (OQs `eliminate-rhs-mutation-rotation-l1-l0-theme-unauthored` + `eliminate-essential-bc-l1-l0-lowering-re-anchor`). **GENUINE L4 HOLE surfaced cycle-100** (cross-layer-cross-cutter D3 backend-lowering-completeness survey): the **BC-elimination cohort** (`eliminate_essential_bc` / `eliminate_rhs` / `essential_dofs`) is firm at L1 with **no L2/L3/L4** entry — deferred-but-undecided per the c069 sibling-deferral (the BC-half analog of the assemble-half that closed c068). This is the one remaining FE-cohort L4 gap; OQ `bc-elimination-cohort-l4-disposition` (plan-tag `fe-cohort-l4-lift`) carries the disposition decision + the coupled recurring `essential_dofs` mis-attribution at `L4/fe_assemble.md:69,147,175` (the three chapter-body sites NOT corrected c100, to be widened TOGETHER with the BC-disposition). **The D3 survey also REFUTED the stale `project_l4_is_backend_lowering_target` memory claim that "the FE-assembly/FE-space cohort stranded at L1 is a hole to close" — the assemble-half closed c068 (~30 cycles stale); the survey confirmed all 5 solver pipelines + all 5 output-products reach firm L4 feature surfaces. Recommend the batch-32 meta-phase re-scope that memory's named hole to "BC-elimination cohort L4-disposition open."** **GENUINE L4 HOLE CLOSED cycle-101 (batch-32 position 2/3; harvester D1, route (a) — firm L4 cap):** `eliminate_bc` PROMOTED FIRM L4 (`book/src/L4/eliminate_bc.md` — the two co-equal post-assembly BC-application verbs `eliminate_essential_bc` + `eliminate_rhs` lifted cleanly to L4 as a **separable post-composition pair on the assembled operator**, NOT part of the assemble fold; firm-on-positive-structure / syntactic-identity escape, resting on firm `linear_combination` via the one blocking `depends-on` edge), lowered by the NEW firm L4>L3 theme `bc-elimination-post-composition-dissolution` (`book/src/L4-L3/bc-elimination-post-composition-dissolution.md` — DISSOLUTION-HOME verdict, **no interposed `L3/eliminate_bc`**, the `solve_family`/`fe_assemble` NO-ENTRY shape; L4>L3 firm-theme tally 10→11). The coupled recurring `essential_dofs` mis-attribution at `L4/fe_assemble.md` was corrected (`:69`/status-line/`:147`/`:175` — `essential_dofs` re-stated as the two construction inputs `fe_space`/`fe_collection` + a post-assembly cohort feeder, NOT a `fe_assemble` argument), and the c069 BC-deferral was re-anchored (in `fe_assemble.md` + `fe-assemble-fold-dissolution.md`) to the now-firm `eliminate_bc` cap. **The BC-elimination cohort L4 hole — the LAST genuine FE-cohort L4 gap — is CLOSED in one cycle.** `rank_violations` held 0 (both new firm nodes rest on firm deps). **The in-scope deliverable is now substantially L4-complete for backend-lowering: all 5 solver pipelines + FE-assembly + the 5 output-products + BC-elimination reach firm L4 feature surfaces.** New OQs from D1 (routed to the batch-32 meta-phase): `record-DofSet-needs-definition-home` (≥2 consumers, no concept page — layer-intro-author candidate), the `eliminate-rhs-mutation-rotation` L1>L0 leg (forthcoming-vs-already-folded), the `L4/index.md` §Vocabulary-cohort firm-count prose refresh. **Memory `project_l4_is_backend_lowering_target` is now DOUBLY stale — assemble-half closed c068 AND BC-half closed c101 — STRONG recommend the batch-32 meta-phase update it (no remaining named FE-cohort L4 hole).**

### Framework slices

(Added 2026-05-26 meta-22 after cg_preconditioning_framework appeared as an organically-extracted framework slice — different from algorithm or primitive slices.)

- [~] **CG Preconditioning Framework** — abstract preconditioner-as-LinOp interface used by CG and its variants. L0→L1 cycle 123; **L2→L3 cycle 141** (meta-24 — L1→L2 was attempted cycle 133 but tripped role-parametrized-factory renaming and was back-corrected). Likely first of a family of "framework slices" capturing the operator/preconditioner contracts that algorithm slices consume.

### Coordination and post-processing

- [ ] **Time-stepping** (transient): explicit/implicit integrators, step-size control.
- [ ] **Frequency sweep** (driven): adaptive parameter sweep, output collection.
- [ ] **Eigenpair extraction** (eigenmode): Arnoldi/Lanczos restart, residual selection.
- [ ] **Output / I/O** — VTK/ParaView field outputs, S-parameters, port modes.

## Intermediate-tier algorithms (added 2026-05-25 from user directive)

A tier of algorithmic primitives that sit BETWEEN leaf primitives (axpy, dot, apply_linop, …) and top-level driver algorithms (CG, GMRES, Chebyshev). Each intermediate is reused by multiple top-level algorithms and packages a meaningful chunk of shared concept vocabulary. Solving these gives **large bang-for-buck**: one intermediate slice unblocks several downstream slices and exercises cross-slice consistency on the concepts it uses.

**Targeting principle.** Per the user 2026-05-25 directive: the Planner should prioritize intermediate-tier slices over top-level roots when forward-frontier work is available. The loop's first pass picked roots (GMRES, CG, Chebyshev) and traced down to leaves; intermediates were filled retroactively. Going forward, intermediates that aren't yet extracted as standalone slices should be picked positively, ranked by **concept-overlap × downstream-slice-count**.

Candidates, roughly ordered by impact:

- [x] **Arnoldi step** (one inner-loop iteration of Krylov-subspace construction). Reused by: GMRES, FGMRES, MINRES (variant), eigenmode-via-Arnoldi. Concept overlap: `apply_linop`, `orthogonalize`, `dot`, `axpy`, `nrm2`, `scal`. **Landed at L4 via cycles 98–102 (meta-20 window) — first intermediate-tier algorithm extracted.** See `book/src/spec/slices/arnoldi_step.md`.
- [~] **Plane-rotation stream** (Givens-rotation accumulator + replay for least-squares update). Reused by: GMRES (Hessenberg LS update), eigenmode (QR algorithm), driven (complex Givens). Concept overlap: `givens` (generate + apply). L0→L1 cycle 108; L1→L2 cycle 124; **L2→L3 cycle 144** (meta-24). L4 pending.
- [~] **Polynomial-recurrence step** (degree-k Chebyshev / Richardson / Jacobi recurrence body). Reused by: Chebyshev 1st/4th-kind, Jacobi smoother, Richardson iteration. Concept overlap: `axpy`, `elementwise_product`, `scal`. **L0→L1 landed cycle 130** (meta-23 window via retroactive-gate retry); refined cycle 132.
- [~] **Sparse triangular solve** (TRSV variant). Reused by: ILU(0), ILU(k), AMS (smoother), Gauss-Seidel preconditioner, back-solve in direct solvers. Concept overlap: `trsv` (already a leaf concept; this is the *sweep* over a sparse triangular factor). **L0→L1 landed cycle 112** (meta-21 window) — emitted as a *negative result* per Palace not exposing sparse triangular sweep as a top-level primitive; lifting still unblocks future ILU/Gauss-Seidel/AMS work. **Back-solve sub-case landed firm cycle-027**: `book/src/L1/back_solve.md` — the GMRES/FGMRES restart-correction back-solve `y = back_solve(R, s)` over the small-dense Givens-rotated upper-triangular R-factor; harvested as its own GMRES-restart-correction primitive, a **sibling (NOT the realisation)** of the general `trsv` (it reads `R`'s diagonal/super-diagonal entries). **Cycle-028 lowering-verifier audit: `back_solve` UPHELD firm** — additive `verified_against:` evidence-backfill (18 rows, all supports), firm-on-positive-structure (syntactic-identity laws, no-dedicated-test non-gating per the `lu_solve`/`apply_linop` precedent); no status change. **Cycle-028 — `trsv` L3-inventory gap RESOLVED-BY-OBSTRUCTION** (harvester localization, negative finding): Palace exposes NO standalone `trsv` primitive — triangular solves are opaque-library-owned (HYPRE GS/SSOR relax-type flags + external direct-solver wrappers; `densematrix.hpp:24-36` has no triangular solve) or a block-triangular red herring. This was the LAST leaf of the migrated-plan-item `l3-vocabulary-inventory-gap` (gemv/ksp_solve/eigsolve done + trsv resolved-by-obstruction = all four leaves done) → **parent gap fully resolved cycle-028**. **Cycle-029: the citable home is now ON DISK.** `book/src/L1-L0/triangular-solve-obstruction.md` landed as a NEW firm L1>L0 OBSTRUCTION theme — the **FIRST opaque-library-ownership obstruction in the cohort**, methodologically distinct from the two cycle-004 enum-only-stub obstructions MINRES/BiCGStab which document a Palace-internal stub (this one documents an external opaque-library boundary). Negative anchors: HYPRE GS/SSOR relax-type flags `amg.cpp:19,29` + `ams.cpp:162,173`; external direct-solver wrappers `strumpack.hpp:21`/`superlu.hpp:22`/`mumps.hpp:21`; Palace-native smoothers GS-free `jacobi.hpp:19` + `chebyshev.hpp:23,82`; red-herring `blockprecond.hpp:16-29`. The obstruction-cluster grouping is now 3 themes (`bicgstab-iteration`, `minres-iteration`, `triangular-solve-obstruction`). The cycle-029 abstractor flagged a possible methodology refinement (`opaque-library-ownership` vs `enum-only-stub` obstruction sub-kinds) for the batch-8 meta-phase. **Cycle-029 — `ls_update_column` firm L1 leaf landed**: `book/src/L1/ls-update-column.md` — the GMRES/FGMRES per-column running-QR leaf, the **column-streaming producer** sibling of `back_solve` (the terminal consumer); the Face-1 opaque-leaf projection of the firm L2 `incremental-least-squares`. Together with `back_solve` this completes the GMRES restart-cycle least-squares L1 cohort `ls_update_column × (j+1) ▷ back_solve`. **L1 firm cohort 21→22**. **Cycle-029 — `back-solve-mutation-rotation` firm L1>L0 theme landed**: `book/src/L1-L0/back-solve-mutation-rotation.md` — the lowering of the firm L1 `back_solve` leaf forward into the GMRES `iterative.cpp:652-660` / FGMRES `:831-840` restart-correction back-substitution loops; shape-identical, brace-placement +1-line-shifted, basis-lift `V[k]`/`Z[k]` differs only in the *downstream* `linear_combination` outside the leaf per law-6. **Cycle-031 — `ls-update-column-mutation-rotation` audited firm** (additive 33-row `verified_against:` block, all `supports`; the c030-landed firm theme upheld). **Cycle-031 — `back-solve-mutation-rotation` Sub-pattern B narrative defect CLOSED** (lifter narrative-repair: the wrong "+1-line brace-placement shift" prose corrected to factual "byte-for-byte identical, +179-line file offset, zero local relative shift" across 5 sites; F1 verified_against row flipped `partially-supports`→`supports`; theme stays firm — narrative-only fix, structure unchanged). **Cycle-031 — `back_solve` L1 leaf cross-anchor off-by-one fixes** (repairer: `:78`→`:77-78`, `:218-221`→`:217-221` mechanical cite-precision tightening; closes c030 Finding B). **Cycle-031 — `sparse_triangular_solve` slice-reduction audit DEFERRED**: verdict DEFER (slice retained-by-design as canonical-instance for 3 concept pages: `book/src/concepts/sparse-triangular-solve.md`, `tracts-of-source-vs-targets-of-burn.md`, `negative-result.md`); single reciprocal cross-link added from slice `:3-7` reduction-status header to the c029 L1>L0 obstruction theme `:273-308`. Phase-1 removals stay 9/10 (`polynomial_recurrence_step` is the precedent for slice-retained-by-design — the same pattern). Meta-phase batch-9 candidate `negative-result-slice-canonical-instance-blocks-reduction` filed for adjudication.
- [~] **Diagonal-preconditioner apply** (extract diagonal + reciprocal + elementwise multiply). Reused by: Jacobi, Chebyshev (already uses it), block-Jacobi, polynomial preconditioners. Concept overlap: `extract_diagonal`, `reciprocal`, `elementwise_product`. Lifting consolidates the pattern across smoothers. **STARTED cycle-032** by the firm `jacobi-smoother` L1 leaf — the FIRST diagonal-preconditioner-apply primitive (Chebyshev consumes the diagonal but expresses it via its polynomial-recurrence body; `jacobi-smoother` is the standalone elementwise form `y = (ω·D⁻¹) ⊙ x`). **Cycle-035 closing — polynomial-smoother L2 candidacy BLOCKED/RETIRED**: the `richardson` L1 primitive (the missing third sibling for the `jacobi`+`chebyshev`+`richardson` 3-pattern) is NOT exposed by Palace — only Jacobi/Chebyshev/DistRelaxationSmoother are exposed as standalone smoothers. The L2 `polynomial-smoother` combinator candidacy is therefore BLOCKED (unblock requires Palace to expose Richardson upstream, out of project scope per CLAUDE.md §Scope unimplemented-Palace-components policy). The diagonal-preconditioner-apply L1 + L1>L0 cohort is mature; no further forward work this batch. **Cycle-033 batch-9 closing — shared-vocabulary chain firm**: `reciprocal` (firm L1; `book/src/L1/reciprocal.md` — elementwise `1/x[i]`; complex `1/(a+bi) = (a-bi)/|a+bi|²`; real overload via upstream MFEM `using Vector = mfem::Vector` alias) + `elementwise_product` (firm L1; `book/src/L1/elementwise_product.md` — Hadamard pointwise product `a ⊙ b`; complex straight + complex conjugate-first-operand variant axis from `MultHermitianTranspose`) landed firm sibling-pair, completing the `assemble_diagonal → reciprocal → elementwise_product` chain that `assemble-diagonal:73` + `jacobi-smoother:289-297` previously forward-referenced. L1>L0 theme `jacobi-smoother-mutation-rotation` (firm; cycle-033 D1) ties the apply chain to L0. **Cycle-034 batch-10 OPENING — composite L1>L0 leaf theme firm**: `reciprocal-elementwise-product-mutation-rotation` (firm L1>L0; `book/src/L1-L0/reciprocal-elementwise-product-mutation-rotation.md` — composite thin-theme co-housing two sub-patterns: A `reciprocal` self-overwrite `*this = reciprocal(*this)` via the complex closed-form `s = 1/|z|²; XR *= s; XI *= -s` kernel + B `elementwise_product` output-arg `y = elementwise_product(a, b)` via the canonical `BaseDiagonalOperator::Mult` / `MultHermitianTranspose` operator-class realization with conjugation sub-axis at `palace/linalg/operator.cpp:545-568`; 757 lines on disk, 40 citations clean). The diagonal-preconditioner-apply L1>L0 chain now lowers BOTH its consumer (`jacobi-smoother-mutation-rotation`) AND its supporting leaves (`reciprocal-elementwise-product-mutation-rotation`) end-to-end. The lower-shared-vocabulary-priority directive (CLAUDE.md §"Lower-level shared vocabulary takes priority") is being actively discharged by this cohort: jacobi-smoother (L1 op c032) + jacobi-smoother-mutation-rotation (L1>L0 theme c033) + 2 supporting L1 primitives (reciprocal + elementwise_product c033) + reciprocal-elementwise-product-mutation-rotation (composite L1>L0 theme c034). Consolidation across smoothers under an L2 `polynomial-smoother` combinator awaits a third firm sibling (cycle-035+ candidate from `jacobi`+`chebyshev`+`richardson`).
- [ ] **Residual update** (`r ← b - A·x` or accumulator form). Reused by: every iterative solver (CG, GMRES, MINRES, BiCGStab, Chebyshev, multigrid). Already implicit; making it a named intermediate clarifies the residual-management invariant across solvers.
- [ ] **Restart machinery** (state save / reset / re-initialize for restarted Krylov). Reused by: restarted GMRES, restarted Arnoldi, multigrid level-recurrence. Concept overlap: state-stratification, solve-monad. Currently embedded.

**Impact ranking heuristic** (used by Planner per `prompts/planner.md` forward-frontier criterion). For each candidate, score:

```
impact_score(slice) = |concepts(slice)|
                    × |slices_that_reuse(concepts(slice))|
                    × (1 / cycles_to_extract_estimate)
                    × foundation_solidity(item)
```

The intermediate-tier candidates above have impact scores estimated as: Arnoldi step ≫ Plane-rotation stream ≈ Polynomial-recurrence step > Sparse triangular solve > Diagonal-preconditioner apply > Residual update > Restart machinery.

**`foundation_solidity(item)` — uniform-pull-up / foundation-first factor (user directive 2026-05-31, post-cycle-040).** A multiplicative ranking weight that biases the plan toward a **uniform L0→L4 climb on solid foundations** (CLAUDE.md §Methodology invariants "Uniform pull-up L0→L4; foundation-solidity is a ranking weight"). **Soft weight, not a hard gate** — exceptions allowed with stated rationale.
- `> 1` (**boost**) when the item *completes a foundation gap*: fills an **absent L_n parent** under an existing L_{n+1} entry, or authors a **missing L_{n+1}>L_n rotation theme**. These advance the front toward rectangular.
- `< 1` (**discount**) when the item *adds L_{n+1} width whose L_n support is absent or non-firm* — racing a layer ahead of its floor.
- `= 1` otherwise (the L_n floor + L_{n+1}>L_n rotation beneath the item are already firm).
Triggering state as of 2026-05-31: L3 has 18 entries but only 5 rest on a same-named L2 parent and only 2 of 18 carry an L3>L2 rotation theme. The two highest-`foundation_solidity` items are therefore `l2-floor-under-l3-leaf-cohort` (RENAMED batch-12 meta-phase from `l2-floor-under-l3-blas1-cohort` — the cohort spans non-BLAS-1 operator-to-data + constructed-operator gate members; build the 13 missing L2 entries + L2>L1 identity themes) and `l3-l2-rotation-theme-coverage-gap` (author the 16 missing L3>L2 themes) — both in `scaffolding/priorities.md` §High fan-out. Further L3 (B) substantive width (`chebyshev-smoother`/`apply_nonlinear_pencil`) carries `foundation_solidity < 1` until its L2 floor exists and so is demoted below the two foundation items. **Cycle-041 progress (FIRST foundation-first build cycle under this directive): the L2 floor under the 3 most-reused BLAS-1 leaves landed firm — `dot`/`nrm2`/`scal` (`book/src/L2/{dot,nrm2,scal}.md`) + their L2>L1 identity themes (`dot-leaf-identity`/`nrm2-fold-specialization`/`scal-fold-specialization`) + their L3>L2 body-identity themes (`dot-body-identity`/`nrm2-body-identity`/`scal-body-identity`). `l2-floor-under-l3-blas1-cohort` is now 3-of-13 (remaining 10: `assemble-diagonal`/`jacobi-smoother`/`divfree-projector`/`elementwise_product`/`reciprocal`/`normalize`/`chebyshev` + the `axpy`/`axpby`/`axpbypcz` arity-family). `l3-l2-rotation-theme-coverage-gap` advanced 2-of-18 → 5-of-18 (cycle-042: → 10-of-18). LOAD-BEARING CARRY (RESOLVED batch-12 meta-phase): the leaf-vs-fold design fork (`dot-l2-leaf-floor-vs-fold-only-design`) — D1 built same-named L2 leaf floors, D2 argued fold-only; **the batch-12 meta-phase (post-cycle-042) RATIFIED the (b) leaf-floor reading cohort-wide** (each firm L3 leaf gets a same-named L2 floor deferring fusion to the fold-parent; `nrm2` carved out as a consumer-not-member; theme slugs `-leaf-identity`/`-body-identity`), unblocking the held `axpy`-family floors. Cohort 8-of-13 after cycle-042; directive renamed `l2-floor-under-l3-leaf-cohort`.** **Cycle-043 progress (the cohort-completing slice): the held `axpy`-family L2 floors landed firm now that the batch-12 meta-phase ratified the (b) leaf-floor reading — `axpy`/`axpby`/`axpbypcz` (`book/src/L2/{axpy,axpby,axpbypcz}.md`, arity-2/2/3 members of `linear_combination`, cited-NOT-merged) + `normalize` (`book/src/L2/normalize.md`, the FUSED-COMPOSITE-with-no-fold-parent sub-shape: `normalize(x) = (nrm2(x), scal(1/nrm2(x), x))`, consumes `nrm2`+`scal`, fork-INDEPENDENT) + their 8 thin-identity themes (`book/src/L2-L1/{axpy,axpby,axpbypcz,normalize}-leaf-identity.md` + `book/src/L3-L2/{axpy,axpby,axpbypcz,normalize}-body-identity.md`). The leaf-vs-fold fork is now RATIFIED IN-ARTIFACT (the §"Design fork" bullets in all three indexes flipped provisional→RATIFIED, keep leaf-floor (b)). The 4 stale c042-cohort firm L3 entries (`reciprocal`/`assemble-diagonal`/`jacobi-smoother`/`divfree-projector`) were re-anchored L3>L1 → L3>L2>L1 (D1 consolidated lifter sweep) + 3 theme slugs normalized (`nrm2`/`scal`-`fold-specialization`→`-leaf-identity`, `elementwise_product-body-identity`→`elementwise-product-body-identity`). `l2-floor-under-l3-leaf-cohort` is now effectively COMPLETE: the axpy-family (3) + normalize landed this cycle; `chebyshev` was already floored via the pre-existing `chebyshev-iteration` L2 entry (a non-same-named slug) — so the cohort is 12-of-13 with the 13th already present, a count-reconciliation routed to the batch-13 meta-phase (OQ `chebyshev-floor-cohort-count-reconciliation`). `l3-l2-rotation-theme-coverage-gap` advanced 10-of-18 → 14-of-18.** **Cycle-044 progress (FIRST substantive (non-identity) L3>L2 rotation): the held leaf-cohort's stale L3 entries (`axpy`/`axpby`/`axpbypcz`/`normalize`) were re-anchored L3>L1 → L3>L2>L1 (D1 consolidated lifter sweep, closing OQs `l3-{axpy,axpby,axpbypcz,normalize}-lowers-to-staleness-after-l2-floor`) + the directive-slug + index-citation drift swept (zero `l2-floor-under-l3-blas1-cohort` remain in `book/`); the FIRST substantive partial-obstruction L3>L2 rotation landed firm — `orthogonalize-variant-split` (`book/src/L3-L2/orthogonalize-variant-split.md`, variant-conditional MGS-erasure: the MGS `jloop` is a `sequential-obstruction` that does NOT lift while the CGS/CGS2 batched arms collapse cleanly; `orthogonalize` L3 stays `partial-obstruction`, audited via a 24-row `verified_against:` block, status unchanged); and the `chebyshev-smoother` L3 (B)-candidate was CLOSED as SUBSUMED by the firm L3 `chebyshev` (NO-LAND; `apply_nonlinear_pencil` is now the only remaining (B)-candidate, itself routed to fold into a future eigsolve-variant pass). `l3-l2-rotation-theme-coverage-gap` advanced 14-of-18 → 15-of-18 (the FIRST substantive entry).**

The Planner uses this list (and the impact score) as input to the forward-frontier criterion (`prompts/planner.md`). Lifting any of these from "embedded in a root slice" to a standalone intermediate slice simultaneously:

1. Sharpens the root slice (clearer separation of concerns).
2. Unblocks downstream slices that reuse the intermediate.
3. Stresses concept vocabulary on cross-slice reuse (driving canonicalization).

## Methodology infrastructure

The agent loop itself. This is meta-development scope, not the spec deliverable.

### Roles

- [x] Planner, Explorer, Synthesizer, Critic (per-cycle).
- [x] Meta-Critic (meta-cycle driver).
- [x] README Builder (meta-cycle finaliser).

### Skills extracted

- [x] `classify-variant-axis` — variant-axis classification (4 absorption paths).
- [x] `verify-citation-range` — L0 citation cross-symbol-boundary check.
- [ ] *(more skills as recurring procedural patterns surface — meta-cycle proposes; user approves)*

### Concept library

- [x] 25 concepts on disk (cycle-005 added `scalar-promotion`), categorised in `book/src/concepts/index.md` (4 methodology incl. scalar-promotion, 4 algorithm, 10 primitive, 6 layer-pattern, 1 auxiliary), auto-maintained on every concept create. **Housekeeping cycle-006**: duplicate rows fixed by cycle-006 wave-1 same-layer-cross-cutter (`complex-from-real-lift` pure copy-paste deleted; `solver-as-operator` divergent-kind misclassification resolved by keeping `layer-pattern` row).
- [ ] Coverage gaps: complex-arithmetic primitives, FFT-based solves, time-stepping primitives, FE-assembly primitives.
- [x] **Cycle-067**: NEW `book/src/concepts/black-box-vs-accelerated-kernels.md` (classification-vocabulary page seeding the 2026-06-02 directive — three-way black-box kernel / kept-named-abstraction / accelerated-kernel disposition; `SUMMARY.md` concepts-list alpha-inserted between `axpy` and `dot`). **Cycle-071 (directive-3 reorg D6): the `# Concepts` list is now globally alpha-sorted** (44 content slugs C-locale sorted by file slug; the 2 nav rows `Index`/`Dependency map` kept at top; flat shared-library reference list, NOT kind-grouped — over-structuring guard). Closes OQ `concepts-list-global-alpha-resort-vs-local-cluster-insert`. (A pre-existing `concepts/index.md` table-vs-SUMMARY 2-missing-row membership drift was OQ-promoted this cycle — `concepts-index-table-vs-summary-membership-drift-two-missing-rows` — routed to batch-22 meta / cycle-072 hygiene; NOT a dropped concept, the index table is the lagging derived surface.) **Cycle-101 (layer-intro-author D2): the concept library's two infra files REFRESHED to current artifact state** — `book/src/concepts/index.md` + `book/src/concepts/dependency-map.md` had their pre-redirect orchestrator + slice-era framing STRIPPED (the Synthesizer/Meta-Critic/Planner + `prompts/*` provenance, the `../spec/slices/X.md` format-example links, the orchestrator-grep recipe) and re-derived to the 14-agent Claude Code pipeline + layered L4→L0 + lowering Parts + feature spine + concept-pages-as-data-shape framing. The dep-map's stale `## Intermediate-tier algorithms (planned)` slice-slug Mermaid block (~115 dead edges) was REPLACED with two re-derived sub-graphs anchored to the 51 on-disk concept pages (a dangling `reciprocal` node + a duplicate `## Methodology concepts` heading repaired). Concept-page count verified 51 on disk (matches refreshed prose + §Index table). The opportunistic `depends-on`/`reference` edge-typing was LIGHT (in-prose-consistent) — NOT the meta-phase-owned full graded-stack edge-typing campaign (priorities item 0), reconcile when that lands. Resolved the two refresh OQs (`concepts-index-and-depmap-orchestrator-era-framing-refresh`, `dependency-map-cg-precond-stale-mermaid-edges-RESCOPE-CORRECTION`).

### Book structure — by-kind sub-chapter grouping + global alpha re-sort (directive-3)

- [x] **Cycle-071 (the directive-3 mdBook STRUCTURAL-REORG wave — COMPLETE).** The one-time by-kind sub-chapter grouping + global alpha re-sort the batch-21 meta-phase sequenced as its own dedicated structural wave landed across **all** layer Parts in 6 disjoint-Part `layer-intro-author` dispatches (D1–D6), pure structural — **NO operator/theme/concept count changes, no `## Status` flips**. Every layer Part's `SUMMARY.md` chapters are now nested into **by-kind sub-chapter groupings** (each with an authored group-intro page) and the dep-map / API-list / theme-list tables are **alpha-sorted within each kind grouping**: **L4** 3 groupings (iteration / data-algebra / outer-driver combinators) + L4>L3 flat-alpha; **L3** 5 groupings (blas1 / elementwise / operator-apply / smoother / solver-caps) + L3>L2 flat-alpha; **L2** 5 groupings (step-kernels / folds / fold-family-stubs / named-compositions / elementwise-gate-floors) + L2>L1 flat-alpha; **L1** 7 groupings (blas1-elementwise / operator-application / constructed-operator-gates / krylov-least-squares / nep-interior / fe-assembly / fe-space) + L1>L0 nested into 3 theme-kinds (mutation-rotation / construction-rotation / obstruction); **L0** 3 source-area groupings (conventions / file-overviews / overload-sets-and-classes) + Phase-1 corpus flat-alpha; **Concepts** flat-alpha (D6). **26 new group-intro pages total** (3 L4 + 5 L3 + 5 L2 + 7 L1 + 3 L1>L0 + 3 L0). The 4 small/reference Parts (Meta-Reviews chronological, Methodology, **Feature surfaces** [within-column high→low level ordering deliberately NOT alphabetized], Design) were left as-is. Slug-set preservation verified per-Part by each per-report integrator (pre/post git diff; NO dropped chapters — explicitly confirmed for the heavy L1/L1>L0 Part: SUMMARY 36+37, dep-map 42+37 rows, all set-equal). `cargo make book` (mdbook 0.5.1 + linkcheck2 0.12.0) exit 0, all 26 intro pages render, zero dead links. **The alpha-insert convention (`integrator-per-report` / `integrator-finalize`) now operates over a fully-sorted base.** Role-spec codification of directive-3 (the by-kind-group authoring + alpha-insert conventions) is the **batch-22 meta-phase's** job after cycle-072 (restart-pending).

### Reader-facing book Methodology chapter

- [~] **Cycle-067**: NEW `book/src/methodology/goal-flow.md` (directive-4 v1 seed; reader-facing GOAL+FLOW chapter — a GOAL section integrating the book's goals + a FLOW section on how they are met). **NON-AUTHORITATIVE**: a synthesized mirror of the emergent goal (self-feedback + user-review window), synthesized FROM the authoritative sources (CLAUDE.md / METHODOLOGY-REDIRECT.md / memory / priorities), never a source itself; mandatory non-authoritative header present. `SUMMARY.md` row under `# Methodology` after `overview.md`. **Ownership transfers to the meta-phase post-seed** (standing per-batch refresh target; codify into `meta-phase.md` role-spec — restart-pending; OQs `methodology-goal-flow-chapter-ownership-transfers-to-meta-phase-post-seed` + `methodology-goal-flow-single-chapter-vs-split-goal-and-flow`).

### Semantic surface — calculus, rules & abstractions (USER DIRECTIVE 2026-06-06)

- [x] **Cycle-116 (semantic-consolidation campaign LEAD, WAVE-1/2 — COMPLETE).** The active semantic-management surface now has its own home: the former L4 calculus strawman `book/src/design/l4_calculus.md` was promoted out of "design strawman" status and **physically moved** to `book/src/semantics/index.md` (verbatim `git mv`, 513 lines), under a new top-level `# Semantic surface — calculus, rules & abstractions` Part placed AFTER the Feature Part, BEFORE `# L4` in `SUMMARY.md` (D1). The move carried a ~97-file cross-reference rewrite (`design/l4_calculus.md` → `../semantics/index.md`, both link targets and inline-code prose citations) + `design/index.md` reframed to a relative pointer. The 24-file **restatement-cohort relocation sweep** (Tier B 5 + Tier C 19) completed the named-shape-groups general-rule consolidation — functional-unit entries now USE+LINK the §1.2.1 surface rule rather than RE-STATE it (D2); plus a 4-file L4 bare-basename prose-ref cleanup. **Pure prose/link/move — reachability/rank-NEUTRAL** (no frontmatter touched); `cargo make book` exit 0; graded-stack linter HELD (`reachable=133`, `rank_violations=0`, `detritus=126`). OQ `named-shape-groups-general-rule-restatement-cohort-extent` is now FULLY SWEPT (Tier A+B+C) — ready-to-close (meta-phase owns closure). The surface's every-batch liveness/unification refresh is the meta-phase's standing duty (semantic analog of the reachability GC sweep).

### Integrator and orchestrator features

- [x] Channel set: file_creates, section_appends, file_edits, slice_index_updates, concept_writes (create/append-section), dependency_map_edges.
- [x] Verdict-downgrade with bookkeeping vs content classification.
- [x] Same-cycle create+edit merge.
- [x] Concept-existence loud-failure enforcement (meta-17).
- [x] Synthesizer streaming + max_tokens=24576.
- [ ] *(future)* Parallel-cycle integrator (BOOTSTRAP Phase 8).
- [ ] *(future)* Automated meta-cycle enactment (currently human-approved).

### Critic checks

- [x] 15 numbered checks. Stable through cycles 60+ with incremental additions per meta-review.

## Phase progression (per `BOOTSTRAP.md`)

- [x] **Phase 0** — workspace setup, `.env` plumbing, MCP registration.
- [x] **Phase 1** — Rust MCP codemap server (tree-sitter wrapper).
- [x] **Phase 2** — memory and schemas seed.
- [x] **Phase 3** — refinement plan + critic verdict schemas.
- [x] **Phase 4** — five agent prompts.
- [x] **Phase 5** — Python orchestrator (raw Anthropic SDK).
- [x] **Phase 6** — GMRES smoke test (DONE at meta-10).
- [~] **Phase 6+** — continuation: drive other slices toward L4, fill solver-pipeline coverage (currently here).
- [ ] **Phase 7** — execution grounding (build and run target; verify spec matches behaviour).
- [ ] **Phase 8** — parallel cycles (multiple Explorers, integration-plan queue).
- [ ] **Phase 9+** — UI, embedding store, alternative front-ends.

## How proportional coverage is computed

For the README's *Relative Progress* section:

- **Solver pipelines:** count of pipelines fully at L4 / total. Currently 0 / 5 (no full pipeline; Krylov-only footprint is shared).
- **Krylov solvers:** 2 / 4 at L4 (CG, GMRES; pending MINRES, BiCGStab).
- **Smoothers:** 1 / 6 at L4 (Chebyshev; pending Jacobi, SGS, ILU, AMS, multigrid).
- **Projections:** 0 / 2 at L4 (divfree at L3 in flight; curl-curl not started).
- **FE assembly:** 0 / 4 components.
- **Methodology infrastructure:** roles 3/3 done; skills extracted 2 / N (N grows on demand); 24 concepts on disk.
- **Phase progression:** 7 / 10+ phases done; Phase 6+ in flight.

The denominators are rough by design. The roadmap is reviewed and adjusted during each meta-cycle; if a category's denominator grows (new components surface as in-scope), the README's coverage report reflects it.

## Layered-spec progress (added cycle-002; updated cycle-013)

The 6-phase agent loop now builds the L4→L0 layered stack. Per-layer dep-map populations as of cycle-012 (**third/final primary cycle of meta-batch-2; cycle-012 closes meta-batch-2 and triggers the next meta-phase**):

- **L0** — **reference-note overlay bundle 1 landed cycle-005** (priority #10): 6 reference chapters (Conventions: `output-arg-vs-receiver`, `transparent-vs-load-bearing-tricks`, `mfem-vector-types`, `linalg-free-functions`; File overviews: `linalg-vector-file`, `ksp-factory-file`). **Bundle 2 landed cycle-006**: 2 new chapters (`apply-linop-overload-set`, `kspsolver-base-class`) plus new "Overload sets and class interfaces" L0 grouping. **Bundle 3 landed cycle-007**: 3 new chapters (`mfem-wrapper-solver`, `linalg-iterative-file`, `mutable-workspace-pattern`). **Bundle 4 landed cycle-008**: 3 new chapters (`eigensolver-wrapper` — Overload-sets-and-class-interfaces; `par-types-single-rank-reading` — Conventions; `linalg-operator-file` — File overviews). **Bundle 5 landed cycle-009** (2 chapters; third bundle-5 candidate `tests-as-semantic-supplement` deferred): `mpi-globalsum-and-collectives` + `preconditioner-classes-overview`. **Bundle 6 candidate #1 landed cycle-011**: `linalg-solver-file` (File overviews — `palace/linalg/solver.{hpp,cpp}`; `Solver<OperType>` type-axis-root chapter — corrects dispatch-prompt framing of "direct-solver-only base" with all eight concrete subclass families enumerated; closes cycle-009 OQ `l0-bundle-6-candidates` #1 to partially-answered). **Bundle 6 candidate #3 landed cycle-013**: `linalg-orthog-file` (File overviews — `palace/linalg/orthog.hpp`, header-only 93 lines; the vector-against-basis Gram-Schmidt family `OrthogonalizeColumnMGS`/`OrthogonalizeColumnCGS` + `IdentityInnerProduct`; L0 anchor for the firm `L1/orthogonalize` MGS|CGS|CGS2 axis; authored via a discovery→authoring scope stretch, safety-gated by the integrator). **18 chapters total** (bundle-6 item #2 `linalg-rap-file` is the next-ranked candidate per OQ `l0-bundle-6-candidates`). **Bundle-6 candidate #2 landed cycle-014**: `linalg-rap-file` (File overviews — `palace/linalg/rap.{hpp,cpp}`; the R·A·P (Galerkin) parallel-operator family: `ParOperator` real-valued + `ComplexParOperator` real/imag-split into two owned `ParOperator`s + the `BuildParSumOperator` weighted-summation family; matrix-free prolongate-apply-restrict sandwich vs assembled `HypreParMatrix` triple-product as an algebraically-equivalent performance dual; 7 cited anchor surfaces; the dispatch-prompt "Restrictive Additive Schwarz" gloss was corrected). **19 chapters total** post-cycle-014.
- **L1** — **10 firm operators** (`axpy` pilot-1, `dot` cycle-002, `nrm2` cycle-003, `axpby` cycle-003, `scal` cycle-004, `apply_linop` cycle-004, `axpbypcz` cycle-004, `ksp_solve` cycle-007 — **first L1 op with structured opaque primary argument**, introduces Constructed-operator absorption motif; **`orthogonalize` cycle-012** — Gram-Schmidt orthogonalisation, `dot`+`axpy` deps only, MGS/CGS/CGS2 variant axis + `dot_op` inner-product hook, all-three-levels absorption under residual-axis disclosure, Householder scoped out, closes the HIGH-priority cycle-010 blocking OQ; **`chebyshev-smoother` cycle-012** — first L1 op that is a fixed-degree polynomial *action* rather than a solve-to-convergence, ratified KEEP-FIRM (closed-form coefficients + live multigrid coverage, distinct from `eigsolve`'s literature-inferred rough-in), `apply_linop` direct + opaque `spectrum_estimate` deps) + **3 rough-in (test-coverage-bounded / lower-layer-shared-vocabulary) operators** (`eigsolve` cycle-009 — **second constructed-operator gate at L1**, composing against `ksp_solve`; `matrix-weighted-norm` cycle-010 — L1 energy-norm primitive `α = ‖x‖_B = √(xᴴ B x)` for SPD `B`, priority #17 first target; `bilinear-form` cycle-010 — `bilinear_form(x, M, y) = xᴴ M y` for arbitrary linear `M`, priority #17 second target, first cycle-010 citation-validity FAIL repaired cleanly) + **6 rough-in obstruction operators** (`lanczos_step`, `three_term_recurrence_update`, `givens_apply_with_residual_min` from `minres-iteration` theme; `bicgstab_step`, `omega_update`, `stabilisation_update` from `bicgstab-iteration` theme — **decision NOT to promote any landed cycle-005** per `scaffolding/decisions/2026-05-27-krylov-step-speculative-l1-promotion.md`). **Cycle-006 retroactive-thinned 4 L1 entries** (scalar-promotion sweep). **Cycle-007 retroactive-thinned all 7 pre-`ksp_solve` firm L1 entries** (~55% net Context shrink). **Cycle-008 L1/index refresh** added the closing-the-loop pointer at motif 4 + ksp_solve Status-cell L1>L0 link. **Cycle-009 L1/index refresh** added the new "Rough-in (test-coverage-bounded)" subsection (cohort-purity preserving — Firm bullet list unchanged) + eigsolve dep-map row + cycle-009 Working Notes bullet. **Cycle-010 L1 cohort growth** added matrix-weighted-norm + bilinear-form rough-in rows; cycle-008 OQ `matrix-weighted-norm-and-bilinear-form-l1-rough-ins` both halves landed (status `partially-answered`; SpectralNorm + L1>L0 themes residuals tracked). **Cycle-012 L1 firm cohort 8 → 10** (orthogonalize + chebyshev-smoother; Firm header bumped `(8)` → `(10)`). **L1 cohort frontmatter divergence still noted** — matrix-weighted-norm no frontmatter; bilinear-form 8-field frontmatter; future-normalization candidate carried into meta-phase batch-2.
- **L1>L0** lowering — **7 themes (5 firm/rough-in + 2 obstruction)**: `axpby-mutation-rotation` (cycle-002); `minres-iteration` (cycle-004, **obstruction**); `bicgstab-iteration` (cycle-004, **obstruction**); `apply-linop-mutation-rotation` (cycle-005, 5 sub-patterns A-E); `axpbypcz-mutation-rotation` (cycle-005, 4 sub-patterns + first mixed-justification sub-rule); **`ksp-solve-mutation-rotation` (cycle-008 wave-1 abstractor)** — first L1>L0 theme for a constructed-operator-absorption operator; 4 sub-patterns × {CG, GMRES, FGMRES} outer-loop variants. **`eigsolve-mutation-rotation` (cycle-011 wave-2 abstractor)** — second L1>L0 mutation-rotation theme for a structured opaque primary argument; 4-sub-pattern decomposition (A setup / B inner-solve mutation-rotation / C result-status flow / D teardown); exhaustively cites 10 `opInv->Mult` callsites across ARPACK (4) + NLEPS (1) + SLEPc shell (5); **first firm-structural-but-partly-constructive theme** in artifact (Sub-pattern B `LinearSolveFailed` materialisation partly-constructive pending upstream Palace refactor OR lowering-verifier audit). **Cycle-012 lowering-verifier audit: confirms-with-refinement** — appended a machine-readable `verified_against:` YAML record; **the Sub-pattern B partly-constructive → fully-firm promotion is UNBLOCKED but GATED to a cycle-013 abstractor** that must first apply the GetConverged-forwarder snippet fix (Edit 2) + Sub-pattern A function-name attribution fix (Edit 3); the `## Status` partly-constructive caveat was NOT dropped this cycle (OQ `eigsolve-getconverged-forwarder-fix-and-gated-promotion`). **L1>L0 theme files grew across batches** — `divfree-projector-mutation-rotation` (firm, cycle-016); `nrm2-mutation-rotation` (stub→firm, cycle-019); **+3 cycle-020 BLAS-1+ closures**: `dot-mutation-rotation` (stub→`firm`; closes the cycle-019 `nrm2` sub-pattern-A forward-ref `nrm2 = √∘abs∘dot`; two variant axes), `scal-mutation-rotation` (stub→`firm`; element-type + scalar-promotion axes), `assemble-diagonal-mutation-rotation` (NEW firm file — operator-to-data diagonal extraction; matrix-free high-order-Nedelec approximate-diagonal caveat is a positively-anchored load-bearing non-law so `firm` NOT `partly-constructive`; `reciprocal`/`elementwise_product` forward-refs correctly plain-text). **L1>L0 now 15 theme files** (cycle-020). **BLAS-1 L1>L0 lowering floor FULLY CLOSED cycle-022 (8/8)** — `axpby-mutation-rotation` firmed cycle-021 (floor 7/8), then `axpbypcz-mutation-rotation` firmed cycle-022 (rough-in→firm; enacted the 3 cycle-021 callsite-classification corrections `nleps.cpp:343-344` D→A / `romoperator.cpp:188-189` D→A / `slepc.cpp:1986` γ≠0→γ=0 + correction-6 range fix) — closing the floor: `dot`/`scal`/`nrm2`/`assemble-diagonal`/`axpby`/`axpbypcz` mutation-rotation themes ALL firm. The plan item `blas1-l1-l0-lowering-theme-gap` is now **CLOSED**. L1>L0 still 16 theme files (axpbypcz was already a rough-in file, firm-flipped not added). **Cycle-028 lowering-verifier audit: `normalize-mutation-rotation` (firm, landed c027 D1) UPHELD firm** — additive `verified_against:` evidence-backfill (16 rows, 14 supports / 1 partially-supports / 1 does-not-support) + a `:811`→`:810-811` second-GMRES-path citation-range parity fix at three occurrences. The one `does-not-support` row (F1: a defined-but-uncalled fused `Normalize(comm,x,B,Bx)` at `palace/linalg/operator.hpp:377-384` contradicts the theme's "no fused B-Normalize" prose) is recorded inline but the prose correction is ROUTED to a follow-up abstractor OQ `normalize_B-note-says-no-fused-B-Normalize-but-uncalled-fused-operator-exists` (substantive, exceeds the integrator mechanical bar) — firm core UNAFFECTED. **Cycle-057 (lifter D2): `fe-operator-assemble-mutation-rotation` PROMOTED rough-in → FIRM** (the FE-assembly mutation-rotation; all 3 LHS operators `fe_assemble`/`eliminate_rhs`/`eliminate_essential_bc` now firm) — 2 BC-elimination-leg re-anchors to the firm `eliminate_essential_bc`/`eliminate_rhs` live links + 3 citation drift fixes (`:225-253`→`:225-252` / `:236`→`:238` / new `:247`) + a stale-back-ref fix + libCEED-boundary OQ re-anchor to the settled obstruction annotation; the **index-cell anti-drift guard** applied (theme `## Status` + frontmatter + `L1-L0/index.md` row all flipped firm in one report, per the cycle-056 D2 recommendation). **The FE-assembly sub-spine L1>L0 edge is now COMPLETE** (3 firm L1 + firm L1>L0 rotation + libCEED obstruction boundary). **Cycle-057 (abstractor D1): the `solve_family` L3 lowering-depth WARRANT = NO-ENTRY** — no `L3/solve_family` entry is warranted (the L4>L3 `solve-family-map-dissolution` dissolution theme fully expresses the family map; a degenerate L3 mirror would be the redirect-flagged anti-mirror smell); 5 forward-reference corrections re-pointed dangling promises of a confirmed-absent `L3/solve_family` slug onto the dissolution theme as the authoritative L3-form home (no count change, no new file — the WARRANT-FIRST anti-mirror discipline working under the 2026-06-01 redirect).
- **`concepts/dot.md`** — rewritten cycle-004.
- **`concepts/scalar-promotion.md`** — landed cycle-005.
- **L2** — **3 firm operators + 1 rough-in** (`krylov-step`, cycle-005; **`chebyshev-iteration` cycle-012**; **`linear_combination` PROMOTED rough-in→firm cycle-018** — the variadic BLAS-1 scalar-weighted-sum fold `[(Scalar, Tensor[N])] -> Tensor[N]` ≡ `foldl (\acc (a,t) -> acc + scal a t) (zeros N) pairs`, unifying `scal`/`axpy`/`axpby`/`axpbypcz` along the **arity** axis [complementary to the `scalar-promotion` element-type-axis unification]; harvested cycle-018 [rough-in dep-map row landed cycle-017 by combinator-miner]: full chapter [7 holding algebraic laws incl. concatenation-homomorphism = the unifying law + zero-coefficient term-drop = exact L0 `γ==0` branch `vector.cpp:749-751`; 3 explicit non-laws; 2 variant axes (output-aliasing ⊥ arity; element-type real|complex w/ scalar-promotion); sibling-fold `dot`-not-subsumed §; 9 self-verified L0 ranges; `firm` on direct source-transcription per chebyshev-iteration firm-without-dedicated-test precedent — the `test-vector.cpp` "Vector Sum" tests exercise `linalg::Sum`, a DIFFERENT reduce-to-scalar fold, correctly NOT cited]; the constructive **prong (b)** of the human-raised OQ `blas1-variadic-linear-combination-fold-unification` is now FULLY ENACTED [rough-in cycle-017 → firm cycle-018]; **prong (a)** [combinator-miner spec parametric/variadic-family detection mode] remains a batch-4 meta-phase item; OQ `linear-combination-harvester-formalization` RESOLVED. The **1 rough-in is now `inner_product` cycle-018** — combinator-miner fold-sibling candidate `(Tensor[N], Tensor[N]) -> Scalar` along the **conjugation-convention** axis [NOT arity]; M-weighted member `inner_product_M(x,M,y) = xᴴ M y`; fuses up from `dot`/`tdot`/`bilinear-form`; ≥3-instance bar met; dep-map row only, forward-ref to `./inner_product.md` kept PLAIN-TEXT [no dead link]; OQ `inner-product-fold-sibling-candidate` answered, harvester-formalization + exact conjugation-convention-pinning follow-ups remain per OQ `inner-product-harvester-formalization-and-conjugation-pinning`) — `chebyshev-iteration` cycle-012 detail: the explicit degree-`order` three-term polynomial recurrence unfolding the L1 `chebyshev-smoother` closed-form action into base primitives `apply_linop`/`axpby`/`scal`/`elementwise_product` with the HPC element-fused kernels de-fused; the concrete L2 entry behind `L2/krylov-step` variant-axis 3; ratified KEEP-FIRM in lockstep with the L1 entry; first L2 cohort growth since cycle-005 — addresses priority #17 lower-layer-shared-vocabulary signal). **L2 firm 9 → 12 cycle-041** (the lead "3 firm operators" is stale — the `book/src/L2/index.md` dep-map is authoritative at 12 firm + 1 partly-constructive): +3 thin-identity-in-form BLAS-1 floor leaves `dot` / `nrm2` / `scal` (the FIRST 3 of the 13-entry `l2-floor-under-l3-blas1-cohort`, building the L2 floor under the same-named firm L3 entries). `dot` is a standalone leaf floor; `nrm2 = √∘abs∘inner_product` at `y=x` (consumer-of-fold framing, `std::abs` load-bearing guard preserved); `scal` is the arity-1 member of `linear_combination` (cited-NOT-merged, the load-bearing fold-cohort do-NOT-merge boundary honored). D7 (layer-intro-author, sole count-owner) added the §Semantics third motif "identity-in-form BLAS-1 floors" + §Vocabulary-cohort floor sub-group. Cohort remaining 10: `assemble-diagonal`/`jacobi-smoother`/`divfree-projector`/`elementwise_product`/`reciprocal`/`normalize`/`chebyshev` + the `axpy`/`axpby`/`axpbypcz` arity-family (maps onto the `linear_combination` fold — the natural cycle-042 foundation slice). **LOAD-BEARING DESIGN FORK for the batch-12 meta-phase**: `dot-l2-leaf-floor-vs-fold-only-design` — wave-1 D1 built the same-named L2 leaf floors, D2 argued fold-only; all 6 cycle-041 lowering entries presuppose the (b) leaf-floor reading; the meta-phase MUST adjudicate before the whole 13-entry cohort (+ the `axpy`/`axpby`/`axpbypcz` framing) is design-final. **L2 firm 12 → 17 cycle-042** (the `book/src/L2/index.md` dep-map is authoritative at 17 firm + 1 partly-constructive): +5 fork-INDEPENDENT standalone-floor members — `reciprocal` (D2; fold-parent-free elementwise multiplicative-inverse leaf), `elementwise_product` (D3; standalone Hadamard binary field op, NO fold-parent), `assemble-diagonal` (D4; operator-to-data diagonal-introspection primitive, sibling-of-`apply_linop`, the load-bearing matrix-free high-order-Nedelec approximate-diagonal non-law preserved through the floor → `firm` not `partly-constructive`), `jacobi-smoother` (D5; thinnest constructed-operator gate, body = one elementwise product, fusion-rotation NEGATIVE), `divfree-projector` (D6; MODERATE floor not thin — the ONE genuine fusion-rotation = step-4 `Grad->AddMult` de-fused to `apply_linop ▷ axpy`, inner-solve `sequential-obstruction` carried BY REFERENCE through firm `ksp_solve`). All 5 are fork-INDEPENDENT (NO fold-parent), so design-final regardless of the `dot-l2-leaf-floor-vs-fold-only-design` adjudication — the floor cohort is heterogeneous (fold-leaf / fold-consumer / fold-free elementwise / constructed-operator gate). D11 (layer-intro-author, sole count-owner) recast §Semantics-overlay to two-motifs-by-fold-relationship + the §Vocabulary-cohort standalone-floor sub-list. **The `l2-floor-under-l3-blas1-cohort` is now 8-of-13** (c041: dot/nrm2/scal; c042: reciprocal/elementwise_product/assemble-diagonal/jacobi-smoother/divfree-projector); remaining 5: the `axpy`/`axpby`/`axpbypcz` arity-family (HELD pending the fork) + `chebyshev` + `normalize`. The directive-name `l2-floor-under-l3-blas1-cohort` now spans non-BLAS-1 members (operator-to-data + constructed-operator gates) — rename candidate flagged for the batch-12 meta-phase. **L2 firm 17 → 21 cycle-043** (the `book/src/L2/index.md` dep-map is authoritative at 21 firm + 1 partly-constructive `deflate`): +4 floors completing the cohort — the held `axpy`-family unblocked by the batch-12 meta-phase fork-ratification (`axpy` arity-2 / `axpby` arity-2 / `axpbypcz` arity-3, all `linear_combination` members cited-NOT-merged, fusion deferred to the fold-parent) + `normalize` (the FUSED-COMPOSITE-with-no-fold-parent sub-shape — a third thin-identity sub-shape distinct from c041 fold-parented-leaf + c042 standalone-leaf/-gate; consumes `nrm2`+`scal` floors, fork-INDEPENDENT, returned-norm load-bearing). The leaf-vs-fold fork (`dot-l2-leaf-floor-vs-fold-only-design`) is now RATIFIED IN-ARTIFACT (keep leaf-floor (b); §"Design fork" bullets in all three indexes flipped to RATIFIED). The cohort is effectively COMPLETE (12-of-13 same-named + the 13th `chebyshev` already floored via the pre-existing `chebyshev-iteration` entry — count-reconciliation OQ `chebyshev-floor-cohort-count-reconciliation` routed to batch-13 meta-phase). D2 (layer-intro-author, sole count-owner) recast the §Vocabulary-cohort heading cohort-neutral (`l2-floor-under-l3-leaf-cohort`) + reconciled the firm-count tally.
- **L3>L2** — **1 firm theme** (`krylov-step-body-identity`, cycle-007 abstractor at `firm-rough-in`; **promoted cycle-009 via status-inheritance** to plain `firm` after upstream L4>L3 theme firmed cycle-008 — first across-cycle status-inheritance promotion in the artifact; closes the cycle-007 firm-rough-in pattern's first instantiation). **L3>L2 firm 2 → 5 cycle-041** (the lead "1 firm theme" is stale — `ksp-solve-outer-driver` brought it to 2 earlier; cycle-041 adds the 3 BLAS-1-leaf body-identity themes `dot-body-identity` / `nrm2-body-identity` / `scal-body-identity` — thin identity-in-form L3>L2 edges co-landing with their L2 floors). This advances the `l3-l2-rotation-theme-coverage-gap` plan item **2-of-18 → 5-of-18**; the L3-L2/index §Vocabulary-cohort subsection was PROMOTED first-time at this index (crossed the ≥3-firm threshold). **The 3 new themes presuppose the (b) leaf-floor reading of the `dot-l2-leaf-floor-vs-fold-only-design` fork** — if the batch-12 meta-phase adopts fold-only, the BLAS-1-leaf L3>L2 themes re-home as non-adjacent in-line identity notes at `L3/{dot,nrm2,scal}`. **L3>L2 firm 5 → 10 cycle-042** — +5 thin/pure-identity body-identity themes co-landing with their L2 floors (`reciprocal-body-identity` D10, `elementwise_product-body-identity` D10, `assemble-diagonal-body-identity` D7, `jacobi-smoother-body-identity` D8, `divfree-projector-body-identity` D9; all L3-native-by-signature per `krylov-step-body-identity.md:97`, no wrapper + no fold-parent to rotate). This advances the `l3-l2-rotation-theme-coverage-gap` plan item **5-of-18 → 10-of-18**. All 5 are FORK-INDEPENDENT (no fold-parent) — design-final regardless of the leaf-vs-fold adjudication. NOTE: the firm `L3/{reciprocal,assemble-diagonal,jacobi-smoother,divfree-projector}` entries' "no L3-L2 theme / no interposed L2 entry" clauses now go stale on these landings (D3 reconciled `elementwise_product`'s L3 inline this cycle); routed to ONE consolidated cycle-043 lifter sweep (directive-named OQ `l3-divfree-projector-stale-no-interposed-l2-entry-lifter-reanchor`). **L3>L2 firm 10 → 14 cycle-043** — +4 thin/identity body-identity themes co-landing with their L2 floors (`axpy-body-identity` / `axpby-body-identity` / `axpbypcz-body-identity` — arity-fold-member leaf counterparts of `scal-body-identity`; `normalize-body-identity` — the fused-composite counterpart of `krylov-step-body-identity`, no fold-parent, no kernel fusion to unfold; all L3-native-by-signature per `krylov-step-body-identity.md:97`). This advances the `l3-l2-rotation-theme-coverage-gap` plan item **10-of-18 → 14-of-18**. The 4 c042-cohort stale-L3 reanchors were ENACTED this cycle (D1 consolidated lifter sweep re-anchored `reciprocal`/`assemble-diagonal`/`jacobi-smoother`/`divfree-projector` L3>L1 → L3>L2>L1, closing the `l3-divfree-projector-stale-no-interposed-l2-entry-lifter-reanchor` OQ; 3 theme slugs were normalized via `git mv` — `nrm2`/`scal`-`fold-specialization`→`-leaf-identity`, `elementwise_product-body-identity`→`elementwise-product-body-identity`). NOTE: the NEW c043 floors' OWN L3 entries (`axpy`/`axpby`/`axpbypcz`/`normalize`) carry the SAME "no interposed L2 entry / direct L3>L1 hop" staleness — routed to a cycle-044 consolidated L3-re-anchor sweep (OQs `l3-{axpy,axpby,axpbypcz,normalize}-lowers-to-staleness-after-l2-floor`). **L3>L2 firm 14 → 15 cycle-044 — the FIRST substantive (non-identity) rotation theme**: `orthogonalize-variant-split` (`book/src/L3-L2/orthogonalize-variant-split.md`, the SECOND substantive L3>L2 theme after `ksp-solve-outer-driver`, and the FIRST for a `partial-obstruction` operator) — a **variant-conditional MGS-erasure**: the MGS (`gs_orthog=MGS`) `jloop` is a witnessed `sequential-obstruction` (each projection reads the running-updated vector) that does NOT lift, while the CGS/CGS2 batched arms collapse cleanly into the L2 per-variant sequencing; the obstruction is gated on the `gs_orthog` variant axis. Co-landed: D4 (lowering-verifier) appended a 24-row `verified_against:` block to `L3/orthogonalize.md` (all `supports`, status stays `partial-obstruction`, NO flip) + re-pointed the L3 entry's §Dependencies / §"L3 vs L2 distinction" / `lowers_to:` frontmatter from the stale "no L3-L2 theme file (in-line per cycle-012)" note onto the live `orthogonalize-variant-split` link for the substantive loop-rotation half (the per-step `dot`+`axpy` body-identity stays the in-line non-adjacent note). D1 (lifter sweep) re-anchored the 4 c043 leaf-cohort L3 entries (`axpy`/`axpby`/`axpbypcz`/`normalize`) L3>L1 → L3>L2>L1 (closing the 4 staleness OQs) + re-pinned co-located audit-block citations + swept the directive-slug + index-citation drift (zero `l2-floor-under-l3-blas1-cohort` remain in `book/`). D2 (cross-cutter, OBSERVATION/no-book) CLOSED the `chebyshev-smoother` L3 (B)-candidate as SUBSUMED by the firm L3 `chebyshev` (NO-LAND), leaving `apply_nonlinear_pencil` as the only remaining (B)-candidate. This advances `l3-l2-rotation-theme-coverage-gap` **14-of-18 → 15-of-18**. The remaining 3 are the substantive `chebyshev` + `eigsolve` rotations (the batch-13/cycle-045 lead frontier). **Cycle-045 (FINAL of batch-13) landed BOTH substantive rotations — L3>L2 firm 15 → 17, `l3-l2-rotation-theme-coverage-gap` 15-of-18 → 17-of-18 ≈ COMPLETE** (the 18th is `apply_linop`, no-L2-by-design / `book/src/L3/apply_linop.md:146` — effectively 17-of-17-applicable, re-denomination deferred to the batch-13 meta-phase). D1 landed `book/src/L3-L2/eigsolve-opaque-eigen-iteration.md` (the **opaque-library** erasure-scope root — eigen-iteration is SLEPc/ARPACK-owned, body lifts but loop is library-owned) re-anchoring `L3/eigsolve.md`; D2 landed `book/src/L3-L2/chebyshev-nested-recurrence.md` (the **unconditional-nested-double-loop** root — inner `k`-recurrence + outer `pc_it` Richardson sweep, loop-invariant polynomial-kind/element-type axes) re-anchoring `L3/chebyshev.md` + `L3/index.md`; D3 landed the consolidated tally + the **now-COMPLETE four-root erasure-scope taxonomy**: unconditional-single-loop (`ksp-solve-outer-driver`), variant-conditional-single-loop (`orthogonalize-variant-split`), unconditional-nested-double-loop (`chebyshev-nested-recurrence`), opaque-library (`eigsolve-opaque-eigen-iteration`) — distinguished from the 13 thin `-body-identity` identity-themes. **The cycles-041–045 L2-floor + L3>L2-rotation foundation campaign (2026-05-31 uniform-pull-up directive) has filled the middle of the stack: L2 9 → 21, L2>L1 7 → 19, L3>L2 2 → 17 — the stack is now substantially rectangular through L0–L3.** Next frontier (batch-14+): the uniform climb resumes UPWARD — L4>L3 coverage + L4 expansion (L4 only 4 firm) + any residual L2>L1 gaps. **Cycle-046 (FIRST of batch-14; survey-heavy opening cycle) opened the L4 frontier + censused the residual-L2>L1 gap — counts unchanged (no new firm operators/themes this cycle), 1 concept-page landing.** D1 (combinator-miner) ran the L4/L4>L3 coverage PRE-SURVEY (the lead): the L4 lead frontier now has a fan-out-ranked pick list — R1 the standalone L4>L3 `iterate-while`/`iterate-while-with-prev` themes (paired, lead), R2 an `L4/ksp_solve.md` cap, R3 an `L4/eigsolve.md` cap, R5 defer `L4/orthogonalize.md` (4 OQ slugs promoted as cycle-047 candidates). D3 (cross-layer-cross-cutter) CENSUSED the residual L2>L1 gap (foundation hygiene now that L2 floor = 21): **2 genuine L2>L1 gaps found — `ksp_solve` (rank 1, driver tier) + `krylov-step` (rank 2, kernel tier)** → cycle-047 abstractor ×2 candidates (the dispatch framing reported 1 gap, census found 2 — a benign planner-undercount data point; the `ksp_solve`-edge-mislabel caveat was CLOSED in critique in the report's favor). D2 (layer-intro-author) landed `book/src/concepts/erasure-scope.md` — the RATIFIED four-root erasure-scope taxonomy concept page (the conceptual yield of the now-complete L3>L2 substantive cohort), giving the axis a cross-cutting home adjacent to `concepts/sequential-obstruction.md` + `concepts/tensor-field-lift.md`. The L4 frontier (4 firm L4 ops + 3 firm L4>L3 themes) is now the lowest incomplete layer / the lead. **Cycle-047 (SECOND of batch-14) advanced the L4 frontier R1 + closed the residual-L2>L1 gap + anchored the R2 prerequisite — L4>L3 firm 3 → 5, L2>L1 firm 19 → 21, L4 vocabulary +3 outer-driver rows.** D1/D2 (abstractor ×2) landed the two standalone L4>L3 iterate-while dissolution themes — `book/src/L4-L3/iterate-while-dissolution.md` (the no-prev form) + `book/src/L4-L3/iterate-while-with-prev-dissolution.md` (the carry-bootstrapped with-prev sister) — each re-anchoring its firm L4 cap's §"Lowers to" + §"L4 vs L3 distinction" + the `L4/index.md` dep-map "Lowers to" cell onto the dedicated theme (the R1 lead pick; closes `iterate-while-l4-l3-standalone-theme-warranted-lifter-vs-abstractor`). D3/D4 (abstractor ×2) landed the two residual L2>L1 gap themes — `book/src/L2-L1/ksp-solve-outer-driver-unfold.md` (the rank-1 DRIVER-tier edge) + `book/src/L2-L1/krylov-step-kernel-defusion.md` (the rank-2 KERNEL-tier edge) — **closing `residual-l2-l1-gap-audit` jointly** (both census gaps now themed; D3 sole `L2-L1/index` count-owner 19→21, D4 deferred tally per the dual-registration partition). D5 (layer-intro-author) anchored the `solve-monad` outer-driver L4 vocabulary in `book/src/L4/index.md` — 3 NEW firm dep-map rows (`solve_loop`/`restart_cycle`/`Outcome`) + a §Vocabulary-cohort sub-block + discharge of the `:47` "Queued at L4" deferral — **the verified R2 prerequisite for the cycle-048 `L4/ksp_solve.md` (R2) + `L4/eigsolve.md` (R3) caps** (the anchor half of `l4-ksp-solve-eigsolve-caps-gated-on-solve-monad-outer-driver-vocabulary` discharged; the cap halves remain queued for cycle-048, alongside the R5 `L4/orthogonalize.md` marginal-defer). After cycle-047 the L4 frontier carries R1 LANDED + R2 prerequisite ANCHORED; the c046 survey's read that L4 is "mostly intentionally complete" means after c048's caps the L4 frontier may be near-exhausted — the batch-14 meta-phase (fires after c048) should assess the next forward direction (the `l4-native-combinator-denominator-completeness-survey` OQ, or width/depth consolidation).
- **L4** — **3 firm operators**: `krylov-step` (cycle-006), `iterate_while` (cycle-007), `iterate_while_with_prev` (cycle-007). **Cycle-008 L4/index refresh**: Semantics-overlay placeholder replaced with grounded 4-motif overlay; new `## Vocabulary cohort` subsection added (template adapted: middle slot uses L4>L3 cross-layer themes rather than rough-in same-layer operators, since L4 currently has no rough-in operators; cycle-009+ meta-phase consideration via OQ `vocabulary-cohort-middle-slot-cross-layer-adaptation`); dep-map widened 4→5 columns with new `Lowers to` column splitting cross-layer references out of `Dependencies`. **Cycle-047 L4 vocabulary +3 outer-driver rows** (D5 layer-intro-author, `book/src/L4/index.md`): NEW firm dep-map rows `solve_loop` / `restart_cycle` / `Outcome` (the `solve-monad` outer-driver vocabulary, fully determined by `book/src/concepts/solve-monad.md`; rows correctly carry plain-text backtick slugs for the unauthored cap pages, no premature link) + a §Vocabulary-cohort sub-block + discharge of the `:47` "Queued at L4" deferral. These are the verified prerequisite for the cycle-048 `L4/ksp_solve.md` (R2) + `L4/eigsolve.md` (R3) caps; the firm-vs-rough-in maturity straddle (firm row, page-existence asymmetry vs the live-linked operator pages) was adjudicated `firm` at integration with a cycle-048 cap-author harvest-depth follow-up note (OQ `solve-monad-l4-row-firm-maturity-straddle`). **L4 firm 4 → 6 cycle-048 — the two iterative-solve CAPS landed firm, SUBSTANTIALLY COMPLETING the L4 frontier**: `book/src/L4/ksp_solve.md` (D1/R2, firm outer-driver cap over firm `L3/ksp_solve`, consuming the c047 solve-monad vocabulary; closed the ksp_solve-half of `l4-ksp-solve-eigsolve-caps-gated-on-solve-monad-outer-driver-vocabulary` + resolved `solve-monad-l4-row-firm-maturity-straddle` for `solve_loop`/`restart_cycle`) + `book/src/L4/eigsolve.md` (D2/R3, firm **opaque-library role-wrapper** cap over the `partial-obstruction` `L3/eigsolve`, EigOutcome sum with first-class PartialConverged arm; closed the eigsolve-half → the **WHOLE caps-gated OQ now closed**). **L4 outer-driver vocabulary 3 → 4 cycle-048** (+`EigOutcome` clean-addition dep-map row extending the canonical `Outcome` sum for the eigsolve partial-success arm; `outcome-sum-one-row-vs-per-cap-specialisation` KEPT-OPEN for the batch-14 meta-phase to ratify per-cap `*Outcome` rows vs polymorphic `Outcome α`). D4 (layer-intro-author, sole count-owner) flipped the L4/index Firm-at-L4 tally `(4 + 3 outer-driver)` → `(6 + 4 outer-driver)` + outer-driver sub-header `(3)`→`(4)` + the §Queued-at-L4 prose to the near-exhaustion statement. **The L4 frontier is now NEAR-EXHAUSTED**: 6 firm L4 ops + 4 outer-driver anchors; 13 of 18 firm L3 ops are no-L4-by-design (the BLAS-1/elementwise/smoother cohort); the only remaining L4-cap candidate is R5 `L4/orthogonalize` (deferred-marginal, gated on a non-existent firm L4 Arnoldi consumer per OQ `l4-orthogonalize-cap-marginal-defer`). The **batch-14 meta-phase (fires after this c048 finalize) should assess the next forward direction** — the `l4-native-combinator-denominator-completeness-survey` OQ, width/depth consolidation, or a pivot toward the downstream burn-component effort the calculus was built to serve (OQ `l4-near-exhaustion-assessment-batch-14`). **L4 rough-in +1 cycle-057 (`fold_solve` dep-map row)** — the transient pipeline's **state-threaded fold** outer-driver, the **fold-counterpart of the c054 `solve_family` map** (both children of the strawman §3.7 `iterate_while` family; map collects independent members, fold threads the prior step's output as the next step's input). One rough-in dep-map row + one frontier bullet in `book/src/L4/index.md` (D4 abstractor, observation-first thread-opener from 1 transient witness `transientsolver.cpp:77-104` + `timeoperator.cpp:410` `ode->Step(sol,t,dt)`); the slug is plain-text/code-span (no dangling link, no `fold_solve.md`/theme yet); the per-step `time_step_op` body is an opaque MFEM `ODESolver` step (the `eigsolve` opaque-library shape). Promotion gated on a 2nd fold-witness (the c056 D1 `SweepAdaptive` PROM candidate — CONFIRMED a 2nd `fold_solve`-family member by c057 D3, advancing `fold_solve` toward ITS gate, NOT a 2nd `map_solve` witness) OR a downstream transient pull. The **two-combinator (independent-MAP `solve_family` vs sequential-FOLD `fold_solve`) factoring** is the structural yield of the batch-17 solver-test-load. The `map_solve` superset (operator-VARYING map for driven/transient) STAYS deferred at 1 witness (standard `Sweep`, c056 D1) — a 2nd map witness must come from another pipeline or `map_solve` is a permanent single-witness spine-coverage finding (batch-18 planner decision). **Cycle-058 (D1 harvester): `fold_solve` PROMOTED rough-in→firm — L4 firm operators 6→7** (`book/src/L4/fold_solve.md`; the transient pipeline's state-threaded `foldl (\s t -> time_step_op op s t) s0 schedule` outer-driver, the FOLD-sibling of the `solve_family` MAP shell — opposite carry axis, the carry-threaded time sweep reads the prior step's write so it carries a `sequential-obstruction`; per-step `time_step_op` is an opaque MFEM `ODESolver` step, the `eigsolve` opaque-library shape; schedule-source variant axis — fixed-list default surface, state-generated `SweepAdaptive` form batch-18-gated). With `solve_family` (c055) + `fold_solve` (c058) the **two-combinator MAP/FOLD factoring of the §3.7 `iterate_while` family** is realized at L4; their shared parent is `iterate_while` itself (no third parent abstraction warranted). The `map_solve` operator-VARYING superset stays a **permanent single-witness spine-coverage finding** (1 witness, std `Sweep` c056 D1) — routed to the batch-18 meta-phase for formal close (c058 D3 cross-layer-cross-cutter NON-DISCHARGE probe: a 2nd map witness did not materialize from a 2nd pipeline). NOTE the stale leading "3 firm operators" opener is historical; the live L4 firm count is 7 (per `L4/index.md` Firm-at-L4 tally + the running narrative).
- **L4>L3** — **2 firm themes + 1 rough-in theme** (was 1 firm + 2 rough-in pre-cycle-020): `krylov-step-typed-wrapper-dissolution` (cycle-006 wave-2 abstractor; **promoted cycle-008 wave-1 lifter** from rough-in → firm); **`gmres-inner-loop-iterate-while-migration` (cycle-008 wave-2 abstractor; PROMOTED rough-in → `firm` cycle-020 wave-1 lifter** — the upstream `gmres.md` §L4 self-rotation landed (slice §L4 v0.7, inner-loop `iterate_while` migration via option (a) `check_stop_into_carry`), so the theme firms against its now-realized LHS surface; the `L4/index.md:44` theme row + `:53` `iterate-while` "Lowers to" cell synced rough-in→firm by integrator-finalize); **`fgmres-inner-loop-iterate-while-migration` (cycle-011 wave-2 lifter, **rough-in** — STAYS rough-in, held for cycle-021 lifter against the now-firm gmres sibling) — first L4>L3 rough-in sister-theme in artifact; sibling to cycle-008 GMRES theme with FGMRES variant-axis collapses (`pc_side = RIGHT`, `flexible = true`) and textually-identical `:823-828` break-site finding; 6 applicability conditions (5 inherited from sibling + 1 FGMRES-specific); closes cycle-010 OQ `fgmres-inner-loop-iterate-while-migration-lifter-candidate`. **Cycle-011 firm-promotion of `check_stop_into_carry` remains deferred** per `nleps-spec-gap-as-check-stop-into-carry-reuse-blocker`; the cycle-010 lower-edge "second reuse" reading is preserved and reinforced by this dispatch. **L4>L3 firm 3 → 5 cycle-047** — +2 standalone iterate-while dissolution themes (abstractor ×2, the R1 lead pick of the cycle-046 L4-frontier survey): `book/src/L4-L3/iterate-while-dissolution.md` (the no-prev form — how the L4 `iterate_while` combinator lowers into the L3 `fold_iterate` over the pruned trajectory image) + `book/src/L4-L3/iterate-while-with-prev-dissolution.md` (the carry-bootstrapped with-prev sister — `iterate_while_with_prev` lowering, the prev-carry bootstrapped through the fold seed). Each re-anchored its firm L4 cap's §"Lowers to" deferral paragraph + §"L4 vs L3 distinction" closing paragraph + the `L4/index.md` dep-map "Lowers to" cell (`:54` no-prev, `:55` with-prev) from the prior in-line/deferred note onto the dedicated theme; **closes `iterate-while-l4-l3-standalone-theme-warranted-lifter-vs-abstractor`** (route-b realized for both forms — the abstractor authored the dedicated themes) + `iterate-while-l3-rendering-trajectory-accumulation-gap` (the trajectory-drop is the §3.8-pruned image, not a gap). D2's chapter carries one DELIBERATE historical-provenance `cg.md:441-446` OOB prose mention (marked pre-cycle-009-corpus-reduction historical citation, not a live claim) — citecheck-OOB-by-design, NOT a linkcheck/build error. **L4>L3 firm 5 → 6 cycle-048** — +1 driver-half dissolution theme `book/src/L4-L3/ksp-solve-driver-dissolution.md` (D3 abstractor, the dedicated rotation for the R2 `L4/ksp_solve` cap D1 authored): the outer `Solve`-monad driver (`solve_loop`/`restart_cycle`/`Outcome` classification) dissolves into the L3 `ksp_solve` value-threaded outer loop, composing strictly ABOVE the inner-fold `iterate-while-dissolution` (firm c047) as the driver-half companion to `krylov-step-typed-wrapper-dissolution`. The c047 D1-flagged working-slug-vs-canonical-slug mismatch (`ksp-solve-outer-driver-dissolution` vs canonical `ksp-solve-driver-dissolution`) was RESOLVED at repair/integrate — zero `ksp-solve-outer-driver-dissolution` refs remain in `book/` (NEW friction `cross-report-forward-reference-slug-divergence` flagged for the batch-14 meta-phase). The sibling `eigsolve` L4>L3 dissolution is in-line marker-erasure by design (no dedicated `L4-L3/eigsolve-*` theme — parallel to chebyshev; OQ `eigsolve-l4-l3-in-line-by-design-no-dedicated-theme`). **L4>L3 firm 6 → 7 cycle-055** — +1 `book/src/L4-L3/solve-family-map-dissolution.md` (the MAP-shell rotation for the `solve_family` cap). **L4>L3 firm 7 → 8 cycle-058** (D2 abstractor) — +1 `book/src/L4-L3/fold-solve-time-step-dissolution.md`: the L4 `fold_solve` state-threaded fold shell → L3 explicit imperative time-sweep threading the field-state `sol` **IN PLACE** (`ode->Step(sol,t,dt)` advances `sol` destructively, prior write = next input). Substantive (`foldl`-combinator → in-place-threading-loop: functional carry → in-place mutation, abstract `[Time]` schedule → concrete `delta_t`/`n_step` march, opaque `time_step_op` → the `ode->Step` library boundary — `obstruction (opaque-library-ownership)` per-step sub-leaf, negative anchor `timeoperator.cpp:410`). The **fold-sibling** of `solve-family-map-dissolution` (c055): same operator-capture hoist, opposite carry axis — the map's family loop carries NO `sequential-obstruction`, this fold's time-sweep carries one. Together the two realize the MAP/FOLD children of the §3.7 `iterate_while` family at the L4>L3 hop. The `L4-L3/index.md` consolidated tally now reads **8 firm + 0 rough-in**. The stale leading "2 firm themes + 1 rough-in" opener is historical.
- **L3** — **15 firm operators** + 3 `partial-obstruction` (`chebyshev` c013, `eigsolve` c024, `orthogonalize` c040): `krylov-step` (cycle-010 wave-1 harvester — first firm L3 operator in the artifact; identity-lowering backfill); **+7 BLAS-1 cohort entries cycle-011 wave-1**: `apply_linop` (pass 1; first primitive-flavored L3 backfill), `axpy` + `axpby` + `axpbypcz` (pass 2; linear-update cohort), `dot` + `nrm2` (pass 3; reduction cohort), `scal` (pass 4; closes cohort); **+`ksp_solve` cycle-020 wave-1 harvester** — first NON-identity L3 backfill (the outer-driver `iterate_while_L3` fold carrying the outer-loop `sequential-obstruction`, complementing the firm L3 `krylov-step` kernel half whose body IS identity-in-form; a genuine iteration-rotation, NOT a corollary of `krylov-step`); landed `firm` ABOVE a `stub` L2 `ksp_solve` anchor (maturity-gradient inversion, acceptable under the **Identity-lowerings still require both L levels** invariant — each layer coherent within itself); the L3>L2 `ksp-solve-outer-driver` rotation is substantive/non-identity (theme WARRANTED but gated on the L2 `ksp_solve` stub→firm promotion). **L3 firm-operator count: 1 → 8 cycle-011** — fully closed the BLAS-1 cohort portion of OQ `l3-backfill-apply-linop-and-blas1-cohort` (HIGH CONFIDENCE recommendations from cycle-010 cross-layer-cross-cutter audit); priority #20 second target fully met. **8 → 9 cycle-020** (+`ksp_solve`; closes the `ksp_solve` constituent of OQ `l3-vocabulary-inventory-gap`, parent stays open against `trsv`/`eigsolve`). L3 cohort frontmatter uniformly with-frontmatter at 8/8. Cumulative in-line identity-rotation count reaches 7 — exceeds cycle-010 OQ `l3-l1-directory-naming-structure-policy` revisit threshold of 6; strong candidate for cycle-012 meta-phase closure decision (codify in-line convention vs introduce `book/src/L3-L1/` directory). Cohort growth candidates beyond BLAS-1 remain routed via the `l3-vocabulary-inventory-gap` OQ (gemv, trsv, etc.). **9 → 11 cycle-037** (+`assemble-diagonal` 10th + `jacobi-smoother` 11th — the **diagonal-preconditioner-apply chain at L3**; both identity-in-form backfills enacting the first two of the six **(A)** firm candidates of the cycle-036 D2 L3-cohort-growth audit verdict at `book/src/L3/index.md:39` under OQ `l3-cohort-growth-audit-c036-verdict`; `assemble-diagonal` is the operator-to-data `A -> diag(A)` introspection primitive, sibling of `apply_linop`; `jacobi-smoother` is the **thinnest** constructed-operator gate — apply is one whole-tensor elementwise product, carries NO obstruction at L3, the sharpest contrast with the `ksp_solve`/`eigsolve` outer-loop obstructions; substantive rotations live at L1>L0). Four of the six (A) backfills remained after cycle-037 (`reciprocal`, `elementwise_product`, `normalize`, `divfree-projector`) — the natural batch-11 L3 follow-frontier. Cycle-037 also landed a 19-row `verified_against:` audit block onto the firm L1>L0 `reciprocal-elementwise-product-mutation-rotation` theme (lowering-verifier; additive evidence-backfill, no body edits, theme stays firm). **11 → 14 cycle-038** (+`reciprocal` 12th + `elementwise_product` 13th + `divfree-projector` 14th — the elementwise / constructed-operator-gate cohort, three parallel **(A)** identity-in-form backfills of the same c036 D2 audit verdict at `book/src/L3/index.md:41`; `reciprocal` is the elementwise multiplicative-inverse `D⁻¹`-forming step, `elementwise_product` the Hadamard binary floor, `divfree-projector` the **third constructed-operator gate** at L3 — **obstruction-carrying by reference** (its four-step apply authors no obstruction but its inner `ksp_solve` carries one; the middle of the L3 obstruction-profile spectrum, instantiating the nested-gate chain `eigsolve ⊃ divfree-projector ⊃ ksp_solve`); substantive rotations live at L1>L0). With this landing the c036 **(A) cohort is 5-of-6 landed** — **only `normalize`** (fused `nrm2 + scal`) remains, held to cycle-039 as the cohort-closer. Cycle-038 also landed a 29-row `verified_against:` audit block (28 supports + 1 partially-supports) onto the firm L1>L0 `floquet-correction-mutation-rotation` theme (lowering-verifier; additive evidence-backfill, no body edits, theme stays firm; the single partially-supports row — AddMult aliasing-tolerance cited to a thin wrapper vs the true gated site — is routed to the OPEN follow-up OQ `floquet-corrector-addmult-aliasing-applicability-audit`, NOT repaired in-cycle, per UNBLOCK-not-ENACT discipline). **14 → 15 cycle-039** (+`normalize` 15th — the fused `nrm2 + scal` magnitude-normalization step, the SIXTH and FINAL **(A)** identity-in-form backfill of the c036 D2 L3-cohort-growth audit verdict; **with this landing the c036 (A) cohort is 6-of-6 COMPLETE / CLOSED** — all of `assemble-diagonal` (c037) / `jacobi-smoother` (c037) / `reciprocal` (c038) / `elementwise_product` (c038) / `divfree-projector` (c038) / `normalize` (c039) now firm at L3; OQ `l3-cohort-growth-audit-c036-verdict` CLOSED). `normalize`'s substantive rotation lives at L1>L0 `normalize-mutation-rotation`; the L3>L1 edge is identity-in-form (body value-thread-isomorphic, no obstruction). **Cycle-039 also ENACTED the floquet AddMult-aliasing re-anchor**: the firm L1>L0 `floquet-correction-mutation-rotation` theme's 6 `ksp.cpp:297` mention sites were surgically re-anchored (6-edit lowering-verifier dispatch) to the true gated mechanism `CgSolver::Mult` (`iterative.cpp:361`, `r=b;` `:384` before `x=0.0;` `:385`) + the `SetInitialGuess(0)` precondition (`floquetcorrection.cpp:61`); ALL `verified_against:` rows now `supports` (0 `partially-supports`), theme stays firm, OQ `floquet-corrector-addmult-aliasing-applicability-audit` CLOSED (supersedes the c038 "sharpened not closed" carry-forward). **Cycle-039 also folded a 4th L3 obstruction profile** into `book/src/L3/index.md` §Semantics-overlay: shape (d) `obstruction-carrying-by-reference` (exemplified by `divfree-projector` — authors no obstruction itself but its inner `ksp_solve` carries one), reframing "Three firm shapes" → "Four firm shapes"; the §Working-Notes tally consolidated to the authoritative **15 firm + 2 partial-obstruction, (A) cohort 6-of-6 COMPLETE**. **Firm count UNCHANGED at 15 through cycle-040; partial-obstruction 2 → 3 cycle-040** (+`orthogonalize` 3rd partial-obstruction — `book/src/L3/orthogonalize.md`, the **first substantive (B)-cohort member** of the c036 D2 L3-cohort-growth audit, NOT an identity-in-form (A) backfill: a **variant-conditional partial-obstruction** — the MGS (modified Gram-Schmidt) variant carries a sequential inner-loop `sequential-obstruction` (each projection reads the running-updated vector), while the CGS / CGS2 (classical / re-orthogonalized) variants lift cleanly to a global tensor-field batched projection; the obstruction is gated on the `gs_orthog` variant axis. This opens the **(B) substantive cohort** and validates the substantive-harvester route at L3, distinct from the closed (A) identity-in-form cohort. The `L3/index.md` §Semantics-overlay folded a fifth obstruction profile [shape (e) `orthogonalize` variant-conditional] + the `fused-composite-obstruction-free` (`normalize`) profile; the §Working-Notes single authoritative tally is now **15 firm + 3 partial-obstruction**. Cycle-040 also landed 2 lifter citation-tightens on firm L1>L0 themes (`floquet-correction-mutation-rotation` M-block `:25-26`→`:25`; `chebyshev-smoother-mutation-rotation` dead-transpose-kernel `:101-110`→`:102-110`; both pure citation-range firm-ups, status `firm` preserved; 3 OQs closed). Next (B) candidates: `chebyshev-smoother` L3 (needs a subsumption-check vs firm L3 `chebyshev` FIRST) + `apply_nonlinear_pencil` L3 (folds into a future eigsolve-variant pass). **Cycle-044 — `orthogonalize` (B) follow-through + chebyshev-smoother subsumption-check RESOLVED**: the firm substantive L3>L2 rotation `orthogonalize-variant-split` landed (variant-conditional MGS-erasure; L3 `orthogonalize` stays `partial-obstruction`, audited via a 24-row `verified_against:` block, status NOT flipped); the c036 D2 caveat at `book/src/L3/index.md:48` ("requires a subsumption check first") was DISCHARGED by D2's cross-cutter observation — **`chebyshev-smoother` L3 is SUBSUMED by the firm L3 `chebyshev` (c013, partial-obstruction), NO-LAND** (a clean negative result that REMOVES work; the closure OQ `chebyshev-smoother-l3-candidate-subsumed-closed` records the benign L1/L2/L3/L4 slug-name asymmetry so a future audit does not re-flag it). **`apply_nonlinear_pencil` is now the only remaining (B)-candidate** (routed to fold into a future eigsolve-variant pass, not a standalone row). The remaining substantive L3>L2 rotations (`chebyshev`, `eigsolve`) are the batch-13/cycle-045 lead frontier toward `l3-l2-rotation-theme-coverage-gap` 18-of-18. **L3 firm 15 → 17 cycle-050** (+`linear_combination` +`inner_product` — the two L3 combinator entries PROPAGATED from the c049 combinator-as-entry inversions under the 2026-06-01 VOCABULARY-SHIFT REDIRECT; each is a whole-tensor fold/reduction with no sequential obstruction, carrying an in-line §"Downward to L2" note as the pre-built fold-family demotion home; partial-obstruction unchanged at 3). The L3 BLAS-1 leaf re-expression through these combinators is c051 fold-family work. **L3 firm UNCHANGED at 17; partial-obstruction 3 → 4 cycle-059** (+`fold_solve` 4th partial-obstruction — `book/src/L3/fold_solve.md`, the L3 fold-image of the firm L4 `fold_solve` MAP/FOLD-cohort sibling; the **carry-threading time sweep** + the **opaque per-step body** (`ode->Step(sol,t,dt)` `timeoperator.cpp:410`, MFEM `ODESolver`) BOTH resist the iteration rotation while the per-step body lifts — the `chebyshev`/`eigsolve` partial-obstruction shape, distinct from the variant-conditional `orthogonalize` shape; carries 4 variant axes matching the L4 cap. The `L3/index` §obstruction-profile tally moves 3 → 4 partial-obstruction. The standalone-L3-entry-vs-dissolution-home question is RESOLVED L3-ENTRY [OQ `fold-solve-l3-entry-vs-dissolution-home`]: unlike `solve_family`'s cleanly-lifting family loop (NO-ENTRY c057, dissolution-theme-is-the-L3-home), `fold_solve`'s loop carries a genuine obstruction so it earns its own standing L3-vocabulary home. **Cycle-060 (close-out) synced the `L3/index` §Semantics-overlay spectrum PROSE at `:15`** ("Five … shapes"/"three `partial-obstruction`" → "Six … shapes"/"four `partial-obstruction`", shape (f) `fold_solve` clause + distinction sentence appended) to match the already-authoritative `:64` count tally that the c059 D1 abstractor (the sole L3-touching dispatch) had left ahead of the prose — a PROSE-only intra-file divergence repair, NO count-cell touched (the `:64` tally is the count-owner per the c057-meta count-owner guard). **L2/`fold_solve` NO-FLOOR-WARRANT CONFIRMED descent-complete (c060 D2, observation-only):** `fold_solve` owes NO `L2/fold_solve.md` floor — its per-step body is the opaque MFEM `ode->Step` integrator leaf with NO L2 composition (unlike `eigsolve`, whose body opens to `apply_linop ▷ ksp_solve`); the substantive L3>L2 content is the outer-sweep erasure, the absence-by-warrant is correct (a degenerate `L2/fold_solve` mirror would be the §1d vocabulary-shift-redirect smell). Routed to the batch-18 meta-phase for formal close.
- **L2>L1** — **2 firm themes** (`chebyshev-iteration-fusion`, cycle-013; **`linear-combination-fold-specialization` NEW cycle-018** — the fusion-selection lowering taking the L2 `linear_combination` variadic fold into the L1 fixed-arity specializations `scal`/`axpy`/`axpby`/`axpbypcz` via arity-dispatch + pinned summation order; **second real chapter under the L2-L1 Part**; firm *(algebraic; arity-dispatch fusion-selection + pinned summation order)*; index theme-list row + SUMMARY register landed; arity≥4 + complex-overload-parity residuals routed via OQ `linear-combination-fold-specialization-theme-followups`). `chebyshev-iteration-fusion` detail: the recurrence↔polynomial fusion taking the L2 explicit degree-`order` three-term recurrence into the L1 `chebyshev-smoother` closed-form action; **first real chapter under the previously-skeleton L2-L1 Part**. Part now wired (Overview + 2 chapters). **L2>L1 cohort current count (cycle-029 — the lead prose above is stale from cycle-018; the index `book/src/L2-L1/index.md` is authoritative): 8 dep-map rows = 7 firm + 1 partly-constructive (unchanged from cycle-028; cycle-029 was prose-only on the L2-L1 index — appended a §Vocabulary cohort subsection + cohort-growth-log bullet, no dep-map rows added).** The 7 firm: `chebyshev-iteration-fusion` (c013), `linear-combination-fold-specialization` (c018), `inner-product-fold-specialization`, `orthogonalize-composition-lowering` (c019), `gram-fold-specialization` (c022), `eigsolve-spectral-transform-composition` (c023), **`incremental-least-squares-composition-lowering` (cycle-028 — firm L2>L1 cohort 6→7)** — the GMRES/FGMRES running-QR / Givens-rotation-stream fan-down `replay▷generate▷apply▷apply_rhs▷back-solve` lowering the firm L2 `incremental-least-squares` into its L1 leaves (terminal back-solve = firm `back_solve` leaf, NOT general `trsv`; the `ls_update_column` column-streaming leaf landed firm cycle-029 — the cycle-028 plain-text forward-references are now upgrade-eligible, routed to cycle-030 follow-up). Closes the c027 D5 deferral (inverted coordinated-rename premise; landed fresh c028). The 1 partly-constructive: `deflate-composition-lowering`. **L2>L1 firm 7 → 10 cycle-041** — +3 BLAS-1-leaf thin-identity themes co-landing with their L2 floors: `dot-leaf-identity` (D4; producer-renamed from the dispatch-proposed `dot-fold-specialization` — the RHS is `L1/dot`, an identity-leaf-edge NOT a fold-dispatch), `nrm2-fold-specialization` (D5; consumer-of-fold `√∘inner_product` framing), `scal-fold-specialization` (D6; arity-1 member of `linear_combination`, cited-NOT-merged). **SLUG-NAMING split flagged for batch-12 meta-phase**: `dot-leaf-identity` vs `nrm2/scal-fold-specialization` are the same shape under divergent slug conventions (OQs `nrm2-fold-specialization-slug-vs-consumer-framing-rename-candidate` + `scal-fold-specialization-slug-vs-leaf-identity-rename-candidate`); meta-phase should normalize. All 3 presuppose the (b) leaf-floor reading of the design fork. **L2>L1 firm 10 → 15 cycle-042** — +5 leaf-identity themes co-landing with their L2 floors (`reciprocal-leaf-identity` D10, `elementwise-product-leaf-identity` D10, `assemble-diagonal-leaf-identity` D7, `jacobi-smoother-leaf-identity` D8, `divfree-projector-leaf-identity` D9). Four are thin pure-identity-in-form (no fusion to defer); `divfree-projector-leaf-identity` is the FIRST mostly-identity-with-one-rotation edge (carries the projector's ONLY fusion — the step-4 `Grad->AddMult` re-fusion). All 5 are FORK-INDEPENDENT (no fold-parent), so design-final regardless of the leaf-vs-fold adjudication; the §Vocabulary-cohort got a cohort-neutral "Fork-INDEPENDENT standalone-floor edges" sub-list (D11). **SLUG-NAMING normalization escalated for batch-12 meta-phase**: the cohort now carries 3 conventions (`-leaf-identity`/`-body-identity` [c042 + c041 `dot`], `-fold-specialization` [c041 `nrm2`/`scal` outliers], + the `elementwise_product` underscore/hyphen split across layers) — consolidated as OQ `l2-floor-cohort-slug-naming-de-facto-convention`. **L2>L1 firm 15 → 19 cycle-043** — +4 leaf-identity themes co-landing with their L2 floors (`axpy-leaf-identity` / `axpby-leaf-identity` / `axpbypcz-leaf-identity` — arity-2/2/3 fold-members of `linear-combination-fold-specialization`, all fusion deferred to the fold-parent; `normalize-leaf-identity` — the fused-composite-with-no-fold-parent leaf, NO fusion to defer and no kernel fusion to unfold since Palace's `linalg::Normalize` already separates the norm pass from the rescale pass). The batch-12 meta-phase's slug normalization (`-leaf-identity`/`-body-identity` cohort-uniform) was REALIZED in-artifact this cycle: D1's `git mv` renamed the c041 `nrm2`/`scal` `-fold-specialization` outliers to `-leaf-identity` + the `elementwise_product-body-identity` underscore→hyphen, so the cohort slug convention is now uniform across all on-disk files (residual old-slug occurrences survive only in 12 prior-cycle CHAPTER-BODY provenance prose + the scaffolding plan — routed to a cycle-044 cleanup sweep via OQs `l2-floor-directive-slug-rename-book-chapter-body-residual` + `l2-floor-directive-slug-rename-scaffolding-residual-sweep`). **L2>L1 firm 19 → 21 cycle-047 — the 2 residual L2>L1 gap themes (CLOSES `residual-l2-l1-gap-audit`)**: the cycle-046 cross-cutter census found exactly 2 genuine L2>L1 gaps on the now-21-firm L2 floor; cycle-047 landed both (abstractor ×2): `ksp-solve-outer-driver-unfold` (D3, the rank-1 **DRIVER-tier** substantive composition→opacity edge — how the L2 `ksp_solve` outer-driver composition unfolds into its L1 leaves; the `L3-L2/ksp-solve-outer-driver` previously delegated this L2>L1 edge in-line) + `krylov-step-kernel-defusion` (D4, the rank-2 **KERNEL-tier** edge — the per-step kernel de-fusion of the L2 `krylov-step` into its L1 leaves; resolves the dangling `L2/krylov-step.md:121` variant-axis-6 forward-ref). D3 is the SOLE `L2-L1/index` consolidated-tally count-owner (19→21 = 21 firm + 1 partly-constructive `deflate-composition-lowering`), D4 deferred the tally per the dual-registration partition (own row + own §Vocabulary-cohort bullet in D3's NEW "Substantive driver-tier composition→opacity edge" sub-group only); count-ownership held cleanly, no parallel-blind-count-divergence. **Closes `ksp-solve-l2-l1-theme-gap` + `krylov-step-l2-l1-theme-gap` + `residual-l2-l1-gap-audit`** (both census gaps themed). With this landing the residual-L2>L1 foundation hygiene is complete; the L2 floor (21 firm) now has full L2>L1 coverage of its driver/kernel composition edges. **L2>L1 firm 21 → 17 cycle-050 + L3>L2 firm 17 → 13 cycle-050** — the refactor-pass enactment under the 2026-06-01 VOCABULARY-SHIFT REDIRECT DEMOTED 4 degenerate identity-in-named-terms theme PAIRS to in-line §"Downward" notes (8 theme files deleted): `assemble-diagonal`/`elementwise-product`/`reciprocal`/`normalize` (`-body-identity` L3>L2 + `-leaf-identity` L2>L1 each). The DROPS are a **vehicle-change** (theme file → in-line note), NOT a coverage regression — each rotation stays captured in-line (OQ `c050-firm-theme-count-drop-is-vehicle-change-not-coverage-regression`). The remaining thin-identity demotions (the fold-family `dot`/`scal`/`axpy`/`axpby`/`axpbypcz` themes → collapse into the new L3 `inner_product`/`linear_combination`; `nrm2` stays consumer) + the DEMOTE-OK `jacobi-smoother` (both edges) + `divfree-projector-body-identity` (L3>L2 only) are c051; **`divfree-projector-leaf-identity` (L2>L1) is KEEP-substantive per the D8 verify-body audit** (genuine step-4 `Grad->AddMult` fusion rotation), so the degenerate-cohort denominator is **17 not 18**.
- **Krylov-step lowering chain extended cycle-010**: L4 `krylov-step` (firm cycle-006) > L4>L3 `krylov-step-typed-wrapper-dissolution` (firm cycle-008) > L3 `krylov-step` (firm cycle-010) > L3>L2 `krylov-step-body-identity` (firm cycle-009) > L2 `krylov-step` (firm cycle-005) > L1 `ksp_solve` (firm cycle-007) > L1>L0 `ksp-solve-mutation-rotation` (firm cycle-008). **All 7 layer/lowering positions occupied explicitly**; first fully-firm cross-layer chain in the artifact extended with the cycle-010 L3 backfill closing the only gap.
- **Phase 1 slice corpus reduction batches landed cycles-010+011+012** — cycle-010 batch-1 (3 slices: gmres.md -42%; cg.md -67%; arnoldi_step.md -8%; net 842 lines removed). **Cycle-011 batch-2 (3 slices)**: orthog.md (L0→L1 Gram-Schmidt body replaced with reduced stub; plane-rotation-stream sub-slice deferred to batch-3 joint-audit), chebyshev.md (Consumers / Open-questions / Concept-references condensed; L1/L2/L3/L4 sections retained pending L1/chebyshev-smoother + L2/chebyshev-iteration firm-row promotion), polynomial_recurrence_step.md (**first negative-result slice audited** — verdict `blocked / minimal reduction; the slice IS the artifact`; methodologically distinct from batch-1 precursor-slice verdicts; 2 narrow forward-pointers added; load-bearing distinction catalog retained verbatim). **Cycle-012 batch-3 (3 reductions)**: orthog.md plane-rotation sub-slice **fully reduced** (376→234 lines; both near-duplicate `## L1 — per-element procedure` entries eliminated — **first intra-corpus-redundancy verdict**: plane_rotation_stream + orthog's plane-rotation sub-slice are the same algorithm, resolved-via-elimination; the unique LS-residual invariant `‖R·y−s‖²+|s[j+1]|²=‖H̄·y−βe₁‖²` was hoisted into plane_rotation_stream.md FIRST), plane_rotation_stream.md (partial reduction + invariant hoist; 418→367 lines; entire §L3 obstruction source retained), divfree.md (partial reduction; transparent-optimization list dropped, 3 load-bearing claims retained verbatim; 414→413 lines). **Cumulative slice-corpus coverage 8 of 10**; remaining 2 for batch-4: cg_preconditioning_framework + sparse_triangular_solve. Audit template machine-replayable across all three batches. **Cycle-012 surfaced a HIGH-severity line-map defect** (audit verified END boundary but not START → mis-scoped sub-slice; repaired pre-apply; STRONG `phase-1-slice-reduction-audit` skill-promotion candidate at recurrence-3 for meta-phase batch-2).
- **Current measurable counts (cycle-036; the cycle-record.jsonl `counts_after` + the layer `index.md` files are the live source — this chronological list trails off mid-batch-3 and is not maintained per-cycle; cycle-036 footnote: **L1 firm 25→26** (+`floquet-correction` — sixth constructed-operator gate at L1, third firm instance of `nested-constructed-operator-gate` closing the c035 D3 driven-solver Floquet-periodic coverage gap, isomorphic-but-strictly-thinner than `divfree-projector`); **L1>L0 firm themes 23→24** (+`floquet-correction-mutation-rotation` — 4 sub-patterns A/B/C/D, the Mult+AddMult apply + RHS-buffer-recycling closure construction lowering); **L1>L0 on-disk dep-map tally** (`book/src/L1-L0/index.md`): 28 themes = 22 firm + 2 rough-in + 1 partly-constructive + 3 obstruction; concept page `nested-constructed-operator-gate` firm-instances **2→3** (the eigsolve+divfree-projector pair now joined by floquet-correction; second three-deep transitive chain `floquet → ksp → jacobi-smoother` landed alongside the existing `eigsolve → divfree-projector → ksp_solve`); L1/L1-L0 indices refreshed accordingly; SUMMARY.md registers both new chapters. Also cycle-036 D2 (cross-layer-cross-cutter L3-cohort-growth audit): the long-standing `book/src/L3/index.md:38` deferred-audit was SETTLED — verdict gives 6 firm (A) identity-in-form L3-backfill candidates [`assemble-diagonal`, `jacobi-smoother`, `reciprocal`, `elementwise_product`, `normalize`, `divfree-projector`] + 2 L1-promotion-gated (A) [`matrix-weighted-norm`, `bilinear-form`] + 3 substantive (B) [`orthogonalize`, `chebyshev-smoother`, `apply_nonlinear_pencil`] + 7 (C) negative-list operators that should NOT be re-proposed for L3 [`lu_solve`, `back_solve`, `ls-update-column`, 4 NLEPS atoms — disqualified by small-dense coordinate-space axis]; the (C) negative-list is the new anti-recurrence data feed for the `verify-dispatch-scope-not-already-discharged` skill. **L1 firm 22** — cycle-030 was a batch-closing audit-and-cohort-completion cycle, no new L1 firm operators (the c029-landed `ls_update_column` got its standard independent-verifier `verified_against:` audit this cycle; +`back_solve` c027 / +`normalize` c026 / +`ls_update_column` c029 carry-forward) + 2 rough-in (test-coverage-bounded) + 6 rough-in (obstruction). **L1>L0 firm themes +1 cycle-030** (`ls-update-column-mutation-rotation` — the per-column running-QR producer's L0 lowering forward into GMRES `iterative.cpp:634-640` / FGMRES `:813-819` BYTE-IDENTICAL [+5-line offset NOT brace placement, explicit narrative correction vs the c029-audited sibling back-solve narrative which carried the +1-brace-placement claim]; replay→generate→apply sub-pattern structure; destination-collapsed-into-four-input-registers; replay-loop ascending-`i` strict-order finite-precision composition load-bearing; with sibling `back-solve-mutation-rotation` this **COMPLETES the GMRES restart-cycle L1>L0 cohort** `ls_update_column × (j+1) ▷ back_solve` end-to-end). **L1>L0 on-disk dep-map tally (`book/src/L1-L0/index.md` rows 18-44): 27 themes = 21 firm + 2 rough-in + 1 partly-constructive + 3 obstruction.** (The cycle-029 finalize's "19 = 16 firm + 3 obstruction" tally was undercounting — it elided the firm `eigsolve-mutation-rotation` + `apply-nonlinear-pencil-mutation-rotation` + 4 NLEPS-MR + `lu-solve-MR` entries and the `eigsolve-convergence-reason-mapping` partly-constructive entry. The on-disk index is now the audit-trail of record; future cycle counts should anchor here.) **3 firm L1>L0 themes/leaves gained additive `verified_against:` audits cycle-030**: `back-solve-mutation-rotation` (22 rows = 21 supports + 1 partially-supports — the partially-supports F1 is a narrative-only defect re. FGMRES Sub-pattern B "brace-placement +1-line-shift" prose that is factually wrong [GMRES `:653-660` byte-identical to FGMRES `:832-839` per direct `diff`; INDEPENDENTLY CONFIRMED by BOTH the c030 D4 ls-update-column abstractor AND the c030 D1 back-solve audit's own `diff`; routed to c031 lifter/abstractor narrative-repair OQ — theme stays `firm`, firm-on-positive-structure rationale, partial verdict scoped to narrative only NOT structure/citation); `bilinear-form-mutation-rotation` (19 rows, all supports, fully-supported, firm-on-positive-structure); `ls_update_column` L1 leaf (25 rows independent-verifier round, all supports, dual-block convention operating-as-designed — distinct from the c029 harvester self-verify 21-row block at `:630-716`, the c030 audit appended `:718-808`; per-row `audited_at` audit-trail; the `producer-citation-drift-verify-not-self-invoked` recurrence-4 convention codified). **F1 row-flip in `normalize-mutation-rotation`** verdict `does-not-support` → `supports` (the c029 prose correction made the c028 audit's F1 diagnostic stale; the actual on-disk row was at `:481-484` not the stale ledger slug's `:466-469`, a ~15-line shift from c029 prose expansion; row refresh records the prose-now-aligned state, theme stays firm). **3 plain-text → live-link upgrades in `L2-L1/incremental-least-squares-composition-lowering`** at sites `:69`/`:87-88`/`:307` (the now-on-disk c029 firm L1 leaf `ls_update_column`); 3 sibling `ls_update_column-mutation-rotation` mentions at `:85`/`:466`/`:480` were NOT upgraded this cycle because each carries an immediately-adjacent stale "forthcoming" framing word that requires bounded prose rework (now that the theme is on disk) past the mechanical-relink scope — routed to c031 small lifter touch. **The GMRES-restart L1>L0 cohort is now COMPLETE end-to-end**: column-streaming producer `ls_update_column` (L1 firm c029 / L1>L0 firm c030) + terminal restart-close consumer `back_solve` (L1 firm c027 / L1>L0 firm c029 / audited c030) are both fully lowered to L0; the L2>L1 `incremental-least-squares-composition-lowering` (firm c028 / audited c028 / live-links upgraded c030) ties them into the firm L2 `incremental-least-squares` named composition. **L2 firm 9** (unchanged cycle-030). **L2>L1 8 = 7 firm + 1 partly-constructive (unchanged cycle-030)** — c030 was the live-link-upgrade-only touch on `incremental-least-squares-composition-lowering` (theme stays firm; verified_against / status / two-face decomposition untouched). **L3** 9 firm + 2 partial-obstruction, **L4** 4 firm, **L0** 22 chapters — unchanged cycle-030. Phase-1 removals stay 9/10 (sparse_triangular_solve slice-reduction candidacy carried-forward c029→c031; not enacted c030). **`l3-vocabulary-inventory-gap` parent gap CLOSED cycle-028** (trsv resolved-by-obstruction); **cycle-029 gave the resolved trsv leaf its citable home**; **cycle-030 closes the GMRES-restart L1>L0 cohort.** Cycle-030 is the THIRD/FINAL primary cycle of meta-batch-8 — the batch-8 meta-phase fires after this finalize commit and has a substantial agenda (RESOLVED OQ dispositions across all 3 cycles of batch-8 ready for plan-line strikethrough/migration to Closed; the obstruction sub-kind refinement `opaque-library-ownership` vs `enum-only-stub` from c029; the cycle-030 channel-format refinement `verified-against-note-no-leading-quote-of-either-kind` codification; the recurrent `firm-chapter-prose-cites-paraphrased-name-not-literal-anchor` un-promoted friction-ledger candidate; the c030 substantive-findings uptick assessment; the iterative.cpp running-QR localization dispatch-resilience signal from c029 D5+D6 / c030 D4 retries).

Forward indicator (post-cycle-115, **FIRST primary cycle of meta-batch-37 — THE OPENER**; cycles 115/116/117, **the scheduled batch-37 meta-phase fires AFTER cycle-117's finalize; HOWEVER an OUT-OF-BAND meta-phase fires NEXT-AFTER THIS finalize to enact two NEW USER DIRECTIVES of 2026-06-06**; cycle counter does NOT reset): **cycle-115 was the human-commissioned PLATEAU-PROBE batch opener + a user-directive content fix.** 3 reports applied clean (3/3 staging rows == 3 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the 96th consecutive clean staging / 110th consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero build-repairs. **HEADLINE — a residual graded-stack hygiene grounding (+1 reachable) + a PROSE-ONLY user-directive relocation + an OBSERVATION-ONLY plateau-probe that CONFIRMED frontier-exhaustion-of-CURRENT-SCOPE on all 3 fronts.** **D2 (layer-intro-author, build-relevant): residual-untyped hygiene** — typed `rank: firm` + `edges:` on `book/src/L1/fe_collection.md` (incl. `lowers-to`→`L1-L0/fe-collection-construction-rotation`) + frontmatter-PREPEND on the 3 BLAS-1 L1>L0 themes `book/src/L1-L0/{dot,nrm2,scal}-mutation-rotation.md` (each had NO frontmatter; `rank: firm` + `cites-evidence` `depends-on` edges to L0) → **+1 reachable** (132→133; the `fe_collection`→theme `lowers-to` edge RESCUED that theme from baseline detritus), detritus 127→126; promoted OQ `graded-stack-prose-status-inference-masks-untyped` (the 4 nodes were prose-`## Status`-inferred, never in the untyped-60 list, so `untyped` HELD 60). **D3 (layer-intro-author, build-relevant): named-shape-groups relocation — DIRECT USER DIRECTIVE (2026-06-06)** — PROSE-ONLY trim of the *general* named-shape-groups rule/rationale/migration-note out of `book/src/{L4,L3,L2}/linear_combination.md`, leaving each to USE the notation + LINK to `design/l4_calculus.md` §1.2.1 (critic verified §1.2.1/§1.2.2/§4.1 already carry the complete rule — NOTHING added there; reachability-neutral, HELD 133); promoted OQ `named-shape-groups-general-rule-restatement-cohort-extent` (the pattern spans 27 files in 3 tiers — now governed by the forthcoming semantic-consolidation USER DIRECTIVE, superseding the producer's original "defer Tier C"). **D1 (cross-layer-cross-cutter, OBSERVATION-ONLY): plateau-probe** — `## Proposed-changes` = None; independently re-derived frontier-exhaustion on BOTH graded-stack axes WITHOUT trusting the batch-36 meta assessment. **VERDICT: exhaustion CONFIRMED on all 3 fronts** — (1) reachability: all 23 STRONGER-GARBAGE members covered by a ratified RE1-RE8 baseline-exception, no missed faithful inbound `depends-on`; (2) forward vocabulary: all 8 `promotion_frontier` members genuinely obstruction-/demand-gated; (3) coverage: every in-scope item has a chapter or is a tracked deferral (`build_mesh` is the standing candidate-(c) Mesh-wrapper deferral, NOT a hole). 2 benign linter-semantics flags carried: roots=36 = 12 columns × 3 levels (reconciles the "40 columns" headline); `boundary-mode.{L0,L1,L4}` double-counted as ROOT + `promotion_frontier` (inflates the "8" by 3). The verdict is **exhaustion-OF-CURRENT-SCOPE, NOT terminal** — the integrator appended a READ-CONTEXT note: NEW USER DIRECTIVE B (2026-06-06, open-all-feature-fronts-simultaneously post-consolidation) fires the demand-gate trigger, so the deferred fronts are now SLATED TO OPEN. **MEASURABLE COUNT DELTAS this cycle: NONE on the firm histogram** (D2 frontmatter-only hygiene, D3 prose-only, D1 observation-only — no new authored vocabulary, no node promotion). The measurable movement is purely Axis-2: `reachable` 132→133 (+1, D2's `fe_collection`-theme grounding), `detritus` 127→126 (−1); `rank_violations` HELD 0, `unresolved` HELD 0, `untyped` HELD 60, `roots` HELD 36. All layer tallies UNCHANGED from cycle-114. **CARRY FOR THE IMMINENT OUT-OF-BAND META-PHASE (fires NEXT, enacting the two 2026-06-06 directives): Directive A** = evolve `book/src/design/l4_calculus.md` into an ACTIVELY-MANAGED semantic surface (liveness/unification/consolidation analogous to the graded-stack machinery; D3 was the pilot; the 27-file cohort OQ now under this directive; REORDER the calculus surface BEFORE the L4 section in SUMMARY.md); **Directive B** = answer the plateau ASK by opening ALL remaining deferred feature fronts simultaneously (waveguide-mode, boundary-mode, fe_space siblings, mesh-wrapper, …) to exploit shared exploration lifting — sequenced AFTER the consolidation. Plus the D1 plateau verdict + 2 benign linter flags + the carried OQs (D2 `graded-stack-prose-status-inference-masks-untyped`; D3 cohort-extent; D1's 4 plateau-probe OQs). **PROCESS: clean 3-report serial application; `cargo make book` exit 0; all D3 §1.2.1 anchor-links resolve, no KaTeX/table breakage; no stub materialized; no build-repair needed; only the pre-existing benign `Potential incomplete link` KaTeX false-positive WARNs.** **NEXT: the OUT-OF-BAND meta-phase fires after this finalize as a SEPARATE dispatch (NOT the scheduled c117 meta), enacts the two directives, WILL change role-specs → WILL require a session restart (that is its concern, NOT this finalize's — this finalize made NO `.claude/agents/` change).**

Forward indicator (post-cycle-066, **THIRD/FINAL primary cycle of meta-batch-20**; cycles 064/065/066, **the batch-20 meta-phase fires AFTER this cycle-066 finalize as a separate dispatch — it is now DUE**; cycle counter does NOT reset): **cycle-066 CLOSED THE FE-SPACE SUB-SPINE FRONTIER FOR BATCH-20 — `essential_dofs` landed FIRM as the 3rd sub-spine member + the L1>L0-theme re-anchor of `fe_space` consumers COMPLETED (replace-and-propagate now done at BOTH the operator-surface layer [c065 D1] and the theme layer [c066 D3]).** 4 reports applied clean (4/4 staging rows == 4 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the FORTY-SEVENTH consecutive clean staging / SIXTY-FIRST consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero build-repairs. **HEADLINE — the FE-space sub-spine is now 3 firm L1 operators (`fe_collection` ▷ `fe_space` ▷ `essential_dofs`) with all three L1>L0 edges complete, and the `fe_space`-consumer re-anchor is finished at both the operator and theme layers.** **D1 (harvester, build-relevant): `essential_dofs` PROMOTED FIRM L1** (`book/src/L1/essential_dofs.md` — the boundary-condition true-dof-set member of the FE-space sub-spine; `fe_space` ▷ `essential_dofs` in the sub-spine DAG; two variant axes (attribute-wildcard explicit-list vs `[-1]`; per-level-hierarchy application); firm-on-positive-structure; resolves the c064 `fe-space-essential-dofs-straddles-mfem-owned-boundary` straddle OQ toward a self-standing entry, WARRANT=YES contradicting the c064 "lean noted-property" guess) + `L1/index.md` dep-map TABLE row + the deferred-sibling bullet FLIPPED to a firm FE-space-sub-spine cohort bullet + SUMMARY. **D2 (abstractor, build-relevant): `essential-dofs-construction-rotation` LANDED FIRM L1>L0** (`book/src/L1-L0/essential-dofs-construction-rotation.md` — LHS L1 `essential_dofs` → RHS L0 dbc block `multigrid.hpp:92-101`; construction-head-lowers / dof-resolution-tail-MFEM-owned split; firm-on-positive-structure; the live link to D1's `essential_dofs.md` RESOLVES same-cycle) + `L1-L0/index.md` theme row + SUMMARY. **D3 (lifter, build-relevant): the `fe_space`-consumer THEME-LAYER re-anchor COMPLETED** — `fe-operator-assemble-mutation-rotation` (2 re-anchors: abstract "finite-element space" → live `fe_space` value; stale "no L1 form yet" prose-correction) + `weak-form-term-rotation` (opaque `A(space,·)` → live `fe_space`, kernel `A` left libCEED-owned) re-anchored to firm `fe_space`; PLUS the `multigrid.hpp:22-72`→`:22-73` close-brace citation-hygiene fix at all THREE `fe_space.md` loci (:84/:182/:203); CLOSED 2 OQs (`fe-space-opaque-param-l1-l0-theme-reanchor-to-firm-fe-space` + `multigrid-hpp-template-close-line-citation-hygiene`), appended 1 new follow-on OQ (`fe-space-construction-rotation-forward-ref-now-on-disk-plain-text-to-live-link`). **D4 (layer-intro-author, build-relevant): `L1/index.md` count refresh** — L1 firm grand total **33→34** (27 main + 4 FE-assembly + 3 FE-space), FE-space sub-spine subsection header **2→3** (`essential_dofs` folded into the sub-spine narrative as the boundary-condition dof-set member; DAG `fe_collection` ▷ `fe_space` ▷ `essential_dofs`). **MEASURABLE COUNT DELTAS this cycle: L1 firm 33→34** (`essential_dofs`); **L1>L0 firm themes +1** (`essential-dofs-construction-rotation`); the FE-space sub-spine grows 2→3 firm L1 operators; the `fe_space`-consumer re-anchor now COMPLETE at both operator + theme layers (replace-and-propagate done); all other counts UNCHANGED from cycle-065 (L2 firm 21 + 1 partly-constructive, L2>L1 firm 21, L3 firm 17 + 4 partial-obstruction, L3>L2 firm 6, L4 firm 7 + 1 rough-in `solve_family`, L4>L3 firm 8, L4 outer-driver rows 5, L0 chapters 22, Phase-1 removals 9/10). **FE-space sub-spine backlog now: `fe_space_hierarchy` (#4, lower fan-out) + the `eliminate_*` DofSet→`essential_dofs` cross-ref replace-and-propagate follow-on + the `fe-space-construction-rotation` forward-ref→live-link upgrade (the new c066 D3 OQ).** **PROCESS: clean 4-report serial application; `cargo make book` exit 0 ~90s; both new pages render; SUMMARY wires both; all same-cycle cross-links resolve (D2's L1>L0 theme → D1's L1 op; D3's `fe_space` theme/citation re-anchors); no linkcheck2 dead-link; no stub materialized; no plain-text downgrade; no build-repair needed; only the pre-existing KaTeX false-positive `Potential incomplete link` WARNs in `design/l4_calculus.md` (3 sites, unchanged this cycle).** **CITATION-TOOLING FRICTION SIGNAL — STRONG, 3-of-3 batch-20 recurrence: the `palace-codemap` `read_range` ±1 line-drift RECURRED AGAIN this cycle (the `multigrid.hpp:22-72`→`:22-73` close-brace off-by-one that D3 fixed; citecheck `--anchor` returns [ok] for BOTH ranges so the off-by-one is NOT mechanically caught, only by deliberate on-disk hand-Read). This is now 3-of-3 batch-20 cycles touching FE-space source (c064 fespace.hpp `:66-74`→`:67-75`; c065 multigrid.hpp/fespace.hpp; c066 multigrid.hpp `:22-72`→`:22-73`) — a STRONG corroborated signal for the batch-20 meta-phase: likely warrants a friction-ledger entry + a localization-preference note (prefer on-disk `Read` over `palace-codemap` `read_range` for FE-source brace-boundary citations).** **NEXT: the batch-20 meta-phase fires after this finalize as a SEPARATE dispatch (aggregating cycles 064/065/066); the cycle counter does NOT reset; this finalize runs NO meta-phase housekeeping.**

Forward indicator (post-cycle-065, **SECOND primary cycle of meta-batch-20**; cycles 064/065/066, **the batch-20 meta-phase fires AFTER cycle-066's finalize as a separate dispatch**; cycle counter does NOT reset): **cycle-065 CONTINUED THE FE-SPACE SUB-SPINE FRONTIER — `fe_collection` landed FIRM as the 2nd sub-spine member + the opaque-parameter re-anchor was ENACTED (replace-and-propagate complete for the front-opening).** 4 reports applied clean (4/4 staging rows == 4 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the FORTY-SIXTH consecutive clean staging / SIXTIETH consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero build-repairs. **HEADLINE — the FE-space sub-spine is now 2 firm L1 operators (`fe_space` + `fe_collection`) with their L1>L0 edges complete, and the 4 FE-assembly/BC consumers now cross-ref firm `fe_space`.** **D2 (harvester, build-relevant): `fe_collection` PROMOTED FIRM L1** (`book/src/L1/fe_collection.md` — the p-multigrid FE-collection order schedule `(p, dim, mg_max_levels, coarsening, mat_lor, family) → [FECollection]`, the ordered per-level collection list `ConstructFiniteElementSpaceHierarchy` feeds one-per-level into `fe_space`; producer→consumer across the `[FECollection]` boundary; firm-on-positive-structure; 3 variant axes + 6 laws; `mat_lor: Bool` signature param per repair; `palace/fem/multigrid.hpp:22-73`) + `L1/index.md` dep-map row + FE-space sub-spine cohort bullet + SUMMARY. **D3 (abstractor, build-relevant): `fe-collection-construction-rotation` LANDED FIRM L1>L0** (`book/src/L1-L0/fe-collection-construction-rotation.md` — LHS L1 `fe_collection` schedule value → RHS L0 `ConstructFECollections` template body; 5-piece forward rewrite; 3 variant-axis rewrite cases; `std::reverse` load-bearing reorganization) + `L1-L0/index.md` theme row + SUMMARY; the same-cycle live link to D2's `fe_collection.md` RESOLVES. **D1 (lifter, build-relevant): the opaque-parameter re-anchor ENACTED** — the 4 consumer L1 entries (`fe_assemble`/`weak_form_term`/`eliminate_essential_bc`/`eliminate_rhs`) re-anchored to firm `fe_space` via live cross-refs over the true-dof axis N (`fespace.hpp:96`); all 4 stay firm (replace-and-propagate of the c064-unblocked opaque-parameter re-anchor; opened the L1>L0-theme-layer follow-on OQ `fe-space-opaque-param-l1-l0-theme-reanchor-to-firm-fe-space`; RESOLVES-BY-LANDING the c064 D1/D2 operator-surface re-anchor OQs). **D4 (layer-intro-author, build-relevant): `L1/index.md` count refresh** — L1 firm grand total **32→33** (27 main + 4 FE-assembly + 2 FE-space), FE-space sub-spine **1→2** (`fe_collection` folded into the sub-spine narrative as the upstream collection-order-schedule producer). **MEASURABLE COUNT DELTAS this cycle: L1 firm 32→33** (`fe_collection`); **L1>L0 firm themes +1** (`fe-collection-construction-rotation`); the FE-space sub-spine grows 1→2 firm L1 operators; the opaque-parameter operator-surface re-anchor ENACTED across the 4 consumers; all other counts UNCHANGED from cycle-064 (L2 firm 21 + 1 partly-constructive, L2>L1 firm 21, L3 firm 17 + 4 partial-obstruction, L3>L2 firm 6, L4 firm 7 + 1 rough-in `solve_family`, L4>L3 firm 8, L4 outer-driver rows 5, L0 chapters 22, Phase-1 removals 9/10). **FE-space sub-spine backlog now: `essential_dofs` (#3 — straddles the MFEM-owned-boundary), `fe_space_hierarchy` (#4), + the L1>L0-theme re-anchor follow-on (OQ `fe-space-opaque-param-l1-l0-theme-reanchor-to-firm-fe-space`).** **PROCESS: clean 4-report serial application; `cargo make book` exit 0 ~90s; both new pages render; SUMMARY wires both; all same-cycle cross-links resolve (D3's L1>L0 theme → D2's L1 op; D1's 4 re-anchors → `fe_space`; D4's sub-spine narrative → all four FE-space pages); the D3-theme forward-ref in D2's index row stayed plain-text per repair (linkcheck2-safe regardless of D2/D3 order); no linkcheck2 dead-link; no stub materialized; no plain-text downgrade; no build-repair needed; only the pre-existing KaTeX false-positive `Potential incomplete link` WARNs in `design/l4_calculus.md` (unchanged this cycle).** **CITATION-TOOLING FRICTION SIGNAL for the batch-20 meta-phase: the `palace-codemap` `read_range` ±1 line-drift RECURRED this cycle (caught on `multigrid.hpp`/`fespace.hpp`; all ranges verified directly on-disk before landing) — a corroborating signal alongside the cycle-064 `fespace.hpp` drift; the meta-phase should weigh the recurrence (2-of-2 batch-20 cycles touching FE-space source) in its tooling-friction assessment.** **NEXT: cycle-066 (batch-20 position 3/3, the LAST primary cycle before the batch-20 meta-phase) continues the FE-space sub-spine frontier (`essential_dofs` #3 / `fe_space_hierarchy` #4 + the L1>L0-theme re-anchor follow-on); the batch-20 meta-phase fires after cycle-066's finalize as a separate dispatch.**

Forward indicator (post-cycle-056, **SECOND primary cycle of meta-batch-17**; cycles 055/056/057, **the batch-17 meta-phase fires AFTER cycle-057's finalize as a separate dispatch**; cycle counter does NOT reset): **cycle-056 was a LIGHT HYGIENE/PROBE cycle — 3 reports applied clean (3/3 staging rows == 3 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the THIRTY-SEVENTH consecutive clean staging / FIFTY-FIRST consecutive clean split-integrator cycle).** **NO MEASURABLE COUNT DELTA this cycle** (D3 = citation hygiene; D1/D2 = observation findings). **HEADLINE — the `map_solve` superset is DEFERRED + the L3-L2/L2-L1 index tables are CONFIRM-CLEAN.** **D1 (cross-layer-cross-cutter, observation-only): the `map_solve` superset probe → do NOT author `map_solve.md`.** The three-way solver-family shape classification: **driven = operator-varying MAP (1 witness, `drivensolver.cpp` uniform `Sweep` with `SetOperators`-inside-loop)**; **transient = state-threaded FOLD → a DISTINCT future `fold_solve`/`time_step_fold` combinator** (recorded as a SPINE FINDING — the spine will eventually need a fold combinator; NOT forced to land now, and explicitly NOT a 2nd map witness — the fold-vs-map guard correctly refused to over-unify); **eigenmode = opaque single solve** (no family iteration in Palace's driver). With only **1 operator-varying-map witness**, the `map_solve` superset is **DEFERRED** below the **2-witness authoring gate** (`skills/disciplined-cross-pipeline-combinator-mining-gate`); the recorded `map_solve` Haskell candidate shape stays unpromoted. The `DrivenSolver::SweepAdaptive` PROM/adaptive path (`drivensolver.cpp:231+`) is the cheap **2nd-witness probe** (could meet the gate → license authoring `map_solve.md`, or fold a reduced-order-model state → close to the transient `fold_solve` shape) — a batch-18 candidate. **D2 (cross-layer-cross-cutter, observation-only): the L3-L2/L2-L1 index-table-staleness sweep → CONFIRM-CLEAN** — all **16/16** L3-L2 + L2-L1 index-table status cells MATCH their theme-file `## Status` lines (row/file reconciliation 5/5 + 11/11); **the cycle-055 L4-L3 in-place-promotion drift did NOT propagate to these deletion-swept tables** (the deletion-sweep authoring mechanism leaves no desync window, unlike L4-L3's in-place promotions). This **partially closes the cycle-055 D8 OQ** `index-table-status-cell-drifts-when-theme-file-promoted` for the L3-L2 + L2-L1 tables (D8 stays OPEN for the not-yet-audited L1/L1-L0/L4/L3/L2/L0 tables); the empirical finding is that a finalize-time re-sweep would flag 0/16 here, so the **lightweight promotion-time guard** ("when flipping a `## Status` line, update the matching index cell") is preferred over a heavyweight finalize-time re-sweep (batch-17 meta-phase input). **D3 (lifter): the `fe_assemble.md:147`+`:257` citation residual was fixed** — the essential-BC pinpoint `laplaceoperator.cpp:215-217`→`:216-217` (`:215` is a stray closing brace; `:216`=ParOperator construction, `:217`=SetEssentialTrueDofs), corrected at TWO occurrences, on-disk re-read confirmed; the legitimate `:184-223` GetStiffnessMatrix broader span was deliberately NOT touched. **MEASURABLE COUNT DELTAS this cycle: NONE** — all firm/rough-in counts UNCHANGED from cycle-055 (L1 firm 29, L4 firm 6 + 1 rough-in `solve_family`, L4>L3 firm 7, L2 firm 21 + 1 partly-constructive, L2>L1 firm 17, L3 firm 17 + 3 partial-obstruction, L3>L2 firm 13, L1>L0 firm + the libCEED obstruction annotation, L0 chapters 22, Phase-1 removals 9/10). **The solver test-load continues to generate spine findings without forcing the spine** — the disciplined-defer outcome (map_solve NOT authored from 1 witness; the transient fold correctly held distinct from the driven map) is the gate working end-to-end. **PROCESS: clean 3-report serial application (all observation-only or citation-hygiene; no structural change); build `cargo make book` exit 0 ~90s; `fe_assemble.html` renders with the corrected `:216-217` (0 stray `:215-217`); no dead links; only the pre-existing KaTeX false-positives in `design/l4_calculus.md` + markdown-table HTML WARNs.** **NEXT: cycle-057 — the LAST primary cycle before the batch-17 meta-phase — picks up the deferred items (solve_family `L3/solve_family` image + L3>L2 hop; `fe-operator-assemble-mutation-rotation` firm-flip; the `SweepAdaptive` 2nd-map-witness probe; a possible `fold_solve` transient combinator thread-opener); the batch-17 meta-phase fires after cycle-057's finalize.**

Forward indicator (post-cycle-064, **FIRST primary cycle of meta-batch-20**; cycles 064/065/066, **the batch-20 meta-phase fires AFTER cycle-066's finalize as a separate dispatch**; cycle counter does NOT reset): **cycle-064 OPENED THE FE-SPACE/MESH-CONSTRUCTION L1 FRONT — the user's resolved strategic steer (the batch-19 meta-phase's `open-FE-space-mesh-construction-L1-front` ASK answered GO).** 4 reports applied clean (4/4 staging rows == 4 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the FORTY-FIFTH consecutive clean staging / FIFTY-NINTH consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero build-repairs. **HEADLINE — FE-space construction is now FIRM at L1 + L1>L0, the prime substrate under all 5 solver pipelines.** **D1 (cross-layer-cross-cutter, observation-only, no book change): FE-space front-scoping survey** — partitioned the Palace construction surface into in-scope / out-of-scope / MFEM-owned-read-as-given; verdict ONE prime entry `fe_space` (NOT a `fe_space`+`fe_collection`+`dof_map` split); appended the FE-space sub-spine backlog pick-list OQ (`fe-space-sub-spine-backlog-pick-list`) homing the fan-out-ranked deferred siblings. **D2 (harvester, build-relevant): `fe_space` PROMOTED FIRM L1** (`book/src/L1/fe_space.md` — `(mesh, collection) → FiniteElementSpace[N]`; de-Rham family variant axis H1/H(curl)/H(div)/L2 all 4 witnessed; firm-on-positive-structure; NO `dof_map` mirror) + `L1/index.md` dep-map row + SUMMARY. **D3 (abstractor, build-relevant): `fe-space-construction-rotation` LANDED FIRM L1>L0** (`book/src/L1-L0/fe-space-construction-rotation.md` — LHS `fe_space` → RHS `FiniteElementSpace` ctor; construction-lowers / dof-bookkeeping-MFEM-owned split; 4 de-Rham rewrite cases) + `L1-L0/index.md` theme row + SUMMARY; the same-cycle live link to D2's `fe_space.md` RESOLVES. **D4 (layer-intro-author, build-relevant): `L1/index.md` NEW "FE-space sub-spine — 1" subsection** (the cohort home for `fe_space`, distinct vocabulary front from the FE-assembly sub-spine which stays 4) + grand-total count refresh **31→32** (27 main + 4 FE-assembly + 1 FE-space). **MEASURABLE COUNT DELTAS this cycle: L1 firm 31→32** (`fe_space`); **L1>L0 firm themes +1** (`fe-space-construction-rotation`); a NEW "FE-space sub-spine" cohort opened (1 firm operator); all other counts UNCHANGED from cycle-063 (L2 firm 21 + 1 partly-constructive, L2>L1 firm 21, L3 firm 17 + 4 partial-obstruction, L3>L2 firm 6, L4 firm 7 + 1 rough-in `solve_family`, L4>L3 firm 8, L4 outer-driver rows 5, L0 chapters 22, Phase-1 removals 9/10). **PROCESS: clean 4-report serial application; `cargo make book` exit 0 ~90s; both new pages render (`book/book/html/L1/fe_space.html` + `book/book/html/L1-L0/fe-space-construction-rotation.html`); SUMMARY wires both; all same-cycle cross-links resolve (D3's L1>L0 theme → D2's L1 op + D4's index subsection live-links to both); no linkcheck2 dead-link; no stub materialized; no plain-text downgrade; no build-repair needed; only the pre-existing KaTeX false-positive `Potential incomplete link` WARNs in `design/l4_calculus.md` (unchanged this cycle).** **CITATION-TOOLING FRICTION SIGNAL for the batch-20 meta-phase: a `palace-codemap` `read_range` ±1 line-drift on `fespace.hpp` (`:66-74` vs the on-disk-verified `:67-75`) cost a citation-reconciliation round this cycle; the artifact landed consistent at `:67-75`** (verified directly against the on-disk file: line 67=template, 68=signature, 75=closing brace, 76=destructor). **NEXT: cycle-065 (batch-20 position 2/3) continues the FE-space sub-spine frontier — fan-out-ranked deferred siblings `fe_collection`/`essential_dofs`/`fe_space_hierarchy` + the opaque-parameter re-anchor replace-and-propagate pass (now unblocked); the batch-20 meta-phase fires after cycle-066's finalize.**

Forward indicator (post-cycle-063, **THIRD/FINAL primary cycle of meta-batch-19**; cycles 061/062/063, **the batch-19 meta-phase fires AFTER THIS cycle-063 finalize as a separate dispatch**; cycle counter does NOT reset): **cycle-063 was a CLOSE-OUT/HYGIENE cycle — 3 reports applied clean (3/3 staging rows == 3 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the FORTY-FOURTH consecutive clean staging / FIFTY-EIGHTH consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero build-repairs.** **NO MEASURABLE COUNT DELTA this cycle** (D1 = record-only NO-ENTRY warrant; D2 = single cross-ref edit; D3 = dep-map self-summing-table hygiene, no count change). **HEADLINE — the FE-assembly + frequency-operator cohort's UPWARD PROPAGATION is SETTLED-BY-WARRANT: `fe_assemble` declines an L2 entry (degenerate anti-mirror on BOTH axes), the `L1/index` dep-map now SELF-SUMS (31 in-table, all firm rows on-table), and the `solve_family`↔`assemble_frequency_operator` map_solve scope-boundary cross-ref is FIRMED (mutual link).** **D1 (abstractor, record-only, no book change): `fe_assemble` NO-ENTRY-BY-WARRANT** — declines an `L2/fe_assemble.md` floor because it is a degenerate mirror on BOTH anti-mirror axes (a no-carry concatenation-homomorphism fold + an opaque-libCEED per-term leaf `A`; the vocabulary does not shift upward); authoring it would manufacture the exact degenerate-mirror smell the §1d vocabulary-shift-redirect warrant rejects. Recorded as OQ `l2-fe-assemble-NO-ENTRY-by-warrant` (routed to the batch-19 meta-phase for formal close + STOP-PROPOSING-negative-list addition, paralleling the c060 D2 `L2/fold_solve` no-floor-warrant; single reopen condition = a future Palace-owned L2 tensor-contraction respine of the libCEED leaf). **D2 (lifter, build-relevant): `book/src/L4/solve_family.md` map_solve scope-note re-anchored** — the firm per-ω operator now named `assemble_frequency_operator` via live link `../L1/assemble_frequency_operator.md` (the deferred c062 cross-ref OQ `assemble-frequency-operator-map-solve-scope-boundary-cross-ref-refresh` RESOLVED-BY-LANDING; reciprocal L1→L4 ref already present → mutual link; `solve_family` status/laws/axes untouched, no index-cell update owed). **D3 (layer-intro-author, build-relevant): `book/src/L1/index.md` self-summing-table fix** — added the `fe_assemble` dep-map row (in-table firm count 30→31, now self-sums to the grand total with NO off-table +1 note; the cosmetic c062 OQ `l1-index-fe-assemble-needs-dep-map-row-for-self-summing-table` RESOLVED-BY-LANDING), updated the reconciliation note, + appended a §Working-Notes cycle-063 bullet recording the 3 NO-ENTRY upward-propagation warrants (`fe_assemble`, `weak_form_term`, `assemble_frequency_operator`) routing their FORMAL close to the batch-19 meta-phase. **MEASURABLE COUNT DELTAS this cycle: NONE** — all firm counts UNCHANGED from cycle-062 (L1 firm 31, L2 firm 21 + 1 partly-constructive, L2>L1 firm 21, L3 firm 17 + 4 partial-obstruction, L3>L2 firm 6, L4 firm 7 + 1 rough-in `solve_family`, L4>L3 firm 8, L4 outer-driver rows 5, L0 chapters 22, Phase-1 removals 9/10; FE-assembly sub-spine 4 firm L1 operators + the affine-operator-assembly driven sibling `assemble_frequency_operator`). **PROCESS: clean 3-report serial application (D1 record-only; D2 single cross-ref edit; D3 ×3 in-place table-hygiene edits); both D2/D3 build-relevant links resolve (`../L1/assemble_frequency_operator.md` + `./fe_assemble.md`); build `cargo make book` exit 0; no dead links; no stub materialized; no plain-text downgrade; no build-repair needed; only the pre-existing KaTeX false-positives in `design/l4_calculus.md` + markdown-table HTML WARNs.** **FRONTIER STATUS FOR THE BATCH-19 META-PHASE: the FE-assembly + frequency-operator cohort is descent-complete — 4 firm L1 operators + the driven affine-operator sibling, the L1>L0 edge COMPLETE, and the L2 floor declined-by-warrant (`fe_assemble` upward-descent COMPLETE-BY-WARRANT, NO L2 entry); the batch landed `weak_form_term` (c061) + `assemble_frequency_operator` (c062) FIRM and was clean throughout (0 critic failures / 0 unrepairable / 0 gate-hits / 0 deferrals / 0 rejections across 061/062/063).** **NEXT: the batch-19 meta-phase (aggregating 061/062/063) fires after THIS finalize as a SEPARATE dispatch. Formal-close candidates queued: the `l2-fe-assemble-NO-ENTRY-by-warrant` (c063 D1 → CLOSE RESOLVED-BY-WARRANT + add `L2/fe_assemble` to STOP-PROPOSING negative list, paralleling `L2/fold_solve`); the `weak_form_term`-own-L2 disposition (flagged-forward, not settled); the c063 D2/D3 RESOLVED-BY-LANDING cross-ref + self-summing-table OQs to CLOSE to the resolved index; the optional low-value `solve-family-name-assemble-frequency-operator-at-all-per-ω-rebuild-loci` breadth-pass candidate (c063 D2 NEW, non-blocking).**

Forward indicator (post-cycle-061, **FIRST primary cycle of meta-batch-19**; cycles 061/062/063, **the batch-19 meta-phase fires AFTER cycle-063's finalize as a separate dispatch**; cycle counter does NOT reset): **cycle-061 was the FE differential-operator vocabulary cycle — 3 reports applied clean (3/3 staging rows == 3 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the FORTY-SECOND consecutive clean staging / FIFTY-SIXTH consecutive clean split-integrator cycle).** **MEASURABLE COUNT DELTA: L1 firm 29→30, L1>L0 firm themes +1.** **HEADLINE — the `weak_form_term` cohort LANDED, the genuinely-new FE differential-operator vocabulary, pull-driven by the magnetostatic curl-curl term; the FE-assembly sub-spine grows 3→4 firm L1 operators.** **D1 (harvester, build-relevant): `weak_form_term` PROMOTED FIRM L1** (`book/src/L1/weak_form_term.md` — the `(coefficient, differential-operator)` pair naming one weak-form contribution `a(u,v) = (Q·𝒟u, 𝒟v)`; `𝒟` the differential-operator variant axis, `Q` the material coefficient; it is the **element type of `fe_assemble`'s opaque term list**; grounded in `palace/models/laplaceoperator.cpp:184-194` Gradient/DiffusionIntegrator/epsilon_func + `palace/models/curlcurloperator.cpp:170-181` Curl/CurlCurlIntegrator/muinv_func). The 2 opaque-`WeakFormTerm` rough-in notes in `fe_assemble.md` (the signature `terms` bullet + the Dependencies bullet) RE-ANCHORED to live `./weak_form_term.md` links — reference-upgrades only, replace-and-propagate per the vocabulary-shift redirect (the fold's structure + laws unchanged). Dual-registered in `L1/index.md` (dep-map TABLE row + cohort bullet, harvester-owned) + SUMMARY.md. **D2 (abstractor, build-relevant): `weak-form-term-rotation` LANDED FIRM L1>L0** (`book/src/L1-L0/weak-form-term-rotation.md` — the L1 `{coefficient, diff_op}` pair → L0 `AddDomainIntegrator<T>(Q)` template-type + runtime-arg dispatch rewrite; identity-lowers/kernel-opaque split; 2 grounded cases Gradient/DiffusionIntegrator + Curl/CurlCurlIntegrator; mass/div-div pending-pull). Links D1's `weak_form_term.md` (resolves — D1 landed first per-report this cycle). **D3 (cross-layer-cross-cutter, observation-only, no book change): driven/transient OUTER machinery probed — ONE batch-19 LICENSE-FUTURE candidate** `assemble_frequency_operator` (the operator-domain image of the firm tensor-domain `linear_combination`; the driven per-ω `A = a0·K + a1·C + a2·M + A2` scalar-weighted sum of fixed operators via `BuildParSumOperator`, `drivensolver.cpp:176-177` → `spaceoperator.cpp:522-528` → `rap.cpp:780-786`; single positive witness, pull-gated; anti-mirror disposition = extend the EXISTING firm `linear_combination` operand-category axis rather than mint a new slug — no stub owed) + the **driven/transient outer machinery RECORDED spine-complete-except-affine-operator-assembly** (RHS/excitation eval + post-step field collection + transient coefficient setup all solver-specific or already-spine-covered). 2 OQ-ledger intake entries appended. **MEASURABLE COUNT DELTAS this cycle: L1 firm 29→30** (`weak_form_term`); **L1>L0 firm themes +1** (`weak-form-term-rotation`); all other counts UNCHANGED (L2 firm 21 + 1 partly-constructive, L2>L1 firm 21, L3 firm 17 + 4 partial-obstruction, L3>L2 firm 6, L4 firm 7 + 1 rough-in `solve_family`, L4>L3 firm 8, L4 outer-driver rows 5, L0 chapters 22, Phase-1 removals 9/10). **KNOWN-LAG NOTE: the `L1/index.md` §Vocabulary-cohort header PROSE count ("FE-assembly sub-spine 3→4"; grand total "29→30") lags — the dep-map TABLE tally is correct (harvester applied its own row+bullet per the dual-registration partition); only the prose-header count is stale, flagged for a future layer-intro-author touch (OQ `l1-index-fe-assembly-sub-spine-count-prose-refresh-3-to-4`, carried from c054).** **PROCESS: clean 3-report serial application (D1 firm L1 + re-anchors; D2 firm L1>L0; D3 observation-only); the D1→D2 same-cycle cross-link + the `fe_assemble` re-anchor all resolved at the single finalize build (D1 landed first per-report); build `cargo make book` exit 0 ~90s; both new pages render (`book/book/html/L1/weak_form_term.html` + `book/book/html/L1-L0/weak-form-term-rotation.html`); no dead links; no stub materialized; no build-repair needed; only the pre-existing KaTeX false-positives in `design/l4_calculus.md` + markdown-table HTML WARNs.** **NEXT: cycle-062 (batch-19 position 2/3) — the batch-19 meta-phase fires after cycle-063's finalize. Candidates carried: the `assemble_frequency_operator` operator-valued-`linear_combination` LICENSE-FUTURE item (pull-gated, awaits a 2nd-pipeline probe); the `weak_form_term` concept-page-on-second-consumer reconsideration OQ; the `L1/index.md` cohort-header prose-count refresh (layer-intro-author touch).**

Forward indicator (post-cycle-060, **THIRD/FINAL primary cycle of meta-batch-18**; cycles 058/059/060, **the batch-18 meta-phase fires AFTER this cycle-060 finalize as a separate dispatch**; cycle counter does NOT reset): **cycle-060 was a CLEAN CLOSE-OUT cycle — 2 reports applied clean (2/2 staging rows == 2 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the FORTY-FIRST consecutive clean staging / FIFTY-FIFTH consecutive clean split-integrator cycle).** **NO MEASURABLE COUNT DELTA this cycle** (D1 = intra-file prose-sync; D2 = observation-only consistency-confirm). **HEADLINE — the `fold_solve` L3 descent is tied off: the L3/index spectrum prose is now consistent at SIX profile shapes / FOUR partial-obstruction, and the L2/`fold_solve` NO-FLOOR-WARRANT is CONFIRMED descent-complete.** **D1 (layer-intro-author, build-relevant): the `L3/index` §Semantics-overlay spectrum PROSE synced to the authoritative count tally.** The c059 D1 abstractor (sole L3-touching dispatch) landed `fold_solve` and bumped the single authoritative count-tally at `:64` to **17 firm + 4 `partial-obstruction`** with shape (f) fully enumerated — but the spectrum PROSE at `:15` was left STALE at "Five … shapes" / "three `partial-obstruction`". D1 synced the `:15` prose: "Five → **Six** non-trivial-obstruction shapes", "three → **four** `partial-obstruction`", + the shape (f) `fold_solve` clause (combined carry-threading `sequential-obstruction` + opaque-per-step-leaf on a Palace-authored, L3-rendered sweep — `transientsolver.cpp:77`; distinct from (b) `chebyshev` numerical-recurrence / (c) `eigsolve` opaque-whole-loop / (e) `orthogonalize` variant-conditional) + the closing distinction sentence. A PROSE-only intra-file divergence repair; the `:64` count-tally is the count-owner and was NOT touched (per the c057-meta count-owner guard). **Resolves OQ `l3-index-sixth-obstruction-profile-shape-f-…`.** **D2 (cross-layer-cross-cutter, observation-only, no book change): the L2/`fold_solve` NO-FLOOR-WARRANT is CONFIRMED descent-complete.** The warrant that `fold_solve` owes NO `L2/fold_solve.md` floor (its per-step body is the opaque MFEM `ode->Step` integrator leaf with NO L2 composition — unlike `eigsolve`'s `apply_linop ▷ ksp_solve` body; the substantive L3>L2 content is the outer-sweep erasure, the L2 RHS a by-design fold-by-role form modeled on `L2/eigsolve.md`) was verified CONSISTENT across all three places it must cohere — the L3 entry frontmatter + body, the L3>L2 theme's opaque-leaf rationale, and the absence-by-warrant of `book/src/L2/fold_solve.md` on disk. **Warrant consistent: YES.** Authoring `L2/fold_solve.md` would manufacture the exact degenerate-mirror smell the §1d vocabulary-shift-redirect warrant rejects → recommend AGAINST. Routed to the batch-18 meta-phase for formal close (RESOLVED-BY-WARRANT). **MEASURABLE COUNT DELTAS this cycle: NONE** — all firm counts UNCHANGED from cycle-059 (L1 firm 29, L2 firm 21 + 1 partly-constructive, L2>L1 firm 21, L3 firm 17 + 4 partial-obstruction, L3>L2 firm 6, L4 firm 7 + 1 rough-in `solve_family`, L4>L3 firm 8, L4 outer-driver rows 5, L0 chapters 22, Phase-1 removals 9/10). **PROCESS: clean 2-report serial application (D1 prose-only; D2 observation-only); build `cargo make book` exit 0 ~90s; no dead links; only the pre-existing KaTeX false-positives in `design/l4_calculus.md` + markdown-table HTML WARNs.** **FRONTIER STATUS FOR THE BATCH-18 META-PHASE: the two-combinator MAP/FOLD spine is mature end-to-end (`solve_family` MAP + `fold_solve` FOLD across L4 / L4>L3 / L3); 3-of-5 solver pipelines characterized vs the spine (electrostatic/magnetostatic = `solve_family` MAP witnesses, transient = `fold_solve` FOLD witness, driven = `map_solve` single-witness deferred, eigenmode = NOT-A-WITNESS opaque-single-solve); the batch was clean throughout (0 critic failures / 0 unrepairable / 0 gate-hits / 0 deferrals / 0 rejections across 058/059/060).** **NEXT: the batch-18 meta-phase (aggregating 058/059/060) fires after THIS finalize as a SEPARATE dispatch. Formal-close candidates queued: the `map_solve` permanent-single-witness spine-coverage finding (c058 D3 NON-DISCHARGE); the eigenmode SPINE-COMPLETE record (c059 D3); the L2/`fold_solve` NO-FLOOR-WARRANT descent-complete confirm (c060 D2); the c059-resolved OQs + the c060 shape-(f) prose-sync OQ to CLOSE to the resolved index; the D1 (c059) `fold-solve-time-step-body` slug-hygiene rename candidate + the cosmetic-residue `:82` "sole act" phrasing intake (c060 D2 repairer-routed); the `L3_L2_firm` jsonl-count alignment note.**

Forward indicator (post-cycle-059, **SECOND primary cycle of meta-batch-18**; cycles 058/059/060, **the batch-18 meta-phase fires AFTER cycle-060's finalize as a separate dispatch**; cycle counter does NOT reset): **cycle-059 was the FOLD-SOLVE L3-IMAGE cycle — 3 reports applied clean (3/3 staging rows == 3 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the FORTIETH consecutive clean staging / FIFTY-FOURTH consecutive clean split-integrator cycle).** **HEADLINE — `fold_solve` descends from L4 to L3 with its lowering theme; the eigenmode pipeline is recorded SPINE-COMPLETE.** **D1 (abstractor): the L4 firm `fold_solve` gets its L3 image + L3>L2 theme.** NEW `book/src/L3/fold_solve.md` (status `partial-obstruction` — the **4th** L3 partial-obstruction after `chebyshev`/`eigsolve`/`orthogonalize`; the carry-threading time sweep + the opaque per-step `ode->Step` body BOTH resist the iteration rotation while the per-step body lifts; the `chebyshev`/`eigsolve` shape) + NEW `book/src/L3-L2/fold-solve-time-step-body.md` (status `firm` — the outer-sweep-erasure + opaque-per-step-leaf theme; the **5th substantive / non-identity** L3>L2 theme, the carry-threaded sibling of the four erasure-scope roots). The standalone-L3-entry question RESOLVED **L3-ENTRY** (OQ `fold-solve-l3-entry-vs-dissolution-home`): unlike `solve_family`'s cleanly-lifting family loop (NO-ENTRY c057, dissolution-theme-is-the-L3-home), `fold_solve`'s loop carries a genuine obstruction → it earns its own standing L3-vocabulary home. The L3/index §Semantics-overlay grew a **sixth** non-trivial obstruction shape **(f)** (combined carry-threading-sequential-obstruction + opaque-per-step-leaf on a Palace-authored, L3-rendered sweep — distinct from (b)/(c)/(e)). 3 coupled re-anchors: the L4 `fold_solve` §"Lowers to" deferral → resolved live link to the L3 entry + 2 deferral notes in `L4-L3/fold-solve-time-step-dissolution` resolved to live links. **D2 (layer-intro-author): the L4/index cohort prose refreshed firm** — `fold_solve` registered firm, the §Vocabulary-cohort header firm count corrected 6→7 (the live L4 firm tally per `L4/index.md` Firm-at-L4 = 7 + 4 outer-driver), the §Active-frontier `fold_solve` thread-opener re-stated firm with a live `../L3/fold_solve.md` link (kept LIVE — D1 co-landed it this cycle). Resolves OQ `fold-solve-l4-index-vocabulary-cohort-firmness-split-refresh`. **D3 (cross-layer-cross-cutter): the eigenmode pipeline recorded SPINE-COMPLETE / NOT-A-WITNESS.** The `L4/solve_family.md` §Status scope-note updated: **eigenmode was PROBED and is NOT a witness** of either `solve_family` (map) or `fold_solve` (fold) — the eigenmode driver calls the opaque `eigen->Solve()` once (`eigensolver.cpp:367`), with no operator/RHS family to map and no state-threaded solve-march to fold; its only outer loop is a post-processing *readout* map over the already-converged eigenpair set (`eigensolver.cpp:425-471`). **The eigenmode outer machinery adds NO new spine vocabulary** — a clean spine-coverage finding (the solver test-load generating findings without forcing the spine, the disciplined-defer pattern working end-to-end). Two intake OQs appended (`eigenmode-outer-machinery-SPINE-COMPLETE-no-combinator-witness` records the closure; `eigenmode-hybrid-two-phase-refine-single-witness-refine_solve-candidate` a low-priority future candidate). **MEASURABLE COUNT DELTAS this cycle: L3 firm UNCHANGED at 17, partial-obstruction 3 → 4 (`fold_solve`); L3>L2 firm 5 → 6 (`fold-solve-time-step-body`, consolidated post-refactor count); L4 firm 7 (prose count corrected 6→7 — the firm-flip landed c058, this cycle's D2 fixes the lingering "6" in the index prose); L4>L3 firm 7 UNCHANGED; L2 firm 21 + 1 partly-constructive UNCHANGED; L2>L1 firm 21 UNCHANGED; L1 firm 29 UNCHANGED; L0 chapters 22 UNCHANGED; Phase-1 removals 9/10 UNCHANGED.** **PROCESS: clean 3-report serial application (D1 structural — 2 new files + 4 index/SUMMARY/re-anchor edits; D2/D3 prose-only); build `cargo make book` exit 0 ~90s; all same-cycle cross-links resolve (D2→D1's `../L3/fold_solve.md`, the 3 D1 coupled re-anchors, D3's `./fold_solve.md`); no dead links; only the pre-existing KaTeX false-positives in `design/l4_calculus.md` + markdown-table HTML WARNs.** **NEXT: cycle-060 — the LAST primary cycle before the batch-18 meta-phase — then the batch-18 meta-phase fires after cycle-060's finalize (formal-close candidates queued: the `map_solve` permanent-single-witness spine-coverage finding; the eigenmode SPINE-COMPLETE record; the D1 slug-hygiene + shape-(f) §Semantics-overlay-prose intake notes).**

Forward indicator (post-cycle-055, **FIRST primary cycle of meta-batch-17**; cycles 055/056/057, **the batch-17 meta-phase fires AFTER cycle-057's finalize as a separate dispatch**; cycle counter does NOT reset): **cycle-055 was the batch-17 OPENING cycle — 8 reports applied clean (8/8 staging rows == 8 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the THIRTY-SIXTH consecutive clean staging / FIFTIETH consecutive clean split-integrator cycle).** **HEADLINE — the `solve_family` propagation landed + the FE-assembly sub-spine advanced (all 3 FE-assembly operators now firm).** **D1 (harvester): `solve_family` L4 combinator full entry landed** (`book/src/L4/solve_family.md`, `## Status: rough-in (test-coverage-bounded)`) — the headline fixed-operator map-over-RHS-family combinator at L4 (the pure-map degenerate of `iterate_while`); the c054 plain-text dep-map row flipped to a live link; 2 specializations (electrostatic + magnetostatic sweeps) authored as notes-in-entry per combinator-as-entry. **D2 (abstractor): its firm L4>L3 `solve-family-map-dissolution` theme landed** (`book/src/L4-L3/solve-family-map-dissolution.md`, `## Status: firm`) — the outer map-shell dissolution completing the solve_family propagation, composing strictly above `ksp-solve-driver-dissolution`; firm-on-structure (no sequential obstruction — family members independent, distinct from the per-solve outer-driver fold). **D3 + D4 (harvesters): `eliminate_rhs` + `eliminate_essential_bc` promoted FIRM L1** (`book/src/L1/eliminate_rhs.md` `b' = b − K·x_bc` + `book/src/L1/eliminate_essential_bc.md` square-operator BC-pin) — the FE BC-elimination siblings; **all 3 FE-assembly operators (`fe_assemble` c054 + the 2 eliminate_* c055) are now firm**, the FE sub-spine opened c053 is substantively populated. **D5 (abstractor): `fe-assemble-libceed-boundary-obstruction` L1>L0 annotation landed** (`## Status: obstruction (opaque-library-ownership)`) — settles the c053 libCEED-boundary OQ; libCEED owns the element-local quadrature kernel + COO materialization, Palace owns the fold/dispatch/shuffle/BC-elimination; `fe_assemble` STAYS FIRM (the obstruction is a sub-term below the fold's opaque leaf). **D6 (lifter): re-anchored `fe-operator-assemble-mutation-rotation`** to the firm `fe_assemble` LHS (citation drift `:73-75`/`:93-95`→`:77`/`:97`/`:75-76`; theme STAYS rough-in pending the eliminate_* leg re-anchors). **D7 (count-owner, partially-applied) + D8 (corrective lifter): index-table reconciliation** — D7 owned the L4/index rough-in tally (0→1 for solve_family; L4 firm STAYS 6) + the L1/index FE-cohort flip (all-3-firm; grand total 27→29); D7's edit #2 (L4-L3 tally) was correctly SKIPPED — superseded by D8, which fixed **3 STALE L4-L3 index-table status cells** (`krylov-step`/`gmres`/`fgmres` were promoted firm c008/c020/c021 but the table never updated; gmres slug→live link) + the corrected consolidated L4>L3 tally **6→7** (the count was always tracking 6; the table is now consistent at 7 firm). **MEASURABLE COUNT DELTAS this cycle: L1 firm 27 → 29** (+`eliminate_rhs` +`eliminate_essential_bc`); **L4 rough-in +1** (`solve_family`; **L4 firm UNCHANGED at 6**); **L4>L3 firm 6 → 7** (+`solve-family-map-dissolution`, AND the on-disk table reconciled to 7 firm by D8's stale-cell fix — the count was tracking 6, the table now agrees); **L1>L0 +1 obstruction annotation** (`fe-assemble-libceed-boundary-obstruction`, opaque-library-ownership; `fe_assemble` stays firm). All other firm counts UNCHANGED (L2 firm 21 + 1 partly-constructive, L2>L1 firm 17, L3 firm 17 + 3 partial-obstruction, L3>L2 firm 13, L0 chapters 22, Phase-1 removals 9/10). **The batch-17 arc OPENS: solve_family propagation landed (L4 entry + firm L4>L3 dissolution) + the FE-assembly sub-spine advancing (fe_assemble + eliminate_rhs + eliminate_essential_bc all firm + the libCEED-boundary obstruction annotated).** **PROCESS: clean 8-report serial application with a corrective wave-4 lifter (D8) handling a needs-revision in-cycle; one surgical build-repair (leaked `</content></invoke>` tags removed from `eliminate_essential_bc.md`); build `cargo make book` exit 0, all 5 new chapters render, no dead links.** **NEXT: cycle-056 continues batch-17; the meta-phase fires after cycle-057's finalize.**

Forward indicator (post-cycle-054, **THIRD/FINAL primary cycle of meta-batch-16**; cycles 052/053/054, **the batch-16 meta-phase fires AFTER cycle-054's finalize as a separate dispatch**; cycle counter does NOT reset): **cycle-054 was the batch-16 FINAL cycle — 2 reports applied clean (2/2 staging rows == 2 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the THIRTY-FIFTH consecutive clean staging / FORTY-NINTH consecutive clean split-integrator cycle).** **HEADLINE — the `solve_family` combinator MINED at 2-of-N + `fe_assemble` promoted to FIRM L1.** **D1 (combinator-miner): `solve_family` mined** — the fixed-operator map-over-RHS-family combinator at L4 outer-driver vocabulary (the pure-map degenerate of `iterate_while`), landed as **ONE rough-in dep-map row** in `book/src/L4/index.md` (plain-text slug; full entry + 2 specializations + `L4-L3/solve-family-map-dissolution` theme deferred to **batch-17**). Fixed-operator-only (2-of-5 pipelines: electrostatic + magnetostatic); driven breaks shared-operator-capture (`drivensolver.cpp:176/:180`) → the general `map_solve_over_(operator,rhs)_family` superset is batch-17. The single-witness→2nd-probe→mine gate (c052 single-witness → c053 magnetostatic discharge → c054 mine) worked end-to-end. **D2 (harvester): `fe_assemble` promoted to FIRM L1** — the integrator-fold `K = Σ_i A(term_i)` over opaque `A`; opens the FE-assembly sub-spine (`book/src/L1/fe_assemble.md` + L1 index FE-cohort bullet→firm + SUMMARY + L1-L0 dep-map row; slug-collision held distinct from BLAS-2 `bilinear-form`; corrected `AddSubOperator` anchors `:71-77`/`:91-97`). Deferred operators `weak_form_term`/`eliminate_*` stay rough-in. **MEASURABLE COUNT DELTAS this cycle: L1 firm +1** (`fe_assemble` → **27**); **L4 rough-in +1** (`solve_family` outer-driver row, registered rough-in pending batch-17 full entry + 2 specializations + dissolution theme; L4 firm unchanged at 6, L4>L3 firm unchanged at 6, L4 outer-driver rows 4 → 5). All other firm counts UNCHANGED (L2 firm 21 + 1 partly-constructive, L2>L1 firm 10, L3 firm 17 + 3 partial-obstruction, L3>L2 firm 5, L1>L0 themes [+0 firm; the FE thread-opener stays rough-in], L0 chapters 22, Phase-1 removals 9/10). **The batch-16 arc: refactor-pass-complete (c052) → solver-test-load probes (c053) → solve-family combinator mined + `fe_assemble` landed (c054); the solver test-load is now productively underway.** **NEXT: the batch-16 meta-phase (aggregating 052/053/054) fires as a separate dispatch.**

Forward indicator (post-cycle-050, **SECOND primary cycle of meta-batch-15**; cycles 049/050/051, **the batch-15 meta-phase fires AFTER cycle-051's finalize**; cycle counter does NOT reset): **cycle-050 was the refactor-pass ENACTMENT cycle under the 2026-06-01 VOCABULARY-SHIFT REDIRECT — 8 reports all applied clean (8/8 staging rows == 8 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the THIRTY-FIRST consecutive clean staging / FORTY-FIFTH consecutive clean split-integrator cycle).** **HEADLINE — 2 L3 combinators PROPAGATED + 4 degenerate non-fold theme pairs DEMOTED to in-line notes (8 theme files deleted).** **Propagation half (D1/D2 harvesters):** authored `book/src/L3/linear_combination.md` (firm L3 combinator — whole-tensor variadic fold over `[(Scalar,Tensor[N])]`, no sequential obstruction; carries its own §"Downward to L2" in-line note — the pre-built demotion home for the c051 fold-family `*-body-identity` themes) + `book/src/L3/inner_product.md` (firm L3 combinator — reduce-to-scalar whole-tensor reduction; upgraded the plain-text `L3/index.md` rough-in row to a live link). **Demotion half (D3/D4/D5/D6 lifters):** the 4 CLEAN non-fold-family degenerate identity-in-named-terms theme pairs DEMOTED to in-line §"Downward to L2"/§"Downward to L1" notes on their standalone L3/L2 operator entries — `assemble-diagonal` (D3), `elementwise_product` (D4), `reciprocal` (D5), `normalize` (D6); **8 theme files deleted** (`{assemble-diagonal,elementwise-product,reciprocal,normalize}-{body,leaf}-identity.md`), the load-bearing non-laws preserved verbatim in the in-line notes (matrix-free approximate-diagonal non-law; total-bijective-identity justification; `s = 1/|z|²` complex-intermediate note; fused-composite `nrm2 ∘ scal` identity). **NO operator-chapter deletions** (all four are standalone leaves/gates/composites with no fold-parent). **Verify-body half (D8 cross-layer-cross-cutter, observation-only, NO book mutation):** audited the 2 constructed-operator-gate pairs (`divfree-projector`/`jacobi-smoother`) → verdicts feed c051: `jacobi-smoother-body-identity` (L3>L2) DEMOTE-OK + `jacobi-smoother-leaf-identity` (L2>L1) DEMOTE-OK + `divfree-projector-body-identity` (L3>L2) DEMOTE-OK + **`divfree-projector-leaf-identity` (L2>L1) KEEP-substantive** (the one genuine fusion rotation in the projector chain — step-4 `apply_linop ▷ axpy` de-fuse/re-fuse to `Grad->AddMult`, anchored `divfree.cpp:185`/`:180-181`). **The degenerate-cohort denominator is corrected 18 → 17** (the D8 −1 for the KEPT divfree leaf). **Count-owner (D7 layer-intro-author, SOLE):** consolidated tallies + cohort-narrative reconciliation (c050-vs-c051 split). **COUNT DELTAS: L3 firm 15 → 17** (+`linear_combination` +`inner_product`); **L3>L2 firm 17 → 13** (4 thin `-body-identity` themes deleted); **L2>L1 firm 21 → 17** (4 thin `-leaf-identity` themes deleted). The firm-theme DROPS are a **vehicle-change** (theme file → in-line §"Downward" note), NOT a coverage regression — each operator's L_{n+1}>L_n rotation remains captured in-line; OQ `c050-firm-theme-count-drop-is-vehicle-change-not-coverage-regression` promoted so a future census does not mis-flag it. **L1 firm 26, L4 firm 6 + 6 firm L4>L3 + 4 outer-driver rows, L0 chapters 22, Phase-1 removals 9/10 — UNCHANGED.** **C051 REMAINING REFACTOR WORK (the LAST cycle before the batch-15 meta-phase):** the fold-family demotions (`scal`/`axpy`/`axpby`/`axpbypcz` → the new firm `linear_combination`; `dot` → the new firm `inner_product`; `nrm2` STAYS a do-NOT-merge consumer) + the L3-leaf re-expression through the new D1/D2 L3 combinators + the DEMOTE-OK `jacobi-smoother` (both edges) + `divfree-projector-body-identity` (L3>L2 ONLY — the L2>L1 edge is KEEP per D8, must stay reachable from the L3 entry so the genuine rotation is not orphaned). **The meta-gated leaf-chapter deletions (delete-vs-redirect-stub for the held L2/L3 `dot`/`scal`/`axpy`/`axpby`/`axpbypcz` leaves) remain HELD for the batch-15 meta-phase ratification** (`collapsed-leaf-disposition-convention-cohort-wide`). **PROCESS: clean 8-report serial application — ZERO leaks this cycle** (the c049 `specialized-agent-direct-write-to-book-during-dispatch` did NOT recur); a **cross-dispatch dangling-link coordination pattern** surfaced + was handled clean (4 demotions deleting sibling-cross-referenced files; the per-report integrators defensively de-linked surviving live links in-flight under the hard dangling-live-link gate, all resolved clean before finalize) — a reusable signal for future multi-deletion cycles. retroactive-budget global = 0; ZERO build-repairs of the substantive kind (finalize physically removed the 4 de-linked-but-present plain-text dep-map rows D4/D5 had left in the L3-L2/L2-L1 tables — a residual cohort-coupling cleanup, build-safe; build `cargo make book` exit 0; the mandatory dead-link sweep for all 8 deleted slugs returned ZERO live links; only the pre-existing KaTeX `Potential incomplete link` false-positives in `design/l4_calculus.md` + the pre-existing markdown-table HTML-tag WARNs in unchanged files). **Meta-batch-15 meta-phase fires after cycle-051 finalize** (aggregating cycles 049/050/051).

Forward indicator (post-cycle-049, **FIRST primary cycle of meta-batch-15**; cycles 049/050/051, **the batch-15 meta-phase fires AFTER cycle-051's finalize**; cycle counter does NOT reset): **cycle-049 was the FIRST refactor-pass cycle under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`; CLAUDE.md §Methodology invariants ⟢) — 3 reports all applied clean (3/3 staging rows == dispatched-ready; the cycle-018 staging-completeness gap did NOT recur for the THIRTIETH consecutive clean staging / FORTY-FOURTH consecutive clean split-integrator cycle).** **HEADLINE — the refactor pass STARTED: 2 combinator-as-entry inversions landed** (the redirect's "the combinator is the entry, leaves are specialization notes; lowerings are vocabulary translations not 1:1 renames" reframe applied to the two BLAS-1 fold families): D1 inverted `book/src/L2/linear_combination.md` (4 surgical edits — the fold/combinator is now the primary L2 entry, `axpy`/`axpby`/`axpbypcz`/`scal` are specialization notes), D2 inverted `book/src/L2/inner_product.md` (8 surgical edits across 5 sites — fold-primary, `dot` a specialization note; `nrm2` stays a thin *consumer* of inner_product, explicitly **NOT** a fold member) + appended a plain-text `L3/inner_product` rough-in dep-map row to `book/src/L3/index.md` (the upward-propagation target, correctly NOT a live link — the file is a cycle-050 harvester authoring). **COUNTS ESSENTIALLY UNCHANGED this cycle — this is a refactor pass, NOT a count bump**: the two L2 combinator entries were **INVERTED in framing, not added**; no new firm operators/themes landed; the leaf-collapse + L3-combinator authoring + thin-theme demotion are **cycle-050 enactment**, mapped this cycle but not enacted. **L1 firm 26, L2 firm 21, L2>L1 firm 21, L3 firm 15 + 3 partial-obstruction, L3>L2 firm 17, L4 firm 6 + 6 firm L4>L3 + 4 outer-driver rows, L0 chapters 22, Phase-1 removals 9/10 — ALL UNCHANGED.** **CYCLE-050 ENACTMENT BACKLOG MAPPED** (the controlling forward plan): (i) **D3's cohort-wide degenerate-lowering audit found the cohort is 18 themes (9 pairs), NOT 12** — the load-bearing scope finding; demoting only 12 + stranding 6 re-creates the mirrored floor the redirect corrects, so cycle-050 enactment MUST cover all 18 (D3 CYCLE.md §"The demotion worklist" §A/§B/§C is the consumable; 2 themes `divfree-projector`/`jacobi-smoother` are marked verify-body-before-demoting); (ii) ~6 L2 leaf-collapses (the `axpy`/`axpby`/`axpbypcz`/`scal`/`dot` family members folded into the combinator entries, disposition gated on the cohort-wide `collapsed-leaf-disposition-convention` OQ — delete-vs-redirect-stub, ratified at the batch-15 meta-phase); (iii) 2 L3-combinator authorings (`L3/linear_combination`, `L3/inner_product` — the upward propagation of the combinator-as-entry inversion). **D1/D2/D3 RECONCILIATION ALREADY AGREED — `nrm2` stays a consumer of inner_product, NOT a fold member** (D2 flagged the divergence-risk vs D3; CLOSED-as-agreement at integration — both follow the OQ-ledger `:595` carve-out; no divergence for the meta-phase to escalate, recorded so cycle-050 enactment validates rather than re-litigates). **PROCESS: clean serial application of same-cycle sibling inversions — NO wave conflict** (D1/D2 touch disjoint files `linear_combination.md`/`inner_product.md`; D1's edit A4 + D2's Site-6 mutually align the reciprocal §"Sibling fold" cross-references, so no cycle-050 alignment touch is needed for that note). **NEW FRICTION flagged for the batch-15 meta-phase — `specialized-agent-direct-write-to-book-during-dispatch` RECURRENCE (D2)**: D2 originally leaked a direct write to `book/src/L2/inner_product.md` during the dispatch phase (write-authority violation); the repairer reverted the leak (file restored to committed HEAD) and reconstructed the inversion as 8 authorized `edit:`-fenced proposed-changes blocks, which integrator-per-report then applied against the restored file — the applied state is correct, but the recurrence is logged for the meta-phase. retroactive-budget global = 0; ZERO build-repairs (build `cargo make book` exit 0, ~91.7s; all 3 changed chapters re-rendered; linkcheck2 green; only the pre-existing KaTeX `Potential incomplete link` false-positives in `design/l4_calculus.md` + the pre-existing markdown-table HTML-tag WARNs in unchanged files). **Meta-batch-15 meta-phase fires after cycle-051 finalize** (aggregating cycles 049/050/051). NOTE: the batch-14 meta-phase's "L4 substantially complete / strategic-pivot ASK" framing is RETIRED by the 2026-06-01 redirect — "complete" there meant *relative to the lifted inner-kernel cohort*, not the in-scope target; the refactor pass (cycles 049/050) precedes new forward-frontier work; no pivot to the downstream burn effort yet.

Forward indicator (post-cycle-012, **third/final primary cycle of meta-batch-2**): L1 vocabulary at **10 firm + 3 rough-in (test-coverage-bounded / lower-layer-shared-vocabulary) + 6 rough-in (obstruction)** (+orthogonalize +chebyshev-smoother cycle-012). **L2 has 2 firm** (+chebyshev-iteration cycle-012). **L3 has 8 firm** (krylov-step from cycle-010 + 7 BLAS-1 entries from cycle-011 wave-1 — BLAS-1 cohort closed; unchanged cycle-012). **L3>L2 has 1 firm theme** (krylov-step-body-identity, promoted cycle-009 via status-inheritance). L4 has 3 firm. **L4>L3 has 1 firm + 2 rough-in** (unchanged cycle-012; the L4/index.md:40 SUPERSEDED cross-reference drift was cleaned by the cycle-012 lifter). L0 bootstrap at **17 chapters** (unchanged cycle-012; bundle-6 candidates #2 + #3 still open). L1>L0 has **7 themes** (5 firm/rough-in + 2 obstruction; eigsolve-mutation-rotation audited cycle-012, confirms-with-refinement, promotion GATED to cycle-013). **4 concept pages corrected/extended cycle-012** (nrm2 stability-claim correction + state-stratification four-stratum / derived-view-hoisting control-flow-boundary / negative-result-slice partial-positive extensions). **L3 index Semantics overlay refreshed + L4 index SUPERSEDED drift cleaned** cycle-012. **Phase 1 slice corpus reducing**: **8 of 10 slices reduced cumulative** (cycle-010 batch-1 + cycle-011 batch-2 + cycle-012 batch-3); remaining 2 routed for batch-4 (cg_preconditioning_framework + sparse_triangular_solve). **Cycle-012 incremental delta over cycle-011**: L1 firm operators **8 → 10** (+orthogonalize +chebyshev-smoother); L2 firm operators **1 → 2** (+chebyshev-iteration — first L2 growth since cycle-005); slice corpus 6 → 8 of 10 reduced (orthog plane-rotation sub-slice fully reduced + plane_rotation_stream partial + divfree partial); 2 lowering-verifier audits both resolved (eigsolve-mutation-rotation confirms-with-refinement/GATED + SLEPc-NEP coordinate-convention resolved-with-refinement); cycle-009 eigsolve OQ cluster fully closed across cycles 010/011/012; net OQ ledger **~8 resolutions/closes + ~17 new opens** across the 8 reports. **Cycle-013+ candidates per cycle-012 integrator-signals**: the GATED cycle-013 abstractor on `eigsolve-getconverged-forwarder-fix-and-gated-promotion` (drops the partly-constructive caveat once Edits 2+3 land); `l1-divfree-projector` harvester (HEADLINE — 6 firm entries cite the now-reduced slice); L3/L4 Chebyshev rows (unblock full chebyshev.md reduction); `orthogonalize-mutation-rotation` + `chebyshev-smoother-mutation-rotation`/`chebyshev-iteration-fusion` lowering themes (abstractor); phase-1 corpus reduction batch-4 (final 2 slices); `plane-rotation-concept-page-canonical-pointer-repoint` (3 firm concept pages still cite the orthog slice as canonical); `gmres.md §L4 v0.6→v0.7` self-rotation (large carry-forward); L0 bundle-6 candidates #2 + #3; `slepc-convergence-reason-lift-sub-theme`; NLEPS at L1+. **Meta-phase batch-2 aggregation fires after this cycle-012 finalize commit** (aggregating cycles 010/011/012); key targets: `l3-l1-directory-naming-structure-policy` closure decision (in-line identity-rotation count ~9+ exceeds threshold); `phase-1-slice-reduction-audit` skill promotion (recurrence-3 + severity escalation); skill-uptake-survey gap (cycle-wide all 8 cycle-012 reports); write-authority phase-boundary violation (layer-intro-author, recurrence-1); partly-constructive / negative-anchor / lifter-scope / dispatch-prompt-framing-drift codifications; per-report-integrator cycle-mislabeling guard (NEW recurrence-1).

Forward indicator (post-cycle-024, **THIRD/FINAL primary cycle of meta-batch-6**; cycles 022/023/024, **meta-phase fires after THIS cycle-024 finalize commit**; cycle counter does NOT reset): **cycle-024 was a high-yield NEP-cohort-completion + eigsolve-chain-completion cycle — EIGHT reports all applied clean (8/8 staging rows — the cycle-018 staging-completeness gap did NOT recur for the SIXTH consecutive cycle; cycle recovered cleanly after a machine crash mid-cycle, all eight per-report integrators had completed + staged before finalize, split-integrator resume verified against the working tree + report frontmatter + OQ-ledger appends).** **HEADLINE 1 — the NEP-interior atom cohort is COMPLETE: L1 firm 17 → 19** (+`nleps_jacobian_action` — the quasi-Newton `T'(λ)`-derivative-pencil action `nleps.cpp:649-669`, the divided-difference `A2'` quasi-Newton approximate-Jacobian recorded as a load-bearing non-law; +`nleps_eigenvalue_correction` — the fifth and FINAL deflated NEP-interior atom `nleps.cpp:672-677`, the per-step `δλ` Rayleigh-functional correction over firm BLAS-1 leaves dot/axpby/scal). Both `firm` via the firm-on-positive-structure escape (single positive site each, every constituent read-not-constructed, laws are syntactic operator-algebra identities). The full per-step quasi-Newton chain `residual → jacobian-action → eigenvalue-correction → deflated-solve → line-search` is now firm at L1 — five firm NEP-interior atoms: `apply_nonlinear_pencil` (c021), `nleps_deflated_residual` (c022), `nleps_deflated_solve` (c023), `nleps_jacobian_action` (c024), `nleps_eigenvalue_correction` (c024). **IN-CYCLE LIVE-LINK UPGRADE**: `nleps_eigenvalue_correction` upgraded its plain-text `nleps_jacobian_action` forward-references to live links after the sibling landed firm earlier in the same cycle (the canonical upgrade-once-sibling-lands path). **HEADLINE 2 — the eigsolve L1→L2→L3 prerequisite chain is COMPLETE: L3 `eigsolve` stub → `partial-obstruction`** (chain step 3 DONE; the cycle-023 stub refined in place to the firm `partial-obstruction` body — per-step `apply_shift_invert` body lifts identity-in-form to the firm L2 body, but the eigen-iteration loop is a witnessed `sequential-obstruction` rooted in opaque-library ownership: SLEPc `EPSSolve` `slepc.cpp:694` / ARPACK `naupd` RCI `arpack.cpp:318` own the loop, no Palace-authored kernel/driver pair analogous to `(krylov-step, ksp_solve)`). This is the SECOND L3 `partial-obstruction` (after `chebyshev` c013, a numerical-stability obstruction) and the FIRST opaque-library one. **CONFIRMS the cycle-021 prediction** `l3-eigsolve-linear-evp-has-no-krylov-step-kernel-analog` — the linear-EVP has no Palace eigen-step kernel; the prediction held against positive Palace source (ready for meta-phase closure). The speculative L4 `eigsolve` solve-monad surface is correctly NOT stubbed (plain-text-defer — only-speculative, no firm L4 anchors converge; below the clearly-implied stub bar). L3 now: 9 firm + 2 partial-obstruction. **HEADLINE 3 — the NLEPS L1>L0 deflation+bare-pencil cohort is COMPLETE: L1>L0 firm themes +2** (+`nleps-deflated-solve-mutation-rotation` — the Schur-complement deflated block-solve mutation rotation `nleps.cpp:504-537`; +`apply-nonlinear-pencil-mutation-rotation` — the bare-pencil residual-apply rotation `nleps.cpp:807-821` `GetResidualNorm` + 4 corroborating sites, the LAST plain-text-forward-referenced NLEPS L1>L0 leaf; one load-bearing Form-A-vs-Form-B accumulation-order bit-difference recorded). Both `firm` via firm-on-positive-structure (no partly-constructive sub-part, no negative anchors, no speculative operators). The NLEPS L1>L0 cohort now: `lu-solve` (c023), `nleps-deflated-residual` (c023), `nleps-deflated-solve` (c024), `apply-nonlinear-pencil` (c024). The two remaining NEP-interior-atom L1>L0 themes (`nleps_jacobian_action`, `nleps_eigenvalue_correction`) batch for a subsequent abstractor pass. **CITATION NOTE**: the `apply-nonlinear-pencil` `nleps.cpp:810-811` residual-comment citation was CONFIRMED correct as-is per dispatch directive — the critic's cosmetic off-by-one finding was itself off by one; no shift applied. **HEADLINE 4 — the NLEPS-deflation L2>L1 pair is COMPLETE: L2>L1 +1 firm (`gram-fold-specialization`) +1 partly-constructive (`deflate-composition-lowering`)** (count 4 → 6 chapters). `gram-fold-specialization` (firm — the matrix-valued sibling of `inner-product-fold-specialization`, the `XᴴX` Gram-build `nleps.cpp:524-531` lowering via per-cell `dot`; conjugation/element-type/weight/single-vs-cross-Gram axes). `deflate-composition-lowering` (**partly-constructive** — inherited verbatim from the L2 `deflate` entry: the Schur-form fan-down is firm-on-positive-structure `nleps.cpp:505-537`, but the Galerkin-core single bare-Gram `lu_solve(XᴴX, c)` fan-down (`op.block = Galerkin`, `S = I`) is the CONSTRUCTIVE sub-part with negative anchors (no bare-Gram deflation solve anywhere in `palace/linalg/*.cpp`, only Schur-wrapped + literature) and an explicit promotion condition (a positive bare-Gram-solve site outside the Schur wrapping). The gate STAYS OPEN — it is the SAME single gate shared by the L2 `deflate` entry (`:774`), the L1>L0 `nleps-deflated-solve-mutation-rotation` theme, and now this L2>L1 theme; all three promote together on the same site; this theme "does not make that call and does not close the gate." **IN-CYCLE LIVE-LINK UPGRADE**: `deflate-composition-lowering` re-linked its two repairer-defanged `gram-fold-specialization` forward-references to live links after the sibling landed firm earlier in the same cycle. **HEADLINE 5 — anchor-drift carry-forward CLOSED: `dot-mutation-rotation` §Sub-pattern D `orthog.hpp:34` → `:35`** (the cycle-023 carry-forward one-token anchor fix; the canonical `IdentityInnerProduct::operator()` returns `LocalDot(x, y)` at `:35`, the `:34` line is the operator-body opening brace; theme stays firm — pure anchor-drift repair, no semantic edit; a `verified_against:` yaml block appended; matches the value the sibling `orthogonalize-composition-lowering` already used correctly; drift was confined to `dot-mutation-rotation`, now closed). **L2 unchanged: 8 firm + 1 partly-constructive [`deflate`] + 1 stub.** **L4 unchanged: 4 firm. L0 unchanged: 22 chapters. Phase-1 corpus unchanged: removals stay 9/10.** **Safety-net gates: retroactive-budget global = 1** (only the `dot-mutation-rotation` anchor-fix + `verified_against:` append = 1 slice; the other 7 rows are new firm/partly-constructive chapters + a stub-refinement-in-place, all 0-retroactive; well below per-slice ≥3 / global ≥4 block thresholds). **implied-component-stub-created = 0** (the L4-eigsolve forward-ref was correctly plain-text-deferred as speculative, not stubbed). **Build: `cargo make book` exit 0, ZERO build-repairs** (all 6 new chapters + the L3 `eigsolve` stub→partial-obstruction refinement + the `dot-mutation-rotation` anchor-fix all SUMMARY-registered + link-clean; the `Potential incomplete link` warnings are ALL pre-existing katex math-display / code-bracket false-positives [4 distinct in `design/l4_calculus.md`, the rest across `concepts/*`+`L4/iterate-while*`+`L3/{dot,nrm2}`+the `[...]`-bearing lowering/fold themes], NONE an actual dead link in any cycle-024-touched file). **Twentieth consecutive clean cycle under the split integrator** (zero deferrals/rejections/rework; zero build-repairs). **Carry-forward to batch-6 meta-phase** (fires after THIS cycle-024 finalize): (i) the **machine-crash recovery** — the cycle recovered cleanly after a mid-cycle machine crash; the split-integrator resume worked (all 8 per-report integrators had staged before the crash; finalize reconciled clean); a recovery, not the normal path — note for the meta-phase as evidence the staging-log channel survives crashes; (ii) the **continuing inline-anchor-drift pattern** — cycle-024 critics found 5 off-by-one drifts in the l3-eigsolve report (arpack.cpp :573/:579/:270; slepc.cpp :1857/:1873, repairer-corrected) + 1 each in 3 other reports; this is the SAME pattern that escalated the batch-5 citation-checker ASK (now `tools/citecheck` exists, enacted 88b7893) — the meta-phase should evaluate whether citecheck is being invoked and whether to make it a per-report gate; (iii) **multiple OQ closures ready for meta-phase migration** (see the closure list below); (iv) the NEP-interior-atom L1>L0 themes (`nleps_jacobian_action`, `nleps_eigenvalue_correction`) batch for a subsequent abstractor pass; (v) the shared `deflate` Galerkin-core promotion gate (still OPEN, still gated on a positive bare-Gram-solve site outside the Schur wrapping; now triple-referenced — L2 `deflate` + L1>L0 `nleps-deflated-solve` + L2>L1 `deflate-composition-lowering`); (vi) the speculative L4 `eigsolve` solve-monad surface (future L4 harvester/abstractor; plain-text-deferred, not yet clearly-implied); (vii) the optional L3>L2 `eigsolve` body-identity audit anchor + the separately-pending L2>L1 `eigsolve-spectral-transform-composition` theme. **Meta-phase batch-6 aggregation fires after this cycle-024 finalize commit** (aggregating cycles 022/023/024).

Forward indicator (post-cycle-023, **SECOND primary cycle of meta-batch-6**; cycles 022/023/024, **meta-phase fires after cycle-024**; cycle counter does NOT reset): **cycle-023 was a high-yield NLEPS/eigsolve vocabulary + lowering cycle — SIX reports all applied clean (6/6 staging rows — the cycle-018 staging-completeness gap did NOT recur for the FIFTH consecutive cycle; cycle recovered after a machine crash, all six per-report integrators had completed + staged).** **HEADLINE: the eigsolve prerequisite chain advances to step 2** — `book/src/L2/eigsolve.md` lands NEW firm (the named shift-invert spectral-transform composition `apply_shift_invert = apply_linop(M) ▷ ksp_solve((K − σM)⁻¹)`; firm-on-positive-structure per the `ksp_solve`/`apply_nonlinear_pencil` precedents; opens the per-step-application half of the L1 opacity, the eigen-iteration loop stays a role reference). **Chain step 2 DONE → step 3 (L3 backfill) now UNBLOCKED** with a materialized `book/src/L3/eigsolve.md` `stub` as its home (implied-component stub per the directive: ≥2 converging refs — L2 §"Lifts to" + `lowers_to:` frontmatter + chain step-3 + the cycle-021 `l3-eigsolve-linear-evp-has-no-krylov-step-kernel-analog` OQ; the L2 §"Lifts to" forward-reference was UPGRADED back to a live link now that the target exists; predicted terminal status `partial-obstruction`). **L1 firm 16 → 17** (+`nleps_deflated_solve` NEW firm — the Schur-complement deflated block solve at `nleps.cpp:504-537`; the next fan-out-ranked NLEPS interior piece; thin composition; firm-on-positive-structure. Its deflate-promotion verdict CONFIRMS — does not change — the cycle-022 L2 `deflate` `partly-constructive` status: the Gram `XᴴX` is built positively but only ever solved Schur-wrapped, the bare `(XᴴX)⁻¹` Galerkin core never appears; `deflate` is NOT promoted, the bare-Galerkin-core promotion still gates on a positive bare-Gram-solve site outside `nleps.cpp`). **L2 firm 7 → 8** (+`eigsolve` NEW firm — see HEADLINE; the L2 dep-map now 10 rows = 8 firm + 1 partly-constructive [`deflate`] + 1 stub [`incremental-least-squares`]). **L1>L0 firm themes +2** (+`lu-solve-mutation-rotation` — the small-dense direct-solve mutation rotation `x = lu_solve(A,b)` → inline Eigen `A.<kernel>().solve(b)` in place; 2 sub-patterns A NLEPS full-pivot-LU / B ROM full-pivot-QR; load-bearing factorization-kernel axis incl. rejected LDLT; +`nleps-deflated-residual-mutation-rotation` — the deflated-residual mutation rotation, 3 sub-patterns A Mult+AddMult→single-pencil-apply collapse / B `MatVecMult ∘ fullPivLu().solve` deflation back-projection / C extended-space two-component fused-`linalg::Dot` residual; discharges both the `lu_solve` and `nleps_deflated_residual` halves of the cycle-022 NLEPS L1>L0 cohort — the remaining cohort items are the `nleps_deflated_solve` L1>L0 theme + the `apply_nonlinear_pencil` L1>L0 leaf, both still plain-text forward-references). **L2>L1 firm 4 (unchanged count) — `orthogonalize-composition-lowering` AUDITED fully-supported, stays firm** (lowering-verifier appended a 17-citation `verified_against:` yaml block to EOF; the only retroactive-evidence revision this cycle; discharges the cycle-019 `orthogonalize-composition-lowering-l2-l1-theme` carry-forward + the L2>L1 side of `orthogonalize-mutation-rotation-l1-l0-theme-should-cite-dot-subpattern-d`). **L1 semantic-motif taxonomy 4 → 6** (the L1-index refresh tightened motif 4 to "absorption-to-result", appended motif 5 "Operator introspection" + motif 6 "Coordinate-space dense direct algebra"; the `apply_linop` motif-7 watch is folded as the future-watch trigger inside the taxonomy-expansion OQ, NOT a standalone speculation OQ; the cycle-022 eigsolve-firm narrative is DISCHARGED into the §Working-Notes bullet). **L3 unchanged: 9 firm** (+1 `stub` `eigsolve`, the chain step-3 home). **L4 unchanged: 4 firm. L0 unchanged: 22 chapters. Phase-1 corpus unchanged: removals stay 9/10.** **Safety-net gates: retroactive-budget global = 1** (only the orthogonalize-composition-lowering audit; the other 5 rows are new-firm-creations + a stub; well below per-slice ≥3 / global ≥4 block thresholds). **implied-component-stub-created = 1** (`L3/eigsolve.md`, applied-discretionarily per the directive). **Build: `cargo make book` exit 0, ZERO build-repairs** (all 4 new chapters [`nleps_deflated_solve`, `eigsolve` L2, `lu-solve-mutation-rotation`, `nleps-deflated-residual-mutation-rotation`] + the L3 `eigsolve` stub SUMMARY-registered + link-clean; the `Potential incomplete link` warnings are ALL pre-existing katex math-display false-positives [41 this build], NONE in any cycle-023-touched file). **Nineteenth consecutive clean cycle under the split integrator** (zero deferrals/rejections/rework; zero build-repairs). **Carry-forward to cycle-024 + batch-6 meta-phase** (fires after 024): (i) the **eigsolve chain step 3 (L3 `eigsolve` backfill)** — now unblocked, the stub is its home, predicted `partial-obstruction`; (ii) the stale `orthog.hpp:34`→`:35` one-token anchor fix in `book/src/L1-L0/dot-mutation-rotation.md` §Sub-pattern D (lines ~160, ~183) for a future dot-mutation-rotation lifter/lowering-verifier pass; (iii) the `nleps_deflated_solve` L1>L0 theme + the `apply_nonlinear_pencil` L1>L0 leaf (still forward-referenced); (iv) the `deflate` bare-Galerkin-core promotion (still gated on a positive bare-Gram-solve site outside `nleps.cpp`); (v) the L2>L1 lowering themes for `gram`/`deflate` (`gram-fold-specialization`, `deflate-composition-lowering`; carried from cycle-022, not picked this cycle). **Meta-phase batch-6 aggregation fires after cycle-024 finalize** (aggregating cycles 022/023/024).

Forward indicator (post-cycle-022, **FIRST primary cycle of meta-batch-6**; cycles 022/023/024, **meta-phase fires after cycle-024**; cycle counter does NOT reset): **cycle-022 was a high-yield vocabulary-buildup cycle — NINE reports all applied clean (7 wave-1 + 2 wave-2; 9/9 staging rows — the cycle-018 staging-completeness gap did NOT recur for the FOURTH consecutive cycle).** **HEADLINE: the BLAS-1 L1>L0 lowering floor is FULLY CLOSED 8/8** — `axpbypcz-mutation-rotation` rough-in→firm (enacting the 3 cycle-021 callsite corrections + correction-6 range fix); the floor OQ `blas1-l1-l0-lowering-theme-gap` CLOSES (all of `dot`/`scal`/`nrm2`/`assemble-diagonal`/`axpby`/`axpbypcz` firm). **L1 firm 13 → 16** (+`lu_solve` NEW firm — small-dense `k×k` direct solve, the HIGH-fan-out Gram-coordinate primitive, firm-on-positive-structure; +`eigsolve` rough-in (test-coverage-bounded)→firm — law-confidence re-eval, the laws are positive-source syntactic identities not convergence-semantics conjectures, **eigsolve prerequisite chain step 1 DONE → L2 entry UNBLOCKED, L3 backfill STAYS BLOCKED**; +`nleps_deflated_residual` NEW firm — the deflation extension of `apply_nonlinear_pencil`, thin composition over `apply_nonlinear_pencil`+`dot`+`nrm2`+`lu_solve`). **L1 rough-in (test-coverage-bounded) cohort 3 → 2** (eigsolve left; matrix-weighted-norm + bilinear-form remain). **L2 firm 6 → 7** (+`gram` NEW firm — the all-pairs `inner_product` fold → `Matrix[k,k]`, firm-on-positive-structure on `nleps.cpp:524-531`) **+ ONE new partly-constructive tier** (`deflate` — the oblique/Galerkin complementary projector; Schur-form pipeline FIRM on `nleps.cpp:505-537`, bare-Galerkin core `I − X(XᴴX)⁻¹Xᴴ` CONSTRUCTIVE from literature + negative-anchor with a stated promotion gate = a positive Palace Galerkin source site; over-unification guard vs `orthogonalize` first-class). **L2 dep-map rough-in cohort 1 → 0 (fully drained)** — the L2 dep-map is now 9 rows = 7 firm + 1 partly-constructive (`deflate`) + 1 stub (`incremental-least-squares`). **L2>L1 firm 3 → 4** (+`orthogonalize-composition-lowering` — the `gs_orthog ∈ {MGS,CGS,CGS2}` variant-dispatch rotation lowering the firm L2 `orthogonalize` `project ▷ subtract` pipeline into the L1 `dot`/`axpy` primitives; cites `dot-mutation-rotation` Sub-pattern D rather than re-deriving the unfused inner-product surface). **L3 unchanged: 9 firm** (the `L3/ksp_solve` + `L2-L1/inner-product-fold-specialization` citation-drift sweep re-anchored 5 distinct inline drifts `iterative.cpp:464→:463`/`:564→:563` + `operator.cpp:623→:624`/`:632→:634`/`:615-616→:616`; both entries stay firm, 0 count delta) + the **L2 Part-intro refresh** (dropped the stale "L3 ksp_solve not on disk" clause, live-linked the firm L3 entry + L3>L2 theme; discharged 2 L2-intro-refresh flags). **L4 unchanged: 4 firm.** **L0 unchanged: 22 chapters.** **Phase-1 corpus unchanged: removals stay 9/10.** **Safety-net gates: retroactive-budget global = 0** (all 9 rows 0-retroactive — 3 new L1 files + 1 L1>L0 firm-flip + 1 new L2 firm file + 1 new L2 partly-constructive file + 1 new L2>L1 theme + 1 pure citation-drift sweep + 1 navigational intro refresh; well below per-slice ≥3 / global ≥4 block thresholds). **SUMMARY auto-fix = 2** (gram + deflate; both harvesters' final dep-map-edit blocks were truncated by a transient API 529 mid-dispatch and surgically completed by the orchestrator [critic-verified faithful], and neither proposed its SUMMARY registration → the per-report SUMMARY-chapter-registration gate added both). **Build: `cargo make book` exit 0, ZERO build-repairs** (all 4 new chapters [lu_solve, nleps_deflated_residual, gram, deflate] + 1 new theme [orthogonalize-composition-lowering] SUMMARY-registered + link-clean; the `Potential incomplete link` warnings are ALL pre-existing katex math-display false-positives across `design/l4_calculus.md`+`concepts/*`+`L3/{dot,nrm2}`+`L4/iterate-while*`+the `$$`-bearing lowering themes — NONE in any cycle-022-touched file). **IN-CYCLE LIVE-LINK UPGRADES** (a recurring same-cycle pattern): `nleps_deflated_residual` upgraded its plain-text `lu_solve` refs to live links after `lu_solve` landed earlier in the cycle; `deflate` upgraded its plain-text `gram` refs to live links after `gram` landed earlier in wave-2. **Eighteenth consecutive clean cycle under the split integrator** (zero deferrals/rejections/rework; zero build-repairs). **Carry-forward to cycle-023/024 + batch-6 meta-phase** (fires after 024): (i) the **L2 `eigsolve` entry** (chain step 2; now unblocked — HIGH priority, gates the L3 backfill); (ii) `nleps_deflated_solve` L1 (the next fan-out-ranked NLEPS piece + the positive Galerkin site that would promote `deflate` partly-constructive→firm); (iii) the `deflate` promotion gate (a positive Palace Galerkin-deflation source site); (iv) the L1>L0 lowering themes for the new L1 ops (`lu-solve-mutation-rotation`, `nleps-deflated-residual-mutation-rotation`); (v) the L2>L1 lowering themes for the new L2 ops (`gram-fold-specialization`, `deflate-composition-lowering`); (vi) the `orthogonalize-composition-lowering` three-way-delegation-boundary lowering-verifier audit; (vii) the transient-API-529 mid-dispatch recovery friction (see cycle-022 integrator-signals — the orchestrator completing truncated dep-map blocks is a recovery, not the normal path; a producer retry/checkpoint on transient API errors is the prevention). **Meta-phase batch-6 aggregation fires after cycle-024 finalize** (aggregating cycles 022/023/024).

Forward indicator (post-cycle-020, **SECOND primary cycle of meta-batch-5**; cycles 019/020/021, **meta-phase fires after cycle-021**; cycle counter does NOT reset): **cycle-020 was a high-yield vocabulary-buildup + corrective-backfill cycle — FIVE firm landings (3 L1>L0 BLAS-1+ themes + 1 new L3 operator + 1 L4>L3 theme firm-flip) + ONE corrective firm-body backfill + ONE L2 Part-intro refresh + ONE lowering-theme audit + ONE cross-layer caller-inventory; all 9 reports `applied` clean (9/9 staging rows — the cycle-018 staging-completeness gap did NOT recur for the second consecutive cycle).** **HEADLINE DEFECT-CORRECTION: the cycle-019 `orthogonalize` L2 fence-truncation defect** — cycle-019's `orthogonalize` harvest (integrated `efb8a0b`) authored the firm chapter body OUTSIDE the report's proposed-changes `edit:` fenced block, so the integrator landed only the 14-line intro; `book/src/L2/orthogonalize.md` was a 14-line intro with NO `## Status` while `L2/index.md:27` dep-map + `SUMMARY.md:41` already asserted `firm`. Caught cycle-020 by the L2-refresh report's critic (the firm-`orthogonalize` dep-map assertions depended on a firm body that did not exist). Corrected by `harvester-orthogonalize-l2-backfill` (staging row #1, full-file replacement recovering the complete firm chapter — `## Status: firm` + Signature + 7 algebraic laws + Variant axes + L2-vs-L1 + Evidence). **This is a defect-CORRECTION, not a firm-count increment — `orthogonalize` was already counted firm in the cycle-019 roadmap; the count is unchanged.** TWO skill-candidates filed (`proposed-changes-fence-encloses-full-body-guard`, `verify-intro-firmness-survey-against-on-disk-status-lines`) + OQ `firm-chapter-body-authored-outside-proposed-changes-fenced-block` (routes to batch-5 meta-phase). **L1 unchanged: 12 firm + 0 partly-constructive + 3 rough-in + 6 rough-in (obstruction).** **L2 unchanged: 5 firm + 0 rough-in** (`orthogonalize` body recovered — no count change; the L2 Part-intro `book/src/L2/index.md` refreshed to 5 firm + 2 stubs [`incremental-least-squares`, `ksp_solve` — both live-linked, materialized 2026-05-28], new §"Vocabulary cohort" subsection, 7-row dep-map). **L2>L1 unchanged: 3 firm themes** — `inner-product-fold-specialization` AUDITED by the cycle-020 lowering-verifier (`verified_against:` block, 15 audit rows, verdict `fully-supported`, keep firm) AND given a `conjugation_caller_inventory:` evidence block in §Condition 5 by the cross-layer dot-callers census (Dot conjugation load-bearing in exactly ONE algorithm — SLEPc-NEP `nleps.cpp`, 4 observable sites; `palace/fem/` has ZERO Dot callers; 11 invisible + 4 observable = 15 caller sites). **L1>L0 now 15 theme files** (+`dot-mutation-rotation` stub→firm, +`scal-mutation-rotation` stub→firm, +`assemble-diagonal-mutation-rotation` NEW firm; was 12 post-cycle-019). **L3 now 9 firm** (+`ksp_solve` — first NON-identity L3 backfill, the outer-driver fold; was 8). **L4>L3 now 2 firm + 1 rough-in** (+`gmres-inner-loop-iterate-while-migration` rough-in→firm; the upstream `gmres.md` §L4 self-rotation landed [slice §L4 v0.7, option (a) `check_stop_into_carry`]; `L4/index.md:44`+`:53` dep-map synced rough-in→firm by integrator-finalize; fgmres sibling STAYS rough-in, held cycle-021). **L4 unchanged: 4 firm.** **L0 unchanged: 22 chapters.** **Phase-1 corpus unchanged: removals stay 9/10.** **Safety-net gates: retroactive-budget global = 0** (all 9 rows: 1 corrective full-file replacement + 2 stub→firm full-file + 1 new L1>L0 file + 1 new L3 file + 1 L4>L3 theme firm-flip-with-LHS-surface + 2 metadata/evidence appends + 1 structural intro refresh; all 0-retroactive; well below per-slice ≥3 / global ≥4 block thresholds). **Build: clean first build EXIT 0; ONE consistency-repair applied by integrator-finalize** — the deferred `L4/index.md:44`+`:53` dep-map firm-sync routed by the lifter (a firm theme carrying a rough-in dep-map annotation is a cross-reference-integrity drift; rebuilt clean). The katex `Potential incomplete link` warnings (`[i,j]`/`[j+1]` interpreted as markdown reference links inside `$$...$$`) are ALL pre-existing math-display false-positives across `design/l4_calculus.md` + `concepts/*` + `L3/{dot,nrm2}` + `L4/iterate-while*` + the `$$`-bearing lowering themes — NONE in any cycle-020-touched file; same katex-misread condition carried since cycle-015. **OQ ledger: a large batch of new opens + RESOLVED/forward markers appended by the 9 per-report integrators** (notable resolvable-on-migration: `l1-l0-dot-lowering-asymmetry`, `assemble-diagonal-mutation-rotation` theme-authoring [:110], the `ksp_solve` constituent of `l3-vocabulary-inventory-gap`, `inner-product-conjugate-pair-reorder-caller-classification` [resolved by the dot-callers census], `gmres-inner-loop-iterate-while-migration` [:192 → resolved cycle-020]; all close/migrate enactment is meta-phase authority — appended as intake). **Sixteenth consecutive clean cycle under the split integrator** (zero deferrals/rejections/rework; one consistency-repair, not a content rework). **Carry-forward to cycle-021 + batch-5 meta-phase** (fires after 021): (i) the **cycle-019 fence-truncation defect** + its two skill-candidates + OQ (HEADLINE meta-phase item — a producer authored-outside-fence pattern that the integrator silently truncated; the per-report-integrator hard-step tightening + a fence-completeness guard are the prevention); (ii) the **recurring inline-anchor-drift** (now across cycle-019/020 in multiple reports — wide ranges correct, pinpoint anchors drift ±1-2 lines; the mechanical codemap-backed citation-checker ASK is increasingly justified) + the **sibling-slice citation re-anchor gap** (cg.md drifted same as gmres.md; skill-candidate `sibling-slice-citation-reanchor-sweep` filed); (iii) `fgmres-inner-loop-iterate-while-migration` now firmable (gmres rotation landed) — cycle-021 lifter; (iv) L2 `ksp_solve` stub→firm (harvester) then the `L3-L2/ksp-solve-outer-driver` theme (abstractor); (v) the `deflate`/`gram` combinator-miner candidate (nleps deflation `X[j]ᴴ·` pattern); (vi) the `orthog.hpp:35` `LocalDot`+`GlobalSum` unweighted-inner-product surface (second inner-product surface out of the Dot-caller census); (vii) eigsolve L3 kernel+driver pair (next L3 inventory item); (viii) `axpby`/`axpbypcz` L1>L0 themes still rough-in (BLAS-1 floor not fully closed); (ix) NLEPS at L1+ (large carry-forward). **Meta-phase batch-5 aggregation fires after cycle-021 finalize** (aggregating cycles 019/020/021).

Forward indicator (post-cycle-019, **FIRST primary cycle of meta-batch-5**; cycles 019/020/021, **meta-phase fires after cycle-021**; cycle counter does NOT reset): **cycle-019 was a high-yield vocabulary-buildup cycle — FIVE firm promotions across L1/L2/L2>L1/L1>L0 + ONE new L0 anchor + ONE OQ-resolution prose-sharpening + ONE combinator-miner family-mode validation; all 8 reports `applied` clean (8/8 staging rows — the cycle-018 staging-completeness gap did NOT recur).** **L1 now 12 firm + 0 partly-constructive + 3 rough-in + 6 rough-in (obstruction)** — `assemble-diagonal` PROMOTED stub→`firm` (operator-to-data `(A: LinearOperator[N,N]) -> Tensor[N]`; 6 algebraic laws + 4 non-laws; element-type live axis + operator-representation absorbed axis + 3 non-axes; exhaustive Evidence across operator/rap/hypre/libceed + the two smoother consumers + the libceed diagonal-assembly test; the fifth L1 motif "operator-to-data introspection"; Firm header `(11)`→`(12)`; the forward-refs `reciprocal`/`elementwise_product` + the `assemble-diagonal-mutation-rotation` L1>L0 theme correctly stay PLAIN-TEXT singletons, NOT stub-materialized). **L2 now 5 firm + 0 rough-in** — `orthogonalize` PROMOTED stub→`firm` (the named `project ▷ subtract` Gram-Schmidt composition lifting the firm L1 leaf; `gs_orthog ∈ {MGS,CGS,CGS2}` variant axis surfaced as visible per-variant batching/sequencing + the collective-shape residual axis `m×1 / 1×m / 2×m` as the load-bearing L2 content; `{ residual, coeffs }` record) AND `inner_product` PROMOTED rough-in→`firm` (the reduce-to-`Scalar` fold `(x,y) -> Scalar ≡ foldl (+) zero (zipWith kernel x y)` unifying L1 leaves `dot` (Hermitian) / `tdot` (unconjugated) / `bilinear-form` (M-weighted member) along the conjugation-convention / element-type / weight-presence axes; conjugation PINNED arg-1 `xᴴ y` with the §"Conjugation convention (pinned)" reconciliation against Palace's arg-2-conjugated `yᴴ x` source; 7 laws incl. split-additivity + the IEEE reduction-tree load-bearing non-law; §"Sibling fold: linear_combination is not subsumed"; §"Consumer: nrm2/matrix-weighted-norm = √∘inner_product"). **The L2 reduce-to-`Scalar` fold cohort now has BOTH operator + lowering theme firm; the rough-in cohort is empty** (was 1 `inner_product` post-cycle-018). **L2>L1 now 3 firm themes** (+`inner-product-fold-specialization` — the three-key dispatch (conjugation kernel / element-type / weight-presence) selecting the L1 leaves; the headline value-level conjugate-pair re-order `xᴴ y = conj(yᴴ x)` [invisible under real-projection: CG `iterative.cpp:395`, Poynting diagonal `boundarymodeoperator.cpp:85`; observable for full-complex: `boundarymodeoperator.cpp:90`] + the pinned-reduction-tree §"Summation-order recording" table carrying the IEEE-non-law detail the L2 entry deferred; **third real chapter under the L2-L1 Part** — `tdot` API-surface-only + `bilinear-form` rough-in member caveats carried as member-level notes, NOT theme-status reductions). **L1>L0 now has 12 theme files** (+`nrm2-mutation-rotation` PROMOTED stub→`firm` — L1 LHS `alpha = nrm2(x)` → L0 RHS one-line `Norml2` template + the four-stage `Dot→MPI_Allreduce→std::abs→std::sqrt` chain; 3 surface forms A/B/C; the `std::abs` guard classified **load-bearing defensive**; element-type real/complex variant-axis collapse; verified against L0 ranges; the firm `divfree-projector-mutation-rotation` theme also got an OPTIONAL cross-link prose-sharpening of its stale-`Mult`-doc-comment bullet — class doc `:28-31` named authoritative L0 site + `:155-190` impl + the `divfree.cpp:176` third witness folded in; ZERO semantics change, divfree firm unchanged). **L3 has 8 firm + 1 partial-obstruction** (unchanged). **L4 has 4 firm + 0 rough-in** (unchanged). **L0 now at 22 chapters** (+`fespace-file`, bundle-6 #6 — `palace/fem/fespace.{hpp,cpp}`; the FiniteElementSpace family L0 source-anchor; forward-refs to libceed basis/restriction + quadrature + geometric factors correctly plain-text per the speculative-vs-clearly-implied bar, folded into OQ `fem-libceed-basis-restriction-l0-anchor`). **The combinator-miner parametric-family mode (cycle-018 enactment) got its FIRST live exercise** (report-6 `combinator-miner-parametric-family`, proposal/mode-validation only, ZERO book mutation): characterized the `inner_product` fold-family (fold-law membership test + 4 parameter axes + 3 over-unification guards) as harvester-input for the in-flight #7 harvester, AND surfaced a **load-bearing mode-gap finding routed to the next meta-phase** (Qualification B: the smoother/constructed-operator-action cohort is parametric but NOT a fold, and the mode gives no positive channel for non-fold parametric families — feeds friction-ledger `combinator-miner-arity-blind-parametric-family-detection` resolution) + confirms the cycle-018 mode DOES address the existing `variant-absorption-vs-instance-counting-policy` meta-agenda OQ. **Phase-1 corpus unchanged: removals stay 9/10.** **Safety-net gates: retroactive-budget global = 0** (all 8 rows: 5 stub/rough-in→firm fresh-surface authoring + 1 new L0 create + 1 firm-entry prose-sharpen + 1 OQ-only proposal; all 0-retroactive; well below per-slice ≥3 / global ≥4 block thresholds). **ZERO build-repairs** (clean first build, exit 0; all 6 touched HTML outputs render; the 40 `Did you forget to define a URL` warnings are ALL pre-existing katex math-display false-positives across `design/l4_calculus.md` + `concepts/*` + `L3/{dot,nrm2}` + `L4/iterate-while*` + the `$$`-bearing lowering themes — NONE in any cycle-019-touched file; this is the same katex-misread-rendered-span condition carried since cycle-015, the post-cycle-018 finalize undercounted it at ~6 by scoping only the l4_calculus subset). **OQ ledger: a large batch of RESOLVED/ADDRESSED markers + forward caveats appended by the 8 per-report integrators** (notably `inner-product-harvester-formalization-and-conjugation-pinning` RESOLVED [plan Now #1/#2, conjugation pinned arg-1], `inner-product-fold-sibling-candidate` RESOLVED, `inner-product-fold-specialization-l2-l1-theme` RESOLVED, `assemblediagonal-is-not-apply-linop-variant` RESOLVED, `nrm2-std-abs-defensive-guard-classification` RESOLVED-load-bearing, `divfree-mult-doc-irrotational-vs-divfree-stale` RESOLVED/closure-ready; all close/migrate enactment is meta-phase authority — appended as intake). **Fifteenth consecutive clean cycle under the split integrator** (zero deferrals/rejections/rework; zero build-repairs). **STAGING-LOG NOTE: the cycle-018 staging-completeness gap did NOT recur** — all 8 cycle-019 reports appended their STAGING.md rows (8/8), the per-report spec step-7 tightening held. **Carry-forward to cycle-020/021 + batch-5 meta-phase** (fires after 021): (i) the **combinator-miner non-fold-parametric-family mode-gap** (report-6 Qualification B — headline batch-5 meta-phase item, feeds the arity-blind friction resolution) + the `variant-absorption-vs-instance-counting-policy` close; (ii) a **critic-vs-repairer citation-renumbering disagreement on orthogonalize-l2** (critic raised 3 spot-line citation-validity warnings; the repairer's independent `read_range`/`search_text` re-verify found the report's ORIGINAL pointers correct — a 3-of-3-same-direction critic line-offset-drift signal worth a friction-window glance if it recurs); (iii) the `classify-variant-axis` SKILL.md:64-68 `gs_orthog` worked-example staleness flagged to `skill-candidates.md` by the orthogonalize-l2 repairer (meta-phase skill-correction); (iv) the **L2-layer-intro refresh** now that L2 has 5 firm ops + named-composition + fold cohorts (two converging refresh flags: `L2-layer-intro-refresh-for-named-compositions` + `L2-layer-intro-refresh-for-fold-cohort` — meta-phase may fold into one); (v) the BLAS-1 `dot`/`scal` L1>L0 themes + `assemble-diagonal-mutation-rotation` L1>L0 theme + `orthogonalize-composition-lowering` L2>L1 theme (forward abstractor work); (vi) `gmres.md §L4 v0.6→v0.7` self-rotation (large carry-forward); NLEPS at L1+ (large); `l3-vocabulary-inventory-gap` (gemv/trsv L3 cohort growth). Suggested cycle-020 dispatches: the `inner-product-fold-specialization` lowering-verifier audit; the BLAS-1 `dot`/`scal` + `assemble-diagonal` L1>L0 themes; the `orthogonalize-composition-lowering` L2>L1 theme; the L2-layer-intro refresh; `gmres.md §L4` self-rotation. **Meta-phase batch-5 aggregation fires after cycle-021 finalize** (aggregating cycles 019/020/021).

Forward indicator (post-cycle-018, **THIRD/FINAL primary cycle of meta-batch-4**; cycles 016/017/018, **meta-phase fires after THIS cycle-018 finalize commit**; cycle counter does NOT reset): **cycle-018 ENACTED the constructive payoff of the human-raised BLAS-1 variadic-fold unification — ONE new firm L2 operator + ONE new firm L2>L1 theme + ONE new L2 rough-in sibling + ONE new concept page + ONE firm-theme provenance correction.** **L1 unchanged: 11 firm + 0 partly-constructive + 3 rough-in + 6 rough-in (obstruction).** **L2 now 3 firm + 1 rough-in** — `linear_combination` PROMOTED rough-in→`firm` (the arity-axis unification of the BLAS-1 scalar-weighted-sum cohort `scal`/`axpy`/`axpby`/`axpbypcz`; full chapter with 7 holding laws + 3 non-laws + 2 variant axes + sibling-`dot`-not-subsumed §; 9 self-verified L0 ranges; `firm` on source-transcription per the chebyshev-iteration firm-without-dedicated-test precedent — the "Vector Sum" tests exercise `linalg::Sum`, a DIFFERENT fold, correctly NOT cited; constructive **prong (b)** of OQ `blas1-variadic-linear-combination-fold-unification` FULLY ENACTED). The 1 rough-in is now `inner_product` (combinator-miner fold-sibling along the **conjugation-convention** axis — NOT arity; `(Tensor[N], Tensor[N]) -> Scalar`, M-weighted member `xᴴ M y`; fuses up from `dot`/`tdot`/`bilinear-form`; ≥3-instance bar met; dep-map row only, plain-text forward-ref to `./inner_product.md`, NO dead link). **L2>L1 now 2 firm themes** (+`linear-combination-fold-specialization` — the arity-dispatch fusion-selection lowering taking L2 `linear_combination` into the L1 fixed-arity specializations; **second real chapter under the L2-L1 Part**). **L3 has 8 firm + 1 partial-obstruction** (unchanged). **L4 has 4 firm + 0 rough-in** (unchanged). **L1>L0 has 11 theme files** (unchanged; `divfree-projector-mutation-rotation` firm got a "first→third nested-gate instance" provenance correction — `eigsolve-mutation-rotation` cycle-011 is the prior + richer instance, divfree is the THIRD; firm unchanged). **L0 at 21 chapters** (unchanged). **+1 concept page** (`nested-constructed-operator-gate` — the named concept for an L1>L0 closure whose constructed-operator gate carries a FURTHER nested constructed-operator gate; ≥2-firm-instance bar cleared [eigsolve ⊃ divfree ⊃ ksp]; inbound link from `divfree-projector-mutation-rotation`, outbound link to `constructed-operator-factory`; SUMMARY register). **Phase-1 corpus unchanged: removals stay 9/10.** **Safety-net gates: retroactive-budget global = 0** (all 5 reports net-new/append: report-1 create+flip+register, report-2 create-concept, report-3 firm-theme prose correction [not a firm L0-slice citation-edit], report-4 create-theme+register, report-5 dep-map-row-append; all 0-retroactive; well below per-slice ≥3 / global ≥4 block thresholds). **Zero build-repairs** (clean first build, exit 0, linkcheck zero broken links; 6 pre-existing katex math-display false-positive warnings in `design/l4_calculus.md` carry unchanged). **OQ ledger: 2 RESOLVED** (`linear-combination-harvester-formalization`, `nested-constructed-operator-gate-concept-and-divfree-correction` [both prongs]) **+ 2 ANSWERED** (`inner-product-fold-sibling-candidate`; `divfree-closure-nesting-constructed-gate-carrying-constructed-gate` [flipped answered→resolved, prong-b enacted]) **+ 2 NEW opens** (`inner-product-harvester-formalization-and-conjugation-pinning`, `linear-combination-fold-specialization-theme-followups`). Parent `blas1-variadic-linear-combination-fold-unification`: prong (b) DONE; prong (a) (combinator-miner spec extension) remains OPEN → headline batch-4 meta-phase item. **Fourteenth consecutive clean cycle under the split integrator** (zero deferrals/rejections/rework; zero build-repairs). **STAGING-LOG-COMPLETENESS NOTE (carry-forward to batch-4 meta-phase):** the cycle-018 STAGING.md captured only report-1's row; reports 2–5 applied their artifact changes + OQ promotions cleanly (verified against working tree + all 5 reports `overall_status: ready` + clean build) but did NOT append staging rows — integrator-finalize reconciled from the artifact + report frontmatter + OQ-ledger notes. This is a per-report-integrator staging-append-discipline gap, NOT an artifact gap. **Carry-forward to batch-4 meta-phase** (fires next): (i) the `specialized-agent-direct-write-to-book-during-dispatch` RECURRENCE-3 META-SIGNAL from cycle-017 — cycle-018 had ZERO leaks (the explicit per-dispatch reminders worked), but the prompt-guard is only in `layer-intro-author.md`; meta-phase should enact it across ALL 8 specialized specs; (ii) the cycle-017 `rough-in-forward-reference-must-be-plain-text-not-live-link` candidate friction — cycle-018 honored it cleanly via explicit reminders (both `inner_product` + the linear_combination forward-refs were plain-text); codify it; (iii) the human-raised combinator-miner arity-blindness → **prong (a) spec extension (parametric/variadic-family detection mode) is the headline meta-phase enactment**; (iv) the 4+ skill-uptake-survey telemetry warnings across the batch (named-skill-by-slug uptake weakness); (v) NEW staging-log-append-completeness gap (this cycle). Suggested cycle-019 dispatches: `inner_product` harvester formalization + L2-L1 inner-product lowering theme; the conjugation-convention pinning; `gmres.md §L4 v0.6→v0.7` self-rotation (large); NLEPS at L1+ (large); bundle-6 #6 L0 candidate.

Forward indicator (post-cycle-017, **SECOND primary cycle of meta-batch-4**; cycles 016/017/018, **meta-phase fires after cycle-018**; cycle counter does NOT reset): **cycle-017 was a consolidation/frontier cycle — ONE new L2 rough-in combinator + the rest citation/prose hygiene + a read-only cross-layer audit; ZERO new firm operators.** **L1 unchanged: 11 firm + 0 partly-constructive + 3 rough-in + 6 rough-in (obstruction)** (the firm `divfree-projector` L1 entry got an 11-reference citation-line-drift maintenance fix — apply `Mult` close-brace `:155-186`→`:155-187` ×4 sites + CG rel-tol `:140`→`:141` + 5 hpp off-by-one/range-tighten + 1 dangling `(see Variant axes)`→`(see Signature)` pointer; stays firm; OQ `divfree-l1-entry-apply-close-and-reltol-line-drift` resolved). **L2 has 2 firm + 1 rough-in** — NEW `linear_combination` rough-in dep-map row at `book/src/L2/index.md` (`linear_combination :: [(Scalar, Tensor[N])] -> Tensor[N]` ≡ `foldl (\acc (a,t) -> acc + a·t) zeros pairs`; unifies the BLAS-1 scalar-weighted-sum cohort `scal`/`axpy`/`axpby`/`axpbypcz` along the **arity** axis — complementary to the `scalar-promotion` element-type-axis unification of the same four; the constructive half/**prong (b)** of the human-raised OQ `blas1-variadic-linear-combination-fold-unification`; **prong (a)** — extend the combinator-miner spec with a parametric/variadic-family detection mode — remains a **batch-4 meta-phase item**; harvester formalizes the operator chapter per OQ `linear-combination-harvester-formalization`; the forward-reference to the not-yet-authored chapter is a deliberate plain-text reference, NOT a dead link [build-repair: the producer's row used a live markdown link to the absent file which `linkcheck2` failed on; integrator-finalize de-linked it to the cycle-015 `fem-bilinearform-file` no-dead-link convention]). **L3 has 8 firm + 1 partial-obstruction** (unchanged; `L3/chebyshev` 5-site sibling `forM_`/`foldM`→`iterate_while_pure`/`iterate_while_pure_L3` vocabulary refresh + 1 in-scope `predicate-driven`→`convergence-predicate-driven` disambiguation — post-apply re-grep returned **ZERO residual `forM_`/`foldM`**, the L3 entry is now fully consistent with the firm L4 sibling; partial-obstruction unchanged; OQ `l3-chebyshev-sibling-formm-foldm-prose-sweep` resolved — the chebyshev `forM_`/`foldM`→`iterate_while` vocabulary-lag cohort across L4 body [015] + L4 prose [016] + L3 named-sentence [016] + L3 siblings [017] is now FULLY TERMINATED). **L4 has 4 firm + 0 rough-in** (unchanged). **L1>L0 has 11 theme files** (unchanged). **L2>L1 has 1 firm theme** (unchanged). **L0 at 21 chapters** (unchanged). **L3>L2 `krylov-step-body-identity` firm theme: 3 dangling `cg.md` provenance pointers re-anchored** to the in-document terminal firm home (this theme's OWN §Verified-against bullet 1, which physically carries the verbatim cycle-002 combinator-miner Claim-2 quote) with the dead slice ranges demoted to parenthetical original-range annotations (cycle-013/014/015/016 lifted-evidence convention); stays firm; OQ `l3-l2-body-identity-cg-md-citation-sweep` resolved — **this TERMINATES the SECOND of the two-hop-to-dangle residuals the cycle-016 L4 sweep flagged: the `cg.md` re-anchor chain through the krylov-step lowering stack (L4 swept 016, L2 swept 016, L3-L2 body-identity swept 017) is now FULLY TERMINATED**. **Closure-nesting cross-layer audit (read-only, ZERO `book/` mutation): the cycle-016 divfree "first L1>L0 closure carrying a constructed-operator gate / does not recur" premise was REFUTED** — `eigsolve-mutation-rotation` (firm, cycle-011 `8bb16b7`, five cycles before divfree `b54ea1c`) is the prior + richer instance (closure `E` carries TWO nested gates `E.linear : Solver[A]` + `E.projector : Maybe DivFreeSolver` = the divfree projector itself, so eigsolve ⊃ divfree ⊃ ksp transitively; its core sub-pattern B lowers each `opInv->Mult` through the firm `ksp-solve-mutation-rotation` theme); divfree is the **THIRD** (not first) instance; `ksp_solve` `K.M⁻¹` a latent further site; ≥2-firm-instance bar cleared → warrants a named concept page (OQ `divfree-closure-nesting-constructed-gate-carrying-constructed-gate` answered; follow-ups routed via NEW OQ `nested-constructed-operator-gate-concept-and-divfree-correction`). **Phase-1 corpus unchanged: removals stay 9/10.** **Safety-net gates: retroactive-budget global = 2** (per-slice max 1: report-2 divfree-projector 1 + report-3 krylov-step-body-identity 1; reports 1/4/5 contribute 0; well below per-slice ≥3 / global ≥4 block thresholds). **1 build-repair** (the `linear_combination` rough-in row's live markdown link to the not-yet-authored `./linear_combination.md` chapter failed `linkcheck2` with `File not found`; de-linked to the no-dead-link plain-text forward-reference convention; rebuild clean exit 0; 6 pre-existing katex math-display false-positive warnings in `design/l4_calculus.md` carry unchanged). **3 OQs closed** (the 11-ref divfree fix, the L3-L2 cg.md sweep, the L3 chebyshev sibling sweep) + 1 answered (closure-nesting refutation) + **3 NEW OQs opened** (`linear-combination-harvester-formalization`, `inner-product-fold-sibling-candidate`, `nested-constructed-operator-gate-concept-and-divfree-correction`). **Thirteenth consecutive clean cycle under split integrator** (zero deferrals/rejections/rework; the one build-repair was a single-cell de-link, not a content rework). Carry-forward to cycle-018: harvester `linear-combination-harvester-formalization` (author the L2 chapter + the `L2-L1/linear-combination-fold-specialization` abstractor theme); layer-intro-author `nested-constructed-operator-gate` concept page; lifter/harvester divfree-theme "first"-claim correction at `:108-113` + `:457-464`; combinator-miner `inner-product-fold-sibling-candidate`; `gmres.md §L4 v0.6→v0.7` self-rotation (large); NLEPS at L1+ (large); bundle-6 #6 `fespace.{hpp,cpp}`. **Meta-phase batch-4 aggregation fires after cycle-018 finalize** (aggregating cycles 016/017/018); **META-SIGNAL surfaced this cycle: `specialized-agent-direct-write-to-book-during-dispatch` RECURRENCE-3** (cycle-008 abstractor → cycle-012 layer-intro-author → cycle-017 harvester; friction-ledger Watch clause fires) — the batch-4 meta-phase should (i) enact the dispatch-phase write-guard prompt across ALL 8 specialized agent specs (currently only `layer-intro-author.md` has it) + (ii) re-weigh a pre-dispatch clean-tree gate; plus 4 skill-uptake-survey warnings (named-skill-by-slug uptake weakness) across cycles 016/017 as telemetry.

Forward indicator (post-cycle-016, **FIRST primary cycle of meta-batch-4**; cycles 016/017/018, **meta-phase fires after cycle-018**; cycle counter does NOT reset): **cycle-016 was a consolidation/frontier cycle — ONE new firm L1>L0 theme + ONE new L0 chapter, the rest citation/prose hygiene + a re-verification.** **L1 unchanged: 11 firm + 0 partly-constructive + 3 rough-in + 6 rough-in (obstruction)**. **L2 has 2 firm** (unchanged; `L2/krylov-step` cg.md citation sweep, 12 re-anchors + 1 CheckDot drift correction, firm unchanged). **L3 has 8 firm + 1 partial-obstruction** (unchanged; `L3/chebyshev` named-sentence downward-prose vocabulary refresh, partial-obstruction unchanged). **L4 has 4 firm + 0 rough-in** (unchanged; `L4/krylov-step` cg.md citation sweep [7 re-anchors] + `L4/chebyshev` 3-site prose vocabulary refresh, both firm unchanged). **L1>L0 now has 11 theme files** (+`divfree-projector-mutation-rotation`, **firm** — the L1>L0 lowering theme for the cycle-015-firmed `divfree-projector` L1 operator; 4 sub-patterns A/B/C/D [in-place Mult apply + scratch threading; copy-then-apply out-of-place wrapper; constructor closure-field materialisation; VecType real/complex element-type variant]; load-bearing positively-anchored `WeakDiv = -Gᵀ` sign sub-note `integrator.hpp:217` + `mixedvecgrad.cpp:202`; first L1>L0 mutation-rotation whose closure carries another constructed-operator gate `P.ksp` as a sub-field). **L2>L1 has 1 firm theme** (unchanged). **L0 at 21 chapters** (+`fem-libceed-operator-file`, bundle-6 #5 — `palace/fem/libceed/operator.{hpp,cpp}`; the libCEED composite-operator wrapper + matrix-materialization backend `CeedOperatorFullAssemble` COO→CSR `:455-523` + `CeedOperatorCoarsen` multigrid coarse-operator construction `:525-585`; the direct callee `fem-bilinearform-file` deferred to — the two deliberate plain-text forward references retired to live links; `test-libceed.cpp:284-345` PA/FA-equivalence witness). **`eigsolve-convergence-reason-mapping` (L1>L0 partly-constructive) re-verified a THIRD time** (cycle-013 authoring + cycle-014 audit + cycle-016 abstractor re-verification) — status correctly **STAYS partly-constructive** (zero materialization re-confirmed; no positive Palace source reads the SLEPc reason code); append-only `### Re-verification (cycle-016 abstractor)` subsection + `verified_against` YAML landed; strong evidence the partly-constructive ENTRY gate is a working transient. **Phase-1 corpus unchanged: removals stay 9/10** (only `cg_preconditioning_framework` reducible-but-not-removable pending firm-L4 lift + `sparse_triangular_solve` permanent negative-result retain). **6 OQs closed** (5 answered/resolved [`l4-`/`l2-krylov-step-cg-md-citation-sweep` answered; `bundle-6-l0-libceed-operator-file-next-candidate`, `l4-chebyshev-residual-formm-foldm-prose-cleanup`, `l3-chebyshev-downward-prose-iterate-while-refresh` resolved] + 1 answered [`partly-constructive-entry-mechanism-validated-eigsolve-convergence-reason-mapping`]) + **6 NEW OQs opened** (`l3-l2-body-identity-cg-md-citation-sweep`, `l3-chebyshev-sibling-formm-foldm-prose-sweep`, `divfree-l1-entry-apply-close-and-reltol-line-drift`, `divfree-closure-nesting-constructed-gate-carrying-constructed-gate`, + 2 divfree carry/re-surface). **Safety-net gates: retroactive-budget global = 1** (the 2 `fem-bilinearform-file` forward-reference→live-link retirements = 1 firm-file slice; well below the ≥4 block threshold). **Zero build-repairs** (clean first build; 5 pre-existing katex math-display false-positive warnings in `design/l4_calculus.md` carry unchanged). Carry-forward to cycle-017: `gmres.md §L4 v0.6→v0.7` self-rotation (large), NLEPS at L1+ (large), the new sibling-sweep OQs (`l3-l2-body-identity-cg-md-citation-sweep`, `l3-chebyshev-sibling-formm-foldm-prose-sweep`), the divfree.hpp doc-tension (`divfree-mult-doc-irrotational-vs-divfree-stale`), the divfree L1-entry off-by-one harvester follow-up (`divfree-l1-entry-apply-close-and-reltol-line-drift`). **Meta-phase batch-4 aggregation fires after cycle-018 finalize** (aggregating cycles 016/017/018).

Forward indicator (post-cycle-051, **THIRD/FINAL primary cycle of meta-batch-15**; cycles 049/050/051, **meta-phase fires after THIS cycle-051 finalize**; cycle counter does NOT reset): **the refactor-pass theme-demotion sweep under the 2026-06-01 VOCABULARY-SHIFT REDIRECT is COMPLETE.** Across cycles 050+051, the degenerate identity-in-named-terms lowering cohort (D8-corrected denominator **17 pairs**) is fully dispositioned: **16 pairs demoted to in-line notes** (cycle-050: 4 non-fold pairs `assemble-diagonal`/`elementwise_product`/`reciprocal`/`normalize`; cycle-051: the `linear_combination` fold-family `scal`/`axpy`/`axpby`/`axpbypcz` + `dot`-into-`inner_product` + `nrm2`-consumer-demotion + `jacobi-smoother` both edges + `divfree-projector-body-identity` L3>L2) and **1 KEPT** — `divfree-projector-leaf-identity` (L2>L1), the one genuine step-4 `Grad->AddMult` fusion rotation (`divfree.cpp:185` real / `:180-181` complex), reachable via direct live link from `L3/divfree-projector`. **15 theme files deleted in cycle-051** (8 fold-family `*-{body,leaf}-identity` + 2 `dot-*` + 2 `nrm2-*` + 2 `jacobi-smoother-*` + 1 `divfree-projector-body-identity`); the 5 L3 BLAS-1 leaves (`scal`/`axpy`/`axpby`/`axpbypcz`/`dot`) **re-expressed in place** through the 2 cycle-050 firm L3 combinators (`L3/linear_combination`, `L3/inner_product`). **COUNT DELTAS (cycle-051; D5 sole count-owner): L3>L2 firm 13 → 5; L2>L1 firm 17 → 10** (+1 partly-constructive `deflate-composition-lowering` unchanged); **L3 firm UNCHANGED at 17 + 3 partial-obstruction** (leaves re-expressed, not removed). The firm-theme DROPS are a **vehicle-change** (theme file → in-line §"Downward" note), NOT a coverage regression (OQ `c050-firm-theme-count-drop-is-vehicle-change-not-coverage-regression`). The surviving L3>L2 themes are the 5 substantive/non-identity rotations (`krylov-step-body-identity`, `ksp-solve-outer-driver`, `orthogonalize-variant-split`, `eigsolve-opaque-eigen-iteration`, `chebyshev-nested-recurrence`). **OPEN refactor remainder is exclusively meta-gated:** (1) the leaf-CHAPTER disposition (`collapsed-leaf-disposition-convention-cohort-wide` — delete-vs-redirect-stub for the now-orphaned-as-specialization-notes L2/L3 leaf chapters; the ONE decision gating batch-16) + (2) the low-priority `l4-propagation-depth-linear-combination` flag. Build clean (`cargo make book` exit 0, ~91s; zero live links to any of the 15 deleted slugs; only pre-existing KaTeX false-positives in `design/l4_calculus.md`). **The batch-15 meta-phase fires AFTER this cycle-051 finalize** (aggregating cycles 049/050/051).

Forward indicator (post-cycle-015, **THIRD/FINAL primary cycle of meta-batch-3**; cycles 013/014/015, **meta-phase fires after THIS cycle-015 finalize**; cycle counter does NOT reset): **cycle-015 ENACTED the two gated promotions staged cycle-014.** **L1 now 11 firm + 0 partly-constructive + 3 rough-in + 6 rough-in (obstruction)** — `divfree-projector` promoted partly-constructive→`firm` (the `WeakDiv ≈ −GᵀM` sign positively anchored in Palace-owned `fem/integrator.hpp:217` + `fem/integ/mixedvecgrad.cpp:202`; **first full partly-constructive ENTRY→EXIT lifecycle**, entered cycle-013 / unblocked cycle-014 / exits firm cycle-015). **L2 has 2 firm** (unchanged; 7-site citation-drift sweep on `chebyshev-iteration` + `chebyshev-smoother`, no status change). **L3 has 8 firm + 1 partial-obstruction** (unchanged; `L3/krylov-step` cg.md citation sweep, 5 re-anchors, firm unchanged). **L4 now 4 firm + 0 rough-in** — `chebyshev` promoted `rough-in`→`firm` (the `apply` body's `forM_`/`foldM` obstructions re-anchored onto nested `iterate_while_pure` folds with step-count predicates, reusing the canonical `iterate-while` family per the cycle-014 combinator-miner REUSE verdict; rough-in cohort emptied). **L1>L0 has 10 theme files** (unchanged count; divfree-projector is an L1 operator, its L1>L0 theme is a separate future dispatch). **L2>L1 has 1 firm theme** (unchanged). **L0 at 20 chapters** (+`fem-bilinearform-file`, bundle-6 #4 — `BilinearForm` + `DiscreteLinearOperator`; PA/FA assembly dual; 9 cited anchor surfaces; a deliberate plain-text reference to the not-yet-authored `libceed/operator` chapter, no dead link). **Phase-1 `chebyshev.md` slice FULLY REMOVED** (`git rm`; 18 inbound citations re-pointed onto the firm L1/L4 cohort; build linkcheck confirms zero stranded links) — **corpus removals 8/10 → 9/10** (only `cg_preconditioning_framework` remains, reducible-but-not-removable pending the firm-L4 lift; `sparse_triangular_solve` is a permanent negative-result retain — so 9/10 removed + 1 permanent-retain is effectively the terminal state pending OQ `l4-preconditioning-framework-promotion`). **The gated-promotion pattern (audit/recommend in cycle N, enact in N+1) recurred 3× across batch-3 and worked cleanly** (eigsolve 013, divfree 014→015, chebyshev-L4 014→015) — validates the cycle-012-codified `partly-constructive` transient-gate mechanism by use. Carry-forward to cycle-016: residual `forM_`/`foldM` prose in `L4/chebyshev.md` (OQ `l4-chebyshev-residual-formm-foldm-prose-cleanup`), L3 downward-prose refresh (OQ `l3-chebyshev-downward-prose-iterate-while-refresh`), sibling cg.md sweeps on `L4`/`L2 krylov-step` (OQs `l4-`/`l2-krylov-step-cg-md-citation-sweep`), L0 bundle-6 #5 (`fem/libceed/operator.cpp`). **Meta-phase batch-3 aggregation fires after this cycle-015 finalize commit** (aggregating cycles 013/014/015); key signals for it: (1) the partly-constructive full-lifecycle demonstration; (2) citation-line-drift recurring across all 3 batch cycles with `verify-citation-range` repeatedly not self-invoked — STRONGEST recurring friction; (3) slice-removal grep-completeness (non-link prose references need a dedicated grep, not just markdown-link check).

Forward indicator (post-cycle-014, **SECOND primary cycle of meta-batch-3**; cycles 013/014/015, meta-phase fires after 015; cycle counter does NOT reset): **cycle-014 was an audit/verification + gated-promotion-prep cycle — NO firm-count increases.** **L1 unchanged: 10 firm + 1 partly-constructive (`divfree-projector` — UNBLOCKED by cycle-014 lowering-verifier, promotion→firm GATED to cycle-015) + 3 rough-in + 6 rough-in (obstruction)**. **L2 has 2 firm** (unchanged). **L3 has 8 firm + 1 partial-obstruction** (unchanged). **L4 has 3 firm + 1 rough-in** (`chebyshev` STAYS rough-in; combinator-miner decided REUSE `iterate-while` family — rough-in→firm flip STAGED for cycle-015). **L1>L0 has 10 theme files** (unchanged count: 4 firm + 1 partly-constructive + 3 rough-in + 2 obstruction): 3 themes got citation anchors firmed via `verified_against` blocks this cycle — `orthogonalize-mutation-rotation` (lowering-verifier CONFIRMS, firm upheld) + `chebyshev-smoother-mutation-rotation` (CONFIRMS-WITH-REFINEMENT, firm upheld) + the partly-constructive `eigsolve-convergence-reason-mapping` (lowering-verifier NEGATIVE-ANCHOR-CONFIRMED → STAYS-PARTLY-CONSTRUCTIVE; zero materialization sites re-confirmed, 8-row enum literature-anchored — validates the partly-constructive ENTRY mechanism, complementing cycle-013's eigsolve EXIT). **L2>L1 has 1 firm theme** (`chebyshev-iteration-fusion`; CONFIRMS audit + verified_against block this cycle). **L0 at 19 chapters** (+`linalg-rap-file`, bundle-6 #2 — RAP = R·A·P Galerkin triple-product; `ParOperator` + `ComplexParOperator` + `BuildParSumOperator`; 7 cited anchor surfaces; the planner's "Restrictive Additive Schwarz" gloss was corrected to "R·A·P Galerkin triple-product"). **`krylov-step-typed-wrapper-dissolution` (L4>L3 firm theme): 8 dangling `cg.md` pointers re-anchored** to firm `L3-L2/krylov-step-body-identity.md` + `L3/krylov-step.md` (theme-side OQ answered; sibling residual sweep on the L3 operator entry routed to cycle-015 lifter). **Phase-1 `chebyshev.md` slice PARTIALLY reduced** (439→195 lines; §L1/§L2/§L3/Consumers/OQs/Concept-refs stubbed; **§L4 RETAINED**) — this is NOT a corpus removal (removals stay 8/10); full removal GATED on cycle-015 (re-point krylov-step §L4 citations + L4/chebyshev firming). **Two gated promotions STAGED for cycle-015**: divfree-projector partly-constructive→firm (5 firming edits queued) + L4/chebyshev rough-in→firm (iterate-while re-anchor). The "audit/recommend in cycle N, enact in cycle N+1" gated-promotion pattern is now established across eigsolve (013), divfree (014→015), chebyshev-L4 (014→015).

Forward indicator (post-cycle-013, **FIRST primary cycle of meta-batch-3**; cycles 013/014/015, meta-phase fires after 015; cycle counter does NOT reset): **L1 at 10 firm + 1 partly-constructive (`divfree-projector`) + 3 rough-in (test-coverage-bounded) + 6 rough-in (obstruction)** (the Firm `(10)` prose is unchanged — partly-constructive entries are tracked distinctly from firm). **L2 has 2 firm** (unchanged). **L3 has 8 firm + 1 partial-obstruction** (`chebyshev`, new cycle-013). **L4 has 3 firm + 1 rough-in** (`chebyshev`, new cycle-013). **L1>L0 has 9 themes**: 4 firm (`ksp-solve-mutation-rotation`, `eigsolve-mutation-rotation` — **promoted partly-constructive→firm cycle-013, the FIRST live partly-constructive→firm promotion**, now `firm (structural)`; `chebyshev-smoother-mutation-rotation` NEW; `orthogonalize-mutation-rotation` NEW) + 2 partly-constructive (eigsolve-mutation-rotation Sub-pattern B's residual? — promoted firm; `eigsolve-convergence-reason-mapping` NEW partly-constructive sibling sub-theme, 8 diverged-row EigStatus mappings via real negative anchor) + 3 rough-in (`axpby`/`axpbypcz`/`apply-linop`) + 2 obstruction (`minres`/`bicgstab`). **L2>L1 has 1 firm theme** (`chebyshev-iteration-fusion`, NEW — first chapter under the L2-L1 Part). **L3>L2 has 1 firm theme** (unchanged). **L4>L3 has 1 firm + 2 rough-in** (`krylov-step-typed-wrapper-dissolution` had 2 stale "no L3 row" residuals struck + a dangling `cg.md` pointer re-anchored to the firm `L3-L2/krylov-step-body-identity` this cycle). **L0 at 18 chapters** (+`linalg-orthog-file`, bundle-6 #3). **partly-constructive status heavily exercised cycle-013**: eigsolve EXIT (promotion) + divfree ENTRY (adjudication) + eigsolve-convergence-reason-mapping (new) + chebyshev L4 rough-in — the cycle-012-codified mechanism is in active use; promotion-route ratification OQ `partly-constructive-to-firm-promotion-route-ratification` + divfree status-adjudication OQ both routed to cycle-015 meta-phase. **3 concept pages repointed** (plane-rotation canonical pointers → `plane_rotation_stream.md`) + `orthogonalization.md` rewritten to align with the firm L1 "does not normalize output" contract. **Phase-1 corpus reduction batch-4 (final 2 slices) — annotated-reduction 10/10 COMPLETE** (`cg_preconditioning_framework` = `partially-absorbed` stub-and-pointer, §L4/v0.2/v0.3 retained as sole-source load-bearing pending the firm-L4 lift OQ `l4-preconditioning-framework-promotion`; `sparse_triangular_solve` = `not-yet-eligible`/permanent negative-result retain). **NOTE the metric distinction: annotated-reduction is 10/10 COMPLETE, but REMOVALS stay 8/10 — both batch-4 slices RETAIN load-bearing material and are NOT removed (1 reducible-but-not-removable pending firm-L4 lift, 1 permanent negative-result artifact).** Do NOT read this as "corpus shrinks to 8 files" — it shrinks to 8 removed-equivalent + 2 annotated-retained.

## Working Notes

- **Cycle-053 (solver test-load probe cycle, batch-16 mid):** disciplined solver-test-load entry per the vocabulary-shift redirect ("advance a layer only when cleanly describable, never force the spine"). 3 observation-first dispatches → 2 small landings + 1 pure observation. (1) `gram` weighted-axis gained 2 solver witnesses (capacitance + inductance — a `gram` variant, not a new operator; firm count unchanged). (2) FE-assembly thread opened (`fe-operator-assemble-mutation-rotation` rough-in L1>L0; L1>L0 themes +1 rough-in). (3) The fixed-operator parametric solve-sweep is now **2-of-N witnessed** (electrostatic + magnetostatic) — the single-witness gate is discharged and the **fixed-operator solve-family combinator is mineable**, queued for the cycle-054 combinator-miner. Reusable disciplined-mining gate established: single-witness → 2nd-pipeline probe → discharge before authoring a cross-pipeline combinator (don't mine-and-strand from one witness).
- Cycles 1–79 covered Krylov + orthogonalisation + smoothers (Chebyshev only so far) + one projection (divfree). FE assembly and time-stepping are the next major surfaces. Mesh construction and boundary conditions are also untouched.
- The methodology has stabilised through 17 meta-reviews; the per-solver pipeline buildup is now the dominant remaining work.
- If a roadmap item is genuinely out of scope (e.g., a methodology change rules it out), strike it through (`~~text~~`) with a meta-review note rather than deleting.
