# Cycle-034 resume notes

**Written by:** cycle-033 meta-phase (batch-9 closure; 2026-05-30T210000Z). The orchestrator should restart the Claude Code session BEFORE dispatching cycle-034 plan.

## Why a restart is needed

Per friction-ledger `new-agent-defs-need-session-restart`: when meta-phase changes role-specs / CLAUDE.md invariants / promotes skills, the new text must be loaded in the agent registries before the next dispatch. The batch-9 meta-phase enacted:

1. **`.claude/agents/cycle-planner.md`** — §Discipline gained a MANDATORY pre-dispatch deliverable-presence check ENFORCEMENT bullet (the four-step procedure: file existence + maturity / `verified_against:`-block presence + OQ-RESOLVED-grep + structural-block check). This is the c031/c032 stale-recruitment recurrence-3 addressed-by, codifying the c033 working precedent inline-per-dispatch convention.

2. **`CLAUDE.md`** — §Methodology invariants "Phase 1 corpus reduces as material is lifted" amended with a canonical-instance carve-out: a slice MAY be retained-by-design (`annotated-and-retained`) when it is the named §Canonical-instance referent of ≥2 concept pages AND carries unique L0 navigation. Precedents: `polynomial_recurrence_step` (c013), `sparse_triangular_solve` (c031). The audit-side check is in `skills/phase-1-slice-reduction-audit/SKILL.md` step 7.

3. **`skills/verify-dispatch-scope-not-already-discharged/SKILL.md`** — newly promoted. Four-step deliverable-presence procedure. Audience: cycle-planner (primary) + orchestrator (safety net). The c033 cycle-planner CYCLE.md `## Deliverable-presence verification` section is the canonical working precedent (3/3 genuinely-open dispatches).

4. **`skills/phase-1-slice-reduction-audit/SKILL.md`** — step 7 (canonical-instance check) + Discipline bullet + cycle-031 worked example.

Without a restart, the cycle-034 cycle-planner dispatch may use the old role-spec text and not run the four-step deliverable-presence check.

## Context-reset

The restart also resets the primary conversation context, which subsumes the old per-meta `/compact` step (retired by user directive 2026-05-29 per CLAUDE.md §Methodology invariants). **Do NOT include a separate `/compact` reminder** — the restart is the reset mechanism.

## Plan c034 active head (3 fan-out-ranked picks)

Per `scaffolding/priorities.md` post-batch-9 re-rank:

1. **(abstractor) `reciprocal-mutation-rotation` + `elementwise-product-mutation-rotation` L1>L0 themes (COMPOSITE candidate)** — TOP cycle-034 pick. Both forward-referenced as plain text in 4+ chapters. Likely composite single theme per the `ksp-solve-mutation-rotation` thin-theme precedent. Fan-out: medium-high. OQs: `reciprocal-l1-l0-mutation-rotation-theme` + `elementwise-product-l1-l0-mutation-rotation-theme`.

2. **(lowering-verifier) jacobi/chebyshev/axpby dead-code complex-transpose kernel cohort audit** — thin verdict-only sweep. Fan-out: low. OQ: `jacobi-mutation-rotation-dead-code-complex-transpose-kernel-lowering-verifier-audit`.

3. **(open slot / TBD)** — c034 cycle-planner-chosen substantive landing per the MANDATORY pre-dispatch deliverable-presence check (per the new role-spec bullet). Candidates: further L1 BLAS-like primitives, L3 firm operator additions (cohort static since c020), L2 firm operator harvest.

## Critical reminder for the cycle-034 cycle-planner

The cycle-033 cycle-planner CYCLE.md (`reports/2026-05-30T150000Z-cycle-planner-cycle-033/CYCLE.md`) is the **canonical working precedent** for the new four-step deliverable-presence check. The cycle-034 plan MUST emit a similar `## Deliverable-presence verification` section recording the four-step check outcome for each candidate dispatch (or an explicit "open by construction" skip note per the role-spec skip-condition). The c033 precedent landed 3/3 genuinely-open; the c032 stale-recruitment incident (4 of 6 picks stale) is what made the role-spec ENFORCEMENT bullet necessary.

## Batch-10 watch-list

- **`cycle-planner-stale-priorities-line-recruitment` recurrence watch** — if recurrence-4 surfaces in batch-10 despite the role-spec ENFORCEMENT bullet + skill + c033 precedent, the swap-cycle-planner-to-opus HELD escalation moves from HELD to GO. The cycle-034 planner's adherence to the four-step procedure is the load-bearing test.
- **`dispatch-resilience-iterative-cpp-running-qr-region`** — the batch-8 ask (commit f582a66) had no live test in batch-9 (no iterative.cpp running-QR dispatches). Effectiveness on that region remains untested-but-not-falsified. Add any new fail-and-recover region to the watch-list per the role-spec bullet's extension rule.
- **Phase-1 corpus canonical-instance carve-out first-use watch** — confirm a c034+ slice-reduction audit runs the step-7 second-axis check (concept-page grep). If a future audit reduces a slice that IS a canonical instance (failure mode), reopen.
