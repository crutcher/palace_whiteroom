# Cycle-049 resume notes (post-batch-14 meta-phase, AMENDED by the 2026-06-01 VOCABULARY-SHIFT REDIRECT)

## SESSION RESTART REQUIRED before cycle-049

Two sets of role-spec changes landed and require a restart before dispatch:

**(1) The 2026-06-01 VOCABULARY-SHIFT REDIRECT (user directive — the headline; full spec `METHODOLOGY-REDIRECT.md`).** Edits to **5 agent definitions** (`combinator-miner`, `harvester`, `abstractor`, `cycle-planner`, `layer-intro-author`) install the corrected methodology:
- The stack is a sequence of **representational + component-vocabulary shifts**, NOT a rectangular projection. Each layer is concise **in itself**; conciseness drives **in-layer combinators**. Each lowering is a **translation across vocabularies**, not a 1:1 rename — a degenerate identity lowering is a **smell**.
- `combinator-miner`: **replace-and-propagate**, not mine-and-strand. `harvester`: formalize in the layer's concise vocabulary, combinator-as-entry, no mirrored base-form floors. `abstractor`: lowerings are translations; degenerate `-body-identity` themes are a smell. `cycle-planner`: plan the refactor-pass-first program; solvers as low-priority test-load. `layer-intro-author`: layer's own concise vocabulary, retire rectangular-floor framing.
- **Supersedes:** the 2026-05-31 "uniform pull-up → rectangular" directive + its `foundation_solidity` / count-ownership / dual-registration rectangular-floor machinery; "Identity-lowerings still require both L levels"; the leaf-vs-fold fork ratified (b) (→ fold/combinator-primary).

**(2) The batch-14 meta-phase's `cross-report-forward-reference-slug-divergence` codification** (cycle-planner/harvester/abstractor canonical-slug bullets) — landed in commit `8aee4be`, still in force, also needs the restart.

## Cycle-049 / batch-15 lead frontier (from the reshaped plan — `scaffolding/priorities.md` §CYCLE-049 / batch-15 active head)

The forward-frontier construction is **not** winding down (the batch-14 "L4 substantially complete" read was complete only *relative to the lifted inner-kernel cohort*, not the in-scope target — all 5 solver pipelines + FE assembly are unlifted). **Batch-15 program, in priority order:**

1. **REFACTOR PASS (highest priority).** Collapse the cycles-041–048 base-form L2/L3 leaf floors into in-layer combinators (`axpy`/`axpby`/`axpbypcz`/`scal` → `linear_combination`; `dot`/`nrm2` → `inner_product`; …), propagate the combinator upward (L3/L4 express *through* it), demote the thin `-body-identity` L3>L2 / L2>L1 themes to in-line notes, re-audit touched lowerings under the translation/smell test. Route `combinator-miner` (identify family + replace + propagate) → `harvester`/`abstractor`/`lifter` (enact). **Precedes new forward-frontier work.**
2. **Continued shared-spine abstraction** under the corrected model.
3. **Solvers as a LOW-PRIORITY test-load** — pull the 5 pipelines + FE assembly up through the layers only when **cleanly describable** in existing shared vocabulary; never preempt or distort the spine; feed back what they can't cleanly say as spine findings.

The cycle-049 planner reshapes this into a concrete dispatch plan against the refactor pass once the role-specs are loaded.

## ASK ITEMS FOR THE HUMAN

**None outstanding.** The batch-14 strategic-pivot ASK ("is layer-construction done / pivot to the downstream burn effort?") is **ANSWERED by the redirect**: continue building the shared spine on the corrected (vocabulary-shift) model, with solvers as a low-priority test-load; **no pivot to the downstream burn effort yet.** The Outcome-convention ratification (b) and the slug-divergence codification (batch-14) stand.
