# Cycle-031 resume notes (post-batch-8 meta-phase)

**Session restart REQUIRED before cycle-031 begins.** The batch-8 meta-phase (post-cycle-030) edited agent definitions under `.claude/agents/`. Per friction-ledger `new-agent-defs-need-session-restart`, the session that wrote these edits does NOT see them in its cached agent registry; restart the Claude Code session so the new definitions load before the first cycle-031 dispatch. The restart also resets the primary conversation context (subsuming the retired per-meta `/compact` step — do NOT run `/compact`).

## Agent-defs changed (why each needs the restart)

1. **`.claude/agents/lowering-verifier.md`** — gained the **"No `verified_against:` `note:` value may begin with a quote character of EITHER kind"** Discipline bullet (cycle-030 meta-phase codification of friction-ledger `verified-against-note-no-leading-quote-of-either-kind`, recurrence-2 generalizing the c028 leading-double-quote-only flag). The bullet states the generalized predicate (note value's first non-whitespace character ∈ {`'`, `"`}), names the producer self-check, and prescribes a `yaml.safe_load` mechanical check pre-emit. The c028-codified rule was too narrow; the c030 single-quote variant slipped past a producer self-check that named only the narrower form. The lowering-verifier must load the new bullet so cycle-031+ audits don't repeat the single-quote variant.

2. **`.claude/agents/critic.md`** — check 1 `citation-validity` gained the **`verified_against:` YAML round-trip sub-check**: for any report carrying a proposed `verified_against:` block, extract the block and run `yaml.safe_load` against it; a `ParserError: expected <block end>, but found '<scalar>'` is the signature of the leading-quote-of-either-kind defect and is flagged as `citation-validity: fail` with the line+column from the parser error. The critic must load the extended check so cycle-031+ critique pipelines mechanically catch the defect at producer-emit time, not at integrator-apply time.

3. **`.claude/agents/abstractor.md`** — gained the **"Obstruction themes — name the sub-kind in `## Status`"** Discipline bullet (cycle-030 meta-phase codification of friction-ledger `obstruction-sub-kind-opaque-library-vs-enum-only-stub`). The bullet specifies the two sub-kinds (`enum-only-stub` — Palace-internal TODO / aborting branch; `opaque-library-ownership` — HYPRE/SLEPc/external-direct-solver boundary) with default-rules (Palace-owned method body → enum-only-stub; non-Palace callable → opaque-library-ownership) and example precedents. Going forward, every new obstruction theme MUST tag its sub-kind inline: `## Status: obstruction (<sub-kind>)`. The abstractor must load the new bullet so cycle-031+ obstruction-theme dispatches carry the sub-kind tag.

## CLAUDE.md change (loads on every session, no restart needed but noted)

- **CLAUDE.md §Methodology invariants** — new invariant bullet "**Obstruction themes have two sub-kinds: `enum-only-stub` and `opaque-library-ownership`**" inserted between the `partial-obstruction` qualifier bullet and the "Integration may materialize implied components as stubs" bullet. This is the project-wide canonical reference for the sub-kind distinction; the abstractor role-spec bullet above mirrors it on the producer side. Cross-layer-cross-cutter consumers may treat the two sub-kinds differently (enum-only-stub = anticipated upstream work; opaque-library-ownership = permanently library-owned, never re-promotable).

## New skills (load from disk; no restart needed but noted)

- **`skills/verified-against-note-no-leading-quote-of-either-kind/SKILL.md`** — promoted from the cycle-030 repairer's skill-candidate. Producer self-check + critic mechanical check + repairer rephrase pattern. The deterministic procedure for the generalized leading-quote-of-either-kind channel-format hazard.
- **`skills/establish-negative-finding-exhaustiveness/SKILL.md`** — promoted from the cycle-028 critic's skill-candidate. Producer- and critic-facing 5-step bar for negative-localization-with-routing reports (the unimplemented-stub / opaque-library obstruction family): stated-terms + broadened-sweep + residual-token accounting + positive-API confirmation + critic re-run.

## Cycle-031 plan head (from `scaffolding/priorities.md`)

The active head is set for cycle-031 (FIRST primary cycle of meta-batch-9; cycles 031/032/033; meta-phase fires after cycle-033 finalize). Top picks, fan-out-ranked:

1. **(lowering-verifier) ls-update-column-mutation-rotation cycle-031 `verified_against:` audit** — standard firm-theme sibling-follow-up (closes the c030-landed firm theme's audit-row).
2. **(lifter or abstractor) back-solve-mutation-rotation Sub-pattern B brace-placement narrative repair** — **REAL content defect in a firm + integrated theme**, independently confirmed 3× across c030 (`diff` shows byte-identical, NOT +1-line brace shift; offset is +5 lines from preceding code). Narrative-only fix; theme stays firm; non-mechanical prose rework.
3. **(lifter or repairer) L2-L1/incremental-least-squares-composition-lowering bounded prose-rework pass** — fold 4 c030-routed items into one chapter-touch: (a) 3 ls_update_column-mutation-rotation plain-text mentions with stale "forthcoming" framing; (b) §Status historical paragraph c027-authored; (c) §Open-questions historical-judgment entries c027-authored.
4. **(repairer) back_solve L1 leaf three minor off-by-one cross-anchor imprecisions** — cosmetic; safe to batch with pick #3.
5. **(same-layer-cross-cutter) book/src/spec/slices/sparse_triangular_solve.md Phase-1 slice-reduction candidacy** — carry-forward from c029 now that the L1>L0 obstruction theme is on disk.
6. **(open slot)** — c031 cycle-planner-chosen substantive landing: candidates include (a) next-batch L1 firm operator (`apply_linop` lift, new BLAS-like primitive); (b) L2 firm operator harvest (the L2 cohort hasn't grown since c026); (c) L3 firm-or-partial-obstruction backfill (the L3 cohort hasn't grown since c020).

## Open ASK items for the human (surfaced by the batch-8 meta-phase; NOT enacted)

1. **Dispatch-resilience signal: iterative.cpp running-QR localization region (3 retries across batch-8).** Friction-ledger `dispatch-resilience-iterative-cpp-running-qr-region`, status `watching (ask-surfaced)`. c029 D5 + D6 + c030 D4 all hit transient API socket/timeout failures during initial dispatch on the same source region (`iterative.cpp:634-640` GMRES Arnoldi column update + `:813-819` FGMRES variant — token-dense + template-heavy). All 3 were fixed clean by a CONSTRAINED retry where the orchestrator pre-supplied the exact L0 anchors in the retry dispatch prompt (skipping the over-long localization loop that triggered the socket failure). The workaround is *manual orchestrator process work* that doesn't surface in role-specs or skills (per memory `escalate-process-issues-dont-work-around`: silting). Two repair paths to choose between:
   - **(a) Cycle-planner role-spec bullet** — when dispatch scope includes `iterative.cpp` running-QR or similar known-heavy regions (a watch-list), pre-fetch the codemap range and embed it in the dispatch scope, eliminating the inflated context. Project-local fix.
   - **(b) Harness-level dispatch retry/backoff** — automatic constrained-anchor retry on transient API failure, with the anchors derived from the dispatch scope's named source regions. Harness change; may need code changes the meta-phase can't enact.
   
   The workaround is effective enough that c031+ batch-9 cycles will continue running cleanly; the ask is whether to address the underlying friction at recurrence-3 or to wait for recurrence-4. **If recurrence-4 surfaces before the human responds, the meta-phase will unilaterally enact path (a) on the next batch's meta-phase (lower-cost repair); the harness-level fix stays open as a longer-term option.**

2. **Pre-harvest slug-collision check as a standing producer-spec bullet (carried-forward from batch-7 meta-phase).** This ASK was opened by the cycle-027 meta-phase and remains unresolved. The cycle-027 D4/D5 `ls_update_column` slug collision was avoidable — the L2 entry already used both colliding slugs with distinct meanings. A pre-harvest grep of existing artifact vocabulary before a producer introduces a NEW slug would stop the collision at the source. Low cost (a one-bullet producer-spec change across harvester/abstractor), but worth the human's appetite-confirmation vs. relying on the repairer-side gate (`skills/audit-slug-meaning-before-coordinated-cross-report-rename`, promoted c027). NOT enacted this batch (the repairer-gate has held; recurrence-1 stable).
