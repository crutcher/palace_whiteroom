# Cycle 032 — L1 firm +1 (`jacobi-smoother` — FIRST diagonal-preconditioner-apply primitive) + back-solve-MR additive c032 reverify (closes c031 D2 repair-loop) + L2-L1 incremental-LS prose-currency follow-on (SECOND primary cycle of meta-batch-9)

**Date:** 2026-05-30 · **Commit:** see git log · **Status:** clean (3 of 3 dispatched-ready reports applied; zero deferrals; zero rejections; zero build-repairs; twenty-eighth consecutive clean split-integrator cycle)

**Batch position:** cycle-032 is the **SECOND** primary cycle of **meta-batch-9** (cycles 031/032/033). **The batch-9 meta-phase fires after the cycle-033 integrator-finalize commit** (3:1 cadence; cycle counter does NOT reset across batch boundaries). This `log/cycle-32.md` + the `scaffolding/integrator-signals.md` cycle-032 section CONTINUE the batch-9 evidence window.

(Filename note: a `cycle-032.md` from the slice-vertical era already exists; the layered-flow era reclaims the `cycle-32.md` namespace, per the cycle-020→031 precedent. No file rename was needed this cycle — `log/cycle-32.md` did not exist before.)

## Summary

A **frontier-advancing-but-narrow** cycle that lands ONE substantive new firm L1 operator (`jacobi-smoother`), closes the c031 D2 lifter narrative-repair loop with an additive `verified_against:` block, and discharges a small lifter prose-currency follow-on. Only 3 dispatched-ready reports — the c032 cycle-planner's proposed 6-dispatch plan was BROADLY STALE (4 of 6 picks targeted already-discharged deliverables), and the orchestrator overrode the picks with existence-verified open work. **This is a recurrence of the c031 `cycle-planner-stale-priorities-line-recruitment` friction at recurrence-level NOW IN BOTH c031 AND c032 within batch-9** — strong batch-9 meta-phase evidence that the c031-codified file-existence check is insufficient and a deeper *deliverable-presence* check is needed.

## Headlines

- **HEADLINE 1 — L1 firm +1 (`jacobi-smoother`; the FIRST diagonal-preconditioner-apply primitive in the L1 cohort).** `book/src/L1/jacobi-smoother.md` lands firm (~550 lines, 68 citations into `palace/linalg/jacobi.{cpp,hpp}`, 13 live cross-links all on-disk-resolved). Signature `jacobi_smoother :: (op: DiagonalPreconditionerOp, x: Tensor[VecType]) -> Tensor[VecType]`; semantics `y = (ω · D⁻¹) ⊙ x` — the canonical Jacobi smoother / damped Jacobi / pre-smoother for Chebyshev / block-Jacobi base / multigrid coarsening. **Six algebraic laws** (decomposition into `extract_diagonal → reciprocal → scal → elementwise_product`, linearity in `x`, identity at `ω=1` + `D=I`, commutativity with diagonal operators, idempotence-when-spectral-radius-1, inverse-when-`D=A`-and-`ω=1`) + **four explicit non-laws** (no general SPD-guard, no zero-diagonal handling, no MPI collective, no general smoothing-property without a separate convergence argument). **Two variant axes** both explicitly enumerated + absorbed: element-type `{Real, Complex}` + damping-mode `{omega == 0.0 adaptive-fixed-point | omega > 0.0 explicit}` (with `sf_max` correctly identified as parameter-not-axis). **Firm-on-positive-structure per the `chebyshev-smoother` precedent**: every law is operator-algebra on a fully-read closure, the missing dedicated unit test (the area has only integration coverage) does not gate the syntactic-identity laws. Consumes the firm `assemble-diagonal` primitive at setup. The constructed-operator solve-gate is a thin elementwise product. SUMMARY-registered + L1/index dep-map row + L1/index `## Vocabulary cohort` "Firm (22)→(23)" + new gate "diagonal-preconditioner-apply Jacobi smoother" appended. **Routes 4 cycle-033+ OQs:** (a) `jacobi-smoother-mutation-rotation-l1-l0` — the TOP routed follow-up, natural next step now that the L1 op is firm; (b) `reciprocal-and-elementwise-product-l1-primitives` — stub-or-harvest decision (2 converging forward-references at the cycle-022 stub-creation invariant threshold; harvester preferred per CLAUDE.md "Lower-level shared vocabulary takes priority"); (c) `jacobi-fixed-damping-mode-consumer-coverage` — low-priority variant-axis-coverage audit (0-of-5-consumer-sites asymmetry on the fixed-damping mode); (d) `polynomial-smoother-l2-combinator-from-jacobi-and-chebyshev` — cycle-034+ combinator-miner candidate awaiting a third firm sibling (`richardson` is the missing one). Implied-component-stub-creation correctly DEFERRED for `reciprocal`/`elementwise_product` per user prompt direction ("Use judgment; do NOT force it") — `chebyshev-smoother` has lived with similar forward-refs for 24 cycles without stubs, and per CLAUDE.md the primitives merit a proper harvester dispatch, not claim-free placeholders; the chapter is build-clean as plain-text without stubs.

- **HEADLINE 2 — `back-solve-mutation-rotation` additive c032 4-row `verified_against:` re-verification block (closes c031 D2 lifter narrative-repair loop).** A second `verified_against:` yaml block landed at `book/src/L1-L0/back-solve-mutation-rotation.md:888-912` (after the c030 22-row baseline block at `:796-886`), confirming **GMRES at `iterative.cpp:653-660` is byte-identical to FGMRES at `:832-839`** with the +179-line file offset NOT brace placement. The 4 rows record: GMRES body / FGMRES body / GMRES brace / FGMRES brace, all `supports`, paired with `--anchor 'for (int i = j; i >= 0; i--)'` zero-drift at both `:653` and `:832`. Theme stays `firm`; the c031 narrative repair across 5 disjoint sites is now independently re-audited and confirmed correct. Proper ` ```yaml ` fence (the c030 channel-format refinement `verified-against-note-no-leading-quote-of-either-kind` held — no leading-quote defect; the c030 `~~~yaml` substitution issue from the c031-repair did NOT recur because the c031 repairer + this c032 per-report integrator both used the 4-space-indented→top-level-` ```yaml `-block convention matching the c030 baseline).

- **HEADLINE 3 — `incremental-least-squares-composition-lowering` prose-currency follow-on (4 "forthcoming"→"firm" qualifier flips).** `book/src/L2-L1/incremental-least-squares-composition-lowering.md` at `:114/:276/:300/:306` — 4 single-line edits flipping the obsolete "forthcoming" framing on `ls_update_column` L1>L0 theme references, now that the theme has been firm-on-disk since c030. Site `:306` collapses "firm-or-forthcoming-firm vocabulary" → "firm vocabulary". The 4 OTHER "forthcoming" mentions at `:15/:145/:204/:541` were correctly left untouched (correctly-quoted historical references to the L2 entry's deferred-non-law text). Closes c031 OQ `incremental-ls-composition-lowering-residual-forthcoming-mentions-c032` to RESOLVED. Theme `## Status: firm` line, signatures, decompositions, applicability conditions, verified-against block all untouched.

- **HEADLINE 4 — Cycle-planner-staleness RECURRENCE within batch-9 (c031 noops + c032 stale-plan override).** The c032 cycle-planner's proposed dispatch plan (`reports/2026-05-30T052756Z-cycle-planner-cycle-032/CYCLE.md`) was BROADLY STALE: D3 re-proposed the **just-completed** `sparse_triangular_solve` slice-reduction audit (DEFER verdict landed c031), D4/D5 (matrix-weighted-norm promotion) are test-coverage-gated/blocked, D6 re-proposed lowering-verifier audits of 4 batch-6 themes that **already have `verified_against:` blocks** (apply-nonlinear-pencil 2 blocks, deflate 1, gram 1, orthogonalize 2). The orchestrator overrode: ran on-disk existence + already-audited checks, retired nothing further (the c031 retirement stood), and substituted the existence-verified genuinely-open work (D1 OQ-filed prose-currency follow-on = `incremental-ls-composition-lowering-residual-forthcoming-mentions-c032`; D2 c031-repair-loop-close additive `verified_against`; D3 the jacobi-smoother frontier landing). **This is recurrence-2-in-batch-9 of `cycle-planner-stale-priorities-line-recruitment`** (c031 was 2 noops on stale file-existence; c032 is 4-of-6 picks on stale already-discharged deliverables). The c031-codified file-existence check is necessary but insufficient; the planner needs a **deeper deliverable-presence check** — verify the *deliverable* isn't already present (e.g. a firm theme already having a `verified_against:` block, or a slice audit already done, or an operator already firm). **Strong batch-9 meta-phase evidence** for friction-ledger codification + the `verify-dispatch-scope-not-already-discharged` skill promotion + a cycle-planner role-spec ENFORCEMENT bullet.

## Layer-stack counts (verified on disk this cycle)

| Layer | Count |
|---|---|
| L0 | 22 chapters |
| L1 | **23 firm** (**+1: `jacobi-smoother`**) + 2 rough-in (test-coverage-bounded) + 6 rough-in (obstruction) |
| L1>L0 | 27 theme files (on-disk dep-map tally) = 21 firm + 2 rough-in + 1 partly-constructive + 3 obstruction (**+1 audit** on back-solve-MR — no count delta; cohort still complete) |
| L2 | 9 firm + 1 partly-constructive + 0 stub |
| L2>L1 | 8 = 7 firm + 1 partly-constructive (**+1 prose-currency follow-on** on incremental-LS — no count delta) |
| L3 | 9 firm + 2 partial-obstruction |
| L4 | 4 firm |
| Phase-1 removals | 9/10 |

## Build

`cargo make book` exit 0 in **88.55 seconds**, **zero build-repairs**. The new L1 chapter `book/src/L1/jacobi-smoother.md` (550 lines) + SUMMARY entry at `:88` + L1/index prose row at `:54` + L1/index table row at `:97` + the `back-solve-mutation-rotation` `verified_against:` second yaml block at `:888-912` + the 4 prose-currency edits in L2-L1/incremental-LS-composition-lowering ALL SUMMARY-registered + link-clean + parse-clean. The 13 live cross-links in jacobi-smoother.md resolve (8 unique `](./` targets: `./assemble-diagonal.md`, `./chebyshev-smoother.md`, `./ksp_solve.md`, `./eigsolve.md`, `./divfree-projector.md`, `./apply_linop.md`, `./apply_nonlinear_pencil.md` + `../concepts/variant-absorption.md`). The dead-link defects to `reciprocal.md` + `elementwise_product.md` were stripped at repair-time from live-link to plain-text inline-code (the per-report integrator confirmed: `grep` for `reciprocal.md|elementwise_product.md` on landed chapter returns 0 matches). Citecheck `--scan` on the landed chapter = 45/45 ok. Build warnings: only the 3 pre-existing KaTeX `Potential incomplete link` false-positives confined to `design/l4_calculus.md` + `concepts/plane-rotation-stream.md`, NONE introduced this cycle.

## Gate results (safety-net)

- retroactive-budget global: **0** (well under the ≥4 block threshold)
- implied-component-stub-created: **0** (correctly DEFERRED `reciprocal`/`elementwise_product` per user prompt direction; routed to cycle-033 planner as OQ `reciprocal-and-elementwise-product-l1-primitives`)
- in-cycle-live-link-upgrade: 0
- SUMMARY-registration auto-fix: 0
- path-hygiene repair: 0
- yaml-leading-quote-of-either-kind repair: 0 (the c030 codified rule held; the c031 fence-form lift held; the c032 D2 per-report integrator applied verbatim per the NOTE TO INTEGRATOR with the correct top-level ` ```yaml ` form)
- yaml-basename-AMBIG repair: 0
- citation-validity repair: 0
- cross-reference-integrity repair: 0
- staging-completeness: **3/3** rows == 3 dispatched-ready reports (no gap — thirteenth consecutive cycle)
- commit atomicity: single commit + push
- consumed-report frontmatter integrity: 3 `integrated_at` touches

## Open questions promoted (cycle-033 routing)

- **`jacobi-smoother-mutation-rotation-l1-l0`** — TOP cycle-033 routed follow-up. Natural next step now that the L1 op is firm. Structural sub-patterns A (setup body lift), B (`Mult` apply body lift), C (`omega == 0.0` spectral-radius `GetLambdaMax` sub-action — lifts identically to chebyshev-smoother-MR precedent). Routing: future-cycle abstractor dispatch — likely combined with the absent `assemble-diagonal-mutation-rotation` into a single "diagonal-preconditioner-apply mutation rotation" batched theme, OR standalone if the sibling theme is independently scheduled. Fan-out: medium-high (5 firm consumer sites become the lowering theme's downstream call-site catalog). Precedent: `ksp-solve-mutation-rotation` thin-theme.
- **`reciprocal-and-elementwise-product-l1-primitives`** — stub-or-harvest decision deferred to cycle-033 planner. 2 converging forward-references at the cycle-022 stub-creation invariant threshold; harvester dispatch preferred over claim-free stubs per CLAUDE.md "Lower-level shared vocabulary takes priority". `chebyshev-smoother` has lived with similar forward-refs for 24 cycles without stubs — the primitives merit a proper harvester dispatch.
- **`jacobi-fixed-damping-mode-consumer-coverage`** — low-priority variant-axis-coverage audit (0-of-5-consumer-sites asymmetry on the fixed-damping mode).
- **`polynomial-smoother-l2-combinator-from-jacobi-and-chebyshev`** — cycle-034+ combinator-miner candidate awaiting a third firm sibling (`richardson` is the missing one).

## Routed follow-ups for cycle-033

- **(`abstractor`, `jacobi-smoother-mutation-rotation` L1>L0 theme)** — the TOP follow-up; OQ `jacobi-smoother-mutation-rotation-l1-l0` carries full sub-pattern sketch.
- **(`harvester` or stub-create, `reciprocal` + `elementwise_product` L1 primitives)** — cycle-033 planner ranking decision; the cycle-022 stub-creation invariant threshold (≥2 converging refs) is met; harvester preferred.
- **(`combinator-miner`, `polynomial-smoother` L2 combinator from jacobi+chebyshev+richardson)** — cycle-034+ candidate; awaits a third firm sibling.
- **(cycle-planner DELIBERATE WARNING)** — the cycle-033 planner MUST run the deeper deliverable-presence check (not just file existence): verify the target deliverable is NOT already present (firm theme already has `verified_against:` block / slice already audited / promotion already test-gated-blocked). The c031 file-existence check is necessary but insufficient; c032 demonstrates the recurrence.

## Batch-9 meta-phase deferred agenda items (fires after cycle-033 finalize)

- **(a)** adjudicate friction-ledger candidate `cycle-planner-stale-priorities-line-recruitment` — **RECURRENCE NOW IN BOTH c031 AND c032** (strong batch-9 evidence); c031 was 2 noop dispatches on stale file-existence; c032 was 4-of-6 stale picks on already-discharged deliverables; the c031-codified file-existence check catches file presence but NOT already-discharged work.
- **(b)** promote skill `verify-dispatch-scope-not-already-discharged` — recurrence ≥2 in-batch demonstrated; the c027/c031 sketches need extension to deliverable-presence not just file-existence.
- **(c)** consider a `cycle-planner` role-spec ENFORCEMENT bullet — the c031 codification was guidance; the c032 recurrence suggests a hard check before each dispatch candidate is finalized.
- **(d)** carry-forward: friction-ledger candidate `negative-result-slice-canonical-instance-blocks-reduction` (carried from c031).

## Cycle character

SECOND primary cycle of meta-batch-9 (cycles 031/032/033; meta-phase fires after cycle-033 finalize). Twenty-eighth consecutive clean split-integrator cycle. 3/3 dispatched-ready reports applied clean. NO crash this cycle. Build clean, zero build-repairs, all warnings pre-existing KaTeX false-positives. The cycle is **narrow in scope** (only 3 dispatches) because the planner-staleness recurrence eliminated 3 of the proposed 6 picks; the salvageable 3 were existence-verified by orchestrator override. The substantive yield is the `jacobi-smoother` L1 leaf — a genuine frontier landing that opens the diagonal-preconditioner-apply shared-vocabulary cohort and the cycle-034+ `polynomial-smoother` L2 combinator candidacy. The cycle's process-friction signal (planner-staleness recurrence-2-in-batch) is the load-bearing meta-phase evidence; the batch-9 meta-phase (fires after cycle-033 finalize) should treat the deeper deliverable-presence check as the lever-point.
