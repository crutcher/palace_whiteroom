## 2026-06-04 cycle-097 — 6 reports applied clean — ninety-second consecutive cycle under split integrator — **POSITION 1/3 OF META-BATCH-31 (the FIRST primary cycle; the batch-31 meta-phase fires AFTER cycle-099, NOT this cycle)** — THE GRADED-STACK P2 SLICE-DELETION CAMPAIGN FIRST TRANCHE: **4 of the 9 Phase-1 `spec/slices/*` slices DELETED + made fully unreachable** (their material absorbed into firm homes + ~18 inbound markdown links repointed; the slice corpus shrank **9 → 5**) + the FIRST cycle finalize **step-5b (the finalize-runs-linters build-gate companion)** ran on the landed tree — pass

> **Note:** this file (`cycle-97.md`) is the current-run, structural-redirect-era cycle-097 log; it supersedes any stale May-25 slice-vertical-era `cycle-97` numbering (preserved as `cycle-097.md` + in git history). The current-run numbering does NOT reset across batch boundaries.

**POSITION 1/3 OF META-BATCH-31** (3:1 cadence; cycles 097/098/099; the cycle counter does NOT reset across batch boundaries; **the batch-31 meta-phase fires AFTER cycle-099's finalize as a SEPARATE dispatch aggregating 097/098/099** — this cycle-097 finalize runs NO meta-phase housekeeping). Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) + the 2026-06-02/2026-06-03 user directives + the **2026-06-04 GRADED RESOLUTION LADDER + FEATURE-ROOT REACHABILITY user directive** (full spec `METHODOLOGY-GRADED-STACK.md`; the campaign is priorities item-0) + the batch-30 meta-phase enactment (which **GO'd the P2 slice-deletion tranche as the batch-31 LEAD** and **wired finalize step-5b** via the session restart — this is the FIRST cycle the step-5b build-gate companion ran).

### Headline

The deferred **P2 slice-deletion tranche** (the batch-31 LEAD) LAUNCHED. **4 of the 9 Phase-1 `book/src/spec/slices/*` slices were DELETED and made fully unreachable from any GC root**, their material absorbed into firm homes and ~18 inbound markdown links repointed. The slice corpus shrank **9 → 5**. The 5 survivors (`arnoldi_step`, `cg`, `gmres`, `orthog`, `polynomial_recurrence_step`) are deferred to c098/c099 by design — the krylov trio `cg`/`gmres`/`arnoldi_step` is the deeply-interwoven ~30-anchor sub-campaign.

6 of 6 dispatched-ready reports applied clean (6/6 staging rows == dispatched-ready — the cycle-018 staging-completeness gap did NOT recur for the SEVENTY-EIGHTH consecutive clean staging / NINETY-SECOND consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs.

### Reports

- **D1** (same-layer-cross-cutter, build-relevant) — `cg_preconditioning_framework` slice **DELETED** + 7 concept-page links repointed onto the firm `book/src/L4/preconditioning-framework.md` (the c096-authored firm chapter — the P2 vocabulary gap that unblocked this deletion). All 7 inbound were `reference`-class navigational; no inbound `depends-on` blocking edge targeted the slice; rank-gate PASS (reachability-safe leaf deletion).
- **D2** (same-layer-cross-cutter, build-relevant) — `divfree` slice **DELETED** + 3 `book/src/L1/` re-anchors (`ksp_solve.md:131` parenthetical + Evidence bullet → `divfree-projector.md`; the footer slice-corpus-precedent link → claim-free Provenance prose). The absorb was a no-op — the firm L1 homes already carried the positive L0 `divfree.cpp:175`.
- **D3** (same-layer-cross-cutter, build-relevant) — `sparse_triangular_solve` slice **DELETED** + 3 absorbed L0 findings folded into `book/src/L1-L0/triangular-solve-obstruction.md` §(d) (d1/d2/d3) + 7 L0 anchors appended to the Verified-against list + 3 concept repoints + the firm-theme self-link collapsed + the `annotated-and-retained` framing retired (the theme declared sole home; the `verified_against:` YAML slice entry → `verdict: absorbed-and-deleted`). One discretionary trailing-connective smoothing on `scope-out-obstruction.md` Change-3a (avoided a doubled-"with" garble; link target + meaning exactly as proposed).
- **D4** (same-layer-cross-cutter, build-relevant) — `plane_rotation_stream` slice **DELETED** + the L3 replay-prefix worked example absorbed into `book/src/concepts/sequential-obstruction.md` §"Givens-stream replay-prefix (GMRES least-squares update)" re-anchored DIRECTLY to L0 `iterative.cpp:634-640` (+3 absorbed sub-analyses: local-triviality-at-extend, cross-target-no-batch-dim, Householder-WY sibling boundary) + 5 concept repoints (`givens.md`/`plane-rotation-stream.md` → `../L2/incremental-least-squares.md`; `givens_apply.md`/`givens_generate.md` → `../L1/ls-update-column.md`). Repairer fired pre-integration (citation-validity: OQ-reconcile line-numbers corrected to :72=template/:73=signature; the `ls-update-column-firm-home-placeholder` token inlined to the resolved path). D3↔D4 same-file coupling on `sequential-obstruction.md` fully dissolved (D3's :53 edit and D4's :84-113 worked-example edit ~30 lines apart, non-overlapping).
- **D6** (lifter, build-relevant) — `book/src/L4/domain_energy_reduce.md` 3 within-file matrix-weighted-norm maturity re-anchors (`:377` Folded-L1-primitives + `:268` Lowers-to + `:374` Supporting-test — `rough-in (test-coverage-bounded)` → `firm` under the firm-on-positive-structure escape; both folded primitives firm: participation_ratio c077 + matrix-weighted-norm c091). Discharges the c096-opened OQ `domain_energy_reduce-377-mwn-stale-rough-in-residue`. NO node status flip (`rank: firm` already on disk; a within-file maturity-NARRATION re-anchor). The `:313-316` gram_reduce "STAYS rough-in" residue deliberately scoped OUT (a different verb's residue; logged as a new follow-up OQ).
- **D5** (layer-intro-author, build-relevant — the per-slice GC completion) — removed the 4 deleted-slice rows from `book/src/SUMMARY.md` + `book/src/spec/index.md` and the snake_case `divfree`/`cg_preconditioning_framework`/`plane_rotation_stream` mermaid `-->` edges from `book/src/concepts/dependency-map.md` (Edit ×10 across the L1/L2/L3/L4 blocks). This makes the 4 deleted slices FULLY unreachable from any root over any edge — the campaign's per-slice mark-sweep completion. The 5 SURVIVOR rows + their index/SUMMARY/mermaid references INTENTIONALLY left intact for c098/c099. Repairer fired pre-integration (softened the green-build claim to cycle-scope + corrected the 44→61 mermaid-edge tally; proposed-change blocks untouched).

### Build + step-5b

`cargo make book` (mdbook + linkcheck2) **EXIT 0**, ~92s. The 4 slice deletions + ~18 inbound repoints + D5's SUMMARY/index/mermaid row removals all co-landed link-safe: linkcheck2 zero dead links (no dangling `[..](..)` to a deleted slice; no SUMMARY entry at a deleted file; the surviving-5 slices' references to the deleted-4 were all bare-backtick inline-code, build-safe — confirmed against the build). Only pre-existing benign KaTeX `Potential incomplete link` WARNs (4, in `design/l4_calculus.md`). NO finalize build-repair needed (clean first build) — the cycle's hard co-landing constraint (D5's single-owner SUMMARY/index/mermaid removals co-landing with the D1–D4 deletions) held.

**STEP-5b graded-stack linters** (the build-gate companion to `cargo make book`, FIRST run this cycle after the batch-30 meta-phase wired it) — `python3 tools/graded-stack-lint/graded_stack_lint.py --json` on the LANDED tree:

- **`rank_violations: 0`** (was 0 at c096; the baseline-exceptions were fully discharged at c096 → ANY violation would be NEW and BLOCK — there are NONE; **GATE PASSES**).
- **NO newly-orphaned node** (the 4 deleted slices were `reference`-class detritus leaves already in the pre-P1 detritus mass — an INTENDED deletion, not an orphaning; the second step-5b block condition also clears).
- `rank_histogram: {firm: 192, rough-in: 7, partly-constructive: 3, obstruction: 2, partial-obstruction: 4}`.
- `files=356` (was 360 — the 4 deleted slices), `typed=208`, `untyped=148` (was 152 — the 4 deleted slice nodes left the untyped tail), `roots=36`, `promotion_frontier=10`, `detritus=172`, `unresolved_depends_on_targets=35`, `expected_unreachable_outside_dag=22`.
- The slice-node set in the linter output is now EXACTLY the 5 survivors; the 4 deleted are gone. The high untyped/detritus mass is P2 mid-campaign (5 slices still present + the as-yet-untyped pre-P1 tail) — informational, NOT a build-gate failure. The `cites-evidence` L0-range `depends-on` edges remain exempt from slug-resolution + rank-check.

### Counts

UNCHANGED from c096 (the slice corpus is Phase-1 raw material, NOT the layered-vocabulary deliverable; D6 is a within-file maturity-NARRATION re-anchor with `rank: firm` already on disk): L1 firm 32 main / 39 grand · L4 firm 20 main / 24 grand · L4 rough-in 0/0 cohort EMPTY · L4>L3 10 · L3 17+4po · L3>L2 6 · L2 21+1pc · L2>L1 11 · L0 22 · concepts 33 · methodology 4 · feature spine 11 FIRM / 1 seed · L4 reduce-family 4 verbs ALL firm. **SLICE CORPUS: 9 → 5** (`spec/slices/`: `arnoldi_step`, `cg`, `gmres`, `orthog`, `polynomial_recurrence_step` remain).

### Process

retroactive-budget global = 0 (4 absorb-and-deletes into existing firm homes + 1 index cleanup + 1 within-file re-anchor; no retroactive rewrites of existing firm-chapter CLAIMS). Repair fired on D4 + D5 (mechanical; proposed-change blocks untouched). Per-report safety-net gates all PASS/N/A. 0 implied-component stubs. **5 OQs recommended-CLOSE by per-report intake** (flagged for the batch-31 meta unify — per-report integrators have no OQ-close authority; finalize RECORDS the recommendation, the meta-phase closes): `l4-preconditioning-framework-promotion` (D1), `plane-rotation-givens-l0-citation-range-reconcile` (D4), `domain_energy_reduce-377-mwn-stale-rough-in-residue` (D6), `sparse-trisolve-rename-to-sparse-direct-solver-wrapper` + `sparse-trisolve-mfem-superlu-factor-allgatherv-family` (D3). **NEW follow-ups (per-report intake):** `dependency-map-cg-precond-stale-mermaid-edges` (D1 — D5 removed the snake_case edges this cycle; closeable at the meta unify), `domain_energy_reduce-313-gram_reduce-bilinear-form-c095-stale-rough-in-narration` (D6, gram_reduce-cohort land-clean), + a still-open end-bound citation-divergence sub-note in `arnoldi_step`/`polynomial_recurrence_step`/`composition-lowering` (D4 critic Issue 3, for a downstream verify-citation-range pass — appended under the closed reconcile OQ, NOT itself closed). `scaffolding/priorities.md` + `scaffolding/open-questions.md` also modified (cycle-097 planner co-owned plan write + per-report OQ intake), committed atomically. NO `.claude/agents/` changes this cycle (the step-5b enactment was the batch-30 meta-phase's; this finalize only RUNS it) → no session-restart concern from this cycle.

### Campaign state

graded-stack two-axis health campaign (priorities item-0): P0 DONE c094; P3 DONE c094; P1 DONE (rank-violation criterion MET = 0 for the typed subset c096); **P2 slice-deletion FIRST TRANCHE landed c097** (4 of 9 deleted + unreachable; slice corpus 9 → 5). REMAINING P2 (c098/c099): the krylov trio `cg`/`gmres`/`arnoldi_step` is the deeply-interwoven ~30-anchor sub-campaign; plus `orthog` (MPI-collective-shape absorb) + `polynomial_recurrence_step` (5-axis-table absorb, Cohort-A-adjacent). Completion criterion = the reachability GC shows `spec/slices/*` empty.

### Next

cycle-098 (batch-31 position 2/3) continues P2 — the krylov-trio sub-campaign and/or `orthog`+`polynomial_recurrence_step`. The batch-31 meta-phase fires after cycle-099's finalize (aggregating 097/098/099) and should close the 5 recommended-resolved OQs above + the `dependency-map-cg-precond-stale-mermaid-edges` verify-after-D5 OQ.

— written by `integrator-finalize` (split integrator-per-report ×6 + finalize ×1)
