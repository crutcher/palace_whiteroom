# Cycle 029 — L1>L0 firm themes +2 (back-solve-mutation-rotation + bilinear-form-mutation-rotation) + L1>L0 obstruction theme +1 (triangular-solve-obstruction — first opaque-library-ownership obstruction) + L1 firm 21→22 (+ls_update_column GMRES per-column running-QR leaf) + normalize_B F1 prose correction + L2-L1/L2 index prose refresh (second primary cycle of meta-batch-8)

**Date:** 2026-05-30 · **Commit:** `e44896d` · **Status:** clean (6 of 6 dispatched-ready reports applied; zero deferrals; zero rejections; zero build-repairs; twenty-fifth consecutive clean split-integrator cycle)

**Batch position:** cycle-029 is the **SECOND** primary cycle of **meta-batch-8** (cycles 028/029/030). The batch-8 meta-phase fires after the cycle-030 finalize commit (3:1 cadence; cycle counter does NOT reset across batch boundaries). This `log/cycle-29.md` + the `scaffolding/integrator-signals.md` cycle-029 section CONTINUE the batch-8 evidence window opened by cycle-028.

(Filename note: the prior `cycle-029.md` (slice-vertical-era frozen stub, 6 lines, "back chebyshev — pass" from 2026-05-25) has been renamed to `cycle-029-legacy.md`; the layered-flow era reclaims the `cycle-29.md` namespace, per the cycle-020→028 precedent. The `log/README.md` legacy index entry has been updated to point at the renamed file.)

## Summary

A vocabulary-buildup cycle on the **GMRES restart-cycle least-squares L1 cohort** and a **methodologically-noteworthy first opaque-library-ownership obstruction**. 6 dispatched-ready reports, all applied clean (6/6 staging rows == dispatched-ready-reports — the cycle-018 staging-completeness gap did NOT recur for the **tenth** consecutive cycle). Four substantive landings (two firm L1>L0 themes, one obstruction L1>L0 theme, one firm L1 leaf), one F1 prose correction following up on a cycle-028 lowering-verifier finding, and one navigational index refresh.

## Headlines

- **HEADLINE 1 — L1>L0 firm themes +2 (joining the GMRES restart-correction back-solve and the matrix-weighted bilinear-form into their L0 forms).**
  - `back-solve-mutation-rotation` NEW firm L1>L0 theme. Lowers the firm L1 leaf `back_solve(R, s) -> y` forward into the GMRES restart-correction back-substitution loop at `palace/linalg/iterative.cpp:652-660` (GMRES) / `:831-840` (FGMRES) — shape-identical, brace placement +1-line-shifted, basis-lift `V[k]` vs `Z[k]` differs only in the *downstream* `linear_combination` (outside the leaf — basis-lift independence is the L1 entry's law-6). The rewrite is structural: pure-functional `y = back_solve(R, s)` lowers to an in-place loop overwriting the RHS buffer `s[0..j]` with `y`, reading the upper-triangular factor `R` out of the flat column-major Hessenberg register `H` via the stride pointer `Hi = H.data() + i*(max_dim+1)`, descending-`i` / descending-`k` back-substitution sweep. One load-bearing numerical detail (descending-column reduction order, classified per the L1 entry's reduction-order non-law) recorded as an explicit non-rewrite. Registered between `normalize-mutation-rotation` and `nleps-deflated-residual-mutation-rotation` (dep-map) / between `normalize-mutation-rotation` and `lu-solve-mutation-rotation` (SUMMARY).
  - `bilinear-form-mutation-rotation` NEW firm L1>L0 theme. The off-diagonal sibling of `matrix-weighted-norm-mutation-rotation` (the `y == x` SPD-weighted case): lowers `bilinear_form(x, M, y) = xᴴ M y` forward into the L0 callsites `Bttr` (Hermitian, `palace/models/boundarymodeoperator.cpp:85`) + `Atn` (non-Hermitian cross-coupling, `:90`) via `M.Mult(y, Mx); dot=Dot(comm, x, Mx)`. The repaired L1 composition identity is the upstream-canonical `dot(x, apply_linop(M,y))` (NOT `dot(apply_linop(M,y), x)` — repaired in three places). Internally-allocated workspace `Mx` (Category-4 — distinct from `matrix-weighted-norm`'s caller-supplied `Bx` — Category 4 here is the in-theme contract, not the L0 workspace category). The firm theme lowering of a `rough-in (test-coverage-bounded)` L1 entry — same firm-theme-over-rough-in-L1-entry pattern as `matrix-weighted-norm-mutation-rotation` / `eigsolve-mutation-rotation`. Registered after `matrix-weighted-norm-mutation-rotation` in both index and SUMMARY (grouped by L0 file block).

- **HEADLINE 2 — L1>L0 obstruction themes +1: `triangular-solve-obstruction` (the FIRST opaque-library-ownership obstruction in the cohort).** Status `obstruction`, claim-free. Resolves the `trsv` leaf of the migrated-plan-item `l3-vocabulary-inventory-gap` as **resolved-by-obstruction** (giving the resolved leaf its citable home; the parent gap was already CLOSED cycle-028). Negative anchors: HYPRE GS/SSOR relax-type flags `amg.cpp:19,29` + `ams.cpp:162,173`; external direct-solver wrappers `strumpack.hpp:21`, `superlu.hpp:22`, `mumps.hpp:21`; Palace-native smoothers GS-free `jacobi.hpp:19` + `chebyshev.hpp:23,82`; red-herring block-triangular non-example `blockprecond.hpp:16-29`. **Methodologically distinct from the two cycle-004 enum-only-stub obstructions** (`minres-iteration`, `bicgstab-iteration`): those document a Palace-internal stub (enum + `MFEM_ABORT`); this one documents an external opaque-library boundary (HYPRE / external direct solvers own the kernel; Palace ships *callers* into them, not the kernel itself). The cycle-029 abstractor surfaced this sub-kind distinction in §Justification-kind; flagged for the batch-8 meta-phase as a possible methodology refinement (`opaque-library-ownership` vs `enum-only-stub` obstruction sub-kinds). The obstruction-cluster grouping is now 3 themes — `bicgstab-iteration` / `minres-iteration` / `triangular-solve-obstruction`. Registered clustered with the two enum-only-stub obstructions (index: after `bicgstab-iteration`; SUMMARY: after `minres-iteration`, before `chebyshev-smoother-mutation-rotation`). The TWO plain-text references the report carried to `back-solve-mutation-rotation` were **UPGRADED in-line to live links** by the per-report integrator (in-cycle live-link upgrade ×2) since report-1's landing made the target on disk earlier in the cycle.

- **HEADLINE 3 — L1 firm 21→22 (+`ls_update_column`, the GMRES/FGMRES per-column running-QR leaf; closes the c027 `ls_update_column` slug-collision saga).** `ls_update_column :: (variant, cs, sn, s, j, h_new) → K'` advances the incremental QR factorisation of the upper-Hessenberg least-squares system by one column — replays stored rotations `0..j-1` against the new Hessenberg column, generates one new rotation `(cs[j], sn[j])` annihilating the sub-diagonal, applies to the column and the RHS pair, and **exposes the LS residual norm** `β = |s[j+1]|` as a unitary byproduct (no explicit residual evaluation). The **column-streaming producer** sibling of `back_solve` (the terminal consumer): one runs per Arnoldi column (this leaf) and produces the R-factor + rotated RHS; the other runs once at restart-cycle close (the back-solve) and finishes the LS solve. The Face-1 opaque-leaf projection of the firm L2 named composition `incremental-least-squares` — co-extensive with the de-fused Face-2 scalar-Givens-kernel-pair presentation surfaced by the L2>L1 theme `incremental-least-squares-composition-lowering` (firm cycle-028). The **factorisation-streaming sibling of `orthogonalize`** (basis-streaming) on the Krylov-state-advance axis — both invoked once per Arnoldi column inside `krylov-step`; `orthogonalize`'s output **is** this leaf's `h_new` input. Firm-on-positive-structure: read in full in both the GMRES (`iterative.cpp:634-640`) and the line-for-line identical FGMRES (`:813-819`) arms; the absent dedicated running-QR test does not gate the syntactic-identity laws (the `back_solve` / `lu_solve` / `apply_linop` precedent). Two load-bearing non-laws: replay-chain bit-level reduction order; per-column call-sequence non-commutativity. **Closes the c027 D5 forward-reference / `back_solve` slug-collision saga end-to-end**: the c027 deferred D5 abstractor draft inverted the slug premise — `ls_update_column` legitimately denotes the column-streaming step (the c029 harvest) while the terminal back-solve must re-anchor to the c027 firm `back_solve` leaf; the c028 D1 abstractor landed the L2>L1 theme with `ls_update_column` plain-text forward-references (correct at the time, target not on disk); cycle-029 lands the on-disk leaf with the matching slug.

- **HEADLINE 4 — `normalize_B` F1 prose CORRECTED across `normalize-mutation-rotation.md` + `normalize.md` (RESOLVES the c028 OQ from the lowering-verifier audit).** The c028 lowering-verifier audit found ONE `does-not-support` row (F1: a defined-but-uncalled fused `Normalize(comm,x,B,Bx)` at `palace/linalg/operator.hpp:377-384` contradicts the theme's "no fused B-Normalize" prose). The c029 abstractor rewrote in 3+4 paired edits: §Speculative-L1-operators rough-in note "no fused" → **"exists but uncalled"** (both files); §promotion-gate tightened "find that the function exists" → **"find a positive *callsite*"** (both files; the gate STAYS open but the bar tightens — mere existence is now explicitly insufficient). Path-hygiene safety-net repair at land-time: 3 bare-basename `operator.hpp` references introduced by the edit blocks were replaced with the unambiguous `palace/linalg/operator.hpp` full path (against the sibling `palace/fem/libceed/operator.hpp` ambiguity). The c028 audit's F1 `verified_against:` row at `normalize-mutation-rotation.md:466-469` is now **stale** — the prose F1 cited is no longer present in the artifact; routed to a follow-up lowering-verifier refresh OQ (low-priority, non-gating, audit-row staleness only — NOT a defect of the firm unweighted core).

- **HEADLINE 5 — L2-L1 + L2 index prose REFRESHED (no chapter-body content edited; pure navigational refresh).** `L2-L1/index.md`: appended Vocabulary-cohort subsection (7 firm + 1 partly-constructive — between the dep-map table and Working Notes) + appended cohort-growth-log bullet to Working Notes. `L2/index.md`: refreshed the Named-compositions motif paragraph at §Semantics overlay (extended from 2-exemplar `orthogonalize`/`ksp_solve` to 4-exemplar including `incremental-least-squares`/`eigsolve` framing); removed the now-empty "Queued at L2 (stub)" subsection from §Vocabulary cohort; added `incremental-least-squares` + `eigsolve` to the "Firm at L2" sub-list (closing the stale-by-omission gap the critic flagged); refreshed the Working-Notes stub-queue bullet to a `l2-named-composition-lifts`-closed bullet. Counts re-verified post-apply: `grep -c "^| \[" L2-L1/index.md` returns 8 (exact match); `grep -c "^| \[" L2/index.md` returns 10 (exact match).

## Layer-stack counts (verified on disk this cycle)

| Layer | Count |
|---|---|
| L0 | 22 chapters |
| L1 | **22 firm** (+1: `ls_update_column`) + 2 rough-in (test-coverage-bounded) + 6 rough-in (obstruction) |
| **L1>L0** | **19 theme files** = **16 firm** (+2: `back-solve-mutation-rotation`, `bilinear-form-mutation-rotation`) + **3 obstruction** (+1: `triangular-solve-obstruction`) |
| L2 | 9 firm + 1 partly-constructive + 0 stub |
| L2>L1 | 8 = 7 firm + 1 partly-constructive |
| L3 | 9 firm + 2 partial-obstruction |
| L4 | 4 firm |
| Phase-1 removals | 9/10 |

## Build

`cargo make book` exit 0, **zero build-repairs**. All 3 new L1>L0 chapter files + 1 new L1 leaf + the 2 prose-corrected files + the 2 index files all SUMMARY-registered + link-clean. The 9 live links the `triangular-solve-obstruction` theme carries (incl. the two in-cycle-upgraded ones to `back-solve-mutation-rotation`) all resolve on disk. Build warnings: the same KaTeX `Potential incomplete link` false-positives confined to `design/l4_calculus.md` math-display + the pre-existing HTML-tag-in-prose warnings on `L1-L0/ksp-solve-mutation-rotation.md` / `L0/linalg-solver-file.md` / `meta-reviews/2026-05-24-cycles-25-30.md` — NONE introduced this cycle. The pre-existing `tools/citecheck` MISS at `book/src/L2/index.md:70` (historical/provenance bullet `spec/slices/chebyshev.md:354-362` narrating a cycle-015 absorption — the parenthetical literally says "absorbed the former") was noted by the report-6 integrator but is **semantically intentional** (the slice was removed and the prose narrates that removal); NOT a new defect, NOT touched this cycle.

## Gate results (safety-net)

- retroactive-budget global: **0** (well under the ≥4 block threshold)
- implied-component-stub-created: 0
- in-cycle-live-link-upgrade: **2** (`triangular-solve-obstruction` → `back-solve-mutation-rotation` ×2; report-3 integrator applied the `upgrade-plain-text-ref-to-live-link-when-target-on-disk` skill after verifying the target landed by report-1 earlier in the same cycle)
- SUMMARY-registration auto-fix: 0
- path-hygiene repair: **1** (report-5: 3 bare-basename `operator.hpp` references → `palace/linalg/operator.hpp` full path)
- citation-validity repair: **3** (report-2: misquoted L1 composition identity `dot(apply_linop(M,y),x)` → upstream-canonical `dot(x, apply_linop(M,y))` in three places; report-2 also: cosmetic `:88-90` → `:88-89` Atn-construction span correction; report-4: off-by-one anchor `:88` → `:87-88` on the slug-bearing sentence in the L2-L1 theme repaired in three places)
- cross-reference-integrity repair: **1** (report-3: §Related section + 3 `verified_against:` positive-cross-reference rows + 1 §Open questions / caveats entry added by repairer — all surgical pointer-and-relationship additions, no new substantive claims)
- staging-completeness: 6/6 rows == 6 dispatched-ready reports (no gap)
- commit atomicity: single commit + push
- consumed-report frontmatter integrity: 6 `integrated_at` touches

## Open-questions routed (full list)

Per-report integrators promoted the following OQs (newest-prepended sections appended to `scaffolding/open-questions.md` between the c028 meta-phase unification pass and this finalize):

- From report-1 (back-solve-mutation-rotation): 5 OQs — `back-solve-law-6-leaf-prose-tightening-c029`, `back-solve-mutation-rotation-cycle-030-verified-against-audit-c029`, `back-solve-mutation-rotation-l2-l1-incremental-least-squares-boundary-c029`, `back-solve-mutation-rotation-empty-cycle-j-minus-one-reachability-c029`, `back-solve-mutation-rotation-reduction-order-section-promotion-stylistic-c029`.
- From report-2 (bilinear-form-mutation-rotation): 4 OQs — `bilinear-form-mutation-rotation-cycle-030-verified-against-audit-c029`, `bilinear-form-l0-surface-comment-callout-polish-c029`, `bilinear-form-l2-weighted-inner-product-reduction-combinator-c029`, `bilinear-form-l1-entry-upstream-variant-axis-coverage-gaps-c029-tracking`.
- From report-3 (triangular-solve-obstruction): 2 OQ sections — `triangular-solve-obstruction-l1-l0-theme-LANDED-c029` (resolution-record + cascading closures), `sparse-triangular-solve-slice-reduction-after-l1l0-theme-lands`.
- From report-4 (ls_update_column): 2 OQs — `ls-update-column-l2-l1-theme-plain-text-ref-upgrade-to-live-link-c029`, `ls-update-column-mutation-rotation-l1l0-theme-forthcoming-c029`.
- From report-5 (normalize-B prose correction): 2 OQs — `normalize_B-note-says-no-fused-B-Normalize-but-uncalled-fused-operator-exists-RESOLVED-c029`, `normalize-mutation-rotation-verified-against-row-466-469-stale-after-c029-prose-correction`.
- From report-6 (L2-L1/L2 index prose refresh): 0 OQs (all 3 report-§Open-questions items confirmed by critic+repairer as "not a defect / framing choice" — none required ledgering).

## Routed follow-ups (for cycle-030 planner / batch-8 meta-phase)

- **(`lowering-verifier`, `back-solve-mutation-rotation`)** — cycle-030 verified_against audit (standard sibling-theme follow-up).
- **(`lowering-verifier`, `bilinear-form-mutation-rotation`)** — cycle-030 verified_against audit (standard sibling-theme follow-up).
- **(`lowering-verifier`, `ls_update_column`)** — cycle-030 verified_against audit (firm L1 leaf follow-up).
- **(`abstractor`, `ls-update-column-mutation-rotation`)** — the forthcoming L1>L0 theme; sibling to `back-solve-mutation-rotation` (c029) + `orthogonalize-mutation-rotation` (firm); completes the GMRES inner-loop L1>L0 cohort.
- **(`lifter` or `same-layer-cross-cutter`, `L2-L1/incremental-least-squares-composition-lowering`)** — plain-text-ref → live-link upgrade for the now-on-disk `ls_update_column` at `:69`/`:87-88`/`:307-310`. The `:87-88` reference requires substantive prose rewrite (the entire "forthcoming / not yet on disk / plain text per the rough-in-forward-reference convention" sentence is now factually obsolete and needs replacement, not just slug-relinking) — beyond mechanical-token-relink integrator skill scope.
- **(`lowering-verifier`, `normalize-mutation-rotation`)** — F1 `verified_against:` row :466-469 refresh (stale after c029 prose correction; low-priority audit-row staleness only).
- **(`same-layer-cross-cutter`, `book/src/spec/slices/sparse_triangular_solve.md`)** — Phase-1 slice-reduction candidacy now that the L1>L0 obstruction theme has landed (low-fan-out follow-up the repairer surfaced).

**Cycle-030 is the THIRD/FINAL primary cycle of meta-batch-8** — the batch-8 meta-phase fires after the cycle-030 integrator-finalize commit. Cycle-030 should be sized to leave room for the batch-8 meta-phase enactments (codemap-drift codification, the obstruction sub-kind refinement if the meta-phase elects it, OQ unification, etc.).

## Meta-phase-deferred actions (NOT enacted by finalize — batch-8 meta after cycle-030)

- Strike the plan-owned RESOLVED OQ lines in `priorities.md` (the per-report integrators recorded RESOLVED disposition sections in `open-questions.md` for the meta-phase to migrate).
- Close the `l3-vocabulary-inventory-gap` parent in the plan (already done as resolved-by-obstruction cycle-028; the priorities.md plan-owner pointer can now be retired).
- Adjudicate the skill candidate `establish-negative-finding-exhaustiveness` (filed c028).
- Address the leading-`"` `verified_against:` note channel-format hazard (flagged c028: a YAML scalar beginning with a literal double-quote needs single-quote wrapping).
- **NEW c029 candidate** — the obstruction sub-kind refinement (`opaque-library-ownership` vs `enum-only-stub`) the `triangular-solve-obstruction` report surfaced. Methodologically distinguishes the c029 cohort entry from the cycle-004 cohort entries.
- The several c029 follow-up OQs listed in §Routed follow-ups (verified_against F1 row refresh, ls_update_column plain-text-ref upgrade, ls-update-column-mutation-rotation theme, sparse_triangular_solve slice-reduction candidacy).
- (drive-by) The `log/` legacy-index cleanup — pre-existing dangling legacy entries for `cycle-NNN.md` files the current era has clobbered (out of finalize scope; this cycle did the `cycle-029` instance only, as housekeeping).

## Cycle character notes

- The D5 (`abstractor-triangular-solve-obstruction`) and D6 (`layer-intro-author-L2-L1/L2-index-prose-refresh`) dispatches each needed **3 attempts** due to two API failures during initial dispatch. Both completed clean on attempt-3 and applied without defect. Recorded as a cycle_character note in `scaffolding/cycle-record.jsonl`; not a methodology pattern (transient infra noise), but a data point in case the cluster repeats.
- **Twenty-fifth consecutive clean cycle** under the split-integrator architecture (cycles 005→029 inclusive; the cycle-018 staging-completeness gap was a one-shot architectural defect that has not recurred for 10 consecutive cycles).
