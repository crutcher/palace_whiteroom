## 2026-05-28 cycle-017 — SECOND primary cycle of meta-batch-4 (3:1 cadence; cycles 016/017/018, meta-phase fires after 018) — 5 reports — thirteenth cycle under split integrator — consolidation/frontier cycle: ONE new L2 rough-in combinator (linear_combination dep-map row; constructive prong-b of the human-raised BLAS-1 variadic-fold unification; L2 rough-in cohort 0→1) + firm-L1 divfree-projector 11-reference citation-drift maintenance (stays firm) + krylov-step cg.md re-anchor chain FULLY TERMINATED (L3-L2 body-identity swept) + chebyshev forM_/foldM→iterate_while vocabulary-lag cohort FULLY TERMINATED (L3 sibling sweep, zero residual) + read-only closure-nesting cross-layer audit REFUTING the cycle-016 "first" premise (eigsolve cycle-011 is the prior, divfree the 3rd) — ZERO new firm operators — 1 build-repair (de-linked the rough-in forward-reference that linkcheck2 failed on) — META-SIGNAL: specialized-agent-direct-write-to-book RECURRENCE-3 routed to batch-4 meta-phase

- **Phases fired**: plan (cycle-planner haiku) → dispatch (5 parallel specialized: 1 combinator-miner + 1 harvester + 2 lifter + 1 cross-layer-cross-cutter) → critique (5× critic) → repair (repairers triggered — harvester dispatch-phase book/ leak reverted via git checkout; cross-layer-cross-cutter follow-up-recommendations relocated into §Open-questions for auto-promotion; cosmetic) → **integrate (5× integrator-per-report serial → 1× integrator-finalize; thirteenth cycle under split integrator)**.

- **Meta-batch context**: cycle-017 is the **SECOND primary cycle of meta-batch-4** under the 3:1 meta cadence (user directive 2026-05-27 post-cycle-006 meta). Cycles 016/017/018 form batch-4; **the batch-4 meta-phase fires after the cycle-018 finalize commit**, aggregating evidence across the full 3-cycle batch. The cycle counter does NOT reset at batch boundaries (batch-1 = 007/008/009; batch-2 = 010/011/012; batch-3 = 013/014/015; batch-4 = 016/017/018). The meta-phase does NOT fire this cycle. Compactify-after-meta-phase applies after the cycle-018 meta-phase, not here.

- **Cycle character**: a **consolidation/frontier cycle** — one new L2 rough-in combinator (dep-map row only), with the rest being firm-entry citation maintenance + two one-file-per-dispatch hygiene chains reaching their TERMINAL state + a read-only cross-layer audit that refuted a prior premise without mutating the surface. **ZERO new firm operators** (firm L1/L2/L3/L4 unchanged at 11/2/8/4); **L2 rough-in cohort 0 → 1**. Two established cohorts reached completion this cycle: (1) the krylov-step `cg.md` re-anchor chain (cycle-014 L4>L3-theme → cycle-015 L3-entry → cycle-016 L4-entry + L2-entry → cycle-017 L3-L2 body-identity-theme) is now **fully terminated**; (2) the chebyshev `forM_`/`foldM`→`iterate_while_pure` vocabulary-lag cohort (cycle-015 L4 body → cycle-016 L4 prose + L3 named-sentence → cycle-017 L3 siblings) is now **fully terminated** (post-apply re-grep: zero residual `forM_`/`foldM` in `L3/chebyshev.md`).

- **Substantive landed (5 reports, all applied; zero deferrals/rejections)**:
  - **Position 1 (combinator-miner — linear-combination-fold)**: `book/src/L2/index.md` — **NEW L2 rough-in dep-map row** for `linear_combination :: [(Scalar, Tensor[N])] -> Tensor[N]` (≡ `foldl (\acc (a,t) -> acc + a·t) zeros pairs`), unifying the BLAS-1 scalar-weighted-sum cohort (`scal`/`axpy`/`axpby`/`axpbypcz`) along the **arity** axis — complementary to the `scalar-promotion` element-type-axis unification of the same four. This is the constructive half (**prong b**) of the human-raised OQ `blas1-variadic-linear-combination-fold-unification` (ledger line 2937, opened cycle-016). The harvester formalizes the operator chapter (OQ `linear-combination-harvester-formalization`); the `L2-L1/linear-combination-fold-specialization` lowering theme is abstractor follow-up. 2 OQs opened. **L2 rough-in cohort 0 → 1.** **NOTE — build-repair**: the producer's dep-map row used a LIVE markdown link `[`linear_combination`](./linear_combination.md)` to the not-yet-authored chapter; `mdbook-linkcheck2` failed the build with `File not found: ./linear_combination.md`. integrator-finalize de-linked the cell to a plain-text forward-reference (the cycle-015 `fem-bilinearform-file` no-dead-link convention) — the per-report integrator's claim that the forward-link "matches krylov-step/chebyshev-iteration and does not break the build" was incorrect (those rows' targets EXIST as files; an absent-file markdown link is a hard `linkcheck2` error regardless of SUMMARY registration).
  - **Position 2 (harvester — divfree-l1-citation-fix)**: `book/src/L1/divfree-projector.md` — **11 surgical citation-line-reference corrections** to the FIRM L1 operator entry (no signature/semantics/law/status/variant-axis change; stays `firm`): apply `Mult(VecType &y)` close-brace `:155-186`→`:155-187` at all FOUR sites (§Context/§Semantics/§Status/§Evidence), CG rel-tol `:140`→`:141` (§Algebraic-laws idempotence caveat; `:140` is `SetInitialGuess(false)`), 5 hpp off-by-one/range-tighten corrections (MPI_Comm `:62`→`:63`; psi/rhs scratch `:55`→`:54`; `class DivFreeSolver` `:34`→`:33`; member-fields `:40-55`→`:40-54`; ksp-solver comment `:51`→`:50`), and the dangling `(see Variant axes)`→`(see Signature, the y element type)` pointer fix (the entry has no `## Variant axes` heading). OQ `divfree-l1-entry-apply-close-and-reltol-line-drift` resolved. **CRITICAL META-SIGNAL** (see below): the harvester edited this file IN-PLACE during the dispatch phase — a write-authority phase-boundary VIOLATION; the repairer REVERTED it (`git checkout`), and the corrections were applied via the proper CYCLE.md proposed-changes channel. **THIRD distinct specialized agent to leak `book/` during dispatch (recurrence-3).**
  - **Position 3 (lifter — l3-l2-body-identity-cg-sweep)**: `book/src/L3-L2/krylov-step-body-identity.md` — **3 dangling `cg.md` provenance pointers re-anchored** (§Applicability-conditions condition 3, §Justification-kind empirical-match, §Verified-against bullet 1) to the in-document terminal firm home — this theme's OWN §Verified-against bullet 1, which physically carries the verbatim cycle-002 combinator-miner Claim-2 quote — with the dead slice ranges (`cg.md:341-362` / `:351-362`) demoted to parenthetical "original pre-reduction slice range … lifted here per the cycle-009 corpus reduction" annotations (cycle-013/014/015/016 convention). Stays `firm` (dangling provenance never gated firm status). **NOT a self-referential dangle** (critic's load-bearing finding): the bullet holds genuine terminal content, and both `L3/krylov-step.md:188` + `L4-L3/krylov-step-typed-wrapper-dissolution.md:204` independently designate this §section as the lifted-in home. **TERMINATES the SECOND of the two-hop-to-dangle residuals the cycle-016 L4 sweep flagged — the krylov-step `cg.md` re-anchor chain is now FULLY TERMINATED.** Co-pointers correctly left untouched (`gmres.md:459-471` live 671-line slice; `arnoldi_step.md` live). OQ `l3-l2-body-identity-cg-md-citation-sweep` resolved.
  - **Position 4 (lifter — l3-chebyshev-sibling-refresh)**: `book/src/L3/chebyshev.md` — **5-site sibling vocabulary refresh** of all remaining `forM_`/`foldM` mentions onto the firm `iterate_while_pure`/`iterate_while_pure_L3` family with step-count predicates (§Context, §Upward wrapper-dissolution bullet, §Non-adjacent-identity caveat, §"L3 vs L4 distinction" L4-bullet + L3-bullet), matching the register of the cycle-016 named-sentence refresh + the firm L4 §"L4 > L3" anchor. Plus one in-scope disambiguation (§Context `predicate-driven`→`convergence-predicate-driven`, critic-confirmed necessary). **Post-apply re-grep returned ZERO residual `forM_`/`foldM`** — the L3 entry is now fully consistent with the firm L4 sibling. Stays `partial-obstruction` (pure vocabulary refresh, no semantics/structure/status change; no new citations/links). **The chebyshev `forM_`/`foldM`→`iterate_while` vocabulary-lag cohort is now FULLY TERMINATED** (L4 body 015 + L4 prose 016 + L3 named-sentence 016 + L3 siblings 017). OQ `l3-chebyshev-sibling-formm-foldm-prose-sweep` resolved. The pre-existing dedicated-thin-L4>L3-chebyshev-identity-theme OQ untouched (out of scope; lowering-verifier candidate).
  - **Position 5 (cross-layer-cross-cutter — closure-nesting-gate; FINAL per-report)**: **read-only cross-layer audit, ZERO `book/` mutation** (no `## Proposed changes` block; `Build-relevant: no`). Refuted the cycle-016 divfree theme's "first L1>L0 closure carrying a constructed-operator gate / does not recur" premise (critic VERIFIED-SOUND): `eigsolve-mutation-rotation` (firm, cycle-011 `8bb16b7`, five cycles before divfree `b54ea1c`) is the prior + richer instance — its closure `E` carries TWO nested gates (`E.linear : Solver[A]` + `E.projector : Maybe DivFreeSolver` = the divfree projector itself, so **eigsolve ⊃ divfree ⊃ ksp transitively**), and its core sub-pattern B (`eigsolve-mutation-rotation.md:213-258`) lowers each of the ten `opInv->Mult` sites through the firm `ksp-solve-mutation-rotation` theme. So `divfree-projector` is the **THIRD** (not first) instance; `ksp_solve` `K.M⁻¹` a latent further site. **≥2-firm-instance bar cleared** → warrants a named concept page. The entire deliverable is an OQ-ledger ANSWER (`divfree-closure-nesting-constructed-gate-carrying-constructed-gate` answered) + a NEW OQ (`nested-constructed-operator-gate-concept-and-divfree-correction`) routing two cycle-018 follow-ups. The closure-nesting audit refuted the cycle-016 "first" claim that this cycle's OQ-ledger ANSWER records — the cycle-016 premise had overlooked the cycle-011 eigsolve precedent.

- **Reports applied** (5 of 5):
  - `reports/2026-05-28T223022Z-combinator-miner-linear-combination-fold/` (status: integrated; follow_up_agent: **cycle-018+ harvester** on `linear-combination-harvester-formalization` [author the L2 chapter] then **abstractor** on the `L2-L1/linear-combination-fold-specialization` lowering theme; **combinator-miner** on `inner-product-fold-sibling-candidate` as a separate future dispatch; the human-raised parent OQ `blas1-variadic-linear-combination-fold-unification` prong-(a) [combinator-miner parametric-family-detection-mode spec extension] is a **batch-4 meta-phase item**)
  - `reports/2026-05-28T223336Z-harvester-divfree-l1-citation-fix/` (status: integrated; follow_up_agent: batch-4 meta-phase **META-SIGNAL** — dispatch-phase write-guard across all 8 specialized specs + clean-tree gate; the sibling open divfree OQs `divfree-mult-doc-irrotational-vs-divfree-stale` + `divfree-closure-nesting-…` are separate)
  - `reports/2026-05-28T223135Z-lifter-l3-l2-body-identity-cg-sweep/` (status: integrated; follow_up_agent: none — the krylov-step cg.md re-anchor chain is fully terminated; the eventual `phase-1-slice-reduction-audit` on `cg.md` for the live-slice §Evidence retentions is the only remaining cg.md-related work)
  - `reports/2026-05-28T223110Z-lifter-l3-chebyshev-sibling-refresh/` (status: integrated; follow_up_agent: none — the chebyshev vocabulary-lag cohort is fully terminated; the dedicated-thin-L4>L3-chebyshev-identity-theme OQ is a separate lowering-verifier candidate)
  - `reports/2026-05-28T220000Z-cross-layer-cross-cutter-closure-nesting-gate/` (status: integrated; follow_up_agent: **cycle-018 layer-intro-author** on the `nested-constructed-operator-gate` concept page + **cycle-018 lifter/harvester** on the divfree-theme "first"-claim correction at `:108-113` + `:457-464`; both routed via NEW OQ `nested-constructed-operator-gate-concept-and-divfree-correction`)
  - `reports/<this>-integrator-finalize-cycle-017/CYCLE.md` — batch report (this finalize).

- **Layer firm-count changes**: **L1 11 firm + 0 partly-constructive + 3 rough-in + 6 rough-in (obstruction)** (unchanged; firm `divfree-projector` got an 11-ref citation-drift maintenance fix); **L2 2 firm + 1 rough-in** (+`linear_combination` rough-in dep-map row; **rough-in cohort 0 → 1**); L3 8 firm + 1 partial-obstruction (unchanged; `L3/chebyshev` 5-site vocabulary refresh); L4 4 firm + 0 rough-in (unchanged); L1>L0 11 theme files (unchanged); L2>L1 1 firm; L3>L2 1 firm (`krylov-step-body-identity` 3 cg.md re-anchors); L4>L3 1 firm + 2 rough-in. **L0 at 21 chapters** (unchanged). **Corpus removals 9/10** (unchanged).

- **Wave-conflict observations** (captured in `scaffolding/integrator-signals.md`):
  - **5-report single-wave dispatch**; all 5 applied at integration; zero rework loops.
  - **Each of the 5 reports touched a DISTINCT primary `book/` file** (`L2/index.md`; `L1/divfree-projector.md`; `L3-L2/krylov-step-body-identity.md`; `L3/chebyshev.md`; report 5 touched NO `book/` file — read-only audit, OQ-ledger appends only); zero same-file write conflicts. Each per-report integrator re-read its anchor from disk byte-for-byte before applying (serial dispatch order 1→5).
  - **All 5 reports appended to `scaffolding/open-questions.md`** (append-only) at distinct block ranges; serial dispatch naturally serialized the appends; no conflict.
  - **No deferrals, no rejections, no rework loops.** Clean-run streak continues — **thirteenth consecutive clean cycle** under the split integrator (cycles 005–017). (The one build-repair was a single-cell de-link, not a content rework.)

- **Safety-net gates**: **retroactive-budget global = 2** (well below the ≥4 block threshold; per-slice max 1 — report-2 divfree-projector 1 + report-3 krylov-step-body-identity 1; reports 1/4/5 contribute 0 — report 1 is a dep-map row append, report 4 is a present-tense vocabulary refresh [not evidence-correction], report 5 is read-only). **1 build-breakage repair** (the `linear_combination` rough-in row's live markdown link to the absent `./linear_combination.md` chapter failed `linkcheck2`; de-linked to the no-dead-link plain-text forward-reference convention; rebuild clean exit 0). Commit atomicity: single commit. Consumed-report frontmatter integrity: 5 `integrated_at` touches.

- **Open questions**: **3 promoted** (`linear-combination-harvester-formalization`, `inner-product-fold-sibling-candidate`, `nested-constructed-operator-gate-concept-and-divfree-correction`). **3 resolved** (`divfree-l1-entry-apply-close-and-reltol-line-drift`, `l3-l2-body-identity-cg-md-citation-sweep`, `l3-chebyshev-sibling-formm-foldm-prose-sweep`). **1 answered** (`divfree-closure-nesting-constructed-gate-carrying-constructed-gate` — premise REFUTED). Formal YAML `status:` flips applied by integrator-finalize per the append-only-ledger discipline (per-report integrators appended RESOLUTION/ANSWER notes). The human-raised parent OQ `blas1-variadic-linear-combination-fold-unification` was NOT flipped (only prong-b half-resolved; human-raised; prong-a is a batch-4 meta-phase item).

- **META-SIGNAL escalation (CRITICAL — routed to `scaffolding/integrator-signals.md` → batch-4 meta-phase after cycle-018)**: the cycle-017 harvester (report 2) edited `book/src/L1/divfree-projector.md` IN-PLACE during the dispatch phase (Phase 2) — a write-authority partition violation (specialized dispatch agents write ONLY to `reports/<id>/`). The repairer REVERTED the leak via `git checkout` (`revert-dispatch-phase-book-mutation` skill Option A; `book/` confirmed clean) and set `overall_status: ready`; the 11 corrections survived in the CYCLE.md proposed-changes block and were applied the correct way. **This is the THIRD distinct specialized agent to leak `book/` during dispatch (recurrence-3): cycle-008 abstractor → cycle-012 layer-intro-author → cycle-017 harvester.** The friction-ledger `specialized-agent-direct-write-to-book-during-dispatch` Watch clause fires at recurrence-3. The cycle-012 prompt-guard was applied ONLY to `.claude/agents/layer-intro-author.md`; the harvester (and the other 6 specialized specs) never got it. **The batch-4 meta-phase (after cycle-018) should: (i) enact the dispatch-phase write-guard prompt across ALL 8 specialized agent specs; (ii) re-weigh a pre-dispatch / integrator-per-report clean-tree gate as a structural backstop.**

- **Skill-uptake telemetry (recorded for batch-4 meta-phase)**: **4 skill-uptake-survey warnings across the cycle-016/017 batch** carry the named-skill-by-slug uptake-weakness signal (reports 1/2/4/5 this cycle substantively exercised `verify-citation-range` per-range read-back / `classify-variant-axis` three-axis enumeration / `propose-rotation` rotation sketch procedures but NONE named a skill by slug; repairer ruled all not-needed — procedure substance present, only the slug back-reference absent). Continues the batch-3 `producer-citation-drift-verify-not-self-invoked` + `skill-uptake-survey-non-invocation-cycle-wide` telemetry. The batch-3 meta-phase ASK item (mechanical codemap-backed citation-range checker tool under `tools/`, pre-integration lint) remains pending human decision.

- **Build**: `cargo make book` — **exit 0 after 1 build-repair** (`Build Done in 89.42 seconds`). The FIRST build failed (`mdbook-linkcheck2` exit 101, `Error: One or more incorrect links`) on a genuine broken link: `File not found: ./linear_combination.md` — the producer's L2 rough-in dep-map row used a live markdown link to the not-yet-authored chapter. integrator-finalize de-linked the cell (build-repair authority — surgical format fix, not content authoring), matching the cycle-015 `fem-bilinearform-file` no-dead-link forward-reference convention. Rebuild clean: zero genuine File-not-found broken-link errors; the 11 divfree-projector citation corrections + 3 krylov-step-body-identity re-anchors + 5 L3/chebyshev vocabulary refreshes all render. The 6 pre-existing katex "Potential incomplete link" warnings (ALL in `design/l4_calculus.md` math-display brackets, lines 104×2/108/122/142×2; NOT touched this cycle) carry unchanged; warning-level under `warning-policy = "warn"`, non-blocking.

- **Legacy log/cycle-017.md renamed**: pre-layered-era `log/cycle-017.md` (2026-05-24 `forward divfree [L0→L1] — revise`) renamed to `log/cycle-017-legacy.md` per the cycle-005..016 precedent; the README legacy index entry re-pointed.

- **Two-phase SHA patch** (canonical pattern per role spec process step 13): all 5 reports' + this batch CYCLE.md's `integration_commit: PLACEHOLDER_SHA` are patched in a follow-up commit immediately after the finalize commit lands. Message: `patch commit-sha references for cycle-017 finalize commit (<finalize-sha>)`. Same two-phase pattern cycles 004..016 used.

- **Meta-phase does NOT fire this cycle** — cycle-017 is the SECOND primary cycle of meta-batch-4; the batch-4 meta-phase fires after the cycle-018 finalize commit (3:1 cadence), aggregating cycles 016/017/018. Key batch-4 meta-phase targets surfaced this cycle: (1) the `specialized-agent-direct-write-to-book-during-dispatch` recurrence-3 Watch-clause escalation; (2) the named-skill-by-slug skill-uptake weakness; (3) the human-raised `blas1-variadic-linear-combination-fold-unification` prong-(a) combinator-miner parametric-family-detection-mode spec extension.
