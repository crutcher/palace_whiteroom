# 2026-06-01 cycle-044 — integration summary

**FIRST substantive (non-identity) L3>L2 rotation: `orthogonalize-variant-split` (variant-conditional MGS-erasure) + the c043 leaf-cohort L3 re-anchor + the orthogonalize L3 audit + the chebyshev-smoother (B)-candidate closure + the directive-slug/index-citation drift sweep.**

> NOTE on numbering: this is the **layered-era cycle-044** (post-2026-05-26 structural redirect). A legacy **slice-vertical-era cycle-44** entry (2026-05-25, "forward chebyshev [L1→L2]") is preserved at the bottom of this file for historical continuity; the two are distinct cycles that collide only on the zero-padded filename.

**Kind:** integration (primary cycle — phases plan → dispatch → critique → repair → integrate)
**Meta-batch:** batch-13, position 2 of 3 (cycles 043/044/045). **The batch-13 meta-phase fires AFTER cycle-045's finalize commit, as a SEPARATE dispatch — NOT run in this cycle.** The cycle counter does NOT reset across batch boundaries.
**Written by:** `integrator-finalize` (split integrator-per-report ×4 + finalize ×1).

## Headline

The second primary cycle of meta-batch-13 landed the **FIRST substantive (non-identity) L3>L2 rotation** and finished the leaf-cohort's L3-side consistency:

- **L3>L2 firm 14 → 15** — `book/src/L3-L2/orthogonalize-variant-split.md` (NEW firm SUBSTANTIVE theme; `l3-l2-rotation-theme-coverage-gap` advanced **14-of-18 → 15-of-18**, the FIRST substantive entry in the gap and the FIRST substantive L3>L2 theme for a `partial-obstruction` operator). It is the SECOND substantive L3>L2 theme overall, after `ksp-solve-outer-driver`.
- The theme is a **variant-conditional MGS-erasure**: gated on the `gs_orthog` axis, the MGS (modified Gram-Schmidt) `jloop` is a witnessed `sequential-obstruction` (each projection reads the running-updated vector) that does NOT lift, while the CGS / CGS2 (classical / re-orthogonalized) batched arms collapse cleanly into the L2 per-variant sequencing.
- The c043 leaf-cohort's **4 stale L3 entries re-anchored** (`axpy`/`axpby`/`axpbypcz`/`normalize`) L3>L1 → L3>L2>L1, closing OQs `l3-{axpy,axpby,axpbypcz,normalize}-lowers-to-staleness-after-l2-floor`.
- The `orthogonalize` L3 entry **audited** (24-row `verified_against:` block, all `supports`) — **status stays `partial-obstruction`, NOT flipped**.
- The `chebyshev-smoother` L3 (B)-candidate **CLOSED as SUBSUMED** by the firm L3 `chebyshev` (NO-LAND), leaving `apply_nonlinear_pencil` as the only remaining (B)-candidate.
- The **directive-slug + index-citation drift swept** — zero `l2-floor-under-l3-blas1-cohort` occurrences remain in `book/`.

## The substantive theme (D3, abstractor)

`book/src/L3-L2/orthogonalize-variant-split.md` — the variant-conditional MGS loop-erasure. The per-step `dot`+`axpy` body is identity-in-form (annotated in-line per the cycle-012 non-adjacent convention); the SUBSTANTIVE half is the **loop-structure rotation** — the MGS sequential `jloop` vs the CGS/CGS2 batched arms — exhaustively cited per-arm with the `gs_orthog ∈ {MGS, CGS, CGS2}` variant axis covered exhaustively. D3 was the SOLE count-owner for `L3-L2/index.md` this cycle and applied the full dual-registration: TABLE row + a NEW §"Vocabulary-cohort" "Substantive / non-identity" sub-grouping (`ksp-solve-outer-driver` + `orthogonalize-variant-split`) + the consolidated TALLY (firm 14→15, coverage-gap 14-of-18→15-of-18) + the first-substantive-partial-obstruction taxonomy bullet. The D3 erasure surfaced an erasure-scope/root taxonomy axis (unconditional / variant-conditional / opaque-library) — OQ `substantive-l3-l2-erasure-scope-taxonomy`, a candidate concepts page or index taxonomy note.

## The orthogonalize L3 audit (D4, lowering-verifier)

`book/src/L3/orthogonalize.md` — a deferred-from-c040 `verified_against:` backfill: 24-row YAML block at EOF, all `supports`, status `partial-obstruction` UNCHANGED (no contradiction found, no flip). D4 also re-pointed the L3 entry's §Dependencies / §"L3 vs L2 distinction" / `lowers_to:` frontmatter from the stale "no `L3-L2/` theme file (in-line per cycle-012)" note onto the live `../L3-L2/orthogonalize-variant-split.md` link for the SUBSTANTIVE loop-rotation half (the per-step body-identity stays the in-line non-adjacent note). The dangling-link risk was AVOIDED by application order — D3 landed the target first, so D4's links resolved on apply (the critic-flagged sequencing warning resolved exactly as predicted; no demote-to-plain-text fallback needed).

## The D1 consolidated lifter sweep

D1 enacted the c043-carry-forward in one pass:
- Re-anchored the 4 c043 leaf-cohort firm L3 entries (`axpy`/`axpby`/`axpbypcz`/`normalize`) L3>L1 → L3>L2>L1 — closing the 4 staleness OQs.
- Re-pinned co-located self-referential audit-block citations across the L3 cohort.
- Swept the directive-slug rename `l2-floor-under-l3-blas1-cohort` → `l2-floor-under-l3-leaf-cohort` book-wide — **ZERO old-slug occurrences remain in `book/`** (was 25 across 12 files).

## The chebyshev-smoother subsumption closure (D2, cross-cutter — OBSERVATION, no book mutation)

D2 is a read-only observation report (no `book/` mutation). It DISCHARGES the c036 D2 caveat at `book/src/L3/index.md:48` ("requires a subsumption check first"): **`chebyshev-smoother` L3 is SUBSUMED by the firm L3 `chebyshev` (c013, partial-obstruction) — NO-LAND.** A clean negative result that REMOVES work from the batch-13 substantive-L3 frontier. The only artifact-adjacent actions were scaffolding-only (the candidate-closure in `priorities.md` + the closure OQ `chebyshev-smoother-l3-candidate-subsumed-closed`, which records the benign L1/L2/L3/L4 slug-name asymmetry so a future audit does not re-flag it). `apply_nonlinear_pencil` is now the only remaining (B)-candidate (routed to fold into a future eigsolve-variant pass, not a standalone row).

## Process

- **4 of 4 dispatched-ready reports applied clean.** 4/4 staging rows == dispatched-ready — the cycle-018 staging-completeness gap did NOT recur for the **TWENTY-FIFTH consecutive** cycle. Zero deferrals, zero rejections, zero build-repairs.
- **Thirty-ninth consecutive cycle under the split integrator.**
- **retroactive-budget global = 1** (D4's deferred-from-c040 audit) — well under the ≥4 block.
- **`cycle-planner-stale-priorities-line-recruitment` did NOT recur** — clean opus-planner cycle.
- **Application order resolved the cross-report dependency cleanly** — D1 (lifter sweep) → D3 (abstractor theme, the link target) → D4 (lowering-verifier, the consumer of D3's target) → D2 (observation). D4's same-file overlap with D1's prior touches resolved by TEXT-MATCH (disjoint regions); D4's forward-link to D3's file resolved because D3 landed first.
- **Build clean** — `cargo make book` exit 0; the only warnings are the pre-existing KaTeX "Potential incomplete link" false-positives + the long-standing unclosed-`<opertype>`/`<vectype>` HTML-tag warnings (both pre-existing, non-fatal). linkcheck2 green; `orthogonalize-variant-split.md` SUMMARY-wired with all relative link targets resolving; `L3/orthogonalize.md`'s 2 live links + `lowers_to:` frontmatter ref to it resolve. Zero build-repairs.

## Carry-forward to cycle-045 (the FINAL primary cycle of batch-13; meta-phase fires after)

1. **Remaining substantive L3>L2 rotations are the lead frontier.** `chebyshev` + `eigsolve` complete `l3-l2-rotation-theme-coverage-gap` (15-of-18 → toward 18). Plus the `orthogonalize` L3>L2 erasure-scope/root taxonomy axis (unconditional / variant-conditional / opaque-library) surfaced by D3 — candidate for a concepts page or index taxonomy note (OQ `substantive-l3-l2-erasure-scope-taxonomy`).
2. **The L4→L3 / L2→L1 coverage frontier** is the next foundation tier once L3>L2 saturates — the uniform climb.

## Standing batch-13 meta-phase items (after cycle-045)

- **(a) dual-registration convention codification** — producers add BOTH the table row + their own cohort bullet; the count-owner adds only the consolidated tally (c043 friction; applied inline this cycle by D3 as sole count-owner).
- **(b) chebyshev cohort-count reconciliation** — 12-of-13, the 13th floored via the non-same-named `chebyshev-iteration` entry (OQ `chebyshev-floor-cohort-count-reconciliation`) + the `normalize` fused-composite-no-fold-parent sub-shape classification (OQ `normalize-fused-composite-no-fold-parent-sub-shape`).
- **(c) `l2-floor-directive-slug-rename-scaffolding-residual-sweep`** — `priorities.md` / `roadmap.md` old-slug occurrences (meta-phase owns the plan; `book/` is already clean).
- **(d) `l3-leaf-cohort-l2-floor-reanchor-deferred-from-c043` process signal** — L2-floor landings should imply a same-cycle L3 re-anchor (a cycle-planner dispatch-design note candidate, so the re-anchor is not deferred a cycle).

## Counts after cycle-044

L1 firm 26 · L2 firm 21 · L2>L1 firm 19 · L3 firm 15 + 3 partial-obstruction · **L3>L2 firm 15** · L4 firm 4 · L0 chapters 22 · Phase-1 removals 9/10.

---

## (legacy / historical) 2026-05-25 cycle-44 — forward chebyshev [L1→L2] — pass

- Synthesis: Chebyshev L1→L2 rotation_claims retroactive on the already-present `## L2 — primitive composition` section of `book/src/spec/slices/chebyshev.md`. The L2 section unfolds the L1 Richardson-like sweep into a sequence of named base primitives (`copy`, `zero`, `apply_linop(A,·)`, `axpy`, `elementwise_product`, `scal`) while preserving constructed-operator variant absorption: 4th-kind and 1st-kind share the same primitive sequence, differing only in the scalar generator `scalars(op, k)`. Per-edge rotation_claims cover (1) the residual unfold `r := x - A*y` → `copy`+`apply_linop`+`axpy`, (2) the initial-direction unfold `d := alpha_0 * dinv .* r` → `elementwise_product`+`scal`, (3) the inner-recurrence direction update `d := sd_k * d + sr_k * dinv .* r` → `elementwise_product`+`scal`+`axpy`, (4) the scalar-coefficient resolution from variant tag to closed-form (4th-kind) / three-term recurrence (1st-kind) `(alpha_0, sd_k, sr_k)`, and (5) the variant-absorption preservation at the primitive-sequence axis.

retroactive_claim_evidence:
  - claim_index: 0
    on_disk_path: book/src/spec/slices/chebyshev.md
    section: ## L2 — primitive composition / Apply primitives
    quoted_lines: |
      # 1. residual r = x - A y  (or r = x if !initial_guess on first sweep)
      if it == 1 and not initial_guess:
        r ← copy(x)
        zero(y)
      else:
        r ← copy(x)
        Ay ← apply_linop(op.A, y)
        axpy(-1, Ay, r)                         # r ← r - A y
  - claim_index: 1
    on_disk_path: book/src/spec/slices/chebyshev.md
    section: ## L2 — primitive composition / Apply primitives
    quoted_lines: |
      # 2. initial direction:  d = alpha_0 * dinv .* r
      a0 = scalars(op, 0).alpha_0
      d  ← elementwise_product(dinv, r)
      scal(a0, d)
  - claim_index: 2
    on_disk_path: book/src/spec/slices/chebyshev.md
    section: ## L2 — primitive composition / Apply primitives
    quoted_lines: |
      (sd, sr) = scalars(op, k)
      # d ← sd * d + sr * (dinv .* r)
      t ← elementwise_product(dinv, r)
      scal(sd, d)
      axpy(sr, t, d)
  - claim_index: 3
    on_disk_path: book/src/spec/slices/chebyshev.md
    section: ## L2 — primitive composition / Apply primitives
    quoted_lines: |
      scalars(op, k):
        if op.variant == 4th-kind:
          alpha_0 = 4/3 / op.lam_max
          sd_k    = (2k - 1) / (2k + 3)
          sr_k    = (8k + 4) / ((2k + 3) * op.lam_max)
        else:  # 1st-kind, three-term Chebyshev recurrence centered at theta
          alpha_0 = 1 / op.theta
          rho_k tracked across k: rho_0 = delta / (2*theta), then
            rho_k = 1 / (2*theta/delta - rho_{k-1}) for k >= 1
          sd_k    = rho_k * rho_{k-1}
          sr_k    = 2 * rho_k / op.delta
  - claim_index: 4
    on_disk_path: book/src/spec/slices/chebyshev.md
    section: ## L2 — primitive composition / Variant absorption at L2
    quoted_lines: |
      The primitive *sequence* in `apply_linop` is identical across variants. Only the scalar-generator `scalars(op, k)` branches on variant. This is the (c) primitive-sequence axis of variant absorption per `concepts/variant-absorption.md`, achieved here because both polynomial families admit a uniform `(alpha_0, sd_k, sr_k)` recurrence parameterization — 4th-kind via closed-form, 1st-kind via a `rho_k` scalar carried across `k`.
- Verdict: pass.
- Friction: none.
- Structural change: applied: 1 dep-map edge(s); 5 rotation_claim(s).
