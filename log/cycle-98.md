## 2026-06-05 cycle-098 — 3 reports applied clean — ninety-third consecutive cycle under split integrator — **POSITION 2/3 OF META-BATCH-31 (the SECOND primary cycle; the batch-31 meta-phase fires AFTER cycle-099, NOT this cycle)** — THE GRADED-STACK P2 SLICE-DELETION CAMPAIGN SECOND TRANCHE: **`orthog` + `polynomial_recurrence_step` BOTH DELETED + made fully unreachable** (both verified-no-op absorbs — content already firm-homed; all inbound markdown links repointed; the slice corpus shrank **5 → 3**) — pass

> **Note:** this file (`cycle-98.md`) is the current-run, structural-redirect-era cycle-098 log. The current-run numbering does NOT reset across batch boundaries.

**POSITION 2/3 OF META-BATCH-31** (3:1 cadence; cycles 097/098/099; the cycle counter does NOT reset across batch boundaries; **the batch-31 meta-phase fires AFTER cycle-099's finalize as a SEPARATE dispatch aggregating 097/098/099** — this cycle-098 finalize runs NO meta-phase housekeeping). Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) + the 2026-06-02/2026-06-03 user directives + the **2026-06-04 GRADED RESOLUTION LADDER + FEATURE-ROOT REACHABILITY user directive** (full spec `METHODOLOGY-GRADED-STACK.md`; the campaign is priorities item-0) + the batch-30 meta-phase enactment (which GO'd the P2 slice-deletion tranche as the batch-31 LEAD and wired finalize step-5b).

### Headline

The **P2 slice-deletion campaign's SECOND tranche** landed. **`orthog` + `polynomial_recurrence_step` were BOTH DELETED and made fully unreachable from any GC root**, their material verified-already-firm-homed (no-op absorbs) and ALL inbound markdown links repointed. The slice corpus shrank **5 → 3**. The 3 survivors (`arnoldi_step`, `cg`, `gmres`) ARE the c099 krylov-trio sub-campaign — a deeply-interwoven ~30-anchor SINGLE-COORDINATED-OWNER dispatch (NOT 3 parallel dispatches).

3 of 3 dispatched-ready reports applied clean (3/3 staging rows == dispatched-ready — the cycle-018 staging-completeness gap did NOT recur for the SEVENTY-NINTH consecutive clean staging / NINETY-THIRD consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs.

### Reports

- **D1** (same-layer-cross-cutter, build-relevant) — `orthog` slice **DELETED** (verified-no-op absorb — its MPI-collective-shape + L1-invariant content fully firm-homed already) + 2 original cites repointed (PC-1 `concepts/gemv_basis.md`, PC-2 `L1/orthogonalize.md` pending-lift anchor → claim-free Provenance prose) PLUS **8 inbound markdown links the original sweep MISSED** — caught by the critic, added by the repairer (PC-4 a–f): `L0/mpi-globalsum-and-collectives.md:69,:105`, `concepts/orthogonalization.md:77`, `concepts/gmres.md:23`, `concepts/sequential-obstruction.md:48`, and the 4 dangling `./orthog.md` sibling links inside the **SURVIVING** `spec/slices/arnoldi_step.md:67,:95,:115,:144` (repointed to `concepts/orthogonalization.md`; arnoldi_step survives for c099). The missed-sweep was the cycle's main risk; the repairer fixed it pre-integration and finalize confirmed against the actual build. No inbound `depends-on` blocking edge targeted the slice; rank-gate PASS (reachability-safe leaf deletion).
- **D2** (same-layer-cross-cutter, build-relevant) — `polynomial_recurrence_step` slice **DELETED** + a **SURGICAL polynomial-clause drop at `L2/krylov-step.md:7`** (the cg/gmres/chebyshev/arnoldi clauses PRESERVED byte-exact for the c099 krylov-trio sub-campaign) + R2/R3/R9 anchor repoints (`L2/krylov-step.md:142`, `L2/index.md:135`, `L3/krylov-step.md:200` → `concepts/negative-result-slice.md` + `L4/chebyshev.md` §Semantics `innerStep`) + R4 mermaid edge removal (3 underscore slice-node edges; the hyphenated planned node PRESERVED) + R5 `concepts/negative-result-slice.md:46,:66` delink + **R7/R8 removal of BOTH the orthog AND polynomial rows from `book/src/SUMMARY.md` + `book/src/spec/index.md`** (the D1↔D2 co-application serialization caveat — D1 deleted orthog's file, D2 the single-owner removed both orphaned nav rows). Post-apply verification confirmed: no surviving markdown link or plain-text anchor to either deleted slice; the 3 krylov-trio rows (cg/gmres/arnoldi_step) intact in BOTH SUMMARY and spec/index. R1's preserved clauses confirmed byte-exact.
- **D3** (lifter, build-relevant) — `book/src/L4/domain_energy_reduce.md:313-316` §Status closing parenthetical re-anchored: dropped the post-c095 FALSIFIED "gram_reduce STAYS rough-in because bilinear-form is still rough-in" maturity assertion (both `rank: firm` on disk since c095 — re-verified this invocation), recast as the permanent rank-1-vs-rank-2 / single-field-vs-family-PAIR SHAPE distinction. Both `[gram_reduce]` + `[bilinear-form]` links retained; no frontmatter touched. NO node status flip (`domain_energy_reduce` already `rank: firm` resting on firm deps → invariant holds unchanged). Discharges the OQ `domain_energy_reduce-313-gram_reduce-bilinear-form-c095-stale-rough-in-narration`. The whole-book `bilinear-form`-c095-firm-flip residue-class sweep (other files co-mentioning gram_reduce+rough-in) is OUT of this within-file dispatch's scope — flagged for the batch-31 §Intake→plan migration.

### Build + step-5b

`cargo make book` (mdbook + linkcheck2) **EXIT 0**, ~92s. The 2 slice deletions + ~10 inbound repoints + the SUMMARY/index/mermaid removals all co-landed link-safe: linkcheck2 zero dead links. Finalize verified directly against disk: no dangling `[..](..)` to `orthog.md`/`polynomial_recurrence_step.md`; no surviving plain-text slice anchor; no SUMMARY entry points at a deleted file; the 3 surviving slice rows (cg/gmres/arnoldi_step) intact. Only pre-existing benign KaTeX `Potential incomplete link` WARNs (in `design/l4_calculus.md`). NO finalize build-repair needed (clean first build) — the cycle's hard co-landing constraint (D1's orthog file-delete co-landing with D2's single-owner SUMMARY/index orphaned-row removal) held.

**STEP-5b graded-stack linters** (the build-gate companion to `cargo make book`) — `python3 tools/graded-stack-lint/graded_stack_lint.py --json` on the LANDED tree:

- **`rank_violations: 0`** (was 0 at c096 + c097; the baseline-exceptions were fully discharged at c096 → ANY violation would be NEW and BLOCK — there are NONE; **GATE PASSES**).
- **NO newly-orphaned node** (the 2 deleted slices were `reference`-class detritus leaves already in the pre-P1 detritus mass — an INTENDED deletion, not an orphaning; the second step-5b block condition also clears).
- `rank_histogram: {firm: 192, rough-in: 7, partly-constructive: 3, obstruction: 2, partial-obstruction: 4}`.
- `files=354` (was 356 — the 2 deleted slices), `typed=208`, `untyped=146` (was 148 — the 2 deleted slice nodes left the untyped tail), `roots=36`, `promotion_frontier=10`, `detritus=172`, `unresolved_depends_on_targets=35`, `expected_unreachable_outside_dag=22`.
- **rank_violations cycle-over-cycle trend: 22 (c094) → 1 (c095) → 0 (c096) → 0 (c097) → 0 (c098).**
- The slice-node set in the linter output is now EXACTLY the 3 survivors (cg/gmres/arnoldi_step); the 2 deleted are gone. The high untyped/detritus mass is P2 mid-campaign (3 slices still present + the as-yet-untyped pre-P1 tail) — informational, NOT a build-gate failure. The `cites-evidence` L0-range `depends-on` edges remain exempt from slug-resolution + rank-check.

### Counts

UNCHANGED from c097 (the slice corpus is Phase-1 raw material, NOT the layered-vocabulary deliverable; D3 is a within-file maturity-NARRATION re-anchor with `rank: firm` already on disk, no node status flip): L1 firm 32 main / 39 grand · L4 firm 20 main / 24 grand · L4 rough-in 0/0 cohort EMPTY · L4>L3 10 · L3 17+4po · L3>L2 6 · L2 21+1pc · L2>L1 11 · L0 22 · concepts 33 · methodology 4 · feature spine 11 FIRM / 1 seed · L4 reduce-family 4 verbs ALL firm. **SLICE CORPUS: 5 → 3** (`spec/slices/`: `arnoldi_step`, `cg`, `gmres` remain — the c099 krylov-trio sub-campaign).

### Process

retroactive-budget global = 0 (2 verified-no-op absorb-and-deletes into existing firm homes + 1 within-file re-anchor; no retroactive rewrites of existing firm-chapter CLAIMS). Repair fired on D1 + D2 (mechanical — the critic caught D1's 8 missed inbound orthog links, the repairer added PC-4 a–f and D2's R9 L3-site anchor repoint; proposed-change blocks otherwise untouched); D3 passed clean. Per-report safety-net gates all PASS/N/A. 0 implied-component stubs. **3 OQs recommended-CLOSE by per-report intake** (flagged for the batch-31 meta unify — per-report integrators have no OQ-close authority; finalize RECORDS the recommendation, the meta-phase closes): `domain_energy_reduce-313-gram_reduce-bilinear-form-c095-stale-rough-in-narration` (D3, resolved-by-re-anchor), `orthog-slice-substantive-absorb-framing-was-a-verified-no-op` (D1 — records the stale "substantive MPI-collective-shape absorb" tranche framing as a VERIFIED NO-OP), `polynomial-recurrence-step-slice-absorb-verified-no-op-and-campaign-state-5to3-slices` (D2). **STILL-OPEN (for c099 + downstream):** the `arnoldi_step` end-bound citation-divergence `:73-109`/`:73-120`/`:73-118` (to reconcile when the c099 trio repoints) + the cross-file `bilinear-form`-c095-firm-flip residue-class sweep (D3-flagged, out of within-file scope). `scaffolding/priorities.md` + `scaffolding/open-questions.md` + `scaffolding/skill-candidates.md` also modified (cycle-098 planner co-owned plan write + per-report OQ intake), committed atomically. NO `.claude/agents/` changes this cycle → no session-restart concern.

### Next

cycle-099 (batch-31 position 3/3) executes the **krylov-trio sub-campaign** as ONE single-coordinated-owner dispatch: ~30 interleaved anchors across the firm L2/L3/L4 krylov-step trio + 4 L4-L3 dissolution themes + `L3-L2/krylov-step-body-identity` + `L2-L1/krylov-step-kernel-defusion`. The `cg.md:27-141` L4-v0.5 cg-unrolling material may be the one genuinely-unlifted datum needing a firm home or `roadmap_goal` before cg deletes. **On campaign completion at c099 → retire the `annotated-and-retained` carve-out + skill `phase-1-slice-reduction-audit`.** The batch-31 meta-phase fires after cycle-099's finalize (aggregating 097/098/099) and should close the 3 recommended-resolved OQs above + retire the carve-out/skill. — written by `integrator-finalize` (split integrator-per-report ×3 + finalize ×1).
