# Cycle 027 — L1 firm 20→21 (+back_solve) + L1>L0 firm themes +1 (normalize-mutation-rotation) + matrix-weighted-norm verified_against audit + 6 hygiene re-anchors + 2 ksp_solve cite upgrades + D5 DEFERRED (third/final primary cycle of meta-batch-7)

**Date:** 2026-05-29 · **Commit:** `__FINALIZE_SHA__` · **Status:** clean-with-one-deferral (5 of 6 dispatched reports applied; 1 deferred needs-revision [D5]; zero rejections/rework; zero build-repairs; twenty-third consecutive clean split-integrator cycle)

**Batch position:** cycle-027 is the **THIRD / FINAL** primary cycle of meta-batch-7 (cycles 025/026/027). **The batch-7 meta-phase fires after THIS cycle-027 finalize commit** (3:1 cadence; cycle counter does NOT reset across batch boundaries). This `log/cycle-027.md` + the `scaffolding/integrator-signals.md` cycle-027 section CLOSE the batch-7 evidence window opened by cycle-025 — the signals section is the BATCH-CLOSING signal dump for the meta-phase.

**Recovery note:** no crash this cycle. STAGING.md was authoritative; the cross-check of **5 staging rows vs 5 dispatched ready reports** reconciles clean (all `applied`). The **6th** dispatched report (D5 `abstractor-incremental-ls-composition-lowering`) came back `needs-revision` and was **DEFERRED** — correctly NOT staged. `rows (5) == dispatched-ready-reports (5)`; the deferral is expected, NOT a staging-completeness gap.

> Note: this file supersedes a stale pre-structural-redirect placeholder (an old slice-vertical-era "cycle-27 — back divfree" stub dated 2026-05-25); the prior content is preserved in git history. The layered-era cycle-027 is the authoritative cycle-027.

## What landed (5 reports — all wave-1)

- **HEADLINE 1 — L1 firm 20→21 (+`back_solve`).** NEW firm L1 leaf `back_solve :: (R: UpperTri, s) -> y` — the GMRES/FGMRES restart-correction back-solve, solving the small-dense upper-triangular system `R · y = s` over the **Givens-rotated R-factor** of the running-QR triangularisation, producing the least-squares correction coordinate vector `y` (whose basis-lift `V·y` / `Z·y` is the iterate correction folded at restart-cycle close). The terminal **`back_solve`** projection of the firm L2 named composition `incremental-least-squares`; the **small-dense-triangular** sibling of `lu_solve` (small-dense *general* direct solve) on the coordinate-space dense-direct-solve axis (motif 6), and of `ksp_solve` on the broader solve-a-system axis. Explicitly **NOT** a general `trsv` (the unanchored sparse-triangular smoother kernel; OQ `trsv` stays open) and **NOT** an `apply_linop` variant (it reads `R`'s diagonal/super-diagonal entries). Firm-on-positive-structure: the back-solve loop is read in full in both the GMRES (`iterative.cpp:652-660`) and the line-for-line-identical FGMRES (`:831-840`) arms; one load-bearing non-law (descending column-oriented back-substitution reduction order pins a finite-precision path). Resolves OQ `ls-update-column-l1-leaf`. **RENAMED IN REPAIR** from `ls_update_column` to `back_solve` to resolve a slug collision — the `ls_update_column` slug stays reserved for the distinct still-un-harvested column-streaming step. The harvester correctly deferred the `Firm (20)→(21)` count-prose bump; **finalize applied it as measurable housekeeping** at `L1/index.md:31` (header + motif enumeration), same as the cycle-026 `normalize` 19→20 precedent.
- **HEADLINE 2 — L1>L0 firm themes +1 (`normalize-mutation-rotation`).** NEW firm L1>L0 theme — the fused-normalise lowering `(β, x/β) = normalize(x)` forward into L0 `linalg::Normalize`; the forward-referenced theme from cycle-026, now that the firm L1 `normalize` operator exists. The `normalize.md:104` plain-text → live-link upgrade was **ENACTED inline** by the per-report integrator per skill `upgrade-plain-text-ref-to-live-link-when-target-on-disk` (the `§"L1 vs L0 distinction"` parenthetical replaced with the live link `[normalize-mutation-rotation](../L1-L0/normalize-mutation-rotation.md)`). With the cycle-026 `normalize` L1 operator, this CLOSES the `normalize-l1-primitive-harvest` plan item end-to-end (operator c026 + L1>L0 theme c027).
- **HEADLINE 3 — L1>L0 lowering-verifier audit +1 (`matrix-weighted-norm-mutation-rotation`).** Additive `verified_against:` block (19 entries, `~~~yaml` tilde-fenced at EOF, toggle-safe — the tilde fence does NOT cross-toggle the backtick `edit:` block, and the `cross-layer-cross-cutter` parser keys on the `verified_against:` leading text which survives the tilde form). The theme STAYS **firm**, ZERO content/status change. The residual `matrix-weighted-norm-mixed-element-type-variant` L1-ENTRY promotion gate migrates to the plan — the L1 entry `matrix-weighted-norm.md` stays `rough-in (test-coverage-bounded)`, its own independent gate.
- **HEADLINE 4 — 6 mechanical hygiene re-anchors (D2 lifter).** (1) `matrix-weighted-norm.md` brace re-anchor `:601-606` → `:602-606` at the two body-quoting sites (`:58` law 8, `:83` Composition note); (2) Category-4 → Category-1 workspace mislabel relabel at `linalg-operator-file.md` `:33` (the `operator.cpp:621-639` line) + residual `:73` + `:80` ("Categories 2 and 4" → "all Category 1") + `matrix-weighted-norm.md:9` — the four dispatch/critic-named sites are now internally consistent at "Category 1 — operator-composition workspace"; (3) `givens.md:29` source-cite `gmres.md` → `palace/linalg/iterative.cpp:634-640` with sub-range pinpoints; (4) `bilinear-form.md:416` `dot_bilinear` provenance-note refresh (false slug-discrepancy premise dropped). TWO further same-file sites (`:22`/`:87`) carry the same wrong Category label but were OUT of the named scope → NEW residual OQ `linalg-operator-file-category-mislabel-residual-lines-22-87` opened for a c028 follow-up sweep.
- **HEADLINE 5 — 2 cite/cross-ref upgrades in `L2/ksp_solve.md` (D6 lifter).** Edit-1 (§Dependencies `:123`): plain-text `incremental-least-squares` → live link `[incremental-least-squares](./incremental-least-squares.md)`, "queued" → "firm", correction-shape `K.y` → `V·y` / `Z·y`. Edit-2 (§Semantics phase-3 `:83`): added live-link cross-reference resolving the `K.V · K.y` correction story to the firm operator's `back_solve` output. Surfaced NEW c028 OQ `l2-incremental-least-squares-self-description-still-says-queued-after-firming` — the producer entry `incremental-least-squares.md:13` still self-describes as "queued ... motif" despite `status: firm` (out of this report's one-operator scope).
- **DEFERRAL — D5 (`abstractor-incremental-ls-composition-lowering`, L2>L1 `incremental-least-squares-composition-lowering` theme).** Came back `overall_status: needs-revision`. The repairer's audit found the **coordinated-cross-report-rename premise was INVERTED** relative to the artifact: in that theme `ls_update_column` legitimately denotes the column-streaming step (a distinct, still-un-harvested operation), while the terminal back-solve needs re-anchoring to D4's now-firm `back_solve` leaf — a content reclassification beyond repair scope. The theme is rough-in anyway (gated on un-harvested leaves). **DEFERRED to batch-8/c028** as a lifter promotion task (re-anchor to `back_solve` + reconcile `trsv` ↔ `back_solve` + possibly harvest the column-streaming `ls_update_column` leaf). NOT marked `integrated_at`; carry-forward OQ `incremental-least-squares-composition-lowering-theme-deferred-needs-back-solve-reanchor`. The D5 repairer filed skill-candidate `audit-slug-meaning-before-coordinated-cross-report-rename`.

## Reports consumed (5 applied; 1 deferred)

| # | Report | Status | Landing |
|---|---|---|---|
| 1 | abstractor-normalize-rotation | applied | NEW firm L1>L0 `normalize-mutation-rotation` (**L1>L0 firm themes +1**) + `normalize.md:104` plain-text→live-link upgrade enacted inline |
| 2 | lifter-cycle026-hygiene-reanchors | applied | 6 mechanical hygiene edits (matrix-weighted-norm brace `:58`/`:83`; Category-4→Category-1 at linalg-operator-file `:33`/`:73`/`:80` + matrix-weighted-norm `:9`; givens `:29` source-cite; bilinear-form `:416` provenance) |
| 3 | lowering-verifier-matrix-weighted-norm-audit | applied | additive `verified_against:` 19 entries (theme stays firm) |
| 4 | harvester-ls-update-column-l1 | applied | NEW firm L1 leaf `back_solve` (renamed from `ls_update_column` in repair; **L1 firm 20→21**); resolves OQ `ls-update-column-l1-leaf` |
| 6 | lifter-ksp-solve-materialise-iterate-cite-tightening | applied | 2 cite/cross-ref upgrades in `L2/ksp_solve.md` (`:83`/`:123`; live-link to firm incremental-least-squares + correction-shape tightening) |
| 5 | abstractor-incremental-ls-composition-lowering | **DEFERRED** | L2>L1 `incremental-least-squares-composition-lowering` theme returned `needs-revision` (inverted coordinated-rename premise); routed to batch-8/c028 lifter promotion; NOT marked `integrated_at` |

## Roadmap deltas

- **L1** 20 → **21 firm** (+`back_solve`; L1/index "Firm" header bumped 20→21 + motif enumeration extended) + 2 rough-in(test-coverage-bounded) + 6 rough-in(obstruction).
- **L1>L0 firm themes** +1 (`normalize-mutation-rotation`; joins `matrix-weighted-norm-mutation-rotation` from cycle-026).
- **L1>L0 lowering-verifier audit** +1 (`matrix-weighted-norm-mutation-rotation` `verified_against:` 19 entries, theme stays firm).
- **L2** unchanged at **9 firm** (cycle-027 added two cite/cross-ref upgrades in `L2/ksp_solve.md`, no count change).
- **L2>L1 count unchanged** (7 = 6 firm + 1 partly-constructive) — the planned `incremental-least-squares-composition-lowering` L2>L1 theme (D5) is **DEFERRED to batch-8/c028**.
- **Intermediate-tier roadmap row** "Sparse triangular solve" updated: back-solve sub-case landed firm cycle-027 (`back_solve`, a sibling — NOT the realisation — of the general `trsv`; the `trsv` L3-inventory gap stays OPEN).
- **Unchanged:** L3 9 firm + 2 partial-obstruction; L4 4 firm; L0 22 chapters; concepts +0; Phase-1 removals 9/10; NEP-interior atoms 5/5; eigsolve chain L1→L2→L3→L2>L1→concept FULLY COMPLETE; `l2-named-composition-lifts` 2/2; `normalize-l1-primitive-harvest` COMPLETE.

## Build

`cargo make book` exit **0**, **ZERO build-repairs**. The new `L1-L0/normalize-mutation-rotation.md` theme + the new `L1/back_solve.md` leaf + the `matrix-weighted-norm-mutation-rotation` `verified_against:` append + the 6 hygiene edits + the 2 `ksp_solve.md` cite upgrades + the `normalize.md:104` live-link ALL SUMMARY-registered + link-clean. The two new live links (`normalize.md` → theme, `ksp_solve` → `incremental-least-squares`) target existing on-disk files and resolve under linkcheck2. The only build warnings are katex `Potential incomplete link` false-positives ALL confined to `design/l4_calculus.md` (math-display LaTeX substitution-bracket / lambda forms), NONE in a cycle-027-touched file. The expected non-blocking citecheck `operator.cpp:621-639` AMBIG is inside D2's preserve-verbatim payload in the report CYCLE.md (must match `linalg-operator-file.md:33`'s bare-basename convention verbatim), NOT in the artifact — not chased.

## Safety-net gates

- **retroactive-budget global: 0** (all 5 rows 0-retroactive; the hygiene re-anchors + cite/cross-ref upgrades + additive `verified_against:` append are not surface-rewrites; well below the ≥4 block threshold).
- **build-breakage repair:** none required (clean build).
- **commit atomicity:** single commit (artifact + scaffolding + log + book output + consumed-report frontmatter + staging log).
- **consumed-report frontmatter integrity:** the 5 APPLIED reports marked `integrated_at` + `integration_commit` + `integration_notes`; D5 (deferred) deliberately NOT marked.
- **implied-component-stub-created: 0** (no dangling forward-ref required a stub — D1's `normalize.md:104` ref resolved to a live link because the theme landed same-cycle ahead of it).
- **in-cycle-live-link-upgrade: 1** (`normalize.md:104`, enacted by the per-report integrator).
- **SUMMARY-chapter-registration auto-fix: 0** (every report proposed its own SUMMARY edit, using the correct slugs `normalize-mutation-rotation` + `back_solve`).

## Staging-log-completeness note

**5/5 rows == dispatched-ready-reports — the cycle-018 staging-completeness gap did NOT recur for the NINTH consecutive cycle.** STAGING.md was authoritative; the cross-check of 5 staging rows vs 5 dispatched ready reports reconciles clean (all `applied`). The 6th dispatched report (D5) was DEFERRED `needs-revision` and correctly NOT staged — this is the expected deferral path, not a completeness gap.

## Wave-conflict observations

- **Shared-file serialized cleanly.** `SUMMARY.md` was touched by report 1 (`normalize-mutation-rotation` registration between `matrix-weighted-norm-mutation-rotation` and `lu-solve-mutation-rotation`) and report 4 (`back_solve` registration under L1 after `nleps_eigenvalue_correction`) — two disjoint anchors; the serial per-report integrator order re-read SUMMARY from disk before each edit, no collision. `L1-L0/index.md` (report 1) + `L1/index.md` (report 4) likewise disjoint.
- **`matrix-weighted-norm` touched by two reports without conflict.** D2 (lifter-hygiene) touched the **L1 entry** `L1/matrix-weighted-norm.md` (brace re-anchor + Category relabel) while D3 (lowering-verifier-audit) touched the **L1>L0 theme** `L1-L0/matrix-weighted-norm-mutation-rotation.md` (additive `verified_against:` at EOF) — distinct files, no contention; each re-read its target from disk before editing.
- **`back_solve` ↔ `incremental-least-squares` serial dependency held (no stub needed).** D4 landed the firm L1 `back_solve` leaf; D6 (integrates LAST) then upgraded `L2/ksp_solve.md` to live-link the firm `incremental-least-squares` and cross-reference the `back_solve` output — the target was already firm on-disk (cycle-026), and `back_solve` landed earlier same-cycle, so no plain-text forward-reference dangled.
- **D4/D5 slug collision (the coordinated-rename trap).** D4 (`back_solve` harvest) and D5 (`incremental-least-squares-composition-lowering` theme) collided on the `ls_update_column` slug. The coordinated-rename instruction's premise was INVERTED relative to the artifact (D5's theme legitimately keeps `ls_update_column` for the column-streaming step; D4's leaf is the terminal back-solve and was renamed `back_solve`). D4 applied clean; D5's repairer caught the inversion and returned `needs-revision` → DEFERRED. Resolution: D4's `back_solve` slug-collision re-confirmed collision-FREE before applying (grepped `book/src/L1/` + `SUMMARY.md` for `back_solve` → zero hits).

## Integration-tooling friction (batch-7 evidence-window — third / batch-closing entry)

- **codemap `read_range` +1 brace-boundary drift — CONFIRMED across batches 5/6/7 (STRONG enactment candidate).** Cycles 024/025/026/027 all hit it; this cycle the D2 lifter's `matrix-weighted-norm.md` brace re-anchor `:601-606`→`:602-606` is the same `+1` class on a brace-opening line, plus multiple producers + audits re-confirmed. The standing OQ `codemap-read-range-plus-one-drift-on-brace-boundary` recommends strengthening role-specs to "codemap is localization-only; citecheck/on-disk is the citation source of truth" + possibly a standing citecheck `--anchor` gate (now that `tools/citecheck` is wired). **The batch-7 meta-phase should ENACT this.**
- **NEW process-friction signal — the coordinated-cross-report-rename trap.** This cycle's D4/D5 slug collision led to a coordinated-rename instruction whose premise was INVERTED relative to the artifact; the D5 repairer caught it and filed skill-candidate `audit-slug-meaning-before-coordinated-cross-report-rename`. The meta-phase should evaluate that candidate + consider whether harvester/abstractor dispatches should run a pre-harvest slug-collision check against the existing artifact vocabulary BEFORE harvesting.
- **Non-blocking citecheck AMBIG prose tokens** inside report CYCLE.md files (the expected `operator.cpp:621-639` AMBIG in D2's preserve-verbatim payload) — NOT in the artifact, not chased.
- **`scaffolding/integrator-signals.md` well over the ~500-line budget** (archival backlog since ~cycle-007) — meta-phase archival task.

## Carry-forward to the batch-7 meta-phase (fires after THIS finalize — BATCH-CLOSING)

1. **codemap `read_range` +1 brace-boundary drift — confirmed batches 5/6/7; STRONG enactment candidate** (role-spec wording "codemap is localization-only; citecheck/on-disk is citation source of truth" + possibly a standing citecheck `--anchor` per-report gate). **ENACT.**
2. **NEW process-friction — the coordinated-cross-report-rename trap.** Evaluate skill-candidate `audit-slug-meaning-before-coordinated-cross-report-rename` + consider a pre-harvest slug-collision check against existing artifact vocabulary.
3. **D5 deferral.** `incremental-least-squares-composition-lowering` L2>L1 theme deferred to c028 (needs `back_solve` re-anchor + `trsv` reconciliation + possibly the column-streaming `ls_update_column` leaf harvest). HIGH-value c028 plan item (OQ `incremental-least-squares-composition-lowering-theme-deferred-needs-back-solve-reanchor`).
4. **Carry-forward residuals:** the `:22`/`:87` Category-mislabel residual sweep (c028, OQ `linalg-operator-file-category-mislabel-residual-lines-22-87`); the `incremental-least-squares.md:13` "queued" self-description staleness (c028 lifter, OQ `l2-incremental-least-squares-self-description-still-says-queued-after-firming`); the `matrix-weighted-norm` L1-entry `rough-in`→`firm` promotion gate (test-coverage-bounded); the still-open general `trsv` L3-inventory gap; the deferred `normalize`/`back_solve`/`incremental-least-squares` lowering-verifier audits (c028, firm→next-cycle-audit pattern).
5. **OQ-ledger + `integrator-signals.md` hygiene.** Many OQ lines retirement/unification-ready (the `:327`/`:322` lines from c025 + this batch's many clause-scoped dispositions); `integrator-signals.md` well over the ~500-line budget. **OQ unification pass + signals archival.**
6. **batch-7 cohort summary:** L1 firm 19→21 (+`normalize` c026, +`back_solve` c027); L2 firm 8→9 (+`incremental-least-squares` c026); L1>L0 firm themes +2 (matrix-weighted-norm c026, normalize c027); `l2-named-composition-lifts` + `normalize-l1-primitive-harvest` cohorts COMPLETE; eigsolve cohort fully complete+audited; big multi-cycle citation-hygiene sweep.

## Suggested next-cycle dispatches (cycle-028 — first of meta-batch-8, post-meta-phase)

- (`lifter`, `incremental-least-squares-composition-lowering-promotion`) — the deferred D5 theme: re-anchor the terminal back-solve refs to the now-firm `back_solve` leaf + reconcile `trsv` ↔ `back_solve` + reconcile `ls_update_column` (column-streaming step) vs `back_solve` (terminal solve).
- (`harvester`, `ls_update_column-column-streaming-leaf`) — the still-un-harvested column-streaming `ls_update_column` leaf (the GMRES/FGMRES per-column running-QR streaming step, distinct from the terminal `back_solve`), if the D5 promotion needs it.
- (`lifter`/`repairer`, `linalg-operator-file-category-mislabel-residual-sweep`) — the `:22`/`:87` Category-4→Category-1 residual sweep + the `incremental-least-squares.md:13` "queued" self-description drop.
- (`lowering-verifier`, `normalize-mutation-rotation-audit` + `back_solve` paired) — the standard `verified_against:` audit of the now-firm `normalize-mutation-rotation` L1>L0 theme + the `back_solve` law-confidence audit (the firm→next-cycle-audit pattern).
