## 2026-06-05 cycle-107 — 2 reports applied clean — 102nd consecutive cycle under split integrator — **POSITION 2/3 OF META-BATCH-34, the MIDDLE cycle (cycles 106/107/108; the batch-34 meta-phase fires NEXT-AFTER-108)** — the carried WAVE-3 dofset/BC-cluster reachability gap RESOLVED by GROUNDING (per a NEW 2026-06-05 user directive): the firm-but-absorbed BC-elimination + divfree clusters grounded from the feature-spine roots with 3 honestly-typed, citation-grounded `depends-on` edges → `reachable` 88→95 (+7 nodes rescued, 0 regressions), the WAVE-3 record-rescue tranche is COMPLETE; + 15/16 strict-zero non-node concept pages typed (`untyped` 76→61); `rank_violations` HELD 0 — pass

> **Note:** this file (`cycle-107.md`) is the current-run, structural-redirect-era cycle-107 log (it overwrites the stale pre-redirect slice-vertical-era `cycle-107.md`, recoverable from git history). The current-run numbering does NOT reset across batch boundaries.

**POSITION 2/3 OF META-BATCH-34** (3:1 cadence; cycles 106/107/108; the cycle counter does NOT reset across batch boundaries; **the batch-34 meta-phase fires AFTER cycle-108, as a SEPARATE dispatch aggregating 106/107/108** — this cycle-107 finalize runs NO meta-phase housekeeping). Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) + the 2026-06-02/2026-06-03 user directives + the 2026-06-04 GRADED RESOLUTION LADDER + FEATURE-ROOT REACHABILITY user directive (`METHODOLOGY-GRADED-STACK.md`) + a **NEW 2026-06-05 user directive on grounding** (below). No session restart since c106 (mid-batch; context continuity holds). c106 landed `7592988`.

### Headline — the carried WAVE-3 dofset/BC-cluster reachability gap is RESOLVED by GROUNDING; the WAVE-3 record-rescue tranche is COMPLETE (all 6 internal records + the BC/divfree absorbed clusters now root-reachable, `reachable` 88→95)

**The NEW 2026-06-05 user directive behind this cycle's LEAD:** when the reachability-GC surfaces an unreachable node that is a genuine **future-or-absorbed dependency of a goal node**, **GROUND it** into the goal node with an honestly-typed `depends-on` edge (a temporary grounding) — rather than deleting it or filing it as detritus. This supersedes the c106 faithful-path-OR-finding default of routing-as-a-finding for this class of absorbed-below-column firm node.

**D1** (THE LEAD, HIGH) audited the carried BC/divfree cluster and confirmed the c106-recommended `column→eliminate_bc` edge would be an UNFAITHFUL over-link (the driver columns compose `fe_assemble`; BC-elimination is a SEPARABLE post-composition absorbed into `models/`-level operator construction, NONE in `palace/drivers/`). Per the new grounding directive, D1 GROUNDED the cluster from the feature-spine roots with **3 honestly-typed, citation-grounded (critic-verified faithful) `depends-on` edges** — rescuing **+7 nodes, `reachable` 88→95, 0 regressions, `rank_violations` HELD 0** — and resolving BOTH carried OQs grounded-and-rescued. **D2** (LOW) typed 15 of the 16 strict-zero cluster-B/C non-node concept pages with the batch-33-ratified `reference`-only encoding (`untyped` 76→61), deferring the 16th (`counter-update`) as a node needing `rank:`.

### What landed

- **D1 (layer-intro-author, build-relevant) — THE LEAD: GROUND the firm-but-absorbed BC-elimination + divfree clusters from the feature-spine roots (the dofset / set_subvector_zero rescue).** Authored 3 frontmatter-only grounding `depends-on` edges, each citation-grounded against Palace `models/` sites and critic-verified faithful:
  - `book/src/L4/fe_assemble.md` — added `depends-on (kind: absorbed-post-composition) → L4/eliminate_bc` (all three pre-existing `reference:` entries — `L4/index`, `concepts/black-box-vs-accelerated-kernels`, `concepts/state-stratification` — PRESERVED).
  - `book/src/feature/eigenmode.L4.md` — added `depends-on (kind: constrains-eigvec) → L3/divfree-projector` (between the `eigsolve composes` and `cites-evidence` edges).
  - `book/src/L3/divfree-projector.md` — added two `depends-on (kind: uses)` edges → `L1/set_subvector_zero` + `concepts/set_subvector_zero`.
  - **RESCUE MEASURED (live tree, `--show-inbound` + JSON detritus set-diff):** `reachable` 88→95 (+7), `detritus` 156→149 (−7), `rank_violations` 0→0 HELD, `untyped` 76→76 HELD, 0 regressions. ACTUAL 7-node rescue set: `L4/eliminate_bc`, `concepts/dofset`, `L3/divfree-projector`, `L2/divfree-projector`, `L1/set_subvector_zero`, `concepts/set_subvector_zero`, AND `L4-L3/bc-elimination-post-composition-dissolution` (rode along transitively via `eliminate_bc`'s `depends-on`). Resolved OQs `bc-driver-column-eliminate-bc-edge-gap-blocks-dofset-rescue` (c106 D3) + `set-subvector-zero-cluster-reachability-not-rescued-by-reference-backlink` (c106 D4) grounded-and-rescued. Opened the consolidated OQ `bc-divfree-absorbed-clusters-grounded-and-rescued`.
  - **ENUMERATION DISCREPANCY (recorded, NOT a defect):** the report enumerated `L1/divfree-projector` as one of the 7 rescued nodes, but it did NOT flip — it stays `[garbage?]`. Root cause: `book/src/L2/divfree-projector.md` is a PRE-SCHEME chapter with NO `edges:` block, so the mark-sweep dead-ends at `L2/divfree-projector` (its outbound to `L1/divfree-projector` is prose-only, untyped). `L1/divfree-projector` is an edge-untypedness-artifact detritus node, not genuine garbage; it flips when the pre-P1 lowering-edge typing lands on `L2/divfree-projector`. `L4-L3/bc-elimination-post-composition-dissolution` flipped in its place, so the headline +7/−7 is exactly correct. Routed into the batch-34 OQ `lowering-chain-liveness-not-propagated-to-l1-ops` (same root cause as the L1 BC-op tail).
- **D2 (layer-intro-author, build-relevant, LOW) — the 15 strict-zero cluster-B/C non-node concept pages get the batch-33-ratified `reference`-only `edges:` block:** prepended a `reference`-only `edges:` block (NO `rank:`, per the batch-33-ratified non-node concept-page encoding `graded-stack-scheme.md` §5/§6) onto 15 of the 16 strict-zero concept pages — `build-time-vs-run-time-stratification`, `capability-typing`, `chebyshev-iteration`, `constructed-operator-factory`, `constructed-operators`, `convergence-test`, `derived-view-hoisting`, `eigsolve`, `erasure-scope`, `ksp_solve`, `nested-constructed-operator-gate`, `rotation`, `solve-monad`, `solver-as-operator`, `state-stratification` (101 edges total, 45 distinct targets, all on-disk). **DEFERRED:** the 16th page `concepts/counter-update` — DELIBERATELY NOT touched; it is a node needing `rank:` (sole-definition site of L2 `counter_update`), ratified→node but its rank is unguessable from the on-disk page (no `## Status` / firm apparatus). Captured by promoted OQ `concepts-counter-update-needs-node-rank-and-depends-on-edges` (routed to batch-34; no live rank/liveness impact — no DAG node carries a typed `depends-on: concepts/counter-update` today). **METRIC:** `untyped` 76→61 (−15, exactly the 15 typed pages), `typed` 279→294 (+15), `reachable` HELD 95, `rank_violations` HELD 0, `unresolved_depends_on_targets` HELD 0.

### Build

`cargo make book` (mdbook + linkcheck2) **EXIT 0** (~93s); **NO build-repair needed**. All 18 touched `book/` files (D1's 3 grounding-edge chapters + D2's 15 concept pages) are frontmatter-only edits; every edge target resolves to an on-disk file, so linkcheck2 is clean. Only the 3 pre-existing benign KaTeX `Potential incomplete link` WARNs in `design/l4_calculus.md` (math-notation false-positives, NOT from any cycle-107-edited file). No new file was created (every chapter pre-existed + was already SUMMARY-registered), so no SUMMARY/index insert was needed.

### Step-5b — graded-stack linters (the build-gate companion, ran on the landed tree)

**`rank_violations: 0`** (baseline fully discharged c096 so ANY violation would be NEW and BLOCK — there are NONE, **GATE PASSES**; D1's 3 grounding edges are firm→firm — `fe_assemble`→`eliminate_bc`, `eigenmode.L4`→`divfree-projector`, `divfree-projector`→`set_subvector_zero` — or firm→untyped-non-node — `concepts/set_subvector_zero`, tolerated exactly as the pre-existing `divfree-projector → L2/divfree-projector` untyped edge — and D2's 15 non-node blocks carry no `rank:` / no `depends-on`, so there is no promotion flip to gate) + **NO newly-orphaned node** (`reachable` HELD 95 across D2; verified by git-stash set-diff of the `detritus` JSON node-sets). **`unresolved_depends_on_targets: 0`** (HELD). Totals on the landed tree: `files=355`, `typed=294`, **`untyped=61`** (was 76, −15), `roots=36`, **`reachable=95`** (was 88), `rank_violations=0`, `unresolved_depends_on_targets=0`, `promotion_frontier=8`, **`detritus=163`** (`detritus_no_typed_edges_pre_p1_artifact=125`, `detritus_with_typed_edges_stronger_signal=38`, `expected_unreachable_outside_dag=44`).

**DETRITUS-RISE CAVEAT (NOT a regression):** detritus rose 149 (post-D1) → 163 (post-D2), +14. This is PURELY the untyped→typed-non-node reclassification — the 14 newly-typed `concepts/*` pages carry no liveness by construction (a non-node asserts no blocking dependency) and join the counted-detritus set, the same disposition the pre-existing `concepts/{dot,nrm2,scal,apply_linop,axpy}` reference-only pages already hold. **0 nodes lost reachability** (`reachable` held 95→95, `rank_violations` held 0→0), verified by git-stash set-diff: the 14 ENTERED-detritus nodes are EXACTLY the 14 newly-typed non-node pages; 0 nodes LEFT detritus, 0 newly-orphaned. The 15th typed page (`concepts/solve-monad`) became reachable via an inbound navigational edge (the delta is +14 not +15). **Do not misread the detritus number as backsliding.**

**rank_violations trend: 22 (c094) → 1 (c095) → 0 (c096) → … → 0 (c105) → 0 (c106) → 0 (c107). reachable trend over the WAVE-3 campaign: 36 (c105-end, pre-linter-fix) → 81 (batch-33 meta-phase) → 88 (c106) → 95 (c107).**

**On the high `untyped`/`detritus` mass:** informational, NOT a block (per step-5b — the as-yet-untyped pre-P1 tail + the typed-non-node reference-only pages). Only a *new* rank violation or a *newly*-orphaned node gates; neither occurred.

**Carried follow-up (the L1-BC-op-tail lowering-chain-liveness gap), routed to the batch-34 meta-phase:** `L1/divfree-projector` did NOT flip reachable (the enumeration-discrepancy edge-untypedness artifact above) because `L2/divfree-projector.md` is a PRE-SCHEME chapter with no `edges:` block — the L2→L1 mark-sweep dead-ends. Same root cause as the L1 BC-op tail + the `L1-L0/set-subvector-zero-mutation-rotation` untyped legacy edges. OQ `lowering-chain-liveness-not-propagated-to-l1-ops`.

### Counts

No firm-count status flips this cycle — D1's 3 grounding edges + D2's 15 non-node typings are all frontmatter-only (no new authored vocabulary, no node promotion). Layer tallies unchanged: L1 firm 33 main / 40 grand · L4 firm 21 main / 25 grand · L4>L3 11 · L3 17+4po · L3>L2 6 · L2 21+1pc · L2>L1 11 · L0 22 · concepts 34 · methodology 4 · feature spine 11 firm / 1 seed · L4 reduce-family 4 verbs ALL firm. SLICE CORPUS: 0. The measurable movement is on the reachability axis (`reachable` 88→95, 7 absorbed-cluster nodes grounded-and-rescued — the WAVE-3 record-rescue tranche COMPLETE) + the typed-edge axis (`untyped` 76→61).

### Process

retroactive-budget global = 0 (per-row sum across both staging rows = 0, far under the ≥4 block threshold). Per-report safety-net gates all PASS/N/A across both rows (retroactive-budget, concept_writes-on-existing-slug, forward-edge-without-surface, edge-label/prose-mismatch, H1-reuses-page-heading, append-on-missing-slug, variant-axis-missing, SUMMARY-registration, alpha-position, rank-well-foundedness). 0 implied-component stubs created. 2/2 staging rows == 2 dispatched-ready reports (the cycle-018 staging-completeness gap did NOT recur — 88th consecutive clean staging / 102nd consecutive clean split-integrator cycle). D2's `counter-update` deferral was a deliberate planned single-page deferral captured by its OQ (NOT a report deferral; both reports applied clean). A pre-integration repairer path-hygiene fix on D1's bare-path `graded_stack_lint.py:431` citation (→ `tools/graded-stack-lint/graded_stack_lint.py:431`) held; per-report YAML round-trip PASS on all 18 edited files. `scaffolding/priorities.md` + `scaffolding/open-questions.md` + `scaffolding/integrator-signals.md` + `scaffolding/roadmap.md` + `scaffolding/cycle-record.jsonl` + `log/` all modified, committed atomically with the artifact. NO `.claude/agents/` changes → no session-restart concern.

### Signals for the batch-34 meta-phase (fires after c108)

- **The grounding directive (NEW 2026-06-05) is now in live use** — D1 applied it to rescue the absorbed BC/divfree cluster (the c106 faithful-path-OR-finding default would have routed-as-a-finding + recommended a baseline-exception; the new directive prefers GROUNDING the absorbed-but-firm dependency into the goal node). The meta-phase should codify the grounding-vs-route disposition (when to ground an absorbed/future dependency vs file detritus vs baseline-exception) into the role-specs + `METHODOLOGY-GRADED-STACK.md`.
- **The L1-BC-op-tail lowering-chain-liveness gap** (OQ `lowering-chain-liveness-not-propagated-to-l1-ops`) — `L1/divfree-projector` + the L1 BC-op tail + `L1-L0/set-subvector-zero-mutation-rotation` stay detritus because the intervening PRE-SCHEME lowering chapters (esp. `L2/divfree-projector.md`) carry no typed `edges:` block, dead-ending the L2→L1 mark-sweep. A bounded lazy-tail typing target (type the pre-scheme lowering chapters' outbound edges) — the natural batch-34/c108 candidate.
- **`concepts/counter-update` node-typing** (OQ `concepts-counter-update-needs-node-rank-and-depends-on-edges`) — the single deferred page; ratified→node but rank unguessable on-disk; a small node-typing dispatch (read the L2 `counter_update` apparatus, assign `rank:` + `depends-on`).
- **Recommended c108 candidate work** (the BATCH-CLOSING cycle): the L1-op-tail lowering-edge typing, the `counter-update` node-typing, or remaining lazy-untyped-tail acquisition — the clean-gated forward-vocabulary frontier remains substantially exhausted (the measurable movement is on the reachability/typed-edge axes).
- **The integrator-signals chain is unbroken** (the c106 finalize appended; this c107 finalize appends its section) — recorded against the c106-noted "integrator-signals stale at cycle-019" planner false-read.

— written by `integrator-finalize` (split integrator-per-report ×2 + finalize ×1).
