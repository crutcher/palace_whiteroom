## 2026-06-06 cycle-110 — 2 reports applied clean — 105th consecutive cycle under split integrator — **POSITION 2/3 OF META-BATCH-35, the MIDDLE cycle (cycles 109/110/111; the batch-35 meta-phase fires NEXT-AFTER cycle-111's finalize, aggregating 109/110/111)** — the c109 Group-B finding (`l2-reduce-orthogonalize-cohort-itself-unreachable-blocks-theme-grounding`) RESOLVED by a single faithful host edit (D1: +3 `composes` `depends-on` edges on `L4/krylov-step` cascade the reduce-to-scalar chain reachable + ground the orthogonalize leg) + the lazy-tail BLAS-leaf typing (D2: `rank: firm` + `edges:` on `L1/{axpy,axpby,axpbypcz}`) → TRUE CUMULATIVE `reachable` 107→119 (+12: D1 +10 reduce chain, D2 +2 axpby/axpbypcz themes), STRONGER GARBAGE SIGNAL 34→26, `detritus` 152→140 (−12); `rank_violations` HELD 0, `untyped` HELD 60 — pass

> **Note:** this file (`cycle-110.md`) is the current-run, structural-redirect-era cycle-110 log (it overwrites the stale pre-redirect slice-vertical-era `cycle-110.md`, recoverable from git history). The current-run numbering does NOT reset across batch boundaries.

**POSITION 2/3 OF META-BATCH-35 — THE MIDDLE CYCLE** (3:1 cadence; cycles 109/110/111; the cycle counter does NOT reset across batch boundaries; **the batch-35 meta-phase fires AFTER cycle-111's finalize, as a SEPARATE dispatch aggregating 109/110/111**). Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) + the 2026-06-02/2026-06-03 user directives + the 2026-06-04 GRADED RESOLUTION LADDER + FEATURE-ROOT REACHABILITY user directive (`METHODOLOGY-GRADED-STACK.md`) + the 2026-06-05 GROUND-don't-remove grounding directive (codified into `METHODOLOGY-GRADED-STACK.md` §2f/§8 by the batch-34 meta-phase). c109 landed its finalize commit; the batch-34 meta-phase landed `4c26325`.

### Headline — the reduce-to-scalar chain + orthogonalize leg are GROUNDED by one faithful host edit; the axpy-family lazy-tail is typed (`reachable` 107→119)

The c109 Group-B finding routed the next tranche: the L2 reduce/orthogonalize/chebyshev cohort was off-spine because nothing reachable pointed `depends-on` into the reduce verbs. The reduce-to-scalar chain (`inner_product`/`nrm2`/`dot`) was typed correctly DOWNWARD (L4→L3→L2) but **dead at the top**. The §(g) GROUND-don't-remove faithful fix: add `composes` `depends-on` edges from the already-reachable consumer `L4/krylov-step` (reachable ← `L4/ksp_solve` ← driven/electrostatic/magnetostatic.L4) into the reduce/orthogonalize verbs its body GENUINELY calls. ONE host edit cascades the entire reduce-to-scalar chain reachable + grounds the orthogonalize leg.

`reachable` 107→119 (+12), `rank_violations` HELD 0, `untyped` HELD 60, `detritus` 152→140 (−12; STRONGER GARBAGE SIGNAL 34→26, edge-untyped artifact 118→114). All 4 touched `book/` files are FRONTMATTER-ONLY edits.

### What landed

- **D1 (layer-intro-author, build-relevant, MEDIUM) — THE LEAD: the reduce/orthogonalize/chebyshev cohort grounding pass.** One frontmatter-only `edges:` edit on `book/src/L4/krylov-step.md`: +3 `composes` `depends-on` edges → `L4/dot`, `L4/nrm2`, `L2/orthogonalize` (inserted after the `L2/krylov-step` lowers-to edge, before the `concepts/op-params` uses-record block), each confirmed REAL from the `krylov-step` body prose before typing. The single host's three body edges cascade the whole reduce chain down to L2 (D1-isolation `reachable` 107→117, +10). ROUTED 4 OQs to the batch-35 meta-phase: `reduce-to-scalar-chain-grounded-via-krylov-step-body-composes-edges` (RESOLVED-PARTIAL, advances the carried `l2-reduce-orthogonalize-cohort-itself-unreachable-blocks-theme-grounding`); `chebyshev-jacobi-preconditioner-leg-absorbed-below-column-baseline-exception`; `gram-reduce-inner-product-is-sibling-not-composes-edge-declined`; `l3-l2-reduce-orthogonalize-midnodes-lack-typed-edges-blocks`.
- **D2 (layer-intro-author, build-relevant) — the lazy-tail BLAS-leaf typing.** `rank: firm` + typed `edges:` blocks prepended onto `book/src/L1/{axpy,axpby,axpbypcz}.md` (which previously carried NO frontmatter — began directly at the H1), mirroring the `scal`/`apply_linop`/`set_subvector_zero` convention: blocking `depends-on` = the rank-terminal POSITIVE L0 source (via `kind: cites-evidence`) + the `lowers-to` edge to the L1>L0 mutation-rotation theme; siblings + concept narrative as `reference`. Standalone +2 `reachable` (the `L1-L0/axpby-mutation-rotation ← L1/axpby, L1/axpy` and `L1-L0/axpbypcz-mutation-rotation ← L1/axpbypcz` themes flip; both LEFT detritus before). `untyped` HELD 60 — the predicted 60→57 was WRONG: the linter's prose-`## Status` rank fallback already ranked these prose-firm; the real win was Axis-2 reachability (+2), not Axis-1 untyped. RESOLVES the c109 repairer-filed OQ `l1-blas-leaves-axpy-family-lack-rank-frontmatter` (recorded for the meta-phase to unify/close; the OQ ledger is append-only between meta-phases). Promoted 1 OQ: `l1-l0-axpy-family-themes-need-scheme-frontmatter`.
- **TRUE CUMULATIVE (both applied, re-measured by the per-report integrator on the post-apply tree):** `reachable` 107→119 (+12: D1 +10 reduce chain, D2 +2 themes); `rank_violations` HELD 0; `untyped` HELD 60; firm histogram 201 HELD; `detritus` 152→140; STRONGER GARBAGE SIGNAL 34→26. Do NOT sum per-dispatch isolation deltas — the 119 is the re-measured cumulative figure.

### Build

`cargo make book` (mdbook + linkcheck2) **EXIT 0**; **NO build-repair needed**. All 4 touched `book/` files are frontmatter-only edits; every edge target resolves to an on-disk file, so linkcheck2 is clean. The `Potential incomplete link` WARNs are the pre-existing benign markdown-table / KaTeX `$...$` false-positives (bracketed prose in dep-map cells + math spans), NOT link errors. No new file was created (every chapter pre-existed + was already SUMMARY-registered), so no SUMMARY/index insert was needed.

### Step-5b — graded-stack linters (the build-gate companion, ran on the landed tree)

**`rank_violations: 0`** (`[]`; baseline fully discharged c096 so ANY violation would be NEW and BLOCK — there are NONE, **GATE PASSES**; D1's 3 `composes` edges are firm→firm; D2's 3 BLAS leaves typed firm with `cites-evidence` rank-terminal L0 `depends-on` + `lowers-to` to the L1>L0 theme) + **NO newly-orphaned node** (`reachable` CLIMBED 107→119) + **`unresolved_depends_on_targets: 0`** (HELD). Totals on the landed tree: `files=355`, `typed=295`, **`untyped=60`** (HELD), `roots=36`, **`reachable=119`** (was 107, +12), `rank_violations=0`, `unresolved_depends_on_targets=0`, `promotion_frontier=8`, **`detritus=140`** (`detritus_no_typed_edges_pre_p1_artifact=114`, `detritus_with_typed_edges_stronger_signal=26`, `expected_unreachable_outside_dag=44`). `rank_histogram: {firm:201, typed-no-rank:80, rough-in:5, partly-constructive:3, obstruction:2, partial-obstruction:4}`.

**rank_violations trend: 22 (c094) → 1 (c095) → 0 (c096) → … → 0 (c108) → 0 (c109) → 0 (c110). reachable trend across the campaign: 36 (c105-end, pre-linter-fix) → 81 (batch-33 meta-phase) → 88 (c106) → 95 (c107) → 102 (c108) → 107 (c109) → 119 (c110)** — c110 grounded the reduce-to-scalar chain + orthogonalize leg + 2 BLAS mutation-rotation themes.

**On the high `untyped`/`detritus` mass:** informational, NOT a block (per step-5b — the as-yet-untyped pre-P1 tail + the typed-non-node reference-only pages + the typed-but-unreached nodes). Only a *new* rank violation or a *newly*-orphaned node gates; neither occurred. STRONGER GARBAGE SIGNAL (typed-but-unreached nodes) dropped 34→26 — the grounded reduce/orthogonalize/theme nodes left the typed-detritus set.

### Counts

No firm-count status flips this cycle — D1's 3 edges + D2's 3 BLAS-leaf typings are all frontmatter-only (no new authored vocabulary, no node promotion). Layer tallies unchanged: L1 firm 33 main / 40 grand · L4 firm 21 main / 25 grand · L4>L3 11 · L3 17+4po · L3>L2 6 · L2 21+1pc · L2>L1 11 · L0 22 · concepts 34 · methodology 4 · feature spine 11 firm / 1 seed · L4 reduce-family 4 verbs ALL firm. SLICE CORPUS: 0. The measurable movement is entirely on the reachability axis (`reachable` 107→119, the reduce-to-scalar chain + orthogonalize leg + 2 BLAS themes grounded, STRONGER GARBAGE SIGNAL 34→26).

### Process

retroactive-budget global = 0 (both staging rows contribute 0, far under the ≥4 block threshold). Per-report safety-net gates all PASS/N/A on both rows (retroactive-budget, concept_writes-on-existing-slug, forward-edge-without-surface, edge-label/prose-mismatch, H1-reuses-page-heading, append-on-missing-slug, variant-axis-missing, SUMMARY-registration, alpha-position, rank-well-foundedness). 0 implied-component stubs created. 2/2 staging rows == 2 dispatched-ready reports (the cycle-018 staging-completeness gap did NOT recur — 91st consecutive clean staging / 105th consecutive clean split-integrator cycle). **REPAIR PHASE DID RUN this cycle:** 2 reports, each with 2 low/medium measurement-misattribution warnings fixed in-place — the producers' headline reachability figures corrected to the standalone/cumulative truth (D1 119/+12 → 117/+10 isolation; D2 +12 → +2 standalone with a finalize-re-measure instruction); the `edges:` blocks were left untouched. **CITECHECK NOTE (non-blocking):** the per-report integrators noted AMBIG/MISS/OOB citecheck warnings — ALL on report PROSE / OQ-prose / linter-tool-source references (`graded_stack_lint.py:425-437` is the linter tool's OWN source line, not a Palace L0 citation; `krylov-step.md:94` bare-basename AMBIG; `gmres.md:471-489` OOB intending a Palace source), NONE in any applied `edges:` block; no prose-claim text lands in `book/` (frontmatter-only edits), so all are non-blocking and recorded-not-repaired. Per-report YAML round-trip PASS on all 4 edited files. `scaffolding/open-questions.md` + `scaffolding/priorities.md` + `scaffolding/integrator-signals.md` + `scaffolding/roadmap.md` + `scaffolding/cycle-record.jsonl` + `log/` all modified, committed atomically with the artifact. NO `.claude/agents/` changes → no session-restart concern from finalize.

### Signals for the batch-35 meta-phase (fires AFTER cycle-111's finalize, aggregating 109/110/111)

- **MEASUREMENT-HYGIENE FRICTION CANDIDATE — `parallel-dispatch-reachability-measurement-contamination`:** both parallel D1/D2 layer-intro-author dispatches initially MISreported reachability because each measured the linter with the OTHER's not-yet-reverted edits in the working tree (apply→lint→revert contamination across parallel dispatches). Both were caught by the critics + fixed by the repairers (the producer headlines corrected to the standalone/cumulative truth), but the recurring pattern is a candidate friction-ledger entry: when two layer-intro-author dispatches both run the reachability linter on a shared working tree, each must isolate its own edit-set (git-stash the sibling's, or measure isolation + instruct finalize to re-measure cumulatively). The per-report-integrator re-measure-on-landed-tree step is the safety net that produced the authoritative 119; consider whether the producer-side measurement protocol needs tightening.
- **THE NEXT MECHANICAL TRANCHE (the carried Group-B residual):** the lazy-tail mid-node typing of `L2/orthogonalize` / `L3/orthogonalize` / `L3/nrm2` would flip `orthogonalize-composition-lowering` — once those mid-nodes carry typed `edges:`, the already-grounded `L2/orthogonalize` (now reachable via D1's `L4/krylov-step` composes edge) propagates down. A clean mechanical pick for cycle-111.
- **ROUTED DISPOSITION CALLS for the meta-phase:** (1) the chebyshev/jacobi preconditioner leg recommended for a baseline-exception (absorbed-below-column, the c107 BC/divfree pattern); (2) `gram_reduce`→`inner_product` is a SIBLING `reference` NOT a `composes` `depends-on` edge (decline confirmed by D1, critic-verified); (3) the `incremental-least-squares` absorbed routing.
- **OQ to UNIFY:** `l1-blas-leaves-axpy-family-lack-rank-frontmatter` (the c109 repairer-filed OQ) is RESOLVED by D2 — the 3 L1 leaves now carry explicit `rank: firm` + `edges:` frontmatter, so inbound `depends-on` edges from L2 consumers now rest verified firm→firm instead of vacuously. The meta-phase has unify/close authority (the ledger is append-only between meta-phases).
- **The clean-gated forward-vocabulary frontier remains substantially exhausted** — the measurable movement is on the reachability/typed-edge axes, not new authored vocabulary. The `promotion_frontier: 8` members are ALL obstruction-/demand-gated (not clean picks).
- **The integrator-signals chain is unbroken** (the c109 finalize appended; this c110 finalize appends its section).

— written by `integrator-finalize` (split integrator-per-report ×2 + finalize ×1).
