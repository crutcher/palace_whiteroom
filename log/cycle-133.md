## 2026-05-26 cycle-133 — forward cg_preconditioning_framework [L1→L2] — revise

- Synthesis: cg_preconditioning_framework L1→L2: composes factory + bind + apply primitives; extracts solver-as-operator, constructed-operator-factory, complex-from-real-lift, finest-level-unwrap, counter-update concepts. skills_consulted: [skill-selection (applied), propose-rotation (applied per primitive), classify-variant-axis (n/a — variant axes already classified at L1; L2 confirms factory absorbs them), verify-citation-range (n/a — no new L0 citations introduced)]
- Verdict: revise.
- Friction: The `constructed_operator_factory(role: "krylov"|"preconditioner", ...)` rotation collapses two L0 functions with materially different signatures (one takes `fespaces`/`aux_fespaces`, the other does not; one consumes krylov-method×side×orthog×restart axes, the other consumes preconditioner-type×multigrid×scalar-field axes) into a single primitive whose `role` parameter then re-fans-out internally. The 'coarser substitution' criterion (b) requires that L2 admits substitution at a grain L1 cannot — but no downstream slice will ever substitute a `role="krylov"` factory call for a `role="preconditioner"` factory call; the two roles are not interchangeable. What the concept actually captures is the SHAPE-LEVEL pattern (enum → typed handle) shared by two distinct factories, not a single primitive. The state-hiding is real (the variant axes vanish from downstream prose) but that hiding is already done by the per-role factory; lifting to a single `role`-parametrised primitive is a renaming wrapper, not a rotation..
- Structural change: applied: 5 concept_write(s), 6 dep-map edge(s), 2 lesson(s); 6 rotation_claim(s).
