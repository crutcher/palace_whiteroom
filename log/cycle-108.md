## 2026-06-05 cycle-108 — 2 reports applied clean — 103rd consecutive cycle under split integrator — **POSITION 3/3 OF META-BATCH-34, the BATCH-CLOSING cycle (cycles 106/107/108; the batch-34 meta-phase fires NEXT-AFTER this finalize, aggregating 106/107/108)** — the ONE carried follow-up (`lowering-chain-liveness-not-propagated-to-l1-ops`) RESOLVED by a SYSTEMATIC `lowers-to` GROUNDING pass down the BC + divfree lowering chains: 8 pre-scheme lowering-chain chapters typed → `reachable` 95→102 (+7 nodes rescued, 0 regression); + `concepts/counter-update` typed as a `firm` node (`untyped` 61→60, honestly NOT forced reachable); `rank_violations` HELD 0 — pass

> **Note:** this file (`cycle-108.md`) is the current-run, structural-redirect-era cycle-108 log (it overwrites the stale pre-redirect slice-vertical-era `cycle-108.md`, recoverable from git history). The current-run numbering does NOT reset across batch boundaries.

**POSITION 3/3 OF META-BATCH-34 — THE BATCH-CLOSING CYCLE** (3:1 cadence; cycles 106/107/108; the cycle counter does NOT reset across batch boundaries; **the batch-34 meta-phase fires AFTER this cycle-108 finalize, as a SEPARATE dispatch aggregating 106/107/108**). Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) + the 2026-06-02/2026-06-03 user directives + the 2026-06-04 GRADED RESOLUTION LADDER + FEATURE-ROOT REACHABILITY user directive (`METHODOLOGY-GRADED-STACK.md`) + the 2026-06-05 grounding directive. No session restart since c106 (mid-batch; context continuity holds). c107 landed `24c3e71`.

### Headline — the carried lowering-chain-liveness gap is RESOLVED by a systematic `lowers-to` grounding pass; the BC + divfree L1/L0 lowering homes are now root-reachable (`reachable` 95→102)

The ONE carried follow-up from c107 (OQ `lowering-chain-liveness-not-propagated-to-l1-ops`) was that the L1 BC ops (`L1/eliminate_essential_bc`, `L1/essential_dofs`) + `L1/divfree-projector` + their L1-L0 lowering homes stayed detritus because the intervening **PRE-SCHEME lowering chapters** carried no typed `edges:` block — the L2→L1 / L4-L3→L1 mark-sweep dead-ended. Per the grounding directive (these L1/L0 lowering homes are genuine absorbed dependencies of already-reachable L_{n+1} goal nodes), **D1 GROUNDED them by typing the missing `edges:` on the 8 intervening chapters** — `reachable` 95→102 (+7 nodes rescued), 0 regression, `rank_violations` HELD 0. The pass was FAITHFUL: D1 caught a would-be over-edge (`essential_dofs` is a consumed operand reaching root via its OWN `essential-dofs-construction-rotation` theme, NOT a dissolution lowering target — no forced edge), the faithful-path-or-finding discipline holding.

**D2** (LOW) discharged the c107-D2 deferral by typing `concepts/counter-update.md` as a `firm` NODE (`untyped` 61→60). Honestly NOT forced reachable — its edges are `reference`-only and no DAG node carries a typed `depends-on: concepts/counter-update`, so no `depends-on` was manufactured to chase reachability; this is the EXPECTED critic-reproduced outcome.

### What landed

- **D1 (layer-intro-author, build-relevant, MEDIUM) — THE LEAD: the lowering-chain-liveness grounding pass — type the missing `edges:` blocks down the BC + divfree lowering chains so the already-reachable L4/L3 goal nodes propagate liveness to their L1/L0 lowering homes.** 8 frontmatter edits, each `lowers-to` relationship verified REAL from the chapter prose before typing:
  - **(BC chain)** `book/src/L4-L3/bc-elimination-post-composition-dissolution.md` (legacy `lhs:`/`rhs:` → scheme `edges:` block; `lowers-to → {eliminate_essential_bc, eliminate_rhs, fe-operator-assemble-mutation-rotation}`, `lifts-from eliminate_bc`, `essential_dofs` as a `reference` operand — NOT a `lowers-to` target, the caught over-edge); `book/src/L1/eliminate_essential_bc.md` (legacy → scheme `edges:`; `depends-on {essential_dofs (uses), fe-operator-assemble-mutation-rotation (lowers-to)}`, `variant_axes` preserved verbatim); `book/src/L1/essential_dofs.md` (legacy → scheme `edges:`; `depends-on essential-dofs-construction-rotation (lowers-to)`, `variant_axes` preserved); `book/src/L1-L0/essential-dofs-construction-rotation.md` (authored `edges:` from scratch; `lowers-to L1/essential_dofs` + 3 `cites-evidence` L0).
  - **(divfree chain)** `book/src/L2/divfree-projector.md` (authored `edges:` from scratch — had NO frontmatter; `lowers-to {L1/divfree-projector, L2-L1/divfree-projector-leaf-identity}`, `depends-on L2/ksp_solve`); `book/src/L1/divfree-projector.md` (authored `edges:` from scratch — had NO frontmatter; `lowers-to L1-L0/divfree-projector-mutation-rotation`, `depends-on {ksp_solve, apply_linop, axpy}`); `book/src/L2-L1/divfree-projector-leaf-identity.md` (authored `edges:` from scratch; `lifts-from L2/divfree-projector`, `lowers-to L1/divfree-projector`); `book/src/L1-L0/divfree-projector-mutation-rotation.md` (authored `edges:` from scratch; `lowers-to L1/divfree-projector` + 3 `cites-evidence` L0).
  - **RESCUE MEASURED (live tree, `--show-inbound`):** `reachable` 95→102 (+7), `detritus` 163→156 (−7), `rank_violations` 0→0 HELD, `untyped` 61 HELD, 0 regressions. All 8 target nodes now carry typed inbound `<-` edges, none shows residual `[garbage?]`. The c107-noted `L1/divfree-projector` discrepancy (pre-scheme L2 parent dead-ending the sweep) is now CLOSED. Resolved OQ `lowering-chain-liveness-not-propagated-to-l1-ops` grounded-and-rescued. Routed 2 findings to the batch-34 meta-phase: `l2-l1-theme-cohort-reachability-gap` (the other ~10 L2-L1 themes, identical operator→operator-not-operator→theme cause) + `lowering-theme-reachability-vs-well-foundedness-scheme-clarification` (a `graded-stack-scheme.md` §5 one-line note).
- **D2 (layer-intro-author, build-relevant, LOW) — type `concepts/counter-update.md` as a node (the c107-D2 deferral):** prepended the graded-stack NODE frontmatter (`rank: firm`, `kind: primitive`, `edges: depends-on: []`, `reference: [concepts/state-stratification, L4/preconditioning-framework, L3/krylov-step]`); body prose UNCHANGED. Judged `firm` by the firm-on-positive-structure logic (a fully-specified `## L2 form` + classification basis + ≥2 consumers); the `depends-on → state-stratification` candidate was down-typed to `reference` per well-foundedness (`state-stratification` carries no `rank:` line on-disk — a non-node — so a firm `depends-on` would be well-foundedness murk). **METRIC:** `untyped` 61→60 (−1), `typed` 294→295 (+1), `reachable` HELD 102, `rank_violations` HELD 0, `detritus` 156→157 (+1, `counter-update` joins as a typed-but-not-yet-reached node — honestly not forced live). Resolved OQ `concepts-counter-update-needs-node-rank-and-depends-on-edges`.

### Build

`cargo make book` (mdbook + linkcheck2) **EXIT 0**; **NO build-repair needed**. All 9 touched `book/` files (D1's 8 lowering-chain chapters + D2's 1 concept page) are frontmatter-only edits; every edge target resolves to an on-disk file, so linkcheck2 is clean. The `Potential incomplete link` WARNs are the pre-existing benign markdown-table false-positives (bracketed prose like `cs[j]` / `[Time]` in dep-map cells), NOT link errors. No new file was created (every chapter pre-existed + was already SUMMARY-registered), so no SUMMARY/index insert was needed.

### Step-5b — graded-stack linters (the build-gate companion, ran on the landed tree)

**`rank_violations: 0`** (baseline fully discharged c096 so ANY violation would be NEW and BLOCK — there are NONE, **GATE PASSES**; D1's grounding edges are firm→firm or firm→untyped-non-node lowering home, no promotion flip to gate; D2's node carries `depends-on: []` + `reference`-only edges, so the rank gate is vacuous) + **NO newly-orphaned node** (`reachable` HELD/CLIMBED 95→102) + **`unresolved_depends_on_targets: 0`** (HELD). Totals on the landed tree: `files=355`, `typed=295`, **`untyped=60`** (was 61, −1), `roots=36`, **`reachable=102`** (was 95), `rank_violations=0`, `unresolved_depends_on_targets=0`, `promotion_frontier=8`, **`detritus=157`** (`detritus_no_typed_edges_pre_p1_artifact=122`, `detritus_with_typed_edges_stronger_signal=35`, `expected_unreachable_outside_dag=44`).

**DETRITUS-SEMANTICS CAVEAT (NOT a regression):** detritus 156 (post-D1) → 157 (post-D2), +1. This is D2's `counter-update` joining as a **typed-but-not-yet-reached NODE** (a `firm` node honestly not forced live). The `detritus` total mixes THREE dispositions — genuine-garbage + typed-non-node (reference-only, no liveness by construction) + typed-but-unreached-NODE — so it is a MISLEADING single-cycle health signal. **0 nodes lost reachability** (`reachable` climbed 95→102). One of the carried findings recommends a linter `totals`-split to separate these three signals so the GC health read is unambiguous. **Do not misread the detritus number as backsliding.**

**rank_violations trend: 22 (c094) → 1 (c095) → 0 (c096) → … → 0 (c106) → 0 (c107) → 0 (c108). reachable trend across the WAVE-3/grounding campaign: 36 (c105-end, pre-linter-fix) → 81 (batch-33 meta-phase) → 88 (c106) → 95 (c107) → 102 (c108)** — the c105 linter-fix made it measurable, then c106/c107/c108 grounded the record + BC + divfree + lowering-chain nodes.

**On the high `untyped`/`detritus` mass:** informational, NOT a block (per step-5b — the as-yet-untyped pre-P1 tail + the typed-non-node reference-only pages + the typed-but-unreached nodes). Only a *new* rank violation or a *newly*-orphaned node gates; neither occurred.

### Counts

No firm-count status flips this cycle — D1's 8 lowering-chain edge typings + D2's 1 node typing are all frontmatter-only (no new authored vocabulary, no node promotion). Layer tallies unchanged: L1 firm 33 main / 40 grand · L4 firm 21 main / 25 grand · L4>L3 11 · L3 17+4po · L3>L2 6 · L2 21+1pc · L2>L1 11 · L0 22 · concepts 34 · methodology 4 · feature spine 11 firm / 1 seed · L4 reduce-family 4 verbs ALL firm. SLICE CORPUS: 0. The measurable movement is on the reachability axis (`reachable` 95→102, 7 lowering-chain nodes grounded) + the typed-edge axis (`untyped` 61→60).

### Process

retroactive-budget global = 0 (per-row sum across both staging rows = 0, far under the ≥4 block threshold). Per-report safety-net gates all PASS/N/A across both rows (retroactive-budget, concept_writes-on-existing-slug, forward-edge-without-surface, edge-label/prose-mismatch, H1-reuses-page-heading, append-on-missing-slug, variant-axis-missing, SUMMARY-registration, alpha-position, rank-well-foundedness). 0 implied-component stubs created. 2/2 staging rows == 2 dispatched-ready reports (the cycle-018 staging-completeness gap did NOT recur — 89th consecutive clean staging / 103rd consecutive clean split-integrator cycle). Per-report YAML round-trip PASS on all 9 edited files. `scaffolding/priorities.md` + `scaffolding/open-questions.md` + `scaffolding/integrator-signals.md` + `scaffolding/roadmap.md` + `scaffolding/cycle-record.jsonl` + `log/` all modified, committed atomically with the artifact. NO `.claude/agents/` changes → no session-restart concern from finalize.

### Signals for the batch-34 meta-phase (fires NOW, after this finalize)

- **THE CAMPAIGN ARC (for the batch aggregate):** the GRADED-STACK P1 typed-edge campaign LED meta-batch-34. The reachability axis was made MEASURABLE by the batch-33 meta-phase's linter block-mapping-edge parser fix, then GROUNDED across the three primary cycles — `reachable` **36 → 81 (batch-33 meta) → 88 (c106) → 95 (c107) → 102 (c108)**: c106 typed the WAVE-3 op-chapters + authored `krylov-step` (5 records rescued); c107 grounded the absorbed BC/divfree clusters (the 6th record + divfree leg); c108 grounded the L1/L0 lowering-chain homes. `rank_violations` HELD 0 throughout; `unresolved_depends_on_targets` 21→0 (c106). The record set + BC + divfree + lowering-chain homes are all root-reachable.
- **Findings ROUTED for the meta-phase to weigh (carried in `scaffolding/priorities.md` + the OQ ledger):**
  - `l2-l1-theme-cohort-reachability-gap` (c108 D1) — the residual ~10 L2-L1 lowering themes that stay detritus by the SAME root cause this cycle's pass addressed (operator→operator-not-operator→theme dead-ending). A bounded next-tranche typing target.
  - `lowering-theme-reachability-vs-well-foundedness-scheme-clarification` (c108 D1) — a one-line note to `graded-stack-scheme.md` §5 disambiguating lowering-theme reachability from well-foundedness.
  - **the linter `totals`-split recommendation** (carried from c107, sharpened c108) — separate the `detritus` total into genuine-garbage vs typed-non-node vs typed-but-unreached-node so the GC health read is unambiguous (today it requires a manual git-stash set-diff).
  - **the grounding-disposition codification** — codify grounding-vs-route-vs-baseline-exception (when to ground an absorbed/future dependency vs file detritus vs baseline-exception) into the role-specs + `METHODOLOGY-GRADED-STACK.md`.
  - **the latent linter-reader bug** (`graded-stack-lint-block-mapping-misparse-on-legacy-edge-prose-colon`, c106 D5) — still latent; a fix-reader-vs-rely-on-migration decision.
- **The clean-gated forward-vocabulary frontier remains substantially exhausted** — the measurable movement this whole batch is on the reachability/typed-edge axes, not new authored vocabulary. The `promotion_frontier: 8` members are ALL obstruction-/demand-gated (not clean picks).
- **The integrator-signals chain is unbroken** (the c107 finalize appended; this c108 finalize appends its section) — recorded against the c106-noted "integrator-signals stale at cycle-019" planner false-read.

— written by `integrator-finalize` (split integrator-per-report ×2 + finalize ×1).
