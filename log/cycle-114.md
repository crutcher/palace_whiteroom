## 2026-06-06 cycle-114 — 2 reports applied clean — 109th consecutive cycle under split integrator — **POSITION 3/3 OF META-BATCH-36, THE BATCH-CLOSING cycle (cycles 112/113/114; the batch-36 meta-phase fires NEXT-AFTER THIS finalize, aggregating 112/113/114)** — TWO FRONTMATTER-ONLY grounding dispatches landing the c113-routed `L1/weak_form_term` GROUNDABLE candidate + a faithful L1-op→theme sweep (D1, layer-intro-author `fe-assemble-cluster-ground`: canonical `edges:` on `L1/fe_assemble` + `L1/fe_space` → +5 reachable [3 direct nodes `weak_form_term`/`fe_space`/`fe_collection` + 2 transitive L1>L0 themes]; D2, layer-intro-author `l1-theme-grounding-sweep`: `L1/{dot,nrm2,scal}` op→theme edge `reference`→`depends-on(kind:lowers-to)` → +3 reachable) → TRUE CUMULATIVE `reachable` 124→132 (+8), `detritus` 135→127 (−8), STRONGER GARBAGE SIGNAL 24→23 (−1); `rank_violations` HELD 0, `unresolved` HELD 0, `untyped` HELD 60 — pass

> Note: this file supersedes a stale pre-redirect-era `cycle-114` log entry (old slice-vertical numbering, 2026-05-26, `sideways cg,gmres`); the cycle counter collided across the 2026-05-26 structural redirect. The current cycle-114 is the batch-36 closing cycle under the layered-stack flow. Old content recoverable from git history.

---

**POSITION 3/3 OF META-BATCH-36 — THE BATCH-CLOSING cycle** (3:1 cadence; cycles 112/113/114; the cycle counter does NOT reset across batch boundaries; **the batch-36 meta-phase fires NEXT-AFTER THIS finalize, as a SEPARATE dispatch aggregating 112/113/114** — this finalize ran NO meta-phase housekeeping). Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) + the 2026-06-02/2026-06-03 user directives + the 2026-06-04 GRADED RESOLUTION LADDER + FEATURE-ROOT REACHABILITY user directive (`METHODOLOGY-GRADED-STACK.md`) + the 2026-06-05 GROUND-don't-remove grounding directive + the 2026-06-06 named-shape-groups L4-calculus notation (codified batch-35 meta-phase). NO session restart since c112 (mid-batch, no `.claude/agents/` change) → context continuity holds across the batch.

### Headline — the c113-routed `L1/weak_form_term` GROUNDABLE candidate LANDED (the FE-assemble cluster) + the faithful L1-op→theme sweep for dot/nrm2/scal (`reachable` 124→132, +8)

c113's audit-first pivot produced the c114 work plan: (1) land the one confirmed GROUNDABLE candidate `L1/weak_form_term` (via grounding the whole FE-assemble cluster), and (2) execute the high-fan-out systematic L1-op→theme grounding sweep flagged by the c113 `stale-pre-c108-rank-direction-error-prose-on-L1-ops` finding (the reachable-theme subset). Both landed frontmatter-only — pure Axis-2 (reachability) movement, no new authored vocabulary, no node promotion, the firm histogram HELD.

### What landed

- **D1 (layer-intro-author) — `fe-assemble-cluster-ground`.** Two frontmatter-only graded-stack edge-grounding migrations onto the batch-33-ratified scheme:
  - `book/src/L1/fe_assemble.md` — migrated off legacy `firmness: firm` + `lowers_to:` + empty `depends_on: []` to `rank: firm` + typed `edges:`: `composes`→{`weak_form_term` (the `[WeakFormTerm]` fold element-type), `fe_space` (the `space:` input)} + preserved the existing `lowers_to` as a `lowers-to` `depends-on` edge to `fe-operator-assemble-mutation-rotation` + a `reference` to `bilinear-form`.
  - `book/src/L1/fe_space.md` — authored an `edges:` block from-scratch (bare `status: firm`, NO edges before): `rank: firm` + `composes`→`fe_collection` + `cites-evidence`→`palace/fem/fespace.hpp:67-75` (the L0 backing) + `lowers-to`→`fe-space-construction-rotation` + 4 `reference` (`fe_assemble`, `weak_form_term`, `eliminate_essential_bc`, `eliminate_rhs`).
  - **+5 reachable** exactly as the dispatch predicted: 3 direct nodes (`weak_form_term`, `fe_space`, `fe_collection`) + 2 transitive L1>L0 themes (`fe-assemble-libceed-boundary-obstruction` via `weak_form_term`'s `lowers_to`, `fe-space-construction-rotation` via `fe_space`'s new `lowers-to`). `weak_form_term` flips out of STRONGER GARBAGE SIGNAL (−1). Promoted 2 OQs (`fe_collection-own-constituents-future-pass`, `fe_space-deferred-siblings-still-ungrounded`).
- **D2 (layer-intro-author) — `l1-theme-grounding-sweep`.** Frontmatter-only edge upgrade on three firm L1 BLAS-1 operators — `dot`, `nrm2`, `scal` — promoting each operator's edge to its L1>L0 mutation-rotation theme from `reference` (navigational) → `depends-on (kind: lowers-to)`, per the c108 §5 L1-op→theme grounding convention (the exact c113 D2 `set_subvector_zero` move). Each op is reachable (verified via `--show-inbound`) but its lowering theme was still garbage (zero inbound `depends-on`). Routing liveness down the `lowers-to` edge flips the three themes reachable. **+3 reachable** (the `dot`/`nrm2`/`scal` mutation-rotation themes). The three ops carried NO stale pre-c108 prose → a clean upgrade (no prose correction needed, unlike c113's `set_subvector_zero`). Promoted OQ `l1l0-theme-grounding-projection-correction`. Preserved each op's existing kept edges (`concepts/dot`; `depends-on: L1/dot` on `nrm2`; `reference: L1/axpby` on `scal`).
- **TRUE CUMULATIVE (both applied, re-measured by integrator-finalize on the landed tree):** `reachable` 124→132 (+8: D1 +5, D2 +3 — disjoint, confirmed exactly); `detritus` 135→127 (−8); STRONGER GARBAGE SIGNAL 24→23 (−1, `weak_form_term`); `rank_violations` HELD 0; `unresolved_depends_on_targets` HELD 0; `untyped` HELD 60; firm histogram HELD. Here the per-dispatch isolation deltas ARE additive (disjoint file sets, disjoint flipped nodes); 132 is the re-measured cumulative figure.

### Measurement-framing correction (OQ `l1l0-theme-grounding-projection-correction`)

Flipping an EDGE-UNTYPED-DETRITUS theme reachable drops `detritus_no_typed_edges_pre_p1_artifact`, NOT the STRONGER subset (`detritus_with_typed_edges_stronger_signal`). D2's −3 cleared edge-untyped detritus only (STRONGER held for D2's contribution); D1's −5 was mixed (`weak_form_term` was STRONGER → −1 STRONGER, the other 4 edge-untyped). Net STRONGER 24→23 came entirely from `weak_form_term`. Worth codifying so future planner reachability projections distinguish the two detritus subsets accurately (a c113-era dispatch brief had projected STRONGER −3 for an edge-untyped flip — the faithful result holds STRONGER).

### Build

`cargo make book` (mdbook + linkcheck2) EXIT 0. All 5 touched `book/` files are frontmatter-only edits with on-disk edge targets → linkcheck2-clean; no new file → no SUMMARY/index insert. Only the pre-existing benign `Potential incomplete link` WARNs (markdown-table / KaTeX false-positives, NOT link errors) + the long-standing KaTeX render WARNs on type-signature angle-bracket / lambda-binder constructs (content kept, not on this cycle's files). **NO finalize build-repair needed.**

### Step-5b — graded-stack linters (the build-gate companion, ran on the landed tree)

**`rank_violations: 0`** (`[]`; baseline fully discharged c096 so ANY violation would be NEW and BLOCK — there are NONE, **GATE PASSES**; all D1 source nodes `rank: firm` with all `composes`/`lowers-to` targets on-disk ≥-firm, all 3 D2 edges firm-op(rank 3) → firm-theme(rank 3)) + **NO newly-orphaned node** (`reachable` CLIMBED 124→132) + **`unresolved_depends_on_targets: 0`** (HELD). Totals on the landed tree: `files=355`, `typed=295`, **`untyped=60`** (HELD), `roots=36`, **`reachable=132`** (was 124, +8), `rank_violations=0`, `unresolved_depends_on_targets=0`, `promotion_frontier=8`, **`detritus=127`** (`detritus_no_typed_edges_pre_p1_artifact=104`, `detritus_with_typed_edges_stronger_signal=23`, `expected_unreachable_outside_dag=44`).

**rank_violations trend: 22 (c094) → 1 (c095) → 0 (c096) → … → 0 (c112) → 0 (c113) → 0 (c114). reachable trend across the campaign: 36 (c105-end, pre-linter-fix) → 81 (batch-33 meta-phase) → 88 (c106) → 95 (c107) → 102 (c108) → 107 (c109) → 119 (c110) → 122 (c111) → 123 (c112) → 124 (c113) → 132 (c114).**

**BATCH-36 ARC: reachable 122→132 over c112/c113/c114 (+10), rank_violations HELD 0 throughout.** c112 typed the L3 reduce/orthogonalize/linear_combination mid-nodes (+1 via the faithful `L3/nrm2 → L2/nrm2` RE5 transitive-ground); c113 grounded `set_subvector_zero`'s theme + audited the STRONGER GARBAGE SIGNAL into the RE6-RE8 baseline-exception recommendations; c114 grounded the FE-assemble cluster (+5) + the dot/nrm2/scal themes (+3). The entire batch was frontmatter-only typed-edge GROUNDING work — Axis-2 (reachability) movement, NOT new authored vocabulary; the firm histogram + `untyped`-60 HELD throughout.

**On the high `untyped`/`detritus` mass:** informational, NOT a block (per step-5b — the as-yet-untyped pre-P1 tail + the typed-non-node reference-only pages + the typed-but-unreached nodes awaiting the RE6-RE8 ratification). Only a *new* rank violation or a *newly*-orphaned node gates; neither occurred.

### Counts

No firm-count status flips this cycle — both dispatches are frontmatter-only edge-grounding migrations (no new authored vocabulary, no node promotion). Layer tallies unchanged: L1 firm 33 main / 40 grand, L4 firm 21 main / 25 grand, L4>L3 11, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 34, methodology 4, feature spine 11 firm / 1 seed, L4 reduce-family 4 verbs ALL firm. SLICE CORPUS: 0 (corpus fully lifted + deleted across c097/098/099). The measurable movement is entirely on the reachability axis (`reachable` 124→132, the FE-assemble cluster + dot/nrm2/scal themes grounded).

### Process

Retroactive-budget global = 0. Per-report safety-net gates all PASS/N/A across both rows (rank-well-foundedness 0, edge-label/prose-mismatch 0, YAML round-trip 0, SUMMARY-registration N/A [all slugs pre-exist], forward-edge-without-surface 0, append-on-missing-slug 0, variant-axis-missing 0). 0 implied-component stubs created. 2/2 staging rows == 2 dispatched-ready reports — the cycle-018 staging-completeness gap did NOT recur (95th consecutive clean staging / 109th consecutive clean split-integrator cycle). **Both reports applied clean** — no repair-phase warnings carried to finalize. **CITECHECK NOTE (non-blocking):** the per-report integrators noted AMBIG citecheck hits (D1: 5, D2: 3) — ALL on the reports' OWN derivation-prose bare-basename slug-citations (basenames now collide across sibling layers, e.g. `fe_assemble.md`/`dot.md`); NONE in the landed frontmatter (the landed `edges:` targets use full slug paths that resolve, and D1's one landed L0 citation `palace/fem/fespace.hpp:67-75` is fully-pathed and critic-verified). Recorded not repaired — a report-prose path-hygiene nit, not a landed-citation defect. 3 OQs appended to the ledger this cycle (append-only between meta-phases; the meta-phase unifies/closes). `scaffolding/{open-questions,roadmap,integrator-signals,cycle-record}.md/.jsonl` + `log/` committed atomically with both `book/` edits + both consumed-report `integrated_at` touches. NO `.claude/agents/` changes → no session-restart concern FROM THIS FINALIZE (but the batch-36 meta-phase that fires next MAY enact role-spec changes requiring a restart).

### Signals for the batch-36 meta-phase (fires NEXT-AFTER this finalize, aggregating 112/113/114) — THE BATCH CLOSES HERE; the FULL CARRY SET

- **BATCH-36 ARC HEADLINE:** reachable 122→132 over c112/c113/c114 (+10), `rank_violations` HELD 0 throughout. The whole batch was frontmatter-only Axis-2 grounding; firm histogram + `untyped`-60 HELD.
- **THE RE6-RE8 BASELINE-EXCEPTION RATIFICATION BATCH (the meta-phase's MAIN job)** — from c113 D1's audit, needs formal ratification into `scaffolding/graded-stack-baseline-exceptions.md`: **RE6** (axpy-family `L2/{axpy,axpby,axpbypcz}` + `L3/{axpy,axpby,axpbypcz}` absorbed into the reachable `linear_combination` combinator, 6 nodes — **MUST include the arity-1 `scal` members**); **RE7** (the diagonal-preconditioner cluster `L2/elementwise_product` + `L3/elementwise_product` + `L3/assemble-diagonal` + `L3/jacobi-smoother`, 4 nodes — **RE7-vs-RE1-extension is an id-split-or-merge judgment call**); **RE8** (the unconsumed L3 iteration-views `L3/fold_solve` + `L3/krylov-step`, 2 nodes — **RE8-vs-RE2-extension is an id-split-or-merge judgment call**). The corrected enumeration is 13 = 1 GROUNDABLE (now LANDED c114) + 12 baseline-exception (6 RE6 + 4 RE7 + 2 RE8).
- **THE CANDIDATE FRICTION `stale-pre-c108-rank-direction-error-prose-on-L1-ops`** — mostly resolved: c113 fixed the `set_subvector_zero` prose; c114's dot/nrm2/scal had NO stale prose (clean upgrade). Remaining question: are there more L1 ops with stale rank-direction prose? Meta-phase to assess ledger-vs-close.
- **THE HYGIENE-ONLY L1-op→theme EXCLUSIONS** (`normalize` / `reciprocal` / `elementwise_product`) — these are RE5 garbage ops; their L1>L0 themes CANNOT be flipped reachable by op-grounding (the op itself is unreachable), so the c113-flagged systematic sweep does NOT extend to them. For the meta-phase's RE-handling (they belong under RE5 / RE7, not a new grounding tranche).
- **THE MEASUREMENT-FRAMING CLARIFICATION (`l1l0-theme-grounding-projection-correction`)** — flipping an edge-untyped-detritus theme reachable drops edge-untyped detritus, NOT STRONGER. Worth codifying so future planner projections are accurate.
- **c114 FUTURE-PASS OQs:** `fe_collection-own-constituents-future-pass` (now-reachable `fe_collection`'s own constituents are pre-scheme); `fe_space-deferred-siblings-still-ungrounded` (`essential_dofs` / `fe_space_hierarchy` / de-Rham interpolator); plus the carried-open c112 OQs (`re2-shadows-orthogonalize-variant-split-theme` etc.).
