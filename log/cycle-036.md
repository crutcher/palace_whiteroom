## 2026-05-31 cycle-036 — THIRD/FINAL primary cycle of meta-batch-10 — 2 reports applied (1 substantive firm-L1 + firm-L1>L0 landing — first instance of a new constructed-operator gate, third firm `nested-constructed-operator-gate` instance + 1 observation-only L3-cohort-growth audit that SETTLES the long-standing deferred audit at `book/src/L3/index.md:38`) — thirty-first consecutive cycle under split integrator — **the batch-10 meta-phase fires AFTER this cycle-036 finalize, NOT this cycle**

- **Phases fired**: plan (cycle-planner) → dispatch (2× parallel: harvester / cross-layer-cross-cutter) → critique (2× critic) → repair (2× repairer; D1 hit the recurrence-3 fence-truncation friction, caught by the cycle-021 detection guard and repaired by the cycle-024 promoted skill) → integrate (per-report ×2 serial → finalize this commit) → meta (NEXT — fires after this finalize).
- **Batch-10 cadence position**: third/final primary cycle (c034/c035/c036). The batch-10 meta-phase fires AFTER this commit; cycle-037 opens batch-11.

- **Substantive landed**:
  - `book/src/L1/floquet-correction.md` — **new firm L1 chapter** (402 lines; sixth constructed-operator gate at L1 after `ksp_solve`, `eigsolve`, `chebyshev-smoother`, `divfree-projector`, `jacobi-smoother`; closes the c035 D3-surfaced driven-solver Floquet-periodic coverage gap; firm-on-positive-structure; structurally isomorphic to but strictly thinner than `divfree-projector` — no boundary-zeroing, no gradient correction, no empty-boundary nullspace pin; gate transitively three-deep `floquet → ksp → jacobi-smoother`; element-type scope-out (only `<ComplexVector>` instantiated — the first L1 gate with a deliberately-narrowed element-type scope)).
  - `book/src/L1-L0/floquet-correction-mutation-rotation.md` — **new firm L1>L0 theme** (525 lines; 4 sub-patterns A/B/C/D — Mult+AddMult apply surface + RHS-buffer-recycling closure construction + element-type scope-out; AddMult-as-axpy non-law records a load-bearing buffer-economy fusion).
  - `book/src/L1/index.md` — Firm count 25→26 + vocabulary cohort paragraph + dep-map row.
  - `book/src/L1-L0/index.md` — theme-list row appended.
  - `book/src/SUMMARY.md` — both new chapters registered.
  - `book/src/concepts/nested-constructed-operator-gate.md` — Firm instances 2→3; second three-deep transitive nesting chain (`floquet → ksp → jacobi-smoother`) recorded alongside the existing `eigsolve → divfree-projector → ksp_solve` chain — direct evidence the nested-gate pattern is load-bearing across multiple pipelines (not eigsolve-incidental).
  - `book/src/L3/index.md:38` — long-standing deferred-audit bullet replaced with the c036-settled verdict — 6 firm (A) identity-in-form L3-backfill candidates [`assemble-diagonal`, `jacobi-smoother`, `reciprocal`, `elementwise_product`, `normalize`, `divfree-projector`] + 2 L1-promotion-gated (A) [`matrix-weighted-norm`, `bilinear-form`] + 3 substantive (B) [`orthogonalize`, `chebyshev-smoother`, `apply_nonlinear_pencil`] + 7-operator (C) negative-list that should NOT be re-proposed for L3 [`lu_solve`, `back_solve`, `ls-update-column`, 4 NLEPS atoms — disqualified by small-dense coordinate-space axis]. The (C) negative-list is the new anti-recurrence data feed for the cycle-033-promoted `verify-dispatch-scope-not-already-discharged` skill.

- **Friction observed**:
  - **`firm-chapter-body-authored-outside-proposed-changes-fence` RECURRENCE-3** (cycles 019/021/024/036) — the D1 harvester emitted the floquet firm body with an inner `text` fence inside the proposed-changes block that broke the outer fence parity, truncating the apparent body. **Caught**: critic detection guard `proposed-changes-fence-encloses-full-body-guard` (promoted cycle-021). **Repaired**: per-report integrator applied the cycle-024-promoted skill `convert-nested-fences-to-indented-code-in-proposed-changes-block` — the inner `text` fence in §Signature was converted to 4-space indent, the first proposed-changes block now encloses the FULL 402-line firm body. The detect-then-repair pipeline held; question for the batch-10 meta-phase: producer-side pre-emission prevention vs steady-state critic-detect+repair.
  - **`cycle-planner-stale-priorities-line-recruitment` SOFTER RECURRENCE in c036** (decreasing-severity pattern across batch-10) — c034 D3 was recurrence-1 (1 stale); c035 was recurrence-2 (2-of-3 stale, planner CLAIMED-but-didn't-verify); c036 cycle-planner CORRECTLY REJECTED the stale batch-6 audit candidates WITH inline evidence (partial recovery from c035), but the D2 pick still required orchestrator override (reframe-needed) — the planner proposed `assemble-diagonal` L3 as a "quick identity-in-form backfill" without an upfront audit, which is unsound framing for an operator-to-data primitive that carries a representation-dependent caveat. **NOTABLY** the audit CONFIRMED `assemble-diagonal` IS (A) identity-in-form — the planner's INSTINCT was correct, the METHOD (reflexive harvest vs audit-first) was wrong. The c034/c035/c036 recurrence pattern is the headline batch-10 meta-phase agenda; the batch-9 ENFORCEMENT bullet (codified in `.claude/agents/cycle-planner.md`) is demonstrably insufficient at the planner side. Repair-path candidates for the batch-10 meta-phase: (a) migrate `verify-dispatch-scope-not-already-discharged` from producer-side to PLANNER-side; (b) mechanical pre-dispatch gate; (c) escalate the planner to opus.
  - **Path-hygiene auto-fix in D1** (recurrence-1 of a `concepts→L1` cross-layer-link convention drift): the D1 harvester's added content in `book/src/concepts/nested-constructed-operator-gate.md` used `./jacobi-smoother.md` (concept-relative) instead of `../L1/jacobi-smoother.md` (cross-layer) at 2 sites. The cross-reference-integrity critic check missed this specific dead-link variant because the harvester was focused on the fence repair. Auto-fixed inline by the per-report integrator via `Edit replace_all`. Recurrence-1 of `concepts→L1` cross-layer-link convention drift; may warrant a friction-ledger entry if it recurs.

- **Critic findings**: clean across both reports; the fence-truncation defect was caught by the detection guard skill at critic time (and repaired by the cycle-024 promoted skill at repair time — the detect/repair pipeline held).

- **Repair outcomes**: D1 fence-repair applied cleanly (skill `convert-nested-fences-to-indented-code-in-proposed-changes-block`); D2 no repairs needed.

- **All reports `overall_status: ready`**.

- **Safety-net gates**: retroactive-budget global = 0; staging-completeness 2/2 rows == 2 dispatched-ready reports (the c018 staging-completeness gap did NOT recur for the SEVENTEENTH consecutive cycle); 0 implied-component stubs created; 1 path-hygiene auto-fix; 1 proposed-changes fence-truncation repair (recurrence-3, both detected + repaired by promoted skills); zero build-repairs.

- **Open questions promoted to ledger**: 3 NEW (`floquet-correction-real-vector-instantiation-dead-code` — trigger-gated on upstream Palace PR adding `<Vector>` instantiation; `floquet-corrector-addmult-aliasing-applicability-audit` — trigger-gated on a lowering-verifier dispatch on the theme; `l3-cohort-growth-audit-c036-verdict` — records the (A)/(B)/(C) classification, supersedes 2 predecessor OQs).
- **OQs closed/resolved this cycle**: 2 (`floquet-correction-l1-gate-harvest` — resolved by D1 firm L1+L1>L0 landings; `nested-constructed-operator-gate-second-three-deep-chain-codified` — resolved by D1 concept-page upgrade closing the tracker).

- **Counts after**: L1 firm **25→26** (+`floquet-correction`); L1>L0 firm themes **23→24** (+`floquet-correction-mutation-rotation`); concept `nested-constructed-operator-gate` firm instances **2→3**; L2/L3/L4/L0 unchanged; Phase-1 removals unchanged (9/10). The cycle-record `counts_after` block is the audit-trail of record.

- **Build**: `cargo make book` exit 0 in ~90s; only pre-existing KaTeX `Potential incomplete link` false-positives in `design/l4_calculus.md` + the across-corpus pre-existing warns + the `<ComplexVector>` template-in-prose warns added in the new floquet chapter (per the existing convention, NOT new breakage); linkcheck2 backend clean. Zero build-repairs.

- **Reports**:
  - `reports/2026-05-31T200500Z-harvester-floquet-correction-l1/` — CYCLE.md, META.md.
  - `reports/2026-05-31T200500Z-cross-layer-cross-cutter-l3-cohort-growth-audit/` — CYCLE.md, META.md.
  - `reports/cycle-036-integrator-staging/STAGING.md` — per-report integrator staging log (2 rows).
  - `reports/2026-05-31T230000Z-integrator-finalize-cycle-36/CYCLE.md` — batch report (this commit).
