## 2026-06-06 cycle-109 — 1 report applied clean — 104th consecutive cycle under split integrator — **POSITION 1/3 OF META-BATCH-35, the OPENING cycle (cycles 109/110/111; the batch-35 meta-phase fires NEXT-AFTER cycle-111's finalize, aggregating 109/110/111)** — the batch-34-meta-migrated LEAD `graded-stack-l2-l1-theme-cohort-grounding` landed as a bounded one-edge-per-theme GROUNDING pass over the L2-L1 lowering-theme cohort (FAITHFUL-PATH-OR-FINDING): 4 on-spine Group-A themes GROUNDED (each flipped OUT of `[garbage?]`) + 1 cheap faithful edge-lay (`inner_product`, non-flipping) + 5 off-spine Group-B themes ROUTED as a structured finding → `reachable` 102→107 (+5; exceeds the predicted +4 because authoring `L2/krylov-step`'s `edges:` block from scratch also makes `L2/krylov-step` itself a typed-and-reachable node), STRONGER GARBAGE SIGNAL 35→34, `detritus` 157→152 (−5); `rank_violations` HELD 0, `untyped` HELD 60 — pass

> **Note:** this file (`cycle-109.md`) is the current-run, structural-redirect-era cycle-109 log (it overwrites the stale pre-redirect slice-vertical-era `cycle-109.md`, recoverable from git history). The current-run numbering does NOT reset across batch boundaries.

**POSITION 1/3 OF META-BATCH-35 — THE OPENING CYCLE** (3:1 cadence; cycles 109/110/111; the cycle counter does NOT reset across batch boundaries; **the batch-35 meta-phase fires AFTER cycle-111's finalize, as a SEPARATE dispatch aggregating 109/110/111**). Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) + the 2026-06-02/2026-06-03 user directives + the 2026-06-04 GRADED RESOLUTION LADDER + FEATURE-ROOT REACHABILITY user directive (`METHODOLOGY-GRADED-STACK.md`) + the 2026-06-05 GROUND-don't-remove grounding directive (codified into `METHODOLOGY-GRADED-STACK.md` §2f/§8 by the batch-34 meta-phase). Session restarted after the batch-34 meta-phase (it enacted `.claude/agents/*` edits; resume-notes `scaffolding/cycle-109-resume-notes.md`). c108 landed `fd5fabd`; the batch-34 meta-phase landed `4c26325`.

### Headline — the L2-L1 lowering-theme cohort gap is GROUNDED for the 4 on-spine themes; the operator→theme edge-convention fix flips them reachable (`reachable` 102→107)

The batch-34 meta-phase MIGRATED `l2-l1-theme-cohort-reachability-gap` into the plan as the batch-35 LEAD `graded-stack-l2-l1-theme-cohort-grounding`. The root cause: the ~10 L2-L1 lowering themes stayed `[garbage?]` because the `lowers-to` edge convention points **operator→operator** and never **operator→theme**, so a lowering theme has no inbound `depends-on` from a reachable node. The faithful fix (mirroring the c108 `L2/divfree-projector` block-mapping precedent): add a `lowers-to`-kind `depends-on` edge `L2/<op> → L2-L1/<theme>` on each host op.

D1 applied this as a bounded one-edge-per-theme GROUNDING pass under the §(g) GROUND-don't-remove disposition, as **FAITHFUL-PATH-OR-FINDING**. On-disk verification confirmed the planner's split EXACTLY:
- **4 Group-A themes** (host L2 op itself reachable) → GROUNDED this cycle, all 4 flipped OUT of `[garbage?]`.
- **1 cheap faithful edge-lay** (`inner_product`) → edge typed correctly but does NOT flip (host `L2/inner_product` is itself `[GARBAGE*]`) — laid for when the reduce cohort grounds.
- **5 Group-B themes** (host L2 op itself unreachable) → ROUTED as a structured finding (OQ filed). The faithful `lowers-to` is real for all 5; the one-edge fix cannot reach them.
- **`deflate` / `deflate-composition-lowering`** → NOT TOUCHED (demand-gated FRONTIER member, STOP-PROPOSING negative list).

`reachable` 102→107 (+5; **exceeds** the predicted +4 because authoring `L2/krylov-step`'s `edges:` block from scratch also makes `L2/krylov-step` itself a typed-and-reachable node, on top of its 4 sibling theme flips), `rank_violations` HELD 0, `untyped` HELD 60, `detritus` 157→152 (−5; STRONGER GARBAGE SIGNAL 35→34, edge-untyped artifact 122→118). All 5 edits are FRONTMATTER-ONLY.

### What landed

- **D1 (layer-intro-author, build-relevant, MEDIUM) — THE LEAD: the L2-L1 lowering-theme-cohort grounding pass.** 5 frontmatter-only `edges:` edits, each `lowers-to` confirmed REAL from the theme/host chapter prose before typing:
  - `book/src/L2/eigsolve.md` — ADD `depends-on lowers-to → L2-L1/eigsolve-spectral-transform-composition` (theme prose `eigsolve.md:171`). Group-A → FLIPPED.
  - `book/src/L2/ksp_solve.md` — ADD `depends-on lowers-to → L2-L1/ksp-solve-outer-driver-unfold` (the canonical L2↔L1 outer-driver edge). Group-A → FLIPPED.
  - `book/src/L2/krylov-step.md` — AUTHOR `edges:` frontmatter FROM SCRATCH (the file had no frontmatter): `rank: firm`, 7 firm L1 leaves `depends-on` (the chapter's OWN §Dependencies, `krylov-step.md:96`: `apply_linop, axpy, axpby, axpbypcz, dot, nrm2, scal`) + `lowers-to → L2-L1/krylov-step-kernel-defusion` + 10 concept `reference`s. Group-A → FLIPPED; **`L2/krylov-step` itself becomes a typed-and-reachable node** (the +5-vs-+4 surplus).
  - `book/src/L2/linear_combination.md` — UPGRADE `reference`→`depends-on lowers-to → L2-L1/linear-combination-fold-specialization` (a navigational `reference` creates no reachability — the c106-D4 lesson). Group-A → FLIPPED.
  - `book/src/L2/inner_product.md` — UPGRADE `reference`→`depends-on lowers-to → L2-L1/inner-product-fold-specialization`. Cheap faithful edge-lay → does NOT flip (host `L2/inner_product` is itself `[GARBAGE*]`, its only inbound from the unreachable `L3/dot`/`L3/inner_product`); documented, not a failure.
  - **RESCUE MEASURED (live tree, `--show-inbound`):** `reachable` 102→107 (+5), `detritus` 157→152 (−5), STRONGER GARBAGE SIGNAL 35→34, `rank_violations` 0→0 HELD, `untyped` 60 HELD, 0 regressions. The 4 Group-A themes (`eigsolve-spectral-transform-composition ← L2/eigsolve`, `ksp-solve-outer-driver-unfold ← L2/ksp_solve`, `krylov-step-kernel-defusion ← L2/krylov-step`, `linear-combination-fold-specialization ← L2/linear_combination`) all show reachable inbound; the 5 Group-B themes + `deflate-composition-lowering` (FRONTIER, untouched) + `inner-product-fold-specialization` REMAIN `[garbage?]` (expected — the finding). Routed 1 finding to the batch-35 meta-phase: `l2-reduce-orthogonalize-cohort-itself-unreachable-blocks-theme-grounding` (the next-tranche blocker).

### Build

`cargo make book` (mdbook + linkcheck2) **EXIT 0**; **NO build-repair needed**. All 5 touched `book/` files are frontmatter-only edits; every edge target resolves to an on-disk file, so linkcheck2 is clean. The `Potential incomplete link` WARNs are the pre-existing benign markdown-table false-positives (bracketed prose like `cs[j]` / `[Time]` in dep-map cells), NOT link errors. No new file was created (every chapter pre-existed + was already SUMMARY-registered), so no SUMMARY/index insert was needed.

### Step-5b — graded-stack linters (the build-gate companion, ran on the landed tree)

**`rank_violations: 0`** (`[]`; baseline fully discharged c096 so ANY violation would be NEW and BLOCK — there are NONE, **GATE PASSES**; the 5 new `lowers-to depends-on` edges are firm→firm — the 4 host ops firm, theme targets + `apply_linop`/`dot`/`nrm2`/`scal` carry `rank: firm` — or firm→typed-no-rank: the 3 BLAS leaves `axpy`/`axpby`/`axpbypcz` carry no rank token, so their inbound edges hold the `rank(u)≤rank(v)` invariant **vacuously**, a no-rank target cannot register a violation) + **NO newly-orphaned node** (`reachable` CLIMBED 102→107) + **`unresolved_depends_on_targets: 0`** (HELD). Totals on the landed tree: `files=355`, `typed=295`, **`untyped=60`** (HELD), `roots=36`, **`reachable=107`** (was 102, +5), `rank_violations=0`, `unresolved_depends_on_targets=0`, `promotion_frontier=8`, **`detritus=152`** (`detritus_no_typed_edges_pre_p1_artifact=118`, `detritus_with_typed_edges_stronger_signal=34`, `expected_unreachable_outside_dag=44`). `rank_histogram: {firm:201, typed-no-rank:80, rough-in:5, partly-constructive:3, obstruction:2, partial-obstruction:4}`.

**rank_violations trend: 22 (c094) → 1 (c095) → 0 (c096) → … → 0 (c107) → 0 (c108) → 0 (c109). reachable trend across the campaign: 36 (c105-end, pre-linter-fix) → 81 (batch-33 meta-phase) → 88 (c106) → 95 (c107) → 102 (c108) → 107 (c109)** — c109 grounded the 4 on-spine L2-L1 themes + `L2/krylov-step`.

**On the high `untyped`/`detritus` mass:** informational, NOT a block (per step-5b — the as-yet-untyped pre-P1 tail + the typed-non-node reference-only pages + the typed-but-unreached nodes). Only a *new* rank violation or a *newly*-orphaned node gates; neither occurred. STRONGER GARBAGE SIGNAL (typed-but-unreached nodes) ticked 35→34 — the 4 grounded themes left the typed-detritus set.

### Counts

No firm-count status flips this cycle — D1's 5 edits are all frontmatter-only (no new authored vocabulary, no node promotion; `L2/krylov-step` was authored only at the frontmatter/`edges:` level — its body pre-existed). Layer tallies unchanged: L1 firm 33 main / 40 grand · L4 firm 21 main / 25 grand · L4>L3 11 · L3 17+4po · L3>L2 6 · L2 21+1pc · L2>L1 11 · L0 22 · concepts 34 · methodology 4 · feature spine 11 firm / 1 seed · L4 reduce-family 4 verbs ALL firm. SLICE CORPUS: 0. The measurable movement is entirely on the reachability axis (`reachable` 102→107, 4 themes + `L2/krylov-step` grounded, STRONGER GARBAGE SIGNAL 35→34).

### Process

retroactive-budget global = 0 (the single staging row contributes 0, far under the ≥4 block threshold). Per-report safety-net gates all PASS/N/A on the single row (retroactive-budget, concept_writes-on-existing-slug, forward-edge-without-surface, edge-label/prose-mismatch, H1-reuses-page-heading, append-on-missing-slug, variant-axis-missing, SUMMARY-registration, alpha-position, rank-well-foundedness). 0 implied-component stubs created. 1/1 staging rows == 1 dispatched-ready report (the cycle-018 staging-completeness gap did NOT recur — 90th consecutive clean staging / 104th consecutive clean split-integrator cycle). **REPAIR PHASE DID RUN this cycle:** one low-severity rank-invariant warning repaired in-place by rationale-only prose softening (the `edges:` blocks were left untouched). **CITECHECK NOTE (non-blocking):** 2 `AMBIG` citecheck warnings on report-PROSE bare-basenames (`eigsolve.md:171`, `krylov-step.md:96`) — NO prose-claim text lands in `book/` (frontmatter-only edits), so non-blocking, recorded-not-repaired. Per-report YAML round-trip PASS on all 5 edited files. `scaffolding/open-questions.md` + `scaffolding/integrator-signals.md` + `scaffolding/roadmap.md` + `scaffolding/cycle-record.jsonl` + `log/` all modified, committed atomically with the artifact. NO `.claude/agents/` changes → no session-restart concern from finalize.

### Signals for the batch-35 meta-phase (fires AFTER cycle-111's finalize, aggregating 109/110/111)

- **THE NEXT-TRANCHE BLOCKER (the Group-B finding, the headline carry):** `l2-reduce-orthogonalize-cohort-itself-unreachable-blocks-theme-grounding` — the 5 Group-B themes stay detritus because their **host L2 op is itself unreachable**, not because of the operator→theme edge convention (which this cycle's pass addressed for the 4 on-spine themes). The L2 reduce/orthogonalize/chebyshev ops are off-spine: their only inbound `depends-on` come from L3 ops that are THEMSELVES off-spine (`L3/dot`, `L3/inner_product`, `L3/chebyshev`, `L3/orthogonalize`, `L3/gram`, …). Grounding the L2 reduce/orthogonalize/chebyshev cohort (tracing up through the unreachable L3 reduce/iteration cohort — a larger, structurally-distinct pass, NOT the one-edge-per-theme LEAD shape) is the recommended next tranche; once an L2 reduce/orthogonalize op is itself reachable, its already-laid (`inner_product`) or to-be-laid (`chebyshev`/`gram`/`ils`/`orthogonalize`) theme edge flips it automatically. **NOTABLE for the meta-phase (the c107 disposition pattern):** `L2/inner_product` being `[GARBAGE*]` while it is a high-fan-out reduce-to-scalar combinator means the reduce cohort has NO live `depends-on` path from a driver column — either (a) a genuine spine gap to GROUND from a column (the reduce verbs are genuine constituents of the solve/postprocess columns — a residual-norm/energy/gram reduction a solve performs), or (b) an absorbed-below-column situation like the c107 BC/divfree clusters to track as a baseline-exception. Recommended bias per §(g): GROUND from the column where a faithful, honestly-typed path exists.
- `l1-blas-leaves-axpy-family-lack-rank-frontmatter` (the repairer filed this during repair) — the 3 high-fan-out L1 BLAS leaves (`axpy`/`axpby`/`axpbypcz`) lack `rank:`/`edges:` frontmatter (typed-no-rank), so inbound edges hold vacuously. A latent typing gap, not a current violation.
- **The clean-gated forward-vocabulary frontier remains substantially exhausted** — the measurable movement is on the reachability/typed-edge axes, not new authored vocabulary. The `promotion_frontier: 8` members are ALL obstruction-/demand-gated (not clean picks).
- **The integrator-signals chain is unbroken** (the c108 finalize appended; this c109 finalize appends its section).

— written by `integrator-finalize` (split integrator-per-report ×1 + finalize ×1).
