## 2026-06-05 cycle-099 — 3 reports applied clean — ninety-fourth consecutive cycle under split integrator — **POSITION 3/3 OF META-BATCH-31 (the LAST / BATCH-CLOSING primary cycle; the batch-31 meta-phase fires AFTER this cycle-099 finalize as a SEPARATE dispatch aggregating 097/098/099)** — THE GRADED-STACK P2 SLICE-DELETION CAMPAIGN **COMPLETE**: **the krylov trio `cg`/`gmres`/`arnoldi_step` DELETED + made fully unreachable, `book/src/spec/index.md` DELETED, the SUMMARY `# Phase 1 corpus` Part removed — `spec/slices/` is now EMPTY; the Phase-1 slice corpus is fully lifted + deleted (9 → 0 across cycles 097/098/099)** — pass

> **Note:** this file (`cycle-99.md`) is the current-run, structural-redirect-era cycle-099 log. The current-run numbering does NOT reset across batch boundaries.

**POSITION 3/3 OF META-BATCH-31** (3:1 cadence; cycles 097/098/099; the cycle counter does NOT reset across batch boundaries; **the batch-31 meta-phase fires AFTER this cycle-099 finalize as a SEPARATE dispatch aggregating 097/098/099** — this cycle-099 finalize runs NO meta-phase housekeeping). Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) + the 2026-06-02/2026-06-03 user directives + the **2026-06-04 GRADED RESOLUTION LADDER + FEATURE-ROOT REACHABILITY user directive** (full spec `METHODOLOGY-GRADED-STACK.md`; the campaign is priorities item-0) + the batch-30 meta-phase enactment (which GO'd the P2 slice-deletion tranche as the batch-31 LEAD and wired finalize step-5b).

### Headline — the GRADED-STACK P2 slice-deletion campaign is COMPLETE; the Phase-1 slice corpus is fully lifted + deleted (9 → 0)

The krylov trio `cg`/`gmres`/`arnoldi_step` — the deeply-interwoven ~30-anchor single-coordinated-owner sub-campaign that was the last remaining P2 work — landed this cycle as ONE coordinated 3-dispatch cycle. The 3 slice files are DELETED + made fully unreachable; `book/src/spec/index.md` is DELETED (corpus empty); the SUMMARY `# Phase 1 corpus` Part is removed; and the `spec/slices/` directory is now EMPTY. **The Phase-1 slice corpus is fully lifted + deleted (9 → 0 across cycles 097/098/099).** 3 of 3 dispatched-ready reports applied clean (3/3 staging rows == dispatched-ready — the cycle-018 staging-completeness gap did NOT recur for the EIGHTIETH consecutive clean staging / NINETY-FOURTH consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs.

### What landed

- **D1 (harvester, build-relevant) — the cg slice's one genuinely-unlifted datum ABSORBED.** The `cg.md:27-141` v0.5 first-iteration-unrolling worked example was absorbed into `book/src/L4/krylov-step.md` as a NEW firm `### Worked example — CG Form B (v0.5 first-iteration-unrolling)` subsection (CG-concrete `cg_first_step`/`cg_steady_step` typed bodies + `cg_solve` driver via `iterate_while_with_prev` + `forget_beta_prev` projection + v0.4↔v0.5 equivalence + pcg variant + L0-ground prose); re-anchored that file's dangling `cg.md:*` slice-pointers (§Semantics:82, §Status:152, §Evidence:171). **Rank stays 0** (firm-on-positive-structure — syntactic L4-self-rotation identities on the `CgSolver::Mult` read closure, no convergence-semantics claim gated; the L0 ground is a rank-exempt `cites-evidence` edge to `iterative.cpp:360-486`). Non-duplication-vs-concept-page verified: `concepts/first-iteration-unrolling.md` carries the ABSTRACT generic rotation; the inserted subsection lands ONLY the CG-concrete bodies + cross-references the concept page.
- **D2 (harvester, build-relevant) — the campaign completion.** Repointed ALL 31 inbound Class-A markdown links + the Class-B plain-text hub mentions across the whole krylov hub (L2/L3/L4-L3/L3-L2/L1/L1-L0/L0/concepts), reconciled the arnoldi plane-rotation end-bound (`incremental-least-squares-composition-lowering.md:112`, `iterative.cpp:73-118 → :73-109`), DELETED the 3 slice files `cg`/`gmres`/`arnoldi_step`, removed the SUMMARY `# Phase 1 corpus` section (header + Index parent link + 3 slice children), removed the now-dangling `introduction.md:23` `[Specification → Slice Status]` nav bullet, and DELETED `book/src/spec/index.md` (corpus empty; producer option-(a) full removal). The per-report integrator confirmed BOTH inbound-link sweeps return ZERO on the REAL applied tree.
- **D3 (lifter, no-op) — verified clean residue sweep.** The cycle-095 `bilinear-form`-firm-flip residue is confined to the krylov hub; `L2/index.md:89` re-confirmed NON-stale on disk (reads "`bilinear-form` (M-weighted member, **firm** — promoted cycle-095)"); zero genuinely-stale instances outside the hub; bookkeeping-only, no artifact edit.

### Build

`cargo make book` (mdbook + linkcheck2) **EXIT 0**. The 33 modified + 4 deleted (3 slices + `spec/index.md`) all co-landed link-safe. Finalize re-verified BOTH inbound-link sweeps → **ZERO** (`grep -rnE '\]\([^)]*slices/(cg|gmres|arnoldi_step)\.md'` → 0; `grep -rnoE '\]\([^)]*spec/index\.md\)'` → 0); the `# Phase 1 corpus` Part is cleanly gone from SUMMARY with no orphaned structure; the new `L4/krylov-step.md` Worked-example subsection builds; `spec/slices/` is EMPTY (`spec/` now holds only the empty `slices/` dir — harmless to git + mdBook). Only the pre-existing 101 benign KaTeX `Potential incomplete link` WARNs in `design/l4_calculus.md`. NO finalize build-repair needed.

### Step-5b — graded-stack linters (the build-gate companion, ran on the landed tree)

**`rank_violations: 0`** (baseline-exceptions fully discharged at c096 so ANY violation would be NEW and BLOCK — there are NONE, **GATE PASSES**) + **NO newly-orphaned node** (the 3 deleted slices were `reference`-class detritus leaves in the pre-P1 detritus mass, an INTENDED deletion not an orphaning — the second block condition also clears). **THE SLICE-NODE COUNT IN THE LINTER OUTPUT IS NOW ZERO** — the reachability-GC detritus from `spec/slices/*` has collapsed to zero. Totals: `files=350` (was 354 at c098 — the 3 deleted slices + `spec/index.md`), `typed=208`, `untyped=142` (was 146 — the 3 deleted slice nodes left the untyped tail), `roots=36`, `reachable=36`, `promotion_frontier=10`, `detritus=172`, `unresolved_depends_on_targets=35`. **rank_violations cycle-over-cycle trend: 22 (c094) → 1 (c095) → 0 (c096) → 0 (c097) → 0 (c098) → 0 (c099).** The remaining detritus/untyped mass is the as-yet-untyped pre-P1 tail (NOT slice nodes anymore) — informational, NOT a block.

### Counts

UNCHANGED (the slice corpus is Phase-1 raw material, NOT the layered-vocabulary deliverable; D1's absorb is a NEW firm subsection on the already-firm `L4/krylov-step.md` chapter, no node status flip): L1 firm 32 main / 39 grand · L4 firm 20 main / 24 grand · L4>L3 10 · L3 17+4po · L3>L2 6 · L2 21+1pc · L2>L1 11 · L0 22 · concepts 33 · methodology 4 · feature spine 11 FIRM / 1 seed · L4 reduce-family 4 verbs ALL firm. **SLICE CORPUS: 3 → 0** (`spec/slices/` EMPTY; `book/src/spec/` holds only the empty `slices/` dir).

### Process

retroactive-budget global = 0 (< 4 threshold); repair fired on D1 + D2 (mechanical — citation-validity + cross-reference-integrity; proposed-change blocks otherwise untouched); D3 clean (critic-set ready on an all-pass no-op); per-report safety-net gates all PASS/N/A; 0 implied-component stubs. **4 OQs recommended-CLOSE by per-report intake** (flagged for the batch-31 meta unify — per-report integrators have no OQ-close authority; finalize RECORDS the recommendation, the meta-phase closes): `cg-slice-27to141-fully-homed-clear-to-delete-and-evidence-pointer-residue-class-B` (D1), `krylov-trio-slice-corpus-3to0-campaign-complete-retire-carveout-and-skill` (D2, the CAMPAIGN-COMPLETE trigger), `krylov-trio-class-B-plaintext-mention-residue-batch31-cleanup` (D2), `bilinear-form-c095-residue-sweep-clean-noop-and-L2-index-89-confirmed-non-stale` (D3). `scaffolding/priorities.md` + `scaffolding/open-questions.md` also modified, committed atomically; NO `.claude/agents/` changes this cycle → no session-restart concern.

### CAMPAIGN-COMPLETE TRIGGER (recorded for the batch-31 meta-phase to ENACT)

The Phase-1 slice corpus is EMPTY → **retire the `annotated-and-retained` carve-out** (CLAUDE.md §Methodology-invariants "Phase 1 corpus reduces") **+ the skill `phase-1-slice-reduction-audit`** (per METHODOLOGY-GRADED-STACK.md §6/§7). These are CLAUDE.md + skills/ write-authority = a meta-phase enactment, NOT this finalize's job — RECORDED here, the meta-phase ENACTS. Also for the meta-phase: the residual ~50 plain-text Class-B slice-range provenance mentions (stale-but-harmless, meta-reviews-convention kind — dedicated batch-32 cleanup vs accept-as-historical); the `non-book-artifact-orphan-review` user directive (already in open-questions.md); + the OQs recommended-for-close across cycles 097/098/099 (the slice-deletion OQ closures, the arnoldi end-bound reconcile, the domain_energy_reduce residues, the orthog no-op-framing).

### Campaign state

**P0 DONE; P3 DONE; P1 DONE (rank_violations = 0); P2 slice-deletion DONE — the Phase-1 slice corpus is EMPTY (9 → 0). THE WHOLE GRADED-STACK CAMPAIGN (priorities item-0) IS COMPLETE.**

### NEXT

The batch-31 meta-phase fires after this finalize (a SEPARATE dispatch aggregating 097/098/099) — it should close the recommended-resolved OQs, enact the campaign-complete trigger (retire the `annotated-and-retained` carve-out + skill `phase-1-slice-reduction-audit`), migrate the residual Class-B plain-text mentions + the non-book-artifact-orphan-review directive, and pick the batch-32 LEAD now that the graded-stack campaign is fully discharged.

— written by `integrator-finalize` (split integrator-per-report ×3 + finalize ×1)
