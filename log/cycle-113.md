## 2026-06-06 cycle-113 — 2 reports applied clean — 108th consecutive cycle under split integrator — **POSITION 2/3 OF META-BATCH-36, THE MIDDLE cycle (cycles 112/113/114; the batch-36 meta-phase fires AFTER cycle-114's finalize, aggregating 112/113/114)** — PIVOT off blind lazy-tail typing (the c112 F1 re-baseline finding) to AUDIT-FIRST grounding (D1, cross-layer-cross-cutter, OBSERVATION-ONLY: audited the 13 un-baseline-excepted STRONGER GARBAGE SIGNAL members → 1 GROUNDABLE + 12 baseline-exception recommendations RE6/RE7/RE8 for the batch-36 meta-phase; D2, layer-intro-author: applied the one already-confirmed faithful grounding edge `L1/set_subvector_zero → L1-L0/set-subvector-zero-mutation-rotation` reference→depends-on(lowers-to), the theme flips out of STRONGER GARBAGE SIGNAL) → TRUE CUMULATIVE `reachable` 123→124 (+1, all D2; D1 observation-only neutral), `detritus` 136→135 (−1), STRONGER GARBAGE SIGNAL 25→24 (−1); `rank_violations` HELD 0, `unresolved` HELD 0, `untyped` HELD 60 — pass

> Note: this file supersedes a stale pre-redirect-era `cycle-113` log entry (old slice-vertical numbering, 2026-05-26, `orthog L4→L4 refinement`); the cycle counter collided across the 2026-05-26 structural redirect. The current cycle-113 is the batch-36 middle cycle under the layered-stack flow. Old content recoverable from git history.

---

**POSITION 2/3 OF META-BATCH-36 — THE MIDDLE cycle** (3:1 cadence; cycles 112/113/114; the cycle counter does NOT reset across batch boundaries; **the batch-36 meta-phase fires AFTER cycle-114's finalize, as a SEPARATE dispatch aggregating 112/113/114** — this finalize ran NO meta-phase housekeeping). Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) + the 2026-06-02/2026-06-03 user directives + the 2026-06-04 GRADED RESOLUTION LADDER + FEATURE-ROOT REACHABILITY user directive (`METHODOLOGY-GRADED-STACK.md`) + the 2026-06-05 GROUND-don't-remove grounding directive + the 2026-06-06 named-shape-groups L4-calculus notation (codified batch-35 meta-phase). c112 landed `eddd7e6` (+ patch `b86b42a`); NO session restart since c112 (mid-batch, no `.claude/agents/` change) → context continuity holds.

### Headline — PIVOT off blind lazy-tail typing to AUDIT-FIRST grounding; `set-subvector-zero` L1>L0 theme grounded via the c108 §5 L1-op→theme convention (`reachable` 123→124)

The c112 cross-cutting F1 finding (the lazy-tail `untyped` count does NOT decrement for legacy-edged files — the linter auto-migrates them) RE-BASELINED the campaign: continued blind lazy-tail typing of the remaining L3 mid-nodes is LOW measurable value. The higher-fan-out move this cycle is an AUDIT-FIRST cross-cutter sweep to FIND the next FAITHFUL grounding edges among the STRONGER GARBAGE SIGNAL set, PLUS apply the one already-confirmed faithful grounding edge (a real +1). This is the F1 routing being acted on at the planner level BEFORE the batch-36 meta-phase formalizes it.

### What landed

- **D1 (cross-layer-cross-cutter, OBSERVATION-ONLY) — `strong-garbage-audit`.** NO `## Proposed changes` block; mutates NO `book/` artifact (confirmed by direct on-disk read). Audited the 13 un-baseline-excepted STRONGER GARBAGE SIGNAL members (the 25-set minus the RE1-RE5-covered subset, post-repair corrected from 11 to 13 by folding in `L3/jacobi-smoother`) and dispositioned each per §2f FAITHFUL-PATH-OR-FINDING:
  - **1 GROUNDABLE:** `L1/weak_form_term` via the faithful `L1/fe_assemble → L1/weak_form_term` `depends-on` edge (+ companion `fe_assemble→fe_space`, `fe_space→fe_collection`) — routed as a **c114 grounding-dispatch candidate** (a real reachability +Δ).
  - **12 baseline-exception recommendations** for the batch-36 meta-phase to ratify: **RE6** (axpy-family `L2/{axpy,axpby,axpbypcz}` + `L3/{axpy,axpby,axpbypcz}` absorbed into the reachable `linear_combination` combinator — combinator-primary; the arity-1 `scal` members MUST be included, 6 nodes); **RE7** (`L2/elementwise_product` + `L3/elementwise_product` + `L3/assemble-diagonal` + `L3/jacobi-smoother` — the diagonal-preconditioner / diagonal-apply cluster absorbed below the column, 4 nodes; **RE7-vs-RE1-extension is an id-split-or-merge judgment call** for the meta-phase); **RE8** (`L3/fold_solve` + `L3/krylov-step` unconsumed L3 iteration-views — consumers compose the L2/L4 surfaces directly, 2 nodes; **RE8-vs-RE2-extension is an id-split-or-merge judgment call**).
  - Appended 5 OQ sections to `scaffolding/open-questions.md` (RE6 / RE7 / RE8 / `weak_form_term` by the producer + `RE7-cluster-completeness-add-L3-jacobi-smoother` by the per-report integrator, carrying the repairer's post-repair RE7 correction so the meta-phase ratification starts from the exact 4-node RE7 set). One cross-reference-integrity critic warning repaired (canonical token). Did NOT touch `priorities.md` (cycle-planner/meta-phase own the plan).
- **D2 (layer-intro-author, build-relevant) — `set-subvector-zero-theme-grounding`.** Applied the one already-confirmed faithful grounding edge: upgraded one edge in `book/src/L1/set_subvector_zero.md` frontmatter from `reference` (navigational) → `depends-on (kind: lowers-to)` pointing at `L1-L0/set-subvector-zero-mutation-rotation` (the c108-codified §5 L1-op→theme asymmetric grounding convention), and corrected the stale pre-c108 "rank-direction error" prose in 3 locations (§Status well-foundedness, §Downward, the frontmatter comment). `L1/set_subvector_zero` is reachable (`feature/eigenmode.L4 → L3/divfree-projector → L1/set_subvector_zero`); the theme is `rank: firm` so `rank(op=3) ≤ rank(theme=3)` holds. The theme flips out of STRONGER GARBAGE SIGNAL. Standalone delta CONFIRMED exactly as the planner predicted. Promoted OQ `stale-pre-c108-rank-direction-error-prose-on-L1-ops`.
- **TRUE CUMULATIVE (both applied, re-measured by integrator-finalize on the landed tree):** `reachable` 123→124 (+1, all D2; D1 observation-only neutral); `detritus` 136→135 (−1); STRONGER GARBAGE SIGNAL 25→24 (−1); `rank_violations` HELD 0; `unresolved_depends_on_targets` HELD 0; `untyped` HELD 60; firm histogram HELD. Do NOT sum per-dispatch isolation deltas — 124 is the re-measured cumulative figure (D1 contributes 0; the whole +1 is D2's grounding).

### Build

`cargo make book` (mdbook + linkcheck2) EXIT 0. D2's edit is frontmatter+prose on an existing chapter with on-disk edge targets → linkcheck2-clean; D1 wrote no `book/` artifact (OQ-ledger append only); no new file → no SUMMARY/index insert. Only the pre-existing benign `Potential incomplete link` WARNs (markdown-table / KaTeX false-positives, NOT link errors) + the long-standing KaTeX render WARNs on type-signature angle-bracket / lambda-binder constructs (content kept, not on this cycle's files). **NO finalize build-repair needed.**

### Step-5b — graded-stack linters (the build-gate companion, ran on the landed tree)

**`rank_violations: 0`** (`[]`; baseline fully discharged c096 so ANY violation would be NEW and BLOCK — there are NONE, **GATE PASSES**; D2's upgraded edge is firm-op(rank 3) → firm-theme(rank 3), rank gate holds; D1 authored no edges) + **NO newly-orphaned node** (`reachable` CLIMBED 123→124) + **`unresolved_depends_on_targets: 0`** (HELD). Totals on the landed tree: `files=355`, `typed=295`, **`untyped=60`** (HELD), `roots=36`, **`reachable=124`** (was 123, +1), `rank_violations=0`, `unresolved_depends_on_targets=0`, `promotion_frontier=8`, **`detritus=135`** (`detritus_no_typed_edges_pre_p1_artifact=111`, `detritus_with_typed_edges_stronger_signal=24`, `expected_unreachable_outside_dag=44`).

**rank_violations trend: 22 (c094) → 1 (c095) → 0 (c096) → … → 0 (c111) → 0 (c112) → 0 (c113). reachable trend across the campaign: 36 (c105-end, pre-linter-fix) → 81 (batch-33 meta-phase) → 88 (c106) → 95 (c107) → 102 (c108) → 107 (c109) → 119 (c110) → 122 (c111) → 123 (c112) → 124 (c113).** c113 ground the `set-subvector-zero` L1>L0 theme via the faithful L1-op→theme §5 edge.

**On the high `untyped`/`detritus` mass:** informational, NOT a block (per step-5b — the as-yet-untyped pre-P1 tail + the typed-non-node reference-only pages + the typed-but-unreached nodes). Only a *new* rank violation or a *newly*-orphaned node gates; neither occurred. The STRONGER GARBAGE SIGNAL (`detritus_with_typed_edges_stronger_signal`) moved 25→24 (the −1 cleared the `set-subvector-zero-mutation-rotation` theme — flipped reachable by D2's grounding).

### Counts

No firm-count status flips this cycle — D2 is a single frontmatter+prose edge upgrade (no new authored vocabulary, no node promotion) and D1 is observation-only. Layer tallies unchanged. SLICE CORPUS: 0 (corpus fully lifted + deleted across c097/098/099). The measurable movement is entirely on the reachability axis (`reachable` 123→124, the `set-subvector-zero` L1>L0 theme grounded).

### Process

Retroactive-budget global = 0. Per-report safety-net gates all PASS/N/A across both rows (rank-well-foundedness 0, edge-label/prose-mismatch 0 — D2's 3 prose corrections all match the new edge, YAML round-trip 0, SUMMARY-registration N/A, forward-edge-without-surface 0). 0 implied-component stubs created. 2/2 staging rows == 2 dispatched-ready reports — the cycle-018 staging-completeness gap did NOT recur (94th consecutive clean staging / 108th consecutive clean split-integrator cycle). **REPAIR PHASE DID RUN on D1 ONLY** (one cross-reference-integrity warning — canonical token — repaired before integration; D1 is observation-only so the repair touched prose/OQ-routing, not any edge). **CITECHECK NOTE (non-blocking):** D1's citecheck noted 2 failing — both NON-BLOCKING (observation-only prose, lands in no `book/` artifact, both referents verified to resolve, both trivially-correctable margin/path slips): `fe_assemble.md:67-68` (bare-basename path-hygiene slip; means `book/src/L1/fe_assemble.md`) + `graded-stack-baseline-exceptions.md:116-137` (off-by-one EOF overshoot; intended RE1-RE5 content present at 116-136). Recorded not repaired. 5 OQs appended to the ledger this cycle (append-only between meta-phases; the meta-phase unifies/closes). `scaffolding/{open-questions,roadmap,integrator-signals,cycle-record}.md/.jsonl` + `log/` committed atomically with D2's `book/` edit + both consumed-report `integrated_at` touches. NO `.claude/agents/` changes → no session-restart concern from finalize.

### Signals for the batch-36 meta-phase (fires AFTER cycle-114's finalize, aggregating 112/113/114) — THE IMPORTANT CARRY this cycle

- **THE RE6-RE8 BASELINE-EXCEPTION RATIFICATION BATCH** (D1's audit output) — needs formal ratification into `scaffolding/graded-stack-baseline-exceptions.md` at the meta-phase: **RE6** (axpy-family + `scal` → `linear_combination`, 6 nodes — **MUST include the arity-1 `scal` members**); **RE7** (the diagonal-preconditioner cluster incl `L3/jacobi-smoother`, 4 nodes — **RE7-vs-RE1-extension is an id-split-or-merge judgment call**); **RE8** (the unconsumed L3 iteration-views `fold_solve`/`krylov-step`, 2 nodes — **RE8-vs-RE2-extension is an id-split-or-merge judgment call**). The corrected enumeration is 13 = 1 GROUNDABLE + 12 baseline-exception (6 RE6 + 4 RE7 + 2 RE8); starting from the exact 4-node RE7 set (with `L3/jacobi-smoother` folded in) avoids re-tripping the "count climbs without a ratified RE" trigger.
- **THE c114 GROUNDING-DISPATCH CANDIDATE:** `L1/weak_form_term` via `L1/fe_assemble → L1/weak_form_term` (+ companion `fe_assemble→fe_space`, `fe_space→fe_collection`) — a real reachability +Δ for c114.
- **THE CANDIDATE FRICTION `stale-pre-c108-rank-direction-error-prose-on-L1-ops`** (D2) — more c104-era L1 leaves (`normalize` / `reciprocal` / `elementwise_product` / `scal`) likely carry the same stale pre-c108 "rank-direction error" prose + an un-upgraded `reference` edge to their L1>L0 themes (leaving those themes reachable-dead). A **systematic L1-op→theme grounding sweep is a high-fan-out c114 tranche** — each is a faithful +1 like D2's.
- **The carried-open OQs:** `re2-shadows-orthogonalize-variant-split-theme` + the rest of the c112 set still open; plus the new c113 sections (`...RE6`, `...RE7`, `...RE8`, `weak_form_term-groundable-...`, `RE7-cluster-completeness-add-L3-jacobi-smoother`).
- **The c112 F1 finding is being acted on** — this cycle's pivot off blind lazy-tail typing to audit-first grounding IS the F1 routing in practice (planner-level), ahead of the meta-phase formalizing the campaign's `untyped` re-baselining.
