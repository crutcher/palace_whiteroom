## 2026-06-06 cycle-111 — 2 reports applied clean — 106th consecutive cycle under split integrator — **POSITION 3/3 OF META-BATCH-35, THE BATCH-CLOSING cycle (cycles 109/110/111; the batch-35 meta-phase fires NEXT-AFTER THIS finalize, aggregating 109/110/111)** — the carried c110 mid-node tranche RESOLVED (D1: from-scratch `edges:` blocks on `L2/orthogonalize` + `L1/orthogonalize` ground the orthogonalize chain down to L0, flipping `L2-L1/orthogonalize-composition-lowering` + `L1-L0/orthogonalize-mutation-rotation` reachable) + the c110-filed `l1-l0-axpy-family-themes-need-scheme-frontmatter` RESOLVED (D2: from-scratch `edges:` blocks on `L1-L0/{axpby,axpbypcz}-mutation-rotation`, reachability-neutral) → TRUE CUMULATIVE `reachable` 119→122 (+3, all D1; D2 neutral), STRONGER GARBAGE SIGNAL HOLDS 26, `detritus` 140→137 (−3); `rank_violations` HELD 0, `untyped` HELD 60 — pass

> **Note:** this file (`cycle-111.md`) is the current-run, structural-redirect-era cycle-111 log (it overwrites the stale pre-redirect slice-vertical-era `cycle-111.md`, recoverable from git history). The current-run numbering does NOT reset across batch boundaries.

**POSITION 3/3 OF META-BATCH-35 — THE BATCH-CLOSING CYCLE** (3:1 cadence; cycles 109/110/111; the cycle counter does NOT reset across batch boundaries; **the batch-35 meta-phase fires AFTER this finalize, as a SEPARATE dispatch aggregating 109/110/111**). Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) + the 2026-06-02/2026-06-03 user directives + the 2026-06-04 GRADED RESOLUTION LADDER + FEATURE-ROOT REACHABILITY user directive (`METHODOLOGY-GRADED-STACK.md`) + the 2026-06-05 GROUND-don't-remove grounding directive (codified into `METHODOLOGY-GRADED-STACK.md` §2f/§8 by the batch-34 meta-phase). c110 landed its finalize commit `eaca075`.

### Headline — the orthogonalize chain is GROUNDED down to L0; the axpy L1>L0 themes are scheme-typed (`reachable` 119→122)

The c110 finalize routed the next mechanical tranche: the orthogonalize chain was reachable AT `L2/orthogonalize` (c110 grounded it via the `L4/krylov-step →composes→ L2/orthogonalize` edge), but its lowering home `L2-L1/orthogonalize-composition-lowering`, the `L1/orthogonalize` op, and the `L1-L0/orthogonalize-mutation-rotation` theme were all untyped and **dead**. D1's from-scratch `edges:` blocks type the chain down to L0, flipping all three reachable. D2 closes the carried scheme-hygiene OQ by typing the two axpy-family L1>L0 themes (reachability-neutral — they were already reachable via inbound `reference` edges).

`reachable` 119→122 (+3, all D1), `rank_violations` HELD 0, `untyped` HELD 60, `detritus` 140→137 (−3; STRONGER GARBAGE SIGNAL HOLDS 26 — the −3 cleared WEAKER `[garbage?]` untyped-detritus, not the stronger typed-but-unreached bucket). All 4 touched `book/` files are FRONTMATTER-ONLY edits.

### What landed

- **D1 (layer-intro-author, build-relevant, THE LEAD) — the orthogonalize-chain grounding pass.** From-scratch `edges:` blocks on two pre-existing chapters that had a BARE `# orthogonalize` H1 and zero frontmatter: `book/src/L2/orthogonalize.md` (`rank: firm` + 3 `depends-on` [`L1/orthogonalize`, `L1/dot`, `L1/axpy`] + `lowers-to → L2-L1/orthogonalize-composition-lowering` + 3 `reference` concepts) and `book/src/L1/orthogonalize.md` (`rank: firm` + 4 `cites-evidence` L0 `depends-on` + `lowers-to → L1-L0/orthogonalize-mutation-rotation` + 4 `reference`). The chain grounds down to L0 → `reachable` 119→122 (+3: `L1/orthogonalize`, `L2-L1/orthogonalize-composition-lowering`, `L1-L0/orthogonalize-mutation-rotation` flip in). Promoted/confirmed OQ `l3-orthogonalize-sub-chain-no-faithful-reachable-depender` (already present at `scaffolding/open-questions.md:1494`; no duplicate — the `L3/orthogonalize` sub-chain has no faithful reachable depender, routed to the batch-35 meta-phase).
- **D2 (layer-intro-author, build-relevant) — the axpy L1>L0 theme scheme-typing (FRONTMATTER-ONLY hygiene).** From-scratch `edges:` blocks on `book/src/L1-L0/axpby-mutation-rotation.md` (`rank: firm` + 5 `cites-evidence` L0 `depends-on` + 2 `reference` [`L1/axpy`, `L1/axpby`]) and `book/src/L1-L0/axpbypcz-mutation-rotation.md` (`rank: firm` + 4 `cites-evidence` L0 `depends-on` + 3 `reference`). Reachability-NEUTRAL — both themes were already reachable via inbound `reference` edges (`L1-L0/axpby-mutation-rotation ← L1/axpby, L1/axpy`; `L1-L0/axpbypcz-mutation-rotation ← L1/axpbypcz`), confirmed by `--show-inbound`. The producer correctly DECLINED the dispatch-suggested `reference: L1-L0/dot-mutation-rotation` (don't-manufacture discipline — grep of both bodies returns nothing). RESOLVES the c110 D2-filed OQ `l1-l0-axpy-family-themes-need-scheme-frontmatter` (recorded for the meta-phase to unify/close; the OQ ledger is append-only between meta-phases). A carry-forward body note: a pre-existing prose mislabel in `axpby-mutation-rotation.md:25-26` (labels `vector.cpp:739-743` as an "AXPBYPCZ member form"; on-disk `739-743` is the `AXPBY` overload) — flagged + deferred by producer + critic to a future harvester/lowering-verifier body pass; the frontmatter `cites-evidence` comments authored here use the accurate on-disk labels.
- **TRUE CUMULATIVE (both applied, re-measured by integrator-finalize on the landed tree):** `reachable` 119→122 (+3, all D1; D2 neutral); `rank_violations` HELD 0; `untyped` HELD 60; firm histogram HELD; `detritus` 140→137 (−3); STRONGER GARBAGE SIGNAL HOLDS 26. Do NOT sum per-dispatch isolation deltas — the 122 is the re-measured cumulative figure.

### Build

`cargo make book` (mdbook + linkcheck2) **EXIT 0**; **NO build-repair needed**. All 4 touched `book/` files are frontmatter-only edits; every edge target resolves to an on-disk file, so linkcheck2 is clean. The `Potential incomplete link` WARNs are the pre-existing benign markdown-table / KaTeX `$...$` false-positives (bracketed prose in dep-map cells + math spans), NOT link errors. No new file was created (every chapter pre-existed + was already SUMMARY-registered), so no SUMMARY/index insert was needed.

### Step-5b — graded-stack linters (the build-gate companion, ran on the landed tree)

**`rank_violations: 0`** (`[]`; baseline fully discharged c096 so ANY violation would be NEW and BLOCK — there are NONE, **GATE PASSES**; D1's `depends-on` edges are firm→firm or firm→typed-no-rank/L0-cites-evidence; D2's edges are firm with `cites-evidence` rank-terminal L0 + `reference`) + **NO newly-orphaned node** (`reachable` CLIMBED 119→122) + **`unresolved_depends_on_targets: 0`** (HELD). Totals on the landed tree: `files=355`, `typed=295`, **`untyped=60`** (HELD), `roots=36`, **`reachable=122`** (was 119, +3), `rank_violations=0`, `unresolved_depends_on_targets=0`, `promotion_frontier=8`, **`detritus=137`** (`detritus_no_typed_edges_pre_p1_artifact=111`, `detritus_with_typed_edges_stronger_signal=26`, `expected_unreachable_outside_dag=44`).

**rank_violations trend: 22 (c094) → 1 (c095) → 0 (c096) → … → 0 (c109) → 0 (c110) → 0 (c111). reachable trend across the campaign: 36 (c105-end, pre-linter-fix) → 81 (batch-33 meta-phase) → 88 (c106) → 95 (c107) → 102 (c108) → 107 (c109) → 119 (c110) → 122 (c111)** — c111 grounded the orthogonalize chain down to L0 + scheme-typed the axpy L1>L0 themes. **BATCH-35 ARC: `reachable` 102→122 over c109/c110/c111 (+20); `rank_violations` HELD 0 throughout.**

**On the high `untyped`/`detritus` mass:** informational, NOT a block (per step-5b — the as-yet-untyped pre-P1 tail + the typed-non-node reference-only pages + the typed-but-unreached nodes). Only a *new* rank violation or a *newly*-orphaned node gates; neither occurred. STRONGER GARBAGE SIGNAL HOLDS 26 (the −3 detritus drop cleared WEAKER untyped-detritus, not the stronger typed-but-unreached bucket).

### Counts

No firm-count status flips this cycle — D1's 2 from-scratch `edges:` blocks + D2's 2 from-scratch `edges:` blocks are all frontmatter-only (no new authored vocabulary, no node promotion). Layer tallies unchanged: L1 firm 33 main / 40 grand · L4 firm 21 main / 25 grand · L4>L3 11 · L3 17+4po · L3>L2 6 · L2 21+1pc · L2>L1 11 · L0 22 · concepts 34 · methodology 4 · feature spine 11 firm / 1 seed · L4 reduce-family 4 verbs ALL firm. SLICE CORPUS: 0. The measurable movement is entirely on the reachability axis (`reachable` 119→122, the orthogonalize chain grounded down to L0, the axpy L1>L0 themes scheme-typed).

### Process

retroactive-budget global = 0 (both staging rows contribute 0, far under the ≥4 block threshold). Per-report safety-net gates all PASS/N/A on both rows (retroactive-budget, concept_writes-on-existing-slug, forward-edge-without-surface, edge-label/prose-mismatch, H1-reuses-page-heading, append-on-missing-slug, variant-axis-missing, SUMMARY-registration, alpha-position, rank-well-foundedness). 0 implied-component stubs created. 2/2 staging rows == 2 dispatched-ready reports (the cycle-018 staging-completeness gap did NOT recur — 92nd consecutive clean staging / 106th consecutive clean split-integrator cycle). **REPAIR PHASE DID RUN this cycle on D1 ONLY:** one low-severity garbage-bucket-wording warning fixed in-place (measurement-wording prose in CYCLE.md corrected; the `edges:` blocks left untouched); D2 was an all-pass clean critic-set, set ready directly. **CITECHECK NOTE (non-blocking):** the per-report integrators noted AMBIG/MISS/OOB citecheck warnings — ALL on report PROSE / OQ-prose / linter-tool-source references, NONE in any applied `edges:` block; no prose-claim text lands in `book/` (frontmatter-only edits), so all are non-blocking and recorded-not-repaired. Per-report YAML round-trip PASS on all 4 edited files. `scaffolding/open-questions.md` + `scaffolding/roadmap.md` + `scaffolding/integrator-signals.md` + `scaffolding/cycle-record.jsonl` + `log/` all modified, committed atomically with the artifact. NO `.claude/agents/` changes → no session-restart concern from finalize (the batch-35 meta-phase, a SEPARATE dispatch, may enact role-spec changes requiring a restart).

### Signals for the batch-35 meta-phase (fires AFTER this finalize, aggregating 109/110/111 — THE BATCH CLOSES HERE)

- **BATCH-35 ARC (the headline for the meta-phase):** `reachable` 102→122 over c109/c110/c111 (+20), `rank_violations` HELD 0 throughout. c109 grounded the 4 on-spine L2-L1 themes; c110 grounded the reduce-to-scalar chain + orthogonalize leg + 2 BLAS mutation-rotation themes; c111 grounded the orthogonalize chain down to L0 + scheme-typed the axpy L1>L0 themes.
- **CARRIED FINDINGS/OQs the meta-phase must triage:** (1) `l3-orthogonalize-sub-chain-no-faithful-reachable-depender` (D1, `open-questions.md:1494`) — the `L3/orthogonalize` sub-chain has no faithful reachable depender; (2) the normalize/reciprocal internal-utility chain routing; (3) the chebyshev/jacobi preconditioner-leg baseline-exception ratification (carried from c110 — absorbed-below-column, the c107 BC/divfree pattern); (4) the gram + incremental-least-squares routings; (5) `l1-l0-axpy-family-themes-need-scheme-frontmatter` RESOLVED by D2 (meta-phase to unify/close); (6) the latent linter-reader block-mapping-misparse bug (still latent, batch-34 NO-GO).
- **FRICTION RECURRENCE NOTE — `parallel-dispatch-reachability-measurement-contamination` did NOT recur:** the c110-flagged candidate friction (parallel layer-intro-author dispatches misreporting reachability via apply→lint→revert contamination on a shared working tree) did NOT recur this cycle — c111's per-report integrators correctly isolated each dispatch's delta (D1 measured its own +3 standalone; D2 confirmed reachability-neutral; finalize re-measured the authoritative cumulative on the landed tree). The discipline HELD this cycle (recurrence-1, no recurrence-2). The meta-phase should weigh whether to ledger-and-monitor vs codify a producer-side measurement-isolation instruction given the single occurrence + holding discipline.
- **The clean-gated forward-vocabulary frontier remains substantially exhausted** — the measurable movement is on the reachability/typed-edge axes, not new authored vocabulary. The `promotion_frontier: 8` members are ALL obstruction-/demand-gated (not clean picks).
- **The integrator-signals chain is unbroken** (the c110 finalize appended; this c111 finalize appends its section).

— written by `integrator-finalize` (split integrator-per-report ×2 + finalize ×1).
