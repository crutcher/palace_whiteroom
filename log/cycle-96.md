## 2026-06-04 cycle-096 — 5 reports applied clean — ninety-first consecutive cycle under split integrator — **POSITION 3/3 OF META-BATCH-30 (the LAST / BATCH-CLOSING primary cycle; the batch-30 meta-phase fires AFTER this finalize)** — THE GRADED-STACK CAMPAIGN'S MECHANICAL COMPLETION for the typed frontier: **rank violations 22 (c094) → 1 (c095) → 0 (c096)** — the residual O1 discharged two ways at once (D3 typed the theme firm; D4's `read_status_line` parse fix retired the prose-fallback false-positive class) + the P2 first tranche landed (the `preconditioning-framework` firm L4 chapter) — pass

> **Note:** this file supersedes any stale May-25 slice-vertical-era `cycle-96`-era numbering cohort (the pre-structural-redirect run reused these cycle numbers). The current-run numbering does NOT reset; the prior content is preserved in git history.

**POSITION 3/3 OF META-BATCH-30** (3:1 cadence; cycles 094/095/096; the cycle counter does NOT reset across batch boundaries; **the batch-30 meta-phase fires AFTER this cycle-096 finalize as a SEPARATE dispatch aggregating 094/095/096** — this finalize does NOT run meta-phase housekeeping). Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) + the 2026-06-02/2026-06-03 user directives + the **2026-06-04 GRADED RESOLUTION LADDER + FEATURE-ROOT REACHABILITY user directive** (full spec `METHODOLOGY-GRADED-STACK.md`; the campaign is priorities item-0) + the batch-29 meta-phase enactment.

**Headline: the graded-stack two-axis artifact-health campaign reached its mechanical completion for the typed frontier — `rank_violations` 22 (c094 baseline) → 1 (c095) → 0 (c096).** The c095 residual O1 (`L4/solve_family → L4-L3/solve-family-map-dissolution`) was discharged two ways at once: **D3** typed the theme `rank: firm` (clears the genuine edge by construction — all 4 `depends-on` endpoints firm on disk), and **D4**'s `read_status_line` `tools/` parse-bug fix retired the prose-fallback FALSE-POSITIVE class (the ~20 untyped-node maturity mis-reads that masked firm nodes as rough-in/partly-constructive/obstruction), so the histogram is now ACCURATE. In parallel, the **P2 first tranche** landed: **D1** authored the NEW firm L4 chapter `book/src/L4/preconditioning-framework.md` (the P2 campaign vocabulary gap). Two within-file stale-residue land-cleans (**D2** resolution-ladder.md worked example, **D5** matrix-weighted-norm theme) tidied the c091/c095-cascade residues.

- **Staging completeness:** 5 of 5 dispatched-ready reports applied clean (5/5 staging rows == dispatched-ready — the cycle-018 staging-completeness gap did NOT recur for the SEVENTY-SEVENTH consecutive clean staging / NINETY-FIRST consecutive clean split-integrator cycle); **no repair phase fired** (all 5 critics passed clean — critic set `ready` directly); zero deferrals, zero rejections, zero gate-hits, zero finalize build-repairs.

### What landed

- **D1 (layer-intro-author, build-relevant) — the P2 genuine-gap author: a NEW firm L4 chapter `book/src/L4/preconditioning-framework.md`.** The capability-typed preconditioner-binding composition root (`rank: firm`, typed `edges: depends-on [L4/ksp_solve (caps-the-binding), reference/palace/.../ksp.cpp:276-293 (cites-evidence)]`, 8 concept refs as `reference`) + repointed the two `book/src/concepts/capability-typing.md` slice references (:26, :55) to the new chapter §"Capability typing" + an alpha-positioned `SUMMARY.md` row (between `ksp_solve` and `solve_family`, Outer-driver kind-group) + an `L4/index.md` dep-map row (alpha between `Outcome` and `restart_cycle`). **L4 firm +1 (19→20 main / 23→24 grand).** **Rank-gate PASSED:** the sole ladder `depends-on` dep `L4/ksp_solve` reads firm on disk → `rank(preconditioning-framework=3) ≤ rank(ksp_solve=3)`; the `ksp.cpp` edge is `cites-evidence` to L0 source, constrains no rank. (The slice `book/src/spec/slices/cg_preconditioning_framework.md` is NOT deleted this cycle — deferred to the batch-31 P2 slice-deletion tranche.)
- **D3 (lifter, build-relevant) — O1 lazy-tail typing → `rank_violations` 1 → 0.** `book/src/L4-L3/solve-family-map-dissolution.md` (had NO frontmatter) got a prepended typed graded-stack block: `layer: L4-L3`, `rank: firm`, `edges: depends-on [L4/solve_family, L4/ksp_solve, L4-L3/ksp-solve-driver-dissolution, L3/ksp_solve]` + 6 `reference` edges (3 sibling themes + 3 concept pages). Pure edge-typing — no §Status prose change, no LHS/RHS body change, no maturity re-judgment. **Rank-gate PASSED:** all 4 `depends-on` endpoints re-read firm on disk in-invocation (solve_family c086, ksp_solve c048, ksp-solve-driver-dissolution c048, L3/ksp_solve c020) → `rank(theme=3) ≤ min(deps=3)`. This clears the c095 residual O1 by construction (it bypasses the buggy prose fallback regardless of D4's ordering).
- **D4 (layer-intro-author, `tools/`-only — NOT build-relevant) — the `read_status_line` token-priority parse-bug fix + regression fixture.** `tools/graded-stack-lint/graded_stack_lint.py`: `read_status_line` rewritten to the leading-inline-code-token rule (the blob-scan removed; module-level `_STATUS_TOKENS` ordered longest-/most-qualified-first so `rough-in (test-coverage-bounded)` precedes bare `rough-in`/`firm`/`seed`); `derive_rank` precedence (explicit `rank:`/`firmness:`/`status:` frontmatter wins over the prose fallback) preserved. NEW regression fixture `fixture/book/src/L1/prose_firm_provenance.md` + `fixture/book/src/feature/widget.L4.md` wiring + README parse-rule paragraph. ZERO `book/` edits. This retires the prose-fallback FALSE-POSITIVE class — the ~20 untyped-node maturity mis-reads (firm nodes whose §Status paragraph mentions "rough-in" within 5 lines as a provenance/disclaimer phrase) — making the histogram ACCURATE.
- **D2 (layer-intro-author, build-relevant) — the `resolution-ladder.md` worked-example repair.** `book/src/methodology/resolution-ladder.md` (×3 edits): the c094 FALSIFIED forward-prediction (gram_reduce held-back-by-rough-in-bilinear-form, as the "the invariant holds things back" illustration) re-told as the COMPLETED c091/c095 two-wave cascade discharge — §heading now "cycles 088–095 cascade, completed"; the DAG diagram surfaces gram_reduce's second off-diagonal `bilinear-form` leaf; closing paragraphs re-told as the discharge. A reader-facing NON-AUTHORITATIVE methodology page (banner at :3-18); carries no `rank:`/`edges:` frontmatter → all typed-edge gates no-op. Resolves the resolution-ladder.md half of OQ `bilinear-form-firm-flip-stale-narration-in-meta-owned-methodology-pages` (the `goal-flow.md:260-266` half stays OPEN — meta-phase write-territory, batch-30 refresh).
- **D5 (lifter, build-relevant) — the matrix-weighted-norm within-theme stale-residue fix.** `book/src/L1-L0/matrix-weighted-norm-mutation-rotation.md` (×2 edits): the OPERATOR's maturity re-anchored to the post-c091 firm reality at line 5 (opening prose `rough-in` → `firm`) and line 317 (§Speculative-L1-operators clause `(rough-in, test-coverage-bounded)` → `(firm, promoted cycle-091)`). Prose maturity-word swap only — no decomposition/signature/structural change. The theme's OWN `## Status` (firm, a theme-maturity not an operator-maturity claim) deliberately untouched. D5's cross-file whole-book grep-guard surfaced one CLEAN cross-file stale operator-maturity assertion at `book/src/L4/domain_energy_reduce.md:377` — correctly FLAGGED-not-fixed (out of the one-theme scope), routed to a batch-31 land-clean.

### Graded-stack linter — the 22 → 1 → 0 drop (the campaign's typed-frontier mechanical completion)

Per `METHODOLOGY-GRADED-STACK.md` §8 the finalize step should eventually run the linters as a build-gate companion to `cargo make book`. **The finalize-runs-linters build-gate exit-code wiring is NOT yet formally enacted** (OQ `graded-stack-finalize-json-wiring-role-spec` for the batch-30 meta-phase) — but finalize RAN the linter on the LANDED state per the dispatch and recorded the totals. Ran `python3 tools/graded-stack-lint/graded_stack_lint.py --json`:

| metric | value |
|---|---|
| files | 360 |
| typed | 208 |
| untyped | 152 |
| roots | 36 |
| **rank_violations** | **0** (was 1 at c095, 22 at the c094 baseline — a clean 22 → 1 → 0 burn-down) |
| **rank_histogram** | **`{firm: 192, rough-in: 7, partly-constructive: 3, obstruction: 2, partial-obstruction: 4}`** (now ACCURATE — D4's parse fix retired the prose-fallback mis-reads) |
| promotion_frontier | 10 |
| unresolved_depends_on_targets | 35 |
| detritus (reachability GC, informational) | 172 (no_typed_edges_pre_p1_artifact 110 + with_typed_edges 62) |

**The `rank_violations: 0` is the batch-closing headline.** The typed subset now has ZERO genuine rank gaps — the well-foundedness invariant `rank(u) ≤ min over depends-on deps of rank(v)` holds for every typed edge. The histogram is the OTHER half of the win: D4's fix means the ~20 untyped nodes the prose-fallback was mis-reading (as rough-in/partly-constructive/obstruction) now read correctly as firm — the linter became ACCURATE, not a maturity change. The high `untyped`/`detritus` counts are the P2 mid-campaign standing reachability-GC detritus sweep (the as-yet-untyped pre-P1 nodes — `spec/slices/*` etc.) — **informational, NOT a build-gate failure: there is NO new rank violation and NO newly-orphaned node this cycle**, the two step-5b/6 block conditions. (Minor campaign parse-tail: the new `preconditioning-framework` node appears in `unresolved_depends_on_targets` as `-> target: L4/ksp_solve` — the linter's dict-form edge-syntax note (`- target: …` vs bare-string); the target `L4/ksp_solve.md` exists and the rank-gate read it correctly, so it is NOT a dangling edge.)

### Counts

- **L4 firm 19 → 20 main / 23 → 24 grand** (`preconditioning-framework` — NEW firm L4 chapter, D1).
- All other layer-vocabulary counts UNCHANGED from cycle-095: L1 firm 32 main / 39 grand, L4 rough-in 0 plain / 0 test-coverage-bounded (cohort EMPTY), L4>L3 firm 10, L3 firm 17 + 4 partial-obstruction, L3>L2 firm 6, L2 firm 21 + 1 partly-constructive, L2>L1 firm 11, L0 chapters 22, concepts 33, methodology 4, feature spine 11 FIRM / 1 seed (only `boundary-mode`, honestly typed rough-in), L4 reduce-family 4 verbs (all firm).

### Process

- retroactive-budget global = **0** (one new firm chapter + 3 in-place re-anchors/typings + a `tools/` fix — no retroactive edits to existing chapters; well under the ≥4 block threshold).
- **No repair phase** — all 5 critic METAs set the canonical `ready` set directly (all-pass clean reports); no finalize build-repair.
- **Per-report safety-net gates all PASS / N/A.** D1 rank-gate PASS (new firm node over firm `ksp_solve`); D3 rank-gate PASS (typed firm theme over 4 firm `depends-on` endpoints); D2 (non-authoritative methodology page, no typed frontmatter — typed-edge gates no-op); D5 (prose maturity-word swap, no node status flip — rank-gate not-triggered); D4 (`tools/`-only, no `book/` node — all book-write gates N/A; its one citecheck "MISS" is a benign tool-self-reference to `graded_stack_lint.py:319-324`, the removed blob-scan, structurally invisible to citecheck which scans `reference/*` + `book/src` roots only). citecheck across the 4 book-relevant reports: D1 11 ok, D2 7 ok, D3 9 ok, D5 9 ok — 0 failing.
- **Build:** `cargo make book` (mdbook + linkcheck2) exit 0 (~92s); ONE new book file (`L4/preconditioning-framework.md`) + 4 existing book pages edited. The new L4 chapter `preconditioning-framework.html` built and all its internal links (`./ksp_solve.md`, `./krylov-step.md`, and the 8 `../concepts/*.md` refs) resolve under linkcheck2; zero dead links; **NO build-repair needed** (clean first build). Only pre-existing benign KaTeX "Potential incomplete link" WARNs remain (4, in `design/l4_calculus.md:104` — the rendered `<span>` bracket-notation artifacts, NOT dead links).
- **Baseline-exceptions ledger burn-down 1 → 0 tracked.** `scaffolding/graded-stack-baseline-exceptions.md` TRACKED-OPEN-1 (O1, `L4/solve_family → L4-L3/solve-family-map-dissolution`) is now **DISCHARGED-by-c096-D3** (the `rank: firm` typing satisfies its promotion condition; the landed-state linter reports `rank_violations: 0`). Finalize annotated the ledger with the discharge note + an updated burn-down summary (this is cycle-end housekeeping finalize owns; the append-only OQ-ledger entries are left for the meta-phase to unify).
- **OQs (per-report intake; 0 closed/opened by finalize):** CLOSEABLE at the batch-30 meta unify — `l4-preconditioning-framework-promotion` (D1), `graded-stack-lint-read-status-line-token-priority-bug` (D4), `matrix-weighted-norm-mutation-rotation-within-theme-stale-rough-in-residue` (D5), and the resolution-ladder.md half of `bilinear-form-firm-flip-stale-narration-in-meta-owned-methodology-pages` (D2, PARTIAL — goal-flow.md half stays open). NEW — `domain_energy_reduce-377-mwn-stale-rough-in-residue` (D5, batch-31 land-clean) + a benign 2nd-consumer watch `record-OpBinding-may-need-concept-page` (D1).
- **NO `.claude/agents/` changes this cycle → no session-restart concern.** The meta-phase does NOT run in this finalize (it fires after cycle-096 as a separate dispatch).

### Campaign state + signals for the batch-30 meta-phase

- **Graded-stack two-axis health campaign (priorities item-0): P0 DONE; P3 DONE; P1 DONE (frontier-first typed — `rank_violations` criterion MET = 0 for the typed subset; the prose-fallback false-positive class RETIRED via D4's parse fix); P2 FIRST TRANCHE landed (the `preconditioning-framework` vocabulary gap authored firm).** The dominant remaining P2 work is the **slice-deletion tranche — the batch-31 LEAD candidate:** the 9 Phase-1 slices under `book/src/spec/slices/` + their `spec/index.md` rows + SUMMARY entries + the ~30 slice-line-anchor repoints; completion criterion = the reachability GC shows `spec/slices/*` unreachable (they sit in the pre-P1 detritus mass now).
- **The batch-30 meta-phase (fires after this finalize, aggregating 094/095/096) should:** (i) mark the closeable OQs above; (ii) decide the **batch-31 LEAD** (the P2 slice-deletion tranche); (iii) refresh the `goal-flow.md:260-266` stale worked-example half (the meta-owned remaining half of `bilinear-form-firm-flip-stale-narration-in-meta-owned-methodology-pages`); (iv) decide the carried P1 **finalize-runs-linters build-gate wiring** (`graded-stack-finalize-json-wiring-role-spec`) — now that `rank_violations: 0`, the exit-code gate is cheap to enforce; (v) the carried `cites-evidence-l0-edge-linter-slug-resolution-exemption` convention + the c094 P1 edge-home fork.

Written by `integrator-finalize` (split integrator-per-report ×5 + finalize ×1).
