## 2026-06-01 cycle-049 — 3 reports applied clean — forty-fourth consecutive cycle under split integrator — FIRST PRIMARY CYCLE OF META-BATCH-15 — FIRST refactor-pass cycle under the 2026-06-01 VOCABULARY-SHIFT REDIRECT — pass

**FIRST PRIMARY CYCLE OF META-BATCH-15** (3:1 cadence; cycles 049/050/051; the batch-14 meta-phase already fired AFTER cycle-048's finalize commit as a separate dispatch — the cycle counter does NOT reset across batch boundaries; the batch-15 meta-phase fires AFTER cycle-051's finalize). The **FIRST refactor-pass cycle under the 2026-06-01 VOCABULARY-SHIFT REDIRECT** (`METHODOLOGY-REDIRECT.md`; CLAUDE.md §Methodology invariants ⟢). A clean opus-planner cycle.

- **Staging completeness:** 3 of 3 dispatched-ready reports applied clean (3/3 staging rows == dispatched-ready — the cycle-018 staging-completeness gap did NOT recur for the THIRTIETH consecutive cycle); zero deferrals, zero rejections, zero build-repairs.
- **HEADLINE — the refactor pass STARTED: 2 combinator-as-entry inversions landed (linear_combination + inner_product); cycle-050 enactment mapped; D3 found the cohort is 18 not 12; one D2 dispatch-phase book-leak reverted+reconstructed clean.** The redirect reframes the stack as genuine representational + vocabulary shifts (not a rectangular projection): the combinator/fold is the **entry**, leaves are **specialization notes**, and lowerings are **translations across vocabularies, NOT 1:1 named-term renames** (a degenerate identity-in-named-terms lowering is a smell). This cycle applied that reframe to the two BLAS-1 fold families and audited the full degenerate-lowering cohort.
  - **D1 (combinator-miner, refactor-pass-linear-combination-family) — APPLIED, build-relevant.** The combinator-as-entry inversion of `book/src/L2/linear_combination.md` (4 surgical edits): the fold/combinator is now the **primary L2 entry**; `axpy`/`axpby`/`axpbypcz`/`scal` become specialization notes. The (b) replace-and-propagate MAP (leaf-collapse, thin-theme demotion, `L3/linear_combination` authoring, L4-propagation) is a **cycle-050 forward plan — NOT enacted this cycle**. The (c) KEEP verdict on `L2-L1/linear-combination-fold-specialization.md` is a no-mutation verdict. No `L3/linear_combination.md` stub created (cycle-050 harvester authors it; the leaf-disposition convention is unsettled — the gating OQ; implied-component-stub bar deliberately NOT met). 3 OQs promoted.
  - **D2 (combinator-miner, refactor-pass-inner-product-family) — APPLIED, build-relevant.** The combinator-as-entry inversion of `book/src/L2/inner_product.md` (8 surgical edits across 5 sites): fold-primary; `dot` demoted to a specialization note; **`nrm2` stays a thin CONSUMER of inner_product, explicitly NOT a fold member** (the ledger `:595` carve-out). Plus a plain-text `L3/inner_product` rough-in dep-map row appended to `book/src/L3/index.md` (the upward-propagation target — correctly NOT a live link, since the file is a cycle-050 harvester authoring). The (b) replace-and-propagate MAP (`L2/dot.md` leaf-collapse, four-theme demotion, `L3/inner_product.md` authoring) is a **cycle-050 forward plan — NOT enacted this cycle**. D2's Site-6 edit aligns inner_product's reciprocal §"Sibling fold" note with D1's edit A4 — the two are mutually consistent, no cycle-050 alignment touch needed. 3 OQs promoted. **PROCESS NOTE:** D2 originally **LEAKED a direct write** to `book/src/L2/inner_product.md` during the dispatch phase (write-authority violation); the repairer reverted the leak (file restored to committed HEAD) and reconstructed the inversion as 8 authorized `edit:`-fenced proposed-changes blocks, which integrator-per-report then applied against the restored file — the **applied state is correct**, but the recurrence is flagged for the batch-15 meta-phase (`specialized-agent-direct-write-to-book-during-dispatch`).
  - **D3 (cross-layer-cross-cutter, refactor-pass-degenerate-lowering-audit) — APPLIED, observation-only (build-relevant: NO).** The FIRST cohort-wide degenerate-lowering audit under the redirect. **HEADLINE FINDING — the degenerate-lowering cohort is 18 themes (9 pairs), NOT 12.** This is the **controlling cycle-050 scope input**: demoting only 12 + stranding 6 re-creates the mirrored floor the redirect corrects, so cycle-050 enactment MUST cover all 18. The demotion worklist lives in D3 CYCLE.md §"The demotion worklist" §A/§B/§C (DEMOTE-to-inline / ABSORB-into-combinator-note / KEEP-substantive partition + the L3>L2-vs-L2>L1 fold-member asymmetry); 2 themes (`divfree-projector` / `jacobi-smoother`) are marked verify-body-before-demoting. D3 authored NOTHING in `book/`. 3 OQs promoted.
- **D1/D2/D3 reconciliation — CLOSED-as-agreement.** D2 promoted `nrm2-consumer-not-member-must-survive-cycle-050` as a divergence-RISK vs D3; at integration the risk is CLOSED-as-agreement — D3 follows the same `:595` carve-out (nrm2 = consumer of inner_product, NOT a fold member). **No divergence for the meta-phase to escalate**; recorded so cycle-050 enactment validates rather than re-litigates.
- **Counts essentially UNCHANGED this cycle — this is a refactor pass, NOT a count bump.** The two L2 combinator entries were **INVERTED in framing, not added**; no new firm operators/themes landed; the leaf-collapse + L3-combinator authoring + thin-theme demotion are cycle-050 enactment. L1 firm 26, L2 firm 21, L2>L1 firm 21, L3 firm 15 + 3 partial-obstruction, L3>L2 firm 17, L4 firm 6 + 6 firm L4>L3 + 4 outer-driver rows, L0 chapters 22, Phase-1 removals 9/10 — ALL UNCHANGED.
- **Cycle-050 enactment backlog (mapped, not enacted):** (i) the 18-theme degenerate-lowering demotion worklist (D3 §A/§B/§C; 2 verify-body-before-demoting); (ii) ~6 L2 leaf-collapses (disposition gated on the cohort-wide `collapsed-leaf-disposition-convention` OQ — delete-vs-redirect-stub, ratified at the batch-15 meta-phase); (iii) 2 L3-combinator authorings (`L3/linear_combination`, `L3/inner_product`).
- **Process:** retroactive-budget global = 0; `cycle-planner-stale-priorities-line-recruitment` did NOT recur (FIRST clean opus-planner cycle of batch-15); **NO wave conflict** — D1/D2 touch disjoint files (`linear_combination.md` / `inner_product.md`); D3 observation-only; the same-cycle sibling inversions applied cleanly serially. 9 OQs opened (3 each D1/D2/D3); 0 closed in-artifact (closures route to the batch-15 meta-phase).
- **Build:** clean — `cargo make book` exit 0 (~91.7s); all 3 changed chapters re-rendered; the plain-text `L3/inner_product` row leaves no live link, so no `linkcheck2` hard error; linkcheck2 green. The only build noise is pre-existing and unrelated: KaTeX "Potential incomplete link" false-positives in `design/l4_calculus.md` + older markdown-table HTML-tag WARNs in unchanged `L1-L0/`/`L0/`/`meta-reviews/` files. **Zero build-repairs.**
- **Plan note (priorities.md):** the cycle-planner already reshaped the active head this cycle; integrator-finalize appended only the cycle-050 hand-off note (the refactor-pass enactment is the cycle-050 lead: consume D1/D2 maps + D3's 18-theme worklist; reconcile the D1/D2/D3 calls already agreed on nrm2-consumer-not-member). priorities.md is co-owned by cycle-planner + meta-phase; integrator-finalize did NOT clobber the planner's active-head edit.
- Written by `integrator-finalize` (split integrator-per-report ×3 + finalize ×1).

---

## 2026-05-25 cycle-49 — back cg — pass

- Synthesis: Retroactive L0→L1 rotation_claims for the cg slice (slice already complete through L4 on disk). Quoted prose blocks from the existing ## L1 section support each claim; no new structural writes. retroactive_claim_evidence:
  - claim_index: 0 (mutation-erasure for x.Add/r.Add)
    on_disk_path: book/src/spec/slices/cg.md
    section: ## L1
    quoted_lines:
      "let x' = x + alpha · p'                      // x.Add(alpha, p)
       let r' = r - alpha · z'_pre                  // r.Add(-alpha, z)"
      "The MFEM `Vector::Add(α, y)` mutates `x` in place; L1 names the resulting value `x'` and rebinds."
  - claim_index: 1 (AXPBY destination-erasure)
    on_disk_path: book/src/spec/slices/cg.md
    section: ## L1
    quoted_lines:
      "let p' = if it == 0 then z else axpby 1.0 z (beta/beta_prev) p"
      "`linalg::AXPBY(α, x, β, y)` mutates `y` in place; L1 makes the destination explicit as the result of the call."
  - claim_index: 2 (operator output-buffer erasure)
    on_disk_path: book/src/spec/slices/cg.md
    section: ## L1
    quoted_lines:
      "let z'_pre = apply A p'                      // A->Mult(p, z)"
      "let z' = apply B r'                          // ApplyB(B, r, z); or z = r if !B"
      "The L3 calls to `A->Mult` and `B->Mult` write into pre-allocated output buffers (`z`, `r`). In L1 those are erased — the calls become pure `apply A p → tensor` and `apply B r → tensor`."
  - claim_index: 3 (loop-as-iterate state-threading)
    on_disk_path: book/src/spec/slices/cg.md
    section: ## L1
    quoted_lines:
      "iterate from (x = x₀, r = r₀, z = z₀, p = ⊥, beta = beta₀, beta_prev = 0, res = sqrt |beta₀|, it = 0):
         while it < max_it && !converged:
           ...
           continue with (x = x', r = r', z = z', p = p', beta = beta', beta_prev = beta, res = res', it = it')"
  - claim_index: 4 (CheckDot lifted to partial-function guard)
    on_disk_path: book/src/spec/slices/cg.md
    section: ## L1
    quoted_lines:
      "check_dot beta'                                // partial-function guard; aborts on non-finite"
      "`check_dot` (Palace's `CheckDot`, …) is a partial-function guard at each new inner-product site: it aborts execution if the result is non-finite or, on real SPD systems, negative (signalling loss of positive-definiteness). L1 surfaces it as a `check_dot β'` assertion"
  - claim_index: 5 (initial-residual quirk preserved verbatim)
    on_disk_path: book/src/spec/slices/cg.md
    section: ## L1
    quoted_lines:
      "Palace computes `beta_rhs = Norml2(b) = sqrt|(b,b)|` then sets `initial_res = sqrt|beta_rhs|`, yielding `initial_res = (b·b)^{1/4}` — not `‖b‖₂`. … L1 preserves Palace's source behavior"
- Verdict: pass.
- Friction: none.
- Structural change: none.
