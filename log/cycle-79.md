## 2026-06-03 cycle-079 — 4 reports applied clean — seventy-fourth consecutive cycle under split integrator — POSITION 1/3 OF META-BATCH-25 — both c075 reduce verbs' 2nd gate discharged + a NEW L4 verb authored — pass

**Position 1/3 of meta-batch-25** (3:1 cadence; cycles 079/080/081; the cycle counter does NOT reset across batch boundaries). This is **the FIRST primary cycle after the batch-24 meta-phase**. **The batch-25 meta-phase fires AFTER cycle-081's finalize as a SEPARATE dispatch** aggregating 079/080/081 — this finalize does NOT run meta-phase housekeeping.

Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) + the 2026-06-02/2026-06-03 user directives, with the FEATURE-SURFACE SPINE codified into the role-specs + CLAUDE.md (batch-22 meta-phase, commit `387fa56`) + the batch-23 record-definition obligation + by-kind grouping (commit `a23bbce`) + the **batch-24 meta-phase enactments** (the `record` Kind RATIFIED; `domain_energy_reduce` verb MIGRATED to the plan; the output-product↔driver 1:1 cross-link convention AMENDED for driver-agnostic energy-fields; the reduce-verb 2nd-gate dischargeability SHARPENED — cite existing postprocess tests, do NOT author new; the batch-25 active head reshaped to FIRM-the-seed-surface).

4 of 4 dispatched-ready reports applied clean (4/4 staging rows == dispatched-ready — the cycle-018 staging-completeness gap did NOT recur for the SEVENTY-FOURTH consecutive clean split-integrator cycle / SIXTIETH consecutive clean staging). Zero deferrals, zero rejections, zero gate-hits, zero build-repairs.

### Headline — both c075 output-product reduce verbs' 2nd (test-coverage) gate DISCHARGED + a NEW L4 verb `domain_energy_reduce` authored

The FEATURE-SURFACE SPINE column build-out is COMPLETE (13 `seed` columns); the frontier is now **FIRMING the seed surface**, not adding columns. This cycle led with **THE LEAD (HIGH fan-out): the reduce-verb 2nd-gate discharge via existing-test citation** — discharging the SECOND (test-coverage) gate on both c075 reduce verbs IN write-scope by citing the existing Palace postprocess unit tests as L0-equivalent semantic documentation (**batch-24 meta-phase decision (e)**, the route explicitly sanctioned). Both verbs promote `rough-in` → `rough-in (test-coverage-bounded)` — **NOT firm** (the tests witness only the reduction-OUTPUT invariant; the field-assembly path is not directly exercised). **NO firm-count change** — these are maturity-qualifier upgrades + one new rough-in chapter.

- **D1 (lowering-verifier — THE LEAD): `sparameter_reduce` 2nd-gate discharge** — `book/src/L4/sparameter_reduce.md` `## Status` promoted `rough-in` → `rough-in (test-coverage-bounded)` by citing `test/unit/test-postoperator.cpp` (the `check_port_data` dimensionless-S round-trip invariant over both port kinds) as L0-equivalent documentation. The audit witnesses only the reduction-OUTPUT invariant (the test uses `RandomMeasurement()` and never calls `MeasureSParameter`), so the verb lands `rough-in (test-coverage-bounded)`, NOT `firm`. Gate-b (the per-port projection L1 home) was ALSO recorded already-discharged out-of-band by the firm L1 `port_projection` (c077). A `verified_against:` yaml block landed; the L4/index dep-map row + the coupled `sparameters` column's `composes:` frontmatter (`bilinear-form`→`port_projection`) were refreshed. **The coupled `sparameters` feature column STAYS `seed`** (a constituent is not yet firm — by its own stated rule). The `sparameters.L1.md` PROSE down-link repoint (`:39,60,64`) was DEFERRED (one-theme-per-invocation) — a next-cycle follow-up. Closes OQ `sparameter-reduce-status-promotion-double-gated` (the sparameter half); re-confirms `sparameter-reduce-l1-port-projection-home`.
- **D2 (lowering-verifier): `eigenfreq_qfactor_reduce` 2nd-gate discharge** — `book/src/L4/eigenfreq_qfactor_reduce.md` `## Status` promoted `rough-in` → `rough-in (test-coverage-bounded)` by citing `test/unit/test-postoperator.cpp` (the `[idempotent]` round-trip CHECK-asserting `mode_port_kappa` `:216`/`:259` + `participation_ratio` `:160-188` as round-trip-invariant L0-equivalent reduction-output fields). Gate-a (the κ-participation) was already firm via L1 `participation_ratio` (c077) — this does NOT re-open the c077-resolved κ-participation route. The audit witnesses only the reduction-OUTPUT invariant (the test never runs the eigenpair→`(f,Q)` assembly), so the verb lands `rough-in (test-coverage-bounded)`, NOT `firm`. An 8-entry `verified_against:` yaml block landed (top-level fenced, indent stripped per the repairer's nested-fence-truncation guard); the L4/index dep-map row refreshed; a new paragraph appended to the column's `## Status`. **The coupled `eigenfrequency-qfactor` column STAYS `seed`.** Closes OQ `eigenfreq-qfactor-reduce-status-promotion-double-gated` (the eigenfreq half — both halves of the original combined OQ now resolved-to-qualifier); opens successor `eigenfreq-qfactor-reduce-firm-needs-l1-eigenvalue-untransform-primitive` (the residual STRUCTURE-side gate to full `firm`). **Carry-forward staleness:** the `eigenfrequency-qfactor.L4.md` pre-existing Status opening paragraph (~line 68) still carries the old κ-participation "rough-in" reasoning (the appended paragraph carries the corrected narration) — flag for a next-cycle prose cleanup.
- **D3 (harvester — MEDIUM): NEW L4 verb `domain_energy_reduce` authored at `rough-in`** — `book/src/L4/domain_energy_reduce.md` (new file, status `rough-in`): the per-domain `(energyᵢ, pᵢ)` energy-table reduction, the **per-DOMAIN sibling of the per-MODE `eigenfreq_qfactor_reduce`** (both reduce-to-scalar-TABLE, rank-1; NOT a `gram_reduce` family-PAIR grid — the c074 D6 do-NOT-over-unify guard honored). It folds TWO L1 primitives per row: the domain-restricted `matrix-weighted-norm`-squared energy `½⟨field, M_idx field⟩` AND the firm `participation_ratio` quotient. Carries an in-chapter `## Record definition` for the single-consumer input record `DomainOpMap` (`{idx → M_idx}` = C++ `DomainPostOperator::M_i`). The `domain_energy_reduce` dep-map row was alpha-inserted in the L4 "Data-algebra combinators & named verbs" grouping (immediately before `dot`); the rough-in tally bumped `(1)`→`(2)`; the chapter alpha-registered in `SUMMARY.md`. The energy-fields column's plain-text forward-refs (`energy-fields.L4.md:62,134,156`) were **upgraded plain-text→live-link** now that the target exists. **L4 rough-in 1→2; the L4 reduce-family is now 4 AUTHORED verb files.** Closes OQ `domain_energy_reduce-l4-verb-needs-authoring`; opens successors `domain_energy_reduce-promotion-double-gated` + `record-DomainOpMap-promote-watch` + a possible `problems/` drive-by `domain-field-energy-participation-guard-inconsistency`.
- **D4 (combinator-miner — observation-only confirm-probe): DISTINCT-VERB-WARRANTED** — the redirect's do-not-mint-vocabulary-that-does-not-simplify check for D3's verb. Verdict: `domain_energy_reduce` does NOT collapse into a bare `participation_ratio` fold-inline (the per-domain numerator is itself a domain-restricted SPD energy reduction, so it folds two L1 primitives per row) and does NOT collapse into `gram_reduce` (rank-1-table, not rank-2-Gram). **NO `book/` mutation** (observation-only). The verdict was already consumed by D3; the probe's four harvester-firming notes are all already represented by the D3 OQ appends. Closes OQ `domain_energy_reduce-distinct-verb-vs-inline-confirm-probe` (verdict-provenance marker only).

### Build

`cargo make book` (mdbook + linkcheck2) exit 0 (Build Done ~91s). The load-bearing checks PASS: the new `book/src/L4/domain_energy_reduce.md` resolves in `SUMMARY.md` (alpha-registered before `dot` in the Data-algebra combinators & named verbs grouping) with no orphan; the `energy-fields.L4.md` plain-text→live-link upgrades (`:62,:134,:156`) resolve to the now-on-disk target; the two reduce-verb status-cell refreshes + the L4/index dep-map row insert are consistent. `linkcheck2` clean — **zero dead links, zero build-repair**. Only the 4 pre-existing benign KaTeX "Potential incomplete link" WARNs in `design/l4_calculus.md` (math-notation brackets mis-read as link syntax — the long-standing book-wide false-positive pattern, NOT dead links; predate this cycle).

### Counts

**NO firm-count change** (maturity-qualifier upgrades + 1 new rough-in chapter; firm UNCHANGED). **MEASURABLE DELTAS: L4 rough-in 1→2** (+`domain_energy_reduce`); **both c075 reduce verbs `rough-in` → `rough-in (test-coverage-bounded)`**. The **L4 reduce-family is now 4 AUTHORED verb files**: `gram_reduce` (`rough-in (test-coverage-bounded)`) / `sparameter_reduce` (`rough-in (test-coverage-bounded)`, c079) / `eigenfreq_qfactor_reduce` (`rough-in (test-coverage-bounded)`, c079) / `domain_energy_reduce` (`rough-in`, NEW c079). Both c075 verbs' 2nd (test-coverage) gate is now discharged; both still gated on a STRUCTURE-side firm gate before full `firm` (`sparameter_reduce`: an assembly-fold test; `eigenfreq_qfactor_reduce`: the eigenvalue-un-transform L1 primitive + an assembly test). All other layer-vocabulary counts UNCHANGED from c078 (L1 firm 29 main / 36 grand, L4 firm 14, L4>L3 10, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, Phase-1 9/10, concepts 33 + `record` Kind [RATIFIED batch-24], methodology 2; feature spine 13 columns by-kind-grouped, all `seed`).

### Process

retroactive-budget global = 0 (D1/D2 cite EXISTING postprocess tests as L0-equivalent documentation — the batch-24 decision-(e) sanctioned route, not retroactive citation; D3 is a new rough-in chapter add; D4 is observation-only). **0 dispatch-phase write-partition leaks** (D4 combinator-miner made ZERO `book/` mutation — observation-only, verified clean by the per-report leak-watch + the critic; the batch-24 c077 D4 leak recurrence was recorded 4→5 + HELD NO-GO by the batch-24 meta-phase, NOT recurred this cycle). **0 implied-component stubs** (the energy-fields forward-refs resolved to live links by D3 authoring the actual verb file, NOT a stub). **4 OQs closed in-artifact** (both reduce-verb double-gated OQs RESOLVED-to-qualifier + the verb-authoring OQ + the confirm-probe OQ, +1 re-confirmed); **4 NEW OQs opened**, all by the dispatch-phase/per-report intake blocks (none by finalize). Serial apply per staging-row ORDER (newest-LAST authoritative; `applied_at` advisory): D1 (sparameter_reduce 2nd-gate — created the staging log) → D2 (eigenfreq_qfactor_reduce 2nd-gate) → D3 (domain_energy_reduce L4 verb authored) → D4 (combinator-miner confirm-probe, observation-only, LAST). Partition: `L4/index.md` was touched by all of D1/D2/D3 — each per-report integrator re-read fresh and preserved the prior cycle-079 refreshes (the two status-cell refreshes preserved when D3 added its new row + bumped the rough-in tally); otherwise byte-disjoint. No file collision.

### Follow-ons — for the cycle-080 planner

- The `sparameters.L1.md` PROSE down-link repoint (`:39,60,64` `bilinear-form`→`port_projection`) was DEFERRED (one-theme-per-invocation) — route as a next-cycle `layer-intro-author`/`lifter` follow-up.
- The `eigenfrequency-qfactor.L4.md` Status opening paragraph (~line 68) still carries the old κ-participation "rough-in" reasoning — a minor next-cycle prose cleanup.
- **`matrix-weighted-norm`'s test-coverage gate is incidentally addressable via `test-domainpostoperator.cpp` (`GetElectricFieldEnergy` positive SI-energy assertion) — a STRONG next-cycle candidate** that would firm `domain_energy_reduce`'s energy-form constituent gate (surfaced by the c079 planner).
- Successor firming OQs: `eigenfreq-qfactor-reduce-firm-needs-l1-eigenvalue-untransform-primitive` (harvest the eigenvalue-un-transform L1 primitive + an assembly test) + `domain_energy_reduce-promotion-double-gated` (the energy-form firm + a per-domain participation test).
- `record-DomainOpMap-promote-watch` (promote `DomainOpMap` to a `concepts/` record-definition page when a 2nd consumer names it) + the possible `problems/` drive-by `domain-field-energy-participation-guard-inconsistency`.

- Verdict: pass.
- Friction: none this cycle (staging-completeness gap did not recur; 74th consecutive clean split-integrator cycle; 0 dispatch-phase leaks; retroactive-budget global = 0).
