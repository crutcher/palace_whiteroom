## 2026-06-06 cycle-112 — 2 reports applied clean — 107th consecutive cycle under split integrator — **POSITION 1/3 OF META-BATCH-36, THE OPENER (cycles 112/113/114; the batch-36 meta-phase fires AFTER cycle-114's finalize, aggregating 112/113/114)** — two FRONTMATTER-ONLY graded-stack P1 lazy-tail typing dispatches in the LEAD `graded-stack-lazy-tail-typing` (D1: `rank:`+`edges:` on `L3/orthogonalize`+`L3/nrm2`, the faithful `L3/nrm2 → L2/nrm2` RE5 transitive-ground flips `L2/nrm2` reachable; D2: `rank:`+`edges:` on `L3/scal`+`L3/linear_combination`, zero standalone delta — representation upgrade) → TRUE CUMULATIVE `reachable` 122→123 (+1, all D1; D2 neutral), `detritus` 137→136 (−1); `rank_violations` HELD 0, `unresolved` HELD 0, `untyped` HELD 60 — pass

> Note: this file supersedes a stale pre-redirect-era `cycle-112` log entry (old slice-vertical numbering, 2026-05-26, `sparse_triangular_solve`); the cycle counter collided across the 2026-05-26 structural redirect. The current cycle-112 is the batch-36 opener under the layered-stack flow. Old content recoverable from git history.

---

**POSITION 1/3 OF META-BATCH-36 — THE OPENER** (3:1 cadence; cycles 112/113/114; the cycle counter does NOT reset across batch boundaries; **the batch-36 meta-phase fires AFTER cycle-114's finalize, as a SEPARATE dispatch aggregating 112/113/114** — this finalize ran NO meta-phase housekeeping). Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) + the 2026-06-02/2026-06-03 user directives + the 2026-06-04 GRADED RESOLUTION LADDER + FEATURE-ROOT REACHABILITY user directive (`METHODOLOGY-GRADED-STACK.md`) + the 2026-06-05 GROUND-don't-remove grounding directive + the 2026-06-06 named-shape-groups L4-calculus notation (codified batch-35 meta-phase). The most recent commits are the user calculus-notation follow-ups (`9ac3554`, etc.) + the batch-35 meta-phase (`ec260e9`); cycle-112 is the first primary cycle of batch-36. No `.claude/agents/` changes this cycle → no session-restart concern from finalize.

### Headline — the L3 lazy-tail mid-node typing pass; `L2/nrm2` grounded via the faithful adjacent-layer `L3/nrm2 → L2/nrm2` edge (`reachable` 122→123)

Two frontmatter-only typed-edge migrations in the LEAD `graded-stack-lazy-tail-typing` campaign. The measurable value this cycle is the **representation upgrade** (legacy `lowers_to`/`lifts_from` → canonical `rank:`+`edges:`) plus **one faithful adjacent-layer ground** (D1's `L3/nrm2 → L2/nrm2`, +1 reachable). The campaign's `untyped`-count expectation needs re-baselining — see the F1 cross-cutting signal below.

### What landed

- **D1 (layer-intro-author, build-relevant, THE LEAD) — `L3/orthogonalize` + `L3/nrm2` typing.** Frontmatter migrate legacy `layer:`/`firmness:`/`lifts_from:`/`lowers_to:` → a `rank:` token + a typed `edges:` block (block-mapping `- target:`/`kind:` form), `variant_axes:` preserved verbatim. `book/src/L3/orthogonalize.md` → `rank: partial-obstruction` (+ `obstruction_resolution: firm`); **HELD GARBAGE** — RE2 honored, only outbound edges authored, no forced inbound depender (verified `[GARBAGE*]` in linter output). `book/src/L3/nrm2.md` → `rank: firm`; the genuine adjacent-layer `L3/nrm2 → L2/nrm2` lowers-to `depends-on` edge from the already-reachable `L3/nrm2` GROUNDS the previously-unreachable `L2/nrm2` → `reachable` 122→123 (+1), `detritus` 137→136 (−1). This is RE5 transitive-grounding, NOT a manufactured flip. The `L3/orthogonalize → L3-L2/orthogonalize-variant-split` rescue edge is structurally-correct-but-latent (rides RE2; rescued only when a faithful reachable L3-iteration-view consumer lands → OQ `re2-shadows-orthogonalize-variant-split-theme`). One edge-label-fidelity critic warning (a mis-stated PRECEDENT in the rationale prose, NOT an edge defect) was repaired in-place pre-integration; the edges themselves are sound. Promoted 3 OQs: `re2-shadows-orthogonalize-variant-split-theme`, `lazy-tail-untyped-no-decrement-for-legacy-edged-files` (the F1 finding), `obstruction-resolution-firm-linter-keying-untested`.
- **D2 (layer-intro-author, build-relevant) — `L3/scal` + `L3/linear_combination` typing (FRONTMATTER-ONLY).** Frontmatter migrate legacy `firmness:`/`lowers_to:`/`lifts_from:` → `rank: firm` + a typed `edges:` block in the BARE-SLUG surface form (no `kind:` qualifier — mirrors the already-typed sibling `L3/dot`): `book/src/L3/scal.md` (`depends-on: [L2/linear_combination]`; `reference: [L3/linear_combination, L1/scal, L2-L1/linear-combination-fold-specialization]`) and `book/src/L3/linear_combination.md` (`depends-on: [L2/linear_combination]`; `reference: [L4/linear_combination, L2-L1/linear-combination-fold-specialization]`). **ZERO standalone delta** exactly as the producer predicted — both files already carried legacy `lowers_to`/`lifts_from` and were shim-counted typed before the edit, so `untyped` HOLDS (not 60→58); a representation upgrade, not a typed-count change. `L3/scal` verified NOT in untyped + still in detritus `[GARBAGE*]` (GROUND-candidate per OQ `L3-scal-reachable-via-normalize-grounding` — NOT force-flipped); `L3/linear_combination` verified reachable via `L4/linear_combination`. `rank: firm` rests on `L2/linear_combination` which is `rank: firm` on-disk (firm-rests-on-firm holds). Promoted 2 OQs: `L3-scal-reachable-via-normalize-grounding`, `linter-legacy-shim-line-citation-527-532-not-546-547`.
- **TRUE CUMULATIVE (both applied, re-measured by integrator-finalize on the landed tree):** `reachable` 122→123 (+1, all D1; D2 neutral); `rank_violations` HELD 0; `unresolved_depends_on_targets` HELD 0; `untyped` HELD 60; firm histogram HELD; `detritus` 137→136 (−1). Do NOT sum per-dispatch isolation deltas — the 123 is the re-measured cumulative figure.

### Cross-cutting signal F1 (carried to the batch-36 meta-phase) — the lazy-tail `untyped` count does NOT decrement for legacy-edged files

The linter auto-migrates pre-existing legacy `lowers_to`/`lifts_from` fields (shim-counting those files as typed before any edit), so typing a file that already carried legacy edges does NOT move the `untyped` count — only the representation upgrades. Consequence: the remaining `untyped`-60 tail is **mostly truly-edge-less pages** (L0 ground-truth, frozen meta-reviews, methodology/design/SUMMARY) NOT the L3 mid-nodes the campaign is sweeping. The campaign's `untyped` expectation needs re-baselining; the measurable lazy-tail value is (i) the representation upgrade legacy→canonical `edges:` and (ii) faithful adjacent-layer grounds like D1's +1, NOT the `untyped` count. Filed as OQ `lazy-tail-untyped-no-decrement-for-legacy-edged-files`; routed to the batch-36 meta-phase.

### Build

`cargo make book` (mdbook + linkcheck2) EXIT 0. All 4 touched `book/` files are frontmatter-only edits with on-disk edge targets → linkcheck2-clean; no new file → no SUMMARY/index insert. Only the pre-existing benign `Potential incomplete link` WARNs (135; markdown-table / KaTeX false-positives, NOT link errors) + the long-standing KaTeX render WARNs on lambda-binder / underline-combining-char constructs (content kept, not on this cycle's files). **NO finalize build-repair needed.**

### Step-5b — graded-stack linters (the build-gate companion, ran on the landed tree)

**`rank_violations: 0`** (`[]`; baseline fully discharged c096 so ANY violation would be NEW and BLOCK — there are NONE, **GATE PASSES**; D1's `nrm2` firm rests on firm `L1/nrm2`/`L2/nrm2`/`L3/dot` and the `orthogonalize` partial-obstruction outbound edges are RE2-honored; D2's two firm nodes rest on `L2/linear_combination` firm) + **NO newly-orphaned node** (`reachable` CLIMBED 122→123) + **`unresolved_depends_on_targets: 0`** (HELD). Totals on the landed tree: `files=355`, `typed=295`, **`untyped=60`** (HELD), `roots=36`, **`reachable=123`** (was 122, +1), `rank_violations=0`, `unresolved_depends_on_targets=0`, `promotion_frontier=8`, **`detritus=136`** (`detritus_no_typed_edges_pre_p1_artifact=111`, `detritus_with_typed_edges_stronger_signal=25`, `expected_unreachable_outside_dag=44`).

**rank_violations trend: 22 (c094) → 1 (c095) → 0 (c096) → … → 0 (c110) → 0 (c111) → 0 (c112). reachable trend across the campaign: 36 (c105-end, pre-linter-fix) → 81 (batch-33 meta-phase) → 88 (c106) → 95 (c107) → 102 (c108) → 107 (c109) → 119 (c110) → 122 (c111) → 123 (c112).** c112 ground `L2/nrm2` via the faithful `L3/nrm2 → L2/nrm2` adjacent-layer edge.

**On the high `untyped`/`detritus` mass:** informational, NOT a block (per step-5b — the as-yet-untyped pre-P1 tail + the typed-non-node reference-only pages + the typed-but-unreached nodes). Only a *new* rank violation or a *newly*-orphaned node gates; neither occurred. The STRONGER GARBAGE SIGNAL (`detritus_with_typed_edges_stronger_signal`) moved 26→25 (the −1 cleared one typed-but-unreached node — `L2/nrm2` flipping reachable).

### Counts

No firm-count status flips this cycle — both dispatches are frontmatter-only (no new authored vocabulary, no node promotion). Layer tallies unchanged. SLICE CORPUS: 0 (corpus fully lifted + deleted across c097/098/099). The measurable movement is entirely on the reachability axis (`reachable` 122→123, `L2/nrm2` grounded).

### Process

Retroactive-budget global = 0. Per-report safety-net gates all PASS/N/A across both rows (rank-well-foundedness 0, edge-label/prose-mismatch 0, YAML round-trip 0, SUMMARY-registration 0, forward-edge-without-surface 0). 0 implied-component stubs created. 2/2 staging rows == 2 dispatched-ready reports — the cycle-018 staging-completeness gap did NOT recur (93rd consecutive clean staging / 107th consecutive clean split-integrator cycle). **REPAIR PHASE DID RUN on D1 ONLY** (one edge-label-fidelity warning fixed in-place pre-integration; the `edges:` blocks left untouched; D2 was an all-pass clean critic-set set ready directly). **CITECHECK NOTE (non-blocking):** the per-report integrators noted AMBIG/MISS warnings — ALL on report PROSE / OQ-prose / linter-tool-source references (book-relative chapter-prose pointers + a `graded_stack_lint.py:518-547` tool-source pointer), NONE in any applied `edges:` block; no prose-claim text lands in `book/` (frontmatter-only edits), so all are non-blocking and recorded-not-repaired. 5 OQs promoted to the batch-36 meta-phase (append-only between meta-phases; the meta-phase unifies/closes). `scaffolding/{open-questions,roadmap,integrator-signals,cycle-record}.md/.jsonl` + `log/` committed atomically with the per-report `book/` edits + consumed-report `integrated_at` touches. NO `.claude/agents/` changes → no session-restart concern from finalize.

### Signals for the batch-36 meta-phase (fires AFTER cycle-114's finalize, aggregating 112/113/114)

- **F1 — the lazy-tail `untyped` count does NOT decrement for legacy-edged files** (the linter auto-migrates `lowers_to`/`lifts_from`); the remaining `untyped`-60 tail is mostly truly-edge-less pages (L0/meta-reviews/methodology/design/SUMMARY) NOT L3 mid-nodes — RE-BASELINE the campaign's `untyped` expectation; the measurable value is the representation upgrade + faithful grounds, not the count. OQ `lazy-tail-untyped-no-decrement-for-legacy-edged-files`.
- **`re2-shadows-orthogonalize-variant-split-theme`** — the `L3/orthogonalize → L3-L2/orthogonalize-variant-split` rescue edge is structurally-correct-but-latent (rides RE2); rescued only when a faithful reachable L3-iteration-view consumer lands.
- **`obstruction-resolution-firm-linter-keying-untested`** — the `obstruction_resolution: firm` token on `L3/orthogonalize`'s `rank: partial-obstruction` is not exercised by the linter's keying; confirm the linter reads it as intended.
- **`L3-scal-reachable-via-normalize-grounding`** — `L3/scal` is a GROUND-candidate (still `[GARBAGE*]`); a faithful reachable consumer (e.g. via `normalize`) would flip it; NOT force-flipped this cycle.
- **`linter-legacy-shim-line-citation-527-532-not-546-547`** — a line-citation correction on the linter's legacy-shim block (the cited lines are `527-532`, not `546-547`).
