## 2026-06-07 cycle-123 — 4 reports applied clean — 118th consecutive cycle under split integrator — **POSITION 3/3 OF META-BATCH-39, THE BATCH-CLOSING / THIRD PRIMARY CYCLE (cycles 121/122/123; the batch-39 meta-phase fires AFTER this finalize, aggregating all three)** — **RE2 + RE8 DISCHARGED VIA A REAL DEPENDS-ON REACHABILITY FLIP (the krylov-iteration infrastructure column) + the carried AMR navigational hygiene + the RefinementData record-definition page; `rank_violations` HELD 0; `unresolved_depends_on_targets` HELD 0; the reference-edge-liveness scheme question now has a CLEAN CONTRASTING DATA CLASS; residual RE set = RE3 + RE6.** The third and final cycle of the 2026-06-07 RE-SCOPE batch; the L3 iteration-views consumer + the carried-over AMR cleanup + the record-definition home. 4 dispatches, ALL applied clean. `cargo make book` EXIT 0, ZERO finalize build-repairs; graded-stack linter `rank_violations=0` (HELD) / `unresolved_depends_on_targets=0` (HELD) / RE2+RE8 nodes FLIPPED reachable.

# cycle-123 — 2026-06-07 — batch-39 position 3/3 (the BATCH-CLOSING / THIRD primary cycle)

**Meta-batch-39, position 3/3 (BATCH-CLOSING).** Cycles 121/122/123 form meta-batch-39; the batch-39 meta-phase fires AFTER this finalize, aggregating all three as a separate dispatch. The cycle counter does NOT reset at batch boundaries. This finalize ran NO meta-phase housekeeping.

(Note: an unrelated slice-vertical-era `cycle-123` log from 2026-05-26 — `cg_preconditioning_framework` — was renamed to `log/cycle-123-slice-era.md` at this finalize to free the filename for the live layered-flow cycle-123; the cycle counter collided across the pre/post-redirect eras.)

Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT + the 2026-06-04 GRADED RESOLUTION LADDER + FEATURE-ROOT REACHABILITY directive + the 2026-06-05 GROUND-don't-remove directive + the 2026-06-06 SEMANTIC CONSOLIDATION + OPEN-ALL-FEATURE-FRONTS directives + **the 2026-06-07 RE-SCOPE (DIRECTIVE-1/2/3)**.

## Headline

**RE2 + RE8 DISCHARGED via a REAL depends-on reachability flip (the krylov-iteration infrastructure column), plus the carried AMR navigational hygiene and the RefinementData record-definition home.** 4 dispatches, ALL applied clean (4/4 staging rows == 4 dispatched-ready; the cycle-018 staging-completeness gap did NOT recur — 104th consecutive clean staging). Zero deferrals, zero rejections, zero per-report gate-hits, ZERO finalize build-repairs.

Headline outcomes:
1. **RE2 (`L3/orthogonalize`) + RE8 (`L3/krylov-step`, `L3/fold_solve`) DISCHARGED** via the new krylov-iteration infrastructure feature column's blocking `depends-on (composes)` edges — a GENUINE depends-on root→node reachability flip (all three confirmed REACHABLE on the landed tree, out of the STRONGER/detritus lists), mechanically DISTINCT from the c122 reference-only-reachable cohort.
2. **The deferred-from-c122 AMR navigational hygiene LANDED** — group-intro authored, the 2 flat AMR verbs re-nested under their by-kind SUMMARY grouping, the `index.md` dep-map group header de-staled. OQ `amr-estimate-mark-group-intro-needs-authoring` CLOSED.
3. **The `RefinementData` record-definition concepts page landed firm** (≥2-consumer bar met). OQ `record-RefinementData-needs-concept-definition-home` CLOSED.
4. **The 2 batch-39-meta headline items are set up cleanly** (see below).

## Dispatches (apply-order)

- **D4 (layer-intro-author, `refinement-data-concepts-page`)** — landed `book/src/concepts/RefinementData.md` FIRM (the AMR config record-definition page; ≥2-consumer bar met by `dorfler_mark` + `amr-estimate-mark-refine` + the lifecycle.L4 AMR fold) + re-pointed the consumers' θ-field / `RefineConfig` references to the new page (struct cite `:97-119`/`:96-125` → `:97-154`). Registered in the SUMMARY concepts list alpha between `rotation` and `scal`. Integrated FIRST so D1's forward link resolves. CLOSES OQ `record-RefinementData-needs-concept-definition-home`.
- **D1 (layer-intro-author, `amr-estimate-mark-group-intro`)** — created `book/src/L1/amr-estimate-mark-intro.md` (a `navigational-container (group intro)`, NO `rank:`, `reference`-only to the 2 AMR verbs) + re-nested the 2 flat AMR verbs under the `AMR estimate / mark vocabulary` SUMMARY grouping (after the FE-space sub-spine grouping) + de-staled the `index.md` dep-map group header (`**Rough-in (AMR estimate/mark vocabulary)**` → `**AMR estimate/mark vocabulary**`; both verb rows already `firm`). The c122-deferred navigational hygiene. CLOSES OQ `amr-estimate-mark-group-intro-needs-authoring`.
- **D2 (layer-intro-author, `krylov-iteration-column`)** — landed the KRYLOV-ITERATION INFRASTRUCTURE FEATURE COLUMN at L4+L1 (`book/src/feature/krylov-iteration.{L4,L1}.md`, `feature_root: seed`, `rank: rough-in`; the L3 iteration-rotation composition-root). Its blocking `depends-on (composes)` edges to `L3/krylov-step` + `L3/fold_solve` + `L3/orthogonalize` produced a GENUINE depends-on reachability flip that DISCHARGES RE2 + RE8 (all three REACHABLE on the landed tree). `L3/eigsolve-impl` + `L3/lanczos_step` correctly STAYED detritus (the `reference` edges to them gained no liveness — the correct second data class). Landed `rough-in` (NOT firm): well-foundedness CAPS it — its blocking L3 deps are `partial-obstruction` (≈2.5), so `firm` would VIOLATE `rank(u) ≤ min deps`. Also KEPT the honest GMG member-status `(rough-in.)`→`(firm.)` reconcile in `feature/infrastructure.md`. Promoted OQ `krylov-iteration-rough-in-vs-firm-over-partial-obstruction-iteration-views` (the firm-vs-rough-in adjudication for the meta) + an optional cosmetic `eigsolve-impl-reference-uplink-to-krylov-iteration-column`.
- **D3 (same-layer-cross-cutter, `correction-step-wider-propagate`)** — PURE OBSERVATION (no book edit; no `## Proposed changes`). CLOSED 2 OQs (`correction-step-wider-replace-and-propagate-set-l1-and-feature-column` c122-opened + `correction-step-replace-and-propagate-scope` c121-opened, transitive) + promoted the NEW OQ `correction-step-l4-reference-edge-adds-to-reference-only-reachable-liveness-evidence` flagging the candidate L4 `feature/geometric-multigrid-preconditioner.L4 → L2/correction_step` reference down-link as COMPLEMENTARY reference-only-reachable meta-evidence (the candidate edge is NOT enacted this cycle — surfaced for a future producer dispatch).

## Build + step-5b linters (landed tree)

- **`cargo make book`** (mdbook + linkcheck2 0.12.0) **EXIT 0**, NO finalize build-repair. D1's group-intro re-nest + D4's RefinementData page + D1's link to it + the new krylov-iteration column SUMMARY/index entries ALL resolve clean; 0 dead links. Only the pre-existing benign `Potential incomplete link` / HTML-tag-in-prose WARNs in unrelated files remain (+ one benign `<key>` definition-list HTML-tag WARN in the new `concepts/RefinementData.md`, NOT a linkcheck failure).
- **Graded-stack linters — both step-5b block-conditions PASS:**
  - **`rank_violations: 0`** (baseline fully discharged c096 → ANY violation would be NEW + BLOCK; NONE — GATE PASSES). The krylov-iteration column landed `rough-in` PRECISELY to keep `rank(u) ≤ min deps` over its partial-obstruction L3 deps; RefinementData firm rests only on `cites-evidence` L0-ground depends-on (vacuous).
  - **NO newly-orphaned node** — no previously-reachable node went dark; the RE2/RE8 nodes FLIPPED reachable (a gain, not a loss); `L3/eigsolve-impl` + `L3/lanczos_step` stayed detritus as designed.
- **Totals (re-measured on the landed tree):** `files=389 (+4), typed=328 (+4), untyped=61 (HELD), roots=43 (+2 the krylov-iteration L4/L1 feature_root: seed), reachable=158, rank_violations=0 (HELD), unresolved_depends_on_targets=0 (HELD), promotion_frontier=14, detritus=132 (no_typed_edges=108, stronger=24), expected_unreachable_outside_dag=47, rank_histogram={firm:225, roadmap_goal:7, typed-no-rank:83, rough-in:4, partly-constructive:3, obstruction:2, partial-obstruction:4}`.
- **Trend:** `rank_violations` HELD 0 (… → 0 c121 → 0 c122 → 0 c123); `unresolved_depends_on_targets` HELD 0 (c122→c123); `reachable` 150 → 158 (RE2/RE8 depends-on flip + new column nodes).

## ⟢ The two batch-39-meta headline items

**(a) The REFERENCE-EDGE-LIVENESS SCHEME QUESTION (c122 headline, persists — now with a CLEAN CONTRASTING DATA CLASS).** firm/roadmap_goal nodes reachable from the roots ONLY via `reference`-class edges (combinator-primary specialization-notes, `realizes-kernel-api`, kernel-impl realizes, the c123-D3 candidate L4 `correction_step` down-link) are flagged `[GARBAGE*]` by the depends-on-only GC. **The c123-D2 RE2/RE8 discharge gave a clean CONTRASTING data class** — a REAL depends-on reachability flip (krylov-step/fold_solve/orthogonalize → REACHABLE), mechanically distinct from the reference-only-reachable cohort (eigsolve-impl/lanczos_step stayed detritus; the correction_step candidate would stay detritus too). The batch-39 meta now has BOTH classes side-by-side to adjudicate: **do `reference`-class edges to root-reachable nodes count toward liveness?**

**(b) The ROUGH-IN-vs-FIRM-OVER-PARTIAL-OBSTRUCTION question** (c123-D2 OQ `krylov-iteration-rough-in-vs-firm-over-partial-obstruction-iteration-views`). The krylov-iteration column landed `rough-in` (well-foundedness caps it — its blocking L3 deps are `partial-obstruction`) vs the GMG firm-on-positive-structure precedent (where the blocking dep was firm). Meta adjudication.

## RE-discharge state (the batch-39 close)

- **RE2 (`L3/orthogonalize`) + RE8 (`L3/krylov-step`, `L3/fold_solve`) DISCHARGED this cycle** via the krylov-iteration column's REAL depends-on reachability flip (distinct from the reference-only-reachable cohort — confirmed on the landed tree).
- **CUMULATIVE BATCH-39 RE DISCHARGES:** RE10 (c121) + RE9/RE1/RE5/RE7 GROUNDED (c121-c122 GMG) + RE2/RE8 (c123).
- **RESIDUAL RE SET = RE3** (deflate/NLEPS — consumer-gated; lifts when its NLEPS-deflated-eigensolve/preconditioner consumer surfaces) **+ RE6** (axpy-family arity leaves — the combinator-arity-notes refactor).
- The batch-36→38 "RE set = permanent faithful floor" framing is fully superseded: under DIRECTIVE-2 the in-scope RE set was a DISCHARGE TARGET; across batch-39 the lift-through campaign discharged/grounded 8 of the 10, leaving 2 genuinely consumer-/refactor-gated residuals.

## Counts

- +1 firm concepts page (`RefinementData`).
- The krylov-iteration column landed `rough-in` (Infrastructure kind, tracked separately; +2 GC roots → 43).
- 0 main-cohort firm promotions this cycle (D2 rough-in, D1 navigational + hygiene, D4 concepts page, D3 observation-only).
- SLICE CORPUS: 0.

## Process

- retroactive-budget global = 0; per-report gates all PASS / N/A; 0 implied-component stubs; ZERO finalize build-repairs.
- OQs promoted/closed by the per-report integrators this cycle (finalize made no duplicate append): CLOSED `record-RefinementData-needs-concept-definition-home`, `amr-estimate-mark-group-intro-needs-authoring`, `correction-step-wider-replace-and-propagate-set-l1-and-feature-column`, `correction-step-replace-and-propagate-scope`; promoted `krylov-iteration-rough-in-vs-firm-over-partial-obstruction-iteration-views`, `eigsolve-impl-reference-uplink-to-krylov-iteration-column`, `correction-step-l4-reference-edge-adds-to-reference-only-reachable-liveness-evidence`.
- `scaffolding/{roadmap,integrator-signals,cycle-record}` + `log/` committed atomically + the 4 consumed-report `integrated_at` touches.
- Two-phase SHA-patch follows.
- NO `.claude/agents/` changes FROM THIS FINALIZE.

## The carry to the batch-39 meta-phase (fires next, aggregating 121/122/123)

1. **The REFERENCE-EDGE-LIVENESS SCHEME QUESTION** — now adjudicable with BOTH data classes: the depends-on flip (c123-D2) vs the reference-only-reachable cohort (c122 cohort + the c123-D3 `correction_step` candidate).
2. **The ROUGH-IN-vs-FIRM-over-partial-obstruction adjudication** (OQ `krylov-iteration-rough-in-vs-firm-over-partial-obstruction-iteration-views`).
3. **The residual RE3** (deflate/NLEPS, consumer-gated) **+ RE6** (axpy-arity-notes refactor) — the only 2 remaining baseline-exceptions after batch-39's 8 discharges/groundings.
4. The candidate L4 `correction_step` reference down-link + the 2 L1 downward annotations (a future producer dispatch, per the closed `correction-step-wider-...` OQ).
5. The carried optional `eigsolve-impl-reference-uplink-to-krylov-iteration-column` cosmetic uplink + the V-cycle recursive-combinator + MultigridConfig record-definition mining candidates.

Written by `integrator-finalize` (split integrator-per-report ×4 + finalize ×1).
