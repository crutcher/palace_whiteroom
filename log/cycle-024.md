# Cycle 024 — NEP-interior cohort COMPLETE + eigsolve chain COMPLETE; crash-recovered (third/final primary cycle of meta-batch-6)

**Date:** 2026-05-29 · **Commit:** `INTEGRATION_COMMIT_SHA` · **Status:** clean (zero deferrals/rejections/rework; zero build-repairs; twentieth consecutive clean split-integrator cycle)

**Batch position:** cycle-024 is the **THIRD/FINAL** primary cycle of meta-batch-6 (cycles 022/023/024). **The batch-6 meta-phase fires after THIS cycle-024 finalize commit** (3:1 cadence; cycle counter does NOT reset) — it is dispatched separately. This `log/cycle-024.md` + the `scaffolding/integrator-signals.md` cycle-024 section close the batch-6 evidence window opened by cycle-022.

**Recovery note:** this cycle was **recovered after a mid-cycle machine crash**. All eight `integrator-per-report` runs had completed and appended their STAGING.md rows before the crash; `integrator-finalize` ran clean on the staged state (staging-log + working-tree cross-check reconciled, 8 rows == 8 dispatched ready reports). No reconciliation-from-working-tree recovery was needed — the staging log was authoritative. Second consecutive crash-recovered cycle (cycle-023 also crash-recovered); both validate the split-integrator design's crash-resilience.

> Note: this file replaces a legacy `cycle-24` stub (2026-05-24 "forward gmres [L1→L2]" era, pre-structural-redirect) that shared the same path; the prior content is preserved in git history.

## What landed (8 reports — all wave-1)

- **`nleps_jacobian_action` L1 operator — NEW firm file** (harvester) — `book/src/L1/nleps_jacobian_action.md`: the quasi-Newton **`T'(λ)`-derivative-pencil action** (`palace/linalg/nleps.cpp:649-669`); the fourth NEP-interior atom; firm via the firm-on-positive-structure escape (single positive site, every constituent read-not-constructed, laws are syntactic operator-algebra identities). The divided-difference `A2'` (quasi-Newton approximate Jacobian, `δ = √ε`) is recorded as a **load-bearing non-law**, NOT a `partly-constructive` caveat. Realizes the `T'` derivative-pencil that `apply_nonlinear_pencil` law 5 had recorded as a deferred follow-up. L1/index Firm-count headline **17→18** + cohort bullet + dep-map row; SUMMARY `:78`.
- **`nleps_eigenvalue_correction` L1 operator — NEW firm file** (harvester) — `book/src/L1/nleps_eigenvalue_correction.md`: the per-step **`δλ` Rayleigh-functional eigenvalue correction** (`palace/linalg/nleps.cpp:672-677`), the **FIFTH and FINAL** deflated NEP-interior atom, over the firm BLAS-1 leaves dot/axpby/scal; firm-on-positive-structure. L1/index Firm-count headline **18→19** (reconciled additively from the parallel jacobian-action 17→18) + cohort bullet + dep-map row; SUMMARY `:79`. **IN-CYCLE LIVE-LINK UPGRADE**: its two plain-text `nleps_jacobian_action` forward-references were upgraded to live links after the sibling landed firm earlier this cycle (the canonical upgrade-once-sibling-lands path). **L1 firm 17 → 19.** **The NEP-interior atom cohort is now COMPLETE** — the full per-step quasi-Newton chain `residual → jacobian-action → eigenvalue-correction → deflated-solve → line-search` is firm at L1 (5 atoms: apply_nonlinear_pencil c021, nleps_deflated_residual c022, nleps_deflated_solve c023, nleps_jacobian_action c024, nleps_eigenvalue_correction c024).
- **`nleps-deflated-solve-mutation-rotation` L1>L0 theme — NEW firm file** (abstractor) — `book/src/L1-L0/nleps-deflated-solve-mutation-rotation.md`: the Schur-complement **deflated block-solve mutation rotation** (`nleps.cpp:504-537`); firm-on-positive-structure (composes only already-firm leaves; no partly-constructive sub-part, no negative anchors). ENACTS the cycle-023 OQ `nleps-deflated-solve-l1-l0-lowering-theme` (`:784`). CONFIRMS — does not change — the cycle-022 L2 `deflate` partly-constructive verdict from a lowering viewpoint (the Gram is built positively but never solved bare, only Schur-wrapped). L1-L0/index dep-map firm row; SUMMARY under the L1>L0 Part.
- **`apply-nonlinear-pencil-mutation-rotation` L1>L0 theme — NEW firm file** (abstractor) — `book/src/L1-L0/apply-nonlinear-pencil-mutation-rotation.md`: the bare-pencil **residual-apply rotation** (`nleps.cpp:807-821` `GetResidualNorm` + 4 corroborating sites); 3 sub-patterns A/B/C + 4 axes; firm-on-positive-structure. The **LAST plain-text-forward-referenced NLEPS L1>L0 leaf** — with this landing the NLEPS L1>L0 **deflation + bare-pencil** cohort is complete (`lu-solve` c023, `nleps-deflated-residual` c023, `nleps-deflated-solve` c024, `apply-nonlinear-pencil` c024). One load-bearing recording: the form-A-vs-form-B accumulation-order bit-difference. **CITATION NOTE**: per dispatch directive the `nleps.cpp:810-811` residual-comment citation is CORRECT as-is — the critic's cosmetic off-by-one finding was itself off by one; no `:809-810` shift applied. L1-L0/index dep-map firm row; SUMMARY under the L1>L0 Part. **L1>L0 firm themes +2 (with the deflated-solve theme above).**
- **`gram-fold-specialization` L2>L1 theme — NEW firm file** (abstractor) — `book/src/L2-L1/gram-fold-specialization.md`: the **matrix-valued sibling of `inner-product-fold-specialization`** — the `XᴴX` Gram-build (`nleps.cpp:524-531`, cell body `:529`) lowering via per-cell `dot`; conjugation/element-type/weight/single-vs-cross-Gram axes; firm via read-direct positive evidence composed pointwise with the firm sibling scalar theme. FENCE PARITY verified (8 backtick lines = 4 balanced `text` pairs; 2 balanced `$$` math pairs). The FIFTH chapter under the L2-L1 Part. L2-L1/index theme-list row + SUMMARY under the L2>L1 Part.
- **`deflate-composition-lowering` L2>L1 theme — NEW `partly-constructive` file** (abstractor) — `book/src/L2-L1/deflate-composition-lowering.md`: inherited verbatim from the L2 `deflate` entry. **Firm part** = the Schur-form fan-down (every stage read from the positive `deflated_solve` block `nleps.cpp:505-537`, all firm L1/L2 leaves). **Constructive sub-part** = the Galerkin-core single bare-Gram `lu_solve(XᴴX, c)` fan-down (`op.block = Galerkin`, `S = I`); negative anchors = no bare-Gram deflation solve anywhere in `palace/linalg/*.cpp`, only Schur-wrapped + literature; explicit promotion condition = a positive bare-Gram-solve site outside the Schur wrapping. **The gate STAYS OPEN — the SAME single gate** shared by the L2 `deflate` entry (`:774`), the L1>L0 `nleps-deflated-solve-mutation-rotation` theme, and now this L2>L1 theme; all three promote together; this theme "does not make that call and does not close the gate." **IN-CYCLE LIVE-LINK UPGRADE**: the two repairer-defanged `gram-fold-specialization` forward-references were re-linked to live links after the sibling landed firm earlier this cycle. The SIXTH chapter under the L2-L1 Part. **L2>L1 +1 firm + 1 partly-constructive (4 → 6 chapters); the NLEPS-deflation L2>L1 pair (`gram` builds, `deflate` solves) is now complete.**
- **`eigsolve` L3 — REFINED stub → `partial-obstruction`** (harvester) — `book/src/L3/eigsolve.md`: the cycle-023 `stub` fully replaced in place with the firm `partial-obstruction` body (zero stub residue). The per-step `apply_shift_invert` body lifts **identity-in-form** to the firm L2 body (laws are syntactic identities on positive `ApplyOp`/`__pc_apply_EPS` source); the **eigen-iteration loop is a witnessed `sequential-obstruction`** rooted in opaque-library ownership — SLEPc `EPSSolve` `slepc.cpp:694` / ARPACK `naupd` RCI `arpack.cpp:318` own the loop, no Palace-authored kernel/driver pair analogous to `(krylov-step, ksp_solve)`. This is the **SECOND L3 partial-obstruction** (after `chebyshev` c013, a numerical-stability obstruction) and the **FIRST opaque-library one**. **CONFIRMS the cycle-021 prediction** `l3-eigsolve-linear-evp-has-no-krylov-step-kernel-analog`. The speculative L4 `eigsolve` solve-monad surface is correctly NOT stubbed (plain-text-defer — only-speculative, no firm L4 anchors converge). 5 corrected off-by-one citations applied by the repairer (arpack.cpp :573/:579/:270; slepc.cpp :1857/:1873). L3/index dep-map row + SUMMARY surgical `(stub)`-marker drop at `:31`. **L3 9 firm + 2 partial-obstruction. The eigsolve L1→L2→L3 prerequisite chain is now COMPLETE** (L1-firm c022 → L2-firm c023 → L3-partial-obstruction c024).
- **`dot-mutation-rotation` §Sub-pattern D anchor fix — AUDITED, stays firm** (lowering-verifier) — `book/src/L1-L0/dot-mutation-rotation.md`: the cycle-023 carry-forward one-token anchor fix `orthog.hpp:34` → `:35` (the canonical `IdentityInnerProduct::operator()` returns `LocalDot(x, y)` at `:35`; `:34` is the operator-body opening brace `{`) at the §Sub-pattern D prose anchor (line 160) + the citation-list anchor (line 183), plus a `verified_against:` yaml block appended at EOF. Verdict supports-with-one-token-anchor-drift; theme STAYS `firm` (pure anchor-drift repair, no semantic edit). The only retroactive-evidence revision this cycle (1 slice). Matches the value the sibling `orthogonalize-composition-lowering` already used correctly; drift was confined to `dot-mutation-rotation`, now closed. **No count change.** RESOLVES the cycle-023 carry-forward OQ `dot-mutation-rotation-subpattern-d-citation-fix` (`:847`).

## Reports consumed (8)

| report | agent | status | follow-up |
|---|---|---|---|
| `2026-05-29T105500Z-harvester-nleps-jacobian-action-l1` | harvester | applied | `nleps-jacobian-action-firm-landed-cycle-024`; `nleps-interior-atoms-jacobian-action-half-resolved-eigenvalue-correction-remains`; `nleps-jacobian-action-mutation-rotation-l1-l0-lowering-theme` |
| `2026-05-29T105500Z-harvester-nleps-eigenvalue-correction-l1` | harvester | applied (in-cycle live-link upgrade) | `nleps-eigenvalue-correction-firm-landed-cycle-024-nep-cohort-complete`; `nleps-eigenvalue-correction-mutation-rotation-l1-l0-lowering-theme` |
| `2026-05-29T105500Z-abstractor-nleps-deflated-solve-rotation` | abstractor | applied | `nleps-deflated-solve-l1-l0-lowering-theme-firm-landed-cycle-024` (enacts `:784`); `nleps-l1-l0-lowering-cohort-deflation-themes-complete-pencil-leaf-remains` |
| `2026-05-29T105500Z-abstractor-apply-nonlinear-pencil-rotation` | abstractor | applied | `apply-nonlinear-pencil-mutation-rotation-l1-l0-landed-firm-cycle-024-nleps-l1-l0-cohort-complete`; `apply-nonlinear-pencil-mutation-rotation-lowering-verifier-audit-followup` |
| `2026-05-29T105500Z-abstractor-gram-fold-specialization` | abstractor | applied | `gram-fold-specialization-l2-l1-theme-firm-landed-cycle-024`; `gram-percell-dot-vs-fused-matmul-tree-loadbearing`; `gram-fold-specialization-l2-gram-forward-reference-closure-followup` |
| `2026-05-29T105500Z-abstractor-deflate-composition-lowering` | abstractor | applied (in-cycle live-link upgrade) | `deflate-composition-lowering-l2-l1-theme-partly-constructive-landed-cycle-024`; `deflate-composition-lowering-galerkin-core-promotion-gate-shared-with-l2-deflate-stays-open` (threaded to `:774`); `deflate-composition-lowering-mutation-rotation-lowering-verifier-audit-followup` |
| `2026-05-29T105500Z-harvester-l3-eigsolve-backfill` | harvester | applied | `eigsolve-l3-backfill-partial-obstruction-landed-cycle-024-chain-step-3-done`; `l3-eigsolve-linear-evp-has-no-krylov-step-kernel-analog-CONFIRMED-cycle-024`; `eigsolve-l4-surface-solve-monad-unauthored-future-dispatch`; `eigsolve-l3-l2-body-identity-audit-anchor-optional-followup` |
| `2026-05-29T105500Z-lowering-verifier-dot-anchor-fix` | lowering-verifier | applied | `dot-mutation-rotation-subpattern-d-orthog-anchor-fix-RESOLVED-cycle-024` (discharges `:847`) |

## Roadmap deltas

- **L1 firm 17 → 19** (+`nleps_jacobian_action`, +`nleps_eigenvalue_correction`). **NEP-interior atom cohort COMPLETE** (5 firm atoms). L1 rough-in (test-coverage-bounded) cohort unchanged at 2.
- **L1>L0 firm themes +2** (+`nleps-deflated-solve-mutation-rotation`, +`apply-nonlinear-pencil-mutation-rotation`). **NLEPS L1>L0 deflation+bare-pencil cohort COMPLETE.** The two NEP-interior-atom L1>L0 themes (jacobian-action, eigenvalue-correction) batch for a subsequent abstractor pass.
- **L2>L1 +1 firm (`gram-fold-specialization`) + 1 partly-constructive (`deflate-composition-lowering`)** — chapter count 4 → 6. **NLEPS-deflation L2>L1 pair COMPLETE.**
- **L3 `eigsolve` stub → `partial-obstruction`** — chain step 3 DONE; **eigsolve L1→L2→L3 chain COMPLETE** (9 firm + 2 partial-obstruction at L3).
- **`dot-mutation-rotation` §Sub-pattern D anchor fix** `orthog.hpp:34` → `:35` + `verified_against:` block (stays firm; carry-forward closed).
- **L2 unchanged: 8 firm + 1 partly-constructive (`deflate`) + 1 stub. L4 unchanged: 4 firm. L0 unchanged: 22 chapters. Phase-1 corpus removals stay 9/10.**
- **`deflate` partly-constructive status CONFIRMED, not changed** (the bare-Galerkin core never appears positively; promotion still gates on a positive bare-Gram-solve site outside the Schur wrapping — now triple-referenced: L2 `deflate` + L1>L0 `nleps-deflated-solve` + L2>L1 `deflate-composition-lowering`, all sharing one gate).

## Build

clean — `cargo make book` exit 0, no dead-link (`linkcheck2`) errors, **ZERO build-repairs**. All 6 new chapters (`nleps_jacobian_action`, `nleps_eigenvalue_correction`, `nleps-deflated-solve-mutation-rotation`, `apply-nonlinear-pencil-mutation-rotation`, `gram-fold-specialization`, `deflate-composition-lowering`) + the L3 `eigsolve` stub→partial-obstruction refinement + the `dot-mutation-rotation` anchor-fix are SUMMARY-registered and link-clean (all 7 HTML outputs render under `book/book/html/`). The L4 `eigsolve` solve-monad forward-reference is correctly plain-text-deferred (speculative, below the clearly-implied stub bar — NOT a dead link). The `Potential incomplete link` `katex` warnings (4 distinct in `design/l4_calculus.md`, the rest across `concepts/*` + `L4/iterate-while*` + `L3/{dot,nrm2}` + the `[...]`-bearing lowering/fold themes incl. the two new cycle-024 ones inside code/math spans) are ALL pre-existing math-display / code-bracket false-positives — NONE an actual dead link in any cycle-024-touched file (carried since cycle-015).

## Safety-net gates

- **retroactive-budget global = 1** (only the `dot-mutation-rotation` §Sub-pattern D anchor-fix + `verified_against:` append — per-slice 1. The other 7 rows are new firm/partly-constructive chapters + a stub-refinement-in-place, all 0-retroactive). Well below per-slice ≥3 / global ≥4 block thresholds.
- **implied-component-stub-created = 0** (the L4-eigsolve forward-ref was correctly plain-text-deferred as speculative, not stubbed — below the clearly-implied bar).
- **in-cycle live-link upgrade = 2** (`nleps_eigenvalue_correction` → `nleps_jacobian_action`; `deflate-composition-lowering` → `gram-fold-specialization`).
- build-breakage = none (zero build-repairs). commit atomicity = single commit. consumed-report frontmatter integrity = all 8 marked `integrated_at` + `integration_commit` (+ two-phase SHA patch).

## Staging-log-completeness note

**8/8 rows — the cycle-018 staging-completeness gap did NOT recur for the SIXTH consecutive cycle.** STAGING.md was authoritative this cycle; the cross-check of 8 staging rows vs 8 dispatched ready reports reconciles clean (all `applied`, no `partially-applied`/`deferred`/`rejected`). Notable given the mid-cycle machine crash — the per-report integrators had fully staged before the crash, so no working-tree reconciliation recovery was needed (same as cycle-023).

## Wave-conflict observations

- **`book/src/L1/index.md` (Firm-count headline + cohort-list + dep-map) + `book/src/SUMMARY.md` shared between the two NEP-atom harvests (reports 1 + 2)** — only the single Firm-count cell needed additive reconciliation (report 1 took 17→18, report 2 re-read disk and took 18→19); all cohort bullets, dep-map rows, and SUMMARY lines are textually non-overlapping by construction (sequential after the prior NEP atom). Clean serial handoff.
- **`book/src/L1-L0/index.md` + `book/src/SUMMARY.md` shared between the two L1>L0 abstractor themes (reports 3 + 4)** — distinct slugs, non-overlapping inserts; report 4 re-read disk first and matched on-disk anchors verbatim. No Firm-count cell at L1>L0 (no count reconciliation). Both NLEPS L1>L0 rows coexist.
- **`book/src/L2-L1/index.md` + `book/src/SUMMARY.md` shared between the two L2>L1 abstractor themes (reports 5 + 6)** — distinct slugs; report 6 re-anchored its inserts after report 5's `gram-fold-specialization` row/entry (the sibling moved the on-disk last entry), non-clobbering. No L2-L1 count cell. Plus the in-cycle live-link upgrade: report 6 re-linked its `gram-fold-specialization` forward-references once report 5's file existed on disk.
- **No L3 / `dot-mutation-rotation` conflict** — report 7 was the sole cycle-024 writer of the L3 index + `L3/eigsolve.md` (a surgical SUMMARY relabel at `:31`); report 8 was the sole writer of `dot-mutation-rotation.md`. Both textually non-overlapping with the L1/L1>L0/L2>L1 appends.

## Integration-tooling friction (batch-6 evidence-window — third/final entry)

1. **Second consecutive machine-crash recovery — finalize resumed from a fully-staged state (recovery-not-normal-path).** A machine crash interrupted the cycle after all eight `integrator-per-report` runs had completed and staged. `integrator-finalize` recovered by reading the (complete, authoritative) STAGING.md + cross-checking the working tree; no per-report re-application or working-tree reconciliation was needed. With cycle-023 this is now TWO clean crash/resume cycles in the batch — strong evidence the split-integrator design's staging-log channel survives crashes idempotently. No tooling gap surfaced; worth noting to the batch-6 meta-phase as a resilience datapoint.
2. **Continuing inline-anchor-drift pattern (RECURRENCE).** Cycle-024 critics found **5 off-by-one drifts in the l3-eigsolve report** (arpack.cpp :573/:579/:270; slepc.cpp :1857/:1873, repairer-corrected) **+ 1 each in 3 other reports**. This is the SAME pattern that escalated the batch-5 citation-checker ASK — now realized as `tools/citecheck` (enacted `88b7893`, user go). The batch-6 meta-phase should evaluate whether `citecheck` is being invoked by producers/critics and whether to make it a per-report gate. The cycle-024 `dot-mutation-rotation` carry-forward `orthog.hpp:34`→`:35` is itself the discharge of a prior drift instance.
3. **The `apply-nonlinear-pencil` critic-off-by-one-on-an-off-by-one.** The critic flagged `nleps.cpp:810-811` as a cosmetic off-by-one to shift to `:809-810`; the dispatch directive (and the repairer) correctly determined the original `:810-811` is right — the critic's finding was itself off by one. A datapoint that anchor-drift findings need verification both ways (the checker tool would adjudicate mechanically).

## Carry-forward to the batch-6 meta-phase (fires after THIS cycle-024 finalize)

1. **Machine-crash recovery** — clean split-integrator resume (two consecutive crash-recovered cycles 023/024); evidence the staging-log channel survives crashes. A resilience note, not a tooling gap.
2. **Continuing inline-anchor-drift pattern** — 5 drifts in l3-eigsolve + 1 each in 3 other reports; feeds the batch-5-escalated citation-checker ASK now realized as `tools/citecheck`. Meta-phase should evaluate citecheck invocation + the per-report-gate question.
3. **Multiple OQ closures ready for migration** (see the consolidated closure list returned to the parent): `l3-eigsolve-linear-evp-has-no-krylov-step-kernel-analog` CONFIRMED (close `:624`); the NEP-interior cohort OQs (`:779` + its `:859` carry-forward — both halves landed firm); `nleps-deflated-solve-l1-l0-lowering-theme` ENACTED (`:784`); `dot-mutation-rotation-subpattern-d-citation-fix` RESOLVED (`:847`); the eigsolve chain prerequisite OQs (`:613` step-3 DONE, `:802` stub-materialization → RESOLVED); plus the per-report landing records.
4. **NEP-interior-atom L1>L0 themes** (`nleps_jacobian_action`, `nleps_eigenvalue_correction`) — batch for a subsequent abstractor pass (the two remaining NLEPS L1>L0 items).
5. **Shared `deflate` Galerkin-core promotion gate** — still OPEN, still gated on a positive bare-Gram-solve site outside the Schur wrapping; now triple-referenced (L2 `deflate` + L1>L0 `nleps-deflated-solve` + L2>L1 `deflate-composition-lowering`), all promoting together.
6. **Speculative L4 `eigsolve` solve-monad surface** — future L4 harvester/abstractor; plain-text-deferred, not yet clearly-implied.
7. **Optional L3>L2 `eigsolve` body-identity audit anchor** + the separately-pending **L2>L1 `eigsolve-spectral-transform-composition`** theme + the still-absent `concepts/eigsolve` page.

## Suggested next-cycle dispatches (post-meta-phase)

- abstractor on the NEP-interior-atom L1>L0 themes (`nleps_jacobian_action`, `nleps_eigenvalue_correction`) — the remaining NLEPS L1>L0 items.
- lowering-verifier audits on the new firm themes (`apply-nonlinear-pencil-mutation-rotation`, `deflate-composition-lowering` — the latter may UNBLOCK the shared Galerkin-core promotion without ENACTING).
- abstractor on the L2>L1 `eigsolve-spectral-transform-composition` theme + layer-intro-author on the absent `concepts/eigsolve` page.
- harvester / lowering-verifier on the optional L3>L2 `eigsolve` body-identity audit anchor.
- forward-frontier: the next NLEPS / eigenmode-pipeline pieces, or a shift to a different solver pipeline's shared infrastructure.
