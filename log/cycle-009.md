## 2026-05-27 cycle-009 — third primary cycle of meta-batch-1 (3:1 cadence) — CLOSES META-BATCH-1 — 4 reports — krylov-step lowering chain fully firm (L3>L2 promotion via status-inheritance) + L0 bundle 5 + eigsolve L1 rough-in (test-coverage-bounded; second constructed-operator gate) + check_stop_into_carry combinator defer-verdict

- **Phases fired**: plan (cycle-planner haiku) → dispatch (4 wave-1 parallel; no wave-2) → critique (4× critic) → repair (4× repairer) → **integrate (4× integrator-per-report serial → 1× integrator-finalize; fifth cycle under split integrator)** → (meta-phase fires as separate parent-orchestrated dispatch immediately after this finalize commit lands; `/compact` after meta-phase per 3:1 cadence + post-meta-compactification user directive).

- **Meta-batch context**: cycle-009 is the **third and final primary cycle of meta-batch-1** under the 3:1 meta cadence (user directive 2026-05-27, post-cycle-006 meta). Cycles 007/008/009 form batch-1; meta-phase fires after this cycle-009 finalize, aggregating evidence across the full 3-cycle batch. Cycle counter does not reset at batch boundaries (cycles 010/011/012 form batch-2). Resume notes (`scaffolding/cycle-007-resume-notes.md`) span the full meta-batch and are **deleted at this finalize** per their own §"Resuming the session" step 7.

- **Substantive landed (4 reports, all `ready`)**:
  - **Pass 1 (wave-1, lifter — PRIORITY)**: `book/src/L3-L2/krylov-step-body-identity.md` **promoted from `firm-rough-in` → plain `firm` via status-inheritance**. Cycle-008 wave-1 lifter firmed the upstream L4>L3 `krylov-step-typed-wrapper-dissolution` theme; cycle-009 wave-1 lifter mechanically promoted the downstream L3>L2 theme per the plan-kind-consistency convention (the `firm-rough-in` qualifier inherited the upstream `rough-in`, so the upstream firming auto-eligibilized the downstream promotion). 4 in-place edits: §Status full rewrite with inheritance-acknowledgment paragraph; Context bullet flip; §Speculative L3 operators block updated for upstream firm + L4 `iterate_while` firm; §Verified-against L4/L3 evidence line updated for upstream firm cycle-008 with line pointer + patch summary. Plus 1 dep-map row edit in `book/src/L3-L2/index.md`. **First across-cycle status-inheritance promotion in the artifact**; the "first in-cycle status inheritance" pattern flagged in cycle-007 integrator-signals line 167 now has its first across-cycle precedent.
  - **Pass 2 (wave-1, layer-intro-author)**: **L0 bootstrap bundle 5** — 2 new L0 chapters: `mpi-globalsum-and-collectives` (File overviews — `palace/utils/communication.hpp`, the 429-line header housing the `palace::Mpi` collective wrappers + the `palace::mpi::DataType<T>` template family; file-level companion to `par-types-single-rank-reading`'s §"MPI collectives" section; catalogues the 42 `GlobalSum` sites + 36 `GlobalMin`/`GlobalMax` sites across `linalg/` and `models/`); `preconditioner-classes-overview` (Overload-sets-and-class-interfaces — 7-class survey across `palace/linalg/{amg,ams,jacobi,chebyshev,distrelaxation,gmg,blockprecond}.{hpp,cpp}`; three-group split by inheritance and orchestration). **L0 chapter count 14→16**. **Closes cycle-008 OQ `l0-bundle-5-candidates`**. 2 new follow-up OQs opened (`tests-as-semantic-supplement-l0-vs-concepts-decision` — placement decision for the deferred third bundle-5 candidate; `l0-bundle-6-candidates` — bundle-6 forward-routing).
  - **Pass 3 (wave-1, harvester)**: `book/src/L1/eigsolve.md` (206 lines) — **L1 rough-in (test-coverage-bounded) operator landed**, **second constructed-operator gate at L1** composing against `ksp_solve` (the inner linear solver is itself a constructed-operator absorption). Rough-in status motivated by the absence of a dedicated `test-eigensolver.cpp` (only indirect coverage via `test-boundarymodeoperator.cpp`'s three `ModeEigenSolver` cases, exercising the linear path with `LARGEST_REAL` only). Introduces 4 preserved variant axes (problem-type / spectrum-target / spectral-transformation / scaling), 3 collapsed axes (orchestration-pattern / slepc-internal-method / slepc-problem-type), and 3 explicit out-of-scope items, all enumerated with rationale. New "Rough-in (test-coverage-bounded)" L1 cohort subsection added to `book/src/L1/index.md` between Firm bullet list (8 firm) and "Rough-in (obstruction)" subsection (cohort-purity preserving). **Partially closes cycle-008 OQ `eigsolve-l1-operator-rough-in-candidate`** (status `open` → `partially-answered`; firm-promotion follow-up tracked via 4 new OQs).
  - **Pass 4 (wave-1, combinator-miner — INSPECTION-ONLY)**: `check_stop_into_carry` helper-promotion decision. **Verdict: defer.** Among the four candidate downstream consumers (FGMRES, MINRES/BiCGStab stubs, eigenmode solvers, transient stepping, NLEPS) the only slice that reuses the GMRES check-stop *shape* is FGMRES — and FGMRES is **not a second slice** in the L1+ corpus (already absorbed into `gmres.md` as the `op.flexible` variant axis). NLEPS's Quasi-Newton inner loop has a structurally-similar 3-condition stop but is **not yet spec'd at L1+** (no `nleps.md` slice exists). The helper has exactly one *current* call site; cycle-008 promotion criterion "defer until a second slice needs it" unmet. **First cross-cycle abstractor-criterion → combinator-miner-verdict round-trip** in the artifact: cycle-008 set the promotion criterion, cycle-009 ran the survey. Methodology signal for cycle-009 meta-phase.

- **Reports applied** (4 of 4):
  - `reports/2026-05-27T191730Z-lifter-krylov-step-body-identity-firm-promotion/` (status: integrated; follow_up_agent: null — promotion mechanical, no follow-up needed)
  - `reports/2026-05-27T192051Z-layer-intro-author-L0-bootstrap-bundle-5/` (status: integrated; follow_up_agent: cycle-010+ layer-intro-author for L0 bundle 6 per `l0-bundle-6-candidates` OQ; meta-phase or planner for `tests-as-semantic-supplement-l0-vs-concepts-decision` placement question)
  - `reports/2026-05-27T191929Z-harvester-eigsolve-L1/` (status: integrated; follow_up_agent: cycle-010+ lifter / harvester / lowering-verifier for 4 follow-up OQs `eigsolve-linear-solve-failed-status-anchor`, `eigsolve-scaling-coordinate-convention`, `eigsolve-initial-space-axis-placement`, `eigsolve-iteration-count-result-field`)
  - `reports/2026-05-27T192047Z-combinator-miner-check-stop-into-carry-reuse/` (status: integrated; follow_up_agent: cycle-010+ harvester on NLEPS at L1+; lifter / combinator-miner concurrent with NLEPS promotion; meta-phase for `variant-absorption-vs-instance-counting-policy` + `combinator-miner-authority-defer-verdict-status-edit-scope`)
  - `reports/2026-05-27T200036Z-integrator-finalize-cycle-009/CYCLE.md` — batch report (this finalize).

- **Krylov-step lowering chain fully firm**: with cycle-009 pass-1 promoting L3>L2 body-identity to `firm`, the **entire chain is now firm**: L4 `krylov-step` (cycle-006) > L4>L3 `krylov-step-typed-wrapper-dissolution` (cycle-008) > L3 rendering (in the L4>L3 theme) > L3>L2 `krylov-step-body-identity` (cycle-009) > L2 `krylov-step` (cycle-005). **First fully-firm cross-layer chain in the artifact**. Significant structural milestone closing meta-batch-1.

- **Wave-conflict observations** (captured in `scaffolding/integrator-signals.md`):
  - **4-wave-1 + 0-wave-2 split** — first cycle since cycle-005 to run pure wave-1 (cycles 006/007/008 all had wave-2 polish/refresh). Cycle-009's planner deliberately did not stage wave-2 (priority items all wave-1-tractable; eigsolve's L1/index refresh was bundled into the wave-1 harvester per cohort-restructure convention). Validates pure-wave-1 cycles remain available under split integrator + 12-cap.
  - **SUMMARY.md touched 3 times** (passes 2 L0 ×2 + pass 3 L1) with zero collisions. Pattern continues: cycle-005 (5/6) / cycle-006 (4/5) / cycle-007 (5/6) / cycle-008 (3/7) / cycle-009 (3/4).
  - **open-questions.md touched 4 times this cycle** (passes 2/3/4) at 4 distinct line ranges. Zero collisions; append-before-Dropped convention held cleanly per-OQ YAML status authority. Cycle-008 carry-over drift observation routed to cycle-009 meta-phase aggregation continues to hold.
  - **No index-placeholder displacement this cycle** — all relevant indexes (L0/index, L1/index, L3-L2/index) already past the placeholder-displacement step. Cycle-007 was the last cycle to displace a placeholder (L3-L2/index); cycle-008's L1-L0/index displacement was the fourth and most recent overall.
  - **First across-cycle status-inheritance promotion** — pass 1 promoted L3>L2 `krylov-step-body-identity` from `firm-rough-in` to plain `firm` via the cycle-007-flagged status-inheritance convention. First time the convention has fired across cycles (cycle-007 set it up in-cycle as an inherited-rough-in qualifier; cycle-009 enacts it across-cycle now that the upstream firmed cycle-008).
  - **First cross-cycle abstractor-criterion → combinator-miner-verdict round-trip** — pass 4 ran the cycle-008 abstractor's "defer until a second slice needs it" promotion criterion through a programmatic survey, producing the verdict `defer`. First instance of this dispatch pattern; methodology signal for meta-phase.
  - **No deferrals, no rejections, no rework loops.** All 4 reports applied as-is. Clean-run streak continues (cycles 005 / 006 / 007 / 008 / 009).

- **Critic findings**: ~32 checks total across 4 METAs; ~24 pass / 8 warning / 0 fail. All warnings repaired or correctly classified `not-needed` by repairer. Pass-4 combinator-miner's draft §Status-block-edit on the cycle-008 upstream theme was dropped per Finding 4 (Decision: repaired) and re-routed via the new OQ `combinator-miner-authority-defer-verdict-status-edit-scope`.

- **Safety-net gates**: **0 hits cycle-wide** (no index-placeholder-displacement-auto-fix this cycle; no SUMMARY-chapter-registration-auto-fix; no retroactive-budget exceedances; no concept_writes on existing slug; no forward-edge-without-surface; no edge-label/prose mismatch; no H1 reuses page heading; no append-on-missing-slug; no variant-axis-missing; no bookkeeping-incomplete). **Cleanest gate-hit cycle since cycle-005.**

- **Build**: `cargo make book` — Build Done in 88.42 seconds, exit 0. **Zero new warnings; no build-repair needed.** New chapters (`mpi-globalsum-and-collectives`, `preconditioner-classes-overview`, `eigsolve`) all rendered correctly. New L1/index "Rough-in (test-coverage-bounded)" subsection slotted between Firm bullet list and "Rough-in (obstruction)" subsection without breakage. L3-L2/index dep-map firm-row update rendered cleanly. Pre-existing katex-link warnings (in `concepts/plane-rotation-stream.md` etc.) unchanged.

- **Open questions promoted to ledger**: **11 new** (2 from pass 2, 4 from pass 3, 5 from pass 4 — combinator-miner inspection-only dispatch yielded the highest OQ count this cycle).

- **Open questions answered**: **1** closed (`l0-bundle-5-candidates`, cycle-008; closed by pass 2 L0 bundle 5 — 2 of 3 candidates landed; tests-as-semantic-supplement deferred via new OQ). **1 partially-answered** (`eigsolve-l1-operator-rough-in-candidate`, cycle-008; status `open` → `partially-answered` by pass 3 harvester eigsolve rough-in; firm-promotion gated on 4 new follow-up OQs). **Net ledger change cycle-009**: 11 new − 1 closed = +10 net (combinator-miner inspection-only dispatch contributes 5 OQs; eigsolve rough-in's firm-promotion follow-ups contribute 4 OQs; net positive but reflects deliberate "carry the work as recorded follow-ups" pattern for the combinator-miner survey + the rough-in-with-promotion-gating pattern for eigsolve).

- **Cycle-008 integrator-signals items landed cycle-009**:
  - "(CYCLE-009 mechanical follow-up) `lifter` or `integrator-per-report` for L3>L2 `krylov-step-body-identity` status promotion" — **landed** (wave-1 pass 1; lifter via dispatch — chose lifter over per-report-inheritance per planner's wave-1-priority routing).
  - "(`layer-intro-author`, `L0 bootstrap bundle 5`)" — **landed** (wave-1 pass 2; 2 of 3 candidates, third deferred via OQ).
  - "(`harvester`, `eigsolve @ L1`)" — **landed** (wave-1 pass 3; rough-in per test-coverage pre-check).
  - "(`lifter` or `combinator-miner`, `check_stop_into_carry` promotion decision)" — **landed** (wave-1 pass 4; combinator-miner inspection-only; verdict defer).
  - "(`harvester`, `matrix-weighted-norm` / `bilinear-form` L1 rough-ins)" — **deferred from cycle-009 planner per capacity**; routes to cycle-010+ via cycle-008 OQ `matrix-weighted-norm-and-bilinear-form-l1-rough-ins` carried forward.
  - "(`abstractor` or `lifter`, `gmres.md §L4 v0.6 → v0.7 self-rotation`)" — **deferred to cycle-010+** per cycle-008 planner's "large dispatch" tag; routes to cycle-010+ planner.
  - "(`same-layer-cross-cutter` or `meta-phase`, `open-questions.md section-drift cleanup`)" — **routed to cycle-009 meta-phase aggregation** per cycle-008 §Suggested dispatches item routing.
  - "MCP codemap rollout decision" — **routes to cycle-009 meta-phase** per user directive (carried forward unchanged across batch-1).
  - **4 of 7 dispatchable suggested dispatches landed this cycle** (saturation 4/7 = 57%); 3 explicitly deferred to cycle-010+ per planner capacity. Lower saturation than cycle-008's 7/7 — reflects cycle-009's planner's smaller wave-size (4 vs cycle-008's 7) plus the meta-batch-boundary effect (meta-phase aggregation work routes via signals rather than concrete dispatches).

- **Write-authority discipline**: **zero recurrences** of cycle-008 wave-1 dispatch #2 violation (abstractor writing directly to `book/`). All 4 cycle-009 dispatches held write-authority discipline cleanly. Cycle-009 meta-phase will judge whether the single-instance cycle-008 violation warrants role-spec wording-prominence boost or is treated as a one-off. Per-report `integrated_at:` write-authority drift: **fourth consecutive clean cycle** (cycle-006 enacted; cycle-007/008/009 zero recurrences). Friction-ledger entry `integrated-at-write-authority-drift` markable `addressed` at cycle-009 meta-phase aggregation.

- **MCP codemap pilot**: no new pilot this cycle (persistent permission-denied across batch-1). Rollout decision routes to cycle-009 meta-phase per user directive.

- **Resume notes deleted**: `scaffolding/cycle-007-resume-notes.md` — per its own §"Resuming the session" step 7 and §"Meta-phase cadence change (3:1)" addendum, this file spans the full meta-batch-1 and is consumed at the end of cycle-009 finalize. **Deleted in this finalize commit** per the file's own deletion-on-completion instruction.

- **Legacy log/cycle-009.md renamed**: `log/cycle-009.md` (pre-layered-era 2026-05-24 `back gmres — revise`) renamed to `log/cycle-009-legacy.md` per cycle-005/006/007/008 precedent. Pre-flagged in cycle-008 integrator-signals tail.

- **Integrator-signals append**: cycle-009 section prepended above cycle-008 in `scaffolding/integrator-signals.md`. Includes meta-batch-1 close context (cycle-009 = batch-1 position 3 of 3; meta-phase fires next), fifth-cycle-under-split-integrator observations, krylov-step chain fully-firm milestone, first across-cycle status-inheritance promotion, first cross-cycle abstractor-criterion → combinator-miner-verdict round-trip, 4/7 dispatchable-saturation result.

- **Two-phase SHA patch** (canonical pattern per role spec process step 13): all 4 reports' `integration_commit: PLACEHOLDER_SHA` are patched in a follow-up commit immediately after finalize commit lands. Message: `patch commit-sha references for cycle-009 finalize commit (<finalize-sha>)`. Same two-phase pattern cycles 004/005/006/007/008 used.

- **Meta-phase batch-1 aggregation fires next**: separate parent-orchestrated dispatch immediately after this finalize commit lands. Aggregation targets surfaced via cycle-009 integrator-signals + batch-aggregation summary at `reports/2026-05-27T200036Z-integrator-finalize-cycle-009/CYCLE.md` §"Meta-batch-1 closure summary". Critical aggregation items: `abstractor-write-authority-violation-cycle-008` (single-instance, judge one-off vs pattern); MCP codemap rollout decision; user-raised mid-cycle-009 notification-hook misfiring issue (newly surfaced; meta-phase to examine and propose refinement); open-questions.md section-drift cleanup; combinator-miner defer-verdict authority scope; variant-absorption-vs-instance-counting policy; vocabulary-cohort middle-slot adaptation; dep-map `Lowers to` column back-application; index-placeholder displacement pattern formalization (4 instances stable); per-report `integrated_at:` drift markable `addressed` (4 consecutive clean cycles).
