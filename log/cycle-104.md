## 2026-06-05 cycle-104 — 4 reports applied clean — ninety-ninth consecutive cycle under split integrator — **POSITION 2/3 OF META-BATCH-33 (the batch-33 meta-phase fires AFTER cycle-105)** — the GRADED-STACK TYPED-EDGE CAMPAIGN P1 SECOND TRANCHE LANDED: 6 record-concept pages typed firm (closes the c103 carry-forward) + 1 NEW firm L1 entry `set_subvector_zero` (homeless-primitive resolved) + 12 feature-column `uses-record` edges (config-record + op-params rescued from GC-garbage) + 2 prose-drift fixes; linter `untyped` 78 → 77, `rank_violations` HELD 0 — pass

> **Note:** this file (`cycle-104.md`) is the current-run, structural-redirect-era cycle-104 log (it overwrites the stale pre-redirect slice-vertical-era `cycle-104.md`, recoverable from git history). The current-run numbering does NOT reset across batch boundaries.

**POSITION 2/3 OF META-BATCH-33** (3:1 cadence; cycles 103/104/105; the cycle counter does NOT reset across batch boundaries; **the batch-33 meta-phase fires AFTER cycle-105's finalize as a SEPARATE dispatch aggregating 103/104/105** — this cycle-104 finalize runs NO meta-phase housekeeping). Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) + the 2026-06-02/2026-06-03 user directives + the 2026-06-04 GRADED RESOLUTION LADDER + FEATURE-ROOT REACHABILITY user directive (`METHODOLOGY-GRADED-STACK.md`). The middle cycle of batch-33; the P1 typed-edge campaign continued LEADING + the content tail closed.

### Headline — the P1 typed-edge campaign's second tranche typed the 6 record-concept pages firm, homed the last homeless primitive (`set_subvector_zero`), and began rescuing the records from reachability-garbage via feature-column `uses-record` edges

The c103 first tranche left 6 record-concept pages (`krylov`/`op-params`/`sim-state`/`step-outputs`/`prev-carry`/`solve-result`) carry-forward-untyped. This cycle typed all 6 as **fully-typed firm ranked nodes** (D1), created the NEW firm L1 `set_subvector_zero` entry that resolves the last homeless-primitive leg (D3), and added the **12 feature-column `depends-on (kind: uses-record)` edges** that make `config-record` (reachable from 11 columns) + `op-params` (from transient) **root-reachable** — 2 of the 8 graded-stack records rescued from GC-garbage (D2). The linter's `untyped` moved **78 → 77**, with **`rank_violations` HELD at 0** and **no newly-orphaned node**.

### What landed

- **D1 (layer-intro-author, build-relevant) — P1 record-concept pages typed firm (6 pages):** typed `edges:` frontmatter prepended onto the 6 existing record-definition concept pages — `concepts/krylov.md`, `op-params.md`, `sim-state.md`, `step-outputs.md`, `prev-carry.md`, `solve-result.md` — each `rank: firm`, `kind: record`, with `depends-on (kind: cites-evidence)` edges to L0 `iterative.{hpp,cpp}` source ranges + `reference` edges to consumers/siblings. The 3 constructive records (step-outputs/prev-carry/solve-result) have no single named C++ struct, so their cites-evidence edges point at per-field positive L0 backing ranges (a by-design struct-granularity exception, critic-verified). **Closes the c103 carry-forward OQ `graded-stack-six-record-concept-pages-need-frontmatter`.**
- **D3 (harvester, build-relevant) — `set_subvector_zero` NEW firm L1 entry + 2 concept-page repoints:** `book/src/L1/set_subvector_zero.md` CREATED — the `s=0.0` `SetSubVector` index-set vector-zeroing primitive (the diagonal 0/1 orthogonal projector `Z_idx = I − P_idx`), the vector-side BC-cleanup atom (`rank: firm`, firm-on-positive-structure escape; 3 `cites-evidence` `depends-on` → L0 `vector.cpp:461-474` real + `:476-492` complex + `vector.hpp:220-221` decl; 4 `reference`). **Repairer had repaired 2 build-critical warnings before integration** — a dangling `depends-on` to the forthcoming L1>L0 theme (retyped to the L0 `cites-evidence` edges) + a dangling `reference` to the speculative L3 seed (removed) — so the well-foundedness invariant holds and the build is dangling-free. Registered alpha-after `scal` in SUMMARY (BLAS-1 group) + the L1 dep-map. `concepts/trsv.md` + `concepts/gemv_basis.md` repointed (live links + §Disposition sections; stale citations corrected). **Resolves all 3 legs of the c103 homeless-primitive OQ `concept-primitive-without-L1-home-trsv-set_subvector_zero-gemv_basis`.**
- **D2 (layer-intro-author, build-relevant) — 12 feature-column `uses-record` edges + 4 back-refs:** inserted `depends-on (kind: uses-record)` edges into the 11 feature columns (7 driver/lifecycle/boundary-mode + 4 output-product repairer-addendum columns) all pointing at `concepts/config-record`, plus a 12th edge `transient.L4 → concepts/op-params`; + appended 4 reciprocal back-references to `concepts/config-record.md` (now lists 11 columns). **Reachability outcome: `config-record` + `op-params` become root-reachable (2 of 8 records rescued); the other 6 internal solve/BC records await WAVE-3 op-chapter edges.** `energy-fields.L4` DELIBERATELY NOT LINKED — its `PostprocessConfig`+`DomainData` have no concept pages yet (0 `uses-record` edges on disk, no dangler). Opened the HIGH OQ `solve-record-reachability-needs-op-chapter-uses-record-edges` (cycle-105 LEAD candidate) + the MEDIUM OQ `energy-fields-config-and-domaindata-records-need-concept-pages`.
- **D4 (lifter, build-relevant) — 2 prose-drift fixes:** de-staled the `eliminate_rhs is now FIRM` cohort bullet in `L1/index.md` (dropped the stale "L1>L0 lowering `eliminate-rhs-mutation-rotation` forthcoming" tail, re-anchored to the live firm theme `L1-L0/fe-operator-assemble-mutation-rotation.md`); slug-fixed `concepts/incremental-least-squares.md` §Dependencies (non-existent `givens-rotation` → live `[givens](./givens.md)`). Both resolve pre-existing c103 LOW OQs `eliminate-rhs-l1-index-bullet-stale-forthcoming-prose` + `incremental-least-squares-prose-names-nonexistent-givens-rotation-slug`.

### Build

`cargo make book` (mdbook 0.5.1 + linkcheck2) **EXIT 0** (~92s); **NO build-repair needed**. The new `set_subvector_zero.md` (SUMMARY-wired + L1-index-registered) + 6 record-page frontmatter prepends + 12 feature-column edge inserts all co-landed link-safe; `energy-fields` deliberately unlinked (0 dangling); D3's repaired edges + D4's repointed slugs all resolve on disk. The finalize-side L1-index firm-count narration edit re-built clean. Only the 4 pre-existing benign KaTeX `Potential incomplete link` WARNs in `design/l4_calculus.md` (NOT from any cycle-104-edited file). Per-report citecheck all clean except non-load-bearing residue (`config-record` `main.cpp` ×4 AMBIG = **PRE-EXISTING** prose-body basename-shorthand, NOT touched this cycle, confirmed by diff; lifter report self-reference AMBIG on `incremental-least-squares.md:43` = report-prose not a citation) — none on an applied edge's evidence.

### Step-5b — graded-stack linters (the build-gate companion, ran on the landed tree)

**`rank_violations: 0`** (baseline fully discharged c096 so ANY violation would be NEW and BLOCK — there are NONE, **GATE PASSES**; all new ranked nodes — the 6 record pages + `set_subvector_zero` + the 12 new `uses-record` edges — rest on `cites-evidence`-to-L0 or firm targets, so `rank(u)≤rank(v)` holds for every `depends-on` edge) + **NO newly-orphaned node**. Totals on the landed tree: `files=354` (was 353, +1 `set_subvector_zero`), `typed=277` (was 275, +2), **`untyped=77` (was 78, −1)**, `roots=36`, `reachable=36`, `rank_violations=0`, `promotion_frontier=8`, `unresolved_depends_on_targets=35`, `detritus=228`. **rank_violations trend: 22 (c094) → 1 (c095) → 0 (c096) → … → 0 (c103) → 0 (c104).**

**On the untyped count (78 → 77, not the predicted ~71-72):** 5 of the 6 record pages were ALREADY counted `typed` at c103-end (they carried `reference`-only frontmatter from c103 that registered `read_any_edge=True`), so D1's firm-typing only flipped 1 node out of `untyped`; `set_subvector_zero` is a NEW file that registers as `typed` (raises `files`+`typed`, not `untyped`). Net `untyped` −1 is **monotone-non-increasing** (gate respected). All 6 record pages + `set_subvector_zero` confirmed `untyped=False, rank=3.0` in the graph.

**EXPECTED linter noise this cycle (NOT fixed — `tools/` is meta-phase authority):** the `is_likely_outside_dag` heuristic still misses the group-intro pages + `concepts/dependency-map` (detritus noise). SEPARATELY — and SURFACED THIS CYCLE — the linter's hand-rolled frontmatter parser (`parse_frontmatter`) does **NOT** parse the multi-line block-mapping `- target: X` / `  kind: Y` edge form (only the inline-flow `{target: X, kind: Y}` form): block-mapping `depends-on` targets store as malformed `"target: X"` strings that match no node slug, so the reachability mark cannot traverse them. CONSEQUENCE: `config-record` + `op-params` still read `garbage?` in `--show-inbound` despite their correct on-disk inbound `uses-record` edges — the **content is right; the linter just can't see block-mapping edges**. This is a PRE-EXISTING, uniform parser gap (it affects every block-mapping edge including c103's, NOT this-cycle-introduced, NOT a new orphan) — routed to meta-phase/tools alongside the `is_likely_outside_dag` + `uses-record-kind` items. Exit trips ONLY on `rank_violations` so exit stays 0.

### Counts

CHANGED: **L1 firm 32 main / 39 grand → 33 main / 40 grand** (+1 `set_subvector_zero`, the 33rd main-cohort firm member; on-disk dep-map firm-row count = 40 confirmed; finalize is the count-owner and updated the `L1/index.md` count narration — D3 deferred it). **concepts: 34** (no new page — the 6 record pages already existed). NO other vocabulary status flips (this cycle is record-page typing + 1 new L1 leaf + edge-typing metadata + prose-drift). Snapshot: L1 firm 33 main / 40 grand · L4 firm 21 main / 25 grand · L4>L3 11 · L3 17+4po · L3>L2 6 · L2 21+1pc · L2>L1 11 · L0 22 · concepts 34 · methodology 4 · feature spine 11 firm / 1 seed · L4 reduce-family 4 verbs ALL firm; SLICE CORPUS: 0.

### Process

retroactive-budget global = 0; per-report safety-net gates all PASS/N/A across all 4 staging rows; 0 implied-component stubs; **4 OQs opened by per-report intake** (`record-concept-prose-citation-pm1-drift`, `set-subvector-zero-mutation-rotation-theme-forthcoming`, `solve-record-reachability-needs-op-chapter-uses-record-edges` [HIGH], `energy-fields-config-and-domaindata-records-need-concept-pages` [MEDIUM]); **per-report intake recommended-close 4 OQs** (D1 ×1 the 6-record-page tranche; D3 ×1 the homeless-primitive 3-leg OQ; D4 ×2 the prose-drift OQs — finalize/meta-phase close at batch unify, per-report has append-only OQ authority). 4/4 staging rows == 4 dispatched-ready — the cycle-018 staging-completeness gap did NOT recur for the EIGHTY-FIFTH consecutive clean staging / NINETY-NINTH consecutive clean split-integrator cycle. `scaffolding/priorities.md` + `scaffolding/open-questions.md` also modified (cycle-planner + per-report intake), committed atomically; NO `.claude/agents/` changes this cycle → no session-restart concern.

### Signals recorded prominently for the batch-33 meta-phase (fires after cycle-105)

1. **WAVE-3 op-chapter typing is the KEY remaining P1 gap** — the 6 internal solve/BC records (`sim-state`/`krylov`/`step-outputs`/`prev-carry`/`solve-result`/`dofset`) reach the GC roots ONLY via op-chapter `uses-record` edges, and the L4 solve/BC op chapters still carry pre-scheme frontmatter (`krylov-step` has none). OQ `solve-record-reachability-needs-op-chapter-uses-record-edges` (HIGH) is the natural **cycle-105 LEAD**.
2. **`energy-fields` names `PostprocessConfig` + `DomainData`** (no concept pages yet) — OQ `energy-fields-config-and-domaindata-records-need-concept-pages` (MEDIUM).
3. **STILL meta-phase-owned (carried)** — node-status convention unification (D1/D3 `reference`-only-block vs D2 no-frontmatter); navigational-container scheme ratification into `graded-stack-scheme.md`; the linter-gap fixes — (a) `is_likely_outside_dag` group-intros + `concepts/dependency-map`, (b) **the block-mapping-edge parser gap surfaced this cycle** (`parse_frontmatter` ignores `- target:/kind:` multi-line edges; this is why `config-record`/`op-params` still read garbage in the GC despite correct on-disk edges) / `uses-record-kind` recognition; role-fit friction (P1 typing has no dedicated role).
4. **Untyped trajectory** — 142 (c103 start) → 78 (c103 end) → **77 (c104 end)** for meta-phase campaign-progress tracking.
5. **Minor carried OQs** — record-concept prose ±1 citation drift (op-params/sim-state); `config-record` `main.cpp` prose-body basename-shorthand AMBIG (4 sites, pre-existing — should be fully-qualified `palace/main.cpp`); the forthcoming `set-subvector-zero-mutation-rotation` L1>L0 theme.

### Campaign state

Graded-stack campaign: P0 DONE; P3 DONE; P2 slice-deletion DONE (corpus 9 → 0, c097/098/099); **P1 typed-edge campaign SECOND TRANCHE landed** (6 record pages typed firm + `set_subvector_zero` homed + first feature-column `uses-record` edges, untyped 78 → 77, config-record/op-params root-reachable). **NEXT:** cycle-105 (batch-33 position 3/3, BATCH-CLOSING) runs the standing forward frontier — the natural LEAD is the WAVE-3 op-chapter `uses-record` typing that rescues the remaining 6 internal records; the batch-33 meta-phase fires after cycle-105's finalize (aggregating 103/104/105) and should unify the node-status convention divergence, ratify the container convention, route the linter gaps (now including the block-mapping-edge parser gap) to tools, and confirm the P1-edge-typing role home. Written by `integrator-finalize` (split integrator-per-report ×4 + finalize ×1).
