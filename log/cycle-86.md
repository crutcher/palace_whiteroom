## 2026-06-04 cycle-086 — 2 reports applied clean — eighty-first consecutive cycle under split integrator — POSITION 2/3 OF META-BATCH-27 — a THIRD firm-on-positive-structure FIRM PROMOTION this batch arc: the L4 solve-family combinator `solve_family` PROMOTED `rough-in (test-coverage-bounded)` → `firm`; the in-scope law-confidence escape route now EXTENDS BEYOND reduce verbs to the solve-family combinator — pass

**Position 2/3 of meta-batch-27 — the SECOND primary cycle after the batch-26 meta-phase, post-SESSION-RESTART** (3:1 cadence; cycles 085/086/087; the cycle counter does NOT reset across batch boundaries). **The batch-27 meta-phase fires AFTER cycle-087's finalize as a SEPARATE dispatch** aggregating 085/086/087 — this finalize does NOT run meta-phase housekeeping.

Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) + the 2026-06-02/2026-06-03 user directives, with the FEATURE-SURFACE SPINE codified into the role-specs + CLAUDE.md (batch-22) + the batch-23 record-definition obligation + by-kind grouping + the batch-24 enactments (`record` Kind RATIFIED) + the batch-25 enactments (SEED-SURFACE FIRMING CEILING CONDITIONAL) + the batch-26 meta-phase enactments (the USER DIRECTIVE `feature-column-promotion-break-the-seed-deadlock` ENACTED — the OWN-COMPOSITION column-promotion rule; the `matrix-weighted-norm` √-cascade NO-GO HELD; SESSION RESTART before c085) — with c085 (the batch-27 LEAD) having promoted 6 feature columns off the terminal `seed` state under the new OWN-COMPOSITION rule.

2 of 2 dispatched-ready reports applied clean (2/2 staging rows == dispatched-ready — the cycle-018 staging-completeness gap did NOT recur for the EIGHTY-FIRST consecutive clean split-integrator cycle / SIXTY-SEVENTH consecutive clean staging). Zero deferrals, zero rejections, zero gate-hits, zero build-repairs.

### Headline — a THIRD firm-on-positive-structure FIRM PROMOTION this batch arc: `solve_family` `rough-in (test-coverage-bounded)` → `firm`; the escape route extends BEYOND reduce verbs to the solve-family combinator

The L4 solve-family combinator **`solve_family` was PROMOTED `rough-in (test-coverage-bounded)` → `firm`** via the SAME firm-on-positive-structure / syntactic-identity escape that promoted `eigenfreq_qfactor_reduce` (c082) and `sparameter_reduce` (c083). This is the **THIRD** such promotion this batch arc, and the significant methodological extension: the in-scope law-confidence escape route — which had been demonstrated only on reduce verbs — now **extends beyond reduce verbs to the solve-family combinator**. The element-independence law (no cross-element state across the solve-family corners) is read off the const `BaseKspSolver::Mult` body (`palace/linalg/ksp.cpp:297-310`), so the laws are syntactic identities over positive source — no axiom smuggled.

**`solve_family` was the SOLE L4 `rough-in (test-coverage-bounded)` entry**, so the promotion empties that sub-cohort: **L4 firm 16→17 main / 20→21 grand; L4 rough-in (test-coverage-bounded) 1→0** (`domain_energy_reduce` plain-rough-in remains the only L4 rough-in).

### The 1-of-2-gates finding — `electrostatic`/`magnetostatic` STAY `seed` (NO column flip)

Firming `solve_family` discharges the FIRST of the two own-constituent gates on the `electrostatic` + `magnetostatic` driver columns. **But those columns STAY `seed`** — the SECOND gate, `gram_reduce` (`rough-in (test-coverage-bounded)`, folding plain-rough-in `matrix-weighted-norm` + `bilinear-form`), STILL gates them. The §Status gate narrowed honestly from TWO own-constituent rough-in gates to ONE (`gram_reduce` only); `status: seed` on both was correctly NOT flipped. The convergent blocker is the `matrix-weighted-norm` √-cascade (NO-GO-HELD by the batch-26 meta-phase), which now blocks `gram_reduce` → electrostatic/magnetostatic/capacitance/inductance columns AND (via `domain_energy_reduce`) energy-fields — accumulating downstream demand the batch-27 meta-phase should re-weigh.

- **D1 (lowering-verifier — COUNT OWNER):** the verb promotion (`solve_family.md` frontmatter `rough-in (test-coverage-bounded)`→`firm` + `## Status` firm re-narration via the firm-on-positive-structure escape, Laws 1/2/3, the discharged no-cross-element-state claim, the UNCHANGED-scope note, the column-gate non-flip note + a fresh `verified_against:` block of 8 `supports` entries) + the count-owner `L4/index.md` refresh (per-operator maturity cell `*(rough-in (test-coverage-bounded); cycle-055 D1)*`→`*(firm; cycle-086 D1)*` + §Vocabulary-cohort firm tally `(16 + 4 outer-driver)`→`(17 + 4 outer-driver)`). The critic independently verified the promotion sound (rotation-quality pass, the decisive check). The repairer's nested-fence→indented-code repair of the second edit block was applied in its repaired form.
- **D2 (lifter — consumer re-anchor; ZERO column flip):** propagated the `solve_family` maturity WORD `rough-in (test-coverage-bounded)`→`firm` to the 4 consumer files (`gram_reduce.md` consumes-row `:8` + dep-map row `:202-203`; `electrostatic.L4.md` composes-row/prose/dep-map; `magnetostatic.L4.md` mirror) + NARROWED the electrostatic/magnetostatic §Status gate from TWO own-constituent rough-in gates to ONE (`gram_reduce` only) + cleaned the `solve_family.md` §Evidence/§Provenance stale-after-promotion lines (which had RE-LOCATED on-disk to `:213`/`:214` after D1's §Status body replacement and were verified STILL stale). `gram_reduce`'s OWN `firmness: rough-in (test-coverage-bounded)` at `:4` was DELIBERATELY UNCHANGED (it folds the rough-in `matrix-weighted-norm` + `bilinear-form`, √-cascade NO-GO-HELD). Both `electrostatic.L4.md:5` + `magnetostatic.L4.md:5` `status: seed` verified UNCHANGED post-edit (the honest 1-of-2-gates non-flip).

### Build

`cargo make book` (mdbook + linkcheck2) exit 0. The edited `book/src/L4/{solve_family,index,gram_reduce}.md` + `book/src/feature/{electrostatic,magnetostatic}.L4.md` render + resolve. No new files, no `SUMMARY.md` change (status promotion + maturity-word re-anchor + prose, no new chapter). `linkcheck2` clean — zero dead links, zero build-repair. Only the pre-existing benign KaTeX "Potential incomplete link" WARNs (math-notation brackets in `design/l4_calculus.md` + a couple of bracket-prose dep-map false-positives) + the long-standing unclosed-HTML-tag-like WARNs in pre-existing `L1-L0/`/`L0/`/`meta-reviews/` files — NOT dead links, NOT introduced by this cycle's files. **Zero build-repair.**

### Process

- **Retroactive-budget global = 0** — D1 is a law-confidence promotion resting on positive const-source structure (the firm-on-positive-structure escape, NOT a retroactive draw); D2 is a consumer maturity-word re-anchor + gate-narrowing + stale-line cleanup (no claim authored). Well under the ≥4 block threshold. PASS.
- **Zero dispatch-phase write-partition leaks** — both reports applied via the proposed-changes channel; the per-report integrators reported 0 dispatch-phase `book/` mutation.
- **0 implied-component stubs** — no dead-link build-repair needed; all edited files pre-existed on disk.
- **Staging-completeness gap did NOT recur** — 2 rows == 2 dispatched-ready reports; 67th consecutive clean staging / 81st consecutive clean split-integrator cycle.
- **Serial apply per staging-row ORDER** (newest-LAST authoritative; `applied_at` advisory): D1 (lowering-verifier, COUNT OWNER, created the staging log) → D2 (lifter, consumer re-anchor). D1 owns `solve_family.md` frontmatter + §Status body + `index.md`; D2 owns the 4 consumer files + `solve_family.md` §Evidence/§Provenance — disjoint, verified. `scaffolding/priorities.md` also modified (the cycle-086 planner's co-owned plan write, committed atomically).

### Open questions

- **PROMOTED (D1, durable; for the batch-27 meta-phase):** `solve-family-firmed-discharges-one-of-two-electrostatic-magnetostatic-column-gates` — the 1-of-2-gates finding (`solve_family` firm but electrostatic/magnetostatic STAY seed because `gram_reduce` still gates them, convergently blocked on the `matrix-weighted-norm` √-cascade NO-GO-HELD).
- **CLOSED (by c086 finalize; RESOLVED by D2):** `solve-family-md-stale-evidence-provenance-lines-after-firm-promotion` — D2 edit #4 cleaned the `solve_family.md` §Evidence/§Provenance stale-after-promotion lines.

### Carry-forwards for the cycle-087 planner + the batch-27 META-PHASE

1. **The 1-of-2-gates finding** — electrostatic/magnetostatic now gate ONLY on `gram_reduce` (the gate narrowed from 2 own-constituents to 1); the convergent blocker is the `matrix-weighted-norm` √-cascade (NO-GO-HELD) which now blocks `gram_reduce` → electrostatic/magnetostatic/capacitance/inductance columns AND `domain_energy_reduce`/energy-fields — accumulating downstream demand the meta-phase should re-weigh under its sharpened re-weigh trigger.
2. **The firm-token-choice question** (carried from c085, still open; OQ `feature-column-firm-token-choice-batch-27-meta-phase`) — whether `firm` is the right promoted token for a composition-root or a feature-specific token; a batch-27 meta-phase decision.
3. **c087 is the land-clean cycle** before the batch-27 meta-phase (land-clean discipline for the last pre-meta primary cycle).

### Counts after cycle-086

**L4 firm 16→17 main / 20→21 grand** (+`solve_family` promoted via the firm-on-positive-structure escape) · **L4 rough-in (test-coverage-bounded) 1→0** (`solve_family` was the sole entry; `domain_energy_reduce` plain-rough-in remains the only L4 rough-in). **Feature spine columns UNCHANGED at 6 FIRM / 6 seed** (NO column flip this cycle). Firm columns: `driven`, `eigenmode`, `transient`, `eigenfrequency-qfactor`, `sparameters`, `lifecycle`. Seed: `boundary-mode`, `capacitance`, `electrostatic`, `energy-fields`, `inductance`, `magnetostatic`.

ALL other layer-vocabulary counts UNCHANGED from c085: L1 firm 30 main / 37 grand · L2 firm 21 (+1 partly-constructive) · L2>L1 firm 11 · L3 firm 17 (+4 partial-obstruction) · L3>L2 firm 6 · L4>L3 firm 10 · L0 chapters 22 · concepts 33 (+ `record` Kind RATIFIED) · methodology chapters 2 · FEATURE-SURFACE SPINE 12 columns by-kind-grouped (6 firm / 6 seed) · L4 reduce-family 4 verbs (`eigenfreq_qfactor_reduce` FIRM c082 / `sparameter_reduce` FIRM c083 / `gram_reduce` `rough-in (test-coverage-bounded)` / `domain_energy_reduce` `rough-in`).

Commit: `PLACEHOLDER_SHA` (patched in the follow-up two-phase SHA commit).
