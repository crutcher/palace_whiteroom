## 2026-06-06 cycle-116 — 2 reports applied clean — 111th consecutive cycle under split integrator — **POSITION 2/3 OF META-BATCH-37 (cycles 115/116/117; the scheduled batch-37 meta fires after c117)** — the **semantic-consolidation campaign LEAD** (user directive A 2026-06-06): WAVE-1 = the physical move of the L4 calculus surface out of `design/` into its own `# Semantic surface` home + ~97-file cross-ref rewrite (D1); WAVE-2 = the 24-file restatement-cohort relocation sweep completing the named-shape-groups general-rule consolidation (D2) → **reachability/rank-NEUTRAL** (pure prose/link/move; no frontmatter touched); `cargo make book` EXIT 0; graded-stack linter HELD (`reachable=133`, `rank_violations=0`, `detritus=126`)

> Note: this file supersedes a stale pre-redirect-era `cycle-116` log entry (old slice-vertical numbering, 2026-05-26, `refinement chebyshev`); the cycle counter collided across the 2026-05-26 structural redirect. The current cycle-116 is the batch-37 position-2 cycle under the layered-stack flow. Old content recoverable from git history.

---

**POSITION 2/3 OF META-BATCH-37** (3:1 cadence; cycles 115/116/117; the cycle counter does NOT reset across batch boundaries; **the scheduled batch-37 meta-phase fires after cycle-117's finalize**; the OUT-OF-BAND meta-phase that ran post-c115 already enacted the two 2026-06-06 directives — this finalize ran NO meta-phase housekeeping). Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) + the 2026-06-02/03 user directives + the 2026-06-04 GRADED RESOLUTION LADDER + FEATURE-ROOT REACHABILITY (`METHODOLOGY-GRADED-STACK.md`) + the 2026-06-05 GROUND-don't-remove grounding directive + the 2026-06-06 SEMANTIC CONSOLIDATION (directive A) + OPEN-ALL-FEATURE-FRONTS (directive B). Session was restarted post-c115-out-of-band-meta (role-specs changed) → context continuity established for this cycle. This finalize made NO `.claude/agents/` change → no restart concern from the finalize itself.

### Headline — the semantic surface gets its own home + the named-shape-groups cohort consolidation completes (reachability/rank-NEUTRAL)

Cycle-116 was the **first execution wave of the semantic-consolidation campaign** (user directive A, the c116 LEAD set by the out-of-band meta-phase). Two serial `layer-intro-author` dispatches, both VERIFY-NOT-REDO (the dispatches applied edits directly in `book/src`; the per-report integrators verified the on-disk state rather than re-applying):

- **D1 — `semantic-surface-move`.** The former L4 calculus strawman `book/src/design/l4_calculus.md` was **physically moved** (verbatim `git mv`, 513 lines, 39117 bytes) to `book/src/semantics/index.md`, completing the de-strawman'ing the out-of-band meta started (it had reordered the SUMMARY Part link but DEFERRED the physical path-move to this LEAD because of the ~97 cross-references). The move carried:
  - a **~97-file cross-reference rewrite** — `design/l4_calculus.md` → `../semantics/index.md`, reaching BOTH `]()` link targets AND inline-code prose citations (`grep -rn 'l4_calculus\.md' book/src` → 0; `grep -rl 'design/l4_calculus' book/src` → 0; both hard gates HOLD);
  - **`SUMMARY.md`** Part link rewritten — new top-level `# Semantic surface — calculus, rules & abstractions` Part header + `- [L4 calculus & spec semantics (active-management surface)](./semantics/index.md)`, placed AFTER the Feature Part, BEFORE `# L4` (per the consolidation directive ordering);
  - **`design/index.md`** reframed — the same-dir `(./l4_calculus.md)` link converted to a relative `(../semantics/index.md)` pointer note.
- **D2 — `cohort-sweep`.** The **24-file restatement-cohort relocation sweep** completed the named-shape-groups general-rule consolidation:
  - **Tier B (5 files** — `L2/nrm2`, `L3/nrm2`, `L2-L1/linear-combination-fold-specialization`, `L3/blas1-intro`, `concepts/elementwise-product`): dropped the residual general "NOT rank-1 / named shape groups per" echo, KEPT the §1.2.1 back-link.
  - **Tier C (19 files / 25 occurrences** — L2/L3/L4 dot/inner_product/normalize/reciprocal/elementwise_product/axpy-family + `L2/gram`, `L2/scal`, `L2/axpby`, `L2/axpbypcz`, `L4/sparameter_reduce`): trimmed the bare "(arbitrary, unknown rank — NOT rank-1)" parenthetical to the op's OWN "arbitrary, unknown rank" fact, retaining the existing §1.2.1 back-link.
  - Plus a **4-file L4 bare-basename prose-ref cleanup** (`L4/iterate-while` 10×, `L4/ksp_solve` 4×, `L4/chebyshev` 1×, `L4/index` 1×): stale inline-code `l4_calculus.md:NNN-MMM` citations → `index.md:NNN-MMM` (line ranges preserved by the verbatim move). Both hard gates HOLD (`grep` for residual basename + for cohort-echo phrasing → 0).

  With D2, functional-unit entries now USE+LINK the §1.2.1 surface rule rather than RE-STATE it — the semantic analog of the degenerate-identity-lowering / un-grounded-detritus smell is resolved by relocation-to-the-surface + a back-link.

### What this means for the OQ ledger

The OQ `named-shape-groups-general-rule-restatement-cohort-extent` (opened c115, migrated-to-plan by the out-of-band meta as the LEAD) is now **FULLY SWEPT (Tier A c115 pilot + Tier B + Tier C this cycle)** — D2 appended a RESOLUTION NOTE (READY-TO-CLOSE) to it; the meta-phase owns closure. Two new D1 OQs were promoted: `ambiguous-bare-index-md-prose-refs-after-semantic-surface-move` (bare-`index.md:NNN` prose-ref ambiguity, build-neutral hygiene) and `l4-entries-section-3.7-line-range-citation-drift` (pre-existing §3.7 line-range drift preserved verbatim by the move; out-of-D1-scope).

### Build

`cargo make book` (mdbook + linkcheck2 0.12.0) EXIT 0. The move + ~97-file cross-ref rewrite + SUMMARY Part link + cohort trim all resolve — the new `semantics/index.md` exists, `design/l4_calculus.md` is gone, all rewritten links point to live targets. Only the pre-existing benign `Potential incomplete link` WARNs (135; markdown-table / KaTeX false-positives, NOT link errors). **NO finalize build-repair needed** — no dead links, no new SUMMARY/index insert beyond the proposed-and-applied Part link, no implied-stub materialization.

### Step-5b — graded-stack linters (the build-gate companion, ran on the landed tree)

**`rank_violations: 0`** (baseline fully discharged c096, so ANY violation would be NEW and BLOCK — there are NONE; **GATE PASSES**; both dispatches left frontmatter untouched, so no edges authored, no rank claim moved) + **NO newly-orphaned node** (`reachable` HELD 133) + **`unresolved_depends_on_targets: 0`** (HELD). Totals on the landed tree:

`files=356`, `typed=295`, `untyped=61`, `roots=36`, `reachable=133`, `rank_violations=0`, `unresolved_depends_on_targets=0`, `promotion_frontier=8`, `detritus=126` (`detritus_no_typed_edges_pre_p1_artifact=103`, `detritus_with_typed_edges_stronger_signal=23`, `expected_unreachable_outside_dag=45`).

**Delta vs c115:** `files` 355→356 (+1 — the new `semantics/index.md`); `expected_unreachable_outside_dag` 44→45 (+1). **The predicted matcher-flip did NOT happen** — the linter's `is_likely_outside_dag` matcher CORRECTLY classifies `semantics/index.md` as expected-unreachable-outside-dag (it matched both `design/index` and `semantics/index`), so the move did NOT produce a new orphan; the +1 is simply the new file being correctly caught by the matcher. `reachable` / `rank_violations` / `untyped` / `detritus` (incl. both subsets) / `promotion_frontier` / `roots` all HELD exactly. **Both step-5b block-conditions PASS** (no new rank violation; no newly-orphaned node). This is a benign measurement note for the batch-37 meta — NOT a defect, NOT force-fixed.

**On the high `untyped`/`detritus` mass:** informational, NOT a block (the pre-P1 untyped tail + the typed-but-unreached nodes under the ratified RE1-RE8 baseline-exceptions). Only a *new* rank violation or a *newly*-orphaned node gates; neither occurred.

### Counts

**No firm-count status flips this cycle** — D1 a physical move + cross-ref rewrite, D2 a prose-trim sweep + basename cleanup; no new authored vocabulary, no node promotion. Layer tallies unchanged from c115: L1 firm 33 main / 40 grand, L4 firm 21 main / 25 grand, L4>L3 11, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 34, methodology 4, feature spine 11 firm / 1 seed, L4 reduce-family 4 verbs ALL firm. SLICE CORPUS: 0. The structural movement is the new `# Semantic surface` Part (1 file moved into it) + the cohort consolidation; both reachability/rank-neutral.

### Process

Retroactive-budget global = 0. Per-report safety-net gates all PASS/N/A across both rows (rank-well-foundedness 0, edge-label/prose-mismatch 0, YAML round-trip N/A [no frontmatter touched], SUMMARY-registration: D1's Part link was proposed AND applied by the dispatch, forward-edge-without-surface 0, append-on-missing-slug 0, variant-axis-missing 0). 0 implied-component stubs created. **2/2 staging rows == 2 dispatched-ready reports** — the cycle-018 staging-completeness gap did NOT recur (97th consecutive clean staging / 111th consecutive clean split-integrator cycle). **Both reports applied clean** — no repair-phase warnings carried to finalize. **CITECHECK NOTE (non-blocking):** D1 — 2 ok / 5 failing, but all 5 failures (2× MISS `l4_calculus.md:NNN`, 3× AMBIG `index.md:NNN`) are inside the report's OWN `## Open questions / caveats` PROSE illustrating the exact residual it is flagging (the MISS hits resolve to nothing because `l4_calculus.md` no longer exists — that IS the move; the AMBIG hits are the bare-`index.md` ambiguity promoted as the D1 OQ); NONE backs an applied change. D2 — 25 ok / 0 failing. Recorded not repaired — finding-prose, not landed-citation defects. OQs appended to the ledger this cycle (append-only between meta-phases; the meta-phase unifies/closes). `scaffolding/{open-questions,roadmap,integrator-signals,cycle-record}` + `log/` committed atomically with the `book/` edits + both consumed-report `integrated_at` touches. NO `.claude/agents/` changes FROM THIS FINALIZE → no session-restart concern from the finalize itself.

### Signals for the scheduled batch-37 meta-phase (fires after cycle-117's finalize, aggregating 115/116/117)

- **The semantic-consolidation campaign (directive A) is COMPLETE through its restatement-cohort LEAD.** The surface now lives at `book/src/semantics/index.md` under its own `# Semantic surface` Part; the named-shape-groups general rule is consolidated (USE+LINK everywhere). The meta-phase's standing every-batch liveness/unification refresh of the semantic surface now has a single, correctly-placed home to refresh.
- **Ready-to-close:** OQ `named-shape-groups-general-rule-restatement-cohort-extent` (FULLY SWEPT Tier A+B+C).
- **Two new D1 OQs to triage:** `ambiguous-bare-index-md-prose-refs-after-semantic-surface-move`, `l4-entries-section-3.7-line-range-citation-drift`.
- **Benign linter note:** `semantics/index.md` is correctly caught by the `expected_unreachable_outside_dag` matcher (no matcher-flip, no force-fix needed); `expected_unreachable_outside_dag` 44→45 is just the new file. Bundle alongside the kept-deferred batch-37 linter-maintenance items.
- **NEXT (c117):** opens campaign-2 — the directive-B `open-all-feature-fronts` wide multi-dispatch wave (waveguide-mode 6th output-product column, boundary-mode promotion, fe_space deferred siblings, mesh-wrapper single-machine vocabulary, any other in-scope deferral) — sequenced AFTER the consolidation, which this cycle completed.
