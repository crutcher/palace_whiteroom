## 2026-06-03 cycle-080 — 3 reports applied clean — seventy-fifth consecutive cycle under split integrator — POSITION 2/3 OF META-BATCH-25 — a NEW firm L1 `eigenvalue-untransform` discharges `eigenfreq_qfactor_reduce` gate-(a) + a `matrix-weighted-norm` 2nd-gate warrant-sharpening + prose hygiene — pass

**Position 2/3 of meta-batch-25** (3:1 cadence; cycles 079/080/081; the cycle counter does NOT reset across batch boundaries). This is **the SECOND primary cycle after the batch-24 meta-phase**. **The batch-25 meta-phase fires AFTER cycle-081's finalize as a SEPARATE dispatch** aggregating 079/080/081 — this finalize does NOT run meta-phase housekeeping.

Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) + the 2026-06-02/2026-06-03 user directives, with the FEATURE-SURFACE SPINE codified into the role-specs + CLAUDE.md (batch-22 meta-phase, commit `387fa56`) + the batch-23 record-definition obligation + by-kind grouping (commit `a23bbce`) + the **batch-24 meta-phase enactments** (the `record` Kind RATIFIED; `domain_energy_reduce` verb MIGRATED to the plan; the output-product↔driver 1:1 cross-link convention AMENDED for driver-agnostic energy-fields; the reduce-verb 2nd-gate dischargeability SHARPENED — cite existing postprocess tests, do NOT author new; the batch-25 active head reshaped to FIRM-the-seed-surface).

3 of 3 dispatched-ready reports applied clean (3/3 staging rows == dispatched-ready — the cycle-018 staging-completeness gap did NOT recur for the SEVENTY-FIFTH consecutive clean split-integrator cycle / SIXTY-FIRST consecutive clean staging). Zero deferrals, zero rejections, zero gate-hits, zero build-repairs.

### Headline — a NEW firm L1 primitive `eigenvalue-untransform` discharges gate-(a) of the L4 verb `eigenfreq_qfactor_reduce`; L1 firm 29→30 main / 36→37 grand

The FEATURE-SURFACE SPINE column build-out is COMPLETE (13 `seed` columns); the frontier is **FIRMING the seed surface**, not adding columns. This cycle landed a NEW firm L1 primitive that, together with the c077 `participation_ratio`, means **both per-mode scalar maps the L4 verb `eigenfreq_qfactor_reduce` folds are now firm L1** — discharging the verb's structure-side **gate-(a)**. The verb itself STAYS `rough-in (test-coverage-bounded)` (NOT promoted to firm) — its residual gate-(b) is the eigenpair→`(f,Q)` assembly test, which is open and out of project write-scope.

- **D2 (harvester — COUNT OWNER): NEW firm L1 `eigenvalue-untransform`** — `book/src/L1/eigenvalue-untransform.md` (new file, status `firm`): the per-mode eigenvalue→ω un-transform scalar map (`ω = √μ` for the linear EVP / `ω = λ/i` for the quadratic EVP), keyed on the structural predicate `!C && !has_A2` (EVP degree); carries an in-chapter `EvpDegree` §Record definition. **Firm via the firm-on-positive-structure escape** (the laws are syntactic identities on fully-specified positive source — the `apply_linop` precedent — so the missing dedicated test does not gate them). This is the SECOND per-mode scalar building block of `eigenfreq_qfactor_reduce` (the first being `participation_ratio`, c077); with both folded maps now firm L1, the verb's **gate-(a) is DISCHARGED**. D2 coupled-re-anchored `book/src/L4/eigenfreq_qfactor_reduce.md` (frontmatter `lowers_to` + §"Lowers to" prose + §Status gate-(a) marked DISCHARGED + §Evidence positive-site bullet) — **the verb STAYS `rough-in (test-coverage-bounded)`, NOT promoted to firm.** D2 owns the consolidated `L1/index.md` tally (29→30 main / 36→37 grand; dep-map row alpha-inserted between `dot` and `elementwise_product`) + the `SUMMARY.md` chapter entry (alpha position). **L1 firm 29→30 main / 36→37 grand (+1).** Closes OQs `eigenvalue-untransform-l1-primitive` + `eigenfreq-qfactor-reduce-firm-needs-l1-eigenvalue-untransform-primitive`; opens successor `eigenfreq-qfactor-reduce-firm-needs-assembly-test` (gate-(b), the residual firm-blocker).
- **D1 (lowering-verifier): `matrix-weighted-norm` 2nd-gate audit → warrant SHARPENED (+0 firm)** — `book/src/L1/matrix-weighted-norm.md`: the SPD radicand `⟨E, M E⟩` + the `½` factor is now POSITIVELY test-covered (`test-domainpostoperator.cpp`), but the outer `√` at the named entry point `linalg::Norml2` is NOT (the firm-on-positive-structure escape was considered and RULED OUT — the √-overload entry point is the un-witnessed step). The §Status gate-(a) bullet was sharpened, the closing Evidence line rewritten, and a `verified_against:` block appended (3 entries: `test-domainpostoperator.cpp:75-93` partially-supports, `domainpostoperator.cpp:219-231` partially-supports, `operator.cpp:599-619` supports). **Token UNCHANGED — stays `rough-in (test-coverage-bounded)`; +0 firm** (a warrant-sharpening + a `verified_against:` block, NOT a promotion). D1 also coupled-re-anchored `book/src/L4/domain_energy_reduce.md` (the c079 verb): its energy-form constituent's radicand is now test-covered (gate-(a) PARTIALLY advanced, not discharged); maturity token UNCHANGED `rough-in`. This ATTEMPTED the c079-surfaced "STRONG candidate to firm `matrix-weighted-norm` via `test-domainpostoperator.cpp`" — resolved partially (the radicand firms; the √-entry-point is now the residual gate; a full firm promotion would cascade a ~30-file re-anchor sweep, NOT triggered). PARTIALLY-ADVANCES OQs `matrix-weighted-norm-and-bilinear-form-stay-rough-in-with-sharpened-per-operator-gates-c028` + `domain_energy_reduce-promotion-double-gated`.
- **D3 (lifter — LOW/hygiene): c079-deferred prose cleanup** — `book/src/feature/sparameters.L1.md` (×6): the 6 stale port-projection `bilinear-form` refs repointed to the firm L1 `port_projection` (the composition pseudo-code line, the §The-composition projection bullet + fold paragraph, the §L1-vs-L4 bullet, the §Constituent-down-links dep-map cell `rough-in`→`firm`, and the §Status paragraph re-anchored onto the still-rough-in `sparameter_reduce`); column STAYS `seed`. `book/src/feature/eigenfrequency-qfactor.L4.md` (×2): the two internally-contradictory stale Status blocks reconciled to current on-disk maturity (verb `rough-in (test-coverage-bounded)`, κ-half firm L1 `participation_ratio` c077, eigenvalue-un-transform narrated as residual blocker); column STAYS `seed`. This discharges the c079 "companion prose cleanup" + the deferred `sparameters.L1.md` PROSE down-link repoint. Opens 2 coupled-column promotion-gate follow-up OQs (`eigenfrequency-qfactor-L4-column-promotion-coupled-to-D2-untransform-firming` — NOTES the post-D2 partial staleness — + `sparameters-L1-column-promotion-coupled-to-sparameter-reduce-firming`).

### Build

`cargo make book` (mdbook + linkcheck2) exit 0 (Build Done ~93s). The load-bearing checks PASS: the new `book/src/L1/eigenvalue-untransform.md` resolves in `SUMMARY.md` (alpha-inserted between `dot` and `elementwise_product` in the L1 grouping) with no orphan and renders to `book/book/html/L1/eigenvalue-untransform.html`; the `L1/index.md` dep-map row + count line (30 main / 37 grand) are consistent; the `matrix-weighted-norm` `verified_against:` block + the `domain_energy_reduce` consumes-line re-anchor + the `sparameters.L1.md` `bilinear-form`→`port_projection` repoints (dep-map cell `rough-in`→`firm`) + the `eigenfrequency-qfactor.L4.md` Status reconciliation are all consistent. `linkcheck2` clean — **zero dead links, zero build-repair**. Only the pre-existing benign KaTeX "Potential incomplete link" WARNs in `design/l4_calculus.md` (math-notation brackets mis-read as link syntax — the long-standing book-wide false-positive pattern, NOT dead links; predate this cycle).

### Process

- **Retroactive-budget global = 0** — zero retroactive-evidence draws across all 3 rows. D2 is a NEW firm L1 chapter (firm-on-positive-structure on positive source); D1 is a lowering-verifier 2nd-gate audit citing EXISTING postprocess unit tests as L0-equivalent documentation (the batch-24 decision-(e) sanctioned route — positive evidence, not retroactive); D3 is pure prose-hygiene re-anchor. Well under the ≥4 block threshold. PASS.
- **Zero dispatch-phase write-partition leaks** — all 3 reports applied via the proposed-changes channel.
- **0 implied-component stubs** — no dead-link build-repair needed; D2's `eigenvalue-untransform` was authored as a full firm chapter (not a stub); D3 repointed an already-on-disk `port_projection` (live link).
- **OQs:** 2 CLOSED-RESOLVED in-artifact (D2: `eigenvalue-untransform-l1-primitive` + `eigenfreq-qfactor-reduce-firm-needs-l1-eigenvalue-untransform-primitive`); 2 PARTIALLY-ADVANCED (D1: the `matrix-weighted-norm`-c028 gate + `domain_energy_reduce-promotion-double-gated` gate-(a)); 3 NEW OQs opened, all by the dispatch-phase/per-report intake blocks (none by finalize): `eigenfreq-qfactor-reduce-firm-needs-assembly-test` (D2 successor), `eigenfrequency-qfactor-L4-column-promotion-coupled-to-D2-untransform-firming` + `sparameters-L1-column-promotion-coupled-to-sparameter-reduce-firming` (D3).
- **Serial apply** per staging-row ORDER (newest-LAST authoritative; `applied_at` advisory): D2 (count owner — applied FIRST, created the staging log) → D1 → D3. **Byte-disjoint** — no two reports touched the same file. NOTE the deliberate VERB-vs-COLUMN distinction: D2 touched the L4 verb `eigenfreq_qfactor_reduce.md`; D3 touched the feature COLUMN `eigenfrequency-qfactor.L4.md` — DISTINCT files (confirmed by the D3 per-report integrator's fresh re-read), so no contention despite the similar slug. **Count-coordination held**: D2's `L1/index.md` tally was authored count-owner-blind to D1 with a conditional "IF D1 promotes `matrix-weighted-norm`, fold +1→31/38" note; D1 landed **+0 firm**, so the conditional degrades to a no-op and the applied tally (30 main / 37 grand) is correct as-is (D1 did not touch `L1/index.md` or `SUMMARY.md`; D2 is the sole count-owner).

### Carry-forwards for the cycle-081 planner

- **D3-staleness clause (trivial, mechanical, NOT a build error):** `book/src/feature/eigenfrequency-qfactor.L4.md` still contains a now-stale "the eigenvalue-un-transform has no firm L1 entry" clause (D3's reconciled prose, written before D2's firm `eigenvalue-untransform` landed the SAME cycle; D3 applied LAST, D2 FIRST). A trivial next-cycle prose touch should tighten it to "the un-transform IS now firm L1 `eigenvalue-untransform` (c080); the residual `firm`-blocker is gate-(b) the assembly test." Low fan-out. The D3 per-report integrator correctly applied D3 AS-PROPOSED and logged the partial-staleness OQ rather than over-reaching (tightening it is a substantive seed-gate re-narration D3 itself deferred); the verb STAYS `rough-in (test-coverage-bounded)` so the column correctly STAYS `seed` regardless — a harmless wording lag, not an incorrect maturity claim. `integrator-finalize` weighed the optional-tightening directive and left it (not a build error). Tracked by OQ `eigenfrequency-qfactor-L4-column-promotion-coupled-to-D2-untransform-firming`.
- **`eigenfreq_qfactor_reduce` gate-(b) (the assembly test):** the residual gate to firming the verb past `rough-in (test-coverage-bounded)` (gate-(a) discharged this cycle). OQ `eigenfreq-qfactor-reduce-firm-needs-assembly-test`. Whether an assembly-confidence `lowering-verifier` pass (cite existing postprocess tests, the batch-24 decision-(e) route) could discharge it in-scope is a batch-25 meta-phase question.
- **`matrix-weighted-norm` √-entry-point:** the residual gate (the radicand is now test-covered). A full firm promotion would cascade a ~30-file re-anchor sweep — weigh the fan-out before triggering.

— written by `integrator-finalize` (split integrator-per-report ×3 + finalize ×1).
