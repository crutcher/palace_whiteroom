## 2026-06-07 cycle-121 — 9 reports applied clean — 116th consecutive cycle under split integrator — **POSITION 1/3 OF META-BATCH-39, THE OPENING PRIMARY CYCLE (cycles 121/122/123; the batch-39 meta-phase fires AFTER cycle-123's finalize, aggregating all three)** — **THE LIFT-THROUGH CAMPAIGN OPENER, THE WIDE ALL-FRONTS FAN-OUT THAT BREAKS THE BATCH-36/37/38 PLATEAU**: the first cycle of the 2026-06-07 RE-SCOPE (DIRECTIVE-1/2/3). 9 dispatches, ALL applied clean. The LEAD landed the geometric-multigrid preconditioner feature column (THE highest-fan-out lift-through consumer); RE10 DISCHARGED, RE9/RE1/RE5/RE7 GROUNDED, the constructive-kernel frontier OPENED. `cargo make book` EXIT 0 after ONE surgical build-repair (a README de-link in the new eigsolve-impl chapter); graded-stack linter `rank_violations=0` / `reachable=139→156 (+17, plateau BROKEN)` / STRONGER `27→22`.

> Note: this file supersedes a stale pre-redirect-era `cycle-121` log entry (old slice-vertical numbering, 2026-05-26, `forward divfree [L1→L2]`); the cycle counter collided across the 2026-05-26 structural redirect. The current cycle-121 is the batch-39 position-1 (opening) cycle under the layered-stack flow. Old content recoverable from git history.

---

**POSITION 1/3 OF META-BATCH-39, THE OPENING CYCLE** (3:1 cadence; cycles 121/122/123; the cycle counter does NOT reset across batch boundaries; **the batch-39 meta-phase fires AFTER cycle-123's finalize, aggregating 121/122/123 as a SEPARATE dispatch**; this finalize ran NO meta-phase housekeeping). Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) + the 2026-06-04 GRADED RESOLUTION LADDER + FEATURE-ROOT REACHABILITY (`METHODOLOGY-GRADED-STACK.md`) + the 2026-06-05 GROUND-don't-remove directive + the 2026-06-06 SEMANTIC CONSOLIDATION + OPEN-ALL-FEATURE-FRONTS directives + **the 2026-06-07 RE-SCOPE (DIRECTIVE-1 MPI/sharding deferred; DIRECTIVE-2 lift the deferred in-scope work through, build the grounded consumers + discharge RE1-RE10; DIRECTIVE-3 spine-dependency opaque-library kernels get BOTH a kernel-API surface AND a constructive kernel-impl node)**. This finalize made NO `.claude/agents/` change → no session-restart concern from the finalize itself (the restart already happened post the out-of-band rescope meta).

### Headline — the wide all-fronts fan-out: plateau BROKEN, RE10 discharged, RE9/RE1/RE5/RE7 grounded, constructive-kernel frontier opened

Cycle-121 is the **first cycle of the lift-through campaign** — the wide multi-dispatch fan-out the "post consolidation, OPEN ALL REMAINING FEATURE FRONTS SIMULTANEOUSLY" directive called for, lifting the shared substrate once across related fronts rather than re-discovering it per-front. 9 dispatches, all applied clean (serial apply-order D3→D1→D2→D9→D8→D4→D5→D6→D7):

- **D1 (layer-intro-author, `geometric-multigrid-preconditioner`) — THE LEAD.** The GEOMETRIC-MULTIGRID PRECONDITIONER feature column at L4+L1 (`book/src/feature/geometric-multigrid-preconditioner.{L4,L1}.md`, rank `rough-in`) + a NEW 4th feature sub-kind "Infrastructure / shared-substrate columns" with its group-intro `book/src/feature/infrastructure.md`. THE highest-fan-out lift-through consumer (DIRECTIVE-2 grounded consumer-(1)): it GROUNDS **RE9** (composes firm `L1/fe_space_hierarchy` via `GetProlongationOperators()`), **RE1** (chebyshev + jacobi-smoother), **RE5** (`normalize`), **RE7** (`reciprocal`) as firm `depends-on` constituents. Capped at `rough-in` by the smoother leg (`multigrid-relaxation-smoother` rough-in), NOT firm. Single-machine-valid (parallelism by composition).
- **D3 (harvester, `multigrid-relaxation-smoother`) — the GS-SSOR kernel-IMPL (DIRECTIVE-3 dual-surface).** `book/src/L1/multigrid-relaxation-smoother.md` FIRM (the triangular-solve / GS-SSOR relaxation constructive realization); its kernel-API is the repositioned `L1-L0/triangular-solve-obstruction` (now carries the `kernel-api` role-label, stays `obstruction (opaque-library-ownership)`), linked by a free `realizes-kernel-api` reference edge. Integrated FIRST so D1's forward-ref resolves; made LIVE intra-cycle by D1's hard `depends-on`.
- **D8 (layer-intro-author, `re10-interpolator-ground`) — RE10 DISCHARGED.** Grounded `L1/interpolator` via two faithful `depends-on (uses)` edges: `L1/divfree-projector` (discrete-`Grad`, `divfree.cpp:117`) + `L4/waveguide_mode_reduce` (discrete-curl `Bz`, `boundarymodesolver.cpp:319-323`). `L1/interpolator` + its `L1-L0/interpolator-construction-rotation` theme flipped OFF the STRONGER garbage list. The c117 RE10 premise (falsified at c120 D1) is now closed.
- **D2 (layer-intro-author, `fe-space-hierarchy-concepts-page`).** `book/src/concepts/FiniteElementSpaceHierarchy.md` record-definition page (firm) + trimmed `L1/fe_space_hierarchy`'s in-chapter §Record-definition to a back-link (the ≥2-consumer promotion under the live "≥2 consumers, not ≥2 FIRM" rule; producer + GMG column = 2 consumers).
- **D9 (layer-intro-author, `waveguide-mode-drift-cleanup`) — the c118-surfaced FINDING-2 cleanup.** Reconciled the waveguide-mode `.L0`/index maturity drift (`rough-in`→`firm` prose; feature-column firm count 12→13, seed 1→0; `feature_root: seed` CORRECTLY KEPT). Pure liveness/honesty hygiene — no new claim, no new citation.
- **D4 (abstractor, `libceed-quadrature-kernel-impl`) — DIRECTIVE-3 constructive-kernel founding kernel.** `book/src/L1/libceed-quadrature-kernel-impl.md` (`roadmap_goal`; the matrix-free FE Gᵀ Bᵀ D B G contraction pipeline realizing the libCEED element-quadrature kernel-API). Lands INTENTIONALLY as grounded-future `[GARBAGE*]` (only inbound is the free `realizes-kernel-api`/`realizes-leaf` reference edges; its 4 substrate `depends-on` targets are declared-future).
- **D5 (abstractor, `eigsolve-kernel-impl`) — DIRECTIVE-3 SLEPc-EPS constructive kernel.** `book/src/L3/eigsolve-impl.md` + `book/src/L3/lanczos_step.md` (both `roadmap_goal`; the constructive Krylov-Schur/Lanczos loop realizing the SLEPc EPS kernel-API `L3/eigsolve`, which keeps `partial-obstruction` + gains the `kernel-api` role-label). Grounded-future `[GARBAGE*]` awaiting c122 consumers (RE3/RE8).
- **D6 (combinator-miner, `kernel-shared-substrate`).** The `L2/correction_step` rough-in dep-map row (`y + B·(x − A·y)`, the shared smoother/V-cycle per-sweep core Palace names verbatim at `gmg.cpp:174-176` / `distrelaxation.cpp:104`). Single row, no chapter (harvester formalizes later); replace-and-propagate FLAGGED not enacted (correct for a rough-in row).
- **D7 (abstractor, `amr-estimate-mark-refine`) — DIRECTIVE-2 grounded consumer-(2), the AMR front opener.** `book/src/L1-L0/amr-estimate-mark-refine.md` theme (rough-in; ZZ flux-recovery estimate / Dörfler bulk mark / MFEM-opaque refine obstruction) + 2 rough-in L1 verb rows (`dorfler_mark`, `flux_recovery_estimate`). The outer loop NOT re-homed (already firm L4 `fold_solve` state-generated — re-homing would be a degenerate-identity smell).

### Build — EXIT 0 after ONE surgical build-repair

`cargo make book` (mdbook + linkcheck2 0.12.0) initially EXIT 105 with two `File not found: ../../README.md` linkcheck errors, BOTH in the new `book/src/L3/eigsolve-impl.md` (D5): the author wrote `[lowering-verifier](../../README.md)` at two sites. The root `README.md` was DELETED in the batch-31 cleanup, and `lowering-verifier` is a **pipeline agent role**, not an in-book chapter — so there is no valid in-book target. Surgical repair: **de-linked both to plain inline-code** `` `lowering-verifier` `` (the de-link fallback per the role spec — an agent role is NOT a clearly-implied spec component, so stub-creation does not apply). Rebuild EXIT 0. All other touched files linkcheck-clean (the 9 new chapters + their SUMMARY/index inserts all resolve). Only pre-existing benign `Potential incomplete link` / `j+1` KaTeX-adjacent WARNs remain.

### Step-5b — graded-stack linters (the build-gate companion, ran on the landed tree)

**`rank_violations: 0`** (baseline fully discharged c096, so ANY violation would be NEW and BLOCK — there are NONE; **GATE PASSES**) + **NO newly-orphaned node** (`reachable` CLIMBED 139→156; the 3 intentional `[GARBAGE*]` roadmap_goal kernel-impls are grounded-future per DIRECTIVE-3 + `feedback_gc_ground_dont_remove_future_deps`, NOT orphaned). **`unresolved_depends_on_targets: 6`** — the declared-future substrate deps (4 libceed substrate ops `element_restrict`/`basis_apply`/`quad_point_contract`/`geom_factor_build` + 2 AMR verbs `dorfler_mark`/`flux_recovery_estimate`), all WARNING-non-strict (the graded-stack-tracked declared-future-dep state), linter exit 0. Totals on the landed tree:

`files=378 (+9)`, `typed=317 (+9)`, `untyped=61 (HELD)`, `roots=41 (+2 the GMG L4/L1 columns)`, `reachable=156 (+17)`, `rank_violations=0`, `unresolved_depends_on_targets=6`, `promotion_frontier=11`, `detritus=123 (−9)` (`detritus_no_typed_edges_pre_p1_artifact=101`, `detritus_with_typed_edges_stronger_signal=22`, `expected_unreachable_outside_dag=47`). rank_histogram: firm 218, typed-no-rank 82, roadmap_goal 3, rough-in 5, partly-constructive 3, obstruction 2, partial-obstruction 4.

**Delta vs c120** (`files=369, typed=308, untyped=61, roots=39, reachable=139, detritus=132, STRONGER=27, rank_violations=0, unresolved=0, promotion_frontier=6`):
- **reachable 139 → 156 (+17, the largest single-cycle reachability jump of the campaign — PLATEAU BROKEN).** STRONGER 27 → 22 (−5: RE10 interpolator + its construction-rotation theme off, smoother + neighbors live, partly offset by +3 intentional roadmap_goal kernel-impls landing in STRONGER). rank_violations HELD 0. unresolved 0 → 6 (declared-future substrate deps, intended).

**Both step-5b block-conditions PASS.** On the high `untyped`/`detritus` mass: informational, NOT a block. **rank_violations trend: 22 (c094) → 0 (c096) → … → 0 (c119) → 0 (c120) → 0 (c121). reachable trend: 36 → 81 → … → 139 → 139 → 156 (+17).**

### RE disposition (the central signal for the c122 RE-recheck + the batch-39 meta)

- **RE10 = DISCHARGED.** `L1/interpolator` LIVE (inbound: `L1/divfree-projector`, `L1/multigrid-relaxation-smoother`, `L4/waveguide_mode_reduce`, `L1-L0/interpolator-construction-rotation`). The construction-rotation theme is transitively live.
- **RE9/RE1/RE5/RE7 = GROUNDED.** The geometric-multigrid preconditioner column composes `fe_space_hierarchy`(RE9) / chebyshev+jacobi-smoother(RE1) / normalize(RE5) / reciprocal(RE7) as firm `depends-on`; `L1/fe_space_hierarchy` is now LIVE via both GMG levels. Under DIRECTIVE-2 the RE set is a discharge target, NOT a permanent floor.
- **CONSTRUCTIVE-KERNEL FRONTIER OPENED.** 3 roadmap_goal kernel-impls (`libceed-quadrature-kernel-impl`, `eigsolve-impl`, `lanczos_step`) land grounded-future `[GARBAGE*]` awaiting c122 consumers + the D6 shared-substrate harvest.
- Finalize did NOT ratify/move any RE in the baseline-exception ledger (meta-phase authority). The authoritative RE-recheck is the c122 planner's standing `--show-inbound` linter re-run.

### Counts

- Firm-count flips: +1 L1 firm (`multigrid-relaxation-smoother`, kernel-impl kind — tracked SEPARATELY from the main-cohort firm tally per its OQ); +1 firm concepts page (`FiniteElementSpaceHierarchy`); feature-column firm 12→13 (D9 waveguide-mode); GMG column rough-in (Infrastructure kind, tracked separately); 3 new roadmap_goal nodes; +1 L2 rough-in row (`correction_step`); +1 L1>L0 rough-in theme (`amr-estimate-mark-refine`) + 2 L1 rough-in verb rows.
- SLICE CORPUS: 0 (fully deleted cycles 097/098/099)

### Process

- retroactive-budget global = 0; per-report safety-net gates all PASS/N/A across all 9 reports
- 0 implied-component stubs created by finalize; 1 finalize build-repair (the README de-link — fallback, not a stub)
- **9/9 staging rows == 9 dispatched-ready** (the cycle-018 staging-completeness gap did NOT recur; 102nd consecutive clean staging); zero deferrals, zero rejections, zero per-report gate-hits
- 32 Open questions promoted by the per-report integrators across the 9 reports (appended to `scaffolding/open-questions.md`); finalize made no duplicate append — closure/migration is meta-phase authority
- `scaffolding/{roadmap,integrator-signals,cycle-record}` + `log/` committed atomically with the 9 consumed-report `integrated_at` touches; two-phase SHA-patch follows
- NO `.claude/agents/` changes FROM THIS FINALIZE
