## 2026-05-26 cycle-147 — refinement chebyshev [Ln→Ln] — revise

- Synthesis: Chebyshev L4 self-rotation: tighten apply signature from `ChebOp -> Field E -> Bool -> Solve (Field E) (Field E) ()` (sim state as positional args) to `ChebOp E S -> Solve (ChebSim E) ()` (sim state as capability-typed monad environment), making the read-only-x / read-write-y discipline visible at the type surface; clarify that variant absorption at L4 yields two distinct closure *types* (not a union) with one procedural shape; small clarifications around the unused `_rN`/`_stN` bindings in the fold. No layer advancement; tightening of the L4 form's type discipline.
- Verdict: revise.
- Friction: The diff is a lessons + log_synthesis emission only — no `slice_writes`, no `section_appends`, no `file_edits` carrying L4 prose actually appear in the patch shown. Two rotation_claims target the L3→L4 edge (more precisely, L4→L4 self-rotation by the claim's own admission) but no L4 prose lands on disk this cycle. Per check #14 (rotation_claims_require_surface), this is claims-without-surface: either emit the L4 prose in this cycle (revised apply signature, ChebSim record, capability typing) OR set `plan_kind=retroactive_claims` with a `retroactive_claim_evidence` block quoting the existing on-disk L4 prose that the claims reference.; Same structural issue: the variant-absorption clarification (two distinct closure types `ChebOp<E, Unit>` vs `ChebOp<E, { rho_prev: E }>` sharing one procedural shape) is asserted as a 'to_form' but the prose change is not present in any visible slice_writes/section_appends in this diff. The clarification is substantive (it disambiguates whether dispatch is by runtime tag vs by closure type) and needs to land in chebyshev.md's L4 section, not only in log_synthesis..
- Structural change: applied: 2 lesson(s); 2 rotation_claim(s).
