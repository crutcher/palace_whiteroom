## 2026-06-06 cycle-115 — 3 reports applied clean — 110th consecutive cycle under split integrator — **POSITION 1/3 OF META-BATCH-37, THE OPENER (cycles 115/116/117; an OUT-OF-BAND meta-phase fires NEXT-AFTER THIS finalize to enact two NEW USER DIRECTIVES of 2026-06-06; the scheduled batch-37 meta still fires after c117)** — the human-commissioned PLATEAU-PROBE batch opener + a user-directive content fix (D2 residual graded-stack hygiene `reachable` 132→133 +1; D3 PROSE-ONLY named-shape-groups relocation by user directive, reachability-neutral; D1 OBSERVATION-ONLY plateau-probe, VERDICT = frontier-exhaustion-OF-CURRENT-SCOPE CONFIRMED on all 3 fronts) → TRUE CUMULATIVE `reachable` 132→133 (+1, all D2), `detritus` 127→126 (−1); `rank_violations` HELD 0, `unresolved` HELD 0, `untyped` HELD 60 — pass

> Note: this file supersedes a stale pre-redirect-era `cycle-115` log entry (old slice-vertical numbering, 2026-05-26, `refinement arnoldi_step`); the cycle counter collided across the 2026-05-26 structural redirect. The current cycle-115 is the batch-37 opening cycle under the layered-stack flow. Old content recoverable from git history.

---

**POSITION 1/3 OF META-BATCH-37 — THE OPENER** (3:1 cadence; cycles 115/116/117; the cycle counter does NOT reset across batch boundaries; **an OUT-OF-BAND meta-phase fires NEXT-AFTER THIS finalize, as a SEPARATE dispatch, to enact two NEW USER DIRECTIVES of 2026-06-06 — this is NOT the scheduled batch-37 meta, which still fires after cycle-117's finalize**; this finalize ran NO meta-phase housekeeping). Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) + the 2026-06-02/2026-06-03 user directives + the 2026-06-04 GRADED RESOLUTION LADDER + FEATURE-ROOT REACHABILITY user directive (`METHODOLOGY-GRADED-STACK.md`) + the 2026-06-05 GROUND-don't-remove grounding directive + the 2026-06-06 named-shape-groups L4-calculus notation. NO session restart since c112 (mid-flow, no `.claude/agents/` change FROM THIS FINALIZE) → context continuity holds; **but the OUT-OF-BAND meta-phase that fires next WILL change role-specs and require a restart (its concern, not this finalize's).**

### Headline — the human-commissioned PLATEAU-PROBE batch opener + a user-directive content fix (`reachable` 132→133, +1)

Cycle-115 opened meta-batch-37 with three dispatches: a residual graded-stack hygiene grounding (D2, +1 reachable), a DIRECT-USER-DIRECTIVE prose-only relocation (D3), and an OBSERVATION-ONLY plateau-probe (D1) that independently re-derived frontier-exhaustion on both graded-stack axes and CONFIRMED exhaustion-OF-CURRENT-SCOPE on all 3 commissioned fronts. No firm-count flip — D2 frontmatter-only, D3 prose-only, D1 observation-only; the measurable movement is purely Axis-2 (reachability).

### What landed

- **D2 (layer-intro-author) — `residual-untyped-hygiene`.** Frontmatter-only typed-edge hygiene for the four c114-grounded, already-reachable, pre-scheme nodes:
  - `book/src/L1/fe_collection.md` — `rank: firm` + typed `edges:`: `depends-on` (`cites-evidence` → the L0 `ConstructFECollections` template `palace/fem/multigrid.hpp:22-73`; `lowers-to` → `L1-L0/fe-collection-construction-rotation`) + `reference` → `L1/fe_space` (consumer relationship).
  - `book/src/L1-L0/{dot,nrm2,scal}-mutation-rotation.md` — these carried **NO YAML frontmatter at all** (they started directly at the `#` heading); each gains a frontmatter PREPEND with `rank: firm` + `cites-evidence` `depends-on` edges to the core L0 source each theme lowers to (dot 6, nrm2 4, scal 2). Per the dispatch constraint these get their OWN outbound `cites-evidence` edges to L0 only (the op→theme `lowers-to` edge already landed c114 on the op side).
  - **+1 reachable** (132→133): the `fe_collection` → `L1-L0/fe-collection-construction-rotation` `lowers-to` `depends-on` edge RESCUED that theme from baseline detritus (forward mark-sweep from roots). `detritus` 127→126. `untyped` did NOT drop — the four nodes were never in the untyped-60 list (the linter prose-`## Status`-inferred their rank), which is exactly the subject of the promoted OQ `graded-stack-prose-status-inference-masks-untyped`.
- **D3 (layer-intro-author) — `named-shape-groups-relocation`. DIRECT USER DIRECTIVE (2026-06-06).** PROSE-ONLY trim of the *general* named-shape-groups rule/rationale/migration-note out of the three `linear_combination` entries, leaving each to USE the notation + LINK to `book/src/design/l4_calculus.md` §1.2.1:
  - `book/src/L4/linear_combination.md` — removed the general same-shape-contract restatement + the `Tensor[N]`-rank-1 anti-pattern teaching + the past-edit migration note; KEPT the op's OWN shape fact ("congruent over one group `S` of arbitrary/unknown rank; element-local at every position of `S`") + added a parenthetical pointing the general convention to §1.2.1.
  - `book/src/L3/linear_combination.md` — removed "the name `S` carries the same-shape contract; reuse of `S` … asserts congruence"; KEPT op's own shape fact + §1.2.1 pointer.
  - `book/src/L2/linear_combination.md` — removed "the name `S` carries the same-shape contract"; KEPT the aligned-fusion-kernels precondition (this op's OWN consequence, rephrased) + §1.2.1 pointer.
  - **NO `l4_calculus.md` edit** — the critic verified §1.2.1/§1.2.2/§4.1 already carry the complete rule; nothing added there. Reachability-neutral (HELD 133). Promoted OQ `named-shape-groups-general-rule-restatement-cohort-extent` — the pattern spans 27 files in 3 tiers, now governed by the forthcoming semantic-consolidation USER DIRECTIVE (superseding the producer's original "defer Tier C" recommendation).
- **D1 (cross-layer-cross-cutter) — `plateau-probe`. OBSERVATION-ONLY** (`## Proposed-changes` = None; mutates no `book/` artifact). Independently re-derived frontier-exhaustion on BOTH graded-stack axes WITHOUT trusting the batch-36 meta assessment. **VERDICT: exhaustion CONFIRMED on all 3 commissioned fronts** — (1) reachability: every one of the 23 STRONGER-GARBAGE members is covered by a ratified RE1-RE8 baseline-exception; no genuinely-faithful inbound `depends-on` from a reachable node was missed (the would-be grounds RE2 `krylov-step→L3/orthogonalize`, RE8 `transient→L3/fold_solve` confirmed UNFAITHFUL — the reachable consumers compose at L2/L4, deliberately skipping the L3 iteration-views); (2) forward vocabulary: all 8 `promotion_frontier` members are genuinely obstruction-/demand-gated (each chapter's §Status read + gate cited); (3) coverage holes: every in-scope feature/vocabulary item has a chapter or is a tracked deferral — `build_mesh` is the standing candidate-(c) "Mesh-wrapper vocabulary proper" deferral under the MFEM-opaque + single-machine scope-out, NOT an unfiled hole. The verdict is **exhaustion-OF-CURRENT-SCOPE, NOT terminal** — the integrator appended a READ-CONTEXT note: NEW USER DIRECTIVE B (2026-06-06, open-all-feature-fronts-simultaneously post-consolidation) fires the demand-gate trigger, so the deferred fronts are now SLATED TO OPEN.
- **TRUE CUMULATIVE (all three applied, re-measured by integrator-finalize on the landed tree):** `reachable` 132→133 (+1, all D2; D3 prose-only neutral, D1 observation-only neutral); `detritus` 127→126 (−1); `rank_violations` HELD 0; `unresolved_depends_on_targets` HELD 0; `untyped` HELD 60; `roots` HELD 36; firm histogram HELD.

### Two benign linter-semantics flags carried (from D1, for the meta-phase)

- **roots=36 vs the "40 columns" headline** — counting convention: roots=36 = 12 feature columns × 3 levels. Reconciles cleanly.
- **`boundary-mode.{L0,L1,L4}` double-counted** as ROOT + `promotion_frontier` — inflates the "8" `promotion_frontier` figure by 3.

Both are benign measurement-semantics notes, carried into D1's OQ for the meta-phase; neither is a defect in the artifact.

### Build

`cargo make book` (mdbook + linkcheck2) EXIT 0. D2's 4 frontmatter edits + D3's 3 prose-only trims all have on-disk targets; D3's §1.2.1 anchor-links all resolve (`[`l4_calculus`](../design/l4_calculus.md)` references intact: L4 = 3 incl. the new parenthetical, L3 = 2, L2 = 2; `design/l4_calculus.md` §1.2.1 exists; no fragment-anchor in the link URLs → nothing to break; no KaTeX/table breakage from the trim). No new file → no SUMMARY/index insert. Only the pre-existing benign `Potential incomplete link` WARNs (markdown-table / KaTeX false-positives, NOT link errors). **NO finalize build-repair needed.**

### Step-5b — graded-stack linters (the build-gate companion, ran on the landed tree)

**`rank_violations: 0`** (`[]`; baseline fully discharged c096 so ANY violation would be NEW and BLOCK — there are NONE, **GATE PASSES**; all D2 source nodes `rank: firm` with all `cites-evidence`/`lowers-to` targets on-disk ≥-firm or rank-terminal-L0; D3 prose-only authored no edges) + **NO newly-orphaned node** (`reachable` CLIMBED 132→133) + **`unresolved_depends_on_targets: 0`** (HELD). Totals on the landed tree: `files=355`, `typed=295`, **`untyped=60`** (HELD), `roots=36`, **`reachable=133`** (was 132, +1), `rank_violations=0`, `unresolved_depends_on_targets=0`, `promotion_frontier=8`, **`detritus=126`** (`detritus_no_typed_edges_pre_p1_artifact=103`, `detritus_with_typed_edges_stronger_signal=23`, `expected_unreachable_outside_dag=44`).

**rank_violations trend: 22 (c094) → 1 (c095) → 0 (c096) → … → 0 (c113) → 0 (c114) → 0 (c115). reachable trend across the campaign: 36 (c105-end, pre-linter-fix) → 81 (batch-33 meta-phase) → 88 (c106) → 95 (c107) → 102 (c108) → 107 (c109) → 119 (c110) → 122 (c111) → 123 (c112) → 124 (c113) → 132 (c114) → 133 (c115).**

**On the high `untyped`/`detritus` mass:** informational, NOT a block (per step-5b — the as-yet-untyped pre-P1 tail + the typed-but-unreached nodes under the ratified RE1-RE8 baseline-exceptions). Only a *new* rank violation or a *newly*-orphaned node gates; neither occurred.

### Counts

No firm-count status flips this cycle — D2 frontmatter-only hygiene, D3 prose-only, D1 observation-only (no new authored vocabulary, no node promotion). Layer tallies unchanged: L1 firm 33 main / 40 grand, L4 firm 21 main / 25 grand, L4>L3 11, L3 17+4po, L3>L2 6, L2 21+1pc, L2>L1 11, L0 22, concepts 34, methodology 4, feature spine 11 firm / 1 seed, L4 reduce-family 4 verbs ALL firm. SLICE CORPUS: 0 (corpus fully lifted + deleted across c097/098/099). The measurable movement is entirely on the reachability axis (`reachable` 132→133, the `fe_collection`-theme grounding).

### Process

Retroactive-budget global = 0. Per-report safety-net gates all PASS/N/A across all 3 rows (rank-well-foundedness 0, edge-label/prose-mismatch 0, YAML round-trip 0, SUMMARY-registration N/A [all slugs pre-exist], forward-edge-without-surface 0, append-on-missing-slug 0, variant-axis-missing 0). 0 implied-component stubs created. 3/3 staging rows == 3 dispatched-ready reports — the cycle-018 staging-completeness gap did NOT recur (96th consecutive clean staging / 110th consecutive clean split-integrator cycle). **All 3 reports applied clean** — no repair-phase warnings carried to finalize. **CITECHECK NOTE (non-blocking):** the per-report integrators noted citecheck hits — D2: 20 ok / 0 failing; D3: 10 ok / 16 AMBIG (ALL bare-basename slugs confined to the §"Cohort-wide extent" FINDING-section inventory listing, NONE backs an applied change); D1: 21 ok / 2 MISS (both `scaffolding/priorities.md` INTERNAL cross-references outside citecheck's search roots, backing a FINDING not an applied change, both verified to resolve on disk at `:801`/`:848`). Recorded not repaired — informational finding-prose, not landed-citation defects. OQs appended to the ledger this cycle (append-only between meta-phases; the meta-phase unifies/closes). `scaffolding/{open-questions,roadmap,integrator-signals,cycle-record}.md/.jsonl` + `log/` committed atomically with the `book/` edits + all 3 consumed-report `integrated_at` touches. NO `.claude/agents/` changes FROM THIS FINALIZE → no session-restart concern from the finalize itself (but the OUT-OF-BAND meta-phase that fires next WILL enact role-spec changes requiring a restart).

### Signals for the IMMINENT OUT-OF-BAND meta-phase (fires NEXT-AFTER this finalize — NOT the scheduled c117 meta — to enact the two 2026-06-06 directives)

- **Directive A (semantic consolidation):** evolve the draft calculus section `book/src/design/l4_calculus.md` into an ACTIVELY-MANAGED semantic surface (liveness/unification/consolidation of semantic rules/defs/abstractions, analogous to the graded-stack machinery); per-unit semantic-rule restatements get trimmed/consolidated (D3 was the pilot — and the OQ `named-shape-groups-general-rule-restatement-cohort-extent` finds the pattern across 27 files in 3 tiers, now governed by THIS directive, not the old "defer Tier C"); the calculus surface is to be REORDERED to sit BEFORE the L4 section in SUMMARY.md.
- **Directive B (open all feature fronts simultaneously, post-consolidation):** answers the plateau ASK — open ALL remaining deferred feature fronts at once (waveguide-mode, boundary-mode, fe_space siblings, mesh-wrapper, etc.) to exploit shared exploration lifting; sequenced AFTER the consolidation.
- **The D1 plateau verdict** = exhaustion-OF-CURRENT-SCOPE (NOT terminal — Directive B now opens the deferred fronts) + the 2 benign linter-semantics flags (roots=36 = 12 columns × 3 levels reconciles the "40 columns" headline; `boundary-mode.{L0,L1,L4}` double-counted as ROOT + `promotion_frontier` inflates the "8" by 3).
- **The carried OQs:** D2's `graded-stack-prose-status-inference-masks-untyped`; D3's `named-shape-groups-general-rule-restatement-cohort-extent` (the 27-file cohort, now under Directive A); D1's 4 plateau-probe OQs (`plateau-probe-front1-no-missed-faithful-ground`, `plateau-probe-front2-all-8-frontier-members-genuinely-gated`, `plateau-probe-front3-no-true-coverage-hole`, `plateau-probe-linter-roots-36-vs-columns-40-and-seed-root-in-frontier`).
- **NEXT:** the OUT-OF-BAND meta-phase fires after this finalize as a SEPARATE dispatch; it WILL change role-specs → WILL require a session restart. The scheduled batch-37 meta-phase still fires after cycle-117's finalize.
