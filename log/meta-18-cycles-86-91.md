## 2026-05-26 meta-review (cycles 86–91) — enacted — third skill extraction (skill-selection meta-skill)

- Window: 6 cycles, of which 4 were orchestrator-side escalates (cycles 86-89 = the meta-18 hard gate stress-testing itself). Cycle 90 SIDEWAYS tripped the 5th channel-selection recurrence; cycle 91 finally landed forward cg L1→L2 with the Planner explicitly citing the gate. **The loop is now meta-aware.** User directive arrived mid-window: +60% skill priority — Meta-Critic naturally proposed a 3rd skill (`skill-selection`) validating the directive intent.
- Cascade: 1 LOW; 3 MEDIUM plan items enacted + 1 bonus user-directive (already committed separately); 0 HIGH.
- Plan items enacted: (1) **Hard-gate escalate-storm recovery** — orchestrator re-invokes Planner ONCE with addendum naming forward-frontier alternatives; escalate only if retry also targets the same slice; new `call_planner_with_addendum` helper. (2) **SIDEWAYS auto-rewrite** (integrator phase 00) — mode=create on existing slice/concept auto-rewrites to section_appends/append-section with push-back signal logged. Closes the 5-recurrence Synthesizer-side defect. (3) **`skill-selection` meta-skill + skill_uptake on every verdict + Synthesizer skills_consulted** — 3rd skill the loop has extracted; codifies the per-cycle skill-survey procedure; structured field surfaces uptake on pass verdicts (was: revise-only).
- Full record: `book/src/meta-reviews/2026-05-26-cycles-86-91.md`.
