## 2026-06-01 cycle-051 — 5 reports applied clean — forty-sixth consecutive cycle under split integrator — THIRD/FINAL PRIMARY CYCLE OF META-BATCH-15 — refactor-pass theme-demotion sweep COMPLETE under the 2026-06-01 VOCABULARY-SHIFT REDIRECT — pass

**THIRD/FINAL PRIMARY CYCLE OF META-BATCH-15** (3:1 cadence; cycles 049/050/051; the cycle counter does NOT reset across batch boundaries; the **batch-15 meta-phase fires AFTER this cycle-051 finalize as a separate dispatch**). The **LAST refactor-pass enactment cycle under the 2026-06-01 VOCABULARY-SHIFT REDIRECT** (`METHODOLOGY-REDIRECT.md`; CLAUDE.md §Methodology invariants ⟢). A clean opus-planner cycle.

**Headline: the refactor-pass theme-demotion sweep COMPLETE — final batch-15 cycle: fold-family + nrm2-consumer + jacobi + divfree-L3>L2 demoted (15 themes deleted), 5 L3 leaves re-expressed through `linear_combination`/`inner_product`, `divfree-projector-leaf-identity` KEPT (genuine fusion rotation); L3>L2 13→5, L2>L1 17→10; leaf-CHAPTER deletions HELD for batch-15 meta-phase.**

- **Staging completeness:** 5 of 5 dispatched-ready reports applied clean (5/5 staging rows == dispatched-ready — the cycle-018 staging-completeness gap did NOT recur for the THIRTY-SECOND consecutive cycle); zero deferrals, zero rejections.
- **HEADLINE — refactor-pass theme-demotion COMPLETES.** Under the redirect, the stack is a sequence of genuine representational + component-vocabulary shifts; a degenerate identity-in-named-terms lowering is a smell (the vocabulary failed to shift). The D8-corrected **17-pair degenerate-lowering cohort** is now fully dispositioned across cycles 050+051: **16 pairs demoted to in-line notes, 1 KEPT-substantive.** This cycle landed the fold-family + consumer + verified-gate remainder.
  - **D1 (lifter) `linear_combination` family — APPLIED, build-relevant.** Deleted 8 themes (`{scal,axpy,axpby,axpbypcz}-{body,leaf}-identity`); re-expressed the 4 L3 leaves (`scal` arity-1, `axpy` arity-2-second-coeff-1, `axpby` general arity-2, `axpbypcz` arity-3 incl γ==0 collapse) **through `L3/linear_combination`** (frontmatter `lowers_to`/`lifts_from`, H1, Context, §"Lowers to"→combinator route, Dependencies, Evidence dep-blocks). **BUILD-CRITICAL** `L3/index.md` repairer block re-pointed the 6 stale live links in rows 24/25/26 to the combinator route + KEPT fold-specialization theme. Defensive de-links of `scal-*-identity` live links in sibling delete-target files (jacobi/divfree). 1 OQ promoted. First per-report integrator of cycle-051 (created STAGING.md).
  - **D2 (lifter) `dot` family — APPLIED, build-relevant.** Deleted `dot-{body,leaf}-identity`; re-expressed `L3/dot` **through `L3/inner_product`** (6 [old/new] blocks: frontmatter, lede, §Context, §"Lowers to"→§"Downward to L2 (through inner_product)", §Dependencies combinator + L2>L1-translation anchors, §Status, §Evidence, §"L3 vs L1 distinction"). De-tensed `L3/inner_product` + `L2/inner_product` pre-built-home notes to past-tense statements of fact. 3-way co-edit on KEPT `divfree-projector-leaf-identity.md` line 266: D2's `dot-leaf-identity` de-link applied via the distinct dot-only substring. 2 OQs promoted.
  - **D3 (lifter) `nrm2` consumer-demotion — APPLIED, build-relevant.** Deleted `nrm2-{body,leaf}-identity`; added in-line §"Downward to L2 (consumer identity-in-form; no theme file)" notes on `L3/nrm2` + `L2/nrm2`. **CONSUMER-DEMOTION (do-NOT-merge) honored** — `nrm2` NOT folded into `inner_product`; `inner_product`/L1/any-fold-entry NOT touched. The `std::abs` guard preserved as an explicit load-bearing claim in BOTH notes; `vector.hpp:255-260` Norml2 L0 anchor preserved byte-exact. De-linked the surviving `nrm2-leaf-identity` live link on the KEPT divfree-leaf line 266 (third of the 3-way co-edit). 2 OQs promoted.
  - **D4 (lifter) jacobi + divfree-L3>L2 — APPLIED, build-relevant.** Deleted `jacobi-smoother-{body,leaf}-identity` + `divfree-projector-body-identity` (3 theme files); re-anchored `L3/jacobi-smoother` + `L2/jacobi-smoother` + `L3/divfree-projector` to in-line degenerate-identity notes (`jacobi.cpp:38` `DI[i]*X[i]` anchor preserved; `divfree.cpp:185`/`:180-181`/`:155-187` anchors preserved). **KEPT `divfree-projector-leaf-identity` (L2>L1)** — the one genuine step-4 `Grad->AddMult` fusion rotation — preserved (file, SUMMARY line, L2-L1 index row 27, working-notes bullet) and made **reachable via 2 DIRECT live links from `L3/divfree-projector`** (§Context Downward + §Lowers-to). The cross-dispatch danglers D2/D3 flagged at `divfree-projector-body-identity.md:22,231` RESOLVED — that file is deleted. 1 OQ promoted.
  - **D5 (layer-intro-author, SOLE count-owner) — APPLIED, build-relevant.** Consolidated tallies: `L3-L2/index.md` cohort-growth `17 → 13 (cycle-050) → 5 (cycle-051)` + ALL-SIX-DEMOTED discharge note; `L2-L1/index.md` cohort-growth `21 → 17 (cycle-050) → 10 (cycle-051)`; `L3/index.md` degenerate-cohort discharge note (authoritative L3 line-63 tally NOT touched — stays 17 firm + 3 partial-obstruction). Serial-order honored (A.2's `[old]` was D4's `[new]` `17 → 13 → 11` verbatim, corrected to FINAL consolidated 5). Change-C RECONCILED against D2's prior past-tensing per the dispatch's explicit reconcile instruction. 3 OQs promoted. FINAL per-report integrator.
- **Count deltas — a vehicle-change, NOT a coverage regression (D5 sole count-owner).** **L3>L2 firm 13 → 5** (8 fold-family `-body-identity` + `dot-body-identity` + `nrm2-body-identity` + `jacobi-smoother-body-identity` + `divfree-projector-body-identity` deleted); **L2>L1 firm 17 → 10** (+1 partly-constructive `deflate-composition-lowering` unchanged; 8 fold-family `-leaf-identity` + `dot-leaf-identity` + `nrm2-leaf-identity` + `jacobi-smoother-leaf-identity` deleted, `divfree-projector-leaf-identity` KEPT); **L3 firm UNCHANGED at 17 + 3 partial-obstruction** (the 5 leaves were re-expressed in place, not removed). The firm-theme DROPS are a **vehicle-change** (theme file → in-line §"Downward" note / combinator re-expression), NOT a coverage regression (OQ `c050-firm-theme-count-drop-is-vehicle-change-not-coverage-regression`). The surviving 5 L3>L2 themes are the substantive/non-identity rotations: `krylov-step-body-identity`, `ksp-solve-outer-driver`, `orthogonalize-variant-split`, `eigsolve-opaque-eigen-iteration`, `chebyshev-nested-recurrence`. Unchanged: L1 firm 26, L2 firm 21, L4 firm 6 + 6 firm L4>L3 + 4 outer-driver rows, L0 chapters 22, Phase-1 removals 9/10.
- **Degenerate-cohort fully dispositioned (D8-corrected 17-pair denominator):** **16 pairs demoted** across c050 (4 non-fold: `assemble-diagonal`/`elementwise_product`/`reciprocal`/`normalize`) + c051 (`linear_combination` fold-family ×4 + `dot` + `nrm2`-consumer + `jacobi-smoother` + `divfree-projector-body-identity`), **1 KEPT** — `divfree-projector-leaf-identity` (the one genuine step-4 `Grad->AddMult` fusion rotation). 15 theme files deleted this cycle; 5 L3 leaves re-expressed in place through the 2 cycle-050 firm L3 combinators.
- **Process:** retroactive-budget global = 0; ZERO leaks. **NOTABLE — multi-deletion cross-dispatch dangling-link coordination at SCALE (15 deletions):** the 3-way co-edit of the KEPT `divfree-projector-leaf-identity.md` line 266 (`scal-`/`dot-`/`nrm2-` de-links by D1/D2/D3) was resolved via distinct-substring narrowing so the three de-links **composed order-independently** — a strong reusable signal for future mass-deletion cycles. The cross-dispatch danglers in sibling delete-target files (D4's `divfree-projector-body-identity.md:22,231`) died with the files; per-report integrators correctly did NOT edit moot delete-targets. 10 OQs opened across D1–D5; the count-reconciliation OQs effectively resolved by D5's landing, the rest route to the batch-15 meta-phase.
- **Build:** clean — `cargo make book` exit 0 (~91s); the **mandatory dead-link sweep** for all 15 deleted slugs returned ZERO live links (the 6 live links to the KEPT `divfree-projector-leaf-identity` are CORRECT and excluded). GFM-table contiguity verified after the row deletions: `L3-L2/index.md` = 5 theme rows, `L2-L1/index.md` = 11 rows (10 firm + 1 partly-constructive) — both render (D1's repairer used the kept-neighbor-re-emit pattern). The only build noise is pre-existing and unrelated: KaTeX "Potential incomplete link" false-positives in `design/l4_calculus.md` + older markdown-table HTML-tag WARNs in unchanged files. **NO build-repair needed.** The 2 citecheck OOB warnings (`L2-L1/index.md:73`, `L3-L2/index.md:63`) are non-load-bearing report-PROSE line-drift in append-only OQ prose — a foreseeable residual of mass-deletion cycles; NOT artifact defects (flagged for the batch-15 meta-phase as a candidate finalize-time line-reference re-pin convention).
- **Open refactor remainder is exclusively meta-gated:** (1) the leaf-CHAPTER disposition (`collapsed-leaf-disposition-convention-cohort-wide` — delete-vs-redirect-stub for the now-orphaned-as-specialization-notes L2/L3 leaf chapters; the ONE gating decision blocking batch-16) + (2) the low-priority `l4-propagation-depth-linear-combination` flag.
- **Plan note (priorities.md):** the cycle-planner already reshaped the active head this cycle; integrator-finalize appended only a batch-15-meta-phase hand-off note (refactor-pass theme-demotion COMPLETE; the meta-phase must ratify `collapsed-leaf-disposition-convention-cohort-wide`, assess redirect program items 2+3 as the batch-16 frontier, and triage the friction signals). priorities.md is co-owned by cycle-planner + meta-phase; integrator-finalize did NOT clobber the planner's active-head edit.
- **NEXT: the batch-15 meta-phase (aggregating cycles 049/050/051) fires as a separate dispatch after this finalize.** The cycle counter does NOT reset.
- Written by `integrator-finalize` (split integrator-per-report ×5 + finalize ×1).

---

## 2026-05-25 cycle-51 — back cg — pass

- Synthesis: Retroactive L0→L1 rotation_claims for cg.md v0.3: emitted 4 per-substitution claims (mutation-erasure with destination-renaming for AXPBY/Add/A->Mult/ApplyB; workspace-aliasing rename of p→p_tmp; CheckDot partial-function guard surfacing; faithful rendering of the !B-branch initial-residual quirk). retroactive_claim_evidence: 
  - claim_index: 0 (mutation-erasure)
    on_disk_path: book/src/spec/slices/cg.md
    section: ## L1
    quoted_lines: |
      let r₀ = b - apply A x_in                    // A->Mult(x, r); AXPBY(1, b, -1, r)
      ...
      let p' = if it == 0 then z else axpby 1.0 z (beta/beta_prev) p
      let z'_pre = apply A p'                      // A->Mult(p, z)
      let x' = x + alpha · p'                      // x.Add(alpha, p)
      let r' = r - alpha · z'_pre                  // r.Add(-alpha, z)
      let z' = apply B r'                          // ApplyB(B, r, z); or z = r if !B
  - claim_index: 1 (workspace-aliasing rename)
    on_disk_path: book/src/spec/slices/cg.md
    section: ## L1 (setup block) and Notes
    quoted_lines: |
      let p_tmp = apply B b                        // tmp use of p as Bb workspace
      sqrt |dot p_tmp b|                           // or Norml2(b) if !B
      ...
      - The `initial_guess` branch reuses `p` as scratch for computing `(Bb, b)`; L1 binds it to a local `p_tmp` since it is not the iteration's search direction.
  - claim_index: 2 (CheckDot guard surfacing)
    on_disk_path: book/src/spec/slices/cg.md
    section: ## L1 Notes
    quoted_lines: |
      - `check_dot` (Palace's `CheckDot`, [palace/linalg/iterative.cpp:244-250](../../../../reference/palace/linalg/iterative.cpp#L244-L250)) is a partial-function guard at each new inner-product site: it aborts execution if the result is non-finite or, on real SPD systems, negative (signalling loss of positive-definiteness). L1 surfaces it as a `check_dot β'` assertion;
  - claim_index: 3 (initial-residual quirk faithful rendering)
    on_disk_path: book/src/spec/slices/cg.md
    section: ## L1 Notes
    quoted_lines: |
      - **Initial-residual quirk in the no-preconditioner branch (`!B && initial_guess`).** Palace computes `beta_rhs = Norml2(b) = sqrt|(b,b)|` then sets `initial_res = sqrt|beta_rhs|`, yielding `initial_res = (b·b)^{1/4}` — not `‖b‖₂`. The preconditioned branch computes the correct `sqrt|(Bb, b)|`. L1 preserves Palace's source behavior; the L4 modeling and Working Notes flag this as a likely Palace bug.
- Verdict: pass.
- Friction: none.
- Structural change: applied: 2 lesson(s); 4 rotation_claim(s).
