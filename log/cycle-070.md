## 2026-06-02 cycle-070 — 5 reports applied clean — sixty-fifth consecutive cycle under split integrator — **FIRST/LEAD PRIMARY CYCLE OF META-BATCH-22** — the **DRIVEN SOLVE-half reaches L4** (`frequency_sweep` L4 + `frequency-sweep-dissolution` L4>L3) — the **LAST OPEN PIPELINE-HALF is CLOSED; the whole assemble+solve deliverable now reaches L4 across all 5 pipelines** — + the **NEW FEATURE-SURFACE SPINE OPENED** (first exemplar column: electrostatic at L4/L1/L0) — **+1 firm L4 (13→14), +1 firm L4>L3 (9→10), +1 feature-surface Part** — pass

> **Note:** this file supersedes a stale May-25 slice-vertical-era `cycle-70` placeholder (forward gmres L0→L1 citation tightening; pre-structural-redirect numbering collision). The prior content is preserved in git history.

**FIRST/LEAD PRIMARY CYCLE OF META-BATCH-22** (3:1 cadence; cycles 070/071/072; the cycle counter does NOT reset across batch boundaries; **the batch-22 meta-phase fires AFTER cycle-072's finalize as a SEPARATE dispatch** — not this cycle; this finalize does NOT run meta-phase housekeeping). Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) + the FIVE 2026-06-02 user directives: directive-1 *L4 is the backend-lowering target* (every in-scope feature must reach L4); directive-2 *black-box vs accelerated kernels*; directive-3 *mdBook sub-chapter grouping + alphabetical API lists*; directive-4 *reader-facing Methodology GOAL+FLOW chapter*; directive-5 *FEATURE-SURFACE SPINE* (recorded post-batch-21-meta — Palace's entry-point features get a top-down composition-root spine parallel to the bottom-up vocabulary spine).

**Headline: the DRIVEN SOLVE-half reaches L4, closing the last open pipeline-half — the whole assemble+solve deliverable now reaches L4 across all 5 pipelines — and the new FEATURE-SURFACE SPINE opened with its first exemplar column.** The batch-21 meta-phase decided verdict (a) LIFT for the driven solve-half: this cycle authored the driven feature's OWN single-witness L4 form `frequency_sweep` (the operator-VARYING per-ω sweep re-solving through the firm `L4/assemble_frequency_operator` at each swept frequency) + its coupled L4>L3 dissolution. Completeness directive (directive-1) governs upward-to-L4 and is orthogonal to the shared-combinator-mining stop — the shared `map_solve` generalization stays BARRED at the ≥2-witness STOP (reconciled, NOT lifted). In parallel, the FEATURE-SURFACE SPINE (directive-5) opened with the electrostatic exemplar column at L4/L1/L0, and two hygiene re-anchors (L3 dot/nrm2 leaf staleness; concept-page L1→L4 link upgrade) closed their parent OQs.

- **Staging completeness:** 5 of 5 dispatched-ready reports applied clean (5/5 staging rows == dispatched-ready — the cycle-018 staging-completeness gap did NOT recur for the FIFTY-FIRST consecutive clean staging / SIXTY-FIFTH consecutive clean split-integrator cycle); zero deferrals, zero rejections, zero gate-hits, zero build-repairs.

### What landed

- **D1 (harvester, build-relevant) — `frequency_sweep` PROMOTED FIRM L4 (the driven solve-half; the LAST OPEN pipeline-half closed).** `book/src/L4/frequency_sweep.md`: the driven pipeline's solve-half single-witness L4 form — the **operator-VARYING per-ω sweep** that re-solves through the firm `L4/assemble_frequency_operator` at each swept frequency. The **batch-21-meta-phase-decided LIFT (verdict (a))**: authors the driven feature's OWN single-witness L4 form (directive-1: every in-scope feature must reach L4) while the shared `map_solve` generalization stays BARRED at the ≥2-witness STOP — the completeness directive governs upward-to-L4, orthogonal to the shared-combinator-mining stop. Status **firm** (4 variant axes declared, 1 load-bearing + 3 absorbed/pinned). Appended its OWN `L4/index.md` §Vocabulary-cohort bullet (alpha, between `fold_solve` and `solve_family`) + dep-map row + the firm tally bump `(13 + 4)`→`(14 + 4)` + SUMMARY L4 insert. (First per-report integrator of c070 — created the staging dir + file. The critic's alternative `(13 + 5 outer-driver)` tally was VERIFIED WRONG by the repairer; 13→14 firm-chapter is correct, `+4` outer-driver unchanged.) `citecheck --scan`: 27 ok / 0 fail. No OQ promoted.
- **D2 (abstractor, build-relevant) — `frequency-sweep-dissolution` LANDED FIRM L4>L3.** `book/src/L4-L3/frequency-sweep-dissolution.md`: the coupled L4>L3 lowering of the operator-varying sweep (operator-capture axis fixed|per-element load-bearing, exhaustively handled). LHS live-link `../L4/frequency_sweep.md` resolves (D1 landed it earlier this cycle). Sole L4>L3-index toucher: consolidated-tally FULL-PARAGRAPH REPLACE `8 → 9 / 9 firm` → `9 → 10 / 10 firm` (corrected the stale "D3 owns the L4 tally" → "D1") + dep-map row + §Vocabulary-cohort bullet + SUMMARY L4-L3 insert. `citecheck --scan`: 6 ok / 0 fail. No OQ promoted. (Second per-report integrator of c070.)
- **D3 (layer-intro-author, build-relevant) — the NEW FEATURE-SURFACE SPINE opened with its first exemplar column (directive-5).** Created a NEW top-level Part `# Feature surfaces — entry points`: `book/src/feature/index.md` (the spine overview + feature×level matrix + chapter-kind status) + the **electrostatic** exemplar at L4/L1/L0 (`book/src/feature/electrostatic.{L4,L1,L0}.md`). The composition root: config → `fe_assemble` (assemble K once) → `solve_family` (fixed-operator corner) → capacitance reduction (Vⱼᵀ K Vᵢ) → capacitance-out. **FIRST instance of the composition-root chapter kind** (`status: seed (exemplar)`), authored ahead of role-spec codification. SUMMARY wires the new Part between Methodology and L4 (disjoint region from D1/D2's L4/L4-L3 list edits — no collision). All in-chapter code 4-space-indented (ZERO triple-backtick fences). Firmness floor (repair-corrected, on-disk verified): TWO of four composed L1 constituents firm (`fe_assemble`, `ksp_solve`); BOTH capacitance-reduction primitives rough-in (`matrix-weighted-norm` test-coverage-bounded + `bilinear-form`) — stage-3 rests on rough-in primitives, correct for a seed. `citecheck --scan`: 20 ok / 0 fail; all 10 distinct relative links resolve on-disk. **2 OQs promoted (standing batch-22-meta items, LEFT OPEN):** `feature-surface-kind-adapted-check-codification` + `feature-surface-part-path-layout-and-within-column-level-ordering-ratification`. (Third per-report integrator of c070.)
- **D4 (lifter, build-relevant) — `L3/dot` + `L3/nrm2` LEAF stale-no-L4 → live-link re-anchor.** `book/src/L3/dot.md` + `book/src/L3/nrm2.md`: the `lifts_from` frontmatter + §"Lifts from" prose flipped from "no L4 entry exists" → firm live links `../L4/dot.md` / `../L4/nrm2.md` (identity-in-form / consumer-not-fold-member framing), each with a `> Superseded` blockquote preserving the cycle-010 "no-L4-by-design" rationale; `book/src/L3/index.md:66` BLAS-1-cohort clause corrected in-line to the per-case `black-box-vs-accelerated-kernels` §2 disposition. Both L3 entries stay **firm** (no status flip, no index-cell touch). ENACTS + CLOSES the OQ `l3-dot-nrm2-stale-no-l4-entry-lines-need-reanchor` (closed-ENACTED-c070-D4 in the OQ-ledger migrated-to-plan index). `citecheck --scan`: 10 ok / 0 fail; all link targets resolve on-disk. (Fourth per-report integrator of c070; zero file overlap — L3 only, no SUMMARY touch.)
- **D5 (lifter, build-relevant) — `black-box-vs-accelerated-kernels` concept page `fe_assemble` L1→L4 link upgrade.** `book/src/concepts/black-box-vs-accelerated-kernels.md`: two `fe_assemble` references re-pointed `../L1/fe_assemble.md` → `../L4/fe_assemble.md` (`:69` case-1 sibling list + `:143` See-also) + the `:69` prose tightening "the assemble combinator" → "the risen assemble combinator" (backed by the firm `L4/fe_assemble.md:169` rise claim). The two were the COMPLETE set of `../L1/fe_assemble` links on the page (post-edit grep confirms zero remain; the L1 cap stays the firm lower home via the lowering chain). ENACTS + CLOSES the OQ `l4-fe-assemble-absent-forward-ref-for-blackbox-kernel-page` (closed-ENACTED-c070-D5). `citecheck --scan`: 5 ok / 0 fail. (Fifth/last per-report integrator of c070; zero file overlap — concept page only, no SUMMARY touch.)

### Counts

- **+1 firm L4 entry; L4 firm 13→14** (`frequency_sweep` added to the prior 13). The L4 outer-driver vocabulary rows (solve_loop/restart_cycle/Outcome/EigOutcome = 4) unchanged; the `(13 + 4)` inner+outer-driver tally moves to `(14 + 4)`. L4 rough-in (1) `solve_family` unchanged.
- **+1 firm L4>L3 theme; L4>L3 firm 9→10** (`frequency-sweep-dissolution`).
- **+1 feature-surface Part / +1 exemplar column** — the NEW `book/src/feature/` top-level Part (1 overview + the electrostatic L4/L1/L0 exemplar column, `status: seed (exemplar)`).
- All other layer counts UNCHANGED from cycle-069: L1 firm 34, L2 firm 21 + 1 partly-constructive, L2>L1 firm 21, L3 firm 17 + 4 partial-obstruction, L3>L2 firm 6, L0 chapters 22, Phase-1 removals 9/10; FE-assembly sub-spine 4, FE-space sub-spine 3 firm L1 operators; concepts pages 26, methodology chapters 2.
- **Per-pipeline L4-completeness picture: COMPLETE across all 5 pipelines (assemble + solve).** With c068 (`fe_assemble`) + c069 (`assemble_frequency_operator`) closing the assemble-half and c070 (`frequency_sweep`) closing the driven solve-half — the one remaining gap — **the whole assemble+solve deliverable now reaches L4 across all 5 pipelines.** The `map_solve`-over-(operator,rhs)-family shared generalization remains barred at the ≥2-witness STOP (reconciled by the driven feature's own L4 form, NOT lifted).

### Process

- retroactive-budget global = **0** (D1/D2/D3 new firm entries — `frequency_sweep` + `frequency-sweep-dissolution` + the feature exemplar column; D4/D5 pure pointer/link re-anchors with no source-citation END moved). ZERO dispatch-phase leaks; 0 NEW OQ opened; **2 OQs closed-in-artifact** (D4 + D5 ENACTED their parent OQs) + 2 closure-notes appended; 2 standing batch-22-meta OQs (feature-surface kind-check + path/level-ordering ratification) LEFT OPEN per dispatch; 0 resolved-in-report-notes.
- **Serial per-report application order D1→D2→D3→D4→D5 (per staging applied_at 230022Z/230145Z/230907Z/231530Z/232140Z) partitioned cleanly with ZERO file overlap.** D1 created `L4/frequency_sweep.md` + its own `L4/index` row/bullet/tally + SUMMARY L4 insert; D2 created `L4-L3/frequency-sweep-dissolution.md` + the `L4-L3/index` consolidated-tally REPLACE + row/bullet + SUMMARY L4-L3 insert; D3 created the NEW `book/src/feature/` Part (index + electrostatic L4/L1/L0) + SUMMARY new top-level Part insert; D4 edited `L3/{dot,nrm2,index}.md` (L3 only); D5 edited `concepts/black-box-vs-accelerated-kernels.md` (concept page only). **D1-first ordering is load-bearing** — D2's L4>L3 LHS live-link `../L4/frequency_sweep.md` resolves because D1 landed first. SUMMARY.md touched by D1/D2/D3 in disjoint regions (L4 list / L4-L3 list / new top-level Part) — no collision. No reconciliation needed.
- **Same-cycle / cross-cycle cross-links all resolve:** D1↔D2 `frequency_sweep`↔`frequency-sweep-dissolution`; D3's feature down-links to firm L4/L1 ops; D4/D5's L4 `dot`/`nrm2`/`fe_assemble` targets (all firm on-disk). No stub materialized, no plain-text downgrade.
- **Build:** `cargo make book` exit 0 (~92s); all 3 new pages render plus the NEW `feature/` Part (`book/book/html/feature/index.html` + `feature/electrostatic.{L4,L1,L0}.html`; `book/book/html/L4/frequency_sweep.html`; `book/book/html/L4-L3/frequency-sweep-dissolution.html`); `SUMMARY.md` wires all; `linkcheck2` cache clean — no dead-link; no build-repair needed. The only build noise is the pre-existing benign "Potential incomplete link" WARNs — the `[Inputs]`/`[Scalar]` variant-axis-prose bracket pattern shared across `solve_family`/`fold_solve`/`iterate-while` etc. (`frequency_sweep.md:18` matches the established `solve_family.md:15` pattern — NOT a dead link). No tool-tag leaks.
- **TRANSITIONAL STATE for the directive-3 reorg:** the new cohort bullets are alpha-placed within their kind grouping; the SUMMARY entries + dep-map rows sit in a transitional chronological-tail placement (acceptable per the repair pass until the batch-22 directive-3 by-kind sub-chapter grouping + global alpha re-sort wave lands). The feature Part's within-column level ordering is deliberately high→low (L4→L1→L0), NOT alpha (pending the standing ratification OQ).

### Queued for batch-22 (cycles 071/072 + the batch-22 meta-phase; LEFT OPEN this cycle)

- **(i) feature-surface kind codification** — OQ `feature-surface-kind-adapted-check-codification` (codify the surface-or-evidence ADAPT + rotation-quality/variant-axis NO-OP for the feature-surface composition-root chapter kind).
- **(ii) feature-surface path/layout ratification** — OQ `feature-surface-part-path-layout-and-within-column-level-ordering-ratification` (ratify Part-placement / flat `feature/<feature>.<level>.md` path-layout / high→low within-column level ordering).
- **(iii) directive-3 mdBook reorg wave** — the one-time by-kind sub-chapter grouping + global alpha re-sort (its own structural wave; the SUMMARY/dep-map entries remain in a transitional mixed alpha/chronological state pending it).
- **(iv) FEATURE-SURFACE SPINE role-spec codification** — codify directive-5 into cycle-planner + layer-intro-author/harvester role-specs + CLAUDE.md §"Extraction goal" + the directive-3 kind list (the batch-22 meta-phase).

Written by `integrator-finalize` (split integrator-per-report ×5 + finalize ×1).
