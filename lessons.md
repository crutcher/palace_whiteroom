# Lessons

Cross-cycle observations the Critic finds worth carrying forward. Updated on disagreement *and* on validated non-obvious choices.

Format: one-line entries, dated, terse. The point is rapid scanning during planning, not narrative.

```
YYYY-MM-DD  <one-line pattern note>
```

## Entries

2026-05-24  When a slice's own 'Open questions' section contains a link that is load-bearing for the reduction chain, the L0→L1 rotation is not closed and should be split or deferred rather than asserted.
2026-05-24  When the Synthesizer emits a full slice spec with zero rotation_claim entries, treat the diff as unverifiable and require resubmission with per-assertion claims — narrative spec prose without claim/citation pairs cannot be audited.
2026-05-24  When an L1→L2 rotation maps each L1 line 1:1 to a named BLAS call with identical threaded state, it is a renaming, not a rotation; the real L2 primitive is usually one level coarser (e.g., arnoldi_step) and admits algorithmic substitution (MGS↔CGS2) that the line-by-line form cannot.
2026-05-24  When a slice supports orthogonal axes of variation (e.g., preconditioning side, orthogonalization variant, flexible vs. fixed preconditioner), the L1 procedure must specify behavior under EACH combination or explicitly scope variants out; otherwise the L1→L2 contract has hidden branches that will surface as missing_case at L2.
2026-05-24  When a slice has a 'variant' (FGMRES vs GMRES, restarted vs full, left vs right preconditioning), the L1 form should be stated parametrically so the variant is a binding rather than a bolted-on paragraph — otherwise the rotation hides less state than it appears to.
2026-05-24  Parametric variant absorption at L1 can hide a non-uniformity at L2 when one parameter value requires a post-processing step the others don't (here: right-precond fixed-M needs a terminal M.apply that left-precond and FGMRES don't); the fix is to redefine the L1 abstraction so the primitive sequence is uniform, pushing the variant into a storage/laziness choice rather than a branch in the primitive chain.
2026-05-24  When a slice's L1 parameter space collapses multiple orthogonal axes into one because the codebase only instantiates a diagonal of the product, say so explicitly — otherwise L1 overclaims generality and downstream L2 unfolds will struggle to recover the hidden axes.
2026-05-24  When source is not pre-fetched, the Critic cannot discharge citation_does_not_support / mutation_pattern_mismatch checks and must mark them `unclear` rather than passing; consider making source pre-fetch mandatory for L0→L1 rotations.
2026-05-24  When a slice cites both a single-element and a block/batched variant of the same operation, the L1 form should explicitly state whether the block form is a trivial lift or a distinct primitive — silent omission leaves an unhandled case.
2026-05-24  When an out-of-place wrapper is implemented as 'copy input to output, then apply in place', the L1 mutation pattern is 'out_of_place pure function', not 'alias_with_input' — the latter term should be reserved for genuine output-aliases-input dataflow.
2026-05-24  When a slice absorbs variants via a constructed-operator and the L1 procedure references the variant 'exactly once' at coefficient selection, the Critic should still verify that the variant-conditional setup (here lambda_min / theta / delta) is itself accounted for in the state schema — partial absorption hides easily in the setup step.
2026-05-24  Exercised: rotation-quality (all three criteria explicitly named per claim), variant-absorption (two orthogonal axes pc_side×flexible? and gs_orthog both addressed with constructed-operators for the former and parametric for the latter, with primitive-sequence shape preserved), prose-alignment (L1 names role 'update_least_squares' / 'residual_estimate' with forward-reference to L2 Givens-QR rather than naming the mechanism), setup-schema (FgmresState explicitly enumerates basis_pc as the variant-conditional bundle extension; build_preconditioned_ops centralises setup-time variant binding). Carry-through (Arnoldi invariant) was correctly declared. Trivially carried: mutation-pattern (L1 form is functional-style with explicit returns, sidestepping in-place mutation classification at this layer).
2026-05-24  Variant absorption via 'constructed operator M' is incomplete for flexible-preconditioner variants (FGMRES, flexible-CG): the per-step preconditioned vector z_j is the genuine threaded state, not M itself. Slices that absorb the preconditioner-side axis via M must additionally thread a preconditioned-basis Z to keep cycle-close primitive-sequence-uniform across fixed-vs-flexible variants; otherwise the cycle-close chain silently diverges and variant-absorption check (c) fails.
2026-05-24  When a slice cleanly absorbs a discrete variant (preconditioner side) via a constructed operator at L1, the state-partition section can still leak L2 mechanism terms (e.g., 'Givens scratch') in the operator-internal bullet; prefer role-level naming ('incremental least-squares update scratch') for full prose-alignment.
2026-05-24  Synthesizer: verify Explorer citations independently when prior L1 was authored without per-line confirmation. Cycle's Explorer corrected the `orthog.hpp:55-89 SLEPc wrappers` miscitation — the second wrapper lives in `romoperator.cpp` on the ROM path, not SLEPc. A casual citation range in an earlier draft survived into the spec; only the variant-tracing pass caught it.
2026-05-24  Frictionless-pass sanity (orthog cycle refinement): exercised checks were citation accuracy (Explorer-corrected miscitation from SLEPc to romoperator.cpp was the trigger for this cycle and is now verified in the diff), variant-absorption levels (a/b/c — single primitive with one dispatch site, residual axes disclosed: MPI shape, m==0 micro-difference, normalization), and prose-rotation alignment (L1 prose uses role names mgs_pass/cgs_pass; mechanism names confined to citations). Rotation-quality (state hiding + coarser substitution) and mutation-pattern (H written, w in-place, V read-only) carried through from the prior slice unchanged. Setup-schema check N/A (no constructed operator). Pass is genuine: the refinement narrowed citations and disclosed two real residual axes rather than rubber-stamping.
2026-05-24  Frictionless pass: exercised mutation-pattern check (claim 1, correctly reclassifying out-of-place wrapper from alias_with_input to pure per prior lesson) and citation-support check (claims 2 and 3, both confirmed by explorer-provided line numbers in mixedvecgrad.cpp/integrator.hpp and nleps.cpp); rotation-quality and variant-absorption checks carried through trivially (this is a consolidation/correction cycle, not a layer rotation); prose-alignment and setup-schema checks N/A (no new L_{n+1} primitives introduced).
2026-05-24  Variant-absorption at L1: when a primitive has a runtime algorithm tag with a substitutable contract (same invariant, same return shape), the tag is absorbed by inspecting it exactly once at dispatch. Residual axes (here: MPI collective shape) are disclosed as cost annotations rather than allowed to bleed into the L1 statement.
2026-05-24  Frictionless pass on orthog L0→L1: exercised variant-absorption (all three levels for {MGS,CGS,CGS2} axis with explicit residual MPI-shape disclosure) and prose-alignment (L1 names mgs_pass/cgs_pass as roles, defers sequencing to L2); rotation-quality check exercised via state-hiding (refine boolean and per-variant kernel sequencing hidden); mutation-pattern and setup-schema checks carried through trivially (no setup-time variant bindings, mutation pattern explicitly stated).
2026-05-24  Persisted state at L1 is what survives setup, not what setup names. Chebyshev 1st-kind setup mentions lambda_max/lambda_min but only theta/delta survive the function. Distinguish derived persisted scalars from transient setup intermediates in the L1 state schema.
2026-05-24  When a rotation's spec edit is small (state-schema tightening or alias collapse), verify the diff actually touches the spec file and not only lessons.md / log_synthesis — meta-claims about rotations are not the rotation.
2026-05-24  Variant axes (pc_side, gs_orthog, max_dim, flexible) can all be absorbed at L1 when each is named at exactly one dispatch site: the variants enter `initial_residual`/`apply_BA`/`apply_correction` (pc_side), `orthogonalize` (gs_orthog), the inner-break test (max_dim), and the basis-capture / correction-basis choice (flexible). The main procedure then has a single shape-stable primitive chain.
2026-05-24  The (fixed-vs-flexible-preconditioner) axis is absorbed not by making `M` part of the state, but by promoting the per-step preconditioned vector `z` (which already exists as a transient inside `apply_BA`) to threaded basis state `Z[j]`. The cleanest way to absorb 'this thing varies per step' is often to capture an already-present transient, not to introduce new state.
2026-05-24  When a variant axis is 'forced' by another (here: flexible ⇒ pc_side=RIGHT enforced at construction), record the forcing as a one-line invariant near the OpParams schema so the reader can verify the dispatch-site count without re-deriving the constraint.
2026-05-24  L2 vocabulary extraction: when a slice unfolds into BLAS-1/2-style primitives for the first time, extract each as its own concepts/ entry (apply_linop, axpy, dot, nrm2, scal, givens, trsv). Bottom-up vocabulary lets later slices (CG, eigensolvers, FE assembly) reuse the entries without re-establishing.
2026-05-24  Numerical-claim register pattern: when emitting L2, split numerical properties into 'load-bearing' (rotation algorithm, column-update order, β-as-rotated-RHS-tail) vs. 'transparent' (storage layout, fusion choices). The register is part of the L1→L2 rotation_claim, not commentary.
2026-05-24  When L2 unfoldings include axpy/scal in-place mutations, pseudocode should use explicit copy() primitives rather than '=' assignments between operand names — readers reconstructing the mutation pattern from L2 alone can otherwise miss that the accumulator is being aliased.
2026-05-24  L0→L1 cycles dispatched with slice=`unknown` and no ExplorationFinding have no rotation to perform; the Synthesizer should emit an obstruction claim plus a minimal stub rather than fabricate subject matter. Consider a dispatch-layer guard so these cycles short-circuit before reaching synthesis.
2026-05-24  Cycles dispatched with slice=`unknown` and empty ExplorationFinding produce obstruction-only outputs; a dispatch-layer precondition (non-placeholder slice name + at least one L0 fact or prior slice content) would prevent these wasted cycles from reaching the Synthesizer.
2026-05-24  Variant-absorption levels need not be uniform across layers: a slice can absorb a variant parametrically at L1 (one entry point, one invariant) while its L2 primitive-sequence genuinely diverges across variant values. This is honest, not partial — disclose the divergence and explain why the variants exist precisely because of it (orthog MGS vs. CGS vs. CGS2 have different L2 collective shapes by design).
2026-05-24  When unfolding a loop body into L2 primitives, ask whether the per-iteration updates are independent. Independent updates fuse into a batched primitive (gemv_basis); dependent updates (each iteration reads the previous iteration's mutation) must stay as the unfolded sequence. MGS vs. CGS is the canonical example: same arithmetic, different dependency graph, different L2 form.
2026-05-24  When extracting a new L2 primitive to absorb a fusion (gemv_basis here), pair it with its negative-case statement (when the primitive does NOT apply) in the same concept doc — this is what makes the variant divergence at L2 honest rather than hand-waved.
2026-05-24  Synthesizer: GMRES L1→L2 — `ls_update_column` is the load-bearing unfolding. The L1 role 'incremental LS triangularisation' becomes the L2 sequence `replay stored rotations on new column → generate new rotation from column tail → apply new rotation to column and to RHS`. The O(1)-per-step residual proxy `K.beta = |s[j+1]|` is the entire reason GMRES does not run an LS solve in the inner loop.
2026-05-24  Synthesizer: when an L1 primitive's variants (MGS / CGS / CGS2) share the L2 primitive *set* but differ in *batching*, the variant absorption survives L2 — the L2→L3 edge is where MPI-collective shape distinguishes them. Don't pre-split at L2.
2026-05-24  Critic: prose-alignment check (#10) catches mechanism-leak in L1 form even when the structural rotation is clean — e.g., 'replays previously-recorded rotations' names the L2 Givens mechanism. Flag as borderline-revise rather than reject; the surface should accumulate with the friction noted for next-cycle prose tightening.
2026-05-25  Co-introducing a parent slice (gmres) and an extracted-kernel slice (orthog) in the same cycle is the right move when the kernel has its own variant axes orthogonal to the parent — the parent's L1 can then scope the kernel out cleanly via a forward reference rather than carrying the variant axes itself.
2026-05-25  When a variant axis shifts the *evaluation point* of a quantity rather than its formula (CGS vs MGS w' state, or left- vs right-preconditioned residual norm), L1 absorption via a parenthetical qualifier is acceptable but should be explicitly flagged as 'shape-preserving via state-binding'; the actual primitive-sequence absorption is then the L2 obligation.
2026-05-25  L2 vocabulary for constructed-operator absorption is `ksp_solve(ksp, b)` paired with `apply_linop(op, x)`: the forward and approximate-inverse twins. Per-apply procedures call them uniformly across MG-vs-AMG, CG-vs-GMRES, etc., with all configuration bound at construction time. Extract `ksp_solve` as a first-class concept the first time a slice uses a preconditioned Krylov solve as a per-step primitive (here: divfree's inner H1 Poisson solve).
2026-05-25  Essential-BC enforcement between operator-apply and solver-invoke is a named primitive, `set_subvector_zero(x, idx)`, not a generic vector assignment. Naming it surfaces the BC-enforcement intent at the L2 call site without forcing the reader into the assembly internals; the ordering constraint (after assemble, before solve) becomes an explicit L2 claim rather than a buried convention.
2026-05-25  When a complex-vector apply is a primitive-level unrolling of the real apply over {re, im} with the same primitive chain shape, the L2 form can emit the real path canonically and present the complex path as the unrolled variant — no new primitives are introduced, only overloads + a ComplexOperator wrapper bound at construction. This is the divfree pattern and likely the canonical template for component-blind complex paths.
2026-05-25  The `apply_linop` / `ksp_solve` pairing (forward action vs approximate-inverse action) is emerging as the canonical L2 constructed-operator vocabulary; future slices should reach for this pair before inventing new primitives for solver invocations.
2026-05-25  When a prior cycle emitted L1 and L2 content in the same diff but failed to record the L0→L1 rotation_claim, surface the omission in the next synthesizer cycle as a claim-only emission rather than re-writing the slice content — the structural compression is already on disk; only the audit trail is missing.
2026-05-25  Claim-only emission cycles (where prior cycle shipped L1/L2 content but omitted the rotation_claim record) are legitimate and should be verdicted on the claim's fidelity to the already-on-disk surface, not as new content.
2026-05-25  Polynomial smoothers expose a uniform L2 primitive sequence across variants when both polynomial families admit `(alpha_0, sd_k, sr_k)` parameterization — the variant scalar generator is the only branch point. Constructed-operator absorption holds cleanly through L2.
2026-05-25  Chebyshev L1→L2 is a clean exemplar of variant-absorption axis (c): when variant-conditional state is bundled into a `scalars(op, k)` generator at L1, the L2 primitive chain is identical across variants and only the scalar source branches.
2026-05-25  Verification-only / no-op synthesis cycles (where the slice exists on disk and the Synthesizer emits only rotation_claims) should still cite the on-disk L_{n+1} primitive-sequence when claiming primitive-sequence absorption — otherwise the Critic cannot independently confirm (c)-level absorption from the claim text alone.
2026-05-25  L2→L3 for iterative-solver slices: the field-side primitives (axpy/dot/nrm2/scal/apply_linop) lift uniformly via the support-operator template; the load-bearing L3 spec content is in the operator-composition statements and the recorded sequential obstructions. Extract `tensor-field-lift` as the uniform template so each iterative slice doesn't re-state it.
2026-05-25  Sequential obstructions classify into two regimes by the state being recurred over: field state (heavy; drives algorithmic substitution like Jacobi-vs-Gauss-Seidel) vs. small dense state (benign; the L3 form is the L1 form). GMRES's Givens recurrence is the canonical small-dense-state example — record it, don't try to hide it.
2026-05-25  Obstruction rotation_claims with `justification_kind: obstruction` are first-class: a slice whose L2→L3 produces no obstructions is suspicious in a setting where most numerical algorithms have at least one sequential sub-procedure.
2026-05-25  Sequential obstructions split cleanly by the *state* being recurred over (field vs. small-dense), not just by 'is there a loop' — this dichotomy generalizes beyond GMRES (Gauss-Seidel, sparse triangular solves) and should be the L3 obstruction-classification convention going forward.
2026-05-25  L3→L4 rotation on restarted Krylov solvers: the state-stratification split (Sim / OpParams / ephemeral Krylov) makes variant-absorption a typing invariant rather than a procedural discipline. Once OpParams is readonly and only the constructed-operator helpers close over the variant selectors, the main procedure provably cannot re-inspect them.
2026-05-25  L4 sequential obstructions surface AS plain functional recurrences on small-dense state, NOT as monadic effects. The monad threads SimState (field-level effects); the dense Givens-replay / back-solve are pure on the Krylov bundle. Hiding sequential obstructions inside the monad would be a misrepresentation — the calculus honours the obstruction by not pretending it has a parallel form.
2026-05-25  L3→L4 on restarted Krylov methods: the natural rotation is state-stratification (Sim/OpParams/ephemeral) + state-monad over Sim, with ephemeral bundles as let-bound values inside one cycle. Sequential obstructions (Givens replay, back-solve) appear AS pure functions on the small-dense bundle, not as monadic effects — the calculus honours the obstruction by refusing to pretend it has a parallel form.
2026-05-25  L4 stratification is load-bearing: typing SimState/OpParams/Ephemeral separately encodes lifecycle invariants L1–L3 only state in prose. Co-mingling them in one record at L4 hides what the rotation was supposed to make structural.
2026-05-25  L4 monadic coordination should thread ONLY the sim-state class; operator-params are closed over, ephemeral intermediates are plain function arguments. Wrapping ephemeral state in the monad falsely suggests it persists across the full solve.
2026-05-25  Sequential obstructions identified at L3 should NOT be wrapped in a monadic effect at L4. Type them as plain pure functions on small-dense state — the calculus reflects the obstruction by not hiding it.
2026-05-25  Variant absorption at L4 is realized by typing the constructed-operator closures as fields of OpParams. The main monadic procedure must contain zero references to variant selectors; all dispatch lives inside the closures.
2026-05-25  When an L3→L4 rotation introduces typed stratification, the methodology concept (state-stratification) and the coordination concept (solve-monad) are naturally co-introduced; treating them as separate concept files with explicit dependency edges (solve-monad depends on state-stratification) keeps each focused on one rotation criterion (state-hiding vs threaded-state compression).
2026-05-25  When L_{n+1} content is already drafted inline by a prior cycle, the Synthesizer's job is to retroactively emit the rotation_claims plus the concept extractions the inline draft skipped — not to redraft the prose.
2026-05-25  Retroactive rotation-claim emission for already-inlined content is legitimate when the prior slice drafted L2 prose without separating the rotation justifications — the Synthesizer's role becomes concept-extraction plus claim formalization rather than prose authorship.
2026-05-25  L3→L4 on a solver slice: extract `state-stratification` and `solve-monad` as the canonical L4 vocabulary. The first solver slice to reach L4 should establish these concepts so subsequent solvers (CG, BiCGStab, MINRES, …) reuse rather than re-derive. — gmres cycle, L3→L4.
2026-05-25  When L4 types small-dense recurrence state (rotation registers, bidiagonal coefficients) as part of the ephemeral phase bundle rather than `SimState`, the sequential-obstruction recorded at L3 surfaces naturally as a pure function on that bundle — no monadic effect, no field-level operator. The obstruction is visible by absence rather than presence.
2026-05-25  When L4 stratifies state into persistent/fixed/ephemeral, small-dense sequential obstructions (recurrences with no global tensor-field expression) become visible-by-absence — they appear as pure functions on the ephemeral bundle, with no monadic effect to lift. This is the canonical L4 shape for solver slices and should be reused for CG/BiCGStab/MINRES.
2026-05-25  L4→L4 self-rotation: when a layer's open-questions list flags its own over-articulation ('plumbing X is over-articulated'), that is a push-back signal from the writer to themselves — the next cycle on that slice should consume it and tighten, not defer. The friction surface IS the work-item.
2026-05-25  When a rotation_claim describes a self-rotation that tightens an existing layer, the diff must include the actual surface edit to that layer — a lessons-only diff with rotation_claims describing structural L4 changes is unverifiable and conflates the meta-observation (the lesson) with the work (the tightening).
2026-05-25  Retroactive rotation_claims emission cycles (where the spec content already exists and the diff is a no-op log) are a legitimate pattern for backfilling structural verification, but should explicitly mark themselves as such so the Meta-Critic can distinguish them from cycles that failed to produce a diff.
2026-05-25  L4→L4 tightening: when an Li form reads a configuration tuple more than once, the rotation candidate is a constructed-operator absorption — build a small named value (here `Convergence`) that captures the tuple and exposes one predicate. The main loop then closes over the value, not the parameters.
2026-05-25  Constructed-operator absorption surfaces should enumerate BOTH the OpParams fields AND the SimState fields they uniquely read/write — variant absorption that names only one ownership domain leaves the other unaudited.
2026-05-25  Helmholtz-decomposition projectors rotate to L2 cleanly as a four-primitive chain (apply_linop · set_subvector_zero · ksp_solve · axpy); the triple product Gᵀ M G is never materialized — the rotation hides the never-formed operator rather than a materialized one.
2026-05-25  Complex-vector apply often L2-unrolls as a primitive-level repetition over {re, im} with one component-blind ksp_solve in the middle; this is a recurring shape (projector-style operators that are real-operator block-diagonal on Re/Im with a complex-typed solver in the kernel) worth tracking as a candidate methodology pattern.
2026-05-25  Projector-style operators whose kernel is real-operator block-diagonal on {Re, Im} with a complex-typed solver in the middle recur as an L2 pattern (divfree complex apply; likely to recur in other Helmholtz-decomposition / curl-curl preconditioners) — worth promoting to a named methodology shape if a second instance appears.
2026-05-25  When a rotation_claim invokes 'constructed operator OR closure variable' as alternatives for where variant-private state lives, that disjunction is itself a check-#11 (setup/state schema) failure — the absorption isn't pinned until the state location is named.
2026-05-25  Orthogonalization-strategy choice (CGS / MGS / CGS2 / Householder) is load-bearing in the lessons.md sense — it affects the finite-precision numerical result, not merely the implementation path. It is therefore preserved as an explicit L1 parameter and absorbed parametrically rather than as a transparent fusion.
2026-05-25  Chebyshev iteration is the inner-product-free counterpart to CG: same minimax-polynomial structure, but uses a fixed polynomial from a supplied spectral interval rather than an adaptive Krylov subspace. This makes it the canonical communication-light smoother for multigrid and GPU/distributed settings.
2026-05-25  When a load-bearing variant family contains a member with structurally different state (e.g., Householder's reflector sequence vs. Gram-Schmidt's maintained Q), level-(c) primitive-sequence absorption typically fails for that outlier even when level-(a) invariant absorption succeeds. The discipline is to either declare the residual axis explicitly at L1 (variant-conditional state schema) or split the outlier into a sibling slice; silent absorption of structurally-distinct variants is a known failure mode.
2026-05-25  When a cycle's diff is a log-only block with no file_create / file body, the per-cycle Critic should flag it as `unclear` (no salvageable surface accumulates) rather than evaluating the rotation_claims in isolation — the orchestrator's diff-apply override (meta-review #5) only catches malformed diffs, not intentionally-empty ones.
2026-05-25  Backfill cycles that add rotation_claims against already-on-disk prose (rather than emitting new prose) leave the Critic unable to verify level-(c) primitive-sequence disclosures or L2 cost annotations from the diff alone — the verdict has to trust prior cycles. Consider an Explorer-pre-fetch convention for backfill cycles that surfaces the relevant L_{n+1} prose so the Critic can audit cross-layer consistency.
2026-05-25  Retroactive rotation_claim emission (claims pointing at content already on disk in a prior cycle's L3 section) is a legitimate accumulation mode when the prior cycle landed structural content without the structured claim records; the Critic should verify against the on-disk section, not require a fresh diff.
2026-05-25  Retroactive_claims cycles with a complete retroactive_claim_evidence block (quoted on-disk prose per claim) are straightforwardly auditable; this is the pattern to encourage when a prior cycle's diff landed structure but did not emit rotation_claims at the time.
2026-05-25  L4 rotation pattern: when multiple termination reasons share a return path (GMRES: converged, max_it, max_dim), collapse them into a sum type (Outcome = Continue | Done Bool) and classify once at the loop boundary. Avoids scattered re-tests at the L3→L4 edge.
2026-05-25  L4 constructed-operator surfaces extend beyond linear operators: the convergence policy (rel_tol, abs_tol, initial_residual rescaling) absorbs into a `Convergence = { epsilon, satisfied }` value built once per restart cycle. Same pattern as apply_BA absorbing pc_side — pull variant-conditioned dispatch into a constructed value built at solve start, called uniformly thereafter.
2026-05-25  L3 obstruction → L4 typing: a sequential-obstruction record at L3 should NOT be hidden by a monadic effect at L4. Type the obstruction-bearing operation as a plain pure function on its small-dense state bundle (Krylov → Krylov for ls_update_column); the function signature itself is the calculus statement that this is a scalar recurrence, distinct from field-level tensor primitives.
2026-05-25  L4 rotation patterns recur: state-bundle stratification by lifecycle (persistent/operator-internal/ephemeral), monad over the persistent bundle only, sum-type collapse of multi-reason termination, and constructed-operator absorption of policy (Convergence here, analogous to apply_BA for pc_side). Worth abstracting as a checklist for future L4 slices.
2026-05-25  L2→L3 is clean when the L2 primitive chain has no surviving cross-step sequential dependency — each primitive lifts to a global tensor-field map independently. divfree is the canonical clean case: four global ops, with the only sequential machinery (CG iteration) confined inside the ksp_solve primitive's own L3 obstruction (recorded in the cg slice). The compositional rule: a slice's L3 lift is clean iff every primitive it composes either has a clean L3 lift or names its own internal obstruction.
2026-05-25  Sequential obstructions are slice-local, not global. The divfree slice's L3 form is global tensor-field even though ksp_solve internally drives a sequential CG. The obstruction lives at the primitive's interior and the slice composing it remains clean — provided the slice doesn't expose the per-iteration state.
2026-05-25  Compositional L2→L3 cleanness rule: a slice's L2→L3 lift is clean iff every composed primitive has a clean L3 lift OR names its own interior obstruction without exposing it through the slice's threaded state. The divfree slice formalizes this via ksp_solve: CG's interior sequential obstruction is recorded in the cg slice and does not propagate to divfree because the slice does not thread per-CG-iteration state.
2026-05-25  When a slice has a variant axis where some variants admit a global L3 form and others don't, name the obstruction at L3 as a first-class claim — don't silently restrict L3 to the parallelizable variants. The slice's variant-absorption posture changes shape at L3 (some variants unify, others split off as obstructions); make the shape change explicit.
2026-05-25  L2-to-L3 on already-batched primitives is a notational compression, not a structural rotation: `[m local dots, allreduce_sum, gemv_basis]` is just the realization of `Vᴴ w` followed by `V H`. The rotation is real (the L3 form names the global operator the L2 chain implements), but the justification is reduction_chain rather than novel hiding.
2026-05-25  Sequential-obstruction at L3 is a legitimate justification_kind: when a variant axis splits at the rotation boundary (some variants admit a global form, others have intrinsic sequential dependencies), naming the obstruction is itself the L3 claim for those variants — variant absorption splits rather than unifies.
2026-05-25  Initial-residual computations in iterative solvers sometimes apply norm-of-norm by accident (Palace CG: `sqrt|Norml2(b)|` in the no-preconditioner branch yields `(b·b)^{1/4}`, not `‖b‖₂`). When auditing L0, check the type of each intermediate in a residual/tolerance formula — confusing `Dot(b,b)` (returns squared norm) with `Norml2(b)` (returns norm) compounds a sqrt and silently rescales convergence thresholds.
2026-05-25  Assertion-only side-effects like Palace's `CheckDot` are partial-function guards at L1, not runtime branches — at L4 they map to preconditions on the algebraic form, not to `if` expressions. Model them in L1 so the Critic can verify their presence; drop them at L4 in favor of the precondition statement (SPD, finite arithmetic).
2026-05-25  Back-correction cycles (Explorer audit → Synthesizer fix) are a legitimate cycle shape when prior layers contain algebraic errors discovered by re-reading L0 — verdicting on rotation quality of the *corrected* form (not on the fact that it's a fix) keeps the loop honest.
2026-05-25  Retroactive L0→L1 rotation claims for mutation-erasure are well-served by the four-pattern taxonomy (in-place left-operand mutation, AXPBY destination convention, output-buffer write, loop-carried-variable bundle); future slices that wrap MFEM primitives can reuse this taxonomy verbatim.
2026-05-25  Retroactive_claims cycles that omit the `retroactive_claim_evidence` quoted-prose block put the Critic in an unauditable position; the schema-level fix (require the block) should be enforced at plan-validation time, not only at critic time.
2026-05-25  Retroactive-claims cycles for already-landed slice content are productive when the original synthesis cycle skipped per-edge claim emission. CG slice v0.3 had L0/L1/L2/L3/L4 content on disk but only one summary-claim from the prior cycle; this cycle emits per-substitution claims for the L0→L1 edge — mutation-erasure, workspace-aliasing rename (`p_tmp`), `CheckDot` partial-function guard surfacing, and the faithful-rendering of the `!B && initial_guess` quirk — each citing specific source line ranges. Pattern: when a slice ships with summary-level claims, schedule a retroactive-claims cycle per edge to recover per-primitive granularity (meta-12 item 1).
2026-05-25  Workspace-aliasing renames (Palace's `p` reused as scratch for `(Bb, b)` then later as the search direction) are a recurring L0→L1 rotation kind across Krylov slices — worth tracking as a sub-flavor of state-hiding when GMRES, BiCGSTAB, etc. enter the spec.
2026-05-25  Retroactive-claims cycles that include a retroactive_claim_evidence block with quoted on-disk prose per claim are auditable from the Critic's input alone; this is the pattern meta-11 prescribed and it works in practice for cg.md v0.3.
2026-05-25  Chebyshev L3 is the prototypical partial obstruction: body lifts to a global field expression, both loops (degree-k recurrence and pc_it Richardson sweep) are sequential. The k-recurrence sequentiality is *load-bearing* — the closed-form polynomial expansion is numerically unstable, so the recurrence is not an implementation choice but a stability requirement (Phillips & Fischer 2022 §2). Cite the stability rationale when recording the obstruction, otherwise the obstruction reads as 'we didn't try hard enough'.
2026-05-25  L4 rotations on iterative solvers benefit from a third constructed-operator surface for convergence policy (build_convergence value + Convergence ADT), symmetric to apply_BA's absorption of pc_side — pulls rel_tol/abs_tol/initial_res reads out of the main control flow.
2026-05-25  Collapsing a termination decision table (final_res × ε × max_it × max_dim × converged_flag) into a single Outcome ADT returned by the inner loop is a recurring L3→L4 move: the L3 imperative form fans the decision across multiple sites; the L4 form classifies once at the outer loop and folds via the monad.
2026-05-25  Retroactive-claims cycles that include quoted retroactive_claim_evidence per claim auditable from the Critic's input alone — the meta-11 discipline pays for itself when L4 prose already exists on disk and the rotation reasoning is being captured after the fact.
2026-05-25  Krylov outer loops are the canonical sequential-obstruction case at L2→L3: iteration k depends on k−1 via residual + scalars. The per-step body lifts cleanly to L3 (tensor-algebra composition); the loop itself does not. Same pattern expected for GMRES, LOBPCG, time-stepping.
2026-05-25  Two-part L2→L3 claims (positive step body + negative outer loop) are the canonical shape for Krylov/time-stepping slices; record the obstruction explicitly rather than forcing a unifying global tensor op.
2026-05-25  Retroactive_claims cycles must place quoted on-disk prose in a dedicated `retroactive_claim_evidence` block in log_synthesis (one quote per claim, with §/line anchors), not inline in justification fields — otherwise the Critic cannot audit the cycle from its input alone (check #12).
2026-05-25  Retroactive rotation_claims for an existing on-disk L4 section: emit plan_kind=retroactive_claims, no slice/concept writes, quote on-disk prose per claim in log_synthesis.retroactive_claim_evidence so the Critic can verify each claim against the slice content.
2026-05-25  L4 v0.3 demand-driven pruning (§3.8) discharges the residual-norm logging push-back without adding a Writer effect or phase-config flag — record the rotation as state-hiding (the runtime print-vs-no-print flag is hidden behind ordinary record-field consumption).
2026-05-25  Variant-absorption at L4 via primary/variant pair (cg_step vs pcg_step) with an Identity-equivalence note is a clean way to discharge a runtime if-branch present in the source (`B ? B->Mult : z=r`); the equivalence statement is what makes the modeling inversion (unpreconditioned-as-primary) honest rather than divergent.
2026-05-25  Retroactive-claims cycles with proper retroactive_claim_evidence blocks (path + section + quoted prose per claim) are auditable from Critic input alone — this is the working pattern for back-filling rotation_claims against pre-existing on-disk slices, distinct from the misclassified-retroactive failures meta-13 was written to catch.
2026-05-25  Derived scalars (res = sqrt|beta|, norms, condition estimates) belong in step-output records subject to §3.8 pruning, not in iteration-threaded state — storing them defeats demand-driven pruning by forcing computation on every step regardless of consumer demand.
2026-05-25  L_n→L_n self-rotations are valid cycles when they name a rotation that was implicit in the prior revision — making design rationale auditable matters even when the on-disk form doesn't change shape.
2026-05-25  Derived-view hoisting (v = f(s) for cheap pure f) is a recurring state-hiding rotation pattern at L4 — worth checking on every iteration-schema slice whether scalars in the threaded state are actually derived views of other state fields.
2026-05-25  L4 makes the MGS-vs-CGS distinction notationally cheap: it is the difference between a chain of get/modify actions on a state field and a single snapshot-read + atomic modify. The sequential obstruction stops being an apology and becomes the typical Solve-monad shape; CGS is the special case that happens to commute.
2026-05-25  Solve-monad lifting can invert an L3 'obstruction' framing into an L4 'default semantics' framing: a sequential-dependency apology at the math level becomes the typical case at the calculus level, and the commuting variants become the special case requiring atomic-modify justification.
2026-05-25  L3→L4 on a projector slice: scratch buffers that persist in the L1 state schema are internal-parameter storage at L4, not sim state — the calculus distinction (parameter vs. state vs. ephemeral) is the load-bearing rotation, not the syntax.
2026-05-25  L3→L4 cycles that claim variant absorption via constructed-operators must enumerate which DivFreeParams (or analogue) fields are variant-conditional at construction time — otherwise the absorption is silent partial (cycle-12 chebyshev pattern repeating).
2026-05-25  Audit-rotation rule for slices that arrived with multi-layer content but missing producer-side claims: emit a retroactive_claims plan with `plan_kind=retroactive_claims` and the per-claim on-disk quoted prose, NOT a new_content plan. The slice file already exists; channel-selection rule forbids slice_writes mode=create here.
2026-05-25  Chebyshev L3→L4: a sequential-obstruction inner loop becomes `foldM` over the range with the carried state threaded explicitly; the body stays as pure field-algebra expressions. The scalar recurrence (e.g., 1st-kind `rho_k`) rides inside the foldM accumulator alongside the field state — no separate scalar-state monad needed.
2026-05-25  Variant absorption via constructed-operator at L4: the variant choice collapses into the scalar-generator closure stored on the operator record; the `apply` action has zero `case variant of` branches. This is the cleanest L4 surface for a (c)-level absorbed axis.
2026-05-25  L4 rotation claims emitted without a corresponding slice-file write (only dependency_map_edges + lessons + log_synthesis) are structurally unauditable — the claims point at no on-disk form. Per check #13, this is a plan_kind discipline failure: either the diff should include the L4 section write, or the plan should be retroactive_claims with a retroactive_claim_evidence block quoting existing L4 prose.
2026-05-25  Retroactive_claims with quoted on-disk evidence (per meta-11) make L0→L1 rotation audits tractable even without source pre-fetch — the quoted L1 prose itself is the audit surface for state stratification and variant-absorption claims.
2026-05-25  Derived-view hoisting (state-schema tightening): a scalar that is a pure function of another state field should be exposed as a step-output, not stored as a state field. Storing it forces the step to maintain a redundant invariant on every transition, which defeats §3.8 demand-driven pruning. CG v0.4 demonstrates this for `res = sqrt|beta|`; the same pattern will apply to GMRES's residual estimate and any iterative solver tracking auxiliary norms.
2026-05-25  L4→L4 self-rotations are legitimate when they make an implicit design choice explicit. CG v0.3 had the right schema (no `res` field) but the rotation that produced it was un-named; v0.4 names it without changing the algorithm, making the rationale auditable for downstream slices facing the same choice.
2026-05-25  Derived-view hoisting is a recognizable instance of state hiding (rotation.md): when a state field is a pure function of another state field, hoisting it to step-output is a genuine L4 rotation because the hidden invariant (s.res == sqrt|s.beta|) is load-bearing for downstream pruning. Worth surfacing as a named pattern for future iterative-solver slices.
2026-05-25  L4 state-schema choices are load-bearing for §3.8 demand-driven pruning: a scalar that is a pure function of another state field (e.g., `res = sqrt|beta|`) should be hoisted to a step-output field, not stored as state. Storing it forces per-step computation and defeats pruning. The rotation is `state hiding` per `concepts/rotation.md` — name it explicitly even when the v_prev schema was already correct, so the design rationale is auditable for the next slice facing the same choice (GMRES residual tracking).
2026-05-25  Retroactive-claims cycles that re-attribute within-layer refinements (vN.x→vN.y schema tightening) to a cross-layer edge (L_{n-1}→L_n) erode edge taxonomy; either introduce a `L_n→L_n` self-rotation edge in the schema or require retroactive claims to quote the actual L_{n-1} prose they rotate from.
2026-05-25  State-field elimination via derived-view hoisting is a first-class L4 self-rotation. When a scalar can be recomputed from another state field (here, `res = sqrt|beta|`), promoting it from state to step-output enables §3.8 demand-driven pruning. The choice is load-bearing — a state field forces computation; a step-output allows pruning. Name the rotation explicitly so downstream slices (GMRES, eigensolvers) can reuse the pattern.
2026-05-25  Retroactive-naming cycles (plan_kind=tightening on already-landed content) are legitimate when the structural change was correct but unnamed. The rotation_claim documents the design rationale for future readers; no prose churn required if the on-disk form is already correct.
2026-05-25  Retroactive-naming cycles must satisfy check #12 (retroactive_claim_evidence) even when the plan_kind is framed as 'tightening' rather than the literal string 'retroactive_claims' — the discriminator is 'no new prose + claims against existing surface', not the plan_kind label.
2026-05-25  Derived scalars (res = sqrt|beta|, ‖r‖² caches, etc.) belong in step-output records, not iteration state. Storing them in state forces unconditional computation and defeats L4 §3.8 demand-driven pruning. The rule: if a field f satisfies f == g(other_state_fields) by a pure function g, hoist f to the step's return record as an output-extra.
2026-05-25  L_n → L_n self-rotations are legitimate cycles when they make an implicit design rationale auditable. The cg L4 v0.3 → v0.4 cycle changed no semantics but named the state-hiding rotation that produced the schema; future slices (GMRES residual tracking, LOBPCG eigenvalue traces) inherit the worked example.
2026-05-25  rotation_claims that target a slice's layer section MUST be accompanied either by the corresponding slice_writes/section_appends in the same cycle, or by a retroactive_claim_evidence block quoting the on-disk prose; emitting a methodology concept + dep-edges + lesson is not a substitute for landing (or quoting) the layer prose the claim is about.
2026-05-25  L4→L4 self-rotation pattern: when a slice's L4 schema embeds a derived view as a stored field (res == sqrt|beta|), hoist the field to the step's return record to make L4-calculus §3.8 demand-driven pruning targetable. The state schema becomes consumption-independent — readers cannot tell whether the derived view is being computed downstream, and do not need to know. The CG slice's v0.3→v0.4 rotation is the worked example; the general rule lives at concepts/derived-view-hoisting.md.
2026-05-25  Derived-view-hoisting as a self-rotation (Ln→Ln within the same layer) is a legitimate rotation pattern when the hoist makes a downstream calculus (here §3.8 pruning) targetable; criterion (a) state-hiding applies even when the 'hidden' state is a single scalar field whose presence would pin computation.
2026-05-25  Retroactive L1→L2 emission for orthog cleanly demonstrates the legitimate declared-residual-axis pattern at L2: variant-absorption level (c) deliberately not achieved because primitive-sequence divergence IS the cost-structure motivating the variants' coexistence — disclosing this explicitly avoids the silent-partial-absorption failure mode.
2026-05-25  Verification passes on a multi-layer slice should tighten L0 citations to exact line ranges where ±2 drift would mislead a reader (e.g., a sub-range citing an adjacent helper rather than the named function). Drift within a coherent code block is audit-tolerable; drift that crosses function boundaries is not.
2026-05-25  When the L0 source reuses a single scratch buffer (`r`) for three semantically distinct roles (residual / preconditioner-apply scratch / correction accumulator), the pure-functional L1 form correctly hides the aliasing, but should explicitly note the L0 buffer-reuse so the L2 mutation-pattern reader does not have to re-derive it from source.
2026-05-25  When a cycle's log_synthesis narrates substantive slice edits ('added L0.11a', 'disclosed storage reuse', 'noted scratch reuse') but the visible diff contains only lessons/log appends, the cycle is structurally indistinguishable from a route-around of the retroactive-claims gate — check #12/#15 fires regardless of plan_kind label.
2026-05-25  Citation tightening that crosses a function boundary (cited end past `}` of the named function into the next function or template-instantiation block) is NOT audit-tolerable per the 2026-05-25 rule; intra-function ±1-3 line drift IS tolerable. The GMRES audit caught L0.13 cited 733-875 spilling past `FgmresSolver::Mult` (ends 871) into the template-instantiation block (starts 877) — this is the canonical case where re-anchoring is required, not optional.
2026-05-25  When an L0 entry says 'real and complex specialisations' but cites only one specialisation's line range, the prose claim is structurally unsupported. Either widen the cited range or split into L0.Xa / L0.Xb sub-entries; the latter is preferred when the specialisations have substantively different structure (as with `GeneratePlaneRotation`'s 36-line real path vs. 112-line complex path).
2026-05-25  Within-L0 citation-tightening passes that produce no L1+ prose change but emit a single L0→L1 rotation_claim describing the citation re-anchoring are a legitimate self-rotation pattern: the 'rotation' is the strengthened evidence base, not a new layer.
2026-05-25  When a slice's L_n+1 content has already landed on disk in a prior cycle (here, cycle 21 wrote the gmres L2 section_append), the follow-up retroactive_claims cycle MUST set plan_kind=retroactive_claims, set retroactive_against_cycle, and quote the on-disk prose in log_synthesis.retroactive_claim_evidence per meta-11 item 1 — not re-emit the section.
2026-05-25  GMRES L1→L2 has a mixed rotation profile: ls_update_column is the load-bearing state-hiding rotation (the rotation registers and the replay-generate-apply sequence are the new L2 surface), while initial_residual / apply_BA / orthogonalize / apply_correction are carry-through unfoldings into the support-operator vocabulary. back_solve is the explicit non-unfolding case — the LS-side state does not enter the support-operator namespace at L2, foreshadowing the L3 sequential-obstruction record.
2026-05-25  Retroactive_claims cycles that quote on-disk prose per claim_index are auditable from the Critic's input alone — the structured retroactive_claim_evidence block (claim_index → section → quoted_lines) is the right shape for meta-11/meta-15 evidence and should be the template for future backfills.
2026-05-25  Retroactive rotation_claims for already-landed prose: when L2/L3/L4 content from earlier cycles lacks per-edge claims, a `plan_kind: retroactive_claims` cycle backfills them; the granularity rule (one claim per primitive substitution / state-hiding step) generates N claims for an L_{n+1} section with N L_n building blocks, not one claim for the section as a whole.
2026-05-25  GMRES `ls_update_column` is the L1→L2 rotation's load-bearing case: L1 hides the rotation-register state and the replay/generate/apply recurrence behind the role 'incremental triangularisation'; L2 reveals the three-phase Givens recurrence over named primitives. This is rotation-criterion (a) state-hiding, and the revealed sequential dependency at L2 is what the L3 cycle correctly classified as a sequential obstruction on small-dense state.
2026-05-25  Retroactive rotation_claims at sub-section granularity (one per L1 building block) is the right calibration for L1→L2 GMRES-scale rotations: 5 claims for 5 named building blocks gives reviewers a 1:1 mapping between L1 contract names and L2 primitive sequences, surfacing the load-bearing case (ls_update_column state-hiding) as a distinct claim rather than hiding it inside a bulk section claim.
2026-05-25  Retroactive L2→L3 rotation_claims for the gmres slice document the dual L3 result: positive (field-side primitives + CGS lift to global tensor-field ops) and negative (ls_update_column / back_solve recorded as sequential-obstruction on small-dense state with no DoF index set). Both are first-class L3 output per the L2→L3 edge rotation rules — negative L3 results are not failures, they are pin-down of a structural fact.
2026-05-25  Retroactive L2→L3 claims with quoted on-disk evidence work cleanly when both positive (lift) and negative (obstruction) results are claimed against the same slice — the dual-result pattern is a useful template for slices that contain a mix of field-side and small-dense-state work.
2026-05-25  When a cycle emits a self-rotation section (L4→L4 derived-view hoisting in cg v0.4), the underlying L3→L4 rotation that produced the schema may still lack per-building-block claims on the original edge. The self-rotation cycle is the right place to backfill them — both edges are part of the same conceptual rotation chain, and the self-rotation prose makes the L4 decisions explicit enough to claim against.
2026-05-25  Retroactive-claims cycles that backfill against prior on-disk prose work cleanly when each claim is paired with a quoted-evidence block naming the exact section — claim 1 (forget_z) and claim 2 (iterate_while + print correspondence) each map to a discrete prose locus, which makes the audit mechanical.
2026-05-25  L3→L4 rotation on a slice that already carries L4 prose is a retroactive_claims plan, not new_content. The structural moves (state stratification, monadic coordination, convergence absorption, sequential-obstruction typing) are each their own rotation_claim with quoted on-disk evidence — one summary claim would lose the per-building-block granularity (meta-16 item 1).
2026-05-25  When L4 introduces a Convergence type that absorbs rel_tol/abs_tol/initial_res into a single .satisfied predicate built once per restart cycle, that is a state-hiding rotation symmetric with apply_BA's pc_side absorption — call it out as its own claim, do not bundle into the monadic-coordination claim.
2026-05-25  The `readonly` markers on SimState/OpParams in L4 TypeScript-style schemas are load-bearing: they encode the variant-absorption (b) contract at the type level — OpParams is captured once, never re-read for variant dispatch outside the constructed-operator helpers. Worth naming explicitly in the rotation_claim justification.
2026-05-25  When a retroactive-claims cycle for an L_n→L_{n+1} rotation includes a constructed-type (e.g., Convergence) that absorbs a previously-threaded policy into a single predicate built once per cycle, treat that as its own claim symmetric to other constructed-operator absorptions — do not bundle it into the broader monadic-coordination claim.
2026-05-25  L4→L4 self-rotations (re-justifying an existing schema choice as a named rotation) are best emitted as retroactive_claims plans when the schema landed in an earlier cycle. The rotation_claim's from_form is the rejected hypothetical schema (named in the on-disk v0.N commentary), not a prior on-disk schema; this is legitimate so long as the commentary block exists on disk and quotes the rejected form.
2026-05-25  Self-rotations (L_n→L_n re-justification of an existing schema as a named rotation) are a legitimate retroactive_claims pattern when the rejected hypothetical form is itself quoted in on-disk commentary — the from_form need not be a prior on-disk state, only a named-and-quoted alternative the current form replaces.
2026-05-25  Orthogonalization L2→L3 is a four-claim rotation: (1) CGS lifts to projector form (Vᴴw, VH); (2) CGS2 lifts to two-pass projector form with non-fusing composition; (3) variant absorption tightens at L3 — CGS and CGS2 unify on primitive sequence; (4) MGS is a structural sequential-obstruction. Negative L3 result for the third sibling is first-class output, not a gap.
2026-05-25  When variant absorption tightens across a layer transition (more variants unifying on primitive sequence), record it as a distinct rotation_claim — it is a structural rotation in its own right, not a side-effect of the per-variant lifts. CGS/CGS2 tighten at L3 while MGS obstructs; both are recordable structural claims about the same variant axis.
2026-05-25  When variant absorption tightens across a layer transition (siblings that had divergent primitive sequences at L_n unifying on primitive shape at L_{n+1}), it is a distinct structural rotation_claim — co-equal with the per-variant lifts and the obstruction claim for any non-lifting sibling. The orthog L2→L3 four-claim pattern (per-variant lift × 2, tightening claim, obstruction claim) is the template.
2026-05-25  Chebyshev L3→L4 (cycle 76): the L3 partial-obstruction pattern (body lifts, loop doesn't) maps mechanically to L4 as `foldM step init [1..N]` for the lifted-body case — the fold accumulator threads exactly the L3 carried state. This is the canonical L4 shape for a witnessed sequential obstruction and worth reusing for any Krylov/smoother inner recurrence (CG, GMRES, Lanczos all have this shape).
2026-05-25  Constructed-operator variant absorption gets STRICTLY stronger at L4 than L1/L2: the closure captures the variant-specialized scalar generator at setup and the apply body becomes variant-free. If L1/L2 prose still hedges 'variant-dependent scalars X, Y', the L4 closure form pushes the hedge entirely into setup's case-on-variant and leaves apply uniform.
2026-05-25  L3 partial-obstruction (body lifts, loop doesn't) maps mechanically to L4 foldM with the accumulator typed as the L3 carried tuple — this is the canonical L4 shape for any inner Krylov/smoother recurrence and should be recognized as a reusable pattern across CG/GMRES/Lanczos/Chebyshev.
2026-05-25  Bottom-up vocabulary: the first slice cycle for any solver should establish the support-operator concepts (apply_linop, axpy, dot) before relying on them in the L1 prose — they're the readable substrate that lets the L1 procedure stay compact.
2026-05-25  Constructed-operators is a clean absorption route for variant axes that are stable across a solve (preconditioner side, variable-preconditioner FGMRES). It defers the absorption from per-step branching to one-time operator construction.
2026-05-25  When two slices (cg, gmres) are introduced together at L0→L1, surfacing their shared vocabulary as concepts in the same cycle prevents the second slice from re-establishing primitives the first slice already needed.
2026-05-25  Bottom-up concept emission alongside the first slice that needs them (apply_linop, axpy, dot as primitives; constructed-operators, variant-absorption as methodology) is the right pattern for a fresh subsystem — it lets L1 prose stay compact and prevents the second slice in the cycle from re-establishing vocabulary.
2026-05-25  CG L2→L3 is the canonical 'carry-through step body + negative-result outer loop' pattern for Krylov methods. The substantive content is the sequential-obstruction claim, not a step-body transformation. Future Krylov slices (GMRES, MINRES, LOBPCG) should emit the same shape: one carry-through claim for the inner-kernel primitives, one obstruction claim for the outer iteration.
2026-05-25  Krylov-family slices share a canonical L2→L3 shape: carry-through for the step body (L2 primitives are already L3 atoms — apply_linop, axpy, axpby, dot) plus a negative L3 result on the outer iteration (sequential-obstruction, survives to L4 as iterate_while). GMRES, MINRES, LOBPCG L2→L3 cycles should reuse this two-claim template.
2026-05-25  L3→L4 rotation_claims emission lag: cycles 21-onward landed L4 prose for CG (v0.2 → v0.3 → v0.4) but the L3→L4 per-claim record was implicit in the prose, not enumerated as per-edge rotation_claims. Backfilled here as a retroactive_claims plan against the on-disk L4 section. Lesson: when a slice's L4 lands, the same cycle should emit per-claim L3→L4 records — not deferred.
2026-05-25  Retroactive L3→L4 claim backfill against existing on-disk prose is the correct remediation pattern when prior cycles landed Ln+1 content without enumerated rotation_claims — provided retroactive_claim_evidence quotes the supporting prose per claim (meta-11/15 discipline satisfied here).
2026-05-25  Derived-view hoisting: any candidate state field `f` satisfying `f == g(other_fields)` for pure `g` should be hoisted out of the iteration-threaded state and exposed as a step-output field, so L4 §3.8 demand-driven pruning handles compute-or-skip uniformly. Storing `f` in state forces unconditional computation and defeats pruning. Pattern surfaced from CG v0.4 (res=sqrt|beta|); applies to GMRES residual tracking, LOBPCG eigenvalue traces, time-stepping diagnostics.
2026-05-25  L4 self-rotations (L_n→L_n) are first-class. When a schema choice is load-bearing for a calculus property (here: §3.8 pruning), the rotation that produced the schema deserves to be named even if the prose was implicitly correct — auditability beats implicit-correctness for downstream reuse.
2026-05-25  L_n→L_n self-rotations are legitimate when a prior schema choice was load-bearing for a calculus property but undocumented as a rotation; the self-rotation names the criterion (here: state hiding for §3.8 pruning) and lifts the pattern to a reusable concept page. Critic should accept self-rotations when (a) the rotation criterion is named, (b) the load-bearing property is identified, and (c) the pattern is hoisted to a concept for reuse.
2026-05-25  Per-building-block rotation_claim granularity (meta-16 item 1): when an L_{n+1} section introduces N named building blocks (here: 6 — initial_residual, apply_BA, orthogonalize, ls_update_column, back_solve, apply_correction), the corresponding cycle MUST emit ~N rotation_claims, not one summary claim. The cycle-21 GMRES L2 section landed with insufficient per-block claim coverage; this cycle backfills.
2026-05-25  back_solve is the canonical 'carry-through' building block in the GMRES L1→L2 unfolding: its L1 role (back-substitution on small dense triangular state) already names a scalar recurrence with no field-side primitives, so L2 is shape-identical. Recording it as carry-through (rather than omitting it) makes the per-block coverage complete and signals to readers that the absence of primitive-substitution there is intentional, not a defect.
2026-05-25  Retroactive per-building-block claim backfills are a clean pattern when paired with retroactive_claim_evidence quoting on-disk prose for each block — including carry-through blocks (back_solve) makes the per-block coverage complete and signals intentional shape-stability rather than omission.
2026-05-25  GMRES L2→L3 fits the canonical state-stratification template: field-side primitives lift via the support-operator template (tensor-field-lift), LS-side dense recurrences (ls_update_column, back_solve) are first-class sequential obstructions on small-dense state. This split is exactly what state stratification at L1/L2 already prepared — the L3 rotation is mostly bookkeeping once the strata are clean.
2026-05-25  Per-variant lift fidelity is a useful diagnostic at L2→L3: CGS/CGS2 admit a single-shot global form (batched tall-skinny gemv), MGS does not (sequential per-k dot+axpy). When variant axes already dispatch at L1, the per-variant L3 behaviour can be reported per dispatch branch without re-litigating the variant absorption.
2026-05-25  L2→L3 retroactive backfill at per-building-block granularity (6 claims for 6 L2 building blocks, each with quoted L3 prose) is the canonical shape for retroactive_claims plans — addresses the meta-16 granularity directive without re-emitting prose.
2026-05-25  L3→L4 rotation on a multi-variant Krylov slice (GMRES/FGMRES) decomposes into ≥6 distinct rotation claims, one per L4 surface: (1) state stratification into three typed bundles, (2) monadic threading of the persistent bundle, (3) constructed-operator typing of variant-absorbed helpers, (4) convergence-test as a constructed-operator surface, (5) Outcome ADT collapsing scattered termination-path checks, (6) explicit obstruction-typing of small-dense recurrences. Per-building-block granularity (meta-16 item 1) applies at L4 just as at L2: each named L4 surface deserves its own claim, not a single 'L4 done' summary.
2026-05-25  The convergence-test factory is a third variant-absorbed surface alongside apply_BA (pc_side) and apply_correction (flexible). When L3 prose has the convergence test inline with raw rel_tol/abs_tol/initial_res reads, the L4 rotation hoists those into a Convergence value with `satisfied :: real -> Bool` — symmetric to how the other two surfaces close over OpParams. Worth flagging proactively at L1/L2 in future Krylov slices (CG, MINRES, BiCGSTAB) so the convergence-policy abstraction is established before L4.
2026-05-25  Sequential-obstruction typing at L4 has a distinctive structural signature: pure function on the relevant ephemeral bundle, NO OpParams (variants don't apply), NO monadic effect (no field-state interaction), NO tensor-field operator. The absence of these is the structural mark of the obstruction — a reader scanning L4 sees these functions as type-distinct from the field-side helpers. ls_update_column and back_solve in GMRES, triangular_solve in any preconditioner with a Cholesky/LU factor, would share this shape.
2026-05-25  L3→L4 rotations on multi-surface Krylov slices benefit from per-L4-surface rotation_claim granularity (one per typed bundle / monad layer / constructed-operator surface / ADT introduction / obstruction-typing site) — symmetric to meta-16's per-building-block discipline at L2.
2026-05-25  2026-05-26: support-operator slices (orthog, divfree, chebyshev) often have clean L1 forms because their variant axes are either operator-internal (set at construction) or absorbed via the constructed-operator route (e.g., B-weighted inner product, polymorphic ksp_solve, polymorphic D⁻¹). Surfacing those absorption choices in a 'Variant axes and absorption status' subsection at L1 makes the residual-axis question explicit and machine-checkable.
2026-05-25  2026-05-26: orthogonalization scheme (CGS/CGS2/MGS) is the canonical example of a level-(c)-breaking variant — invariant and procedure unify, but per-column primitive sequence differs (block-dot+block-axpy vs. interleaved). Document as residual L1 axis to be resolved at L2 with three primitive-chain variants, not papered over.
2026-05-25  Multi-slice cycles for support-operator primitives (orthog/divfree/chebyshev) work well when each slice independently declares its variant-axis absorption status; the residual-axis disclosure for orthog (CGS/CGS2/MGS) is the canonical level-(c)-break example and should be cited from future variant-absorption discussions.
2026-05-25  Per-building-block rotation_claim backfills (meta-16 item 1): when a layer section landed in an earlier cycle with one summary claim, retroactive_claims cycles can grant per-block granularity. The CG L2 section had three identifiable structural moves (state-bundle compression, load-bearing branch classification, axpy rename canonicalization); backfilling one claim per move gives the Critic-aligned granularity without re-emitting the L2 prose.
2026-05-25  GMRES L1→L2 rotation_claims emitted retroactively against cycle-21 on-disk content. Six per-building-block claims (initial_residual, apply_BA, orthogonalize, ls_update_column, back_solve, apply_correction) match the six named primitives in the L2 'L1 → L2 unfoldings' subsection, satisfying per-building-block granularity (meta-16 item 1). Claim granularity validated against L2 prose's explicit sub-section structure.
2026-05-25  Per-building-block retroactive_claims with explicit retroactive_claim_evidence quoting each L2 sub-section by name is the cleanest backfill pattern observed — claim_index aligns 1:1 with L2 sub-sections, making verification mechanical.
2026-05-25  L2→L3 rotation claims must distinguish field-side state (lifts to tensor-field forms) from small-dense state (records as sequential-obstruction). GMRES exhibits both in the same slice: the field-side basis ops lift to tall-skinny gemv/reduction; the rotation registers and back-solve are small-dense recurrences that do NOT lift. The L3 form correctly carries one global form per field-side primitive AND one obstruction per small-dense recurrence — five claims for one rotation, not one summary claim.
2026-05-25  When an orthogonalization variant axis spans both global forms (CGS, CGS2) and a sequential form (MGS), the L2→L3 claim records the global form for the lifting variants AND notes the internal obstruction for the non-lifting variant, with the obstruction routed to the relevant downstream slice. The L1 dispatch contract is preserved — variant absorption survives the rotation.
2026-05-25  L2→L3 rotation claims for slices with mixed field-side and small-dense state should emit per-building-block claims partitioned into lifts (one per field-side primitive) and obstructions (one per small-dense recurrence); one summary claim hides the small-dense-state subcase.
2026-05-25  Retroactive rotation_claims with full per-building-block granularity (5 claims for 5 distinct L2 surfaces: MGS, CGS, CGS2, allreduce promotion, variant-dispatch) plus a structured retroactive_claim_evidence block is the model pattern — each claim is structurally auditable from the Critic's input alone without re-reading the slice.
2026-05-25  Support-operator family (axpy/dot/nrm2/scal/apply_linop) lifts L2→L3 uniformly via tensor-field-lift; the rotation is structural identity-on-semantics that hides the per-DoF loop. CGS-shaped orthogonalize collapses the per-k loop into one batched Vᴴ·w because CGS's dots are mutually independent; MGS does not admit this lift and is an internal obstruction (routed to the orthog slice). Small-dense LS state (H, s, cs, sn) is not field state and ls_update_column / back_solve are recorded as sequential-obstruction (small-dense subcase); the L1 form IS the L3 form for those primitives.
2026-05-25  Retroactive per-building-block claims for L2→L3 lifts naturally cluster into three kinds: (i) support-operator identity lift, (ii) per-k loop → batched tall-skinny reduction (CGS-shape), (iii) small-dense-state sequential-obstruction record — this taxonomy may generalise across Krylov slices.
2026-05-25  L3→L4 negative-L3 outer-loops cleanly discharge to `iterate_while`. A genuine sequentiality obstruction at L3 (Krylov-style iteration-k-depends-on-iteration-k-1) is not a defect — it is the precondition for the L4 calculus' iteration combinator to do useful work. The rotation criterion (a) state hiding fires on the loop counter / convergence flag / scalar accumulators all folding into the threaded record. Expect this pattern to repeat at GMRES, LOBPCG, time-stepping.
2026-05-25  L3→L4 variant absorption via equivalence projection. When L4 modeling inverts the polarity of an L0-L3 variant (Palace's `B=Identity` degenerate case → L4's preconditioned-as-variant-of-unpreconditioned), the equivalence is witnessed by a state-projection function (`forget_z`) and a behavioral identity (`pcg_step opA Identity ≡ cg_step opA ∘ forget_z`). The projection is the audit trail; without it the inversion is just an unsupported modeling preference.
2026-05-25  Negative-L3 results (genuine sequentiality obstructions) are productive inputs to L3→L4 rotation: they identify exactly which threaded-state record `iterate_while` needs to carry, and the L4 calculus' iteration combinator turns the obstruction into a surface feature rather than papering over it.
2026-05-25  Retroactive L3→L4 rotation_claims for an obstruction-shaped layer (CG's sequential outer loop) work best as a small family: (i) obstruction carry-through (loop becomes `iterate_while`), (ii) inner-step mechanical correspondence, (iii) any derived-view hoistings, (iv) any variant-absorption modeling choices that L4 introduces (here: unprec-as-primary), (v) any partial-function-guard liftings to preconditions. Splitting the rotation along these orthogonal axes gives per-building-block granularity (5 claims for 5 distinct L4 surfaces) without inventing claims.
2026-05-25  Retroactive L3→L4 rotation_claims for an obstruction-shaped slice decompose cleanly into ~5 orthogonal axes (obstruction carry-through, inner-step mechanical correspondence, derived-view hoisting, variant-absorption modeling, partial-guard→precondition lift); this is a reusable template for retroactive per-building-block granularity on layers where the outer obstruction forces a thin L4 surface.
