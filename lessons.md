# Lessons

Cross-cycle observations the Critic finds worth carrying forward. Updated on disagreement *and* on validated non-obvious choices.

Format: one-line entries, dated, terse. The point is rapid scanning during planning, not narrative.

```
YYYY-MM-DD  <one-line pattern note>
```

## Entries

2026-05-24  When a slice's own 'Open questions' section contains a link that is load-bearing for the reduction chain, the L0→L1 rotation is not closed and should be split or deferred rather than asserted.
2026-05-24  When the Synthesizer emits a full slice spec with zero rotation_claim entries, treat the diff as unverifiable and require resubmission with per-assertion claims — narrative spec prose without claim/citation pairs cannot be audited.
2026-05-24  When an L1→L2 rotation maps each L1 line 1:1 to a named BLAS call with identical threaded state, it is a renaming, not a rotation; the real L2 primitive is usually one level coarser (e.g., arnoldi_step) and admits algorithmic substitution (MGS↔CGS2) that the line-by-line form cannot.
2026-05-24  When a slice supports orthogonal axes of variation (e.g., preconditioning side, orthogonalization variant, flexible vs. fixed preconditioner), the L1 procedure must specify behavior under EACH combination or explicitly scope variants out; otherwise the L1→L2 contract has hidden branches that will surface as missing_case at L2.
2026-05-24  When a slice has a 'variant' (FGMRES vs GMRES, restarted vs full, left vs right preconditioning), the L1 form should be stated parametrically so the variant is a binding rather than a bolted-on paragraph — otherwise the rotation hides less state than it appears to.
2026-05-24  Parametric variant absorption at L1 can hide a non-uniformity at L2 when one parameter value requires a post-processing step the others don't (here: right-precond fixed-M needs a terminal M.apply that left-precond and FGMRES don't); the fix is to redefine the L1 abstraction so the primitive sequence is uniform, pushing the variant into a storage/laziness choice rather than a branch in the primitive chain.
2026-05-24  When a slice's L1 parameter space collapses multiple orthogonal axes into one because the codebase only instantiates a diagonal of the product, say so explicitly — otherwise L1 overclaims generality and downstream L2 unfolds will struggle to recover the hidden axes.
2026-05-24  When source is not pre-fetched, the Critic cannot discharge citation_does_not_support / mutation_pattern_mismatch checks and must mark them `unclear` rather than passing; consider making source pre-fetch mandatory for L0→L1 rotations.
2026-05-24  When a slice cites both a single-element and a block/batched variant of the same operation, the L1 form should explicitly state whether the block form is a trivial lift or a distinct primitive — silent omission leaves an unhandled case.
2026-05-24  When an out-of-place wrapper is implemented as 'copy input to output, then apply in place', the L1 mutation pattern is 'out_of_place pure function', not 'alias_with_input' — the latter term should be reserved for genuine output-aliases-input dataflow.
2026-05-24  When a slice absorbs variants via a constructed-operator and the L1 procedure references the variant 'exactly once' at coefficient selection, the Critic should still verify that the variant-conditional setup (here lambda_min / theta / delta) is itself accounted for in the state schema — partial absorption hides easily in the setup step.
2026-05-24  Exercised: rotation-quality (all three criteria explicitly named per claim), variant-absorption (two orthogonal axes pc_side×flexible? and gs_orthog both addressed with constructed-operators for the former and parametric for the latter, with primitive-sequence shape preserved), prose-alignment (L1 names role 'update_least_squares' / 'residual_estimate' with forward-reference to L2 Givens-QR rather than naming the mechanism), setup-schema (FgmresState explicitly enumerates basis_pc as the variant-conditional bundle extension; build_preconditioned_ops centralises setup-time variant binding). Carry-through (Arnoldi invariant) was correctly declared. Trivially carried: mutation-pattern (L1 form is functional-style with explicit returns, sidestepping in-place mutation classification at this layer).
2026-05-24  Variant absorption via 'constructed operator M' is incomplete for flexible-preconditioner variants (FGMRES, flexible-CG): the per-step preconditioned vector z_j is the genuine threaded state, not M itself. Slices that absorb the preconditioner-side axis via M must additionally thread a preconditioned-basis Z to keep cycle-close primitive-sequence-uniform across fixed-vs-flexible variants; otherwise the cycle-close chain silently diverges and variant-absorption check (c) fails.
2026-05-24  When a slice cleanly absorbs a discrete variant (preconditioner side) via a constructed operator at L1, the state-partition section can still leak L2 mechanism terms (e.g., 'Givens scratch') in the operator-internal bullet; prefer role-level naming ('incremental least-squares update scratch') for full prose-alignment.
2026-05-24  Synthesizer: verify Explorer citations independently when prior L1 was authored without per-line confirmation. Cycle's Explorer corrected the `orthog.hpp:55-89 SLEPc wrappers` miscitation — the second wrapper lives in `romoperator.cpp` on the ROM path, not SLEPc. A casual citation range in an earlier draft survived into the spec; only the variant-tracing pass caught it.
2026-05-24  Frictionless-pass sanity (orthog cycle refinement): exercised checks were citation accuracy (Explorer-corrected miscitation from SLEPc to romoperator.cpp was the trigger for this cycle and is now verified in the diff), variant-absorption levels (a/b/c — single primitive with one dispatch site, residual axes disclosed: MPI shape, m==0 micro-difference, normalization), and prose-rotation alignment (L1 prose uses role names mgs_pass/cgs_pass; mechanism names confined to citations). Rotation-quality (state hiding + coarser substitution) and mutation-pattern (H written, w in-place, V read-only) carried through from the prior slice unchanged. Setup-schema check N/A (no constructed operator). Pass is genuine: the refinement narrowed citations and disclosed two real residual axes rather than rubber-stamping.
2026-05-24  Frictionless pass: exercised mutation-pattern check (claim 1, correctly reclassifying out-of-place wrapper from alias_with_input to pure per prior lesson) and citation-support check (claims 2 and 3, both confirmed by explorer-provided line numbers in mixedvecgrad.cpp/integrator.hpp and nleps.cpp); rotation-quality and variant-absorption checks carried through trivially (this is a consolidation/correction cycle, not a layer rotation); prose-alignment and setup-schema checks N/A (no new L_{n+1} primitives introduced).
2026-05-24  Variant-absorption at L1: when a primitive has a runtime algorithm tag with a substitutable contract (same invariant, same return shape), the tag is absorbed by inspecting it exactly once at dispatch. Residual axes (here: MPI collective shape) are disclosed as cost annotations rather than allowed to bleed into the L1 statement.
2026-05-24  Frictionless pass on orthog L0→L1: exercised variant-absorption (all three levels for {MGS,CGS,CGS2} axis with explicit residual MPI-shape disclosure) and prose-alignment (L1 names mgs_pass/cgs_pass as roles, defers sequencing to L2); rotation-quality check exercised via state-hiding (refine boolean and per-variant kernel sequencing hidden); mutation-pattern and setup-schema checks carried through trivially (no setup-time variant bindings, mutation pattern explicitly stated).
2026-05-24  Persisted state at L1 is what survives setup, not what setup names. Chebyshev 1st-kind setup mentions lambda_max/lambda_min but only theta/delta survive the function. Distinguish derived persisted scalars from transient setup intermediates in the L1 state schema.
2026-05-24  When a rotation's spec edit is small (state-schema tightening or alias collapse), verify the diff actually touches the spec file and not only lessons.md / log_synthesis — meta-claims about rotations are not the rotation.
2026-05-24  Variant axes (pc_side, gs_orthog, max_dim, flexible) can all be absorbed at L1 when each is named at exactly one dispatch site: the variants enter `initial_residual`/`apply_BA`/`apply_correction` (pc_side), `orthogonalize` (gs_orthog), the inner-break test (max_dim), and the basis-capture / correction-basis choice (flexible). The main procedure then has a single shape-stable primitive chain.
2026-05-24  The (fixed-vs-flexible-preconditioner) axis is absorbed not by making `M` part of the state, but by promoting the per-step preconditioned vector `z` (which already exists as a transient inside `apply_BA`) to threaded basis state `Z[j]`. The cleanest way to absorb 'this thing varies per step' is often to capture an already-present transient, not to introduce new state.
2026-05-24  When a variant axis is 'forced' by another (here: flexible ⇒ pc_side=RIGHT enforced at construction), record the forcing as a one-line invariant near the OpParams schema so the reader can verify the dispatch-site count without re-deriving the constraint.
2026-05-24  L2 vocabulary extraction: when a slice unfolds into BLAS-1/2-style primitives for the first time, extract each as its own concepts/ entry (apply_linop, axpy, dot, nrm2, scal, givens, trsv). Bottom-up vocabulary lets later slices (CG, eigensolvers, FE assembly) reuse the entries without re-establishing.
2026-05-24  Numerical-claim register pattern: when emitting L2, split numerical properties into 'load-bearing' (rotation algorithm, column-update order, β-as-rotated-RHS-tail) vs. 'transparent' (storage layout, fusion choices). The register is part of the L1→L2 rotation_claim, not commentary.
2026-05-24  When L2 unfoldings include axpy/scal in-place mutations, pseudocode should use explicit copy() primitives rather than '=' assignments between operand names — readers reconstructing the mutation pattern from L2 alone can otherwise miss that the accumulator is being aliased.
2026-05-24  L0→L1 cycles dispatched with slice=`unknown` and no ExplorationFinding have no rotation to perform; the Synthesizer should emit an obstruction claim plus a minimal stub rather than fabricate subject matter. Consider a dispatch-layer guard so these cycles short-circuit before reaching synthesis.
2026-05-24  Cycles dispatched with slice=`unknown` and empty ExplorationFinding produce obstruction-only outputs; a dispatch-layer precondition (non-placeholder slice name + at least one L0 fact or prior slice content) would prevent these wasted cycles from reaching the Synthesizer.
2026-05-24  Variant-absorption levels need not be uniform across layers: a slice can absorb a variant parametrically at L1 (one entry point, one invariant) while its L2 primitive-sequence genuinely diverges across variant values. This is honest, not partial — disclose the divergence and explain why the variants exist precisely because of it (orthog MGS vs. CGS vs. CGS2 have different L2 collective shapes by design).
2026-05-24  When unfolding a loop body into L2 primitives, ask whether the per-iteration updates are independent. Independent updates fuse into a batched primitive (gemv_basis); dependent updates (each iteration reads the previous iteration's mutation) must stay as the unfolded sequence. MGS vs. CGS is the canonical example: same arithmetic, different dependency graph, different L2 form.
2026-05-24  When extracting a new L2 primitive to absorb a fusion (gemv_basis here), pair it with its negative-case statement (when the primitive does NOT apply) in the same concept doc — this is what makes the variant divergence at L2 honest rather than hand-waved.
2026-05-24  Synthesizer: GMRES L1→L2 — `ls_update_column` is the load-bearing unfolding. The L1 role 'incremental LS triangularisation' becomes the L2 sequence `replay stored rotations on new column → generate new rotation from column tail → apply new rotation to column and to RHS`. The O(1)-per-step residual proxy `K.beta = |s[j+1]|` is the entire reason GMRES does not run an LS solve in the inner loop.
2026-05-24  Synthesizer: when an L1 primitive's variants (MGS / CGS / CGS2) share the L2 primitive *set* but differ in *batching*, the variant absorption survives L2 — the L2→L3 edge is where MPI-collective shape distinguishes them. Don't pre-split at L2.
2026-05-24  Critic: prose-alignment check (#10) catches mechanism-leak in L1 form even when the structural rotation is clean — e.g., 'replays previously-recorded rotations' names the L2 Givens mechanism. Flag as borderline-revise rather than reject; the surface should accumulate with the friction noted for next-cycle prose tightening.
2026-05-25  Co-introducing a parent slice (gmres) and an extracted-kernel slice (orthog) in the same cycle is the right move when the kernel has its own variant axes orthogonal to the parent — the parent's L1 can then scope the kernel out cleanly via a forward reference rather than carrying the variant axes itself.
2026-05-25  When a variant axis shifts the *evaluation point* of a quantity rather than its formula (CGS vs MGS w' state, or left- vs right-preconditioned residual norm), L1 absorption via a parenthetical qualifier is acceptable but should be explicitly flagged as 'shape-preserving via state-binding'; the actual primitive-sequence absorption is then the L2 obligation.
2026-05-25  L2 vocabulary for constructed-operator absorption is `ksp_solve(ksp, b)` paired with `apply_linop(op, x)`: the forward and approximate-inverse twins. Per-apply procedures call them uniformly across MG-vs-AMG, CG-vs-GMRES, etc., with all configuration bound at construction time. Extract `ksp_solve` as a first-class concept the first time a slice uses a preconditioned Krylov solve as a per-step primitive (here: divfree's inner H1 Poisson solve).
2026-05-25  Essential-BC enforcement between operator-apply and solver-invoke is a named primitive, `set_subvector_zero(x, idx)`, not a generic vector assignment. Naming it surfaces the BC-enforcement intent at the L2 call site without forcing the reader into the assembly internals; the ordering constraint (after assemble, before solve) becomes an explicit L2 claim rather than a buried convention.
2026-05-25  When a complex-vector apply is a primitive-level unrolling of the real apply over {re, im} with the same primitive chain shape, the L2 form can emit the real path canonically and present the complex path as the unrolled variant — no new primitives are introduced, only overloads + a ComplexOperator wrapper bound at construction. This is the divfree pattern and likely the canonical template for component-blind complex paths.
2026-05-25  The `apply_linop` / `ksp_solve` pairing (forward action vs approximate-inverse action) is emerging as the canonical L2 constructed-operator vocabulary; future slices should reach for this pair before inventing new primitives for solver invocations.
2026-05-25  When a prior cycle emitted L1 and L2 content in the same diff but failed to record the L0→L1 rotation_claim, surface the omission in the next synthesizer cycle as a claim-only emission rather than re-writing the slice content — the structural compression is already on disk; only the audit trail is missing.
2026-05-25  Claim-only emission cycles (where prior cycle shipped L1/L2 content but omitted the rotation_claim record) are legitimate and should be verdicted on the claim's fidelity to the already-on-disk surface, not as new content.
2026-05-25  Polynomial smoothers expose a uniform L2 primitive sequence across variants when both polynomial families admit `(alpha_0, sd_k, sr_k)` parameterization — the variant scalar generator is the only branch point. Constructed-operator absorption holds cleanly through L2.
2026-05-25  Chebyshev L1→L2 is a clean exemplar of variant-absorption axis (c): when variant-conditional state is bundled into a `scalars(op, k)` generator at L1, the L2 primitive chain is identical across variants and only the scalar source branches.
2026-05-25  Verification-only / no-op synthesis cycles (where the slice exists on disk and the Synthesizer emits only rotation_claims) should still cite the on-disk L_{n+1} primitive-sequence when claiming primitive-sequence absorption — otherwise the Critic cannot independently confirm (c)-level absorption from the claim text alone.
2026-05-25  L2→L3 for iterative-solver slices: the field-side primitives (axpy/dot/nrm2/scal/apply_linop) lift uniformly via the support-operator template; the load-bearing L3 spec content is in the operator-composition statements and the recorded sequential obstructions. Extract `tensor-field-lift` as the uniform template so each iterative slice doesn't re-state it.
2026-05-25  Sequential obstructions classify into two regimes by the state being recurred over: field state (heavy; drives algorithmic substitution like Jacobi-vs-Gauss-Seidel) vs. small dense state (benign; the L3 form is the L1 form). GMRES's Givens recurrence is the canonical small-dense-state example — record it, don't try to hide it.
2026-05-25  Obstruction rotation_claims with `justification_kind: obstruction` are first-class: a slice whose L2→L3 produces no obstructions is suspicious in a setting where most numerical algorithms have at least one sequential sub-procedure.
2026-05-25  Sequential obstructions split cleanly by the *state* being recurred over (field vs. small-dense), not just by 'is there a loop' — this dichotomy generalizes beyond GMRES (Gauss-Seidel, sparse triangular solves) and should be the L3 obstruction-classification convention going forward.
2026-05-25  L3→L4 rotation on restarted Krylov solvers: the state-stratification split (Sim / OpParams / ephemeral Krylov) makes variant-absorption a typing invariant rather than a procedural discipline. Once OpParams is readonly and only the constructed-operator helpers close over the variant selectors, the main procedure provably cannot re-inspect them.
2026-05-25  L4 sequential obstructions surface AS plain functional recurrences on small-dense state, NOT as monadic effects. The monad threads SimState (field-level effects); the dense Givens-replay / back-solve are pure on the Krylov bundle. Hiding sequential obstructions inside the monad would be a misrepresentation — the calculus honours the obstruction by not pretending it has a parallel form.
2026-05-25  L3→L4 on restarted Krylov methods: the natural rotation is state-stratification (Sim/OpParams/ephemeral) + state-monad over Sim, with ephemeral bundles as let-bound values inside one cycle. Sequential obstructions (Givens replay, back-solve) appear AS pure functions on the small-dense bundle, not as monadic effects — the calculus honours the obstruction by refusing to pretend it has a parallel form.
2026-05-25  L4 stratification is load-bearing: typing SimState/OpParams/Ephemeral separately encodes lifecycle invariants L1–L3 only state in prose. Co-mingling them in one record at L4 hides what the rotation was supposed to make structural.
2026-05-25  L4 monadic coordination should thread ONLY the sim-state class; operator-params are closed over, ephemeral intermediates are plain function arguments. Wrapping ephemeral state in the monad falsely suggests it persists across the full solve.
2026-05-25  Sequential obstructions identified at L3 should NOT be wrapped in a monadic effect at L4. Type them as plain pure functions on small-dense state — the calculus reflects the obstruction by not hiding it.
2026-05-25  Variant absorption at L4 is realized by typing the constructed-operator closures as fields of OpParams. The main monadic procedure must contain zero references to variant selectors; all dispatch lives inside the closures.
2026-05-25  When an L3→L4 rotation introduces typed stratification, the methodology concept (state-stratification) and the coordination concept (solve-monad) are naturally co-introduced; treating them as separate concept files with explicit dependency edges (solve-monad depends on state-stratification) keeps each focused on one rotation criterion (state-hiding vs threaded-state compression).
2026-05-25  When L_{n+1} content is already drafted inline by a prior cycle, the Synthesizer's job is to retroactively emit the rotation_claims plus the concept extractions the inline draft skipped — not to redraft the prose.
2026-05-25  Retroactive rotation-claim emission for already-inlined content is legitimate when the prior slice drafted L2 prose without separating the rotation justifications — the Synthesizer's role becomes concept-extraction plus claim formalization rather than prose authorship.
2026-05-25  L3→L4 on a solver slice: extract `state-stratification` and `solve-monad` as the canonical L4 vocabulary. The first solver slice to reach L4 should establish these concepts so subsequent solvers (CG, BiCGStab, MINRES, …) reuse rather than re-derive. — gmres cycle, L3→L4.
2026-05-25  When L4 types small-dense recurrence state (rotation registers, bidiagonal coefficients) as part of the ephemeral phase bundle rather than `SimState`, the sequential-obstruction recorded at L3 surfaces naturally as a pure function on that bundle — no monadic effect, no field-level operator. The obstruction is visible by absence rather than presence.
2026-05-25  When L4 stratifies state into persistent/fixed/ephemeral, small-dense sequential obstructions (recurrences with no global tensor-field expression) become visible-by-absence — they appear as pure functions on the ephemeral bundle, with no monadic effect to lift. This is the canonical L4 shape for solver slices and should be reused for CG/BiCGStab/MINRES.
2026-05-25  L4→L4 self-rotation: when a layer's open-questions list flags its own over-articulation ('plumbing X is over-articulated'), that is a push-back signal from the writer to themselves — the next cycle on that slice should consume it and tighten, not defer. The friction surface IS the work-item.
2026-05-25  When a rotation_claim describes a self-rotation that tightens an existing layer, the diff must include the actual surface edit to that layer — a lessons-only diff with rotation_claims describing structural L4 changes is unverifiable and conflates the meta-observation (the lesson) with the work (the tightening).
2026-05-25  Retroactive rotation_claims emission cycles (where the spec content already exists and the diff is a no-op log) are a legitimate pattern for backfilling structural verification, but should explicitly mark themselves as such so the Meta-Critic can distinguish them from cycles that failed to produce a diff.
2026-05-25  L4→L4 tightening: when an Li form reads a configuration tuple more than once, the rotation candidate is a constructed-operator absorption — build a small named value (here `Convergence`) that captures the tuple and exposes one predicate. The main loop then closes over the value, not the parameters.
2026-05-25  Constructed-operator absorption surfaces should enumerate BOTH the OpParams fields AND the SimState fields they uniquely read/write — variant absorption that names only one ownership domain leaves the other unaudited.
2026-05-25  Helmholtz-decomposition projectors rotate to L2 cleanly as a four-primitive chain (apply_linop · set_subvector_zero · ksp_solve · axpy); the triple product Gᵀ M G is never materialized — the rotation hides the never-formed operator rather than a materialized one.
2026-05-25  Complex-vector apply often L2-unrolls as a primitive-level repetition over {re, im} with one component-blind ksp_solve in the middle; this is a recurring shape (projector-style operators that are real-operator block-diagonal on Re/Im with a complex-typed solver in the kernel) worth tracking as a candidate methodology pattern.
2026-05-25  Projector-style operators whose kernel is real-operator block-diagonal on {Re, Im} with a complex-typed solver in the middle recur as an L2 pattern (divfree complex apply; likely to recur in other Helmholtz-decomposition / curl-curl preconditioners) — worth promoting to a named methodology shape if a second instance appears.
2026-05-25  When a rotation_claim invokes 'constructed operator OR closure variable' as alternatives for where variant-private state lives, that disjunction is itself a check-#11 (setup/state schema) failure — the absorption isn't pinned until the state location is named.
2026-05-25  Orthogonalization-strategy choice (CGS / MGS / CGS2 / Householder) is load-bearing in the lessons.md sense — it affects the finite-precision numerical result, not merely the implementation path. It is therefore preserved as an explicit L1 parameter and absorbed parametrically rather than as a transparent fusion.
2026-05-25  Chebyshev iteration is the inner-product-free counterpart to CG: same minimax-polynomial structure, but uses a fixed polynomial from a supplied spectral interval rather than an adaptive Krylov subspace. This makes it the canonical communication-light smoother for multigrid and GPU/distributed settings.
2026-05-25  When a load-bearing variant family contains a member with structurally different state (e.g., Householder's reflector sequence vs. Gram-Schmidt's maintained Q), level-(c) primitive-sequence absorption typically fails for that outlier even when level-(a) invariant absorption succeeds. The discipline is to either declare the residual axis explicitly at L1 (variant-conditional state schema) or split the outlier into a sibling slice; silent absorption of structurally-distinct variants is a known failure mode.
2026-05-25  When a cycle's diff is a log-only block with no file_create / file body, the per-cycle Critic should flag it as `unclear` (no salvageable surface accumulates) rather than evaluating the rotation_claims in isolation — the orchestrator's diff-apply override (meta-review #5) only catches malformed diffs, not intentionally-empty ones.
2026-05-25  Backfill cycles that add rotation_claims against already-on-disk prose (rather than emitting new prose) leave the Critic unable to verify level-(c) primitive-sequence disclosures or L2 cost annotations from the diff alone — the verdict has to trust prior cycles. Consider an Explorer-pre-fetch convention for backfill cycles that surfaces the relevant L_{n+1} prose so the Critic can audit cross-layer consistency.
2026-05-25  Retroactive rotation_claim emission (claims pointing at content already on disk in a prior cycle's L3 section) is a legitimate accumulation mode when the prior cycle landed structural content without the structured claim records; the Critic should verify against the on-disk section, not require a fresh diff.
2026-05-25  Retroactive_claims cycles with a complete retroactive_claim_evidence block (quoted on-disk prose per claim) are straightforwardly auditable; this is the pattern to encourage when a prior cycle's diff landed structure but did not emit rotation_claims at the time.
2026-05-25  L4 rotation pattern: when multiple termination reasons share a return path (GMRES: converged, max_it, max_dim), collapse them into a sum type (Outcome = Continue | Done Bool) and classify once at the loop boundary. Avoids scattered re-tests at the L3→L4 edge.
2026-05-25  L4 constructed-operator surfaces extend beyond linear operators: the convergence policy (rel_tol, abs_tol, initial_residual rescaling) absorbs into a `Convergence = { epsilon, satisfied }` value built once per restart cycle. Same pattern as apply_BA absorbing pc_side — pull variant-conditioned dispatch into a constructed value built at solve start, called uniformly thereafter.
2026-05-25  L3 obstruction → L4 typing: a sequential-obstruction record at L3 should NOT be hidden by a monadic effect at L4. Type the obstruction-bearing operation as a plain pure function on its small-dense state bundle (Krylov → Krylov for ls_update_column); the function signature itself is the calculus statement that this is a scalar recurrence, distinct from field-level tensor primitives.
2026-05-25  L4 rotation patterns recur: state-bundle stratification by lifecycle (persistent/operator-internal/ephemeral), monad over the persistent bundle only, sum-type collapse of multi-reason termination, and constructed-operator absorption of policy (Convergence here, analogous to apply_BA for pc_side). Worth abstracting as a checklist for future L4 slices.
2026-05-25  L2→L3 is clean when the L2 primitive chain has no surviving cross-step sequential dependency — each primitive lifts to a global tensor-field map independently. divfree is the canonical clean case: four global ops, with the only sequential machinery (CG iteration) confined inside the ksp_solve primitive's own L3 obstruction (recorded in the cg slice). The compositional rule: a slice's L3 lift is clean iff every primitive it composes either has a clean L3 lift or names its own internal obstruction.
2026-05-25  Sequential obstructions are slice-local, not global. The divfree slice's L3 form is global tensor-field even though ksp_solve internally drives a sequential CG. The obstruction lives at the primitive's interior and the slice composing it remains clean — provided the slice doesn't expose the per-iteration state.
2026-05-25  Compositional L2→L3 cleanness rule: a slice's L2→L3 lift is clean iff every composed primitive has a clean L3 lift OR names its own interior obstruction without exposing it through the slice's threaded state. The divfree slice formalizes this via ksp_solve: CG's interior sequential obstruction is recorded in the cg slice and does not propagate to divfree because the slice does not thread per-CG-iteration state.
2026-05-25  When a slice has a variant axis where some variants admit a global L3 form and others don't, name the obstruction at L3 as a first-class claim — don't silently restrict L3 to the parallelizable variants. The slice's variant-absorption posture changes shape at L3 (some variants unify, others split off as obstructions); make the shape change explicit.
2026-05-25  L2-to-L3 on already-batched primitives is a notational compression, not a structural rotation: `[m local dots, allreduce_sum, gemv_basis]` is just the realization of `Vᴴ w` followed by `V H`. The rotation is real (the L3 form names the global operator the L2 chain implements), but the justification is reduction_chain rather than novel hiding.
2026-05-25  Sequential-obstruction at L3 is a legitimate justification_kind: when a variant axis splits at the rotation boundary (some variants admit a global form, others have intrinsic sequential dependencies), naming the obstruction is itself the L3 claim for those variants — variant absorption splits rather than unifies.
2026-05-25  Initial-residual computations in iterative solvers sometimes apply norm-of-norm by accident (Palace CG: `sqrt|Norml2(b)|` in the no-preconditioner branch yields `(b·b)^{1/4}`, not `‖b‖₂`). When auditing L0, check the type of each intermediate in a residual/tolerance formula — confusing `Dot(b,b)` (returns squared norm) with `Norml2(b)` (returns norm) compounds a sqrt and silently rescales convergence thresholds.
2026-05-25  Assertion-only side-effects like Palace's `CheckDot` are partial-function guards at L1, not runtime branches — at L4 they map to preconditions on the algebraic form, not to `if` expressions. Model them in L1 so the Critic can verify their presence; drop them at L4 in favor of the precondition statement (SPD, finite arithmetic).
2026-05-25  Back-correction cycles (Explorer audit → Synthesizer fix) are a legitimate cycle shape when prior layers contain algebraic errors discovered by re-reading L0 — verdicting on rotation quality of the *corrected* form (not on the fact that it's a fix) keeps the loop honest.
2026-05-25  Retroactive L0→L1 rotation claims for mutation-erasure are well-served by the four-pattern taxonomy (in-place left-operand mutation, AXPBY destination convention, output-buffer write, loop-carried-variable bundle); future slices that wrap MFEM primitives can reuse this taxonomy verbatim.
2026-05-25  Retroactive_claims cycles that omit the `retroactive_claim_evidence` quoted-prose block put the Critic in an unauditable position; the schema-level fix (require the block) should be enforced at plan-validation time, not only at critic time.
2026-05-25  Retroactive-claims cycles for already-landed slice content are productive when the original synthesis cycle skipped per-edge claim emission. CG slice v0.3 had L0/L1/L2/L3/L4 content on disk but only one summary-claim from the prior cycle; this cycle emits per-substitution claims for the L0→L1 edge — mutation-erasure, workspace-aliasing rename (`p_tmp`), `CheckDot` partial-function guard surfacing, and the faithful-rendering of the `!B && initial_guess` quirk — each citing specific source line ranges. Pattern: when a slice ships with summary-level claims, schedule a retroactive-claims cycle per edge to recover per-primitive granularity (meta-12 item 1).
2026-05-25  Workspace-aliasing renames (Palace's `p` reused as scratch for `(Bb, b)` then later as the search direction) are a recurring L0→L1 rotation kind across Krylov slices — worth tracking as a sub-flavor of state-hiding when GMRES, BiCGSTAB, etc. enter the spec.
2026-05-25  Retroactive-claims cycles that include a retroactive_claim_evidence block with quoted on-disk prose per claim are auditable from the Critic's input alone; this is the pattern meta-11 prescribed and it works in practice for cg.md v0.3.
2026-05-25  Chebyshev L3 is the prototypical partial obstruction: body lifts to a global field expression, both loops (degree-k recurrence and pc_it Richardson sweep) are sequential. The k-recurrence sequentiality is *load-bearing* — the closed-form polynomial expansion is numerically unstable, so the recurrence is not an implementation choice but a stability requirement (Phillips & Fischer 2022 §2). Cite the stability rationale when recording the obstruction, otherwise the obstruction reads as 'we didn't try hard enough'.
2026-05-25  L4 rotations on iterative solvers benefit from a third constructed-operator surface for convergence policy (build_convergence value + Convergence ADT), symmetric to apply_BA's absorption of pc_side — pulls rel_tol/abs_tol/initial_res reads out of the main control flow.
2026-05-25  Collapsing a termination decision table (final_res × ε × max_it × max_dim × converged_flag) into a single Outcome ADT returned by the inner loop is a recurring L3→L4 move: the L3 imperative form fans the decision across multiple sites; the L4 form classifies once at the outer loop and folds via the monad.
2026-05-25  Retroactive-claims cycles that include quoted retroactive_claim_evidence per claim auditable from the Critic's input alone — the meta-11 discipline pays for itself when L4 prose already exists on disk and the rotation reasoning is being captured after the fact.
