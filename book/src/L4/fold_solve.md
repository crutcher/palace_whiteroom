---
layer: L4
operator: fold_solve
firmness: firm
rank: firm
edges:
  depends-on:
    - target: L4/iterate-while
      kind: folds                     # the strawman §3.7 family whose carry-threading non-degenerate form fold_solve IS — the shared parent of solve_family's map + this fold; NO third parent abstraction
    - target: concepts/op-params
      kind: uses-record               # signature: fold_solve :: OpParams -> TimeState -> [Time] -> TimeState — op captured once at TimeOperator construction, readonly
    - target: L4-L3/fold-solve-time-step-dissolution
      kind: lowers-to                 # the L4>L3 dissolution to the L3 explicit for-loop threading the field-state in place, the per-step body a role-naming wrapper over the opaque MFEM ODESolver step (canonical slug fold-solve-time-step-dissolution)
  reference:
    - L4/solve_family                 # the independent-MAP contrast-sibling (NOT consumed; referenced for the map/fold distinction)
    - concepts/state-stratification   # op captured once at TimeOperator construction / readonly; TimeState the persistent per-step-threaded carry stratum
    - concepts/sequential-obstruction # the fold spine cannot reorder — each step's input is the prior step's output; AND the per-step body is an opaque library step
    - concepts/derived-view-hoisting  # the §3.8 demand-pruning governing whether the intermediate-state trajectory materializes
variant_axes:
  - schedule-source (fixed-list: the carry consumes a precomputed [Time] schedule, foldl over a uniform list — transient | state-generated: the carry GENERATES the next input + the loop bound from accumulated state, an error-terminated march — TWO witnesses: driven-PROM SweepAdaptive (greedy frequency-sampling) + the AMR Solve→Estimate→Mark→Refine loop (basesolver.cpp:190, error-indicator-terminated). THE load-bearing axis; the fixed-list form is the default surface, the state-generated form the recorded generalization, now twice-witnessed)
  - per-step-operator (opaque-library: the step bottoms out in a library integrator/sampler the L4 entry quantifies over — MFEM ODESolver for transient, RomOperator greedy sampler for SweepAdaptive; absorbed into the op : OpParams stratum)
  - carry-shape (single field-state TimeState — transient | field-state + growing reduced basis + error history — SweepAdaptive; absorbed into the carry type, does not shape the spine)
  - element-type (real — transient | complex — driven-PROM; absorbed into OpParams / the carry, as at the ksp_solve cap)
---

# fold_solve

The L4 **state-threaded fold outer-driver combinator**: thread a persistent field-state carry through a schedule of steps, advancing it one opaque per-step operator (`time_step_op`) at a time, where **each step's input is the prior step's output**, and return the final state. Where [`solve_family`](./solve_family.md) is the **independent map** over an RHS family (no carry between elements; the trajectory IS the collected family), `fold_solve` is its **sequential fold sibling** — the previous step's output is the next step's input, so the steps do NOT commute and cannot be collected independently. Both are children of the strawman §3.7 [`iterate-while`](./iterate-while.md) family (RATIFIED batch-17 meta decision 2): a **map is the degenerate fold whose step ignores the accumulator**; `fold_solve` is the **non-degenerate** member that threads the accumulator. It reuses the firm iterate-while vocabulary rather than introducing a new iteration primitive — the same route [`chebyshev`](./chebyshev.md) and [`solve_family`](./solve_family.md) took.

## Context

L4's job is to write algorithms in a graph-evaluation calculus that makes lifetimes, dispatch sites, and effect placement structural (`L4/index.md:7-13`). `fold_solve` names the coordination shape one shell *above* a per-step solve, the fold-sibling of [`solve_family`](./solve_family.md)'s independent map:

- [`solve_family`](./solve_family.md) names the **independent-map shell** — a fixed `op`, a family `[rhs_i]` → the solution family `[x_i]`, *no carry between elements* (each solve independent; the family commutes).
- `fold_solve` (this entry) names the **sequential-fold shell** — a captured `op`, a seed state `s0`, a schedule → the final state, *carry threaded forward* (each step's input is the prior step's output; the steps do NOT commute).

The architectural altitude is the same as [`solve_family`](./solve_family.md)'s and `solve_loop`'s — all are value-threaded loop combinators in the strawman's §3.7 family (`book/src/design/l4_calculus.md:150-184`). The combinator differs only by the **map/fold axis**: [`solve_family`](./solve_family.md) is the **pure-map degenerate** (each element independent, no carry), `fold_solve` is the **non-degenerate fold** (the carry is threaded). This is exactly the strawman §3.7 reading: `iterate_while` threads a `state` carry forward; a map is the special case where the step's output does not depend on the prior carry, a fold the general case where it does. The §3.7 `iterate_while` family **IS** the shared parent of both — no third parent abstraction is warranted (RATIFIED batch-17 meta decision 2; `L4/index.md:62`).

The combinator is defined **in L4 vocabulary** (high→low discipline, CLAUDE.md §Methodology invariants): its semantics, signature, and laws are stated in terms of the [`iterate-while`](./iterate-while.md) family, the [`state-stratification`](../concepts/state-stratification.md) operator/carry strata, and the [`sequential-obstruction`](../concepts/sequential-obstruction.md) of the carry threading — NOT in terms of L3 value-threading or the L0 C++ time-loop. The L4>L3 dissolution (the `foldl` collapsing to an explicit `for`-loop threading the field-state in place, the per-step body a role-naming wrapper over the opaque MFEM `ODESolver` step) is a separate cycle-058 L4>L3 theme (`fold-solve-time-step-dissolution`, authored by D2 this same cycle), narrated forward from L4 to L3; it is **not** authored here.

`fold_solve` at L4 is a **methodology-level combinator** distilled from two Palace driver-source state-threaded sweeps (transient time-march + driven-PROM greedy basis construction), not a Palace-source artefact per se — there is no single L0 range that "is" the L4 `fold_solve`. The Palace evidence is the two sweeps in §Specializations; L4 names the fold combinator and the carry-threading those two sweeps share.

## Signature

The combinator captures the operator once, seeds the carry once, and threads the carry through the schedule by `foldl`. The **default surface** is the fixed-list schedule (transient witness); the state-generated-schedule generalization is the `schedule-source` variant axis (§Variant axes) rendered through the §3.7 carry form.

    -- entry point (default surface): fixed-list state-threaded fold
    -- capture op once, seed s0 once, thread the carry through a precomputed schedule
    fold_solve :: OpParams -> TimeState -> [Time] -> TimeState
    fold_solve op s0 schedule = foldl (\s t -> time_step_op op s t) s0 schedule

    -- the opaque per-step operator the combinator quantifies over:
    -- advances the field-state one step; bottoms out in a library integrator
    time_step_op :: OpParams -> TimeState -> Time -> TimeState

    -- equivalently, as the NON-degenerate member of the strawman §3.7 iterate_while family
    -- (carry = field-state + schedule-state; the step reads the prior carry to advance it):
    fold_solve op s0 schedule =
      (iterate_while
         { field: s0, remaining: schedule }      -- carry: persistent field-state + schedule-state
         (\c -> not (null c.remaining))            -- continue while schedule remains
         (\c -> let t  = head c.remaining
                    s' = time_step_op op c.field t -- prior FIELD is read: this is the fold, not a map
                in { state: { field: s', remaining: tail c.remaining } }))
        .final_state.field                         -- == foldl (\s t -> time_step_op op s t) s0 schedule

Shape contract (bunsen-style; named records and axes; the operator/carry strata per [`state-stratification`](../concepts/state-stratification.md)):

- `OpParams` — operator-internal configuration, **captured once at construction; `readonly`** per [`state-stratification`](../concepts/state-stratification.md). This is the L4 typing of the `TimeOperator time_op(iodata, space_op, dJdt_coef)` built *once* outside the loop (transient `transientsolver.cpp:33`), whose ODE-integrator `op` is constructed once (`timeoperator.cpp:312`). `op` is the *shared* operator stratum threaded *unchanged* into every `time_step_op` call.
- `TimeState` — the persistent field-state carry the fold threads forward. For transient it is the coupled `(E, B)` field bundle (the `sol` vector `timeoperator.cpp:410` advances in place); each step reads the *prior* `TimeState` and produces the next. **This is what makes the combinator a fold and not a map**: `time_step_op op s_k t_{k+1}` reads `s_k` (the prior step's output), so the steps are strictly sequential and do not commute.
- `[Time]` (default surface) — the **fixed** schedule the fold ranges over: a precomputed uniform list of `n_step` timesteps (transient `n_step = config::GetNumSteps(...)` `transientsolver.cpp:36`, uniform `delta_t` `transientsolver.cpp:35`). The schedule-source is the load-bearing variant axis: for the state-generated form (SweepAdaptive) the schedule is *generated* from the carry, not consumed from a fixed list (§Variant axes, §Specializations).
- result `TimeState` — the final field-state after the whole schedule is threaded. Only `.final_state` is observed (per §3.8 demand-pruning the trajectory of intermediate states is pruned unless a per-step consumer reads it — and transient DOES consume each step's `(E, B)` for postprocessing `transientsolver.cpp:98-99`, so the trajectory materializes in practice).
- `FoldCarry = { field: TimeState, remaining: [Time] }` — the `iterate_while` carry in the §3.7-form rendering; transient (the `foldl` form `foldl (time_step_op op) s0 schedule` does not name it). For the state-generated form the carry additionally holds the schedule-generating state (the growing reduced basis + error history — §Specializations).

The shape contract makes two things structural at the fold level that are merely conventional in the Palace C++ sweep:

1. **The operator is captured once, outside the fold.** `op : OpParams` appears once in the signature, bound before the fold; every per-step `time_step_op` reads the *same* `op`. This is the L4 typing of `TimeOperator` built once outside the time loop (transient `transientsolver.cpp:33` / `timeoperator.cpp:312`, outside the `:77` loop).
2. **The carry is threaded; the steps are sequential.** `TimeState` is threaded forward by the fold; each `time_step_op` reads the prior carry. This forbids reordering at the type level — the property [`solve_family`](./solve_family.md)'s map does **not** have (its elements are independent and commute). The map/fold distinction is exactly the carry-dependence: a map's step ignores the accumulator, a fold's reads it.

## Semantics

`fold_solve op s0 schedule` is the complete state-threaded march expressed as a `foldl` of the opaque per-step operator `time_step_op` over the schedule, seeded by `s0`, with a single shared operator capture. It has two equivalent presentations (the §Signature renders both):

1. **Direct fold form** — `fold_solve op s0 schedule = foldl (\s t -> time_step_op op s t) s0 schedule`. The combinator *is* a left fold: the seed `s0` is threaded through the schedule, each step advancing the field-state by one opaque `time_step_op`. This is the form the combinator is named for, and the form that makes the algebraic laws (§Algebraic laws) immediate.

2. **Non-degenerate member of the §3.7 `iterate_while` family** — the family rendering (`book/src/design/l4_calculus.md:150-184`), where the carry is `{ field, remaining }`, the predicate is `not (null c.remaining)`, the step pops one `Time`, reads the prior `field`, and runs one `time_step_op`. By §3.7 the `.final_state.field` is exactly `foldl (time_step_op op) s0 schedule`. This presentation reuses the firm [`iterate-while`](./iterate-while.md) family rather than introducing a new iteration vocabulary (the [`chebyshev`](./chebyshev.md) / [`solve_family`](./solve_family.md) route, `L4/index.md:37`); the choice between the two presentations is a presentation rotation, not a semantic distinction.

The combinator's **structural contrast with [`solve_family`](./solve_family.md)** — the reason it is a distinct entry rather than a reuse of the map — is the **carry-dependence of the step**. [`solve_family`](./solve_family.md)'s step `ksp_solve op inp` ignores any accumulator (each element independent → the map's concatenation-homomorphism, element-independence, embarrassing-parallelism laws). `fold_solve`'s step `time_step_op op s t` reads `s` (the prior carry) → the steps are sequential, do NOT commute, and do NOT split into independent partitions. This is the §3.7 reading of the map/fold axis: the map is the degenerate fold whose step is constant in the accumulator; `fold_solve` is the non-degenerate fold whose step threads it. The two are siblings under the one §3.7 parent, not instances of one combinator.

Per [`state-stratification`](../concepts/state-stratification.md), `op` is the shared `readonly` operator stratum captured once; `TimeState` is the threaded persistent carry stratum. The fold does not introduce a new monadic effect — the per-step `time_step_op` discharges its own step effect (advancing the field-state) and the fold threads the result. The combinator is therefore a **pure function** `(op, s0, schedule) -> TimeState` (modulo whatever non-determinism the opaque per-step operator inherits; the fold introduces none).

### The opaque per-step body

`time_step_op` bottoms out in an **opaque library step** the L4 entry only quantifies over — it does not render the per-step body. For transient the step is the MFEM `ODESolver::Step` call (`timeoperator.cpp:410` `ode->Step(sol, t, dt)`, dispatched from `transientsolver.cpp:93` `time_op.Step(t, delta_t)`); the integrator (`GeneralizedAlphaSolver` / `SDIRK23Solver` / `ARKStepSolver`) is selected at construction (`timeoperator.cpp:312`+) and is library-owned. At L4 the entry names the per-step operator by role (`time_step_op`) and quantifies over it; **the opacity is a lowering-layer concern** — the L4>L3 theme `fold-solve-time-step-dissolution` (cycle-058 D2) records the per-step body as `obstruction (opaque-library-ownership)` (the `eigsolve` opaque-library shape, `L4/eigsolve.md`), NOT a Palace-authored loop. At L4 the entry just folds over the opaque operator.

### Demand-pruning interaction

Under the §3.8 pruning rule (`book/src/design/l4_calculus.md:186-228`), the **threaded carry is never pruned** (every step's `field` is read by the next step's `time_step_op` — the fold's defining data-dependence), but the per-step *trajectory* of intermediate states is pruned when no consumer reads it. In Palace's transient sweep each step's `(E, B)` IS consumed immediately (postprocessing per step: `transientsolver.cpp:98` `time_op.GetE()` / `:99` `time_op.GetB()`), so the trajectory materializes in practice; but the combinator's typing makes the demand-driven materialization of intermediate states structural (only the `final_state` survives when intermediate states are unobserved — the §3.7 trajectory-pruning the fold inherits).

## Algebraic laws

`fold_solve` is a **`foldl` combinator**, so its laws are the laws of a left fold over an opaque step specialised to the captured-operator march. Absences are catalogued explicitly to prevent decoration drift. Every law is a **syntactic identity on the read fold** — read directly off the two positive driver loops (the firm-on-positive-structure basis for the `firm` status).

1. **Fold-threading associativity / schedule-split** (the load-bearing law). `fold_solve op s0 (a ++ b) = fold_solve op (fold_solve op s0 a) b`. The fold of a concatenated schedule is the fold of the second segment seeded by the fold of the first — the standard `foldl (a ++ b) = foldl b . foldl a` law, specialised to the captured operator. This is the algebraic statement of the carry-threading: a march can be **checkpointed and resumed** (split the schedule, fold the prefix, fold the suffix from the prefix's final state). It is the **fold analog of [`solve_family`](./solve_family.md)'s concatenation-homomorphism** — but note the difference: `solve_family` *distributes* over `++` (`f (a++b) = f a ++ f b`, independent), whereas `fold_solve` *threads* through `++` (`f s0 (a++b) = f (f s0 a) b`, sequential). The map splits into independent halves; the fold splits into a checkpoint-and-resume.

2. **Operator-capture-once / construction-hoist**. `fold_solve op s0 schedule = foldl (time_step_op op) s0 schedule`, and the `op`-dependent integrator construction is **invariant across the fold**, so it hoists out (computed once, before the schedule is threaded). This is the L4 typing of `TimeOperator` / the ODE integrator built once outside the time loop (transient `transientsolver.cpp:33` / `timeoperator.cpp:312`, outside the `:77` loop). Same hoist as [`solve_family`](./solve_family.md) law 2 — the operator capture is shared by the whole §3.7 family.

3. **Seed left-identity on the empty schedule** (`fold_solve op s0 [] = s0`). The empty schedule threads the seed unchanged — the `foldl ... s0 [] = s0` identity. A degenerate (not algebraic) identity; Palace's transient march always has `n_step > 0` (`transientsolver.cpp:36`), so the empty case is a calculus-level total-definition convenience, not a witnessed Palace path. (Contrast [`solve_family`](./solve_family.md) law 4 `f op [] = []`: the empty map yields the empty family; the empty fold yields the seed.)

Laws that explicitly **do not** hold:

- **Element-independence / commutativity / distribution over `++`** (the load-bearing *non*-law — the map/fold distinction). `fold_solve op s0 (a ++ b) ≠ fold_solve op s0 a ‹combined-with› fold_solve op s0 b` for any independent combination — because each step's input is the prior step's output. The schedule does **NOT** commute (`fold_solve op s0 (permute schedule) ≠ permute (...)`): reordering the timesteps changes the field-state trajectory. This is precisely what distinguishes `fold_solve` from [`solve_family`](./solve_family.md) (whose law 1/law 3 ARE distribution + commutativity). A fold is not embarrassingly parallel; the carry-threading is a [`sequential-obstruction`](../concepts/sequential-obstruction.md).
- **Step fusion into a single closed-form whole-march op.** The per-step `time_step_op` invocations do not fuse into one closed-form whole-schedule operator — each is an opaque library integrator step with its own internal state advance. The fold threads independent opaque steps; it does not collapse them. (The schedule-split law 1 is *checkpoint-resume*, not *fusion*.)
- **Linearity of the final state in the seed or schedule.** `time_step_op` is in general a nonlinear/implicit integrator step (the transient solver is `IMPLICIT`, `timeoperator.cpp:310`-region); the final state is not linear in `s0` nor in the schedule. No linearity law is claimed.
- **Schedule-source uniformity across the variant axis.** The fixed-list laws (1–3) hold for the **fixed-schedule** form (transient). The **state-generated** form (SweepAdaptive) generates its schedule + loop bound from the carry (the next input `omega_star = FindMaxError(state)` `drivensolver.cpp:389`, the bound `memory < convergence_memory` `drivensolver.cpp:384,398`); the schedule-split law 1 still holds *for a realized schedule* but the schedule is no longer a free `++`-decomposable input. The laws here are scoped to the fold spine both forms share; the schedule-source generalization is the `schedule-source` variant axis (§Variant axes) + OQ `fold-solve-greedy-schedule-source-generalization`.

## Specializations

Per replace-and-propagate (CLAUDE.md §Methodology invariants vocabulary-shift redirect), `fold_solve` is the **entry**; the two Palace state-threaded sweeps are **specialization notes re-expressing THROUGH it**, not separate rectangular leaf chapters. Both instantiate the state-threaded fold spine (carry threaded forward, prior output = next input, opaque per-step body); they differ on the **schedule-source** variant axis (the load-bearing difference).

- **Transient fixed-schedule time-march** (`palace/drivers/transientsolver.cpp`, `TransientSolver::Solve`). The **default surface** (the `[Time]`-list fold). `op = TimeOperator` built once (`:33`) with its ODE integrator constructed once (`timeoperator.cpp:312`) — outside the loop. The seed `s0` is the initial field-state set by `time_op.Init()` (`:89`). The schedule is a **fixed uniform** list: `n_step = config::GetNumSteps(0.0, max_t, delta_t)` (`:36`) timesteps of uniform `delta_t` (`:35`). The fold body is `time_op.Step(t, delta_t)` (`:93`) → `ode->Step(sol, t, dt)` (`timeoperator.cpp:410`), which advances the persistent `sol` field-state in place — **the prior step's `sol` is the next step's input**, the genuine `foldl`. The loop is the `for (int step = 0; step < n_step; step++)` (`:77`). Per-step `(E, B)` consumed for postprocessing (`:98`/`:99`). Element-type: real. This witness gives the fixed-list signature surface directly.

- **Driven-PROM greedy basis construction (SweepAdaptive)** (`palace/drivers/drivensolver.cpp`, `DrivenSolver::SweepAdaptive`, `:231`; dispatched `adaptive ? SweepAdaptive : SweepUniform` `:73`). The **state-generated-schedule** form — strictly richer than the fixed-list fold: the schedule (which frequencies to sample) is **generated from the carry**, not consumed from a fixed list. The carry is the growing reduced basis + error history (the `RomOperator prom_op` + `max_errors` accumulator). The greedy loop `for (std::size_t it0 = it; it < max_size_per_excitation && memory < convergence_memory; it++)` (`:384`) has a **state-derived loop bound** (`memory` is updated from per-step error `:398`; PROM greedy params `offline_tol` `:241`, `convergence_memory` `:242`, `max_size_per_excitation` `:243`, `prom_indices` `:244`) AND a **state-derived per-step input** (`omega_star = prom_op.FindMaxError(excitation_idx)[0]` `:389` — the next sample frequency is the argmax of the *current* PROM error). Each step reads the prior carry (basis + errors) and produces the next (one sample added). Element-type: complex.

- **AMR mesh-refinement loop (Solve→Estimate→Mark→Refine)** (`palace/drivers/basesolver.cpp`, `BaseSolver::SolveEstimateMarkRefine`, `:153-276`). A **2nd state-generated-schedule** witness (alongside SweepAdaptive) — the schedule (the sequence of refined meshes) is **generated from the carry**, not consumed from a fixed list. The seed `s0` is the initial solve + error estimate (`auto [indicators, ntdof] = Solve(mesh)` `:174`, `err = indicators.Norml2(comm)` `:175`). The loop `while (use_amr && !ExhaustedResources(it, ntdof) && err >= refinement.tol)` (`:190`) has a **state-derived loop bound** — the genuine termination criterion is the recomputed error indicator `err >= refinement.tol` (`err` re-evaluated each iteration from the new solve, `:267`), with resource caps `it >= refinement.max_it` (`:182`) and `ntdof > refinement.max_size` (`:184`) as bounds — AND a **state-derived per-step input**: the refined mesh `GeneralRefinement(marked_elements, ...)` (`:239`), where `marked_elements` is Dörfler-thresholded from the **current** error-indicator state. Each step reads the prior carry (mesh + error indicators) and produces the next (refined mesh + new solve `std::tie(indicators, ntdof) = Solve(mesh)` `:266`). This is the **2nd witness** that the `schedule-source = state-generated` value is genuinely multi-witness (not a SweepAdaptive singleton). The per-step body bottoms out in the opaque `Solve(mesh)` driver call (the same opaque-library quantification). AMR is currently unsupported for transient (`:157-162`).

Both the transient + the two state-generated sweeps share the **state-threaded fold spine** (carry threaded forward, prior output = next input, opaque per-step body). The difference is the **`schedule-source` axis**: transient consumes a fixed `[Time]`; SweepAdaptive generates `[omega_star_k]` and AMR generates `[refined_mesh_k]`, both from accumulated state with a state-derived termination. The fixed-list form is the special case of the §3.7 carry form where the carry's next-input is `head remaining` and the predicate is `not (null remaining)`; the greedy form is the general case where next-input and predicate are both state-derived. **Classification decision (load-bearing): ONE combinator** — the §3.7 carry form covers both; the schedule-source is the recorded variant axis, not a distinct entry. I did NOT force the fixed `foldl-over-[Time]` signature onto SweepAdaptive (it exhibits no fixed `[Time]`) — the fixed-list signature is the *default surface*, with the state-generated form rendered through the §3.7 carry. (If the state-generated greedy form proves to warrant its own chapter once a downstream PROM consumer pulls on it, that is a batch-18 size-judgment per OQ `fold-solve-greedy-schedule-source-generalization`; the default here is notes-in-entry per combinator-as-entry.)

## Dependencies

L4 rows this combinator consumes:

- [`iterate-while`](./iterate-while.md) — the strawman §3.7 family whose **non-degenerate carry-threading member** `fold_solve` IS (the carry is threaded; the step reads the prior carry). This is the **shared parent** of [`solve_family`](./solve_family.md) (map = degenerate fold) and `fold_solve` (non-degenerate fold); no third parent abstraction is introduced (RATIFIED batch-17 meta decision 2). Reused rather than introducing a new iteration vocabulary — the [`chebyshev`](./chebyshev.md) / [`solve_family`](./solve_family.md) route.

L4 contrast-sibling (not consumed, referenced for the map/fold distinction):

- [`solve_family`](./solve_family.md) — the independent-**map** sibling. Same §3.7 parent, same operator-capture-once stratification; differs solely on the carry axis (map's step ignores the accumulator → independence/commutativity/distribution laws; fold's step threads it → checkpoint-resume/sequential-obstruction laws).

L4 concept references:

- [`state-stratification`](../concepts/state-stratification.md) — `op` is the shared `readonly` operator stratum captured once at `TimeOperator` construction; `TimeState` is the persistent carry stratum threaded by the fold.
- [`sequential-obstruction`](../concepts/sequential-obstruction.md) — the carry-threading IS a sequential obstruction (each step's input is the prior step's output; the schedule does not commute) — the load-bearing contrast with [`solve_family`](./solve_family.md)'s commuting map. Additionally the per-step body is an opaque-library step (a second obstruction, recorded at the lowering layer).
- [`derived-view-hoisting`](../concepts/derived-view-hoisting.md) — the §3.8 demand-pruning governing whether the *intermediate-state trajectory* materializes (the threaded carry itself is never pruned — the fold's defining data-dependence).

Speculative rough-in operators referenced (sub-components, refined when a downstream consumer pulls):

- `time_step_op` — the opaque per-step operator the fold quantifies over (one MFEM `ODESolver` step for transient; one greedy PROM sample for SweepAdaptive). Bottoms out in a library boundary at L0 (`timeoperator.cpp:410`); `obstruction (opaque-library-ownership)` at the lowering layer.
- `TimeState` — the persistent field-state carry the fold threads.

**Strawman reference**: `book/src/design/l4_calculus.md` §3.7 (`iterate_while` + the `iterate_while_pure` sugar, `:150-184`) is the family this fold's carry-threading non-degenerate form joins; §3.8 (demand-pruning, `:186-228`) governs intermediate-state materialization.

## Lowers to

L4 `fold_solve` lowers to an L3 explicit `for`-loop threading the field-state in place (the Palace C++ shape, §Specializations) with the operator construction hoisted outside the loop — via the L4>L3 dissolution theme [`fold-solve-time-step-dissolution`](../L4-L3/fold-solve-time-step-dissolution.md) (**cycle-058 D2 abstractor; authored this same cycle**; canonical slug `fold-solve-time-step-dissolution`). The rotation is **substantive** (not identity-in-form): the `foldl` collapses to a positional `for (int step = ...)` loop threading the persistent `sol` field-state in place; the `op`-capture-once becomes the `TimeOperator`-built-outside-the-loop placement; the per-step body becomes a **role-naming wrapper over the opaque MFEM `ODESolver` step** (`ode->Step(sol, t, dt)` `timeoperator.cpp:410`) — `obstruction (opaque-library-ownership)` (the [`eigsolve`](./eigsolve.md) opaque-library shape, NOT a Palace-authored loop). This entry records the rotation *direction* (L4 fold combinator → L3 explicit in-place-threading loop with an opaque per-step body) in-line per high→low discipline; it does **not** author the theme. The standalone L3 entry question is **RESOLVED (cycle-059 D1, verdict L3-ENTRY)**: a standalone [`L3/fold_solve`](../L3/fold_solve.md) entry IS warranted (status `partial-obstruction`), because — unlike `solve_family`'s family loop (which lifts cleanly — no obstruction, so its dissolution theme is the authoritative L3-form home, NO-ENTRY c057) — `fold_solve`'s loop carries BOTH a carry-threading sequential-obstruction AND an opaque-library per-step body, so the L3 form is a genuine iteration-rotation `partial-obstruction` (the `chebyshev` / `eigsolve` shape) with its own standing L3-vocabulary home. The L3 entry is the standing iteration-rotation definition of the obstruction structure; this dissolution theme records the L4→L3 rewrite *direction* (forward) — the `chebyshev`/`eigsolve` division of labor. (Resolves OQ `fold-solve-l3-entry-vs-dissolution-home`.)

## Variant axes

Four axes, one load-bearing (schedule-source) and three absorbed:

1. **schedule-source** (`fixed-list | state-generated`) — **THE load-bearing axis**, and the map/fold-spine generalization boundary. `fixed-list` (the default surface, transient): the carry consumes a precomputed `[Time]` schedule, `foldl` over a uniform list (`transientsolver.cpp:35-36,77`). `state-generated` is now **twice-witnessed** (both: the carry **generates** the next input AND the loop bound from accumulated state, an error-terminated march; neither has a free `[Time]` input):
   - **driven-PROM SweepAdaptive** — next input `omega_star = FindMaxError(state)` (`drivensolver.cpp:389`), loop bound `memory < convergence_memory` (`:384,398`); the greedy frequency-sampling march.
   - **AMR Solve→Estimate→Mark→Refine** (`basesolver.cpp:153-276`, `BaseSolver::SolveEstimateMarkRefine`) — next input is the **refined mesh** generated from the prior step's error indicators (`GeneralRefinement(marked_elements, ...)` `:239`, where `marked_elements` is Dörfler-thresholded from the error-indicator state), and the loop bound is the recomputed **error indicator** (`err = indicators.Norml2(comm)` re-evaluated each iteration `:267`; `while (use_amr && !ExhaustedResources(it, ntdof) && err >= refinement.tol)` `:190`). The next input + termination are both state-generated, not a fixed list — the same state-generated shape as SweepAdaptive.

   Both state-generated witnesses share the fold spine (carry threaded, prior output = next input); the difference from `fixed-list` is whether the schedule is a free `[Time]` input or a state-derived generation. **This entry's default signature is `fixed-list`** (the transient witness); the `state-generated` form is rendered through the §3.7 carry and recorded here, with the question of whether the (now twice-witnessed) state-generated form warrants a dedicated chapter deferred to OQ `fold-solve-greedy-schedule-source-generalization` (batch-18+).
2. **per-step-operator** (`opaque-library`) — the per-step body bottoms out in a library boundary the L4 entry quantifies over (MFEM `ODESolver` for transient `timeoperator.cpp:410`; `RomOperator` greedy sampler for SweepAdaptive `drivensolver.cpp:389`). **Absorbed into `op : OpParams`**; the opacity is a lowering-layer concern (`obstruction (opaque-library-ownership)`). Does not shape the fold spine.
3. **carry-shape** (`single field-state | field-state + reduced-basis + error-history`) — transient threads a single `TimeState` (`(E, B)`); SweepAdaptive threads a richer carry (growing basis + `max_errors`). **Absorbed into the carry type**; does not shape the spine (the fold threads whatever carry the step needs).
4. **element-type** (`real | complex`) — absorbed into `OpParams` / the carry as at the [`ksp_solve`](./ksp_solve.md) cap (`L4/ksp_solve.md:153`). Transient real; driven-PROM complex.

## Status

`firm` — the **firm-on-positive-structure escape** applies (CLAUDE.md §Methodology invariants "Two rough-in qualifiers are first-class", the `apply_nonlinear_pencil` precedent). The combinator's structural signature is well-anchored at L0 (the seed/schedule/carry shape, the operator-capture-once stratification, the carry-threading sequential-obstruction) by **two confirmed fold witnesses** — transient `transientsolver.cpp:33-99` + `timeoperator.cpp:312,410` (fixed-schedule march) and driven-PROM SweepAdaptive `drivensolver.cpp:231-398` (state-generated greedy march). This **meets the ≥2-witness mining-gate bar** (`disciplined-cross-pipeline-combinator-mining-gate`, 2-of-N fold-witness discharge) for the fold *spine*. Every law (§Algebraic laws) is a **syntactic identity on the read fold**: the schedule-split law is `foldl (a++b) = foldl b . foldl a`, the seed left-identity is `foldl _ s0 [] = s0`, the construction-hoist is read directly off `TimeOperator`-built-outside-the-loop, and the load-bearing *non*-law (no commutativity/distribution) is read directly off the prior-output-is-next-input carry-threading. These are read-off identities on the positive driver loops, NOT test-gated convergence-semantics claims (the `eigsolve` distinction) — so the missing dedicated unit test does NOT gate them, and the entry is `firm` rather than `rough-in (test-coverage-bounded)`. (The per-step body's opaque-library internals ARE library-owned and untested-here, but the entry quantifies over the opaque step rather than asserting its semantics — the opacity is recorded at the lowering layer as `obstruction (opaque-library-ownership)`, not as an unconfirmed law here.)

**Scope (load-bearing)**: `fold_solve` (state-threaded fold) is witnessed by **transient + driven-PROM SweepAdaptive** (2-of-5 pipelines, the two state-threaded sweeps). The fold *spine* (carry-threading, operator-capture-once, opaque per-step body) is firm on both. The **`schedule-source` generalization** (fixed-list ⊂ state-generated) is recorded as a variant axis but the entry's firm signature surface is the `fixed-list` form (transient); the state-generated greedy form is the recorded generalization, with its dedicated-chapter question deferred to OQ `fold-solve-greedy-schedule-source-generalization`. Do NOT read this entry as claiming the greedy state-generated form as a separate firm combinator — it is the §3.7 carry form with a state-generated schedule, covered by the spine. The other three pipelines (electrostatic, magnetostatic, eigenmode): electrostatic + magnetostatic are the independent-**map** [`solve_family`](./solve_family.md) (NOT folds — no carry); eigenmode's eigen-iteration is opaque-library-owned (`L4/eigsolve.md`, not probed as a fold here).

This dispatch (cycle-058 D1, LEAD) is the **L4 fold-combinator firm-up** promoting the c057 D4 rough-in thread-opener (`L4/index.md:62,82`) to a firm full entry, per replace-and-propagate (the combinator is the entry; the two sweeps are specialization notes under it) and the RATIFIED batch-17 meta decision 2 (`fold_solve` is a §3.7 `iterate_while`-child, the fold-sibling of [`solve_family`](./solve_family.md)'s map; the §3.7 family is the shared parent — no third combinator). The L4>L3 dissolution theme (`L4-L3/fold-solve-time-step-dissolution`) is cycle-058 D2's job this same cycle; this entry records the rotation direction in-line but does not author the theme.

## L4 vs L3 distinction

- **L3**: value-threaded explicit outer sweep — a positional `for (int step = ...)` loop threading the persistent `sol` field-state in place, the operator construction hoisted outside the loop by hand (the Palace C++ shape), the per-step body a role-naming wrapper over the opaque MFEM integrator step. The fold combinator collapses to the explicit in-place-threading loop; the operator-capture-once is a coding convention (`TimeOperator` built outside the `for`), not a type-level stratification. The carry-threading sequential-obstruction + the opaque per-step body are both made explicit at L3 (the likely `partial-obstruction` status, the `chebyshev` shape).
- **L4**: the `foldl` combinator `fold_solve op s0 schedule = foldl (time_step_op op) s0 schedule`. The operator-capture-once is *structural* (`op : OpParams` `readonly`, bound once outside the fold); the carry-threading is *typed* (`TimeState` threaded forward, the step reads the prior carry); the map/fold axis is the §3.7 degenerate-vs-non-degenerate distinction. The L4>L3 dissolution erases the fold-combinator naming and the `readonly` typing, recovering the L3 explicit in-place-threading loop.

## Evidence

`fold_solve` at L4 is a methodology-level combinator distilled from two Palace driver sweeps; Palace's C++ does not realise the L4 fold form (it writes the explicit in-place-threading loop). All L0 citations self-verified against on-disk source this dispatch via `tools/citecheck/citecheck.py --anchor` (the codemap `read_range` +1 drift was caught and corrected on `transientsolver.cpp:35`, `timeoperator.cpp:312`, `drivensolver.cpp:241`, `:398`, `transientsolver.cpp:98`).

- **Fold witness 1 — transient fixed-schedule march (positive):**
  - `palace/drivers/transientsolver.cpp:33` (`TimeOperator time_op(iodata, space_op, dJdt_coef)` — operator built once, outside the loop), `:34` (`delta_t = iodata.solver.transient.delta_t` declaration; the uniform step is read at `:35`), `:35` (`delta_t` value — uniform timestep), `:36` (`n_step = config::GetNumSteps(0.0, iodata.solver.transient.max_t, delta_t)` — fixed schedule length), `:77` (`for (int step = 0; step < n_step; step++)` — the fold loop), `:89` (`time_op.Init()` — the seed `s0` / initial field-state), `:93` (`time_op.Step(t, delta_t)` — the per-step fold body call), `:98` (`time_op.GetE()` per-step readout), `:99` (`time_op.GetB()` per-step readout).
  - `palace/models/timeoperator.cpp:312` (`op = std::make_unique<TimeDependentFirstOrderOperator>(...)` — the per-step operator constructed once), `:410` (`ode->Step(sol, t, dt)` — the opaque MFEM `ODESolver` step advancing the persistent `sol` field-state in place; the prior step's `sol` is the next step's input — the genuine `foldl`).
- **Fold witness 2 — driven-PROM SweepAdaptive state-generated greedy march (positive, state-generated-schedule variant):**
  - `palace/drivers/drivensolver.cpp:73` (`adaptive ? SweepAdaptive(space_op) : SweepUniform(space_op)` — the adaptive dispatch), `:231` (`ErrorIndicator DrivenSolver::SweepAdaptive(SpaceOperator &space_op) const` — the greedy march entry), `:241` (`offline_tol = iodata.solver.driven.adaptive_tol`), `:242` (`convergence_memory = iodata.solver.driven.adaptive_memory`), `:243` (`max_size_per_excitation = iodata.solver.driven.adaptive_max_size`), `:244` (`nprom_indices = iodata.solver.driven.prom_indices.size()` — the PROM greedy params), `:384` (`for (std::size_t it0 = it; it < max_size_per_excitation && memory < convergence_memory; it++)` — the state-derived loop bound), `:389` (`omega_star = prom_op.FindMaxError(excitation_idx)[0]` — the state-derived per-step input: next sample = argmax of current PROM error), `:398` (`memory = max_errors.back() < offline_tol ? memory + 1 : 0` — the state-derived termination counter update from per-step error).
- **Fold witness 3 — AMR Solve→Estimate→Mark→Refine loop (positive, 2nd state-generated-schedule variant):**
  - `palace/drivers/basesolver.cpp:153` (`void BaseSolver::SolveEstimateMarkRefine(std::vector<std::unique_ptr<Mesh>> &mesh) const` — the AMR march entry, range `:153-276`), `:174` (`auto [indicators, ntdof] = Solve(mesh)` — the seed solve + error estimate), `:175` (`double err = indicators.Norml2(comm)` — the seed error-indicator state), `:182` (`ret |= (it >= refinement.max_it)` — the iteration resource cap), `:184` (`ret |= (refinement.max_size > 0 && ntdof > refinement.max_size)` — the DOF resource cap), `:190` (`while (use_amr && !ExhaustedResources(it, ntdof) && err >= refinement.tol)` — the state-derived loop bound: the genuine termination is the error indicator `err >= refinement.tol`), `:239` (`fine_mesh.GeneralRefinement(marked_elements, -1, refinement.max_nc_levels)` — the state-derived per-step input: the refined mesh generated from the Dörfler-marked error-indicator state), `:266` (`std::tie(indicators, ntdof) = Solve(mesh)` — the per-step solve advancing the carry), `:267` (`err = indicators.Norml2(comm)` — the recomputed error indicator that re-derives the loop bound each iteration). Each step reads the prior carry (mesh + error indicators) and produces the next (refined mesh + new solve) — the genuine state-generated fold. The per-step body bottoms out in the opaque `Solve(mesh)` driver call. (AMR currently unsupported for transient, `:157-162`.)
- **Map contrast-sibling (negative for fold):**
  - [`solve_family`](./solve_family.md) (electrostatic + magnetostatic) is the independent-**map** sibling — no carry between elements; the elements commute. Cited for the map/fold spine distinction, not as fold evidence.
- **Firm vocabulary grounding:**
  - `book/src/design/l4_calculus.md:150-184` (§3.7 `iterate_while` + `iterate_while_pure` sugar — the family the non-degenerate carry-threading fold joins; the shared parent of map + fold), `:186-228` (§3.8 demand-pruning — intermediate-state materialization).
  - `book/src/L4/iterate-while.md:9` (already names "transient time-stepping" among the algorithms that reduce to `iterate_while`-folds — the anchor `fold_solve` realizes).
  - `book/src/L4/solve_family.md` (the map sibling whose structure this entry is parallel to — operator-capture-once law 2, §3.7-family reuse, combinator-as-entry-with-specialization-notes).
  - `book/src/L4/index.md:62` (the c057 D4 thread-opener bullet establishing fold-vs-map=fold for transient), `:82` (the rough-in dep-map row promoted by this dispatch), `:37` (the `chebyshev` precedent for reusing the iterate-while family).
  - `book/src/concepts/state-stratification.md` (the `readonly` operator-stratum-captured-once + persistent-carry typing), `book/src/concepts/sequential-obstruction.md` (the carry-threading + opaque per-step obstructions).
- **No dedicated test** exercises the `Solve(mesh)` time-march or the SweepAdaptive greedy loop (the drivers are integration-level, not unit-tested under `reference/palace/test/unit/`); the L0 evidence is the driver source above. This does NOT gate the fold-spine laws (they are read-off syntactic identities on the positive loops — the firm-on-positive-structure escape), so the entry is `firm`.
- **Provenance**: thread-opened by `reports/2026-06-02T025700Z-abstractor-fold-solve-transient-thread-opener/CYCLE.md` (c057 D4; the rough-in sketch, 1 witness); 2nd fold-witness (SweepAdaptive) confirmed by the cycle-056 D1 PROM candidate; landed as the rough-in dep-map row `book/src/L4/index.md:82`; firmed (to `firm` on the firm-on-positive-structure escape, fold spine on 2 witnesses) by this dispatch (cycle-058 D1, LEAD).
