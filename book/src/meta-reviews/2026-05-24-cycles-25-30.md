# Meta-review 2026-05-24 — cycles 25–30

(Ninth meta-review. First batch under the depth-6 cadence — meta-review fires every 6 cycles, not 3.)

**Window:** cycles 25–30.
**Trigger:** automatic.
**Status:** enacted.

## Context

Big substance — **GMRES reached L3** (cycle 30) and crossed the Phase 6 finish line on its 4th DONE criterion before this meta-review. SIDEWAYS dispatch contract from meta-8 worked end-to-end (cycle 25's planner output correctly populated `comparison_slices=['gmres', 'orthog']`). Two new concepts landed (`spectrum_estimate`, `gmres` concept) plus inline content advances across divfree, chebyshev. But four of six cycles were auto-downgraded — every one of those downgrades was a *bookkeeping-only* failure: substantive content landed cleanly; only the `spec/index.md` status-table row update failed.

The methodology now needs to distinguish *content writes failed* from *bookkeeping writes failed* — they are not equivalent for verdict purposes, and treating them as such hides real progress.

## Cycles covered

- **Cycle 25** — `sideways gmres,orthog`. Critic PASS, orchestrator downgraded (apply failure). The dispatch contract worked (comparison_slices populated) but the Synthesizer emitted `slice_writes mode=create` on already-existing `gmres.md`, `orthog.md`, and `concept_writes mode=create` on already-existing `orthogonalization` — the meta-7 channel-selection rule existed for forward pushes but did not generalize to SIDEWAYS.
- **Cycle 26** — `forward divfree [L1→L2]`. Critic PASS, downgraded on `spec/index.md` file_edits anchor mismatch.
- **Cycle 27** — `back divfree`. PASS (restores).
- **Cycle 28** — `forward chebyshev [L1→L2]`. Critic PASS, downgraded on `spec/index.md` file_edits anchor mismatch.
- **Cycle 29** — `back chebyshev`. PASS (restores).
- **Cycle 30** — `forward gmres [L2→L3]`. Critic PASS, downgraded on `spec/index.md` file_edits anchor mismatch. **The substantive L3 rotation landed**: GMRES outer-loop sequentiality recorded as obstruction (negative L3 per Phase 6 charter), per-step body lifted to global tensor-field operation (positive L3).

Token usage: ~225k input / ~40k output across cycles 25-30.

## Friction summary

### LOW (1)
- **SIDEWAYS channel-selection guidance missing.** Cycle 25's Synthesizer reached for `mode=create` on existing files because the prompt's channel-selection rule was scoped to forward pushes; SIDEWAYS-specific guidance was absent.

### MEDIUM (3)
1. **`file_edits` anchor brittleness on `spec/index.md`.** Cycles 26/28/30 all failed the same way: `old_string` didn't match the live row. The Synthesizer was guessing the row from context rather than copying it verbatim. Recurrence #1 of a new failure mode; producer-side strengthening per recurrence protocol.
2. **Downgrade rule can't distinguish content from bookkeeping failures.** Cycles 25/26/28/30 all had Critic verdict pass with substantive content landed cleanly — only the `spec/index.md` bookkeeping row failed. The meta-5 downgrade rule was designed for substantive content failures (corrupt diffs producing no on-disk content). The current recurrence is a different category; treating them identically buries genuine progress signal.
3. **SIDEWAYS output discipline absent.** Meta-8 fixed the dispatch contract (Planner names ≥2 slices); the *output* contract for what a SIDEWAYS cycle should produce was never specified. Cycle 25 produced a stub.

### HIGH (0)
None.

## Plan as enacted

| Item | Source friction | Enacted change | Deviation from proposal |
|---|---|---|---|
| (LOW) SIDEWAYS channel-selection guidance | Cycle 25 mode=create on existing files | `prompts/synthesizer.md` channel-selection rule extended with explicit SIDEWAYS guidance: prefer `section_appends` on both compared slices; concept-consolidation uses `append-section`. | None |
| (1) `file_edits` anchor verification | Cycles 26/28/30 anchor mismatches | `prompts/synthesizer.md` new "`file_edits` anchor verification" subsection. Synthesizer must copy `spec/index.md` row content verbatim from the live file; the row anchored on slice slug is uniquely identifying; fall back to omitting the bookkeeping write if anchor cannot be confirmed. | None |
| (2) Refined downgrade rule | Cycles 25/26/28/30 bookkeeping-only downgrades | `orchestrator/loop.py`: integrator now returns a dict `{apply_failed, substantive_landed, failed_paths, bookkeeping_only_failure}`. Verdict-downgrade refined: pass + apply_failed + bookkeeping_only_failure → hold pass + set `bookkeeping_incomplete` flag (recorded in episodic for the Meta-Critic). pass + apply_failed + ANY substantive failure → downgrade as before. Bookkeeping paths defined as `book/src/spec/index.md`, `book/src/meta-reviews/index.md`, `book/src/SUMMARY.md`. | None |
| (3) SIDEWAYS output discipline | Cycle 25 stub output | `prompts/synthesizer.md` new "SIDEWAYS output discipline" subsection. Typical shape: `section_appends` on BOTH compared slices ("## Cross-slice: comparison with <other>"), `concept_writes` for shared primitives (append-section if exists, create if new), optional dep-map edges. SIDEWAYS does NOT emit `slice_writes mode=create` for the compared slices. Allows mode=diff with justification in `log_synthesis`. | None |

## Recurring patterns and the watch list

- **Diff-apply mismatch on `spec/index.md` row updates** — recurrence #1 closed by producer-side rule (anchor verification). If cycles 31-36 still fail this way (recurrence #2), escalate to dedicated `slice_index_update` channel (mechanical row-edit by slug).
- **Pass-with-bookkeeping-failure** — now distinguished from pass-with-content-failure via `bookkeeping_incomplete` flag. The Meta-Critic should track accumulated bookkeeping debt; if it exceeds ~5 across a window, that's the trigger for the channel intervention.
- **SIDEWAYS** — dispatch fixed (meta-8), output discipline fixed (meta-9). Next SIDEWAYS cycle should produce substantive cross-slice comparison content.
- **Exercised_checks structured field** — meta-8 plan item 2; episodic captures from cycles 25-30 don't show evidence the field is consistently populated. Hold this on the watch list — needs verification next batch, not yet a recurrence.
- **Anti-grind, anti-procrastination, file_edits non-index, file_creates** — STILL CLEAR.

## Phase 6 DONE scoreboard

| Criterion | Status |
|---|---|
| GMRES pushed to L4 | partial: L1 ✓, L2 ✓, L3 ✓ (cycle 30, negative outer-loop + positive per-step body); L4 pending |
| First 3 rotation_claims hand-verify | possible (across cg/orthog/divfree/chebyshev/gmres) |
| ≥1 BACK push | ✓ |
| ≥1 SIDEWAYS push | ✓ (multiple now: cycle 22 stub, cycle 25 substantive-pending-re-emit) |
| Meta-review fires | ✓ × 9 |

GMRES at L3 with the expected character (negative outer-loop + positive per-step lift) was the most substantive Phase 6 milestone since L1 landing. L4 remaining.

## Approval

Approved by human; enactment by Claude Code with no substantive deviations.
