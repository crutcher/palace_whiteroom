# Meta-review 2026-05-24 — cycles 22–24

(Eighth meta-review.)

**Window:** cycles 22–24.
**Trigger:** automatic.
**Status:** enacted.

## Context

The three-cycle batch that confirmed everything from meta-reviews #6 and #7 actually works end-to-end:

- **SIDEWAYS fired** (cycle 22) — anti-procrastination clause engaged, Planner explicitly cited "SIDEWAYS has not fired in 21 cycles despite multiple eligible pairs."
- **gmres L1→L2 re-emitted cleanly** (cycle 24) via the new `section_appends` channel. Cycle 21's beautiful-but-unapplied L2 prose now on disk.
- **All three cycles passed** with no auto-downgrades. The file_edits + section_appends + mermaid auto-init triad covers the edit topology surface.
- **orthog reached L2** (cycle 23) — second slice past L1, plus the first concrete substitutable-primitive vocabulary check (MGS/CGS/CGS2 variants absorbed cleanly).

Three plan items surface from this batch (all MEDIUM). They're refinements, not architecture changes — the methodology is converging.

## Cycles covered

- **Cycle 22** — `sideways orthog,chebyshev`. PASS. But emitted `slice='unknown'` because the Planner output's `slices=a,b` field was parsed as an opaque string and not exposed as comparison_slices. The cycle's own lesson noted the defect ("dispatch-layer guard so these cycles short-circuit before reaching synthesis"). Synthesis produced a minimal-stub output — the SIDEWAYS dispatch contract needs hardening.
- **Cycle 23** — `forward orthog [L1→L2]`. PASS. Vocabulary check across MGS/CGS/CGS2 variants exercised the variant-absorption methodology concept cleanly.
- **Cycle 24** — `forward gmres [L1→L2]`. PASS. Cycle 21's prose re-emitted via `section_appends`. Four new concept_writes (some create-on-existing failed silently — see plan item LOW), five dep-map edges (all applied because mermaid auto-init from meta-review #7 fired), two lessons including the in-place-mutation pseudocode discipline now codified as plan item 3.

Token usage: ~95k input / ~25k output across cycles 22-24.

## Friction summary

### LOW (1)
- **concept_writes create-on-existing silent skip.** Cycle 24's Synthesizer used `mode=create` on existing concept pages (nrm2, dot). The orchestrator's `create_concept_file` returns False (silent no-op), so the writes were lost without an apply-failure signal. Existing channel-selection rule from meta-review #6 covers slices/files but didn't generalize to concepts.

### MEDIUM (3)
1. **SIDEWAYS dispatch contract.** Cycle 22 fired SIDEWAYS but with `slice='unknown'` (parser defect: `slices=a,b` ignored). The orchestrator must enforce a precondition: ≥2 named slices that exist on disk.
2. **Critic exercised_checks not consistently emitted.** Meta-review #6 introduced exercised-checks as a prose-only convention; cycles 19-24 had pass verdicts without exercised-check annotations. Promote to a structured `exercised_checks` field on the verdict envelope so it's reliably present.
3. **Mutation pseudocode discipline.** Cycle 24's lesson: in L2 pseudocode with in-place primitives, use `t ← copy(x)` explicitly; raw `t = x` followed by mutation is ambiguous to readers. Promote from lessons.md to Synthesizer prompt — L2 unfoldings of other Krylov solvers (CG, eigensolvers) will hit the same pattern.

### HIGH (0)
None.

## Plan as enacted

| Item | Source friction | Enacted change | Deviation from proposal |
|---|---|---|---|
| (LOW) concept_writes existence check | Cycle 24 silent skip on nrm2/dot | `prompts/synthesizer.md` channel-selection rule extended: before `mode=create` on a concept, verify the page doesn't exist; use `mode=append-section` if it does. | None |
| (1) SIDEWAYS dispatch contract | Cycle 22 slice='unknown' | (a) `prompts/planner.md` criterion 4: SIDEWAYS directive must name ≥2 slices in `slices=a,b` plus comparison-axis in `reason=`. (b) `orchestrator/roles.py` `parse_push_line`: for kind=sideways, parse `slices=` into `comparison_slices` list. (c) `orchestrator/loop.py`: precondition — if comparison_slices has <2 entries OR a named slice doesn't exist on disk, the cycle is converted to an `escalate` rather than passed to the Synthesizer (preventing degenerate stubs). | None |
| (2) Critic exercised_checks structured field | Meta-7 watch escalation; cycles 19-24 had no annotations | (a) `schemas/critic_verdict.json`: new `exercised_checks` array field, each entry `{check: 1-11, outcome: exercised\|trivially_carried\|not_applicable, note?: string}`. REQUIRED on pass verdicts. (b) `prompts/critic.md`: enumeration section rewritten to require the structured field; all 11 checks should appear on pass verdicts with explicit outcomes. | None |
| (3) Mutation pseudocode discipline | Cycle 24 lesson | `prompts/synthesizer.md`: new "Mutation pseudocode discipline" subsection under Output discipline. Codifies the `t ← copy(x)` rule with acceptable/unacceptable forms and the cycle-24 example. | None |

## Recurring patterns and the watch list

- **Diff-apply mismatch** — STILL CLOSED across three sub-modes (file_creates / file_edits / section_appends).
- **SIDEWAYS dispatch defect** — NEW; addressed in plan item 1 this round.
- **Critic exercised-checks** — escalation watch from meta-7 triggered; promoted to structured field this round.
- **In-place mutation pseudocode** — predicted to recur across Krylov L2 unfoldings; promoted to prompt this round.
- **Anti-procrastination working as designed** — cycle 22 fired SIDEWAYS exactly per the meta-7 clause.
- **Concept extraction working** — cycle 24's `i_2, dependencies on the support-concept vocabulary (incremental_least_squares, apply_linop, axpy, dot, nrm2, scal, givens, trsv) all integrate cleanly.

## Phase 6 DONE scoreboard

| Criterion | Status |
|---|---|
| GMRES pushed to L4 | partial: L1 ✓ (cycle 20), L2 ✓ (cycle 24); L3, L4 still to do |
| First 3 rotation_claims hand-verify | possible; claims on disk in cg/orthog/divfree/chebyshev/gmres |
| ≥1 BACK push | ✓ |
| ≥1 SIDEWAYS push | ✓ (cycle 22) |
| Meta-review fires | ✓✓✓✓✓✓✓✓ (8 times) |

Phase 6 is substantially complete. GMRES at L4 is the remaining concrete deliverable; the methodology infrastructure is robust enough to drive it.

## Approval

Approved by human; enactment by Claude Code with no substantive deviations.
