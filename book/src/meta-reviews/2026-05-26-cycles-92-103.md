# Meta-review 2026-05-26 — cycles 92–103

(Twentieth meta-review. First under depth-12 cadence.)

**Window:** cycles 92–103.
**Trigger:** automatic.
**Status:** enacted.

## Context

The big substantive result of this window: **arnoldi_step landed at L4 in cycles 98–102** — the **first intermediate-tier algorithm** extracted. The Planner picked the highest-impact intermediate per the meta-18 impact-ranking heuristic (`|concepts| × |slices_that_reuse| × (1/cycles_to_extract)`), validating the user 2026-05-25 directive on intermediate-tier prioritization end-to-end. Cycle 102 emitted a `forward arnoldi_step [L0→L0]` self-tightening — slightly unusual edge label but PASS.

But cycles 92–97 (the six before arnoldi_step) were **all retroactive_claims** across cg/gmres/orthog — the loop rotated through slices but stayed in backfill mode for half the window. The per-slice retroactive-budget gate didn't fire because each slice had ≤2 consecutive. A **global** retroactive counter is the missing complement.

Plus: bookkeeping_incomplete fired 4 times because `slice_index_updates` channel couldn't find a row to rewrite for the new `arnoldi_step` slice — append-by-slug fallback is the fix.

## Cycles covered

- **Cycle 92** — `forward gmres L1→L2` retroactive_claims. PASS.
- **Cycle 93** — `forward gmres L2→L3` retroactive_claims. PASS.
- **Cycle 94** — `forward orthog L1→L2` retroactive_claims. PASS.
- **Cycle 95** — `forward gmres L2→L3` retroactive_claims. PASS.
- **Cycle 96** — `forward cg L3→L4` retroactive_claims. PASS.
- **Cycle 97** — `forward cg L3→L4` retroactive_claims. PASS.
- **Cycle 98** — `forward arnoldi_step L0→L1`. PASS. **First intermediate-tier slice created.**
- **Cycle 99** — `forward arnoldi_step L1→L2`. REVISE.
- **Cycle 100** — `forward arnoldi_step L1→L2`. PASS.
- **Cycle 101** — `forward arnoldi_step L2→L3`. PASS.
- **Cycle 102** — `forward arnoldi_step L3→L4`. PASS. **arnoldi_step complete to L4.**
- **Cycle 103** — `forward arnoldi_step L0→L0` (self-tightening). PASS.

Token usage: ~430k input / ~95k output. Six cycles wasted on retroactive backfill before the Planner picked arnoldi_step.

## Friction summary

### LOW (2)
- Problems-sensitivity calibration: 0/12 = 0.000 actual rate vs 0.067 target. Below 0.5× target → increment to 4. (Enacted.)
- Roadmap update: arnoldi_step `[ ]` → `[x]`. (Enacted.)

### MEDIUM (3)
1. **slice_index_update needs append-by-slug fallback.** Cycles 98–102 each emitted `bookkeeping_incomplete` because `arnoldi_step` had no row in the slice index table to rewrite. The meta-10 `slice_index_updates` channel only handled row-rewriting; new slices need a path.
2. **Global retroactive counter.** Cycles 92–97 = 6 consecutive retroactive cycles across 3 slices. Per-slice gate (meta-18) didn't fire because each slice had ≤2 consecutive. The pattern (rotate-through-slices-via-retroactives) is the same anti-forward-progress signal the per-slice gate was designed to catch.
3. **Skill_uptake field unobserved through 12 cycles.** Meta-19 added the structured field; cycles 92–103 produced zero non-empty `skill_uptake` arrays in episodic. Diagnostic needed: is the Critic emitting it and the orchestrator dropping it, or is the Critic not emitting at all?

### HIGH (0)
None.

## Plan as enacted

| Item | Source friction | Enacted change | Deviation |
|---|---|---|---|
| (LOW1) Problems-sensitivity 3→4 | rate 0/12 below 0.5× target | `scaffolding/problems-sensitivity.md` `sensitivity:` updated, calibration history row appended. | None |
| (LOW2) Roadmap arnoldi_step → done | cycles 98–102 landed L0→L4 | `scaffolding/roadmap.md` *Intermediate-tier algorithms*: `[ ] Arnoldi step` → `[x] ... Landed at L4 via cycles 98-102 — first intermediate-tier algorithm extracted.` | None |
| (1) `append_slice_index_row` helper + integrator fallback | bookkeeping_incomplete × 5 on arnoldi_step | (a) `orchestrator/state.py`: new `append_slice_index_row(slice, layer, date, summary, link_title?)` helper finds the status-table boundaries via regex, appends a new row at the end of the rows block. Idempotent — returns False if the slug-anchor row already exists. (b) `orchestrator/loop.py` integrator: when `update_slice_index_row` raises `FileNotFoundError` (slug not found), call `append_slice_index_row` as a fallback. Push-back signal records the append for Meta-Critic visibility. | None |
| (2) Global retroactive counter | cycles 92-97 6-consec global, per-slice gate missed | (a) `prompts/planner.md`: documents the co-equal global counter (`consecutive_retroactive_global`) alongside `consecutive_retroactive_on_slice`. Threshold 4 (vs 3 for per-slice — slightly looser because rotating through slices is healthier than stuck-on-one-slice). (b) `orchestrator/loop.py`: scans recent episodic for consecutive retroactives across slices, resets on substantive forward content. Triggers the meta-19 retry-once mechanism with a "global vs per-slice" gate-reason. | None |
| (3) Skill_uptake emission diagnostic | 12 cycles, zero observation | (a) `orchestrator/loop.py`: episodic now records `skill_uptake_emitted: bool` (True iff the Critic verdict carried a list). (b) `prompts/critic.md` check #15: restated with explicit MUST-appear language + minimal-form example showing all-`triggered:false` entries. The next window's episodic will show whether the Critic's emission is the gap. | None |

## Recurring patterns and the watch list

- **Intermediate-tier prioritization working** — meta-18 directive landed an arnoldi_step extraction within 1 window. Next intermediate-tier candidate (per impact rank): plane-rotation stream or polynomial-recurrence step. Watch which the Planner picks.
- **Per-slice retroactive gate** held through this window (no auto-escalates); global counter catches the cross-slice case.
- **Skill_uptake invisible** — diagnostic instrumentation added; if the next window still shows `skill_uptake_emitted: false` everywhere, the issue is Critic-side prompt compliance rather than orchestrator plumbing.
- **Problems-sensitivity** — now at 4 (eager). Watch whether problem filings appear in the next 12-cycle window; recalibration runs again at meta-21.

## Phase 6+ status

- Per-solver pipelines: 0/5 (unchanged).
- Krylov solvers: 2/4 at L4 (unchanged).
- Orthogonalisation: 1/2 (unchanged).
- Smoothers: 1/6 (unchanged).
- Projections: 0/2 at L4 (divfree unchanged at L3).
- **Intermediate-tier algorithms: 1/7 extracted (arnoldi_step ✓).**
- FE assembly: 0/4 (unchanged).
- Coordination/post-processing: 0/4 (unchanged).
- Methodology: 6 roles; 15 Critic checks; **3 skills**; **6 active slices** (cg, gmres, orthog, chebyshev, divfree, **arnoldi_step**); ~25 concepts.
- Phase progression: Phases 0-6 done; Phase 6+ in flight.

## Approval

Approved by human (auto-approval mode); enactment by Claude Code. The arnoldi_step landing validates the intermediate-tier directive; the global retroactive counter complements the per-slice gate; sensitivity bumped to 4 to encourage problem filings.
