# Meta-review 2026-05-25 — cycles 37–43

(Eleventh meta-review. First with skill extraction.)

**Window:** cycles 37–43.
**Trigger:** automatic (retry — initial Meta-Critic emission had a JSON syntax error; cycles 37-42 + cycle 43 = 7 cycles since last meta).
**Status:** enacted.

## Context

The window after meta-10 enacted three big-arch changes: `slice_index_updates` channel, `plan_kind` classification, and the Planner self-tightening heuristic. All three worked end-to-end in this window:

- **Cycle 37** — Planner self-tightening fired: chose `forward gmres L4→L4` to consume cycle-35's deferred Outcome ADT friction. Heuristic working as designed.
- **Cycles 38/39** — divfree L1→L2 and chebyshev L1→L2 advanced via clean section_appends.
- **Cycle 40** — SIDEWAYS orthog/chebyshev. The dispatch contract from meta-8 worked; the *output* discipline from meta-9 was ignored (mode=create on existing files).
- **Cycle 41** — back orthog L1 — surfaced a real methodology gap (Householder structurally-distinct variant).
- **Cycle 42** — back orthog L2 — disclosed primitive-sequence divergence per variant-absorption discipline.
- **Cycle 43** — forward gmres L2→L3 (retroactive: L3 already on disk from earlier cycles; this emitted rotation_claims).

Five of seven cycles were `plan_kind = retroactive_claims`. The meta-10 classification surfaced this as a distinct cycle class; meta-11 plan addresses the auditability gap retroactive cycles create.

**Skill extraction trigger fired.** The Meta-Critic prompt directive from earlier this session (skill extraction as a meta-pass consideration) produced its first output: `classify-variant-axis` is the first skill the loop has emitted, derived from the variant-absorption methodology accumulating across three meta-reviews.

## Cycles covered

- **Cycle 37** — `forward gmres L4→L4` (self-tightening). PASS.
- **Cycle 38** — `forward divfree L1→L2`. PASS (retroactive after some content already from cycle 33).
- **Cycle 39** — `forward chebyshev L1→L2`. PASS (retroactive_claims with 0 substantive writes).
- **Cycle 40** — `sideways orthog,chebyshev`. REVISE — mode=create on existing files.
- **Cycle 41** — `back orthog L1`. REVISE — surfaced Householder methodology gap.
- **Cycle 42** — `back orthog L2`. PASS — primitive-sequence divergence disclosed.
- **Cycle 43** — `forward gmres L2→L3` (retroactive). PASS.

Token usage: ~225k input / ~50k output. Initial meta-critic emission failed JSON parse (line 56 syntax error in an embedded literal); retry produced valid JSON.

## Friction summary

### LOW (1)
- **SIDEWAYS channel-selection recurrence #3.** Cycle 40 used `mode=create` on already-existing `orthog.md` and the `orthogonalization` concept page. Cycles 22/25/40 — same Synthesizer-side defect across three independent SIDEWAYS attempts. Rule exists; gate didn't fire.

### MEDIUM (3)
1. **Retroactive_claims cycles unauditable.** 5 of 7 cycles this window were `retroactive_claims`; 4 produced 0 substantive writes. The Critic verdicts these by trusting prior cycles — there's no diff content to audit against. Plan_kind classifies in episodic; doesn't change Critic's information access.
2. **Structurally-distinct variants** — recurring methodology gap. Cycle 7 (FGMRES per-step), cycle 40 (Householder); lesson predicts curl-curl projector. Pattern: one variant in an otherwise-uniform family has fundamentally distinct threaded state, breaking level-(c) primitive-sequence absorption. Two resolution paths: residual-axis disclosure OR sibling-slice split. Not yet documented in `variant-absorption.md`.
3. **Skill-extraction candidate: `classify-variant-axis`.** Variant-absorption methodology has accumulated three meta-reviews of refinement (levels in meta-3, constructed-operator limits in meta-5, structurally-distinct now in meta-11). The procedure "classify each axis-value as constructed-operator / parametric / scope-out / residual-axis" is procedural, recurs on every multi-variant slice, and is stable enough for skill extraction. **First skill ever proposed by the loop.**

### HIGH (0)
None.

## Plan as enacted

| Item | Source friction | Enacted change | Deviation |
|---|---|---|---|
| (LOW) SIDEWAYS channel-selection gate | Cycle 40 mode=create recurrence #3 | `prompts/synthesizer.md` SIDEWAYS subsection: pre-emit verification gate — every slice_writes/concept_writes entry on an already-existing target must use append/edit channels, never mode=create. Phrased as an explicit gate the Synthesizer hits before plan emission. | None |
| (1) Retroactive-claims quoted prose | Retroactive cycles unauditable | (a) `prompts/synthesizer.md`: `retroactive_claims` plans MUST include a `retroactive_claim_evidence` block in `log_synthesis` quoting on-disk prose per claim — local lines, not the entire section. (b) `prompts/critic.md`: new check #12 — retroactive_claims without the quoted-prose block downgrade to revise (analogue of check #1 citation_does_not_support but for on-disk content). | None |
| (2) Structurally-distinct variants documented | Cycle 40 + cycle 7 + predicted curl-curl | `book/src/concepts/variant-absorption.md` new section "Structurally-distinct variants in otherwise-uniform families" with canonical examples (Householder, FGMRES), two fix paths (residual-axis disclosure / sibling-slice split), and choice criterion. Cross-referenced from `constructed-operators.md` (already-existing Limits section). | None |
| (3) **NEW SKILL: `classify-variant-axis`** | Methodology procedure recurring on every multi-variant slice | `skills/classify-variant-axis/SKILL.md` created with full procedure: four absorption paths, decision criterion (which level a/b/c breaks), output format for slice `## Working Notes`. `prompts/synthesizer.md` Variant axis subsection rewritten as a skill-invocation reference with a 2-line summary kept inline for reading speed. | None |
| (bonus) `book.toml` traverse-parent-directories | Citation-link convention requires it | `book/book.toml` linkcheck2: `traverse-parent-directories = true`. Links from book pages to `../../skills/` and `../../../../reference/` are legitimate per the citation/skill conventions; the previous setting blocked them. | Not in plan — required to make the methodology changes buildable. |

## Recurring patterns and the watch list

- **SIDEWAYS channel-selection** — recurrence #3 of the same Synthesizer-side defect (mode=create on existing files). If recurrence #4 fires (cycle 44+), escalate to a Critic-side check or to a tooling intervention (e.g., orchestrator pre-validation that rejects mode=create on existing slice paths within the SIDEWAYS code path).
- **Skill uptake** — the first skill exists; the question now is whether Synthesizer cycles actually consult it. Watch: variant-classification fidelity in cycles 44+. If the inline summary alone is what gets used and the skill body is ignored, the skill-extraction approach needs revisiting.
- **Background sections** — the user-directed Background-section rule landed in commit 07ab8a0. Watch whether cycles 44+ actually emit Background sections on newly-touched slices.
- **Citation links** — same; clickable-link convention landed in 07ab8a0. Watch whether new prose actually uses the link form.
- **Meta-Critic JSON robustness** — the initial emission this round had a syntax error. If recurrence #2, look at parser tolerance (extracting partial JSON from malformed output) or at the Meta-Critic prompt's emphasis on strict-JSON.

## Phase 6+ status

5 active slices, 22 concepts, GMRES at L4. Phase 6 DONE-criteria all met since meta-10. This window's substantive on-disk advances: chebyshev L2 (cycle 39 emitted; cycle 38 divfree L2 emitted; cycle 42 disclosed Householder/MGS-family residual axis at orthog L2; cycle 37 tightened GMRES L4). The methodology now produces consistent forward progress without architectural surprises.

## Approval

Approved by human (auto-approval mode authorized at meta-10 enactment); enactment by Claude Code with one bonus fix (book.toml traverse-parent-directories) required to make the citation-link convention buildable.
