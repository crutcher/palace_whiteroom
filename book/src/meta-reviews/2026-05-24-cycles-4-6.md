# Meta-review 2026-05-24 — cycles 4–6

(Filename suffix `-cycles-4-6` disambiguates from the same-day meta-review #1 at `2026-05-24.md` which covered cycles 1–3.)

**Window:** cycles 4–6.
**Trigger:** automatic (every 3 cycles per `config.toml`).
**Status:** enacted.

## Context

Second meta-review of the loop's life. Window covered three cycles all targeting the GMRES slice — one FORWARD (cycle 4) and two BACK (cycles 5, 6). First BACK pushes of the loop ever, both driven by Critic friction surfaced in cycle 4. The first SIDEWAYS push has not happened yet.

This meta-review fired automatically and the Meta-Critic correctly used the project's actual file paths (`prompts/<role>.md`, `book/src/concepts/...`) — the file-path drift flagged in `2026-05-24.md` *Recurring patterns to watch* did NOT recur. One watch item cleared.

The Meta-Critic also correctly invoked the recurrence-detection protocol: cycle 4 reproduced the rename-not-rotation anti-pattern from cycle 3 (recurrence #1 of a resolved problem), and the plan addresses producer-side capture rather than just strengthening consumer-side detection.

## Cycles covered

- **Cycle 4** — `forward gmres [L1→L2] → revise`. 4 rotation_claims emitted (the rotation_claim coverage rule from meta-review #1 worked). Critic correctly applied check #8 and cited `rotation.md`, flagging the rotation as renaming despite claim coverage. Also flagged claim 2 for conflating two primitives (trsv_upper + gemv) and equivocating on canonical form.
- **Cycle 5** — `back gmres → revise`. **First BACK push of the loop.** Planner responded to cycle 4's friction by pushing back on L1 rather than retrying L2 forward. Critic verdict revise; flagged procedure fusion (Arnoldi + Givens + residual monitor compressed into a single numbered list).
- **Cycle 6** — `back gmres → revise`. Second BACK push. Sophisticated Critic verdict: acknowledged L0→L1 **does** satisfy criterion (a) state-hiding (so it IS a genuine rotation), and flagged FGMRES-as-bolted-on as a SEPARATE quality issue worthy of its own check.

Token usage: ~95k input / ~22k output across cycles 4–6.

## Friction summary

### LOW (0)
None.

### MEDIUM (4 as proposed; enacted with adjustments noted below)
1. **Rotation_claim granularity unspecified** — cycle 4 emitted a single claim covering two distinct BLAS primitives (`trsv_upper` and `gemv`) with equivocation on canonical form. Producer-side rule for granularity + canonicalization missing.
2. **Rotation-as-renaming recurrence (cycle 4 = recurrence #1 of cycle 3 problem)** — meta-review #1's `rotation.md` + Critic check #8 catches the failure at consumer side, but Synthesizer has no producer-side pre-emit check to prevent emission.
3. **Variant-handling and procedure-fusion lessons unpromoted** — lessons.md entries from meta-review #1 remained as terse one-liners; cycle 5+6 surfaced them as friction without producer-side capture.
4. **No concept page for variant absorption** — load-bearing for criterion (1) state-hiding; deserves a peer concept to `rotation.md`.

### HIGH (0)
None.

## Plan as proposed

See `meta-review-pending.md` (cleared on enactment). Full plan JSON preserved in commit message of this enactment.

## Plan as enacted

| Item | Source friction | Enacted change | Deviation from proposal |
|---|---|---|---|
| (1) Claim granularity + canonicalization | Cycle 4 trsv/gemv conflation | New "Claim granularity and canonicalization" subsection in `prompts/synthesizer.md` *Output discipline* | None |
| (2) Rotation self-check (pre-emit) | Cycle 4 renaming recurrence | New "Rotation self-check (pre-emit)" subsection in `prompts/synthesizer.md` *Output discipline* | **With carry-through allowance.** The original proposal required the Synthesizer to name a criterion satisfaction for the rotation. The user (during enactment review) flagged this would over-pressure the Synthesizer to claim transformations for concepts that legitimately carry through unchanged. The enacted version requires naming criterion satisfaction for the **changed portion** AND optionally identifying carry-through concepts with their L_{n+1} idiomaticity. A claim that names neither criterion-satisfaction nor honest-carry-through fails the check. |
| (3a) Variant absorption concept | Cycles 5+6 variant friction | New `book/src/concepts/variant-absorption.md` (peer to `rotation.md`) | None |
| (3b) Variant absorption Critic check | Same | New check #9 in `prompts/critic.md` | None |
| (3c) Synthesizer reference to variant absorption | Same | New "Variant absorption" subsection in `prompts/synthesizer.md` *Output discipline* | None |
| **Bonus** | User feedback during enactment | New "## Carry-through: not every concept must rotate" section in `book/src/concepts/rotation.md` with worked CG example | NEW — not in the proposal; arose from user pointing out the original rule's pressure toward over-transformation |

The carry-through clause is the substantive deviation from the proposed plan. It softens the rotation-quality rule *correctly* (per user direction): the rule was always "something has to move", never "everything has to move", but the original phrasing pressured the latter reading. Item 2's enactment then aligns with the softer rule.

## Recurring patterns and the watch list

Update to the watch list from `2026-05-24.md`:

- **Meta-Critic file-path drift** — **CLEARED.** Meta-Critic #2 used correct project paths (`prompts/`, `book/src/concepts/`) throughout. The previous drift was cycle-1-specific, possibly due to the absence of meta-review-#1 in context (it was the first one).
- **Rotation-as-renaming** — **RECURRED ONCE.** Cycle 4 = recurrence #1 of cycle 3's resolved problem. Per Meta-Critic protocol (recur-once = strengthen the fix; recur-twice = escalate to High), the producer-side self-check enacted here is the strengthening. If a future cycle (7+) reproduces the rename-only L1→L2 anti-pattern despite the producer-side check, that's recurrence #2 and the methodology itself (rotation criteria) needs a design-level rethink at High cascade.
- **NEW: Date drift between Meta-Critic and orchestrator** — Meta-Critic #2's plan JSON contained `meta_review_date: "2026-06-14"`, three weeks in the future. The orchestrator's actual run was 2026-05-23/24 UTC. The Meta-Critic prompt does not currently include the current date and the model hallucinates one. **Watch:** if this recurs in meta-review #3, escalate to a Meta-Critic prompt update that passes the current date in the user message.

## Approval

Approved by human at 2026-05-23 (actual system date — note the meta-review and cycle commits used UTC dates of 2026-05-24, hence the disambiguating filename). Enactment performed by Claude Code as the human's editing agent (orchestrator v1's manual-enactment path), with one substantive deviation (carry-through clause added per user feedback during review).
