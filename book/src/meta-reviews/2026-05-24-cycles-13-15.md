# Meta-review 2026-05-24 — cycles 13–15

(Fifth meta-review. Filename suffix `-cycles-13-15` continues the disambiguation pattern.)

**Window:** cycles 13–15.
**Trigger:** automatic (every 3 cycles per `config.toml`).
**Status:** enacted, with one substantive implementation deviation noted.

## Context

Three cycles, all on `gmres` (the planner returned to its grind pattern after the cycles-10-12 leaf-slice detour). All three diff-applies failed despite the strengthened producer-side checklist from meta-review #4 — fired the recurrence-#2 escalation to a tooling-level intervention.

Key signal: the apply-on-revise rule (from `a9b76bb`) and the leaf-slice seeding (`371b719`) BOTH worked for cycles 10-12 (3 leaf slices landed on disk). When the cycles returned to gmres at cycle 13, the same diff-apply failure mode reappeared **three times in a row**. Producer-side text-emission discipline has not converged on long unified diffs.

## Cycles covered

- **Cycle 13** — `forward gmres [L0→L1] → pass`. 1 rotation_claim. **Diff failed to apply** (header/body line-count mismatch). `gmres.md` NOT on disk despite verdict=pass. **Bug surfaced**: pass verdict despite failed diff falsely advances scoreboard.
- **Cycle 14** — `forward gmres [L1→L2] → revise`. 5 rotation_claims. **Diff failed to apply** (corrupt patch at line 143; header asserted 142, body had different count). Critic correctly flagged variant-absorption #4 (FGMRES primitive-sequence divergence).
- **Cycle 15** — `back gmres → pass`. 2 rotation_claims. **Diff failed to apply** (corrupt patch at line 90; header asserted 93, body had 89). Same pass-with-failed-diff bug as cycle 13.

Token usage: ~92k input / ~21k output across cycles 13-15.

## Friction summary

### LOW (0)
None.

### MEDIUM (4)

1. **Diff-apply recurrence #2** (cycles 13/14/15 — three failures in three cycles). Producer-side mitigation hasn't converged. Per cycles-10-12 watch list, escalates to tooling-level intervention.
2. **Same-slice grind continues** (cycles 13-15 all on gmres; 11 of 14 total). Leaf-slice seeding worked once (10-12) then the Planner reverted to grind. Methodology gap: Planner has no anti-grind heuristic.
3. **Pass-with-failed-diff bug** (cycles 13 and 15). Frictionless-pass sanity from meta-review #4 didn't anticipate this. The Critic verdicts pass on substantive claims; the orchestrator's diff-apply fails; the cycle commits with verdict=pass and the scoreboard falsely advances.
4. **Variant-absorption cluster reaches recurrence #4** (cycles 6, 7, 9, 14 — all on gmres). Cycle 14's FGMRES observation is a concrete methodology refinement worth promoting to `constructed-operators.md`. The lessons-promotion procedure from meta-review #4 fires here for the first time.

### HIGH (0)
None. Variant-absorption is now recurrence #4 but still same-slice; per the meta-review #3 escalation rule, cross-slice recurrence would escalate to High.

## Plan as proposed

See `meta-review-pending.md` (cleared on enactment). 4 MEDIUM items.

## Plan as enacted

| Item | Source friction | Enacted change | Deviation from proposal |
|---|---|---|---|
| (1) Tooling intervention for new-file emission | Cycles 13/14/15 diff-apply failures | **Orchestrator-side**: extended Synthesizer's JSON output with `file_creates: [{path, content}]` field. Orchestrator writes these directly (no diff-apply); enforces safe-path policy (only `book/src/`, `scaffolding/`, `problems/`). Synthesizer prompt updated to describe file-creates vs diff channels. | **Implementation location deviation.** Meta-Critic proposed adding to the MCP codemap server; I implemented orchestrator-side instead. Reasoning: the Synthesizer is sync and has no MCP tool-use plumbing (only Explorer does); adding tool-use to Synthesizer is a bigger architectural change than adding an output field. Orchestrator already handles diffs; symmetric handling of `file_creates` is cleaner. Same intent (new-file path bypasses unified-diff parsing); simpler implementation. If true MCP integration is later wanted, easy to promote. |
| (2) Planner anti-grind heuristic | 11 of 14 cycles on gmres | Added "Anti-grind heuristic" section to `prompts/planner.md`. Trigger: 3 consecutive `revise` verdicts on same slice with *novel* friction each time → rotate. Exemptions for genuine convergence (last verdict was pass; revises flag same issue; explicit BACK queued). | None |
| (3) Verdict downgrade on apply failure | Cycles 13/15 pass-with-failed-diff | **Orchestrator-side**: hard rule in `loop.py` — if `apply_failed` is true and verdict is `pass`, auto-downgrade to `revise` and append a push-back signal. Also added clarifying note to `prompts/critic.md` documenting that the orchestrator enforces this. | **Implementation location deviation.** Meta-Critic proposed a Critic-side rule; I implemented orchestrator-side. Reasoning: the Critic verdicts BEFORE the diff is applied, so it cannot observe apply-failure at verdict time. The check must live where the failure is visible — the orchestrator. The Critic prompt now documents the orchestrator's downgrade rule but doesn't try to enforce it itself. |
| (4) Promote FGMRES lesson to concept page | Cycle 14 lesson (per-step variants need threaded Z, not constructed M) | New "Limits of constructed-operator absorption" section in `book/src/concepts/constructed-operators.md` with decision rule (construction vs. per-step). Cross-reference added to `variant-absorption.md` *Levels of absorption* (c) primitive-sequence subsection. **First explicit use of the lessons-promotion procedure introduced in meta-review #4.** | None |

The two implementation deviations (items 1 and 3) preserve methodology intent; only the location of the logic changed. Both deviations move work from agent-prompt-level enforcement to orchestrator-level enforcement where the failure is actually observable. Document and proceed.

## Recurring patterns and the watch list

Update to the watch list:

- **Meta-Critic file-path drift** — STILL CLEAR (5 meta-reviews in a row).
- **Rotation-as-renaming** — STILL CLEAR.
- **Diff-apply mismatch** — recurrence #2 fired this round → enacted the file_creates tooling intervention. **Watch reset**: if `file_creates` writes start failing (path-safety rejection, etc.), that's a NEW failure mode and a fresh watch starts. The diff-apply-on-existing-file-edits path remains; if THAT starts failing, separate watch.
- **Pass-without-exercise** — replaced by the harder pass-with-failed-diff failure (cycles 13/15). Hard rule now enforced orchestrator-side; should not recur as a structural issue. If frictionless-pass on a slice with a successful diff is still suspicious, the meta-review #4 sanity note applies.
- **Same-slice grind** — anti-grind heuristic added. Watch whether it actually fires in cycles 16+ when the Planner sees 11/14 cycles on gmres.
- **Variant absorption cluster** — recurrence #4 (cycles 6/7/9/14), all same slice. Cycle 14's lesson promoted to concept page. **Escalation trigger remains**: recurrence in a DIFFERENT slice would be cross-slice and escalate to High (methodology-level rethink of how variants are handled in L0→L1).
- **NEW: Meta-Critic implementation-location proposals.** Twice this round (items 1 and 3), the Meta-Critic proposed a change in a location that wasn't quite right (MCP server when orchestrator was cleaner; Critic prompt when orchestrator was the only place the failure is observable). The deviations didn't change methodology, just location. Worth watching whether this becomes a pattern — if Meta-Critic consistently proposes locations that are deflected during enactment, the Meta-Critic's mental model of the project structure may need a prompt update.

## Phase 6 DONE scoreboard

| Criterion | Status |
|---|---|
| GMRES pushed to L4 with rotation chains | ✗ (gmres slice STILL does not exist on disk; 14 cycles total of grinding produced nothing) |
| First 3 rotation_claims hand-verify | possible (claims accumulated in orthog/divfree/chebyshev on disk) |
| ≥1 BACK push | ✓ (5, 6, 8, 9, 15) |
| ≥1 SIDEWAYS push | ✗ (Planner has never picked SIDEWAYS) |
| Meta-review fires automatically | ✓✓✓✓✓ (5 times) |

Phase 6 is stalled on GMRES-to-L4 (the slice hasn't even reached L1 on disk) and on SIDEWAYS (Planner has never picked it). The anti-grind heuristic + file_creates tooling should unblock both in the next batch — anti-grind forces diversification; file_creates makes gmres-L1 actually land.

## Approval

Approved by human at 2026-05-23 (system date). Enactment by Claude Code with two implementation-location deviations from the Meta-Critic's plan (preserved intent, simpler implementation in both cases).
