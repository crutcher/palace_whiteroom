# Meta-review 2026-05-26 — cycles 86–91

(Nineteenth meta-review.)

**Window:** cycles 86–91.
**Trigger:** automatic (last batch under depth-6; depth-12 takes effect next loop invocation).
**Status:** enacted.

## Context

This window was the **meta-18 hard-gate stress test**. The orchestrator-side retroactive-budget gate fired four consecutive times (cycles 86-89), each auto-escalating because the Planner kept proposing cg cycles (the previously-saturated slice from cycles 83-85 + the prior windows). On cycle 90 the Planner finally rotated to SIDEWAYS (orthog/divfree/chebyshev) — and tripped the SIDEWAYS channel-selection defect for the **fifth** time (cycles 22/25/40/79/90 — 100% failure rate across all SIDEWAYS attempts ever). Cycle 91 finally landed forward cg L1→L2; the Planner's reason explicitly cited "Retroactive-budget gate satisfied by rotating to different slice" — the loop is now meta-aware of the gate.

Plus a user directive arrived mid-window: **raise propose/modify/split a skill priority by 60%**. The Meta-Critic naturally proposed a **third skill** (`skill-selection` — a meta-skill) in its plan, validating the directive's intent independently of the formal enactment.

## Cycles covered

- **Cycle 86** — `forward cg L4→L4`. ESCALATE (orchestrator-side hard gate; consec=3).
- **Cycle 87** — `forward cg L0→L1`. ESCALATE (gate still firing; the Planner was attempting different edges on the same slice but the gate counts consecutive retroactives ON THE SLICE, not on the edge).
- **Cycle 88** — `forward cg L3→L4`. ESCALATE.
- **Cycle 89** — `forward cg L4→L4`. ESCALATE.
- **Cycle 90** — `sideways orthog,divfree,chebyshev`. PASS→REVISE (apply failure: SIDEWAYS recurrence #5 — mode=create on existing).
- **Cycle 91** — `forward cg L1→L2`. PASS (Planner cited the gate; loop is meta-aware).

Token usage: ~280k input / ~70k output across 6 cycles (substantial portion wasted on the 4 escalates).

## Friction summary

### LOW (1)
- **Escalate-storm pattern** — the meta-18 hard gate works but produces successive escalates with no productive next push. Surface for plan item 1.

### MEDIUM (3)
1. **Hard-gate escalate-storm**. Cycles 86-89 = 4 cycles of escalate. The forward-frontier criterion (meta-18 item 2) and intermediate-tier prioritization (meta-18 item 4) exist as prompt rules but didn't engage. The gate prevents bad work but offers no recovery path.
2. **SIDEWAYS recurrence #5** (cycle 90). 100% failure rate across all SIDEWAYS attempts. Producer-side rules across meta-9/11/13/16/17 have not converged. Time for integrator-side auto-rewrite.
3. **Skill-uptake invisibility unresolved through 4 meta-cycles**. Meta-11/16/17/18 all flagged it. Meta-18 added structured field but cycles 86-89 escalated (no Critic verdict), cycle 90 downgraded, cycle 91 was a forward — we have zero observation. Plus the user directive +60% skill priority arrived this window. Solution: a **third skill** (meta-skill: `skill-selection`) that codifies the consultation procedure for both Synthesizer and Critic.

### HIGH (0)
None.

## Plan as enacted

| Item | Source friction | Enacted change | Deviation |
|---|---|---|---|
| (LOW) Document escalate-storm | Cycles 86-89 | Recorded in this meta-review record + plan item 1 enacted as recovery. | None |
| (1) Hard-gate escalate-storm recovery | Cycles 86-89 escalate-storm | `prompts/planner.md`: when retroactive-budget gate would fire, MUST emit a forward-frontier dispatch (intermediate-tier candidate / missing-L4 / new roadmap slice / SIDEWAYS) — NOT escalate. `orchestrator/loop.py`: when auto-escalate-on-3-consecutive fires, **re-invoke the Planner once** with an explicit addendum naming the gate trigger and listing forward-frontier alternatives; only escalate if the retry is ALSO a retroactive on the same slice. New helper `call_planner_with_addendum` in `roles.py`. | Implemented retry-once pattern as specified. |
| (2) SIDEWAYS auto-rewrite | Recurrence #5 (cycles 22/25/40/79/90) | `orchestrator/loop.py` integrator phase 00 (pre-everything): scan slice_writes/concept_writes mode=create entries; if target exists on disk, auto-rewrite to `section_appends` (slices) or `mode=append-section` (concepts). Push-back signal logged for Meta-Critic visibility. The Synthesizer's defect is now an orchestrator-handled no-op rather than a downgrade. | None |
| (3) `skill-selection` meta-skill + skill_uptake on every verdict + Synthesizer skills_consulted | Skill uptake unmeasurable 4 meta-cycles in a row | (a) **THIRD SKILL**: `skills/skill-selection/SKILL.md` codifies the procedure for the per-cycle agents (Synthesizer pre-emission, Critic pre-verdict) to survey active skills, check triggers, apply, and record. (b) `prompts/critic.md` check #15: populate `skill_uptake` on EVERY verdict (was: verdict-revise paths only); list every active skill with `triggered: bool`. (c) `prompts/synthesizer.md`: `log_synthesis.skills_consulted` array (or inline phrase). Validates the meta-pass skill-extraction directive — third skill confirms the pattern is producing real artifacts. | None |
| (bonus) +60% skill priority (user directive landing mid-window) | User 2026-05-25 directive | Already committed separately (`prompts/meta_critic.md` rewritten + `schemas/refinement_plan.json` adds `skill_pass` field). Meta-19's plan was emitted under the OLD Meta-Critic prompt; this plan item validates the directive's intent (3rd skill proposed organically). | Already committed pre-meta-19; recorded here for the audit trail. |

## Recurring patterns and the watch list

- **Retroactive-budget gate** — works structurally; now has a recovery path. Watch cycles 92+ for: (i) the retry-once succeeding (Planner producing a different slice on second chance); (ii) the escalate-as-final-fallback rate dropping.
- **SIDEWAYS auto-rewrite** — closes the 5-cycle Synthesizer-side defect. Watch for: (i) auto-rewrite firing on SIDEWAYS cycles; (ii) the rewritten cycles producing on-disk content (the desired outcome); (iii) any case where the auto-rewrite loses content (push-back signals would name it).
- **Skill uptake** — `skill_uptake` on every verdict + Synthesizer `skills_consulted` + `skill-selection` meta-skill. Watch cycles 92+ for: (i) the first non-empty `skill_uptake` arrays in episodic; (ii) cycles citing `skill-selection` itself.
- **Loop meta-awareness** — cycle 91's Planner reason cited the gate by name. The methodology is now self-reflective at the per-cycle level.
- **Skill priority +60%** — user-directed; first effect was meta-19's `skill-selection` proposal arriving organically. Watch next meta for an explicit `skill_pass` field in the plan (the schema is in place).

## Phase 6+ status

- Per-solver pipelines: 0/5 (unchanged).
- Krylov solvers: 2/4 at L4 (unchanged).
- Orthogonalisation: 1/2 (unchanged).
- Smoothers: 1/6 (unchanged).
- Projections: 0/2 at L4 (divfree unchanged at L3).
- Intermediate-tier algorithms: 0/7 extracted (unchanged — the gate's recovery path now positively pushes the Planner toward these).
- **Skills extracted: 3** (was 2 — `skill-selection` added).
- FE assembly: 0/4 (unchanged).
- Coordination/post-processing: 0/4 (unchanged).
- Phase progression: Phases 0-6 done; Phase 6+ in flight.

The window's substantive output is **methodology infrastructure** (3rd skill, retry-once gate recovery, integrator auto-rewrite) rather than slice progress. This is expected for a window where the gate forced rotation; subsequent windows under the fixed methodology should resume slice forward progress.

## Approval

Approved by human (auto-approval mode); enactment by Claude Code. The user directive (+60% skill priority) committed separately pre-meta-19; this meta validates the directive organically via the 3rd skill proposal.
