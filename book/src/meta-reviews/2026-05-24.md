# Meta-review 2026-05-24

**Window:** cycles 1–3.
**Trigger:** automatic (every 3 cycles per `config.toml`).
**Status:** enacted.

## Context

First meta-review of the loop's life. Cycles 1–3 covered the Phase 5 DONE check (cycle 1: `cg_solver_integration` exploratory cycle) and the first two cycles of the Phase 6 GMRES push (cycles 2 and 3, both on slice `gmres`).

This record is **immutable** once committed — subsequent Meta-Critics read it as project history. Modifications would break the historical-truth contract of the meta-review trail.

## Cycles covered

- **Cycle 1** — `forward cg_solver_integration [L0→L1] → revise`. Synthesizer attempted a slice that spanned three concerns (ProblemType dispatch, IoData resolution, BaseKspSolver composition) with the middle link left unverified. Critic flagged scope.
- **Cycle 2** — `forward gmres [L0→L1] → pass`. Substantive L1 spec produced (Krylov basis, Hessenberg, Givens, Initialize/Update/Mult/Arnoldi/reconstruction, FGMRES delta). But the diff failed to apply (`git apply` rejected — corrupt patch header). And the diff carried zero `rotation_claim` entries. Critic's verdict was `pass` on what was actually structurally unauditable.
- **Cycle 3** — `forward gmres [L1→L2] → revise`. Critic correctly identified L1→L2 as collapse-to-renaming.

Total token usage: ~118k input / 28k output across cycles 2 and 3 (cycle 1's usage logged separately under Phase 5 DONE).

## Friction summary

### LOW (1)

- **Diff format malformedness** (cycle 2). `@@ -0,0 +1,166 @@` header asserted 166 lines but body had ~117. `git apply` rejected. Resolution: Synthesizer prompt now has a "Diff hygiene" section under *Output discipline*.

### MEDIUM (3)

- **Over-scoped slice with unverified internal link** (cycle 1). Resolution: "Slice acceptance criteria" added to `book/src/spec/index.md`, requiring a slice to form a single closed rotation chain; load-bearing unknowns force a split.
- **Substantive slice with zero rotation_claims** (cycle 2). Resolution: per-assertion rotation_claim coverage required by both `prompts/synthesizer.md` (*Output discipline*) and `book/src/spec/index.md` (slice acceptance).
- **Rotation collapsed to renaming** (cycle 3). Resolution: new `book/src/concepts/rotation.md` codifies quality criteria — state hiding / coarser substitution / threaded-state compression — at least one of which must hold for a rotation to count. `prompts/critic.md` gains verification check #8 to apply the criteria.

### HIGH (0)

None.

## Plan as proposed

Per `meta-review-pending.md` (cleared on enactment). Four items. Full plan JSON is in the commit message of this enactment, and a copy is preserved in the orchestrator's audit trail.

## Plan as enacted

| Item | Source friction | Enacted change | Path correction from proposal |
|---|---|---|---|
| Diff hygiene reminder (LOW) | Cycle 2 corrupt patch | "Output discipline" section in `prompts/synthesizer.md` | `agents/synthesizer.md` → `prompts/synthesizer.md` |
| Slice-scoping rule (MEDIUM) | Cycle 1 over-scope | "Slice acceptance criteria" in `book/src/spec/index.md` | `book/src/spec/slices/index.md` (doesn't exist) → `book/src/spec/index.md` (the slice index) |
| Rotation_claim coverage (MEDIUM) | Cycle 2 zero claims | `prompts/synthesizer.md` + slice-acceptance criterion in `book/src/spec/index.md` | Same path corrections |
| Rotation quality criteria (MEDIUM) | Cycle 3 rename-not-rotation | New `book/src/concepts/rotation.md`; verification check #8 in `prompts/critic.md` | `agents/critic.md` → `prompts/critic.md` |

All four items enacted exactly per the proposal's substance, with path corrections noted above. No items dropped, deferred, or substantially modified.

## Recurring patterns to watch

This is the first meta-review; no recurrences yet. Patterns to watch for in subsequent reviews:

- **Meta-Critic file-path drift.** The Meta-Critic referenced `agents/<role>.md` for all role-prompt edits, but the project's convention is `prompts/<role>.md`. The meta-cycle prompt's input enumeration should pin the project layout explicitly. If this recurs in meta-review 2 or 3, escalate from "ad-hoc path correction during enactment" to a Meta-Critic prompt update (Medium-cascade plan item, not direct).
- **Synthesizer-vs-Critic alignment.** Synthesizer previously emitted rotation_claims as optional and Critic required citations to verify; this first meta-review aligned them (every L1 assertion → rotation_claim). Watch whether the new requirement actually fires correct rejections on next cycles, or produces false-positive rejections of legitimate slices.
- **Rotation-quality false positives.** The criteria in `book/src/concepts/rotation.md` may be too sharp. The "Working Notes" section there flags this; subsequent cycles' verdicts should be reviewed for whether modest-but-legitimate rotations are being rejected.

## Approval

Approved by human at 2026-05-24. Enactment performed by Claude Code as the human's editing agent (orchestrator v1's `[meta-review] approved by human; enactment is manual in v1` path). The orchestrator's automated-enactment is deferred to v2; the meta-review-pending.md handshake remains the formal human-approval gate either way.
