# Meta-review 2026-05-24 — cycles 16–18

(Sixth meta-review. First batch under the integration-plan architecture.)

**Window:** cycles 16–18.
**Trigger:** automatic.
**Status:** enacted.

## Context

First batch where the orchestrator processes integration plans rather than `{diff, file_creates, rotation_claims}`. Two big wins, one new failure mode:

**Working as designed:**
- **Anti-grind heuristic fired correctly.** Cycle 16 chose `orthog` over `gmres`, citing the cycles-13-15 grind explicitly in its reason. Cycle 17 chose `divfree`. Cycle 18 BACK-pushed orthog. Planner is no longer single-track on gmres.
- **Verdict-downgrade rule worked.** All three cycles had `pass` from Critic auto-downgraded to `revise` because writes failed. The orchestrator override prevented scoreboard pollution.
- **Integration plans validated and applied.** The Synthesizer correctly emitted plans validating against `schemas/integration_plan.json`. The integrator applied them section by section. The dependency map got its first cycle-generated edge (`orthogonalize_column → variant-absorption` in the L1 mermaid block — semantic correctness aside, the integrator's mermaid surgery worked).

**New failure mode:**
- **Diff-apply-on-edit** is the next-round failure of the same recurring problem. The `file_creates` channel from meta-review #5 only covered new files; edits to existing files (orthog.md, divfree.md from cycles 10/11) still routed through unified-diff parsing and failed. Recurrence count: cycles 2, 12, 13, 14, 15 (new files; resolved by file_creates) + 16, 17, 18 (existing files; new symmetric problem).

## Cycles covered

- **Cycle 16** — `forward orthog [L0→L1]`. Planner cited "leaf-support unblock; Q-orthog-leaf enables GMRES L2 substitution and breaks grind." Verdict pass→revise (apply failure: orthog.md already exists; Synthesizer used mode=create).
- **Cycle 17** — `forward divfree [L0→L1]`. Same pattern: Planner picked a different slice (good), Synthesizer used mode=create on existing file (bad).
- **Cycle 18** — `back orthog`. Planner correctly diagnosed cycles 16-17 as "diff-apply failures indicate malformed patch generation"; pushed back to fix. Same downgrade pattern.

Token usage: ~85k input / ~18k output across cycles 16-18.

## Friction summary

### LOW (0)
None.

### MEDIUM (2)

1. **Diff-apply-on-edit failures dominate** (cycles 16/17/18 = 3/3 this window; cumulative 8 of 17 normal cycles have hit this mode). Producer-side checklists have not converged. Meta-review #5's `file_creates` channel solved it for new files; symmetric extension to edits is the natural next move.
2. **Critic-check exercise invisibility under verdict downgrade**. When the orchestrator auto-downgrades pass→revise on apply failure, the Critic's content judgment is buried. The Meta-Critic can't tell "content was sound, tooling failed" from "content unverified AND tooling failed".

### HIGH (0)
None.

## Plan as enacted

| Item | Source friction | Enacted change | Deviation from proposal |
|---|---|---|---|
| (1) `file_edits` channel | Cycles 16/17/18 diff-on-edit failures | New field in `schemas/integration_plan.json`: array of `{path, old_string, new_string, replace_all?}`. Orchestrator integrator performs find/replace with exactly-one-match check (rejects ambiguous unless replace_all=true). Safe-path policy same as file_creates. Synthesizer prompt updated: channel-selection rule documents file_edits as preferred edit path; verify path existence before choosing mode. | None substantive. Meta-Critic anticipated the orchestrator-vs-MCP deviation pattern; I confirmed orchestrator is the right place. |
| (2) Preserve Critic verdict + downgrade marker | Cycles 16/17/18 downgrades buried content judgment | Orchestrator captures `verdict_original` and `downgrade_applied` flags in episodic.jsonl. Critic prompt gains "Exercised-checks enumeration" subsection: Critic lists which substantive checks were exercised even on pass verdicts so downgrade-driven revises can be distinguished from content-driven revises. | None |

Both items enacted as-proposed. No deviations this round.

## Recurring patterns and the watch list

- **Meta-Critic file-path drift** — STILL CLEAR (6 in a row).
- **Rotation-as-renaming** — STILL CLEAR.
- **Variant-absorption cluster** — last fire was cycle 14 (recurrence #4 same-slice); cycles 15-18 didn't touch it.
- **Same-slice grind** — anti-grind heuristic FIRED CORRECTLY in cycle 16. Watch cleared (pending: still need to see SIDEWAYS fire eventually).
- **Diff-apply mismatch** — recurrence pattern continues (new files solved meta-review #5; edits solved this meta-review #6). If a NEW failure mode of file_edits surfaces in cycles 19+, that's a new pattern. If file_edits fails the same way file_creates did NOT, this design needs revisiting.
- **NEW: Synthesizer mode-selection defect** — Synthesizer used `mode=create` on existing files. The channel-selection rule in the updated synthesizer.md prompt addresses this. Watch whether it actually rules out the pattern in cycles 19+.
- **SIDEWAYS never fired** — 18 cycles in, still no SIDEWAYS push. The Planner's criterion 4 ("multiple slices at same layer with no friction") has had a clean trigger condition since cycle 11 (orthog L1, divfree L1, chebyshev L1 — three same-layer with no friction). Either the criterion isn't engaging or the Planner is interpreting it conservatively. **Watch escalation**: if cycles 19-21 still don't fire SIDEWAYS, surface as a Planner-prompt fix.

## Phase 6 DONE scoreboard

| Criterion | Status |
|---|---|
| GMRES pushed to L4 | ✗ (gmres slice still doesn't exist on disk; planner now spending cycles on leaf slices) |
| First 3 rotation_claims hand-verify | possible (claims on disk in orthog/divfree/chebyshev) |
| ≥1 BACK push | ✓ (multiple) |
| ≥1 SIDEWAYS push | ✗ (never fired in 18 cycles) |
| Meta-review fires | ✓✓✓✓✓✓ (6 times) |

## Approval

Approved by human; enactment by Claude Code with no substantive deviations.
