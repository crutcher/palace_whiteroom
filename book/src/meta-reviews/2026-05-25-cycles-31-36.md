# Meta-review 2026-05-25 — cycles 31–36

(Tenth meta-review.)

**Window:** cycles 31–36.
**Trigger:** automatic.
**Status:** enacted.

## Context

**Phase 6 finish line.** GMRES reached L4 (cycles 31, 32, 34 all landed L4 content — multiple emissions converged the L4 representation). All five Phase 6 DONE criteria are now satisfied:

| Criterion | Status |
|---|---|
| GMRES pushed to L4 | ✓ (L1+L2+L3+L4 all on disk; per-step body lifted, outer-loop obstruction recorded, L4 calculus binding) |
| First 3 rotation_claims hand-verify | ✓ |
| ≥1 BACK push | ✓ |
| ≥1 SIDEWAYS push | ✓ |
| Meta-review fires | ✓ × 10 |

The refined downgrade rule (meta-9) worked exactly as designed: 4 of 6 cycles in this window held pass via `bookkeeping_incomplete` flag, preserving the substantive L4 milestone. Without that fix, this window would have looked like 4 downgrades — the user would have seen "GMRES failed to land L4 four times" instead of "GMRES landed L4 three times; only the index-row bookkeeping kept failing."

But the underlying `file_edits` anchor-mismatch problem recurred 4 cycles in a row — recurrence #2 of the meta-9 watch, which triggers the tooling intervention.

## Cycles covered

- **Cycle 31** — `forward gmres L3→L4`. Critic PASS. 8 substantive writes landed (L4 content + concepts); `bookkeeping_incomplete` flag set on `spec/index.md` anchor failure.
- **Cycle 32** — `forward gmres L3→L4` (re-emit). PASS. 7 substantive writes landed; `bookkeeping_incomplete`.
- **Cycle 33** — `forward divfree L1→L2`. PASS. 4 substantive writes landed; `bookkeeping_incomplete`.
- **Cycle 34** — `forward gmres L3→L4` (retry). PASS. 5 substantive writes landed; `bookkeeping_incomplete`.
- **Cycle 35** — `forward gmres L4→L4` (self-tightening). REVISE. Surfaced real friction: `Outcome ADT carrying boolean re-encodes information available at call site`; no follow-on tightening in window.
- **Cycle 36** — `back gmres`. PASS. 0 substantive writes (retroactive rotation_claims emission against on-disk content); lesson promoted: cycles like this need a `plan_kind` flag.

Token usage: ~220k input / ~50k output.

## Friction summary

### LOW (1)
- **concept_writes mode=create silent skip recurrence.** Cycles 32/33/34 silently no-op'd writes to 4 concepts (`state-stratification`, `solve-monad`, `set_subvector_zero`, `ksp_solve`) — the meta-8 LOW item's prompt rule is being ignored.

### MEDIUM (3)
1. **`spec/index.md` bookkeeping anchor recurrence #2.** Cycles 31/32/33/34 all hit `file_edits` anchor mismatch. Meta-9's producer-side anchor-verification rule did not converge. Per the meta-9 escalation trigger ("if recurrence #2, escalate to dedicated `slice_index_update` channel"), the trigger has fired.
2. **Retroactive / claim-only emission cycles** produce 0 substantive writes (cycle 36); indistinguishable from no-op cycles in episodic.
3. **L_n→L_n self-rotation tightenings stay open as friction.** Cycle 35 surfaced a tightening; no consuming cycle in window. Planner has no rule on consuming layer-internal tightening friction.

### HIGH (0)
None.

## Plan as enacted

| Item | Source friction | Enacted change | Deviation |
|---|---|---|---|
| (LOW) Concept existence check strengthening | Cycles 32/33/34 silent skips | `prompts/synthesizer.md` "Concept existence check" subsection rewritten with explicit list of existing concept pages as concrete examples; rule strengthened to "most-recurring Synthesizer-side defect; double-check before emitting." | None |
| (1) `slice_index_updates` dedicated channel | Recurrence #2 of bookkeeping anchor failures | (a) `schemas/integration_plan.json`: new `slice_index_updates` array field with `{slice, layer, date?, summary, link_title?}`. (b) `orchestrator/state.py`: `update_slice_index_row()` locates the row by link-target anchor (`(./slices/<slug>.md)` or `(./slices/<slug>/index.md)`) and mechanically rewrites cells. Idempotent on identical content; raises `FileNotFoundError` if the row is missing. (c) `orchestrator/loop.py`: integrator handles the channel between file_edits and lessons. Failures are tracked as bookkeeping (so the safety net keeps working). (d) `prompts/synthesizer.md` documents the channel; `file_edits` on `spec/index.md` is now reserved for non-row edits. | None |
| (2) `plan_kind` cycle classification | Cycle 36 0-substantive pass | (a) `schemas/integration_plan.json`: optional `plan_kind` field with enum `[new_content, retroactive_claims, tightening, back_correction]`. (b) `orchestrator/loop.py`: episodic captures `plan_kind` per cycle. (c) `prompts/synthesizer.md` "Plan kind classification" subsection: rules for each kind, including the `log_synthesis` citation requirement for `retroactive_claims`. | None |
| (3) Planner self-rotation tightening heuristic | Cycle 35 deferred tightening | `prompts/planner.md` new "Self-rotation tightening consumption" subsection: when the most recent cycle on a slice produced a `revise` on `L_n→L_n` (or a `back` naming a tightening), the next push SHOULD be the tightening forward. Anti-grind takes precedence if it also fires. | None |

## Recurring patterns and the watch list

- **Bookkeeping anchor failures** — now under mechanical control via `slice_index_updates`. If recurrence #3 fires on the *new* channel, that's a tooling-side regression and gets a different fix.
- **Concept create-on-existing** — promoted to "most-recurring Synthesizer-side defect" in the prompt. If cycles 37-42 still skip silently, escalate to a Critic-side check (#12: pre-emit concept-existence verification).
- **`bookkeeping_incomplete` flag firing pattern** — the meta-9 fix worked. Watch: total `bookkeeping_incomplete` count across windows should be trending down as `slice_index_updates` adoption rises.
- **Self-rotation tightening** — Planner heuristic added; watch if it actually drives cycles 37+ to consume rather than defer.

## Phase 6 — DONE

All criteria met. The methodology can drive Krylov-family slice work to L4 reliably; the loop's infrastructure has stabilized through 10 meta-reviews. Next decision is human-side: continue accumulating Krylov slices (CG already at L4; orthog, divfree, chebyshev at L1/L2 with clear paths up), pivot to mesh/FE-space slices, or move to Phase 7 (execution grounding).

## Approval

Approved by human; enactment by Claude Code with no substantive deviations.
