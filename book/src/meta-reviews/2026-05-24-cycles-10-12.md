# Meta-review 2026-05-24 — cycles 10–12

(Fourth meta-review of the loop's life. First batch under the apply-on-revise rule from `a9b76bb`. Same-day disambiguation suffix `-cycles-10-12`.)

**Window:** cycles 10–12.
**Trigger:** automatic (every 3 cycles per `config.toml`).
**Status:** enacted.

## Context

First meta-review where the **surface actually accumulated**. The apply-on-revise methodology fix (commit `a9b76bb`), combined with the leaf-slice seeding (`371b719`) that diversified the Planner's targets, produced three slice files on disk in three cycles — versus 8 prior GMRES cycles that produced zero accumulated content.

Cycle distribution: 3 FORWARD pushes, all on first-touch leaf slices (`orthog`, `divfree`, `chebyshev`). 0 BACK, 0 SIDEWAYS. The Meta-Critic's plan JSON refers to these as "Three SIDEWAYS cycles in a row" — that's a terminology slip: they are FORWARD pushes on different slices, not SIDEWAYS comparisons. (SIDEWAYS is a specific push kind that compares two slices' content; FORWARD on a new slice is initialization, not comparison.) Worth noting for the watch list, not for plan-item promotion.

Important non-recurrences:
- Rotation-as-renaming did NOT recur (cycle 12's chebyshev claimed variant absorption via constructed-operators; per the cycle's own lessons.md self-observation, the absorption was partial in setup, but the rotation structure was real).
- Meta-Critic file-path drift remained clear (third meta-review in a row with correct paths).
- Variant-absorption did NOT recur (the leaf slices either avoided variants or absorbed them cleanly per the methodology).

## Cycles covered

- **Cycle 10** — `forward orthog [L0→L1] → pass`. 1 rotation_claim. Diff applied (111 lines → `book/src/spec/slices/orthog.md`). First slice ever to land on disk via the apply-on-revise rule. Substantive variant analysis (MGS / CGS / CGS2 unified parametrically).
- **Cycle 11** — `forward divfree [L0→L1] → pass`. 3 rotation_claims. Diff applied (122 lines → `book/src/spec/slices/divfree.md`). Non-trivial: 3 claims, divergence-free projection algorithm with multiple primitive references.
- **Cycle 12** — `forward chebyshev [L0→L1] → pass`. 3 rotation_claims. **Diff FAILED to apply** — corrupt patch at line 113. Episodic captures the full diff text in `friction_observed`, so the content is recoverable, but `book/src/spec/slices/chebyshev.md` does not exist on disk. Cycle committed (with `verdict: pass`) but with diff-apply failure recorded as push-back signal.

Token usage: ~120k input / ~25k output across cycles 10–12.

## Friction summary

### LOW (0)
None.

### MEDIUM (3)

1. **Diff-apply recurrence** — cycle 12 chebyshev failure mirrors cycle 2 GMRES failure. Header/body line-count mismatch. The diff-hygiene rule from meta-review #1 hasn't eliminated the mode. Per recurrence protocol: recur-once = strengthen the fix (this round); if recurrence #2 in the next batch, escalate to a tooling-level intervention (MCP server gains file-creation tool — Medium MCP service change).

2. **Frictionless-pass sanity** — cycles 10/11/12 all pass with zero Critic friction despite cycle 12's lessons.md self-observation that the constructed-operator absorption was partial in setup. The Critic's checks may not be activating on first-touch slices. New check #11 covers the specific failure mode (setup/state schema for variant absorption); a general "state which checks fired" note surfaces pass-without-exercise.

3. **Lessons promotion pathway under-used** — 3 candidate concept-shaped lessons from cycles 10-12 (out-of-place wrapper mutation pattern, single-vs-block primitive distinction, constructed-operator partial-absorption in setup) sit in `lessons.md` without explicit promotion-or-retain decisions. Meta-reviews #1/#2/#3 promoted lessons → concepts ad-hoc; formalize the procedure.

### HIGH (0)
None.

## Plan as proposed

See `meta-review-pending.md` (cleared on enactment). Full plan JSON preserved in commit message.

## Plan as enacted

| Item | Source friction | Enacted change | Deviation from proposal |
|---|---|---|---|
| (1) Strengthen diff-hygiene | Cycle 12 corrupt patch (recurrence #1) | "Diff hygiene" subsection in `prompts/synthesizer.md` expanded with pre-emit checklist (count `+` lines, verify against `@@` header, restate count), `/dev/null` pattern for new-file diffs, future-tooling note about MCP file-creation if recurrence #2 fires | None |
| (2) Critic check #11 + frictionless-pass sanity | Cycle 12 lesson 11 (setup schema hiding) | Check #11 added to `prompts/critic.md` (setup/state schema coverage); plus a "Frictionless-pass sanity" note requiring brief check-exercise enumeration on pass verdicts | None |
| (3) Lessons promotion procedure | 3 candidate lessons from cycles 10-12 unpromoted | New "Lessons promotion" section in `book/src/meta-reviews/index.md` formalizing the (a) promote / (b) keep / (c) retire decision; promotion threshold = "second instance, or single instance clearly going to recur" | None |

Also (procedural cleanup): the Records table in `book/src/meta-reviews/index.md` was placeholder (`_(none yet)_`); updated with all four meta-review records.

No deviations from the Meta-Critic's proposed plan this round. The 3 candidate lessons from cycles 10-12 are NOT promoted in this enactment — the new procedure goes in place first, and the next meta-review (or a manual scan) decides which to promote.

## Recurring patterns and the watch list

Update to the watch list:

- **Meta-Critic file-path drift** — STILL CLEAR (4 meta-reviews in a row).
- **Rotation-as-renaming** — STILL CLEAR (didn't recur in cycles 10-12).
- **Variant absorption** — STILL CLEAR (3-recurrence-cross-slice trigger NOT fired; leaf slices either avoided variants or absorbed cleanly).
- **NEW: Diff-apply mismatch** — recurrence #1 (cycles 2 and 12). Producer-side checklist strengthened. **If recurrence #2 in cycle 13+, escalate to MCP service change** (Medium-cascade tooling intervention per `feedback-tooling-changes-proposable`).
- **NEW: Pass-without-exercise** — first observation. Cycles 10/11/12 all pass with zero exercise of the deeper checks. Hard to know if this is "first-touch slices are genuinely clean" or "Critic under-examines on first touch." Check #11 + frictionless-pass sanity note are the producer/consumer mitigations. Watch whether next batch's pass verdicts surface exercised checks or remain silent.
- **NEW (terminology drift, not a plan item)**: Meta-Critic conflates "FORWARD on different slices" with "SIDEWAYS". They're distinct push kinds. If this terminology slips into a future meta-review's plan items (and influences action), surface as a Meta-Critic-prompt issue. For now, the actual SIDEWAYS push count is **still 0**.

## Phase 6 DONE scoreboard

For visibility:

| Criterion | Status |
|---|---|
| GMRES pushed to L4 with rotation chains | ✗ (gmres slice doesn't exist on disk; 8 cycles of grinding produced nothing before apply-on-revise) |
| First 3 rotation_claims hand-verify | possible NOW (claims accumulated in orthog.md + divfree.md) |
| ≥1 BACK push | ✓ (cycles 5, 6, 8, 9) |
| ≥1 actual SIDEWAYS push | ✗ (terminology drift aside; Planner has 4 slices available — should fire soon) |
| Meta-review fires automatically | ✓✓✓✓ (4 times) |

Phase 6 is in motion; SIDEWAYS is the most-likely-next-fire criterion now that 4 slices exist at varying layers. GMRES-to-L4 is the most-distant unmet criterion.

## Approval

Approved by human at 2026-05-23 (orchestrator UTC dates show 2026-05-24). Enactment performed by Claude Code as the human's editing agent. No deviations from proposed plan; only the procedural cleanup of the Records table added.
