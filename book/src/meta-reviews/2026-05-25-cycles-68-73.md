# Meta-review 2026-05-25 — cycles 68–73

(Sixteenth meta-review.)

**Window:** cycles 68–73.
**Trigger:** automatic.
**Status:** enacted.

## Context

Productive batch: orthog L1→L2 (cycle 68), gmres restored to clean state through L0/L1/L2/L3 (cycles 69-73). No grind — Planner stayed on gmres for 4 cycles but each cycle moved a different edge. The meta-15 self-tightening termination heuristic prevented re-grind on cg.

Two refinement patterns surfaced:

1. **Retroactive_claims granularity regression** — cycles 71-73 backfilled per-building-block claims for an L2 section that landed back in cycle 21 with a single summary claim. The meta-12 "original-emission claim discipline" existed but didn't specify granularity. 50-cycle gap between landing and per-block audit.
2. **Citation-tightening as a recurring L0→L1 self-rotation** (cycles 69-70 on GMRES). The 2026-05-25 lesson "cross-function-boundary citation drift is NOT audit-tolerable; intra-function ±1-3 line drift IS" lived in lessons.md but had no skill, concept, or Critic check. **Skill-extraction candidate** — second skill the loop has proposed (after `classify-variant-axis` in meta-11).

## Cycles covered

- **Cycle 68** — `forward orthog L1→L2`. PASS.
- **Cycle 69** — `forward gmres L0→L1`. REVISE — citation-tightening on FgmresSolver::Mult; cross-function-boundary anchor mismatch.
- **Cycle 70** — `forward gmres L0→L1`. PASS — clean L0/L1 rotation chain.
- **Cycle 71** — `forward gmres L1→L2`. PASS — but emitted 6 retroactive claims against cycle-21/24 L2 surface.
- **Cycle 72** — `forward gmres L1→L2`. PASS — 5 more retroactive claims same surface.
- **Cycle 73** — `forward gmres L2→L3`. PASS — 2 obstruction claims on Givens replay + back-solve.

Token usage: ~315k input / ~75k output.

## Friction summary

### LOW (1)
- **Cycle 69 same-cycle edit-then-edit anchor brittleness.** Two `file_edits` in one plan where the second's `old_string` reflected pre-first-edit state. Distinct from the meta-12 create-then-edit case (this is edit-then-edit on a non-create file).

### MEDIUM (2)
1. **Retroactive_claims granularity regression.** Recurrence #2 of meta-12 item 1, now with granularity dimension. Per-building-block claims should be emitted at original layer landing, not backfilled later.
2. **Citation-tightening as unnamed recurring L0→L1 self-rotation.** Skill-extraction candidate — codify the cross-function-boundary check as `skills/verify-citation-range/SKILL.md`.

### HIGH (0)
None.

## Plan as enacted

| Item | Source friction | Enacted change | Deviation |
|---|---|---|---|
| (LOW) Same-cycle edit-then-edit | Cycle 69 anchor brittleness | `prompts/synthesizer.md` Same-cycle subsection extended with edit-then-edit guidance: second edit's `old_string` must reflect post-first-edit state. Two clean forms: chain into one combined edit, OR keep `old_string` ≤3 lines with no region overlap. | None |
| (1) Per-building-block granularity | 50-cycle gap from cycle 21 GMRES L2 landing to per-block backfill in 71-73 | (a) `prompts/synthesizer.md` Rotation-claim subsection extends with per-building-block rule: K visibly-named blocks → ≥K rotation_claims. Building-block defined: distinct named primitive/role/constructed-operator surface at L_{n+1} with its own paragraph/sub-section. (b) `prompts/critic.md` check #13 strengthened with part (c) granularity gate, tolerance ±1. | None |
| (2) NEW SKILL `verify-citation-range` | Cycles 69-70 citation drift; lesson lived only in lessons.md | `skills/verify-citation-range/SKILL.md` created with 5-step procedure (retrieve source, identify named symbol, verify lexical containment, catch drift, multi-specialization split). Two worked examples (FgmresSolver::Mult 733-875 → 733-871; GeneratePlaneRotation split to real + complex specializations). `prompts/synthesizer.md` cross-references the skill. | None — **second skill the loop has proposed**, validating the skill-extraction directive in the Meta-Critic prompt. |

## Recurring patterns and the watch list

- **Per-building-block granularity** — codified. Watch whether new layer emissions in cycles 74+ emit K+ claims (where K = building-block count).
- **Citation-range skill uptake** — watch whether Explorer/Synthesizer cycles touching L0 citations actually consult the skill. If skill silently ignored, the rule-as-skill is weaker than rule-as-prompt; revisit the skill-extraction approach if so.
- **Same-cycle edit-then-edit** — guidance added. Watch for recurrence in a different sub-mode.
- **Self-tightening grind** — meta-15 termination held; no recurrence in this window.
- **Loop meta-awareness** — cycle 62 self-citing recurrence #5 (meta-15 context) shows the methodology is now self-aware. This batch saw the Planner consistently citing prior friction by cycle number.

## Phase 6+ status

5 active slices: cg (L4), gmres (L4 + L0/L1/L2/L3 re-audited cleanly), orthog (L4), divfree (L3 + L4 pending), chebyshev (L3 + L4 pending). 24 concepts auto-maintained in index. 2 skills active.

## Approval

Approved by human (auto-approval mode); enactment by Claude Code with no deviations.
