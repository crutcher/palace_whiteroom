# Meta-review 2026-05-25 — cycles 44–49

(Twelfth meta-review. First meta-review after Phase 6 DONE.)

**Window:** cycles 44–49.
**Trigger:** automatic.
**Status:** enacted.

## Context

Big substantive batch: cycle 45 landed GMRES L4 fully; cycles 46/47 lifted divfree/orthog to L3; cycle 44 took chebyshev L1→L2. Cycle 48 started a new sub-slice (preconditioned CG L0→L1) but file_edit failed on a same-plan created file (anchor built from intended emission, not disk).

The meta-11 enacted artifacts worked: the `classify-variant-axis` skill exists; the retroactive-claims discipline (check #12) fires. But skill *uptake* was not visible — multi-variant cycles (chebyshev 4th-kind vs 1st-kind) did not produce a `## Variant axes` block. Skill body is being silently consulted, silently ignored, or relied on only via the inline 2-line summary — no way to tell.

Also: 5 of 6 cycles in this window were `retroactive_claims`. The meta-11 evidence-quoting requirement (check #12) made them auditable but didn't reduce their frequency. Synthesizers are deferring `rotation_claims` from the original emission cycle to subsequent backfill cycles. This is a discipline gap addressable producer-side.

## Cycles covered

- **Cycle 44** — `forward chebyshev L1→L2`. PASS. Polynomial-step primitives + variant-scalar generator emitted; classify-variant-axis skill silently consulted (or not — no Variant axes block).
- **Cycle 45** — `forward gmres L3→L4`. PASS. **GMRES L4 fully landed** with state-stratification, solve-monad vocabulary from the L4 calculus.
- **Cycle 46** — `forward divfree L2→L3`. PASS. Project-onto-kernel obstruction recorded.
- **Cycle 47** — `forward orthog L2→L3`. PASS. MGS/CGS/CGS2 lifted to field-level forms preserving variant-divergence at L3.
- **Cycle 48** — `forward cg L0→L1` (preconditioned variant). REVISE — file_edit anchor mismatch on same-plan-created cg.md.
- **Cycle 49** — `back cg L1`. PASS — restored anchor.

Token usage: ~250k input / ~60k output.

## Friction summary

### LOW (1)
- **Cycle 48 same-cycle create+edit anchor mismatch.** Synthesizer created cg.md in the plan and then emitted a file_edits whose 14-line monadic-pseudocode old_string didn't match the emitted content verbatim. Distinct failure mode from the spec/index.md row recurrence (resolved by `slice_index_updates` in meta-10).

### MEDIUM (3)
1. **Retroactive_claims cycles still dominate** (5 of 6 in window). Auditability gap closed by meta-11; *frequency* gap remains. Synthesizers defer claims rather than emitting at content time.
2. **Skill uptake of `classify-variant-axis` is unmeasurable.** Multi-variant cycles in this window did not produce the prescribed `## Working Notes` block. No mechanism distinguishes "silently consulted" from "ignored."
3. **Same-cycle create+edit anchor mismatch** (cycle 48 promoted from LOW above). Recurrence-1 of a specific producer-side gap. The Synthesizer's file_edits old_string was built from intended emission text rather than the actually-emitted text; orchestrator-side defense can catch this category of bug.

### HIGH (0)
None.

## Plan as enacted

| Item | Source friction | Enacted change | Deviation |
|---|---|---|---|
| (LOW) Same-cycle create+edit prompt note | Cycle 48 brittleness | `prompts/synthesizer.md` "Same-cycle create-then-edit" subsection added: prefer folding into create content; if separate file_edits, keep `old_string` ≤3 lines, no fenced-block boundary, no trailing whitespace. | None |
| (1) Original-emission claim discipline | 5/6 cycles retroactive_claims | (a) `prompts/synthesizer.md` "Rotation-claim emission at content time" subsection: plans with `plan_kind ∈ {new_content, back_correction}` and layer-content writes MUST emit `rotation_claims` for those layer edges. (b) `prompts/critic.md` new check #13: verdict revise on empty rotation_claims for layer-content emissions. (c) `schemas/critic_verdict.json`: extend `exercised_checks.check` max to 13. | None |
| (2) `## Variant axes` block contract | Skill uptake unmeasurable | `skills/classify-variant-axis/SKILL.md` output contract refined: when L0 source has ≥2 axis values, the slice MUST include a `## Variant axes` block in L1 (or L2 if divergence first surfaces there). Critic check #9 refined: verify the block presence when L0 source has visible axis variability. The block makes skill uptake measurable. | None |
| (3) Same-cycle create+edit integrator merge | Cycle 48 producer-side gap | `orchestrator/loop.py` integrator phase 0 (new): scan file_edits for paths that match same-plan slice_writes/concept_writes creates; fold the find/replace into the create payload before the create runs. If find/replace fails (old_string absent in create content), reject with explicit error message ("anchor not present in create content; fold or remove"). Caught at integrator level so the create+edit pattern is either silently merged or loudly rejected — no more silent failure. | None |

## Recurring patterns and the watch list

- **Skill uptake** — was unmeasurable; now structurally measurable via `## Variant axes` block presence. Watch cycles 50-55: do multi-variant cycles produce the block?
- **Retroactive_claims frequency** — should drop sharply with check #13 enforcement. If cycles 50-55 still have ≥50% retroactive_claims with layer-content writes, the upstream issue is in Planner dispatch (not Synthesizer discipline).
- **linkcheck2 false-positive on reference/ + skills/** — bonus fix in book.toml: exclude these paths from linkcheck2 validation. Citation links to `reference/<repo>/<file>.cpp#L<n>-L<m>` are agent-meaningful but linkcheck2 treats the `#L<n>-L<m>` fragment as part of the filename. Excluding from validation is the right call — linkcheck2 can't verify line numbers in source files anyway.
- **Same-cycle create+edit** — recurrence-1, addressed integrator-side. Watch for recurrence #2 in the new mode (find/replace fails on create content).

## Phase 6+ status

5 active slices on disk: cg (L4 + preconditioned-variant L1 in progress), gmres (L4), orthog (L3), divfree (L3), chebyshev (L2). 22+ concepts. Methodology now produces consistent forward progress across multiple slices in parallel.

## Approval

Approved by human (auto-approval mode); enactment by Claude Code with one build-config bonus fix (book.toml exclude rules for linkcheck2) required for the book to build.
