# Meta-review 2026-05-26 — cycles 116–127

(Twenty-second meta-review.)

**Window:** cycles 116–127.
**Trigger:** automatic.
**Status:** enacted.

## Context

Productive 12-cycle window with substantial slice progress and a methodology pattern surfacing: refinement-cycle discipline is eroding under the new push_kind. **Two refinement cycles (116 chebyshev, 125 gmres) both emitted rotation_claims describing surface changes with no actual surface edits and no retroactive_claim_evidence** — recurrence #2 of the cycle-115 evasion pattern. The meta-21 producer-side rule didn't engage; promotion to integrator-side enforcement is the standard escalation.

Also: a **new framework-tier slice** (`cg_preconditioning_framework`) was extracted organically at cycle 123 via retroactive-gate retry — not on the intermediate-tier list, but a related abstraction layer (preconditioner-as-LinOp interface). Three new concepts landed (`solver_as_operator`, `two_operator_split`, `complex_from_real_lift`). plane_rotation_stream advanced L1→L2 (cycle 124). cg fully traversed L1→L2→L3→L4 (cycles 117-119).

problems-sensitivity is now at cap=5 with 0/24 filings across two consecutive windows — the calibration knob has saturated.

## Cycles covered

- **116** `refinement chebyshev Ln→Ln`. REVISE (surface-or-evidence missing — cycle-115 pattern recurring).
- **117-119** cg L1→L2→L3→L4 chain (full stack).
- **120-122** divfree L1→L2 + L2→L3 (advancing).
- **123** original cg L4→L4 retroactive gate fired → retry succeeded: **forward cg_preconditioning_framework L0→L1** (new framework slice).
- **124** original cg L2→L3 retroactive gate → retry: **plane_rotation_stream L1→L2**.
- **125** `refinement gmres Ln→Ln`. REVISE (surface-or-evidence missing — recurrence #2 of 116/115 pattern).
- **126** chebyshev L1→L2 retroactive backfill.
- **127** original cg L1→L2 retroactive gate → retry: **arnoldi_step L2→L3**.

Token usage: ~430k input / ~115k output.

## Friction summary

### LOW (3)
1. Problems-sensitivity recalibration — 0/12 filings, at cap; flag for human review (saturation).
2. Roadmap updates: cg_preconditioning_framework new framework slice at L1; plane_rotation_stream advanced to L2.
3. plan_kind misclassification flag on cycle 118 (orchestrator now captures it — Synthesizer didn't self-correct).

### MEDIUM (4)
1. **Refinement surface-or-evidence rule recurrence #2** — promote producer-side rule (meta-21 item 4) to integrator-side enforcement.
2. **Edge-label fidelity + plan_kind discipline erosion under refinement push_kind** — auto-rewrite cross-layer edges to self-edges when no L_{n+1} prose lands; enforce zero-mode=create on retroactive_claims at integrator time.
3. **Skill trigger-detection refinement for classify-variant-axis** — under-firing on cycles 120-124 that discussed variant axes without ≥2 L0 axis values present.
4. **Problems-sensitivity saturation** — cap=5, 0/24 over two windows; surface for human review; add worked examples + decision protocol.

### HIGH (0)
None — though the sensitivity saturation is approaching escalation (3 consecutive windows would per the new protocol).

## Plan as enacted

| Item | Source | Enacted | Deviation |
|---|---|---|---|
| (LOW1) Sensitivity hold + saturation flag | 0/24 over two windows | `scaffolding/problems-sensitivity.md` calibration row appended; new **Saturation** section with 2-interpretation framing + 4 worked examples of borderline filing candidates + decision protocol (3rd consecutive 0/12 window → HIGH escalation). | None |
| (LOW2) Roadmap updates | cycles 123, 124 | `scaffolding/roadmap.md`: plane_rotation_stream L2 noted; new "Framework slices" section with cg_preconditioning_framework `[~]` at L1. | None |
| (LOW3) plan_kind misclassification flag | cycle 118 | Documented in this meta-review record; orchestrator capture already in place (meta-13). | None |
| (1) Refinement surface-or-evidence integrator gate | recurrence #2 | `orchestrator/loop.py`: when `push_kind=refinement` OR any rotation_claim has self-edge (regex `L<N>→L<N>` or `Ln→Ln`), check for (a) surface edit on slice OR (b) retroactive_claim_evidence in log_synthesis. If neither, set `apply_failed=True` and emit `refinement_surface_or_evidence_missing` push-back. Mirrors meta-19 SIDEWAYS auto-rewrite enforcement pattern. | None |
| (2) Edge-label fidelity + plan_kind integrator enforcement | meta-15 #2 erosion under refinement | Folded into plan item 1's integrator check — the refinement-surface gate catches both the no-surface and the misclassified-edge cases since both manifest as "self-edge claim without surface evidence." Plus orchestrator already catches plan_kind misclassification (meta-13). | Combined with item 1 rather than separate code path; achieves the same effect. |
| (3) Classify-variant-axis trigger refinement | cycles 120-124 under-firing | `prompts/critic.md` check #15 trigger language broadened: fires when cycle content discusses ANY variant axis (preconditioner-side, orthogonalization, polynomial family, flexible-vs-fixed, real-vs-complex, single-vs-block, framework-instance) regardless of L0 pre-fetch. Worked examples included. | None |
| (4) Problems-sensitivity saturation surface | 0/24 cap-reached | `scaffolding/problems-sensitivity.md` new Saturation section. Worked examples cite recent borderline cases from meta-19/20/21 + user directives. Decision protocol: 3 consecutive cap-zero windows → HIGH escalation. | None |
| (bonus) Book link fix | solver_as_operator referenced non-existent ../spec/slices/fgmres.md | sed-fixed to ../spec/slices/gmres.md#fgmres. Book builds clean. | None — required to build. |

## Recurring patterns and the watch list

- **Refinement-cycle discipline** — recurrence #2 of surface-or-evidence missing under refinement. Integrator-side enforcement landed. Watch cycles 128+ for refinement cycles producing actual surface edits or evidence.
- **Intermediate-tier prioritization compounding further** — 4 distinct slices in motion across the last 2 windows (arnoldi_step at L4, plane_rotation_stream at L2, sparse_triangular_solve at L1, cg_preconditioning_framework at L1 as a related framework-tier slice).
- **Retroactive-gate retry mechanism dominant** — 5 firings this window, all redirected successfully. The retry-once pattern has become the loop's primary way of breaking out of saturated-slice grinding.
- **Skill trigger-detection refinement** — classify-variant-axis under-firing; broadened trigger conditions should produce more visible uptake in next window.
- **Problems-sensitivity at cap with 0/24** — the calibration knob has saturated. If next window is also 0, escalate.

## Phase 6+ status

- Per-solver pipelines: 0/5 (unchanged).
- Krylov solvers: 2/4 at L4 (unchanged).
- Orthogonalisation: 1/2 (unchanged).
- Smoothers: 1/6 (chebyshev L1→L2 retroactive backfill this window).
- Projections: 0/2 (divfree at L3 — advanced L2→L3 this window).
- **Intermediate-tier algorithms: 1 at L4 (arnoldi_step), 1 at L2 (plane_rotation_stream), 1 at L1 (sparse_triangular_solve)** — 3/7 in motion.
- **NEW: Framework slices: 1 at L1 (cg_preconditioning_framework)** — separate tier from intermediate-tier.
- FE assembly: 0/4 (unchanged).
- Coordination/post-processing: 0/4 (unchanged).
- Methodology: 6 roles; 15 Critic checks; 3 skills; **9 active slices** (cg, gmres, orthog, chebyshev, divfree, arnoldi_step, plane_rotation_stream, sparse_triangular_solve, **cg_preconditioning_framework**); 25+ concepts.

## Approval

Approved by human (auto-approval mode); enactment by Claude Code. The methodology surface continues to compound. Framework slices are an organic new tier discovered by the loop; sensitivity saturation may surface a deeper methodology question in the next window.
