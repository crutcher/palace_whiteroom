# Meta-review 2026-05-26 — cycles 104–115

(Twenty-first meta-review.)

**Window:** cycles 104–115.
**Trigger:** automatic.
**Status:** enacted.

## Context

The richest window the loop has produced. Multiple methodology mechanisms fired for the first time:

- **Two new intermediate-tier slices extracted** via the retroactive-budget retry-once mechanism: `plane_rotation_stream` (cycle 108) and `sparse_triangular_solve` (cycle 112; emitted as a negative L1 — Palace doesn't expose this primitive directly). The retry rerouted the Planner from cg/gmres targets to forward-frontier intermediates.
- **Refinement push kind fired for the first time** — cycle 113 (`refinement orthog`) PASS, cycle 115 (`refinement arnoldi_step`) revise. The user 2026-05-26 directive on Refinement is now end-to-end validated, with cycle 113 working cleanly and cycle 115 exposing a surface-or-evidence discipline gap.
- **SIDEWAYS landed substantive content** — cycle 114 (cg vs gmres on variant-absorption strategy) PASS.
- **Global retroactive counter caught real cases** — gate fired 4 times (cycles 108, 109, 111, 112) and the retry-once mechanism redirected to forward-frontier each time, all succeeding.

## Cycles covered

- **104** `forward gmres L4→L4` (self-tightening). REVISE.
- **105** `forward cg L1→L2`. PASS.
- **106** `forward cg L2→L3`. REVISE (concept-append H1-vs-H2 failure on sequential-obstruction).
- **107** `forward cg L2→L3` (retry). REVISE (same H1-vs-H2 failure).
- **108** original: `forward gmres L4→L4`. RETROACTIVE GATE FIRED (0 consec on slice — gate trigger was global counter passing 4); retry succeeded with **`forward plane_rotation_stream L0→L1`**. PASS.
- **109** original: `forward cg L2→L3`. RETROACTIVE GATE FIRED (3 consec on cg); retry succeeded with **`forward arnoldi_step L0→L1`**. PASS.
- **110** `forward gmres L3→L4`. PASS.
- **111** original: `forward cg L3→L4`. RETROACTIVE GATE FIRED (3 consec on cg); retry succeeded with `forward divfree L1→L2`. PASS.
- **112** original: `forward cg L3→L4`. RETROACTIVE GATE FIRED (3 consec on cg); retry succeeded with **`forward sparse_triangular_solve L0→L1`** (negative result). PASS.
- **113** **`refinement orthog Ln→Ln`**. PASS.
- **114** `sideways cg,gmres` (variant-absorption axis). PASS.
- **115** **`refinement arnoldi_step Ln→Ln`**. REVISE — surface-or-evidence gap.

Token usage: ~440k input / ~110k output across the 12-cycle window.

## Friction summary

### LOW (3)
1. **Problems-sensitivity recalibration**: 0/12 filings vs 0.067 target. Currently at 4; still below 0.5× target → +1 (now at cap 5).
2. **Roadmap update**: plane_rotation_stream + sparse_triangular_solve `[ ]` → `[~]` (L0→L1 landed).
3. **Concept-append H1 vs H2 discipline**: cycles 106/107 emitted `# Sequential obstruction` (H1) when `## ...` (H2) was required after auto-rewrite. Synthesizer-side rule.

### MEDIUM (4)
1. **Concept-append H1→H2 auto-normalization**: the integrator's create→append-section auto-rewrite (meta-19 item 2) was channel-correct but didn't normalize the heading level. Two wasted cycles. Integrator-side fix: H1 → H2 promotion during auto-rewrite.
2. **Schema gap for self-rotation edges**: cycles 113/115 emitted `edge=Ln→Ln` but the rotation_claim schema's edge enum only accepted L_n→L_{n+1}. The cycle-115 Critic explicitly flagged this as a problems/ candidate. Formalize self-edges in the schema.
3. **Skill_uptake aggregate stats**: meta-20 confirmed emission via `skill_uptake_emitted: bool`; the array contents are still opaque in episodic. Add `skill_uptake_triggered / _applied / _missed` counts + summary list to episodic so the Meta-Critic can audit per-skill rates.
4. **Self-rotation/refinement surface-or-evidence discipline**: cycle 115 narrated an L1 prose edit in `log_synthesis` but the diff contained only lessons + rotation_claim. The meta-15 `plan_kind=tightening` evasion pattern recurring under `push_kind=refinement`. Producer-side rule.

### HIGH (0)
None.

## Plan as enacted

| Item | Source | Enacted change | Deviation |
|---|---|---|---|
| (LOW1) Sensitivity 4→5 | rate 0/12 still below 0.5×target | `scaffolding/problems-sensitivity.md` updated; calibration history extended. **Now at cap**; if next window is also 0, the bar may be structurally too high — surface for review. | None |
| (LOW2) Roadmap intermediate-tier `[ ]`→`[~]` | cycles 108, 112 | `scaffolding/roadmap.md` updated for plane_rotation_stream + sparse_triangular_solve. | None |
| (LOW3) H1→H2 prompt discipline | cycles 106/107 | `prompts/synthesizer.md` — note added in the Concept-append-heading-level subsection. | None |
| (1) H1→H2 integrator auto-normalize | concept-append channel-rewrite mismatch | `orchestrator/loop.py` auto-rewrite phase 00: when content's first non-blank line is `# X`, rewrite to `## X` before the channel migration. Push-back signal records the normalization. | None |
| (2) Self-rotation edges in schema | cycles 113/115 | `schemas/rotation_claim.json`: `edge` enum extended to include L0→L0, L1→L1, L2→L2, L3→L3, L4→L4, Ln→Ln. Documents the self-edge as first-class (not a workaround). | None |
| (3) Skill_uptake aggregate stats | meta-20 + this window | `orchestrator/loop.py` episodic record now includes `skill_uptake_triggered`, `_applied`, `_missed`, `_summary`. Meta-Critic gets per-cycle skill-uptake counts in episodic without re-reading verdicts. | None |
| (4) Refinement surface-or-evidence rule | cycle 115 evasion | `prompts/synthesizer.md` new "Self-rotation / refinement surface-or-evidence discipline" subsection: when `push_kind=refinement` OR `edge ∈ {L_n→L_n}`, plan MUST include actual surface edit OR retroactive_claim_evidence. Cross-referenced check #14. | None |
| (bonus) Book link fix | plane_rotation_stream references ./fgmres.md (non-existent) | sed `s|(./fgmres.md)|(./gmres.md#fgmres)|` on plane_rotation_stream.md. Book builds clean. | Not in plan; required to build. |

## Recurring patterns and the watch list

- **Intermediate-tier prioritization compounding**: 3 of 7 candidates extracted (arnoldi_step done; plane_rotation_stream + sparse_triangular_solve at L1). The retroactive-gate retry-once is now the dominant mechanism for picking intermediates. Watch the next windows for these maturing toward L4 and for additional candidates (polynomial-recurrence-step, diagonal-preconditioner-apply, residual-update, restart-machinery) being picked.
- **Refinement** firing for the first time validates the user directive. Watch for the surface-or-evidence rule reducing the cycle-115-style evasion.
- **Global retroactive counter** fired 4 times in this window — exactly the rotate-through-slices pattern meta-20 plan item 2 targeted. Working as designed.
- **Problems-sensitivity at cap** with 0 filings across two full windows — if next window is also 0, the agents are structurally not filing rather than the bar being wrong. Surface for review.

## Phase 6+ status

- Per-solver pipelines: 0/5 (unchanged).
- Krylov solvers: 2/4 at L4 (unchanged).
- Orthogonalisation: 1/2 (unchanged).
- Smoothers: 1/6 (unchanged).
- Projections: 0/2 (divfree L1→L2 advanced this window — cycle 111 retry).
- **Intermediate-tier algorithms: 1 at L4 (arnoldi_step), 2 at L1 (plane_rotation_stream, sparse_triangular_solve)** — 3 of 7 in motion.
- FE assembly: 0/4 (unchanged).
- Coordination/post-processing: 0/4 (unchanged).
- Methodology: 6 roles; 15 Critic checks; 3 skills; **8 active slices** (cg, gmres, orthog, chebyshev, divfree, arnoldi_step, **plane_rotation_stream**, **sparse_triangular_solve**); ~25 concepts.
- Phase progression: Phases 0-6 done; Phase 6+ in flight.

## Approval

Approved by human (auto-approval mode); enactment by Claude Code. The methodology surface continues to compound — refinement, SIDEWAYS, retroactive-gate-redirect, and skill_uptake all observable in this window for the first time.
