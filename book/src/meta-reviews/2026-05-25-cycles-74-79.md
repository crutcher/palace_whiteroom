# Meta-review 2026-05-25 — cycles 74–79

(Seventeenth meta-review. First with the README-builder trigger active.)

**Window:** cycles 74–79.
**Trigger:** automatic.
**Status:** enacted.

## Context

Substantive batch across multiple slices:

- **Cycle 78 landed chebyshev L4** — chebyshev is now the fourth slice to complete the full layer stack (after cg, gmres, orthog).
- gmres L4 emission again (cycle 75); cg L4 tightenings (74, 76); orthog L2→L3 (77).
- Cycle 79 SIDEWAYS cg+gmres surfaced the **fourth recurrence** of SIDEWAYS channel-selection failure (mode=create on existing files). Producer-side rules across meta-9/11/13/16 have not converged; the meta-11 watch trigger "recurrence #4 → integrator-side enforcement" has fired.

The window also regressed on retroactive_claims (5 of 6 cycles after dropping to 2/6 in meta-14). The meta-16 per-building-block granularity rule made backfill cycles productive, inadvertently incentivizing more backfill dispatch.

## Cycles covered

- **Cycle 74** — `forward cg L4→L4` (tightening derived-view hoisting). PASS.
- **Cycle 75** — `forward gmres L3→L4`. PASS.
- **Cycle 76** — `forward cg L4→L4` (more derived-view tightening). PASS.
- **Cycle 77** — `forward orthog L2→L3`. PASS.
- **Cycle 78** — `forward chebyshev L3→L4`. **PASS — chebyshev at L4.**
- **Cycle 79** — `sideways cg,gmres`. PASS→REVISE (downgrade): mode=create on existing slices + 5 core concepts.

Token usage: ~310k input / ~75k output.

## Friction summary

### LOW (1)
- **Concept create-on-existing recurrence #6+** (cycle 79: apply_linop, axpy, dot, constructed-operators, variant-absorption). Five core concepts the loop has had since cycles 20-24. Exemplar list expansion only.

### MEDIUM (3)
1. **SIDEWAYS channel-selection recurrence #4.** Cycles 22, 25, 40, 79. Producer-side rules have not converged. The meta-11 watch trigger "if recurrence #4 fires, escalate to integrator-side check or tooling intervention" has fired.
2. **Retroactive_claims regression** (5/6 from a 2/6 baseline in meta-14). Planner is dispatching backfill cycles rather than forward edges because meta-16 made backfills productive. The codified check #13 catches missing claims on new_content but doesn't constrain Planner's choice of dispatch.
3. **Skill-uptake invisibility** for both `verify-citation-range` (meta-16) and `classify-variant-axis` (meta-11). Two skills exist; neither has produced a visible artifact since extraction.

### HIGH (0)
None. Recurrence #4 of SIDEWAYS would normally escalate per meta-11 watch; the watch instruction itself said "integrator-side check OR tooling intervention" — both are Medium-cascade. Enacted as Medium.

## Plan as enacted

| Item | Source friction | Enacted change | Deviation |
|---|---|---|---|
| (LOW) Concept-existence exemplar list expansion | Cycle 79 recurrence #6+ on core concepts | `prompts/synthesizer.md` Concept-existence-check rule sharpened: lists all BLAS-style and methodology concepts as known-existing; explicit SIDEWAYS-MUST-NOT-create-them rule. Notes integrator-side enforcement (item 1) makes this a hard gate. | None |
| (1) Integrator-side enforcement of concept-existence | SIDEWAYS recurrence #4 | `orchestrator/loop.py`: concept_writes mode=create on already-existing concept page is no longer a silent skip — now `_record_fail("concept_write_create_on_existing", ...)` with a structured push-back signal naming the substitution. Verdict downgrades on these failures (preserves the prior silent-skip behavior was masking the issue from the Critic). | None |
| (2) Retroactive-backfill budget | 5/6 retroactive regression | `prompts/planner.md` new "Retroactive-backfill budget" subsection: when ≥2 of last 3 cycles on a slice were retroactive_claims, next push MUST be FORWARD to un-claimed edge OR SIDEWAYS. Anti-grind precedence preserved. | None |
| (3) Skill-invocation visibility | Two skills, zero visible uptake | `prompts/critic.md` new check #15: when a cycle's content matches a skill's trigger condition (≥2 axis values for classify-variant-axis; any L0 citation edit for verify-citation-range) AND the prescribed artifact is missing AND log_synthesis does NOT explain why, verdict revise. Pure visibility — surfaces skill uptake to episodic; doesn't enforce invocation. `schemas/critic_verdict.json` extends max check to 15. | None |

## Recurring patterns and the watch list

- **SIDEWAYS channel-selection** — recurrence #4 now enforced integrator-side. Watch cycles 80+: if a SIDEWAYS attempt trips the new gate, the failure is now LOUD with actionable text; if SIDEWAYS cycles still emit mode=create after the prompt update + integrator gate combined, escalate to a separate Critic-side semantic check.
- **Retroactive_claims dispatch** — budget rule added. Watch cycles 80-85 for the frequency dropping back toward the meta-14 baseline (2/6).
- **Skill uptake** — check #15 makes it measurable. Watch for: (a) skill-invoked artifacts appearing in slice prose, (b) explanations of non-applicability in log_synthesis, (c) the verdict-revise rate on artifact-missing-AND-no-explanation cases.
- **chebyshev at L4** — fourth slice past L4 milestone. Only divfree remains at L3 with L4 still in flight.

## Phase 6+ status

5 active slices: **cg, gmres, orthog, chebyshev at L4**; divfree at L3 + L4 in flight. 24 concepts auto-indexed. 2 skills active. Methodology surface stable; the major remaining work is the divfree L4 landing and the SIDEWAYS comparison work that surfaces cross-slice unification.

## Approval

Approved by human (auto-approval mode); enactment by Claude Code with no deviations. **The README-builder prompt (commit e573369) takes effect this meta-cycle** — `README.md` regenerates as the final step.
