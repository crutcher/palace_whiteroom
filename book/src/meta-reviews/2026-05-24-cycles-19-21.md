# Meta-review 2026-05-24 — cycles 19–21

(Seventh meta-review.)

**Window:** cycles 19–21.
**Trigger:** automatic.
**Status:** enacted.

## Context

This is the round GMRES finally landed. Cycle 20 created `book/src/spec/slices/gmres.md` (180 lines, L1 form complete) plus a new `book/src/concepts/incremental_least_squares.md`. Cycle 21 extracted SEVEN support-operator concepts (`apply_linop`, `axpy`, `dot`, `givens`, `nrm2`, `scal`, `trsv`) — the bottom-up vocabulary the methodology calls for. The Critic gave both cycles `pass` on content; cycle 21 was orchestrator-downgraded because the L2-section append to `gmres.md` failed to apply (no channel fit the topology) and the dep-map edges had no L2 mermaid block to write into.

Two clean tooling extensions plus one Planner-prompt revision recover the lost content.

## Cycles covered

- **Cycle 19** — `forward chebyshev [L0→L1]` (auto-downgrade-from-#16-batch refile). PASS. The `file_edits` channel from meta-review #6 worked: chebyshev.md was modified in-place (22 net lines added). First confirmed on-disk edit via `file_edits`.
- **Cycle 20** — `forward gmres [L0→L1]`. PASS. **gmres.md created on disk** (180 lines) plus `incremental_least_squares.md` concept (1 new concept). Anti-grind heuristic remained correctly applied (Planner chose gmres because leaf primitives now provide L2-substitutable vocabulary, not because of grind).
- **Cycle 21** — `forward gmres [L1→L2]`. PASS→REVISE (downgrade). The Critic verdict was `pass` based on content; the orchestrator downgraded because:
  - The L2 section-append to `gmres.md` used `slice_writes` mode=diff with a non-line-numbered `@@`; `git apply` rejected it.
  - 8 of 8 `dependency_map_edges` targeting `## L2 —` failed because `dependency-map.md`'s L2 section had no `mermaid` block (only prose).
  - 7 NEW concept files (apply_linop, axpy, dot, givens, nrm2, scal, trsv) DID land successfully via `concept_writes` mode=create.

The cycle-21 L2 prose itself was high-quality (5 building-block unfoldings, primitive sequence, numerical-claim register distinguishing load-bearing rotation algorithm and column-update order from transparent fusions). `verdict_original = pass` correctly preserved this judgment.

Token usage: ~70k input / ~25k output across cycles 19-21.

## Friction summary

### LOW (0)
None.

### MEDIUM (3)

1. **Section-append topology** doesn't fit any current channel. `file_edits` requires a unique anchor (no good one for end-of-file); `slice_writes mode=diff` is the unreliable diff path. Section-append is the natural form of an L_n→L_{n+1} forward push on an established slice (the new layer section goes to the end of the file).
2. **Dep-map auto-init** for empty layer sections. `dependency-map.md`'s `## L2 —` section had only prose at cycle-21 emission time; all 8 edge inserts crashed because `add_dependency_map_edge` requires a `mermaid` block to write into.
3. **SIDEWAYS never fired in 21 cycles.** Trigger conditions clean since cycle 11 (three leaf slices at L1 with no friction). Meta-review #6 watch explicitly said cycles 19-21 was the escalation point — trigger met.

### HIGH (0)
None.

## Plan as enacted

| Item | Source friction | Enacted change | Deviation from proposal |
|---|---|---|---|
| (1) `section_appends` channel | Cycle 21 L2-to-gmres.md failure | New field in `schemas/integration_plan.json`: array of `{path, heading, content}`. Integrator handler in `loop.py`: `state.append_section(rel, heading, content)` with idempotency check on heading line. Safe-path policy same as file_edits. Synthesizer prompt updated with section_appends channel selection rule. | None |
| (2) Dep-map mermaid auto-init | Cycle 21 8 dep-map edge failures into empty L2 section | `state.add_dependency_map_edge` now auto-initializes an empty `mermaid` block (`graph BT`) in the target layer section if missing, then proceeds with the edge insert. Idempotent. | Not in Meta-Critic plan — added as a small bonus fix that directly addresses cycle 21's specific failure. The Meta-Critic's plan item 1 was about plan-internal phase ordering, which the current order already satisfies (structural → relational); the dep-map's empty-mermaid case is orthogonal and now resolved at the integrator level. |
| (3) Integrator phase order documented | Meta-Critic plan item 1 | Synthesizer prompt now documents the integrator's 5-phase order: structural creates → structural appends → in-place edits → relational dep-map → cross-cycle records. Synthesizer can reason about plan validity (a single plan can section_append + add dep-map edges to the new section because phases 2 and 4 run in that order). | None — current code already applied operations in this order; this just makes it visible to the Synthesizer. |
| (4) SIDEWAYS strengthening | 21 cycles, escalation point | `prompts/planner.md` criterion 4 rewrites: concrete trigger ("≥2 slices at same edge with pass + no open friction → SIDEWAYS is DEFAULT"); action description ("compare invariants, vocabularies, ownership; surface shared primitives for concept extraction"); **anti-procrastination clause: if SIDEWAYS hasn't fired in ≥10 cycles AND trigger conditions hold, the Planner MUST emit SIDEWAYS next**. | None |

## Recurring patterns and the watch list

- **Diff-apply mismatch** — pattern now closed across THREE sub-modes:
  - new files (meta-review #5: `file_creates`/`slice_writes mode=create`)
  - in-place edits (meta-review #6: `file_edits`)
  - section appends (meta-review #7: `section_appends`)

  The diff channel (`slice_writes mode=diff`) survives as last-resort escape hatch for multi-hunk changes within existing sections, but is no longer the primary edit path. Watch: if cycles 22+ continue to attempt diffs over reachable alternatives, surface as a Synthesizer-prompt fix.

- **Same-slice grind** — anti-grind worked, planner has been rotating cleanly. Pattern is closed under the current rule.

- **SIDEWAYS** — must fire in the next cycle eligible by criterion 4. If it doesn't fire, the anti-procrastination clause is broken and requires a meta-cycle escalation.

- **Frictionless-pass sanity / Critic exercised-checks enumeration** — Critic correctly verdicted pass on cycles 19-21 content but did NOT include exercised-checks annotations (meta-review #6 plan item 2). Watch: if cycles 22+ continue without exercised-check annotations, surface as a Critic-prompt fix.

- **Rotation-as-renaming**, **Variant-absorption cluster**, **Meta-Critic file-path drift** — STILL CLEAR.

## Phase 6 DONE scoreboard

| Criterion | Status |
|---|---|
| GMRES pushed to L4 | partial: L1 ✓ (cycle 20), L2 prose ready but unapplied (cycle 21); L3, L4 still to do |
| First 3 rotation_claims hand-verify | possible — claims on disk in cg/orthog/divfree/chebyshev/gmres |
| ≥1 BACK push | ✓ |
| ≥1 SIDEWAYS push | ✗ (anti-procrastination clause should force it in cycles 22-23) |
| Meta-review fires | ✓✓✓✓✓✓✓ (7 times) |

## Approval

Approved by human; enactment by Claude Code with no substantive deviations from the proposal. The mermaid-auto-init addition is an additive fix that directly resolves cycle-21's dep-map crash; it does not conflict with any plan item.
