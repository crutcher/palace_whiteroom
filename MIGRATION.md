# MIGRATION.md — structural-redirect spec (history-stub)

> **COMPACTED TO A HISTORY-STUB (2026-06-05, batch-31 meta-phase, user directive.)**
>
> This was the Phase A working plan for the **2026-05-26 structural redirect** — migrating from the old loop (Python orchestrator + slice-vertical artifact) to the new shape (Claude-subagent coordination + an L4→L0 layered artifact with themed lowering passes). It specified, in full:
> - **§1 What we left:** the Python orchestrator under `orchestrator/`, the prompted-role flow, the integration-plan channel architecture, the integrator pipeline gates, the original skills + numbered Critic checks, the meta-review record, the `episodic.jsonl` record.
> - **§2 What we went to:** the layered L4→L0 nine-Part mdBook structure, the themed lowering layers, the 6-phase Claude-subagent cycle, the write-authority partition, the methodology invariants.
> - **§3+ the migration steps** that carried the Phase-1 slice corpus + concept library into the new layered artifact.
>
> The redirect is **complete and fully internalized**: `CLAUDE.md` is its operational distillation, the `METHODOLOGY-REDIRECT.md` / `METHODOLOGY-GRADED-STACK.md` / `METHODOLOGY-CHANGELOG.md` notes carry the subsequent course-corrections, and the `scaffolding/` workshop carries the live state. The Phase-1 slice corpus the migration moved was itself fully lifted and deleted across cycles 097–099 (corpus 9→0). The original 741-line plan is **no longer load-bearing**.

**Full original text is preserved in git history.** Retrieve with:

```
git show c83e04f:MIGRATION.md      # last revision carrying the full 741-line plan
```

(any commit up to and including `c83e04f` carries the full blob; the file was last substantively edited at `8ac1f37`). For current methodology read `CLAUDE.md` + the `METHODOLOGY-*.md` notes.
