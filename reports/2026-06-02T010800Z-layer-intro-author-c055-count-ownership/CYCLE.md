---
agent: layer-intro-author
invoked_at: 2026-06-02T010800Z
scope: cycle-055 (batch-17) consolidated-count owner — L4/index, L4-L3/index, L1/index tallies + cohort/frontier narratives
status: integrated
integrated_at: 2026-06-02T034000Z
integration_commit: e9bbbbf9fcee8786ad94305a482f6835d2e0f40b
integration_notes: "D7 cycle-055, PARTIALLY-APPLIED per dispatch. Applied edit #1 (L4/index rough-in tally 0→1 for solve_family, L4 firm STAYS 6, retired stale near-exhausted-frontier prose) + edit #3 (L1/index FE-cohort header flip all-3-firm + grand total 27→29). Edit #2 (L4-L3 tally) deliberately SKIPPED — superseded by D8 (it had trusted the stale L4-L3 table). Discretionary producer-bullet dedup (did not re-emit the 3 FE bullets already on disk at :72-74). The repairer's needs-revision on edit #2 was the correct catch."
---

# CYCLE: cycle-055 consolidated counts (D7 — sole count-owner)

## Summary

I am dispatch **D7** of cycle-055 (batch-17, under the 2026-06-01 VOCABULARY-SHIFT REDIRECT), the **SOLE consolidated-count owner** for the wave. D1–D6 deferred ALL consolidated tallies + cohort/frontier narratives to me; they each own only their own dep-map rows, §Vocabulary-cohort bullets, and SUMMARY.md entries. This report proposes the consolidated count/narrative edits in three indexes, each reconciled against the **ACTUAL on-disk landed state** (not the planner's projections — several came out differently).

**Counts reconciled FROM ON-DISK (read this dispatch):**

1. **`book/src/L4/index.md`** — L4 firm UNCHANGED at **6** (D1's `solve_family` landed `## Status: rough-in (test-coverage-bounded)`, verified against D1's report — NOT firm). The "Rough-in at L4 (0)" line flips to **(1)** (`solve_family`). The §Vocabulary-cohort header + the stale "substantially complete / near-exhausted" L4-frontier prose are reworded to reflect the **active solver-test-load frontier** (`solve_family` is the first solver-driven L4 combinator generating genuine new spine vocabulary).
2. **`book/src/L4-L3/index.md`** — **CORRECTION TO THE DISPATCH'S "6→7" PROJECTION.** The on-disk L4-L3 theme-list table carries **3 firm** rows (`iterate-while-dissolution`, `iterate-while-with-prev-dissolution`, `ksp-solve-driver-dissolution`), NOT 6. (The dispatch said "6→7"; the planner's "6" does not match the on-disk table.) D2's `solve-family-map-dissolution` lands firm, so the on-disk-truthful tally is **3→4 firm**. I author the consolidated tally on D2's seeded §Vocabulary-cohort section reflecting 4 firm. The "6" vs on-disk-"3" divergence is flagged as an OQ (likely the planner conflated the L4-index narrative prose, which mis-labels `fgmres-inner-loop` firm, with the L4-L3 table, which has it rough-in).
3. **`book/src/L1/index.md`** — the FE-assembly sub-spine subsection header (`:70`) flips from "Rough-in (FE-assembly sub-spine)" to "Firm (FE-assembly sub-spine)": all 3 members are now firm (`fe_assemble` firm c054 + D3's `eliminate_rhs` firm + D4's `eliminate_essential_bc` firm). The main "Firm (26)" cohort header (which enumerates exactly 26 main-cohort bullets, NOT including the FE sub-spine) gets a grand-total annotation: **27 firm grand total → 29** (26 main-cohort + `fe_assemble` = 27 before this cycle; + the 2 `eliminate_*` = 29). D5's libCEED-boundary obstruction is noted in the FE-thread narrative.

## Proposed changes

### 1. `book/src/L4/index.md` — L4 firm UNCHANGED at 6; rough-in (0)→(1); active-frontier reword

```edit:book/src/L4/index.md
[old]: ## Vocabulary cohort

**Firm at L4 (6 + 4 outer-driver)** — the typed-wrapper Krylov step kernel, the two value-threading loop combinators that drive it, the fixed-degree polynomial smoother, and (cycle-048) the two iterative-solve outer-driver **caps** (`ksp_solve`, `eigsolve`); plus the four `solve-monad` outer-driver vocabulary anchors:
[new]: ## Vocabulary cohort

**Firm at L4 (6 + 4 outer-driver)** — UNCHANGED this cycle (cycle-055 added one *rough-in* combinator, `solve_family`, not a firm entry): the typed-wrapper Krylov step kernel, the two value-threading loop combinators that drive it, the fixed-degree polynomial smoother, and (cycle-048) the two iterative-solve outer-driver **caps** (`ksp_solve`, `eigsolve`); plus the four `solve-monad` outer-driver vocabulary anchors:
```

```edit:book/src/L4/index.md
[old]: **Rough-in at L4 (0)** — none currently.
[new]: **Rough-in at L4 (1)** — the first solver-test-load-driven combinator, awaiting law confirmation:

- [`solve_family`](./solve_family.md) *(rough-in (test-coverage-bounded))* — the fixed-operator **map-over-RHS-family outer-driver combinator** (capture the system operator `op` once / `SetOperators(*K,*K)` hoisted outside the loop, build the solver once, map the [`ksp_solve`](./ksp_solve.md) cap over the RHS family `[rhs_i]`, collect `[x_i]`). The **pure-`map` degenerate** of the strawman §3.7 [`iterate-while`](./iterate-while.md) family (each element independent, no carry; the trajectory IS the collected family — reusing the firm iterate-while vocabulary, the [`chebyshev`](./chebyshev.md) route), sitting one shell *above* the [`ksp_solve`](./ksp_solve.md) cap (`solve_loop` iterate-whiles over inner cycles; `solve_family` maps over an independent RHS family). It is a **combinator-as-entry**: the electrostatic terminal-boundary sweep and the magnetostatic surface-current sweep are **specialization notes re-expressing THROUGH it** (NOT two rectangular leaf chapters); they differ only in the absorbed family-index domain / RHS-construction / collection, all absorbed into `[Inputs]`. Status `rough-in (test-coverage-bounded)`: its structure is firm on two fixed-operator witnesses, but the load-bearing concatenation-homomorphism + operator-capture-once laws are stated against the strawman §3.7 family / `map` list-homomorphism algebra and are **test-unconfirmed** (the drivers are integration-level; no unit test exercises the family-map at the `Solve(mesh)` entry). Promotion route: a dedicated family-map test OR a strawman-derivation pass raising the list-homomorphism law confidence to `ksp_solve`-equivalent (a batch-17 lowering-verifier may promote it on the firm-on-positive-structure escape once the `KspSolver`-reuse statelessness is confirmed). **Scope: fixed-operator ONLY (2-of-5 pipelines** — electrostatic + magnetostatic; the driven pipeline rebuilds the operator per-frequency (`drivensolver.cpp:176-180`) and is the `per-element` superset `map_solve_over_(operator,rhs)_family`, batch-17-gated). Harvested cycle-055 D1 from the c054 combinator-miner mine.
```

```edit:book/src/L4/index.md
[old]: **Queued at L4 (0 — substantially complete)** — the two iterative-solve CAP chapters [`ksp_solve`](./ksp_solve.md) and [`eigsolve`](./eigsolve.md) **both landed firm cycle-048**, consuming the c047-anchored `solve-monad` outer-driver vocabulary; the queue is now empty of gated caps. The L4 frontier is **substantially complete / near-exhausted**: per the cycle-046 L4-survey read ("L4 mostly intentionally complete"), 13 of the 18 firm L3 operators are no-L4-by-design (the BLAS-1 / elementwise / smoother cohort whose L4 form would add no calculus beyond their firm L3 rendering), and after these two caps the only remaining L4-cap candidate is:

- `L4/orthogonalize` *(R5; marginal — deferred)* — would thread `{residual, coeffs}` through an Arnoldi step but adds no novel calculus beyond the MGS/CGS variant-split already recorded at [`L3/orthogonalize`](../L3/orthogonalize.md). **Deferred-marginal** per OQ `l4-orthogonalize-cap-marginal-defer` (R5): gated on a firm L4 Arnoldi consumer (a firm L4 `arnoldi_step` / GMRES-Arnoldi driver) that does not exist; it is not queued-as-gated-and-ready, only deferred-pending-a-consumer.

With the queue empty of ready caps, the **next forward-direction assessment is teed up for the batch-14 meta-phase** (fires after cycle-048): whether the L4 frontier is exhausted (the `l4-native-combinator-denominator-completeness-survey` reading), or width/depth consolidation is the next pick. This refresh does not pre-judge that assessment.
[new]: **Active frontier — the solver-test-load is generating new spine vocabulary (cycle-055).** The batch-14 "substantially complete / near-exhausted" framing is **RETIRED**: under the 2026-06-01 VOCABULARY-SHIFT REDIRECT, "complete" there meant *complete relative to the lifted inner-kernel cohort*, not relative to the in-scope solver target. The solver pipelines (all 5, pulled up as a low-priority **test-load** on the shared spine) are now generating genuinely new L4 combinators: `solve_family` (cycle-055 D1) is the **first solver-driven L4 combinator** — a coordination shell *above* the [`ksp_solve`](./ksp_solve.md) cap, distilled from the electrostatic + magnetostatic outer sweeps, that did NOT exist in the inner-kernel cohort. So the L4 frontier is **not exhausted**; it advances whenever a solver is cleanly describable in (or extends) the shared vocabulary. The two iterative-solve CAP chapters [`ksp_solve`](./ksp_solve.md) and [`eigsolve`](./eigsolve.md) (both firm cycle-048) remain the cap layer; the open combinator front is now:

- [`solve_family`](./solve_family.md) *(rough-in (test-coverage-bounded); cycle-055 D1)* — the fixed-operator family-map shell over `ksp_solve` (see §Vocabulary cohort above). Its **batch-17 superset** `map_solve_over_(operator,rhs)_family` (operator rebuilt per family-element — the driven pipeline) is the next solver-driven combinator candidate, gated on a 3rd-pipeline probe (OQ `solve-family-general-operator-rhs-superset-probe`): confirm driven's per-ω rebuild is the only difference, and check whether **transient** is a `map`-over-family or a stateful `fold`/`solve_loop` shape (a fold does NOT join the `solve_family` family).
- `L4/orthogonalize` *(R5; marginal — deferred)* — would thread `{residual, coeffs}` through an Arnoldi step but adds no novel calculus beyond the MGS/CGS variant-split already recorded at [`L3/orthogonalize`](../L3/orthogonalize.md). **Deferred-marginal** per OQ `l4-orthogonalize-cap-marginal-defer` (R5): gated on a firm L4 Arnoldi consumer that does not exist; not queued-as-gated-and-ready, only deferred-pending-a-consumer.

The 13-of-18 BLAS-1 / elementwise / smoother L3 operators remain no-L4-by-design (their L4 form would add no calculus beyond their firm L3 rendering) — that observation stands; what is retired is the inference that the *whole* L4 frontier is therefore exhausted. The solver-test-load is the live source of new combinators.
```

### 2. `book/src/L4-L3/index.md` — consolidated tally on D2's seeded §Vocabulary-cohort (3→4 firm, on-disk-truthful)

D2 seeds the `## Vocabulary-cohort` section with its OWN `solve-family-map-dissolution` bullet under a "Substantive themes (firm)" sub-list. I author the consolidated running-count tally + coverage line + growth-log as a deferred-to-D7 block, appended after D2's bullet within that section. (If the integrator applies D2 before D7, D2's section seed exists; my edit anchors on D2's bullet line. If anchoring fails because D2's exact seed text differs, integrate this as an append to the `## Vocabulary-cohort` section.)

```edit:book/src/L4-L3/index.md
[old]: - [`solve-family-map-dissolution`](./solve-family-map-dissolution.md) — the L4 `solve_family` **outer map shell** → L3 explicit positional accumulating loop. A genuine `map`-combinator → explicit-accumulating-`for` translation with the **operator-capture hoist** (the L4 once-captured `readonly` `op` stratum → the `SetOperators(*K,*K)` construction hand-hoisted outside the loop) the load-bearing dissolution detail. Composes strictly above the per-solve [`ksp-solve-driver-dissolution`](./ksp-solve-driver-dissolution.md). The family loop carries NO `sequential-obstruction` (members independent — the contrast with the per-solve outer-driver fold it wraps). Firm on structure; fixed-operator scope (2-of-5, driven `per-element` superset batch-17-gated). Cycle-055 D2.
[new]: - [`solve-family-map-dissolution`](./solve-family-map-dissolution.md) — the L4 `solve_family` **outer map shell** → L3 explicit positional accumulating loop. A genuine `map`-combinator → explicit-accumulating-`for` translation with the **operator-capture hoist** (the L4 once-captured `readonly` `op` stratum → the `SetOperators(*K,*K)` construction hand-hoisted outside the loop) the load-bearing dissolution detail. Composes strictly above the per-solve [`ksp-solve-driver-dissolution`](./ksp-solve-driver-dissolution.md). The family loop carries NO `sequential-obstruction` (members independent — the contrast with the per-solve outer-driver fold it wraps). Firm on structure; fixed-operator scope (2-of-5, driven `per-element` superset batch-17-gated). Cycle-055 D2.

**Consolidated tally (firm L4>L3 themes: 3 → 4 this cycle).** Counted from the on-disk theme-list table above: **4 firm** themes — `iterate-while-dissolution` (c047), `iterate-while-with-prev-dissolution` (c047), `ksp-solve-driver-dissolution` (c048), and `solve-family-map-dissolution` (cycle-055 D2, this cohort); **3 rough-in** — `krylov-step-typed-wrapper-dissolution` (c006), `gmres-inner-loop-iterate-while-migration` (c008), `fgmres-inner-loop-iterate-while-migration` (c011). The cycle-055 D2 landing is **substantive** (a `map`-combinator → explicit-accumulating-loop translation with operator-hoist, not an identity-in-named-terms rename — honoring the vocabulary-shift redirect). The full L4 `solve_family` combinator lowers to the full L3 family-sweep by composing this theme (the outer map shell) above `ksp-solve-driver-dissolution` (each per-member solve) above `iterate-while-dissolution` + `krylov-step-typed-wrapper-dissolution` (the inner fold + per-step body) — a 4-shell stratified hop.

> **On-disk/record divergence (flagged for the integrator/finalize, NOT my fix).** The cycle-055 dispatch projected this layer at "6 → 7 firm". The on-disk theme-list table carries only **3 firm** rows pre-cycle (→ 4 with D2). The projected "6" likely conflated the L4 Part-overview prose at [`../L4/index.md`](../L4/index.md) "L4>L3 lowering themes" (which mis-labels `fgmres-inner-loop-iterate-while-migration` as *firm*) with this authoritative L4-L3 table (where that row is **rough-in**, line ~17). The two surfaces disagree on `fgmres-inner-loop` (and `gmres-inner-loop`) firmness. This consolidated tally is computed from the **on-disk table** (the authoritative dep-map), per the count-owner discipline. The L4-index-prose-vs-L4-L3-table firmness mismatch is an upstream landing-gap (OQ `l4-l3-fgmres-firmness-prose-vs-table-divergence`).
```

### 3. `book/src/L1/index.md` — FE sub-spine header flip (Rough-in→Firm); grand-total firm 27→29

```edit:book/src/L1/index.md
[old]: **Rough-in (FE-assembly sub-spine — THREAD-OPENER cycle-053)** — speculative L1 operators opening the finite-element assembly surface (the MFEM-equivalent assembly sub-spine, in scope per CLAUDE.md mesh/FE). Proposed by the [`fe-operator-assemble-mutation-rotation`](../L1-L0/fe-operator-assemble-mutation-rotation.md) L1>L0 thread-opener; await harvester promotion + integrator-term-cohort enumeration:
[new]: **Firm (FE-assembly sub-spine — 3; opened cycle-053, completed cycle-055)** — the finite-element assembly surface (the MFEM-equivalent assembly sub-spine, in scope per CLAUDE.md mesh/FE), opened as a thread by the [`fe-operator-assemble-mutation-rotation`](../L1-L0/fe-operator-assemble-mutation-rotation.md) L1>L0 thread-opener (cycle-053). **All 3 members are now firm**: the integrator-fold assembler `fe_assemble` (firm cycle-054) and its two **separable BC-treatment post-compositions** `eliminate_essential_bc` (firm cycle-055 D4) + `eliminate_rhs` (firm cycle-055 D3) — both compose AFTER the `fe_assemble` fold (NOT part of it), one pinning the operator's essential rows/cols, one lifting inhomogeneous Dirichlet data into the RHS. The per-term assembly leaf `A(term_i)` inside the firm fold is **libCEED-owned** (the cycle-055 D5 obstruction theme [`fe-assemble-libceed-boundary-obstruction`](../L1-L0/fe-assemble-libceed-boundary-obstruction.md), `opaque-library-ownership`, settles the thread-opener's libCEED-boundary OQ — the boundary is identical across all 5 solver pipelines; `fe_assemble` STAYS FIRM, the obstruction is a strict sub-term below the fold's leaf, not a downgrade). The `weak_form_term` type remains a deferred rough-in input the fold quantifies over opaquely.

- **`fe_assemble` is FIRM** (cycle-054) — see [`fe_assemble`](./fe_assemble.md). Assemble a global FE operator from a space + immutable weak-form term list as the integrator-fold `K = Σ_i A(term_i)`; clean-gate PROMOTE (the fold is definable treating `weak_form_term` as an opaque rough-in input — it does not crack open the term). **Slug-collision note**: NOT the existing [`bilinear-form`](./bilinear-form.md) (the BLAS-2 reduction `xᴴ M y`); `fe_assemble` is the C++ `BilinearForm`-*class* assembler. Laws: empty-term identity, concatenation-homomorphism, single-term reduction, term-position commutativity. The `weak_form_term` type + the per-term assembly map `A(·)` (libCEED boundary) stay deferred-rough-in inputs the fold quantifies over.
- **`eliminate_essential_bc` is FIRM** (cycle-055 D4) — see [`eliminate_essential_bc`](./eliminate_essential_bc.md). Pin essential (Dirichlet) true dofs into the assembled square operator: zero the rows/cols at the essential-dof set, set the eliminated diagonal per the **diagonal-policy** variant axis (`DIAG_ONE`/`DIAG_ZERO`); clean-gate PROMOTE (a separable post-composition that composes AFTER [`fe_assemble`](./fe_assemble.md), NOT part of the assembly fold). Laws: idempotence, free-block preservation, policy-determines-only-essential-diagonal, distribution-over-assembly (the free-block projection `K ↦ P_F K P_F`). L0: `ParOperator::SetEssentialTrueDofs`/`mfem::HypreParMatrix::EliminateBC` — `palace/linalg/rap.cpp:36-47,141-143`, witness `palace/models/laplaceoperator.cpp:216-217`.
- **`eliminate_rhs` is FIRM** (cycle-055 D3) — see [`eliminate_rhs`](./eliminate_rhs.md). Lift inhomogeneous Dirichlet data into the RHS as the separable post-composition `b' = b − K·x_bc` (essential rows pinned per diagonal policy); clean-gate PROMOTE (the body is `apply_linop`(`A->Mult`, `palace/linalg/rap.cpp:69`) + `axpy`(`b.Add(-1.0,·)`, `:73`) + the `set_subvector` essential-dof pin (`:64,76,80`) — entirely firm-spine vocabulary, no spine gap). **Separable post-composition**, NOT part of the `fe_assemble` fold — it consumes the already-assembled `K` (witness `laplaceoperator.cpp:252`). Laws: interior-block linearity in `b` and in `x_bc`, homogeneous-BC interior identity, separable-post-composition-with-`fe_assemble`; non-laws: not linear-in-`b`-as-a-whole (the pin overwrites), not idempotent. L1>L0 lowering `eliminate-rhs-mutation-rotation` forthcoming (plain text — theme not yet authored).
```

```edit:book/src/L1/index.md
[old]: **Firm (26)** — element-wise updates, BLAS-1 reductions, the fused-normalise primitive, the opaque-operator gate, the constructed-operator solve gate, the eigenmode-solve gate, the polynomial-smoother gate, the divergence-free projector gate, the nonlinear-pencil interior atom, the NEP deflated-residual extension, the small-dense direct-solve gate, the NEP deflated-solve extension, the NEP quasi-Newton Jacobian action, the NEP quasi-Newton eigenvalue-correction step, the GMRES/FGMRES restart-correction back-solve, the GMRES/FGMRES per-column running-QR leaf, the diagonal-preconditioner-apply Jacobi smoother, the elementwise multiplicative-inverse primitive, the elementwise (Hadamard) pointwise-product primitive, and the floquet-periodicity B-field correction gate:
[new]: **Firm (26 main cohort; 29 firm grand total incl. the FE-assembly sub-spine).** The 26 main-cohort firm operators are listed below; the FE-assembly sub-spine adds 3 more firm (`fe_assemble` c054 + `eliminate_essential_bc` + `eliminate_rhs` both c055 — see the §"Firm (FE-assembly sub-spine)" subsection), bringing the L1 firm grand total to **29** (was 27 before cycle-055: 26 main + `fe_assemble`; cycle-055 D3+D4 added the two `eliminate_*` BC-treatment post-compositions). The 26 main-cohort firm operators are element-wise updates, BLAS-1 reductions, the fused-normalise primitive, the opaque-operator gate, the constructed-operator solve gate, the eigenmode-solve gate, the polynomial-smoother gate, the divergence-free projector gate, the nonlinear-pencil interior atom, the NEP deflated-residual extension, the small-dense direct-solve gate, the NEP deflated-solve extension, the NEP quasi-Newton Jacobian action, the NEP quasi-Newton eigenvalue-correction step, the GMRES/FGMRES restart-correction back-solve, the GMRES/FGMRES per-column running-QR leaf, the diagonal-preconditioner-apply Jacobi smoother, the elementwise multiplicative-inverse primitive, the elementwise (Hadamard) pointwise-product primitive, and the floquet-periodicity B-field correction gate:
```

## Supporting evidence

- **L4 firm UNCHANGED at 6 / rough-in 0→1** — D1's report `reports/2026-06-02T010700Z-harvester-solve-family-firm-entry/CYCLE.md`: `solve_family.md` frontmatter `firmness: rough-in (test-coverage-bounded)` + §Status "honors the planner default of `rough-in (test-coverage-bounded)` rather than `firm`"; D1 explicitly DEFERRED the "Rough-in at L4 (0)" → "(1)" flip to D7 (D1 §Open-questions final bullet, `L4/index.md:47`).
- **L4>L3 firm 3→4 (on-disk; dispatch said 6→7)** — verified by direct read of `book/src/L4-L3/index.md` theme-list table: 3 firm rows on-disk (`iterate-while-dissolution`, `iterate-while-with-prev-dissolution`, `ksp-solve-driver-dissolution`), 3 rough-in (`krylov-step-typed-wrapper-dissolution`, `gmres-inner-loop-iterate-while-migration`, `fgmres-inner-loop-iterate-while-migration`). D2's report `reports/2026-06-02T010800Z-abstractor-solve-family-map-dissolution/CYCLE.md`: `solve-family-map-dissolution.md` §Status `firm` (firm-on-structure, independent of the LHS test-gated independence law). The "6" projection mismatch is logged as an OQ.
- **L1 FE sub-spine: 3 firm; grand total 27→29** — D3 `reports/2026-06-02T010700Z-harvester-eliminate-rhs-firm-l1/CYCLE.md`: `eliminate_rhs.md` `firmness: firm` (clean-gate PROMOTE). D4 `reports/2026-06-02T010700Z-harvester-eliminate-essential-bc-firm-l1/CYCLE.md`: `eliminate_essential_bc.md` `firmness: firm` (clean-gate PROMOTE). `fe_assemble` already firm c054 (on-disk `book/src/L1/index.md:72`). D5 `reports/2026-06-02T010700Z-abstractor-libceed-boundary-obstruction/CYCLE.md`: `fe-assemble-libceed-boundary-obstruction.md` `status: obstruction`, `sub_kind: opaque-library-ownership` (the per-term leaf `A(term_i)` is libCEED-owned; `fe_assemble` stays firm). On-disk firm count verified: 26 main-cohort §Vocabulary-cohort bullets (axpy … floquet-correction) + `fe_assemble` = 27 pre-cycle; +2 `eliminate_*` = 29.
- **Index-registration partition honored** — every D1–D6 producer's report explicitly deferred ONLY the consolidated tally / cohort-header count to D7 and registered its own row + bullet + SUMMARY entry. My `old` anchors target the COUNT/NARRATIVE regions (the cohort headers, the rough-in/queued lines, the frontier prose, D2's seeded cohort section), NOT any producer's row/bullet.

## Open questions / caveats

- **OQ `l4-l3-fgmres-firmness-prose-vs-table-divergence` (NEW; flag for integrator/finalize).** The L4 Part-overview prose at `book/src/L4/index.md` §"L4>L3 lowering themes" labels `fgmres-inner-loop-iterate-while-migration` (and the GMRES sibling) as *firm* (lines ~52-53), but the authoritative L4-L3 theme-list table at `book/src/L4-L3/index.md:16-17` carries both as **rough-in**. The dispatch's "6→7 firm" projection appears to have counted the prose's firmness, not the table's. I computed the consolidated tally from the on-disk **table** (the authoritative dep-map), per the count-owner discipline ("survey from the on-disk `## Status`, not the cycle record"). This is an upstream landing gap (a lifter re-anchor that updated the L4 prose but not the L4-L3 table, or vice versa); resolving it is out of my count-owner scope. A future lifter should reconcile the two surfaces (and a cross-layer-cross-cutter could audit whether `gmres`/`fgmres-inner-loop` are actually firm-promotable now that their upstream `gmres.md §L4 v0.7` migration landed).

- **`solve_family` rough-in-tier promotion route (carried from D1).** `solve_family` landed `rough-in (test-coverage-bounded)`; D1 flagged (OQ `solve-family-status-firm-on-positive-structure-vs-test-coverage-bounded`, batch-17) that the concatenation-homomorphism IS a syntactic `map` identity and the operator-capture hoist IS read off positive source — only the *independence* claim (no hidden cross-element `KspSolver` state) is test-unconfirmed. A batch-17 lowering-verifier confirming the `KspSolver`-reuse statelessness would promote it to firm on the firm-on-positive-structure escape, flipping the L4 "Rough-in at L4 (1)" line back toward "(0)" / moving `solve_family` into the firm cohort. My frontier reword anticipates this (the combinator front stays live).

- **`solve-family-map-dissolution` firm-on-structure vs inherited LHS caveat (carried from D2).** D2 marked the theme `firm` (NOT inheriting the LHS `rough-in (test-coverage-bounded)`), reasoning that the rotation *shape* is a structural identity independent of the cap's test-gated independence law. OQ `solve-family-map-dissolution-firm-on-structure-vs-lhs-test-coverage` (batch-17) for the verifier to confirm. My L4>L3 tally counts it as firm per D2's landed status (verified on-disk-truthful against D2's report).

- **Sibling `fe_assemble.md` citation drift (carried from D4 — for a future lifter, NOT a count-owner fix).** D4 flagged that `book/src/L1/fe_assemble.md` carries an un-corrected `laplaceoperator.cpp:215-217` citation in 2 places (§laws ~line 147, §Evidence ~line 257); the on-disk truth is `216-217` (construction `:216`, `SetEssentialTrueDofs` call `:217`). This is a citation-fix for a future lifter/citation pass; it is out of my count-owner scope and out of D4's harvester scope. Surfaced here as a residual for the integrator/finalize to log (OQ `fe-assemble-laplaceoperator-citation-drift-215-vs-216`).

- **L1 main-cohort "Firm (26)" header vs grand total.** The on-disk "Firm (26)" header enumerates exactly 26 main-cohort bullets (axpy … floquet-correction); the FE-assembly sub-spine (`fe_assemble` + the 2 `eliminate_*`) lives in its own subsection and is NOT one of those 26. I kept the "26 main cohort" count accurate to the enumerated bullets and added the "29 firm grand total" annotation rather than renumbering the 26 (which would mis-describe the bullet list). If a future refresh prefers a single unified firm count, the cleanest move is to fold the FE sub-spine bullets into the main cohort list and renumber to 29 — flagged, not enacted (it would touch the producers' own bullets, outside the count-owner partition).
