---
agent: cycle-planner
invoked_at: 2026-06-02T050136Z
scope: cycle-058 dispatch plan
status: pending
---

# Cycle 058 dispatch plan

FIRST primary cycle of meta-batch-18 (cycles 058/059/060; batch-18 meta-phase fires AFTER cycle-060's finalize). Operates under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (combinator-as-entry, lowerings-are-translations-not-renames, solvers-as-low-priority-test-load, replace-and-propagate). Batch-17 role-spec edits loaded (session restarted): `lifter.md` + `layer-intro-author.md` index-cell anti-drift promotion-time guard.

## Goals selected this cycle

Advance the two-combinator MAP/FOLD solver spine. The LEAD is `fold_solve`: its `disciplined-cross-pipeline-combinator-mining-gate` ≥2-witness bar is now MET (transient + SweepAdaptive ROM, c057 D3), so promote the c057 D4 rough-in thread to a full L4 §3.7-`iterate_while`-child entry + its L4>L3 lowering — with the load-bearing classification decision of whether `fold_solve` is ONE schedule-source-parameterized combinator or whether the greedy/error-terminated sub-shape is distinct. Alongside: a discipline-gated `weak_form_term` FE-cohort probe (genuinely-new FE vocabulary, pipeline-pull-gated), the final `map_solve` 2nd-witness probe (discharge-or-record-as-finding), and the L1/L1-L0 index-staleness hygiene audit. No new parent abstraction (batch-17 decision 2). No L3 `solve_family` mirror (negative-listed, c057 D1 NO-ENTRY warrant).

## Deliverable-presence verification (paste-inline-evidence; load-bearing discipline)

**D1 `fold-solve-promotion` — target slugs (verify ABSENT; open-to-author):**
- `book/src/L4/fold_solve.md`: `ls` → `ls: cannot access 'book/src/L4/fold_solve.md': No such file or directory` — **ABSENT, OK to author.**
- L4>L3 fold lowering theme: `ls book/src/L4-L3/ | grep -i "fold\|time"` → (empty, no matches) — **ABSENT, OK to author.** Canonical slug assigned below: `book/src/L4-L3/fold-solve-time-step-dissolution.md`.
- Current on-disk state: `grep "fold_solve" book/src/L4/index.md` → TWO rough-in references (line 62 frontier bullet + line 82 dep-map row `| fold_solve ... | rough-in (thread-opener; cycle-057 D4) |`). Confirms `fold_solve` is a c057 rough-in thread-opener (one dep-map row + one frontier bullet), NOT a firm entry → promotion is open, not a no-op. Maturity on disk = `rough-in`; D1 promotes the L4 entry to firm and the L4>L3 theme is new-firm.
- Structural-block check: gate = `disciplined-cross-pipeline-combinator-mining-gate` ≥2-witness bar. Per batch-17 meta decision 3 + c057 D3, SweepAdaptive is a CONFIRMED fold-family member → transient + SweepAdaptive = 2 fold witnesses → **bar MET, gate NOT blocking.** Cite the skill at dispatch (2-of-N fold-witness discharge).

**D2 `weak-form-term-fe-cohort` — target (verify ABSENT; pipeline-pull-gated):**
- `ls book/src/L1/ | grep -i "weak\|term"` → (empty, no matches) — **ABSENT.**
- `grep "weak_form_term" book/src/L1/index.md` → line 70 + line 72: `weak_form_term` is named only as a "deferred rough-in input the fold quantifies over opaquely" — confirms it is an unwritten deferred cohort, not a no-op. CLEAN-GATE applies (see D2 scope): author a term form only if a pipeline pull this cycle NEEDS a non-diffusion term; else observation-first cohort-enumeration sketch. Open by construction for the sketch framing.

**D3 `map-solve-second-pipeline-probe` — observation-only (no slug to create):**
- Open by construction (observation-first probe; no artifact authored regardless of verdict per batch-17 decision 3). Pre-evidence pasted in D3 scope below: magnetostatic `ksp.SetOperators(*K, *K)` at `drivensolver`-sibling `magnetostaticsolver.cpp:36` is ONCE before the boundary sweep loop (`:65`) → fixed-operator map (a `solve_family` member), NOT a 2nd operator-varying-map witness. Probe likely closes `map_solve` as a single-witness finding.

**D4 `l1-l1-l0-index-staleness-audit` — observation-only (no slug to create):**
- `ls book/src/L1/index.md book/src/L1-L0/index.md` → both exist. Open by construction (observation-first sweep comparing index-table status cells against linked chapters' `## Status` lines; CONFIRM-CLEAN expected, mirrors c056 D2). No artifact authored unless a divergence routes to a later lifter.

**STOP-PROPOSING NEGATIVE LIST check:** none of the four picks match a disqualified slug (`lu_solve`/`back_solve`/`ls-update-column`/4 NLEPS atoms/`apply_nonlinear_pencil`-HELD/`polynomial_smoother`). **`L3/solve_family` backfill is NOW negative-listed** (c057 D1 NO-ENTRY warrant) — NOT proposed this cycle (the L4>L3 dissolution is the authoritative L3-form home). The firm L2/L3 BLAS/projector/smoother surface is combinator-complete — NOT re-scanned. All four picks pass.

## Codemap path verification (cited anchors, codemap-confirmed)

- `palace/models/timeoperator.cpp:410` `ode->Step(sol, t, dt)` — codemap `search_text` hit (NOTE: under `palace/models/`, NOT `palace/linalg/` — the c057 dep-map row's `timeoperator.cpp:410` is correct, the directory is `models/`). This is the Palace CALL into the opaque MFEM `ODESolver` step; D1's L4>L3 obstruction bottoms out HERE, not in MFEM internals.
- `palace/drivers/transientsolver.cpp:93` `time_op.Step(t, delta_t)` — codemap-confirmed; the transient outer time sweep (fold-witness 1).
- `palace/drivers/drivensolver.cpp:231` `DrivenSolver::SweepAdaptive` (`:73` dispatch `adaptive ? SweepAdaptive : SweepUniform`) — codemap-confirmed; PROM greedy params `offline_tol`/`convergence_memory`/`max_size_per_excitation`/`prom_indices` at `:240-244` (read-confirmed) → fold-witness 2 (greedy/error-terminated sub-shape).
- `palace/drivers/magnetostaticsolver.cpp:36` `ksp.SetOperators(*K, *K)` (ONCE, before the `:65` boundary sweep loop) — codemap read-confirmed; the D3 map-probe anchor (fixed-operator → NOT operator-varying).

## Dispatches

**D1 — (`harvester`, LEAD)** — `fold-solve-promotion`: author the full **`book/src/L4/fold_solve.md`** entry (promote the c057 D4 rough-in thread-opener; currently a dep-map row at `L4/index.md:82` + frontier bullet at `:62`). The state-threaded FOLD combinator, the fold-sibling of `solve_family`'s independent MAP; both are §3.7 `iterate_while`-family children distinguished by whether the step carries state (batch-17 decision 2). **NOT a new parent abstraction.** **Load-bearing classification decision:** the two fold witnesses differ — transient (`transientsolver.cpp:93` → `timeoperator.cpp:410`) is a FIXED-schedule march (`foldl` over a uniform `[Time]`); SweepAdaptive (`drivensolver.cpp:231`, PROM params `:240-244`) is GREEDY/error-terminated (state-derived loop bound AND state-derived per-step input, strictly richer than a fixed-schedule fold). D1 MUST decide and state explicitly: is `fold_solve` ONE combinator parameterized over the schedule-source (fixed-`[Time]` vs greedy-predicate), OR is the greedy sub-shape a distinct entry? Prefer the parameterized-over-schedule-source reading if it does not distort either witness (in-layer conciseness); record the alternative as a §Specializations note or an OQ if the greedy shape resists unification. Status `rough-in (test-coverage-bounded)` is acceptable (the firm-on-positive-structure escape applies if every law is a syntactic identity on the read fold). **Cite the `disciplined-cross-pipeline-combinator-mining-gate` skill (2-of-N fold-witness discharge: transient + SweepAdaptive).** deps: none. rationale: active-head item-1 LEAD-IF-GATE-FIRES; the gate fires (2 fold witnesses); completes the FOLD half of the two-combinator spine.

**D2 — (`abstractor`, LEAD)** — `fold-solve-promotion` (L4>L3 lowering): author **`book/src/L4-L3/fold-solve-time-step-dissolution.md`** (canonical slug — D2 authors it this cycle; D1 forward-references it). The dissolution of the outer `foldl` into the explicit `for (int step ...)` time sweep threading `sol` in place (`transientsolver.cpp:77-104`), with the per-step `time_step_op` body bottoming out in the **opaque MFEM `ODESolver` step** — an `obstruction (opaque-library-ownership)` sub-leaf citing Palace's CALL `palace/models/timeoperator.cpp:410` `ode->Step(sol, t, dt)` (NOT MFEM internals; per `time-step-op-opaque-mfem-integrator-boundary`). The lowering is a genuine vocabulary translation (the fold combinator → the in-place state-threading loop with the opaque integrator leaf), NOT a rename. deps: forward-references D1's `book/src/L4/fold_solve.md` (canonical slug — D1 authors it this cycle); sequence in wave 2 so the per-report integrator wires a live link. rationale: active-head item-1; the L4>L3 edge for the promoted combinator (replace-and-propagate).

**D3 — (`cross-layer-cross-cutter`, observation-only)** — `map-solve-second-pipeline-probe`: ONE cheap observation-first probe of a **non-driven multi-operator sweep** for a 2nd operator-varying-MAP witness (per batch-17 decision 3). Pre-evidence (codemap-confirmed, pasted above): magnetostatic sets `ksp.SetOperators(*K, *K)` ONCE at `magnetostaticsolver.cpp:36` BEFORE the surface-current boundary sweep loop at `:65` → the operator is FIXED across the sweep (a `solve_family` fixed-operator MAP member, NOT operator-varying); electrostatic is the analogous fixed-operator terminal sweep. So the strong prior is **non-discharge** — no non-driven pipeline rebuilds the operator per family-element. Confirm against electrostatic (`electrostaticsolver.cpp`) for completeness. **Cite the `disciplined-cross-pipeline-combinator-mining-gate` skill.** **Do NOT author `map_solve.md` regardless** — a discharge LICENSES batch-19 authoring; the expected non-discharge RECORDS `map_solve` as a permanent single-witness spine-coverage finding (the redirect's "what a solver can't cleanly say is a finding about the spine"). deps: none. rationale: active-head item-3; closes the `map_solve` disposition (keep-probing-one-more-pipeline-then-retire per decision 3).

**D4 — (`cross-layer-cross-cutter`, observation-only)** — `l1-l1-l0-index-staleness-audit`: sweep the **`book/src/L1/index.md` + `book/src/L1-L0/index.md`** index-table status cells against each linked chapter's `## Status` line (the highest in-place-promotion-churn drift sub-class per c056 D2; the only drift class escaping the dead-link build check). The c057-meta promotion-time guard prevents NEW drift but does not retroactively fix historical residue. CONFIRM-CLEAN is the expected verdict (mirrors c056 D2's 16/16 L3-L2/L2-L1 result). If a divergence IS found, route it to a c059 lifter (no mid-audit fix). deps: none. rationale: active-head item-4; closes the open L1/L1-L0 half of friction `index-table-status-cell-drifts-when-theme-file-promoted`.

**`weak-form-term-fe-cohort` (active-head item-2) — NOT DISPATCHED this cycle; HELD with rationale.** The clean-gate is not met: no pipeline pull-up this cycle NEEDS a non-diffusion term (D1/D2 are transient-fold work; D3/D4 are probe/hygiene). Authoring term forms speculatively across the 5 pipelines would violate the redirect's "author only as a pipeline pull-up first NEEDS them, NOT speculatively." The cohort stays the standing next-FE-width candidate (OQs `fe-assemble-weak-form-term-cohort-width-after-firm-flip` + `-enumeration`); recruit it in c059/c060 when a 2nd-pipeline FE assembly (driven/eigenmode curl-curl or transient mass term) is pulled up and concretely needs a non-diffusion term. This is an explicit exception (item-2 is MEDIUM but pull-gated; the LEAD fold work + the two cheap probes fill the cycle without forcing speculative FE vocabulary).

## Overlap analysis

- **D1 ↔ D2:** D1 authors `book/src/L4/fold_solve.md` + edits the `L4/index.md:82` dep-map row (rough-in → live-link firm) + the `:62` frontier bullet. D2 authors `book/src/L4-L3/fold-solve-time-step-dissolution.md` + appends an `L4-L3/index.md` theme-list row + §Vocabulary-cohort bullet + SUMMARY entry. **Forward-reference dependency** (D2 links D1's not-yet-existing `fold_solve.md`) → D2 in wave 2. Both touch `book/src/L4/index.md`? D2 does NOT edit `L4/index.md` (it edits `L4-L3/index.md`); D1 owns `L4/index.md`. **NOT overlapping at the file level; sequenced only for the live-link.** Canonical slug stated in BOTH scopes (`book/src/L4/fold_solve.md` and `book/src/L4-L3/fold-solve-time-step-dissolution.md`) per the cross-report-forward-reference-slug convention — neither producer guesses.
- **D1 ↔ D3, D1 ↔ D4, D2 ↔ D3, D2 ↔ D4:** D3/D4 are observation-only (no `book/` mutation; OQ/report appends only). D3 reads `magnetostaticsolver.cpp`/`electrostaticsolver.cpp` (drivers source); D4 reads `L1/index.md` + `L1-L0/index.md`. No artifact-region overlap with D1's `L4/*` or D2's `L4-L3/*`. **Non-overlapping → parallel.**
- **D3 ↔ D4:** distinct observation scopes (driver-source map-witness vs L1-index-cell hygiene); if both append to `scaffolding/open-questions.md` they append distinct intake headers (anchor-distinct, parallel-safe per the dep-map-rows convention). **Non-overlapping → parallel.**
- **Index count-ownership:** no two dispatches co-write one index's consolidated tally this cycle. D1 owns `L4/index.md` cells (its own dep-map row flip + frontier bullet); D2 owns `L4-L3/index.md` (its own theme-list row + bullet + any L4-L3 firm-count tally for its single new theme). These are different index files → no shared-tally contention; the c057-meta promotion-time guard has D1 flip the `fold_solve` cell WITH its status change, and D2 owns the L4-L3 count for its one landing. No count-owner partition dispatch needed (single-landing-per-index).

## Sequencing schedule

- **Wave 1 (parallel):** D1 (`harvester` `fold_solve` L4 entry), D3 (`cross-layer-cross-cutter` map-witness probe), D4 (`cross-layer-cross-cutter` L1/L1-L0 index audit).
- **Wave 2 (after D1's report lands):** D2 (`abstractor` `fold-solve-time-step-dissolution` L4>L3 theme) — sequenced after D1 so the per-report integrator wires the `../L4/fold_solve.md` reference as a live link.

(Standard pipeline: 4 dispatches → 4 critics → repairers as needed → `integrator-per-report` ×4 serial → ONE `integrator-finalize`. Waves order dispatches by forward-reference dependency only; the book is not rebuilt between waves and finalize runs once.)

## Open questions / caveats

- **`fold_solve` schedule-source classification (D1 load-bearing call).** The transient (fixed-schedule) vs SweepAdaptive (greedy/error-terminated) fold sub-shapes differ in BOTH loop-bound source and per-step-input source. D1 must judge whether one parameterized combinator covers both without distorting either. If D1 finds the greedy shape resists clean unification, the correct outcome is a §Specializations note + an OQ for the batch-18 meta-phase (do NOT force a single combinator that asserts a shape neither witness exhibits — that would be the over-unification anti-pattern the mining-gate guards). Either outcome (one parameterized entry, or one entry + a distinct-greedy OQ) is valid and should be recorded.
- **`map_solve` likely closes as a single-witness finding (D3).** The codemap pre-evidence is strong that no non-driven pipeline rebuilds the operator per family-element (magnetostatic/electrostatic both fix the operator before their sweeps). If D3 confirms, `map_solve` becomes a permanent single-witness spine-coverage finding — that is the redirect's intended outcome, not a gap. Routes to the batch-18 meta-phase for formal close.
- **`weak_form_term` cohort remains pull-gated (held, not dropped).** Flagged for c059/c060 recruitment when a concrete 2nd-pipeline FE assembly needs a non-diffusion term. If batch-18 advances driven/eigenmode/transient FE assembly, this becomes recruitable. No friction-ledger action needed yet (the deferral is by-design per the redirect).
- No escalating friction patterns require fresh plan candidates this cycle (the ledger's `escalating` entries are the long-standing addressed citation/skill-uptake telemetry ones; the c057-meta index-cell drift guard is already enacted and loaded). No `priorities.md` append needed beyond the in-flight active-head dispatch marks.
