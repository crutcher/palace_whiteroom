---
agent: layer-intro-author
invoked_at: 2026-06-01T223300Z
scope: cycle-052 D4 — L2/index.md + L3/index.md dep-map row + consolidated-cohort-narrative reconciliation for the 12 reduced fold-family leaves (SOLE index-owner + count-owner of the wave); co-scheduled bounded narrative micro-sweep
status: pending
integrated_at: 2026-06-02T010000Z
integration_commit: 9633c134b333932b31f2823c558398fafdaa9750
integration_notes: "cycle-052 D4 — applied clean (SOLE index/count owner; 21 edit blocks reconciling L2/L3 index dep-map rows + cohort narrative [rectangular-floor framing retired] + micro-sweep removing stale normalize.md OOB code-spans + softening L3/axpy.md:58/:75 OOB-after-reduction pinpoints to section-level); count-integrity-no-delta gate VERIFIED; closed collapsed-leaf-disposition-convention for the fold family; no build-repair needed."
inputs:
  - reports/2026-06-01T223300Z-lifter-l2-linear-combination-leaf-stubs/CYCLE.md (D1)
  - reports/2026-06-01T223300Z-lifter-l3-linear-combination-leaf-stubs/CYCLE.md (D2)
  - reports/2026-06-01T223300Z-lifter-inner-product-family-leaf-stubs/CYCLE.md (D3)
  - book/src/L2/index.md
  - book/src/L3/index.md
  - book/src/L3/linear_combination.md (micro-sweep)
  - book/src/L2/normalize.md (micro-sweep)
---

# CYCLE: L2/L3 index reconciliation + cohort-narrative + count (cycle-052 D4, wave-2)

## Summary

This dispatch is **wave-2** of the cycle-052 refactor-completion cycle, applied AFTER the three
wave-1 leaf-reduction producers (D1/D2/D3). I am the **SOLE index-owner + count-owner** for the
wave: D1/D2/D3 reduced the 12 fold-family leaf chapters to stubs (D1: `L2/{scal,axpy,axpby,axpbypcz}`
→ specialization-stubs; D2: `L3/{scal,axpy,axpby,axpbypcz}` → specialization-stubs; D3: `L2/dot`+`L3/dot`
→ specialization-stubs, `L2/nrm2`+`L3/nrm2` → consumer-stubs) and **deferred ALL index/narrative/count
reconciliation to me**. They own ONLY their own leaf-chapter bodies; I own the `index.md` dep-map ROWS,
the consolidated §Vocabulary-cohort / §Working-Notes narrative, and the consolidated count tally.

The reductions are **information-non-lossy reduce-to-stub** (files KEPT on disk, no deletes — every
inbound link stays LIVE by construction; D1/D3 confirmed zero-dangling via the
`deleted-slug-inbound-live-link-sweep` gate). Statuses stay `firm`. So my reconciliation is a
**narrative + dep-map-row** reconciliation, NOT a count delta — the 12 chapters still exist firm, only
their bodies collapsed to specialization/consumer pointers. **The L2 firm count is UNCHANGED at 21 firm
+ 1 partly-constructive (22 dep-map rows); the L3 count is UNCHANGED at 17 firm + 3 partial-obstruction.**

What changes:
1. **L2/index.md dep-map rows** for the 6 reduced L2 leaves (`scal`/`axpy`/`axpby`/`axpbypcz` →
   specialization-stub of `linear_combination`; `dot` → specialization-stub of `inner_product`; `nrm2`
   → consumer-stub of `inner_product`) — the row prose retires the "thin floor / leaf-floor (b) /
   foundation-first / rectangular" framing for the **combinator-as-entry** framing (the combinator is the
   home; the leaf row is a specialization/consumer pointer).
2. **L2/index.md §Vocabulary-cohort + §Semantics-overlay + §Working-Notes** cohort prose — retires the
   rectangular-floor / leaf-vs-fold-fork / `l2-floor-under-l3-leaf-cohort` framing the
   2026-06-01 vocabulary-shift redirect SUPERSEDES; restates the 6 leaves as reduced specialization /
   consumer stubs under the combinator-as-entry model.
3. **L3/index.md dep-map rows** for the 6 reduced L3 leaves — the `dot`/`nrm2` rows updated to
   specialization-stub / consumer-stub of `inner_product`; the `scal`/`axpy`/`axpby`/`axpbypcz` rows
   already point through `linear_combination` (cycle-051) but their "still stands firm in its own right"
   framing is updated to "reduced specialization-stub" + the count-tally bullet records the refactor-pass
   completion.
4. **Frontmatter field-name coordination** (D1/D3 flag): the stubs' frontmatter renames `fold_parent:` →
   `specialization_of:`. Neither index keys off the frontmatter field name (the dep-map rows reference
   the combinator by markdown link, not by frontmatter field), so this is a naming alignment with **no
   index consuming-convention to change** — noted, not structurally acted on.
5. **Co-scheduled bounded micro-sweep** (cosmetic, non-build-breaking): the stale-future-tense /
   demoted-slug code-spans in `L3/linear_combination.md` (`:26`/`:61`/`:117` "is cycle-051 / will collapse
   into / stand firm as of this cycle" → past tense + "reduced to specialization-stubs cycle-052"), the
   `L3/index.md:31` `linear_combination` row "collapses"→"collapsed" tense, the L2/index.md cohort-history
   prose, and the stale `book/src/L2/scal.md:223-228` bare code-span refs in `normalize.md:111/141/164`
   (re-pointed off the now-deleted scal §Dependencies range).

**The refactor pass COMPLETES when this lands** — the fold-family leaf cohort (the 12
`{scal,axpy,axpby,axpbypcz,dot,nrm2}` chapters at L2+L3) is fully reduced to combinator-pointer stubs,
the rectangular-floor framing is retired from both layer indexes, and no thin `-body-identity` /
base-form-floor residue remains in the fold-family cohort narratives.

## Proposed changes

### 1. L2/index.md — §Semantics-overlay "Identity-in-form floors" motif (retire rectangular-floor framing)

```edit:book/src/L2/index.md
[old]:- **Identity-in-form floors** — same-named L2 entries for the most-reused leaves / gates, present so the firm L3 cohort rests on adjacent same-named L2 parents (per CLAUDE.md §Methodology invariants **Identity-lowerings still require both L levels**), under the 2026-05-31 foundation-first directive `l2-floor-under-l3-leaf-cohort`. Each is value-thread-isomorphic to its firm L1 leaf/gate (the fusion rotation is a no-op or near-no-op on the primitive — the L0 form is already the unfolded composition), with the fusion-rotation *framing* added in L2 vocabulary and any load-bearing numerical trick preserved as an explicit algebraic claim. The floors split into **two motifs by fold-relationship**:
  - **Fold-parented floors** (cycle-041 `dot`/`nrm2`/`scal` + cycle-043 `axpy`/`axpby`/`axpbypcz`) — same-named leaves of the L2 fold-parents `inner_product` / `linear_combination`: `dot` is the conjugation-axis *leaf-of* `inner_product` (NOT merged); `scal`/`axpy`/`axpby`/`axpbypcz` are the arity-1/2/2/3 *members-of* `linear_combination` (NOT merged); `nrm2` is a *consumer-of* `inner_product` (`√ ∘ abs ∘ inner_product` at `y=x`, NOT a fold member; the `std::abs` guard preserved as an explicit algebraic claim). The do-NOT-merge boundary (§"Fold-cohort boundary") is load-bearing for all of them. **Their leaf-vs-fold realization is RATIFIED (keep leaf-floor (b)), batch-12 meta-phase** (`dot-l2-leaf-floor-vs-fold-only-design`; see §Working Notes — the c042 cross-cutter audit recommendation was adopted: each firm L3 leaf gets a same-named L2 floor cited as leaf-of / member-of the fold, deferring all fusion content to the fold-parent). The fork could only ever re-anchor a *fold-parented* leaf, so it governs this sub-cohort alone; the `nrm2` carve-out is **fork-invariant on membership** (it consumes the `inner_product` fold either way).
  - **Fork-INDEPENDENT standalone floors** (cycle-042; `reciprocal`, `elementwise_product`, `assemble-diagonal`, `jacobi-smoother`, `divfree-projector`) — same-named L2 parents under firm L3 entries with **NO fold-parent**: standalone elementwise leaves (`reciprocal` the nonlinear multiplicative-inverse self-map; `elementwise_product` the Hadamard binary op, the inverse-subsumption generalisation of `scal`) and constructed-operator / operator-to-data gates (`assemble-diagonal` the operator-to-data sibling of `apply_linop`; `jacobi-smoother` the thinnest constructed-operator gate; `divfree-projector` the four-step Helmholtz gate). Four are thin identity-in-form floors; `divfree-projector` is a **moderate** floor (one genuine `AddMult` apply-accumulate de-fusion). Because none has a fold-parent, **this cohort is design-final** — the leaf-vs-fold fork cannot reach it (there is no fold to fold a leaf into).
[new]:- **Combinator-as-entry + specialization/consumer stubs** — for the BLAS-1 fold families, the L2 entry for the family is the **fold combinator** (`linear_combination`, `inner_product`); the once-standalone same-named leaves are **reduced specialization / consumer stubs** that point up to the combinator and carry only the leaf-level facts the combinator does not (the leaf's unique L0 anchors + its one variant-axis row). This is the **vocabulary-shift-redirect (2026-06-01) refactor model** (refactor-pass COMPLETE cycle-052): the combinator is the home, the leaf is a thin specialization pointer. It **supersedes** the retired "rectangular floor / leaf-vs-fold fork / `l2-floor-under-l3-leaf-cohort` foundation-first" framing — a same-named base-form floor mirrored beside the combinator was the rectangular pattern the redirect supersedes; the redirect replaces it with the combinator-as-entry, leaves-as-specialization-notes model. The fold-family stubs:
  - **Specialization-stubs of `linear_combination`** (cycle-052 reduction of the cycle-041/043 floors): `scal` (arity-1), `axpy` (arity-2, second coeff 1), `axpby` (arity-2 general), `axpbypcz` (arity-3) — each `scal(α,x) = linear_combination [(α,x)]` etc., the family member at a fixed arity; the duplicated semantics/laws/fusion body is collapsed into the combinator, the stub keeps its unique L0 anchors + the output-aliasing variant row (which is the **FOLD's** axis).
  - **Specialization-stub + consumer-stub of `inner_product`** (cycle-052 reduction of the cycle-041 floors): `dot` is the **`M = I` Hermitian/symmetric specialization** of `inner_product` (the conjugation variant-axis — `dot` Hermitian vs `tdot` unconjugated — is the value-bearing leaf-level fact the stub keeps); `nrm2` is the **consumer** `√ ∘ abs ∘ inner_product` at `y=x` (NOT a fold member — do-NOT-merge; the `std::abs` defensive guard preserved as an explicit load-bearing numerical claim). The do-NOT-merge §"Fold-cohort boundary" remains load-bearing.
- **Standalone elementwise / gate floors** (cycle-042; `reciprocal`, `elementwise_product`, `assemble-diagonal`, `jacobi-smoother`, `divfree-projector`) — same-named L2 entries under firm L3 entries with **NO fold-parent**: standalone elementwise leaves (`reciprocal` the nonlinear multiplicative-inverse self-map; `elementwise_product` the Hadamard binary op) and constructed-operator / operator-to-data gates (`assemble-diagonal` the operator-to-data sibling of `apply_linop`; `jacobi-smoother` the thinnest constructed-operator gate; `divfree-projector` the four-step Helmholtz gate). Four are thin identity-in-form entries; `divfree-projector` carries one genuine `AddMult` apply-accumulate de-fusion. Having no fold combinator to point up to, these stay full standalone entries (no specialization-stub reduction applies — there is no combinator-as-entry to defer to).
```

### 2. L2/index.md — §Vocabulary-cohort split (retire the floor-cohort sub-bullets)

```edit:book/src/L2/index.md
[old]:*Identity-in-form leaf-cohort floors (`l2-floor-under-l3-leaf-cohort`; FOLD-PARENTED sub-cohort — present so the firm L3 cohort rests on adjacent same-named L2 parents; distinct from the fold-parents — do-NOT-merge; **leaf-vs-fold realization RATIFIED (keep leaf-floor (b)), batch-12 meta-phase**). The cohort heading is cohort-neutral (decision 3): the cohort spans the BLAS-1 leaves AND the cycle-043 arity-family additions, all members-of / consumers-of a fold-parent:*

- `dot` — conjugation-axis **leaf-of** `inner_product` (the plain `M = I` Hermitian / symmetric member); thin identity-in-form floor, laws inherited unchanged from the L1 leaf. Firm cycle-041 (D1).
- `nrm2` — **consumer-of** `inner_product` (`√ ∘ abs ∘ inner_product` at `y=x`, NOT a fold member); the `std::abs` defensive guard preserved as an explicit load-bearing numerical claim. **Carve-out: fork-invariant on membership** — `nrm2` consumes the `inner_product` fold either way, so the leaf-vs-fold ratification does not touch its consumer relationship; only its *floor* rides the `l2-floor-under-l3-leaf-cohort` decision. Firm cycle-041 (D2).
- `scal` — arity-1 **member-of** `linear_combination` (`scal(α,x) = linear_combination [(α,x)]`, NOT merged); firm-on-positive-structure (syntactic-identity laws on the small fully-present `operator*=` surface). Firm cycle-041 (D3).
- `axpy` — arity-2 **member-of** `linear_combination` (`axpy(α,x,y) = linear_combination [(α,x),(1,y)]`, second coefficient fixed to 1, NOT merged); thin identity-in-form floor; the output-aliasing variant axis is the **fold's**, not leaf-specific. Firm cycle-043 (D3).
- `axpby` — arity-2 **member-of** `linear_combination` (`axpby(α,x,β,y) = linear_combination [(α,x),(β,y)]`, NOT merged); thin identity-in-form floor; output-aliasing axis is the fold's. Firm cycle-043 (D4).
- `axpbypcz` — arity-3 **member-of** `linear_combination` (`axpbypcz(α,x,β,y,γ,z) = linear_combination [(α,x),(β,y),(γ,z)]`, NOT merged); thin identity-in-form floor; output-aliasing axis is the fold's. Firm cycle-043 (D5).

*Fused-composite floor (cycle-043; a NEW thin-identity sub-shape — fork-INDEPENDENT on membership like `nrm2`, but a composite-with-no-fold-parent rather than a single leaf):*

- `normalize` — the fused vector-normalisation composite `normalize(x) = (nrm2(x), scal(1/nrm2(x), x))`, returning **both** the norm and the unit vector. **NO fold-parent** — neither a member of `inner_product` (its codomain is the unit `Tensor[N]` alongside the norm `Scalar`, not a bare `Scalar`) nor of `linear_combination` (the fused norm-then-rescale pairing is not a term-axis scalar-weighted-sum). Cites the same-layer L2 `nrm2` + `scal` floors as *consumed* constituents (NOT a fold of which it is a member). Thin / identity-in-form (no genuine kernel fusion to unfold — `linalg::Normalize` is already the one-line norm-then-rescale composition); the partiality non-law at `x=0` and the IEEE-754 reduction-tree caveats transport unchanged. **Fork-invariant / design-final** — the leaf-vs-fold fork can only re-anchor a fold-parented *leaf*, and `normalize` has no fold to fold into. Firm cycle-043 (D9).

*Fork-INDEPENDENT standalone floors (cycle-042; same-named L2 parents under firm L3 entries, like the cycle-041 floors, but with **NO fold-parent** — a distinct motif: each is a standalone elementwise leaf or constructed-operator / operator-to-data gate, NOT a member/leaf/consumer of `inner_product` / `linear_combination`. **Design-final** — the leaf-vs-fold fork cannot re-anchor a leaf that has no fold to fold into):*
[new]:*Fold-family specialization / consumer stubs (cycle-052 vocabulary-shift-redirect refactor — the combinator is the entry, these are thin specialization/consumer pointers; refactor-pass COMPLETE. NOT standalone floors — each defers all semantics/laws/fusion content to its combinator and keeps only its unique L0 anchors + its one variant-axis row):*

- `dot` — **`M = I` Hermitian/symmetric specialization-stub** of `inner_product`; the conjugation variant-axis (`dot` Hermitian-conjugated vs `tdot` unconjugated — value-bearing for complex vectors) is the leaf-level fact the stub keeps. Do-NOT-merge (§"Fold-cohort boundary"). Reduced cycle-052 (D3).
- `nrm2` — **consumer-stub** of `inner_product` (`√ ∘ abs ∘ inner_product` at `y=x`, NOT a fold member — do-NOT-merge); the `std::abs` defensive guard preserved as an explicit load-bearing numerical claim. Reduced cycle-052 (D3).
- `scal` — **arity-1 specialization-stub** of `linear_combination` (`scal(α,x) = linear_combination [(α,x)]`). Reduced cycle-052 (D1).
- `axpy` — **arity-2 specialization-stub** of `linear_combination` (`axpy(α,x,y) = linear_combination [(α,x),(1,y)]`, second coefficient fixed to 1); the output-aliasing variant axis is the **fold's**, not leaf-specific. Reduced cycle-052 (D1).
- `axpby` — **arity-2 specialization-stub** of `linear_combination` (`axpby(α,x,β,y) = linear_combination [(α,x),(β,y)]`); output-aliasing axis is the fold's. Reduced cycle-052 (D1).
- `axpbypcz` — **arity-3 specialization-stub** of `linear_combination` (`axpbypcz(α,x,β,y,γ,z) = linear_combination [(α,x),(β,y),(γ,z)]`); output-aliasing axis is the fold's. Reduced cycle-052 (D1).

*Fused composite (cycle-043; NO fold combinator to defer to — stays a full standalone entry):*

- `normalize` — the fused vector-normalisation composite `normalize(x) = (nrm2(x), scal(1/nrm2(x), x))`, returning **both** the norm and the unit vector. **NO fold-parent** — neither a member of `inner_product` (its codomain is the unit `Tensor[N]` alongside the norm `Scalar`, not a bare `Scalar`) nor of `linear_combination` (the fused norm-then-rescale pairing is not a term-axis scalar-weighted-sum). Cites the same-layer L2 `nrm2` + `scal` stubs as *consumed* constituents. Thin / identity-in-form (no genuine kernel fusion to unfold — `linalg::Normalize` is already the one-line norm-then-rescale composition); the partiality non-law at `x=0` and the IEEE-754 reduction-tree caveats transport unchanged. Firm cycle-043 (D9).

*Standalone elementwise / gate entries (cycle-042; same-named L2 entries under firm L3 entries with **NO fold combinator** to defer to — each is a standalone elementwise leaf or constructed-operator / operator-to-data gate; they stay full standalone entries, no specialization-stub reduction applies):*
```

### 3. L2/index.md — §Working-Notes "Fold-cohort boundary" leaf-floor generalization sub-bullet

```edit:book/src/L2/index.md
[old]:  - **Leaf-floor generalization (RATIFIED, batch-12 meta-phase, decision 1).** Each firm L3 leaf gets a same-named L2 floor (the "both L levels" invariant); the floor is cited as leaf-of / consumer-of / member-of the relevant fold and defers all fusion content to the fold-parent — a layer-coherence pointer, not a rival fold. This is the cohort-wide rule the batch-12 meta-phase ratified (keeping leaf-floor (b) over the fold-only (a) reading); it covers the held `axpy`/`axpby`/`axpbypcz` arity floors (landed cycle-043) so they do not re-litigate the leaf-vs-fold question, and the `nrm2` consumer carve-out (fork-invariant on membership — it consumes the fold either way). The do-NOT-merge boundary above is what makes the floor a *pointer* rather than a duplicate of the fold.
[new]:  - **Combinator-as-entry + specialization-stub leaves (SUPERSEDES the batch-12 leaf-floor ratification; vocabulary-shift redirect 2026-06-01, refactor COMPLETE cycle-052).** The fold family's L2 entry is the **combinator** (`inner_product` / `linear_combination`); the once-standalone same-named leaves are **reduced specialization / consumer stubs** that defer all semantics/laws/fusion content to the combinator and keep only their unique L0 anchors + one variant-axis row. This **retires** the batch-12 "keep leaf-floor (b)" ratification and the `l2-floor-under-l3-leaf-cohort` foundation-first framing: a same-named base-form floor mirrored beside the combinator was the rectangular pattern the redirect supersedes (the "leaf-vs-fold fork" is moot — the resolution is the combinator-as-entry, not either fork branch). The do-NOT-merge boundary above is what keeps `dot` a *specialization* and `nrm2` a *consumer* of `inner_product` rather than merged into it. The stubs land cycle-052 (D1: `scal`/`axpy`/`axpby`/`axpbypcz`; D3: `dot`/`nrm2`); the leaf chapters are KEPT on disk (reduce-to-stub, not delete) so all inbound links stay live.
```

### 4. L2/index.md — dep-map row: scal → specialization-stub of linear_combination

```edit:book/src/L2/index.md
[old]:| [`scal`](./scal.md) | `scal :: Scalar -> Tensor[N] -> Tensor[N]` (`α, x → α·x`) | **Base scalar-vector-multiply leaf; arity-1 member of [`linear_combination`](./linear_combination.md) (cited, NOT merged — fold-cohort boundary load-bearing).** Leaf primitive (no L2 constituents; sub-operations below L2 resolution). Fold-parent: `linear_combination` (`scal(α,x) = linear_combination [(α,x)]`). Concepts: [`scalar-promotion`](../concepts/scalar-promotion.md) (element-type axis + real⊑complex sub-axis), [`scal`](../concepts/scal.md) (cross-cut prose). Sibling-subsumed by `axpby` (β=0). L1 anchor via identity-in-form rotation (whole-tensor in/out at both layers; no kernel fusion to unfold beyond the degenerate arity-1 single-pass). | `firm` (harvested cycle-041 D3; L2 floor backfill under foundation-first directive `l2-floor-under-l3-leaf-cohort`; identity-lowering per CLAUDE.md §Methodology invariants **Identity-lowerings still require both L levels** — floors the firm L3 [`scal`](../L3/scal.md), cycle-011; firm-on-positive-structure — syntactic-identity laws on the small fully-present `operator*=` surface) |
[new]:| [`scal`](./scal.md) | `scal :: Scalar -> Tensor[N] -> Tensor[N]` (`α, x → α·x = linear_combination [(α,x)]`) | **Arity-1 specialization-stub of [`linear_combination`](./linear_combination.md)** (the combinator is the L2 family entry — see its §"Arity specializations"; `scal` is a specialization note under it, NOT merged). Semantics / laws / fusion-rotation framing deferred to the combinator; the stub keeps `scal`'s unique L0 anchors (the receiver-mutating `operator*=` member idiom + `Normalize`) + its element-type / scalar-promotion variant row. L1 anchor: [`scal`](../L1/scal.md) (carries the load-bearing one-to-one L0-symbol shape). | `firm` (specialization-stub; reduced cycle-052 D1 per the 2026-06-01 vocabulary-shift redirect — a same-named base-form floor mirrored beside the combinator was the retired rectangular pattern; firm pointer up to the firm combinator `linear_combination` (cycle-018). Originally harvested cycle-041 D3 as a thin L2 floor) |
```

### 5. L2/index.md — dep-map row: axpy → specialization-stub of linear_combination

```edit:book/src/L2/index.md
[old]:| [`axpy`](./axpy.md) | `axpy :: Scalar -> Tensor[N] -> Tensor[N] -> Tensor[N]` (`α, x, y → α·x + y`) | **Base scalar-vector fused-update leaf; arity-2 member of [`linear_combination`](./linear_combination.md), second coeff fixed to 1 (cited, NOT merged — fold-cohort boundary load-bearing).** Leaf primitive (no L2 constituents; the fused scaled-add is kept whole per `axpby-as-primitive`, NOT decomposed into `scal` + tensor-add; sub-operations below L2 resolution). Fold-parent: `linear_combination` (`axpy(α,x,y) = linear_combination [(α,x),(1,y)]`). Concepts: [`scalar-promotion`](../concepts/scalar-promotion.md) (element-type axis + real⊑complex sub-axis), [`axpy`](../concepts/axpy.md) (cross-cut prose). Sibling subsumption (not dep): `axpy = axpby(α,x,1,y)` (β=1 of `axpby`); `scal(α,x) = axpy(α,x,0)`. **Output-aliasing is the FOLD's axis (carried by reference, NOT a leaf axis** — OQ `arity-family-leaf-floors-output-aliasing-axis-is-the-folds`). L1>L0 in-place via `axpby-mutation-rotation` sub-pattern A (β=1). L1 anchor via identity-in-form rotation (whole-tensor in/out at both layers; no kernel fusion to unfold beyond the arity-2 single-aligned pass). | `firm` (harvested cycle-043 D3; L2 floor backfill under foundation-first directive `l2-floor-under-l3-leaf-cohort`; identity-lowering per CLAUDE.md §Methodology invariants **Identity-lowerings still require both L levels** — floors the firm L3 [`axpy`](../L3/axpy.md), cycle-011; firm-on-positive-structure — syntactic-identity laws on the small fully-present `AXPY` free-function / `ComplexVector::AXPY` surface; leaf-vs-fold fork resolved keep-(b)) |
[new]:| [`axpy`](./axpy.md) | `axpy :: Scalar -> Tensor[N] -> Tensor[N] -> Tensor[N]` (`α, x, y → α·x + y = linear_combination [(α,x),(1,y)]`) | **Arity-2 specialization-stub of [`linear_combination`](./linear_combination.md)**, second coefficient fixed to 1 (the combinator is the L2 family entry; `axpy` is a specialization note under it, NOT merged). Semantics / laws / fusion-rotation framing deferred to the combinator; the stub keeps `axpy`'s unique L0 anchors (the `ComplexVector::AXPY` member + real-α-on-complex overloads) + its element-type / scalar-promotion variant row. **Output-aliasing is the FOLD's axis** (carried by reference, NOT leaf-specific). L1 anchor: [`axpy`](../L1/axpy.md) (L1>L0 in-place via `axpby-mutation-rotation` sub-pattern A, β=1). | `firm` (specialization-stub; reduced cycle-052 D1 per the 2026-06-01 vocabulary-shift redirect — the retired rectangular floor collapsed into the combinator; firm pointer up to the firm combinator `linear_combination`. Originally harvested cycle-043 D3 as a thin L2 floor) |
```

### 6. L2/index.md — dep-map row: axpby → specialization-stub of linear_combination

```edit:book/src/L2/index.md
[old]:| [`axpby`](./axpby.md) | `axpby :: Scalar -> Tensor[N] -> Scalar -> Tensor[N] -> Tensor[N]` (`α, x, β, y → α·x + β·y`) | **Base fused two-scalar two-vector linear-combination leaf; arity-2 member of [`linear_combination`](./linear_combination.md) (cited, NOT merged — fold-cohort boundary load-bearing).** Leaf primitive (no L2 constituents; the two scalar-multiplies + one add are below L2 resolution; fuse-don't-decompose per `scaffolding/decisions/axpby-as-primitive.md`). Fold-parent: `linear_combination` (`axpby(α,x,β,y) = linear_combination [(α,x),(β,y)]`). The arity-2 single-aligned `add(α,x,β,y,y)` pass is the fold's §"Fusion note"; the output-aliasing axis is the FOLD's (orthogonal to arity) — both deferred to `linear_combination`, NOT leaf-specific. Concepts: [`scalar-promotion`](../concepts/scalar-promotion.md) (element-type axis + real⊑complex sub-axis, real-(α,β)-on-complex). Subsumes `axpy` (β=1), `scal` (α=0 or β=0); sibling-subsumed by `axpbypcz` (γ=0). L1 anchor via identity-in-form rotation (whole-tensor in/out at both layers; the only fusion note is the arity-2 single-pass). | `firm` (harvested cycle-043 D4; L2 floor backfill under foundation-first directive `l2-floor-under-l3-leaf-cohort`, resolving the leaf-vs-fold fork in favor of leaf-floor (b); identity-lowering per CLAUDE.md §Methodology invariants **Identity-lowerings still require both L levels** — floors the firm L3 [`axpby`](../L3/axpby.md), cycle-011; firm-on-positive-structure — syntactic-identity laws on the small fully-present `AXPBY` surface; mirrors the cycle-041 D3 [`scal`](./scal.md) sibling floor) |
[new]:| [`axpby`](./axpby.md) | `axpby :: Scalar -> Tensor[N] -> Scalar -> Tensor[N] -> Tensor[N]` (`α, x, β, y → α·x + β·y = linear_combination [(α,x),(β,y)]`) | **Arity-2 specialization-stub of [`linear_combination`](./linear_combination.md)**, general second coefficient (the combinator is the L2 family entry; `axpby` is a specialization note under it, NOT merged). Semantics / laws / fusion-rotation framing (incl. the arity-2 single-aligned `add(α,x,β,y,y)` fusion pass) deferred to the combinator; the stub keeps `axpby`'s unique L0 anchors (the `ComplexVector::AXPBY` member + complex/real-scalar overloads) + its element-type / scalar-promotion variant row. **Output-aliasing is the FOLD's axis** (carried by reference, NOT leaf-specific). L1 anchor: [`axpby`](../L1/axpby.md) (L1>L0 in-place via `axpby-mutation-rotation`). | `firm` (specialization-stub; reduced cycle-052 D1 per the 2026-06-01 vocabulary-shift redirect — the retired rectangular floor collapsed into the combinator; firm pointer up to the firm combinator `linear_combination`. Originally harvested cycle-043 D4 as a thin L2 floor) |
```

### 7. L2/index.md — dep-map row: axpbypcz → specialization-stub of linear_combination

```edit:book/src/L2/index.md
[old]:| [`axpbypcz`](./axpbypcz.md) | `axpbypcz :: Scalar -> Tensor[N] -> Scalar -> Tensor[N] -> Scalar -> Tensor[N] -> Tensor[N]` (`α,x,β,y,γ,z → α·x + β·y + γ·z`; fused three-scalar three-vector combination) | **Base fused three-term linear-combination leaf; arity-3 member of [`linear_combination`](./linear_combination.md) (cited, NOT merged — fold-cohort boundary load-bearing).** Leaf primitive (no L2 constituents; the three scalar-multiplies + two adds are below L2 resolution; no kernel fusion to unfold at the leaf — the single-aligned `add(α,x,β,y,z)` pass / `γ==0` arity-collapse is deferred to the fold's §"Fusion note"). Fold-parent: `linear_combination` (`axpbypcz(α,x,β,y,γ,z) = linear_combination [(α,x),(β,y),(γ,z)]`). The **output-aliasing** in-place/out-of-place variant is the fold's axis, NOT leaf-specific. Concepts: [`scalar-promotion`](../concepts/scalar-promotion.md) (element-type axis + real⊑complex sub-axis, all-or-none across the scalar triple). Sibling-subsumed: `axpby` (γ=0), `axpy` (β=1,γ=0), `scal` (α=0,β=0). L1 anchor via identity-in-form rotation (whole-tensor in/out at both layers; no leaf kernel fusion to unfold beyond the arity-3 single pass). | `firm` (harvested cycle-043 D5; L2 floor backfill under plan item `l2-floor-under-l3-leaf-cohort`; identity-lowering per CLAUDE.md §Methodology invariants **Identity-lowerings still require both L levels** — floors the firm L3 [`axpbypcz`](../L3/axpbypcz.md), cycle-011; the arity-3 fold-member sibling of the cycle-041 [`scal`](./scal.md) floor; leaf-floor reading (b) per the batch-12-resolved `dot-l2-leaf-floor-vs-fold-only-design` fork; firm-on-positive-structure — syntactic-identity laws on the three fully-present `AXPBYPCZ` template specialisations `vector.cpp:745-772`) |
[new]:| [`axpbypcz`](./axpbypcz.md) | `axpbypcz :: Scalar -> Tensor[N] -> Scalar -> Tensor[N] -> Scalar -> Tensor[N] -> Tensor[N]` (`α,x,β,y,γ,z → α·x + β·y + γ·z = linear_combination [(α,x),(β,y),(γ,z)]`) | **Arity-3 specialization-stub of [`linear_combination`](./linear_combination.md)** (the maximal fixed-arity L0 symbol; the combinator is the L2 family entry; `axpbypcz` is a specialization note under it, NOT merged). Semantics / laws / fusion-rotation framing (incl. the single-aligned `add(α,x,β,y,z)` pass + the `γ==0` arity-collapse, which is the combinator's zero-coefficient term-drop law) deferred to the combinator; the stub keeps `axpbypcz`'s unique L0 anchors (the `AXPBYPCZ` member + the three template specialisations `palace/linalg/vector.cpp:745-772`) + its element-type / scalar-promotion variant row. **Output-aliasing is the FOLD's axis** (carried by reference, NOT leaf-specific). L1 anchor: [`axpbypcz`](../L1/axpbypcz.md). | `firm` (specialization-stub; reduced cycle-052 D1 per the 2026-06-01 vocabulary-shift redirect — the retired rectangular floor collapsed into the combinator; firm pointer up to the firm combinator `linear_combination`. Originally harvested cycle-043 D5 as a thin L2 floor) |
```

### 8. L2/index.md — dep-map row: dot → specialization-stub of inner_product

```edit:book/src/L2/index.md
[old]:| [`dot`](./dot.md) | `(x: Tensor[N], y: Tensor[N]) -> Scalar` (Hermitian `xᴴ y` / symmetric; `tdot` = unconjugated co-variant) | **Conjugation-axis leaf floor of [`inner_product`](./inner_product.md) (do NOT merge — codomain/fold distinction load-bearing, §"Fold-cohort boundary").** The plain (`M = I`) Hermitian / symmetric member of the reduce-to-`Scalar` fold, rendered as its own L2 chapter so the L3 [`dot`](../L3/dot.md) leaf rests on an adjacent same-named L2 parent (foundation-first directive `l2-floor-under-l3-leaf-cohort`). Same-layer deps: none (leaf). Fusion content deferred to `inner_product` §"Fusion note". L1 anchor: [`dot`](../L1/dot.md) (firm). Consumers: `krylov-step`, `orthogonalize`, `gram`, `deflate`. Concepts: `dot`. | `firm` (harvested cycle-041 wave-1; thin identity-in-form L2 floor entry; value-thread-isomorphic to L1 leaf; laws inherited unchanged; no leaf-unique fusion beyond fold-parent) |
[new]:| [`dot`](./dot.md) | `(x: Tensor[N], y: Tensor[N]) -> Scalar` (Hermitian `xᴴ y` / symmetric; `tdot` = unconjugated co-variant) | **`M = I` Hermitian/symmetric specialization-stub of [`inner_product`](./inner_product.md)** (do NOT merge — codomain/fold distinction load-bearing, §"Fold-cohort boundary"). The combinator is the L2 family entry; `dot` is a specialization note under it. Semantics / laws / fusion-rotation framing deferred to the combinator §"Specializations"; the stub keeps the **conjugation variant-axis** (`dot` Hermitian-conjugated vs `tdot` unconjugated — value-bearing for complex vectors) + `dot`'s unique L0 anchors (the `ComplexVector::Dot`/`TransposeDot` kernels + the self-dot fast path). L1 anchor: [`dot`](../L1/dot.md) (firm). Consumers: `krylov-step`, `orthogonalize`, `gram`, `deflate`. Concepts: `dot`. | `firm` (specialization-stub; reduced cycle-052 D3 per the 2026-06-01 vocabulary-shift redirect — the retired conjugation-axis leaf floor collapsed into the combinator; firm pointer up to the firm combinator `inner_product` (cycle-050). Originally harvested cycle-041 wave-1 as a thin L2 floor) |
```

### 9. L2/index.md — dep-map row: nrm2 → consumer-stub of inner_product

```edit:book/src/L2/index.md
[old]:| [`nrm2`](./nrm2.md) | `Tensor[N] -> Scalar` (≡ `√ ∘ abs ∘ inner_product` at `y=x`; `nrm2(x) = √(abs(inner_product(x, x)))`, always real, non-negative) | **Consumer of the `inner_product` fold (NOT a fold member — do-NOT-merge per §"Fold-cohort boundary").** Consumes: L2 [`inner_product`](./inner_product.md) (the length-axis fold at `y=x`) + scalar `abs` / `√` post-composition. The `std::abs` defensive guard preserved as an explicit load-bearing numerical claim (classification at [`L1-L0/nrm2-mutation-rotation`](../L1-L0/nrm2-mutation-rotation.md)). One variant axis: element-type (real/complex, collapsed — result always real). Consumer: [`krylov-step`](./krylov-step.md) (residual-norm readout + Arnoldi sub-diagonal). Identity-in-form floor under firm L3 [`nrm2`](../L3/nrm2.md). | `firm` (harvested cycle-041 wave-1 / D2; identity-in-form L2 floor under the firm L3 BLAS-1 cohort per the 2026-05-31 `l2-floor-under-l3-leaf-cohort` foundation-first directive; the fusion rotation is a no-op — `linalg::Norml2` is already the one-line unfolded composition) |
[new]:| [`nrm2`](./nrm2.md) | `Tensor[N] -> Scalar` (≡ `√ ∘ abs ∘ inner_product` at `y=x`; `nrm2(x) = √(abs(inner_product(x, x)))`, always real, non-negative) | **Consumer-stub of the [`inner_product`](./inner_product.md) fold (NOT a fold member — do-NOT-merge per §"Fold-cohort boundary").** Consumes: L2 `inner_product` (the length-axis fold at `y=x`) + scalar `abs` / `√` post-composition. Semantics / laws deferred to the combinator + the in-line §Downward consumer note; the stub keeps the **load-bearing `std::abs` defensive guard** (preserved as an explicit numerical claim; classification at [`L1-L0/nrm2-mutation-rotation`](../L1-L0/nrm2-mutation-rotation.md)) + the `Norml2` anchor + the element-type variant row (collapsed — result always real). Consumer: [`krylov-step`](./krylov-step.md) (residual-norm readout + Arnoldi sub-diagonal). L1 anchor: [`nrm2`](../L1/nrm2.md) (firm). | `firm` (consumer-stub; reduced cycle-052 D3 per the 2026-06-01 vocabulary-shift redirect — `nrm2` is the do-NOT-merge consumer carve-out, NOT a fold member; firm pointer up to the firm combinator `inner_product` (cycle-050). Originally harvested cycle-041 wave-1 / D2 as a thin L2 floor) |
```

### 10. L2/index.md — §Working-Notes cycle-041 cohort bullet (retire rectangular framing, append refactor-completion)

```edit:book/src/L2/index.md
[old]:- **Cycle-041 BLAS-1-floor cohort (the first L2-floor-under-L3 slice; firm 9 → 12).** Three identity-in-form **floor** entries landed under the 2026-05-31 foundation-first directive `l2-floor-under-l3-leaf-cohort`, raising the firm cohort **9 → 12** (partly-constructive `deflate` unchanged at 1). These are the **first L2-floor-under-L3 entries**: each gives a firm L3 BLAS-1 leaf (backfilled cycle-011) a *present adjacent same-named L2 parent* per **Identity-lowerings still require both L levels**, rather than the L3 leaf skipping a layer down to L1. (1) [`dot`](./dot.md) (firm cycle-041 D1) — the conjugation-axis **leaf-of** `inner_product` (the plain `M = I` Hermitian / symmetric member), a thin identity-in-form floor whose laws are inherited unchanged from the firm L1 leaf; **NOT merged** into the fold-parent (the codomain/fold do-NOT-merge boundary is load-bearing). (2) [`nrm2`](./nrm2.md) (firm cycle-041 D2) — a **consumer-of** `inner_product` (`√ ∘ abs ∘ inner_product` at `y=x`), **NOT a fold member**; the fusion rotation is a no-op (`linalg::Norml2` is already the one-line unfolded composition) and the `std::abs` defensive guard is preserved as an explicit load-bearing numerical claim. (3) [`scal`](./scal.md) (firm cycle-041 D3) — the arity-1 **member-of** `linear_combination` (`scal(α,x) = linear_combination [(α,x)]`), **cited NOT merged**; firm-on-positive-structure (syntactic-identity laws on the small fully-present `operator*=` surface). The companion adjacent thin-identity themes landed the same cycle (L2>L1: `dot-leaf-identity` / `nrm2-leaf-identity` / `scal-leaf-identity` — the latter two renamed cycle-043 from `-fold-specialization`; L3>L2: `dot-body-identity` / `nrm2-body-identity` / `scal-body-identity`).
[new]:- **Fold-family leaf cohort — REDUCED to combinator-pointer stubs cycle-052 (refactor-pass COMPLETE; vocabulary-shift redirect 2026-06-01).** The six fold-family L2 leaves originally harvested as thin floors (cycle-041 `dot`/`nrm2`/`scal` + cycle-043 `axpy`/`axpby`/`axpbypcz` under the now-retired `l2-floor-under-l3-leaf-cohort` foundation-first directive) are **reduced to specialization / consumer stubs** under their fold combinators: `scal`/`axpy`/`axpby`/`axpbypcz` are arity-1/2/2/3 **specialization-stubs of `linear_combination`** (D1); `dot` is the `M = I` Hermitian/symmetric **specialization-stub of `inner_product`** (D3); `nrm2` is the do-NOT-merge **consumer-stub of `inner_product`** (D3). Each stub defers semantics / laws / fusion-rotation framing to its combinator and keeps only its unique L0 anchors + one variant-axis row (the conjugation axis for `dot`, the `std::abs` guard for `nrm2`, the element-type / scalar-promotion axis for the arity members; output-aliasing is the FOLD's axis for the linear-combination members). **The count is UNCHANGED** — reduce-to-stub keeps every file on disk (no delete), so all inbound links stay live and the dep-map row count is unchanged (21 firm + 1 partly-constructive = 22 rows). This **completes the refactor pass**: the retired rectangular-floor framing (a same-named base-form floor mirrored beside the combinator) is gone from the L2 fold-family cohort; the combinator is the entry, the leaves are thin specialization pointers. The companion `-body-identity` / `-leaf-identity` thin themes were demoted cycle-051 (the L3>L2 / L2>L1 degenerate identity-in-named-terms cohort, consolidated counts at `L3-L2/index.md` / `L2-L1/index.md`).
```

### 11. L3/index.md — dep-map row: dot → specialization-stub of inner_product

```edit:book/src/L3/index.md
[old]:| [`dot`](./dot.md) | `Tensor[N] -> Tensor[N] -> Scalar` (real `dot` → real, complex `dot` → complex; complex `tdot` → complex; per-element kernel parameterized by element-type and conjugation-convention). | (leaf reduction at L3; consumed by `krylov-step`). Concepts: `dot`. | L1 [`dot`](../L1/dot.md) (identity-in-form on the primitive's signature; rotation work is at the consuming `krylov-step` body wrapper, captured by `book/src/L3-L2/krylov-step-body-identity.md` §"Applicability conditions" point 3). No L3-L1 theme file — identity-in-form annotation lives in-line. | `firm` (harvested cycle-011T231500Z; identity-lowering backfill per CLAUDE.md §Methodology invariants — supersedes cycle-006 "no L3 row needed" verdict for the BLAS-1 cohort) |
[new]:| [`dot`](./dot.md) | `Tensor[N] -> Tensor[N] -> Scalar` (real `dot` → real, complex `dot` → complex; complex `tdot` → complex; per-element kernel parameterized by element-type and conjugation-convention). | **`M = I` Hermitian/symmetric specialization of the L3 combinator [`inner_product`](./inner_product.md)** (`dot = inner_product` at the Hermitian/symmetric kernel; do-NOT-merge). The stub keeps the value-bearing conjugation variant-axis + the consuming-`krylov-step` context; semantics / laws deferred to the combinator §"Specializations". Concepts: `dot`. | L2 [`inner_product`](../L2/inner_product.md) via the combinator's §"Downward to L2 (through inner_product)" identity edge (read at the Hermitian/symmetric kernel value), then the substantive L2>L1 [`inner-product-fold-specialization`](../L2-L1/inner-product-fold-specialization.md); transitively L1 [`dot`](../L1/dot.md) in-line (no `L3-L1/` directory; no per-leaf `dot-body-identity` theme — demoted cycle-051). | `firm` (specialization-stub; harvested cycle-011 as an identity-in-form backfill, re-expressed cycle-051 D3 through the `inner_product` combinator, reduced to a specialization-stub cycle-052 D3 per the 2026-06-01 vocabulary-shift redirect; firm pointer up to the firm L3 combinator `inner_product` (cycle-050)) |
```

### 12. L3/index.md — dep-map row: nrm2 → consumer-stub of inner_product

```edit:book/src/L3/index.md
[old]:| [`nrm2`](./nrm2.md) | `Tensor[N] -> Scalar` (always real-valued; element-type axis collapses to single operator). | L3: [`dot`](./dot.md) via the defining identity `nrm2(x) = √dot(x, x)`. Concepts: `nrm2`, `convergence-test` (consumer-side). | L1 [`nrm2`](../L1/nrm2.md) (identity-in-form on the primitive's signature; rotation work is at the consuming `krylov-step` body wrapper / outer convergence-test consumer, captured by `book/src/L3-L2/krylov-step-body-identity.md` §"Applicability conditions" point 3). No L3-L1 theme file — identity-in-form annotation lives in-line. | `firm` (harvested cycle-011T231500Z; identity-lowering backfill per CLAUDE.md §Methodology invariants) |
[new]:| [`nrm2`](./nrm2.md) | `Tensor[N] -> Scalar` (≡ `√ ∘ abs ∘ inner_product` at `y=x`; always real-valued; element-type axis collapses to single operator). | **Consumer of the L3 combinator [`inner_product`](./inner_product.md) (NOT a fold member — do-NOT-merge): `√ ∘ abs ∘ inner_product` at `y=x`.** The stub keeps the load-bearing `std::abs` defensive guard + the consuming context; semantics deferred to the combinator + its in-line §Downward consumer note. Concepts: `nrm2`, `convergence-test` (consumer-side). | L2 [`inner_product`](../L2/inner_product.md) via the in-line §Downward consumer note (the `√ ∘ abs ∘` post-composition onto the identity-in-form fold), then transitively L1 [`nrm2`](../L1/nrm2.md) in-line (no `L3-L1/` directory; no per-leaf theme). | `firm` (consumer-stub; harvested cycle-011 as an identity-in-form backfill, demoted-as-CONSUMER cycle-051 D3 (NOT a fold member), reduced to a consumer-stub cycle-052 D3 per the 2026-06-01 vocabulary-shift redirect; firm pointer up to the firm L3 combinator `inner_product` (cycle-050)) |
```

### 13. L3/index.md — dep-map row: scal → specialization-stub of linear_combination (refresh "leaf" framing)

```edit:book/src/L3/index.md
[old]:| [`scal`](./scal.md) | `scal :: Scalar -> Tensor[N] -> Tensor[N]` (`α, x → α·x`) | (leaf; sibling-subsumed by `axpby` β=0). Concepts: [`scalar-promotion`](../concepts/scalar-promotion.md), [`scal`](../concepts/scal.md). L1 anchor via identity-in-form rotation on the primitive's signature shape (whole-tensor in / whole-tensor out at both layers). | L1 [`scal`](../L1/scal.md) via identity-in-form rotation on the primitive's signature (no `L3-L1/` theme file — identity-in-form annotation lives in-line per cycle-011 wave-1 cohort precedent). | `firm` (harvested cycle-011T234540Z; identity-lowering backfill per CLAUDE.md §Methodology invariants; **closes the BLAS-1 cohort portion** of OQ `l3-backfill-apply-linop-and-blas1-cohort` HIGH CONFIDENCE recommendations) |
[new]:| [`scal`](./scal.md) | `scal :: Scalar -> Tensor[N] -> Tensor[N]` (`α, x → α·x = linear_combination [(α,x)]`) | **Arity-1 specialization of the L3 combinator [`linear_combination`](./linear_combination.md)** (`scal = linear_combination` at term-list length 1). The stub keeps `scal`'s unique receiver-mutating `operator*=` L0 surface + the element-type / scalar-promotion variant row; semantics / laws deferred to the combinator §"Arity specializations". Concepts: [`scalar-promotion`](../concepts/scalar-promotion.md), [`scal`](../concepts/scal.md). | L2 [`linear_combination`](../L2/linear_combination.md) via the combinator's §"Downward to L2" identity edge (read at length 1), then the substantive L2>L1 [`linear-combination-fold-specialization`](../L2-L1/linear-combination-fold-specialization.md); transitively L1 [`scal`](../L1/scal.md) in-line (no `L3-L1/` directory; no per-leaf body-identity theme — demoted cycle-051). | `firm` (specialization-stub; harvested cycle-011 as an identity-in-form backfill, re-expressed cycle-051 D2 through the `linear_combination` combinator, reduced to a specialization-stub cycle-052 D2 per the 2026-06-01 vocabulary-shift redirect; firm pointer up to the firm L3 combinator `linear_combination` (cycle-050)) |
```

### 14. L3/index.md — §Working-Notes: the cycle-050/051 combinator bullet (refactor-pass-complete tense + count tally; the single authoritative L3 count)

```edit:book/src/L3/index.md
[old]:The four L3 BLAS-1 leaves (`scal`/`axpy`/`axpby`/`axpbypcz`) + the `dot` reduction were **re-expressed through these combinators cycle-051** (D1/D2 — `scal`/`axpy`/`axpby`/`axpbypcz` speak through [`linear_combination`](./linear_combination.md) as arity-1/2/2/3 specializations; `dot` is the Hermitian/symmetric specialization of [`inner_product`](./inner_product.md)); `nrm2` was demoted-as-CONSUMER (D3 — `√ ∘ abs ∘ inner_product` at `y=x`, NOT a fold member, do-NOT-merge — its identity note lands on its own entry, not absorbed into the combinator). Each L3 leaf chapter still stands firm in its own right (re-expressed in place, NOT deleted — the leaf-CHAPTER disposition remains HELD/meta-gated under OQ `l2-l3-leaf-chapter-disposition-cohort-wide` / `collapsed-leaf-disposition-convention-cohort-wide`; only the THEME files were deleted). The L3>L2 edge of each combinator is now a degenerate identity-in-named-terms in-line §"Downward to L2" note (the home the four `{scal,axpy,axpby,axpbypcz}-body-identity` / the `dot-body-identity` themes were demoted into cycle-051 — those theme files deleted, their identity-in-form content absorbed); the substantive rotation in each chain is the existing firm L2>L1 fold-specialization theme (`linear-combination-fold-specialization` / `inner-product-fold-specialization`, both KEPT cycle-049). With cycle-051 the **refactor-pass theme-demotion sweep is COMPLETE** (the degenerate identity-in-named-terms cohort fully discharged across the D8 −1-corrected 17 degenerate {body,leaf} pairs — 23 themes demoted + 1 KEPT-substantive `divfree-projector-leaf-identity`; equivalently, all 16 fully-degenerate pairs demoted both edges and the 17th `divfree-projector` pair demoted body-only with its leaf edge KEPT; consolidated counts at `L3-L2/index.md` / `L2-L1/index.md` §Working-Notes). This bullet's count update remains folded into the authoritative tally above — **L3 firm count is UNCHANGED at 17** (the leaf re-expression is in-place; the two combinators were counted at c050).
[new]:The four L3 BLAS-1 leaves (`scal`/`axpy`/`axpby`/`axpbypcz`) + the `dot` reduction were **re-expressed through these combinators cycle-051** (D1/D2 — `scal`/`axpy`/`axpby`/`axpbypcz` speak through [`linear_combination`](./linear_combination.md) as arity-1/2/2/3 specializations; `dot` is the Hermitian/symmetric specialization of [`inner_product`](./inner_product.md)); `nrm2` was demoted-as-CONSUMER (D3 — `√ ∘ abs ∘ inner_product` at `y=x`, NOT a fold member, do-NOT-merge — its identity note lands on its own entry, not absorbed into the combinator). **Cycle-052 (this cycle) completed the refactor by REDUCING the six fold-family leaf chapters to combinator-pointer stubs** (`L3/{scal,axpy,axpby,axpbypcz}` → specialization-stubs of `linear_combination` (D2); `L3/dot` → specialization-stub + `L3/nrm2` → consumer-stub of `inner_product` (D3); the four L2 + `L2/dot`/`L2/nrm2` siblings reduced D1/D3): the duplicated semantics/laws body is collapsed into the combinator, each stub keeps only its unique L0 anchors + one variant-axis row. **The leaf chapters are KEPT on disk (reduce-to-stub, NOT delete)** — every inbound link stays live by construction, and the leaf-CHAPTER disposition is now SETTLED (reduce-to-stub) under the `collapsed-leaf-disposition-convention-cohort-wide` convention. The L3>L2 edge of each combinator is the degenerate identity-in-named-terms in-line §"Downward to L2" note (the home the four `{scal,axpy,axpby,axpbypcz}-body-identity` / the `dot-body-identity` themes were demoted into cycle-051 — those theme files deleted, their identity-in-form content absorbed); the substantive rotation in each chain is the existing firm L2>L1 fold-specialization theme (`linear-combination-fold-specialization` / `inner-product-fold-specialization`, both KEPT cycle-049). With cycle-052 the **refactor pass is COMPLETE** across both the theme-demotion sweep (cycle-051; the degenerate identity-in-named-terms cohort fully discharged across the D8 −1-corrected 17 degenerate {body,leaf} pairs — 23 themes demoted + 1 KEPT-substantive `divfree-projector-leaf-identity`; consolidated counts at `L3-L2/index.md` / `L2-L1/index.md` §Working-Notes) and the leaf-chapter reduction (cycle-052). **L3 firm count is UNCHANGED at 17 firm + 3 partial-obstruction** (the leaf reductions are in-place, no file deleted; the two combinators were counted at c050) — this remains folded into the single authoritative tally in the `orthogonalize`-landing bullet above. (Cycle-052 D4 layer-intro-author is the wave's SOLE index-owner + count-owner per the cycle-039 meta-phase count-ownership convention, friction-ledger `parallel-blind-shared-index-count-divergence`; D1/D2/D3 each owned only their own leaf-chapter bodies and deferred the index rows + count tally entirely.)
```

### 15. L3/index.md — dep-map row: linear_combination (the "collapses"→"collapsed" tense touch, D2-flagged)

```edit:book/src/L3/index.md
[old]: Same-layer L3 BLAS-1 leaves [`scal`](./scal.md)/[`axpy`](./axpy.md)/[`axpby`](./axpby.md)/[`axpbypcz`](./axpbypcz.md) (the arity-1/2/2/3 specializations, recovered as term-list length — cycle-051 collapses them into §"Arity specializations" notes); concepts [`scalar-promotion`](../concepts/scalar-promotion.md), [`tensor-field-lift`](../concepts/tensor-field-lift.md). Consumed by [`krylov-step`](./krylov-step.md) (the GMRES basis-correction sum is a `linear_combination`).
[new]: Same-layer L3 BLAS-1 leaves [`scal`](./scal.md)/[`axpy`](./axpy.md)/[`axpby`](./axpby.md)/[`axpbypcz`](./axpbypcz.md) (the arity-1/2/2/3 specializations, recovered as term-list length — collapsed cycle-051 into §"Arity specializations" notes, reduced to specialization-stubs cycle-052); concepts [`scalar-promotion`](../concepts/scalar-promotion.md), [`tensor-field-lift`](../concepts/tensor-field-lift.md). Consumed by [`krylov-step`](./krylov-step.md) (the GMRES basis-correction sum is a `linear_combination`).
```

### 16. L3/linear_combination.md — §Context residual future-tense (`:26`, D2 micro-sweep)

```edit:book/src/L3/linear_combination.md
[old]:This entry is the **propagate half** of the cycle-049 D1 replace-and-propagate map (`reports/2026-06-01T190900Z-combinator-miner-refactor-pass-linear-combination-family/CYCLE.md` (b.3)): cycle-049 D1 inverted the L2 entry to combinator-as-entry (the four arity leaves became specialization notes under it) but did not propagate to L3. This dispatch authors the L3 analog. The four L3 leaf chapters (`L3/{scal,axpy,axpby,axpbypcz}.md`) are **not** re-expressed through this combinator yet — that is cycle-051, gated on the unsettled cohort-wide leaf-disposition convention (delete-vs-redirect-stub; OQ `l2-l3-leaf-chapter-disposition-cohort-wide`). Until then the L3 leaves stand firm in their own right and this entry is the family entry they will collapse into.
[new]:This entry is the **propagate half** of the cycle-049 D1 replace-and-propagate map (`reports/2026-06-01T190900Z-combinator-miner-refactor-pass-linear-combination-family/CYCLE.md` (b.3)): cycle-049 D1 inverted the L2 entry to combinator-as-entry (the four arity leaves became specialization notes under it) but did not propagate to L3; this entry is the L3 analog. The four L3 leaf chapters (`L3/{scal,axpy,axpby,axpbypcz}.md`) were re-expressed through this combinator cycle-051 (D2) and **reduced to specialization-stubs cycle-052** (D2) under the `collapsed-leaf-disposition-convention-cohort-wide` convention (reduce-to-stub, files KEPT on disk so inbound links stay live): each is the arity-1/2/2/3 readout label pointing up to this combinator, with its body collapsed into the §"Arity specializations" notes here. This combinator IS the L3 family entry.
```

### 17. L3/linear_combination.md — §Arity-specializations residual future-tense (`:61`, D2 micro-sweep)

```edit:book/src/L3/linear_combination.md
[old]:These names remain useful as *readout labels* for the bounded-arity L0 call shapes. They are NOT separate L3 operators with their own algebra — every law below is the combinator's; the per-arity facts (`axpby` bilinearity, `axpbypcz` trilinearity, etc.) are the multilinearity law (law 3) read at a fixed list length. **The standalone L3 leaf chapters `L3/{scal,axpy,axpby,axpbypcz}.md` still exist firm as of this cycle** — their collapse into these notes is cycle-051 (gated on the leaf-disposition convention); this entry is the family entry they will collapse into. The L3 form differs from the four L3 leaves only in **resolution**, along the arity axis: the leaves see four distinct fixed-arity operators; this combinator sees one variadic fold whose list length recovers each fixed arity.
[new]:These names remain useful as *readout labels* for the bounded-arity L0 call shapes. They are NOT separate L3 operators with their own algebra — every law below is the combinator's; the per-arity facts (`axpby` bilinearity, `axpbypcz` trilinearity, etc.) are the multilinearity law (law 3) read at a fixed list length. **The L3 leaf chapters `L3/{scal,axpy,axpby,axpbypcz}.md` were reduced to specialization-stubs cycle-052** (reduce-to-stub, files KEPT on disk) — each defers its semantics / laws to these notes and points up to this combinator. The L3 form differs from the four leaf readout labels only in **resolution**, along the arity axis: the leaves see four distinct fixed-arity operators; this combinator sees one variadic fold whose list length recovers each fixed arity.
```

### 18. L3/linear_combination.md — §Dependencies residual future-tense (`:117`, D2 micro-sweep)

```edit:book/src/L3/linear_combination.md
[old]:**Same-layer (L3)**: this combinator unifies the four L3 BLAS-1 linear-update leaves [`scal`](./scal.md) (arity 1), [`axpy`](./axpy.md) (arity 2, second coeff 1), [`axpby`](./axpby.md) (arity 2), [`axpbypcz`](./axpbypcz.md) (arity 3) as list-length specializations (law 6). These L3 leaf chapters stand firm as of this cycle; their re-expression through this combinator (collapse into the §"Arity specializations" notes) is cycle-051 (gated on the leaf-disposition convention — OQ below). The composition surfaces that *consume* `linear_combination` at L3 are the iterate-stratum update inside [`krylov-step`](./krylov-step.md)'s `krylov_update` (the GMRES basis-correction sum is a `linear_combination` over scalar-weighted basis terms).
[new]:**Same-layer (L3)**: this combinator unifies the four L3 BLAS-1 linear-update leaves [`scal`](./scal.md) (arity 1), [`axpy`](./axpy.md) (arity 2, second coeff 1), [`axpby`](./axpby.md) (arity 2), [`axpbypcz`](./axpbypcz.md) (arity 3) as list-length specializations (law 6). These four leaf chapters were re-expressed through this combinator cycle-051 and reduced to specialization-stubs cycle-052 (reduce-to-stub, files KEPT on disk) — each defers to the §"Arity specializations" notes here. The composition surfaces that *consume* `linear_combination` at L3 are the iterate-stratum update inside [`krylov-step`](./krylov-step.md)'s `krylov_update` (the GMRES basis-correction sum is a `linear_combination` over scalar-weighted basis terms).
```

### 19. L2/normalize.md — stale `scal.md:223-228` code-span (`:111`, D1 micro-sweep)

```edit:book/src/L2/normalize.md
[old]:- Bare `scal(1/nrm2(x), x)` (norm discarded) is the projection `snd ∘ normalize`. When a consumer needs only the unit vector and not the norm, the `scal ∘ nrm2` spelling is equivalent; `normalize` is preferred when the norm is consumed downstream (the load-bearing case that justifies naming the fusion). This is exactly the open question the [`scal`](./scal.md) §Dependencies "Sibling subsumption" note flagged (`book/src/L2/scal.md:223-228`) — `Normalize(x) = scal(1/nrm2(x), x)` paired with the returned norm; this entry closes it (the fused composite gets its own L2 floor).
[new]:- Bare `scal(1/nrm2(x), x)` (norm discarded) is the projection `snd ∘ normalize`. When a consumer needs only the unit vector and not the norm, the `scal ∘ nrm2` spelling is equivalent; `normalize` is preferred when the norm is consumed downstream (the load-bearing case that justifies naming the fusion). This is exactly the open question the [`scal`](./scal.md) specialization-stub once flagged in its §Dependencies "Sibling subsumption" note (`Normalize(x) = scal(1/nrm2(x), x)` paired with the returned norm); this entry closes it (the fused composite gets its own L2 entry).
```

### 20. L2/normalize.md — stale `scal.md:223-228` code-span (`:141`, D1 micro-sweep)

```edit:book/src/L2/normalize.md
[old]:This dispatch is the **L2 floor backfill** (cycle-043 D9) under the foundation-first directive `l2-floor-under-l3-leaf-cohort`: the L2 form was previously referenced only as the fused `nrm2 + scal` construct inside [`scal`](./scal.md) §Dependencies (`book/src/L2/scal.md:223-228`) and the `orthogonalize` output-normalisation step; it now has its own L2 entry per **Identity-lowerings still require both L levels** — flooring the firm L3 [`normalize`](../L3/normalize.md) (cycle-039). It is the **last genuine missing floor** of the directive's leaf cohort.
[new]:This dispatch is the **L2 floor backfill** (cycle-043 D9) under the (now-retired) foundation-first directive `l2-floor-under-l3-leaf-cohort`: the L2 form was previously referenced only as the fused `nrm2 + scal` construct inside the [`scal`](./scal.md) §Dependencies sibling-subsumption note and the `orthogonalize` output-normalisation step; it now has its own L2 entry per **Identity-lowerings still require both L levels** — flooring the firm L3 [`normalize`](../L3/normalize.md) (cycle-039). It is the **last genuine missing floor** of that leaf cohort.
```

### 21. L2/normalize.md — stale `scal.md:223-228` code-span (`:164`, D1 micro-sweep)

```edit:book/src/L2/normalize.md
[old]:- `book/src/L2/scal.md` (firm cycle-041 D3) — the rescale constituent floor (`û = scal(1/β, x)`); the arity-1 member-of `linear_combination` whose law-8 round-trip makes `normalize` reconstruction-exact (law 3). Its §Dependencies sibling-subsumption note (`book/src/L2/scal.md:223-228`) flagged `Normalize(x) = scal(1/nrm2(x), x)` paired with the returned norm as an open question; this entry closes it.
[new]:- `book/src/L2/scal.md` (firm cycle-041 D3; reduced to an arity-1 specialization-stub of `linear_combination` cycle-052) — the rescale constituent (`û = scal(1/β, x)`); the arity-1 specialization of `linear_combination` whose law-8 round-trip makes `normalize` reconstruction-exact (law 3). Its former §Dependencies sibling-subsumption note flagged `Normalize(x) = scal(1/nrm2(x), x)` paired with the returned norm as an open question; this entry closes it.
```

## Supporting evidence

- **Producer reports** (wave-1, applied before this dispatch):
  - D1 `reports/2026-06-01T223300Z-lifter-l2-linear-combination-leaf-stubs/CYCLE.md` — `L2/{scal,axpy,axpby,axpbypcz}` reduced to specialization-stubs; frontmatter `fold_parent:`→`specialization_of:`; §Open-questions explicitly defers `index.md` dep-map rows + consolidated narrative + the `normalize.md:111/141/164` stale code-spans to D4 (the count-owner).
  - D2 `reports/2026-06-01T223300Z-lifter-l3-linear-combination-leaf-stubs/CYCLE.md` — `L3/{scal,axpy,axpby,axpbypcz}` reduced to specialization-stubs; flags the `L3/index.md` row + the `L3/linear_combination.md` residual future-tense + the `L3/index.md:31` "collapses"→"collapsed" tense touch as D4 micro-sweep targets.
  - D3 `reports/2026-06-01T223300Z-lifter-inner-product-family-leaf-stubs/CYCLE.md` — `L2/dot`+`L3/dot` → specialization-stubs; `L2/nrm2`+`L3/nrm2` → consumer-stubs; frontmatter `fold_parent:`→`specialization_of:`; index rows + consolidated narrative + §"Fold-cohort boundary" coherence deferred to D4.
- **The combinators (the family entries the stubs point up to)**: `book/src/L2/linear_combination.md` (firm cycle-018, inverted-to-entry cycle-049), `book/src/L2/inner_product.md` (firm cycle-019/050), `book/src/L3/linear_combination.md` (firm cycle-050), `book/src/L3/inner_product.md` (firm cycle-050).
- **Methodology**: CLAUDE.md §Methodology invariants ⟢ VOCABULARY-SHIFT REDIRECT (2026-06-01) — the combinator is the entry, leaves are specialization notes; rectangular-floor framing retired. `METHODOLOGY-REDIRECT.md`.
- **Count-ownership convention**: cycle-039 meta-phase, friction-ledger `parallel-blind-shared-index-count-divergence` — D4 (layer-intro-author) is the wave's SOLE count-owner; D1/D2/D3 defer the consolidated tally. The L3 single-authoritative-tally bullet (the `orthogonalize`-landing bullet) is unchanged at 17 firm + 3 partial-obstruction; the L2 dep-map row count is unchanged at 21 firm + 1 partly-constructive (reduce-to-stub keeps files on disk → no count delta).

## Frontmatter field-name coordination (D1/D3 flag — resolved as no-op)

D1 and D3 renamed the stubs' frontmatter `fold_parent:` → `specialization_of:`. I verified neither `book/src/L2/index.md` nor `book/src/L3/index.md` keys off the frontmatter field name — the dep-map rows reference the combinator by markdown link (`[`linear_combination`](./linear_combination.md)`), and the §Vocabulary-cohort / §Working-Notes prose references slugs in prose, never the frontmatter key. There is **no index consuming-convention to change**; the field rename is a per-stub-file naming alignment that the index does not observe. Noted, not structurally acted on. (If a future tooling pass introspects frontmatter for a specialization graph, `specialization_of:` is the consistent field — but no such consumer exists today.)

## Open questions / caveats

- **No count delta — verify at finalize.** This is a narrative + dep-map-row reconciliation, NOT a count change: reduce-to-stub keeps all 12 files on disk, so the L2 dep-map stays 22 rows (21 firm + 1 partly-constructive) and the L3 tally stays 17 firm + 3 partial-obstruction. The single-authoritative-tally bullets are NOT edited to change a number — the `orthogonalize`-landing bullet (L3) keeps 17+3; my L3 §Working-Notes edit (change 14) re-states the unchanged count and records the refactor-pass completion. Finalize should confirm no off-by-one crept in.
- **`L3/index.md` §Semantics-overlay obstruction-spectrum prose (`:15`) still lists `dot`/`scal` as obstruction-free-end leaves** — this is unchanged and CORRECT (the stubs are still obstruction-free; specialization-stub status does not change the obstruction profile). I did NOT touch it. Flagging only so a future reader does not mistake the un-edited "leaf" wording there for stale framing — "leaf" in the obstruction-profile sense (no loop) is orthogonal to "leaf-floor" in the retired rectangular sense.
- **`L3/index.md:31` `linear_combination` row "Lowers to" already correct** — it already says "cycle-051 collapses them into §Arity specializations notes" with the demoted-theme framing; I updated the §Dependencies cell tense ("collapses"→"collapsed", + "reduced to specialization-stubs cycle-052") in change 15. The "Lowers to" cell of that row was already past-tense-correct (demoted cycle-051) and is not touched.
- **The retired `l2-floor-under-l3-leaf-cohort` directive name is left in place in a few historical §Working-Notes bullets** (the cycle-042/043 cohort-growth narratives at `L2/index.md:117-118`) as historical landing-narrative, parenthetically marked "(now-retired)" where I touched adjacent prose (changes 10, 20). I did NOT rewrite the full cycle-042/043 historical bullets — they narrate what landed those cycles and the standalone-floor cohort (`reciprocal` etc.) is genuinely still a full-entry cohort (no combinator to defer to), so those bullets remain accurate as history. Only the fold-family-specific framing was retired (changes 1, 2, 3, 10).
- **OQ `l2-l3-leaf-chapter-disposition-cohort-wide` / `collapsed-leaf-disposition-convention-cohort-wide` is now CLOSED for the fold-family cohort** — the disposition is SETTLED as reduce-to-stub (files KEPT, bodies collapsed to combinator pointers). Surfacing for the OQ ledger: the convention applied uniformly across all 12 fold-family leaves this cycle; no leaf remained HELD. The integrator-per-report should promote this closure when applying D1/D2/D3/D4.
- **citecheck not run** — these are all prose/narrative edits to `book/src/L*/index.md` + two operator chapters; the only L0 line-pinpoints in my edits are inherited verbatim from the producers' stubs (e.g. `vector.cpp:745-772` in the `axpbypcz` row, carried from D1's self-verified stub) or are the combinator-row citations already on-disk-firm. No NEW L0 citations were authored. The stale `scal.md:223-228` bare code-spans were REMOVED (not re-pinned), per the batch-15 meta-phase NO-GO on a re-pin convention for append-only/shrinking prose.
