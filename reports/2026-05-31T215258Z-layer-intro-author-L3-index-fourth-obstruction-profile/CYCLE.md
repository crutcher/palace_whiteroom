---
agent: layer-intro-author
invoked_at: 2026-05-31T21:52:58Z
scope: L3 index — fold fourth obstruction profile into §Semantics-overlay taxonomy + own consolidated §Working-Notes firm-count tally
status: pending
integrated_at: 2026-05-31T222451Z
integration_commit: 1f178ff
integration_notes: "Applied clean by integrator-per-report (cycle-039 D3). 2 prose edits to L3/index.md: §Semantics-overlay 4th obstruction profile shape (d) obstruction-carrying-by-reference (Three->Four firm shapes) + §Working-Notes consolidated tally 15 firm + 2 partial-obstruction (c036 (A) cohort 6-of-6 COMPLETE). D3 owned the count tally this cycle per COUNT-OWNERSHIP partition; cleanly avoided the c038 parallel-blind count-divergence. No build-repair needed."
---

# CYCLE: L3 index — fourth obstruction profile + consolidated firm-count tally

## Summary

Two coordinated edits to `book/src/L3/index.md`, both intro/overview maintenance (no operator-chapter authoring):

**TASK 1 — §Semantics-overlay taxonomy.** The §Semantics-overlay "Sequential obstructions" bullet enumerates three firm obstruction *shapes* (a)/(b)/(c), all cases where Palace authors or owns the un-lifting loop. It does NOT yet name the **obstruction-carrying-by-reference** profile that the firm-c038 `divfree-projector` instantiates: a constructed-operator gate whose own four-step apply is obstruction-free straight-line composition, but which carries an inner gate's (`ksp_solve`'s) `sequential-obstruction` **by reference** — neither introducing a new obstruction nor erasing the inner one (per the `nested-constructed-operator-gate` fidelity rule). I fold this in as shape **(d)**, positioned in the prose as the spectrum-intermediate profile BETWEEN the obstruction-free gates (`jacobi-smoother`/`apply_linop`) and the obstruction-authoring gates (`ksp_solve`/`eigsolve`). This resolves the cycle-038 D3 "pending" flag.

**TASK 2 — consolidated §Working-Notes firm-count tally.** The §Working-Notes bullets carry per-cycle running counts authored parallel-blind across c037 (says "11 firm + 2") and c038 (says "14 firm + 2", "only `normalize` remains", "5-of-6 landed", "pending" fourth-profile flag). With cycle-039 D1 (harvester) landing `book/src/L3/normalize.md` — the 15th firm L3 operator and the CLOSER of the c036 (A) identity-in-form backfill cohort — these running counts diverge from the post-c039 truth. I replace the trailing c038 + pending-flag bullets with a single authoritative consolidated tally bullet reflecting the post-cycle-039 state: **15 firm + 2 partial-obstruction**, **c036 (A) cohort 6-of-6 COMPLETE**, and the fourth-obstruction-profile flag RESOLVED (now enacted in §Semantics-overlay). D1 is instructed not to touch the count tally; I own it.

The two edits hit operationally-distinct regions (§Semantics-overlay prose ~line 15; §Working-Notes tally ~lines 57-58). D1 appends a distinct dep-map ROW to the table (lines 21-36 region) — neither of my edits touches the dep-map table, so a serial apply after D1's row-append lands cleanly. My `edit:` fence context-anchors quote enough surrounding text to re-locate after any line-number shift.

## Proposed changes

### Edit 1 — §Semantics-overlay: fold in the fourth obstruction profile (shape (d))

Replaces the single "Sequential obstructions" bullet (currently enumerating three shapes (a)/(b)/(c)) with a version that adds the fourth shape (d) and reframes the lead sentence "Three firm shapes coexist" → "Four firm shapes coexist". All other bullets in the §Semantics-overlay list are untouched.

```edit:book/src/L3/index.md
[old]: - **Sequential obstructions** — explicit markers where global form is unavailable, with reason (recorded per `concepts/sequential-obstruction.md`). Three firm shapes coexist at L3: (a) the outer loop folding `krylov-step` does not lift, but the firm driver [`ksp_solve`](./ksp_solve.md) still renders it as an explicit value-threaded tail recursion (firm cycle-020); (b) [`chebyshev`](./chebyshev.md) (`partial-obstruction`) — body lifts, but the inner `k`-recurrence + outer `pc_it` Richardson sweep are sequential obstructions rooted in **numerical stability**; (c) [`eigsolve`](./eigsolve.md) (`partial-obstruction`) — body lifts, but the eigen-iteration loop is a sequential obstruction rooted in **opaque-library-ownership** (Palace authors no loop — SLEPc `EPSSolve` / ARPACK RCI own it). (b) and (c) are the two `partial-obstruction` operators; they share the body-lifts-loop-doesn't shape but differ in the *reason* the loop does not lift.
[new]: - **Sequential obstructions** — explicit markers where global form is unavailable, with reason (recorded per `concepts/sequential-obstruction.md`). Four firm shapes coexist at L3, arranged on an obstruction-profile spectrum from obstruction-free through obstruction-carrying to obstruction-authoring: (a) the outer loop folding `krylov-step` does not lift, but the firm driver [`ksp_solve`](./ksp_solve.md) still renders it as an explicit value-threaded tail recursion (firm cycle-020); (b) [`chebyshev`](./chebyshev.md) (`partial-obstruction`) — body lifts, but the inner `k`-recurrence + outer `pc_it` Richardson sweep are sequential obstructions rooted in **numerical stability**; (c) [`eigsolve`](./eigsolve.md) (`partial-obstruction`) — body lifts, but the eigen-iteration loop is a sequential obstruction rooted in **opaque-library-ownership** (Palace authors no loop — SLEPc `EPSSolve` / ARPACK RCI own it); (d) [`divfree-projector`](./divfree-projector.md) (`firm`, cycle-038) — **obstruction-carrying by reference**: its own apply is a fixed obstruction-free straight-line composition (`WeakDiv → Z_{bdr_eff} → ksp_solve → Grad`) authoring no loop of its own, but step 3 invokes the firm-L3 [`ksp_solve`](./ksp_solve.md) whose outer fold IS a sequential-obstruction; the gate neither introduces a new obstruction nor erases the inner one (the inner iteration stays interior to `ksp_solve` per the [`nested-constructed-operator-gate`](../concepts/nested-constructed-operator-gate.md) fidelity rule). Profile (d) sits in the spectrum **between** the obstruction-free constructed-operator gates / leaves ([`jacobi-smoother`](./jacobi-smoother.md), [`apply_linop`](./apply_linop.md) — apply is a single field op, no loop of any kind) and the obstruction-authoring gates ((a)/(c): `ksp_solve` authors its own fold, `eigsolve` delegates to an opaque library loop). (b) and (c) are the two `partial-obstruction` operators (body-lifts-loop-doesn't, differing only in the *reason* the loop does not lift: numerical stability vs. opaque-library-ownership); (a) and (d) are `firm` (their obstruction is rendered or carried-by-reference, not a partial lift). The distinction across (a)/(c)/(d) is *whose* loop carries the obstruction: `ksp_solve` its own, `eigsolve` an opaque library's, `divfree-projector` its inner `ksp_solve` gate's.
```

### Edit 2 — §Working-Notes: consolidated post-cycle-039 firm-count tally

Replaces the two trailing §Working-Notes bullets — the c038 three-backfill bullet (which carries the now-stale "14 firm + 2", "only `normalize` remains", "5-of-6 landed" running counts) and the "Fourth-obstruction-profile taxonomy note pending" bullet — with a single c038 bullet (count-claims stripped, narrative kept) followed by one authoritative consolidated tally bullet. The c038 narrative is preserved (it records what landed cycle-038 and the obstruction-carrying-by-reference distinguishing fact); only its trailing count sentence is removed, because the consolidated tally now owns the count. The fourth-profile pending flag is removed (resolved by Edit 1).

```edit:book/src/L3/index.md
[old]: - **Three firm L3 identity-in-form backfills landed cycle-038 (the elementwise / constructed-operator-gate cohort)**: [`reciprocal`](./reciprocal.md) (12th firm — the elementwise multiplicative-inverse self-map `x -> (1/x[i])ᵢ`, the `D⁻¹`-forming step of the `assemble_diagonal → reciprocal → elementwise_product` diagonal-preconditioner-apply chain; nonlinear, partial at `x[i]=0`; carries NO obstruction at L3 — embarrassingly parallel), [`elementwise_product`](./elementwise_product.md) (13th firm — the whole-tensor Hadamard binary product `a ⊙ b`; the Hadamard floor of the elementwise cohort, the operator-action realisation of a diagonal operator, sibling-subsumes `scal`; firm-on-positive-structure, carries NO obstruction at L3), and [`divfree-projector`](./divfree-projector.md) (14th firm — the divergence-free projector as a whole-tensor field operation; the **third constructed-operator gate** at L3 after `ksp_solve` c020 and `jacobi-smoother` c037). The three are (A) firm identity-in-form backfills of the cycle-036 D2 audit verdict (`book/src/L3/index.md:41`). `divfree-projector`'s distinguishing fact: it is **obstruction-carrying by reference** — its own four-step apply (`WeakDiv → Z_{bdr_eff} → ksp_solve → Grad`) is a fixed straight-line composition authoring NO obstruction, but step 3 invokes the firm-L3 `ksp_solve` inner H1 solve (`palace/linalg/divfree.cpp:175`), whose outer fold is a `sequential-obstruction`. The gate neither introduces a new obstruction nor erases the inner one — the inner CG iteration stays interior to `ksp_solve` per the [`nested-constructed-operator-gate`](../concepts/nested-constructed-operator-gate.md) fidelity rule. This places `divfree-projector` in the **middle of the L3 obstruction-profile spectrum**: between the obstruction-authoring `ksp_solve`/`eigsolve` (which own their loops) and the obstruction-free leaf `jacobi-smoother`/`apply_linop`/`dot`/`scal`. It instantiates the nested-gate transitive chain `eigsolve ⊃ divfree-projector ⊃ ksp_solve` at L3. All three are layer-coherence backfills per **Identity-lowerings still require both L levels**; the substantive rotations live at L1>L0 (`reciprocal-elementwise-product-mutation-rotation`; `divfree-projector-mutation-rotation`, delegating its inner solve to `ksp-solve-mutation-rotation`). **L3 firm-operator count now 14 firm + 2 `partial-obstruction`** (firm: `krylov-step` c010, `apply_linop` + `axpy` + `axpby` + `axpbypcz` + `dot` + `nrm2` + `scal` c011, `ksp_solve` c020, `assemble-diagonal` + `jacobi-smoother` c037, `reciprocal` + `elementwise_product` + `divfree-projector` c038; partial-obstruction: `chebyshev` c013, `eigsolve` c024). Of the six (A) backfills, **only `normalize` remains** (fused `nrm2 + scal`; held to cycle-039 — the cohort-closer); the c036 (A) cohort is now **5-of-6 landed**. See the c036 verdict list at `:41` under OQ `l3-cohort-growth-audit-c036-verdict`.
- **Fourth-obstruction-profile taxonomy note pending** (cycle-038 D3 flag, OQ `l3-index-fourth-obstruction-profile-obstruction-carrying-by-reference`): the §Semantics-overlay sequential-obstruction enumeration (three firm shapes (a)/(b)/(c)) does not yet name the **obstruction-carrying-by-reference** profile that `divfree-projector` instantiates (a gate that authors no obstruction of its own but transmits an inner gate's). A layer-intro-author follow-up dispatch should fold this fourth profile into the §Semantics-overlay taxonomy.
[new]: - **Three firm L3 identity-in-form backfills landed cycle-038 (the elementwise / constructed-operator-gate cohort)**: [`reciprocal`](./reciprocal.md) (the elementwise multiplicative-inverse self-map `x -> (1/x[i])ᵢ`, the `D⁻¹`-forming step of the `assemble_diagonal → reciprocal → elementwise_product` diagonal-preconditioner-apply chain; nonlinear, partial at `x[i]=0`; carries NO obstruction at L3 — embarrassingly parallel), [`elementwise_product`](./elementwise_product.md) (the whole-tensor Hadamard binary product `a ⊙ b`; the Hadamard floor of the elementwise cohort, the operator-action realisation of a diagonal operator, sibling-subsumes `scal`; firm-on-positive-structure, carries NO obstruction at L3), and [`divfree-projector`](./divfree-projector.md) (the divergence-free projector as a whole-tensor field operation; the **third constructed-operator gate** at L3 after `ksp_solve` c020 and `jacobi-smoother` c037). The three are (A) firm identity-in-form backfills of the cycle-036 D2 audit verdict (`book/src/L3/index.md:44`). `divfree-projector`'s distinguishing fact: it is **obstruction-carrying by reference** — its own four-step apply (`WeakDiv → Z_{bdr_eff} → ksp_solve → Grad`) is a fixed straight-line composition authoring NO obstruction, but step 3 invokes the firm-L3 `ksp_solve` inner H1 solve (`palace/linalg/divfree.cpp:175`), whose outer fold is a `sequential-obstruction`. The gate neither introduces a new obstruction nor erases the inner one — the inner CG iteration stays interior to `ksp_solve` per the [`nested-constructed-operator-gate`](../concepts/nested-constructed-operator-gate.md) fidelity rule. This is the **fourth obstruction profile** now enumerated as shape (d) in the §Semantics-overlay taxonomy (resolving the cycle-038 D3 pending flag): `divfree-projector` sits in the **middle of the L3 obstruction-profile spectrum**, between the obstruction-authoring `ksp_solve`/`eigsolve` (which own their loops) and the obstruction-free leaf `jacobi-smoother`/`apply_linop`/`dot`/`scal`. It instantiates the nested-gate transitive chain `eigsolve ⊃ divfree-projector ⊃ ksp_solve` at L3. All three are layer-coherence backfills per **Identity-lowerings still require both L levels**; the substantive rotations live at L1>L0 (`reciprocal-elementwise-product-mutation-rotation`; `divfree-projector-mutation-rotation`, delegating its inner solve to `ksp-solve-mutation-rotation`).
- **`normalize` landed cycle-039 — the c036 (A) cohort closer; consolidated L3 firm-count tally**: [`normalize`](./normalize.md) (the fused `nrm2 + scal` whole-tensor self-map) lands firm cycle-039, the **sixth and final (A) identity-in-form backfill** of the cycle-036 D2 audit verdict (`book/src/L3/index.md:44`), closing the **c036 (A) cohort at 6-of-6**: `assemble-diagonal` (c037) + `jacobi-smoother` (c037) + `reciprocal` (c038) + `elementwise_product` (c038) + `divfree-projector` (c038) + `normalize` (c039) all landed under OQ `l3-cohort-growth-audit-c036-verdict` (now CLOSED for the (A) cohort). **L3 firm-operator count is now 15 firm + 2 `partial-obstruction`** — firm: `krylov-step` c010; `apply_linop` + `axpy` + `axpby` + `axpbypcz` + `dot` + `nrm2` + `scal` c011; `ksp_solve` c020; `assemble-diagonal` + `jacobi-smoother` c037; `reciprocal` + `elementwise_product` + `divfree-projector` c038; `normalize` c039 — partial-obstruction: `chebyshev` c013, `eigsolve` c024. This bullet is the **single authoritative count tally**; the per-cycle running counts in the c024/c037/c038 bullets above are superseded snapshots (kept as narrative of what landed each cycle, not as the live count). The §Semantics-overlay taxonomy now enumerates **four** firm obstruction shapes (a)/(b)/(c)/(d), the fourth being `divfree-projector`'s obstruction-carrying-by-reference profile (OQ `l3-index-fourth-obstruction-profile-obstruction-carrying-by-reference` RESOLVED).
```

## Supporting evidence

- **Operators currently harvested at L3** (post-cycle-039, from the dep-map table + D1's in-flight `normalize`): 15 firm — `krylov-step` (c010), `apply_linop`/`axpy`/`axpby`/`axpbypcz`/`dot`/`nrm2`/`scal` (c011), `ksp_solve` (c020), `assemble-diagonal`/`jacobi-smoother` (c037), `reciprocal`/`elementwise_product`/`divfree-projector` (c038), `normalize` (c039, D1 in-flight); plus 2 `partial-obstruction` — `chebyshev` (c013), `eigsolve` (c024).
- **Fourth-profile exemplar — `divfree-projector`** (`book/src/L3/divfree-projector.md`): the §"Iteration-rotation marker" section (lines 233-265) is the authoritative source for the obstruction-carrying-by-reference framing my taxonomy prose continues — "The gate authors NO iteration obstruction of its own" + carries a `sequential-obstruction` **by reference** through inner `ksp_solve` (step 3, `palace/linalg/divfree.cpp:175`) + "neither introduces ... nor erases" + `nested-constructed-operator-gate` fidelity rule. The four-step apply `WeakDiv → Z_{bdr_eff} → ksp_solve → Grad` and the spectrum positioning (between obstruction-free `jacobi-smoother`/`apply_linop` and obstruction-authoring `ksp_solve`/`eigsolve`) are stated verbatim there (lines 259-265).
- **Contrast exemplars** confirming the spectrum endpoints: `jacobi-smoother` row (`book/src/L3/index.md:33`) — "carries **NO obstruction** at L3, the sharpest contrast"; `apply_linop` row (`:22`) — leaf operator-apply; `ksp_solve` row (`:31`) — "the outer-loop obstruction this operator carries"; `eigsolve` row (`:32`) — opaque-library loop obstruction; `chebyshev` row (`:30`) — `partial-obstruction`, numerical-stability.
- **Citations in my edits are intra-book doc-links** (`./ksp_solve.md`, `../concepts/nested-constructed-operator-gate.md`, etc.), not source citations — these resolve within the book; the one source line referenced (`palace/linalg/divfree.cpp:175`) is carried unchanged from the existing c038 §Working-Notes bullet (already on-disk, already integrated), not a new source claim.

## Open questions / caveats

- **Parallel-apply ordering with D1 (harvester, `normalize`).** D1 appends a dep-map ROW for `normalize` to the table (lines 21-36 region) and creates `book/src/L3/normalize.md`; per the dispatch brief D1 does NOT touch the §Working-Notes count tally (I own it). My Edit 2 [new] text asserts `normalize` is firm and links `[`normalize`](./normalize.md)` as a live link — this is correct ONLY IF D1's `normalize.md` lands in the same integrator batch. If for any reason D1's `normalize` does NOT land this cycle, the `[`normalize`](./normalize.md)` live link in my Edit 2 would be a missing-anchor `linkcheck2` build error and the "15 firm" count would be premature. **Recommended integrator ordering: apply D1's `normalize.md` + dep-map row FIRST, then my §Semantics-overlay + §Working-Notes edits.** If D1 slips, integrator should defang my `./normalize.md` link to plain-text `normalize *(rough-in; no anchor yet)*` and roll the count back to "14 firm + 2 (normalize pending cycle-039)". Flagging per the `rough-in-rows-must-be-plain-text-when-anchor-missing` convention.
- **Superseded per-cycle running counts left in place (intentional).** I did NOT rewrite the count sentences inside the older c024 / c037 bullets ("count now 9 firm + 2", "count now 11 firm + 2") — those are kept as accurate historical snapshots of what was true *at that cycle*, and my new consolidated tally bullet explicitly labels them "superseded snapshots ... not the live count". If the integrator/critic prefers a single live count with zero stale snapshots, those three inline count sentences (c024:55, c037:56, plus the c038 one I already stripped) could be pruned in a follow-up §Working-Notes compaction pass — but that is a larger rewrite than this dispatch's scope and risks churn on already-integrated narrative. Flagging as a candidate compaction, not enacting it.
- **OQ closure.** Two OQs are stated RESOLVED/CLOSED in my edits: `l3-index-fourth-obstruction-profile-obstruction-carrying-by-reference` (resolved by Edit 1 enacting shape (d)) and the (A)-cohort portion of `l3-cohort-growth-audit-c036-verdict` (6-of-6 complete with `normalize`). The integrator-per-report should mark both accordingly in `scaffolding/open-questions.md` when applying this report (alongside D1's, which closes the same cohort OQ from the operator side).
