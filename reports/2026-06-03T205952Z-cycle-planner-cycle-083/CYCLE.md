---
agent: cycle-planner
invoked_at: 2026-06-03T205952Z
scope: cycle-083 dispatch plan
status: pending
integrated_at: 2026-06-03T212210Z
integration_commit: 16e2dc8
integration_notes: "cycle-083 (batch-26 pos 2/3) dispatch plan. 2 dispatches planned, 2 ran, 2 applied clean (D1 sparameter_reduce lowering-verifier promotion + D2 eigenmode.L4 lifter hygiene). Plan executed as written; scaffolding/priorities.md co-owned plan write committed atomically."
---

# Cycle 083 dispatch plan

## Goals selected this cycle

Cycle-083 is **position 2 of 3 in meta-batch-26** (the batch-26 meta-phase fires after c084). It is the cycle BEFORE the meta-phase, so the discipline is **LAND-CLEAN closeable work, not opening large threads**. The batch-26 frontier shape is settled (cycle-082 D1 survey, AFFIRMED-CLOSED): 5-driver→L4 is COMPLETE on both halves (no driver-composition gap), and the (A) forward-frontier is the **output-product-reduce-verb cohort**, fan-out-ranked A1 `sparameter_reduce` > A2 `eigenfreq_qfactor_reduce` (DONE, firm c082) > A3 `gram_reduce` (foundation-gated) > A4 `domain_energy_reduce` (foundation-gated). This cycle takes the **single highest-fan-out cleanly-describable closeable item — a `lowering-verifier` deepen-audit on `sparameter_reduce` (A1)** testing whether the firm-on-positive-structure / syntactic-identity escape that promoted A2 to firm in c082 also applies to A1's laws (the exact in-scope route OQ `output-product-reduce-verb-test-coverage-bounded-promotion-route` names as its trigger) — plus the **directive-permitted mechanical hygiene fix** of the now-stale `eigenmode.L4.md:55` "rough-in" clause (A2 is firm as of c082). Either D1 outcome (promote-to-firm OR honest-ceiling-record) closes the open OQ — a clean land. **Two dispatches, single parallel wave.** Per the NEW USER DIRECTIVE 2026-06-03 (`project_feature_column_promotion_rule`): the feature-column promotion-rule amendment is RESERVED for the batch-26 meta-phase to enact — this plan does NOT change the promotion rule, re-author column promotion prose, or flip any column status; it folds in ONLY the cheap mechanical maturity-hygiene the directive itself names (the eigenmode.L4:55 fix).

## Deliverable-presence verification

Paste-inline evidence per the MANDATORY pre-dispatch four-step check (role-spec §Discipline; skill `verify-dispatch-scope-not-already-discharged`).

### D1 — `lowering-verifier` deepen-audit on `book/src/L4/sparameter_reduce.md` (A1)

1. **File existence** — `ls book/src/L4/sparameter_reduce.md` → present; `stat` → `26871 bytes, mtime 2026-06-03 10:24:36`. EXISTS.
2. **Maturity / already-discharged** — `## Status` line read from disk (`:240`): ``rough-in (test-coverage-bounded)``. `grep -c verified_against` → **2** blocks (the c079 test-coverage citation). D1's deliverable is a law-completeness DEEPEN-audit (raise the verdict EITHER to `firm` via the firm-on-positive-structure escape OR record the honest ceiling) — strictly ABOVE the on-disk maturity (a deepen-audit on a `rough-in (test-coverage-bounded)` verb, not a re-statement). NOT a no-op.
3. **OQ-ledger RESOLVED-grep** — `grep -c 'sparameter.reduce.*RESOLVED\|...'` → 7 matches, BUT all 7 are HISTORICAL resolution markers for the verb's *prior* gates (gate-a, gate-b/`port_projection`-home c077). The D1-target OQ `output-product-reduce-verb-test-coverage-bounded-promotion-route` is **OPEN** (`open-questions.md:1059`): it explicitly asks "can a `lowering-verifier` law-completeness / literature-anchor deepen-audit raise A1 (`sparameter_reduce`) to firm via the documented alternate route ... rather than waiting on the (out-of-write-scope) `MeasureSParameter`-entry-point test?" and names *"a lowering-verifier deepen-audit dispatch on `sparameter_reduce` (A1)"* as its TRIGGER. D1 IS that trigger. NOT stale; NOT closed.
4. **Structural-block check** — NOT structurally blocked. The verb's gate-b (`port_projection` L1 home) is FIRM (`book/src/L1/port_projection.md` Status `:266` = ``firm`` — firm-on-positive-structure, verified on-disk this plan). The only residual is the assembly-level test bound, which is EXACTLY the question D1 adjudicates in-scope (it is not a permanent obstruction: the verb's own Status `:282-289` says the firm-on-positive-structure escape "would apply if the assembly laws were purely syntactic identities" — D1's job is to decide whether they are, or whether the cached-projection cross-pass stitching genuinely blocks it). The `MeasureSParameter`-entry-point assembly test is out-of-write-scope, but D1 does NOT need it — the in-scope route is the law-confidence pass (the route that just firmed A2). NOT on the STOP-PROPOSING negative list (those are L3 backfills: `lu_solve`/`back_solve`/`ls-update-column`/`nleps_*`). Framing CORRECT (audit-first `lowering-verifier`, which is what the OQ trigger names — not a reflexive harvest).

ALL FOUR PASS. Source anchor codemap-confirmed: `mcp__palace-codemap__search_text MeasureSParameter` → `palace/models/postoperator.cpp:1246` (matches the on-disk-cited `:1246-1309`).

### D2 — `lifter` hygiene fix on `book/src/feature/eigenmode.L4.md:55`

1. **File existence** — `ls book/src/feature/eigenmode.L4.md` → present; `stat` → `12917 bytes`. EXISTS.
2. **Maturity / already-discharged** — line 55 read from disk: `"... the only thing keeping this column at \`seed\` ... is the readout stage's reduction into the [\`eigenfrequency-qfactor\`](./eigenfrequency-qfactor.L4.md) output-product column, which is itself \`seed\` (its \`eigenfreq_qfactor_reduce\` verb is \`rough-in\`)."` — the clause calls `eigenfreq_qfactor_reduce` ``rough-in``. On-disk reality: the verb was PROMOTED to ``firm`` in c082 (`open-questions.md:1056` CLOSED-RESOLVED-BY-AUDIT). The clause is STALE → the hygiene fix is genuinely open (NOT already discharged; cycle-082 D2 touched the verb file + the `eigenfrequency-qfactor.{L4,L1}` column, NOT the `eigenmode.L4` driver column — confirmed disjoint per the c082 resolution marker "the `.L0` column carried no verb-gate prose; ... eigenfrequency-qfactor.{L4,L1} feature-column verb-gate prose refreshed" — the EIGENMODE column was not in c082's edit set).
3. **OQ-ledger RESOLVED-grep** — the fix is named directly in the USER DIRECTIVE 2026-06-03 (`open-questions.md:1068`, enactment item (3): "fix the now-stale `eigenmode.L4:55` clause (calls `eigenfreq_qfactor_reduce` rough-in; firm as of c082)") as a permitted mechanical hygiene touch (the directive carves this out as cheap, NOT the reserved promotion-rule change). OPEN.
4. **Structural-block check** — NOT blocked. This is a maturity-token sync to current on-disk reality (`rough-in`→`firm` for the cross-linked verb), the standard `lifter` staleness-clear shape (the c081 D-precedent). It does NOT touch the column's `seed` status or its promotion-rule prose (those are the meta-phase's reserved enactment). NOT on any negative list. Framing CORRECT (`lifter` prose staleness-clear).

ALL FOUR PASS.

### A4 `domain_energy_reduce` (secondary candidate — ASSESSED, NOT dispatched)

Assessed per the directive's secondary-candidate note. `book/src/L4/domain_energy_reduce.md` Status `:268` = ``rough-in``; its gate-1 is the folded `matrix-weighted-norm` (Status `:110` = ``rough-in (test-coverage-bounded)``, **√-entry-point gate (a) STILL OPEN** per `:114-118`). That √-entry-point firm is the trigger-gated `matrix-weighted-norm-full-firm-cascade-wave` structural item (NO-GO batch-25; a ~30-file re-anchor sweep best run as its own dedicated cycle, NOT bundled into the cycle before a meta-phase). So **A4 is NOT cleanly promotable this cycle** — an A4 deepen-audit would only re-record the same foundation-gated ceiling A1 sits closer to discharging. Lower fan-out than A1 (A1's gate-b is already firm; A4 is double-gated behind a NO-GO cascade). DEFERRED — correctly NOT dispatched.

## Dispatches

1. **agent**: `lowering-verifier`
   **scope**: Law-completeness / firm-on-positive-structure DEEPEN-audit of `book/src/L4/sparameter_reduce.md` (A1, currently `rough-in (test-coverage-bounded)`, 2 `verified_against:` blocks). Adjudicate the OQ `output-product-reduce-verb-test-coverage-bounded-promotion-route`'s open sub-question: do `sparameter_reduce`'s §Algebraic-laws qualify for the firm-on-positive-structure / syntactic-identity escape (the exact route that promoted the sibling A2 `eigenfreq_qfactor_reduce` rough-in→firm in c082), OR does the assembly fold's stitching genuinely block it? The verb's gate-b is DISCHARGED — `port_projection` (`book/src/L1/port_projection.md`, Status `firm`, firm-on-positive-structure) is the firm L1 home of the `sᵢ·E` dual-pairing projection it folds. The decisive contrast the audit must resolve: A2's laws were syntactic identities / closed-form compositions over firm primitives with no axiom requiring an unverified mathematical property (so the escape applied); A1's own Status (`:282-289`) already flags that "the self-term + scaling stitching crosses the cached projection from the per-port measure pass, so the assembly is treated as integration-level" — the audit must decide whether that cross-pass stitching is a *syntactic read-off of the positive `MeasureSParameter` body* (`postoperator.cpp:1246-1309`, codemap-confirmed `:1246`; the two structurally-identical port-kind loops — lumped `√(R_src/R_dst)` self-term `−1` scaling, wave `exp(ikₙd)` scaling) OR carries genuine untested numerical/semantic content (the A2-vs-`matrix-weighted-norm` distinction). **IF the laws qualify** (syntactic identities over the firm `port_projection` + positive `MeasureSParameter` source, no unverified-property axiom): PROMOTE `sparameter_reduce` `rough-in (test-coverage-bounded)`→`firm`, land a `verified_against:` block, flip the L4/index dep-map status cell + firm/rough-in counts. **IF they carry genuine untested semantic content** (the cross-pass assembly stitching is integration-level, the escape is RULED OUT exactly as the c080 `matrix-weighted-norm` √-entry-point audit ruled it out): RECORD that verdict cleanly in the verb's Status + a `verified_against:`/findings note (the honest seed-surface-firming-ceiling outcome), leave the verb `rough-in (test-coverage-bounded)`, and update OQ resolution to CLOSED-RESOLVED-BY-AUDIT-CEILING. **DO NOT** force a promotion. **DO NOT** touch the `sparameters.{L4,L1,L0}` feature column status / promotion-rule prose (the column-promotion-rule amendment is the batch-26 meta-phase's reserved enactment per the USER DIRECTIVE 2026-06-03; this dispatch touches ONLY the L4 verb chapter + its L4/index dep-map row/counts if promotion lands). If promotion lands, the verb's down-link consumers' MATURITY references (not statuses) may be re-anchored only within the verb's own dep-map; broader column re-anchors are deferred to the meta-phase column re-evaluation (batch-27 lead). Codemap-confirm `postoperator.cpp:1246-1309` on-disk before citing (it is a known token-dense postprocess region — the cited range is from the verb's own evidence block; verify the END line `:1309` on-disk per the brace-boundary drift caution).
   **deps**: none

2. **agent**: `lifter`
   **scope**: Mechanical staleness-clear of the now-stale maturity clause at `book/src/feature/eigenmode.L4.md:55`. The clause reads "...its `eigenfreq_qfactor_reduce` verb is `rough-in`" — but that verb was PROMOTED to `firm` in c082 (`book/src/L4/eigenfreq_qfactor_reduce.md`, CLOSED-RESOLVED-BY-AUDIT). Re-token the stale `rough-in` reference to current on-disk maturity (the verb is now `firm`), and reconcile the surrounding sentence so the stated reason the eigenmode column sits at `seed` is accurate to current reality. **STRICT SCOPE — directive-bounded:** this is the cheap mechanical maturity-hygiene the USER DIRECTIVE 2026-06-03 explicitly carves out (enactment item (3)); it is NOT the reserved promotion-rule amendment. **DO NOT** change the eigenmode column's own `status: seed` token, **DO NOT** re-author the column's promotion-rule prose ("promote past `seed` only once ALL constituents firm"), and **DO NOT** flip any column status — the all-column re-evaluation under the new promotion rule is reserved for the batch-26 meta-phase + the batch-27 lead. Touch ONLY the line-55 maturity-of-the-cross-linked-verb clause to reflect that `eigenfreq_qfactor_reduce` is now firm. Live-link the `eigenfreq_qfactor_reduce` reference if not already linked (skill `upgrade-plain-text-ref-to-live-link-when-target-on-disk`; the `eigenfrequency-qfactor.L4.md` link on this line is already live).
   **deps**: none

## Overlap analysis

- **D1 ↔ D2**: NON-OVERLAPPING. D1 modifies `book/src/L4/sparameter_reduce.md` (+ its own `book/src/L4/index.md` dep-map row/counts ONLY if it promotes). D2 modifies `book/src/feature/eigenmode.L4.md` (a single line). Disjoint files; disjoint operator/verb names (D1 = `sparameter_reduce`; D2 = the eigenmode driver column's reference to `eigenfreq_qfactor_reduce`, a DIFFERENT verb that D1 does not touch). No shared operator-name forward-reference. No shared consolidated index tally is written by both: D1 touches the L4/index dep-map only conditionally-on-promotion; D2 touches no index at all. PARALLEL-SAFE.

## Sequencing schedule

- **Wave 1 (parallel)**: D1, D2.

Single wave — both dispatches are file-disjoint and have no forward-reference dependency on each other's not-yet-landed output. Per the conflict-tolerance philosophy, when in doubt mark PARALLEL; here there is no doubt (fully disjoint files).

Pipeline reminder (role-spec): plan → 2 dispatches (1 wave) → 2 critics → repairers as needed → `integrator-per-report` ×N (serial) → ONE `integrator-finalize` (rebuild book + commit + push + housekeeping). The book is NOT rebuilt between waves; finalize runs once.

## Open questions / caveats

- **D1's likely outcome is the honest ceiling, not a promotion** — and that is fine. The verb's own on-disk Status (`:282-289`) already reasons that the assembly fold "crosses the cached projection from the per-port measure pass, so the assembly is treated as integration-level until the assembly test or a confidence-raising audit lands," and the batch-25 meta-phase recorded the `seed-surface-firming-ceiling-needs-out-of-scope-assembly-tests` friction (recurrence-3). So the strong prior is that D1 finds A1's assembly laws carry genuine integration-level content (unlike A2's pure syntactic identities) and RECORDS the ceiling. The dispatch is still worth running because (a) it is the in-scope route the OQ explicitly names as its trigger, (b) it CLOSES the open OQ either way (promote OR ceiling-record), and (c) a clean ceiling-record is itself the batch-26 spine finding the meta-phase will aggregate. The dispatch scope is written to accept BOTH outcomes without forcing the promotion — flagging here so the critic/integrator do not read a ceiling-record as a failed dispatch.

- **The feature-column promotion-rule deadlock is NOT addressed this cycle by design.** The USER DIRECTIVE 2026-06-03 (`project_feature_column_promotion_rule`) diagnoses the mutual-blocking seed deadlock and prescribes the rule change, but explicitly RESERVES enactment (the CLAUDE.md §Extraction-goal + `layer-intro-author` role-spec amendment → session restart + the all-13-column re-evaluation) for the batch-26 meta-phase, with the all-column re-evaluation queued as the batch-27 lead. This plan touches ONLY the directive's carved-out cheap mechanical hygiene (the eigenmode.L4:55 maturity-token fix). The meta-phase (after c084) is the correct owner of the rule change and the column re-evaluation. No caveat for the human — this is the directive's intended sequencing; flagging for the next planner / meta-phase so the queued batch-27 re-evaluation is not lost.

- **Light cycle, by design.** The forward frontier is genuinely thin: 5-driver→L4 is complete (no driver-composition gap, c082 AFFIRMED-CLOSED); the A-cohort lead A1 is the one deepen-audit-eligible verb; A3/A4 are foundation-gated behind the trigger-gated `matrix-weighted-norm` √-entry-point cascade (NO-GO batch-25, not to be bundled into the pre-meta cycle). Manufacturing a rectangular pull-up is forbidden by the VOCABULARY-SHIFT REDIRECT. Two land-clean closeable dispatches is the honest plan for the cycle before the meta-phase. No fresh plan-candidate appended to `priorities.md` this cycle — the A-cohort backlog (A1 deepen-audit = D1; A3/A4 trigger-gated; the `matrix-weighted-norm` cascade wave) is already correctly ranked, and the queued batch-27 column re-evaluation is captured in the USER DIRECTIVE 2026-06-03 OQ block for the meta-phase.

- **No escalating friction pattern requires new work this cycle.** Scanned the friction-ledger for `escalating` / recurrence-≥4 patterns: the two live ones (`producer-citation-drift-verify-not-self-invoked`, `dispatch-phase-write-leak`) are both `addressed` with working detect+repair nets (citecheck wired; revert-skill + universal prompt-guard); the write-leak watch (combinator-miner 2-of-5; re-escalate on a 3rd combinator-miner leak OR two-in-a-batch) is NOT triggered (this cycle dispatches no combinator-miner). Both D1 (`lowering-verifier`) and D2 (`lifter`) should run `citecheck --anchor` at emit time per the wired-in producer self-verify discipline; D1 in particular touches a token-dense postprocess region (`postoperator.cpp:1246-1309`) — confirm the END line on-disk (brace-boundary +1 drift caution).
