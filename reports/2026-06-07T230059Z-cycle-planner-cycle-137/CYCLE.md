---
agent: cycle-planner
invoked_at: 2026-06-07T230059Z
scope: cycle-137 dispatch plan
status: pending
---

# Cycle 137 dispatch plan

Batch-44 position **2/3** (MIDDLE primary cycle of meta-batch-44 — cycles 136/137/138). The batch-44 meta-phase fires AFTER cycle-138's finalize, aggregating 136/137/138 as a separate dispatch/commit. The cycle counter does NOT reset.

## Goals selected this cycle

Batch-44 LEAD = **the SYNTHESIS section** (USER DIRECTIVE 2026-06-07; `project_synthesis_section_directive`). c136 stood up the `# Synthesis` Part with **5 of 6 chapters bodied**; the **`drivers` library body is the sole deferred Synthesis deliverable** (the deliberate topological-last chapter — it composes everything below it). This cycle's LEAD fills the `drivers` body — lifting the entry-point surfaces (5 sim drivers + lifecycle ROOT + 6 output products) from the already-firm Feature L4 spine, rendered as synthesized library code that **composes** the now-rendered `types`/`iteration`/`data-algebra`/`coordination` calculus libraries. In parallel, a middle-cycle-appropriate **lowering-verifier** audit checks the c136-rendered library defs against their authoritative L4 chapter bodies (the directive explicitly licenses this; there is a specific flagged trigger — the `mk_matrix_free_operator` impl↔L2-chain correspondence). NO dedicated maintenance-floor cross-cutter this cycle: per the batch-43-enacted item-0 cadence, the full-hygiene sweep runs **once per BATCH** and already fired at c136 (D5); the per-cycle floor is the `integrator-finalize` step-5b two-invariant tripwire (one command, no dispatch).

## Deliverable-presence verification

Per the MANDATORY pre-dispatch deliverable-presence check (paste-inline-evidence). Run from repo root.

**D1 — `book/src/synthesis/drivers.md` (fill the `drivers` library body):**
1. File existence: `ls book/src/synthesis/drivers.md` → **present** (`-rw-rw-r-- 2662 Jun 7 15:31 drivers.md`).
2. Maturity / already-discharged: `grep -m1 '^status:' book/src/synthesis/drivers.md` → **`status: stub`** (the c136-landed intro shell; body deliberately deferred). The proposed deliverable (render the full topologically-ordered def bodies + flip off `stub`) is STRICTLY ABOVE the on-disk maturity → NOT a no-op. **OPEN by construction** (the deferred carry-forward named in the c136 finalize "carry to c137/c138" + OQ `synthesis-drivers-library-body-deferred`).
3. OQ-ledger RESOLVED-grep: `grep -i 'drivers.library.body' scaffolding/open-questions.md` → `## synthesis-drivers-library-body-deferred` (OPEN, not RESOLVED/CLOSED — it is the carry-forward note, not a closure).
4. Structural-block check: none. The body composes already-rendered firm vocabulary + lifts from firm Feature L4 columns; no methodology gate blocks it (the redirect's no-forced-rectangular-pull-up is SATISFIED — Synthesis RENDERS the existing firm surface as library code, it pulls up no new vocabulary). NOT on the STOP-PROPOSING negative list. Framing = composition (route `layer-intro-author` per the directive's `types`/`drivers` ownership), NOT new operator algebra.

**D2 — lowering-verifier audit of c136-rendered library defs vs L4 (target chapters `synthesis/iteration.md` + `synthesis/data-algebra.md`):**
1. File existence: `ls book/src/synthesis/{iteration,data-algebra}.md` → both **present** (iteration 23682 b, data-algebra 31439 b; bodied at c136). L4 audit-against chapters present: `ls book/src/L4/{iterate-while,krylov-step,chebyshev,linear_combination,fe_assemble,gram_reduce}.md` → all **present**.
2. Maturity: the synthesis chapters are filled `navigational-container` implementation-VIEW (def bodies rendered Wave-2, normalized at c136 finalize to no-`status:` convention). NO prior lowering-verifier audit of the rendered-def correspondence exists (the rendered defs landed only c136) → audit deliverable is OPEN. **OPEN by construction** (no prior-cycle audit history on synthesis/ chapters; the rendered defs are one cycle old).
3. OQ-ledger: `grep -i 'synthesis-data-algebra-mk-matrix-free-apply-chain-inline-render' scaffolding/open-questions.md` → present + carries an explicit *Trigger: lowering-verifier audit of the impl↔L2-chain correspondence* (OPEN — it is the named audit trigger, not a closure).
4. Structural-block check: none. The directive (`lowering-verifier` RE-SCOPE bullet + CLAUDE.md §SYNTHESIS "a rendered def's correspondence to its L4 chapter body is reviewable (`lowering-verifier` may audit it)") explicitly licenses this audit-class dispatch.

## Dispatches

**D1 — (`layer-intro-author`, `synthesis-drivers-library-body`, deps: none) — THE LEAD.**
- **scope:** Fill the `drivers` library body in `book/src/synthesis/drivers.md` (the deferred 6th Synthesis chapter; the topological-LAST library — it composes everything below it). Render, in topological order, the synthesized implementation defs of the entry-point surfaces, each **composing the already-rendered calculus-library defs by name** and lifting the composition from the firm Feature L4 columns:
  - **per-driver config records FIRST** (each clustered immediately before its driver def, bundled with the config's *utility* API — constructors/accessors/predicates; consumer methods stay in the driver def) per the type-placement rule (`synthesis/index.md#type-placement--cluster-a-type-with-its-api-group`);
  - the **5 sim-driver composition defs** — `electrostatic` / `magnetostatic` / `driven` / `transient` / `eigenmode` (lift from `feature/{electrostatic,magnetostatic,driven,transient,eigenmode}.L4.md`; compose `fe_assemble`/`solve_family`/`ksp_solve`/`gram_reduce`/`eigsolve`/`fold_solve`/`frequency_sweep` from the rendered `data-algebra`/`coordination` libraries);
  - the **6 output-product composition points** — capacitance / inductance / S-parameters / eigenfrequency+Q / energy-fields / waveguide-mode (the reduce VERBS `domain_energy_reduce`/`eigenfreq_qfactor_reduce`/`sparameter_reduce`/`waveguide_mode_reduce`/`gram_reduce` are ALREADY rendered in `data-algebra.md` — the drivers library COMPOSES them by name; lift the wiring from `feature/{capacitance,inductance,sparameters,eigenfrequency-qfactor,energy-fields,waveguide-mode}.L4.md`);
  - the **lifecycle ROOT** LAST (the meta-feature spine root — `config → mesh → (per-driver) Solve → estimate-mark-refine → product`, dispatching on `IoData.problem.type`; lift from `feature/lifecycle.L4.md`, compose `fold_solve` + the 5 driver defs).
- **rendering conventions (from `synthesis/index.md#rendering-conventions`):** L4 pseudo-language (Haskell `::` sigs + TS `{ field: type }` records), **all `$`-sigil pseudocode INSIDE a ` ```text ` fence** (the KaTeX `$`-sigil-fence rule — copy a landed `$`-sigil sibling such as `../L4/gram_reduce.md`); topological def order; Haskell `where` for private helpers; code-doc per def (`# Arguments`/`# Returns`); deep-linked-unchanged lower artifacts rendered INLINE; `#extern NAME` after the type sig for any opaque-kernel boundary (`time_step_op` MFEM ODESolver in `transient`; the AMR estimate-mark-refine leaf if it surfaces); **LINK** to the Feature columns + the composed calculus-library defs, do NOT re-cite L0 (the L4 chapter + concept pages own the citations); **reference-class navigational edges ONLY** — no `depends-on` blocking edge (this is the implementation VIEW, not a rank/liveness constituent).
- **status-token convention:** flip `drivers` off `status: stub` to the **SAME filled-VIEW convention the c136 finalize normalized the 3 calculus libraries to** — a filled implementation-VIEW library chapter carries **NO `status:` field** (kind-only `navigational-container`, no `rank:`, no resolution claim). (Resolves the `drivers` arm of OQ `synthesis-coordination-chapter-status-seed-token-reconciliation-c136`.)
- **per-driver config-record homes:** define each per-driver config record in itself (single-consumer → in-chapter `## Record definition`/type+utility block, back-linked to its `concepts/config-record` / `IoData` schema home; if a record crosses the ≥2-consumer bar flag `record-<name>-needs-definition-home` rather than duplicating). Note the `EigState` precedent (single-consumer in-chapter block, back-linked to `EigResult`; per OQ `record-EigState-schema-home-is-EigResult`) — if the `eigenmode` driver column names `EigState` directly it crosses the ≥2-consumer bar and should migrate to a `concepts/EigState.md` page (do NOT duplicate the in-chapter block).
- **rationale:** the batch-44 LEAD; the sole deferred Synthesis deliverable. Completes the `# Synthesis` Part (6/6 chapters bodied). fan-out: HIGH (the synthesized-library deliverable; the bridge toward the downstream burn build). Plan-tag `synthesis-section`. Routed to `layer-intro-author` per the directive's `types`/`drivers`-library ownership (composition, not new operator algebra). One owner for the whole file (it is ONE topologically-ordered file — splitting across producers would be same-file overlap).

**D2 — (`lowering-verifier`, `synthesis-rendered-def-vs-l4-correspondence-audit`, deps: none) — MIDDLE-CYCLE AUDIT.**
- **scope:** Audit the c136-rendered Synthesis library defs against their authoritative L4 chapter bodies — verify the rendered synthesized code form faithfully corresponds to the L4 chapter's def (the directive: "a rendered def's correspondence to its L4 chapter body is reviewable"). Cover **two anchored targets**:
  - **`synthesis/iteration.md`** — `krylov_step` (Form A + Form B / CG worked def) and `iterate_while` / `iterate_while_with_prev` vs `book/src/L4/{krylov-step,iterate-while}.md` (+ `chebyshev` setup/apply vs `L4/chebyshev.md` if budget permits): confirm signature, body composition, and named shape contracts match; flag any divergence the same way as a lowering RHS that does not appear in evidence.
  - **`synthesis/data-algebra.md`** — the **flagged trigger**: the `mk_matrix_free_operator` `apply_chain` `where`-helper (the inline kernel-IMPL render of the firm L2 five-stage contraction chain `Gᵀ ∘ B_𝒟ᵀ ∘ D ∘ B_𝒟 ∘ G`) vs its authoritative home `book/src/L2/matrix-free-operator-apply.md` — audit the impl↔L2-chain correspondence (chain order + the `_T` transpose-pairing glue spelling); AND confirm the DIRECTIVE-3 dual surface is intact (`#extern assemble_term` kernel-API opaque leaf in `fe_assemble` vs the inline `mk_matrix_free_operator` kernel-impl). (Per OQ `synthesis-data-algebra-mk-matrix-free-apply-chain-inline-render-c136`.)
  - **edge-typing check:** confirm every audited `synthesis/` outbound edge is `reference`-class (a Synthesis chapter carrying a `depends-on` onto an L4 op would mis-type the rendering relationship as a build dependency — flag if mis-typed). Confirm no semantic RE-STATEMENT (the implementation-VIEW must USE+LINK, not duplicate L4/`semantics/` law-claims — SEMANTIC-CONSOLIDATION discipline).
- **output discipline:** audit-class. Writes the audit verdict to `reports/<id>/CYCLE.md` + OQ intake; MAY emit a `verified_against:` note onto the audited chapters (`synthesis/iteration.md` / `synthesis/data-algebra.md`) — these are DISTINCT files from D1's `synthesis/drivers.md` (non-overlapping). Does NOT mutate `drivers.md`.
- **rationale:** the directive-licensed rendered-def-vs-L4 audit; a good middle-cycle fit (consolidation/verification of the c136 LEAD output while the c138 cycle can take any follow-on). Resolves/advances the flagged `mk_matrix_free_operator` impl↔L2-chain trigger OQ. fan-out: hygiene/verification (de-risks the Synthesis rendering before the batch-44 meta). Plan-tag `synthesis-section`.

## Overlap analysis

- **D1 ↔ D2:** D1 authors `book/src/synthesis/drivers.md` (creates the body of ONE file). D2 audits `synthesis/iteration.md` + `synthesis/data-algebra.md` (DIFFERENT files) and at most appends a `verified_against:` note onto THOSE files — it does NOT touch `drivers.md`. **No shared file region; no shared operator-name authoring** (D1 *composes* `data-algebra`/`coordination` defs by NAME via reference links, it does not re-author them; D2 *reads* those defs to audit). **NON-OVERLAPPING → PARALLEL.**
  - Minor note: D1's drivers defs forward-reference (by name, as reference-class links) defs that D2 audits in `iteration`/`data-algebra`. This is a navigational reference to an already-landed (c136) sibling chapter — both target files already exist on disk, so no forward-reference-slug coordination is needed (D1 links to canonical landed slugs; D2 does not rename them). No cross-report forward-reference-slug divergence risk.
- **No shared consolidated-tally / shared-index concern:** this cycle does NOT land ≥2 chapters into a common layer index with a consolidated running count. D1 lands one Synthesis library body (and may flip the `synthesis/index.md` `drivers`-row Status cell `stub`→`navigational (rendered)` — a single-cell edit owned solely by D1, the layer-intro-author who owns the index narrative). D2 emits audit notes only. No dual-registration partition needed (single producer per artifact region).

## Sequencing schedule

**Wave 1 (parallel — both dispatches, no inter-dependency):**
- D1 (`layer-intro-author`, `synthesis-drivers-library-body`)
- D2 (`lowering-verifier`, `synthesis-rendered-def-vs-l4-correspondence-audit`)

Single wave. Both are deps: none. D2 audits c136-landed (already-on-disk) chapters, so it does not wait on D1; D1 composes c136-landed (already-on-disk) calculus defs, so it does not wait on D2. The per-report integrators apply serially (D1's drivers body, then D2's audit notes onto distinct files); `integrator-finalize` runs ONCE at cycle end (rebuild book + step-5b two-invariant tripwire + step-5c KaTeX `$`-sigil assertion + commit/push). No dedicated maintenance-floor cross-cutter (per item-0 per-batch cadence; the batch-44 sweep already fired at c136 D5; the per-cycle floor is the step-5b tripwire, no dispatch).

## Open questions / caveats

- **Single-file LEAD sizing.** The `drivers` body is ONE file rendering ~12 composition defs (5 drivers + 6 output-product composition points + lifecycle ROOT) + their clustered config records/utility APIs. This is a substantial single-owner authoring task, but it is **composition of already-rendered firm vocabulary** (the calculus libraries + output-product reduce verbs landed c136; the Feature L4 columns are the firm lift source) — the layer-intro-author's wheelhouse, and the file is intrinsically a single topologically-ordered unit (splitting across producers would be same-file overlap). If the dispatched author finds the scope exceeds a single context budget, the natural sub-split is **(a) the 5 driver defs + their config records** and **(b) the output-product composition + lifecycle ROOT** as two SEQUENTIAL same-file passes (NOT parallel — same file) — but the default is one dispatch. Flag to the orchestrator if the single-dispatch render shows token-budget pressure (escalation trigger: a specialized agent's input/output exceeds reasonable budget on one region → cycle-planner subdivides next cycle).
- **`#extern` leaves in the drivers body.** The `transient` driver composes the MFEM `ODESolver` time-step (`time_step_op` kernel-API, rendered `#extern` in `coordination.md`); the `drivers` rendering should compose it by reference, not re-render the `#extern`. If AMR's estimate-mark-refine surfaces in the lifecycle ROOT composition, render it as a reference to its feature/AMR home (AMR is in active scope per DIRECTIVE-2 but its synthesized impl is not a c137 deliverable) — do not fabricate a def. Caveat surfaced for the D1 author; not blocking.
- **Status-token convention for `drivers`.** The c136 finalize normalized the 3 calculus-library chapters to the no-`status:`-field filled-VIEW convention (resolving the inconsistency). D1 should apply the SAME convention to `drivers` (no `status:` field once bodied). This closes the `drivers` arm of OQ `synthesis-coordination-chapter-status-seed-token-reconciliation-c136`; the meta-phase (after c138) owns the final convention ratification across all 6 chapters.
- **Maintenance cadence note (for the batch-44 meta, fires after c138).** The c136 D5 per-batch maintenance sweep ran BEFORE the Synthesis Part was on disk and could not audit `synthesis/` (it promoted OQ `synthesis-edges-next-batch-maintenance-floor-audit` deferring the synthesis/ edge-correctness + `$`-sigil-fence + no-semantic-restatement audit to the next batch sweep). With the `drivers` body landing this cycle, the **full `# Synthesis` Part is complete after c137** — the batch-44 meta-phase (or a c138 opportunistic touch) should fold the deferred synthesis/-audit into its per-batch sweep rather than waiting for batch-45. D2's audit this cycle covers the *rendered-def-vs-L4 correspondence* + edge-typing on `iteration`/`data-algebra`, partially pre-empting that sweep for those two chapters; the `drivers` + `types` + `index` edge-audit remains for the batch sweep. Surfaced for the meta-phase; not a c137 dispatch.
