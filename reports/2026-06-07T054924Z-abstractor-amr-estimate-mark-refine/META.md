---
verifies: ../CYCLE.md
critiqued_at: 2026-06-07T061500Z
critic_version: 1
checks:
  citation-validity: pass
  surface-or-evidence: warning
  rotation-quality: pass
  variant-axis-coverage: pass
  cross-reference-integrity: warning
  edge-label-fidelity: pass
  plan-kind-consistency: pass
  skill-uptake-survey: pass
repaired_at: 2026-06-07T063000Z
repairer_version: 1
repairs:
  citation-validity: not-needed
  surface-or-evidence: repaired
  rotation-quality: not-needed
  variant-axis-coverage: not-needed
  cross-reference-integrity: repaired
  edge-label-fidelity: not-needed
  plan-kind-consistency: not-needed
  skill-uptake-survey: not-needed
overall_status: ready
follow_up_agent: null
---

# META: verification of "L1>L0 theme sketch — amr-estimate-mark-refine"

## Critique

### Checks run

**citation-validity — pass.** Every load-bearing pinpoint was independently re-verified against on-disk Palace source via `palace-codemap` `search_text` (the authoritative line-map; I did NOT rely on `read_range`, which carries the documented +1 brace-boundary drift — and indeed a raw `read_range` of `dorfler.cpp` would have made the report's `:20`/`:28`/`:31`/`:36` look off-by-one; `search_text` settles them as EXACT). Confirmed exactly: `SolveEstimateMarkRefine` opens `basesolver.cpp:153`, initial solve `:174`, `err = Norml2` `:175`, `while` guard `:190`, `ComputeDorflerThreshold` call `:223`, `MarkedElements` call `:225`, `GeneralRefinement` `:239`, `RebalanceMesh` `:249`, re-solve `:266`, function END `}` `:276`; `MarkedElements` body `:103-115` with the `ind.Append(i)` loop; `dorfler.cpp` `std::sort` `:20`, `std::partial_sum` `:28`, un-square `x=std::sqrt(x)` `:31`, `std::lower_bound` pivot `:36`; `errorestimator.cpp` `ComputeErrorEstimates` `:184`, `projector.Mult(F,G)` `:193`, prolongation `:203-204`, `Vector estimates(mesh.GetNE())` `:209`, `CeedOperatorApplyAdd` `:252-253` (+complex `:259`); `GradFluxErrorEstimator` ctor `:273`, qfunction selectors `:348/:352`, Sqrt energy-correction `:386`; `CurlFluxErrorEstimator` ctor `:391`, 2D-scalar-curl branch `:450`, qfunction selectors `:470/:474`, Sqrt `:508`; composite `grad_estimates += curl_estimates` `:536` (TimeDependent) and `:566` (BoundaryMode); `main.cpp:304` `solver->SolveEstimateMarkRefine(mesh)`. The single-rank reading of `dorfler.cpp` (cross-rank bisection `Mpi::Global*` degenerating, min==max threshold on one rank) is a correct DIRECTIVE-1 application. Only loose-but-consistent items: the frontmatter input `errorestimator.cpp:511-560` and the body region labels `:511-538`/`:540-568` are coarse class-region brackets rather than tight pinpoints, but each correctly encloses its cited `grad_estimates += curl_estimates` line — acceptable for region citations. No `verified_against:` YAML block in frontmatter (the "Verified-against" section is plain prose bullets), so the YAML round-trip sub-check does not apply.

**surface-or-evidence — warning.** This is a refinement-shaped lowering theme that modifies surface (new L1>L0 chapter + dep-map rows) AND carries L0 evidence for the rotation — the core shape is sound (not a pure rotation_claim). The warning is the **record-definition sub-check**: the `amr_step` signature names three opaque record/closure types with no definition home anywhere in the book — `Estimator`, `RefineConfig` (`grep` finds them nowhere in `book/src/`), and the result/marking type `IndexSet[E]` (used in `dorfler_mark`'s signature; `IndexSet` appears only in unrelated concept pages, never defined as the marking type). `AmrCarry` is acceptable — the inline comment `{ mesh, indicators, ntdof, err }` plus the cross-ref to `lifecycle.L4 §3` (where the carry shape IS presented) gives it a definition home. `Estimator` is partially excused (OQ `flux-projector-constructed-operator-gate-vs-absorbed` explicitly routes its construction-time-closure status to the harvester), and `RefineConfig` is the thin `refinement.*` IoData surface — but neither is given fields/types/meaning here, and `IndexSet[E]` is undefined. Flag for the producer/harvester to either add a `## Record definition` note (or route them via Open questions). Softened to warning (not fail) because the theme is rough-in/speculative and `Estimator` is already OQ-flagged.

**rotation-quality — pass.** The asserted rotation is a genuine vocabulary translation, NOT a rename. The L0 form is an imperative loop body (libCEED-backed flux-projection solve writing mutable grid-function buffers → in-place sort/partial-sum/threshold-bisection over a copied estimate vector → in-place `ParMesh` mutation); the L1 form is a pure three-verb pipeline `(mesh', indicators') = refine(mesh, mark(estimate(...)))` over a referentially-transparent carry. Distinct vocabulary + distinct semantic organization (mutation→value-threading; named verbs replacing inlined imperative stages) — not identity-in-named-terms. The sharp three-way estimate/mark/refine boundary is real and correctly drawn from the loop body. Crucially, the producer's **judgment to NOT author a duplicate loop `roadmap_goal`** is sound and well-evidenced: I confirmed `fold_solve.md:20` (the `schedule-source = state-generated` variant_axes line) explicitly names "the AMR Solve→Estimate→Mark→Refine loop (basesolver.cpp:190, error-indicator-terminated)" as a witness; `lifecycle.L4.md:52` is stage-3 "Adaptive estimate-mark-refine outer fold" composing the firm `fold_solve`; `lifecycle.L0.md:39-42` is the L0 loop composition; `spine-root.md:21` names the lifecycle's directly-owned firm estimate-mark-refine fold. Re-homing the loop WOULD have been the degenerate-identity smell — correctly avoided.

**variant-axis-coverage — pass.** The orthogonal flux-channel axis (Grad-only `ε`-flux / Curl-only `μ⁻¹`-flux / Grad+Curl composite) is explicitly identified and covered: the report collapses the four concrete estimators (`Grad`/`Curl`/`TimeDependent`/`BoundaryMode`) onto ONE `flux_recovery_estimate` verb with a flux-channel variant axis, observing that the composites `sum` Grad+Curl (`grad_estimates += curl_estimates`, verified `:536`/`:566`) as an `axpy(1,·)`-shape over indicators rather than introducing new verbs. The 2D-scalar-curl sub-branch (`:450`, verified) and the complex/real arms (`ComputeErrorEstimates` `is_same<ComplexVector>` split) are both noted. The collapse-vs-separate-verbs question is explicitly SCOPED OUT to the harvester via OQ `flux-recovery-estimate-flux-channel-axis-vs-separate-verbs` — a clean scope-out, not a hidden branch.

**cross-reference-integrity — warning.** Two distinct issues. (1) **Live links to non-existent files (linkcheck2 hazard):** the new chapter prose uses live markdown links `[flux_recovery_estimate](../L1/flux_recovery_estimate.md)` and `[dorfler_mark](../L1/dorfler_mark.md)` (CYCLE.md:105-106, :131, :138) to files that do NOT yet exist (`L1/flux_recovery_estimate.md` / `L1/dorfler_mark.md` are absent on disk, as expected for rough-in speculative ops). At build, these are hard `linkcheck2` errors. The producer was careful to make the dep-map ROWS plain-text (no anchor, per the rough-in-rows convention) — but the chapter BODY links are live. This needs the integrator to either materialize stubs for the two verbs (the stub policy permits this: ≥2 converging references exist) or de-link the prose to plain text. (2) **Imprecise SUMMARY.md insertion instruction:** the SUMMARY proposed-change says to insert "under the L1 > L0 Part, in the obstruction/construction grouping alpha-position (after `amr` sorts before `apply`)" — but the actual SUMMARY L1>L0 Part has NO flat list; it has three KIND sub-groups (Mutation-rotation / Construction-rotation / Obstruction, each with an intro page). `apply-linop-mutation-rotation` lives in the Mutation group, so "amr sorts before apply" describes a flat ordering that does not exist. The L1-L0/index.md change is more precise (files it "adjacent to the construction-rotation group head as a mixed-kind theme"), but the SUMMARY instruction and the index instruction must agree on ONE target KIND group. As a mixed-kind (estimate/mark = construction, refine = obstruction) theme the landing group is a genuine judgment the integrator must resolve; the instruction as written is ambiguous. All other references resolve (`fold_solve`, `lifecycle.L4/L0`, `spine-root`, `triangular-solve-obstruction`, `fe-assemble-libceed-boundary-obstruction`, `nrm2`, `ksp_solve`, `L1/index.md`, `SUMMARY.md`, `L1-L0/index.md` all exist on disk).

**edge-label-fidelity — pass.** The theme is an L1>L0 lowering and the prose discusses exactly that edge (L1 pure pipeline LHS → L0 imperative loop body RHS). The obstruction sub-kind on the `refine` leaf is correctly classified: `obstruction (opaque-library-ownership)` is right because `mfem::ParMesh::GeneralRefinement` (`:239`) is MFEM-owned — Palace never exposes mesh refinement as a standalone callable, it calls the MFEM method (the correct trigger for opaque-library-ownership vs enum-only-stub). The report also correctly argues `refine` is NOT a DIRECTIVE-3 kernel-api/impl candidate (hanging-node constraint generation is genuinely MFEM-structural, not a well-understood-in-our-tensor-semantics kernel) — so no constructive impl is owed, distinguishing it from the libCEED-quadrature kernel-api the estimate verb leans on. The negative anchor (no Palace-authored mesh-refinement body) is stated, with the `geodata.cpp` `mesh::RefineMesh` a-priori wrapper as corroboration.

**plan-kind-consistency — pass.** Declared `rough-in` matches content shape: the structural three-way decomposition is positively L0-anchored, but the two lowering endpoints (`flux_recovery_estimate`, `dorfler_mark`) are speculative L1 vocabulary proposed THIS cycle, so the theme is correctly rough-in pending harvester promotion (well-foundedness: a lowering theme is at most as resolved as its least-resolved endpoint). The frontmatter `rank: rough-in`, the `## Status` section, and the dep-map row maturity all agree. No firm-claim apparatus is mis-attached; the firm-body-inside-fence guard is N/A (no firm claim).

**skill-uptake-survey — pass (telemetry).** The report's shape (citation-heavy lowering theme) implies the `verify-citation-range`/citecheck procedure; the "Verified-against" section reports `citecheck [ok]` per pinpoint plus direct on-disk END-line confirmation — appropriate uptake. The obstruction sub-kind classification implies `establish-negative-finding-exhaustiveness`; the report does carry a negative anchor for the refine obstruction. Pure presence check, non-blocking.

### Issues found

1. **(warning, surface-or-evidence) — record-definition gap.** `Estimator`, `RefineConfig`, and `IndexSet[E]` are named in the `amr_step` / `flux_recovery_estimate` / `dorfler_mark` signatures (CYCLE.md:120-138) with no definition home anywhere in `book/src/` (grep-confirmed absent). `AmrCarry` is OK (inline `{mesh,indicators,ntdof,err}` + `lifecycle.L4 §3` cross-ref). `Estimator` is partially excused (OQ `flux-projector-constructed-operator-gate-vs-absorbed`); `RefineConfig` (the `refinement.*` IoData surface) and `IndexSet[E]` (the marking result type) have no home or routing. Candidate repair: add a short `## Record definition` note for `RefineConfig`/`IndexSet` or flag `record-<name>-needs-definition-home` in Open questions.

2. **(warning, cross-reference-integrity) — live links to non-existent files.** Chapter prose (CYCLE.md:105-106, :131, :138) live-links `../L1/flux_recovery_estimate.md` and `../L1/dorfler_mark.md`, neither of which exists on disk → hard `linkcheck2` build error at integration. Candidate repair: integrator materializes the two as `stub` files (≥2 converging references present, stub policy applies), OR de-link the prose to plain text matching the rough-in dep-map-row convention the report already used.

3. **(warning, cross-reference-integrity) — ambiguous SUMMARY.md insertion target.** The SUMMARY proposed-change (CYCLE.md:279-282) instructs an alpha-position "after `amr` sorts before `apply`" that presumes a flat L1>L0 list; the actual SUMMARY has three KIND sub-groups (Mutation / Construction / Obstruction), and `apply-linop-...` is in the Mutation group. The L1-L0/index.md change files it under the construction-rotation group head as a mixed-kind theme. The two instructions must be reconciled to one landing KIND group. Candidate repair: rewrite the SUMMARY instruction to name the explicit sub-group + intro-page anchor (mixed-kind: the integrator chooses Construction-rotation or Obstruction group consistently with the index change).

---

## Repair

### Fixes attempted

- **Finding 1 — surface-or-evidence: record-definition gap** (`Estimator` / `RefineConfig` / `IndexSet[E]` named in signatures with no definition home; `AmrCarry` OK).
  - **Decision**: repaired.
  - **Action**: Added a `## Record definition` section to `CYCLE.md` (the new L1>L0 chapter `book/src/L1-L0/amr-estimate-mark-refine.md`), placed between the `## L1 form (LHS)` `where:` block and `## L0 form (RHS)`. It defines all four single-consumer types in themselves: **`AmrCarry`** (`{ mesh, indicators, ntdof, err }`, cross-ref `lifecycle.L4 §3`); **`RefineConfig`** (L0 home = `RefinementData` struct `palace/utils/configfile.hpp:96-125`, field `fraction` ← `update_fraction :118-119`, construction-time stratum); **`IndexSet[E]`** (element-index subset into the `E: n_elem` axis, L0 home = the `mfem::Array<int>` filled by `MarkedElements` `ind.Append(i)` loop `basesolver.cpp:103-115`); **`Estimator`** (construction-time flux-recovery closure; fields-to-be-firmed routed to the harvester via the existing OQ `flux-projector-constructed-operator-gate-vs-absorbed`). The `RefinementData` backing struct + line range were verified against on-disk source via `palace-codemap` (`read_range configfile.hpp:90-125`); the marking-result and carry shapes are transcribed from already-cited L0 sites. This is mechanical (transcribing already-cited backing structs + already-stated semantics), not substantive authoring — single-consumer in-chapter `## Record definition` is the convention's prescribed surgical fix.

- **Finding 2 — cross-reference-integrity: live links to non-existent files** (`L1/flux_recovery_estimate.md` / `L1/dorfler_mark.md` → hard `linkcheck2` error).
  - **Decision**: repaired.
  - **Action**: De-linked the live markdown links in the chapter PROSE to plain-text forward-references per the rough-in forward-reference convention. In `## Status`, rewrote `[`flux_recovery_estimate`](../L1/flux_recovery_estimate.md)`/`[`dorfler_mark`](../L1/dorfler_mark.md)` to backticked plain text + a note that the integrator may materialize them as rough-in stub rows. In the `## L1 form` `where:` block, annotated the `flux_recovery_estimate` signature line as a speculative-rough-in plain-text forward-ref. Verified via `grep` that NO live links to `flux_recovery_estimate.md` / `dorfler_mark.md` remain anywhere in CYCLE.md (the dep-map rows were already plain-text). The live link to `../L4/fold_solve.md` (which exists on disk) is preserved. Note for integrator: the stub policy permits materializing the two verbs as rough-in stubs (≥2 converging references present); de-linking guarantees a clean rebuild regardless of that choice.

- **Finding 3 — cross-reference-integrity: ambiguous SUMMARY.md insertion target** (flat-list presumption vs the 3 KIND sub-groups; SUMMARY ↔ index inconsistency).
  - **Decision**: repaired.
  - **Action**: Reconciled BOTH the `book/src/SUMMARY.md` and `book/src/L1-L0/index.md` proposed-change blocks onto ONE landing group: the **Construction-rotation** sub-group, alpha-FIRST position. Verified against on-disk structure — SUMMARY's L1>L0 Part (lines 234-279) has three KIND sub-groups (`Mutation-rotation themes` :236 / `Construction-rotation themes` :266 / `Obstruction themes` :275), each a nested intro-page head; `apply-linop-mutation-rotation` is in the Mutation group (:237), so the original "amr before apply" flat ordering does not exist. The index.md table mirrors the same three group-header rows (`**Mutation-rotation**` :32 / `**Construction-rotation**` :62 / `**Obstruction**` :71). `amr-estimate-mark-refine` sorts alpha-first within Construction-rotation (before `build-mesh-construction-rotation`). The mixed-kind landing-group judgment (estimate/mark constructive endpoints dominate; refine is a documented opaque-library obstruction SUB-leaf, not the theme's kind) is resolved to Construction-rotation in BOTH instructions, with the rationale stated inline. This is mechanical reconciliation against verified on-disk structure, not a content decision.

### Unrepairable findings

None. All three flagged findings were surgically repairable: (1) record definitions are transcriptions of already-cited backing structs / already-stated semantics into the convention's prescribed `## Record definition` section; (2) de-linking to plain-text forward-references is the standard rough-in mechanical fix; (3) the SUMMARY/index reconciliation is a structural alpha-position correction verified against the on-disk SUMMARY + index, with the mixed-kind group choice resolved consistently across both blocks.

## Suggested resolution

`ready`. Notes for the integrator:
- The two speculative L1 verbs (`flux_recovery_estimate` / `dorfler_mark`) are now plain-text forward-references in the prose AND already-plain-text rough-in dep-map rows. The stub policy permits materializing them as `stub`/rough-in files (≥2 converging references present); either path rebuilds cleanly. If you do stub them, you may then re-link the prose — but that is optional, not required for a clean build.
- The SUMMARY + L1-L0/index insertions both land `amr-estimate-mark-refine` in the **Construction-rotation** sub-group, alpha-first (before `build-mesh-construction-rotation`). Insert at that exact position in both files; do not append flat.
- The `Estimator` record's full field set is intentionally deferred to the harvester (OQ `flux-projector-constructed-operator-gate-vs-absorbed`); the in-chapter `## Record definition` gives it a definition home (the construction-time closure it is) without pre-empting that harvest decision.
