---
agent: layer-intro-author
invoked_at: 2026-06-09T023253Z
scope: c152 D2 — de-bulk L2/index + L2-L1/index + L3/index + L3-L2/index (F-class + L2 E-class)
status: integrated
integrated_at: 2026-06-09T025046Z
integration_commit: PLACEHOLDER_SHA
integration_notes: "Applied clean (staging row D2). F-class de-bulk of 4 NO-FRONTMATTER-RANK indexes L2/L2-L1/L3/L3-L2 (## Working Notes stripped; ## Structural fact chebyshev-floor + ## L4 routing of the L3 cohort LIFTED); status tokens byte-exact; 4 witness-log citations preserved-in-authoritative-homes. RETIRED the stale prose-slug dot-l2-leaf-floor-vs-fold-only-design defining home in L2/index.md — OQ reciprocal-stale-prose-slug-dot-l2-leaf-floor-ref RESOLVED (with D4). Baseline HELD EXACTLY; build EXIT 0; step-5b/5c/5d clean."
---

# CYCLE: c152 D2 — L2 / L2-L1 / L3 / L3-L2 index de-bulk

## Summary

D/E/F de-bulk scale-out wave, dispatch D2. Applied the `finalization-debulk` skill (+ the 3 meta-150 sections) and the c151 PILOT pattern to the four navigational-container layer/lowering index files. STRIPPED the slice-era `## Working Notes` (F-class) section from all four; for `L2/index.md` the lone E-class date-provenance occurrence sat inside that section (no E-class outside it). COUPLING-LIFT-AWARE: lifted three genuine load-bearing static facts to explicit structural sections rather than dropping them. `## Context` sections were NOT touched. All dep-map cell SOLE-rank-carrier status tokens, citations, and `[link]`s preserved. **Graded-stack baseline HELD EXACTLY.** Build `cargo make book` EXIT 0.

**OQ RESOLUTION:** the prompt's named OQ `reciprocal-stale-prose-slug-dot-l2-leaf-floor-ref` is resolved — see the dedicated section below (with one honest residual flagged for the c153 closer).

I edited the four files directly (this is the de-bulk write-mode dispatch, not a proposed-changes dispatch).

## Per-file outcome

### `book/src/L2/index.md` — F-class + E-class

- **Sections stripped:** `## Working Notes` (8 bullets) stripped wholesale. Process content removed: combinator-miner-output meta; the `krylov_step` six-variant-axis + five-pattern-instance **witness log** (the `iterative.cpp:360-486` / `:543-705` / `:563-683` + `L4/krylov_step.md` / `L4/chebyshev.md` pointer list — process accounting pointing AT constructs owned elsewhere); the MINRES/BiCGStab decision-log pointer (the lone date occurrence, `scaffolding/decisions/2026-05-27-…`); and the four duplicate-of-overlay restatements (fold-cohort boundary, combinator-as-entry, L2 kernel/driver pair, gram/deflate/eigsolve, standalone-gate-floors) — every one already stated in `## Semantics (overlay)` (§"Fold cohorts"/§"Named compositions"/§"Standalone elementwise/gate floors") + `## Vocabulary cohort` + the dep-map rows.
- **Section lifted (coupling-lift-aware):** the **`chebyshev` floor naming exception** — a genuine load-bearing static structural fact (why `L2/chebyshev-iteration.md` deviates from the same-named-floor convention) NOT stated elsewhere in the file — lifted to a new `## Structural fact` section.
- **E-class:** the only `2026-0X-XX` date in the file was the decision-log path inside the stripped `## Working Notes`; removed with the section. No stray date-provenance remains (`grep` clean).
- **`## Context` (lines 20-28):** untouched.
- **Citations before → after:** the four witness-log citations dropped (`iterative.cpp:360-486`, `:543-705`, `:563-683`, `nleps.cpp:524-531`) were all process-log witnesses pointing at constructs whose authoritative homes retain them: `nleps.cpp:524-531` → `L2/gram.md:10,:101`; the `iterative.cpp` Krylov pattern instances → `L4/krylov_step.md` + `L0/linalg-iterative-file.md`. No citation lost from the artifact. The 9 unique source citations now in the file are all in `## Semantics (overlay)` / dep-map (load-bearing per-operator anchors), unchanged.
- **Dep-map status-token count before → after:** 18 rows = 17 `firm` + 1 `partly-constructive` (`deflate`) — **unchanged byte-for-byte** (the strip was entirely below the dep-map; header self-summing tally "17 firm + 1 partly-constructive = 18" still correct).

### `book/src/L2-L1/index.md` — F-class

- **Sections stripped:** `## Working Notes` (2 bullets) wholesale. Content: the optimization-trick-unfolding meta-note + the "Leaf-floor design realization (b)" restatement — both already carried by `## Vocabulary cohort` (the in-line absorption note + the standalone-gate-edge bullet) and the `## Theme list` status cells.
- **Lifted:** none needed — no unique load-bearing static fact (the `divfree-projector-leaf-identity` one-genuine-fusion fact restated there is already in the theme list status cell + the §Vocabulary cohort bullet).
- **`## Context` (lines 26-27):** untouched.
- **Citations before → after:** no source citations in the removed lines; the 5 unique source citations (`nleps.cpp:533-535`, etc., in the `## Theme list` status cells) preserved.
- **Dep-map status-token count:** 11 theme-list rows, status cells untouched.

### `book/src/L3/index.md` — F-class (incl. the `### L3-cohort-growth audit (SETTLED)` subsection)

- **Sections stripped:** `## Working Notes` (3 bullets + the `### L3-cohort-growth audit (SETTLED)` subsection) wholesale. Process/judgment content removed: the pipeline-destination meta; the canonical-write-up pointer; the leaf-cohort combinator-as-entry restatement (already in §Vocabulary cohort + §Semantics overlay); and the **SETTLED verdict log** — the (A)/(B)/(C) audit-history classification of the 18 L1 ops, the "DO NOT re-propose" / "STALE and should be rejected" process directives. This is exactly slice-era verdict/judgment accounting.
- **Section lifted (coupling-lift-aware):** the **two load-bearing static facts** buried in the audit verdict — (i) the L4-routing disposition (which L3 ops rise to L4 vs stay low, per `black-box-vs-accelerated-kernels` §2) and (ii) the **small-dense-coordinate-space disqualifier criterion** for L3 membership — lifted to a new `## L4 routing of the L3 cohort` section, restated as static structural facts (dropping the "(C) NEGATIVE LIST / DO NOT re-propose" process framing while keeping the criterion + the named disqualified ops).
- **`## Context` (lines 20-21) + `## Semantics (overlay)`:** untouched.
- **Inbound line-anchor safety:** the inbound refs into `L3/index.md` from `L3/orthogonalize.md`, `L3/normalize.md`, `L3/reciprocal.md`, `L3/elementwise_product.md`, `L3/divfree_projector.md` all use line numbers `:13`/`:46`/`:48`/`:58` (into `## Semantics (overlay)`, ABOVE the stripped `## Working Notes` at line 95+) — these lines did NOT shift, refs preserved. No `#`-anchor refs to the audit subsection exist (grep clean).
- **Citations before → after:** no source citations in removed lines; the 9 unique source citations (in the dep-map Lowers-to/Status cells, e.g. `slepc.cpp:694`, `arpack.cpp:318`, `transientsolver.cpp:77`) preserved.
- **Dep-map status-token count:** 14 `firm` + 18 `partial-obstruction` + 8 `roadmap_goal`-bearing cells across the dep-map (Dependencies + Lowers-to + Status columns) — **untouched** (strip was below the dep-map + below the consolidated-tally `## Vocabulary cohort`); the authoritative tally "17 firm + 4 partial-obstruction" in §Vocabulary cohort preserved.

### `book/src/L3-L2/index.md` — F-class (incl. the stale-slug reference)

- **Sections stripped:** `## Working Notes` (4 bullets) wholesale. Content: the negative-result definitional meta; the degenerate-`-body-identity`-edges restatement (already in §Vocabulary cohort §"Degenerate identity-in-named-terms"); the full **erasure-scope taxonomy restatement** (already stated in §Vocabulary cohort — the per-theme bullets + the line-57 five-root summary); and the **"Design fork RATIFIED — leaf-floor (b)"** ratification-log bullet, which carried the stale slug `dot-l2-leaf-floor-vs-fold-only-design` reference.
- **Lifted:** none needed — every static fact (the five erasure-scope corners, the kept-vs-in-line edge dispositions) is already present in §Vocabulary cohort (lines 49-57). The "Design fork RATIFIED" load-bearing fact (BLAS-1/axpy-family `-body-identity` edges keep their same-named L2 leaf-floor RHS) is already implicit in the §Vocabulary cohort degenerate-edges bullet + the L2/index dep-map.
- **`## Context` (lines 20-22):** untouched.
- **Citations before → after:** no source citations in removed lines; the 4 unique source citations (`slepc.cpp:694`, `arpack.cpp:315-339`, `timeoperator.cpp:410`, `transientsolver.cpp:77`, in the `## Theme list` cells) preserved.
- **Dep-map status-token count:** 6 theme-list rows, all `firm`, status cells untouched.

## OQ resolution — `reciprocal-stale-prose-slug-dot-l2-leaf-floor-ref`

**RESOLVED (with one honest residual flagged).** The stale prose slug `dot-l2-leaf-floor-vs-fold-only-design` named the `L2/index.md` §"Working Notes" section (the "Combinator-as-entry + specialization-stub leaves" / "Design fork" framing). That section is now **stripped**, so the slug's **defining/referent home is retired** — the OQ's root cause (a prose slug pointing at a slice-era Working-Notes log) is gone.

**Residual (out of this dispatch's 4-file scope — flagged for the c153 closer):** dangling *prose* references to the now-retired slug + the now-gone `§Working-Notes` target survive in sibling files that are themselves de-bulk targets:
- `book/src/L2/normalize.md:39`, `:109`, `:158` — three prose mentions of `dot-l2-leaf-floor-vs-fold-only-design` + `[L2/index](./index.md) §Working-Notes`.
- `book/src/L2/reciprocal.md:79`, `:379` — two prose mentions.
- `book/src/L3-L2/fold-solve-time-step-body.md:15` — one `L3-L2/index.md §Working-Notes` prose pointer.

These are **`linkcheck2`-safe** (the `[L2/index](./index.md)` file link is valid; `§Working-Notes` is prose-only, not a markdown `#`-anchor), so **no build break** — confirmed by `cargo make book` EXIT 0. They are now stale prose pointers to be cleaned when those chapters' own §Working-Notes / prose-slug content is de-bulked in the c153 closer. I did NOT reach outside my 4-file scope to edit them (atomic-dispatch discipline). **The slug as a live section is retired; the residual is dangling prose in not-yet-de-bulked siblings.**

## Verification

- **Inbound-anchor check:** `grep -rn '<file>#working-notes'` for all four files → **0 inbound `#working-notes` anchor refs** (none to re-point). Inbound *line-number* refs into `L3/index.md` (`:46`/`:48`/`:58`) point above the strip and are preserved.
- **F-section residue:** `grep '## Working Notes'` across all four → **0 remaining** (clean).
- **E-class residue (L2/index):** `grep '2026-0X-XX'` → **0 stray date-provenance** (clean).
- **Stale-slug as live section:** the `L2/index.md` §Working-Notes home of `dot-l2-leaf-floor-vs-fold-only-design` is gone; `L3-L2/index.md` reference removed; sibling-file prose residuals flagged above.
- **Status tokens:** L2/index 18 rows (17 firm + 1 partly-constructive) byte-exact; L2-L1 11 rows, L3 dep-map cells, L3-L2 6 rows all untouched.
- **Citations:** the 4 dropped L2/index witness-log citations preserved in authoritative homes (`L2/gram.md`, `L4/krylov_step.md`, `L0/linalg-iterative-file.md`); all per-operator dep-map anchors preserved.
- **Graded-stack baseline:** `python3 tools/graded-stack-lint/graded_stack_lint.py --book-src book/src` → **HELD EXACTLY** — `files=392, typed=331, untyped=61, rank_violations=0, unresolved_depends_on_targets=0, promotion_frontier=11, detritus=123, true_detritus=51`.
- **Build:** `cargo make book` → **EXIT 0** (warnings present are all pre-existing + unrelated: `<opertype>` in `L0/linalg-solver-file.md`, `<other>` in a meta-review, search-index size — none in the 4 edited files).

## Open questions / caveats

- The OQ-residual sibling-file prose mentions of the retired slug (`L2/normalize.md`, `L2/reciprocal.md`, `L3-L2/fold-solve-time-step-body.md`) are the natural c153-closer cleanup — they belong to chapters that carry their own §Working-Notes / fold-fork prose and are D/E/F targets in their own right. No action needed this dispatch (build-safe); flagged so the closer sweep picks them up.
- No node/edge/rank/status/semantics move occurred; the four files remain `kind: navigational-container` (no `rank:`), edges unchanged.
