---
agent: cycle-planner
invoked_at: 2026-06-05T044329Z
scope: cycle-100 dispatch plan (batch-32, position 1/3 — batch-OPENING cycle)
status: pending
---

# Cycle 100 dispatch plan

## Goals selected this cycle

The graded-stack two-axis artifact-health campaign is fully discharged (corpus 9→0, `book/src/spec/` gone, `rank_violations` held at 0). The batch-32 LEAD returns to the standing forward frontier: **bottom-up vocabulary + 5-driver→L4 backend-lowering completeness** (priorities item #1, HIGH).

Reading the *actual* eligible frontier (graded-stack linter `promotion_frontier: 10`) against the on-disk maturity ground truth, the standing LEAD is now mostly worked off: all 5 driver columns + all output-product columns + the lifecycle ROOT are **firm** (only `boundary-mode` stays `seed`, demand-gated); `solve_family` is **firm** (the c086 stale priorities line is discharged — do NOT re-propose it). The genuinely-open, clean-gated, highest-fan-out work that remains is the **promotion of two well-anchored `rough-in` L1>L0 themes whose Status text explicitly defers the final per-sub-rule confirmation to `lowering-verifier`**: `apply-linop-mutation-rotation` (every matvec lowers through it) and `ksp-solve-mutation-rotation` (all 5 driver pipelines lower through `BaseKspSolver::Mult`). Plus the LEAD's observational half — an **L4 per-pipeline backend-lowering completeness survey** — and the one decided LOW hygiene micro-sweep (item #2, the Class-B slice-deletion residue, 73 stale plaintext mentions confirmed on disk).

This cycle pushes the bottom-up vocabulary promotion frontier (D1/D2), establishes the "is the in-scope feature set L4-complete?" picture as an evidence artifact (D3), and discharges the decided narrative-honesty residual (D4). Per the VOCABULARY-SHIFT REDIRECT: these are warrant-first audits of already-warranted themes (no mirroring), and no solver is forced to advance a layer.

## Dispatches

### D1 — (`lowering-verifier`, `L1-L0/apply-linop-mutation-rotation` rough-in→firm full per-sub-rule audit, deps: none)
- **scope**: Audit `book/src/L1-L0/apply-linop-mutation-rotation.md` (currently `rough-in`, `## Status` :346) against the L0 corpus to discharge the explicitly-deferred promotion gate. The Status text states: "Full sub-rule verification against the L0 corpus and integration testing with the sister theme `axpby-mutation-rotation` (specifically the sub-pattern D / E composition path) deferred to `lowering-verifier`." Verify the sub-pattern recognition rules row-by-row against the cited L0 ranges; append `verified_against:` rows for any uncovered sub-rule; render the firm-promotion verdict. The `apply_linop` L1 operator is firm; its laws are syntactic identities on positive `mfem::Operator::Mult` source (the firm-on-positive-structure / syntactic-identity escape — the `apply_linop` situation named in the CLAUDE.md `rough-in (test-coverage-bounded)` invariant, NOT a numerically-asserted-axiom situation), so the verifier should weigh whether the escape promotes this theme to `firm` outright. **Codemap-confirmed L0 anchors** (verified this cycle via `read_range` — confirm on-disk; these are localization HINTS, the verifier re-reads): `palace/linalg/operator.hpp:21` (`using Operator = mfem::Operator;` — confirmed), `:54` (`ComplexOperator::Mult` pure-virtual — confirmed), `:56` (`MultTranspose` decl — confirmed; END-bound `:56` is a single-decl line, low drift risk). On firm flip, update this theme's own dep-map row in `book/src/L1-L0/index.md:21` (status column only).
- **rationale**: HIGHEST-fan-out genuinely-open bottom-up work. `apply_linop` is the most-reused operator in the stack (every matvec); firming its L1>L0 lowering completes the matvec lowering floor that every solver pipeline rests on. Serves priorities item #1 (bottom-up vocabulary). Promotion-frontier node (graded-stack linter). Not structurally blocked (positive L0 source present); the gate is the deferred per-sub-rule audit, which is exactly this dispatch.

### D2 — (`lowering-verifier`, `L1-L0/ksp-solve-mutation-rotation` rough-in→firm full per-sub-rule audit, deps: none)
- **scope**: Audit `book/src/L1-L0/ksp-solve-mutation-rotation.md` (currently `rough-in`, `## Status` :762; note the OQ ledger line 30 loosely calls it "firm" in the cg-quirk-lift closure — the ON-DISK Status is `rough-in`, governing) against the L0 corpus to discharge the deferred gate. Status text: "Full per-step sub-rewrite verification (cross-checking each axpy / dot / Mult invocation inside each Krylov body against the sister themes' recognition rules) deferred to `lowering-verifier`." The outer-composition rewrite at `BaseKspSolver::Mult` is fully cited; verify the four sub-pattern recognition rules + the three inner per-method bodies' section-level cites against the sister themes' (`axpby`/`dot`/`apply_linop`) rules; append `verified_against:` rows; render verdict. The unimplemented-Krylov boundary (applicability condition §1) and the sibling obstruction themes stay as documented — do NOT attempt to fill them. **Codemap-confirmed L0 anchor** (verified this cycle via `read_range`): `palace/linalg/ksp.cpp:296-310` (`template <typename OperType> void BaseKspSolver<OperType>::Mult(const VecType &x, VecType &y) const { BlockTimer; ksp->Mult; if(!GetConverged) Mpi::Warning; ksp_mult++; ksp_mult_it += GetNumIterations; }` — the cited four-surface-concern body confirmed byte-current; END `:310` is the `}` close-brace — on-disk re-confirm the END bound per the codemap-read-range-plus-one-drift guard). On firm flip, update this theme's own dep-map row in `book/src/L1-L0/index.md:36` (status column only).
- **rationale**: HIGH-fan-out genuinely-open bottom-up work. `ksp_solve` is the outer driver every one of the 5 solver pipelines lowers through; firming its L1>L0 rotation firms the driver-lowering floor under the (already-firm) driver feature columns. Serves priorities item #1. Promotion-frontier node. Not structurally blocked.

### D3 — (`cross-layer-cross-cutter`, L4 per-pipeline backend-lowering completeness survey [observational], deps: none)
- **scope**: A single cross-layer coverage observation: produce the **L4 backend-lowering completeness matrix** for the in-scope feature set — for each of the 5 solver pipelines + the FE-assembly cohort + the output-products, state whether it reaches L4 (a firm L4 cap or L4 feature surface) or carries an on-disk `no-L4-by-design` verdict, and identify any GENUINE remaining hole (a firm-L3-or-L1 construction in scope that neither reaches L4 nor carries a no-L4 verdict). The L4 feature surfaces ARE the outward backend-lowering entry points (`project_l4_is_backend_lowering_target`). This is the LEAD's "per-pipeline L4-completeness picture" half. **OBSERVATIONAL — no `book/` mutation** (a coverage-gap/edge-mismatch observation in CYCLE.md proposed-changes form; any materialization is a downstream dispatch). Anchor against the firm L4 cohort (`book/src/L4/` + `book/src/feature/*.L4.md`), the firm-driver-column status survey done this planning cycle (all 5 drivers + lifecycle ROOT + output-products firm; `boundary-mode` seed/demand-gated), and `roadmap.md`. Cross-reference the FE-assembly-at-L1 hole flagged in memory `project_l4_is_backend_lowering_target` ("the FE-assembly/FE-space cohort stranded at L1 is a hole to close") — confirm or refute it on disk.
- **rationale**: Closes the standing "is the in-scope stack L4-complete for backend-lowering?" question as a mechanically-grounded evidence artifact, which is the LEAD's observational half and the input that tells the batch-32 frontier whether any genuine bottom-up→L4 gap remains (e.g. the FE-assembly cohort) or whether the frontier is exhausted. Low cost (read-only), high decision-value. Serves priorities item #1.

### D4 — (`same-layer-cross-cutter`, Class-B slice-deletion residue cleanup [one consolidated micro-sweep], deps: none)
- **scope**: The decided dedicated micro-sweep for priorities item #2 (`batch-31-slice-deletion-class-B-residue-cleanup`). After the corpus 9→0 deletion, ~73 firm-chapter plaintext provenance pointers to deleted `book/src/spec/slices/*` line-ranges survive book-wide (confirmed on disk this cycle: e.g. `L3/chebyshev.md:542`, `L3/index.md`, `L2/chebyshev-iteration.md:269`, `L1-L0/triangular-solve-obstruction.md:339,:533`, `L2/index.md:133`, `L4/krylov-step.md:255-256` per priorities, `L1/orthogonalize.md:300`, `L1/chebyshev-smoother.md`) PLUS ~22 stale Mermaid edges in `concepts/dependency-map.md` keyed off the deleted `cg_preconditioning_framework` node. These are **build-SAFE** (plaintext / inline-code / Mermaid node-labels, NOT markdown links — `linkcheck2` passes; confirmed the deleted-slug references are not live `[link]`s) but point at files that no longer exist → **repoint to the firm absorb-home (citing L0 directly) or strike**. Carry the inbound/within-file maturity-grep disciplines (the `deleted-slug-inbound-live-link-sweep` skill, broadened to any `book/src/**` deletion). **EXCLUDE `meta-reviews/`** — those 5 mentions ARE accept-as-historical (frozen records, like git history). Consolidate into ONE dispatch (the residue is cross-cutting but mechanical).
- **rationale**: Item #2 (LOW/hygiene, decided dedicated-cleanup not accept-as-historical, because the pointers reference deleted files). Narrative-honesty residual; no build break, no false claim. Migrated OQs `krylov-trio-class-B-plaintext-mention-residue-batch31-cleanup` + `dependency-map-cg-precond-stale-mermaid-edges`.

## Deliverable-presence verification (paste-inline-evidence, per the strengthened pre-dispatch discipline)

**D1 — `L1-L0/apply-linop-mutation-rotation` (named-artifact scope; audit-class):**
1. File existence: `ls book/src/L1-L0/apply-linop-mutation-rotation.md` → `-rw-rw-r-- ... apply-linop-mutation-rotation.md` (EXISTS).
2. Maturity / already-discharged: on-disk `## Status` :346 → ``` `rough-in` — sub-pattern recognition rules sketched; ... Full sub-rule verification against the L0 corpus and integration testing ... deferred to `lowering-verifier`. ``` → **rough-in, audit explicitly deferred to lowering-verifier = OPEN** (the proposed deliverable is firm-promotion, ABOVE on-disk maturity). `verified_against:` block count = 1 (partial, 10 citation rows; full per-sub-rule audit NOT yet done per Status). Not a no-op.
3. OQ RESOLVED-grep: `grep -niE 'apply-linop-mutation-rotation.*(RESOLVED|CLOSED)' scaffolding/open-questions.md` → **no matches**. Not stale-closed.
4. Structural-block: positive L0 source present (`operator.hpp:54` Mult pure-virtual, confirmed via codemap this cycle); the firm-on-positive-structure / syntactic-identity escape applies → NOT structurally blocked. The gate is the deferred per-sub-rule audit = this dispatch. NOT on the STOP-PROPOSING negative list.

**D2 — `L1-L0/ksp-solve-mutation-rotation` (named-artifact scope; audit-class):**
1. File existence: `ls book/src/L1-L0/ksp-solve-mutation-rotation.md` → EXISTS (41KB-class chapter).
2. Maturity: on-disk `## Status` :762 → ``` `rough-in` — the four sub-pattern recognition rules are sketched; the outer-composition rewrite at `BaseKspSolver::Mult` is fully cited; ... Full per-step sub-rewrite verification ... deferred to `lowering-verifier`. ``` → **rough-in = OPEN**. (NOTE: OQ ledger :30 calls it "firm" in the cg-quirk-lift prose — the on-disk Status governs per the paste-evidence rule; it is `rough-in`.) `verified_against:` block count = 1 (22 citation rows; per-sub-rule audit deferred).
3. OQ RESOLVED-grep: the only match is `cg-initial-residual-quirk-palace-bug-flag-lift` (a struck sub-OQ about a *different* deliverable — the cg-quirk annotation lift, landed c035; NOT the per-sub-rule firm audit). No RESOLVED/CLOSED on the audit deliverable itself.
4. Structural-block: outer rewrite fully cited at `ksp.cpp:296-310` (confirmed byte-current via codemap this cycle); not structurally blocked. The unimplemented-Krylov sub-bodies stay documented-obstruction (NOT a target, correctly out of scope). NOT on STOP-PROPOSING list.

**D3 — L4 completeness survey:** OPEN by construction (observational cross-layer survey, no named-artifact deliverable, no prior-cycle history of THIS survey under the discharged-campaign state). Skip-justified: open by construction.

**D4 — Class-B residue cleanup:** target-presence confirmed inline this cycle — `grep -rnE 'spec/slices/|cg_preconditioning_framework|arnoldi_step\.md|gmres\.md:|polynomial_recurrence_step' book/src --include=*.md | grep -v meta-reviews/ | wc -l` → **73** (live residue present; deliverable is open). Sample confirmed: `L3/chebyshev.md:542`, `L2/chebyshev-iteration.md:269`, `L1-L0/triangular-solve-obstruction.md:339`, `L2/index.md:133`. Item #2 is decided-open in priorities (not struck).

**solve_family — REJECTED as a candidate (stale priorities line caught):** the Backlog carries `solve-family-list-homomorphism-law-confidence-pass` (HIGH-fan-out, line 229) as an open `lowering-verifier` pick. On-disk check: `book/src/L4/solve_family.md` `## Status` :144 → ``` `firm`. **Reasoning (firm-on-positive-structure / syntactic-identity escape, c086 lowering-verifier law-confidence pass):** ... ``` → **already firm, landed c086.** The law-confidence pass is DISCHARGED; do NOT re-propose. (The electrostatic + magnetostatic driver columns it was meant to unblock are both `firm` since c095, confirmed on disk.)

## Overlap analysis

- **D1 ↔ D2**: each audits a DISTINCT L1-L0 theme file (`apply-linop-mutation-rotation.md` vs `ksp-solve-mutation-rotation.md`) — disjoint. On a firm flip each edits its OWN dep-map row in `book/src/L1-L0/index.md` (D1 → :21, D2 → :36; 15 lines apart, non-adjacent, distinct rows). Per the established precedent (cycle-019/020 `L1-L0/index.md` multi-row case resolved cleanly via serial per-report re-read-before-edit) and the conflict-tolerance philosophy (distinct dep-map rows are NOT operationally overlapping), this is **PARALLEL-safe**. Neither names operators the other authors.
- **D1/D2 ↔ D3**: D3 is read-only (observational survey, no `book/` mutation). NON-overlapping with everything.
- **D1/D2 ↔ D4**: D4's target file set (the 73 stale-slice-mention files: `L3/chebyshev.md`, `L3/index.md`, `L2/chebyshev-iteration.md`, `L2/index.md`, `L1-L0/triangular-solve-obstruction.md`, `L4/krylov-step.md`, `L1/orthogonalize.md`, `L1/chebyshev-smoother.md`, `concepts/dependency-map.md`, …) does **NOT** include either audit theme file (confirmed: `grep -l` on the 3 candidate theme files returned "none carry stale slice mentions") NOR `L1-L0/index.md` (confirmed 0 stale mentions there). Disjoint → **PARALLEL-safe**.
- **D3 ↔ D4**: D3 read-only; D4 edits a disjoint residue file set. NON-overlapping.

All four are mutually non-overlapping → one wave.

## Sequencing schedule

**Wave 1 (all parallel):** D1, D2, D3, D4.

No forward-reference dependencies between dispatches (no dispatch references another's not-yet-landed slug; D1/D2 are independent audits, D3 is observational, D4 is a residue sweep). One wave. Standard pipeline tail: 4 critics → repairers as needed → `integrator-per-report` ×4 (serial) → ONE `integrator-finalize`.

## Open questions / caveats

- **`eigsolve-convergence-reason-mapping` deliberately NOT dispatched this cycle (structural-block discipline).** It is the 3rd `promotion_frontier` `rough-in`/`partly-constructive` L1-L0 theme, but its firm-promotion of the 8 diverged-reason rows is **structurally blocked** by an unchanged upstream gate: OQ `eigsolve-convergence-reason-mapping-promotion` line 94 — *Trigger: Palace reads `EPSGetConvergedReason` (currently only prints via `EPSConvergedReasonView`)* — and the whole Palace tree has zero `EPS_*_REASON` reads. Per the Step-4 structural-block check I do NOT propose it as a firm-promotion. A `lowering-verifier` *ratify-only* audit (confirm the SLEPc enum set + accept the forward-looking shape as a methodology pattern per the cycle-012 partly-constructive escape, WITHOUT flipping firm) is legitimate but LOW/MEDIUM fan-out and trigger-gated (OQ `...-slepc-enum-upstream-confirm`, line 95); deferred. Flag for the batch-32 meta-phase if a ratify-audit is wanted.
- **`boundary-mode` feature column stays `seed`/demand-gated** (confirmed on disk: the only non-firm of 12 columns). Not dispatched — the demand-gate trigger (a downstream waveguide-mode consumer OR an output-product spine-completion size-judgment dispatch) has not fired. Standing gate, carried.
- **Dead orchestrator-era SKILLS retirement (priorities item #3a)** — the 5 dead skills (`cluster-friction-patterns`, `survey-friction-window`, `verify-rotation-citation`, `propose-rotation`, `skill-selection`, referencing the deleted `episodic.jsonl`/`prompts/`/`schemas/`) are a **meta-phase enactment** (`skills/` write-authority), NOT a specialized-agent dispatch. I do NOT dispatch it; it is the batch-32 meta-phase's skill-retirement sweep. Recorded here + carried in priorities item #3. The 2 ASK items (BOOTSTRAP/MIGRATION compact — note: these were just compacted to history-stubs per the recent commit `8667583`; the ASK may now be moot — and `.env.example`) await human disposition.
- **Wave is 4 substantive dispatches (well under the 12 cap).** Deliberate — the discharged-campaign frontier offers few clean-gated picks; "fewer is fine when the priorities don't fill 12 slots." Filling slots with the structurally-blocked D3-eigsolve or the demand-gated boundary-mode would be false-recruitment. Quality over slot-count.
- **D1/D2 firm-flip → `L1-L0/index.md` dep-map row edits are PARALLEL-marked** (distinct non-adjacent rows). If the integrator observes a merge touch, that is the useful integration-tooling signal the conflict-tolerance philosophy wants surfaced — not a planning error. Cheaply corrected by the per-report integrator's re-read-before-edit (the established precedent).
