---
agent: cycle-planner
invoked_at: 2026-06-05T010427Z
scope: cycle-099 dispatch plan
status: pending
---

# Cycle 099 dispatch plan

## Goals selected this cycle

THE LEAD = the **krylov-trio P2 slice-deletion sub-campaign** = the COMPLETION of `graded-stack-p2-slice-deletion-tranche` (priorities item-0). cycle-098 landed the slice corpus at **9 → 3**; the 3 survivors are the mutually-referencing Krylov slices `cg` / `gmres` / `arnoldi_step`. Per the c098 finalize guidance + the explicit forward-looking wave-conflict warning, the trio's ~31 inbound markdown links + ~16 plain-text evidence-citation mention-files INTERLEAVE on the SAME shared krylov-step chapters (the firm L2/L3/L4 `krylov-step` trio + the `L4-L3/*` dissolution themes + `L3-L2/krylov-step-body-identity` + `L2-L1/krylov-step-kernel-defusion`), so this MUST be ONE single-coordinated-owner sub-campaign sequenced across waves — **NOT 3 parallel slice-split dispatches that would collide on the same chapters.** My on-disk audit confirms the trio is GC-graph-clean to delete (no typed `depends-on`/`reference` frontmatter edge points INTO any slice — all references are plain-text evidence citations + consumer-list markdown links), with exactly ONE genuinely-unlifted live datum (`cg.md:27-141`, the CG-concrete `cg_first_step`/`cg_steady_step`/`forget_beta_prev` worked instance) that needs a firm home BEFORE `cg` deletes. I am dispatching a **single-owner sequenced chain** (absorb-the-one-unlifted-datum → coordinated hub-repoint of all interleaved anchors + 31 links → delete 3 files + SUMMARY/spec-index rows). On clean completion the slice corpus reaches **3 → 0** and the campaign completes (flag the `annotated-and-retained` carve-out + skill `phase-1-slice-reduction-audit` retirement for the batch-31 meta-phase). A second byte-disjoint low-fan-out dispatch (the c098-D3-intake bilinear-form-c095 residue sweep) runs in parallel as hygiene fill.

## Deliverable-presence verification

Per the MANDATORY pre-dispatch deliverable-presence discipline (paste-inline-evidence). All scopes here are **open by construction OR deletion-targets verified-present**; each row carries pasted command output.

**D1/D2 (the trio campaign) — slices verified PRESENT (deletion targets):**
```
$ ls book/src/spec/slices/cg.md book/src/spec/slices/gmres.md book/src/spec/slices/arnoldi_step.md
book/src/spec/slices/arnoldi_step.md
book/src/spec/slices/cg.md
book/src/spec/slices/gmres.md   (exit 0)
```
Sizes: cg 15139B, gmres 66313B, arnoldi_step 32092B. The campaign is OPEN: cycle-098 finalize explicitly recorded the trio as "the SOLE remaining P2 slice-deletion work and the clean c099 LEAD" (integrator-signals cycle-098 §3, line 36) and deferred it to c099. Not a re-proposal of landed work.

**Firm repoint-target homes verified PRESENT:**
```
$ ls book/src/L2/krylov-step.md book/src/L3/krylov-step.md book/src/L4/krylov-step.md \
     book/src/concepts/first-iteration-unrolling.md
book/src/L2/krylov-step.md
book/src/L3/krylov-step.md
book/src/L4/krylov-step.md
book/src/concepts/first-iteration-unrolling.md   (all exit 0)
```
`L3/krylov-step.md` frontmatter: `firmness: firm` (confirmed). `L2/krylov-step.md` + `L4/krylov-step.md` are firm (cycle-005/006). `concepts/first-iteration-unrolling.md` carries the ABSTRACT/generic rotation (generic `first_step`/`steady_step` signatures + driver shape + what-gets-hidden); it does NOT carry the CG-CONCRETE worked bodies — those are the genuinely-unlifted `cg.md:27-141` material.

**The one genuinely-unlifted datum verified on disk (`cg.md:27-141`):** `## L4 v0.5 — first-iteration unrolling (self-rotation)` at `cg.md:27`, carrying the concrete `cg_first_step`/`cg_steady_step` typed bodies (`type CgState<S>` with `beta_prev` dropped), the `forget_beta_prev` projection making the v0.4↔v0.5 equivalence formal, the `### Variant: pcg under v0.5`, and the `### Equivalence to v0.4` (`cg.md:120`) ratification. `L4/krylov-step.md:171` Form B EXPLICITLY names this as "the unique material RETAINED live in the reduced slice" and cites `cg.md:52` (`cg_first_step`), `cg.md:69` (`cg_steady_step`), `cg.md:95-108` (`iterate_while_with_prev` driver). So Form B is the natural absorb home (it already references it abstractly). VERDICT: NOT a no-op absorb — this material MUST land a firm home before `cg` deletes (absorb-before-delete discipline; integrator-signals c098 line 37).

**Inbound markdown-link inventory (linkcheck2-breaking on delete) — pasted counts:**
```
cg     link count: 12
gmres  link count: 17
arnoldi link count: 2     (total 31 links across ~18 files)
```
Link-bearing files (Class A): `concepts/{apply_BA,apply_linop,axpy,constructed-operators,derived-view-hoisting,dot,first-iteration-unrolling,gmres,nrm2,orthogonalization,plane-rotation-stream,scal,solve-monad,state-stratification,variant-absorption}.md` + `L0/ksp-factory-file.md` + `spec/index.md` + `SUMMARY.md`.

**Plain-text evidence-citation mention files (Class B; NOT links — won't break build, but are second-source-of-truth refs to repoint):** `L2/krylov-step.md` (`:7,:69,:119,:138,:139,:141,:146,:147`), `L3/krylov-step.md` (`:93,:123,:158,:188,:189,:196,:197,:199,:204,:205`), `L4/krylov-step.md` (`:171,:172,:173`), `L4-L3/{krylov-step-typed-wrapper-dissolution,iterate-while-dissolution,gmres-inner-loop-iterate-while-migration,fgmres-inner-loop-iterate-while-migration,iterate-while-with-prev-dissolution}.md`, `L3-L2/krylov-step-body-identity.md`, `L1/orthogonalize.md:306`, `L1-L0/minres-iteration.md:144`, `L3/apply_linop.md`, `L4/iterate-while.md`, `L4/iterate-while-with-prev.md`, `L2/index.md`.

**OUT OF SCOPE (verified, do NOT touch):** `book/src/meta-reviews/2026-05-24-cycles-19-21.md:11` — references `book/src/spec/slices/gmres.md` as a **historical-narration prose mention** ("Cycle 20 created book/src/spec/slices/gmres.md (180 lines...)"), in a code-span, NOT a markdown link `](...)`. Per the "meta-reviews/ are frozen historical records + git history is the record" convention, the statement "cycle 20 created X" stays true after X is deleted. It is not a link → will NOT break linkcheck2. Explicitly excluded from the repoint set.

**GC / rank baseline (pasted) — confirms delete is graph-clean:**
```
$ python3 tools/graded-stack-lint/graded_stack_lint.py --book-src book/src
RANK VIOLATIONS: none.
RESULT: 0 rank violation(s), 172 detritus node(s), 146 untyped (warning).
```
No frontmatter `depends-on`/`reference` edge points INTO any slice (verified: all slice references are plain-text evidence citations + consumer-list markdown links, none in frontmatter edge syntax). The firm trio's frontmatter carries NO slice dep. Therefore deleting the 3 slices removes NO graph edge, loses NO firm node's reachability, and keeps rank_violations at 0. The completion criterion is met **mechanically by deletion + link-repoint**.

**arnoldi_step end-bound citation-divergence — RESOLVED via codemap (pasted resolution):** the still-open divergence is `:73-109` (arnoldi_step slice §35) vs `:73-118` (`L2-L1/incremental-least-squares-composition-lowering.md:112`) vs `:73-120` (the now-DELETED polynomial slice). Codemap `read_range palace/linalg/iterative.cpp:71-122`: the real-form `template<typename T> GeneratePlaneRotation(const T dx, ...)` has its `template` line at `:71`, signature at `:72`, body closing `}` at `:109`; the COMPLEX specialization's `template` line is at `:110`, signature `:111-112`. **Canonical real-form range = `:73-109` (sig-to-close) — the `:73-118`/`:73-120` end-bounds wrongly over-run INTO the complex specialization (`:111+`).** The one LIVE wrong citation to reconcile is `L2-L1/incremental-least-squares-composition-lowering.md:112` (`:73-118` → `:73-109`); the polynomial `:73-120` is already gone. Folded into D2's repoint pass (touches the same composition-lowering theme region). Closes OQ `plane-rotation-givens-l0-citation-range-reconcile` end-bound residue (c097-D4 critic Issue 3).

**D3 (residue sweep) — open by construction** (a fresh whole-book grep hygiene pass surfaced as c098-D3 intake / integrator-signals c098 line 54; no prior-cycle deliverable). Skip-justified.

## Dispatches

> **Single-coordinated-owner constraint.** D1 and D2 are the SAME owner-role's sequenced work on the shared krylov-hub fileset. They are dispatched as **two sequential waves of ONE agent** (NOT two parallel agents) — D2 depends on D1 (the absorb must land before the repoint+delete so cg's unique material has a firm home the repoint can point at). This is the deliberate "one agent owning the shared krylov-hub files across sequential waves rather than parallel dispatches that would collide" shape the c098 guidance + the §wave-conflict warning mandate.

- **D1 — `harvester` — ABSORB the one genuinely-unlifted CG-concrete worked datum into a firm home.** Scope: transcribe the `cg.md:27-141` L4-v0.5 first-iteration-unrolling **CG-concrete worked instance** (the `cg_first_step` / `cg_steady_step` typed bodies with `beta_prev` dropped from the steady-state `CgState<S>` schema; the `forget_beta_prev` projection; the `### Equivalence to v0.4` ratification at `cg.md:120-133`; the `### Variant: pcg under v0.5` at `cg.md:131`) into `book/src/L4/krylov-step.md` as a worked-example subsection UNDER the already-present **Form B** §Semantics (`L4/krylov-step.md:28,:82` already name Form B abstractly and `:171` already flags this exact material as "the unique material RETAINED live in the reduced slice"). The abstract/generic rotation already lives in `concepts/first-iteration-unrolling.md` — D1 does NOT duplicate the generic signatures; it lands ONLY the CG-concrete bodies + the v0.4↔v0.5 equivalence as the worked example Form B references. Re-anchor the L0 evidence to the firm terminal home (`L1-L0/ksp-solve-mutation-rotation.md` Sub-pattern B, `iterative.cpp:360-486` / per-step for-loop `:427-464` — the v0.5 branch-unrolled form's L0 ground is the same CG body). Keep `status: firm` (firm-on-positive-structure: these are syntactic L4-self-rotation identities on a fully-specified read closure — the firm-on-positive-structure escape, like the existing Form A/Form B law confidence). **Verify-present, paste-inline-evidence:** before transcribing, read `cg.md:27-141` on disk + `L4/krylov-step.md:152-173` §Status/§Evidence to confirm the Form-B home + cite the exact source ranges. Output: proposed-changes to `L4/krylov-step.md` ONLY (the absorb); flag in Open questions that the slice `cg.md:27-141` is now fully homed and clear-to-delete (consumed by D2). deps: none.

- **D2 — `harvester` (SAME owner as D1, sequential) — coordinated hub-repoint of ALL interleaved anchors + 31 links, then DELETE the 3 slices + SUMMARY/spec-index rows.** Scope, in order:
  1. **Repoint Class-B plain-text evidence-citation mentions** in the firm krylov-step hub + themes so they cite ONLY firm homes (drop the `book/src/spec/slices/cg|gmres|arnoldi_step.md:NNN` "remains live in the reduced slice" parentheticals — the material is now fully homed by D1's absorb + the existing firm §Evidence anchors). Files: `L2/krylov-step.md` (`:7` — surgically DROP the three slice-range clauses for cg/gmres/arnoldi leaving the firm-home clauses; `:69,:119,:138,:139,:141,:146,:147`), `L3/krylov-step.md` (`:93,:123,:158,:188,:189,:196,:197,:199,:204,:205`), `L4/krylov-step.md` (`:171-173` — now point at D1's absorbed worked example, NOT the slice), `L4-L3/{krylov-step-typed-wrapper-dissolution,iterate-while-dissolution,gmres-inner-loop-iterate-while-migration,fgmres-inner-loop-iterate-while-migration,iterate-while-with-prev-dissolution}.md`, `L3-L2/krylov-step-body-identity.md`, `L1/orthogonalize.md:306`, `L1-L0/minres-iteration.md:144`, `L3/apply_linop.md`, `L4/iterate-while.md`, `L4/iterate-while-with-prev.md`, `L2/index.md`. For each: the slice content is ALREADY firm-homed (D1 closes the last gap) — repoint to the firm home, drop the slice line-range.
  2. **Repoint the 31 Class-A markdown links** off the deleted slices to firm homes (the c097/c098 precedent: orthog/polynomial bullets → firm `concepts/*` / firm operator). The `concepts/*` "Slices that use this primitive" consumer bullets (`- [cg](../spec/slices/cg.md)`, `- [gmres](...)` in `dot,axpy,apply_linop,nrm2,scal,dot,apply_BA,orthogonalization,variant-absorption,constructed-operators,derived-view-hoisting,first-iteration-unrolling,solve-monad,state-stratification,plane-rotation-stream,gmres.md`) → repoint to the firm CG/GMRES home `book/src/L2/krylov-step.md` (the firm operator naming CG/GMRES/Arnoldi as the canonical instances) OR delink to plain-text per the c097/c098 consumer-bullet precedent (producer judgment per bullet; prefer a live link to the firm home where one cleanly exists). `L0/ksp-factory-file.md:56` → repoint the `[spec/slices/cg]`/`[spec/slices/gmres]` anchors to the firm `L2/krylov-step.md` / `L1/ksp_solve.md` homes.
  3. **Reconcile the arnoldi_step end-bound divergence** while touching the composition-lowering region: `L2-L1/incremental-least-squares-composition-lowering.md:112` `iterative.cpp:73-118` → `:73-109` (codemap-confirmed canonical real-form `GeneratePlaneRotation` range; the `:73-118` over-runs into the complex specialization at `:111+`). Closes OQ `plane-rotation-givens-l0-citation-range-reconcile` end-bound residue.
  4. **DELETE the 3 slice files** `book/src/spec/slices/{cg,gmres,arnoldi_step}.md`.
  5. **Remove the SUMMARY.md + spec/index.md rows** (SOLE owner this cycle — single-index-owner discipline): `SUMMARY.md:292-294` (the 3 slice rows under `# Phase 1 corpus`); SINCE the corpus is now EMPTY, ALSO collapse the now-orphaned `# Phase 1 corpus` section + `- [Index — Slice Status](./spec/index.md)` row (SUMMARY:290-291) per producer judgment (verify `spec/index.md` has no remaining slice rows + decide whether `spec/index.md` itself should become a stub/be removed — it now indexes zero slices); `spec/index.md:15-17` (the 3 status-table rows). **MANDATORY producer-side inbound-link preflight** (the c098-D1-missed-8-links lesson, integrator-signals c098 line 61): before declaring done, run `grep -rnE '\]\([^)]*slices/(cg|gmres|arnoldi_step)\.md' book/src --include='*.md'` and confirm ZERO remaining inbound links (catch the full set producer-side, not via the critic safety-net). **Verify-present, paste-inline-evidence** for each link/mention before editing. Output: proposed-changes across the ~18 files + 3 file deletions + SUMMARY/spec-index row removals. deps: D1.

- **D3 — `lifter` (PARALLEL, byte-disjoint from D1/D2) — the bilinear-form-c095 residue sweep (low-fan-out hygiene).** Scope: `grep` the whole book for files co-mentioning `gram_reduce` + `rough-in` that carry c095-stale narration (the D3-flagged residue class from c098; integrator-signals c098 line 54) — other than the krylov-hub files D2 owns. The c095 bilinear-form cascade firmed `gram_reduce`/the 4 columns; any remaining "stays rough-in because bilinear-form" narration in a NON-krylov file is stale. Re-anchor to post-cascade reality. **Verify-present, paste-inline-evidence** the grep results before editing; if the grep returns ZERO non-krylov-hub residue (the c098-D3 `:313` fix may have been the last one), report clean-no-op (acceptable — a verified-no-op is a valid land). **Byte-disjoint guard:** D3 must NOT touch any file in D2's repoint set (the krylov hub + themes + concepts consumer-bullets + SUMMARY/spec-index). deps: none.

## Overlap analysis

- **D1 ∩ D2:** OVERLAPPING by design — both touch `L4/krylov-step.md` (D1 absorbs the Form-B worked example; D2 repoints `:171-173` to that absorbed example). Resolved by SEQUENCING (D2 deps D1): D2 cannot repoint to the absorbed material until it exists. This is the deliberate single-owner-across-sequential-waves shape (same `harvester` role; D2 reads D1's landed absorb). NOT parallel.
- **D1/D2 ∩ D3:** NON-overlapping (byte-disjoint). D1/D2 own the krylov-hub fileset (krylov-step trio + themes + `concepts/{dot,axpy,apply_linop,...}` consumer-bullets + SUMMARY/spec-index + the slices). D3 owns NON-krylov `gram_reduce`+`rough-in` residue files only, with an explicit guard to not touch any D2 file. The `gram_reduce` residue and the krylov-step files are disjoint regions (different operators, different chapters). Shared-file risk: `L2/index.md` is touched by D2 (slice-mention repoint at `:135`-region) — D3 must NOT touch `L2/index.md` (added to D3's byte-disjoint exclusion). With that guard: no overlap.
- **No shared consolidated-tally / dual-registration concern:** D2 is the SOLE owner of all SUMMARY.md + spec/index.md row removals (no co-landing producer writes those). No layer-index consolidated-count is bumped this cycle (deletions only; the firm-count headers are unaffected — slices were never in the firm counts). The parallel-blind-shared-index guard does not bind (only one writer of each shared index this cycle).

## Sequencing schedule

- **Wave 1 (parallel):** D1 (`harvester`, absorb the cg.md:27-141 worked datum into L4/krylov-step Form B) + D3 (`lifter`, byte-disjoint residue sweep).
- **Wave 2 (after D1 report lands):** D2 (`harvester`, SAME owner as D1 — coordinated hub-repoint of all Class-A links + Class-B mentions + arnoldi end-bound reconcile + DELETE 3 slices + remove SUMMARY/spec-index rows). D2 deps D1 (the per-report integrator wires D2's `:171-173` repoint to D1's landed absorbed worked example; D2's repoint of the Class-B "remains live in the slice" parentheticals is valid only once D1 has homed the material).

Three dispatches, two waves. Well under the ≤12 ceiling. (Critic + repairer + integrator-per-report ×N + ONE integrator-finalize follow per the standard pipeline; the §inbound-link-completeness safety-net is the critic's backstop to D2's mandated producer-side preflight.)

## Open questions / caveats

- **Single-cycle full-completion vs clean-partial.** My on-disk audit judges the FULL trio completion (absorb + repoint + delete, corpus 3→0) achievable in ONE cycle as a sequenced single-owner chain, because (a) the GC graph is unaffected (zero typed edges into slices — pasted-evidence confirmed), (b) rank stays 0, (c) the only genuinely-unlifted datum is ONE bounded worked instance (`cg.md:27-141`) with an obvious pre-named home (`L4/krylov-step.md` Form B). The c096/c097/c098 planners deferred it as "multi-dispatch" largely because they were batch-positioned to defer (c096 was batch-closing; c097/c098 had independent low-coupling survivors to clear first), and the corpus has since shrunk to exactly the trio — the residual is now bounded. **However:** D2 is a LARGE single dispatch (~18 files, 31 links + ~16 mention-files + 3 deletions + SUMMARY/spec-index surgery). If the D2 producer or critic judges the repoint set too large to land clean in one pass, a **clean PARTIAL is acceptable** (D1 absorb + the Class-B mention repoint + arnoldi reconcile land this cycle; the Class-A link-repoint + file-deletion defer to a tight batch-32 follow): the batch-31 meta-phase can re-lead the residue. **Prefer a clean partial over an unclean full completion** (the explicit task directive). The completion criterion (`spec/slices/*` fully unreachable + rank invariant holds zero slice nodes) is only met when ALL 3 files are gone + ALL inbound links repointed — a partial does NOT trigger the carve-out retirement.

- **On FULL completion this cycle → flag for integrator-finalize / batch-31 meta-phase:** retire the `annotated-and-retained` slice carve-out (CLAUDE.md §Methodology-invariants "Phase 1 corpus reduces") + the skill `phase-1-slice-reduction-audit` (per METHODOLOGY-GRADED-STACK.md §6/§7; integrator-signals c098 line 38). These are `.claude/agents/` + `skills/` + CLAUDE.md write-authority = a batch-31 META-PHASE enactment, NOT a c099 dispatch. The c099 integrator-finalize should RECORD the trigger in integrator-signals; the batch-31 meta-phase enacts. **Verify before flagging:** the carve-out's whole purpose (retain a slice as a ≥2-concept-page §Canonical-instance referent) is moot once the corpus is empty — confirm zero slices remain `annotated-and-retained` (the trio are `reduced`, not retained-by-design, so this should be clean).

- **`spec/index.md` disposition (D2 producer judgment).** Once the 3 slice rows are removed, `spec/index.md` indexes ZERO slices and the `# Phase 1 corpus` SUMMARY section is empty. D2 should DECIDE (and the critic verify): either (a) remove `spec/index.md` + the `# Phase 1 corpus` SUMMARY section entirely (cleanest — the corpus is gone), or (b) reduce `spec/index.md` to a one-line "corpus fully lifted; see git history" stub retained for navigation. I lean (a) (the corpus-empty state is the campaign's whole point; a stub index of nothing is detritus), but flag it as a producer/critic decision since it touches the SUMMARY top-level Part structure. Whichever: linkcheck2 must stay green (no dangling `./spec/index.md` link).

- **`non-book-artifact-orphan-review` user directive is a META-PHASE task, NOT a c099 dispatch** (per the task framing + open-questions tail). Explicitly NOT scheduled this cycle; the batch-31 meta-phase owns it. Noting here so the next reader does not mistake it for unscheduled c099 work.

- **For the batch-31 meta-phase (surfaced, not a c099 dispatch):** the citecheck bare-basename-vs-`spec/`-prefix path-resolution false-positive (integrator-signals c098 line 62) recurs every slice-touching cycle and will fire on D2's deletion reports (expected-MISS on the slices being deleted + AMBIG on bare `gmres.md` matching both `concepts/` + `spec/slices/`). Benign, but a citecheck path-resolution hardening candidate — and the LAST slice-touching cycle, so the recurrence ends here (the trio is the final delete). One-line note for the §Intake→plan.
