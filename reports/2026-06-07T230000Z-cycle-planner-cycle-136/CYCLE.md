---
agent: cycle-planner
invoked_at: 2026-06-07T230000Z
scope: cycle-136 dispatch plan
status: pending
---

# Cycle 136 dispatch plan

## Goals selected this cycle

c136 is the OPENER (1/3) of meta-batch-44. The batch-44 LEAD is the NEW USER DIRECTIVE **BUILD THE SYNTHESIS SECTION** (`priorities.md` CYCLE-136 head; CLAUDE.md §"The SYNTHESIS section"; memory `project_synthesis_section_directive`) — a new top-level `# Synthesis` Part rendering the spec's L4 surface as a synthesized implementation library. The maintenance floor is now the SURROUND on the new per-batch cadence (item 0: full-hygiene sweep is once-per-BATCH, not per-cycle). This OPENER does the structural-foundation move the directive's LEAD-sequencing prescribes: **(a) lay the `# Synthesis` Part shell + SUMMARY wiring + the 5 per-library chapter intros, and (b) author the foundational `types` library** (layer-intro-author), since the per-operator synthesized defs (abstractor/harvester) DEPEND on the library chapters existing. Then a Wave-2 begins rendering the 3 calculus libraries' synthesized defs (`iteration`/`data-algebra`/`coordination`), each into its own chapter, deferring the `drivers` library (which composes everything) to a later batch-44 cycle. Plus the one per-batch maintenance-floor hygiene cross-cutter (the batch-44 per-batch sweep, placed in this opener).

## Deliverable-presence verification

Per the MANDATORY pre-dispatch four-step check (paste-inline evidence). The Synthesis section is NEW — almost everything is **open by construction** (nothing under `book/src/synthesis/` exists); the maintenance dispatch is open-by-construction (standing audit). Evidence:

- **`book/src/synthesis/` (the whole Part) — DOES NOT EXIST → all Synthesis dispatches are OPEN BY CONSTRUCTION:**
  `ls book/src/synthesis/` → `ls: cannot access 'book/src/synthesis/': No such file or directory`.
  `grep -nc 'synthesis' book/src/SUMMARY.md` → no `# Synthesis` Part in SUMMARY (top order confirmed: line 4 `# Methodology` → line 10 `# Feature surfaces` → line 61 `# Semantic surface` → line 63 `# L4`). The directive's placement point (immediately BEFORE `# Feature surfaces`, i.e. between SUMMARY line 9 and line 10) is empty.
  → D1 (shell + intros + `types`), D2/D3/D4 (per-library defs) are all OPEN BY CONSTRUCTION (no prior-cycle history, no on-disk file, no RESOLVED OQ).
- **D5 (maintenance-floor cross-cutter) — OPEN BY CONSTRUCTION** (the standing per-batch maintenance-floor audit; item 0/item 1; audit-class, produces an observation CYCLE.md, no named target file). Per item 0's new cadence this is the ONE per-batch dedicated maintenance dispatch, placed in the batch opener.
- **STOP-PROPOSING negative-list check:** none of the Synthesis dispatches names a disqualified L3-backfill slug (`lu_solve`/`back_solve`/`ls-update-column`/the `nleps_*` set). Synthesis RENDERS the existing firm L4 surface as library code — it is NOT a vocabulary pull-up (the redirect's no-forced-rectangular-pull-up governs and is honored: no new operator algebra, no new floors).
- **Structural-block check:** no methodology gate blocks Synthesis — it is the human-directed batch-44 LEAD; the directive lives in CLAUDE.md §SYNTHESIS + priorities.md item (the role-specs are NOT yet updated, so each dispatch scope below QUOTES the load-bearing directive bullets). DIRECTIVE-1 boundary (MPI/sharding OUT) is not touched (Synthesis renders single-machine vocabulary). Kernel-API/impl integrity preserved (opaque kernels render as `#extern NAME`, not lifted).

## L4 doc-group → library partition (grounding for D2/D3/D4)

Confirmed on disk (`book/src/SUMMARY.md:65-91`); the 3 calculus libraries mirror the 3 L4 doc-groups exactly:
- **`iteration`** ← "Iteration & step combinators" (4 ops): `chebyshev`, `iterate-while`, `iterate-while-with-prev`, `krylov-step`.
- **`data-algebra`** ← "Data-algebra combinators & named verbs" (14 ops): `assemble_frequency_operator`, `domain_energy_reduce`, `dot`, `eigenfreq_qfactor_reduce`, `eliminate_bc`, `fe_assemble`, `gram_reduce`, `inner_product`, `linear_combination`, `mk_matrix_free_operator`, `nrm2`, `sharding-decompose-reduce` (rank-0 roadmap_goal — render as a future-direction stub note, not a firm def), `sparameter_reduce`, `waveguide_mode_reduce`.
- **`coordination`** ← "Outer-driver caps & coordination combinators" (6 ops): `eigsolve`, `fold_solve`, `frequency_sweep`, `ksp_solve`, `preconditioning-framework`, `solve_family`.
- **`drivers`** ← lifted from the `# Feature surfaces` Part (5 sim drivers + lifecycle ROOT + output products) — composes the calculus libraries; **DEFERRED to a later batch-44 cycle** (it composes everything, must come last per the directive's LEAD-sequencing (d)).
- **`types`** ← the genuinely SHARED / cross-cutting record/type defs only (config-record, per-solver state carriers, L4 record types). The 59-page `book/src/concepts/` library is the authoritative home for shared record defs (semantic-consolidation "define once") — the `types` library LINKS to those + renders the synthesized type-def form; per the type-placement rule, a type that CLUSTERS with one API group does NOT go here (it goes immediately before that group, bundled with its utility API, in D2/D3/D4).

## Dispatches

1. **agent: `layer-intro-author`**
   **scope: `synthesis-section-shell` — author the `# Synthesis` Part shell + SUMMARY wiring + the 5 per-library chapter intros (as `stub`/`roadmap_goal` shells) + the foundational `types` library.**
   Create `book/src/synthesis/index.md` (the Part overview: states this is the synthesized-library IMPLEMENTATION VIEW rendered in the L4 pseudo-language — Haskell + TS, fenced ```text; KaTeX `$`-sigil-must-be-fenced rule applies; a DISTINCT chapter KIND that LINKS to L4/semantics/concepts for authoritative defs and renders the synthesized code form, NOT a semantic restatement). Wire the new `# Synthesis` top-level Part into `book/src/SUMMARY.md` **immediately BEFORE `# Feature surfaces`** (between SUMMARY line 9 `methodology/semantic-consolidation.md` and line 10 `# Feature surfaces — entry points`). Seed the **5 library chapters** as intro shells (`book/src/synthesis/{types,iteration,data-algebra,coordination,drivers}.md`), in the directive's order `types` → `iteration` → `data-algebra` → `coordination` → `drivers`; each intro states the library's scope, that its defs are in TOPOLOGICAL order, the `#extern NAME`-after-type-signature convention for opaque kernels (libCEED quadrature, SLEPc eigsolve, triangular-solve/GS-SSOR — the existing kernel-API nodes), the deep-link-unchanged-lower-artifacts-rendered-INLINE policy, and the Haskell-`where`-clause-for-private-helpers convention. Then AUTHOR the **`types`** library body holding ONLY the genuinely SHARED / cross-cutting record/type defs (the type-placement rule: types that cluster with one API group are NOT here — they go before their group in D2/D3/D4; only types shared across ≥2 API groups live here, ahead of the groups that use them). LINK each rendered type to its authoritative `book/src/concepts/<record>.md` home (semantic-consolidation: define once; render the synthesized type-def form + code-doc I/O block, do NOT restate the authoritative def). Mark the per-library def-rendering chapters (`iteration`/`data-algebra`/`coordination`) `status: stub` (D2/D3/D4 fill them this cycle); `drivers` `status: stub` (deferred). **DIRECTIVE QUOTES the agent must carry (role-spec not yet updated):** CLAUDE.md §"The SYNTHESIS section" (the 5-library seed; type-placement; `#extern`; deep-link-inline; topological; `where`; implementation-VIEW-links-not-restates) + the §"SEMANTIC CONSOLIDATION" USE+LINK-don't-restate discipline + the `$`-sigil-must-be-fenced rule.
   **deps: none (open by construction).**
   **rationale:** the directive's LEAD-sequencing steps (a)+(b) — the shell + SUMMARY wiring + per-library intros + `types` library must exist before any per-operator synthesized def can land into a library chapter. fan-out: HIGH (unblocks the entire Synthesis section; the shared substrate — `#extern` set, shared type defs, type-placement + deep-link policy — is established once here). Plan-tag `synthesis-section`.

2. **agent: `abstractor`**
   **scope: `synthesis-iteration-library-defs` — render the `iteration` library's synthesized def bodies (the 4 Iteration & step combinators) into `book/src/synthesis/iteration.md`.**
   Render concrete def bodies in the L4 pseudo-language for `chebyshev`, `iterate-while`, `iterate-while-with-prev`, `krylov-step`, in TOPOLOGICAL order (a def appears after everything it uses), each with a code-doc section (per-def docstring: explicit I/O sets, named shape contracts, the bunsen `# Arguments`/`# Returns` style). Per the type-placement rule, place the group's clustering type (e.g. `CgState`/the iteration step-state carrier) immediately BEFORE the group, bundled with its UTILITY API (constructors/accessors/predicates), with consumer methods staying in the group AFTER the type+utility block. Render deep-linked unchanged lower-level artifacts INLINE where an L4 form is identity-in-form to its L3/L2/L1 version (it IS the implementation). Use `#extern NAME` after the type signature for any opaque-kernel boundary. LINK each def to its authoritative `book/src/L4/<op>.md` chapter (implementation-VIEW; the correspondence is reviewable by `lowering-verifier`). Flip `iteration.md` `status: stub` → the rendered library state. **DIRECTIVE QUOTES:** same CLAUDE.md §SYNTHESIS bullets as D1.
   **deps: 1 (the `iteration.md` chapter + Part shell + `types` must exist first).**
   **rationale:** directive LEAD-sequencing step (c) for the iteration group (smallest group, 4 ops — clean first def-rendering). fan-out: MEDIUM (part of the HIGH Synthesis deliverable). Plan-tag `synthesis-section`.

3. **agent: `abstractor`**
   **scope: `synthesis-data-algebra-library-defs` — render the `data-algebra` library's synthesized def bodies (the 14 data-algebra combinators & named verbs) into `book/src/synthesis/data-algebra.md`.**
   Render concrete def bodies in the L4 pseudo-language for the 14 ops (`assemble_frequency_operator`, `domain_energy_reduce`, `dot`, `eigenfreq_qfactor_reduce`, `eliminate_bc`, `fe_assemble`, `gram_reduce`, `inner_product`, `linear_combination`, `mk_matrix_free_operator`, `nrm2`, `sparameter_reduce`, `waveguide_mode_reduce`), in TOPOLOGICAL order, each with a code-doc section. **`sharding-decompose-reduce` is rank-0 `roadmap_goal`** — render it as a future-direction stub note (claim-free placeholder; do NOT manufacture a firm def for it), preserving the DIRECTIVE-1 boundary (MPI mechanism cited-not-lifted). `fe_assemble` / `mk_matrix_free_operator`: the libCEED element-quadrature kernel renders as `#extern` at the kernel-API boundary (the `libceed-quadrature-kernel-impl` / `fe-assemble-libceed-boundary-obstruction` nodes — DO NOT lift the opaque kernel; the matrix-free impl is the constructive node, render its from-our-primitives form inline where firm). Type-placement rule for any clustering type; deep-link-unchanged-inline; LINK each def to its `book/src/L4/<op>.md` chapter. Flip `data-algebra.md` `status: stub` → rendered.
   **deps: 1.**
   **rationale:** directive LEAD-sequencing step (c) for the data-algebra group (the largest group). fan-out: MEDIUM-HIGH (the bulk of the calculus surface). Plan-tag `synthesis-section`.

4. **agent: `harvester`**
   **scope: `synthesis-coordination-library-defs` — render the `coordination` library's synthesized def bodies (the 6 outer-driver caps & coordination combinators) into `book/src/synthesis/coordination.md`.**
   Render concrete def bodies in the L4 pseudo-language for `eigsolve`, `fold_solve`, `frequency_sweep`, `ksp_solve`, `preconditioning-framework`, `solve_family`, in TOPOLOGICAL order, each with a code-doc section. **`eigsolve`: the SLEPc EPS eigsolve loop renders as `#extern` at the kernel-API boundary** (the `eigsolve-impl` constructive node — Lanczos/Arnoldi/Krylov-Schur in our `lanczos_step`/`krylov-step` vocabulary — renders inline where firm; the opaque loop is `#extern`). Per the type-placement rule, place the coordination clustering types (`EigState`/`SimState` — the per-solver state carriers) immediately BEFORE the group, bundled with their UTILITY API; consumer methods stay in the group. Deep-link-unchanged-inline; LINK each def to its `book/src/L4/<op>.md` chapter. Flip `coordination.md` `status: stub` → rendered.
   **deps: 1.**
   **rationale:** directive LEAD-sequencing step (c) for the coordination group. Routed to `harvester` (vs abstractor) to balance the 3 calculus-library def-rendering dispatches across producers and because the coordination caps are the most operator-formalization-shaped (per-solver state carriers + cap composition). fan-out: MEDIUM. Plan-tag `synthesis-section`.

5. **agent: `cross-layer-cross-cutter`**
   **scope: `maintenance-floor-standing-hygiene` — the ONE per-batch maintenance-floor hygiene sweep (batch-44, placed in the opener).**
   AUDIT (NO book mutation unless a one-line stale token in a DISJOINT file): (i) RE-set re-check — RE4 consumer-gated (premise should HOLD, no GMRES-variant column c136); the `sharding-decompose-reduce` §2g-extension member (solve-generalization-consumer-gated, no consumer in flight); RE11 §2g escalate-guard (confirm the batch-43-meta re-baselined counts HOLD: `files=386, typed=325, untyped=61, roots=45, reachable=163, reference_reachable=247, detritus=123, true_detritus=51, rank_violations=0, unresolved=0, promotion_frontier=11, roadmap_goal-bucket=4` — guard must NOT fire). (ii) kernel-API/impl integrity (the 3 `realizes-kernel-api` edges — `libceed-quadrature-kernel-impl`, `eigsolve-impl`, `multigrid-relaxation-smoother` — stay `reference`-class on disk; especially RELEVANT this cycle since the Synthesis `#extern` rendering of those same kernels lands in D2/D3/D4 — confirm the `#extern` callouts do NOT introduce a `depends-on` mis-typing). (iii) semantic-surface liveness (`semantics/index.md` path/anchor drift scan; no stale `design/l4_calculus.md` refs). (iv) DIRECTIVE-1 boundary spot-check (no MPI/`Par*`/RAP lift in any landed content). **NOTE the new `# Synthesis` Part nodes:** the Synthesis chapters are an implementation-VIEW that LINKS (reference-class) to L4/semantics/concepts — they should be reference-reachable navigational nodes, NOT new `depends-on` blocking edges; flag if any Synthesis chapter manufactures a `depends-on` edge that would move a firm node's rank/liveness. Deliverable: a clean-bill audit OR a flagged-residual list for the batch-44 meta. fan-out: LOW/hygiene.
   **deps: none (audit-class; reads the post-apply tree at integration via the per-report serialization — see overlap note).** Plan-tag `graded-stack-hygiene`.
   **rationale:** item 0's new cadence — the full-hygiene sweep runs once per BATCH (folded into the batch opener as the one dedicated maintenance-floor dispatch); the per-cycle floor is the existing `integrator-finalize` step-5b two-invariant tripwire (no dispatch). This is that one per-batch dispatch.

## Overlap analysis

- **D1 ↔ D2 / D1 ↔ D3 / D1 ↔ D4:** D1 CREATES the Part shell + all 5 library chapter files (incl. `iteration.md`/`data-algebra.md`/`coordination.md` as `stub` shells) + wires SUMMARY + authors `types`. D2/D3/D4 each AUTHOR the def body INTO one of those stub chapters (the same file region D1 seeds). This is a genuine same-file overlap → **SEQUENTIAL: D2/D3/D4 depend on D1** (D1 must land the chapter shell + SUMMARY wiring first so the per-report integrator wires live links and the def-rendering dispatch lands into an existing chapter). → Wave 2 after Wave 1.
- **D2 ↔ D3 ↔ D4:** each authors a DISJOINT chapter file (`iteration.md` vs `data-algebra.md` vs `coordination.md`); no shared file region, no shared operator-name authoring (each renders its own L4 group). They reference the shared `types` library + `#extern` set authored by D1, but only READ/LINK it (navigational), do not co-author it. → **PARALLEL** among themselves (within Wave 2).
- **D5 ↔ D1/D2/D3/D4:** D5 is AUDIT-class (reads the typed graph + re-runs the linter, produces an observation CYCLE.md; at most a one-line stale-token fix in a DISJOINT file). It does NOT author any Synthesis chapter. The only conceptual touch-point is the post-apply tree D5 audits — the per-report integrators serialize the Synthesis applies before D5's audit reads the tree (per-report serialization, NOT a dispatch-wave boundary; the book is NOT rebuilt between per-report applies, but D5 is observation-only so it does not need the rebuilt book). → **PARALLEL** with the Synthesis wave at the dispatch level (no shared write region); D5 may be placed in Wave 1 (it audits the held baseline + the standing RE/kernel/semantic state, which the Synthesis adds reference-class navigational nodes to, not blocking edges).
- **No consolidated-tally collision:** the Synthesis chapters are an implementation-VIEW (reference-class navigational nodes); they add NO firm-count to any layer index, and there is no shared running-count across D1-D4 (each owns its own chapter; D1 owns the `# Synthesis` SUMMARY block + the Part index). No count-ownership partition needed.
- **No floor→adjacent-entry re-anchor coupling:** Synthesis renders the EXISTING L4 surface; no lower-layer floor lands under a firm parent.
- **Cross-report forward-reference slugs (stated explicitly in BOTH scopes to avoid divergence):** D2 → `book/src/synthesis/iteration.md`; D3 → `book/src/synthesis/data-algebra.md`; D4 → `book/src/synthesis/coordination.md`; the `types` library → `book/src/synthesis/types.md`; the `drivers` library → `book/src/synthesis/drivers.md` (D1-seeded stub, deferred). D1 authors all 5 chapter files at these canonical slugs; D2/D3/D4 each land into the file D1 created at the stated slug. The Part index/overview is `book/src/synthesis/index.md` (D1-owned).

## Sequencing schedule

- **Wave 1 (parallel): D1 + D5.**
  - D1 (`layer-intro-author`, `synthesis-section-shell`) — the Part shell + SUMMARY wiring + 5 library intros + `types` library.
  - D5 (`cross-layer-cross-cutter`, `maintenance-floor-standing-hygiene`) — the per-batch maintenance sweep (audit-class, disjoint).
- **Wave 2 (parallel, after Wave-1 reports land): D2 + D3 + D4.**
  - D2 (`abstractor`, `synthesis-iteration-library-defs`) — into `synthesis/iteration.md`.
  - D3 (`abstractor`, `synthesis-data-algebra-library-defs`) — into `synthesis/data-algebra.md`.
  - D4 (`harvester`, `synthesis-coordination-library-defs`) — into `synthesis/coordination.md`.
- The `drivers` library (directive step (d)) and any refinement of the seed 5-library modularization are DEFERRED to a later batch-44 cycle (drivers compose everything, must come last).
- ONE `integrator-finalize` at cycle end (rebuild book + commit + push). Per-report integrators serialize the applies (D1 before D2/D3/D4 so the def-rendering dispatches land into the existing chapter shells and live links resolve).

## Open questions / caveats

- **Wave-2 same-cycle vs next-cycle judgment.** I am scheduling D2/D3/D4 in Wave 2 THIS cycle (per the OPENER guidance "defs can begin once intros exist — some can be same-cycle if non-overlapping"). The risk: D2/D3/D4 are authored against the D1 shell that has not yet landed on disk at dispatch time — they author from the dispatch scope's description of the shell + the on-disk L4 chapters (which DO exist). This is the standard wave-2 forward-reference pattern (the per-report integrator wires live links after D1 lands). If the orchestrator judges the shell's exact intro conventions (type-placement spelling, `#extern` format) too underspecified for D2/D3/D4 to author cleanly before seeing D1's landed intros, it may DEFER D2/D3/D4 to c137 and run c136 as shell-only (D1 + D5). I lean toward dispatching all 5 this cycle (the directive's conventions are fully specified in CLAUDE.md §SYNTHESIS, which each scope quotes), but flag this as the orchestrator's call.
- **Role-specs not yet updated for Synthesis (batch-44 meta job).** The `layer-intro-author`/`abstractor`/`harvester`/`lowering-verifier` role-specs do NOT yet carry the Synthesis discipline (that lands at the batch-44 meta-phase after c138). For THIS batch the directive lives in CLAUDE.md §SYNTHESIS + priorities.md; every dispatch scope above QUOTES the load-bearing bullets so each agent has them in-context. Flag for the batch-44 meta: codify the Synthesis chapter-KIND mechanics into the role-specs.
- **`types` library scope boundary (type-placement rule).** The split between "shared cross-cutting types → `types` library" and "clustering types → before their API group bundled with utility API" is a judgment call D1 (for shared) and D2/D3/D4 (for clustering) make per-type. If a type's home is ambiguous, the producer should flag it rather than duplicate it (semantic-consolidation: define once). The 59-page `book/src/concepts/` library is the authoritative record-def home; Synthesis LINKS + renders the synthesized form.
- **`drivers` library deferred.** Per the directive's LEAD-sequencing (d), `drivers` composes everything and comes LAST; D1 seeds it as a `stub`. A later batch-44 cycle authors its body (per-driver config records cluster before their column with the config's utility API). This is a multi-cycle arc, as the directive anticipates.
- **Maintenance-floor cadence (item 0) — first application.** This is the FIRST batch under the new per-batch maintenance cadence. D5 is the one dedicated per-batch maintenance dispatch, placed in the batch opener. If the batch-44 meta prefers to fold the full sweep into its own standing duties instead, c137/c138 carry no maintenance dispatch (the per-cycle step-5b tripwire suffices). Flag for the meta.
