---
agent: layer-intro-author
invoked_at: 2026-06-01T063231Z
scope: cycle-042 D11 — sole-owner consolidated-tally refresh of three indices (L2, L2-L1, L3-L2) for the standalone-floor cohort (5 L2 floors + 10 themes)
status: pending
integrated_at: 2026-06-01T081245Z
integration_commit: PLACEHOLDER_SHA
integration_notes: "cycle-042 batch integration (foundation-first L2-floor build); applied clean; see reports/2026-06-01T081245Z-integrator-finalize-cycle-42/CYCLE.md + cycle-042 STAGING row."
---

# CYCLE: L2 / L2-L1 / L3-L2 index refresh (cycle-042 D11 — sole count-owner)

## Summary

I am the **sole count-owner** for cycle-042 of THREE shared consolidated indices. The 5 L2-floor
harvesters (D2–D6) and 5 theme abstractors (D7–D10) each appended **only their own** dep-map /
theme-list rows and **deferred the running-count tally to me** (per the c039 count-ownership
convention; confirmed in each report's §Open-questions). This report refreshes the **narrative
sections I own** — the §Vocabulary-cohort firm lists, the §Working-Notes consolidated tallies, and
the cohort-motif prose — to fold in the cycle-042 landings. I do **NOT** re-emit the per-operator
dep-map / theme-list rows (those are D2–D10's edits, already landed via per-report integration
ordering — re-emitting would double-apply).

Consolidated tallies (verified row-by-row against on-disk tables + the 10 dispatch reports):

| Index | Firm before | Firm after | partly-constructive | Net rows after |
|---|---|---|---|---|
| `book/src/L2/index.md` (dep-map) | 12 | **17** (+5) | 1 (`deflate`, unchanged) | 18 |
| `book/src/L2-L1/index.md` (theme-list) | 10 | **15** (+5) | 1 (`deflate-composition-lowering`, unchanged) | 16 |
| `book/src/L3-L2/index.md` (theme-list) | 5 | **10** (+5) | 0 | 10 |

The 5 L2 floors (all `firm`): `reciprocal` (D2), `elementwise_product` (D3), `assemble-diagonal`
(D4), `jacobi-smoother` (D5), `divfree-projector` (D6). The 10 themes (all `firm`): the 5
`-leaf-identity` (L2>L1) + 5 `-body-identity` (L3>L2) edges (D7–D10).

`l3-l2-rotation-theme-coverage-gap` advances **5-of-18 → 10-of-18**.

**Cohort framing (the load-bearing distinction this refresh introduces):** the cycle-042 cohort is
the **fork-INDEPENDENT standalone-floor cohort** — elementwise leaves (`reciprocal`,
`elementwise_product`) + constructed-operator / operator-to-data gates (`assemble-diagonal`,
`jacobi-smoother`, `divfree-projector`), **none of which has a fold-parent**. This is a *distinct
motif* from the cycle-041 fold-parented BLAS-1-leaf cohort (`dot`/`nrm2`/`scal`, which are
leaf-of / consumer-of / member-of the `inner_product` / `linear_combination` folds). Because they
have no fold-parent, the cycle-042 cohort is **design-final** — untouched by the batch-12 meta-phase
leaf-vs-fold fork (`dot-l2-leaf-floor-vs-fold-only-design`), which can only ever re-anchor
fold-parented leaves. `divfree-projector` is a **moderate** floor (one genuine `AddMult`
apply-accumulate de-fusion); the other four are thin identity-in-form floors.

## Proposed changes

### 1. `book/src/L2/index.md` — Vocabulary-cohort floor sub-list (add the standalone-floor cohort)

```edit:book/src/L2/index.md
[old]: *Identity-in-form BLAS-1 floors (cycle-041; present so the firm L3 cohort rests on adjacent same-named L2 parents; distinct from the fold-parents — do-NOT-merge):*

- `dot` — conjugation-axis **leaf-of** `inner_product` (the plain `M = I` Hermitian / symmetric member); thin identity-in-form floor, laws inherited unchanged from the L1 leaf. Firm cycle-041 (D1).
- `nrm2` — **consumer-of** `inner_product` (`√ ∘ abs ∘ inner_product` at `y=x`, NOT a fold member); the `std::abs` defensive guard preserved as an explicit load-bearing numerical claim. Firm cycle-041 (D2).
- `scal` — arity-1 **member-of** `linear_combination` (`scal(α,x) = linear_combination [(α,x)]`, NOT merged); firm-on-positive-structure (syntactic-identity laws on the small fully-present `operator*=` surface). Firm cycle-041 (D3).
[new]: *Identity-in-form BLAS-1 floors (cycle-041; FOLD-PARENTED — present so the firm L3 cohort rests on adjacent same-named L2 parents; distinct from the fold-parents — do-NOT-merge; **leaf-vs-fold realization under batch-12 meta-phase adjudication**):*

- `dot` — conjugation-axis **leaf-of** `inner_product` (the plain `M = I` Hermitian / symmetric member); thin identity-in-form floor, laws inherited unchanged from the L1 leaf. Firm cycle-041 (D1).
- `nrm2` — **consumer-of** `inner_product` (`√ ∘ abs ∘ inner_product` at `y=x`, NOT a fold member); the `std::abs` defensive guard preserved as an explicit load-bearing numerical claim. Firm cycle-041 (D2).
- `scal` — arity-1 **member-of** `linear_combination` (`scal(α,x) = linear_combination [(α,x)]`, NOT merged); firm-on-positive-structure (syntactic-identity laws on the small fully-present `operator*=` surface). Firm cycle-041 (D3).

*Fork-INDEPENDENT standalone floors (cycle-042; same-named L2 parents under firm L3 entries, like the cycle-041 floors, but with **NO fold-parent** — a distinct motif: each is a standalone elementwise leaf or constructed-operator / operator-to-data gate, NOT a member/leaf/consumer of `inner_product` / `linear_combination`. **Design-final** — the leaf-vs-fold fork cannot re-anchor a leaf that has no fold to fold into):*

- `reciprocal` — standalone elementwise multiplicative-inverse leaf (`y[i] = 1/x[i]`); **NO fold-parent** (a *nonlinear* self-map, `1/(a+b) ≠ 1/a + 1/b`). Thin identity-in-form floor, eight laws + single element-type axis inherited unchanged from L1. Firm cycle-042 (D2).
- `elementwise_product` — standalone Hadamard binary field op (`a ⊙ b`); **NO fold-parent** (a binary `(Tensor[N], Tensor[N]) -> Tensor[N]` op, neither reduce-to-`Scalar` nor reduce-to-`Tensor[N]`; the inverse-subsumption generalisation of `scal`). Thin identity-in-form floor, ten laws + element-type / conjugation axes. Firm cycle-042 (D3).
- `assemble-diagonal` — standalone operator-to-data diagonal-extraction primitive (`A → diag(A)`); **NO fold-parent**; the operator-to-data sibling of `apply_linop` (NOT an `apply_linop` variant — operator/data divide load-bearing). Thin floor, **load-bearing matrix-free high-order-Nedelec approximate-diagonal non-law preserved through the floor** (so `firm`, not `partly-constructive`). Firm cycle-042 (D4).
- `jacobi-smoother` — the **thinnest** constructed-operator gate (apply `op.dinv ⊙ x = (ω·D⁻¹) ⊙ x`, one elementwise product); **NO fold-parent**; the fusion rotation is **negative** (no fused multi-operation kernel to unfold, unlike `chebyshev-iteration`). Degree-zero member of the `chebyshev-iteration` recurrence (cited, not dep). Firm cycle-042 (D5).
- `divfree-projector` — the divergence-free Helmholtz-projection constructed-operator gate (fixed four-step `WeakDiv → Z_{bdr_eff} → ksp_solve → Grad`); **NO fold-parent**; a **moderate** floor — one genuine `AddMult` apply-accumulate de-fusion (step-4 `Grad->AddMult` → `apply_linop ▷ axpy`), all else identity-in-form; direct L2 dep `ksp_solve` (the inner projected solve carrying its `sequential-obstruction` **by reference**). Firm cycle-042 (D6).
```

### 2. `book/src/L2/index.md` — Semantics-overlay motif list (add the standalone-floor motif)

```edit:book/src/L2/index.md
[old]: - **Identity-in-form BLAS-1 floors** — same-named leaf entries for the most-reused BLAS-1 primitives, present so the firm L3 cohort rests on adjacent same-named L2 parents (per CLAUDE.md §Methodology invariants **Identity-lowerings still require both L levels**), under the 2026-05-31 foundation-first directive `l2-floor-under-l3-blas1-cohort`. Three landed cycle-041 (`dot`, `nrm2`, `scal`): each is value-thread-isomorphic to its firm L1 leaf (the fusion rotation is a no-op on the primitive — the L0 form is already the unfolded composition), with the fusion-rotation *framing* added in L2 vocabulary and any load-bearing numerical trick (the `nrm2` `std::abs` guard) preserved as an explicit algebraic claim. They are **distinct from the fold-parents** `inner_product` / `linear_combination`: `dot` is the conjugation-axis *leaf-of* `inner_product` (NOT merged); `scal` is the arity-1 *member-of* `linear_combination` (NOT merged); `nrm2` is a *consumer-of* `inner_product` (`√ ∘ abs ∘ inner_product` at `y=x`, NOT a fold member). The do-NOT-merge boundary (§"Fold-cohort boundary") is load-bearing for all three. **The leaf-vs-fold design fork for this floor cohort is under batch-12 meta-phase adjudication (see §Working Notes).**
[new]: - **Identity-in-form floors** — same-named L2 entries for the most-reused leaves / gates, present so the firm L3 cohort rests on adjacent same-named L2 parents (per CLAUDE.md §Methodology invariants **Identity-lowerings still require both L levels**), under the 2026-05-31 foundation-first directive `l2-floor-under-l3-blas1-cohort`. Each is value-thread-isomorphic to its firm L1 leaf/gate (the fusion rotation is a no-op or near-no-op on the primitive — the L0 form is already the unfolded composition), with the fusion-rotation *framing* added in L2 vocabulary and any load-bearing numerical trick preserved as an explicit algebraic claim. The floors split into **two motifs by fold-relationship**:
  - **Fold-parented floors** (cycle-041; `dot`, `nrm2`, `scal`) — same-named leaves of the L2 fold-parents `inner_product` / `linear_combination`: `dot` is the conjugation-axis *leaf-of* `inner_product` (NOT merged); `scal` is the arity-1 *member-of* `linear_combination` (NOT merged); `nrm2` is a *consumer-of* `inner_product` (`√ ∘ abs ∘ inner_product` at `y=x`, NOT a fold member; the `std::abs` guard preserved as an explicit algebraic claim). The do-NOT-merge boundary (§"Fold-cohort boundary") is load-bearing for all three. **Their leaf-vs-fold realization is under batch-12 meta-phase adjudication** (`dot-l2-leaf-floor-vs-fold-only-design`; see §Working Notes — the c042 cross-cutter audit recommends ratifying the leaf-floor reading (b)).
  - **Fork-INDEPENDENT standalone floors** (cycle-042; `reciprocal`, `elementwise_product`, `assemble-diagonal`, `jacobi-smoother`, `divfree-projector`) — same-named L2 parents under firm L3 entries with **NO fold-parent**: standalone elementwise leaves (`reciprocal` the nonlinear multiplicative-inverse self-map; `elementwise_product` the Hadamard binary op, the inverse-subsumption generalisation of `scal`) and constructed-operator / operator-to-data gates (`assemble-diagonal` the operator-to-data sibling of `apply_linop`; `jacobi-smoother` the thinnest constructed-operator gate; `divfree-projector` the four-step Helmholtz gate). Four are thin identity-in-form floors; `divfree-projector` is a **moderate** floor (one genuine `AddMult` apply-accumulate de-fusion). Because none has a fold-parent, **this cohort is design-final** — the leaf-vs-fold fork cannot reach it (there is no fold to fold a leaf into).
```

### 3. `book/src/L2/index.md` — Working-Notes consolidated tally (add the cycle-042 cohort note)

```edit:book/src/L2/index.md
[old]: - **Cycle-041 BLAS-1-floor cohort (the first L2-floor-under-L3 slice; firm 9 → 12).** Three identity-in-form **floor** entries landed under the 2026-05-31 foundation-first directive `l2-floor-under-l3-blas1-cohort`, raising the firm cohort **9 → 12** (partly-constructive `deflate` unchanged at 1).
[new]: - **Cycle-042 fork-INDEPENDENT standalone-floor cohort (firm 12 → 17).** Five `firm` floor entries landed under the `l2-floor-under-l3-blas1-cohort` directive (extended this cycle from the BLAS-1 cohort to elementwise / operator-to-data / constructed-operator-gate primitives), raising the firm L2 cohort **12 → 17** (partly-constructive `deflate` unchanged at 1; **dep-map now 18 rows = 17 firm + 1 partly-constructive**). The cohort is a **distinct motif** from the cycle-041 fold-parented BLAS-1-leaf floors: every cycle-042 floor is a **standalone leaf or gate with NO fold-parent**, so it is **design-final** — untouched by the batch-12 leaf-vs-fold fork (`dot-l2-leaf-floor-vs-fold-only-design`), which can only ever re-anchor fold-parented leaves. (1) [`reciprocal`](./reciprocal.md) (D2) — the standalone elementwise multiplicative-inverse leaf (`y[i] = 1/x[i]`; nonlinear self-map, `1/(a+b) ≠ 1/a + 1/b`), thin identity-in-form floor with eight laws + single element-type axis inherited unchanged. (2) [`elementwise_product`](./elementwise_product.md) (D3) — the standalone Hadamard binary field op (`a ⊙ b`; the inverse-subsumption generalisation of `scal` — `scal(α,x) = elementwise_product(broadcast(α,N), x)`), thin floor with ten laws. (3) [`assemble-diagonal`](./assemble-diagonal.md) (D4) — the standalone operator-to-data diagonal-extraction primitive (`A → diag(A)`; operator-to-data sibling of `apply_linop`, NOT an `apply_linop` variant — OQ `assemblediagonal-is-not-apply-linop-variant`); **firm, not partly-constructive**, because the load-bearing matrix-free high-order-Nedelec approximate-diagonal non-law is preserved through the floor (positively anchored `rap.cpp:163-164` + test-witnessed `test-libceed.cpp:367-376`). (4) [`jacobi-smoother`](./jacobi-smoother.md) (D5) — the **thinnest** constructed-operator gate (apply `op.dinv ⊙ x`, one elementwise product); the fusion rotation is **negative** (no fused multi-operation kernel to unfold); the degree-zero member of the `chebyshev-iteration` recurrence (cited, not dep). (5) [`divfree-projector`](./divfree-projector.md) (D6) — the divergence-free Helmholtz-projection gate (fixed four-step `WeakDiv → Z_{bdr_eff} → ksp_solve → Grad`); a **moderate** floor — one genuine `AddMult` apply-accumulate de-fusion (step-4 `Grad->AddMult(ψ,y,1.0)` → `apply_linop ▷ axpy`), all else identity-in-form; direct L2 dep [`ksp_solve`](./ksp_solve.md) carrying that gate's `sequential-obstruction` **by reference** (the `nested-constructed-operator-gate` fidelity rule — neither introduced nor erased). The companion adjacent thin-identity themes landed the same cycle (L2>L1 `-leaf-identity` ×5; L3>L2 `-body-identity` ×5, advancing `l3-l2-rotation-theme-coverage-gap` 5-of-18 → 10-of-18).
- **Cycle-041 BLAS-1-floor cohort (the first L2-floor-under-L3 slice; firm 9 → 12).** Three identity-in-form **floor** entries landed under the 2026-05-31 foundation-first directive `l2-floor-under-l3-blas1-cohort`, raising the firm cohort **9 → 12** (partly-constructive `deflate` unchanged at 1).
```

### 4. `book/src/L2/index.md` — Working-Notes leaf-vs-fold fork (add the c042 audit recommendation)

```edit:book/src/L2/index.md
[old]:  - **LOAD-BEARING META-PHASE SIGNAL — leaf-vs-fold design fork (`dot-l2-leaf-floor-vs-fold-only-design`; for the batch-12 meta-phase, post-c042).** Wave-1 surfaced a **contradiction between two co-dispatched harvesters**: D1 built `L2/dot` as a same-named conjugation-axis **leaf floor** of `inner_product` (the **(b)** realization — a standalone `dot` chapter, cited as leaf-of, not merged); D2 argued the opposite — that the L2 inner-product surface should be **ONLY** the `inner_product` fold, with **NO `dot` leaf at L2** (the **(a) fold-only** reading), flagging the per-leaf L2 floor as arguably redundant with the fold-cohort vocabulary. **All six cycle-041 entries (the three floors + their six themes) presuppose the (b) "same-named floor" realization.** The batch-12 meta-phase must **ratify or adjust** this design before the cohort is treated as stable. *If it adopts the (a) fold-only reading,* the leaf floors (`dot` certainly; `scal` as the arity-1 member; `nrm2` is unaffected on the fold question since it consumes the fold either way, but its L2 *floor* rides the same `l2-floor-under-l3-blas1-cohort` decision) and their adjacent themes **re-anchor to the fold-parents**: the L2>L1 leaf-identity edges fold into `inner-product-fold-specialization` / `linear-combination-fold-specialization`, and the L3>L2 body-identity edges re-point their L2 RHS from a same-named leaf to the fold-parent. This is upstream of the whole cohort; surfaced here in the L2 Part overview so a reader navigating the floor cohort sees the design is provisional. (Recorded by D1/D2/D4/D5/D6 in their §Open-questions; consolidated as the batch-12 meta-phase OQ.)
[new]:  - **LOAD-BEARING META-PHASE SIGNAL — leaf-vs-fold design fork (`dot-l2-leaf-floor-vs-fold-only-design`; for the batch-12 meta-phase, post-c042; the c042 cross-cutter audit recommends KEEPING leaf-floor (b)).** Wave-1 (cycle-041) surfaced a **contradiction between two co-dispatched harvesters**: D1 built `L2/dot` as a same-named conjugation-axis **leaf floor** of `inner_product` (the **(b)** realization — a standalone `dot` chapter, cited as leaf-of, not merged); D2 argued the opposite — that the L2 inner-product surface should be **ONLY** the `inner_product` fold, with **NO `dot` leaf at L2** (the **(a) fold-only** reading), flagging the per-leaf L2 floor as arguably redundant with the fold-cohort vocabulary. **All six cycle-041 entries (the three floors + their six themes) presuppose the (b) "same-named floor" realization.** The batch-12 meta-phase must **ratify or adjust** this design before the cohort is treated as stable. **The cycle-042 cross-cutter leaf-vs-fold audit (`reports/2026-06-01T063231Z-cycle-042-cross-cutter-leaf-vs-fold-audit/`) recommends KEEPING the leaf-floor reading (b) and ratifying it cohort-wide**: the +files are thin pointers (≤220 ln, identity-in-form, all fusion content deferred to the fold-parent) below the duplication-explosion threshold; the leaf is a genuinely-distinct dual of the fold on the *layer-coherence* axis (the firm L3 `dot`/`scal` leaves already exist and the "both L levels" invariant compels a same-named L2 parent); and it is the consistent extension of the already-accepted L1 `axpby-as-primitive` "keep leaves firm, fuse don't decompose" decision. The audit further finds the D1-vs-D2 disagreement is **narrower than "contradiction"** — D2 never authored a fold-only `dot`, it declined to author a leaf; the live question is the narrow one "does a firm L3 leaf compel a same-named L2 floor?" (audit answer: YES per invariant). It recommends a one-line generalization into §"Fold-cohort boundary" so the held `axpy`-family arity floors don't re-litigate, and an explicit `nrm2` carve-out (consumer-not-member, fork-invariant on membership). The decision remains the **batch-12 meta-phase's to make** — this is a recommendation, not an enactment. *Were the meta-phase to instead adopt the (a) fold-only reading* (against the audit's recommendation), the leaf floors (`dot` certainly; `scal` as the arity-1 member; `nrm2` is unaffected on the fold question since it consumes the fold either way, but its L2 *floor* rides the same `l2-floor-under-l3-blas1-cohort` decision) and their adjacent themes would **re-anchor to the fold-parents**: the L2>L1 leaf-identity edges fold into `inner-product-fold-specialization` / `linear-combination-fold-specialization`, and the L3>L2 body-identity edges re-point their L2 RHS from a same-named leaf to the fold-parent. This (a)-branch consequence is upstream of the whole cohort; surfaced here in the L2 Part overview so a reader navigating the floor cohort sees the design is provisional until ratified. (Recorded by D1/D2/D4/D5/D6 in their §Open-questions; consolidated as the batch-12 meta-phase OQ.) **Note: this fork governs ONLY the cycle-041 fold-parented floors; the cycle-042 standalone-floor cohort (`reciprocal`/`elementwise_product`/`assemble-diagonal`/`jacobi-smoother`/`divfree-projector`) is fork-INDEPENDENT and design-final — having no fold-parent, none can be re-anchored into a fold whatever the meta-phase decides.**
```

### 5. `book/src/L2/index.md` — Working-Notes slug-naming note (add c042 normalization data)

```edit:book/src/L2/index.md
[old]:  - **Slug-naming inconsistency within the L2>L1 cohort (for the meta-phase to normalize).** The L2>L1 `dot` theme is `dot-leaf-identity` (D4 deliberately adjusted from the dispatch-proposed `dot-fold-specialization`, on the reasoning that the `-fold-specialization` suffix names a *fold→leaf dispatch* and would mis-name an identity-leaf-lowering + collide with the existing `inner-product-fold-specialization` whose RHS already IS `L1/dot`), while the `nrm2` and `scal` L2>L1 themes use `-fold-specialization` (`nrm2-fold-specialization`, `scal-fold-specialization`) for sibling-naming continuity — even though `nrm2` is a *consumer* (not a fold member) and `scal`'s edge is the degenerate arity-1 *single-term shadow* (no dispatch). The three L3>L2 themes are consistently `-body-identity`. The cohort's L2>L1 naming is therefore split (`-leaf-identity` vs `-fold-specialization`) for three structurally-similar thin-identity edges. Flagged for the batch-12 meta-phase to normalize the cohort naming (candidate: a uniform `-leaf-identity` / `-body-identity` pairing, since none of the three L2>L1 edges is actually a fold-dispatch).
[new]:  - **Slug-naming inconsistency within the L2>L1 cohort (for the meta-phase to normalize).** The cycle-041 L2>L1 `dot` theme is `dot-leaf-identity` (D4 deliberately adjusted from the dispatch-proposed `dot-fold-specialization`, on the reasoning that the `-fold-specialization` suffix names a *fold→leaf dispatch* and would mis-name an identity-leaf-lowering + collide with the existing `inner-product-fold-specialization` whose RHS already IS `L1/dot`), while the cycle-041 `nrm2` and `scal` L2>L1 themes use `-fold-specialization` (`nrm2-fold-specialization`, `scal-fold-specialization`) for sibling-naming continuity — even though `nrm2` is a *consumer* (not a fold member) and `scal`'s edge is the degenerate arity-1 *single-term shadow* (no dispatch). **The cycle-042 cohort used `-leaf-identity` (L2>L1) / `-body-identity` (L3>L2) consistently across all five floors** (`reciprocal`/`elementwise-product`/`assemble-diagonal`/`jacobi-smoother`/`divfree-projector`), establishing the uniform pairing as the de-facto convention; the cycle-041 `nrm2-fold-specialization` / `scal-fold-specialization` remain the **outliers awaiting meta-phase normalization** (candidate: rename both to `-leaf-identity`, since neither edge is a fold-dispatch — none of the L2>L1 identity edges is). One additional cycle-042 wrinkle (D10): the `elementwise_product` chapter filename is underscored (`elementwise_product.md`, matching the firm L1/L3 entries) while its concept page is hyphenated (`concepts/elementwise-product.md`) and the L2>L1 theme slug is hyphenated (`elementwise-product-leaf-identity`) but the L3>L2 theme slug is underscored (`elementwise_product-body-identity`) — the underscore-vs-hyphen split is consistent within the family but flagged for the same normalization pass.
```

### 6. `book/src/L2-L1/index.md` — Vocabulary-cohort floor-edge sub-list (add the standalone-floor edges)

```edit:book/src/L2-L1/index.md
[old]: *Identity-in-form BLAS-1-floor edges (cycle-041; the L2>L1 thin-identity edges of the new same-named L2 floors `dot`/`nrm2`/`scal`; all fusion content deferred to the fold-parents):*

- `dot-leaf-identity` — the L2 `dot` leaf-floor lowers to the L1 `dot` primitive identity-in-form on the signature; all L2-layer fusion deferred to the fold-parent `inner-product-fold-specialization` (no leaf-unique fusion surplus). Slug `-leaf-identity` (NOT `-fold-specialization`): the edge is an identity-leaf-lowering, not a fold→leaf dispatch.
- `nrm2-fold-specialization` — the L2 `nrm2` floor lowers to the single L1 `nrm2` leaf; the BLAS-1-leaf **consumer** sibling of `inner-product-fold-specialization` (`nrm2 = √ ∘ abs ∘ inner_product` at `y=x`, NOT a fold member; no dispatch / no decomposition / no buffer); `√`/`abs` post-steps drop below L1 resolution, `std::abs` guard preserved-as-claim at L2 → absorbed-by-non-negativity-claim at L1.
- `scal-fold-specialization` — the L2 `scal` floor lowers to the L1 `scal` leaf; the degenerate **arity-1 single-term shadow** of `linear-combination-fold-specialization` (no arity dispatch, no pinned-summation-order residue — one term ⇒ one rounding, value+bit-exact); arity-1 fold member cited NOT merged.
[new]: *Identity-in-form BLAS-1-floor edges (cycle-041; FOLD-PARENTED — the L2>L1 thin-identity edges of the same-named L2 floors `dot`/`nrm2`/`scal`; all fusion content deferred to the fold-parents):*

- `dot-leaf-identity` — the L2 `dot` leaf-floor lowers to the L1 `dot` primitive identity-in-form on the signature; all L2-layer fusion deferred to the fold-parent `inner-product-fold-specialization` (no leaf-unique fusion surplus). Slug `-leaf-identity` (NOT `-fold-specialization`): the edge is an identity-leaf-lowering, not a fold→leaf dispatch.
- `nrm2-fold-specialization` — the L2 `nrm2` floor lowers to the single L1 `nrm2` leaf; the BLAS-1-leaf **consumer** sibling of `inner-product-fold-specialization` (`nrm2 = √ ∘ abs ∘ inner_product` at `y=x`, NOT a fold member; no dispatch / no decomposition / no buffer); `√`/`abs` post-steps drop below L1 resolution, `std::abs` guard preserved-as-claim at L2 → absorbed-by-non-negativity-claim at L1.
- `scal-fold-specialization` — the L2 `scal` floor lowers to the L1 `scal` leaf; the degenerate **arity-1 single-term shadow** of `linear-combination-fold-specialization` (no arity dispatch, no pinned-summation-order residue — one term ⇒ one rounding, value+bit-exact); arity-1 fold member cited NOT merged.

*Fork-INDEPENDENT standalone-floor edges (cycle-042; the L2>L1 thin-identity edges of the new fork-INDEPENDENT L2 floors — each with **NO fold-parent**, so there is no fold to defer fusion to; all `-leaf-identity`):*

- `assemble-diagonal-leaf-identity` — the L2 `assemble-diagonal` operator-to-data floor lowers to the L1 `assemble-diagonal` leaf identity-in-form; **fork-INDEPENDENT, NO fold-parent** (the operator-to-data sibling of `apply_linop`); L2 fusion is **degenerate** (representation-specific diagonal-extraction mechanics absorbed into the representation axis, deferred to L1>L0); the **load-bearing matrix-free high-order-Nedelec approximate-diagonal non-law is preserved through the edge, NOT erased**.
- `jacobi-smoother-leaf-identity` — the L2 `jacobi-smoother` constructed-operator-gate floor lowers to the L1 gate identity-in-form on the apply (single elementwise product); **fork-INDEPENDENT, NO fold-parent**; the L2 fusion observation is **negative** (no fused multi-operation kernel), so the edge is identity with the fusion treatment a documented no-op (NOT a fold deferral).
- `divfree-projector-leaf-identity` — the L2 `divfree-projector` four-step gate floor lowers to the L1 gate; **standalone gate — NO fold-parent, fork-independent**; mostly identity-in-form with **exactly ONE genuine fusion rotation**: the L2 de-fused step-4 `apply_linop(P.Grad,ψ) ▷ axpy` RE-FUSES into the L1 fused `Grad->AddMult(ψ,y,1.0)` apply-accumulate (the one fusion lives HERE, not on a fold-parent); inner-solve `sequential-obstruction` carried BY REFERENCE through firm `ksp_solve`.
- `reciprocal-leaf-identity` — the L2 `reciprocal` floor lowers to the L1 `reciprocal` leaf identity-in-form; **fold-parent-FREE** (a nonlinear self-map), so NO fusion to defer (contrast `dot-leaf-identity`); only the transparent `s = 1/|z|²` complex-intermediate note; design-final on the leaf-vs-fold fork.
- `elementwise-product-leaf-identity` — the L2 `elementwise_product` floor lowers to the L1 leaf identity-in-form (ten laws + element-type / conjugation axes); **fork-INDEPENDENT, NO fold-parent** (a binary field op; the inverse-subsumption generalisation of `scal`), so NO fusion to defer — the L0 `forall_switch` per-element multiply is already unfolded.
```

### 7. `book/src/L2-L1/index.md` — Working-Notes cohort-growth log (prepend the c042 cohort)

```edit:book/src/L2-L1/index.md
[old]: - Cohort growth log (most-recent first): `dot-leaf-identity` + `nrm2-fold-specialization` + `scal-fold-specialization` firm cycle-041 (the BLAS-1-floor-edge cohort — the L2>L1 thin-identity edges of the new same-named L2 floors `dot`/`nrm2`/`scal`, firm 7 → 10; all fusion deferred to the fold-parents, all identity-in-form on the primitive; **presuppose the (b) leaf-floor design realization — under batch-12 meta-phase adjudication, see §"Design fork" below**); `incremental-least-squares-composition-lowering` firm cycle-028 (closes the L2 `l2-named-composition-lifts` lowering side); `eigsolve-spectral-transform-composition` + `gram-fold-specialization` firm cycle-022/023 (eigsolve chain-step-2 + Gram fold-lift); `deflate-composition-lowering` partly-constructive cycle-022 (first L2>L1 partly-constructive entry); `orthogonalize-composition-lowering` firm cycle-019; `inner-product-fold-specialization` + `linear-combination-fold-specialization` firm cycle-018/019 (the variadic-fold unification); `chebyshev-iteration-fusion` firm cycle-013 (first L2-L1 chapter).
[new]: - Cohort growth log (most-recent first): `assemble-diagonal-leaf-identity` + `jacobi-smoother-leaf-identity` + `divfree-projector-leaf-identity` + `reciprocal-leaf-identity` + `elementwise-product-leaf-identity` firm cycle-042 (the **fork-INDEPENDENT standalone-floor-edge cohort** — the L2>L1 thin-identity edges of the five new same-named L2 floors, each with **NO fold-parent**; firm **10 → 15** = 15 firm + 1 partly-constructive; four pure identity-in-form, `divfree-projector-leaf-identity` carries the one genuine step-4 `AddMult` re-fusion; **design-final, NOT subject to the §"Design fork"** — having no fold-parent, none can be re-anchored into a fold); `dot-leaf-identity` + `nrm2-fold-specialization` + `scal-fold-specialization` firm cycle-041 (the FOLD-PARENTED BLAS-1-floor-edge cohort — the L2>L1 thin-identity edges of the same-named L2 floors `dot`/`nrm2`/`scal`, firm 7 → 10; all fusion deferred to the fold-parents, all identity-in-form on the primitive; **presuppose the (b) leaf-floor design realization — under batch-12 meta-phase adjudication, see §"Design fork" below**); `incremental-least-squares-composition-lowering` firm cycle-028 (closes the L2 `l2-named-composition-lifts` lowering side); `eigsolve-spectral-transform-composition` + `gram-fold-specialization` firm cycle-022/023 (eigsolve chain-step-2 + Gram fold-lift); `deflate-composition-lowering` partly-constructive cycle-022 (first L2>L1 partly-constructive entry); `orthogonalize-composition-lowering` firm cycle-019; `inner-product-fold-specialization` + `linear-combination-fold-specialization` firm cycle-018/019 (the variadic-fold unification); `chebyshev-iteration-fusion` firm cycle-013 (first L2-L1 chapter).
```

### 8. `book/src/L2-L1/index.md` — Working-Notes Design-fork note (scope it to cycle-041 + add c042 audit)

```edit:book/src/L2-L1/index.md
[old]: - **Design fork (`dot-l2-leaf-floor-vs-fold-only-design`; load-bearing batch-12 meta-phase signal).** The three cycle-041 floor-edge themes presuppose the **(b) same-named leaf-floor** realization of the L2 BLAS-1 surface (a standalone `dot`/`scal` L2 chapter, cited as leaf-of / member-of the fold-parents but NOT merged; `nrm2` a consumer-of). Wave-1 D2 argued the **(a) fold-only** reading (no `dot` leaf at L2 — the L2 inner-product surface is only the `inner_product` fold). If the meta-phase adopts (a), `dot-leaf-identity` dissolves into `inner-product-fold-specialization`'s conjugation dispatch and `scal-fold-specialization` into `linear-combination-fold-specialization`'s arity-1 row; the `nrm2` edge is unaffected on the fold question (it consumes the fold either way) but its LHS floor rides the same `l2-floor-under-l3-blas1-cohort` decision. Also flagged: the L2>L1 cohort slug split (`dot-leaf-identity` vs `nrm2`/`scal` `-fold-specialization`) for three structurally-similar identity edges — for the meta-phase to normalize.
[new]: - **Design fork (`dot-l2-leaf-floor-vs-fold-only-design`; load-bearing batch-12 meta-phase signal — governs the cycle-041 cohort ONLY).** The three **cycle-041** floor-edge themes presuppose the **(b) same-named leaf-floor** realization of the L2 BLAS-1 surface (a standalone `dot`/`scal` L2 chapter, cited as leaf-of / member-of the fold-parents but NOT merged; `nrm2` a consumer-of). Wave-1 D2 argued the **(a) fold-only** reading (no `dot` leaf at L2 — the L2 inner-product surface is only the `inner_product` fold). If the meta-phase adopts (a), `dot-leaf-identity` dissolves into `inner-product-fold-specialization`'s conjugation dispatch and `scal-fold-specialization` into `linear-combination-fold-specialization`'s arity-1 row; the `nrm2` edge is unaffected on the fold question (it consumes the fold either way) but its LHS floor rides the same `l2-floor-under-l3-blas1-cohort` decision. **The cycle-042 cross-cutter audit (`reports/2026-06-01T063231Z-cycle-042-cross-cutter-leaf-vs-fold-audit/`) recommends KEEPING leaf-floor (b)** — the +files are thin pointers below the duplication-explosion threshold, the leaf is genuinely distinct on the layer-coherence axis, and (b) honors the already-firm L3 `dot`/`scal` leaves' "both L levels" demand; it further finds D1-vs-D2 is narrower than "contradiction" (D2 declined to author a leaf, never built a fold-only `dot`). **This fork does NOT reach the cycle-042 standalone-floor edges** (`assemble-diagonal`/`jacobi-smoother`/`divfree-projector`/`reciprocal`/`elementwise-product`) — each has **NO fold-parent**, so there is no fold to dissolve a leaf into; that cohort is design-final regardless of the adjudication. Also flagged: the cycle-041 L2>L1 cohort slug split (`dot-leaf-identity` vs `nrm2`/`scal` `-fold-specialization`) for three structurally-similar identity edges; the cycle-042 cohort used `-leaf-identity` uniformly, making the two cycle-041 `-fold-specialization` slugs the outliers for the meta-phase to normalize.
```

### 9. `book/src/L3-L2/index.md` — Vocabulary-cohort body-identity sub-list (add the standalone-floor body edges)

```edit:book/src/L3-L2/index.md
[old]: *Identity-in-form BLAS-1-leaf body edges (cycle-041; the L3>L2 thin-identity edges of the BLAS-1 leaves whose L2 floors landed this cycle; all the leaf-primitive analogue of `krylov-step-body-identity` with NO wrapper rotation — the leaf has no `(op, K, s)` tuple and no outer loop):*

- `dot-body-identity` — the L3 whole-tensor `dot` reduction lowers to the L2 same-named leaf-floor identity-in-form on the body; `dot` is L3-native by signature shape (no element loop), so the iteration rotation is already done at the signature level.
- `nrm2-body-identity` — the L3 whole-tensor `nrm2` norm lowers to the L2 `√ ∘ abs ∘ inner_product` consumer floor; the only textual change is the inner-reduction name (`dot` leaf at L3 → `inner_product` fold at the diagonal at L2) + the surfacing of the `abs` guard as an explicit L2 claim.
- `scal-body-identity` — the L3 whole-tensor `scal` field operation lowers to the L2 base scalar-vector-multiply floor leaf (arity-1 fold member); the body IS the identity, there is no wrapper to rotate.
[new]: *Identity-in-form BLAS-1-leaf body edges (cycle-041; FOLD-PARENTED — the L3>L2 thin-identity edges of the BLAS-1 leaves whose L2 floors landed cycle-041; the leaf-primitive analogue of `krylov-step-body-identity` with NO wrapper rotation — the leaf has no `(op, K, s)` tuple and no outer loop):*

- `dot-body-identity` — the L3 whole-tensor `dot` reduction lowers to the L2 same-named leaf-floor identity-in-form on the body; `dot` is L3-native by signature shape (no element loop), so the iteration rotation is already done at the signature level.
- `nrm2-body-identity` — the L3 whole-tensor `nrm2` norm lowers to the L2 `√ ∘ abs ∘ inner_product` consumer floor; the only textual change is the inner-reduction name (`dot` leaf at L3 → `inner_product` fold at the diagonal at L2) + the surfacing of the `abs` guard as an explicit L2 claim.
- `scal-body-identity` — the L3 whole-tensor `scal` field operation lowers to the L2 base scalar-vector-multiply floor leaf (arity-1 fold member); the body IS the identity, there is no wrapper to rotate.

*Fork-INDEPENDENT standalone-floor body edges (cycle-042; the L3>L2 thin-identity edges of the five fork-INDEPENDENT L2 floors — each with **NO fold-parent**; all the leaf/gate-primitive analogue of `krylov-step-body-identity` with NO wrapper rotation):*

- `assemble-diagonal-body-identity` — the L3 whole-operator `assemble-diagonal` operator-to-data field operation lowers to the L2 standalone floor (operator-to-data sibling of `apply_linop`, NO fold-parent); identity-in-form, **load-bearing matrix-free approximate-diagonal non-law preserved through the edge, NOT erased**.
- `jacobi-smoother-body-identity` — the L3 whole-tensor `jacobi-smoother` constructed-operator-gate field operation lowers to the L2 same-named gate floor; the body IS the identity (one elementwise product `op.dinv ⊙ x`), no wrapper to rotate; fork-INDEPENDENT, the thinnest constructed-operator-gate member.
- `divfree-projector-body-identity` — the L3 whole-tensor `divfree-projector` four-step gate lowers to the L2 same-named floor value-thread-isomorphic on the four-step body (`WeakDiv → Z_{bdr_eff} → ksp_solve → Grad`); inner-solve obstruction carried BY REFERENCE; the step-4 `AddMult` de-fusion is the L2>L1 edge's content, NOT this edge's; standalone gate — fork-independent.
- `reciprocal-body-identity` — the L3 whole-tensor `reciprocal` field operation lowers to the L2 standalone multiplicative-inverse floor leaf (NO fold-parent, nonlinear self-map); the body IS the identity, no wrapper and no fold-parent to defer to; design-final on the leaf-vs-fold fork.
- `elementwise_product-body-identity` — the L3 whole-tensor `elementwise_product` Hadamard binary field op lowers to the L2 standalone floor leaf (fork-INDEPENDENT, NO fold-parent); identity-in-form on the body (ten laws + element-type / conjugation axes); no wrapper and no fold-parent to defer to.
```

### 10. `book/src/L3-L2/index.md` — Working-Notes coverage-gap tally (advance 5-of-18 → 10-of-18)

```edit:book/src/L3-L2/index.md
[old]: - **Cohort growth + coverage-gap progress (firm 2 → 5; `l3-l2-rotation-theme-coverage-gap` 2-of-18 → 5-of-18).** Cycle-041 landed the first three BLAS-1-leaf L3>L2 thin-identity themes (`dot-body-identity` / `nrm2-body-identity` / `scal-body-identity`), each the leaf-primitive analogue of the firm `krylov-step-body-identity` (cycle-007/009) but strictly simpler — a single leaf, no wrapper to rotate. This **begins closing** the `l3-l2-rotation-theme-coverage-gap` plan item (the iteration-rotation rewrite — the defining content of the L3 layer — was documented for only 2 of 18 L3 entries; now 5 of 18, with 13 still relying on inline identity annotations). The remaining gap splits into **thin-identity** edges (the rest of the BLAS-1 / identity-in-form L3 cohort) and **substantive** edges (`chebyshev`, `eigsolve` `partial-obstruction`, `orthogonalize` MGS-vs-CGS) — author thin where identity-in-form, firm-substantive where the rotation carries real content.
[new]: - **Cohort growth + coverage-gap progress (firm 5 → 10; `l3-l2-rotation-theme-coverage-gap` 5-of-18 → 10-of-18).** Cycle-042 landed five more L3>L2 thin-identity `-body-identity` themes — the **fork-INDEPENDENT standalone-floor** cohort (`assemble-diagonal-body-identity` / `jacobi-smoother-body-identity` / `divfree-projector-body-identity` / `reciprocal-body-identity` / `elementwise_product-body-identity`), each the leaf-/gate-primitive analogue of the firm `krylov-step-body-identity` (cycle-007/009) but strictly simpler — a single leaf or fixed-step gate, no wrapper to rotate, **no fold-parent** (distinct from the cycle-041 BLAS-1-leaf cohort, which is fold-parented and rides the batch-12 leaf-vs-fold fork; this cohort is design-final). This continues closing the `l3-l2-rotation-theme-coverage-gap` plan item: the iteration-rotation rewrite (the defining content of the L3 layer) was documented for 5 of 18 L3 entries after cycle-041; now **10 of 18**, with 8 still relying on inline identity annotations. The remaining gap splits into **thin-identity** edges (the rest of the identity-in-form L3 cohort) and **substantive** edges (`chebyshev`, `eigsolve` `partial-obstruction`, `orthogonalize` MGS-vs-CGS) — author thin where identity-in-form, firm-substantive where the rotation carries real content. (Prior: cycle-041 landed the first three BLAS-1-leaf themes `dot-body-identity` / `nrm2-body-identity` / `scal-body-identity`, taking the gap 2-of-18 → 5-of-18.)
- **Design fork (`dot-l2-leaf-floor-vs-fold-only-design`) reaches ONLY the cycle-041 cohort.** The cycle-041 `-body-identity` themes presuppose the (b) same-named L2 leaf-floor RHS and re-point to the fold-parent under (a); the cycle-042 standalone-floor `-body-identity` themes have **NO fold-parent** and so are unaffected by the adjudication (their L2 RHS is a same-named standalone floor with no fold to re-anchor into). The c042 cross-cutter audit recommends KEEPING leaf-floor (b) for the cycle-041 cohort — see `book/src/L2/index.md` §Working-Notes.
```

## Supporting evidence

- **Tally verification (row-by-row, on-disk + reports):**
  - `book/src/L2/index.md` dep-map: 13 rows on disk pre-c042 (12 firm + `deflate` partly-constructive). The 5 new firm rows (`reciprocal`/`elementwise_product`/`assemble-diagonal`/`jacobi-smoother`/`divfree-projector`) are appended by D2–D6's own `edit:book/src/L2/index.md` blocks → 18 rows = **17 firm + 1 partly-constructive**. Firm 12 → 17.
  - `book/src/L2-L1/index.md` theme-list: 11 rows on disk pre-c042 (10 firm + `deflate-composition-lowering` partly-constructive). The 5 new firm `-leaf-identity` rows are appended by D7–D10 → 16 rows = **15 firm + 1 partly-constructive**. Firm 10 → 15.
  - `book/src/L3-L2/index.md` theme-list: 5 rows on disk pre-c042 (all firm). The 5 new firm `-body-identity` rows are appended by D7–D10 → **10 firm**. Firm 5 → 10; coverage-gap 5-of-18 → 10-of-18.
- **All five L2 floors are `firm`** (verified from each report's `## Status` + frontmatter `firmness: firm`): D2 `reciprocal` (firm-on-positive-structure, complex kernel `vector.cpp:248-261`); D3 `elementwise_product` (firm-on-positive-structure, `BaseDiagonalOperator::Mult`); D4 `assemble-diagonal` (firm, NOT partly-constructive — approximate-diagonal non-law preserved); D5 `jacobi-smoother` (firm-on-positive-structure, negative fusion); D6 `divfree-projector` (firm, moderate floor, one `AddMult` de-fusion).
- **All ten themes are `firm`** (verified from each report's theme-list row status column).
- **`deflate` / `deflate-composition-lowering` partly-constructive entries stay OUT of the firm counts** — verified unchanged on disk; held at 1 partly-constructive in L2 and L2-L1, 0 in L3-L2.
- **Cohort distinction (fork-independent vs fold-parented)** sourced from each harvester's design-finality statement (e.g. `reciprocal` report `:26`; `elementwise_product` report `:542`; `assemble-diagonal` report `:18`) and the cross-cutter audit recommendation (`reports/2026-06-01T063231Z-cycle-042-cross-cutter-leaf-vs-fold-audit/CYCLE.md`, §Recommendation: KEEP leaf-floor (b); §1 the `nrm2` carve-out; §3 the duplication-explosion verdict).
- **Slug-naming**: all 5 L2>L1 themes use `-leaf-identity`; all 5 L3>L2 use `-body-identity`. Outliers (cycle-041 `nrm2-fold-specialization` / `scal-fold-specialization`) flagged unchanged for meta-phase normalization. `elementwise_product` underscore-vs-hyphen filename split flagged (D10 report `:594-600`).

## Open questions / caveats

- **OQ (carried forward, NOT mine to close): the leaf-vs-fold fork `dot-l2-leaf-floor-vs-fold-only-design`** stays open for the batch-12 meta-phase (fires after this cycle). My refresh records the c042 cross-cutter's **recommendation** (keep leaf-floor (b), ratify cohort-wide, add a one-line §"Fold-cohort boundary" generalization, carve out `nrm2`) but does NOT enact it — the meta-phase decides go/no-go. I have scoped every fork reference in all three indices to make explicit that the fork governs **only the cycle-041 fold-parented cohort**; the cycle-042 standalone-floor cohort is design-final and fork-independent.
- **OQ (carried, NOT mine to close): cohort slug normalization.** The cycle-042 cohort establishes `-leaf-identity` / `-body-identity` as the de-facto uniform pairing; the cycle-041 `nrm2-fold-specialization` / `scal-fold-specialization` are the remaining outliers + the `elementwise_product` underscore-vs-hyphen filename split — both for the meta-phase to normalize. Recorded in the L2/index §Working-Notes slug note.
- **Caveat — I did NOT re-emit the per-operator dep-map / theme-list ROWS.** Those are D2–D10's own `edit:` blocks, landed via per-report integration ordering. My proposed-changes touch ONLY the narrative sections I own (Vocabulary-cohort lists, Semantics motif list, Working-Notes tallies + cohort prose). If the integrator applies my blocks before some D2–D10 row-edits, the narrative will reference rows not yet present — but per-report integration is serial and my dispatch (D11, wave-2 tail) is ordered after the producers, so the rows are on disk first. Flagging the ordering dependency for the integrator's awareness.
- **Caveat — `jacobi-smoother` directive-name variant.** D5's report cites the directive as `l2-floor-under-l3-jacobi-smoother` (in its dep-map row status) whereas the cohort directive is `l2-floor-under-l3-blas1-cohort` (extended this cycle to non-BLAS-1 primitives). This is a D5 row-level wording artifact, not a tally issue; I use the canonical `l2-floor-under-l3-blas1-cohort` (extended) framing in my narrative. Flagged for the integrator/critic in case D5's row wants a one-word normalization (out of my write-scope — I do not edit D5's row).
- No L0 citations emitted in this refresh (narrative / structural only); `citecheck` not applicable. The few source anchors I quote (`rap.cpp:163-164`, `vector.cpp:248-261`, etc.) are transcribed verbatim from the producers' already-verified rows, not independently re-cited.
