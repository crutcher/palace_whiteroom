---
agent: cross-layer-cross-cutter
invoked_at: 2026-06-09T020212Z
scope: cycle-151 D1 — once-per-batch maintenance-floor full-hygiene sweep + comprehensive A–F book-wide FINALIZATION residue-class scan (batch-50 OPENER)
status: pending
---

# CYCLE: Cross-layer observation — c151 hygiene sweep + A–F residue baseline

## Summary

Ran the standing per-batch maintenance-floor full-hygiene checklist (6 items) and the comprehensive
A–F book-wide FINALIZATION residue-class scan. **Hygiene verdict: CLEAN BILL 6/6** — the
graded-stack two-invariant tripwire HELD EXACTLY against the c150 baseline (no newly-orphaned node,
no rank violation, escalate-guard not tripped), all gated RE-set / `eigsolve-impl` promotion gates
NON-FIRING, semantic-surface discipline intact, the 3 `realizes-kernel-api` edges stay
`reference`-class, no MPI/sharding lifted as active work. The A–F scan establishes the authoritative
residue baseline: **A=B=C=0** (already discharged), and the live D/E/F campaign target is
**D=1 (F-coupled), E=21 substantive files (+2 load-bearing KEEP), F=14 process-narrative files**.

**One material observation (audit kind — scan-definition refinement, surfaced for the batch-50
meta + the campaign):** the F-class as literally defined in my dispatch prompt (`## Origin | Working
Notes | Critic's role | Context`) over-captures by **121 files** — every per-operator/theme chapter
carries a `## Context` orientation section. I verified those 121 `## Context`-only sections are
**load-bearing static orientation prose, NOT slice-era process residue** (zero of them carry any
retired-infra reference, cycle-tag, or forward-process speculation). The genuine F-target is the
**14 files** carrying `## Origin` / `## Working Notes` / `## Critic's role` (5 concept pages + 9
layer/lowering indexes) — which match the planner's pre-seed count. `## Context` must be EXCLUDED
from the F-completion-gate grep, or it will never reach 0 and the campaign can never declare F-class
complete. This is the cross-layer analog of an edge-label-fidelity finding: the residue-class label
(F) was applied to a heading that, across the corpus, denotes static content at every operator scope.

## Observation kind

**Audit residue** — A–F residue-class baseline + an F-class scan-definition over-capture (the
`## Context` heading is a static-orientation section corpus-wide, not the slice-era process-narrative
class the campaign targets). No `book/` mutation; OQ-ledger-surface + meta-flag only.

## Specific finding

### Part 1 — Hygiene checklist: CLEAN BILL 6/6

| # | Check | Verdict |
|---|---|---|
| 1 | Graded-stack two-invariant tripwire | **PASS** — `files=392, typed=331, untyped=61, roots=45, rank_violations=0, unresolved_depends_on_targets=0, promotion_frontier=11, detritus=123 (true 51 / ref-reachable 72), expected_unreachable=54`. All totals HELD EXACTLY vs the c150 baseline. No newly-orphaned node; detritus escalate-guard not tripped (`true_detritus=51` unchanged). |
| 2 | RE-set premise re-check | **PASS** — the gated members sit in the PROMOTION FRONTIER (11), none firm: `L3/eigsolve-impl`, `L3/nleps-deflated-eigensolve`, `L2/deflate`, `L4/sharding-decompose-reduce`, `L3/lanczos_step` all sub-firm. `eigsolve-impl` promotion gate NON-FIRING (still `roadmap_goal`, waiting on a blocking `depends-on` consumer). |
| 3 | Semantic-surface liveness | **PASS** — `semantics/index.md:3` SEMANTIC-CONSOLIDATION governing header intact; `:24` §0.1 active-management discipline intact; no new restatement cohort observed in the scan. |
| 4 | Kernel-API/impl integrity (DIRECTIVE 3) | **PASS** — all 3 `realizes-kernel-api` edges declared under `reference:` blocks (`L3/eigsolve-impl.md:19-23`, `L1/libceed-quadrature-kernel-impl.md:12-14`, `L1/multigrid-relaxation-smoother.md:24-26`). Free / navigational, no rank/liveness carry. |
| 5 | DIRECTIVE-1 MPI boundary | **PASS** — `L4/sharding-decompose-reduce.md` stays `rank: roadmap_goal` / `status: roadmap_goal` (rank 0, claim-free FUTURE-DIRECTION sketch). No MPI/sharding lifted as active work. |
| 6 | Opportunistic detritus/edge-typing GC | **PASS** — `true_detritus=51` (the consumer-gated GROUND-don't-remove cohorts; 72 reference-reachable §2g/RE11). No genuinely-new dead-intent node. |

### Part 2 — A–F residue-class scan (authoritative baseline)

Carve-out applied to D/E/F greps: `grep -vE 'meta-reviews|methodology/'` (per the planner's flag, the
D/E carve-out is widened to `methodology/` **generally**, not just `goal-flow` — the worked-example
cycle refs in `methodology/resolution-ladder.md` + `methodology/graded-stack-scheme.md` + `goal-flow.md`
are deliberately-kept methodology process records, NOT residue).

**A. `## Verified-against` sections — 0. CLEAN** (discharged c150).
`grep -rlE '^## Verified-against' book/src` → 0 files.

**B. `verified_against:` YAML blocks — 0. CLEAN.**
`grep -rlE '^verified_against:' book/src` → 0 files.

**C. `reports/…` prose pointers — 0. CLEAN.**
`grep -rlE 'reports/[0-9]' book/src | grep -v meta-reviews` → 0 files.

**D. inline `cycle-NNN`/`cNNN`/`batch-NNN`/`wave-N` tags — 1 file (ex carve-out). STRIP (F-coupled).**

| count | file | disposition |
|---|---|---|
| 1 | `book/src/concepts/variant-absorption.md` | STRIP — the single hit (l.18, a "**Cycle 6** back-push on GMRES L1, with the Critic acknowledging cycle-5's improvements …" enactment narrative) lives INSIDE the file's process-narrative section, co-located with its `## Origin`/`## Working Notes`. Discharged automatically when the F-pass de-bulks this page. D-residual collapses to the methodology carve-out + this one F-coupled file. |

*Carve-out hits (NOT residue, deliberately kept):* `methodology/goal-flow.md`, `methodology/graded-stack-scheme.md`, `methodology/resolution-ladder.md`.

**E. directive-date provenance (`2026-0X-XX` in prose) — 21 substantive files (ex carve-out) + 2 load-bearing KEEP.**

Strip targets (rephrase-to-drop-the-date, KEEP the structural fact):

| count | file | note |
|---|---|---|
| 5 | `book/src/concepts/variant-absorption.md` | co-de-bulk with its F-pass (coupling-lift exemplar) |
| 4 | `book/src/concepts/rotation.md` | the **D2 pilot** — discharged this cycle |
| 3 | `book/src/concepts/constructed-operators.md` | co-de-bulk with F-pass |
| 1 | `book/src/concepts/black-box-vs-accelerated-kernels.md` | rephrase-to-drop-the-date |
| 1 | `book/src/concepts/dependency-map.md` | co-de-bulk with F-pass |
| 3 | `book/src/L3/assemble_diagonal.md` | rephrase-to-drop-the-date |
| 2 | `book/src/L1/multigrid-relaxation-smoother.md` | rephrase (kernel-impl page; KEEP edges/citations) |
| 2 | `book/src/L3/elementwise_product.md` | rephrase-to-drop-the-date |
| 2 | `book/src/L3/linear_combination.md` | rephrase-to-drop-the-date |
| 2 | `book/src/L2/normalize.md` | rephrase-to-drop-the-date |
| 2 | `book/src/L4/assemble_frequency_operator.md` | rephrase-to-drop-the-date |
| 1 | `book/src/L1/essential_dofs.md` | rephrase-to-drop-the-date |
| 1 | `book/src/L1-L0/essential-dofs-construction-rotation.md` | rephrase-to-drop-the-date |
| 1 | `book/src/L1/index.md` | layer-index — co-de-bulk with its F-pass; PRESERVE sole-rank-carrier tokens |
| 1 | `book/src/L2/index.md` | layer-index — co-de-bulk with its F-pass |
| 1 | `book/src/L2/correction_step.md` | rephrase-to-drop-the-date |
| 1 | `book/src/L2/inner_product.md` | rephrase-to-drop-the-date |
| 1 | `book/src/L2/linear_combination.md` | rephrase-to-drop-the-date |
| 1 | `book/src/L2/reciprocal.md` | rephrase-to-drop-the-date |

Load-bearing KEEP (NOT residue — do NOT de-bulk):

| count | file | reason |
|---|---|---|
| 1 | `book/src/semantics/index.md` | the `:3` SEMANTIC-CONSOLIDATION **governing-directive header blockquote** — its job IS to record the directive governing the file's active-management discipline. KEEP per the skill's E-class rule + the planner's note. |
| 22 | `book/src/SUMMARY.md` | NOT a chapter body; the 22 hits are meta-review TOC link labels (dated filenames `meta-reviews/2026-05-24*.md`). KEEP. |

**F. slice-era process-narrative sections — 14 files (ex carve-out). STRIP (coupling-lift-aware).**

The genuine F-target — `## Origin` / `## Working Notes` / `## Critic's role`:

| file | sections | note |
|---|---|---|
| `book/src/concepts/rotation.md` | Context, Critic's role, Origin, Working Notes | **D2 pilot — discharged this cycle** |
| `book/src/concepts/variant-absorption.md` | Context, Critic's role, Origin, Working Notes | coupling-lift exemplar (lift "relationship to rotation" → `## Relationship to rotation`); ALSO the sole D-residual file |
| `book/src/concepts/constructed-operators.md` | Context, Origin, Working Notes | strip + co-discharge E-dates |
| `book/src/concepts/dependency-map.md` | Origin | thinnest |
| `book/src/concepts/index.md` | Context, Working Notes | strip |
| `book/src/L0/index.md` | Context, Working Notes | layer-index — PRESERVE sole-rank-carrier tokens + load-bearing structural prose |
| `book/src/L1/index.md` | Context, Working Notes | layer-index |
| `book/src/L1-L0/index.md` | Context, Working Notes | lowering-index |
| `book/src/L2/index.md` | Context, Working Notes | layer-index — carries the OQ `reciprocal-stale-prose-slug…` fix |
| `book/src/L2-L1/index.md` | Context, Working Notes | lowering-index |
| `book/src/L3/index.md` | Context, Working Notes | layer-index |
| `book/src/L3-L2/index.md` | Context, Working Notes | lowering-index |
| `book/src/L4/index.md` | Context, Working Notes | layer-index |
| `book/src/L4-L3/index.md` | Context, Working Notes | lowering-index |

## Recommendation

1. **Defer (record-only) — A/B/C are CLEAN; the D/E/F campaign target is REAL.** The hygiene
   checklist is a clean 6/6 bill; no repair action. The A–F baseline above IS the campaign's
   drive-to-zero / completion-gate reference.

2. **Route to the batch-50 meta (scan-definition refinement) — EXCLUDE `## Context` from the
   F-completion-gate grep.** The F-class as literally written in the dispatch prompt and the skill's
   §"A–F residue-class scan" line F (`^## (Origin|Working Notes|Critic'?s role)` — already excludes
   Context) is the CORRECT target; but my dispatch prompt's prose F-definition listed `## Context`
   among the F sections. Reconciling: the skill's actual grep line F does NOT include `Context`, so
   the skill is already correct — the meta should ensure the prompt-level F-definition matches the
   skill's grep (Context EXCLUDED). Evidence: of 121 `## Context`-only files, **zero** carry any
   retired-infra reference / cycle-tag / forward-process marker; they are static orientation prose
   (what the operator IS, its layer, cross-layer relationships, evidence base, live citations). The
   14 process-narrative files are the genuine F-target and match the planner's pre-seed count. Without
   this exclusion the "clean F-scan" completion gate can NEVER reach 0 (every operator chapter has a
   `## Context`).

3. **Confirm the planner's D/E carve-out widening (`methodology/` generally).** The scan applied
   `grep -vE 'meta-reviews|methodology/'` and it cleanly separated the 3 deliberately-kept
   methodology worked-example files from residue. Recommend the meta codify the widened carve-out
   into the skill's A–F scan grep lines D/E (currently `meta-reviews|methodology/goal-flow`) so the
   completion gate does not false-positive on `resolution-ladder.md` / `graded-stack-scheme.md`.

## Supporting evidence

- Linter: `tools/graded-stack-lint/graded_stack_lint.py --book-src book/src` — RESULT line: `0 rank violation(s), 123 detritus (51 true / 72 reference-reachable §2g), 61 untyped`.
- A/B/C greps → 0 (commands in the skill §"A–F residue-class scan").
- D: `book/src/concepts/variant-absorption.md:18` (the in-F-block Cycle-6 narrative).
- E KEEP: `book/src/semantics/index.md:3` (governing header), `book/src/SUMMARY.md:394-397+` (meta-review TOC labels).
- F-target vs over-capture: `grep -rlE "^## (Origin|Working Notes|Critic'?s role)" book/src | grep -vE 'meta-reviews|methodology/'` → 14; `^## Context` adds 121 process-marker-free orientation sections (verified: 0 carry retired-infra/cycle/forward-process markers).
- Kernel-API: `L3/eigsolve-impl.md:19-23`, `L1/libceed-quadrature-kernel-impl.md:12-14`, `L1/multigrid-relaxation-smoother.md:24-26` (all `reference:`).
- DIRECTIVE-1: `L4/sharding-decompose-reduce.md:4-5` (`rank: roadmap_goal`).

## Open questions / caveats

- **OQ (for the batch-50 meta): F-completion-gate grep must exclude `## Context`.** The skill's grep
  line F already does; the dispatch-prompt-level F prose did not. Reconcile so the campaign's "F→0"
  completion gate counts only the 14 process-narrative files, not the 121 static-orientation
  `## Context` sections. Slug: `f-class-context-heading-orientation-vs-process-narrative`.
- **OQ (carried from the planner): D/E carve-out grep should be `methodology/` generally** (not just
  `goal-flow`). The scan confirmed this cleanly separates the 3 methodology worked-example files.
  Slug: `af-scan-de-carveout-widen-methodology-general`.
- **Per-operator `## Context` orientation sections are out of scope for the de-bulk campaign** — they
  are static spec content (what the component IS), the legitimate KEEP analog of the finalization
  directive's "what each component IS" surface. The campaign de-bulks only the 14 process-narrative
  files' `## Origin`/`## Working Notes`/`## Critic's role` (+ the D2 pilot's already-done `rotation.md`
  Context-section, which on the concept pages IS slice-era framing, distinct from the operator-chapter
  orientation Context). This concept-page-Context vs operator-chapter-Context split is itself worth a
  one-line note in the c152 scale-out scopes: on the 5 **concept pages** + indexes, `## Context` is
  process framing (strip); on the **operator/theme chapters** it is orientation (keep).
- No `book/` mutation performed; this is an audit-class dispatch (linter `--json`-equivalent reads +
  greps only). No node/edge/rank/status moved.
