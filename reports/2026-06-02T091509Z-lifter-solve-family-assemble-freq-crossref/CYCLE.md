---
agent: lifter
invoked_at: 2026-06-02T091509Z
scope: L4 solve_family map_solve scope-boundary cross-ref firm — name assemble_frequency_operator as the per-ω operator
status: pending
integrated_at: 2026-06-02T101500Z
integration_commit: PLACEHOLDER_SHA
integration_notes: "cycle-063 D2 (batch-19 close-out, build-relevant). Applied single in-place edit to book/src/L4/solve_family.md (§Status Scope-load-bearing paragraph :146): map_solve scope-note re-anchored to name the firm per-ω operator assemble_frequency_operator via live link ../L1/assemble_frequency_operator.md (reciprocal L1->L4 ref already present -> mutual link). RESOLVED-BY-LANDING-c063-D2 the deferred c062 cross-ref OQ assemble-frequency-operator-map-solve-scope-boundary-cross-ref-refresh; opened optional low-value breadth-pass OQ solve-family-name-assemble-frequency-operator-at-all-per-omega-rebuild-loci. solve_family status/laws/axes UNTOUCHED -> no L4/index status-cell update owed (anti-drift guard NOT fired). No gate triggered. citecheck --scan 6 ok / 0 failing. Build: cargo make book exit 0; link rendered in book/book/html/L4/solve_family.html."
inputs:
  - book/src/L4/solve_family.md
  - book/src/L1/assemble_frequency_operator.md
---

# CYCLE: Re-anchor solve_family map_solve scope-boundary cross-ref

## Summary
`book/src/L4/solve_family.md` carries (in its §Status "Scope (load-bearing)" paragraph) the `map_solve` scope-boundary note: driven is scoped OUT of the fixed-operator `solve_family` because driven rebuilds its operator per-frequency (`SetOperators` inside the loop, `drivensolver.cpp:176-180`). As of cycle-062 that per-ω operator rebuild is now a FIRM named L1 operator — [`assemble_frequency_operator`](`book/src/L1/assemble_frequency_operator.md`, firm, the affine-in-ω fixed-basis operator family `A(ω)=K+iω·C−ω²·M+A2(ω)`, the operator-operand specialization of `linear_combination`). This is a **pure cross-reference firming**: the scope-boundary note previously referred to the per-ω rebuild only by its L0 site; it can now NAME the firm operator via a live link. `solve_family`'s status (`rough-in (test-coverage-bounded)`), laws, signature, and the note's structure are untouched — only the per-ω-rebuild phrase gains a named live link to the firm operator. (The OQ being closed is `assemble-frequency-operator-map-solve-scope-boundary-cross-ref-refresh`.)

Live-link target verified on disk: `book/src/L1/assemble_frequency_operator.md` exists (`firmness: firm`, read this dispatch); relative path from `book/src/L4/solve_family.md` is `../L1/assemble_frequency_operator.md`. L0 citation `drivensolver.cpp:176-180` re-verified against on-disk source this dispatch (`:176-177` = `GetSystemMatrix(...)` per-ω rebuild; `:180` = `ksp.SetOperators(*A, *P)`).

## Proposed changes

```edit:book/src/L4/solve_family.md
[old]: **Scope (load-bearing)**: `solve_family` (fixed-operator) is witnessed by **electrostatic + magnetostatic ONLY** (2-of-5 pipelines). The other three: **driven** breaks shared-operator-capture (operator rebuilt per-frequency, `drivensolver.cpp:176-180`, `SetOperators` inside the loop) — it is a witness of the `per-element` superset `map_solve_over_(operator,rhs)_family`, NOT of `solve_family`; **transient** is unprobed (the canonical `fold` candidate, now homed at [`fold_solve`](./fold_solve.md)); **eigenmode** was probed (cycle-059 cross-layer-cross-cutter, `reports/2026-06-02T061737Z-cross-layer-cross-cutter-eigenmode-outer-machinery-probe/CYCLE.md`) and is **NOT a witness** of either `solve_family` or `fold_solve` — the eigenmode driver calls the opaque `eigen->Solve()` once (`eigensolver.cpp:367`), with no operator/RHS family to map and no state-threaded solve-march to fold; its only outer loop is a post-processing *readout* map over the already-converged eigenpair set (`eigensolver.cpp:425-471`). Do NOT claim cross-pipeline generality beyond the two fixed-operator witnesses.
[new]: **Scope (load-bearing)**: `solve_family` (fixed-operator) is witnessed by **electrostatic + magnetostatic ONLY** (2-of-5 pipelines). The other three: **driven** breaks shared-operator-capture (operator rebuilt per-frequency, `drivensolver.cpp:176-180`, `SetOperators` inside the loop) — it is a witness of the `per-element` superset `map_solve_over_(operator,rhs)_family`, NOT of `solve_family`. The per-ω operator driven rebuilds is the now-firm [`assemble_frequency_operator`](../L1/assemble_frequency_operator.md) (the affine-in-ω fixed-basis operator family `A(ω)=K+iω·C−ω²·M+A2(ω)`, the operator-operand specialization of `linear_combination`); it is the named per-element operator of the `map_solve_over_(operator,rhs)_family` superset, and its existence sharpens (does not move) this scope boundary: driven's per-element operator is not arbitrary but a fixed-basis affine combination, yet it is still *per-element* (rebuilt inside the loop), which is exactly the `operator-capture = per-element` axis value that scopes driven out of the `fixed`-only `solve_family`. **transient** is unprobed (the canonical `fold` candidate, now homed at [`fold_solve`](./fold_solve.md)); **eigenmode** was probed (cycle-059 cross-layer-cross-cutter, `reports/2026-06-02T061737Z-cross-layer-cross-cutter-eigenmode-outer-machinery-probe/CYCLE.md`) and is **NOT a witness** of either `solve_family` or `fold_solve` — the eigenmode driver calls the opaque `eigen->Solve()` once (`eigensolver.cpp:367`), with no operator/RHS family to map and no state-threaded solve-march to fold; its only outer loop is a post-processing *readout* map over the already-converged eigenpair set (`eigensolver.cpp:425-471`). Do NOT claim cross-pipeline generality beyond the two fixed-operator witnesses.
```

## Discipline notes
- **Pure cross-reference firming** (in-scope per the lifter "structural rewrite, citation re-anchor" remit). The only change is the addition of one named live link (`../L1/assemble_frequency_operator.md`) plus the firm-operator framing sentence into the existing §Status scope-boundary note. No restructuring of the note; no change to `solve_family`'s status line (`rough-in (test-coverage-bounded)`), signature, semantics, algebraic laws, variant axes, or evidence block.
- The framing sentence I added is **not new content** — it restates, with the now-firm name, what the entry already says in multiple loci: that driven's per-ω rebuild is the `per-element` superset's per-element operator and the scope boundary (lines 65, 90, 137, 146 already make this claim abstractly). The c061 D3 / c062 D3 findings established "the per-ω operator rebuild that scopes driven out of `solve_family` IS this affine-operator-family evaluation (`assemble_frequency_operator`)" — and the firm L1 entry itself states the reciprocal cross-ref ("This operator is the **named content of the `map_solve` scope boundary**", `book/src/L1/assemble_frequency_operator.md:24`; "the per-ω `(operator, rhs)` family it parameterizes is the `map_solve_over_(operator,rhs)_family` superset scope boundary recorded at `book/src/L4/solve_family.md`", `:116`). This dispatch makes the L4→L1 direction of that bidirectional cross-ref a live link, which previously dangled as an abstract L0-only reference.
- **Single locus chosen deliberately.** The §Status "Scope (load-bearing)" paragraph is *the* canonical `map_solve` scope-boundary note (it is the paragraph that enumerates which pipelines are/aren't witnesses and names the `map_solve_over_(operator,rhs)_family` superset for the driven scope-out). The other three abstract mentions of `drivensolver.cpp:176-180` (line 65 shape-contract item 1, line 90 algebraic-law-2 consequence, line 137 variant-axis 1) are *law/typing* loci, not the scope-boundary note; per the task ("Keep it surgical — one cross-ref locus") and the lifter "do not restructure" discipline, I left them as-is. Naming the operator in those three additional loci would be defensible but would exceed the surgical single-locus instruction and risk decoration drift; if the integrator/planner wants the operator named at every per-ω-rebuild mention, that is a separate (larger) cross-ref pass — flagged in Open questions.
- **High→low discipline preserved.** The added link points downward (L4 → L1), consistent with the entry's existing downward cross-references; no reverse-direction (L1-lifts-into-L4) prose was introduced into the formal chapter.
- **No index-cell status flip** — this dispatch does not change `solve_family`'s `## Status` line, so no `book/src/L4/index.md` status-cell update is owed (the index-table-status-cell anti-drift guard only fires on a status flip).

## Supporting evidence
- `book/src/L1/assemble_frequency_operator.md` — firm L1 operator (frontmatter `firmness: firm`, line 4; `## Status: firm` line 132). Signature `assemble_frequency_operator :: FrequencyOperatorFamily[N] -> Scalar -> LinearOperator[N, N]` (`:32-33`); affine-in-ω framing `A(ω) = K + iω·C − ω²·M + A2(ω)` (`:18`, `:43-49`, `:73`); the operator-operand specialization of `linear_combination` (`:18`, `:60-69`); the reciprocal `map_solve` scope-boundary cross-ref (`:24`, `:116`). Landed cycle-062 D3 (harvester:2026-06-02T083220Z, provenance `:151`).
- `book/src/L4/solve_family.md:146` — the §Status "Scope (load-bearing)" paragraph being re-anchored (the `map_solve` scope-boundary note).
- `palace/drivers/drivensolver.cpp:175-180` — re-verified on-disk this dispatch via `palace-codemap` `read_range`: `:175` `A2 = GetExtraSystemMatrix(...)`, `:176-177` `A = GetSystemMatrix(1.0+0.0i, 1i*omega, -omega*omega+0.0i, K,C,M,A2)` (per-ω rebuild), `:180` `ksp.SetOperators(*A, *P)` (per-ω capture). The existing `drivensolver.cpp:176-180` citation in the note is accurate; it is preserved unchanged.
- c061 D3 / c062 D3 findings establishing the per-ω rebuild ≡ `assemble_frequency_operator` (cross-layer-cross-cutter `reports/2026-06-02T075145Z-cross-layer-cross-cutter-driven-transient-outer-machinery-probe/CYCLE.md`, cited in the L1 entry's provenance `:151`).

## Open questions / caveats
- **Additional per-ω-rebuild loci left un-named (by design, surgical scope).** Lines 65 (shape-contract item 1), 90 (algebraic-law-2 consequence), and 137 (variant-axis 1) each mention the driven per-ω operator rebuild (`drivensolver.cpp:176-180`) abstractly without naming `assemble_frequency_operator`. This dispatch deliberately re-anchored only the canonical §Status scope-boundary note per the surgical single-locus instruction. If a fuller cross-ref refresh is wanted (name the firm operator at all four per-ω-rebuild loci), that is a follow-up lifter pass — not blocking, low value (the scope-boundary note is the navigable home and now carries the link). No abstractor reread is needed: the firm L1 signature/framing matches every assumption these loci already encode (per-element, operator rebuilt inside the loop), so there is no signature contradiction — purely a question of cross-ref breadth, not correctness.
