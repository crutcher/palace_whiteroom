---
agent: cycle-planner
invoked_at: 2026-05-29T205713Z
scope: cycle-029 dispatch plan
status: pending
---

# Cycle 029 dispatch plan

## Goals selected this cycle

Cycle-029 is the **second primary cycle of meta-batch-8** (cycles 028/029/030). The cycle-028 integrator landed **7 of 7 dispatched reports clean**, completing five firming tasks (the deferred c027 D5 `incremental-least-squares-composition-lowering` re-anchor + three hygiene passes + three lowering-verifier audits) and surfacing **six actionable plan candidates** all fully unblocked (integrator-signals.md top section). This cycle dispatches **the highest-fan-out open work**: two HIGH-impact L1>L0 mutation-rotation themes (`back-solve` + `bilinear-form`) that unblock downstream lowering-verifier audits and Krylov-solver-specific optimization pathways, one prose-correction task to firm the normalize-mutation-rotation theme, and lower-fan-out obstruction documentation + leaf harvesting. The dispatch mix prioritizes the **shared Krylov/GMRES/FGMRES restart machinery** (`back-solve-mutation-rotation` + its pair concept harvests) and **foundational matrix algebra** (`bilinear-form-mutation-rotation`) — both HIGH-concept-overlap × HIGH-downstream-reuse per the roadmap's intermediate-tier impact model.

## Dispatches

1. **abstractor** — `back-solve-mutation-rotation` L1>L0 theme — **deps: none**
   - Scope: Author the firm L1 leaf `back_solve` (landed c027, audited c028) → L0 mutation-rotation lowering theme. L1 form: `back_solve :: Tensor[N,1], Tensor[N,N] -> Tensor[N,1]` (solve `R·y = s` where R is upper-triangular). L0 surface: the GMRES/FGMRES restart-correction back-substitution loops (palace/linalg/iterative.cpp:652-660 GMRES, :831-840 FGMRES per the c028 normalize audit's F3 range-parity anchor; also the generic forward-substitution path via `back_solve` applied to `L`-factors). Cite the restart-machinery intermediate-tier candidate (roadmap §Intermediate-tier algorithms "Sparse triangular solve" / c027 `back_solve.md`); the theme is the terminal step of the `incremental-least-squares-composition-lowering` L2>L1 theme (already firm c028 D1). HIGH fan-out: closes a core GMRES/FGMRES restart gap and unblocks the follow-on `back-solve-mutation-rotation-lowering-verifier-audit` (c030 candidate — do NOT dispatch here; note as a c030 follow-on).
   - Rationale: **Signaled unblocked** (integrator-signals.md c028 "Unblocked" #1; plan active-head prior c028). The firm `back_solve` leaf is now audited (c028); the theme authors its L0 lowering. Highest-priority Krylov infrastructure gap; co-keys the GMRES restart machinery (`incremental-least-squares` L2 firm → terminal back-solve; both firm c028).
   - Citation: integrator-signals.md:58, priorities.md (c028 active-head prior, now closed per integrator-signals.md:50 RESOLVED), roadmap.md:96 "Back-solve sub-case".

2. **abstractor** — `bilinear-form-mutation-rotation` L1>L0 theme — **deps: none**
   - Scope: Author the missing L1>L0 lowering theme for the firm L1 rough-in `bilinear-form` operator (status: `rough-in (test-coverage-bounded)` at book/src/L1/bilinear-form.md per the c028 survey). L1 form: `bilinear_form :: Form, Tensor[...] -> Scalar`. L0 surface: the operator-weighted inner-product evaluation pattern in `operator.cpp:599-619` (the `linalg::Norml2(comm,x,B,Bx)` energy-norm lowering, already anchoring the paired `matrix-weighted-norm-mutation-rotation` firm theme c026); Palace's `BilinearForm` wrapper (palace/fem/operator.hpp § BilinearForm class). The theme should document the bilinear-form action as a weighted inner product, inherit the MPI-collective reduction chain from the concept page, and surface any variant-axis (real/complex element-type, weighted/unweighted, Hermitian-vs-non-Hermitian forms) not yet absorbed. Test-coverage gate (c028 OQ narrowed :769) is out-of-scope (needs Palace source change); the mutation-rotation theme is in-scope + highest-impact missing step toward bilinear-form firmness.
   - Rationale: **Signaled unblocked** (integrator-signals.md c028 "Unblocked" #2 + "Suggested next dispatches" #2; c028 survey report-6 confirms absent on disk). Cheapest + highest-fan-out next step toward `bilinear-form` firmness (the test-coverage gates require out-of-scope Palace source edits). The paired `matrix-weighted-norm-mutation-rotation` theme is firm (c026), so the abstractor has a precedent structure to follow and the downward lowering path is mapped.
   - Citation: integrator-signals.md:59, c028-survey report-6 findings, OQ `bilinear-form-mutation-rotation-l1-l0-theme-needed-c028`, roadmap.md:52 "energy-norm consumers".

3. **abstractor** — `normalize_B` F1 prose correction on `normalize-mutation-rotation` + `L1/normalize.md` — **deps: none**
   - Scope: The c028 cycle-3 lowering-verifier audit (report-3) flagged a **does-not-support finding (F1)** on the `normalize_B` rough-in note: the theme prose claims "Palace has no fused B-weighted `linalg::Normalize` free function" (book/src/L1-L0/normalize-mutation-rotation.md:283-287 + book/src/L1/normalize.md:83-95), but the audit found a **defined-but-uncalled** fused `Normalize(comm,x,B,Bx)` at `palace/linalg/operator.hpp:377-384`. Rewrite the theme + L1 entry prose from "does not exist" → "exists but uncalled" + tighten the `normalize_B` promotion gate (currently gated on a positive Palace call site; the gate STAYS OPEN but prose is now accurate). The firm core of the theme (the unweighted `Normalize(comm,x)` / `nrm2∘scal` lowering) is unaffected; only the B-weighted sibling note is corrected.
   - Rationale: **Routed as follow-up** (integrator-signals.md:62 "Suggested next dispatches" #4; c028 cycle-3 report OQ `normalize_B-note-says-no-fused-B-Normalize-but-uncalled-fused-operator-exists`). Mechanical prose correction on an already-firm theme (no status change); the correction tightens the promotion gate semantics without loosening the bar.
   - Citation: c028 cycle-3 report-3 (normalize-mutation-rotation-audit) OQ append, integrator-signals.md:62.

4. **abstractor** — `triangular-solve-obstruction` L1>L0 theme — **deps: none**
   - Scope: Author the L1>L0 **obstruction theme** for the resolved-by-obstruction `trsv` leaf (c028 harvester report-7 confirmed negative finding: Palace has no standalone triangular-solve primitive — densematrix.hpp:24-36 API lists no `Solve(triangular)` method; triangular solves are opaque-library-owned via HYPRE relax-type flags for GS/SSOR or external direct-solver wrappers, or a block-triangular red herring). The theme documents the obstruction, cites HYPRE relax-type sites (negative anchors) + external direct-solver wrapper sites (e.g., palace/linalg/mumps.cpp, palace/linalg/superlu.cpp), and notes the sibling `back_solve` (dispatch #1 this cycle) as the concrete instantiation of one triangular-system component. Cross-reference to book/src/L3/index.md:7 (the existing L3 inventory gap row noting `trsv`-resolved-by-obstruction), and cite book/src/L1/back_solve.md for the terminal-solve sibling. This is a **claim-free obstruction documentation**, not a constructive theme.
   - Rationale: **Signaled unblocked** (integrator-signals.md c028 "Unblocked" #3 + "Suggested next dispatches" #3; c028 harvester report-7 resolves the `trsv` leaf as resolved-by-obstruction). LOW fan-out (the resolved-by-obstruction leaf feeds no upstream combinator), but gives the resolved-by-obstruction gap a citable documentation home. Cheaper alternative: accept the existing L3/index.md:7 line as already-sufficient (integrator-signals.md:68). **Judgment call**: dispatch the full theme for precision and completeness; the one-line L3 reference is less discoverable.
   - Citation: integrator-signals.md:61, c028 report-7 (harvester-trsv-l1-localization) OQ `triangular-solve-obstruction-l1-l0-theme-needed-c028`, roadmap.md:96 "trsv L3-inventory gap RESOLVED-BY-OBSTRUCTION".

5. **harvester** — `ls_update_column` column-streaming leaf — **deps: none**
   - Scope: Harvest the column-streaming leaf `ls_update_column` at L1 (the Face-1 running-QR accumulation step for GMRES/FGMRES incremental least-squares). The c028 D1 lifter's `incremental-least-squares-composition-lowering` L2>L1 theme forward-references this leaf plain-text (not yet harvested); authoring it tightens the fan-down and completes the incremental-least-squares composition. L0 anchor: palace/linalg/iterative.cpp running-QR streaming (the per-column Givens-update pattern that operates on the Hessenberg/H-bar matrix column-by-column as it accumulates). Small leaf, cokey-ed with the `back_solve` cohort (both are GMRES restart components). **Note**: the `back_solve` leaf (c027 renamed from `ls_update_column`) is the TERMINAL solve step; `ls_update_column` is the PER-COLUMN update step. These are distinct roles in the running-QR stream.
   - Rationale: **Signaled unblocked** (integrator-signals.md c028 "Unblocked" #5 + "Suggested next dispatches" #5; c028 D1 lifter report names it as plain-text forward-ref). Completes the l2-named-composition-lifts cohort (`orthogonalize` firm c019 + `incremental-least-squares` firm c026 → running-QR now needs the two-step `ls_update_column` + `back_solve` breakdown at L1). MEDIUM fan-out: unblocks completion of the GMRES restart machinery L1-vocabulary.
   - Citation: integrator-signals.md:63, c028 cycle-1 report-1 (lifter-incremental-ls-composition-lowering-reanchor) STAGING notes + open-questions OQ append, priorities.md planned dispatch #5.

6. **lifter** or **layer-intro-author** — `L2-L1/index.md` + `L2/index.md` prose refresh — **deps: #1 lands**
   - Scope: Refresh the L2>L1 Part overview prose and the L2 Part overview prose to re-sync with current artifact state. The cycle-028 D1 lifter created a new firm L2>L1 theme file (`incremental-least-squares-composition-lowering`), raising the Part's firm-theme count from 7 to 8 (7 firm + 1 partly-constructive). The roadmap lead prose at `book/src/L2-L1/index.md` introduction still reads stale "2 firm themes" (from cycle-018 era); the actual count is 8 (7 firm + `deflate-composition-lowering` partly-constructive per the c022 partial-promotion). Similarly, the L2 Part overview should note the now-firm `incremental-least-squares` operator (firm c026) as completed within the l2-named-composition-lifts cohort. **Navigational prose refresh only; no structural edits to dep-maps or status lines** — the dep-maps are already authoritative.
   - Rationale: **Routed as quality-of-navigation follow-up** (integrator-signals.md c028 "Suggested next dispatches" #6). Prose re-sync is LOW-priority + hygiene; dispatch **only if there is cycle capacity** after the five higher-fan-out dispatches above complete. This is a wave-2 candidate.
   - Citation: integrator-signals.md:65, c028 cycle-1 report-1 (lifter-incremental-ls-composition-lowering-reanchor) integration notes + the artifact state (L2-L1 index Part, L2 Part overview prose).

## Overlap analysis

- **Dispatches #1 + #2 (abstractor, back-solve-mutation-rotation + bilinear-form-mutation-rotation):** Both author NEW L1>L0 mutation-rotation themes. The artifacts touched are disjoint: dispatch #1 creates `book/src/L1-L0/back-solve-mutation-rotation.md` + registers at `book/src/L1-L0/index.md`; dispatch #2 creates `book/src/L1-L0/bilinear-form-mutation-rotation.md` + registers at the same index. **Both append to the same L1-L0 Part index.md and SUMMARY.md files.** The integrator-per-report (serial) will re-read both index and SUMMARY between report 1 and report 2 → clean append parity. **Potential conflict at integration**: if both propose identical L1-L0/index.md row-append locations (both wanting to append a new firm row to the same section), the second report's integrator will find the index already has the first report's row and must append cleanly after it. Both reports should document their append plan; the integrator will apply row-wise, not blind-search. **PARALLEL dispatch is safe**: both are leaf-new-files + append-only to shared infrastructure; serial per-report integration handles the serialization. No concurrent edits to the same file line-range.

- **Dispatches #1 + #3 (abstractor, back-solve-mutation-rotation + normalize_B prose correction):** Disjoint artifacts: dispatch #1 is a NEW file + index register; dispatch #3 edits EXISTING files (`L1-L0/normalize-mutation-rotation.md` + `L1/normalize.md`). **NO overlap.** Can run in parallel.

- **Dispatches #1 + #5 (abstractor/harvester, back-solve-mutation-rotation + ls_update_column leaf):** Dispatch #1 authors the terminal-solve step of the incremental-least-squares machinery; dispatch #5 harvests the per-column update step. **Both touch `book/src/L1/index.md` and SUMMARY.md** (each harvester/abstractor registers their new L1 entry). **Same serial-per-report integration behavior as #1+#2** — both re-read SUMMARY and index between reports. **NO line-range overlap**, disjoint new files. **PARALLEL dispatch is safe**.

- **Dispatches #2 + #4 (abstractor, bilinear-form-mutation-rotation + triangular-solve-obstruction):** Disjoint artifacts: #2 creates `book/src/L1-L0/bilinear-form-mutation-rotation.md`; #4 creates `book/src/L1-L0/triangular-solve-obstruction.md` (obstruction theme, claim-free). Both append to L1-L0 index and SUMMARY (same serial-per-report re-read pattern as #1+#2). **NO line-range overlap**, disjoint new files. **PARALLEL dispatch is safe**.

- **Dispatch #3 (abstractor, normalize_B prose correction):** Edits EXISTING files in place. **Does not touch any file created or modified by dispatches #1, #2, #4, #5, or #6.** Clean parallel with all.

- **Dispatch #4 (abstractor, triangular-solve-obstruction):** Creates new file + indexes; **does not overlap with #1, #2, #3, #5** (all disjoint). **Does depend on #1 for cross-reference accuracy** if dispatch #1 is intended to note the sibling `back_solve` relationship in the triangular-solve-obstruction theme. **Judgment**: #1 and #4 are thematically related (back-solve as sibling to the unimplemented trsv), but neither MUST land before the other for artifact correctness. Dispatch #1 lands a firm mutation-rotation theme that stands alone; dispatch #4 lands an obstruction theme that cites back_solve as a sibling for context. **If #4 forward-references the file name of #1's output**, then #1 MUST land first (forward-reference resolution). **If #4 only cites back_solve via plain-text**, then #4 is PARALLEL. **Current judgment: dispatch #4 should forward-reference #1's file by citation slug (not a live link, since #1 is not yet on disk at dispatch-4 planning time), and the integrator-per-report will upgrade the plain-text reference once #1 lands.** **SEQUENCING**: #1 must land before #4 to resolve the forward-reference cleanly. Place them in different waves: #1 in wave-1, #4 in wave-2.

- **Dispatch #5 (harvester, ls_update_column):** The c028 D1 lifter forward-referenced this leaf plain-text in the `incremental-least-squares-composition-lowering` theme (already firm, integrated c028 D1). Dispatch #5 harvests the leaf. **No conflict with #1 (disjoint new files); no conflict with #2, #3, #4, #6.** **PARALLEL safe with #1, #2, #3, #6**; **potentially wave-ordered after #4 if #4's theme text cites ls_update_column** (unlikely — #4 is trsv obstruction, unrelated to incremental-least-squares). **PARALLEL with all is safe.**

- **Dispatch #6 (lifter/layer-intro-author, L2-L1/L2 index prose refresh):** Edits EXISTING Part overview files (`book/src/L2-L1/index.md` intro prose + `book/src/L2/index.md` intro prose). **Touches SHARED infrastructure files** (the intro sections). **Does not conflict with #1–#5** (they create new chapters in same Parts; #6 refreshes Part overview prose only, not the dep-map structure). **POTENTIAL serialization if #1 (back-solve-mutation-rotation theme, which lands a new L1>L0 row) is intended to be cited in the L2-L1 Part intro prose refresh**. If #6 wants to say "the L2>L1 Part now has 8 firm themes" as evidenced by the new #1 landing, then #1 should land before #6 (wave-ordered). **Judgment**: #6 is LOW-priority hygiene prose-refresh; it should land AFTER #1 so the prose can accurately cite the new theme count. **SEQUENCING**: #1 in wave-1, #6 in wave-2.

**Summary:**
- **Wave-1 (parallel):** Dispatches #1, #2, #3, #5 (all append to L1-L0/index.md and SUMMARY.md in serial per-report order; no line-range conflicts; #3 edits existing L1-L0 theme in-place, disjoint from new-file creates).
- **Wave-2 (parallel, depends on wave-1):** Dispatches #4, #6 (both can run in parallel with each other; #4 forward-references #1's sibling relationship; #6 prose-refreshes L2-L1/L2 index now that #1 has landed; both safe to run together after wave-1).

## Sequencing schedule

**Wave-1 (4 dispatches, parallel):**
- `abstractor` — `back-solve-mutation-rotation` L1>L0 theme (dispatch #1)
- `abstractor` — `bilinear-form-mutation-rotation` L1>L0 theme (dispatch #2)
- `abstractor` — `normalize_B` F1 prose correction (dispatch #3)
- `harvester` — `ls_update_column` L1 column-streaming leaf (dispatch #5)

**Wave-2 (2 dispatches, parallel, after wave-1 reports land):**
- `abstractor` — `triangular-solve-obstruction` L1>L0 theme (dispatch #4; forward-references #1's `back_solve` theme for sibling relationship)
- `lifter`/`layer-intro-author` — `L2-L1/index.md` + `L2/index.md` prose refresh (dispatch #6; re-syncs Part overview prose now that #1 has landed + counts updated)

**Integrator:** One `integrator-finalize` at cycle end (per user directive 2026-05-27 — waves are dispatch ordering, not multiple finalizes). The finalize rebuilds the book, commits, pushes, updates roadmap/cycle-record/integrator-signals/log, and appends batch-record CYCLE.md.

## Open questions / caveats

1. **Dispatch #6 (index prose refresh) is LOW-PRIORITY hygiene.** If wave-1 reports reveal integration friction or build breakage, defer #6 to cycle-030. The primary cycle mission is #1–#5 (the two HIGH-fan-out themes + prose correction + leaf harvest). **Judgment**: keep #6 in the plan as a wave-2 candidate; promote to wave-1 only if integrator signaling after wave-1 reports indicates capacity.

2. **Dispatch #4 (triangular-solve-obstruction theme) carries a "cheaper alternative" caveat** (integrator-signals.md:68 notes "Accept the existing L3/index.md:7 line as already-sufficient"). The L3 inventory row exists and documents `trsv`-resolved-by-obstruction. **The decision to dispatch #4 (vs. accepting the single-line L3 note) is a judgment call on documentation precision vs. cycle throughput.** Arguments for dispatch: gives the obstruction a citable theme home (precedent: cycle-004's MINRES/BiCGStab obstruction themes). Arguments for deferral: the L3/index.md row already documents the gap; incremental value is low. **Current judgment: dispatch #4 for completeness and precedent consistency (obstruction themes are documented at L1>L0, not just inventory notes).** If planner reassesses, move to cycle-030 or mark as deferred-contingent in the plan.

3. **Forward-reference handling in dispatch #4.** The `triangular-solve-obstruction` theme will note `back_solve` as a concrete sibling component. Dispatch #1 (`back_solve-mutation-rotation` theme) is in wave-1; dispatch #4 is in wave-2. By the time wave-2 integrators run, #1 will have landed on disk (book/src/L1-L0/back-solve-mutation-rotation.md). **Dispatch #4 can cite #1 as a live link.** The integrator-per-report will verify the link resolves. No plain-text forward-reference needed; the dependency is satisfied by wave-ordering.

4. **Cycle-029 audit follow-ons (do NOT dispatch this cycle):** The two HIGH-fan-out themes landing this cycle (#1 `back-solve-mutation-rotation` + #2 `bilinear-form-mutation-rotation`) will become audit candidates for cycle-030. **Firm themes get a standard `verified_against:` lowering-verifier audit in the next cycle.** Do not schedule the audits now; note for the c030 planner via integrator-signals. **Judgment**: the audits are automatic follow-ons; the planner priority signal is that these themes will gate c030 audits (medium fan-out precedent audits), so if c030 runs tight, the audits may slip.

5. **`ls_update_column` vs. `back_solve` naming collision (resolved c027).** The c027 D4 harvester (back_solve) renamed the leaf from `ls_update_column` to resolve a slug collision with the D5 theme's column-streaming forward-reference. **Dispatch #5 (this cycle) reserves the `ls_update_column` slug for the column-streaming leaf, distinct from the terminal-solve `back_solve` leaf.** The naming is now clean: `back_solve` (terminal L1 leaf) and `ls_update_column` (column-streaming L1 leaf, Face-1 of the incremental-least-squares composition). **No cycle-029 friction expected; caveat noted for historical record.**

6. **normalize_B promotion gate stays OPEN.** Dispatch #3 corrects the prose from "does not exist" to "exists but uncalled" but does NOT promote `normalize_B` from rough-in to firm. The promotion gate remains: **find a positive Palace call site or evidence that the B-weighted variant is used.** The existing gate was correct; only the prose was inaccurate. **No gate change this cycle.**

---

**No ask-class caveats. All six dispatches are fan-out-ranked, fully unblocked, and integrate cleanly per the overlap analysis.**
