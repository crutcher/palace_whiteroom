---
agent: cycle-planner
invoked_at: 2026-06-04T000130Z
scope: cycle-086 dispatch plan
status: pending
---

# Cycle 086 dispatch plan

Cycle-086 is the **SECOND primary cycle of meta-batch-27** (cycles 085/086/087; the batch-27 meta-phase fires AFTER cycle-087's finalize). Cycle-085 spent the batch-27 LEAD (the all-12-column FEATURE-SURFACE SPINE re-evaluation under the OWN-COMPOSITION promotion rule: 6 columns promoted `seed`→`firm`). The standing forward frontier resumes. Land-clean discipline does NOT apply yet (that is c087) — c086 takes a substantive forward-frontier pick.

## Goals selected this cycle

Take the single highest-fan-out, genuinely-open, cleanly-describable forward-frontier pick the c085 finalize surfaced: a **`lowering-verifier` list-homomorphism / firm-on-positive-structure law-confidence pass on `book/src/L4/solve_family.md`** (currently `rough-in (test-coverage-bounded)`), the c082/c083 reduce-verb firm-on-positive-structure analog. Firming `solve_family` firms a widely-consumed L4 outer-driver combinator and discharges ONE of the TWO own-constituent gates on the `electrostatic` + `magnetostatic` driver columns. The plan couples the verdict-conditional consumer re-anchor (form (ii) of `floor-landing-implies-same-cycle-adjacent-entry-reanchor`) as a wave-2 `lifter` dispatch. This is a deliberately FOCUSED 2-dispatch plan because the bottom-up frontier is genuinely light (reduce-verb law-confidence route EXHAUSTED for the 2 all-primitives-firm verbs c082/c083; `matrix-weighted-norm` √-cascade NO-GO HELD; 5-driver→L4 AFFIRMED-COMPLETE c082 — re-proposing forbidden).

## HONEST FINDING — firming `solve_family` does NOT by itself flip electrostatic/magnetostatic

The task asked to determine whether firming `solve_family` alone unblocks the electrostatic/magnetostatic column promotions, or whether `gram_reduce` still gates them. **On-disk verdict (pasted evidence below): `gram_reduce` STILL gates them.** Both `electrostatic.L4.md` and `magnetostatic.L4.md` carry TWO `rough-in (test-coverage-bounded)` own-constituents in their dep-maps: `solve_family` AND `gram_reduce`. Their §Status both say verbatim: "firming `solve_family` + `gram_reduce` is the promotion route." So:

- Firming `solve_family` (D1) discharges the FIRST of two own-constituent gates → the columns move from "two own-constituents rough-in" to "ONE own-constituent rough-in (`gram_reduce`)". The column `status: seed` does **NOT** flip this cycle.
- `gram_reduce` is **NOT** cleanly firmable this cycle: it is `rough-in (test-coverage-bounded)` but it FOLDS `matrix-weighted-norm` (plain `rough-in`) + `bilinear-form` (plain `rough-in`) — and a reduction is as firm as its least-firm folded primitive. The c082/c083 firm-on-positive-structure escape applies ONLY when the folded primitives are all firm; here they are not. `gram_reduce`'s law-confidence escape is therefore blocked on `matrix-weighted-norm` firming, which is the **NO-GO-HELD √-cascade** (the convergent foundation blocker; c080 D1 ruled the escape INAPPLICABLE for `matrix-weighted-norm` because its triangle/Cauchy–Schwarz/parallelogram laws are inner-product-structure theorems the L0 source only numerically asserts). So NO `gram_reduce` law-confidence dispatch this cycle — it would predictably hit the same wall as `domain_energy_reduce`.

D1 is still the highest-fan-out available pick: it firms a widely-consumed combinator, discharges one of two gates on two columns (moving them one gate from promotion), and either outcome is a clean spine finding. The `electrostatic`/`magnetostatic` column FLIP is NOT scheduled this cycle and is NOT claimed — it remains gated on the `gram_reduce` → `matrix-weighted-norm` cascade.

## Deliverable-presence verification (paste-inline-evidence)

### D1 — `solve_family` law-confidence pass — ALL FOUR CHECKS PASS, genuinely-open-verified

**(1) File existence:**
```
-rw-rw-r-- 1 crutcher crutcher 34282 Jun  3 ... book/src/L4/solve_family.md
EXISTS (exit 0)
```

**(2) Maturity / Status line (read from disk):**
```
4:firmness: rough-in (test-coverage-bounded)
```
The §Status (`:144`) names the promotion route: "a literature/strawman-derivation harvester pass that raises the list-homomorphism law confidence to `ksp_solve`-equivalent (the map is a standard total list combinator, so the strawman-derivation route is the more likely one)" AND "The default may be revisited by a batch-17 lowering-verifier pass confirming the `KspSolver`-reuse carries no cross-element state." The dispatch deliverable (firm OR a recorded ceiling) is ABOVE the on-disk maturity — NOT a no-op.
**(2b) `verified_against:` count (audit-already-discharged check):**
```
grep -c '^verified_against:' book/src/L4/solve_family.md  →  0
```
Zero prior audit blocks — no law-confidence pass has been run. Genuinely open.

**(3) OQ-ledger RESOLVED-grep:**
```
grep 'solve-family.*RESOLVED|solve-family.*CLOSED|solve-family-list-homomorphism' open-questions.md
```
No CLOSED/RESOLVED hit for `solve-family-list-homomorphism-law-confidence-pass`. The only `solve-family` closure-index hits are for OTHER items (the c073 D5 §Specializations citation lint `solve-family-md-specialization-note-plus-one-anchor-drift` CLOSED; the dissolution-theme landing) — NOT this law-confidence pass. Genuinely open.

**(4) Structural-block check:** route is IN-SCOPE. The §Status names the law-confidence route AND the firm-on-positive-structure escape DOES apply here: "the concatenation-homomorphism *is* a syntactic identity on `map` (and the operator-capture-once hoist *is* read directly off the positive `SetOperators`-outside-the-loop source)". This is the c082/c083 escape (syntactic identity on positive structure), DISTINCT from the matrix-weighted-norm NO-GO (whose laws are inner-product-structure theorems the source only numerically asserts — escape ruled INAPPLICABLE c080 D1). Codemap-confirmed positive anchors (on-disk this plan): `electrostaticsolver.cpp:35-36` (`KspSolver ksp(...)` + `ksp.SetOperators(*K,*K)` — operator captured once, OUTSIDE the `:60` `for(...GetSources())` family loop; `ksp.Mult(RHS, V[step])` per-element solve `:69`); `magnetostaticsolver.cpp:36` (byte-identical `ksp.SetOperators(*K,*K)` hoist). NOT on the STOP-PROPOSING negative list (that list = `lu_solve`/`back_solve`/`ls-update-column`/`nleps_*` L3 backfills + `L2/fe_assemble` + `L2/fold_solve` NO-ENTRY warrants; `solve_family` is none).

### D2 — consumer re-anchor (lifter) — open by construction (verdict-conditional follow-on coupled to D1 this cycle)

D2 is the same-cycle adjacent-entry re-anchor coupled to D1's landing (`floor-landing-implies-same-cycle-adjacent-entry-reanchor`, form (ii) — a co-scheduled lifter, because the re-anchor is conditional on D1's verdict and spans 3 files + narrative, exceeding the mechanical-bundle threshold). Stale-label inventory (pasted, the lines that go stale IF D1 firms `solve_family`):
```
book/src/L4/gram_reduce.md:8     (consumes-row: solve_family rough-in (test-coverage-bounded))
book/src/L4/gram_reduce.md:202   (dep-map: solve_family rough-in (test-coverage-bounded))
book/src/feature/electrostatic.L4.md:8, :39, :56, :63, :69   (solve_family rough-in labels + "firming solve_family + gram_reduce is the promotion route" clause)
book/src/feature/magnetostatic.L4.md:8, :39, :56, :63, :69   (mirror)
```
D2 is open-by-construction: it is the conditional consumer re-anchor of D1's status flip; if D1 does NOT firm (records the ceiling instead), D2 records the unchanged state (no-op). No prior-cycle history.

## Dispatches

**D1 — (`lowering-verifier`, "L4 combinator: `solve_family` list-homomorphism / firm-on-positive-structure law-confidence pass", deps: none)**
- **scope**: Audit `book/src/L4/solve_family.md` (`rough-in (test-coverage-bounded)`, 0 `verified_against:` blocks) for the firm-on-positive-structure / list-homomorphism law-confidence promotion. Judge whether the load-bearing **concatenation-homomorphism** (law 1) + the **operator-capture-once / `SetOperators`-hoist** (law 2) + element-independence/order-preservation (law 3) discharge to `ksp_solve`-equivalent confidence via the strawman-derivation route — the map is a standard total list combinator and the laws are SYNTACTIC IDENTITIES on `map`, read directly off the positive `SetOperators`-outside-the-loop source (electrostatic `electrostaticsolver.cpp:35-36`/`:60`/`:69` + magnetostatic `magnetostaticsolver.cpp:36` — codemap-confirmed on-disk this plan). The ONE thing the integration-level-only coverage leaves test-unconfirmed is the load-bearing claim the laws ENCODE: that each element's solve is genuinely independent given the shared `op` (no hidden cross-element state in the reused `KspSolver`). The verifier's judgment: does the strawman-derivation + the positive `SetOperators`-hoist source discharge the no-cross-element-state claim (the in-scope analog of the missing driver-level unit test)? IF YES → promote `solve_family` `rough-in (test-coverage-bounded)` → `firm`, emit a `verified_against:` block citing the strawman §3.7 list-homomorphism derivation + the two positive driver-sweep hoist anchors. IF NO (the `KspSolver`-reuse cross-element-state claim cannot be discharged without a positive test) → RECORD that verdict cleanly in the §Status + a `verified_against:`/findings note, leave the verb `rough-in (test-coverage-bounded)` (NOT a forcing — a clean spine finding). **Touches `book/src/L4/solve_family.md` ONLY** (the verb file + its frontmatter + §Status). Does NOT touch the consumer/feature files (D2's job). Does NOT flip any feature column's `status: seed` (gram_reduce still gates).
- **rationale**: the highest-fan-out genuinely-open cleanly-describable forward-frontier pick (priorities active-head Backlog candidate `solve-family-list-homomorphism-law-confidence-pass`, appended by the c085 planner; reaffirmed by the c085 finalize integrator-signals §Suggested-next-dispatches as "the highest-fan-out next column-promotion lever"). The c082/c083 firm-on-positive-structure analog. Either outcome is clean.

**D2 — (`lifter`, "consumer re-anchor of `solve_family` maturity in gram_reduce + electrostatic.L4 + magnetostatic.L4 per D1's verdict", deps: D1)**
- **scope**: Re-anchor the `solve_family` maturity labels in the consumers IF AND ONLY IF D1 firms `solve_family`. Edit (a) `book/src/L4/gram_reduce.md:8`, `:202` (the `solve_family` consumes-row + dep-map row: `rough-in (test-coverage-bounded)` → `firm`); (b) `book/src/feature/electrostatic.L4.md:8`, `:39`, `:56`, `:63`, `:69` and (c) `book/src/feature/magnetostatic.L4.md:8`, `:39`, `:56`, `:63`, `:69` — flip the `solve_family` maturity labels to `firm` AND re-narrate the §Status / §"lowers cleanly outward" promotion-route clause from "firming `solve_family` + `gram_reduce` is the promotion route" → "`solve_family` now FIRM (c086); the column stays `seed` on the REMAINING own-constituent gate, `gram_reduce` (`rough-in (test-coverage-bounded)`, itself gated on the `matrix-weighted-norm` √-cascade NO-GO-HELD)". **DO NOT flip either column's `status: seed`** — `gram_reduce` is the remaining own-constituent gate; the column promotion is NOT unblocked by `solve_family` alone (the honest finding above). IF D1 does NOT firm (records the ceiling): D2 is a NO-OP confirming the unchanged `rough-in (test-coverage-bounded)` labels (emit a one-line "no re-anchor — D1 recorded the ceiling; solve_family stays rough-in (test-coverage-bounded)" note, no edits).
- **rationale**: the same-cycle adjacent-entry re-anchor coupled to a floor/maturity landing (`floor-landing-implies-same-cycle-adjacent-entry-reanchor`, form (ii) — co-scheduled lifter because the re-anchor spans 3 files + narrative and is verdict-conditional, exceeding the mechanical-bundle threshold). Prevents the cross-cycle stale-assertion window (consumers asserting `solve_family rough-in` after it firms). The honest non-flip of `status: seed` is load-bearing — it keeps the columns' promotion-route prose accurate to the remaining `gram_reduce` gate.

## Overlap analysis

- **D1 ↔ D2**: D1 touches `book/src/L4/solve_family.md` ONLY (the verb's frontmatter + §Status). D2 touches `book/src/L4/gram_reduce.md` + `book/src/feature/electrostatic.L4.md` + `book/src/feature/magnetostatic.L4.md` (the CONSUMERS) and does NOT touch `solve_family.md`. **No file-region overlap.** BUT D2 is verdict-CONDITIONAL on D1 (D2 re-anchors the consumer labels to whatever maturity D1 lands; if D1 firms, D2 flips the labels; if D1 records the ceiling, D2 is a no-op). This is a genuine **forward-reference / verdict dependency**, so D2 is SEQUENTIAL after D1 (wave 2). No shared operator-name authoring conflict (D1 names/promotes `solve_family`; D2 only references it). No shared consolidated-index tally (neither writes a layer-index running count; the `feature/index.md` matrix is NOT touched — no column `status` flips, so no matrix-cell drift; `L4/index.md`'s firm count IS bumped by D1's promotion but that is the integrator-finalize's housekeeping, not a producer write).

## Sequencing schedule

- **Wave 1 (single dispatch)**: D1 (`lowering-verifier` on `solve_family`). The substantive forward-frontier pick.
- **Wave 2 (single dispatch, after D1's report lands)**: D2 (`lifter` consumer re-anchor, conditional on D1's verdict). The per-report integrator wires D2's re-anchor against D1's already-applied status flip.

Two waves, one dispatch each — the verdict dependency is the only sequencing constraint.

## Open questions / caveats

- **The `electrostatic`/`magnetostatic` column FLIP is NOT scheduled and NOT claimed this cycle.** Firming `solve_family` discharges only ONE of their two own-constituent gates; `gram_reduce` (rough-in, folding rough-in `matrix-weighted-norm` + `bilinear-form`) remains. The columns' promotion is convergently gated on the `matrix-weighted-norm` √-entry-point cascade (NO-GO-HELD batch-26). This is recorded honestly here so the batch-27 meta-phase (fires after c087) can weigh whether the convergent blocker (now gating `gram_reduce` → capacitance/inductance/electrostatic/magnetostatic AND `domain_energy_reduce` → energy-fields) has accumulated enough downstream demand to justify the dedicated √-cascade own-cycle wave under its sharpened re-weigh trigger.
- **Firm-token-choice question (carried from c085, flagged by all 3 c085 dispatches):** whether `firm` is the right promoted token for a composition-root feature column, or a feature-specific token, is a batch-27 meta-phase decision (OQ `feature-column-firm-token-choice-batch-27-meta-phase`, status needs-more). It does NOT affect this cycle (D1/D2 operate on the L4 vocabulary `solve_family` verb + its consumer labels, NOT a feature-column `status` flip). Noted for the meta-phase.
- **If D1 firms `solve_family`**: the `L4_rough_in_test_coverage_bounded` cycle-record count is currently `1` on disk while THREE L4 entries carry `rough-in (test-coverage-bounded)` (`solve_family`, `gram_reduce`) + one plain `rough-in` (`domain_energy_reduce`). This count appears to track only a sub-cohort (the reduce-family main-cohort verbs, not the outer-driver `solve_family` row). The integrator-finalize should reconcile the `counts_after` L4 sub-tallies when D1 lands (out of planner scope; flagged for finalize). NOT a blocker for the plan.
- **Single substantive pick is the honest size this cycle.** The bottom-up frontier is genuinely light (the reduce-verb law-confidence route is exhausted for the all-firm-primitive verbs; the remaining reduce-verb tail is √-cascade-gated; 5-driver→L4 is affirmed-complete). Manufacturing a second parallel substantive dispatch would risk re-proposing absorbed/closed work. D1 + its coupled re-anchor (D2) is the right-sized substantive cycle before the c087 land-clean cycle.
