# Cycle-024 integrator staging log

Per-report integration landings, newest LAST (append-only). Read by integrator-finalize to reconcile the cycle.

---

## 2026-05-29T105500Z-harvester-nleps-jacobian-action-l1
applied_at: 2026-05-29T11:30:00Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L1/nleps_jacobian_action.md (new — firm L1 operator chapter; full body written verbatim from the `new:` fence — plain Markdown sections, the only fenced blocks are the three nested ` ```text ` Signature/Semantics blocks, all balanced)
- book/src/L1/index.md (edit ×3 — Firm count 17→18 headline (line 31) + cohort one-liner extended; cohort bullet after `nleps_deflated_solve` bullet / before the `**Rough-in (test-coverage-bounded)**` header; dep-map row after `nleps_deflated_solve` row / before `lanczos_step` row)
- book/src/SUMMARY.md (edit — `[nleps_jacobian_action]` entry after `[nleps_deflated_solve]` (line 78), under the L1 Part, before the `# L1 > L0 — Lowering` header)
- scaffolding/open-questions.md (append — 4 OQ sections)

Gate hits:
- (none non-zero) — no retroactive-budget hit (new firm entry + additive registration rows, no retroactive evidence edits); no concept_writes-on-existing-slug (brand-new slug); no forward-edge-without-surface (all 9 cross-reference targets verified on disk: apply_nonlinear_pencil, nleps_deflated_residual, nleps_deflated_solve, lu_solve, ksp_solve, apply_linop, eigsolve at L1; linear_combination at L2; eigensolver-wrapper at L0); no edge-label/prose mismatch (single-layer L1 entry, no lowering edge); no H1-reuse (H1 `# nleps_jacobian_action` = slug, distinct from the L1 Part heading); no append-on-missing-slug; no variant-axis-missing (two live axes deflation-present + damping-present, three collapsed axes enumerated); bookkeeping complete (Firm count + cohort bullet + dep-map row + SUMMARY all applied); no SUMMARY auto-fix needed (report proposed the SUMMARY edit explicitly); no index-placeholder displacement; no implied-component stub created.

Count delta:
- L1 firm: 17 → 18 (on-disk headline now reads `**Firm (18)**`). New firm operator: `nleps_jacobian_action` (the fourth NEP-interior atom at L1, after apply_nonlinear_pencil / nleps_deflated_residual / nleps_deflated_solve).

Open questions promoted:
- nleps-jacobian-action-firm-landed-cycle-024 (landing record)
- nleps-interior-atoms-jacobian-action-half-resolved-eigenvalue-correction-remains (partial-closure carry-forward of `:779`)
- apply-nonlinear-pencil-law-5-jacobian-followup-now-realized (low-fan-out back-reference bookkeeping)
- nleps-jacobian-action-mutation-rotation-l1-l0-lowering-theme (future L1>L0 abstractor candidate)

Build-relevant: yes

Notes:
- SHARED-FILE COORDINATION (CRITICAL — for the NEXT report, the parallel `nleps_eigenvalue_correction` harvester): `book/src/L1/index.md` Firm-count cell + cohort-list + dep-map AND `book/src/SUMMARY.md` are ALSO edited by `nleps_eigenvalue_correction`. I took the additive `17→18` bump for `nleps_jacobian_action` (the report's claimed increment). **The next report (eigenvalue-correction) MUST bump `18→19`** — it should re-read disk FIRST (the on-disk headline now reads `**Firm (18)**`), then take `18→19`, and anchor ITS cohort bullet + dep-map row + SUMMARY line AFTER `nleps_jacobian_action`'s (which now sit immediately after `nleps_deflated_solve`'s, in cohort-bullet / dep-map-row / SUMMARY-line order). The two harvesters' inserts are textually non-overlapping by construction (sequential after the prior NEP atom); only the single Firm-count cell needs the additive reconciliation. If for any reason the eigenvalue-correction report does NOT land firm this cycle, the on-disk `**Firm (18)**` stands as-is (no reconciliation needed).
- PARTIAL OQ CLOSURE: this landing closes the `nleps_jacobian_action` half of the standing cohort OQ `nleps-interior-atoms-remaining-jacobian-action-and-eigenvalue-correction` (open-questions.md:779). I did NOT edit the `:779` entry in place (append-only between meta-phases; meta-phase owns migration) — I appended a landing record + a partial-closure carry-forward OQ. If the parallel `nleps_eigenvalue_correction` harvester also lands firm this cycle, finalize/meta-phase can close the whole `:779` cohort OQ.
- REALIZES a deferred follow-up: this entry firms the `T'` derivative-pencil + Jacobian action that `apply_nonlinear_pencil` law 5 (`book/src/L1/apply_nonlinear_pencil.md:65`) recorded as a deferred follow-up. The harvester correctly did NOT edit `apply_nonlinear_pencil.md` (one operator per invocation); a future lifter pass may add the back-reference (captured as OQ `apply-nonlinear-pencil-law-5-jacobian-followup-now-realized`).
- Status rationale on-disk: `firm` via the firm-on-positive-structure escape (single positive site `palace/linalg/nleps.cpp:649-669`, every constituent read-not-constructed, laws are syntactic operator-algebra identities). The divided-difference `A2'` (quasi-Newton approximate Jacobian, `δ = √ε`) is recorded as a load-bearing **non-law**, NOT a `partly-constructive` caveat (no negative-anchor-materialized sub-part) and NOT a test-gated `rough-in` (the laws do not depend on convergence behaviour). NLEPS no-dedicated-test caveat inherited and non-gating.
- deferred integrated_at to finalize per role-spec (did NOT touch the consumed report's frontmatter).

---

## 2026-05-29T105500Z-harvester-nleps-eigenvalue-correction-l1
applied_at: 2026-05-29T11:50:00Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L1/nleps_eigenvalue_correction.md (new — firm L1 operator chapter; full body written verbatim from the `new:` fence — plain Markdown sections, the only fenced blocks are the two nested ` ```text ` Signature/Semantics blocks, both balanced, total fence count 4 / even parity). **Forward-ref upgrade applied:** the report authored the `nleps_jacobian_action` producer reference as plain-text per the `rough-in-forward-reference-must-be-plain-text-not-live-link` convention (sibling not yet on disk at authoring); since the parallel `nleps_jacobian_action` report landed firm on disk immediately before this one (prior staging row), I **upgraded the two cross-references** (§Context atom-(1), §Dependencies `jac_action` producer) to live links `[nleps_jacobian_action](./nleps_jacobian_action.md)` and removed the now-stale "(plain-text forward-reference — that chapter is not yet on disk)" qualifier in §Dependencies. The §Evidence descriptive prose mention ("the `nleps_jacobian_action` atom is the producer") stays inline-code (prose, not a cross-reference). Link target verified on disk — no `mdbook-linkcheck2` risk.
- book/src/L1/index.md (edit ×3 — Firm count 18→19 headline (was `**Firm (18)**` on disk after the parallel jacobian-action landing; reconciled additively to `**Firm (19)**`) + cohort one-liner extended; cohort bullet after the `nleps_jacobian_action` bullet / before the `**Rough-in (test-coverage-bounded)**` header; dep-map row after the `nleps_jacobian_action` row / before the `lanczos_step` row)
- book/src/SUMMARY.md (edit — `[nleps_eigenvalue_correction]` entry after `[nleps_jacobian_action]` (line 79), under the L1 Part, before the `# L1 > L0 — Lowering` header)
- scaffolding/open-questions.md (append — 3 OQ sections)

Gate hits:
- (none non-zero) — no retroactive-budget hit (new firm entry + additive registration rows, no retroactive evidence edits); no concept_writes-on-existing-slug (brand-new slug); no forward-edge-without-surface (all cross-reference targets verified on disk: dot, axpby, scal, axpbypcz, ksp_solve, lu_solve, eigsolve, nleps_deflated_residual, nleps_deflated_solve, nleps_jacobian_action, apply_nonlinear_pencil at L1; linear_combination at L2; eigensolver-wrapper at L0 — the lone forward-ref `nleps_jacobian_action` is now a SATISFIED surface, file on disk, upgraded to live link); no edge-label/prose mismatch (single-layer L1 entry, no lowering edge); no H1-reuse (H1 `# nleps_eigenvalue_correction` = slug, distinct from the L1 Part heading); no append-on-missing-slug; no variant-axis-missing (two live axes deflation-present + purpose, three collapsed axes enumerated); bookkeeping complete (Firm count + cohort bullet + dep-map row + SUMMARY all applied); no SUMMARY auto-fix needed (report proposed the SUMMARY edit explicitly); no index-placeholder displacement; no implied-component stub created (the one forward-ref resolved to a now-on-disk sibling).

Count delta:
- L1 firm: 18 → 19 (on-disk headline now reads `**Firm (19)**`). New firm operator: `nleps_eigenvalue_correction` (the FIFTH and final deflated NEP-interior atom at L1, after apply_nonlinear_pencil / nleps_deflated_residual / nleps_deflated_solve / nleps_jacobian_action). Reconciled additively from the on-disk 18 (the parallel jacobian-action report's 17→18 landed immediately before this one); the report's proposed 17→18 was the parallel-authoring value, corrected to 18→19 per the dispatch coordination note.

Open questions promoted:
- nleps-eigenvalue-correction-firm-landed-cycle-024-nep-cohort-complete (landing record + NEP-interior cohort-completion note — five firm atoms; explicitly flags meta-phase to CLOSE the `:779` cohort OQ AND its `:859` carry-forward, both halves now landed firm)
- nleps-eigenvalue-correction-jacobian-action-forward-ref-upgraded-to-live-link-cycle-024 (closed-disposition record of the forward-ref upgrade)
- nleps-eigenvalue-correction-mutation-rotation-l1-l0-lowering-theme (future L1>L0 abstractor candidate; batches with the four sibling NEP-interior L1>L0 themes)

Build-relevant: yes

Notes:
- SHARED-FILE COORDINATION (resolved): this report and the parallel `nleps_jacobian_action` harvester both edit `book/src/L1/index.md` (Firm-count cell + cohort-list + dep-map) and `book/src/SUMMARY.md`. I RE-READ DISK FIRST per the prior staging row's instruction — on disk the headline read `**Firm (18)**` with the jacobian-action cohort bullet + dep-map row + SUMMARY line already present (immediately after `nleps_deflated_solve`'s). I took the additive `18→19` bump and anchored ALL three of my inserts (cohort bullet, dep-map row, SUMMARY line) AFTER the `nleps_jacobian_action` entries. The two reports' inserts are textually non-overlapping by construction; only the single Firm-count cell needed reconciliation (done). No clobbering.
- NEP-INTERIOR COHORT COMPLETE: this is the fifth and final firm NEP-interior atom at L1. The full per-step quasi-Newton chain `residual → jacobian-action → eigenvalue-correction → deflated-solve → line-search` is now firm at L1. The standing cohort OQ `:779` (`nleps-interior-atoms-remaining-jacobian-action-and-eigenvalue-correction`) and its cycle-024 carry-forward `:859` are both now fully satisfiable — finalize/meta-phase can close both to the Closed index (I did NOT edit them in place — append-only between meta-phases; meta-phase owns migration; I appended a landing record stating the close-condition is met).
- FORWARD-REF UPGRADE: exercised the `rough-in-forward-reference-must-be-plain-text-not-live-link` convention's "upgrade-once-sibling-lands" path — plain-text at authoring, live-link at integration because the sibling landed firm this same cycle. The dispatch explicitly authorized this. Two links upgraded; build-safe (target on disk).
- Status rationale on-disk: `firm` via the firm-on-positive-structure escape (single positive site `palace/linalg/nleps.cpp:672-677`, every constituent read-not-constructed, laws are syntactic operator-algebra identities on firm BLAS-1 leaves dot/axpby/scal). The four non-laws (`λ`-nonlinearity upstream; well-definedness fails at `⟨[w0;w2],w⟩=0`; `δλ` undamped; reduction-tree non-determinism) are recorded as non-laws, NOT `partly-constructive` caveats and NOT test-gated `rough-in`. NLEPS no-dedicated-test caveat inherited and non-gating.
- deferred integrated_at to finalize per role-spec (did NOT touch the consumed report's frontmatter).

---

## 2026-05-29T105500Z-abstractor-nleps-deflated-solve-rotation
applied_at: 2026-05-29T12:10:00Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L1-L0/nleps-deflated-solve-mutation-rotation.md (new — firm L1>L0 theme chapter; full body written verbatim from the `new:` fence. Chapter uses 4-space-indented code blocks throughout — `grep -c '```'` returns 0 triple-backtick fences, so fence parity is trivially balanced, no cycle-019 fence-truncation risk. Single H1 `# nleps-deflated-solve-mutation-rotation` = slug, distinct from the L1>L0 Part heading.)
- book/src/L1-L0/index.md (edit — one firm dep-map row inserted after the `lu-solve-mutation-rotation` row / before the `minres-iteration` obstruction row; anchor matched verbatim on disk at line 32)
- book/src/SUMMARY.md (edit — `[nleps-deflated-solve-mutation-rotation]` entry after `[lu-solve-mutation-rotation]` (was the last L1>L0 chapter, line 101) / before the `# L0 — Cited Palace Source` Part header, under the `# L1 > L0 — Lowering` Part)
- scaffolding/open-questions.md (append — 4 OQ sections)

Gate hits:
- (none non-zero) — no retroactive-budget hit (new firm chapter + two additive registration rows, no retroactive evidence edits); no concept_writes-on-existing-slug (brand-new slug); no forward-edge-without-surface (all 13 cross-reference targets verified on disk: nleps_deflated_solve, ksp_solve, lu_solve, dot, axpy at L1; linear_combination, gram, deflate, index at L2; linear-combination-fold-specialization at L2-L1; lu-solve-mutation-rotation, nleps-deflated-residual-mutation-rotation, dot-mutation-rotation at L1-L0); no edge-label/prose mismatch (declared edge L1>L0, prose narrates exactly L1→L0 forward, high→low discipline respected — only reverse-direction material confined to §Open-questions working notes per the critic); no H1-reuse (H1 = slug); no append-on-missing-slug (dep-map row + SUMMARY entry point at the just-created chapter); no variant-axis-missing (Applicability conditions enumerate k-cardinality / element-type / in-place-destination / coupling / single-rank axes); bookkeeping complete (chapter + dep-map row + SUMMARY all applied); no SUMMARY auto-fix needed (report proposed the SUMMARY edit explicitly); no index-placeholder displacement (the index.md theme list had a real `lu-solve` row anchor, no `(empty — Phase B skeleton.)` placeholder); no implied-component stub created (no dangling forward-references — every leaf already firm on disk).

Open questions promoted:
- nleps-deflated-solve-l1-l0-lowering-theme-firm-landed-cycle-024 (landing record + closed-disposition; ENACTS the standing OQ `:784`)
- nleps-l1-l0-lowering-cohort-deflation-themes-complete-pencil-leaf-remains (cohort-progress marker)
- nleps-deflated-solve-l1-l0-confirms-deflate-promotion-gate-stays-open-from-lowering-view (confirmation of `:774`, no change)
- nleps-deflated-solve-source-comment-vs-code-S-vs-Sinv-recorded (recorded-not-a-defect note)

OQ enacted/closed:
- ENACTS OQ `nleps-deflated-solve-l1-l0-lowering-theme` (open-questions.md:784, opened cycle-023). The theme is now authored firm on disk. I appended a closed-disposition landing record (the new `...firm-landed-cycle-024` section above, with answer-link `book/src/L1-L0/nleps-deflated-solve-mutation-rotation.md`) — I did NOT edit the `:784` Open entry in place (append-only between meta-phases; meta-phase owns the Closed-index migration). *Meta-phase action flagged in the landing record:* migrate `:784` to the Closed index.

Build-relevant: yes

Notes:
- SHARED-FILE COORDINATION (CRITICAL — for the NEXT report, the parallel `apply-nonlinear-pencil-mutation-rotation` L1>L0 abstractor, which I integrate NEXT): `book/src/L1-L0/index.md` (theme dep-map) and `book/src/SUMMARY.md` (L1>L0 Part) are ALSO edited by that report. My slug is `nleps-deflated-solve-mutation-rotation`; the sibling's is `apply-nonlinear-pencil-mutation-rotation`. The two inserts are textually NON-OVERLAPPING by construction (distinct slugs, distinct rows/entries). I anchored BOTH my inserts after `lu-solve-mutation-rotation`: in `index.md` my new firm row sits immediately after the `lu-solve-mutation-rotation` row (now line 33) / before the `minres-iteration` obstruction row; in `SUMMARY.md` my new entry sits immediately after `[lu-solve-mutation-rotation]` (now line 102) / before the `# L0 —` Part header. **The next report (apply-nonlinear-pencil) MUST re-read disk FIRST** — both files now carry my `nleps-deflated-solve-mutation-rotation` row/entry. It should anchor its `apply-nonlinear-pencil-mutation-rotation` row/entry at WHICHEVER on-disk anchor its proposed-change names (likely also after `lu-solve-mutation-rotation` or after my new `nleps-deflated-solve-mutation-rotation` row — either placement is non-overlapping; just match the on-disk anchor verbatim, do NOT clobber my row). No Firm-count cell is involved (the L1>L0 index.md theme list has no count headline, unlike L1 index.md), so there is NO additive count reconciliation here — purely additive non-overlapping row/entry inserts.
- COHORT: this completes the NLEPS L1>L0 **deflation** lowering themes (lu-solve cycle-023, nleps-deflated-residual cycle-023, nleps-deflated-solve cycle-024). The bare-pencil `apply_nonlinear_pencil` L1>L0 theme (parallel report, next) closes the NLEPS L1>L0 lowering cohort per the cycle-023 staging note `:165`. The three NEP-interior-atom L1>L0 themes (`nleps_jacobian_action`, `nleps_eigenvalue_correction`, and `apply_nonlinear_pencil` if not landed this cycle) remain flagged as future abstractor batch (OQs `:869`/`:884`).
- DEFLATE GATE: this theme CONFIRMS (does not change) the cycle-022 L2 `deflate` `partly-constructive` verdict from a lowering viewpoint (the Gram is built positively but never solved bare — only Schur-wrapped). It does NOT touch the L2 `deflate` entry (out of scope). The `:774` deflate-promotion gate stays OPEN.
- Status rationale on-disk: `firm` via the firm-on-positive-structure escape (single positive site `palace/linalg/nleps.cpp:504-537`, every constituent read-not-constructed, laws are syntactic operator-algebra identities on firm L1/L2 leaves; no `partly-constructive` sub-part, no negative anchors). NLEPS no-dedicated-test caveat inherited and non-gating. Speculative L1 operators: None (composes only already-firm leaves).
- deferred integrated_at to finalize per role-spec (did NOT touch the consumed report's frontmatter).

---

## 2026-05-29T105500Z-abstractor-apply-nonlinear-pencil-rotation
applied_at: 2026-05-29T12:30:00Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L1-L0/apply-nonlinear-pencil-mutation-rotation.md (new — firm L1>L0 theme chapter; full body written verbatim from the `new:` fence — plain Markdown sections; the only fenced blocks are the 8 nested ` ```text ` blocks (L1 form / Form A / Form B / Sub-pattern A code / law 3 / Sub-pattern B code / law 5 / Sub-pattern C code), all balanced. Single H1 `# apply-nonlinear-pencil-mutation-rotation` = slug, distinct from the L1>L0 Part heading. No cycle-019 fence-truncation risk.)
- book/src/L1-L0/index.md (edit — one firm dep-map row inserted after the `nleps-deflated-residual-mutation-rotation` row / before the `assemble-diagonal-mutation-rotation` row; anchor matched verbatim on disk at line 30. NOTE: the report's proposed-change anchored on the `nleps-deflated-residual-mutation-rotation` row, NOT the sibling's just-landed `nleps-deflated-solve-mutation-rotation` row (line 33) — I matched the proposed anchor verbatim. Placement is non-overlapping with the sibling's line-33 row; both NLEPS L1>L0 rows now coexist.)
- book/src/SUMMARY.md (edit — `[apply-nonlinear-pencil-mutation-rotation]` entry inserted after the sibling's just-landed `[nleps-deflated-solve-mutation-rotation]` (line 102) / before the `# L0 — Cited Palace Source` Part header, under the `# L1 > L0 — Lowering` Part. The report's proposed anchor was `[lu-solve-mutation-rotation]`, but on disk that line (101) is now followed by the sibling's line-102 entry; per the shared-anchor coordination note I matched the on-disk reality and placed my entry after the now-last L1>L0 entry — non-clobbering, both siblings' entries intact.)
- scaffolding/open-questions.md (append — 5 OQ sections)

Gate hits:
- (none non-zero) — no retroactive-budget hit (new firm chapter + two additive registration rows, no retroactive evidence edits to existing entries); no concept_writes-on-existing-slug (brand-new slug); no forward-edge-without-surface (all 6 cross-reference targets verified on disk: ../L1/apply_nonlinear_pencil.md, ../L1/eigsolve.md; nleps-deflated-residual-mutation-rotation.md, apply-linop-mutation-rotation.md, axpby-mutation-rotation.md, axpbypcz-mutation-rotation.md — all live per index.md theme list); no edge-label/prose mismatch (declared edge L1>L0, prose narrates exactly L1→L0 forward, high→low discipline respected); no H1-reuse (H1 = slug); no append-on-missing-slug (dep-map row + SUMMARY entry point at the just-created chapter); no variant-axis-missing (3 sub-patterns A/B/C + 4 axes: damping `Maybe C` / build-form / A2-representation / purpose-coefficient-vector all enumerated, last scoped-out as upstream-deferred witness); bookkeeping complete (chapter + dep-map row + SUMMARY all applied); no SUMMARY auto-fix needed (report proposed the SUMMARY edit explicitly); no index-placeholder displacement (the index.md theme list had real row anchors, no `(empty — Phase B skeleton.)` placeholder); no implied-component stub created (no dangling forward-references — every leaf already firm on disk).

CITATION NOTE (per dispatch directive):
- The dispatch explicitly directed: the report's citation `nleps.cpp:810-811` (the source's own residual comment) is CORRECT as-is — do NOT apply any `:809-810` shift. Confirmed: the chapter file is written with `:810-811` verbatim from the `new:` fence (Form A code comment + Sub-pattern A citation list + Verified-against). NO shift applied. The critic's cosmetic off-by-one finding was itself off by one (the repairer correctly applied no edit); the on-disk citation is right.

Open questions promoted:
- apply-nonlinear-pencil-mutation-rotation-l1-l0-landed-firm-cycle-024-nleps-l1-l0-cohort-complete (landing record + NLEPS L1>L0 cohort-completion milestone)
- apply-nonlinear-pencil-a2-closure-assembly-below-l1-resolution-future-harvest-boundary (report OQ #1)
- apply-nonlinear-pencil-jacobian-derivative-pencil-decision-upstream-deferred (report OQ #2 — cross-references resolved upstream OQ `:461` + the cycle-024 jacobian-action landing)
- apply-nonlinear-pencil-mutation-rotation-lowering-verifier-audit-followup (report OQ #4 — standard lowering-verifier audit follow-up)
- (report OQ #3 "Integrator note" was a shared-file coordination note, not promoted as a standalone OQ — its content is captured in this staging row's Notes)

Build-relevant: yes

Notes:
- SHARED-FILE COORDINATION (resolved): this report and the parallel `nleps_deflated_solve` L1>L0 abstractor (integrated immediately before, prior staging row) both edit `book/src/L1-L0/index.md` (theme dep-map) and `book/src/SUMMARY.md` (L1>L0 Part). I RE-READ DISK FIRST per the prior staging row's instruction. The two slugs are textually non-overlapping (`apply-nonlinear-pencil-mutation-rotation` vs `nleps-deflated-solve-mutation-rotation`). In index.md my row anchored after `nleps-deflated-residual-mutation-rotation` (the anchor my proposed-change named verbatim, line 30) — the sibling's `nleps-deflated-solve` row at line 33 is untouched and intact. In SUMMARY.md the report's proposed anchor (`[lu-solve-mutation-rotation]`) had a moved neighbour on disk (the sibling's `[nleps-deflated-solve-mutation-rotation]` now at line 102 sits between `[lu-solve]` and the `# L0` header); I placed my entry AFTER the sibling's now-last L1>L0 entry — non-clobbering. No Firm-count cell involved (the L1>L0 index.md theme list has no count headline). No clobbering of the sibling's row/entry.
- NLEPS L1>L0 COHORT MILESTONE: `apply_nonlinear_pencil` was the LAST plain-text-forward-referenced NLEPS L1>L0 leaf (the three firm siblings `nleps_deflated_residual`/`lu_solve`/`nleps_deflated_solve` referenced its lowering as "no L1>L0 theme yet — plain-text reference"). With this landing the NLEPS L1>L0 **deflation + bare-pencil** cohort is complete: `lu-solve-mutation-rotation` (c023), `nleps-deflated-residual-mutation-rotation` (c023), `nleps-deflated-solve-mutation-rotation` (c024), `apply-nonlinear-pencil-mutation-rotation` (c024). The two remaining NEP-interior-atom L1>L0 themes (`nleps_jacobian_action` OQ `:869`, `nleps_eigenvalue_correction` OQ `:884`) batch for subsequent abstractor passes.
- Status rationale on-disk: `firm` via the firm-on-positive-structure escape (clean positive site `palace/linalg/nleps.cpp:807-821` `GetResidualNorm` + 4 corroborating positive sites `:496-499`/`:556-559`/`:655`/`:729`; every constituent read-not-constructed, laws are syntactic operator-algebra identities — `apply_linop` law 3 term-decomposition + law 5 operator-sum-distributes). No `partly-constructive` sub-part, no negative anchors. One load-bearing recording: the form-A-vs-form-B accumulation-order bit-difference (recorded not erased, per the trick taxonomy). NLEPS no-dedicated-test caveat inherited and non-gating. Speculative L1 operators: None (composes only already-firm `apply_linop`/`axpby`/`axpbypcz`).
- deferred integrated_at to finalize per role-spec (did NOT touch the consumed report's frontmatter).

---

## 2026-05-29T105500Z-abstractor-gram-fold-specialization
applied_at: 2026-05-29T12:50:00Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L2-L1/gram-fold-specialization.md (new — firm L2>L1 theme chapter; full body written verbatim from the `new:` fence, CYCLE.md lines 40-514. FENCE PARITY VERIFIED: 8 triple-backtick lines = 4 balanced ` ```text ` pairs (L2-form / L1-form / dispatch-rewrite-table / conjugate-pair-re-order code blocks); 4 `$$` markers = 2 balanced display-math pairs (the `gram[i,j]` definition + the `xᴴy = conj(yᴴx)` identity). No cycle-019 fence-truncation signature. Single H1 `# gram-fold-specialization` = slug, distinct from the L2>L1 Part heading `# L2 > L1 — Lowering layer`. The report-wrapper sections (`## Proposed changes`, `## Speculative operators proposed`, `## Supporting evidence`, the second report-level `## Open questions / caveats`) are report metadata and were correctly NOT copied into the chapter — the chapter's own in-fence `## Open questions / caveats` (the reverse-direction lifting working-note, per high→low discipline) is its last section.)
- book/src/L2-L1/index.md (edit — one firm theme-list row appended after the `orthogonalize-composition-lowering` row (was line 16 on disk); the 4 existing rows untouched. The L2>L1 index theme list has NO count headline, so no additive count reconciliation needed.)
- book/src/SUMMARY.md (edit — `[gram-fold-specialization]` entry appended after `[orthogonalize-composition-lowering]` (was line 56 on disk) / before the `# L1 — Mutation-Lifted Forms` Part header, under the `# L2 > L1 — Lowering` Part)
- scaffolding/open-questions.md (append — 3 OQ sections)

Gate hits:
- (none non-zero) — FENCE PARITY (the dispatch-flagged gate): PASS (8 backtick lines = 4 balanced `text` pairs; 4 `$$` = 2 balanced math pairs; critic's fence enumeration independently confirmed). No retroactive-budget hit (new chapter + 2 additive registration rows, no retroactive evidence edits to existing entries). No concept_writes-on-existing-slug (brand-new slug `gram-fold-specialization`). No forward-edge-without-surface (all 5 in-chapter cross-reference targets verified on disk: ../L2/gram.md, ../L1/dot.md, ../L1/bilinear-form.md, ./inner-product-fold-specialization.md, ../L2/inner_product.md). No edge-label/prose mismatch (declared edge L2>L1; prose narrates L2→L1 forward throughout; the reverse-direction lifting note confined to the chapter's `## Open questions / caveats` working-note section per high→low discipline). No H1-reuse (H1 = slug, distinct from Part heading). No append-on-missing-slug (index row + SUMMARY entry both point at the just-created chapter). No variant-axis-missing (axes enumerated: conjugation/hook canonical-Hermitian-`dot`-vs-unconjugated-`tdot`, element-type real-vs-complex, weight canonical-`XᴴX`-vs-B-weighted-`XᴴBX`, single-set-vs-cross-Gram `gram`/`gram2`; the symmetry-mirror correctly classified as a transparent-perf-trick non-axis). No SUMMARY auto-fix needed (report proposed the SUMMARY edit explicitly). No index-placeholder displacement (the L2-L1 index theme-list had real row anchors, no `(empty — Phase B skeleton.)` placeholder). No implied-component stub created (no dangling forward-references — every leaf already firm/rough-in on disk).

Open questions promoted:
- gram-fold-specialization-l2-l1-theme-firm-landed-cycle-024 (landing record + closed-disposition; notes the theme closes the `L2/gram:242-246` forward-reference)
- gram-percell-dot-vs-fused-matmul-tree-loadbearing (genuinely-new OQ — the matrix-lift per-cell-`Dot`-vs-fused-matmul bit-reproduction / load-bearing classification question; needs an NLEPS convergence-sensitivity analysis; NOT blocking)
- gram-fold-specialization-l2-gram-forward-reference-closure-followup (low-fan-out cross-reference-refresh follow-up — lifter / layer-intro-author scope; the report's carry-forward review confirmed the sibling-scalar-theme follow-ups are all inherited, none folded in, no new burden)

Build-relevant: yes

Notes:
- SHARED-FILE COORDINATION (resolved for this report; CRITICAL note for the NEXT report — the parallel cycle-024 `deflate-composition-lowering` L2>L1 abstractor, report 6 of 8, integrated NEXT): `book/src/L2-L1/index.md` (theme-list table) and `book/src/SUMMARY.md` (L2>L1 Part) are ALSO edited by `deflate-composition-lowering`. I RE-READ DISK FIRST — on disk the L2-L1 index theme list had exactly 4 rows (chebyshev / linear-combination / inner-product / orthogonalize, last on line 16) and SUMMARY had 4 L2>L1 entries (last `orthogonalize` on line 56); the `deflate` sibling had NOT yet landed. My slug is `gram-fold-specialization`; the sibling's is `deflate-composition-lowering`. The two inserts are textually NON-OVERLAPPING by construction (distinct slugs). I appended BOTH my inserts after `orthogonalize-composition-lowering` (index row now sits after the orthogonalize row; SUMMARY entry now sits after the orthogonalize entry / before the `# L1 —` Part header). The L2-L1 index theme list has NO count headline (unlike L1 index.md), so there is NO additive count reconciliation — purely additive non-overlapping row/entry inserts. **The next report (deflate-composition-lowering) MUST RE-READ DISK FIRST** — both files now carry my `gram-fold-specialization` row/entry; it should anchor its `deflate-composition-lowering` row/entry at whichever on-disk anchor its proposed-change names (likely after `orthogonalize` or after my new `gram-fold-specialization` row — either placement is non-overlapping; match the on-disk anchor verbatim, do NOT clobber my row).
- **DEFLATE RE-LINK OPPORTUNITY (confirmed — for the NEXT report's integration):** the parallel `deflate-composition-lowering` report has a forward-reference to MY slug `gram-fold-specialization` that the repairer DEFANGED to plain-text (because `gram-fold-specialization.md` did not exist on disk at that report's authoring/repair time). Now that **`book/src/L2-L1/gram-fold-specialization.md` exists on disk** (this integration), the next integration (report 6, deflate-composition-lowering) MAY re-link deflate's plain-text reference to a live link `[gram-fold-specialization](./gram-fold-specialization.md)` — the same "upgrade-once-sibling-lands" path the cycle-024 `nleps_eigenvalue_correction` integration exercised for its `nleps_jacobian_action` reference (see staging row 2). The target is now build-safe (file on disk). This is OPTIONAL (plain-text is also valid) but preferred per the `rough-in-forward-reference-must-be-plain-text-not-live-link` convention's upgrade path.
- COHORT: `gram-fold-specialization` is the FIFTH chapter under the `book/src/L2-L1/` Part (after chebyshev-iteration-fusion, linear-combination-fold-specialization, inner-product-fold-specialization, orthogonalize-composition-lowering). It is the matrix-valued sibling of `inner-product-fold-specialization` — the two reduce-to-X fold siblings (`inner_product` scalar, `gram` matrix) now both have specialization themes. With the parallel `deflate-composition-lowering` landing next, the NLEPS-deflation L2>L1 lowering pair (`gram` builds the matrix, `deflate` solves it) is complete.
- Status rationale on-disk: `firm` via read-direct positive evidence (the sole literal `XᴴX` Gram-build site `palace/linalg/nleps.cpp:524-531`, cell body `:529`) composed pointwise with the already-firm sibling scalar theme `inner-product-fold-specialization`. No `partly-constructive` sub-part, no negative anchors, no speculative operator. Coverage caveat (one Gram-build site Palace-wide + no dedicated NLEPS unit test) recorded as a non-firmness-gate. NLEPS no-dedicated-test caveat inherited and non-gating.
- deferred integrated_at to finalize per role-spec (did NOT touch the consumed report's frontmatter).

---

## 2026-05-29T105500Z-abstractor-deflate-composition-lowering
applied_at: 2026-05-29T13:10:00Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L2-L1/deflate-composition-lowering.md (new — `partly-constructive` L2>L1 theme chapter; full body written from the `new:` fence. FENCE PARITY VERIFIED: `grep -c '```'` returns 14 = 7 balanced ` ```text ` pairs (L2-form / Stage-1 dot-loop / Stage-2 gram / Stage-3 Schur block / Stage-3 Galerkin solve / Stage-4 back-projection / Stage-5 axpy), no `$$` math blocks; no cycle-019 fence-truncation signature. Single H1 `# deflate-composition-lowering` = slug, distinct from the L2>L1 Part heading `# L2 > L1 — Lowering layer`. The report-wrapper sections `## Speculative operators proposed` / `## Supporting evidence` / the report-level second `## Open questions / caveats` are report metadata and were correctly NOT copied into the chapter — the chapter ends at its in-fence `## Working notes (reverse-direction lifting; NOT chapter content)` section. **PARTLY-CONSTRUCTIVE CAVEAT PRESERVED VERBATIM:** the chapter §Status carries (i) the constructive sub-part = the Galerkin-core single bare-Gram `lu_solve(XᴴX, c)` fan-down (`op.block = Galerkin`, `S = I`); (ii) its negative anchors = no bare-Gram deflation solve anywhere in `palace/linalg/*.cpp`, only the Schur-wrapped `:533-535` form + literature `:354-362`, cross-referenced to the L2 entry's same anchor `deflate.md:343-348, 386-391`; (iii) the explicit promotion condition = a positive bare-Gram-solve site outside the `S = λI − H` Schur wrapping; and the explicit "**This theme does not make that call and does not close the gate.**" The firm Schur-form fan-down is firm-on-positive-structure; only the constructive sub-part carries the open gate. **RE-LINK APPLIED** — see Notes.)
- book/src/L2-L1/index.md (edit — one partly-constructive theme-list row appended after the `gram-fold-specialization` row (line 17 on disk, landed by the prior report 5) / as the now-last theme row. RE-ANCHORED: the report's proposed-change anchored on the `orthogonalize-composition-lowering` row, but on disk the parallel `gram-fold-specialization` sibling's row now sits between `orthogonalize` and the row end; I anchored after `gram-fold-specialization` per the prior staging row's coordination note — non-overlapping, both NLEPS-pair rows coexist. The L2-L1 index theme list has NO count headline, so no additive count reconciliation.)
- book/src/SUMMARY.md (edit — `[deflate-composition-lowering]` entry appended after the `[gram-fold-specialization]` entry (line 57 on disk) / before the `# L1 — Mutation-Lifted Forms` Part header, under the `# L2 > L1 — Lowering` Part. RE-ANCHORED from the report's proposed `[orthogonalize-composition-lowering]` anchor for the same reason — the sibling's `[gram-fold-specialization]` entry now sits between `orthogonalize` and the `# L1` header; placed after it, non-clobbering.)
- scaffolding/open-questions.md (append — 4 OQ sections)

Gate hits:
- (none non-zero) — FENCE PARITY (cycle-019 guard): PASS (14 backtick lines = 7 balanced `text` pairs; no `$$`). No retroactive-budget hit (new chapter + 2 additive registration rows, no retroactive evidence edits to existing entries — the partly-constructive caveat is authored IN the new chapter, not retro-edited into an existing one). No concept_writes-on-existing-slug (brand-new slug `deflate-composition-lowering`). No forward-edge-without-surface (all cross-reference targets verified on disk: ../L2/deflate.md, ../L2/gram.md, ../L1/lu_solve.md, ../L2/linear_combination.md, ../L1/dot.md at L1/L2; ./gram-fold-specialization.md + ./orthogonalize-composition-lowering.md at L2-L1; ../L1/nleps_deflated_residual.md at L1 — the lone formerly-defanged forward-ref `gram-fold-specialization` is now a SATISFIED surface, file on disk, UPGRADED to live link). No edge-label/prose mismatch (declared edge L2>L1; prose narrates L2→L1 forward throughout; reverse-direction lifting note confined to the chapter's `## Working notes` section per high→low discipline). No H1-reuse (H1 = slug, distinct from Part heading). No append-on-missing-slug (index row + SUMMARY entry both point at the just-created chapter). No variant-axis-missing (the central `op.block ∈ {Galerkin, Schur}` axis is the explicit Stage-3 fan-out point with BOTH values' L1 `lu_solve`-sequences pinned — Schur = three positively-sourced solves, Galerkin = one constructive bare-Gram solve; secondary axes `op.dot` hook / element-type complex-vs-real / in-place-vs-out-of-place all handled). No SUMMARY auto-fix needed (report proposed the SUMMARY edit explicitly). No index-placeholder displacement (the L2-L1 index theme-list had real row anchors, no `(empty — Phase B skeleton.)` placeholder). No implied-component stub created (the one forward-ref `gram-fold-specialization` resolved to a now-on-disk sibling and was re-linked, not stubbed).

PARTLY-CONSTRUCTIVE GATE STATUS:
- The inherited Galerkin-core promotion gate **STAYS OPEN** (NOT closed by this integration). It is the SAME single gate shared by the L2 `deflate` entry (standing OQ `:774`), the L1>L0 `nleps-deflated-solve-mutation-rotation` theme (confirmed-not-closed `:891`), and now this L2>L1 theme — all three promote together on the SAME positive-bare-Gram-solve site. Promoted as the dedicated OQ `deflate-composition-lowering-galerkin-core-promotion-gate-shared-with-l2-deflate-stays-open` (threaded to `:774`; do NOT close independently). The chapter §Status and §Working-notes both reaffirm the theme "does not make that call and does not close the gate."

Open questions promoted:
- deflate-composition-lowering-l2-l1-theme-partly-constructive-landed-cycle-024 (landing record; notes the NLEPS-deflation L2>L1 pair `gram-fold-specialization`+`deflate-composition-lowering` is now complete, and the re-link)
- deflate-composition-lowering-galerkin-core-promotion-gate-shared-with-l2-deflate-stays-open (the inherited gate — OPEN, threaded to the shared `:774` deflate-promotion OQ; report OQ #1)
- deflate-composition-lowering-mutation-rotation-lowering-verifier-audit-followup (standard lowering-verifier audit follow-up; may UNBLOCK the Galerkin-core promotion without ENACTING)
- deflate-composition-lowering-ss-buffer-aliasing-and-no-nleps-test-caveats-recorded (report OQs #3 + #4 — both non-gating caveats: `SS` buffer aliasing + no NLEPS unit test)

Build-relevant: yes

Notes:
- SHARED-FILE COORDINATION (resolved): this report (6 of 8) and the parallel `gram-fold-specialization` abstractor (report 5, integrated immediately before — prior staging row) both edit `book/src/L2-L1/index.md` (theme-list table) and `book/src/SUMMARY.md` (L2>L1 Part). I RE-READ DISK FIRST per the prior staging row's CRITICAL instruction — on disk the L2-L1 index theme list carried the `gram-fold-specialization` row at line 17 (after `orthogonalize-composition-lowering`) and SUMMARY carried `[gram-fold-specialization]` at line 57 (after `[orthogonalize-composition-lowering]`). The two slugs are textually non-overlapping (`deflate-composition-lowering` vs `gram-fold-specialization`). My report's proposed anchors named `orthogonalize-composition-lowering` (the authoring-time last entry), but the sibling moved the on-disk last entry to `gram-fold-specialization`; I re-anchored BOTH inserts after `gram-fold-specialization` (index row after line 17; SUMMARY entry after line 57) — non-clobbering, both NLEPS-pair rows/entries coexist. No count reconciliation (L2-L1 index has no count headline).
- RE-LINK APPLIED (per dispatch directive + prior staging row's flagged opportunity): the repairer had defanged this report's two `gram-fold-specialization` forward-references to plain-text (intro "Sibling to …" sentence ~CYCLE.md:60-63; Stage 2 ~CYCLE.md:193) because `gram-fold-specialization.md` did not exist on disk at repair time. Since the parallel `gram-fold-specialization` sibling landed firm on disk in report 5 of THIS cycle (immediately before this integration), I UPGRADED both plain-text references back to live links `[`gram-fold-specialization`](./gram-fold-specialization.md)` — the same "upgrade-once-sibling-lands" path exercised by the cycle-024 `nleps_eigenvalue_correction` (jacobian-action ref) and explicitly flagged for me in the report-5 staging row. VERIFIED: `grep` shows exactly 2 `](./gram-fold-specialization.md)` live links (intro line 16, Stage 2 line 147); the third mention (line 320, inside §Verified-against) stays inline-code descriptive prose (the gram leaf's own fan-down named, NOT a cross-reference) — matching the report's original framing. Link target `book/src/L2-L1/gram-fold-specialization.md` confirmed on disk (34957 bytes). No `mdbook-linkcheck2` risk.
- NLEPS-DEFLATION L2>L1 PAIR COMPLETE: `gram-fold-specialization` (builds the Gram matrix `XᴴX`, c024 report 5) + `deflate-composition-lowering` (solves it, c024 report 6) together complete the NLEPS-deflation L2>L1 lowering pair. The deflation chain now has L2>L1 coverage at both the gram-build and the deflate-solve composition levels.
- STATUS RATIONALE on-disk: `partly-constructive` inherited verbatim from the L2 `deflate` entry (`book/src/L2/deflate.md:362-415`). Firm part = the Schur-form fan-down (every stage read from the positive `deflated_solve` block `:505-537`, all leaves firm L1/L2 vocabulary). Constructive sub-part = the Galerkin-core single bare-Gram `lu_solve(XᴴX, c)` fan-down (the `S = I` reduction; literature `:354-362` + negative anchor `deflate.md:343-348`). Promotion condition = positive bare-Gram-solve site outside the Schur wrapping. NOT marked firm (the constructive sub-part isn't), NOT downgraded to rough-in (the structure IS firm). Gate STAYS OPEN. No speculative operator (composes only already-firm leaves dot/gram/lu_solve/linear_combination/axpy).
- deferred integrated_at to finalize per role-spec (did NOT touch the consumed report's frontmatter).

---

## 2026-05-29T105500Z-harvester-l3-eigsolve-backfill
applied_at: 2026-05-29T13:30:00Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L3/eigsolve.md (REFINED IN PLACE — the cycle-023 `stub` fully replaced with the firm `partial-obstruction` body, written verbatim from the report's `edit:book/src/L3/eigsolve.md` fence; NOT a new file. Frontmatter `firmness: partial-obstruction`; single H1 `# eigsolve` = slug (distinct from the L3 Part heading); 237 lines. FENCE PARITY: 6 triple-backtick lines = 3 balanced ` ```text ` pairs (Signature `eigsolve ::` / per-step `apply_shift_invert ::` / Value-threaded form), even parity, no cycle-019 risk. ZERO stub residue (no "claim-free placeholder"/"Implied by"/"Refinement pending" markers — fully superseded).)
- book/src/L3/index.md (edit — one `partial-obstruction` dep-map row for `eigsolve` inserted after the `ksp_solve` row (now line 31) / before the `## Working Notes` header; anchor matched verbatim on disk. The L3 index dep-map table has no count headline, so no additive count reconciliation.)
- book/src/SUMMARY.md (edit — SURGICAL OLD→NEW relabel at line 31: `- [eigsolve (stub)](./L3/eigsolve.md)` → `- [eigsolve](./L3/eigsolve.md)`, dropping the `(stub)` marker now that the entry is `partial-obstruction`. NOT an append — the entry already existed from cycle-023; verified exactly one `L3/eigsolve.md` entry on disk, no duplicate.)
- scaffolding/open-questions.md (append — 5 OQ sections)

Gate hits:
- (none non-zero) — no retroactive-budget hit (refinement of a claim-free stub in place + two additive registration edits; the stub carried no citations/claims so replacing it is not a retroactive edit to firm evidence). No concept_writes-on-existing-slug (this is an L3 operator entry, refinement-in-place of the same `eigsolve` slug — not a concept page, not a write on a different existing slug). No forward-edge-without-surface (frontmatter `lifts_from`/`lowers_to` → L2/eigsolve.md exists firm; same-layer deps ksp_solve + apply_linop exist firm; all 6 concept links exist on disk; the L4 edge is correctly NOT claimed live — plain-text "unauthored"). No edge-label/prose mismatch (declared L3↔L2 edge; prose narrates L3→L2 forward §Lowers to + L2→L3 §Lifts from; critic passed edge-label-fidelity). No H1-reuse (H1 `# eigsolve` = slug, distinct from the L3 Part heading `# L3 — Global tensor-field operations`). No append-on-missing-slug (SUMMARY edit is a surgical relabel of the existing stub entry; dep-map row points at the just-refined chapter). No variant-axis-missing (5 axes enumerated frontmatter + §Variant axes: spectral-transformation, problem-type, backend-orchestration, element-type, scaling; critic passed variant-axis-coverage). No SUMMARY auto-fix needed (entry already existed; report proposed the surgical relabel explicitly). No index-placeholder displacement (L3 index dep-map had real row anchors, no `(empty — Phase B skeleton.)` placeholder). No implied-component stub created (the lone not-yet-existing referenced slug `L4/eigsolve.md` is genuinely SPECULATIVE — no firm L4 layer anchors converge; the clearly-implied bar is not cleared; plain-text-defer is the correct fallback, recorded as OQ `eigsolve-l4-surface-solve-monad-unauthored-future-dispatch`).

DEAD-L4-LINK CONFIRMATION (per dispatch directive):
- ZERO live `](../L4/eigsolve.md)` links survive in the book file (grep exit=1). The repairer's 6 defangs (CYCLE.md lines 44/59/103/197/228/239) applied as-is — all L4-eigsolve forward-references are plain-text code-spans with the parenthetical "the `L4/eigsolve.md` chapter is not yet authored". The two `../L4/iterate-while.md` links that remain in the book file (lines 34, 214) are LIVE/VALID — that chapter exists on disk (`book/src/L4/iterate-while.md`, 24696 bytes). Repaired content applied verbatim: 5 corrected off-by-one citations (arpack.cpp :573/:579/:270; slepc.cpp :1857/:1873) are in the on-disk file.

Status delta:
- L3 `eigsolve`: `stub` (cycle-023) → `partial-obstruction` (cycle-024). The CANONICAL OPAQUE-LIBRARY partial-obstruction (per-step body lifts identity-in-form to the firm L2 body; eigen-iteration loop is a witnessed `sequential-obstruction` rooted in opaque-library-ownership — SLEPc `EPSSolve` `slepc.cpp:694` / ARPACK `naupd` RCI `arpack.cpp:318` own the loop; no Palace-authored kernel/driver pair analogous to `(krylov-step, ksp_solve)`). Distinct from `chebyshev` (numerical-stability obstruction). SECOND L3 partial-obstruction landing and the FIRST opaque-library one. **EIGSOLVE CHAIN COMPLETE: this completes the eigsolve L1→L2→L3 prerequisite chain** (L1-firm cycle-022 → L2-firm cycle-023 → L3-partial-obstruction cycle-024) — structurally complete across all three core layers.
- L3 operator count: 9 firm + 2 partial-obstruction (chebyshev cycle-013, eigsolve cycle-024). (The L3/index.md Working-Notes prose bullet noting the second partial-obstruction is layer-intro-author scope, NOT this harvester's — promoted as OQ for a future index touch, NOT authored here.)

Open questions promoted:
- eigsolve-l3-backfill-partial-obstruction-landed-cycle-024-chain-step-3-done (landing record + chain-completion; flags meta-phase to re-frame `:613` step-3-DONE, mark `eigsolve-l3-backfill` plan candidate done, migrate the `:802` stub-materialization OQ to RESOLVED)
- l3-eigsolve-linear-evp-has-no-krylov-step-kernel-analog-CONFIRMED-cycle-024 (CONFIRMS the cycle-021 prediction at `:624`; flags meta-phase to CLOSE `:624` to the Closed index — the prediction held: opaque-library partial-obstruction, no Palace-authored eigen-step kernel)
- eigsolve-l4-surface-solve-monad-unauthored-future-dispatch (future L4 harvester/abstractor candidate; the speculative L4 surface — NOT stubbed, plain-text-defer rationale recorded)
- eigsolve-l3-l2-body-identity-audit-anchor-optional-followup (optional L3>L2 lowering-verifier audit anchor + cross-reference to the separately-pending L2>L1 theme `:807`)
- l3-second-partial-obstruction-operator-landed-cycle-024-index-working-note-followup (low-fan-out layer-intro-author L3/index Working-Notes refresh candidate)

OQ confirmed/closed (flagged for finalize/meta-phase — NOT edited in place):
- CONFIRMED + READY-TO-CLOSE: `l3-eigsolve-linear-evp-has-no-krylov-step-kernel-analog` (open-questions.md:624, cycle-021 prediction). The prediction held against positive Palace source. Closure is meta-phase migration (append-only between meta-phases; I appended a CONFIRMED record, did NOT edit `:624` in place).
- The eigsolve-chain prerequisite OQs (`:613` step-3, `:802` stub-materialization) are now satisfiable for closure/re-frame — flagged for meta-phase, NOT edited in place.

Build-relevant: yes

Notes:
- NO SHARED-FILE COORDINATION CONFLICT: this report (7 of 8) edits `book/src/L3/eigsolve.md` (sole writer — no other cycle-024 report touches L3), `book/src/L3/index.md` (sole writer this cycle — the L1/L1-L0/L2-L1 index edits in prior staging rows do not touch L3 index), and `book/src/SUMMARY.md` (a surgical single-line relabel at line 31, in the L3 Part — textually non-overlapping with the prior reports' L1-Part / L1>L0-Part / L2>L1-Part appends). Re-read disk before each edit: SUMMARY.md:31 confirmed `- [eigsolve (stub)](./L3/eigsolve.md)` on disk before the relabel; L3 index `ksp_solve` row anchor matched verbatim. No clobbering.
- EIGSOLVE CHAIN MILESTONE: this is the terminal step of the three-cycle eigsolve prerequisite chain. The chain's remaining open edges (all flagged as OQs, none blocking): the separately-pending L2>L1 theme `eigsolve-spectral-transform-composition` (`:807`, cycle-025+ abstractor), the optional L3>L2 audit anchor, and the unauthored speculative L4 `solve-monad` surface.
- Status rationale on-disk: `partial-obstruction` per CLAUDE.md §Methodology invariants "Two rough-in qualifiers are first-class" — the status reflects the LOOP structure, not the body. The body laws (1,2,4,5) are syntactic identities on positive `ApplyOp`/`__pc_apply_EPS` source (the firm-on-positive-structure basis inherited from firm L1/L2), identity-in-form to firm L2; the loop is the obstruction. NOT firm (loop doesn't lift), NOT full obstruction (operator IS implemented + body DOES lift), NOT `rough-in (test-coverage-bounded)` (body laws are syntactic identities, not test-gated convergence semantics — §Status Caveat argues this explicitly). No `L3-L2/` theme file (in-line non-adjacent-identity annotation per cycle-012 convention).
- deferred integrated_at to finalize per role-spec (did NOT touch the consumed report's frontmatter).

---
## 2026-05-29T105500Z-lowering-verifier-dot-anchor-fix
applied_at: 2026-05-29T13:50:00Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L1-L0/dot-mutation-rotation.md (edit ×3 — (1) §Sub-pattern D line 160 prose anchor `orthog.hpp:34`→`:35`; (2) line 183 citation-list anchor `return LocalDot(x, y)` at `:34`→`:35`; (3) EOF append of the `verified_against:` yaml block after the `## Status` section. Both surgical OLD→NEW token old_strings matched verbatim on disk and were unique (re-read before editing; this file was NOT touched by any other cycle-024 report, so on-disk state matched what the auditor read). The `verified_against:` block applied verbatim from the repairer-normalized standard ` ```yaml ` triple-backtick fence — status stays `firm`, no semantic content change.)
- scaffolding/open-questions.md (append — 1 RESOLVED closed-disposition OQ section recording the carry-forward anchor-fix closure)

Gate hits:
- (none non-zero) — retroactive-budget: this is the ONLY retroactive-evidence-edit report in cycle-024 (off-by-one anchor corrections + an audit-evidence `verified_against:` append to ONE existing slice = 1 slice), well under per-slice ≥3 / global ≥4 thresholds — no block (global aggregate is finalize's call across the staging log, but the other 7 cycle-024 reports were new firm chapters / a stub-refinement-in-place, not retroactive evidence edits). No concept_writes-on-existing-slug (not a concept page; refinement-in-place of an existing L1>L0 theme). No forward-edge-without-surface (no new cross-references; pure anchor correction). No edge-label/prose mismatch (no edge label or prose direction changed; L1>L0 forward narration untouched). No H1-reuse (no heading touched). No append-on-missing-slug (appending to an existing on-disk slug). No variant-axis-missing (no variant enumeration touched). No bookkeeping-incomplete (no count headline / SUMMARY / dep-map registration involved — refinement of existing content; the theme is already registered at SUMMARY.md:93, no new chapter). No SUMMARY auto-fix needed. No index-placeholder displacement. No implied-component stub created (no dangling forward-reference).

Fence parity check:
- `book/src/L1-L0/dot-mutation-rotation.md` `grep -c '```'` = 2 (was 0 — the file uses 4-space-indented code blocks throughout; the new `verified_against:` block is the file's sole triple-backtick fence pair, balanced/even). No cycle-019 fence-truncation risk.

Anchor verification (post-edit):
- Zero `orthog.hpp:34` / "at `:34`" anchor tokens remain in the file. Two corrected `:35` anchors at lines 160 (prose) + 183 (citation list); the `verified_against:` block cites the corrected `palace/linalg/orthog.hpp:35` (line 404). Matches the value the sibling `orthogonalize-composition-lowering` theme already used correctly (its lines 161, 302) — drift was confined to `dot-mutation-rotation`, now closed.

Open questions promoted:
- dot-mutation-rotation-subpattern-d-orthog-anchor-fix-RESOLVED-cycle-024 (RESOLVED closed-disposition record — discharges the cycle-023 carry-forward OQ `dot-mutation-rotation-subpattern-d-citation-fix` at open-questions.md:847; flags meta-phase to migrate `:847` to the Closed index + mark the plan candidate done; also carries forward the deliberately-out-of-scope `:29-36`→`:29-37` range tidy-up as a low-fan-out future-lifter note)

OQ closed (flagged for finalize/meta-phase — NOT edited in place):
- The cycle-023 carry-forward `dot-mutation-rotation-subpattern-d-citation-fix` (open-questions.md:847, opened by the cycle-023 `orthogonalize-composition-audit`) is RESOLVED by this landing. Per append-only-between-meta-phases discipline I did NOT edit the `:847` Open entry in place — I appended the RESOLVED closed-disposition record above. Meta-phase owns the Closed-index migration.

Build-relevant: yes

Notes:
- NO SHARED-FILE COORDINATION CONFLICT: this report (8 of 8) is the sole cycle-024 writer of `book/src/L1-L0/dot-mutation-rotation.md` (no prior staging row touched it — the L1-L0 edits in rows 3/4 were `index.md` + new chapter files, not this theme). Re-read the §Sub-pattern D region (lines 155-188) + the file tail before editing; both surgical old_strings matched verbatim and uniquely. No clobbering.
- VERDICT: supports-with-one-token-anchor-drift — the §Sub-pattern D claim ("the canonical `IdentityInnerProduct::operator()` returns `LocalDot(x, y)`") is fully supported at the corrected `:35`; only the line number drifted by one (line 34 = the operator-body opening brace `{`). Theme stays `firm` (no status change; pure anchor-drift repair, no semantic edit). The auditor's all-other-§Sub-pattern-D anchors (`:48`, `:46-52`, `:66-88`, `:29-36` range) were independently re-confirmed correct and NOT touched.
- FENCE-SUBSTITUTION HANDOFF (resolved by repairer pre-integration): the report originally emitted the `verified_against:` block inside a `~~~~edit:` tilde-wrapper with a prose substitution note; the repairer normalized it to the standard ` ```yaml ` triple-backtick fence matching the sibling-theme convention (`orthogonalize-mutation-rotation.md:265`, `orthogonalize-composition-lowering.md`). I applied the inner ` ```yaml ` block verbatim — no fence-interpretation step needed.
- COMPLETES CYCLE-024: this is the 8th and FINAL ready report of cycle-024. All 8 cycle-024 reports are now staged (rows 1-8). Ready for integrator-finalize.
- deferred integrated_at to finalize per role-spec (did NOT touch the consumed report's frontmatter).

---
