---
agent: cycle-planner
invoked_at: 2026-06-03T221019Z
scope: cycle-085 dispatch plan
status: pending
---

# Cycle 085 dispatch plan

FIRST primary cycle of meta-batch-27 (cycles 085/086/087; the batch-27 meta-phase fires AFTER cycle-087's finalize). Fresh session immediately after the batch-26 meta-phase enacted the USER DIRECTIVE `feature-column-promotion-break-the-seed-deadlock` (CLAUDE.md §Extraction-goal + `layer-intro-author` §FEATURE-SURFACE edits + session restart).

## Goals selected this cycle

THE LEAD (active-head #1, HIGH) — the all-13-column re-evaluation under the new OWN-COMPOSITION promotion rule: re-author every FEATURE-SURFACE SPINE column's promotion-rule prose (drop the deadlock "until ALL composed constituents incl. the cross-linked sibling column are firm" clause), and FLIP the `status:` token of every column whose **own composition + directly-owned constituents** are firm (cross-linked sibling columns are references, NOT blockers). This is the user-directive's downstream authoring payload — it promotes the first feature columns off the terminal `seed` state and validates the whole spine-promotion mechanism. The plan is deliberately scoped to the LEAD alone (split into 3 by-kind cohorts for bounded context + one index-owner), because the on-disk verification below establishes the LEAD is a tightly-coupled whole-cohort sweep that should land as one coherent wave, and the standing bottom-up forward frontier (active-head #2) is genuinely light this cycle (the reduce-verb law-confidence route is EXHAUSTED for the two all-primitives-firm verbs; `matrix-weighted-norm` cascade is NO-GO HELD; the 5-driver→L4 is AFFIRMED-COMPLETE c082 — re-proposing any of it is forbidden). No manufactured forward-frontier companion: a small clean LEAD-only wave is the honest plan.

## Deliverable-presence verification (paste-inline evidence)

The LEAD is **open by construction** — it is the batch-26 meta-phase-routed enactment authored into the active head post-c084, with no prior-cycle history (no column re-evaluation has run; all 13 columns are at `seed` on disk). The c027 cycle-record/STAGING/struck-Backlog open-check is satisfied trivially (the cycle-084 + batch-26 meta-phase rows are the last records; cycle-085 is the next cycle; no re-proposal of landed work). The substance of this section is the **per-column constituent-maturity evidence** that determines the FLIP-vs-STAY-seed verdict — this is where the plan adds value over restating priorities.md (which predicted electrostatic/magnetostatic flip; the on-disk evidence OVERRIDES that — they do NOT qualify).

**All 39 column files + index at `seed` on disk (`grep -m1 ^status:` per file):**
```
eigenmode.{L4,L1,L0} -> seed   electrostatic.{L4,L1,L0} -> seed   magnetostatic.{L4,L1,L0} -> seed
driven.{L4,L1,L0} -> seed      transient.{L4,L1,L0} -> seed       eigenfrequency-qfactor.{L4,L1,L0} -> seed
sparameters.{L4,L1,L0} -> seed capacitance.{L4,L1,L0} -> seed     inductance.{L4,L1,L0} -> seed
energy-fields.{L4,L1,L0} -> seed  boundary-mode.{L4,L1,L0} -> seed lifecycle.{L4,L1,L0} -> seed
```

**Constituent vocab-op maturity (on-disk `## Status` value, the GROUND TRUTH that determines each verdict):**
```
L4/fe_assemble                 -> `firm` (firm-on-positive-structure)
L4/ksp_solve                   -> `firm`
L4/eigsolve                    -> `firm` (cap firm; obstruction it carries is the opaque eigen-loop)
L4/assemble_frequency_operator -> `firm`
L4/frequency_sweep             -> `firm` (firm-on-positive-structure)
L4/fold_solve                  -> `firm` (firm-on-positive-structure; 2 fold witnesses)
L4/solve_family                -> `rough-in (test-coverage-bounded)`   <-- NOT firm
L4/gram_reduce                 -> `rough-in (test-coverage-bounded)`   <-- NOT firm
L4/sparameter_reduce           -> `firm` (c083 firm-on-positive-structure)
L4/eigenfreq_qfactor_reduce    -> `firm` (c082 firm-on-positive-structure)
L4/domain_energy_reduce        -> `rough-in`                           <-- NOT firm
L1/port_projection             -> `firm`     L1/participation_ratio -> `firm`   L1/eigenvalue-untransform -> `firm`
L1/matrix-weighted-norm        -> `rough-in (test-coverage-bounded)`   <-- NOT firm
L1/bilinear-form               -> `rough-in`                           <-- NOT firm
```

**Per-column composes-block evidence (read from each column's `.L4.md` frontmatter `composes:` list):**
- `eigenmode` composes `fe_assemble`(firm) + `eigsolve`(firm). Its OWN stage-(3) readout is its own per-mode `map`; its reduction is owned by the cross-linked `eigenfrequency-qfactor` column (a SIBLING reference, NOT a constituent). → **FLIP** (own composition all firm).
- `driven` composes `fe_assemble`(firm) + `assemble_frequency_operator`(firm) + `frequency_sweep`(firm) + `ksp_solve`(firm). → **FLIP** (all firm; "the first feature column all three composition stages compose FIRM combinators" per its own prose).
- `transient` composes `fe_assemble`(firm) + `fold_solve`(firm). → **FLIP** (all firm).
- `electrostatic` composes `fe_assemble`(firm) + **`solve_family`(rough-in)** + `ksp_solve`(firm) + **`gram_reduce`(rough-in)**. → **STAY seed** (its OWN directly-composed `solve_family` AND `gram_reduce` are rough-in — these are NOT sibling columns, they are directly-composed vocab ops). **This CONTRADICTS the priorities.md #1 expectation that electrostatic flips — the on-disk evidence governs.**
- `magnetostatic` composes `fe_assemble`(firm) + **`solve_family`(rough-in)** + `ksp_solve`(firm) + **`gram_reduce`(rough-in)**. → **STAY seed** (same as electrostatic; on-disk override).
- `boundary-mode` composes `fe_assemble`(firm) + `eigsolve`(firm) — but its own prose (`boundary-mode.L4:79`) names the ONE seed-reason as its **stage-(3) readout reduction into a waveguide-mode product that has NO dedicated output-product column yet** (a forward-ref to an unhomed product). The readout is its OWN directly-owned constituent and has no firm home. → **STAY seed** (own readout unhomed; matches priorities.md #1 "boundary-mode if its own readout has no firm home").
- `eigenfrequency-qfactor` (output-product) composes own verb `eigenfreq_qfactor_reduce`(**firm** c082) + cross-link `eigenmode`(seed, a REFERENCE). → **FLIP** (own reduce verb firm; the `eigenmode` cross-link is a sibling reference, NOT a blocker — this is the EXACT deadlock the directive breaks; `eigenfrequency-qfactor.L4:55` currently asserts the deadlock "stays seed because eigenmode is seed").
- `sparameters` (output-product) composes own verb `sparameter_reduce`(**firm** c083) + cross-link `driven`(a REFERENCE). → **FLIP** (own reduce verb firm; `sparameters.L4:54` currently holds the seed "pending the batch-26 meta-phase" — that meta-phase has now fired).
- `capacitance` (output-product) composes own verb **`gram_reduce`(rough-in)**. → **STAY seed** (own reduce verb rough-in).
- `inductance` (output-product) composes own verb **`gram_reduce`(rough-in)**. → **STAY seed** (own reduce verb rough-in).
- `energy-fields` (output-product) composes own verb **`domain_energy_reduce`(rough-in)** + **`matrix-weighted-norm`(rough-in)** + `participation_ratio`(firm). → **STAY seed** (own reduce verb + own folded energy-form rough-in).
- `lifecycle` (spine-ROOT) composes `fold_solve`(firm; driver-agnostic) + the 5 driver feature columns (SIBLING references, per the spine-ROOT sub-kind). Its directly-owned driver-agnostic vocabulary (`fold_solve`, the mesh-build + adaptive estimate-mark-refine fold) is firm; sibling columns are references. → **FLIP candidate — author confirms** (under OWN-COMPOSITION its only directly-owned vocab op `fold_solve` is firm; the per-driver dispatch is over references; `lifecycle.L4:64` already states "stage (3) IS the firm `fold_solve` ... stage (2) dispatches over the per-driver feature columns"). Routed to D3 to make the final maturity call from on-disk constituent status.

**FLIP set (6 columns → promoted token): eigenmode, driven, transient, eigenfrequency-qfactor, sparameters, lifecycle (author-confirmed).**
**STAY-seed set (5 columns, with re-authored OWN-COMPOSITION rule prose explaining the own-constituent reason): electrostatic, magnetostatic, capacitance, inductance, energy-fields, boundary-mode.** (6 stay-seed — electrostatic + magnetostatic + the 3 output-products + boundary-mode.)

**Target promoted token:** `firm`. The columns are composition-roots whose compositional claim is sound and whose directly-owned constituents are all firm; per the role-spec the author makes the final per-column maturity call from on-disk evidence, but the expected flip is `seed → firm` (a composition-root over all-firm vocabulary carries no un-firm content of its own). D-authors confirm `firm` vs a `rough-in` intermediate per column; the LEAD expectation is `firm`.

**Shared-index surface:** `feature/index.md` is the ONLY shared mutable narrative across cohorts — its §Chapter-kind status (`index.md:59-61`) + the cohort-wide rule-prose at `index.md:55` ("a feature column may promote past `seed` only once ALL its composed constituents are firm") + `:57` carry the OLD deadlock rule and the blanket-`seed` claim. `SUMMARY.md` §Feature surfaces carries NO status tokens (verified: titles + links only) → no SUMMARY status write needed. The `feature/index.md` matrix (`:28-44`) has NO per-column status CELL (level-link cells only) → no matrix-cell status flip needed; "flip the matrix cell" reduces to the narrative §Chapter-kind status re-authoring. **Single-index-owner discipline: D1 SOLE-OWNS the `feature/index.md` narrative re-authoring** (the rule-prose + §Chapter-kind status, re-narrated to reflect the full-cohort flip outcome); D2/D3 touch ONLY their own column files and do NOT write `feature/index.md`.

## Dispatches

1. **D1 — `layer-intro-author` — DRIVER-LEAF cohort re-evaluation + SOLE owner of `feature/index.md`.**
   - **scope:** Re-evaluate the 6 driver-leaf columns under the OWN-COMPOSITION rule. (a) For each of the 6 columns (eigenmode, driven, transient, electrostatic, magnetostatic, boundary-mode) × 3 levels (L4/L1/L0): re-author the promotion-rule prose to the OWN-COMPOSITION rule (drop the "until ALL composed constituents / the cross-linked sibling column firms" clause; state the rule as "promotes off seed when its OWN composition + directly-owned constituents are firm; cross-linked sibling columns are references"). (b) FLIP `status: seed → firm` in the frontmatter of **eigenmode.{L4,L1,L0}**, **driven.{L4,L1,L0}**, **transient.{L4,L1,L0}** (own composition all firm per the evidence above), re-narrating each §Status block to the OWN-composition justification (e.g. eigenmode: "promotes because its own `fe_assemble`×3 + `eigsolve` are firm; the `eigenfrequency-qfactor` cross-link is a sibling reference, not a blocker"). (c) KEEP **electrostatic.{L4,L1,L0}**, **magnetostatic.{L4,L1,L0}** at `seed` but re-author their §Status to the OWN-composition reason: their directly-composed `solve_family` AND `gram_reduce` are `rough-in (test-coverage-bounded)` (NOT a sibling-column blocker — a genuine own-constituent gate). (d) KEEP **boundary-mode.{L4,L1,L0}** at `seed`, re-authoring its §Status to: its own composed `fe_assemble`+`eigsolve` are firm, but its directly-owned stage-(3) readout reduces into a waveguide-mode output-product that has no firm home yet (the own-readout gate; the `waveguide-mode` column is demand-gated). (e) **SOLE-OWN `feature/index.md`**: re-author the cohort-wide rule-prose (`:55`, `:57`) from the deadlock rule to the OWN-COMPOSITION rule, and re-narrate §Chapter-kind status (`:59-61`) from blanket-`seed` to the post-flip cohort state — name the 6 columns now `firm` (eigenmode/driven/transient + the two output-products eigenfrequency-qfactor/sparameters [D2's flips] + lifecycle [D3's flip, author-confirmed]) and the 6 staying `seed` (electrostatic/magnetostatic + capacitance/inductance/energy-fields + boundary-mode) with the OWN-composition reason for each seed-stay. Verify on-disk constituent `## Status` for each column's directly-owned ops before flipping (the evidence table above is the pre-localized hint; on-disk-confirm). Keep within-column high→low ordering; ZERO matrix-cell-link changes (no status cell exists). Memory `project_feature_column_promotion_rule`; CLAUDE.md §Extraction-goal FEATURE-SURFACE SPINE.
   - **deps:** none (D1 authors the index narrative from the canonical full-cohort verdict table supplied in this plan + on-disk constituent confirmation; it does not need D2/D3 to land first).
   - **rationale:** active-head #1 (THE LEAD); the user-directive enactment's driver-leaf payload + the single-index-owner narrative. Highest-fan-out (promotes the first driver columns off terminal `seed`).

2. **D2 — `layer-intro-author` — OUTPUT-PRODUCT cohort re-evaluation (own column files only; NO index write).**
   - **scope:** Re-evaluate the 5 output-product columns under OWN-COMPOSITION. (a) For each of eigenfrequency-qfactor, sparameters, capacitance, inductance, energy-fields × 3 levels: re-author the promotion-rule prose to the OWN-COMPOSITION rule (drop the deadlock clause). (b) FLIP `status: seed → firm` in **eigenfrequency-qfactor.{L4,L1,L0}** (own verb `eigenfreq_qfactor_reduce` firm c082 — re-narrate §Status: "promotes because its own reduce verb is firm; the `eigenmode` driver cross-link is a sibling reference, NOT a blocker" — this directly retires the `eigenfrequency-qfactor.L4:55` mutual-blocking assertion) and **sparameters.{L4,L1,L0}** (own verb `sparameter_reduce` firm c083 — re-narrate §Status: "promotes on its own firm reduce verb; the batch-26 meta-phase the c083 prose deferred to HAS now fired and enacted the OWN-COMPOSITION rule" — retiring the `sparameters.L4:54` "held pending batch-26" clause). (c) KEEP **capacitance.{L4,L1,L0}**, **inductance.{L4,L1,L0}** at `seed`, re-authoring §Status to the OWN reason: own verb `gram_reduce` is `rough-in (test-coverage-bounded)`. (d) KEEP **energy-fields.{L4,L1,L0}** at `seed`, re-authoring §Status to: own verb `domain_energy_reduce` (rough-in) + own folded `matrix-weighted-norm` (rough-in). **D2 does NOT touch `feature/index.md`** (D1 sole-owns it). Verify each column's own verb `## Status` on-disk before flipping. Within-column high→low preserved.
   - **deps:** none (disjoint column-file set from D1/D3; the index narrative D1 authors already accounts for D2's deterministic flip outcomes per the supplied verdict table).
   - **rationale:** active-head #1 (THE LEAD); the output-product payload — the two columns whose reduce verbs firmed c082/c083 and that the directive most concretely unblocks (the eigenfrequency-qfactor↔eigenmode deadlock; the sparameters held-pending-batch-26 status).

3. **D3 — `layer-intro-author` — SPINE-ROOT lifecycle re-evaluation (own column files only; NO index write).**
   - **scope:** Re-evaluate the `lifecycle` spine-ROOT column under OWN-COMPOSITION × 3 levels (L4/L1/L0). Re-author the promotion-rule prose to the OWN-composition rule. **Judge the flip from on-disk constituent status:** lifecycle's directly-owned driver-agnostic vocabulary is `fold_solve` (firm, the state-generated adaptive estimate-mark-refine outer fold) + the mesh-build stage; its stage-(2) per-driver dispatch is over the 5 driver feature columns, which per the spine-ROOT sub-kind are **other feature columns (references), NOT directly-owned vocab-op constituents**. If every directly-owned driver-agnostic constituent is firm (the evidence shows only `fold_solve`, which is firm), **FLIP `status: seed → firm`** in lifecycle.{L4,L1,L0}, re-narrating §Status (`lifecycle.L4:64`) to: "promotes because its own driver-agnostic composition (mesh-build + the firm `fold_solve` adaptive fold) is firm; the per-driver dispatch is over sibling feature columns, which are references, NOT blocking constituents." If the author finds a directly-owned constituent that is rough-in, KEEP `seed` with the OWN reason recorded (the conservative path). **D3 does NOT touch `feature/index.md`** (D1 sole-owns; D1's §Chapter-kind status names lifecycle in the `firm` set per the author-confirmed expectation). Within-column high→low preserved.
   - **deps:** none (disjoint column-file set; lifecycle.{L4,L1,L0} touched by no other dispatch).
   - **rationale:** active-head #1 (THE LEAD); the spine-ROOT payload. Distinct sub-kind judgment (constituents are sibling columns) — isolated into its own dispatch so the meta-feature reasoning is not conflated with the leaf-column sweeps.

## Overlap analysis

- **D1 ∩ D2:** D1 writes the 6 driver-leaf column files (eigenmode/driven/transient/electrostatic/magnetostatic/boundary-mode × 3) + `feature/index.md`. D2 writes the 5 output-product column files (eigenfrequency-qfactor/sparameters/capacitance/inductance/energy-fields × 3) and explicitly NOT `feature/index.md`. **Disjoint file sets. PARALLEL-SAFE.** The only shared surface is `feature/index.md` — D1 sole-owns it; D2 is instructed NOT to touch it (single-index-owner discipline; parallel-blind-shared-index guard). D1's index narrative names D2's deterministic flip outcomes (eigenfrequency-qfactor + sparameters → firm) from the canonical verdict table, so D1 does not need D2 to land first.
- **D1 ∩ D3:** D1's driver-leaf files + index vs D3's lifecycle.{L4,L1,L0}. **Disjoint file sets. PARALLEL-SAFE.** D3 does NOT touch `feature/index.md`; D1's §Chapter-kind status names lifecycle in the firm set per the author-confirmed expectation (if D3 conservatively keeps lifecycle at seed, the index narrative would carry a one-cell drift — a minor wave conflict the per-report integrator reconciles, surfaced as an integrator-signal; acceptable per the conflict-tolerance philosophy, and the evidence strongly indicates lifecycle flips). NOTE: lifecycle.L4 frontmatter live-links the 5 driver columns; D1 flips 3 of those driver columns' `status:` frontmatter — this does NOT alter any link target (the file paths are unchanged), so no cross-file link breakage.
- **D2 ∩ D3:** output-product files vs lifecycle files. **Disjoint. PARALLEL-SAFE.** No shared surface (neither touches the index).
- **DUAL-REGISTRATION partition (the index-dual-registration guard):** the `feature/index.md` matrix carries NO per-column status row/cell and NO per-column §Vocabulary-cohort bullet that producers own — the only index artifacts are (1) the cohort-wide rule prose and (2) the §Chapter-kind status narrative, BOTH of which are a single consolidated narrative, NOT per-producer rows/bullets. So the dual-registration split (own-row + own-bullet vs consolidated-tally) collapses to: **D1 owns the ENTIRE index narrative (both pieces); D2/D3 own ONLY their column-file §Status blocks.** Stated explicitly so D2/D3 do not author any index bullet/row (there are none to author) and do not defer-then-strand any index content.

## Sequencing schedule

**Single wave (all parallel):** D1, D2, D3 fire together. All three file sets are disjoint; the sole shared surface (`feature/index.md`) is sole-owned by D1; the cross-cohort flip outcomes D1's index narrative references are deterministic from the canonical verdict table in this plan (not dependent on D2/D3 landing order). Per the conflict-tolerance philosophy, when in doubt mark PARALLEL — and here there is no genuine file-region overlap. If D3 conservatively keeps lifecycle at seed against the expected flip, the resulting one-cell index-narrative drift is a cheap per-report-integrator reconcile, not a reason to sequentialize.

Pipeline: D1/D2/D3 (parallel) → 3 critics (parallel) → repairers (as needed) → `integrator-per-report` ×3 (serial) → ONE `integrator-finalize`.

## Open questions / caveats

- **The priorities.md #1 expectation that electrostatic + magnetostatic flip is WRONG per on-disk evidence — the plan overrides it.** Both columns directly compose `solve_family` (rough-in (test-coverage-bounded)) AND `gram_reduce` (rough-in), which are vocab-op constituents (NOT sibling columns). Under the OWN-COMPOSITION rule they STAY seed. The directive's text in CLAUDE.md/role-spec names "the driver columns whose solve+assemble combinators are firm" — for electrostatic/magnetostatic the solve combinator (`solve_family`) is NOT firm, so they correctly do not qualify. The active-head #1 prose said "per the 5-driver→L4 AFFIRMED-COMPLETE survey c082 — verify each driver's own solve/assemble constituents are all firm"; the verification shows 3 of 5 drivers (eigenmode/driven/transient) have all-firm own constituents and 2 (electrostatic/magnetostatic) do not. This is the value of the paste-inline deliverable-presence verification. **Flag for the batch-27 meta-phase:** the active-head #1 column-flip list was an expectation, not a verified set; the realized flip set is {eigenmode, driven, transient, eigenfrequency-qfactor, sparameters, lifecycle}, NOT including electrostatic/magnetostatic. This is consistent with the directive (own-composition gate), just narrower than the active-head prose anticipated.
- **`solve_family` / `gram_reduce` rough-in are now the convergent gate for the electrostatic/magnetostatic/capacitance/inductance columns' future promotion.** A future high-fan-out item: firming `solve_family` (a list-homomorphism law-confidence pass — its own §Status names "a literature/strawman-derivation harvester pass that raises the list-homomorphism law confidence to ksp_solve-equivalent" as the likely route) would unblock electrostatic + magnetostatic driver columns; firming `gram_reduce` (gated on `matrix-weighted-norm` + `bilinear-form` firm — the NO-GO-HELD cascade) would unblock capacitance + inductance. These are NOT dispatched this cycle (the cascade is NO-GO HELD; the `solve_family` law-confidence pass is a fresh candidate I am appending to the plan Backlog for batch-27 ranking). The `solve_family`-law-confidence-pass candidate is genuinely open and cleanly-describable (analogous to the c082/c083 reduce-verb firm-on-positive-structure promotions) and may be the highest-fan-out forward-frontier pick for c086/c087.
- **Target promoted token = `firm` (author-confirmed per column).** The role-spec does not literally name the post-`seed` token; the natural value for a composition-root over all-firm constituents is `firm`. If a critic or the meta-phase prefers a distinct feature-column promoted token (e.g. `composed` / `promoted`), that is a token-vocabulary question for the batch-27 meta-phase — the authors should use `firm` this cycle (the maturity the evidence supports) and the meta-phase can re-token uniformly if it wants a feature-specific value. Flagged so the meta-phase can rule.
- **lifecycle flip is author-confirmed, not planner-asserted.** I route the final maturity call to D3 from on-disk constituent status because the spine-ROOT sub-kind's "directly-owned vs reference" boundary is a judgment (its stage-2 IS other feature columns). The evidence (`fold_solve` firm; sibling columns are references) supports a flip; D3 confirms.
