---
agent: cycle-planner
invoked_at: 2026-06-07T071941Z
scope: cycle-122 dispatch plan
status: pending
---

# Cycle 122 dispatch plan

c122 is the SECOND primary cycle of meta-batch-39 (cycles 121/122/123; the batch-39 meta fires AFTER c123 finalize). NO session restart since c121 (c121 made no role-spec edits; context continuity holds). c121 landed at commit `ae2e2f4`.

## Goals selected this cycle

c121 (the lift-through campaign opener) broke the plateau: reachable 139→156 (+17), STRONGER 27→22, rank_violations HELD 0. c122 is the **natural consumer-wiring + grounding follow-up**: harvest the c121 rough-in/roadmap_goal rows to firm (the AMR verb cohort + the `correction_step` L2 combinator), discharge the `unresolved_depends_on_targets: 6` (the 2 AMR verbs + the 4 libceed substrate ops), audit the 3 DIRECTIVE-3 impl-realizes-API correspondences (the standing meta integrity duty), and evaluate the GMG column rough-in→firm promotion. The RE re-check (below) confirms RE10 DISCHARGED + RE9/RE1 GROUNDED; the residual RE set (RE2/RE3/RE5/RE6/RE7/RE8) is consumer-gated to the c122/c123 fronts, NOT advanced by this cycle's harvest. MPI/sharding stays OUT (DIRECTIVE-1).

## RE-premise re-check on the LANDED tree (standing Axis-2 duty)

Live linter baseline read off disk this cycle (`python3 tools/graded-stack-lint/graded_stack_lint.py`, EXIT 0): `files=378, typed=317, untyped=61, roots=41, reachable=156, detritus=123, STRONGER=22, rank_violations=0, unresolved_depends_on_targets=6, promotion_frontier=11, expected_unreachable_outside_dag=47`. Matches the c121 finalize cumulative exactly.

Mapping the live STRONGER-22 set against the RE ledger (`scaffolding/graded-stack-baseline-exceptions.md`):

| RE | members | in STRONGER-22? | verdict |
|----|---------|-----------------|---------|
| **RE10** | `L1/interpolator` (+theme) | NO (off the list) | **DISCHARGED ✓** — confirmed; the 2 c121-D8 faithful `depends-on (uses)` edges (divfree-projector + waveguide_mode_reduce) ground it. |
| **RE9** | `L1/fe_space_hierarchy` | NO (off the list) | **GROUNDED ✓** — the GMG column composes it by name (`feature/geometric-multigrid-preconditioner.L1 → L1/fe_space_hierarchy composes`); LIVE. |
| **RE1** | `L4/chebyshev`, `L3/chebyshev`, `L2/chebyshev-iteration`, `L2/jacobi-smoother`, `L4/preconditioning-framework` | NO (none on the list) | **GROUNDED ✓** — the GMG column composes the smoother legs / preconditioning-framework by name; the chebyshev/jacobi vocabulary is now reachable via the live column. |
| **RE5** | `L2/normalize`, `L3/normalize`, `L2/reciprocal`, `L3/reciprocal`, `L2/nrm2` | YES (`L2/normalize`, `L3/normalize`, `L2/reciprocal`, `L3/reciprocal` = 4) | **NOT discharged** — the GMG column grounded the *L1* `normalize`/`reciprocal` ops (firm constituents); the L2/L3 iteration-views remain absorbed below the spine (no faithful inbound consumer composes the L2/L3 views by name). Stays baseline-exception. |
| **RE7** | `L2/elementwise_product`, `L3/elementwise_product`, `L3/assemble-diagonal`, `L3/jacobi-smoother` | YES (`L2/elementwise_product`, `L3/elementwise_product`, `L3/assemble-diagonal` = 3) | **NOT discharged** — same shape as RE5; the diagonal-apply/extract L2/L3 views ride the absorbed leg, no by-name composer. Stays baseline-exception. |
| **RE6** | `L2/axpy`, `L2/axpby`, `L2/axpbypcz`, `L2/scal`, `L3/axpy`, `L3/axpby`, `L3/axpbypcz`, `L3/scal` | YES (all 8) | **NOT discharged** — combinator-arity-notes refactor (item-4c) pending; not scheduled c122. |
| **RE2** | `L3/orthogonalize` | YES (1) | **NOT discharged** — needs the L3-iteration-view feature column (item-4b); consumer-gated. |
| **RE8** | `L3/fold_solve`, `L3/krylov-step` | YES (2) | **NOT discharged** — same item-4b consumer gate. |
| **RE3** | `L2/gram`, `L2/deflate` | NO (in the demand-gated/edge-untyped bucket, not STRONGER) | gated on `deflate`/NLEPS consumer (item-4a, couples to eigsolve-impl). |

**STRONGER-22 = 22 accounted: RE5(4) + RE7(3) + RE6(8) + RE2(1) + RE8(2) = 18 ratified RE members; + 4 c121-NEW grounded-future nodes (`L1/libceed-quadrature-kernel-impl`, `L3/eigsolve-impl`, `L3/lanczos_step`, `L1-L0/amr-estimate-mark-refine`).** The 4 new members are the INTENDED DIRECTIVE-3 grounded-future disposition (only inbound is the free `realizes-kernel-api`/`realizes-leaf` `reference` edge or a rough-in theme) — `[GARBAGE*]` by design, awaiting their c122/c123 blocking consumers. NOT a count CLIMB without a ratified RE (the escalate guard does NOT fire): the +4 are the c121-authored constructive frontier the campaign is mid-building, and reachable CLIMBED 17.

**RE-recheck verdict:** RE10 DISCHARGED, RE9/RE1 GROUNDED (the c121 GMG/interpolator landings fired their promotion conditions exactly as predicted). Residual ratified live set = **RE2/RE3/RE5/RE6/RE7/RE8** (RE4 members not in this snapshot). All residual REs are consumer-gated to fronts NOT advanced by c122's harvest-and-audit slate; the batch-39 meta ratifies the discharged set (RE9/RE10) + the residual set after c123.

## Deliverable-presence verification (paste-inline evidence)

| Dispatch | scope file(s) | check | evidence |
|---|---|---|---|
| D1 `flux_recovery_estimate` | `book/src/L1/flux_recovery_estimate.md` | (1) existence | `ls` → "No such file or directory" — **OPEN (fresh harvest)**. Rough-in dep-map row stands at `book/src/L1/index.md:195` (`palace/linalg/errorestimator.cpp:184-268,:273-378,:391-500`, rough-in). |
| D2 `dorfler_mark` | `book/src/L1/dorfler_mark.md` | (1) existence | `ls` → "No such file or directory" — **OPEN (fresh harvest)**. Rough-in dep-map row stands at `book/src/L1/index.md:194` (`palace/utils/dorfler.cpp:14-171`, rough-in). |
| D3 `correction_step` | `book/src/L2/correction_step.md` / `book/src/L2/residual-correction-step.md` | (1) existence | both `ls` → "No such file or directory" — **OPEN (fresh harvest + replace-and-propagate)**. Rough-in dep-map row stands at `book/src/L2/index.md:96` (combinator; replace-and-propagate; `gmg.cpp:174-176`+`distrelaxation.cpp:104`). |
| D4 `libceed-substrate-ops` | `book/src/L1/{element_restrict,basis_apply,quad_point_contract,geom_factor_build}.md` | (1) existence | all 4 `ls` → "No such file or directory" — **OPEN (fresh harvest)**. They are the 4 `unresolved_depends_on_targets` from `L1/libceed-quadrature-kernel-impl`. |
| D5 lowering-verifier (smoother↔triangular-solve) | audits `book/src/L1/multigrid-relaxation-smoother.md` ↔ `book/src/L1-L0/triangular-solve-obstruction.md` | (2) maturity | smoother `firmness: firm` (kernel-impl); triangular-solve `kernel-api` role-label at `:545`. (4) audit-class: no `verified_against:` impl-realizes-API block yet — **OPEN (no prior audit)**. |
| D6 lowering-verifier (libceed + eigsolve) | audits `book/src/L1/libceed-quadrature-kernel-impl.md` ↔ `fe-assemble-libceed-boundary-obstruction.md`; `book/src/L3/eigsolve-impl.md` ↔ `book/src/L3/eigsolve.md` | (2) maturity | both impls `roadmap_goal` (correspondence audit is a structural review, not a firm-claim audit). OQ `libceed-quadrature-kernel-impl-realizes-api-faithfulness-audit` + `eigsolve-impl-lowering-verifier-correspondence-audit` OPEN — **OPEN (no prior audit)**. |
| D7 GMG promotion-eval | `book/src/feature/geometric-multigrid-preconditioner.{L1,L4}.md` | (2) maturity | `rank: rough-in`; all blocking `depends-on` (fe_space_hierarchy firm, multigrid-relaxation-smoother firm, reciprocal firm, normalize firm) are firm → NOT rank-blocked. **OPEN (promotion-eval, not yet attempted)**. |

OQ-ledger RESOLVED-grep: `grep -niE "(correction_step|dorfler|flux_recovery|element_restrict|eigsolve-impl|realizes-kernel-api).*(RESOLVED|CLOSED)" scaffolding/open-questions.md` → no RESOLVED/CLOSED matches for any c122 scope (only the c121 grounding-confirm + firmness-gate action entries, both OPEN with `Action (c122 ...)` directives). STOP-PROPOSING negative-list check: none of the c122 scopes match the disqualified L3-backfill slugs (`lu_solve`/`back_solve`/`ls-update-column`/`nleps_*`). DIRECTIVE-1 boundary: NO MPI-associated scope (`rap.{hpp,cpp}`/`geodata.cpp`/MPI collectives) dispatched; the dorfler cross-rank bisection is read single-rank-degenerate (OQ `dorfler-cross-rank-bisection-distributed-note-deferred`).

Source-anchor codemap verification (paths confirmed this cycle, on-disk via the codemap MCP):
- `palace/utils/dorfler.cpp:14` `ComputeDorflerThreshold` + `:198` caller — confirmed.
- `palace/linalg/errorestimator.cpp:184` `ComputeErrorEstimates`, `:273` `GradFluxErrorEstimator`, `:391` `CurlFluxErrorEstimator`, `:529-566` Grad+Curl composite (`+=`) — confirmed.
- `palace/linalg/chebyshev.cpp:178`/`:241` `dinv.Reciprocal()` — the RE5/RE7 diagonal-extract site `correction_step` keeps OUT of run-time apply (construction stratum) — confirmed.

## Dispatches

- **D1 (`harvester`, HIGH-MEDIUM, WAVE-1) — `flux_recovery_estimate` L1 verb (AMR estimate stage).**
  - scope: formalize `book/src/L1/flux_recovery_estimate.md` (ZZ flux-recovery a-posteriori error estimate: recover smooth flux by projecting discontinuous material flux εE/μ⁻¹B onto a smooth FE space, per-element L2 norm of the difference). Source: `palace/linalg/errorestimator.cpp:184-268` (`ComputeErrorEstimates`), `:273-378` (`GradFluxErrorEstimator`), `:391-500` (`CurlFluxErrorEstimator`). Model the **flux-channel variant axis** (Grad-only / Curl-only / Grad+Curl composite) — confirm the OQ `flux-recovery-estimate-flux-channel-axis-vs-separate-verbs` collapse holds (the `+=` composite at `:529-566` is an `axpy(1,·)`/`linear_combination`-over-indicators shape, NOT a new estimate verb). Resolve the `flux-projector-constructed-operator-gate-vs-absorbed` OQ (lean: `FluxProjector` is a construction-time member absorbed into the `Estimator` closure). The libCEED quadrature integrator below it is the on-disk `kernel-api` leaf (`fe-assemble-libceed-boundary-obstruction`) — link, do not re-derive. Resolves 1 of 6 `unresolved_depends_on_targets`.
  - deps: none.
  - rationale: priorities item-3 (`amr-estimate-mark-refine`, DIRECTIVE-2 grounded consumer-(2)); one of the 2 verbs gating the `amr-estimate-mark-refine` theme firm-flip (OQ `amr-estimate-mark-refine-theme-firmness-gate`). New vocabulary cohort.

- **D2 (`harvester`, MEDIUM, WAVE-1) — `dorfler_mark` L1 verb (AMR mark stage).**
  - scope: formalize `book/src/L1/dorfler_mark.md` (Dörfler bulk marking `θ ▷ indicators → IndexSet`: smallest element index set whose summed squared error covers ≥ θ of total; sort + partial-sum + lower-bound threshold + threshold→index-set; load-bearing over-mark tie-break). Source: `palace/utils/dorfler.cpp:14-171` (`ComputeDorflerThreshold`), `:198` caller, `palace/drivers/basesolver.cpp:103-115`. **Read single-rank** (the cross-rank GlobalMin/Max/Sum bisection `:67-68`/`:84-85`/`:101-166` is degenerate single-rank: `min_threshold == max_threshold`; flag the distributed reconciliation as the deferred DIRECTIVE-1 future-direction note per OQ `dorfler-cross-rank-bisection-distributed-note-deferred`, do NOT lift it). Resolves 1 of 6 `unresolved_depends_on_targets`.
  - deps: none.
  - rationale: priorities item-3; the second verb gating the `amr-estimate-mark-refine` theme firm-flip. Self-contained (no kernel-api leaf below).

- **D3 (`harvester`, HIGH, WAVE-1) — `correction_step` L2 combinator (replace-and-propagate).**
  - scope: formalize `book/src/L2/correction_step.md` (canonical slug; the c121 D6 dep-map row at `book/src/L2/index.md:96` names this slug — author THIS file) — the preconditioned residual-correction step `y + B·(x − A·y)`. **Replace-and-propagate, NOT mine-and-strand** (OQ `correction-step-replace-and-propagate-scope`): make `correction_step` the L2 entry; re-express the L2 `jacobi-smoother` (`B=ω·D⁻¹`) and `chebyshev-iteration` (`B=p_order(D⁻¹A)`) bodies as `correction_step` specialization-notes choosing `B`; the D1-c121 V-cycle L1 surface / D3-c121 `multigrid-relaxation-smoother` express their per-sweep bodies THROUGH `correction_step`. Source: `gmg.cpp:174-176` + `distrelaxation.cpp:104` (the verbatim per-sweep body). Decide the one-vs-two-operator conjugated form (OQ `correction-step-one-vs-two-operator-conjugated-form`: `correction_step` with any-LinOp `B` vs a distinct `conjugated_correction_step`; lean (a) single combinator, conjugation a note). Keep `assemble-diagonal`/`reciprocal` `B`-construction OUT of the run-time apply (construction stratum, OQ `correction-step-construction-vs-apply-stratum-assemblediagonal-reciprocal`). Annotate `divfree-projector` as a borderline specialization-note, NOT a core instance (OQ `correction-step-divfree-projector-borderline-7th-instance`). Over-unification guard (from the dep-map row): the bare `B` apply / the Krylov shift-invert step / the libceed contraction substrate are explicitly OUT.
  - deps: none (own L2 file + propagation edits to `L2/jacobi-smoother.md`, `L2/chebyshev-iteration.md` — distinct files from D1/D2).
  - rationale: priorities item-2 / item-1 coupling; HIGH fan-out (the shared smoother/V-cycle per-sweep core; the redirect's not-optional replace-and-propagate close of the c121 mine). The combinator must reach upward as the entry; the smoother bodies become specialization notes.

- **D4 (`harvester`, MEDIUM, WAVE-1) — libceed shared-substrate ops (the 4 `unresolved_depends_on_targets`).**
  - scope: mine the 4 shared-substrate ops the `L1/libceed-quadrature-kernel-impl` depends-on but that have NO file: `element_restrict`, `basis_apply`, `quad_point_contract`, `geom_factor_build` — the matrix-free FE operator-application-as-tensor-contraction primitives (`A = Gᵀ B_𝒟ᵀ D B_𝒟 G`: gather/restrict → basis-eval → quad-point contract → geometry-factor). **Clean-gate per the redirect:** if the substrate is cleanly composable from firm L1 tensor vocabulary, author the firm/rough-in chapters (resolving the 4 unresolved targets + promoting `libceed-quadrature-kernel-impl` roadmap_goal→rough-in per OQ `libceed-quadrature-kernel-impl-roadmap-goal-vs-rough-in-disposition`); if the genuine vocabulary shift (flat-vector BLAS vs `[E, P, ...]` rank structure) means they are intended/speculative, seed them as `roadmap_goal` rank-0 chapters (the `unresolved → resolved-as-roadmap_goal` route — live links, no false firm claim). Confirm the `basis_apply` sum-factorization is a transparent trick (one-line note, OQ `libceed-quadrature-kernel-impl-sum-factorization-classification`). Source: `palace/fem/libceed/operator.cpp` + the `fe-assemble-libceed-boundary-obstruction` kernel-api contract.
  - deps: none (the 4 substrate files are disjoint from all other dispatches; the inbound edges already exist in `libceed-quadrature-kernel-impl`).
  - rationale: priorities item-2a (`constructive-spine-kernels` shared substrate); resolves 4 of 6 `unresolved_depends_on_targets`; the c121 D6 probe + D4 kernel-impl converge on this cohort (HIGH shared-substrate fan-out across the D3/D4/D5 c121 kernel-impl cohort).

- **D5 (`lowering-verifier`, MEDIUM, WAVE-1) — `multigrid-relaxation-smoother` realizes-kernel-api correspondence audit.**
  - scope: audit `book/src/L1/multigrid-relaxation-smoother.md` (kernel-impl, firm) ↔ `book/src/L1-L0/triangular-solve-obstruction.md` (kernel-api role-label) — confirm the constructive GS-SSOR/distributive relaxation impl computes the same per-sweep result as the opaque triangular-solve/HYPRE relax the API documents (the `realizes-kernel-api` correspondence DIRECTIVE-3 wants reviewed; append a `verified_against:` impl-realizes-API block). Confirm the `reference`-class edge does NOT carry a `depends-on` (integrity gate). Sequential-obstruction noted for the GS recurrence — confirm the boundary.
  - deps: none (audit-only; appends to the smoother chapter + may append the audit block).
  - rationale: priorities standing kernel-API/impl-integrity gate (DIRECTIVE-3, the meta standing duty); OQ (the smoother realizes-kernel-api audit, the firm-impl member of the DIRECTIVE-3 integrity cohort).

- **D6 (`lowering-verifier`, MEDIUM, WAVE-1) — libceed + eigsolve realizes-kernel-api correspondence audits.**
  - scope: audit BOTH (i) `book/src/L1/libceed-quadrature-kernel-impl.md` ↔ `book/src/L1-L0/fe-assemble-libceed-boundary-obstruction.md` (kernel-api) — the constructive `A = Gᵀ B_𝒟ᵀ D B_𝒟 G` pipeline ↔ opaque libCEED kernel; strongest empirical anchor `test/unit/test-libceed.cpp` `TestCeedOperatorFullAssemble` (assembled matrix matches MFEM reference to 1e-12), OQ `libceed-quadrature-kernel-impl-realizes-api-faithfulness-audit`; AND (ii) `book/src/L3/eigsolve-impl.md` ↔ `book/src/L3/eigsolve.md` (kernel-api, partial-obstruction) — the constructive Lanczos/Arnoldi/Krylov-Schur ↔ opaque `EPSSolve` eigenpair correspondence (modulo tolerance + the four L1 non-determinism sources), OQ `eigsolve-impl-lowering-verifier-correspondence-audit`. Both impls are `roadmap_goal` — the audit is a STRUCTURAL correspondence review (does the sketched pipeline faithfully match the contract?), the `empirical-match` justification stays deferred to firming. Also adjudicate the `realizes-leaf` label question (OQ `realizes-leaf` label disposition: keep distinct / fold into plain `reference` / reuse `realizes-kernel-api`) — a free relabel either way.
  - deps: none (audit-only; distinct files from D5).
  - rationale: priorities standing kernel-API/impl-integrity gate (DIRECTIVE-3); the 2 remaining members of the impl-realizes-API integrity cohort.

- **D7 (`layer-intro-author`, LOW-MEDIUM, WAVE-1) — GMG column rough-in→firm promotion-eval.**
  - scope: evaluate `book/src/feature/geometric-multigrid-preconditioner.{L1,L4}.md` for the rough-in→firm flip. The blocking `depends-on` constituents are ALL firm on disk (`L1/fe_space_hierarchy` firm, `L1/multigrid-relaxation-smoother` firm, `L1/reciprocal` firm, `L1/normalize` firm — the V-cycle is `cites-evidence` `gmg.cpp:126-205`; `L3/chebyshev`/`L2/jacobi-smoother` are `reference`-class, not blocking) → the well-foundedness invariant does NOT block promotion. Promote ONLY if the column's own composition algebra (the pure V-cycle recursion + the per-level restrict/prolong/relax composition) is exhaustively cited and firm; if a genuine evidence gap remains (e.g. the V-cycle recursion needs a fuller pure-function rendering, or a constituent caveat), KEEP rough-in and record the precise promotion condition. Do NOT force the flip. If promoting, keep `feature_root: seed` (GC-root marker, not a maturity tier).
  - deps: none (own feature-column files; disjoint from all other dispatches).
  - rationale: priorities item-1 (the LEAD column); the natural maturity step once its firm constituents landed c121. Low-medium fan-out (the column is already reachable/live; this is a maturity flip, not new vocabulary).

## Overlap analysis

Pairwise (changes to the same operator entry OR one's output names operators the other rewrites = OVERLAPPING → sequential; distinct dep-map rows / distinct files = parallel-safe):

- **D1 ↔ D2**: distinct L1 files (`flux_recovery_estimate.md` vs `dorfler_mark.md`). Both append a dep-map row to `book/src/L1/index.md` (the c121 rough-in AMR group, rows `:194`/`:195`). Distinct anchor-rows → NOT overlapping at the operational level → PARALLEL. NO consolidated tally in the L1 index AMR group (the index carries no firm-count for this group). Each owns its OWN dep-map row (flip rough-in→firm in place) + its own SUMMARY/§Vocabulary-cohort registration.
- **D1/D2 ↔ D3**: D3 is L2 (`correction_step.md` + propagation edits to `L2/jacobi-smoother.md`/`L2/chebyshev-iteration.md`). Disjoint files/layer from D1/D2. PARALLEL.
- **D1/D2/D3 ↔ D4**: D4 is the 4 libceed substrate L1 files (`element_restrict`/`basis_apply`/`quad_point_contract`/`geom_factor_build`). Disjoint from the AMR verbs (D1/D2) and the L2 combinator (D3). Both D1/D2 and D4 append to `book/src/L1/index.md` — distinct group/distinct rows (AMR group vs a new libceed-substrate group) → NOT overlapping → PARALLEL. D4 owns its own new index group + rows.
- **D4 ↔ D6**: D6 audits `libceed-quadrature-kernel-impl.md` (the CONSUMER of D4's 4 ops); D4 authors the 4 substrate ops + may flip `libceed-quadrature-kernel-impl` roadmap_goal→rough-in. POTENTIAL same-file touch on `libceed-quadrature-kernel-impl.md` (D4 edits its status/edges-resolution; D6 appends a `verified_against:` audit block). Mark SEQUENTIAL: D6 in WAVE-2 after D4 (so D6 audits the post-resolution state and the audit block lands on the resolved node). Per the conflict-tolerance philosophy this is a genuine same-file region touch → sequential is correct.
- **D5 ↔ D6**: D5 audits the smoother↔triangular-solve pair; D6 audits the libceed + eigsolve pairs. Distinct chapter files. PARALLEL (both lowering-verifier, but different scopes/files — the agent type is the same, the artifact regions are disjoint).
- **D3 ↔ D7**: D3 may reference the GMG smoother's per-sweep body expressing through `correction_step`; D7 evaluates the GMG column. D3 edits `L2/*`; D7 edits `feature/geometric-multigrid-preconditioner.{L1,L4}.md`. Disjoint files. D3 does NOT edit the GMG column (it edits the L2 smoother bodies + its own L2 file). PARALLEL. (If D3's propagation set wanted to re-express the GMG L1 per-sweep body through `correction_step`, that would touch D7's file — but the OQ scopes D3's propagation to the L2 unfoldings + the V-cycle body via the combinator's L1 analog, NOT the GMG feature-column file; the GMG column links DOWN to `correction_step`, it does not get rewritten. Kept PARALLEL; minor conflict, if any, is cheaply merged.)
- **D7 ↔ all**: own feature-column files, disjoint. PARALLEL.

No shared consolidated-tally / shared-index-count collision this cycle (no ≥2 parallel dispatches landing chapters into a layer index that carries a consolidated firm/partial-obstruction running count — the L1/L2 index AMR + libceed-substrate groups carry no consolidated tally; the rough-in→firm flips are in-place row edits, not cohort aggregates). No cross-report forward-reference to a not-yet-existing sibling slug (D3's canonical slug `book/src/L2/correction_step.md` is named in its OWN scope + already on disk as the dep-map row name; no sibling forward-ref).

## Sequencing schedule

- **WAVE-1 (parallel)**: D1, D2, D3, D4, D5, D7 (6 dispatches). All have `deps: none` and touch disjoint artifact regions (modulo the parallel-safe distinct-row index appends).
- **WAVE-2 (after WAVE-1 reports land)**: D6 (dep D4 — audits the post-resolution `libceed-quadrature-kernel-impl` state so the audit block lands on the resolved node).

7 dispatches total (well within the 12 cap). The book is NOT rebuilt between waves; ONE `integrator-finalize` runs at cycle end (the WAVE-2 ordering is forward-reference/same-file ordering for the per-report integrator, not a per-wave finalize).

## priorities.md update

Appended the c122 planner-dispatch note to the CYCLE-121/batch-39 active head (marking the c122 picks against items 1/2/3 + the standing kernel-api/impl-integrity gate + the AMR consumer front), and recorded the RE-recheck verdict (RE9/RE10 discharged; RE2/RE3/RE5/RE6/RE7/RE8 residual, consumer-gated). No new fan-out-ranked backlog candidate needed — all c122 picks are direct consumers/follow-ups of the batch-39 active head (items 1/2/3) already in the plan.

## Open questions / caveats

- **The `correction_step` replace-and-propagate (D3) is the redirect's not-optional close** of the c121 D6 mine — if the harvester finds the propagation set larger than the OQ-named instances (jacobi/chebyshev/distributive + the V-cycle body), it should propagate to all of them (not strand the combinator). The same-layer-cross-cutter confirmation of the propagation set (OQ `correction-step-replace-and-propagate-scope` names a same-layer-cross-cutter follow-on) is deferred to c123 if the propagation surfaces a wider set than D3 can cleanly land — flag for the batch-39 meta if so.
- **D4 disposition (firm/rough-in vs roadmap_goal) is a genuine clean-gate judgment** — the c121 OQ leans roadmap_goal (the `[E,P,...]` rank-structure vocabulary shift is real). If D4 lands them roadmap_goal, the 4 `unresolved_depends_on_targets` resolve to LIVE roadmap_goal links (warning clears, no false firm claim) but `libceed-quadrature-kernel-impl` STAYS roadmap_goal (well-foundedness: cannot exceed its least-resolved dep). That is the correct grounded-future state, NOT a defect.
- **The residual RE set (RE2/RE3/RE5/RE6/RE7/RE8) is NOT advanced by c122** — its consumers (the deflate/NLEPS consumer for RE3; the L3-iteration-view feature column for RE2/RE8; the combinator-arity-notes refactor for RE6; a preconditioner-construction feature surface for RE5/RE7) are item-4 fronts, consumer-gated to c123+/the batch-39 meta. c122 is deliberately the harvest-and-audit consolidation cycle (the c121 wide wave's natural settling), not a second wide-open wave. If c123 has budget, item-4b (the L3-iteration-view column discharging RE2/RE8, coupling to the c121 eigsolve-impl) is the highest-fan-out residual-RE discharge.
- **GMG promotion (D7) is a judgment-gated flip, not a forced one** — if the V-cycle pure-function rendering has a genuine evidence gap, KEEP rough-in. Recorded so the integrator does not treat a kept-rough-in as a missed deliverable.
- **Carried linter-maintenance ask** (`--show-stronger` per-node STRONGER-attribution flag): the RE-recheck above still required a manual JSON-parse-and-diff of the STRONGER-22 list against the RE ledger. Now that the project is in a forward-build posture (the batch-38 NO-GO is moot), this flag would make every cycle's RE-recheck mechanical. Re-flag for the batch-39 meta as a GO-candidate (carried from the c121 integration-tooling-friction note).
