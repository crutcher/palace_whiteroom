---
agent: cycle-planner
invoked_at: 2026-06-02T075145Z
scope: cycle-061 dispatch plan
status: pending
---

# Cycle 061 dispatch plan

FIRST primary cycle of meta-batch-19 (cycles 061/062/063; the batch-19 meta-phase fires AFTER cycle-063's finalize). The two-combinator MAP/FOLD solver spine is COMPLETE and DESCENDED (batch-18). The frontier moves to the next genuinely-new vocabulary front under the 2026-06-01 VOCABULARY-SHIFT REDIRECT.

## Goals selected this cycle

**The `weak_form_term` FE-vocabulary cohort lead RECRUITS this cycle — the pull-check FIRES.** The active-head #1 is pull-gated: author non-diffusion FE term forms ONLY when a pipeline pull-up concretely needs one. The codemap pull-check found a concrete, in-scope, firm-pipeline pull: the **magnetostatic** `CurlCurlOperator::GetStiffnessMatrix` (`palace/models/curlcurloperator.cpp:170-181`) builds its K-matrix with the SAME `BilinearForm`-fold pattern the firm electrostatic `fe_assemble` witness uses (`laplaceoperator.cpp:188-194`), differing ONLY by the term: `CurlCurlIntegrator(muinv)` vs `DiffusionIntegrator(epsilon)`. The magnetostatic pipeline is in-scope and already characterized (a firm `solve_family` fixed-operator member). So a 2nd pipeline concretely needs a non-diffusion (curl-curl) term — the redirect clean-gate is satisfied. We recruit the `weak_form_term` L1 vocabulary entry (the `(coefficient, differential-operator)` term abstraction `fe_assemble` already folds over opaquely) grounded by BOTH the witnessed diffusion term AND the now-pulled curl-curl term, plus its concept page. This is genuinely-new FE vocabulary (NOT a rectangular floor) serving every assembled-operator pipeline. Secondary: a cheap observation-first probe of the remaining driven/transient outer-machinery (per active-head #2). The in-layer-conciseness combinator family (#3) stays GATED (no ≥2-witness recurrent pattern surfaces this cycle; the next in-layer combinator comes from new solver/FE material, not the combinator-complete BLAS/projector surface).

## Pull-check verdict (active-head #1, the load-bearing first job)

**VERDICT: PULL FIRES → RECRUIT the `weak_form_term` cohort (curl-curl + the term abstraction) this cycle.**

Codemap evidence (all `mcp__palace-codemap__search_text` / `read_range`, pasted):

- Electrostatic (the firm `fe_assemble` witness) — `laplaceoperator.cpp:194`: `k.AddDomainIntegrator<DiffusionIntegrator>(epsilon_func);` then `:194` `k.Assemble(...)`. Term = **diffusion** `(Q∇u,∇v)`.
- Magnetostatic (the PULL) — `curlcurloperator.cpp:181`: `k.AddDomainIntegrator<CurlCurlIntegrator>(muinv_func);` inside `CurlCurlOperator::GetStiffnessMatrix()` (`:170`), then `auto k_vec = k.Assemble(GetNDSpaces(), skip_zeros);`. Term = **curl-curl** `(Q curl u, curl v)` — a NON-diffusion term, same build/fold structure.
- The two K-matrix builders are structurally identical (`BilinearForm k(space); k.AddDomainIntegrator<TERM>(coef); k.Assemble(...)`) — they differ ONLY in the `weak_form_term` (the `(coefficient, differential-operator)` pair). This is exactly the cohort `fe_assemble` already folds over as an opaque `WeakFormTerm` input.
- `fe_assemble.md:155-167` (on disk) already names the cohort opaquely and DEFERS it: "`WeakFormTerm` (type) — opaque rough-in input; the `(coefficient, differential-operator)` pair ... the witnessed differential-operator cohort so far is ∇ (diffusion), identity (mass), curl (curl-curl), div (div-div) (`palace/fem/integrator.hpp:39-130`)." `fe_assemble.md:208-209`: "Defining `fe_assemble` therefore does NOT require formalizing `weak_form_term` first." → so authoring `weak_form_term` now is a clean REFINEMENT of an already-anticipated opaque rough-in input, not a forced new abstraction.
- The term family is a clean L0-anchored type hierarchy: `integrator.hpp:39-65` `BilinearFormIntegrator` base = the `(coefficient Q, differential-operator)` term abstraction; concrete terms `MassIntegrator :69` `(Qu,v)`, `VectorFEMassIntegrator :80`, `DiffusionIntegrator :100` `(Q∇u,∇v)`, `CurlCurlIntegrator :110` `(Q curl u, curl v)`, `DivDivIntegrator :120` `(Q div u, div v)`, `DiffusionMassIntegrator :130`. The term form is cleanly describable as `weak_form_term = (coefficient, differential-operator)` with the operator drawn from a witnessed cohort {∇, identity, curl, div}.

Scope discipline (redirect): author the **`weak_form_term` term abstraction + the curl-curl term form** grounded by 2 witnesses (diffusion already-firm-in-`fe_assemble`, curl-curl now-pulled). Do NOT speculatively enumerate the FULL cohort (mass/div-div/boundary) beyond what the 2-witness pull licenses — those land as a pipeline pulls each in turn (mass is multi-witness across eigenmode/transient/postprocess per the codemap, but no in-scope solver-K pull NEEDS it THIS cycle; div-div has no Palace solver-K witness at all in the model builders — RT div-div appears only in `domainpostoperator`/integrator declarations). The curl-curl pull is the clean 2nd witness; the entry is written term-abstraction-first with diffusion+curl-curl as the two grounded variant-axis members and mass/div-div noted as cohort siblings pending their own pull.

## Deliverable-presence verification

Per the MANDATORY paste-inline-evidence pre-dispatch check. The lead is open-by-refinement (the cohort was an explicitly-deferred opaque rough-in input; no prior cycle authored a `weak_form_term` entry — verified absent below). The probe is observation-only / open-by-construction.

**D1 — `book/src/L1/weak_form_term.md` (+ concept page `book/src/concepts/weak-form-term.md`):**
1. File existence — `ls` results (pasted):
   - `book/src/L1/weak_form_term.md` → `No such file or directory` (ABSENT)
   - `book/src/L1/weak-form-term.md` → `No such file or directory` (ABSENT)
   - `book/src/L1/curl_curl_term.md` → ABSENT; `book/src/L1/mass_term.md` → ABSENT; `book/src/L1/weak_form_term_curlcurl.md` → ABSENT
   - `book/src/L2/weak_form_term.md` → `No such file or directory` (ABSENT)
   - `book/src/concepts/weak-form-term.md` → ABSENT; `book/src/concepts/fe-term.md` → ABSENT
2. Maturity check — N/A (no file on disk). The cohort exists ONLY as an opaque rough-in INPUT named inside `fe_assemble.md:155-167` and the `fe-operator-assemble-mutation-rotation` theme; there is no `weak_form_term` ENTRY anywhere. Not a no-op.
3. OQ-ledger RESOLVED-grep — `grep 'weak.form.term' scaffolding/open-questions.md`: the slug appears at `:49`, `:141`, `:781` as **MIGRATED to the plan (CYCLE-058 #2) / kept-deferred-pending-pull**, NOT as RESOLVED/CLOSED. The OQs `fe-assemble-weak-form-term-cohort-enumeration` + `fe-assemble-weak-form-term-cohort-width-after-firm-flip` are unified-and-open, owned by the plan. No closure — the line is live, not stale.
4. Structural-block check — the clean-gate (redirect) gated this cohort on a pipeline pull-up. The block is NOW LIFTED by the magnetostatic curl-curl pull (pasted codemap evidence above). The STOP-PROPOSING negative list (`priorities.md:92`, `:38`) does NOT contain `weak_form_term` / `curl_curl` / `curlcurl` (grep confirmed — the only hits are the active-head item text itself). Not blocked.
→ ALL FOUR PASS + not on negative list + framing correct (harvester term-abstraction-first per the redirect, NOT a rectangular floor). RECRUIT.

**D2 — `book/src/L1-L0/weak-form-term-rotation.md` (the curl-curl + diffusion term-form L1>L0 lowering theme):**
1. File existence — `ls book/src/L1-L0/weak-form-term-rotation.md` → ABSENT (verified — directory grep for `weak` in `book/src/L1-L0/` returns only `fe-operator-assemble-mutation-rotation.md`, which is the FOLD theme, not the per-term theme). Canonical slug stated for the D1 forward-reference: `book/src/L1-L0/weak-form-term-rotation.md`.
2. Maturity — N/A (absent).
3. OQ-grep — no `weak-form-term-rotation` RESOLVED/CLOSED entry (the cohort OQs are open-migrated, see D1#3).
4. Structural-block — none; the term forms lower to the concrete `*Integrator` `Assemble` declarations (`integrator.hpp:69-120`) + the coefficient-construction sites (`curlcurloperator.cpp:178-181` `muinv_func`; `laplaceoperator.cpp:191-194` `epsilon_func`). Open-by-construction.
→ RECRUIT (sequenced after D1 for the live forward-reference).

**D3 — driven/transient remaining-outer-machinery probe (observation-only):**
- Open-by-construction (observation-first probe, no named-slug deliverable, authors nothing to `book/` — it either yields a finding or records the outer machinery spine-complete). Skip the four-step check per the explicit carve-out ("open by construction (observation-only probe, no prior-cycle authoring)"). Cites the `disciplined-cross-pipeline-combinator-mining-gate` skill if a fold/map shape surfaces.

## Dispatches

**D1 — (`harvester`, `weak-form-term-fe-cohort` LEAD)**
- **scope:** Author the firm L1 entry `book/src/L1/weak_form_term.md` — the `(coefficient, differential-operator)` weak-form term abstraction the firm `fe_assemble` folds over opaquely. Write it **term-abstraction-first** (the `weak_form_term = (coefficient: MaterialPropertyCoefficient, differential_operator: ∇|identity|curl|div)` pair; the abstraction over `palace/fem/integrator.hpp:39-65` `BilinearFormIntegrator`), with **differential-operator as the load-bearing VARIANT AXIS** grounded by the TWO pulled witnesses: `diffusion` `(Q∇u,∇v)` (`DiffusionIntegrator`, `integrator.hpp:100-107`; electrostatic K `laplaceoperator.cpp:188-194`) and `curl-curl` `(Q curl u, curl v)` (`CurlCurlIntegrator`, `integrator.hpp:109-117`; magnetostatic K `curlcurloperator.cpp:170-181`). Name `mass` `(Qu,v)` (`MassIntegrator :68-76` / `VectorFEMassIntegrator :78-90`) and `div-div` `(Q div u, div v)` (`DivDivIntegrator :119-127`) as the remaining cohort members pending their own pipeline pull (do NOT fully author them — the 2-witness pull licenses diffusion+curl-curl; the others are noted variant-axis siblings). Also note `DiffusionMassIntegrator :130` as the witnessed composite (a term-sum, lowers via the existing `fe_assemble` sum-of-terms law, NOT a new primitive). Forward-reference the per-term lowering theme `book/src/L1-L0/weak-form-term-rotation.md` (canonical slug — D2 authors it this cycle). Co-emit the concept page `book/src/concepts/weak-form-term.md` ONLY IF the harvester judges the term-family taxonomy warrants a dedicated concept home (≥2 prospective consumers: `fe_assemble` + the rotation theme); else defer to a layer-intro-author follow-up (record the judgment). REPLACE-AND-PROPAGATE: update `fe_assemble.md`'s §weak_form_term opaque-input note (`:155-167`) to a LIVE link to the new entry (this is a forward-reference upgrade the integrator can wire, NOT a re-authoring of `fe_assemble`'s body). NOT a rectangular floor — this is the genuinely-new FE vocabulary the redirect names.
- **deps:** none (D1 is the lead; it authors the entry, D2 references it back).
- **rationale:** active-head #1, the pull-fires lead. The genuinely-new FE differential-operator vocabulary, recruited because the magnetostatic curl-curl K-assembly concretely pulls a non-diffusion term (pull-check above). fan-out MEDIUM (serves every assembled-operator pipeline's K/M-matrix build). Redirect item-1 (vocabulary-shift, not rectangular projection): the term abstraction is a real new vocabulary the layer needs to describe FE assembly concisely.

**D2 — (`abstractor`, `weak-form-term-fe-cohort` LEAD, lowering half)**
- **scope:** Author the firm L1>L0 lowering theme `book/src/L1-L0/weak-form-term-rotation.md` (canonical slug — D1 forward-references it) — how the L1 `weak_form_term` abstraction lowers to the L0 concrete `*Integrator` construction. Narrate forward L1→L0: a `weak_form_term (coef, diff_op)` lowers to the matching `mfem`/Palace `BilinearFormIntegrator` subclass instantiation — `diffusion`→`DiffusionIntegrator(Q)` (`integrator.hpp:100-107`, the `(Q∇u,∇v)` quadrature contraction), `curl-curl`→`CurlCurlIntegrator(Q)` (`:109-117`, `(Q curl u, curl v)`) — with the coefficient `Q` lowering to the `MaterialPropertyCoefficient` construction sites (`curlcurloperator.cpp:178-180` `muinv_func` from `GetCurlCurlInvPermeability`; `laplaceoperator.cpp:190-191` `epsilon_func` from `GetPermittivityReal`). The per-term element-local→global assembly map `A` (the `Assemble` method body) is the opaque `obstruction (opaque-library-ownership)` libCEED leaf already classified by the FE-assembly thread — cite that boundary (`bilinearform.cpp:67-70`), do NOT re-open it. This is a genuine vocabulary translation (term abstraction → concrete templated integrator subclass), NOT an identity-in-named-terms rename — the L0 form is the C++ template instantiation, the L1 form is the `(coef, diff_op)` data pair.
- **deps:** D1 (forward-references `book/src/L1/weak_form_term.md` as its LHS; sequenced wave-2 for the live inbound link, though per the serial-per-report-then-finalize model a same-cycle live link resolves either ordering — see overlap analysis).
- **rationale:** active-head #1 lowering half. Completes the L1>L0 edge for the new term vocabulary. fan-out MEDIUM (the assembly-half lowering for every pipeline).

**D3 — (`cross-layer-cross-cutter`, `driven-full-sweep-and-transient-time-stepping-remaining-solver-vocab` probe)**
- **scope:** ONE cheap observation-first probe of the remaining solver-pipeline OUTER machinery beyond the now-firm `Sweep`/`SweepAdaptive`/transient-fold shapes: (a) the driven FULL sweep driver machinery surrounding the `map_solve`/`solve_family`-classified outer loop (`drivensolver.cpp` post-solve per-ω postprocessing / output cadence / port-S-parameter extraction); (b) the transient time-stepping DRIVER machinery surrounding the opaque `fold_solve` per-step body (adaptive `dt` control, output cadence, error estimation in `transientsolver.cpp`/`timeoperator.cpp`). Question: is there any NEW cleanly-describable spine vocabulary in this outer machinery, or is it already covered by the firm spine (like the eigenmode pipeline, recorded SPINE-COMPLETE c059)? Do NOT author regardless — a finding either licenses a future landing or records the pipeline outer-machinery spine-complete (the redirect's spine-finding outcome). Cite the `disciplined-cross-pipeline-combinator-mining-gate` skill if a fold/map shape surfaces (it must NOT over-unify — the gate's ≥2-witness + fold-vs-map discipline applies).
- **deps:** none (observation-only, touches no `book/` file the other dispatches touch).
- **rationale:** active-head #2, observation-first. Continues the solver test-load; cheap; either yields new vocabulary (→ batch-19 landing) or records a spine-coverage finding. fan-out MEDIUM (gated).

## Overlap analysis

Pairwise:
- **D1 × D2:** D1 authors `book/src/L1/weak_form_term.md`; D2 authors `book/src/L1-L0/weak-form-term-rotation.md`. Distinct files, distinct directories. D2 forward-references D1's entry (cross-report forward-reference — the canonical slug `book/src/L1/weak_form_term.md` is stated in BOTH scopes per the `cross-report-forward-reference-slug-divergence` convention, so neither producer guesses). D1's replace-and-propagate touch on `fe_assemble.md`'s opaque-input note is a forward-reference-upgrade D2 does not touch. They co-touch `book/src/SUMMARY.md` (one new chapter line each) and potentially the `book/src/L1/index.md` + `book/src/L1-L0/index.md` dep-map tables — these are **distinct-row appends** (D1 appends an L1 row, D2 an L1-L0 row), parallel-safe per the distinct-dep-map-rows rule. NEITHER is a consolidated running-count tally owner — the L1/L1-L0 index counts were audited CONFIRM-CLEAN 68/68 (c058 D4) and bump by the linked-chapter `## Status` line, not a shared blind tally; under the c057-meta promotion-time guard each producer owns its own row+bullet. To be safe and explicit per the DUAL-REGISTRATION partition: **D1 owns its L1/index dep-map row + §cohort bullet + any L1-firm-count tally touch (it is the new-firm-entry author); D2 owns its L1-L0/index dep-map row + §cohort bullet ONLY, and DEFERS any consolidated L1-L0-firm-count tally to D1 is N/A here** — they write DIFFERENT indices (D1→L1/index, D2→L1-L0/index), so there is no shared consolidated tally between them. Each is the sole count-owner of its OWN layer index this cycle. → **NON-OVERLAPPING at the operational level.** Mild same-cycle forward-reference is exactly the conflict-tolerance signal the project wants surfaced if it materializes; mark sequential ONLY for the live-link ordering (wave-2), not for a true region conflict.
- **D1 × D3:** D3 is observation-only, authors nothing to `book/`. No shared region, no shared operator name (D3 reads `drivensolver.cpp`/`transientsolver.cpp`/`timeoperator.cpp`; D1 reads `integrator.hpp`/`curlcurloperator.cpp`/`laplaceoperator.cpp`). → NON-OVERLAPPING. Parallel.
- **D2 × D3:** D3 authors nothing; disjoint source regions. → NON-OVERLAPPING. Parallel.

## Sequencing schedule

- **Wave 1 (parallel):** D1 (`weak_form_term` L1 entry), D3 (driven/transient probe). D1 and D3 are fully non-overlapping; D3 authors nothing.
- **Wave 2 (after D1's report lands):** D2 (`weak-form-term-rotation` L1>L0 theme) — sequenced after D1 purely so its forward-reference to `book/src/L1/weak_form_term.md` resolves as a live link at the per-report integration (the canonical slug is stated in both scopes regardless). Per the serial-per-report-then-finalize model this live link would resolve either ordering, but wave-ordering the lowering after the entry is the clean default for a same-cycle entry↔theme pair (matches the c058 D1→D2 / c059 D1→{D2,D3} precedent).

Reminder: there is exactly ONE `integrator-finalize` at cycle end; the waves are dispatch / forward-reference ORDERING only — the book is not rebuilt between waves and finalize does not run per-wave.

## Open questions / caveats

- **Cohort-width judgment (for the harvester + the batch-19 meta-phase):** D1 authors diffusion+curl-curl (the 2-witness pull) and names mass/div-div as pending-pull siblings. The codemap shows `mass`/`VectorFEMass` is heavily multi-witness (eigenmode `modeeigensolver.cpp`, transient mass term, postprocess) — a future cycle's eigenmode/transient FE-assembly pull will likely recruit the mass term; `div-div` has NO solver-K witness in the model operator builders (only `domainpostoperator` + the `DivDivIntegrator` declaration) so it may stay a named-but-unauthored cohort sibling indefinitely (a possible spine-coverage finding: "div-div is declared in the integrator cohort but no in-scope Palace solver assembles a div-div K-matrix"). Flag for the next planner: the next FE-term pull is most likely the eigenmode/transient MASS term.
- **Concept-page decision deferred to D1's judgment:** whether `book/src/concepts/weak-form-term.md` lands this cycle (co-emitted by the harvester) or as a layer-intro-author follow-up depends on the harvester's ≥2-consumer call. If D1 defers it, append a layer-intro-author candidate to the plan next cycle. Not load-bearing for the entry.
- **`fe_assemble` opaque-input note upgrade:** D1's replace-and-propagate touch upgrades the `fe_assemble.md:155-167` plain-text `WeakFormTerm` mention to a live link. This is a forward-reference-upgrade (`upgrade-plain-text-ref-to-live-link-when-target-on-disk` skill territory), NOT a re-authoring of the firm `fe_assemble` body — flagged so the critic does not read it as an unwarranted firm-entry edit.
- **In-layer-conciseness combinator family (active-head #3) NOT dispatched this cycle:** it is gated on a ≥2-witness recurrent pattern from NEW material; the `weak_form_term` cohort is a single new vocabulary entry, not yet a combinator-mining surface. If D1/D3 surface a recurrent term-assembly or outer-driver pattern across ≥2 witnesses, that becomes a combinator-miner candidate for cycle-062 — noted for the next planner. (The firm BLAS/projector/smoother surface stays combinator-complete per c052 D5 — not re-scanned.)
- This is the FIRST cycle of batch-19; the friction-ledger / OQ-ledger I read are at most ~0 cycles stale (the batch-18 meta-phase just ran, commit c8e32e2). No methodology-adjustment pattern surfaced that the ledger lacks. No session restart was needed (no agent-def edits this batch).
