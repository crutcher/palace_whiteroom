---
agent: cycle-planner
invoked_at: 2026-05-28T234500Z
scope: cycle-017 dispatch plan
status: pending
---

# Cycle 017 dispatch plan

**Context**: Second primary cycle of meta-batch-4 (cycles 016/017/018; meta-phase fires after cycle-018). Cycle counter does not reset. Cycle-016 was a consolidation cycle (1 firm L1>L0 theme + 1 L0 chapter + 7 citation/prose hygiene reports); cycle-017 is a forward-frontier + carry-forward balance cycle.

## Goals selected this cycle

This cycle prioritizes **two explicit vectors**: (1) **The BLAS-1 variadic linear-combination fold unification** (human-raised priority #6 in cycle-017 Now section), which consolidates the fixed-arity `scal`/`axpy`/`axpby`/`axpbypcz` family into a single parametric combinator at L2, unifying three representation levels (L1 leaves, L1>L0 themes, L3 identity cohort) into one semantic root. Dispatch 1 (`combinator-miner`) leads the wave. (2) **Firm-L1-entry hygiene + carry-forward small dispatches** from cycle-016 integrator-signals Unblocked + Suggested-next-dispatches: the divfree L1-entry off-by-one harvester/repairer follow-up, the two cg.md citation sweeps (L3>L2 theme + the other two krylov-step sweeps), and the chebyshev sibling-sweep vocabulary refresh. These four dispatches consolidate residual entry-level hygiene and complete the chebyshev `forM_`/`foldM`→`iterate_while_pure` prose vocabulary gap identified in cycle-015. Balance: keep the wave focused at 6 dispatches (all actionable from cycle-016 integrator-signals), weight lower-layer/unification work (the combinator is L2 foundational; the lower-tier sweeps are hygiene/vocabulary on firm entries) over higher-layer expansion.

## Dispatches

| # | Agent | Scope | Deps | Rationale |
|---|---|---|---|---|
| 1 | **combinator-miner** | L2 combinator: variadic `linear_combination` fold (`[(Scalar, Tensor[N])] -> Tensor[N]`) with four fixed-arity specializations (`scal` = nullary fold / `axpy` = binary fold / `axpby` = ternary / `axpbypcz` = quaternary); in-place forms as output-aliasing; distinguish sibling `dot` inner-product fold (reduce-to-scalar, different functor). Parametric/variadic-family detection to inform spec upgrade post-dispatch (currently arity-blind heuristic). | **none** | **HEADLINE dispatch per human priority #6 (cycle-017 Now)** — the `scal`/`axpy`/`axpby`/`axpbypcz` family are fixed-arity specializations of a single variadic fold, currently represented 3× across layers (L1 leaves, L1>L0 themes, L3 identity cohort) with zero unification. Combinator-miner is the exact role for family discovery. Frame dispatch prompt **explicitly parametric**: cite `palace/linalg/vector.hpp:307-316` (free-function template signatures) + the L1 entries (`book/src/L1/scal.md`, `/axpy.md`, `/axpby.md`, `/axpbypcz.md`) as fixed-arity signatures of the underlying fold. Proposal must include the `dot` sibling-note (inner-product fold = reduce-to-scalar, structurally orthogonal, do NOT over-unify). OQ `blas1-variadic-linear-combination-fold-unification` + cycle-015 carry-forward + cycle-016 integrator-signals suggested-next + human directive. |
| 2 | **harvester**/**repairer** | Firm L1-entry hygiene: `book/src/L1/divfree-projector.md` off-by-one + anchor drifts (apply close-brace `:155-186`→`:187`; CG rel-tol `:140`→`:141`; inherited dangling pointer `:43`; cycle-015-enactment carry-forward hpp anchor hygiene `:68-72`→`:67-71`, `:28-31`→`:27-30`). Fold into one surgical pass (firm L1 operator hygiene = harvester authority). | **none** | **Small unblocked from cycle-016 integrator-signals Unblocked section** — the L1>L0 abstractor (cycle-016 dispatch 1) found four co-located entry-level drifts on a firm operator file. OQ `divfree-l1-entry-apply-close-and-reltol-line-drift` (cycle-016 abstractor). Harvester scope: rewrite the four line anchors, verify all citations still resolve, re-read codemap sources at dispatch time per role-spec discipline. |
| 3 | **lifter** | Firm L3>L2 theme hygiene: `book/src/L3-L2/krylov-step-body-identity.md` 1-pointer cg.md citation sweep (line 125, dangling `cg.md:341-362`). Co-note the out-of-scope gmres.md co-pointer; note for future gmres v0.6→v0.7 self-rotation. | **1** | **Small unblocked from cycle-016 integrator-signals Unblocked section** — two-hop-to-dangle residual from the L4 cg.md sweep (dispatch 2 finds the L4 entry needs it; dispatch 3 finds the L3>L2 theme also carries it). OQ `l3-l2-body-identity-cg-md-citation-sweep` (cycle-016 lifter). Lifter scope: re-read disk at edit time per role-spec; verify `cg.md:341-362` is terminal firm home (it is, from cycle-016 integrator-signals resolution-implications notes). |
| 4 | **lifter** | Firm L4/L2 krylov-step entries cg.md citation sweeps (L4 entry 8 dangling pointers + L2 entry 12 dangling pointers; each re-anchored to terminal firm homes in L3>L2 theme / L1>L0 themes / L0 iterative.cpp + retained-live `cg.md` stub chunks `:27-141`/`:86-106`). Sibling to dispatch 2 (L3>L2) + dispatch 3 (L2). | **1** | **Small unblocked from cycle-016 integrator-signals Unblocked section** — the cycle-016 L4 cg.md sweep found 8 re-anchors; the L2 entry carries 12 separate sweeps + 1 CheckDot drift. Two krylov-step entries (L4/L2), two sweeps in one dispatch for coherence. OQ `l4-`/`l2-krylov-step-cg-md-citation-sweep` (cycle-016 lifter). Lifter scope: per role-spec §Discipline, verify relocated pointers land at TERMINAL firm homes (cycle-016 integrator-signals explicitly notes this hazard after L3 sweep found two mislanded targets). |
| 5 | **lifter** | Firm L3 chebyshev entry vocabulary refresh: 5 sibling `forM_`/`foldM` mentions (`book/src/L3/chebyshev.md` lines 46/55/96/475/480 post-earlier-edit; re-grep at dispatch time for drift). Pure vocabulary harmonization: replace stale `forM_`/`foldM` narrative prose with canonical `iterate_while_pure` term + cross-reference to L4 terminal home. | **1** | **Small unblocked from cycle-016 integrator-signals Unblocked section** — closes the chebyshev `forM_`/`foldM`→`iterate_while_pure` vocabulary-lag cohort (L4 body cycle-015 + L4 prose cycle-016 + L3 named-sentence cycle-016 + L3 siblings next = this dispatch). OQ `l3-chebyshev-sibling-formm-foldm-prose-sweep` (cycle-016 integrator-per-report). Lifter scope: read the prose carefully; distinguish conceptual `forM_` narrative mentions (historical, may stay) from technical prose (should refresh). Similar discipline to cycle-016 L4 chebyshev prose refresh (kept 4 historical, refreshed 3 descriptive). |
| 6 | **cross-layer-cross-cutter** | Construct-operator pattern recurrence audit: whether the `P.ksp : Solver[P.M]` closure-nesting pattern (constructed-operator gate carrying another constructed-operator gate as a sub-field) recurs across the L1 operator set beyond the firm `divfree-projector` (it does not in the current set). Assess whether it warrants a named concept sibling to `constructed-operator-factory`. Informational scope; filing result under the OQ clarifies the scope of constructed-operator gate abstraction. | **2** (divfree L1 entry digest from dispatch 2) | **Informational unblocked from cycle-016 integrator-signals Unblocked section** — the cycle-016 abstractor (divfree L1>L0 dispatch) introduced the `P.ksp` nesting pattern as a NEW structural dependency; cross-cutter determines whether this is a one-off pattern or the leading edge of a recurring motif. OQ `divfree-closure-nesting-constructed-gate-carrying-constructed-gate` (cycle-016 abstractor). Cross-cutter scope: read the firm L1 operator entries (11 firm); check for analogous closure sub-field nesting; report finding as pure metadata (no edits proposed) for potential cycle-018 concept or pattern-library expansion. |

## Overlap analysis

**Two dispatch regions:**

- **Dispatches 1 (combinator-miner)**: independent. No artifact touch (L2 combinator discovery at proposal stage, not emission stage per combinator-miner role-spec "produces proposed-changes block, does NOT write book/ directly").
- **Dispatches 2–6 (hygiene + audit cohort)**: share firm-artifact entry names but distinct scopes:
  - **Dispatch 2** (harvester): `L1/divfree-projector.md` four-anchor surgical edits. **Non-overlapping with others** (no other dispatch touches this file).
  - **Dispatch 3** (lifter): `L3-L2/krylov-step-body-identity.md` line-125 single-pointer edit. **Non-overlapping with 2/4/5/6** (distinct file).
  - **Dispatch 4** (lifter): `L4/krylov-step.md` + `L2/krylov-step.md` two files, distinct edits within each. **Non-overlapping with 2/3/5/6** (distinct files; no same-file pairs).
  - **Dispatch 5** (lifter): `L3/chebyshev.md` 5-site vocabulary refresh. **Non-overlapping with 2/3/4/6** (distinct file; sibling-file to the L4/chebyshev refresh cycle-016 completed).
  - **Dispatch 6** (cross-cutter): read-only audit; no artifact mutation. **Non-overlapping with all** (produces OQ metadata only, per cross-layer-cross-cutter role-spec).

**Dep-map structure**: Dispatch 2's L1-entry digest informs dispatch 6's pattern-audit scope (soft dependency, not blocking; both complete in parallel, integrate serially via dispatch ordering). No other dispatch has hard dependencies; all can execute in parallel once dispatch 1 completes.

## Sequencing schedule

**Wave 1 (parallel):**
- Dispatch 1 (combinator-miner) — `linear_combination` L2 combinator discovery.

**Wave 2 (parallel, after wave-1 report lands):**
- Dispatches 2–6 (harvester/repairer/lifter/cross-cutter) — all non-overlapping at the artifact level; seven dispatch-independent reads all complete in parallel; three distinct per-report artifact writes serialize naturally under integrator-per-report serial dispatch.

**Rationale for two waves:** Dispatch 1's proposal-stage discovery informs the planner's understanding of L2 vocabulary (output of the combinator-miner); keeping it in a separate wave ensures the report lands before integrator-per-report processes waves 2–6. Practically: the combinator-miner report is advisory (no book/ writes), so wave separation has zero impact on integration order — could be run parallel with wave 2 — but the planner's discipline is to sequence proposal-stage work (agents that output proposed-changes, not artifact edits) in a separate wave for clarity and to avoid confusing the integrator with back-to-back discovery→application in one wave. Per conflict-tolerance philosophy: parallel-when-in-doubt; this is a case where sequential is structurally justified (not false sequentialization), so two waves.

## Open questions / caveats

- **Combinator-miner parametric/variadic detection mode**: cycle-017 combinator-miner dispatch discovers the fold family and proposes the L2 combinator. The underlying arity-blind heuristic ("instance count > threshold") does NOT surface parametric families; the cycle-017 dispatch manually frames the search as "parametric family". **Batch-4 meta-phase (after cycle-018) should consider a combinator-miner spec upgrade** to add a "variadic/parametric-family detection mode" (scan arity as a dimension, cluster fixed-arity instances by root functor, propose parameterized combinator). This is a meta-level methodology note, not blocking the dispatch.

- **`cg.md` live-slice citations**: Dispatches 3 and 4 retain 2 live `cg.md:27-141` + `:86-106` slice citations (per cycle-016 integrator-signals notes on intentional retention pending future `cg.md` full-removal audit). This is **not a concern for cycle-017** — the citations are valid anchors (the slice exists and is on disk). When `phase-1-slice-reduction-audit` eventually targets `cg.md` (after the `cg_preconditioning_framework` L4 lift unblocks it), that dispatch will audit these live citations and decide removal or reformulation. Marked in dispatch 4 CYCLE.md for integration cross-reference.

- **`forM_`/`foldM` prose in dispatch 5**: The L3 chebyshev vocabulary refresh (dispatch 5) occurs **after** the L4 chebyshev prose cleanup (cycle-016 completed) but **before** any potential upper-layer text edits. If a concurrent L4>L3 theme authoring (e.g., a future chebyshev lowering theme) emits parallel narrative text touching the same L3 file, integrator serial dispatch of dispatch 5 first ensures the L3 entry is coherent before any theme edit reads it. No structural conflict; noted for integration visibility.

---

**Dispatch plan: 6 sub-agents, 1 combinator-miner lead (wave 1), 5 specialist hygiene/audit (wave 2, parallel). Estimated completion: ~3 hours wall-clock under typical per-report latency. All proposed-changes blocks land in cycle-017 STAGING.md; integrator-finalize commits after wave 2 completes.**
