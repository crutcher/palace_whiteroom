---
agent: layer-intro-author
invoked_at: 2026-06-04T210500Z
scope: cycle-095 D5 (Wave 3) — P1 feature-root closure typing — author the typed `edges:` + `feature_root: seed` + `rank:` SPLIT frontmatter on the NON-cascade feature-spine columns (driven / eigenmode / transient / eigenfrequency-qfactor / sparameters / energy-fields / boundary-mode × {L4,L1,L0}) + the lifecycle spine-ROOT × {L4,L1,L0} + the spine-root group intro, per scheme §4(c) composes:→edges: migration; plus the 5 D4-handed stale `(seed)` down-link re-anchors
status: integrated
integrated_at: 2026-06-04T231500Z
integration_commit: PLACEHOLDER_SHA
integration_notes: "cycle-095 D5 (staging position 5/7). The P1 feature-root closure typing: 25 files — the 7 NON-cascade feature columns + the spine-ROOT got the scheme §3 feature_root: seed + rank: SPLIT + typed edges: migration; lifecycle ROOT firm under OWN-COMPOSITION; boundary-mode honestly typed rank: rough-in; THE GC ROOT SET IS NOW FULLY TYPED. Rank-gate PASS (7 firm columns rest on firm vocab; boundary-mode rough-in <= firm deps). Applied clean (+5 discretionary within-partition sibling-status re-anchors); retroactive-budget 0. No NEW OQs (shared cites-evidence OQ already promoted by D4)."
inputs:
  - reports/2026-06-04T204023Z-cycle-planner-cycle-095/CYCLE.md (D5 scope; the partition; the stale-edge false-positive thesis)
  - reports/2026-06-04T205500Z-layer-intro-author-cycle-095-four-column-reeval/CYCLE.md (D4 — the 4 cascade columns flipped firm; the 5 stale-(seed) mentions D4 flagged for D5; D4 sole-owns feature/index.md + output-product.md + driver-leaf.md this cycle)
  - book/src/methodology/graded-stack-scheme.md (§1 rank ladder, §2 typed edges, §3 feature_root split, §4(c) composes:/l0_ground_truth:→edges: migration, §5 un-fronted/orientation pages)
  - the 21 non-cascade column files + lifecycle ×3 + spine-root.md (all read end-to-end this dispatch)
---

# CYCLE: P1 feature-root closure typing — the non-cascade feature columns + the spine-ROOT

## Summary

This is **D5, Wave 3** of cycle-095 (the GRADED-STACK campaign's P1 launch). It types the **feature-root closure** — the GC root set the reachability linter marks from — on the feature-spine columns that **D4 did NOT type** (D4 owns the 4 bilinear-form-cascade columns capacitance/inductance/electrostatic/magnetostatic + `feature/index.md`). D5 authors the scheme §3 SPLIT frontmatter (`feature_root: seed` + `rank:`) + the typed `edges:` block (converted from the legacy `composes:`/`l0_ground_truth:`/`lifts_to:`/`specializes_to:` frontmatter per scheme §4(c)) on:

- the **non-cascade driver columns** — `driven`, `eigenmode`, `transient`, `boundary-mode` × {L4, L1, L0};
- the **non-cascade output-product columns** — `eigenfrequency-qfactor`, `sparameters`, `energy-fields` × {L4, L1, L0};
- the **lifecycle spine-ROOT** × {L4, L1, L0};
- the **`spine-root.md`** group intro (its stale closing-line status narration; it is an orientation page, NOT a DAG node — no frontmatter, scheme §5).

**The migration (scheme §4(c)), applied uniformly:**
- a `composes:` target that is a **vocabulary op** (`L4/fe_assemble`, `L4/eigsolve`, `L4/fold_solve`, `L1/port_projection`, `L1/participation_ratio`, `L1/matrix-weighted-norm`, the reduce verbs …) → `edges: depends-on:` (blocking; constrains rank AND carries liveness);
- a `composes:` target that is a **sibling feature column** (a driver column ↔ its output-product column; the lifecycle ROOT ↔ its per-driver columns) → `edges: reference:` (the OWN-COMPOSITION rule, scheme §3: an edge to a feature root is `reference`, never `depends-on` — sibling roots never gate each other's rank);
- `l0_ground_truth:` (a `palace/...:lo-hi` source range) → `edges: depends-on:` with `kind: cites-evidence`;
- `lifts_to:` (the L0-level → L1-level sibling navigational pointer) → `edges: reference:` (a level-sibling pointer carries no liveness/rank constraint — the authoritative typed lowering edge is the op-level L1↔L0 theme, not the feature-surface level pointer; this is exactly D4's L0 convention);
- **the free-text maturity qualifiers in every `composes:` string are DROPPED from the edge** (the dep's rank is read from the dep's OWN frontmatter, never restated on the edge — scheme §4(c), the index-cell-drift lesson). This is precisely what clears the stale-edge false positives: the `eigenmode.L1`/`boundary-mode.L1 → L1/eigsolve` "violations" (the linter read `(rough-in (test-coverage-bounded))` off the stale prose, but `L1/eigsolve` is `firm` on disk — see §"Stale-edge audit findings" below) vanish once the edge restates no maturity.

**Typing ≠ promotion.** This is a pure edge-typing + rank-token-write pass — I write each column's `rank:` to match its OWN on-disk `## Status`, surveyed from the file (NOT the index cell, NOT the cycle record):

| column | on-disk `## Status` | `rank:` written | note |
|---|---|---|---|
| `driven` (L4/L1/L0) | `firm` | `firm` | all own vocab ops firm |
| `eigenmode` (L4/L1/L0) | `firm` (promoted c085) | `firm` | own `fe_assemble`+`eigsolve` firm |
| `transient` (L4/L1/L0) | `firm` | `firm` | own `fe_assemble`+`fold_solve` firm |
| `eigenfrequency-qfactor` (L4/L1/L0) | `firm` | `firm` | own verb `eigenfreq_qfactor_reduce` firm c082 |
| `sparameters` (L4/L1/L0) | `firm` | `firm` | own verb `sparameter_reduce` firm c083 |
| `energy-fields` (L4/L1/L0) | `firm` | `firm` | own verb `domain_energy_reduce` firm c091 |
| `boundary-mode` (L4/L1/L0) | `seed` (own-readout gate) | `rough-in` | **stays at its honest composition-maturity — its directly-owned stage-(3) readout reduces into a waveguide-mode output product with NO firm home; its solve corner `fe_assemble`+`eigsolve` is firm but the readout is unhomed → `rough-in`, NOT `firm`. `feature_root: seed` (permanent root) is orthogonal.** |
| `lifecycle` (L4/L1/L0) | `firm` | `firm` | own mesh-build + `fold_solve` firm; per-driver dispatch is over sibling columns (references) |

**Rank-invariant check** (`rank(column) ≤ min over depends-on deps`): every `firm` column's `depends-on` targets are firm vocabulary ops (rank 3) + `cites-evidence` ranges (not §1-ladder-constrained — they are ground truth). `boundary-mode`'s `depends-on` deps (`fe_assemble`/`eigsolve`, both firm) satisfy `rank(rough-in=2) ≤ 3`. `feature_root: seed` is a parallel axis, not a rank — it does not enter the invariant. The `reference:` sibling-column edges constrain nothing. The invariant holds for every column at every level.

**Ownership partition (per the plan + D4's report):**
- **D5 (this dispatch) owns:** the **non-cascade columns' frontmatter** (21 column files: driven/eigenmode/transient/boundary-mode/eigenfrequency-qfactor/sparameters/energy-fields × {L4,L1,L0}) + the **lifecycle ×3** frontmatter + the **`spine-root.md`** group-intro closing line + the **D4-handed 5 stale-`(seed)` down-link re-anchors** that fall in D5-owned files + the within-D5-partition stale sibling-status re-anchors (driven↔sparameters, eigenmode↔eigenfrequency-qfactor, lifecycle down-link cells).
- **D5 does NOT touch:** `feature/index.md` (D4 sole-owner), `feature/output-product.md` + `feature/driver-leaf.md` (D4 edits their closing lines this cycle), the 4 cascade columns (D4), any vocabulary index (`L1/index.md` D1; `L4/index.md` contested — D2/integrator), any vocabulary leaf chapter (D6).

## HARD-gate-new compliance

This is a typing pass over already-firm (or honestly-rough-in) nodes — it does NOT promote any node. Each column nevertheless lands a `rank:` token matching its on-disk `## Status` + a fully-typed `edges:` block satisfying the rank invariant (checked above). `feature_root: seed` is carried on every column (permanent categorical root marker, scheme §3) separately from `rank:`.

## Stale-edge audit findings (P1 thesis confirmation — flag for D6/D7/batch-30 meta-phase)

The planner's headline thesis — *the prose/`composes:`-qualifier dep-map drifts behind operator frontmatter, producing stale-edge false-positive rank violations that re-typing clears* — is **confirmed at the feature-column surface** this dispatch. The genuinely-stale `composes:` maturity qualifiers I dropped on conversion:

- **`eigenmode.L1` / `boundary-mode.L1` `composes: L1/eigsolve (firm — …)`** — the planner's residual-violation list cited `eigenmode.L1`/`boundary-mode.L1 → L1/eigsolve (rough-in-tcb)`. **Verified on disk this dispatch: `L1/eigsolve.md:165` §Status = `firm`** (cycle-022 route-(b) law-confidence re-eval, promoted from `rough-in (test-coverage-bounded)`). These were **stale-edge FALSE POSITIVES** — the linter read a stale `rough-in-tcb` qualifier from somewhere (NOT from these columns' frontmatter, which already say `firm`; the source is the prose dep-map / an L2/L3-side `composes:` string D6 owns). My re-typed `depends-on: L1/eigsolve` edge restates NO maturity → the linter reads `L1/eigsolve`'s own `firm` frontmatter → violation clears. **D6 owns the `L2/eigsolve → L1/eigsolve` vocabulary-side edge; D5 confirms the L1/eigsolve frontmatter is firm so D6's re-type clears that one too.** This resolves the planner's "`L1/eigsolve` rank — verify at typing" OQ: **`L1/eigsolve` is firm; all three eigsolve-chain violations are stale false positives.**
- **`energy-fields.L4:7` / `energy-fields.L1:7` `composes: electrostatic (seed — …)`** — stale: `electrostatic` firmed by D4 c095. Dropped on conversion (the composes-string is replaced by the typed `edges:` block; `electrostatic` becomes a `reference:` with no maturity restated).
- **`lifecycle.L4:7,8` `composes: electrostatic/magnetostatic (seed — …)`** — same: both firmed by D4 c095; dropped on conversion.

**Confirmation for the batch-30 meta-phase:** every feature-column maturity drift this dispatch found was in a *free-text qualifier string* (a `composes:` parenthetical or a prose `(**seed**)` / "(its own `status: seed`)"), NEVER in the column's own `status:`/`rank:` frontmatter. The `edges:`-frontmatter-as-sole-truth migration is correct — the qualifier strings ARE the drifting second source of truth the campaign removes.

## Proposed changes

### Group A — non-cascade DRIVER columns

#### driven.L4.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/driven.L4.md
---
kind: feature-surface
feature: driven
level: L4
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L4/fe_assemble
      kind: composes
    - target: L4/assemble_frequency_operator
      kind: composes
    - target: L4/frequency_sweep
      kind: composes
    - target: L4/ksp_solve
      kind: composes
    - target: palace/drivers/drivensolver.cpp:37-75
      kind: cites-evidence
    - target: palace/drivers/drivensolver.cpp:77-229
      kind: cites-evidence
  reference:
    - feature/sparameters.L4
---
```

#### driven.L4.md — §"Constituent down-links" sparameters cell (`:160`)

The sparameters verb is firm (c083) and the column is firm (c095 D5 typing); the `*rough-in*` / `seed (column)` labels are stale.

```edit:book/src/feature/driven.L4.md
| S-parameter reduction (output product) | [`sparameters`](./sparameters.L4.md) output-product column (verb [`sparameter_reduce`](../L4/sparameter_reduce.md), firm c083; sibling reference) | firm | `drivensolver.cpp:205-216` |
```

#### driven.L1.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/driven.L1.md
---
kind: feature-surface
feature: driven
level: L1
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L1/fe_assemble
      kind: composes
    - target: L1/assemble_frequency_operator
      kind: composes
    - target: L1/ksp_solve
      kind: composes
    - target: palace/drivers/drivensolver.cpp:77-229
      kind: cites-evidence
  reference:
    - feature/sparameters.L1
---
```

#### driven.L1.md — §Status sibling-status prose re-anchor (`:144`)

The "fixed-operator columns whose stage-3 inductance/capacitance bilinear-form primitives are rough-in" clause is stale: `bilinear-form` firmed (D1 c095) and the capacitance/inductance columns firmed (D4 c095).

```edit:book/src/feature/driven.L1.md
are firm; cross-linked sibling columns are references, NOT blockers). All three
directly-composed L1 operators are firm ([`fe_assemble`](../L1/fe_assemble.md),
[`assemble_frequency_operator`](../L1/assemble_frequency_operator.md),
[`ksp_solve`](../L1/ksp_solve.md)) — the driven L1 vocabulary is fully firm (as are
the fixed-operator columns' stage-3 inductance/capacitance bilinear-form primitives,
firm since c091/c095). The stage-3 S-parameter reduction is the driven
output-product surface, presented as its own [`sparameters`](./sparameters.L1.md)
column — a **sibling cross-link (a reference)**, NOT a directly-owned constituent, so
it does not gate promotion (the `sparameters` column promotes independently on its own
firm reduce verb). The chapter carries the compositional claim only; per-op algebraic
claims live in the linked chapters. Evidence: the L0 driver range
`drivensolver.cpp:77-229` (`SweepUniform`) realizing the composition, plus the firm L1
constituent down-links (line ranges self-verified on-disk).
```

#### driven.L0.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/driven.L0.md
---
kind: feature-surface
feature: driven
level: L0
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: palace/drivers/drivensolver.cpp:37-75
      kind: cites-evidence
    - target: palace/drivers/drivensolver.cpp:77-229
      kind: cites-evidence
    - target: palace/drivers/drivensolver.cpp:231
      kind: cites-evidence
    - target: palace/drivers/drivensolver.hpp:22-34
      kind: cites-evidence
  reference:
    - feature/driven.L1
---
```

#### eigenmode.L4.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/eigenmode.L4.md
---
kind: feature-surface
feature: eigenmode
level: L4
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L4/fe_assemble
      kind: composes
    - target: L4/eigsolve
      kind: composes
    - target: palace/drivers/eigensolver.cpp:32-477
      kind: cites-evidence
  reference:
    - feature/eigenfrequency-qfactor.L4
---
```

#### eigenmode.L4.md — §"Constituent down-links" eigenfrequency-qfactor cell (`:70`)

`eigenfrequency-qfactor` is firm; the `seed (column)` cell is stale.

```edit:book/src/feature/eigenmode.L4.md
| per-mode readout (ω, Q, B=-1/(iω)∇×E) | [`eigenfrequency-qfactor`](./eigenfrequency-qfactor.L4.md) output-product column (sibling reference) | firm | `eigensolver.cpp:424-458` |
```

#### eigenmode.L1.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/eigenmode.L1.md
---
kind: feature-surface
feature: eigenmode
level: L1
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L1/fe_assemble
      kind: composes
    - target: L1/eigsolve
      kind: composes
    - target: palace/drivers/eigensolver.cpp:32-477
      kind: cites-evidence
  reference:
    - feature/eigenfrequency-qfactor.L1
---
```

#### eigenmode.L1.md — §"Constituent down-links" eigenfrequency-qfactor cell (`:57`)

The `(output-product column; forward-ref — not authored here)` / `(forward-ref)` labels are stale: `eigenfrequency-qfactor.L1` is on disk and firm.

```edit:book/src/feature/eigenmode.L1.md
| per-mode readout (ω, Q, B=-1/(iω)∇×E) | [`eigenfrequency-qfactor.L1`](./eigenfrequency-qfactor.L1.md) output-product column (sibling reference) | firm | `eigensolver.cpp:424-458` |
```

#### eigenmode.L0.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/eigenmode.L0.md
---
kind: feature-surface
feature: eigenmode
level: L0
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: palace/drivers/eigensolver.cpp:32-477
      kind: cites-evidence
    - target: palace/drivers/eigensolver.hpp:20-28
      kind: cites-evidence
  reference:
    - feature/eigenmode.L1
---
```

#### transient.L4.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/transient.L4.md
---
kind: feature-surface
feature: transient
level: L4
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L4/fe_assemble
      kind: composes
    - target: L4/fold_solve
      kind: composes
    - target: palace/drivers/transientsolver.cpp:24-116
      kind: cites-evidence
    - target: palace/models/timeoperator.cpp:65-67
      kind: cites-evidence
    - target: palace/models/timeoperator.cpp:407-413
      kind: cites-evidence
---
```

#### transient.L1.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/transient.L1.md
---
kind: feature-surface
feature: transient
level: L1
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L1/fe_assemble
      kind: composes
    - target: L4/fold_solve
      kind: composes
    - target: palace/drivers/transientsolver.cpp:24-116
      kind: cites-evidence
    - target: palace/models/timeoperator.cpp:65-67
      kind: cites-evidence
    - target: palace/models/timeoperator.cpp:407-413
      kind: cites-evidence
---
```

#### transient.L0.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/transient.L0.md
---
kind: feature-surface
feature: transient
level: L0
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: palace/drivers/transientsolver.cpp:24-116
      kind: cites-evidence
    - target: palace/drivers/transientsolver.cpp:118-175
      kind: cites-evidence
    - target: palace/drivers/transientsolver.hpp:21-30
      kind: cites-evidence
    - target: palace/models/timeoperator.cpp:65-67
      kind: cites-evidence
    - target: palace/models/timeoperator.cpp:311-373
      kind: cites-evidence
    - target: palace/models/timeoperator.cpp:407-413
      kind: cites-evidence
  reference:
    - feature/transient.L1
---
```

#### boundary-mode.L4.md — frontmatter SPLIT + typed edges (stays at composition-maturity `rank: rough-in`)

```edit:book/src/feature/boundary-mode.L4.md
---
kind: feature-surface
feature: boundary-mode
level: L4
feature_root: seed
rank: rough-in
edges:
  depends-on:
    - target: L4/fe_assemble
      kind: composes
    - target: L4/eigsolve
      kind: composes
    - target: palace/drivers/boundarymodesolver.cpp:201-341
      kind: cites-evidence
    - target: palace/main.cpp:276-278
      kind: cites-evidence
  reference:
    - feature/eigenmode.L4
---
```

#### boundary-mode.L1.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/boundary-mode.L1.md
---
kind: feature-surface
feature: boundary-mode
level: L1
feature_root: seed
rank: rough-in
edges:
  depends-on:
    - target: L1/fe_assemble
      kind: composes
    - target: L1/eigsolve
      kind: composes
    - target: palace/drivers/boundarymodesolver.cpp:201-341
      kind: cites-evidence
  reference:
    - feature/eigenmode.L1
---
```

#### boundary-mode.L0.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/boundary-mode.L0.md
---
kind: feature-surface
feature: boundary-mode
level: L0
feature_root: seed
rank: rough-in
edges:
  depends-on:
    - target: palace/drivers/boundarymodesolver.cpp:201-341
      kind: cites-evidence
    - target: palace/drivers/boundarymodesolver.hpp:15-28
      kind: cites-evidence
    - target: palace/main.cpp:276-278
      kind: cites-evidence
  reference:
    - feature/boundary-mode.L1
---
```

### Group B — non-cascade OUTPUT-PRODUCT columns

#### eigenfrequency-qfactor.L4.md — frontmatter SPLIT + typed edges

`eigenmode` (the producing driver column) → `reference` (sibling root, OWN-COMPOSITION); the reduce verb → `depends-on`.

```edit:book/src/feature/eigenfrequency-qfactor.L4.md
---
kind: feature-surface
feature: eigenfrequency-qfactor
level: L4
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L4/eigenfreq_qfactor_reduce
      kind: folds
    - target: palace/drivers/eigensolver.cpp:424-439
      kind: cites-evidence
    - target: palace/models/postoperator.cpp:1171-1203
      kind: cites-evidence
  reference:
    - feature/eigenmode.L4
---
```

#### eigenfrequency-qfactor.L4.md — §Status reciprocal-guard prose re-anchor (`:68`)

The "(its own `status: seed`)" parenthetical on the eigenmode cross-link is stale: `eigenmode` is `status: firm` (promoted c085), and this cycle carries `feature_root: seed` (root marker) + `rank: firm` (composition-maturity) under the scheme §3 split. The point (sibling = reference, not blocker) is unchanged; only the stale maturity word is corrected.

```edit:book/src/feature/eigenfrequency-qfactor.L4.md
`firm` — an output-product **leaf feature column** authored under the FEATURE-SURFACE SPINE directive (2026-06-02), the rank-1 per-mode-table sibling of the rank-2 Gram output products [capacitance](./capacitance.L4.md) / [inductance](./inductance.L4.md). The composition is sound: stage (1) consumes the [`eigenmode.L4`](./eigenmode.L4.md) driver column's converged eigenpair family; stage (2) composes the [`eigenfreq_qfactor_reduce`](../L4/eigenfreq_qfactor_reduce.md) per-mode scalar-ratio reduction at the problem-type un-transform + resistive-lumped-port κ. **The column promotes off `seed` to `firm` under the OWN-COMPOSITION rule (USER DIRECTIVE 2026-06-03; codified batch-26 meta-phase; memory `project_feature_column_promotion_rule`):** a column promotes when its OWN composition + directly-owned constituents are firm. This column's sole directly-owned constituent — the [`eigenfreq_qfactor_reduce`](../L4/eigenfreq_qfactor_reduce.md) reduce verb — is **`firm`** (cycle-082 lowering-verifier law-confidence pass; firm-on-positive-structure escape — both folded per-mode primitives firm L1, the κ-participation-ratio half via [`participation_ratio`](../L1/participation_ratio.md) (cycle-077) and the eigenvalue-un-transform half via [`eigenvalue-untransform`](../L1/eigenvalue-untransform.md) (cycle-080), and the eigenpair→`(f, Q)` assembly is bare scalar arithmetic over two firm halves carrying no inner-product-axiom content). The cross-link to the [`eigenmode.L4`](./eigenmode.L4.md) driver column (which is itself `firm` — its `feature_root: seed` is the permanent root marker, not a maturity, under the scheme §3 split) is a **SIBLING reference, NOT a blocker** — it is the reciprocal drift-guard, not a constituent-firmness dependency. This retires the earlier mutual-blocking deadlock (the prior text held this column at `seed` because `eigenmode` was seed, while `eigenmode` was symmetrically held seed for reducing into this column — the exact reciprocal deadlock the directive breaks). This chapter carries the *compositional* claim (the `(f, Q)` table = the per-mode scalar-ratio reduction over the eigenmode driver's eigenpair family), not the constituents' per-op algebraic claims (those live in [`eigenfreq_qfactor_reduce`](../L4/eigenfreq_qfactor_reduce.md) and the [`eigenmode.L4`](./eigenmode.L4.md) driver column). The defining structural fact: a rank-1 per-mode scalar-ratio table, NOT a `gram_reduce` family-PAIR grid (c074 D6 closed-negative). Evidence: the L0 readout / Q-factor ranges `eigensolver.cpp:424-439` (the eigenvalue un-transform) + `postoperator.cpp:1171-1203` (`MeasureLumpedPortsEig`, the Q-factor) realizing the reduction, all anchors confirmed on-disk via palace-codemap `read_range` + citecheck `--anchor` this dispatch, plus the constituent down-links.
```

#### eigenfrequency-qfactor.L1.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/eigenfrequency-qfactor.L1.md
---
kind: feature-surface
feature: eigenfrequency-qfactor
level: L1
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L4/eigenfreq_qfactor_reduce
      kind: folds
    - target: L1/eigenvalue-untransform
      kind: folds
    - target: L1/participation_ratio
      kind: folds
    - target: palace/drivers/eigensolver.cpp:424-439
      kind: cites-evidence
    - target: palace/models/postoperator.cpp:1171-1203
      kind: cites-evidence
  reference:
    - feature/eigenmode.L1
---
```

#### eigenfrequency-qfactor.L1.md — §Status reciprocal-guard prose re-anchor (`:64`)

```edit:book/src/feature/eigenfrequency-qfactor.L1.md
`firm` — the L1 pure-function composition root for the eigenfrequency / Q-factor output product (the output-product **leaf feature column**), authored under the FEATURE-SURFACE SPINE directive (2026-06-02), the L1 counterpart of the [eigenfrequency-qfactor.L4](./eigenfrequency-qfactor.L4.md) composition root. It consumes the [`eigenmode.L1`](./eigenmode.L1.md) driver column's converged eigenpair set, then maps each mode to its `(f, Q)` row (the problem-type eigenvalue un-transform + the resistive-port κ participation ratio + the `f/κ` quotient). **The column promotes off `seed` to `firm` under the OWN-COMPOSITION rule (USER DIRECTIVE 2026-06-03):** its sole directly-owned constituent, the reduction's L4 home [`eigenfreq_qfactor_reduce`](../L4/eigenfreq_qfactor_reduce.md), is **`firm`** (promoted cycle-082 on the firm-on-positive-structure escape; both of its folded per-mode primitives firm L1 — the eigenvalue un-transform via [`eigenvalue-untransform`](../L1/eigenvalue-untransform.md) (cycle-080) and the κ participation ratio via [`participation_ratio`](../L1/participation_ratio.md) (cycle-077) — and the eigenpair→`(f, Q)` assembly carries no inner-product-axiom content). The cross-link to the [`eigenmode.L1`](./eigenmode.L1.md) driver column (which is itself `firm`; its `feature_root: seed` is the permanent root marker, not a maturity, under the scheme §3 split) is a **SIBLING reference, NOT a blocker** — the reciprocal drift-guard, not a constituent-firmness dependency. This retires the earlier mutual-blocking deadlock (the prior text held the column seed because `eigenmode` was seed, while `eigenmode` was symmetrically held seed for reducing into this column). The verb-side gate (OQ `eigenfreq-qfactor-reduce-firm-needs-assembly-test`) was discharged at c082. The chapter carries the compositional claim only; per-op algebraic claims live in the linked chapters. The defining structural fact carried from L4: a rank-1 per-mode scalar-ratio table, NOT a `gram_reduce` family-PAIR grid (c074 D6 closed-negative). Evidence: the L0 readout / Q-factor ranges `eigensolver.cpp:424-439` + `postoperator.cpp:1171-1203` realizing the reduction, plus the constituent down-links.
```

#### eigenfrequency-qfactor.L0.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/eigenfrequency-qfactor.L0.md
---
kind: feature-surface
feature: eigenfrequency-qfactor
level: L0
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: palace/drivers/eigensolver.cpp:424-475
      kind: cites-evidence
    - target: palace/models/postoperator.cpp:1171-1203
      kind: cites-evidence
  reference:
    - feature/eigenfrequency-qfactor.L1
---
```

#### sparameters.L4.md — frontmatter SPLIT + typed edges

`driven` (producing driver column) → `reference`; the reduce verb → `depends-on`.

```edit:book/src/feature/sparameters.L4.md
---
kind: feature-surface
feature: sparameters
level: L4
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L4/sparameter_reduce
      kind: folds
    - target: palace/models/postoperator.cpp:1246-1307
      kind: cites-evidence
    - target: palace/models/lumpedportoperator.cpp:283-294
      kind: cites-evidence
    - target: palace/models/waveportoperator.cpp:780-793
      kind: cites-evidence
  reference:
    - feature/driven.L4
---
```

#### sparameters.L4.md — §Status reciprocal-guard prose re-anchor (`:67`)

The "(its own `status: seed`)" parenthetical on the driven cross-link is stale: `driven` is `status: firm` (promoted c085).

```edit:book/src/feature/sparameters.L4.md
`firm` — an output-product **leaf feature column** authored under the FEATURE-SURFACE SPINE directive (2026-06-02). The composition is sound: stage (1) consumes the [`driven.L4`](./driven.L4.md) driver column's per-ω solution family; stage (2) composes the [`sparameter_reduce`](../L4/sparameter_reduce.md) *(firm, c083)* port-projection reduction (the port-projection sibling of the c074 energy-Gram reductions, NOT a `gram_reduce` weight specialization). **The column promotes off `seed` to `firm` under the OWN-COMPOSITION rule (USER DIRECTIVE 2026-06-03; codified batch-26 meta-phase; memory `project_feature_column_promotion_rule`):** a column promotes when its OWN composition + directly-owned constituents are firm. This column's sole directly-owned constituent — [`sparameter_reduce`](../L4/sparameter_reduce.md) — is `firm` (c083 lowering-verifier firm-on-positive-structure promotion). The batch-26 meta-phase the c083 prose deferred to has now fired and enacted the OWN-COMPOSITION rule, so the earlier "held pending the batch-26 meta-phase" clause is retired; the cross-link to the [`driven.L4`](./driven.L4.md) driver column (which is itself `firm`; its `feature_root: seed` is the permanent root marker, not a maturity, under the scheme §3 split) is a **SIBLING reference, NOT a blocker** — the reciprocal drift-guard, not a constituent-firmness dependency. This chapter carries the *compositional* claim (S-parameters = the port-projection reduction over the driven driver's per-ω solution family), not the constituents' per-op algebraic claims (those live in `sparameter_reduce` and the L0 projection sites). Evidence: the L0 reduction range `postoperator.cpp:1246-1307` (`MeasureSParameter`) + the port-projection verbs (`lumpedportoperator.cpp:283-294`, `waveportoperator.cpp:780-793`), all self-verified on-disk via palace-codemap this dispatch, plus the constituent down-links.
```

#### sparameters.L1.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/sparameters.L1.md
---
kind: feature-surface
feature: sparameters
level: L1
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L1/port_projection
      kind: folds
    - target: palace/models/postoperator.cpp:1246-1307
      kind: cites-evidence
    - target: palace/models/lumpedportoperator.cpp:283-294
      kind: cites-evidence
    - target: palace/models/waveportoperator.cpp:780-793
      kind: cites-evidence
  reference:
    - feature/driven.L1
---
```

#### sparameters.L1.md — §Status reciprocal-guard prose re-anchor (`:64`)

```edit:book/src/feature/sparameters.L1.md
`firm` — the L1 pure-function composition root for the scattering-matrix output product (the output-product **leaf feature column**), authored under the FEATURE-SURFACE SPINE directive (2026-06-02). It consumes the [`driven.L1`](./driven.L1.md) driver column's per-ω solution family, then folds the firm L1 port-mode projection ([`port_projection`](../L1/port_projection.md), firm as of cycle-077) over the (port, frequency) grid with the self-reflection + port-kind closing. The per-mode projection primitive is firm; as of cycle-083 the whole-grid reduction it composes — [`sparameter_reduce`](../L4/sparameter_reduce.md) at L4 — is **also `firm`** (the lowering-verifier firm-on-positive-structure promotion). **The column promotes off `seed` to `firm` under the OWN-COMPOSITION rule (USER DIRECTIVE 2026-06-03):** its sole directly-owned constituent — the reduction's L4 home [`sparameter_reduce`](../L4/sparameter_reduce.md) — is firm (c083). The batch-26 meta-phase the c083 prose deferred to has now fired and enacted the OWN-COMPOSITION rule; the cross-link to the [`driven.L1`](./driven.L1.md) driver column (which is itself `firm`; its `feature_root: seed` is the permanent root marker, not a maturity, under the scheme §3 split) is a **SIBLING reference, NOT a blocker** — the reciprocal drift-guard, not a constituent-firmness dependency. The chapter carries the compositional claim only; per-op algebraic claims live in the linked chapters. Evidence: the L0 reduction range `postoperator.cpp:1246-1307` + the port-projection verbs (`lumpedportoperator.cpp:283-294`, `waveportoperator.cpp:780-793`), self-verified on-disk this dispatch, plus the constituent down-links.
```

#### sparameters.L0.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/sparameters.L0.md
---
kind: feature-surface
feature: sparameters
level: L0
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: palace/models/postoperator.cpp:1246-1307
      kind: cites-evidence
    - target: palace/models/postoperator.cpp:1141
      kind: cites-evidence
    - target: palace/models/postoperator.cpp:1239
      kind: cites-evidence
    - target: palace/models/lumpedportoperator.cpp:283-294
      kind: cites-evidence
    - target: palace/models/waveportoperator.cpp:780-793
      kind: cites-evidence
  reference:
    - feature/sparameters.L1
---
```

#### energy-fields.L4.md — frontmatter SPLIT + typed edges (driver-AGNOSTIC: the producing driver is a generic reference, NOT a 1:1 reciprocal)

`energy-fields` is the driver-agnostic shared postprocess (ratified batch-24): the producing driver(s) are generic cross-links, NOT a single 1:1 reciprocal. The `electrostatic` `composes:` entry becomes a `reference` (a field-bearing-driver pointer); the reduce verb + the two folded L1 primitives → `depends-on`. The stale `(seed)` qualifier on `electrostatic` is dropped on conversion (`electrostatic` is firm c095).

```edit:book/src/feature/energy-fields.L4.md
---
kind: feature-surface
feature: energy-fields
level: L4
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L4/domain_energy_reduce
      kind: folds
    - target: L1/participation_ratio
      kind: folds
    - target: L1/matrix-weighted-norm
      kind: folds
    - target: palace/models/postoperator.cpp:1021-1077
      kind: cites-evidence
    - target: palace/models/domainpostoperator.cpp:255-298
      kind: cites-evidence
  reference:
    - feature/electrostatic.L4
---
```

#### energy-fields.L1.md — frontmatter SPLIT + typed edges

The stale `(seed)` qualifier on `electrostatic.L1` is dropped on conversion (`electrostatic.L1` is firm c095).

```edit:book/src/feature/energy-fields.L1.md
---
kind: feature-surface
feature: energy-fields
level: L1
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L1/participation_ratio
      kind: folds
    - target: L1/matrix-weighted-norm
      kind: folds
    - target: palace/models/postoperator.cpp:1021-1077
      kind: cites-evidence
    - target: palace/models/domainpostoperator.cpp:255-298
      kind: cites-evidence
  reference:
    - feature/electrostatic.L1
---
```

#### energy-fields.L1.md — prose `(**seed**)` re-anchor (`:43`, the D4-handed stale mention)

`electrostatic.L1` is firm c095; the inline `(**seed**)` is stale.

```edit:book/src/feature/energy-fields.L1.md
   [`electrostatic.L1`](./electrostatic.L1.md) (**firm**, the potential `V` → `E`), or any driver
```

#### energy-fields.L0.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/energy-fields.L0.md
---
kind: feature-surface
feature: energy-fields
level: L0
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: palace/models/postoperator.cpp:1021-1077
      kind: cites-evidence
    - target: palace/models/domainpostoperator.cpp:255-298
      kind: cites-evidence
  reference:
    - feature/energy-fields.L1
---
```

### Group C — lifecycle spine-ROOT (the meta-feature)

The per-driver columns are SIBLING feature columns → `reference` (the spine-ROOT sub-kind: stage-(2) constituents are *other feature columns*, which are references not blockers). The driver-agnostic vocabulary (`fold_solve`, `fe_assemble`, `ksp_solve`) → `depends-on`. The stale `(seed)` qualifiers on electrostatic/magnetostatic (L4:7,8, D4-handed) are dropped on conversion.

#### lifecycle.L4.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/lifecycle.L4.md
---
kind: feature-surface
feature: lifecycle
level: L4
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L4/fold_solve
      kind: composes
    - target: palace/main.cpp:158-328
      kind: cites-evidence
    - target: palace/drivers/basesolver.cpp:153-276
      kind: cites-evidence
  reference:
    - feature/electrostatic.L4
    - feature/magnetostatic.L4
    - feature/eigenmode.L4
    - feature/driven.L4
    - feature/transient.L4
    - feature/boundary-mode.L4
---
```

#### lifecycle.L4.md — §"Constituent down-links" sibling-column cells (`:57-58`)

`electrostatic`/`magnetostatic` are firm (D4 c095); the `seed` cells are stale. The "on disk" cell for eigenmode/driven/transient becomes their firm status; they are sibling references.

```edit:book/src/feature/lifecycle.L4.md
| dispatch → electrostatic column | [`electrostatic.L4`](./electrostatic.L4.md) (sibling reference) | firm | `palace/main.cpp:267` |
| dispatch → magnetostatic column | [`magnetostatic.L4`](./magnetostatic.L4.md) (sibling reference) | firm | `palace/main.cpp:270` |
| dispatch → eigenmode / driven / transient / boundary-mode columns | [eigenmode.L4](./eigenmode.L4.md) / [driven.L4](./driven.L4.md) / [transient.L4](./transient.L4.md) / [boundary-mode.L4](./boundary-mode.L4.md) (sibling references) | firm / firm / firm / rough-in | `palace/main.cpp:264, 261, 273, 276` |
```

#### lifecycle.L1.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/lifecycle.L1.md
---
kind: feature-surface
feature: lifecycle
level: L1
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L1/fe_assemble
      kind: composes
    - target: L1/ksp_solve
      kind: composes
    - target: L4/fold_solve
      kind: composes
    - target: palace/main.cpp:158-328
      kind: cites-evidence
    - target: palace/drivers/basesolver.cpp:153-276
      kind: cites-evidence
  reference:
    - feature/electrostatic.L1
    - feature/magnetostatic.L1
    - feature/eigenmode.L1
    - feature/driven.L1
    - feature/transient.L1
    - feature/boundary-mode.L1
---
```

#### lifecycle.L1.md — §"Constituent down-links" sibling-column cells (`:56-58`)

`electrostatic.L1`/`magnetostatic.L1` are firm (D4 c095); the `seed` cells are stale. The eigenmode/driven/transient row's "(forthcoming — not yet authored)" / "not yet authored" is also stale (those columns are on disk and firm).

```edit:book/src/feature/lifecycle.L1.md
| per-driver dispatch (electrostatic) | [`electrostatic.L1`](./electrostatic.L1.md) (sibling reference) | firm | `palace/main.cpp:267` |
| per-driver dispatch (magnetostatic) | [`magnetostatic.L1`](./magnetostatic.L1.md) (sibling reference) | firm | `palace/main.cpp:270` |
| per-driver dispatch (eigenmode / driven / transient / boundary-mode) | [eigenmode.L1](./eigenmode.L1.md) / [driven.L1](./driven.L1.md) / [transient.L1](./transient.L1.md) / [boundary-mode.L1](./boundary-mode.L1.md) (sibling references) | firm / firm / firm / rough-in | `palace/main.cpp:264, 261, 273, 276` |
```

#### lifecycle.L1.md — §"The composition" stage-2 forthcoming prose re-anchor (`:34`)

The "eigenmode / driven / transient forthcoming (not yet authored)" clause is stale.

```edit:book/src/feature/lifecycle.L1.md
2. **Dispatch the per-driver specialization** — `dispatch :: ProblemType -> (Config -> Mesh -> Product)` selects ONE per-driver feature column by `problem_type cfg`. Each branch is a per-driver feature column's L1 root: [`electrostatic.L1`](./electrostatic.L1.md), [`magnetostatic.L1`](./magnetostatic.L1.md), [`eigenmode.L1`](./eigenmode.L1.md), [`driven.L1`](./driven.L1.md), [`transient.L1`](./transient.L1.md), [`boundary-mode.L1`](./boundary-mode.L1.md) — all on disk. The selected driver `Solve` is the **specialization** of this spine — the lifecycle composes the *column*, not the column's internal ops (those are the column's own down-links). L0: the `switch (iodata.problem.type)` (`palace/main.cpp:257-280`).
```

#### lifecycle.L0.md — frontmatter SPLIT + typed edges

The L0 `l0_ground_truth:`→`depends-on kind: cites-evidence`; `lifts_to:`→`reference`; `specializes_to:` (the per-driver column pointers) → `reference` (sibling feature columns).

```edit:book/src/feature/lifecycle.L0.md
---
kind: feature-surface
feature: lifecycle
level: L0
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: palace/main.cpp:158-328
      kind: cites-evidence
    - target: palace/drivers/basesolver.cpp:153-276
      kind: cites-evidence
    - target: palace/drivers/basesolver.hpp:31-67
      kind: cites-evidence
  reference:
    - feature/lifecycle.L1
    - feature/electrostatic.L0
    - feature/magnetostatic.L0
---
```

### Group D — group-intro orientation page (NOT a DAG node — no frontmatter; closing-line status narration only)

`spine-root.md` is an orientation/overview page (scheme §5 "Index pages" — a navigational surface, not a DAG leaf node carrying claims), so it carries NO `rank:`/`edges:` frontmatter. The only edit is its stale closing line (`:9`): the lifecycle ROOT promoted to `firm` (own composition firm; per-driver dispatch over sibling references) — the "Status is `seed` … promotes past `seed` only once all its composed constituents are firm" narration is doubly stale (lifecycle is firm; and the gating rule it states is the pre-OWN-COMPOSITION rule).

```edit:book/src/feature/spine-root.md
The within-column level ordering is **high→low** (L4 → L1 → L0), the deliberate FEATURE-SURFACE exception to alpha-within-cohort ordering. The lifecycle ROOT is `firm` under the OWN-COMPOSITION promotion rule (a column promotes off `seed` when its OWN composition + directly-owned constituents are firm; cross-linked sibling columns are references, NOT blockers): its directly-owned driver-agnostic composition — the mesh-build scaffold + the firm [`fold_solve`](../L4/fold_solve.md) state-generated adaptive estimate-mark-refine fold — is firm, and the per-`ProblemType` dispatch is over the per-driver **sibling feature columns**, which are references, NOT blocking constituents (the spine-ROOT meta-feature sub-kind: stage-(2) constituents are *other feature columns*).
```

## Supporting evidence

- **`L1/eigsolve` is `firm`** (the planner's "verify at typing" OQ resolved): `book/src/L1/eigsolve.md:165` §Status `firm` (cycle-022 route-(b), promoted from `rough-in (test-coverage-bounded)`). → the `eigenmode.L1`/`boundary-mode.L1`/`L2/eigsolve → L1/eigsolve` rank "violations" are stale-edge false positives the typed-edge re-write clears.
- **D4's 4 cascade columns flipped firm** (capacitance/inductance/electrostatic/magnetostatic): `reports/2026-06-04T205500Z-layer-intro-author-cycle-095-four-column-reeval/CYCLE.md` — the source of the 5 stale-`(seed)` down-link mentions handed to D5 (`energy-fields.L4:7`, `energy-fields.L1:7,43`, `lifecycle.L4:7,8`), all re-anchored above (4 via `composes:`→`edges:` qualifier-drop, 1 via the `energy-fields.L1:43` prose `(**firm**)` re-anchor).
- **Scheme §3 SPLIT + §4(c)/§5 migration**: `book/src/methodology/graded-stack-scheme.md:137-184` (feature_root split; composes→edges; maturity-qualifier drop) + `:215-252` (un-fronted/orientation pages: index/group-intro pages are navigational, no `rank:`).
- **On-disk `## Status` survey** (NOT index cells, NOT cycle record): driven/eigenmode/transient/eigenfrequency-qfactor/sparameters/energy-fields/lifecycle = `firm`; boundary-mode = `seed` (own-readout gate → composition-maturity `rough-in`). See §Summary table.
- **Driver↔output-product cross-link convention** (driven↔sparameters, eigenmode↔eigenfrequency-qfactor): the reciprocal up-links exist in the bodies (`driven.L4:97`, `eigenmode.L4:40`), so each is a `reference:` edge (sibling root). `energy-fields` is the driver-AGNOSTIC exception (batch-24) — `electrostatic` is a generic field-bearing-driver `reference`, NOT a 1:1 reciprocal.

## Open questions / caveats

- **`cites-evidence` edges target `palace/...:lo-hi` ranges, NOT book slugs (linter-convention question — same as D4's flag).** Every L0 / evidence `depends-on kind: cites-evidence` edge I wrote points at a Palace source range, not a `book/src/<slug>.md` node. If the rank/reachability linters try to slug-resolve a `cites-evidence` target they will `NOANC`. The linter should treat `kind: cites-evidence` as exempt from slug-resolution + rank-check (a Palace-source citation is ground truth, not a DAG node with a `rank:`). **This is the uniform shape ALL feature columns (and every L0 entry under P1) will take — flag for the batch-30 meta-phase / D7 baseline-exceptions if the linter does not already special-case it.** (D4 raised the identical flag; this is a single shared convention question, not two.)
- **`boundary-mode` typed `rank: rough-in`, NOT `firm` — and NOT `seed` (a non-rank).** Its legacy `status: seed` conflated the root-marker with the composition-maturity (scheme §3). I split it: `feature_root: seed` (permanent root) + `rank: rough-in` (its honest composition-maturity — solve corner firm, but its directly-owned stage-(3) readout reduces into a waveguide-mode output product with no firm home). If the batch-30 meta-phase prefers `seed`/`stub`-equivalent for an own-readout-gated column, that is a one-token frontmatter change; `rough-in` is the most defensible map (the structure is anchored, the readout reduction is stated-but-unhomed). I did NOT change its `## Status` prose (still `seed` with the own-readout-gate narration — typing ≠ promotion/demotion of the prose status word; only the `rank:` token is written).
- **eigenmode/driven `## Status` prose still says "promoted c085" / carries `status: firm` — I did NOT touch the `## Status` word, only added the `rank:` token + `edges:` block + corrected the STALE sibling-status mentions (the down-link table cells + the reciprocal-guard "(its own status: seed)" parentheticals).** The reciprocal-guard parentheticals in eigenfreq/sparameters/eigenmode/driven referenced a sibling's `status: seed` as the drift-guard anchor; under the scheme split those siblings are `firm` (rank) + `feature_root: seed` (root), so I re-anchored the parentheticals to "(itself `firm`; its `feature_root: seed` is the permanent root marker, not a maturity)". The point (sibling = reference not blocker) is unchanged.
- **The lifecycle ROOT now `reference:`-lists all 6 driver columns (incl. eigenmode/driven/transient/boundary-mode), not just the 2 D4-flipped — but the dispatch is over ALL of them.** The prior `composes:` only listed electrostatic+magnetostatic (the two authored at the time); the dispatch body always referenced all 5/6. Listing all 6 as `reference:` edges is the correct typed-edge picture (the ROOT dispatches over every driver column; all are sibling roots = references). This is a fuller edge set than the legacy `composes:`, not a new dependency.
- **Did NOT touch `feature/index.md`, `output-product.md`, `driver-leaf.md` (D4 sole-owns this cycle), nor any vocabulary index/leaf (D1/D2/D6).** The `spine-root.md` group intro is mine (no cascade column is in the spine-ROOT grouping; D4 explicitly did not touch it). If the integrator serializes D4 before D5, D4's `electrostatic`/`magnetostatic` firm flips are on disk when D5's `reference:` edges + re-anchored sibling-status cells apply — the cross-references resolve to firm.
- **Group-intro pages carry no DAG-node status (scheme §5) — confirm the linter does not expect a `rank:` on `spine-root.md`/`driver-leaf.md`/`output-product.md`.** I treated them as orientation pages (no frontmatter). If the linter wants group intros typed, that is a meta-phase convention call (the same index-page-node-status OQ `graded-stack-index-and-concept-node-status` the planner deferred).
