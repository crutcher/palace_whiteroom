---
verifies: ../REPORT.md
critiqued_at: 2026-06-07T06:18:00Z
critic_version: 1
checks:
  citation-validity: pass
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: pass
  cross-reference-integrity: pass
  edge-label-fidelity: pass
  plan-kind-consistency: pass
  skill-uptake-survey: pass
overall_status: ready
---

# META: verification of "L1 kernel-impl — libceed-quadrature-kernel-impl"

## Critique

### Checks run

**citation-validity — pass.** Every load-bearing Palace pinpoint was re-verified independently via palace-codemap `read_range` / `get_symbol_def` (not from the producer's chain-of-thought). `AssembleCeedOperator` is defined at `integrator.cpp:423-513` (`get_symbol_def`), so the `:423-445` signature range and the `:423-512` inputs-block range are both in-range and correct; the signature parameter list (`trial_restr`/`test_restr`/`trial_basis`/`test_basis`/`geom_data`/`geom_data_restr`) matches the prose exactly. The `q_w` quad-weight `CEED_EVAL_WEIGHT` input is at line 462 (report says 462 — zero drift). `AddOperatorActiveInputFields`/`AddOperatorActiveOutputFields` are at 492/493 (report says 492/493 — zero drift). The build-QFunction range `:340-419` (`f_build_geom_factor_*`) is confirmed: `attr` (CEED_EVAL_INTERP), `q_w` (CEED_EVAL_WEIGHT), `grad_x` (CEED_EVAL_GRAD), `geom_data` output land at the report's claimed `:388`/`:390`/`:397` pinpoints. `QuadratureDataAssembly` is at `:219` (within the cited `:215-308` range). `EvalMode { Weight, None, Interp, Grad, Div, Curl }` is at `integrator.hpp:14-23` (enum keyword at 15) — both the frontmatter `:14-23` and the supporting-evidence `:15-23` are in-range. `BilinearFormIntegrator::Assemble` pure-virtual is at `integrator.hpp:58-61` (confirmed `= 0`). `bilinearform.cpp:64-70` confirmed (`trial_restr`:64, `test_restr`:66, `trial_basis`:68, `test_basis`:69); `AddSubOperator` at `:77` confirmed. No bounds errors, no path-hygiene issues, no drift. (Note: the report carries a `## Verified-against` section, but it is authored as prose evidence — NOT a fenced `verified_against:` YAML block destined for re-fencing — so the YAML round-trip sub-check is not applicable.)

**surface-or-evidence — pass.** This is a kernel-impl roadmap_goal (rank-0) chapter, not a refinement of an existing operator. It makes NO new positive algebraic claim of its own (the rank-0 disposition is explicit and honest); the structural decomposition `A = Gᵀ Bᵀ D B G` is read directly off the cited `AssembleCeedOperator` field-wiring + build-QFunction, with the speculative content scoped strictly to "the *existence* of firm L1 substrate operators" (§Justification kind). Record-definition sub-check: the signature introduces `GeomData`, `BasisMode`/`EvalMode`, `MeshNodes`, `QuadWeights`, `ElemRestriction`, `Basis` as type names in the speculative substrate-operator signatures. These are flagged as rough-in/speculative substrate (`sig (best guess)`) carried by the four declared `depends-on` roadmap-deps and routed to harvester promotion + the combinator-miner D6 shared-substrate probe; they are not asserted as defined firm records here, so the "described only by USE, never defined" gap does not bite a rank-0 node whose substrate is explicitly declared-future. `EvalMode` is grounded at its L0 home (`integrator.hpp:15-23`). No surface-or-evidence failure.

**rotation-quality — pass (not a rotation claim).** This chapter asserts no L_{n+1}→L_n rotation; it is a same-layer (L1) constructive-realization node. The "vocabulary shift" it invokes (flat-vector BLAS vs `[E, P, ...]` rank-structured element-local tensors) is the *justification for rank-0* (the substrate is genuinely new, not re-expressible in firm L1 terms), which is exactly the honest reading — it is an argument for NON-collapse to firm, not a compaction claim. No renaming-only / 1:1 mapping is presented as a rotation. Check is effectively no-op for this kind; marked pass.

**variant-axis-coverage — pass.** The two representation variants of the kernel (partial-assembly / matrix-free vs full-assembly / materialized-COO) are explicitly addressed (§L1 form: full assembly = "a derived materialization of the same contraction, not a different algorithm", cited to `operator.cpp:483` `CeedOperatorAssembleCOO`). The differential-operator axis `𝒟 ∈ {Identity, Gradient, Curl, Divergence}` selecting the `B` eval-mode is covered, and the applicability conditions explicitly scope OUT non-de-Rham/non-polynomial integrands and the GSLIB point-interpolation facility (routed to `interpolator`). Single-machine vs multi-rank is scoped out per DIRECTIVE-1. No hidden branches.

**cross-reference-integrity — pass.** All linked book chapters resolve on disk: `L1/fe_assemble.md`, `L1/weak_form_term.md`, `L1/interpolator.md`, `L1-L0/fe-assemble-libceed-boundary-obstruction.md`, `concepts/tensor-field-lift.md`, `concepts/build-time-vs-run-time-stratification.md`, `L1/index.md`, `L1/fe-assembly-intro.md`, `SUMMARY.md` all EXIST. The SUMMARY.md insertion point matches: the FE-assembly sub-spine block currently lists `eliminate_essential_bc`/`eliminate_rhs`/`fe_assemble`/`weak_form_term`, and the proposed alphabetical insertion (after `fe_assemble`, before `weak_form_term`) is correct. The index.md dep-map insertion (after the `fe_assemble` row at line 168, before `weak_form_term` at 169) is alphabetically correct. The obstruction-theme `## Status` find/replace target ("`obstruction (opaque-library-ownership)` — the mandatory sub-kind tag … The entire callable that") matches the on-disk text at lines 30-31 EXACTLY, and the replacement preserves the "The entire callable that…" continuation — surgical and well-formed. The "still 4 firm" claim for the FE-assembly sub-spine matches the on-disk index tally.

**edge-label-fidelity — pass (load-bearing for this kind; verified carefully).** The `realizes-kernel-api` edge to `fe-assemble-libceed-boundary-obstruction` is correctly declared `reference`-class (under `edges.reference:`, NOT `depends-on`), with prose throughout reinforcing free/navigational/non-blocking/no-rank-constraint/no-liveness semantics — matching DIRECTIVE-3 exactly. The obstruction theme is KEPT (`status: obstruction (opaque-library-ownership)` unchanged), gaining only the inline `kernel-api` role-label — NOT downgraded. The new `realizes-leaf` edge (impl → firm `fe_assemble`) is also correctly `reference`-class and is explicitly self-flagged in Open questions as a new label for meta/lowering-verifier review (appropriately surfaced, not silently introduced). Well-foundedness: the node is rank-0 `roadmap_goal`, so the rank invariant `rank(u) ≤ rank(v)` is vacuously satisfied for every `depends-on` edge — and the four `depends-on` substrate targets are explicitly declared as not-yet-firm roadmap-deps with NO false firm/rough-in claim. The roadmap_goal disposition is honest: an intended/speculative substrate that does not yet exist in any L1 form correctly sits at rank-0 (vs `stub`/`rough-in`, which require a real-but-undissected referent) — matches METHODOLOGY-GRADED-STACK §1e. No edge mislabel.

**plan-kind-consistency — pass.** The declared kind (roadmap_goal / rank-0 kernel-impl) matches the content shape throughout: the `## Status` calls ROADMAP_GOAL not firm, the frontmatter `rank: roadmap_goal`, the SUMMARY/index/cohort-bullet registrations all consistently register it as roadmap_goal (kernel-impl) and explicitly DON'T bump the firm count. The `kernel-impl` role-label is applied to this node and `kernel-api` to the obstruction theme, per DIRECTIVE-3. No firm apparatus is claimed inside a roadmap_goal shell; no rough-in placeholders are mislabeled firm. Consistent.

**skill-uptake-survey — pass (telemetry only).** The report states all citations were self-verified via `citecheck --anchor` + codemap `read_range` (§Supporting evidence), referencing the citation-verification procedure. The shape (roadmap_goal disposition, graded-stack rank/edge typing) is governed by methodology directives rather than a named invocable skill, so no further skill invocation is implied. Surfaced for telemetry; non-blocking.

### Issues found

None. All 8 checks pass. The report is a well-formed rank-0 roadmap_goal kernel-impl chapter: citations verify with zero drift against palace-codemap; the `realizes-kernel-api` edge is correctly `reference`-class and the obstruction theme is kept (not downgraded) with only an inline `kernel-api` role-label; the rank-0 disposition is honest (genuine flat-vector-BLAS → `[E,P,...]` vocabulary shift, no false firm/rough-in claim); well-foundedness is vacuously satisfied; and all cross-references + insertion points resolve precisely on disk. The producer appropriately self-flagged the one novel construct (the `realizes-leaf` reference-edge label) in Open questions for downstream meta/lowering-verifier review — that is a forward-routed methodology question, not a defect in this report.
