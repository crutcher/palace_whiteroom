---
agent: cycle-planner
invoked_at: 2026-06-06T232937Z
scope: cycle-118 dispatch plan (batch-38 opener — THE MESH→FE_SPACE SUBSTRATE LOWERING + GROUNDING campaign)
status: pending
---

# Cycle 118 dispatch plan

## Goals selected this cycle

Cycle-118 opens **meta-batch-38** (cycles 118/119/120; batch-38 meta fires after c120). The active head is **THE MESH→FE_SPACE SUBSTRATE LOWERING + GROUNDING campaign** — the forward continuation of the c117 all-fronts wave, which HOMED 3 new firm L1 ops (`build_mesh`, `fe_space_hierarchy`, `interpolator`) + the `waveguide-mode` rough-in column but left their L1>L0 lowering homes unauthored and the substrate ungrounded (detritus). This cycle pushes all four ranked head items at once (high shared substrate, low overlap):

1. **LEAD (HIGH):** the 3 L1>L0 construction-rotation themes (`build-mesh` / `fe-space-hierarchy` / `interpolator`) — completes the lowering homes for the whole new substrate; on each `status: firm`, upgrade the op's forthcoming-theme edge `reference` → `depends-on (kind: lowers-to)` (grounds the *L1>L0 home*, rank 3 ≤ 3). The 3 themes **share mesh→fe_space exploration substrate** — but the one-theme-per-invocation rule + distinct target files means **3 separate, fully-parallel dispatches** (shared-exploration lifting is realized by co-scheduling them in one wave, not by one dispatch).
2. **HIGH (reachability):** the faithful `feature/lifecycle.L1 → L1/build_mesh` `composes`(`depends-on`) edge (§2f GROUND) — discharges `build_mesh` off detritus (`reachable` +1).
3. **MEDIUM:** the `waveguide_mode_reduce` L4 verb home — on firm, the `waveguide-mode` column promotes rough-in→firm (closes the 6th output-product column).
4. **LOW/MEDIUM:** the `Mesh`/`FiniteElementSpaceHierarchy`/`WaveguideModeTable` record-definition cohort + the `Mesh & FE-space construction` kind-grouping fold-vs-standalone decision — bundled into one layer-intro-author dispatch.

6 dispatches, well within the 12 budget. The redirect's vocabulary-shift discipline governs: each lowering theme is a **translation across vocabularies** (typed `config → Mesh` pipeline → the L0 imperative free-function chain), NOT a 1:1 rename; a degenerate identity-in-named-terms result is a smell to resolve in-line.

## Deliverable-presence verification

Per the MANDATORY pre-dispatch four-step check (paste-inline evidence; friction `cycle-planner-stale-priorities-line-recruitment` `escalating`):

**D1 — `book/src/L1-L0/build-mesh-construction-rotation.md`** (the L1>L0 theme):
1. File existence: `MISSING (open): book/src/L1-L0/build-mesh-construction-rotation.md` — OPEN, target does not exist. ✅
2. Maturity: n/a (file absent — fresh authoring).
3. OQ-grep: OQ `build-mesh-construction-rotation-l1-l0-theme` is OPEN at ledger `:1454-1460` ("slug only; no chapter authored this cycle … *Trigger:* a follow-on abstractor/harvester dispatch authoring the L1>L0 theme"). No RESOLVED/CLOSED line. ✅
4. Structural block: NONE. The firm L1 op `build_mesh` is on disk (`book/src/L1/build_mesh.md`, `rank: firm`) carrying a `reference`→`L1-L0/build-mesh-construction-rotation` slug-as-text (`build_mesh.md:185` "No `lowers-to` edge is asserted … yet"); authoring + firming the theme promotes it to `depends-on (lowers-to)`. The L0 substrate is on disk (`main.cpp:287-302`, verified via codemap). No gate. ✅

**D2 — `book/src/L1-L0/fe-space-hierarchy-construction-rotation.md`** (the L1>L0 theme):
1. File existence: `MISSING (open): book/src/L1-L0/fe-space-hierarchy-construction-rotation.md` — OPEN. ✅
2. Maturity: n/a (absent).
3. OQ-grep: OQ `fe-space-hierarchy-construction-rotation` OPEN at `:1486-1490` ("named, NOT authored … No `lowers-to` edge is asserted from the `fe_space_hierarchy` chapter this cycle"). ✅
4. Structural block: NONE. `book/src/L1/fe_space_hierarchy.md` `rank: firm`, cites `multigrid.hpp:78-126`; carries `reference`→`L1/build_mesh` only (no forthcoming-theme reference edge yet — see Overlap note). L0 substrate verified (`multigrid.hpp:78` = `ConstructFiniteElementSpaceHierarchy`). ✅

**D3 — `book/src/L1-L0/interpolator-construction-rotation.md`** (the L1>L0 theme):
1. File existence: `MISSING (open): book/src/L1-L0/interpolator-construction-rotation.md` — OPEN. ✅
2. Maturity: n/a (absent).
3. OQ-grep: OQ `interpolator-construction-rotation-l1-l0-theme-needed` OPEN at `:1510-1516` ("named, NOT authored; the authoring tracker for the demoted `reference` edge … *Trigger:* a follow-on abstractor/lowering-verifier dispatch authors `book/src/L1-L0/interpolator-construction-rotation.md`; when authored + `status: firm` … promote the `reference` edge to a live `depends-on (kind: lowers-to)`"). ✅
4. Structural block: NONE. `book/src/L1/interpolator.md` `rank: firm` carries the explicit `reference`→`L1-L0/interpolator-construction-rotation` slug-as-text (`:19-20`, `:294-299`) pending exactly this theme. GSLIB sibling is `obstruction (opaque-library-ownership)` — to be carried as an in-theme sub-note, NOT targeted (per §Scope unimplemented/opaque policy). L0 de-Rham substrate verified (`GetDiscreteInterpolator` at `fespace.hpp:107`; the dispatch authors the construction `BuildDiscreteInterpolator` body, `fespace.cpp:173-238` per the OQ). ✅

**D4 — `book/src/feature/lifecycle.L1.md`** (frontmatter + §Dependencies edit; the GROUND edge):
1. File existence: `EXISTS: book/src/feature/lifecycle.L1.md`. ✅
2. Maturity: `rank: firm`, `feature_root: seed` (read on disk `:5-6`). The edit is an ADD of a new `depends-on (composes)` edge, not a re-author.
3. OQ-grep: NEW OQ this meta-phase (`build-mesh-lifecycle-grounding-edge`, plan head item-2; no prior chapter). Open by construction.
4. Structural block: NONE. **Confirmed `build_mesh` is NOT currently in lifecycle.L1's frontmatter at all** (grep of `:19-35` reference list → only `reference:` header, no `build_mesh`); the do-block calls `build_mesh cfg` at `:39` + prose stage-1 at `:44` cite `main.cpp:287-302`. So this is a clean NEW `depends-on (composes)` edge — the §2f GROUND disposition, faithful (the lifecycle genuinely composes `build_mesh` as its stage-1). `build_mesh` is firm (rank 3); lifecycle is firm (rank 3 ≤ 3 well-founded). ✅

**D5 — `book/src/L4/waveguide_mode_reduce.md`** (the L4 reduce-verb home):
1. File existence: `MISSING (open): book/src/L4/waveguide_mode_reduce.md` — OPEN. ✅
2. Maturity: n/a (absent). The `waveguide-mode.{L4,L1}` columns are `rank: rough-in / feature_root: seed` (read on disk: `waveguide-mode.L4.md:5-6`, `.L1.md:5-6`), explicitly gated on this verb ("`waveguide_mode_reduce` *(rough-in; no firm L4 verb chapter yet — OQ `waveguide-mode-reduce-needs-l4-verb-home`)*", `.L4.md:83`).
3. OQ-grep: OQ `waveguide-mode-reduce-needs-l4-verb-home` OPEN at `:1422`. No RESOLVED line. ✅
4. Structural block: NONE. L0 substrate verified (`boundarymodesolver.cpp:300-340` — the per-mode reduce: VD back-transform, power-normalize, `Bz = curl(Et)/(iω)`). **GUARD (carried into scope): do NOT force-unify with `eigenfreq_qfactor_reduce`** (the guard-OQ; the column itself flags it shares the `eigsolve` solve-corner but is a distinct propagation-mode reduce carrying mode FIELDS, `.L4.md:73-74`). ✅

**D6 — record-definition cohort + kind-grouping fold** (layer-intro-author bundle):
1. File existence: candidate concept pages OPEN — `MISSING (open): book/src/concepts/Mesh.md`, `book/src/concepts/mesh.md` (neither exists; the in-chapter `## Record definition` for `Mesh` lives in `build_mesh.md`). The `mesh-construction-intro.md` group-intro stub EXISTS (`book/src/L1/mesh-construction-intro.md`, read on disk: `# L1 — Mesh & FE-space construction`, "Currently one member: build_mesh … fold question open"). ✅
2. Maturity: the 3 records currently have in-chapter definitions (`Mesh` in `build_mesh.md`; `FiniteElementSpaceHierarchy` in `fe_space_hierarchy.md`; `WaveguideModeTable` in `waveguide-mode.{L4,L1}`). The dispatch is a **≥2-consumer-bar JUDGMENT** per record (promote to `concepts/<record>.md` only if ≥2 consumers; else keep in-chapter + flag) + the fold-vs-standalone decision — NOT a forced page authoring.
3. OQ-grep: OQs `record-Mesh-needs-definition-home` (`:1446`), `record-FiniteElementSpaceHierarchy-needs-definition-home` (`:1478`), `record-WaveguideModeTable-needs-definition-home` (`:1430`), `build-mesh-fe-space-kind-grouping-fold-residual-c117` (`:1502`, PARTIALLY-RESOLVED — stub exists, fold question open) all OPEN. ✅
4. Structural block: NONE (record-definition obligation is always-eligible judgment work; the fold residual is navigation hygiene). ✅

**STOP-PROPOSING negative-list check:** none of D1–D6 match a disqualified slug (`lu_solve` / `back_solve` / `ls-update-column` / the `nleps_*` cohort). ✅

**Already-landed check (c027 bullet):** linter live on disk (`tools/graded-stack-lint`, run this cycle) = `files=363, reachable=136, roots=39, detritus=129, STRONGER=24, rank_violations=0, untyped=61, expected-unreachable=46` — matches the c117 finalize values exactly (no drift; nothing from this plan already landed). All 4 c118 head items are open per the OQ grep above. ✅

## Linter baseline (live on disk, run this cycle via `tools/graded-stack-lint`)

```
files scanned:        363
untyped (WARNING):    61
feature roots:        39
reachable from roots: 136
DETRITUS:             129
STRONGER GARBAGE:     24
rank_violations:      0
unresolved_depends_on_targets: 0 (implied — RESULT: 0 rank violations)
expected-unreachable (outside-DAG): 46
```

Projected deltas this cycle: **D4 grounds `build_mesh`** → `reachable` 136→137, `detritus` 129→128 (the GROUND disposition). **D1/D2/D3** each flip their L1>L0 theme reachable IF the upper-endpoint op carries the `lowers-to` edge AT the theme (the c114 §5 scheme note: an L1>L0 theme is reachable iff its L1 op points at it) — D1 promotes `build_mesh`'s `reference`→`depends-on(lowers-to)`, D3 promotes `interpolator`'s; D2 must ADD the `fe_space_hierarchy`→theme `lowers-to` edge (it has none today). So each authored+firmed theme that is pointed-at-by-a-reachable-op flips reachable: `build_mesh` is reachable AFTER D4, so its theme (D1) flips reachable; `interpolator`/`fe_space_hierarchy` remain RE9/RE10 baseline-excepted (their ops stay detritus), so D2/D3's themes ground the *home* but may stay detritus until the op grounds — **this is expected and correct, NOT a regression** (the §3 two-detritus-subset framing). `rank_violations` MUST hold 0. **RE9/RE10 are NOT discharged this cycle** (no faithful inbound consumer for `fe_space_hierarchy`/`interpolator` ops lands — D2/D3 ground homes, not ops; per the baseline-exceptions `:163-165` note). D5 is frontmatter-additive (new L4 verb node + the column flip to firm).

## Dispatches

| # | agent | scope | deps |
|---|---|---|---|
| **D1** | `abstractor` | **L1>L0 theme: `build-mesh-construction-rotation`** — author `book/src/L1-L0/build-mesh-construction-rotation.md`. Narrate how the typed pure `build_mesh :: Config → Mesh` lowers into the L0 imperative free-function chain `mesh::Load` ▷ `solver->Preprocess` ▷ `mesh::Partition` (read single-rank) ▷ `mesh::RefineMesh` ▷ `make_unique<Mesh>` (`palace/main.cpp:287-302` — codemap-confirmed; END line is the close-brace of the build block, on-disk-confirm). Include the in-place `unique_ptr` mesh-handle mutation (the level-vector grown in place by `RefineMesh`, `geodata.cpp:421-450`) + the `EnsureNodes()`/`Update()` construction finalization (`mesh.hpp:79-80`). On `status: firm`, **the dispatch's proposed-changes UPGRADE `book/src/L1/build_mesh.md`'s `reference`→`L1-L0/build-mesh-construction-rotation` edge to `depends-on (kind: lowers-to)`** (the coupled re-anchor — bundle it INTO this dispatch per the floor-landing-implies-same-cycle-reanchor convention; `build_mesh` is the upper endpoint, small, mechanical). Vocabulary-shift: a genuine translation (typed pipeline → imperative C++ free-function chain + in-place handle mutation), NOT a rename. | none |
| **D2** | `abstractor` | **L1>L0 theme: `fe-space-hierarchy-construction-rotation`** — author `book/src/L1-L0/fe-space-hierarchy-construction-rotation.md`. Narrate how the typed `AddLevel`-fold of `fe_space_hierarchy` lowers into the L0 imperative `ConstructFiniteElementSpaceHierarchy` body (`palace/fem/multigrid.hpp:78-126` — codemap-confirmed `:78` = the function; END `:126` is a close-brace, on-disk-confirm per the brace-boundary drift caveat): the `coarse_mesh_l` computation (`:87-88`), the `make_unique<FiniteElementSpace>` seed (`:89-90`), the two `AddLevel` refinement loops (`:104-112`, `:115-123`), the optional per-level `GetEssentialTrueDofs` block (`:92-101`). On `status: firm`, **proposed-changes ADD a NEW `depends-on (kind: lowers-to)`→`L1-L0/fe-space-hierarchy-construction-rotation` edge to `book/src/L1/fe_space_hierarchy.md`** (it has NO forthcoming-theme reference edge today — this is an ADD, not an upgrade; bundle the re-anchor into this dispatch). NOTE: `fe_space_hierarchy` the OP stays RE9 baseline-excepted (the theme grounds the home, not the op). | none |
| **D3** | `harvester` | **L1>L0 theme: `interpolator-construction-rotation`** — author `book/src/L1-L0/interpolator-construction-rotation.md`. Narrate how the pure L1 `interpolator(aux, primal)` de-Rham discrete grid-transfer construction lowers into the L0 `BuildDiscreteInterpolator` body (`palace/fem/fespace.cpp:173-238` per the OQ; the `GetDiscreteInterpolator` accessor at `fespace.hpp:107` — codemap-confirmed): the cache-drop + lazy-rebuild memoization rotation (`G.reset()` on auxiliary-space change, `fespace.hpp:108-114`), the map-type-pair dispatch → MFEM interpolator kernel selection, and the **transparent 2D-native-vs-libCEED-PA representation note** (2D scalar-curl branch bypasses libCEED PA). **The GSLIB point-interpolation sibling is an `obstruction (opaque-library-ownership)` — carry it as an in-theme sub-note (NOT a lowering rule, NOT a fill-in target; `interpolator.hpp:50-56` GSLIB decls + the `MFEM_ABORT` GSLIB-absent fallbacks).** On `status: firm`, **proposed-changes UPGRADE `book/src/L1/interpolator.md`'s `reference`→`L1-L0/interpolator-construction-rotation` edge to `depends-on (kind: lowers-to)`** (already structured for exactly this in `interpolator.md:294-299`; bundle the re-anchor). NOTE: `interpolator` the OP stays RE10 baseline-excepted. | none |
| **D4** | `layer-intro-author` | **GROUND edge: `feature/lifecycle.L1 → L1/build_mesh`** — edit `book/src/feature/lifecycle.L1.md` to ADD a NEW `depends-on` edge `{ target: L1/build_mesh, kind: composes }` to the frontmatter `edges.depends-on:` block (currently `build_mesh` is in NEITHER the depends-on nor the reference list — confirmed by grep; the do-block calls it at `:39`, prose stage-1 at `:44`). Add the matching §Dependencies prose row (`build_mesh` = the lifecycle ROOT's stage-1 `config→mesh` composition-root constituent; firm). This is the §2f GROUND disposition (faithful — the lifecycle genuinely composes `build_mesh` as stage-1; the edge was previously absent only because `build_mesh` did not exist as a node when the lifecycle was authored). Grounds `build_mesh` off detritus (`reachable` +1). Well-founded: lifecycle firm (3) ≤ build_mesh firm (3). | none |
| **D5** | `combinator-miner` | **L4 verb home: `waveguide_mode_reduce`** — author `book/src/L4/waveguide_mode_reduce.md`, the per-mode propagation-mode reduce verb (the propagation-mode member of the output-product reduce-verb family). Signature `waveguide_mode_reduce :: EigResult → Scalar → WaveguideModeTable` (per `waveguide-mode.L4.md:59`, using the semantic-surface §1.2.1 named-shape-groups notation for the rank-generic mode field-vectors). Body = the per-mode map: eigenvalue un-transform → `kn`; VD back-transform → `(Et, En)`; power-normalize to `|P|=1`; conditional `Bz = curl(Et)/(iω)` for propagating modes. L0: `boundarymodesolver.cpp:300-340` (codemap-confirmed — the readout reduce). **GUARD: do NOT force-unify with `eigenfreq_qfactor_reduce`** (per the guard-OQ; it shares the `eigsolve` solve-corner but is a distinct reduce carrying mode FIELDS, not scalar-only — keep them as sibling members of the reduce-verb algebra). On `status: firm`, **proposed-changes flip `waveguide-mode.{L4,L1}` rough-in→firm** (the column's promotion gate; `feature_root: seed` KEPT as the permanent GC-root marker). Add the `waveguide-mode.L4` `depends-on (composes)`→`L4/waveguide_mode_reduce` edge. | none |
| **D6** | `layer-intro-author` | **record-definition cohort + kind-grouping fold (bundle)** — (a) PER-RECORD ≥2-consumer-bar JUDGMENT for `Mesh` / `FiniteElementSpaceHierarchy` / `WaveguideModeTable`: promote to a `book/src/concepts/<record>.md` page ONLY where ≥2 distinct consumers exist (e.g. `Mesh` is consumed by `build_mesh` + `fe_space_hierarchy` + lifecycle — likely a page; `WaveguideModeTable` consumed by `waveguide-mode.{L4,L1}` — judge); else keep the in-chapter `## Record definition` + record the disposition. Define the DATA SHAPE (fields, types, construction-vs-run-time stratum, the L0 backing struct/`IoData` home), NOT operator algebra. (b) DECIDE the `Mesh & FE-space construction` kind-grouping fold-vs-standalone (`book/src/L1/mesh-construction-intro.md` group-intro stub EXISTS; OQ `build-mesh-fe-space-kind-grouping-fold-residual-c117`): keep `Mesh & FE-space construction` as a standalone grouping or fold into the FE-space sub-spine. **D6 OWNS any `concepts/index.md` / SUMMARY rows it creates** (sole owner this cycle). | none |

## Overlap analysis

Pairwise (OVERLAPPING = same file region modified OR one names a node the other authors):

- **D1 ↔ D2 ↔ D3** (the 3 L1>L0 themes): **NON-OVERLAPPING / PARALLEL.** Distinct target files (`build-mesh-` / `fe-space-hierarchy-` / `interpolator-construction-rotation.md`), distinct upper-endpoint op files each dispatch re-anchors (`L1/build_mesh.md` / `L1/fe_space_hierarchy.md` / `L1/interpolator.md` — disjoint). They SHARE mesh→fe_space *exploration context* (the shared-substrate lifting intent) but the one-theme-per-invocation rule forbids merging; co-scheduling them in one wave realizes the shared lifting. Each appends its OWN `L1-L0/index.md` dep-map row + SUMMARY entry (anchor-distinct, parallel-safe). **No consolidated tally in `L1-L0/index.md` is touched by all three** (these are distinct theme rows, not a cohort count) — no count-owner partition needed.
- **D1 ↔ D4** (build-mesh theme ↔ lifecycle GROUND edge): **NON-OVERLAPPING / PARALLEL.** D1 edits `L1/build_mesh.md`'s `lowers-to` edge (downward); D4 edits `feature/lifecycle.L1.md`'s `depends-on` edge (a DIFFERENT file's upward edge naming `L1/build_mesh` as a target). D4 names `L1/build_mesh` as an EXISTING firm node (it is on disk, firm); D4 does NOT author or modify `build_mesh.md`. D1 does NOT touch lifecycle. No shared file region. Marked PARALLEL (when-in-doubt-parallel; any mild reachability-measurement interplay is the per-report-integrator-re-measure case, surfaces as an integrator-signal not a conflict).
- **D5 ↔ all**: **NON-OVERLAPPING / PARALLEL.** D5 authors a NEW L4 node (`L4/waveguide_mode_reduce.md`) + flips `waveguide-mode.{L4,L1}` rough-in→firm + edits `L4/index.md` (alpha-insert) + SUMMARY. No other dispatch touches the `feature/waveguide-mode.*` columns, the `L4/` op set, or `waveguide_mode_reduce`. (D6's `WaveguideModeTable` record judgment is a DIFFERENT artifact — the record's data-shape home, not the reduce verb; if D6 promotes a `concepts/WaveguideModeTable.md` page, D5's `waveguide_mode_reduce.md` will `reference` it — a forward-reference; see canonical-slug coordination below.)
- **D6 ↔ all**: **NON-OVERLAPPING / PARALLEL with one forward-reference coordination.** D6 owns the record-definition pages + the kind-grouping fold + any `concepts/index.md`/SUMMARY rows it creates (sole owner — no shared-index count divergence). The forward-reference: D5 (`waveguide_mode_reduce.md`) references `WaveguideModeTable`; D3 (`interpolator-construction-rotation.md`) / D1 / D2 reference `Mesh` / `FiniteElementSpaceHierarchy`. **Canonical-slug coordination (the `cross-report-forward-reference-slug-divergence` guard):** D6's per-record JUDGMENT decides whether each becomes a `concepts/<record>.md` page or stays in-chapter — so the OTHER dispatches must NOT invent a `concepts/` link. **Scope instruction (written into D1/D2/D3/D5): reference the record by its CURRENT in-chapter home** (`Mesh` → `[`Mesh`](../L1/build_mesh.md#record-definition)`; `FiniteElementSpaceHierarchy` → `[…](../L1/fe_space_hierarchy.md#record-definition)`; `WaveguideModeTable` → in-chapter in `waveguide-mode.L4.md`) and DO NOT link a `concepts/<record>.md` path. If D6 promotes a record to a `concepts/` page, the per-report integrator (or a follow-on lifter) re-points the back-links — D6 is the sole authority on whether the page exists, avoiding a guessed-slug divergence. This is a deliberate ordering: D6 in a LATER wave so the per-report integrator wires its pages before the references would resolve.

No two dispatches modify the same operator entry or rewrite the same theme body. The only genuine ordering constraint is the D6 forward-reference (record-page-existence), handled by wave placement + the reference-the-current-home instruction.

## Sequencing schedule

**Wave 1 (parallel) — D1, D2, D3, D4, D5** (5 dispatches):
- The 3 L1>L0 themes (D1/D2/D3) + the lifecycle GROUND edge (D4) + the L4 reduce verb (D5). All touch disjoint files; all reference only already-on-disk firm nodes. The shared mesh→fe_space substrate is lifted once across D1/D2/D3 co-scheduled here.

**Wave 2 (after wave-1 reports land) — D6** (1 dispatch):
- The record-definition cohort + kind-grouping fold. Placed in wave 2 because D6's per-record page-vs-in-chapter JUDGMENT is the authority that wave-1 dispatches (D1/D2/D3/D5) forward-reference; running D6 after wave-1 lands lets the per-report integrator wire any new `concepts/<record>.md` pages and re-point the wave-1 in-chapter back-links cleanly. (Wave-1 dispatches reference the CURRENT in-chapter homes per their scope, so wave-1 is link-valid on its own even if D6 changes nothing — wave 2 is for the upgrade, not a hard block. This is dispatch/forward-reference ORDERING; the book is not rebuilt between waves, and there is exactly ONE `integrator-finalize` at cycle end.)

Reminder: pipeline is D1–D6 → 6 critics → repairers (on warn/fail) → `integrator-per-report` ×6 (serial apply) → ONE `integrator-finalize` (rebuild + commit + push + housekeeping).

## Open questions / caveats

- **RE9/RE10 are NOT discharged this cycle and that is correct.** D2/D3 ground the L1>L0 *homes* (the theme files), NOT the `fe_space_hierarchy`/`interpolator` *ops* (which stay detritus / baseline-excepted until a faithful inbound consumer — geometric-multigrid preconditioner / divfree-projector / field-probe — lands). The STRONGER GARBAGE SIGNAL (24) should hold or drop by at most the theme-flip subset; it should NOT require a new RE. If the linter shows an unexpected STRONGER climb beyond 24 without a new ratified RE, the integrator-finalize should flag it for the batch-38 meta-phase (per the baseline-exceptions `:167` escalate guard).
- **Brace-boundary END-line drift watch (D1/D2/D3):** the cited ranges `main.cpp:287-302`, `multigrid.hpp:78-126`, `fespace.cpp:173-238` are codemap localization HINTS; the END lines (close-braces) are the off-by-one-prone bound (friction `codemap-read-range-plus-one-drift-on-brace-boundary`). I confirmed `main.cpp:287-302` and `multigrid.hpp:78` on disk via codemap this cycle, but the producers must on-disk-confirm the END close-brace lines before citing (`citecheck --anchor` is blind to END drift).
- **`waveguide-mode` column promotion (D5):** the column flip rough-in→firm is bundled INTO D5's proposed-changes (the floor-landing-implies-same-cycle-reanchor convention: the verb's firming is exactly the column's promotion gate, and the column files are small/mechanical to flip). If the critic judges the verb-firmness insufficient for a clean column promotion, the flip can defer to a follow-on lifter — note this as the fallback, but the expected path is in-D5 (matching the `sparameter_reduce`/`sparameters` c083 precedent the column itself cites).
- **Kind-grouping fold (D6) is a JUDGMENT, not a forced restructure.** If D6 judges "keep standalone" (the `Mesh & FE-space construction` grouping is coherent as-is), that is a valid clean disposition that closes the OQ — do not force a fold. The group-intro stub already exists and is wired, so either disposition is link-safe.
- **Linter-maintenance carried items** (`graded-stack-prose-status-inference-masks-untyped`, the roots-39-re-measure, the `semantics/index` expected-unreachable-matcher note) remain ask-class `tools/` changes bundled for a future `tools/`-code cycle — NOT dispatched this cycle (no `tools/` write authority in a primary cycle; meta-phase owns `tools/`). Flagging here so the batch-38 meta-phase (after c120) keeps them on the radar.
- **Cadence note:** c118 is the FIRST primary cycle of batch-38; the friction-ledger/priorities I read may be up to ~3 cycles stale by c120. No pattern observed this cycle warrants a mid-batch methodology adjustment beyond the carried linter-maintenance items above.
