---
agent: cycle-planner
invoked_at: 2026-06-02T061737Z
scope: cycle-059 dispatch plan
status: pending
---

# Cycle 059 dispatch plan

## Goals selected this cycle

Cycle-059 is the SECOND primary cycle of meta-batch-18 (058/059/060; the batch-18 meta-phase
fires after cycle-060's finalize). Cycle-058 landed the `fold_solve` firm L4 entry + firm L4>L3
dissolution theme, recorded `map_solve` as a permanent single-witness spine-coverage finding, and
confirm-clean'd the L1/L1-L0 index tables. Cycle-059 continues the solver-test-load frontier under
the 2026-06-01 VOCABULARY-SHIFT REDIRECT by **propagating the `fold_solve` combinator one layer
DOWN** (warrant-first L3-image, anti-mirror discipline), doing the **cheap clean L4/index
cohort-firmness-split prose refresh** the c058 harvester flagged, and running **one cheap
observation-first continuing-frontier probe** (eigenmode outer machinery). It is a focused 3-dispatch
cycle. `weak-form-term-fe-cohort` STAYS HELD — no pipeline pull-up this cycle NEEDS a non-diffusion
term, and authoring term forms speculatively violates the redirect clean-gate (solvers advance the
spine only when cleanly describable, never forcing it).

## Deliverable-presence verification (paste-inline-evidence; mandatory per the load-bearing discipline)

**D1 — `book/src/L3/fold_solve.md` + `book/src/L3-L2/fold-solve-time-step-body.md` (the L3-image + its L3>L2 hop):**
1. File existence:
   - `ls book/src/L3/fold_solve.md` → `ls: cannot access 'book/src/L3/fold_solve.md': No such file or directory` (ABSENT)
   - `ls book/src/L3-L2/fold-solve*` → `ls: cannot access 'book/src/L3-L2/fold-solve*': No such file or directory` (ABSENT)
2. Maturity: N/A (file absent — open by construction).
3. OQ-ledger RESOLVED-grep: `grep -i "fold-solve-l3.*RESOLVED\|fold-solve-l3.*CLOSED" scaffolding/open-questions.md` → no matches. The OQ `fold-solve-l3-entry-vs-dissolution-home` (open-questions.md:812) is OPEN (intake block, NOT in the Closed-index; the batch-17 unify-log closed only the c055/056/057 blocks, this OQ is a c058 D1 intake).
4. Structural-block check: NOT blocked. The two firm c058 anchors the L3-image lifts from are on disk — `book/src/L4/fold_solve.md` (PRESENT firm; `ls` confirmed) + `book/src/L4-L3/fold-solve-time-step-dissolution.md` (PRESENT firm; `ls` confirmed). The L0 anchors are already cited firm in the c058 D2 theme (`transientsolver.cpp:93` `time_op.Step` [codemap-confirmed], `timeoperator.cpp:410` `ode->Step(sol,t,dt)` [codemap-confirmed], outer sweep `transientsolver.cpp:77-95` [codemap-read]). The `L3/solve_family` STOP-PROPOSING negative-list entry does NOT match: the negative list bars an `L3/solve_family` MIRROR (embarrassingly-parallel loop → NO-ENTRY warrant); `fold_solve`'s loop is the OPPOSITE shape (carry-threading sequential-obstruction + opaque per-step body), which the negative-list ADD explicitly carves an exception for ("re-open only on a witnessed ... family sweep whose family loop carries a genuine cross-member sequential-obstruction" — `fold_solve` IS that shape). Warrant-first, either outcome valid.

**D2 — `book/src/L4/index.md` PROSE cohort-firmness-split refresh (NOT a new slug; an existing-file prose touch):**
1. File existence: `ls book/src/L4/index.md` → PRESENT (read this cycle).
2. Maturity / already-discharged: the stale prose is on disk. `grep -n` confirmed: line 32 `**Firm at L4 (6 + 4 outer-driver)** — UNCHANGED this cycle (cycle-055 added one *rough-in* combinator, solve_family, not a firm entry)` [STALE — `fold_solve` firm-promoted c058, not registered]; line 62 `fold_solve / time_step_fold *(rough-in thread-opener; cycle-057 D4)*` [STALE — now firm c058 D1]. The TABLE row (line 82 `[fold_solve](./fold_solve.md) | ... | firm (harvested cycle-058 D1 LEAD ...)`) is ALREADY firm — only the PROSE remains, which is the layer-intro-author's domain (the c058 D1 OQ note: "the dep-map TABLE row was flipped to firm by this dispatch ... only the prose cohort framing remains, which is the layer-intro-author's domain"). NOT a no-op.
3. OQ-ledger RESOLVED-grep: `grep` of `fold-solve-l4-index-vocabulary-cohort-firmness-split-refresh` → present at open-questions.md:816 as an OPEN c058 D1 intake (NOT in Closed-index). Resolve trigger explicitly names "a cycle-059+ layer-intro-author refresh dispatch on L4/index.md."
4. Structural-block check: NOT blocked. Cheap prose touch on an existing file.

**D3 — eigenmode-outer-machinery probe (observation-ONLY; authors NO named artifact):**
- Open by construction (observation-only continuing-frontier probe; no target slug; no prior-cycle history of this specific probe). The probe writes a CYCLE.md finding ONLY — it does NOT author `book/`. Skip-justified: open by construction (observation-only, no deliverable slug).

**Result: all 3 dispatches verified OPEN.** D1/D2 carry pasted evidence above; D3 is open-by-construction (observation-only). No pick is on the STOP-PROPOSING negative list (D1's `fold_solve` L3-image is carved-out by the negative-list's own sequential-obstruction exception; it is NOT an `L3/solve_family` mirror). No pick matches a disqualified slug.

## Dispatches

1. **agent**: `abstractor`
   **scope**: `fold-solve-l3-image` — WARRANT-FIRST L3-image of the c058 firm L4 `fold_solve`
   combinator (resolves OQ `fold-solve-l3-entry-vs-dissolution-home`). FIRST judge (anti-mirror per
   the 2026-06-01 redirect) whether `fold_solve`'s L3 image is a genuine concise iteration-rotation
   form: the **likely-YES axis** is that the transient time-sweep carry IS a genuine cross-step
   `sequential-obstruction` (prior step's output = next step's input, cannot reorder) AND the
   per-step body is opaque-library-owned (`ode->Step`, `timeoperator.cpp:410`) → an L3 entry
   recording a **`partial-obstruction`** (the `chebyshev`/`eigsolve` shape — body lifts, loop does
   not) is the likely warranted outcome, in CONTRAST to `solve_family`'s embarrassingly-parallel
   family loop that earned the c057 D1 NO-ENTRY warrant. IF warranted: author firm
   `book/src/L3/fold_solve.md` (`partial-obstruction`) + the L3>L2 hop
   `book/src/L3-L2/fold-solve-time-step-body.md` (**canonical slug** — author this cycle). ELSE
   record the NO-ENTRY verdict in the report + OQ-disposition (both VALID). Couple INTO this same
   dispatch the re-anchor of the L4-cap / L4>L3 forward framing if the L3 entry lands (the firm
   `book/src/L4/fold_solve.md` §"L3" in-line note + the c058 D2 `fold-solve-time-step-dissolution`
   §"L3 form" framing currently say "standalone L3 entry question deferred" — re-anchor to the live
   L3 entry; per the `floor-landing-implies-same-cycle-adjacent-entry-reanchor` convention, bundle
   the mechanical re-anchor into the floor-landing dispatch). L0 anchors already firm in the c058 D2
   theme (transient outer sweep `palace/drivers/transientsolver.cpp:77-95`, the step call `:93`
   `time_op.Step(t, delta_t)`; opaque per-step boundary `palace/models/timeoperator.cpp:410`
   `ode->Step(sol, t, dt)` — cite Palace's CALL, NOT MFEM internals).
   **deps**: none.
   **rationale**: the c059 LEAD; continues the headline `fold_solve` combinator's downward
   propagation (the second solver-driven combinator after `solve_family`). Resolves OQ
   `fold-solve-l3-entry-vs-dissolution-home`. fan-out HIGH (the transient + ROM pipelines' L3
   iteration-rotation surface).

2. **agent**: `layer-intro-author`
   **scope**: `fold-solve-l4-index-vocabulary-cohort-firmness-split-refresh` — PROSE-ONLY refresh of
   `book/src/L4/index.md` (resolves the c058 D1 OQ of the same name). The dep-map TABLE row (line 82)
   is ALREADY firm — touch ONLY the stale PROSE: (a) line 32 `**Firm at L4 (6 + 4 outer-driver)** —
   UNCHANGED this cycle (cycle-055 added one *rough-in* combinator, solve_family ...)` → register
   `fold_solve` as the SECOND solver-driven firm L4 combinator (the state-threaded FOLD sibling of
   `solve_family`'s independent MAP; both are §3.7 `iterate_while`-family children per the batch-17
   two-combinator MAP/FOLD ratification, NO third parent abstraction); update the firm-cohort count
   to reflect `fold_solve` firm; (b) the §"Vocabulary cohort" firmness-split bullet(s) (lines ~40/50
   `solve_family` block) — add the `fold_solve` firm cohort bullet alongside `solve_family`; (c) line
   62 §"Active frontier" `fold_solve / time_step_fold *(rough-in thread-opener; cycle-057 D4)*` →
   re-state as firm (cycle-058 D1). **Count firm from each linked chapter's `## Status` line, NOT
   from the drift-prone index cells** (the c057-meta count-owner guard / `.claude/agents/
   layer-intro-author.md` survey bullet). NOTE the canonical slug `book/src/L3-L2/fold-solve-time-step-body.md`
   that D1 may author this cycle — if D2's prose forward-references the L3 entry / its L3>L2 hop, use
   D1's canonical slugs (`book/src/L3/fold_solve.md`, `book/src/L3-L2/fold-solve-time-step-body.md`);
   prefer a plain-text reference if D1's NO-ENTRY verdict makes the L3 entry not materialize this
   cycle (D2 is wave-2, so D1's verdict is known at D2 dispatch).
   **deps**: D1 (sequenced wave-2 so D2's prose can reference D1's actual L3-entry verdict — live
   link if the L3 entry lands, plain-text if NO-ENTRY).
   **rationale**: closes the c058 D1 OQ; keeps the L4 index narrative honest (it currently
   mis-describes `fold_solve` as a rough-in thread-opener). fan-out LOW (hygiene, but the L4 index is
   the cohort's navigational front).

3. **agent**: `cross-layer-cross-cutter`
   **scope**: `eigenmode-outer-machinery-cleanly-describable-probe` — ONE cheap **observation-ONLY**
   continuing-frontier probe (per the redirect's solver-test-load item-3). Beyond the already-firm
   `book/src/L3/eigsolve.md` (`partial-obstruction`) + `book/src/L4/eigsolve.md` cap (the
   role-naming `EigOutcome`-wrapper over the opaque SLEPc `EPSSolve` / ARPACK `naupd` RCI
   eigen-iteration), is there any NEW cleanly-describable spine vocabulary in the eigenmode pipeline's
   OUTER machinery (the eigenmode driver's outer sweep / multi-mode collection / post-processing
   beyond the opaque eigen-iteration), or is the eigenmode L4 surface already spine-complete? **Do NOT
   author any `book/` artifact regardless of verdict** — a finding either LICENSES a future landing
   (if genuinely-new cleanly-describable vocabulary surfaces) or RECORDS the pipeline as
   spine-complete (the redirect's "what a solver can't cleanly say is a finding about the spine"
   outcome). Cite the `disciplined-cross-pipeline-combinator-mining-gate` skill IF a fold/map shape
   surfaces (e.g. a multi-mode family collection that might be a `solve_family`/`map_solve` witness or
   a `fold_solve` witness). Localize via codemap (`drivers/eigensolver.cpp` outer machinery — path to
   be confirmed at dispatch).
   **deps**: none.
   **rationale**: continuing solver-test-load frontier; cheap, gated. Either licenses future eigenmode
   spine work or formally records the eigenmode pipeline as spine-complete. fan-out LOW (observation;
   gated outcome).

## Overlap analysis

- **D1 × D2**: D1 authors `book/src/L3/fold_solve.md` + `book/src/L3-L2/fold-solve-time-step-body.md`
  (+ a coupled in-line re-anchor touch to `book/src/L4/fold_solve.md` §L3-note and the c058 D2 theme
  §L3-form). D2 touches `book/src/L4/index.md` PROSE (lines 32/40/50/62). **Distinct files** —
  D1 does NOT touch `L4/index.md`; D2 does NOT touch `L3/` or `L3-L2/`. The ONE coupling is a
  **forward-reference**: D2's refreshed prose may link to D1's L3 entry / L3>L2 slug. This is
  ORDERING (wave), not a file-region conflict. **SEQUENCED (D1 → D2)** so D2 references D1's actual
  verdict (live-link the L3 entry if it lands; plain-text if NO-ENTRY). The canonical slugs
  (`book/src/L3/fold_solve.md`, `book/src/L3-L2/fold-solve-time-step-body.md`) are stated in BOTH
  D1's and D2's scope per the `cross-report-forward-reference-slug-divergence` convention.
- **D1 × D3**: D1 authors `L3/`/`L3-L2/` fold_solve artifacts; D3 is observation-ONLY (no `book/`
  write) on the EIGENMODE pipeline. No shared file, no shared operator name (`fold_solve` transient
  vs `eigsolve` eigenmode). NON-OVERLAPPING → parallel.
- **D2 × D3**: D2 touches `L4/index.md` prose; D3 writes no `book/` artifact. NON-OVERLAPPING →
  parallel. (Even if D3's finding later implies an `L4/index.md` Active-frontier touch, D3 authors
  nothing this cycle — no region collision.)

No two dispatches modify the same operator entry or rewrite the same theme body. The only sequencing
is the D1→D2 forward-reference ordering.

## Sequencing schedule

- **Wave 1 (parallel)**: D1 (`abstractor` — fold_solve L3-image warrant-first), D3
  (`cross-layer-cross-cutter` — eigenmode outer-machinery probe, observation-only). Independent files
  / no `book/` write for D3.
- **Wave 2 (after D1's report lands)**: D2 (`layer-intro-author` — L4/index prose refresh). Sequenced
  after D1 so D2's refreshed Active-frontier / cohort prose can reference D1's actual L3-entry verdict
  (live link to `book/src/L3/fold_solve.md` if it landed firm, or a plain-text reference if D1
  recorded NO-ENTRY). The per-report integrator wires the live link at finalize-rebuild; there is ONE
  `integrator-finalize` at cycle end (waves are dispatch/forward-reference ordering, not multiple
  finalizes).

Pipeline: planner → 3 dispatches (D1+D3 wave-1, D2 wave-2) → 3 critics → repairers (on
warn/fail) → `integrator-per-report` ×3 (serial) → ONE `integrator-finalize`.

## Open questions / caveats

- **D1 warrant outcome is genuinely open.** The likely-YES (a `partial-obstruction` L3 entry) is
  the predicted outcome given the carry-threading sequential-obstruction + opaque per-step body, but
  D1 is dispatched WARRANT-FIRST — a NO-ENTRY verdict (dissolution-theme-is-home, like
  `solve_family`) is equally valid if D1 finds the L3 image degenerate-mirror. The plan does NOT
  force the land. If D1 records NO-ENTRY, D2's prose references the c058 D2 dissolution theme as the
  authoritative L3-form home instead (plain-text or live-link, both already firm on disk).
- **D2 forward-reference contingency.** Because D2 is wave-2, D1's verdict is known when D2 is
  dispatched; the orchestrator should pass D1's verdict (L3-entry-landed vs NO-ENTRY) into D2's brief
  so D2 chooses live-link vs plain-text correctly. Canonical slugs stated in both scopes to avoid the
  `cross-report-forward-reference-slug-divergence` failure.
- **`weak-form-term-fe-cohort` HELD this cycle (NOT recruited).** No pipeline pull-up this cycle
  needs a non-diffusion term (c059 is fold-solve-L3 lead + index refresh + eigenmode probe). It stays
  the standing next-FE-width candidate; recruit when a 2nd-pipeline FE assembly concretely needs a
  non-diffusion mass/curl-curl/div-div term (verified-ABSENT this cycle: `book/src/L1/weak_form_term.md`
  + `book/src/L2/weak_form_term.md` both not-found).
- **`map_solve` NON-DISCHARGE (c058 D3) is routed to the batch-18 meta-phase** for formal close as a
  permanent single-witness spine-coverage finding — NOT a c059 dispatch (it is a meta-phase
  disposition, not forward-frontier work).
- **Meta-batch cadence note:** the batch-18 meta-phase fires AFTER cycle-060's finalize. If D1's L3
  warrant or D3's eigenmode probe surfaces a pattern warranting methodology adjustment (e.g. a recurring
  L4-combinator→L3-image warrant procedure that could be skilled, mirroring the
  `disciplined-cross-pipeline-combinator-mining-gate`), it is at most ~2 primary cycles stale by the
  batch-18 meta — flagged here so the meta-phase can catch it. No such pattern is escalating yet (the
  combinator-miner mining-gate already covers the discovery side; the L3-image-warrant side is the
  anti-mirror discipline, working as designed across c057/c058/c059).
