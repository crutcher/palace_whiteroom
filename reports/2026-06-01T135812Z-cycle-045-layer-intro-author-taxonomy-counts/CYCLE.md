---
agent: layer-intro-author
invoked_at: 2026-06-01T135812Z
scope: L3>L2 index refresh — erasure-scope taxonomy materialization + consolidated count tally (sole count-owner, cycle-045 D3, post-D1/D2)
status: pending
inputs:
  - book/src/L3-L2/index.md (the layer-overview I refresh — the §Working-Notes tally + a new §Erasure-scope-taxonomy note)
  - reports/2026-06-01T135812Z-cycle-045-abstractor-eigsolve-L3-L2-theme/CYCLE.md (D1; landed eigsolve-opaque-eigen-iteration — opaque-library root; added its own row/bullet/SUMMARY; deferred the tally to me)
  - reports/2026-06-01T135812Z-cycle-045-cross-cutter-chebyshev-L3-L2-decision/CYCLE.md (D2; landed chebyshev-nested-recurrence — unconditional-nested-double-loop root; verdict LAND; added its own row/bullet/SUMMARY; deferred the tally to me)
  - book/src/L3-L2/ksp-solve-outer-driver.md (firm c021 — unconditional-single-loop root)
  - book/src/L3-L2/orthogonalize-variant-split.md (firm c044 — variant-conditional-single-loop root)
  - book/src/L3/apply_linop.md (the by-design no-L2-entry / no-L3-L2-theme operator — the remaining 1-of-18)
  - scaffolding/open-questions.md slugs l3-l2-rotation-theme-coverage-gap / remaining-substantive-l3-l2-rotations-chebyshev-eigsolve / l3-l2-substantive-erasure-scope-taxonomy
integrated_at: 2026-06-01T143625Z
integration_commit: PLACEHOLDER_SHA
integration_notes: "cycle-045 batch integration; D3 layer-intro-author (SOLE count-owner) — consolidated L3-L2/index §Working-Notes tally (firm 15→17, l3-l2-rotation-theme-coverage-gap 15-of-18→17-of-18 + denominator-reconciliation prose apply_linop=by-design-no-L2) + the now-COMPLETE four-root erasure-scope taxonomy (unconditional-single-loop / variant-conditional-single-loop / unconditional-nested-double-loop / opaque-library, distinguished from the 13 thin -body-identity themes); D2 Change-5 supersede honored; applied clean; see reports/2026-06-01T143625Z-integrator-finalize-cycle-45/CYCLE.md + cycle-045 STAGING row 3."
---

# CYCLE: L3>L2 index — erasure-scope taxonomy + consolidated count tally

## Summary

Two jobs, both confined to `book/src/L3-L2/index.md` prose (no chapter bodies, no D1/D2 rows/bullets,
no SUMMARY):

**(a) Erasure-scope taxonomy.** I author a §Erasure-scope-taxonomy note (folded into the §Working-Notes
overlay) naming the **substantive-L3>L2 erasure-scope axis**, now with its **four** populated roots —
the taxonomy the c044 D3 OQ + the c045 D1/D2 OQs requested. The four roots are distinguished from the
thin `-body-identity` identity-themes (the BLAS-1 / standalone-floor cohort, 13 of the now-17 firm
themes), which carry **no** substantive erasure (the body IS the identity, the loop view is absent):

| Erasure-scope root | Substantive theme | Cycle | Loop structure |
|---|---|---|---|
| **unconditional-single-loop** | `ksp-solve-outer-driver` | c021 | single outer fold (the loop IS the operator) |
| **variant-conditional-single-loop** | `orthogonalize-variant-split` | c044 | single MGS `j`-loop (one variant branch) |
| **unconditional-nested-double-loop** | `chebyshev-nested-recurrence` | c045 D2 | nested inner `k`-recurrence + outer `pc_it` sweep |
| **opaque-library (Palace authors no loop)** | `eigsolve-opaque-eigen-iteration` | c045 D1 | no Palace recurrence — SLEPc `EPSSolve` / ARPACK `naupd` RCI |

**(b) Consolidated count tally (sole count-owner).** After D1 + D2 land, the L3>L2 firm count goes
**15 → 17** and `l3-l2-rotation-theme-coverage-gap` advances **15-of-18 → 17-of-18**. I verified the
post-cohort on-disk row enumeration: the §"Theme list" table holds **15** firm rows pre-D1/D2; D1 adds
`eigsolve-opaque-eigen-iteration`, D2 adds `chebyshev-nested-recurrence` → **17** firm rows. I am the
sole writer of this tally; D1/D2 added only their own rows/bullets (dual-registration) and explicitly
deferred the tally to D3 (D1 OQ#3; D2 Change 5 NOTE + caveats).

**Denominator reconciliation (verified + noted for the meta-phase).** The denominator **18** is the
count of **L3 operators** (`ls book/src/L3/*.md` minus `index` = 18). The coverage-gap counts how many
of those 18 have their L3>L2 rotation **captured** (a dedicated theme file, OR — for `apply_linop` — a
sufficient by-design in-line annotation). After D1/D2 the remaining **1-of-18 is `apply_linop`**, which
lowers **directly L3 → L1** with **no interposed L2 entry and no L3-L2 theme** by design (the cycle-010
audit verdict "CONFIRMED-NOT-NEEDED-WITH-CAVEAT": L2 hosts no standalone `apply_linop` entry, so there
is no L2 RHS for an L3>L2 theme to target). So 17-of-18 is **effectively complete** — the last 1 is a
by-design no-theme case, not an open gap. I record this in the tally and flag it for the meta-phase to
ratify (the gap may be re-denominated 17-of-17-applicable, or `apply_linop` formally annotated as the
non-applicable 18th).

## D1/D2 coordination note (overlapping edit on `index.md:62-63`)

Both D1 and D2 propose edits that touch the §Working-Notes block I own:

- **D2 Change 5** replaces one sentence inside the cohort-growth bullet (`index.md:62`) AND its NOTE
  explicitly defers the firm-count numeral + the gap numeral to D3 ("This report does NOT edit the
  `firm 15 → 16` numeral").
- **D1 OQ#3** explicitly says D1 did NOT touch the tally and that D3 owns it, and asks D3 to fold the
  third (opaque-library) corner into the §Working-Notes taxonomy paragraph + the "substantive
  candidates" line.

To avoid a conflicting double-edit on the same lines, **my Edit 1 replaces the ENTIRE current
cohort-growth bullet (`index.md:62`) and Edit 2 replaces the ENTIRE current taxonomy paragraph
(`index.md:63`)** with the consolidated post-cohort versions. This **supersedes D2 Change 5** (its
single-sentence replacement of line 62) — the integrator should apply **my** Edit 1/Edit 2 as the
authoritative version of lines 62–63 and **skip D2 Change 5** (the line it targets is wholly rewritten
by Edit 1; applying both would conflict). D1/D2's table rows, §Vocabulary-cohort substantive bullets,
SUMMARY rows, and chapter-body re-anchors are untouched by me and stand as-is.

## Proposed changes

### Edit 1 — consolidated count tally (replaces the c044 cohort-growth §Working-Notes bullet, `index.md:62`; supersedes D2 Change 5)

```edit:book/src/L3-L2/index.md
[old]: - **Cohort growth + coverage-gap progress (firm 14 → 15; `l3-l2-rotation-theme-coverage-gap` 14-of-18 → 15-of-18).** Cycle-044 landed the **second substantive / non-identity** L3>L2 theme, `orthogonalize-variant-split` — and the **first substantive theme for a `partial-obstruction` operator**. Unlike the cycle-041/042/043 `-body-identity` cohort (clean identity-lowerings on leaves / gates / fused composites), this theme carries real rotation content: the L3 `orthogonalize` variant-split iteration-rotation form (CGS/CGS2 straight-line global statements + the explicit MGS `jloop` tail recursion carrying the named `sequential-obstruction`) lowers to the L2 `project ▷ subtract` per-variant-sequenced composition, with the MGS obstruction **erased to its L2 shadow** (the column-order-non-commutativity non-law + the `m×1` residual axis). The erasure is **variant-conditional** (MGS branch only; CGS/CGS2 clean lifts; body identity-in-form) — the variant-split counterpart of the **unconditional** `ksp-solve-outer-driver`. This advances `l3-l2-rotation-theme-coverage-gap` from **14 of 18 → 15 of 18**. The remaining gap is `chebyshev` (rotation in-line at `chebyshev-iteration` already; obstruction unconditional), `eigsolve` (`partial-obstruction`, opaque-library-owned loop — substantive), and any leaf residual. (Prior: cycle-043 landed four `-body-identity` themes — the leaf-cohort batch `axpy`/`axpby`/`axpbypcz`/`normalize` — taking the gap 10-of-18 → 14-of-18; cycle-042 landed the five fork-INDEPENDENT standalone-floor themes, 5-of-18 → 10-of-18; cycle-041 landed the first three BLAS-1-leaf themes, 2-of-18 → 5-of-18.)
[new]: - **Cohort growth + coverage-gap progress (firm 15 → 17; `l3-l2-rotation-theme-coverage-gap` 15-of-18 → 17-of-18) — cycle-045 two-landing cohort.** Cycle-045 landed the **third and fourth substantive / non-identity** L3>L2 themes in one wave, completing the substantive-cohort across **all four erasure-scope roots** (see §Erasure-scope taxonomy below): `chebyshev-nested-recurrence` (D2 cross-cutter, verdict LAND — the **unconditional-nested-double-loop** root) and `eigsolve-opaque-eigen-iteration` (D1 abstractor — the **opaque-library** root). Both carry real rotation content (iteration view erased; first-class L3 `sequential-obstruction`(s) shadowed to L2-vocabulary non-laws), unlike the 13 thin `-body-identity` identity-themes (clean identity-lowerings on leaves / gates / fused composites, no substantive erasure). This advances `l3-l2-rotation-theme-coverage-gap` from **15 of 18 → 17 of 18**. **Denominator reconciliation (flagged for the meta-phase):** the **18** is the count of L3 operators (`book/src/L3/*.md` minus `index`); the coverage-gap counts how many have their L3>L2 rotation captured. After this cohort the remaining **1-of-18 is `apply_linop`**, which lowers **directly L3 → L1** with **no interposed L2 entry and no L3-L2 theme by design** (the cycle-010 audit "CONFIRMED-NOT-NEEDED-WITH-CAVEAT" — L2 hosts no standalone `apply_linop` entry, so there is no L2 RHS for an L3>L2 theme; see `L3/apply_linop` §"Lowers to"). 17-of-18 is therefore **effectively complete** — the residual 1 is a by-design no-theme case, not an open gap; the meta-phase may re-denominate to 17-of-17-applicable or formally annotate `apply_linop` as the non-applicable 18th. (Prior: cycle-044 landed `orthogonalize-variant-split` 14-of-18 → 15-of-18; cycle-043 landed four `-body-identity` themes — `axpy`/`axpby`/`axpbypcz`/`normalize` — 10-of-18 → 14-of-18; cycle-042 landed the five fork-INDEPENDENT standalone-floor themes, 5-of-18 → 10-of-18; cycle-041 landed the first three BLAS-1-leaf themes, 2-of-18 → 5-of-18.)
```

### Edit 2 — Erasure-scope taxonomy note (replaces the c044 "First substantive theme for a `partial-obstruction` operator" §Working-Notes paragraph, `index.md:63`)

```edit:book/src/L3-L2/index.md
[old]: - **First substantive theme for a `partial-obstruction` operator (taxonomy note).** `orthogonalize-variant-split` is the first L3>L2 theme whose LHS is a `partial-obstruction` L3 entry, and the first to record a **variant-conditional** substantive erasure (the substantive content is confined to one variant branch — the MGS arm — while the CGS/CGS2 arms lift cleanly on both sides of the hop and the per-step body is identity-in-form across all arms). The two substantive L3>L2 themes now map two erasure scopes: **unconditional** (`ksp-solve-outer-driver` — the whole operator IS the loop) and **variant-conditional** (`orthogonalize-variant-split` — one branch of a variant-split partial-obstruction). The remaining substantive candidates are `eigsolve` (`partial-obstruction`, opaque-library-owned — a third erasure-root) and `chebyshev` (in-line already). Flagged for the meta-phase: whether the "substantive erasure scope" axis (unconditional / variant-conditional / opaque-library) wants a named taxonomy across the substantive L3>L2 cohort.
[new]: - **Erasure-scope taxonomy (the substantive-L3>L2 axis — now four populated roots; the c044/c045 meta-phase-flagged taxonomy).** The L3>L2 themes split into two kinds. **Thin `-body-identity` identity-themes** (13 of the 17 firm themes — the BLAS-1 leaves, the fork-INDEPENDENT standalone floors, the fused composites, the constructed-operator gates) carry **no substantive erasure**: the body IS the identity, there is no iteration view to erase (the operator is L3-native by signature shape — no element loop, no `sequential-obstruction`), so the rotation is a value-thread-isomorphic body-identity (the leaf/gate analogue of `krylov-step-body-identity` with no wrapper to rotate). **Substantive / non-identity themes** (4 of the 17) carry real rotation content: the L3 form names a first-class `sequential-obstruction` (and, where Palace authors the loop, renders an explicit `iterate_while`-family tail recursion), and the L3>L2 hop **erases the iteration view** — the obstruction survives only as L2-vocabulary non-laws. The four substantive themes populate the four corners of the **erasure-scope axis**:
- **unconditional-single-loop** — `ksp-solve-outer-driver` (cycle-021): the whole operator IS a Palace-authored outer fold; the L3 explicit `iterate_while_L3` tail recursion + named outer-loop obstruction lowers to the L2 outer-driver-by-role composition (obstruction shadowed to the fold non-laws). The erasure holds for every parameter value.
- **variant-conditional-single-loop** — `orthogonalize-variant-split` (cycle-044): the substantive erasure is confined to **one variant branch** (the MGS arm's single `j`-loop); the CGS/CGS2 arms lift cleanly on both sides, and the per-step body is identity-in-form across all arms. The MGS obstruction (numerical-stability-rooted) shadows to the column-order-non-commutativity non-law + the `m×1` collective-shape residual axis.
- **unconditional-nested-double-loop** — `chebyshev-nested-recurrence` (cycle-045 D2): a **nested double loop** (the inner degree-`order` `k`-recurrence + the outer `pc_it` Richardson sweep, both explicit `iterate_while_pure_L3` tail recursions carrying first-class obstructions) lowers to the L2 `sweep`-iterated-by-role composition; the two obstructions shadow to the step-reordering / `pc_it`-commutativity / polynomial-expansion non-laws. **Unconditional** (like `ksp-solve-outer-driver`), but a nested-double-loop structure neither single-loop sibling exhibits; the inner obstruction shares `orthogonalize`'s numerical-stability root.
- **opaque-library (Palace authors no loop)** — `eigsolve-opaque-eigen-iteration` (cycle-045 D1): the loop lives **entirely outside Palace** (SLEPc folds inside `EPSSolve(eps)`; ARPACK inside the `naupd` RCI driver), so the L3 form **cannot even render the loop** as a tail recursion — it attaches an obstruction **marker** at the library boundary, and the L2 hop **erases the marker** (it shadows to the "Opening of the eigen-iteration fold at L2" + "Fold-merge / restart associativity" non-laws). The distinguishing structural fact: a substantive iteration-rotation erasure can arise from a loop Palace never authored. Obstruction sub-kind `opaque-library-ownership` (per CLAUDE.md) — never re-promotable.

  The axis is now **complete and in live use across four firm themes** (the meta-phase-flagged candidate `l3-l2-substantive-erasure-scope-taxonomy` has all corners populated). RECOMMEND the meta-phase ratify the taxonomy name and decide whether it warrants a `book/src/concepts/<slug>.md` page (the four roots cut across the substantive L3>L2 cohort and the `sequential-obstruction` / `tensor-field-lift` concept pages — a `concepts/erasure-scope.md` would give the axis a cross-cutting home). See §Open questions.
```

## Supporting evidence

**On-disk row enumeration (post-cohort, the count-ownership verification):**

- Pre-D1/D2 the §"Theme list" table holds **15** firm rows (`grep -c '^| \[\`' book/src/L3-L2/index.md` = 15):
  `krylov-step-body-identity`, `dot-body-identity`, `nrm2-body-identity`, `ksp-solve-outer-driver`,
  `scal-body-identity`, `axpy-body-identity`, `axpby-body-identity`, `axpbypcz-body-identity`,
  `assemble-diagonal-body-identity`, `jacobi-smoother-body-identity`, `reciprocal-body-identity`,
  `elementwise-product-body-identity`, `normalize-body-identity`, `divfree-projector-body-identity`,
  `orthogonalize-variant-split`.
- D1 adds `eigsolve-opaque-eigen-iteration` (its CYCLE.md proposed-changes block, table-row edit).
- D2 adds `chebyshev-nested-recurrence` (its CYCLE.md Change 3, table-row edit).
- Post-cohort = **17** firm rows. The tally `firm 15 → 17` is the post-cohort total, counting BOTH
  cohort landings (the count-ownership discipline — I count all of the cohort's landings, not just one).

**Erasure-scope roots (the four substantive themes, on-disk-verified):**

- `book/src/L3-L2/ksp-solve-outer-driver.md` (firm c021) — unconditional-single-loop.
- `book/src/L3-L2/orthogonalize-variant-split.md` (firm c044) — variant-conditional-single-loop.
- `book/src/L3-L2/chebyshev-nested-recurrence.md` (firm c045 D2, landing this cycle) — unconditional-nested-double-loop.
- `book/src/L3-L2/eigsolve-opaque-eigen-iteration.md` (firm c045 D1, landing this cycle) — opaque-library.

**Identity-vs-substantive split (the 13 thin themes):** the §"Vocabulary cohort" already groups the
identity-themes (BLAS-1-leaf `dot`/`nrm2`/`scal`/`axpy`/`axpby`/`axpbypcz` + standalone-floor
`assemble-diagonal`/`jacobi-smoother`/`divfree-projector`/`reciprocal`/`elementwise-product`/`normalize`
+ the kernel-body `krylov-step-body-identity`); the taxonomy note formalizes that they are the
**no-substantive-erasure** kind, against the 4 substantive themes.

**Denominator (the 18 + the by-design residual):**

- `ls book/src/L3/*.md` minus `index` = **18** L3 operators: `apply_linop`, `assemble-diagonal`,
  `axpby`, `axpbypcz`, `axpy`, `chebyshev`, `divfree-projector`, `dot`, `eigsolve`,
  `elementwise_product`, `jacobi-smoother`, `krylov-step`, `ksp_solve`, `normalize`, `nrm2`,
  `orthogonalize`, `reciprocal`, `scal`.
- 17 of those have a captured L3>L2 rotation (a theme file) after the cohort lands. The 18th,
  `apply_linop`, has **no L2 entry and no L3-L2 theme by design** — `book/src/L3/apply_linop.md`
  §"Lowers to" (line 146): "L3 `apply_linop` lowers to L1 `apply_linop` directly — **no interposed L2
  entry, no L3-L2 theme**" (the cycle-010 audit L2 verdict). It is the remaining 1-of-18, but a
  by-design non-applicable case, not an open gap.

## Open questions / caveats

1. **Taxonomy ratification + `concepts/` page decision (for the meta-phase) — RECOMMEND ratify.** The
   erasure-scope axis (`l3-l2-substantive-erasure-scope-taxonomy`) is now complete with all four corners
   populated across four firm themes (unconditional-single-loop / variant-conditional-single-loop /
   unconditional-nested-double-loop / opaque-library). Both D1 (OQ#1) and D2 (forward OQ) recommend the
   meta-phase name the taxonomy. I concur and add: **decide whether it warrants a `book/src/concepts/`
   page.** The axis cuts across the substantive L3>L2 cohort and is adjacent to the
   `concepts/sequential-obstruction.md` + `concepts/tensor-field-lift.md` pages; a `concepts/erasure-scope.md`
   (one-line semantics per root + forward-citations to the four themes + the list of operators that
   exhibit each root) would give the axis a cross-cutting home consistent with the broadened concept-page
   scope. As a layer-intro-author concept-page is one-per-invocation and out of this dispatch's scope, I
   only flag it; a follow-up layer-intro-author dispatch would author it if the meta-phase says go.

2. **Coverage-gap denominator reconciliation (for the meta-phase).** The `l3-l2-rotation-theme-coverage-gap`
   reaches **17-of-18** this cycle. The remaining 1 (`apply_linop`) is a **by-design no-L2-entry /
   no-L3-L2-theme** case (cycle-010 audit "CONFIRMED-NOT-NEEDED-WITH-CAVEAT"), not an open gap. The gap
   metric is therefore effectively closed. RECOMMEND the meta-phase either (a) re-denominate to
   **17-of-17-applicable** (excluding `apply_linop` as having no L2 RHS), or (b) keep 17-of-18 with
   `apply_linop` formally annotated as the non-applicable 18th. I left the artifact at **17-of-18** with
   the reconciliation recorded in-prose (Edit 1), deferring the re-denomination decision to the
   meta-phase (it is a methodology-metric call, not a layer-shell call). The OQ
   `remaining-substantive-l3-l2-rotations-chebyshev-eigsolve` (opened c044 D3) is now fully resolved —
   both its named candidates landed this cycle; recommend closing it on integration.

3. **D2 Change 5 is superseded by my Edit 1 (integrator note).** D2's Change 5 replaces one sentence
   inside `index.md:62`; my Edit 1 replaces the whole bullet. The integrator should apply **my Edit 1**
   as the authoritative version of line 62 and **skip D2 Change 5** (applying both conflicts — they edit
   the same line). D2's other changes (the new file, SUMMARY row, table row, cohort bullet, and the
   `L3/chebyshev` + `L3/index` re-anchors) are untouched by me and stand. Likewise D1's row/bullet/SUMMARY
   + `L3/eigsolve` re-anchors stand untouched.

4. **No L0 citations in this dispatch (no citecheck pass needed).** My two edits are structural / tally
   prose citing only book-internal slugs (`L3/apply_linop`, the four theme slugs, the L3-operator list);
   they carry no `path:lo-hi` source ranges, so the `citecheck` self-verification pass (role-spec
   §Discipline) is a no-op here. The L0 anchors for the four substantive themes were self-verified by D1
   (eigsolve, with a caught `arpack.cpp` `[DRIFT +2]` re-anchor) and D2 (chebyshev, codemap `read_range`)
   in their own dispatches; I do not re-emit them.
