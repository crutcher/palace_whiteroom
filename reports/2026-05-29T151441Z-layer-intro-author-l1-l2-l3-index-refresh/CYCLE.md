---
agent: layer-intro-author
invoked_at: 2026-05-29T151441Z
scope: L1 / L2 / L3 Part-overview prose refresh (batch-6 completions — NEP-interior atom cohort + eigsolve L1→L2→L3 chain)
status: integrated
integrated_at: 2026-05-29T17:15:00Z
integration_commit: 210e622
integration_notes: "cycle-025 finalize (first primary cycle of meta-batch-7). 4 surgical navigational-prose edits: L1/index.md:108 (cycle-009 Working-Notes bullet → past-tense historical marker + eigsolve-is-firm); L2/index.md:81 (repairer-folded single 1:1 [old]→[new] preserving the cycle-020 L3 driver/kernel complementarity bullet + appended consolidated batch-6 Working-Notes bullet); L3/index.md:15 (Sequential-obstructions overlay → names all three firm shapes: firm-driver ksp_solve + chebyshev/eigsolve partial-obstructions) + :43 (two Working-Notes bullets, count 9 firm + 2 partial-obstruction). No operator semantics authored; high→low structure preserved. 2 OQs RESOLVED on open-questions.md:322 (eigsolve-firm-stale-cycle-009-narrative-bullet ENACTED; lu-solve-layer-intro-count-refresh already-satisfied-on-disk) — the :322 line is now retirement-ready pending the 3rd-clause l1-index-fifth-motif confirm. retroactive-budget 0; clean build (all cross-refs live links to extant entries)."
---

# CYCLE: L1 / L2 / L3 index (Part-overview) cohort refresh

## Summary

Navigational prose refresh of the three layer-index Part overviews to reflect the batch-6 (cycles 022/023/024) completions: the NEP-interior atom cohort (L1 firm 13→19) and the eigsolve L1→L2→L3 prerequisite chain (L1-firm c022 → L2-firm c023 → L3 `partial-obstruction` c024). No operator semantics authored; high→low document structure preserved; all dep-map rows / §Vocabulary-cohort splits already carry the firm apparatus on disk (surveyed per role discipline — see Supporting evidence). The refresh is light and bounded:

- **`book/src/L1/index.md`** — the §Semantics six-motif framing, the `Firm (19)` cohort split, the §Vocabulary `eigsolve` bullet, and the dep-map are **already current on disk** (refreshed during the cycle-024 landings). The single residual staleness is the **cycle-009 Working-Notes narrative bullet** (line 108), which still opens in present tense ("the `eigsolve` rough-in lands…") and closes "status is rough-in pending…". The adjacent cycle-022 bullet (line 109) already records the retirement and calls line 108 "kept as history," so the surgical fix is a one-clause historical marker on the cycle-009 bullet's opening so a reader does not mistake the rough-in framing for current. Routes OQ `eigsolve-firm-stale-cycle-009-narrative-bullet-routes-to-layer-intro-author`. (OQ `lu-solve-layer-intro-count-refresh-and-fifth-motif` is already satisfied on disk — motif 6 `lu_solve` and the count are present; see Open questions.)
- **`book/src/L2/index.md`** — the `eigsolve` (firm c023) and `deflate` (partly-constructive c022) dep-map rows + the §Vocabulary-cohort split + the count (8 firm + 1 partly-constructive + 1 stub) are **already current on disk**. The only navigational gap is the **Working Notes**: no batch-6 narrative bullet records the `gram` / `deflate` (c022) or `eigsolve` (c023) landings. One consolidated batch-6 Working-Notes bullet added.
- **`book/src/L3/index.md`** — the `eigsolve` dep-map row (`partial-obstruction`, c024) is **already current on disk**. Two navigational gaps: (1) the §Semantics "Sequential obstructions" overlay bullet names only `krylov-step`'s outer loop — it should name the two `partial-obstruction` operators (`chebyshev`, `eigsolve`) and the firm-driver `ksp_solve`; (2) the Working Notes have no bullet for the cycle-020 `ksp_solve` firm landing or the cycle-024 `eigsolve` `partial-obstruction` landing, and no count line. Refresh the §Semantics bullet + add two Working-Notes bullets carrying the count line (9 firm + 2 partial-obstruction).

## Proposed changes

```edit:book/src/L1/index.md
[old]: - **Cycle-009**: the `eigsolve` rough-in lands as the **second constructed-operator gate at L1**, composing against `ksp_solve` (the inner linear solver is itself a constructed-operator absorption). Per the cycle-008 OQ `eigsolve-l1-operator-rough-in-candidate` and the pre-check verdict (no dedicated `test-eigensolver.cpp`; narrow indirect coverage via `test-boundarymodeoperator.cpp` only), status is rough-in pending either expanded test coverage or additional literature anchoring. The rough-in introduces the **partial-convergence** semantic (`PartialConverged` status) as a distinguishing feature relative to `ksp_solve`'s soft-fail — the eigenvalue iteration can converge `0 < K < K_max` pairs without being an outright failure, a case `ksp_solve` has no analog for.
[new]: - **Cycle-009** *(historical — superseded by the cycle-022 bullet below; `eigsolve` is **firm** at L1, not rough-in)*: the `eigsolve` rough-in landed as the **second constructed-operator gate at L1**, composing against `ksp_solve` (the inner linear solver is itself a constructed-operator absorption). Per the cycle-008 OQ `eigsolve-l1-operator-rough-in-candidate` and the pre-check verdict (no dedicated `test-eigensolver.cpp`; narrow indirect coverage via `test-boundarymodeoperator.cpp` only), status *at the time* was rough-in pending either expanded test coverage or additional literature anchoring — that gating framing was **retired cycle-022** (see next bullet). The rough-in introduced the **partial-convergence** semantic (`PartialConverged` status) as a distinguishing feature relative to `ksp_solve`'s soft-fail — the eigenvalue iteration can converge `0 < K < K_max` pairs without being an outright failure, a case `ksp_solve` has no analog for; that distinguishing semantic survives the promotion.
```

```edit:book/src/L2/index.md
[old]: - **L3 driver/kernel complementarity** (cycle-020 harvester flag; the L3 entry [`L3/ksp_solve`](../L3/ksp_solve.md) landed firm cycle-020): the L2 [`ksp_solve`](./ksp_solve.md) outer-driver wrap and the L3 [`krylov-step`](../L3/krylov-step.md) kernel form a driver/kernel pair across the L2↔L3 boundary, mirroring the L2-kernel / L4-driver pair (`krylov-step` at L2, `iterate_while` at L4). The L3 `ksp_solve` entry re-erases the iteration view that L2 erases — the non-identity hop captured by the firm L3>L2 theme [`ksp-solve-outer-driver`](../L3-L2/ksp-solve-outer-driver.md).
[new]: - **L3 driver/kernel complementarity** (cycle-020 harvester flag; the L3 entry [`L3/ksp_solve`](../L3/ksp_solve.md) landed firm cycle-020): the L2 [`ksp_solve`](./ksp_solve.md) outer-driver wrap and the L3 [`krylov-step`](../L3/krylov-step.md) kernel form a driver/kernel pair across the L2↔L3 boundary, mirroring the L2-kernel / L4-driver pair (`krylov-step` at L2, `iterate_while` at L4). The L3 `ksp_solve` entry re-erases the iteration view that L2 erases — the non-identity hop captured by the firm L3>L2 theme [`ksp-solve-outer-driver`](../L3-L2/ksp-solve-outer-driver.md).
- **Batch-6 cohort growth (Gram/deflate fold-lift + eigsolve chain-step-2)**: three landings raised the firm cohort 6→8 + added the first L2 `partly-constructive` tier. (1) [`gram`](./gram.md) (firm cycle-022, promoted from rough-in) — the **all-pairs `inner_product` fold → `Matrix[k,k]`**, the matrix-valued lift of the `inner_product` scalar fold (firm-on-positive-structure: all-pairs syntactic-identity laws on the positive Gram-build site `nleps.cpp:524-531`); Hermitian + PSD, consumed by `deflate`. (2) [`deflate`](./deflate.md) (the **first L2 `partly-constructive` entry**, cycle-022) — the oblique/Galerkin complementary projector `I − X(XᴴX)⁻¹Xᴴ`, the `coords ▷ schur-solve ▷ back-project` named composition over `gram` + [`lu_solve`](../L1/lu_solve.md) + `linear_combination` + `dot`; the **Schur-form pipeline is firm** (positive `deflated_solve` site `nleps.cpp:505-537`), the **bare-Galerkin core** (`S = I`) is the constructive sub-part (literature + negative anchor, promotion-gated on a positive Palace Galerkin-deflation site). The cycle-023 [`nleps_deflated_solve`](../L1/nleps_deflated_solve.md) landing **confirmed (did not change)** the partly-constructive status — the bare `(XᴴX)⁻¹` Galerkin core appears only Schur-wrapped, never standalone. Over-unification guard: `deflate` is NOT `orthogonalize` (`orthogonalize` = `deflate` at `gram = I`; the `(XᴴX)⁻¹` Gram solve is load-bearing). (3) [`eigsolve`](./eigsolve.md) (firm cycle-023 wave-1) — **chain-step-2** of the eigsolve prerequisite chain (L1-firm c022 → **this L2 entry** → L3-backfill); the **named shift-invert spectral-transform composition** `apply_shift_invert = apply_linop(M) ▷ ksp_solve((K − σM)⁻¹)`, the first L2 named composition whose direct constituent is itself a constructed-solver composition. A **partial-opening** named composition: the per-step *body* is Palace-authored and opened, but the eigen-iteration *fold* is opaque-library-owned (SLEPc `EPSSolve` / ARPACK RCI) and named by role only — the inverse decomposition from [`ksp_solve`](./ksp_solve.md) (whose fold IS opened). This is the load-bearing reason the L3 backfill landed `partial-obstruction` (cycle-024; chain-step-3 complete — see [`L3/eigsolve`](../L3/eigsolve.md)).
```

```edit:book/src/L3/index.md
[old]: - **Sequential obstructions** — explicit markers where global form is unavailable, with reason (the outer loop folding `krylov-step` does not lift; recorded per `concepts/sequential-obstruction.md`).
[new]: - **Sequential obstructions** — explicit markers where global form is unavailable, with reason (recorded per `concepts/sequential-obstruction.md`). Three firm shapes coexist at L3: (a) the outer loop folding `krylov-step` does not lift, but the firm driver [`ksp_solve`](./ksp_solve.md) still renders it as an explicit value-threaded tail recursion (firm cycle-020); (b) [`chebyshev`](./chebyshev.md) (`partial-obstruction`) — body lifts, but the inner `k`-recurrence + outer `pc_it` Richardson sweep are sequential obstructions rooted in **numerical stability**; (c) [`eigsolve`](./eigsolve.md) (`partial-obstruction`) — body lifts, but the eigen-iteration loop is a sequential obstruction rooted in **opaque-library-ownership** (Palace authors no loop — SLEPc `EPSSolve` / ARPACK RCI own it). (b) and (c) are the two `partial-obstruction` operators; they share the body-lifts-loop-doesn't shape but differ in the *reason* the loop does not lift.
```

```edit:book/src/L3/index.md
[old]: - **First firm L3 partial-obstruction landed cycle-013**: `chebyshev` (the fixed-degree polynomial smoother). Unlike the BLAS-1 cohort (clean identity-lowerings) and `krylov-step` (firm body, non-lifting predicate-driven fold), `chebyshev` is the canonical **partial-obstruction** case — the per-inner-step body lifts to a global tensor-field expression, but the inner `k`-recurrence (degree `order`) and the outer `pc_it` Richardson sweep are witnessed sequential obstructions with a cited non-removability reason (Phillips & Fischer 2022 §2: recurrence form chosen for numerical stability over explicit polynomial expansion). The body is identity-in-form to the cycle-012 firm L2 `chebyshev-iteration` and, transitively (L3>L2 ∘ L2>L1 identity), to the L1 `chebyshev-smoother` — annotated in-line, no `L3-L2/` or `L3-L1/` directory (cycle-012 meta-phase non-adjacent-identity convention). It is inner-product-free (no `dot`/`nrm2`) — the structural distinction from `krylov-step`. Landing this row + the L4 `chebyshev` row unblocked full reduction of the Phase-1 slice `book/src/spec/slices/chebyshev.md` (removed cycle-015; material now authoritative across the firm `L1`–`L4` chebyshev cohort).
[new]: - **First firm L3 partial-obstruction landed cycle-013**: `chebyshev` (the fixed-degree polynomial smoother). Unlike the BLAS-1 cohort (clean identity-lowerings) and `krylov-step` (firm body, non-lifting predicate-driven fold), `chebyshev` is the canonical **partial-obstruction** case — the per-inner-step body lifts to a global tensor-field expression, but the inner `k`-recurrence (degree `order`) and the outer `pc_it` Richardson sweep are witnessed sequential obstructions with a cited non-removability reason (Phillips & Fischer 2022 §2: recurrence form chosen for numerical stability over explicit polynomial expansion). The body is identity-in-form to the cycle-012 firm L2 `chebyshev-iteration` and, transitively (L3>L2 ∘ L2>L1 identity), to the L1 `chebyshev-smoother` — annotated in-line, no `L3-L2/` or `L3-L1/` directory (cycle-012 meta-phase non-adjacent-identity convention). It is inner-product-free (no `dot`/`nrm2`) — the structural distinction from `krylov-step`. Landing this row + the L4 `chebyshev` row unblocked full reduction of the Phase-1 slice `book/src/spec/slices/chebyshev.md` (removed cycle-015; material now authoritative across the firm `L1`–`L4` chebyshev cohort).
[new]: - **Ninth firm L3 operator landed cycle-020 (the driver half)**: [`ksp_solve`](./ksp_solve.md) (the value-threaded outer-driver fold `iterate_while_L3 (krylov-step op)`). It is the **driver-half layer-coherence complement** of the firm `krylov-step` kernel: where `krylov-step` is the firm per-step kernel, `ksp_solve` renders the outer convergence-test loop as an explicit value-threaded tail recursion (the outer loop is a `sequential-obstruction`, but Palace authors it, so it renders rather than going opaque — the contrast with `eigsolve` below). The L3↔L2 rotation is **substantive (NOT identity-in-form)**: the L3 explicit fold consolidates into the L2 outer-driver-by-role, captured by the firm L3>L2 theme [`ksp-solve-outer-driver`](../L3-L2/ksp-solve-outer-driver.md) (cycle-021). Closes the `ksp_solve` portion of OQ `l3-vocabulary-inventory-gap`.
[new]: - **Second firm L3 partial-obstruction landed cycle-024 — the canonical opaque-library case**: [`eigsolve`](./eigsolve.md) (the **final step of the eigsolve prerequisite chain** L1-firm c022 → L2-firm c023 → **this L3 entry**). Like `chebyshev` it is body-lifts-loop-doesn't, but the loop's non-lift reason is **distinct**: where `chebyshev`'s obstruction is **numerical stability** (a Palace-authored recurrence chosen over explicit expansion), `eigsolve`'s is **opaque-library-ownership** — Palace authors **no** eigen-iteration loop at all (no kernel/driver pair analogous to `(krylov-step, ksp_solve)`); the loop is entirely inside SLEPc `EPSSolve` (`palace/linalg/slepc.cpp:694`) / ARPACK `naupd` RCI (`palace/linalg/arpack.cpp:318`), with Palace supplying only a per-step matvec callback. The per-step body — the named shift-invert composition `apply_shift_invert = apply_linop(op.operand) ▷ ksp_solve(op.inv)` — lifts whole-tensor and is identity-in-form to the firm L2 [`eigsolve`](../L2/eigsolve.md) body (in-line annotation per the cycle-012 non-adjacent-identity convention, no `L3-L2/` theme file). This is the cycle-021 OQ `l3-eigsolve-linear-evp-has-no-krylov-step-kernel-analog` prediction enacted. **L3 firm-operator count now 9 firm + 2 `partial-obstruction`** (firm: `krylov-step` c010, `apply_linop` + `axpy` + `axpby` + `axpbypcz` + `dot` + `nrm2` + `scal` c011, `ksp_solve` c020; partial-obstruction: `chebyshev` c013, `eigsolve` c024).
```

## Supporting evidence

**On-disk firmness survey** (per role Discipline — surveyed the explicit `## Status` / frontmatter `firmness:` lines, not the cycle record):

- `book/src/L1/eigsolve.md:165-167` — `## Status` declares `firm` (cycle-022, route-(b) law-confidence re-eval; promoted from `rough-in (test-coverage-bounded)`). Matches the L1 index dep-map row (line 79) and §Vocabulary `eigsolve` bullet (line 41) — both already current.
- `book/src/L2/eigsolve.md:153-155` — `## Status` declares `firm` (cycle-023 wave-1; chain-step-2). Matches the L2 index dep-map row (line 61) + §Vocabulary cohort (line 38-area, under Firm).
- `book/src/L2/deflate.md:362-364` — `## Status` declares `partly-constructive` (firm Schur-form pipeline + constructive bare-Galerkin core). Matches the L2 index dep-map row (line 60) + §Vocabulary Partly-constructive cohort (line 42).
- `book/src/L3/eigsolve.md:4` — frontmatter `firmness: partial-obstruction`. Matches the L3 index dep-map row (line 31).

**Batch-6 counts** (from `scaffolding/cycle-record.jsonl`, cycle-024 integration `counts_after`): `L1_firm: 19`, `L1_roughin_test_coverage_bounded: 2`, `L1_roughin_obstruction: 6`, `nep_interior_atoms_firm: 5`, `l1_semantic_motif_taxonomy: 6`; `L2_firm: 8`, `L2_partly_constructive: 1`, `L2_stub: 1`; `L3_firm: 9`, `L3_partial_obstruction: 2`. All three scope-brief count assertions confirmed exactly.

**L0 citations referenced in the new L3 prose** (citecheck-verified, `tools/citecheck/citecheck.py --anchor`):
- `palace/linalg/slepc.cpp:694` anchor `EPSSolve` — `[ok]` (anchor at line 694).
- `palace/linalg/arpack.cpp:318` anchor `naupd` — `[ok]` (anchor at line 318).
Both are pre-existing citations already in the L3 `eigsolve` dep-map row (line 31); re-verified per role discipline before re-stating them in the Working-Notes bullet. No new L0 citations introduced.

**Cross-references forwarded** (all to firm/extant on-disk entries; live links only — no plain-text-defer needed):
- L2: `[gram](./gram.md)`, `[deflate](./deflate.md)`, `[eigsolve](./eigsolve.md)`, `[lu_solve](../L1/lu_solve.md)`, `[nleps_deflated_solve](../L1/nleps_deflated_solve.md)`, `[ksp_solve](./ksp_solve.md)` — all exist.
- L3: `[ksp_solve](./ksp_solve.md)`, `[chebyshev](./chebyshev.md)`, `[eigsolve](./eigsolve.md)`, `[ksp-solve-outer-driver](../L3-L2/ksp-solve-outer-driver.md)`, `[eigsolve](../L2/eigsolve.md)` — all exist (the L3>L2 outer-driver theme is firm cycle-021).

## Open questions / caveats

- **OQ `lu-solve-layer-intro-count-refresh-and-fifth-motif` appears already satisfied on disk.** The L1 §Semantics overlay already carries **six** motifs (motif 5 = operator-introspection `assemble-diagonal`; motif 6 = coordinate-space dense direct algebra `lu_solve`), the `Firm (19)` cohort split, and the dep-map `lu_solve` row — all present from the cycle-019/022/024 landings. The OQ phrasing ("fifth motif") predates the motif-6 addition; no L1 §Semantics edit was needed beyond the cycle-009 historical-marker fix. Flagging for OQ closure rather than re-opening. (The OQ may have been migrated to the plan as a layer-intro task that the cycle-024 landings already absorbed; the integrator should confirm and close.)
- **The three index files were already substantially refreshed in-place during the cycle-024 landings** (dep-map rows, §Vocabulary cohorts, count splits all current). This dispatch's residual is purely the **navigational narrative** (Working-Notes bullets + the L3 §Semantics overlay bullet + the L1 cycle-009 historical marker) — the parts that lag because they are append-only narrative, not the structural tables the integrators touch per landing. No structural / count contradiction between on-disk state and the cycle-record was found.
- **No L4 `eigsolve` exists** (the `L4/eigsolve.md` chapter is unauthored; the L1 entry anchors the `solve-monad` L4 surface but it is not written). The L3 `eigsolve` lifts from L2 only. This is correctly recorded in the L3 `eigsolve` dep-map row and frontmatter; not a gap this refresh introduces, but noting it as the next natural eigsolve-chain extension (L4 surface) for the planner.
- **Did not touch** the wave-1/dispatch-2 theme surfaces (`L1-L0/`, `L2-L1/`, `concepts/`) per scope; refreshed only the three index files. The two `nleps_deflated_solve` / NEP-atom L2-L1 themes and any concept pages are out of this dispatch's scope.
