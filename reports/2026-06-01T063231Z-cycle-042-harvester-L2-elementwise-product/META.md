---
verifies: ../CYCLE.md
critiqued_at: 2026-06-01T071500Z
critic_version: 1
checks:
  citation-validity: pass
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: pass
  cross-reference-integrity: warning
  edge-label-fidelity: pass
  plan-kind-consistency: pass
  skill-uptake-survey: pass
repaired_at: 2026-06-01T073000Z
repairer_version: 1
repairs:
  citation-validity: not-needed
  surface-or-evidence: not-needed
  rotation-quality: not-needed
  variant-axis-coverage: not-needed
  cross-reference-integrity: repaired
  edge-label-fidelity: not-needed
  plan-kind-consistency: not-needed
  skill-uptake-survey: not-needed
overall_status: ready
follow_up_agent: null
---

# META: verification of "Formalize elementwise_product at L2"

## Critique

### Checks run

**citation-validity — pass.** `citecheck --scan` on the report returns `15 ok, 0 failing`. All load-bearing L0 pinpoints were re-verified mechanically with `--anchor` and confirmed in-range, then meaning-read against the source: `palace/linalg/operator.cpp:486 'Y[i] = D[i] * X[i]'` (real Mult, `[ok]`); `:504-505 'YR[i] = DR[i] * XR[i]'` (complex straight FMAs, `[ok]`); `:564-565 'YR[i] = DR[i] * XR[i] + DI[i] * XI[i]'` (conjugate variant, `[ok]`); `:478-507` and `:545-568` range anchors resolve in-range; `operator.hpp:279 'Mult(x, y)'` (real `MultTranspose` alias, `[ok]`). I read `operator.cpp:476-570` directly: the real multiply at :486, complex straight product at :504-505 (`YR = DR·XR − DI·XI`, `YI = DI·XR + DR·XI`), and the conjugate `MultHermitianTranspose` at :564-565 (`YR = DR·XR + DI·XI`, `YI = −DI·XR + DR·XI`, the two sign flips realising `d̄ ⊙ x`) all match the report's §Semantics and §Evidence verbatim — no +1 drift on any of the three canonical sites. The report's `MultHermitianTranspose` range `:545-568` starts one line before the `template <>` at :546, but the range encloses the full function body and the anchor resolves at :548, in-range (tolerance, not a drift). The L1 source-of-truth anchors (`:43` Algebraic laws, `:41` scal, `:99` firm) and concept-page anchors (`:17` dinv, `:19` L3) all resolve. No `verified_against:` block in this report, so that sub-check no-ops. The ten laws + non-law set reproduced in the report match `book/src/L1/elementwise_product.md:47-56` exactly, including the conjugate-variant non-commutativity caveat.

**surface-or-evidence — pass.** Not a refinement of an existing operator; this is a net-new firm L2 floor entry (`new file book/src/L2/elementwise_product.md` + one dep-map row in `L2/index.md` + one SUMMARY row). New-entry shape, not a rotation_claim backfill against existing surface, so the refinement-surface gate does not bind. The dep-map row addition to `L2/index.md` is an additive registration of the new chapter, not a modification of an existing operator's surface.

**rotation-quality — pass.** The report asserts the L2↔L1 rotation is **identity-in-form** and argues it explicitly: `elementwise_product` is a leaf binary field operation with no multi-operation kernel fusion to unfold (the L0 `forall_switch` per-element multiply is already the single-pass unfolded form). This is the correct disposition for a thin floor — it is NOT a rename-only defect because the report does not *claim* a compaction rotation and then fail to deliver one; it correctly classifies the edge as a no-op fusion rotation and justifies the no-op (contrast `dot`, which defers a family of fused reduction kernels to its `inner_product` fold-parent — `elementwise_product` has no fold-parent and so no fusion content to defer). The substantive rotation in the chain is correctly located at L1>L0 (`reciprocal-elementwise-product-mutation-rotation` sub-pattern B). Identity-in-form floor under the cycle-009 **Identity-lowerings still require both L levels** invariant is a sanctioned, precedented disposition (cycle-041 `dot`/`nrm2`/`scal`).

**variant-axis-coverage — pass.** Two variant axes are enumerated and each combination is covered: (1) element-type (real | complex), collapsed to one parameterised operator with both L0 specialisations cited; (2) conjugation sub-axis on the complex side (straight `Mult` / conjugate `MultHermitianTranspose`), with the real-side absence justified by the `MultTranspose`→`Mult` alias at `operator.hpp:279` (verified). Non-axes (constant-folding on `a`/`b`; operator-action-vs-free-binary) are explicitly recorded as non-axes with rationale. No hidden branch: the report explicitly states the L0 has no constant-folding branches and the conjugation is a template-instantiation axis, not a value branch on `imag(a)==0`. The conjugate variant's law impact (breaks commutativity, changes law 10) is correctly carried.

**cross-reference-integrity — warning.** All `[link]` targets resolve on disk (`L1/`, `L3/`, `concepts/`, `L2/dot.md`, `L2/scal.md`, `L2/inner_product.md`, `L2/linear_combination.md`, `L2/chebyshev-iteration.md`, `L2/apply_linop` via `L1/apply_linop.md`, `L1-L0/reciprocal-elementwise-product-mutation-rotation.md`), the new `L2/elementwise_product.md` is the only "missing" path (it is the file being created), and the SUMMARY.md registration is a clean non-duplicate insertion into the L2 Part block. Fence parity is clean: 6 fences / 3 balanced blocks, and `## Status` (line 402) plus the full firm apparatus (Signature :129, Algebraic laws :210, Evidence :459) all sit INSIDE the `edit:book/src/L2/elementwise_product.md` fence (42–539) — no fence-truncation defect; the post-fence "Operator content" / "Supporting evidence" sections are correctly framed as a summary of the in-fence body, not the body. **The warning is a consistency defect, not a build-break:** the existing firm `book/src/L3/elementwise_product.md` *positively asserts, three times*, that NO L2 entry exists — frontmatter line 6 ("no L3-L2/L3-L1 theme"), §Downward line 28 ("**The L2 layer hosts no standalone `elementwise_product` entry**"; "the L3>L1 hop is therefore direct"), and §Lowers-to line 149 ("**no interposed L2 entry**"; "The L2 layer does not host an `elementwise_product` entry … the L3>L1 hop is therefore direct"). Once this dispatch lands the L2 floor, those three statements in the L3 entry become factually false, and the report proposes NO edit to `L3/elementwise_product.md` to reconcile them. The report is aware of the tension (CYCLE.md §Evidence line 476: "the L3 entry's 'Lowers to' prose notes the no-interposed-L2-entry situation this dispatch now closes") but "closes" it only in the report's own prose, not by editing the now-stale L3 entry. This is the load-bearing issue (Issue 1). Note: the cycle-041 L3 siblings (`dot`/`nrm2`/`scal`) did NOT have this defect — they frame their lowering as "there is no L3-L1 lowering *theme*" (still true after their L2 floors landed) and never positively claimed "no L2 entry exists"; `L3/elementwise_product.md` is the outlier that made the stronger, now-falsified claim.

**edge-label-fidelity — pass.** The entry's declared edges are L2↔L1 (lowers-to / lifts-from) and the L3>L2/L3>L1 references. The §Lowers to prose discusses the L2→L1 identity-in-form hop; §Lifts from discusses L1→L2; the L3 references are framed as "the L3 leaf this floor goes under." Each prose section discusses the exact edge its heading names. No L_{n+1}→L_n mislabel.

**plan-kind-consistency — pass.** Declared kind is `firm` L2 floor operator. Content shape matches: canonical signature anchored to positive source, ten algebraic laws (all syntactic identities on the positive `BaseDiagonalOperator::Mult` multiply lambdas), complete variant-axis enumeration, no rough-in placeholders, no unfilled TODOs. The `firm`-on-thin-floor justification is sound and precedented: the §Status invokes the **firm-on-positive-structure** escape (laws are syntactic identities on fully-specified positive source; the absent `test-elementwise-product` does not gate firm — the `apply_linop` situation, not the `eigsolve`-convergence situation), which is the cycle-021-codified escape exactly matching this case. The NO-fold-parent claim is internally consistent and correctly reasoned: `elementwise_product` is `(Tensor[N], Tensor[N]) -> Tensor[N]` with no fold skeleton; the `scal(α,x) = elementwise_product(broadcast(α,N), x)` relation is correctly characterised as the INVERSE subsumption (elementwise_product *generalises* scal), a sibling-subsumption identity (law 7), NOT a fold membership — so fork-independence from the batch-12 leaf-vs-fold adjudication is justified, not asserted. Count-ownership is correctly observed: the report appends only its own dep-map row + chapter body + SUMMARY row and explicitly does NOT touch the L2/index running tally (deferred to D11 per dispatch), avoiding the `parallel-blind-shared-index-count-divergence` friction.

**skill-uptake-survey — pass.** The report references `tools/citecheck/citecheck.py --anchor` for L0 self-verification (the `verify-citation-range` mechanical realization) and names the cycle-041 `dot`/`scal` floor templates as the structural precedent it copied — appropriate skill/precedent uptake for a thin-floor harvest. No missing-skill signal.

### Issues found

1. **(cross-reference-integrity, medium) Stale now-false "no L2 entry" assertions left in `book/src/L3/elementwise_product.md`.** The report creates `book/src/L2/elementwise_product.md` but does not edit the existing firm L3 entry, which positively asserts the absence of exactly this entry in three places: frontmatter line 6 ("no L3-L2/L3-L1 theme — see Lowers-to"), §Downward line 28 ("**The L2 layer hosts no standalone `elementwise_product` entry** … the L3>L1 hop is therefore direct"), and §Lowers-to line 149 ("**no interposed L2 entry, no `L3-L2`/`L3-L1` theme file** … The L2 layer does not host an `elementwise_product` entry … the L3>L1 hop is therefore direct"). After this dispatch lands, those statements are factually wrong: an L2 entry now exists and the L3>L1 hop is no longer "direct" (it passes through the present L2 floor). The report's CYCLE.md §Evidence line 476 acknowledges this ("the no-interposed-L2-entry situation this dispatch now closes") but proposes no reconciling edit. Repair candidate: add an `edit:book/src/L3/elementwise_product.md` block (or a follow-up) updating lines 6, 28, 149 to "L3>L1 lowers via the present adjacent L2 floor `L2/elementwise_product.md` (identity-in-form), per **Identity-lowerings still require both L levels**" — mirroring how the cycle-041 L3 `dot`/`nrm2`/`scal` entries are phrased (which avoided this defect by referring to the absent L3-L1 *theme*, not an absent L2 entry). Severity is medium: not a `linkcheck2` build-break (no dead link is introduced), but it leaves an internal factual contradiction between two firm chapters in the same family.

2. **(citation-validity, informational — no action) `MultHermitianTranspose` range starts one line early.** The §Evidence range `palace/linalg/operator.cpp:545-568` opens at :545, one line before the `template <>` at :546 (the function body runs :546-567, closing brace :567). The anchor resolves at :548 and the range fully encloses the function, so this is within tolerance and NOT a drift — flagged only for completeness. No repair needed.

3. **(non-defect, surfaced-by-design) Underscore-vs-hyphen filename wrinkle.** Per the dispatch focus note, the operator chapter is `elementwise_product.md` (underscore, matching firm L1/L3) while the concept page is `elementwise-product.md` (hyphen). The report surfaces this correctly in Open question 1 as a meta-phase convention signal, not a defect — every link in the entry points at the correctly-spelled on-disk target. This is consistent with the existing L1/L3 family spelling and is NOT a cross-reference failure. No action for this dispatch; recorded as the dispatch instructed.

---

## Repair

### Fixes attempted

- **Finding (Issue 1, cross-reference-integrity, medium, load-bearing)**: The firm `book/src/L3/elementwise_product.md` positively asserts in three places that NO L2 `elementwise_product` entry exists and that "the L3>L1 hop is therefore direct" (frontmatter line 6, §Downward line 28, §Lowers-to line 149). This D3 dispatch creates exactly that L2 floor (`book/src/L2/elementwise_product.md`), so the two firm chapters would contradict after landing.
  - **Decision**: repaired.
  - **Action**: Appended three `edit:book/src/L3/elementwise_product.md` proposed-changes blocks to the CYCLE.md (`## Proposed changes`, immediately after the `edit:book/src/SUMMARY.md` block), each a `<<<<<<< SEARCH / ======= / >>>>>>> REPLACE` conflict-marker edit (the repo's established in-place-edit-to-existing-firm-file form, matching the cycle-036 floquet re-anchor precedent). The three sites are re-pointed to reflect the now-present adjacent L2 floor:
    - **Site 1 — frontmatter `lowers_to` (line 6)**: "no L3-L2/L3-L1 theme" → lowers through `book/src/L2/elementwise_product.md` "via the `elementwise_product-body-identity` L3>L2 theme."
    - **Site 2 — §Downward (line 28)**: heading reframed "Downward to L1" → "Downward to L2"; "lowers to L1 … directly, with no interposed L2 entry and no L3-L2/L3-L1 theme file … the L3>L1 hop is therefore direct" → "lowers to the present adjacent L2 floor … via the `elementwise_product-body-identity` L3>L2 theme, and onward to L1 … the L3>L2 hop is identity-in-form through the adjacent floor, per **Identity-lowerings still require both L levels**, mirroring the cycle-041 `dot`/`nrm2`/`scal` L3>L2 floor discipline."
    - **Site 3 — §Lowers-to (lines 149-151)**: "lowers to L1 … no interposed L2 entry, no L3-L2/L3-L1 theme file … the L3>L1 hop is therefore direct" → "lowers to the present adjacent L2 floor … via the `elementwise_product-body-identity` L3>L2 theme, and onward to L1 … so the L3>L2 hop passes through the adjacent floor rather than skipping a layer to L1, per **Identity-lowerings still require both L levels**"; the second paragraph's "no `book/src/L3-L1/` directory exists … captures the identity rotation in-line" reframed to "the L3>L2 identity rotation is captured by the adjacent-edge `elementwise_product-body-identity` L3>L2 theme (per the cycle-012 per-adjacent-edge lowering-directory convention)"; closing "L3>L1 hop is … a layer-coherence rotation" → "the L3>L2 and L2>L1 hops are … layer-coherence rotations."
  - **Scope check**: mechanical cross-file consistency reconciliation only. The replacement text preserves all existing algebraic content, citations, and the identity-in-form characterization verbatim; only the now-false "no L2 entry / direct L3>L1 hop" framing is corrected to "lowers via the adjacent L2 floor / `elementwise_product-body-identity` L3>L2 theme." No new claims authored. SEARCH text verified verbatim against the on-disk file (lines 6, 28, 149-151) before authoring. This is the producer's own-dispatch blind spot (one-operator-per-dispatch discipline prevents the harvester from editing the sibling L3 entry), squarely within repair authority (trivial cross-reference / internal-consistency fix).

### Unrepairable findings

None. Issues 2 and 3 are critic-marked informational / non-defect with no repair required.

### Note for the integrator (not a defect; scope-bounded out of this repair)

- The §Evidence citation-gloss at `book/src/L3/elementwise_product.md:166` describes the `scal` sibling's historical "no-L2-entry / no-theme-file rotation shape." Since the cycle-041 `scal` L2 floor now exists, that gloss is also mildly stale, but it is (a) a description of a *sibling's* framing inside a citation gloss, not a positive assertion about `elementwise_product`'s own L2 absence, and (b) a fourth site neither the critic enumerated nor the dispatch named (the dispatch was explicit: reconcile the three named sites only, "Do NOT do substantive content authoring beyond the three-site reconciliation"). Left untouched to stay within named-three-site repair scope; flagged here for a future lifter/cross-cutter pass on the `scal`/`dot`/`nrm2` L3 evidence glosses if the meta-phase wants the floor-cohort sibling references uniformly updated. Not blocking — no contradiction or dead link is introduced by leaving it.

## Suggested resolution

`ready`. The single load-bearing finding (Issue 1) is mechanically reconciled in-place via three SEARCH/REPLACE edit blocks; all checks are now `pass` (from critic) or `repaired`. Integrator notes: (1) the L3 reconciliation depends on the co-landing L3>L2 `elementwise_product-body-identity` theme (D10) being created — the edits *name* that theme by slug; if D10 lands in the same cycle the references resolve to it as the adjacent-edge home for the identity rotation (the theme name is used as a plain prose reference, not a live `[link]`, so it does not gate `linkcheck2` even if D10 lands a cycle later). (2) The three `edit:book/src/L3/elementwise_product.md` SEARCH blocks are verbatim-matched to the current on-disk file; they will apply cleanly unless another report in the same cycle also edits those exact lines (none does — this is the only reconciliation of the L3 entry's lowering prose). (3) Optional sibling-gloss cleanup at L3:166 is flagged above as non-blocking follow-up, not a gate.
