---
agent: cycle-planner
invoked_at: 2026-06-09T004208Z
scope: cycle-149 dispatch plan
status: pending
---

# Cycle 149 dispatch plan

## Goals selected this cycle

Cycle-149 is the MIDDLE (2/3) of meta-batch-49 under the active **(A) maintenance-floor** posture. The c148 opener's once-per-batch full-hygiene sweep surfaced a genuine STANDING-invariant residue (CLAUDE.md §Methodology-invariants "FINALIZATION") — the batch-47 FINALIZATION campaign reached the per-operator chapters but missed a cohort of chapters/indexes that still carry inline `cycle-NNN`/`batch-NNN`/`wave-N` process-accounting attributions. The parent fully characterized the cohort: **17 files, ~39 attributions** (carve-outs `methodology/` + `meta-reviews/` excluded). The c148 precedent established the disposition — **discharge it, don't park it.** This cycle dispatches a **grouped FINALIZATION de-bulk wave** (5 parallel dispatches) applying the `finalization-debulk` skill, strictly preserving every citation + every `## Status`/dep-map rank-carrier token (the sole-rank-carrier subtlety) + the graded-stack baseline (must HOLD EXACTLY — a pure prose/table-cell de-bulk moves no node/edge/rank/status). No substantive frontier (none exists — in-scope steady-state, 7th consecutive complete batch); this IS legitimate maintenance.

## Deliverable-presence verification

All 17 cohort files verified present on disk with the parent-stated residue counts (`grep -cE 'cycle-[0-9]|c0[0-9][0-9]|batch-[0-9]|wave-[0-9]'`):

```
OK   13  book/src/L3-L2/ksp-solve-outer-driver.md
OK    3  book/src/L4-L3/krylov-step-typed-wrapper-dissolution.md
OK    3  book/src/L3-L2/fold-solve-time-step-body.md
OK    2  book/src/L4-L3/iterate-while-with-prev-dissolution.md
OK    2  book/src/L4-L3/iterate-while-dissolution.md
OK    2  book/src/L4-L3/gmres-inner-loop-iterate-while-migration.md
OK    2  book/src/L4/fold_solve.md
OK    2  book/src/concepts/constructed-operators.md
OK    1  book/src/synthesis/data-algebra.md
OK    1  book/src/L4/frequency_sweep.md
OK    1  book/src/L3/fold_solve.md
OK    1  book/src/L2/reciprocal.md
OK    1  book/src/L2-L1/inner-product-fold-specialization.md
OK    1  book/src/L2/index.md
OK    1  book/src/feature/infrastructure.md
OK    1  book/src/feature/index.md
OK    1  book/src/concepts/variant-absorption.md
```

- **Open by construction:** this is a fresh de-bulk slate surfaced by the c148 hygiene sweep (OQ `sibling-layer-index-finalization-debulk-residue-check`, broadened by the parent's full-cohort grep). Each file genuinely carries the residue (counts above are live HEAD `cf52a8f`). The c148 L1/index.md de-bulk did NOT touch any of these 17 — disjoint. No prior-cycle history to be stale against.
- **Skill present:** `skills/finalization-debulk/SKILL.md` exists (the c148 de-bulk's working precedent).
- **Structural-block check:** NONE. This is the standing FINALIZATION invariant (CLAUDE.md §Methodology-invariants "FINALIZATION") actively in force; the residue is exactly what the invariant says producers must not carry. Discharging it is in-discipline maintenance, not a forced frontier.
- **Note — OQ scope vs cohort scope:** OQ `sibling-layer-index-finalization-debulk-residue-check` named L2/L3/L0 index.md as candidates; the live grep shows only `L2/index.md` carries residue (1 tag), while `L3/index.md`/`L0/index.md` carry 0 (already clean) — so they are correctly absent from the cohort. The parent's full-corpus grep is the authoritative slate.

## Dispatches

Each dispatch applies the `finalization-debulk` skill with the load-bearing safety invariants stated identically in every scope:
> **SAFETY INVARIANTS (every dispatch):** preserve EVERY citation byte-for-byte (no `(file:lo-hi)` removed/altered); preserve EVERY `## Status` leading token AND every dep-map/TABLE cell status token (the `## Status`-as-sole-rank-carrier subtlety — these no-frontmatter-rank chapters carry rank ONLY in the prose/cell token; de-bulk must NOT strip it); move NO node / edge / rank / status / semantics; the graded-stack baseline must HOLD EXACTLY (`files 392, typed 331, untyped 61, roots 45, rank_violations 0, unresolved_depends_on_targets 0, promotion_frontier 11, detritus 123, true_detritus 51`); preserve EVERY link slug + anchor (de-bulk NEVER renames — cross-refs stay valid); KEEP load-bearing structural / coupling content (lift "compare to X" to a `## Relationship`/`## Structural fact` section if it currently rides on process prose) and strip ONLY process framing; for borderline worked-example refs, REPHRASE to a static descriptor rather than deleting the example. Apply directly to disk per the FINALIZATION static-state-surface convention; integrator-per-report STAGES + gates (does not re-apply).

- **D1** — `abstractor` — **scope:** FINALIZATION de-bulk of the HEAVY `book/src/L3-L2/ksp-solve-outer-driver.md` (13 attributions; its own dispatch given the extra care). Strip: the `firm cycle-NNN wave-N` process parentheticals inside the dep-map / §Evidence cite lines (leave the bare citation + the firm STATUS token; the `firm` classification stays, the `cycle-021 wave-1` provenance goes); the **"the cycle-020 critic's mild tension"** framing prose in §"Why the labour divides" / §"Kernel-identity + driver-non-identity" — **LIFT the load-bearing structural claim** ("kernel body collapses identity-in-form, driver loop is the genuine iteration-rotation; the two classifications are about disjoint subjects" — this is a `## Structural fact`/`## Relationship`, KEEP it) while stripping the "the critic noticed / is not a tension / is ratified" process meta-narrative; the **"Authored cycle-021 wave-2 (abstractor), enacting *Identity-lowerings still require both L levels*"** provenance footer — DELETE it entirely (it is doubly stale: process attribution AND it cites a RETIRED/SUPERSEDED methodology directive). PRESERVE every L0/L2/L3 citation and the `firm` `## Status` token. **rationale:** the largest single residue concentration + the only file citing a retired directive; needs structural-content care so the kernel-identity/driver-non-identity contrast survives as a static fact.

- **D2** — `abstractor` — **scope:** FINALIZATION de-bulk of the 4-file `L4-L3` dissolution/migration cohort: `book/src/L4-L3/krylov-step-typed-wrapper-dissolution.md` (3), `book/src/L4-L3/iterate-while-with-prev-dissolution.md` (2), `book/src/L4-L3/iterate-while-dissolution.md` (2), `book/src/L4-L3/gmres-inner-loop-iterate-while-migration.md` (2). Strip inline `cycle-NNN`/`firm cycle-NNN`/`batch-N`/`wave-N` attributions from prose + cite lines, preserving the bare citations + status tokens + every cross-link slug/anchor (these are L4-L3 lowering themes that other cohort files reference — links must stay valid). **rationale:** one coherent kind/directory cohort (L4>L3 iteration-rotation dissolutions); uniform de-bulk shape.

- **D3** — `abstractor` — **scope:** FINALIZATION de-bulk of the `fold_solve` family (3 files): `book/src/L4/fold_solve.md` (2), `book/src/L3/fold_solve.md` (1), `book/src/L3-L2/fold-solve-time-step-body.md` (3). In `L4/fold_solve.md`: **KEEP the load-bearing "ONE combinator" classification decision + the recorded `schedule-source` variant axis** (these are static design facts), strip ONLY the "batch-18 size-judgment" forward-process speculation and the OQ-deferral framing (`per OQ fold-solve-greedy-schedule-source-generalization … batch-18+`) — restate as a static "the state-generated form is a recorded variant rendered through the §3.7 carry; a dedicated chapter is not warranted" without the cycle/OQ/forward-deferral scaffolding. Strip the singletons' inline attributions in `L3/fold_solve.md` + the 3 in the L3-L2 body theme, preserving citations + status tokens. **rationale:** one operator-family cohort spanning L4/L3/L3-L2; D3 owns the `fold_solve` classification-prose care so the ONE-combinator decision survives.

- **D4** — `harvester` — **scope:** FINALIZATION de-bulk of the 3 L2/L1-region operator/theme singletons: `book/src/L2/reciprocal.md` (1), `book/src/L2-L1/inner-product-fold-specialization.md` (1), `book/src/L4/frequency_sweep.md` (1). Strip the single inline `cycle-NNN`/`batch-N`/`wave-N` attribution in each, preserving the citation + `## Status` token + dep-map. **rationale:** low-residue operator/theme singletons clustered by lower-layer region; mechanical de-bulk, one agent.

- **D5** — `layer-intro-author` — **scope:** FINALIZATION de-bulk of the 6 index/concept/feature/synthesis-shell singletons: `book/src/L2/index.md` (1), `book/src/concepts/constructed-operators.md` (2, BORDERLINE), `book/src/concepts/variant-absorption.md` (1), `book/src/synthesis/data-algebra.md` (1), `book/src/feature/infrastructure.md` (1), `book/src/feature/index.md` (1). Per-file care: (i) `L2/index.md` — **KEEP "design-final / NOT a fold member"** (load-bearing design fact in the `normalize` dep-map cell), strip ONLY "the batch-12 leaf-vs-fold fork (`dot-l2-leaf-floor-vs-fold-only-design`)" — restate as "design-final: a composite with no fold-parent cannot be re-anchored into one" without the batch/OQ-slug scaffolding; PRESERVE the cell `firm` status token + the `Tensor[(S: ...)]`/`Tensor[$S]` named-shape-group content (inline-code-fenced, step-5c-safe). (ii) `concepts/constructed-operators.md` — BORDERLINE worked-example refs ("cycle-7 / cycle-9 shape", "cycle-7's `side` case is the worked counter-example"): **REPHRASE to a static descriptor** (e.g. "the un-constructed two-variant shape", "the orthogonal-`side`-variant counter-example") rather than deleting the worked example — the example is pedagogically load-bearing, only its cycle-tag identity is process residue. (iii) the remaining 4 singletons — strip the lone attribution, preserve content/links. **rationale:** `layer-intro-author` owns index/concepts/feature/synthesis-shell files (the navigational-container + concept-library + feature/synthesis kinds); D5 carries the two care-cases (L2/index design-final + constructed-operators worked-example rephrase).

## Overlap analysis

The 5 dispatches operate on **disjoint file sets** (no file appears in two dispatches):
- D1: `{L3-L2/ksp-solve-outer-driver.md}`
- D2: `{L4-L3/krylov-step-typed-wrapper-dissolution, iterate-while-with-prev-dissolution, iterate-while-dissolution, gmres-inner-loop-iterate-while-migration}`
- D3: `{L4/fold_solve, L3/fold_solve, L3-L2/fold-solve-time-step-body}`
- D4: `{L2/reciprocal, L2-L1/inner-product-fold-specialization, L4/frequency_sweep}`
- D5: `{L2/index, concepts/constructed-operators, concepts/variant-absorption, synthesis/data-algebra, feature/infrastructure, feature/index}`

**No two dispatches touch the same file region or rewrite the same theme body → no genuine overlap → fully PARALLEL.**

**Shared-cross-reference / slug coordination:** cohort files DO cross-link (e.g. `L3/fold_solve.md`→`L4/frequency_sweep.md`; D1's ksp-solve-outer-driver references D2's `krylov-step-typed-wrapper-dissolution` + `iterate_while`; D3's fold_solve family is referenced from D1/D2). **No coordination required:** finalization de-bulk NEVER renames a slug or anchor (it strips only process prose), so every existing cross-link remains valid regardless of which dispatch de-bulks which file. There is NO new-slug forward-reference and NO rename premise to coordinate (the `cross-report-forward-reference-slug-divergence` / `coordinated-cross-report-rename` guards do not apply). 

**No shared consolidated index tally:** none of the 5 dispatches writes a cohort-spanning running count (the `parallel-blind-shared-index-count-divergence` guard does not apply — `L2/index.md` is touched only by D5, and its single residue tag is a dep-map cell de-bulk, not a tally). No dual-registration partition needed (no producer adds rows/bullets — this is strip-only de-bulk of existing content).

## Sequencing schedule

**Single wave — all 5 dispatches parallel (D1, D2, D3, D4, D5).** Disjoint file sets, no rename/slug coordination, no shared tally. Then the standard tail: 5 critics (parallel) → repairers as needed → `integrator-per-report` ×5 (serial; STAGE + gate each, do NOT re-apply — the de-bulk is applied directly to disk per the FINALIZATION convention, c148 precedent) → ONE `integrator-finalize` (rebuild book + step-5b two-invariant tripwire + step-5c KaTeX + step-5d frontmatter-leak gates + commit + push).

**Per-cycle tripwire expectation:** baseline must HOLD EXACTLY (`files 392 … rank_violations 0 … detritus 123 / true_detritus 51`) — a pure prose/table-cell de-bulk moves no node/edge/rank. Build EXIT 0 expected; step-5c vacuously safe for any file whose only `$`-content is inline-code-fenced named-shape-group notation (untouched); step-5d clean (no frontmatter introduced). Any baseline movement is a defect signal for the finalize to catch.

## Open questions / caveats

- **Cohort completeness:** the parent's grep is the authoritative slate (17 files); I confirmed each count live at HEAD `cf52a8f`. If the c149 finalize's post-de-bulk grep shows a residual tag in any cohort file (a missed instance the de-bulk skill left), it is an in-cycle repairer fix, not a new cycle. After this wave the residue cohort should be 17→0 (modulo the `methodology/`+`meta-reviews/` carve-outs which are NOT de-bulked).
- **OQ disposition for batch-49 meta:** OQ `sibling-layer-index-finalization-debulk-residue-check` is DISCHARGED-BY-SUPERSESSION here — the parent broadened it from the L2/L3/L0-index question to the full-corpus cohort, and this wave discharges that cohort. The batch-49 meta (after c150) should CLOSE-RESOLVE it (and note L3/L0 index.md were already clean — the residue was concentrated in the 17 files swept here). Flagging for the meta-phase unify pass; I do not edit the OQ ledger (meta-phase authority).
- **D1 retired-directive deletion:** `ksp-solve-outer-driver.md` cites the SUPERSEDED "Identity-lowerings still require both L levels" directive in its provenance footer. D1 DELETES the whole provenance footer (it is process accounting), which incidentally removes the stale-directive reference — correct outcome, no separate action needed. The theme's substantive content (the substantive non-identity iteration-rotation between two firm endpoints) is independent of that retired-directive framing and is preserved.
- **No substantive frontier this cycle** — consistent with the active (A) maintenance posture and the 7th-consecutive-in-scope-complete-batch texture. This de-bulk wave is hygiene discharge of a standing-invariant residue, not new vocabulary; no consumer-gated front is re-opened (RE4 / sharding solve-generalization / eigsolve-impl all stay consumer-gated).
