---
agent: lifter
invoked_at: 2026-05-28T223135Z
scope: L3>L2 theme re-anchor — krylov-step-body-identity (own dangling cg.md provenance pointer sweep)
status: integrated
integrated_at: 2026-05-28T230323Z
integration_commit: 80db8d6
integration_notes: |
  Applied cycle-017 (per-report position 3). 3 dangling cg.md provenance pointers
  in the FIRM L3>L2 theme book/src/L3-L2/krylov-step-body-identity.md re-anchored
  to the in-document terminal firm home (this theme's own §Verified-against
  bullet 1, the verbatim cycle-002 Claim-2 quote); dead slice ranges demoted to
  parenthetical provenance; theme stays firm. TERMINATES the second of the
  two-hop-to-dangle residuals the cycle-016 L4 sweep flagged — the krylov-step
  cg.md re-anchor chain (L4 016, L2 016, L3-L2 017) is now FULLY TERMINATED.
  OQ l3-l2-body-identity-cg-md-citation-sweep resolved.
inputs:
  - book/src/L3-L2/krylov-step-body-identity.md
  - book/src/spec/slices/cg.md (reduced 165-line stub; confirms cg.md:341-362 / :351-362 dangle)
  - book/src/L4-L3/krylov-step-typed-wrapper-dissolution.md (upstream theme — designates THIS theme's §Verified-against as the lifted-in home)
  - book/src/L3/krylov-step.md (cross-chain consensus at line 188 — names this entry's §Verified-against as the terminal home preserving the verbatim Claim-2 quote)
  - book/src/L2/krylov-step.md (lifted-evidence annotation-convention precedent — §Evidence lines 138/146)
---

# CYCLE: Re-anchor krylov-step-body-identity

## Summary

The firm L3>L2 theme `book/src/L3-L2/krylov-step-body-identity.md` cites the reduced-slice range `cg.md:341-362` (and the sub-range `cg.md:351-362`) at three sites — §Applicability-conditions condition 3 (line 97), §Justification-kind empirical-match (line 107), and §Verified-against bullet 1 (line 125) — as if the slice still held the verbatim cycle-002 combinator-miner Claim-2 ("the L2→L3 rotation on the step body is the identity in form"). The `cg.md` slice was reduced to a 165-line stub (cycle-010); grepping the stub for `identity in form` / `step body lifts` / lines 341/351/360 returns nothing but the unrelated L0-supersession line — the verbatim claim is no longer in the slice, so all three ranges dangle. This is exactly the circularity OQ `l3-l2-body-identity-cg-md-citation-sweep` flagged: the upstream L4>L3 theme (line 204) and the L3 entry (line 188) both already designate **this theme's own §Verified-against** as the *lifted-in terminal firm home* that preserves the verbatim Claim-2 quote — but this theme's §Verified-against line still pointed at the dead slice range as a live source, making the supposed terminal home itself dangle. The re-anchor declares this entry's §Verified-against as the terminal home (it physically carries the verbatim quote) and demotes `cg.md:341-362` / `:351-362` to parenthetical "original pre-reduction range, lifted here per the cycle-009 corpus reduction" provenance annotations, applying the cycle-013/014/015/016 lifted-evidence annotation convention verbatim (mirroring L2:138/146 and L3:188). Pure re-anchoring; no body-shape, signature, or decomposition change. The co-pointer `gmres.md:459-471` resolves in the live 671-line GMRES slice (the `inner_loop` body) and is left untouched; the `arnoldi_step.md:178-213` / `:185-188` co-citations resolve in the live 302-line slice and are likewise untouched.

## Proposed changes

```edit:book/src/L3-L2/krylov-step-body-identity.md
[old]: 3. **Every primitive in the let-chain is in the firm L1 vocabulary, and each primitive is L3-native by its signature shape.** The seven L1 primitives used (`apply_linop`, `axpy`, `axpby`, `axpbypcz`, `dot`, `nrm2`, `scal`) are firm post-cycle-004; each operates on whole-tensor inputs with no element-loop exposed at L2. This is what makes the L3>L2 rotation identity-in-form rather than requiring a decomposition step (each L1 primitive is *also* L3-native because its signature has no per-element loop visible). The cycle-002 combinator-miner argument (`cg.md:351-362`) is the original observation; the cycle-006 audit re-confirmed it. Currently satisfied.
[new]: 3. **Every primitive in the let-chain is in the firm L1 vocabulary, and each primitive is L3-native by its signature shape.** The seven L1 primitives used (`apply_linop`, `axpy`, `axpby`, `axpbypcz`, `dot`, `nrm2`, `scal`) are firm post-cycle-004; each operates on whole-tensor inputs with no element-loop exposed at L2. This is what makes the L3>L2 rotation identity-in-form rather than requiring a decomposition step (each L1 primitive is *also* L3-native because its signature has no per-element loop visible). The cycle-002 combinator-miner argument (preserved verbatim in this theme's §Verified-against bullet 1 — the terminal firm home; original pre-reduction slice range `cg.md:351-362`, lifted here per the cycle-009 corpus reduction) is the original observation; the cycle-006 audit re-confirmed it. Currently satisfied.
```

```edit:book/src/L3-L2/krylov-step-body-identity.md
[old]: **Empirical-match (dominant)**: the cycle-002 combinator-miner claim — that L2's primitive vocabulary is already L3-native by inspection of the slice corpus's L2 and L3 prose — is the original empirical evidence. The cycle-006 audit (`reports/2026-05-27T081913Z-abstractor-L4-L3-krylov-step-lowering/CYCLE.md` §"Audit of cycle-002 identity-in-form claim") re-read the cited evidence ranges (`cg.md:341-362`, `arnoldi_step.md:178-213`) and confirmed the assertion. The L2>L3 lift of every primitive call in the kernel body is the identity rotation; therefore the L3>L2 lowering on the body — running the same edge in the opposite direction — is also the identity rotation. **This is the strongest justification kind for the theme**: the assertion is observational about the slice corpus's evidence, not derivational from algebraic laws or reduction rules.
[new]: **Empirical-match (dominant)**: the cycle-002 combinator-miner claim — that L2's primitive vocabulary is already L3-native by inspection of the slice corpus's L2 and L3 prose — is the original empirical evidence. The cycle-006 audit (`reports/2026-05-27T081913Z-abstractor-L4-L3-krylov-step-lowering/CYCLE.md` §"Audit of cycle-002 identity-in-form claim") re-read the cited evidence ranges (the CG body-identity claim, now preserved verbatim in this theme's §Verified-against bullet 1 — the terminal firm home; original pre-reduction slice range `cg.md:341-362`, lifted there per the cycle-009 corpus reduction — and the live anchor `arnoldi_step.md:178-213`) and confirmed the assertion. The L2>L3 lift of every primitive call in the kernel body is the identity rotation; therefore the L3>L2 lowering on the body — running the same edge in the opposite direction — is also the identity rotation. **This is the strongest justification kind for the theme**: the assertion is observational about the slice corpus's evidence, not derivational from algebraic laws or reduction rules.
```

```edit:book/src/L3-L2/krylov-step-body-identity.md
[old]: - `book/src/spec/slices/cg.md:341-362` — combinator-miner cycle-002 Claim 2 ("step body lifts as identity"). The cited range contains the verbatim claim at line 360: *"The L2→L3 rotation on the step body is therefore the **identity in form**: no unfolding, no global lift, no schema change."* with the justification (lines 353-358) that L2's primitive vocabulary is already L3-native by signature shape. Re-read for the cycle-006 audit; assertion confirmed.
[new]: - **The combinator-miner cycle-002 Claim 2 ("step body lifts as identity") — terminal firm home: THIS entry.** The claim reads verbatim: *"The L2→L3 rotation on the step body is therefore the **identity in form**: no unfolding, no global lift, no schema change."* with the justification that L2's primitive vocabulary is already L3-native by signature shape. This verbatim quote was lifted into this §Verified-against bullet per the cycle-009 corpus reduction and is preserved here as the terminal firm home — the upstream `L4-L3/krylov-step-typed-wrapper-dissolution.md` §"Audit of cycle-002 identity-in-form claim" and the firm `L3/krylov-step.md` §dep-map (line 188) both point at this bullet as the lifted-in anchor for the CG body-identity evidence. (Original pre-reduction slice range `cg.md:341-362`, with the verbatim claim at original slice line 360 and its justification at original slice lines 353-358; the `cg.md` slice is now a 165-line stub and that range no longer resolves.) Re-read for the cycle-006 audit; assertion confirmed.
```

[Notation: the L3>L2 high→low direction (LHS = L3 form, RHS = L2 form), the body's line-for-line identity-in-form mapping, the four applicability conditions, the `empirical-match`/`structural` justification kinds, and the `firm` status are all unchanged. The Haskell-`::`/`text`-fence/TypeScript-record conventions in §"L3 form (LHS)" and §"L2 form (RHS)" are untouched. This is a pure provenance-pointer re-anchor: three dangling `cg.md` ranges → the in-document terminal firm home (this entry's §Verified-against bullet 1) plus a parenthetical original-range annotation, per the cycle-009 corpus-reduction lifted-evidence convention already applied at L2:138/146 and L3:188.]

[No "Speculative L3 operators" trim is needed — that section already reads "None" (line 116); the theme introduces no new L3 vocabulary, so nothing to firm up there. Status stays `firm` (it was already firm before this sweep; the dangling provenance pointer did not gate the status, only the citation hygiene).]

## Discipline notes

- **What changed**: three citations to the reduced-slice range `cg.md:341-362` / `:351-362` (lines 97, 107, 125) re-anchored to the in-document terminal firm home (this entry's own §Verified-against bullet 1, which physically carries the verbatim cycle-002 Claim-2 quote), with the slice range demoted to a parenthetical "original pre-reduction range, lifted here per the cycle-009 corpus reduction" provenance annotation. This is the cycle-013/014/015/016 lifted-evidence annotation convention applied verbatim — identical in shape to the convention already in force at `book/src/L2/krylov-step.md` §Evidence (lines 138, 146) and `book/src/L3/krylov-step.md` §dep-map (lines 188, 196, 204).
- **Why the terminal home is THIS entry's §Verified-against** (not a deeper L0 source range): the body-identity claim is an *observational/empirical* claim about the slice corpus's L2/L3 prose ("L2's primitive vocabulary is already L3-native by signature shape"), authored by the cycle-002 combinator-miner. Its verbatim text was lifted from the cg.md slice into this theme's §Verified-against during the cycle-009 corpus reduction (per the L3 entry's line-188 note: "preserved there with the verbatim Claim-2 quote at original slice line 360"). It does not reduce further to an L0 Palace source range — it is a methodological observation about primitive signature shapes, not a citation of a Palace source site. The L0 source for the underlying CG primitive sequence lives separately at `book/src/L1-L0/ksp-solve-mutation-rotation.md` Sub-pattern B (`iterative.cpp:360-486`), already the terminal L0 home named at L2:138; the *body-identity observation itself* terminates in this theme. Pointing the three sites at this entry's §Verified-against is therefore pointing at the genuine terminal home, not a relay.
- **Self-verify (cycle-015/016 producer-emit-time procedure, friction-ledger `producer-citation-drift-verify-not-self-invoked`)**: I confirmed the new target is the TERMINAL firm home and not another relocated-dangle (step 4 of the verify-citation-range producer-emit procedure). The verbatim Claim-2 quote physically sits in this entry's §Verified-against bullet 1 (read in this dispatch); the slice range `cg.md:341-362` was confirmed dangling (the 165-line stub no longer contains "identity in form" / "step body lifts" — grep over the stub returned only the unrelated L0-supersession line 8). The two live co-citations were each re-read and confirmed live (NOT touched): `gmres.md:459-471` resolves in the 671-line live slice (the GMRES `inner_loop :: OpParams -> Convergence -> Krylov -> Solve (Krylov, Outcome)` body); `arnoldi_step.md:178-213` resolves in the 302-line live slice (the MGS sequential-obstruction content). The `arnoldi_step.md:185-188` sub-range cited in the upstream theme also sits inside the live `:178-213` block.
- **No content correction needed**: the theme's prose is correct; only the citation provenance was stale. No convention-backwards / drifted-claim / contradicting-L0 fix was warranted, so the bounded-content-correction authority (friction-ledger `lifter-scope-content-correction-boundary`) was not exercised.
- **Direction discipline**: the theme stays high→low (LHS = L3 form, RHS = L2 form, prose narrates the L3→L2 rewrite forward). No inversion. The re-anchor touches only provenance citations, not the rewrite direction.

## Supporting evidence

- `book/src/spec/slices/cg.md` — 165-line reduced stub (confirmed via `wc -l`); grep for `341|351|360|identity in form|step body lifts` returns only the unrelated L0-supersession line 8 → `cg.md:341-362` and `:351-362` dangle (the verbatim claim was lifted out).
- `book/src/L3/krylov-step.md:188` — cross-chain consensus: "`book/src/L3-L2/krylov-step-body-identity.md` §Verified-against (line 125; lifted from the original `book/src/spec/slices/cg.md:341-362` per the cycle-009 corpus reduction, preserved there with the verbatim Claim-2 quote at original slice line 360)". Confirms this entry's §Verified-against IS the designated terminal home.
- `book/src/L4-L3/krylov-step-typed-wrapper-dissolution.md:204` — "The CG half of this evidence (`cg.md:351-362`) has since been lifted into the firm theme [`L3-L2/krylov-step-body-identity`] §Verified-against per the cycle-009 corpus reduction; the historical slice ranges are retained here as the audit's provenance record" — the upstream theme already applies the same convention and points HERE.
- `book/src/L2/krylov-step.md:138,146` — precedent renderings of the cycle-009 lifted-evidence annotation convention ("terminal firm home is ...; the ... rendering remains live ...; (Original pre-reduction slice ranges: ...)").
- `book/src/spec/slices/gmres.md` — 671-line live slice; `:459-471` (the `inner_loop` body) resolves → co-pointer out of scope, left untouched.
- `book/src/spec/slices/arnoldi_step.md` — 302-line live slice; `:178-213` resolves → co-citation live, untouched.
- `skills/verify-citation-range/SKILL.md` §"Producer self-verification before emitting citations" — the emit-time procedure applied to this sweep (step 4: confirm new target is terminal, not relocated-dangle).

## Open questions / caveats

- **None blocking.** This is a clean pure re-anchor. The defect was a self-referential dangling provenance pointer (the theme's §Verified-against citing the dead slice range as a live source while the rest of the chain already named that §Verified-against as the lifted-in home). Closes OQ `l3-l2-body-identity-cg-md-citation-sweep` (opened cycle-016) with answer-link `book/src/L3-L2/krylov-step-body-identity.md` §Verified-against bullet 1 (re-anchored to the in-document terminal firm home).
- **Lifting-note (working-notes-only, NOT for chapter content, per the high→low directive)**: the body-identity observation is a terminal *methodological* claim — it does not lower further to an L0 Palace source. Were a future dispatch to want an L0 anchor for the CG primitive *sequence* (distinct from the body-identity *observation*), the terminal L0 home is `book/src/L1-L0/ksp-solve-mutation-rotation.md` Sub-pattern B (`iterative.cpp:360-486`), already named at L2:138. No action; recorded only so a later auditor does not re-open this as a missing-L0 gap.
- **Co-pointer note (in-scope acknowledgement, no action)**: `gmres.md:459-471` at §Verified-against bullet 4 (and the lighter `gmres.md` references elsewhere in the chain) resolves in the live slice and is correct as-is; it is explicitly OUT OF SCOPE for this cg.md sweep and was left untouched. Should the `gmres.md` slice later be reduced, a parallel lifter sweep would re-anchor that pointer the same way — not this dispatch's job.
