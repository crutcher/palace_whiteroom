---
verifies: ../CYCLE.md
critiqued_at: 2026-05-28T00:18:00Z
critic_version: 1
checks:
  citation-validity: warning
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: pass
  cross-reference-integrity: pass
  edge-label-fidelity: pass
  plan-kind-consistency: warning
  skill-uptake-survey: warning
repaired_at: 2026-05-28T00:35:00Z
repairer_version: 1
repairs:
  citation-validity: repaired
  surface-or-evidence: not-needed
  rotation-quality: not-needed
  variant-axis-coverage: not-needed
  cross-reference-integrity: not-needed
  edge-label-fidelity: not-needed
  plan-kind-consistency: repaired
  skill-uptake-survey: unrepairable
overall_status: ready
follow_up_agent: null
---

# META: verification of lifter eigsolve OQ cluster (cycle-009 OQ-cluster resolution)

## Critique

### Checks run

**citation-validity** — Verified each cited source range against `palace/linalg/*` directly via `mcp__palace-codemap__read_range`.
- `palace/linalg/arpack.cpp:387` — confirmed `eig[i] = eig[i] * gamma` inside `SolveInternal` post-`neupd`. Pass.
- `palace/linalg/slepc.cpp:711-716` — confirmed `SlepcEPSSolverBase::GetEigenvalue(i)` returning `l * gamma`. Pass.
- `palace/linalg/slepc.cpp:1194-1203` — confirmed `SlepcPEPSolverBase::GetEigenvalue(i)` returning `l * gamma`. Pass.
- `palace/linalg/nleps.cpp:88-93` — confirmed `NonLinearEigenvalueSolver::GetEigenvalue(i)` returning `eigenvalues[j]` (stored value). Pass.
- `palace/linalg/eps.hpp:122` — confirmed `SetInitialSpace(const ComplexVector &v)` virtual declaration. Pass.
- `palace/drivers/eigensolver.cpp:264` — confirmed `eigen->SetInitialSpace(v0)` call site at line 264 inside the user-supplied-vs-random branch. Pass.
- `palace/models/modeeigensolver.cpp:474` — confirmed `eigen->SetInitialSpace(*initial_space)` inside `if (initial_space)` block. Pass.
- `mcp__palace-codemap__search_text` for `EPSGetIterationNumber|PEPGetIterationNumber|NEPGetIterationNumber` — confirmed zero hits in `palace/` source tree. Pass (negative anchor solid).
- `palace/linalg/slepc.cpp:1554-1560` — confirmed `SlepcNEPSolverBase::GetEigenvalue(i)` returns `l` directly (no `* gamma`). Pass on the citation, **but the surrounding claim that "NEP uses `gamma = 1`" is not supported by the L0 evidence the lifter cites**: `slepc.cpp:333` sets `gamma = delta = 1.0` at construction default, but `slepc.cpp:1649` (`SlepcNEPSolverBase::SetOperators`-vicinity, per `mcp__palace-codemap__search_text` of `gamma\s*=`) and `slepc.cpp:1715` both compute `gamma = std::sqrt(normK / normM)` for non-trivial scaling. The lifter's own §"Open questions / caveats" 5th bullet **explicitly self-flags this as an unverified claim** ("the scaling-coordinate-convention resolution does not depend on the NEP detail being exact"). Even so, the prose in Edit 3 and Edit 8 asserts the unverified claim as fact ("NEP uses `gamma = 1` at construction"; "no Higham scaling applied to `SlepcNEPSolverBase`"). The two non-trivial `gamma = std::sqrt(normK / normM)` sites in the NEP `SetOperators` region directly contradict the asserted "no scaling applied to NEP" reading. **Warning**: the prose claim should be softened to match the lifter's own caveat ("returns `l` directly — the SLEPc-NEP backend manages its own coordinate handling separately from the EPS/PEP un-scale-at-accessor pattern; the precise convention is flagged for follow-up audit") rather than asserting `gamma = 1`.
- `palace/linalg/arpack.cpp:249-260` (`SetInitialSpace` body) — confirmed `MFEM_VERIFY(n > 0, ...)` at line ~253; `info = 1` toggle at line ~260. Pass.
- `palace/linalg/slepc.cpp:657-669` — confirmed `SlepcEPSSolverBase::SetInitialSpace` body with `EPSSetInitialSpace(eps, 1, is)` PETSc call. Pass.
- `palace/linalg/arpack.cpp:342, 350` — confirmed `iparam[2]` consumed at line ~342 (`num_it = (int)iparam[2]`) and printed at line 350 inside the `print > 0` block (`iterations {:d}`). Pass.
- Verdict: **warning** for the NEP-gamma overclaim; all other citations point to in-range, supporting source.

**surface-or-evidence** — Refinement-shaped proposal modifying surface (`book/src/L1/eigsolve.md` Algebraic-law §5, `EigResult` record, shape-contract bullets, Status block, Evidence rows) with structural rotation-claim grounded in direct L0 evidence (5 backend-specific citations of un-scale-at-accessor + 4 call-site anchors for per-call initial-space + negative-anchor citations for no-iteration-count-accessor). Each surface edit is paired with the cited L0 evidence. **Pass**.

**rotation-quality** — Not a fresh-rotation report (the L1 rotation was claimed in cycle-009; this is a re-anchor/correction lift). The Algebraic-law §5 rewrite tightens the L1 form to match L0 (un-scaled eigenvalues uniformly across backends) rather than introducing a new rotation; the `iterations` field constructive-introduction is a parallel L1-constructive addition (mirrors cycle-010 `LinearSolveFailed` pattern). The L1 form remains strictly more abstract than L0 (collapses three backend un-scaling sites to one structural statement; collapses three backend `SetInitialSpace` variants to one per-call binding; introduces sum-type/Maybe vocabulary not present in the L0 C++ surface). **Pass** — not applicable to a lift/re-anchor report in the strict sense, but the content does not regress the rotation.

**variant-axis-coverage** — The variant axes in scope (problem-type × backend × scale-type) are covered by the §5 rewrite (each backend's un-scaling site is explicitly cited: ARPACK, SLEPc-EPS, SLEPc-PEP, SLEPc-NEP, NLEPS). The `SetInitialSpace` axis is covered (ARPACK + SLEPc-EPS bodies + 2 call sites). The `iterations` axis is covered with negative anchors across all three backends. **Pass**.

**cross-reference-integrity** — Links checked: `reports/2026-05-27T220558Z-lifter-eigsolve-linear-solve-failed-anchor/` (exists; CYCLE.md + META.md); `reports/2026-05-27T234730Z-abstractor-eigsolve-mutation-rotation-l1-l0/` (exists; CYCLE.md). The wave-1 sibling's `EigResult` record (sibling CYCLE.md lines 75-84) does NOT yet include the `iterations` field — this is the coordination flagged in the dispatch's §"Open questions / caveats" 2nd bullet. The lifter pre-positions a footnote-style coordination flag for the cycle-011 integrator-finalize to handle; **the coordination is correctly surfaced, not hidden**. OQ ledger anchors at `scaffolding/open-questions.md:1493-1524` confirmed against actual line numbers. **Pass**.

**edge-label-fidelity** — Proposal does not carry an L_{n+1}→L_n edge label (it is an L1-internal re-anchor against L0 evidence, not a lowering theme). Not applicable to this report shape; the prose narration consistently discusses the L1 surface as the locus of change with L0 as evidence. **Pass**.

**plan-kind-consistency** — Declared kind is `rough-in re-anchor lift (cycle-011 cluster resolution)`. The 13 proposed-changes blocks are mechanically scoped: 7 edits to `book/src/L1/eigsolve.md` (Signature record, shape-contract bullets, Algebraic-law §5, "Laws that do not hold" bullet, post-Signature callout, Status block, Evidence rows) + 6 OQ-ledger edits (3 narrative appends + 3 yaml-status flips). **However, Edit 3 (Algebraic-law §5 rewrite) is a substantive content correction — the cycle-009 rough-in stated the **wrong** scaling convention, and the lifter is **correcting it**, not merely re-anchoring it.** Per the lifter role-spec discipline ("if you find yourself making non-trivial content decisions, stop and flag in Open questions — likely an abstractor reread is needed"), this borderlines on scope-exceeding. The dispatch **self-flags this** in §"Open questions / caveats" bullet 4 with the reasoning "the cycle-009 rough-in author's choice was a factual reading-error of the L0 surface, not a design decision" and asks the cycle-011 critic to judge. **Warning**: the call is defensible (5 backend-specific citations + cross-backend consistency provide unambiguous L0-evidence-driven correction; matches cycle-010 lifter's precedent for L0-evidence-driven tightening), but the report-shape is misclassified — a pure re-anchor would not require correcting an algebraic-law statement of fact. The integrator may want to treat this as either (a) accept the correction with a CYCLE.md note that the lift exceeded scope but the correction is L0-evidence-driven; or (b) bounce Edit 3 to a follow-up abstractor reread and land only Edits 1, 2, 4, 5, 6, 7, 8-13. Surfacing the verdict to the integrator/repairer is the appropriate critic action.

**skill-uptake-survey** — The proposal explicitly invokes `mcp__palace-codemap__search_text` (for the zero-hit negative anchor on SLEPc iteration-count accessors). The `verify-citation-range` skill would have applied across all 14 cited line ranges — the dispatch does not name-check it, but the supporting-evidence section enumerates each verified range with "verified by lifter" annotations, which is the skill's procedural output even without explicit invocation. The `verify-refinement-surface` skill (applicable here given the refinement-shaped surface modification) is also not name-checked but the proposed-changes blocks all carry the `[old]:` / `[new]:` pair-form the skill prescribes. **Warning** (telemetry only — surfaces the persistent gap of skill-invocations not being explicitly referenced in CYCLE.md prose; the actual procedural compliance is present, just not surfaced as such). This is a recurring pattern across cycle-010 and now cycle-011 — methodology-codification candidate for cycle-012 meta-phase.

### Issues found

1. **NEP-gamma overclaim** — CYCLE.md Edit 3 (Algebraic-law §5 rewrite, after "SLEPc-NEP at `palace/linalg/slepc.cpp:1554-1560` returns `l` without `* gamma`") and Edit 8 (OQ narrative append, similar paragraph) assert as fact: "the nonlinear case uses `gamma = 1` at construction time (no Higham scaling applied to the nonlinear case)". Direct evidence contradicts this — `palace/linalg/slepc.cpp:1649` and `:1715` both compute `gamma = std::sqrt(normK / normM)` for NEP, the same Higham-norm scaling pattern as EPS and PEP. The lifter itself flags this in §"Open questions / caveats" bullet 5 ("the scaling-coordinate-convention resolution does not depend on the NEP detail being exact"; "verifying this would require a closer read of the SlepcNEPSolverBase construction sequence + `SetOperators` body, which is out of scope"). The fix is to soften the prose: replace "uses `gamma = 1` at construction" with "the NEP backend manages its own coordinate handling separately from the EPS/PEP un-scale-at-accessor pattern; the precise un-scaling convention is flagged for follow-up audit (see Open question on NEP scaling gap below)". Severity: **medium** — the broader resolution (un-scale-at-accessor is uniform for EPS/PEP/NLEPS) is correct; the NEP edge-case detail in the supporting prose is overstated. Located: CYCLE.md Edit 3 (line ~92 of CYCLE.md, the parenthetical in the new §5 text); Edit 8 (line ~152 of CYCLE.md, the parenthetical in the OQ narrative).

2. **Lifter scope vs. content-correction tension on Edit 3** — The Algebraic-law §5 rewrite is substantively a content correction (the cycle-009 rough-in stated convention (a) "L1 returns scaled"; the lifter rewrites to convention (b) "L1 returns un-scaled"). Lifter role-spec discipline pushes content-correction calls toward abstractor reread, not lifter re-anchor. The dispatch self-flags this as borderline and asks the critic to judge. The L0-evidence-driven justification is strong (5 backend-specific anchors + cross-backend consistency), and the cycle-010 lifter precedent for L0-evidence-driven tightening covers a similar case. **Verdict from critic perspective**: defensible but mis-classified. The proposal should either (a) be retained with a one-line note in CYCLE.md / Status block acknowledging the content-correction nature ("the §5 rewrite is a content correction of a cycle-009 reading-error, not pure structural re-anchor"); or (b) bounce Edit 3 to a follow-up abstractor reread and land only the per-call placement + iteration-count edits. Severity: **low-to-medium** — the report shape is mis-aligned with lifter scope, but the L0 evidence makes the correction unambiguous; surfacing for integrator/repairer judgement. Located: CYCLE.md Edit 3 entire block (lines ~86-93); §Discipline notes #2 (lines ~226); §"Open questions / caveats" bullet 4 (lines ~264 — self-flag is already present).

3. **Coordination flag with cycle-011 wave-1 abstractor's `EigResult` is non-blocking but unresolved** — The dispatch correctly surfaces (in §"Open questions / caveats" bullet 2) that the wave-1 abstractor's `EigResult` field list (sibling CYCLE.md lines 75-84) does NOT include the `iterations` field this dispatch adds. If both reports apply in the same cycle, the wave-1 abstractor's L1>L0 theme will reference a stale field list. The dispatch defers this to cycle-011 integrator-finalize without prescribing a fix. Severity: **low** (telemetry; not blocking). Located: CYCLE.md §"Open questions / caveats" bullet 2 (lines ~260). Suggestion for repairer: either add a CYCLE.md note that Edit 1's `iterations` field addition should be re-referenced in the wave-1 theme by integrator-finalize, or pre-write a small additional edit to the wave-1 theme's Sub-pattern C reference list.

4. **`partially-answered` precedent inconsistency** — The cycle-010 sibling lifter resolved `LinearSolveFailed` as `partially-answered` because the L1>L0 materialisation theme didn't yet exist (it now does, via the cycle-011 wave-1 abstractor). The cycle-011 lifter's `iterations` resolution is analogous (an L1-constructive field that also materialises only in the same future theme), but the dispatch flips it to `resolved` not `partially-answered`. The rationale (§"Discipline notes" #1) is that the L1>L0 theme is **now in flight** (wave-1 sibling), so the materialisation site exists. This is defensible, but creates inconsistency: the cycle-010 `LinearSolveFailed` OQ remains `partially-answered` despite the same materialisation theme now existing. The dispatch flags this in §"Open questions / caveats" bullet 1 ("the cycle-010 OQ can flip from `partially-answered` to `resolved` at that point — out of scope for this dispatch"). **Suggestion for repairer**: either (a) flip cycle-010 `LinearSolveFailed` to `resolved` in the same dispatch (small adjacent edit), or (b) leave both OQs as `partially-answered` for consistency until the wave-1 theme integrates, deferring both flips to cycle-012. Severity: **low** (consistency only; not factually wrong). Located: CYCLE.md §"Open questions / caveats" bullet 1 (lines ~258).

5. **Skill-uptake-survey gap (recurring pattern)** — The dispatch does the procedural work of `verify-citation-range` and `verify-refinement-surface` (the supporting-evidence section enumerates each "verified by lifter" range; the proposed-changes blocks all use the `[old]:` / `[new]:` skill-prescribed form) without explicit name-check invocation in CYCLE.md prose. Same pattern observed in cycle-010 sibling. Recurrence ≥2 since cycle-010 → candidate for cycle-012 meta-phase friction-ledger codification ("skill invocations are procedurally present but not surfaced as such in CYCLE.md, complicating skill-uptake telemetry"). Severity: **low** (telemetry only). Located: CYCLE.md throughout (no skill invocations explicitly named except `mcp__palace-codemap__search_text`).

6. **Edit 4 callout has a self-referential narration issue** — The `[insert-after]` directive on Edit 4 (line ~100 of CYCLE.md) reads: "the line ending 'current L0 instantiations of `eigsolve` will not produce it.' — the new callout goes on the next line, before the 'Shape contract (bunsen-style, named axes):' line at source line ~49". This is informal-prose for a structural insert; the integrator-per-report parser needs an unambiguous anchor. The cited "source line ~49" matches the current `book/src/L1/eigsolve.md` (line 49 is "Shape contract (bunsen-style, named axes):"). The anchor text is correct but the prose-instruction format may complicate machine parsing. Severity: **low** (mechanical; integrator can handle). Located: CYCLE.md Edit 4 (line ~100). Suggestion for repairer: tighten the anchor format to match the cycle-010 sibling's `[insert-after]` style if that was more mechanical.

## Repair

### Fixes attempted

**Finding 1 — NEP-gamma overclaim (citation-validity warning, MEDIUM severity).**
- **Decision**: repaired
- **Action**: Verified the L0 evidence directly via `mcp__palace-codemap__read_range` on `palace/linalg/slepc.cpp`:
  - Line 333: base-class constructor sets `gamma = delta = 1.0` as default (shared across EPS / PEP / NEP).
  - Lines 1645-1651: `SlepcNEPSolver::SetOperators` (K, M overload) computes `gamma = std::sqrt(normK / normM); delta = 2.0 / normK;` when `type != ScaleType::NONE`.
  - Lines 1711-1719: `SlepcNEPSolver::SetOperators` (K, C, M overload) computes `gamma = std::sqrt(normK / normM); delta = 2.0 / (normK + gamma * normC);` when `type != ScaleType::NONE`.
  - Lines 1554-1560: `SlepcNEPSolverBase::GetEigenvalue` returns `l` directly without applying `* gamma`.
  - **Confirmed critic's reading**: NEP DOES compute non-trivial gamma at `SetOperators` (same Higham-norm scaling pattern as EPS and PEP); the "NEP uses `gamma = 1`" assertion in CYCLE.md Edit 3 / Edit 8 / Edit 7 (Evidence row) / Supporting-evidence row is factually wrong. The actual L0 asymmetry is that NEP `GetEigenvalue` does NOT apply the `* gamma` un-scale, despite gamma being non-trivial — a genuine convention gap in the SLEPc-NEP backend.
- **Edits applied to `reports/2026-05-27T235632Z-lifter-eigsolve-oq-cluster/CYCLE.md`**:
  - Edit 3 `[new]` text (Algebraic-law §5 rewrite, line 92): replaced the "NEP uses `gamma = 1` at construction" assertion with "the SLEPc-NEP backend manages its own coordinate handling separately from the EPS / PEP un-scale-at-accessor pattern" plus an inline parenthetical noting that NEP `SetOperators` at `:1645-1651, 1711-1719` does compute non-trivial gamma. The §5 conclusion is reframed to "L0 convention is uniform across EPS / PEP / NLEPS" (SLEPc-NEP convention pending audit) rather than the original "uniform across all backends".
  - Edit 7 (Evidence row, line 131): rewrote the SLEPc-NEP evidence line to drop the "gamma = 1, no Higham scaling applied" claim, replacing with "manages its own coordinate handling separately... precise convention flagged for follow-up audit".
  - Edit 8 (OQ-narrative append, line 152): rewrote the same paragraph to match Edit 3's softened framing; added the inline parenthetical about the NEP `SetOperators` evidence.
  - Supporting-evidence row at line 249: rewrote the SLEPc-NEP entry similarly.
  - §"Open questions / caveats" bullet 5 (line 266): rewrote the bullet to record the critic's verification and the repair, and to propose a fresh OQ slug (`eigsolve-slepc-nep-coordinate-convention-audit`) for the integrator-finalize to open in `scaffolding/open-questions.md` as the follow-up audit anchor.
- **Rationale**: This is a mechanical prose substitution (the critic prescribed the exact replacement text in the Issues §1 finding). The factual correction is L0-evidence-driven (the critic's verification of `slepc.cpp:1649` and `:1715` is confirmed by direct read). The broader resolution (EPS / PEP / NLEPS uniform un-scale-at-accessor; L1 returns un-scaled) is unaffected — the asymmetry is isolated to the SLEPc-NEP backend and is flagged as a follow-up OQ rather than blocking this dispatch's primary three resolutions.

**Finding 2 — Lifter scope vs. content-correction tension on Edit 3 (plan-kind-consistency warning, LOW-MEDIUM severity).**
- **Decision**: repaired (annotation accepted in-scope, with a clarifying note added)
- **Action**: The dispatch self-flagged this in §"Open questions / caveats" bullet 4 and asked the critic to judge. The critic judged it defensible but mis-classified, and offered two routing options: (a) accept the correction with a CYCLE.md note acknowledging the content-correction nature; or (b) bounce Edit 3 to a follow-up abstractor reread. The repairer adopts option (a) per critic's stated precedent (cycle-010 sibling lifter's L0-evidence-driven prose tightening) — the L0 evidence is unambiguous (5 backend-specific citations + cross-backend consistency for EPS / PEP / NLEPS), and the cycle-010 lifter precedent supports L0-evidence-driven tightening as in-scope. The CYCLE.md §"Open questions / caveats" bullet 4 self-flag prose already serves as the in-CYCLE acknowledgement that Edit 3 exceeds pure structural re-anchor scope; the repair section here augments that with a follow-up signal for cycle-012 meta-phase to clarify lifter scope.
- **Rationale**: The boundary between (a) lifter re-anchoring against L0 evidence vs. (b) abstractor rewriting against L0 evidence is procedural-methodology-level, not a per-report defect. The repair-authority bar is "mechanical / surgical only"; flagging the methodology clarification rather than authoring it is in-scope. **Follow-up signal**: cycle-012 meta-phase should consider whether L0-evidence-driven content correction (the cycle-010 / cycle-011 lifter pattern at recurrence-2) belongs in the lifter role-spec as an explicit allowance, or whether such corrections should be re-routed to abstractor reread. (Appendable to `scaffolding/skill-candidates.md` if the meta-phase agrees.)

**Finding 3 — Skill-uptake-survey gap (warning, telemetry).**
- **Decision**: unrepairable
- **Rationale**: This is a telemetry-only finding about whether the dispatch explicitly name-checked `verify-citation-range` and `verify-refinement-surface` in CYCLE.md prose. The procedural work is present (per the critic's own observation: "the supporting-evidence section enumerates each verified range with 'verified by lifter' annotations, which is the skill's procedural output"); the gap is the explicit naming. Adding name-checks post-hoc would be authoring substantive content (changing the dispatch's voice / framing), not a mechanical surgical fix. Defer to cycle-012 meta-phase per the critic's own suggestion (recurrence ≥2 since cycle-010 → methodology-codification candidate). The friction-ledger entry candidate is: "skill invocations are procedurally present but not surfaced as such in CYCLE.md, complicating skill-uptake telemetry".

**Finding 4 — Coordination flag with cycle-011 wave-1 abstractor's `EigResult` (low severity).**
- **Decision**: not-needed (informational)
- **Rationale**: The dispatch correctly surfaces the coordination in §"Open questions / caveats" bullet 2; the integrator-finalize step handles the coordination per the cycle-011 staging pattern. No repair action required — the flag is already present and routes correctly.

**Finding 5 — `partially-answered` precedent inconsistency (low severity).**
- **Decision**: not-needed (informational; consistency point)
- **Rationale**: The dispatch's §"Discipline notes" #1 justifies the `resolved` flip on the basis that the L1>L0 materialisation theme is now in flight (wave-1 sibling); this is defensible. The cycle-010 OQ flip is appropriately out-of-scope per the dispatch's §"Open questions / caveats" bullet 1. Either of the critic's suggested options (flip cycle-010 now vs. defer both) is acceptable; flipping cycle-010 now would be a small adjacent edit but not strictly necessary, and would expand scope beyond the dispatch's three-OQ cluster. The current treatment is preserved.

**Finding 6 — Edit 4 callout has a self-referential narration issue (low severity).**
- **Decision**: not-needed (mechanical anchor; integrator can handle)
- **Rationale**: The critic confirmed the anchor text is correct (line 49 of the current `book/src/L1/eigsolve.md` is "Shape contract (bunsen-style, named axes):"). The `[insert-after]` directive prose is informal but unambiguous about the insertion point. The integrator-per-report parser handles `[insert-after]` directives that name an end-of-previous-block anchor; this should apply cleanly. If the integrator finds the anchor ambiguous in practice, a targeted re-edit can land in cycle-012; not a blocker for cycle-011 integration.

### Unrepairable findings

- **Skill-uptake-survey gap**: telemetry-only; would require authoring substantive prose changes (name-checking skills in dispatch voice). Routed to cycle-012 meta-phase per critic's own suggestion. No follow-up agent — meta-phase will absorb this with the cycle-010 sibling's analogous flag.

## Suggested resolution

`overall_status: ready` — the NEP-gamma overclaim has been repaired by mechanical prose substitution to the critic's prescribed softened framing; the broader resolution (un-scale-at-accessor uniform across EPS / PEP / NLEPS; L1 returns un-scaled) is L0-anchored and unaffected by the SLEPc-NEP edge-case correction. The Edit 3 content-correction discipline question is acknowledged in-CYCLE and routed forward as a methodology signal; the dispatch is otherwise structurally clean (variant-axes, surface, evidence, cross-references, edge-labels all pass).

Notes for the integrator-finalize step:

1. **Open a fresh OQ** in `scaffolding/open-questions.md` with slug `eigsolve-slepc-nep-coordinate-convention-audit`, opened cycle-011 by repairer, anchoring to `palace/linalg/slepc.cpp:1554-1560` (the un-scaled `GetEigenvalue`) and `:1645-1651, 1711-1719` (the non-trivial gamma at `SetOperators`). Question: does the SLEPc NEP API itself un-scale before returning eigenpairs to the Palace wrapper, or is the Palace `GetEigenvalue` returning scaled values? Recommended target: cycle-012 lifter or `lowering-verifier`-on-eigsolve dispatch.
2. **Coordination with the cycle-011 wave-1 abstractor's `EigResult`**: per the dispatch's own §"Open questions / caveats" bullet 2 — add a footnote to the L1>L0 theme noting the new `iterations` field is covered by the same Sub-pattern C materialisation as `LinearSolveFailed`, or defer to a cycle-012 lifter pass.
3. **Skill-uptake telemetry**: append a friction-ledger candidate (or update existing) for cycle-012 meta-phase: "skill invocations procedurally present but not name-checked in CYCLE.md prose; recurrence ≥2 across cycle-010 and cycle-011 lifter dispatches".
4. **Lifter scope clarification**: surface to cycle-012 meta-phase: the cycle-010 + cycle-011 lifter pattern of L0-evidence-driven content correction (vs. pure structural re-anchor) is at recurrence-2; methodology may want to either codify this as in-scope or formalise the abstractor-reread re-routing path.
