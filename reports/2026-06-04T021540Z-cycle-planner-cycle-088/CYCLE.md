---
agent: cycle-planner
invoked_at: 2026-06-04T021540Z
scope: cycle-088 dispatch plan
status: pending
---

# Cycle 088 dispatch plan

**Position:** FIRST primary cycle of meta-batch-28 (cycles 088/089/090; the batch-28 meta-phase fires AFTER cycle-090's finalize, aggregating 088/089/090). Fresh session post-batch-27-meta-phase + the CLAUDE.md de-bulk commit (468cfde). NOT a land-clean cycle (that is c090, the 3rd-of-batch) — this cycle takes the substantive LEAD.

## Goals selected this cycle

Take the batch-28 LEAD (active-head #1): the SCOPED `lowering-verifier` literature-anchor PROBE on `matrix-weighted-norm`'s norm-axiom laws — the cheap dischargeability test of the convergent foundation-blocker that gates 5 of the 6 stay-seed feature columns. The probe judges whether a standard inner-product-norm literature anchor can raise the triangle/Cauchy–Schwarz/parallelogram law confidence to `inner_product`-equivalent WITHOUT a positive √-entry-point test; two clean outcomes (DISCHARGE → queue the √-cascade wave as the next batch-28 item; CONFIRM-CEILING → record the explicit-verdict ceiling, stay `rough-in (test-coverage-bounded)`). Plus the LOW/hygiene eigenfreq-qfactor stale-cross-ref land-clean (active-head #3 — the LAST pre-codification firm-promotion residue). Active-head #2 (bottom-up frontier) is genuinely light this cycle — the firm-on-positive-structure route is EXHAUSTED for the all-primitives-firm cohort, and the next reduce-verb law-confidence pass is itself GATED on item #1's outcome — so NO third substantive dispatch is manufactured (a rectangular pull-up is forbidden by the redirect).

## Dispatches

### D1 — `lowering-verifier` — THE LEAD (HIGH)
- **scope:** `matrix-weighted-norm-norm-axiom-law-confidence-probe` — a SCOPED literature-anchor PROBE on `book/src/L1/matrix-weighted-norm.md`'s norm-axiom laws (law 4 triangle inequality `:54`; law 6 Cauchy–Schwarz in the B-inner-product `:56`; law 7 parallelogram identity `:57`). The verifier judges **dischargeability**: can a standard inner-product-norm literature anchor (the well-known result that any SPD `B` induces a genuine inner product `⟨x,y⟩_B := xᴴ B y`, under which the energy norm `√(xᴴ B x)` inherits triangle / Cauchy–Schwarz / parallelogram by the standard inner-product-space theorems) raise these three laws' confidence to `inner_product`-equivalent WITHOUT a positive test at the √-entry point? This is the structure-side gate the **c080 D1 audit ruled the firm-on-positive-structure escape INAPPLICABLE for** (the laws are inner-product-structure theorems the L0 source only NUMERICALLY asserts via `MFEM_ASSERT(dot.real() > 0.0 && |dot.imag()| < 1e-9·dot.real())` round-off witnesses — NOT syntactic identities read off positive source like the `solve_family` element-independence read-off). The probe is the in-scope analog of an assembly test applied to the structure-side gate the escape could not reach.
  - **Codemap-confirmed source anchors (embed in the brief; on-disk-confirmed this plan via `mcp__palace-codemap__read_range palace/linalg/operator.cpp:599-619`):** the √-overload entry point `linalg::Norml2(comm, x, B, Bx)` is `palace/linalg/operator.cpp:599-619` — real specialization `:600-607` (`B.Mult(x, Bx); double dot = Dot(comm, Bx, x); MFEM_ASSERT(dot > 0.0, ...); return std::sqrt(dot);` — the outer √ at `:606`); complex specialization `:609-619` (`B.Mult(x.Real(), Bx.Real()); B.Mult(x.Imag(), Bx.Imag()); std::complex<double> dot = Dot(comm, Bx, x); MFEM_ASSERT(dot.real() > 0.0 && std::abs(dot.imag()) < 1.0e-9 * dot.real(), ...); return std::sqrt(dot.real());` — the comment `// For SPD B, xᴴ B x is real.` at `:611`, the round-off-only Hermiticity witness at `:616-617`, the outer √ at `:618`). These confirm the probe's premise: the only L0-side checks of the norm structure are the two `MFEM_ASSERT` numerical witnesses; there is NO positive site verifying triangle/Cauchy–Schwarz/parallelogram. (The named decl `palace/linalg/operator.hpp:372-374` carries the SPD-precondition comment `Calculate the vector norm with respect to an SPD matrix B.`)
  - **TWO CLEAN OUTCOMES, both acceptable landings:**
    - **(a) DISCHARGE** — the literature anchor closes the structure-side gate (the SPD-induced-inner-product framing is judged a sufficient anchor to raise law-4/6/7 confidence to `inner_product`-equivalent). Record a `verified_against:` block citing the literature anchor + the SPD-induced-inner-product structure; the verb's §Status NARROWS to note the norm-axiom gate discharged, the √-entry-point-test gate (gate (a) in the verb) becomes the SOLE remaining gate. **Queue `matrix-weighted-norm-full-firm-cascade-wave` as the next batch-28 item** (the c089/c090 candidate) — do NOT run the ~30-file cascade in THIS dispatch.
    - **(b) CONFIRM-CEILING** — the norm axioms genuinely need a positive test the corpus lacks (the literature anchor establishes the math but does not substitute for a positive Palace site, paralleling the c080 ruling). Record the explicit verdict in the verb's §Status + a findings/`verified_against:` note; leave it `rough-in (test-coverage-bounded)`. This is NOT a forcing — the explicit-verdict ceiling IS the load-bearing finding (it converts the √-cascade NO-GO from "inertia-held" to "verdict-held").
  - **HARD CONSTRAINT:** do NOT trigger the ~30-file cascade in this dispatch regardless of outcome. The probe is the gate-test; the cascade is a SEPARATE trigger-gated wave (`matrix-weighted-norm-full-firm-cascade-wave`) gated on outcome (a). Touch `book/src/L1/matrix-weighted-norm.md` ONLY (its §Status + a `verified_against:` / findings block); do NOT re-anchor any of its ~30 consumers.
- **deps:** none.
- **rationale:** active-head #1, THE LEAD, HIGH fan-out — the convergent foundation-blocker for 5 of 6 stay-seed columns (`gram_reduce` → electrostatic/magnetostatic/capacitance/inductance; `domain_energy_reduce` → energy-fields). The cheap dischargeability test enacted by the batch-27 meta-phase (friction `matrix-weighted-norm-full-firm-cascades-thirty-file-reanchor-sweep`, addressed; OQ `matrix-weighted-norm-and-bilinear-form-stay-rough-in-with-sharpened-per-operator-gates-c028`). Cites the c080 D1 ruling as the prior.

### D2 — `lifter` — LOW / hygiene
- **scope:** `eigenfreq-qfactor-column-stale-cross-ref-land-clean` — a bounded land-clean pass re-anchoring the 3 confirmed-stale maturity cross-references in the `eigenfrequency-qfactor` feature column (the LAST residue from the pre-codification firm-promotion cycles; the now-codified whole-`book/src/`-grep discipline, batch-27 GO, would have caught these coupled to the c085 flip / c082 promotion):
  - **`book/src/feature/eigenfrequency-qfactor.L4.md:36`** — `[`eigenmode.L4`](./eigenmode.L4.md) (**seed**)` → re-anchor to `(**firm**)` (eigenmode.L4 is `status: firm` on disk, flipped c085).
  - **`book/src/feature/eigenfrequency-qfactor.L4.md:38`** — `[`eigenfreq_qfactor_reduce`](../L4/eigenfreq_qfactor_reduce.md) (**rough-in (test-coverage-bounded)**)` → re-anchor to `(**firm**)` (eigenfreq_qfactor_reduce is `firmness: firm` on disk, promoted c082). NOTE: this is the same line flagged in the c087 integrator-signals "REINFORCING RESIDUAL DRIFT" note + suggested-next-dispatch.
  - **`book/src/feature/eigenfrequency-qfactor.L1.md:34`** — `[`eigenmode.L1`](./eigenmode.L1.md) (**seed**)` → re-anchor to `(**firm**)` (eigenmode.L1 is `status: firm` on disk, flipped c085).
  - **ZERO status/count change** — the cross-referenced entries' OWN frontmatter is already firm on disk; this is a PURE prose maturity re-anchor of the stale REFERENCES, no operator/column flip, no SUMMARY change, no `feature/index.md` cell change (the column's own `status: firm` flipped c085 is correct and untouched). Touch the two named files ONLY.
- **deps:** none.
- **rationale:** active-head #3, LOW fan-out (drift-guard hygiene; closes the batch-27 whole-book-grep migration). Friction `firm-promotion-coupled-re-anchor-needs-whole-book-cross-reference-grep` (recurrence-2, addressed by the batch-27 codification). This is the LAST land-clean residue from the pre-codification cycles — any c088+ firm-promotion now carries the whole-book grep itself.

## Deliverable-presence verification

Paste-inline-evidence per the strengthened batch-10 requirement. All checks run on-disk this plan.

### D1 (`matrix-weighted-norm-norm-axiom-law-confidence-probe`) — OPEN, 4/4 PASS
1. **File existence:** `book/src/L1/matrix-weighted-norm.md` EXISTS (Read returned 160 lines this plan).
2. **Maturity / already-discharged check:** on-disk §Status line `:110` reads `` `rough-in (test-coverage-bounded)` `` — the probe's deliverable (a norm-axiom-law literature-anchor verdict) is NOT at-or-below this; the dispatch is not a no-op. The file carries exactly **ONE** `verified_against:` block (`grep -c` returned `1`) — the c080 RADICAND-CONSTITUENT audit (3 citations: `test-domainpostoperator.cpp:75-93` partially-supports, `domainpostoperator.cpp:219-231` partially-supports, `operator.cpp:599-619` supports), explicitly `note`-scoped to "does NOT cover the outer √ nor the named entry point" and "gate (a) stays open at this entry point." `grep -i 'literature|triangle|Cauchy|parallelogram' ... | grep -i 'verified|audited'` → **"NO existing literature-anchor audit block on norm-axiom laws."** The norm-axiom-law literature-anchor probe is genuinely un-discharged.
3. **OQ-ledger RESOLVED-grep:** the governing OQ `matrix-weighted-norm-and-bilinear-form-stay-rough-in-with-sharpened-per-operator-gates-c028` is OPEN (carried in the active-head standing-gates; the batch-27 meta-phase ENACTED the probe as the batch-28 LEAD precisely because it is undischarged). No `RESOLVED`/`CLOSED` line covers the norm-axiom probe.
4. **Structural-block check:** the c080 D1 ruling (firm-on-positive-structure escape INAPPLICABLE) is the very gate this probe TESTS — the probe is the meta-phase-enacted dischargeability test, NOT a re-proposal of a structurally-blocked firm-promotion. The batch-27 friction-ledger re-weigh note explicitly GOs the scoped probe (NO-GO held only on the heavy ~30-file wave). `matrix-weighted-norm` is NOT on any STOP-PROPOSING negative list (verified: the list is `lu_solve`/`back_solve`/`ls-update-column`/4 NLEPS atoms/`apply_nonlinear_pencil`-HELD/L2-L3 BLAS-projector-smoother/`L3-solve_family`/`L2-fold_solve`/`L2-fe_assemble`/`map_solve`/`weak_form_term`-L2 — `matrix-weighted-norm` appears on NONE). **Framing:** audit-first / scoped-probe (NOT reflexive-harvest, NOT a firm-promotion forcing) — correct per the meta-phase enactment.

### D2 (`eigenfreq-qfactor-column-stale-cross-ref-land-clean`) — OPEN, 4/4 PASS
1. **File existence:** `book/src/feature/eigenfrequency-qfactor.L4.md` + `.L1.md` both EXIST (sed-read the cited line regions this plan).
2. **Maturity / stale-ref check (on-disk, the ground truth):** `eigenfrequency-qfactor.L4.md:36` reads `[`eigenmode.L4`](./eigenmode.L4.md) (**seed**)`; `:38` reads `[`eigenfreq_qfactor_reduce`](../L4/eigenfreq_qfactor_reduce.md) (**rough-in (test-coverage-bounded)**)`; `.L1.md:34` reads `[`eigenmode.L1`](./eigenmode.L1.md) (**seed**)`. The referenced entries' OWN on-disk maturity: `grep -m1 '^status:' eigenmode.L4.md` → `status: firm`; `eigenmode.L1.md` → `status: firm`; `eigenfreq_qfactor_reduce.md` → `firmness: firm`. All three references are STALE (label lags the on-disk firm maturity of the referent). The re-anchor is genuinely needed — not yet applied (c087 land-clean was `solve_family`-only scope; these eigenfreq-qfactor refs were OUT-OF-SCOPE and the integrator explicitly flagged `:38` as not-corrected).
3. **OQ-ledger RESOLVED-grep:** no `eigenfrequency-qfactor`-stale-cross-ref OQ is closed; this is a batch-27-migrated active-head item (#3), not a prior-cycle-discharged audit.
4. **Structural-block check:** none — a pure prose maturity re-anchor with ZERO status/count/SUMMARY change. Not blocked by any methodology gate; explicitly the batch-27-codified whole-book-grep residue. NOT on any STOP-PROPOSING list.

## Overlap analysis

- **D1 ↔ D2:** D1 touches `book/src/L1/matrix-weighted-norm.md` ONLY (its §Status + a `verified_against:`/findings block). D2 touches `book/src/feature/eigenfrequency-qfactor.L4.md` + `.L1.md` ONLY (3 prose maturity-label re-anchors). **No shared file, no shared operator name, no shared dep-map table, no shared index, no shared consolidated tally.** D1's HARD CONSTRAINT bars it from re-anchoring any consumer (so it cannot reach into the feature/ tree). NON-OVERLAPPING → parallel.
- No shared consolidated-index tally this cycle: D1 does not write any layer index (the probe touches only the operator's own §Status; the cascade — which WOULD touch `L1/index.md` counts — is explicitly deferred). D2 writes no index (ZERO count change). No single-index-owner partition needed.
- No cross-report forward-reference: neither dispatch references a slug the other authors this cycle.

## Sequencing schedule

**Single wave (both parallel):**
- **Wave 1:** D1 (`lowering-verifier`, the norm-axiom probe) ‖ D2 (`lifter`, the eigenfreq-qfactor land-clean).

Both are dependency-free and byte-disjoint. One `integrator-finalize` runs once at cycle end per the standing pipeline (planner → 2 dispatches → 2 critics → repairers → integrator-per-report ×2 serial → ONE integrator-finalize).

## Open questions / caveats

- **The LEAD is a PROBE, not a promotion.** I have been explicit in the D1 scope that BOTH outcomes (DISCHARGE / CONFIRM-CEILING) are acceptable clean landings, and that the cascade is NOT triggered in this dispatch regardless. If the verifier judges DISCHARGE, the c089 planner inherits `matrix-weighted-norm-full-firm-cascade-wave` as a now-worth-running dedicated own-cycle wave (the c071 reorg-wave precedent) — I have NOT pre-scheduled it (it is gated on D1's outcome, which is not known at plan time). If CONFIRM-CEILING, the √-cascade stays NO-GO with an explicit verdict and the c089/c090 frontier returns to the bottom-up width survey.
- **Deliberately TWO dispatches (LEAD + hygiene), no manufactured third.** Active-head #2 (`bottom-up-vocabulary-and-5-driver-l4-completeness`) is genuinely light: the in-scope firm-on-positive-structure route is EXHAUSTED for the all-primitives-firm cohort (the 3 reduce-verbs + `solve_family` all promoted across batches 26/27), and the priorities text itself states the next reduce-verb law-confidence pass is GATED on item #1 (a verb whose folded `matrix-weighted-norm`-family form is still rough-in cannot be firmed until the √-cascade discharges). The redirect forbids manufacturing a rectangular pull-up to fill a slot. A 5-driver→L4 survey would re-confirm the c082 AFFIRMED-COMPLETE picture (no new finding) — not worth a slot this cycle. If D1 returns DISCHARGE, c089 has a clear high-fan-out wave (the cascade) and the bottom-up frontier re-opens behind it.
- **Plan-candidate appended to `priorities.md`** (per cycle-planner write-authority): `matrix-weighted-norm-full-firm-cascade-wave-CONDITIONAL-on-c088-D1-DISCHARGE` recorded in the active-head standing-gates as the c089 conditional pick (it already exists as a trigger-gated Backlog item; I am sharpening its trigger to "c088 D1 returns outcome (a)" and noting it is the c089 candidate-on-DISCHARGE). No new Backlog tier item is minted (the cascade is pre-existing; only its trigger is now concrete).
- **No friction-pattern gap to surface for the next meta-phase** beyond what is already in-ledger: the firm-promotion-whole-book-grep drift is codified (batch-27 GO); the √-cascade dischargeability is being actively tested by this cycle's LEAD. The c088 plan is consistent with the current ledger.
