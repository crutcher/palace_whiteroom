---
agent: cycle-planner
invoked_at: 2026-06-06T001708Z
scope: cycle-110 dispatch plan
status: pending
---

# Cycle 110 dispatch plan

(SECOND primary cycle of meta-batch-35 — cycles 109/110/111; the batch-35 meta-phase fires AFTER cycle-111's finalize, as a SEPARATE dispatch aggregating 109/110/111. NO session restart since c109 [mid-batch]; context continuity holds. c109 landed `aaf36ed`/`d95eae2`.)

## Goals selected this cycle

THE LEAD = the batch-35 LEAD's **next, structurally-distinct tranche** (the c109 Group-B finding, OQ `l2-reduce-orthogonalize-cohort-itself-unreachable-blocks-theme-grounding`): **GROUND the L2/L3/L4 reduce / orthogonalize / chebyshev cohort** that is itself off-spine. The pre-dispatch trace established the crux concretely — **the reduce-to-scalar chain (`inner_product`/`nrm2`/`dot`/`gram`) is internally typed correctly downward but has NO inbound `depends-on` from any reachable consumer; the reachable consumers `L4/krylov-step` and `L4/gram_reduce` GENUINELY USE these reduce verbs in their bodies but only via `reference:` (free, navigational), never `depends-on`.** This is a textbook §2f GROUND-from-column situation: the faithful fix flips `reference`→`depends-on` (or adds a `composes`/`uses` `depends-on`) on the reachable consumer for verbs it truly composes. The orthogonalize/chebyshev legs are a partial sub-case (no L4-level op; the host is `L4/krylov-step →composes→ L2/orthogonalize` / the smoother chain via `preconditioning-framework`, itself garbage) — these may flip partially or route as a finding. FAITHFUL-PATH-OR-FINDING governs: type only the faithful edges, route the rest. Co-schedule the LOW disjoint hygiene item (axpy-family rank/edges typing) that closes OQ `l1-blas-leaves-axpy-family-lack-rank-frontmatter` and removes the vacuous-rank caveat on every `krylov-step`-class inbound BLAS edge.

## Linter baseline (this invocation, live tree)

`python3 tools/graded-stack-lint/graded_stack_lint.py --show-inbound`:
- **reachable from roots: 107** (matches the c109 finalize headline)
- **RANK VIOLATIONS: none** (`rank_violations: 0` — the single-number health signal, HELD 0 across the campaign 22 c094 → … → 0 c107/c108/c109)
- **STRONGER GARBAGE SIGNAL: 34** (typed-but-unreachable nodes — the measurable target; dominated by exactly the reduce/orthogonalize/iteration cohort below)
- **untyped (WARNING): 60** (pre-P1 lazy tail; warn-not-fail)
- **detritus: 152** (118 edge-untyped pre-P1 artifact + 34 stronger-signal)
- `unresolved_depends_on_targets: 0`, `promotion_frontier: 8`

The STRONGER GARBAGE SIGNAL (34) set is dominated by the reduce/iteration cohort at every layer: `L2/inner_product`, `L2/nrm2`, `L3/dot`, `L3/inner_product`, `L3/nrm2`, `L4/dot`, `L4/inner_product`, `L4/nrm2`, `L3/orthogonalize`, `L3/chebyshev`, `L3/krylov-step`, `L2/axpy/axpby/axpbypcz/scal/elementwise_product/reciprocal/normalize/jacobi-smoother`, `L3/*` mirrors, `L4/preconditioning-framework`. Plus `[garbage?]` (edge-typed-but-host-unreachable): `L2/orthogonalize ← L3/orthogonalize`, `L2/chebyshev-iteration ← L3/chebyshev`, `L2/gram`, `L2/incremental-least-squares`, `L4/chebyshev ← L3/chebyshev`, and the 5 carried Group-B L2-L1 themes (`inner-product-fold-specialization`, `chebyshev-iteration-fusion`, `gram-fold-specialization`, `incremental-least-squares-composition-lowering`, `orthogonalize-composition-lowering`).

## Per-op grounding table (the crux: faithful grounding host + edge per off-spine L2/L4 reduce op)

The trace established the reduce chains are **internally typed correctly downward** (`L4/inner_product →depends-on→ L3/inner_product →…→ L2/inner_product`; `L4/nrm2 →depends-on→ {L4/inner_product, L3/nrm2}`; `L4/dot →depends-on(specializes)→ {L4/inner_product, L3/dot}`). The whole chain is dead at the TOP: nothing reachable points `depends-on` into `L4/inner_product`/`L4/nrm2`/`L4/dot`. Grounding the TOP cascades the entire chain reachable in one stroke per chain.

| off-spine op (chain top) | proposed faithful grounding host (REACHABLE) | proposed edge | faithful? | expected flip |
|---|---|---|---|---|
| `L4/inner_product` (+ cascades `L3/inner_product`, `L2/inner_product`, `L1/dot`, and via the laid c109 edge `L2-L1/inner-product-fold-specialization`) | `L4/gram_reduce` (reachable ← capacitance/electrostatic/inductance/magnetostatic.L4) — its body computes `Mᵢⱼ = (Aⱼᵀ K Aᵢ)` = operator-weighted inner products; currently `reference: L4/inner_product` | **flip `reference`→`depends-on` (kind: `composes`/`reduces-with`)** `L4/gram_reduce → L4/inner_product` | **Y** (gram_reduce IS the operator-weighted Gram = inner-product reduction; its own §body names `inner_product` the reduce-to-scalar sibling) — confirm from gram_reduce.md:23-33 prose | inner_product chain (L4→L3→L2) flips; `L2-L1/inner-product-fold-specialization` flips (the c109 pre-laid edge) |
| `L4/nrm2` (+ cascades `L3/nrm2`, `L2/nrm2`, `L2/normalize`) | `L4/krylov-step` (reachable ← `L4/ksp_solve` ← driven/electrostatic/magnetostatic.L4) — body computes residual norm via `nrm2`/`dot` (krylov-step.md:99,142-161 `dot Ap p'`, `dot r' r'`, `nrm2`) | **add `depends-on` (kind: `composes`/`uses`)** `L4/krylov-step → {L4/nrm2 OR L4/dot OR L4/inner_product}` for the reduce verbs its body genuinely uses | **needs-read** — krylov-step body uses `dot`/`nrm2` (cited :99,:142-161); determine whether the faithful target is the L4 reduce ops, the L2 reduce ops, or L1/dot,L1/nrm2 (krylov-step already carries `depends-on L2/krylov-step` lowers-to; its body-primitive calls are the candidate `composes` edges). Producer reads + picks the faithful altitude. | nrm2 chain flips once a reachable consumer roots it (nrm2 = √inner_product, already `depends-on L4/inner_product` so the gram_reduce edge alone may cascade it; verify) |
| `L4/dot` (+ cascades `L3/dot`) | same as nrm2 — `L4/krylov-step` body `dot Ap p'`/`dot r' r'` (krylov-step.md:142,145,158,161) | `L4/krylov-step → L4/dot` (kind: `composes`) | **needs-read** (faithful: krylov-step body literally calls `dot`) | `L3/dot` flips (`L4/dot →depends-on→ L3/dot`) |
| `L2/orthogonalize` (← `L3/orthogonalize`, no L4 op) | `L4/krylov-step` — body uses `gs_orthog ∈ {MGS,CGS,CGS2}` orthogonalization (krylov-step.md:224,255,288 `orthogonalize-mutation-rotation` `orthog.hpp:41-88`) | `L4/krylov-step → {L3/orthogonalize OR L2/orthogonalize}` (kind: `composes`) | **needs-read / partial** — orthogonalization IS a genuine krylov-step constituent (the Arnoldi/Gram-Schmidt stage); faithful, BUT no L4-level orthogonalize op exists, so the edge crosses to L3/L2 directly. Verify the altitude convention (krylov-step's other body edges) before laying. | `L3/orthogonalize`, `L2/orthogonalize` flip; `L2-L1/orthogonalize-composition-lowering` flips (its host op grounds) |
| `L2/chebyshev-iteration` (← `L3/chebyshev`), `L4/chebyshev` (← `L3/chebyshev`) | `L4/preconditioning-framework` (ITSELF `[GARBAGE*]`) / `L4/ksp_solve` preconditioner slot — chebyshev is a SMOOTHER/preconditioner | candidate: a reachable solve's preconditioner-slot `depends-on` chebyshev | **needs-read / likely FINDING** — the chebyshev smoother is reached only through `preconditioning-framework` (itself garbage) and the jacobi-smoother cohort (also garbage). If no reachable solve column genuinely `depends-on` a concrete chebyshev/jacobi preconditioner via a faithful path, this is **absorbed-below-column detritus** → ROUTE AS FINDING + recommend baseline-exception (the c107 BC/divfree pattern), do NOT force a column→preconditioner edge. | likely does NOT flip this cycle — route |
| `L2/gram` (← nothing typed) | `L4/gram_reduce` if its body composes the L2 `gram` primitive (vs computing the reduction directly) | possible `L4/gram_reduce → L2/gram` (kind: `composes`) | **needs-read** — does gram_reduce decompose into the L2 `gram` op, or is `L2/gram` a separate Gram-Schmidt-internal primitive reached only via orthogonalize? Producer determines. | flips IF gram_reduce composes L2/gram; else flips via the orthogonalize leg or routes |
| `L2/incremental-least-squares` (← nothing typed) | GMRES least-squares update — reached via `krylov-step`'s LS-residual / Givens machinery (krylov-step.md:471-489 gmres LS-residual) | possible `L4/krylov-step → {L3 OR L2}/incremental-least-squares` | **needs-read** — the GMRES incremental-LS column-update IS a krylov-step (GMRES-variant) constituent; faithful if the body composition names it. Producer determines; route if absorbed into the krylov-step body without a separable op edge. | flips if faithful; else routes |

**Bias (per OQ recommendation + §2f preference order GROUND > ROUTE > DELETE):** GROUND from the reachable column/consumer where a faithful honestly-typed `depends-on` path exists. The reduce-to-scalar cohort (`inner_product`/`nrm2`/`dot`/`gram`) is the **high-confidence GROUND subset** — these are unambiguous genuine constituents of `gram_reduce` (operator-weighted inner products) and `krylov-step` (residual norms, CG/Arnoldi dot products). The orthogonalize leg is **faithful but altitude-sensitive** (no L4 op). The chebyshev/jacobi-smoother preconditioner leg is the **likely-FINDING subset** (reached only through `preconditioning-framework`, itself garbage — absorbed-below-column, the c107 pattern). A zero-edge or partial-edge outcome on the preconditioner leg is a LEGITIMATE landing, not a failed dispatch.

## Deliverable-presence verification (paste-inline evidence)

**D1 (grounding-host consumers — these are reachable firm nodes we ADD edges to; open by the c109 routed finding):**
```
EXISTS  book/src/L4/krylov-step.md             | rank: firm
EXISTS  book/src/L4/gram_reduce.md             | rank: firm
EXISTS  book/src/L4/inner_product.md           | ## Status (firm; rank in edges block)
EXISTS  book/src/L4/nrm2.md                     | ## Status
EXISTS  book/src/L4/dot.md                      | ## Status
EXISTS  book/src/L4/chebyshev.md               | ## Status
EXISTS  book/src/L4/preconditioning-framework.md| rank: firm
```
- D1 is **open by construction** — it is the explicit c109-routed next-tranche (OQ `l2-reduce-orthogonalize-cohort-itself-unreachable-blocks-theme-grounding`, finalize-flagged as the c110 LEAD). The work is ADDING `depends-on` edges that do not yet exist (verified: `L4/gram_reduce` carries `reference: L4/inner_product` NOT `depends-on`; `L4/krylov-step` carries `depends-on` only to `L2/krylov-step`+records, none to reduce verbs). NOT already discharged.
- OQ-ledger RESOLVED-grep: `grep 'l2-reduce-orthogonalize.*RESOLVED\|l2-reduce-orthogonalize.*CLOSED' scaffolding/open-questions.md` → no matches (OQ status is OPEN — confirmed at :1321).
- Structural-block check: NONE. The grounding hosts are reachable firm nodes; the §2f GROUND-don't-remove disposition (batch-34-codified) is the ENABLING gate, not a block. No `promotion_frontier` member is a grounding host (frontier = bicgstab/minres/eigsolve-convergence/deflate/deflate-composition/boundary-mode.{L0,L1,L4} — zero overlap with the reduce/orthogonalize cohort).

**D2 (axpy-family rank/edges typing — open by the c109 repairer-filed OQ):**
```
book/src/L1/axpy.md      first line: "# axpy"        (NO frontmatter — confirmed typed-no-rank)
book/src/L1/axpby.md     first line: "# axpby"       (NO frontmatter)
book/src/L1/axpbypcz.md  first line: "# axpbypcz"    (NO frontmatter)
```
- D2 is **open by construction** — OQ `l1-blas-leaves-axpy-family-lack-rank-frontmatter` filed c109 (:1311), status OPEN; the three leaves carry no `rank:`/`edges:` block (verified — they begin with `# axpy` etc.). Sibling leaves `apply_linop`/`dot`/`nrm2`/`scal` already carry `rank: firm`+`edges:` (the authoring template).
- OQ-ledger RESOLVED-grep: `grep 'l1-blas-leaves-axpy.*RESOLVED\|l1-blas-leaves-axpy.*CLOSED' scaffolding/open-questions.md` → no matches (OPEN at :1311).
- Structural-block check: NONE. These are firm-in-prose BLAS leaves resting on L1-L0 mutation-rotation themes; assigning `rank: firm`+`edges:` is mechanical mirroring of `L1/scal.md`/`L1/apply_linop.md`.

## Dispatches

1. **D1 (`layer-intro-author`, the reduce/orthogonalize/chebyshev cohort GROUNDING pass — THE LEAD, MEDIUM-HIGH, WAVE-1).**
   - **scope:** GROUND the off-spine reduce/orthogonalize/chebyshev cohort by adding faithful `depends-on` edges (or flipping existing `reference`→`depends-on`) from the REACHABLE consumers down into the reduce/iteration verbs they genuinely compose, per the per-op grounding table above. **FAITHFUL-PATH-OR-FINDING governs — verify each `composes`/`uses` relationship from the consumer's chapter prose + L0 cites BEFORE typing.** Concretely:
     - **(high-confidence GROUND) `L4/gram_reduce → L4/inner_product`:** flip the existing `reference: L4/inner_product` to a `depends-on` (kind: `composes` or `reduces-with`) — gram_reduce computes operator-weighted inner products (`Mᵢⱼ = Aⱼᵀ K Aᵢ`, gram_reduce.md:23-33). This roots the whole reduce-to-scalar chain (`L4/inner_product →…→ L2/inner_product`) and flips the c109-pre-laid `L2-L1/inner-product-fold-specialization` theme.
     - **(GROUND, read altitude) `L4/krylov-step → {reduce verbs}`:** krylov-step's body literally calls `dot Ap p'`, `dot r' r'`, `nrm2` (residual norm), `axpy`/`axpby`/`axpbypcz`/`scal` (krylov-step.md:99,142-161), and orthogonalization `gs_orthog` (:224,:255,:288). Add `depends-on` (kind: `composes`/`uses`) edges to the reduce/orthogonalize verbs it genuinely uses, **at the faithful altitude** (L4 reduce ops where they exist; L2/L3 orthogonalize since no L4 orthogonalize op exists). Determine the altitude convention from krylov-step's existing edges + the L2/krylov-step companion before laying.
     - **(likely FINDING) chebyshev/jacobi-smoother preconditioner leg:** `L2/chebyshev-iteration`/`L4/chebyshev`/`L2/jacobi-smoother` are reached only through `L4/preconditioning-framework` (itself `[GARBAGE*]`). If NO reachable solve column genuinely `depends-on` a concrete chebyshev/jacobi preconditioner via a faithful path, ROUTE AS A FINDING + recommend a baseline-exception ledger entry to the batch-35 meta-phase (absorbed-below-column, the c107 BC/divfree pattern). **Do NOT force a column→preconditioner edge.**
     - **(read) `L2/gram`, `L2/incremental-least-squares`:** determine whether `gram_reduce` composes the L2 `gram` primitive and whether the GMRES LS-update names a separable `incremental-least-squares` op in krylov-step's body; ground if faithful, route if absorbed.
     - After each edge tranche, re-run `python3 tools/graded-stack-lint/graded_stack_lint.py --show-inbound` and CONFIRM the cohort flips reachable + `rank_violations` HOLDS 0 + STRONGER GARBAGE SIGNAL drops. Report the exact flip count + which legs grounded vs routed.
   - **deps:** none.
   - **rationale:** OQ `l2-reduce-orthogonalize-cohort-itself-unreachable-blocks-theme-grounding` (the c109 Group-B finding, finalize-flagged as the c110 LEAD); plan item 1 `graded-stack-l2-l1-theme-cohort-grounding` next tranche; §2f GROUND-don't-remove. The measurable target is the STRONGER GARBAGE SIGNAL count (34) — grounding the reduce-to-scalar chain alone cascades ~8-12 nodes (inner_product/nrm2/dot at L2/L3/L4 + the 5 carried Group-B themes flip as their host ops ground). fan-out: MEDIUM-HIGH (the reduce verbs are the highest-reuse vocabulary in the artifact; this is the dominant remaining typed-but-unreachable cohort).

2. **D2 (`layer-intro-author`, axpy-family rank/edges typing — LOW, WAVE-1).**
   - **scope:** Give the three high-fan-out L1 BLAS leaves `book/src/L1/{axpy,axpby,axpbypcz}.md` a `rank: firm` token + an `edges:` block, mirroring `book/src/L1/scal.md` / `book/src/L1/apply_linop.md` (each `lowers-to` its `L1-L0/{axpy,axpby,axpbypcz}-mutation-rotation` theme + `cites-evidence` its L0 ranges + `reference` siblings + the `scaffolding/decisions/axpby-as-primitive.md` decision for axpby/axpbypcz). These are firm-in-prose; the typing is mechanical. Re-run the linter; confirm `untyped` drops 3, `rank_violations` HOLDS 0, and the inbound `depends-on` edges into the axpy-family (from `L2/krylov-step`, `L2/linear_combination`, `L2/axpy/axpby/axpbypcz`) become verified firm→firm rests (no longer vacuous-hold).
   - **deps:** none.
   - **rationale:** OQ `l1-blas-leaves-axpy-family-lack-rank-frontmatter` (c109 repairer-filed); plan item 2 `graded-stack-lazy-tail-typing`. fan-out: LOW (hygiene; removes the vacuous-rank caveat on every `krylov-step`-class inbound BLAS edge — which D1's krylov-step body edges may further exercise this very cycle).

## Overlap analysis

- **D1 write-set:** `L4/gram_reduce.md` + `L4/krylov-step.md` frontmatter, and (faithful-subset) `L4/{inner_product,nrm2,dot,chebyshev,preconditioning-framework}.md` IF a back-edge is needed there (D1 primarily ADDS outbound `depends-on` on the two reachable consumers; it does NOT need to edit the chain-interior reduce ops, which are already typed downward). Plus the report routes findings for the chebyshev/jacobi leg (no file edit).
- **D2 write-set:** `L1/{axpy,axpby,axpbypcz}.md` frontmatter ONLY.
- **D1 ∩ D2 = ∅** — D1 touches `L4/` consumer chapters; D2 touches `L1/` BLAS leaves. **DISJOINT write-sets.** No shared operator entry rewritten; no shared theme body. No consolidated-tally collision (both are per-page frontmatter `edges:` edits, NOT a layer-index cohort count — no `index.md` Working-Notes running count touched this cycle).
- **Logical coupling, NOT a write conflict:** D1's `L4/krylov-step` body may lay `depends-on` edges into the axpy-family (`L1/axpy`/`axpby`/`axpbypcz`) at the same time D2 types those leaves' `rank:`. Per OQ `l1-blas-leaves-axpy-family-lack-rank-frontmatter`, an inbound edge into an as-yet-untyped (typed-no-rank) target holds the rank invariant VACUOUSLY — no violation either way. After both land, the edges become verified firm→firm. No ordering requirement, no conflict — they compose.
- **No new-slug forward-reference** — every `depends-on`/`reference` target D1 and D2 lay is an EXISTING stable slug on disk (the reduce/orthogonalize/chebyshev chain ops + the L1-L0 mutation-rotation themes all verified present). The `cross-report-forward-reference-slug-divergence` guard does not apply (no co-dispatched report authors a not-yet-existing chapter the other references).

## Sequencing schedule

**SINGLE WAVE — D1 ∥ D2 (both parallel).** Disjoint write-sets (`L4/` consumer chapters vs `L1/` BLAS leaves); the only coupling is vacuous-rank-hold which is order-independent. The linter rescue is verified by the finalize step-5b re-run (`reachable` should climb past 107 as the reduce-to-scalar chain grounds; STRONGER GARBAGE SIGNAL should drop from 34; `rank_violations` HOLDS 0; `untyped` drops 3 from D2).

## Open questions / caveats

- **The reduce-cohort GROUND-vs-FINDING split is the deeper meta-phase call the OQ flags** — D1 is scoped to make the per-leg determination WITH citations (high-confidence GROUND for the reduce-to-scalar chain; faithful-but-altitude-sensitive for orthogonalize; likely-FINDING for the chebyshev/jacobi preconditioner leg). If D1 lands a partial result (reduce-to-scalar grounded, preconditioner leg routed-as-finding + baseline-exception recommendation), that is the EXPECTED and LEGITIMATE faithful-path-or-finding outcome — NOT a shortfall. The batch-35 meta-phase (after c111) makes the final ground-from-column-vs-absorbed-detritus ratification + records any baseline-exception ledger entries D1 recommends.
- **Altitude convention for body-composition edges** — krylov-step's body uses reduce verbs that exist at L2, L3, AND L4 (`inner_product`/`nrm2`/`dot` are present at all three; orthogonalize only at L2/L3). D1 must determine the faithful altitude for a `composes`/`uses` body edge (the convention is likely "point at the same-layer op where it exists, else the nearest-below" — but this is a SCHEME question D1 should surface if ambiguous). If D1 finds the altitude convention genuinely underspecified, that is a scheme-clarification finding for the batch-35 meta-phase (analogous to the c108-routed `lowering-theme-reachability-vs-well-foundedness-scheme-clarification`).
- **chebyshev/jacobi smoother + `L4/preconditioning-framework` reachability** — `preconditioning-framework` is itself `[GARBAGE*]`. If a reachable solve column SHOULD `depends-on` its preconditioner (a genuine constituent), grounding `preconditioning-framework` first would cascade the chebyshev/jacobi smoother cohort. D1 should check whether `L4/ksp_solve` (reachable) has a faithful preconditioner-slot `depends-on` to `preconditioning-framework` — if so, that single edge may be a higher-leverage GROUND than per-smoother edges. This is in D1's read scope; surface as a finding if the preconditioner-slot edge is faithful (it would be a clean §2f GROUND, not a forced edge).
- **STOP-PROPOSING honored** — no `promotion_frontier` member (`bicgstab`/`minres`/`eigsolve-convergence-reason-mapping`/`deflate`/`deflate-composition-lowering`/`boundary-mode.{L0,L1,L4}`) is proposed as a forward pick; `deflate`/`deflate-composition-lowering` correctly stay garbage (demand-gated, excluded by design). The disqualified-L3-backfill negative list (`lu_solve`/`back_solve`/`ls-update-column`/`nleps_*`) is not touched.
