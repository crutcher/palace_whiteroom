---
agent: cycle-planner
invoked_at: 2026-06-07T171246Z
scope: cycle-129 dispatch plan
status: pending
---

# Cycle 129 dispatch plan

## Goals selected this cycle

c129 is the **BATCH-CLOSING** cycle of meta-batch-41 (cycles 127/128/129; the batch-41 meta fires AFTER this finalize). c128's ASK-2 "B" capstone found the in-scope FEATURE-SURFACE SPINE **L4-COMPLETE** (no GAP) and recommended "wind to maintenance" — so c129 is naturally a **CONSOLIDATION + CLEANUP** cycle (NOT a new-vocabulary wave) that closes the c128-surfaced loose ends before the meta. The slate is a tight **2-dispatch coupled pair**: **D1** (layer-intro-author, the semantic-surface owner) PINS the operator-transformer-codomain adjudication into the now-landed semantics §1.3.1 — this GATES the sweep's scope; **D2** (lifter) then runs the closure-signature non-compliant-cohort rewrite sweep over the 2 L4 chapters + the index dep-map mirror rows + the `eliminate_bc` chapter↔index reconcile, AND folds in the two D3-surfaced stale-token cleanups (both touch `fe_assemble.md` / `lifecycle.L4.md`). NO RE fires (read-only adjudication + prose/spelling sweep — no maturity/edge/status change). The D1-routed BNF-promotion + discipline-bullet + whole-book compliance-cohort OQs are META work (NOT planned into c129).

## Deliverable-presence verification (paste-inline evidence)

All c129 dispatches resolve to named `book/src/` file paths → four-step sequence run per scope.

**D1 target — `book/src/semantics/index.md` §1.3.1 (PIN the adjudication):**
1. File existence — `-rw-rw-r-- 1 crutcher crutcher 45975 Jun  7 09:49 book/src/semantics/index.md` (EXISTS).
2. Maturity — the active semantic-management surface (the §0.1-governed surface; not a rank'd operator). §1.3.1 EXISTS on disk (`:130 #### 1.3.1 Closure-returning signatures (the calculus is high-order)`) with the paren-grouping convention + the `Op[…]`-vs-bare-closure table + the `op-with-params` introduction form (`:160`). The adjudication (`oq-highorder-operator-transformer-codomain-convention`) is NOT yet pinned: §1.3.1's existing text says "for `Op[…]` the brackets already group the in/out arrow, so the codomain is unambiguous without outer parens" (`:155`) — strongly implying bracketed-is-compliant, but it is NOT stated AS the operator-TRANSFORMER ruling. **Open: the explicit codomain ruling is not on disk** — D1 adds it. NOT a no-op.
3. OQ-RESOLVED grep — `grep -c '...oq-highorder-operator-transformer-codomain-convention.*RESOLVED...' scaffolding/open-questions.md` → `0` (no matches; OPEN — opened_at cycle-128, routed to D1/batch-41 meta).
4. Structural-block — no gate blocks; the convention is the active semantic-management surface (`SEMANTIC CONSOLIDATION` invariant, surface-owner = layer-intro-author). The adjudication is explicitly "must be pinned BEFORE the c129 sweep runs" (OQ text) — sequenced as wave-1.

**D2 targets — the closure-signature non-compliant cohort + 2 stale tokens:**
1. File existence —
   `-rw-rw-r-- ... book/src/L4/assemble_frequency_operator.md` (EXISTS);
   `-rw-rw-r-- ... book/src/L4/fe_assemble.md` (EXISTS);
   `-rw-rw-r-- ... book/src/L4/index.md` (EXISTS);
   `-rw-rw-r-- ... book/src/L4/eliminate_bc.md` (EXISTS);
   `-rw-rw-r-- ... book/src/feature/lifecycle.L4.md` (EXISTS).
2. Maturity / not-already-discharged — the non-compliant applied-spelling sites are CONFIRMED still on disk (NOT a no-op):
   - `assemble_frequency_operator.md:99` `:: FrequencyOperatorFamily[N] -> Scalar -> LinearOperator[N, N]` (signature codomain) + `:293` restated sig + `:106` `A2 : Scalar -> LinearOperator[N, N]` (closure-valued record field) + `:127` `fam.A2 — Scalar -> LinearOperator[N, N]` prose restatement — ALL present.
   - `fe_assemble.md:60` `fe_assemble :: FiniteElementSpace[N] -> [WeakFormTerm] -> LinearOperator[N, N]` + `:71`/`:35` `assemble_term :: FiniteElementSpace[N] -> WeakFormTerm -> LinearOperator[N, N]` — ALL present.
   - `L4/index.md` dep-map MIRROR rows: **OQ line-numbers `:61,62` DRIFTED** — on-disk re-localization shows `:61` = `eliminate_bc` chapter-list-narrative row (carries `eliminate_essential_bc :: LinearOperator[N,N] -> LinearOperator[N,N]` applied-spelling), `:62` = `fe_assemble` chapter-list-narrative row (carries `assemble_term :: ... -> LinearOperator[N,N]`). The dep-map TABLE rows are at `:110` (`assemble_frequency_operator`, already bracketed `LinOp[(S: ...), $S]`) + `:114` (`eliminate_bc`, already bracketed `LinOp[(S: ...), $S] -> ... -> LinOp[$S, $S]`). **D2 must on-disk re-localize ALL index sites (do NOT trust the OQ `:61,62`)** — the `codemap-read-range-plus-one-drift` / stale-line-number guard. The bracketed `:110`/`:114` rows are LIKELY compliant per the D1 adjudication (bracketed-is-compliant); only the applied-spelling narrative rows `:61`/`:62` + the chapter bodies need the sweep — D1's ruling settles which.
   - Stale token `fe-assemble-stale-mk-matrix-free-roadmap-goal-token`: `fe_assemble.md:16` (`constructs-via` frontmatter annotation says `mk_matrix_free_operator` is a "rank-0 `roadmap_goal`") + `:164` (matrix-free leaf prose: "[`mk_matrix_free_operator`](./mk_matrix_free_operator.md) (`roadmap_goal`, c126 D1)") — CONFIRMED on disk; `mk_matrix_free_operator` is `firm` since c127. Present, stale, NOT discharged.
   - Stale token `lifecycle-l4-stale-boundary-mode-rough-in-token`: `feature/lifecycle.L4.md:72` (the dispatch table cell `eigenmode / driven / transient / boundary-mode ... | firm / firm / firm / rough-in`) — CONFIRMED on disk; `feature/boundary-mode.L4.md:6` is `rank: firm`. Present, stale, NOT discharged.
3. OQ-RESOLVED grep — `grep -c '...{the four c129 slugs}...RESOLVED...' scaffolding/open-questions.md` → `0` (no matches; all OPEN, opened_at cycle-128).
4. Structural-block — no gate blocks. The sweep is prose/signature-spelling fidelity (NO status/rank/edge change; the OQ itself says "NOT a semantic-ambiguity emergency … style/fidelity"). The two stale tokens are maturity-snapshot drift (firm-on-disk nodes mis-described), no gate. **Sequenced wave-2 (dep D1)** because the `eliminate_essential_bc` / index `:114` scope is GATED on D1's adjudication.

**Confirmed-OPEN (no STOP-PROPOSING-list match; not re-proposing landed work).** None of the c129 scopes match the STOP-PROPOSING negative list (`lu_solve`/`back_solve`/`ls-update-column`/`nleps_*`). Verified against the cycle-record tail (c126/c127/c128 the last 3 rows; the c128 finalize landed the `L4/index.md:119` `mk_matrix_free_operator` row as the SEPARATE-TRACK finalize lockstep — explicitly NOT in the c129 cohort per the OQ; verified on disk `:119` already reads `Op[Tensor[(N: ...)] → Tensor[(N: ...)]]`, done, NOT re-swept).

## Dispatches

- **D1 [LEAD] (`layer-intro-author`, WAVE-1) — PIN the operator-transformer-codomain adjudication into semantics §1.3.1 (`oq-highorder-operator-transformer-codomain-convention`).**
  - **scope:** Edit `book/src/semantics/index.md` §1.3.1 to PIN the ruling: does a codomain written in the bracketed operator-value spelling `LinOp[(S: ...), $S]` / `Op[τ_in → τ_out]` ALREADY satisfy the closure-grouping convention (the brackets group the in/out arrow → YES, already-compliant — the reading §1.3.1:155 already strongly implies and the critic flagged the chapter form as *clearly* compliant), or does the directive want operator-CONSTRUCTOR / operator-TRANSFORMER codomains *additionally* paren-grouped even when bracketed? Add a short explicit clause (and/or an operator-TRANSFORMER row to the §1.3.1 `Op[…]`-vs-bare-closure table) settling: **(a)** whether `eliminate_essential_bc` (`L4/eliminate_bc.md:83-84` + index `:114`, an operator-in→operator-out TRANSFORMER whose codomain is already bracketed `LinOp[$S, $S]`) stands as ALREADY-COMPLIANT or is in-scope for the sweep; **(b)** the canonical fix-target spelling — `Op[Tensor[$N] → Tensor[$N]]` (explicit arrow inside brackets) vs the square-operator `LinOp[(N: ...), $N]` form (`semantics/index.md:95` already sanctions `LinOp[(S: ...), $S]` as the square-operator calculus spelling). D1 OWNS the semantic surface; the ruling D1 pins is the authoritative scope-gate D2 consumes. Append a RESOLUTION MARKER for `oq-highorder-operator-transformer-codomain-convention` (header-close = batch-41 meta unify-authority). Prose-only on the surface; NO L4 chapter edits (those are D2). **fan-out: HIGH (governs how the whole closure-signature cohort states its codomains; the scope-gate for D2 and for the meta's whole-book compliance-cohort decision).** Plan-tag `semantic-consolidation`.
  - **deps:** none.

- **D2 (`lifter`, WAVE-2, dep D1) — the closure-signature NON-COMPLIANT-cohort lifter sweep + 2 stale-token cleanups (`highorder-signature-noncompliant-cohort-c129-lifter-sweep` + `fe-assemble-stale-mk-matrix-free-roadmap-goal-token` + `lifecycle-l4-stale-boundary-mode-rough-in-token`).**
  - **scope:** Reading D1's now-pinned §1.3.1 adjudication, rewrite the non-compliant high-order/closure-returning signatures to the §1.3.1-compliant operator-value spelling (the canonical target spelling D1 pins — `Op[Tensor[$N] → Tensor[$N]]` and/or `LinOp[(N: ...), $N]`), citing the now-authoritative §1.3.1 + the fixed `mk_matrix_free_operator` exemplar (`L4/mk_matrix_free_operator.md:60`) as the template:
    - **`assemble_frequency_operator.md`**: the signature codomain `:99` + restated sig `:293` (`-> LinearOperator[N, N]`), and the **closure-valued record field** `FrequencyOperatorFamily.A2` `:106` + its shape-contract prose restatement `:127` (`Scalar -> LinearOperator[N, N]` — the clearest closure-RETURNING field). JUDGE per D1's ruling whether the plain operator-VALUE record fields `K/C/M : LinearOperator[N,N]` (`:103-105`) stay as-is (they are operator-VALUES, not closure-returning — the convention governs closure-RETURNING signatures) — likely UNCHANGED; do not over-rewrite plain value fields.
    - **`fe_assemble.md`**: `fe_assemble :: ... -> LinearOperator[N, N]` `:60` (the assemble-fold's assembled-operator product) + `assemble_term :: ... -> LinearOperator[N, N]` `:35`/`:71` (the libCEED opaque operator-VALUED leaf).
    - **`L4/index.md` dep-map MIRROR rows** — **ON-DISK RE-LOCALIZE; do NOT trust the OQ's `:61,62`** (drifted). Sweep the applied-spelling sites in the chapter-list narrative rows (`fe_assemble` narrative ~`:62` `assemble_term :: ... -> LinearOperator[N,N]`; the `eliminate_bc` narrative ~`:61` `eliminate_essential_bc :: LinearOperator[N,N] -> LinearOperator[N,N]`) to match the swept chapters. The dep-map TABLE rows `:110` (`assemble_frequency_operator`, already bracketed `LinOp[(S: ...), $S]`) + `:114` (`eliminate_bc`, already bracketed `LinOp[(S: ...), $S] -> ... -> LinOp[$S, $S]`) are LIKELY already-compliant per D1's adjudication — touch them ONLY if D1's ruling says the bracketed form needs the fix. The `:119` `mk_matrix_free_operator` row is DONE (c128 finalize) — do NOT touch.
    - **`eliminate_bc` chapter↔index internal-consistency reconcile**: bring `eliminate_bc.md:83-84` (bracketed `LinOp[(S: ...), $S] -> ... -> LinOp[$S, $S]`) and the index `:114` row into agreement with whatever D1's adjudication settles (if bracketed-is-compliant, the index narrative `:61` applied-spelling is what changes; if not, both move to the canonical spelling). State the reconcile explicitly.
    - **Stale-token cleanup 1** (`fe-assemble-stale-mk-matrix-free-roadmap-goal-token`): `fe_assemble.md:16` (`constructs-via` frontmatter comment "rank-0 `roadmap_goal`") + `:164` (matrix-free leaf prose "(`roadmap_goal`, c126 D1)") → update to `firm` (c127). The `constructs-via` `reference`-class relationship is correct regardless; only the maturity TOKEN is stale. NO edge/rank change.
    - **Stale-token cleanup 2** (`lifecycle-l4-stale-boundary-mode-rough-in-token`): `feature/lifecycle.L4.md:72` dispatch-table cell `... boundary-mode ... | firm / firm / firm / rough-in` → flip the `boundary-mode` maturity to `firm` (`feature/boundary-mode.L4.md:6` is `rank: firm`). One-cell token fix.
  - **Discipline:** prose/signature-spelling FIDELITY sweep + 2 maturity-token corrections — NO status/rank/edge/maturity-of-DAG-node change (the tokens describe nodes already firm on disk; this corrects the DESCRIPTION, not the node). Replace-in-place; do not introduce new edges. Cite §1.3.1 (don't restate the convention — USE+LINK per SEMANTIC CONSOLIDATION). **fan-out: MEDIUM (closes the c128 closure-signature cohort + the two firm-on-disk stale tokens; honesty/fidelity + completes the §1.3.1 rollout for the in-scope L4 constructors).** Plan-tag `semantic-consolidation` + `index-count-hygiene`.
  - **deps:** D1 (the adjudication ruling gates the `eliminate_essential_bc` / index `:114` scope + the canonical fix-target spelling).

## Overlap analysis

- **D1 ↔ D2:** COUPLED, DISJOINT files. D1 edits ONLY `book/src/semantics/index.md` §1.3.1 (the semantic surface) + appends an OQ RESOLUTION MARKER. D2 edits ONLY the L4 chapters (`assemble_frequency_operator.md`, `fe_assemble.md`, `eliminate_bc.md`), `L4/index.md`, and `feature/lifecycle.L4.md`. **No shared file region.** The dependency is purely INFORMATIONAL — D2 reads D1's pinned ruling to settle the `eliminate_essential_bc` / bracketed-codomain scope — so D2 sequences in wave-2. This is a clean role-ownership partition (semantic-surface authoring = layer-intro-author; the consuming sweep = lifter), NOT an artifact-region conflict.
- **No consolidated-tally / shared-index collision:** D2 sole-owns every L4-chapter + `L4/index.md` edit; D1 sole-owns the semantic surface. No layer index carries a consolidated running-count that both touch (the sweep changes no firm-count — no node maturity changes; the two stale tokens only correct DESCRIPTIONS of already-firm nodes). No floor-landing→adjacent-re-anchor coupling (no floor lands). No cross-report forward-reference to a NEW slug (D2 references the already-on-disk `L4/mk_matrix_free_operator.md:60` exemplar + the D1-pinned §1.3.1 — both existing, no new-slug guess risk).

## Sequencing schedule

- **Wave 1:** D1 (layer-intro-author — pin the §1.3.1 adjudication). Must land first because D2's `eliminate_essential_bc` / index `:114` / canonical-fix-spelling scope is gated on the ruling.
- **Wave 2:** D2 (lifter — the cohort sweep + 2 stale-token cleanups), after D1's report lands so the per-report integrator wires D2's reads against the pinned §1.3.1.
- **ONE `integrator-finalize`** at cycle end (rebuild book + commit + push + the batch-CLOSING housekeeping; the batch-41 meta is a SEPARATE dispatch/commit AFTER this finalize).

## RE-discharge tracking (batch-41, c129)

**NO RE fires this cycle.** Both dispatches are prose/signature-fidelity + maturity-token corrections — no maturity/rank/edge change to any DAG node, so the linter baseline HOLDS unchanged: `files=385, typed=324, untyped=61, roots=45, reachable=163, reference_reachable=247, rank_violations=0, unresolved_depends_on_targets=0, promotion_frontier=10, detritus=122, true_detritus=50`. RE4 stays consumer-gated (no GMRES-variant column c129); residual RE11 (deliberate-reference-only-reachable cohort) premises HOLD — no new deliberate-reference-only-reachable node, so any `detritus`/STRONGER climb would flag (§2g escalate-guard). The two stale-TOKEN fixes correct DESCRIPTIONS of already-firm nodes (`mk_matrix_free_operator` firm c127, `boundary-mode.L4` firm) — they do NOT move a node's rank, so no RE/reachability recompute is owed.

## Open questions / caveats

- **The OQ line-numbers `:61,62` for the `L4/index.md` mirror rows DRIFTED** (they now point at chapter-list-narrative rows, with the dep-map TABLE rows at `:110/:114`). The plan instructs D2 to ON-DISK re-localize all index sites and not trust the OQ numbers (the stale-line-number / `codemap-read-range-plus-one-drift` guard class). Flagging so the orchestrator pre-supplies the on-disk-confirmed sites if it briefs D2.
- **The adjudication outcome shapes D2's exact edit-set.** If D1 pins "bracketed-is-compliant" (the likely reading per §1.3.1:155 + the critic's note), then `eliminate_essential_bc` (`:83-84` + index `:114`) + the bracketed index `:110` rows stand as-is, and D2's sweep is narrower (only the applied-spelling `-> LinearOperator[N,N]` sites in the two chapter bodies + the narrative index rows `:61/:62`). If D1 pins "additionally paren-group even when bracketed," the sweep widens. D2's scope is written to handle either ruling (the reconcile is stated as "agree with whatever D1 settles"). No further human decision needed — the adjudication IS the decision, owned by D1 this cycle.
- **For the batch-41 meta (NOT c129 work):** the c128 D1-routed OQs are META-owned and deliberately NOT planned into c129 — (a) `closure-signature-introduction-form-into-bnf-and-role-discipline-bullet` (promote the §1.3.1 `op-with-params {…}` introduction form INTO the §1.3 BNF `e ::=` production + add a harvester/abstractor USE+LINK discipline bullet); (b) `closure-signature-l4-constructor-restatement-compliance-cohort-sweep` (the whole-book L4-constructor compliance sweep — the same shape as the named-shape-groups restatement-cohort OQ). The meta also judges the ASK-2 "B" L4-COMPLETE capstone → "wind to maintenance" decision (E = maintenance the live fallback per `project_ask2_forward_direction_batch41`), and CLOSEs OQ `matrix-free-operator-apply-l4-placeholder-now-stale` (discharged c128 D4). I have NOT planned any of these into c129.
- **Budget:** 2 dispatches (well under the 12 cap). c129 is deliberately LIGHT — a batch-closing consolidation cycle, no new vocabulary, the capstone already declared the spine L4-complete. Folding D3's two stale tokens INTO D2 (they touch the same `fe_assemble.md` / `lifecycle.L4.md`) keeps the slate tight rather than spinning a 3rd low-value dispatch (couple-the-touching-edits judgment).
