---
agent: layer-intro-author
invoked_at: 2026-06-07T210728Z
scope: L4 index — add roadmap_goal dep-map row for sharding-decompose-reduce; resolve 2 flagged OQs
status: pending
integrated_at: 2026-06-07T220000Z
integration_commit: PLACEHOLDER_SHA
integration_notes: |
  Applied clean by integrator-per-report (staging row `l4-index-sharding-row`, status applied). Landed one `roadmap_goal` dep-map row for `sharding-decompose-reduce` into book/src/L4/index.md's "Data-algebra combinators & named verbs" table, alpha-positioned between `nrm2` and `sparameter_reduce` (mirroring the `mk_matrix_free_operator` rank-0 precedent at index.md:119); signature cell's Haskell list-comprehension bar ESCAPED `\|`. Resolved 2 OQs (L4-index-listing → YES; SUMMARY-placement → RATIFIED). FINALIZE BUILD-VERIFY PASSED: cargo make book EXIT 0; the new row renders as a well-formed 5-cell (5-column) `<td>` table row in book/book/html/L4/index.html — the escaped `\|` did NOT split it into an extra column; 0 katex-in-pre blocks (no `$`-sigil collision). Graded-stack lint counts UNCHANGED vs c134 (`files=386, rank_violations=0, unresolved=0` — a `reference`-class navigational row adds NO typed-graph `depends-on` edge).
---

# CYCLE: L4 index — `sharding-decompose-reduce` roadmap_goal dep-map row

## Summary

Cycle-134 landed the new rank-0 `roadmap_goal` chapter `book/src/L4/sharding-decompose-reduce.md` (the sharding-as-decomposition-abstraction MATH sketch) and wired it into `book/src/SUMMARY.md` (between `nrm2` and `sparameter_reduce` in the "Data-algebra combinators & named verbs" group). The abstractor flagged two OQs. This dispatch:

1. **Adds the parallel `roadmap_goal` dep-map row** to `book/src/L4/index.md`'s "Data-algebra combinators & named verbs" table, alpha-positioned between the `nrm2` row (`index.md:120`) and the `sparameter_reduce` row (`index.md:121`) — matching the SUMMARY placement — following the `mk_matrix_free_operator` rank-0-row precedent (`index.md:119`) exactly. The row reflects `roadmap_goal` status and `reference`-class composition of the firm reduce roots.
2. **Resolves OQ-1** (does the L4 index dep-map want a `roadmap_goal`-row listing): **YES** — the L4 index dep-map already carries `roadmap_goal`-class rows (`mk_matrix_free_operator`, `index.md:119`), so the new chapter gets a parallel row per that precedent. SUMMARY + frontmatter wiring is NOT sufficient on its own; the index dep-map is the navigational mirror and carries rank-0 rows.
3. **Resolves/ratifies OQ-2** (SUMMARY placement alpha-correct): **CONFIRMED** — `SUMMARY.md:81-83` reads `nrm2` → `sharding-decompose-reduce` → `sparameter_reduce`; `nr` < `sh` < `sp`, alpha-correct within the group. No change needed; the c134 correction stands.

This is a **navigational/index touch only**: NO maturity/rank/edge change to any node, NO new typed-graph `depends-on` edge. The dep-map row is a `reference`-class navigational listing (the L4 index frontmatter is `kind: navigational-container (layer index)`, `reference`-edges-only, NO `rank:`). `rank_violations=0` is preserved (a roadmap_goal navigational row introduces no `depends-on` edge; the rank linter enforces `rank(u) ≤ min(deps)` over `depends-on` edges only — see the chapter's own §Status `index.md`-sibling reasoning and `sharding-decompose-reduce.md:227-231`).

## Proposed changes

```edit:book/src/L4/index.md
[old]: | [`nrm2`](./nrm2.md) | `nrm2 :: Tensor[(S: ...)] -> Scalar`; `nrm2 x = sqrt (abs (inner_product x x))`. The kept named 2-norm verb `‖x‖₂ = √⟨x, x⟩` — `√ ∘ abs ∘ inner_product` at the diagonal `y = x`. A **CONSUMER** of the combinator, NOT a fold member (split-additivity lost under `√` — the do-NOT-merge guard). Result always real ≥ 0. Pure value-producing scalar map on a pure reduction — no `Solve` monad / carry / predicate. | Concepts: `black-box-vs-accelerated-kernels` (§2 kept-named-abstraction rises), `nrm2` (BLAS-1 heritage). L4 rows: **consumes** [`inner_product`](./inner_product.md) at the diagonal (the permitted dual — `nrm2` is a consumer verb, NOT a member; combinator law 5 PSD-at-diagonal is the √ well-definedness witness); sibling named verb [`dot`](./dot.md). | L3 [`nrm2`](../L3/inner_product.md#consumer-nrm2-and-matrix-weighted-norm) by **identity-in-form on the body** (value-thread-isomorphic; **no dedicated L4>L3 theme** — in-line §"Downward to L3", the in-line-marker route); substantive downward content is the L1>L0 [`nrm2-mutation-rotation`](../L1-L0/nrm2-mutation-rotation.md) (the four-stage chain + the `abs`-guard classification) + the inner reduction's L2>L1 fold-specialization. | `firm` (cycle-069 D2 — risen from firm [`L3/nrm2`](../L3/inner_product.md#consumer-nrm2-and-matrix-weighted-norm) as the diagonal consumer of firm [`inner_product`](./inner_product.md); the kept named abstraction that rises to L4 as a named consumer verb, `concepts/black-box-vs-accelerated-kernels.md` §2; the five norm laws are syntactic identities / standard norm facts / syntactic-identity escape; the `abs` defensive guard preserved) |
| [`sparameter_reduce`](./sparameter_reduce.md) |
[new]: | [`nrm2`](./nrm2.md) | `nrm2 :: Tensor[(S: ...)] -> Scalar`; `nrm2 x = sqrt (abs (inner_product x x))`. The kept named 2-norm verb `‖x‖₂ = √⟨x, x⟩` — `√ ∘ abs ∘ inner_product` at the diagonal `y = x`. A **CONSUMER** of the combinator, NOT a fold member (split-additivity lost under `√` — the do-NOT-merge guard). Result always real ≥ 0. Pure value-producing scalar map on a pure reduction — no `Solve` monad / carry / predicate. | Concepts: `black-box-vs-accelerated-kernels` (§2 kept-named-abstraction rises), `nrm2` (BLAS-1 heritage). L4 rows: **consumes** [`inner_product`](./inner_product.md) at the diagonal (the permitted dual — `nrm2` is a consumer verb, NOT a member; combinator law 5 PSD-at-diagonal is the √ well-definedness witness); sibling named verb [`dot`](./dot.md). | L3 [`nrm2`](../L3/inner_product.md#consumer-nrm2-and-matrix-weighted-norm) by **identity-in-form on the body** (value-thread-isomorphic; **no dedicated L4>L3 theme** — in-line §"Downward to L3", the in-line-marker route); substantive downward content is the L1>L0 [`nrm2-mutation-rotation`](../L1-L0/nrm2-mutation-rotation.md) (the four-stage chain + the `abs`-guard classification) + the inner reduction's L2>L1 fold-specialization. | `firm` (cycle-069 D2 — risen from firm [`L3/nrm2`](../L3/inner_product.md#consumer-nrm2-and-matrix-weighted-norm) as the diagonal consumer of firm [`inner_product`](./inner_product.md); the kept named abstraction that rises to L4 as a named consumer verb, `concepts/black-box-vs-accelerated-kernels.md` §2; the five norm laws are syntactic identities / standard norm facts / syntactic-identity escape; the `abs` defensive guard preserved) |
| [`sharding-decompose-reduce`](./sharding-decompose-reduce.md) | `subdomain_reduce :: (Tensor[(Sb: ...)] -> r) -> Partition -> Tensor[(S: ...)] -> r`; `subdomain_reduce reduce P field = mconcat [reduce (restrict_to_block b field) \| b <- blocks P]`. The **sharding-as-decomposition-abstraction MATH**: split a global tensor-field reduction over a PARTITION of its index set into per-block sub-problems, run a firm reduce verb on each block independently, and recover the global result by the firm verbs' **standing concatenation-homomorphism** — `subdomain_reduce = mconcat ∘ map (reduce ∘ restrict_to_block) ∘ blocks`. Adds NO new reduction algebra: a partition is a `++`-decomposition the firm reduce verbs' homomorphism law already describes. **Claim-free `roadmap_goal`** (no positive L4 source claim; speculative future-direction sketch — the MATH of decomposition, NOT the message-passing). | Reference (free, navigational — NOT rank-constrained; a rank-0 node composing firm roots under `reference:` ONLY, NEVER `depends-on`, so no rank violation): the firm reduce roots whose standing concatenation/split homomorphism IS the recovery law — [`domain_energy_reduce`](./domain_energy_reduce.md) (the STRUCTURAL MODEL, a firm domain-RESTRICTED reduce composing firm primitives), [`inner_product`](./inner_product.md) (split-additivity / shape-concatenation monoid-homomorphism), [`linear_combination`](./linear_combination.md) (concatenation-homomorphism), [`gram_reduce`](./gram_reduce.md) (per-pair map-homomorphism over the family grid), [`L2/gram`](../L2/gram.md) (the L2 Gram form under the family-pair reduction). `pulled-by`: the human batch-43 (C) sharding-MATH gate + the c133 WAVE-1 hard non-destabilization probe CLEAR. Deferred-future MECHANISM cited NOT lifted (DIRECTIVE-1): `geodata.cpp` partitioning, `rap.{hpp,cpp}` `ParOperator`/RAP. | The reduction case lowers to the firm reduce verbs' existing L3 forms by the **standing homomorphism** (no new lowering theme — the recovery IS the firm verbs' law applied to a partition's blocks); the per-sub-domain SOLVE/assemble generalization + the distributed `mconcat` collective are the deferred MECHANISM (cited, NOT lifted). | `roadmap_goal` (rank 0; c134 — the batch-43 (C) sharding-MATH gate sketch, landed claim-free with its firm-root composition wired `reference`-class so `rank_violations: 0` holds; the laws are intended target shape DERIVED from standing firm homomorphism laws, not asserted; promotes when a single-machine-valid domain-decomposition / additive-Schwarz consumer pulls the restrict/compose abstraction by name and the `reference` edges are re-examined for which become blocking `depends-on` at that rank. Rank-0 nodes do not bump the firm count) |
| [`sparameter_reduce`](./sparameter_reduce.md) |
```

## Supporting evidence

- **Precedent for a `roadmap_goal` row in the L4 index dep-map:** `book/src/L4/index.md:119` — the `mk_matrix_free_operator` rank-0 row, format-matched exactly (signature in code spans + prose; `Reference (free, navigational — NOT rank-constrained): …`; Lowers-to via the `reference`-class relationship; `Status` cell explicitly `roadmap_goal (rank 0; …)` with the `rank_violations: 0`-holds reasoning and the "Rank-0 nodes do not bump the firm count" closer). This is the answer to OQ-1.
- **The new chapter** `book/src/L4/sharding-decompose-reduce.md` (c134-landed): frontmatter `rank: roadmap_goal` / `status: roadmap_goal` / `edges.reference: [L4/domain_energy_reduce, L4/inner_product, L4/linear_combination, L4/gram_reduce, L2/gram]` (`:4-12`); signature `subdomain_reduce :: (Tensor[(Sb: ...)] -> r) -> Partition -> Tensor[(S: ...)] -> r` (`:48-54`); the `subdomain_reduce = mconcat ∘ map (reduce ∘ restrict_to_block) ∘ blocks` framing (`:57`); the `reference`-only / no-`depends-on` / `rank_violations=0`-preserved reasoning (`:28-35`, `:167-172`, `:227-231`); deferred-MECHANISM-not-lifted (`:187-207`, DIRECTIVE-1). The row's signature, dependency-set, lowers-to, and status cells are transcribed from this chapter (the index row must match its owning chapter's signature — done).
- **SUMMARY placement (OQ-2 ratification):** `book/src/SUMMARY.md:81-83` — `nrm2` → `sharding-decompose-reduce` → `sparameter_reduce`, alpha-correct within the "Data-algebra combinators & named verbs" group (`SUMMARY.md:70`). `nr` < `sh` < `sp`. The c134 correction stands; the proposed dep-map row insertion matches this placement (between `index.md:120` `nrm2` and `index.md:121` `sparameter_reduce`).
- **`rank_violations=0` preserved:** the row is a navigational `reference`-class listing in a `kind: navigational-container (layer index)` page (`index.md:2-10`, `reference`-edges-only, NO `rank:`). It introduces NO `depends-on` edge. The rank linter enforces `rank(u) ≤ min over depends-on deps of rank(v)` over `depends-on` edges only; a `reference` edge from a rank-0 node to firm (rank-3) reduce roots carries no rank constraint (`sharding-decompose-reduce.md:227-231`). No node's maturity/rank/edge-typing changes.

## Open questions / caveats

- **OQ-1 — RESOLVED (close):** "Does `book/src/L4/index.md`'s dep-map want a `roadmap_goal`-row listing for `sharding-decompose-reduce`, or is SUMMARY + frontmatter wiring sufficient?" → **YES, give it a parallel row.** The L4 index dep-map already carries `roadmap_goal`-class rows (`mk_matrix_free_operator`, `index.md:119`); the new chapter gets a parallel row per that precedent, alpha-positioned between `nrm2` and `sparameter_reduce`. SUMMARY + frontmatter wiring alone leaves the index dep-map (the navigational mirror) drifted from the on-disk chapter set. (This proposed-changes block lands the row.)
- **OQ-2 — RATIFIED (close):** "Confirm the SUMMARY placement is alpha-correct (between `nrm2` and `sparameter_reduce`)." → **CONFIRMED**, `SUMMARY.md:81-83`; `nr` < `sh` < `sp`. No change to SUMMARY needed; the c134 correction stands.
- **No new typed-graph edge introduced.** Per the dispatch scope, this is a navigational/index touch only. The chapter's own `edges.reference:` block (authored at c134) already carries the firm-root references in the typed graph; the index row mirrors them navigationally. No `depends-on` edge is added anywhere; `rank_violations=0` and the reachability GC posture are unchanged (the chapter is `reference`-only-reachable by design as a rank-0 roadmap_goal — its genuine grounding is the future domain-decomposition consumer noted in `sharding-decompose-reduce.md:208-214`, not a forced edge).
- **Cohort note (no action this dispatch):** the "Data-algebra combinators & named verbs" group now holds two rank-0 `roadmap_goal` rows (`mk_matrix_free_operator`, `sharding-decompose-reduce`) interleaved alphabetically among the firm verbs. The §Vocabulary-cohort "Firm at L4" prose count is unaffected (rank-0 nodes do not bump the firm count, per both rows' status cells) — no tally edit needed.
