---
agent: layer-intro-author
invoked_at: 2026-06-04T053300Z
scope: cycle-091 D4 — feature-column re-evaluation under the OWN-COMPOSITION rule, coupled to the matrix-weighted-norm firm flip (D1) + the domain_energy_reduce/gram_reduce reduce-verb verdicts (D3); SOLE owner of book/src/feature/index.md
status: pending
integrated_at: 2026-06-04T080000Z
integration_commit: PLACEHOLDER_SHA
integration_notes: "cycle-091 D4 (batch-29 LEAD cascade, LAST report). energy-fields output-product column seed → firm ×3 levels (own composition all-firm — domain_energy_reduce c091 + participation_ratio c077); feature spine 6→7 firm / 6→5 seed. 4 columns capacitance/inductance/electrostatic/magnetostatic STAY seed on gram_reduce→bilinear-form residual gate. D4 applied a discretionary magnetostatic.L4:56 stale-NO-GO-HELD symmetry fix; finalize caught the residual line-41 symmetric twin D4 omitted (the friction signal). Applied clean by integrator-per-report; build clean (cargo make book exit 0, 1 finalize build-repair)."
inputs:
  - reports/2026-06-04T053300Z-cycle-planner-cycle-091/CYCLE.md (the plan — D4 scope)
  - reports/2026-06-04T053300Z-lowering-verifier-cycle-091-reduce-verb-rejudgment/CYCLE.md (D3 — domain_energy_reduce → FIRM; gram_reduce → STAYS rough-in)
  - book/src/feature/index.md + the energy-fields / capacitance / inductance / electrostatic / magnetostatic column files + output-product.md (on-disk this dispatch)
---

# CYCLE: cycle-091 D4 — feature-column re-evaluation + feature/index reconciliation

## Summary

D3 returned the two load-bearing reduce-verb verdicts: **`domain_energy_reduce` → FIRM** (both folded primitives now firm L1 — matrix-weighted-norm c091 + participation_ratio c077 — firm-on-positive-structure escape) and **`gram_reduce` → STAYS `rough-in (test-coverage-bounded)`** (residual `bilinear-form` off-diagonal gate). Under the OWN-COMPOSITION promotion rule I re-evaluated every feature column whose OWN composition's firmness changed this cycle:

- **`energy-fields` → FLIP `seed` → `firm`** (× 3 levels L4/L1/L0). Its OWN composition is now all-firm: own reduce verb `domain_energy_reduce` firm (D3), own folded `matrix-weighted-norm` firm (D1), own folded `participation_ratio` firm (c077). The producing driver columns are SIBLING references (driver-agnostic shared postprocess), NOT blockers. Verified on disk: energy-fields folds NO other rough-in directly-owned constituent → the flip is honest. This is D3's prominent verdict, confirmed against the on-disk `composes:`/down-link constituents.
- **`capacitance` / `inductance` / `electrostatic` / `magnetostatic` → STAY `seed`.** Their OWN reduce/solve composition still folds `gram_reduce`, which STAYS `rough-in` on the residual `bilinear-form` gate (D3). I re-narrate their promotion-route prose to the CURRENT gate (`matrix-weighted-norm` is now firm; the gate is now `gram_reduce` rough-in on `bilinear-form` alone, NOT on `matrix-weighted-norm` anymore) and re-anchor every stale `matrix-weighted-norm` rough-in label to firm, PRESERVING every `bilinear-form` rough-in label.

**`feature/index.md` count delta (I am SOLE owner): firm 6 → 7, seed 6 → 5.** (Verified current on-disk counts: the §"Chapter-kind status" block lists `firm (6 columns)` / `seed (6 columns)`; energy-fields moves from the seed sub-list to the firm sub-list.)

I stayed entirely within `book/src/feature/`. I did NOT touch `book/src/L1/index.md` / `book/src/L4/index.md` (D1+D3), the reduce-verb own-entries (D3), `book/src/L1/matrix-weighted-norm.md` (D1), or the vocabulary-spine consumer files (D2).

## Per-column verdict (flip/stay + the OWN-constituent basis)

| Column | Verdict | OWN-composition constituents (on-disk basis) | Sibling cross-links (references, NOT blockers) |
|---|---|---|---|
| **energy-fields** | **FLIP seed→firm** | `domain_energy_reduce` **firm** (D3 verdict, on-disk §Status flipped) + `matrix-weighted-norm` **firm** (D1) + `participation_ratio` firm (c077) — ALL firm | the field-bearing driver columns (`electrostatic`/`magnetostatic`/…) — driver-agnostic shared postprocess |
| **capacitance** | STAY seed | own reduce verb `gram_reduce` **rough-in** (D3: residual `bilinear-form` gate) | `electrostatic` (producing driver) |
| **inductance** | STAY seed | own reduce verb `gram_reduce` **rough-in** | `magnetostatic` (producing driver) |
| **electrostatic** | STAY seed | own `gram_reduce` capacitance reduction **rough-in** (own `solve_family` firm c086; own `fe_assemble` firm) | `capacitance` (output product) |
| **magnetostatic** | STAY seed | own `gram_reduce` inductance reduction **rough-in** (own `solve_family` firm c086; own `fe_assemble` firm) | `inductance` (output product) |

**Honesty check on the energy-fields flip:** I read the on-disk `energy-fields.L4.md` `composes:` block (`:6-10`) and the §"Constituent down-links" table (`:156-161`). Its directly-owned constituents are exactly `domain_energy_reduce`, `matrix-weighted-norm`, `participation_ratio` — all three now firm. The `electrostatic.L4`/`magnetostatic.L4`/… producing columns listed in `composes:` are SIBLING references (the column is explicitly driver-agnostic, "a shared postprocess all field-bearing drivers point at", `:151`). No other rough-in own-constituent → the flip is warranted.

## feature/index.md count delta (exact)

- `## Chapter-kind status` block, on-disk: **`firm` (6 columns)** / **`seed` (6 columns)**.
- After this cycle: **`firm` (7 columns)** / **`seed` (5 columns)**. `energy-fields` moves from the seed sub-list (where it was held on "own reduce verb `domain_energy_reduce` + own folded `matrix-weighted-norm` are `rough-in`") to the firm sub-list (own composition now all-firm).
- The 4 `gram_reduce`-gated columns (electrostatic/magnetostatic/capacitance/inductance) STAY in the seed sub-list; their seed-rationale prose is re-narrated to the narrowed gate (gram_reduce rough-in on `bilinear-form` alone — `matrix-weighted-norm` discharged c091).

## Sibling-status grep (the firm-promotion-coupled whole-book-feature grep)

Coupled to the `energy-fields` flip, I ran `grep -rn 'energy-fields' book/src/feature | grep -E '\(\*?\*?seed\*?\*?\)'` to find SIBLING-column prose that still calls energy-fields `(seed)`. Energy-fields is driver-agnostic — no driver column carries an UP-link to it (the driver-agnostic exception: a missing per-driver up-link to a shared postprocess is correct, not drift). The only place its maturity is asserted by a sibling is `output-product.md:9` (the output-product group-intro, which calls `domain_energy_reduce` `rough-in`) and `index.md` (the matrix + cohort block) — both of which I own and re-anchor below. No stale `(seed)` energy-fields mention remains in a sibling column I do not touch.

## Proposed changes

### A. energy-fields — FLIP seed → firm (× 3 levels) + re-anchor constituent labels

#### A1. book/src/feature/energy-fields.L4.md

**A1a. frontmatter `status:` (`:5`) → firm.**

```edit:book/src/feature/energy-fields.L4.md
[old]:
status: seed
[new]:
status: firm
```

**A1b. frontmatter `composes:` (`:8`, `:10`) — domain_energy_reduce + matrix-weighted-norm labels → firm.**

```edit:book/src/feature/energy-fields.L4.md
[old]:
  - book/src/L4/domain_energy_reduce.md (rough-in — the per-domain energy-table reduction verb, minted cycle-078)
  - book/src/L1/participation_ratio.md (firm — the energy_i/E_total per-domain ratio the reduction folds)
  - book/src/L1/matrix-weighted-norm.md (rough-in (test-coverage-bounded) — the ½⟨field,M_i field⟩ per-domain energy form, squared radicand restricted to one domain attribute)
[new]:
  - book/src/L4/domain_energy_reduce.md (firm c091 — the per-domain energy-table reduction verb, minted cycle-078; promoted rough-in→firm by the batch-29 firm-flip-and-cascade wave once both its folded primitives firmed)
  - book/src/L1/participation_ratio.md (firm — the energy_i/E_total per-domain ratio the reduction folds)
  - book/src/L1/matrix-weighted-norm.md (firm c091 — the ½⟨field,M_i field⟩ per-domain energy form, squared radicand restricted to one domain attribute; promoted to firm by the batch-29 firm-flip-and-cascade wave on the firm-on-positive-structure escape)
```

**A1c. stage-(2) body (`:62-63`) — domain_energy_reduce label → firm.**

```edit:book/src/feature/energy-fields.L4.md
[old]:
2. **The per-domain energy-table reduction** — [`domain_energy_reduce`](../L4/domain_energy_reduce.md)
   (**rough-in**, authored cycle-079). The L4 per-domain energy-table reduction combinator
[new]:
2. **The per-domain energy-table reduction** — [`domain_energy_reduce`](../L4/domain_energy_reduce.md)
   (**firm** c091, authored cycle-079, promoted by the batch-29 firm-flip-and-cascade wave). The L4 per-domain energy-table reduction combinator
```

**A1d. §"Why this is a distinct output-product column" — the folded-primitive labels (`:135-137`) → firm.**

```edit:book/src/feature/energy-fields.L4.md
[old]:
- The reduction is [`domain_energy_reduce`](../L4/domain_energy_reduce.md), a **per-domain scalar
  map** folding two scalar projections per domain: the domain-restricted energy form
  (`matrix-weighted-norm`-squared, rough-in) and the participation ratio (firm
  [`participation_ratio`](../L1/participation_ratio.md)).
[new]:
- The reduction is [`domain_energy_reduce`](../L4/domain_energy_reduce.md) (**firm** c091), a **per-domain scalar
  map** folding two scalar projections per domain: the domain-restricted energy form
  (`matrix-weighted-norm`-squared, **firm** c091) and the participation ratio (firm
  [`participation_ratio`](../L1/participation_ratio.md)).
```

**A1e. §"Why this is a distinct output-product column" closing-paragraph promotion-rule prose (`:144-152`) — restate as FLIPPED to firm.**

```edit:book/src/feature/energy-fields.L4.md
[old]:
`energy_fields = domain_energy_reduce (doms, e_total) ∘ driver_field` — a one-reduction tail on a
field-bearing driver column. Under the **OWN-COMPOSITION rule** (USER DIRECTIVE 2026-06-03;
CLAUDE.md §Extraction-goal) a column promotes off `seed` when its OWN directly-owned constituents
are firm; this column STAYS `seed` because its OWN reduce verb
[`domain_energy_reduce`](../L4/domain_energy_reduce.md) is `rough-in` and its OWN folded
domain-restricted energy form is the [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md)
`rough-in (test-coverage-bounded)` primitive (the firm [`participation_ratio`](../L1/participation_ratio.md)
half is necessary but not sufficient). These are genuine OWN-constituent gates, NOT cross-linked-sibling
blockers — energy-fields is driver-agnostic (a shared postprocess all field-bearing drivers point at),
so the field-bearing driver columns are SIBLING references, not the gate.
[new]:
`energy_fields = domain_energy_reduce (doms, e_total) ∘ driver_field` — a one-reduction tail on a
field-bearing driver column. Under the **OWN-COMPOSITION rule** (USER DIRECTIVE 2026-06-03;
CLAUDE.md §Extraction-goal) a column promotes off `seed` when its OWN directly-owned constituents
are firm; this column is now **firm** (promoted `seed`→`firm` cycle-091 by the batch-29
firm-flip-and-cascade wave) because its OWN composition is now all-firm: its OWN reduce verb
[`domain_energy_reduce`](../L4/domain_energy_reduce.md) firmed (c091, once both its folded primitives
firmed), its OWN folded domain-restricted energy form is the now-firm
[`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (c091), and the
[`participation_ratio`](../L1/participation_ratio.md) half was already firm — all three
directly-owned constituents firm. The field-bearing driver columns remain SIBLING references, NOT
blockers — energy-fields is driver-agnostic (a shared postprocess all field-bearing drivers point
at), and a sibling reference never gated the promotion.
```

**A1f. §"Constituent down-links" table (`:159-160`) — domain_energy_reduce + matrix-weighted-norm status cells → firm.**

```edit:book/src/feature/energy-fields.L4.md
[old]:
| per-domain energy-table reduction | [`domain_energy_reduce`](../L4/domain_energy_reduce.md) *(rough-in)* | rough-in | `postoperator.cpp:1036-1042, 1061-1066` |
| per-domain energy form (folded) | [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md)-squared (domain-restricted `M_i`) | rough-in (test-coverage-bounded) | `domainpostoperator.cpp:255-275, 277-298` |
[new]:
| per-domain energy-table reduction | [`domain_energy_reduce`](../L4/domain_energy_reduce.md) *(firm c091)* | firm | `postoperator.cpp:1036-1042, 1061-1066` |
| per-domain energy form (folded) | [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md)-squared (domain-restricted `M_i`) | firm c091 | `domainpostoperator.cpp:255-275, 277-298` |
```

**A1g. §Status (`:163-187`) — FLIP to firm; restate the promotion as enacted.**

```edit:book/src/feature/energy-fields.L4.md
[old]:
`seed` — an output-product **leaf feature column** authored under the FEATURE-SURFACE SPINE
directive (2026-06-02), the rank-1 **per-domain**-table sibling of the rank-1 per-mode-table
[eigenfrequency-qfactor](./eigenfrequency-qfactor.L4.md) and the rank-2 Gram/port-projection
products [capacitance](./capacitance.L4.md) / [inductance](./inductance.L4.md) /
[sparameters](./sparameters.L4.md). The composition is sound: a field-bearing driver column
supplies the solution field; the `domain_energy_reduce` reduction
maps each configured domain to its `(energyᵢ, pᵢ)` row, folding the domain-restricted energy form
([`matrix-weighted-norm`](../L1/matrix-weighted-norm.md)-squared) and the firm
[`participation_ratio`](../L1/participation_ratio.md). **Under the OWN-COMPOSITION rule
(USER DIRECTIVE 2026-06-03; codified batch-26 meta-phase; memory `project_feature_column_promotion_rule`)
a column promotes off `seed` when its OWN composition + directly-owned constituents are firm; this column
STAYS `seed`** because its OWN reduce verb `domain_energy_reduce` is `rough-in` and its OWN folded
domain-restricted energy form is the `matrix-weighted-norm` `rough-in (test-coverage-bounded)` primitive
(the firm `participation_ratio` half is necessary but not sufficient). These are genuine OWN-constituent
gates, NOT cross-linked-sibling blockers — the field-bearing driver columns are SIBLING references
(this is a driver-agnostic shared postprocess), not the gate. This chapter carries the *compositional*
claim (the domain energy table = the per-domain energy reduction over a field-bearing driver's
field), NOT the constituents' per-op algebraic claims (those live in the linked chapters). The
defining structural fact: a rank-1 per-domain scalar table, NOT a `gram_reduce` family-PAIR grid
(c074 D6 do-NOT-over-unify, honored). Evidence: `MeasureDomainFieldEnergy`
(`postoperator.cpp:1021-1077`) + the energy form (`domainpostoperator.cpp:255-298`), all anchors
confirmed on-disk via palace-codemap `read_range`/`search_text` this dispatch, plus the
constituent down-links.
[new]:
`firm` — an output-product **leaf feature column** authored under the FEATURE-SURFACE SPINE
directive (2026-06-02), the rank-1 **per-domain**-table sibling of the rank-1 per-mode-table
[eigenfrequency-qfactor](./eigenfrequency-qfactor.L4.md) and the rank-2 Gram/port-projection
products [capacitance](./capacitance.L4.md) / [inductance](./inductance.L4.md) /
[sparameters](./sparameters.L4.md). The composition is sound: a field-bearing driver column
supplies the solution field; the `domain_energy_reduce` reduction
maps each configured domain to its `(energyᵢ, pᵢ)` row, folding the domain-restricted energy form
([`matrix-weighted-norm`](../L1/matrix-weighted-norm.md)-squared) and the firm
[`participation_ratio`](../L1/participation_ratio.md). **Under the OWN-COMPOSITION rule
(USER DIRECTIVE 2026-06-03; codified batch-26 meta-phase; memory `project_feature_column_promotion_rule`)
a column promotes off `seed` when its OWN composition + directly-owned constituents are firm; this column
was promoted `seed`→`firm` cycle-091** by the batch-29 firm-flip-and-cascade wave, once its OWN
composition became all-firm: its OWN reduce verb `domain_energy_reduce` firmed (firm c091 — both its
folded primitives now have firm L1 homes, `matrix-weighted-norm` c091 + `participation_ratio` c077,
the firm-on-positive-structure escape), its OWN folded domain-restricted energy form is the now-firm
`matrix-weighted-norm` (firm c091, the batch-29 firm flip), and the `participation_ratio` half was
already firm — all three directly-owned constituents firm. The field-bearing driver columns are
SIBLING references (this is a driver-agnostic shared postprocess), NOT blockers — a sibling reference
never gated the promotion. This chapter carries the *compositional*
claim (the domain energy table = the per-domain energy reduction over a field-bearing driver's
field), NOT the constituents' per-op algebraic claims (those live in the linked chapters). The
defining structural fact: a rank-1 per-domain scalar table, NOT a `gram_reduce` family-PAIR grid
(c074 D6 do-NOT-over-unify, honored). Evidence: `MeasureDomainFieldEnergy`
(`postoperator.cpp:1021-1077`) + the energy form (`domainpostoperator.cpp:255-298`), all anchors
confirmed on-disk via palace-codemap `read_range`/`search_text` this dispatch, plus the
constituent down-links.
```

#### A2. book/src/feature/energy-fields.L1.md

**A2a. frontmatter `status:` (`:5`) → firm.**

```edit:book/src/feature/energy-fields.L1.md
[old]:
status: seed
[new]:
status: firm
```

**A2b. frontmatter `composes:` (`:9`) — matrix-weighted-norm label → firm.**

```edit:book/src/feature/energy-fields.L1.md
[old]:
  - book/src/L1/matrix-weighted-norm.md (rough-in (test-coverage-bounded) — the ½⟨field,M_i field⟩ per-domain energy form)
[new]:
  - book/src/L1/matrix-weighted-norm.md (firm c091 — the ½⟨field,M_i field⟩ per-domain energy form; promoted to firm by the batch-29 firm-flip-and-cascade wave)
```

**A2c. §"Constituent down-links" table (`:108`) — matrix-weighted-norm status cell → firm.**

```edit:book/src/feature/energy-fields.L1.md
[old]:
| per-domain energy form | [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md)-squared (domain-restricted `M_i`) | rough-in (test-coverage-bounded) | `domainpostoperator.cpp:255-275, 277-298` |
[new]:
| per-domain energy form | [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md)-squared (domain-restricted `M_i`) | firm c091 | `domainpostoperator.cpp:255-275, 277-298` |
```

**A2d. §Status (`:113-128`) — FLIP to firm.**

```edit:book/src/feature/energy-fields.L1.md
[old]:
`seed` — the L1 pure-function composition root for the domain field-energy table output product
(the output-product **leaf feature column**), authored under the FEATURE-SURFACE SPINE directive
(2026-06-02), the L1 counterpart of the [energy-fields.L4](./energy-fields.L4.md) composition root.
It consumes a field-bearing driver column's solution field, then maps each configured domain to its
`(energyᵢ, pᵢ)` row (the domain-restricted energy form + the participation ratio). **Under the
OWN-COMPOSITION rule (USER DIRECTIVE 2026-06-03) a column promotes off `seed` when its OWN
directly-owned constituents are firm; this column STAYS `seed`** because its OWN reduce verb's L4 home
`domain_energy_reduce` is `rough-in` and its OWN folded domain energy form is the `matrix-weighted-norm`
`rough-in (test-coverage-bounded)` primitive (only the [`participation_ratio`](../L1/participation_ratio.md)
half is firm). These are OWN-constituent gates, NOT cross-linked-sibling blockers — the field-bearing
driver columns are SIBLING references (a driver-agnostic shared postprocess), not the gate. The
chapter carries the compositional claim only; per-op
algebraic claims live in the linked chapters. The defining structural fact carried from L4: a
rank-1 per-domain scalar table, NOT a `gram_reduce` family-PAIR grid (c074 D6). Evidence: the L0
energy-table ranges `postoperator.cpp:1021-1077` + the energy form `domainpostoperator.cpp:255-298`,
confirmed on-disk this dispatch, plus the constituent down-links.
[new]:
`firm` — the L1 pure-function composition root for the domain field-energy table output product
(the output-product **leaf feature column**), authored under the FEATURE-SURFACE SPINE directive
(2026-06-02), the L1 counterpart of the [energy-fields.L4](./energy-fields.L4.md) composition root.
It consumes a field-bearing driver column's solution field, then maps each configured domain to its
`(energyᵢ, pᵢ)` row (the domain-restricted energy form + the participation ratio). **Under the
OWN-COMPOSITION rule (USER DIRECTIVE 2026-06-03) a column promotes off `seed` when its OWN
directly-owned constituents are firm; this column was promoted `seed`→`firm` cycle-091** by the
batch-29 firm-flip-and-cascade wave, once its OWN composition became all-firm: its OWN reduce verb's
L4 home `domain_energy_reduce` firmed (firm c091) and its OWN folded domain energy form is the now-firm
`matrix-weighted-norm` (firm c091, the batch-29 firm flip), with the
[`participation_ratio`](../L1/participation_ratio.md) half already firm — all three directly-owned
constituents firm. The field-bearing driver columns are SIBLING references (a driver-agnostic shared
postprocess), NOT blockers — a sibling reference never gated the promotion. The
chapter carries the compositional claim only; per-op
algebraic claims live in the linked chapters. The defining structural fact carried from L4: a
rank-1 per-domain scalar table, NOT a `gram_reduce` family-PAIR grid (c074 D6). Evidence: the L0
energy-table ranges `postoperator.cpp:1021-1077` + the energy form `domainpostoperator.cpp:255-298`,
confirmed on-disk this dispatch, plus the constituent down-links.
```

#### A3. book/src/feature/energy-fields.L0.md

The L0 file carries NO stale `matrix-weighted-norm`/`domain_energy_reduce` maturity LABEL (it names the ops as lift targets without a rough-in token; its only constituent-status label is `participation_ratio` "firm", already correct). It gets the `status:` flip + a §Status promotion line.

**A3a. frontmatter `status:` (`:5`) → firm.**

```edit:book/src/feature/energy-fields.L0.md
[old]:
status: seed
[new]:
status: firm
```

**A3b. §Status opening (`:94-97`) — FLIP to firm.**

```edit:book/src/feature/energy-fields.L0.md
[old]:
`seed` — the L0 ground-truth surface for the domain field-energy table output product (the
output-product **leaf feature column**), authored under the FEATURE-SURFACE SPINE directive
(2026-06-02), mirroring the [eigenfrequency-qfactor.L0](./eigenfrequency-qfactor.L0.md) /
[capacitance.L0](./capacitance.L0.md) output-product exemplars.
[new]:
`firm` — the L0 ground-truth surface for the domain field-energy table output product (the
output-product **leaf feature column**), authored under the FEATURE-SURFACE SPINE directive
(2026-06-02), mirroring the [eigenfrequency-qfactor.L0](./eigenfrequency-qfactor.L0.md) /
[capacitance.L0](./capacitance.L0.md) output-product exemplars. Promoted `seed`→`firm` cycle-091
by the batch-29 firm-flip-and-cascade wave (the column's OWN composition became all-firm — own
reduce verb `domain_energy_reduce` firm c091, own folded `matrix-weighted-norm` firm c091,
`participation_ratio` firm c077; the L0 surface follows its L1/L4 siblings off `seed`).
```

### B. capacitance — STAY seed; re-anchor matrix-weighted-norm → firm; re-narrate gate to bilinear-form residual

#### B1. book/src/feature/capacitance.L4.md

**B1a. §"Constituent down-links" table (`:59`) — matrix-weighted-norm status cell → firm (gram_reduce row unchanged — STAYS rough-in).**

```edit:book/src/feature/capacitance.L4.md
[old]:
| diagonal Vᵢᵀ K Vᵢ (folded) | [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) | rough-in (test-coverage-bounded) | `electrostaticsolver.cpp:118-119` |
[new]:
| diagonal Vᵢᵀ K Vᵢ (folded) | [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) | firm c091 | `electrostaticsolver.cpp:118-119` |
```

**B1b. §Status (`:64`) — re-anchor matrix-weighted-norm to firm; re-narrate the gate to the bilinear-form residual. STAYS seed.**

```edit:book/src/feature/capacitance.L4.md
[old]:
This column STAYS `seed` because its OWN reduce verb [`gram_reduce`](../L4/gram_reduce.md) is `rough-in (test-coverage-bounded)` — its folded L1 bilinear primitives ([`matrix-weighted-norm`](../L1/matrix-weighted-norm.md), [`bilinear-form`](../L1/bilinear-form.md)) are rough-in and no dedicated Gram-reduction test exists. This is a genuine OWN-constituent gate, NOT a cross-linked-sibling blocker: the [`electrostatic.L4`](./electrostatic.L4.md) producing driver column is a SIBLING reference, not the gate. (Firming `gram_reduce` — gated on `matrix-weighted-norm` + `bilinear-form` going firm — would unblock this column.)
[new]:
This column STAYS `seed` because its OWN reduce verb [`gram_reduce`](../L4/gram_reduce.md) is `rough-in (test-coverage-bounded)`. The cascade NARROWED that gate this cycle: the diagonal folded primitive [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) firmed (firm c091, the batch-29 firm-flip-and-cascade wave), but the off-diagonal folded primitive [`bilinear-form`](../L1/bilinear-form.md) is still rough-in (its own `lower-layer-shared-vocabulary` gate), and a reduction is as firm as its least-firm folded primitive — so `gram_reduce` STAYS rough-in on the sole RESIDUAL `bilinear-form` gate (D3 cycle-091 re-judgment). This is a genuine OWN-constituent gate, NOT a cross-linked-sibling blocker: the [`electrostatic.L4`](./electrostatic.L4.md) producing driver column is a SIBLING reference, not the gate. (Firming `gram_reduce` — now gated on `bilinear-form` alone going firm; the `matrix-weighted-norm` half is discharged c091 — would unblock this column.)
```

#### B2. book/src/feature/capacitance.L1.md

**B2a. frontmatter `composes:` (`:8`) — matrix-weighted-norm label → firm.**

```edit:book/src/feature/capacitance.L1.md
[old]:
  - book/src/L1/matrix-weighted-norm.md (rough-in (test-coverage-bounded) — diagonal Vᵢᵀ K Vᵢ)
[new]:
  - book/src/L1/matrix-weighted-norm.md (firm c091 — diagonal Vᵢᵀ K Vᵢ; promoted to firm by the batch-29 firm-flip-and-cascade wave)
```

**B2b. body (`:34`) — "rough-in matrix-weighted-norm" → firm.**

```edit:book/src/feature/capacitance.L1.md
[old]:
   - diagonal `Cᵢᵢ = Vᵢᵀ K Vᵢ` — the operator-weighted self-form, the rough-in [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) squared (`matrix_weighted_norm(Vᵢ, K)² = Vᵢᵀ K Vᵢ`; L0 builds it as `M_elec->Mult(V_gf, D_gf)` then `linalg::Dot(V_gf, D_gf)`, `:118-119`).
[new]:
   - diagonal `Cᵢᵢ = Vᵢᵀ K Vᵢ` — the operator-weighted self-form, the now-firm [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) squared (`matrix_weighted_norm(Vᵢ, K)² = Vᵢᵀ K Vᵢ`; L0 builds it as `M_elec->Mult(V_gf, D_gf)` then `linalg::Dot(V_gf, D_gf)`, `:118-119`).
```

**B2c. body (`:36`) — the "(rough-in) + (rough-in)" fold pair → "(firm c091) + (rough-in)".**

```edit:book/src/feature/capacitance.L1.md
[old]:
   The result is the symmetric `C` (and its LAPACK inverse `Cinv`, `:139-140`). This stage is a pure fold of bilinear-form evaluations over the family-pair grid — no L1 operator is *new* here; the reduction composes [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (rough-in) + [`bilinear-form`](../L1/bilinear-form.md) (rough-in). At L4 this exact fold is named the [`gram_reduce`](../L4/gram_reduce.md) combinator (voltage `w = 1` specialization); L1 sees the unfolded grid.
[new]:
   The result is the symmetric `C` (and its LAPACK inverse `Cinv`, `:139-140`). This stage is a pure fold of bilinear-form evaluations over the family-pair grid — no L1 operator is *new* here; the reduction composes [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (firm c091) + [`bilinear-form`](../L1/bilinear-form.md) (rough-in). At L4 this exact fold is named the [`gram_reduce`](../L4/gram_reduce.md) combinator (voltage `w = 1` specialization); L1 sees the unfolded grid.
```

**B2d. §"Constituent down-links" table (`:56`) — matrix-weighted-norm status cell → firm.**

```edit:book/src/feature/capacitance.L1.md
[old]:
| diagonal Vᵢᵀ K Vᵢ | [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) | rough-in (test-coverage-bounded) | `electrostaticsolver.cpp:118-119` |
[new]:
| diagonal Vᵢᵀ K Vᵢ | [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) | firm c091 | `electrostaticsolver.cpp:118-119` |
```

**B2e. §Status (`:61`) — re-anchor the "two rough-in primitives" narration to "one firm + one residual rough-in"; STAYS seed.**

```edit:book/src/feature/capacitance.L1.md
[old]:
It consumes the [`electrostatic.L1`](./electrostatic.L1.md) driver column's solution family, then folds the two rough-in L1 bilinear primitives ([`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) `rough-in (test-coverage-bounded)`, [`bilinear-form`](../L1/bilinear-form.md) rough-in) over the family-pair grid. **Under the OWN-COMPOSITION rule (USER DIRECTIVE 2026-06-03) a column promotes off `seed` when its OWN directly-owned constituents are firm; this column STAYS `seed` because its OWN reduce primitives are rough-in** (the reduction rests on the rough-in L1 bilinear primitives, whose L4 home `gram_reduce` is `rough-in (test-coverage-bounded)`).
[new]:
It consumes the [`electrostatic.L1`](./electrostatic.L1.md) driver column's solution family, then folds two L1 bilinear primitives over the family-pair grid — the now-firm diagonal [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (firm c091, the batch-29 firm-flip-and-cascade wave) and the still-rough-in off-diagonal [`bilinear-form`](../L1/bilinear-form.md). **Under the OWN-COMPOSITION rule (USER DIRECTIVE 2026-06-03) a column promotes off `seed` when its OWN directly-owned constituents are firm; this column STAYS `seed` because its OWN reduce verb is still rough-in** — the off-diagonal [`bilinear-form`](../L1/bilinear-form.md) is rough-in (the sole RESIDUAL gate; `matrix-weighted-norm` discharged c091), so the L4 home `gram_reduce` STAYS `rough-in (test-coverage-bounded)` on that residual (D3 cycle-091 re-judgment).
```

### C. inductance — STAY seed; same re-anchor + gate re-narration

#### C1. book/src/feature/inductance.L4.md

inductance.L4 carries NO stale `matrix-weighted-norm` maturity LABEL: the stage-(2) body (`:36`) names "the self-bilinear ([`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) radicand)" with NO maturity token, the down-link table row (`:60`) is `gram_reduce (w = 1/(IᵢIⱼ))` with no folded-primitive parenthetical, and the §Status (`:65`) says "its folded L1 bilinear primitives are rough-in" generically (no mwn name). So the only edit is the §Status gate re-narration: the generic "folded L1 bilinear primitives are rough-in" is now stale (the diagonal `matrix-weighted-norm` firmed), so narrow it to the bilinear-form residual. STAYS seed.

**C1a. §Status (`:65`) — re-narrate the generic "folded L1 bilinear primitives are rough-in" to the bilinear-form residual; STAYS seed.**

```edit:book/src/feature/inductance.L4.md
[old]:
This column STAYS `seed` because its OWN reduce verb [`gram_reduce`](../L4/gram_reduce.md) is `rough-in (test-coverage-bounded)` (its folded L1 bilinear primitives are rough-in, and no dedicated test exercises the Gram reduction). This is a genuine OWN-constituent gate, NOT a cross-linked-sibling blocker: the [`magnetostatic`](./magnetostatic.L4.md) producing driver column is a SIBLING reference, not the gate. (Firming `gram_reduce` would unblock this column, jointly with [capacitance](./capacitance.L4.md).)
[new]:
This column STAYS `seed` because its OWN reduce verb [`gram_reduce`](../L4/gram_reduce.md) is `rough-in (test-coverage-bounded)`. The cycle-091 batch-29 firm-flip-and-cascade wave NARROWED that gate: of `gram_reduce`'s two folded L1 bilinear primitives the diagonal [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) firmed (firm c091), leaving the off-diagonal [`bilinear-form`](../L1/bilinear-form.md) (still rough-in on its own `lower-layer-shared-vocabulary` gate) as the sole RESIDUAL gate — a reduction is as firm as its least-firm folded primitive, so `gram_reduce` STAYS rough-in (D3 cycle-091 re-judgment). This is a genuine OWN-constituent gate, NOT a cross-linked-sibling blocker: the [`magnetostatic`](./magnetostatic.L4.md) producing driver column is a SIBLING reference, not the gate. (Firming `gram_reduce` — now gated on `bilinear-form` alone; the `matrix-weighted-norm` half is discharged c091 — would unblock this column, jointly with [capacitance](./capacitance.L4.md).)
```

#### C2. book/src/feature/inductance.L1.md

**C2a. frontmatter `composes:` (`:8`) — matrix-weighted-norm label → firm.**

```edit:book/src/feature/inductance.L1.md
[old]:
  - book/src/L1/matrix-weighted-norm.md (rough-in (test-coverage-bounded) — diagonal Aᵢᵀ K Aᵢ)
[new]:
  - book/src/L1/matrix-weighted-norm.md (firm c091 — diagonal Aᵢᵀ K Aᵢ; promoted to firm by the batch-29 firm-flip-and-cascade wave)
```

**C2b. body (`:40`) — "rough-in matrix-weighted-norm" → firm.**

```edit:book/src/feature/inductance.L1.md
[old]:
   - diagonal `Mᵢᵢ = (Aᵢᵀ K Aᵢ)/Iᵢ²` — the operator-weighted self-form normalized by the squared current, the rough-in [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) squared (`matrix_weighted_norm(Aᵢ, K)² = Aᵢᵀ K Aᵢ`; L0 builds it as `post_op.GetDomainPostOp().M_mag->Mult(A_gf, H_gf)` then `linalg::Dot<Vector>(..., A_gf, H_gf) / (I_inc[i]*I_inc[i])`, `magnetostaticsolver.cpp:129-131`).
[new]:
   - diagonal `Mᵢᵢ = (Aᵢᵀ K Aᵢ)/Iᵢ²` — the operator-weighted self-form normalized by the squared current, the now-firm [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) squared (`matrix_weighted_norm(Aᵢ, K)² = Aᵢᵀ K Aᵢ`; L0 builds it as `post_op.GetDomainPostOp().M_mag->Mult(A_gf, H_gf)` then `linalg::Dot<Vector>(..., A_gf, H_gf) / (I_inc[i]*I_inc[i])`, `magnetostaticsolver.cpp:129-131`).
```

**C2c. body (`:42`) — the "(rough-in) + (rough-in)" fold pair → "(firm c091) + (rough-in)".**

```edit:book/src/feature/inductance.L1.md
[old]:
   The result is the symmetric `M` (upper triangle computed, lower mirrored, `:140-149`) and its LAPACK inverse `Minv` (`:151-152`). This stage is a pure fold of current-normalized bilinear-form evaluations over the solution-family pair grid — no L1 operator is *new* here; it composes [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (rough-in) + [`bilinear-form`](../L1/bilinear-form.md) (rough-in), with the current normalization a scalar weight on each entry.
[new]:
   The result is the symmetric `M` (upper triangle computed, lower mirrored, `:140-149`) and its LAPACK inverse `Minv` (`:151-152`). This stage is a pure fold of current-normalized bilinear-form evaluations over the solution-family pair grid — no L1 operator is *new* here; it composes [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (firm c091) + [`bilinear-form`](../L1/bilinear-form.md) (rough-in), with the current normalization a scalar weight on each entry.
```

**C2d. §"Constituent down-links" table (`:62`) — matrix-weighted-norm status cell → firm.**

```edit:book/src/feature/inductance.L1.md
[old]:
| diagonal Aᵢᵀ K Aᵢ / Iᵢ² | [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) | rough-in (test-coverage-bounded) | `magnetostaticsolver.cpp:129-131` |
[new]:
| diagonal Aᵢᵀ K Aᵢ / Iᵢ² | [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) | firm c091 | `magnetostaticsolver.cpp:129-131` |
```

**C2e. §Status — re-narrate the "BOTH directly-owned bilinear primitives are rough-in" claim; STAYS seed.**

```edit:book/src/feature/inductance.L1.md
[old]:
BOTH directly-owned bilinear primitives are rough-in — the diagonal [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) is `rough-in (test-coverage-bounded)` (no dedicated test exercises the SPD-weighted overload) and the off-diagonal [`bilinear-form`](../L1/bilinear-form.md) is rough-in (its `α = xᴴ M y` signature covers the cross-pairing). **Under the OWN-COMPOSITION rule (USER DIRECTIVE 2026-06-03) a column promotes off `seed` when its OWN directly-owned constituents are firm; this column STAYS `seed` because its OWN reduce primitives are rough-in** (the reduction rests on the rough-in L1 bilinear primitives, whose L4 home `gram_reduce` is `rough-in (test-coverage-bounded)`).
[new]:
of the two directly-owned bilinear primitives the diagonal [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) is now **firm** (firm c091, the batch-29 firm-flip-and-cascade wave) but the off-diagonal [`bilinear-form`](../L1/bilinear-form.md) is still rough-in (its `α = xᴴ M y` signature covers the cross-pairing; the sole RESIDUAL gate after this cycle). **Under the OWN-COMPOSITION rule (USER DIRECTIVE 2026-06-03) a column promotes off `seed` when its OWN directly-owned constituents are firm; this column STAYS `seed` because its OWN reduce verb is still rough-in** — the reduction still rests on the rough-in off-diagonal `bilinear-form`, whose L4 home `gram_reduce` STAYS `rough-in (test-coverage-bounded)` on that residual (D3 cycle-091 re-judgment).
```

### D. electrostatic — STAY seed; re-anchor matrix-weighted-norm → firm; re-narrate gate (incl. the now-stale √-cascade NO-GO-HELD clause)

#### D1. book/src/feature/electrostatic.L4.md

**D1a. stage-(3) body (`:41`) — the "rough-in L1 building blocks" + "gram_reduce is rough-in BECAUSE those folded constituents are" narration → split (matrix-weighted-norm firm, bilinear-form residual).**

```edit:book/src/feature/electrostatic.L4.md
[old]:
The combinator's `map`-then-`reduce` body composes the rough-in L1 building blocks — the diagonal `Vᵢᵀ K Vᵢ` is the [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) radicand, the off-diagonal `Vⱼᵀ K Vᵢ` is the [`bilinear-form`](../L1/bilinear-form.md) — folded over the upper-triangle family-pair grid with the symmetric mirror; `gram_reduce` is rough-in BECAUSE those folded constituents are.
[new]:
The combinator's `map`-then-`reduce` body composes two L1 building blocks — the diagonal `Vᵢᵀ K Vᵢ` is the now-**firm** [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) radicand (firm c091, the batch-29 firm-flip-and-cascade wave), the off-diagonal `Vⱼᵀ K Vᵢ` is the still-rough-in [`bilinear-form`](../L1/bilinear-form.md) — folded over the upper-triangle family-pair grid with the symmetric mirror; `gram_reduce` is rough-in BECAUSE its off-diagonal `bilinear-form` constituent still is (the sole RESIDUAL gate; the `matrix-weighted-norm` diagonal half discharged c091).
```

**D1b. §"lowers cleanly outward" paragraph (`:56`) — re-narrate the now-stale √-cascade NO-GO-HELD clause; matrix-weighted-norm firmed, so the gate is now bilinear-form.**

```edit:book/src/feature/electrostatic.L4.md
[old]:
The [`solve_family`](../L4/solve_family.md) per-terminal map is now **firm** (c086, the firm-on-positive-structure / syntactic-identity escape), so the own-constituent gate has narrowed from two directly-owned rough-in constituents to ONE: firming `gram_reduce` is the remaining promotion route — and that is itself convergently blocked on the `matrix-weighted-norm` √-cascade (NO-GO-HELD), which `gram_reduce` folds.
[new]:
The [`solve_family`](../L4/solve_family.md) per-terminal map is now **firm** (c086, the firm-on-positive-structure / syntactic-identity escape), so the own-constituent gate has narrowed from two directly-owned rough-in constituents to ONE: firming `gram_reduce` is the remaining promotion route. After the cycle-091 batch-29 firm-flip-and-cascade wave that gate narrowed again — `gram_reduce`'s diagonal `matrix-weighted-norm` folded primitive firmed (firm c091), leaving `gram_reduce` rough-in on its sole residual off-diagonal [`bilinear-form`](../L1/bilinear-form.md) primitive (still rough-in on its own `lower-layer-shared-vocabulary` gate, D3 cycle-091 re-judgment) — so this column does not flip this cycle.
```

**D1c. §"Constituent down-links" table (`:65`) — the gram_reduce row STAYS rough-in (it is the verb's own status), but the folded-primitive parenthetical re-anchors matrix-weighted-norm to firm.**

```edit:book/src/feature/electrostatic.L4.md
[old]:
| capacitance reduction (Vⱼᵀ K Vᵢ) | [`gram_reduce`](../L4/gram_reduce.md) (`w = 1` voltage specialization) — folding L1 [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (diagonal) / [`bilinear-form`](../L1/bilinear-form.md) (off-diagonal) | rough-in (test-coverage-bounded) | `electrostaticsolver.cpp:95, 100-138` |
[new]:
| capacitance reduction (Vⱼᵀ K Vᵢ) | [`gram_reduce`](../L4/gram_reduce.md) (`w = 1` voltage specialization) — folding L1 [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (diagonal, firm c091) / [`bilinear-form`](../L1/bilinear-form.md) (off-diagonal, rough-in — the residual gate) | rough-in (test-coverage-bounded) | `electrostaticsolver.cpp:95, 100-138` |
```

**D1d. §Status (`:69`) — re-narrate the now-stale √-cascade NO-GO-HELD clause; matrix-weighted-norm firmed; STAYS seed.**

```edit:book/src/feature/electrostatic.L4.md
[old]:
The composition is sound (stage (1) the firm [`fe_assemble`](../L4/fe_assemble.md), the reduction a clean fold of evaluations), but one directly-owned constituent (`gram_reduce`) remaining rough-in is the own-constituent gate; firming `gram_reduce` is the remaining promotion route — and `gram_reduce` is itself convergently blocked on the `matrix-weighted-norm` √-cascade (NO-GO-HELD), so this column does not flip this cycle. This chapter carries the *compositional* claim (electrostatic = this composition of these constituent pieces), not the constituents' per-op algebraic claims (those live in the linked chapters).
[new]:
The composition is sound (stage (1) the firm [`fe_assemble`](../L4/fe_assemble.md), the reduction a clean fold of evaluations), but one directly-owned constituent (`gram_reduce`) remaining rough-in is the own-constituent gate; firming `gram_reduce` is the remaining promotion route. The cycle-091 batch-29 firm-flip-and-cascade wave NARROWED that gate — `gram_reduce`'s diagonal `matrix-weighted-norm` folded primitive firmed (firm c091), so `gram_reduce` is now rough-in on its sole RESIDUAL off-diagonal [`bilinear-form`](../L1/bilinear-form.md) primitive (still rough-in on its own `lower-layer-shared-vocabulary` gate, D3 cycle-091 re-judgment) — so this column does not flip this cycle. This chapter carries the *compositional* claim (electrostatic = this composition of these constituent pieces), not the constituents' per-op algebraic claims (those live in the linked chapters).
```

#### D2. book/src/feature/electrostatic.L1.md

**D2a. frontmatter `composes:` (`:9`) — matrix-weighted-norm label → firm.**

```edit:book/src/feature/electrostatic.L1.md
[old]:
  - book/src/L1/matrix-weighted-norm.md (rough-in (test-coverage-bounded) — diagonal Vᵢᵀ K Vᵢ)
[new]:
  - book/src/L1/matrix-weighted-norm.md (firm c091 — diagonal Vᵢᵀ K Vᵢ; promoted to firm by the batch-29 firm-flip-and-cascade wave)
```

**D2b. body (`:37`) — "rough-in matrix-weighted-norm" → firm.**

```edit:book/src/feature/electrostatic.L1.md
[old]:
   - diagonal `Cᵢᵢ = Vᵢᵀ K Vᵢ` — the operator-weighted self-form, the rough-in [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) squared (`matrix_weighted_norm(Vᵢ, K)² = Vᵢᵀ K Vᵢ`; the L0 source builds it directly as `M_elec->Mult(V_gf, D_gf)` then `linalg::Dot(V_gf, D_gf)`, `:118-119`).
[new]:
   - diagonal `Cᵢᵢ = Vᵢᵀ K Vᵢ` — the operator-weighted self-form, the now-firm [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) squared (`matrix_weighted_norm(Vᵢ, K)² = Vᵢᵀ K Vᵢ`; the L0 source builds it directly as `M_elec->Mult(V_gf, D_gf)` then `linalg::Dot(V_gf, D_gf)`, `:118-119`).
```

**D2c. body (`:39`) — the "(rough-in) + (rough-in)" fold pair → "(firm c091) + (rough-in)".**

```edit:book/src/feature/electrostatic.L1.md
[old]:
   The result is the symmetric `C` (and its LAPACK inverse `Cinv`, `:139-140`). This stage is a pure fold of bilinear-form evaluations over the solution-family pair grid — no L1 operator is *new* here; the reduction composes [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (rough-in) + [`bilinear-form`](../L1/bilinear-form.md) (rough-in).
[new]:
   The result is the symmetric `C` (and its LAPACK inverse `Cinv`, `:139-140`). This stage is a pure fold of bilinear-form evaluations over the solution-family pair grid — no L1 operator is *new* here; the reduction composes [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (firm c091) + [`bilinear-form`](../L1/bilinear-form.md) (rough-in).
```

**D2d. §"Constituent down-links" table (`:60`) — matrix-weighted-norm status cell → firm.**

```edit:book/src/feature/electrostatic.L1.md
[old]:
| diagonal Vᵢᵀ K Vᵢ | [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) | rough-in (test-coverage-bounded) | `electrostaticsolver.cpp:118-119` |
[new]:
| diagonal Vᵢᵀ K Vᵢ | [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) | firm c091 | `electrostaticsolver.cpp:118-119` |
```

**D2e. §Status (`:65`) — re-narrate "BOTH primitives rough-in"; STAYS seed.**

```edit:book/src/feature/electrostatic.L1.md
[old]:
Two of the four composed L1 operators are firm ([`fe_assemble`](../L1/fe_assemble.md), [`ksp_solve`](../L1/ksp_solve.md)); BOTH capacitance-reduction primitives are rough-in — the diagonal [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) is `rough-in (test-coverage-bounded)` (no dedicated test exercises the SPD-weighted overload) and the off-diagonal [`bilinear-form`](../L1/bilinear-form.md) is rough-in (its `α = xᴴ M y` signature covers the cross-pairing, so the down-link is correct). The entire stage-3 reduction therefore rests on rough-in L1 primitives — consistent with the column being a `seed`, not a firm composition.
[new]:
Two of the four composed L1 operators are firm ([`fe_assemble`](../L1/fe_assemble.md), [`ksp_solve`](../L1/ksp_solve.md)); of the two capacitance-reduction primitives the diagonal [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) is now **firm** (firm c091, the batch-29 firm-flip-and-cascade wave) but the off-diagonal [`bilinear-form`](../L1/bilinear-form.md) is still rough-in (its `α = xᴴ M y` signature covers the cross-pairing, so the down-link is correct; the sole RESIDUAL gate after this cycle). The stage-3 reduction therefore still rests on the rough-in off-diagonal `bilinear-form` (whose L4 home `gram_reduce` STAYS `rough-in (test-coverage-bounded)` on that residual, D3 cycle-091) — consistent with the column being a `seed`, not a firm composition.
```

### E. magnetostatic — STAY seed; same re-anchor + gate re-narration

#### E1. book/src/feature/magnetostatic.L4.md

**E1a. §"Constituent down-links" table (`:65`) — matrix-weighted-norm folded-primitive parenthetical → firm; bilinear-form residual annotated.**

```edit:book/src/feature/magnetostatic.L4.md
[old]:
| inductance reduction (Aⱼᵀ K Aᵢ / Iᵢ Iⱼ) | [`gram_reduce`](../L4/gram_reduce.md) (`w = 1/(IᵢIⱼ)` current-normalized specialization) — folding L1 [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (diagonal) / [`bilinear-form`](../L1/bilinear-form.md) (off-diagonal) | rough-in (test-coverage-bounded) | `magnetostaticsolver.cpp:108, 110-152` |
[new]:
| inductance reduction (Aⱼᵀ K Aᵢ / Iᵢ Iⱼ) | [`gram_reduce`](../L4/gram_reduce.md) (`w = 1/(IᵢIⱼ)` current-normalized specialization) — folding L1 [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (diagonal, firm c091) / [`bilinear-form`](../L1/bilinear-form.md) (off-diagonal, rough-in — the residual gate) | rough-in (test-coverage-bounded) | `magnetostaticsolver.cpp:108, 110-152` |
```

**E1b. §Status (`:69`) — re-anchor the "folds the rough-in L1 diagonal matrix-weighted-norm" clause to firm; re-narrate the now-stale √-cascade NO-GO-HELD clause; STAYS seed.**

```edit:book/src/feature/magnetostatic.L4.md
[old]:
the [`gram_reduce`](../L4/gram_reduce.md) `w = 1/(IᵢIⱼ)` current-normalized inductance reduction (`rough-in (test-coverage-bounded)` — the same shared symmetric-Gram reduction as the electrostatic capacitance, the weight the only difference; it folds the rough-in L1 diagonal [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) + rough-in off-diagonal [`bilinear-form`](../L1/bilinear-form.md) over the family-pair grid).
[new]:
the [`gram_reduce`](../L4/gram_reduce.md) `w = 1/(IᵢIⱼ)` current-normalized inductance reduction (`rough-in (test-coverage-bounded)` — the same shared symmetric-Gram reduction as the electrostatic capacitance, the weight the only difference; it folds the now-firm L1 diagonal [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (firm c091) + still-rough-in off-diagonal [`bilinear-form`](../L1/bilinear-form.md) over the family-pair grid — the bilinear-form being the sole residual gate after the cycle-091 cascade).
```

**E1c. §Status (`:69`) — re-narrate the now-stale √-cascade NO-GO-HELD clause.**

```edit:book/src/feature/magnetostatic.L4.md
[old]:
The composition is sound (stage (1) the firm [`fe_assemble`](../L4/fe_assemble.md), the reduction a clean fold of evaluations), but one directly-owned constituent (`gram_reduce`) remaining rough-in is the own-constituent gate; firming `gram_reduce` is the remaining promotion route — and `gram_reduce` is itself convergently blocked on the `matrix-weighted-norm` √-cascade (NO-GO-HELD), so this column does not flip this cycle.
[new]:
The composition is sound (stage (1) the firm [`fe_assemble`](../L4/fe_assemble.md), the reduction a clean fold of evaluations), but one directly-owned constituent (`gram_reduce`) remaining rough-in is the own-constituent gate; firming `gram_reduce` is the remaining promotion route. The cycle-091 batch-29 firm-flip-and-cascade wave NARROWED that gate — `gram_reduce`'s diagonal `matrix-weighted-norm` folded primitive firmed (firm c091), so `gram_reduce` is now rough-in on its sole RESIDUAL off-diagonal [`bilinear-form`](../L1/bilinear-form.md) primitive (still rough-in on its own `lower-layer-shared-vocabulary` gate, D3 cycle-091 re-judgment), so this column does not flip this cycle.
```

#### E2. book/src/feature/magnetostatic.L1.md

**E2a. frontmatter `composes:` (`:9`) — matrix-weighted-norm label → firm.**

```edit:book/src/feature/magnetostatic.L1.md
[old]:
  - book/src/L1/matrix-weighted-norm.md (rough-in (test-coverage-bounded) — diagonal Aᵢᵀ K Aᵢ)
[new]:
  - book/src/L1/matrix-weighted-norm.md (firm c091 — diagonal Aᵢᵀ K Aᵢ; promoted to firm by the batch-29 firm-flip-and-cascade wave)
```

**E2b. body (`:37`) — "rough-in matrix-weighted-norm" → firm.**

```edit:book/src/feature/magnetostatic.L1.md
[old]:
   - diagonal `Mᵢᵢ = (Aᵢᵀ K Aᵢ)/Iᵢ²` — the operator-weighted self-form normalized by the squared current, the rough-in [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) squared (`matrix_weighted_norm(Aᵢ, K)² = Aᵢᵀ K Aᵢ`; the L0 source builds it directly as `M_mag->Mult(A_gf, H_gf)` then `linalg::Dot(A_gf, H_gf)`, then `/ (I_inc[i]*I_inc[i])`, `:129-131`).
[new]:
   - diagonal `Mᵢᵢ = (Aᵢᵀ K Aᵢ)/Iᵢ²` — the operator-weighted self-form normalized by the squared current, the now-firm [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) squared (`matrix_weighted_norm(Aᵢ, K)² = Aᵢᵀ K Aᵢ`; the L0 source builds it directly as `M_mag->Mult(A_gf, H_gf)` then `linalg::Dot(A_gf, H_gf)`, then `/ (I_inc[i]*I_inc[i])`, `:129-131`).
```

**E2c. body (`:39`) — the "(rough-in) + (rough-in)" fold pair → "(firm c091) + (rough-in)".**

```edit:book/src/feature/magnetostatic.L1.md
[old]:
   The result is the symmetric `M` (and its LAPACK inverse `Minv`, `:151-152`). This stage is a pure fold of current-normalized bilinear-form evaluations over the solution-family pair grid — no L1 operator is *new* here; the reduction composes [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (rough-in) + [`bilinear-form`](../L1/bilinear-form.md) (rough-in), with the current normalization a scalar weight on each entry.
[new]:
   The result is the symmetric `M` (and its LAPACK inverse `Minv`, `:151-152`). This stage is a pure fold of current-normalized bilinear-form evaluations over the solution-family pair grid — no L1 operator is *new* here; the reduction composes [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (firm c091) + [`bilinear-form`](../L1/bilinear-form.md) (rough-in), with the current normalization a scalar weight on each entry.
```

**E2d. §"Constituent down-links" table (`:60`) — matrix-weighted-norm status cell → firm.**

```edit:book/src/feature/magnetostatic.L1.md
[old]:
| diagonal Aᵢᵀ K Aᵢ / Iᵢ² | [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) | rough-in (test-coverage-bounded) | `magnetostaticsolver.cpp:129-131` |
[new]:
| diagonal Aᵢᵀ K Aᵢ / Iᵢ² | [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) | firm c091 | `magnetostaticsolver.cpp:129-131` |
```

**E2e. §Status (`:65`) — re-narrate "BOTH primitives rough-in"; STAYS seed.**

```edit:book/src/feature/magnetostatic.L1.md
[old]:
Two of the four composed L1 operators are firm ([`fe_assemble`](../L1/fe_assemble.md), [`ksp_solve`](../L1/ksp_solve.md)); BOTH inductance-reduction primitives are rough-in — the diagonal [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) is `rough-in (test-coverage-bounded)` (no dedicated test exercises the SPD-weighted overload) and the off-diagonal [`bilinear-form`](../L1/bilinear-form.md) is rough-in (its `α = xᴴ M y` signature covers the cross-pairing, so the down-link is correct). The entire stage-3 reduction therefore rests on rough-in L1 primitives — consistent with the column being a `seed`, not a firm composition.
[new]:
Two of the four composed L1 operators are firm ([`fe_assemble`](../L1/fe_assemble.md), [`ksp_solve`](../L1/ksp_solve.md)); of the two inductance-reduction primitives the diagonal [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) is now **firm** (firm c091, the batch-29 firm-flip-and-cascade wave) but the off-diagonal [`bilinear-form`](../L1/bilinear-form.md) is still rough-in (its `α = xᴴ M y` signature covers the cross-pairing, so the down-link is correct; the sole RESIDUAL gate after this cycle). The stage-3 reduction therefore still rests on the rough-in off-diagonal `bilinear-form` (whose L4 home `gram_reduce` STAYS `rough-in (test-coverage-bounded)` on that residual, D3 cycle-091) — consistent with the column being a `seed`, not a firm composition.
```

### F. output-product.md — re-anchor the energy-fields group-intro bullet

The output-product group-intro bullet for energy-fields calls `domain_energy_reduce` `rough-in` and `matrix-weighted-norm`-squared without re-anchoring; both firmed this cycle.

```edit:book/src/feature/output-product.md
[old]:
- [`energy-fields`](./energy-fields.L4.md) — **per-domain scalar-table (rank-1)**, the `domain_energy_reduce` per-domain `(energyᵢ, pᵢ)` map (minted c078, `rough-in`), folding the domain-restricted SPD energy form `½⟨field, M_i field⟩` ([`matrix-weighted-norm`](../L1/matrix-weighted-norm.md)-squared) and the **firm** [`participation_ratio`](../L1/participation_ratio.md), over a *single solution field* from any field-bearing driver (not a solution family). The per-domain sibling of the per-mode `eigenfrequency-qfactor` table. Levels: [L4](./energy-fields.L4.md) · [L1](./energy-fields.L1.md) · [L0](./energy-fields.L0.md).
[new]:
- [`energy-fields`](./energy-fields.L4.md) — **per-domain scalar-table (rank-1)**, the `domain_energy_reduce` per-domain `(energyᵢ, pᵢ)` map (minted c078, **firm** c091 — promoted by the batch-29 firm-flip-and-cascade wave once both its folded primitives firmed), folding the domain-restricted SPD energy form `½⟨field, M_i field⟩` (**firm** [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md)-squared, firm c091) and the **firm** [`participation_ratio`](../L1/participation_ratio.md), over a *single solution field* from any field-bearing driver (not a solution family). The per-domain sibling of the per-mode `eigenfrequency-qfactor` table. **The column is `firm`** (own composition all-firm). Levels: [L4](./energy-fields.L4.md) · [L1](./energy-fields.L1.md) · [L0](./energy-fields.L0.md).
```

### G. feature/index.md — count delta (firm 6→7, seed 6→5) + cohort/gate prose (SOLE OWNER)

**G1. §"output-product cohort" energy-fields bullet (`:53`) — re-anchor the rough-in labels.**

```edit:book/src/feature/index.md
[old]:
- **Per-domain scalar-table (rank-1)** — cycle-078: [`energy-fields`](./energy-fields.L4.md), over a *single solution field* (not a family) from any field-bearing driver. It composes the new `domain_energy_reduce` reduction (minted c078, `rough-in`) — a per-domain `(energyᵢ, pᵢ)` map folding the domain-restricted SPD energy form `½⟨field, M_i field⟩` ([`matrix-weighted-norm`](../L1/matrix-weighted-norm.md)-squared, rough-in) and the **firm** [`participation_ratio`](../L1/participation_ratio.md). It is the **per-domain** sibling of the per-mode `eigenfreq_qfactor_reduce` table (both reduce-to-scalar-TABLE, rank-1) — NOT a `gram_reduce` family-PAIR grid (the c074 D6 do-NOT-over-unify guard, honored).
[new]:
- **Per-domain scalar-table (rank-1)** — cycle-078: [`energy-fields`](./energy-fields.L4.md), over a *single solution field* (not a family) from any field-bearing driver. It composes the `domain_energy_reduce` reduction (minted c078, **firm c091** — promoted by the batch-29 firm-flip-and-cascade wave once both its folded primitives firmed) — a per-domain `(energyᵢ, pᵢ)` map folding the domain-restricted SPD energy form `½⟨field, M_i field⟩` (**firm** [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md)-squared, firm c091) and the **firm** [`participation_ratio`](../L1/participation_ratio.md). It is the **per-domain** sibling of the per-mode `eigenfreq_qfactor_reduce` table (both reduce-to-scalar-TABLE, rank-1) — NOT a `gram_reduce` family-PAIR grid (the c074 D6 do-NOT-over-unify guard, honored).
```

**G2. §"OWN-COMPOSITION promotion rule" paragraph (`:55`) — add energy-fields to the firm side; narrow the remaining-seed gate to gram_reduce-on-bilinear-form.**

```edit:book/src/feature/index.md
[old]:
Under the OWN-COMPOSITION promotion rule (CLAUDE.md §Extraction-goal FEATURE-SURFACE SPINE; user directive 2026-06-03; memory `project_feature_column_promotion_rule`), a feature column promotes off `seed` when its **OWN composition + directly-owned constituents** are firm; **cross-linked sibling columns are references, NOT blockers**. Two output-product columns whose own reduce verb is firm therefore promote — [`eigenfrequency-qfactor`](./eigenfrequency-qfactor.L4.md) (own verb `eigenfreq_qfactor_reduce`, firm c082) and [`sparameters`](./sparameters.L4.md) (own verb `sparameter_reduce`, firm c083) — independent of their producing driver cross-links. The remaining three output-product columns stay `seed` on a genuine **own-constituent gate**: their composed reduce verbs ([`gram_reduce`](../L4/gram_reduce.md) for capacitance/inductance; `domain_energy_reduce` + the folded [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) for energy-fields) are themselves `rough-in`. (This supersedes the earlier "promote only once ALL composed constituents — including cross-linked sibling columns — are firm" rule, which combined with the output-product↔driver reciprocal cross-link to make `seed` a permanent terminal state — the mutual-blocking deadlock the directive breaks.)
[new]:
Under the OWN-COMPOSITION promotion rule (CLAUDE.md §Extraction-goal FEATURE-SURFACE SPINE; user directive 2026-06-03; memory `project_feature_column_promotion_rule`), a feature column promotes off `seed` when its **OWN composition + directly-owned constituents** are firm; **cross-linked sibling columns are references, NOT blockers**. Three output-product columns whose own reduce verb is firm therefore promote — [`eigenfrequency-qfactor`](./eigenfrequency-qfactor.L4.md) (own verb `eigenfreq_qfactor_reduce`, firm c082), [`sparameters`](./sparameters.L4.md) (own verb `sparameter_reduce`, firm c083), and [`energy-fields`](./energy-fields.L4.md) (own verb `domain_energy_reduce`, firm c091 — promoted by the cycle-091 batch-29 firm-flip-and-cascade wave once both its folded primitives firmed: `matrix-weighted-norm` c091 + `participation_ratio` c077) — independent of their producing driver cross-links. The remaining two output-product columns ([`capacitance`](./capacitance.L4.md) / [`inductance`](./inductance.L4.md)) stay `seed` on a genuine **own-constituent gate**: their composed reduce verb [`gram_reduce`](../L4/gram_reduce.md) is itself `rough-in (test-coverage-bounded)` — and after the cycle-091 cascade that gate has NARROWED to its sole residual off-diagonal [`bilinear-form`](../L1/bilinear-form.md) folded primitive (the diagonal [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) firmed c091; `bilinear-form` stays rough-in on its own `lower-layer-shared-vocabulary` gate). (This supersedes the earlier "promote only once ALL composed constituents — including cross-linked sibling columns — are firm" rule, which combined with the output-product↔driver reciprocal cross-link to make `seed` a permanent terminal state — the mutual-blocking deadlock the directive breaks.)
```

**G3. §"Chapter-kind status" block (`:61-72`) — firm 6→7, seed 6→5; move energy-fields to firm; narrow the gram_reduce-gated columns' gate prose; flip energy-fields' seed-list bullet out.**

```edit:book/src/feature/index.md
[old]:
- **`firm` (6 columns)** — own composition + directly-owned constituents all firm; cross-linked sibling columns are references, not blockers:
  - driver-leaf: [`eigenmode`](./eigenmode.L4.md) (own `fe_assemble`×3 + `eigsolve` firm; `eigenfrequency-qfactor` is a sibling cross-link), [`driven`](./driven.L4.md) (own `fe_assemble` + `assemble_frequency_operator` + `frequency_sweep` + `ksp_solve` firm; `sparameters` is a sibling cross-link), [`transient`](./transient.L4.md) (own `fe_assemble` + `fold_solve` firm; no output-product sibling).
  - output-product: [`eigenfrequency-qfactor`](./eigenfrequency-qfactor.L4.md) (own verb `eigenfreq_qfactor_reduce` firm c082; `eigenmode` is a sibling cross-link), [`sparameters`](./sparameters.L4.md) (own verb `sparameter_reduce` firm c083; `driven` is a sibling cross-link).
  - spine-ROOT: [`lifecycle`](./lifecycle.L4.md) (own driver-agnostic composition — mesh-build + the firm `fold_solve` adaptive fold — firm; the per-driver dispatch is over sibling feature columns, references not blockers).
- **`seed` (6 columns)** — held on a genuine **own-constituent gate** (a directly-owned constituent is rough-in or unhomed), NOT a sibling-column blocker:
  - [`electrostatic`](./electrostatic.L4.md) + [`magnetostatic`](./magnetostatic.L4.md) — own `gram_reduce` is `rough-in (test-coverage-bounded)` (own `solve_family` firmed c086; the own-constituent gate has narrowed to `gram_reduce` alone).
  - [`capacitance`](./capacitance.L4.md) + [`inductance`](./inductance.L4.md) — own reduce verb `gram_reduce` is `rough-in`.
  - [`energy-fields`](./energy-fields.L4.md) — own reduce verb `domain_energy_reduce` + own folded `matrix-weighted-norm` are `rough-in`.
  - [`boundary-mode`](./boundary-mode.L4.md) — own stage-(3) readout reduces into a waveguide-mode output product with no firm home (own-readout gate; the waveguide-mode product column is demand-gated).
[new]:
- **`firm` (7 columns)** — own composition + directly-owned constituents all firm; cross-linked sibling columns are references, not blockers:
  - driver-leaf: [`eigenmode`](./eigenmode.L4.md) (own `fe_assemble`×3 + `eigsolve` firm; `eigenfrequency-qfactor` is a sibling cross-link), [`driven`](./driven.L4.md) (own `fe_assemble` + `assemble_frequency_operator` + `frequency_sweep` + `ksp_solve` firm; `sparameters` is a sibling cross-link), [`transient`](./transient.L4.md) (own `fe_assemble` + `fold_solve` firm; no output-product sibling).
  - output-product: [`eigenfrequency-qfactor`](./eigenfrequency-qfactor.L4.md) (own verb `eigenfreq_qfactor_reduce` firm c082; `eigenmode` is a sibling cross-link), [`sparameters`](./sparameters.L4.md) (own verb `sparameter_reduce` firm c083; `driven` is a sibling cross-link), [`energy-fields`](./energy-fields.L4.md) (own verb `domain_energy_reduce` firm c091 + own folded `matrix-weighted-norm` firm c091 + `participation_ratio` firm c077 — promoted `seed`→`firm` cycle-091 by the batch-29 firm-flip-and-cascade wave; driver-agnostic, the field-bearing driver columns are sibling references not blockers).
  - spine-ROOT: [`lifecycle`](./lifecycle.L4.md) (own driver-agnostic composition — mesh-build + the firm `fold_solve` adaptive fold — firm; the per-driver dispatch is over sibling feature columns, references not blockers).
- **`seed` (5 columns)** — held on a genuine **own-constituent gate** (a directly-owned constituent is rough-in or unhomed), NOT a sibling-column blocker:
  - [`electrostatic`](./electrostatic.L4.md) + [`magnetostatic`](./magnetostatic.L4.md) — own `gram_reduce` is `rough-in (test-coverage-bounded)` (own `solve_family` firmed c086; after the cycle-091 cascade the own-constituent gate has narrowed further — `gram_reduce`'s diagonal `matrix-weighted-norm` folded primitive firmed c091, leaving `gram_reduce` rough-in on its sole residual off-diagonal `bilinear-form`).
  - [`capacitance`](./capacitance.L4.md) + [`inductance`](./inductance.L4.md) — own reduce verb `gram_reduce` is `rough-in` (the residual gate after c091 is its off-diagonal `bilinear-form` folded primitive; the diagonal `matrix-weighted-norm` firmed c091).
  - [`boundary-mode`](./boundary-mode.L4.md) — own stage-(3) readout reduces into a waveguide-mode output product with no firm home (own-readout gate; the waveguide-mode product column is demand-gated).
```

**G4. §"Chapter-kind status" intro sentence (`:61`) — note the cycle-091 cascade re-evaluation (the c085 re-eval line is updated to record c091's flip).**

```edit:book/src/feature/index.md
[old]:
**Cycle-085 re-evaluated all 13 columns under the OWN-COMPOSITION promotion rule** (memory `project_feature_column_promotion_rule`); the cohort is no longer blanket-`seed`:
[new]:
**Cycle-085 re-evaluated all 13 columns under the OWN-COMPOSITION promotion rule** (memory `project_feature_column_promotion_rule`); the cohort is no longer blanket-`seed`. **Cycle-091 (the batch-29 firm-flip-and-cascade wave) re-evaluated the cascade-touched columns** under the same rule: the `matrix-weighted-norm` firm flip firmed `domain_energy_reduce`, which flipped [`energy-fields`](./energy-fields.L4.md) `seed`→`firm` (its OWN composition became all-firm); the `gram_reduce`-gated columns stayed `seed` (their off-diagonal `bilinear-form` folded primitive is the residual gate). The current split:
```

## feature/index.md count delta (the exact old→new)

| Cohort | Old (on-disk) | New |
|---|---|---|
| `firm` columns | **6** | **7** (+ energy-fields) |
| `seed` columns | **6** | **5** (− energy-fields) |

energy-fields moves seed→firm; the 4 `gram_reduce`-gated columns + boundary-mode stay seed (5 total).

## Supporting evidence

- **energy-fields OWN composition (the flip basis):** `book/src/feature/energy-fields.L4.md` `composes:` (`:6-10`) + §"Constituent down-links" (`:156-161`) — directly-owned constituents `domain_energy_reduce` (firm c091, D3), `matrix-weighted-norm` (firm c091, D1), `participation_ratio` (firm c077). Driver columns listed in `composes:` are SIBLING references (driver-agnostic, `:151`). No other rough-in own-constituent.
- **gram_reduce STAYS rough-in (the 4 stay-seed columns' basis):** D3 report §"PROMINENT VERDICT" + §gram_reduce — bilinear-form residual gate, `bilinear-form.md:4` `firmness: rough-in` confirmed on disk.
- **D3 verdict source:** `reports/2026-06-04T053300Z-lowering-verifier-cycle-091-reduce-verb-rejudgment/CYCLE.md` §"PROMINENT VERDICT FOR D4" — `domain_energy_reduce` → FLIP firm, `gram_reduce` → STAYS rough-in.
- **Current on-disk index counts:** `book/src/feature/index.md` §"Chapter-kind status" — `firm (6 columns)` (`:63`) / `seed (6 columns)` (`:67`).
- **Stale-label catalog (re-anchored to firm):** all `matrix-weighted-norm` rough-in maturity labels in capacitance.{L4,L1}, inductance.{L4,L1}, electrostatic.{L4,L1}, magnetostatic.{L4,L1}, energy-fields.{L4,L1}, output-product.md, index.md — enumerated per-file above. The L0 driver-column files (capacitance/electrostatic/inductance/magnetostatic) carry `matrix-weighted-norm` only as a lift-target NAME with no maturity token → no re-anchor needed.

## Scope confirmation

I stayed entirely within `book/src/feature/`. Files touched (proposed-changes only): `energy-fields.{L4,L1,L0}.md`, `capacitance.{L4,L1}.md`, `inductance.{L4,L1}.md`, `electrostatic.{L4,L1}.md`, `magnetostatic.{L4,L1}.md`, `output-product.md`, `index.md`. I did NOT touch `book/src/L1/index.md`, `book/src/L4/index.md` (D1+D3), `book/src/L4/gram_reduce.md` / `domain_energy_reduce.md` (D3), `book/src/L1/matrix-weighted-norm.md` (D1), or any vocabulary-spine consumer file (D2).

## Open questions / caveats

- **bilinear-form is the sole remaining gate for the 4 gram_reduce-gated columns** (electrostatic/magnetostatic/capacitance/inductance). After this cycle the cascade has discharged `matrix-weighted-norm`; the next gate that would unblock these four is `bilinear-form` firming (its own `rough-in (lower-layer-shared-vocabulary, cycle-010-wave-1)` status, a separate dischargeability question NOT in this cycle's scope). This is the natural batch-29 forward-frontier follow-up D3's gram_reduce verdict surfaces — I have NOT added a fresh candidate (consistent with the plan's matching note); the meta-phase will see it.
- **All proposed-changes `old_string`s verified on-disk this dispatch.** After the first draft I re-read the inductance.L4 / magnetostatic.L4 / inductance.L1 files (the three I had reconstructed from grep previews) and corrected the blocks to the exact on-disk text: inductance.L4 carries NO matrix-weighted-norm maturity LABEL (its only edit is the §Status generic "folded L1 bilinear primitives are rough-in" → bilinear-form-residual re-narration, C1a); magnetostatic.L4's down-link row + §Status both carry a `matrix-weighted-norm` (diagonal) folded label that I re-anchor (E1a/E1b/E1c); inductance.L1's §Status reads "BOTH directly-owned bilinear primitives are rough-in" (C2e fixed to that exact text). Every other block's `old_string` was confirmed by fixed-string grep against the on-disk file.
- **energy-fields L0 (A3)** carries no constituent-maturity rough-in label to re-anchor (only the `status:` frontmatter + §Status line flip); the L0 file names `matrix-weighted-norm`/`domain_energy_reduce` as lift-target names without tokens, so it is the lightest of the three flips.
