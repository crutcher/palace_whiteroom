---
verifies: ../CYCLE.md
critiqued_at: 2026-06-01T16:43:18Z
critic_version: 1
checks:
  citation-validity: warning
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: pass
  cross-reference-integrity: pass
  edge-label-fidelity: warning
  plan-kind-consistency: pass
  skill-uptake-survey: pass
repaired_at: 2026-06-01T17:05:00Z
repairer_version: 1
repairs:
  citation-validity: repaired
  surface-or-evidence: not-needed
  rotation-quality: not-needed
  variant-axis-coverage: not-needed
  cross-reference-integrity: not-needed
  edge-label-fidelity: repaired
  plan-kind-consistency: unrepairable
  skill-uptake-survey: not-needed
overall_status: ready
follow_up_agent: null
---

# META: verification of "L4 intro — anchor the `solve-monad` outer-driver vocabulary"

## Critique

### Checks run

**citation-validity — warning.** Ran `tools/citecheck/citecheck.py --scan` on CYCLE.md: 12 ok, 0 failing (all `reference/`-tree palace cites pass bounds). Book-internal cites verified by hand against disk. The load-bearing anchors all hold: `book/src/L4/index.md:47` is exactly the "Queued at L4 — none currently" deferral line being discharged; the dep-map `:53-56` rows match. `book/src/concepts/solve-monad.md` confirms every cited construct — `Solve a = StateT SimState Identity a` (`:8`), the `solve op inp = execState (solve_loop op inp) initial_state` entry (`:11`, covered by the report's `:7-17` span cite), the `solve_loop`/`restart_cycle`/`one_cycle` recursion (`:13-17`), the `restart_cycle` body + Krylov-as-plain-value lifecycle (`:51-56`), and `Outcome = Continue | Done Bool` with its three classified paths (`:60-66`, the report's `:58-68` span). The two L3 consumer anchors are valid: `L3/ksp_solve.md:142` (the `solve-monad` "L4 outer-driver surface ... dissolved to explicit `iterate_while_L3`") and `:160` (the `convergence-failure-policy` soft-fail `Bool` → `Outcome` lift). All six `L3/eigsolve.md` pinpoints (`:19,:34,:78,:166,:203,:214`) land on genuine references to the unauthored `solve-monad` L4 surface / richer sum-typed termination. The L1 anchors `L1/eigsolve.md:78` (the `PartialConverged`/`K < K_max` partial-success case = `0 < converged < requested`) and `L1/ksp_solve.md:91` (`Bool` → `Outcome` sum lift) both support their claims. The warning is a single pinpoint drift in the `restart_cycle` dep-map row (CYCLE.md:62): it cites `[L3/ksp_solve]:90` for "the restart-shaped *outer* of the double-nested fold ... (GMRES/FGMRES re-seed-`K` outer loop)" — but line 90 is the `fold_iterate` final-iterate-materialisation paragraph; the GMRES/FGMRES double-nested re-seed-`K` outer restart loop is described at `:94`. The same `:90` pinpoint *does* support the second half of the clause ("the `modify`-once correction ↦ positional `fold_iterate` at restart boundaries"), so one pinpoint is being asked to carry two claims and lands on only one (+4-line drift for the re-seed-loop half). See Issue 1.

**surface-or-evidence — pass.** This is a vocabulary-anchor proposal that modifies surface (the L4 index `## Vocabulary cohort` cohort block, the `:47` deferral prose, three new dep-map rows). It is not a rotation-claim-only refinement, so the surface-or-evidence gate is satisfied by the surface edits themselves; the rows carry concept-page + strawman evidence for the vocabulary. The substantive question the dispatch raised — whether `firm` is justified — is addressed under plan-kind-consistency and Issue 2 below; the maturity question does not trip surface-or-evidence (the surface is present and evidenced).

**rotation-quality — pass (largely no-op).** Not applicable as a primary check — this is a vocabulary anchor, not an L_{n+1}→L_n rotation proposal. The rows *describe* downward lowering-direction (e.g. `solve_loop` → L3 explicit `iterate_while_L3` tail-recursion; `Outcome` → L3 soft-fail `Bool`), but those are forward-references to themes that "ride the cap chapters (cycle-048)", not rotation claims this dispatch asserts and must prove. No renaming-only-masquerading-as-rotation issue. The single genuine abstraction note — `Outcome` (sum type) collapsing the L3 form's scattered termination tests into a single decision site — is correctly attributed to the concept page, not claimed as this dispatch's rotation work.

**variant-axis-coverage — pass.** The one variant axis in scope is the restart/non-restart split, and it is explicitly handled: `restart_cycle` carries `one_cycle` as the "non-restarted-solver specialisation" in both the cohort block and the dep-map row (no hidden branch). The `Outcome` partial-success axis (`ksp_solve` 3-arm vs `eigsolve` 4-arm `0 < converged < requested`) is explicitly *scoped out* of this anchor and deferred — the report names it as a single canonical pattern with the eigsolve extension flagged as a clean follow-up (Open question 3). That is an explicit scope-out, not a hidden branch.

**cross-reference-integrity — pass.** Build-readiness verified mechanically. Fence parity: `grep -n '```'` returns 6 markers — three `edit:book/src/L4/index.md` blocks opening at 25/50/58 and closing at 48/56/64, balanced, no nested fences. All three `[old]` strings match disk byte-exactly (`book/src/L4/index.md` lines 32-39, line 47, line 56 respectively — confirmed by `diff`). Every live markdown link in the new content resolves on-disk (13 distinct links: `./chebyshev.md`, `./iterate-while.md`, `./iterate-while-with-prev.md`, `./krylov-step.md`, `../concepts/{solve-monad,state-stratification,first-iteration-unrolling}.md`, `../L1/{eigsolve,ksp_solve}.md`, `../L2/chebyshev-iteration.md`, `../L3/{chebyshev,eigsolve,ksp_solve}.md`). The concept slugs in the `Concepts:` columns (`convergence-test`, `sequential-obstruction`, `constructed-operators`) all exist on disk and are rendered plain-text (matching the existing dep-map convention). Critically, the **premature-link guard holds**: `book/src/L4/ksp_solve.md` and `book/src/L4/eigsolve.md` do NOT exist on disk, and the report references them ONLY as backtick-quoted plain text (`` `L4/ksp_solve` ``, `` `L4/eigsolve` ``) marked "forthcoming"/"cycle-048" — no live or dead link to either, so no `linkcheck2` break. `L4/index.md` is already wired into `SUMMARY.md:7`, so no SUMMARY change is needed for this in-place EDIT. The `Outcome` table cell escapes its literal pipe (`Continue \| Done Bool`), so the markdown table parses.

**edge-label-fidelity — warning.** The new rows' dependency edges are correctly labelled: `solve_loop` sits *above* `iterate-while` and consumes `restart_cycle`/`Outcome`; `restart_cycle` runs the inner `iterate-while`-family fold, is consumed by `solve_loop`, and produces `Outcome`; `Outcome` is produced-by `restart_cycle` / pattern-matched-by `solve_loop` — these intra-L4 edges are internally consistent and match the concept page's `solve_loop`/`restart_cycle`/`Outcome` structure. The FLOOR-LANDING-REANCHOR boundary is honored correctly: the report touches ONLY `book/src/L4/index.md` (all three `edit:` blocks), so the L3 `eigsolve`/`ksp_solve` "no firm L4 cap exists / not yet authored" assertions (`L3/eigsolve.md:19,:34,:78,:166,:203,:214`) stay TRUE and untouched — anchoring rows did not erroneously re-anchor any cap-existence assertion. The warning is the lowering-direction-edge pinpoint drift carried over from citation-validity: the `restart_cycle` row's L3-lowering-direction annotation ("the restart-shaped *outer* of the double-nested fold per `[L3/ksp_solve]:90`") labels the edge against `:90` when the outer-restart-loop content is at `:94`. The edge *direction* (L4 `restart_cycle` → L3 outer restart loop) is correct; only the pinpoint anchor is off. See Issue 1.

**plan-kind-consistency — warning-adjacent / pass-with-flag.** The declared shape (vocabulary anchor: signature + role + first-two-consumers + L3 lowering-direction, with Algebraic-laws deferred to the cap chapters per the `book/src/L4/index.md:65` dep-map contract) matches the content. The `firm` maturity call is the dispatch's flagged "maturity straddle" (Open question 2). I judge `firm` *defensible as a vocabulary anchor* but NOT clean: the three rows' signatures, roles, and the `Outcome` sum are fully determined by `book/src/concepts/solve-monad.md` (nothing about the *vocabulary* is speculative), and the dep-map row contract genuinely excludes Algebraic-laws (they ride the operator page). However, every *other* `firm` row in this dep-map (`krylov-step`, `iterate-while`, `chebyshev`) is backed by an authored operator *page* whose laws have been harvested; these three are `firm` rows with NO operator page yet (the pages are the cycle-048 caps). That is a real maturity asymmetry the integrator/cycle-048 planner should see — `firm`-the-row vs `firm`-the-operator. The report itself surfaces this honestly (Open question 2: "if the c048 cap authors find the rows under-specified ... a thin harvester pass ... may be warranted"). This is a flag for downstream attention, not a mis-classification I can call `fail`: the row content *is* firm by the row contract; the asymmetry is in what "firm row without a firm page" means. See Issue 2. Plan-kind otherwise consistent: scope boundary (vocabulary anchor, NOT cap chapters) respected — `L4/ksp_solve.md`/`L4/eigsolve.md` not authored, named forthcoming only.

**skill-uptake-survey — pass.** The dispatch's shape (citation-bounds verification, fence-parity build-readiness, on-disk link resolution) implies the `verify-citation-range` / `proposed-changes-fence-encloses-full-body-guard` family, but those are critic-side skills, not producer-invoked authoring procedures; no producer-side skill is strongly implied by a vocabulary-anchor dep-map edit (the layer-intro-author dep-map/Vocabulary-cohort conventions are role-spec, not a skill). The report cites its role spec and the strawman authority explicitly. Pure telemetry; nothing blocking.

### Issues found

**Issue 1 (citation-validity / edge-label-fidelity, severity: warning) — `restart_cycle` dep-map row pinpoint drift `:90` → `:94`.** In CYCLE.md:62 (the `restart_cycle` row, `Lowers to`/L3 column), the clause "the `restart`-shaped *outer* of the double-nested fold per [`L3/ksp_solve`](../L3/ksp_solve.md):90 (GMRES/FGMRES re-seed-`K` outer loop)" pins the GMRES/FGMRES re-seed-`K` outer restart loop to `L3/ksp_solve.md:90`. Line 90 is the `fold_iterate` final-iterate-materialisation paragraph; the double-nested / outer-restart-loop re-seed content is at `L3/ksp_solve.md:94` ("The **restart nesting** ... GMRES/FGMRES are a **double-nested** fold: the outer restart loop ..."). The same `:90` pinpoint correctly supports the *adjacent* clause ("the `modify`-once correction ↦ positional `fold_iterate` at restart boundaries" — line 90 IS the `fold_iterate` restart-boundary correction), so the single pinpoint is double-duty and lands on only one of its two referents. Repair candidate: split the pinpoint — keep `:90` for the `fold_iterate`-correction half, retarget the re-seed-outer-loop half to `:94` (or cite `:94` alongside `:90`).

**Issue 2 (plan-kind-consistency, severity: warning / for-integrator-and-cycle-048-attention) — `firm`-row maturity straddle without a backing operator page.** The three new rows are marked `firm` while every other `firm` row in this dep-map is backed by an authored, law-harvested operator page; these three have no page yet (the pages are the cycle-048 R2/R3 caps). The `firm` call is *defensible by the dep-map row contract* (signature + role + consumers present; Algebraic-laws excluded from the dep-map per `index.md:65`; vocabulary fully determined by `concepts/solve-monad.md`), and the report flags it honestly (Open question 2). But the asymmetry — "firm row, no firm page" — is real and load-bearing for the cycle-048 cap authors, who will need genuine per-operator harvest depth (monad-law / `execState`-fusion identities, the full `Outcome`-classification variant axis, demand-pruning interaction) that the row does not carry. Not a `fail` (no claim is unsupported; the row IS firm by its contract), but the integrator and cycle-048 planner should see this surfaced: a thin harvester row-depth pass on the outer-driver vocabulary may be warranted before/with the caps, as the report itself recommends. No mechanical repair — this is a judgment flag for downstream, raised here per the dispatch's explicit request to evaluate the firm-call.

**Issue 3 (citation-validity, severity: nit) — `Outcome` row omits the strongest partial-success anchor from its own cell.** The `Outcome` dep-map row (CYCLE.md:63) cites the partial-success extension as `[L3/eigsolve]:166`; `:166` says "sum-typed termination richer than `ksp_solve`'s soft-fail" but does not itself enumerate the `0 < converged < requested` case. The page that *does* enumerate that exact case (`PartialConverged`, `K < K_max`) is `L1/eigsolve.md:78`, which the report cites in §Supporting evidence but not in the `Outcome` row cell. Non-blocking: the claim is supported by the report's evidence set as a whole; the row's pinpoint is merely the weaker of the two available anchors. Optional repair: add `L1/eigsolve.md:78` alongside `:166` in the `Outcome` row's partial-success clause (consistent with the `Bool`-lift clause, which already pairs `L1/ksp_solve.md:91` + `L3/ksp_solve.md:160`).

## Repair

### Fixes attempted

- **Finding (Issue 1, citation-validity + edge-label-fidelity, warning):** the `restart_cycle` dep-map row (CYCLE.md:62) pins the GMRES/FGMRES re-seed-`K` outer-restart loop to `[L3/ksp_solve]:90`, but `:90` is the `fold_iterate` final-iterate-materialisation paragraph; the outer-restart re-seed content is at `:94`. One pinpoint doing double-duty.
  - **Decision:** repaired.
  - **Action:** CYCLE.md §Proposed changes, 3rd `edit:` block, `restart_cycle` row (`Lowers to`/L3 column). Split the double-duty pinpoint: retargeted the re-seed-outer-loop half to `[L3/ksp_solve]:94` (cited as "the **restart nesting**: GMRES/FGMRES re-seed-`K` outer loop wrapping the inner Arnoldi fold"), and kept `[L3/ksp_solve]:90` on the `modify`-once `fold_iterate`-correction half. Both pinpoints verified against on-disk `book/src/L3/ksp_solve.md`: `:90` is the `fold_iterate` paragraph, `:94` is the "**restart nesting** ... double-nested fold: the outer restart loop ... re-seeds `K`" paragraph. This repairs both the citation-validity pinpoint drift and the edge-label-fidelity lowering-direction-edge anchor (the edge direction was already correct; only the anchor was off).

- **Finding (Issue 3, citation-validity, nit):** the `Outcome` row (CYCLE.md:63) cites the partial-success extension only as `[L3/eigsolve]:166` (the weaker anchor, which says "richer than `ksp_solve`'s soft-fail" but does not enumerate the `0 < converged < requested` case); the stronger anchor `L1/eigsolve.md:78` (already in §Supporting evidence) enumerates exactly that `PartialConverged` case.
  - **Decision:** repaired.
  - **Action:** CYCLE.md §Proposed changes, 3rd `edit:` block, `Outcome` row (`Lowers to`/L3 column). Added `[L1/eigsolve]:78` alongside `:166` in the partial-success clause and named the case inline (`0 < converged < requested` / `PartialConverged`), matching the `Bool`-lift clause's L1+L3 anchor pairing. Verified `L1/eigsolve.md:78` on disk: it frames partial convergence as "the L1 form's distinguishing semantic feature relative to `ksp_solve`" — the count strictly-less-than-requested-without-failure case — which is exactly the claim. New live link `../L1/eigsolve.md` resolves on disk (the critic confirmed the file exists and is already linked elsewhere in the report).

- **Finding (Issue 2, plan-kind-consistency, warning / for-integrator-and-cycle-048-attention):** the 3 new dep-map rows are marked `firm` but have no backing operator page, unlike every other firm row in the dep-map.
  - **Decision:** unrepairable (not a mechanical defect — a maturity judgment call).
  - **Rationale:** flipping `firm` → `rough-in` is a substantive maturity decision, not a surgical fix, and the critic explicitly judged `firm` *defensible by the dep-map row contract* (signature + role + first-two-consumers present; Algebraic-laws excluded from the dep-map per `index.md:65`; vocabulary fully determined by `concepts/solve-monad.md`). Both readings are real: (a) **firm-the-row** is correct — the row content carries everything the L4 dep-map row contract requires, nothing about the *vocabulary* is speculative; (b) **firm-the-page asymmetry** is real — every other firm row is backed by an authored, law-harvested operator page, and these three have only the cycle-048 R2/R3 caps forthcoming, which will need genuine per-operator harvest depth (monad-law / `execState`-fusion identities, the full `Outcome`-classification variant axis, demand-pruning interaction) the row does not carry. This is a downstream judgment flag, not a verdict-inverting defect. Routed to integrator/cycle-048 awareness (see Suggested resolution).

### Unrepairable findings

- **Issue 2 — `firm`-row maturity straddle.** Not mechanically repairable: maturity flips are content decisions outside repair authority, and the critic judged `firm` defensible. Routed to **integrator-per-report** (may confirm `firm` or request `rough-in` at apply time) and **cycle-048 cap authors** (who will firm the backing `L4/ksp_solve.md` / `L4/eigsolve.md` pages and may run a thin harvester row-depth pass on the outer-driver vocabulary, as the report's Open question 2 recommends). No follow-up agent blocks readiness — this is an awareness flag, not a revision requirement.

## Suggested resolution

`overall_status: ready`. Content is sound; the two repairable warnings were pinpoint drifts (now fixed and verified against disk), and the maturity-straddle is a non-verdict-inverting integrator/cycle-048 judgment flag, not a defect.

Notes for the integrator:
- The two repairs are confined to the 3rd `edit:` block's row-cell prose; the three `[old]` anchor strings are untouched, fence parity is unchanged (3 balanced `edit:` blocks), and both repairs introduce only already-resolving live links (`../L3/ksp_solve.md`, `../L1/eigsolve.md`). Build-readiness is preserved.
- On the `firm`-row straddle: the integrator-per-report may apply the rows as `firm` (defensible by the row contract) or down-grade to `rough-in` if it prefers firm-row⇔firm-page symmetry. Either is consistent with the critique; surfacing it so the cycle-048 planner sees the "firm row, no firm page" asymmetry and can schedule the cap-author harvest depth (the OQ `l4-ksp-solve-eigsolve-caps-gated-on-solve-monad-outer-driver-vocabulary` should be re-scoped to "caps unblocked; awaiting cycle-048 authoring", per the report's Open question 1).
