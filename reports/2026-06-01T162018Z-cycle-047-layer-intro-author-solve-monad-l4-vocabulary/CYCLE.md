---
agent: layer-intro-author
invoked_at: 2026-06-01T16:27:12Z
scope: L4 index — anchor the solve-monad outer-driver vocabulary (restart_cycle / solve_loop / Outcome)
status: integrated
integrated_at: 2026-06-01T171229Z
integration_commit: bcd3bed
integration_notes: "Applied clean (D5; cycle-047). L4/index.md anchor: §Vocabulary-cohort header (4)->(4 + 3 outer-driver) + solve-monad block; §Queued-at-L4 :47 deferral discharge; 3 NEW firm dep-map rows (solve_loop/restart_cycle/Outcome). Maturity adjudicated firm at integration (dep-map row contract excludes laws; vocabulary fully determined by concepts/solve-monad.md; the page-existence asymmetry surfaced as a cycle-048 cap-author harvest-depth follow-up note). Anchor-half of l4-ksp-solve-eigsolve-caps-gated-on-solve-monad-outer-driver-vocabulary DISCHARGED; the R2/R3 caps L4/ksp_solve.md + L4/eigsolve.md remain queued for cycle-048. OQs opened: solve-monad-l4-row-firm-maturity-straddle + outcome-sum-one-row-vs-per-cap-specialisation. Build clean, linkcheck2 green (premature-link guard PASS — cap pages absent, no live link)."
---

# CYCLE: L4 intro — anchor the `solve-monad` outer-driver vocabulary

## Summary

Discharges the `:47` deferral in `book/src/L4/index.md` for the **outer-driver vocabulary half**: anchors the `solve-monad`'s outer-driver surface — `solve_loop` / `restart_cycle` (+ the non-restarted `one_cycle`) drivers and the `Outcome = Continue | Done Bool` termination sum — as explicit L4 vocabulary rows. This is the verified prerequisite for the cycle-048 `L4/ksp_solve.md` (R2) and `L4/eigsolve.md` (R3) caps, both of which are gated on this surface being anchored: `book/src/L3/ksp_solve.md:142,:160` and `book/src/L3/eigsolve.md:19,:34,:78,:166,:203,:214` all reference an unanchored `solve-monad` outer-driver / `Outcome`-sum surface as their future L4 home.

The vocabulary is **firmly specifiable** from `book/src/concepts/solve-monad.md` (the `Solve a = StateT SimState Identity a` definition, the `solve_loop` recursion, the `restart_cycle` body, and the §"Termination as a sum type" `Outcome = Continue | Done Bool` with its three classified paths) + the strawman monad conventions (`book/src/design/l4_calculus.md` §§3.6–3.7). The rows are marked **`firm`** as **vocabulary anchors** (signature + role + first-two-consumers evidence) — the dep-map convention being that Algebraic-laws live in the operator page, not the dep-map row; the per-operator *law* depth rides the cycle-048 CAP chapters (`L4/ksp_solve.md`, `L4/eigsolve.md`), which the rows name as forthcoming. See Open questions for the maturity-boundary note (this anchor straddles layer-intro-author concept-page authority + harvester L4-row authority; the planner judged vocabulary-anchor sufficient to unblock).

Three edits to `book/src/L4/index.md`:
1. Extend the §"Vocabulary cohort" Firm-at-L4 cohort with a `solve-monad` outer-driver block (count `(4)` → `(4 + 3 outer-driver)`).
2. Flip the `:47` "Queued at L4" deferral prose (outer-driver vocabulary now discharged; the CAP chapters remain queued).
3. Add three rows (`solve_loop` / `restart_cycle` / `Outcome`) to the operator dep-map table.

**FLOOR-LANDING-REANCHOR note (honored):** anchoring the `solve-monad` ROWS does NOT land a CAP chapter, so the L3 `eigsolve` / `ksp_solve` "no firm L4 cap exists / not yet authored" assertions stay TRUE and are NOT re-anchored this cycle. The new rows note the cap consumers are forthcoming (cycle-048).

## Proposed changes

```edit:book/src/L4/index.md
[old]: **Firm at L4 (4)** — the typed-wrapper Krylov step kernel, the two value-threading loop combinators that drive it, and the fixed-degree polynomial smoother:

- [`krylov-step`](./krylov-step.md) — typed-wrapper Krylov step kernel against the three-stratum state record; Form A consumes `iterate-while`, Form B consumes `iterate-while-with-prev`. The L4 calculus's first firm step-body shape.
- [`iterate-while`](./iterate-while.md) — value-threaded tail-recursive loop with demand-pruned trajectory of per-step extras; canonical iteration primitive at L4 (every iterative algorithm reduces to one or more folds). Inherits small-step semantics from the strawman §3.7.
- [`iterate-while-with-prev`](./iterate-while-with-prev.md) — carry-bootstrapped variant of `iterate-while` that threads a `PrevCarry` closure parameter for the previous-iteration recurrence variable. The driver for [`first-iteration-unrolling`](../concepts/first-iteration-unrolling.md)'s unrolled form; degenerates to `iterate-while` when `β = ()`.
- [`chebyshev`](./chebyshev.md) — typed-wrapper fixed-degree polynomial smoother; the `ChebOp` constructed-operator closure absorbs the polynomial-kind variant via distinct closure types; `apply` is a `Solve (ChebSim E)` action over a capability-typed `ChebSim`. Inner-product-free and convergence-test-free (the fixed-degree contrast to `krylov-step`): both bounded obstructions (outer `pc_it`, inner `k`) are `iterate_while_pure` folds with **step-count predicates** (the counter folded into the carry), reusing the firm [`iterate-while`](./iterate-while.md) family rather than a separate iteration vocabulary (cycle-014 combinator-miner route (i); re-anchored cycle-015). The fixed-count-vs-convergence distinction lives in the predicate, not the combinator.

**Rough-in at L4 (0)** — none currently.
[new]: **Firm at L4 (4 + 3 outer-driver)** — the typed-wrapper Krylov step kernel, the two value-threading loop combinators that drive it, and the fixed-degree polynomial smoother; plus the three `solve-monad` outer-driver vocabulary anchors:

- [`krylov-step`](./krylov-step.md) — typed-wrapper Krylov step kernel against the three-stratum state record; Form A consumes `iterate-while`, Form B consumes `iterate-while-with-prev`. The L4 calculus's first firm step-body shape.
- [`iterate-while`](./iterate-while.md) — value-threaded tail-recursive loop with demand-pruned trajectory of per-step extras; canonical iteration primitive at L4 (every iterative algorithm reduces to one or more folds). Inherits small-step semantics from the strawman §3.7.
- [`iterate-while-with-prev`](./iterate-while-with-prev.md) — carry-bootstrapped variant of `iterate-while` that threads a `PrevCarry` closure parameter for the previous-iteration recurrence variable. The driver for [`first-iteration-unrolling`](../concepts/first-iteration-unrolling.md)'s unrolled form; degenerates to `iterate-while` when `β = ()`.
- [`chebyshev`](./chebyshev.md) — typed-wrapper fixed-degree polynomial smoother; the `ChebOp` constructed-operator closure absorbs the polynomial-kind variant via distinct closure types; `apply` is a `Solve (ChebSim E)` action over a capability-typed `ChebSim`. Inner-product-free and convergence-test-free (the fixed-degree contrast to `krylov-step`): both bounded obstructions (outer `pc_it`, inner `k`) are `iterate_while_pure` folds with **step-count predicates** (the counter folded into the carry), reusing the firm [`iterate-while`](./iterate-while.md) family rather than a separate iteration vocabulary (cycle-014 combinator-miner route (i); re-anchored cycle-015). The fixed-count-vs-convergence distinction lives in the predicate, not the combinator.

**`solve-monad` outer-driver vocabulary (3)** — the outer-coordination surface the iterative-solve CAPs (`L4/ksp_solve`, `L4/eigsolve`; both forthcoming cycle-048) consume. These are the `Solve = StateT SimState Identity` outer drivers per [`solve-monad`](../concepts/solve-monad.md); they sit *above* the `iterate-while` family (the per-step kernel-fold), coordinating the restart / convergence structure the kernel folds inside:

- `solve_loop` — the outer driver `solve_loop :: OpParams -> Inputs -> Solve ()`; tail-recurses on `restart_cycle` (or `one_cycle` for non-restarted solvers) until the returned `Outcome` says stop. The entry point `solve op inp = execState (solve_loop op inp) initial_state` per [`solve-monad`](../concepts/solve-monad.md) §Shape. The `do`-with-`unless` exit makes interior termination visible at the coordination layer (the `restart_cycle`/`one_cycle` body owns the `SimState.it`-bounded inner loop).
- `restart_cycle` — the per-cycle driver `restart_cycle :: OpParams -> Inputs -> Solve Outcome`; builds a fresh ephemeral `Krylov` bundle (`fresh_krylov`), runs the inner `iterate-while`-family kernel fold (the inner `inner_loop` whose sole `SimState` effect is `modify (\s -> s { it = s.it + 1 })`), folds the basis correction into `SimState.x` via `modify` exactly once at the boundary, and classifies the returned bundle into an `Outcome`. The non-restarted-solver specialisation is `one_cycle` (a single cycle, no re-seed). Threads `Krylov` as a plain `let`-bound value (born at cycle entry, discarded at exit) — not a monadic effect — per the [`state-stratification`](../concepts/state-stratification.md) ephemeral-bundle discipline.
- `Outcome` — the termination sum `Outcome = Continue | Done Bool`: `Done True` (converged), `Done False` (exhausted `max_it`), `Continue` (hit `max_dim`, another restart warranted). Classified **once** at the cycle boundary against `(K.beta, K.j, SimState.it, ε)`, replacing the L3 form's scattered termination tests with a single decision site; `solve_loop` pattern-matches on it and the `Bool` inside `Done` folds uniformly into `SimState.converged`. This is the L4 lift of `ksp_solve`'s L3 soft-fail `Bool` to a sum type per [`L3/ksp_solve`](../L3/ksp_solve.md):160 (`result.converged : Bool` at L3 ↦ `Outcome` at L4) and [`solve-monad`](../concepts/solve-monad.md) §"Termination as a sum type". The `eigsolve` cap extends this to a richer partial-success sum (max-iter with `0 < converged < requested`) per [`L3/eigsolve`](../L3/eigsolve.md):166 — anchored here as the `Outcome`-sum *pattern*, specialised per-cap.

**Rough-in at L4 (0)** — none currently.
```

```edit:book/src/L4/index.md
[old]: **Queued at L4** — none currently. The three firm operators close the cohort needed for the cycle-008-frontier work; the `solve-monad`'s `restart_cycle` and `solve_loop` outer drivers are referenced from concept pages but not yet anchored as L4 rows (potential cycle-009+ harvester scope; not blocking forward-frontier work).
[new]: **Queued at L4** — the two iterative-solve CAP chapters. The `solve-monad` outer-driver vocabulary (`solve_loop` / `restart_cycle` / `Outcome`) is now **anchored** (Firm-at-L4 cohort above; rows in the dep-map) — discharging the prior deferral for the outer-driver surface. What remains queued are the per-operator CAP chapters that consume this vocabulary:

- `L4/ksp_solve` *(cap; cycle-048 R2)* — the L4 outer-driver cap over [`L3/ksp_solve`](../L3/ksp_solve.md); consumes `solve_loop` / `restart_cycle` / `Outcome` (the `Outcome` is the sum-typed lift of the L3 soft-fail `Bool` per [`L3/ksp_solve`](../L3/ksp_solve.md):160). Gated on this anchor (now satisfied); not yet authored.
- `L4/eigsolve` *(cap; cycle-048+ R3)* — the L4 outer-driver cap over the `partial-obstruction` [`L3/eigsolve`](../L3/eigsolve.md); consumes the same outer-driver surface with a *richer* `Outcome`-sum termination (the partial-success `0 < converged < requested` case has no `ksp_solve` analog, per [`L3/eigsolve`](../L3/eigsolve.md):166 and [`L1/eigsolve`](../L1/eigsolve.md):78). Gated on this anchor (now satisfied); not yet authored. NOTE the L3 `eigsolve` entry's "no firm L4 cap exists" assertions remain TRUE this cycle — anchoring the outer-driver *rows* does not land the cap chapter.
```

```edit:book/src/L4/index.md
[old]| [`chebyshev`](./chebyshev.md) | `setup :: LinOp[E] -> SetupParams -> Variant -> Solve s (ChebOp E S)`; `apply :: ChebOp E S -> Bool -> Solve (ChebSim E) ()`. Constructed-operator smoother; `ChebOp` `readonly` closure (variant absorbed via distinct closure types `S`); `ChebSim = { x: Read, y: ReadWrite }` capability-typed sim-state. | Concepts: `state-stratification`, `solve-monad`, `constructed-operators`, `variant-absorption`, `derived-view-hoisting`, `sequential-obstruction`, `first-iteration-unrolling`, `elementwise-product`. L4 rows: [`iterate-while`](./iterate-while.md) — both bounded loops are its `iterate_while_pure` sugar with a **step-count predicate** (`s.it <= op.pc_it` outer, `c.k <= op.order - 1` inner; counter folded into the carry), per strawman §6.5 step 5 (`l4_calculus.md:418`); the inner `st`/`rho_prev` recurrence-carry may alternatively use [`iterate-while-with-prev`](./iterate-while-with-prev.md) (entry adopts the plain carry-`st` form; see entry §Variant axes). | L3 [`chebyshev`](../L3/chebyshev.md) via typed-wrapper dissolution (substantive at wrapper; identity-in-form on body; no L4-L3 theme file — same shape as `krylov-step-typed-wrapper-dissolution`), then L2 [`chebyshev-iteration`](../L2/chebyshev-iteration.md). | `firm` (harvested cycle-013T143923Z; re-anchored cycle-015T202138Z lifter — the `forM_`/`foldM` obstructions re-expressed as `iterate_while_pure` folds with step-count predicates per cycle-014 combinator-miner route (i), reusing the firm `iterate-while` family; wrapper-vocabulary blocker closed) |
[new]| [`chebyshev`](./chebyshev.md) | `setup :: LinOp[E] -> SetupParams -> Variant -> Solve s (ChebOp E S)`; `apply :: ChebOp E S -> Bool -> Solve (ChebSim E) ()`. Constructed-operator smoother; `ChebOp` `readonly` closure (variant absorbed via distinct closure types `S`); `ChebSim = { x: Read, y: ReadWrite }` capability-typed sim-state. | Concepts: `state-stratification`, `solve-monad`, `constructed-operators`, `variant-absorption`, `derived-view-hoisting`, `sequential-obstruction`, `first-iteration-unrolling`, `elementwise-product`. L4 rows: [`iterate-while`](./iterate-while.md) — both bounded loops are its `iterate_while_pure` sugar with a **step-count predicate** (`s.it <= op.pc_it` outer, `c.k <= op.order - 1` inner; counter folded into the carry), per strawman §6.5 step 5 (`l4_calculus.md:418`); the inner `st`/`rho_prev` recurrence-carry may alternatively use [`iterate-while-with-prev`](./iterate-while-with-prev.md) (entry adopts the plain carry-`st` form; see entry §Variant axes). | L3 [`chebyshev`](../L3/chebyshev.md) via typed-wrapper dissolution (substantive at wrapper; identity-in-form on body; no L4-L3 theme file — same shape as `krylov-step-typed-wrapper-dissolution`), then L2 [`chebyshev-iteration`](../L2/chebyshev-iteration.md). | `firm` (harvested cycle-013T143923Z; re-anchored cycle-015T202138Z lifter — the `forM_`/`foldM` obstructions re-expressed as `iterate_while_pure` folds with step-count predicates per cycle-014 combinator-miner route (i), reusing the firm `iterate-while` family; wrapper-vocabulary blocker closed) |
| `solve_loop` | `solve_loop :: OpParams -> Inputs -> Solve ()`; entry `solve op inp = execState (solve_loop op inp) initial_state`. Outer driver: tail-recurses `do { o <- restart_cycle op inp; unless (done o) (solve_loop op inp) }` (`one_cycle` for non-restarted solvers) until the returned `Outcome` says stop. | Concepts: `solve-monad`, `state-stratification`, `convergence-test`. L4 rows: `restart_cycle` (the per-cycle body it recurses on); `Outcome` (its termination signal); sits *above* [`iterate-while`](./iterate-while.md) (the inner kernel-fold `restart_cycle` runs). | L3 explicit `iterate_while_L3` outer tail-recursion + positional `(K, s)` threading per [`L3/ksp_solve`](../L3/ksp_solve.md):142 (the `Solve` monad dissolves; `do`/`unless` ↦ the predicate's read of `s.converged`). L4>L3 theme rides the `L4/ksp_solve` cap (cycle-048). | `firm` (outer-driver vocabulary anchor; cycle-047 — specified from [`solve-monad`](../concepts/solve-monad.md) §Shape + strawman §§3.6–3.7; first-two consumers [`L3/ksp_solve`](../L3/ksp_solve.md), [`L3/eigsolve`](../L3/eigsolve.md); per-operator laws ride the forthcoming `L4/ksp_solve` cap) |
| `restart_cycle` | `restart_cycle :: OpParams -> Inputs -> Solve Outcome` (`one_cycle` non-restarted specialisation). Builds `fresh_krylov`, runs the inner `iterate-while`-family kernel fold (sole `SimState` effect `modify (\s -> s { it = s.it + 1 })`), folds the correction into `SimState.x` via one `modify`, classifies the returned bundle into an `Outcome` once at the boundary. `Krylov` threaded as a plain `let`-bound value (born at cycle entry, discarded at exit). | Concepts: `solve-monad`, `state-stratification` (ephemeral `Krylov` bundle vs persistent `SimState`), `sequential-obstruction` (dense recurrences stay pure inside the cycle), `constructed-operators`. L4 rows: [`iterate-while`](./iterate-while.md) / [`iterate-while-with-prev`](./iterate-while-with-prev.md) (the inner kernel-fold it runs); consumed by `solve_loop`; produces `Outcome`. | L3 the `restart`-shaped *outer* of the double-nested fold per [`L3/ksp_solve`](../L3/ksp_solve.md):94 (the **restart nesting**: GMRES/FGMRES re-seed-`K` outer loop wrapping the inner Arnoldi fold); the `modify`-once correction ↦ positional `fold_iterate` at restart boundaries per [`L3/ksp_solve`](../L3/ksp_solve.md):90. L4>L3 theme rides the `L4/ksp_solve` cap (cycle-048). | `firm` (outer-driver vocabulary anchor; cycle-047 — specified from [`solve-monad`](../concepts/solve-monad.md) §"Worked example — GMRES"; first-two consumers [`L3/ksp_solve`](../L3/ksp_solve.md), [`L3/eigsolve`](../L3/eigsolve.md); per-operator laws ride the forthcoming `L4/ksp_solve` cap) |
| `Outcome` | `Outcome = Continue \| Done Bool` (termination sum). `Done True` converged; `Done False` exhausted `max_it`; `Continue` hit `max_dim`, restart warranted. Classified once at the cycle boundary against `(K.beta, K.j, SimState.it, ε)`; the `Bool` inside `Done` folds uniformly into `SimState.converged`. The `eigsolve` cap extends the sum with a partial-success arm (`0 < converged < requested`). | Concepts: `solve-monad` §"Termination as a sum type", `convergence-test` (the predicate the classification reads). L4 rows: produced by `restart_cycle`, pattern-matched by `solve_loop`. | L3 the soft-fail `Bool` `result.converged` per [`L3/ksp_solve`](../L3/ksp_solve.md):160 (the L4 `Outcome` sum collapses to L3's single `Bool`; the multi-reason classification scatters into L3's per-test branches); richer partial-success sum (`0 < converged < requested` / `PartialConverged`) per [`L1/eigsolve`](../L1/eigsolve.md):78 + [`L3/eigsolve`](../L3/eigsolve.md):166. L4>L3 theme rides the iterative-solve caps (cycle-048). | `firm` (outer-driver vocabulary anchor; cycle-047 — specified from [`solve-monad`](../concepts/solve-monad.md) §"Termination as a sum type"; the `ksp_solve` `Bool`-lift consumer is [`L1/ksp_solve`](../L1/ksp_solve.md):91 + [`L3/ksp_solve`](../L3/ksp_solve.md):160, the partial-success extension [`L3/eigsolve`](../L3/eigsolve.md):166) |
```

## Supporting evidence

**Operators currently anchored at L4** (before this dispatch): `krylov-step`, `iterate-while`, `iterate-while-with-prev`, `chebyshev` — all firm; no rough-ins; the `solve-monad` outer-driver surface deferred at `:47`.

**The anchored vocabulary is firmly specifiable** from the concept page + strawman:
- `book/src/concepts/solve-monad.md:7-17` — the `Solve a = StateT SimState Identity a` definition, `solve op inp = execState (solve_loop op inp) initial_state` entry, and the `solve_loop` recursion on `restart_cycle` / `one_cycle`.
- `book/src/concepts/solve-monad.md:51-56` — the `restart_cycle` body (`fresh_krylov`, inner loop, `modify`-once correction); the `Krylov`-threaded-as-plain-value lifecycle (`:56`).
- `book/src/concepts/solve-monad.md:58-68` — §"Termination as a sum type": `Outcome = Continue | Done Bool` with the three classified paths and the single-decision-site rationale.
- `book/src/design/l4_calculus.md:88` — the `Sim`/`Solve`-monad state-threading conventions (`get`/`put`/`modify`; linear state). §3.7 (`:150-184`) — the `iterate_while` shape the outer drivers sit above.

**First-two-consumers evidence** (the two L3 driver consumers that reference the unanchored surface as their future L4 home):
- `book/src/L3/ksp_solve.md:142` — "[`solve-monad`] — the L4 outer-driver surface; at L3 dissolved to the explicit `iterate_while_L3` tail recursion + positional `(K, s)` threading."
- `book/src/L3/ksp_solve.md:160` — the `convergence-failure-policy` axis: "the L4 `solve-monad` lifts it to an `Outcome` sum per [`solve-monad`]" (the soft-fail `Bool` ↦ `Outcome` lift this anchor records).
- `book/src/L3/eigsolve.md:19,:34,:78,:166,:203,:214` — the `partial-obstruction` eigsolve repeatedly names the unauthored `solve-monad` L4 surface (with sum-typed termination richer than `ksp_solve`'s soft-fail) as its future L4 home.
- `book/src/L1/ksp_solve.md:91` + `book/src/L1/eigsolve.md:78` — the L1 anchors of the `Outcome`-sum / partial-success termination pattern.

**Maturity verdict (firm-as-vocabulary-anchor):** the rows carry signature + role + first-two-consumers + L3 lowering-direction — exactly the L4 dep-map row contract (Algebraic-laws are recorded inline in each operator's own page, not in the dep-map, per `book/src/L4/index.md:65`). The per-operator *law* depth (the monad-law / `execState`-fusion identities, the full `Outcome`-classification variant axis) is CAP-chapter content riding cycle-048. The rows are not `rough-in`: the vocabulary itself (signatures, the `Outcome` sum, the driver roles) is fully determined by the concept page — nothing about the *vocabulary* is speculative. What is "forthcoming" is the *cap operator pages*, which the rows correctly name as queued, not the vocabulary.

## Scope-boundary compliance

- Did NOT author `book/src/L4/ksp_solve.md` or `book/src/L4/eigsolve.md` — those are the cycle-048 R2/R3 caps. Named them as forthcoming in the rows + Queued prose only.
- FLOOR-LANDING-REANCHOR honored: did NOT touch the `L3/eigsolve.md` / `L3/ksp_solve.md` "no firm L4 cap exists / not yet authored" assertions — anchoring the outer-driver ROWS does not land a CAP chapter, so those assertions stay TRUE this cycle. The new rows + Queued prose note the caps are forthcoming (cycle-048) without re-anchoring the L3 assertions.
- One layer document touched (`book/src/L4/index.md`); no other layer intros modified; no concept page modified.
- No `book/` write performed in this dispatch phase — all changes are inside `## Proposed changes` `edit:` blocks for `integrator-per-report` to apply (Phase 5).

## Open questions

- **`l4-ksp-solve-eigsolve-caps-gated-on-solve-monad-outer-driver-vocabulary` (anchor half — ADVANCED toward closure).** The anchor half is now discharged: the `solve_loop` / `restart_cycle` / `Outcome` outer-driver vocabulary is anchored as firm L4 rows. The OQ remains OPEN for the **cap half**: `L4/ksp_solve.md` (R2) and `L4/eigsolve.md` (R3) are unblocked but not yet authored (cycle-048). Recommend the OQ be re-scoped to "caps unblocked; awaiting cycle-048 authoring" rather than closed.
- **Maturity straddle (layer-intro-author vs harvester authority) — flagged per dispatch instruction.** This anchor straddles concept-page/vocabulary authority (mine) and per-operator L4-row harvest depth (harvester's). I marked the rows `firm` *as vocabulary anchors* because the vocabulary is fully determined by `book/src/concepts/solve-monad.md` and the dep-map row contract excludes Algebraic-laws (they live in the op page). **However**, the cycle-048 cap authors will want genuine per-operator harvest depth: the monad-law identities (`execState`/`StateT` fusion, `solve_loop` tail-recursion-as-fold equivalence to `iterate_while`), the full `Outcome`-classification variant axis (the `ksp_solve` 3-arm vs `eigsolve` 4-arm partial-success split), and the demand-pruning interaction with the outer driver. **If the c048 cap authors find the rows under-specified for the cap body, a thin harvester pass on the outer-driver vocabulary (NOT a full new operator page — a row-depth firming) may be warranted before/with the cap.** The planner judged vocabulary-anchor sufficient to unblock; the deeper harvest can ride the cap. Flagging so the c048 planner can decide.
- **`Outcome`-sum as one row vs per-cap specialisation.** I anchored `Outcome = Continue | Done Bool` as the canonical *pattern* (the `ksp_solve` 3-path shape from the concept page) and noted the `eigsolve` cap extends it with a partial-success arm. An alternative is two distinct sum types (`KspOutcome` / `EigOutcome`) anchored separately. I chose the single-pattern-anchor because the concept page (`book/src/concepts/solve-monad.md:60`) defines exactly one `Outcome`, and the `eigsolve` richer-sum is described at L1 (`book/src/L1/eigsolve.md:78`) as an *extension* of the same shape, not a distinct vocabulary. If the c048 `eigsolve` cap finds the partial-success arm wants its own L4 row, that is a clean follow-up addition — not a contradiction with this anchor.
- **Touches `l4-native-combinator-denominator-completeness-survey`.** The L4 native-combinator denominator now includes the three outer-driver vocabulary anchors above the `iterate-while` family. The outer-driver surface is *coordination* vocabulary (it sits above the kernel-fold combinators), so whether it counts toward the "native combinator" denominator the survey tracks is a definitional question for that survey — flagging the addition so the survey can decide whether outer drivers are in-scope (they are monad-coordination, arguably distinct from the pure combinator denominator).

## Provenance

- Role spec: `.claude/agents/layer-intro-author.md` (layer-intro + concept-page authoring; dep-map + Vocabulary-cohort conventions; L4 strawman notation conventions).
- Dispatch: cycle-047 D5 — anchor the `solve-monad` outer-driver L4 vocabulary as the verified prerequisite for the cycle-048 `L4/ksp_solve.md` (R2) + `L4/eigsolve.md` (R3) caps.
- Inputs read in full before authoring: `book/src/L4/index.md` (esp. `:47` deferral + dep-map `:53-56`), `book/src/concepts/solve-monad.md`, `book/src/design/l4_calculus.md` §§3.6–3.8 + monad conventions, `book/src/L3/ksp_solve.md`, `book/src/L3/eigsolve.md`, plus the L1 anchors `book/src/L1/ksp_solve.md:91` + `book/src/L1/eigsolve.md:78` for the `Outcome`-sum / partial-success framing.
- Notation: Haskell `::` signatures inside the dep-map (per L4 strawman conventions); `Outcome = Continue | Done Bool` sum form per `book/src/concepts/solve-monad.md:60`. No new notation introduced.
