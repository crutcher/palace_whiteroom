---
agent: layer-intro-author
invoked_at: 2026-06-02T091509Z
scope: L1/index.md — add fe_assemble dep-map row (self-summing table) + record NO-ENTRY upward-propagation warrants
status: pending
inputs:
  - book/src/L1/index.md (the L1 Part overview — dep-map table + Vocabulary-cohort + Working Notes)
  - book/src/L1/fe_assemble.md (firm cycle-054 — the bilinear-form assembly fold; `## Status` = `firm`; signature :57-62)
  - book/src/L1/weak_form_term.md (firm cycle-061; `## Status` = `firm`)
  - book/src/L1/eliminate_essential_bc.md (firm cycle-055; `## Status` = `firm`)
  - book/src/L1/eliminate_rhs.md (firm cycle-055; `## Status` = `firm`)
  - book/src/L1/assemble_frequency_operator.md (firm cycle-062; `## Status` = `firm`)
  - reports/2026-06-02T091509Z-abstractor-fe-assemble-upward-warrant/CYCLE.md (D1 — fe_assemble → NO L2 ENTRY, anti-mirror warrant)
closes_oq: l1-index-fe-assemble-needs-dep-map-row-for-self-summing-table
integrated_at: 2026-06-02T101500Z
integration_commit: PLACEHOLDER_SHA
integration_notes: "cycle-063 D3 (batch-19 close-out, build-relevant). Applied 3 in-place edits to book/src/L1/index.md: (a) fe_assemble dep-map row added (fold-then-members, before eliminate_rhs); (b) §Vocabulary-cohort reconciliation note updated off-table->in-table self-summing (31 in-table, all firm rows on-table); (c) §Working-Notes cycle-063 bullet appended recording the 3 NO-ENTRY upward-propagation warrants (fe_assemble, weak_form_term, assemble_frequency_operator) routing their formal close to the batch-19 meta-phase. Count self-sum verified: in-table firm rows 30->31 with the fe_assemble row; 31 = grand total (27 main + 4 FE-assembly); grand total UNCHANGED (no double-count). NO status flip -> index-status-cell anti-drift guard NOT fired. RESOLVED-BY-LANDING-c063-D3 the cosmetic c062 OQ l1-index-fe-assemble-needs-dep-map-row-for-self-summing-table. No gate triggered. citecheck --scan 8 ok / 0 failing. Build: cargo make book exit 0; ./fe_assemble.md row link rendered in book/book/html/L1/index.html."
---

# CYCLE: L1 intro — `fe_assemble` dep-map row + FE-assembly/frequency-operator NO-ENTRY warrants

## Summary

Two surgical edits to `book/src/L1/index.md`, both record-only structural hygiene (no semantic content change to any operator):

- **(a)** Add the missing `fe_assemble` dep-map row. `fe_assemble` is firm L1 (`book/src/L1/fe_assemble.md`, `## Status` = `firm`) but was narrated only in the §Vocabulary-cohort FE-assembly subsection prose and had NO row in the dep-map table — so the in-table firm-row count (30) was one short of the firm grand total (31). Adding the row moves `fe_assemble` from off-table to in-table, making the in-table count **self-sum to 31 = grand total**. The grand total is unchanged (still 31). The §Vocabulary-cohort reconciliation note that said "30 in-table + 1 off-table `fe_assemble`" is updated to "31 in-table, all firm rows on-table."
- **(b)** Record the three self-evident NO-ENTRY upward-propagation warrants for the FE-assembly + frequency-operator cohort in §Working Notes, marking the cohort's upward descent complete-by-warrant and routing the formal close to the batch-19 meta-phase.

**Count discipline (count-owner read, per the count-owner guard):** I read each of the five relevant chapter `## Status` lines directly (NOT the index cells). All five are `firm`:
- `fe_assemble.md` → `` `firm`. **Clean-gate call: PROMOTE — clean.** ``
- `weak_form_term.md` → `` `firm`. **Clean-gate call: PROMOTE — clean (pulled, not speculative).** ``
- `eliminate_essential_bc.md` → `` `firm`. **Clean-gate call: PROMOTE — clean.** ``
- `eliminate_rhs.md` → `` `firm`. **Clean-gate call: PROMOTE — clean.** ``
- `assemble_frequency_operator.md` → `` `firm` — **firm-on-positive-structure** ``

In-table firm rows BEFORE this edit: 30 (32 named-link main+FE rows minus the 2 rough-in(test-coverage-bounded) rows `matrix-weighted-norm` + `bilinear-form`; the 6 rough-in(obstruction) MINRES/BiCGStab rows are not firm). AFTER adding `fe_assemble`: 31 = the firm grand total. Self-consistent.

## Proposed changes

### Edit 1 — add the `fe_assemble` dep-map row (place it with its FE-assembly siblings)

`fe_assemble` is the parent fold; its members `eliminate_rhs` / `eliminate_essential_bc` / `weak_form_term` are the last three rows of the table. The row is inserted immediately before `eliminate_rhs` so the FE-assembly cohort reads fold-then-members (`fe_assemble` ▷ `eliminate_rhs` ▷ `eliminate_essential_bc` ▷ `weak_form_term`). Dependencies column follows the `fe_assemble.md` signature (`:57-62`): the fold quantifies over `weak_form_term` opaquely (consumed-by, NOT a dep) and the per-term leaf `A(space, ·)` is libCEED-owned.

```edit:book/src/L1/index.md
[old]: | [`assemble_frequency_operator`](./assemble_frequency_operator.md) | `(fam: FrequencyOperatorFamily[N], ω: Scalar) → LinearOperator[N, N]` (i.e. `A(ω) = K + iω·C − ω²·M + A2(ω)`) | [`linear_combination`](../L2/linear_combination.md) (the fold this specializes — operator-operand corner of the operand-category axis, NOT a new fold); `apply_linop` (operands + result are opaque square operators) | `firm` (driven per-ω system-operator assembly; **operator-operand specialization of `linear_combination`** — replace-and-propagate, 2026-06-01 anti-mirror; L0: `palace/drivers/drivensolver.cpp:91-93,175,176-177,180` + `palace/models/spaceoperator.cpp:521-528` + `palace/linalg/rap.cpp:764-787`; L1>L0: [`assemble-frequency-operator-rotation`](../L1-L0/assemble-frequency-operator-rotation.md); harvested cycle-062; firm-on-positive-structure, no-dedicated-test caveat non-gating; **affine modulo A2** (A2 is an ω-dependent operand carrying coeff 1, not an ω-dependent coefficient); **single-pipeline-by-design** (driven only — transient bakes excitation into the captured op, electro/magnetostatic capture a single fixed K, eigenmode opaque — no 2nd-pipeline discharge owed); sharpens the `map_solve` scope boundary (`book/src/L4/solve_family.md`)) |
| [`eliminate_rhs`](./eliminate_rhs.md) | `(K: LinearOperator[N, N], x_bc: Tensor[N], b: Tensor[N], policy: DiagonalPolicy) → Tensor[N]` (i.e. `b − K·x_bc`, essential rows pinned per policy) | `apply_linop` (direct, `K·x_bc`), `axpy` (direct, `b − K·x_bc`); `set_subvector` (concept, essential-dof gather/pin — shared with `divfree-projector`, NOT a spine dep) | `firm` (FE essential-BC RHS-lift; separable post-composition on the assembled operator, NOT part of the `fe_assemble` fold; L0: `palace/linalg/rap.cpp:56-82` + witness `palace/models/laplaceoperator.cpp:225-252`; harvested cycle-055; firm-on-positive-structure, no-dedicated-test caveat non-gating per `fe_assemble`/`apply_linop` precedent; diagonal-policy + bc-data-homogeneity variant axes; interior-block-only linearity, not-idempotent non-laws) |
[new]: | [`assemble_frequency_operator`](./assemble_frequency_operator.md) | `(fam: FrequencyOperatorFamily[N], ω: Scalar) → LinearOperator[N, N]` (i.e. `A(ω) = K + iω·C − ω²·M + A2(ω)`) | [`linear_combination`](../L2/linear_combination.md) (the fold this specializes — operator-operand corner of the operand-category axis, NOT a new fold); `apply_linop` (operands + result are opaque square operators) | `firm` (driven per-ω system-operator assembly; **operator-operand specialization of `linear_combination`** — replace-and-propagate, 2026-06-01 anti-mirror; L0: `palace/drivers/drivensolver.cpp:91-93,175,176-177,180` + `palace/models/spaceoperator.cpp:521-528` + `palace/linalg/rap.cpp:764-787`; L1>L0: [`assemble-frequency-operator-rotation`](../L1-L0/assemble-frequency-operator-rotation.md); harvested cycle-062; firm-on-positive-structure, no-dedicated-test caveat non-gating; **affine modulo A2** (A2 is an ω-dependent operand carrying coeff 1, not an ω-dependent coefficient); **single-pipeline-by-design** (driven only — transient bakes excitation into the captured op, electro/magnetostatic capture a single fixed K, eigenmode opaque — no 2nd-pipeline discharge owed); sharpens the `map_solve` scope boundary (`book/src/L4/solve_family.md`)) |
| [`fe_assemble`](./fe_assemble.md) | `(space: FiniteElementSpace[N], terms: [WeakFormTerm]) → LinearOperator[N, N]` (i.e. `Σ_{t ∈ terms} A(space, t)` — the bilinear-form assembly fold) | (the term-axis fold quantifies over [`weak_form_term`](./weak_form_term.md) opaquely — consumed-by, NOT a dependency; the per-term assembly leaf `A(space, ·)` is libCEED-owned ([`fe-assemble-libceed-boundary-obstruction`](../L1-L0/fe-assemble-libceed-boundary-obstruction.md), `opaque-library-ownership`), a strict sub-term below the fold's leaf, NOT a spine dep) | `firm` (FE-assembly sub-spine fold; the `BilinearForm`-class assembler `K = Σ_i A(term_i)`, NOT the BLAS-2 [`bilinear-form`](./bilinear-form.md) reduction `xᴴ M y`; L0: `palace/fem/bilinearform.cpp:61-107` fold core (`AddSubOperator` accumulate `:77`/`:97`, `Finalize` `:104`); harvested cycle-054; clean-gate PROMOTE; firm-on-positive-structure, no-dedicated-test caveat non-gating; laws: empty-term identity, concatenation-homomorphism, single-term reduction, term-position commutativity; the BC-treatment post-compositions `eliminate_essential_bc`/`eliminate_rhs` compose AFTER this fold; **NO L2 entry by warrant — see Working Notes**) |
| [`eliminate_rhs`](./eliminate_rhs.md) | `(K: LinearOperator[N, N], x_bc: Tensor[N], b: Tensor[N], policy: DiagonalPolicy) → Tensor[N]` (i.e. `b − K·x_bc`, essential rows pinned per policy) | `apply_linop` (direct, `K·x_bc`), `axpy` (direct, `b − K·x_bc`); `set_subvector` (concept, essential-dof gather/pin — shared with `divfree-projector`, NOT a spine dep) | `firm` (FE essential-BC RHS-lift; separable post-composition on the assembled operator, NOT part of the `fe_assemble` fold; L0: `palace/linalg/rap.cpp:56-82` + witness `palace/models/laplaceoperator.cpp:225-252`; harvested cycle-055; firm-on-positive-structure, no-dedicated-test caveat non-gating per `fe_assemble`/`apply_linop` precedent; diagonal-policy + bc-data-homogeneity variant axes; interior-block-only linearity, not-idempotent non-laws) |
```

### Edit 2 — update the §Vocabulary-cohort reconciliation note (off-table → in-table; count self-sums)

```edit:book/src/L1/index.md
[old]: Count discipline: the grand total is computed by reading each linked chapter's `## Status` line, not the index cells — 27 main + 4 FE-assembly = 31; equivalently the dep-map table holds 30 `firm` rows (incl. `assemble_frequency_operator`, c062) plus the off-table `fe_assemble` (firm, narrated in the FE-assembly subsection, no dep-map row) = 31.
[new]: Count discipline: the grand total is computed by reading each linked chapter's `## Status` line, not the index cells — 27 main + 4 FE-assembly = 31; equivalently the dep-map table now holds **31** `firm` rows (incl. `assemble_frequency_operator` c062 and `fe_assemble` c054 — the latter's dep-map row was added cycle-063 D3, moving it from off-table to on-table so the in-table firm-row count self-sums to the grand total). All firm rows are now on-table; there is no off-table firm operator.
```

### Edit 3 — record the three NO-ENTRY upward-propagation warrants in §Working Notes

Appended as a single compact bullet at the end of §Working Notes (after the cycle-022 `eigsolve`-promotion bullet, line 135).

```edit:book/src/L1/index.md
[old]: - **Cycle-022 (`eigsolve` promotion `rough-in (test-coverage-bounded)`→`firm`)**: the cycle-009 "test-coverage-bounded" qualifier above is **retired** as over-stated, via a route-(b) law-confidence re-eval (the laws are syntactic identities on positive source, so the absence of a dedicated `test-eigensolver.cpp` does not gate firm — the `apply_nonlinear_pencil`/`chebyshev-smoother` no-dedicated-test precedent). The cycle-009 narrative bullet above is kept as history; this bullet records that the qualifier is now retired. Full per-law anchoring is carried by the §Vocabulary-cohort `eigsolve` bullet and the dep-map row above (and `book/src/L1/eigsolve.md:165-171` §Status); not re-stated here.
[new]: - **Cycle-022 (`eigsolve` promotion `rough-in (test-coverage-bounded)`→`firm`)**: the cycle-009 "test-coverage-bounded" qualifier above is **retired** as over-stated, via a route-(b) law-confidence re-eval (the laws are syntactic identities on positive source, so the absence of a dedicated `test-eigensolver.cpp` does not gate firm — the `apply_nonlinear_pencil`/`chebyshev-smoother` no-dedicated-test precedent). The cycle-009 narrative bullet above is kept as history; this bullet records that the qualifier is now retired. Full per-law anchoring is carried by the §Vocabulary-cohort `eigsolve` bullet and the dep-map row above (and `book/src/L1/eigsolve.md:165-171` §Status); not re-stated here.
- **Cycle-063 — FE-assembly + frequency-operator cohort: upward descent complete-by-warrant (three NO-ENTRY warrants; routed to the batch-19 meta-phase for formal close).** Three firm L1 operators decline an L2 entry by self-evident anti-mirror warrant (per the 2026-06-01 vocabulary-shift redirect §1d identity-in-named-terms smell — a degenerate mirror is NOT a layer). These route to the batch-19 meta-phase, which adds them to the planner's STOP-PROPOSING negative list:
  - **`fe_assemble` → NO L2 ENTRY** (cycle-063 D1 abstractor warrant, `reports/2026-06-02T091509Z-abstractor-fe-assemble-upward-warrant/CYCLE.md`). Strongest NO-ENTRY: fails the L2-floor warrant on BOTH anti-mirror axes — (1) the fold is a no-carry concatenation homomorphism `Σ_i A(space, term_i)` (the `solve_family` axis: order-commutative `AddSubOperator` accumulation, `palace/fem/bilinearform.cpp:77`/`:97`, no sequential carry → no fusion/iteration content, and so no L3 obstruction either), and (2) the per-term leaf `A(space, ·)` is opaque-library-owned (the `fold_solve`/`weak_form_term` axis: libCEED `CeedOperatorFullAssemble`, no L2 base-primitive composition to unfold). The abstract term-axis fold is ALREADY shared L2 vocabulary ([`linear_combination`](../L2/linear_combination.md) is its `Tensor[N]`-codomain sibling; `fe_assemble` is the `LinearOperator[N,N]`-codomain analog of the same `foldl (+) zero (map leaf terms)` skeleton). No GENUINE-FORM ≥2-pipeline combinator emerges that is not a renamed L1 fold. No batch-20 candidate.
  - **`weak_form_term` → NO L2 ENTRY** (self-evident, the same Axis-2 reasoning as the c060 D2 `fold_solve` no-floor-warrant). An inert `(coefficient, differential-operator)` data leaf that the `fe_assemble` fold quantifies over opaquely at every layer; its per-term realization `A` is the libCEED-owned opaque kernel below the fold. FE vocabulary that lives at L1 as the fold's element type, NOT an L2 composition — no fusion content to shift.
  - **`assemble_frequency_operator` → NO OWN L2/L3 ENTRY** (self-evident from c062: already propagated THROUGH [`linear_combination`](../L2/linear_combination.md)'s `operand-category` variant axis as the operator-operand corner, replace-and-propagate not mine-and-strand). It is not a new fold and owes no own floor — the fold generality lives in `linear_combination`; this operator is its driven-pipeline-only (single-pipeline-by-design) operator-operand specialization. The per-ω rebuild is an affine-operator-family evaluation, not a new L2/L3 form.
```

## Supporting evidence

- **Firm grand-total = 31, all on-table after this edit.** Count-owner read of each chapter `## Status` line (NOT index cells): `fe_assemble.md`, `weak_form_term.md`, `eliminate_essential_bc.md`, `eliminate_rhs.md`, `assemble_frequency_operator.md` are all `firm`. In-table firm rows go 30 → 31 with the `fe_assemble` row added; 31 = the §Vocabulary-cohort firm grand total (27 main-cohort + 4 FE-assembly sub-spine).
- **`fe_assemble` row content** matches `book/src/L1/fe_assemble.md`: signature `:57-62` (`fe_assemble :: (space, terms) -> LinearOperator[N, N]`, `= Σ_{t ∈ terms} A(space, t)`); `## Status` = `firm` (clean-gate PROMOTE, cycle-054); the four laws (empty-term identity, concatenation-homomorphism, single-term reduction, term-position commutativity); slug-collision disambiguation from the BLAS-2 `bilinear-form`; the libCEED-owned per-term leaf cross-link to `fe-assemble-libceed-boundary-obstruction` (`opaque-library-ownership`). Row placement: fold-then-members within the FE-assembly cohort at table-end.
- **NO-ENTRY warrants** sourced from `reports/2026-06-02T091509Z-abstractor-fe-assemble-upward-warrant/CYCLE.md` (D1, the `fe_assemble` two-axis warrant + the `weak_form_term` consistency note §Open-questions) and the c062 `assemble_frequency_operator` propagate-through-`linear_combination` finding (already recorded in the §Vocabulary-cohort `assemble_frequency_operator` bullet + dep-map row). The disposition (route to batch-19 meta-phase, STOP-PROPOSING list) is the D1 §Route disposition.

## Open questions / caveats

- **`fe_assemble`'s per-term leaf `A` libCEED classification is itself a standing OQ** carried by the firm L1 entry (`book/src/L1/fe_assemble.md` §Open questions — transitive-firm leaf vs. opaque-library-ownership vs. future tensor-contraction respine). The NO-ENTRY warrant is rendered for the CURRENT feature set (opaque consumption via `integ->Assemble` → libCEED); if a future cycle establishes that `A` admits a genuine L2 tensor-contraction respine, the warrant reopens. This does not weaken the present NO-ENTRY verdict; it names the single revisit condition (carried verbatim in D1 §Open-questions).
- **Formal close is the meta-phase's, not this dispatch's.** This edit RECORDS the cohort's upward-descent-complete status in §Working Notes and routes the formal close + STOP-PROPOSING-list additions to the batch-19 meta-phase (which fires after this cycle's finalize). No `book/src/L2/` chapter is created or referenced (NO-ENTRY = no chapter to register); no SUMMARY.md touch.
- **Scope discipline:** only `book/src/L1/index.md` is touched (the dep-map table + §Vocabulary-cohort reconciliation note + §Working Notes), one Part overview per the one-layer-per-invocation rule. No operator-entry, theme, or other-layer content modified.
