---
verifies: ../CYCLE.md
critiqued_at: 2026-05-30T16:15:00Z
critic_version: 1
checks:
  citation-validity: pass
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: warning
  cross-reference-integrity: warning
  edge-label-fidelity: pass
  plan-kind-consistency: pass
  skill-uptake-survey: pass
repaired_at: 2026-05-30T16:45:00Z
repairer_version: 1
repairs:
  citation-validity: not-needed
  surface-or-evidence: not-needed
  rotation-quality: not-needed
  variant-axis-coverage: repaired
  cross-reference-integrity: repaired
  edge-label-fidelity: not-needed
  plan-kind-consistency: not-needed
  skill-uptake-survey: not-needed
overall_status: ready
follow_up_agent: null
---

# META: verification of harvester report `elementwise_product` L1 firm

## Critique

### Checks run

**citation-validity — pass.** Mechanical `python3 tools/citecheck/citecheck.py --scan` reports **39 ok, 0 failing (39 citations checked)** across the whole CYCLE.md — bounds + path hygiene clean. Spot-anchor verification confirmed all six load-bearing pinpoints land on the cited content: `operator.cpp:478-487` (real `Mult` lambda) anchors `Y[i] = D[i] * X[i]` at `:486`; `operator.cpp:489-507` (complex straight `Mult`) anchors the cross-term subtraction at `:504`; `operator.cpp:545-568` (`MultHermitianTranspose`) anchors the cross-term addition at `:564`; `operator.hpp:256-289` anchors `class BaseDiagonalOperator` at `:257`; `jacobi.cpp:30-39` anchors the real-`Apply` lambda at `:38`; `jacobi.cpp:41-69` anchors the complex straight branch at `:57`. The two emit-time drift corrections recorded in the report's §Supporting-evidence are both verified: `operator.hpp:279` exists in-tree as `void MultTranspose(const VecType &x, VecType &y) const override { Mult(x, y); }` (anchor `MultTranspose` hits `[279, 283]` within the broader `:256-289` range); the five formerly-AMBIG bare basenames are now full `palace/linalg/operator.cpp`/`operator.hpp` / `jacobi.cpp` / `chebyshev.cpp` paths that resolve unambiguously to `reference/palace/palace/linalg/`. Cross-chapter forward-reference citations all confirm: `jacobi-smoother.md:289-297` anchors `elementwise_product` at `:293`; `assemble-diagonal.md:73` anchors both `reciprocal` and `elementwise_product`; `L2/chebyshev-iteration.md:189` anchors `elementwise-product`. Source re-read confirms the §Semantics complex-multiplication six-FMA claim (lines `:504-505` six-fused-multiply-add) and the conjugate sign-flip claim (lines `:564-565` three sign flips on cross-terms — `+ DI*XI`, `−DI*XR`, retained `+ DR*XI`) match the source verbatim. No `verified_against:` block is emitted by this report, so the YAML round-trip sub-check does not apply.

**surface-or-evidence — pass.** This is a **new-chapter** (`new:book/src/L1/elementwise_product.md`) proposal — not a refinement of an existing operator — so the refinement-shape rule "modifies surface AND has rotation_claim" does not bind. The proposed changes are a whole firm L1 chapter (the `new:` block at lines 27-156), two `edit:` blocks against `L1/index.md` (vocabulary-cohort bullet + dep-map row), and one `edit:` block against `SUMMARY.md` (chapter wiring). All four are surface mutations with direct positive-source evidence attached at each load-bearing claim.

**rotation-quality — pass.** The L1 lift from L0 is a genuine compaction along three axes: (i) destination-buffer mutation drops out (the L0 method form writes through `y`; the L1 signature returns a fresh `result`); (ii) operator-class wrapping drops out (the L0 site lives inside `BaseDiagonalOperator::Mult` against the wrapped `d` member; the L1 signature is a free binary operator on two equally-shaped tensors); (iii) per-template-specialisation/per-method-virtual surface (real `Mult` / complex `Mult` / complex `MultHermitianTranspose` plus the duplicate `jacobi.cpp` `Apply` template) collapses into one operator with two named variant axes. This is strictly-more-abstract / strictly-more-equational than the L0 form. Not a renaming. The `firm-on-positive-structure` justification (every law is a syntactic identity on positive source) is well-precedented and applied correctly: each of the 10 laws cites either a positive multiply-lambda line (laws 1, 5, 9 from `operator.cpp:486` / `:504-505`), an algebraic-field property (laws 2-4, 6-8), or the conjugate-variant sign-flip cross-term identity (law 10 from `:564-565`). Law 9 — the diagonal-operator-action identity `apply_linop(DiagonalOperator(d), x) = elementwise_product(d, x)` — is sound: source verification confirms `BaseDiagonalOperator<OperType>` inherits from `OperType` (real `Operator` / complex `ComplexOperator`) at `operator.hpp:257-289` so it IS a `LinearOperator` in `apply_linop`'s domain (`apply_linop.md:14-15` accepts any `LinearOperator[M, N]`); the law equates two distinct L1 operators by stating the canonical `BaseDiagonalOperator::Mult` body (`Y[i] = D[i] * X[i]` at `:486`) IS the elementwise product against the wrapped vector. Sound cross-operator identity.

**variant-axis-coverage — warning.** Element-type (real | complex) × conjugation (straight | conjugate-first-operand, complex-only) is the **named** axis structure, and both coverage and absorption are exhaustive: real `Mult` (`:478-487`), complex straight `Mult` (`:489-507`), complex conjugate `MultHermitianTranspose` (`:545-568`), with the real `MultTranspose` recorded as identity-aliased to `Mult` (`operator.hpp:279` verified). The non-axes section explicitly scopes out constant-folding (no L0 branches) and operator-action-vs-free-binary (the L1 abstraction unification). No hidden branches. **However**, the report's §Variant-axes precedent claim — "This matches the variant-axis treatment of `dot` (whose `tdot` / `udot` unconjugated variant is similarly one axis on the complex element-type)" — does not match the on-disk `dot.md` model. `dot.md:16-20` defines **two operators** (`dot` and `tdot`) in one chapter explicitly because "they share the entire reduction skeleton (sum over `N`) and differ only by the per-element kernel"; `dot.md:94` reiterates "At L1 these are **distinct operators** (sharing only the reduction skeleton), because the algebraic laws differ — `dot` is positive semi-definite at `y = x`, `tdot` is not." The actual `dot` precedent is "two co-housed operators with shared variant-axis section", NOT "one operator with a conjugation sub-axis" as the report's §Variant-axes and §OQ-2(b) both assert. The `elementwise_product` chapter operationally models the conjugate variant as the same operator with a sub-axis on the complex element-type (signature block at lines 53-56 reads `elementwise_product_conj` as "the same operator with a conjugation variant axis"), which is a *different* convention than `dot`. The OQ-2 justification (a) shared sign-flip kernel + (b) `dot` precedent + (c) law-catalog absorption — point (b) is misaligned with the on-disk `dot.md`. Points (a) and (c) stand independently. Sub-axis modeling is defensible for `elementwise_product` (the eight non-conjugation-sensitive laws ARE identical, the variant absorbs cleanly), but the precedent claim itself is incorrect and should be either dropped or re-grounded against a different precedent. Flagged as warning rather than fail because the variant-axis MODEL chosen is internally consistent and the coverage is exhaustive; only the precedent citation is wrong.

**cross-reference-integrity — warning.** Build-readiness fence-parity guard: `grep -n '\`\`\`'` reports 8 fence positions (lines 27, 156, 158, 165, 167, 174, 176, 183) — even parity, four open/close pairs. The firm-body-inside-fence check passes: section enumeration inside the `new:book/src/L1/elementwise_product.md` block (lines 27→156) confirms all 8 firm-apparatus sections (`## Context`, `## Signature`, `## Semantics`, `## Algebraic laws`, `## Dependencies`, `## Variant axes`, `## Status`, `## Evidence`) are enclosed within the fence. On-disk presence check: all 10 cross-referenced files exist (`concepts/elementwise-product.md`, `L1/apply_linop.md`, `L1/assemble-diagonal.md`, `L1/axpby.md`, `L1/axpbypcz.md`, `L1/axpy.md`, `L1/chebyshev-smoother.md`, `L1/jacobi-smoother.md`, `L1/scal.md`, `L2/chebyshev-iteration.md`). The `L1/reciprocal.md` link does NOT exist on disk (correctly flagged by OQ-3 — D2 is authoring it this cycle). The OLD-text anchors in both `edit:book/src/L1/index.md` blocks match the on-disk content verbatim (the full `jacobi-smoother` vocabulary bullet at `L1/index.md:55` and the dep-map row at `:99`); SUMMARY.md OLD-text matches the on-disk `:88` line. **Issues**:

1. **D2/D3 co-edit anchor collision on `L1/index.md` and `SUMMARY.md`.** Sibling report D2 (`reports/2026-05-30T153000Z-harvester-reciprocal-l1/CYCLE.md`) also inserts a vocabulary-cohort bullet, a dep-map row, and a SUMMARY chapter entry — ALL three target the SAME anchor (the `jacobi-smoother` bullet/row at `L1/index.md:55`, `:99`, `SUMMARY.md:88`). D2 uses a *prose-instruction* form (no `<<<OLD ... === NEW ... >>>` blocks) — the integrator must interpret prose. D3 (this report) uses the surgical-diff form with verbatim OLD-text. On serial integrator-per-report apply, whichever runs first will succeed; the second will find its OLD-text anchor (the `jacobi-smoother` line) still present (both inserts go *after* that line) and likely land cleanly, but the resulting ordering of `reciprocal` vs `elementwise_product` inserts depends on apply order. The report's OQ-3 only flagged the missing-`reciprocal.md` live-link problem, not the apply-anchor positioning. Mild collision: not blocking, but should be acknowledged in repair notes.

2. **`L1/index.md` "Firm (23)" count drift on combined apply.** The on-disk header at `L1/index.md:31` reads `**Firm (23)**`. D2's edit-block bumps the count to "Firm (24)" (per its (a) instruction). D3's edit-block (lines 158-165 of the report) does NOT touch the "Firm (23)" header at all — it only mutates the `jacobi-smoother` bullet and appends a dep-map row. If both D2 and D3 apply in the same cycle, the resulting count would read "Firm (24)" even though the actual firm-bullet count would be 25 (the prior 23 + reciprocal + elementwise_product). Off-by-one drift on the cohort-count header. This is a known sibling-co-edit pattern; the integrator's per-report serial apply or finalize-time hand-correction can resolve it.

3. **OQ-3 mitigation path b — `reciprocal` stub creation precedent ambiguity.** OQ-3 lists "(b) create a `stub` for `reciprocal` per the **Integration may materialize implied components as stubs** invariant" as a fallback. The invariant text in `CLAUDE.md` (§Methodology-invariants "Integration may materialize implied components as stubs") explicitly authorises the integrator to materialize stubs for clearly-implied references; both D2 and D3 are converging on `reciprocal`, so the stub-creation precondition (≥2 converging references) is met. But the cleaner path is mitigation-(a) "apply both in same cycle" — flagging here for completeness since OQ-3 doesn't rank them.

**edge-label-fidelity — pass.** Report is a single L1 operator add. No edge label is asserted on a lowering theme (the prose names the L1>L0 mutation-rotation theme as "forthcoming, plain-text forward-reference"); the only edge mentioned is `elementwise-product-mutation-rotation`, correctly stated as L1>L0 in §Status. The L1 vs L0 distinction section explicitly contrasts L0 method-form vs L1 free-binary-operator and the §Status forthcoming-theme note describes the L1>L0 rewrite direction correctly (destination-binding `y = a ⊙ b` at L1 → `forall_switch` per-element kernel writing `Y[i] = A[i] * B[i]` at L0).

**plan-kind-consistency — pass.** Declared kind is a firm L1 operator (per dispatch plan D3 "cycle-033 plan D3 dispatch (lower-shared-vocabulary)"). Content shape matches: §Status: firm with the firm-on-positive-structure justification; full apparatus (Signature, Semantics, 10 Algebraic laws + 5 non-laws, Dependencies as leaf, Variant axes with element-type × conjugation, L1 vs L0 distinction, Evidence with 16 citations). Not a rough-in / partly-constructive / obstruction. Promotion criteria for the firm-on-positive-structure precedent (every law a syntactic identity on positive source) are checked law-by-law and the precedent set is correctly cited (`apply_linop`, `lu_solve`, `back_solve`, `ls_update_column`, `jacobi-smoother`).

**skill-uptake-survey — pass (telemetry: low).** The report explicitly references `tools/citecheck/citecheck.py --anchor` (the cycle-024 mechanical realization of `verify-citation-range`) in §Operator-content and §Supporting-evidence — the cite-anchor sub-skill is invoked. Other shape-relevant skills (`classify-variant-axis`, `verify-refinement-surface`, `find-tests-for-region`) are not explicitly named in the report. The report's no-dedicated-test claim is asserted at §Status and §Evidence final bullet (line 155) and includes the disclaimer that "search confirms" the absence in `reference/palace/test/unit/` — implicit `find-tests-for-region` invocation, no skill name cited. Telemetry-only signal; no blocking concern.

### Issues found

1. **§Variant-axes / §OQ-2(b) — incorrect `dot`/`tdot` precedent citation** (line 116, line 239). The report claims "This matches the variant-axis treatment of `dot` (whose `tdot` / `udot` unconjugated variant is similarly one axis on the complex element-type)" and at OQ-2(b) "matches the existing `dot` / `tdot` (`udot`) convention (one operator, one conjugation axis)". On-disk `book/src/L1/dot.md:16-20` defines `dot` and `tdot` as **two operators in one chapter**; `dot.md:94` explicitly says "At L1 these are distinct operators ... because the algebraic laws differ — `dot` is positive semi-definite at `y = x`, `tdot` is not." The `dot.md` model is not "one operator with sub-axis" but "two co-housed operators sharing a variant-axis section". The report's modeling decision (one operator with conjugation sub-axis for `elementwise_product`) is defensible on its own merits (the eight non-conjugation laws are genuinely identical, unlike the `dot` vs `tdot` law divergence), but the precedent citation is wrong. **Severity: warning** (substantive: a precedent-citation defect that affects the variant-axis decision rationale, but the chosen model itself is internally consistent). **Where:** `CYCLE.md:116` (last sentence of §Variant-axes element-type bullet's continuation) and `CYCLE.md:239` (OQ-2 second-half justification point b).

2. **`L1/index.md` "Firm (count)" header off-by-one on D2+D3 combined apply** (§Proposed-changes line 158-165). D2's edit bumps "Firm (23)" → "Firm (24)" (adding reciprocal). D3's edit does NOT touch the "Firm (23)" header. If both reports apply in the same cycle, the on-disk header will read "Firm (24)" but the on-disk firm-bullet count will be 25 (23 + reciprocal + elementwise_product). Off-by-one drift on the cohort summary count. **Severity: warning** (mechanical; the integrator or repairer can update the count to "Firm (25)" at finalize time). **Where:** `CYCLE.md:158-165` (the D3 `edit:book/src/L1/index.md` block omits a count-header update).

3. **D2 / D3 co-edit anchor collision on `L1/index.md` + `SUMMARY.md`** (OQ-3, §Proposed-changes). D2 (reciprocal) and D3 (elementwise_product) both target the SAME `jacobi-smoother` anchor in `L1/index.md` (§Vocabulary-cohort bullet at `:55`, dep-map row at `:99`) and in `SUMMARY.md` (line `:88`). D2's edits are in prose-instruction form (no `<<<OLD ... === NEW ... >>>` markers); D3's are surgical-diff form. Serial integrator-per-report apply may produce non-deterministic ordering (which sibling lands directly after `jacobi-smoother`). OQ-3 flagged only the `reciprocal.md` missing-link aspect of co-application, not the anchor-positioning. **Severity: warning** (mechanical; both inserts go *after* the same anchor and remain compatible, but the resulting D2-then-D3 vs D3-then-D2 ordering is implementation-defined). **Where:** `CYCLE.md:158-165` and `CYCLE.md:176-183`.

4. **§OQ-3 mitigation list does not rank options or commit to a preferred path** (line 241). OQ-3 names three mitigations: (apply both same cycle, downgrade-to-plain-text, or stub-creation). The CLAUDE.md "Integration may materialize implied components as stubs" invariant explicitly names **stub-creation as the preferred path** when ≥2 converging forward-references exist; D2 and D3 BOTH forward-reference `reciprocal` (≥2 converging refs), and D2 IS authoring it this cycle. The report's mitigation list does not surface this preference ordering. **Severity: low / informational** (OQ guidance for the integrator's decision tree; not blocking). **Where:** `CYCLE.md:241`.

5. **§Status forthcoming L1>L0 theme note vs OQ-1 — minor redundancy** (lines 130 vs 237). The §Status block already names `elementwise-product-mutation-rotation` as the forthcoming L1>L0 theme with the rewrite-shape sketch; OQ-1 reiterates the same content plus the recommendation to cycle-planner. Redundant but not incorrect. **Severity: low / informational** (housekeeping). **Where:** `CYCLE.md:130` and `CYCLE.md:237`.

6. **§Variant-axes "dead-code conjugate path" claim about `jacobi.cpp:61-69`** (line 122). The report claims `palace/linalg/jacobi.cpp:61-69` (the `Transpose = true` complex `Apply` template instantiation) is "unreferenced in Palace because `JacobiSmoother::MultTranspose` aliases to `MultHermitianTranspose` which aliases through to `Mult` for the real-`dinv` smoother." Source check confirms the `Apply` template definition exists at `jacobi.cpp:41-70` with the `if constexpr (!Transpose)` straight branch at `:52-60` and the `else` transpose branch at `:61-69`. The dead-code claim depends on the call-graph from `JacobiSmoother::MultTranspose` → `MultHermitianTranspose` → `Mult`; this aliasing chain is asserted but not cited at the specific line that establishes the alias (the `jacobi.cpp:103` line is `Apply(dinv, x, y);` with no Transpose template-arg, so the call is `Apply<false>`). The cycle-032 firm `jacobi-smoother` chapter at `jacobi-smoother.md` already carries this dead-code caveat (verified live at `L1/index.md:55` end). The claim is cross-witnessed against the cycle-032 entry rather than re-derived from a citation in this report. **Severity: low / informational** (the dead-code claim's substance is sound and cross-witnessed; only the citation could be tighter — e.g. citing `jacobi.cpp:99-104` to show `Mult` calls `Apply(dinv, x, y)` with no Transpose). **Where:** `CYCLE.md:122`.

## Repair

### Fixes attempted

- **Finding 1** — §Variant-axes / §OQ-2(b) incorrect `dot`/`tdot` precedent citation.
  - **Decision**: repaired.
  - **Action**: rewrote `CYCLE.md:116` (§Variant-axes element-type bullet's continuation, "Variant-axis decision rationale" sentence) to justify the single-primitive-with-conjugation-sub-axis decision on its own terms — anchored on the eight non-conjugation-sensitive laws being identical between straight and conjugate variants (commutativity-of-straight-form, associativity, all-ones identity, all-zeros absorption, distributivity, scalar absorption, broadcast subsumption of `scal`, negation), with the conjugation-specific facts (law 10 involution, broken commutativity non-law) absorbing cleanly. Replaced the wrong precedent claim with a parenthetical note explicitly contrasting against on-disk `book/src/L1/dot.md:16-20,:94` (verified on-disk: `dot` and `tdot` are TWO distinct co-housed operators sharing only the reduction skeleton, with genuinely-divergent laws; for `elementwise_product` the law overlap is much larger, hence the different modeling choice — `dot`/`tdot` is **not** the precedent for the sub-axis treatment). Applied the parallel fix at `CYCLE.md:239` (OQ-2 second-half justification): replaced point (b) "matches `dot`/`tdot` convention" with the on-its-own-terms law-overlap justification, and added the same "note on precedent" disambiguation. Citation `book/src/L1/dot.md` verified on-disk against the cited `:16-20` and `:94` line ranges.
  - **Rationale**: this is a precedent-citation-correction repair — the decision (one primitive with conjugation sub-axis) stands on its own merits; only the wrong precedent claim is excised and replaced with self-contained justification. Mechanical and surgical; not substantive authoring.

- **Finding 2** — `L1/index.md` "Firm (count)" header off-by-one on D2+D3 combined apply.
  - **Decision**: repaired.
  - **Action**: added a new edit-block sub-step (`(a) Bump the §Vocabulary-cohort count header`) in the rewritten `edit:book/src/L1/index.md` block at `CYCLE.md:158-188`, mutating `**Firm (24)** —` → `**Firm (25)** —`. The OLD-text anchor (`**Firm (24)**`) matches the *post-D2* on-disk state (D2 bumps `Firm (23)` → `Firm (24)`; D3 then bumps `24` → `25`). Annotated with an explicit "NOTE TO INTEGRATOR" comment at the top of the edit-block stating the post-D2 assumption and the fallback if D2 has not landed first.
  - **Rationale**: mechanical surgical fix (add the missing count edit; no substantive authoring). The D2-then-D3 sequencing assumption is documented inline so the integrator can reconcile if the dispatch order differs.

- **Finding 3** — D2/D3 co-edit anchor collision on `L1/index.md` + `SUMMARY.md`.
  - **Decision**: repaired.
  - **Action**: rewrote all three D3 edit-block anchors to target the **`reciprocal`** lines (which D2 inserts) rather than the `jacobi-smoother` lines. Specifically: (b) the cohort-bullet OLD-text anchors on `- [`reciprocal`](./reciprocal.md) — pure-functional elementwise multiplicative-inverse `result[i] = 1/x[i]`.` (verbatim from D2's CYCLE.md:165 reciprocal-bullet leading sentence); (c) the dep-map row OLD-text anchors on the leading-prefix of D2's `reciprocal` dep-map row (D2's CYCLE.md:169, truncated at the `firm` cell to preserve the integrator's ability to append-after-the-row regardless of D2's exact trailing-cell text); the `SUMMARY.md` edit-block OLD-text anchors on `- [reciprocal](./L1/reciprocal.md)` (D2's SUMMARY insertion). The reciprocal-row truncation is documented in an inline note instructing the integrator to treat the OLD as a prefix-anchor and append the NEW row as a fresh line below.
  - **Rationale**: mechanical re-anchoring — D3's inserts go AFTER `reciprocal` instead of after `jacobi-smoother`, removing the ordering ambiguity. The "NOTE TO INTEGRATOR" comment at the top of the edit-block documents the post-D2 assumption and reconciliation fallback.

- **Finding 4** — §OQ-3 mitigation list does not rank options.
  - **Decision**: not-needed.
  - **Rationale**: OQ-3's mitigation list is informational guidance for the integrator's decision tree, not a defect in the report's proposed changes. The CLAUDE.md "Integration may materialize implied components as stubs" invariant authoritatively names stub-creation as the preferred path; the integrator can apply that invariant directly. Reordering the OQ's mitigation list would not change the proposed changes or the rotation/surface/evidence — it would be a stylistic OQ-prose edit. Outside the repairer's mechanical-fix authority.

- **Finding 5** — §Status forthcoming L1>L0 theme note vs OQ-1 minor redundancy.
  - **Decision**: not-needed.
  - **Rationale**: critic flagged as low/informational (housekeeping). Both mentions are factually consistent; removing one would be stylistic, not surgical. No correctness or build-readiness defect.

- **Finding 6** — §Variant-axes "dead-code conjugate path" citation could be tighter.
  - **Decision**: not-needed.
  - **Rationale**: critic notes the dead-code claim's substance is sound and cross-witnessed against the cycle-032 firm `jacobi-smoother` chapter (which carries the verified caveat on-disk). Tightening the citation (adding `jacobi.cpp:99-104` as the alias-establishing line) would be additive authoring rather than a mechanical fix. The existing cross-witness is sufficient.

### Unrepairable findings

None. All warnings are either mechanically repaired (1, 2, 3) or correctly classified as low/informational housekeeping that does not require repair (4, 5, 6).

### Post-repair verification

- **Fence parity**: `grep -n '^```' CYCLE.md` reports 6 fences (3 open/close pairs) at lines 27/156/158/188/190/196 — even parity, well-formed. The chapter body `new:book/src/L1/elementwise_product.md` (lines 27-156) fully encloses all eight firm-apparatus sections (`## Context`, `## Signature`, `## Semantics`, `## Algebraic laws`, `## Dependencies`, `## Variant axes`, `## Status`, `## L1 vs L0 distinction`, `## Evidence`).
- **Citecheck**: `python3 tools/citecheck/citecheck.py --scan reports/2026-05-30T153000Z-harvester-elementwise-product-l1/CYCLE.md` reports **33 ok, 0 failing (33 citations checked)** — clean. (Citation count drops from the critic's pre-repair tally of 39 → 33 because the repair consolidated the two original `L1/index.md` edit-blocks into one multi-step block, removing duplicate citations from the second block's NEW-text; no citation is lost.)
- **On-disk anchor verification**: `book/src/L1/dot.md:16-20` reads `dot   :: (x: Tensor[N], y: Tensor[N]) -> Scalar` / `tdot  :: (x: Tensor[N], y: Tensor[N]) -> Scalar     -- complex-only variant` ... `Two operators in one chapter because they share the entire reduction skeleton (sum over `N`) and differ only by the per-element kernel.` — confirms the "two distinct co-housed operators" framing. `book/src/L1/dot.md:94` reads `At L1 these are distinct operators (sharing only the reduction skeleton), because the algebraic laws differ — `dot` is positive semi-definite at `y = x`, `tdot` is not.` — confirms the algebraic-law divergence. Both citations support the corrected disambiguation note.

## Suggested resolution

`ready` — all critic warnings are either mechanically repaired or correctly classified as low/informational housekeeping that does not require repair. The report is in a state the integrator-per-report can apply directly, assuming D2 (`reciprocal`) lands first (the standard cycle-033 dispatch ordering per the cycle-planner's D2-then-D3 plan). The "NOTE TO INTEGRATOR" comment inline in the rewritten `edit:book/src/L1/index.md` block documents the post-D2 anchor assumption and the reconciliation path if D2 has not landed first.
