---
agent: cycle-planner
invoked_at: 2026-05-31T235349Z
scope: cycle-040 dispatch plan
status: pending
---

# Cycle 040 dispatch plan

## Goals selected this cycle

Open meta-batch-12 by advancing the **lead L3 frontier**: the c036 D2 (A) identity-in-form cohort is 6-of-6 CLOSED (batch-11), so the frontier shifts to the **(B) substantive L3 cohort** — real iteration-rotation / obstruction analysis, not identity backfills. The headline dispatch is **`orthogonalize` L3** (the suggested first (B) pick across the c038/c039 integrator signals): a genuine `partial-obstruction` candidate where MGS does not lift but CGS/CGS2 do — the third `partial-obstruction` L3 row after `chebyshev` (c013) and `eigsolve` (c024). Co-schedule (a) the queued **L3/index §Semantics-overlay fifth-obstruction-profile fold** + §Working-Notes stale-snapshot compaction (layer-intro-author, owns the shared L3/index count tally per the new count-ownership convention), and (b) an opportunistic **lifter citation-tighten** pair (two fired-trigger LOW-fan-out hygiene one-liners in disjoint L1>L0 files). Three dispatches — deliberately lean; the (B) substantive cohort carries longer per-cycle cost than the just-closed identity backfills, and the audit slot for `orthogonalize`'s `verified_against:` cannot fire until it is firm-on-disk (next cycle).

## Deliverable-presence verification

Per the MANDATORY paste-inline-evidence pre-dispatch procedure (cycle-036 meta-phase strengthening; friction-ledger `cycle-planner-stale-priorities-line-recruitment`). Literal command output pasted per dispatch.

### D1 — `harvester`, L3 `orthogonalize` (substantive (B) backfill)

1. **File existence** — `ls book/src/L3/orthogonalize.md`:
   ```
   ls: cannot access 'book/src/L3/orthogonalize.md': No such file or directory
   ```
   → NOT on disk. OPEN.
2. **Maturity** — file does not exist → no on-disk maturity; deliverable is a fresh firm L3 entry. (Prerequisite homes confirmed firm: `book/src/L1/orthogonalize.md` `## Status: firm`; `book/src/L2/orthogonalize.md` `## Status: firm`; `book/src/L2-L1/orthogonalize-composition-lowering.md` firm + 17-citation `verified_against:` audit per OQ closed c023.)
3. **OQ-ledger RESOLVED-grep** — `grep 'orthogonalize.*RESOLVED\|l3.*orthogonalize' scaffolding/open-questions.md`: the only orthogonalize-RESOLVED lines are the **L2 / L2>L1** themes (c022/c023/c025) — NO `l3-orthogonalize` resolution exists. The L3 entry is named OPEN by the c036 D2 audit (`l3-cohort-growth-audit-c036-verdict`, (B) substantive list) and re-flagged open by the c039 finalize routed-follow-up. NOT stale.
4. **Structural-block check** — NOT blocked. This is a substantive (B) iteration-rotation, not a gated firm-promotion. The (C) STOP-PROPOSING NEGATIVE LIST (`lu_solve`/`back_solve`/`ls-update-column`/4 NLEPS atoms) does NOT contain `orthogonalize`. Framing is correct as **harvester substantive** (NOT identity-in-form; NOT reflexive-harvest) — the L0 bodies confirm a real obstruction split (MGS sequential, CGS/CGS2 lifts), so this is genuine iteration-rotation analysis, exactly the audit-distinct producer route.
   - L0 anchors pre-localized (codemap + on-disk; embedded in D1 scope below): MGS body `orthog.hpp:41-53`; CGS/CGS2 body `orthog.hpp:57-89`; dispatch wrapper `iterative.cpp:308-325` (variant select at `:315-322`).
   → **ALL FOUR PASS. RECRUIT.**

### D2 — `layer-intro-author`, L3/index §Semantics-overlay 5th-obstruction-profile fold + §Working-Notes compaction (+ owns the shared L3/index count tally)

1. **File existence** — `ls book/src/L3/index.md`: exists.
2. **Maturity / already-discharged** — the §Semantics-overlay currently enumerates **four** firm obstruction shapes (a)/(b)/(c)/(d) (read on disk, `L3/index.md:15`: "Four firm shapes coexist at L3"); the **fifth** profile (`fused-composite-obstruction-free`, `normalize` exemplar) is **NOT yet folded** — it is an OPEN OQ:
   ```
   919:- `l3-index-fifth-obstruction-profile-fused-composite-obstruction-free` — opened cycle-039 D1. ... A one-clause overlay-taxonomy mention would aid navigation. *Trigger:* next layer-intro-author L3-index touch (co-schedulable with the §Working-Notes stale-snapshot compaction below).
   920:- `l3-index-working-notes-stale-snapshot-compaction-candidate` — opened cycle-039 D3. ... a future compaction pass *could* prune them ... *Trigger:* a dedicated §Working-Notes compaction dispatch.
   ```
   → The two co-schedulable OQs are OPEN with fired triggers (this is the "next layer-intro-author L3-index touch"). NOT discharged.
3. **OQ-ledger RESOLVED-grep** — the two OQs above are in the OPEN section, NOT the Closed index. NOT stale.
4. **Structural-block check** — NOT blocked; the trigger is a layer-intro-author L3-index touch, fired this cycle by the co-scheduled D1 landing (which itself adds a NEW obstruction shape — D2 must account for it; see deps).
   → **ALL FOUR PASS. RECRUIT.**

### D3 — `lifter`, two fired-trigger citation tightens (floquet m-block + chebyshev-smoother-applyorder0)

1. **File existence** — `ls book/src/L1-L0/floquet-correction-mutation-rotation.md` and `…/chebyshev-smoother-mutation-rotation.md`: both exist (firm themes on disk).
2. **Maturity / already-discharged** — both are open citation-hygiene OQs (the over-extensions are real on disk, not yet tightened):
   ```
   923:- `floquet-mutation-rotation-m-block-comment-citation-over-extension` — opened cycle-038 D4 (minor, non-blocking). Theme body line 229 cites the M-block comment as `:25-26`; on-disk `:25` is the comment, `:26` the opening brace `{` — a one-line over-extension ... An abstractor may tighten to `:25` opportunistically. *Trigger:* any future lifter / same-layer-cross-cutter touch on the theme.
   908:- `chebyshev-smoother-mutation-rotation-applyorder0-true-citation-tighten-sibling` — opened cycle-035 D1. ... cited as `:101-110`; tight dead `else`-block body is `:102-110` (1-line overshoot on start). ... *Trigger:* a future lifter / same-layer-cross-cutter touch.
   ```
   → Both OPEN, fired-trigger ("a future lifter touch"). NOT discharged.
3. **OQ-ledger RESOLVED-grep** — both OQs are in the OPEN/deferred section, NOT the Closed index. NOT stale.
4. **Structural-block check** — NOT blocked (pure citation-range tightens; `verify-citation-range` skill + `citecheck --anchor`). Both pass `--anchor` today (not citecheck-failing drift) — these are precision hygiene, opportunistic-only, which is exactly the role-spec's co-schedulable lifter-touch class.
   → **ALL FOUR PASS. RECRUIT.**

## Dispatches

1. **agent:** `harvester`
   **scope:** **L3 `orthogonalize`** — substantive (B) iteration-rotation backfill. Author `book/src/L3/orthogonalize.md` as the third `partial-obstruction` L3 entry (after `chebyshev` c013 + `eigsolve` c024). The substantive rotation: lift the per-column orthogonalization to a global tensor-field form **where it lifts**, and record the obstruction **where it does not**, per the `partial-obstruction` invariant (body-lifts-loop-doesn't, with the cited non-removability reason). Pre-localized L0 anchors (codemap + on-disk verified; embed and read these directly — do NOT enter a localization loop):
     - **Dispatch wrapper** `palace/linalg/iterative.cpp:308-325` (variant select `:315-322`: `MGS` → `OrthogonalizeColumnMGS`, `CGS` → `OrthogonalizeColumnCGS`, `CGS2` → `OrthogonalizeColumnCGS(..., true)`).
     - **MGS body** `palace/linalg/orthog.hpp:41-53` — the inner `j`-loop is a **`sequential-obstruction`**: `w.Add(-H[j], V[j])` mutates `w` in-place *before* `H[j+1] = dot_op(w, V[j+1])` is computed, so each column projection depends on the prior subtraction. Does NOT lift to a single global tensor-field op (numerical-stability-rooted recurrence, like `chebyshev`'s `k`-recurrence).
     - **CGS / CGS2 body** `palace/linalg/orthog.hpp:57-89` — **lifts cleanly**: all `H[j] = dot_op(w, V[j])` inner products are computed against the *original unmutated* `w` (the projection loop completes before any `w.Add`), then a single batched subtraction `w -= Σ H[j] V[j]`. This is a global form: `H = Vᴴw` (one batched matvec) followed by `w -= V H`. CGS2 (`refine=true`) is two such passes. Record the CGS/CGS2 path as lifting; the MGS path as the obstruction.
     - Cite the L1 firm leaf `book/src/L1/orthogonalize.md` (L0 read in full at `orthog.hpp:18-90`) and the firm L2 `book/src/L2/orthogonalize.md` for upward context; the L3>L2 / L3>L1 edge narrates downward per high→low discipline. **Count-ownership: D1 appends ONLY its own L3/index dep-map row + SUMMARY registration; it does NOT touch the §Working-Notes consolidated firm/partial-obstruction count tally — D2 (layer-intro-author) owns the tally this cycle (see Overlap analysis).**
   **deps:** none
   **rationale:** Lead L3 frontier (priorities.md active head #1; Backlog Medium `l3-substantive-cohort-from-c036-audit`; plan-tag `l3-substantive-cohort-b-batch-12`). The suggested first (B) pick across c038/c039 integrator signals. Substantive iteration-rotation with real obstruction analysis — distinct from the just-closed identity backfills.

2. **agent:** `layer-intro-author`
   **scope:** **L3/index.md §Semantics-overlay + §Working-Notes refresh.** Two co-scheduled OQ-driven touches plus the count-tally ownership: (a) fold the **fifth obstruction profile** `fused-composite-obstruction-free` (`normalize` exemplar — the cleanest end of the spectrum: a fused composite carrying NO obstruction because *both* constituents `nrm2`+`scal` are individually obstruction-free) as a one-clause mention in the §Semantics-overlay taxonomy (OQ `l3-index-fifth-obstruction-profile-fused-composite-obstruction-free`); (b) the §Working-Notes stale-snapshot compaction — prune/label the older c024/c037 inline running-count snapshots so the live count is unambiguous (OQ `l3-index-working-notes-stale-snapshot-compaction-candidate`). **(c) COUNT-OWNERSHIP (this cycle's sole owner of the L3/index consolidated tally):** D2 authors the single authoritative §Working-Notes firm / partial-obstruction count, **accounting for D1's `orthogonalize` landing** — if D1 lands `orthogonalize` as `partial-obstruction`, the tally becomes **15 firm + 3 partial-obstruction** and the §Semantics-overlay gains a **sixth** firm/partial-obstruction shape (orthogonalize's MGS-sequential / CGS-lifts split, a numerical-stability-rooted `partial-obstruction` sibling of `chebyshev`); fold that shape into the taxonomy alongside the fifth-profile note. D1 defers the tally to D2 entirely.
   **deps:** D1 (forward-reference: D2's count tally + the new obstruction-shape mention must reflect `orthogonalize` on disk; D2 runs in wave-2 so the per-report integrator wires the live `[orthogonalize](./orthogonalize.md)` link and D2 reads the landed `## Status`).
   **rationale:** Discharges the two c039-opened co-schedulable L3-index OQs (fired trigger: "next layer-intro-author L3-index touch") AND owns the shared-index count tally per the new `parallel-blind-shared-index-count-divergence` count-ownership convention (cycle-039 meta-phase). Keeps the L3/index narrative current as the (B) cohort opens.

3. **agent:** `lifter`
   **scope:** Two opportunistic citation-range tightens (fired-trigger LOW-fan-out hygiene), both via `verify-citation-range` + `citecheck --anchor`, in disjoint L1>L0 files:
     - `book/src/L1-L0/floquet-correction-mutation-rotation.md` body line ~229: tighten the M-block comment citation `:25-26` → `:25` (on-disk `:25` is the comment, `:26` the opening brace) — OQ `floquet-mutation-rotation-m-block-comment-citation-over-extension`.
     - `book/src/L1-L0/chebyshev-smoother-mutation-rotation.md`: tighten the first dead-code complex-transpose kernel citation `:101-110` → `:102-110` (1-line start overshoot; tight dead `else`-block body is `:102-110`) — OQ `chebyshev-smoother-mutation-rotation-applyorder0-true-citation-tighten-sibling`.
   **deps:** none
   **rationale:** Two fired-trigger ("future lifter touch") minor citation hygiene OQs, co-scheduled into one lifter dispatch. Disjoint files from D1/D2. LOW fan-out but cheap and closes two lingering one-liners while a citation-touch agent is in flight (priorities.md active head #2 / Backlog audit slot).

## Overlap analysis

- **D1 ↔ D2** — **OVERLAPPING on `book/src/L3/index.md`** (and forward-reference on `orthogonalize.md`). D1 appends a dep-map ROW + SUMMARY registration; D2 rewrites the §Semantics-overlay taxonomy prose + the §Working-Notes **consolidated count tally**. Per the count-ownership convention (cycle-039 meta-phase; friction-ledger `parallel-blind-shared-index-count-divergence`): distinct dep-map *rows* are parallel-safe, but the **consolidated tally is a shared mutable derived value** — exactly one owner. **D2 (layer-intro-author) is the sole tally owner this cycle; D1 appends ONLY its dep-map row + SUMMARY entry and DEFERS the §Working-Notes firm/partial-obstruction count to D2.** Additionally D2's overlay narrative must reference D1's `orthogonalize` (new obstruction shape) and wants a live link → **sequential: D1 in wave-1, D2 in wave-2** so the per-report integrator wires the live `[orthogonalize](./orthogonalize.md)` link and D2 reads `orthogonalize.md`'s landed `## Status`. This is the c039 working precedent (D3 layer-intro-author sole-owned the tally; D1 harvester appended only its dep-map row), applied identically.
- **D1 ↔ D3** — **NON-overlapping.** D1 touches `book/src/L3/orthogonalize.md` (+ L3/index row) and reads `iterative.cpp`/`orthog.hpp`. D3 touches `book/src/L1-L0/floquet-correction-mutation-rotation.md` + `chebyshev-smoother-mutation-rotation.md`. Disjoint files, disjoint operator names (no shared slug; the orthogonalization source is not cited by either floquet or chebyshev-smoother theme). PARALLEL.
- **D2 ↔ D3** — **NON-overlapping.** D2 touches `L3/index.md`; D3 touches two L1>L0 theme files. Disjoint. PARALLEL.

## Sequencing schedule

- **Wave 1 (parallel):** D1 (`harvester` L3 orthogonalize), D3 (`lifter` citation tightens). No shared region; no forward-reference between them.
- **Wave 2 (after D1's report lands):** D2 (`layer-intro-author` L3/index overlay + count tally). D2 forward-references D1's `orthogonalize` (needs the live link + landed `## Status` to author the obstruction-shape mention and the authoritative count), and owns the shared L3/index tally per the count-ownership partition. Wave-2 is dispatch/forward-reference ORDERING only — the book is NOT rebuilt between waves; there is exactly ONE `integrator-finalize` at cycle end.

(D3 may equivalently run in wave-2; placed in wave-1 since it is fully independent and parallel-when-in-doubt is the default.)

## Open questions / caveats

- **`chebyshev-smoother` / `apply_nonlinear_pencil` (B) candidates deferred, not dropped.** The (B) cohort has three members; I dispatched only `orthogonalize` this cycle (the suggested first pick and the cleanest substantive case). `chebyshev-smoother` L3 REQUIRES a subsumption check FIRST against the existing firm L3 `chebyshev` (it may fold in rather than land standalone) — that subsumption analysis is itself a worthwhile cross-layer-cross-cutter dispatch, scheduled for cycle-041 once `orthogonalize` validates the (B) harvester route. `apply_nonlinear_pencil` L3 should fold into a future eigsolve-variant deepening pass (NOT a standalone L3 row) per the c036 D2 audit — also cycle-041+. Both remain in priorities.md Backlog Medium (`l3-substantive-cohort-from-c036-audit`).
- **`orthogonalize` L3 `verified_against:` audit could not be scheduled this cycle** (lowering-verifier audit slot empty) — the audit needs the L3 entry firm-on-disk, which happens only at this cycle's finalize. Routed to cycle-041 as the natural follow-up (a `lowering-verifier` `orthogonalize` L3 `verified_against:` audit, analogous to the c023 L2>L1 17-citation audit).
- **Lean slate (3 dispatches) is intentional.** The (B) substantive cohort carries longer per-cycle cost than identity backfills (real obstruction analysis), and the natural co-dispatches (the audit, the next (B) member) are gated on this cycle's landings. The 3-dispatch slate front-loads the highest-fan-out substantive frontier item plus two cheap fired-trigger housekeeping closes. Well under the 12-cap; fewer-is-fine per Discipline when priorities don't fill the slots with genuinely-open, non-blocked work.
- **L1-promotion-gated track untouched** (`matrix-weighted-norm` / `bilinear-form` L1 `rough-in (test-coverage-bounded)`, and the dependent `normalize_B` L3) — correctly NOT dispatched (the L1 firm-promotion is gated on out-of-scope dedicated test coverage; no upstream Palace test change since the priorities line was authored, so the gate is still blocking). Track-but-don't-dispatch per the cycle-040 caveat. Direction (i) of active-head slot #3 (authoring the `matrix-weighted-norm-mutation-rotation` L1>L0 theme) is a viable cycle-041+ pick if a producer slot opens.
- No methodology-adjustment candidate surfaced that isn't already in the friction-ledger. The batch-11 enactments (count-ownership convention, stale-priorities CLOSED) are applied in this plan (D1/D2 count partition; full paste-inline-evidence deliverable-presence section). The codemap +1-drift guard held during my localization (I read on-disk for the `orthog.hpp` anchors; the codemap `read_range` matched the on-disk content at `:18-90`, no boundary drift on this region).

## priorities.md update

Marking the dispatched picks in `scaffolding/priorities.md` active head (D1 = active-head #1 `orthogonalize` (B); D3 = active-head #2 lifter touches; D2 = active-head slot #3 direction (iii) L3-index overlay refresh). No fresh plan candidates appended (no new fired-trigger OQ / integrator-signal surfaced actionable work not already in the Backlog).
