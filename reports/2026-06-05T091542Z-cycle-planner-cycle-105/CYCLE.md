---
agent: cycle-planner
invoked_at: 2026-06-05T091542Z
scope: cycle-105 dispatch plan
status: pending
---

# Cycle 105 dispatch plan

## Goals selected this cycle

Cycle-105 is the **batch-CLOSING** cycle of meta-batch-33 (cycles 103/104/105; the batch-33 meta-phase fires AFTER this cycle's finalize, aggregating 103/104/105). Per batch-closing discipline (c096/c099 precedent) this cycle takes **LOW-RISK, cleanly-closeable content-tail + hygiene** rather than launching the large WAVE-3 op-chapter typed-edge migration, which would straddle the meta boundary and whose core reachability payoff is currently **invisible to the linter** (the c104-surfaced block-mapping-edge parser bug — a `tools/`/meta-phase-owned fix that fires next). The cycle closes four self-contained, disjoint items: the last unlinked output-product column's record-reachability gap (energy-fields), the record-concept prose ±1 citation drift, the config-record `main.cpp` basename-AMBIG citation hygiene, and the forthcoming `set-subvector-zero-mutation-rotation` L1>L0 lowering leg.

## Batch-closing judgment — WAVE-3 op-chapter typing vs content-tail

**Decision: DEFER the WAVE-3 op-chapter typed-edge migration to the meta-phase + cycle-106; run a clean bounded content-tail this batch-closing cycle.** Rationale, with on-disk evidence:

1. **The WAVE-3 reachability payoff is linter-INVISIBLE until the meta-phase fixes the block-mapping-edge parser** (the c104 critical finding, integrator-signals c104 §Integration-tooling-friction). The 6 internal records (`sim-state`/`krylov`/`step-outputs`/`prev-carry`/`solve-result` + `dofset`) reach the GC roots ONLY through op-chapter edges, and the chain that would rescue them is **two block-mapping hops the linter cannot read**: (a) feature column → L4 op is a block-mapping `depends-on (kind: composes)` edge (verified on disk: `feature/eigenmode.L4.md` → `L4/eigsolve` / `L4/fe_assemble`), and (b) the op chapters would themselves need block-mapping `uses-record` edges to the records. Until `parse_frontmatter` learns the `- target:/  kind:` multi-line form, none of these traverse — so the WAVE-3 deliverable's headline payoff ("rescue 6 records from garbage") **cannot be measured this cycle**. The fix is `tools/`-authority = meta-phase, which fires next.

2. **WAVE-3 is large pre-scheme→scheme MIGRATION, and `krylov-step` is from-scratch.** On-disk frontmatter audit of the L4 solve/BC op chapters: only `preconditioning-framework.md` carries the new `edges:` scheme block; `ksp_solve`/`eligsolve`/`eliminate_bc`/`solve_family`/`fold_solve` carry **pre-scheme** `consumes:/lowers_to:/depends_on:` (no typed `edges:`, so zero typed outbound edges); and **`krylov-step.md` has NO frontmatter at all** — the chapter the 6 records all `reference`, and the one whose signatures name all 6 records (`SimState`/`Krylov`/`OpParams`/`StepOutputs`/`PrevCarry`/`SolveResult`, verified at `krylov-step.md:25,31,32,37`). Authoring krylov-step's scheme frontmatter from scratch + migrating 5 pre-scheme op chapters is a multi-dispatch campaign tranche, not a batch-closing slice.

3. **The signal's own bounded-slice suggestion ("type just the L4 solve op chapters that already have partial frontmatter") still inherits the linter-blindness in (1)** — the rescue edges it would author are block-mapping edges the GC can't mark across. So even the bounded slice yields an unmeasurable payoff this cycle and is cleaner to do AFTER the meta-phase linter fix lands (cycle-106), when the campaign can author the op-chapter edges AND see the rescue in `--show-inbound`. **Coupling the WAVE-3 tranche to the post-fix cycle is the correct sequencing** — it avoids re-touching the same op chapters once to author block-mapping edges (now) and again to verify (post-fix).

**Conclusion:** the batch-closing cycle does clean, immediately-measurable closes (links resolve, citecheck flips to ok, a new firm node lands) and leaves the WAVE-3 campaign + its linter dependency + the carried meta-owned items (node-status convention unification, navigational-container scheme ratification, the 2+1 linter-gap fixes incl. the block-mapping-parser bug, role-fit decision) to the meta-phase that OWNS them and fires next. This is exactly the c096/c099 batch-closing precedent.

## Deliverable-presence verification (paste-inline evidence)

**D1 — energy-fields record pages + uses-record edge** (open by construction for the two new pages; reachability-edge is gap-confirmed):
- File existence: `ls book/src/concepts/postprocess-config.md` → `No such file or directory`; `ls book/src/concepts/domaindata.md book/src/concepts/domain-data.md` → both `No such file or directory`. The two record-definition pages do NOT exist → author-new.
- Reachability gap: `grep -c 'uses-record' book/src/feature/energy-fields.L4.md` → `0`. The last output-product column carries NO config-record edge (deliberately not-linked c104, OQ `energy-fields-config-and-domaindata-records-need-concept-pages` MEDIUM).
- OQ-ledger: OQ `energy-fields-config-and-domaindata-records-need-concept-pages` is OPEN (needs-more, opened c104) — `grep` shows it carries the explicit route "(a) decide config-record-fold vs own page for `PostprocessConfig`, then (b) add the analogous `uses-record` edge". Not RESOLVED/CLOSED.
- Structural-block: none. Codemap-confirmed L0 homes: config surface `PostprocessConfig`/`DomainData` config-side at `palace/utils/configfile.hpp:313` (`struct DomainData`); result-side `Measurement::DomainData` fwd-decl `palace/models/postoperator.hpp:43` (`struct DomainData;`), consumed at `postoperator.hpp:389`. PASS.

**D2 — record-concept prose ±1 citation drift fix** (open; OQ confirmed open, drift confirmed on disk):
- File existence: `book/src/concepts/op-params.md` + `book/src/concepts/sim-state.md` both exist (typed firm c104).
- Drift confirmed: `op-params.md:76` prose reads ``rel_tol, abs_tol` (`iterative.hpp:42`), `max_it` (`iterative.hpp:45`)``; codemap `read_range iterative.hpp:40-55` shows exact `:41` = `double rel_tol, abs_tol;`, `:44` = `int max_it;`, `:52-54` = the `mutable converged/initial_res,final_res/final_it`. The prose is ±1 high — confirmed.
- OQ-ledger: OQ `record-concept-prose-citation-pm1-drift` OPEN (LOW, opened c104) — explicitly "PRE-EXISTING prose-citation drift ... does NOT affect the rank/edge typing ... Route: a future citation-audit / harvester pass re-anchors the prose ±1 drifts". Not closed.
- Structural-block: none (NON-build-breaking prose; not a markdown link, so `linkcheck2` unaffected). PASS.

**D3 — config-record `main.cpp` basename-AMBIG citation hygiene** (open; AMBIG sites confirmed on disk):
- File existence: `book/src/concepts/config-record.md` exists (firm, kind: record, c103).
- AMBIG sites confirmed: `grep -ni 'main.cpp' config-record.md` → `:50 palace/main.cpp:231`, `:96 main.cpp:259`, `:97 main.cpp:257-281`, `:134 main.cpp:231 / main.cpp:259`, `:137 main.cpp:262-280`. The bare `main.cpp:NNN` (lines 96/97/134/137) are basename-shorthand; the canonical `palace/main.cpp` already appears at `:50`/`:134` — citecheck flags the bare ones AMBIG (c104 signal §5).
- OQ-ledger: surfaced in c104 finalize signal §5 ("`config-record` `main.cpp` prose-body basename-shorthand AMBIG (4 sites, pre-existing — should be fully-qualified `palace/main.cpp`)"). Carried, not closed.
- Structural-block: none (citation-format hygiene; touches no claim). PASS.

**D4 — `set-subvector-zero-mutation-rotation` L1>L0 lowering leg** (open by construction — theme not yet authored; firm L1 entry landed c104):
- File existence: `ls book/src/L1-L0/set-subvector-zero-mutation-rotation.md` → does NOT exist (the "(forthcoming)" forward-reference in `L1/set_subvector_zero.md` §Downward/§Status, NOT a dangling live link — confirmed plain-text in c104 finalize).
- OQ-ledger: OQ `set-subvector-zero-mutation-rotation-theme-forthcoming` OPEN (LOW-MEDIUM, opened c104) — "*Trigger:* an L1>L0 lowering pass authoring `book/src/L1-L0/set-subvector-zero-mutation-rotation.md`". Trigger = this dispatch.
- Structural-block: none. The L1 entry `set_subvector_zero` is already firm (rests on `cites-evidence` to `vector.cpp:461-474`/`:476-492`/`vector.hpp:220-221`); the theme is a faithful lowering narration, not a rank-gating dep. PASS.

All four candidates: ALL checks pass, none on the STOP-PROPOSING negative list, framing correct (D1/D4 author-new; D2/D3 hygiene-on-existing).

## Dispatches

1. **(`layer-intro-author`, "energy-fields record pages + uses-record reachability edge", deps: none — WAVE-1)**
   Scope: (a) make the content judgment for `PostprocessConfig` — author `book/src/concepts/postprocess-config.md` as its own record page (the config-postprocess domain-set surface; L0 home `palace/utils/configfile.hpp:313` `struct DomainData` config-side + the `PostprocessConfig`/`IoData` postprocess sub-record) UNLESS it cleanly folds into `concepts/config-record` (decide per the OQ; if fold, record the fold verdict + add the edge to `config-record` instead of a new page); (b) author `book/src/concepts/domaindata.md` for the result-side `Measurement::DomainData` per-domain energy-table row (L0 home `palace/models/postoperator.hpp:43` fwd-decl + `:389` consumer; `energy-fields.L4.md:92,104` already name `Measurement::DomainData`); both pages typed firm + `kind: record` + `edges:` (`depends-on kind:cites-evidence` → the L0 ranges; `reference` → `energy-fields.L4` + `concepts/config-record`), HARD-gate-new per the graded-stack scheme; (c) add the `depends-on (kind: uses-record)` edge(s) from `feature/energy-fields.L4.md` → the new page(s) + the reciprocal back-references. Resolves OQ `energy-fields-config-and-domaindata-records-need-concept-pages` (MEDIUM) + advances OQ `record-DomainData-needs-definition-home`. **Codemap-confirmed L0 anchors (HINTS — on-disk-confirm; END lines brace-drift-prone):** `configfile.hpp:313` (`struct DomainData`), `postoperator.hpp:43` (`struct DomainData;` fwd-decl), `postoperator.hpp:389` (consumer). **fan-out: MEDIUM** (closes the last output-product column's record-reachability + two record-definition homes).
   Rationale: closes the one deliberately-not-linked output-product column from c104; self-contained authoring; the content judgment is bounded (config-fold vs own page, one decision).

2. **(`harvester`, "record-concept prose ±1 citation-drift re-anchor on op-params.md + sim-state.md", deps: none — WAVE-1)**
   Scope: re-anchor the ±1-off PROSE-body citations in `book/src/concepts/op-params.md` and `book/src/concepts/sim-state.md` to the exact declaration lines: `iterative.hpp:42`→`:41` (`rel_tol, abs_tol`), `:45`→`:44` (`max_it`), `:49-50`→`:48-49` (`A,B` operator/preconditioner), `:53-55`→`:52-54` (the `mutable` solve-statistics). Touch ONLY the prose-body citations; do NOT alter the typed `cites-evidence` frontmatter (which anchors the verified enclosing region `iterative.hpp:26-115` and is already sound). Resolves OQ `record-concept-prose-citation-pm1-drift` (LOW). **Codemap-confirmed exact lines (on-disk-confirm):** `read_range iterative.hpp:40-55` shows `:41` `double rel_tol, abs_tol;`, `:44` `int max_it;`, `:48-49` `const OperType *A; const Solver<OperType> *B;`, `:52-54` `mutable bool converged; mutable double initial_res, final_res; mutable int final_it;`. **fan-out: LOW** (cosmetic citation correction; not a markdown link).
   Rationale: clean, mechanical, immediately-verifiable (citecheck flips); a harvester is the right home for exact-anchor re-citation on a record page it understands.

3. **(`lifter`, "config-record main.cpp basename-AMBIG full-path hygiene", deps: none — WAVE-1)**
   Scope: full-path the bare `main.cpp:NNN` citations in `book/src/concepts/config-record.md` (lines `:96`, `:97`, `:134`, `:137`) to `palace/main.cpp:NNN` (matching the already-canonical `palace/main.cpp:231` at `:50`/`:134`), so citecheck resolves them `[ok]` instead of `[AMBIG]`. Pure citation-format hygiene; touch NO claim, NO frontmatter edge. Re-run citecheck on the file to confirm clean. Resolves c104 finalize signal §5 (`config-record` `main.cpp` prose-body basename-shorthand AMBIG, 4 sites). **fan-out: LOW** (citation hygiene).
   Rationale: clean batch-closing hygiene; the 4 AMBIG sites are pre-existing prose-body shorthand on a firm record page, build-safe, fixed in one pass.

4. **(`abstractor`, "set-subvector-zero-mutation-rotation L1>L0 lowering theme", deps: none — WAVE-1)**
   Scope: author `book/src/L1-L0/set-subvector-zero-mutation-rotation.md` — the lowering theme for the firm c104 L1 entry `set_subvector_zero`, narrating the in-place `x.ReadWrite(use_dev)` destination-is-input idiom, the `rows.Read(use_dev)` index gather, the `mfem::forall_switch` device-vs-host kernel dispatch, and the complex `XR`/`XI` two-buffer threading. LHS L1 (the `set_subvector_zero` pure form), RHS L0 (`palace/linalg/vector.cpp:461-474` real + `:476-492` complex + decl `vector.hpp:220-221`). De-stale the "(forthcoming)" plain-text forward-reference in `L1/set_subvector_zero.md` §Downward/§Status to the live theme (coupled re-anchor, floor-landing-implies-adjacent-reanchor convention — the abstractor extends its own proposed-changes to the parent L1 entry's §Downward since the re-anchor is mechanical). Author its `edges:` typed from the start (HARD-gate-new): `depends-on kind:cites-evidence` → the 3 L0 ranges; `reference` → `L1/set_subvector_zero`. Resolves OQ `set-subvector-zero-mutation-rotation-theme-forthcoming`. **fan-out: LOW-MEDIUM** (one lowering theme; the L1 entry is already firm without it). **Codemap anchors (HINTS):** `vector.cpp:461-474` (real `SetSubVector`), `:476-492` (complex), `vector.hpp:220-221` (decl).
   Rationale: closes a forthcoming-leg forward-reference cleanly; an abstractor is the lowering-theme author; the parent re-anchor is bundled in-dispatch per the coupled-re-anchor convention.

## Overlap analysis

Pairwise file-region check (all four touch DISJOINT artifact regions):
- **D1 ∩ D2:** D1 writes `concepts/postprocess-config.md` (new) + `concepts/domaindata.md` (new) + `feature/energy-fields.L4.md` (edge insert + back-refs). D2 writes `concepts/op-params.md` + `concepts/sim-state.md` prose-body citations. Disjoint files. NON-overlapping → parallel.
- **D1 ∩ D3:** D3 writes `concepts/config-record.md` prose `main.cpp` citations. D1 may add a `reference: concepts/config-record` edge in its NEW pages and a `uses-record` edge in `energy-fields.L4` IF it folds PostprocessConfig into config-record — but that is an edge IN the new page / feature column, NOT a write to `config-record.md` (D1 appends a reciprocal back-ref only if it authors a new page that config-record should list; if it FOLDS into config-record, D1 would append to config-record.md). **Potential mild same-file touch on `config-record.md` IF D1 chooses the fold path.** Per conflict-tolerance philosophy (when in doubt PARALLEL; false-sequentialization is the worse error), and because D3's edit is to the prose-body `main.cpp` citations (lines 96/97/134/137) while any D1 fold-append is a §back-reference / §consumers list (distinct region), these are byte-disjoint regions of the same file in the worst case → **mark PARALLEL**; the per-report integrators apply serially in staging order and re-read off disk (the c104 D3/D4 `L1/index.md` precedent: two writers, distinct regions, applied serially clean). If D1 takes the own-page path (the more likely judgment), there is NO config-record.md write at all.
- **D1 ∩ D4 / D2 ∩ D3 / D2 ∩ D4 / D3 ∩ D4:** fully disjoint files (D4 = `L1-L0/set-subvector-zero-mutation-rotation.md` new + `L1/set_subvector_zero.md` §Downward re-anchor). NON-overlapping → parallel.

No shared consolidated tally / running count this cycle: D1's two new concept pages are registered in SUMMARY (concepts group) + register their own rows; there is no per-cohort consolidated count being summed by ≥2 dispatches (the `concepts:` count is a finalize-owned narration, single-owner). No dual-registration partition needed (each dispatch owns its own rows/bullets; no count-owner contention). No cross-report forward-reference slug divergence: D4's theme slug `set-subvector-zero-mutation-rotation` is the canonical slug already named in the c104 OQ and the `L1/set_subvector_zero.md` forward-reference (no sibling authors it); D1's new page slugs (`postprocess-config` / `domaindata`) are authored AND referenced by the same dispatch.

## Sequencing schedule

**Single wave (all four parallel).** D1/D2/D3/D4 touch disjoint file sets (with the one mild, byte-disjoint, integrator-serialized worst-case same-file touch on `config-record.md` between D1-fold-path and D3, marked parallel per conflict-tolerance). No forward-reference dependency: no dispatch references another's not-yet-landed slug. The per-report integrators apply serially (artifact writes serialize naturally), then ONE `integrator-finalize` rebuilds + commits + pushes + runs step-5b linters at cycle end.

- **Wave 1 (parallel):** D1, D2, D3, D4.

## Open questions / caveats

- **WAVE-3 op-chapter typed-edge migration is DEFERRED to the meta-phase + cycle-106, NOT dropped.** It is the natural LEAD continuation, but its reachability payoff is linter-invisible until the meta-phase fixes the block-mapping-edge `parse_frontmatter` bug (c104 finding), and `krylov-step` needs from-scratch scheme frontmatter. The cleanest sequencing is: meta-phase fixes the parser (+ ratifies navigational-container, unifies node-status convention, fixes `is_likely_outside_dag`/`uses-record` recognition) → cycle-106 runs the WAVE-3 op-chapter typing campaign tranche with measurable rescue. Flagged for the batch-33 meta-phase (fires after this cycle's finalize).
- **Carried meta-owned items (NOT dispatched — meta-phase authority, fires next):** (1) the **block-mapping-edge parser bug** in `tools/graded-stack-lint/graded_stack_lint.py` `parse_frontmatter` (the new c104 finding — blocks measurement of ALL block-mapping `depends-on`/`uses-record`/`composes` edges, incl. the c103/c104 feature-column rescue edges); (2) node-status convention unification (D1/D3 `reference`-only-block vs D2 no-frontmatter on non-node concept pages); (3) `kind: navigational-container` scheme ratification into `graded-stack-scheme.md`; (4) `is_likely_outside_dag` misses group-intros + `concepts/dependency-map`; (5) the P1-edge-typing role-fit friction (no dedicated edge-typer role; `layer-intro-author` stretched); (6) the doubly-stale memory `project_l4_is_backend_lowering_target` (FE-cohort hole falsified twice; user-owned memory — an ASK).
- **D1 content judgment (config-record-fold vs own page for `PostprocessConfig`) is delegated to the producer's localization** — the plan names both anchors and the decision criterion (does PostprocessConfig fold cleanly into the `IoData`/`config-record` shape, like the per-driver `*Config` records, or carry distinct postprocess-domain-set structure warranting its own page?). Either outcome resolves the OQ; the overlap analysis covers both paths.
- **Linter exit unaffected this cycle:** all four dispatches add only `cites-evidence`-to-L0 / `reference` / `uses-record` edges resting on firm-or-L0 targets, so `rank(u)≤rank(v)` holds and `rank_violations` stays 0 (the gate). The `untyped` count may tick down slightly (D1's 1-2 new typed record pages) but the headline reachability rescue for the 6 internal records is NOT this cycle's deliverable (it is WAVE-3, deferred).
