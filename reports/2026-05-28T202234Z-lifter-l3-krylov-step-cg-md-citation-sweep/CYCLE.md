---
agent: lifter
invoked_at: 2026-05-28T202234Z
scope: L3 operator-entry re-anchor — l3-krylov-step-cg-md-citation-sweep
status: integrated
integrated_at: 2026-05-29T0030Z
integration_commit: 1af0c3d
integration_notes: "Applied cycle-015 (per-report position 4). Pure citation re-anchor — 5 dangling cg.md reduced-slice pointers in L3/krylov-step.md re-anchored to terminal firm homes (sequential-obstruction concept page + arnoldi_step.md:194-213; L3-L2/krylov-step-body-identity.md:125; L2/krylov-step.md:138/:146 — repairer corrected 2 producer pointers that had relocated dangles one hop). Entry stays firm. OQ l3-krylov-step-cg-md-citation-sweep answered. Sibling L4/L2 sweeps filed as cycle-016 OQs. Book build clean."
inputs:
  - book/src/L3/krylov-step.md
  - book/src/L3-L2/krylov-step-body-identity.md
  - book/src/concepts/sequential-obstruction.md
  - book/src/spec/slices/cg.md
  - book/src/spec/slices/arnoldi_step.md
  - book/src/L4/krylov-step.md
  - book/src/L2/krylov-step.md
---

# CYCLE: Re-anchor l3-krylov-step-cg-md-citation-sweep

## Summary
The firm L3 operator entry `book/src/L3/krylov-step.md` carries **5 dangling `cg.md:NNN-MMM` pointer lines** (file lines 108, 129, 188, 196, 204 — the exact set the OQ named; verified by grep). The slice `book/src/spec/slices/cg.md` was reduced cycle-009/cycle-010 to a 165-line stub holding ONLY the L4-v0.5 first-iteration-unrolling material; every `cg.md` range these pointers cite (`341-349`, `341-362`, `103-115`, `172-188`, `393-425`, `208-220`, `430-446`) is beyond line 165 and no longer resolves. This is the **sibling sweep** of cycle-014's L4>L3-theme re-anchor (`reports/2026-05-28T193413Z-lifter-krylov-step-typed-wrapper-dissolution-cg-md-citation-sweep/CYCLE.md`), flagged there in Open Questions and routed to this dispatch's OQ. Pure citation re-anchor: structure stays, no claim changes, status stays `firm`. Applies cycle-014's lifted-evidence convention verbatim — keep the historical `cg.md` range as a parenthetical provenance note, point at the firm home the content was lifted into, name the live `arnoldi_step.md` co-anchor where one exists.

The 5 pointer lines split into three semantic families, each with a distinct firm home:

- **Outer-loop sequential-obstruction (cycle-002 Claim 1)** — range `cg.md:341-349`. Lines 108, 129. The firm narrative home is THIS entry's own §"Iteration-rotation marker" (line 108) + §"Algebraic laws" non-lift catalogue (line 129) in L3 vocabulary, plus the firm concept page [`concepts/sequential-obstruction`](../../book/src/concepts/sequential-obstruction.md). The live co-anchor `arnoldi_step.md:194-213` (slice is 302 lines, reduced-header but §L3 material retained — non-dangling) is unchanged. The re-anchor swaps the dangling `cg.md:341-349` slice pointer for the live `arnoldi_step.md` anchor + the firm concept page, retaining the historical range as provenance.
- **Body-identity evidence registry (cycle-002 Claim 2)** — range `cg.md:341-362`. Line 188. Lifted into firm [`L3-L2/krylov-step-body-identity`](../../book/src/L3-L2/krylov-step-body-identity.md) §Verified-against (line 125), which preserves `cg.md:341-362` with the verbatim Claim-2 quote (claim text at original slice line 360).
- **Step-body + outer-driver slice instances (the "Five Phase-1 slice instances" + "Outer-driver consumer sites" registries)** — ranges `cg.md:103-115`, `:172-188`, `:393-425` (line 196; CG L2 / L4 / L4-v0.5 step bodies) and `cg.md:208-220`, `:430-446` (line 204; CG `cg_solve` calling `iterate_while` / `iterate_while_with_prev`). Lifted into firm [`L2/krylov-step`](../../book/src/L2/krylov-step.md) (§Evidence line 138 step bodies, line 146 outer-driver consumer sites) — the **terminal firm home** carrying the source ranges. The firm [`L4/krylov-step`](../../book/src/L4/krylov-step.md) restates the L4 Form A/B step bodies (§Evidence lines 170-171) but cites them transitively via the L2 entry and carries no `cg_solve`-driver source citation of its own, so the re-anchors point at L2 (not L4) as the terminal anchor and note L4 as a transitive restatement. The CG L4-v0.5 first-iteration-unrolling form (`cg_first_step`/`cg_steady_step` split + the `cg_solve` driver) IS the unique material retained live in the reduced `cg.md` stub (stub lines 27-141 for the bodies; stub lines 86-106 for `cg_solve`), so a short live pointer at the stub's retained section is added alongside the firm L2 home.

`arnoldi_step.md` co-anchors (`:178-213`, `:185-188`, `:194-213`, `:99-105`, `:285-298`) are **all live** (302-line slice, §L1/L2/L3/L4 material retained) and are NOT touched. The L0 anchors at lines 190-192 (`iterative.cpp`, `iterative.hpp`, `ksp.cpp`) and the L4/L2/L3-L2 firm-entry pointers at lines 184-187 are live `book/`/`reference/` pointers and are NOT touched.

## Proposed changes

All re-anchors apply the cycle-014 convention verbatim: the historical `cg.md` range is retained as a parenthetical lifted-evidence provenance note, immediately followed by the firm home it was lifted into and the live `arnoldi_step.md` co-anchor where one exists.

### Re-anchor 1 — §"Iteration-rotation marker" (line 108): `cg.md:341-349` (Claim 1, outer-loop obstruction)

```edit:book/src/L3/krylov-step.md
[old]: L3 is the iteration-rotation layer, and `krylov-step`'s iteration view is the relationship between successive carries `(K, s) -> (K', s')`. **The body itself lifts as a whole-tensor operation** — every primitive in the let-chain is L3-native by signature shape. **The outer loop that folds this body does not lift** — the trajectory of carries `(K_0, s_0), (K_1, s_1), …, (K_n, s_n)` is intrinsically sequential because each step reads scalars (`α`, `β`, `ρ`, `ω`, `θ`) produced by the previous step, and the scalar dependence is not closed-form in the carry. The outer-loop sequentiality is the **outer-loop `sequential-obstruction`** documented at `book/src/spec/slices/cg.md:341-349` and `book/src/spec/slices/arnoldi_step.md:194-213`; it is a property of the surrounding `iterate_while_L3` tail-recursion, not of the `krylov-step` body itself. This entry's body is identity-in-form to the L2 body precisely because all sequentiality is pushed into the surrounding loop or below into the orthogonalization-MGS sub-primitive.
[new]: L3 is the iteration-rotation layer, and `krylov-step`'s iteration view is the relationship between successive carries `(K, s) -> (K', s')`. **The body itself lifts as a whole-tensor operation** — every primitive in the let-chain is L3-native by signature shape. **The outer loop that folds this body does not lift** — the trajectory of carries `(K_0, s_0), (K_1, s_1), …, (K_n, s_n)` is intrinsically sequential because each step reads scalars (`α`, `β`, `ρ`, `ω`, `θ`) produced by the previous step, and the scalar dependence is not closed-form in the carry. The outer-loop sequentiality is the **outer-loop `sequential-obstruction`** documented at the firm [`sequential-obstruction`](../concepts/sequential-obstruction.md) concept page and corroborated by the live anchor `arnoldi_step.md:194-213` (the original CG evidence at `cg.md:341-349` was lifted into this §"Iteration-rotation marker" + the §"Algebraic laws" non-lift catalogue per the cycle-009 corpus reduction); it is a property of the surrounding `iterate_while_L3` tail-recursion, not of the `krylov-step` body itself. This entry's body is identity-in-form to the L2 body precisely because all sequentiality is pushed into the surrounding loop or below into the orthogonalization-MGS sub-primitive.
```

### Re-anchor 2 — §"Algebraic laws", non-lift catalogue, outer-loop-lift item (line 129): `cg.md:341-349` (Claim 1)

```edit:book/src/L3/krylov-step.md
[old]: - **Outer-loop lift to a single tensor-field op**. The trajectory of carries `(K_0, s_0), …, (K_n, s_n)` does not lift to a closed-form whole-tensor operation in `n` steps, because each step's scalar update depends on the previous step's iterate-bundle through inner products that are not closed-form in the carry. This is the outer-loop `sequential-obstruction` of L3 — the body is L3-native (whole-tensor primitives), but the fold over the body is not. Documented at `book/src/spec/slices/cg.md:341-349` and `book/src/spec/slices/arnoldi_step.md:194-213`. The L3 form names the loop tail-recursively; it does not claim the trajectory lifts.
[new]: - **Outer-loop lift to a single tensor-field op**. The trajectory of carries `(K_0, s_0), …, (K_n, s_n)` does not lift to a closed-form whole-tensor operation in `n` steps, because each step's scalar update depends on the previous step's iterate-bundle through inner products that are not closed-form in the carry. This is the outer-loop `sequential-obstruction` of L3 — the body is L3-native (whole-tensor primitives), but the fold over the body is not. Documented at the firm [`sequential-obstruction`](../concepts/sequential-obstruction.md) concept page and the live anchor `arnoldi_step.md:194-213` (the original CG evidence at `cg.md:341-349` was lifted into this non-lift catalogue + §"Iteration-rotation marker" per the cycle-009 corpus reduction). The L3 form names the loop tail-recursively; it does not claim the trajectory lifts.
```

### Re-anchor 3 — §"Evidence", body-identity registry item (line 188): `cg.md:341-362` (Claim 2)

```edit:book/src/L3/krylov-step.md
[old]: - `book/src/spec/slices/cg.md:341-362` — the cycle-002 combinator-miner identity-in-form claim (Claim 2: "step body lifts as identity"). The L2 primitive vocabulary is L3-native by signature shape; this is the upstream evidence for both the L4>L3 wrapper-dissolution audit and the L3>L2 body-identity ratification.
[new]: - `book/src/L3-L2/krylov-step-body-identity.md` §Verified-against (line 125; lifted from the original `book/src/spec/slices/cg.md:341-362` per the cycle-009 corpus reduction, preserved there with the verbatim Claim-2 quote at original slice line 360) — the cycle-002 combinator-miner identity-in-form claim (Claim 2: "step body lifts as identity"). The L2 primitive vocabulary is L3-native by signature shape; this is the upstream evidence for both the L4>L3 wrapper-dissolution audit and the L3>L2 body-identity ratification.
```

### Re-anchor 4 — §"Evidence", "Five Phase-1 slice instances" registry, CG-step-bodies item (line 196): `cg.md:103-115`, `:172-188`, `:393-425`

```edit:book/src/L3/krylov-step.md
[old]: - `book/src/spec/slices/cg.md:103-115`, `:172-188`, `:393-425` (CG L2 / L4 / L4-v0.5 step bodies; the L3 form is the wrapper-dissolved image of the L4 body).
[new]: - CG L2 / L4 / L4-v0.5 step bodies — lifted into firm `book/src/L2/krylov-step.md` §Evidence (line 138; the terminal firm home, carrying all three CG step-body ranges `cg.md:103-115` L2 / `:172-188` L4 Form A / `:393-425` L4-v0.5 Form B) per the cycle-009 corpus reduction. The firm `book/src/L4/krylov-step.md` §Evidence (lines 170-171) restates the L4 Form A / Form B step bodies but cites them transitively via the L2 entry (it carries no terminal source range of its own), so the terminal anchor is L2:138. The L4-v0.5 first-iteration-unrolling form remains the unique live material retained in the reduced slice at `book/src/spec/slices/cg.md:27-141`. (Original pre-reduction ranges: `cg.md:103-115`, `:172-188`, `:393-425`.) The L3 form is the wrapper-dissolved image of the L4 body.
```

### Re-anchor 5 — §"Evidence", "Outer-driver consumer sites at L3" registry, CG-cg_solve item (line 204): `cg.md:208-220`, `:430-446`

```edit:book/src/L3/krylov-step.md
[old]: - `book/src/spec/slices/cg.md:208-220, :430-446` (CG `cg_solve` calling the L4 `iterate_while` / `iterate_while_with_prev`; at L3 these dissolve to the tail-recursive form per the upstream theme).
[new]: - CG `cg_solve` calling the L4 `iterate_while` / `iterate_while_with_prev` — lifted into firm `book/src/L2/krylov-step.md` §Evidence "Outer-driver consumer sites" (line 146; the terminal firm home, carrying both `cg.md:208-220` and `:430-446`) per the cycle-009 corpus reduction. (The firm `book/src/L4/krylov-step.md` carries no `cg_solve`-driver source citation of its own — the L4 entry names the driver pattern via `concepts/solve-monad.md` and inherits its evidence transitively through the L2 entry — so the terminal anchor is L2:146.) The L4-v0.5 `cg_solve` driver (with `iterate_while_with_prev`) remains live in the reduced slice at `book/src/spec/slices/cg.md:86-106`. (Original pre-reduction ranges: `cg.md:208-220`, `:430-446`.) At L3 these dissolve to the tail-recursive form per the upstream theme.
```

## Discipline notes
- **Pure citation re-anchor, no claim/structure/status change.** Every edit swaps a dangling `cg.md:NNN-MMM` reduced-slice pointer for the firm entry the content was lifted into (or, for the outer-loop family whose narrative home is in-file, for the firm concept page + the live `arnoldi_step.md` co-anchor), preserving the historical slice range as parenthetical provenance. No signature, semantics, algebraic-law, variant-axis, or applicability-condition change. The entry's `firm` status is unchanged.
- **Convention inherited verbatim from cycle-014 (and cycle-013 before it).** The lifted-evidence annotation pattern — "the original CG evidence at `cg.md:NNN-MMM` was lifted into [firm entry] per the cycle-009 corpus reduction; `arnoldi_step.md:NNN-MMM` remains the valid live anchor" — is exactly the phrasing the cycle-014 lifter applied to the L4>L3 theme sibling (`reports/2026-05-28T193413Z-lifter-krylov-step-typed-wrapper-dissolution-cg-md-citation-sweep/CYCLE.md`). This dispatch closes the residual that report's Open Questions explicitly routed to OQ `l3-krylov-step-cg-md-citation-sweep`.
- **Outer-loop family (Re-anchors 1/2): narrative home is in-file, not a cross-file lift.** Unlike the body-identity family (which was lifted into a separate firm theme), the outer-loop `sequential-obstruction` claim's narrative home is THIS entry's own §"Iteration-rotation marker" + §"Algebraic laws" non-lift catalogue — the obstruction is stated here in L3 vocabulary. So the re-anchor does not point elsewhere for the *claim*; it swaps the dangling slice *citation* for the firm concept page [`concepts/sequential-obstruction`](../../book/src/concepts/sequential-obstruction.md) (which carries the MGS / Givens-stream worked examples and the L2→L3-lift-failure definition) plus the live `arnoldi_step.md:194-213` anchor. This mirrors how cycle-014's Re-anchor 1/4/8 pointed the *theme's* outer-loop pointers AT this entry's §Algebraic-laws as the authoritative narrative home — the loop closes consistently.
- **`sequential-obstruction.md` link upgraded inline (Re-anchors 1/2; bounded link-hygiene).** Lines 108 and 129 referenced the obstruction as bare prose with a dangling slice citation; the re-anchor renders the concept reference as a proper relative-path markdown link `[\`sequential-obstruction\`](../concepts/sequential-obstruction.md)`, consistent with every other reference to that page in this entry (e.g., the §Context line 24, the §Dependencies line 137). Bounded link-hygiene firm-up incidental to the citation re-anchor, not a content change.
- **Live co-anchors and firm-entry pointers left untouched.** The `arnoldi_step.md` ranges (`:178-213`, `:185-188`, `:194-213`, `:99-105`, `:285-298`) resolve against the live 302-line slice and are not re-anchored. The L0 anchors at lines 190-192 (`iterative.cpp:22-32`, `iterative.hpp:52-55`, `ksp.cpp:296-310`) are live `reference/` pointers; the firm-entry pointers at lines 184-187 (`L4/krylov-step.md`, `L4-L3/...`, `L2/krylov-step.md`, `L3-L2/krylov-step-body-identity.md`) and the `polynomial_recurrence_step.md:119-160` / `gmres.md:459-471` / `chebyshev.md:354-362` slice ranges (lines 197-200; those slices not yet reduced) are out of scope for a `cg.md` sweep and untouched.
- **High→low direction preserved.** The entry remains an L3 operator definition in L3 vocabulary; the re-anchors touch only citation pointers in the §Iteration-rotation-marker / §Algebraic-laws / §Evidence prose, add no reverse-direction (L0→L3 lift) narrative, and do not invert any rewrite direction. Friction-ledger `layer-definition-discipline-high-to-low`.
- **No L0-evidence-driven prose correction performed.** Every claim re-anchored is supported by the firm homes (`L3-L2/krylov-step-body-identity.md:125` preserves the verbatim Claim-2 quote; `concepts/sequential-obstruction.md` carries the obstruction definition; `L4/krylov-step.md` and `L2/krylov-step.md` §Evidence registries carry the step-body and consumer-site instances). No backward convention, drifted citation beyond the dangling-range issue, or contradicting claim was found that would warrant a bounded content correction.

## Supporting evidence
- `book/src/spec/slices/cg.md` (165-line reduced stub; verified `wc -l`) — the reduced slice. Its §"Firm entries that supersede this slice's ... content" (stub lines 5-14) lists `book/src/L2/krylov-step.md` (firm; cycle-005, supersedes §"L2"), `book/src/L3/krylov-step.md` (firm; cycle-010, supersedes §"L3"), `book/src/L3-L2/krylov-step-body-identity.md` (firm; cycle-009), `book/src/L4/krylov-step.md` Form A (firm; cycle-006, supersedes the v0.1-v0.4 typing) as the supersessors. The only live material is the L4-v0.5 first-iteration-unrolling derivation (stub lines 27-141, with `cg_solve` at lines 86-106). All five L3-entry `cg.md` pointer ranges (341-349, 341-362, 103-115, 172-188, 393-425, 208-220, 430-446) exceed line 165 → all dangle.
- `book/src/L3-L2/krylov-step-body-identity.md:125` — firm home for the body-identity evidence (Claim 2), preserving `cg.md:341-362` with the verbatim Claim-2 quote ("*The L2→L3 rotation on the step body is therefore the **identity in form**: no unfolding, no global lift, no schema change.*", at original slice line 360).
- `book/src/concepts/sequential-obstruction.md` — firm concept home for the outer-loop obstruction claim (definition lines 1-16; MGS worked example lines 37-48; Givens-stream replay-prefix lines 83-112). The narrative claim for `krylov-step`'s outer loop lives in-file at `L3/krylov-step.md` §"Iteration-rotation marker" (line 108) + §"Algebraic laws" non-lift catalogue (line 129).
- `book/src/L2/krylov-step.md` §Evidence (line 138: `cg.md:103-115` L2 / `:172-188` L4 Form A / `:393-425` L4-v0.5 Form B step bodies; line 146: `cg.md:208-220`, `:430-446` `cg_solve` outer-driver consumer sites) — the **terminal firm home** carrying the source ranges for both the CG step bodies (Re-anchor 4) and the outer-driver consumer sites (Re-anchor 5). NOTE: this firm entry ITSELF still carries dangling `cg.md` pointers — flagged in Open Questions as the `l2-krylov-step-cg-md-citation-sweep` sibling sweep.
- `book/src/L4/krylov-step.md` §Evidence (lines 169-176) — restates the L4 Form A / Form B step bodies (lines 170-171) but cites them **transitively via the L2 entry, not re-anchored there** (the section's own caveat: "Palace's C++ source does not realise the L4 form. All L0 evidence is transitive through the L2 entry"), and carries NO `cg_solve`-driver source citation of its own. So L4 is a transitive restatement, NOT the terminal home for Re-anchors 4/5 — the re-anchors point at L2:138/:146 and note L4 as transitive. Flagged in Open Questions as the `l4-krylov-step-cg-md-citation-sweep` sibling sweep.
- `book/src/spec/slices/arnoldi_step.md` (302 lines; live, reduced-header but §L1/L2/L3/L4 retained) — the co-anchor `arnoldi_step.md:194-213` (and `:178-213`/`:185-188`/`:99-105`/`:285-298`) ranges remain valid; not re-anchored.
- `reports/2026-05-28T193413Z-lifter-krylov-step-typed-wrapper-dissolution-cg-md-citation-sweep/CYCLE.md` (cycle-014 sibling) — established the lifted-evidence annotation convention this sweep extends; its Open Questions explicitly routed this `L3/krylov-step.md` residual to OQ `l3-krylov-step-cg-md-citation-sweep`.

## Open questions / caveats
- **`L4/krylov-step.md` and `L2/krylov-step.md` carry the SAME dangling `cg.md` pointers — OUT OF SCOPE; needs further sibling sweeps.** While locating the firm homes for Re-anchors 4/5, I found that the firm L4 entry `book/src/L4/krylov-step.md` still cites the now-dangling reduced-slice ranges at its lines 14 (`cg.md:352-362`), 82 (`cg.md:393-425`), 96 (`cg.md:325-339`), 133 (`cg.md:352-362`), 150 (`cg.md:172-188`, `:393-425`), 170 (`cg.md:172-188`), 171 (`cg.md:393-425`); and the firm L2 entry `book/src/L2/krylov-step.md` still cites them at lines 7 (`cg.md:103-115`, `:172-188`, `:393-425`), 9 (`cg.md:341-349`), 67 (`cg.md:288`), 69 (`cg.md:172-188`, `:393-425`), 77 (`cg.md:325-339`), 79 (`cg.md:103-115`), 81 (`cg.md:103-115`), 116 (`cg.md:228-257`), 119 (`cg.md:172-188`, `:393-425`), 138 (`cg.md:103-115`, `:172-188`, `:393-425`), 146 (`cg.md:208-220`, `:430-446`), 172 (`cg.md:288`). These dangle for the same cycle-009-reduction reason, but are in DIFFERENT files (an L4 operator entry and an L2 operator entry, not this L3 entry) — re-anchoring them is a separate lifter dispatch each. Flagging here rather than touching out-of-scope `book/`. **Recommend two follow-up OQs / cycle-015+-or-016 lifter scopes**: `l4-krylov-step-cg-md-citation-sweep` and `l2-krylov-step-cg-md-citation-sweep`. (Caveat: my Re-anchors 4/5 point this L3 entry's pointers AT the **L2** §Evidence registry as the terminal firm home — L2:138 for the step bodies, L2:146 for the consumer sites — because L2 carries the actual source ranges; the L4 §Evidence restatement is transitive-via-L2 and carries no `cg_solve`-driver citation, so it is named only as a transitive restatement, not as a terminal anchor. The L2 target's own internal `cg.md` pointers dangling does not invalidate it as the authoritative narrative home for the step-body and consumer-site claims — the content lives there in L2 vocabulary regardless of its own citation hygiene — but the `l2-krylov-step-cg-md-citation-sweep` sibling sweep should follow to close the chain fully, exactly as cycle-014 routed this very dispatch.)
- **No abstractor reread needed.** The firm homes (`L3-L2/krylov-step-body-identity.md`, `concepts/sequential-obstruction.md`, `L4/krylov-step.md`, `L2/krylov-step.md`) do not contradict any signature, semantics, algebraic law, variant-axis profile, or applicability condition the L3 entry asserts. This was a pure vocabulary/citation re-anchor; the entry's `firm` status is correct and unchanged.
- **OQ `l3-krylov-step-cg-md-citation-sweep` closure.** This dispatch addresses that OQ in full for `book/src/L3/krylov-step.md` (all 5 dangling pointer lines re-anchored). Integrator may mark it `answered` with answer-link this CYCLE.md, contingent on the 5 re-anchors landing. The L4/L2 sibling sweeps flagged above are the residual; recommend NEW OQs for them rather than holding this one open.
