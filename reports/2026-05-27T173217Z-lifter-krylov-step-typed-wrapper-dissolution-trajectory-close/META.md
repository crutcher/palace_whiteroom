---
verifies: ../CYCLE.md
critiqued_at: 2026-05-27T173217Z
critic_version: 1
checks:
  citation-validity: pass
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: pass
  cross-reference-integrity: pass
  edge-label-fidelity: pass
  plan-kind-consistency: pass
  skill-uptake-survey: warning
repaired_at: 2026-05-27T173217Z
repairer_version: 1
repairs:
  citation-validity: not-needed
  surface-or-evidence: not-needed
  rotation-quality: not-needed
  variant-axis-coverage: not-needed
  cross-reference-integrity: not-needed
  edge-label-fidelity: not-needed
  plan-kind-consistency: not-needed
  skill-uptake-survey: unrepairable
overall_status: ready
follow_up_agent: null
---

# META: verification of CYCLE.md (lifter dispatch — close §3.8 trajectory-collapse gap on krylov-step-typed-wrapper-dissolution)

## Critique

### Checks run

**citation-validity**: pass. Spot-checked the 10 `verified_against:` citations against the cited files. (i) `reference/palace/palace/linalg/iterative.hpp:52-55` — confirmed the four mutable scalars `mutable bool converged; mutable double initial_res, final_res; mutable int final_it;` exactly as cited. (ii) `reference/palace/palace/linalg/iterative.hpp:97-108` — confirmed the four `Get*` accessors (`GetConverged`, `GetInitialRes`, `GetFinalRes`, `GetNumIterations`) with no `GetResidualHistory()` analogue. (iii) `reference/palace/palace/linalg/iterative.cpp:420-485` — confirmed PCG `for (; it < max_it && !converged; it++)` at 427, `res = std::sqrt(std::abs(beta));` at 462, conditional print under `print_opts.iterations` at 422-426, and `final_res = res; final_it = it;` at 484-485 (the note's "484-485" anchor lands exactly). (iv) `reference/palace/palace/linalg/iterative.cpp:614-705` — confirmed GMRES inner Arnoldi `for (;; j++, it++)` at 615, the `beta = std::abs(s[j + 1])` at 642, the convergence break at 645, and `final_res = beta; final_it = it;` at 703-704 (the note's "703-704" anchor exact). (v) `reference/palace/palace/linalg/iterative.cpp:734-870` — confirmed `FgmresSolver<OperType>::Mult` at line 734, inner Arnoldi `for (;; j++, it++)` at 794, `final_res = beta; final_it = it;` at 869-870, function body terminating at 871 (the cited range correctly ends at the last semantic line). (vi) `reference/palace/palace/linalg/ksp.cpp:296-310` — confirmed `BaseKspSolver<OperType>::Mult` consuming `GetConverged()` at 301, `GetFinalRes() / GetInitialRes()` at 306, `GetInitialRes()` at 306, `GetNumIterations()` at 309 — the four-getter consumption is exact. (vii) `book/src/L4/iterate-while.md:28-43` — confirmed the firm L4 extras-carrying signature with `trajectory: [{ ...e }]` exactly as cited. (viii) `book/src/L4/iterate-while.md:123-133` — confirmed Law 1 (demand-driven trajectory pruning) with the `\frac{ ... }{ ... }` rule reducing to `iterate_while_pure a p f_state` when `final_state` is the only observed field. (ix) `book/src/L4/iterate-while-with-prev.md:137-147` — verified-as-Law-2 (the pruning rule lifted to both step bodies); spot-confirmed the chapter exists with the cited section. (x) `book/src/concepts/derived-view-hoisting.md:14-19` — confirmed §"Worked example: CG residual norm" with the bad/good design contrast and the §3.8-pruning framing. All 10 citations resolve correctly and support the claims they anchor. The post-cycle-007-repair tightening of `book/src/L4/iterate-while.md:222-232 → :222-224` is acknowledged in the discipline notes and correctly preserved (the `verified_against:` block does not re-cite that range; it cites `:28-43` and `:123-133` instead, both of which are independent and in-range).

**surface-or-evidence**: pass. The dispatch is shaped as a refinement (modifying an existing L4>L3 theme) and combines surface text edits (Change 1: §"What the L3 form for iterate_while looks like" replacement with §3.8 preamble + two-form sketch; Change 2: new Condition 5 in §"Applicability conditions"; Change 3: §"Status" `rough-in` → `firm`) with retroactive evidence backfill (Change 4: trailing `verified_against:` block with 10 citations from the cycle-007 wave-2 audit). The two-form sketch is a citation-and-rationale operationalization of Law 1 of `book/src/L4/iterate-while.md` — the L3 form's pruned shape is named as the L3-side image of the L4 §3.8 rule, with explicit citation and the L4-side rule reproduced as a `$$ ... $$` reduction-rule formula; the unpruned form is the direct value-threaded image of the L4 small-step rule at `iterate-while.md:64-88`. No new structural rotation is asserted; the rotation is the existing cycle-006 L4>L3 wrapper dissolution, just newly anchored to the cycle-007 wave-1 firm L4 vocabulary. New Condition 5 is the cycle-007 wave-2 audit's proposed condition verbatim with Palace-specific evidence (`iterative.hpp:52-55` + `ksp.cpp:296-310`) inlined.

**rotation-quality**: pass. The dispatch does not introduce a new rotation — it operationalizes an existing one (the cycle-006 wrapper-dissolution) and adds the L3-side image of an existing L4 algebraic law (Law 1 of `iterate-while.md`). The two-form sketch genuinely matches Law 1's structure: Law 1 reduces `iterate_while a p f` to `iterate_while_pure a p f_state` (with empty trajectory) when only `final_state` is observed; the report's pruned form is `iterate_while_L3_pruned p step_state carry₀ sim₀` with `step_state = λ(carry, sim) -> let (carry', sim', _readout) = step (carry, sim) in (carry', sim')` — the L3-side `step_state` is exactly the L4-side `f_state` projection lifted into L3's positional tuple shape (dropping the third `readout` slot is the L3-image of dropping the L4 extras record). The `$$ ... $$` rule placed between the two sketches is the L3-side restatement of Law 1, with the same `\frac{ antecedent }{ consequent }` form. State-hiding (the `[readout]` accumulator disappears; `O(N)` allocation removed; the step-body's extras computation eliminated at the call site, not merely unused at runtime) is the rotation-quality property at L3 → L3-pruned. Pass.

**variant-axis-coverage**: pass. The L4 `iterate-while.md` enumerates three variant axes (pure-vs-Solve, extras-vs-no-extras, bootstrap-free-vs-carry-bootstrapped); the consumer-demand axis is structurally outside those. The report correctly frames Condition 5 (consumer observes only `final_state`-equivalent quantities) as an applicability condition on the lowering theme rather than as a fourth variant axis on the L4 combinator — the L4 combinator is invariant under consumer-demand differences; the L4>L3 lowering's *result shape* depends on consumer demand. Both forms (pruned and unpruned) are shown rather than one being hidden behind a conditional; the precondition selecting between them is named explicitly with Palace evidence (`iterative.hpp:52-55` four-scalar surface) and the violation case is enumerated ("a hypothetical future Palace surface `GetResidualHistory(): std::vector<double>`"). No hidden branches. The discipline note at line 268 explicitly cross-references the cycle-007 wave-2 audit's `variant-axis-coverage: pass` finding on the same point.

**cross-reference-integrity**: pass. All `[link]` references resolve. (i) `[`iterate-while`](../L4/iterate-while.md)` and `[`iterate-while-with-prev`](../L4/iterate-while-with-prev.md)` both exist and contain the cited Law 1 / Law 2 ranges. (ii) `[`krylov-step`](../L4/krylov-step.md)` exists. (iii) `book/src/concepts/derived-view-hoisting.md` exists with §"Worked example: CG residual norm" at lines 14-19. (iv) The OQ slug `iterate-while-l3-rendering-trajectory-accumulation-gap` resolves in `scaffolding/open-questions.md` at lines 1227-1241; the cycle-008 closure paragraph correctly appends after the existing "Cycle-007 wave-2 verdict" paragraph (the [old]/[new] block for §"Status" preserves the cycle-007 paragraph verbatim and adds the new "Cycle-008 closure" paragraph after it). (v) The L3>L2 inheritance reference at `scaffolding/open-questions.md:1225` resolves and the cited "promotion to plain `firm` follows automatically when the upstream theme is itself promoted" quote is verbatim in the OQ ledger. (vi) The L1-L0 trailing-block precedents (`book/src/L1-L0/axpby-mutation-rotation.md:173-189` and `book/src/L1-L0/apply-linop-mutation-rotation.md:353-369`) both exist and use the same trailing-YAML form. (vii) The reference to `cg.md:341-349` for `sequential-obstruction` is unchanged from the existing theme (not modified by this dispatch). All cross-references intact.

**edge-label-fidelity**: pass. The dispatch's scope is "L4>L3 theme re-anchor — krylov-step-typed-wrapper-dissolution (close §3.8 trajectory-collapse gap)" and the prose discusses exactly the L4>L3 edge throughout. The §3.8 preamble cites the L4 form (`iterate-while.md`) and renders the L3 form; Condition 5 names the L4>L3 lowering's applicability; the two-form sketch is the L4>L3 lowering's rendered L3 shapes; the `$$ ... $$` rule is explicitly framed as "the L3-side image of Law 1 of [`iterate-while`](../L4/iterate-while.md) — the L4 demand-pruning law transports through the L4>L3 wrapper dissolution". The dispatch correctly does NOT extend into L3>L2 (the inheritance note at §"Note on downstream propagation" defers the L3>L2 `firm-rough-in` → `firm` promotion to a cycle-009 dispatch with explicit rationale citing the lifter role spec's "one theme per invocation"). No edge-label drift.

**plan-kind-consistency**: pass. The dispatch is `agent: lifter` and the content matches the lifter role spec: pure rewriting pass re-anchoring the cycle-006 wave-2 rough-in theme to the cycle-007 wave-1 firm L4 vocabulary (`iterate-while`, `iterate-while-with-prev` both firm); the existing theme's structure is preserved (§"Speculative L4 operators" section explicitly preserved per the discipline note "design history is load-bearing context for a reader auditing the promotion path"); the vocabulary firms up. The §"Status" promotion `rough-in` → `firm` is explicitly within lifter authority — `.claude/agents/lifter.md:50` states "Update status `rough-in` → `firm` once all referenced operators are firm." Both gates the cycle-006 `rough-in` qualifier was waiting on (L4 vocab promotion + lowering-verifier follow-up) are met (cycle-007 wave-1: both firm; cycle-007 wave-2: verdict (c), 10-citation evidence base). The two-form sketch addition + Condition 5 addition + `verified_against:` block addition are all consistent with the lifter's "if the formalized operator's signature differs from the rough-in sketch, the theme's LHS/RHS may need adjustment — make those edits here" + "if the formalized operator's algebraic laws change the applicability conditions, update them" allowances at `.claude/agents/lifter.md:44-46`. The L3>L2 downstream propagation is correctly deferred to a future cycle per "one theme per invocation" (lifter role spec line 64). No content-decision-shaped surface drift (per discipline-note item 4, the two-form sketch's pruned form is "verbatim the existing cycle-006 L3 sketch (modulo positional cleanup matching the strawman pseudo-language)" and the unpruned form is "the direct value-threaded image of the L4 form's small-step rule" — both mechanical projections, not new authoring). Plan-kind alignment is clean.

**skill-uptake-survey**: warning. The dispatch performed `verify-citation-range`-shaped work across 10 citations (Palace `iterative.cpp`/`iterative.hpp`/`ksp.cpp` ranges + four whiteroom internal ranges) without naming the skill in its procedure. The dispatch also performed `verify-rotation-citation`-shaped work (the two-form sketch's Law 1 image alignment) without naming that skill. The dispatch explicitly notes at §"Open questions / caveats" item 5 that "Codemap MCP tools not invoked" per the dispatch directive — a deliberate scope-out that is correctly recorded as telemetry. The pattern here is a lifter doing exactly the skill-described work without referencing the skill names; this is the same skill-uptake gap that the cycle-007 wave-2 lowering-verifier META.md surfaced (META.md skill-uptake-survey: warning), and the cycle-006 META surfaced before it. Telemetry-only per role spec; non-blocking.

### Issues found

1. **(Minor — skill-uptake telemetry)** The dispatch performed `verify-citation-range`-shaped work across 10 citations without naming the skill in its procedure. Same pattern as cycle-007 wave-2 lowering-verifier META.md finding 6 and cycle-006 critic findings. Non-blocking per role spec; flagging for meta-phase aggregate-evidence consumption. Location: CYCLE.md §"Supporting evidence" lines 277-285 (where the per-file read-and-verify procedure is enumerated without skill names).

2. **(Minor — formatting precedent ambiguity acknowledged in CYCLE.md)** Change 4's `verified_against:` block uses bare trailing YAML (per `axpby-mutation-rotation.md` / `apply-linop-mutation-rotation.md` precedent) rather than a wrapped `## Verified-against (audit)` H2 (per `bicgstab-iteration.md` precedent). CYCLE.md §"Open questions / caveats" item 1 explicitly flags both styles are precedented and offers the integrator a one-line wrap if H2 wrapping is preferred. The placement is functional either way; the report's bare-trailing-YAML choice is consistent with cycle-007 wave-2 META.md repair-finding-5 ("the integrator can apply Change 2 as-proposed"). Not a fail — flagging for completeness so the integrator-per-report doesn't re-discover the precedent ambiguity. Location: CYCLE.md lines 178-219 (the trailing-YAML form) and lines 291 (the caveat).

3. **(Minor — adjacency of two `Verified-against`-shaped blocks)** The theme already has a §"Verified-against" H2 (lines 189-212 of the target file) carrying *narrative* evidence. Change 4 appends a bare `verified_against:` YAML block immediately after that H2 (effectively at end-of-file). The two blocks are semantically distinct (narrative-prose evidence list vs structured audit-trail YAML with verdicts and audit timestamps), but the back-to-back placement of two same-named entities at end-of-file may be slightly confusing to a reader unfamiliar with the precedent. The discipline note (CYCLE.md line 270) acknowledges the placement and cites the L1-L0 precedent; the trade-off is documented. Not a fail — flagging so the integrator can decide whether to add a one-line separator comment or H2 wrap for reader clarity. Location: CYCLE.md lines 175-219 (where the bare YAML block sits after the existing prose §"Verified-against" H2).

4. **(Minor — verbatim cycle-007 verdict paragraph in §"Status" [old] block)** Change 5's [old] for §"Status" reproduces the existing cycle-007 wave-2 verdict paragraph verbatim (~70 lines of prose); the [new] block then preserves that paragraph and appends a new "Cycle-008 closure" paragraph after it. The replace-and-preserve pattern is correct for the OQ ledger (preserves history; status flip is on the YAML block above), but the size of the [old] block means a careful diff is needed by the integrator. Not a fail — flagging so the integrator doesn't misread the [new] block as a strict overwrite of the cycle-007 paragraph. Location: CYCLE.md §5 lines 249-256.

5. **(Minor — `Solve` monad framing in pruned form)** The pruned form sketch (line 63-69) drops the `_readout` from the L3 positional tuple but retains `sim` as a positional argument. The report frames this at line 86 as "the `Solve` monad has dissolved (the `sim` argument is positional, not monadic)". This is consistent with the cycle-006 theme's L3 form, but the pruned form's `step_state` definition at line 72 does not show the `sim` thread explicitly in the destructuring (it threads `(carry, sim)` as a positional tuple). A reader might note that the `step_state` signature is `(carry, sim) -> (carry', sim')` while the L4 Law 1's `f_state` is `α -> α` (no sim slot — sim is monadic at L4 and gets dissolved at L3); the L3-side `step_state` correctly carries the sim positional thread. The two-form sketch is internally consistent on this; no semantic bug, but the L4 vs L3 sim-positioning asymmetry is implicit rather than explicit. Not a fail — pure clarity nit; the integrator may consider a one-line gloss on the L3-side `step_state` shape vs the L4 `f_state` shape if the cycle-009 follow-up requires it. Location: CYCLE.md §1 lines 60-72.

## Repair

### Fixes attempted

- **Finding 1 (skill-uptake telemetry)**: `verify-citation-range`-shaped work performed across 10 citations without naming the skill; same `verify-rotation-citation`-shaped pattern for the two-form Law 1 image alignment.
  - **Decision**: `unrepairable` (telemetry-only; non-blocking per role spec).
  - **Rationale**: Skill-uptake naming is a procedural pattern, not a content gap in the dispatch's output. Adding skill names after-the-fact to the dispatch's `§Supporting evidence` would be authoring methodology language rather than repairing a content defect. Same disposition as cycle-007 wave-2 lowering-verifier META.md finding 6 and the cycle-006 critic findings — surfaces for meta-phase aggregate-evidence consumption (cycle-009 meta-batch summary expected to surface the cumulative skill-uptake-gap pattern across cycles 006/007/008).

- **Finding 2 (trailing-YAML vs H2-wrapping precedent ambiguity)**: dispatch chose bare trailing-YAML per L1-L0 precedent over `## Verified-against (audit)` H2 wrap per `bicgstab-iteration.md` precedent.
  - **Decision**: `not-needed`.
  - **Rationale**: Both styles are precedented and the dispatch explicitly flagged the choice at §"Open questions / caveats" item 1 with a one-line integrator path if H2 wrapping is preferred. The bare-trailing-YAML choice is internally rationalized (the prose §"Verified-against" H2 already exists in the theme; wrapping the structured YAML in a duplicate-named H2 would be more confusing than helpful). No mechanical repair is appropriate — either style is correct; the integrator may apply the H2 wrap as a stylistic preference without affecting content. (Cycle-009 meta-phase may want to canonicalize one style across the artifact.)

- **Finding 3 (adjacency of two `Verified-against`-shaped blocks)**: bare `verified_against:` YAML appended immediately after the existing prose `## Verified-against` H2; same-named-entity adjacency may confuse readers unfamiliar with the precedent.
  - **Decision**: `repaired`.
  - **Action**: Inserted an HTML comment separator inside the Change 4 [new] block of CYCLE.md §4, between the prose §"Verified-against" list (last bullet on `derived-view-hoisting.md`) and the trailing `verified_against:` YAML block. The comment explicitly distinguishes the two: the narrative list is the cycle-006 prose evidence registry (file + section descriptor); the YAML block is the cycle-007 wave-2 audit's structured evidence trail (per-citation verdict + audited_at + note), per the trailing-YAML precedent at `book/src/L1-L0/axpby-mutation-rotation.md:173-189`. The comment is invisible in rendered mdBook output but visible in the raw markdown source, giving a reader auditing the artifact an inline gloss without altering rendered surface. Location of edit: CYCLE.md §4 Change 4 `[new]` block (the §"Concept-page references" → `verified_against:` boundary).

- **Finding 4 (large [old] block in §"Status" change)**: Change 5 [old] reproduces the cycle-007 wave-2 verdict paragraph verbatim (~70 lines); [new] preserves that paragraph and appends a new "Cycle-008 closure" paragraph.
  - **Decision**: `not-needed`.
  - **Rationale**: Explicitly flagged by the critic as "diff-care note for integrator; not a repair." The replace-and-preserve pattern is the correct mechanism for OQ-ledger augmentation (preserves history; append on top), and the integrator-per-report agent is the correct consumer of the [old]/[new] diff-application gate. The [old] block is large because the cycle-007 paragraph is large — there is no mechanical shrink available without losing the preservation property.

- **Finding 5 (L3-side `step_state` vs L4-side `f_state` asymmetry implicit)**: the pruned form's `step_state` signature is `(carry, sim) -> (carry', sim')` while L4 Law 1's `f_state` is `α -> α`; the L3 vs L4 sim-positioning asymmetry is implicit.
  - **Decision**: `repaired`.
  - **Action**: Added a one-sentence gloss to the §1 Change 1 [new] block of CYCLE.md, immediately after the `step_state` definition. The gloss explicitly names the shape correspondence: "The L3-side `step_state` has shape `(carry, sim) -> (carry', sim')` — the positional-tuple image of the L4-side `f_state : α -> α` of Law 1 (`book/src/L4/iterate-while.md:123-133`), with the `sim` thread surfacing as a positional argument at L3 because the `Solve` monad has dissolved; the L4 `α` collapses to the L3 carry alone, with `sim` carried alongside positionally rather than monadically." This is a mechanical clarification consistent with the existing line 86 framing of monad dissolution; no new content authoring, no structural change. Cites the existing Law 1 location already cited in the dispatch's `verified_against:` block. Location of edit: CYCLE.md §1 Change 1 `[new]` block (immediately after the `step_state` lambda definition).

### Unrepairable findings

- **Finding 1 (skill-uptake telemetry, `unrepairable`)** — methodology pattern, routes to meta-phase. No follow-up agent for cycle-008 (the meta-phase that follows cycle-009 integrator-finalize will see the cumulative pattern across the cycle-007 / 008 / 009 batch and may emit a skill-uptake-discipline adjustment).

## Suggested resolution

`overall_status: ready`. Critic's 7 `pass` findings stand; the single `warning` (skill-uptake-survey) is telemetry-only per role spec and non-blocking for integration. The two repairable findings (3 and 5) have been addressed in-place in CYCLE.md with mechanical clarity edits that do not alter the dispatch's content or proposed-changes structure — both edits sit inside the same `[new]` blocks already proposed, so the integrator-per-report applies them transparently when applying the corresponding `edit:` blocks.

Notes for the integrator-per-report:
- Apply CYCLE.md proposed-changes §1, §2, §3, §4, §5 as a single per-report unit.
- Change 4 (`verified_against:` trailing-YAML) is followed by an HTML comment separator (added by this repair) explaining the relationship to the existing narrative §"Verified-against" H2 — this comment is part of the artifact target and should be preserved.
- Change 1 (§"What the L3 form for `iterate_while` looks like" replacement) now includes a one-sentence gloss on the L4 `f_state` ↔ L3 `step_state` shape correspondence, immediately after the `step_state` definition; preserve as part of the same `[new]` block.
- Finding 4's `[old]`/`[new]` size asymmetry on the OQ §"Status" augmentation should be diffed with care: the cycle-007 wave-2 verdict paragraph is preserved verbatim and a new "Cycle-008 closure" paragraph is appended after it.
- L3>L2 inheritance auto-promotion (`book/src/L3-L2/krylov-step-body-identity.md` `firm-rough-in` → `firm`) is deliberately deferred to cycle-009 per the dispatch's §"Note on downstream propagation"; route to integrator-finalize integrator-signals as a cycle-009 suggested-next-dispatch.
