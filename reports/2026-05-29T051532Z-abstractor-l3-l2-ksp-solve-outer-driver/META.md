---
verifies: ../REPORT.md
critiqued_at: 2026-05-29T053000Z
critic_version: 1
checks:
  citation-validity: warning
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: warning
  cross-reference-integrity: warning
  edge-label-fidelity: pass
  plan-kind-consistency: pass
  skill-uptake-survey: warning
repaired_at: 2026-05-29T055000Z
repairer_version: 1
repairs:
  citation-validity: repaired
  surface-or-evidence: not-needed
  rotation-quality: not-needed
  variant-axis-coverage: repaired
  cross-reference-integrity: repaired
  edge-label-fidelity: not-needed
  plan-kind-consistency: not-needed
  skill-uptake-survey: not-needed
overall_status: ready
follow_up_agent: null
---

# META: verification of "L3>L2 theme sketch — ksp-solve-outer-driver"

## Critique

### Checks run

**citation-validity — warning.** I independently re-verified every L0 pointer via `palace-codemap read_range`. The two flagged corrections are **confirmed correct**: `iterative.cpp:463` is `converged = (res < eps);` (the in-loop CG test — the L3 entry's `:464` is genuinely off-by-one) and `iterative.cpp:563` is `for (; it < max_it; restart++)` (the GMRES restart loop — the L3 entry's `:564` is off-by-one). All other cited lines verify line-exact: `:417` (`eps = std::max(rel_tol * initial_res, abs_tol);`), `:418` (pre-loop `converged`), `:427` (CG loop guard), `:484-485` (`final_res = res; final_it = it;`), `:703-704` (`final_res = beta; final_it = it;`), `iterative.hpp:52-54` (the three result-field members — `:55` is the trailing closing/blank, so `:52-54` is the precise span as the report states), `iterative.hpp:98` (`GetConverged()` gate), `ksp.cpp:297-310` (`BaseKspSolver::Mult` body; `:300` `ksp->Mult`, `:301-306` warning, `:308-309` counters), `ksp.cpp:312-313` (template instantiations), `ksp.cpp:34-56` (factory switch; `:53-55` MINRES/BICGSTAB/DEFAULT labels, `:56` `MFEM_ABORT`). The CG/GMRES/FGMRES `Mult` def-ranges `:361-486` / `:544-705` / `:734-871` are confirmed (def bodies start `:361` / `:544` / `:734`; `:296` is the `template <typename OperType>` prefix, `:297` the def signature — the report's `:297-310` precision-distinction is accurate). The warning (not pass) is for a single citation-integrity defect: the OQ-closure pointer. The report's §Verified-against and §Open-questions name the slug `ksp-solve-l3-l2-theme-pending` as the OQ this theme closes, but **no such slug exists in `scaffolding/open-questions.md`**. The actual matching ledger entry is `l3-l2-ksp-solve-outer-driver-theme-warranted-gated-on-l2-promotion` (referenced at OQ-ledger ~line 410). The report self-flags this as non-blocking ("if the actual ledger slug differs … the integrator should align"), but the named slug is a dangling pointer, not merely a guess to be confirmed.

**surface-or-evidence — pass.** This is a NEW firm theme (surface-creating, not refinement), so the refinement-shape rule is satisfied by construction — the proposal authors `book/src/L3-L2/ksp-solve-outer-driver.md` plus a dep-map row and a SUMMARY register. The non-identity rotation characterization (L3 explicit `iterate_while_L3` tail-recursion + first-class outer-loop `sequential-obstruction` DISSOLVES into L2 `iterate_while (krylov-step op) s_init predicate` by-role wrap, the iteration view ERASED, the obstruction surviving only as the L2 non-laws "Fold-merge / associativity" + "Identity / lift of the fold to a single tensor-field op at L2") is grounded at both endpoints. I cross-checked against the firm L3 entry (`book/src/L3/ksp_solve.md` §"Iteration-rotation marker", §"Lowers to") and against dispatch #2's firm L2 ksp_solve (`reports/2026-05-29T051532Z-harvester-l2-ksp-solve-firm/CYCLE.md`): the L2 form block (`setup K b` ▷ `init_convergence` ▷ `iterate_while … s_init predicate` ▷ `materialise_iterate` ▷ `extract_result`), the two named non-laws, and the §Semantics phase-2 handoff quote ("the explicit-recursion iteration-rotation view, and the `sequential-obstruction` it carries, are the [`L3/ksp_solve`] concern") are all present verbatim in dispatch #2. The obstruction-erasure-to-non-law-shadow narration is faithful to both entries.

**rotation-quality (CRUX) — pass.** The high→low forward narration L3→L2 is sound and the rotation is a genuine abstraction, not a renaming: the L3 explicit-recursion view is *erased* to a role reference at L2 (more abstract / iteration-hidden), and the load-bearing `sequential-obstruction` collapses to its L2-expressible residue (two non-laws). This is state/structure hiding, not 1:1 mapping — it passes the rotation bar. The kernel-identity / driver-non-identity ratification of the cycle-020 critic's "mild tension" is **sound**. I read the sibling precedent `book/src/L3-L2/krylov-step-body-identity.md`: its §"Rewrite shape" surface-adjustment (2) (line 74) states the L3 outer tail-recursion → L2 outer-driver-by-role collapse is "a **wrapper change, not a body change**" and is identity-in-form *on the body*. The report correctly identifies that for `ksp_solve` "the loop IS the operator," so this same surface adjustment is the *whole* content of the hop (hence non-identity), whereas for `krylov-step` it is a wrapper change around an identity body. The two classifications are about **disjoint subjects** — the kernel *body* (line-for-line identity at L3>L2) vs. the *iteration over the body* (the driver loop, the genuine non-identity rotation). There is no contradiction; the resolution is exact and the contrast tables (report §"Ratifying…" and §"Kernel-identity / driver-non-identity contrast") are accurate against the sibling theme. The firm L3 entry's §"Iteration-rotation marker" independently attributes the obstruction to "the surrounding `iterate_while_L3` tail-recursion, not the `krylov-step` body itself" — corroborating the disjoint-subjects framing.

**variant-axis-coverage — warning.** Largely n/a for a theme (no operator-level variant matrix to enumerate), and the report's §"Applicability conditions" appropriately scopes the rotation to the firm five-slice corpus and notes MINRES/BiCGStab are obstruction-only / not implementation targets. The warning is for one descriptive imprecision in applicability-condition 4 (CYCLE.md:140): it describes the L2 driver's six loop-shaping axes as "the L3 five plus the explicit preconditioner-side and convergence-criterion split-outs." That arithmetic does not close (5 + 2 = 7, not 6). The actual relationship (verified against dispatch #2 §"Variant axes" and the L3 entry §"Variant axes"): the L3 form has five axes {krylov-method, element-type, initial-guess-policy, convergence-failure-policy, **restart-shape**}; the L2 form has six {solver-method, element-type, preconditioner-side, convergence-criterion, initial-guess-policy, convergence-failure-policy}. L3's `restart-shape` is **absorbed into** L2's `solver-method` (not carried as a separate L2 axis), and L2 adds `preconditioner-side` + `convergence-criterion`. So it is "L3 four shared + restart-shape-folded-into-solver-method + 2 new," not "L3 five + 2." The miscount is in an aside, not in any load-bearing rotation claim, hence warning not fail.

**cross-reference-integrity (fence guard) — warning.** All `[link]` targets resolve to real files: `L3/ksp_solve.md`, `L2/ksp_solve.md` (currently a `stub` on disk — see below), `L3-L2/krylov-step-body-identity.md`, `L1/ksp_solve.md`, `L2/krylov-step.md`, `L3/krylov-step.md`, `L4/krylov-step.md`, `L4-L3/krylov-step-typed-wrapper-dissolution.md`, `L4/iterate-while.md`, `L4/iterate-while-with-prev.md`, `L1-L0/minres-iteration.md`, `L1-L0/bicgstab-iteration.md`, `L2/index.md`, `L3/index.md`, `concepts/{sequential-obstruction,convergence-test,solve-monad,state-stratification,derived-view-hoisting,solver-as-operator}.md` — all present. **Fence guard: clean.** The three proposed `edit:` blocks are well-formed and the entire firm theme body sits INSIDE the fenced block: block 1 (`ksp-solve-outer-driver.md`) opens at CYCLE.md:49 and closes at :232, with the two inner ` ```text ` form-blocks (74-85, 93-104) properly nested and closed; block 2 (dep-map row, 234-236) and block 3 (SUMMARY, 238-240) are single-line and well-formed. No cycle-019-style fence-truncation. The warning is the not-yet-integrated dependency: the report's RHS is dispatch #2's firm L2 ksp_solve, which is NOT yet on disk — `book/src/L2/ksp_solve.md` is currently a `stub` (claim-free placeholder). The report's live `[L2/ksp_solve]` links resolve safely against the stub file (linkcheck passes), and the report correctly relies on the integrator applying #2 before #3. This is the intended sequencing, so it is not a defect — but the theme makes specific structural claims about the firm L2 form's content (the exact §"Algebraic laws" non-law titles, the §Semantics phase-2 quote, the §Signature body) that are unverifiable against the on-disk stub and exist only in dispatch #2's proposed-changes. I verified those claims against dispatch #2's CYCLE.md and they match exactly; the warning records the ordering dependency for the integrator's attention (if #2 is reworked or rejected, #3's RHS claims would dangle). Reverse-direction ("Lifts to") notes are correctly kept in-line in the L2 entry (dispatch #2 §"Lifts to"), not in this theme chapter — high→low discipline observed.

**edge-label-fidelity — pass.** The edge is L3>L2 throughout: LHS = L3 (`ksp_solve` value-threaded fold), RHS = L2 (`ksp_solve` outer-driver-by-role composition), forward narration L3→L2 ("lowers into"). The dep-map row (CYCLE.md:235) places the entry in the `L3-L2/index.md` theme table with LHS column = L3, RHS column = L2. The prose consistently discusses exactly this edge; no L2>L3 / L4>L3 mislabeling. The §"Abstraction-direction note" (line 150) correctly identifies L3 as the higher-abstraction layer for this edge and L3→L2 as the lowering direction.

**plan-kind-consistency — pass.** Declared kind is a firm L3>L2 theme; content matches. Three proposed-changes: a fully-fenced new `L3-L2/ksp-solve-outer-driver.md` chapter, a dep-map row in `L3-L2/index.md`, and a SUMMARY register line. No rough-in placeholders in firm-marked content; the §Status justification (both endpoints firm, structural + secondary reduction-chain justification, no speculative L3 vocabulary) is consistent with `firm`. The "Speculative L3 operators: None" and "Speculative operators proposed: None" sections are consistent — the theme introduces no new vocabulary, only connects two firm endpoints.

**skill-uptake-survey — warning.** The report's shape (a lowering theme between two firm endpoints, with citation-range verification and a rotation characterization) implies several relevant skills exist — `verify-citation-range`, `verify-rotation-citation`, `propose-rotation`, `classify-variant-axis` — yet the report references none by name. The L0 verification is described as "self-verified … via `palace-codemap` `read_range` this dispatch," which is the correct mechanism but does not cite the `verify-citation-range` skill (whose cycle-012 extension covers exactly the inherited-citation / off-by-one sub-case this report exercises when it corrects the L3 entry's `:464`/`:564`). Pure telemetry surface, non-blocking: skill invocations are not recorded, so uptake cannot be confirmed.

### Issues found

1. **OQ-closure pointer is a dangling slug** — `CYCLE.md` §Verified-against (line 216) and §Open questions (line 259), severity **low**. The report names `ksp-solve-l3-l2-theme-pending` as the OQ this theme closes; no such slug exists in `scaffolding/open-questions.md`. The matching ledger entry is `l3-l2-ksp-solve-outer-driver-theme-warranted-gated-on-l2-promotion`. The report self-flags the uncertainty and defers slug-alignment to the integrator, but the named slug points at nothing.

2. **Variant-axis arithmetic in applicability-condition 4 does not close** — `CYCLE.md:140`, severity **low**. "The L3 five plus the explicit preconditioner-side and convergence-criterion split-outs" implies 5 + 2 = 7 axes, but the L2 form has six. The real relationship: L3's `restart-shape` is absorbed into L2's `solver-method`; L2 = {4 shared with L3} + preconditioner-side + convergence-criterion. Descriptive aside, not a load-bearing claim.

3. **RHS depends on not-yet-integrated dispatch #2** — `CYCLE.md` §"L2 form (RHS)" / §"Rewrite shape" / §Verified-against, severity **low** (informational, sequencing). The firm L2 ksp_solve form the theme reproduces and cites (the §Signature body, the two named §"Algebraic laws" non-laws, the §Semantics phase-2 handoff quote) exists only in dispatch #2's proposed-changes; the on-disk `book/src/L2/ksp_solve.md` is still a `stub`. I confirmed every such claim against dispatch #2's CYCLE.md (exact match, including the corrected `:463`/`:563` lines and the same flagged L3 drift). The integrator must apply #2 before #3 (the report states this); if #2 is reworked/rejected the RHS claims would dangle. Live links resolve against the stub, so the build is safe regardless.

4. **No skill invocations referenced** — `CYCLE.md` (whole report), severity **low** (telemetry). The L0 range-verification and rotation-characterization shape implies `verify-citation-range` (incl. its inherited-citation sub-case) and `verify-rotation-citation` / `propose-rotation`; none are named. Non-blocking.

5. **(Out-of-scope, drive-by) L3 entry carries off-by-one inner citations** — `book/src/L3/ksp_solve.md` (lines 74, 88, 185, 190 cite `:464`; lines 94, 157, 161, 186 cite `:564`), severity **low**, NOT a defect of this report. The report correctly uses the verified `:463`/`:563` and flags the L3 drift in §Open questions for a future lifter/lowering-verifier pass. The L3 entry is firm + integrated (append-only), so this report rightly does not touch it. Independently corroborated by dispatch #2, which flags the same drift. Recorded here so the repairer/integrator can route the correction to the L3 (and any inherited L2) inner point-citations.

## Repair

### Fixes attempted

- **Finding 1 — [cross-reference-integrity] OQ-closure pointer is a dangling slug.** The report named `ksp-solve-l3-l2-theme-pending` (CYCLE.md §Verified-against line 216, §Open questions line 259) as the OQ this theme closes; no such slug exists in `scaffolding/open-questions.md`. The matching ledger entry is `l3-l2-ksp-solve-outer-driver-theme-warranted-gated-on-l2-promotion` (verified at `scaffolding/open-questions.md:356`).
  - **Decision**: repaired.
  - **Action**: CYCLE.md §Verified-against (Open-questions-ledger bullet) — rewrote the dangling slug to the canonical `l3-l2-ksp-solve-outer-driver-theme-warranted-gated-on-l2-promotion`. CYCLE.md §Open questions ("OQ slug for this theme's closure" bullet) — rewrote to point at the canonical slug (with `:356` ledger line), removed the speculative-alternate-slug guess, and noted the repairer correction. This is a trivial cross-reference fix (broken slug → real ledger slug confirmed from context) within repair authority.

- **Finding 2 — [variant-axis] applicability-condition 4 arithmetic does not close.** Condition 4 (CYCLE.md:140) described the L2 six axes as "the L3 five plus the explicit preconditioner-side and convergence-criterion split-outs" (implies 5 + 2 = 7, but L2 has six). The real relationship — verified against dispatch #2 §"Variant axes" and the L3 entry §"Variant axes": L3's `restart-shape` is **absorbed into** L2's `solver-method` (it does not survive as a separate L2 axis), L2 = {4 shared} + preconditioner-side + convergence-criterion.
  - **Decision**: repaired.
  - **Action**: CYCLE.md §Applicability conditions, condition 4 — rewrote the arithmetic/wording to: four shared axes (incl. L3 krylov-method ≡ L2 solver-method) + restart-shape folded into solver-method + two new at L2 (preconditioner-side, convergence-criterion), stated as `L2 six = four-shared + restart-shape-folded-into-solver-method + two-new`, NOT `L3 five + 2`. Axes are clearly enumerable from the prose (both endpoint forms list them), so this is the variant-axis-classification-from-prose mechanical case — within repair authority. No load-bearing rotation claim was touched (the miscount was in an aside).

- **Finding 3 — [cross-reference-integrity] RHS depends on not-yet-integrated dispatch #2.** The firm L2 `ksp_solve` form the theme reproduces/cites exists only in dispatch #2's proposed-changes; the on-disk `book/src/L2/ksp_solve.md` is still a `stub`. The critic verified all reproduced L2 claims match dispatch #2's CYCLE.md exactly.
  - **Decision**: not-needed (no edit). The report's `[L2/ksp_solve]` references are intra-book Markdown links that resolve against the file path regardless of the file's `status:` frontmatter; the critic confirmed linkcheck passes against the on-disk stub, so the build is safe whether or not #2 has integrated. The references are already forward-ref-safe — there is no live link that breaks before #2 integrates. This is a sequencing constraint for the integrator (apply #2 before #3), not a defect; recorded in §Suggested resolution below.

- **Finding 4 — [skill-uptake] no skill invocations referenced.** Pure telemetry surface; non-blocking.
  - **Decision**: not-needed. Skill-uptake is telemetry only and not a content/correctness defect; nothing mechanical to repair.

- **Finding 5 — [drive-by] L3 entry carries off-by-one inner citations (`:464`/`:564`).** Out-of-scope; NOT this report's defect.
  - **Decision**: not-needed (do not edit). The L3 entry (`book/src/L3/ksp_solve.md`) is firm + integrated (append-only); the report correctly uses the verified `:463`/`:563` and flags the L3 drift in §Open questions for a future lifter/lowering-verifier pass. Per dispatch directive, the L3 entry is not touched by this report or this repair.

### Unrepairable findings

None. All findings are either repaired (1, 2), pass from the critic (surface-or-evidence, rotation-quality, edge-label-fidelity, plan-kind-consistency), or not-needed (3 sequencing-note, 4 telemetry, 5 out-of-scope drive-by). No substantive authoring was required for any finding.

## Suggested resolution

`overall_status: ready` — no unrepairable findings; no follow-up agent.

**Integration-ordering constraint (finding 3): apply dispatch #2 before dispatch #3.** This theme's RHS reproduces and cites the firm L2 `ksp_solve` form authored by cycle-021 wave-1 dispatch #2 (`reports/2026-05-29T051532Z-harvester-l2-ksp-solve-firm/`). On disk `book/src/L2/ksp_solve.md` is still a `stub`; dispatch #2 firms it. The integrator MUST apply #2 before this report (#3) so the theme's structural claims about the firm L2 form (the §Signature body, the two named §"Algebraic laws" non-laws "Fold-merge / associativity" + "Identity / lift of the fold to a single tensor-field op at L2", and the §Semantics phase-2 handoff quote) land against the firmed L2 entry rather than the claim-free stub. The critic verified every reproduced L2 claim matches dispatch #2's CYCLE.md exactly; the build is safe regardless of ordering (the live `[L2/ksp_solve]` links resolve against the stub file), but if #2 is reworked or rejected before #3 integrates, this report's RHS claims would dangle and #3 should be re-critiqued against the revised L2 form.

**OQ closure (finding 1):** on integration, close `scaffolding/open-questions.md` slug `l3-l2-ksp-solve-outer-driver-theme-warranted-gated-on-l2-promotion` with answer-link `book/src/L3-L2/ksp-solve-outer-driver.md`.

**Drive-by routing (finding 5):** the off-by-one inner point-citations (`:464`→`:463`, `:564`→`:563`) in the firm `book/src/L3/ksp_solve.md` (and any inherited L2 inner citations) remain open for a future lifter / lowering-verifier pass, as the report's §Open questions notes. Not actionable by this report or this repair (append-only firm entry); already tracked in the OQ ledger by dispatch #2.
