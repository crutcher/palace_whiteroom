---
agent: cycle-planner
invoked_at: 2026-05-31T192956Z
scope: cycle-037 dispatch plan
status: pending
---

# Cycle 037 dispatch plan

cycle-037 is the **FIRST primary cycle of meta-batch-11** (cycles 037/038/039; the batch-10 meta-phase fired AFTER cycle-036 finalize, commit `c318bd1`). This plan honors the batch-10 meta-phase enactments — in particular the **PASTE-INLINE-EVIDENCE** strengthening of the deliverable-presence check (friction-ledger `cycle-planner-stale-priorities-line-recruitment`, status `escalating`, recurrence-6). Every named-artifact-slug dispatch below carries literal pasted command output in the `## Deliverable-presence verification` section — not a bare assertion of compliance.

## Goals selected this cycle

Open meta-batch-11 with the frontier-settled, high-confidence **L3-backfill cohort** that the c036 D2 cross-layer-cross-cutter audit unblocked (it settled the long-standing deferred-audit at `book/src/L3/index.md:38` into a concrete (A)/(B)/(C) classification). The cycle pushes the **diagonal-preconditioner-apply chain to L3**: two identity-in-form L3 backfills (`assemble-diagonal` = chain head; `jacobi-smoother` = chain consumer) plus a `verified_against:` audit firming the per-line L0 evidence of the c034-landed leaf-lowering theme (`reciprocal-elementwise-product-mutation-rotation`) that both L3 entries reference downward. Three non-overlapping dispatches, disjoint write surfaces.

## Deliverable-presence verification

Per the batch-10 PASTE-INLINE-EVIDENCE requirement, the `verify-dispatch-scope-not-already-discharged` skill (four-step + step-5 STOP-PROPOSING consult + step-6 audit-first framing) was run on each candidate. Literal command output below.

### D1 — L3 `assemble-diagonal` (harvester, identity-in-form backfill)
- **Step 1 (file existence):** `ls book/src/L3/assemble-diagonal.md` → `ABSENT: book/src/L3/assemble-diagonal.md` (VERIFIED-OPEN — no L3 file exists). Full L3 dir listing confirms: `apply_linop.md axpby.md axpbypcz.md axpy.md chebyshev.md dot.md eigsolve.md index.md krylov-step.md ksp_solve.md nrm2.md scal.md` — no `assemble-diagonal.md`.
- **Step 2 (maturity / L1-home check):** L1 home exists and is firm. `grep '^## Status' book/src/L1/assemble-diagonal.md` → `91:## Status`; line-after value → `` `firm` — signature is canonical (matches the `AssembleDiagonal(diag)` virtual on both the real and complex operator hierarchies …) ``. Firm L1 home present; L3 deliverable (a new identity-row L3 chapter) is NOT at-or-below the on-disk L3 maturity (which is absent) → not a no-op.
- **Step 3 (OQ-ledger RESOLVED-grep):** the c036 D2 audit OQ `l3-cohort-growth-audit-c036-verdict` (`open-questions.md:880`) is `resolved cycle-036 D2` and NAMES `assemble-diagonal` as a firm (A) identity-in-form L3-backfill candidate — i.e. the resolution is the *go-ahead to author the L3 entry*, not a closure of the L3 entry itself. No OQ marks `book/src/L3/assemble-diagonal.md` as RESOLVED/landed. Open.
- **Step 4 (structural-block check):** no methodology gate blocks an identity-in-form L3 backfill — the CLAUDE.md invariant **"Identity-lowerings still require both L levels"** AFFIRMATIVELY mandates this entry (`krylov-step` L3 backfill, cycle-010, is the codified precedent). Not blocked; mandated.
- **Step 5 (STOP-PROPOSING NEGATIVE LIST):** `assemble-diagonal` is NOT on the (C) list (`lu_solve`, `back_solve`, `ls-update-column`, 4 NLEPS atoms). Clear.
- **Step 6 (audit-first vs reflexive-harvest framing):** `assemble-diagonal` was the c036 D2 origin question and was CONFIRMED (A) identity-in-form by that completed audit. The cohort question is already audit-settled — a reflexive identity-in-form harvest is now the CORRECT framing (the c036 reframe risk does not recur; the audit that the c036 planner skipped has since been performed and landed). **PASS — recruit as harvester identity-in-form backfill.**

### D2 — L3 `jacobi-smoother` (harvester, identity-in-form backfill)
- **Step 1 (file existence):** `ls book/src/L3/jacobi-smoother.md` → `ABSENT: book/src/L3/jacobi-smoother.md` (VERIFIED-OPEN). Confirmed absent in the L3 dir listing above.
- **Step 2 (maturity / L1-home check):** `grep '^## Status' book/src/L1/jacobi-smoother.md` → `374:## Status`; line-after value → `` `firm` — the signature is a direct transcription of the `Mult` member method ``. Firm L1 home present; L3 deliverable not a no-op (L3 absent).
- **Step 3 (OQ-ledger RESOLVED-grep):** same `l3-cohort-growth-audit-c036-verdict` resolution NAMES `jacobi-smoother` as a firm (A) candidate (go-ahead, not closure). No OQ marks the L3 entry landed. Open.
- **Step 4 (structural-block check):** identity-in-form per the c036 audit ("single elementwise_product; trivially identity-in-form"); same "Identity-lowerings still require both L levels" mandate. Not blocked.
- **Step 5 (STOP-PROPOSING):** NOT on the (C) list. Clear.
- **Step 6 (audit-first framing):** audit-settled (A) identity-in-form per c036 D2 — reflexive harvest is the correct framing. **PASS — recruit as harvester identity-in-form backfill.**

### D3 — `verified_against:` audit of `reciprocal-elementwise-product-mutation-rotation` (lowering-verifier)
- **Step 1 (file existence):** `ls book/src/L1-L0/reciprocal-elementwise-product-mutation-rotation.md` → file EXISTS (landed firm c034 D1). This is an audit-class dispatch, so the deliverable is a `verified_against:` block, not a new file.
- **Step 2 (audit-already-present check):** `grep -c '^verified_against:' book/src/L1-L0/reciprocal-elementwise-product-mutation-rotation.md` → `0`. NO `verified_against:` block on disk → the audit deliverable is genuinely ABSENT → not a no-op. (Contrast: the c031/c032/c035 stale-audit picks all had a `verified_against:` block already present; this one does not.)
- **Step 3 (OQ-ledger RESOLVED-grep):** the theme is firm (c034) but carries no `verified_against:` per-line evidence block; no OQ marks a `verified_against:` audit of this theme as RESOLVED. The per-theme provenance OQ `reciprocal-elementwise-product-mr-dead-code-transpose-consumer-branch` (`open-questions.md:494`) is the only related OQ and is `needs-more (informational)` — it is the dead-code caveat pointer, NOT a closure of the per-line audit. Open.
- **Step 4 (structural-block check):** no gate — a `verified_against:` per-line audit on a firm theme is standard lowering-verifier work; the c034 D2 verdict-only audit confirmed the dead-code caveats but did NOT emit a `verified_against:` block on THIS theme (it audited the consumer `jacobi-mutation-rotation` dead-code question). Not blocked.
- **Step 5 (STOP-PROPOSING):** N/A (audit of an existing L1>L0 theme; not an L3 backfill). Clear.
- **Step 6 (audit-first framing):** N/A — this IS an audit dispatch. **PASS — recruit as lowering-verifier `verified_against:` audit.**

**Supporting evidence (not a skip — recruited dispatches all PASS above):**
- L3 identity-row template precedents are firm and present: `book/src/L3/apply_linop.md` (`firm`, 201 lines) + `book/src/L3/krylov-step.md` (`firm`, 225 lines) — both cited as templates in D1/D2 scope.
- `divfree-projector` L3 (a richer (A) candidate, deferred this cycle) has its dependency satisfied: `book/src/L3/ksp_solve.md` EXISTS firm — noted for cycle-038 routing, NOT dispatched now (keeping the opener to the two thinnest identity-in-form backfills + one audit).

## Dispatches

1. **agent:** `harvester`
   **scope:** L3 operator `assemble-diagonal` — author `book/src/L3/assemble-diagonal.md` as an identity-in-form L3 backfill (the iteration-rotation-layer entry for the diagonal-extraction "operator-to-data" primitive). The L3 body is value-thread-isomorphic to the firm L1 form (`book/src/L1/assemble-diagonal.md`, firm, 119 lines); the iteration rotation is identity-in-form (diagonal extraction is a per-row read, no loop-recurrence obstruction). Template the firm L3 `apply_linop` (`book/src/L3/apply_linop.md`) + L3 `krylov-step` (`book/src/L3/krylov-step.md`) identity-row precedents — value-threaded positional signature, the same algebraic laws that hold at L1 inherited unchanged, non-laws catalogued, variant-axis profile inherited. The thin L3>L2 / non-adjacent identity relationship is annotated **in-line** per the CLAUDE.md "Identity rotations across non-adjacent layers are annotated in-line" invariant (do NOT create a non-adjacent lowering directory). Wire into `book/src/SUMMARY.md` + `book/src/L3/index.md` dep-map. **deps:** none. **rationale:** cycle-037 active head #1 — the frontier-settled high-confidence batch-11 opener; head of the diagonal-preconditioner-apply chain; the c036 D2 origin question, confirmed (A) identity-in-form by the completed audit. Plan-tag `l3-cohort-growth-c037-batch-11-opener`. fan-out: Medium (closes the diagonal-extraction chain end-to-end at L3; gates the `jacobi-smoother` L3 entry's lower context).

2. **agent:** `harvester`
   **scope:** L3 operator `jacobi-smoother` — author `book/src/L3/jacobi-smoother.md` as an identity-in-form L3 backfill (the iteration-rotation-layer entry for the Jacobi smoother apply). The L3 body is value-thread-isomorphic to the firm L1 form (`book/src/L1/jacobi-smoother.md`, firm, 548 lines — a single `elementwise_product` of the inverse-diagonal against the residual; trivially identity-in-form per the c036 D2 audit). Same template precedents (`apply_linop` / `krylov-step` L3 identity rows) and same in-line non-adjacent-identity annotation convention. Wire into `book/src/SUMMARY.md` + `book/src/L3/index.md` dep-map. **deps:** none (independent file; D1 and D2 both APPEND distinct dep-map rows to `book/src/L3/index.md` — distinct rows are NOT overlapping per the conflict-tolerance philosophy; see overlap analysis). **rationale:** cycle-037 active head #1 (second (A) pick) — the chain-consumer counterpart to D1; together D1+D2 advance the diagonal-preconditioner-apply chain at L3 in one cycle. fan-out: Medium.

3. **agent:** `lowering-verifier`
   **scope:** `verified_against:` per-line audit of the firm L1>L0 theme `book/src/L1-L0/reciprocal-elementwise-product-mutation-rotation.md` (landed firm c034 D1; `verified_against:` count currently 0 on disk). Audit the theme's per-citation rows against L0 evidence and emit a fenced ` ```yaml verified_against: ``` ` block (per the lowering-verifier channel-format discipline; observe the `verified-against-note-no-leading-quote-of-either-kind` rule). The theme co-houses two L1 leaves (sub-pattern A `reciprocal` receiver self-overwrite; sub-pattern B `elementwise_product` output-arg), citing `palace/linalg/operator.cpp:478-507` / `:545-568` + the dead-code transpose-kernel caveats. **deps:** none. **rationale:** cycle-037 active head #3 — firms the per-line L0 evidence of the diagonal-preconditioner leaf-lowering that D1 (`assemble-diagonal` L3) and D2 (`jacobi-smoother` L3) reference downward; the c034 D2 verdict-only audit confirmed the dead-code caveats but emitted no `verified_against:` block on THIS theme. fan-out: Low-Medium (per-line evidence; firms one firm theme's audit trail; complements the two L3 backfills' lower context).

## Overlap analysis

Pairwise:

- **D1 × D2** — both author a NEW distinct file (`book/src/L3/assemble-diagonal.md` vs `book/src/L3/jacobi-smoother.md`) and both APPEND a distinct dep-map row to `book/src/L3/index.md` + a distinct SUMMARY entry. Per the Discipline rule "two dispatches that append distinct rows to the same dep-map table are NOT overlapping at the operational level → parallel," and the conflict-tolerance philosophy (when in doubt, PARALLEL; distinct-row appends are corrected cheaply by the per-report integrator if they mildly collide). The two operators do not name each other's slug as a primary deliverable (jacobi-smoother L1 *consumes* an inverse-diagonal but the L3 *entries* are independent identity rows). **NOT overlapping → parallel.**
- **D1 × D3** — D1 writes `book/src/L3/assemble-diagonal.md` (+ L3 index/SUMMARY); D3 writes a `verified_against:` block into `book/src/L1-L0/reciprocal-elementwise-product-mutation-rotation.md`. Disjoint files; disjoint layers (L3 vs L1-L0). D3's audited theme is the *lower context* D1's L3 entry references, but D1 does not write to the theme and D3 does not write to L3. **NOT overlapping → parallel.**
- **D2 × D3** — D2 writes `book/src/L3/jacobi-smoother.md` (+ L3 index/SUMMARY); D3 writes the `verified_against:` block into the L1-L0 reciprocal/elementwise-product theme. Disjoint files; disjoint layers. The `jacobi-smoother` L1 home consumes `reciprocal`/`elementwise_product`, but the L3 entry and the L1-L0 theme are distinct artifacts with no shared write region. **NOT overlapping → parallel.**

No pair modifies the same operator entry or rewrites the same theme body. The only shared file is `book/src/L3/index.md` (D1 + D2 distinct-row appends), which is explicitly the non-overlapping case.

## Sequencing schedule

**Single wave (all parallel):**
- Wave 1: D1, D2, D3 — dispatched in parallel.

No forward-reference ordering constraint: neither L3 backfill references the other's slug as a live link (each is an independent identity row), and D3's audit target already exists on disk. The per-report integrator serializes the artifact writes (including the two D1/D2 distinct-row appends to `book/src/L3/index.md`) as a matter of course; any mild collision on that dep-map table is the cheap-to-correct false-parallelization case the conflict-tolerance philosophy accepts. One `integrator-finalize` at cycle end rebuilds the book once and commits.

## Open questions / caveats

- **Deferred (A) backfills for cycle-038+:** 4 of the 6 firm (A) identity-in-form L3-backfill candidates remain open after this cycle — `reciprocal`, `elementwise_product`, `normalize`, `divfree-projector` (the latter's L3 `ksp_solve` dependency is confirmed satisfied on disk). These are the natural cycle-038 opener; each was VERIFIED-OPEN this cycle (all absent under `book/src/L3/`). I kept cycle-037 to two backfills + one audit rather than dispatching all six at once, because (a) the two thinnest chain-coherent picks (`assemble-diagonal` head + `jacobi-smoother` consumer) plus the leaf-lowering audit form a coherent "diagonal-preconditioner-apply chain to L3" cycle, and (b) it leaves dispatch budget headroom and avoids six near-identical wave-mates appending to the same L3 index dep-map in one cycle (a higher false-parallelization collision surface on a single table than the meta-phase's conflict-tolerance philosophy needs me to take on at once).
- **Substantive (B) cohort still queued:** `orthogonalize` L3 (would be the third `partial-obstruction` row), `chebyshev-smoother` L3 (needs a subsumption check against existing L3 `chebyshev` first), `apply_nonlinear_pencil` L3 (fold into a future eigsolve-variant deepening pass, NOT standalone). Longer-horizon; deferred past the (A) cohort per the active head.
- **Track-but-don't-dispatch:** the 2 L1-promotion-gated (A) candidates (`matrix-weighted-norm`, `bilinear-form`) remain L1 `rough-in`; they ride the same L1 promotion cycle when it lands — not dispatchable standalone this cycle.
- **Second `verified_against:` audit candidate available:** `book/src/L1-L0/floquet-correction-mutation-rotation.md` (firm c036; `verified_against:` count 0 on disk) is also a genuinely-open audit target. I chose the reciprocal/elementwise-product theme for D3 instead because it is the leaf-lowering that this cycle's two L3 backfills reference downward (tighter cohort coherence); the floquet theme audit is a clean cycle-038 candidate (trigger-gated OQ `floquet-corrector-addmult-aliasing-applicability-audit` already names a lowering-verifier dispatch on it).
- **Process note for the batch-11 meta-phase (fires after cycle-039):** the cycle-planner is now opus (commit `d3ee5dc`, user directive 2026-05-31, blanket model upgrade) — this enacts the haiku→opus escalation that the batch-10 meta-phase had HELD as an ASK pending recurrence-7 of `cycle-planner-stale-priorities-line-recruitment`. cycle-037 is the first opus-planner cycle and the first observation point for whether the escalating stale-recruitment friction abates under opus + the paste-inline-evidence requirement. No new friction surfaced this cycle that warrants a fresh plan candidate ahead of the batch-11 meta-phase.
