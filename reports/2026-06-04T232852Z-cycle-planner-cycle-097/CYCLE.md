---
agent: cycle-planner
invoked_at: 2026-06-04T232852Z
scope: cycle-097 dispatch plan
status: pending
---

# Cycle 097 dispatch plan

**FIRST primary cycle of meta-batch-31 (cycles 097/098/099; batch-31 meta-phase fires after cycle-099's finalize).** Fresh session post the batch-30 meta-phase (commit `9136f17`) — restart loaded the new `integrator-finalize.md` step-5b (finalize-runs-linters wiring) + `lifter.md` (graded-stack typed-edge+rank bullet).

## Goals selected this cycle

The LEAD is the batch-31 **`graded-stack-p2-slice-deletion-tranche`** (priorities item 1) — the dominant remaining graded-stack phase: absorb each Phase-1 slice's worked-example content into a concept/theme home citing L0 directly, repoint inbound citations off slice anchors, then delete the slice + its `spec/index.md` row + SUMMARY entry, with the reachability-GC `spec/slices/*`-unreachable + rank-invariant-holds completion criterion. **The c096 planner deferred this as too large for one cycle; the c094/c096 planners flagged the krylov trio (`cg`/`gmres`/`arnoldi_step`) as the deeply-interwoven HARD blocker.** My on-disk citation-web audit (evidence below) confirms the campaign splits cleanly into two cohorts by coupling: **Cohort A** (krylov trio — ~30 line-anchors woven through the firm L2/L3/L4 `krylov-step` trio + 4 `L4-L3/` dissolution themes + `L3-L2/krylov-step-body-identity` + `L2-L1/krylov-step-kernel-defusion` + `L2/index` dep-map) is its own multi-dispatch sub-campaign → **DEFER to c098/c099**; **Cohort B** (the 6 independent slices `divfree`/`sparse_triangular_solve`/`cg_preconditioning_framework`/`orthog`/`plane_rotation_stream`/`polynomial_recurrence_step`) is lower-coupling and is c097's target. c097 takes the **byte-disjoint near-deletable subset of Cohort B**: each dispatch owns ONE slice + its single absorption target + its inbound repoints; one dispatch sole-owns the shared `SUMMARY.md` + `spec/index.md` deletions; the krylov trio is untouched. Item 2 (`domain_energy_reduce.md:377` land-clean residue) folds in as a non-conflicting fill dispatch since the batch's lifter-with-graded-stack-bullet now owns that re-anchor class.

## Scoping evidence (the citation-web audit that drives the cohort split)

**Precise slice-path inbound references (the deletion blockers), and line-anchor (load-bearing) cites:**

- **`cg_preconditioning_framework`** — framework material ALREADY absorbed into firm `book/src/L4/preconditioning-framework.md` (c096-D1; on disk `rank: firm`, line 336 states "absorbed from the `cg_preconditioning_framework` slice's §L4/§L4-v0.2/§L4-v0.3, the slice is the [source]"). **ZERO line-anchor (`.md:NN`) cites.** Remaining inbound: 7 concept pages (`build-time-vs-run-time-stratification`, `complex-from-real-lift`, `constructed-operator-factory`, `counter-update`, `finest-level-unwrap`, `solver-as-operator`, `two_operator_split`) + `spec/index.md` + SUMMARY. **→ near-deletable: repoint the 7 concept refs to `L4/preconditioning-framework.md` (or drop the slice link), then delete.**
- **`divfree`** — **ZERO line-anchor cites outside the slice itself.** Inbound: `L1/divfree-projector.md:326` (a single "supersedes its L1 content" pointer to `divfree.md:24-100`), `L1/ksp_solve.md:131,:143` (use-pattern precedent prose), 4 concept pages, dep-map. The firm homes `L1/divfree-projector.md` + `L1/ksp_solve.md` already carry the positive L0 (`divfree.cpp:175`). **→ near-deletable: re-anchor the 3 `L1/` prose pointers to the firm L0 + drop the slice link, then delete.**
- **`sparse_triangular_solve`** — **ZERO line-anchor cites.** A negative-result slice; firm home `L1-L0/triangular-solve-obstruction.md` (on disk `## Status` present, cites the slice at `:277`/`:464`). Inbound: `triangular-solve-obstruction.md`, `concepts/negative-result-slice.md`, `concepts/scope-out-obstruction.md`, `concepts/sequential-obstruction.md:53` (worked-example anchor). **→ needs the 3 unique sparse-trisolve L0 findings absorbed into `triangular-solve-obstruction.md` (the slice's "by construction NO firm L0–L4 entry … annotated-and-retained" header at `:8` is the OLD carve-out the campaign retires) + the concept anchors repointed, then delete.**
- **`orthog`** — `orthogonalize-mutation-rotation.md` (L1>L0 theme) is **already authored + `firm`** on disk (Status `:243` — "every L0 form read in full from `orthog.hpp:18-90`"; the priorities/c094 "un-authored `orthogonalize-mutation-rotation`" note is **STALE**). But the `orthog` slice's §L2/L3/L4 still uniquely hold the L1 invariants + the **MPI-collective-shape disclosure** (MGS: m reductions size 1; CGS: 1 of size m; CGS2: 2 of size m) flagged "NOT in firm entries" + OQ `l1-orthogonalize-promotion-from-arnoldi-step-and-orthog`. Inbound line-anchor: `L1/orthogonalize.md:302` ("`orthog.md:18`"). **→ needs the MPI-collective-shape disclosure absorbed into `concepts/orthogonalization.md` (or `L1/orthogonalize.md`) + the `:302` anchor + `concepts/sequential-obstruction.md:48` repointed, then delete. MORE WORK than divfree/precond — judge as a own-dispatch.**
- **`plane_rotation_stream`** — **NO `.md:NN` slice line-anchor cites in book/src.** The "`:72-108`/`:73-108` off-by-one" in priorities is NOT a slice anchor: it is the Palace **L0** range `iterative.cpp:73-108` (`GeneratePlaneRotation`). **Codemap-confirmed:** `iterative.cpp:72` is the `template <typename T>` line, `:73` is the `inline void GeneratePlaneRotation(...)` signature line. The firm chapters (`L1/ls-update-column.md`, `L2/incremental-least-squares.md`, `L2-L1/incremental-least-squares-composition-lowering.md`, `L1-L0/ls-update-column-mutation-rotation.md`, `concepts/givens.md`) uniformly cite **`:73-108`** (signature-from-body, CANONICAL). The ONLY `:72-108` site is INSIDE the slice itself (`plane_rotation_stream.md:7,:43`), tracked by OQ `plane-rotation-givens-l0-citation-range-reconcile`. **→ reconcile = canonicalize to `:73-108` (the firm form), which dies with the slice; absorb the elided plane-rotation L3 replay-prefix analysis into `concepts/sequential-obstruction.md` §"Worked example" + repoint the 4 givens/plane-rotation concept canonical-instances, then delete.**
- **`polynomial_recurrence_step`** — line-anchor `:119-160` ("catalog of three instances") live at `L2/krylov-step.md:7` AND `L2/index.md:135` (dep-map Consumed-by row). `concepts/negative-result-slice.md:60,:80` names the slice as its canonical worked example but **does NOT carry the 5-axis table itself** (it describes the pattern; the table content still lives only in the slice). **→ needs the polynomial 5-axis difference table + 4-site catalog + falsification criterion absorbed into `concepts/negative-result-slice.md` + the `L2/krylov-step.md:7` and `L2/index.md:135` `:119-160` cites repointed to the firm `L4/chebyshev.md` §Semantics + `negative-result-slice` home, then delete. TOUCHES `L2/krylov-step.md` + `L2/index.md` (Cohort-A-adjacent) — handle carefully or defer.**
- **Cohort A (`cg`/`gmres`/`arnoldi_step`) — DEFERRED:** `arnoldi_step` alone carries live anchors `:99-105`/`:101-108`/`:107-113`/`:129-131`/`:178-213`/`:185-188`/`:194-213`/`:285-298`/`:5` across the firm `L2/krylov-step.md`, `L3/krylov-step.md`, `L4/krylov-step.md`, `L4-L3/krylov-step-typed-wrapper-dissolution.md`, `L4-L3/iterate-while-dissolution.md`, `L4-L3/gmres-inner-loop-iterate-while-migration.md`, `L3-L2/krylov-step-body-identity.md`, `L2-L1/krylov-step-kernel-defusion.md`, `L1/orthogonalize.md`. `cg`/`gmres` similarly. This is the largest repoint and a sub-campaign of its own (≥3-dispatch) — **NOT attempted c097.**

## Deliverable-presence verification (paste-inline-evidence)

All c097 dispatches are slice-deletion absorptions; each names file paths. Checks per the four-step sequence:

**D1 — `cg_preconditioning_framework` absorb+delete.** Target slice + absorb-into-home both on disk; the absorb-into-home is already firm (no re-author).
- `ls`: `book/src/spec/slices/cg_preconditioning_framework.md` exists (51566 bytes, May 28). `book/src/L4/preconditioning-framework.md` exists (20641 bytes, Jun 4).
- Maturity: `L4/preconditioning-framework.md` line 5 = `rank: firm`; line 336 = "absorbed from the `cg_preconditioning_framework` slice's §L4/§L4-v0.2/§L4-v0.3 (the slice is the [source])". The absorption target is FIRM and explicitly states it already absorbed the slice's L4 content → the slice's framework material has a firm home; only the 7 concept repoints + deletion remain. NOT a no-op (deletion + repoint not yet done).
- OQ-grep: `grep 'cg_preconditioning_framework.*RESOLVED' open-questions.md` → no matches (deletion is OPEN).
- Structural block: none — the c096 OQ `l4-preconditioning-framework-promotion` closed CLOSEABLE means the gap is filled; deletion is unblocked. **PASS.**

**D2 — `divfree` absorb+delete.**
- `ls`: `book/src/spec/slices/divfree.md` exists (22551 bytes). `book/src/L1/divfree-projector.md` + `book/src/L1/ksp_solve.md` exist (firm homes).
- Maturity: the inbound `L1/divfree-projector.md:326` is a "supersedes its L1 content" pointer (firm home already supersedes); `L1/ksp_solve.md:131` already carries the positive L0 `divfree.cpp:175`. ZERO line-anchor cites. → repoint-and-delete, no re-author.
- OQ-grep: no `divfree.*RESOLVED` deletion match. Structural block: none. **PASS.**

**D3 — `sparse_triangular_solve` absorb+delete (3 findings → `triangular-solve-obstruction.md`).**
- `ls`: slice exists (13884 bytes, May 29). `book/src/L1-L0/triangular-solve-obstruction.md` exists (cites slice `:277`/`:464`, has `## Status` `:477`).
- Maturity: the obstruction theme is the firm home but the slice's 3 unique L0 findings are NOT yet absorbed into it (slice header `:8` still asserts the OLD `annotated-and-retained` carve-out). → absorb (substantive) before delete. NOT a no-op.
- OQ-grep: no RESOLVED-deletion match. Structural block: the `annotated-and-retained` carve-out at slice `:8` is the EXACT carve-out the graded-stack §6 campaign RETIRES (per priorities completion note) — so it is NOT a block, it is the thing being removed. **PASS.**

**D4 — `plane_rotation_stream` absorb+delete + the `:72-108`/`:73-108` reconcile.**
- `ls`: slice exists (19738 bytes). `book/src/concepts/sequential-obstruction.md` exists (worked-example `:83`).
- Maturity: codemap-confirmed canonical L0 form `:73-108` (`:73` = `GeneratePlaneRotation` signature; `:72` = `template`). The ONLY `:72-108` site is the slice itself (`:7,:43`); all firm chapters use `:73-108`. → canonicalize-to-firm-form (dies with slice) + absorb the L3 replay-prefix analysis. OQ `plane-rotation-givens-l0-citation-range-reconcile` is the tracked open item this closes.
- OQ-grep: the reconcile OQ is OPEN (the off-by-one is unresolved). Structural block: none. **PASS.**

**D5 — single-owner SUMMARY.md + spec/index.md deletion-row owner (deps D1–D4).**
- `ls`: `book/src/SUMMARY.md` (slice rows `:294`/`:295`/`:298`/`:300` for the 4 D1–D4 slices), `book/src/spec/index.md` (slice-status rows). Both exist.
- This dispatch removes ONLY the SUMMARY + spec/index rows for the slices D1–D4 DELETE this cycle; it is the SINGLE owner of both shared files (the parallel-blind-shared-index guard). OPEN by construction (deletion not yet done). **PASS.**

**D6 (fill) — `domain_energy_reduce.md:377` mwn stale-rough-in land-clean (item 2).**
- `ls`: `book/src/L4/domain_energy_reduce.md` exists.
- Maturity: per c096 integrator-signals the `:377` `(rough-in (test-coverage-bounded))` assertion is FALSIFIED by the c091 matrix-weighted-norm firm-flip → should read `(firm, promoted cycle-091)`. A clean cross-file prose re-anchor.
- OQ-grep: OQ `domain_energy_reduce-377-mwn-stale-rough-in-residue` is KEPT-DEFERRED (batch-31 land-clean) per the batch-30 unify — explicitly migrated to batch-31 land-clean → OPEN, trigger fired. Structural block: none (the c091 flip landed). **PASS.**

**NOT proposed this cycle (deferred, with reason):** `polynomial_recurrence_step` absorb (touches `L2/krylov-step.md:7` + `L2/index.md:135` — Cohort-A-adjacent firm krylov files; safer to do WITH the krylov-trio repoint in c098); `orthog` absorb (the MPI-collective-shape disclosure is a substantive absorb into `concepts/orthogonalization.md` — own-dispatch, schedule c098); the `cg`/`gmres`/`arnoldi_step` krylov-trio (the ~30-anchor sub-campaign, c098/c099). **None of the 9 slice slugs are on the STOP-PROPOSING negative list** (checked: `lu_solve`/`back_solve`/`ls-update-column`/4 NLEPS atoms/`L3/solve_family`/`L2/fold_solve`/`L2/fe_assemble`/`map_solve`/`weak_form_term` — disjoint from the slice set).

## Dispatches

1. **agent: `same-layer-cross-cutter`** (audit-first absorb-and-delete; the phase-1-slice-reduction class)
   **scope:** Absorb-and-delete `book/src/spec/slices/cg_preconditioning_framework.md`. Verify (paste-inline) that `book/src/L4/preconditioning-framework.md` (firm, c096-D1) already carries the slice's §L4/§L4-v0.2/§L4-v0.3 framework material; repoint the 7 inbound concept pages (`concepts/build-time-vs-run-time-stratification.md`, `complex-from-real-lift.md`, `constructed-operator-factory.md`, `counter-update.md`, `finest-level-unwrap.md`, `solver-as-operator.md`, `two_operator_split.md`) off the slice link onto `L4/preconditioning-framework.md` (or drop the slice link where it is a bare provenance mention); delete the slice FILE. **Do NOT touch `SUMMARY.md` / `spec/index.md`** (D5 owns those rows). **Do NOT touch any krylov-trio file.** Carries the graded-stack rank bullet: confirm no inbound `depends-on` edge targets the slice (it is reachability-GC detritus) before deleting.
   **deps:** none
   **rationale:** LEAD item 1, the lowest-risk near-deletable slice (its firm absorb-home already landed c096); a clean first deletion validates the absorb→repoint→delete loop. fan-out HIGH (collapses one slice from the detritus mass).

2. **agent: `same-layer-cross-cutter`** (absorb-and-delete)
   **scope:** Absorb-and-delete `book/src/spec/slices/divfree.md`. Verify the firm homes `book/src/L1/divfree-projector.md` + `book/src/L1/ksp_solve.md` already carry the positive L0 (`divfree.cpp:175`, the L1/L2 use-pattern); re-anchor the 3 inbound `L1/` prose pointers (`L1/divfree-projector.md:326` "supersedes its L1 content `divfree.md:24-100`"; `L1/ksp_solve.md:131,:143` slice-precedent prose) onto the firm L0/firm-home form and drop the slice link; repoint the 4 inbound concept pages off the slice link; delete the slice FILE. **Do NOT touch `SUMMARY.md` / `spec/index.md`** (D5). **Do NOT touch krylov-trio files.** Rank bullet: confirm reachability-GC-detritus before delete.
   **deps:** none
   **rationale:** LEAD item 1, zero line-anchor cites — clean repoint-and-delete. fan-out HIGH.

3. **agent: `same-layer-cross-cutter`** (audit-first absorb; the negative-result class)
   **scope:** Absorb-and-delete `book/src/spec/slices/sparse_triangular_solve.md`. Absorb the slice's 3 unique sparse-trisolve L0 findings (the opaque-forwarding catalog + the per-site distinction) INTO the firm `book/src/L1-L0/triangular-solve-obstruction.md` citing the Palace L0 directly (verify-present, paste-inline the L0 ranges); repoint the inbound concept anchors (`concepts/negative-result-slice.md`, `scope-out-obstruction.md`, `sequential-obstruction.md:53`) off the slice; REMOVE the OLD `annotated-and-retained` carve-out framing (slice `:8`) per the graded-stack §6 retirement; delete the slice FILE. **Do NOT touch `SUMMARY.md` / `spec/index.md`** (D5). **Do NOT author the polynomial 5-axis table** (that is the deferred `polynomial_recurrence_step` absorb — different slice). Rank bullet: detritus-confirm before delete.
   **deps:** none
   **rationale:** LEAD item 1; absorbs the negative-result findings into their firm obstruction home + exercises the carve-out retirement the campaign mandates. fan-out HIGH.

4. **agent: `same-layer-cross-cutter`** (absorb + L0-anchor reconcile)
   **scope:** Absorb-and-delete `book/src/spec/slices/plane_rotation_stream.md` + close OQ `plane-rotation-givens-l0-citation-range-reconcile`. RECONCILE FIRST: canonicalize the GeneratePlaneRotation L0 range to the FIRM form `iterative.cpp:73-108` (codemap-confirmed `:73` = signature line, `:72` = `template` line; the firm chapters `concepts/givens.md` / `L1/ls-update-column.md` / `L2/incremental-least-squares.md` all use `:73-108`); the divergent `:72-108` exists ONLY in the slice (`:7,:43`) and dies with it — paste-inline the codemap line + the firm-chapter cites confirming `:73-108` is canonical, then state the OQ resolved-by-deletion. Absorb the elided plane-rotation L3 replay-prefix analysis into `concepts/sequential-obstruction.md` §"Worked example: Givens-stream replay-prefix" (verify-present, cite L0 directly); repoint the 4 inbound concept canonical-instances (`concepts/givens.md:40`, `givens_apply.md:27`, `givens_generate.md:27`, `plane-rotation-stream.md:37`, `sequential-obstruction.md:85`) off the slice; delete the slice FILE. **Do NOT touch `SUMMARY.md` / `spec/index.md`** (D5). **Do NOT touch krylov-trio files.**
   **deps:** none
   **rationale:** LEAD item 1 — includes the priorities-named HARD-blocker reconcile (which turned out to be an L0-anchor off-by-one internal to the slice, NOT a slice-anchor repoint), closing a tracked OQ. fan-out HIGH (clears a HARD blocker the krylov trio also benefits from).

5. **agent: `layer-intro-author`** (single-owner SUMMARY.md + spec/index.md deletion rows)
   **scope:** SOLE owner of `book/src/SUMMARY.md` + `book/src/spec/index.md` slice-row deletions this cycle. Remove the SUMMARY rows for ONLY the slices D1–D4 delete (`SUMMARY.md:294` cg_preconditioning_framework, `:295` divfree, `:300` sparse_triangular_solve, `:298` plane_rotation_stream) + their corresponding `spec/index.md` status rows; leave the 5 still-present slice rows (arnoldi_step, cg, gmres, orthog, polynomial_recurrence_step) intact. Update the `spec/index.md` running count/header prose to reflect 4 deletions. If the first `roadmap_goal` chapter is minted this cycle (none planned — D1–D4 absorb into existing firm homes), add the `## Roadmap goals — unbuilt frontier` SUMMARY grouping; otherwise do NOT (defer per `roadmap-goal-unbuilt-frontier-SUMMARY-grouping-deferred`). Run `cargo make book` mentally / verify no dangling SUMMARY link remains after the 4 deletions (linkcheck2 hard-errors a SUMMARY entry pointing at a deleted file — so the slice files MUST be deleted by D1–D4 BEFORE this row removal lands, AND no surviving non-SUMMARY link may point at the 4 deleted files — D1–D4 own their own repoints).
   **deps:** 1, 2, 3, 4
   **rationale:** the parallel-blind-shared-index guard — `SUMMARY.md` + `spec/index.md` are consolidated shared files; exactly ONE owner removes the deletion rows after the absorb dispatches land their repoints + file deletions. Sequenced AFTER D1–D4 so the slice files are gone and no live inbound link survives (linkcheck2 safety). fan-out MEDIUM (mechanical, but gates a clean build).

6. **agent: `lifter`** (land-clean cross-file re-anchor; item 2)
   **scope:** Re-anchor `book/src/L4/domain_energy_reduce.md:377`'s stale `(rough-in (test-coverage-bounded))` operator-maturity assertion on `matrix-weighted-norm` to `(firm, promoted cycle-091)` (falsified by the c091 mwn firm-flip; OQ `domain_energy_reduce-377-mwn-stale-rough-in-residue`, batch-31 land-clean). Re-read `domain_energy_reduce.md` end-to-end for any OTHER within-file mwn-maturity stale narration (the within-file self-consistency grep discipline) and re-anchor in the same pass. Carries the NEW lifter graded-stack rank bullet: if the re-anchor flips any `rank:`/`edges:` frontmatter, confirm `rank(u) ≤ min over depends-on deps` on disk before landing (no new rank violation; baseline is 0). **Do NOT touch the `L2/index.md:112,121` normalize_B phrasing** (judged mildly-stale-in-framing only; a separate normalize-cohort follow-up — leave for a future cycle). **Do NOT touch any slice file or SUMMARY/spec-index** (D1–D5 own those).
   **deps:** none
   **rationale:** item 2 land-clean residue; trigger fired (batch-30 unify kept-deferred to batch-31 land-clean). Non-conflicting fill — byte-disjoint from all slice dispatches. fan-out LOW (benign maturity-prose tidy). Routed to `lifter` (the cross-file maturity re-anchor + graded-stack rank-bullet owner per the batch-30 enactment).

## Overlap analysis

Pairwise (the dispatch plan IS the overlap reasoning):

- **D1 × D2 × D3 × D4** — each owns a DISJOINT slice file + a DISJOINT absorption target: D1 → `L4/preconditioning-framework.md` (verify-only, already firm) + 7 specific concept pages; D2 → `L1/divfree-projector.md`/`L1/ksp_solve.md` + 4 concept pages; D3 → `L1-L0/triangular-solve-obstruction.md` + 3 concept pages; D4 → `concepts/sequential-obstruction.md` + 5 givens/plane-rotation concept pages. **Concept-page overlap check:** D3 and D4 both touch `concepts/sequential-obstruction.md` — **D3 repoints its `:53` sparse-trisolve anchor; D4 absorbs into the `:83` Givens-stream worked-example + repoints `:85`.** These are anchor-distinct regions of the same file → MILD potential conflict. Per the conflict-tolerance philosophy (when in doubt, PARALLEL; false sequentialization is the worse error), I mark them PARALLEL — they edit different sections (sparse-trisolve example block vs Givens-stream worked-example block) and the integrator's per-report serial apply + merge handling resolves any line-adjacency cleanly; a real conflict surfaces as an `integrator-signals` data point. **No shared OPERATOR names; no shared krylov-trio files.** → PARALLEL (wave 1).
- **D5 × {D1,D2,D3,D4}** — D5 removes SUMMARY + spec/index ROWS for the 4 slices; D1–D4 delete the slice FILES + repoint non-SUMMARY inbound links. **Genuine ordering dependency** (NOT a region conflict): linkcheck2 hard-errors if a SUMMARY entry points at a deleted file, OR if any surviving non-SUMMARY link points at a deleted file. So the file-deletions + repoints (D1–D4) MUST land before D5 removes the SUMMARY rows. → D5 is **SEQUENTIAL after D1–D4** (wave 2). D5 is the SOLE owner of `SUMMARY.md` + `spec/index.md` (single-index-owner guard; D1–D4 are instructed NOT to touch them).
- **D6 × all** — `lifter` on `L4/domain_energy_reduce.md` ONLY. Byte-disjoint from every slice file, every absorption target, and `SUMMARY.md`/`spec/index.md`. `domain_energy_reduce.md` is not referenced by D1–D5. → PARALLEL (wave 1), zero overlap.

**No two dispatches modify the same operator entry or rewrite the same theme body.** The one shared-file region adjacency (D3/D4 on `sequential-obstruction.md` distinct sections) is marked PARALLEL per conflict-tolerance; the one genuine ordering edge (D5 after D1–D4) is sequenced into wave 2.

## Sequencing schedule

- **Wave 1 (parallel):** D1, D2, D3, D4, D6 — the 4 slice absorb-and-deletes + the independent land-clean re-anchor. All byte-disjoint at the file level (modulo the D3/D4 distinct-section adjacency on `sequential-obstruction.md`, marked parallel).
- **Wave 2 (after wave-1 reports land):** D5 — the SUMMARY.md + spec/index.md row removal, sequenced AFTER the slice files are deleted + inbound links repointed (linkcheck2 safety: no live link to a deleted file at build time).

One `integrator-finalize` runs once at cycle end (rebuild book + the new step-5b graded-stack linter run — expect the reachability GC to show 4 fewer `spec/slices/*` nodes in the detritus mass + rank_violations still 0; commit + push). The waves order dispatches by the SUMMARY-deletion-after-file-deletion forward dependency; the book is NOT rebuilt between waves.

## Open questions / caveats

- **The campaign is correctly staged, not completed, this cycle.** c097 deletes 4 of 9 slices (the byte-disjoint near-deletable Cohort-B subset). Remaining for c098/c099: (a) the `polynomial_recurrence_step` absorb (5-axis table → `concepts/negative-result-slice.md` + repoint `L2/krylov-step.md:7` / `L2/index.md:135` — Cohort-A-adjacent, do WITH the krylov repoint); (b) the `orthog` absorb (MPI-collective-shape disclosure → `concepts/orthogonalization.md` — substantive own-dispatch); (c) the **krylov trio `cg`/`gmres`/`arnoldi_step`** — the ~30-anchor sub-campaign across the firm L2/L3/L4 `krylov-step` trio + 4 `L4-L3/` dissolution themes + `L3-L2/krylov-step-body-identity` + `L2-L1/krylov-step-kernel-defusion`. The completion criterion (reachability-GC `spec/slices/*` fully unreachable + rank invariant holds with zero slice nodes) is NOT met until all 9 are gone; the `annotated-and-retained` carve-out + skill `phase-1-slice-reduction-audit` retirement is **deferred to campaign completion** (likely the batch-31 meta-phase after c099), NOT this cycle.
- **Stale priorities note corrected:** priorities item 1 + the c094 active head call `orthogonalize-mutation-rotation` an "un-authored L1>L0 theme to mint as a roadmap_goal" — it is ALREADY authored + `firm` on disk (`book/src/L1-L0/orthogonalize-mutation-rotation.md:243`). No roadmap_goal chapter is minted this cycle (D1–D4 all absorb into EXISTING firm homes). Surfaced for the next planner / the batch-31 meta-phase intake: the "mint roadmap_goal for not-yet-describable homes" sub-task of the LEAD may be a no-op if every slice's material has a firm home already — verify per-slice on touch.
- **The `:72-108`/`:73-108` "HARD blocker" was a mis-framing in priorities.** It is not a slice line-anchor repoint blocking the krylov trio; it is an L0-anchor off-by-one internal to `plane_rotation_stream.md` (`:72` = `template`, `:73` = signature, codemap-confirmed), tracked by OQ `plane-rotation-givens-l0-citation-range-reconcile`, resolved-by-deletion in D4. The actual krylov-trio HARD blocker is the `arnoldi_step` anchor web (9 distinct line-ranges across the firm krylov files), correctly deferred.
- **Conflict-tolerance call recorded:** D3 and D4 both edit `concepts/sequential-obstruction.md` (distinct sections — sparse-trisolve `:53` vs Givens-stream `:83-85`). Marked PARALLEL per the when-in-doubt-PARALLEL directive; if the per-report integrator hits a line-adjacency merge, it is a cheap correction + an `integrator-signals` data point (false-parallelization is the recoverable error; false-sequentialization costs throughput). Flagging here so the integrator expects the touch.
- **Capacity note:** 6 dispatches (well under the 12 ceiling). I deliberately did NOT manufacture additional slice dispatches — the krylov trio + `orthog`/`polynomial_recurrence_step` are genuinely higher-coupling and forcing them into c097 would risk an unclean cycle (the c096 planner's explicit "do NOT let a single cycle attempt both the full absorption AND the deletion if the citation web is larger than one clean cycle" guidance). Item 3 (the standing bottom-up vocab + 5-driver→L4 frontier) is NOT dispatched this cycle — the LEAD slice campaign correctly owns the cycle's focus, and item 3 resumes once P2 is fully sequenced.
