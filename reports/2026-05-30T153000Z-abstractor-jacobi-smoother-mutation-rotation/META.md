---
verifies: ../CYCLE.md
critiqued_at: 2026-05-30T06:33:50Z
critic_version: 1
checks:
  citation-validity: pass
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: pass
  cross-reference-integrity: pass
  edge-label-fidelity: pass
  plan-kind-consistency: pass
  skill-uptake-survey: pass
---

# META: verification of abstractor `jacobi-smoother-mutation-rotation` (L1>L0 firm theme, cycle-033 D1)

## Critique

### Checks run

**citation-validity: pass.** Ran `tools/citecheck/citecheck.py --scan` on the report — all 37 citations clear bounds + path-hygiene (`37 ok, 0 failing`). Then ran load-bearing `--anchor` adjudications independently against on-disk `reference/palace/palace/linalg/jacobi.{cpp,hpp}` (which I also read in full to cross-check). All confirmed exactly as cited: `SetOperator @74-97` (signature at 75; `dinv.SetSize` @77, `UseDevice` @78, `AssembleDiagonal` @79, `Reciprocal` @80, `omega == 0.0` branch @84, `lambda_max` @86, `lambda_min` @87, `omega = 2.0/...` @88, damping-fold `dinv *= omega` @92 within `:90-93`, base-contract @95-96 with `height = op.Height()` at 95), `Mult @99-104` (`MFEM_ASSERT` @102, `Apply(dinv, x, y)` @103), real `Apply<Transpose>` kernel `:30-39` (`forall_switch` lambda `Y[i] = DI[i] * X[i]` at 38), complex `Apply<Transpose>` kernel `:41-70` (forward branch 52-60, dead-code Transpose=true branch 61-69), `GetLambdaMax` real `:14-20` and complex `:22-28` (SPD comment at 16 confirmed, Hermitian flag `A.IsReal()` confirmed in complex overload), template instantiations `:106-107`, `MultTranspose` self-alias `jacobi.hpp:43`, hpp signature/member citations at :19/:28/:31/:34/:39/:41. Sibling-precedent citations all clear: `chebyshev.cpp:13-27` (identical `GetLambdaMax`), `chebyshev.cpp:177-178` (AssembleDiagonal in chebyshev SetOperator), `vector.cpp:248-261` (Reciprocal body), `solver.hpp:32-33` (initial_guess base member at line 33). Consumer citations all clear: `ksp.cpp:198-200`, `errorestimator.cpp:75-77`, `floquetcorrection.cpp:65`, `spaceoperator.cpp:640`, `timeoperator.cpp:85`, all anchor-match `JacobiSmoother` token on the asserted line. Inline `ksp.cpp:199` shorthand (in Applicability §2, §6 inside-fence) refers to the actual `make_unique<JacobiSmoother>` line at 199 (line 198 is the `case` label) — consistent with the enclosing `:198-200` citation. Two chebyshev dead-code transpose citations `:101-110` and `:150-159` raised `[DRIFT]` against my naive "Apply" anchor query, but on read-through they correctly enclose the complex Transpose=true branches (lines 102-110 = order-1 complex transpose body ending at brace; lines 148-155 = order-k complex transpose body) — `--anchor` was finding "Apply" in nearby function declarations, not in the body content. Re-anchored with `RR` / `DR` body tokens, both ranges return OK. No `verified_against:` YAML block is present in the report (the `## Verified-against` section is prose markdown, not a YAML scalar), so the YAML round-trip sub-check does not apply. Net: zero unresolved drifts.

**surface-or-evidence: pass.** This is a **net-new** L1>L0 theme chapter (`new:book/src/L1-L0/jacobi-smoother-mutation-rotation.md`) lowering an existing firm L1 anchor. Both surface (the chapter body with Slug/L1 form/L0 form/4 sub-patterns/Applicability/Justification kind/Verified-against/Status) AND rotation evidence (37 positive Palace citations exhaustively covering the SetOperator chain, both Mult/Apply overloads, the GetLambdaMax helper, the self-alias override, the template instantiations, and 5 consumer sites) are present. Not a refinement of an existing entry — the surface IS the proposal.

**rotation-quality: pass.** The lowering direction is correct: LHS is the L1 form `y = jacobi_smoother(op, x) = (ω · D⁻¹) ⊙ x` (lines 47-50 of the chapter body); RHS is the L0 in-place `Mult(x, y) → Apply(dinv, x, y)` with `Y[i] = DI[i] * X[i]` (sub-pattern B body at 161-171). Prose is narrated forward ("Narrated forward: the L1 pure action dissolves into the L0 output-argument mutation idiom..." at line 32). The L_{n+1}→L_n compression is genuine: at L1 the value is a pure tensor function over an opaque closure; at L0 it is a destination-arg mutation through a named buffer plus a class-bound diagonal-prep sequence — the L1 form **erases** the buffer mechanics (`SetSize`/`UseDevice`/`Read`/`Write`), the damping-mode branching (folded into closure construction), the precondition `MFEM_ASSERT(!initial_guess)`, and the C++ template-instantiation element-type axis. This is real state-hiding and threaded-state compression, not renaming. Firm status is justified per the established firm-on-positive-structure escape clause: every sub-pattern is a syntactic identity on fully-specified positive Palace source (the `apply_linop` / `apply_nonlinear_pencil` / `chebyshev-smoother` precedents). Sub-pattern C (the `GetLambdaMax` spectral-estimate sub-action) is **NOT an obstruction-shadow**: the helper body IS positively cited (`jacobi.cpp:14-20` real + `:22-28` complex, both adjudicated `OK` with `DiagonalOperator Dinv` / `ProductOperator DinvA` / `linalg::SpectralNorm` content); only the downstream `linalg::SpectralNorm` callable boundary is opaque, and that opacity is consistently routed as "closure-field handling, same as chebyshev sibling sub-pattern D" — a precedented opaque-callable pattern, not a missing-source pattern. The closed-form optimal-`ω` arithmetic (lines 291-294) is shown algebraically. The four sub-patterns map onto the L1 anchor's law catalogue cleanly: sub-pattern A's damping-fold = L1 law 3; sub-pattern B's elementwise multiply = L1 law 1; sub-pattern C's substituted-`ω` = L1 law 4; sub-pattern D's self-alias = L1 law 5. I cross-checked the law numbering against `book/src/L1/jacobi-smoother.md:157-219` and every reference is correct.

**variant-axis-coverage: pass.** Two real variant axes (the chebyshev-style "real OperType=Operator vs complex OperType=ComplexOperator" element-type axis, and the "damping-mode" axis — default ω=1.0, fixed ω≠0, estimated ω=0) are both covered explicitly: element-type via template-instantiations `:106-107` (cited) + the two `Apply` overloads (both cited); damping-mode via sub-pattern A's `SetOperator` branching at `:84-93` + sub-pattern C's full coverage of the ω=0 path. The dead-code complex `Apply<Transpose=true>` kernel `:61-69` is **explicitly** scoped as a recognition rule (lines 212-219; reiterated in Status caveats at 591-598 and Open questions at 622-630) — not hidden. The `initial_guess` precondition is treated as a precondition rather than an axis (justified: the L0 `MFEM_ASSERT` asserts its negation hard, so there is no apply-time variation to absorb), and contrasted explicitly with the chebyshev-sibling's `initial_guess` per-call argument (lines 195-203). No hidden branches.

**cross-reference-integrity: pass.** All on-disk cross-reference targets verified to exist: `book/src/L1/jacobi-smoother.md`, `book/src/L1/assemble-diagonal.md`, `book/src/L1/apply_linop.md`, `book/src/L1/apply_nonlinear_pencil.md`, `book/src/L1/chebyshev-smoother.md`, `book/src/L1-L0/chebyshev-smoother-mutation-rotation.md`, `book/src/L1-L0/assemble-diagonal-mutation-rotation.md`, `book/src/L1-L0/apply-linop-mutation-rotation.md`, `book/src/L1-L0/ksp-solve-mutation-rotation.md`, `book/src/concepts/elementwise-product.md`, plus the L1-L0/index.md insertion-landmarks (`nleps-eigenvalue-correction-mutation-rotation.md` and `minres-iteration.md`). The two forward-references to NOT-YET-EXISTING targets are correctly recorded as PLAIN TEXT (backtick-quoted, NOT `[link]()`-form): `reciprocal` (used at chapter lines 103-105, 454-460, 472-474, 612-622; verified `book/src/L1/reciprocal.md` is absent on disk) and `elementwise_product` (used at 461-466, 472-474; verified `book/src/L1/elementwise-product.md` is absent on disk). The chapter body explicitly states "recorded here as plain text" at both occurrences. Fence-parity / build-readiness guard: `grep -n '^\`\`\`'` returns exactly 6 fence lines (at 22, 656, 658, 670, 672, 683) — even parity, three balanced `new:`/`edit:`/`edit:` blocks. The `new:` chapter fence at 22-656 ENCLOSES the firm `## Status` heading (line 564), the §Verified-against block (484-562), and the chapter's own §Open questions / caveats (610-655) — firm-body-inside-fence guard PASSES. The L1-L0/index.md surgical insert specifies the row-anchor row precisely ("after the existing nleps-... row and before the minres-iteration row"); I verified those are on-disk lines 41 and 42, so the insertion target is unambiguous. The SUMMARY.md surgical insert specifies "currently line 102 of SUMMARY.md" for the chebyshev-smoother-mutation-rotation line — I verified the chebyshev SUMMARY entry IS at line 102 on disk. Both surgical inserts are well-targeted.

**edge-label-fidelity: pass.** Slug `jacobi-smoother-mutation-rotation`, directory `book/src/L1-L0/`, LHS labelled "L1 form" (line 42), RHS labelled "L0 form" (line 62), narration explicitly forward ("Narrated forward: the L1 pure action dissolves into the L0..." at 32; "Lowers the pure L1 form ... into Palace's in-place L0..." at 26-29). All four sub-patterns discuss the L1→L0 lowering direction. No L_{n+1} entity is described in L_n vocabulary inside its own definition (L1 closure operations stay in L1 vocabulary; the lowering theme prose is the only place that crosses the edge). Consistent with the high→low layer-definition discipline and the L1>L0 directional convention.

**plan-kind-consistency: pass.** Declared `firm` (Status section at lines 564-588 of the chapter body; reiterated in the report Summary at 18). Content matches the firm-theme shape: full Slug + L1 form + L0 form + 4 sub-patterns (each with prose body + structural-vs-algebraic justification-kind tag + citations block) + Applicability conditions (7 numbered items) + Justification kind summary + Verified-against block (extensive) + Status with caveats + Open questions. Substantive prose (~530 lines inside the new: fence). Firm-on-positive-structure justification is invoked correctly per the `apply_linop`/`apply_nonlinear_pencil`/`chebyshev-smoother` precedent (laws are syntactic identities on fully-specified positive source; test-jacobi.cpp absence does not gate firm — confirmed `reference/palace/test/unit/test-jacobi.cpp` does not exist on disk, and the integration coverage via 5 consumer sites is broader than the chebyshev sibling's). No rough-in placeholders inside the firm body; the only plain-text deferrals (`reciprocal`, `elementwise_product`) are forward-references to sibling L1 primitives the theme depends on but does not author, scoped correctly.

**skill-uptake-survey: pass.** The report invokes `tools/citecheck/citecheck.py --anchor` explicitly at three sites (lines 486, 567, 706), signalling the cycle-024 mechanical-citecheck realization of `verify-citation-range`. The `proposed-changes-fence-encloses-full-body-guard` is implicitly satisfied (fence-parity is clean, firm-apparatus is inside fence). The `upgrade-plain-text-ref-to-live-link-when-target-on-disk` skill is acknowledged via the integrator-uptake note at 472-474 + 616-622 + 758-765 (the integrator may upgrade once D2/D3 land, or materialize stubs per the "Integration may materialize implied components as stubs" directive). The `partly-constructive-promotion-checklist` is NOT invoked because this is a fresh firm theme rather than a promotion — correctly absent. Good skill-mention telemetry.

### Issues found

No blocking issues. Three minor observations worth surfacing as low-severity:

1. **Minor (cosmetic) — Summary sub-pattern count phrasing.** The report Summary at line 18 opens with "Three sub-patterns: (A) ... (B) ... (C) ..." then adds "Plus a thin sub-pattern D — the `MultTranspose` self-alias". The chapter body lists four sub-patterns (A/B/C/D as parallel `### Sub-pattern` headings at chapter lines 75, 159, 256, 333), and the §Justification kind summary at chapter lines 426-444 enumerates all four. The Summary's "three plus a thin D" phrasing is stylistic emphasis (D is shorter than the others), not a contradiction with the body, but a reader skimming the Summary alone might miscount. **Severity: low / cosmetic.** Location: report `CYCLE.md:18` (report-level Summary, outside the new: fence).

2. **Minor — duplicate `## Open questions / caveats` headings.** The report contains two `## Open questions / caveats` headings: one INSIDE the new: chapter-body fence at line 610 (the chapter's own §Open questions — correct placement), and one OUTSIDE the fence at line 752 (a report-level integrator-uptake duplicate, explicitly annotated at 754-756 as "duplicates the high points for the integrator's open-questions ledger uptake"). Both contain overlapping content (the `reciprocal`/`elementwise_product` forward-ref note, the `spectrum_estimate` L1 candidacy note, the L2 unification candidate, the dead-code transpose kernel). The duplication is intentional and well-annotated, but it means the integrator parsing the report may see the same OQ twice — once carried into the chapter body verbatim, once carried into the OQ ledger. **Severity: low.** Not a structural issue; the explicit annotation defangs it. Location: report `CYCLE.md:610` (in-fence) and `CYCLE.md:752` (out-of-fence).

3. **Minor (mechanical-only) — `chebyshev.cpp:101-110` and `:150-159` are body-content ranges that pass anchor-by-token-`Apply`-search as `[DRIFT]` because they enclose lambda-bodies rather than function-declaration lines.** This is NOT a real drift — the cited ranges correctly enclose the complex Transpose=true ELSE-branch bodies of the order-1 and order-k Apply overloads respectively (verified by reading the chebyshev source 95-160 and re-anchoring with body-content tokens `RR` and `DR`, both of which adjudicate OK in-range). The dead-code-Hermitian-recognition claim the report makes about these ranges is accurate. Worth recording only because the citecheck tool's default `--anchor` invocation against the function name "Apply" would naively suggest drift; future critics auditing this report (or the chebyshev sibling, which has the same body-range citation pattern) should know to re-anchor with body-content tokens, not the enclosing function name. **Severity: trivia / tooling-note.** Location: report `CYCLE.md:219` (sub-pattern B caveat citing the chebyshev sibling kernels).

All 8 critic checks pass. Citation hygiene is mechanically clean; the rotation is genuine and forward-narrated; firm status is justified per the established firm-on-positive-structure escape clause; cross-references and forward-refs are correctly classified live-vs-plain-text; fence enclosure is well-formed for build readiness; surgical insertion points (L1-L0/index.md row, SUMMARY.md line) are on-disk-accurate.
