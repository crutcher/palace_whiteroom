---
agent: cycle-planner
invoked_at: 2026-06-03T041103Z
scope: cycle-074 dispatch plan
status: pending
---

# Cycle 074 dispatch plan

## Goals selected this cycle

Cycle-074 is position 2/3 of meta-batch-23 (cycles 073/074/075; meta-phase fires after 075). Cycle-073 landed the FEATURE-SURFACE SPINE driver-column half at 5-of-5 and the shared L4 reduction combinator `gram_reduce` (`rough-in (test-coverage-bounded)`). This cycle pushes three things, fan-out-ranked: **(1) close the c073-deferred `gram_reduce` replace-and-propagate** into the electrostatic/magnetostatic feature columns (the redirect's mine-and-strand close — NOT optional, the LEAD); **(2) open the OUTPUT-PRODUCT feature cohort** (capacitance + inductance columns, now unblocked because `gram_reduce` is firm-track and gives them a real combinator to compose) — the next FEATURE-SURFACE SPINE kind per CLAUDE.md §Extraction-goal; **(3) hygiene** — wire the lifecycle ROOT to its now-on-disk driver columns, normalize the status-token divergence, and a cheap observation-only `gram_reduce` 3rd-witness/hazard probe. All FEATURE-SURFACE SPINE work runs in parallel with the bottom-up vocabulary frontier per the standing directive; this cycle the highest-fan-out eligible work IS the spine close + the unblocked output cohort.

## Deliverable-presence verification (paste-inline evidence)

**D1 (`gram-reduce-feature-chapter-reanchor`) — re-anchor existing files (NOT a fresh land; the inversion targets must EXIST):**
- `ls book/src/feature/electrostatic.L4.md` → EXISTS (8886 bytes, mtime Jun 2 16:07). `ls book/src/feature/magnetostatic.L4.md` → EXISTS (9909 bytes, Jun 2 19:15).
- `ls book/src/L4/gram_reduce.md` → EXISTS (17620 bytes, Jun 2 20:48); frontmatter `firmness: rough-in (test-coverage-bounded)` (read from disk).
- Stale-positive loci confirmed on disk via grep — electrostatic.L4.md: `:40` "no *new* L4 combinator here … see Open questions", `:53` "no iterative obstruction", `:64` dep-map "no dedicated L4 reduction combinator yet", `:68` §Status "the capacitance reduction has no dedicated L4 reduction combinator yet — a forward mine". magnetostatic.L4.md: `:40`/`:53`/`:64`/`:68` the mirror statements ("no dedicated L4 reduction combinator yet" / "a forward mine shared with the electrostatic capacitance reduction").
- OQ open: `grep gram-reduce-feature-chapter-reanchor scaffolding/open-questions.md` → line 929, `(c073 D1) … sequenced to c074, deliberately left untouched this cycle` — OPEN, trigger fired (it is c074). NOT in any RESOLVED/CLOSED index.
- Structural gate: none — this is replace-and-propagate per redirect §1 (combinator-primary), the explicit deferred-to-c074 coupled half. Open by deferral.

**D2 (`capacitance-output-product-feature-column`) — fresh land (open by construction, no prior history):**
- `ls book/src/feature/capacitance.{L4,L1,L0}.md` → all three `No such file or directory` (absent — verified). Open by construction (new output-product cohort kind; no prior-cycle history).
- L0 anchors codemap-verified: `PostprocessTerminals` def `electrostaticsolver.cpp:100` (codemap `search_text` hit), called `:95`, `mfem::DenseMatrix C(V.size())` `:111`, `Cinv` `:139`, `GetStiffnessMatrix()` `:30`. Down-link target `book/src/L4/gram_reduce.md` EXISTS (firm-track).

**D3 (`inductance-output-product-feature-column`) — fresh land (open by construction):**
- `ls book/src/feature/inductance.{L4,L1,L0}.md` → all three absent (verified). Open by construction.
- L0 anchors codemap-verified: `PostprocessTerminals` def `magnetostaticsolver.cpp:110` (codemap hit), called `:105`, `mfem::DenseMatrix M(A.size())` `:122`, `Minv` `:151`, `GetStiffnessMatrix()` `:30`. Down-link target `gram_reduce.md` EXISTS.

**D4 (`lifecycle-driver-column-live-link-reanchor`) — re-anchor existing file:**
- `ls book/src/feature/lifecycle.L4.md` → EXISTS (9164 bytes, Jun 2 19:21). The 3 driver columns it forward-refs all EXIST on disk now: `feature/{driven,transient,eigenmode}.{L4,L1,L0}.md` all present (ls confirmed, 9 files, mtimes Jun 2 20:40-20:45).
- Stale plain-text loci confirmed via grep: lifecycle.L4.md `:37` "forthcoming feature columns (not yet authored)", `:59` dep-map "*(forthcoming — not yet authored)* … not yet authored", `:64` §Status "eigenmode/driven/transient forthcoming, plain-text".
- c073 integrator-signals §Unblocked named this: "the 3 newly-landed driver columns … can be upgraded to live links in a future pass (the columns now exist on disk)". Open / unblocked.

**D5 (`feature-column-status-token-normalization`) — cosmetic re-anchor:**
- `grep -l "seed (exemplar)\|seed (composition-root)" book/src/feature/*.md` → 6 files: electrostatic.{L4,L1,L0}.md + lifecycle.{L4,L1,L0}.md (magnetostatic + the c073 columns already bare `seed`). Targets confirmed on disk.
- OQ open: `feature-column-status-token-divergence-hygiene-c074` (c073 integrator-signals §Suggested-next + §Integration-tooling-friction "routed to c074+"). OPEN.
- Structural note: batch-22 meta-phase codified the uniform bare-`seed` token (memory `project_feature_surface_spine`: "the prose names the sub-kind"). Gate cleared.

**D6 (`gram-reduce-third-witness-probe`) — observation-only (no `book/` mutation; open by construction):**
- OQ open: `grep gram-reduce-third-witness-probe scaffolding/open-questions.md` → line 930, `(c073 D1) … MEDIUM fan-out, not a blocker`. OPEN. Observation-first per the redirect; no deliverable file, no pre-existence check applicable.

**STOP-PROPOSING negative-list check:** none of D1–D6 match any disqualified slug (`lu_solve`/`back_solve`/`ls-update-column`/4 NLEPS atoms; `map_solve` from single witness; `L3/solve_family`, `L2/fold_solve`, `L2/fe_assemble`, `weak_form_term` L2 floors). D6 explicitly carries the over-unification anti-mirror guard (S-params NOT symmetric-Gram in general) so it cannot manufacture a degenerate subsume.

## Dispatches

- **D1** — agent: `lifter`; scope: `gram-reduce-feature-chapter-reanchor` — re-anchor `book/src/feature/electrostatic.L4.md` (loci :40/:53/:64/:68) + `book/src/feature/magnetostatic.L4.md` (loci :40/:53/:64/:68) §reduction prose + §"Constituent down-links" table + `composes:` frontmatter + §Status: invert "no dedicated L4 reduction combinator yet — a forward mine" / "two rough-in L1 primitives" → "the rough-in-track L4 [`gram_reduce`](../L4/gram_reduce.md) reduction" (electrostatic = the `w = 1` voltage specialization `Vⱼᵀ K Vᵢ`; magnetostatic = the `w = 1/(IᵢIⱼ)` current-normalized specialization `(Aⱼᵀ K Aᵢ)/(IᵢIⱼ)`). Combinator-primary per redirect §1: the per-pipeline reduction is a specialization of `gram_reduce`, not a strand. Keep the rough-in L1 `matrix-weighted-norm`/`bilinear-form` down-links as the *constituents `gram_reduce` itself folds* (do not delete them — `gram_reduce` is rough-in BECAUSE they are), but re-frame them as "the building blocks the `gram_reduce` fold composes" rather than "the reduction is done directly by these". The §Status stays `seed` (D5 handles the token normalization — do NOT also flip the `(exemplar)` qualifier here; that is D5's, and D5 is sequenced after you to avoid a double-edit conflict on the §Status region of electrostatic.L4.md). deps: none.
  - rationale: the c073-deferred replace-and-propagate (OQ `gram-reduce-feature-chapter-reanchor-sequences-to-c074`); the redirect's mine-and-strand close (`gram_reduce` was mined c073 but left stranded — the two pipelines that motivated it still say "no combinator yet"). THE LEAD, HIGH.

- **D2** — agent: `layer-intro-author`; scope: `capacitance-output-product-feature-column` — author NEW `book/src/feature/capacitance.{L4,L1,L0}.md` (status `seed`, output-product composition-root kind): config (terminal excitations) → the electrostatic driver column's solution family → the L4 [`gram_reduce`](../L4/gram_reduce.md) reduction (`w = 1`) → the Maxwell capacitance matrix `C` (+ inverse `Cinv`). Compose-DOWN: `gram_reduce` (firm-track L4) + down-link to [`electrostatic.L4`](./electrostatic.L4.md) as the producing driver column. L0: `ElectrostaticSolver::PostprocessTerminals` def `electrostaticsolver.cpp:100` (called `:95`), `mfem::DenseMatrix C` `:111`, energy-form `Mult`/`Dot` `:118-127`, inverse `:139-140`. **D2 IS THE COHORT OWNER:** D2 SOLE-authors the `feature/index.md` matrix output-product rows for BOTH capacitance AND inductance (add a §output-products grouping or 2 rows per the existing matrix shape; leaf-drivers-then-outputs ordering) AND the `book/src/SUMMARY.md` `# Feature surfaces` block rows for BOTH new columns (within-column high→low L4/L1/L0, the deliberate non-alpha exception). D3 authors ONLY its own 3 chapter files and DEFERS its index/SUMMARY rows to you — wire D3's `inductance.{L4,L1,L0}.md` into the matrix + SUMMARY by canonical slug (the files land same-cycle; if a fallback is needed flag it in your staging row). deps: D3 (D2 references D3's `inductance.*` slugs in the shared index/SUMMARY — D3's files should be on disk before D2 applies the consolidated block; per the c072/c073 cohort-owner happy-path, schedule D3 in the same wave but order D2's apply after D3's in the per-report sequence).
  - rationale: the unblocked output-product cohort opener (c073 integrator-signals §Unblocked + §Suggested-next: "an OUTPUT-PRODUCT feature column — capacitance OR inductance … now unblocked by firm-track `gram_reduce`"); the named FEATURE-SURFACE SPINE cohort-3 kind. MEDIUM-HIGH.

- **D3** — agent: `layer-intro-author`; scope: `inductance-output-product-feature-column` — author NEW `book/src/feature/inductance.{L4,L1,L0}.md` (status `seed`, output-product composition-root): config (surface-current sources) → the magnetostatic driver column's solution family → the L4 [`gram_reduce`](../L4/gram_reduce.md) reduction (`w = 1/(IᵢIⱼ)`) → the Maxwell inductance matrix `M` (+ inverse `Minv`). Compose-DOWN: `gram_reduce` (firm-track L4) + down-link to [`magnetostatic.L4`](./magnetostatic.L4.md). L0: `MagnetostaticSolver::PostprocessTerminals` def `magnetostaticsolver.cpp:110` (called `:105`), `mfem::DenseMatrix M` `:122`, energy-form `:129-138`, inverse `:151-152`. **D3 authors ONLY its 3 chapter files** — its `feature/index.md` matrix row AND its `SUMMARY.md` `# Feature surfaces` block row are DEFERRED to D2 (the cohort owner); do NOT touch `feature/index.md` or `SUMMARY.md` (parallel-blind-shared-index guard). Use the canonical slug `inductance` so D2's matrix/SUMMARY references resolve. deps: none (authors disjoint files; the index/SUMMARY ownership belongs to D2).
  - rationale: the second output-product column; pairs with D2 (the electrostatic/magnetostatic capacitance/inductance reductions are the two `gram_reduce` witnesses). MEDIUM-HIGH.

- **D4** — agent: `lifter`; scope: `lifecycle-driver-column-live-link-reanchor` — upgrade `book/src/feature/lifecycle.L4.md` plain-text forward-refs to the 3 now-on-disk driver columns (driven/transient/eigenmode, landed c073) to live links, using skill `upgrade-plain-text-ref-to-live-link-when-target-on-disk`: locus `:37` ("eigenmode, driven, transient — are forthcoming feature columns (not yet authored)" → live `[eigenmode.L4](./eigenmode.L4.md)` / `[driven.L4](./driven.L4.md)` / `[transient.L4](./transient.L4.md)`), locus `:59` (dep-map table cell "eigenmode.L4 / driven.L4 / transient.L4 *(forthcoming — not yet authored)*" → live links + drop the forthcoming qualifier), locus `:64` (§Status "eigenmode/driven/transient forthcoming, plain-text" → "on disk, live-linked"). Verify each target file on disk before linking (all 9 present). deps: none.
  - rationale: c073 integrator-signals §Unblocked ("the 3 newly-landed driver columns are now live-linked from the lifecycle ROOT's compose-DOWN block … can be upgraded to live links"); completes the spine ROOT's 5-branch navigation. MEDIUM (hygiene with real navigation fan-out).

- **D5** — agent: `lifter`; scope: `feature-column-status-token-normalization` — normalize the status token `seed (exemplar)` / `seed (composition-root)` → bare `seed` across the 6 older-column files: `book/src/feature/electrostatic.{L4,L1,L0}.md` + `book/src/feature/lifecycle.{L4,L1,L0}.md` (per the batch-22-meta-codified uniform token; the prose already names the sub-kind, so the qualifier is redundant). Edit ONLY the `## Status` line token; leave the descriptive prose that follows (it names exemplar/composition-root/meta-feature in words, which is correct). deps: D1 (D1 and D5 both touch the §Status region of electrostatic.L4.md — D1 edits the §Status reduction-reasoning prose, D5 edits the §Status token; sequence D5 after D1 so the two §Status edits do not collide).
  - rationale: OQ `feature-column-status-token-divergence-hygiene-c074` (c073 integrator-signals §Integration-tooling-friction, "routed to c074+ … COSMETIC"). LOW.

- **D6** — agent: `cross-layer-cross-cutter`; scope: `gram-reduce-third-witness-probe` (OBSERVATION-ONLY, no `book/` mutation) — probe whether (a) the eigenmode Q-factor/eigenfrequency energy post-processing and (b) the driven S-parameter port-overlap reductions share the operator-weighted symmetric-Gram shape of `gram_reduce` (Vⱼᵀ·Op·Vᵢ over a family-pair grid). **CLEAR THE OVER-UNIFICATION HAZARD FIRST:** S-parameters are NOT symmetric-Gram in general (per OQ `gram-reduce-third-witness-probe-eigenmode-driven-postprocess`) — establish whether the symmetric-Gram shape positively holds for each candidate BEFORE concluding it is a witness; eigenmode introduces a complex element-type axis value. Cite the `disciplined-cross-pipeline-combinator-mining-gate` skill. **Do NOT author `book/` regardless** — a clean 3rd witness LICENSES a future `gram_reduce` broadening + informs the deferred S-param/eigenfreq output-product columns; a non-match RECORDS the boundary as a spine finding (the redirect's "what a solver can't cleanly say is a finding"). deps: none.
  - rationale: OQ `gram-reduce-third-witness-probe-eigenmode-driven-postprocess` (c073 D1); cheap hazard-clear ahead of the deferred S-param/eigenfreq output-product columns. LOW-MEDIUM, observation-first.

## Overlap analysis

Pairwise (artifact-region / shared-operator-name basis):

- **D1 × D2** — D1 edits `electrostatic.L4.md` (a driver column); D2 creates `capacitance.{L4,L1,L0}.md` + edits `feature/index.md` + `SUMMARY.md`. Disjoint files. D2 down-links TO `electrostatic.L4` (a live link to an existing file — resolves regardless of D1's edits; D1 does not rename/move it). NOT overlapping. (Soft narrative-consistency preference to land D1 first so the electrostatic column already reads "via gram_reduce" when D2 cross-links it — handled by wave ordering, not a hard conflict.)
- **D1 × D3** — D1 edits `magnetostatic.L4.md`; D3 creates `inductance.*` + down-links to `magnetostatic.L4` (live link, existing file). Disjoint files. NOT overlapping (same soft preference as D1×D2).
- **D1 × D4** — D1 edits electrostatic.L4 + magnetostatic.L4; D4 edits lifecycle.L4. Disjoint. NOT overlapping.
- **D1 × D5** — **OVERLAPPING.** Both edit `book/src/feature/electrostatic.L4.md` §Status region (D1 inverts the reduction-reasoning prose inside §Status; D5 flips the `seed (exemplar)` token at the head of §Status). Same file region → SEQUENTIAL: D5 after D1.
- **D1 × D6** — D6 is observation-only (no book mutation). NOT overlapping.
- **D2 × D3** — both touch `feature/index.md` + `SUMMARY.md` `# Feature surfaces` block. **Managed by the cohort-owner partition** (parallel-blind-shared-index guard): D2 SOLE-owns the consolidated index matrix rows + SUMMARY block for BOTH columns; D3 authors ONLY its own 3 chapter files and DEFERS its index/SUMMARY rows to D2. With that partition the two are non-conflicting on the shared index (D3 never writes it). D2 references D3's `inductance.*` slugs → order D3's chapter-file apply before D2's index/SUMMARY apply (per-report serial ordering) so the matrix/SUMMARY cells resolve live. Canonical slug `inductance` stated in BOTH D2 and D3 scopes (cross-report-forward-reference-slug guard).
- **D2 × D4 / D2 × D5 / D2 × D6** — D4 edits lifecycle.L4 (disjoint from capacitance.* + index/SUMMARY rows… BUT D4 does NOT touch feature/index or SUMMARY, only lifecycle.L4 body); D5 edits electrostatic/lifecycle §Status tokens (disjoint from D2's new files; D2 touches feature/index + SUMMARY which D5 does not). Wait: D2 edits SUMMARY.md (adds capacitance/inductance rows); D4 does not touch SUMMARY; D5 does not touch SUMMARY. NOT overlapping. D6 observation-only.
- **D3 × D4 / D3 × D5 / D3 × D6** — disjoint files (D3 = inductance.* only; D4 = lifecycle.L4; D5 = electrostatic/lifecycle §Status; D6 = none). NOT overlapping.
- **D4 × D5** — **POTENTIAL overlap on lifecycle.L4.md.** D4 edits lifecycle.L4 forward-ref loci (:37/:59/§Status :64 forthcoming-clause); D5 edits lifecycle.L4 §Status TOKEN (:64 `seed (composition-root)` → `seed`). Both touch §Status of lifecycle.L4 (locus :64). Per the conflict-tolerance philosophy these are *adjacent but distinct edits* (D4: the "forthcoming, plain-text" clause; D5: the bare token at the head). They are close enough to risk a mild collision → SEQUENTIAL to be safe: D5 after D4. (Marking sequential here is cheap; both are lifter tasks on the same file region.)
- **D6 × all** — observation-only, no artifact writes. Fully parallel-safe.

## Sequencing schedule

- **Wave 1 (parallel):** D1, D2, D3, D4, D6.
  - D1 (re-anchor electrostatic.L4 + magnetostatic.L4) — disjoint from D2/D3/D4/D6.
  - D2 + D3 (output-product columns) — cohort-owner partition (D2 sole-owns index/SUMMARY; D3 defers); per-report apply orders D3's chapter files before D2's consolidated index/SUMMARY block (same wave, intra-wave per-report ordering).
  - D4 (lifecycle live-link re-anchor) — disjoint file.
  - D6 (observation-only probe) — no writes.
- **Wave 2 (after wave-1 reports land):** D5 (status-token normalization).
  - D5 depends on D1 (shared electrostatic.L4 §Status region) AND D4 (shared lifecycle.L4 §Status region). Running D5 last lets it normalize the token over the final §Status text both D1 and D4 produced.

Note: there is ONE `integrator-finalize` for the cycle; the waves order *dispatches* by forward-reference / shared-region dependency, not multiple finalizes. The book is not rebuilt between waves.

## Open questions / caveats

- **`gram_reduce` stays rough-in-track; the feature columns correctly compose a non-firm combinator.** D1/D2/D3 all down-link to `gram_reduce` which is `rough-in (test-coverage-bounded)`. That is fine for the `seed` output-product columns + the re-anchored driver columns (a seed/composition-root composes whatever maturity its constituents have). The `gram_reduce` promotion is double-gated (L1 `matrix-weighted-norm`/`bilinear-form` firming + a dedicated Gram test, OQ `gram-reduce-status-promotion-double-gated`) and is NOT this cycle's work.
- **D6 over-unification hazard is the load-bearing guard, not optional framing.** If D6 finds S-parameters do fit symmetric-Gram it should be skeptical and cite the positive shape explicitly; the default expectation (per the OQ) is that S-params are NOT symmetric-Gram → a non-witness finding. Do not let D6's discharge license an over-broad `gram_reduce` subsume.
- **Output-product cohort layout question for the batch-23 meta-phase.** The capacitance/inductance reductions ALSO live inside the electrostatic/magnetostatic driver columns' stage-3 (as down-links to `gram_reduce`). The new dedicated capacitance/inductance feature columns are the cohort-3 "output product" surfaces (config → product). There is mild conceptual redundancy (the driver column's stage-3 and the output-product column both describe the same reduction from different entry points — top-down-by-driver vs top-down-by-product). This is *by design* per CLAUDE.md (output products are a named distinct cohort), but the meta-phase should confirm the cross-linking convention (the output-product column down-links to `gram_reduce` + cross-links to the producing driver column; the driver column's stage-3 should cross-link UP to the output-product column) so the two surfaces stay coherent and don't drift. Flagging for the batch-23 meta-phase (fires after c075).
- **Status-token normalization (D5) is the last `(exemplar)`/`(composition-root)` cleanup.** After c074, all feature columns carry bare `seed`; the OQ closes. No further token hygiene owed.
- **No role-spec/agent-def changes implied this cycle** — all 6 dispatches are forward-frontier + hygiene under existing role-specs; no session restart needed before c075.
- **Cadence note:** the friction-ledger/priorities I read may be up to ~3 primary cycles stale (meta-phase enacts only at batch boundaries). I saw no new escalating friction this cycle beyond the long-standing benign `skill-uptake-survey` telemetry pattern (status `escalating` but repeatedly no-go'd as benign for opus-tier agents). Nothing meta-warranting surfaced that isn't already ledgered.
