---
agent: cycle-planner
invoked_at: 2026-06-07T112037Z
scope: cycle-124 dispatch plan (batch-40 OPENER; cycles 124/125/126, meta after 126)
status: pending
---

# Cycle 124 dispatch plan

## Goals selected this cycle

Batch-40 is the **lift-through tail**: discharge the residual RE set (RE3 deflate/NLEPS, RE6
axpy-arity) and firm the constructive-kernel substrate (the element-local rank-tensor vocabulary
that firms the 4 libceed substrate roadmap_goals + the libceed-quadrature kernel-impl), ranked by
fan-out. Per the active-head sequencing note + the role-spec "prefer fewer high-fan-out dispatches
on the lift-through tail" instruction, this cycle leads with the **two HIGH fronts** — item-1
(deflate/NLEPS, the LEAD; discharges RE3 + grounds the eigsolve-impl/lanczos_step RE11 nodes) and
item-2 (element-local-rank-tensor; firms 5 substrate nodes at once, the burn/GPU-relevant
matrix-free build that ASK-2 named the post-batch-40 lead "A") — plus the self-contained RE6
refactor (item-3) and a bundled cheap-hygiene pick (items 4+5). Eight dispatches, kept deliberately
narrow rather than a wide thin fan-out.

## Deliverable-presence verification (per role-spec MANDATORY paste-inline-evidence check)

All scopes resolve to named `book/src/` paths → four-step check run per dispatch; pasted evidence below.

**D1/D2 (item-1 deflate/NLEPS consumer + eigsolve-impl correspondence):**
- File existence: `book/src/L2/deflate.md` EXISTS; `book/src/L2/gram.md` EXISTS; `book/src/L3/eigsolve-impl.md` EXISTS; `book/src/L3/lanczos_step.md` EXISTS; `book/src/L1/nleps_deflated_solve.md` / `nleps_deflated_residual.md` / `nleps_eigenvalue_correction.md` / `nleps_jacobian_action.md` all EXIST.
- Maturity: `L2/deflate.md` → `` `partly-constructive` `` (structure firm; `lu_solve` constructive sub-part). `L2/gram.md` → `firm` (needs small-dense `lu_solve` per its OQ note, but firm). `L3/eigsolve-impl.md` → `status: roadmap_goal` / `rank: roadmap_goal` (awaiting a BLOCKING `depends-on` consumer — §Pulled-by:122 "RE3 deflate / NLEPS-deflated eigensolve (c122 consumer) — the natural primary blocking depends-on consumer. (Coupling NOTED, edge NOT forced this cycle.)"). `L3/lanczos_step.md` → `roadmap_goal` (kernel-impl-constituent; promotion gated on MINRES/symmetric-Lanczos positive structure). The 4 NLEPS L1 ops → all `firm`. **NOT a no-op:** the deflate/NLEPS-deflated-eigensolve CONSUMER node (the thing that composes `eigsolve-impl` + `deflate`/`gram` by name) does NOT yet exist — building it fires RE3 + grounds eigsolve-impl/lanczos_step off RE11. This is exactly the migrated-and-open work.
- OQ RESOLVED-grep: `grep -i 'deflate-nleps.*RESOLVED\|nleps-deflated-eigensolve.*RESOLVED' scaffolding/open-questions.md` → no matches; the active head + OQ unified-section explicitly MIGRATE "the residual-RE consumers + the element-local-rank-tensor front + the correction_step back-link follow-ups → c124 plan". OPEN.
- Structural-block check: no gate. RE3's promotion condition is precisely "a downstream NLEPS/deflation consumer surfaces" (`graded-stack-baseline-exceptions.md` RE3 row); `eigsolve-impl` §Pulled-by:122-124 names the deflate consumer as the intended blocking edge. The DIRECTIVE-3 kernel-api/impl integrity gate APPLIES (D2): the `realizes-kernel-api` edge stays `reference`-class; D2 audits it as `reference`, not `depends-on`.

**D3/D4/D5 (item-2 element-local-rank-tensor front):**
- File existence: `book/src/L1/element_restrict.md`, `basis_apply.md`, `quad_point_contract.md`, `geom_factor_build.md` all EXIST; `book/src/L1/libceed-quadrature-kernel-impl.md` EXISTS; `book/src/L1/fe_assemble.md` EXISTS (`rank: firm`); `book/src/concepts/element-local-tensor.md` → NONE (does not exist — D5 authors it).
- Maturity: `element_restrict`/`basis_apply`/`quad_point_contract`/`geom_factor_build` → all `rank: roadmap_goal`; `libceed-quadrature-kernel-impl` → `rank: roadmap_goal`. **NOT a no-op:** firming the substrate (roadmap_goal → rough-in/firm via the genuine `Tensor[(E,L)]`/`[(E,P,C)]`/`[(E,P,G)]` vocabulary shift) is open by construction. Each substrate file's §Status states the exact promotion route ("once the element-rank tensor substrate is firm L1 vocabulary, this promotes roadmap_goal → rough-in → firm on the usual gates").
- OQ RESOLVED-grep: `grep -i 'element-local-rank.*RESOLVED' scaffolding/open-questions.md` → no matches; MIGRATED to c124 plan (OQ unified §). OPEN.
- Structural-block check: no gate; this IS the genuine vocabulary shift (flat-vector BLAS `Tensor[N]` → element-local rank structure) the substrate §Status sections name as their promotion route. Semantic-surface coupling: the canonical surface is `book/src/semantics/index.md` §1.2.1 (named shape groups) — NOT `book/src/design/l4_calculus.md` (that path moved batch-37; the substrate files cite the stale path, a fix D5 folds in). USE+LINK-don't-restate-semantics governs D5.

**D6 (item-3 RE6 axpy/scal arity-notes refactor):**
- File existence: `L2/{axpy,axpby,axpbypcz,scal}.md` + `L3/{axpy,axpby,axpbypcz,scal}.md` all EXIST; `L2/linear_combination.md` + `L3/linear_combination.md` EXIST.
- Maturity: the 8 arity leaves are firm standalone DAG nodes (the RE6 baseline-exception members). **NOT a no-op:** the refactor DEMOTES them to in-chapter `## Arity specializations` notes inside `linear_combination` (removing them as standalone nodes) — the "higher-value disposition" RE6's promotion-condition row names. Open by construction (the refactor has not run).
- OQ RESOLVED-grep: `grep -i 'RE6\|arity-leaves.*RESOLVED' scaffolding/open-questions.md` → RE6 appears as a still-LIVE refactor-gated baseline-exception (`graded-stack-baseline-exceptions.md` RESIDUAL table); not resolved. OPEN.
- Structural-block check: no gate; RE6's promotion condition IS this refactor ("demote the arity-specialization leaves to in-chapter notes … removing them as standalone DAG nodes").

**D7 (items 4+5 bundled cheap-hygiene + correction_step back-links):**
- File existence: `book/src/feature/geometric-multigrid-preconditioner.L4.md` EXISTS (the `vcycle` body :67-73, the `bind_preconditioner` line :63); `geometric-multigrid-preconditioner.L1.md` EXISTS; `book/src/L1/multigrid-relaxation-smoother.md` EXISTS; `book/src/L2/correction_step.md` EXISTS (firm, c122); `book/src/L1/eigsolve.md` EXISTS.
- Maturity: `L2/correction_step` → firm. The GMG L4 `vcycle` prose currently does NOT name `correction_step` in its recursion body (`grep correction_step` on the L4 file → no hits; only `vcycle`/`bind_preconditioner`). **NOT a no-op:** the c123-D3 verdict flagged the 1 L4 reference down-link + 2 L1 downward annotations as unwritten propagation. Open.
- OQ RESOLVED-grep: the active head item-4 cites "the c123-D3-flagged propagation" + the OQ unified § migrates "the correction_step back-link follow-ups → c124 plan". OPEN. The item-5 eigsolve `:330-333`→`:331-334` arpack-ido99 citation nit is a known carried citation-drift (active head item-5).
- Structural-block check: no gate (navigational back-links + a citation correction; combinator-primary navigability completion per the active head).

**RESULT: all 8 dispatches PASS all four checks. None on the STOP-PROPOSING negative list** (the negative list disqualifies `lu_solve`/`back_solve`/`ls-update-column`/the 4 standalone `nleps_*` slugs as **L3 backfills**; item-1 does NOT backfill those as L3 nodes — it builds a deflation-EIGENSOLVE CONSUMER that COMPOSES the existing firm L1 `nleps_*` ops + `L3/eigsolve-impl` + `L2/deflate`/`gram` by name, the migrated-and-open RE3 discharge, a different operation).

## Dispatches

**D1 — `harvester` (item-1 LEAD, HIGH fan-out, WAVE-1).**
Scope: author the **deflate / NLEPS-deflated-eigensolve CONSUMER** node — the L3 (eigenmode-driver-side) chapter that composes the deflation-eigensolve loop BY NAME: the outer Quasi-Newton/NEP loop that grows the invariant-pair basis `X` by one converged column per eigenpair (`palace/linalg/nleps.cpp:613-619`, the `X.resize(k+1); X[k]=v` deflation-basis extension) wrapping the deflated linear solve (`nleps.cpp:504-542`, the `deflated_solve` lambda) and the deflated residual (`nleps.cpp:547`). The consumer COMPOSES, via faithful `depends-on (composes)` edges: `L3/eigsolve-impl` (the constructive Krylov-Schur/Arnoldi eigen-iteration kernel-impl, roadmap_goal — this consumer is the BLOCKING depends-on edge its §Pulled-by:122-124 names) + `L2/deflate` (the oblique projector, partly-constructive) + `L2/gram` (the all-pairs Gram, firm) + the firm `L1/nleps_deflated_solve` / `nleps_deflated_residual` / `nleps_eigenvalue_correction` / `nleps_jacobian_action` ops. Name `lanczos_step` as the symmetric-pencil basis-extension constituent (carried via `eigsolve-impl`). State the deflation laws (oblique-projection complementarity; locked-vector invariance) at the composition level; cite the SLEPc-NEP deflation scheme at `nleps.cpp:356-359` (minimality-index-1) + `:505-537`. Wiring the blocking `depends-on` edges FIRES RE3 (`deflate → L2/gram` faithful edge) and GROUNDS `L3/eigsolve-impl` + `L3/lanczos_step` off the RE11 reference-only-reachable cohort (a faithful depends-on consumer now composes them). Honest clean-gate: if the constructive deflation-eigensolve loop is not fully positively sourced (the outer loop IS Palace-authored at `nleps.cpp`, but its composition with the eigsolve-impl Krylov body is partly constructive), land the consumer at the maturity the evidence supports (`partly-constructive` or `rough-in`) — the GROUNDING (RE3 fire + eigsolve-impl/lanczos_step liveness) lands regardless of the consumer's own rank, because liveness flows over `depends-on` independent of rank. Verify `mcp__palace-codemap__read_range` on-disk for every anchor (the `nleps.cpp` deflation region is template/Eigen-dense — pre-localized hints: `:356-359` deflation-scheme comment, `:504-542` `deflated_solve`, `:547` deflated residual, `:600-619` outer converged-pair basis extension; on-disk-confirm the END lines).
Deps: none.
Rationale: active-head item-1 (the LEAD). Discharges RE3, grounds 2 RE11 nodes, couples to the eigenmode driver. Plan-tag `re-discharge-tail`. fan-out: HIGH.

**D2 — `lowering-verifier` (item-1 correspondence, MEDIUM fan-out, WAVE-2, dep D1).**
Scope: audit the **kernel-api/impl correspondence** the moment D1's consumer wires `L3/eigsolve-impl` live (if D1 promotes it off roadmap_goal). Verify (i) the `realizes-kernel-api` edge from `L3/eigsolve-impl` → `L3/eigsolve` (and → `L4/eigsolve`) stays `reference`-class — a mis-type to `depends-on` is a DEFECT (the new `lowering-verifier` kernel-api-integrity §Discipline bullet, this batch); (ii) the impl↔api eigenpair correspondence holds (the constructive Krylov-Schur loop computes the same eigenpairs the opaque SLEPc/ARPACK api defers to); (iii) D1's deflation-consumer `depends-on` edges to `eigsolve-impl`/`deflate`/`gram` are faithful constituent-use, not forced. Append a `verified_against:` block to `L3/eigsolve-impl.md` (or the consumer) per the DIRECTIVE-3 impl-realizes-API audit pattern (the c122 D5/D6 precedent). If D1 leaves `eigsolve-impl` at roadmap_goal (no promotion), narrow to auditing only the new consumer→impl `depends-on` faithfulness + the `reference`-edge integrity (a roadmap_goal impl reaching root only via `realizes-kernel-api` `reference` is the INTENDED RE11 disposition, not decay — per the resume-note lowering-verifier bullet).
Deps: D1 (audits D1's wiring).
Rationale: active-head item-1 ("+ lowering-verifier (the eigsolve-impl correspondence, if it firms)") + the standing kernel-API/impl integrity gate. Plan-tag `re-discharge-tail`. fan-out: MEDIUM.

**D3 — `harvester` (item-2, HIGH-MEDIUM fan-out, WAVE-1).**
Scope: firm the **two arithmetic libceed substrate ops** that carry the genuine element-local rank-tensor vocabulary shift: `L1/basis_apply` (`B` — per-element-dof → per-quad-point eval: interp/grad/curl/div, `Tensor[(E,L)] → Tensor[(E,P,C)]`) + `L1/quad_point_contract` (`D` — pointwise geom×coeff×weight contraction at quad points, `Tensor[(E,P,C)] → Tensor[(E,P,C)]`). Promote each roadmap_goal → rough-in/firm by establishing the `Tensor[(E,P,C)]`/`[(E,P,G)]` element-local rank-tensor as firm L1 vocabulary (the shape semantics + the contraction algebra). Cite the libCEED `CeedBasisApply` / `CeedQFunction` realization (codemap-verify on-disk). USE+LINK the named-shape-groups convention from `book/src/semantics/index.md` §1.2.1 — do NOT restate it; FIX the stale `book/src/design/l4_calculus.md` citation in these chapters to the live `book/src/semantics/index.md` path. Confirm the sum-factorization tensor-contraction decomposition as a transparent performance trick (the L1 form is the unfolded tensor contraction; sum-factorization is a one-line note). Honest clean-gate: if the rank-structured substrate is not yet firm L1 vocabulary at authoring time, land at the maturity the evidence + the D5 record-page support (they share the cohort context this wave).
Deps: none (shares the element-local exploration context with D4/D5; non-overlapping files).
Rationale: active-head item-2 (the arithmetic substrate half). Plan-tag `constructive-spine-kernels`. fan-out: HIGH-MEDIUM.

**D4 — `harvester` (item-2, HIGH-MEDIUM fan-out, WAVE-1).**
Scope: firm the **two indexing/geometry libceed substrate ops**: `L1/element_restrict` (`G`/`Gᵀ` — global true-dof ↔ per-element local-dof gather/scatter-add, `Tensor[(N: ...)] ↔ Tensor[(E,L)]`) + `L1/geom_factor_build` (the build-QFunction: Jacobian × quad-weight → `geom_data`, `Tensor[(E,P,G)]`). Promote each roadmap_goal → rough-in/firm on the same element-local rank-tensor vocabulary D3 establishes (the `[E,L]`/`[E,P,G]` shapes; the pure gather/scatter-add indexing for `element_restrict`, the Jacobian-determinant geometry build for `geom_factor_build`). The flat global axis `N` stays a genuine rank-1 `Tensor[(N: ...)]` per the L1/L0 flat-vector convention (already noted in `element_restrict.md`). Cite the `CeedElemRestriction` construction + index-map builder + the `J × qw` geometry QFunction (codemap-verify on-disk). USE+LINK `semantics/index.md` §1.2.1; FIX the stale `design/l4_calculus.md` citation.
Deps: none (shares the cohort context with D3/D5; non-overlapping files).
Rationale: active-head item-2 (the indexing/geometry substrate half). Plan-tag `constructive-spine-kernels`. fan-out: HIGH-MEDIUM.

**D5 — `layer-intro-author` (item-2, HIGH-MEDIUM fan-out, WAVE-2, dep D3+D4).**
Scope: author the **`concepts/element-local-tensor.md` record-definition page** (the `[E,L]`/`[E,P,C]`/`[E,P,G]` element-local rank-tensor shape family — the data-shape home for the whole substrate cohort: element axis `E`, local-dofs-per-element `L`, quad-points `P`, components `C`, geometry-factors `G`; the construction-vs-run-time stratum of each axis; the L0 home of the backing libCEED tensor layouts). ≥2-consumer bar fired (all 4 substrate ops + `libceed-quadrature-kernel-impl` reference the shape family). On firm, re-point the substrate ops' inline shape-family references to it. ALSO: if D3+D4 firmed the 4 substrate ops, promote the consumer `L1/libceed-quadrature-kernel-impl` roadmap_goal → rough-in/firm (its `depends-on` edges to the now-firm substrate become firm-resting; the rank invariant `rank(impl) ≤ min(deps)` permits the lift) — and update the named-shape-groups elementwise/reduce cohort coverage in `semantics/index.md` §1.2 IF the element-local shape family warrants a new sub-section (the `project_named_shape_groups_notation` elementwise/reduce-cohort-pending coupling; semantic-surface owner). De-register nothing; ADD the concepts page to `SUMMARY.md` (concepts Part, alpha position) + wire its dep-map row.
Deps: D3, D4 (the record page composes the shapes D3/D4 firm; the kernel-impl promotion needs the 4 substrate ops firm).
Rationale: active-head item-2 (the record-definition home + named-shape-groups coupling + the kernel-impl consumer promotion); the layer-intro-author owns the semantic surface + concepts pages. Plan-tag `constructive-spine-kernels`. fan-out: HIGH-MEDIUM.

**D6 — `combinator-miner` (item-3, MEDIUM fan-out, WAVE-1).**
Scope: the **RE6 combinator-arity-notes refactor** — REPLACE-AND-PROPAGATE (not mine-and-strand, per the redirect combinator-miner re-mandate): demote the 8 axpy/scal arity-specialization leaves (`L2/axpy`, `L2/axpby`, `L2/axpbypcz`, `L2/scal` + the 4 L3 siblings) to in-chapter `## Arity specializations` notes INSIDE `L2/linear_combination` + `L3/linear_combination`, removing them as standalone DAG nodes. Each leaf is the arity-N readout of the `linear_combination` combinator (the typed edge runs leaf→combinator; combinator-primary). For each demoted leaf: fold its concrete arity body + its L0 callsite anchors into a `## Arity specializations` row inside the combinator chapter, then de-register the standalone chapter (remove from `SUMMARY.md`, the L2/L3 index dep-maps, and any inbound `reference` links re-pointed to the combinator's specialization section). This DISCHARGES RE6 by ELIMINATING the off-spine leaves rather than grounding them (the higher-value disposition per RE6's promotion-condition row). NOTE the `scal` arity-1 cluster-completeness (RE6 explicitly includes `L2/scal`/`L3/scal`). Co-own the de-registration index/SUMMARY edits with D5 IF both touch `SUMMARY.md` — see Overlap (they touch DISJOINT regions: D5 the concepts Part, D6 the L2/L3 Parts).
Deps: none.
Rationale: active-head item-3. Discharges RE6, tightens the combinator-primary model. Plan-tag `re-discharge-tail`. fan-out: MEDIUM.

**D7 — `layer-intro-author` (items 4+5 bundled, LOW fan-out, WAVE-1).**
Scope: the bundled cheap-hygiene cohort (active-head items 4 + 5, the bundle-able LOW picks):
(a) **correction_step back-links (item-4):** author the c123-D3-flagged inbound navigational propagation — the 1 L4 `reference` down-link `feature/geometric-multigrid-preconditioner.L4 → L2/correction_step` + reword the GMG L4 `vcycle` prose (`book/src/feature/geometric-multigrid-preconditioner.L4.md:67-73`, the `vcycle ps bs b0 l x` recursion body) to NAME `correction_step` as the coarse-grid-correction verb; + the 2 L1 downward navigational annotations in `L1/multigrid-relaxation-smoother.md` + `feature/geometric-multigrid-preconditioner.L1.md`. `reference`-class edges (navigational, free — combinator-primary navigability completion, NOT depends-on).
(b) **eigsolve arpack-ido99 citation nit (item-5):** correct the carried `:330-333`→`:331-334` ARPACK ido-99 citation drift in `book/src/L1/eigsolve.md` (codemap-verify the exact on-disk line). + the interpolator backward-reference-note trim (item-5).
Deps: none.
Rationale: active-head items 4+5 (LOW, bundle-able). Plan-tag `constructive-spine-kernels` (4) / `gmg-amr-hygiene` (5). fan-out: LOW.

(NOTE — item-5's heavier sub-picks DEFERRED to c125/c126: `vcycle-level-recursive-combinator-mining-candidate` (a real combinator-miner dispatch, not bundle-able into D7's navigational pass), `record-MultigridConfig-needs-definition-home` (2nd-consumer-gated — verify the 2nd consumer fired before recruiting), the GMG-smoother L3 partial-obstruction home (couples to the krylov-iteration/L3-iteration-view territory, a substantive harvest). Kept off this wave to honor "prefer fewer high-fan-out dispatches on the lift-through tail.")

## Overlap analysis

Pairwise (8 dispatches):
- **D1 ↔ D2:** D2 AUDITS D1's output (`L3/eigsolve-impl.md` consumer wiring) → SEQUENTIAL (D2 after D1). Genuine dependency, not a write-race.
- **D1 ↔ D3/D4/D5/D6/D7:** D1 creates the new deflation-consumer chapter + edits `L3/eigsolve-impl.md`/`L2/deflate.md`/`L2/gram.md` edges + the L3 index dep-map. Disjoint from the L1 substrate files (D3/D4), the concepts page (D5), the linear_combination chapters (D6), the GMG/eigsolve-L1 files (D7). The only shared file is `SUMMARY.md` (D1 adds its new chapter line in the L3 Part) — DISJOINT region from D5 (concepts Part) and D6 (L2/L3 BLAS-1 de-registration). PARALLEL-safe (per-report on-disk re-read serializes the distinct `SUMMARY.md` regions).
- **D3 ↔ D4:** both firm libceed substrate ops but DISJOINT files (D3: `basis_apply.md`+`quad_point_contract.md`; D4: `element_restrict.md`+`geom_factor_build.md`). Both edit the `L1/index.md` dep-map (distinct ROWS — parallel-safe per the role-spec distinct-dep-map-rows rule) + `SUMMARY.md` (the 4 substrate ops are ALREADY in SUMMARY as roadmap_goal lines; D3/D4 flip in-place, no new lines, distinct slugs). PARALLEL-safe. **Shared L1/index consolidated tally:** the `L1/index.md` may carry a firm-count aggregate. PARTITION (parallel-blind-shared-index guard): **D5 OWNS the `L1/index.md` substrate-cohort consolidated tally + any firm-count bump this wave** (it is the layer-intro-author in the wave + lands last in the substrate dep order); D3 and D4 each append ONLY their own dep-map ROWS (and flip their own `SUMMARY.md` lines in-place), and DEFER the consolidated tally to D5.
- **D3/D4 ↔ D5:** D5 composes the shapes D3/D4 firm + promotes the `libceed-quadrature-kernel-impl` consumer whose `depends-on` edges point at D3/D4's nodes → SEQUENTIAL (D5 after D3+D4). D5 authors `concepts/element-local-tensor.md` (new file) + edits `semantics/index.md` + `SUMMARY.md` (concepts Part) + `L1/libceed-quadrature-kernel-impl.md` + owns the `L1/index.md` tally.
- **D5 ↔ D6:** both touch `SUMMARY.md` — D5 the concepts Part + (if a new shape sub-section) the semantics Part; D6 the L2/L3 BLAS-1 chapter de-registration. DISJOINT regions → PARALLEL-safe at the operational level. They do NOT share a chapter body or a consolidated tally (D6's de-registration touches the L2/L3 index dep-maps; D5 touches the concepts + L1 index). PARALLEL-safe.
- **D6 ↔ D7:** disjoint files (D6: `linear_combination` chapters + L2/L3 BLAS-1 de-registration; D7: GMG L4/L1 + `L1/eigsolve.md` + `L1/multigrid-relaxation-smoother.md`). No shared region. PARALLEL.
- **D7 ↔ all:** D7's files (GMG L4/L1, `L1/eigsolve.md`, `L1/multigrid-relaxation-smoother.md`, interpolator note) are disjoint from D1 (deflation consumer + eigsolve-IMPL, a different file from `L1/eigsolve.md`), D3/D4 (substrate ops), D5 (concepts), D6 (linear_combination). NOTE: D1 touches `L3/eigsolve-impl.md`; D7 touches `L1/eigsolve.md` — DIFFERENT files (L3 kernel-impl vs L1 kernel-api/driver). No overlap. PARALLEL.

**Consolidated-tally ownership (parallel-blind-shared-index guard, role-spec):** the only wave with ≥2 parallel landings into one index carrying a consolidated count is the L1 substrate cohort (D3+D4 → `L1/index.md`). **D5 sole-owns the `L1/index.md` consolidated firm-count tally + growth-log this wave;** D3/D4 each emit ONLY their own dep-map ROW + their own §Vocabulary-cohort BULLET (anchor-distinct, parallel-safe — NOT deferred) and DEFER the consolidated tally to D5. No other index has a ≥2-parallel-landing collision (D1 sole-lands into the L3 index; D6 sole-de-registers from L2/L3 indices; D7 is navigational-only).

**Forward-reference slug coordination (cross-report-forward-reference-slug guard):** D1's deflation-consumer references `L3/eigsolve-impl` (canonical, already on disk — no guess) + `L2/deflate`/`L2/gram` (on disk). D5's `concepts/element-local-tensor.md` is referenced by D3/D4's substrate ops — canonical slug **`concepts/element-local-tensor`** stated explicitly in BOTH D5's authoring scope AND D3/D4's reference scope (so D3/D4 do not invent a divergent working slug). D6 re-points demoted-leaf inbound references to `L2/linear_combination#arity-specializations` / `L3/linear_combination#arity-specializations` (canonical anchor stated in D6's scope).

## Sequencing schedule

- **Wave 1 (parallel):** D1 (deflation consumer), D3 (basis_apply + quad_point_contract), D4 (element_restrict + geom_factor_build), D6 (RE6 arity refactor), D7 (cheap hygiene bundle). Five parallel dispatches, all non-overlapping at the chapter-body level; `SUMMARY.md` / index touches are disjoint-region or distinct-row (per-report on-disk re-read serializes).
- **Wave 2 (parallel, after wave-1 reports land):** D2 (eigsolve-impl correspondence audit — needs D1's wiring), D5 (element-local-tensor concepts page + kernel-impl promotion + L1/index tally — needs D3+D4 firm).

Two waves. One `integrator-finalize` at cycle end (waves are dispatch/forward-reference ordering, not multiple finalizes; the book is not rebuilt between waves).

## Open questions / caveats

- **Item-1 maturity is evidence-bounded.** The deflation-eigensolve consumer's OWN rank depends on how positively-sourced the composition of the Palace-authored outer loop (`nleps.cpp`) with the `eigsolve-impl` constructive Krylov body is. The GROUNDING outcome (RE3 fire + eigsolve-impl/lanczos_step liveness flip) is INDEPENDENT of the consumer's own rank — liveness flows over `depends-on` regardless of rank — so even a `partly-constructive`/`rough-in` consumer discharges RE3 and grounds the RE11 nodes. D1 lands at the honest maturity; the discharge stands. (Flagged so the integrator does not read a non-firm consumer as a failed discharge.)
- **Item-2 depth is the batch-shaping unknown.** The element-local rank-tensor front is a GENUINE vocabulary shift (`Tensor[N]` → `Tensor[(E,L)]`/`[(E,P,C)]`/`[(E,P,G)]`) and may be multi-cycle (the active-head sequencing note + ASK-2's "A" direction both flag this). c124 firms what is cleanly firmable (the 4 substrate ops + the record page + the kernel-impl consumer if the deps firm); if the substrate does not fully firm in one cycle, the residual carries to c125 (and the deepen-into-a-layer "A" work is the post-batch-40 lead). D3/D4 land at the honest maturity; a substrate op that stays roadmap_goal/rough-in this cycle is not a failure — it is the shape of a real vocabulary shift.
- **Stale semantic-surface path in the substrate files.** The 4 libceed substrate roadmap_goals cite `book/src/design/l4_calculus.md` §1.2.1, but the canonical surface moved to `book/src/semantics/index.md` (batch-37). D3/D4/D5 fix this on touch (USE+LINK the live path). Flagged for the meta-phase in case other batch-37-era files carry the same stale path (a candidate semantic-surface drift-check item).
- **D2 contingency.** If D1 leaves `eigsolve-impl` at roadmap_goal (no positive-structure promotion), D2 narrows to auditing the consumer→impl `depends-on` faithfulness + the `realizes-kernel-api` reference-edge integrity (a roadmap_goal impl reaching root only via the `reference` edge is the INTENDED RE11 disposition per the resume-note lowering-verifier bullet — NOT decay). The orchestrator may collapse D2 into D1's wave if D1's report makes clear no correspondence-audit-worthy promotion occurred; D2 is cheap and low-risk either way.
- **MultigridConfig 2nd-consumer gate (deferred from this wave).** `record-MultigridConfig-needs-definition-home` (active-head item-5) is 2nd-consumer-gated — verify the 2nd consumer actually fired before any c125/c126 recruitment (do not author the concepts page on a single consumer). Noted for the next planner.
- **DIRECTIVE-1 boundary holds.** None of the 8 dispatches lifts MPI/sharding (D1 reads the `nleps.cpp` deflation loop single-rank; D3/D4 read libCEED element-local kernels single-rank — parallelism is by composition). Confirmed clean.
