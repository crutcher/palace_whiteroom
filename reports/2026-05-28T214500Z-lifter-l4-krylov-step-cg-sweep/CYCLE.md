---
agent: lifter
invoked_at: 2026-05-28T213558Z
scope: L4 operator-entry re-anchor — l4-krylov-step-cg-md-citation-sweep
status: integrated
integrated_at: 2026-05-28T221238Z
integration_commit: b54ea1c
integration_notes: "Applied cycle-016 (per-report position 2). Pure citation re-anchor of 7 dangling cg.md range pointers in firm L4/krylov-step.md across 6 edit blocks; status stays firm (no signature/semantics/law/variant-axis change). OQ l4-krylov-step-cg-md-citation-sweep answered (ledger:2780); NEW OQ l3-l2-body-identity-cg-md-citation-sweep opened. Retroactive-budget 0. Book build clean (exit 0)."
inputs:
  - book/src/L4/krylov-step.md
  - book/src/L2/krylov-step.md
  - book/src/L3-L2/krylov-step-body-identity.md
  - book/src/concepts/sequential-obstruction.md
  - book/src/concepts/derived-view-hoisting.md
  - book/src/spec/slices/cg.md
  - book/src/spec/slices/arnoldi_step.md
---

# CYCLE: Re-anchor l4-krylov-step-cg-md-citation-sweep

## Summary
The firm L4 operator entry `book/src/L4/krylov-step.md` carries **7 dangling `cg.md:NNN-MMM` range pointers** across 7 file-lines (14, 82, 96, 133, 152, 170, 171 — verified by grep; the cycle-015 L3 sweep's OQ predicted lines 14/82/96/133/150/170/171, and line 150→152 is a 2-line drift on the Status paragraph). The slice `book/src/spec/slices/cg.md` was reduced cycle-009/cycle-010 to a 165-line stub holding ONLY the L4-v0.5 first-iteration-unrolling derivation (`verified wc -l = 165`); every `cg.md` range these pointers cite (`172-188`, `325-339`, `352-362`, `393-425`) is beyond line 165 and no longer resolves at the cited range. This is the **sibling sweep** the cycle-015 L3 sweep (`reports/2026-05-28T202234Z-lifter-l3-krylov-step-cg-md-citation-sweep/CYCLE.md`) explicitly enumerated and routed to OQ `l4-krylov-step-cg-md-citation-sweep` (that report's §"Open questions / caveats" listed this exact L4 pointer set). Pure citation re-anchor applying the cycle-013/014/015 lifted-evidence convention verbatim: structure stays, no claim/signature/law/variant-axis change, status stays `firm`. Keep the historical `cg.md` range as parenthetical provenance, point at the firm home the content was lifted into, name the live `arnoldi_step.md` co-anchor where one exists, and where the cited content is the unique material RETAINED live in the reduced stub note its new in-stub location.

The 7 dangling range pointers split into **four** semantic families, each with a distinct firm terminal home:

- **Combinator-miner cycle-002 body-identity / obstruction-below-the-body assertion (Claim 1+2)** — range `cg.md:352-362`. Lines 14, 133. Firm home: [`L3-L2/krylov-step-body-identity`](../../book/src/L3-L2/krylov-step-body-identity.md) §Verified-against (line 125 preserves the original `cg.md:341-362` range with the verbatim Claim-2 quote "*…the identity in form: no unfolding, no global lift, no schema change.*", original slice line 360). The live co-anchor `arnoldi_step.md:185-188` (302-line slice, §L2/L3 material retained — non-dangling; covers the variant-dependent obstruction + Hessenberg-column-write identity) is **unchanged**. Both L4 sites co-cite `arnoldi_step.md:185-188`, which stays live.
- **Residual-norm derived-view hoisting (Law 1 worked example)** — range `cg.md:325-339`. Line 96. Firm home: [`concepts/derived-view-hoisting`](../../book/src/concepts/derived-view-hoisting.md) §"Worked example: CG residual norm" (lines 14-19). The reduced `cg.md` stub header (stub line 14) names this concept page as the explicit supersessor of the slice's "§L4 v0.4 derived-view hoisting (self-rotation) … residual-norm hoisting worked example". The co-cited `gmres.md:471-489` LS-residual range is OUT OF SCOPE for a `cg.md` sweep (gmres.md not reduced — that pointer is left untouched).
- **CG L4 Form A step body** — range `cg.md:172-188`. Lines 152, 170. Firm home: [`L2/krylov-step`](../../book/src/L2/krylov-step.md) §Evidence (line 138; the authoritative narrative registry carrying the CG L4 Form A `cg_step` step-body instance in L2 vocabulary). The reduced `cg.md` stub header (stub line 13) names `book/src/L4/krylov-step.md` Form A as the supersessor of the slice's §"L4" v0.1-v0.4 — i.e. the L4 entry IS the firm home for the Form-A typing, and its own §Evidence (lines 170-171) already states it cites the bodies "transitively via the L2 entry, not re-anchored here". So the L4 entry's §Evidence Form-A pointer is re-anchored to NAME the L2 narrative registry as the firm home (consistent with the entry's own transitive-via-L2 caveat at line 176).
- **CG L4-v0.5 Form B step body (the `(cg_first_step, cg_steady_step)` split)** — range `cg.md:393-425`. Lines 82, 152, 171. This is the **unique material RETAINED live in the reduced stub** (`cg.md` stub lines 27-141, the v0.5 first-iteration-unrolling derivation: `cg_first_step` at stub line 52, `cg_steady_step` at stub line 69, the `iterate_while_with_prev` driver at stub lines 95-108, the `forget_beta_prev` equivalence at stub line 129). The content lives, but the historical range `393-425` is beyond line 165 so the citation range itself dangles. Re-anchor: point at the live retained material's NEW stub location (`cg.md:27-141`, with `cg_first_step`/`cg_steady_step` at stub:52/:69) as the live anchor, naming `concepts/first-iteration-unrolling.md` as the firm concept home (the stub header at stub line 16 names this concept page as the canonical consumer of the retained v0.5 material), and `L2/krylov-step.md` §Evidence (line 138, which carries the `:393-425` Form B instance) as the L2 narrative registry.

## Proposed changes

All re-anchors apply the cycle-013/014/015 convention verbatim: the historical `cg.md` range is retained as a parenthetical lifted-evidence provenance note, immediately followed by the firm home the content was lifted into and the live co-anchor (`arnoldi_step.md`, or the reduced `cg.md` stub's retained section for the v0.5 Form B material) where one exists.

### Re-anchor 1 — §"Context", L3-edge identity assertion (line 14): `cg.md:352-362` (combinator-miner cycle-002 Claim 1/2)

```edit:book/src/L4/krylov-step.md
[old]: The L4>L2 lowering is **not identity-in-form**: L4 carries the `SimState` / `OpParams` / `Krylov` typing and the optional `(first_step, steady_step)` split, both of which collapse to value-threading on the way down. The intermediate L3 lowering is plausibly identity-in-form on the kernel body itself (per the combinator-miner cycle-002 assertion, cited at `cg.md:352-362`, `arnoldi_step.md:185-188`), with the L4>L3 transition carrying the substantive rotation; this is the rotation that the cycle-006 wave-2 abstractor dispatch is auditing. If the abstractor finds non-identity rotation at L3, an L3 `krylov-step` row will follow in cycle-007.
[new]: The L4>L2 lowering is **not identity-in-form**: L4 carries the `SimState` / `OpParams` / `Krylov` typing and the optional `(first_step, steady_step)` split, both of which collapse to value-threading on the way down. The intermediate L3 lowering is plausibly identity-in-form on the kernel body itself (per the combinator-miner cycle-002 assertion, now firm in [`L3-L2/krylov-step-body-identity`](../L3-L2/krylov-step-body-identity.md) §Verified-against and co-anchored by the live `arnoldi_step.md:185-188`; the original CG evidence at `cg.md:352-362` was lifted into that firm theme per the cycle-009 corpus reduction), with the L4>L3 transition carrying the substantive rotation; this is the rotation that the cycle-006 wave-2 abstractor dispatch is auditing. If the abstractor finds non-identity rotation at L3, an L3 `krylov-step` row will follow in cycle-007.
```

### Re-anchor 2 — §"Semantics", Form B body description (line 82): `cg.md:393-425` (CG L4-v0.5 Form B — RETAINED live in stub)

```edit:book/src/L4/krylov-step.md
[old]: Form B (first-iteration-unrolled) splits the body into two named functions per `concepts/first-iteration-unrolling.md` §"The rotation". `first_step` produces the initial `PrevCarry` from a base-case computation; `steady_step` consumes `PrevCarry` (the prior iteration's recurrence variable, e.g., `β_prev`) as a closure argument rather than reading it from `Krylov`. The `Krylov` schema in Form B is one slot lighter (no `β_prev` field); the branch-free `steady_step` is the body folded by `iterate_while_with_prev` (per `cg.md:393-425` v0.5). Both forms are valid L4 renderings of the same L2 `krylov-step`; the choice is the `first-iteration-unrolled` variant axis (inherited unchanged from L2).
[new]: Form B (first-iteration-unrolled) splits the body into two named functions per [`first-iteration-unrolling`](../concepts/first-iteration-unrolling.md) §"The rotation". `first_step` produces the initial `PrevCarry` from a base-case computation; `steady_step` consumes `PrevCarry` (the prior iteration's recurrence variable, e.g., `β_prev`) as a closure argument rather than reading it from `Krylov`. The `Krylov` schema in Form B is one slot lighter (no `β_prev` field); the branch-free `steady_step` is the body folded by `iterate_while_with_prev` (the CG v0.5 `cg_first_step` / `cg_steady_step` derivation is the canonical evidence for `concepts/first-iteration-unrolling.md`, retained live in the reduced slice at `cg.md:27-141` — `cg_first_step` at `cg.md:52`, `cg_steady_step` at `cg.md:69`; this is the unique material the cycle-009 reduction kept in the stub, formerly cited as `cg.md:393-425`). Both forms are valid L4 renderings of the same L2 `krylov-step`; the choice is the `first-iteration-unrolled` variant axis (inherited unchanged from L2).
```

### Re-anchor 3 — §"Algebraic laws", Law 1 demand-pruning worked-example pointer (line 96): `cg.md:325-339` (residual-norm hoisting)

```edit:book/src/L4/krylov-step.md
[old]: This is the demand-pruning law underwriting the residual-norm hoisting at `cg.md:325-339` and the LS-residual proxy at `gmres.md:471-489`.
[new]: This is the demand-pruning law underwriting the residual-norm hoisting documented in the firm [`derived-view-hoisting`](../concepts/derived-view-hoisting.md) §"Worked example: CG residual norm" (lines 14-19; the original CG evidence at `cg.md:325-339` was lifted into that concept page per the cycle-009 corpus reduction, which the reduced slice's stub header names as the supersessor) and the LS-residual proxy at `gmres.md:471-489`.
```

### Re-anchor 4 — §"Lowers to", L3>L2 identity-in-form claim (line 133): `cg.md:352-362` (combinator-miner cycle-002 Claim 1/2)

```edit:book/src/L4/krylov-step.md
[old]: - **L3 > L2** (plausibly identity-in-form on the kernel body): per the combinator-miner cycle-002 assertion (cited at `cg.md:352-362`, `arnoldi_step.md:185-188`), the L3 kernel body and the L2 kernel body are isomorphic — the L3 form is the value-threaded body with the outer loop's `sequential-obstruction` made explicit at L3, but the body itself does not change. The cycle-006 abstractor will audit this assertion; if non-identity rotation is found, an L3 `krylov-step` row will be promoted in cycle-007.
[new]: - **L3 > L2** (plausibly identity-in-form on the kernel body): per the combinator-miner cycle-002 assertion (now firm in [`L3-L2/krylov-step-body-identity`](../L3-L2/krylov-step-body-identity.md) §Verified-against, co-anchored by the live `arnoldi_step.md:185-188`; the original CG evidence at `cg.md:352-362` was lifted into that firm theme per the cycle-009 corpus reduction), the L3 kernel body and the L2 kernel body are isomorphic — the L3 form is the value-threaded body with the outer loop's `sequential-obstruction` made explicit at L3, but the body itself does not change. The cycle-006 abstractor's audit landed the firm L3>L2 body-identity theme (the firm L3 `krylov-step` row was subsequently promoted cycle-010 per the identity-lowerings-still-require-both-L-levels directive).
```

### Re-anchor 5 — §"Status", attestation slice-section roll-call (line 152): `cg.md:172-188`, `:393-425`

```edit:book/src/L4/krylov-step.md
[old]: The pattern is well-attested at the L4 level across four slices' explicit L4 sections (cg.md:172-188, cg.md:393-425, gmres.md:459-471, arnoldi_step.md:285-298), and the slot is the consumed-by surface for the L4 concepts `solve-monad`, `state-stratification`, and `first-iteration-unrolling`, which previously referenced "step" without a vocabulary anchor.
[new]: The pattern is well-attested at the L4 level across four slices' explicit L4 sections (CG L4 Form A `cg_step` and L4-v0.5 Form B `cg_first_step`/`cg_steady_step` — lifted into the firm `book/src/L2/krylov-step.md` §Evidence registry, line 138, per the cycle-009 corpus reduction; the Form B v0.5 derivation remains live in the reduced slice at cg.md:27-141; gmres.md:459-471; arnoldi_step.md:285-298), and the slot is the consumed-by surface for the L4 concepts `solve-monad`, `state-stratification`, and `first-iteration-unrolling`, which previously referenced "step" without a vocabulary anchor.
```

### Re-anchor 6 — §"Evidence", explicit-L4-slice-section roll-call, CG Form A + Form B items (lines 170-171): `cg.md:172-188`, `:393-425`

```edit:book/src/L4/krylov-step.md
[old]:   - `book/src/spec/slices/cg.md:172-188` — CG L4 `cg_step` (Form A).
  - `book/src/spec/slices/cg.md:393-425` — CG L4 v0.5 `cg_first_step` / `cg_steady_step` split (Form B).
[new]:   - CG L4 `cg_step` (Form A) — lifted into firm `book/src/L2/krylov-step.md` §Evidence (line 138, the narrative registry carrying the CG L4 Form A step-body instance) per the cycle-009 corpus reduction; the firm `book/src/L4/krylov-step.md` Form A typing (this entry) is the L4 supersessor named in the reduced slice's stub header. (Original pre-reduction range: `cg.md:172-188`.)
  - CG L4 v0.5 `cg_first_step` / `cg_steady_step` split (Form B) — the unique material RETAINED live in the reduced slice at `book/src/spec/slices/cg.md:27-141` (`cg_first_step` at `cg.md:52`, `cg_steady_step` at `cg.md:69`, the `iterate_while_with_prev` driver at `cg.md:95-108`); the canonical evidence for `concepts/first-iteration-unrolling.md`, also registered in firm `book/src/L2/krylov-step.md` §Evidence (line 138). (Original pre-reduction range: `cg.md:393-425`.)
```

## Discipline notes
- **Pure citation re-anchor, no claim/structure/status change.** Every edit swaps a dangling `cg.md:NNN-MMM` reduced-slice range pointer for the firm entry the content was lifted into (or, for the v0.5 Form B material that was RETAINED live, for its new in-stub location), preserving the historical slice range as parenthetical provenance. No signature, semantics, algebraic-law, variant-axis, or applicability-condition change. The entry's `firm` status is unchanged. This is the sibling sweep cycle-015's L3 sweep §"Open questions" enumerated by exact line number and routed to OQ `l4-krylov-step-cg-md-citation-sweep`.
- **Convention inherited verbatim from cycle-013/014/015.** The lifted-evidence annotation pattern — "the original CG evidence at `cg.md:NNN-MMM` was lifted into [firm entry] per the cycle-009 corpus reduction; `arnoldi_step.md:NNN-MMM` remains the valid live anchor" — is exactly the phrasing the cycle-014 lifter applied to the L4>L3 theme and the cycle-015 lifter applied to the L3 operator entry. Both prior sweeps are the direct precedents.
- **Terminal-firm-home self-verification (the dispatch's CRITICAL self-verify; verify-citation-range producer-emit discipline).** For each re-anchor I confirmed the destination is a firm `book/` entry holding the content in firm vocabulary, NOT another relocated-dangle *range pointer*:
  - **Re-anchors 1, 4 → `L3-L2/krylov-step-body-identity.md` §Verified-against (line 125)**: read; it preserves the verbatim Claim-2 quote (original slice line 360) in firm L3>L2 theme vocabulary and is `firm` (§Status line 154). CAVEAT (recorded honestly): line 125 of that firm theme ITSELF still cites the dangling `cg.md:341-362` range as ITS provenance note — but I am pointing at the firm theme's *section* (which holds the claim text in firm vocabulary), not at the dangling slice range; that firm theme's own citation hygiene is the separate `l3-l2-body-identity-cg-md-citation-sweep` residual (see Open questions). The co-anchor `arnoldi_step.md:185-188` was read (slice is 302 lines, live) and confirmed to cover the variant-dependent-obstruction + Hessenberg-column-write identity evidence — it is a TERMINAL live slice range, not relocated.
  - **Re-anchor 3 → `concepts/derived-view-hoisting.md` §"Worked example: CG residual norm" (lines 14-19)**: read; it holds the CG residual-norm hoisting worked example in firm concept vocabulary and is named as the explicit supersessor in the reduced `cg.md` stub header (stub line 14). The concept page's own internal `../spec/slices/cg.md` link (line 16) is a BARE filename link (no range) that still resolves (cg.md exists) — not a dangling range. Terminal firm home confirmed.
  - **Re-anchors 2, 6 (Form B), 5 (Form B portion) → live `cg.md` stub retained material (`cg.md:27-141`)**: read the reduced stub; confirmed `cg_first_step` at stub:52, `cg_steady_step` at stub:69, `iterate_while_with_prev` driver at stub:95-108, `forget_beta_prev` at stub:129 — all WITHIN the 165-line stub, i.e. LIVE terminal ranges (not relocated, not dangling). The v0.5 Form B material was RETAINED (not lifted-away) by the cycle-009 reduction; the stub header (stub line 16) names it as the canonical evidence for `concepts/first-iteration-unrolling.md`.
  - **Re-anchors 5, 6 (Form A) → `L2/krylov-step.md` §Evidence (line 138)**: read; it carries the CG L4 Form A `cg_step` instance (`cg.md:172-188`) and the Form B `:393-425` instance in L2 vocabulary as the narrative registry. CAVEAT (recorded honestly): `L2/krylov-step.md:138` ITSELF still cites the dangling `cg.md:103-115`, `:172-188`, `:393-425` ranges — flagged by cycle-015 as the pending `l2-krylov-step-cg-md-citation-sweep`. As with the body-identity theme, I point at L2's §Evidence *registry section* (which holds the step-body instances in firm L2 vocabulary, per the entry's own primitive-composition decomposition), NOT at L2's dangling slice ranges; L2's citation hygiene is the separate sibling sweep. The L4 entry's OWN §Evidence caveat (line 176: "All L0 evidence is transitive through the L2 entry") already establishes L2 as the transitive narrative anchor, so naming it here is consistent with the entry's existing self-description, not a new claim.
- **Line 126 bare `cg.md` mention left untouched (in scope-bound).** Line 126 (§Dependencies, L2 row) reads "…the cited slice corpus evidence (cg.md, gmres.md, chebyshev.md, arnoldi_step.md, polynomial_recurrence_step.md)…" — a bare filename roll-call with NO line range, accurately describing what the L2 entry references. It carries no dangling range and is a correct statement (the L2 entry does reference those slices). Not a citation-sweep target; left untouched.
- **Out-of-scope co-citations left untouched.** Re-anchor 3's co-cited `gmres.md:471-489` (LS-residual proxy), Re-anchor 5's `gmres.md:459-471` + `arnoldi_step.md:285-298`, and Re-anchor 1/4's `arnoldi_step.md:185-188` are NON-cg.md ranges. `gmres.md` and `arnoldi_step.md` are not reduced slices (arnoldi_step.md verified 302 lines, live); their ranges resolve and are out of scope for a `cg.md` sweep. Only the `cg.md` ranges are re-anchored.
- **Re-anchor 4 staleness firm-up (bounded, incidental to the sweep).** Line 133's trailing sentence "The cycle-006 abstractor will audit this assertion; if non-identity rotation is found, an L3 `krylov-step` row will be promoted in cycle-007" referenced a future-tense audit that has since LANDED (the firm `L3-L2/krylov-step-body-identity.md` theme is firm; the firm `L3/krylov-step.md` row was promoted cycle-010). I updated the trailing sentence to past-tense reflecting the landed state, because the SAME edit replaces the dangling `cg.md:352-362` citation that anchored the now-stale future-tense clause — leaving a half-edited sentence (firm citation + stale future tense) would be incoherent. This is bounded link/tense-hygiene incidental to the citation re-anchor (supported by the firm entries' own §Status lines: `L3-L2/krylov-step-body-identity.md:154` firm; the L3 entry exists firm per the reduced `cg.md` stub header line 10 "firm; cycle-010 wave-1"), NOT a content/structure change — the identity-in-form claim itself is unchanged. Recorded per the lifter content-correction-boundary discipline. (Re-anchor 1's parallel future-tense clause "this is the rotation that the cycle-006 wave-2 abstractor dispatch is auditing … will follow in cycle-007" was LEFT as-is — it is not made incoherent by the citation swap, since that clause describes the L4>L3 transition audit which is narrated elsewhere in the entry's §"Lowers to"; tightening it would exceed the bounded-incidental scope.)
- **High→low direction preserved.** The entry remains an L4 operator definition in L4 (state-stratification / solve-monad) vocabulary; the re-anchors touch only citation pointers in the §Context / §Semantics / §Algebraic-laws / §Lowers-to / §Status / §Evidence prose, add no reverse-direction (L0→L4 lift) narrative, and do not invert any rewrite direction. Friction-ledger `layer-definition-discipline-high-to-low`.

## Supporting evidence
- `book/src/L4/krylov-step.md` (firm; cycle-006) — the target entry. `grep -n "cg\.md"` returns 8 lines (14, 82, 96, 126, 133, 152, 170, 171); line 126 is a bare-filename mention (no range, not a sweep target); the other 7 carry dangling ranges and are re-anchored.
- `book/src/spec/slices/cg.md` (165-line reduced stub; verified `wc -l = 165`) — the reduced slice. Stub header (lines 5-14) names the firm supersessors: `L2/krylov-step.md` (§"L2"), `L4/krylov-step.md` Form A (§"L4" v0.1-v0.4), `L3-L2/krylov-step-body-identity.md` (§L3>L2), `concepts/derived-view-hoisting.md` (the residual-norm hoisting worked example, stub line 14). Stub lines 16, 27-141 retain the unique L4-v0.5 first-iteration-unrolling material LIVE (`cg_first_step` stub:52, `cg_steady_step` stub:69, driver stub:95-108, `forget_beta_prev` stub:129). All four re-anchored ranges (`172-188`, `325-339`, `352-362`, `393-425`) exceed line 165 → all dangle at the cited range.
- `book/src/L3-L2/krylov-step-body-identity.md:125` (firm; §Status line 154) — firm home for the combinator-miner Claim 1/2 body-identity assertion (Re-anchors 1, 4); preserves the verbatim Claim-2 quote at original slice line 360. (Carries its own dangling `cg.md:341-362` provenance pointer — the separate `l3-l2-body-identity-cg-md-citation-sweep` residual.)
- `book/src/concepts/derived-view-hoisting.md:14-19` (firm concept) — firm home for the CG residual-norm hoisting worked example (Re-anchor 3); named as supersessor in the reduced `cg.md` stub header (stub line 14). Bare `../spec/slices/cg.md` link at line 16 resolves (no range).
- `book/src/concepts/sequential-obstruction.md` (firm concept; definition lines 1-16) — read for the outer-loop obstruction narrative; NOT a re-anchor target in THIS entry (the L4 entry does not carry an outer-loop-obstruction `cg.md` pointer — that family lives in the L3 entry, swept cycle-015), included for completeness of the family-mapping audit.
- `book/src/L2/krylov-step.md` §Evidence (line 138 step bodies, line 146 outer-driver consumer sites; firm cycle-005) — the L2 narrative registry naming the CG L4 Form A / Form B step-body instances (Re-anchors 5, 6 Form A); the L4 entry's own §Evidence (line 176) already declares L2 the transitive anchor. (Carries its own dangling `cg.md` pointers — the pending `l2-krylov-step-cg-md-citation-sweep` sibling sweep; see Open questions.)
- `book/src/spec/slices/arnoldi_step.md` (302 lines; live, §L2/L3/L4 retained) — the co-anchor `arnoldi_step.md:185-188` (variant-dependent obstruction + Hessenberg-column-write identity) for Re-anchors 1/4 is a TERMINAL live range, not re-anchored.
- `reports/2026-05-28T202234Z-lifter-l3-krylov-step-cg-md-citation-sweep/CYCLE.md` (cycle-015 sibling) — enumerated this exact L4 pointer set in §"Open questions / caveats" (line 90) and routed it to OQ `l4-krylov-step-cg-md-citation-sweep`.
- `reports/2026-05-28T193413Z-lifter-krylov-step-typed-wrapper-dissolution-cg-md-citation-sweep/CYCLE.md` (cycle-014 sibling) — established the lifted-evidence annotation convention this sweep applies.

## Open questions / caveats
- **`L2/krylov-step.md` and `L3-L2/krylov-step-body-identity.md` carry their OWN dangling `cg.md` pointers — the firm narrative homes are authoritative for the CLAIM but their citation hygiene is a separate residual.** My re-anchors point this L4 entry's pointers AT firm `book/` entries (the L2 §Evidence registry; the L3-L2 body-identity §Verified-against; the `derived-view-hoisting` concept page; the live `cg.md` stub retained section) that hold the content in firm vocabulary. Three of those firm targets STILL carry their own dangling `cg.md` slice ranges in their own provenance notes: `L2/krylov-step.md` (lines 116/119/138/146/172 etc. — the `l2-krylov-step-cg-md-citation-sweep` sibling that cycle-015 already flagged and routed), and `L3-L2/krylov-step-body-identity.md:125-128` (the `cg.md:341-362` + `gmres.md:459-471` provenance — a NOT-YET-routed `l3-l2-body-identity-cg-md-citation-sweep` residual I observed during this sweep). This does NOT invalidate them as the authoritative narrative homes for the claims (the content lives there in firm L2 / L3>L2 vocabulary regardless of those entries' own citation hygiene — exactly the precedent the cycle-015 L3 sweep set when it pointed at L2:138/:146 despite L2's own pending sweep). **Recommend**: keep the existing `l2-krylov-step-cg-md-citation-sweep` OQ open; OPEN a NEW OQ `l3-l2-body-identity-cg-md-citation-sweep` for the body-identity theme's own dangling `cg.md:341-362` provenance pointer (a one-pointer cg.md sweep plus a `gmres.md` co-pointer that is out-of-scope-for-cg). Both are separate lifter dispatches (DIFFERENT files), exactly as cycle-014 routed the L3 residual and cycle-015 routed this L4 residual.
- **`concepts/first-iteration-unrolling.md` Form-B citations not audited this dispatch.** Re-anchors 2/5/6 name `concepts/first-iteration-unrolling.md` as the firm concept home for the v0.5 Form B material (consistent with the reduced `cg.md` stub header line 16 naming it the canonical consumer). I did NOT open that concept page to check whether IT carries dangling `cg.md:393-425` pointers — if it does, that is a `first-iteration-unrolling-cg-md-citation-sweep` residual for a future dispatch. The v0.5 Form B material IS live in the stub (`cg.md:27-141`), so the concept page's pointers (if range-bearing) may or may not dangle depending on whether they cite the pre-reduction `393-425` range or the retained-stub range — out of scope for THIS L4-entry sweep; flagging for completeness only.
- **No abstractor reread needed.** The firm homes (`L3-L2/krylov-step-body-identity.md`, `concepts/derived-view-hoisting.md`, `L2/krylov-step.md`, the live `cg.md` stub) do not contradict any signature, semantics, algebraic law, variant-axis profile, or applicability condition the L4 entry asserts. The firmed-up vocabulary matches the entry's rough-in assumptions exactly (Form A / Form B split, the demand-pruning Law 1, the L3>L2 body-identity). This was a pure vocabulary/citation re-anchor; the entry's `firm` status is correct and unchanged.
- **OQ `l4-krylov-step-cg-md-citation-sweep` closure.** This dispatch addresses that OQ in full for `book/src/L4/krylov-step.md` (all 7 dangling range-pointer lines re-anchored; the 1 bare-filename mention correctly left untouched). Integrator may mark it `answered` with answer-link this CYCLE.md, contingent on the 6 re-anchor blocks landing. The `l2-` (open) and proposed-new `l3-l2-body-identity-` / `first-iteration-unrolling-` sibling sweeps are the residuals; recommend NEW OQs rather than holding this one open.
