---
verifies: ../CYCLE.md
critiqued_at: 2026-05-30T053000Z
critic_version: 1
checks:
  citation-validity: pass
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: pass
  cross-reference-integrity: pass
  edge-label-fidelity: pass
  plan-kind-consistency: pass
  skill-uptake-survey: pass
---

# META: verification of lifter back-solve-mutation-rotation Sub-pattern B narrative repair

## Critique

### Checks run

**citation-validity — pass.** The report's load-bearing factual claim — that GMRES `iterative.cpp:653-660` and FGMRES `:832-839` are byte-identical, with both arms placing `{` on its own line and the offset between them being a uniform +179-line file offset with zero local relative shift — was independently re-verified this critic dispatch. (i) Ran `cmp <(sed -n '653,660p' …) <(sed -n '832,839p' …)` → returned identical (8-line block byte-match); (ii) ran `cmp <(sed -n '645,660p' …) <(sed -n '824,839p' …)` → returned identical (16-line block byte-match, confirming the byte-identity extends backward through the preceding outer-`for(;;)` break-out epilogue, exactly as the report claims); (iii) direct `Read` of `:650-664` and `:829-843` showed `:654` and `:833` are both bare `{` lines on their own (brace-on-its-own-line style identical in both arms), `:658` and `:837` are both bare `{` lines (inner-loop), `:832 − :653 = +179` confirmed, and the report's per-line correspondence `653↔832 / 655↔834 / 656↔835 / 657↔836 / 659↔838 (+179 each)` matches the file content; (iv) `python3 tools/citecheck/citecheck.py --scan` over the report → `13 ok, 0 failing`; (v) `citecheck --anchor 'for (int i = j; i >= 0; i--)'` on both `:653-660` and `:832-839` → ok, anchor at 653/832, zero-drift; (vi) `citecheck --anchor 'using GmresSolver'` on `iterative.hpp:250` and `--anchor 'std::vector<VecType> Z'` on `:256` → both zero-drift. The prior "+1-line brace-placement shift" claim the report is repairing is indeed wrong, and the new "byte-identical, +179-line file offset" claim is independently verified correct. **`verified_against:` YAML round-trip sub-check on the flipped `:832` row → ROUND-TRIP OK** (`python3 -c "import yaml; yaml.safe_load(…)"` on the extracted block parsed and re-emitted without `ParserError`; the `note:` value is single-quote-delimited and its scalar body begins with `F` (prose) — the quote is the YAML delimiter, not a leading-quote-character of the scalar body, satisfying the friction-ledger `verified-against-note-no-leading-quote-of-either-kind` invariant).

**surface-or-evidence — pass.** This is a refinement-shaped proposal (changes to an existing firm + integrated L1>L0 theme); the proposal modifies surface (5 narrative edits to `book/src/L1-L0/back-solve-mutation-rotation.md` — §Sub-pattern B prose at :198-244, §Variant axes GMRES-vs-FGMRES bullet at :575-580, §Justification kind Sub-pattern B clause at :518-521, §Status two-form-bullet at :729-731, §Verified-against `:832` row at :811-814); and the surface change is fully evidenced by L0 citations (cmp byte-identity, citecheck anchors, direct read of brace-style lines). Not a pure rotation-claim-without-surface case; not a pure retroactive evidence backfill case either — it is a surface correction grounded in re-verified L0 evidence, which is exactly the in-scope lifter authority for "L0-evidence-driven prose correction is in-scope when bounded + evidenced + recorded".

**rotation-quality — pass.** Not applicable in the strict sense — this is a narrative repair within an already-firm theme, not a fresh rotation claim. The underlying L1>L0 rotation (the four-element rewrite, the in-place RHS-as-destination collapse, the flat column-major stride trick) is unchanged; only the prose describing GMRES↔FGMRES correspondence is corrected. The repair's content shifts the description from "+1 brace-style line-shift" (wrong) to "byte-identical, +179-line file offset, zero local relative shift" (correct) — neither framing is a "renaming-only" trick; the underlying rotation it describes remains the same firm structural rewrite. No degradation of rotation quality results.

**variant-axis-coverage — pass.** Not applicable in the strict sense — the proposal does not modify the variant-axis taxonomy. The §Variant axes edit (edit 2) re-states the GMRES-vs-FGMRES bullet's *narrative content* (byte-identical instead of content-identical-line-shifted) but does NOT add, remove, or re-scope any axis; element-type / GMRES-vs-FGMRES / restart-dimension / pc-side bullets are all preserved as-is in structure. The variant-axis coverage of the theme is unchanged by this repair.

**cross-reference-integrity — pass.** All `[link]` references in the new prose resolve on disk: `[`L1/back_solve`](../L1/back_solve.md)` (target exists, has the correct law-6 phrasing at :223-230), `[`linear-combination-fold-specialization`](../L2-L1/linear-combination-fold-specialization.md)` (un-touched, still resolves), and the L0 citations all anchor cleanly. The 5 proposed-changes blocks' [old] strings each match the on-disk theme verbatim (verified by reading lines :198-244 / :575-580 / :518-521 / :729-731 / :811-814) — no integration drift. **Build-readiness fence-parity guard:** `grep -n '^```' CYCLE.md` shows exactly 10 fence lines at :20/:130 :132/:148 :150/:161 :163/:173 :175/:185 (5 open + 5 close, even parity). The 11th match from `grep -c '```'` is inline backticks in line :199's `Note on fence rendering` prose, not a real fence. Inside each fence the code-snippet sample (only edit 1 carries one) is rendered as a 4-space-indented code block, NOT a nested ` ```text ... ``` ` fence — confirmed by reading the inner content of edit 1's [new] payload at :70-81; this preserves the on-disk Sub-pattern B sample's existing 4-space-indented form (also verified) and avoids the cycle-024 fence-truncation defect. The firm-body-inside-fence guard is not triggered (this is a narrative repair to an already-firm theme, not a firm chapter authoring; the fenced edits carry surgical [old]/[new] payloads, not full apparatus bodies).

**edge-label-fidelity — pass.** The theme carries edge label L1>L0 (`back-solve-mutation-rotation.md`); the prose throughout the report discusses precisely that edge — `back_solve` (L1) ↔ `iterative.cpp:653-660` / `:832-839` (L0). The report's incidental references to L2 `linear_combination` are correctly framed as "outside the leaf, the consuming L2 composition" (boundary marker, not the edge under discussion). No misframing of the edge.

**plan-kind-consistency — pass.** The report's declared shape is a lifter narrative-repair re-anchor (not a fresh firm operator or rough-in). The frontmatter (`agent: lifter`, scope: "L1>L0 theme Sub-pattern B narrative repair"), the §Summary explicit framing ("This is a pure narrative repair within lifter authority"), the §Discipline-notes bounded-correction record, and the §Open-questions explicit "the theme stays `## Status: firm`" all consistently align with the bounded-prose-correction kind. No mis-classification: no signature change, no new decomposition, no new sub-pattern, no variant-axis change — exactly the bounded re-anchor shape.

**skill-uptake-survey — pass.** The report references `tools/citecheck/citecheck.py --anchor` invocations (§Supporting evidence — both `:653-660` and `:832-839` anchored, zero-drift); the cycle-024 fence-truncation guard / `convert-nested-fences-to-indented-code-in-proposed-changes-block` skill is explicitly referenced in §Discipline-notes ("Note on fence rendering"). For the `verified_against:` row flip, the friction-ledger `verified-against-note-no-leading-quote-of-either-kind` invariant is observed (note begins with the single-quote delimiter, then `F` (prose) — the quoted body starts on prose, not a quote-character; YAML round-trip confirmed). All relevant skill invocations are surfaced.

### Issues found

**None.** The 8 checks all pass cleanly. The load-bearing factual claim (byte-identity between GMRES `:653-660` and FGMRES `:832-839`) was independently re-verified by this critic with `cmp` (8-line and 16-line ranges), direct `Read` (brace-style identical at `:654/:833` and `:658/:837`), and `citecheck --anchor` (zero-drift on both anchors). The prior "+1-line brace-placement shift" claim is indeed wrong, and the new "byte-identical, +179-line file offset, zero local relative shift" claim is correct. The 5 [old] strings match the on-disk theme verbatim. The 5 fenced proposed-changes blocks pass the fence-parity guard (10 fence lines, even parity, 4-space-indented inner code, no nested-text-fence variant). The flipped `:832` verified_against row round-trips through `yaml.safe_load` cleanly and observes the no-leading-quote-of-either-kind invariant. The repair is bounded, evidenced, and recorded as required by lifter authority. No issues for the repairer to address.
