---
agent: cycle-planner
invoked_at: 2026-06-01T172507Z
scope: cycle-048 dispatch plan
status: pending
---

# Cycle 048 dispatch plan

(THIRD/FINAL primary cycle of meta-batch-14 — cycles 046/047/048; the batch-14 meta-phase fires AFTER this cycle's integrator-finalize as a separate dispatch.)

## Goals selected this cycle

Land the two iterative-solve **L4 caps** unblocked by the cycle-047 `solve-monad` outer-driver anchor (`solve_loop`/`restart_cycle`/`Outcome` now firm in `L4/index.md`): **R2 `L4/ksp_solve.md`** (highest absolute fan-out — the Krylov chain's missing L4 driver-half) and **R3 `L4/eigsolve.md`** (shares the `solve-monad` vocabulary; opaque-library obstruction so the cap is mostly an `Outcome`-sum/role wrapper). Both lift against the firm outer-driver vocabulary under the L4 strawman conventions (`book/src/design/l4_calculus.md`). Each cap landing **firms a backing page for the c047 firm dep-map rows** (resolving the `solve-monad-l4-row-firm-maturity-straddle`) and is **coupled in-cycle to its parent L3 §Upward re-anchor** (the codified floor-landing-reanchor convention — load-bearing this cycle). A single `layer-intro-author` owns the shared `L4/index.md` consolidated Firm-count tally + §Queued-at-L4 prose flip. R5 (`L4/orthogonalize.md`) stays deferred. After these caps the L4 frontier is plausibly near-exhausted — flagged for the batch-14 meta-phase (fires after c048).

## Dispatches

**D1 — `harvester` — L4 cap `book/src/L4/ksp_solve.md` (R2; the lead) + its own L3 `ksp_solve` §Upward/solve-monad-ref re-anchor (floor-landing coupling, form (i))**
- **scope:** Author the firm L4 outer-driver cap over the firm L3 `ksp_solve` driver, under the L4 strawman conventions (Haskell `::` signatures / TypeScript records / `do`-notation bodies / `$$` reduction rules per `book/src/design/l4_calculus.md` §§3.6–3.7 + the firm `L4/krylov-step.md` precedent). Consumes the firm c047 outer-driver vocabulary: `solve_loop :: OpParams -> Inputs -> Solve ()` (entry `solve op inp = execState (solve_loop op inp) initial_state`), `restart_cycle :: OpParams -> Inputs -> Solve Outcome` (`one_cycle` non-restarted specialisation), and `Outcome = Continue | Done Bool` (the 3-arm soft-fail lift of L3's `result.converged : Bool`). Carry the genuine per-operator harvest depth the c047 vocabulary rows do NOT carry (per OQ `solve-monad-l4-row-firm-maturity-straddle` cycle-048 follow-up note): the monad-law / `execState`-`StateT` fusion identities, the `solve_loop` tail-recursion-as-`iterate_while` fold equivalence, the full `Outcome`-classification variant axis (the `ksp_solve` 3-arm shape), the demand-pruning interaction with the outer driver. L0/L1/L3 anchors all firm + on-disk-verified (do NOT re-localize from memory): L3 driver bodies `palace/linalg/iterative.cpp:361-486` (CG single-loop) / `:544-705` (GMRES restart-nested) + the `IterativeSolver` base `iterative.hpp:25-115` + the soft-fail policy `ksp.cpp:296-310` (esp. `:301-307`); the firm L3 parent `book/src/L3/ksp_solve.md` + L1 `book/src/L1/ksp_solve.md` + the concept page `book/src/concepts/solve-monad.md`. **DUAL-REGISTRATION (state explicitly):** D1 adds BOTH (1) its OWN `L4/index.md` dep-map TABLE row for `L4/ksp_solve` (live link, page now exists) AND (2) its OWN §Vocabulary-cohort BULLET in the Firm-at-L4 cohort; D1 DEFERS (3) the consolidated Firm-count tally (`(4 + 3 outer-driver)` → `(5 + 3 outer-driver)` after D1, → `(6 + 3 outer-driver)` after D1+D3) and the §Queued-at-L4 prose flip to **D4 (the count-owner)** — D1 does NOT touch the `:32` count token or the `:53-58` Queued prose. **FLOOR-LANDING RE-ANCHOR (form (i), bundled into D1's own proposed-changes):** `L3/ksp_solve` carries NO false "no L4 cap exists" assertion (verified — zero such grep hits), so this is a **live-link upgrade**, NOT a stale-assertion fix: upgrade the solve-monad forward-references that now have a firm cap target — §Semantics `:78`, Dependencies `:142`, Variant-axes `:160` (the `Outcome`-sum lift), and §Lifts-from / §Context "L4 outer-driver surface" mentions — from plain-text/concept-page-only framing to cite the now-firm `L4/ksp_solve` cap as the upward home (apply `upgrade-plain-text-ref-to-live-link-when-target-on-disk` where the reference is to `ksp_solve`'s own cap). Keep `L3/ksp_solve` status `firm` (unchanged).
- **deps:** none (the prerequisite `solve-monad` vocabulary is firm on disk from c047).
- **rationale:** R2 of the c046 survey — the highest-absolute-fan-out L4 expansion (the calculus's solver-coordination vocabulary the downstream burn effort consumes). Serves `l4-l3-coverage-and-l4-expansion` (Backlog High, lead frontier) + the cap-half of OQ `l4-ksp-solve-eigsolve-caps-gated-on-solve-monad-outer-driver-vocabulary` + resolves the `solve-monad-l4-row-firm-maturity-straddle` for the `solve_loop`/`restart_cycle` rows.

**D2 — `harvester` — L4 cap `book/src/L4/eigsolve.md` (R3) + its own L3 `eigsolve` §Upward re-anchor (floor-landing coupling, form (i) — the 7 enumerated stale sites)**
- **scope:** Author the firm L4 outer-driver cap over the `partial-obstruction` L3 `eigsolve`, under the same L4 strawman conventions. Consumes the SAME outer-driver surface as D1 but with a **richer `Outcome`-sum termination**: the partial-success arm `0 < converged < requested` (no `ksp_solve` analog) per `book/src/L1/eigsolve.md:78` + `book/src/L3/eigsolve.md:166`. Because the eigen-iteration is **opaque-library-owned** (SLEPc `EPSSolve` `palace/linalg/slepc.cpp:694` — codemap-confirmed this cycle; ARPACK `naupd` RCI `palace/linalg/arpack.cpp:318` — codemap-confirmed), the cap is mostly a **role / `Outcome`-wrapper over an obstruction marker**, NOT a clean `iterate_while` fold — state this explicitly (the L4 form names the eigen-iteration by role and marks the obstruction; it does not render a Palace-authored loop because Palace authors none). Decide the `Outcome`-sum shape per OQ `outcome-sum-one-row-vs-per-cap-specialisation`: the c047 anchor took the single-pattern reading (`Outcome = Continue | Done Bool` canonical, `eigsolve` extends it); if this cap finds the partial-success arm wants its OWN L4 row (`EigOutcome` or an extended sum), that is a clean ADDITION — register it as D2's own row, do NOT contradict the c047 anchor. Anchors all firm/on-disk-verified: the firm L2 parent `book/src/L2/eigsolve.md`, the partial-obstruction L3 `book/src/L3/eigsolve.md`, L1 `book/src/L1/eigsolve.md`, the body `apply_shift_invert = apply_linop ▷ ksp_solve ▷ scale_untransform [▷ project]`. **DUAL-REGISTRATION (state explicitly):** D2 adds BOTH (1) its OWN `L4/index.md` dep-map TABLE row for `L4/eigsolve` (live link) AND (2) its OWN §Vocabulary-cohort BULLET; D2 DEFERS (3) the consolidated Firm-count tally + §Queued-at-L4 prose flip to **D4** — D2 does NOT touch the `:32` count token or the Queued prose. **FLOOR-LANDING RE-ANCHOR (form (i), bundled into D2's own proposed-changes; the LOAD-BEARING coupling this cycle):** `L3/eigsolve` carries SEVEN explicit "no firm L4 `eigsolve` exists / `L4/eigsolve.md` not yet authored / L4 surface is a future dispatch" assertions that go FALSE the moment this cap lands — re-anchor ALL of them: `:19` (intro), `:34` (§Upward), `:78` (§Semantics no-Solve-monad), `:166` (Dependencies solve-monad row), `:172` (the explicit "L4: eigsolve — not yet authored" line), `:203` (§"L3 vs L1/L2 distinction"), `:214` (§Lowers-to/Lifts). Rewrite each from "not yet authored / future dispatch" to "lowers from the firm `L4/eigsolve` cap (opaque-library `partial-obstruction` lift; the loop is marked-not-rendered)". Keep `L3/eigsolve` status `partial-obstruction` (unchanged — the cap does not change the loop's library-ownership).
- **deps:** D1 (shared `solve-monad`/`Outcome` vocabulary — D2 reuses D1's `Outcome`-sum framing and may live-link `L4/ksp_solve`; sequence after R2 per the survey). Same-file (`L4/index.md` dep-map + cohort) co-write with D1/D4 → serial ordering, see Overlap analysis.
- **rationale:** R3 of the c046 survey — shares the `solve-monad` vocabulary; the second iterative-solve cap. Serves `l4-l3-coverage-and-l4-expansion` + the cap-half of OQ `l4-ksp-solve-eigsolve-caps-gated...` (eigsolve half) + closes the 7 stale L3-eigsolve §Upward sites same-cycle (the floor-landing-reanchor convention).

**D3 — `abstractor` — L4>L3 theme `book/src/L4-L3/ksp-solve-driver-dissolution.md` (the dedicated rotation theme for the R2 cap)**
- **scope:** Author the dedicated L4>L3 rotation theme narrating forward how the firm `L4/ksp_solve` cap (D1) lowers into the L3 `ksp_solve` driver form. Substantive content = the **`Solve`-monad dissolution**: `do`-block / `StateT` threading → explicit `iterate_while_L3` tail recursion + positional `(K, s)` threading; the `solve_loop`/`restart_cycle` outer drivers → the L3 outer fold + restart nesting; `Outcome = Continue | Done Bool` → the scattered L3 per-test termination branches + `result.converged : Bool` (the `do`/`unless` → predicate-read-of-`s.converged` collapse). This is the L4>L3 analog the survey flagged as warranted for the cap (parallel to how `krylov-step-typed-wrapper-dissolution` covers the kernel half; the c047 vocabulary rows note "L4>L3 theme rides the `L4/ksp_solve` cap (cycle-048)"). Cite the firm `book/src/L4-L3/krylov-step-typed-wrapper-dissolution.md` §"What the L3 form for `iterate_while` looks like" + the c047 `book/src/L4-L3/iterate-while-dissolution.md` for the inner kernel-fold dissolution this composes above. **DUAL-REGISTRATION:** D3 adds its OWN `L4-L3/index.md` dep-map row + §Vocabulary-cohort bullet (substantive driver-dissolution sub-grouping). **Count note:** `L4-L3/index.md` carried NO consolidated tally in c047 (the c047 finalize recorded D1/D2 "just append rows since L4-L3/index has no consolidated tally") — verify this holds; if a consolidated L4>L3 firm-count exists, D3 is the sole L4-L3/index count-owner this cycle (only L4>L3 landing). The `L4/index.md` Firm-at-L4 tally is D4's, NOT D3's (D3 lands an L4>L3 theme, not an L4 operator).
- **deps:** D1 (the `L4/ksp_solve` cap must exist on disk so D3's forward-narration LHS resolves to a live link; sequence D3 after D1 — wave-2).
- **rationale:** completes the R2 cap with its dedicated rotation theme (the survey's R2 deliverable was "L4 cap + its `L4-L3/ksp-solve-driver-dissolution` theme"). Whether `eigsolve` warrants a *dedicated* L4>L3 theme is assessed in `## Open questions / caveats` (likely NOT — the loop is opaque-library-owned, so the "dissolution" is a marker-erasure better noted in-line in the eigsolve cap; deferred to the meta-phase / c049). Serves `l4-l3-coverage-and-l4-expansion`.

**D4 — `layer-intro-author` — `L4/index.md` SOLE consolidated-count owner (Firm-at-L4 tally + §Queued-at-L4 prose flip) + L4 cohort narrative refresh**
- **scope:** SOLE owner this cycle of the shared `L4/index.md` consolidated aggregates that D1/D2 defer: (a) the **Firm-at-L4 count tally** `:32` `**Firm at L4 (4 + 3 outer-driver)**` → `**Firm at L4 (6 + 3 outer-driver)**` (the two new caps `ksp_solve` + `eigsolve` — read the actual landed count from disk after D1/D2 apply; if either cap lands at a non-firm maturity adjust accordingly); (b) the **§Queued-at-L4 prose flip** `:53-58` — the two iterative-solve CAP chapters are no longer queued (both authored this cycle); rewrite "What remains queued are the per-operator CAP chapters..." to reflect the caps are now landed + note whether anything remains queued (per the near-exhaustion read, likely "none currently — the L4 frontier is substantially complete; see batch-14 meta-phase"); (c) any §Vocabulary-cohort narrative motif refresh now that the outer-driver-consuming caps exist (the `solve-monad` cohort now has its two first consumers on disk). D4 does NOT author D1's or D2's own dep-map rows or own cohort bullets (those are producer-owned per dual-registration) — D4 writes ONLY the consolidated tally + Queued prose + cohort narrative.
- **deps:** D1 + D2 (must read the actual landed cap maturities from disk to write the correct consolidated count; wave-2/3).
- **rationale:** count-ownership convention (cycle-039 meta) + dual-registration partition (cycle-045 meta) — ≥2 caps bump the shared `(4+3)` Firm-count tally, so exactly ONE owner writes it. The `layer-intro-author` owns the index narrative + Queued prose, so it is the natural count-owner. Serves the count-ownership / dual-registration discipline.

## Overlap analysis

- **D1 × D2** — Both author a NEW distinct chapter file (`L4/ksp_solve.md` / `L4/eigsolve.md`) — non-overlapping at the chapter level. BUT both **append a dep-map TABLE row + a §Vocabulary-cohort BULLET into the SAME `L4/index.md`** (distinct rows/bullets = anchor-distinct, parallel-safe per the dual-registration rule) AND both re-anchor their OWN (distinct) L3 parent (`L3/ksp_solve` vs `L3/eigsolve`) — non-overlapping. The ONLY genuinely-shared mutable region is the `L4/index.md` consolidated Firm-count tally `:32` + §Queued prose, which BOTH defer to D4 (neither writes it) — so D1/D2 do not actually conflict there. D2 also reuses D1's `Outcome`-sum vocabulary + may live-link `L4/ksp_solve` → forward-reference dependency (D2 after D1). **Verdict: sequential D1→D2** (shared `solve-monad`/`Outcome` framing + same-file index appends; the survey explicitly sequences R3 after R2). Per conflict-tolerance philosophy the index-row appends alone would be parallel-safe, but the shared `Outcome` vocabulary framing is a genuine content dependency → sequence.
- **D1 × D3** — D3's theme forward-narrates FROM `L4/ksp_solve` (D1's output): D3 names/links the cap D1 creates → forward-reference. **Sequential D1→D3.** Different files otherwise (`L4-L3/ksp-solve-driver-dissolution.md` + `L4-L3/index.md` vs D1's `L4/ksp_solve.md` + `L4/index.md`) — no region overlap beyond the forward-link.
- **D1 × D4 / D2 × D4** — D4 writes the `L4/index.md` consolidated tally + Queued prose that D1/D2 explicitly DEFER; D4 must read the landed cap maturities from disk → D4 after D1+D2. Same-file (`L4/index.md`) but **partitioned by region** (D1/D2 own their rows+bullets; D4 owns the tally+Queued prose) — the dual-registration partition makes this clean, but D4 reads-after-write so it is **sequential D1,D2 → D4** (wave-2/3).
- **D2 × D3** — D3 caps `ksp_solve` (not `eigsolve`); different files; no shared region. D3 depends on D1 not D2 → D2 and D3 are mutually independent (both wave-2, can run parallel to each other after D1).
- **D3 × D4** — different index files (`L4-L3/index.md` vs `L4/index.md`); no overlap. Parallel-safe; both wave-2/3 after their respective deps.

## Sequencing schedule

- **Wave 1 (after planner):** **D1** (`L4/ksp_solve.md` cap + its L3 ksp_solve live-link re-anchor). Sole wave-1 dispatch — it is the shared-vocabulary anchor D2/D3/D4 all build on.
- **Wave 2 (after D1's report lands; parallel):** **D2** (`L4/eigsolve.md` cap + 7-site L3 eigsolve re-anchor; reuses D1's `Outcome` framing) **and** **D3** (`L4-L3/ksp-solve-driver-dissolution.md` theme; forward-links D1's cap). D2 and D3 are mutually independent.
- **Wave 3 (after D1+D2 reports land):** **D4** (`L4/index.md` consolidated Firm-count tally + §Queued prose flip; reads landed cap maturities from disk).

(Waves order DISPATCH / forward-reference; the book is NOT rebuilt between waves and there is exactly ONE `integrator-finalize` at cycle end. Per-report integrators apply D1→D2→D3→D4 serially; the single finalize build resolves all same-cycle live links — D2→D1, D3→D1 — green, as in c047.)

## Deliverable-presence verification

Per the MANDATORY paste-inline-evidence procedure. Every dispatch resolves to a named `book/src/` path; all checks run on disk this cycle.

**D1 — `L4/ksp_solve.md` (R2 cap):**
1. **File existence:** `ls book/src/L4/ksp_solve.md` →
   ```
   ls: cannot access 'book/src/L4/ksp_solve.md': No such file or directory
   ABSENT: L4/ksp_solve.md
   ```
   ABSENT — open by construction (fresh cap, no prior-cycle history). `ls book/src/L4/` → `chebyshev.md  index.md  iterate-while.md  iterate-while-with-prev.md  krylov-step.md` (no ksp_solve).
2. **Maturity check:** N/A (file absent — nothing to no-op against).
3. **Parent firm:** `grep -A1 '^## Status' book/src/L3/ksp_solve.md` → `:167: \`firm\` — the value-threaded fold ... canonical iteration-rotation form for the preconditioned-Krylov outer-driver loop ...`. Parent FIRM. Prerequisite `solve-monad` vocabulary firm on disk: `grep solve_loop book/src/L4/index.md` → `:41 solve_loop` + `:66` dep-map row tagged `\`firm\` (outer-driver vocabulary anchor; cycle-047 ...)`. PRESENT + FIRM.
4. **Structural-block check:** the cap was structurally blocked on the `solve-monad` outer-driver vocabulary anchor (OQ `l4-ksp-solve-eigsolve-caps-gated-on-solve-monad-outer-driver-vocabulary`); that gate is **discharged** — the anchor-half landed c047 (`scaffolding/open-questions.md:742` "ANCHOR HALF DISCHARGED ... caps now unblocked but not yet authored (cycle-048)"). Gate CLEARED. **PASS.**

**D2 — `L4/eigsolve.md` (R3 cap):**
1. **File existence:** `ls book/src/L4/eigsolve.md` →
   ```
   ls: cannot access 'book/src/L4/eigsolve.md': No such file or directory
   ABSENT: L4/eigsolve.md
   ```
   ABSENT — open by construction.
2. **Maturity check:** N/A (absent).
3. **Parent present:** `grep -A1 '^## Status' book/src/L3/eigsolve.md` → `:191: \`partial-obstruction\` — the per-step body ... lifts cleanly ... the eigen-iteration loop is a witnessed sequential-obstruction rooted in opaque-library-ownership ...`. Parent is `partial-obstruction` (the iteration-rotation home — firm-enough as a cap target; the cap lifts the body + marks the loop, consistent with the partial-obstruction). Firm L2 parent: `book/src/L2/eigsolve.md` referenced firm from `L3/eigsolve.md:19` ("identity-in-form to the firm L2 eigsolve"). Same `solve-monad` prerequisite firm (see D1 step 3). Library source paths codemap-confirmed this cycle: `slepc.cpp:694` `EPSSolve(eps)`, `arpack.cpp:318` `naupd`.
4. **Structural-block check:** same gate as D1 (the `solve-monad` anchor) — DISCHARGED c047. The `partial-obstruction` status is NOT a block on the cap (the cap is the L4 lift of the body + the obstruction-marker, exactly as the L3 entry lifts the body + marks the loop). The 7 stale "not yet authored" §Upward assertions are the re-anchor target, not a block. **PASS.**

**D3 — `L4-L3/ksp-solve-driver-dissolution.md` (R2 theme):**
1. **File existence:** `ls book/src/L4-L3/ksp-solve-driver-dissolution.md` →
   ```
   ls: cannot access 'book/src/L4-L3/ksp-solve-driver-dissolution.md': No such file or directory
   ABSENT
   ```
   (verified: `ls book/src/L4-L3/` → `index.md`, `krylov-step-typed-wrapper-dissolution.md`, `gmres-inner-loop-iterate-while-migration.md`, `fgmres-inner-loop-iterate-while-migration.md`, `iterate-while-dissolution.md`, `iterate-while-with-prev-dissolution.md` — 5 themes + index; NO ksp-solve-driver-dissolution). ABSENT — open by construction.
2. **Maturity check:** N/A (absent).
3. **Parents:** LHS `L4/ksp_solve` is D1's output (forward-reference, wave-2 ordering). The kernel-dissolution precedent `book/src/L4-L3/krylov-step-typed-wrapper-dissolution.md` firm (cycle-008) + `iterate-while-dissolution.md` firm (cycle-047). PRESENT.
4. **Structural-block check:** none — the theme is warranted-once-cap-exists (survey R2 deliverable); the cap exists after D1. **Count-ownership confirmed:** `grep '...firm count token...' book/src/L4-L3/index.md` → (no output) — `L4-L3/index.md` carries NO consolidated firm-count tally (confirming the c047 finalize note); D3 purely APPENDS its own dep-map row + cohort bullet, no count-owner conflict. **PASS** (conditioned on D1, the wave-2 dep).

**D4 — `L4/index.md` count-owner (consolidated tally + Queued prose):**
1. **File existence:** `ls book/src/L4/index.md` → EXISTS (read this cycle). This is a TOUCH of an existing index, not a new file.
2. **Maturity / discharge check:** the §Queued-at-L4 prose `:53-58` currently says the two CAP chapters are queued (`L4/ksp_solve` + `L4/eigsolve`); the consolidated Firm-count `:32` reads `(4 + 3 outer-driver)`. Both go stale the moment D1/D2 land → the touch is NOT a no-op; it is the count-owner write that D1/D2 defer. PRESENT-to-update.
3. **OQ-ledger check:** no RESOLVED/CLOSED entry blocks this — the count-ownership write is the standing convention, not an OQ.
4. **Structural-block check:** none — this is the count-ownership convention enactment (cycle-039 meta + cycle-045 dual-registration). **PASS.**

**R5 `L4/orthogonalize.md`:** NOT dispatched (marginal-defer per OQ `l4-orthogonalize-cap-marginal-defer`; gate = a firm L4 Arnoldi consumer, not yet present). Confirmed absent (`ls` → not found) but intentionally NOT recruited.

**STOP-PROPOSING NEGATIVE LIST cross-check:** none of D1–D4 match a disqualified slug (`lu_solve`, `back_solve`, `ls-update-column`, the 4 NLEPS atoms; `apply_nonlinear_pencil` HELD). `ksp_solve` / `eigsolve` are firm/partial-obstruction L3 operators with the marker-(c) outer-driver structure that warrants an L4 cap — NOT on any negative list. PASS.

## Open questions / caveats

- **Does `eigsolve` warrant a DEDICATED L4>L3 theme (an `eigsolve` analog of D3)?** I did NOT dispatch one. My read: probably NOT — the eigen-iteration is opaque-library-owned (no Palace-authored loop to dissolve), so the L4>L3 "rotation" is a marker-erasure (the L4 cap names the eigen-iteration by role + marks the obstruction; the L3 marks the same obstruction) — better recorded **in-line** in the `L4/eigsolve` cap's §Lowers-to (the in-line non-adjacent/identity-style convention for a no-removable-recurrence case), parallel to how `chebyshev`'s L4>L3 is in-line-by-design. This mirrors the c046 survey's framing (R3 is "mostly an `Outcome`-sum/role wrapper", not a dissolution). I instruct D2 to record the eigsolve L4>L3 relationship in-line in the cap rather than as a separate theme; if the meta-phase or a c049 abstractor judges a dedicated theme warranted, it is a clean follow-up addition. **Flag for the batch-14 meta-phase.**
- **`Outcome`-sum: one row vs per-cap specialisation (OQ `outcome-sum-one-row-vs-per-cap-specialisation`, KEEP-OPEN).** D2 may find the eigsolve partial-success arm (`0 < converged < requested`) wants its own L4 row/sum rather than an extension of the canonical `Outcome`. I instruct D2 to treat a separate row as a clean ADDITION (not a contradiction of the c047 single-pattern anchor). The disposition is a legitimate c048 cap-author decision; record the outcome for the meta-phase.
- **`solve-monad-l4-row-firm-maturity-straddle` (OQ):** the c047 anchor flagged that the cap authors may find the firm vocabulary rows under-specified for the cap body, possibly warranting a thin harvester row-depth pass on the outer-driver vocabulary before/with the caps. I did NOT schedule a separate row-depth pass — I instruct D1 (the first/lead cap) to carry the genuine per-operator harvest depth (the monad-law / `execState`-fusion identities, the full `Outcome` variant axis) directly in the cap body, which firms the backing page for the `solve_loop`/`restart_cycle` rows and discharges the straddle. If D1 finds the vocabulary rows themselves need deepening (vs just the cap page), that surfaces as a c049/meta-phase follow-up.
- **L4 frontier near-exhaustion — THE BATCH-14 META-PHASE ASSESSMENT FLAG (fires after c048).** The c046 survey read L4 as "mostly intentionally complete" (13 of 18 L3 ops carry an on-disk `CONFIRMED-NOT-NEEDED` no-L4-by-design verdict); after this cycle's R2/R3 caps land, the only remaining L4 candidate is R5 `orthogonalize` (marginal, gated on a firm L4 Arnoldi consumer that does not exist). **After c048 the L4 frontier is plausibly exhausted.** The batch-14 meta-phase (fires after this finalize) should assess the next forward direction: (a) the `l4-native-combinator-denominator-completeness-survey` OQ (does the L4 native-combinator denominator include outer-driver coordination vocabulary like `solve_loop`/`restart_cycle` — definitional question raised by the c047 D5 landing, `scaffolding/open-questions.md:748`); (b) width/depth consolidation of the now-substantially-complete stack; (c) whether the project pivots from the layer-construction frontier toward the downstream burn-component effort the calculus vocabulary was built to serve. I flag this prominently so the meta-phase has a clear "is L4 done, and what's next?" decision teed up.
- **Count-owner reads landed maturity from disk (not from this plan).** D4 must write the Firm-count `:32` from the ACTUAL on-disk landed cap maturities after D1/D2 apply — I project `(4+3)` → `(6+3)` assuming both caps land firm, but if D2's `eigsolve` cap lands at a qualified maturity (e.g. the cap inherits the parent's `partial-obstruction` framing for the loop-half while the body-lift is firm), D4 adjusts the count + cohort narrative to match what is actually on disk. Stated so D4 does not blindly apply the projected number.
- **Floor-landing-reanchor asymmetry (noted for the integrator).** D1's re-anchor is a LIVE-LINK UPGRADE (ksp_solve carries no false "no cap" assertion — verified zero grep hits); D2's is a STALE-ASSERTION FIX (eigsolve carries 7 explicit "not yet authored" sites that go false). Both are bundled into their own cap-harvester's proposed-changes (form (i) of the codified convention) — no separate lifter dispatch needed this cycle. This is the load-bearing coupling; if either harvester's scope proves too large to bundle the re-anchor cleanly, the fallback is a co-scheduled same-cycle lifter (form (ii)) — but I assess the re-anchors as mechanical enough to bundle (the ksp_solve upgrade is ~4 references; the eigsolve fix is 7 enumerated sites with known line numbers).
