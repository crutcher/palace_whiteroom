---
verifies: ../REPORT.md
critiqued_at: 2026-05-29T191500Z
critic_version: 1
checks:
  citation-validity: pass
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: pass
  cross-reference-integrity: pass
  edge-label-fidelity: pass
  plan-kind-consistency: pass
  skill-uptake-survey: pass
repaired_at: 2026-05-29T193000Z
repairer_version: 1
repairs:
  citation-validity: not-needed
  surface-or-evidence: not-needed
  rotation-quality: not-needed
  variant-axis-coverage: not-needed
  cross-reference-integrity: unrepairable
  edge-label-fidelity: not-needed
  plan-kind-consistency: not-needed
  skill-uptake-survey: not-needed
overall_status: needs-revision
follow_up_agent: integrator-per-report
---

# META: verification of L2>L1 `incremental-least-squares-composition-lowering` theme (cycle-027 dispatch-5)

## Critique

### Checks run

**citation-validity — pass.** `tools/citecheck/citecheck.py --scan` returns `35 ok, 0 failing (35 citations checked)` — exactly the report's claim (line 401: "self-verified L0 source"; Status section). All `iterative.cpp` ranges are in-bounds (file has 882 lines). `--anchor` spot-checks on the load-bearing pinpoints all clear mechanically: replay `:634-636` (anchor `ApplyPlaneRotation` at 636), generate `:638` (`GeneratePlaneRotation`), apply `:639` / apply_rhs `:640` (`ApplyPlaneRotation`), beta `:642` (`beta = std::abs`), unitarity comment `:118` (`cs is real`), FGMRES generate `:817` within `:812-821`, back-solve `:652-660` (`Reconstruct the solution` at 652). The c026 `iterative.hpp:193-194` +1 brace-drift correction the report claims (lines 318-320, 368-372) is CONFIRMED on-disk: `--anchor 'ScalarType> s, sn'` resolves to line 193 exact, `--anchor 'RealType> cs'` to line 194 exact — no residual drift. I direct-read `iterative.cpp:630-684` and `:810-849`: the GMRES stream (`:634-642`), the back-solve (`:652-660` `s[i] /= Hi[i]` @656 / `s[k] -= Hi[k] * s[i]` @659), the V reconstruction `x.Add(s[k], V[k])` @666, the right-precond `r.Add(s[k], V[k])` @674 + `ApplyB`/`x += V[0]` @676-677, the FGMRES stream (`:813-821`), back-solve (`:831-840`), and Z reconstruction `x.Add(s[k], Z[k])` @843 all match the report's claimed line numbers and source text verbatim. (A handful of my own `--anchor` re-runs returned NOANC, but only because of shell backslash-escaping in the regex form `s\[i\] /= Hi\[i\]`; the report's own self-verification used literal anchors and the direct source read confirms every cited line. No drift.) The L2/concept anchors `book/src/L2/incremental-least-squares.md:334-340` (deps forward-ref) and `:278-285` (non-law) are in-range and substantively correct (see surface-or-evidence). Concept-page line cites `:22-27` (within 36 lines) and `:7-15`/`:21-23`/`:25-33` (within 38 lines) are in-range.

**surface-or-evidence — pass.** This is a `new:` L2>L1 theme chapter (new surface), not a refinement of existing operator/theme text, so the refinement-surface gate is satisfied by the new-chapter authoring itself; the abundant rotation/lowering evidence (the per-sub-step L0 table + self-verified anchors) backs it. The three load-bearing claims are confirmed against source: (1) **replay-before-generate non-commutative ordering = L2 law 2** — the L2 entry's law 2 (`incremental-least-squares.md:234-243`) states exactly this and the source `:634-638` shows the replay loop strictly preceding `GeneratePlaneRotation`; (2) **fixed sub-step sequence, no MGS/CGS-style axis** — the L2 entry §Variant axes (`:370-374`) explicitly states "There is no Givens-variant axis on the reduction-ordering — the replay-then-generate sequence is fixed and load-bearing," and both axes are typed "parametric absorption"; the report's contrast against the sibling's three-way `[dot,axpy]` sequence selection is well-grounded; (3) **NO MPI collective, unlike the orthogonalize sibling's GlobalSum** — I confirmed the GMRES/FGMRES stream (`:634-642`/`:813-821`) contains only element-local `ApplyPlaneRotation`/`GeneratePlaneRotation` 2-vector updates with no reduction, whereas the sibling theme's collective-shape table (`orthogonalize-composition-lowering.md:193-197`) records `Mpi::GlobalSum` shapes per MGS/CGS/CGS2. The residue is correctly characterized as rotation-ordering + LAPACK scaling, not a collective shape.

**rotation-quality — pass.** The L2→L1 fan-down is a genuine rotation, not a rename. The LHS is the firm L2 named composition `incremental_least_squares` (one composition-level operation exposing `{state, beta}`); the RHS fans it down into L1 vocabulary along two co-extensive faces (opaque `ls_update_column` leaf / de-fused scalar Givens sub-step sequence) + a terminal back-solve = triangular solve ▷ `linear_combination` reconstruction. The L1 RHS is strictly more decomposed/concrete than the L2 LHS (the four scalar sub-steps + back-solve stages are made explicit where L2 held them as one named-composition step), and the rewrite carries real content (the fixed replay-before-generate ordering, the pinned finite-precision reduction path). This is the correct lowering direction (more concrete at L_n), faithful to the L2 entry's decomposition.

**variant-axis-coverage — pass.** Two orthogonal axes are present and both explicitly handled: `op.variant ∈ {real, complex}` (scalar-kernel element-type substitution; applicability condition 3, lines 256-259, cited to `iterative.hpp:193-194` + `:227-241`) and `op.basis_kind ∈ {V, Z}` (GMRES/FGMRES reconstruction basis; condition 4, lines 261-265, cited to `:666`/`:674-677`/`:843`). Both are correctly classified "parametric absorption" matching the L2 entry §Variant axes. The empty-stream (`j=-1`) and single-column (`j=0` skip-replay) boundary cases are covered (lines 227-234, matching L2 law 5). Householder is explicitly scoped out per the unimplemented-component policy (lines 311-312, 192-193). No hidden branches: the right-preconditioned V-correction sub-branch (`:674-677`) is surfaced, not hidden.

**cross-reference-integrity — pass.** All live `[link]` references in the new chapter body resolve on-disk: `L2/incremental-least-squares`, `L2-L1/orthogonalize-composition-lowering`, `concepts/{incremental-least-squares,givens,givens_generate,givens_apply,plane-rotation-stream,sequential-obstruction}`, `L2/{linear_combination,ksp_solve,krylov-step}` — all 11 verified present. The two forward-references (`ls_update_column`, `trsv`) are correctly backtick-quoted PLAIN TEXT (no live `](...)` link anywhere in the body — grep-confirmed), so they will not break linkcheck2; both targets are genuinely absent on-disk (dispatch-4's `ls_update_column` not yet integrated; no L1 `trsv` entry exists). Build-readiness fence guard: `grep -n '\`\`\`'` returns 6 markers (even parity), three balanced proposed-changes blocks — `new:` (24→459), `edit:index.md` (461→464), `edit:SUMMARY.md` (468→471). The new chapter's `## Status` apparatus is at line 397, INSIDE the new: fence (24-459) — this is NOT the cycle-019 fence-truncation defect; the full body is enclosed. No nested triple-backtick or `~~~` fences; all body pseudo-code uses 4-space-indented code blocks (verified lines 54-62 etc.), the truncation-safe form. The index.md edit anchor (the `orthogonalize-composition-lowering` row at `index.md:16`) exists and the new row's 4-column shape matches the table header; the SUMMARY.md anchor (`orthogonalize-composition-lowering` at `SUMMARY.md:56`) is unique and the new entry is correctly absent (added by this report). New SUMMARY link is a live link (correct — the new chapter file is created on-disk by this report's `new:` block).

**edge-label-fidelity — pass.** The edge label is L2>L1 (theme in `book/src/L2-L1/`, LHS the firm L2 `incremental-least-squares`, RHS the L1 leaves). The prose discusses exactly this edge throughout: "Narrated forward (L2 → L1)" (line 32), the §"L2 form (LHS)" / §"L1 form (RHS)" / §"The fan-down rewrite (L2 → L1)" structure. Direction discipline is correct per the "Layers are defined high→low" invariant: the theme narrates how the L2 form lowers into the L1 form (forward), and explicitly defers the reverse-direction and the L1>L0 in-place mechanics to the forthcoming `ls_update_column` L1>L0 theme (condition 5, lines 267-273; OQ line 435-441). The L1>L0 boundary ("this theme stops at the L1 leaves") is drawn cleanly and consistently, matching the sibling.

**plan-kind-consistency — pass.** Declared `rough-in`; content shape matches. The Status section (lines 399-418) is well-reasoned: the lowering STRUCTURE is fully recognized and exhaustively cited (firm-quality evidence), but the theme is held at `rough-in` for ONE concrete, correct reason — the Face-1 L1 leaf `ls_update_column` (dispatch-4) is not yet on-disk and is forward-referenced as plain text per the rough-in-forward-reference convention. This is the appropriate qualifier: it is NOT a `firm` entry (a load-bearing RHS leaf reference is unresolved), and NOT a weaker tier (the structure + evidence are firm). The promotion condition is explicit and crisp: (a) dispatch-4's `ls_update_column` lands so the Face-1 link resolves; optionally (b) the L1 `trsv` leaf lands. This is a genuinely promotable rough-in gated on an integration-ordering dependency, exactly as the dispatch framing describes.

**skill-uptake-survey — pass.** The report references `verify-citation-range` (Producer self-verification sub-case) for the L0 self-verification and `classify-variant-axis` is the contract the L2 entry's variant-axis output follows; it correctly names `upgrade-plain-text-ref-to-live-link-when-target-on-disk` as the post-dispatch-4 link-upgrade path for the integrator (line 466). The mechanical `tools/citecheck/` realization is used for self-verification. Skill uptake is well-surfaced for a fan-down theme of this shape.

### Issues found

No blocking issues. The report is faithful to the L2 LHS, exhaustively and correctly cited (35/35 citecheck-clean, all load-bearing anchors confirmed including the c026 brace-drift correction), build-ready (full body enclosed, no nested fences, 4-space-indented code), and correctly classified `rough-in` with a crisp promotion condition. Items below are advisory notes for the repairer/integrator, not defects in the report.

1. **INTEGRATION-ORDERING (flag for integrator; informational — not a report defect).** `reports/.../CYCLE.md` Status section + OQ (lines 404-407, 422-427) and the dep-map integrator note (line 466) correctly state that dispatch-4 (the `ls_update_column` L1 leaf) MUST integrate BEFORE dispatch-5 (this theme) so a follow-up (lifter/finalize) can upgrade the Face-1 plain-text forward-ref to a live link. CONFIRMED on-disk: `book/src/L1/` contains no `ls_update_column` entry as of this critique, so if this theme integrated first, the Face-1 reference must remain plain text (which it correctly is) and `linkcheck2` will pass either way — but the link-upgrade follow-up is blocked until dispatch-4 lands. Surfacing this as the dispatch framing requested: the integrator should sequence dispatch-4 before dispatch-5, and the link-upgrade is a post-both-landed action. The report has already wired this correctly (plain-text refs, explicit ordering note); no action needed on the report.

2. **Secondary forward-ref `trsv` — correctly plain-text (no defect).** The back-solve triangular-solve target `trsv` (lines 148-149, 304-307, 411-413, 429-433) has no L1 entry on-disk and is referenced as backtick-quoted plain text throughout (grep-confirmed: zero live links to `trsv`). This is correct per the rough-in-forward-reference convention; the L0 site `iterative.cpp:652-660` is cited and citecheck-clean. It is a secondary promotion-gate (lower priority than `ls_update_column`), correctly recorded as such. No action.

3. **Minor — `correction_basis` typing note (advisory, non-blocking).** The L2-form `back_solve` projection (line 66) reads `op.basis_kind` but the `back_solve :: LsqState' -> {...}` signature line does not thread `op` explicitly (the `select st'.basis (op.basis_kind)` at line 141 references `op` from enclosing scope). This is a cosmetic pseudo-code scoping informality consistent with the L2 entry's own narration style and does not affect the lowering's correctness or the edge fidelity. Noted only for completeness; not a repair target.

---
repaired_at: 2026-05-29T193000Z
repairer_version: 1

## Repair

The dispatch handed me a **coordinated cross-report rename** mandated by cycle-027 dispatch-4's own repair this cycle: dispatch-4's terminal back-solve leaf is being renamed `ls_update_column` → `back_solve` (because `ls_update_column` was already bound to a DIFFERENT operation — the column-streaming step `(K, j, h_new) → K'` — in `L2/incremental-least-squares.md:412` + `concepts/incremental-least-squares.md:14`, while the terminal back-solve is `back_solve` at `L2/...:81-83`). I was asked to rename, in THIS theme, every `ls_update_column` reference that **means the terminal back-solve leaf** → `back_solve`, while preserving (per the explicit CAUTION) any `ls_update_column` reference that means the column-streaming step. I read the theme exhaustively before touching anything; the outcome inverts the instruction's stated premise (see below), so the literal rename is a no-op and the genuine consistency gap is `unrepairable` (cross-report content reconciliation).

### What dispatch-4 actually lands (ground-truth read)

`reports/2026-05-29T175529Z-harvester-ls-update-column-l1/CYCLE.md:49` lands exactly one new L1 file: `new:book/src/L1/back_solve.md`. Its signature is the **small-dense triangular back-solve** `y = back_solve(R, s)` solving `R·y = s` over the `(j+1)×(j+1)` running-QR R-factor (CYCLE.md:50-107). That file explicitly argues it is **NOT a general `trsv`** (`:90-107`). Dispatch-4 does NOT land any `ls_update_column` column-streaming-step leaf. On-disk confirmation: `book/src/L1/` contains neither `back_solve.md`, `ls_update_column.md`, nor `trsv.md` yet (all forthcoming/this-cycle).

### How THIS theme uses the two slugs (exhaustive grep)

A line-by-line audit of every `ls_update_column` occurrence in this theme's CYCLE.md (lines 9, 20, 33, 90, 94, 106, 130, 159, 170, 181, 234, 268, 282, 291, 298, 380, 382, 404, 407, 413-414, 422-423, 435, 438, 453, 463, 466) shows **every single one refers to the per-column running-QR update leaf / column-streaming step** — signature `ls_update_column :: (K: Krylov, j: Int, h_new: HessCol) -> Krylov'` (line 94, Face-1). **None** refers to the terminal back-solve leaf. The terminal back-solve in this theme is consistently referenced as **`trsv`** ("an L1 `trsv` leaf is the natural lowering target, forthcoming" — lines 20, 148-149, 411-413, 429-433, 463, 466) and the L2-level projection name `back_solve` / `trsv_upper` (lines 64-66, 140).

### Fixes attempted

- **Finding (coordinated cross-report rename): rename back-solve-leaf `ls_update_column` → `back_solve`.**
  - **Decision**: not-needed (no matching reference) for the literal rename target; the real consequence is **unrepairable** (see next finding).
  - **Rationale**: The dispatch instruction's premise — "this theme forward-references the terminal back-solve leaf as `ls_update_column`" — is factually inverted for this report. In this theme `ls_update_column` is used ONLY for the column-streaming step (which the dispatch's CAUTION explicitly protects from renaming) and NEVER for the back-solve leaf. Under the instruction's own conditional ("rename only the back-solve-leaf ones ... if the theme only ever uses `ls_update_column` for the streaming step ... [those stay]"), the set of references to rename is **empty**. Renaming the streaming-step references would corrupt the theme (they correctly forward-reference the still-forthcoming `ls_update_column` column-streaming leaf, which dispatch-4 did NOT land). So I applied **zero** `ls_update_column → back_solve` edits — the faithful execution of the rename-with-CAUTION is a no-op here.

- **Finding (consequential, surfaced during the audit): this theme's `trsv` back-solve-target references vs dispatch-4's landed `back_solve` slug.**
  - **Decision**: unrepairable.
  - **Rationale**: The operation this theme forward-references as a forthcoming `trsv` L1 leaf (the small-dense `(R,s)→y` triangular solve, `iterative.cpp:652-660`) is *the same operation* dispatch-4 actually lands — under the slug `back_solve`. So the genuine consistency reconciliation is `trsv → back_solve` (the inverse of what the instruction described), NOT `ls_update_column → back_solve`. But this is a **content reclassification**, not a mechanical rename: dispatch-4's `back_solve.md:90-107` deliberately argues its leaf is NOT a general `trsv` (the general `trsv` / `sparse_triangular_solve` is a separate, currently-unanchored/blocked target per `open-questions.md:24,:448`). Deciding whether this theme's back-solve fan-down target should be re-pointed from the forthcoming-general-`trsv` forward-ref onto dispatch-4's specific `back_solve` leaf — vs. left as a distinct `trsv` forward-ref with `back_solve` as an intermediate — is a substantive cross-report naming decision. Dispatch-4's own critic flagged exactly this collision (`META.md:134-148`) as requiring reconciliation across dispatch-4 / dispatch-5 / the concept pages. That is integrator/follow-up authority, not surgical repair.

### Unrepairable findings

- **Cross-report back-solve-target naming reconciliation** (cross-reference-integrity). The theme's terminal-back-solve fan-down forward-references a forthcoming general `trsv` L1 leaf; dispatch-4 lands the matching small-dense back-solve as `back_solve` (explicitly distinguished from a general `trsv`). The two must be reconciled so the lowering target resolves correctly once both land. **Follow-up routing: `integrator-per-report`** — when applying these two reports (dispatch-4 FIRST per the existing integration-ordering note, lines 404-407, 422-427, 466), reconcile the back-solve-target naming: either (a) re-point this theme's terminal-back-solve fan-down at `back_solve` (dispatch-4's leaf) and demote the general-`trsv` mention to a forward note, or (b) keep both with an explicit note that `back_solve` is the concrete landed leaf and general `trsv` remains a distinct forthcoming target. The plain-text-forward-ref discipline means `linkcheck2` passes either way (no live link to a missing slug), so this is a content-correctness reconciliation, not a build blocker. The companion column-streaming `ls_update_column` leaf is itself still forthcoming (NOT landed by dispatch-4), so this theme's Face-1 `ls_update_column` references correctly remain plain-text and require no change.

### Verification

- `tools/citecheck/citecheck.py --scan` on this report: **35 ok, 0 failing (35 citations checked)** — unchanged (no citation ranges were touched; the slugs in question are not citation ranges). Build-readiness fence parity unchanged (no body edits applied).
- Frontmatter: the critic recorded `verifies: ../REPORT.md` (the producer file is `CYCLE.md`, per the post-cycle-004 rename); left as-authored by the critic — not a repairer-owned field and not load-bearing for this finding. No other stale frontmatter.

## Suggested resolution

`needs-revision` — one `unrepairable` cross-report finding the integrator should reconcile, not a defect in this report's own authoring. The report is internally consistent, citecheck-clean (35/35), build-ready, and correctly `rough-in`. The single open item is the cross-report back-solve-target naming (this theme's forthcoming-`trsv` forward-ref vs dispatch-4's landed `back_solve` leaf), which is a content reconciliation outside repair authority and is already flagged on dispatch-4's META for the same cross-report resolution.

**For `integrator-per-report`:** sequence dispatch-4 (`back_solve` leaf) before dispatch-5 (this theme) per the existing ordering note, then reconcile the back-solve-target naming so the lowering target resolves to the landed `back_solve` leaf (option (a) above is the cleaner outcome; option (b) is acceptable if a distinct general `trsv` is wanted as a separate forward note). Do **not** rename this theme's column-streaming `ls_update_column` Face-1 references — that leaf is genuinely forthcoming and the slug is correct. The link-upgrade of the plain-text forward-refs is the standard post-both-landed `upgrade-plain-text-ref-to-live-link-when-target-on-disk` follow-up.
