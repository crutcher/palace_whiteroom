---
agent: cycle-planner
invoked_at: 2026-06-02T083220Z
scope: cycle-062 dispatch plan
status: pending
---

# Cycle 062 dispatch plan

## Goals selected this cycle

Cycle-062 is the SECOND primary cycle of meta-batch-19 (cycles 061/062/063; the batch-19 meta-phase fires AFTER cycle-063's finalize). The MAP/FOLD spine is complete and descended; c061 opened the next genuinely-new vocabulary front (the `weak_form_term` FE differential-operator cohort, firm L1 + firm L1>L0, pull-driven by the magnetostatic curl-curl term). This cycle advances that front along TWO pull-fired axes under the 2026-06-01 VOCABULARY-SHIFT REDIRECT: (1) the **mass-term pull FIRES** — `GetMassMatrix` builds the M-matrix via the identical `BilinearForm`-fold differing only in the `VectorFEMassIntegrator` slot, so the `Identity`/mass differential-operator axis point gets grounded as a **specialization note IN-PLACE** within the firm `weak_form_term` (combinator-primary, NOT a new mirrored entry); (2) the **`assemble_frequency_operator` LICENSE-FUTURE candidate is author-now** — the c061 D3 finding established it is cleanly describable as the operator-operand extension of the firm `linear_combination` fold (single-pipeline-by-design, NO 2nd-pipeline probe owed), so it lands as an L1 operator + L1>L0 theme re-expressed THROUGH an operand-category-extended `linear_combination` (replace-and-propagate, NOT a mirrored fold). Plus the cheap deferred L1-index prose-count refresh (count-owner discipline). No `weak_form_term` L2 floor (anti-mirror smell — the term is inert `(coefficient, differential-operator)` data whose kernel is the libCEED-opaque `A`; no fusion content to unfold at L2).

## Deliverable-presence verification (paste-inline evidence — load-bearing per `verify-dispatch-scope-not-already-discharged`)

**D1 — mass-term `Identity` specialization note IN `book/src/L1/weak_form_term.md` (re-anchor pending-pull → grounded):**
1. File existence: `ls book/src/L1/weak_form_term.md` → EXISTS (`book/src/L1/weak_form_term.md`).
2. Maturity: `grep -m1 '^## Status' …` → `## Status` `firm` (PROMOTE — clean, pulled-not-speculative). The `Identity`/mass axis point is currently a NAMED **pending-pull sibling** (NOT grounded): `:187` "`Identity` (mass) … The **most-likely next pull** … Awaits a pipeline pull that NEEDS a mass-term solver-K/M witness (not speculatively authored here)". The mass note re-anchor (pending-pull → grounded-by-`GetMassMatrix`) is NOT yet on disk → the deliverable is OPEN (it is an edit to a firm entry adding a grounded variant point, NOT a re-author of the firm entry).
3. OQ-ledger: `weak-form-term-concept-page-reconsideration-on-second-consumer` is OPEN (deferred/contingent) — explicitly names a mass-term pull as the likeliest 2nd-consumer trigger; the mass grounding is the trigger firing, not a stale line.
4. Structural block: NONE. The redirect's pull-only clean-gate is the gate, and it is SATISFIED this cycle (concrete `GetMassMatrix` M-build pull, codemap-confirmed below). Combinator-primary discipline (redirect §1) mandates IN-PLACE specialization note, NOT a new `book/src/L1/weak_form_term_mass.md` (verified ABSENT: `ls book/src/L1/weak_form_term_mass.md` → No such file — and it must STAY absent; authoring it would be the anti-mirror smell).

**Mass-term PULL-CHECK (codemap, pasted):**
- `mcp__palace-codemap__search_text "MassIntegrator|VectorFEMassIntegrator" -g palace/models/*.cpp` → hits at `spaceoperator.cpp:268,278,283,293,597`, `modeeigensolver.cpp:62,100,103,131,135,139,231,234,263,267,892,895,976,979`, `domainpostoperator.cpp:38,55,63,80,90,99,198,212`, `romoperator.cpp:341,378,424` — HEAVILY multi-witness across eigenmode/driven/transient/ROM/postprocess.
- `read_range spaceoperator.cpp:255-300` → `AddIntegrators(BilinearForm &a, …)` at `:260` branches `VectorFEMassIntegrator(*f)` at `:278` (the mass/`Identity` term) vs `CurlCurlIntegrator(*df)` at `:274` (the curl term) — SAME `BilinearForm`-fold, integrator-slot-only difference (identical to the c061 curl-curl pull pattern).
- `search_text "GetMassMatrix|AddIntegrators" -g palace/models/spaceoperator.cpp` → `GetMassMatrix` at `:438` calls `AddIntegrators` (`:333`/`:346`) → builds the M-matrix the driven `A=(K+iωC−ω²M)` family + eigenmode + transient all consume. **VERDICT: PULL FIRES** — a concrete in-scope solver-M build NEEDS the mass term this cycle; grounds the `Identity` axis point exactly as c061's curl-curl pull grounded `Curl`.

**D2 — `book/src/L1/index.md` prose-count refresh (sole count-owner this cycle):**
1. File existence: `ls book/src/L1/index.md` → EXISTS.
2. Maturity / stale-prose: `grep -n 'FE-assembly sub-spine\|grand total'` → `:31` "29 firm grand total" + "bringing the L1 firm grand total to **29**"; `:70` "Firm (FE-assembly sub-spine — **3** …)". STALE: the cycle-record `counts_after` L1_firm = **30** (c061 landed `weak_form_term`; `tail -1 cycle-record.jsonl` → L1_firm 30). The dep-map TABLE row + cohort bullet for `weak_form_term` ARE present (`:115`); only the §Vocabulary-cohort HEADER prose count + the sub-spine subsection count lag → the deliverable is OPEN.
3. OQ-ledger: `l1-index-fe-assembly-sub-spine-count-prose-refresh-3-to-4` is OPEN (deferred/contingent — layer-intro-author domain), explicitly the home of this touch.
4. Structural block: NONE (prose-only count sync; count-owner guard applies — count firm from linked chapters' `## Status` lines, NOT the drift-prone index cells).

**D3 — `book/src/L1/assemble_frequency_operator.md` + `book/src/L1-L0/assemble-frequency-operator-rotation.md` (NEW):**
1. File existence: `ls book/src/L1/assemble_frequency_operator.md book/src/L1-L0/assemble-frequency-operator-rotation.md` → BOTH "No such file or directory" — ABSENT, open by construction (no prior-cycle authoring; the c061 D3 finding is the immediately-prior-cycle integrator-signals justification for recruiting).
2. Maturity: n/a (absent).
3. OQ-ledger: `driven-affine-frequency-operator-as-operator-valued-linear-combination` is OPEN (c061 cycle-061 New-intake, LICENSE-FUTURE) — `grep 'driven-affine.*RESOLVED\|driven-affine.*CLOSED' open-questions.md` → no matches (NOT closed; the license is live, this is the authoring that discharges it).
4. Structural block: NONE. The c061 D3 finding ruled the *fold* generality firm-by-extension (tensor-operand BLAS-1 cohort) and the *driven specialization* single-pipeline BY DESIGN ("do not route a 2nd-pipeline probe expecting a discharge (none will come)") — so the single-witness state is NOT a blocking gate (it is a recorded by-design property). The operand-category extension target exists: `ls book/src/L2/linear_combination.md book/src/L3/linear_combination.md` → BOTH EXIST (firm); the operand-category variant axis (`tensor-operand | operator-operand`) extends those firm entries (replace-and-propagate). NOT on the STOP-PROPOSING negative list (checked: `lu_solve`/`back_solve`/`ls-update-column`/4 NLEPS atoms/`apply_nonlinear_pencil`/`polynomial_smoother`/`L3/solve_family`/`L2/fold_solve` — `assemble_frequency_operator` is none of these).

**Anti-mirror check on `weak_form_term` UPWARD propagation (no D for it — recorded as a finding):** `ls book/src/L2/fe_assemble.md book/src/L3/fe_assemble.md book/src/L4/fe_assemble.md` → ALL ABSENT; `ls book/src/L2/weak_form_term.md` → ABSENT. `fe_assemble` itself has no L2/L3/L4 entry, and `weak_form_term`'s kernel is the libCEED-opaque `A` (`fe-assemble-libceed-boundary-obstruction`, opaque-library-ownership). A `weak_form_term` L2 floor would be a degenerate mirror — the term is inert `(coefficient, differential-operator)` data with NO fusion content to unfold at L2 (identity-in-form), the exact redirect §1d anti-mirror smell. **Do NOT author an L2 `weak_form_term` floor this cycle** (it stays a recorded finding; re-open only if an L2 term-folding combinator materializes as a 2nd consumer).

## Pull-check verdicts (summary)

- **Mass-term: RECRUIT (PULL FIRES).** `GetMassMatrix` (`spaceoperator.cpp:438`) builds M via `AddIntegrators`→`VectorFEMassIntegrator(*f)` (`:278`), same `BilinearForm`-fold as the c061 curl-curl pull, integrator-slot-only difference; heavily multi-witness (eigenmode `modeeigensolver.cpp:62`, driven/transient/ROM/postprocess). Lands as the `Identity`/mass specialization NOTE IN-PLACE within firm `weak_form_term` (combinator-primary), NOT a new entry.
- **`assemble_frequency_operator`: AUTHOR-NOW (single-pipeline-by-design).** c061 D3 established it cleanly describable as the operator-operand extension of the firm `linear_combination` fold; the *fold* generality is firm-by-extension, the *driven specialization* is permanently single-pipeline (no 2nd-pipeline probe owed). Replace-and-propagate: extend `linear_combination`'s operand-category variant axis; do NOT author a mirrored fold.

## Dispatches

- **D1 — agent: `harvester`** — scope: ground the **`Identity`/mass differential-operator axis point** as a **specialization NOTE IN-PLACE** within firm `book/src/L1/weak_form_term.md` (re-anchor the `:187`/`:264` `Identity`/mass note from "pending-pull sibling" → "GROUNDED (pulled by the `GetMassMatrix` M-build)"): cite the witness `palace/models/spaceoperator.cpp:278` (`VectorFEMassIntegrator(*f)` inside `AddIntegrators` `:260-300`) + `palace/models/spaceoperator.cpp:438` (`GetMassMatrix`), parallel to the existing `Gradient`/`Curl` grounded points; note the eigenmode/transient multi-witness corroboration (`modeeigensolver.cpp:62`). Combinator-primary per redirect §1 — this is an in-place variant-point grounding, NOT a new `weak_form_term_mass.md` entry (which must stay ABSENT). Touch ONLY `book/src/L1/weak_form_term.md` body (do NOT touch `book/src/L1/index.md` — D2 is the count/cohort owner this cycle). Resolves the mass-pull arm of OQ `weak-form-term-concept-page-reconsideration-on-second-consumer` (note: mass is a *consumer-of-the-axis* via the same `fe_assemble` fold, NOT yet a 2nd independent concept-page consumer — leave the concept-page reconsideration OPEN, it has not crossed the ≥2 bar). deps: none. rationale: the mass-term PULL FIRES (codemap-confirmed concrete in-scope M-build); active-head item 1 (`weak-form-term-fe-cohort`), the genuinely-new FE differential-operator vocabulary; HIGH-confidence clean-gate satisfied.

- **D2 — agent: `layer-intro-author`** — scope: **sole `book/src/L1/index.md` count-owner this cycle.** Refresh the lagging PROSE counts: (a) `:70` "Firm (FE-assembly sub-spine — **3**)" → **4** (add `weak_form_term`, firm c061, as the 4th sub-spine member — the term abstraction the fold quantifies over); (b) `:31` "**29** firm grand total" / "bringing the L1 firm grand total to **29**" → the authoritative total read from on-disk chapter `## Status` lines (c061 `weak_form_term` brought it to **30**; D3's `assemble_frequency_operator` brings it to **31** this cycle — count from the linked chapters' `## Status`, NOT the drift-prone index cells, per the c057-meta count-owner guard). Also fold D3's new `assemble_frequency_operator` into the §Vocabulary-cohort header narrative (the new firm BLAS-1-adjacent operator-assembly operand-category extension). Do NOT re-author the dep-map TABLE rows or cohort bullets that D1/D3 own (D1 owns the `weak_form_term` body; D3 appends its OWN `assemble_frequency_operator` row + bullet) — D2 owns the CONSOLIDATED tally + header prose only. deps: D1, D3 (wave-2; reads their landed chapters so the count is real on disk). rationale: closes OQ `l1-index-fe-assembly-sub-spine-count-prose-refresh-3-to-4` + keeps the L1 grand-total honest after two same-cycle L1 landings; cheap hygiene; count-owner discipline.

- **D3 — agent: `harvester`** — scope: author the NEW **`book/src/L1/assemble_frequency_operator.md`** (firm) — the driven per-ω affine-in-ω fixed-basis operator assembly `A = (a0·K + a1·C + a2·M + A2)` (`palace/models/drivensolver.cpp:176-177`), bottoming out in `SpaceOperator::GetSystemMatrix` → `BuildParSumOperator({a0,a1,a2,1},{K,C,M,A2})` (`palace/models/spaceoperator.cpp:522-528`) → `sum->AddOperator(ops[i], coeff[i])` for `coeff[i]≠0` (`palace/linalg/rap.cpp:765-786`; the per-column heavy-region anchors are NOT needed — these are short branch bodies). Re-express it **THROUGH** the firm `linear_combination` fold by **extending that combinator's operand-category variant axis** (`tensor-operand | operator-operand`) on the existing firm `book/src/L3/linear_combination.md` (§Variant-axes `:132-160`) + `book/src/L2/linear_combination.md` (§Variant-axes `:240-267`) — replace-and-propagate per redirect §1, NOT a new mirrored fold. Record `assemble_frequency_operator` as a **single-pipeline-by-design specialization** (driven only; transient bakes excitation into the captured op, electro/magnetostatic capture a single fixed `K`, eigenmode is opaque `eigen->Solve` — per c061 D3, do NOT claim cross-pipeline generality on the specialization and do NOT route a 2nd-pipeline probe). Handle the `A2` ω-dependence per c061 D3's caveat (absorb as a coefficient-1 4th basis term OR note-separately — harvester decides + records). Author the **L1>L0 theme `book/src/L1-L0/assemble-frequency-operator-rotation.md`** (canonical slug — stated in THIS scope; D3 authors BOTH so no cross-report forward-ref divergence) narrating `GetSystemMatrix`/`BuildParSumOperator` → the scalar-weighted operator sum. Append D3's OWN `assemble_frequency_operator` dep-map row + §Vocabulary-cohort bullet to `book/src/L1/index.md` (anchor-distinct, parallel-safe per the dual-registration partition) BUT **DEFER the consolidated grand-total tally + sub-spine count + header narrative to D2** (state: "D2 sole-owns the L1/index consolidated tally this cycle; D3 appends ONLY its own dep-map row + cohort bullet"). Concept page: at harvester judgment (the operator-operand `linear_combination` extension may warrant a concept-page note, but ≥2-consumer bar applies — likely defer). deps: none (parallel with D1 in wave-1). rationale: c061 D3 LICENSE-FUTURE discharge; active-head — clears OQ `driven-affine-frequency-operator-as-operator-valued-linear-combination`; the operator-domain image of the firm `linear_combination`, cleanly describable now (redirect item-3 solver-test-load advancing the spine when cleanly describable).

## Overlap analysis

- **D1 × D3:** D1 edits `book/src/L1/weak_form_term.md` body (mass note in-place). D3 creates `book/src/L1/assemble_frequency_operator.md` + `book/src/L1-L0/assemble-frequency-operator-rotation.md` + edits `book/src/L2/linear_combination.md` + `book/src/L3/linear_combination.md` (operand-category axis) + appends to `book/src/L1/index.md`. NO shared file region, NO shared operator name (`weak_form_term` vs `assemble_frequency_operator`/`linear_combination`). **NON-overlapping → PARALLEL** (wave-1).
- **D1 × D2:** D1 edits `weak_form_term.md` body; D2 edits `L1/index.md` (count prose + the `weak_form_term` cohort-bullet COUNT, which D1 does NOT touch — D1 stays out of `index.md` by scope). The `weak_form_term` dep-map TABLE row already exists (c061); D2 only adjusts the consolidated count prose. NO shared file. But D2's sub-spine count (3→4) accounts for `weak_form_term` which D1 grounds further — purely additive, no region conflict. **NON-overlapping at the file level**, but D2's grand-total must reflect D3's new operator → see D2×D3.
- **D2 × D3:** BOTH write `book/src/L1/index.md`. This is the dual-registration partition case: D3 appends its OWN anchor-distinct dep-map row + §Vocabulary-cohort bullet (parallel-safe); D2 owns the CONSOLIDATED grand-total tally + sub-spine count + header narrative (the shared mutable derived value). **Partition stated explicitly in both scopes** (D3 DEFERS the tally to D2; D2 sole-owns it). The grand-total D2 writes DEPENDS on D3's new operator being on disk → **D2 SEQUENCED AFTER D3** (wave-2). This is the documented count-owner pattern, not a genuine same-region conflict; sequencing eliminates the stale-count window.

## Sequencing schedule

- **Wave 1 (parallel):** D1 (`harvester` — mass-term grounding in `weak_form_term.md`), D3 (`harvester` — `assemble_frequency_operator` L1 + L1>L0 + operand-category axis extension + own dep-map row/bullet). Non-overlapping artifact regions.
- **Wave 2 (after wave-1 reports land):** D2 (`layer-intro-author` — `L1/index.md` consolidated count refresh, reading the on-disk firm chapters from D1/D3). D2 is the sole count-owner; it runs last so the grand-total reflects both same-cycle L1 changes.

Then: 3 critics (parallel) → repairers as needed → `integrator-per-report` ×3 (serial) → ONE `integrator-finalize`.

## Open questions / caveats

- **`A2` ω-dependence (D3):** c061 D3 flagged `A2` as an ω-DEPENDENT operand (`drivensolver.cpp:174`), not just an ω-dependent coefficient — the literal `BuildParSumOperator` absorbs it as a coefficient-1 4th basis term, so the "affine-in-ω fixed-basis family" abstraction holds modulo the `A2` ω-dependence. D3 must decide absorb-as-4th-term vs note-separately and RECORD the choice + caveat (this is the one place the clean "affine family" framing has a wrinkle; if it does not lower cleanly, that is a spine finding, not a forced land — but c061 D3 already judged the shape cleanly describable, so author-now is warranted).
- **Concept-page reconsideration (D1):** the mass grounding is a SAME-fold consumer (via `fe_assemble`), NOT a 2nd independent concept-page consumer of the `(coefficient, differential-operator)` factorization — so OQ `weak-form-term-concept-page-reconsideration-on-second-consumer` stays OPEN (the ≥2-consumer bar is not crossed). D3's `assemble_frequency_operator` is an operator-operand consumer of `linear_combination`, not of `weak_form_term`, so it also does not trip the bar. Flag for the batch-19 meta-phase if a future L2 term-folding combinator materializes.
- **`weak_form_term` L2 floor NOT authored (anti-mirror finding):** recorded above — the term is inert data, kernel libCEED-opaque, no L2 fusion content; an L2 floor would be the redirect §1d degenerate-mirror smell. This is a finding (parallel the `L2/fold_solve` NO-FLOOR-WARRANT), not a gap. Consider routing to the batch-19 meta-phase as a formal note if it recurs as a candidate.
- **`Divergence`/div-div remains pending-pull (NO in-scope witness):** the c061 D3 + `weak_form_term` entry already record that `DivDivIntegrator` has no `AddDomainIntegrator` instantiation in `palace/models/*.cpp` (codemap-confirmed this cycle: the mass search returned no div-div use site). It stays a possible spine-coverage finding — do NOT author speculatively. No D this cycle.
- **Batch cadence:** c062 is the SECOND batch-19 primary cycle; the friction-ledger / priorities I read may be up to ~1 cycle stale relative to the next meta-phase (fires after c063). No new methodology adjustment surfaced this cycle beyond the recorded findings.

## Plan candidates appended to priorities.md

None required — both pulled items (mass-term grounding, `assemble_frequency_operator`) are already in the c061 active head / OQ ledger as LICENSE-FUTURE / pending-pull, and this cycle discharges them. The remaining batch-19 frontier (next FE-term pull if a 3rd pipeline needs div-div or a distinct term; the `fold-solve-greedy-schedule-source` PROM generalization) stays as-is in the backlog.
