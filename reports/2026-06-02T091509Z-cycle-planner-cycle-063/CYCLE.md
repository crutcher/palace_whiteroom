---
agent: cycle-planner
invoked_at: 2026-06-02T091509Z
scope: cycle-063 dispatch plan
status: pending
---

# Cycle 063 dispatch plan

## Goals selected this cycle

Cycle-063 is the THIRD/FINAL primary cycle of meta-batch-19 (cycles 061/062/063; the batch-19 meta-phase fires AFTER this finalize as a separate dispatch). Per the "prefer-clean-close" directive for the last cycle before meta, this is a **focused, fully-closing 3-dispatch cycle** with **no half-landed threads dangling into the meta-phase**. The frontier this cycle is the **upward-propagation warrant resolution** for the three FE-assembly-adjacent L1 operators landed across batch-19 (`weak_form_term` c061, `assemble_frequency_operator` c062, `fe_assemble` c054) under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (anti-mirror / replace-and-propagate), plus the two cheap c062 cross-ref/hygiene closes that the integrator-signals tail named as tractable.

**Warrant verdicts (the headline — all decided pre-dispatch from on-disk evidence; details in §Warrant analysis):**
- **`weak_form_term` → NO L2/L3 ENTRY (anti-mirror).** Inert `(coefficient, differential-operator)` data pair, libCEED-opaque kernel, no fold/composition to shift. Same warrant class as `fold_solve`'s no-L2-floor. Stays at L1. (No dispatch — recorded as a confirm-and-route observation inside D3.)
- **`assemble_frequency_operator` → NO OWN L2/L3 ENTRY; already propagated through `linear_combination`.** It is authored as the operator-operand specialization *through* `linear_combination` (operand-category axis already extended at L2 + L3 in c062, replace-and-propagate). An own `assemble_frequency_operator` L2/L3 entry would be the exact mirrored `operator_linear_combination` the redirect rejects. The upward propagation already happened. Stays at L1 riding `linear_combination`. (No dispatch — confirmed in D3.)
- **`fe_assemble` → WARRANT-FIRST probe this cycle; likely NO L2+ ENTRY (warrant-record), does NOT force a land.** `fe_assemble` is the integrator-fold `K = Σ_i A(term_i)` over a libCEED-opaque per-term leaf. The genuine question (is the fold-over-opaque-leaf a concise MAP/FOLD-family L2/L3 form, or a degenerate mirror?) deserves an explicit warrant verdict, but the leaf is opaque-library-owned, so the expected outcome is a recorded NO-ENTRY warrant (structurally parallel to `solve_family`'s c057 NO-ENTRY and `fold_solve`'s L2 no-floor). D1 (`abstractor`, warrant-first, observation/record-only) renders the verdict; it authors at most a NO-ENTRY warrant note, NOT a forced L2/L3 chapter.

## Deliverable-presence verification

Per the MANDATORY paste-inline-evidence deliverable-presence check. Evidence pasted below per dispatch.

### D1 — fe_assemble upward warrant (warrant-first abstractor; record-only)
- **Open by construction (warrant-first verdict dispatch).** No prior-cycle `fe_assemble` upward-form authoring exists. The deliverable is a VERDICT (NO-ENTRY warrant note OR a licensed batch-20 candidate), not a pre-existing slug to refine.
- **fe_assemble upward forms ABSENT on disk (confirmed):**
  - `ls book/src/L2/fe_assemble.md` → `No such file or directory`
  - `ls book/src/L3/fe_assemble.md` → `No such file or directory`
  - `ls book/src/L4/fe_assemble.md` → `No such file or directory`
  - `ls book/src/L2-L1/fe-assemble*` → `No such file or directory`
  - `ls book/src/L3-L2/fe-assemble*` → `No such file or directory`
  - `ls book/src/L4-L3/fe-assemble*` → `No such file or directory`
- **`fe_assemble` L1 entry IS firm (the warrant subject exists):** `book/src/L1/fe_assemble.md` present (18822 bytes, mtime Jun 2 01:13); `## Status` = `firm. Clean-gate call: PROMOTE — clean.`
- **Structural-block note:** the per-term leaf `A(term_i)` is `obstruction (opaque-library-ownership)` (the c055 D5 theme `fe-assemble-libceed-boundary-obstruction`, ratified batch-16). This is the gate that makes a degenerate-mirror L2 the likely (anti-mirror) outcome — D1 weighs it, does not author through it.
- **STOP-PROPOSING negative-list check:** `fe_assemble` is NOT on the list (`lu_solve`/`back_solve`/`ls-update-column`/4 NLEPS atoms/`L3/solve_family`/`L2/fold_solve`). PASS.
- **Framing:** warrant-first (anti-mirror), record-only — correct per redirect; NOT a reflexive-harvest.

### D2 — solve_family.md ← assemble_frequency_operator by-name cross-ref (lifter)
- **Target EXISTS, edit is an in-place cross-ref refresh (not a no-op):** `book/src/L4/solve_family.md` present; `grep -c 'assemble_frequency_operator' book/src/L4/solve_family.md` → **0** (the operator is NOT yet named — the refresh is genuinely owed). The file already cites `drivensolver.cpp:176-180` as the per-ω rebuild (`:65,90,163`) but not the firm operator name.
- **OQ status (the dispatch's primary OQ):** `assemble-frequency-operator-map-solve-scope-boundary-cross-ref-refresh` at `open-questions.md:847-852`, `status: open (deferred / contingent)`. `grep 'assemble-frequency-operator-map-solve-scope-boundary-cross-ref-refresh.*RESOLVED\|...CLOSED'` → no matches. OPEN. PASS.
- **Cross-ref source IS firm on disk:** `book/src/L1/assemble_frequency_operator.md` present (23739 bytes); `## Status` = `firm — firm-on-positive-structure`. The live link will resolve.
- **Structural-block:** none. Both endpoints firm.

### D3 — L1/index fe_assemble dep-map row + weak_form_term/assemble_frequency_operator upward-warrant confirm-and-route (layer-intro-author)
- **Target EXISTS, edit adds a missing row (not a no-op):** `book/src/L1/index.md:31` confirms `fe_assemble` is **off-table** ("the dep-map table holds 30 firm rows ... plus the off-table `fe_assemble` (firm, narrated in the FE-assembly subsection, no dep-map row) = 31"). The in-table firm-row count does NOT self-sum to the grand total (31); D3 adds the `fe_assemble` dep-map row to make the table self-summing.
- **OQ status:** `l1-index-fe-assemble-needs-dep-map-row-for-self-summing-table` at `open-questions.md:861-866`, `status: open (deferred / contingent — layer-intro-author / harvester domain)`. `grep '...RESOLVED\|...CLOSED'` → no matches. OPEN. PASS.
- **NOT re-proposing the already-closed sibling:** `l1-index-fe-assembly-sub-spine-count-prose-refresh-3-to-4` is `status: closed-RESOLVED-BY-LANDING` (c062 D2, `open-questions.md:854-859`) — explicitly EXCLUDED from this cycle's plan (the prose count is already current at 31; D3 does NOT touch the grand-total tally, only adds the missing self-summing row).
- **Structural-block:** none. `fe_assemble` firm on disk (the row's live link resolves).
- **Count-owner discipline (c057-meta guard):** D3 is the SOLE `L1/index` writer this cycle → no count-ownership partition needed (single owner). D3 reads the firm count from the linked chapter `## Status` lines, NOT the drift-prone cells; the grand total 31 is already correct and MUST NOT change (adding the off-table row to the table is a presentation refactor, not a count change — D3 reconciles the in-table count from 30→31 by moving `fe_assemble` on-table, removing the +1 off-table note).

## Warrant analysis (the redirect's anti-mirror discipline applied)

The 2026-06-01 redirect: each lowering is a **translation across vocabularies**, NOT a 1:1 named-term rename; a degenerate identity-in-named-terms lowering is a **smell** (resolve as an in-line note, NOT a mirrored entry). Applied to the three batch-19 FE-adjacent L1 operators:

1. **`weak_form_term` (c061, firm L1).** On-disk evidence (`L1/index.md:117` dep-map row): it is a **leaf — an inert `(coefficient, differential-operator)` pair**, "the element type `fe_assemble` folds over opaquely — consumed-by, NOT a dependency; the per-term realization `A(space, ·)` is the libCEED-owned opaque map below the fold." There is **no fold, no composition, no algebraic structure** in `weak_form_term` itself to re-express in L2 vocabulary — its only content is *which* `Q` and *which* `𝒟`, read at the `AddDomainIntegrator<T>(Q)` site, and its kernel is opaque. An L2/L3 `weak_form_term` would be a pure named-term mirror with no vocabulary shift. **VERDICT: NO L2/L3 ENTRY** (same warrant class as `fold_solve`'s no-L2-floor: opaque leaf, nothing to compose). The differential-operator *variant axis* (`Gradient|Identity|Curl|Divergence`) is the in-layer content and already lives on the L1 entry. Recorded as a confirm-and-route observation in D3 (no dispatch needed — the verdict is self-evident from the leaf nature).

2. **`assemble_frequency_operator` (c062, firm L1).** On-disk evidence (`L1/assemble_frequency_operator.md:60-67,112,120-124` + `L1/index.md:114`): it is **already** authored as "the operator-operand specialization of `linear_combination` ... NOT a new fold — it speaks *through* `linear_combination`'s operand-category variant axis (`tensor-operand | operator-operand`), extended this cycle (replace-and-propagate)." c062 D3 already extended the operand-category axis onto `book/src/L2/linear_combination.md` AND `book/src/L3/linear_combination.md`. The upward propagation of `assemble_frequency_operator` **already happened** — it is the operator-operand corner of the firm `linear_combination` L2/L3 entries. An own `assemble_frequency_operator` L2/L3 chapter would be the exact mirrored `operator_linear_combination` fold the redirect §1d names as a smell. **VERDICT: NO OWN L2/L3 ENTRY; covered by `linear_combination`'s extended axis.** The only residual is the cross-ref into `solve_family.md` (D2) and the dep-map self-sum (D3) — both cosmetic, both already-named c062 OQs. Confirmed in D3's confirm-and-route.

3. **`fe_assemble` (c054, firm L1).** On-disk evidence (`L1/index.md:73`): the integrator-fold `K = Σ_i A(term_i)` with laws "empty-term identity, concatenation-homomorphism, single-term reduction, term-position commutativity" over a libCEED-opaque per-term leaf `A(·)`. This is the ONE of the three where the warrant is **not self-evident**: a fold-over-a-list with a concatenation-homomorphism law is structurally a MAP/FOLD-family shape (cf. `solve_family`'s map with its concatenation-homomorphism; `fold_solve`'s fold). The genuine warrant-first question: is `fe_assemble`'s fold a **concise L2/L3 iteration-rotation form** (a genuine vocabulary shift — e.g. the fold IS the L3-iteration-rotation content even with the opaque leaf, like `fold_solve` got a `partial-obstruction` L3 entry), OR a **degenerate mirror** (the leaf is opaque, so there is nothing below to compose and the fold is already fully stated at L1)? The likely-NO axis: unlike `fold_solve` (whose L3 `partial-obstruction` records a genuine cross-step *sequential-obstruction* + opaque leaf), `fe_assemble`'s fold is **embarrassingly parallel / order-commutative** (term-position commutativity is a stated law — no sequential carry), so the fold itself carries no iteration-rotation obstruction to record; and the per-term leaf is opaque-library-owned (`fe-assemble-libceed-boundary-obstruction`), so there is no L2 composition below. That points to **NO-ENTRY warrant** (the fold is already concisely stated at L1; an L2/L3 entry would mirror it). BUT this contrasts with `solve_family`'s c057 NO-ENTRY (embarrassingly-parallel family map → no L3 obstruction → NO-ENTRY) which is the *exact precedent* — so D1 renders the verdict warrant-first and, if NO-ENTRY (expected), records it parallel to the `solve_family` / `fold_solve`-L2 warrants and routes the formal close to the batch-19 meta-phase. D1 authors NO forced chapter; at most a NO-ENTRY warrant note (frontmatter / index annotation) and the report verdict. If D1 finds a genuine concise L4-cap or L3 form (unexpected), it records it as a LICENSED batch-20 candidate (does NOT author it this cycle — keeps the close clean).

## Dispatches

1. **agent: `abstractor`** — **scope:** `fe_assemble` upward-propagation **WARRANT-FIRST verdict** (record-only): judge whether `fe_assemble`'s integrator-fold `K = Σ_i A(term_i)` (firm `book/src/L1/fe_assemble.md`; laws empty-term-identity / concatenation-homomorphism / single-term-reduction / term-position-commutativity over the libCEED-opaque per-term leaf `A(·)`) warrants a genuine concise L2/L3/L4 form OR is a degenerate anti-mirror that stays fully-stated at L1. Apply the anti-mirror warrant explicitly, CONTRASTING the two live precedents: `solve_family` c057 NO-ENTRY (embarrassingly-parallel map, no L3 obstruction → NO-ENTRY) and `fold_solve` c059 L3 `partial-obstruction` (genuine sequential-carry obstruction → DOES get an L3 entry). The likely-YES-for-NO-ENTRY axis: `fe_assemble`'s fold is order-commutative (term-position-commutativity is a stated law → no sequential carry to record as iteration-rotation content) AND the per-term leaf is `opaque-library-ownership` (`fe-assemble-libceed-boundary-obstruction`, ratified batch-16) → no L2 composition below the leaf. **Deliverable:** a warrant VERDICT in the report; if NO-ENTRY (expected) author at most a thin NO-ENTRY warrant note (frontmatter annotation on `L1/fe_assemble.md` and/or an `L1/index.md` warrant bullet) parallel to the `L2/fold_solve` no-floor warrant + route the formal close to the batch-19 meta-phase; if a genuine concise form IS warranted (unexpected) record it as a LICENSED batch-20 candidate (do NOT author the chapter this cycle — keep the close clean). Cite the `disciplined-cross-pipeline-combinator-mining-gate` skill if a fold/map combinator shape surfaces. **deps:** none. **rationale:** the one of the three batch-19 FE-adjacent L1 ops whose upward warrant is not self-evident; resolves the FE-assembly sub-spine's upward boundary before meta (parallel to how `solve_family`/`fold_solve` descents were tied off before their batch metas). Redirect item-1 (vocabulary-shift, anti-mirror) + item-3 (FE-assembly as test-load, advance only when cleanly describable).

2. **agent: `lifter`** — **scope:** in-place cross-ref refresh on `book/src/L4/solve_family.md` — name the firm `assemble_frequency_operator` (`book/src/L1/assemble_frequency_operator.md`, firm) by name as the per-ω operator that the driven `map_solve_over_(operator,rhs)_family` superset's per-element `SetOperators`-inside-the-loop rebuild produces. The file already records the driven scope-out / `map_solve` boundary at `:65,90,163` (citing `drivensolver.cpp:176-180`); D2 sharpens those references to cite `assemble_frequency_operator` by name (live link) as the affine-in-ω fixed-basis operator-valued `linear_combination` that the rebuild evaluates — the per-element operator is "not arbitrary," it is cleanly describable in existing spine vocabulary. Closes OQ `assemble-frequency-operator-map-solve-scope-boundary-cross-ref-refresh` (`open-questions.md:847-852`). **deps:** none. **rationale:** cheap clean close named tractable by the c062 integrator-signals "Unblocked" + "Suggested next dispatches"; tightens the `map_solve` scope-boundary narrative now that its named per-ω operator is firm. Closes a c062 OQ cleanly before meta.

3. **agent: `layer-intro-author`** — **scope:** (a) add the missing `fe_assemble` **dep-map row** to `book/src/L1/index.md` so the in-table firm-row count self-sums to the §Vocabulary-cohort grand total (`fe_assemble` is currently off-table, reconciled via a +1 prose note at `:31`); move `fe_assemble` on-table (in-table firm rows 30→31), remove the "+1 off-table" reconciliation note, **WITHOUT changing the grand total 31** (it is already correct — read firm count from the linked chapters' `## Status` lines per the c057-meta count-owner guard, NOT from the cells). Closes OQ `l1-index-fe-assemble-needs-dep-map-row-for-self-summing-table` (`open-questions.md:861-866`). (b) Record the two self-evident upward-warrant verdicts as confirm-and-route observations in the report (NOT new chapters): `weak_form_term` → NO L2/L3 ENTRY (inert leaf, libCEED-opaque kernel, anti-mirror — `fold_solve`-no-floor warrant class); `assemble_frequency_operator` → NO OWN L2/L3 ENTRY (already propagated through `linear_combination`'s c062-extended operand-category axis at L2+L3, replace-and-propagate — an own entry would be the `operator_linear_combination` mirror smell). Route both formal closes to the batch-19 meta-phase. **deps:** none (D3 reads `fe_assemble.md` firm status off disk; no dependence on D1's verdict — D1's `fe_assemble` upward warrant is the *L2+* question, D3's is the *L1-index dep-map presentation* + the two *other* operators' warrants). **rationale:** cheap clean close named tractable by the c062 integrator-signals; makes the L1-index dep-map self-summing (hygiene) and ties off the two self-evident warrants for meta. Closes a c062 OQ cleanly before meta.

## Overlap analysis

Pairwise artifact-region / operator-name overlap:

- **D1 ↔ D2:** D1 may touch `book/src/L1/fe_assemble.md` (frontmatter NO-ENTRY warrant note) + `book/src/L1/index.md` (a warrant bullet) + its report; D2 touches `book/src/L4/solve_family.md` only. **Disjoint files. NON-overlapping → parallel.** No shared operator name (D1 = `fe_assemble`; D2 = `assemble_frequency_operator` / `solve_family`).
- **D1 ↔ D3:** BOTH may touch `book/src/L1/index.md`. D1's potential touch is a NO-ENTRY *warrant bullet* about `fe_assemble`'s upward form (in the §Semantics-overlay / warrant narrative); D3's touch is the §dep-map *table* (adding the `fe_assemble` row) + the §Vocabulary-cohort count-reconciliation prose at `:31`. These are **anchor-distinct regions of the same file** (table vs warrant-narrative bullet) — per the Discipline ("two dispatches that append distinct rows / touch anchor-distinct regions are NOT overlapping at the operational level → parallel"), AND both concern `fe_assemble`'s status. **To be SAFE on the shared `:31`/dep-map region AND because D1's NO-ENTRY verdict is the semantic input to whether D3's row should carry an "upward: NO-ENTRY warrant" gloss, I sequence D3 AFTER D1** (D3 in wave 2). This lets D3 cite D1's rendered verdict in the dep-map row's upward-form note rather than guessing it, and avoids two writers on the `L1/index.md` warrant/count region in the same wave. (Conflict-tolerance philosophy would permit parallel; I choose sequential here because D1's verdict is a genuine semantic input to D3's row gloss, not merely a region-collision concern.)
- **D2 ↔ D3:** D2 touches `book/src/L4/solve_family.md`; D3 touches `book/src/L1/index.md` (+ report). **Disjoint files. NON-overlapping → parallel.**

No same-cycle new-slug forward-reference (D1 authors at most a warrant note, not a new chapter; D2/D3 reference only already-firm on-disk slugs `assemble_frequency_operator` / `fe_assemble`). No canonical-slug coordination needed.

No shared consolidated-tally writer collision: D3 is the SOLE `L1/index` count writer (single owner — no partition needed); D3 does NOT change the grand total (presentation-only on-table move). D1's optional `L1/index` touch is a warrant *bullet*, not a count.

## Sequencing schedule

- **Wave 1 (parallel):** D1 (`abstractor`, fe_assemble upward warrant), D2 (`lifter`, solve_family cross-ref). Disjoint files; both render independently.
- **Wave 2 (after D1 lands):** D3 (`layer-intro-author`, L1/index dep-map row + two confirm-and-route warrants). Sequenced after D1 so D3 can cite D1's rendered `fe_assemble` upward NO-ENTRY verdict in the dep-map row's upward-form gloss and to keep a single writer on the `L1/index.md` warrant/count region per wave.

Then the standard tail: 3 critics (parallel) → repairers (as needed) → `integrator-per-report` ×3 (serial) → ONE `integrator-finalize`.

## Open questions / caveats

- **This plan deliberately leaves NOTHING half-landed for the meta-phase.** All three warrant verdicts close cleanly: `weak_form_term`/`assemble_frequency_operator` are self-evident NO-ENTRY (D3 confirm-and-route); `fe_assemble`'s is the one genuine warrant-first call (D1), expected NO-ENTRY but record-only either way (no forced chapter). The two cheap OQ closes (D2, D3) discharge the two open c062 cross-ref/hygiene OQs. If D1 unexpectedly finds a genuine concise `fe_assemble` upward form, it is recorded as a LICENSED batch-20 candidate (NOT authored this cycle) — so the close stays clean regardless of D1's verdict.
- **fan-out is modest by design.** This is the last-cycle-before-meta close; the highest-fan-out batch-19 work (`weak_form_term` + `assemble_frequency_operator` landings) already landed c061/c062. The remaining items are warrant-resolution + hygiene — correctly LOW-MEDIUM fan-out, appropriate for a closing cycle.
- **Plan-file note (for the batch-19 meta-phase, not enacted here):** if D1 records the `fe_assemble`-upward NO-ENTRY warrant, the meta-phase should consider adding `L2/fe_assemble` (and `L3/fe_assemble`) to the STOP-PROPOSING negative list parallel to the c060 `L2/fold_solve` add — to prevent a future degenerate-mirror re-proposal. I am NOT appending this to `priorities.md` now (it is contingent on D1's verdict landing); flagging it for the meta-phase unify pass per the cadence note (the friction/plan entry isn't there yet because the verdict hasn't landed).
- **No `weak_form_term` Divergence/div-div 4th-axis-point grounding this cycle** (the c062 integrator-signals "Suggested next dispatches" named it): it is pull-gated (no in-scope solver-K divergence-form witness this cycle), and authoring it speculatively violates the redirect clean-gate. Stays a pending-pull sibling (already recorded on `L1/weak_form_term.md`). Correctly deferred — not a clean-closing item.
