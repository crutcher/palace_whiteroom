---
agent: cycle-planner
invoked_at: 2026-06-03T16:58:37Z
scope: cycle-079 dispatch plan
status: consumed
integrated_at: 2026-06-03T210000Z
integration_commit: bdaf851
integration_notes: "cycle-079 dispatch plan; all 4 planned dispatches (D1 sparameter_reduce 2nd-gate + D2 eigenfreq_qfactor_reduce 2nd-gate + D3 domain_energy_reduce L4 verb + D4 confirm-probe) ran and applied clean. Consumed at cycle-079 finalize."
---

# Cycle 079 dispatch plan

## Goals selected this cycle

Cycle-079 is batch-25 position 1 — the first primary cycle after the batch-24 meta-phase. The FEATURE-SURFACE SPINE column build-out is COMPLETE (13 `seed` columns); the frontier is now **FIRMING the seed surface**, not adding columns. This cycle leads with **THE LEAD (HIGH fan-out): the reduce-verb 2nd-gate discharge via existing-test citation** — discharging the second (test-coverage) gate on both c075 reduce verbs (`sparameter_reduce`, `eigenfreq_qfactor_reduce`) IN write-scope by citing the existing Palace postprocess unit tests as L0-equivalent semantic documentation (batch-24 meta-phase decision (e), the route is explicitly sanctioned), then re-checking the two coupled `seed` output-product columns. It also picks up the **MEDIUM** `domain_energy_reduce` L4 verb-file authoring (with the distinct-verb-vs-inline confirm probe required by the redirect) to resolve the energy-fields column's plain-text forward-ref. Two verb-firming dispatches + one verb-authoring dispatch + one confirm-probe = 4 dispatches, mostly parallel.

## Deliverable-presence verification

Per the role-spec MANDATORY pre-dispatch deliverable-presence check — paste-inline evidence per dispatch. (All `palace-codemap`/on-disk commands run this cycle.)

### D1 — `sparameter_reduce` 2nd-gate discharge (lowering-verifier audit on an existing rough-in verb)
1. **File existence:** `EXISTS  book/src/L4/sparameter_reduce.md` (on disk).
2. **Maturity / already-discharged:** `## Status` at line 238; `grep -c '^verified_against:' book/src/L4/sparameter_reduce.md` → **0** (NO prior audit block — the audit deliverable is genuinely absent). Verb is `rough-in`; the proposed dispatch RAISES law-confidence via test-citation → not a no-op.
3. **OQ-ledger RESOLVED-grep:** `grep -i 'sparameter-reduce-status.*RESOLVED\|sparameter-reduce-status.*CLOSED' scaffolding/open-questions.md` → **no matches** (the 2nd-gate OQ `sparameter-reduce-status-promotion-double-gated` is OPEN).
4. **Structural-block check:** the gate is the 2nd (test-coverage) gate; batch-24 meta-phase decision (e) EXPLICITLY sanctions discharging it in write-scope by citing the existing tests — the gate is now UNBLOCKED for the cite-route (was the structural block before c078). Codemap-confirmed the named tests exercise the entry: `test/unit/test-postoperator.cpp` asserts on the `Measurement` cache `c.S` field (S-parameter), `test/unit/test-postoperatorcsv.cpp` is `PostOperatorCSV<ProblemType::DRIVEN>` exercising the `port-S.csv` reduction-output rows. PASS.

### D2 — `eigenfreq_qfactor_reduce` 2nd-gate discharge (lowering-verifier audit on an existing rough-in verb)
1. **File existence:** `EXISTS  book/src/L4/eigenfreq_qfactor_reduce.md` (on disk).
2. **Maturity / already-discharged:** `## Status` at line 180; `grep -c '^verified_against:' book/src/L4/eigenfreq_qfactor_reduce.md` → **0** (NO prior audit). `rough-in` → not a no-op.
3. **OQ-ledger RESOLVED-grep:** `grep -i 'eigenfreq-qfactor-reduce-status.*RESOLVED\|...CLOSED' scaffolding/open-questions.md` → **no matches** (OQ `eigenfreq-qfactor-reduce-status-promotion-double-gated` OPEN).
4. **Structural-block check:** same as D1 — decision (e) sanctions the cite-route. Codemap-confirmed `test/unit/test-postoperator.cpp` asserts on the `Measurement` cache fields the verb produces: `cache.eigenmode_Q`, `c.quality_factor`, `c.mode_port_kappa`, `cache.freq`, and the per-mode `participation_ratio` map (lines 52-53, 110, 167-168, and the `check_port_data` block). PASS.

### D3 — `domain_energy_reduce` L4 verb-file authoring (NEW file — OPEN BY CONSTRUCTION)
1. **File existence:** `ABSENT  book/src/L4/domain_energy_reduce.md` (verified absent on disk). Open by construction (no prior-cycle history; minted plain-text c078, file never authored).
2. **Maturity:** N/A (absent).
3. **OQ-ledger:** OQ `domain_energy_reduce-l4-verb-needs-authoring` is OPEN (migrated to the batch-25 plan by the batch-24 meta-phase decision (c)); `grep` for RESOLVED → none.
4. **Structural-block check:** gate = "a firm domain-restricted energy-form L1 primitive + the `matrix-weighted-norm` test-coverage gate." The energy-form L1 home `book/src/L1/matrix-weighted-norm.md` EXISTS at `rough-in (test-coverage-bounded)`; `participation_ratio` is firm (c077). The verb file is authorable at `rough-in` now (it composes the firm `participation_ratio` over the rough-in energy form — exactly the maturity the energy-fields column already declares: `energy-fields.L4.md:8` lists `domain_energy_reduce.md (rough-in)` as a dep with the canonical slug). The energy-fields column already forward-references the canonical slug `book/src/L4/domain_energy_reduce.md`, so the authored file resolves the existing plain-text ref to a live link. PASS (author at `rough-in`).

### D4 — distinct-verb-vs-inline confirm probe (combinator-miner, observation-first; resolves the redirect's "do not mint vocabulary that does not simplify" check for D3)
- Open by construction — a fresh confirm-probe with no prior-cycle history; the redirect REQUIRES it before D3's mint is accepted (active head #2 explicitly couples the probe to the verb authoring). Not a named-artifact-slug deliverable (observation-only, no `book/` mutation), so the four-step check is N/A; recorded as open-by-construction.

## Dispatches

### D1 — (`lowering-verifier`, `L4 verb 2nd-gate discharge: sparameter_reduce — cite the existing Palace postprocess tests`, deps: none)
- **Scope:** Audit `book/src/L4/sparameter_reduce.md` (rough-in, Status :238) against the existing Palace postprocess unit tests as L0-equivalent semantic documentation, to discharge the 2nd (test-coverage) gate. Cite (codemap-confirmed; on-disk-confirm each, treat the line ranges as localization HINTS, END lines drift-prone):
  - `reference/palace/test/unit/test-postoperator.cpp` — the `PostOperator` idempotent test asserts dimensionalize/non-dimensionalize round-trip invariance over the `Measurement` cache S-parameter field `c.S` (the `check_port_data` lambda: `CHECK_THAT(std::abs(c.S), WithinRel(std::abs(ndc.S)))` and the "Scattering always non-dim" / "Phase unchanged" assertions; ~lines 195-230) — documents the S-matrix entry's reduction-output semantics + the impedance/normalization invariants.
  - `reference/palace/test/unit/test-postoperatorcsv.cpp` — `PostOperatorCSV<ProblemType::DRIVEN>` fixture exercising the `port-S.csv` measurement-row reduction output (the serialized S-parameter reduction table; `nr_expected_measurement_rows`, `InitializePortVI`, `port-S.csv` data witnesses) — documents the per-port S-reduction-table shape.
  - The verb's own L0 reduction anchors stay as cited (`postoperator.cpp:1246` `MeasureSParameter`, self-term `:1273`, impedance scaling `:1278`; per-port projection `lumpedportoperator.cpp:283-294` / `waveportoperator.cpp:780`).
  - **Promotion verdict:** the tests assert the reduction-OUTPUT dimensional invariants over the `Measurement` cache, NOT the field-assembly itself — so the auditor must decide between `rough-in (test-coverage-bounded)` (the laws are now test-witnessed at the reduction-output level but the assembly-from-fields path is not directly exercised) and `firm` (the firm-on-positive-structure escape, IF the verb's laws are syntactic identities on the fully-specified positive `MeasureSParameter` source). State the chosen status with the gating reason. Append a `verified_against:` block (the `verify-citation-range` audit-report sub-case + the `tools/citecheck/ --anchor`/`--scan` realization).
  - **Coupled re-check (same dispatch):** re-check `book/src/feature/sparameters.{L4,L1,L0}.md` (status `seed`) — repoint its down-links to the now-firmer verb (live-link upgrade if applicable) and re-evaluate the column `seed`→promotion (promote only if the constituent vocabulary now warrants it; otherwise record `seed` with the reason).
- **Rationale:** THE LEAD (active head #1, HIGH). Discharges 1 of the 2 double-gates; unblocks the `sparameters` seed-column re-evaluation. OQ `sparameter-reduce-status-promotion-double-gated`. Sanctioned route = batch-24 meta-phase decision (e).

### D2 — (`lowering-verifier`, `L4 verb 2nd-gate discharge: eigenfreq_qfactor_reduce — cite the existing Palace postprocess tests`, deps: none)
- **Scope:** Audit `book/src/L4/eigenfreq_qfactor_reduce.md` (rough-in, Status :180) against the existing tests as L0-equivalent documentation. Cite (codemap-confirmed; localization HINTS, on-disk-confirm):
  - `reference/palace/test/unit/test-postoperator.cpp` — the `PostOperator` test asserts invariance over the per-mode Q/freq reduction-output cache fields: `cache.eigenmode_Q` (line 53), `cache.freq` (line 52), `c.quality_factor` (line 110), `c.mode_port_kappa` (the mode-port-kappa CHECK in `check_port_data`), and the per-mode `participation_ratio` map (the domain-energy participation block, lines ~43-49 / 167-168) — documents the `(f, Q)` per-mode scalar-table reduction-output semantics.
  - The verb's own L0 anchors stay cited (`postoperator.cpp:1190-1205` mode-coupling Q `Q_mj = ω_m/κ_mj`, `:1200` `quality_factor`; `eigensolver.cpp:424-458` the eigenpair readout + ω-recovery).
  - **Promotion verdict:** same `rough-in (test-coverage-bounded)` vs `firm` decision as D1 (the test asserts reduction-output invariants over the `Measurement` cache, not the eigenpair-to-(f,Q) map's assembly). State the status + gating reason; append a `verified_against:` block.
  - **Coupled re-check (same dispatch):** re-check `book/src/feature/eigenfrequency-qfactor.{L4,L1,L0}.md` (status `seed`) — repoint down-links, re-evaluate seed→promotion (promote only if warranted).
- **Rationale:** THE LEAD's second half (active head #1, HIGH). Discharges the other double-gate; unblocks the `eigenfrequency-qfactor` seed-column re-evaluation. OQ `eigenfreq-qfactor-reduce-status-promotion-double-gated`.

### D3 — (`harvester`, `L4 verb authoring: domain_energy_reduce — the per-domain energy-table reduction (rough-in)`, deps: D4)
- **Scope:** Author `book/src/L4/domain_energy_reduce.md` (status `rough-in`) — the per-domain `(energyᵢ, pᵢ)` scalar-table reduction the energy-fields column composes (currently a plain-text forward-ref). The per-DOMAIN sibling of the per-MODE `eigenfreq_qfactor_reduce`. Inputs = the domain set + the field; output = the per-domain energy table `(energy_i = ½⟨field, M_i field⟩, p_i = energy_i / e_total)`. Compose the **firm** `participation_ratio` (c077) over the domain-restricted energy form (the `rough-in (test-coverage-bounded)` `matrix-weighted-norm`-squared, one domain attribute). L0 anchors (codemap-confirmed; localization HINTS, on-disk-confirm): `palace/models/postoperator.cpp:1021` `MeasureDomainFieldEnergy` (loop `:1029-1075`); per-domain energy form `palace/models/domainpostoperator.cpp:255-275` `GetDomainElectricFieldEnergy` / `GetDomainMagneticFieldEnergy` (`½⟨field, M_i field⟩`); participation `energy_i/energy` `:1038`. Owns its own `L4/index` dep-map row + §Vocabulary-cohort bullet (single new chapter this cycle into L4/index — no co-writer into that index this cycle → parallel-blind-shared-index guard N/A). Use the canonical slug `book/src/L4/domain_energy_reduce.md` (the energy-fields column already forward-refs exactly this slug at `energy-fields.L4.md:8` — authoring it resolves that plain-text ref to a live link). May additionally cite `test/unit/test-domainpostoperator.cpp` (the `GetElectricFieldEnergy` positive SI-energy assertion) as supporting L0-equivalent evidence for the energy-form constituent.
- **Rationale:** active head #2 (MEDIUM). Completes the L4 reduce-family to 4 authored verb files; resolves the energy-fields plain-text forward-ref. **Conditioned on D4's confirm-probe verdict** (see deps): the redirect forbids minting vocabulary that does not simplify — if D4 finds the per-domain restriction+ratio collapses into a `participation_ratio`-fold inlinable into the column, D3 instead records the inline disposition (no standalone verb). OQ `domain_energy_reduce-l4-verb-needs-authoring`.

### D4 — (`combinator-miner`, `distinct-verb-vs-inline confirm probe for domain_energy_reduce (observation-only, no book/ mutation)`, deps: none)
- **Scope:** Confirm whether the per-domain energy-table reduction genuinely warrants a DISTINCT L4 verb (`domain_energy_reduce`) vs. a `participation_ratio`-fold inlined directly into the energy-fields column. Examine the witnesses: the per-domain electric/magnetic energy loops (`postoperator.cpp:1029-1075`), the per-domain energy form (`domainpostoperator.cpp:255-275`), and whether the shape collapses into an existing reduce-shape (`gram_reduce` / `eigenfreq_qfactor_reduce` / a bare `participation_ratio` fold). Verdict: DISTINCT-VERB-WARRANTED (the per-domain restriction + ratio is a 4th reduce-shape that does not collapse) → D3 mints the verb file; or COLLAPSES (inlinable) → D3 records the inline disposition. **OBSERVATION-ONLY — NO `book/` mutation.**
- **Rationale:** active head #2's required redirect-discipline probe (the redirect's "do not mint vocabulary that does not simplify" gate). Note for the meta-phase watch: `combinator-miner` is the 2-of-5 dispatch-phase write-leak repeat offender (friction `specialized_agent_direct_write_leak`, recurrence-5, watch trigger = 3rd combinator-miner leak) — this dispatch is observation-only by design, which keeps it clear of the leak shape (a full firm-body authored to `book/`); the dispatch scope explicitly forbids `book/` writes.

## Overlap analysis

Pairwise (4 dispatches = 6 pairs):

- **D1 × D2:** D1 audits `L4/sparameter_reduce.md` + re-checks `feature/sparameters.*`; D2 audits `L4/eigenfreq_qfactor_reduce.md` + re-checks `feature/eigenfrequency-qfactor.*`. Byte-disjoint verb files; disjoint coupled columns. Both append a `verified_against:` block to DISTINCT files. **NOT overlapping → PARALLEL.**
- **D1 × D3:** D1 touches `sparameter_reduce.md` + `sparameters.*`; D3 creates `domain_energy_reduce.md` + appends ONE dep-map row to `L4/index.md`. D1's coupled-column re-check does NOT touch `L4/index.md` (it repoints the column's own down-links). D3 is the only writer into `L4/index.md` this cycle. **NOT overlapping → PARALLEL.**
- **D1 × D4:** D4 is observation-only (no `book/` writes). **NOT overlapping → PARALLEL.**
- **D2 × D3:** as D1×D3 — disjoint files; D3 sole writer into `L4/index.md`. **NOT overlapping → PARALLEL.**
- **D2 × D4:** D4 observation-only. **NOT overlapping → PARALLEL.**
- **D3 × D4:** D4's verdict CONDITIONS D3's authoring (mint-vs-inline). This is a forward-reference/decision dependency, NOT an artifact-region overlap (D4 writes nothing). Sequenced for the DECISION, not for write-conflict. **Sequential by decision-dependency → D4 before D3.**

`L4/index.md` shared-index note: D3 is the SOLE dispatch landing a chapter into `L4/index.md` this cycle (it owns its own dep-map row + §Vocabulary-cohort bullet + any consolidated tally). The parallel-blind-shared-index guard does NOT fire (no ≥2 parallel writers into that index). D1/D2's coupled-column re-checks touch the feature-column files' own down-links, not `L4/index.md`.

## Sequencing schedule

- **Wave 1 (parallel):** D1, D2, D4.
  - D1 + D2 are independent verb audits + coupled-column re-checks.
  - D4 is the independent observation-only confirm-probe; its verdict is consumed by D3.
- **Wave 2 (after D4's report lands):** D3.
  - D3 authors (or inlines) `domain_energy_reduce` per D4's mint-vs-inline verdict. Placed in wave 2 so the per-report integrator can wire D3's outcome to D4's finding (and so D3 does not mint a verb D4 would have found collapsible).
- One `integrator-finalize` at cycle end (rebuild book + commit + push + housekeeping) — the waves are dispatch/decision ordering, NOT multiple finalizes.

## Open questions / caveats

- **The lead's promotion verdict is genuinely the auditor's call, not pre-decided.** The named tests assert dimensional-invariance / serialization over the `Measurement` reduction-OUTPUT cache (`c.S`, `eigenmode_Q`, `quality_factor`, the per-mode `participation_ratio` map, the `port-S.csv` rows) — they are strong L0-equivalent documentation of the *reduction-output shape and its invariants*, but they do NOT directly exercise the *field-to-reduction assembly* (`MeasureSParameter` / the eigenpair-to-(f,Q) map) end-to-end. The auditors (D1/D2) must choose `rough-in (test-coverage-bounded)` (laws now witnessed at the output level, assembly path still test-gated) vs. `firm` (firm-on-positive-structure escape, IF the laws are syntactic identities on the fully-specified positive source). I have NOT pre-decided this — the dispatch scopes ask for an explicit status + gating reason. Either outcome discharges the double-gate's 2nd gate per decision (e) (the route is sanctioned regardless of which qualifier lands); the coupled column promotes only if the chosen status warrants it.
- **`matrix-weighted-norm` test-coverage gate is incidentally addressable.** `test/unit/test-domainpostoperator.cpp` directly exercises `GetElectricFieldEnergy` with a positive SI-energy numerical assertion (`CHECK_THAT(energy_SI, WithinRel(expected_energy_SI, 0.01))`) — a stronger witness than the round-trip tests. This is the energy-form constituent of `domain_energy_reduce` AND the `matrix-weighted-norm` test-coverage gate. I did NOT add a dedicated `matrix-weighted-norm`-firming dispatch this cycle (it would over-fill the wave and is not on the active head as a lead), but I flag it as a strong next-cycle candidate: a `lowering-verifier` pass citing `test-domainpostoperator.cpp` could advance `matrix-weighted-norm` from `rough-in (test-coverage-bounded)` toward firm, which would in turn firm `domain_energy_reduce`'s constituent gate. Surfaced here for the next planner / the batch-25 meta-phase.
- **Dispatch cap:** 4 dispatches, well under 12. The active head's #3 (continued bottom-up vocabulary / 5-driver→L4 completeness) is MEDIUM-standing and not picked this cycle — the two firming dispatches + the verb authoring are the highest-fan-out cleanly-describable work, and #3 has no single highest-fan-out cleanly-describable item that outranks them this cycle. Standing gates (capacitance/inductance↔gram_reduce double-gate; fold-solve 2-witness; the record-definition ≥2-consumer promote watches; boundary-mode↔waveguide-mode gate) are correctly NOT dispatched (no trigger fired).
- **Plan-update note:** I have NOT mutated `scaffolding/priorities.md` beyond what this plan implies. The batch-25 active head is freshly reshaped and accurately ranks these picks; no fresh fan-out candidate surfaced that is not already in the active head or standing-gates. The `matrix-weighted-norm` next-cycle candidate above is the one item I'd surface — but it is already implied by active head #2's gate note ("the `matrix-weighted-norm` test-coverage gate"), so no append is needed.
