---
agent: cycle-planner
invoked_at: 2026-06-06T185234Z
scope: cycle-115 dispatch plan (OPENER of meta-batch-37; PLATEAU-PROBE)
status: pending
---

# Cycle 115 dispatch plan

## Goals selected this cycle

Cycle-115 opens meta-batch-37 (cycles 115/116/117) as a **deliberate PLATEAU-PROBE**. The batch-36
meta-phase surfaced an ASK — the project is at a natural completion plateau on BOTH axes (reachability
at its faithful floor with RE1–RE8 fully tracking every residual STRONGER-garbage node; the forward-
vocabulary frontier substantially exhausted with `promotion_frontier: 8` all obstruction-/demand-gated,
STOP-PROPOSING in force, all 40 feature columns off `seed`). The human has responded **"Run one more
batch to confirm."** So this batch is NOT trying to manufacture forward progress — it is trying to
either (a) **independently corroborate exhaustion**, or (b) **surface any genuinely-missed clean pick**
the single-source plateau assessment overlooked. Accordingly this cycle dispatches exactly two disjoint
items: **D1** an independent observation-only plateau-probe audit (the load-bearing output for the
batch-37 meta-phase's terminal-state decision), and **D2** the one carried LOW hygiene item
(`graded-stack-residual-untyped-hygiene`) — mechanical, faithful, reachability-neutral frontmatter typing
of the now-reachable-but-pre-scheme nodes. **No forced forward-frontier pick** (the redirect forbids a
rectangular pull-up; STOP-PROPOSING is in force).

## Dispatches

### D1 — INDEPENDENT plateau-probe audit (the POINT of this batch)

- **agent:** `cross-layer-cross-cutter`
- **scope:** Observation-only. INDEPENDENTLY audit whether the frontier is genuinely exhausted on BOTH
  axes — do NOT take the batch-36 meta-phase's word; re-derive it. Three sweeps, each producing an
  explicit verdict line:
  - **(i) Missed reachability +Δ.** For every node currently marked detritus (the `detritus=127`,
    especially the `STRONGER GARBAGE SIGNAL=23` subset), check whether a GENUINELY-FAITHFUL inbound
    `depends-on` grounding edge from a reachable node exists that the c113 audit + RE1–RE8 ratification
    MISSED. Apply §2f FAITHFUL-PATH-OR-FINDING: an edge counts as a missed pick ONLY if it is a true
    constituent-use / lowers-to relationship the chapter prose genuinely supports (NOT a forced
    consumer→producer inversion — recall the c108 over-edge catch and the RE1 direction-reversal
    rationale). Cross-check each STRONGER member against its RE1–RE8 disposition in
    `scaffolding/graded-stack-baseline-exceptions.md` (Axis-2 section, RE1–RE8) and confirm the
    "no faithful reachable depender" claim still holds. Expected verdict: exhaustion confirmed (every
    STRONGER member is correctly RE-tracked, no missed faithful edge). If a genuine missed edge IS
    found, it is a FINDING for the batch-37 meta-phase — do NOT author it.
  - **(ii) `promotion_frontier: 8` re-verification.** For each of the 8 promotion-frontier members
    (`bicgstab`/`minres` enum-only-stubs; `eigsolve-convergence-reason-mapping`/`deflate`/
    `deflate-composition-lowering` opaque-library-/demand-gated; `boundary-mode.{L4,L1,L0}`
    demand-gated), RE-VERIFY the gating is real — i.e. each is genuinely obstruction-/demand-gated and
    NOT actually a clean pick mis-classified as gated. Read the chapter `## Status` + the gate citation;
    confirm the gate is a standing methodology gate (enum-only-stub / opaque-library-ownership /
    demand-gate with no fired trigger), not a stale defer. Expected verdict: all 8 correctly gated.
  - **(iii) True coverage holes.** Sweep for any IN-SCOPE feature/vocabulary that has NO chapter at all
    (a true coverage hole, NOT a maturity gap). Cross-check against CLAUDE.md §Scope: the **5 sim
    drivers** (electrostatic / magnetostatic / eigenmode / driven / transient), **FE assembly**, and
    **mesh / FE-space construction**, single-machine (MPI / multi-rank out of scope). Confirm each
    in-scope surface reaches L4 (or has a documented obstruction) and has a feature-spine column.
    Distinguish a genuine missing-chapter hole from a demand-gated future-vocabulary deferral
    (`fe_space` deferred siblings — `essential_dofs`/`fe_space_hierarchy`/de-Rham interpolator; the
    `waveguide-mode` 6th output-product column; the `boundary-mode` driver-leaf column — these are
    KNOWN demand-gated deferrals, NOT coverage holes; flag only a surface that is in-scope AND
    undocumented AND not a tracked deferral).
  - **Output:** a single load-bearing verdict — **"exhaustion confirmed"** (with the three sub-verdicts)
    OR **"here are N missed picks"** (each as a structured FINDING with citation, routed to the batch-37
    meta-phase's terminal-state decision). Observation-only CYCLE.md; mutate NOTHING (no artifact edge,
    no frontmatter).
- **deps:** none
- **rationale:** This IS the batch — an independent corroboration (or refutation) of the plateau
  assessment the human asked to confirm. Routed `cross-layer-cross-cutter` (not `same-layer`) for the
  broadest coverage: the audit spans all layers L4→L0, the lowering directories, the feature spine, and
  the cross-axis (rank + reachability) question — a cross-layer sweep. Serves the batch-36 ASK
  (plateau confirmation) directly. fan-out: this is the find-step whose verdict gates the entire
  batch-37+ direction decision.

### D2 — `graded-stack-residual-untyped-hygiene` (the one carried LOW item)

- **agent:** `layer-intro-author`
- **scope:** Type the now-reachable-but-pre-scheme nodes with canonical graded-stack frontmatter
  (the batch-33 scheme; template `book/src/L2/krylov-step.md` / the c114 `L1/fe_space` from-scratch
  authoring). Four files, all confirmed reachable and frontmatter-less (see deliverable-presence
  evidence below):
  - `book/src/L1/fe_collection.md` — carries `status: firm` only (no typed `edges:`). Migrate to
    `rank: firm` + a typed `edges:` block adding a `cites-evidence` edge to the L0 backing
    `palace/fem/multigrid.hpp:22-73` (the `ConstructFECollections` template body — the canonical
    schedule source the chapter cites at `:185`/`:216`; **on-disk-confirm the END line per the
    brace-boundary drift caution — `:22-73` is a codemap-class range hint, verify the close-brace END
    on disk, it may be off-by-one**). Discharges OQ `fe_collection-own-constituents-future-pass` (the
    own-frontmatter half — the constituents pass remains future). FAITHFUL-EDGE-OR-FINDING: type only
    the L0 cite the chapter genuinely backs; do NOT invent constituent edges (the `fe_collection` own
    constituents — `Order`/`DeRhamFamily`/`CoarseningPolicy` — are pre-scheme records, NOT this pass).
  - `book/src/L1-L0/{dot,nrm2,scal}-mutation-rotation.md` — each carries NO frontmatter at all (bare
    `# heading`); each `## Status` leads with `` `firm` `` (verified). Add a from-scratch frontmatter
    block: `rank: firm` + a typed `edges:` block with `cites-evidence` edges to the L0 anchor(s) each
    theme already cites in-chapter (faithful, read-from-prose):
    - `dot-mutation-rotation` → the `linalg::Dot` / `LocalDot` reduction site
      (`palace/linalg/vector.cpp:263-267` complex kernel + `vector.hpp` template body — pick the
      canonical lowering anchor the chapter's §Status names; on-disk-confirm).
    - `nrm2-mutation-rotation` → the `Norml2` composition site (`palace/linalg/vector.hpp:254-259` /
      `:261-269` — the chapter's `Dot → MPI_Allreduce → std::abs → std::sqrt` chain; on-disk-confirm).
    - `scal-mutation-rotation` → the in-place `x *= α` member site
      (`palace/linalg/vector.cpp:203-227` / `vector.hpp:262-270`; on-disk-confirm).
    These are L1>L0 lowering themes; per the §5 lowering-theme scheme a theme's edge is `depends-on`
    on both endpoints — but NOTE this pass is **reachability-NEUTRAL**: the three themes were ALREADY
    flipped reachable c114 (D2's `L1/{dot,nrm2,scal} → theme` `depends-on(kind:lowers-to)` upgrade).
    This pass only adds the theme's OWN `rank:` + `cites-evidence` L0 grounding (the missing frontmatter),
    collapsing them out of the `untyped: 60` tail. Do NOT re-add or duplicate the inbound op→theme edge
    (it lives on the L1 op side, landed c114).
- **deps:** none
- **rationale:** The single carried LOW hygiene item (`graded-stack-residual-untyped-hygiene`,
  priorities item 1). Mechanical, faithful, low-risk; collapses a few untyped-tail entries. Explicitly
  reachability-neutral (these nodes are already reachable — fan-out: LOW). The
  `fe_collection-own-constituents-future-pass` OQ trigger ("a future cycle next-touches the FE-collection
  chapter") fires this cycle.

## Overlap analysis

- **D1 ∩ D2:** D1 is **observation-only** (writes only its own CYCLE.md; mutates NO artifact, NO
  frontmatter). D2 writes frontmatter on `book/src/L1/fe_collection.md` +
  `book/src/L1-L0/{dot,nrm2,scal}-mutation-rotation.md`. **DISJOINT** — an observation report vs a
  four-file frontmatter edit share no artifact region and no operator name. D1's sweep MAY *read*
  the same files D2 types, but reading is not a write conflict; and D1's verdict on those nodes
  (they are firm + reachable, correctly NOT detritus) is CONSISTENT with D2's hygiene typing (D2
  changes no reachability, no rank, no edge that would alter D1's audit). **NOT overlapping → parallel.**
- **No consolidated-tally collision:** D2 touches per-page frontmatter only — no cohort count, no
  `book/src/L_n/index.md` Working-Notes running tally, no `feature/index.md` matrix touch. The
  parallel-blind-shared-index guard does not apply (single producer, no shared aggregate).
- **No new-slug forward-reference:** every D2 edge target — `palace/fem/multigrid.hpp` + the three
  `palace/linalg/vector.{hpp,cpp}` L0 anchors — is an EXISTING on-disk L0 source range (cited in-chapter),
  NOT a not-yet-authored sibling-dispatch slug. The cross-report-forward-reference-slug guard does not
  apply. D1 authors no slugs.
- **Contamination-friction** (`parallel-dispatch-reachability-measurement-contamination`,
  ledger-and-monitor, addressed/watch-held through batch-36): D1 measures nothing it mutates
  (observation-only) → no cross-dispatch reachability-measure contamination this cycle. D2 is
  reachability-NEUTRAL (its nodes are already reachable; typing adds `rank:`+`cites-evidence` only) →
  it reports no reachability delta to contaminate. The authoritative re-measure is the finalize
  step-5b run on the landed tree.

## Sequencing schedule

**Single wave (both parallel).** D1 (observation-only) and D2 (disjoint four-file frontmatter edit) have
no overlap, no forward-reference, no shared tally → **WAVE-1: D1, D2 in parallel.** No wave-2.

## Deliverable-presence verification (MANDATORY, paste-inline)

**D1** — open by construction (a fresh independent audit; no prior-cycle deliverable; the batch-37
plateau-probe is explicitly newly-commissioned by the human's "run one more batch to confirm" steer).
Skip-justified.

**D2** — four-step deliverable-presence sequence, pasted inline:

**Step 1 — file existence** (`ls -la`):
```
-rw-rw-r-- 1 crutcher crutcher 16760 Jun  6 03:56 book/src/L1/fe_collection.md
-rw-rw-r-- 1 crutcher crutcher 26438 Jun  6 03:56 book/src/L1-L0/dot-mutation-rotation.md
-rw-rw-r-- 1 crutcher crutcher 13868 Jun  6 03:56 book/src/L1-L0/nrm2-mutation-rotation.md
-rw-rw-r-- 1 crutcher crutcher 11537 Jun  6 03:56 book/src/L1-L0/scal-mutation-rotation.md
```
All four EXIST.

**Step 2 — maturity / already-discharged check** (the dispatch ADDS frontmatter to firm-but-untyped
nodes; the deliverable is "typed frontmatter present", which is ABSENT on all four → NOT a no-op):
- `book/src/L1/fe_collection.md`: `grep ^rank:/^edges:` → only `status: firm` present (line 2);
  NO typed `rank:`/`edges:` block. `## Status`: "**firm (firm-on-positive-structure).**" → deliverable
  (typed `rank:`+`edges:`) is ABSENT, recruit.
- `dot-mutation-rotation.md`: `grep` typed tokens → "NO frontmatter tokens at all"; `## Status` (line 384)
  leads `` `firm` ``. ABSENT, recruit.
- `nrm2-mutation-rotation.md`: `grep` typed tokens → "NO frontmatter tokens at all"; `## Status` (line 223)
  leads `` `firm` ``. ABSENT, recruit.
- `scal-mutation-rotation.md`: `grep` typed tokens → "NO frontmatter tokens at all"; `## Status` (line 202)
  leads `` `firm` ``. ABSENT, recruit.

**Step 3 — OQ-ledger RESOLVED-grep** (`grep '<slug>.*RESOLVED\|<slug>.*CLOSED' scaffolding/open-questions.md`):
```
440:- `dot-mutation-rotation-subpattern-d-...-RESOLVED-cycle-024` ... — resolved cycle-024 — orthog.hpp:34→:35 re-anchor (a DIFFERENT sub-issue; NOT the frontmatter typing)
1360:- `fe_collection-own-constituents-future-pass` (c114 D1) — ... *Trigger:* a future cycle next-touches the FE-collection chapter (lazy-tail typing) — migrate to `rank: firm` + typed `edges:` + the `palace/fem/multigrid.hpp:22-73` L0 cite. Low-priority hygiene, not a reachability blocker.
```
The line-440 `dot` RESOLVED is a cycle-024 citation re-anchor of a SUB-PATTERN, NOT the frontmatter-typing
this dispatch performs — not a staleness hit. The line-1360 OQ explicitly describes D2's exact work as a
KEPT-DEFERRED LOW-priority hygiene item whose trigger ("a future cycle next-touches the chapter") fires
this cycle — confirms OPEN, not stale.

**Step 4 — structural-block check:** NO methodology gate blocks D2. These are firm, already-reachable
nodes; the work is mechanical frontmatter typing per the batch-33 graded-stack scheme
(`book/src/methodology/graded-stack-scheme.md`). NOT `rough-in (test-coverage-bounded)` (all firm),
NOT `partly-constructive`, NOT `partial-obstruction`, NOT `obstruction`. The batch-36 meta-phase set
the CYCLE-115 head to a plateau posture with this as the one LOW hygiene pick — the gate is GREEN.

**STOP-PROPOSING negative-list check:** D2 touches none of `lu_solve` / `back_solve` / `ls-update-column`
/ `nleps_*` (the disqualified L3-backfill slugs). D1 proposes NO forward-frontier pick at all
(observation-only). Neither dispatch proposes a `promotion_frontier: 8` member as a forward pick.
PASS.

All four D2 checks PASS; D1 skip-justified (open by construction). Both recruited.

## Open questions / caveats

- **The plateau-probe is intentionally minimal.** This cycle does NOT manufacture forward work — that is
  the correct posture per the human steer and the redirect's STOP-PROPOSING. If D1 returns "exhaustion
  confirmed," the batch-37 meta-phase (after c117) has the independent corroboration it needs for a
  terminal-state / wind-down decision. If D1 returns missed picks, those become batch-37 plan candidates
  (NOT force-dispatched this cycle).
- **Cycles 116/117 posture (for the next two planners):** absent a D1 finding or a fired trigger, c116/c117
  have no clean forward pick either. The batch-37 planners should be prepared to run thin (a follow-on
  hygiene tranche if any untyped-tail nodes remain, or a second independent confirmation sweep on a
  different cross-cut) rather than recruit forced work. If D1 surfaces a genuine missed pick that is
  cleanly describable in existing shared vocabulary, c116/c117 may land it (clean-gated). Flagging here
  so the batch-37 planners (and the batch-37 meta-phase) see the plateau-probe arc as a deliberate
  three-cycle confirmation, not three independent attempts to find work.
- **D2 anchor-END drift caution** (friction `codemap-read-range-plus-one-drift-on-brace-boundary`): the
  `palace/fem/multigrid.hpp:22-73` END line (`:73`, a full-body close-brace range) is the drift-prone
  bound — the producer must on-disk-confirm it, not take the cited END as authoritative. Same caution
  applies to the producer's chosen `vector.{hpp,cpp}` anchors for the three themes (use the in-chapter
  cited ranges as hints, confirm on disk per the faithful-edge-or-finding rule).
- **No friction pattern warrants a mid-batch methodology adjustment this cycle** — the batch-36 meta-phase
  just landed (RE6–RE8 ratified, the two friction entries addressed, the LEAD retired, the plateau ASK
  surfaced). The plan enters batch-37 clean; no pre-meta-phase pattern to flag.
