---
verifies: ../REPORT.md
critiqued_at: 2026-05-28T233500Z
critic_version: 1
checks:
  citation-validity: pass
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: warning
  cross-reference-integrity: pass
  edge-label-fidelity: pass
  plan-kind-consistency: pass
  skill-uptake-survey: pass
repaired_at: 2026-05-28T234500Z
repairer_version: 1
repairs:
  citation-validity: not-needed
  surface-or-evidence: not-needed
  rotation-quality: not-needed
  variant-axis-coverage: repaired
  cross-reference-integrity: not-needed
  edge-label-fidelity: not-needed
  plan-kind-consistency: not-needed
  skill-uptake-survey: not-needed
overall_status: ready
follow_up_agent: null
---

# META: verification of "Formalize linear_combination at L2"

## Critique

### Checks run

**citation-validity — pass.** All nine Palace L0 citations were spot-verified via codemap `read_range` / `search_text`, including the load-bearing γ==0 branch. `vector.cpp:749-751` is exactly the real-real `AXPBYPCZ` γ==0 branch (`:749` = `if (gamma == 0.0)`, `:750` = `{`, `:751` = `add(alpha, x, beta, y, z);`) — the report's mapping of this to law-5 zero-coefficient term-drop is faithful; note there is a *second* `if (gamma == 0.0)` at `vector.cpp:402` (the complex path) but the report correctly scopes its cite to the real-real one. `vector.cpp:726-730` is the `AXPBY(double,…,double,…)` fusion pass with `add(alpha, x, beta, y, y)` at `:729`. `vector.cpp:702-712` is `AXPY(double,…)` with the `alpha == 1.0` fast path. `vector.cpp:203-227` is `ComplexVector::operator*=` with the `si == 0.0` real fast-path at `:207-211`. `vector.hpp:305-316` carries the three free-function template decls with the exact `// Addition …` comments transcribed in the report (AXPY `:305-307`, AXPBY `:309-311`, AXPBYPCZ `:313-316`). The four call sites — `nleps.cpp:343-344`, `romoperator.cpp:188-189` (both `…, 1.0, *.Real()`/`*.Imag()` γ=1 accumulate-into), `timeoperator.cpp:217` (`AXPBYPCZ(1.0, RHS2, dt, k1, 0.0, k2)` γ=0 collapse), `iterative.cpp:632` (`w *= 1.0 / Hj[j + 1]` scal) — all transcribe exactly. The test-file citations resolve at `test/unit/test-vector.cpp` (the codemap-root-relative path the report uses; consistent with prior firm entries' citation convention) — `:17` "Vector Sum - Real", `:42` "Vector Sum - Real - Asymmetric Sizes", `:76` "Vector Sum - Complex" verified.

**surface-or-evidence — pass.** This is a new firm L2 operator entry (create-file `book/src/L2/linear_combination.md`) plus a rough-in→firm dep-map flip and a SUMMARY register — net-new surface with full evidence, not a refinement of an existing operator. Check is satisfied by construction; the proposal carries both surface (the chapter) and evidence (nine cited L0 ranges + the verified specialization identities).

**rotation-quality — pass, strong.** The asserted L2 fusion-rotation makes the representation strictly more compact and more equational than L1: four distinct fixed-arity operators (`scal`/`axpy`/`axpby`/`axpbypcz`) collapse to one variadic fold `linear_combination :: [(Scalar, Tensor[N])] -> Tensor[N]`, with the arity axis (4 operators at L1) becoming a single list-length parameter at L2. This is genuine state/structure hiding (the bounded-arity call-shape family is unfolded into the canonical multi-term combination; the single aligned pass is de-fused into seed-and-accumulate), not a 1:1 rename. The concatenation-homomorphism law (law 2) is the correct algebraic content of "one operator across arities" — it is a monoid homomorphism `([(Scalar,Tensor[N])], ++, []) → (Tensor[N], +, zeros)`, and the worked `axpbypcz = axpby ++ scal` decomposition is correct. The permutation-invariance EXACT-ARITHMETIC law (law 7) paired with the explicit IEEE-754 non-associative-reduction NON-LAW is exactly the CLAUDE.md load-bearing-numerical-trick discipline applied correctly: value is order-agnostic, but bit-reproduction of any given lowered L0 call requires matching that call's pinned summation order, deferred to the (forthcoming) L2>L1 theme. The companion non-laws (re-fold idempotence; bit-level fusion identity vs the multi-pass form) are sound. No rotation-quality defect.

**variant-axis-coverage — warning.** The two orthogonal variant axes are correctly identified and handled: (a) **output-aliasing (in-place vs out-of-place)** — explicitly scoped out of the L2 algebra (the L2 fold is pure/out-of-place; aliasing is named an L2>L1 lowering concern, with the γ=1 accumulate-into call sites cited as the aliasing-with-coeff-1 case); (b) **element-type / scalar-promotion** — inherited from the L1 leaves via `concepts/scalar-promotion`, promote-all-or-none. The arity axis is correctly framed as the *unification* axis (not a remaining variant), and fusion-order is correctly demoted to an L0 implementation detail, not an L2 axis. The warning is narrow: the report inherits the `scalar-promotion` element-type unification as "already-unified / already firm" (Summary, Status), but the `scalar-promotion` concept page itself records an OPEN dependency — its closure "depends on the L1 calculus formally adopting the `real ⊑ complex` scalar lattice (not yet committed)" (`book/src/concepts/scalar-promotion.md:49`). So the inherited element-type sub-axis silently carries an unclosed upstream dependency that the entry presents as settled. This does not affect the arity-axis rotation (the core of the entry) and does not warrant a status change, but the "already firm/already-unified" phrasing slightly over-states the maturity of the inherited sub-axis.

**cross-reference-integrity — pass.** All cross-references resolve. The four L1 leaf links (`../L1/{scal,axpy,axpby,axpbypcz}.md`), the concept link (`../concepts/scalar-promotion.md`), the precedent link (`./chebyshev-iteration.md`), and the decision link (`../../../scaffolding/decisions/axpby-as-primitive.md`, 3 levels up from `book/src/L2/` = repo root, correct) all point at existing files. The L2 index rough-in row is at `index.md:25` exactly as the frontmatter claims, and the replacement firm row's signature/dependency content matches it. The SUMMARY insertion anchor is correct: `chebyshev-iteration` is at `SUMMARY.md:38`, line 39 blank, line 40 the `# L2 > L1` header, so the new `linear_combination` entry lands as the last L2-Part chapter. The `dot` sibling and the forthcoming `L2-L1/linear-combination-fold-specialization` theme are correctly handled as plain-text forward-references (no live links to non-existent targets).

**edge-label-fidelity — pass.** This is an L2 operator entry, not a lowering-edge theme, so it carries no `L_{n+1}→L_n` edge label. The forward-references it does make (to the forthcoming L2>L1 theme, and the L1>L0 mutation rotation for the aliasing idioms) name the correct adjacent edges and are framed as forward-references, not as claims authored here. Per the high→low invariant, the entry defines `linear_combination` in its own L2 vocabulary (the fold) with the L1 leaves referenced as specializations and L4 referenced only for upward context — direction is correct.

**plan-kind-consistency — pass, with the firm judgment as the load-bearing finding.** Declared kind is `firm`. The content shape is a complete firm operator entry (signature + shape contract, 7 holding laws + 3 non-laws, semantics, two variant axes, fusion note, dependencies, sibling-fold distinction, evidence) with no rough-in placeholders. The firm status rests on direct source-transcription per the explicitly-cited `chebyshev-iteration` precedent, which I verified IS itself firm-with-test-caveat (`chebyshev-iteration.md:216,221-227`: "firm … Caveat (not a status reduction): no dedicated unit test (multigrid-integration coverage only) … firm-without-dedicated-test ratification … source-transcription confidence"). The harvester's central honesty claim — that NO unit test exercises the BLAS-1 linear-combination free functions and that the `test-vector.cpp` "Vector Sum" tests are a DIFFERENT fold — is **verified correct and is not a mis-citation**: all three Vector-Sum TEST_CASEs call `linalg::Sum(comm, v)` producing a scalar (`double` / `std::complex<double>`) checked with `CHECK_THAT(sum, WithinRel(expected))`; `linalg::Sum` is defined at `vector.hpp:278-282` as `LocalSum(x)` + `Mpi::GlobalSum(...)` — a reduce-to-scalar MPI-collective, categorically distinct from the scalar-weighted-tensor-sum fold (`Tensor[N]` result, element-local in N, no collective). A search for `AXPY|AXPBY|AXPBYPCZ|operator*=|.Add(` in `test/unit/test-vector.cpp` returned zero hits, confirming no dedicated witness exists. The harvester correctly REFUSED to cite the Vector-Sum tests as an empirical-match witness — the serious error the dispatch flagged as a risk is absent. **Judgment: `firm` is justified.** The structural decomposition is exhaustively cited (the γ==0 in-source arity-collapse is positive direct evidence that the family is one fold parameterized by arity), every law is a syntactic identity or a standard linear-combination fact, and the missing empirical-match is handled by the established firm-without-dedicated-test precedent rather than hidden. The caveat is framed as a non-status-reduction with an explicit upgrade condition (a future dedicated test → clean `empirical_match`), which matches the chebyshev precedent. A `partly-constructive` status is NOT warranted here: that status is for a constructive sub-part materialized from negative anchors / literature rather than a positive source site; here every claimed construct (each arity, each law) has a positive Palace source site — the only gap is a *test witness*, which is an empirical-match supplement, not a constructive-sub-part gap.

**skill-uptake-survey — pass.** The report's shape (a harvester firming a rough-in with producer-side citation self-verification) implies `verify-citation-range` (producer-emit / self-verification sub-case), and the report explicitly references it ("producer-citation-drift discipline — `verify-citation-range` producer-self-verification", Supporting evidence header) with the self-verified markers on each Palace range. The `propose-rotation` / `verify-rotation-citation` family is implied by the fusion-rotation framing; the report does not name them by slug, but the rotation is well-argued and the citation discipline is the binding one for a harvester firm-up. Pure telemetry; no block.

### Issues found

1. **scalar-promotion inherited sub-axis presented as settled while its upstream dependency is open** — `book/src/L2/linear_combination.md` Summary, Status, and Variant-axes §2 describe the element-type / `real ⊑ complex` scalar-promotion sub-axis as "already-unified" / "already firm in `concepts/scalar-promotion`". The `scalar-promotion` concept page itself records an OPEN dependency: closure of OQ `scalar-promotion-typing-rule` "depends on the L1 calculus formally adopting the `real ⊑ complex` scalar lattice (not yet committed)" (`concepts/scalar-promotion.md:49`). Severity: low. The arity-axis rotation (the actual content of this entry) is unaffected; this is a phrasing precision issue on the inherited sub-axis — a one-clause hedge ("the element-type axis unified at the concept-page level by `scalar-promotion`, whose own L1-calculus adoption is tracked under OQ `scalar-promotion-typing-rule`") would make the inheritance honest. Candidate for a light repair; not a blocker.

2. **(Non-defect, recorded for the repairer/integrator) firm-without-dedicated-test caveat is correctly framed; no fix needed.** The empirical-match caveat in the Status block and OQ #1 is accurate, the `linalg::Sum` ≠ `linear_combination` distinction is verified correct, and the chebyshev precedent it leans on is verified to be a genuine firm-with-test-caveat entry. Flagging explicitly so the repairer does not mistake the self-disclosed caveat for an unrepaired defect: it is a faithful, precedent-grounded disclosure, NOT a status mis-classification.

3. **(Minor, optional) `add(α,x,β,y,z)` is an MFEM free function, cited via its Palace call sites only.** The fusion-note and law-5 evidence describe `add(alpha, x, beta, y, z)` as "the MFEM 5-arg single aligned in-place linear-combine"; there is no local definition of `add` in `vector.cpp` (search returned zero hits), confirming it resolves into MFEM. The report correctly cites the Palace call sites (`vector.cpp:726-730`/`:749-751`) rather than the vendored MFEM definition, per the cite-Palace-not-upstream discipline — so this is compliant, not a defect. Recorded only to confirm the discipline was followed.

## Repair

### Fixes attempted

- **Finding (issue 1, variant-axis-coverage warning, low severity)**: The `linear_combination.md` entry presents the inherited `scalar-promotion` element-type sub-axis as "already-unified / already firm" while the concept page itself records an open upstream dependency — closure "depends on the L1 calculus formally adopting the `real ⊑ complex` scalar lattice (not yet committed)" (`concepts/scalar-promotion.md:49`).
  - **Decision**: repaired.
  - **Action**: Applied a one-clause hedge at every place the create-content block asserted the inherited sub-axis as settled, in `reports/2026-05-28T231026Z-harvester-linear-combination-L2/CYCLE.md` (the `edit:book/src/L2/linear_combination.md` create-file block + the `edit:book/src/L2/index.md` dep-map row). Five surgical edits:
    - `linear_combination.md` Context §: "already unified by … scalar-promotion" → "unified at the concept-page level by … (whose own closure depends on the L1 calculus formally adopting the `real ⊑ complex` scalar lattice, tracked under OQ `scalar-promotion-typing-rule` and not yet committed)".
    - `linear_combination.md` Variant-axes §2: appended a note that the sub-axis is unified at the concept-page level but its closure depends on the not-yet-committed L1-calculus lattice adoption (OQ `scalar-promotion-typing-rule`, `concepts/scalar-promotion.md:49`); inheritance carries the dependency unchanged.
    - `linear_combination.md` Status block: "the element-type unification already firm in `concepts/scalar-promotion`" → "carried by `concepts/scalar-promotion`, whose own closure depends on the L1 calculus adopting the `real ⊑ complex` scalar lattice — OQ `scalar-promotion-typing-rule`, not yet committed — inherited here as a dependency, not as a settled axis".
    - `linear_combination.md` Dependencies bullet: "already-unified sibling … inherited unchanged" → "concept-page-level sibling … inherited unchanged, including its open upstream dependency (… OQ `scalar-promotion-typing-rule`, not yet committed; `concepts/scalar-promotion.md:49`)".
    - `index.md` dep-map row: "already-unified sibling of this arity-axis unification" → "concept-page-level sibling … inherited with its open upstream dependency — OQ `scalar-promotion-typing-rule`, not yet committed".
  - **Rationale for in-scope**: This is a phrasing-precision hedge on an inherited sub-axis whose open dependency the concept page already records — annotation, not substantive authoring. It is NOT a status change: the core arity-axis rotation (the actual content of the entry) is unaffected and stays `firm`. Verified the source line `concepts/scalar-promotion.md:49` reads exactly "Closure depends on the L1 calculus formally adopting the `real ⊑ complex` scalar lattice (not yet committed)" before editing.

### Unrepairable findings

None. The other two recorded items are non-defects the critic explicitly flagged as needing NO fix: issue 2 (the firm-without-dedicated-test caveat is correctly framed, precedent-grounded — a faithful disclosure, not a defect) and issue 3 (the MFEM `add` free function is correctly cited via its Palace call sites per cite-Palace-not-upstream discipline). Neither requires repair.

## Suggested resolution

`ready`. All seven passing checks stand; the single low-severity `variant-axis-coverage` warning is repaired by the hedge-clauses above. No status change (the `firm` judgment the critic ruled JUSTIFIED is preserved). Note for the integrator: the repair touched only the report's proposed-changes blocks (`linear_combination.md` create-content + `index.md` dep-map row); the artifact was not modified. The self-disclosed empirical-match caveat (no dedicated unit test; firm on source-transcription per the `chebyshev-iteration` precedent) is a faithful disclosure to carry forward as-is, not an unrepaired defect.
