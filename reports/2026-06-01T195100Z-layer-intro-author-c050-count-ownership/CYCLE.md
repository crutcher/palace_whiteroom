---
agent: layer-intro-author
invoked_at: 2026-06-01T195100Z
scope: cycle-050 SOLE consolidated-count owner — L3/index + L3-L2/index + L2-L1/index tallies + degenerate-cohort narrative reconciliation
status: integrated
integrated_at: 2026-06-01T222000Z
integration_commit: 6985e03
integration_notes: APPLIED clean (cycle-050 D7, SOLE consolidated-count owner). Reconciled tallies — L3/index 15→17 firm (+linear_combination +inner_product; partial-obstruction unchanged at 3); L3-L2/index 17→13 firm; L2-L1/index 21→17 firm; §Working-Notes c050-vs-c051 split + the D8 17-denominator. On-disk firm-count verified (counted real files): L3 20−3=17, L3-L2 13, L2-L1 18−1=17 — all match. Whole-book dead-link grep for all 8 deleted slugs CLEAN. 1 OQ promoted (c050-firm-theme-count-drop-is-vehicle-change-not-coverage-regression). The 4 D3–D6 d7-count-reconciliation OQs RESOLVED by this landing. NOTE: D7's scope excluded dep-map ROWS — the 4 de-linked-but-present rows D4/D5 left were physically removed by integrator-finalize as build-repair. Build-relevant yes. refactor-pass ENACTMENT under the 2026-06-01 VOCABULARY-SHIFT REDIRECT.
---

# CYCLE: cycle-050 consolidated count + cohort-narrative reconciliation (D7, sole count-owner)

## Summary

I am dispatch **D7** of cycle-050, the **SOLE consolidated-count owner** for the wave (count-ownership partition per cycle-039 meta `parallel-blind-shared-index-count-divergence`; CLAUDE.md §Discipline `index-dual-registration-row-and-own-bullet-vs-consolidated-tally` — I own ONLY the consolidated tally + cohort-narrative + fork/growth-log prose, NOT the producers' own dep-map rows / cohort bullets / SUMMARY removals, which D1–D6 own and emit themselves).

The six wave-1 producers landed two firm-L3 combinator additions (D1 `linear_combination` new; D2 `inner_product` rough-in→firm) and four degenerate-theme demotions (D3–D6 each deleting one `*-body-identity` L3>L2 theme + one `*-leaf-identity` L2>L1 theme — `assemble-diagonal`, `elementwise-product`, `reciprocal`, `normalize`). They ALL deferred every consolidated tally + cohort-narrative reconciliation to me.

Computed deltas (cited to producer reports + verified against on-disk counts):

- **L3 firm-operator count** (`book/src/L3/index.md:62`, the single authoritative tally): **15 firm → 17 firm** (+`linear_combination` D1, +`inner_product` D2); partial-obstruction unchanged at **3**.
- **L3>L2 firm-theme count** (`book/src/L3-L2/index.md` §Working-Notes): **17 → 13** (−4 `-body-identity` themes); thin-identity sub-count **13 → 9**; substantive cohort unchanged at **4**.
- **L2>L1 firm-theme count** (`book/src/L2-L1/index.md` §Working-Notes): **21 firm → 17 firm** (−4 `-leaf-identity` themes); partly-constructive unchanged at **1** (total 22 → 18).

I also reconcile the **degenerate-cohort narrative** (the c050-vs-c051 split + the D8 17-not-18 denominator correction) and the cohort-framing prose D3/D4/D6 flagged ("five fork-INDEPENDENT standalone-floor" → "four"; the L2-L1 cohort bullets D4/D5/D6 left for me to remove). On-disk verification: `ls book/src/L3-L2/*.md` = 17 theme files (matches "firm 17"); `ls book/src/L2-L1/*.md` = 22 (= 21 firm + 1 partly-constructive). Both confirmed before computing the −4 deltas.

## Proposed changes

> **Integrator note on serial-apply collision avoidance.** Every edit below targets a **consolidated-tally / cohort-narrative / growth-log** region, NOT a producer's own dep-map row, cohort bullet, or SUMMARY line. D3–D6 remove/de-link the dep-map ROWS (`L3-L2/index.md:21,23,24,25`, `L2-L1/index.md:22,24,26,25`) and D4/D5/D6 remove their own L2-L1 cohort BULLETS — those are NOT touched here. My L2-L1 §Vocabulary-cohort bullet removals (change 7, three edit blocks) target bullets D3/D4/D5 left for the count-owner (`L2-L1/index.md:64` assemble-diagonal D3, `:67` reciprocal D5, `:68` elementwise-product D4); **D6 removes its own `:69` normalize bullet in change 6b, so D7 does NOT touch `:69`** (the repairer dropped D7's original duplicate `:69` block — cycle-050 D7 META Issue 1). Apply order does not matter for my edits (all anchor on unique narrative strings); if a producer row-removal lands first, my narrative edits still match (they do not overlap the rows).

### 1. `book/src/L3/index.md` — the single authoritative L3 count tally (15 → 17 firm)

The cycle-040 bullet at `:62` is the named single authoritative L3 count tally ("**THIS BULLET IS THE SINGLE AUTHORITATIVE L3 COUNT TALLY**"). I append a cycle-050 count-update sentence to it (D1/D2 each deferred the tally; D1 flagged firm 15→16, D2's `inner_product` makes it 17 — reconciled by counting the actual on-disk firm entries: the 15 enumerated + `linear_combination` + `inner_product` = 17).

```edit:book/src/L3/index.md
[old]: **L3 firm-operator count is now 15 firm + 3 `partial-obstruction`** — firm (15): `krylov-step` c010; `apply_linop` + `axpy` + `axpby` + `axpbypcz` + `dot` + `nrm2` + `scal` c011; `ksp_solve` c020; `assemble-diagonal` + `jacobi-smoother` c037; `reciprocal` + `elementwise_product` + `divfree-projector` c038; `normalize` c039 — partial-obstruction (3): `chebyshev` c013, `eigsolve` c024, `orthogonalize` c040. This bullet is the **single authoritative count tally** (cycle-040 D2 layer-intro-author sole-owned it per the cycle-039 meta-phase count-ownership convention, friction-ledger `parallel-blind-shared-index-count-divergence`; D1 deferred the tally entirely); the per-cycle running counts in the c024/c037/c039 bullets above are **superseded snapshots** (kept as narrative of what landed each cycle, not as the live count). The §Semantics-overlay taxonomy now enumerates **five** non-trivial obstruction shapes (a)/(b)/(c)/(d)/(e) — the fifth being `orthogonalize`'s variant-conditional partial-obstruction profile — plus the obstruction-free-end `fused-composite-obstruction-free` profile (`normalize`), resolving OQ `l3-index-fifth-obstruction-profile-fused-composite-obstruction-free` and OQ `l3-index-working-notes-stale-snapshot-compaction-candidate`.
[new]: **L3 firm-operator count is now 17 firm + 3 `partial-obstruction`** (cycle-050 update below; was 15 firm + 3 at cycle-040) — firm (17): `krylov-step` c010; `apply_linop` + `axpy` + `axpby` + `axpbypcz` + `dot` + `nrm2` + `scal` c011; `ksp_solve` c020; `assemble-diagonal` + `jacobi-smoother` c037; `reciprocal` + `elementwise_product` + `divfree-projector` c038; `normalize` c039; **`linear_combination` + `inner_product` c050** — partial-obstruction (3): `chebyshev` c013, `eigsolve` c024, `orthogonalize` c040. This bullet is the **single authoritative count tally** (cycle-040 D2 layer-intro-author sole-owned it per the cycle-039 meta-phase count-ownership convention, friction-ledger `parallel-blind-shared-index-count-divergence`; cycle-050 D7 layer-intro-author re-owned it as the wave's sole count-owner — D1 (`linear_combination`) + D2 (`inner_product`) each deferred the tally entirely, writing only their own dep-map rows); the per-cycle running counts in the c024/c037/c039 bullets above are **superseded snapshots** (kept as narrative of what landed each cycle, not as the live count). The §Semantics-overlay taxonomy now enumerates **five** non-trivial obstruction shapes (a)/(b)/(c)/(d)/(e) — the fifth being `orthogonalize`'s variant-conditional partial-obstruction profile — plus the obstruction-free-end `fused-composite-obstruction-free` profile (`normalize`), resolving OQ `l3-index-fifth-obstruction-profile-fused-composite-obstruction-free` and OQ `l3-index-working-notes-stale-snapshot-compaction-candidate`.

- **Two firm L3 combinator entries landed cycle-050 (the propagate half of the cycle-049 replace-and-propagate map; vocabulary-shift redirect 2026-06-01)**: [`linear_combination`](./linear_combination.md) (16th firm — D1; the whole-tensor variadic scalar-weighted-tensor-sum fold the BLAS-1 linear-update leaves `scal`/`axpy`/`axpby`/`axpbypcz` speak through as list-length specializations; **no sequential obstruction** — finite static term-list fold, element-local in `N`, the obstruction-free BLAS-1-cohort profile) and [`inner_product`](./inner_product.md) (17th firm — D2; the whole-tensor reduce-to-scalar inner-product the reductions `dot`/`nrm2` re-express through — `dot` = Hermitian specialization, `nrm2` = `√ ∘ abs ∘ inner_product` consumer at `y=x`, do-NOT-merge; **no sequential obstruction** — parallel-clean length-axis reduction, the pinned L0 tree a deferred non-law). Both were inverted to combinator-as-entry at L2 cycle-049 (commit `92327f7`) but never propagated to L3; the L2 combinators carried no L4 calculus content (pure value-producing reductions, not state-monad combinators), so neither lifts above L3. Both join the **obstruction-free end** of the §Semantics-overlay obstruction-profile spectrum alongside the leaves they unify — no new obstruction shape (the taxonomy stays five non-trivial shapes (a)–(e) + the fused-composite-obstruction-free profile). The four L3 BLAS-1 leaves + the `dot`/`nrm2` reductions are **NOT yet re-expressed through** these combinators (each still stands firm in its own right) — that leaf re-expression/slim is cycle-051, gated on the cohort-wide leaf-disposition convention (OQ `l2-l3-leaf-chapter-disposition-cohort-wide`). The L3>L2 edge of each combinator is a degenerate identity-in-named-terms in-line note (the pre-built home the four `{scal,axpy,axpby,axpbypcz}-body-identity` / the `dot-body-identity` themes demote into at cycle-051); the substantive rotation in each chain is the existing firm L2>L1 fold-specialization theme (`linear-combination-fold-specialization` / `inner-product-fold-specialization`, both KEPT cycle-049). This bullet's count update is folded into the authoritative tally above (firm 15 → 17).
```

(The `[new]:` body of change 1 is a single replacement that REPLACES the cycle-040 tally bullet's text AND appends a fresh bullet immediately after it — the two bullets are emitted as one contiguous `[new]:` block, so the integrator's `[old]`→`[new]` substitution lands both. The appended `linear_combination`/`inner_product` bullet becomes the new last bullet of §Working-Notes; no terminator token is written into the file.)

### 2. `book/src/L3-L2/index.md` — cohort-growth + coverage-gap tally (firm 17 → 13)

The `:66` cohort-growth bullet carries "firm 15 → 17" + the `l3-l2-rotation-theme-coverage-gap` denominator prose. The 4 demoted themes are all thin `-body-identity` (NOT substantive), so the 4-of-17 substantive cohort + the coverage of the 17/18 L3 operators is unaffected in *kind* — the demotions remove the *theme files* (the L3>L2 identity is now an in-line note on the L3 entry, still capturing the rotation), so the operators' rotations remain captured; only the firm-theme COUNT drops. I update the count and add the cycle-050 demotion record.

```edit:book/src/L3-L2/index.md
[old]: - **Cohort growth + coverage-gap progress (firm 15 → 17; `l3-l2-rotation-theme-coverage-gap` 15-of-18 → 17-of-18) — cycle-045 two-landing cohort.**
[new]: - **Cohort growth (firm 17 → 13; cycle-050 degenerate-theme demotion).** Cycle-050 (the refactor-pass enactment under the 2026-06-01 VOCABULARY-SHIFT REDIRECT; `METHODOLOGY-REDIRECT.md`) **demoted four thin `-body-identity` L3>L2 themes to in-line §"Downward to L2" notes** on their L3 operator entries: `assemble-diagonal-body-identity` (D3), `elementwise-product-body-identity` (D4), `reciprocal-body-identity` (D5), `normalize-body-identity` (D6). Each was a degenerate identity-in-named-terms lowering (same signature / same laws / same variant profile across the edge — the §1d smell the redirect names), so its dedicated theme chapter was deleted and its one load-bearing fact folded into an in-line note on the standalone L3/L2 operator entry (none has a fold-parent, so no operator chapter collapses). **The L3>L2 rotation for each operator remains captured** (now as an in-line identity note, not a theme file) — the coverage-gap is unaffected in *kind*; only the firm-theme COUNT drops, **17 → 13** (4 thin-identity themes deleted; the 4 substantive themes + the remaining 9 thin-identity themes stay). The remaining cohort: thin `-body-identity` (9) — `krylov-step`, `dot`, `nrm2`, `scal`, `axpy`, `axpby`, `axpbypcz`, `jacobi-smoother`, `divfree-projector`; substantive / non-identity (4) — `ksp-solve-outer-driver`, `orthogonalize-variant-split`, `eigsolve-opaque-eigen-iteration`, `chebyshev-nested-recurrence`. **`jacobi-smoother-body-identity` + `divfree-projector-body-identity` are NEXT (cycle-051)** — both DEMOTE-OK per the D8 verify-body audit (`reports/2026-06-01T195100Z-cross-layer-cross-cutter-verify-divfree-jacobi/CYCLE.md`); they were not demoted this cycle because they are the gated constructed-operator-gate pairs the c049 D3 audit flagged "verify-body-before-demoting" (the audit completed only this cycle). See the cycle-050-vs-051 split note below. *(Prior — coverage-gap history, retained:* cohort grew firm 2→5→10→14→15→17 across cycles 041–045; the `l3-l2-rotation-theme-coverage-gap` reached 17-of-18 effectively complete at cycle-045, the residual 1 being `apply_linop` (lowers L3→L1 directly, no L2 entry / no L3-L2 theme by design — the cycle-010 "CONFIRMED-NOT-NEEDED-WITH-CAVEAT"). The cycle-050 demotion does NOT re-open the coverage-gap: the demoted operators' rotations are captured in-line, not lost.)*
- **(superseded) Cohort growth + coverage-gap progress (firm 15 → 17; `l3-l2-rotation-theme-coverage-gap` 15-of-18 → 17-of-18) — cycle-045 two-landing cohort.**
```

### 3. `book/src/L3-L2/index.md` — erasure-scope taxonomy thin/substantive split denominators (13-of-17 / 4-of-17 → 9-of-13 / 4-of-13)

The `:67` taxonomy bullet states "13 of the 17 firm themes" are thin and "4 of the 17" are substantive. After the −4 demotion the substantive count is unchanged (4) but the denominator and the thin count change.

```edit:book/src/L3-L2/index.md
[old]: **Thin `-body-identity` identity-themes** (13 of the 17 firm themes — the BLAS-1 leaves, the fork-INDEPENDENT standalone floors, the fused composites, the constructed-operator gates) carry **no substantive erasure**:
[new]: **Thin `-body-identity` identity-themes** (9 of the 13 firm themes after the cycle-050 demotion — `krylov-step`, the BLAS-1 leaves `dot`/`nrm2`/`scal`/`axpy`/`axpby`/`axpbypcz`, and the two constructed-operator gates `jacobi-smoother`/`divfree-projector` (both DEMOTE-OK per the D8 audit, scheduled cycle-051); the four cycle-050-demoted standalone-floor / fused-composite identity-themes `assemble-diagonal`/`elementwise-product`/`reciprocal`/`normalize` are now in-line notes on their L3 entries, no longer theme files) carry **no substantive erasure**:
```

```edit:book/src/L3-L2/index.md
[old]: **Substantive / non-identity themes** (4 of the 17) carry real rotation content:
[new]: **Substantive / non-identity themes** (4 of the 13 — unchanged by the cycle-050 demotion, which removed only thin-identity themes) carry real rotation content:
```

### 4. `book/src/L3-L2/index.md` — "five fork-INDEPENDENT standalone-floor themes" → "four" (D3-flagged)

The `:47` §Vocabulary-cohort sub-header names "five fork-INDEPENDENT standalone-floor themes". Of those five (`assemble-diagonal`, `jacobi-smoother`, `divfree-projector`, `reciprocal`, `elementwise-product`), the cycle-050 demotion removes `assemble-diagonal`, `reciprocal`, `elementwise-product` (3 deleted) — leaving `jacobi-smoother` + `divfree-projector` as the only standalone-floor `-body-identity` theme files still present (both scheduled cycle-051). The producers (D3/D4/D5/D6) removed their own ROWS + bullets from this §Vocabulary-cohort sub-list (`:49`, `:52`, `:53`, `:54`); I own only the SUB-HEADER COUNT prose. (D3 flagged the "five → four" framing; the corrected count after this cycle's 3-of-5 standalone demotions is **two** standalone-floor theme files remaining, both cycle-051 DEMOTE-OK — I state the live count, not D3's preliminary "four".)

```edit:book/src/L3-L2/index.md
[old]: *Fork-INDEPENDENT standalone-floor body edges (cycle-042; the L3>L2 thin-identity edges of the five fork-INDEPENDENT L2 floors — each with **NO fold-parent**; all the leaf/gate-primitive analogue of `krylov-step-body-identity` with NO wrapper rotation):*
[new]: *Fork-INDEPENDENT standalone-floor / gate body edges (cycle-042; originally five fork-INDEPENDENT L2-floor edges, each with **NO fold-parent**; the leaf/gate-primitive analogue of `krylov-step-body-identity` with NO wrapper rotation). **Three were demoted to in-line notes cycle-050** (`assemble-diagonal-body-identity`, `reciprocal-body-identity`, `elementwise-product-body-identity` — degenerate identity-in-named-terms lowerings, per the 2026-06-01 vocabulary-shift redirect); the **two constructed-operator-gate edges below remain theme files this cycle** (`jacobi-smoother-body-identity`, `divfree-projector-body-identity`) and are both DEMOTE-OK for cycle-051 per the D8 verify-body audit — see the §Working-Notes cycle-050-vs-051 split:*
```

### 5. `book/src/L3-L2/index.md` — cycle-050-vs-cycle-051 split + D8 17-denominator note (NEW §Working-Notes bullet)

A new §Working-Notes bullet recording the c050-vs-c051 split + the D8 denominator correction explicitly, so the count drop is not misread as stranding. Inserted after the cohort-growth bullet (change 2).

```edit:book/src/L3-L2/index.md
[old]: - **Erasure-scope taxonomy (the substantive-L3>L2 axis — now four populated roots; the c044/c045 meta-phase-flagged taxonomy).**
[new]: - **Degenerate-cohort demotion: cycle-050-vs-051 split + the D8 17-denominator correction (so the count drop is NOT misread as stranding).** The 2026-06-01 vocabulary-shift redirect refactor-pass demotes the degenerate identity-in-named-terms `-body-identity` / `-leaf-identity` theme pairs to in-line notes. The c049 D3 audit worklist identified the candidate cohort; the demotion enacts across **two cycles**. **Cycle-050 (this cycle) demoted the 4 clean non-fold standalone pairs** — `assemble-diagonal`, `elementwise-product`, `reciprocal`, `normalize` (D3–D6; each a standalone leaf / gate / fused-composite with NO fold-parent, so the demotion is clean — no operator chapter collapses, only the thin theme files are deleted and their content folded into in-line §"Downward" notes). **Cycle-051 carries the remainder**: (i) the **fold-family** pairs — the BLAS-1 leaves `scal`/`axpy`/`axpby`/`axpbypcz` (which collapse into the new firm `linear_combination` combinator landed this cycle) + `dot` (which collapses into the new firm `inner_product` combinator; `nrm2` STAYS a standalone consumer, NOT a fold member — do-NOT-merge); (ii) the **DEMOTE-OK `jacobi-smoother` pair** (both edges, per the D8 verify-body audit); and (iii) **`divfree-projector-body-identity`** (the L3>L2 edge ONLY — DEMOTE-OK per D8). The L2>L1 edge **`divfree-projector-leaf-identity` is KEEP-substantive** (D8: it carries exactly one genuine step-4 `Grad->AddMult` apply-accumulate fusion rotation — a real translation across the L2↔L1 vocabulary boundary, not an identity; it stays a standalone theme). **D8 denominator correction**: the degenerate-cohort count used by the cycle-051 demotion plan and the batch-15 meta-phase is **17, not 18** — `divfree-projector-leaf-identity` is substantive, so it is NOT in the demotion denominator (one of the four c049-D3-head-only-flagged gated themes turned out substantive on body audit). The four cycle-050 thin-theme demotions are NOT stranded: their L3>L2 (and L2>L1) identity rotations are now captured as in-line notes on the standalone L3/L2 operator entries (each layer stays internally coherent — the redirect's prescribed resolution); the firm-theme count drops because the *vehicle* changed (theme file → in-line note), not because any rotation was lost. (Provenance: `reports/2026-06-01T195100Z-lifter-demote-{assemble-diagonal,elementwise-product,reciprocal,normalize}/CYCLE.md` D3–D6; `reports/2026-06-01T195100Z-cross-layer-cross-cutter-verify-divfree-jacobi/CYCLE.md` D8.)
- **Erasure-scope taxonomy (the substantive-L3>L2 axis — now four populated roots; the c044/c045 meta-phase-flagged taxonomy).**
```

### 6. `book/src/L2-L1/index.md` — cohort-growth log tally (firm 21 → 17) + cycle-050 demotion record

The `:78` cohort-growth-log bullet leads with "firm **19 → 21**" (cycle-047) as the current head; the running L2-L1 firm total is 21 firm + 1 partly-constructive (22 total on-disk, confirmed). The four `-leaf-identity` demotions (D3–D6) drop firm 21 → 17. I prepend a cycle-050 demotion entry to the most-recent-first growth log.

```edit:book/src/L2-L1/index.md
[old]: - Cohort growth log (most-recent first): `ksp-solve-outer-driver-unfold` (D3) + `krylov-step-kernel-defusion` (D4) firm cycle-047
[new]: - Cohort growth log (most-recent first): **[cycle-050 DEMOTION — firm 21 → 17 (= 17 firm + 1 partly-constructive; 4 thin `-leaf-identity` themes deleted)]** the 2026-06-01 vocabulary-shift-redirect refactor-pass demoted four degenerate identity-in-named-terms `-leaf-identity` L2>L1 themes to in-line §"Downward to L1" notes on their L2 operator entries — `assemble-diagonal-leaf-identity` (D3), `elementwise-product-leaf-identity` (D4), `reciprocal-leaf-identity` (D5), `normalize-leaf-identity` (D6); each was the L2>L1 thin-identity edge of a standalone leaf / gate / fused-composite floor with NO fold-parent (so no operator chapter collapses — only the thin theme files are deleted, their content folded into in-line notes), value-thread-isomorphic across the edge (same signature / laws / variant profile — the §1d smell the redirect names). The L2>L1 rotation for each remains captured (in-line, not a theme file); only the firm-theme COUNT drops. **NOTE the cycle-050-vs-051 split (see also `L3-L2/index.md` §Working-Notes):** the fold-family pairs (`scal`/`axpy`/`axpby`/`axpbypcz`-`leaf-identity` → collapse into the new firm `linear_combination`; `dot-leaf-identity` → collapse into the new firm `inner_product`; `nrm2-leaf-identity` STAYS — `nrm2` is a do-NOT-merge consumer) + the `jacobi-smoother-leaf-identity` (DEMOTE-OK per D8) are **cycle-051**. **`divfree-projector-leaf-identity` is NOT demoted — KEEP-substantive** (D8 verify-body audit: it carries exactly one genuine step-4 `Grad->AddMult` apply-accumulate **fusion rotation**, a real L2↔L1 translation, NOT an identity; it stays a standalone theme alongside the other substantive composition-lowerings). So the demotion denominator the cycle-051 plan / batch-15 meta-phase uses is **17 across both lowering layers, not 18** (the D8 −1 correction). (Provenance: `reports/2026-06-01T195100Z-lifter-demote-{assemble-diagonal,elementwise-product,reciprocal,normalize}/CYCLE.md` D3–D6; `reports/2026-06-01T195100Z-cross-layer-cross-cutter-verify-divfree-jacobi/CYCLE.md` D8.) **[/cycle-050]** `ksp-solve-outer-driver-unfold` (D3) + `krylov-step-kernel-defusion` (D4) firm cycle-047
```

### 7. `book/src/L2-L1/index.md` — remove the three demoted §Vocabulary-cohort bullets D7 owns (D6 owns `:69` itself)

D4/D5 left their L2-L1 §Vocabulary-cohort cohort bullets for the count-owner (D5 report enumerates `:67`; D4 enumerates `:68`). The `assemble-diagonal-leaf-identity` bullet (`:64`) is D3's; D3's report removed only the table row and deferred the bullet handling. **D6 removes the `normalize-leaf-identity` bullet `:69` in its own change 6b — so D7 does NOT touch `:69`** (the repairer dropped D7's original duplicate `:69` edit block, which would have collided with D6's removal under serial apply; cycle-050 D7 META critique Issue 1). I remove the **three** demoted bullets D7 owns — `:64` (assemble-diagonal), `:67` (reciprocal), `:68` (elementwise-product) — so the §Vocabulary-cohort "fork-INDEPENDENT standalone-floor edges" sub-list reflects the collapsed cohort. (These are the cohort-narrative bullets — count-owner scope — NOT the dep-map TABLE rows, which D3–D6 own.)

```edit:book/src/L2-L1/index.md
[old]: - `assemble-diagonal-leaf-identity` — the L2 `assemble-diagonal` operator-to-data floor lowers to the L1 `assemble-diagonal` leaf identity-in-form; **fork-INDEPENDENT, NO fold-parent** (the operator-to-data sibling of `apply_linop`); L2 fusion is **degenerate** (representation-specific diagonal-extraction mechanics absorbed into the representation axis, deferred to L1>L0); the **load-bearing matrix-free high-order-Nedelec approximate-diagonal non-law is preserved through the edge, NOT erased**.
[new]: - *(demoted cycle-050)* `assemble-diagonal-leaf-identity` — DEMOTED to an in-line §"Downward to L1" note on `book/src/L2/assemble-diagonal.md` (degenerate identity-in-named-terms, no fold-parent; the load-bearing matrix-free high-order-Nedelec approximate-diagonal non-law is preserved in the in-line note, NOT erased). No longer a theme file.
```

```edit:book/src/L2-L1/index.md
[old]: - `jacobi-smoother-leaf-identity` — the L2 `jacobi-smoother` constructed-operator-gate floor lowers to the L1 gate identity-in-form on the apply (single elementwise product); **fork-INDEPENDENT, NO fold-parent**; the L2 fusion observation is **negative** (no fused multi-operation kernel), so the edge is identity with the fusion treatment a documented no-op (NOT a fold deferral).
[new]: - `jacobi-smoother-leaf-identity` — the L2 `jacobi-smoother` constructed-operator-gate floor lowers to the L1 gate identity-in-form on the apply (single elementwise product); **fork-INDEPENDENT, NO fold-parent**; the L2 fusion observation is **negative** (no fused multi-operation kernel), so the edge is identity with the fusion treatment a documented no-op (NOT a fold deferral). *(DEMOTE-OK per the D8 verify-body audit; scheduled cycle-051 — still a theme file this cycle.)*
```

```edit:book/src/L2-L1/index.md
[old]: - `reciprocal-leaf-identity` — the L2 `reciprocal` floor lowers to the L1 `reciprocal` leaf identity-in-form; **fold-parent-FREE** (a nonlinear self-map), so NO fusion to defer (contrast `dot-leaf-identity`); only the transparent `s = 1/|z|²` complex-intermediate note; design-final on the leaf-vs-fold fork.
[new]: - *(demoted cycle-050)* `reciprocal-leaf-identity` — DEMOTED to an in-line §"Downward to L1" note on `book/src/L2/reciprocal.md` (degenerate identity-in-named-terms; fold-parent-FREE nonlinear self-map; the transparent `s = 1/|z|²` complex-intermediate note is preserved in the in-line note). No longer a theme file.
```

```edit:book/src/L2-L1/index.md
[old]: - `elementwise-product-leaf-identity` — the L2 `elementwise_product` floor lowers to the L1 leaf identity-in-form (ten laws + element-type / conjugation axes); **fork-INDEPENDENT, NO fold-parent** (a binary field op; the inverse-subsumption generalisation of `scal`), so NO fusion to defer — the L0 `forall_switch` per-element multiply is already unfolded.
[new]: - *(demoted cycle-050)* `elementwise-product-leaf-identity` — DEMOTED to an in-line §"Downward to L1" note on `book/src/L2/elementwise_product.md` (degenerate identity-in-named-terms; fork-INDEPENDENT, NO fold-parent — the inverse-subsumption generalisation of `scal`; the L0 `forall_switch` per-element multiply already unfolded). No longer a theme file.
```

(The `normalize-leaf-identity` §Vocabulary-cohort bullet at `:69` is **NOT touched here** — **D6 owns `:69`** and removes it in its own change 6b; repairer dropped D7's duplicate `:69` edit block to avoid the serial-apply collision, per the cycle-050 D7 META critique Issue 1. The `divfree-projector-leaf-identity` §Vocabulary-cohort bullet at `:66` is **NOT touched** — it is KEEP-substantive per D8 and stays a live theme bullet + live theme file.)

## Supporting evidence

- **D1** `reports/2026-06-01T195100Z-harvester-l3-linear-combination/CYCLE.md` — `+1` firm L3 (`linear_combination`); own dep-map row added after `scal`; tally DEFERRED to D7 (its OQ explicitly: "the new firm count is 16 (`linear_combination` added)").
- **D2** `reports/2026-06-01T195100Z-harvester-l3-inner-product/CYCLE.md` — `+1` firm L3 (`inner_product` rough-in→firm); own dep-map row `:29` upgraded to live link; tally `:62` DEFERRED to D7. (D1's "16" + D2's `inner_product` = **17** — reconciled by on-disk enumeration, not by trusting either single number.)
- **D3** `reports/2026-06-01T195100Z-lifter-demote-assemble-diagonal/CYCLE.md` — `−1`/`−1`; removed dangling live-link rows `L3-L2/index.md:21`, `L2-L1/index.md:22`; flagged the "five fork-INDEPENDENT standalone-floor" → "four" framing + the firm-count + thin-sub-count decrements for D7.
- **D4** `reports/2026-06-01T195100Z-lifter-demote-elementwise-product/CYCLE.md` — `−1`/`−1`; de-linked rows `L3-L2/index.md:24`, `L2-L1/index.md:26`; enumerated the cohort bullets (`L3-L2:53`, `L2-L1:68`) + count decrements for D7.
- **D5** `reports/2026-06-01T195100Z-lifter-demote-reciprocal/CYCLE.md` — `−1`/`−1`; rows `L3-L2/index.md:23`, `L2-L1/index.md:24` + bullets `:52`/`:67`; recommended the post-deletion `grep -rn` dead-link sweep (carried to Open questions below).
- **D6** `reports/2026-06-01T195100Z-lifter-demote-normalize/CYCLE.md` — `−1`/`−1`; removed its own rows + cohort bullets (`L3-L2/index.md:25,54,75`, `L2-L1/index.md:25,69,78`-token); tally DEFERRED to D7.
- **D8** `reports/2026-06-01T195100Z-cross-layer-cross-cutter-verify-divfree-jacobi/CYCLE.md` — verify-body verdict **3 DEMOTE-OK / 1 KEEP**: `jacobi-smoother-{body,leaf}-identity` + `divfree-projector-body-identity` DEMOTE-OK (cycle-051); `divfree-projector-leaf-identity` KEEP-substantive (one genuine step-4 fusion rotation). Degenerate-cohort denominator **17, not 18**.
- On-disk count verification (this dispatch): `ls book/src/L3-L2/*.md | grep -v index | wc -l` = **17** (matches the pre-demotion "firm 17"); `ls book/src/L2-L1/*.md | grep -v index | wc -l` = **22** (= 21 firm + 1 partly-constructive). Post-demotion: L3-L2 → 13, L2-L1 → 18 (17 firm + 1 partly-constructive).

## Open questions / caveats

- **Dead-link sweep after all c050 deletions land (D5's residual caveat — flagged for integrator-finalize).** The four sibling theme-file cohorts cross-reference each other's deleted slugs (`elementwise-product-*`, `normalize-*`, `reciprocal-*`, `assemble-diagonal-*`), and all four pairs are being deleted this cycle. D5 enumerated the residual references inside the soon-to-be-deleted files (which vanish with their files) but flagged the **risk**: if any one of the four demotions is rejected/repaired to NOT delete its theme file, the survivor will carry a dead `[…](./…-identity.md)` link to a deleted slug and break `linkcheck2`. **RECOMMENDATION for integrator-finalize**: after all c050 deletions + row/bullet edits land, run `grep -rn 'assemble-diagonal-body-identity\|assemble-diagonal-leaf-identity\|elementwise-product-body-identity\|elementwise-product-leaf-identity\|reciprocal-body-identity\|reciprocal-leaf-identity\|normalize-body-identity\|normalize-leaf-identity' book/src/` and confirm zero residual LIVE-LINK references (plain-text inline-code mentions in historical-narrative prose are build-safe and may remain). This is the build-step dead-link gate, not a count concern.
- **`linkcheck2` on the new firm L3 combinator rows.** D1/D2 each verified their forward-references; D2's `inner_product` row upgrade introduces a live link `[`inner_product`](./inner_product.md)` and D1's `linear_combination` row references `[`inner_product`](./inner_product.md)` as a sibling-fold — both resolve once D2's `book/src/L3/inner_product.md` lands. If D2 lands and D1 lands in the same finalize batch (both this cycle), the cross-reference is live-safe; if applied out of order, D1's plain-text `inner_product` mention (per its report, written plain-text pending the chapter) stays build-safe. No count concern — flagged so the integrator confirms both chapters land together.
- **The `:47` standalone-floor sub-header count — I stated "two remaining theme files", D3 said "four".** D3's "four" was its preliminary read (5 standalone − 1 assemble-diagonal = 4) BEFORE the full wave's scope was known. The live post-c050 count of standalone-floor `-body-identity` THEME FILES remaining is **two** (`jacobi-smoother` + `divfree-projector`, both cycle-051 DEMOTE-OK) — `assemble-diagonal` + `reciprocal` + `elementwise-product` (3 of the 5) demote this cycle. My change 4 states the live count. The discrepancy is resolved in my favor (I see the full wave; D3 saw only its own scope). No further reconciliation needed.
- **Historical cohort-growth figures left as historical record.** Per D5/D6, the cycle-043 "firm **15 → 19**" and cycle-042 "firm **10 → 15**" figures in the L2-L1 growth log are the *historical* state at those cycles; I did NOT retroactively edit them (they remain accurate as-of-their-cycle). My change 6 prepends the *current* cycle-050 demotion entry (firm 21 → 17) as the new most-recent-first head; the historical entries below it are unchanged. Same discipline for the L3-L2 `:66` superseded prior-growth tail (retained, marked superseded).
- **No `concepts/` page touched; one-layer-tally-set per invocation respected.** This dispatch writes only consolidated tallies + cohort narratives across the three named index files (the count-owner scope) — no operator entry, no theme body, no concept page authored. All edits are proposed-changes blocks; no `book/` mutation during dispatch.
