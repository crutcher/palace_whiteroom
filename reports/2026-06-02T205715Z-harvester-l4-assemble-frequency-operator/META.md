---
verifies: ../REPORT.md
critiqued_at: 2026-06-02T211500Z
critic_version: 1
checks:
  citation-validity: pass
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: pass
  cross-reference-integrity: pass
  edge-label-fidelity: pass
  plan-kind-consistency: pass
  skill-uptake-survey: pass
overall_status: ready
---

## Repair

No findings (all 8 critic checks pass). No repair needed; `overall_status: ready` set by orchestrator (clean report — repairer not invoked per the warn/fail-only rule). Integrator note: apply D1 before D2 (D2's count-owner tally links this entry's new L4 chapter).

# META: verification of "Formalize assemble_frequency_operator at L4" (cycle-069 D1)

## Critique

### Checks run

**citation-validity — pass.** `citecheck --scan` reports 21 ok / 0 failing, matching the report's claim verbatim. I re-read the load-bearing L0 pinpoints on-disk (recurrence-6, not `--anchor`): `rap.cpp:766-787` is the `BuildParSumOperator` fold-body excerpt — `:766-767` signature, `:770-771` the `MFEM_VERIFY(it != ops.end())` empty-guard (report's law-5 anchor), `:774-777` the shared-`FiniteElementSpace` precondition, `:779-780` the `SumOperator` seed (the report says `:780-781`; the `make_unique` actually spans `:779-780`, but this is inside the cited span and not a load-bearing pinpoint), `:781-787` the for-loop accumulate with the `coeff[i] != 0` zero-prune (law 4). The close-brace discipline note is exactly right: `:787` closes the for-region and `:789` opens `if (set_essential)` — so `:766-787` is correctly the body excerpt, not the function close, matching the L1 cap's same span. `spaceoperator.cpp:521-528` confirmed: `:527` is the `BuildParSumOperator({a0,a1,a2,ScalarType{1}},{K,C,M,A2})` forward, `:528` the close brace. `drivensolver.cpp:91-93` (fixed `K`/`C`/`M`), `:175` (`A2 = GetExtraSystemMatrix<ComplexOperator>(omega, ...)`), `:176-177` (`A = GetSystemMatrix(1.0+0.0i, 1i*omega, -omega*omega+0.0i, ...)` inside the frequency loop), `:180` (`ksp.SetOperators(*A, *P)`) all confirmed on-disk, matching the firm L1 cap. The firm-endpoint anchors into `L4/linear_combination` (`:196-204`, `:128-183`, `:206-249`, `:40`) and `L1/assemble_frequency_operator` (`:60-69`, `:89-108`) all resolve in-range. No `verified_against:` block in this report (harvester, not lowering-verifier) — round-trip sub-check not applicable.

**surface-or-evidence — pass.** This is a NEW firm L4 entry (a fresh surface authored from scratch), not a refinement of an existing operator/theme, so the surface-AND-rotation-claim gate is satisfied trivially: the proposal IS the surface. The warrant verdict + the L0 re-confirmation + the firm-endpoint inheritance constitute the evidence. Not a pure rotation_claim.

**rotation-quality — pass.** The load-bearing check this dispatch flagged. The warrant verdict (GENUINE-L4-ENTRY vs thin-note-in-`linear_combination`) is sound and is NOT a degenerate identity-mirror smell. The anti-mirror discipline targets authoring a *second fold algebra* (a mirrored `operator_linear_combination` chapter that re-derives the combination) — this entry explicitly does NOT do that: it re-expresses THROUGH `L4/linear_combination`'s operand-category variant axis (operator-operand corner), reads every law off the combinator at that corner, and adds no reduction rule. The combinator on-disk (`L4/linear_combination.md:196-204`) genuinely carries the operand-category axis (`tensor-operand | operator-operand`) and names `assemble_frequency_operator` as the c069-gated next-pull consumer — so the corner this entry occupies is a real, pre-declared distinction, not a rename. The genuine-entry justification is multi-grounded and each ground checks out: (a) the firm L1 cap already gave it a standalone home on the identical warrant (layer-coherence — L1 cap `:60-69` confirmed on-disk); (b) it is the named `map_solve` scope boundary the driven solve half references; (c) operand-category-corner distinctness from the combinator's tensor-operand `§Specializations` (which is reserved for the BLAS-1 leaves). The rotation through the combinator is correctly characterized as identity-in-form on the body (a monoid swap tensor-`+` → operator-`+` that preserves every law), so the entry's *value* is navigational/feature-surface (the assemble verb), not a new compression — which is the correct disposition for a firm-on-positive-structure / syntactic-identity entry (the `fe_assemble`/`linear_combination`/`apply_linop` precedent). This is the legitimate "named abstraction kept as a navigable verb" disposition, not a degenerate 1:1 mirror.

**variant-axis-coverage — pass.** Three axes declared and each disposed: operand-category (operator-operand corner — carried on the combinator, not re-declared, correct per replace-and-propagate), weight-schedule (affine-in-parameter; the `constant` corner explicitly named as the other corner), operand-stationarity (fixed-basis `{K,C,M}` | parameter-dependent-operand `A2`). The general `linear_combination` axes (arity, output-aliasing, element-type) are explicitly pinned: arity→4, element-type→complex (with the `real | complex` collapse explicitly scoped out, not hidden), output-aliasing→pure/out-of-place. No hidden branches.

**cross-reference-integrity — pass.** All link targets resolve on-disk: `L4/linear_combination.md`, `L1/assemble_frequency_operator.md`, `L4/fe_assemble.md`, `L4/solve_family.md`, `L1-L0/assemble-frequency-operator-rotation.md`, `concepts/black-box-vs-accelerated-kernels.md` all exist. The new slug `L4/assemble_frequency_operator.md` is confirmed ABSENT (no collision). The L4/index partition is respected: the report's edit anchors are the `krylov-step` §Vocabulary-cohort bullet (`index.md:34`, on-disk-matched) and the `krylov-step` dep-map row (`index.md:77`, on-disk-matched), inserting in alpha position BEFORE each — it does NOT touch the consolidated firm-count tally line (`**Firm at L4 (10 + 4 outer-driver)**`, `index.md:32`) nor the cycle-068/frontier prose (`:69`), which are explicitly deferred to D2. SUMMARY edit anchor (`SUMMARY.md:8-9`, Overview then krylov-step) matches on-disk. The firm-body fence-parity build-readiness guard passes: 14 fence markers = 7 balanced blocks; the `new:` block (`:34`→`:516`) encloses the full firm body — `## Status` (`:382`), Signature (`:127`), `## Algebraic laws` (`:203`), `## Evidence` (`:430`) all INSIDE the fence; the chapter body uses 4-space indented code (not nested fences), so no nested-fence parity hazard. The report's own `## Operator content` / `## Supporting evidence` sections sit outside the fence but are a summary, not the firm body — NOT the cycle-019 outside-fence defect.

**edge-label-fidelity — pass.** The downward edge is labeled L4→L1 (identity-in-form on the body, via the in-line non-adjacent-identity marker route — no dedicated theme file). The §"Downward to L1" prose discusses exactly that edge and correctly composes the transitive chain (`linear_combination`'s in-line L4>L3 identity, confirmed at `L4/linear_combination.md:206-249`, ∘ the BLAS-1-cohort lower identities ∘ this entry's specialization-to-L1-cap identity) and locates the substantive rotation at the L1>L0 `assemble-frequency-operator-rotation` (which exists). The non-adjacent in-line convention (no `L4-L1/` directory) is correctly applied. Edge label and prose agree.

**plan-kind-consistency — pass.** Declared kind is firm L4 operator entry; content shape matches — full signature, six laws read at the corner + two explicit non-laws, variant axes, downward edge, evidence with positive L0 anchors. No rough-in placeholders. The `firm` status uses the firm-on-positive-structure / syntactic-identity escape, correctly justified: every law is a read-off operator-algebra identity carried from the firm combinator + firm L1 cap, the missing dedicated unit test does not gate (no law is an unconfirmed runtime property — the `apply_linop`/`fe_assemble` situation, not the `eigsolve`-convergence situation). The two caveats (affine-modulo-`A2`; single-pipeline-by-design) are correctly classified as non-gating stated facts, not unconfirmed laws.

**skill-uptake-survey — pass.** The `disciplined-cross-pipeline-combinator-mining-gate` skill (exists on-disk) is invoked and applied: the report discharges the ≥2-witness bar by noting the combinator's existing tensor-operand witnesses + this operator-operand witness, and argues the single-pipeline-by-design landing owes no second-pipeline probe (transient bakes excitation into a captured TimeOperator; electro/magnetostatic capture single fixed `K`; eigenmode is opaque-library-owned). The skill reference is present and substantively used, not merely cited.

### Issues found

No blocking issues. Two cosmetic/below-threshold observations, neither a check failure:

1. **Minor citation-span imprecision (citecheck-clean, non-load-bearing).** The report's §Evidence and §Supporting-evidence describe the `SumOperator` seed as `rap.cpp:780-781`; the `std::make_unique<SumOperator>(...)` statement actually spans `:779-780` on-disk (`:781` is the `for` loop opener). This is inside the cited `:766-787` body span and is descriptive prose, not a standalone citation, so citecheck does not flag it and it carries no claim weight. Location: CYCLE.md §Evidence (`rap.cpp:766-787` bullet) and §Supporting evidence. Severity: trivial.

2. **Insert-position convention flagged-but-unresolved (by design — author surfaced it).** The report inserts the bullet / dep-map row / SUMMARY entry in *alpha position* per the dispatch instruction, while noting the L4 SUMMARY list + index dep-map are currently chronological (the directive-3 mdBook by-kind grouping + alpha re-sort is meta-phase-owned, restart-pending). The author explicitly flags that the integrator may prefer append-position pending the global reorg. This is a correctly-surfaced integration-convention choice, not a defect — recorded so the integrator picks the consistent convention. Location: CYCLE.md §Open questions / caveats, bullet 3. Severity: informational (integrator decision, not a content issue).
