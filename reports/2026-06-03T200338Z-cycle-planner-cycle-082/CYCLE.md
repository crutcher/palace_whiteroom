---
agent: cycle-planner
invoked_at: 2026-06-03T200338Z
scope: cycle-82 dispatch plan
status: pending
---

# Cycle 82 dispatch plan

## Goals selected this cycle

Batch-26 position 1/3 (cycles 082/083/084; meta-phase after 084). The batch-25 meta-phase enacted the **seed-surface firming ceiling** finding and reshaped the active head **away from seed-firming** toward (a) the highest-fan-out bottom-up vocabulary + (b) the 5-driver→L4 backend-lowering completeness picture, keeping ONE in-scope `lowering-verifier` law-confidence pass as the single seed-firming continuation. NO `.claude/agents/` change → no session restart.

The honest state of the frontier this cycle is **light**, and the plan reflects that without manufacturing rectangular work (the vocabulary-shift redirect forbids minting mirror floors):
- The **5-driver→L4 ASSEMBLE+SOLVE deliverable is COMPLETE** across all 5 pipelines (the driven solve-half — the last gap — closed c070; roadmap §Per-solver-pipelines / §Feature-surface-spine).
- The **FE-construction-input cohort is DELIBERATELY ABSORBED**, not stranded: `L4/fe_assemble.md:69,147,174` explicitly absorbs `fe_space`/`fe_collection`/`essential_dofs` into the `readonly` `FiniteElementSpace[N]` construction stratum ("none gets a standalone thin chapter… re-open per downstream-consumer demand"), and `weak_form_term` is NO-L2-by-warrant (`weak-form-term-own-l2-disposition`). These are not holes to fill.
- **Seed-firming is at its in-scope ceiling** (friction `seed-surface-firming-ceiling-needs-out-of-scope-assembly-tests`, recurrence-3, addressed): the reduce verbs' full-`firm` 2nd gate needs a positive assembly test the Palace corpus does not contain, and authoring tests is out of write-scope.

So the cycle pushes forward by: **(D1)** running the active-head-#1 **survey** that ranks the genuinely-open cleanly-describable bottom-up holes vs. the deliberate absorptions / findings / closed-but-stale-pointer items — the highest-fan-out work right now because it de-risks c083/c084 and prevents re-proposing absorbed/closed work; and **(D2)** the ONE in-scope seed-firming continuation — a `lowering-verifier` law-confidence pass on the one reduce verb whose folded primitives are both firm. Two dispatches, single parallel wave; this matches the two active-head picks exactly.

## Deliverable-presence verification

Per the MANDATORY pre-dispatch check (paste-inline evidence; CLAUDE.md / role-spec `verify-dispatch-scope-not-already-discharged`).

### D1 — `cross-layer-cross-cutter` spine-completeness survey
**SKIP the four-step check — open by construction.** D1 is an observation-only survey/audit of a cross-cutting cohort question (the audit-first framing the role-spec prefers for cohort-boundary questions); it resolves to NO named `book/src/` artifact slug it would author (it emits OQ intake + a plan-candidate appendix, no `book/` mutation). The role-spec exempts "a cross-cutting cohort question / audit-first dispatch" from the named-slug deliverable-presence sequence. Verification that the survey is non-redundant is the survey's own first task (it is precisely a freshness audit). One concrete staleness datum already confirmed below feeds it.

Supporting freshness datum (confirms the survey is needed, NOT that it is discharged):
```
$ ls book/src/L2-L1/orthogonalize-composition-lowering.md
book/src/L2-L1/orthogonalize-composition-lowering.md            # FIRM since c022
$ grep -i "orthogonalize-composition-lowering-l2-l1" scaffolding/open-questions.md | grep -i RESOLVED
... resolved cycle-022 — book/src/L2-L1/orthogonalize-composition-lowering.md firm (MGS/CGS/CGS2 ...)
```
— yet the §Open–deferred ledger line 166 still carries `orthogonalize-composition-lowering-l2-l1-theme` as "not yet authored (abstractor)". This is exactly the stale-pointer class the survey exists to catalogue (closed/landed work that stale OQ pointers still name), confirming the survey's fan-out.

### D2 — `lowering-verifier` on `book/src/L4/eigenfreq_qfactor_reduce.md` (NAMED slug → four-step check)

1. **File existence:**
```
$ ls -la book/src/L4/eigenfreq_qfactor_reduce.md
-rw-rw-r-- 1 crutcher crutcher 21562 Jun  3 12:13 book/src/L4/eigenfreq_qfactor_reduce.md   # EXISTS
```
2. **Maturity / already-discharged:**
```
$ grep -nE "^## Status|firmness:" book/src/L4/eigenfreq_qfactor_reduce.md
4:firmness: rough-in                 # rough-in (test-coverage-bounded) per c079/c080 narration
183:## Status
$ grep -c '^verified_against:' book/src/L4/eigenfreq_qfactor_reduce.md
2                                     # 2 blocks = the c079 test-COVERAGE citation (output-cache round-trip)
```
The proposed dispatch's deliverable (a **law-confidence / assembly-map** verdict that either promotes to `firm` or records the ceiling) is ABOVE the on-disk maturity — the 2 existing `verified_against:` blocks are the *test-coverage* gate (gate-(b) discharge to `(test-coverage-bounded)`), NOT the assembly-confidence audit this dispatch runs. NOT a no-op.
Folded primitives both firm (the precondition the pass requires):
```
$ grep -m1 firmness: book/src/L1/participation_ratio.md book/src/L1/eigenvalue-untransform.md
book/src/L1/participation_ratio.md:firmness: firm
book/src/L1/eigenvalue-untransform.md:firmness: firm
```
3. **OQ-ledger RESOLVED-grep:**
```
$ grep "eigenfreq-qfactor-reduce-firm-needs-assembly-test" scaffolding/open-questions.md | grep -iE "RESOLVED|CLOSED"
(no matches)   # the OQ is OPEN; its Trigger explicitly names "an in-scope lowering-verifier
               #  law-confidence pass (now that both folded primitives are firm)" as the route
```
4. **Structural-block check:** NOT structurally blocked. The blocking gate is the assembly-confidence question; the OQ `eigenfreq-qfactor-reduce-firm-needs-assembly-test` (opened c080 D2) names *this exact dispatch* — a lowering-verifier pass on a both-primitives-firm verb — as the in-scope promotion route. The `seed-surface-firming-ceiling` friction (recurrence-3, status `addressed`) does NOT bar the pass; it predicts the verifier may CONFIRM the ceiling rather than discharge it, which is a sanctioned clean outcome (the verdict is a finding either way). Both folded primitives are firm (gate-(a) discharged c080), so the precondition is met. PASS.

**All checks pass for D2; D1 is exempt (open by construction).** Neither scope matches the STOP-PROPOSING negative list (`lu_solve`/`back_solve`/`ls-update-column`/the `nleps_*` L3-backfills). D1's framing is audit-first (correct for the cohort-boundary survey question); D2's framing is the role-named law-confidence verifier pass (correct).

## Dispatches

1. **agent**: `cross-layer-cross-cutter`
   **scope**: THE LEAD — the active-head-#1 spine-completeness SURVEY. A systematic cross-layer audit ranking the genuinely-open, cleanly-describable bottom-up vocabulary pull-ups + any per-pipeline L4-path holes, EXPLICITLY partitioning candidates into: **(a) deliberate absorptions / NO-FLOOR-by-warrant** (the FE-construction inputs absorbed into `L4/fe_assemble.md:69,147,174`; `weak_form_term` NO-L2 per `weak-form-term-own-l2-disposition`; the `solve_family`/`fold_solve`/`fe_assemble` NO-FLOOR warrants) — record as findings, do NOT propose floors; **(b) trigger-gated / pull-gated** items (the `Divergence`/div-div 4th weak-form-term axis-point — no in-scope solver-K witness, likely a permanent spine-coverage finding; the rectangular/multilevel `fe_assemble` axes `fe-assemble-rectangular-and-multilevel-axes`; the BC-elimination L1>L0 split `fe-bc-elimination-l1-l0-theme-split-vs-fold`; `discrete-linear-operator-interpolation-sibling`) — record the trigger, do NOT dispatch; **(c) closed/landed work that stale OQ pointers still name** (e.g. `orthogonalize-composition-lowering` L2>L1 FIRM since c022, but §Open–deferred line 166 still calls it unauthored) — flag for meta-phase ledger unification; **(d) genuinely-open cleanly-describable pull-ups** — the actual fan-out-ranked candidate set for c083/c084. Output = a fan-out-ranked plan-candidate appendix (consumed by the c083 planner) + OQ intake for the stale-pointer / finding items. **Observation-only — emit ZERO `book/` mutation** (the dispatch-phase write-partition guard; route findings via the report's Open-questions block + a plan-candidate section, NOT direct edits to `priorities.md`/`book/`). Cite the source-of-truth on-disk for any absorption/warrant claim. fan-out: HIGH.
   **deps**: none

2. **agent**: `lowering-verifier`
   **scope**: the ONE in-scope seed-firming continuation — a law-confidence audit of `book/src/L4/eigenfreq_qfactor_reduce.md` (`rough-in (test-coverage-bounded)`, 2 existing `verified_against:` blocks). The verb folds two now-firm L1 per-mode scalar maps: `participation_ratio` (κ-half, firm c077) + `eigenvalue-untransform` (ω-half, firm c080). Audit whether the eigenpair→`(f,Q)` **assembly-map** confidence can be raised to `inner_product`-equivalent via the firm-on-positive-structure escape (the read-off-syntactic-identity reasoning that firmed `fe_assemble`/`fold_solve`), i.e. whether the verb's reduction laws are positive-structure syntactic identities over the two firm folded maps + a positive assembly site, OR whether they encode a runtime assembly claim that only a positive assembly test could confirm. **Two clean outcomes, both sanctioned:** (i) IF the assembly-map confidence raises → promote `rough-in (test-coverage-bounded)`→`firm`, attach the `verified_against:` block, AND **COUPLED-re-anchor** the direct critical-path consumer `book/src/feature/eigenfrequency-qfactor.{L4,L1}.md` off `seed` (the floor-landing-implies-same-cycle-adjacent-entry-reanchor guard — bundle the column re-anchor INTO this dispatch; do NOT defer to a later lifter, opening a stale-assertion window; the `.L0` sibling carried no staleness per c081, leave it); (ii) IF the confidence CANNOT raise without a positive assembly test (the `seed-surface-firming-ceiling` prediction) → RECORD that verdict in the verb's `## Status` + a findings note, leave the verb `rough-in (test-coverage-bounded)`, leave the column `seed` (NOT a forcing — the ceiling is the finding). Do NOT author new tests (out of write-scope). Resolves/advances OQ `eigenfreq-qfactor-reduce-firm-needs-assembly-test`. fan-out: MEDIUM.
   **deps**: none

## Overlap analysis

- **D1 × D2** — NON-overlapping. D1 is observation-only (emits zero `book/` mutation — OQ intake + a plan-candidate appendix only). D2 touches `book/src/L4/eigenfreq_qfactor_reduce.md` and, in its promote-branch, `book/src/feature/eigenfrequency-qfactor.{L4,L1}.md`. There is no shared artifact region and neither names an operator the other authors. D1's survey *catalogues* the seed-firming-ceiling situation that D2 *tests* on one verb, but they touch disjoint surfaces and D2's verdict is itself a datum D1's survey will frame at the cohort level — the mild conceptual adjacency is signal, not conflict. Per the conflict-tolerance philosophy (when in doubt, PARALLEL), these run parallel. No shared running-count / consolidated-tally index is written by either (D1 writes no index; D2 touches the L4 verb file's own Status + a single column's dep-map cells, not a cross-cohort tally) — the parallel-blind-shared-index guard and the dual-registration partition are N/A this cycle (no ≥2-parallel-landing into one index). No cross-report forward-reference of a not-yet-existing slug (the slug-divergence guard is N/A).

## Sequencing schedule

**Single wave (both parallel).**
- **Wave 1 (parallel):** D1 (`cross-layer-cross-cutter` survey), D2 (`lowering-verifier` law-confidence pass).

Then the standard tail: 2 critics (parallel) → repairer(s) on any warn/fail → `integrator-per-report` ×(ready reports, serial) → ONE `integrator-finalize` (rebuild + commit + push + housekeeping). No inter-wave book rebuild; one finalize at cycle end.

## Open questions / caveats

- **This is intentionally a light cycle, and that is the correct read of the frontier — not under-planning.** The 5-driver→L4 deliverable is complete, the FE-construction cohort is absorbed-by-design, and seed-firming is ceiling-bound. The redirect forbids manufacturing rectangular pull-ups to fill slots. The two dispatches are the two genuinely-actionable, cleanly-describable, non-forcing items in the active head. If D1's survey surfaces a high-fan-out cleanly-describable pull-up, c083 picks it up with a fuller wave; if it confirms the frontier is genuinely played out on the existing corpus, that is itself the key finding for the batch-26 meta-phase (it would bear on the "is the in-scope target substantially complete" question the redirect flagged as NOT-yet-settled — memory `project_vocabulary_shift_redirect`).
- **D2 likely confirms the ceiling rather than discharging it** (the friction-ledger prediction). That is an explicitly-sanctioned clean outcome — the active head #2 says "if the verifier finds the assembly-map confidence cannot be raised without a positive test, that RECORDS the ceiling cleanly." I dispatched it anyway because (i) it is the one in-scope seed-firming route and the OQ names it directly, (ii) the cost is one bounded lowering-verifier read of an existing on-disk chapter + two firm L1 entries (no heavy-region localization), and (iii) a clean recorded verdict either way retires the open OQ from the "maybe-actionable" state. If a future cycle finds D2-class passes systematically just re-confirm the ceiling, the next meta-phase should consider closing the `reduce-verb-law-confidence-lowering-verifier-pass` route as exhausted rather than re-proposing it per verb.
- **Stale OQ-ledger pointers for landed work** (the `orthogonalize-composition-lowering` line-166 example; possibly others in the §Open–deferred/contingent FE-assembly-siblings and inner_product families which are migration-trail residue). These are meta-phase ledger-unification work, not cycle-082 dispatches; D1's survey will enumerate them as a batch-26-meta-phase input. Flagging here so the next meta-phase (after c084) catches the ledger drift even if D1's enumeration is partial.
- **Cadence note:** the friction-ledger / priorities I read are at most ~0 cycles stale (the batch-25 meta-phase just ran and reshaped the active head). No pattern observed this cycle warrants a methodology adjustment beyond the ledger-unification note above.
