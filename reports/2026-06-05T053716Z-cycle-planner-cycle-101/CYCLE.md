---
agent: cycle-planner
invoked_at: 2026-06-05T053716Z
scope: cycle-101 dispatch plan
status: pending
---

# Cycle 101 dispatch plan

**Batch-32, position 2/3** (cycles 100/101/102; the batch-32 meta-phase fires AFTER cycle-102's
finalize as a SEPARATE dispatch aggregating 100/101/102). Fresh session is NOT required — cycle-100
made NO `.claude/agents/` changes. Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT
(`METHODOLOGY-REDIRECT.md`) + the 2026-06-02/03 feature-spine directives + the 2026-06-04 GRADED
RESOLUTION LADDER (`METHODOLOGY-GRADED-STACK.md`). Post-graded-stack-campaign frontier.

## Goals selected this cycle

Push the **one genuine in-scope L4-completeness hole** the cycle-100 backend-lowering survey
narrowed to — the **BC-elimination cohort L4-disposition** (the BC-half analog of the assemble-half
closed c068; HIGH fan-out = the 5 driver columns; the on-disk separable-post-composition law makes
this a clean warrant-first decision, NOT a forced mirror) — and in parallel discharge the
highest-value hygiene lead, the **`concepts/index.md` + `concepts/dependency-map.md` orchestrator-era
framing refresh** (whole-file pre-redirect staleness + ~40 stale Mermaid edges keyed on deleted
slice-slug nodes; the natural landing site for the graded-stack typed-edge re-derivation). Two
dispatches, one wave. The eligible bottom-up frontier is genuinely light (`promotion_frontier: 8`,
remaining members obstruction / demand-gated — `bicgstab`/`minres`/`deflate*`/`boundary-mode.*`,
NOT targets; `eigsolve-convergence-reason-mapping` structurally blocked) — so per the redirect's
anti-rectangular discipline I deliberately do NOT manufacture filler dispatches (consistent with the
c088/c090/c092 light-frontier precedent).

## Dispatches

### D1 — BC-elimination cohort L4-disposition (`harvester`) — HIGH fan-out, WAVE 1

- **agent**: `harvester`
- **scope**: Decide — and enact — the L4 disposition of the firm-L1 **boundary-condition
  elimination cohort** `eliminate_essential_bc` + `eliminate_rhs` (fed by their `DofSet[N]`
  producer `essential_dofs`). This is the BC-half of plan-tag `fe-cohort-l4-lift` (the assemble-half
  closed c068), resolving OQ `bc-elimination-cohort-l4-disposition`. **Warrant-first decision
  between two routes (do NOT force; record the warrant either way):**
  - **Route (a) — rise as a small post-assembly combinator.** The on-disk **separable
    post-composition law** strongly supports this: `eliminate_rhs(fe_assemble(space, terms), x_bc,
    b, policy)` consumes the *already-assembled* operator `K` and is **independent of HOW `K` was
    assembled** (`book/src/L1/eliminate_rhs.md:142-144`; `book/src/L1/eliminate_essential_bc.md:19-22`
    — "composes AFTER `fe_assemble`, NOT part of the assembly fold"). If route (a): author the L4
    cap(s) as an operator→operator BC-pin verb + an RHS-lift verb under the L4 strawman conventions
    (`book/src/design/l4_calculus.md`; Haskell `::` signatures, TS records, `text`-fenced), firm-on-
    positive-structure escape if the laws are read-off syntactic identities over the firm L1 cohort +
    firm `fe_assemble`. **Canonical slugs IF route (a)** (this single dispatch authors both the cap
    and its dissolution theme — no cross-report forward-reference divergence this cycle): L4 cap
    `book/src/L4/eliminate_bc.md` (combine the operator-pin + RHS-lift verbs into one cohort cap, OR
    two thin caps `book/src/L4/eliminate_essential_bc.md` + `book/src/L4/eliminate_rhs.md` per the
    judgment on whether they are one verb or two — state the warrant); coupled L4>L3 dissolution
    theme `book/src/L4-L3/bc-elimination-post-composition-dissolution.md` (canonical slug). Wire the
    cap into `L4/index.md` disposition list + dep-map table in ALPHA position (per the by-kind /
    alpha-within-kind convention), with typed `edges:` frontmatter + `rank:` (graded-stack HARD-gate-
    new; deps are firm so the rank invariant holds firm/firm).
  - **Route (b) — explicit no-L4-by-design (setup/construction-stratum) verdict.** If the warrant
    is that BC elimination is a construction-stratum concern absorbed into the driver columns'
    assemble preface the way `fe_space`/`fe_collection` absorb into `fe_assemble`, record the
    explicit no-L4-by-design verdict in the L1 cohort entries' §Status / Downward prose + a one-line
    note in `L4/index.md` (replacing the current "L4 disposition open" parentheticals). The
    separable-post-composition law makes (b) the WEAKER reading per the OQ, but the dispatch must
    test it, not assume (a).
  - **MANDATORY coupled re-anchor (same-cycle adjacent-entry re-anchor, per the floor-landing-
    implies-same-cycle-adjacent-entry-reanchor convention):** whichever route, the dispatch OWNS the
    three `essential_dofs` mis-attribution parenthetical corrections in the `L4/fe_assemble.md`
    chapter body at `:69` (shape-contract bullet), `:147` (`state-stratification` concept-reference
    bullet), `:175` (`## Status` variant-axis paragraph) — each currently carries the identical
    `(fe_space / fe_collection / essential_dofs)` absorbed-list parenthetical that wrongly attributes
    `essential_dofs` to `fe_assemble`'s readonly construction stratum (on disk `essential_dofs`
    produces the `DofSet[N]` the *post-assembly* BC cohort consumes — `L1/essential_dofs.md:22-23,72`;
    `L1/fe_assemble.md:60` signature carries no `essential_dofs` parameter). The cycle-100 survey
    corrected the `L4/index.md:48,:100` sites but deliberately left these three body sites to land
    TOGETHER with this disposition (recorded in the OQ). Update the `L4/index.md:48,:100` "L4
    disposition open (see OQ …)" parentheticals to the now-decided verdict in the same pass.
  - **Record-definition obligation note:** `DofSet[N]` is the BC cohort's shared record (named in
    `essential_dofs` output + both BC ops' input). It has 3 consumers and NO `concepts/<record>.md`
    home (`ls book/src/concepts/ | grep -i dofset` → none). If route (a) names `DofSet[N]` in an L4
    signature, this crosses the ≥2-consumer record-definition bar — either author
    `book/src/concepts/dofset.md` (the data-shape page) OR flag `record-DofSet-needs-definition-home`
    in Open questions per the record-definition obligation. Judge on touch; do NOT force a page if
    the L4 form does not surface the record.
- **deps**: none.
- **L0 anchors (codemap-confirmed this cycle — localization hints, on-disk-confirm before citing;
  the firm L1 cohort entries already carry the full verified citation set, so this is a decision +
  composition task, NOT a heavy localization):** `palace/linalg/rap.cpp:56`
  (`ParOperator::EliminateRHS`), `EliminateBC` call sites `palace/models/modeeigensolver.cpp:571,574,
  608,611` + `palace/linalg/rap.cpp:143` + `palace/models/laplaceoperator.cpp:252` (`PtAP_K->
  EliminateRHS`). The mis-attribution-correctness anchors: `L1/essential_dofs.md:22-23,72`,
  `L1/fe_assemble.md:60`, `L1/eliminate_rhs.md:142-144`, `L1/eliminate_essential_bc.md:19-22`.
- **rationale**: The cycle-100 D3 backend-lowering completeness survey confirmed all 5 solvers + 5
  output-products + the assemble-half reach firm L4, and narrowed the ONE genuine in-scope L4 hole
  to exactly this cohort (integrator-signals c100 §Resolution-implications; log/cycle-100.md
  signal 2; memory `project_l4_is_backend_lowering_target` — L4 IS the outward backend-lowering
  target, so closing this hole is the standing-goal forward frontier). HIGH fan-out: every solver
  pipeline performs essential-BC elimination on the assembled operator + RHS-lift before the solve,
  so the fan-out is the 5 driver columns. Deliberately routed `harvester` (per the audit-first
  framing: this is an operator-disposition decision at a cohort boundary with a representation-
  dependent caveat — the harvester makes the warrant-backed rise-vs-no-L4 call and authors the
  positive form; NOT a reflexive mirror).

### D2 — concepts/index + dependency-map orchestrator-era framing refresh (`layer-intro-author`) — Medium fan-out, WAVE 1

- **agent**: `layer-intro-author`
- **scope**: Whole-file refresh of `book/src/concepts/index.md` + `book/src/concepts/dependency-map.md`,
  resolving OQ `concepts-index-and-depmap-orchestrator-era-framing-refresh` (+ folding in the
  re-scoped premise of `dependency-map-cg-precond-stale-mermaid-edges-RESCOPE-CORRECTION`).
  - **`concepts/index.md`:** strip the pre-redirect orchestrator/slice-era framing — decommissioned
    `prompts/*` references, `concept_writes mode=create` recipes, `spec/slices/*` grep recipes
    (the `orchestrator/`/`prompts/`/`schemas/` dirs were DELETED batch-31; `spec/slices/` is empty).
    Re-frame to the live 14-subagent / layered-spec model.
  - **`concepts/dependency-map.md`:** re-derive the Mermaid node SET against the **live firm-chapter
    graph** — the actual stale edges are **~40 edges keyed on deleted krylov-trio + chebyshev-cohort
    slice-slug nodes** (`gmres`, `orthog`, `arnoldi_step`, `cg`, `gmres-L3`, `gmres-L4`,
    `plane-rotation-stream`), NOT "~22 edges off a `cg_preconditioning_framework` node" (the
    re-scope correction: there is NO `cg_preconditioning_framework` Mermaid node; that string
    survives only as accurate prose provenance in `concepts/rotation.md:136`, `L4/index.md:119`,
    `L4/preconditioning-framework.md:336` — leave those). Build-safe (Mermaid node-labels, not
    markdown links) but they point at deleted slices. **Natural pairing with the graded-stack
    typed-edge campaign (priorities #0):** the dep-map node/edge re-derivation IS where the typed-
    edge (`depends-on` vs `reference`) annotation would land — annotate the re-derived edges with the
    typed-edge distinction where it is cleanly readable (do NOT block the refresh on a full
    artifact-wide typing pass — that is the meta-phase-owned standing campaign).
  - **Adjacent residue (same refresh):** normalize the CG Form-B provenance pointer at
    `book/src/L4/krylov-step.md:254` (`spec/slices/cg.md:27-141` / `cg.md:393-425`) to the
    git-history phrasing — it is the CG sibling of the two GMRES/arnoldi pointers D4 repointed c100,
    narratively framed ("absorbed here in cycle-099 so the slice is clear-to-delete") with its own
    live link + L0 ground; same post-deletion residue class.
  - **Wiring caution:** `dependency-map.md` is wired into `SUMMARY.md:292` with an inbound citation
    from `L1-L0/minres-iteration.md:115` — preserve the anchor target.
- **deps**: none.
- **rationale**: integrator-signals c100 §Suggested-next-dispatches candidate (a); log/cycle-100.md
  signal 3a. Whole-file pre-redirect staleness (not a few dead pointers) → a careful
  `layer-intro-author` refresh, NOT a residue micro-sweep. Pairs with the standing graded-stack
  typed-edge work. Medium fan-out (narrative honesty + the dep-map is a reader navigation surface;
  the typed-edge annotation seeds the meta-phase's standing typing campaign).

## Overlap analysis

- **D1 ∩ D2:** **NON-OVERLAPPING → PARALLEL.** D1 touches `book/src/L4/eliminate_bc.md` (or the two
  thin caps) + `book/src/L4-L3/bc-elimination-post-composition-dissolution.md` (new files IF route
  (a)) + `book/src/L4/index.md` (disposition list + dep-map row + the `:48`/`:100` parenthetical
  updates) + `book/src/L4/fe_assemble.md:69,147,175` (the mis-attribution body sites) + possibly the
  3 L1 BC-cohort entries' §Status (route (b)) or `concepts/dofset.md` (record obligation). D2
  touches `book/src/concepts/index.md` + `book/src/concepts/dependency-map.md` + `book/src/L4/
  krylov-step.md:254` (one provenance line). **Verified disjoint:** `grep -nc "eliminate|
  essential_dofs|fe_assemble" book/src/concepts/dependency-map.md` → **0** (D2's dep-map files carry
  NO BC/fe_assemble references, so D2's node re-derivation does not touch D1's BC additions); D1 does
  NOT touch `concepts/` or `krylov-step.md`. The one nominally-shared Part (L4) is touched at
  byte-disjoint files/lines: D1 at `L4/index.md` + `L4/fe_assemble.md` + new L4 caps; D2 at
  `L4/krylov-step.md:254` ONLY. No shared operator names, no shared file regions, no shared
  consolidated tally (only one of the two lands into `L4/index.md`, so no parallel-blind shared-index
  count concern). Mark PARALLEL.

## Sequencing schedule

- **Wave 1 (parallel):** D1, D2.
- No wave 2: both dispatches are independent, neither forward-references the other's not-yet-landed
  slug (D1's BC L4 caps are not referenced by D2's concepts refresh; D2's dep-map node set is
  re-derived against the live firm graph independent of D1's BC additions, which land as new firm
  nodes the NEXT dep-map refresh picks up — not this one). One wave.
- Pipeline reminder: ONE `integrator-finalize` at cycle end (waves order dispatches, not finalizes;
  the book is not rebuilt between waves).

## Deliverable-presence verification

Per the MANDATORY paste-inline-evidence pre-dispatch check. Both dispatches verified this cycle.

**D1 — BC-elimination cohort L4-disposition:**
1. **File existence (the L4 deliverable is OPEN — must NOT already exist):**
   `ls book/src/L4/ | grep -iE "eliminate|essential|bc"` → (empty; `assemble_frequency_operator.md`,
   `fe_assemble.md` are the only matches when grepping `assemble`, NOT BC). `ls book/src/L4-L3/ |
   grep -iE "eliminate|bc"` → (empty; only `fe-assemble-fold-dissolution.md` matches `assemble`).
   `ls book/src/L3 book/src/L2 | grep -iE "eliminate|essential"` → `none`. **VERDICT: no BC L4 /
   L4-L3 / L2 / L3 entry exists — the lift is genuinely OPEN.**
2. **Maturity of the source L1 cohort (firm — the lift rests on firm deps):**
   `eliminate_essential_bc.md` §Status = `` `firm`. **Clean-gate call: PROMOTE — clean.** ``;
   `eliminate_rhs.md` §Status = `` `firm`. **Clean-gate call: PROMOTE — clean.** ``;
   `essential_dofs.md` §Status = `` `firm` — FE-space sub-spine essential-dof-set constructor. ``.
   **VERDICT: all three firm on disk → an L4 lift rests on firm deps (rank invariant holds firm/firm).**
3. **OQ-ledger RESOLVED-grep:** `grep -iE "bc-elimination.*RESOLVED|bc-elimination.*CLOSED|
   eliminate.*RESOLVED" scaffolding/open-questions.md` → **no matches.** The OQ
   `bc-elimination-cohort-l4-disposition` is OPEN (opened c100, `## bc-elimination-cohort-l4-disposition`
   present, not in the Closed index). **VERDICT: open.**
4. **Structural-block check:** NOT blocked. The c069 record is a *deferral* ("sibling deferred
   operators … gated on primitive-L4-presence") — and the primitive L4 presence it was gated on
   (firm `fe_assemble` L4 cap) LANDED c068, AND the L1 cohort is firm — so the gate has fired; this
   is the decision the deferral was waiting for. The on-disk separable-post-composition law
   (`eliminate_rhs.md:142-144`) is a positive structural site (NOT a negative-anchor / opaque-library
   block). **VERDICT: open by construction (the deferral trigger has fired; no structural block).**
   NOT on the STOP-PROPOSING NEGATIVE LIST (`lu_solve`/`back_solve`/`ls-update-column`/4 NLEPS
   atoms / `L3/solve_family`/`L2/fold_solve`/`L2/fe_assemble`/`weak_form_term`/`map_solve` — none is
   a BC-elimination slug). Framing = audit-first / harvester-disposition (correct per the cohort-
   boundary + representation-dependent-caveat test).

**D2 — concepts/index + dependency-map refresh:**
1. **File existence (refresh targets must EXIST):** `ls -la book/src/concepts/index.md
   book/src/concepts/dependency-map.md` → both present (`index.md` 6442 bytes, `dependency-map.md`
   15050 bytes). **VERDICT: both exist (refresh, not create).**
2. **Maturity / already-discharged:** these are concept/navigation pages (no `## Status` maturity
   ladder; they are not operator entries). The cycle-100 D4 class-B sweep explicitly left them out of
   scope (`out of micro-sweep scope — a careful dep-map refresh, not a drive-by string-swap`,
   integrator-signals c100). **VERDICT: not yet discharged; the refresh is the open work.**
3. **OQ-ledger RESOLVED-grep:** `grep -iE "concepts-index-and-depmap.*RESOLVED|
   concepts-index-and-depmap.*CLOSED" scaffolding/open-questions.md` → **no matches.** The OQ
   `concepts-index-and-depmap-orchestrator-era-framing-refresh` is OPEN (opened c100). **VERDICT: open.**
4. **Structural-block check:** NOT blocked (a whole-file refresh of build-safe content; the dep-map
   edges are Mermaid node-labels, not markdown links, so no `linkcheck2` gate). The
   `dependency-map-cg-precond-stale-mermaid-edges-RESCOPE-CORRECTION` OQ supplies the corrected
   premise (the c100 finalize confirmed the literal "~22 edges off a single node" premise was
   inaccurate; the real set is ~40 edges on deleted slice-slug nodes). **VERDICT: open; premise
   corrected and folded in.**

## Plan-update record (priorities.md)

I appended a cycle-101 planner-reshape note to the `## CYCLE-100 / batch-32 active head` section and
marked the two High-tier Backlog leads dispatched (the BC-half of `fe-cohort-l4-lift`; the
concepts/dep-map refresh). No new candidates surfaced beyond what the c100 finalize already migrated.

## Open questions / caveats

- **D1 route decision is the dispatch's own warrant call.** I lean route (a) (the on-disk separable-
  post-composition law is a strong positive signal and the OQ flags (a) as the strong candidate), but
  the harvester must make and record the warrant — if the cohort reads as construction-stratum on
  closer inspection, route (b) (no-L4-by-design) is the honest outcome and is fully acceptable. Do
  NOT force a mirror.
- **One-verb-vs-two judgment (route (a)).** `eliminate_essential_bc` (operator→operator pin) and
  `eliminate_rhs` (RHS-lift) are two distinct verbs sharing the `DofSet[N]` input; the harvester
  judges whether they cap as one cohort chapter `L4/eliminate_bc.md` or two thin caps. Either is
  acceptable; state the warrant (the ≥2-consumer record obligation on `DofSet[N]` may tip toward a
  shared concept page regardless).
- **DofSet record-definition home** may need `concepts/dofset.md` if route (a) surfaces `DofSet[N]`
  in an L4 signature (3 consumers, no home today). Flagged as a judge-on-touch obligation in the D1
  scope; if not authored, the dispatch flags `record-DofSet-needs-definition-home`.
- **Graded-stack typed-edge annotation in D2** is opportunistic, not a full typing pass — the
  artifact-wide type-the-edges-and-audit campaign is meta-phase-owned standing duty. D2 annotates the
  re-derived dep-map edges where the `depends-on`/`reference` distinction is cleanly readable; it does
  NOT attempt the whole-artifact typing. Surfaced for the batch-32 meta-phase: the dep-map refresh is
  a good seam to consider folding the L1>L0 mutation-rotation edge-typing (the c100 watch-item
  `ksp-solve-firm-rests-on-apply-linop-per-step-reference-edge`) into when the typing campaign reaches
  that cohort.
- **Light frontier is intentional.** `promotion_frontier: 8` with remaining members
  obstruction/demand-gated; the redirect forbids manufacturing a rectangular filler dispatch. Two
  focused dispatches is the honest cycle shape (c088/c090/c092 precedent for a light-frontier
  small-dispatch cycle).
