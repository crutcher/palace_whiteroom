---
verifies: ../REPORT.md
critiqued_at: 2026-06-06T211500Z
critic_version: 1
checks:
  citation-validity: pass
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: pass
  cross-reference-integrity: pass
  edge-label-fidelity: pass
  plan-kind-consistency: pass
  skill-uptake-survey: pass
overall_status: ready
---

# META: verification of REPORT "L1 `fe_space_hierarchy` operator"

## Critique

### Checks run

**citation-validity — pass.** Ran `citecheck --scan` on the report: **31 ok, 0 failing**. I then hand-verified the load-bearing L0 pinpoints by direct on-disk read (NOT codemap `read_range`, per the brace-boundary-drift rule). `palace/fem/multigrid.hpp:78-126` reads exactly as the report describes: signature `:78-82`, `MFEM_VERIFY` `:84-86`, `coarse_mesh_l` `:87-88`, coarse-seed `make_unique<FiniteElementSpace>(*mesh[coarse_mesh_l], fecs[0].get())` `:89-90`, optional dbc block `:92-101` (`bdr_attr_max` `:95-97`, `mesh::AttrToMarker` `:98`, `GetEssentialTrueDofs` `:99-100`), h-loop `:104-112` with `AddLevel` at `:106`, p-loop `:115-123` with `AddLevel` at `:117`, `return fespaces` `:125`, close brace `}` `:126`. The record class `palace/fem/fespace.hpp:200-286` reads exactly as cited: `class FiniteElementSpaceHierarchy` `:200`, `fespaces` `:203`, `mutable ... P` `:204`, `BuildProlongationAtLevel` decl `:206`, seed ctor `:210-213`, `GetNumLevels` `:215`, `AddLevel` = `push_back` + `nullptr` slot `:217-221`, `GetFESpaceAtLevel` `:223-234`, `GetFinestFESpace` `:236-247`, `GetProlongationAtLevel`/`GetProlongationOperators` `:249-267`, discrete interpolators `:269-285`, close brace `:286`. `ConstructFECollections` `:22-73` (cited `:41-69` p-MG schedule loop, terminal `std::reverse` `:70`) confirmed. The two book-side law cites resolve in-range: `fe_space.md:149-154` is law 4 (coarse-seed identity), `fe_collection.md:173-177` is law 6 (singleton degeneracy). No drift found.

**surface-or-evidence — pass.** This is a NEW firm L1 operator chapter (not a refinement of existing surface), so the firm-on-positive-structure standard applies. The whole `ConstructFiniteElementSpaceHierarchy` body is read from one positive site (`multigrid.hpp:78-126`), and the five laws are genuine syntactic identities / fold-invariants on that read structure — verified against source: (1) coarse-seed base case = the literal `:89-90` seed; (2) AddLevel-fold structure = the two `AddLevel` loops `:104-112`/`:115-123`, and `AddLevel` is confirmed as `push_back` + `nullptr`-slot append at `fespace.hpp:217-221`; (3) coarse-to-fine level-monotonicity = the loop order over remaining meshes/collections (strict append, order-preserving — confirmed); (4) determinism = pure finite fold, no convergence/iteration semantics (the `coarse_mesh_l` formula is the read `:87-88` expression); (5) per-level essential-dof coherence = the re-applied `GetEssentialTrueDofs(dbc_marker, ...)` at `:99-100`/`:109-110`/`:120-121` (confirmed identical marker re-applied per level). None of these depends on convergence behavior, so the absent dedicated `test-multigrid.cpp` is correctly non-gating (the `fe_space`/`fe_collection`/`fe_assemble`/`apply_linop` precedent). The lazy prolongation `P[l]`/discrete-interpolator machinery is correctly carved out as read-as-given (it is a `mutable` lazily-built property of the record, `fespace.hpp:204,206,249-255` — confirmed `nullptr`-until-first-access), NOT a materialized constructive sub-part, so NOT-partly-constructive is the right call. **Record-definition sub-check:** the signature NAMES `FiniteElementSpaceHierarchy`, and the chapter carries an in-chapter `## Record definition` section (fields `fespaces`/`P` + types + meaning + construction-vs-run-time stratum + the L0 backing class `fespace.hpp:200-286`). Single-consumer justification is sound (no other harvested L1 signature names the type), so the in-chapter section is the correct home (not a concepts page); the `record-FiniteElementSpaceHierarchy-needs-definition-home` Open-questions flag tracks the ≥2-consumer trigger. Obligation satisfied.

**rotation-quality — pass.** This is an L1 operator chapter, not a lowering theme, but it does assert a structural rotation (imperative `push_back`/`AddLevel` vector-building → a pure `AddLevel`-fold combinator). The L1 form is strictly more abstract/equational than the L0 imperative loop: it hides the mutable-vector threading and re-expresses the construction as a fold whose base case is one `fe_space` call. This is a genuine combinator (composes two firm constituents, base case = a single `fe_space` construction), not a 1:1 rename — the imperative two-loop `push_back` structure is compressed into a single fold-with-two-refinement-axes abstraction. Rotation is real.

**variant-axis-coverage — pass.** The two orthogonal refinement axes (h-refinement over meshes at fixed coarsest collection `fecs[0]`; p-refinement over collections at fixed finest mesh `mesh.back()`) are both covered explicitly in law 2 and law 3, and shown to be independent + sequential — matching the source's two separate loops. The optional Dirichlet-BC axis (`dbc_attr`/`dbc_tdof_lists` present vs absent) is covered as the optional per-level essential-dof block (law 5, Config fields). No hidden branch: the `if (dbc_attr && dbc_tdof_lists)` guard at `:93`/`:107`/`:118` is the present/absent axis and it is named. The per-level `essential_dofs` application is scoped as a consumed relation, not folded in as a separate variant.

**cross-reference-integrity — pass (with one tracked sibling-cycle forward dep).** All same-cycle-existing links resolve on disk: `fe_space.md`, `fe_collection.md`, `essential_dofs.md`, `fe-space-intro.md`, `index.md` all present; the three `fe_space.md` re-anchor `[old]` blocks (§Context line 56, law 4 lines 149-154, deferred-sibling line 209) match the file exactly; the SUMMARY block (lines 221-224), `fe-space-intro.md` edges + three-members prose (lines 7-10, 15), and `index.md` lines 47/99/111 all match their `[old]` targets verbatim. The constituent maturity claims are accurate: `fe_space.md` and `fe_collection.md` both carry `## Status` = "**firm (firm-on-positive-structure).**" + `rank: firm` frontmatter on disk. The ONE forward dep is `[build_mesh](./build_mesh.md)` (the `Mesh` record home) — `book/src/L1/build_mesh.md` does NOT yet exist; it is D3 of this same cycle's wide wave. The report explicitly states (§Supporting evidence, §Mesh cross-link, Open questions) that the per-report integrator applies D3 before D4 so the live link resolves. This is the expected sibling-cycle ordering dependency, NOT a broken-link defect — flagged here as a telemetry note for the integrator's apply-order (D3 must land before this report) rather than a finding. If D3 does not land or is renamed off `L1/build_mesh`, the three `build_mesh.md` links in this chapter + the index dep-map row would be dangling; the integrator should confirm at apply-time.

**edge-label-fidelity — pass.** The `Mesh` edge is correctly typed `reference` (not `depends-on`): the prose (§Dependencies, frontmatter comment, §Mesh cross-link) is internally consistent that the fold consumes already-constructed meshes and does NOT invoke `build_mesh`, which is exactly the `reference` (navigational, element-type-home) vs `depends-on` (blocking, constrains rank) distinction. The two `depends-on` edges (`fe_space`, `fe_collection`, both `kind: composes`) correctly carry the blocking dependencies the fold actually invokes per-level. The per-level `essential_dofs` is correctly NOT an edge (consumed hierarchy-application). **Rank-invariant (graded-stack check 9):** this node is `firm` (rank 3); both `depends-on` targets are firm on disk (rank 3); `cites-evidence` is rank-terminal L0. `rank(u) ≤ min(deps)` holds at firm/firm — consistent. **Reachability (check 10):** the node is reachable — it is pulled by the firm FE-space sub-spine and named by the `fe_space`/`fe_collection` deferred-sibling lists; the edges land it in the live FE-space sub-spine grouping.

**plan-kind-consistency — pass.** Declared kind is a firm L1 operator with the `firm (firm-on-positive-structure)` qualifier; the content matches — a full Signature, Record definition, Config fields, five stated laws on positive source, Dependencies, Downward, Status, Evidence. No rough-in placeholders, no TODO stubs in a firm-claimed body. The promotion of the standing `fe_space_hierarchy` *(rough-in; no anchor yet)* index bullet (line 111) to firm is consistent with the new chapter's verdict.

**skill-uptake-survey — pass (telemetry only).** The report's shape (firm-on-positive-structure promotion, record-definition obligation, count-owner consolidation, named-shape-group judgment) implies several procedures. The report demonstrates uptake in prose: it self-verified citations on-disk citing the codemap +1-brace-drift rule, applied the record-definition single-vs-≥2-consumer bar, and applied the named-shape-group KEEP-`[·]`/`[N]` judgment with a link to `book/src/semantics/index.md` §1.2.1. No skill is named by invocation, but this is a pure presence check and the relevant procedural disciplines are visibly exercised. Non-blocking.

### Issues found

No blocking issues. Two minor observations (neither a check failure, neither alters a verdict):

1. **(cosmetic, integrator-routing)** Proposed-changes section 3 carries the heading "`book/src/L1/SUMMARY.md`" but the fence label is `edit:book/src/SUMMARY.md`. The fence path (`book/src/SUMMARY.md`) is the CORRECT one — that is the single mdBook SUMMARY and the `[old]` block matches it exactly (lines 221-224). The heading prose is just mislabeled. The integrator parses the fence, so this is harmless; noting only so it is not mistaken for two distinct files. Location: CYCLE.md §"3. `book/src/L1/SUMMARY.md`".

2. **(apply-order dependency, NOT a defect)** This report's `[build_mesh](./build_mesh.md)` links (chapter body + index dep-map row + the `Mesh` reference edge) depend on D3 (`build_mesh`) landing `book/src/L1/build_mesh.md` at the canonical slug FIRST. The report states this dependency explicitly and correctly. The integrator must apply D3 before this report (D4); if D3 lands non-firm or at a different slug, the integrator must reconcile (the report's count-owner note at 5d already anticipates the grand-total reconciliation against D3's + D5's on-disk `## Status`). Surfaced as telemetry for the per-report integrator's apply-order, not a content fix. Location: CYCLE.md §Supporting evidence + §Open questions ("Count-owner reconciliation").
