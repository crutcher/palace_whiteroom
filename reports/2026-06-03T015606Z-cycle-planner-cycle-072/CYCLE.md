---
agent: cycle-planner
invoked_at: 2026-06-03T015606Z
scope: cycle-072 dispatch plan
status: pending
---

# Cycle 072 dispatch plan

## Goals selected this cycle

Cycle-072 is the **FEATURE-SURFACE SPINE SCALING cycle** and the THIRD/FINAL primary cycle of meta-batch-22 (the batch-22 meta-phase fires AFTER this cycle's finalize). The FEATURE-SURFACE SPINE directive (user 2026-06-02) says run the spine in PARALLEL by fan-out; cycle-070 set the exemplar (electrostatic at L4+L1+L0, the new `# Feature surfaces — entry points` Part), and cycle-071 settled the alpha-grouped SUMMARY base into which new feature columns now insert clean. The two natural next picks are both codemap-confirmed ready: the **magnetostatic feature column** (the lower-risk 2nd exemplar — mirrors electrostatic's fixed-operator pattern, inductance output, composes the SAME firm constituent set) and the **top-level lifecycle root** (`main`→`BaseSolver`, the spine ROOT all 5 drivers hang off — higher fan-out).

**Decision: TWO feature columns this cycle** (magnetostatic LEAD + lifecycle root), plus two cheap LOW hygiene fixes that keep the batch clean for the meta-phase. Rationale for two: (a) both are codemap-verified ready (the c071 pre-stage + my re-confirmation below); (b) the two columns are **fully non-overlapping** (distinct `feature/<name>.<level>.md` files; the only shared files are `feature/index.md` + the `# Feature surfaces` SUMMARY block, partitioned by single count-owner per the ≥2-parallel-landing rule); (c) exercising the feature-surface kind a SECOND and THIRD time before the batch-22 meta-phase codifies it gives the meta-phase a richer evidence base for the adapted-critic-check codification (a fixed-operator sibling-exemplar AND a structurally-different lifecycle-root meta-feature stress two distinct shapes of the kind); (d) the lifecycle root is the highest-fan-out feature item (the composition ROOT all 5 driver columns will hang off), so deferring it costs the most. The meta-phase's need for a clean batch-end state is satisfied — both columns are `seed (exemplar)` new-file authoring with no count-table churn and no forward-frontier-spine disturbance.

Cheap parallel hygiene folded in: the `concepts/index.md` 2-missing-row reconciliation (OQ `concepts-index-table-vs-summary-membership-drift-two-missing-rows`, c071 D6) — closing it pre-meta keeps the batch clean.

## Deliverable-presence verification

Per the MANDATORY paste-inline-evidence deliverable-presence procedure (every named-artifact-slug scope). All feature-column slugs are NEW authoring (must be ABSENT); the hygiene fix targets an EXISTING table missing 2 rows (must be present-and-incomplete).

### D1 — `feature/magnetostatic.{L4,L1,L0}.md` (NEW authoring; must be ABSENT)

- **File existence (target ABSENT):**
  ```
  $ ls book/src/feature/magnetostatic.L4.md book/src/feature/magnetostatic.L1.md book/src/feature/magnetostatic.L0.md
  ls: cannot access 'book/src/feature/magnetostatic.L4.md': No such file or directory
  ls: cannot access 'book/src/feature/magnetostatic.L1.md': No such file or directory
  ls: cannot access 'book/src/feature/magnetostatic.L0.md': No such file or directory
  ```
  → all three ABSENT. **Open by construction (new feature column; no prior-cycle history).** PASS.
- **Constituent vocabulary ON DISK (the composed firm pieces must exist):**
  ```
  $ for f in L4/fe_assemble L4/solve_family L4/ksp_solve L1/matrix-weighted-norm L1/bilinear-form L1/fe_assemble L1/ksp_solve; do ls book/src/$f.md; done
  book/src/L4/fe_assemble.md          (## Status present)
  book/src/L4/solve_family.md         (## Status present)
  book/src/L4/ksp_solve.md            (## Status present)
  book/src/L1/matrix-weighted-norm.md (## Status present)
  book/src/L1/bilinear-form.md        (## Status present)
  book/src/L1/fe_assemble.md          (## Status present)
  book/src/L1/ksp_solve.md            (## Status present)
  ```
  → all 7 constituent chapters on disk. The magnetostatic specialization is ALREADY cited inside `book/src/L4/solve_family.md:109` (§Specializations "Magnetostatic surface-current sweep", exact anchors). PASS (composes firm/rough-in on-disk vocabulary).
- **OQ-ledger RESOLVED-grep:** `grep 'magnetostatic.*RESOLVED\|magnetostatic.*CLOSED' scaffolding/open-questions.md` → no matches (no prior magnetostatic-feature closure). PASS.
- **Structural-block check:** none. `magnetostatic` is NOT on the STOP-PROPOSING NEGATIVE LIST (only `map_solve` SHARED-generalized is barred — not proposed here; the magnetostatic column composes the FIXED-operator `solve_family`, witness 2, NOT `map_solve`). No methodology gate blocks a feature-surface composition root. PASS.

### D2 — `feature/lifecycle.{L4,L1,L0}.md` (NEW authoring; must be ABSENT)

- **File existence (target ABSENT):**
  ```
  $ ls book/src/feature/lifecycle.L4.md book/src/feature/lifecycle.L1.md book/src/feature/lifecycle.L0.md book/src/feature/main.L0.md book/src/feature/basesolver.L0.md
  ls: cannot access '.../lifecycle.L4.md': No such file or directory
  ls: cannot access '.../lifecycle.L1.md': No such file or directory
  ls: cannot access '.../lifecycle.L0.md': No such file or directory
  ls: cannot access '.../main.L0.md': No such file or directory
  ls: cannot access '.../basesolver.L0.md': No such file or directory
  ```
  → all candidate slugs ABSENT. **Open by construction (new feature column; no prior-cycle history).** PASS.
- **Source anchors codemap-confirmed (this dispatch):** `BaseSolver::SolveEstimateMarkRefine` at `palace/drivers/basesolver.cpp:153-276` (codemap `get_symbol_def`); `int main(...)` opens at `palace/main.cpp:158` (codemap `read_range :150-200` confirms the `int main(int argc, char *argv[])` signature at `:158`). PASS (anchors verified, not memory-cited).
- **OQ-ledger RESOLVED-grep:** `grep 'lifecycle.*RESOLVED\|basesolver.*RESOLVED' scaffolding/open-questions.md` → no matches. PASS.
- **Structural-block check:** none. `lifecycle` is NOT on the STOP-PROPOSING NEGATIVE LIST. The lifecycle root is a composition-root meta-feature (the AMR outer loop + the per-solver dispatch), distinct in shape from a single-driver column — exactly the directive's named "top-level lifecycle (main→BaseSolver)" scope item. PASS.

### D3 — `concepts/index.md` 2-missing-row reconciliation (EXISTING table; must be present-and-incomplete)

- **Drift confirmed present:**
  ```
  $ grep -c "nested-constructed-operator-gate\|black-box-vs-accelerated-kernels" book/src/concepts/index.md
  0
  $ grep -n "nested-constructed-operator-gate\|black-box-vs-accelerated" book/src/SUMMARY.md
  261:  - [black-box vs accelerated kernels — methodology concept](./concepts/black-box-vs-accelerated-kernels.md)
  285:  - [nested-constructed-operator-gate](./concepts/nested-constructed-operator-gate.md)
  ```
  → both slugs in SUMMARY + on disk but ABSENT from the `concepts/index.md` API table (0 occurrences). The table carries 42 rows; SUMMARY carries 44 → the 2 genuine omissions. **Deliverable (add 2 alpha-positioned rows) genuinely OPEN.** PASS.
- **OQ-ledger RESOLVED-grep:** `grep 'concepts-index-table-vs-summary.*RESOLVED' scaffolding/open-questions.md` → no matches; the OQ `concepts-index-table-vs-summary-membership-drift-two-missing-rows` is `status: Open` (opened c071, routed to "batch-22 meta-phase OR a cycle-072 hygiene dispatch"). PASS (I am taking the c072-hygiene-dispatch arm).
- **Structural-block check:** none — pure derived-surface reconciliation. PASS.

**STOP-PROPOSING NEGATIVE LIST cross-check (all dispatches):** none of D1/D2/D3 proposes any listed slug (`lu_solve`/`back_solve`/`ls-update-column`/4 NLEPS atoms/`apply_nonlinear_pencil`-HELD/`polynomial_smoother`/`L3/solve_family`/`L2/fold_solve`/`L2/fe_assemble`/`weak_form_term`-L2-floor/`map_solve`-SHARED-generalized). The magnetostatic column composes the FIXED-operator `solve_family` (witness 2) — explicitly the reconciled-STOP-permitted form (a single feature's own use of a firm combinator, NOT a shared-generalized `map_solve` mining). PASS (negative-list-clear).

## Dispatches

- **D1 — `layer-intro-author`** (the directive-designated composition-root feature-chapter author; matches the c070 D3 electrostatic-exemplar precedent)
  - **scope:** Author the **magnetostatic feature column** — the 2nd exemplar of the feature-surface kind — as three composition-root chapters matching the established convention `book/src/feature/<name>.{L4,L1,L0}.md` (high→low within-column level ordering; same frontmatter shape `kind: feature-surface` / `feature: magnetostatic` / `level:` / `composes:` / `l0_ground_truth:` as `book/src/feature/electrostatic.L4.md`):
    - `book/src/feature/magnetostatic.L4.md` — the L4 composition root: config → inductance matrix, body = the composition `inductance_reduce ∘ solve_family ∘ fe_assemble` (the SAME fixed-operator shape as electrostatic, differing only in: curl-curl stiffness `K = curlcurl_op.GetStiffnessMatrix()`; the family = surface-current-boundary index set `GetSurfaceCurrentOp()`; the output = Maxwell INDUCTANCE matrix `Mᵢⱼ = (Aⱼᵀ K Aᵢ)/(IᵢIⱼ)` with the `/Iᵢ²` + `/(IᵢIⱼ)` current-scaling + the `Mm` sign-remix). Links DOWN to firm `L4/fe_assemble`, the FIXED-operator corner of `L4/solve_family` (explicitly the magnetostatic witness 2, named at `book/src/L4/solve_family.md:109`), `L4/ksp_solve`. The inductance reduction (like electrostatic's capacitance reduction) has NO dedicated L4 reduction combinator — a fold of bilinear-form evaluations (forward mine, NOT authored here; the cross-pipeline post-process shared-shape question is a noted Open question, NOT a forced combinator).
    - `book/src/feature/magnetostatic.L1.md` — the L1 pure-function composition root (same feature in L1 vocabulary: `fe_assemble` + per-source-boundary `ksp_solve` + the inductance reduction built from rough-in `L1/matrix-weighted-norm` [diagonal `Aᵢᵀ K Aᵢ`] + rough-in `L1/bilinear-form` [off-diagonal `Aⱼᵀ K Aᵢ`]). Mirror the electrostatic.L1 §"L1 vs L4" + §"Constituent down-links" structure.
    - `book/src/feature/magnetostatic.L0.md` — the L0 ground-truth surface: cited driver source, per-stage site map. **Codemap-confirmed anchors (on-disk-verify at authoring per the source-of-truth + close-brace ±1 discipline):** `MagnetostaticSolver::Solve` at `palace/drivers/magnetostaticsolver.cpp:22-108`; `K = curlcurl_op.GetStiffnessMatrix()` assembled once `:30`; `KspSolver ksp(...)` `:35` + `ksp.SetOperators(*K,*K)` `:36` (OUTSIDE the loop — the fixed-operator capture); `std::vector<Vector> A(n_step)` solution family `:47`; the surface-current family loop `GetSurfaceCurrentOp()` `:66`; `GetExcitationVector(idx, RHS)` `:76` + `ksp.Mult(RHS, A[step])` `:77`; `step++` `:99`; `PostprocessTerminals` `:110-204` (the inductance reduction: `M_mag->Mult(A_gf, H_gf)` + `linalg::Dot` B-weighted gram `:122-150`, the `/Iᵢ²`+`/(IᵢIⱼ)` current-scaling, LAPACK inverse `Minv.Invert()` `:151-152`). These ranges were just confirmed via codemap `get_symbol_def`/`read_range`; the `:108`/`:204`/`:150` END lines are codemap hints — the producer on-disk-confirms (close-brace ±1 drift bound).
  - **Feature-surface kind prompt framing (carry into the dispatch — the kind is still prompt-carried until the batch-22 meta-phase codifies it):** a feature chapter is a NEW composition-root chapter kind, NOT a decomposed operator. inputs = config (the `iodata`/problem definition); outputs = the physical product (the inductance matrix); body = composition of the already-firm decomposed vocabulary at that level (high→low per-level coherence: L4 chapter composes L4 combinators, L1 composes L1 ops, L0 is cited driver source); links DOWN to every composed piece (live links to firm chapters); the chapter carries the COMPOSITIONAL claim (this feature = this composition of these firm pieces), NOT the per-op algebraic claims (those live in the linked chapters). **Adapted-critic-check note (carry into the dispatch + flag for the critic):** the surface-or-evidence check is ADAPTED for this kind — a feature chapter's evidence is the L0 driver-source range + the constituent-op down-links, NOT a single decomposed op's source site; the rotation-quality + variant-axis checks NO-OP on a composition root (it introduces no new operator/rotation). Use `book/src/feature/electrostatic.{L4,L1,L0}.md` + `book/src/feature/index.md` as the pattern template verbatim.
  - **`feature/index.md` registration:** add the magnetostatic row to the §"Feature × level matrix" table (alpha-by-feature: `electrostatic` then `magnetostatic`); update the "Planned (not yet authored)" line to remove magnetostatic. **D1 OWNS the `feature/index.md` matrix-table edit + the `# Feature surfaces` SUMMARY block edit for BOTH columns this cycle** (the single count/registration owner — see Notes for the count-ownership partition); D1 inserts BOTH its own magnetostatic SUMMARY entries (3 chapters, after the electrostatic block, before any later-alpha entry) AND D2's lifecycle SUMMARY entries are deferred TO D1 (D2 emits only its 3 chapter files + its `feature/index.md` row content as plain proposed-text for D1 to place — see overlap analysis).
  - **deps:** none (wave-1).
  - **rationale:** active-head #6 `feature-surface-spine-seed` scaling (the FEATURE-SURFACE SPINE directive, run-in-parallel); the lower-risk 2nd exemplar (mirrors the electrostatic fixed-operator pattern, all constituents firm/on-disk, the magnetostatic sweep already cited in `solve_family.md:109`). Exercises the kind a 2nd time before the batch-22 meta-phase codifies it (per the c071 integrator-signals suggested-next). fan-out MEDIUM-HIGH.

- **D2 — `layer-intro-author`**
  - **scope:** Author the **top-level lifecycle root feature column** — the spine ROOT all 5 driver columns hang off — as `book/src/feature/lifecycle.{L4,L1,L0}.md` (canonical slug `lifecycle`; same composition-root convention + frontmatter shape as the electrostatic exemplar). This is the directive's "top-level lifecycle (`main`→`BaseSolver`)" scope meta-feature: the composition root presenting Palace's WHOLE run as a feature — config-file → solver-dispatch → AMR adaptive loop → output products. The composition body:
    - `book/src/feature/lifecycle.L4.md` — the L4 composition root: config → simulation outputs, body = the top-level wiring (parse config → construct the chosen per-solver feature → the adaptive Solve-Estimate-Mark-Refine outer loop → emit outputs). At L4 the per-solver step is a link to the per-driver feature column (electrostatic / magnetostatic / … — the 2 authored so far live-linked, the other 3 plain-text forward-refs per the missing-anchor convention until they land). The AMR outer loop is a `fold`/`iterate_while`-family sweep (mesh-state-threaded refinement; cite the strawman §3.7 `iterate-while` family — do NOT mint a new combinator; if the loop is a genuine state-threaded fold it is the lifecycle's own composition note, NOT a new spine primitive). **WARRANT-FIRST:** the lifecycle root is a composition root over the 5 driver features + the AMR loop; if a stage cannot yet be cleanly composed (e.g. the 3 un-authored driver columns), that is recorded as a plain-text forward-ref + an Open-question spine finding, NOT a forced claim.
    - `book/src/feature/lifecycle.L1.md` — the L1 pure-function composition root (the lifecycle as a pure `Config -> Outputs` function: parse → dispatch-to-solver → AMR-fold → outputs).
    - `book/src/feature/lifecycle.L0.md` — the L0 ground-truth surface. **Codemap-confirmed anchors (on-disk-verify at authoring):** `int main(int argc, char *argv[])` at `palace/main.cpp:158` (the entry point — arg parse, MPI init, config load, solver dispatch); `BaseSolver::SolveEstimateMarkRefine` at `palace/drivers/basesolver.cpp:153-276` (the AMR adaptive outer loop — the Solve→Estimate→Mark→Refine sweep all 5 drivers run inside). The producer localizes the `main`→`BaseSolver::Solve` dispatch chain + the per-solver-type selection (the switch/dispatch on problem type that picks which driver's `Solve` runs) — pre-localized START anchors `main.cpp:158` + `basesolver.cpp:153`; the producer reads outward from these (END lines `:276` is a codemap hint, on-disk-confirm the close-brace ±1).
  - **Feature-surface kind prompt framing:** identical to D1's (composition-root kind, inputs=config, outputs=physical products, body=composition, links DOWN, adapted-critic-check note). The lifecycle root differs from a single-driver column in that it composes the DRIVER FEATURES (the per-solver columns) + the AMR loop, not the BLAS/assemble vocabulary directly — frame it as the root of the feature spine (the meta-feature the 5 driver columns specialize).
  - **SUMMARY + `feature/index.md` registration is DEFERRED to D1** (the single registration/count owner this cycle): D2 authors ONLY its 3 `feature/lifecycle.*.md` chapter files + emits its `feature/index.md` matrix row + its 3 SUMMARY chapter-line entries as plain proposed-text in its report for D1 (or the per-report integrator) to place. D2 does NOT edit `feature/index.md` or `SUMMARY.md` directly (avoids the 2-parallel-writers-into-one-derived-surface drift). **Forward-reference coordination:** D2's lifecycle.L4 references the magnetostatic driver column D1 authors this cycle — the canonical slugs are `book/src/feature/magnetostatic.L4.md` / `.L1.md` / `.L0.md` (D1 authors them; D2 live-links them by these exact slugs, no guessing) and `book/src/feature/electrostatic.L4.md` (already on disk). The 3 un-authored driver columns (eigenmode/driven/transient) are plain-text forward-refs (no live link — the rough-in-forward-reference-must-be-plain-text convention).
  - **deps:** none for AUTHORING (wave-1, parallel with D1); the SUMMARY/index registration of D2's rows is applied by the per-report integrator AFTER D1's registration edit lands (D1 owns the shared derived surfaces) — this is a serial-application ordering at integration, NOT a dispatch-wave dependency (both produce in wave-1; see Sequencing).
  - **rationale:** active-head #6 scaling, the HIGHEST-fan-out feature item (the composition ROOT all 5 driver columns + the backend-lowering target hang off — establishing it now lets every future driver column slot into a live root). fan-out HIGH. Pairing it with D1 in the same cycle gives the batch-22 meta-phase two structurally-distinct exemplars of the feature-surface kind (a fixed-operator driver column + a root meta-feature) for the adapted-check codification.

- **D3 — `layer-intro-author`** (hygiene; cheap, isolated)
  - **scope:** Reconcile the `book/src/concepts/index.md` API table against the on-disk `concepts/*.md` set + the `# Concepts` SUMMARY block (OQ `concepts-index-table-vs-summary-membership-drift-two-missing-rows`, c071 D6). Add the **2 missing rows** in their correct alpha-by-slug positions with `Kind`/description cells: `black-box-vs-accelerated-kernels` (methodology concept; the case-1/2/3 black-box-vs-accelerated-kernel disposition page) and `nested-constructed-operator-gate` (the constructed-operator-gate concept — divfree/eigsolve/floquet instances). After the add, confirm 1:1 (44 SUMMARY content entries ⟷ 44 index table rows). Pure derived-surface reconciliation — no concept-page content authored, no claims added. Cite the on-disk file set + SUMMARY links as the source of truth (the hand-maintained index table drifted from them).
  - **deps:** none (wave-1; touches ONLY `book/src/concepts/index.md`, disjoint from D1/D2's `feature/` + Feature-SUMMARY-block edits).
  - **rationale:** LOW fan-out hygiene; closes a c071 D6 OQ pre-meta, keeping the batch clean for the batch-22 meta-phase. Routed to the "cycle-072 hygiene dispatch" arm the OQ explicitly offers (the alternative was the meta-phase itself).

## Overlap analysis

Pairwise across D1 / D2 / D3:

- **D1 ↔ D2 (the two feature columns):** Each authors its OWN three `feature/<name>.{L4,L1,L0}.md` files — **distinct new files, zero name collision** (magnetostatic.* vs lifecycle.*). The only genuinely shared surfaces are **`book/src/feature/index.md`** (the §"Feature × level matrix" table + "Planned" line) and **the `# Feature surfaces — entry points` `SUMMARY.md` block** (3 new chapter lines per column). These are **consolidated derived surfaces with ≥2 parallel landings** → per the count-ownership rule, **assign a single owner: D1 owns BOTH the `feature/index.md` matrix-table edit AND the `# Feature surfaces` SUMMARY-block edit for BOTH columns this cycle.** D2 emits its `feature/index.md` row + its 3 SUMMARY chapter-lines as PLAIN PROPOSED-TEXT in its report; D1 (or the per-report integrator applying D1 first) places them. With that partition, **D1 ↔ D2 author-phase is NON-OVERLAPPING → PARALLEL** (distinct chapter files; the shared derived surfaces are single-owner). The within-column high→low level ordering (`.L4` → `.L1` → `.L0`) is NOT alphabetized (the directive-3 alpha-insert applies to the chapter LIST positions across kind groupings, NOT the within-column level sequence — D6 verified this stays high→low; the new columns follow the same convention).
  - **Forward-reference slug coordination (stated in BOTH scopes per the cross-report-forward-reference-slug-divergence convention):** D2's `lifecycle.L4`/`.L1` live-links D1's magnetostatic column by the canonical slugs `book/src/feature/magnetostatic.{L4,L1,L0}.md` (D1 authors them this cycle) and the on-disk `book/src/feature/electrostatic.{L4,L1,L0}.md`. Both scopes name these exact slugs so neither producer invents a divergent working slug. The eigenmode/driven/transient columns (not authored this cycle) are plain-text forward-refs in D2 (no live link). → coordinated; no dead-link, no slug divergence.
- **D1 ↔ D3:** D1 touches `feature/*.md` + `feature/index.md` + the Feature SUMMARY block; D3 touches ONLY `book/src/concepts/index.md` (+ no SUMMARY edit — the Concepts SUMMARY block is the source D3 reads, not edits). **Fully disjoint → PARALLEL.**
- **D2 ↔ D3:** D2 touches `feature/lifecycle.*.md` (+ defers shared-surface edits to D1); D3 touches `concepts/index.md`. **Fully disjoint → PARALLEL.**
- **No operator/theme BODY is rewritten and no operator ENTRY is modified** by any dispatch (D1/D2 author NEW composition-root chapters that only LINK to existing firm ops; D3 reconciles a derived table). The "two dispatches rewrite the same theme body / modify the same operator entry" sequential trigger does NOT fire for any pair.
- **No layer-index consolidated running-count is touched** (the feature columns are `seed (exemplar)` and do NOT register into any L_n firm/partial-obstruction tally; `feature/index.md` carries a matrix-table, not a running count, and is single-owned by D1).

**Conclusion: D1, D2, D3 are all PARALLEL (one wave).** The only shared derived surfaces (`feature/index.md` + the Feature SUMMARY block) are single-owned by D1 with D2 deferring its rows — the canonical ≥2-parallel-landing-into-one-derived-surface partition. Per the conflict-tolerance philosophy, the partition removes the only genuine same-surface contention; the serial per-report integrator applies D1's registration before placing D2's deferred rows.

## Sequencing schedule

- **Wave 1 (all parallel): D1, D2, D3.** All three produce simultaneously (no dispatch-phase forward-reference blocks authoring — D2 live-links D1's magnetostatic slugs by their PRE-AGREED canonical names, which it can write without D1's file existing yet, since the per-report integrator wires the live link at apply-time / the linkcheck runs at finalize). The serial per-report integrator applies in the order **D1 → D2 → D3** so that D1's `feature/index.md` + Feature-SUMMARY registration lands BEFORE D2's deferred rows are placed (D1 is the registration owner) — this is an integration-apply ordering, NOT a dispatch-wave-2. D3 is order-independent (disjoint file). ONE `integrator-finalize` at cycle-end rebuilds the book (`cargo make book` + `linkcheck2` validates the new feature chapters render + all DOWN-links + the D2→D1 cross-column links resolve) + commits.

Single dispatch wave; the D1→D2 integration-apply ordering is a per-report serialization note, not a second wave.

## Notes for the integrator / count-ownership

- **Registration / count-ownership partition (≥2 parallel landings into shared derived surfaces):** **D1 is the SOLE owner** of (a) the `book/src/feature/index.md` §"Feature × level matrix" table (adds BOTH the magnetostatic row AND places D2's lifecycle row from D2's plain proposed-text) and (b) the `# Feature surfaces — entry points` `SUMMARY.md` block (inserts BOTH columns' chapter lines: magnetostatic's 3 + lifecycle's 3, each as a within-column high→low L4→L1→L0 triple, placed after the electrostatic block in feature-alpha order: electrostatic, lifecycle, magnetostatic — OR the author's judged reader-order; the directive-3 alpha applies to the chapter-list positions, the within-column level order stays high→low). **D2 authors ONLY its 3 `feature/lifecycle.*.md` files + emits its index-row + SUMMARY-lines as plain proposed-text — it does NOT edit `feature/index.md` or `SUMMARY.md`.** State this partition is honored: D2's report's proposed-changes for the shared surfaces are plain-text-for-D1-to-place, not direct edits. This is the feature-Part analog of the layer-index count-owner rule.
- **No L_n layer-index count delta this cycle.** The feature columns are a separate Part (`# Feature surfaces`) carrying `seed (exemplar)` chapters; they register NO new operator/theme into any L4/L3/L2/L1/L0 firm tally. The vocabulary-spine counts are untouched (the meta-phase reads a clean count-state).
- **Each feature chapter must carry the adapted-kind frontmatter** (`kind: feature-surface`, `feature:`, `level:`, `composes:`, `l0_ground_truth:`/`lifts_to:` per the electrostatic exemplar) and the `status: seed (exemplar)` line — so the critic's adapted surface-or-evidence check (evidence = L0 driver range + constituent down-links) recognizes the kind and NO-OPs the rotation-quality/variant-axis checks.
- **`linkcheck2` is the safety net** for the D2→D1 cross-column live-links (lifecycle → magnetostatic/electrostatic) + every DOWN-link to firm constituents; the finalize build catches any slug-divergence or dead-link. The 3 un-authored driver columns (eigenmode/driven/transient) in D2 are plain-text (no live link) — no dead-link from those.

## Open questions / caveats

- **One-column-vs-two decision (recorded): TWO columns** (magnetostatic LEAD + lifecycle root) + 1 hygiene fix. I judged the fan-out warrants both: both codemap-verified ready, fully non-overlapping under the D1-owns-registration partition, and authoring two structurally-distinct exemplars (a fixed-operator driver column + a root meta-feature) gives the batch-22 meta-phase a richer evidence base for the adapted-critic-check + path-layout codifications it owns. The batch-end state stays clean (both are `seed (exemplar)` new-file authoring, zero vocabulary-spine count churn, no forward-frontier disturbance). If the orchestrator judges the meta-phase needs a thinner batch-end, D2 (lifecycle root) is the droppable one (keep D1 magnetostatic as the lower-risk 2nd exemplar) — but I recommend both.
- **Batch-22 meta-phase items left OPEN (correctly — these are the meta-phase's to codify after this cycle's finalize):** `feature-surface-kind-adapted-check-codification` (OQ; codify the critic surface-or-evidence ADAPT + rotation/variant NO-OP for the kind + add "feature surfaces / entry points" to the directive-3 kind list); `feature-surface-part-path-layout-and-within-column-level-ordering-ratification` (OQ; ratify the `feature/<feature>.<level>.md` path + high→low within-column ordering). I deliberately did NOT codify these — this cycle carries the kind via per-dispatch prompt framing (D1/D2 scopes), exactly as c070 D3 did, and authoring two more columns gives the meta-phase MORE evidence to codify against. Also left for the meta-phase: directive-3 ROLE-SPEC codification, FEATURE-SURFACE SPINE role-spec codification (carried from c070 ⚠(iii)), the goal-flow refresh.
- **`L4-L3/index.md` `integrator.hpp:58-61` bare-basename path-hygiene lint — NOT dispatched this cycle, routed to the meta-phase.** The c071-noted lint (`integrator.hpp:58-61` cited without the `palace/fem/` directory prefix, at `L4-L3/index.md:15`) is a LOW citecheck-hygiene fix, but NO dispatch this cycle touches `L4-L3/index.md` (the feature columns + concepts-table reconciliation are all disjoint from it), so spinning a dedicated agent for one bare-basename fix is not warranted. I route it to the batch-22 meta-phase's intake-migration pass (or a future cycle that already opens `L4-L3/index.md`) rather than forcing a one-line standalone dispatch — consistent with the "fold in IF a dispatch is already in that file" framing in the cycle brief (no such dispatch exists this cycle).
- **No escalating actionable friction this cycle.** The two `escalating`-status friction-ledger patterns are both old/superseded telemetry-noise entries (the `skill-uptake-survey` broad-telemetry no-go pattern, last_observed cycle-166/cycle-021, both no-go-confirmed across batches) — not actionable, no plan candidate warranted (consistent with the c071 planner's read). The `codemap-read-range-plus-one-drift-on-brace-boundary` pattern (recurrence-6, held) IS relevant this cycle because D1/D2 author NEW L0 citations — I pre-localized the codemap-confirmed START+END anchors into the D1/D2 scopes and flagged the close-brace END lines (`magnetostaticsolver.cpp:108`/`:204`/`:150`, `basesolver.cpp:276`) as codemap HINTS the producer on-disk-confirms (the drift bound), per the discipline.
- **Magnetostatic constituent rough-in honesty:** the inductance reduction (like electrostatic's capacitance reduction) composes the rough-in `L1/matrix-weighted-norm` + rough-in `L1/bilinear-form` — so the magnetostatic column is `seed (exemplar)` with the same rough-in-reduction caveat the electrostatic exemplar carries (the stage-3 reduction rests on rough-in primitives; this is consistent with `seed`, not a firm composition). D1's scope flags this so the chapter's §Status states it honestly (mirroring `electrostatic.L1.md:65`). The cross-pipeline post-process shared-shape question (whether the capacitance + inductance reductions share a dedicated reduction combinator) stays a NOTED Open question / forward mine — NOT a forced combinator this cycle (the c055/c056 `gram-consuming-solver-postprocess-reduction` was assessed genuinely solver-specific; re-open only on a 3rd structurally-matching witness).
