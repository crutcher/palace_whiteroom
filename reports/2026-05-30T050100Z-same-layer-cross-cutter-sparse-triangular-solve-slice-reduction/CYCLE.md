---
agent: same-layer-cross-cutter
invoked_at: 2026-05-30T050100Z
scope: Phase-1 slice-reduction candidacy — book/src/spec/slices/sparse_triangular_solve.md (carry-forward from c029 now that the L1>L0 obstruction theme is on disk)
status: pending
integrated_at: 2026-05-30T051734Z
integration_commit: PLACEHOLDER_SHA
integration_notes: Applied clean (cycle-031 D5 same-layer-cross-cutter). Verdict DEFER — slice retained-by-design as canonical-instance for 3 concept pages (concepts/sparse-triangular-solve.md / tracts-of-source-vs-targets-of-burn.md / negative-result.md). Single reciprocal cross-link added to slice :3-7 reduction-status header pointing at c029 L1>L0 obstruction theme :273-308. Phase-1 removals stay 9/10 unchanged. Meta-phase batch-9 candidate filed: negative-result-slice-canonical-instance-blocks-reduction.
---

# CYCLE: Phase-1 — sparse_triangular_solve slice-reduction candidacy

## Summary

The cycle-029 L1>L0 obstruction theme `book/src/L1-L0/triangular-solve-obstruction.md` is on disk and is the first `obstruction (opaque-library-ownership)` theme, supplying the citable layered home the cycle-013+ corpus-reduction policy normally requires before a Phase-1 slice can be reduced. **Despite that, the `sparse_triangular_solve` slice should NOT be reduced or removed.** The slice is already classified by its own reduction-status header (cycle-013+) as an **annotated-and-retained negative-result slice** — a "the slice IS the artifact" case per the `polynomial_recurrence_step` precedent — and the c029 obstruction theme itself explicitly affirms that classification in its `## Related` and `## Verified-against` sections: "the two records cross-link rather than supersede; the slice's reduction status remains annotated-and-retained." Moreover, the slice is a **named load-bearing referent** of three concept pages — a §"Canonical instance" entry on `scope-out-obstruction.md:68`, a §"Sub-kind: out-of-scope-obstruction" canonical-instance entry on `sequential-obstruction.md:53`, and a §"Examples in this spec" worked-example entry on `negative-result-slice.md:47`; reducing or removing it would orphan all three concept-page citations. The slice also carries unique L0 navigation (Mult/Mult2 interface analysis, MakeWrapperSolver install-as-preconditioner pattern, MPI Allgatherv whole-tree usage, NOREFINE / SamePattern_SameRowPerm factor-reuse contract, small-dense near-relatives forward-pointer) absorbed into firm artifact homes other than the obstruction theme — but the slice is the *only* place where those L0 sites are gathered under the **scope-out-obstruction** narrative that motivates the negative result. The Phase-1 corpus tally stays at 9/10; this slice is the **10th, retained by design**.

## Observation kind

**Variant-axis coverage gap** — the gap is not in the artifact but in the **cycle-013+ corpus-reduction policy's audit shape itself**: the policy's "is there a firm layered home for this slice's material?" check is necessary but not sufficient. A second policy axis — "is the slice a **named canonical instance** in a concept page §Canonical-instance line?" — is load-bearing for negative-result slices and absent from the audit checklist. Recording this as a cross-cutter observation so the methodology question lands in the meta-phase ledger, not as a one-off note inside this slice's audit.

## Specific finding

### Boundary-verification (skill `phase-1-slice-reduction-audit`)

**START of slice** (`book/src/spec/slices/sparse_triangular_solve.md:1-19`):

- Lines 1-9 — title + reduction-status block (cycle-013+) classifying the slice as a **negative-result slice** that is "the artifact, not redundant raw material to be lifted," citing the `polynomial_recurrence_step.md` precedent and naming the slice as **canonical instance** of `scope-out-obstruction.md:68` and `sequential-obstruction.md:53`. **Covered by**: `book/src/L1-L0/triangular-solve-obstruction.md:273-308` (the `## Related` section that explicitly cross-links the slice as annotated-and-retained), `book/src/concepts/scope-out-obstruction.md:66-78` (§"Canonical instance"), `book/src/concepts/sequential-obstruction.md:50-81` (§"Sub-kind: out-of-scope-obstruction"), `book/src/concepts/negative-result-slice.md:44-47` (§"Examples in this spec"). **Coverage status**: the *classification* is preserved across the four concept-page references, but the slice itself is the **referent** of those classifications — not redundant material.
- Lines 11-19 — Context paragraph naming the original scope question (sparse `Ly = b` / `Uy = b`, factor Allgatherv, residual computation; in-place vs out-of-place workspace). **Covered by**: `book/src/L1-L0/triangular-solve-obstruction.md:25-32` (literature signature `trsv(R, s) = the unique y with R · y = s`). **Coverage status**: PARTIAL — the obstruction theme covers the literature signature but does NOT preserve the **factor Allgatherv + residual-check** scope-question framing that motivates the negative result; that framing is the scope-out-obstruction concept page's canonical-instance hook (`concepts/scope-out-obstruction.md:68-78`).

**END of slice** (`book/src/spec/slices/sparse_triangular_solve.md:210-240`):

- Lines 210-241 — §"Methodological status" + the 4-step "L0→L1 scope-out obstruction" pattern: (1) scope question names primitives below a lower system boundary; (2) L0 finds only opaque forwarders; (3) the rotation that *would* apply at the wrapper boundary is not specific to the scope-question's framing; (4) correct output is a negative L1 result with named wrapper-level carry-through. **Covered by**: `book/src/concepts/scope-out-obstruction.md` (the entire concept page formalizes this pattern; the slice is its canonical instance). **Coverage status**: the 4-step pattern's *abstract form* is fully absorbed into the concept page, but the concept page **points back to the slice** as its worked example. The pattern's articulation requires both the concept (the abstract form) and the slice (the worked instance).

**Middle of slice** (lines 21-208 — §Background, §L0, §L1, §Disposition, §Open questions): coverage check:

| Slice section | Covered in firm artifact? | Where |
|---|---|---|
| §Background (textbook framing of sparse triangular solve, Davis 2006 / Li 2005 / Ghysels 2016 / Amestoy 2001) | NOT in obstruction theme; partially in OQ ledger (literature anchors are slice-resident) | nowhere else in artifact |
| §L0 "Negative result: no Palace-level sparse triangular solve" + zero-hit search anchors | YES, expanded | `L1-L0/triangular-solve-obstruction.md:65-89` (§(a) plus the cycle-028 whole-tree codemap zero-hit searches at `:361-367`) |
| §L0 "Sparse-direct factor application is opaque MFEM forwarding" (superlu.hpp:22-60, .cpp:83-132, NOREFINE at :78; strumpack.hpp:18-49; mumps.hpp:19-29) | PARTIAL — wrapper-class declarations covered; `NOREFINE` + `SamePattern_SameRowPerm` reuse-contract detail NOT carried | `L1-L0/triangular-solve-obstruction.md:151-170` (§(b3) covers the wrapper-class declarations); `L0/mfem-wrapper-solver.md` (`SuperLU` / `STRUMPACK` / `MUMPS` named as wrappers, line :42); `L0/linalg-solver-file.md:42` (same). The `NOREFINE` / `SamePattern_SameRowPerm` factor-reuse contract is NOT covered elsewhere; this is a unique slice contribution. |
| §L0 "Solver interface: forward/transpose pair, no triangular-specific surface" (`solver.hpp:42-65`, the `Mult2`/`MultTranspose2` multigrid-smoother analysis) | NOT covered in obstruction theme; partially covered in `L0/linalg-solver-file.md` and `L0/mfem-wrapper-solver.md` | `L0/linalg-solver-file.md` (the `Solver` abstract base is the file's subject); the **Mult2-is-for-multigrid-not-triangular-workspace** disambiguation appears ONLY in the slice |
| §L0 "MPI Allgatherv is not used for factors" (`communication.hpp:337-345`, `geodata.cpp:1536-1540` — whole-tree single-site analysis) | NOT covered anywhere in firm artifact | UNIQUE to slice. The whole-tree Allgatherv usage (single site, in mesh edge-attribute setup) is a load-bearing negative anchor for the "factor-Allgatherv lives below project boundary" claim that supports the scope-out-obstruction canonical-instance designation |
| §L0 "Residual check is the caller's responsibility" (`ksp.cpp:105-200`, `MakeWrapperSolver` install-as-preconditioner pattern) | PARTIAL | `L0/mfem-wrapper-solver.md:59,85,109` (`MakeWrapperSolver` is named as the central wrapper-construction call site); `spec/slices/cg_preconditioning_framework.md:62-64` (the install-as-pc dispatch on `SUPERLU/STRUMPACK/MUMPS`); `L1-L0/minres-iteration.md:69` (the wrapper pattern of `MakeWrapperSolver` at `ksp.cpp:103-`). The **"sparse-direct is one opaque preconditioner step, iterative-refinement explicitly disabled"** observation that grounds the negative result is the slice's unique contribution. |
| §L0 "Small-dense near-relatives (out of scope here)" — `nleps.cpp:563`, `romoperator.cpp:755-767` | YES, fully | `L1/lu_solve.md:12,41,45,47,77,89,100-101`; `L1-L0/lu-solve-mutation-rotation.md` (entire; sub-pattern A on `nleps.cpp:563`, sub-pattern B on `romoperator.cpp:755-767`); `L2/gram.md:269,326`; `L1/nleps_deflated_residual.md:42,93,94,132`; `L2/deflate.md:315`. The slice's forward-pointer to `small_dense_solve` materialized into firm `lu_solve` and its L1>L0 rotation — covered, but the slice is where the **disambiguation** ("these are NOT sparse triangular solves") is first stated. |
| §L1 "Obstruction: no L1 form exists" + wrapper-level carry-through into `apply_linop` / `ksp_solve` | YES, deeper coverage | `L1-L0/triangular-solve-obstruction.md:23-52` (the L1 form section); the **wrapper-level carry-through into `apply_linop` / `ksp_solve`** is preserved at the slice (the obstruction theme does not re-cite it because its §L1 form section is intentionally empty) |
| §Disposition "Scope-out variant resolution" + the load-bearing distinction from silent-partial-absorption | YES | `concepts/scope-out-obstruction.md` (the entire concept page formalizes this; the slice is its canonical instance, `:68`) |
| §Open questions (rename to `sparse_direct_solver_wrapper`; MFEM-level slice family for Allgatherv-on-factor) | PARTIALLY OPEN | the rename OQ is also live in the slice's reduction-status header (`:9` "Live OQs (unchanged)"); the MFEM-slice-family OQ has no other home in the artifact |

**Boundary-verification verdict**: the slice's START + END content is each covered *as classification* in firm artifact homes, but the slice is the **referent** of those classifications and contains material **NOT covered elsewhere** in the middle: the literature framing (Davis 2006 / Li 2005 / Ghysels 2016 / Amestoy 2001), the `NOREFINE` / `SamePattern_SameRowPerm` factor-reuse contract, the `Mult2`-is-for-multigrid-not-triangular-workspace disambiguation, the whole-tree `Mpi::Allgatherv` single-site analysis, the install-as-preconditioner + iterative-refinement-disabled narrative on `ksp.cpp:105-200`, and the **scope-question-framing→negative-result narrative arc** that supports the canonical-instance designations on the three concept pages.

### Non-link prose-reference grep (removal-specific)

Per the skill's removal step, ran whole-tree grep on `book/src/` for `sparse_triangular_solve` and its variants. **Non-link references that would orphan on slice removal** (linkcheck-invisible):

| Hit | Form | Behaviour on removal |
|---|---|---|
| `book/src/L1-L0/triangular-solve-obstruction.md:208` | inline-code `` `sparse_triangular_solve` `` in §Applicability conditions (1) — listing the shape | textual orphan (no link to break, but the inline-code referent loses its anchor) |
| `book/src/L1-L0/triangular-solve-obstruction.md:471` | inline-code `` `sparse_triangular_solve` `` in `verified_against` note for `scope-out-obstruction.md:68` | textual orphan; the verified-against note becomes self-referentially incomplete (cites a slice that no longer exists) |
| `book/src/L1-L0/triangular-solve-obstruction.md:475` | inline-code `` `sparse_triangular_solve` `` in `verified_against` note for `sequential-obstruction.md:53` | textual orphan; same as above |
| `book/src/concepts/sequential-obstruction.md:79` | inline-code `` `sparse_triangular_solve`'s `` in §"Sub-kind: out-of-scope-obstruction" "(in `sparse_triangular_solve`'s case: rename to `sparse_direct_solver_wrapper`)" | textual orphan; the concept-page worked-example reference loses its target |
| `book/src/L2-L1/incremental-least-squares-composition-lowering.md:147` | inline-code `` `sparse_triangular_solve` `` in body "(a general sparse-triangular solve `sparse_triangular_solve` — the …)" | textual orphan; the back_solve / trsv disambiguation narrative loses one of its anchors |
| `book/src/L2-L1/incremental-least-squares-composition-lowering.md:325` | inline-code `` `sparse_triangular_solve` `` in §"Open questions" "(`sparse_triangular_solve`, no positive L0 anchor, `scaffolding/open-questions.md:24`)" | textual orphan |
| `book/src/L2-L1/incremental-least-squares-composition-lowering.md:470` | inline-code `` `sparse_triangular_solve` `` in §"Open questions / caveats" "NOT the general `trsv` / `sparse_triangular_solve`" | textual orphan |
| `book/src/L1/back_solve.md:46` | inline-code `` `sparse_triangular_solve` `` in body "(`sparse_triangular_solve`, the Gauss-Seidel / ILU smoother kernel)" | textual orphan |
| `book/src/L1/back_solve.md:256` | inline-code `` `sparse_triangular_solve` `` in §Non-laws "(`trsv` / `sparse_triangular_solve`, the Gauss-Seidel / ILU smoother kernel…)" | textual orphan |
| `book/src/concepts/negative-result-slice.md:47` | live link `[``sparse_triangular_solve``](../spec/slices/sparse_triangular_solve.md)` in §"Examples in this spec" — **canonical-instance citation** | **HARD linkcheck break**; orphans the concept page's §"Examples in this spec" entry |
| `book/src/concepts/sequential-obstruction.md:53` | live link `[sparse_triangular_solve slice](../spec/slices/sparse_triangular_solve.md)` in §"Sub-kind: out-of-scope-obstruction" — **canonical-instance citation** | **HARD linkcheck break**; orphans the concept page's canonical-instance designation |
| `book/src/concepts/scope-out-obstruction.md:68` | live link `[sparse_triangular_solve](../spec/slices/sparse_triangular_solve.md)` in §"Canonical instance" — **canonical-instance citation** | **HARD linkcheck break**; orphans the concept page's §"Canonical instance" |
| `book/src/spec/index.md:21` | live link in catalog row | **HARD linkcheck break**; trivially removable (de-list the row) but downstream catalog entry is intentional |
| `book/src/L1-L0/triangular-solve-obstruction.md:277` | live link `[``sparse_triangular_solve``](../spec/slices/sparse_triangular_solve.md)` in §"Related" | **HARD linkcheck break** |
| `book/src/L1-L0/triangular-solve-obstruction.md:464` | YAML `citation:` field in `verified_against` block (not a markdown link) | textual orphan; the `verified_against` entry becomes self-referentially incomplete (cites a slice that no longer exists), but does NOT break linkcheck |
| `book/src/SUMMARY.md:151` | live link in TOC | **HARD linkcheck break** |
| `book/src/meta-reviews/2026-05-26-cycles-104-115.md` + `…cycles-116-127.md` + `…cycles-128-139.md` + `…cycles-140-151.md` + `…cycles-152-166.md` | inline-code in historical meta-review records (15 hits across 5 files) | textual orphans (historical, by design — meta-reviews are frozen records) |

**Grep verdict**: 6 hard linkcheck breaks across 6 firm files (`concepts/negative-result-slice.md:47`, `concepts/sequential-obstruction.md:53`, `concepts/scope-out-obstruction.md:68`, `spec/index.md:21`, `L1-L0/triangular-solve-obstruction.md:277`, `SUMMARY.md:151`); plus textual-orphan YAML/inline-code references (including `triangular-solve-obstruction.md:464` in the `verified_against` YAML — textual orphan, not a linkcheck break) and 9 inline-code prose orphans across 4 firm files. Three of the hard breaks are on **canonical-instance §-anchor citations** on concept pages — removing the slice would silently delete the concept pages' worked-example anchors, leaving the pattern definitions free-floating without their named instance.

## Recommendation

**Defer — DO NOT reduce, DO NOT remove.** The slice's reduction-status header (cycle-013+) is correct: this slice is the artifact, not redundant raw material. The c029 L1>L0 obstruction theme's `## Related` section confirms the slice's annotated-and-retained classification is unchanged by the obstruction theme landing. The Phase-1 corpus tally **stays at 9/10**; this slice is the **10th, retained by design**. The cycle-031 planner / OQ `sparse_triangular_solve-slice-reduction-candidacy-c029-on-disk` (if such an OQ exists; the priorities entry at `scaffolding/priorities.md:26` is the closest hit) should be **resolved as resolved-by-policy-not-reduction**: the on-disk obstruction theme does not change the slice's annotated-and-retained status because the slice is a named canonical instance of three concept pages.

Recommendations for follow-up dispatches (in order of fan-out):

1. **layer-intro-author** (or harvester) on `book/src/spec/slices/sparse_triangular_solve.md`'s reduction-status header — add **one line** noting that the c029 `triangular-solve-obstruction` L1>L0 theme is now on disk and cross-links the slice (currently the header lists the concept-page canonical-instance designations and the `polynomial_recurrence_step.md` precedent but predates the obstruction theme; the obstruction theme already cross-links the slice in its `## Related` section, but the slice does not yet reciprocally cross-link the obstruction theme). **Fan-out**: very low / cohort completion; just closes the cross-link loop. Routes through integrator-per-report's proposed-changes block.
2. **layer-intro-author** on `book/src/spec/index.md:21` — the table row's `## Status` column currently reads "L0 (obstruction)" with the disposition "follow-up slice `sparse_direct_solver_wrapper` proposed." Should be updated to note that the L1>L0 obstruction theme is the layered-artifact partner record (c029) and the slice's classification is unchanged. **Fan-out**: low; one-line update.
3. **meta-phase batch-9** — methodology observation: the cycle-013+ corpus-reduction policy's audit shape ("is there a firm layered home for this slice's material?") is necessary but not sufficient. A second axis — "is the slice a **named canonical instance** in a concept page §Canonical-instance line?" — is load-bearing for negative-result slices and is the load-bearing reason this slice is not reducible despite the c029 obstruction theme being on disk. Codifying this as a friction-ledger entry (`negative-result-slice-canonical-instance-blocks-reduction`) would prevent a future cycle from re-litigating the same audit. **Fan-out**: medium; one ledger entry + one bullet in the corpus-reduction policy doc (CLAUDE.md §Methodology invariants "Phase 1 corpus reduces as material is lifted").
4. **DEFER** the OQ rename suggestion (rename slice to `sparse_direct_solver_wrapper`, open a separate wrapper slice with the wrapper-level carry-through into `apply_linop` / `ksp_solve`). The slice's two live OQs are still open (the header at line :9 confirms); they are research questions whose resolution requires a separate harvester pass on the wrapper boundary, not a slice-reduction operation. They survived c029.

## Proposed changes (for integrator-per-report)

> **The cycle-013+ slice-reduction policy and the c029 obstruction theme already encode the recommended verdict** (defer / retain). The single mechanical change worth proposing now is the slice→obstruction-theme reciprocal cross-link to close the loop (recommendation 1 above). This is one line; it is independently dispatchable as a small layer-intro-author scope, but proposing it as a proposed-changes block here lets integrator-per-report enact it at cycle-031 finalize.

### Change 1: add cross-link from slice's reduction-status header to the c029 obstruction theme

**File:** `book/src/spec/slices/sparse_triangular_solve.md`
**Change:** in the cycle-013+ reduction-status block (lines 3-9), append one sentence cross-linking the c029 `triangular-solve-obstruction` L1>L0 theme as the layered-artifact partner record (matching the obstruction theme's reciprocal cross-link at `book/src/L1-L0/triangular-solve-obstruction.md:273-308`).

```text
OLD (lines 3-9):
> **Reduction status (cycle-013+):** this slice is a **negative-result slice** (in the spirit of `concepts/negative-result-slice.md`; that concept page does not yet list this slice in its §"Examples in this spec") and is **retained in full** — it is the artifact, not redundant raw material to be lifted. It is the **canonical instance** of:
> - `book/src/concepts/scope-out-obstruction.md` §"Canonical instance" (`:68`) — the L0→L1 scope-out obstruction (Palace forwards sparse-direct solves into MFEM/SuperLU_DIST/STRUMPACK/MUMPS opaquely; no Palace-level triangular-solve form to lift).
> - `book/src/concepts/sequential-obstruction.md` §"Sub-kind: out-of-scope-obstruction" (`:53`) — the out-of-scope sub-kind distinguished from genuine L2→L3 sequential obstruction.
>
> The §L0 opaque-forwarding evidence (`superlu.{hpp,cpp}`, `strumpack.hpp`, `mumps.hpp`, `communication.hpp`/`geodata.cpp`, `blockprecond.hpp`) is the citation grounding for those concept pages. There is — by construction — NO firm L0–L4 entry this slice's material lifts *into*; a negative result has no positive form to absorb. Per the `polynomial_recurrence_step.md` precedent ("the slice IS the artifact"), the corpus-reduction policy treats this slice as **annotated-and-retained**, not pending-lift.

NEW (lines 3-10, adding one sentence and one bullet):
> **Reduction status (cycle-013+, cross-link c029):** this slice is a **negative-result slice** (in the spirit of `concepts/negative-result-slice.md`; that concept page does not yet list this slice in its §"Examples in this spec") and is **retained in full** — it is the artifact, not redundant raw material to be lifted. It is the **canonical instance** of:
> - `book/src/concepts/scope-out-obstruction.md` §"Canonical instance" (`:68`) — the L0→L1 scope-out obstruction (Palace forwards sparse-direct solves into MFEM/SuperLU_DIST/STRUMPACK/MUMPS opaquely; no Palace-level triangular-solve form to lift).
> - `book/src/concepts/sequential-obstruction.md` §"Sub-kind: out-of-scope-obstruction" (`:53`) — the out-of-scope sub-kind distinguished from genuine L2→L3 sequential obstruction.
> - `book/src/L1-L0/triangular-solve-obstruction.md` (cycle-029) — the L1>L0 obstruction theme that is the **layered-artifact partner record** for this slice's negative result. The theme records the engineered-absence evidence (Adams 2003 polynomial-over-GS, GPU AMG GS→Jacobi flip) that postdates this slice; the two records **cross-link rather than supersede** (see the theme's §"Related" at `:273-308`). The slice's reduction status remains **annotated-and-retained**.
>
> The §L0 opaque-forwarding evidence (`superlu.{hpp,cpp}`, `strumpack.hpp`, `mumps.hpp`, `communication.hpp`/`geodata.cpp`, `blockprecond.hpp`) is the citation grounding for those concept pages. There is — by construction — NO firm L0–L4 entry this slice's material lifts *into*; a negative result has no positive form to absorb. Per the `polynomial_recurrence_step.md` precedent ("the slice IS the artifact"), the corpus-reduction policy treats this slice as **annotated-and-retained**, not pending-lift.
```

**Rationale**: closes the reciprocal cross-link loop. The obstruction theme already cross-links the slice (`L1-L0/triangular-solve-obstruction.md:273-308` and `:464-475`); the slice does not yet reciprocally cross-link the obstruction theme. Both directions are needed for navigation. This is the ONLY change recommended by this dispatch; the slice's annotated-and-retained classification is otherwise unchanged.

## Supporting evidence

- `book/src/spec/slices/sparse_triangular_solve.md:1-240` — the slice in full, with cycle-013+ reduction-status header (`:3-9`), Context (`:11-19`), L0 (`:37-120`), L1 (`:122-152`), Disposition (`:154-196`), Open questions (`:198-208`), Methodological status (`:210-240`).
- `book/src/L1-L0/triangular-solve-obstruction.md:273-308` — §"Related" section explicitly cross-linking the slice as annotated-and-retained, noting overlapping L0 anchors (4 shared sites — superlu / strumpack / mumps / blockprecond red-herring) and the engineered-absence evidence (Adams 2003 + GPU GS→Jacobi flip) that postdates the slice.
- `book/src/L1-L0/triangular-solve-obstruction.md:464-475` — the `verified_against` YAML entries cross-linking the slice (`book/src/spec/slices/sparse_triangular_solve.md`), `concepts/scope-out-obstruction.md:68`, `concepts/sequential-obstruction.md:53`.
- `book/src/concepts/scope-out-obstruction.md:66-78` — §"Canonical instance" naming the slice as **the** canonical instance with the specific scope-question framing (factor Allgatherv, residual checks, etc.).
- `book/src/concepts/negative-result-slice.md:44-47` — §"Examples in this spec" listing the slice as a negative-result-slice example (alongside `polynomial_recurrence_step`); the cycle-013+ reduction policy is rooted here.
- `book/src/concepts/sequential-obstruction.md:50-81` — §"Sub-kind: out-of-scope-obstruction" naming the slice as the worked example of the out-of-scope sub-kind, distinguished from class-(a) sequential obstruction.
- `book/src/concepts/negative-result-slice.md:46-47` — explicit precedent: the `polynomial_recurrence_step` slice has the same annotated-and-retained classification and is the canonical worked example of the negative-result-slice concept. `book/src/spec/slices/polynomial_recurrence_step.md:1-6` — that slice's analogous reduction-status header.
- `book/src/spec/index.md:21` — the slice's catalog row noting "L0 (obstruction)" + "follow-up slice `sparse_direct_solver_wrapper` proposed."
- `scaffolding/priorities.md:26` — the planner's carry-forward note from c029 dispatching this audit; the proposed verdict matches the priorities entry's fan-out ranking (Low / cohort completion).
- `scaffolding/cycle-031-resume-notes.md:32` — same carry-forward note from the resume-notes channel.
- Whole-tree grep on `book/src/` for `sparse_triangular_solve` (results in §"Non-link prose-reference grep" above): **6 hard linkcheck breaks** across 5 firm files (3 on canonical-instance citations) + **9 inline-code prose orphans** across 4 firm files would result from removal. Removal is structurally blocked; reduction-to-stub would still orphan the canonical-instance § anchors (the concept pages cite specific narrative sections, not just the slug).

## Open questions / caveats

1. **Methodology refinement candidate** (for meta-phase batch-9): the cycle-013+ corpus-reduction policy's audit checklist (CLAUDE.md §Methodology invariants "Phase 1 corpus reduces as material is lifted") names ≥2 firm layered entries the slice's material lifts into as the trigger for reduction. The `polynomial_recurrence_step` and `sparse_triangular_solve` slices have established a second policy axis in live use across cycles 013, 022, 028, 031: **if the slice is named as a §Canonical-instance worked example on a concept page, it is annotated-and-retained regardless of the firm-layered-home coverage**. This second axis is implicit in the cycle-013 reduction-status headers but not codified in the meta-phase policy or in CLAUDE.md. Friction-ledger candidate: `negative-result-slice-canonical-instance-blocks-reduction`. Not blocking this audit's verdict; recorded for meta-phase consideration.

2. **OQ unification opportunity**: the slice's two open questions (rename to `sparse_direct_solver_wrapper`; MFEM-level slice family for Allgatherv-on-factor) survived c029 unchanged. The first OQ (rename + wrapper-level re-push at L1) is a **harvester opportunity** for cycle-032+: a `sparse_direct_solver_wrapper` slice or L1 leaf would absorb the slice's L1 wrapper-level carry-through (the SuperLU / STRUMPACK / MUMPS wrappers as opaque `ksp_solve`-shaped preconditioners) into a positive entry while leaving the obstruction theme unchanged. This audit does not propose dispatching it; recorded as a follow-up. The OQ entries should not be promoted into the slice's reduction-status header (the header already mentions them at `:9`).

3. **Caveat on the spec/index.md row update (recommendation 2 above)**: the row currently reads "follow-up slice `sparse_direct_solver_wrapper` proposed" — this part of the row is still accurate (the rename OQ survived c029) and should NOT be changed. The row update is scoped to noting the c029 obstruction theme as the layered-artifact partner record; the rename-OQ phrasing is independent.

4. **Verification of the obstruction theme's `verified_against` self-reference to the slice** (`triangular-solve-obstruction.md:464`): the obstruction theme's YAML entry has `verdict: positive-cross-reference` (`audited_at: 2026-05-29T234506Z`) and the note text matches the slice's annotated-and-retained classification. The cross-link is already firm in the c029 record; the proposed reciprocal link in change 1 above closes the loop.

5. **Bare-path grep coverage caveat**: the grep run was on `book/src/` only. Bare paths to `spec/slices/sparse_triangular_solve.md` in `scaffolding/`, `reports/`, `log/`, `problems/`, and the `skills/` tree were NOT audited (they are out of scope for the linkcheck-readiness audit of the artifact). Spot-check: `scaffolding/priorities.md:26` is the c031 dispatch entry; `scaffolding/cycle-031-resume-notes.md:32` is the resume note; both would survive slice removal as plain-text references but would lose their referent — this is a normal scaffolding-vs-artifact distinction and does not gate the audit verdict.
