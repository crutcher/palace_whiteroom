---
verifies: ../CYCLE.md
critiqued_at: 2026-06-03T214500Z
critic_version: 1
checks:
  citation-validity: pass
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: pass
  cross-reference-integrity: pass
  edge-label-fidelity: pass
  plan-kind-consistency: pass
  skill-uptake-survey: pass
overall_status: ready
---

# META: verification of "Audit sparameter_reduce" (L4 verb law-confidence deepen-audit → firm)

## Critique

### Checks run

**citation-validity — pass.** `citecheck.py --scan` reports 24/24 OK (0 failing). I re-read the load-bearing pinpoints on disk via palace-codemap and every line anchor is exact: `MeasureSParameter` def `postoperator.cpp:1246` + close-brace `:1309`; the lumped self-term `vi.S.real(vi.S.real() - 1.0)` at `:1275` gated `if (idx == drive_port_idx)`; the generalized-S scale `vi.S *= std::sqrt(src_data.R / data.R)` at `:1280` guarded `if (std::abs(data.R) > 0.0)` at `:1278`; the wave de-embed `vi.S *= std::exp(1i * ...kn0 * ...d_offset)` twice at `:1301`/`:1302`; the cache writes `vi.S = data.GetSParameter(*E)` at `:1141` (lumped) and `:1239` (wave); the pure projection kernels `lumpedportoperator.cpp:283-294` (`:287`/`:290`, no self-term/scale) and `waveportoperator.cpp:780-793` (`:789-790`, no self-term/scale). All exact. The `verified_against:` YAML round-trip sub-check passes: I extracted the inner fenced yaml and `yaml.safe_load` round-trips cleanly with all 8 entries; no `note:` value's first character is `'` or `"` (first chars: M/L/W/v/v/f/T/d). `port_projection.md:1-354` is in range (file is 354 lines). The inherited projection-kernel test citations (`test-lumpedportintegration.cpp:367,720`, `test-romoperator.cpp:603`) are honestly disclosed as carried-forward-not-re-read; they are not the firming basis and citecheck cleared their bounds.

**surface-or-evidence — pass.** This is a maturity-promotion audit (rough-in (test-coverage-bounded) → firm) modifying the chapter's `## Status` surface, backed by rotation/evidence reads of the positive `MeasureSParameter` assembly + the firm folded `port_projection`. Not a bare rotation_claim. The record-definition sub-check is satisfied for the in-scope edits: the verb's signature names `[PortMode]` and `Matrix[p,p]` (compositional shapes, not new config/state records introduced here), and `frequency_sweep`/`port_projection` already have homes; no signature-named record is left without a definition home by this report.

**rotation-quality — pass (THE load-bearing check).** The promotion rests on the claim that all 5 holding laws + 2 do-not-hold laws are syntactic identities / closed-form arithmetic over (a) the firm folded `port_projection` and (b) the positive `MeasureSParameter` body — with no smuggled mathematical-property axiom. I verified this on disk. The two `GetSParameter` kernels return the PURE projection with no self-term and no scale (confirmed: lumped `dot((*s)*E.Real(), ...)`; wave the `(E×H_inc⋆)·n` complex recombination) — so the self-term and scaling live entirely in the assembly. The assembly applies only bare scalar arithmetic over the cached value: the literal `- 1.0` self-term, the `*= std::sqrt(R_src/R_dst)` directional scale (guarded by `|R| > 0`), and the `*= std::exp(1i·kn0·d_offset)` de-embed. The cache write `vi.S = data.GetSParameter(*E)` holds the firm primitive output VERBATIM with no intervening transform, so the "cached projection crossing" the verb's prior Status flagged genuinely resolves to an evaluation-strategy/data-flow detail, not an algebraic axiom — confirmed at both `:1141` and `:1239`. None of the laws is a theorem conditional on a numerically-asserted structure: law 1's linearity is inherited from the firm `port_projection` dual-pairing (not asserted here); laws 2/do-not-hold-1 are negative structural read-offs; law 3 is the literal affine `−1`; law 4 is arithmetic of `sqrt`-of-ratio + the symmetric exponential product; law 5 is the no-inter-entry-accumulator loop shape; do-not-hold-2 reads the literal guards. The contrast claim against `matrix-weighted-norm` is confirmed against that chapter: its laws 4/6/7 (triangle / Cauchy–Schwarz / parallelogram) "require the inner-product structure on `B`, which the L0 source does not directly verify" (its gate (c), `matrix-weighted-norm.md:115`) — a genuinely different situation. The firm-on-positive-structure / syntactic-identity escape is correctly applied; this is the same route as the c082 A2 `eigenfreq_qfactor_reduce` promotion. Promotion is sound.

**variant-axis-coverage — pass.** The orthogonal axes are explicitly enumerated and dispositioned: port-kind (lumped XOR wave — covered as two assembly loop variants, with the whole-model XOR guard `:1256-1259` confirmed on disk), scaling-presence (generalized-S via `|R| > 0` guard; de-embed via `d_offset ≠ 0`, both covered as the scale-axis identity in do-not-hold-2), element-type (complex, pinned). The lumped-vs-wave split is correctly framed as a variant axis (two assembly variants of the same driven postprocess), NOT a hidden second pipeline; the single-excitation + one-port-kind preconditions are read on disk. No hidden branch.

**cross-reference-integrity — pass.** All referenced chapter files exist on disk (`sparameter_reduce.md`, `eigenfreq_qfactor_reduce.md`, `port_projection.md`, `matrix-weighted-norm.md`, `index.md`, the three `sparameters.{L4,L1,L0}.md`, `eigenvalue-untransform.md`, `assemble_frequency_operator.md`, `frequency_sweep.md`). Every `[replace]` anchor in the proposed-changes matches the on-disk source verbatim: the `firmness: rough-in` frontmatter line, the §Dependencies stale clause, the index `Firm at L4 (15 + 4 outer-driver)` tally line, the dep-map status-cell (line 107), the reduce-to-matrix prose note (line 79), and all of the feature-column `*(rough-in)*`/`(rough-in)` token sites at the L4/L1/L0 lines named. The count delta is correct: §32 firm 15→16, and the §57 rough-in line (`1 + 1 test-coverage-bounded` = `domain_energy_reduce` + `solve_family`) correctly does NOT decrement because `sparameter_reduce` was never a §57 bullet — the report's tally-audit note flags this pre-existing asymmetry accurately for the cross-layer-cross-cutter. The `port_projection` gate-b claim is confirmed: `port_projection.md` is `firmness: firm` and `:61-64` state it satisfies this verb's gate-b. The coupled `sparameters.{L4,L1,L0}` column correctly stays `seed` with the promotion-rule prose untouched (batch-26 deferral honored; the tension is flagged, not resolved). The stale `bilinear-form` → `port_projection` carry-forward correction in `sparameters.L0.md:28` is verified: the stale `[bilinear-form](../L1/bilinear-form.md)` reference is present on disk and `port_projection` (firm, c077) is the correct firm L1 home per `sparameters.L1.md:8` — a bounded, evidenced citation correction. Build-readiness fence guard: the Edit-1 block uses a 4-backtick outer `` ````edit: `` fence (open `:224`, close `:340`) that correctly ENCLOSES the nested 3-backtick `yaml` block (`:305`–`:339`) — balanced; the firm `## Status` body + `verified_against:` block land INSIDE the fence. All other edit blocks are 3-backtick with balanced open/close parity. No firm-body-outside-fence defect.

**edge-label-fidelity — pass.** No L_{n+1}→L_n edge label is asserted; this is an in-layer L4 verb maturity promotion. The chapter's "Lowers to" is L4→L1 identity-in-form (the per-port projection it folds), and the prose narrates that forward direction. Direction-of-definition is honored (the report's own Open-questions note confirms this).

**plan-kind-consistency — pass.** Declared kind is a lowering-verifier law-confidence audit ending in a firm promotion. The content matches: a per-citation audit with on-disk verdicts, an applicability-conditions section, a per-law discriminating-test pass, and a fresh `verified_against:` block — the audit shape, not a rough-in placeholder masquerading as firm. The firm verdict is fully supported by the syntactic-identity reads (see rotation-quality). Consistent.

**skill-uptake-survey — pass (telemetry only).** The report references `verify-citation-range` mechanically (citecheck `--anchor`/`--scan`) and uses the MCP-first localization path. The relevant skills for this shape (citation-range verification, the firm-on-positive-structure escape precedent) are invoked. No gap.

### Issues found

None. All 8 checks pass. The promotion is sound: the load-bearing rotation-quality claim — that every law is a syntactic read-off over the firm `port_projection` + the bare-scalar-arithmetic assembly with no axiom-needing law — is confirmed against the on-disk `MeasureSParameter` body and the two pure projection kernels. The citations are exact, the `verified_against:` YAML round-trips clean, all cross-references and edit anchors resolve, the count delta is correct, the fences are balanced, and the coupled feature-column deferral to batch-26 is correctly scoped out. The report also self-discloses its one carry-forward non-re-read (the projection-kernel test citations) honestly, and that disclosure does not affect the verdict. `overall_status: ready` set (all-pass clean report; no repairer will run).
