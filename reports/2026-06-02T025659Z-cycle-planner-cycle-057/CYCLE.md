---
agent: cycle-planner
invoked_at: 2026-06-02T025659Z
scope: cycle-057 dispatch plan
status: pending
---

# Cycle 057 dispatch plan

## Goals selected this cycle

Cycle-057 is the THIRD/FINAL primary cycle of meta-batch-17 (cycles 055/056/057; the batch-17 meta-phase fires AFTER this cycle's finalize as a separate dispatch). Land a coherent slice that **closes the two in-flight batch-17 threads** and **opens the two batch-18 spine threads** under the 2026-06-01 VOCABULARY-SHIFT REDIRECT:

1. **`solve_family` lowering depth (LEAD).** Assess + author the L3 image of the firm L4 `solve_family` combinator and its L3>L2 hop — but **judged honestly under the redirect's anti-mirror principle**: only land an `L3/solve_family` entry if the value-threaded family-solve loop is a *genuine concise L3 form*, not a mirror of the L4 map. The L4>L3 `solve-family-map-dissolution` theme already describes the L3 form in detail; the dispatch's first job is the warrant judgment (author / no-entry-warranted are BOTH valid outcomes).
2. **`fe-operator-assemble-mutation-rotation` theme firm-flip.** All 3 LHS operators are now firm (`fe_assemble` c054 + `eliminate_rhs`/`eliminate_essential_bc` c055), the citation residual is closed (c056 D3), and the libCEED boundary is themed (c055 D5). Re-anchor the elimination legs + flip rough-in→firm IF fully anchored.
3. **`DrivenSolver::SweepAdaptive` 2nd-map-witness probe (observation-first, cheap).** Could discharge the `map_solve` 2-witness gate, or route to the `fold_solve` thread.
4. **`fold_solve` transient combinator thread-opener (observation-first/sketch).** Open the transient state-threaded fold thread (the c056 D1 spine finding) — analogous to how the FE-assembly thread was opened, NOT a forced firm landing.

#1 + #2 complete in-flight threads (the headline propagation + the FE sub-spine); #3 + #4 are observation-first findings setting up batch-18. This is a 4-dispatch coherent slice (well under the 12-cap), deliberately scoped light for the last cycle before meta.

## Deliverable-presence verification

Per the MANDATORY paste-inline-evidence pre-dispatch check. Evidence pasted literally from on-disk commands run this planning pass.

### D1 — `L3/solve_family.md` + `L3-L2/solve-family-...` hop (target files MUST be ABSENT)
1. **File existence (L3 image):** `ls book/src/L3/solve_family.md` →
   ```
   ls: cannot access 'book/src/L3/solve_family.md': No such file or directory
   ```
   ABSENT — open by construction (the c055-deferred forward-ref). PASS.
2. **File existence (L3>L2 hop):** `ls book/src/L3-L2/ | grep -i solve` →
   ```
   eigsolve-opaque-eigen-iteration.md
   ksp-solve-outer-driver.md
   ```
   No `solve-family-*` theme present (the two hits are the pre-existing eigsolve + per-member ksp-solve themes). The `solve-family-...` L3>L2 hop is ABSENT. PASS.
3. **OQ-ledger RESOLVED-grep:** no `solve_family`-L3 slug appears in a RESOLVED/CLOSED disposition; the c056 hand-off + c055/c056 integrator-signals name `L3/solve_family + L3>L2 hop` as the SEQUENCED c057 forward-frontier landing (open). PASS.
4. **Structural-block check:** none. The LHS L4 `solve_family` is on disk (`rough-in (test-coverage-bounded)`) and the L4>L3 `solve-family-map-dissolution` is **firm on structure** (the dissolution shape does not depend on the test-coverage-gated independence law — its §Status explicitly scopes the caveat to the upstream cap's laws, NOT the rotation shape). So the L3 image + L3>L2 hop are NOT blocked by the L4 cap's `rough-in` status. PASS.
   - **NOTE (warrant judgment, NOT a block):** under the redirect, the L3 entry is conditional on the L3 family-solve loop being a *genuine concise L3 form*. The dissolution theme's §"L4 vs L3 distinction" already describes the L3 form (explicit positional `for` over the family-index set, operator construction hoisted by hand, pre-sized `std::vector<Vector>` indexed by `step`, embarrassingly-parallel-written-sequentially, NO `sequential-obstruction`). D1's FIRST task is the warrant call — see D1 rationale.

### D2 — `fe-operator-assemble-mutation-rotation.md` firm-flip (target EXISTS, currently rough-in)
1. **File existence:** `ls book/src/L1-L0/fe-operator-assemble-mutation-rotation.md` →
   ```
   -rw-rw-r-- 1 crutcher crutcher 12707 Jun  1 19:05 book/src/L1-L0/fe-operator-assemble-mutation-rotation.md
   ```
   EXISTS. PASS (this is a promotion, not a fresh land).
2. **Maturity / already-discharged check:** `grep '## Status'` / frontmatter →
   ```
   4:status: rough-in
   19:## Status
   ```
   On-disk §Status block reads `rough-in (thread-opener)` with three gating reasons (a)/(b)/(c). **All three are now STALE on disk** (the dispatch's substance): (a) "remaining speculative L1 operators `eliminate_essential_bc`, `eliminate_rhs` still rough-in placeholders" — both are now **firm** (verified below); (b) "libCEED boundary not yet classified obstruction vs transitive-firm" — now **classified** `obstruction (opaque-library-ownership)` and themed (c055 D5 `fe-assemble-libceed-boundary-obstruction.md`); (c) "integrator-term vocabulary only partially witnessed" — the `weak_form_term` cohort stays DEFERRED by design (does not gate the BC-elimination lowering). The dispatch is a genuine promotion (rough-in → firm), NOT a no-op. PASS.
3. **LHS-operator firmness (the (a) gate):** `grep '## Status'` on the 3 LHS ops →
   ```
   book/src/L1/eliminate_rhs.md:204:## Status            → `firm`. Clean-gate call: PROMOTE — clean.
   book/src/L1/eliminate_essential_bc.md:212:## Status   → `firm`. Clean-gate call: PROMOTE — clean.
   book/src/L1/fe_assemble.md:195:## Status             → `firm`. Clean-gate call: PROMOTE — clean.
   ```
   All 3 LHS operators FIRM. The (a) gate is cleared. PASS.
4. **Structural-block check:** the theme firm-flip is gated (per the c055 D6 OQ `865` + the c056 hand-off) on (i) the `eliminate_*` elimination-leg theme-side re-anchors (D3's `eliminate-rhs-mutation-rotation-l1-l0-theme-unauthored` + D4's `eliminate-essential-bc-l1-l0-lowering-re-anchor`) being done IN this dispatch, and (ii) the libCEED boundary being accounted (now themed). These are WORK FOR D2, not external blocks — D2 does the elimination-leg re-anchors AS PART OF the flip. The clean-gate is: if the elimination legs do NOT re-anchor cleanly in firm-spine vocabulary, the theme STAYS rough-in (spine finding, not a forced flip). PASS (clean-gated).

### D3 — `DrivenSolver::SweepAdaptive` 2nd-map-witness probe (observation-only, NO book mutation)
- **Open by construction** — observation-first probe, no named book deliverable. The c056 D1 OQ `drivensolver-sweepadaptive-second-map-witness-probe` (ledger ~906) explicitly names it a batch-18 candidate "surfacing for the plan, not enacting." Source anchor codemap-confirmed this pass: `palace/drivers/drivensolver.cpp:231` `ErrorIndicator DrivenSolver::SweepAdaptive(SpaceOperator &space_op) const` (the PROM/adaptive path; `:73` shows `adaptive ? SweepAdaptive(...) : SweepUniform(...)` dispatch). No deliverable-presence check needed (no slug). The probe MAY append an OQ disposition; it does NOT author `map_solve.md` this cycle regardless of verdict (see D3 rationale + the over-unification guard).

### D4 — `fold_solve`/`time_step_fold` transient combinator thread-opener (observation-first sketch, NO firm land)
- **Open by construction** — thread-opener, analogous to the c053 FE-assembly thread opening. The c056 D1 spine finding (OQ `solve-family-shape-classification-fold-vs-map-and-map-solve-superset-deferred`, ledger ~895) names the transient pipeline as a state-threaded FOLD and a "future `fold_solve` / `time_step_fold` combinator" — a NAMED spine candidate, NOT yet authored. Source anchors codemap-confirmed this pass: transient fold witness `palace/drivers/transientsolver.cpp:93` `time_op.Step(t, delta_t); // Advances t internally`; the persistent-state advance `palace/models/timeoperator.cpp:409` (`ode->Step(sol, t, dt)` per the c056 OQ). No deliverable-presence check needed (sketch, no firm slug). D4 is observation-first — it OPENS the thread (sketches the combinator shape + the witness), it does NOT force a firm `fold_solve` landing (the redirect's "NOT a forced firm landing").

### STOP-PROPOSING NEGATIVE LIST check
None of the 4 dispatches touch a disqualified slug. The list (`lu_solve`, `back_solve`, `ls-update-column`, the 4 NLEPS atoms; `apply_nonlinear_pencil` HELD) is not implicated. PASS.

### Leaked-tool-tag reminder
Per the c055 `eliminate_essential_bc.md` leak + the c056 integration-tooling-friction reminder: D1 (which authors new files IF the warrant call is YES) and D2 (which edits a theme body) carry an explicit **"no leaked tool-call XML tags in new/edited files — scan the file tail before finishing the Write"** instruction in their dispatch scopes.

## Dispatches

### D1 — (`abstractor`) `solve_family` lowering depth: warrant-judge the L3 image, then author the L3 entry + the L3>L2 `solve-family-step-fold-dissolution` hop IF warranted

- **agent:** `abstractor`
- **scope:** Assess whether the value-threaded L3 family-solve loop warrants a distinct `book/src/L3/solve_family.md` entry under the redirect's anti-mirror principle, then act on the judgment:
  - **The warrant call is the FIRST deliverable** (state it explicitly + cite). The L4>L3 `solve-family-map-dissolution` theme (firm, on disk) already describes the L3 form in its §"L4 vs L3 distinction": explicit positional `for` over the family-index set, operator construction hoisted by hand outside the loop, pre-sized `std::vector<Vector>` indexed by a running `step`, family members independent so the sweep is **embarrassingly-parallel-written-sequentially with NO `sequential-obstruction`** (contrast the per-member `ksp_solve` outer-driver fold it wraps, which DOES carry a `sequential-obstruction`). The redirect SUPERSEDED "Identity-lowerings still require both L levels" — do NOT mirror the L4 entry just to "have both levels."
  - **Judge:** is the L3 family-solve loop a *genuine concise L3 form with its own iteration-layer vocabulary*, or does it collapse trivially into what the dissolution theme already says? The likely-affirmative axis: the L3 form has a **distinct iteration-rotation content** — it is the iteration layer's statement that the family sweep is the ONE solver-driver loop in Palace that is parallel-decomposable (no sequential-obstruction), which is a substantive L3 finding (the iteration rotation's whole job is recording which loops lift and which obstruct; "this one lifts cleanly / is embarrassingly parallel" IS iteration-rotation content, distinct from the L4 map-combinator typing). The likely-negative axis: if the only L3 content is "the map became a for-loop," that is the dissolution theme's job and an L3 entry would be a thin mirror.
  - **IF WARRANTED (author):** land `book/src/L3/solve_family.md` (firm — the loop structure is read directly off positive source `electrostaticsolver.cpp:30-90` + `magnetostaticsolver.cpp:30-100`, value-thread-isomorphic, NOT test-gated like the L4 cap's independence law) using L3 iteration-layer vocabulary (the explicit accumulating outer sweep; the no-sequential-obstruction / parallel-decomposable characterization as the substantive payoff) + the `book/src/L3-L2/solve-family-step-fold-dissolution.md` L3>L2 hop (CANONICAL SLUG — narrate forward L3 explicit family-sweep → L2 form; the L2 image is the per-member `ksp_solve` L2 composition mapped over the family). Wire SUMMARY.md, the `L3/index.md` dep-map row + §Vocabulary-cohort bullet, the `L3-L2/index.md` theme-list row + bullet. **D1 is the SOLE count-owner** for both `L3/index.md` and `L3-L2/index.md` consolidated tallies this cycle (no other dispatch writes those indices) — author the row+bullet AND the consolidated tally (count from on-disk `## Status` lines, NOT index cells, per the c055 D8 count-owner lesson). Re-anchor the L4>L3 dissolution theme's forward-reference to the now-existing `L3/solve_family.md` (it currently names the L3 image as a dissolution target by description) — and the L4 `solve_family.md` §"L4 vs L3 distinction" / `lowers_to:` if it asserts "no interposed L3 entry" (per `floor-landing-implies-same-cycle-adjacent-entry-reanchor` — couple the adjacent re-anchor into THIS dispatch).
  - **IF NOT WARRANTED (no-entry verdict):** observation-only — record the verdict ("the L4>L3 dissolution theme fully expresses the lowering; no distinct L3 form") with citation, append the OQ closure, NO book mutation, NO L3 entry. This is an EXPLICITLY VALID outcome per the c057 hand-off.
  - **Leaked-tag scan:** if authoring, scan new-file tails for stray tool-call XML before finishing.
- **deps:** none (LEAD; standalone — the L4 LHS + L4>L3 theme are already firm on disk).
- **rationale:** Active-head LEAD (`solve-family-l3-image`, fan-out HIGH); the c056 hand-off names it "the natural c057 LEAD." Continues the headline combinator's propagation downward. The warrant-first framing honors the redirect's anti-mirror principle — the dispatch is empowered to conclude "no L3 entry warranted," which the redirect explicitly flags as valid. Routed to `abstractor` (not `harvester`) because the deliverable is primarily the lowering hop + the warrant judgment about a derived form, not a fresh operator harvest from source.

### D2 — (`lifter`) `fe-operator-assemble-mutation-rotation` theme firm-flip + `eliminate_*` elimination-leg re-anchors

- **agent:** `lifter`
- **scope:** Promote `book/src/L1-L0/fe-operator-assemble-mutation-rotation.md` rough-in → firm IF fully anchored, doing the gating work in-dispatch:
  - **Re-anchor the elimination legs** to the now-firm operators: the essential-BC leg to firm `book/src/L1/eliminate_essential_bc.md` (narrate how the L1 pure post-composition `K' = eliminate_essential_bc(K, dofs, policy)` lowers into Palace's deferred-config-then-apply two-step: record `(dofs, policy)` on the `ParOperator` wrapper via `SetEssentialTrueDofs` `laplaceoperator.cpp:216-217`, then mutate the assembled `HypreParMatrix` in place via `EliminateBC` at parallel-assemble time; plus the square-operator guards + rectangular reject); the RHS leg to firm `book/src/L1/eliminate_rhs.md` (`b − K·x_bc`, body `apply_linop`+`axpy` per the firm entry; def `rap.cpp:56`, call `laplaceoperator.cpp:252`). Resolves the c055 D3/D4 OQs `eliminate-rhs-mutation-rotation-l1-l0-theme-unauthored` + `eliminate-essential-bc-l1-l0-lowering-re-anchor`.
  - **Rewrite the §Status block:** the three stale (a)/(b)/(c) gating reasons no longer hold — (a) all 3 LHS ops firm, (b) libCEED boundary classified+themed `obstruction (opaque-library-ownership)` (cite the c055 `fe-assemble-libceed-boundary-obstruction.md`), (c) `weak_form_term` cohort DEFERRED-by-design (does not gate the BC-elimination lowering; note it as a scoped-out future-cohort, not a promotion blocker). Reference the libCEED-boundary obstruction theme as the documented boundary the firm theme defers to.
  - **CLEAN-GATE (load-bearing):** flip to `firm` ONLY IF both elimination legs re-anchor cleanly in firm-spine vocabulary AND the libCEED boundary is fully accounted by reference. If an elimination leg does NOT lower cleanly in firm vocabulary, the theme STAYS rough-in and the dispatch records WHAT still gates it (a spine finding — do NOT force the flip). State the gate outcome explicitly.
  - **Co-schedule the belt-and-suspenders citation re-grep** on this theme file only (the `:77`/`:97` `AddSubOperator` pinpoints vs the `fe_assemble.md` range form — both correct per the c055 OQ `865`; verify no NEW drift introduced by the re-anchor edits).
  - **Leaked-tag scan:** scan the edited file tail for stray tool-call XML before finishing.
- **deps:** none (independent of D1 — different layer-edge, different file; the FE operators are all firm on disk).
- **rationale:** Active-head item 2 (`fe-operator-assemble-mutation-rotation` theme firm-flip, fan-out MEDIUM); the c056 integrator-signals name it ready ("can flip rough-in→firm" now the citation residual is closed). Completes the FE-assembly sub-spine's L1>L0 lowering documentation. Clean-gated per the redirect ("advance only when cleanly describable").

### D3 — (`cross-layer-cross-cutter`) `DrivenSolver::SweepAdaptive` 2nd-map-witness probe (observation-only, fold-vs-map GUARDED)

- **agent:** `cross-layer-cross-cutter`
- **scope:** Probe `DrivenSolver::SweepAdaptive` (`palace/drivers/drivensolver.cpp:231+`, the PROM/adaptive path — distinct from the uniform `SweepUniform` loop the c056 D1 examined; dispatch at `:73`) for the `map_solve` 2-witness gate. **Observation-ONLY, no book mutation.** Determine: is `SweepAdaptive` a **second operator-varying-MAP instance** (each sample-point solve a pure function of its index, operator rebuilt per-index, no cross-index state) — which would DISCHARGE the ≥2-witness gate (`skills/disciplined-cross-pipeline-combinator-mining-gate`) and license a batch-18 `map_solve.md` authoring — OR does it **fold a reduced-order-model (PROM) state** across sample points (the offline basis built up greedily: note `offline_tol`/`convergence_memory`/`max_size_per_excitation`/`prom_indices` at `:240-244`, which strongly suggest a state-accumulating greedy basis-growth fold), making it a FOLD (a `fold_solve`-family witness, NOT a 2nd map witness). **Cite the `disciplined-cross-pipeline-combinator-mining-gate` skill.** Apply the fold-vs-map guard rigorously — over-classifying a greedy-basis fold as a map would over-unify. **Do NOT author `map_solve.md` this cycle regardless of verdict** (a discharge LICENSES batch-18 authoring; it does not itself author — the rotation-quality pass on `solve_family ⊂ map_solve` is the separate authoring step). Append the verdict as an OQ disposition (discharge → routes the batch-18 `map_solve` authoring; fold → routes to the D4 `fold_solve` thread).
- **deps:** none (observation-only; reads source, appends OQ).
- **rationale:** Active-head item 3 (`drivensolver-sweepadaptive-second-map-witness-probe`, fan-out HIGH-IF-DISCHARGED, cheap); the c056 D1 OQ surfaced it as the cheap c057 probe. Observation-first + fold-vs-map guarded per the c055/c056 disciplined-mining precedent. Cheap (one source region + the gate check). The codemap peek this pass (PROM `offline_tol`/`convergence_memory`/`max_size`) is a strong prior that this is a fold, but the dispatch makes the call from the body.

### D4 — (`abstractor`) `fold_solve` / `time_step_fold` transient combinator thread-opener (observation-first sketch)

- **agent:** `abstractor`
- **scope:** OPEN the transient state-threaded-fold combinator thread (the c056 D1 spine finding) — analogous to how the c053 FE-assembly thread was opened. **Observation-first / sketch, NOT a forced firm landing.** Sketch the `fold_solve` / `time_step_fold` combinator shape from the transient witness: the outer time sweep `transientsolver.cpp:77-93` (`time_op.Step(t, delta_t)` at `:93`, advancing `t` internally each step) threading the persistent field-state `sol` step→step via `timeoperator.cpp:409` (`ode->Step(sol, t, dt)`) — each step's input IS the prior step's output (a genuine fold, distinct from the `solve_family` map: the map's members are independent, the fold's are sequentially dependent). State the combinator's distinguishing structural signature (a `fold` over the time-index with the field-state as the accumulator; the outer time sweep is a `sequential-obstruction` at L3, unlike the `solve_family` family sweep which is embarrassingly parallel). Record it as a NAMED batch-18 spine candidate (where it would live: an L4 outer-driver vocabulary row alongside `solve_family`; what a 2nd fold-witness or downstream pull would justify a firm landing). **Do NOT force a firm `fold_solve` entry** — this opens the thread (the redirect's "NOT a forced firm landing"); a firm land is a separate combinator-miner dispatch when a 2nd fold-witness lands. May note the D3 SweepAdaptive verdict as a potential 2nd fold-witness IF D3 finds a PROM-state fold (forward-reference only — D4 does not depend on D3's verdict to open the thread). Output is a sketch/observation in the report; if it lands any book artifact it is at most a `stub` L4 outer-driver dep-map row (claim-free placeholder) — prefer recording in the report + OQ over a premature stub. **Leaked-tag scan** if any file is written.
- **deps:** none (observation-first; the transient witness is on disk/source, independent of D3).
- **rationale:** Active-head item 4 (`fold_solve` transient combinator thread-opener, fan-out MEDIUM, observation-first); the c056 D1 spine finding + integrator-signals name it the next distinct solver-spine combinator after `solve_family`. Opening the thread now (last cycle before meta) sets up the batch-17 meta-phase to assess the solver-test-load's combinator yield (`solve_family` mined + propagated; `fold_solve` thread opened) and gives batch-18 a ready candidate. Routed to `abstractor` (sketch/thread-opener role) per the FE-assembly thread-opener precedent.

## Overlap analysis

Pairwise (4 dispatches → 6 pairs):

- **D1 × D2:** NO overlap. D1 touches `book/src/L3/solve_family.md` (new, if warranted), `book/src/L3-L2/solve-family-step-fold-dissolution.md` (new), `L3/index.md` + `L3-L2/index.md` (D1 sole count-owner), and re-anchors the L4 `solve_family.md` / L4>L3 dissolution theme. D2 touches `book/src/L1-L0/fe-operator-assemble-mutation-rotation.md` + reads (does not write) the firm `L1/eliminate_*` entries. Disjoint files, disjoint layer-edges (L3/L3-L2/L4-L3 vs L1-L0). PARALLEL.
- **D1 × D3:** NO overlap. D3 is observation-only (reads `drivensolver.cpp`, appends OQ); D1 writes L3/L3-L2/L4-L3. Disjoint. PARALLEL.
- **D1 × D4:** NO overlap. D4 is observation-first (reads `transientsolver.cpp`/`timeoperator.cpp`, writes a report sketch + OQ, at most a `stub` L4 dep-map row). D1 writes L3/L3-L2. **Watch:** both are `abstractor` and both touch the broad "outer-driver combinator vocabulary" CONCEPT, but on DISJOINT artifacts — D1's L3>L2 `solve-family-step-fold-dissolution` (the MAP family) vs D4's `fold_solve` sketch (the FOLD family); D1 writes `L3/index.md`/`L3-L2/index.md`, D4 (if it writes at all) touches `L4/index.md` dep-map. No shared file region. PARALLEL. (If D4 elects a `stub` L4 row, it is a distinct append to a different index than D1's — non-overlapping per the dep-map-row rule.)
- **D2 × D3:** NO overlap. D2 writes L1-L0; D3 observation-only on driven source. PARALLEL.
- **D2 × D4:** NO overlap. D2 writes L1-L0; D4 observation-first on transient source. PARALLEL.
- **D3 × D4:** NO overlap on artifacts (both observation-first; D3 appends a `map_solve`/SweepAdaptive OQ disposition, D4 appends a `fold_solve`-thread OQ + report sketch — distinct OQ headers, append-only). **Conceptual coupling (not an artifact conflict):** D3's verdict (if SweepAdaptive is a PROM-state fold) is a candidate 2nd fold-witness for D4's thread — but D4 opens the thread independently from the transient witness and only forward-references D3's verdict; no ordering dependency. PARALLEL.

**No shared count-owned index across dispatches:** D1 sole-owns `L3/index.md` + `L3-L2/index.md` tallies; D2 touches only L1-L0 (no consolidated cohort tally moves — a single theme promotion, the L1-L0/index status cell for this one theme is D2's own row, not a shared aggregate); D3/D4 write no index tallies. No count-ownership partition needed beyond D1's sole ownership of its two indices.

## Sequencing schedule

**Single wave — all 4 dispatches PARALLEL (wave 1).**

No forward-reference dependency exists: D1's targets are new slugs whose only forward-refs are to the already-firm-on-disk L4 cap + L4>L3 theme (no sibling-dispatch slug); D2's targets are the already-firm-on-disk L1 operators; D3/D4 are observation-first reading source. The D3→D4 conceptual coupling (SweepAdaptive-as-possible-2nd-fold-witness) is a forward-reference only, not an ordering constraint — D4 opens the `fold_solve` thread from the transient witness regardless of D3's verdict. Per the conflict-tolerance philosophy (when in doubt, PARALLEL), all 4 fire in one wave.

Pipeline after wave 1: 4 critics → repairers on warn/fail → `integrator-per-report` ×4 (serial) → ONE `integrator-finalize` (rebuild + commit + push + housekeeping; the batch-17 meta-phase then fires as a SEPARATE dispatch after this finalize).

## What lands c057 vs what batch-17 meta / batch-18 picks up

- **Lands c057 (forward-frontier):** D1 the `solve_family` lowering depth — EITHER the firm `L3/solve_family` + `L3-L2/solve-family-step-fold-dissolution` hop (if warranted) OR a recorded no-entry verdict (both valid); D2 the `fe-operator-assemble-mutation-rotation` firm-flip (if the clean-gate passes) with the `eliminate_*` leg re-anchors.
- **Lands c057 (findings, no firm artifact):** D3 the SweepAdaptive verdict (discharge-or-fold, OQ disposition); D4 the `fold_solve` thread-opener sketch (OQ + report, named batch-18 candidate).
- **Batch-17 meta-phase (fires after this finalize) picks up:** the batch-17 arc assessment (solver-test-load combinator yield: `solve_family` mined+propagated+lowered, `fold_solve` thread opened); the index-table-staleness drift-class root-cause fix (c055 D8 + c056 D2 OQs — RECOMMENDED lightweight promotion-time guard over finalize-sweep; the L1/L1-L0 next-audit candidate); the `gram-consuming-solver-postprocess-reduction` formal close (re-assessed genuinely solver-specific); the leaked-tool-tag producer-Write guard candidate; the solve_family LHS `rough-in`→firm lowering-verifier pass (KspSolver-statefulness / RHS-buffer-aliasing); the D3/D4 verdicts' batch-18 routing.
- **Batch-18 picks up:** `map_solve.md` authoring IF D3 discharges the 2-witness gate; the firm `fold_solve` landing IF D4's thread gets a 2nd fold-witness or downstream pull; the `weak_form_term` differential-operator cohort (when a 2nd pipeline needs a non-diffusion term); the L1/L1-L0 index-table-staleness audit.

## Open questions / caveats

- **D1 warrant call is genuinely open.** I have framed both outcomes as valid and given D1 the axis to judge on (does the L3 form carry distinct iteration-rotation content — the no-sequential-obstruction / parallel-decomposable characterization — beyond what the dissolution theme says?). My read leans toward WARRANTED (the "this family loop is the one solver-driver loop that lifts cleanly / is embarrassingly parallel" IS substantive iteration-rotation content, and it parallels the firm `L3/ksp_solve` which DOES carry the obstruction — the contrast is the L3 payoff). But the redirect's anti-mirror principle is exactly the thing D1 must weigh independently; I am NOT pre-deciding it. If D1 concludes NO-ENTRY, that is a clean valid outcome and the slice still coheres (D2 + the two probes still land).
- **D2 clean-gate.** I expect the flip to pass (all 3 LHS ops firm, libCEED themed, citation residual closed), but the clean-gate is real: if the `eliminate_essential_bc` deferred-config-then-apply two-step does NOT narrate cleanly as a firm L1>L0 lowering (the in-place `EliminateBC` mutation at parallel-assemble time is a candidate friction point), D2 keeps it rough-in and records the gate. That would itself be a useful batch-17-meta spine finding.
- **D3 strong prior toward FOLD.** The codemap peek (`offline_tol`, `convergence_memory`, `max_size_per_excitation`, `prom_indices`) strongly suggests SweepAdaptive builds a greedy reduced-order basis (a state-accumulating fold), which would NOT discharge the 2-witness gate and would instead feed D4's `fold_solve` thread. If so, the `map_solve` superset stays deferred into batch-18+ pending a genuine 2nd MAP witness, and the solver-spine's two combinator families (independent-MAP `solve_family` vs sequential-FOLD `fold_solve`/`time_step_fold`) are confirmed as the right factoring. Either D3 verdict is informative.
- **Index-table-staleness root-cause is NOT a c057 dispatch.** The c055 D8 + c056 D2 OQs (promotion-time-guard-vs-finalize-sweep; L1/L1-L0 next-audit) are correctly routed to the batch-17 meta-phase, not recruited here — they are methodology/tooling decisions (meta-phase territory), not forward-frontier content. NOTE for the meta-phase: D1 (if it promotes the L4>L3 dissolution forward-ref) and D2 (theme firm-flip) BOTH touch index status cells this cycle — they must update the matching index cell when they flip a `## Status` line (the recommended lightweight promotion-time guard, applied proactively in their scopes). If either misses it, that is the exact c055-class drift the meta-phase guard would catch — a useful empirical data point either way.
- **Cadence note:** this is the last cycle before the batch-17 meta-phase; the friction/priorities I read are at most ~3 cycles stale, but the c056 hand-off is fresh (post-cycle-056 finalize) so no stale-trigger gap surfaced this cycle.
