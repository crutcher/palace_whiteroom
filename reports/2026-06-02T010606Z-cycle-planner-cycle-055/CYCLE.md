---
agent: cycle-planner
invoked_at: 2026-06-02T010606Z
scope: cycle-055 dispatch plan
status: pending
---

# Cycle 055 dispatch plan

## Goals selected this cycle

Cycle-055 is the FIRST primary cycle of meta-batch-17 (055/056/057; the batch-17 meta-phase fires after c057's finalize). Under the 2026-06-01 VOCABULARY-SHIFT REDIRECT, the lead is the **`solve_family` propagation** — completing the headline batch-16 deliverable that landed as a single rough-in dep-map row in `book/src/L4/index.md` (c054 D1). Per replace-and-propagate, the combinator becomes the firm L4 entry and the two pipeline sweeps are **specialization notes UNDER the combinator** (not rectangular leaf chapters). Alongside the lead, this cycle advances the **FE-assembly continuation** — the BC-elimination siblings (clean-gated), the ratified libCEED-boundary obstruction annotation, and the lifter cleanup of the `fe-operator-assemble-mutation-rotation` theme. The `map_solve` superset probe is held to a strictly observation-first cross-cutter (fold-vs-map guard; transient is an untested potential fold) and does NOT author the superset combinator this cycle. The gram-consuming postprocess reduction stays deferred (solver-specific / below the clean-describability bar).

## Deliverable-presence verification

Per the MANDATORY pre-dispatch four-step procedure (paste-inline-evidence). Commands run from `book/src/`.

### D1 — `book/src/L4/solve_family.md` (full firm combinator entry) — OPEN BY CONSTRUCTION (propagation of the c054 rough-in row)
1. **File existence:** `ls L4/solve_family.md` → `ls: cannot access 'L4/solve_family.md': No such file or directory`. ABSENT. ✅
2. **Maturity:** N/A (file absent). The combinator currently exists ONLY as the `L4/index.md:76` rough-in dep-map row (verified present: `grep -n "solve_family" L4/index.md` → line 76, `` `solve_family` *(rough-in)* ``). Authoring the full entry flips that row firm + adds the chapter.
3. **OQ RESOLVED-grep:** `grep -iE "solve.family.*(RESOLVED|CLOSED)" scaffolding/open-questions.md` → no matches against `solve_family`/`solve-family-map-dissolution` (the `solve-family-combinator-confirmed-2-of-n-mine-now` OQ is "answered (action-half)" by the c054 MINE; the PROPAGATION — full entry + specializations + dissolution theme — is explicitly the open batch-17 lead per the c054 integrator-signal "Unblocked" + STAGING D1). NOT closed.
4. **Structural-block:** none. The mined shape, signature, layer-justification, and laws are firm in the c054 D1 report (the authoritative input). The firm vocabulary it consumes — `L4/ksp_solve.md`, `L4/iterate-while.md` — both present on disk (`ls` confirmed). Open by construction.

### D2 — the 2 specialization notes (electrostatic + magnetostatic) — authored WITHIN D1's entry (§Specializations), NOT separate chapters — OPEN BY CONSTRUCTION
- Slugs that would exist IF authored as separate chapters all ABSENT: `ls L4/electrostatic-solve-family.md L4/magnetostatic-solve-family.md L4/solve-family-electrostatic.md L4/solve-family-magnetostatic.md` → all `No such file or directory`; `ls L4/ | grep -i solve` → only `eigsolve.md`, `ksp_solve.md`, `ksp-solve-driver-dissolution.md` (no specialization files). The redirect (combinator-as-entry, leaves-as-notes) routes these as a **§Specializations section inside `solve_family.md`** (D1 owns), NOT 2 rectangular leaf chapters — so there is no separate dispatch. See `## Open questions / caveats` for the fork (if the content proves large, the integrator/c056 may split — flagged).

### D3 — `book/src/L4-L3/solve-family-map-dissolution.md` (the L4>L3 dissolution theme) — OPEN BY CONSTRUCTION
1. **File existence:** `ls L4-L3/solve-family-map-dissolution.md` → `No such file or directory`. ABSENT. ✅
2. **Maturity:** N/A. The `L4/index.md:76` rough-in row names this theme as `*(batch-17; pending)*` in its "Lowers to" cell. Authoring it is the propagation's L4>L3 half.
3. **OQ RESOLVED-grep:** `grep -iE "solve-family-map-dissolution.*(RESOLVED|CLOSED)"` → no matches. NOT closed.
4. **Structural-block:** none. The L3 lowering target is present + firm (`L3/ksp_solve.md` — the value-threaded outer-driver loop the per-member solve dissolves into; verified: `ksp_solve` L3 §Semantics renders the `iterate_while_L3` outer fold). The L4 LHS (`L4/solve_family` slug) is authored by D1 THIS cycle — forward-reference, canonical slug stated in BOTH scopes (see Discipline). Open by construction.

### D4 — `book/src/L1/eliminate_rhs.md` + `book/src/L1/eliminate_essential_bc.md` (FE BC-elimination siblings) — OPEN BY CONSTRUCTION (clean-gated)
1. **File existence:** `ls L1/eliminate_rhs.md L1/eliminate_essential_bc.md` → both `No such file or directory`. ABSENT. ✅
2. **Maturity:** currently `rough-in (no anchor yet)` plain-text rows in `L1/index.md:73-74` (verified: `grep` shows both as `*(rough-in; no anchor yet)*` bullets). Authoring promotes to firm L1.
3. **OQ RESOLVED-grep:** `grep -iE "eliminate.*(RESOLVED|CLOSED)"` → no closure; the `fe-assemble-bc-elimination-siblings-deferred` OQ is "rough-in harvester candidates" per c054 STAGING D2. NOT closed.
4. **Structural-block:** clean-gated, NOT structurally blocked. `eliminate_rhs` body is `apply_linop`+`axpy` (firm-spine vocabulary, c053 D3 judgment); both are separable post-compositions, NOT part of the `fe_assemble` fold. **Source anchor correction (codemap-verified this cycle):** `ParOperator::EliminateRHS` is DEFINED at `palace/linalg/rap.cpp:56` (search_text confirmed `void ParOperator::EliminateRHS(...)` at `:56`); the priorities `:215-217` is the `laplaceoperator.cpp` `SetEssentialTrueDofs` site for `eliminate_essential_bc` (the actual `EliminateRHS` call site is `laplaceoperator.cpp:252`, `PtAP_K->EliminateRHS(X, RHS)`). The producer localizes via codemap at dispatch; the def anchor `rap.cpp:56` is pre-supplied. CLEAN-GATE: if either does NOT lift cleanly in firm-spine vocabulary, it is a SPINE FINDING, not a forced land (per the redirect).

### D5 — libCEED-boundary obstruction annotation — OPEN BY CONSTRUCTION (ratified enactment)
1. **File existence:** a new thin `L1-L0/` (or note) annotation; the ratified target citations `bilinearform.cpp:67-70` (leaf kernel) + `libceed/operator.cpp:455-490` (COO→CSR) are NOT yet annotated as `obstruction (opaque-library-ownership)` anywhere. (The existing `fe-operator-assemble-mutation-rotation.md` theme references `bilinearform.cpp` but does NOT carry the ratified obstruction sub-kind classification.)
2. **Maturity:** N/A (new annotation). The classification was RATIFIED by the batch-16 meta-phase (enactment-3) but "meta-phase does not write `book/`" — this is the migrated enactment.
3. **OQ RESOLVED-grep:** the `fe-assemble-libceed-boundary-classification` OQ is "needs-more → routed to the batch-16 meta-phase" which RATIFIED it (GO); the enactment (the `book/` annotation) is OPEN.
4. **Structural-block:** none — this is the ratified enactment, open by construction. `fe_assemble` stays FIRM (the fold quantifies over the per-term `A` opaquely; firmness independent of this classification per enactment-3).

### D6 — lifter cleanup of `book/src/L1-L0/fe-operator-assemble-mutation-rotation.md` — VERIFIED-STALE (open)
1. **File existence:** `ls L1-L0/fe-operator-assemble-mutation-rotation.md` → present. ✅
2. **Maturity / stale-state:** `grep -n "73-75\|93-95\|:77\|:97" L1-L0/fe-operator-assemble-mutation-rotation.md` → the theme body STILL carries the +2-drift `bilinearform.cpp:73-75` (line 84, line 134, line 162) + `:93-95` (line 85, line 162) `AddSubOperator` anchors — should be `:77`/`:97` (the correct `AddSubOperator` lines; c054 D2 corrected the DEP-MAP row anchors but, per dispatch discipline, did NOT edit this theme body in-place — routed the fix to a batch-17 lifter). The LHS placeholder language also predates the now-firm `fe_assemble`. VERIFIED-STALE on disk — the re-anchor is real work, not a no-op.
3. **OQ RESOLVED-grep:** `fe-assemble-theme-addsuboperator-citation-drift` + `fe-assemble-theme-reanchor-to-firm-lhs` are both "needs-more (propose-only deferral)" per c054 signals — OPEN, routed to this cycle's lifter.
4. **Structural-block:** none. `fe_assemble` is firm (the re-anchor target exists). Open.

### D7 — `layer-intro-author` count-owner + L1/index FE-cohort header reword — open by construction (consolidation)
- Touches `L4/index.md` consolidated firm-count tally + cohort prose (D1 flips the `solve_family` row; D2 specializations are notes-in-entry; the tally is shared mutable derived value) AND `L1/index.md:70` FE-cohort subsection header (1-firm `fe_assemble` + 2-rough-in `eliminate_*` → will be 1-firm + 2-firm if D4 lands both). Open by construction (consolidation owner; see count-ownership partition).

**All seven dispatches: ALL FOUR checks pass (or open-by-construction with explicit justification). None on the STOP-PROPOSING NEGATIVE LIST** (`lu_solve`, `back_solve`, `ls-update-column`, 4 NLEPS atoms, `apply_nonlinear_pencil` HELD — none match any scope here). Framing: the lead is a harvester/abstractor propagation (correct — the combinator is already mined; this is the replace-and-propagate enactment); the `map_solve` superset is correctly framed AUDIT-FIRST (cross-cutter, observation-only, fold-vs-map-guarded — NOT a combinator-miner authoring the superset).

## Dispatches

1. **(agent: `harvester`)** — scope: **L4 operator: full firm `book/src/L4/solve_family.md` entry + the 2 specialization NOTES (electrostatic + magnetostatic) as a §Specializations section WITHIN the entry.** Author the firm combinator chapter from the c054 D1 report (`reports/2026-06-02T002600Z-combinator-miner-solve-family-combinator/CYCLE.md` — the authoritative mined shape/signature/laws/layer-justification). The entry: the fixed-operator map-over-RHS-family combinator, `solve_family :: OpParams -> [Inputs] -> [SimState]`, the pure-`map` degenerate of the strawman §3.7 `iterate_while` family; the load-bearing **concatenation-homomorphism law** `solve_family op (a ++ b) = solve_family op a ++ solve_family op b` (mark `rough-in (test-coverage-bounded)` per the c054 caveat — the drivers are integration-level, no dedicated test); the **operator-capture-once / `SetOperators`-hoist** identity; element-independence/order-preservation; the `solve_family op [] = []` degenerate. The **§Specializations** section names the electrostatic terminal-boundary sweep (`electrostaticsolver.cpp:30`/`:35-36`/`:60-90`/`:68-69`) and the magnetostatic surface-current sweep (`magnetostaticsolver.cpp:30`/`:35-36`/`:66-100`/`:76-77`) as notes-under-the-combinator (re-expressing THROUGH `solve_family`, NOT re-deriving — replace-and-propagate). Variant axes: operator-capture (`fixed` = this combinator; `per-element` = the batch-17 superset), family-index-domain, element-type, collection-shape. **Flip the `L4/index.md:76` rough-in dep-map row → firm** (modifying the EXISTING row — D1 owns this row) + add the `SUMMARY.md` chapter entry. **DEFER the `L4/index.md` consolidated firm-count tally + the §Vocabulary-cohort narrative count-prose to D7** (count-owner). Forward-references D3's theme `book/src/L4-L3/solve-family-map-dissolution.md` (canonical slug — D3 authors it this cycle) in the row's "Lowers to" cell as the now-being-authored L4>L3 theme. deps: none.
   - rationale: THE LEAD. Completes the headline batch-16 combinator per replace-and-propagate. fan-out: HIGH (the shared solve-driver spine across the electrostatic+magnetostatic pipelines; the combinator-as-entry the redirect mandates). Serves priorities active-head #1 (`solve-family-propagation`).

2. **(agent: `abstractor`)** — scope: **L4>L3 theme: `book/src/L4-L3/solve-family-map-dissolution.md`** (canonical slug — stated here AND in D1's scope). Narrate forward (high→low) how the L4 `solve_family` `map` dissolves into the L3 explicit `std::vector<Vector>`-accumulating outer loop: the `map (\inp -> ksp_solve op inp) rhss` collapses to a positional accumulating `for` over the index family, with the **shared-operator capture (`SetOperators(*K,*K)` / `fresh_ksp op`) hoisted OUTSIDE the loop** (the structural payoff of operator-capture-once), each iteration calling the L3 [`ksp_solve`](../L3/ksp_solve.md) value-threaded driver into a pre-sized collection slot. Compose strictly ABOVE the per-solve `ksp-solve-driver-dissolution` (the inner driver-half) — `solve-family-map-dissolution` is the outer map-shell dissolution. LHS = L4 `solve_family` (D1's entry, forward-referenced), RHS = the L3 outer accumulating loop wrapping per-member L3 `ksp_solve`. Add the `L4-L3/index.md` dep-map ROW + its own §Vocabulary-cohort BULLET + the `SUMMARY.md` line; **DEFER the `L4-L3/index.md` consolidated firm-count tally to D7**. deps: 1 (forward-references D1's `L4/solve_family.md` LHS — wave-2 ordering so the per-report integrator can wire the live link; D1's entry lands first).
   - rationale: the L4>L3 half of the lead propagation. fan-out: HIGH (the dissolution that makes the combinator lowerable; completes the L4>L3 edge for the new combinator). Serves active-head #1.

3. **(agent: `harvester`)** — scope: **L1 operator: `book/src/L1/eliminate_rhs.md`** (firm promote, clean-gated). The inhomogeneous-Dirichlet-data lift into the RHS: `ParOperator::EliminateRHS` DEFINED at `palace/linalg/rap.cpp:56` (codemap-verified this cycle; body `:56-82`), call site `palace/models/laplaceoperator.cpp:252` (`PtAP_K->EliminateRHS(X, RHS)`). Body is `apply_linop`+`axpy` (firm-spine vocabulary, c053 D3); a **separable post-composition** on the assembled operator, NOT part of the `fe_assemble` fold. Flip the `L1/index.md:74` rough-in bullet → firm (contiguous-line replacement, NOT append; D7 owns the cohort-header narrative) + add the `SUMMARY.md` entry + the `L1-L0/` lowering forward-reference (plain-text if the L1>L0 theme is absent). **CLEAN-GATE:** if the body does NOT lift cleanly in firm-spine vocabulary, author as a SPINE FINDING (record the obstruction), do NOT force the land. deps: none.
   - rationale: FE-assembly continuation; the BC-elimination sibling. fan-out: MEDIUM (the assembly half of every pipeline). Serves active-head #2 (`fe-assembly-thread-continuation`).

4. **(agent: `harvester`)** — scope: **L1 operator: `book/src/L1/eliminate_essential_bc.md`** (firm promote, clean-gated). Pin essential (Dirichlet) dofs into the assembled operator: L0 `ParOperator::SetEssentialTrueDofs` / `EliminateBC`, `palace/models/laplaceoperator.cpp:215-217` (priorities anchor; producer confirms exact lines via codemap — `EliminateBC` call sites cluster at `rap.cpp:143`, `modeeigensolver.cpp:571-611`, codemap-confirmed this cycle). A separable post-composition (the diagonal-policy `DIAG_ONE`/`DIAG_ZERO` variant axis), NOT part of the `fe_assemble` fold. Flip the `L1/index.md:73` rough-in bullet → firm (contiguous replacement; D7 owns the cohort header) + `SUMMARY.md` entry + L1>L0 forward-reference (plain-text). **CLEAN-GATE** as D3. deps: none.
   - rationale: FE-assembly continuation; the second BC-elimination sibling. fan-out: MEDIUM. Serves active-head #2.

5. **(agent: `abstractor`)** — scope: **libCEED-boundary obstruction annotation** — author the thin `obstruction (opaque-library-ownership)` theme/note RATIFIED by the batch-16 meta-phase (enactment-3): the innermost element-local quadrature kernel (`integ->Assemble`-built `CeedOperator`, `palace/fem/bilinearform.cpp:67-70`) + the COO→CSR `CeedOperatorFullAssemble` (`palace/fem/libceed/operator.cpp:455-490`) is library-owned — a DEEPER-boundary sibling of the HYPRE/SLEPc `opaque-library-ownership` precedents (Palace OWNS the orchestration: the integrator-fold `AddSubOperator` at `bilinearform.cpp:77`, the PA/FA dispatch, BC-elimination; only the leaf kernel is library-owned). State explicitly that `fe_assemble` stays FIRM (the fold quantifies over the per-term `A` opaquely; firmness independent of this classification). Use the mandatory `## Status: obstruction (opaque-library-ownership)` sub-kind tag (per CLAUDE.md obstruction-sub-kind invariant). Cite the HYPRE/SLEPc precedents (`triangular-solve-obstruction.md`) for the routing parallel. deps: none.
   - rationale: ratified enactment; documents the FE boundary so future producers don't re-localize. fan-out: MEDIUM (documents the assembly boundary for all 5 pipelines). Serves active-head #2 (libCEED-boundary annotation).

6. **(agent: `lifter`)** — scope: **theme cleanup: `book/src/L1-L0/fe-operator-assemble-mutation-rotation.md`** re-anchor. (i) Fix the +2-drift `AddSubOperator` body citations `bilinearform.cpp:73-75` → `:77` (domain) and `:93-95` → `:97` (boundary) at the verified-stale sites (lines 84-85, 134, 162); (ii) re-anchor the LHS placeholder language to the now-firm `fe_assemble` (the theme predates the firm L1 operator — point its LHS at `book/src/L1/fe_assemble.md`). NO body re-authoring beyond the mechanical re-anchor + citation fix (lifter discipline). deps: none.
   - rationale: the propose-only citation-drift deferral c054 D2 routed (a producer must not edit another artifact in-place mid-dispatch). fan-out: LOW (citation hygiene + LHS re-anchor on the FE thread). Serves active-head #2 (lifter cleanup).

7. **(agent: `layer-intro-author`)** — scope: **count-ownership + L1/index FE-cohort header reword (SOLE consolidated-tally owner this cycle).** (a) `L4/index.md`: write the consolidated firm-count tally bump (L4 firm 6→7 for the now-firm `solve_family`; the L4>L3 firm 6→7 for D2's dissolution theme; outer-driver-rows tally as appropriate) + the §Vocabulary-cohort narrative for `solve_family` (the combinator-as-entry framing; the 2 specializations as notes-under-it) + flip the "substantially complete / near-exhausted" L4-frontier prose to reflect the new combinator landing (the solver-test-load is generating genuine new spine vocabulary). (b) `L4-L3/index.md`: the consolidated L4>L3 tally (D2 defers to here). (c) `L1/index.md:70`: reword the FE-cohort subsection header from "1 firm + 2 rough-in" to the post-D3/D4 mix (1-firm `fe_assemble` + 2-firm `eliminate_*` IF both land; if a clean-gate fires and one stays a finding, reflect the actual landed mix). D1/D2/D3/D4 each add ONLY their own row/bullet/SUMMARY and DEFER all consolidated tallies + frontier-prose to D7. deps: 1, 2, 3, 4, 5 (reads their landed rows to write accurate consolidated counts; wave-3).
   - rationale: count-ownership convention (≥2 dispatches co-write `L4/index.md` + `L4-L3/index.md`); the FE-cohort header reword. fan-out: LOW (Part-overview consolidation; closes the cohort narrative). Serves active-head #1 + #2.

**NOT dispatched this cycle (held / deferred with rationale):**
- **`map_solve` superset probe (active-head #3)** — HELD as observation-only; NOT authored this cycle. The superset `map_solve_over_(operator,rhs)_family` has <2 operator-VARYING witnesses (driven is 1; transient untested) AND the fold-vs-map guard means transient may be a `fold` (state threaded step→step), NOT a `map` — authoring the superset now would over-unify a fold into a map. Per the c054 caveat + the `disciplined-cross-pipeline-combinator-mining-gate` skill, the superset is gated on a transient/eigenmode shape-confirmation probe. **Sequenced to c056** as an observation-first `cross-layer-cross-cutter` probe (cite the promoted skill; probe transient FIRST for fold-vs-map). Not dispatched c055 to keep the lead the focus and because the probe is a distinct observation, not a propagation step.
- **`gram-consuming-solver-postprocess-reduction` (active-head #4)** — DEFERRED. Below the clean-describability bar (capacitance/inductance-matrix sign-remix + in-place `Invert()` is likely too solver-specific to lift as shared spine vocabulary); per the redirect, a solver advances the spine only when cleanly describable. Re-assess at the batch-17 meta-phase or c056/c057 if cycle budget and the clean bar are met.

## Overlap analysis

Pairwise (genuinely-overlapping = same operator entry modified OR same theme body rewritten OR same index consolidated-tally written by ≥2):

- **D1 × D2**: D2 forward-references D1's `L4/solve_family.md` LHS (the dissolution theme's LHS). Distinct files (D1 = `L4/solve_family.md` + flips the `L4/index.md:76` row; D2 = new `L4-L3/solve-family-map-dissolution.md` + adds an `L4-L3/index.md` row + an `L4/index.md`... NO — D2 does NOT touch `L4/index.md`; the `solve_family` row's "Lowers to" cell is D1's, naming D2's slug). **Forward-reference ordering** (D2 after D1) so the per-report integrator wires the live link to D1's landed entry. Not artifact-overlapping (distinct files); SEQUENTIAL by forward-reference only.
- **D1 × D7**: BOTH would touch `L4/index.md` — D1 flips its OWN `solve_family` dep-map ROW (line 76) firm; D7 writes the CONSOLIDATED firm-count tally + §Vocabulary-cohort narrative. Distinct regions (a single dep-map row vs the cohort-count prose). Per count-ownership: D1 owns ONLY its row + SUMMARY; D7 SOLE-owns the consolidated tally + narrative. D1 DEFERS the tally to D7. SEQUENTIAL (D7 after D1, reads the landed row).
- **D2 × D7**: BOTH would touch `L4-L3/index.md` — D2 adds its OWN dep-map row + §Vocabulary-cohort bullet (anchor-distinct, parallel-safe per the dual-registration convention); D7 SOLE-owns the consolidated L4>L3 tally. D2 DEFERS the tally to D7. SEQUENTIAL (D7 after D2).
- **D3 × D4**: BOTH flip a rough-in bullet in `L1/index.md` (D3 line 74 `eliminate_rhs`; D4 line 73 `eliminate_essential_bc`) — ADJACENT but anchor-distinct bullets (distinct slug-text, contiguous lines). Per the dual-registration / distinct-row convention these are parallel-safe (each owns its own bullet); but the FE-cohort SUBSECTION HEADER reword (line 70, the consolidated "N firm + M rough-in" count) is D7's SOLE responsibility. D3/D4 each flip ONLY their own bullet + add their own SUMMARY entry; DEFER the cohort-header count to D7. PARALLEL (distinct bullets); D7 reconciles the header after both land.
- **D3/D4 × D7**: D7 rewords the `L1/index.md:70` cohort header (consolidated count) after D3/D4 land their bullets. SEQUENTIAL (D7 after D3/D4).
- **D5 × all**: D5 authors a NEW thin obstruction theme/note (libCEED boundary) — distinct file, no shared region with D1/D2/D3/D4/D6. The `fe-operator-assemble-mutation-rotation.md` theme D6 re-anchors is a DIFFERENT file from D5's new annotation; D5 may CITE the FE-assembly thread but does not edit D6's file. PARALLEL with everything except D7 (D7 may reference D5's landed annotation in the L1-index FE-cohort narrative — minor; D7 after D5).
- **D6 × all**: D6 re-anchors `L1-L0/fe-operator-assemble-mutation-rotation.md` (a theme body) — distinct file from D1/D2/D3/D4/D5/D7. The c054 D2 already corrected the `L1-L0/index.md` DEP-MAP row anchors; D6 fixes the THEME BODY (different file region). No overlap. PARALLEL.
- **D5 × D6**: both on the FE thread but distinct files (D5 = new libCEED annotation; D6 = the existing mutation-rotation theme). PARALLEL.

## Sequencing schedule

- **Wave 1 (parallel):** D1 (`solve_family.md` entry + row-flip), D3 (`eliminate_rhs`), D4 (`eliminate_essential_bc`), D5 (libCEED annotation), D6 (lifter theme cleanup). Five disjoint-file dispatches (D3/D4 adjacent-bullet, parallel-safe per dual-registration). No forward-reference among them.
- **Wave 2 (after D1 lands):** D2 (`solve-family-map-dissolution` theme) — forward-references D1's landed `L4/solve_family.md` LHS so the per-report integrator wires a live link.
- **Wave 3 (after D1, D2, D3, D4, D5 land):** D7 (count-owner + FE-cohort-header reword) — reads all landed rows/bullets/annotation to write accurate consolidated tallies (`L4/index.md`, `L4-L3/index.md`) + the `L1/index.md:70` header.

(Reminder: waves order DISPATCHES by forward-reference dependency only; the book is NOT rebuilt between waves, and there is exactly ONE `integrator-finalize` at cycle end.)

## Open questions / caveats

- **FORK on the 2 specializations (electrostatic + magnetostatic): §Specializations-section-within-`solve_family.md` (PLANNED) vs 2 separate leaf chapters.** I have planned them as a **§Specializations section INSIDE D1's `solve_family.md` entry** (notes-under-the-combinator), which is the cleanest reading of the 2026-06-01 redirect (combinator-as-entry, leaves-as-specialization-notes — NOT rectangular leaf chapters; the redirect explicitly retired the rectangular-floor / same-named-leaf-per-layer pattern). The priorities active-head #1 and the c054 report phrase them as "2 specialization entries," which COULD be read as 2 separate chapters. I judge the §Specializations-section reading correct under the redirect, and it keeps the dispatch lean (one harvester, not three). **If the specialization content proves large** (per-pipeline index-domain + RHS-construction + post-processing specifics each warranting a full chapter), the integrator MAY split them into 2 chapters at apply-time, OR c056 may author them as separate specialization chapters — flagged for the producer to size-judge and for the integrator to escalate if D1's entry balloons. This is NOT a blocker; it is a representation choice the redirect tilts toward notes-in-entry.
- **`solve_family` row-flip count interaction.** D1 flips the EXISTING `L4/index.md:76` rough-in row to firm (L4_firm 6→7 in the c054 counts_after). D7 owns the consolidated tally. The L4 §Vocabulary-cohort header currently reads "Firm at L4 (6 + 4 outer-driver)" and "substantially complete / near-exhausted" — D7 must update BOTH the count AND the "near-exhausted" framing (the solver-test-load is now adding genuine new spine vocabulary, so "near-exhausted" is stale). Flagged for D7.
- **Clean-gate on D3/D4 may produce a SPINE FINDING instead of a land.** If `eliminate_rhs` / `eliminate_essential_bc` do NOT lift cleanly in firm-spine vocabulary (e.g. the `EliminateBC` diagonal-policy interacts with the assembled operator in a way that resists clean post-composition), the harvester records the obstruction as a spine finding and does NOT force the land. D7's FE-cohort-header reword must reflect the ACTUAL landed mix (could be 1-firm + 1-firm + 1-finding, etc.). This is the redirect's "what a solver can't cleanly say is a spine finding" applied.
- **Source-anchor drift in the priorities line (corrected this cycle).** The priorities active-head #2 cited `eliminate_rhs` as `laplaceoperator.cpp:215-217`; codemap this cycle shows `:215-217` is the `SetEssentialTrueDofs` (essential-BC) site, while `EliminateRHS` is DEFINED at `rap.cpp:56` and called at `laplaceoperator.cpp:252`. The dispatch scopes carry the corrected anchors; the producers localize via codemap at dispatch (the def anchors are pre-supplied to avoid the localization loop). Noted for the batch-17 meta-phase to optionally re-pin the priorities line.
- **`map_solve` superset + `gram` postprocess deferred to c056/c057** (see "NOT dispatched" above). If the batch-17 meta-phase wants the superset probe sooner, it can re-rank — but the fold-vs-map guard makes the observation-first c056 probe the disciplined sequence.
