---
verifies: ../CYCLE.md
critiqued_at: 2026-06-04T060000Z
critic_version: 1
checks:
  citation-validity: pass
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: pass
  cross-reference-integrity: pass
  edge-label-fidelity: pass
  plan-kind-consistency: pass
  skill-uptake-survey: pass
overall_status: ready
---

# META: verification of the cycle-091 D3 reduce-verb re-judgment (domain_energy_reduce → firm; gram_reduce stays rough-in)

## Critique

### Checks run

**citation-validity — pass.** The load-bearing test claim is the firm flip's foundation, so I verified it independently rather than trusting the report. `citecheck --anchor` confirms all four load-bearing pinpoints: `postoperator.cpp:1021-1099` (anchor `MeasureDomainFieldEnergy` `[ok]`), `domainpostoperator.cpp:262-274` `[ok]`, `test-domainpostoperator.cpp:83-93` `[ok]`, and `test-domainpostoperator.cpp:83` (anchor `GetElectricFieldEnergy` `[ok]`). I read `test-domainpostoperator.cpp:75-95` directly: `:83` calls `dom_post_op.GetElectricFieldEnergy(*E_field)` (the WHOLE-domain total) and `:90-93` asserts `U = ½·ε₀·E₀²·sx·sy·sz` via `WithinRel(..., 0.01)` — exactly as D3 claims, the whole-domain energy FORM, NOT a per-domain call. I independently grepped all of `reference/palace/test/unit/` for `GetDomainElectricFieldEnergy`/`GetDomainMagneticFieldEnergy`: **zero matches** (exit 1). D3's claim that NO unit test exercises the per-domain path is confirmed — so gate (b) is genuinely "supporting-not-firming," not over-claimed. I also read `domainpostoperator.cpp:262-274` and confirmed the energy form is `return 0.5 * dot` with `dot = LocalDot(E.Real(), M·E.Real())` (+ imaginary contribution) — the matrix-weighted-norm-SQUARED radicand `½⟨field, M field⟩` with NO `√` (load-bearing; see surface-or-evidence). The `verified_against:` YAML block (§2e) round-trips under `yaml.safe_load` (5 entries; every `note:` begins with prose, no leading-quote hazard).

**surface-or-evidence — pass (the load-bearing check; firm-on-positive-structure escape is genuinely applicable).** This is the consequential judgment and I weighted it most heavily. The escape turns on whether `domain_energy_reduce`'s OWN map laws are syntactic read-off identities over firm constituents (like c082/c083/c086) or carry inner-product-axiom content the escape does not cover (the c080 matrix-weighted-norm ruling). I confirm the former. The decisive subtlety: the c080 audit ruled the escape OUT for matrix-weighted-norm *itself* because its norm-axiom laws (triangle/Cauchy–Schwarz/parallelogram) carry genuine inner-product-structure content. But `domain_energy_reduce` does NOT invoke those axioms — it folds the matrix-weighted-norm-**SQUARED radicand** `⟨field, M field⟩` (which is `apply_linop` + `dot`, both firm), confirmed at `domainpostoperator.cpp:274` as `0.5 * dot` with no `√`. The verb never routes through the √-overload `Norml2(comm,x,B,Bx)` that was matrix-weighted-norm's lone residual gate. Both folded primitives are genuinely firm: `participation_ratio.md:4` is `firmness: firm` on disk (verified), and `matrix-weighted-norm` is flipped firm by D1 this same cycle (the predicate — see Issues #1). The five map laws (§Algebraic laws) are each read-off identities over the positive `MeasureDomainFieldEnergy` loop and the two firm halves, carrying no inner-product-axiom content of their own. The record-definition sub-check is satisfied: `DomainOpMap` (the signature-named input record) has an in-chapter `## Record definition` section (`:223-251`, fields + types + stratum + L0 home); the `DomainData` output row is correctly cross-referenced to `energy-fields.L4.md` with OQ `record-DomainData-needs-definition-home` flagged. No record gap.

**rotation-quality — pass.** Not a layer rotation report per se (a maturity re-judgment), but the structural-analogy claim is the rotation-equivalent load-bearing assertion. I verified `eigenfreq_qfactor_reduce.md:4` is `firmness: firm` and read its §Status (`:183+`): it firmed because BOTH folded primitives are firm L1 (eigenvalue-untransform c080 + participation_ratio c077) and "the assembly is bare scalar arithmetic composing two firm halves... carries no axiom requiring an unverified mathematical property." `domain_energy_reduce` is genuinely structurally analogous — same rank-1 scalar-table fold, two firm halves, bare scalar assembly. The analogy holds. Notably, `eigenfreq_qfactor_reduce`'s own §Status names "the SIBLING c080 matrix-weighted-norm audit, which RULED OUT the escape" as its contrast — and D3's argument correctly resolves that contrast: the ruling-out applied to the √-norm itself, not to the squared energy form this verb folds, and is moreover discharged once matrix-weighted-norm firms at its own home.

**variant-axis-coverage — pass.** The verb's variant axes (field-kind electric/magnetic, element-type complex, partition-coverage) are each handled in the chapter: field-kind is the load-bearing axis (the reduction runs twice, `postoperator.cpp:1036-1042` / `:1061-1066`), element-type is the real+imaginary radicand sum (`domainpostoperator.cpp:267-272`), and partition-coverage is correctly scoped as config-conditional (gates `Σ pᵢ = 1`, NOT the verb shape or maturity). No hidden branch. The inconsistent C++ participation guard (electric numerator-guard `:1039` vs magnetic denominator-guard `:1064`) is explicitly resolved to the uniform denominator guard with rationale (`domain_energy_reduce.md:182-199`) — a real axis, handled, not hidden.

**cross-reference-integrity — pass.** All L4/index `old_string` anchors match the on-disk (pre-D3) state exactly once each: `:98` Status-cell, `:102` Folds-cell, `:32` firm header, `:57` rough-in header, and the `:49` firm-bullet insertion anchor (each `grep -c` = 1). The `:98` same-line coordination is sound: D1 edits the Folds cell, D3 edits the Status cell of the same dep-map row — distinct non-overlapping text spans, both presently exact-match, and the serialized D1→D3 integration is explicitly flagged in §Coordination note + §3d. No collision. Count arithmetic verified against disk: `:32` is `Firm at L4 (17 + 4)`, `:57` is `Rough-in at L4 (1)` — D3's deltas (firm 17→18, rough-in 1→0) are arithmetically correct and the cohort-empty claim holds. gram_reduce is `rough-in (test-coverage-bounded)` on disk and D3 leaves it there; bilinear-form is `firmness: rough-in` on disk and D3 does not touch it (confirmed). All chapter cross-references (eigenfreq_qfactor_reduce, participation_ratio, matrix-weighted-norm, energy-fields.L4) resolve.

**edge-label-fidelity — pass.** No L_{n+1}→L_n edge label is asserted as the report's subject; the "Lowers to" prose is identity-in-form L4→L1 and the narration matches. No mismatch.

**plan-kind-consistency — pass.** The report is an `audit` (lowering-verifier law-confidence pass) that enacts one promotion + one hold. Content shape matches: per-citation audit, applicability conditions, algebraic-law table, escape warrant, and a complete proposed-changes block. The `domain_energy_reduce` chapter post-flip is genuine firm apparatus (signature, semantics, 5 laws, record definition, evidence) — not a rough-in placeholder mislabeled firm. The two verdicts are internally consistent with the maturity tiers (firm vs `rough-in (test-coverage-bounded)`).

**skill-uptake-survey — pass.** The report references `citecheck --anchor` invocation for the load-bearing pinpoints (the citation-validity skill-shape), and the `verified_against:` block is the lowering-verifier audit convention. Appropriate skill uptake for an audit dispatch.

### Issues found

**Issue #1 (informational, NOT a defect — the load-bearing dependency, surfaced for the integrator): the firm flip is predicated on D1's matrix-weighted-norm flip, which is NOT yet on disk.** `book/src/L4/domain_energy_reduce.md:206` and `book/src/L1/matrix-weighted-norm.md:110` both still read `rough-in (test-coverage-bounded)` on disk; matrix-weighted-norm.md `:115` still calls the firm flip a "recommended batch-29 LEAD" that "LICENSES — but does not itself enact" the flip, and `:143` still states the escape "does not apply" to the √-norm. This is **correct and expected**, not a defect: D1 (`reports/2026-06-04T053300Z-harvester-cycle-091-matrix-weighted-norm-firm-flip/CYCLE.md`) is a concurrent same-cycle dispatch whose firm flip is a proposed-changes block the integrator applies serially BEFORE D3 (per-report integration order D1→D2→D3→D4). I verified D1's report does propose the matrix-weighted-norm firm flip (its §1 edit to `:110`). D3's entire firm warrant for `domain_energy_reduce` rests on that flip landing. **Severity: none if integration order is honored; HARD-BLOCKING if D1 fails to integrate or is rejected.** The integrator MUST apply D1 before D3, and if D1's matrix-weighted-norm flip does not land (e.g. D1 is rejected), D3's `domain_energy_reduce` flip is unwarranted (it would fold a still-rough-in primitive) and must be held. Where: `CYCLE.md` §Coordination note + §2 + §3 (correctly flagged by the report; recorded here for the integrator's apply-order gate). The repairer should leave this as an integrator-coordination flag, not a content fix — the report's reasoning is sound *conditional on D1*, and that condition is the designed intra-cycle dependency.

**Issue #2 (trivial, cosmetic): off-by-one in the verified_against append locator.** §2e says "append at end of file (after `:378`)" but `domain_energy_reduce.md` is 377 lines on disk. The append target is end-of-file regardless, so this is non-load-bearing prose drift, not a placement error. Where: `CYCLE.md` §2e. No functional impact.

### Verdict on the domain_energy_reduce firm promotion

**Sound, not over-claimed** — conditional on D1's matrix-weighted-norm firm flip integrating first (the designed serial dependency). The firm-on-positive-structure escape is genuinely applicable: both folded primitives are firm (participation_ratio firm on disk; matrix-weighted-norm firm via D1 this cycle), the verb folds the matrix-weighted-norm-SQUARED radicand `½⟨field, M field⟩` (`apply_linop`+`dot`, never the √-overload that was the residual gate), every map law is a read-off syntactic identity over two firm halves with no inner-product-axiom content, and the independently-verified absence of any per-domain unit test confirms gate (b) is supporting-not-firming. The `eigenfreq_qfactor_reduce` c082 analogy holds structurally. The c080 ruling-out does not apply because it targeted the √-norm's own inner-product axioms, which this verb does not invoke. gram_reduce is correctly left `rough-in` on its genuine bilinear-form residual gate (verified rough-in on disk, untouched). All 8 checks pass.
