---
verifies: ../CYCLE.md
critiqued_at: 2026-05-29T09:49:02Z
critic_version: 1
checks:
  citation-validity: pass
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: pass
  cross-reference-integrity: fail
  edge-label-fidelity: pass
  plan-kind-consistency: pass
  skill-uptake-survey: pass
repaired_at: 2026-05-29T09:57:16Z
repairer_version: 1
repairs:
  citation-validity: repaired
  surface-or-evidence: not-needed
  rotation-quality: not-needed
  variant-axis-coverage: not-needed
  cross-reference-integrity: repaired
  edge-label-fidelity: not-needed
  plan-kind-consistency: not-needed
  skill-uptake-survey: not-needed
overall_status: ready
follow_up_agent: null
---

# META: verification of "Formalize eigsolve at L2"

## Critique

### Checks run

**citation-validity — pass.** Ran `tools/citecheck/citecheck.py --scan` on the report: **50 citations checked, 0 failing, all ranges in bounds.** Independently spot-verified every load-bearing anchor via codemap `read_range`/`awk` against `reference/palace/`, line-exact:
- ARPACK `ApplyOp` (`arpack.cpp:562-590`): the shift-invert branch `opM->Mult(x1,z1); opInv->Mult(z1,y1); y1 *= gamma` is at :579-581 (report cites :579-581 ✓; law-1 also cites :578-581, where :578 is the `else {` — harmless superset); no-transform branch `opK->Mult; opInv->Mult; *= 1.0/gamma` at :573-575 ✓; divfree `opProj->Mult(y1)` at :586 ✓; host-pointer comment + `x1.Set(px,n,false)` at :569-570 ✓.
- SLEPc shell triple: `__mat_apply_EPS_A0` (`opK->Mult` + `*= delta`) :1801-1814, `*= δ` at :1810 ✓; `__mat_apply_EPS_A1` (`opM->Mult` + `*= delta*gamma`) :1816-1827, `*= δγ` at :1825 ✓; `__pc_apply_EPS` :1847-1877 with inner solve `ctx->opInv->Mult` at :1858 ✓, no-transform un-scale `*= 1.0/(delta*gamma)` at :1861 ✓, shift-invert un-scale `*= 1.0/delta` at :1865 ✓, divfree at :1870 ✓.
- Setup/wiring: `SetShiftInvert` STPRECOND :384 / STSINVERT :388 / shell :391 / `sigma=s` :392 ✓; `SetLinearSolver opInv=&ksp` slepc :366, arpack :193 ✓; `Customize` `EPSSetTarget(eps, sigma/gamma)` :674 ✓; `GetEigenvalue return l*gamma` :715 ✓; construction `modeeigensolver.cpp:1030-1053` with KRYLOVSCHUR :1045, `SetLinearSolver(*ksp)` arpack :1037 / slepc :1050 ✓; driver `SetShiftInvert(1i*target)` :285 / `(target*target)` :305 ✓, `SetBMat(*KM)` :213/:419 ✓; opaque loops `EPSSolve(eps)` :694 / ARPACK RCI `naupd` :318 (range :263-358) ✓; PEP block `arpack.cpp:733-799` with `opInv->Mult` at :761,:778 ✓. The producer's self-caught 4 emit drifts are corrected in the final text — all re-verified anchors land exact. citecheck also confirmed `slepc.cpp:2170-2202` (NEP, OQ #5) in bounds.

**surface-or-evidence — pass.** Refinement-shaped? No — this is a `new:` L2 operator entry (new surface), not a modification of an existing operator/theme, so the "surface AND rotation_claim, or retroactive-evidence" gate applies in its new-surface reading: the entry introduces L2 surface AND backs the rotation with positive evidence. The FIRM-vs-stub judgment is the crux and it is **sound**. The named composition `apply_shift_invert = apply_linop(M) ▷ ksp_solve((K−σM)⁻¹)` is read line-for-line off Palace-owned source (`ArpackEPSSolver::ApplyOp`, verified at `arpack.cpp:579-581`), expressed entirely in firm L2 vocabulary (firm L2 `ksp_solve` + firm L1 `apply_linop`), and corroborated at a second assembly site (SLEPc ST-shell, `slepc.cpp:1847-1877` + `:1801-1827`). The opaque eigen-iteration loop is correctly held out as upward context (the fold-not-opened non-law + the L3 partial-obstruction prediction) and is NOT smuggled into the L2 entry as content — §Semantics phase 2 and the "Laws that do not hold" list both name it by role only with negative anchors (`slepc.cpp:694`, `arpack.cpp:263-358`). The firm basis matches the L1 eigsolve entry's own framing (`book/src/L1/eigsolve.md:140` documents the per-step body as exactly `apply_linop` on a `Solver[A]`-wrapped op; :167-:169 establishes the firm-on-positive-structure / not-test-gated basis via the `apply_nonlinear_pencil`/`chebyshev-smoother` precedents).

**rotation-quality — pass.** The asserted L2↔L1 rotation is **non-identity and strictly more equational**, not a rename. L1 collapses the entire eigensolve into one opaque `(E,control)->EigResult`; L2 un-collapses the per-step-application half into the named composition `apply_linop ▷ ksp_solve ▷ scale_untransform` — exposing the inner `ksp_solve` (the opened `E.linear`), the operand choice (the re-surfaced spectral-transformation axis), and the assembly site (the re-surfaced backend-orchestration axis). This is the legitimate fusion-rotation direction (opacity unfolded back into a composition of firm lower-layer primitives), strictly more decomposed than the L1 form. The boundary type is shared (`EigSolver`/`EigResult`) but the *body* is rotated, which the entry states explicitly and which matches the firm L2 `ksp_solve` precedent (also non-identity L2↔L1). Not a 1:1 mapping; not renaming-only.

**variant-axis-coverage — pass.** Five axes declared in frontmatter; the body partitions them into three opened (spectral-transformation, problem-type, scaling) + two collapsed (backend-orchestration, element-type), each with a positive anchor (spectral-transformation `none`/STSINVERT/STPRECOND at `arpack.cpp:573-575`/:579-581 + `slepc.cpp:384`/:388; problem-type linear/quadratic-PEP/nonlinear-NEP at `arpack.cpp:733-799` + `apply_nonlinear_pencil` ref; scaling NONE/NORM_2 at `slepc.cpp:1810,1825` + `arpack.cpp:581`). No hidden branches: the NEP operand-assembly is explicitly scoped out as "referenced, not fully unfolded" (OQ #5, with `slepc.cpp:2170-2202` cited); the collapsed axes name their absorption. Coverage is complete or explicitly deferred.

**cross-reference-integrity — fail.** Build-readiness fence guard PASSES (see below), but one live link inside the `new:` fence resolves to a **non-existent target**, which is a hard `linkcheck2` build break. **§"Lifts to" (CYCLE.md:191)** uses `L2 eigsolve lifts to L3 [`eigsolve`](../L3/eigsolve.md)` — a live markdown link to `book/src/L3/eigsolve.md`, which DOES NOT EXIST. This is the `rough-in-forward-reference-must-be-plain-text-not-live-link` friction pattern: the L3 entry is BLOCKED/pending (the report itself says so), so the reference must be plain-text (e.g. `` `L3/eigsolve` ``), not `[...](...)`. I enumerated all 19 relative live-links in the fence (:24-:224) against `book/src/L2/` — 18 resolve OK; only `../L3/eigsolve.md` is MISSING. The two correctly-handled speculative refs confirm the producer knew the convention: `concepts/eigsolve` (:160,:262) and `L2-L1/eigsolve-spectral-transform-composition` (:187,:260) are both backtick-plain-text (their targets are also missing — `concepts/eigsolve.md`, `L2-L1/eigsolve-spectral-transform-composition.md` — correctly NOT linked). The `lowers_to:`/`lifts_to:` frontmatter (:30-:32) name `L3/eigsolve.md`/`L2/eigsolve.md` as bare strings (not rendered links — fine). All other targets resolve: L2 `ksp_solve`/`orthogonalize`/`gram`/`deflate`/`krylov-step`/`index`; L1 `eigsolve`/`apply_linop`/`dot`/`nrm2`/`axpy`/`axpby`/`ksp_solve`/`apply_nonlinear_pencil`; concepts `constructed-operators`/`solver-as-operator`/`variant-absorption`/`sequential-obstruction`/`solve-monad`/`apply_BA` — all exist. **Build-readiness guard (firm-body-inside-fence) — clean.** Fence enumeration: 12 triple-backtick markers, even parity, balanced. The `new:book/src/L2/eigsolve.md` fence spans :24-:224 with three properly-nested-and-closed inner ` ```text ` blocks (:66-68, :72-77, :81-92). The full firm apparatus is INSIDE the fence: frontmatter `firmness: firm` (:28), `## Signature` (:64), `## Algebraic laws` (:119), `## Status` (:177), `## Evidence` (:198) all land at lines 24<n<224. The three sections outside the fence (`## Operator content` :236, `## Supporting evidence` :248, `## Open questions` :256) are the report's own meta-summary (explicitly "the full firm chapter body is authored inside the proposed-changes block above"), NOT body authored outside the fence — this is NOT the cycle-019 fence-truncation defect. The `edit:book/src/L2/index.md` block (:226-229) anchors against the existing `deflate` row (current `index.md:60`, reproduced verbatim) then appends the new `eigsolve` row; no pre-existing eigsolve row, so no duplicate. `edit:book/src/SUMMARY.md` (:231-234) anchors against the existing `deflate` line (current `SUMMARY.md:47`) and appends `eigsolve` — valid surgical insert.

**edge-label-fidelity — pass.** The report carries directional edge prose in §"Lowers from" (L2←L1) and §"Lifts to" (L2→L3). Both discuss exactly their stated edges: §"Lowers from" narrates the L1-opaque-collapse → L2-named-composition opening (matches L2←L1); §"Lifts to" narrates the L2-body → L3-partial-obstruction lift (matches L2→L3). The `lifts_to:`/`lowers_to:` frontmatter directions are consistent with the prose. No edge-label/prose mismatch. (The link-target defect at :191 is a cross-reference issue, not an edge-label-direction issue — the prose discusses the correct L2→L3 edge.)

**plan-kind-consistency — pass.** Declared kind is a `firm` L2 operator entry. Content shape matches: full apparatus (Signature, Semantics, four held + six explicit non-laws, Dependencies, Variant axes, Status, Evidence) with no rough-in/stub placeholders. The `firm` claim is justified by positive-source syntactic-identity laws (1,4) + fold-terminal L1-law restatements (2,3) on the firm-on-positive-structure basis, consistent with the L1 eigsolve and L2 ksp_solve precedents. No mis-classification (it is not a stub-with-firm-label nor a firm-label-on-placeholders).

**skill-uptake-survey — pass (telemetry).** The report's shape implies `verify-citation-range` / `verify-rotation-citation` / `proposed-changes-fence-encloses-full-body-guard` are relevant; §Evidence (:200) explicitly states "Citations self-verified against source this dispatch via codemap `read_range` + `tools/citecheck/` `--scan`," documenting the citation-range verification uptake. The rotation-citation and fence-guard procedures are evidenced by the firm-on-positive-structure framing and the inside-fence apparatus authoring. Pure presence check; non-blocking.

### Issues found

1. **[fail — cross-reference-integrity] Live link to non-existent `L3/eigsolve.md`.** CYCLE.md:191 (§"Lifts to"), inside the `new:book/src/L2/eigsolve.md` fence: `lifts to L3 [`eigsolve`](../L3/eigsolve.md)`. Target `book/src/L3/eigsolve.md` does not exist (the L3 entry is BLOCKED/pending per the report's own OQ #1). This is a hard `linkcheck2` build error. **Severity: high** (blocks `cargo make book`). Fix: convert to plain text (backtick-quote the slug, e.g. `` `L3/eigsolve` ``), matching how the report already correctly handles the equally-pending `L2-L1/eigsolve-spectral-transform-composition` and `concepts/eigsolve` references. This is the `rough-in-forward-reference-must-be-plain-text-not-live-link` pattern; alternatively, per the "Integration may materialize implied components as stubs" directive, integration MAY instead create an `L3/eigsolve.md` stub (the component is clearly implied — frontmatter `lowers_to`, OQ #1, and §"Lifts to" all converge on it) so the live link resolves. Repairer/integrator decides which; the defect to flag is that as written it is a dead live-link.

2. **[warning — citation-validity, minor] Law-1 anchor `:578-581` is a one-line superset.** CYCLE.md:123 cites `palace/linalg/arpack.cpp:578-581` for the shift-invert composition body; the actual three statements (`opM->Mult`/`opInv->Mult`/`*= gamma`) are :579-581, and :578 is the `else {` brace. In-range and not wrong, just one line wider than the cited content. The §Evidence and §Variant-axes citations use the precise `:579-581`. **Severity: very low** (cosmetic; the precise form already appears elsewhere in the entry). No action required beyond optional tightening.

3. **[warning — prose precision, minor] "third named composition" undercount.** CYCLE.md:47 (§Context) calls eigsolve "the third named composition after `orthogonalize` and `ksp_solve`." The current `L2/index.md` named-composition cohort also includes `deflate` (a "named composition", :42/:60) and the `incremental-least-squares` stub (the "queued second named composition", :57), so the ordinal "third" is informal/ambiguous. **Severity: very low** (does not affect any claim or citation; the dep-map row the report adds does not assert an ordinal). Optional: soften to "another named composition" or drop the ordinal. Flagged for completeness, not blocking.

No other issues. Citations are line-exact (50/50 citecheck pass + full manual spot-check), the firm named-composition judgment is sound and rests on Palace-owned positive source (ARPACK `ApplyOp` independently confirmed), the opaque eigen-iteration is correctly held as upward context (not smuggled into L2 content), the rotation is genuine non-identity, variant axes are covered-or-scoped, and the build-readiness fence guard is clean (firm apparatus fully inside the fence; this is NOT the cycle-019 defect). The single high-severity finding is the one dead live-link at :191.

## Repair

### Fixes attempted

1. **Finding** (FAIL — cross-reference-integrity): live markdown link `[`eigsolve`](../L3/eigsolve.md)` at CYCLE.md:191 (§"Lifts to", inside the `new:book/src/L2/eigsolve.md` fence) resolves to a non-existent target `book/src/L3/eigsolve.md` (the L3 entry is BLOCKED/pending) — a hard `linkcheck2` build break, the `rough-in-forward-reference-must-be-plain-text-not-live-link` pattern.
   - **Decision**: repaired (in repair authority — "trivial cross-reference fix" / forward-reference-must-be-plain-text; mechanical de-linking, no content authored).
   - **Action**: converted the live link to plain-text inline-code matching the two sibling pending refs (`concepts/eigsolve` :160/:262, `L2-L1/eigsolve-spectral-transform-composition` :187/:260, both already backtick-plain-text). `CYCLE.md` §"Lifts to" (:191) now reads `lifts to L3 `L3/eigsolve` (pending — BLOCKED on this entry; predicted `partial-obstruction`; plain-text reference per the `rough-in-forward-reference-must-be-plain-text-not-live-link` convention since the target file does not yet exist): …`. Verified: confirmed `book/src/L3/eigsolve.md` is MISSING (codemap `ls book/src/L3/` shows only apply_linop / axpby / axpbypcz / axpy / chebyshev / dot / index / krylov-step / ksp_solve / nrm2 / scal — no eigsolve). Re-scanned all relative live-links inside the `new:` fence (:24–:224): 18 unique targets, **all 18 now resolve** against `book/src/L2/` (the `../L3/eigsolve.md` MISS is gone). The `lowers_to:` frontmatter (:32) names `L3/eigsolve.md` as a bare string (not a rendered link — fine, left as-is). Fence parity re-verified: 12 triple-backtick markers (even), the `new:` fence and its three nested ` ```text ` blocks + the two `edit:` blocks all balanced.
   - **Integrator note (per "Integration may materialize implied components as stubs")**: the repair-phase safe fix is plain-text (applied above). The integrator has a second option: `L3/eigsolve.md` is a **clearly-implied** component (≥2 converging references: this entry's §"Lifts to" + `lowers_to:` frontmatter + OQ #1 "L3 backfill unblocked, predicted partial-obstruction" + the cycle-021 `l3-eigsolve-linear-evp-has-no-krylov-step-kernel-analog` prediction / plan chain-step-3). Per the stub-materialization directive, the integrator MAY instead create an `L3/eigsolve.md` `stub` (predicted `partial-obstruction`, "Implied by" provenance = this report's §"Lifts to" + `lowers_to:` + OQ #1) at integration time, in which case the §"Lifts to" reference could be **re-promoted to a live link**. Both paths are valid; the repairer chose plain-text (the build-safe, no-content-authored path) and leaves the stub-vs-plain-text choice to the integrator. The repaired CYCLE.md is build-clean either way.

2. **Finding** (warning — citation-validity, minor): law-1 anchor `palace/linalg/arpack.cpp:578-581` (CYCLE.md §"Algebraic laws" law 1) is a one-line superset — `:578` is the `else {` line, the three-statement body (`opM->Mult`/`opInv->Mult`/`y1 *= γ`) is `:579-581`.
   - **Decision**: repaired (in repair authority — "citation line range off by a small offset"; surgical one-token tighten, no claim changed).
   - **Action**: tightened the law-1 parenthetical to `palace/linalg/arpack.cpp:579-581`. Verified via codemap `read_range arpack.cpp:571-590`: lines 577–578 are `{` / `else`, line 579 `opM->Mult(x1, z1)`, line 580 `opInv->Mult(z1, y1)`, line 581 `y1 *= gamma` — the cited three statements are exactly `:579-581`, matching the precise form already used in §Evidence (:202) and §Variant-axes (:168). The other `:578-581` was the only superset occurrence; no residual remains.

3. **Finding** (warning — prose precision, minor): "the third named composition after `orthogonalize` and `ksp_solve`" (CYCLE.md §Context :47) undercounts the `L2/index.md` named-composition cohort, which also includes `deflate` (firm-ish / `partly-constructive`, index :42/:60) and the `incremental-least-squares` stub (the "queued second named composition", :57), making the ordinal "third" informal/ambiguous.
   - **Decision**: repaired (in repair authority — claim-preserving prose softening; the dep-map row the report adds asserts no ordinal, so dropping the ordinal changes no claim or citation).
   - **Action**: softened "the third named composition after `orthogonalize` and `ksp_solve`" → "another named composition alongside `orthogonalize` … and `ksp_solve` …". Verified the cohort via `grep -n "named composition" book/src/L2/index.md` (confirms `deflate` and the `incremental-least-squares` stub are also in the cohort). No citation affected.

### Unrepairable findings

None. All three findings were mechanical (one dead live-link de-linking, one off-by-one citation tighten, one claim-free prose softening) and fully within repair authority. The critic's seven `pass` checks are untouched; the FAIL was a build-blocker fixed by plain-texting, not a content/judgment defect.

## Suggested resolution

`ready`. The single FAIL was a build-blocker (dead live-link to the pending `L3/eigsolve.md`), now plain-texted — the firm named-composition judgment the critic validated is sound and unchanged. Notes for the integrator:
- The `new:book/src/L2/eigsolve.md` fence is build-clean: all 18 relative live-links resolve, fence parity even (12 markers), the three nested ` ```text ` blocks and two `edit:` blocks all balanced. The `edit:book/src/L2/index.md` block anchors against the existing `deflate` row (current `index.md:60`) + appends the new `eigsolve` row; the `edit:book/src/SUMMARY.md` block anchors against the existing `deflate` line (`SUMMARY.md:47`) + appends `eigsolve` — surgical inserts, no duplicate eigsolve row pre-exists.
- **Optional stub materialization**: per finding 1's integrator note, the integrator may create an `L3/eigsolve.md` stub (clearly-implied, ≥2 converging references; predicted `partial-obstruction`) and re-promote the §"Lifts to" reference to a live link. Not required — the plain-text reference is build-clean as-is. If the integrator does NOT create the stub, OQ #1 (L3 backfill unblocked) remains the standing plan hook for the cycle-planner to schedule chain-step-3.
