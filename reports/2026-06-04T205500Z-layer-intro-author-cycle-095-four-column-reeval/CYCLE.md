---
agent: layer-intro-author
invoked_at: 2026-06-04T205500Z
scope: cycle-095 D4 (Wave 2) — the 4-column (capacitance/inductance/electrostatic/magnetostatic) seed→firm re-evaluation under the OWN-COMPOSITION rule + feature/index.md SOLE-owner + HARD-gate-new typed frontmatter + bilinear-form rough-in re-anchor
status: integrated
integrated_at: 2026-06-04T231500Z
integration_commit: PLACEHOLDER_SHA
integration_notes: "cycle-095 D4 (staging position 4/7). 15 files: the 4 columns capacitance/inductance/electrostatic/magnetostatic x {L4,L1,L0} seed->firm under OWN-COMPOSITION (the planner's outcome-(a) forward-ref MATERIALIZED — gram_reduce firm on disk, no (b)-revert) + feature/index.md SOLE-owner chapter-kind split -> 11 firm / 1 seed + 2 group-intros. Feature spine 7->11 FIRM / 5->1 seed. Rank-gate PASS all 12 columns x 3 levels. Applied clean (+2 discretionary within-file self-consistency re-anchors); retroactive-budget 0. Promoted shared OQ cites-evidence-l0-edge-linter-slug-resolution-exemption."
inputs:
  - reports/2026-06-04T204023Z-cycle-planner-cycle-095/CYCLE.md (D4 scope; the cascade-discharges-10 partition; the OWN-COMPOSITION gate logic)
  - reports/2026-06-04T204500Z-harvester-cycle-095-bilinear-form-firm-flip/CYCLE.md (D1 — bilinear-form flipped rough-in→firm; the column off-diagonal folded primitive is now firm)
  - book/src/L4/gram_reduce.md (D3's re-judgment target; on disk rough-in (test-coverage-bounded), sole residual gate = bilinear-form, which D1 firmed → D3 outcome (a) firms gram_reduce, which clears the 4-column gate)
  - book/src/methodology/graded-stack-scheme.md (§1 rank ladder, §2 typed edges, §3 feature_root split, §4(c) composes:/l0_ground_truth: → edges: migration)
  - the 12 cascade column files + feature/index.md + feature/output-product.md + feature/driver-leaf.md (all read end-to-end this dispatch)
---

# CYCLE: the 4-column seed→firm cascade re-evaluation + feature/index ownership

## Summary

This is **D4, Wave 2** of cycle-095 (the bilinear-form firm-flip-and-cascade-wave / GRADED-STACK P1 launch). The visible cascade payload: the four `seed` feature columns **capacitance / inductance / electrostatic / magnetostatic** are re-evaluated under the **OWN-COMPOSITION rule** and **flip `seed`→`firm`**, because the cascade clears their sole own-constituent gate.

**The gate chain (verified on disk this dispatch):**
1. **D1 firmed `bilinear-form`** (rough-in→firm; the firm-on-positive-structure escape, DISCHARGE c092 — its CYCLE.md is on disk and read this dispatch). The off-diagonal folded primitive `Vⱼᵀ K Vᵢ` / `Aⱼᵀ K Aᵢ` is now firm.
2. **`gram_reduce`'s SOLE residual gate is `bilinear-form`** (read on disk: `book/src/L4/gram_reduce.md:241-247` — "the off-diagonal building block `bilinear-form` is still rough-in … the sole RESIDUAL gate"; the diagonal `matrix-weighted-norm` half was already discharged c091). With `bilinear-form` now firm, **both** folded primitives of `gram_reduce` are firm and every `gram_reduce` law is a syntactic identity on the fold over firm primitives → the firm-on-positive-structure escape applies (the same escape that firmed its reduce-verb siblings `domain_energy_reduce` c091, `eigenfreq_qfactor_reduce` c082, `sparameter_reduce` c083). **D3 firms `gram_reduce`** (the planner's outcome (a); D3 owns that flip, this dispatch reads its resolved rank). The "no dedicated Gram-reduction unit test" residual (gram_reduce §Status item 3) does NOT gate syntactic-identity laws on positive source — the `apply_linop` situation, NOT the convergence-semantics situation (CLAUDE.md §`rough-in (test-coverage-bounded)` firm-on-positive-structure escape).
3. **Each of the 4 columns' directly-owned constituents are now ALL firm:**
   - `electrostatic` / `magnetostatic` (driver-leaf): own `fe_assemble` (firm), `solve_family` (firm c086), `ksp_solve` (firm), and the own stage-(3) readout `gram_reduce` (firm via D3). At L1: own `fe_assemble` (firm), `ksp_solve` (firm §Status), `matrix-weighted-norm` (firm c091 §Status), `bilinear-form` (firm via D1).
   - `capacitance` / `inductance` (output-product): own reduce verb `gram_reduce` (firm via D3); the cross-link to the producing driver column (`electrostatic`/`magnetostatic`) is a SIBLING **reference**, NOT a blocker (OWN-COMPOSITION rule).

Therefore all 4 columns flip `seed`→`firm`. This is the honest, gated outcome — NOT a forcing: the gate genuinely cleared this cycle (the c092 DISCHARGE + D1 enact + D3 firm chain is real on disk).

**Per-constituent firmness survey (from on-disk `## Status` / frontmatter, NOT index cells nor the cycle record):**

| constituent | on-disk maturity | source |
|---|---|---|
| `L4/fe_assemble` | `firm` | `book/src/L4/fe_assemble.md` frontmatter `firmness: firm` |
| `L4/solve_family` | `firm` | `book/src/L4/solve_family.md` frontmatter `firmness: firm` (c086) |
| `L4/ksp_solve` | `firm` | `book/src/L4/ksp_solve.md` frontmatter `firmness: firm` |
| `L4/gram_reduce` | `firm` **via D3 this cycle** | sole residual gate (`bilinear-form`) cleared by D1; planner outcome (a) |
| `L1/fe_assemble` | `firm` | `book/src/L1/fe_assemble.md` frontmatter `firmness: firm` |
| `L1/ksp_solve` | `firm` | `book/src/L1/ksp_solve.md:103` §Status `firm` |
| `L1/matrix-weighted-norm` | `firm` | `book/src/L1/matrix-weighted-norm.md:109` §Status `firm` (c091) |
| `L1/bilinear-form` | `firm` **via D1 this cycle** | D1 CYCLE.md flip rough-in→firm (DISCHARGE c092, ENACT c095) |

**Forward-reference note:** `bilinear-form` (D1) and `gram_reduce` (D3) read `rough-in` / `rough-in (test-coverage-bounded)` on the literal disk right now because D1's + D3's proposed-changes are applied by the integrator AFTER this dispatch (wave ordering). This dispatch references their *flipped* state (per the planner's wave-2-reads-wave-1/within-wave-D3 sequencing); the integrator serializes the applies so the cross-references resolve to the firm state.

**Ownership partition (stated explicitly per the plan):**
- **D4 (this dispatch) owns:** the **4 cascade columns' frontmatter + status prose** (capacitance/inductance/electrostatic/magnetostatic × {L4,L1,L0} = 12 files), the **`feature/index.md`** matrix + `## Chapter-kind status` + the output-product/driver-leaf narration in it, the **`feature/output-product.md`** + **`feature/driver-leaf.md`** group-intro sibling-status lines (the index-cell-drift guard surfaces I own), and the **whole-`book/src/feature/` sibling-status grep**.
- **D5 (Wave 3) owns:** the **NON-cascade columns' frontmatter typing** (driven/eigenmode/transient/boundary-mode/lifecycle/eigenfrequency-qfactor/sparameters/energy-fields × {L4,L1,L0}) — converting their `composes:`→`edges:` + writing `feature_root:`/`rank:`. D5 does NOT touch `feature/index.md` (deferred to D4) and does NOT touch the 4 cascade columns.
- **No SUMMARY edit needed:** the `# Feature surfaces` SUMMARY block (`book/src/SUMMARY.md:9-48`) carries no status tokens in the chapter titles — the flip changes no SUMMARY structure (all 12 chapters already wired in the 3 groupings). I am the SOLE owner of that block this cycle and confirm it needs no edit.
- **HARD constraints respected:** I do NOT touch the L1/L4 vocabulary indexes' count headers (D1 owns `L1/index`; `L4/index` coordinated — see Open questions for the D2-flagged `L4/index:101`), nor `gram_reduce.md`/`domain_energy_reduce.md` (D3).

## HARD-gate-new compliance

Each column promoted to `rank: firm` carries the scheme §3 SPLIT — `feature_root: seed` (permanent categorical root marker) + `rank: firm` (composition-maturity) — plus a typed `edges:` block converted from the legacy `composes:`/`l0_ground_truth:` frontmatter per scheme §4(c):
- a `composes:` target that is a **vocabulary op** → `depends-on` (blocking; constrains rank);
- a `composes:` target that is a **sibling feature column** → `reference` (OWN-COMPOSITION: sibling columns are references, NOT blocking — this is exactly why the cross-linked driver/output-product columns do not gate each other's rank);
- `l0_ground_truth:` → `depends-on` with `kind: cites-evidence`;
- the free-text maturity qualifiers in the `composes:` strings are **dropped** from the edge (the dep's rank is read from its own frontmatter — the index-cell-drift lesson, scheme §4(c)).

**Rank invariant check** (`rank(column=firm=3) ≤ min over depends-on deps`): every `depends-on` target is firm (rank 3) per the survey table above. `feature_root: seed` is a parallel axis, not a rank — it does not enter the invariant. The `reference:` sibling-column edges constrain nothing. The invariant holds for all 4 columns at all 3 levels. The L0 levels' `depends-on` targets are `l0_ground_truth:` evidence citations (rank not gated by the §1 ladder — they are ground truth, `kind: cites-evidence`) plus the `lifts_to:` sibling level (converted to a `reference`, since a level-sibling navigational pointer carries no liveness/rank constraint per scheme §2; the lowering relationship's authoritative typed edge is the L1↔L0 op-level theme, not the feature-surface level pointer).

## Proposed changes

### Column 1 — capacitance (output-product leaf; w = 1 voltage Gram)

#### capacitance.L4.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/capacitance.L4.md
---
kind: feature-surface
feature: capacitance
level: L4
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L4/gram_reduce
      kind: folds
    - target: palace/drivers/electrostaticsolver.cpp:100-140
      kind: cites-evidence
  reference:
    - feature/electrostatic.L4
---
```

#### capacitance.L4.md — §"Why this is a clean output-product column" promotion-rule paragraph (`:51`)

```edit:book/src/feature/capacitance.L4.md
The whole output product therefore lowers cleanly outward to the L4 backend surface: `capacitance = gram_reduce (w ≡ 1) ∘ electrostatic_family` — a one-reduction tail on the electrostatic driver column. Under the **OWN-COMPOSITION rule** (USER DIRECTIVE 2026-06-03; CLAUDE.md §Extraction-goal) a column promotes off `seed` when its OWN directly-owned constituents are firm; this column is **firm** because its OWN reduce verb [`gram_reduce`](../L4/gram_reduce.md) firmed at cycle-095 (the firm-flip-and-cascade wave, D3 re-judgment — its sole residual off-diagonal [`bilinear-form`](../L1/bilinear-form.md) folded primitive firmed via D1, so both folded primitives are now firm and the firm-on-positive-structure escape applies). The cross-link to the [`electrostatic.L4`](./electrostatic.L4.md) producing driver column is a SIBLING reference, NOT a constituent — it does not gate this column's rank.
```

#### capacitance.L4.md — §"The composition" stage-2 status label (`:36`)

```edit:book/src/feature/capacitance.L4.md
2. **The symmetric-Gram reduction at `w = 1`** — [`gram_reduce`](../L4/gram_reduce.md) (**firm**, c095). The L4 operator-weighted symmetric-Gram reduction `gram_reduce k xs w` folds each upper-triangle family pair through the operator-weighted bilinear primitives and mirrors to a symmetric matrix. The capacitance matrix is its **voltage / unit-weight specialization** (`gram_reduce`'s §Specialization "Electrostatic capacitance" bullet, `book/src/L4/gram_reduce.md:167-171`; the literal "positive witness 1" label is at `:279`): `w i j = 1` (the unit-voltage excitation `/Vᵢ² ≡ ×1`), `K = M_elec` (the diffusion-energy operator), `xs = [Vᵢ]` (the per-terminal solution family). The diagonal is `Cᵢᵢ = Vᵢᵀ K Vᵢ`, the off-diagonal `Cᵢⱼ = Vⱼᵀ K Vᵢ`; `gram_inverse` then produces `Cinv = C⁻¹` (LAPACK) for the alternate Maxwell form. The `w = 1` weight is the multiplicative-identity specialization on `gram_reduce`'s load-bearing normalization-weight axis (the magnetostatic inductance product is the `w = 1/(Iᵢ Iⱼ)` sibling specialization of the SAME combinator — see [`inductance.L4`](./inductance.L4.md)). L0: `PostprocessTerminals` (`electrostaticsolver.cpp:95`, def `:100`), `mfem::DenseMatrix C(V.size())` (`:111`), the energy-form `Mult`/`Dot` (`:118-127`), the inverse (`:139-140`).
```

#### capacitance.L4.md — §"Why this is a clean output-product column" lead (`:45`)

```edit:book/src/feature/capacitance.L4.md
The capacitance product is the cleanest output-product composition root because **its single reduction stage is exactly one firm L4 combinator at its simplest weight**:
```

#### capacitance.L4.md — §"Constituent down-links" table rows (`:55-60`)

```edit:book/src/feature/capacitance.L4.md
| Stage | L4 constituent | Status | L0 site |
|---|---|---|---|
| producing driver column | [`electrostatic.L4`](./electrostatic.L4.md) (driver feature column; sibling reference) | firm | `electrostaticsolver.cpp:21-98` |
| symmetric-Gram reduction (w = 1) | [`gram_reduce`](../L4/gram_reduce.md) | firm (c095) | `electrostaticsolver.cpp:100-140` |
| diagonal Vᵢᵀ K Vᵢ (folded) | [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) | firm c091 | `electrostaticsolver.cpp:118-119` |
| off-diagonal Vⱼᵀ K Vᵢ (folded) | [`bilinear-form`](../L1/bilinear-form.md) | firm c095 | `electrostaticsolver.cpp:126` |
```

#### capacitance.L4.md — §Status (`:64`)

```edit:book/src/feature/capacitance.L4.md
## Status

`firm` (promoted `seed`→`firm` at **cycle-095**, the `bilinear-form-firm-flip-and-cascade-wave`) — an output-product **leaf feature column** authored under the FEATURE-SURFACE SPINE directive (2026-06-02). The composition is sound: stage (1) consumes the [`electrostatic.L4`](./electrostatic.L4.md) driver column's solution family; stage (2) composes the [`gram_reduce`](../L4/gram_reduce.md) reduction at the voltage `w = 1` specialization. **Under the OWN-COMPOSITION rule (USER DIRECTIVE 2026-06-03; memory `project_feature_column_promotion_rule`) a column promotes off `seed` when its OWN composition + directly-owned constituents are firm; cross-linked sibling columns are references, NOT blockers.** This column's OWN directly-owned constituent — the reduce verb [`gram_reduce`](../L4/gram_reduce.md) — firmed at cycle-095 (the cascade: D1 firmed the off-diagonal [`bilinear-form`](../L1/bilinear-form.md) folded primitive on the firm-on-positive-structure escape, clearing `gram_reduce`'s sole residual gate so D3 firmed it; the diagonal [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) folded primitive had firmed c091). With its OWN reduce verb firm, this column promotes — independent of the [`electrostatic.L4`](./electrostatic.L4.md) producing driver column, which is a SIBLING **reference**, not a constituent. This chapter carries the *compositional* claim (capacitance = the `w = 1` Gram reduction over the electrostatic driver's solution family), not the constituents' per-op algebraic claims (those live in [`gram_reduce`](../L4/gram_reduce.md) and the linked L1 primitives). Evidence: the L0 reduction range `electrostaticsolver.cpp:100-140` (`PostprocessTerminals`) realizing the reduction, plus the firm constituent down-links.
```

#### capacitance.L1.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/capacitance.L1.md
---
kind: feature-surface
feature: capacitance
level: L1
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L1/matrix-weighted-norm
      kind: folds
    - target: L1/bilinear-form
      kind: folds
    - target: palace/drivers/electrostaticsolver.cpp:100-140
      kind: cites-evidence
  reference:
    - feature/electrostatic.L1
---
```

#### capacitance.L1.md — §"The composition" stage-2 lead (`:33`)

```edit:book/src/feature/capacitance.L1.md
2. **Capacitance-matrix reduction** — the symmetric matrix `Cᵢⱼ = Vⱼᵀ K Vᵢ`, built from L1 bilinear-form evaluations over the solution-family pair grid (firm diagonal + firm off-diagonal):
```

#### capacitance.L1.md — §"The composition" stage-2 off-diagonal bullet (`:35`)

```edit:book/src/feature/capacitance.L1.md
   - off-diagonal `Cᵢⱼ = Vⱼᵀ K Vᵢ` — the operator-weighted cross-pairing, the firm [`bilinear-form`](../L1/bilinear-form.md) `α = xᴴ M y` instantiated `⟨Vⱼ, K Vᵢ⟩` (L0 `:122-127`, the same energy-form `Mult`/`Dot` with the `j` grid function, `:126`).
```

#### capacitance.L1.md — §"The composition" stage-2 closing (`:36`)

```edit:book/src/feature/capacitance.L1.md
   The result is the symmetric `C` (and its LAPACK inverse `Cinv`, `:139-140`). This stage is a pure fold of bilinear-form evaluations over the family-pair grid — no L1 operator is *new* here; the reduction composes [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (firm c091) + [`bilinear-form`](../L1/bilinear-form.md) (firm c095). At L4 this exact fold is named the [`gram_reduce`](../L4/gram_reduce.md) combinator (voltage `w = 1` specialization); L1 sees the unfolded grid.
```

#### capacitance.L1.md — §"Constituent down-links" table (`:53-57`)

```edit:book/src/feature/capacitance.L1.md
| Stage | L1 constituent | Status | L0 site |
|---|---|---|---|
| producing driver column | [`electrostatic.L1`](./electrostatic.L1.md) (driver feature column; sibling reference) | firm | `electrostaticsolver.cpp:21-98` |
| diagonal Vᵢᵀ K Vᵢ | [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) | firm c091 | `electrostaticsolver.cpp:118-119` |
| off-diagonal Vⱼᵀ K Vᵢ | [`bilinear-form`](../L1/bilinear-form.md) | firm c095 | `electrostaticsolver.cpp:122-127` |
```

#### capacitance.L1.md — §Status (`:61`)

```edit:book/src/feature/capacitance.L1.md
`firm` (promoted `seed`→`firm` at cycle-095, the `bilinear-form-firm-flip-and-cascade-wave`) — the L1 pure-function composition root for the capacitance output product (the output-product **leaf feature column**), authored under the FEATURE-SURFACE SPINE directive (2026-06-02). It consumes the [`electrostatic.L1`](./electrostatic.L1.md) driver column's solution family, then folds two firm L1 bilinear primitives over the family-pair grid — the diagonal [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (firm c091) and the off-diagonal [`bilinear-form`](../L1/bilinear-form.md) (firm c095, the cascade-wave). **Under the OWN-COMPOSITION rule (USER DIRECTIVE 2026-06-03) a column promotes off `seed` when its OWN directly-owned constituents are firm; this column is firm because both its directly-owned reduction primitives are firm** (the off-diagonal `bilinear-form` firmed this cycle, so the L4 home `gram_reduce` firmed via D3). The [`electrostatic.L1`](./electrostatic.L1.md) producing driver column is a SIBLING reference, not a constituent. The chapter carries the compositional claim only; per-op algebraic claims live in the linked chapters. Evidence: the L0 reduction range `electrostaticsolver.cpp:100-140` realizing the composition, plus the constituent down-links.
```

#### capacitance.L0.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/capacitance.L0.md
---
kind: feature-surface
feature: capacitance
level: L0
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: palace/drivers/electrostaticsolver.cpp:100-140
      kind: cites-evidence
    - target: palace/drivers/electrostaticsolver.cpp:95
      kind: cites-evidence
    - target: palace/drivers/electrostaticsolver.hpp:34-44
      kind: cites-evidence
  reference:
    - feature/capacitance.L1
---
```

#### capacitance.L0.md — §"The composition, in source" off-diagonal stage (`:30`)

```edit:book/src/feature/capacitance.L0.md
4. **Off-diagonal — the operator-weighted cross-pairing.** The inner loop `for (int j = i + 1; j < C.Width(); j++)` (`:123`) computes the upper-triangle off-diagonals `Cᵢⱼ = Vⱼᵀ K Vᵢ` via `V_gf.SetFromTrueDofs(V[j])` (`:125`) then `C(i,j) = linalg::Dot<Vector>(post_op.GetComm(), V_gf, D_gf)` (`:126`, the same energy-form pairing with the `j` grid function reusing the `D_gf = K·Vᵢ` apply). This is the L0 site the off-diagonal [`bilinear-form`](../L1/bilinear-form.md) (firm c095) (and the L4 [`gram_reduce`](../L4/gram_reduce.md) off-diagonal entry) lift.
```

#### capacitance.L0.md — §Status (`:45`)

```edit:book/src/feature/capacitance.L0.md
`firm` (promoted `seed`→`firm` at cycle-095 alongside its L1/L4 levels, the `bilinear-form-firm-flip-and-cascade-wave`) — the L0 ground-truth surface for the capacitance output product (the output-product **leaf feature column**), authored under the FEATURE-SURFACE SPINE directive (2026-06-02). Every stage is a cited range into `palace/drivers/electrostaticsolver.cpp`, confirmed on-disk via palace-codemap `read_range` this dispatch (`:95` call site, `:100` def, `:111` allocation, `:118-119` diagonal, `:124-126` off-diagonal, `:130-135` mirror, `:139-140` inverse). The chapter's evidence IS the driver-source range + the per-stage site map to the constituent ops (the adapted surface-or-evidence form for the feature-surface kind); as a cited ground-truth surface its rank is firm.
```

### Column 2 — inductance (output-product leaf; w = 1/(IᵢIⱼ) current-normalized Gram)

#### inductance.L4.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/inductance.L4.md
---
kind: feature-surface
feature: inductance
level: L4
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L4/gram_reduce
      kind: folds
    - target: palace/drivers/magnetostaticsolver.cpp:110-152
      kind: cites-evidence
  reference:
    - feature/magnetostatic.L4
---
```

#### inductance.L4.md — §"The composition" stage-1 driver-column label (`:34`)

```edit:book/src/feature/inductance.L4.md
1. **The magnetostatic driver column produces the solution family** — [`magnetostatic.L4`](./magnetostatic.L4.md) (**firm**, sibling reference). The upstream composition root assembles the curl-curl stiffness `K` once ([`fe_assemble`](../L4/fe_assemble.md)) and maps the per-surface-current solve over the source family ([`solve_family`](../L4/solve_family.md)), collecting the solution family `as = [Aᵢ]`. This output-product column **consumes** that `(k, as)` pair; it does not re-derive it (the driver column owns the solve, this column owns the reduction). L0: the `Solve` body assembles + sweeps (`palace/drivers/magnetostaticsolver.cpp:29`, `:66`, `:77`); the family `A` + currents `I_inc` are handed to `PostprocessTerminals` (`:105`).
```

#### inductance.L4.md — §"The composition" stage-2 reduction label (`:36`)

```edit:book/src/feature/inductance.L4.md
2. **The current-normalized symmetric-Gram reduction** — [`gram_reduce`](../L4/gram_reduce.md) (**firm**, c095). The L4 operator-weighted symmetric-Gram reduction combinator `gram_reduce K xs w` folds each upper-triangle family-pair through the operator-weighted bilinear primitives and mirrors to a symmetric matrix. Inductance is its **current-normalized specialization**: `w i j = 1/(Iᵢ Iⱼ)`, `K = M_mag` (the curl-curl magnetic-energy operator), `xs = [Aᵢ]`. The diagonal `Mᵢᵢ = (Aᵢᵀ K Aᵢ)/Iᵢ²` is the self-bilinear ([`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) radicand) current-normalized; the off-diagonal `Mᵢⱼ = (Aⱼᵀ K Aᵢ)/(Iᵢ Iⱼ)` is the cross-bilinear ([`bilinear-form`](../L1/bilinear-form.md)) current-normalized. The COMSOL magnetic-energy formulation `Mᵢᵢ = 2Uₘ(Aᵢ)/Iᵢ²` is exactly the squared-energy radicand scaled by the current weight (`magnetostaticsolver.cpp:115-121` cites the COMSOL AC/DC Module manual p. 97). This is `gram_reduce`'s second positive witness (the current-normalized one; capacitance is the unit-weight first). L0: the diagonal `:129-131`, the off-diagonal `:138`, the symmetric mirror `:143-149`.
```

#### inductance.L4.md — §"Why this composes cleanly" promotion paragraph (`:53`)

```edit:book/src/feature/inductance.L4.md
The whole output product therefore lowers cleanly outward: `inductance = gram_inverse-tail ∘ gram_reduce(w=current-normalized) ∘ magnetostatic_solution`. Under the OWN-COMPOSITION promotion rule (a column promotes off `seed` when its OWN composition + directly-owned constituents are firm; cross-linked sibling columns are references, NOT blockers) this column is **firm**: its OWN reduce verb [`gram_reduce`](../L4/gram_reduce.md) firmed at cycle-095 (the cascade), and the [`magnetostatic`](./magnetostatic.L4.md) producing driver column it cross-links is a SIBLING reference, not a constituent. The current normalization is a scalar weight absorbed into the combinator's `w` argument, not a new shape.
```

#### inductance.L4.md — §"Constituent down-links" table (`:57-61`)

```edit:book/src/feature/inductance.L4.md
| Stage | L4 vocabulary | Status | L0 site |
|---|---|---|---|
| solution family [Aᵢ] | [`magnetostatic.L4`](./magnetostatic.L4.md) (driver column; sibling reference) | firm | `magnetostaticsolver.cpp:29, 66, 77` |
| current-normalized Gram reduction | [`gram_reduce`](../L4/gram_reduce.md) (`w = 1/(IᵢIⱼ)`) | firm (c095) | `magnetostaticsolver.cpp:122, 129-138, 143-149` |
| inverse tail (Minv) | `gram_inverse` (the LAPACK `Invert()` consumer) | — | `magnetostaticsolver.cpp:151-152` |
```

#### inductance.L4.md — §Status (`:65`)

```edit:book/src/feature/inductance.L4.md
`firm` (promoted `seed`→`firm` at cycle-095, the `bilinear-form-firm-flip-and-cascade-wave`) — an output-product leaf feature column under the FEATURE-SURFACE SPINE directive (2026-06-02), the current-normalized sibling of the [capacitance](./capacitance.L4.md) unit-weight output product. The composition is sound: the reduction is the [`gram_reduce`](../L4/gram_reduce.md) combinator's current-normalized specialization (`w = 1/(IᵢIⱼ)`), consuming the [`magnetostatic`](./magnetostatic.L4.md) driver column's solution family `[Aᵢ]`, with the inverse a downstream consumer. **Under the OWN-COMPOSITION rule (USER DIRECTIVE 2026-06-03; memory `project_feature_column_promotion_rule`) a column promotes off `seed` when its OWN composition + directly-owned constituents are firm; cross-linked sibling columns are references, NOT blockers.** This column is firm because its OWN reduce verb [`gram_reduce`](../L4/gram_reduce.md) firmed at cycle-095 (the cascade: D1 firmed the off-diagonal [`bilinear-form`](../L1/bilinear-form.md) folded primitive, clearing `gram_reduce`'s sole residual gate so D3 firmed it; the diagonal [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) folded primitive had firmed c091). The [`magnetostatic`](./magnetostatic.L4.md) producing driver column is a SIBLING reference, not a constituent — it does not gate this column's rank. This chapter carries the *compositional* claim (the inductance output product = this current-normalized specialization of `gram_reduce` over the magnetostatic family), NOT the combinator's per-op algebraic claims (those live in [`gram_reduce`](../L4/gram_reduce.md)). Evidence: `PostprocessTerminals` realizing the current-normalized reduction (`magnetostaticsolver.cpp:110-152`, on-disk-verified this dispatch), plus the `gram_reduce` + magnetostatic-column down-links.
```

#### inductance.L1.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/inductance.L1.md
---
kind: feature-surface
feature: inductance
level: L1
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L1/matrix-weighted-norm
      kind: folds
    - target: L1/bilinear-form
      kind: folds
    - target: palace/drivers/magnetostaticsolver.cpp:110-152
      kind: cites-evidence
  reference:
    - feature/magnetostatic.L1
---
```

#### inductance.L1.md — intro paragraph "two rough-in L1 bilinear operators" (`:18`)

```edit:book/src/feature/inductance.L1.md
At L1 the inductance output product is a pure function `(solution family, currents) → inductance matrix` built from the magnetostatic driver column's solution family `[Aᵢ]` reduced through two firm L1 bilinear operators, with the **mutation already lifted** (the L0 in-place `M_mag->Mult(A_gf, H_gf)` / `linalg::Dot(...)` energy-form writes are lifted to value-returning bilinear evaluations per the constituent ops' L1>L0 mutation rotations).
```

#### inductance.L1.md — §"The composition" stage-1 driver label (`:37`)

```edit:book/src/feature/inductance.L1.md
1. **The magnetostatic driver column produces the solution family** — [`magnetostatic.L1`](./magnetostatic.L1.md) (**firm**, sibling reference). The upstream pure-function composition root assembles `K` once and solves the per-source family `as = [Aᵢ]`. This output-product column consumes that `(k, as)` pair. L0: the `Solve` body (`palace/drivers/magnetostaticsolver.cpp:29`, `:66`, `:77`); the family + currents handed to `PostprocessTerminals` (`:105`).
```

#### inductance.L1.md — §"The composition" stage-2 off-diagonal bullet (`:41`)

```edit:book/src/feature/inductance.L1.md
   - off-diagonal `Mᵢⱼ = (Aⱼᵀ K Aᵢ)/(Iᵢ Iⱼ)` — the operator-weighted cross-pairing normalized by the current product, the firm [`bilinear-form`](../L1/bilinear-form.md) `α = xᴴ M y` instantiated `⟨Aⱼ, K Aᵢ⟩` (L0 the same energy-form `Mult`/`Dot` against the `j` grid function then `/ (I_inc[i]*I_inc[j])`, `:135-138`).
```

#### inductance.L1.md — §"The composition" stage-2 closing (`:42`)

```edit:book/src/feature/inductance.L1.md
   The result is the symmetric `M` (upper triangle computed, lower mirrored, `:140-149`) and its LAPACK inverse `Minv` (`:151-152`). This stage is a pure fold of current-normalized bilinear-form evaluations over the solution-family pair grid — no L1 operator is *new* here; it composes [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (firm c091) + [`bilinear-form`](../L1/bilinear-form.md) (firm c095), with the current normalization a scalar weight on each entry.
```

#### inductance.L1.md — §"Constituent down-links" table (`:59-63`)

```edit:book/src/feature/inductance.L1.md
| Stage | L1 operator | Status | L0 site |
|---|---|---|---|
| solution family [Aᵢ] | [`magnetostatic.L1`](./magnetostatic.L1.md) (driver column; sibling reference) | firm | `magnetostaticsolver.cpp:29, 66, 77` |
| diagonal Aᵢᵀ K Aᵢ / Iᵢ² | [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) | firm c091 | `magnetostaticsolver.cpp:129-131` |
| off-diagonal Aⱼᵀ K Aᵢ / Iᵢ Iⱼ | [`bilinear-form`](../L1/bilinear-form.md) | firm c095 | `magnetostaticsolver.cpp:135-138` |
```

#### inductance.L1.md — §Status (`:67`)

```edit:book/src/feature/inductance.L1.md
`firm` (promoted `seed`→`firm` at cycle-095, the `bilinear-form-firm-flip-and-cascade-wave`) — the L1 pure-function output-product composition root for the inductance matrix, authored under the FEATURE-SURFACE SPINE directive (2026-06-02), the current-normalized sibling of the [capacitance.L1](./capacitance.L1.md) unit-weight output product. Both directly-owned bilinear primitives are now firm — the diagonal [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (firm c091, the batch-29 firm-flip-and-cascade wave) and the off-diagonal [`bilinear-form`](../L1/bilinear-form.md) (firm c095, this cycle's cascade wave; its `α = xᴴ M y` signature covers the cross-pairing). **Under the OWN-COMPOSITION rule (USER DIRECTIVE 2026-06-03) a column promotes off `seed` when its OWN directly-owned constituents are firm; this column is firm because both its reduction primitives are firm** (so the L4 home `gram_reduce` firmed via D3). The [`magnetostatic.L1`](./magnetostatic.L1.md) producing driver column is a SIBLING reference, not a constituent. The chapter carries the compositional claim only; per-op algebraic claims live in the linked chapters. Evidence: `PostprocessTerminals` realizing the current-normalized reduction (`magnetostaticsolver.cpp:110-152`, on-disk-verified this dispatch), plus the L1 constituent down-links.
```

#### inductance.L0.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/inductance.L0.md
---
kind: feature-surface
feature: inductance
level: L0
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: palace/drivers/magnetostaticsolver.cpp:110-152
      kind: cites-evidence
    - target: palace/drivers/magnetostaticsolver.cpp:105
      kind: cites-evidence
  reference:
    - feature/inductance.L1
---
```

#### inductance.L0.md — §"The reduction, in source" off-diagonal stage (`:29`)

```edit:book/src/feature/inductance.L0.md
4. **Off-diagonal — the current-normalized cross-energy.** The inner `j`-loop (`:135`, `for (int j = i + 1; ...)`): `A_gf.SetFromTrueDofs(A[j])` (`:137`) loads `Aⱼ` (`H_gf` still holds `K·Aᵢ` from the diagonal apply); `M(i, j) = linalg::Dot<Vector>(post_op.GetComm(), A_gf, H_gf) / (I_inc[i] * I_inc[j])` (`:138`) forms the off-diagonal `Mᵢⱼ = (Aⱼᵀ K Aᵢ)/(Iᵢ Iⱼ)` — the operator-weighted cross-pairing normalized by the current product; the mutual-inductance bookkeeping `Mm(i, j) = -M(i, j); Mm(i, i) -= Mm(i, j)` (`:139-140`) builds the `Mm` variant. This is the L0 site the off-diagonal [`bilinear-form`](../L1/bilinear-form.md) (firm c095) lifts. The whole diagonal+off-diagonal energy-form computation spans `:129-138`.
```

#### inductance.L0.md — §Status (`:46`)

```edit:book/src/feature/inductance.L0.md
`firm` (promoted `seed`→`firm` at cycle-095 alongside its L1/L4 levels, the `bilinear-form-firm-flip-and-cascade-wave`) — the L0 ground-truth surface for the inductance output product, authored under the FEATURE-SURFACE SPINE directive (2026-06-02), the sibling of the [capacitance.L0](./capacitance.L0.md) output-product surface. Every stage is a cited range into `palace/drivers/magnetostaticsolver.cpp`, confirmed on-disk via palace-codemap `search_text` + `read_range` this dispatch (call `:105`, def `:110`, `M(A.size())` `:122`, diagonal apply `:129` / Dot `:131`, off-diagonal Dot `:138`, inverse `:151-152`). The chapter's evidence IS the driver-source range + the per-stage site map to the constituent ops (the adapted surface-or-evidence form for the feature-surface kind); as a cited ground-truth surface its rank is firm.
```

### Column 3 — electrostatic (driver-leaf; fixed-operator exemplar)

#### electrostatic.L4.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/electrostatic.L4.md
---
kind: feature-surface
feature: electrostatic
level: L4
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L4/fe_assemble
      kind: composes
    - target: L4/solve_family
      kind: composes
    - target: L4/ksp_solve
      kind: composes
    - target: L4/gram_reduce
      kind: composes
    - target: palace/drivers/electrostaticsolver.cpp:21-98
      kind: cites-evidence
  reference:
    - feature/capacitance.L4
---
```

#### electrostatic.L4.md — §"The composition" stage-3 reduction label (`:41`)

```edit:book/src/feature/electrostatic.L4.md
3. **Capacitance-matrix reduction** — [`gram_reduce`](../L4/gram_reduce.md) (**firm**, c095), the `w = 1` voltage specialization. The capacitance matrix is the operator-weighted symmetric-Gram reduction `Cᵢⱼ = Vⱼᵀ K Vᵢ` over the solution family `[Vᵢ]` — the unit-weight (`w i j = 1`) member of the shared L4 reduction `gram_reduce K xs w = Gᵢⱼ = w(i,j)·(xⱼᵀ K xᵢ)` (the COMSOL energy formulation: `Cᵢᵢ = 2Uₑ(Vᵢ)/Vᵢ² ≡ ×1`, off-diagonals from the cross energy; named electrostatic specialization at `book/src/L4/gram_reduce.md:167-171`). `gram_reduce` is the **entry** (replace-and-propagate, CLAUDE.md §VOCABULARY-SHIFT redirect); the capacitance reduction re-expresses THROUGH it as the `w = 1` corner, NOT as a hand-rolled fold. The combinator's `map`-then-`reduce` body composes two firm L1 building blocks — the diagonal `Vᵢᵀ K Vᵢ` is the firm [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) radicand (firm c091), the off-diagonal `Vⱼᵀ K Vᵢ` is the firm [`bilinear-form`](../L1/bilinear-form.md) (firm c095) — folded over the upper-triangle family-pair grid with the symmetric mirror; `gram_reduce` firmed at cycle-095 once both folded primitives firmed. The inverse (`Cinv = C⁻¹`, LAPACK) is the `gram_inverse` consumer downstream of the reduction, not part of it. This stage is the **output product** half of the composition root — authored in full as its dedicated output-product feature column [`capacitance`](./capacitance.L4.md), which links back DOWN to this driver as its producing column. L0: `PostprocessTerminals` (`electrostaticsolver.cpp:95`, def `:100`; the energy-form `Mult`/`Dot` at `:118-127`, the inverse at `:139-140`).
```

#### electrostatic.L4.md — §"Why this is the cleanest exemplar" lead + bullets (`:50-54`)

```edit:book/src/feature/electrostatic.L4.md
The electrostatic feature is the cleanest composition root because **every stage composes a firm L4 combinator with no obstruction at the composition level**:

- The assemble is a single-term `fe_assemble` (law 5; no multi-term concatenation needed) — the simplest possible use of the assemble-fold.
- The solve family is `solve_family`'s **fixed-operator** corner — the operator-capture-once specialization that *holds* the concatenation-homomorphism (the cleanest member; the driven pipeline's per-ω-rebuilt operator is the harder `per-element` superset, NOT this feature).
- The reduction is the firm [`gram_reduce`](../L4/gram_reduce.md) fold of firm bilinear-form evaluations (firm diagonal + firm off-diagonal) — no iterative obstruction.
```

#### electrostatic.L4.md — §"Why this is the cleanest exemplar" closing (`:56`)

```edit:book/src/feature/electrostatic.L4.md
The whole feature therefore lowers cleanly outward to the L4 backend surface: `electrostatic = capacitance_reduce ∘ solve_family ∘ fe_assemble`, a three-stage pipeline of combinators with a single shared operator capture. Under the OWN-COMPOSITION promotion rule (a column promotes off `seed` when its OWN composition + directly-owned constituents are firm; cross-linked sibling columns are references, NOT blockers) this column is **firm**: all its directly-owned constituents are firm — [`fe_assemble`](../L4/fe_assemble.md) (firm), [`solve_family`](../L4/solve_family.md) (firm c086), [`ksp_solve`](../L4/ksp_solve.md) (firm), and the stage-(3) readout [`gram_reduce`](../L4/gram_reduce.md) (firm c095, once both its folded L1 bilinear primitives firmed — `matrix-weighted-norm` c091 + `bilinear-form` c095). The cross-linked output-product column [`capacitance`](./capacitance.L4.md) is a SIBLING reference, not a blocker.
```

#### electrostatic.L4.md — §"Constituent down-links" table (`:60-65`)

```edit:book/src/feature/electrostatic.L4.md
| Stage | L4 combinator | Status | L0 site |
|---|---|---|---|
| assemble K once | [`fe_assemble`](../L4/fe_assemble.md) | firm | `electrostaticsolver.cpp:30` |
| per-terminal solve map | [`solve_family`](../L4/solve_family.md) | firm | `electrostaticsolver.cpp:34-36, 59, 68-69, 89` |
| per-element solve cap | [`ksp_solve`](../L4/ksp_solve.md) | firm | `electrostaticsolver.cpp:69` |
| capacitance reduction (Vⱼᵀ K Vᵢ) | [`gram_reduce`](../L4/gram_reduce.md) (`w = 1` voltage specialization) — folding L1 [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (diagonal, firm c091) / [`bilinear-form`](../L1/bilinear-form.md) (off-diagonal, firm c095) | firm (c095) | `electrostaticsolver.cpp:95, 100-138` |
```

#### electrostatic.L4.md — §Status (`:69`)

```edit:book/src/feature/electrostatic.L4.md
`firm` (promoted `seed`→`firm` at cycle-095, the `bilinear-form-firm-flip-and-cascade-wave`) — the first feature-surface composition-root authored under the FEATURE-SURFACE SPINE directive (2026-06-02). **Re-evaluated under the OWN-COMPOSITION promotion rule** (CLAUDE.md §Extraction-goal FEATURE-SURFACE SPINE; memory `project_feature_column_promotion_rule`): a column promotes off `seed` when its OWN composition + directly-owned constituents are firm; cross-linked sibling columns are references, NOT blockers. This column **is firm**: all its directly-owned constituents are firm — [`fe_assemble`](../L4/fe_assemble.md) (firm), [`solve_family`](../L4/solve_family.md) (firm c086, the firm-on-positive-structure / syntactic-identity escape), [`ksp_solve`](../L4/ksp_solve.md) (firm), and the stage-(3) [`gram_reduce`](../L4/gram_reduce.md) `w = 1` voltage-specialization capacitance reduction, which firmed at cycle-095 once both its folded L1 bilinear primitives firmed (the diagonal [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) firmed c091; the off-diagonal [`bilinear-form`](../L1/bilinear-form.md) firmed c095, the cascade — D1 enacted the firm-on-positive-structure escape, clearing `gram_reduce`'s sole residual gate so D3 firmed it). The cross-linked output-product column [`capacitance`](./capacitance.L4.md) is a SIBLING reference, not a blocker. This chapter carries the *compositional* claim (electrostatic = this composition of these constituent pieces), not the constituents' per-op algebraic claims (those live in the linked chapters). Evidence: the L0 driver range `electrostaticsolver.cpp:21-98` (`Solve`) + `:100-138` (`PostprocessTerminals`) realizing the composition, plus the firm constituent down-links.
```

#### electrostatic.L1.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/electrostatic.L1.md
---
kind: feature-surface
feature: electrostatic
level: L1
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L1/fe_assemble
      kind: composes
    - target: L1/ksp_solve
      kind: composes
    - target: L1/matrix-weighted-norm
      kind: folds
    - target: L1/bilinear-form
      kind: folds
    - target: palace/drivers/electrostaticsolver.cpp:21-98
      kind: cites-evidence
  reference:
    - feature/capacitance.L1
---
```

#### electrostatic.L1.md — intro paragraph "four firm L1 operators" (`:19`)

```edit:book/src/feature/electrostatic.L1.md
At L1 the electrostatic feature is a pure function `config → capacitance matrix` built from four firm L1 operators, with the **mutation already lifted** (each operator is mutation-free; the L0 in-place `ksp.Mult(RHS, V[step])` / `M_elec->Mult(...)` writes are lifted to value-returning forms per the L1>L0 mutation rotation).
```

#### electrostatic.L1.md — §"The composition" stage-3 lead (`:36`)

```edit:book/src/feature/electrostatic.L1.md
3. **Capacitance-matrix reduction** — the symmetric matrix `Cᵢⱼ = Vⱼᵀ K Vᵢ`, built from L1 bilinear-form evaluations (firm diagonal + firm off-diagonal):
```

#### electrostatic.L1.md — §"The composition" stage-3 off-diagonal bullet (`:38`)

```edit:book/src/feature/electrostatic.L1.md
   - off-diagonal `Cᵢⱼ = Vⱼᵀ K Vᵢ` — the operator-weighted cross-pairing, the firm [`bilinear-form`](../L1/bilinear-form.md) `α = xᴴ M y` instantiated `⟨Vⱼ, K Vᵢ⟩` (L0 `:122-127`, the same `Mult`/`Dot` with the `j` grid function).
```

#### electrostatic.L1.md — §"The composition" stage-3 closing (`:39`)

```edit:book/src/feature/electrostatic.L1.md
   The result is the symmetric `C` (and its LAPACK inverse `Cinv`, `:139-140`). This stage is a pure fold of bilinear-form evaluations over the solution-family pair grid — no L1 operator is *new* here; the reduction composes [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (firm c091) + [`bilinear-form`](../L1/bilinear-form.md) (firm c095).
```

#### electrostatic.L1.md — §"Constituent down-links" table (`:56-61`)

```edit:book/src/feature/electrostatic.L1.md
| Stage | L1 operator | Status | L0 site |
|---|---|---|---|
| assemble K once | [`fe_assemble`](../L1/fe_assemble.md) | firm | `electrostaticsolver.cpp:30` |
| per-terminal solve | [`ksp_solve`](../L1/ksp_solve.md) | firm | `electrostaticsolver.cpp:59, 68-69` |
| diagonal Vᵢᵀ K Vᵢ | [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) | firm c091 | `electrostaticsolver.cpp:118-119` |
| off-diagonal Vⱼᵀ K Vᵢ | [`bilinear-form`](../L1/bilinear-form.md) | firm c095 | `electrostaticsolver.cpp:122-127` |
```

#### electrostatic.L1.md — §Status (`:65`)

```edit:book/src/feature/electrostatic.L1.md
`firm` (promoted `seed`→`firm` at cycle-095, the `bilinear-form-firm-flip-and-cascade-wave`) — the L1 pure-function composition root for the electrostatic feature, authored under the FEATURE-SURFACE SPINE directive (2026-06-02). All four composed L1 operators are now firm — [`fe_assemble`](../L1/fe_assemble.md) (firm), [`ksp_solve`](../L1/ksp_solve.md) (firm), and the two capacitance-reduction primitives, the diagonal [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (firm c091) and the off-diagonal [`bilinear-form`](../L1/bilinear-form.md) (firm c095, this cycle's cascade — its `α = xᴴ M y` signature covers the cross-pairing). **Under the OWN-COMPOSITION rule (USER DIRECTIVE 2026-06-03) a column promotes off `seed` when its OWN directly-owned constituents are firm; this column is firm because all four are.** The cross-linked output-product column [`capacitance.L1`](./capacitance.L1.md) is a SIBLING reference, not a blocker. The chapter carries the compositional claim only; per-op algebraic claims live in the linked chapters. Evidence: the L0 driver range `electrostaticsolver.cpp:21-98` + `:100-138` realizing the composition, plus the firm L1 constituent down-links.
```

#### electrostatic.L0.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/electrostatic.L0.md
---
kind: feature-surface
feature: electrostatic
level: L0
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: palace/drivers/electrostaticsolver.cpp:21-98
      kind: cites-evidence
    - target: palace/drivers/electrostaticsolver.cpp:100-160
      kind: cites-evidence
    - target: palace/drivers/electrostaticsolver.hpp:34-44
      kind: cites-evidence
  reference:
    - feature/electrostatic.L1
---
```

#### electrostatic.L0.md — §Status (`:47`)

```edit:book/src/feature/electrostatic.L0.md
`firm` (promoted `seed`→`firm` at cycle-095 alongside its L1/L4 levels, the `bilinear-form-firm-flip-and-cascade-wave`) — the L0 ground-truth surface for the electrostatic feature, authored under the FEATURE-SURFACE SPINE directive (2026-06-02). Every stage is a cited range into `palace/drivers/electrostaticsolver.cpp`, confirmed on-disk via palace-codemap `read_range` this dispatch. The chapter's evidence IS the driver-source range + the per-stage site map to the constituent ops (the adapted surface-or-evidence form for the feature-surface kind); as a cited ground-truth surface its rank is firm.
```

### Column 4 — magnetostatic (driver-leaf; second fixed-operator witness)

#### magnetostatic.L4.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/magnetostatic.L4.md
---
kind: feature-surface
feature: magnetostatic
level: L4
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L4/fe_assemble
      kind: composes
    - target: L4/solve_family
      kind: composes
    - target: L4/ksp_solve
      kind: composes
    - target: L4/gram_reduce
      kind: composes
    - target: palace/drivers/magnetostaticsolver.cpp:22-108
      kind: cites-evidence
  reference:
    - feature/inductance.L4
---
```

#### magnetostatic.L4.md — §"The composition" stage-3 reduction label (`:41`)

```edit:book/src/feature/magnetostatic.L4.md
3. **Inductance-matrix reduction** — [`gram_reduce`](../L4/gram_reduce.md) (**firm**, c095), the `w = 1/(IᵢIⱼ)` current-normalized specialization. The inductance matrix is the operator-weighted symmetric-Gram reduction `Mᵢⱼ = (Aⱼᵀ K Aᵢ)/(Iᵢ Iⱼ)` over the solution family `[Aᵢ]` — the current-normalized (`w i j = 1/(Iᵢ Iⱼ)`) member of the shared L4 reduction `gram_reduce K xs w = Gᵢⱼ = w(i,j)·(xⱼᵀ K xᵢ)` (the COMSOL magnetic-energy formulation: `Mᵢᵢ = 2Uₘ(Aᵢ)/Iᵢ²`, off-diagonals from the cross energy, normalized by the excitation currents `Iᵢ`; named magnetostatic specialization at `book/src/L4/gram_reduce.md:172-176`). It is the **same** reduction as the [electrostatic](./electrostatic.L4.md) capacitance Gram — differing **only** in the weight closure (`w = 1/(IᵢIⱼ)` current vs `w = 1` voltage; the operator `M_mag` and family `[Aᵢ]` are leaf-content absorbed into `K` and `xs`). `gram_reduce` is the **entry** (replace-and-propagate, CLAUDE.md §VOCABULARY-SHIFT redirect); the inductance reduction re-expresses THROUGH it as the current-normalized corner, NOT as a hand-rolled fold. The combinator's `map`-then-`reduce` body composes two firm L1 building blocks — the diagonal `Aᵢᵀ K Aᵢ` is the firm [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) radicand (firm c091), the off-diagonal `Aⱼᵀ K Aᵢ` is the firm [`bilinear-form`](../L1/bilinear-form.md) (firm c095), each scaled by the current normalization absorbed into `w` — folded over the upper-triangle family-pair grid with the symmetric mirror; `gram_reduce` firmed at cycle-095 once both folded primitives firmed. The inverse (`Minv = M⁻¹`, LAPACK) is the `gram_inverse` consumer downstream of the reduction, not part of it. This stage is the **output product** half of the composition root — authored in full as its dedicated output-product feature column [`inductance`](./inductance.L4.md), which links back DOWN to this driver as its producing column. L0: `PostprocessTerminals` (`magnetostaticsolver.cpp:108`, def `:110`; the energy-form `Mult`/`Dot` at `:129-138`, the inverse at `:151-152`).
```

#### magnetostatic.L4.md — §"Why this composes cleanly" lead + bullets (`:50-54`)

```edit:book/src/feature/magnetostatic.L4.md
The magnetostatic feature composes as cleanly as [electrostatic](./electrostatic.L4.md) because **every stage composes a firm L4 combinator with no obstruction at the composition level**:

- The assemble is a single-term `fe_assemble` (law 5; no multi-term concatenation needed) — the ∇× witness of the assemble-fold.
- The solve family is `solve_family`'s **fixed-operator** corner — the operator-capture-once specialization that *holds* the concatenation-homomorphism (the second witness; the driven pipeline's per-ω-rebuilt operator is the harder `per-element` superset, NOT this feature).
- The reduction is the firm [`gram_reduce`](../L4/gram_reduce.md) fold of firm bilinear-form evaluations (firm diagonal + firm off-diagonal), differing from electrostatic's only in the per-element current normalization `Iᵢ Iⱼ` (a scalar weight absorbed into the fold) — no iterative obstruction.
```

#### magnetostatic.L4.md — §"Why this composes cleanly" closing (`:56`)

```edit:book/src/feature/magnetostatic.L4.md
The whole feature therefore lowers cleanly outward to the L4 backend surface: `magnetostatic = inductance_reduce ∘ solve_family ∘ fe_assemble`, a three-stage pipeline of combinators with a single shared operator capture. Under the OWN-COMPOSITION promotion rule (a column promotes off `seed` when its OWN composition + directly-owned constituents are firm; cross-linked sibling columns are references, NOT blockers) this column is **firm**: all its directly-owned constituents are firm — [`fe_assemble`](../L4/fe_assemble.md) (firm), [`solve_family`](../L4/solve_family.md) (firm c086), [`ksp_solve`](../L4/ksp_solve.md) (firm), and the stage-(3) readout [`gram_reduce`](../L4/gram_reduce.md) (firm c095, once both its folded L1 bilinear primitives firmed — `matrix-weighted-norm` c091 + `bilinear-form` c095). The cross-linked output-product column [`inductance`](./inductance.L4.md) is a SIBLING reference, not a blocker.
```

#### magnetostatic.L4.md — §"Constituent down-links" table (`:60-65`)

```edit:book/src/feature/magnetostatic.L4.md
| Stage | L4 combinator | Status | L0 site |
|---|---|---|---|
| assemble K once | [`fe_assemble`](../L4/fe_assemble.md) | firm | `magnetostaticsolver.cpp:29` |
| per-source solve map | [`solve_family`](../L4/solve_family.md) | firm | `magnetostaticsolver.cpp:34-35, 66, 76-77, 99` |
| per-element solve cap | [`ksp_solve`](../L4/ksp_solve.md) | firm | `magnetostaticsolver.cpp:77` |
| inductance reduction (Aⱼᵀ K Aᵢ / Iᵢ Iⱼ) | [`gram_reduce`](../L4/gram_reduce.md) (`w = 1/(IᵢIⱼ)` current-normalized specialization) — folding L1 [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (diagonal, firm c091) / [`bilinear-form`](../L1/bilinear-form.md) (off-diagonal, firm c095) | firm (c095) | `magnetostaticsolver.cpp:108, 110-152` |
```

#### magnetostatic.L4.md — §Status (`:69`)

```edit:book/src/feature/magnetostatic.L4.md
`firm` (promoted `seed`→`firm` at cycle-095, the `bilinear-form-firm-flip-and-cascade-wave`) — the second feature-surface composition-root authored under the FEATURE-SURFACE SPINE directive (2026-06-02), mirroring the [electrostatic](./electrostatic.L4.md) exemplar. **Re-evaluated under the OWN-COMPOSITION promotion rule** (CLAUDE.md §Extraction-goal FEATURE-SURFACE SPINE; memory `project_feature_column_promotion_rule`): a column promotes off `seed` when its OWN composition + directly-owned constituents are firm; cross-linked sibling columns are references, NOT blockers. This column **is firm**: all its directly-owned constituents are firm — [`fe_assemble`](../L4/fe_assemble.md) (firm), [`solve_family`](../L4/solve_family.md) (firm c086, the second witness of the fixed-operator corner, the firm-on-positive-structure / syntactic-identity escape), [`ksp_solve`](../L4/ksp_solve.md) (firm), and the stage-(3) [`gram_reduce`](../L4/gram_reduce.md) `w = 1/(IᵢIⱼ)` current-normalized inductance reduction (the same shared symmetric-Gram reduction as the electrostatic capacitance, the weight the only difference), which firmed at cycle-095 once both its folded L1 bilinear primitives firmed (the diagonal [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) firmed c091; the off-diagonal [`bilinear-form`](../L1/bilinear-form.md) firmed c095, the cascade — D1 enacted the firm-on-positive-structure escape, clearing `gram_reduce`'s sole residual gate so D3 firmed it). The cross-linked output-product column [`inductance`](./inductance.L4.md) is a SIBLING reference, not a blocker. This chapter carries the *compositional* claim (magnetostatic = this composition of these constituent pieces), not the constituents' per-op algebraic claims (those live in the linked chapters). Evidence: the L0 driver range `magnetostaticsolver.cpp:22-108` (`Solve`) + `:110-204` (`PostprocessTerminals`) realizing the composition, plus the firm constituent down-links.
```

#### magnetostatic.L1.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/magnetostatic.L1.md
---
kind: feature-surface
feature: magnetostatic
level: L1
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: L1/fe_assemble
      kind: composes
    - target: L1/ksp_solve
      kind: composes
    - target: L1/matrix-weighted-norm
      kind: folds
    - target: L1/bilinear-form
      kind: folds
    - target: palace/drivers/magnetostaticsolver.cpp:22-108
      kind: cites-evidence
  reference:
    - feature/inductance.L1
---
```

#### magnetostatic.L1.md — intro paragraph "four firm/rough-in L1 operators" (`:19`)

```edit:book/src/feature/magnetostatic.L1.md
At L1 the magnetostatic feature is a pure function `config → inductance matrix` built from four firm L1 operators, with the **mutation already lifted** (each operator is mutation-free; the L0 in-place `ksp.Mult(RHS, A[step])` / `M_mag->Mult(...)` writes are lifted to value-returning forms per the L1>L0 mutation rotation).
```

#### magnetostatic.L1.md — §"The composition" stage-3 lead (`:36`)

```edit:book/src/feature/magnetostatic.L1.md
3. **Inductance-matrix reduction** — the symmetric matrix `Mᵢⱼ = (Aⱼᵀ K Aᵢ)/(Iᵢ Iⱼ)`, built from L1 bilinear-form evaluations (firm diagonal + firm off-diagonal), each normalized by the excitation currents:
```

#### magnetostatic.L1.md — §"The composition" stage-3 off-diagonal bullet (`:38`)

```edit:book/src/feature/magnetostatic.L1.md
   - off-diagonal `Mᵢⱼ = (Aⱼᵀ K Aᵢ)/(Iᵢ Iⱼ)` — the operator-weighted cross-pairing normalized by the current product, the firm [`bilinear-form`](../L1/bilinear-form.md) `α = xᴴ M y` instantiated `⟨Aⱼ, K Aᵢ⟩` (L0 `:135-138`, the same `Mult`/`Dot` with the `j` grid function, then `/ (I_inc[i]*I_inc[j])`).
```

#### magnetostatic.L1.md — §"The composition" stage-3 closing (`:39`)

```edit:book/src/feature/magnetostatic.L1.md
   The result is the symmetric `M` (and its LAPACK inverse `Minv`, `:151-152`). This stage is a pure fold of current-normalized bilinear-form evaluations over the solution-family pair grid — no L1 operator is *new* here; the reduction composes [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (firm c091) + [`bilinear-form`](../L1/bilinear-form.md) (firm c095), with the current normalization a scalar weight on each entry.
```

#### magnetostatic.L1.md — §"Constituent down-links" table (`:56-61`)

```edit:book/src/feature/magnetostatic.L1.md
| Stage | L1 operator | Status | L0 site |
|---|---|---|---|
| assemble K once | [`fe_assemble`](../L1/fe_assemble.md) | firm | `magnetostaticsolver.cpp:29` |
| per-source solve | [`ksp_solve`](../L1/ksp_solve.md) | firm | `magnetostaticsolver.cpp:66, 76-77` |
| diagonal Aᵢᵀ K Aᵢ / Iᵢ² | [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) | firm c091 | `magnetostaticsolver.cpp:129-131` |
| off-diagonal Aⱼᵀ K Aᵢ / Iᵢ Iⱼ | [`bilinear-form`](../L1/bilinear-form.md) | firm c095 | `magnetostaticsolver.cpp:135-138` |
```

#### magnetostatic.L1.md — §Status (`:65`)

```edit:book/src/feature/magnetostatic.L1.md
`firm` (promoted `seed`→`firm` at cycle-095, the `bilinear-form-firm-flip-and-cascade-wave`) — the L1 pure-function composition root for the magnetostatic feature, authored under the FEATURE-SURFACE SPINE directive (2026-06-02), mirroring the [electrostatic.L1](./electrostatic.L1.md) exemplar. All four composed L1 operators are now firm — [`fe_assemble`](../L1/fe_assemble.md) (firm), [`ksp_solve`](../L1/ksp_solve.md) (firm), and the two inductance-reduction primitives, the diagonal [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) (firm c091) and the off-diagonal [`bilinear-form`](../L1/bilinear-form.md) (firm c095, this cycle's cascade — its `α = xᴴ M y` signature covers the cross-pairing). **Under the OWN-COMPOSITION rule (USER DIRECTIVE 2026-06-03) a column promotes off `seed` when its OWN directly-owned constituents are firm; this column is firm because all four are.** The cross-linked output-product column [`inductance.L1`](./inductance.L1.md) is a SIBLING reference, not a blocker. The chapter carries the compositional claim only; per-op algebraic claims live in the linked chapters. Evidence: the L0 driver range `magnetostaticsolver.cpp:22-108` + `:110-204` realizing the composition, plus the firm L1 constituent down-links.
```

#### magnetostatic.L0.md — frontmatter SPLIT + typed edges

```edit:book/src/feature/magnetostatic.L0.md
---
kind: feature-surface
feature: magnetostatic
level: L0
feature_root: seed
rank: firm
edges:
  depends-on:
    - target: palace/drivers/magnetostaticsolver.cpp:22-108
      kind: cites-evidence
    - target: palace/drivers/magnetostaticsolver.cpp:110-204
      kind: cites-evidence
    - target: palace/drivers/magnetostaticsolver.hpp:24-39
      kind: cites-evidence
  reference:
    - feature/magnetostatic.L1
---
```

#### magnetostatic.L0.md — §Status (`:47`)

```edit:book/src/feature/magnetostatic.L0.md
`firm` (promoted `seed`→`firm` at cycle-095 alongside its L1/L4 levels, the `bilinear-form-firm-flip-and-cascade-wave`) — the L0 ground-truth surface for the magnetostatic feature, authored under the FEATURE-SURFACE SPINE directive (2026-06-02), mirroring the [electrostatic.L0](./electrostatic.L0.md) exemplar. Every stage is a cited range into `palace/drivers/magnetostaticsolver.cpp`, confirmed on-disk via palace-codemap `read_range` this dispatch. The chapter's evidence IS the driver-source range + the per-stage site map to the constituent ops (the adapted surface-or-evidence form for the feature-surface kind); as a cited ground-truth surface its rank is firm.
```

### feature/index.md — the column-status matrix narration + `## Chapter-kind status` (SOLE owner)

The matrix TABLE (`:28-44`) carries no per-column status cells (it is a name × level link grid), so it needs no edit. The status lives in the narrative paragraphs below it and in `## Chapter-kind status`. Three edits:

#### feature/index.md — the OWN-COMPOSITION promotion paragraph (`:55`)

```edit:book/src/feature/index.md
Under the OWN-COMPOSITION promotion rule (CLAUDE.md §Extraction-goal FEATURE-SURFACE SPINE; user directive 2026-06-03; memory `project_feature_column_promotion_rule`), a feature column promotes off `seed` when its **OWN composition + directly-owned constituents** are firm; **cross-linked sibling columns are references, NOT blockers**. After the cycle-095 `bilinear-form-firm-flip-and-cascade-wave`, ALL FIVE energy-Gram-family columns whose own reduce verb is now firm have promoted. The three output-product columns whose own reduce verb firmed earlier — [`eigenfrequency-qfactor`](./eigenfrequency-qfactor.L4.md) (own verb `eigenfreq_qfactor_reduce`, firm c082), [`sparameters`](./sparameters.L4.md) (own verb `sparameter_reduce`, firm c083), and [`energy-fields`](./energy-fields.L4.md) (own verb `domain_energy_reduce`, firm c091) — promoted independent of their producing driver cross-links. The remaining cascade closed at cycle-095: the shared reduce verb [`gram_reduce`](../L4/gram_reduce.md) firmed (its sole residual off-diagonal [`bilinear-form`](../L1/bilinear-form.md) folded primitive firmed via D1 on the firm-on-positive-structure escape — the diagonal [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md) had firmed c091 — so both folded primitives are firm and D3 firmed `gram_reduce`), which firmed its two output-product columns [`capacitance`](./capacitance.L4.md) / [`inductance`](./inductance.L4.md) AND its two producing driver columns [`electrostatic`](./electrostatic.L4.md) / [`magnetostatic`](./magnetostatic.L4.md) (whose own remaining rough-in constituent was that same `gram_reduce` stage-(3) readout). (This OWN-COMPOSITION rule supersedes the earlier "promote only once ALL composed constituents — including cross-linked sibling columns — are firm" rule, which combined with the output-product↔driver reciprocal cross-link to make `seed` a permanent terminal state — the mutual-blocking deadlock the directive breaks.)
```

#### feature/index.md — the spine-scope closing paragraph (`:57`)

```edit:book/src/feature/index.md
The FEATURE-SURFACE SPINE directive scope is fully authored (cycle-078 landed the last output product [`energy-fields`](./energy-fields.L4.md) and the 6th-`ProblemType` wave-port / [`boundary-mode`](./boundary-mode.L4.md) driver column, a co-equal leaf driver column under the lifecycle ROOT). Cycle-085 ran the all-12-column re-evaluation under the OWN-COMPOSITION promotion rule, and cycle-091 + cycle-095 closed the gram-Gram cascade; after cycle-095 only [`boundary-mode`](./boundary-mode.L4.md) remains `seed` (its stage-(3) readout reduces into a waveguide-mode output product with no firm home — an own-readout gate, the waveguide-mode product column being demand-gated). A column that cannot yet be cleanly composed — i.e. one of its directly-owned constituents is still rough-in or unhomed — stays `seed` as a *finding about the spine* (surfaced as an open question, the same low-priority test-load discipline the solvers carry on the vocabulary spine).
```

#### feature/index.md — the `## Chapter-kind status` split (`:61-71`)

```edit:book/src/feature/index.md
The feature-surface kind was established by the electrostatic column (the first exemplar, authored under the FEATURE-SURFACE SPINE user directive 2026-06-02 ahead of role-spec codification) and confirmed to scale by the magnetostatic + lifecycle second-wave columns (cycle-072; batch-22 meta-phase codified the kind into the role-specs + the directive-3 by-kind grouping). **Cycle-085 re-evaluated all 12 columns under the OWN-COMPOSITION promotion rule** (memory `project_feature_column_promotion_rule`); the cohort is no longer blanket-`seed`. **Cycle-091 + cycle-095 (the `bilinear-form-firm-flip-and-cascade-wave`) closed the energy-Gram cascade**: the `matrix-weighted-norm` firm flip (c091) firmed `domain_energy_reduce`, flipping [`energy-fields`](./energy-fields.L4.md) `seed`→`firm`; the `bilinear-form` firm flip (c095) firmed the shared `gram_reduce` verb, flipping the four `gram_reduce`-gated columns ([`capacitance`](./capacitance.L4.md), [`inductance`](./inductance.L4.md), [`electrostatic`](./electrostatic.L4.md), [`magnetostatic`](./magnetostatic.L4.md)) `seed`→`firm` in the same cascade. The current split:

- **`firm` (11 columns)** — own composition + directly-owned constituents all firm; cross-linked sibling columns are references, not blockers:
  - driver-leaf: [`eigenmode`](./eigenmode.L4.md) (own `fe_assemble`×3 + `eigsolve` firm; `eigenfrequency-qfactor` is a sibling cross-link), [`driven`](./driven.L4.md) (own `fe_assemble` + `assemble_frequency_operator` + `frequency_sweep` + `ksp_solve` firm; `sparameters` is a sibling cross-link), [`transient`](./transient.L4.md) (own `fe_assemble` + `fold_solve` firm; no output-product sibling), [`electrostatic`](./electrostatic.L4.md) (own `fe_assemble` + `solve_family` c086 + `ksp_solve` + stage-(3) `gram_reduce` firm c095; `capacitance` is a sibling cross-link — promoted `seed`→`firm` c095 by the cascade), [`magnetostatic`](./magnetostatic.L4.md) (own `fe_assemble` + `solve_family` c086 + `ksp_solve` + stage-(3) `gram_reduce` firm c095; `inductance` is a sibling cross-link — promoted `seed`→`firm` c095 by the cascade).
  - output-product: [`eigenfrequency-qfactor`](./eigenfrequency-qfactor.L4.md) (own verb `eigenfreq_qfactor_reduce` firm c082; `eigenmode` is a sibling cross-link), [`sparameters`](./sparameters.L4.md) (own verb `sparameter_reduce` firm c083; `driven` is a sibling cross-link), [`energy-fields`](./energy-fields.L4.md) (own verb `domain_energy_reduce` firm c091 + own folded `matrix-weighted-norm` firm c091 + `participation_ratio` firm c077 — promoted `seed`→`firm` cycle-091; driver-agnostic, the field-bearing driver columns are sibling references not blockers), [`capacitance`](./capacitance.L4.md) (own verb `gram_reduce` firm c095 — the voltage `w = 1` Gram; `electrostatic` is a sibling cross-link — promoted `seed`→`firm` c095 by the cascade), [`inductance`](./inductance.L4.md) (own verb `gram_reduce` firm c095 — the current-normalized `w = 1/(IᵢIⱼ)` Gram; `magnetostatic` is a sibling cross-link — promoted `seed`→`firm` c095 by the cascade).
  - spine-ROOT: [`lifecycle`](./lifecycle.L4.md) (own driver-agnostic composition — mesh-build + the firm `fold_solve` adaptive fold — firm; the per-driver dispatch is over sibling feature columns, references not blockers).
- **`seed` (1 column)** — held on a genuine **own-constituent gate** (a directly-owned constituent is rough-in or unhomed), NOT a sibling-column blocker:
  - [`boundary-mode`](./boundary-mode.L4.md) — own stage-(3) readout reduces into a waveguide-mode output product with no firm home (own-readout gate; the waveguide-mode product column is demand-gated).
```

### feature/output-product.md — group-intro sibling-status line (the index-cell-drift guard)

The `energy-fields` bullet (`:9`) is already correct (firm). The two `gram_reduce` bullets (capacitance `:7`, inductance `:10`) do not carry a status word inline — they describe the specialization, which is unchanged. The load-bearing edit is the SUPERSEDED closing line (`:13`):

```edit:book/src/feature/output-product.md
Columns are alpha-ordered within this grouping. The within-column level ordering is **high→low** (L4 → L1 → L0), the deliberate FEATURE-SURFACE exception to alpha-within-cohort ordering. With [`energy-fields`](./energy-fields.L4.md) (cycle-078) the output-product cohort is complete (5 columns), and after the cycle-091 + cycle-095 energy-Gram cascade **all 5 output-product columns are `firm`** under the OWN-COMPOSITION rule (a column promotes off `seed` when its OWN reduce verb + directly-owned constituents are firm; cross-linked sibling driver columns are references, NOT blockers): [`eigenfrequency-qfactor`](./eigenfrequency-qfactor.L4.md) (own verb firm c082), [`sparameters`](./sparameters.L4.md) (own verb firm c083), [`energy-fields`](./energy-fields.L4.md) (own verb firm c091), and [`capacitance`](./capacitance.L4.md) + [`inductance`](./inductance.L4.md) (own verb [`gram_reduce`](../L4/gram_reduce.md) firm c095, once its off-diagonal `bilinear-form` folded primitive firmed).
```

### feature/driver-leaf.md — group-intro sibling-status line (the index-cell-drift guard)

The SUPERSEDED closing line (`:14`) — "All columns stay `seed` until every composed constituent is firm" — was already stale (driven/eigenmode/transient firm) and now must reflect all 6 drivers' status post-cascade:

```edit:book/src/feature/driver-leaf.md
Columns are alpha-ordered within this grouping. The within-column level ordering is **high→low** (L4 → L1 → L0), the deliberate FEATURE-SURFACE exception to alpha-within-cohort ordering. The `boundary-mode` column is the 6th co-equal leaf driver — the `BoundaryModeSolver` `ProblemType` dispatch branch (`palace/main.cpp:276-278`), routed through the same `switch` as the other 5; it composes the same opaque-library [`eigsolve`](../L4/eigsolve.md) corner as `eigenmode`, distinguished by its 3D-boundary → 2D-submesh extraction preface. Under the OWN-COMPOSITION promotion rule (a column promotes off `seed` when its OWN composition + directly-owned constituents are firm; cross-linked sibling columns are references, NOT blockers), **5 of the 6 driver columns are `firm`** — [`driven`](./driven.L4.md), [`eigenmode`](./eigenmode.L4.md), [`transient`](./transient.L4.md), and (promoted `seed`→`firm` cycle-095 by the `bilinear-form-firm-flip-and-cascade-wave`, once their stage-(3) `gram_reduce` readout firmed) [`electrostatic`](./electrostatic.L4.md) + [`magnetostatic`](./magnetostatic.L4.md). Only [`boundary-mode`](./boundary-mode.L4.md) stays `seed`, on a genuine own-readout gate (its stage-(3) waveguide-mode output product has no firm home; the product column is demand-gated).
```

## Whole-`book/src/feature/` sibling-status grep (the column-flip coupled guard)

Per my role-spec (the `firm-promotion-coupled-re-anchor-needs-whole-book-cross-reference-grep` guard), a `seed`→`firm` column flip stales every SIBLING-column prose reference that still calls the now-firm column `(seed)` / `(**seed**)`. I flipped 4 columns; I ran `grep -rn '(seed\|\*\*seed\*\*\|status: seed' book/src/feature` and audited every genuinely-stale sibling-status mention of the 4 flipped columns:

- **`inductance.L4.md:7`** `composes:` string `magnetostatic.L4.md (seed — ...)` — re-anchored: the `composes:` block is fully replaced by the typed `edges:` block (which drops the maturity qualifier per scheme §4(c)), so the stale `(seed)` is gone.
- **`inductance.L1.md:7`** `composes:` `magnetostatic.L1.md (seed — ...)` — same, replaced by typed `edges:`.
- **`inductance.L4.md:34`** + **`inductance.L1.md:37`** prose `[magnetostatic.L4](...) (**seed**)` / `(**seed**)` — re-anchored to `(**firm**, sibling reference)` in the stage-1 edits above.
- The four flipped columns' OWN `status: seed` frontmatter + §Status `seed` prose + constituent-table `seed` cells — all re-anchored to firm in the per-column edits above.
- **`feature/index.md`**, **`feature/output-product.md`**, **`feature/driver-leaf.md`** sibling-status narration — re-anchored above (I own these surfaces this cycle).

**Cross-grouping check — the OTHER (already-firm, D5-owned) columns that cross-link the 4 I flipped (genuinely-stale `(seed)` mentions of now-firm siblings; FLAGGED for D5, NOT edited — down-links are read-only and these columns are in D5's partition):**

- **`book/src/feature/energy-fields.L4.md:7`** — `composes:` string `electrostatic.L4.md (seed — a producing driver column...)`. Stale: `electrostatic.L4` is firm c095.
- **`book/src/feature/energy-fields.L1.md:7`** — `composes:` string `electrostatic.L1.md (seed — a producing driver column...)`. Stale: `electrostatic.L1` is firm c095.
- **`book/src/feature/energy-fields.L1.md:43`** — prose `[electrostatic.L1](...) (**seed**, the potential V → E)`. Stale: `electrostatic.L1` is firm c095.
- **`book/src/feature/lifecycle.L4.md:7`** — `composes:` string `electrostatic.L4.md (seed — the ELECTROSTATIC ProblemType specialization)`. Stale: firm c095.
- **`book/src/feature/lifecycle.L4.md:8`** — `composes:` string `magnetostatic.L4.md (seed — the MAGNETOSTATIC ProblemType specialization)`. Stale: firm c095.

For ALL five: D5's `composes:`→`edges:` conversion drops the maturity qualifier (scheme §4(c)), resolving the composes-string cases (`energy-fields.L4:7`, `energy-fields.L1:7`, `lifecycle.L4:7-8`); the one prose `(**seed**)` case (`energy-fields.L1:43`) needs a one-word re-anchor to `(**firm**)` in D5's energy-fields pass. If D5's conversion does not land this cycle, the residual stale `(seed)` qualifiers are cosmetic — `linkcheck2` does not read them — and clear whenever those columns are next typed. **These are all in D5's partition (non-cascade columns); I did NOT edit them** (the down-link read-only rule).

**NOT stale (correctly left):** `eigenfrequency-qfactor.L4.md:68` references `eigenmode.L4` `(status: seed)` — `eigenmode` is NOT one of my 4 cascade columns (it stayed firm via its own composition c085; the `(status: seed)` there is the legacy root-marker `status:` token, not a stale composition-maturity claim, and is correct pending D5's `feature_root:`/`rank:` split). The output-product↔driver reciprocal cross-links in the other firm columns (`sparameters`↔`driven`, `eigenfrequency-qfactor`↔`eigenmode`) reference their OWN sibling, not the 4 I flipped.

## Supporting evidence

- **D1 firm-flip of `bilinear-form`** (the off-diagonal folded primitive): `reports/2026-06-04T204500Z-harvester-cycle-095-bilinear-form-firm-flip/CYCLE.md` (read this dispatch) — rough-in→firm on the firm-on-positive-structure escape, DISCHARGE c092, ENACT c095.
- **`gram_reduce` sole-residual-gate = bilinear-form**: `book/src/L4/gram_reduce.md:241-247` (read on disk) — "the off-diagonal building block `bilinear-form` is still rough-in … the sole RESIDUAL gate"; the diagonal `matrix-weighted-norm` half discharged c091 (`:238-240`). With bilinear-form firm (D1), both folded primitives are firm → D3 firms gram_reduce (firm-on-positive-structure escape, the reduce-verb-sibling precedents `domain_energy_reduce` c091 / `eigenfreq_qfactor_reduce` c082 / `sparameter_reduce` c083).
- **Constituent firmness survey from on-disk `## Status`/frontmatter** (NOT index cells, NOT cycle record): the table in §Summary above — `fe_assemble`/`solve_family`/`ksp_solve` firm frontmatter; `L1/ksp_solve.md:103` + `L1/matrix-weighted-norm.md:109` firm §Status.
- **Scheme §3 SPLIT + §4(c) migration**: `book/src/methodology/graded-stack-scheme.md:137-184` — `feature_root: seed` (permanent root marker) + `rank:` (composition-maturity); `composes:` vocabulary-op→`depends-on`, sibling-column→`reference`, `l0_ground_truth:`→`depends-on kind: cites-evidence`; maturity qualifiers dropped from edges.
- **OWN-COMPOSITION rule**: CLAUDE.md §Extraction-goal FEATURE-SURFACE SPINE + memory `project_feature_column_promotion_rule` — a column promotes off `seed` on its OWN composition; sibling columns are references, not blockers.

## Open questions / caveats

- **D2-flagged `L4/index.md:101` bilinear-form-in-column-gate narrative — NOT mine; FLAGGED for the integrator.** The planner asked me to handle the D2-flagged `L4/index.md:101` narrative "IF it's a column-gate prose you own — else flag for the integrator (L4/index count cells are contested this cycle)." It is NOT a feature-column surface (it is the L4 vocabulary index, whose count headers are explicitly HARD-OUT of my scope and "coordinated" per the brief). **I FLAG it for the integrator/D2:** if `L4/index.md:101` carries a "bilinear-form half remains rough-in" / `gram_reduce`-gated-on-bilinear-form narrative, it is staled by D1 (bilinear-form firm) + D3 (gram_reduce firm) this cycle and should be re-anchored to firm in whichever dispatch owns the `L4/index` count cells. I did not edit it (out of partition).
- **L0 column levels typed as `rank: firm` with `cites-evidence` depends-on edges.** The L0 ground-truth surfaces are cited Palace source; per scheme §4(c) their `l0_ground_truth:` entries become `depends-on kind: cites-evidence` edges, and the surface's own rank is firm (it is ground truth). The `cites-evidence` targets are file:line ranges, NOT book slugs — the rank linter should treat a `cites-evidence` edge as not-rank-constrained (a citation into Palace source is not a DAG node with a `rank:`). If the linter tries to resolve `palace/...:lo-hi` as a `book/src/<slug>.md` it will NOANC; this is the same shape every L0/evidence edge will take under P1, so it is a linter-convention question (does `kind: cites-evidence` exempt the target from slug-resolution + rank-check?) — **flag for the batch-30 meta-phase / D7 baseline-exceptions** if the linter does not already special-case it.
- **`feature_root: seed` + `rank:` dual-form on the flipped columns vs the legacy `status:`-only siblings.** My 4 cascade columns now carry the NEW `feature_root: seed` + `rank: firm` SPLIT, while the already-firm non-cascade columns (energy-fields/driven/…) still carry the LEGACY `status: firm` + `composes:` form until D5 converts them. This is the interim dual-form the planner's OQ `graded-stack-feature-root-frontmatter-split` anticipates (the linter accepts both). Confirm the linter's root-detection handles BOTH `status: seed` (legacy) and `feature_root: seed` (new) — if D4's split breaks root detection for the flipped columns, that is a linter-fix OQ for the batch-30 meta-phase.
- **`gram_reduce` firm is a forward-reference (D3).** My 4-column flip is gated on D3 firming `gram_reduce`. I determined D3's outcome (a) (firm) from the gate logic (sole residual = bilinear-form, cleared by D1) + the planner's explicit sequencing, NOT from a landed D3 report (D3's report dir did not exist at dispatch time — D3 is a co-dispatched Wave-2 sibling). **If D3 instead records outcome (b) (gram_reduce STAYS rough-in for an unforeseen reason), this entire 4-column flip must be reverted to `seed`** — the integrator should sequence D3 before D4's apply and, if D3's verdict is (b), DROP my column-flip proposed-changes (the columns stay seed with their existing prose). I have written the report for outcome (a) per the brief's primary path; the (b) fallback is "no edit, columns stay seed."
- **Five stale `(seed)` sibling-status mentions in D5-owned non-cascade columns — flagged for D5** (see §grep above): `energy-fields.L4.md:7`, `energy-fields.L1.md:7`, `energy-fields.L1.md:43` (prose `(**seed**)`), `lifecycle.L4.md:7`, `lifecycle.L4.md:8` — all stale references to my now-firm `electrostatic`/`magnetostatic` columns. Down-links are read-only; these columns are in D5's partition. D5's `composes:`→`edges:` conversion drops the qualifier on the four composes-string cases; the one prose `(**seed**)` (`energy-fields.L1:43`) needs a one-word `(**firm**)` re-anchor in D5's energy-fields pass. (`linkcheck2` does not read these qualifiers — non-blocking if D5 slips to c096.)
- **Output-product/driver-leaf group intros: I edited the closing sibling-status line of EACH.** These are group-intro pages I own as the feature-index single-owner this cycle (the index-cell-drift guard extends to the group-intro sibling-status narration). The bodies' per-column bullets carry no stale status beyond those lines. The `spine-root.md` group intro was not touched (lifecycle was already firm; no cascade column is in that grouping).
