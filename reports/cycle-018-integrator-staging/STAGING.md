# Cycle-018 integrator staging log

Per-report integration staging for cycle-018 (THIRD primary cycle of meta-batch-4 — meta-phase fires after this cycle).
One section per ready report, dispatched serially by the parent. Newest LAST, append-only.
integrator-finalize reads this log to run the book rebuild, mark `integrated_at`, write
`log/cycle-018.md`, append cycle-record + integrator-signals, update roadmap, and commit.

---

## harvester-linear-combination-L2
applied_at: 2026-05-29T021500Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L2/linear_combination.md (create — NEW firm L2 operator entry for `linear_combination :: [(Scalar, Tensor[N])] -> Tensor[N]` (≡ `foldl (\acc (a,t) -> acc + scal a t) (zeros N) pairs`), the arity-family unification of the BLAS-1 scalar-weighted-vector-sum cohort. Full operator content: one-line + Context [the four L1 leaves `scal`/`axpy`/`axpby`/`axpbypcz` as arity-1/2/2/3 specializations of one variadic fold; L2 is the fusion-rotation layer; NOT an L4 combinator — pure value-producing reduction, no monadic state/predicate], Signature [shape contract `all tᵢ : Tensor[N]` shared length axis + element-type `T ∈ {real,complex}` with `real ⊑ complex` scalar-promotion inherited; the four specialization identities], Semantics [pure/out-of-place `Σᵢ aᵢ·tᵢ`, element-local in N, no MPI collective; arity-3 L0 ceiling, >3 open-coded as iterated γ=1-accumulate], 7 holding algebraic laws [empty-list identity, concatenation-homomorphism = the defining/unifying law, multilinearity, coefficient-scaling, zero-coefficient term-drop = exact L0 `γ==0` branch at `vector.cpp:749-751`, specialization identities, permutation-invariance EXACT-ARITHMETIC] + 3 explicit non-laws [permutation under IEEE-754 = load-bearing summation order paired with law 7, re-fold idempotence, bit-level fusion identity vs multi-pass], Dependencies, two Variant axes [output-aliasing in-place-vs-out-of-place orthogonal to arity; element-type/scalar-promotion inherited with its open upstream dep], Fusion note, Sibling-fold `dot`-is-not-subsumed §, Status `firm` with empirical-match caveat, L2-vs-L1 distinction, Evidence [9 self-verified Palace L0 ranges]. Forward-refs to the forthcoming `L2-L1/linear-combination-fold-specialization` theme + the `inner_product` sibling are PLAIN-TEXT/backtick code-spans, NOT live `[...](...)` links — verified before apply, per the cycle-017 build-break lesson; no dangling-link build risk.)
- book/src/L2/index.md (edit ×1 — flipped the `linear_combination` dep-map row rough-in→firm [on-disk line 25]. Slug cell `linear_combination (chapter ./linear_combination.md to be authored by harvester)` → live link `[`linear_combination`](./linear_combination.md)`; Concepts cell `scalar-promotion (… already-unified sibling …)` → `scalar-promotion (… concept-page-level sibling … inherited with its open upstream dependency — OQ scalar-promotion-typing-rule, not yet committed)` [the repairer hedge-clause]; Status cell `(rough-in, proposed-by: combinator-miner:2026-05-28T223022Z)` → `firm (harvested cycle-018; promoted from rough-in proposed-by combinator-miner:2026-05-28T223022Z; constructive prong (b) of OQ blas1-variadic-linear-combination-fold-unification)`. Signature cell unchanged. OLD anchor matched disk byte-for-byte on fresh re-read.)
- book/src/SUMMARY.md (edit ×1 — registered the chapter `- [linear_combination](./L2/linear_combination.md)` as the last entry under the `# L2 — Algebraic Decompositions` Part, AFTER `chebyshev-iteration` [on-disk line 38], before the blank line separating from the `# L2 > L1 — Lowering` Part. Anchored on the `krylov-step`+`chebyshev-iteration` entries + the `# L2 > L1 — Lowering` header; matched disk byte-for-byte.)
- scaffolding/open-questions.md (append — one [RESOLUTION — cycle-018] note after the `linear-combination-harvester-formalization` OQ prose block [ledger ~L2967], recording RESOLVED: firm chapter authored + dep-map flip + SUMMARY register; harvester follow-up items (1)/(2) DONE, item (3) recorded as the honest empirical-match caveat [no dedicated test; firm on source-transcription per chebyshev-iteration precedent], item (4) the L2>L1 lowering theme NOTED as LANDING THIS SAME CYCLE as report 4; append-only, did NOT flip the `status:` YAML field [deferred to finalize].)

Gate hits:
- retroactive-budget: 0-slice (this report) / 0 global — net-new firm operator entry [create-file] + rough-in→firm dep-map flip + SUMMARY register; NO retroactive edits to existing firm slices. Running global tally opens at 0. NOTE: integrator-finalize sees the aggregate across all 5 reports.
- forward-edge-without-surface: 0 (this is an L2 operator entry carrying surface [the chapter] + full evidence [9 cited self-verified L0 ranges], NOT a lowering-edge forward-claim; the forward-refs to the L2>L1 theme + `inner_product` sibling are plain-text, not edge-without-surface claims)
- concept_writes-on-existing-slug: 0 (no concept page touched; `scalar-promotion` referenced as existing, not written)
- edge-label-prose-mismatch: 0 (n/a — L2 operator entry, carries no `L_{n+1}>L_n` edge label; critic edge-label-fidelity = pass — the forward-refs name the correct adjacent edges as forward-refs, not authored claims)
- H1-page-heading-reuse: 0 (the chapter H1 `# linear_combination` is unique; does not reuse a page/Part heading)
- append-on-missing-slug: 0 (create-file for the operator + anchored dep-map-row replace [existing rough-in row] + anchored SUMMARY insert; all anchors matched disk byte-for-byte; no append-to-missing-slug)
- variant-axis-missing: 0 (the entry enumerates the two orthogonal variant axes [output-aliasing in-place/out-of-place; element-type real|complex w/ scalar-promotion sub-axis] and correctly frames ARITY as the UNIFICATION axis [not a remaining variant]; critic variant-axis-coverage = warning→repaired [the inherited scalar-promotion sub-axis hedge], NOT missing)
- index-placeholder-displacement: 0 (the L2 dep-map table is firm content [krylov-step + chebyshev-iteration rows + the `linear_combination` rough-in row]; NOT a `(empty — Phase B skeleton.)` placeholder. The flipped row was a REAL rough-in row [landed cycle-017], not a placeholder displacement.)
- SUMMARY-registration: applied-by-report (the report's own proposed-changes carried the SUMMARY edit; NO auto-fix needed — the chapter `./L2/linear_combination.md` is registered under the L2 Part. Verified the report supplied it rather than relying on the integrator auto-fix.)
- plan-kind-consistency: 0 (declared kind `firm`; content shape is a complete firm operator entry — signature + shape contract + 7 laws + 3 non-laws + semantics + 2 variant axes + fusion note + sibling-distinction + evidence, no rough-in placeholders; critic plan-kind-consistency = pass, firm judgment ruled JUSTIFIED [structural decomposition exhaustively cited; the missing empirical-match is a test-witness gap handled by the chebyshev-iteration firm-without-dedicated-test precedent, NOT a constructive-sub-part gap → `partly-constructive` correctly NOT warranted])
- broken-link / cross-reference-integrity: 0 (all live links resolve: the four L1 leaf links `../L1/{scal,axpy,axpby,axpbypcz}.md`, the concept link `../concepts/scalar-promotion.md`, the precedent link `./chebyshev-iteration.md`, the decision link `../../../scaffolding/decisions/axpby-as-primitive.md` [3 levels up from book/src/L2/ = repo root]; the dep-map row's new `./linear_combination.md` link now resolves to the file created THIS report; the SUMMARY entry resolves; forward-refs to the not-yet-existing L2>L1 theme + `inner_product` are PLAIN-TEXT [no live link to dangle]; critic cross-reference-integrity = pass)
- variant-axis / bookkeeping: complete

Open questions promoted:
- linear-combination-harvester-formalization (RESOLVED — the firm chapter + dep-map flip + SUMMARY register close this harvester-formalization OQ; recorded as a [RESOLUTION — cycle-018] note after the OQ prose; `status:` flip → `resolved` + `last_revisited: cycle-018` deferred to finalize per append-only discipline. Note: item (4) the `L2-L1/linear-combination-fold-specialization` lowering theme is LANDING THIS SAME CYCLE as report 4 — the operator chapter's §Dependencies forward-references it as plain text.)

Build-relevant: yes

Notes: Position 1 of 5 — FIRST per-report integrator of cycle-018 (THIRD primary cycle of meta-batch-4; meta-phase fires after this cycle's finalize). Created this staging dir + STAGING.md (template from cycle-017). **Ordering:** this report was dispatched FIRST by design — the cycle-018 L2>L1 theme report (report 4, `L2-L1/linear-combination-fold-specialization`) links to `book/src/L2/linear_combination.md`; landing the operator chapter first makes that downstream link resolve at the finalize book rebuild. This report ENACTS the constructive payoff of the human-raised OQ `blas1-variadic-linear-combination-fold-unification` (prong b) — the cycle-017 combinator-miner landed the rough-in dep-map row, this cycle-018 harvester firms it: `linear_combination` is the arity-axis unification of the four BLAS-1 leaves (the `scalar-promotion` concept already carried the complementary element-type-axis unification). overall_status `ready` (critic 7/8 pass + 1 variant-axis-coverage warning; repairer REPAIRED that warning via 5 hedge-clause edits on the inherited scalar-promotion sub-axis — `real ⊑ complex` not-yet-committed upstream — applied to the report's proposed-changes blocks, NOT the artifact; the firm judgment was ruled JUSTIFIED and preserved). **Empirical-match caveat carried forward as-is (NOT an unrepaired defect):** there is NO dedicated unit test for the BLAS-1 linear-combination free functions — the `test-vector.cpp` "Vector Sum" tests exercise `linalg::Sum` (a DIFFERENT reduce-to-scalar fold); the entry is `firm` on direct source-transcription per the `chebyshev-iteration` firm-without-dedicated-test precedent (ratified cycle-012), with the explicit upgrade condition (a future dedicated test → clean `empirical_match`). The critic VERIFIED this `linalg::Sum ≠ linear_combination` distinction correct and confirmed the harvester correctly REFUSED to cite the Vector-Sum tests as a witness. **Forward-ref discipline (cycle-017 build-break lesson applied):** verified the report's own forward-references to the forthcoming `L2-L1/linear-combination-fold-specialization` theme + the `inner_product` sibling are PLAIN-TEXT / backtick code-spans, NOT live `[...](...)` markdown links — so no dangling-link build break even though report 4 lands the L2>L1 theme later this same cycle. **Deferred to finalize:** (a) `integrated_at` + `integration_commit` frontmatter on the consumed report (per write-authority partition — per-report integrator does NOT touch report frontmatter; confirmed neither field present in CYCLE.md or META.md pre-apply); (b) formal OQ `status:` flip → `resolved` + `last_revisited: cycle-018` on the `linear-combination-harvester-formalization` YAML block (ledger ~L2962; I appended a [RESOLUTION] note, did not edit the YAML). **For finalize / next-cycle planner:** (1) the human-raised parent OQ `blas1-variadic-linear-combination-fold-unification` (ledger 2937) constructive prong (b) is now FULLY ENACTED — rough-in (cycle-017) → firm operator (this report); prong (a) [combinator-miner spec parametric/variadic-family detection mode] remains a batch-4 meta-phase item (meta fires after this cycle). I did NOT flip the parent OQ's status (human-raised; the firm-up addresses the constructive deliverable but the prong-(a) methodology item remains — finalize/meta decides). (2) `inner-product-fold-sibling-candidate` remains a SEPARATE future combinator-miner family (conjugation-convention axis, NOT arity) — untouched, still open. (3) The L2 `index.md` "Working Notes" could gain a `linear_combination` provenance bullet mirroring the `krylov-step` precedent (report §Open-question 4 flagged this as layer-intro-author's domain, NOT harvester's — a future intro-pass note, not landed here). (4) L2 firm operator count grew by 1 this report (krylov-step + chebyshev-iteration + linear_combination = 3 firm L2 operators) — roadmap-relevant for finalize.

---

## layer-intro-author-nested-gate-concept
applied_at: 2026-05-29T024500Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/concepts/nested-constructed-operator-gate.md (create — NEW cross-cutting concept page naming the gate-carrying-gate shape: a `constructed-operator` closure whose sub-field is ITSELF a constructed-operator gate (`Solver[A]`, `DivFreeSolver`, …), distinct from a closure carrying a RAW operator. Full page content: one-line + Background [the raw-operator-field vs gate-field distinguishing test — `chebyshev-smoother`'s `op.A : LinearOperator[N,N]` + `apply_linop`'s operand = negative/not-nesting cases; `E.linear`/`P.ksp` = gate-field/nesting], §"The cross-layer fidelity rule" [the lowering discipline: inner gate's iteration stays interior to its OWN lowering theme, opaque at the outer theme's resolution; lowering of the whole = COMPOSITION of the two adjacent-edge themes, never a single flattened rewrite; the "composed-not-inherited" remark at `eigsolve.md:140`], §"Firm instances" [TWO firm L1 instances: `eigsolve` (cycle-011 `8bb16b7`) with TWO nested gates `E.linear : Solver[A]` + `E.projector : Maybe DivFreeSolver` per `eigsolve.md:60`, lowered via theme sub-pattern B `eigsolve-mutation-rotation.md:213-258`; `divfree-projector` (cycle-016 `b54ea1c`) with ONE nested gate `P.ksp : Solver[P.M]` per `divfree-projector-mutation-rotation.md:193-198`,`:108-113`. PLUS the three-deep transitive-nesting note `eigsolve ⊃ divfree-projector ⊃ ksp_solve`. PLUS the LATENT `ksp_solve` `K.M⁻¹`-as-`Solver` site `ksp_solve.md:31` — listed LATENT (not firm) with stated promotion condition (no L0 site verified where a `BaseKspSolver`'s preconditioner is itself a `BaseKspSolver`); chebyshev-as-preconditioner `chebyshev-smoother.md:140` noted as WEAKER nesting (smoother-as-operator, not a Solver-gate)], §"Relationship to siblings" [factory = materialisation site of a single gate; this page = composition of gates; + `solver-as-operator`, `constructed-operators`, `variant-absorption`], §"See also". `eigsolve` counted as a CLEAN FIRM instance: the `LinearSolveFailed` partly-constructive sub-part is explicitly distinguished as a SEPARATE status concern orthogonal to the firm source-anchored nesting in sub-pattern B. Page matches `constructed-operator-factory.md` format: no YAML frontmatter, `# <slug>` H1, relative `./concepts/*.md` live links. The cross-Part `book/src/L1-L0/...` + `book/src/L1/...` references are PLAIN-TEXT backtick code-spans, NOT live `[...](...)` links — verified before apply per cycle-017 build-break lesson; no dangling-link risk. Used the repairer-corrected page body: bold removed from the `eigsolve.md:136` quote (repair finding 2); the "firm"-word attribution in the report's evidence ledger re-pointed to `:251` (repair finding 1, evidence-ledger-only — no page-content change).)
- book/src/SUMMARY.md (edit ×1 — registered `  - [nested-constructed-operator-gate](./concepts/nested-constructed-operator-gate.md)` as the FINAL Concepts-Part entry, AFTER `  - [scalar-promotion](./concepts/scalar-promotion.md)` [on-disk line 152, RE-READ fresh — report 1 had edited SUMMARY.md to add the L2 `linear_combination` chapter under the L2 Part; that edit is in a DIFFERENT Part and did not disturb the Concepts-Part anchor], before the blank line + `# Design Artifacts` header. 3-space sub-item indent matches the sibling concept entries. OLD anchor matched disk byte-for-byte on fresh re-read.)
- scaffolding/open-questions.md (append — one [RESOLUTION — prong (a) — cycle-018] note appended at the END of the `nested-constructed-operator-gate-concept-and-divfree-correction` OQ section [after the existing closing "Not blocking; …" prose ~L3000, before `## Dropped`], recording prong (a) the concept page LANDED with all four prong-(a) requirements delivered + both author-confirm caveats honoured [`eigsolve` clean-FIRM with `LinearSolveFailed` distinguished; `ksp_solve` site LATENT]; explicitly recording prong (b) REMAINS OPEN — lands THIS SAME cycle as report 5 (the divfree-correction dispatch). Append-only; did NOT flip the `status:` YAML field [deferred to finalize — likely → `partially-addressed` after prong (b) report 5 lands, or `resolved` if finalize judges both prongs complete].)

Gate hits:
- retroactive-budget: 0-slice (this report) / 0 global — net-new concept page [create-file] + SUMMARY register + OQ append; NO retroactive edits to existing firm slices. (Running global tally: report 1 = 0, this report = 0; finalize sees the aggregate across all 5.)
- concept_writes-on-existing-slug: 0 (the slug `nested-constructed-operator-gate` is NEW — confirmed ABSENT on disk before Write; this is a clean create, NOT a section_append rewrite of an existing concept page)
- forward-edge-without-surface: 0 (concept page carries surface [the page itself] + full evidence [11 self-verified cross-artifact citations]; NOT a lowering-edge forward-claim)
- edge-label-prose-mismatch: 0 (concepts-Part page; carries no `L_{n+1}>L_n` edge label; critic edge-label-fidelity = pass — the cross-layer fidelity rule it STATES is an edge-discipline claim whose prose matches exactly the adjacent edges it names: divfree L1>L0 `ksp->Mult` delegating to `ksp_solve` theme; eigsolve L1>L0 ten `opInv->Mult` delegating to the same)
- H1-page-heading-reuse: 0 (the page H1 `# nested-constructed-operator-gate` is unique; does not reuse a page/Part heading)
- append-on-missing-slug: 0 (create-file for the concept page + anchored SUMMARY insert [existing `scalar-promotion` anchor, matched disk byte-for-byte] + OQ append within the EXISTING `nested-constructed-operator-gate-concept-and-divfree-correction` section; no append-to-missing-slug)
- variant-axis-missing: 0 (n/a — concepts page, not a multi-variant operator; the pattern's own nesting-depth axis is covered explicitly: one-gate [divfree], two-gate [eigsolve], three-deep transitive; negative cases [raw-operator field] carved out; latent site scoped with promotion condition; critic variant-axis-coverage = pass)
- index-placeholder-displacement: 0 (n/a — no index.md touched)
- SUMMARY-registration: applied-by-report (the report's own proposed-changes carried the SUMMARY edit registering the page under the Concepts Part; NO auto-fix needed — matches the cycle-005 concept-page-discoverability precedent where ~35 concept pages are SUMMARY-registered. Verified the report supplied it rather than relying on the integrator auto-fix.)
- plan-kind-consistency: 0 (declared shape = documentation/fidelity concept page; content shape matches exactly — named structural shape + discipline rule + 2 firm instances with citations + 1 explicitly-LATENT site + sibling relationships; no firm-claimed-but-rough-in placeholders; `eigsolve` clean-FIRM judgment ruled JUSTIFIED [the `LinearSolveFailed` partly-constructive caveat is a separate status concern, not a caveat on the nesting structure]; critic plan-kind-consistency = pass)
- broken-link / cross-reference-integrity: 0 (all 5 intra-concepts live links resolve: `./constructed-operators.md`, `./constructed-operator-factory.md`, `./solver-as-operator.md`, `./variant-absorption.md`, `./ksp_solve.md` — all confirmed present on disk; the SUMMARY entry `./concepts/nested-constructed-operator-gate.md` resolves to the file created THIS report; the cross-Part `book/src/L1-L0/...` + `book/src/L1/...` references are PLAIN-TEXT backtick code-spans [no live link to dangle] — and all 6 cited cross-Part files confirmed present on disk anyway; critic cross-reference-integrity = pass)
- variant-axis / bookkeeping: complete

Open questions promoted:
- nested-constructed-operator-gate-concept-and-divfree-correction (PRONG (a) RESOLVED — this report's concept-page deliverable is the prong-(a) closure; recorded as a [RESOLUTION — prong (a) — cycle-018] note appended at the end of the existing OQ section [opened cycle-017 by cross-layer-cross-cutter]. PRONG (b) REMAINS OPEN — the divfree-theme "first"/"no other op" correction lands THIS SAME cycle as report 5 [the divfree-correction lifter/harvester dispatch that LINKS to this concept page — which is precisely why this report was ordered SECOND]. `status:` YAML flip deferred to finalize per append-only discipline — likely → `partially-addressed` pending report 5, or `resolved` if finalize judges both prongs done this cycle.)

Build-relevant: yes

Notes: Position 2 of 5 — dispatched SECOND BY DESIGN. The cycle-018 divfree-correction report (report 5) LINKS to `book/src/concepts/nested-constructed-operator-gate.md`; landing this concept page FIRST (before report 5) makes that downstream link resolve at the finalize book rebuild — the symmetric mirror of the report-1→report-4 ordering. **Re-read SUMMARY.md fresh from disk before editing** (report 1 had added the L2 `linear_combination` chapter entry under the `# L2` Part at on-disk line 39); that edit is in a DIFFERENT Part from the Concepts Part and did not disturb my `scalar-promotion` anchor (on-disk line 152) — anchor matched byte-for-byte. **No overlap with report 1** beyond SUMMARY.md (different Part, clean). **Used the repairer-corrected report body:** 2 surgical repairs were applied to the report's CYCLE.md proposed-changes (NOT the artifact) and I carried them into the landed page — (1) the evidence-ledger "firm"-word attribution re-pointed to `eigsolve-mutation-rotation.md:251` [evidence-ledger-only, no page-content change], (2) bold removed from the `eigsolve.md:136` quote to match source verbatim [page body line + evidence bullet]. The page substance was unchanged by both repairs. **`eigsolve` clean-FIRM counting confirmed:** the page (and I) count `eigsolve` as a clean FIRM instance of the gate-nesting shape — its `LinearSolveFailed` partly-constructive sub-part is a SEPARATE status concern (discarded inner-solve convergence status) orthogonal to the firm, source-anchored nesting in sub-pattern B; the page states this distinction explicitly so a reader does not mistake the caveat for one on the nesting structure. This is consistent with the cycle-012 `partly-constructive`-is-first-class invariant (the caveat is per-status, not structural). **`skill-uptake-survey: warning` is informational only** (the report did the citation-resolution + variant-axis-classification work — 11 self-verified citations + the raw/gate/latent classification — but did not reference `verify-citation-range` / `classify-variant-axis` by name; repairer left it for meta-phase trend visibility); does NOT block application. **Deferred to integrator-finalize:** (a) `integrated_at` + `integration_commit` frontmatter on the consumed report (per write-authority partition — per-report integrator does NOT touch report frontmatter; confirmed neither field present pre-apply); (b) the OQ `status:` flip on `nested-constructed-operator-gate-concept-and-divfree-correction` (I appended a prong-(a) [RESOLUTION] note; did NOT edit the YAML — finalize decides `partially-addressed` vs `resolved` once report 5 lands prong (b)). **For finalize / next-cycle planner:** (1) prong (b) [divfree-theme "first"/"no other op" correction at `divfree-projector-mutation-rotation.md:108-113`, `:457-464` + the OQ-ledger prose, citing `eigsolve-mutation-rotation` sub-pattern B as the prior two-gate instance and re-pointing the in-line note at THIS new concept page] is report 5 this same cycle — confirm both prongs landed before flipping the OQ to `resolved`. (2) the LATENT `ksp_solve` preconditioner-as-`Solver` site listed in the page awaits a future harvester L0 confirmation (a concrete Palace site where a `BaseKspSolver`'s preconditioner is itself a `BaseKspSolver`) before promotion from latent → firm instance; if confirmed, the page's "Latent site" paragraph should be promoted into the "Firm instances" list. (3) the concept-vs-combinator boundary is flagged-not-foreclosed: re-route to `combinator-miner` only if a later cycle finds the nesting needs calculus-level support (e.g. an L4 `withInnerSolver` bracket). (4) NEW concept page count: the Concepts Part grew by 1 (now ends with `nested-constructed-operator-gate` after `scalar-promotion`) — minor roadmap signal for finalize. **Transient transcript slip (self-corrected, no artifact impact):** I briefly mis-edited report 1's staging row (a placeholder typo on its OQ-promoted line) and immediately reverted it byte-for-byte before appending this row — report 1's row is intact; flagging for transparency.

---

## lifter-divfree-first-claim-correction
applied_at: 2026-05-29T031500Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L1-L0/divfree-projector-mutation-rotation.md (edit ×2 — two surgical content-corrections removing the inaccurate "first"/"no-other-op" nested-gate uniqueness claims, repositioning divfree as the SECOND gate-carrying L1>L0 theme / THIRD nested gate overall after eigsolve's two. **Edit 1** = sub-pattern A bullet (on-disk `:108-113`): old "This is the **first** L1>L0 mutation-rotation whose closure carries *another* constructed-operator gate as a sub-field (`P.ksp : Solver[P.M]`)" → new prose that (i) names the `nested-constructed-operator-gate` fidelity rule [inner gate's iteration stays interior to its own lowering theme] with a live link to `../concepts/nested-constructed-operator-gate.md`, (ii) keeps the legitimate fact that `P.ksp` IS a nested gate but adds "It is **not the first** such case", (iii) cites the firm `eigsolve-mutation-rotation` theme (cycle-011) as the prior+richer TWO-gate instance [`E.linear : Solver[A]` + `E.projector : Maybe DivFreeSolver[ComplexVector]`, the latter being this projector itself per `../L1/eigsolve.md` §Shape contract `E`], (iv) states the three-deep transitivity eigsolve ⊃ divfree ⊃ ksp, (v) positions divfree as the **second** gate-carrying L1>L0 theme carrying the **third** nested gate overall. **Edit 2** = Open-questions item (on-disk `:457-464`): old "the **first** such case in the L1>L0 mutation-rotation family … recurring structural shape shared with no other current L1 op" → new prose linking the concept page, citing `eigsolve-mutation-rotation` sub-pattern B [`book/src/L1-L0/eigsolve-mutation-rotation.md:213-258`, the core sub-pattern lowering ten `opInv->Mult` inner-solve sites through the firm `ksp-solve-mutation-rotation` theme] + the prose-named precedent at `../L1/eigsolve.md:60,136,140`, restating the second-theme/third-gate positioning + three-deep transitivity, and pointing at the concept page's instance index. Both `[old]` blocks matched disk byte-for-byte on fresh re-read. The theme's structure, rotation, sub-patterns A–D, justification kinds, and applicability conditions are UNTOUCHED — bounded content-correction only, within the lifter boundary. Used the repairer-tightened report body [the `[new]` blocks already read "second gate-carrying theme / third nested gate overall" rather than the cross-cutter's bare "third L1>L0 mutation-rotation"; gates-vs-themes wording disambiguated — applied to the report's proposed-changes, NOT a further artifact edit].)
- scaffolding/open-questions.md (append ×2 — (1) an [ANSWER ENACTED — prong (b) — cycle-018] note appended after the existing cycle-017 ANSWER on the `divfree-closure-nesting-constructed-gate-carrying-constructed-gate` OQ [already `status: answered`; my note records the artifact-correction enactment, OQ stays `answered`]; (2) a [RESOLUTION — prong (b) — cycle-018] note appended at the END of the `nested-constructed-operator-gate-concept-and-divfree-correction` OQ section [after the existing prong-(a) RESOLUTION note from report 2, before `## Dropped`], recording BOTH prongs now complete (prong a = concept page, report 2; prong b = divfree correction, this report). Append-only; did NOT flip either `status:` YAML field [deferred to finalize — the second OQ likely → `resolved` since both prongs landed this cycle].)

Gate hits:
- retroactive-budget: 0-slice (this report) / aggregate-unknown-to-me global — this report makes 2 retroactive edits to ONE existing firm L1>L0 theme (`divfree-projector-mutation-rotation.md`, landed cycle-016). Per-slice count for divfree = 2 (< the 3-per-slice block threshold). NOTE TO FINALIZE: the global aggregate across all cycle-018 reports is finalize's to compute from the full staging log — I see only this report + the 2 prior staged rows (harvester=0, layer-intro-author=0); on the rows visible to me the running global is 2, well under the ≥4 global block. No re-route-to-revision triggered.
- forward-edge-without-surface: 0 (content-correction of an existing firm theme carrying surface + evidence; not a lowering-edge forward-claim. The corrected passages cite firm artifact [eigsolve theme/op] + a VERIFIED-SOUND cross-cutter audit.)
- concept_writes-on-existing-slug: 0 (no concept page written by THIS report — the `nested-constructed-operator-gate` page was created by report 2; this report only LINKS to it. No section_append-vs-concept_write concern.)
- edge-label-prose-mismatch: 0 (the theme carries the L1>L0 edge label and the corrected prose discusses exactly that edge; the cross-theme references [eigsolve-mutation-rotation at L1>L0; eigsolve.md/ksp_solve.md at L1] are at consistent layers; the "transitively three-deep eigsolve ⊃ divfree ⊃ ksp" chain is a within-L1 gate-nesting statement correctly framed as such; critic edge-label-fidelity = pass)
- H1-page-heading-reuse: 0 (no H1 touched — surgical bullet edits inside existing sections)
- append-on-missing-slug: 0 (both edits are anchored [old]→[new] replacements against byte-for-byte-matched anchors in an EXISTING firm theme; the OQ appends land within EXISTING OQ sections; no append-to-missing-slug)
- variant-axis-missing: 0 (n/a — content-correction of cross-theme provenance framing; no variant axis opened or closed; the theme's sub-patterns A–D + variant axes are preserved untouched; critic variant-axis-coverage = pass)
- index-placeholder-displacement: 0 (n/a — no index.md touched)
- SUMMARY-registration: n/a (no new chapter/concept-page created by this report — the theme already exists in SUMMARY; the linked concept page was SUMMARY-registered by report 2)
- plan-kind-consistency: 0 (declared kind = bounded lifter content-correction; content shape matches — two surgical passage edits, no decomposition/signature/sub-pattern/rotation change, append-only-after-`integrated_at` discipline correctly invoked; critic plan-kind-consistency = pass)
- broken-link / cross-reference-integrity: 0 — ALL FOUR live-link targets in the new prose RESOLVE ON DISK, verified this dispatch: `../concepts/nested-constructed-operator-gate.md` [created report 2 this cycle, confirmed present — this was the critic's `warning` (same-cycle forward reference); RESOLVED by the report-2 prong-a landing being staged + on-disk BEFORE this report, exactly the dispatch ordering the parent + both reports intended], `./eigsolve-mutation-rotation.md`, `../L1/eigsolve.md`, `../L1/ksp_solve.md` all present. The critic's single `cross-reference-integrity: warning` (integration-ordering dependency on the concept page) is DISCHARGED.
- variant-axis / bookkeeping: complete

Open questions promoted:
- divfree-closure-nesting-constructed-gate-carrying-constructed-gate (ANSWERED — already `status: answered` since cycle-017; this report ENACTS prong (b) [the artifact correction giving effect to that answer]. Appended an [ANSWER ENACTED — prong (b) — cycle-018] note recording the two divfree-theme edits; OQ stays `answered` [the answer is unchanged]; `status:`/`last_revisited:` YAML untouched per append-only discipline.)
- nested-constructed-operator-gate-concept-and-divfree-correction (BOTH PRONGS NOW COMPLETE — prong (a) concept page landed report 2, prong (b) divfree correction landed THIS report. Appended a [RESOLUTION — prong (b) — cycle-018] note after report-2's prong-(a) note; recommends finalize flip `status:` → `resolved` + `last_revisited: cycle-018`. YAML untouched per append-only discipline [finalize's authority].)

Build-relevant: yes

Notes: Position 5 of 5 — LAST per-report integrator of cycle-018 (THIRD primary cycle of meta-batch-4; meta-phase fires after this cycle's finalize). This report ENACTS prong (b) of the `nested-constructed-operator-gate-concept-and-divfree-correction` OQ — the symmetric mirror of report 2 (prong a). overall_status `ready` (critic 7/8 pass + 1 cross-reference-integrity `warning`; the warning was the same-cycle forward reference to the prong-(a) concept page, NOT a content defect — repairer marked it `not-needed` [link is correct, do not alter] and routed it to integration-ordering attention; it is now DISCHARGED because report 2 landed the concept page BEFORE this report, both staged + on-disk). The repairer ALSO tightened the gates-vs-themes wording in the report's `[new]` blocks [issue 2: "third L1>L0 mutation-rotation" reads as a theme count but the count is along the gate axis → both blocks now read "second gate-carrying theme … third nested gate overall"]; applied to the report's proposed-changes, NOT a further artifact edit. **STAGING-LOG / DISPATCH DISCREPANCY — FLAG FOR FINALIZE (important):** the parent's dispatch said "reports 1-4 landed", but on fresh read the staging log contains ONLY 2 prior report sections — report 1 (`harvester-linear-combination-L2`) and report 2 (`layer-intro-author-nested-gate-concept`). Reports 3 (`combinator-miner-inner-product-fold`, dispatch dir `reports/2026-05-28T231046Z-combinator-miner-inner-product-fold/`) and 4 (`abstractor-l2-l1-linear-combination-theme`, dispatch dir `reports/2026-05-28T231508Z-abstractor-l2-l1-linear-combination-theme/`) have dispatch dirs on disk but are NOT staged. I did NOT wait for them and did NOT fabricate their rows — I applied THIS report (whose only same-cycle dependency, report 2's concept page, IS staged + on disk) and appended my row as the newest entry. **CRITICAL NUANCE for finalize — report 4's ARTIFACT IS already on disk without a staging row:** `git status` at the time of my apply shows report 4's L2>L1 theme landed at the artifact level — `book/src/L2-L1/linear-combination-fold-specialization.md` is a NEW untracked file and `book/src/L2-L1/index.md` is MODIFIED (the dep-map flip), exactly report 4's expected proposed-changes. So report 4's edits were APPLIED to the artifact but its STAGING ROW was NOT appended here (either appended elsewhere, or the row-append step was skipped). Report 3 (combinator-miner-inner-product-fold) shows no distinct new artifact in `git status` beyond the rows accounted for. **integrator-finalize MUST reconcile this before the book rebuild + commit:** (i) confirm report 4's on-disk artifact is intentional + complete (it is the downstream consumer of report 1's `linear_combination.md` operator chapter per report 1's ordering note — and report 1's chapter IS present, so the L2>L1 theme's link should resolve); (ii) determine report 3's disposition (applied-elsewhere / deferred / dropped); (iii) account for the missing staging rows for reports 3 + 4 — the staging log has only 3 rows (reports 1, 2, 5=me) but at least report 4's artifact is live, so the log UNDER-COUNTS what's actually staged into `book/`. My report's correctness does NOT depend on 3 or 4 (only on report 2's concept page, which is present). But finalize should NOT assume the 3-row staging log is the complete record of cycle-018 artifact mutations — it must `git status`/`git diff` the full `book/` tree to catch report 4's (and possibly report 3's) unrowed landings before rebuilding + committing. **Deferred to integrator-finalize:** (a) `integrated_at` + `integration_commit` frontmatter on THIS consumed report (per write-authority partition — per-report integrator does NOT touch report frontmatter; confirmed neither field present in CYCLE.md/META.md pre-apply; `deferred integrated_at to finalize per role-spec`); (b) the OQ `status:` flips — `nested-constructed-operator-gate-concept-and-divfree-correction` → `resolved` + `last_revisited: cycle-018` (both prongs landed this cycle), and a `last_revisited: cycle-018` touch on `divfree-closure-nesting-constructed-gate-carrying-constructed-gate` (stays `answered`); I appended notes to both, did not edit the YAML. **No book rebuild / no commit / no cycle-end housekeeping performed (finalize's job).**

---

## combinator-miner-inner-product-fold
applied_at: 2026-05-29T031500Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L2/index.md (edit ×1 — appended a NEW `inner_product` rough-in dep-map row AFTER the now-firm `linear_combination` row [on-disk line 25, the cycle-018-report-1 FIRM row — re-read from disk before apply, anchor matched the report-1-modified state byte-for-byte] and BEFORE the `## Working Notes` header. Now on-disk line 26. Row content verbatim from the report's repaired §Proposed-changes block: slug cell uses the PLAIN-TEXT forward-reference "`inner_product` (chapter `./inner_product.md` to be authored by harvester)" — NOT a live `[...](...)` link, per the cycle-017 build-break lesson [verified `book/src/L2/inner_product.md` does not exist; dir holds only index.md + krylov-step.md + chebyshev-iteration.md + linear_combination.md]. Signature cell `(Tensor[N], Tensor[N]) -> Scalar` (≡ `foldl (+) zero (zipWith kernel x y)`) with M-weighted member `inner_product_M(x,M,y)=xᴴ M y` carrying the repairer's INLINE conjugation-convention caveat parenthetical [shorthand — exact conjugation/arg-order to be pinned by harvester; Palace documents `Dot(comm,x,A,y)` as `yᴴ A x`, body `(Ax)ᴴ y = xᴴ Aᴴ y` — see caveat 7]. Dependencies cell names L1 leaves it fuses up from [`dot`, `tdot` firm; `bilinear-form` rough-in], the `apply_linop` L2-composition for the weighted member, the `dot` cross-cutting concept, the **sibling-do-NOT-merge** note [`linear_combination`, reduce-to-`Tensor[N]`, no shared concatenation/PSD structure], and the **consumer-NOT-instance** note [`matrix-weighted-norm` = `√ ∘ inner_product_M` at y=x, SPD B]. Status cell `(rough-in, proposed-by: combinator-miner:2026-05-28T231046Z)`.)
- scaffolding/open-questions.md (append — one [RESOLUTION — cycle-018] note after the `inner-product-fold-sibling-candidate` OQ prose block, recording the recommended follow-up combinator-miner invocation has RUN and landed the rough-in `inner_product` dep-map row; honors the "do-NOT-over-unify / algebra of folds" boundary [sibling kept separate from `linear_combination`; conjugation-convention + weighting as variant axes; `matrix-weighted-norm` held OUT as consumer]; flags the two remaining follow-ups [harvester-formalization of `book/src/L2/inner_product.md` + the `L2-L1/inner-product-fold-specialization` theme; M-weighted conjugation/arg-order pinning against `operator.hpp:386-394` + the `bilinear-form-conjugation-convention-anchor` OQ]. Append-only; did NOT flip the `status:` YAML field [open→resolved deferred to finalize].)

Gate hits:
- retroactive-budget: 0-slice (this report) / 0 global (this report's contribution). The `inner_product` row is a NET-NEW append to the L2 dep-map; it does NOT touch the report-1 `linear_combination` row or any existing firm slice. NOTE: integrator-finalize sees the aggregate across all 5 reports.
- forward-edge-without-surface: 0 (L2-vocabulary rough-in dep-map row carrying its full proposal surface [the row + the report's 5 codemap-verified L0 members + the `Norml2` consumer anchor], NOT a lowering-edge forward-claim. The forward-ref to `./inner_product.md` is a plain-text not-yet-authored-chapter pointer; critic surface-or-evidence = pass.)
- concept_writes-on-existing-slug: 0 (no concept page written or touched; `dot` concept referenced as existing cross-cutting prose, not authored.)
- edge-label-prose-mismatch: 0 (n/a — single-layer L2 vocabulary addition, carries NO `L_{n+1}>L_n` edge label; critic edge-label-fidelity = pass. The L2>L1 lowering theme is explicitly deferred to abstractor, not authored here.)
- H1-page-heading-reuse: 0 (no chapter created; no H1 emitted. Dep-map row only.)
- append-on-missing-slug: 0 (anchored row-append into the EXISTING `book/src/L2/index.md` dep-map table; anchor [the report-1 firm `linear_combination` row + the `## Working Notes` header] matched disk byte-for-byte on fresh re-read. No append-to-missing-slug.)
- variant-axis-missing: 0 (the rough-in row + report enumerate the variant axes — conjugation-convention {real-symmetric | complex-hermitian | complex-unconjugated}, element-type {real|complex}, weighting {plain M=I | M-weighted} — and correctly classify `matrix-weighted-norm` as a CONSUMER not a weighting cell; critic variant-axis-coverage = pass. The PSD-law-breaks-on-tdot per-cell caveat is carried. Not a multi-variant operator with a missing axis.)
- index-placeholder-displacement: 0 (the L2 dep-map table is firm content [krylov-step + chebyshev-iteration + the now-firm `linear_combination` rows]; NOT a `(empty — Phase B skeleton.)` placeholder. Normal row-append below a real firm row, not a first-firm-row placeholder displacement.)
- SUMMARY-registration: n/a (no new chapter file created — combinator-miner emits the rough-in DEP-MAP ROW only; `book/src/L2/inner_product.md` is explicitly deferred to a future harvester. No SUMMARY.md edit needed or proposed; the plain-text forward-ref does not register a chapter.)
- plan-kind-consistency: 0 (declared kind `rough-in`; content shape matches — signature is a "best guess; harvester firms up", the operator chapter is explicitly NOT created, laws are sketched as algebraic intuition, firm-up calls flagged as OQs/caveats; critic plan-kind-consistency = pass. Correctly a rough-in, not a mis-classified firm entry. Row format mirrors the cycle-017 `linear_combination` rough-in precedent.)
- broken-link / cross-reference-integrity: 0 (the appended row introduces NO new live links — the only path it names, `./inner_product.md`, is PLAIN-TEXT [no live link to dangle]; all backtick code-spans [`dot`, `tdot`, `bilinear-form`, `linear_combination`, `matrix-weighted-norm`, `apply_linop`] are inert. critic cross-reference-integrity = pass; all referenced artifact slugs verified to exist.)
- variant-axis / bookkeeping: complete

Open questions promoted:
- inner-product-fold-sibling-candidate (ADDRESSED — the recommended sibling `inner_product` L2 combinator is now landed as a rough-in dep-map row; appended a [RESOLUTION — cycle-018] note after the OQ prose [opened cycle-017 by combinator-miner]. `status:` YAML flip open→resolved deferred to finalize per append-only discipline. NOTE: the rough-in landing is this OQ's resolution; TWO follow-ups remain open beyond it — (1) harvester-formalization [`book/src/L2/inner_product.md` chapter + the `L2-L1/inner-product-fold-specialization` abstractor theme]; (2) M-weighted conjugation/arg-order pinning [shorthand `xᴴ M y` vs Palace `yᴴ A x` / `(Ax)ᴴ y`; inherit `bilinear-form-conjugation-convention-anchor` resolution]. Finalize/next-cycle planner should route these.)

Build-relevant: yes

Notes: Position 3 of 5 (per parent dispatch — the combinator-miner-inner-product-fold report). **APPEND-ORDERING NOTE FOR FINALIZE:** my row is appended LAST in the file even though I am dispatch-position 3, because of a staging-log append race — when I went to write my row, reports 1, 2 AND report 5 (`lifter-divfree-first-claim-correction`) had already appended their sections (and report 5's Notes already flag that MY report was not-yet-staged when it ran). My artifact edits (the L2/index.md `inner_product` row + the open-questions [RESOLUTION] note) WERE applied successfully and are on disk; only my staging-row append landed after report 5's row due to the race. So the staging log is NOT in strict dispatch order — finalize should read by section header, not by position. The per-report serialization the role-spec assumes did not strictly hold this cycle (multiple per-report integrators' writes interleaved); flagging per the cycle-012 friction precedent on staging-log integrity. **Overlap handled exactly as the dispatch directed:** report 1 flipped the `linear_combination` row rough-in→firm at `book/src/L2/index.md:25`; I re-read the file from disk BEFORE applying, matched the report-1-modified (FIRM) state as my `[old]` anchor, and appended the `inner_product` row AFTER it — verified landed at on-disk line 26 (the two edits do not conflict; report 1 modified the `linear_combination` row in place, I appended a new sibling row below it; reports 2 + 5 touched other files [`concepts/...`, `L1-L0/...`] + a different SUMMARY Part, no overlap with my L2 edit). **Forward-ref discipline (cycle-017 build-break lesson):** verified `./inner_product.md` renders as PLAIN-TEXT in the row [no live link], and the file does not exist yet — no dangling-link build risk at the finalize rebuild. The repairer's inline conjugation-convention caveat parenthetical (META Finding 1b) is carried into the row VERBATIM at the point of use, so the future harvester sees the `yᴴ A x` / `(Ax)ᴴ y` flag where it authors the chapter — per the META Suggested-resolution note. **Deferred to finalize:** (a) `integrated_at` + `integration_commit` frontmatter on the consumed report (write-authority partition — per-report integrator does NOT touch report frontmatter; confirmed neither field present in CYCLE.md/META.md pre-apply; `deferred integrated_at to finalize per role-spec`); (b) the `inner-product-fold-sibling-candidate` OQ `status:` YAML flip open→resolved + `last_revisited: cycle-018` (I appended a [RESOLUTION] note, did not edit the YAML). **For finalize / next-cycle planner:** (1) `book/src/L2/inner_product.md` is the next harvester target (firm chapter from this rough-in row), with the `L2-L1/inner-product-fold-specialization` abstractor theme to follow — mirrors the `linear_combination` → report-4 L2>L1 theme sequencing this very cycle. (2) The companion "Working Notes" provenance bullet the report SUGGESTED (mirroring the `krylov-step` / `linear_combination` precedent) was NOT landed — it is layer-intro-author domain (a future intro-pass note), and the report itself flagged it as integrator-may-defer; I deferred it. (3) No retroactive budget consumed; the L2 dep-map now lists 3 firm operators (krylov-step, chebyshev-iteration, linear_combination) + 1 new rough-in (inner_product) — roadmap-relevant for finalize. **No book rebuild / no commit / no cycle-end housekeeping performed (finalize's job).**

---

## abstractor-l2-l1-linear-combination-theme
applied_at: 2026-05-29T025500Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L2-L1/linear-combination-fold-specialization.md (create — NEW firm L2>L1 lowering theme; the fusion-selection rotation for the BLAS-1 scalar-weighted-vector-sum cohort. Narrated forward high→low: the L2 variadic fold `linear_combination :: [(Scalar, Tensor[N])] -> Tensor[N]` lowers by reading the term-list LENGTH and selecting the maximal fused L1 leaf — arity 1 → `scal`, arity 2 → `axpy` (unit 2nd coeff) / `axpby` (general), arity 3 → `axpbypcz` — falling back beyond arity 3 (Palace's fused-kernel ceiling `AXPBYPCZ`, `vector.hpp:305-316`) to an iterated left-fold of `γ=1` `axpbypcz`-into-output chains. Full content: one-line + LHS (L2 variadic fold form, order-agnostic-for-value law 7), RHS (four fixed-arity L1 leaves, each mirroring one Palace L0 symbol), the fusion-selection rewrite table [arity 0/1/2/3/≥4 ⇒ rules + the `accumulate2` chain], two arity-2 sub-selections [unit vs general 2nd coeff], the arity-3→arity-2 `γ==0` in-source fall-through [read off `vector.cpp:749-751`, witnessed live at `timeoperator.cpp:217`], the **pinned summation-order table** [the load-bearing-numerical content the L2 entry's IEEE non-law deferred here: scal `operator*=:203-227`; axpy `:702-712`; axpby fused `:726-730`; axpbypcz γ==0 single-pass `:749-751` vs γ≠0 two-pass split `:753-756`; the two arity-3 branches do NOT agree bit-for-bit], 5 applicability conditions, justification kind `algebraic` [the selection rule IS the L2 entry's already-firm laws 6 + 2 read as a lowering; arity-3→2 is law 5 zero-coefficient term-drop], Speculative-L1 = None [all four RHS leaves firm], Verified-against [7 self-verified L0 ranges + 3 live witnesses `nleps.cpp:343-344`/`romoperator.cpp:188-189`/`timeoperator.cpp:217`], Status `firm`, and the working-notes Open-questions § [lifting-note reverse-direction kept OUT of the high→low body; no-dedicated-test inherited caveat; arity-≥4 chunking value-free/bit-pinned; `inner_product` sibling forward-ref out of scope]. The 3 repairer fixes are present in the applied content: (1) "sums all three"→"sums its two surviving contributions (`α·x + β·y`)" prose correction; (2/3) the MFEM-internal not-Palace-confirmed markers on the summation-table preamble + applicability condition 5; (4) the `scal` row anchor tightened to `vector.cpp:203-227` (`operator*=`) alone, dropping the loose AXPY-range "analogue" co-citation.)
- book/src/L2-L1/index.md (edit ×1 — appended the `linear-combination-fold-specialization` row to the §"Theme list" table immediately AFTER the existing `chebyshev-iteration-fusion` row [on-disk line 13]: `| [linear-combination-fold-specialization](./linear-combination-fold-specialization.md) | `L2/linear_combination` (firm) | `L1/scal` + `axpy` + `axpby` + `axpbypcz` (firm) | firm *(algebraic; arity-dispatch fusion-selection + pinned summation order)* |`. Anchor matched disk byte-for-byte.)
- book/src/SUMMARY.md (edit ×1 — registered `- [linear-combination-fold-specialization](./L2-L1/linear-combination-fold-specialization.md)` as the last entry under the `# L2 > L1 — Lowering` Part, AFTER `chebyshev-iteration-fusion` [on-disk line 43], before the blank line separating from `# L1 — Mutation-Lifted Forms`. Re-read SUMMARY.md from disk before editing — report 1 had registered `./L2/linear_combination.md` under the L2 Part [line 39]; that is a DIFFERENT Part from mine and did not disturb my anchor; matched byte-for-byte.)
- scaffolding/open-questions.md (append — one [RESOLUTION cont'd — cycle-018, item (4) LANDED] note after report-1's [RESOLUTION — cycle-018] note on the `linear-combination-harvester-formalization` OQ [ledger ~L2971], recording item (4) of the harvester follow-up [the `L2-L1/linear-combination-fold-specialization` lowering theme] now ON DISK via this report; the OQ is now fully discharged [items 1/2/4 done, item 3 the honest empirical-match caveat]. Append-only; did NOT edit the `status:` YAML field — deferred to finalize.)

Gate hits:
- retroactive-budget: 0-slice (this report) / 0 global — net-new firm theme [create-file] + index-row append + SUMMARY register; NO retroactive edits to existing firm slices.
- forward-edge-without-surface: 0 (the L2>L1 lowering edge CARRIES full surface — the arity-dispatch rewrite table, the two arity-2 sub-selections, the arity-3→2 fall-through, the pinned summation-order table — backed by 7 positive self-verified L0 anchors + 3 live witnesses; NOT a bare forward-edge claim. critic surface-or-evidence = pass, rotation-quality = pass.)
- concept_writes-on-existing-slug: 0 (no concept page written; `concepts/scalar-promotion.md` referenced as existing — link resolves.)
- edge-label-prose-mismatch: 0 (declared L2>L1 edge; prose narrates L2→L1 forward throughout — LHS variadic fold, RHS fixed-arity leaves, ⇒ arrows L2→L1; the reverse-direction lifting note is correctly quarantined under the working-notes Open-questions §. critic edge-label-fidelity = pass.)
- H1-page-heading-reuse: 0 (chapter H1 `# linear-combination-fold-specialization` is the unique slug; does not reuse a Part/page heading.)
- append-on-missing-slug: 0 (create-file for the theme + anchored index-row append + anchored SUMMARY insert; all anchors matched disk byte-for-byte; no fallback needed.)
- variant-axis-missing: 0 (tabulates all orthogonal axes — arity 0/1/2/3/≥4, the arity-2 unit-vs-general 2nd-coeff sub-axis, γ==0 vs γ≠0 within arity 3, real-vs-complex element type at condition 5. critic variant-axis-coverage = pass.)
- index-placeholder-displacement: 0 (the L2-L1 index theme-list table is firm content [the chebyshev-iteration-fusion row]; NOT a `(empty — Phase B skeleton.)` placeholder. Appended a real second row.)
- SUMMARY-registration: applied-by-report (the report's own proposed-changes carried the SUMMARY edit under the `# L2 > L1 — Lowering` Part; NO auto-fix needed.)
- plan-kind-consistency: 0 (declared kind `theme`, `status: firm`; content shape matches `theme`. critic plan-kind-consistency = pass. critic surfaced `partly-constructive`-vs-`firm` as a NON-BLOCKING status question [Issue 5]; repairer left `firm` standing [JUSTIFIED — arity-1/2/3 real-path rewrite positively anchored + lowering-verifier-confirmed; the two soft sub-parts (arity-≥4 iterated chain, complex operand-order) are peripheral, honestly disclosed in Open questions, arity-≥4 witnessed live as the γ=1 accumulate]. No downgrade applied.)
- broken-link / cross-reference-integrity: 0 (all 8 distinct live link targets resolve on disk, verified this invocation: LHS `../L2/linear_combination.md` [landed by report 1 EARLIER THIS CYCLE — the integration-ordering hazard the critic/repairer flagged is SATISFIED], the four L1 RHS leaves `../L1/{scal,axpy,axpby,axpbypcz}.md`, `../concepts/scalar-promotion.md`, `./chebyshev-iteration-fusion.md`, and `../../../scaffolding/decisions/axpby-as-primitive.md` [repo-root decision doc, mdBook-external relative link, same shape the L2 entry uses; file exists]. critic cross-reference-integrity = warning [ONLY because the LHS `../L2/linear_combination.md` was not-yet-on-disk AT CRITIQUE TIME] — now RESOLVED post report-1 landing; no dangling link at build.)
- variant-axis / bookkeeping: complete

Open questions promoted:
- linear-combination-harvester-formalization (item-4 LANDED follow-on — appended a [RESOLUTION cont'd — cycle-018, item (4) LANDED] note after report-1's [RESOLUTION — cycle-018] note; the `L2-L1/linear-combination-fold-specialization` lowering theme [item 4 of the harvester follow-up] is now ON DISK; the OQ is fully discharged [items 1/2/4 DONE + item 3 the honest empirical-match caveat]. `status:` YAML flip → `resolved` + `last_revisited: cycle-018` deferred to finalize per append-only discipline. The four theme-body Open-questions / caveats items [lifting-note, no-dedicated-test, arity-≥4 chunking, inner_product-sibling] are recorded INSIDE the chapter's working-notes § per the high→low layer-definition discipline — they are chapter content, not new standalone ledger entries; the inner_product sibling is already tracked by the existing `inner-product-fold-sibling-candidate` OQ.)

Build-relevant: yes

Notes: This is **report 4** of cycle-018 (parent's "report 4 of 5"; dispatch dir `reports/2026-05-28T231508Z-abstractor-l2-l1-linear-combination-theme/`). **STAGING-LOG SEQUENCING — RESOLVES report-5's flag:** report 5's staging Notes (the `lifter-divfree-first-claim-correction` row) observed that report 4's ARTIFACT was already on disk WITHOUT a staging row at the time report 5 ran — that artifact is mine, and **this row supplies the missing report-4 staging entry**, closing report-5's "log UNDER-COUNTS what's actually staged into `book/`" concern for report 4. The out-of-order numbering (reports 5 and 3 rows precede mine in the file) is a concurrent-dispatch / append-race artifact — multiple per-report integrators' writes interleaved this cycle (reports 3 and 5 BOTH independently flagged the same staging-log race). My artifact Edits (theme create + index row + SUMMARY register) landed first, but my STAGING-ROW Edit lost repeated races to reports 5 and 3's row-appends; I re-read the now-larger log each time and appended my row as the newest LAST entry per append-only discipline (via atomic append given the active contention on the file). **Finalize: read the staging log BY SECTION HEADER, not by position — it is NOT in strict dispatch order this cycle.** My edits are all intact and verified post-race (`git status`: `?? book/src/L2-L1/linear-combination-fold-specialization.md`, ` M book/src/L2-L1/index.md`, ` M book/src/SUMMARY.md`; the scal repair-4 anchor present; the index + SUMMARY entries present). **INTEGRATION-ORDERING HAZARD RESOLVED:** the critic/repairer's load-bearing carry-forward — "apply the harvester sibling `reports/2026-05-28T231026Z-harvester-linear-combination-L2/` BEFORE or with this theme so `../L2/linear_combination.md` resolves" — IS satisfied: report 1 landed first (its `book/src/L2/linear_combination.md` is on disk, verified `OK` this invocation). No dangling LHS link at `cargo make book`. **Repairer fixes verified present in applied content:** all 3 repaired findings (Issue 1 prose "all three"→"two surviving"; Issue 3 MFEM-internal markers ×2; Issue 4 scal-anchor tightened to `operator*=:203-227`) are reflected in the file I wrote — applied to the report's proposed-changes block by the repairer and transcribed verbatim. Issue 2 (LHS forward-dependency) resolved as above; Issue 5 (`partly-constructive` candidacy) left as `firm` per repairer (not-needed). **Deferred to integrator-finalize:** (a) `integrated_at` + `integration_commit` frontmatter on this consumed report (per write-authority partition — per-report integrator does NOT touch report frontmatter; confirmed neither field present in CYCLE.md / META.md pre-apply; `deferred integrated_at to finalize per role-spec`); (b) the formal OQ `status:` flip → `resolved` + `last_revisited: cycle-018` on the `linear-combination-harvester-formalization` YAML block [ledger ~L2960; I appended a [RESOLUTION cont'd] note, did not edit the YAML]. **For finalize / next-cycle planner:** (1) the `book/src/L2-L1/` Part now holds 2 firm themes (chebyshev-iteration-fusion + linear-combination-fold-specialization) — roadmap-relevant. (2) the standard `lowering-verifier` audit of this theme's selection rule + summation-order table against the L0 source is the named follow-up [chapter Status §], NOT a status reduction — a candidate next-cycle dispatch. (3) the `inner_product` sibling lowering theme (`L2-L1/inner-product-fold-specialization`) is the parallel future track once the `inner_product` L2 operator is harvested (tracked by OQ `inner-product-fold-sibling-candidate`); this theme does not depend on it. **No book rebuild / no commit / no cycle-end housekeeping performed (finalize's job).**

---
