---
agent: cycle-planner
invoked_at: 2026-06-02T194920Z
scope: cycle-068 dispatch plan
status: pending
---

# Cycle 068 dispatch plan

## Goals selected this cycle

Open the batch-21 FE-cohort→L4 lift frontier per **directive 1** (L4 is the outward backend-lowering target; the FE-assembly/FE-space cohort stranded at L1 is the deliverable hole). The c067 D2 survey fan-out-ranked the per-member dispatches: this cycle lands the **rank-1 L4 `fe_assemble` assemble-fold combinator** (the concatenation-homomorphism `foldr`, the homomorphic sibling of `solve_family`, wrapping the opaque libCEED quadrature leaf as a `readonly` black-box-kernel L4 input) **+ its coupled L4>L3 dissolution theme**, and lands the **`L4/index.md:66` "no-L4-by-design" re-examination + rises `linear_combination`/`inner_product` to L4** (the survey sub-finding; directive-2 disposition-2; the precondition that unblocks the rank-2 `assemble_frequency_operator` lift). Rank-2 (`assemble_frequency_operator`), ranks 3-4 (`eliminate_*`), and the directive-3 mdBook reorg sequence to c069 / the batch-21 meta-phase — NOT this cycle (keep the lead the focus; do not pre-empt the meta-phase strategic assessment).

## Deliverable-presence verification (paste-inline evidence)

Per the MANDATORY four-step procedure. New L4/L4>L3 slugs are verify-ABSENT; L0 evidence + L1 source entries + the rank-2 L2/L3 dep are verify-PRESENT.

**D1 — `book/src/L4/fe_assemble.md` (rank-1 L4 combinator) — verify-ABSENT (open by construction; frontier opener, no prior L4 history):**
1. File existence: `ls book/src/L4/fe_assemble.md` → `No such file or directory`; `ls book/src/L4/assemble.md` → `No such file or directory`. L4 inventory on disk = `chebyshev / eigsolve / fold_solve / index / iterate-while / iterate-while-with-prev / krylov-step / ksp_solve / solve_family` — **no FE entry. ABSENT confirmed.**
2. Maturity: N/A (file absent).
3. OQ-ledger RESOLVED-grep: `grep -c 'fe-cohort-l4-lift.*RESOLVED|...|l4-assemble-fold.*RESOLVED'` → `0` (no matches — the lift OQ is OPEN, the survey ranking awaits the c068 landing).
4. Structural-block: none — directive 1 + the D2 warrant-first survey (mining-gate 4 points cleared, finding (ii)) license the L4 landing. NO-L2 warrants bar only the L1→L2 mirror, NOT the upward-to-L4 form (directive-1 anti-mirror note).
- L1 source PRESENT: `ls book/src/L1/fe_assemble.md` → present. On-disk foldr shape confirmed (`fe_assemble.md:60-62`): `fe_assemble(space, terms) = foldr (\t acc -> A(space, t) + acc) zero terms = Σ_{t ∈ terms} A(space, t)`.
- L0 evidence PRESENT (codemap-confirmed this cycle): `palace/fem/bilinearform.cpp:75` + `:95` (`integ->Assemble(ceed, trial_restr, ...)` domain/boundary); `palace/fem/libceed/operator.cpp:455` (`CeedOperatorFullAssemble`). Libceed obstruction PRESENT: `ls book/src/L1-L0/fe-assemble-libceed-boundary-obstruction.md` → present.

**D2 — `book/src/L4-L3/fe-assemble-fold-dissolution.md` (coupled L4>L3 theme) — verify-ABSENT (open by construction):**
1. File existence: `ls book/src/L4-L3/{fe-assemble-fold-dissolution,assemble-fold-dissolution,fe-assemble-dissolution}.md` → all `No such file or directory`. L4-L3 inventory = `fgmres-... / fold-solve-time-step-dissolution / gmres-... / index / iterate-while-dissolution / iterate-while-with-prev-dissolution / krylov-step-typed-wrapper-dissolution / ksp-solve-driver-dissolution / solve-family-map-dissolution` — **no FE/assemble theme. ABSENT confirmed.**
2-4. N/A absent / OQ open (same grep as D1) / no structural block.

**D3 — `book/src/L4/linear_combination.md` + `book/src/L4/inner_product.md` (rise the combinators) — verify-ABSENT; `L4/index.md:66` line — verify-PRESENT:**
1. File existence: `ls book/src/L4/linear_combination.md` → `No such file or directory`; `ls book/src/L4/inner_product.md` → `No such file or directory`. **ABSENT confirmed** (they currently stop at L2/L3 — the directive-2 hole).
2. `L4/index.md:66` read on disk (verbatim): *"The 13-of-18 BLAS-1 / elementwise / smoother L3 operators remain no-L4-by-design (their L4 form would add no calculus beyond their firm L3 rendering) — that observation stands; what is retired is the inference that the *whole* L4 frontier is therefore exhausted."* — the blanket line the survey sub-finding flags partially-wrong; PRESENT and editable.
3. OQ-ledger: `grep -c '...l4-index-13-of-18...RESOLVED'` → `0` (the sub-finding OQ `l4-index-13-of-18-no-l4-by-design-claim-partially-wrong-...` is OPEN).
4. Structural-block: none — directive-2 disposition-2 (project memory `project_blackbox_vs_accelerated_kernels`: "Combinators (linear_combination/inner_product) must reach L4 regardless") licenses the rise; the concept page `concepts/black-box-vs-accelerated-kernels.md` (c067 D3) is PRESENT as the citable classification home.
- L2/L3 deps PRESENT: `ls book/src/L2/linear_combination.md book/src/L3/linear_combination.md book/src/L2/inner_product.md book/src/L3/inner_product.md` → all present (firm).

**STOP-PROPOSING negative-list check:** none of the three dispatches matches a disqualified slug. The negative list bars L2 *mirrors* / specific small-dense atoms — it does NOT bar L4 forms (directive-1: "the STOP-PROPOSING entries stay valid as written; they do not bar L4 forms"). `linear_combination`/`inner_product` rising to L4 is explicitly licensed by directive-2 disposition-2, NOT on the list.

## Dispatches

**D1 — `harvester` — `book/src/L4/fe_assemble.md` (rank-1 L4 `assemble`-fold combinator); deps: none**
- Scope: author the L4 `assemble`-fold combinator (canonical slug **`fe_assemble`**, file `book/src/L4/fe_assemble.md` — match the L1 name; the survey allowed `assemble.md` as an alternative but `fe_assemble` keeps cross-layer name-continuity with the firm `L1/fe_assemble`). The combinator is `fe_assemble :: FiniteElementSpace[N] -> [WeakFormTerm] -> LinearOperator[N,N]`, a **concatenation-homomorphism `foldr` producing a sum** — the **homomorphic** sibling of `solve_family`'s map (the homomorphism HOLDS: term-position commutativity `fe_assemble.md:136-142`; a map-then-reduce, NOT `fold_solve`'s carry-threaded sequential fold). The opaque per-element libCEED quadrature leaf `assemble_term :: FiniteElementSpace[N] -> WeakFormTerm -> LinearOperator[N,N]` rises as a **black-box-kernel `readonly` L4 input** (the IDENTICAL opaque-leaf-wrapping pattern `eigsolve`/`fold_solve` use for `eigen_iterate`/`time_step_op`, `L4/index.md:39,40,83`). `weak_form_term` rides as the fold's list element-type; the three construction inputs (`fe_space`/`fe_collection`/`essential_dofs`) fold into the signature/shape-contract via the existing `OpParams`/`readonly` state-stratification L4 home (`L4/index.md:24`) — **absorb, do NOT dispatch 4 standalone thin chapters** (the survey's recommendation; re-open per downstream-consumer demand). Cite the **`disciplined-cross-pipeline-combinator-mining-gate`** skill (the 4 gate points are pre-cleared in the survey finding (ii), ready to copy: step-1 ≥2 witnesses electrostatic `(ε,∇)` `laplaceoperator.cpp:191-192` / magnetostatic `(μ⁻¹,∇×)` `curlcurloperator.cpp:179-181` / mass `(Q,I)` `spaceoperator.cpp:278`; step-2 no break-witness; step-3 map-not-fold over-unification guard honored; step-4 layer = L4 vocabulary). Cite BOTH the L4 black-box input AND the L1>L0 obstruction it lifts (`L1-L0/fe-assemble-libceed-boundary-obstruction.md`), as `eigsolve` does. **Forward-reference the coupled L4>L3 theme by its canonical slug** `book/src/L4-L3/fe-assemble-fold-dissolution.md` (D2 authors it this cycle — name it so neither producer guesses, per the c048 cross-report-forward-reference-slug convention). Add the `book/src/L4/index.md` dep-map ROW (alpha position per directive-3) + the §Vocabulary-cohort BULLET (D1 owns both — anchor-distinct, NOT deferred); **D3 is this cycle's sole `L4/index.md` consolidated-count/tally owner** (see overlap analysis) — D1 does NOT touch the firm-count tally / growth-log prose.
- L0 anchors (codemap hints — **on-disk-confirm any close-brace END before citing; codemap ENDs are drift-prone, recurrence-6**): `palace/fem/bilinearform.cpp:75-76` (domain `integ->Assemble`) / `:77` (`AddSubOperator`) / `:95-96` (boundary) / `:97` / `:104` (`Finalize`) / `:107` (PartialAssemble close); `palace/fem/libceed/operator.cpp:455` (`CeedOperatorFullAssemble` open — confirmed on disk; the **`:523` close-brace END is the drift-prone bound — confirm by direct on-disk `Read`, NOT `citecheck --anchor`**).
- Rationale: HIGHEST fan-out — the `K`-source for every solver pipeline; the genuinely-new L4 calculus vocabulary that opens the assemble-half of the deliverable's L4 completeness (directive 1). Plan-tag `fe-cohort-l4-lift` rank-1.

**D2 — `abstractor` — `book/src/L4-L3/fe-assemble-fold-dissolution.md` (coupled L4>L3 dissolution theme); deps: none (parallel-safe with D1; see overlap)**
- Scope: author the L4>L3 lowering theme (canonical slug **`fe-assemble-fold-dissolution`** — stated identically in D1's scope) narrating forward L4→L3 how the L4 `assemble`-fold dissolves into its L3 form (the global tensor-field assembly view): the outer `foldr`/map-then-reduce over the term list dissolving into per-term `assemble_term` applications with the term-independence (concatenation-homomorphism) preserved, the per-element libCEED leaf bottoming out in the `obstruction (opaque-library-ownership)` boundary (`L1-L0/fe-assemble-libceed-boundary-obstruction.md`). Cite `propose-rotation`. **Honor the map-not-fold guard** (per OQ `l4-assemble-fold-combinator-scoping-...`): this is the homomorphic family-loop dissolution (the `solve_family` shape), NOT `fold_solve`'s carry-threaded dissolution — do NOT conflate. WARRANT-FIRST per the redirect: judge whether the L3 image is a genuine concise iteration-rotation form vs an identity-skip — given the `fe_assemble` NO-L2 warrant + the libCEED opaque leaf, the L4>L3 theme is likely the authoritative downward home (analogous to `solve-family-map-dissolution`); record the L3-entry-vs-dissolution-home verdict either way. Add the `book/src/L4-L3/index.md` theme ROW (alpha position) + its own §Vocabulary-cohort BULLET if the L4-L3 index carries one; **D2 does NOT touch any consolidated L4-L3 tally if one exists — defer to the count-owner** (D2 owns its own row+bullet, anchor-distinct).
- Rationale: couples the cap (D1) with its lowering in the SAME cycle (avoids the cross-cycle stale-assertion window per the floor-landing-implies-same-cycle-adjacent-entry-reanchor convention, applied here to the cap/theme pair). Plan-tag `fe-cohort-l4-lift` rank-1 theme.

**D3 — `combinator-miner` — rise `linear_combination` + `inner_product` to L4 + correct `L4/index.md:66`; sole `L4/index.md` count-owner; deps: none**
- Scope: the c067 D2 survey sub-finding (OQ `l4-index-13-of-18-no-l4-by-design-claim-partially-wrong-...`) — the **rank-2 unblocker**. (a) Author `book/src/L4/linear_combination.md` + `book/src/L4/inner_product.md` as L4 combinator entries (the BLAS-1 *combinators* that "must reach L4 regardless" per directive-2 disposition-2 / memory `project_blackbox_vs_accelerated_kernels`), each re-expressing THROUGH its firm L2/L3 form (replace-and-propagate, kernel/leaves tied below as specialization notes — NOT a mirror; the L4 form is the calculus-level combinator). Cite the `disciplined-cross-pipeline-combinator-mining-gate` skill + the `concepts/black-box-vs-accelerated-kernels.md` page (c067 D3, on disk) for the three-way disposition. (b) Correct the `L4/index.md:66` blanket "13-of-18 no-L4-by-design" assertion to the **per-case** form: the *combinators* (`linear_combination`/`inner_product`) DO rise; the *kept named abstractions* (`dot`/`nrm2`) rise as named verbs (note them as the next-pull candidates, do NOT author them this cycle unless trivially clean); the *pure accelerated leaves* (`axpy`-family) correctly stay low. (c) As **this cycle's SOLE `L4/index.md` consolidated-count/tally + growth-log + frontier-prose owner**: incorporate the D1 `fe_assemble` L4 entry into the firm-count tally and the §Active-frontier prose (count firm from each linked chapter's `## Status` line, NEVER from drift-prone index cells — c057-meta count-owner guard), and register the two new combinator entries. Add `linear_combination`/`inner_product` own dep-map ROWs + cohort BULLETs.
- Rationale: MEDIUM-HIGH fan-out — the BLAS-1 combinator surface the whole solver test-load reuses; it is the GATE on the rank-2 `assemble_frequency_operator` L4 lift (which rides `linear_combination` rising). Pairing it with the rank-1 lead this cycle clears the rank-2 precondition for c069. Plan-tag `l4-index-no-l4-by-design-reexamination` + `linear-combination-inner-product-rise-to-l4`.

## Overlap analysis

- **D1 ↔ D2**: D1 authors the L4 cap `book/src/L4/fe_assemble.md`; D2 authors the L4>L3 theme `book/src/L4-L3/fe-assemble-fold-dissolution.md` — **distinct files, distinct directories.** D1 forward-references D2's slug (canonical slug stated in BOTH scopes → no slug-divergence). D1 touches `book/src/L4/index.md` (its own dep-map row + bullet); D2 touches `book/src/L4-L3/index.md` (its own row). Different index files. **NON-overlapping → PARALLEL.** (Per the conflict-tolerance philosophy: any residual mild conflict on a forward-link resolves at the per-report integrator, which wires the live link once both land.)
- **D1 ↔ D3**: both touch `book/src/L4/index.md`. D1 appends its own dep-map ROW + §Vocabulary-cohort BULLET (anchor-distinct, parallel-safe per the distinct-rows convention); **D3 is the SOLE owner of the consolidated firm-count tally + growth-log + §Active-frontier prose** (the shared mutable aggregate). D1's scope explicitly DEFERS the tally to D3; D3's scope explicitly OWNS it and counts firm from the linked `## Status` lines (incl. D1's new `fe_assemble` entry). This is the dual-registration partition (own-row + own-bullet are producer-owned; the consolidated tally is count-owner-owned). The distinct-rows are parallel-safe; the tally has exactly one writer → **PARALLEL with the partition stated.** D3 also authors `book/src/L4/linear_combination.md` + `inner_product.md` (distinct new files) — no D1 overlap there.
- **D2 ↔ D3**: D2 touches `book/src/L4-L3/index.md`; D3 touches `book/src/L4/index.md` + 2 new L4 files. **Disjoint files → PARALLEL.**

All three are PARALLEL (one wave) with the `L4/index.md` dual-registration partition stated (D3 = sole count/tally/frontier-prose owner; D1 appends only its own row+bullet and defers the tally).

## Sequencing schedule

**Single wave (all parallel):** D1 ‖ D2 ‖ D3.
- D1 forward-references D2's canonical slug `fe-assemble-fold-dissolution` (the per-report integrator wires the live link when both land — no wave split needed; a same-cycle forward-reference is the standard pattern, and false sequentialization is the worse error per the conflict-tolerance philosophy).
- `L4/index.md` shared-aggregate is partitioned by ownership (D3 sole count-owner), not by wave ordering.

Pipeline after the dispatch wave (unchanged, single finalize): D1/D2/D3 → 3 critics → repairers → `integrator-per-report` ×3 (serial) → ONE `integrator-finalize`.

## Plan file updates

Appended a `⟢ CYCLE-068 PICKS` block to `scaffolding/priorities.md` under the CYCLE-067 active head: marks D1/D2/D3 dispatched; migrates the c067 D2 survey's fan-out ranking into the plan as the batch-21 FE-cohort→L4 sequence; sequences rank-2 (`assemble_frequency_operator` via `linear_combination`, now unblocked once D3 lands), ranks 3-4 (`eliminate_*`), the construction-input chapter-vs-absorb re-open, and the directive-3 mdBook reorg to c069 / the batch-21 meta-phase.

## Open questions / caveats

- **rank-2 sequencing (`assemble_frequency_operator` → L4).** The survey ranks it cheapest (an operand-category propagation onto `linear_combination`), but it is GATED on `linear_combination` rising to L4 (D3 this cycle). I sequenced it to **c069** rather than bundling: it is cleanest as a follow-on once D3's `L4/linear_combination.md` exists on disk (the propagation re-expresses *through* a present L4 entry, avoiding a same-cycle forward-reference into a combinator that is itself being authored). If the c069 planner judges D3 landed clean, rank-2 is a cheap c069 lead.
- **ranks 3-4 (`eliminate_essential_bc` / `eliminate_rhs` → L4 post-compositions).** Deferred to c069. Note the survey caveat (OQ `fe-cohort-l4-lift-caveats-...`): `eliminate_rhs`'s L4 form composes `apply_linop` + `axpy` + `set_essential` — primitives that may stay below L4; it is the case MOST at risk of a degenerate L4 mirror. The c069 dispatch must verify primitive-L4-presence before landing (routes through D3's rise — if `apply_linop`/`inner_product` rise, this partially clears).
- **directive-3 mdBook reorg (the one-time by-kind sub-chapter grouping + alpha re-sort).** Per directive-3 + integrator-signals, this is heavy `book/`-structure work reserved as its OWN dispatch wave, meta-phase-driven (`.claude/agents/` codification needs a restart). NOT bundled with this forward-frontier cycle. I sequence it to c069 (a dedicated structural wave) OR the batch-21 meta-phase (post-c069); flagging for the c069 planner / meta-phase to claim it. New SUMMARY entries this cycle should still be placed in alpha position per the active-immediately orchestrator carry.
- **construction-input chapter-vs-absorb.** D1 absorbs `fe_space`/`fe_collection`/`essential_dofs` as `readonly` typed construction inputs into the `assemble`-fold signature/shape-contract (survey recommendation). If a critic or downstream c069 L4 consumer demands a navigable home for any of them, re-open as a thin L4 input-declaration chapter then (not now).
- **D3 file-name choice.** I cited `book/src/L4/linear_combination.md` + `inner_product.md` (matching the L2/L3 names for cross-layer continuity). If the combinator-miner judges a different L4 canonical name better, that is a producer call — but it must be stated in the report so the c069 rank-2 propagation references the right slug.
- **codemap close-brace drift (recurrence-6, steady-state).** All pre-localized `path:lo-hi` ranges in D1's scope are codemap HINTS; the END lines (esp. `libceed/operator.cpp:523` close brace) are drift-prone — D1 must on-disk-`Read`-confirm before citing, NOT rely on `citecheck --anchor` (blind to END off-by-one). Flagged in the D1 scope.
- **Meta-phase boundary.** Batch-21 meta-phase fires after c069's finalize (aggregates 067/068/069). This plan advances the FE-cohort→L4 arc but does NOT pre-empt the meta-phase's strategic assessment of it; the rank-2/3-4 sequencing and the directive-3/4 ownership transfers are explicitly routed to c069 / the meta-phase.
