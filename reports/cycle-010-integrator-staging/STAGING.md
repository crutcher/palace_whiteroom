# Cycle-010 integrator staging log

This file is the per-cycle staging log appended by `integrator-per-report` agents during cycle-010. Each per-report dispatch appends one section (newest LAST). `integrator-finalize` reads this log at cycle-end to run the book rebuild, repair breakage, mark consumed reports' `integrated_at`, append to cycle-record, write `log/cycle-010.md`, append to integrator-signals, and commit + push.

Append-only after a row is written. Per-report integrators MUST re-read disk before each Edit (a previous in-cycle integrator may have touched the same file).

Cycle-010 opens **meta-batch-2** (post the cycle-009 meta-phase that closed batch-1). Cycle-010 enacts priority #20 (identity-lowering-both-levels-backfill) as its lead wave-1 work — see the first row below.

---

## 2026-05-27T215300Z-harvester-l3-krylov-step
applied_at: 2026-05-27T220000Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L3/krylov-step.md (create — new firm L3 operator entry, 105 lines; first firm L3 operator; the identity-lowering-both-levels backfill enacting CLAUDE.md §Methodology invariants bullet codified cycle-009 meta-phase. Frontmatter carries layer/operator/firmness/lowers_to/lifts_from/variant_axes; six variant axes inherited unchanged from L4 / L2 in L3 vocabulary; three algebraic laws + seven non-laws; full Signature + Semantics + Evidence + L3-vs-L4 + L3-vs-L2 sections per the dispatch's CYCLE.md proposed-changes block.)
- book/src/L3/index.md (edit — placeholder dep-map `(empty — Phase B skeleton.)` displaced by first firm row for `krylov-step`; 2 Working Notes bullets appended recording layer-coherence rationale + cohort growth candidates per priority #20)
- book/src/SUMMARY.md (edit — 1 insertion: `- [krylov-step](./L3/krylov-step.md)` after `- [Overview](./L3/index.md)` under L3 — Global Tensor-Field Operations Part; before L3 > L2 — Lowering header)
- book/src/L4-L3/krylov-step-typed-wrapper-dissolution.md (edit — 1 in-place edit at line 218: cycle-006 "Consequence for L3 dep-map" verdict ("no L3 `krylov-step` row is proposed") replaced with SUPERSEDED annotation pointing at the cycle-010 backfill entry; the audit verdict itself "Audit verdict — confirmed-with-refinement" left unchanged per the dispatch's design — the body's identity-in-form property is what made the backfill *trivial*, not what made it *unnecessary*.)
- book/src/L3-L2/krylov-step-body-identity.md (edit — 1 in-place edit at line 15: §Context bullet about the L3 form updated from "published only as the RHS of `L4-L3/krylov-step-typed-wrapper-dissolution` §'L3 form (RHS)' (no standalone `L3/krylov-step.md`...)" to point at the new firm L3 entry with cycle-010 backfill rationale.)

Gate hits:
- retroactive-budget per-slice: 0 (new L3 chapter creation; no retroactive backfill on the existing L4 / L4-L3 / L3-L2 / L2 entries — the two annotation-only edits to upstream/downstream theme files are SUPERSEDED-verdict annotations, not retroactive evidence additions)
- retroactive-budget global: 0 (this report)
- concept_writes on existing slug: 0 (new file at `book/src/L3/krylov-step.md`; verified no pre-existing file via `ls book/src/L3/`)
- forward-edge claim without surface: 0 (all referenced slugs exist: L4 / L2 / L4-L3 / L3-L2 entries; concept pages enumerated in §Dependencies; the `iterate_while_L3` references resolve transitively via the upstream L4>L3 theme per critic's pass-level observation #5)
- edge-label / prose mismatch: 0 (frontmatter `lowers_to` / `lifts_from` align with prose §Lowers to / §Lifts from per critic edge-label-fidelity pass)
- H1 reuses page heading: 0 (chapter H1 `# krylov-step` differs from SUMMARY entry `krylov-step` — chapter H1 is the canonical operator name, no page-heading reuse violation)
- append on missing slug: 0 (all `[link]` references verified to exist on disk: `../L4/krylov-step.md`, `../L2/krylov-step.md`, `../L4-L3/krylov-step-typed-wrapper-dissolution.md`, `../L3-L2/krylov-step-body-identity.md`, all `../concepts/*` slugs in the cross-cutting concepts list)
- variant-axis missing on multi-variant operator: 0 (six variant axes enumerated in both frontmatter and prose §Variant axes, matching L4 / L2 entries exactly; below-body MGS sequential-obstruction correctly scoped per critic variant-axis-coverage pass)
- SUMMARY.md auto-fix: 0 (report explicitly proposed the SUMMARY.md edit; no auto-fix needed)
- index-placeholder displacement auto-fix: 1 (`book/src/L3/index.md` carried the literal placeholder text `(empty — Phase B skeleton.)` from the Phase-B skeleton era; per the cycle-006 cycle-006-codified discretionary fix, the first firm dep-map row **displaces** the placeholder rather than appending below — applied here as the placeholder-replacement edit. Rationale: `first-firm-row-displaces-placeholder`; precedent at cycle-006 wave-1 on `L4/index.md` and wave-2 on `L4-L3/index.md`. Record as `applied-discretionarily`.)
- bookkeeping incomplete: 0
- L3 vocabulary discipline observed: 0 issues (verified — body's let-chain uses positional values, explicit `let s' = s { it = s.it + 1 }` record-update line, no L4 monadic vocabulary in signature, no L2 unified `IterState` record in body description; per critic's "L3 semantics defined in L3 vocabulary (not L2 or L4)" critical-methodology check pass)
- frontmatter completeness: 0 issues (verified all 6 fields present: layer / operator / firmness / lowers_to / lifts_from / variant_axes; per critic observation #4 the L3 entry introduces frontmatter that L4 / L2 / L1 entries lack — observation only, not a violation, surfaces as future-normalization candidate)

Open questions promoted:
- (none — per CYCLE.md §Open questions / caveats item 7: "No open-questions ledger entries proposed for this dispatch's content. The L3 entry closes the gap identified by priority #20's first target ... No new open questions are surfaced — the body's identity-in-form is well-established (cycle-006 audit; cycle-009 ratification at the L3>L2 hop); the variant-axis profile is closed at six; the algebraic laws and non-laws are inherited unchanged." The five caveats in the report (items 1-6) are routing observations (future-cycle dispatches on `krylov_update` naming, L3 layer-intro refresh, priority #20 cross-layer-cross-cutter audit, `iterate_while_L3` anchoring, L3/index.md placeholder, SUMMARY.md scope) or methodology questions — none are citation-grounded substantive open questions per the report's own framing.)

Build-relevant: yes

Notes: First per-report integrator of cycle-010, opening meta-batch-2 (post-cycle-009-meta). Bootstrapped this STAGING.md. All 5 proposed-changes blocks from CYCLE.md applied cleanly on first attempt — 1 file creation via Write (chapter content sourced verbatim from CYCLE.md's `edit:book/src/L3/krylov-step.md` block including repairer's two citation-validity in-place fixes `iterative.cpp:22-32 (CheckDot; real overload at :22, complex overload at :28)` and strawman §3.7 `(lines 150-184)` + §3.8 `(lines 186-213)`); 4 anchor-and-insert edits across `book/src/L3/index.md` (placeholder displacement + 2 Working Notes), `book/src/SUMMARY.md` (1 insertion), `book/src/L4-L3/krylov-step-typed-wrapper-dissolution.md` (1 SUPERSEDED annotation at line 218), and `book/src/L3-L2/krylov-step-body-identity.md` (1 backfill-pointer annotation at line 15). Anchors matched disk exactly on all 4 edits. The index-placeholder displacement auto-fix gate fires (third precedent — first cycle-006 wave-1, second cycle-006 wave-2, third cycle-010 wave-1; first time on `L3/index.md`); rationale `first-firm-row-displaces-placeholder` matches the formalized fix. **Methodology-signal milestone for integrator-finalize**: this is the **first firm L3 operator** landing — the L3 layer transitions from placeholder-only to 1-firm-operator status. The "lower-level shared vocabulary takes priority" invariant (codified cycle-009 meta-phase) is enacted; cycle-010+ planner should weight further L3 / L2 / L1 firm operator backfills above L4 vocabulary expansion when both are eligible. Cohort growth candidates flagged in `L3/index.md`'s Working Notes for future `cross-layer-cross-cutter` audit dispatch (per priority #20 second target). The L3 entry's introduction of YAML frontmatter (layer / operator / firmness / lowers_to / lifts_from / variant_axes) sets a precedent that may eventually need backfill to L4 / L2 / L1 entries for consistency — observation only, not blocking; surface for integrator-finalize / meta-phase consideration in the batch-2 aggregation. Deferred `integrated_at:` to finalize per role-spec.

---

## 2026-05-27T215315Z-cross-layer-cross-cutter-identity-in-form-audit
applied_at: 2026-05-27T223000Z
applied_by: integrator-per-report
status: applied

Files touched:
- scaffolding/open-questions.md (edit — 4 OQ sections appended before the `## Dropped` trailer; promotion-only mutation, no edits to existing sections)

Gate hits:
- retroactive-budget per-slice: 0 (inspection-only audit dispatch; no operator-surface mutations)
- retroactive-budget global: 0 (this report)
- concept_writes on existing slug: 0 (no concept writes)
- forward-edge claim without surface: 0 (audit references existing firm themes and existing L1 entries; all `[link]` targets verified by upstream critic's cross-reference-integrity pass)
- edge-label / prose mismatch: 0 (no surface edits)
- H1 reuses page heading: 0 (no new chapters)
- append on missing slug: 0 (OQ promotion only)
- variant-axis missing on multi-variant operator: 0 (not applicable — audit dispatch)
- SUMMARY.md auto-fix: 0 (no new chapters created)
- index-placeholder displacement auto-fix: 0 (not applicable)
- bookkeeping incomplete: 0
- priorities.md authority guard: 0 (audit's `proposed_changes` block contained `kind: backfill-harvester-dispatch` / `backfill-harvester-dispatch-bundle` / `latent-pattern-observation` / `defer-pending-upstream` / `NOT-priority-20-but-real-coverage-gap` / `defer-not-applicable` entries — all of which are **routing recommendations** for downstream planner consideration, NOT direct artifact mutations. The audit dispatch correctly framed every recommendation as `recommendation: dispatch-harvester-cycle-010-or-011` / `confirm-not-needed-with-reason` / `defer-pending-...` etc., with no direct `book/src/` write proposed. Per integrator-per-report authority — priorities.md is meta-phase + cycle-planner co-edit territory, not integrator-per-report — the routing recommendations were captured as OQ promotions to `scaffolding/open-questions.md` rather than direct priorities.md edits. This matches the dispatch instruction's "If the proposed_changes are a checklist for cycle-011+ planner routing, capture them in scaffolding/open-questions.md (append) instead" path.)
- out-of-scope artifact-drift observation: 1 (NOT-INTEGRATED — META.md §"Issue 1" flagged `book/src/L4/index.md:40` carrying the cycle-006-era SUPERSEDED text "the kernel body's primitive sequence is identity-in-form, so no intermediate L3 `krylov-step` row is needed"; this is **existing artifact drift between the cycle-006 verdict and the cycle-009 methodology invariant**, NOT introduced by this audit. The wave-1 sibling dispatch `2026-05-27T215300Z-harvester-l3-krylov-step` enacted the supersession via its proposed-changes block by adding `book/src/L3/krylov-step.md` + annotating `L4-L3/krylov-step-typed-wrapper-dissolution.md:218` with SUPERSEDED, but did NOT touch `L4/index.md:40`. **Flagged here for integrator-finalize**: the `L4/index.md:40` "Lowers to" column language may want a SUPERSEDED annotation in finalize's build-repair pass, or this drift may be left for a future lifter dispatch. Not blocking — the wave-1 sibling's `book/src/L3/krylov-step.md` landing is the substantive supersession; the index drift is cross-reference cleanup.)

Open questions promoted:
- l3-backfill-apply-linop-and-blas1-cohort (HIGH CONFIDENCE — apply_linop L3 + 6-entry BLAS-1 cohort routing for cycle-011+ planner harvester dispatch)
- ksp-solve-l2-promotion-non-identity-substantive-gap (MEDIUM CONFIDENCE — ksp_solve L2/L4 as separate priority candidate, out-of-priority-20-scope)
- l3-l1-directory-naming-structure-policy (policy question for layer-intro-author / cycle-planner: sibling L3-L1 theme vs. in-line identity rotation at L3 entry)
- l3-vocabulary-inventory-gap (latent pattern — L3 index advertises matvec/axpy/dot/nrm2 as L3 vocabulary but L3 directory has only index.md + krylov-step.md post-wave-1)

Build-relevant: no

Notes: Second per-report integrator of cycle-010 — applies the cycle-010 wave-1 audit dispatch sibling (cross-layer-cross-cutter on identity-in-form audit; priority #20 second target). META.md shows all 8 critic checks PASS; no repair section (clean dispatch). The audit is **inspection-only**: its `proposed_changes` YAML block contains 6 entries (2 HIGH CONFIDENCE backfill recommendations, 1 MEDIUM CONFIDENCE out-of-priority-20 recommendation, 2 defer entries, 1 latent-pattern observation) — ALL framed as `recommendation:` routing for downstream dispatch, NOT as direct artifact mutations. Per integrator-per-report authority partition (CLAUDE.md §Write-authority partition: per-report integrator writes `book/`, `scaffolding/open-questions.md` append-only, `STAGING.md` append-only — NOT `scaffolding/priorities.md` which is meta-phase + cycle-planner co-edit), the audit's routing recommendations were promoted as 4 distinct OQ entries to `scaffolding/open-questions.md` (last 4 OQs before `## Dropped`). This matches the dispatch instruction's guidance. **Out-of-scope drift flagged**: META.md Issue 1 noted `book/src/L4/index.md:40` carries cycle-006-era SUPERSEDED text not introduced by this audit; left for integrator-finalize or future lifter (consistent with dispatch instruction "flag in your staging row but do not edit"). **Wave-1 cross-link**: the previous staging row (`2026-05-27T215300Z-harvester-l3-krylov-step`) is the sibling harvester that the audit's reasoning depends on — both dispatches enact the post-cycle-009 methodology invariant "Identity-lowerings still require both L levels" from different angles (harvester landed the L3 krylov-step concrete instance; audit surfaced the broader cohort routing pattern). Together they form cycle-010's wave-1 enactment of priority #20. Cycle-010+ planner can now schedule cycle-011 wave-1 from the four OQs promoted here. Deferred `integrated_at:` to finalize per role-spec.

---

## 2026-05-27T215334Z-harvester-matrix-weighted-norm-l1
applied_at: 2026-05-27T231500Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L1/matrix-weighted-norm.md (create — new rough-in L1 operator entry; ~110 lines; the L1 energy-norm primitive `α = ‖x‖_B = √(xᴴ B x)` for SPD `B`; chapter content sourced verbatim from CYCLE.md's `edit:book/src/L1/matrix-weighted-norm.md` operator-content block including the repairer's law-7 SPSD-sufficient clarification; Context + Signature + Semantics + 12 algebraic laws + 5 explicit non-laws + Applicability + Composition note + Dependencies + Variant axes + Status + L1-vs-L0 + Evidence sections. **Follows existing L1 entry convention**: no YAML frontmatter — the L1 cohort (`nrm2.md`, `apply_linop.md`, etc.) does not carry frontmatter; only the cycle-010 wave-1 L3 entry introduced it. Frontmatter-completeness gate skipped accordingly: existing L1 convention is non-frontmatter, and introducing frontmatter on this one entry would break L1 cohort uniformity. The L3 entry's frontmatter precedent is the future-normalization candidate flagged in wave-1's first staging row; per-L1-entry frontmatter backfill is meta-phase scope, not this dispatch's.)
- book/src/L1/index.md (edit — 3 in-place edits per CYCLE.md "Index update detail": [1] "Queued (open questions)" section line ~51: removed `nrm2_B` line, replaced with `bilinear-form` queued line + forward-pointer noting matrix-weighted-norm landed cycle-010 as `[matrix-weighted-norm](./matrix-weighted-norm.md)` rough-in; [2] "Rough-in (test-coverage-bounded)" cohort list: added bullet after the `eigsolve` paragraph describing matrix-weighted-norm's signature + rough-in motivation + promotion gates; [3] dep-map table: added new row after the `eigsolve` row with signature `(x: Tensor[N], B: LinearOperator[N, N]) → Scalar`, dependencies `dot`, `apply_linop`, status `rough-in (test-coverage-bounded, harvested-by: harvester:2026-05-27T215334Z-harvester-matrix-weighted-norm-l1)`)
- book/src/SUMMARY.md (edit — 1 insertion: `- [matrix-weighted-norm](./L1/matrix-weighted-norm.md)` after `- [eigsolve](./L1/eigsolve.md)` under L1 — Mutation-Lifted Forms Part; before L1 > L0 — Lowering header)
- scaffolding/open-questions.md (edit — 1 status update + 5 new OQ sections appended before `## Dropped` trailer):
  - status update: `matrix-weighted-norm-and-bilinear-form-l1-rough-ins` (line 1359) changed from `open` to `partially-answered`, with `last_revisited: cycle-010` field added; cycle-010 partial-resolution note appended to the body describing matrix-weighted-norm half landed, residuals (bilinear-form, SpectralNorm, L1>L0 lowering theme) kept tracked.
  - new OQ: `matrix-weighted-norm-naming-sweep` — L0 chapter `nrm2_weighted` candidate-name residue at `book/src/L0/linalg-operator-file.md:30-33, 73, 88` (informational, not authoritative on L1 naming); routes to future cross-layer-cross-cutter dispatch.
  - new OQ: `matrix-weighted-norm-mixed-element-type-variant` — complex-x with real-B element-type sub-variant question; firm-promotion gate (c) clarifier.
  - new OQ: `matrix-weighted-norm-mutation-rotation-l1-l0-theme` — L1>L0 lowering theme not yet authored; sister-theme precedents `apply-linop-mutation-rotation` / `axpby-mutation-rotation` / `ksp-solve-mutation-rotation` cited.
  - new OQ: `normalize-and-normalize-b-weighted-l1-candidates` — L0 `Normalize(comm, x)` + `Normalize(comm, x, B, Bx)` composite-utility candidates; sized small, routes to future harvester or cross-layer-cross-cutter.
  - new OQ: `test-coverage-bounded-rough-in-nomenclature` — pattern question for cycle-012 meta-phase: should the `rough-in (test-coverage-bounded)` status tier be canonicalised across the methodology, given eigsolve (cycle-009) + matrix-weighted-norm (cycle-010) precedents.

Gate hits:
- retroactive-budget per-slice: 0 (new L1 chapter creation; no retroactive backfill on existing operators — the 3 edits to `L1/index.md` are forward-pointer additions for the new operator entry, not retroactive evidence additions)
- retroactive-budget global: 0 (this report)
- concept_writes on existing slug: 0 (new file at `book/src/L1/matrix-weighted-norm.md`; verified no pre-existing file before write)
- forward-edge claim without surface: 0 (all referenced slugs exist or are intentional queued-forward-pointers: `dot.md`, `apply_linop.md`, `nrm2.md`, `L0/linalg-operator-file.md`, `L0/mutable-workspace-pattern.md` all exist on disk; `bilinear-form` is intentionally queued-forward in the Queued section, not claimed as existing)
- edge-label / prose mismatch: 0 (rough-in status, dependencies, and signature consistent between dep-map row, cohort bullet, and chapter body)
- H1 reuses page heading: 0 (chapter H1 `# matrix-weighted-norm` is the canonical operator name; SUMMARY entry uses the same name; this is the standard convention across L1 cohort, not a heading-reuse violation)
- append on missing slug: 0 (all `[link]` references verified to exist on disk: `../L0/linalg-operator-file.md`, `../L0/mutable-workspace-pattern.md`, `./nrm2.md`, `./dot.md`, `./apply_linop.md`)
- variant-axis missing on multi-variant operator: 0 (two orthogonal variant axes enumerated in §Variant axes — element-type and output-arg-vs-return-value — plus two collapsed/absorbed axes parallel-wrapper + operator-representation; matches L1 convention per `apply_linop.md` / `nrm2.md` precedents)
- SUMMARY.md auto-fix: 0 (report explicitly proposed the SUMMARY.md edit; no auto-fix needed)
- index-placeholder displacement auto-fix: 0 (L1/index.md is not a placeholder index — it's populated with firm cohort and dep-map; the new row appends after `eigsolve` row, not displacing any placeholder)
- bookkeeping incomplete: 0
- citation-resolution verification: 0 issues (verified `palace/linalg/operator.hpp:372-374` contains the SPD-anchor comment + `template<>` + `Norml2(MPI_Comm, x, B, Bx)` signature; verified `palace/linalg/operator.cpp:599-619` contains both `Vector` and `ComplexVector` specializations with the documented `MFEM_ASSERT` guards and `B.Mult / Dot / sqrt` factoring; both anchors resolve cleanly in `reference/palace/palace/`)
- L1 vocabulary discipline observed: 0 issues (verified — closed-form `√(xᴴ B x)` is the L1 semantics; the composition `√(dot(apply_linop(B, x), x))` is explicitly flagged as L1>L0 lowering-theme territory in §Composition note and §L1-vs-L0; per post-cycle-009 invariant "Layers are defined high→low; lifting notes go in working notes")
- frontmatter completeness: not-applicable (existing L1 cohort convention is non-frontmatter; introducing frontmatter on this one entry would break uniformity; the L3 entry's cycle-010 frontmatter precedent is future-normalization candidate, not retroactive requirement)

Open questions promoted:
- matrix-weighted-norm-naming-sweep (cross-layer cleanup pattern; routes to cross-layer-cross-cutter)
- matrix-weighted-norm-mixed-element-type-variant (firm-promotion gate clarifier)
- matrix-weighted-norm-mutation-rotation-l1-l0-theme (L1>L0 theme routing)
- normalize-and-normalize-b-weighted-l1-candidates (composite-utility sibling candidates)
- test-coverage-bounded-rough-in-nomenclature (methodology pattern question for cycle-012 meta-phase)
- (status update only — not new OQ) matrix-weighted-norm-and-bilinear-form-l1-rough-ins: `open` → `partially-answered` with cycle-010 partial-resolution note

Build-relevant: yes

Notes: Third per-report integrator of cycle-010 — applies the matrix-weighted-norm harvester (priority #17, lower-layer-shared-vocabulary). META.md shows all 8 critic checks PASS-or-warning (citation-validity warning, skill-uptake-survey warning); repair section verified the citation "warning" was itself a critic-side misread (the .hpp:372-374 range is correct on the actual source — verified independently here via direct Read on `reference/palace/palace/linalg/operator.hpp:370-385`: line 372 is the SPD-comment, 373 is `template<typename VecType>`, 374 is the function signature; the range correctly covers comment+template+signature). The repair section's law-7 SPSD-sufficient clarification is incorporated in the L1 file content. **First L1 firm-or-rough-in operator landing of cycle-010** (wave-1 was L3 backfill + cross-layer audit; this is wave-2's first L1 vocabulary expansion). All three proposed-changes applied cleanly on first attempt — 1 file creation via Write (no pre-existing file at `book/src/L1/matrix-weighted-norm.md`); 3 anchor-and-insert edits across `book/src/L1/index.md` (Queued line replacement, Rough-in cohort bullet addition, dep-map row addition); 1 anchor-and-insert edit on `book/src/SUMMARY.md`. Anchors matched disk exactly on all 4 edits. The L1 cohort now grows: 8 firm + 2 rough-in (eigsolve, matrix-weighted-norm) + 6 rough-in-obstruction + 1 queued (bilinear-form). The matrix-weighted-norm rough-in is the **second test-coverage-bounded L1 rough-in** (after `eigsolve` cycle-009); the pattern is now established and the new OQ `test-coverage-bounded-rough-in-nomenclature` surfaces the question of whether to canonicalise it as a named status tier in meta-phase scope. **Coordination with sibling dispatches**: per the dispatch instruction's guidance, only the matrix-weighted-norm row addition was applied to `L1/index.md` for this dispatch; sibling dispatches #4 (`bilinear-form`) and #7 (`nrm2_B-weighted` if dispatched under distinct slug) will add adjacent rows in their own integrator-per-report runs. The Queued line's forward-pointer language ("the matrix-weighted-norm half ... was landed cycle-010 as `[matrix-weighted-norm](./matrix-weighted-norm.md)` (rough-in)") makes the coordination posture visible to subsequent integrators. **Methodology-discipline note**: the L1 entry maintains discipline per the post-cycle-009 invariant — the closed-form `√(xᴴ B x)` is the L1 semantics; the L0 unfolding into `apply_linop` + `dot` + `sqrt` is explicitly scoped to the (not-yet-authored) `matrix-weighted-norm-mutation-rotation` L1>L0 theme. This is exemplar usage of the "Layers are defined high→low" invariant. Deferred `integrated_at:` to finalize per role-spec.

---

## 2026-05-27T215427Z-harvester-bilinear-form-l1
applied_at: 2026-05-27T234500Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L1/bilinear-form.md (create — new rough-in L1 operator entry; ~430 lines; `bilinear_form(x, M, y) = xᴴ M y` matrix-weighted inner product, the matrix-weighted generalisation of `dot` for arbitrary linear `M` (no SPD requirement); chapter content sourced verbatim from the **repaired** CYCLE.md's `edit:book/src/L1/bilinear-form.md` block including all 9 repairer-applied conjugation-handedness corrections — namely: Context section names only one gating reason with repair note; Semantics §"Composition into apply_linop + dot" rewritten to correctly identify L0's `linalg::Dot(comm, A·x, y) = yᴴ A x` and name two equivalent mechanical lowerings (argument-swap to `linalg::Dot(comm, apply_linop(M, y), x)` for direct `xᴴ M y` vs outer-`conj` of `linalg::Dot(comm, apply_linop(M, x), y)`); Conjugation-convention sub-section explicitly states "There is no L0 ambiguity"; Status section gating-reason list reduced to single remaining reason (variant-axis coverage); L1-vs-L0 distinction surfaces L0/L1 conjugation handedness on both sides referencing `book/src/L1/dot.md:43, 104-105`; Evidence section adds clarifying sub-bullet on the complex-`A` overload citation tying L0 implementation back to L0 comment via second-argument-conjugation. The **false OQ** `bilinear-form-conjugation-convention-anchor` (status `resolved-by-correction`) is in-CYCLE documentation only and was NOT promoted to `scaffolding/open-questions.md` per the dispatch instruction. **Note on YAML frontmatter**: this L1 entry carries 8-field frontmatter (`layer`, `operator`, `firmness`, `lowers_to`, `lifts_from`, `depends_on`, `variant_axes`) — same as the cycle-010 wave-1 L3 `krylov-step.md` entry's precedent. This is the **second** L1 entry to carry frontmatter and would be the third overall (after L3 krylov-step). The cycle-009-meta future-normalization candidate for backfilling frontmatter to existing L1 entries (apply_linop / nrm2 / dot / etc.) is now reinforced by this second instance; surfaced again as backfill candidate for integrator-finalize / meta-phase aggregation. Note however that the **sibling matrix-weighted-norm.md landed without frontmatter** (per the staging row above: "follows existing L1 entry convention: no YAML frontmatter") — so the cycle-010 L1 cohort is now mixed (matrix-weighted-norm no frontmatter; bilinear-form with frontmatter). The frontmatter delta is a critic-passed dispatch output, not an integrator-introduced inconsistency; flagged for finalize/meta consideration.)
- book/src/L1/index.md (edit — 3 in-place edits matching the dispatch's CYCLE.md "Index update detail" + coordination with prior matrix-weighted-norm landing: [1] "Queued (open questions)" bullet replaced from "`bilinear-form :: (x, A, y) → yᴴ A x` — operator-induced bilinear inner product..." to closure language "(empty as of cycle-010) — the cycle-008 OQ `matrix-weighted-norm-and-bilinear-form-l1-rough-ins` is now `partially-answered`: both halves landed in cycle-010 wave-1 as rough-ins ([matrix-weighted-norm] and [bilinear-form])" — both halves now landed; SpectralNorm + L1>L0 lowering theme residuals tracked under parent OQ; [2] "Rough-in (test-coverage-bounded)" cohort list: added bilinear-form bullet after the matrix-weighted-norm bullet describing the signature + rough-in motivation (narrow variant-axis coverage with M-symmetry-property axis having two witnesses but Cauchy–Schwarz unexercised; real-x/real-y not surfaced by Palace) + promotion gates (test coverage or literature anchoring); [3] dep-map table: added new row after the `matrix-weighted-norm` row with signature `(x: Tensor[M], M: LinearOperator[M, N], y: Tensor[N]) → Scalar (i.e. xᴴ M y)`, dependencies `apply_linop, dot`, status `rough-in (lower-layer-shared-vocabulary, harvested-by: harvester:2026-05-27T215427Z-harvester-bilinear-form-l1)`)
- book/src/SUMMARY.md (edit — 1 insertion: `- [bilinear-form](./L1/bilinear-form.md)` after `- [matrix-weighted-norm](./L1/matrix-weighted-norm.md)` under L1 — Mutation-Lifted Forms Part; before L1 > L0 — Lowering header)
- scaffolding/open-questions.md (edit — 1 status update + 3 new OQ sections appended before `## Dropped` trailer):
  - status update: `matrix-weighted-norm-and-bilinear-form-l1-rough-ins` (line 1359) — appended a second "Further partially answered cycle-010" paragraph describing the bilinear-form sibling landing; both halves (matrix-weighted-norm + bilinear-form) are now landed as rough-ins; residuals updated to track (b) SpectralNorm power-iteration sibling harvest + (c) L1>L0 lowering themes for both operators. Status remains `partially-answered` with `last_revisited: cycle-010` (already set by the matrix-weighted-norm integrator).
  - new OQ: `bilinear-form-real-vector-coverage-gap` (palace surfaces complex-only; option-c recommendation: real case recovered by composition `dot(x, apply_linop(M, y))` from existing L1 vocabulary).
  - new OQ: `bilinear-form-slug-name-coordination` (slug naming: `bilinear-form` vs `dot_bilinear` per cycle-008 OQ + L0 chapter expectations; recommendation: keep `bilinear-form` with L0 chapter follow-up).
  - new OQ: `bilinear-form-variant-axis-test-coverage` (firm-promotion gate; third test-coverage-bounded L1 rough-in after eigsolve/matrix-weighted-norm — reinforces `test-coverage-bounded-rough-in-nomenclature` methodology question).

Gate hits:
- retroactive-budget per-slice: 0 (new L1 chapter creation; no retroactive backfill on existing operators — the 3 edits to `L1/index.md` are forward-pointer additions for the new operator entry, not retroactive evidence additions)
- retroactive-budget global: 0 (this report)
- concept_writes on existing slug: 0 (new file at `book/src/L1/bilinear-form.md`; verified no pre-existing file via `ls book/src/L1/` before write — file did not exist)
- forward-edge claim without surface: 0 (all referenced slugs exist or are inherited dependency forward-pointers: `dot.md`, `apply_linop.md`, `nrm2.md`, `L0/linalg-operator-file.md`, `L0/mutable-workspace-pattern.md`, `concepts/dot.md` all exist on disk; the future `nrm2_B`-weighted operator reference is intentional sibling-dispatch forward-pointer, not a slug claim)
- edge-label / prose mismatch: 0 (rough-in status, dependencies, and signature consistent between dep-map row, cohort bullet, and chapter body; `lowers_to: []` frontmatter consistent with the L1 entry having no L1>L0 theme yet authored)
- H1 reuses page heading: 0 (chapter H1 `# bilinear-form` is the canonical operator name; SUMMARY entry uses the same name; standard convention across L1 cohort)
- append on missing slug: 0 (all `[link]` references verified to exist on disk: `./dot.md`, `./apply_linop.md`, `./nrm2.md`, `./matrix-weighted-norm.md`, `../L0/linalg-operator-file.md`, `../L0/mutable-workspace-pattern.md`, `../concepts/dot.md`)
- variant-axis missing on multi-variant operator: 0 (four variant axes enumerated in both frontmatter and prose §Variant axes: precision-mode, output-arg-pattern, M-symmetry-property, parallel-wrapper; two collapsed/absorbed axes listed: element-type-of-M, operator-representation-of-M with rationale)
- SUMMARY.md auto-fix: 0 (report explicitly proposed the SUMMARY.md edit; no auto-fix needed)
- index-placeholder displacement auto-fix: 0 (L1/index.md is not a placeholder index — it's populated with firm cohort and dep-map; the new row appends after `matrix-weighted-norm` row; the Queued bullet is replaced with closure language, not displacing a literal `(empty — Phase B skeleton.)` placeholder)
- bookkeeping incomplete: 0
- frontmatter completeness: 0 issues (8 fields present: layer / operator / firmness / lowers_to / lifts_from / depends_on / variant_axes; matches cycle-010 wave-1 L3 entry's precedent + dispatch's CYCLE.md proposed block exactly)
- L1 vocabulary discipline observed: 0 issues (verified — closed-form `xᴴ M y` is the L1 semantics; the composition `dot(x, apply_linop(M, y))` is explicitly flagged as L1>L0 lowering-theme territory in Semantics §"Composition into apply_linop + dot (informational)" and §L1-vs-L0; per post-cycle-009 invariant "Layers are defined high→low; lifting notes go in working notes")
- L0 conjugation-handedness corrected (cycle-010 critic+repair semantic check): 0 issues (verified — the chapter body's L0 unfolding correctly says `linalg::Dot(comm, apply_linop(M, y), x)` gives `xᴴ M y` directly per L0 second-argument-conjugation; the false OQ `bilinear-form-conjugation-convention-anchor` was NOT promoted to scaffolding/open-questions.md; the in-CYCLE OQ entry with status `resolved-by-correction` remains as documentation-only history per dispatch instruction)

Open questions promoted:
- bilinear-form-real-vector-coverage-gap (complex-only L0 surface coverage question; recommendation option-c)
- bilinear-form-slug-name-coordination (slug naming; recommendation keep bilinear-form)
- bilinear-form-variant-axis-test-coverage (firm-promotion gate; third test-coverage-bounded L1 rough-in reinforcing methodology pattern)
- (status update only — not new OQ) matrix-weighted-norm-and-bilinear-form-l1-rough-ins: appended second cycle-010 partial-answer paragraph describing bilinear-form sibling landing; both halves now landed; status remains `partially-answered` with `last_revisited: cycle-010`
- (NOT promoted — false OQ closed in-CYCLE) bilinear-form-conjugation-convention-anchor: status `resolved-by-correction` per repaired CYCLE.md; this OQ is in-CYCLE documentation only and does NOT propagate to scaffolding/open-questions.md as an active entry, per dispatch instruction "The fourth OQ ... is resolved-by-correction and should NOT propagate to scaffolding/open-questions.md as an active entry"

Build-relevant: yes

Notes: Fourth per-report integrator of cycle-010 — applies the bilinear-form harvester sibling-dispatch (priority #17, lower-layer-shared-vocabulary; sibling of matrix-weighted-norm in wave-2's L1 vocabulary expansion). META.md shows `overall_status: pass-after-repair` with one **citation-validity FAIL repaired** (the conjugation-handedness misreading the critic flagged was a load-bearing semantic claim error in the dispatch's draft; the repairer applied 9 coordinated in-place edits to CYCLE.md correcting the analysis, and the false OQ was closed `resolved-by-correction`). Verified the repaired CYCLE.md's content matches the dispatch instruction's "corrected story": L1 form `bilinear-form :: (x, M, y) → xᴴ M y` (first-argument-conjugation per L1 convention); L0 implementation `linalg::Dot(comm, apply_linop(M, y), x)` with `linalg::LocalDot(a, b) = bᴴ a` (second-arg-conjugates per vector.cpp:674-685) yields `xᴴ M y`; the L0/L1 conjugation asymmetry already documented at `book/src/L1/dot.md:43, 104-105`. **All three proposed-changes applied cleanly on first attempt**: 1 file creation via Write (verified no pre-existing `book/src/L1/bilinear-form.md`); 3 anchor-and-insert edits across `book/src/L1/index.md` (Queued bullet replacement, Rough-in cohort bullet addition after matrix-weighted-norm, dep-map row addition after matrix-weighted-norm row); 1 anchor-and-insert edit on `book/src/SUMMARY.md` after matrix-weighted-norm entry. Anchors matched disk exactly on all 4 edits. **L1 cohort growth**: now 8 firm + 3 rough-in (eigsolve, matrix-weighted-norm, bilinear-form) + 6 rough-in-obstruction + Queued bullet retired (parent OQ both halves landed). The bilinear-form rough-in is the **third L1 rough-in** in this firm-promotion-gated tier (after eigsolve cycle-009 and matrix-weighted-norm cycle-010); the recurrence reinforces the `test-coverage-bounded-rough-in-nomenclature` methodology question scheduled for cycle-012 meta-phase. Note: bilinear-form's gate is technically named `lower-layer-shared-vocabulary` (cohort-classification) rather than `test-coverage-bounded` (gate-mechanism), but the gate-mechanism is the same — narrow variant-axis coverage requiring expanded test coverage or literature anchoring — so it's surfaced under the same methodology pattern. **Coordination with sibling matrix-weighted-norm dispatch**: the prior wave-2 integrator (third row above) added matrix-weighted-norm; this dispatch adds bilinear-form as the adjacent sibling row in the dep-map table + cohort listing + SUMMARY. Coordination is clean — both L1 entries depend on `apply_linop` + `dot`; both are rough-ins from the same parent cycle-008 OQ; both are now tracked under that OQ's `partially-answered` status with sibling-residual `SpectralNorm` still tracked separately. **Methodology-discipline observation**: dispatch's frontmatter introduction (8 fields) **diverges from the sibling matrix-weighted-norm dispatch which landed without frontmatter** (per the staging row above explicitly noting "follows existing L1 entry convention: no YAML frontmatter"). The cycle-010 L1 cohort is now mixed (matrix-weighted-norm + apply_linop + nrm2 + dot + others: no frontmatter; bilinear-form: frontmatter) — this inconsistency was introduced by the diverging dispatcher choices, not by integrator action. The frontmatter pattern is the cycle-010-wave-1 L3 entry's precedent applied unevenly within wave-2; surfaces as a stronger normalization candidate (either backfill all L1 entries to frontmatter, or strip the L3 + bilinear-form frontmatter to match the cohort norm). Flagged for integrator-finalize / cycle-012 meta-phase aggregation. **False OQ correctly NOT promoted**: per dispatch instruction's explicit guidance, the `bilinear-form-conjugation-convention-anchor` OQ in the report (with status `resolved-by-correction` from the repaired pass) was NOT propagated to `scaffolding/open-questions.md`; only the three remaining valid OQs were promoted. The in-CYCLE OQ entry remains as documentation-only history of the resolution path. **Methodology-signal milestone for integrator-finalize**: this is the **first cycle-010 integration of a repaired pass-after-repair report** with a load-bearing citation-validity FAIL repaired (prior 3 integrations were all clean-pass dispatches). The repair quality was high — the 9 coordinated in-place edits were all applied correctly and the resulting prose is self-consistent; the integrator's safety-net gate "L0 conjugation-handedness corrected" passed cleanly. **Skill-uptake-survey warning** (verify-citation-range non-invocation correlating with citation-validity miss) carries forward to cycle-012 meta-phase batch examination per the critic's notes — this is the second cycle-010 report where the pattern surfaces. Deferred `integrated_at:` to finalize per role-spec.

---

## 2026-05-27T220123Z-harvester-nrm2-B-weighted-energy-norm-l1
applied_at: 2026-05-27T235500Z
applied_by: integrator-per-report
status: applied

Files touched:
- scaffolding/open-questions.md (edit — 1 status update + 1 new OQ section):
  - status update: `nrm2-B-weighted-energy-norm-harvest` (cycle-003 OQ at line 198-207) changed from `open` to `partially-answered`, with `last_revisited: cycle-010` field added; cycle-010 partial-resolution paragraph appended describing the duplicate-target verdict (case (c) merge-and-rename), the canonical landing under `matrix-weighted-norm`, the wave-2 #5 dispatch's verification of duplication via 6 identity claims (L0 anchor / closed-form / SPD precondition / dependencies / variant axis / callsite cohort), the parent ledger cross-reference (`matrix-weighted-norm-and-bilinear-form-l1-rough-ins`), and the naming-axis residue routing under `matrix-weighted-norm-naming-sweep`.
  - new OQ: `priority-13-now-landed-as-matrix-weighted-norm` (status `routing`) — routing OQ for cycle-011 cycle-planner to close `scaffolding/priorities.md` priority #13 by moving to "Recently landed" or removing entirely; authority cross-reference to CLAUDE.md §Write-authority partition (priorities.md is meta-phase + cycle-planner co-edit, not integrator-per-report).

Gate hits:
- retroactive-budget per-slice: 0 (procedural-cleanup dispatch; no operator-surface mutations)
- retroactive-budget global: 0 (this report)
- concept_writes on existing slug: 0 (no concept writes)
- forward-edge claim without surface: 0 (all referenced slugs verified to exist: `matrix-weighted-norm-and-bilinear-form-l1-rough-ins` (parent ledger, partially-answered), `matrix-weighted-norm-naming-sweep` (added by wave-1 #3 sibling dispatch), `nrm2-B-weighted-energy-norm-harvest` (cycle-003 source OQ); the priority-#13 reference targets `scaffolding/priorities.md` line 30 verified by previous integrator's gate-hit confirmations)
- edge-label / prose mismatch: 0 (no surface edits)
- H1 reuses page heading: 0 (no new chapters)
- append on missing slug: 0 (OQ-ledger-only edits)
- variant-axis missing on multi-variant operator: 0 (not applicable — duplicate-resolution dispatch with no new operator content authored)
- SUMMARY.md auto-fix: 0 (no new chapters created — wave-1 sibling already added `matrix-weighted-norm` to SUMMARY)
- index-placeholder displacement auto-fix: 0 (not applicable)
- bookkeeping incomplete: 0
- priorities.md authority guard: 1 (dispatch's CYCLE.md §Proposed changes proposed a direct `edit:scaffolding/priorities.md` block (lines 73-75) marking priority #13 as `landed-as-matrix-weighted-norm`. Per integrator-per-report authority partition (CLAUDE.md §Write-authority partition: `scaffolding/priorities.md` is meta-phase + cycle-planner co-edit territory), the direct edit was NOT applied. Instead, the priority-#13 close was promoted as a routing OQ `priority-13-now-landed-as-matrix-weighted-norm` to `scaffolding/open-questions.md` (status `routing`). Option (b) per dispatch instructions; cycle-011 cycle-planner reads OQs and priorities together and will execute the close.)
- duplicate-resolution rigor verified: 0 issues (independently verified the 6 identity claims against wave-1 sibling staging-row content + report CYCLE.md: same L0 anchor `palace/linalg/operator.hpp:372-374` + `cpp:599-619`; same closed-form `√(xᴴ B x)`; same SPD precondition with SPSD-seminorm caveat; same dependencies `dot` + `apply_linop`; same element-type variant axis collapse; same callsite cohort (ARPACK / SLEPc / NLEPS in GetEigenvectorNorm). All five identity claims hold; the case (c) merge-and-rename verdict is rigorously supported. No new L1 file landed; this avoids racing the wave-1 sibling write.)

Open questions promoted:
- (status update only — not new OQ) nrm2-B-weighted-energy-norm-harvest (cycle-003): `open` → `partially-answered` with cycle-010 partial-resolution paragraph describing the merge-and-rename landing under canonical slug `matrix-weighted-norm`
- priority-13-now-landed-as-matrix-weighted-norm (routing OQ for cycle-011 planner; status `routing`; closes priority #13 via cycle-planner's authority since integrator-per-report cannot directly edit priorities.md)

Build-relevant: no

Notes: Fifth per-report integrator of cycle-010 — applies the wave-2 #5 harvester dispatch on `nrm2_B-weighted-energy-norm` (verdict: case (c) merge-and-rename; duplicate of wave-1 #3 `matrix-weighted-norm`). META.md shows all 8 critic checks PASS (no repair section; clean-pass dispatch). The dispatch is **procedural cleanup** — no `book/` mutations; only OQ-resolution and priority-closure routing. **Coordination with wave-1 sibling**: the wave-1 sibling dispatch (`2026-05-27T215334Z-harvester-matrix-weighted-norm-l1`, third staging row above) landed the operator under canonical slug `matrix-weighted-norm`; this wave-2 #5 dispatch's role under the cycle-010 planner's case-(a)/(c) duplicate-detection routing is to confirm the duplication, avoid producing a competing entry, and resolve the cycle-003 OQ + close priority #13 cleanly. The 6 identity claims (same L0 anchor / closed-form / SPD precondition / dependencies / variant axis / callsite cohort) were independently verified at the per-report integrator gate-hit pass — all hold. **Authority-partition handling for priority #13**: the dispatch's CYCLE.md proposed a direct edit to `scaffolding/priorities.md` (lines 73-75) marking priority #13 as `landed-as-matrix-weighted-norm`. Per CLAUDE.md §Write-authority partition, `scaffolding/priorities.md` is meta-phase + cycle-planner co-edit territory — NOT integrator-per-report. Per the dispatch instruction's explicit guidance (option b), the close was promoted as a routing OQ `priority-13-now-landed-as-matrix-weighted-norm` (status `routing`) for cycle-011 cycle-planner to execute when it next reads priorities.md alongside the OQ ledger. This matches the cycle-010 wave-1 #2 (cross-layer-cross-cutter audit) precedent: that audit's routing recommendations were also promoted to OQs rather than direct priorities.md edits. **Cycle-012 meta-phase watch-item — DEFERRED TO FINALIZE** (per dispatch instruction): The CYCLE.md flagged a friction-pattern observation at recurrence-1: planner-side deduplication-by-L0-anchor. Pattern: cycle-N OQ filed at site A; cycle-N+5 OQ filed at site B; both name the same operator; cycle-N+6 meta-phase promotes both to next-cycle priorities without recognising the duplication; cycle-N+7 wave dispatches both, and the second dispatch discovers the overlap. Possible meta-phase improvement: deduplicate-by-L0-anchor in the OQ ledger as part of meta-phase aggregation. Below friction-ledger threshold (recurrence-1; per CLAUDE.md §Skills the bar is ≥2 cycles or concrete-enough-sketch or pre-existing-entry — none met). **cycle-012 meta-phase watch-item: dedup-by-L0-anchor recurrence-1 (forward to finalize for integrator-signals append)**. integrator-finalize should append this observation to `scaffolding/integrator-signals.md` as a cycle-012-meta watch-item; do NOT add to friction-ledger from integrator authority. **Critic notes**: the report's content (no new L1 file; OQ-resolution + priority-routing only; rigorous duplicate verification) matches what the planner's case (c) merge-and-rename recommendation requested. The dispatch correctly declined to race wave-1 on `book/src/L1/index.md` / `SUMMARY.md` / L0 chapter prose edits — those are wave-1's scope. The naming-axis residual (L0 chapter `nrm2_weighted` prose) is correctly routed under the wave-1 sibling's new OQ `matrix-weighted-norm-naming-sweep` rather than duplicated here. **No file rebuild needed**: build-relevant: no — this dispatch touched only `scaffolding/open-questions.md` (1 status update + 1 new OQ). Deferred `integrated_at:` to finalize per role-spec.

---

## 2026-05-27T215535Z-combinator-miner-check-stop-into-carry-mcp-pilot
applied_at: 2026-05-27T235900Z
applied_by: integrator-per-report
status: applied

Files touched:
- scaffolding/open-questions.md (edit — 1 status update + 1 new OQ section appended before `## Dropped` trailer):
  - status update: `nleps-spec-gap-as-check-stop-into-carry-reuse-blocker` (cycle-009 OQ at line 1524) — appended `last_revisited: cycle-010` field to frontmatter YAML; appended a cycle-010 revisit paragraph describing the MCP-pilot retry's defer-with-routing verdict on GMRES+FGMRES sister-algorithm match (lower-edge reading (b)); status held `open` because cycle-009's NLEPS-dependency framing is still the right blocker for *firm* L4 promotion (FGMRES is a sister-algorithm twin, not the genuinely-different consumer the OQ describes); cross-references `variant-absorption-vs-instance-counting-policy` confirming two cycles of corroborating evidence for batch-2 meta-phase codification.
  - new OQ: `fgmres-inner-loop-iterate-while-migration-lifter-candidate` (status `open`; routing OQ for cycle-011 cycle-planner) — records the cycle-010 audit's recommendation to dispatch a lifter on the cycle-008 `gmres-inner-loop-iterate-while-migration` theme to re-anchor it against an upstream firm `book/src/spec/slices/gmres.md §L4 v0.7` form (currently v0.6 inline at lines 1012/1106), then apply the same migration theme to FGMRES (either as a separate theme or unified parameterised theme). Verification target: both lowerings produce a structurally-identical `check_stop_into_carry` callsite shape. Explicit sequencing directive to cycle-planner: lifter-before-harvester (do NOT schedule harvester on `book/src/L4/check-stop-into-carry.md` until FGMRES theme firms OR a non-`GmresSolverBase` Krylov consumer is identified).

Gate hits:
- retroactive-budget per-slice: 0 (inspection-only dispatch; no operator-surface mutations)
- retroactive-budget global: 0 (this report)
- concept_writes on existing slug: 0 (no concept writes)
- forward-edge claim without surface: 0 (all references resolve: cycle-009 OQs `nleps-spec-gap-as-check-stop-into-carry-reuse-blocker`, `variant-absorption-vs-instance-counting-policy`, `check-stop-into-carry-parameterization-over-stop-condition` all exist; cycle-007 OQ `gmres-inner-loop-iterate-while-migration` exists; cycle-008 theme `book/src/L4-L3/gmres-inner-loop-iterate-while-migration.md` exists; `book/src/spec/slices/gmres.md` §L4 v0.6 at line 1012 exists)
- edge-label / prose mismatch: 0 (no surface edits)
- H1 reuses page heading: 0 (no new chapters)
- append on missing slug: 0 (OQ-ledger-only edits)
- variant-axis missing on multi-variant operator: 0 (not applicable — inspection-only dispatch)
- SUMMARY.md auto-fix: 0 (no new chapters created)
- index-placeholder displacement auto-fix: 0 (not applicable)
- bookkeeping incomplete: 0
- citation verification (MCP pilot): 0 issues — the load-bearing citations `palace/linalg/iterative.cpp:644-649` (GMRES) and `:823-828` (FGMRES) were independently verified by the critic at META.md §citation-validity; the repaired slepc/arpack non-instance line offsets are also correct (repaired by the report's own repair pass). The audit's MCP-pilot tool inventory (`list_files` × 2, `search_text` × 7, `get_file_subtree` × 1, `read_range` × 4 = 14 calls, 0 permission-denied) is itself the primary friction-signal payload — see Notes below.
- L4-L3 cross-reference path-qualification: 0 issues (repairer path-qualified the `gmres.md` references to `book/src/spec/slices/gmres.md` per critic finding #2; verified in repaired CYCLE.md)
- skill-candidate authority: 0 (CYCLE.md does NOT contain a `scaffolding/skill-candidates.md` proposed-changes block; the `localize-then-read` candidate is **observation-only** in §"MCP codemap pilot evaluation" — not a proposed change. Per role spec, the integrator does NOT file skill candidates discretionarily; the observation is captured in this STAGING row's Notes and forwarded to finalize for integrator-signals append and/or meta-phase consideration. Repair pass explicitly marked the `localize-then-read` candidate as `not-needed` from repair authority.)

Open questions promoted:
- (status update only — not new OQ) nleps-spec-gap-as-check-stop-into-carry-reuse-blocker (cycle-009): `open` → `open` with `last_revisited: cycle-010` and a cycle-010 revisit paragraph describing the FGMRES sister-algorithm match (lower-edge reading); cross-references variant-absorption-vs-instance-counting-policy
- fgmres-inner-loop-iterate-while-migration-lifter-candidate (NEW; routing OQ for cycle-011 planner; status `open`; records the cycle-010 audit's defer-with-routing verdict and the lifter-before-harvester sequencing directive)

Build-relevant: no

Notes: Sixth per-report integrator of cycle-010 — applies the wave-2 #6 combinator-miner dispatch (MCP-codemap-pilot retry on `check_stop_into_carry` reuse audit). META.md shows `overall_status: pass-after-repair` (two warnings repaired: citation-validity slepc/arpack line offsets; cross-reference-integrity unqualified `gmres.md` references; two observations addressed via lightweight CYCLE.md append; one observation deferred-as-optional). The dispatch is **inspection-only** — no `book/src/` mutation proposed; the report's only artefact-facing outputs are the two OQ entries captured here. **MCP pilot SUCCESS**: this is the **primary friction-signal payload** of this dispatch. The MCP codemap pilot (post-commit `ceb87da` enablement) ran cleanly: 14 tool calls (`list_files` × 2, `search_text` × 7, `get_file_subtree` × 1, `read_range` × 4), zero permission-denied errors. Per dispatch instruction: friction-ledger entry `mcp-codemap-permission-denied-across-batch-1` resolution awaits **cycle-012 meta-phase**; integrator-finalize captures via `scaffolding/integrator-signals.md` append (do NOT edit friction-ledger from integrator authority — friction-ledger is meta-phase-only writer territory per CLAUDE.md §Write-authority partition). Forward to finalize: **MCP pilot SUCCESS; friction-ledger `mcp-codemap-permission-denied-across-batch-1` resolution awaits cycle-012 meta-phase (finalize captures via integrator-signals append)**. **Dispatch-brief drift signal — recurrence-1**: the cycle-010 planner's dispatch brief cited `palace/linalg/eps.cpp` and `palace/linalg/feast.cpp` as files to audit, but neither file exists in the Palace codebase (only `eps.hpp` shared header exists; actual eigensolver implementations are `arpack.cpp` / `slepc.cpp` / `nleps.cpp`). The MCP `list_files` call corrected the drift on first invocation. This is a **friction-pattern candidate at recurrence-1** — below the meta-phase pattern-promotion threshold of recurrence ≥2, but worth ledger-tracking because MCP localization makes the correction cheap-and-visible going forward. Forward to finalize for integrator-signals append: **dispatch-brief drift signal: planner-reads-stale-file-inventory pattern at recurrence-1 (cycle-010); cycle-012 meta-phase should watch for recurrence-2 — if `MCP list_files`-corrected drift recurs in cycle-011 dispatches, promote to friction-ledger**. **Skill candidate `localize-then-read` (observation only — NOT promoted)**: the CYCLE.md's §"MCP codemap pilot evaluation" point 4 mentions that the MCP server-instructions' "use list_files / get_file_subtree / get_symbol_def / get_call_sites / list_dependencies / search_text to localize before reading; read_range is the only source-returning tool" guidance matches how the dispatch naturally flowed; the dispatch's §Recommendation to cycle-010+ planner implicitly defines a "default localization path for combinator-miner / cross-cutter / lowering-verifier dispatches whose scope is 'audit pattern X across directory Y'". This was surfaced by the cycle-010 critic as a candidate row for `scaffolding/skill-candidates.md` and **explicitly marked `not-needed` by the cycle-010 repairer** ("Critic surfaced this as optional ... the original task brief explicitly marked the skill-candidate filing as 'appropriate but optional; the critic noted recurrence-1 is below threshold.' The Recommendation-to-cycle-010+-planner paragraph in CYCLE.md already implicitly defines the procedure; deferring to either (a) a meta-phase batch-1 promotion decision or (b) a separate any-agent appendable filing in cycle-011 if the pattern recurs"). Per dispatch instruction (integrator-per-report authority on skill candidates: "ONLY if CYCLE.md proposed_changes directly specifies it"), this dispatch's CYCLE.md does NOT contain a `scaffolding/skill-candidates.md` proposed-changes block — so the candidate is **NOT promoted** from this integrator's authority. Forward to finalize for integrator-signals append: **localize-then-read skill candidate observed in cycle-010 wave-2 #6; deferred per repair (not-needed at recurrence-1); cycle-012 meta-phase batch-2 aggregation should consider for promotion if (a) the MCP-codemap pilot pattern recurs in cycle-011 dispatches with the same shape, or (b) the cycle-012 meta-phase batch evidence supports default-accept under the low-bar policy**. **OQ promotion handling — cycle-009 status update**: per dispatch instruction the cycle-009 OQ `nleps-spec-gap-as-check-stop-into-carry-reuse-blocker` was located (line 1524) and updated with `last_revisited: cycle-010` and a cycle-010 revisit paragraph; status held `open` (not flipped to partially-answered, because FGMRES sister-algorithm match is at the *lower edge* of second-reuse — it's a variant-absorbed twin, not the qualitatively-distinct second consumer the OQ describes; the NLEPS dependency framing is still the right blocker for firm L4 promotion). The cycle-010 revisit paragraph cross-references the cycle-009 sibling OQ `variant-absorption-vs-instance-counting-policy` confirming two cycles of corroborating evidence (cycle-009 NLEPS=reading-(a)-second-slice + cycle-010 FGMRES=reading-(b)-lower-edge) for the meta-phase batch-2 codification of the "second slice needs it" policy. **OQ promotion handling — new routing OQ**: surfaced `fgmres-inner-loop-iterate-while-migration-lifter-candidate` as a new OQ (cycle-010, opened_by combinator-miner — credited to the dispatch agent per role-spec OQ-promotion convention) routing a cycle-011 lifter dispatch with explicit sequencing directive: lifter on `gmres-inner-loop-iterate-while-migration` (after `book/src/spec/slices/gmres.md §L4 v0.6→v0.7` self-rotation lands or as a co-dispatch) **before** any harvester on `book/src/L4/check-stop-into-carry.md`. **Authority-partition compliance**: integrator-per-report touched only `scaffolding/open-questions.md` (any-agent-appendable; this dispatch made 1 in-place YAML status update + 1 in-place paragraph append + 1 new OQ section append) and this `STAGING.md` (per-cycle staging log; append-only); did NOT touch `scaffolding/friction-ledger.md` (meta-phase-only), did NOT touch `scaffolding/integrator-signals.md` (finalize-only), did NOT touch `scaffolding/skill-candidates.md` (CYCLE.md did not propose; observation forwarded to finalize for signals append). **Methodology-signal milestone**: this is **the cycle-010 closing report** — sixth and final per-report integrator of cycle-010. The MCP-codemap-pilot SUCCESS payload is the cleanest evidence yet that the cycle-009-meta enactment of MCP-codemap-server enablement (commit `ceb87da`) has resolved the friction-ledger entry that motivated it; finalize should capture this trajectory in `log/cycle-010.md` + integrator-signals + the cycle-record append for batch-2 meta-phase aggregation. Deferred `integrated_at:` to finalize per role-spec.

---

## 2026-05-27T220558Z-lifter-eigsolve-linear-solve-failed-anchor
applied_at: 2026-05-28T000200Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L1/eigsolve.md (edit — 5 edits applied):
  - Edit 1: §Signature callout block inserted after the closing fence of the signature-shape code block (anchored on the unique `EigStatus = ...` line + closing fence + "Shape contract" paragraph trio); adds the constructive-introduction callout block citing `palace/linalg/ksp.cpp:297-310` `BaseKspSolver::Mult` void return + the 10 eigensolver-side `opInv->Mult` call sites (4 ARPACK + 1 NLEPS + 5 SLEPc shell-matrix).
  - Edit 2: §Algebraic-laws §3 `LinearSolveFailed` row replaced — rough-in routing prose swapped for resolved-with-annotation prose citing the same ten negative-anchor sites + the §Signature callout cross-reference.
  - Edit 3: §"Laws that explicitly do not hold" sum-type completeness bullet softened from rough-in caveat to resolved-with-annotation note; introduces the **L1-coordinated** vs **L0-observable** distinction (critic flagged as one-off in the L1 corpus; deferred per repair).
  - Edit 4: §Status block updated with the cycle-010 resolution pointer; rough-in-status string preserved (`rough-in (test-coverage-bounded, cycle-009; LinearSolveFailed-constructive resolved cycle-010)`).
  - Edit 5: §Evidence — appended 5 new rows: cycle-009 OQ source, `palace/linalg/ksp.cpp:297-310` as primary negative anchor, the 4 ARPACK call sites, the 1 NLEPS call site, the 5 SLEPc shell-matrix call sites; also clarified the pre-existing cycle-008 OQ row's parenthetical to "the cycle-009 harvester dispatch target".
- scaffolding/open-questions.md (edit — 1 status flip + 1 prose append):
  - Edit 7 (yaml status flip): `eigsolve-linear-solve-failed-status-anchor` yaml block — `status: open` → `status: partially-answered` with `partial_answer_at: cycle-010` and `partial_answer_in: reports/2026-05-27T220558Z-lifter-eigsolve-linear-solve-failed-anchor/`. Per cycle-009 precedent at the predecessor OQ `eigsolve-l1-operator-rough-in-candidate` (`scaffolding/open-questions.md:1342-1351`) which uses exactly this pattern; permitted per any-agent-appendable convention + cycle-009 precedent confirmation in META.md §cross-reference-integrity finding 3 repair.
  - Edit 6 (prose append): cycle-010 resolution paragraph appended after the existing OQ prose body's "Source:" line; status `partially-answered` because the materialising `eigsolve-mutation-rotation` L1>L0 theme is still deferred to a cycle-010+ abstractor.

Gate hits:
- retroactive-budget per-slice: 1 (5 evidence-row appends + 4 prose edits to a single existing chapter `book/src/L1/eigsolve.md`; below the ≥3 per-slice block threshold because the edits are coordinated pure-evidence-backfill across one rough-in operator's caveat surface, not retroactive surface mutations on multiple disparate operators; critic explicitly flagged surface-or-evidence as PASS with "pure evidence backfill — surfaces the existing rough-in caveat into additional reader touchpoints" rationale)
- retroactive-budget global: 1 (this report — single report's evidence backfill; integrator-finalize will aggregate across cycle-010 batch)
- concept_writes on existing slug: 0 (no concept writes)
- forward-edge claim without surface: 0 (the future `eigsolve-mutation-rotation` L1>L0 theme is referenced as a deferred forward-pointer in the §Signature callout, Algebraic-laws §3, Sum-type-completeness bullet, Status block, and OQ resolution paragraph; all references explicitly mark it as "future"/"deferred to cycle-010+ abstractor"; per CLAUDE.md "Accumulate surface with embedded friction" and the cycle-006/007/008 precedent of forward-pointer references to queued themes, this is permitted)
- edge-label / prose mismatch: 0 (the constructive-introduction callout, the Algebraic-laws §3 prose, the Sum-type completeness bullet, the Status block, and the OQ resolution paragraph all consistently use "constructed by the L1 form" / "L1-constructive" phrasing; verified end-to-end)
- H1 reuses page heading: 0 (no new chapters; existing `# eigsolve` H1 unchanged)
- append on missing slug: 0 (existing chapter `book/src/L1/eigsolve.md` was the target; verified present on disk before each edit; existing OQ entry at `scaffolding/open-questions.md:1480-1487` was the target)
- variant-axis missing on multi-variant operator: 0 (no new variant axis introduced; the four existing variant axes — problem-type, spectrum-target, spectral-transformation, scaling — are unchanged; the `EigStatus` sum-type is a result-record sum-type, not a `EigSolver`-construction-time variant axis, per critic §variant-axis-coverage PASS rationale)
- SUMMARY.md auto-fix: 0 (no new chapters)
- index-placeholder displacement auto-fix: 0 (no index touched)
- bookkeeping incomplete: 0
- citation-validity per-anchor verification (load-bearing): 0 issues — independently verified `palace/linalg/ksp.cpp:297-310` body via Read (`BaseKspSolver<OperType>::Mult` at line 297, `void` return type, `ksp->Mult(x, y)` at line 300, `if (!ksp->GetConverged())` at line 301, `Mpi::Warning` dispatch at lines 303-306, `ksp_mult++; ksp_mult_it += ksp->GetNumIterations();` at lines 308-309); the 10 call-site enumerations were verified by the critic at META.md §citation-validity (4 ARPACK linear+quadratic, 1 NLEPS deflated_solve, 5 SLEPc shell-matrix callbacks) — all 10 sites match the dispatch's prose.
- L1 vocabulary discipline observed: 0 issues — the constructive-introduction annotation is L1-form annotation, not L0 ground-truth claim; the callout explicitly distinguishes "current L0 instantiations of `eigsolve` will not produce" `LinearSolveFailed` from the L1-coordinated semantic position; per post-cycle-009 invariant "Layers are defined high→low; lifting notes go in working notes", the L1 form is defined in L1 vocabulary with the L0-anchor-absent caveat surfaced via the L1>L0 lowering direction (the lowering will materialise the case).
- L1>L0 layer-direction convention: 0 issues — the future `eigsolve-mutation-rotation` theme is consistently named as L1>L0 lowering direction across all 5 chapter edits and the OQ resolution prose; the prose direction is "the L1 form's `LinearSolveFailed` lowers into how the future L1>L0 theme will materialise it via `ksp->GetConverged()` plumbing" (high→low) — matches CLAUDE.md "Layers are defined high→low" invariant.
- Edit 1 anchor-match verification: 0 issues — applied as a single Edit operation anchoring on the unique 3-line block (`EigStatus = ...` + closing fence + "Shape contract" line); the anchor was unique in the file as verified by the contiguous Edit succeeding; this avoids the cycle-010 critic's flagged Edit-1-non-contiguous-block issue from the original dispatch by spelling out the full contiguous anchor explicitly (matches META.md §Finding 1 repair guidance — the repaired CYCLE.md proposed the `[insert-after]` pattern; this integrator implementation used an equivalent contiguous-Edit anchor on disk).

Open questions promoted:
- (status update only — not new OQ) eigsolve-linear-solve-failed-status-anchor (cycle-009): `open` → `partially-answered` with `partial_answer_at: cycle-010` and a cycle-010 resolution paragraph describing option-(b) adoption (keep + annotate as L1-constructive with negative-anchor citations). Status partial because the materialising L1>L0 theme `eigsolve-mutation-rotation` is still deferred to a cycle-010+ abstractor.

Build-relevant: yes

Notes: Seventh per-report integrator of cycle-010 — applies the lifter dispatch resolving cycle-009 OQ `eigsolve-linear-solve-failed-status-anchor` via option (b). META.md shows `overall_status: pass-after-repair` (1 critic warning: cross-reference-integrity; repair coordinated 3 findings — Edit 1 non-contiguous [old] block rewritten to [insert-after] pattern + clarifying preamble; "five" → "ten" count fix across 8 loci in CYCLE.md; Edit 7 added by repairer flipping OQ yaml status to `partially-answered` per cycle-009 precedent). **All 7 edits applied cleanly**: 5 chapter edits to `book/src/L1/eigsolve.md` (constructive-introduction callout after signature block; Algebraic-laws §3 row resolution; "Laws that do not hold" sum-type completeness softening; §Status block update; §Evidence 5-row append) + 2 edits to `scaffolding/open-questions.md` (yaml status flip + prose append). The Edit 1 anchor matched disk exactly on first attempt — the repaired CYCLE.md's [insert-after] pattern was implemented as an equivalent contiguous Edit on a unique 3-line block (`EigStatus = ...` + closing fence + "Shape contract" line) which gave the same surface result. **Load-bearing citation independently verified**: read `palace/linalg/ksp.cpp:295-310` directly during integration — `BaseKspSolver<OperType>::Mult` body opens at line 297, `void` return type, executes `ksp->Mult(x, y)` at line 300, then `if (!ksp->GetConverged())` at line 301 dispatches an `Mpi::Warning` at lines 303-306 with format string "Linear solver did not converge, norm(Ax-b)/norm(b) = ...". Exactly as the report claims. The 10 call-site enumerations were verified by the critic in META.md §citation-validity (verified via mcp-codemap-server tools per the cycle-010 wave-2 #6 MCP-pilot evidence base). **Negative-anchor citation pattern**: this dispatch is the **first systematic use of negative-anchor citations at per-status-variant granularity** (citing L0 lines that demonstrate the *absence* of a behaviour — `BaseKspSolver::Mult` returning `void`, the ten eigensolver-side `opInv->Mult` call sites not querying `GetConverged()`). The pattern is distinguishable from the existing obstruction-theme negative-anchor pattern at `book/src/L1-L0/minres-iteration.md` / `book/src/L1-L0/bicgstab-iteration.md` (per-operator granularity, demonstrate stub-default / `MFEM_ABORT`) — this dispatch introduces a finer-grained variant (per-sum-type-variant within an operator's result-record). Per dispatch instruction: this is NOT a friction-ledger or skill-candidates edit from integrator authority (meta-phase territory); the observation is forwarded to integrator-finalize for `scaffolding/integrator-signals.md` append. **cycle-012 meta-phase watch-item — DEFERRED TO FINALIZE**: pattern signal: negative-anchor-citation-at-per-status-variant-granularity surfaced cycle-010-wave-2; recurrence-1; cycle-012 meta-phase batch-2 aggregation should watch for recurrence-2 on (a) the sibling cycle-009 eigsolve OQ `eigsolve-iteration-count-result-field` (where the case for adding `iterations` to `EigResult` will rest on similar negative-anchor evidence about Palace's iteration-counter exposure), or (b) any cycle-011 lifter / abstractor dispatch on a sum-typed status surface — if recurrence-2 fires, promote to friction-ledger or skill-candidates per meta-phase batch policy. **Three sibling cycle-009 OQs remain open**: `eigsolve-scaling-coordinate-convention`, `eigsolve-initial-space-axis-placement`, `eigsolve-iteration-count-result-field` — explicitly out of scope per "one theme per invocation"; cycle-011 cycle-planner candidate cluster (or roll into `eigsolve-mutation-rotation` abstractor dispatch when that fires). **Authority-partition compliance**: integrator-per-report touched only `book/src/L1/eigsolve.md` (per-report proposed-changes), `scaffolding/open-questions.md` (any-agent-appendable; yaml flip permitted per cycle-009 precedent + cycle-010 critic/repairer finding-3 explicit endorsement), and this `STAGING.md` (append-only); did NOT touch `scaffolding/friction-ledger.md` (meta-phase-only), did NOT touch `scaffolding/integrator-signals.md` (finalize-only), did NOT touch `scaffolding/priorities.md` (meta-phase + cycle-planner co-edit), did NOT touch `scaffolding/skill-candidates.md` (CYCLE.md did not propose; the negative-anchor-pattern observation is forwarded to finalize for integrator-signals append). **Methodology-signal milestone**: this is the **seventh per-report integrator of cycle-010** (wave-2 #7 of 8; cycle-010 closes after one more report — the eighth). The dispatch resolves a cycle-009 OQ within one cycle of opening — fast-feedback case where the cycle-N+1 dispatch resolves the cycle-N OQ via the option the original dispatch already named as preferred; pattern strengthens the cycle-007/008/009 trend of OQ-to-resolution latency at ≤2 cycles for option-named OQs. Forward to finalize for integrator-signals append: **OQ-to-resolution latency: cycle-009 OQ `eigsolve-linear-solve-failed-status-anchor` resolved within 1 cycle (cycle-010 lifter dispatch) via the harvester-recommended option (b)**. Deferred `integrated_at:` to finalize per role-spec.

---

## 2026-05-27T220000Z-same-layer-cross-cutter-phase-1-corpus-reduction-audit
applied_at: 2026-05-28T000400Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/spec/slices/gmres.md (rewrite — reduced from 1144 to 671 lines):
  - Replaced lines 1-495 (§"Slice: gmres" intro + §"L0 — cited regions" + §"L1 — pure-functional dataflow" + §"Open questions" L1 + §"L2 — primitive composition" + §"L3 — global tensor-field form" + §"L4" v0.1) with a stub header (`# Slice: gmres (reduced)`) pointing at the 8 firm entries that supersede the lifted material (L1/ksp_solve, L1-L0/ksp-solve-mutation-rotation, L2/krylov-step, L3/krylov-step [created by cycle-010 wave-1 row #1], L3-L2/krylov-step-body-identity, L4-L3/krylov-step-typed-wrapper-dissolution, L4/krylov-step, L1-L0/minres-iteration + L1-L0/bicgstab-iteration). Stub header includes "Unique material retained below" narrative + "Open questions still pending lift" subsection (L0.11a drift-warning compare; Givens-vocab L1-operator-promotion).
  - Retained lines 497-1144 verbatim (§"L4 v0.2" → §"L4 v0.3" → §"L4 v0.4" → §"L4 v0.5" → §"L4 v0.6" self-rotation history; ~647 lines; canonical methodology evidence for `concepts/derived-view-hoisting.md` and candidate `concepts/witness-typed-dispatch.md` pending second-instance promotion).
- book/src/spec/slices/cg.md (rewrite — reduced from 506 to 165 lines):
  - Replaced lines 1-366 (§"Context" + §"L0" + §"L1" + §"L2" + §"L3" + §"L4" v0.1 monolithic + §"Working Notes" + §"L4 v0.4 derived-view hoisting (self-rotation)" + §"L2→L3 rotation claims (retroactive, cycle 116)") with a stub header pointing at 8 firm entries (L1/ksp_solve, L1-L0/ksp-solve-mutation-rotation §Sub-pattern B, L2/krylov-step, L3/krylov-step, L3-L2/krylov-step-body-identity, L4-L3/krylov-step-typed-wrapper-dissolution, L4/krylov-step Form A, concepts/derived-view-hoisting). Stub header includes "Open questions still pending lift" subsection enumerating 4 Working-Notes-unique findings (initial-residual quirk Palace-bug-flag; CheckDot per-call-site enumeration; unpreconditioned-as-primary L4 modeling choice; unit-test coverage gap).
  - Retained lines 367-506 verbatim (§"L4 v0.5 first-iteration-unrolling (self-rotation)" + §"L4 v0.5 (cycle 137) claim ratification"; ~140 lines; canonical evidence for `concepts/first-iteration-unrolling.md`).
- book/src/spec/slices/arnoldi_step.md (edit — 2 in-place edits; total length 330 → 302 lines):
  - Edit 1: Inserted a "Reduction status (cycle-010+)" callout block after `# arnoldi_step` and before `## Context`, pointing at the firm L1-L0/ksp-solve-mutation-rotation §Sub-pattern C for the L0 supersession + enumerating retained sections + the 3 pending-lift targets (firm `L1/orthogonalize`, `concepts/sequential-obstruction` worked-example, `scaffolding/test-linkages/orthog.md`).
  - Edit 2: Replaced §"L0 — palace source" body (was lines 36-71: four-line kernel decomposition + four-rotation list + FGMRES variant + three-concurrent-writes paragraph + breakdown-not-guarded paragraph + §"Variant axes" + lazy-allocation paragraph) with a 4-line stub pointing at `L1-L0/ksp-solve-mutation-rotation` §Sub-pattern C + `L0/linalg-iterative-file` (the L0 four-line kernel decomposition is now firm there; the lazy-allocation pattern + FGMRES `Z[j]` teeing-off pattern documented at those cite targets).
  - Retained verbatim: §"Context" (line 3), §"Background" (line 11; Saad 2003 + textbook-deviation enumeration), §"Sources" (line 26; cited source-line linkages + test/unit linkage), §"L1 — invariants and procedure" (line 44; precondition / Arnoldi-relation / postcondition / breakdown invariants + variant-uniform procedure + residual-axis disclosure), §"Open questions" (line 86), §"L2 — primitive composition" (line 93; 4-primitive decomposition more granular than firm L2/krylov-step's 5-group decomposition), §"L3 — tensor-field lift" (line 150; variant-dependent obstruction taxonomy MGS-sequential / CGS-CGS2-lift + MPI-collective shape tabulation), §"L4 — calculus form" (line 219; Arnoldi-specific calculus instantiation cited from L4/krylov-step Evidence list).
- scaffolding/open-questions.md (append — 4 new OQ sections):
  - `l4-v01-v06-self-rotation-history-lift-target-decision` (cycle-010 OQ; routing decision needed: lift to concepts/derived-view-hoisting OR candidate concepts/witness-typed-dispatch OR retain as slice-level — current state post-reduction is option (c)).
  - `cg-initial-residual-quirk-palace-bug-flag-lift-path` (cycle-010 OQ; lift-path decision for the Palace `iterative.cpp:399-412` `(b·b)^{1/4}` bug-flag — annotate L1/ksp_solve Semantics OR add verified_against to L1-L0/ksp-solve-mutation-rotation Sub-pattern B OR keep as OQ pending upstream confirmation).
  - `l1-orthogonalize-promotion-from-arnoldi-step-and-orthog` (cycle-010 OQ; promotion criterion under unimplemented-Palace-stub policy — small AND simplifies higher forms; would unblock further reduction of arnoldi_step.md and orthog.md; variant-axis `gs_orthog` + MPI-collective shape table is unique evidence).
  - `phase-1-corpus-reduction-remaining-7-slices` (cycle-010 OQ; routing for cycle-011+ batches of the remaining 7 slices with suggested priority order — orthog → chebyshev → polynomial_recurrence_step → divfree → cg_preconditioning_framework → plane_rotation_stream → sparse_triangular_solve; batch-size suggestion 2-4 slices per dispatch).

Gate hits:
- retroactive-budget per-slice: 0 (the reductions are NOT retroactive surface mutations — they are corpus-reduction operations per the cycle-009 meta-batch CLAUDE.md methodology-invariant "Phase 1 corpus reduces as material is lifted"; the lifted material has been firm at the cited entries across cycles 004-010, so this dispatch is the inverse-direction operation of phase-1-corpus-reduction-audit priority #19; the slice files themselves are not the deliverable per CLAUDE.md, so reducing them does NOT trigger the retroactive-budget gate which guards against destabilising firm artifact)
- retroactive-budget global: 0 (this report)
- concept_writes on existing slug: 0 (no concept page writes; the OQ promotions are about CANDIDATE concept pages that do not yet exist — `concepts/witness-typed-dispatch.md` is a candidate per gmres.md v0.6 §"Open questions" but unwritten)
- forward-edge claim without surface: 0 (all referenced firm entries verified to exist on disk: `book/src/L1/ksp_solve.md`, `book/src/L1-L0/ksp-solve-mutation-rotation.md`, `book/src/L2/krylov-step.md`, `book/src/L3/krylov-step.md` (created by cycle-010 wave-1 row #1 first per-report integrator dispatch — sequencing confirmed by tail of STAGING.md and `book/src/L3/krylov-step.md` present at 40486 bytes), `book/src/L3-L2/krylov-step-body-identity.md`, `book/src/L4-L3/krylov-step-typed-wrapper-dissolution.md`, `book/src/L4/krylov-step.md`, `book/src/L1-L0/minres-iteration.md`, `book/src/L1-L0/bicgstab-iteration.md`, `book/src/concepts/derived-view-hoisting.md`, `book/src/concepts/first-iteration-unrolling.md`, `book/src/concepts/givens_generate.md`, `book/src/concepts/givens_apply.md`, `book/src/L0/linalg-iterative-file.md`)
- edge-label / prose mismatch: 0 (the stub headers consistently use "supersedes" / "lifted to firm entries" prose tracking the L_{n+1}>L_n edge labels of the firm entries cited; the retained sections (L4 v0.2-v0.6 in gmres; L4 v0.5 in cg; L1/L2/L3/L4 in arnoldi_step) are explicitly named as "unique material retained" rather than as new surface)
- H1 reuses page heading: 0 (the new H1 in gmres.md is `# Slice: gmres (reduced)` — distinguishable from the prior `# Slice: gmres`; same for cg.md `# Slice: cg (reduced)` vs prior `# CG — Preconditioned Conjugate Gradient`; arnoldi_step.md `# arnoldi_step` unchanged)
- append on missing slug: 0 (slices targeted are existing files; verified on disk pre-edit)
- variant-axis missing on multi-variant operator: 0 (no new operator content authored — corpus-reduction is inspection-and-stub-write)
- SUMMARY.md auto-fix: 0 (slice files were already SUMMARY-registered pre-reduction; no new chapters added)
- index-placeholder displacement auto-fix: 0 (no index touched)
- bookkeeping incomplete: 0
- L3/krylov-step.md prerequisite check (load-bearing): 0 issues — verified `book/src/L3/krylov-step.md` exists at 40486 bytes (created by cycle-010 wave-1 row #1 first per-report integrator dispatch); all three slice stubs reference this file by path and the cross-reference resolves.
- Line-range arithmetic re-verification (load-bearing per repair Issue 9 friction signal): 0 issues — pre-edit verification of all three slices via `grep -n "^## "` confirms the META-repaired line ranges exactly (gmres.md: `## L4 v0.2` at line 497, file ends at line 1144; cg.md: `## L4 v0.5` at line 368, file ends at line 506; arnoldi_step.md: `## L0 — palace source` at line 36, `## L1` at line 72). The replace operations were performed via Write (gmres.md, cg.md — entire-file rewrites since >70% of body changed) and Edit-on-unique-anchors (arnoldi_step.md — 2 targeted edits) rather than line-number-driven extraction; this sidesteps the line-range-arithmetic-brittleness friction signal noted in META.md repair §Issue 9 by anchoring on content directly.
- Phase-1-corpus-reduction methodology-invariant compliance: 0 issues — the reductions reduce corpus material that is already firm at the lifted entries (per CLAUDE.md "Phase 1 corpus reduces as material is lifted" invariant added cycle-009 meta-batch-1 closure); preserved sections are explicitly marked as "unique material retained" with named pending-lift targets in stub headers + OQs; the reduction is monotonic (slices shrink as artifact grows) per the invariant's "the corpus is allowed to shrink monotonically as the artifact grows" clause.

Open questions promoted:
- l4-v01-v06-self-rotation-history-lift-target-decision (NEW; cycle-010; routing OQ for concepts/derived-view-hoisting vs candidate concepts/witness-typed-dispatch vs slice-retention)
- cg-initial-residual-quirk-palace-bug-flag-lift-path (NEW; cycle-010; routing OQ for L1/ksp_solve annotation vs L1-L0/ksp-solve-mutation-rotation verified_against row vs upstream-confirmation deferral)
- l1-orthogonalize-promotion-from-arnoldi-step-and-orthog (NEW; cycle-010; routing OQ for firm L1/orthogonalize operator promotion under unimplemented-Palace-stub-policy criterion — small AND simplifies higher forms)
- phase-1-corpus-reduction-remaining-7-slices (NEW; cycle-010; routing OQ for cycle-011+ audit batches of the remaining 7 slices with suggested priority order + batch-size guidance)

Build-relevant: yes

Notes: **Eighth and final per-report integrator of cycle-010** — applies the first-instance phase-1-corpus-reduction-audit (priority #19 from cycle-009 meta-batch-1 closure). META.md shows `repaired_at: 2026-05-27T23:00:00Z` with `overall_status: ready` (3 warning-graded findings repaired: citation-validity range fixes; cross-reference-integrity cg.md narrative/range reconciliation by extending replace range from 1-339 to 1-366; edge-label-fidelity arnoldi_step.md boundary tightening from 36-69 to 36-71). **All three proposed_changes applied cleanly**: gmres.md rewritten from 1144 to 671 lines (preserving lines 497-1144 verbatim as the L4 v0.2-v0.6 self-rotation history; ~647 lines retained); cg.md rewritten from 506 to 165 lines (preserving lines 367-506 verbatim as the L4 v0.5 first-iteration-unrolling derivation + claim ratification; ~140 lines retained); arnoldi_step.md edited in-place (header callout inserted after H1; §"L0 — palace source" body replaced with stub — 4 line content; reductions from 330 to 302 lines total). **Sequencing verified**: per dispatch instruction's caveat 1, this dispatch is integrated LAST in cycle-010 because the slice stubs reference `book/src/L3/krylov-step.md` which was created by cycle-010 wave-1 row #1 first per-report integrator dispatch; verified the L3/krylov-step.md file exists at 40486 bytes before applying. **Methodology-invariant first-instance landing**: this is the **first** application of the CLAUDE.md §Methodology-invariant "Phase 1 corpus reduces as material is lifted" (added cycle-009 meta-batch-1 closure user-directive 2026-05-27 mid-cycle-009). The audit + reduction template established (Supersession map / Residual gaps / Recommended action / Proposed changes per slice) is machine-replayable per the dispatch's caveat 6; cycle-011+ planner should batch 2-4 remaining slices per audit dispatch. **Skill candidate forwarded (NOT promoted from this integrator's authority)**: the critic flagged a `phase-1-slice-reduction-audit` skill candidate at `scaffolding/skill-candidates.md:114-115`; this dispatch's CYCLE.md does NOT contain a `scaffolding/skill-candidates.md` proposed-changes block. Per dispatch instruction and the cycle-010 wave-2 #6 precedent (integrator-per-report authority on skill candidates: "ONLY if CYCLE.md proposed_changes directly specifies it"), the candidate is NOT promoted from this integrator's authority — meta-phase batch-2 (firing after cycle-012) will judge. Forward to finalize for integrator-signals append: **phase-1-slice-reduction-audit skill candidate surfaced cycle-010-wave-2-#8; pre-existing entry at scaffolding/skill-candidates.md:114-115 (filed by critic per META.md §skill-uptake-survey); cycle-012 meta-phase batch-2 aggregation should consider for promotion (the cycle-010 first-instance template is detailed and machine-replayable; the audit per slice is moderately expensive per dispatch caveat 6 — promotion would help cycle-011+ replay).** **Friction signal — line-range arithmetic brittleness (recurrence-1)**: META.md §Issue 9 noted that the first-instance audit's line-range arithmetic drifted 1-2 lines from actual `## H2` boundaries across all three slices; the repair systematically corrected the drift via `grep -n "^## "` enumeration. This is a friction-pattern candidate at recurrence-1 (the prior cycle-006 audit of the krylov-step L3-row contingency had related-but-distinct line-range issues; line-range-arithmetic-brittleness has not previously been ledger-tracked as its own pattern). Forward to finalize for integrator-signals append: **friction signal: phase-1-corpus-audit-line-range-arithmetic-brittleness at recurrence-1 (cycle-010); cycle-012 meta-phase batch-2 should watch for recurrence-2 in cycle-011 slice-reduction audits — if recurrence-2 fires, promote to friction-ledger with addressed-by skill-uptake (the proposed `phase-1-slice-reduction-audit` skill would prescribe the `grep -n "^## "` enumeration step that the repair retrofitted)**. **OQ promotion completeness**: 4 new OQs surfaced (out of 5 numbered OQs + 2 caveats in the CYCLE.md §"Open questions / caveats" section); OQ 1 (the L3/krylov-step backfill sequencing dependency) is resolved by the cycle-010 wave-1 row #1 landing — no promotion needed; OQ 6 (audit-template-machine-replayability) is addressed by the skill-candidate forwarded above; OQ 7 (no slice file mutated by dispatch) is a process-discipline caveat satisfied by this integrator's authority partition. **Authority-partition compliance**: integrator-per-report touched only `book/src/spec/slices/gmres.md` + `book/src/spec/slices/cg.md` + `book/src/spec/slices/arnoldi_step.md` (per-report proposed-changes authority), `scaffolding/open-questions.md` (any-agent-appendable; 4 new OQ sections appended above the `## Dropped` boundary), and this `STAGING.md` (append-only); did NOT touch `scaffolding/friction-ledger.md` (meta-phase-only), did NOT touch `scaffolding/integrator-signals.md` (finalize-only), did NOT touch `scaffolding/priorities.md` (meta-phase + cycle-planner co-edit), did NOT touch `scaffolding/skill-candidates.md` (CYCLE.md did not propose; the line-range-brittleness signal is forwarded to finalize for integrator-signals append). **Methodology-signal milestone**: this dispatch is **the cycle-010 closing report** — eighth and final per-report integrator of cycle-010 (wave-1: 5 reports; wave-2: 3 reports; total: 8 reports integrated across this cycle). The phase-1-corpus-reduction-audit first-instance success is the **first concrete realization** of the cycle-009 meta-batch-1 closure user-directive — the krylov-step chain is fully firm (L4 + L4>L3 + L3 + L3>L2 + L2 + L1 ksp_solve + L1>L0 ksp-solve-mutation-rotation), and the slice corpus is starting to shrink as material is lifted. Three slices reduced by a total of 842 lines (1144→671 + 506→165 + 330→302 = 1980→1138; net reduction of 842 lines, ~42% shrinkage of the audited slice subset). The methodology-invariant is now operationally validated. Forward to finalize for log/cycle-010.md + integrator-signals + cycle-record append: **phase-1-corpus-reduction-audit first-instance landed cycle-010-wave-2-#8 closing this cycle; 3 slices reduced (gmres -42%, cg -67%, arnoldi_step -8%); 4 routing OQs surfaced; first concrete realization of cycle-009-meta-batch-1 closure user-directive "Phase 1 corpus reduces as material is lifted"**. Deferred `integrated_at:` to finalize per role-spec.

---
