---
agent: same-layer-cross-cutter
invoked_at: 2026-05-28T034141Z
scope: Phase 1 corpus reduction audit — batch 3 (plane_rotation_stream [joint with orthog plane-rotation sub-slice] / divfree)
status: integrated
integrated_at: 2026-05-28T072500Z
integration_commit: 5964cb4
integration_notes: "Applied cycle-012 (report 8 of 8; closes meta-batch-2). Phase-1 corpus reduction batch-3: orthog.md plane-rotation sub-slice FULLY reduced (376->234; both near-duplicate L1 entries eliminated -- first intra-corpus-redundancy verdict) + plane_rotation_stream.md partial + invariant-hoist-first (418->367) + divfree.md partial (414->413). Cumulative 8/10 slices. Closes cycle-011 OQ orthog-plane-rotation-stream-sub-slice-batch-3-joint-audit; 5 new OQs (incl. HEADLINE l1-divfree-projector-promotion + plane-rotation-concept-page-canonical-pointer-repoint). HIGH-severity line-map defect (END-not-START boundary) repaired pre-apply; STRONG phase-1-slice-reduction-audit skill-promotion candidate at recurrence-3 for meta-phase. 0 gate hits at apply. Build exit 0 (slices not in book nav)."
inputs:
  - book/src/spec/slices/plane_rotation_stream.md (418 lines)
  - book/src/spec/slices/orthog.md (376 lines; plane-rotation sub-slice at lines 225-376 — the full `## Context`-through-EOF block; CORRECTED in repair from the original mis-stated 311-376 — JOINT audit per cycle-011 OQ orthog-plane-rotation-stream-sub-slice-batch-3-joint-audit)
  - book/src/spec/slices/divfree.md (414 lines)
  - book/src/concepts/plane-rotation-stream.md (39 lines; firm)
  - book/src/concepts/sequential-obstruction.md (113 lines; firm — §"Worked example: Givens-stream replay-prefix (plane_rotation_stream)" lines 83-112 lifted directly from plane_rotation_stream.md §L3)
  - book/src/concepts/givens.md (59 lines; firm)
  - book/src/concepts/givens_generate.md (28 lines; firm)
  - book/src/concepts/givens_apply.md (27 lines; firm)
  - book/src/concepts/trsv.md (20 lines; firm)
  - book/src/concepts/apply_linop.md (119 lines; firm — §"L2 use in divfree" lines 41-54 + §"L3 tensor-field form")
  - book/src/concepts/ksp_solve.md (52 lines; firm — cites divfree §L2 step 3)
  - book/src/concepts/set_subvector_zero.md (60 lines; firm — cites divfree §L2 step 2 + L3 lift)
  - book/src/L1/ksp_solve.md (firm; cycle-007 — cites divfree.cpp:175 + divfree §L2 step 3 as canonical use-pattern precedent)
  - book/src/L1/apply_linop.md (firm — cross-cutting prose covers divfree use)
  - book/src/L1/eigsolve.md (firm; cycle-011 — references DivFreeSolver as the optional projector field)
  - book/src/L3/apply_linop.md, book/src/L3/axpy.md (firm — divfree L3 primitives)
  - book/src/concepts/constructed-operators.md, state-stratification.md, derived-view-hoisting.md, solve-monad.md, variant-absorption.md (firm)
  - CLAUDE.md §Methodology invariants ("Phase 1 corpus reduces as material is lifted")
  - scaffolding/priorities.md #19 (phase-1-corpus-reduction-audit)
  - reports/2026-05-27T220000Z-same-layer-cross-cutter-phase-1-corpus-reduction-audit/CYCLE.md (batch-1 template)
  - reports/2026-05-27T234651Z-same-layer-cross-cutter-phase-1-corpus-reduction-batch-2/CYCLE.md (batch-2 template + the OQ that scheduled this joint audit)
---

# CYCLE: Phase 1 corpus reduction audit — batch 3 (plane_rotation_stream [joint] / divfree)

## Summary

Third-instance execution of priority #19 (`phase-1-corpus-reduction-audit`) under the methodology invariant "Phase 1 corpus reduces as material is lifted". Audits two slices: `plane_rotation_stream.md` (418 lines) — performed **jointly** with the retained plane-rotation sub-slice of `orthog.md` (lines 225-376, the full `## Context`-through-EOF block; CORRECTED in repair from the original mis-stated 311-376) per the cycle-011 batch-2 OQ `orthog-plane-rotation-stream-sub-slice-batch-3-joint-audit` — and `divfree.md` (414 lines, independent).

Per-slice headline findings:

- **plane_rotation_stream.md + orthog.md plane-rotation sub-slice (joint)**: This is the audit's first **within-corpus redundancy** finding. `plane_rotation_stream.md` is a **complete, more-detailed L0/L1/L2/L3 dissection** of the Givens-stream incremental-QR pattern (418 lines); the `orthog.md` plane-rotation sub-slice (lines 225-376, 151 lines; CORRECTED in repair — was mis-stated as 311-376 / ~66 lines) is a **near-redundant, less-detailed** dissection of the **same pattern**, comprising a Context / Background / Variant-axes / L0-citations preamble plus **two near-duplicate `## L1 — per-element procedure` entries** (the second a strict superset of the first; both strict subsets of `plane_rotation_stream.md` §L1) and interleaved Open-questions. The two slices dissect the identical algorithm (`GeneratePlaneRotation` / `ApplyPlaneRotation` replay-generate-apply-propagate stream); the `orthog.md` sub-slice adds nothing the `plane_rotation_stream.md` slice lacks except the explicit `back_solve_R` trsv step (firm-side via `concepts/trsv.md` + `L2/krylov-step`), the formal least-squares-residual invariant at orthog.md:350 (the one genuinely unique fragment, hoisted by proposed change 2(A)), and one cross-reference to `concepts/givens.md`. Both slices' Open questions already record the planned structural split (`orthog/gram_schmidt.md` + `orthog/plane_rotation.md`); `plane_rotation_stream.md` IS, in effect, the `orthog/plane_rotation.md` that split was meant to produce. **Verdict: the `orthog.md` plane-rotation sub-slice should be fully reduced to a stub-pointer at `plane_rotation_stream.md`** (the canonical home), AND `plane_rotation_stream.md` itself should be **partially reduced** (L0/L1/L2 stubbed against the firm `concepts/plane-rotation-stream.md` + `concepts/givens*.md` family; L3 retained because it is the unique source for the firm `concepts/sequential-obstruction.md` §"Worked example" and is more detailed than the concept page's excerpt). Residual gap blocking *full* reduction of `plane_rotation_stream.md`: the L3 replay-prefix obstruction derivation (the `O(j²)`-vs-`O(j)` materialization cost argument, the Householder-WY sibling-slice boundary) is unique methodology evidence, only partially excerpted into the firm concept page.
- **divfree.md**: blocked from full reduction by the absence of a firm `L1/divfree-projector` (or `L2/divfree-projector`) operator entry. The slice IS **load-bearing evidence** for multiple firm entries (`L1/ksp_solve.md:131,143` cite `divfree.cpp:175` + divfree §L2 step 3 as the canonical `ksp_solve` use-pattern precedent; `concepts/apply_linop.md:41-54` has a §"L2 use in divfree"; `concepts/ksp_solve.md:34` + `concepts/set_subvector_zero.md:27` cite divfree §L2 steps; `L1/eigsolve.md` references `DivFreeSolver` as the optional `projector` field). Its L2 primitive vocabulary (`apply_linop`, `set_subvector_zero`, `ksp_solve`, `axpy`, `copy`) is **fully** covered by firm concept pages and firm L1/L3 operator entries — but the **composition** (the four-step projector apply `P(y) = y + Grad·K⁻¹(Z_bdr(WeakDiv·y))` and its construction-time operator assembly) is NOT firm anywhere. divfree overlaps `ksp_solve` and `apply_linop` (its primitives) but does **not** overlap krylov-step (it is a *consumer* of a CG solve, not a Krylov method itself) and does **not** overlap an AMS preconditioner entry (no AMS entry exists; the BoomerAMG preconditioner is absorbed inside the constructed `ksp`). Recommended action: **partial-reduction** — stub the §L2 "Optimization opacity" + §L4 "tightening notes" sections that merely restate firm concept-page content; retain the L1/L2/L3/L4 projector composition + the three load-bearing claims (WeakDiv sign convention, step ordering, approximate-solve) + the divergence-free defining condition pending lift of a firm `L1/divfree-projector` operator row.

Five OQ actions are surfaced (4 new + 1 amendment to the cycle-011 joint-audit OQ marking it resolved-by-this-dispatch). Two headline items:
1. **The firm concept pages cite the WRONG slice as the canonical plane-rotation dissection** — `concepts/plane-rotation-stream.md:37`, `concepts/givens.md:40`, `concepts/givens_generate.md:27`, `concepts/givens_apply.md:27` all cite the `orthog` slice as the "primary dissection" / "use site", but the *more detailed and complete* dissection lives in `plane_rotation_stream.md`. After the joint reduction (orthog sub-slice → stub-pointer), these four firm-concept-page cross-references will point at a stub. This is a stale-pointer cleanup that must accompany the reduction (a `layer-intro-author` dep-map / concept-page touch). Surfaced as a residual gap, not enacted here.
2. **A firm `L1/divfree-projector` operator entry is pending lift** — divfree is a clean small constructed-operator (Helmholtz-decomposition projector) whose promotion would let `L1/eigsolve` reference `DivFreeSolver` as a firm operator type rather than a slice-corpus concept, and would let the firm `concepts/ksp_solve.md` / `concepts/apply_linop.md` use-site citations point at a firm L1 entry rather than the slice.

The audit template established in batch-1 (cycle-010) and refined in batch-2 (cycle-011) is applied directly. The cycle-010 friction signal `phase-1-corpus-audit-line-range-arithmetic-brittleness` (recurrence-2) is mitigated by enumerating H1/H2/H3 boundaries via `grep -n "^#\{1,6\} "` before any line-range proposal; this dispatch ALSO surfaced a *new* manifestation of the brittleness — the batch-2 header note at `orthog.md:9` references "lines 313+" for the plane-rotation sub-slice, but post-batch-2 reduction shifted the file to 376 lines. **REPAIR NOTE:** the original dispatch compounded the brittleness it was flagging — it mis-identified the sub-slice as starting at line 311/317 (the §"Open questions" / SECOND L1 entry) when the sub-slice actually starts at line 225 (the `## Context` heading that introduces the `# Orthogonalization (plane-rotation stream)` H1). The grep enumeration evidently used a mis-scoped window; the correct full sub-slice is 225-376. Stale/mis-read embedded line references in prior reductions are exactly the risk this friction signal names; see §"Open questions / caveats" item 5.

## Observation kind

**Redundancy (joint, plane_rotation_stream ↔ orthog plane-rotation sub-slice)** — two slices dissect the identical Givens-stream algorithm; one is strictly more complete. Plus **Variant-axis coverage gap (audit-scoped)** for divfree (the slice overlaps firm primitive entries but the projector composition is not firm). The redundancy verdict is the first of its kind in the corpus-reduction audit series; batches 1-2 found precursor-overlap and negative-result-slice patterns, not intra-corpus redundancy between two distinct slice files.

## Specific finding

### Slice 1 (joint): `book/src/spec/slices/plane_rotation_stream.md` (418 lines) + `book/src/spec/slices/orthog.md` plane-rotation sub-slice (lines 225-376; CORRECTED in repair — was mis-stated as 311-376)

**Audit verdict**:
- `orthog.md` plane-rotation sub-slice (lines 225-376, the full sub-slice from the `## Context` heading through end-of-file): **full reduction** to a stub-pointer at `plane_rotation_stream.md` (it is the redundant, less-complete twin; both near-duplicate L1 entries plus the Context/Background/Variant-axes/L0-citations preamble reduce to the stub).
- `plane_rotation_stream.md` (418 lines): **partial-reduction** (L0/L1/L2 stubbed against firm concept pages; L3 retained as the unique source for the firm `concepts/sequential-obstruction.md` worked example).

**Structure of `plane_rotation_stream.md`** (per `grep -n "^#\{1,6\} "`):
- Line 1: `# plane_rotation_stream` (H1)
- Lines 3-17: `## Context`
- Lines 19-30: `## Background`
- Lines 32-82: `## L0` (### Primitives 34 / ### Call sites 57 / ### Storage 69 / ### Negative result 76)
- Lines 84-171: `## L1` (### State schema 86 / ### Primitives (named at L1) 104 / ### Procedure 119 / ### Cross-target reuse 141 / ### Variant axes 150 / ### Open questions 163)
- Lines 173-291: `## L2 — primitive composition` (### State schema 175 / ### Primitives 193 / ### Procedure 209 / ### Stream operations as primitive sequences 240 / ### Cross-target rotation reuse 262 / ### Variant axes 271 / ### Negative result 283)
- Lines 293-418: `## L3 — tensor-field lift` (### State schema 295 / ### Obstruction: replay-prefix is sequential 313 / ### Obstruction: cross-target reuse is per-step 353 / ### Local triviality at extend 362 / ### Negative result (recorded) 370 / ### What DOES lift 392 / ### Variant axes 403 / ### Push-back consideration 411)

**Structure of `orthog.md` plane-rotation sub-slice** (per `grep -n "^## \|^# "` — CORRECTED in repair: the sub-slice begins at the `## Context` heading, NOT at line 311. The full sub-slice spans the `## Context` block through end-of-file, i.e. lines 225-376, 151 lines):
- Line 225: `## Context` (the plane-rotation-stream Context block — GMRES upper-Hessenberg + Givens-stream framing)
- Line 227: `# Orthogonalization (plane-rotation stream)` (H1 inside the sub-slice — a page-heading reuse)
- Line 235: `## Background` (textbook least-squares-on-Hessenberg via Givens; the Bindel/Demmel/Kahan/Marques 2002 scaled-Givens citation — FIRM-SIDE at `concepts/givens_generate.md:19`)
- Line 248: `## Variant axes` (scalar-type / generation-formula / storage-layout — FIRM-SIDE at `concepts/plane-rotation-stream.md` §"Variants the stream is invariant to" + `plane_rotation_stream.md` §L1 §"Variant axes")
- Line 258: `## L0 — citations` (cites `gmres.cpp:GeneratePlaneRotation` / `ApplyPlaneRotation` / `GmresSolver::Mult` / `FgmresSolver::Mult` — the `gmres.cpp` path the report flags as likely STALE vs the firm `iterative.cpp` citations in `plane_rotation_stream.md` §L0)
- Line 276: `## L1 — per-element procedure` (the **FIRST** L1 entry: ### State 278 / ### Procedure: plane_rotation_step 289 / ### Procedure: back_solve 307 — a strict subset of the SECOND L1 entry below, which is itself a strict subset of `plane_rotation_stream.md` §L1)
- Line 311: `## Open questions` (3 OQs about the plane-rotation stream — L2 entry, L3 viability, cross-slice consolidation)
- Line 317: `## L1 — per-element procedure (plane-rotation stream)` (the **SECOND** L1 entry: ### State 321 / ### Primitive: plane_rotation_step 332 / ### Invariant 348 / ### Primitive: back_solve_R 352 / ### Variant axes 356 / ### Open questions 360 / ### Citations 366 / ### Test linkage 374)

**Intra-file duplication note (repair, incorporating dropped batch-2 finding):** the sub-slice contains TWO near-duplicate L1 entries — the FIRST at 276-310 (`plane_rotation_step` + `back_solve`) and the SECOND at 317-376 (the same 5-step procedure + an explicit `Invariant`, `back_solve_R`, Citations, and Test-linkage). The batch-2 report and the scheduling OQ flagged these as "two near-duplicate L1 entries that should be merged." The full reduction below subsumes the merge: BOTH L1 entries (and the Context / Background / Variant-axes / L0-citations preamble) reduce to the single stub-pointer at `plane_rotation_stream.md`. The merge is realized as elimination, not as an in-file merge of the two entries.

**Redundancy map** (orthog plane-rotation sub-slice section → plane_rotation_stream.md equivalent) — CORRECTED in repair to cover the full sub-slice (225-376), including the preamble and the FIRST L1 entry the original map omitted:
- `orthog.md` §"Context" (225-233) → **fully covered** by `plane_rotation_stream.md` §"Context" (3-17). Same GMRES/FGMRES upper-Hessenberg + Givens-stream entry-point framing. The plane_rotation_stream version is the more-complete caller-surface treatment.
- `orthog.md` `# Orthogonalization (plane-rotation stream)` H1 (227) → a page-heading reuse inside the sub-slice; drops on reduction (the stub supplies its own H2 heading).
- `orthog.md` §"Background" (235-247) → **fully covered**. The textbook least-squares-on-Hessenberg-via-Givens references (Saad 2003 §6.5.3, Golub & Van Loan 2013 §5.1.8–9 / §5.2.4) are firm at `plane_rotation_stream.md:25` + `concepts/givens_generate.md:19`; the Bindel/Demmel/Kahan/Marques 2002 **scaled-Givens** citation is firm verbatim at `concepts/givens_generate.md:19`. No unique fragment.
- `orthog.md` §"Variant axes" (248-256) → **fully covered** by `plane_rotation_stream.md` §L1 §"Variant axes" (150-161) AND firm `concepts/plane-rotation-stream.md` §"Variants the stream is invariant to". Same scalar-type / generation-formula / storage-layout absorption, all absorbed at L1.
- `orthog.md` §"L0 — citations" (258-274) → **superseded** by `plane_rotation_stream.md` §L0 (32-82), whose citations point at `iterative.cpp` (the firm/correct path). The orthog §L0 cites `gmres.cpp:GeneratePlaneRotation`/`ApplyPlaneRotation`/`GmresSolver::Mult`/`FgmresSolver::Mult` — the `gmres.cpp` path the report flags as likely STALE (residual gap 4 / OQ `plane-rotation-givens-l0-citation-range-reconcile`). The reduction drops the stale `gmres.cpp` citations in favor of the firm `iterative.cpp` ones; no unique fragment is lost (the stub points at the canonical L0).
- `orthog.md` §"L1 — per-element procedure" — the **FIRST** L1 entry (276-310): §State (278-287) + §"Procedure: plane_rotation_step" (289-305) + §"Procedure: back_solve" (307-309) → **fully covered**. This first entry is a strict subset of the SECOND L1 entry (it lacks the explicit Invariant, Citations, and Test-linkage subsections); the SECOND entry is itself a strict subset of `plane_rotation_stream.md` §L1. The `back_solve` step here maps onto firm `concepts/trsv.md` §"Role in higher-layer rotations" + `L2/krylov-step` `back_solve`. No unique fragment beyond what the second entry / `plane_rotation_stream.md` already carries. (This entry was OMITTED from the original report's redundancy map — repaired here per critic Issue 2.)
- `orthog.md` §"L1 — per-element procedure (plane-rotation stream)" §State (321-330) → **fully covered** by `plane_rotation_stream.md` §L1 §"State schema" (86-102). Same `(c, s)` stream + `H`/`g_bar` targets. The orthog version uses `g_bar`/`R` MINRES-flavored notation; the plane_rotation_stream version uses `s`/`Hj` GMRES notation. Identical content.
- `orthog.md` §"Primitive: plane_rotation_step" (332-346, the 5-step replay/generate/apply/propagate/read) → **fully covered** by `plane_rotation_stream.md` §L1 §"Procedure (per Arnoldi step j)" (119-139). The orthog version's 5 steps map exactly onto the plane_rotation_stream version's `step_plane_rotation_stream` 4-step procedure + residual read. The plane_rotation_stream version additionally names the two abstract stream operations (replay-prefix / extend) — strictly more.
- `orthog.md` §"Invariant" (348-350) → **partially covered**. The least-squares-residual invariant `‖R[0..k,0..k]·y − g_bar[0..k]‖² + |g_bar[k+1]|² = ‖H_m·y − βe₁‖²` is stated more formally in orthog.md than anywhere in plane_rotation_stream.md (which states the combined effect prose-only at §L1 §Procedure comment + the residual proxy `|s[j+1]|`). **This formal invariant is the ONE unique fragment** of the orthog sub-slice; it should be hoisted into `plane_rotation_stream.md` if reduced.
- `orthog.md` §"Primitive: back_solve_R" (352-354) → **partially covered**. plane_rotation_stream.md does NOT include the back-solve step (it scopes only the rotation stream; back-solve is left to the consuming gmres slice). orthog.md includes the `trsv`-against-R back-solve. **This is the second unique fragment** — but it is itself superseded by the firm `concepts/trsv.md` §"Role in higher-layer rotations" (which states "GMRES `back_solve` is one `trsv` against the upper-triangular block") and is duplicated in `book/src/spec/slices/gmres.md` §L2 (per batch-1's gmres reduction, the gmres L2 is now firm at `L2/krylov-step` `back_solve`). So the back-solve fragment is covered firm-side.
- `orthog.md` §"Variant axes (plane-rotation stream)" (356-358) → **fully covered** by `plane_rotation_stream.md` §L1 §"Variant axes" (150-161) AND by firm `concepts/plane-rotation-stream.md` §"Variants the stream is invariant to" (27-33).
- `orthog.md` §"Open questions" (both at 311-315 and 360-364) → **fully covered**. The "L2 entry", "L3 viability", "slice split", "cross-slice consolidation (MINRES/LSQR)" OQs are ALL realized in `plane_rotation_stream.md` (which HAS the L2 + L3 + the cross-target-reuse and MINRES/LSQR-invariance discussion) and in firm `concepts/plane-rotation-stream.md` (§"Variants the stream is invariant to" item: "candidate shared concept across GMRES, FGMRES, MINRES, and LSQR slices").
- `orthog.md` §"Citations" (366-372) → **partially covered**. The orthog version cites `gmres.cpp` (a Palace path that may not exist — the firm citations all point at `iterative.cpp`); `plane_rotation_stream.md` §L0 has the **correct and more precise** citations (`iterative.cpp:72-108` / `:110-222` / `:226-242` / `:634-643` GMRES site / `:813-822` FGMRES site). **The orthog citations may be STALE** (`gmres.cpp` vs `iterative.cpp`) — see residual gap 3.
- `orthog.md` §"Test linkage" (374-376) → **fully covered** by `plane_rotation_stream.md` §L1 §"Open questions" (165-167: "No targeted unit test ... coverage is integration-only via GMRES KSPType in test-romoperator.cpp"). Both record the same absence.

**Conclusion on the joint redundancy**: the `orthog.md` plane-rotation sub-slice is a **strictly-less-complete duplicate** of `plane_rotation_stream.md`. The only fragment unique to orthog is the formal least-squares-residual invariant (orthog.md:350); the back-solve fragment is covered firm-side via `concepts/trsv.md` + `L2/krylov-step`. The planned `orthog/plane_rotation.md` split (recorded as an OQ in BOTH slices) is **already realized** by the existence of `plane_rotation_stream.md`. Reducing the orthog sub-slice to a stub-pointer (after hoisting the one unique invariant fragment into plane_rotation_stream.md) is safe and eliminates a genuine duplication.

**Supersession map for `plane_rotation_stream.md`** (slice section → firm entries):
- §"Context" (3-17) → **partially** covered by [`concepts/plane-rotation-stream.md` (intro + §"Shape")]. The slice's GMRES/FGMRES caller-surface framing is firm.
- §"Background" (19-30) → **partially** covered by [`concepts/plane-rotation-stream.md` §"Background" (Saad 2003 §6.5.3, Paige & Saunders), `concepts/givens_generate.md` §"Background" (Bindel/Demmel/Kahan/Marques 2002 scaled-Givens)]. The slice's LAPACK `xLARTG`/`xROT` mapping + the real-36-line / complex-112-line branch-count is more granular than the concept pages but is L0-citation detail.
- §"L0" §Primitives (34-55) → **partially** covered by [`concepts/givens_generate.md` §"Signature" + §"Palace citation", `concepts/givens_apply.md` §"Signature" + §"Palace citation", `concepts/givens.md` §"Palace mapping"]. **Unique material**: the precise dual-specialization line ranges (`iterative.cpp:72-108` real / `:110-222` complex generate; `:226-232` real / `:234-242` complex apply) are more granular than the firm concept pages (which cite `gmres.cpp:GeneratePlaneRotation` symbolically OR `iterative.cpp:73-108`/`:227-241`). NOTE: the firm `concepts/givens.md:33-34` cites `iterative.cpp:73-108` and `:227-241`; the slice cites `:72-108` and `:226-242`. Minor off-by-one/range discrepancy worth a verify-citation-range pass (residual gap 4).
- §"L0" §"Call sites" (57-67) → **partially** covered by [`L1-L0/ksp-solve-mutation-rotation.md` §"Sub-pattern C — inner GMRES body" + §"Sub-pattern D — inner FGMRES body"]. **Unique material**: the explicit GMRES site `:634-643` + FGMRES site `:813-822` byte-identical-call-site finding. The firm L1>L0 theme cites the inner Arnoldi loop but the rotation-stream sub-window line ranges are slice-unique.
- §"L0" §"Storage" (69-74) + §"Negative result" (76-82) → **NOT covered** by firm entries as a negative result, but **partially** by [`concepts/givens.md:50-53` ("The (c, s) scalar buffers are append-only and indexed by step ... no fused stream primitive is invoked")]. The firm concept page lifts the no-fused-`xLASR` negative result. **Unique material**: the append-only-indexed-by-step storage detail is firm-side; the negative result is firm-side.
- §"L1" (84-171) → **partially** covered by [`concepts/plane-rotation-stream.md` §"Shape" (the 5-step replay/generate/apply/propagate/read maps 1:1)]. **Unique material**: the §"Cross-target reuse" (141-148) — the structural reason `(c,s)` is stored (each pair applied to two distinct targets `Hj` and `s` without re-derivation) — is stated in firm `concepts/givens.md:55-58` §"Cross-target reuse is the structural reason..." So even this is firm-side. The §L1 named abstract stream operations (replay-prefix / extend) are firm in `concepts/plane-rotation-stream.md` §"Shape" steps 1-4.
- §"L2 — primitive composition" (173-291) → **partially** covered by [`concepts/givens.md` §"L2 usage shape" (36-58), `concepts/plane-rotation-stream.md` §"Shape"]. **Unique material**: the §"Stream operations as primitive sequences" (240-261) detailed replay-prefix-vs-extend primitive unfolding + the read-after-write-on-shared-boundary-slot analysis (which is the input to the L3 obstruction). The firm `concepts/givens.md:50-53` states the explicit-loop / no-fused-primitive finding but does not include the boundary-slot RAW dependency analysis. This is **the bridge** to the L3 obstruction and is partially unique.
- §"L3 — tensor-field lift" (293-418) → **partially** covered by [`concepts/sequential-obstruction.md` §"Worked example: Givens-stream replay-prefix (plane_rotation_stream)" (lines 83-112)]. **This is the load-bearing retention reason**: the firm concept page's worked example is a **direct excerpt** of this slice's §L3 (it cites `plane_rotation_stream.md` at line 85 and reproduces the `for k in 0..j-1: givens_apply(...)` loop + the boundary-slot RAW + the "representational obstruction" framing). BUT the slice's §L3 is **more detailed** than the excerpt: the slice includes (a) the explicit `O(j²) storage / O(j²·m) flops`-vs-`O(j) storage / O(j) flops` quadratic-vs-linear blowup cost argument (343-351), (b) the §"Obstruction: cross-target reuse is per-step, not per-stream" (353-360) showing no `vmap` batch dimension exists, (c) the §"Local triviality at extend" (362-368) showing the extend triple lifts as identity, (d) the §"What DOES lift" (392-401) showing even the cross-target replay is structurally absent, and (e) the §"Push-back consideration" (411-418) Householder-WY sibling-slice boundary. The firm concept page's worked example captures (a) and the boundary-slot RAW but elides (b)-(e). **These are unique methodology evidence and block full reduction.**

**Residual gaps** (blockers for full reduction of `plane_rotation_stream.md`):
1. The L3 obstruction derivation's detail beyond the firm `concepts/sequential-obstruction.md` worked-example excerpt — specifically the quadratic-vs-linear cost argument, the cross-target-no-batch-dim analysis, the local-triviality-at-extend, and the Householder-WY sibling-slice boundary — is unique. **Lifting target**: either extend `concepts/sequential-obstruction.md` §"Worked example" with the cost argument + the Householder-WY boundary, OR retain the slice's §L3 as the canonical detailed source (the firm concept page already cites it). The current proposed action retains §L3.
2. The `orthog.md` plane-rotation sub-slice's formal least-squares-residual invariant (orthog.md:350) is the one fragment NOT present in `plane_rotation_stream.md`. **Lifting target**: hoist it into `plane_rotation_stream.md` §L1 §"Procedure" or §"Invariant" (new subsection) as part of the joint reduction, so reducing the orthog sub-slice loses nothing.
3. The firm concept pages cite the WRONG slice as the canonical plane-rotation dissection. `concepts/plane-rotation-stream.md:37` ("primary dissection of the stream as it appears in GMRES/FGMRES" → `orthog` slice), `concepts/givens.md:40` (cites `plane_rotation_stream slice` — this one is CORRECT), `concepts/givens_generate.md:27` (→ `orthog` slice), `concepts/givens_apply.md:27` (→ `orthog` slice). After the orthog plane-rotation sub-slice is stubbed, three of these point at a stub. **Lifting target**: a `layer-intro-author` dispatch to repoint `concepts/plane-rotation-stream.md`, `concepts/givens_generate.md`, `concepts/givens_apply.md` "Used in" / "primary dissection" cross-references from `orthog` slice to `plane_rotation_stream` slice (the actually-more-complete dissection). This is a dep-map / concept-page cleanup that MUST accompany the reduction.
4. The L0 citation line-range discrepancy (slice cites `iterative.cpp:72-108`/`:226-242`; firm `concepts/givens.md:33-34` cites `:73-108`/`:227-241`). **Lifting target**: a `verify-citation-range` pass to reconcile (likely the firm concept page is canonical; the slice may be off-by-one). Minor.

**Recommended action**:
- `orthog.md` plane-rotation sub-slice (lines 225-376, the full sub-slice from `## Context` through end-of-file; CORRECTED in repair — was mis-stated as 311-376): **full reduction** — replace the entire sub-slice (Context + Background + Variant-axes + L0-citations + BOTH near-duplicate L1 entries + the interleaved Open-questions) with a single stub-pointer at `plane_rotation_stream.md` after hoisting the formal least-squares-residual invariant (orthog.md:350) into `plane_rotation_stream.md`. This realizes the long-recorded `orthog/plane_rotation.md` split (the content's canonical home is `plane_rotation_stream.md`) AND subsumes the batch-2 "two near-duplicate L1 entries should be merged" finding (the merge is realized as elimination of both entries into the stub).
- `plane_rotation_stream.md`: **partial-reduction** — stub §L0 §Primitives + §L1 §Procedure/§Cross-target reuse + §L2 §Primitives/§Procedure against the firm concept-page family (`plane-rotation-stream`, `givens`, `givens_generate`, `givens_apply`); retain §L0 §"Call sites" (unique GMRES/FGMRES line-range windows), §L0 §"Storage"/§"Negative result" (carry the negative-result framing), §L2 §"Stream operations as primitive sequences" (the boundary-slot RAW bridge to L3), and §L3 in full (the canonical detailed obstruction source cited by the firm concept page).

### Slice 2: `book/src/spec/slices/divfree.md` (414 lines)

**Audit verdict**: partial-reduction (full-reduction blocked by the absence of a firm `L1/divfree-projector` operator entry — the slice's L1/L2/L3/L4 projector composition is the only firm divergence-free-projection definition in the artifact, and it is load-bearing evidence cited by `L1/ksp_solve`, `L1/eigsolve`, and three concept pages).

**Structure** (per `grep -n "^#\{1,6\} "`):
- Line 1: `# Slice: divfree` (H1, intro lines 1-8)
- Lines 10-86: `## L1` (### Defining condition 12 / ### State 25 / ### Apply 44 / ### Equivalent abstract form 61 / ### Complex specialization 72 / ### Mutation pattern 80)
- Lines 88-102: `## Variant axes (absorption status)`
- Lines 104-110: `## Call sites`
- Lines 112-126: `## Open questions`
- Lines 128-221: `## L2 — primitive composition` (### Construction 130 / ### Apply real 154 / ### Apply complex 180 / ### Optimization opacity 204)
- Lines 223-306: `## L3 — tensor-field form` (### Global apply real 227 / ### Complex 256 / ### Sequential obstruction lives at L2.5 268 / ### Variant absorption at L3 283 / ### Load-bearing claims 295)
- Lines 308-405: `## L4 — calculus form` (### State stratification 312 / ### Construction 339 / ### Apply 357 / ### Complex 373 / ### Composition into a driver 385 / ### Load-bearing claims 399)
- Lines 406-414: `## L4 tightening notes`

**Supersession map** (slice section → firm entries):
- §intro (1-8) → **NOT covered** by firm entries. The divergence-free-projection scope + the eigensolver-path-only-callers framing (with the driven/transient attribution flagged as an OQ) is unique. `L0/eigensolver-wrapper.md:44` references `opProj` / `DivFreeSolver<ComplexVector>*` as the projector but does not define the projection.
- §"L1" §"Defining condition" (12-23) → **NOT covered**. The `Gᵀ M (P x) = 0` defining condition + the `G`/`M` operator roles are the slice's foundational claim; not firm anywhere.
- §"L1" §"State" (25-42) → **partially** covered by [`concepts/constructed-operators.md` (the construct-once-reuse-per-call pattern), `concepts/ksp_solve.md` (the constructed `ksp` is the canonical constructed-operator absorption example)]. **Unique material**: the specific state members (`M`, `WeakDiv`, `Grad`, `bdr_tdof_list_M` synthetic-pin, `ksp` CG+BoomerAMG, `psi`/`rhs` scratch) and the WeakDiv sign-convention absorption.
- §"L1" §"Apply" (44-59) → **NOT covered**. The 4-step apply (`WeakDiv·y → zero-BC → M⁻¹ solve → +Grad·ψ`) is the slice's core procedure; the individual primitives are firm (see below) but the composition is not.
- §"L1" §"Equivalent abstract form" (61-70) → **NOT covered**. The Helmholtz-decomposition `y = y_divfree + Grad·ψ` framing + the never-materialized `Gᵀ M G` triple product is unique mathematical context.
- §"L1" §"Complex specialization" (72-78) + §"Mutation pattern" (80-86) → **partially** covered by [`concepts/apply_linop.md` §"Variant axes" (real/complex parametric), `concepts/ksp_solve.md` §"Mutation pattern"]. The componentwise-Re/Im + explicit-copy-no-aliasing is unique to the slice's procedure.
- §"Variant axes (absorption status)" (88-102) → **partially** covered by [`concepts/variant-absorption.md`, `concepts/constructed-operators.md`]. The three axes (VecType / H1-depth / empty-boundary) are absorbed via the patterns the concept pages describe; the specific absorption sites (`build_mg_or_amg`, `pin_one_dof_if_empty`) are slice-unique.
- §"Call sites" (104-110) → **partially** covered by [`L0/eigensolver-wrapper.md:44` (cites `DivFreeSolver<ComplexVector>*` in ARPACK `arpack.hpp:81` / SLEPc `slepc.hpp:110` / nonlinear `nleps.hpp:65`), `L1/eigsolve.md` (references the optional `projector` field)]. **Unique material**: the `eigensolver.cpp` `divfree->Mult(v0)` construction call gated on `divfree_max_it > 0`, and the `arpack.cpp`/`slepc.cpp` `opProj->Mult(y1)` inner-kernel call. These specific call sites are firm-side in `L0/eigensolver-wrapper.md` (the projector composition setter) but the per-call use pattern is slice-unique.
- §"Open questions" (112-126) → **partially** lifted. OQ "driven/transient attribution stale?" is unresolved (a documentation-accuracy question). OQ "no `test-divfree.cpp`" survives as a documented test-coverage absence. OQ "WeakDiv sign-convention unverified L0 reading" is a load-bearing verify-citation candidate — the entire correction direction depends on it.
- §"L2 — primitive composition" §"Construction" (130-152) → **NOT covered**. The `construct(...)` pseudocode assembling `M`/`WeakDiv`/`Grad`/`bdr_eff`/`pc`/`ksp` is the only firm divfree construction definition. **Unique material**: the `assemble_h1_mass_hierarchy` / `assemble_weak_divergence` / `assemble_discrete_gradient` / `build_mg_or_amg` / `pin_one_dof_if_empty` assembly steps.
- §"L2" §"Apply — real path" (154-178) → **partially** covered: every individual primitive is firm — `apply_linop` (`L1/apply_linop.md` + `concepts/apply_linop.md` §"L2 use in divfree" lines 41-54 explicitly names `rhs ← apply_linop(WeakDiv, y)` + `t ← apply_linop(Grad, psi)`), `set_subvector_zero` (`concepts/set_subvector_zero.md:27` explicitly names divfree §L2 step 2), `ksp_solve` (`concepts/ksp_solve.md:34` explicitly names `psi ← ksp_solve(ksp, rhs)` divfree §L2 step 3 + `L1/ksp_solve.md:131` cites `divfree.cpp:175`), `axpy` (`L1/axpy.md`). **Unique material**: the *composition* of these four into the projector apply is not firm.
- §"L2" §"Apply — complex specialization" (180-202) → **partially** covered (same primitives, componentwise unrolling). Unique: the Re/Im unrolling shape.
- §"L2" §"Optimization opacity" (204-221) → **partially** covered by [`concepts/apply_linop.md` §"L2 use in divfree" lines 51-54 ("the construction-time distinction between matrix-assembled and partial-assembly operators is transparent at L2"), `concepts/transparent-vs-load-bearing-tricks`-flavored framing in `L0/transparent-vs-load-bearing-tricks.md`]. The transparent list (PA vs assembled, GMG vs AMG, re-orthogonalization) is firm-side framing; the load-bearing list (WeakDiv sign, step ordering, ksp tolerance) is the slice's unique claim and recurs at L3 (295-306) and L4 (399-404).
- §"L3 — tensor-field form" (223-306) → **partially** covered by [`concepts/apply_linop.md` §"L3 tensor-field form" (56-77, the clean lift for assembled + matrix-free + composition), `concepts/set_subvector_zero.md` §"L3 lift"/§"L3 tensor-field form" (38-59, the `Z_S` mask-projector), `concepts/sequential-obstruction.md` (the ksp interior obstruction)]. **Unique material**: the closed-form global projector `P(y) = y + Grad·K⁻¹(Z_bdr_eff(WeakDiv·y))` (234) + the §"Sequential obstruction lives at L2.5, not here" (268-281) framing (the CG obstruction is interior to `ksp_solve`, does not leak into the L3 divfree apply — a clean-lift result). The L2.5-not-here framing is a clean methodological worked example for "obstruction interior to a constructed-operator primitive" — partially unique.
- §"L4 — calculus form" (308-405) → **partially** covered by [`concepts/state-stratification.md` (the params/sim/ephemeral strata), `concepts/solve-monad.md` (the `SolveM` action), `concepts/constructed-operators.md` (the construct-once pattern), `concepts/derived-view-hoisting.md` (the scratch-as-hoisted-view)]. **Unique material**: the `DivFreeParams`/`SimState<V>` typing + the `applyDivFree :: DivFreeParams -> SimState V -> SolveM (SimState V)` monadic procedure + the `eigStep` driver composition (385-397, showing the projector composes with the outer Krylov iteration via plain function application in the monad). This is canonical evidence for the constructed-projector-as-SolveM-action pattern.
- §"L4 tightening notes" (406-414) → **partially** covered by [`concepts/state-stratification.md`, `concepts/derived-view-hoisting.md`]. The cycle-167 tightening (scratch reclassified ephemeral-not-internal-parameter; polymorphism-at-V note) is methodology-history that merely restates the firm concept-page distinctions. **Stub-eligible** (the tightening's content is firm-side).

**Residual gaps** (blockers for full reduction):
1. **No firm `L1/divfree-projector` operator entry.** The slice's L1/L2/L3/L4 projector composition is the only firm divergence-free-projection definition. It is load-bearing evidence cited by `L1/ksp_solve.md:131,143`, `L1/eigsolve.md` (optional `projector` field), `concepts/apply_linop.md:41-54`, `concepts/ksp_solve.md:34`, `concepts/set_subvector_zero.md:27`. **Lifting target**: a harvester dispatch on `L1/divfree-projector` (constructed-operator: Helmholtz-decomposition projector). Promotion criterion (small AND simplifies higher forms): divfree is small (a 4-step apply over a once-constructed operator) AND lifting would let `L1/eigsolve` reference `DivFreeSolver` as a firm operator type and let the three concept-page use-site citations point at a firm L1 entry. Strong promotion candidate.
2. **The WeakDiv sign-convention claim is an unverified L0 reading** (slice OQ at 121-126). The entire correction direction (`+Grad·ψ` vs `−Grad·ψ`) depends on `MixedVectorWeakDivergenceIntegrator` encoding the negative-divergence sign. **Lifting target**: a `verify-citation-range` / harvester pass that anchors the sign to an L0 integrator citation before the L1/divfree-projector entry firms it as a load-bearing claim.
3. **The "sequential obstruction lives at L2.5, not here" framing** (§L3, 268-281) is a clean methodological worked example (obstruction interior to a constructed-operator primitive does not leak into the consuming operator's L3 lift) that is only implicitly covered by `concepts/sequential-obstruction.md`. **Lifting target**: optionally extend `concepts/sequential-obstruction.md` with an "interior-obstruction-does-not-leak" sub-note citing divfree §L3, OR retain in the slice as the canonical example.

**Recommended action**: **partial-reduction**.
- Stub sections that are fully superseded:
  - §"L4 tightening notes" (406-414) — the cycle-167 tightening merely restates `concepts/state-stratification.md` + `concepts/derived-view-hoisting.md` distinctions; replace with a 1-line pointer.
  - §"L2" §"Optimization opacity" (204-221) transparent list — covered by `concepts/apply_linop.md` §"L2 use in divfree" + `L0/transparent-vs-load-bearing-tricks.md`; retain ONLY the load-bearing list (WeakDiv sign, step ordering, ksp tolerance) which recurs as the load-bearing claims at L3/L4.
- Retain (pending firm `L1/divfree-projector`):
  - §"L1" (defining condition, state, apply, equivalent-abstract-form, complex, mutation) — the only firm divfree definition.
  - §"Variant axes", §"Call sites", §"Open questions" (the WeakDiv-sign + stale-attribution + test-absence OQs are unresolved).
  - §"L2" construction + apply (real + complex) — the only firm divfree construction/apply.
  - §"L3" (the closed-form global projector + the L2.5-not-here clean-lift framing).
  - §"L4" (the `DivFreeParams`/`applyDivFree`/`eigStep` calculus form — canonical constructed-projector-as-SolveM-action evidence).
- Add a header note recording the reduction status + the firm entries that cite the slice as evidence + the pending `L1/divfree-projector` lift.

## Recommendation

**Dispatch integrator-per-report applications** that materialize the reductions per the audit verdicts. The action profile for batch-3 is materially different from batches 1-2 (which were precursor-overlap + negative-result):

- `orthog.md` plane-rotation sub-slice: **full reduction** to a stub-pointer at `plane_rotation_stream.md` (the first intra-corpus-redundancy reduction; closes the long-recorded `orthog/plane_rotation.md` split OQ by recognizing `plane_rotation_stream.md` IS that split product). Hoist the one unique fragment (formal LS-residual invariant) first.
- `plane_rotation_stream.md`: **partial-reduction** — stub L0-primitives/L1-procedure/L2-primitives against the firm Givens concept-page family; retain L0-call-sites, L2-stream-operations-bridge, and L3 (the canonical detailed obstruction source the firm concept page cites).
- `divfree.md`: **partial-reduction** — stub §L4-tightening-notes + §L2-optimization-opacity transparent list; retain the L1/L2/L3/L4 projector composition + load-bearing claims pending firm `L1/divfree-projector` lift.

Follow-up dispatches for the residual lift work (new OQs in §"Open questions / caveats"):
1. **`layer-intro-author`** to repoint the firm Givens concept-page "Used in" / "primary dissection" cross-references from `orthog` slice to `plane_rotation_stream` slice (stale-pointer cleanup that MUST accompany the orthog plane-rotation sub-slice reduction). HEADLINE.
2. **harvester** on `L1/divfree-projector` (strong promotion candidate; would firm the divfree definition and let `L1/eigsolve` + three concept pages cite a firm L1 entry). HEADLINE.
3. **verify-citation-range** on (a) the `iterative.cpp:72-108`/`:226-242` (slice) vs `:73-108`/`:227-241` (firm `concepts/givens.md`) discrepancy, and (b) the divfree WeakDiv sign-convention L0 anchor.
4. Optionally extend `concepts/sequential-obstruction.md` §"Worked example" with the plane_rotation_stream cost argument + Householder-WY boundary, and/or with the divfree interior-obstruction-does-not-leak sub-note.

Concretely, the proposed integrator actions per slice are encoded as proposed_changes blocks below.

## Proposed changes

### Proposed change 1: Full reduction of the `orthog.md` plane-rotation sub-slice to a stub-pointer at `plane_rotation_stream.md`

> **REPAIRED (repairer, cycle-012):** the original edit said "Replace lines 311-376", which covered only the SECOND L1 entry + its preceding §"Open questions" and would have orphaned lines 225-310 (the `## Context`, the section H1, `## Background`, `## Variant axes`, `## L0 — citations`, and the FIRST `## L1 — per-element procedure` entry) beneath a stub claiming the content had moved. The edit scope is corrected to the **full sub-slice**, text-anchored from the `## Context` heading through end-of-file. The redundancy verdict is unchanged (the sub-slice is the less-complete twin of `plane_rotation_stream.md`); only the reduction scope is corrected. This also subsumes the batch-2 "two near-duplicate L1 entries should be merged" finding — both L1 entries (276-310 and 317-376) are eliminated into the single stub.

```edit:book/src/spec/slices/orthog.md
[Replace the ENTIRE plane-rotation sub-slice — the contiguous block from the `## Context` heading (currently line 225) through end-of-file (currently line 376) — with the stub-pointer below.

**TEXT-ANCHORED SCOPE (do NOT use the line numbers; locate the boundaries by heading text):**
- START anchor: the `## Context` heading at which the plane-rotation-stream block begins. This is the `## Context` that is IMMEDIATELY FOLLOWED by the `# Orthogonalization (plane-rotation stream)` H1. (There is exactly one such `## Context` followed by that H1; locate it with `grep -n "^# Orthogonalization (plane-rotation stream)"` then take the `## Context` immediately above it.) Do NOT confuse this with anything in the Gram-Schmidt sections above — the Gram-Schmidt block ends with its own `### Open questions` (currently line 220, an H3 inside the §L4 calculus-form section), which is RETAINED verbatim and is NOT part of the replacement.
- END anchor: end-of-file.
- The replacement therefore consumes: `## Context` + `# Orthogonalization (plane-rotation stream)` (H1) + `## Background` + `## Variant axes` + `## L0 — citations` + `## L1 — per-element procedure` (FIRST L1 entry) + `## Open questions` (the plane-rotation OQs) + `## L1 — per-element procedure (plane-rotation stream)` (SECOND L1 entry, incl. its Invariant / back_solve_R / Citations / Test-linkage). ALL of these collapse into the single stub below.

The Gram-Schmidt sections above the START anchor (the L0→L1 / L2 / L3 / L4 Gram-Schmidt content, ending at its `### Open questions`) are retained verbatim and are NOT touched.

BEFORE stubbing, hoist the one unique fragment — the formal least-squares-residual invariant currently at orthog.md:350 (inside the SECOND L1 entry's §"Invariant") — into `plane_rotation_stream.md` (proposed change 2 below adds it as a new §L1 §"Invariant" subsection there). Sequence proposed change 2(A) before this, or apply both atomically (see §"Open questions / caveats" item 10).

The stub text replacing the full sub-slice (`## Context` heading through EOF):

## Plane-rotation stream (reduced — see `plane_rotation_stream.md`)

> **Reduction status (cycle-012+):** the plane-rotation-stream sub-slice formerly dissected here — its Context, Background (scaled-Givens references), Variant axes, L0 citations, and **both** near-duplicate `## L1 — per-element procedure` entries — is a less-complete duplicate of the dedicated `book/src/spec/slices/plane_rotation_stream.md` slice, which carries the full L0/L1/L2/L3 dissection of the Givens-stream incremental-QR pattern. This realizes the long-recorded `orthog/plane_rotation.md` split (OQ in both slices): the canonical home for the plane-rotation stream is `plane_rotation_stream.md`. The intra-file duplication (the two near-duplicate L1 entries previously here, flagged in the cycle-011 batch-2 audit as "should be merged") is resolved by this elimination. The unique formal least-squares-residual invariant formerly stated here has been hoisted into `plane_rotation_stream.md` §L1 §"Invariant". The Background's textbook references (Saad 2003 §6.5.3, Golub & Van Loan 2013 §5.1.8–9, and the Bindel/Demmel/Kahan/Marques 2002 scaled-Givens citation) are firm at `concepts/givens_generate.md` and `plane_rotation_stream.md`; the former L0 citations here pointed at `gmres.cpp` (likely stale — the firm L0 citations in `plane_rotation_stream.md` point at `iterative.cpp`). This slice (`orthog.md`) now scopes ONLY the block Gram-Schmidt orthogonalization (the L0→L1/L2/L3/L4 Gram-Schmidt sections above); the plane-rotation stream lives entirely in `plane_rotation_stream.md`.

**Canonical plane-rotation entries:**
- `book/src/spec/slices/plane_rotation_stream.md` — full L0/L1/L2/L3 dissection (the more-complete twin of this stubbed sub-slice).
- `book/src/concepts/plane-rotation-stream.md` — the firm layer-pattern concept (Shape / Background / Sequential-character / Variants-invariant-to).
- `book/src/concepts/givens.md`, `givens_generate.md`, `givens_apply.md` — the firm 2×2-rotation primitive family.
- `book/src/concepts/trsv.md` — the firm back-solve primitive (`back_solve_R` = one `trsv` against the rotated upper-triangular R; also firm at `L2/krylov-step` `back_solve`).
- `book/src/concepts/sequential-obstruction.md` §"Worked example: Givens-stream replay-prefix (plane_rotation_stream)" — the firm L3 obstruction worked example (cites `plane_rotation_stream.md` §L3).
]
```

### Proposed change 2: Partial reduction of `book/src/spec/slices/plane_rotation_stream.md` + hoist the orthog invariant fragment

```edit:book/src/spec/slices/plane_rotation_stream.md
[Two edits to this file:

(A) HOIST the orthog plane-rotation sub-slice's unique formal least-squares-residual invariant into a new §L1 §"Invariant" subsection. Insert AFTER the §L1 §"Procedure (per Arnoldi step j)" section (which ends at line 139, before §"Cross-target reuse" at line 141):

### Invariant

After `step_plane_rotation_stream(state, j)` returns: the product `G(c_j, s_j) ⋯ G(c_0, s_0)` applied to the original `H̄[0..j+1, 0..j]` equals `R[0..j+1, 0..j]` (upper-triangular through column `j`, with `R[j+1, 0..j] = 0`), and the same product applied to the original `s_0 = (β, 0, …, 0)ᵀ` equals the current `s[0..j+1]`. Equivalently, for any `y ∈ ℂ^{j+1}`: `‖R[0..j, 0..j]·y − s[0..j]‖² + |s[j+1]|² = ‖H̄[0..j+1, 0..j]·y − βe₁‖²`, so minimizing the first term yields the GMRES iterate and the second term gives the residual norm. (Hoisted from the now-reduced `orthog.md` plane-rotation sub-slice, cycle-012.)

(B) PARTIAL-REDUCE the superseded L0/L1/L2 detail. Add a reduction-status header note at line 2 (after the `# plane_rotation_stream` H1) and stub the firm-covered sub-sections. Retain §L0 §"Call sites" + §"Storage" + §"Negative result", §L2 §"Stream operations as primitive sequences", and §L3 in full.

Header note inserted at line 2:

> **Reduction status (cycle-012+):** the Givens-rotation primitives and the per-step procedure of this slice are firm in the concept-page family `book/src/concepts/plane-rotation-stream.md` (Shape) + `book/src/concepts/givens.md` / `givens_generate.md` / `givens_apply.md` (the 2×2 primitives) + `book/src/concepts/trsv.md` (back-solve). The §L0 §Primitives, §L1 §Procedure/§Cross-target-reuse, and §L2 §Primitives/§Procedure are superseded by those firm pages. RETAINED here as unique material: §L0 §"Call sites" (the GMRES `iterative.cpp:634-643` / FGMRES `:813-822` byte-identical-site finding), §L0 §"Negative result" (no fused `xLASR`), §L2 §"Stream operations as primitive sequences" (the boundary-slot read-after-write analysis that bridges to L3), and the **entire §L3** — which is the canonical detailed source for `book/src/concepts/sequential-obstruction.md` §"Worked example: Givens-stream replay-prefix" (that firm worked example cites this slice's §L3 and excerpts the loop + the boundary-slot RAW + the representational-obstruction framing, but elides this slice's quadratic-vs-linear cost argument, the cross-target-no-batch-dim analysis, the local-triviality-at-extend, and the Householder-WY sibling-slice boundary).
>
> **This slice is now the canonical plane-rotation-stream dissection** (the `orthog.md` plane-rotation sub-slice was reduced to a stub pointing here, cycle-012 — realizing the long-recorded `orthog/plane_rotation.md` split). The firm Givens concept pages' "Used in" / "primary dissection" cross-references should be repointed from the `orthog` slice to this slice (pending `layer-intro-author` dispatch; OQ `plane-rotation-concept-page-canonical-pointer-repoint`).
>
> **Pending lift / verify:** the L0 citation line ranges here (`iterative.cpp:72-108` generate-real / `:226-242` apply) differ by one from the firm `concepts/givens.md:33-34` ranges (`:73-108` / `:227-241`); a `verify-citation-range` pass should reconcile (OQ `plane-rotation-givens-l0-citation-range-reconcile`).

The §L0 §Primitives sub-section (lines 34-55) reduces to a pointer (retain the precise line-range citations as a compact citation list, drop the prose branch-count narration which is firm-side in concepts/givens_generate.md §Background):

### Primitives

The two 2×2-rotation primitives are firm at `book/src/concepts/givens_generate.md` (`GeneratePlaneRotation`, real `iterative.cpp:72-108` / complex `:110-222`) and `book/src/concepts/givens_apply.md` (`ApplyPlaneRotation`, real `:226-232` / complex `:234-242`). Both port LAPACK `xLARTG` / `xROT`; the invariant `cs² + |sn|² = 1` and the safe-range branch detail are firm-side. (See the reduction-status note above re: the one-off line-range discrepancy with `concepts/givens.md`.)

The §L1 §Procedure (lines 119-139) + §"Cross-target reuse" (141-148) reduce to a pointer:

### Procedure (per Arnoldi step `j`) and cross-target reuse

The per-step procedure (replay-prefix on the new column tail → generate from the bottom 2-tuple → apply-to-self → propagate to the RHS pair → read residual proxy `|s[j+1]|`) is firm at `book/src/concepts/plane-rotation-stream.md` §"Shape" (steps 1-5). The cross-target reuse — one `(c_j, s_j)` applied to two distinct targets (`Hj` column triangularization + `s` residual-proxy propagation) without re-derivation, the structural reason `(c, s)` is stored — is firm at `book/src/concepts/givens.md` §"L2 usage shape" (cross-target-reuse paragraph). See §"Invariant" above (hoisted from orthog.md) for the formal least-squares-residual relation.

The §L2 §Primitives (193-207) + §Procedure (209-238) reduce to a pointer (retain §"Stream operations as primitive sequences" 240-261 and everything below it):

### Primitives and procedure (L2)

The L2 primitive binding (`givens_gen` / `givens_apply` from the `book/src/concepts/givens.md` family) and the 5-line per-step procedure are firm at `concepts/givens.md` §"L2 usage shape" + `concepts/plane-rotation-stream.md` §"Shape". The unique L2 material — the boundary-slot read-after-write analysis that is the input to the L3 obstruction — is retained in §"Stream operations as primitive sequences" below.
]
```

### Proposed change 3: Partial reduction of `book/src/spec/slices/divfree.md`

```edit:book/src/spec/slices/divfree.md
[Two edits: (A) add a reduction-status header note at line 2 (after the `# Slice: divfree` H1); (B) stub §"L4 tightening notes" (lines 406-414) and trim §"L2" §"Optimization opacity" (lines 204-221) to retain only the load-bearing list. All L1/L2/L3/L4 projector-composition content + the divergence-free defining condition + the load-bearing claims are retained verbatim. The integrator should locate the §"L4 tightening notes" and §"Optimization opacity" headings by text-search, not by stale line numbers.

Header note inserted at line 2 (after `# Slice: divfree`):

> **Reduction status (cycle-012+):** this slice is the cycle-001-era precursor to a firm `L1/divfree-projector` operator entry that has not yet been promoted. The slice's L1/L2/L3/L4 divergence-free-projection content is currently the **only** firm definition of the projector in the artifact, and it is **load-bearing evidence** cited by:
> - `book/src/L1/ksp_solve.md:131` (`divfree.cpp:175` `ksp->Mult(rhs, psi)` is the direct L0 evidence of the `ksp_solve` use pattern) + `:143` (slice-corpus precedent for the L1/L2 `ksp_solve` use).
> - `book/src/L1/eigsolve.md` (`DivFreeSolver[ComplexVector]` is the optional `projector` field bound into the eigensolver).
> - `book/src/concepts/apply_linop.md` §"L2 use in divfree" (lines 41-54) — `rhs ← apply_linop(WeakDiv, y)` + `t ← apply_linop(Grad, psi)`.
> - `book/src/concepts/ksp_solve.md:34` — `psi ← ksp_solve(ksp, rhs)` divfree §L2 step 3.
> - `book/src/concepts/set_subvector_zero.md:27` — divfree §L2 step 2 essential-BC zeroing.
> - `book/src/L0/eigensolver-wrapper.md:44` — `opProj` / `DivFreeSolver<ComplexVector>*` composition setter.
>
> The L2 *primitives* (`apply_linop`, `set_subvector_zero`, `ksp_solve`, `axpy`, `copy`) are each firm; the projector *composition* (`P(y) = y + Grad·K⁻¹(Z_bdr(WeakDiv·y))` + the construction-time operator assembly) is NOT firm. The slice is retained in full (minus the firm-covered tightening notes + the transparent-opacity list) pending lift to a firm `L1/divfree-projector` operator entry.
>
> **Pending lift / verify:**
> - `L1/divfree-projector` — a strong harvester promotion candidate (small constructed-operator: Helmholtz-decomposition projector; lifting would let `L1/eigsolve` reference a firm operator type and let the three concept-page use-site citations point at a firm L1 entry). OQ `l1-divfree-projector-promotion`.
> - The **WeakDiv sign-convention** claim (that `MixedVectorWeakDivergenceIntegrator` encodes the negative-divergence sign, making `+Grad·ψ` the correction) is an unverified L0 reading (slice §"Open questions"). A flipped L0 sign would invert the correction direction at every layer; a `verify-citation-range` pass should anchor it before the firm L1 entry treats it as load-bearing. OQ `divfree-weakdiv-sign-convention-l0-verify`.

The replacement for §"L4 tightening notes" (lines 406-414):

## L4 tightening notes (reduced)

The cycle-167 L4→L4 tightening (scratch `psi`/`rhs`/`t` reclassified as ephemeral `SolveM`-scoped intermediates rather than construction-time internal-parameter storage; the V-parameter polymorphism note) is now firm-side: see `book/src/concepts/state-stratification.md` (the params/sim/ephemeral strata distinction) and `book/src/concepts/derived-view-hoisting.md` (the C++ pooled-scratch realization as a transparent hoist beneath the calculus). The §L4 §"State stratification" above already reflects the corrected stratification; no separate tightening narration is retained.

The trim of §"L2" §"Optimization opacity" (lines 204-221) — REMOVE the transparent list (PA-vs-assembled / GMG-vs-AMG / re-orthogonalization / fused-axpy) which is firm-side at `concepts/apply_linop.md` §"L2 use in divfree" + `L0/transparent-vs-load-bearing-tricks.md`; RETAIN the load-bearing list:

### Optimization opacity

The load-bearing L2 claims (preserved verbatim; these recur as the load-bearing claims at L3 §"Load-bearing claims preserved from L2" and L4 §"Load-bearing claims preserved at L4"):

- The sign convention on `WeakDiv` (so the correction is `+Grad·ψ`, not `−Grad·ψ`). An L0/L1 claim L2 honors verbatim.
- The `set_subvector_zero(rhs, bdr_eff)` step ordering: must run **after** `apply_linop(WeakDiv, y)` and **before** `ksp_solve`. Reorder changes the solution.
- `ksp_solve` returns the converged `ψ`; tolerance is baked into the ksp at construction.

(The transparent-optimization list — partial-vs-full assembly, GMG-vs-AMG, re-orthogonalization, fused apply+axpy — is firm-side: `concepts/apply_linop.md` §"L2 use in divfree" + `L0/transparent-vs-load-bearing-tricks.md`.)
]
```

## Supporting evidence

- `book/src/spec/slices/plane_rotation_stream.md` (418 lines; read in full). Complete L0/L1/L2/L3 Givens-stream dissection. §L3 (293-418) is the canonical detailed source for `concepts/sequential-obstruction.md` §"Worked example" (lines 83-112). H1/H2/H3 boundaries enumerated via `grep -n "^#\{1,6\} "`.
- `book/src/spec/slices/orthog.md` plane-rotation sub-slice (lines 225-376, the full `## Context`-through-EOF block; CORRECTED in repair from 311-376; read in full). Less-complete duplicate of `plane_rotation_stream.md`, containing a Context/Background/Variant-axes/L0-citations preamble + two near-duplicate L1 entries. Unique fragment: the formal LS-residual invariant at orthog.md:350. Note: this file is 376 lines post-batch-2 reduction (the batch-2 header note's "lines 313+" reference is stale — see §"Open questions" item 5).
- `book/src/spec/slices/divfree.md` (414 lines; read in full). L1/L2/L3/L4 Helmholtz-decomposition projector. Load-bearing evidence for `L1/ksp_solve`, `L1/eigsolve`, `concepts/apply_linop`, `concepts/ksp_solve`, `concepts/set_subvector_zero`, `L0/eigensolver-wrapper`.
- `book/src/concepts/plane-rotation-stream.md` (39 lines; read in full). Firm layer-pattern. §"Used in" (35-39) cites `orthog` slice as "primary dissection" (line 37) and `gmres` slice as consumer (line 39) — does NOT cite `plane_rotation_stream.md` (the actually-more-complete dissection). Stale-pointer finding.
- `book/src/concepts/sequential-obstruction.md` (113 lines; read in full). §"Worked example: Givens-stream replay-prefix (plane_rotation_stream)" (83-112) is a direct excerpt of `plane_rotation_stream.md` §L3 and cites it at line 85. The excerpt captures the loop + boundary-slot RAW + cost framing but elides the cross-target-no-batch / local-triviality-at-extend / Householder-WY boundary.
- `book/src/concepts/givens.md` (59 lines; read in full). §"Palace mapping" (33-34) cites `iterative.cpp:73-108` / `:227-241` (vs slice's `:72-108` / `:226-242`). §"L2 usage shape" (36-58) cites `plane_rotation_stream slice` at line 40 (this one is CORRECT) + has the cross-target-reuse paragraph (55-58).
- `book/src/concepts/givens_generate.md` (28 lines; read in full) + `givens_apply.md` (27 lines; read in full). Both cite `orthog` slice in §"Used in" (line 27 each) — stale after the orthog plane-rotation sub-slice reduction.
- `book/src/concepts/trsv.md` (20 lines; read in full). §"Role in higher-layer rotations" covers the GMRES `back_solve` = one `trsv`; supersedes the orthog sub-slice's `back_solve_R` fragment.
- `book/src/concepts/apply_linop.md` (119 lines; read in full). §"L2 use in divfree" (41-54) names the two divfree `apply_linop` calls explicitly + §"L3 tensor-field form" (56-77).
- `book/src/concepts/ksp_solve.md` (52 lines; read in full). §"Where it appears" (32-39) names divfree §L2 step 3.
- `book/src/concepts/set_subvector_zero.md` (60 lines; read in full). §"Where it appears" (26-30) names divfree §L2 step 2; §"L3 lift"/§"L3 tensor-field form" (38-59) is the `Z_S` projector.
- `book/src/L1/ksp_solve.md` (firm; cycle-007). Line 131 cites `divfree.cpp:175` as direct L0 evidence; line 143 lists divfree.md as slice-corpus precedent.
- `book/src/L1/apply_linop.md` (firm). Lines 11 + 108 reference divfree use in the cross-cutting prose.
- `book/src/L1/eigsolve.md` (firm; cycle-011). References `DivFreeSolver[ComplexVector]` as the optional `projector` field bound into the eigensolver (lines 60 + the L0/eigensolver-wrapper composition setters).
- `book/src/L0/eigensolver-wrapper.md` (firm). Line 44 names `opProj` / `DivFreeSolver<ComplexVector>*` for ARPACK/SLEPc/nonlinear.
- `book/src/concepts/dependency-map.md` (read lines 59-318). `plane-rotation-stream:::planned` markers (74-95) vs concrete `plane_rotation_stream` edges (165-318); `divfree` edges (153-392) confirm `ksp_solve`/`apply_linop`/`set_subvector_zero`/`axpy`/`copy`/`constructed-operators`/`state-stratification`/`derived-view-hoisting` dependencies.
- `book/src/concepts/constructed-operators.md`, `state-stratification.md`, `derived-view-hoisting.md`, `solve-monad.md`, `variant-absorption.md` (existence confirmed; firm).
- `reports/2026-05-27T220000Z-...-corpus-reduction-audit/CYCLE.md` (batch-1 template) + `reports/2026-05-27T234651Z-...-batch-2/CYCLE.md` (batch-2 template; §"Open questions" item 1 scheduled this joint audit).
- `CLAUDE.md` §Methodology invariants ("Phase 1 corpus reduces as material is lifted").
- `scaffolding/priorities.md` #19 (`phase-1-corpus-reduction-audit`).

## Open questions / caveats

1. **CLOSES the cycle-011 batch-2 joint-audit OQ.** This dispatch resolves `orthog-plane-rotation-stream-sub-slice-batch-3-joint-audit` (cycle-011 batch-2 §"Open questions" item 1). Verdict: the `orthog.md` plane-rotation sub-slice is a less-complete duplicate of `plane_rotation_stream.md`; full-reduce the orthog sub-slice to a stub-pointer (after hoisting one unique invariant fragment); partial-reduce `plane_rotation_stream.md`. The long-recorded `orthog/plane_rotation.md` structural split (OQ in both slices) is realized by recognizing that `plane_rotation_stream.md` IS that split product. **OQ amendment**: mark `orthog-plane-rotation-stream-sub-slice-batch-3-joint-audit` resolved-by-cycle-012-batch-3.

2. **HEADLINE — stale concept-page canonical pointers.** After the orthog plane-rotation sub-slice is stubbed, the firm concept pages `concepts/plane-rotation-stream.md:37` ("primary dissection" → `orthog` slice), `concepts/givens_generate.md:27` ("Used in" → `orthog` slice), and `concepts/givens_apply.md:27` ("Used in" → `orthog` slice) point at a stub. (`concepts/givens.md:40` already correctly cites `plane_rotation_stream` slice.) These must be repointed to `plane_rotation_stream.md` (the actually-more-complete dissection). **OQ to add to `scaffolding/open-questions.md`**: "`plane-rotation-concept-page-canonical-pointer-repoint` — after the cycle-012 batch-3 reduction of the `orthog.md` plane-rotation sub-slice to a stub, the firm concept pages `concepts/plane-rotation-stream.md:37`, `concepts/givens_generate.md:27`, `concepts/givens_apply.md:27` cite the `orthog` slice as the canonical plane-rotation dissection / use-site, but the more-complete dissection lives in `plane_rotation_stream.md`. Dispatch `layer-intro-author` to repoint these three cross-references to `plane_rotation_stream.md`. Source: cycle-012 phase-1-corpus-reduction-batch-3 §slice-1 residual gap 3."

3. **HEADLINE — firm `L1/divfree-projector` promotion.** divfree is a strong promotion candidate (small constructed-operator; load-bearing evidence for 6 firm entries). Promotion would firm the divfree definition and let `L1/eigsolve` + three concept-page use-site citations point at a firm L1 entry rather than the slice. **OQ to add**: "`l1-divfree-projector-promotion` — a firm `L1/divfree-projector` operator entry (Helmholtz-decomposition projector: `P(y) = y + Grad·K⁻¹(Z_bdr(WeakDiv·y))`) is pending lift. The slice-corpus `book/src/spec/slices/divfree.md` §L1/L2/L3/L4 is currently the only firm divfree definition; it is load-bearing evidence cited by `L1/ksp_solve.md:131,143`, `L1/eigsolve.md` (optional `projector` field), `concepts/apply_linop.md:41-54`, `concepts/ksp_solve.md:34`, `concepts/set_subvector_zero.md:27`, `L0/eigensolver-wrapper.md:44`. Promotion criterion (small AND simplifies higher forms) is met. Source: cycle-012 phase-1-corpus-reduction-batch-3 §slice-2 residual gap 1."

4. **verify-citation-range items.** (a) The plane_rotation_stream L0 ranges (`iterative.cpp:72-108`/`:226-242`) differ by one from firm `concepts/givens.md:33-34` (`:73-108`/`:227-241`). (b) The divfree WeakDiv sign-convention is an unverified L0 reading; a flipped sign would invert the correction at every layer. **OQs to add**: "`plane-rotation-givens-l0-citation-range-reconcile` — reconcile the one-off line-range discrepancy between `plane_rotation_stream.md` §L0 and `concepts/givens.md` §"Palace mapping". Source: cycle-012 batch-3 §slice-1 residual gap 4." AND "`divfree-weakdiv-sign-convention-l0-verify` — anchor the `MixedVectorWeakDivergenceIntegrator` negative-divergence sign to an L0 integrator citation before a firm `L1/divfree-projector` entry treats `+Grad·ψ` as a load-bearing claim. Source: cycle-012 batch-3 §slice-2 residual gap 2."

5. **Friction signal — stale embedded line references in prior reductions (new manifestation of `phase-1-corpus-audit-line-range-arithmetic-brittleness`).** The batch-2 header note at `orthog.md:9` references "the plane-rotation-stream sub-slice (lines 313+ below)", but the post-batch-2 file is 376 lines and the sub-slice actually begins at line **225** (the `## Context` heading that introduces the `# Orthogonalization (plane-rotation stream)` H1), running through end-of-file (376) — 151 lines. **REPAIR NOTE:** the original dispatch itself fell into this brittleness — it mis-stated the sub-slice as beginning at line 311/317 (which are merely the §"Open questions" and the SECOND of two near-duplicate L1 entries), missing the 225-310 preamble + FIRST L1 entry. The critic (META Issue 1, severity high) caught this; the repairer corrected the reduction scope to the full 225-376 block, text-anchored on the `## Context`/`# Orthogonalization (plane-rotation stream)` headings. Embedded/mis-read line-number references go stale or wrong as the file shrinks. **Mitigation applied this dispatch (and reinforced in repair)**: all proposed_changes instruct the integrator to locate sections by text-search (`grep -n "^# Orthogonalization (plane-rotation stream)"`, `grep -n "L4 tightening notes"`, `grep -n "Optimization opacity"`) rather than by line number. **Recommendation for cycle-012 meta-phase**: the friction-ledger entry `phase-1-corpus-audit-line-range-arithmetic-brittleness` (recurrence-2 as of cycle-011) should note this stale-embedded-reference sub-manifestation AND the fresh-mis-scope-during-audit manifestation (the audit's own grep window mis-identified the sub-slice start); the broader mitigation is "reduction header notes should not embed absolute line numbers; use section-heading text-anchors instead, and verify a sub-slice's START boundary (not just its END) when enumerating." Recurrence bumps to 3 — the in-audit mis-scope is a materially worse manifestation than a stale prior reference.

6. **First intra-corpus-redundancy verdict.** Batches 1-2 found precursor-overlap (slices that precede firm entries) and a negative-result slice (a methodological artifact). This batch finds the first **two-slices-dissect-the-same-algorithm redundancy** (`plane_rotation_stream.md` vs `orthog.md` plane-rotation sub-slice). The reduction action (full-reduce the less-complete twin to a stub-pointer at the more-complete twin, after hoisting unique fragments) is a new template entry for future batches. Future audits should check for intra-corpus duplication up-front (two slices may dissect overlapping algorithms even before either is lifted to a firm entry).

7. **divfree does NOT overlap krylov-step or an AMS preconditioner entry.** The dispatch prompt hypothesized divfree "overlaps the AMS preconditioner work and apply_linop." Audit finding: divfree overlaps `apply_linop` + `ksp_solve` (its primitives, both firm) but is a *consumer* of a CG solve, not a Krylov method itself — so it does NOT overlap `krylov-step`. There is no AMS preconditioner entry in the firm artifact; divfree's inner preconditioner is BoomerAMG (cited at `L0/mfem-wrapper-solver.md:60,110` as the inner AMG construction) absorbed inside the constructed `ksp` (constructed-operator absorption). The AMS-overlap hypothesis is not borne out; the divfree↔ksp_solve↔apply_linop overlap is the real relationship.

8. **Batch progress.** Batch-1 (cycle-010) covered 3 slices (gmres, cg, arnoldi_step); batch-2 (cycle-011) covered 3 (orthog, chebyshev, polynomial_recurrence_step); this batch covers `plane_rotation_stream` + the `orthog` plane-rotation sub-slice (joint) + `divfree`. Cumulative: 8 of 10 slice files audited (counting orthog once, now fully audited across batch-2 Gram-Schmidt + batch-3 plane-rotation). Remaining: `cg_preconditioning_framework.md` (priority #5; likely overlaps `L1/ksp_solve` + `L4/krylov-step` Form A + the chebyshev consumer pattern) and `sparse_triangular_solve.md` (priority #7; expected low-overlap / out-of-scope-obstruction per `concepts/sequential-obstruction.md` §"Sub-kind: out-of-scope-obstruction" which already cites it). Suggested batch-4: these final two slices.

9. **No slice file is mutated by this dispatch.** All proposed reductions are encoded as `proposed_changes` blocks for integrator-per-report. The verdicts and supersession/redundancy maps in this CYCLE.md are inspection-only.

10. **Joint-reduction ordering caveat.** Proposed change 1 (stub the orthog plane-rotation sub-slice) depends on proposed change 2(A) (hoist the formal LS-residual invariant into `plane_rotation_stream.md`) landing first or in the same integrator pass — otherwise the one unique fragment is lost. The integrator-per-report applying these should sequence 2(A) before 1, or apply both atomically.
