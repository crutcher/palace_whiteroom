---
verifies: ../CYCLE.md
critiqued_at: 2026-05-28T040000Z
critic_version: 1
checks:
  citation-validity: warning
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: warning
  cross-reference-integrity: pass
  edge-label-fidelity: pass
  plan-kind-consistency: warning
  skill-uptake-survey: warning
repaired_at: 2026-05-28T044500Z
repairer_version: 1
repairs:
  citation-validity: not-needed
  surface-or-evidence: not-needed
  rotation-quality: not-needed
  variant-axis-coverage: repaired
  cross-reference-integrity: not-needed
  edge-label-fidelity: not-needed
  plan-kind-consistency: repaired
  skill-uptake-survey: not-needed
overall_status: ready
follow_up_agent: null
---

# META: verification of "Phase 1 corpus reduction audit — batch 3 (plane_rotation_stream [joint] / divfree)"

## Critique

### Checks run

**citation-validity — warning.** Nearly all citations in the report resolve to real, in-range locations, and the load-bearing claims are well-anchored. Confirmed exactly: `L1/ksp_solve.md:131` (`divfree.cpp:175` `ksp->Mult(rhs, psi)`) + `:143` (slice precedent); `L1/eigsolve.md:60` (`DivFreeSolver[ComplexVector]` optional `projector` field); `L0/eigensolver-wrapper.md:44` (`opProj` / `DivFreeSolver<ComplexVector>*`, ARPACK `arpack.hpp:81` / SLEPc `slepc.hpp:110` / nonlinear `nleps.hpp:65`); `concepts/apply_linop.md:41-54` (§"L2 use in divfree" naming both `apply_linop` calls); `concepts/ksp_solve.md:34` (divfree §L2 step 3); `concepts/set_subvector_zero.md:27` (divfree §L2 step 2); `L1/apply_linop.md:11,108` (divfree cross-cutting prose); `concepts/sequential-obstruction.md:83-112` (worked example citing `plane_rotation_stream.md:85`, with the cost-argument / Householder-WY detail elided as the report claims). The L0-source spot-checks corroborate the report's own off-by-one flag: `GeneratePlaneRotation` real spec template line is `iterative.cpp:72`, function declaration `:73` (slice cites `:72-108`, `concepts/givens.md` cites `:73-108`); `ApplyPlaneRotation` real declaration `:226` (slice `:226-232`, concept `:227-241`). The report correctly surfaces this as residual gap 4 / OQ `plane-rotation-givens-l0-citation-range-reconcile` rather than asserting one is canonical. Two issues hold this to warning (not pass): (1) the report's structural map of the `orthog.md` plane-rotation sub-slice is **factually wrong about line ranges** (see plane-kind-consistency and Issue 1 below) — the report repeatedly cites "lines 311-376" / "~66 lines" for a sub-slice that actually spans lines 225-376; (2) the BoomerAMG support citation `L0/mfem-wrapper-solver.md:60,110` is imprecise — line 60 confirms "divfree.cpp:120 — divergence-free projection's inner AMG preconditioner" and the source confirms `BoomerAmgSolver` at `divfree.cpp:121`, but there is no relevant content at line 110 of that file, and the source line is 120/121 not 175 (175 is the `ksp->Mult` site). The substance (BoomerAMG, not AMS) is correct.

**surface-or-evidence — pass.** This is a corpus-reduction audit, not a refinement of an operator/theme surface. The proposals are reduction (stub/partial-reduce) actions over Phase-1 slice raw material, explicitly authorized by the "Phase 1 corpus reduces as material is lifted" invariant. All three reductions are framed as supersession/redundancy maps backed by firm-entry evidence pointers (retroactive-evidence-shaped, not pure-rotation-claim). The one new surface authored (the hoisted LS-residual invariant in proposed change 2A) is carried with its source anchor (orthog.md:350, which I verified — the formal invariant is present verbatim there). Applicable and clean.

**rotation-quality — pass.** No L_{n+1}→L_n algebraic/structural rotation is being *asserted as new* in this report; the rotation content cited (the §L3 sequential-obstruction worked example, the divfree clean-lift "obstruction lives at L2.5") is pre-existing firm material being referenced as retention rationale. The "what DOES lift / what is obstructed" claims for plane_rotation_stream §L3 are the slice's own pre-existing analysis, and the report's claim that the slice's §L3 is strictly more detailed than the firm concept-page excerpt is verified (the excerpt at sequential-obstruction.md:83-112 captures the loop + boundary-slot RAW + cost framing but elides the cross-target-no-batch-dim, local-triviality-at-extend, and Householder-WY boundary). Not applicable as a fresh-rotation check; marked pass.

**variant-axis-coverage — warning.** The divfree variant axes (VecType / H1-depth / empty-boundary) are surveyed and their absorption sites named; the plane-rotation-stream scalar-type/generation-formula/storage axes are surveyed. However the report's redundancy map for the orthog sub-slice covers only the SECOND L1 entry (orthog.md:317-376) and its sub-sections, and **silently omits an entire orthogonal branch**: the orthog plane-rotation sub-slice contains a FIRST `## L1 — per-element procedure` entry at lines 276-310 (plus its own `## Context` 225, `# Orthogonalization (plane-rotation stream)` H1 227, `## Background` 235, `## Variant axes` 248, `## L0 — citations` 258) that the report's structural map (CYCLE.md:68-70) does not list at all. The batch-2 report and the scheduling OQ both explicitly flagged the sub-slice as "two near-duplicate L1 entries that should be merged" — the batch-3 report dropped that finding and accounts for only one of the two. This is a hidden branch in the reduction scope.

**cross-reference-integrity — pass.** All `[link]`-style references and named slugs resolve. Verified present: `book/src/concepts/{plane-rotation-stream,givens,givens_generate,givens_apply,trsv,apply_linop,ksp_solve,set_subvector_zero,sequential-obstruction,constructed-operators,state-stratification,derived-view-hoisting,solve-monad,variant-absorption,dependency-map}.md`; `book/src/L1/{ksp_solve,apply_linop,eigsolve}.md`; `book/src/L0/{eigensolver-wrapper,mfem-wrapper-solver}.md`; `book/src/L3/{apply_linop,axpy}.md`; `book/src/L2/krylov-step.md`; all three target slice files. The stale-pointer finding (concept pages cite the `orthog` slice as canonical) is itself correct: `concepts/plane-rotation-stream.md:37`, `givens_generate.md:27`, `givens_apply.md:27` cite the `orthog` slice; `givens.md:40` correctly cites `plane_rotation_stream`. The OQ slug `orthog-plane-rotation-stream-sub-slice-batch-3-joint-audit` resolves at `scaffolding/open-questions.md:1946`.

**edge-label-fidelity — pass.** Not a lowering-edge proposal; no L_{n+1}→L_n edge label is carried. The layer references in the supersession maps (L0/L1/L2/L3/L4 sections of the slices, and the firm concept/operator entries they map to) are internally consistent with the slice structures observed. Not applicable; marked pass.

**plan-kind-consistency — warning.** The report declares itself an inspection-only same-layer-cross-cutter audit emitting `proposed_changes` for integrator-per-report, and the bulk of the content matches that shape. The mis-classification is local but consequential: the orthog sub-slice action is labeled "**full reduction**" of the plane-rotation sub-slice, but the proposed edit (Replace lines 311-376) only covers the lower ~66 lines of a sub-slice that actually spans lines 225-376. Applying the proposed change as written would leave lines 225-310 (Context, the section H1, Background, Variant axes, L0 citations, and the FIRST L1 entry) orphaned — i.e. the action is in fact a partial reduction mislabeled as full, and would leave the file with a dangling stub-pointer beneath ~86 lines of un-reduced duplicate content. The verdict's intent (full reduction of the redundant twin) is sound; the encoded scope contradicts the label.

**skill-uptake-survey — warning.** The report's shape implies several skills: `verify-citation-range` (the report surfaces two citation-range reconciliation OQs but does not appear to have invoked the skill on either — the off-by-one and the WeakDiv-sign anchor are both deferred rather than checked); `classify-variant-axis` (the variant-axis surveys for divfree and the plane-rotation stream are done by hand). The report does cite use of `grep -n "^#\{1,6\} "` for header enumeration as the brittleness mitigation, which is the right instinct, but that header enumeration evidently was run against a stale or mis-scoped view of `orthog.md` (it reports the sub-slice as starting at 311, missing the 225-310 block). No skill invocation is recorded for citation-range verification. Pure telemetry; non-blocking.

### Issues found

**Issue 1 (severity: high) — orthog plane-rotation sub-slice line-range map is wrong; "full reduction" scope is incomplete.** CYCLE.md §"Specific finding" Slice 1 (lines 53-82), the structural map (68-70), and Proposed change 1 (172-192). The report states the orthog plane-rotation sub-slice is "lines 311-376 (~66 lines)", "L1-only-plus-OQ", with structure `## Open questions` (311) + `## L1 — per-element procedure (plane-rotation stream)` (317). The actual current `orthog.md` (376 lines total) has the plane-rotation sub-slice running from line **225** (`## Context`) through line 376 — 151 lines — comprising: `## Context` (225), `# Orthogonalization (plane-rotation stream)` H1 (227), `## Background` (235, with the unique Bindel/Demmel/Kahan/Marques 2002 scaled-Givens citation), `## Variant axes` (248), `## L0 — citations` (258, which cite `gmres.cpp` — the very path the report flags as possibly stale), `## L1 — per-element procedure` (276, the FIRST L1 entry), `## Open questions` (311), and `## L1 — per-element procedure (plane-rotation stream)` (317, the SECOND near-duplicate L1 entry). Proposed change 1's "Replace lines 311-376" therefore reduces only the second L1 entry + OQ and leaves lines 225-310 orphaned. Consequence: the integrator would either (a) apply literally and leave ~86 lines of un-reduced duplicate beneath a stub that claims the content moved, or (b) be forced to re-derive the true scope. The redundancy verdict (same Givens-stream algorithm; plane_rotation_stream.md is the more-complete twin) is itself correct and well-evidenced — only the reduction SCOPE / line map is defective.

**Issue 2 (severity: medium) — the "two near-duplicate L1 entries" finding from batch-2 was dropped.** CYCLE.md Slice 1, throughout. The scheduling OQ at `open-questions.md:1954` and the batch-2 report both explicitly recorded that the orthog plane-rotation sub-slice contains "two near-duplicate L1 entries (lines 364-398 and lines 405-464) that should be merged." Those two entries persist in the current file as the FIRST (orthog.md:276-310) and SECOND (orthog.md:317-376) L1 entries. The batch-3 report's redundancy map accounts for only the second; the first is never mentioned. Since this batch was specifically chartered to resolve that joint-audit OQ, the omission means the report's "this realizes the long-recorded split / eliminates the duplication" conclusion is materially incomplete — the intra-file duplication (two L1 entries within orthog.md) is distinct from the cross-file redundancy (orthog vs plane_rotation_stream) and is left unaddressed.

**Issue 3 (severity: low) — OQ "closure" is a recommendation, not an enacted close.** CYCLE.md §"Open questions / caveats" item 1 (line 301) states the dispatch "CLOSES" / "resolves" `orthog-plane-rotation-stream-sub-slice-batch-3-joint-audit` and gives an "OQ amendment: mark resolved-by-cycle-012-batch-3." The OQ at `open-questions.md:1946-1954` is still `status: open` — correctly so, since the same-layer-cross-cutter has no write authority over `open-questions.md` (that is integrator-per-report's append channel and the status flip is an integrator action). The report's phrasing ("CLOSES") slightly overstates an inspection-only dispatch's authority; the body does correctly frame it as a surfaced amendment for the integrator. Note also that, given Issue 1/2, whether this OQ should be marked fully resolved by this dispatch is questionable — the joint audit's mandate (decide canonical home AND avoid stale stubs) is only partially met because the proposed reduction would itself create a stale-stub-over-live-content condition.

**Issue 4 (severity: low) — BoomerAMG support citation imprecise.** CYCLE.md §"Open questions / caveats" item 7 (line 313) cites BoomerAMG "at `L0/mfem-wrapper-solver.md:60,110`". `mfem-wrapper-solver.md:60` confirms "palace/linalg/divfree.cpp:120 — divergence-free projection's inner AMG preconditioner" (correct), but line 110 of that file is not a relevant anchor, and the source-level fact is `BoomerAmgSolver` constructed at `divfree.cpp:121` (wrapper at `:120`), not at `:175` (which is the `ksp->Mult` site). The AMS-vs-BoomerAMG correction itself is correct and well-grounded: source search confirms `BoomerAmgSolver` at `divfree.cpp:121` and confirms AMS is a *different* operator (`hcurl.cpp:92`, per `mfem-wrapper-solver.md:61`), so "no AMS entry; divfree uses BoomerAMG absorbed in the ksp" stands.

**Issue 5 (severity: low, corroborated-positive) — AMS-overlap correction and eigensolver-path-only callers both verified.** CYCLE.md item 7 (line 313) and the divfree call-sites claim. A repo-wide source search for `divfree|DivFree` across `palace/drivers/*.cpp` returns hits ONLY in `eigensolver.cpp` (construction at line 229 gated on `divfree_max_it > 0`, `divfree->Mult(v0)` at line 262) — confirming the report's "eigensolver-path-only callers" finding and supporting the divfree OQ that the driven/transient scope attribution is stale. No defect here; recorded as a positive confirmation of two of the report's headline corrections.

**Issue 6 (severity: low) — stale-pointer claim slightly overstated in mechanism.** CYCLE.md §slice-1 residual gap 3 (line 97) and §"Open questions" item 2 (line 303) say that after the orthog sub-slice is stubbed, three concept-page cross-references "point at a stub." The cross-references (`concepts/plane-rotation-stream.md:37`, `givens_generate.md:27`, `givens_apply.md:27`) point at the `orthog.md` *file* (not a specific line/section), so they would not literally dangle even after a sub-slice stub. The canonical-pointer problem the report identifies is real and worth the `layer-intro-author` repoint, but the "point at a stub" framing is imprecise — and is further undercut by Issue 1 (the proposed reduction does not even stub the whole sub-slice).

---

## Repair

### Fixes attempted

- **Finding (Issue 1, severity high)**: The orthog plane-rotation sub-slice line-range map is wrong — the report claims the sub-slice is "lines 311-376 (~66 lines)" and proposed change 1 ("Replace lines 311-376") would orphan lines 225-310 (Context, the section H1, Background, Variant axes, L0 citations, and the FIRST of two near-duplicate L1 entries) beneath a stub.
  - **Decision**: repaired.
  - **Action**: Verified the true boundaries by direct read + `grep -n "^## \|^# "` of `book/src/spec/slices/orthog.md` (the sub-slice spans `## Context` at line 225 through EOF at line 376; 151 lines; two near-duplicate `## L1 — per-element procedure` entries at 276-310 and 317-376). Corrected the reduction scope everywhere it appeared in CYCLE.md:
    - **Proposed change 1** (CYCLE.md §"Proposed changes") — rewrote the `edit:book/src/spec/slices/orthog.md` block to replace the FULL sub-slice (`## Context` heading through EOF), using **text-anchors** (`grep -n "^# Orthogonalization (plane-rotation stream)"` to locate the H1, then the `## Context` immediately above it; END at EOF) rather than line numbers, per the brittleness-mitigation discipline the dispatch itself adopted. Verified both anchors are unique in the file (the `# Orthogonalization (plane-rotation stream)` H1 occurs exactly once; `## Context` occurs exactly once at 225). Added an explicit caution distinguishing the retained Gram-Schmidt §L4 `### Open questions` (H3, line 220) from the sub-slice boundary. Prepended a `> **REPAIRED (repairer, cycle-012):**` note recording the original 311-376 error.
    - **Slice-1 heading + audit verdict** (CYCLE.md:53-56), **structural map** (CYCLE.md:68-70, expanded to enumerate all 8 sub-slice headings + the FIRST L1 entry's sub-sections), **redundancy map** (CYCLE.md:72-80, added entries for Context / Background / Variant-axes / L0-citations / the omitted FIRST L1 entry), **recommended action** (CYCLE.md:100-101), **Summary headline** (CYCLE.md:38), **frontmatter input** (CYCLE.md:8), and **Supporting evidence** (CYCLE.md:279) — all corrected from 311-376 to 225-376.
    - Updated the **stub text** itself to state that the Context/Background/Variant-axes/L0-citations preamble + BOTH L1 entries reduce into the single stub, and that the Background's scaled-Givens references are firm-side (`concepts/givens_generate.md:19`) and the former `gmres.cpp` L0 citations are dropped in favor of the firm `iterative.cpp` ones.
  - **Verification that full reduction is safe (pass-after-repair judgment, not revise)**: I read the entire sub-slice (orthog.md:225-376) and checked every fragment against the canonical entries:
    - Context / Background / Variant axes → fully covered by `plane_rotation_stream.md` §Context/§L1-Variant-axes + `concepts/plane-rotation-stream.md`; the Bindel/Demmel/Kahan/Marques 2002 scaled-Givens citation is firm verbatim at `concepts/givens_generate.md:19` (confirmed by grep), the Saad/Golub references at `plane_rotation_stream.md:25`.
    - L0 citations (`gmres.cpp`) → superseded by the firm/correct `iterative.cpp` citations in `plane_rotation_stream.md` §L0 (the report's own residual gap 4 flags `gmres.cpp` as likely stale).
    - FIRST L1 entry (276-310) → strict subset of the SECOND L1 entry (317-376), which is a strict subset of `plane_rotation_stream.md` §L1. `back_solve` → firm via `concepts/trsv.md` + `L2/krylov-step`.
    - The ONE genuinely unique fragment, the formal least-squares-residual invariant (orthog.md:350), is already hoisted into `plane_rotation_stream.md` by **proposed change 2(A)** (unchanged by this repair). With 2(A) sequenced first (per caveat item 10), full reduction loses nothing. Confirmed → `pass-after-repair`, not `revise`.

- **Finding (Issue 2, severity medium / variant-axis-coverage warning)**: The batch-2 "two near-duplicate L1 entries (should be merged)" finding was silently dropped; the report's redundancy map accounted for only the second L1 entry.
  - **Decision**: repaired.
  - **Action**: Incorporated the merge into the corrected reduction explicitly. The corrected redundancy map (CYCLE.md:72-80) now maps the FIRST L1 entry (276-310) as a strict subset of the second; the corrected proposed change 1, recommended action, summary, and stub text all state that the merge is realized as **elimination** — both near-duplicate L1 entries collapse into the single stub-pointer (an in-file merge of the two entries is unnecessary because the whole sub-slice reduces). Added an "Intra-file duplication note" to the structural map recording this. This is the mechanical incorporation of an already-decided, already-evidenced finding (the redundancy verdict and canonical-home choice were sound) — no new content authoring.

- **Finding (plan-kind-consistency, warning)**: The orthog sub-slice action is labeled "full reduction" but the encoded edit scope was a partial reduction (would leave 225-310 orphaned) — label/scope contradiction.
  - **Decision**: repaired.
  - **Action**: Resolved by the Issue-1 scope correction — the encoded edit now matches the "full reduction" label (full sub-slice → stub). No relabeling needed; the intent was correct, only the scope was wrong.

- **Finding (citation-validity, warning — sub-part)**: The structural-map line numbers for the orthog sub-slice are factually wrong (the warning's defect (1); defect (2), the `mfem-wrapper-solver.md:60,110` BoomerAMG imprecision, is Issue 4).
  - **Decision**: defect (1) repaired (same edits as Issue 1); defect (2) (BoomerAMG `:110` imprecise, source is `divfree.cpp:120/121` not `:175`) — **not-needed for status** / left as informational. Rationale below.

- **Findings (variant-axis-coverage divfree survey, skill-uptake-survey, Issues 3-6)**: lower-severity / informational.
  - **Decision**: not-needed (informational), per instruction. Notes below.

### Unrepairable findings

None. The single high-severity defect (Issue 1) and its medium companion (Issue 2) were both mechanical line-map / scope corrections backed by directly-verifiable source structure — no substantive authoring was required. The redundancy *verdict* (the sub-slice is the less-complete twin of `plane_rotation_stream.md`; canonical home is `plane_rotation_stream.md`) was sound and is untouched; only the reduction *scope/line-map* was defective, which is squarely in repair authority ("Citation line range off by a small offset" / structural line-map correction, generalized to a sub-slice boundary).

### Informational notes (lower-severity, not blocking)

- **Issue 3 (low) — "CLOSES" overstates inspection-only authority.** Left as-is. The OQ status flip is an integrator-per-report action; the report's body already frames it as a surfaced amendment. Note for the integrator: given the repair, the joint-audit OQ's mandate (canonical home + avoid stale stubs) is now fully met — the corrected reduction stubs the whole sub-slice rather than creating a stale-stub-over-live-content condition. The OQ may now be marked resolved by the integrator with confidence.
- **Issue 4 (low) — BoomerAMG citation imprecise.** `mfem-wrapper-solver.md:110` is not a relevant anchor and the source line is `divfree.cpp:120/121` (wrapper/`BoomerAmgSolver`), not `:175` (the `ksp->Mult` site). The substance (BoomerAMG not AMS, absorbed in the ksp) is correct. This is a caveat-prose citation in CYCLE.md §"Open questions / caveats" item 7, not a load-bearing proposed_change anchor; left as informational. A `verify-citation-range` pass could tighten it but it does not affect any reduction edit.
- **Issue 6 (low) — "point at a stub" framing imprecise.** The concept-page cross-references point at the `orthog.md` file, not a line, so they don't literally dangle. After the repair, the file IS fully stubbed for the plane-rotation content, so the canonical-pointer-repoint OQ (`plane-rotation-concept-page-canonical-pointer-repoint`, surfaced for `layer-intro-author`) remains the right follow-up; the imprecise "dangle" wording is harmless. Left as-is.
- **variant-axis-coverage (divfree) + skill-uptake-survey warnings**: pure telemetry. The divfree variant-axis survey (VecType / H1-depth / empty-boundary) is sound; the un-invoked `verify-citation-range` for the two citation-range OQs is correctly *deferred* (those are integrator/follow-up actions, not in-dispatch obligations). No repair.

## Suggested resolution

`overall_status: ready`. The one high-severity defect and its medium companion are fully repaired by a mechanical, source-verified line-map/scope correction; the redundancy verdict they sat on was sound and is unchanged. The corrected proposed change 1 is text-anchored (verified-unique anchors) and now matches its "full reduction" label, eliminating both near-duplicate L1 entries and the preamble into a single stub-pointer at `plane_rotation_stream.md`. Proposed changes 2 and 3 were not implicated by any defect and are unchanged.

Notes for the integrator:
- **Sequence proposed change 2(A) before proposed change 1**, or apply both atomically (the report's own caveat item 10) — 2(A) hoists the one unique fragment (the formal LS-residual invariant, orthog.md:350) into `plane_rotation_stream.md`, so reducing the orthog sub-slice loses nothing.
- **Apply proposed change 1 by text-anchor, not line number** — locate `# Orthogonalization (plane-rotation stream)` (unique H1), take the `## Context` immediately above it as the START, replace through EOF. Do NOT touch the Gram-Schmidt §L4 `### Open questions` (H3) above the START anchor.
- The three follow-up OQs the report surfaces remain valid post-repair: `plane-rotation-concept-page-canonical-pointer-repoint` (→ `layer-intro-author`), `l1-divfree-projector-promotion` (→ `harvester`), and the two `verify-citation-range` items. None block this report's application.
