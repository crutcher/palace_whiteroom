---
agent: cycle-planner
invoked_at: 2026-06-05T211311Z
scope: cycle-107 dispatch plan
status: pending
---

# Cycle 107 dispatch plan

## Goals selected this cycle

The bottom-up forward frontier is exhausted; the live work is the meta-owned GRADED-STACK typed-edge campaign (priorities item 0). This cycle pushes the **WAVE-3 reachability follow-up** carried from cycle-106 — the `dofset` rescue gap (item 1's ONE residual) — but the on-disk evidence shows the recommended edge is **NOT cleanly faithful at the column-composition altitude**, so the LEAD is scoped audit-first (faithful-path-or-finding, not blind edge-add). I co-schedule the LOW item-3c lazy-tail concept-page typing (16 non-node pages → `reference`-only blocks per the batch-33 ratified convention). **Two dispatches, one wave** (disjoint file sets).

## Pre-dispatch finding (the LEAD is a FINDING, not a blind edge-add)

Cycle-106's finalize recommended adding `feature/{electrostatic,magnetostatic,eigenmode}.L4 →composes eliminate_bc` to rescue `dofset`. The LEAD-prompt correctly required the planner to VERIFY faithfulness before scoping a blind add. I verified against the actual driver-column compositions + the Palace source:

- **None of the three driver columns names `eliminate_bc` as an L4 composition stage.** Their `edges:` `depends-on` lists are: electrostatic = `{fe_assemble, solve_family, ksp_solve, gram_reduce}`; magnetostatic = same; eigenmode = `{fe_assemble, eigsolve}`. The prose compositions (`electrostatic.L4.md:36-43`, `magnetostatic.L4.md:38-46`, `eigenmode.L4.md` `:1a/1b/1c`) compose `fe_assemble` for the assemble stage — BC-elimination is **not** a column-visible stage.
- **The book is internally explicit that this is correct.** `L4/fe_assemble.md:121` ("BC-elimination is NOT part of the fold") + `:71` + `:177` state `eliminate_bc` is a **separable post-composition** running AFTER the assemble fold; `L4/eliminate_bc.md`'s frontmatter carries `reference: L4/fe_assemble` with the explicit comment "post-composes AFTER the assemble fold … NOT a blocking fold dependency — separability law 8."
- **The Palace `Eliminate*` calls live in `models/`, BELOW the driver-pipeline altitude.** `palace-codemap search_text` for `Eliminate` over `palace/**/*.cpp` returns hits ONLY in `palace/models/laplaceoperator.cpp:240,252` (`EliminateRHS`), `palace/models/modeeigensolver.cpp:570-611` (`EliminateBC`), and `palace/linalg/rap.cpp:56,140-143,181`. The driver sources `palace/drivers/{electrostatic,eigen}solver.cpp` make **NO direct `Eliminate*` call** — the elimination is buried inside `GetStiffnessMatrix()` / operator construction (the `models/` layer that `fe_assemble` represents at L4).
- **Consequence:** a `column →composes eliminate_bc` edge would be an **unfaithful over-link** — it asserts a column-level composition stage that does not exist at the column altitude (BC-elimination is absorbed into `fe_assemble`'s assembled-operator below column-visible composition). This is exactly the c104-D2 / cycle-106-D3 discipline: faithful path or finding.

**The reachability state (linter `--show-inbound`, live tree this invocation):** `L4/eliminate_bc` is `[GARBAGE*]` (inbound only from `L4-L3/bc-elimination-post-composition-dissolution` + `concepts/dofset`, neither root-reachable); `concepts/dofset ← L4/eliminate_bc` ONLY → dofset reachable iff eliminate_bc is. `L4/fe_assemble ← all 7 feature columns` (root-reachable). So the candidate faithful liveness path is NOT the column edge but **whether `fe_assemble →(post-composes) eliminate_bc` should carry liveness** (currently a deliberate `reference`, non-blocking) — a producer/meta judgment the LEAD dispatch must make and route.

**Coupled second finding (D4's c106 OQ):** `set-subvector-zero-cluster-reachability-not-rescued-by-reference-backlink`. `L1/set_subvector_zero`'s natural consumer is `divfree-projector` — but `L3/divfree-projector` is ITSELF `[GARBAGE*]` (the whole divfree cluster is detritus; `L2/divfree-projector ← L3/divfree-projector` only). No feature column references divfree-projector OR set_subvector_zero. So the set_subvector_zero rescue has the **same root cause** as dofset: a firm BC/divfree vocabulary cluster absorbed below the L4 column altitude, with no root-reachable composer. **Both OQs are ONE coherent cross-layer reachability-gap finding** → handled by the single LEAD audit dispatch.

## Deliverable-presence verification

Per the MANDATORY pre-dispatch four-step check (paste-inline-evidence). All `ls`/`grep` outputs below are literal command results from this invocation.

**D1 (LEAD) — touches `feature/{electrostatic,magnetostatic,eigenmode}.L4.md`, `L4/eliminate_bc.md`, `concepts/{dofset,set_subvector_zero}.md`, `L1/set_subvector_zero.md`, `L4/fe_assemble.md` (read/audit + at-most one faithful edge-add):**
1. File existence — all EXIST: `feature/electrostatic.L4.md (feature_root: seed)`, `feature/magnetostatic.L4.md (feature_root: seed)`, `feature/eigenmode.L4.md (feature_root: seed)`, `L4/eliminate_bc.md (firmness: firm)`, `concepts/dofset.md (rank: firm)`, `concepts/set_subvector_zero.md`, `L1/set_subvector_zero.md (rank: firm)`, `L3/divfree-projector.md (firmness: firm)`.
2. Maturity / already-discharged — this is NOT a no-op: the WAVE-3 dofset rescue is the ONE carried follow-up (item 1 residual), open by construction (cycle-106 D3 explicitly declined to force the edge and routed the OQ). The dispatch is an AUDIT (determine faithful path) + AT-MOST a faithful edge-add — not a re-author of firm content.
3. OQ-ledger RESOLVED-grep — `grep 'bc-driver-column-eliminate-bc-edge-gap.*RESOLVED\|...CLOSED' scaffolding/open-questions.md` → no matches (the OQ is OPEN, `opened_at: cycle-106`, needs-more). Same for `set-subvector-zero-cluster-reachability-not-rescued-by-reference-backlink` (OPEN, opened cycle-106).
4. Structural-block check — NOT blocked: cycle-106 D3 INSTALLED the second half (`eliminate_bc →uses-record dofset`, now GC-traversed); the first half (the root-reachable composer of eliminate_bc) is the open question this dispatch resolves. The graded-stack faithful-path-or-finding gate (CLAUDE.md §GRADED RESOLUTION LADDER + layer-intro-author §(f)) GOVERNS the dispatch (it must not force an unfaithful edge) but does not BLOCK it.

**D2 — touches the 16 concept pages lacking an `edges:` block (item 3c lazy tail):**
1. File existence — all 16 EXIST (`ls book/src/concepts/*.md` + `grep -L '^edges:'`): `build-time-vs-run-time-stratification`, `capability-typing`, `chebyshev-iteration`, `constructed-operator-factory`, `constructed-operators`, `convergence-test`, `counter-update`, `derived-view-hoisting`, `eigsolve`, `erasure-scope`, `ksp_solve`, `nested-constructed-operator-gate`, `rotation`, `solve-monad`, `solver-as-operator`, `state-stratification`. (Count confirmed: 16 concept pages lack `edges:`.)
2. Maturity — these are the c103-D2 strict-zero cluster-B/C **non-node concept pages** left without `edges:` frontmatter; the batch-33 meta-phase RATIFIED the non-node encoding (`reference`-only `edges:` block + no `rank:`, `graded-stack-scheme.md` §5/§6). Adding the block is the open, ratified next-touched action — NOT a no-op (they currently carry zero `edges:`).
3. OQ-ledger — the node-status convention OQs (`graded-stack-concept-node-status-convention` / `concept-non-node-frontmatter-encoding-reference-only-vs-empty`) were RESOLVED by the batch-33 meta-phase ratification; this dispatch APPLIES the ratified convention (the resolution = the encoding to author).
4. Structural-block — NOT blocked: the convention is ratified and the linter warns-not-fails on untyped (option-(a) incremental tail; these are pointer-pages, no `depends-on` liveness).

**STOP-PROPOSING NEGATIVE LIST check:** no dispatch proposes any of `lu_solve`/`back_solve`/`ls-update-column`/`nleps_*` as an L3 backfill. No `promotion_frontier` obstruction-/demand-gated member proposed as a forward pick (the redirect forbids forcing a rectangular pull-up; `eliminate_bc` is already firm — this is a reachability-edge question, not a promotion).

## Dispatches

**D1 (`layer-intro-author`, HIGH, WAVE-1) — WAVE-3 reachability follow-up: the BC/divfree absorbed-below-column reachability gap (faithful-path-or-finding AUDIT + at-most a faithful edge-add).**
Scope: Resolve the ONE carried follow-up (dofset rescue, item 1 residual) AND the coupled D4 OQ (set_subvector_zero cluster) as ONE cross-layer reachability-gap finding. **Do NOT blindly add `column →composes eliminate_bc`** — the planner's on-disk audit (above) shows that edge is unfaithful at the column altitude (the three driver columns compose `fe_assemble`; BC-elimination is a separable post-composition absorbed into the `models/`-level operator construction, `palace/models/{laplaceoperator.cpp:240,252,modeeigensolver.cpp:570-611}`, NOT a driver-pipeline call; the book asserts this at `L4/fe_assemble.md:121,71,177` + `L4/eliminate_bc.md` `reference: L4/fe_assemble` "NOT a blocking fold dependency — separability law 8"). Instead:
1. **Determine the faithful liveness path** for `eliminate_bc`/`dofset` (and report the same determination for `set_subvector_zero`/`divfree-projector`). Candidate faithful resolutions to EVALUATE (pick the faithful one, or route the negative finding):
   (a) **`L4/fe_assemble →(post-composes) eliminate_bc` carries liveness** — promote the deliberate `reference` (`L4/fe_assemble.md` frontmatter) to a `depends-on (kind: post-composes)` edge IF the separable-post-composition relationship is judged a genuine liveness dependency (fe_assemble's output IS the operator eliminate_bc consumes; fe_assemble is root-reachable from all 7 columns). This rescues `eliminate_bc` + `dofset` + transitively the firm L1 `eliminate_essential_bc`/`eliminate_rhs`/`essential_dofs` WITHOUT an unfaithful column edge. **WEIGH carefully:** the book currently states this is deliberately non-blocking (separability law 8) — flipping it to `depends-on` reverses a documented stance, so it requires an explicit faithfulness argument (does fe_assemble's liveness genuinely IMPLY eliminate_bc's? the post-composition is OPTIONAL — homogeneous-BC pipelines skip eliminate_rhs). If the optionality makes the liveness edge unfaithful, this is a FINDING not an edge.
   (b) **A faithful column→eliminate_bc edge exists after all** — only if the column author judges BC-elimination a column-visible stage (the audit says it is NOT, but the producer re-confirms against the column prose).
   (c) **The finding: `eliminate_bc`/`dofset`/`set_subvector_zero`/`divfree-projector` are correctly absorbed below the L4 column altitude and have NO faithful root-reachable composer** — they are firm vocabulary that is reachable-GC-detritus by construction (the model-level BC/divfree machinery the driver columns abstract over). Route as the resolution: document the BC/divfree cluster as a **known-detritus-but-firm** baseline-exception (the graded-stack baseline-exception ledger, meta-phase-owned), NOT a forced edge.
2. If (a) or (b) is faithful: author the SINGLE faithful edge (block-mapping `depends-on` form; the batch-33 linter traverses it), re-run the linter, confirm `eliminate_bc`/`dofset` flip `[garbage?]`→reachable in `--show-inbound`. If only (c): author NO edge; write the finding + recommend the baseline-exception ledger entry to the batch-34 meta-phase.
3. Report the `set_subvector_zero`/`divfree-projector` cluster determination explicitly (same root cause; its consumer `L3/divfree-projector` is itself `[GARBAGE*]`, no column references divfree — so it is the SAME absorbed-below-column finding; the c106 D4 back-link fix typed it but did not rescue it).
Files (read/audit): `feature/{electrostatic,magnetostatic,eigenmode}.L4.md`, `L4/fe_assemble.md`, `L4/eliminate_bc.md`, `concepts/dofset.md`, `L3/divfree-projector.md`, `L1/set_subvector_zero.md`, `concepts/set_subvector_zero.md`. Files (write, at most one faithful edge): the ONE faithful host (most-likely `L4/fe_assemble.md` frontmatter under path (a), or none under path (c)). Codemap evidence pre-supplied: `palace/models/laplaceoperator.cpp:240,252`, `palace/models/modeeigensolver.cpp:570-611`, `palace/linalg/rap.cpp:140-143,181` (the `Eliminate*` sites — all `models/`/`linalg/`, none in `drivers/`).
Rationale: the ONE carried WAVE-3 follow-up (item 1 residual) + the coupled D4 OQ; resolves OQs `bc-driver-column-eliminate-bc-edge-gap-blocks-dofset-rescue` + `set-subvector-zero-cluster-reachability-not-rescued-by-reference-backlink` (as a faithful edge OR a routed baseline-exception finding). **fan-out: HIGH** (closes the last structurally-large WAVE-3 reachability question — either rescues the BC/divfree firm cluster, or definitively classifies it as firm-but-absorbed detritus, sharpening the GC discriminator).

**D2 (`layer-intro-author`, LOW, WAVE-1) — item-3c lazy-tail: the 16 strict-zero cluster-B/C non-node concept pages get the ratified `reference`-only `edges:` block.**
Scope: Author the batch-33-ratified non-node encoding (`reference`-only `edges:` block + NO `rank:`, per `graded-stack-scheme.md` §5/§6) onto the 16 concept pages that currently carry zero `edges:` frontmatter: `build-time-vs-run-time-stratification`, `capability-typing`, `chebyshev-iteration`, `constructed-operator-factory`, `constructed-operators`, `convergence-test`, `counter-update`, `derived-view-hoisting`, `eigsolve`, `erasure-scope`, `ksp_solve`, `nested-constructed-operator-gate`, `rotation`, `solve-monad`, `solver-as-operator`, `state-stratification`. For each: add a `reference:` list of the pages it points-at (the in-body markdown links), NO `depends-on` (these are navigational pointer-pages, not DAG nodes carrying liveness), NO `rank:`. NOTE the batch-33 borderline reconciliation: `counter-update`→node, `chebyshev-iteration`→non-node (so `counter-update` if touched gets node treatment per the ratification — but it is in the no-edges list, so confirm its ratified disposition: the batch-33 enactment says `counter-update`→node, meaning it should get a `rank:` + `depends-on` if it is a real node; apply the ratified per-page call, defaulting the other 15 to non-node reference-only). Re-run the linter after; `untyped` should drop by ~16 toward 60; `rank_violations` MUST hold 0; NO node newly-orphaned.
Files: the 16 `concepts/*.md` listed (frontmatter only). DISJOINT from D1 (D1 touches `dofset`/`set_subvector_zero`/feature columns/`fe_assemble`/`eliminate_bc`/`divfree-projector`; NONE of those 6 appear in D2's 16). Verify no overlap at dispatch (`eigsolve`/`ksp_solve` are in D2's list and are L4 op-chapter COUSINS but the `concepts/` pointer-pages are distinct files from the `L4/` chapters — D1 does not touch any `concepts/{eigsolve,ksp_solve}` page).
Rationale: item-3c carried lazy tail; applies the ratified non-node convention; collapses the residual untyped tail. **fan-out: LOW** (typed-DAG completion increment; sharpens the GC detritus-vs-live discriminator by removing untyped-warning noise).

## Overlap analysis

- **D1 ∩ D2:** D1 reads/audits `feature/{electrostatic,magnetostatic,eigenmode}.L4`, `L4/{fe_assemble,eliminate_bc}`, `concepts/{dofset,set_subvector_zero}`, `L3/divfree-projector`, `L1/set_subvector_zero`; writes AT MOST `L4/fe_assemble.md` frontmatter (path (a)) or nothing (path (c)). D2 writes 16 `concepts/*.md` frontmatter blocks: `build-time-vs-run-time-stratification`, `capability-typing`, `chebyshev-iteration`, `constructed-operator-factory`, `constructed-operators`, `convergence-test`, `counter-update`, `derived-view-hoisting`, `eigsolve`, `erasure-scope`, `ksp_solve`, `nested-constructed-operator-gate`, `rotation`, `solve-monad`, `solver-as-operator`, `state-stratification`. **The two write-sets are DISJOINT** — D1's only possible write is `L4/fe_assemble.md` (an `L4/` chapter, not a `concepts/` page); none of D1's read/write files appear in D2's 16 `concepts/` pages. **NON-OVERLAPPING → parallel.**
- No shared operator-name collision: D1 may add ONE edge naming `eliminate_bc`/`dofset` (existing stable slugs on disk); D2 authors `reference` lists naming existing stable slugs. No new-slug forward-reference either way → no cross-report slug-divergence risk.
- No consolidated-tally collision: neither dispatch touches a layer-index Working-Notes count or a §Vocabulary-cohort consolidated tally (D1 = at most one frontmatter edge; D2 = per-page frontmatter blocks). No shared-index aggregate written by ≥2 parallel agents.

## Sequencing schedule

**Single wave (WAVE-1, both parallel):** D1, D2. Disjoint write-sets; no forward-reference ordering dependency (D1's possible edge and D2's `reference` lists name only pre-existing stable slugs). The linter rescue/`untyped`-drop is verified by the finalize step-5b re-run.

## Open questions / caveats

- **The LEAD may legitimately land as a FINDING with ZERO edge added.** The strongest on-disk reading is path (c): `eliminate_bc`/`dofset`/`set_subvector_zero`/`divfree-projector` are firm vocabulary the driver columns deliberately abstract over (BC-elimination + divfree-projection are `models/`-level operator-construction machinery below the L4 column altitude), so they may be **correctly unreachable** — firm-but-absorbed detritus, not a gap to close with an edge. If D1 reaches that verdict, the WAVE-3 reachability follow-up CLOSES as a finding (recommend a baseline-exception ledger entry to the batch-34 meta-phase), and the dofset/set_subvector_zero OQs resolve as "no faithful rescue edge exists; classified known-firm-detritus." This is a fully acceptable outcome under faithful-path-or-finding — flagging so the integrator/critic does not read a no-edge D1 as a failed dispatch. Path (a) (`fe_assemble →post-composes eliminate_bc` liveness) is the most-plausible faithful rescue but reverses a documented separability-law-8 non-blocking stance, so it needs an explicit faithfulness argument the producer must make or decline.
- **`counter-update` node-vs-non-node in D2:** the batch-33 enactment reconciled `counter-update`→node (vs `chebyshev-iteration`→non-node), but `counter-update` is in the 16-page no-edges list. D2 applies the ratified per-page call: `counter-update` gets node treatment (`rank:` + `depends-on`) if it is a real DAG node, the other 15 get non-node `reference`-only. If the ratified disposition for `counter-update` is ambiguous on disk, D2 should default it to the most-conservative reading and flag for the next meta-phase rather than guess a `rank:`.
- **The cycle-106 finalize flagged a planner false-read** of the integrator-signals tail as "stale at cycle-019" (recorded for batch-34 meta-phase). This planner read the integrator-signals top section (cycle-106) directly and confirmed the unbroken chain — no append gap exists. If a signal-reading heuristic is keying off a stale anchor, that is a planner-tooling defect for the batch-34 meta-phase to examine, not an append-cadence problem. Noted here for the batch-34 meta-phase (fires after cycle-108).
- **Latent linter-reader bug** (`graded-stack-lint-block-mapping-misparse-on-legacy-edge-prose-colon`, c106 D5 OQ) remains routed to the batch-34 meta-phase (fix-reader-vs-rely-on-migration decision); not triggered by either dispatch this cycle (D1 authors at most one block-mapping edge; D2 authors `reference`-only lists, no `:`-bearing legacy qualifiers).
- **No fresh plan candidate appended** beyond what is already in the active head — both dispatches discharge existing item-1-residual + item-3c. The forward frontier stays exhausted; no `promotion_frontier`/obstruction-gated member proposed.
