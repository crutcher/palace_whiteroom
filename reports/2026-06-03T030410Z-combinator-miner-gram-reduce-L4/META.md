---
verifies: ../REPORT.md
critiqued_at: 2026-06-03T033000Z
critic_version: 1
checks:
  citation-validity: pass
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: pass
  cross-reference-integrity: pass
  edge-label-fidelity: pass
  plan-kind-consistency: pass
  skill-uptake-survey: pass
overall_status: ready
---

# META: verification of cycle-073 D1 — `gram_reduce` L4 combinator-as-entry

## Critique

### Checks run

**citation-validity — pass.** `tools/citecheck/citecheck.py --scan` reports 18 ok / 0 failing on the full CYCLE.md. I hand-anchored the load-bearing L0 pinpoints with `--anchor`: `electrostaticsolver.cpp:118-119` (`Mult`), `:139-140` (`Invert`), `magnetostaticsolver.cpp:105` (`PostprocessTerminals`), `:131` (`I_inc`), `:151-152` (`Invert`) — all resolve in-range. I then hand-read both `PostprocessTerminals` bodies (`electrostaticsolver.cpp:95-144`, `magnetostaticsolver.cpp:105-154`) and they confirm every structural claim the report makes: the `mfem::DenseMatrix C/M(V/A.size())` allocation, the `for i { diagonal Dot; for j>i { off-diagonal Dot }; copy lower triangle }` double loop, the diagonal `Dot` at `:119`/`:131` (the latter divided by `I_inc[i]*I_inc[i]`), the off-diagonal at `:126`/`:138`, the lower-triangle mirror at `:131-138`/`:143-149`, and the `Cinv(C);Cinv.Invert()` / `Minv(M);Minv.Invert()` tail at `:139-140`/`:151-152`. The two feature-chapter witnesses `feature/electrostatic.L4.md:40` and `feature/magnetostatic.L4.md:40` both literally read "there is no *new* L4 combinator here ... is a forward mine, not authored here" — exactly the premise the report cites them for. The report's self-flagged `:108→:105` magnetostatic call-site drift is correct: the actual call is at `:105` (the feature chapter's pre-existing `:108` is a separate, pre-existing drift in a chapter this report does not edit — noted as an issue below, not a fault of this report). This report is itself a YAML-clean producer with no `verified_against:` block, so that sub-check no-ops.

**surface-or-evidence — pass.** This is a new-entry authoring report (combinator-as-entry), not a refinement of an existing operator's surface, so the refinement-shape gate is satisfied trivially: the proposal authors a brand-new `book/src/L4/gram_reduce.md` chapter with full surface (Signature, Semantics, Algebraic-laws, Specialization) AND complete L0 + sibling-combinator + feature-witness evidence. Not a pure rotation_claim backfill.

**rotation-quality — pass.** The report makes a genuine compression claim, not a rename: two skeleton-identical C++ double loops (`electrostaticsolver.cpp:100-140`, `magnetostaticsolver.cpp:110-152`) collapse to ONE L4 `symmetric_from_upper`-over-upper-triangle-pairs fold parameterized by a per-entry weight closure `w(i,j)`, with the two output products distinguished ONLY by `w ≡ 1` vs `w = 1/(IᵢIⱼ)`. State (the explicit `Cm` mirror matrix, the lower-triangle copy loop, the workspace `D_gf`/`H_gf`) is hidden behind the combinator; the two-pipeline difference is reduced to one closure argument. This is the strictly-more-abstract / state-hiding direction, not a 1:1 mapping.

**variant-axis-coverage — pass.** The report enumerates four variant axes (normalization-weight, operator-source, element-type, family-index-domain) and dispositions each: normalization-weight is the one load-bearing structural axis (absorbed into the `w` closure), the other three are leaf-content absorbed into `K`/`xs`/`w`. The `element-type: real` pin is explicitly scoped (the eigenmode complex extension is named as a deferred future-witness axis value, not silently dropped). No hidden branches: the deferred eigenmode/driven pipelines are named DEFERRED with fold-vs-map flags per the mining-gate's step 3.

**cross-reference-integrity — pass.** Every `[link]` target resolves on disk: `L1/matrix-weighted-norm.md`, `L1/bilinear-form.md`, `L4/solve_family.md`, `L4/inner_product.md`, `L4/linear_combination.md`, `L4/fe_assemble.md`, `concepts/black-box-vs-accelerated-kernels.md`, `feature/electrostatic.L4.md`, `feature/magnetostatic.L4.md`. The self-authored coupled-pair link `[gram_reduce](./gram_reduce.md)` (used in the index row + the SUMMARY insertion) is correct for a same-report coupled pair (the chapter is authored in proposed-change (1)). Build-readiness fence guard: `grep -n '```'` shows 4 fences = 2 balanced blocks; block 1 (`gram_reduce.md`, 230→514) encloses the FULL body — `## Signature`, `## Semantics`, `## Algebraic laws`, `## Status`, `## Evidence` all sit inside the fence (all < 514), and the Haskell signature is rendered as INDENTED code (leading-space form, lines 305-322), so there is no nested-```text```-fence truncation hazard. Alpha-within-kind position verified against disk: L4/index.md Data-algebra rows are `dot`(95)/`fe_assemble`(96)/`inner_product`(97)/`nrm2`(99); inserting between `fe_assemble` and `inner_product` is correct (`fe_assemble < gram_reduce < inner_product`). SUMMARY.md Data-algebra sublist is `assemble_frequency_operator`(26)/`dot`(27)/`fe_assemble`(28)/`inner_product`(29)/`linear_combination`(30)/`nrm2`(31); the report's "after `SUMMARY.md:28`" (between `fe_assemble` and `inner_product`) is the correct alpha slot. The SUMMARY insertion IS flagged for the integrator (proposed-change (2), parenthetical at lines 526-530). Frontmatter convention matches neighbors (`firmness:`/`consumes:`/`lowers_to:`/`variant_axes:`, as in `fe_assemble.md`/`inner_product.md`).

**edge-label-fidelity — pass.** No L_{n+1}→L_n lowering-theme edge is authored (the report explicitly chooses the in-line-marker route, NO dedicated L4>L3 theme file, per the `inner_product`/`linear_combination` precedent). The §"Lowers to" prose discusses identity-in-form-on-the-body down to the L1 primitives it folds, consistent with that choice — no mislabeled edge.

**plan-kind-consistency — pass.** Declared kind is combinator-as-entry at `firmness: rough-in (test-coverage-bounded)`, and the content matches: full firm-shaped surface (signature, laws as syntactic identities) but gated to rough-in for two stated reasons (the folded L1 primitives are themselves rough-in; no dedicated Gram-reduction unit test). The status reasoning correctly contrasts against the firm-on-positive-structure escape (`fe_assemble`/`frequency_sweep`, whose folded primitives ARE firm) — the distinction is that `gram_reduce`'s primitives are rough-in, which on-disk is confirmed (`bilinear-form.md` frontmatter `firmness: rough-in`; `matrix-weighted-norm.md` `## Status: rough-in (test-coverage-bounded)`). No firm/rough-in placeholder mismatch. The combinator-as-entry vs. mine-and-strand call is correctly justified per the VOCABULARY-SHIFT redirect: the entry IS the combinator (replace-and-propagate), with the two output-product reductions demoted to §Specialization notes through it, not stranded as a side proposal.

**skill-uptake-survey — pass.** The report's shape (cross-pipeline combinator mining) has a matching skill, and the report references it explicitly: `disciplined-cross-pipeline-combinator-mining-gate` (cited at the §gate heading line 175 and run end-to-end at lines 175-217; the SKILL.md exists on disk). The 4-step gate (≥2 positive witnesses / classify break-witnesses / name deferred pipelines / replace-and-propagate) is walked. Telemetry present, no blocking concern.

### Issues found

No blocking or correctness issues. Three low-severity observations:

1. **(low / informational — out-of-scope for this report, a pre-existing drift this report inherits-but-does-not-edit)** `book/src/feature/magnetostatic.L4.md:40` cites the magnetostatic `PostprocessTerminals` call site at `magnetostaticsolver.cpp:108`, but the actual call is at `:105` (the def is `:110`; `:108` is the closing-brace region of the enclosing `Solve` method). This report correctly identifies and corrects the drift (uses `:105` throughout) and explicitly DEFERS editing the feature chapters to c074 (proposed-change (3)). The stale `:108` in the feature chapter is therefore a pre-existing defect the c074 re-anchor should sweep up — flagging here so it is not lost, NOT a fault of this report.

2. **(low / informational)** The §"Lowers to" frontmatter `lowers_to:` block (CYCLE.md:239-241) lists both L1 primitives as identity-in-form lowering targets, but the in-prose §"Lowers to" (CYCLE.md:436-446) and the index-row lowering cell correctly state there is NO dedicated L4>L3 theme (in-line-marker route). This is internally consistent (the `lowers_to:` frontmatter points at the L1 primitives the fold bottoms out in, the in-line-marker route is the *mechanism*), matching the `inner_product.md` frontmatter precedent where `lowers_to:` names the L3 target while the prose declares no theme file. No action needed; noting that the frontmatter target is an L1 (not adjacent-L3) entry, which is the deliberate non-adjacent in-line-identity convention for a fold whose body is identity-in-form across the L4→L1 span — consistent with the L3/L1 in-line identity convention, but a harvester firming this entry may want to confirm the adjacent-edge chain composes as claimed.

3. **(low / informational)** The Open-questions block (CYCLE.md:577-603) is rich (4 items) and the report states they are appended to `scaffolding/open-questions.md` "see below" — but the CYCLE.md does not contain the actual append block / proposed-change for open-questions.md (the integrator-per-report promotes OQs from the report's Open-questions section, so this is fine procedurally). Noting only that item 1 (the c074 feature-chapter re-anchor) and item 3 (the double-gated promotion coupling) are the load-bearing follow-ons the integrator should ensure land in the OQ ledger so the c074 sequencing decision is not lost.
