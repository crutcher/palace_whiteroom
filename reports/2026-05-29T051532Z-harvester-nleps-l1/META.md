---
verifies: ../REPORT.md
critiqued_at: 2026-05-29T053000Z
critic_version: 1
checks:
  citation-validity: pass
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: pass
  cross-reference-integrity: pass
  edge-label-fidelity: pass
  plan-kind-consistency: pass
  skill-uptake-survey: pass
repaired_at: 2026-05-29T054500Z
repairer_version: 1
repairs:
  citation-validity: repaired
  surface-or-evidence: not-needed
  rotation-quality: not-needed
  variant-axis-coverage: not-needed
  cross-reference-integrity: not-needed
  edge-label-fidelity: not-needed
  plan-kind-consistency: not-needed
  skill-uptake-survey: not-needed
overall_status: ready
follow_up_agent: null
---

# META: verification of "Formalize apply_nonlinear_pencil at L1"

## Critique

### Checks run

**citation-validity — pass.** Independently verified every cited Palace range via `palace-codemap read_range` against the live source. The crux positive site `QuasiNewtonSolver::GetResidualNorm` is **exact**: the function spans `nleps.cpp:807-821`, and its body is the term-by-term accumulation the report claims — `opK->Mult(x, r)`, `if (opC) opC->AddMult(x, r, l)`, `opM->AddMult(x, r, l*l)`, `auto A2 = (*funcA2)(std::abs(l.imag()))`, `A2->AddMult(x, r, 1.0)`, `return linalg::Norml2(comm, r)`. The pencil equation `T(λ) = K + λC + λ²M + A2(λ)` appears **verbatim in the source comment** at the head of this function (lines 810-811: `|| (K + λ C + λ² M + A2(λ)) x ||₂`) and again at `nleps.hpp:146` (class comment, immediately preceding the `class QuasiNewtonSolver` decl at `:147`) — both exact. The `BuildParSumOperator({1, λ, λ², 1}, ...)` sites are exact: `:496-499` (linear-solver setup), `:556-559` (the `compute_residual` lambda; `A->Mult(vv, rr)` at `:559` and the `if (k > 0)` deflation extension at `:560-570` — so the report's "bare apply is the `k == 0` branch at `:559`" claim is **literally correct**), `:729` (system-operator rebuild), and the Jacobian `BuildParSumOperator({0, 1, 2λ, 1}, ...)` at `:655` with `opJ->Mult(v, w)` at `:657` and the divided-difference `opAJ` build at `:653-654` (the `A2'` formula `(A2((1+δ)|Im λ|) − A2(|Im λ|))/(i·δ·|Im λ|)` is a faithful read: source `denom = (0, δ·|Im λ|) = i·δ·|Im λ|`, `opAJ = BuildParSumOperator({1/denom, -1/denom}, {opA2p, A2n})`). The closure type `SetExtraSystemMatrix(std::function<std::unique_ptr<ComplexOperator>(double)> A2)` at `:177-181`, the two `SetOperators` overloads at `:191`/`:221`, `eps.hpp:69-74`, and `nleps.hpp:232`/`:246` (Interpolation / NewtonInterpolationOperator) all resolve in-range. The report's own self-flagged "2-line drift on `807-821`, re-read and corrected" holds up — the range envelope is exact. See Issue 1 for a minor intra-range line drift (residual non-blocking) and Issue 2 for an `eps.hpp` closure-type imprecision.

**surface-or-evidence — pass (CRUX).** This is a new L1 operator (new file + dep-map row + SUMMARY register), not a refinement of an existing entry, so the surface-vs-evidence dichotomy applies as: does the operator carry genuine positive surface backed by in-range evidence? It does. The `firm` status rests on a **clean positive source site** — `GetResidualNorm` is a complete function whose body IS the term decomposition (law 3), read directly, not literature-reconstructed. The pencil form `T(λ) = K + λC + λ²M + A2(λ)` is genuinely in the source (verbatim comment + the five `BuildParSumOperator`/`AddMult` realizations), not reconstructed. The nonlinearity is fully and correctly localized in the opaque `A2` closure: `A2` is a `std::function<std::unique_ptr<ComplexOperator>(double)>` (`:177-181`), and the report treats it as an opaque `Real -> LinearOperator[N, N]` leaf — it does NOT crack `A2` open or assert anything about its internals, so no `partly-constructive` caveat is warranted on the apply. The "most foundational of the 4 candidates" claim is **verified against source**: the deflated residual's bare apply is literally the `k == 0` branch (`:559` precedes the `if (k > 0)` at `:560`); the deflated-solve operator `opA` is the same `BuildParSumOperator` pencil (`:498-499`); the Jacobian is the same construction over coefficient vector `{0, 1, 2λ, 1}` and an `A2'` closure (`:655`, law 5 substitution); the Newton update consumes the residual `u` (`:673-675`). The common-sub-expression argument is sound. See the dedicated status assessment below.

**rotation-quality — pass.** High→low discipline observed: the L1 entry is defined in L1 vocabulary (`apply_linop`, `axpby`/`axpbypcz`, `nrm2`, `NonlinearPencil[N]` opaque type), with the L1>L0 lowering forward-referenced as plain text ("L1>L0 lowering concern") and NOT embedded as a theme. The L1 form (pure-functional `r = apply_nonlinear_pencil(T, λ, v)`, no output buffer, no `A2`-caching, no build-form choice, damping as a `Maybe C` axis) is strictly more abstract / more equational than the L0 form (in-place `Mult`+`AddMult` accumulation into a reused buffer, conditional `if (opC)`, A2-caching across line search, five duplicated build sites). State-hiding (output buffer dissolved) + coarser substitution (two algebraically-identical L0 build forms collapse to one apply by law 3; the A2-representation axis collapses to one opaque closure) — a genuine mutation rotation, not a rename.

**variant-axis-coverage — pass.** Variant axes are classified per `classify-variant-axis` and each is either covered or explicitly scoped/absorbed: damping-present (`Maybe C`, grounded on the two `SetOperators` overloads `:191`/`:221` and the `if (opC)` guard at `:813-816` — verified); purpose/coefficient-vector (residual `{1, λ, λ², 1}` vs Jacobian `{0, 1, 2λ, 1}`, absorbed by laws 4-5 — verified both coefficient vectors in source); A2-representation (frequency-assembly vs Newton-polynomial-interpolation, collapsed to the opaque closure, grounded on `nleps.hpp:232`/`:246` — verified); L0-build-form (term-by-term `AddMult` vs `BuildParSumOperator`+`Mult`, collapsed by law 3 — both forms verified in source). No hidden branches: the `k > 0` deflation extension is explicitly deferred (see Issue 3, a coordination point not a hidden branch).

**cross-reference-integrity (fence guard) — pass.** **Fence-guard PASS**: the firm body `edit:book/src/L1/apply_nonlinear_pencil.md` opens at CYCLE.md:24 and closes with its ``` terminator at CYCLE.md:143, immediately after the last Evidence bullet (the no-test bullet, :142) — the body is fully enclosed; no cycle-019-style fence truncation. All `[link]` targets resolve on disk: `L1/apply_linop.md`, `L1/axpbypcz.md`, `L1/axpby.md`, `L1/nrm2.md`, `L1/eigsolve.md`, `L1/index.md`, `L0/eigensolver-wrapper.md`, `concepts/variant-absorption.md`, `SUMMARY.md` all exist. The cited `apply_linop` "law 5" (operator-sum distributes) exists at `apply_linop.md:54` — accurate. Sibling-coordination verified: the sibling report dir `reports/2026-05-29T051532Z-combinator-miner-deflate-gram` exists; the boundary is clean (combinator-miner owns the **L2** `deflate`/`gram` combinator; this owns the **L1** pencil-apply; the deflation extension `U(λ)v₂` is explicitly deferred to a follow-up so the L1/L2 boundary follows the combinator's needs). No duplication/overlap detected.

**edge-label-fidelity — pass.** Not a lowering-theme report (no `L_{n+1}→L_n` edge label on the proposal). The one structural-edge artifact is the dep-map row in `book/src/L1/index.md`: the proposed row places `apply_nonlinear_pencil` with deps `apply_linop` (direct, per-term), `axpby`/`axpbypcz` (transitive), `nrm2` (adjacent) — these match the Dependencies section of the body and the verified law-3 unfolding. The row's column shape matches the existing `assemble-diagonal` anchor row (`index.md:77`). Consistent.

**plan-kind-consistency — pass.** Declared kind is a firm L1 operator. The three proposed-changes blocks are well-formed and match a firm-operator shape: (1) a complete new L1 file (signature with named-axis shape contract, semantics with three load-bearing points, five algebraic laws + four explicit non-laws, dependencies, two live + two absorbed variant axes, status, L1-vs-L0 distinction, full Evidence list) — no rough-in placeholders; (2) a dep-map row matching the table schema; (3) a SUMMARY register entry. Content shape is firm-operator throughout, consistent with the declared `firm` status. The Open-questions correctly externalize follow-up work (deferred NLEPS pieces, concept-page candidates, layer-intro refresh) without leaving holes in the firm entry.

**skill-uptake-survey — pass (telemetry).** The report references invocation of the relevant skills: `verify-citation-range` (producer self-verification of all ranges at emit time, with the `807-821` re-read after initial drift), `classify-variant-axis` (the four-axis classification), and `find-tests-for-region` (the `search_text` over `test/unit/**` → 0 hits). MCP-first localization (`read_range`/`search_text`) used throughout per the codified convention. Uptake is appropriate to the report shape; nothing missing.

### Status-decision assessment (firm vs partly-constructive vs rough-in) — the crux

I concur that **`firm` is defensible**, and that the `partly-constructive` caveat is correctly NOT applied. Reasoning, independently checked against source:

- The structure is read from a positive site that is a complete, self-contained function (`GetResidualNorm`, `:807-821`), corroborated at four further positive sites — not assembled from negative anchors or literature. This is categorically distinct from the `eigsolve` `partly-constructive`/`rough-in` situation, which is gated on **convergence semantics** (the `PartialConverged`/iteration-count behavior that needs a test or literature). The pencil-apply laws (linearity in `v`, term decomposition) are **syntactic identities on the cited `Mult`/`AddMult` accumulation** — they do not depend on convergence behavior, so the missing NEP convergence test does not gate them.
- The `apply_linop`/`chebyshev-smoother` precedent (firm on structural grounds despite thin/absent dedicated tests) is the correct analogy, not the `eigsolve` precedent. The report makes this distinction explicitly and correctly in Status and in the third Open-questions/caveats bullet.
- The nonlinearity is correctly quarantined in the opaque `A2` closure (a genuine `std::function`), which the report reads-but-does-not-reconstruct. There is no positive claim asserted about `A2`'s internals, so the `partly-constructive` negative-anchor machinery is not triggered.
- The `rough-in (test-coverage-bounded)` fallback is explicitly flagged with "the laws stand either way," which is the right hedge. My recommendation to the repairer/integrator: **`firm` stands**; the missing NEP test should NOT downgrade it, because the operator's contract is operator-algebra, not iteration semantics. The no-test caveat is already recorded inline in the Status section and the dep-map row — that recording is sufficient.

### Issues found

1. **Minor intra-range line drift in Evidence/Semantics citations to `GetResidualNorm`** (severity: low; `CYCLE.md` §Semantics line 71, §Evidence line 131). The `807-821` range envelope is exact, but a few intra-body line pins are off by ~1: the report cites the `A2` assignment at both `:817` (§L1-vs-L0, line 126) and `:818` (§Evidence, line 131), and `linalg::Norml2` / `return` at `:819` — in the verified source, within the 807-821 function, `opM->AddMult(x, r, l*l)` is the line the report calls `:817`, the `auto A2 = (*funcA2)(...)` assignment follows it, and the `return linalg::Norml2(comm, r)` is the last body line (≈`:820`). The two self-cites for the `A2` line (817 vs 818) are internally inconsistent. All content remains in-range and the load-bearing claims are unaffected; this is a polish-level pin tightening.

2. **`eps.hpp:69-74` closure-type imprecision** (severity: low; `CYCLE.md` §Signature line 59, §Evidence line 138, §Status line 122). The report cites `eps.hpp:69-74` as corroborating "the `A2`-as-closure contract" and the complex-only element type. Verified: that overload IS at `:69-74`, but its `A2` parameter is `std::function<const ComplexOperator &(std::complex<double>)>` — a **complex-argument** closure returning a reference, whereas the operative closure the solver actually uses (`SetExtraSystemMatrix`, `:177-181`, and `NewtonInterpolationOperator::funcA2`, `nleps.hpp:235`) is `std::function<std::unique_ptr<ComplexOperator>(double)>` — a **real-argument** closure returning an owned pointer. The report's `A2 : Real -> LinearOperator[N, N]` signature and `|Im λ|` real-arg convention are correctly grounded on the latter (`:177-181`) and are right (all call sites use `(*funcA2)(std::abs(...imag()))`). But `eps.hpp:69-74` corroborates the *pencil shape* `(K + λM + A2)`, not the real-arg closure type — the two cited closures differ in argument type and return type. Suggest narrowing the `eps.hpp:69-74` citation's claim to "the abstract nonlinear-pencil shape and complex-only element type" and not "the `A2`-as-closure contract" (which is `:177-181`'s job). Non-blocking; the operative grounding is correct.

3. **`:729` characterized as "lagged-preconditioner refresh"** (severity: low; `CYCLE.md` §Semantics point 3 line 79, §Evidence line 136, §L1-vs-L0). The `{1, λ, λ², 1}` pencil rebuild verified at `:728-730` is the **system-operator `opA`** rebuild (feeding `opInv->SetOperators(*opA, *opP)`), where the actual preconditioner is the separately-built `opP = (*funcP)(...)`. Calling `:729` a "lagged-**preconditioner** refresh" slightly mislabels the role of the rebuilt object (it is the system operator, not the preconditioner). The load-bearing claim — that the `{1, λ, λ², 1}` pencil shape recurs here as a fifth occurrence — is **correct and verified**; only the role label is imprecise. Suggest "lagged system-operator refresh (`opA` rebuild at the committed `λ`)".

4. **Sibling combinator-miner report has no CYCLE.md yet — coordination claim is forward-looking** (severity: low; `CYCLE.md` §Supporting evidence line 181). The report states the sibling "owns the L2 `deflate`/`gram` combinator … (concurrent, no CYCLE.md yet)." Verified: the sibling dir exists but is empty of a CYCLE.md at critique time (no-shared-context invariant — I cannot inspect its content even if present). The deferral of the deflation extension `U(λ)v₂` to a follow-up is the right hedge and avoids pre-committing the L1/L2 boundary. No defect — flagging only that the non-overlap claim cannot be cross-verified from this report's context and rests on the producer's coordination note; the integrator should confirm no L1-vs-L2 duplication when both land.

---

## Repair

All four critic findings are `citation`/`label`/`informational` low-severity. Findings 1–3 are mechanical citation/label fixes within an already-exact range envelope (no claim moves, no surface authored); finding 4 is informational with no edit. Independently re-verified each pinpoint via `mcp__palace-codemap__read_range` against `palace/linalg/nleps.cpp` and `palace/linalg/eps.hpp` before editing. All 8 critic checks `pass`; the repairs only tighten internal line pins and role labels. `overall_status: ready`.

### Fixes attempted

- **Finding 1 — intra-range line drift in `GetResidualNorm` citations** (critic Issue 1; `citation`, low).
  - **Decision**: repaired.
  - **Action**: Re-read `nleps.cpp:807-821` via codemap. Confirmed exact body lines: `opK->Mult` :812, `if (opC)` :813, `opC->AddMult` :815, `opM->AddMult` :817, `auto A2 = (*funcA2)(...)` :818, `A2->AddMult` :819, `return linalg::Norml2` :820. Reconciled the inconsistent self-cites of the `A2`-assignment line to a single value `:818` (§Semantics point 1 was `:815`→`:818`; §L1-vs-L0 was `:817`→`:818`); corrected the `return linalg::Norml2` pin `:819`→`:820` (§Semantics line 71 + §Evidence line 131); corrected the term-by-term accumulation range `:812-818`→`:812-819` (the last `A2->AddMult` is at :819) at all five occurrences (§Semantics line 71, §L1-vs-L0 line 126, Algebraic-law 3, the bit-determinism non-law, and the L0-build-form variant axis); ordered the §Evidence pinpoint list so the `A2` assignment carries `(818)` and `A2->AddMult` carries `(819)`. Range envelope `:807-821` was already exact and untouched.
- **Finding 2 — `eps.hpp:69-74` closure-type imprecision** (critic Issue 2; `citation`, low).
  - **Decision**: repaired.
  - **Action**: Re-read `eps.hpp:57-86`. Confirmed the third `SetOperators` overload at `:69-74` takes a COMPLEX-argument closure `std::function<const ComplexOperator &(std::complex<double>)> A2`, distinct from the operative REAL-argument closure `SetExtraSystemMatrix(std::function<std::unique_ptr<ComplexOperator>(double)>)` at `nleps.cpp:177-181` that the `A2 : Real -> LinearOperator[N, N]` signature rests on. Clarified in §Signature (line 59), §Status (line 122), and §Evidence (line 138) that `eps.hpp:69-74` corroborates the pencil SHAPE + complex-only element type — NOT the real-arg closure contract, which is `:177-181`'s job. The signature itself was already correctly grounded on `:177-181`; only the corroborating-cite's claim scope was narrowed.
- **Finding 3 — `:729` role-label mislabel ("lagged-preconditioner refresh")** (critic Issue 3; `label`, low).
  - **Decision**: repaired.
  - **Action**: Re-read `nleps.cpp:720-735`. Confirmed `:729` rebuilds the system operator `opA = BuildParSumOperator({1, λ, λ², 1}, ...)`; the preconditioner is the separately-built `opP = (*funcP)(...)`, both then passed to `opInv->SetOperators(*opA, *opP)`. Relabeled "lagged-preconditioner refresh/update" → "lagged system-operator (`opA`) refresh" in §Summary (line 20), §Semantics point 3 (line 79), and §Evidence (line 136), noting the preconditioner `opP` is the separate `funcP` build; tightened the §Operator-content recap (line 161) from "the preconditioner build" → "the linear-solver system-operator build". The load-bearing claim (fifth occurrence of the `{1, λ, λ², 1}` pencil shape) was correct and is preserved.
- **Finding 4 — sibling combinator-miner coordination is forward-looking** (critic Issue 4; informational).
  - **Decision**: not-needed.
  - **Rationale**: Informational only; the critic flags that the non-overlap claim rests on the producer's coordination note and is confirmable only at land time (the L1 pencil-apply vs the sibling's L2 `deflate`/`gram` boundary). The deflation extension `U(λ)v₂` is already explicitly deferred in the report, which is the right hedge. No edit; the integrator confirms no L1-vs-L2 duplication when both this report and the deflate/gram report land this cycle.

### Unrepairable findings

None. All four findings were either mechanically repaired (1–3) or required no action (4, informational). No substantive authoring or content decisions were involved — every fix was a line-pin tightening or role-label correction within already-verified ranges.

## Suggested resolution

`ready`. All 8 checks pass; the firm-status crux is independently defensible (positive site `GetResidualNorm` :807-821 + four corroborating sites; nonlinearity quarantined in the opaque `A2` closure) and the fence-guard is clean. Notes for the integrator: (1) confirm no L1-vs-L2 duplication against the concurrently-integrating `combinator-miner-deflate-gram` report (the L1 pencil-apply vs L2 `deflate`/`gram` boundary — the deflation extension is deferred, so no overlap is expected); (2) the report flags follow-up edits to *other* entries (the `eigsolve.md` cross-reference and the `L1/index.md` cohort-count bump from 12→13) as out-of-dispatch — those are layer-intro-author/follow-up work, correctly externalized to Open questions, not blockers for this landing.
