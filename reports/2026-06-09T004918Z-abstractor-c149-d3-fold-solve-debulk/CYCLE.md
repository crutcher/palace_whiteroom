---
agent: abstractor
invoked_at: 2026-06-09T004918Z
scope: FINALIZATION de-bulk (skill finalization-debulk) — fold_solve family (3 files)
status: pending
integrated_at: 2026-06-09T010000Z
integration_commit: 0877522
integration_notes: "cycle-149 FINALIZATION de-bulk wave (D3). Applied (de-bulk ALREADY on disk; STAGED). fold_solve family (3 files): 6 process attributions -> 0. L4/fold_solve.md (frontmatter rank: firm) Status promotion-section DELETED, load-bearing Scope content LIFTED to Scope section; 2 dropped roll-up aggregate spans, all constituent pinpoints survive in Evidence. L3 partial-obstruction + L3-L2 firm Status sole-rank-carrier tokens PRESERVED. One-combinator classification + schedule-source variant axis kept. graded-stack baseline HELD EXACTLY (firm:224, partial-obstruction:4); build EXIT 0; step-5c/5d PASS."
inputs:
  - book/src/L4/fold_solve.md
  - book/src/L3/fold_solve.md
  - book/src/L3-L2/fold-solve-time-step-body.md
  - skills/finalization-debulk/SKILL.md
  - book/src/L4/krylov_step.md (exemplar)
---

# CYCLE: FINALIZATION de-bulk — fold_solve family

## Summary

Cycle-149 FINALIZATION de-bulk wave, dispatch D3. Applied the `finalization-debulk` skill to the
`fold_solve` family (3 files). Stripped process/judgment accounting — the first-person "I did NOT
force…" classification narrative, the "batch-18 size-judgment", the "(batch-18+)" deferral framing,
the "deferred/gated on the cap's OQ `fold-solve-greedy-schedule-source-generalization`" process
pointers, and the firmness-justification framing ("the firm-on-positive-structure escape/basis",
"so the entry is `firm`") — while KEEPING the load-bearing static spec content: the
**ONE-combinator classification decision** and the **`schedule-source` variant axis**
(fixed-list / state-generated; transient / SweepAdaptive / AMR). Every citation preserved; every
rank/status token preserved (sole-rank-carrier subtlety respected); no node/edge/rank/status/semantics
move. Graded-stack lint baseline HELD EXACTLY.

This is a direct-edit finalization wave (book de-bulk), not a theme sketch — no proposed-changes
fence, no SUMMARY/dep-map registration (no graph mutation). Edits applied in place.

## Per-file: tags before → after

### `book/src/L4/fold_solve.md` (2 prose tags + firmness-framing)
- **Before** (process tags): line 115 OQ-slug `fold-solve-greedy-schedule-source-generalization` in
  a non-law; line 127 first-person "I did NOT force…" + "batch-18 size-judgment per OQ …"; line 164
  "deferred to OQ … (batch-18+)"; plus firmness-framing "firm-on-positive-structure basis/escape" at
  lines 102, 197, and the entire `## Status` firm-promotion-history section (lines 169–173).
- **After**: 0 process tags. The `## Status` section (frontmatter `rank: firm` ⇒ delete promotion
  prose entirely) was DELETED; its load-bearing **Scope** content (which pipelines are folds vs maps,
  the `schedule-source` generalization scope) was LIFTED into a new `## Scope` section, stripped of the
  OQ-pointer. The OQ-slug / batch-18 / first-person tokens removed; the firm-justification framing
  removed (laws still stated as read-off syntactic identities — the static fact kept).
- Citations: see below (only the two AGGREGATE-span tokens in the deleted prose dropped; all their
  constituent pinpoints survive in `## Evidence`).

### `book/src/L3/fold_solve.md` (1 prose tag + firmness-framing)
- **Before**: line 145 "gated on the cap's OQ `fold-solve-greedy-schedule-source-generalization`,
  batch-18"; plus firmness-framing "firm-on-positive-structure basis/escape" at lines 108, 188, 193,
  and the `## Status` tail "so this is not `rough-in (test-coverage-bounded)`".
- **After**: 0 process tags. The `## Status` LEADING TOKEN `partial-obstruction` (frontmatter
  `firmness: partial-obstruction`; non-firm ⇒ keep concise static unresolved-state + obstruction
  sub-kind) PRESERVED; only the firm-justification tail stripped. The OQ/batch-18 pointer restated as
  "(whether the SweepAdaptive dissolution warrants its own chapter is unsettled)". The Evidence
  cross-ref to L4 retargeted `§Status` → `§Scope` (the section it now points at) and de-tagged "(firm)".

### `book/src/L3-L2/fold-solve-time-step-body.md` (3 prose tags)
- **Before**: line 96 "gated on the cap's OQ … (batch-18)"; line 105 "the state-generated superset,
  batch-18-gated"; line 162 "gated on the cap's OQ … (batch-18)".
- **After**: 0 process tags. This file has NO frontmatter — its `## Status` LEADING TOKEN `firm`
  (line 158) is the SOLE rank carrier; it was NOT touched. The line-162 edit is in the Scope
  sub-paragraph BELOW the firm token. All three OQ/batch-18 pointers restated statically
  ("whether it warrants dedicated treatment is unsettled" / "outside this theme's scope" /
  "Cross-pipeline generality is not claimed beyond…").

**Residual-tag scan after edits**: `grep -nE "batch-[0-9]+|cycle-[0-9]+|c[0-9]{3}|OQ |I did NOT|
this dispatch|self-verified|verified_against|reports/|firm-on-positive|gated on the cap|deferred to OQ"`
over all 3 files → **0 matches** (grep EXIT 1).

## Citations: before → after (match)

| file | unique cite-lines HEAD | now | internal .md links HEAD | now |
|---|---|---|---|---|
| L4/fold_solve.md | 54 | 52 | 10 | 10 |
| L3/fold_solve.md | 23 | 23 | 15 | 15 |
| L3-L2/fold-solve-time-step-body.md | 21 | 21 | 10 | 10 |

L3 + L3-L2: citation and link counts IDENTICAL.

L4 −2: the two dropped tokens are `transientsolver.cpp:33-99` and `drivensolver.cpp:231-398` — both
**aggregate-span** citations that appeared ONLY in the deleted `## Status` firm-promotion prose
("two confirmed fold witnesses — transient `…:33-99` … driven-PROM SweepAdaptive `…:231-398`"). Every
constituent PINPOINT they summarized survives verbatim in `## Evidence`: `transientsolver.cpp:33,35,36,
77,89,93,98,99` (10 hits) and `drivensolver.cpp:73,231,241-244,384,389,398` (the witness-2 block is
byte-identical HEAD vs working). **No grounding source range is lost** — only the prose-narrative
roll-up spans went with the deleted promotion narrative. This is the skill-sanctioned outcome
(KEEP rule: pinpoint citations preserved; the deleted prose's summary spans are not separate grounding).
All internal `[..](..)` links: 0 lost across all 3 files.

## Graded-stack lint: baseline HELD

`python3 tools/graded-stack-lint/graded_stack_lint.py --book-src book/src`:

```
files scanned:        392
typed nodes:          331
untyped (WARNING):    61
rank histogram: {typed-no-rank: 90, firm: 224, rough-in: 4, partly-constructive: 3,
                 obstruction: 2, partial-obstruction: 4, roadmap_goal: 4}
RESULT: 0 rank violation(s), 123 detritus (51 true-detritus / 72 reference-reachable §2g), 61 untyped
```

Matches the required baseline EXACTLY: `files=392, typed=331, untyped=61, rank_violations=0,
unresolved_depends_on_targets=0, promotion_frontier=11, detritus=123, true_detritus=51`. Rank
histogram unchanged (`firm: 224`, `partial-obstruction: 4` — the L3 partial-obstruction node intact).
No graph mutation (prose + `## Status`-section editing only).

## How the ONE-combinator / variant-axis content was restated statically

The load-bearing classification decision and the `schedule-source` variant axis were KEPT in full; only
their first-person/process framing was rotated to static statements:

- **First-person → static** (L4 §Specializations, was line 127): "I did NOT force the fixed
  `foldl-over-[Time]` signature onto SweepAdaptive…" → "The fixed `foldl-over-[Time]` signature is NOT
  forced onto SweepAdaptive (it exhibits no fixed `[Time]`): the fixed-list signature is the *default
  surface*, with the state-generated form rendered through the §3.7 carry." The "**Classification
  decision (load-bearing): ONE combinator** — the §3.7 carry form covers both; the schedule-source is
  the recorded variant axis, not a distinct entry" sentence is KEPT verbatim (it is the static FACT of
  what the combinator IS).
- **Unsettled-sub-decision, process-pointer stripped**: "(If the state-generated greedy form proves to
  warrant its own chapter …, that is a batch-18 size-judgment per OQ `…`; the default here is
  notes-in-entry)" → "Whether the twice-witnessed state-generated greedy form warrants its own chapter
  (once a downstream PROM consumer pulls on it) is unsettled; the static default is notes-in-entry per
  combinator-as-entry." The genuine non-firm sub-decision survives as a static unsettled-state note,
  WITHOUT the batch-NNN / OQ-slug bookkeeping (exactly as the dispatch directed).
- **Variant-axis FACT kept** (L4/L3 §Variant axes, L3-L2 §Scope): the `schedule-source`
  (`fixed-list | state-generated`) axis, the fixed-list=transient default surface, and the
  state-generated witnesses (driven-PROM SweepAdaptive + AMR Solve→Estimate→Mark→Refine,
  `basesolver.cpp:190`) — all retained with their citations. Only "deferred to OQ … (batch-18+)"
  framing was rotated to "is unsettled".
- **Firmness-justification → plain static**: "the firm-on-positive-structure basis/escape … so the
  entry is `firm`" tails dropped; the underlying static facts ("Every law is a syntactic identity on
  the read fold", "No dedicated test … does NOT gate the laws") kept, since firmness now lives in
  frontmatter `rank:`.
- **Coupling LIFT**: the deleted L4 `## Status` firm-promotion section's load-bearing **Scope** content
  (fold-spine witness scope; which pipelines are folds vs the `solve_family` maps vs the opaque
  `eigsolve`) was lifted to an explicit `## Scope` chapter component (the skill's LIFT model), not
  deleted.

## Supporting evidence

- Baseline confirmed pre- and post-edit identical (`graded_stack_lint.py`).
- L4 dropped-token analysis: `diff` of sorted-unique citation tokens HEAD vs working showed exactly
  `transientsolver.cpp:33-99` and `drivensolver.cpp:231-398` (both aggregate spans from the deleted
  `## Status` prose); all constituent pinpoints confirmed surviving in `## Evidence`.

## Open questions / caveats

- The L4 frontmatter `variant_axes:` documentation comment (line 20) retains the phrase "now
  twice-witnessed" and "the recorded generalization". These are static factual axis-documentation (no
  batch/cycle/OQ token) and were left intact per the skill's KEEP rule (frontmatter `variant_axes` is
  spec documentation, not `## Status` process prose). If a later pass wants the frontmatter prose
  tightened, "now twice-witnessed" → "twice-witnessed" is the only candidate; it is a static fact, so
  no change was made here.
- The standalone OQ ledger entry `fold-solve-greedy-schedule-source-generalization` in
  `scaffolding/open-questions.md` is untouched (scaffolding is the process workshop, out of de-bulk
  scope); the book chapters no longer point AT it by slug, but the OQ ledger remains the live home for
  the genuine unsettled sub-decision (now stated statically in the chapters as "unsettled").
