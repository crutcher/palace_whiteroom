---
verifies: ../CYCLE.md
critiqued_at: 2026-06-06T234500Z
critic_version: 1
checks:
  citation-validity: pass
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: pass
  cross-reference-integrity: pass
  edge-label-fidelity: pass
  plan-kind-consistency: pass
  skill-uptake-survey: pass
overall_status: ready
---

# META: verification of L1>L0 theme `fe-space-hierarchy-construction-rotation`

## Critique

### Checks run

**citation-validity — pass.** `citecheck --scan` on the report reports **22 ok, 0 failing**. Spot-checked the load-bearing pinpoints with `--anchor` and confirmed each on-disk by direct `Read` of `reference/palace/palace/fem/multigrid.hpp` and `fespace.hpp`: the whole body spans `multigrid.hpp:78-126` with `ConstructFiniteElementSpaceHierarchy` opening at `:78` (anchor ✓), `coarse_mesh_l` at `:87-88`, seed ctor `make_unique<FiniteElementSpace>(...)` at `:89-90` (anchor ✓ at `:90`), dbc block `:92-101`, h-loop body `:104-112` (`AddLevel` at `:106`), p-loop body `:115-123` (`AddLevel` at `:117`), `return fespaces;` at `:125`, close brace `}` at `:126` — all confirmed. `fespace.hpp` ranges confirmed: class `:200-286`, `fespaces` vector `:203`, `P` `:204`, `BuildProlongationAtLevel` `:206`, seed ctor `:210-213` (anchor `FiniteElementSpaceHierarchy` ✓), `AddLevel = push_back + nullptr slot` `:217-221` (anchor `AddLevel` ✓ at `:217`), `GetNumLevels` `:215`, `GetFinestFESpace` `:236-247`. The report's `:124-126` close-brace claim is correct. **One benign note (not a defect):** the `--anchor "h-refinement"` / `"p-refinement"` checks resolve to the *comment* lines `:103` / `:114` (`// h-refinement.` / `// p-refinement.`), one line above the cited loop-body ranges `:104-112` / `:115-123`. This is NOT a drift error — the report cites the loop *body* (deliberately, not the comment) and explicitly flags this in Supporting evidence ("the h-loop comment is at `:103`, the loop body at `:104-112`"). On-disk `:103` is the comment, `:104` is the `for`, body runs through `:112`; the body-range citation is correct and intentional. The `verified_against`/`## Verified-against` payload is prose-bodied (no `note:`-leading-quote round-trip hazard).

**surface-or-evidence — pass.** This is a NEW firm L1>L0 theme (a refinement-shaped proposal that creates surface) with full rotation evidence: the L0 driver range `multigrid.hpp:78-126` + the `fespace.hpp:200-286` `AddLevel`/seed-ctor anchors + the firm L1 endpoint `fe_space_hierarchy` (c117). The surface (new theme chapter text, the L1-chapter `lowers-to` edge, the index/SUMMARY rows) is present and backed. **Record-definition sub-check:** the theme names the `FiniteElementSpaceHierarchy` record in the result type but does NOT introduce a signature-named record needing a new home — the record's definition home is the in-chapter `## Record definition` table in `book/src/L1/fe_space_hierarchy.md:118-138` (single-consumer, fields `fespaces`/`P` with types/meaning/stratum + L0 backing class). The theme correctly *references* that home and defers prolongation/interpolator reads to it, rather than re-defining the record — exactly the USE+LINK posture. No undefined-record gap.

**rotation-quality — pass.** This is a GENUINE vocabulary translation, not a 1:1 rename. The L1 form is a single declarative `foldl AddLevel seed refinements`; the L0 form reorganizes that one fold into four distinct concrete pieces: (a) `coarse_mesh_l` start-index arithmetic (`= max(0, mesh.size()+fecs.size()−1−max(1,mg_max_levels))`, an index computation with no L1 surface — absorbed into the fold's start offset), (b) a seed ctor that is itself `AddLevel(seed)`, (c) **two axis-specialized imperative loops** over two *independent* refinement axes (h: finer meshes at fixed coarsest `fecs[0]`, confirmed `:106` `*mesh[l], fecs[0]`; p: finer collections at fixed finest `mesh.back()`, confirmed `:117` `*mesh.back(), fecs[l]`), and (d) an optional per-level Dirichlet `GetEssentialTrueDofs` interleave. The single declarative fold ⟶ index-arithmetic + seed + two-axis loop split is a real organizational/representational shift (the imperative form makes the concatenation `h-steps ++ p-steps` explicit as two back-to-back loops, and surfaces a `coarse_mesh_l` skip the fold has no operation for). Not degenerate.

**variant-axis-coverage — pass.** The two orthogonal axes are both covered and tabulated (§Variant axis): the refinement-axis (seed / h-step / p-step, 3 cases) and the Dirichlet-presence axis (the optional `dbc_attr && dbc_tdof_lists` block toggling the per-level `emplace_back`). The variant table's "held fixed / indexed / AddLevel body / anchor" columns match the source (seed `:89-90`; h `:104-112` AddLevel `:106`; p `:115-123` AddLevel `:117`). MPI/`Par*` is scoped out once per CLAUDE.md §Scope. No hidden branch.

**cross-reference-integrity — pass.** All `[link]` targets resolve on disk: the three delegated siblings `fe-space-construction-rotation.md` / `fe-collection-construction-rotation.md` / `essential-dofs-construction-rotation.md` (c064/c065/c066) all exist; `L1/fe_space_hierarchy.md` exists (c117, firm on disk); `L1/fe_space.md` / `fe_collection.md` / `essential_dofs.md` all exist. The index.md `edit:` inserts the new row in correct alphabetical position within the **Construction-rotation** by-kind group (`fe-space-construction-rotation` < `fe-space-hierarchy-construction-rotation` < `weak-form-term-rotation`, placed after the fe-space row). The SUMMARY.md `edit:` inserts between `fe-space-construction-rotation` (:269) and `weak-form-term-rotation` (:270) — correct alpha placement. The `FiniteElementSpaceHierarchy` record reference resolves to the current in-chapter home (`fe_space_hierarchy.md:118-138`). No build-readiness fence concern: the theme body is fully enclosed in the `new:` fence; the `## Status` firm apparatus sits inside the block.

**edge-label-fidelity — pass.** The proposal adds one `depends-on (lowers-to)` edge from `L1/fe_space_hierarchy` → `L1-L0/fe-space-hierarchy-construction-rotation` and the theme declares the reciprocal `lowers-to` to `L1/fe_space_hierarchy` plus two `cites-evidence` edges. The edge label is L1>L0 and the prose discusses exactly the L1→L0 lowering of the `AddLevel`-fold into `ConstructFiniteElementSpaceHierarchy` — the edge direction and the narrated edge match. **RE9 invariant — verified clean (special attention).** The added `lowers-to` edge is OUTBOUND from the OP (the op depends-on its lowering theme); the theme's `lowers-to` is OUTBOUND from the theme. Neither creates an *inbound consumer* edge into `fe_space_hierarchy` from a reachable node, so the OP stays RE9 baseline-excepted (the unbuilt geometric-multigrid preconditioner is still its only consumer). D2 grounds the THEME home only; it does NOT force op reachability. The report states this correctly (CYCLE.md §Open-questions "RE9 note" + final §Open-questions). Confirmed against `scaffolding/graded-stack-baseline-exceptions.md` RE9.

**plan-kind-consistency — pass.** Declared kind is a firm L1>L0 theme; content matches — full LHS/RHS, variant table, applicability conditions, justification kind, verified-against, no rough-in placeholders. **Graded-stack rank-invariant (Axis-1):** the theme claims `rank: firm` resting on `depends-on (lowers-to)` to `fe_space_hierarchy` (firm, rank 3, confirmed `rank: firm` on disk) and two `cites-evidence` (rank-terminal L0 ground truth). The constituents `fe_space` / `fe_collection` are both `rank: firm` on disk. Well-foundedness `rank(theme) ≤ min(deps)` holds at firm/firm. The reverse coupled edge (`fe_space_hierarchy` firm → theme firm) likewise holds. **firm-vs-partly-constructive judgment:** firm-on-positive-structure is justified — the entire body is read from one positive site (`multigrid.hpp:78-126`), every law is a syntactic identity / fold-invariant on positive source, and the only read-as-given pieces (`GetEssentialTrueDofs` dof-tail = MFEM-owned, delegated to the essential-dofs sibling; lazy `P[l]` prolongation = property of the result record, sibling-pull-gated) are NOT constructed sub-parts materialized from negative anchors — so this is firm, not partly-constructive. The no-dedicated-`test-multigrid.cpp` caveat is correctly non-gating (syntactic-identity laws, `fe_space`/`fe_collection`/`fe_assemble`/`essential_dofs` precedent). **Reachability (Axis-2):** the theme is a lowering-home grounding for an RE9-excepted op — correctly does not claim to ground op reachability.

**skill-uptake-survey — pass.** The report's shape (citation-heavy L1>L0 theme with on-disk close-brace verification) implies the citation-range / anchor verification procedure, and the report references its uptake (`citecheck --anchor` + on-disk `Read` of `:124-126` in §Verified-against and §Supporting-evidence). Telemetry only; non-blocking.

### Issues found

None. All 8 checks pass; the two graded-stack additions (rank-invariant, reachability) also pass. The RE9 baseline-exception invariant is respected (the theme grounds the lowering home without forcing an inbound consumer edge onto `fe_space_hierarchy`), the firm-on-positive-structure status is justified over partly-constructive, the rotation is a genuine vocabulary shift (fold → index-arithmetic + seed + two-axis loops), and the `FiniteElementSpaceHierarchy` record reference resolves to its current in-chapter definition home. `overall_status: ready` set per the all-pass clean-report rule.
