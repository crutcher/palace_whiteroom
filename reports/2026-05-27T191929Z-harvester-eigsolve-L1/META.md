---
verifies: ../CYCLE.md
critiqued_at: 2026-05-27T193500Z
critic_version: 1
checks:
  citation-validity: warning
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: pass
  cross-reference-integrity: pass
  edge-label-fidelity: pass
  plan-kind-consistency: pass
  skill-uptake-survey: pass
repaired_at: 2026-05-27T194500Z
repairer_version: 1
repairs:
  citation-validity: repaired
  surface-or-evidence: not-needed
  rotation-quality: not-needed
  variant-axis-coverage: repaired
  cross-reference-integrity: not-needed
  edge-label-fidelity: not-needed
  plan-kind-consistency: repaired
  skill-uptake-survey: not-needed
overall_status: ready
follow_up_agent: null
---

# META: verification of harvester-eigsolve-L1 rough-in

## Critique

### Checks run

**citation-validity (warning)** — Spot-checked ~25 source-line citations against the Palace tree. The vast majority verify exactly: `eps.hpp:22-141` (full class span), `eps.hpp:25-29` (`ScaleType`), `eps.hpp:31-42` (9-way `WhichType`), `eps.hpp:44-49` (`ErrorType`), `eps.hpp:57-74` (3-way `SetOperators` overloads), `eps.hpp:92-99` (`SetLinearSolver`/`SetDivFreeProjector`/`SetBMat`), `eps.hpp:116`/`:119`/`:122` (single-line virtuals), `eps.hpp:130-132` (eigenvector-normalisation docstring), `arpack.cpp:263-358` (`SolveInternal`), `arpack.cpp:300-304` (TARGET_REAL/TARGET_IMAGINARY `MFEM_ABORT`), `arpack.cpp:513-560` (`ArpackEPSSolver::Solve`), `arpack.cpp:569-580` (`ApplyOp` with host-pointer comment + shift/non-shift branches), `slepc.cpp:687-709` (`SlepcEPSSolverBase::Solve` with `EPSSolve`/`EPSGetConverged`), `slepc.cpp:711-716` (`GetEigenvalue` returning `l * gamma`), `slepc.hpp:57-67` (8-way `ProblemType`), `slepc.hpp:69-80` (9-way `Type`), `nleps.cpp:351` (`QuasiNewtonSolver::Solve` signature), `nleps.hpp:166` (`linear_eigensolver_` member), `modeeigensolver.cpp:470`/`:477`/`:484-492`/`:472-475`, `waveportoperator.cpp:524`, `drivers/eigensolver.cpp:86`/`:134-189`/`:172-189`/`:291-315`/`:365-374`/`:377-407`, `ksp.cpp:301-307` (`Mpi::Warning` region). However, two citation problems surface: (1) the non-laws section claims `palace/drivers/eigensolver.cpp:480-...` shows "different driver-side sort strategies" — line 480 is the closing namespace brace (the file ends at 480) and the driver does **not** sort eigenvalues at all (grep on `sort\|perm` returns only `modeeigensolver.cpp` hits). This citation is materially wrong. (2) The report self-asserts in Supporting evidence "All cited line ranges are concrete (no ellipsis ranges), per the cycle-008 bundle-4 repair pattern", but two distinct cites use ellipsis ranges: `palace/linalg/nleps.cpp:351-...` (appears in Evidence list and in §Context's "Direct-Newton" bullet) and `palace/drivers/eigensolver.cpp:480-...` (the wrong-line-number one above). The `nleps.cpp:351-...` ellipsis is at minimum sloppy; the `eigensolver.cpp:480-...` one is both ellipsis AND wrong-end-of-file.

**surface-or-evidence (pass)** — Refinement-shaped proposal carrying both surface (new file `book/src/L1/eigsolve.md`, diffs to `book/src/L1/index.md` and `book/src/SUMMARY.md`) and a rotation_claim (in-place `EigenvalueSolver::Solve() → int` + per-pair extraction virtuals rotated to pure-functional `eigsolve(E, control) → EigResult[N, K_max]` record-returning form). Both arms present; not a pure retroactive-evidence backfill. Passes.

**rotation-quality (pass)** — The L1 form is strictly more abstract / more equational than the L0 form. Compaction: (a) three workspace tensors (`mutable ComplexVector x1, y1, z1`) erased; (b) three separate per-pair extraction virtuals (`GetEigenvalue(i)`, `GetEigenvector(i, x)` with out-parameter, `GetError(i, type)`) collapsed into a single `EigResult` record; (c) the count-vs-request comparison the L0 caller has to do (`palace/drivers/eigensolver.cpp:367-374` does this implicitly) is hoisted into the sum-typed `EigStatus` field; (d) 3-way orchestration axis (RCI / shell-matrix / Newton) collapsed into the opaque `EigSolver[problem]` type; (e) 9-way SLEPc internal-method axis and 8-way SLEPc problem-type axis also absorbed; (f) driver-side concerns (`BlockTimer`, `Mpi::Print` convergence log, cumulative inner-solver counters) shed. State hiding + threaded-state compression + variant absorption — substantive abstraction, not renaming. The partial-convergence distinguishing semantic (`PartialConverged` as a structurally-distinct status case vs `ksp_solve`'s converged-or-not binary) is a load-bearing rotation refinement. Passes.

**variant-axis-coverage (pass)** — Five preserved axes (problem-type / spectrum-target 9-way / spectral-transformation / scaling / initial-space-seed) + three collapsed axes (orchestration-pattern 3-way / SLEPc Type 9-way / SLEPc ProblemType 8-way) + three explicit out-of-scope items (real-element, per-pair iterative refinement, MPI) are all enumerated with rationale per axis. Per the dispatcher's flagged check against CLAUDE.md unimplemented-Palace-stub policy: all three collapsed axes are **implementation-realized** in Palace (all three orchestrations are live code; SLEPc's Type/ProblemType sub-axes are vendored-SLEPc functionality, used live). This is a transparent-dispatch collapse parallel to `ksp_solve`'s CG/GMRES/FGMRES collapse, not an unimplemented-stub collapse. The stub-policy applies cleanly to ARPACK's `MFEM_ABORT` on `TARGET_REAL`/`TARGET_IMAGINARY` (which the report defers to "constructor's responsibility to reject" rather than documenting as an L1>L0 obstruction theme — see "Issues found" §3 — but that's a minor scope question, not a coverage gap). Passes.

**cross-reference-integrity (pass)** — All `[link]` references resolve. Verified: `L0/eigensolver-wrapper.md` (cycle-008 bundle-4 anchor), `L1/ksp_solve.md` (cycle-007 firm), `L1/apply_linop.md`, `L1/dot.md`, `L1/nrm2.md`, `L1/axpy.md`, `L1/axpby.md`, `L1/scal.md`, `L1/axpbypcz.md`, `L1-L0/ksp-solve-mutation-rotation.md`, `L1-L0/apply-linop-mutation-rotation.md`, `L1-L0/axpby-mutation-rotation.md`, `L1-L0/axpbypcz-mutation-rotation.md`, `L1-L0/minres-iteration.md`, `concepts/constructed-operators.md`, `concepts/variant-absorption.md`, `concepts/constructed-operator-factory.md`, `concepts/solver-as-operator.md`, `concepts/solve-monad.md`, `design/l4_calculus.md`. The OQ slug `eigsolve-l1-operator-rough-in-candidate` resolves to `scaffolding/open-questions.md:1342-1351` (frontmatter range exactly matches). The four newly-introduced OQ slugs (`eigsolve-linear-solve-failed-status-anchor`, `eigsolve-scaling-coordinate-convention`, `eigsolve-initial-space-axis-placement`, `eigsolve-iteration-count-result-field`) are forward-looking ledger entries; they are new and do not need to pre-exist. The diff target line ranges (`book/src/L1/index.md` 29-38 firm cohort, 51-66 dep-map, 68-76 working notes; `book/src/SUMMARY.md` insertion at line 40 after `ksp_solve`) match the current state of those files. Passes.

**edge-label-fidelity (pass)** — The proposal is an L1 operator entry, not a lowering-theme edge entry, so there is no L_{n+1}→L_n edge label to verify; the chapter title and slug consistently refer to L1 `eigsolve` throughout. Where the prose touches the L1>L0 boundary (e.g., "the L1>L0 lowering reintroduces the per-orchestration body"), the direction is correctly stated as L1→L0 reintroduction-of-mutation, parallel to the cycle-008 `ksp-solve-mutation-rotation` precedent. Passes.

**plan-kind-consistency (pass)** — The dispatch was scoped as "Harvester on `eigsolve` @ L1 — rough-in or firm depending on pre-check". The pre-check verdict (no dedicated `test-eigensolver.cpp`; narrow indirect coverage via `test-boundarymodeoperator.cpp` only — verified independently: `ls reference/palace/test/unit/` shows zero files matching `*eigen*` / `*eps*` / `*arpack*` / `*slepc*`; grep on test-boundarymodeoperator.cpp confirms 3 `TEST_CASE` macros + `ModeEigenSolver` instantiation with `LARGEST_REAL`) supports the rough-in classification. The Status section explicitly captures the rough-in tag with `(test-coverage-bounded, cycle-009)`, lists promotion-to-firm gates (test coverage expansion OR literature anchoring), and the dep-map row's status field uses the verbose form `rough-in (test-coverage-bounded, harvested-by: harvester:2026-05-27T191929Z-harvester-eigsolve-L1)` consistent with existing rough-in entries' status format. Write-authority discipline honored (proposed-changes channel only; no in-place artifact mutation). Passes.

**skill-uptake-survey (pass)** — The report's shape (L1 operator authoring with explicit test-coverage pre-check + variant-axis enumeration) plausibly intersects `verify-citation-range`, `classify-variant-axis`, and `verify-refinement-surface`. None are explicitly invoked-by-name in CYCLE.md, but the substance is present: the Pre-check section performs an explicit citation-range check pattern, the Variant axes section performs an explicit axis-classification pattern with preserved/collapsed/out-of-scope partitioning, and the surface+evidence two-arm structure of the proposal aligns with the refinement-surface skill's discipline. Pure presence-check telemetry; not blocking.

### Issues found

1. **[citation-validity, severity: major]** `palace/drivers/eigensolver.cpp:480-...` cited in §"Laws that explicitly do not hold" → "Eigenvalue ordering" bullet (CYCLE.md line 170) as evidence for "two different driver-side sort strategies". This citation is materially incorrect: (a) the file ends at line 480 (closing `}  // namespace palace` brace at line 479; line 480 is blank/EOF region), and (b) the driver does not sort eigenvalues at all — `grep -n "sort\|perm" drivers/eigensolver.cpp` returns no hits. The driver only invokes `eigen->GetEigenvalue(0)` (line 369) for the "first =" print and `eigen->GetEigenvalue(i)` (line 427) in postprocessing. The sole sort strategy in Palace's eigenmode pipeline is at `modeeigensolver.cpp:484-492` (correctly cited elsewhere). Either remove this claim, drop the count "two", or substitute the correct second site (e.g., wave-port reordering at `waveportoperator.cpp` if any, or just acknowledge `modeeigensolver.cpp:484-492` as the unique driver-side sort).

2. **[citation-validity, severity: minor]** Two ellipsis-range citations contradict the report's self-claim in §Supporting evidence "All cited line ranges are concrete (no ellipsis ranges), per the cycle-008 bundle-4 repair pattern": `palace/linalg/nleps.cpp:351-...` appears in §Context "Direct-Newton" bullet (line 65) and in the Evidence list (line 241); `palace/drivers/eigensolver.cpp:480-...` appears in §"Laws that explicitly do not hold" (line 170). For `nleps.cpp`, the `Solve()` body extends from line 351 to where `QuasiNewtonSolver::Solve` returns (the report has access to the file — should pin the actual end line). For the `drivers/eigensolver.cpp:480-...` cite, see issue #1 (the citation itself is wrong and the ellipsis is a symptom of the underlying error).

3. **[variant-axis-coverage, severity: minor]** ARPACK's `MFEM_ABORT` on `WhichType::TARGET_REAL` / `TARGET_IMAGINARY` (`palace/linalg/arpack.cpp:300-304`) is a per-orchestration axis-incompleteness — 2 of 9 spectrum-target values are unsupported by one of the three orchestrations. The report defers this with "an `E` constructed with ARPACK + `TARGET_REAL` is ill-formed and is the constructor's responsibility to reject" (line 192). This is plausible but underspecified: the L1 form claims to absorb the orchestration axis as transparent dispatch, yet a real subset of `(orchestration, spectrum-target)` pairs is invalid; the constructor-side rejection contract is asserted but not anchored in any L0 surface (the L0 constructor doesn't pre-check this; the failure surfaces at `Solve()` time via `MFEM_ABORT`). Either (a) document this as a small L1>L0 obstruction-style note under variant-absorption with a `WhichType × Orchestration` validity matrix, or (b) reframe `TARGET_REAL`/`TARGET_IMAGINARY` as a fourth-collapsed-axis "vendored-ARPACK-incomplete-which-types" variant the L1 form deliberately scopes out (analogous to the MINRES/BiCGStab stub treatment).

4. **[plan-kind-consistency, severity: minor]** Inconsistency in the diff for `book/src/L1/index.md`: the firm-cohort heading edit changes `**Firm (8)**` to `**Firm (8 + 1 rough-in)**`, then immediately lists 8 firm bullets + 1 rough-in (`eigsolve`) bullet. But the heading says "element-wise updates, BLAS-1 reductions, the opaque-operator gate, the constructed-operator solve gate, **and the eigenmode-solve gate (rough-in)**" — putting the rough-in entry under the "Firm" header by inclusion. There is already a separate "**Rough-in (obstruction)**" subsection (line 40 of the current `index.md`) for rough-in operators. The cleaner pattern would be to either (a) add `eigsolve` to a new "Rough-in (test-coverage-bounded)" subsection parallel to "Rough-in (obstruction)", keeping "Firm (8)" pure; or (b) explicitly note in the heading reformulation that the rough-in classification has been mixed-in, which the proposed diff does but in a way that conflates two cohorts. Not a blocking error — just a categorization smell.

5. **[citation-validity, severity: cosmetic]** §Algebraic-laws §1 quotes `palace/drivers/eigensolver.cpp:369-374` as printing "Found {:d} converged eigenvalues" — the actual print template at line 370 is `" Found {:d} converged eigenvalue{}{}\n"` (singular base with conditional `"s"` suffix). The quoted text is a paraphrase, not a direct quote. Minor — does not change the semantic claim — but the report uses quotation marks suggesting verbatim quotation.

6. **[citation-validity, severity: cosmetic]** §Evidence cites `palace/models/modeeigensolver.cpp:1029-1047` as the "eigensolver-backend dispatch site". Verified that 1029 is whitespace; the meaningful content starts at line 1030 (`if (type == EigenSolverBackend::ARPACK)`) and ends at line 1053 (the closing `}` of the SLEPc `else` branch — line 1054 is a `}` for the constructor). The range is essentially correct but off by one at the start and (slightly) at the end. Pre-existing precedent from cycle-008 bundle-4 (`reports/2026-05-27T173523Z-layer-intro-author-L0-bootstrap-bundle-4/`) used the same 1029-1047 range — so this may be inherited rather than newly introduced.

## Repair

### Fixes attempted

1. **Finding**: [citation-validity, major] `palace/drivers/eigensolver.cpp:480-...` cite is materially wrong — file ends at line 479, and the driver does not sort eigenvalues at all (`grep -n "sort\|perm" drivers/eigensolver.cpp` returns no hits).
   - **Decision**: repaired
   - **Action**: Re-derived the correct sort sites by reading source. Two sort sites in the eigenmode pipeline were located: (a) ARPACK internally re-sorts by Real / Imag / Abs after the RCI loop completes at `palace/linalg/arpack.cpp:374-398` (note: this is ARPACK-internal, not "driver-side"); (b) `ModeEigenSolver::Solve` builds a permutation sorted by proximity to the shift-target at `palace/models/modeeigensolver.cpp:479-492`. Rewrote the §"Laws that explicitly do not hold" / "Eigenvalue ordering" bullet (CYCLE.md line 170) to cite both sites correctly, softening "two different driver-side sort strategies" to "two distinct sites within the eigenmode pipeline" since the ARPACK site is solver-internal rather than driver-side, and explicitly attributed each site's role. Critic's claim that the prose's "two sort strategies" was unsupported was the right concern but the underlying claim is actually substantiable — there ARE two sort sites, just not where the original citation pointed and not both "driver-side".

2. **Finding**: [citation-validity, minor] Two ellipsis-range citations contradict the report's self-claim "All cited line ranges are concrete (no ellipsis ranges)".
   - **Decision**: repaired
   - **Action**: For `nleps.cpp:351-...`: derived end line by reading `grep -n "^}" nleps.cpp` — `QuasiNewtonSolver::Solve()` starts at line 351 and closes at line 805 (verified line 804 is `return nev;`). Replaced both occurrences (CYCLE.md line 65 in §Context Direct-Newton bullet, line 241 in Evidence list) with `:351-805`. Also fixed the corresponding ellipsis in §Out-of-scope "Per-pair iterative refinement" bullet (CYCLE.md line 205) — the prose was attributing `:351-...` to `SetInitialGuess` but `SetInitialGuess` actually spans `nleps.cpp:254-323`; rewrote the bullet to correctly cite `Solve()` at `:351-805` invoking `SetInitialGuess()` at `:254-323` (line 366 is the call site). For `drivers/eigensolver.cpp:480-...`: subsumed by repair #1 (the citation itself was wrong and was replaced wholesale).

3. **Finding**: [variant-axis-coverage, minor] ARPACK's `MFEM_ABORT` on 2 of 9 `WhichType` values (`TARGET_REAL` / `TARGET_IMAGINARY`) handwaved to "constructor's responsibility".
   - **Decision**: repaired
   - **Action**: Per CLAUDE.md "Unimplemented Palace stub policy" (analogous to MINRES/BiCGStab `MFEM_ABORT` stub treatment at `palace/linalg/ksp.cpp:53-57`), expanded the spectrum-target variant-axis bullet (CYCLE.md line 192) to: (a) explicitly enumerate the 2 unsupported values by name (`TARGET_REAL`, `TARGET_IMAGINARY`); (b) quote the exact `MFEM_ABORT` message text; (c) cross-reference the `palace/linalg/ksp.cpp:53-57` precedent and tag the `(ARPACK, TARGET_REAL)` / `(ARPACK, TARGET_IMAGINARY)` pairs as unimplemented stubs rather than transparent dispatch; (d) add an L1>L0 obstruction note flagging the constructor-side rejection contract as needing explicit treatment in a future `eigsolve-mutation-rotation` lowering theme, with reference to the `L1-L0/minres-iteration` precedent for the obstruction pattern.

4. **Finding**: [plan-kind-consistency, minor] `book/src/L1/index.md` diff mixes new rough-in into the Firm cohort heading.
   - **Decision**: repaired
   - **Action**: Rewrote the §"Diff for `book/src/L1/index.md`" change (1) to add a new "Rough-in (test-coverage-bounded)" subsection parallel to the existing "Rough-in (obstruction)" subsection, leaving the Firm (8) heading and its bullets unchanged. The new subsection sits between the Firm bullet list and the "Rough-in (obstruction)" heading, with `eigsolve` as its sole bullet plus a subsection-introductory sentence describing the cohort (operators with anchored structural signatures but reduced algebraic-law confidence pending test coverage or literature anchoring).

5. **Finding**: [citation-validity, cosmetic] §Algebraic-laws §1 paraphrases `eigensolver.cpp:369-374` print template.
   - **Decision**: repaired
   - **Action**: Replaced the paraphrased quoted text "Found {:d} converged eigenvalues" in CYCLE.md §Semantics (the partial-convergence paragraph, line referencing :369-374) with the exact format-string text `" Found {:d} converged eigenvalue{}{}\n"` and added a clause describing the singular-base + conditional `"s"` suffix pattern, removing the false-verbatim impression.

6. **Finding**: [citation-validity, cosmetic] §Evidence cites `modeeigensolver.cpp:1029-1047` (line 1029 is whitespace; range should be 1030-1053).
   - **Decision**: repaired
   - **Action**: Updated the Evidence-list bullet to `:1030-1053` and added a breakdown noting the ARPACK branch spans 1030-1040 and the SLEPc branch spans 1041-1053. Also updated the §Pre-check Decision sentence (CYCLE.md line 36) which referenced the same range in its driver-side dispatch summary. Note: critic flagged this may be inherited from cycle-008 bundle-4's L0 chapter; that pre-existing precedent is out of repair scope here (the L0 chapter is at `book/src/L0/eigensolver-wrapper.md` and would need a separate integrator-time touch-up if propagation is desired — flagged for integrator awareness).

### Unrepairable findings

None. All 6 findings repaired in-place.

## Suggested resolution

`ready` for integration. The major citation repair (finding #1) replaced a materially-wrong citation with a substantiated two-site citation; the underlying "two sort strategies" claim was correct but pointed at the wrong files. The variant-axis stub documentation (finding #3) brings the report into alignment with CLAUDE.md unimplemented-Palace-stub policy. The plan-kind-consistency fix (finding #4) preserves the cohort-purity invariant the L1 Part overview was carrying. Cosmetic fixes (#5, #6) clean up direct-quote / off-by-one issues.

**Integrator note**: cosmetic finding #6's pre-existing precedent at `book/src/L0/eigensolver-wrapper.md` (cycle-008 bundle-4) uses the same `1029-1047` range. Repair scope is per-report; a future cleanup pass could propagate the corrected `1030-1053` range to the L0 chapter, but that is outside this repair's authority.
