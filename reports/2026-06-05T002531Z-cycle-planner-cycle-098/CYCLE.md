---
agent: cycle-planner
invoked_at: 2026-06-05T002531Z
scope: cycle-098 dispatch plan
status: pending
---

# Cycle 098 dispatch plan

## Goals selected this cycle

cycle-098 is POSITION 2/3 of meta-batch-31 (cycles 097/098/099; the batch-31 meta-phase fires AFTER cycle-099's finalize). The LEAD continues = item-1 `graded-stack-p2-slice-deletion-tranche` (SECOND tranche). cycle-097 deleted the 4 clean-absorb Cohort-B slices (9→5). This cycle absorbs+deletes the **two genuinely-independent low-coupling survivors** — `orthog` and `polynomial_recurrence_step` — both of whose load-bearing worked-example content is ALREADY in firm concept homes on disk (so both are clean-repoint+delete, not substantive absorbs), leaving the **deeply-interwoven krylov trio (`cg`/`gmres`/`arnoldi_step`)** as c099's own focused single-coordinated-owner sub-campaign (its ~30 shared-line anchors across the firm L2/L3/L4 `krylov-step` trio + 4 `L4-L3/` dissolution themes + `L3-L2/krylov-step-body-identity` + `L2-L1/krylov-step-kernel-defusion` cannot be split into 3 parallel dispatches — they edit the SAME lines). Plus the byte-disjoint item-2 `domain_energy_reduce:313` gram_reduce-cohort land-clean fill. THREE dispatches, structured to land clean and NOT preempt the c099 krylov sub-campaign. This does NOT finish the campaign (the trio stays for c099; the `annotated-and-retained` carve-out + skill `phase-1-slice-reduction-audit` retirement stays deferred to campaign completion ≈ batch-31 meta-phase).

## On-disk citation-web audit (the decision basis; re-verified AS OF the c097 landed state — the 4 deleted slices are GONE)

Inbound-citation web for the 5 survivors (prose `slice.md:NN` anchor cites + markdown-links, excluding own-file + spec/index.md):

| slice | load-bearing inbound cites | coupling | c098 disposition |
|---|---|---|---|
| `orthog` | EXACTLY 2: `concepts/gemv_basis.md:21` (slice-link), `L1/orthogonalize.md:302` (`orthog.md:18` anchor). MPI-collective-shape content ALREADY in firm homes (`concepts/orthogonalization.md:42-58`, `L2/orthogonalize.md:19,150,347`, `L3/orthogonalize.md:481`, `L1/orthogonalize.md:100-104`). | **LOW** (clean) | **D1 — absorb-already-done → repoint 2 cites + delete** |
| `polynomial_recurrence_step` | 4 anchor sites, 95% separable: `L2/krylov-step.md:7` (the ONE shared line — all-5-slice paragraph), `L2/krylov-step.md:142` (polynomial-only line), `L2/index.md:135` (polynomial-only pattern-instances line), `dependency-map.md:169-171` (polynomial-only mermaid edges). 5-axis difference table + 4-site catalog + falsification criteria ALREADY absorbed in `concepts/negative-result-slice.md:46,61-86`. | **LOW-MED** (95% separable; shares only `L2/krylov-step.md:7` with the trio) | **D2 — absorb-already-done → surgical repoint (DROP only the polynomial clause from `:7`, leaving cg/gmres/arnoldi intact) + delete** |
| `cg` | HEAVY: `L2/krylov-step.md` (6 anchors), `L3/krylov-step.md`, `L3-L2/krylov-step-body-identity.md`, `L4/iterate-while.md`, `L4/iterate-while-with-prev.md` (8), `L4/krylov-step.md`, `L4-L3/krylov-step-typed-wrapper-dissolution.md` (7), `L4-L3/gmres-inner-loop-iterate-while-migration.md` | **HIGH** | **DEFER → c099 trio sub-campaign** |
| `gmres` | HEAVY: `L2/krylov-step.md` (6), `L4/krylov-step.md` (3), `L4-L3/fgmres-inner-loop-iterate-while-migration.md` (9), `L4-L3/gmres-inner-loop-iterate-while-migration.md` (7), `L4/iterate-while.md` | **HIGH** | **DEFER → c099 trio sub-campaign** |
| `arnoldi_step` | HEAVY: `L2/krylov-step.md` (6), `L4-L3/krylov-step-typed-wrapper-dissolution.md` (9), `L4/krylov-step.md` (3), `L3/krylov-step.md`, `L2-L1/krylov-step-kernel-defusion.md`, `L3-L2/krylov-step-body-identity.md`, `L1/orthogonalize.md`, `L1-L0/minres-iteration.md`, 2 `L4-L3/iterate-while*-dissolution.md` | **HIGH** (9 live ranges) | **DEFER → c099 trio sub-campaign** |

**The decisive separability finding:** the krylov trio's inbound anchors are spread across `L2/krylov-step.md`, `L4/krylov-step.md`, the 4 `L4-L3/` dissolution themes, `L3/krylov-step.md`, `L3-L2/krylov-step-body-identity.md`, `L2-L1/krylov-step-kernel-defusion.md` — and cg/gmres/arnoldi anchors are INTERLEAVED on the SAME lines (e.g. `L2/krylov-step.md:7` is a single paragraph naming all of cg/gmres/arnoldi/polynomial anchors). A 3-parallel-dispatch trio split would force three writers onto the same lines = guaranteed conflict. The trio MUST be one coordinated single-owner sub-campaign → c099. **Polynomial is 95% separable** from the trio (its anchors are on polynomial-ONLY lines at `:142`/`L2/index.md:135`/`dependency-map.md:169-171`) — the ONLY shared point is `L2/krylov-step.md:7`, where D2 surgically DROPS only the `polynomial_recurrence_step.md:119-160` clause and leaves the cg/gmres/arnoldi clauses for c099. No same-edit conflict: D2 owns `L2/krylov-step.md` this cycle; c099's trio owner reads D2's landed state.

## Deliverable-presence verification (paste-inline-evidence; per the c036 strengthened mandate)

**D1 — `orthog` absorb+delete** (target file under `book/src/spec/slices/`):
1. **File existence:** `ls book/src/spec/slices/orthog.md` → `book/src/spec/slices/orthog.md` / `EXIT=0` (EXISTS — open by construction, slice not yet deleted).
2. **Maturity/already-discharged:** slice §Reduction-status `:9` reads "the Gram-Schmidt L1 form ... has been partially lifted into the firm artifact ... unique L1 invariants and MPI-collective shape disclosure are retained ... pending lift to a firm `L1/orthogonalize` operator entry." The pending-lift target `L1/orthogonalize.md` is FIRM on disk; the MPI-collective-shape content is ALREADY in firm homes (`grep -c collective-shape book/src/concepts/orthogonalization.md` → 18 hits; `L2/orthogonalize.md:19,150,347`, `L3/orthogonalize.md:481`, `L1/orthogonalize.md:100-104`). **So the "substantive own-dispatch / MPI-collective-shape absorb" framing in the integrator-signals + item-1 is STALE — the absorb is already done.** The dispatch is a clean repoint+delete, not a content absorb. NOT a no-op (the slice file + SUMMARY/spec-index rows + 2 inbound cites still exist).
3. **Inbound cites to repoint (exactly 2, confirmed):** `grep -rn 'slices/orthog\|orthog\.md:' book/src --include='*.md' | grep -v spec/slices/orthog.md | grep -v spec/index.md` → `concepts/gemv_basis.md:21` + `L1/orthogonalize.md:302`. (No OQ RESOLVED match for orthog-deletion — open.)
4. **Structural block:** none — the firm homes exist; deletion is gated only on the 2-cite repoint + the SUMMARY/spec-index co-landing (handled by single-owner partition below). NOT on the STOP-PROPOSING negative list.

**D2 — `polynomial_recurrence_step` absorb+delete** (target file under `book/src/spec/slices/`):
1. **File existence:** `ls book/src/spec/slices/polynomial_recurrence_step.md` → `EXIT=0` (EXISTS — open by construction).
2. **Maturity/already-discharged:** the 5-axis difference table + 4-site distinction-catalog + per-claim falsification criteria are ALREADY absorbed into `concepts/negative-result-slice.md` (`grep -c` of axis/catalog/Chebyshev → 5 hits; the canonical worked-example narrative is at `:46,:61-86` incl. the explicit "five-axis table" + within-Chebyshev partial-positive + falsification surface). So D2 is a clean repoint+delete, not a content absorb. NOT a no-op (slice file + 4 anchor sites + SUMMARY/spec-index rows still exist).
3. **Inbound anchor cites to repoint (4 sites, separability confirmed):** `L2/krylov-step.md:7` (shared all-5 paragraph — DROP only the `polynomial_recurrence_step.md:119-160` clause), `L2/krylov-step.md:142` (polynomial-only line — delete), `L2/index.md:135` (`sed -n '131,135p'` confirms the polynomial anchor is on its OWN line, lines 131-134 are cg/gmres/arnoldi/chebyshev), `dependency-map.md:169-171` (`sed -n '168,172p'` confirms 3 polynomial-only mermaid edges). (No OQ RESOLVED match — open.)
4. **Structural block:** none — `negative-result-slice.md` is the firm home and already holds the content. NOT on the STOP-PROPOSING negative list (`polynomial_recurrence_step` is NOT in the (C) negative-list — that list is `lu_solve`/`back_solve`/`ls-update-column`/4-NLEPS-atoms; this is a slice-deletion, not an L3-backfill).

**D3 — `domain_energy_reduce:313` gram_reduce-cohort land-clean** (target `book/src/L4/domain_energy_reduce.md`):
1. **File existence:** EXISTS (the firm L4 chapter; touched c097-D6 for the `:377` mwn residue — this is a DISTINCT residue at `:313`).
2. **Maturity/stale-narration confirmed OPEN:** `grep 'STAYS rough-in\|gram_reduce' book/src/L4/domain_energy_reduce.md` → `:313` reads "(The off-diagonal contrast is `gram_reduce`, which STAYS rough-in this same cycle because its off-diagonal folded primitive `bilinear-form` is still rough-in...)" — but `grep '^firmness' book/src/L4/gram_reduce.md` → `firmness: firm` (flipped c095). So the `:313-316` narration is STALE (asserts gram_reduce rough-in; gram_reduce is firm on disk). OPEN — a genuine within-file cross-cohort maturity-prose tidy.
3. **OQ-ledger:** the c097 integrator-signals opened `domain_energy_reduce-313-gram_reduce-bilinear-form-c095-stale-rough-in-narration` (NEW / needs-more); NOT yet closed — open.
4. **Structural block:** none — pure prose re-anchor (the lifter within-file self-consistency grep discipline + the lifter graded-stack rank bullet from the batch-30 restart apply).

All three OPEN; none on the STOP-PROPOSING negative list; framings correct (clean-repoint+delete for D1/D2, lifter within-file land-clean for D3).

## Dispatches

1. **agent:** `same-layer-cross-cutter`
   **scope:** P2 slice-deletion — **`orthog`** absorb-verify + repoint + delete. (a) VERIFY the MPI-collective-shape disclosure (MGS m×1 / CGS 1×m / CGS2 2×m reductions) + L1 invariants are fully present in the firm homes (`concepts/orthogonalization.md:42-58`, `L2/orthogonalize.md`, `L3/orthogonalize.md`, `L1/orthogonalize.md:100-104`) — paste the on-disk evidence (the absorb is already done per the planner audit; if ANY unique L1-invariant/collective-shape datum is found NOT in a firm home, absorb it into `concepts/orthogonalization.md` citing L0 `palace/linalg/orthog.hpp:18-90` DIRECTLY, NOT the slice). (b) Repoint the 2 inbound cites OFF the slice: `concepts/gemv_basis.md:21` (`slices/orthog.md` → `concepts/orthogonalization.md` / the firm CGS batched-reduction home) + `L1/orthogonalize.md:302` (the `orthog.md:18` pending-lift anchor → claim-free Provenance prose, since this firm entry IS the lift target). (c) Delete `book/src/spec/slices/orthog.md`. (d) DO NOT touch SUMMARY.md / spec/index.md (D2 single-owns those row removals this cycle — see partition).
   **deps:** none
   **rationale:** item-1 LEAD, the cleanest of the 5 survivors (absorb already done; 2 cites). Byte-disjoint from the krylov hub (`orthog` is NOT cited in `L2/krylov-step.md` or any `L4-L3/` theme). Closes the campaign's `orthog` node toward GC-unreachable.

2. **agent:** `same-layer-cross-cutter`
   **scope:** P2 slice-deletion — **`polynomial_recurrence_step`** absorb-verify + surgical repoint + delete, AND **SOLE-owner of the SUMMARY.md + spec/index.md slice-row removals for BOTH `orthog` AND `polynomial_recurrence_step` this cycle** (the linkcheck2/mdBook co-landing single-owner constraint, mirroring c097-D5). (a) VERIFY the 5-axis difference table + 4-site distinction-catalog + falsification criteria are fully present in `concepts/negative-result-slice.md:46,61-86` — paste the on-disk evidence (already absorbed per the planner audit; if any unique catalog datum is missing, absorb into `negative-result-slice.md` citing the L0 catalog sites DIRECTLY). (b) Surgically repoint the 4 anchor sites OFF the slice: at `L2/krylov-step.md:7` DROP ONLY the `...the three polynomial-recurrence sites cataloged at polynomial_recurrence_step.md:119-160...` clause (re-anchor to `book/src/L4/chebyshev.md` §Semantics `innerStep` + `concepts/negative-result-slice.md`), LEAVING the `cg.md`/`gmres.md`/`arnoldi_step.md` clauses INTACT for the c099 trio owner; delete/re-anchor `L2/krylov-step.md:142` (polynomial-only line → `negative-result-slice.md`); re-anchor `L2/index.md:135` (the polynomial-only pattern-instances line → `negative-result-slice.md`); re-anchor `dependency-map.md:169-171` (the 3 polynomial-only mermaid edges → keep the `negative-result-slice`/`elementwise-product`/`givens` targets but source-node off the slice, OR remove if the firm home carries the dep). (c) Delete `book/src/spec/slices/polynomial_recurrence_step.md`. (d) AFTER D1 lands (dep), remove the SUMMARY.md rows (`:295` orthog, `:296` polynomial) + the spec/index.md rows (`:17` orthog, `:19` polynomial) for BOTH slices in ONE pass (co-landing safety: a deleted slice's SUMMARY/spec-index reference is a dangling mdBook/linkcheck2 hard-fail UNLESS removed THIS cycle).
   **deps:** 1 (D1 must land first so D2 removes BOTH slices' SUMMARY/spec-index rows knowing D1's `orthog` deletion is on disk; the per-report integrators serialize, so D2's row-removal pass reads the post-D1 state)
   **rationale:** item-1 LEAD. Polynomial is 95% separable from the trio (its anchors are on polynomial-only lines except `L2/krylov-step.md:7`, where D2 surgically drops only the polynomial clause). Single-owner of the shared SUMMARY.md/spec/index.md row removals (the parallel-blind-shared-index guard + the c097-D5 co-landing precedent). The shared `L2/krylov-step.md:7` edit is a DROP-clause-only edit that leaves the trio clauses for c099 — no same-edit cross-cycle conflict.

3. **agent:** `lifter`
   **scope:** item-2 land-clean — **`book/src/L4/domain_energy_reduce.md:313-316`** gram_reduce-cohort stale-narration re-anchor. The `:313` parenthetical asserts "`gram_reduce` STAYS rough-in this same cycle because its off-diagonal folded primitive `bilinear-form` is still rough-in" — STALE: `gram_reduce.md` is `firmness: firm` on disk (flipped c095 when `bilinear-form` firmed). Re-anchor the `:313-316` narration to the post-c095 reality (the off-diagonal contrast no longer holds — gram_reduce is firm; re-tell the contrast as the COMPLETED cascade, or drop the stale "STAYS rough-in" framing entirely while preserving the legitimate `domain_energy_reduce`-vs-`gram_reduce` scope distinction). Apply the within-file self-consistency grep discipline (re-read the file end-to-end; re-anchor any OTHER stale `gram_reduce`-rough-in co-mention to firm) + the lifter graded-stack rank bullet (confirm any `depends-on` edges touched satisfy the rank invariant; this is a prose re-anchor, no rank flip).
   **deps:** none
   **rationale:** item-2 byte-disjoint fill; the c097 integrator-signals NEW OQ `domain_energy_reduce-313-gram_reduce-bilinear-form-c095-stale-rough-in-narration`. Distinct file region from D1/D2 (no slice touch). A cheap within-file land-clean the c097 D6 one-residue-cohort scope deliberately left out.

## Overlap analysis

Pairwise artifact-region check:

- **D1 ↔ D2:** D1 touches `concepts/gemv_basis.md`, `L1/orthogonalize.md:302`, `concepts/orthogonalization.md` (if any residual absorb), deletes `spec/slices/orthog.md`. D2 touches `L2/krylov-step.md`, `L2/index.md`, `concepts/dependency-map.md`, `concepts/negative-result-slice.md` (if residual absorb), deletes `spec/slices/polynomial_recurrence_step.md`, AND `SUMMARY.md`+`spec/index.md` row removals for BOTH slices. **The ONLY shared files are `SUMMARY.md` + `spec/index.md`** — and those are partitioned by the single-owner constraint: **D2 SOLE-owns both files' slice-row removals**, D1 does NOT touch them. D1's `orthog` deletion + D2's `orthog`-row-removal is a co-landing constraint resolved by SEQUENCING (D2 deps D1): D2 removes the orthog SUMMARY/spec-index rows AFTER D1's orthog deletion is on disk. Content-disjoint otherwise. **SEQUENTIAL (D2 after D1).**
- **D1 ↔ D3:** fully disjoint. D1 = orthog slice + 2 concept/L1 cites. D3 = `L4/domain_energy_reduce.md` only. No shared file. **PARALLEL.**
- **D2 ↔ D3:** fully disjoint. D2 = krylov-step/index/dependency-map/negative-result-slice/SUMMARY/spec-index + polynomial slice. D3 = `L4/domain_energy_reduce.md` only. No shared file. **PARALLEL.**

No two dispatches modify the same operator entry or rewrite the same theme body. The single shared-index region (SUMMARY.md + spec/index.md) has exactly ONE owner (D2). The single shared-LINE risk (`L2/krylov-step.md:7`, shared with the c099 trio) is intra-D2-this-cycle and cross-cycle-clean (D2 drops only the polynomial clause; c099 reads the landed state).

## Sequencing schedule

- **Wave 1 (parallel):** D1 (`orthog` absorb+repoint+delete), D3 (`domain_energy_reduce:313` land-clean). These are fully disjoint and have no deps.
- **Wave 2 (after D1's per-report integrator lands):** D2 (`polynomial_recurrence_step` absorb+repoint+delete + SOLE-owner SUMMARY/spec-index row removals for BOTH slices). D2 deps D1 so its dual-slice SUMMARY/spec-index row-removal pass reads the post-D1 state (D1's orthog slice already deleted on disk), keeping the co-landing build-gate green.

Note: there is exactly ONE `integrator-finalize` at cycle end (rebuild book + run step-5b linters + commit + push). The wave ordering is dispatch/forward-reference ordering only; the book is NOT rebuilt between waves. The finalize step-5b will run the graded-stack linters on the landed tree — expect `files` 356→354 (2 slices deleted), `untyped` 148→146, `rank_violations` stays 0 (the deleted slices are pre-P1 detritus leaves outside the typed DAG), and the 2 deleted slice nodes leave the reachability-GC detritus mass (campaign progress).

## Open questions / caveats

- **Campaign NOT complete this cycle (by design).** After c098, the slice corpus is 5→3 (`cg`/`gmres`/`arnoldi_step` remain). The completion criterion (GC `spec/slices/*` fully unreachable + rank invariant holds zero slice nodes) is NOT met until the krylov trio is gone → the `annotated-and-retained` carve-out + skill `phase-1-slice-reduction-audit` retirement stays deferred to campaign completion (the c099 trio sub-campaign + the batch-31 meta-phase). **Recorded for the c099 planner: the krylov trio is ONE focused single-coordinated-owner sub-campaign, NOT 3 parallel dispatches** — its ~30 anchors interleave on the SAME lines (`L2/krylov-step.md:7` carries cg+gmres+arnoldi clauses; the 4 `L4-L3/` dissolution themes + `L3/krylov-step.md` + `L3-L2/krylov-step-body-identity.md` + `L2-L1/krylov-step-kernel-defusion.md` cite all three). Sequence it as absorb-verify (the L4-v0.5 cg-unrolling rendering at `cg.md:27-141`/`:39-106` is the ONE genuinely-unlifted live material per `L2/krylov-step.md:69,138` — it needs a firm home or a `roadmap_goal` before cg deletes) → coordinated single-owner hub repoint → delete. Also carry the still-open end-bound citation-divergence OQ (`:73-109`/`:73-120`/`:73-118` in `arnoldi_step`/`polynomial_recurrence_step`/`composition-lowering`, the c097-D4 critic Issue 3 appended under `plane-rotation-givens-l0-citation-range-reconcile`) — reconcile the `arnoldi_step` end-bound when the trio repoints.

- **The "substantive own-dispatch / MPI-collective-shape absorb" framing for `orthog` (in the integrator-signals + item-1) is STALE** — the planner's on-disk audit found the collective-shape content ALREADY fully in firm homes (`concepts/orthogonalization.md` 18 hits + L1/L2/L3 orthogonalize entries). D1 is scoped as absorb-VERIFY + clean-delete, not a substantive absorb. This is the same staleness class the c097 planner flagged for `orthogonalize-mutation-rotation` (the "mint roadmap_goal" no-op) — the item-1 absorb sub-tasks are mostly already-discharged because the firm homes accreted over prior cycles. No `roadmap_goal` chapter minted c098 (every survivor that deletes this cycle has a firm home; the trio's cg-unrolling material is the one possible roadmap_goal, deferred to c099).

- **`polynomial_recurrence_step` is 95% independent of the trio** — its anchors sit on polynomial-only lines EXCEPT `L2/krylov-step.md:7`. The planner verified line-level separability (`L2/index.md:135` polynomial-only; `:142` polynomial-only; `dependency-map.md:169-171` polynomial-only). D2's `:7` edit DROPS only the polynomial clause; the c099 trio owner reads D2's landed state and drops the cg/gmres/arnoldi clauses then. If a critic judges the `:7` surgical-drop risky (the paragraph is dense), the fallback is to defer polynomial WITH the trio to c099 — but the planner's read is that the drop is clean (one clause, clear boundaries). Flagged for the critic's edge-label-fidelity check.

- **Capacity:** 3 dispatches (well under the ≤12 ceiling). The standing item-3 forward frontier (bottom-up vocab + 5-driver→L4) is deliberately NOT filled this cycle — the LEAD is the P2 campaign, the trio is correctly deferred to its own c099 cycle, and a manufactured forward-frontier dispatch would either preempt the campaign focus or risk touching the krylov hub. A focused 3-dispatch wave that lands clean is the right shape for a mid-campaign cycle (matches the c097 6-dispatch / c096 6-dispatch sizing discipline of "land clean, don't sprawl").
