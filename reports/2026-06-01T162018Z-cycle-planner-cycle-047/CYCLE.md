---
agent: cycle-planner
invoked_at: 2026-06-01T162018Z
scope: cycle-047 dispatch plan
status: pending
---

# Cycle 047 dispatch plan

## Goals selected this cycle

Cycle-047 is the SECOND primary cycle of meta-batch-14 (046/047/048; meta fires after 048). Cycle-046 was a survey cycle that handed two fan-out-ranked pick lists to this planner: the L4-frontier picks (D1 combinator-miner survey) and the residual-L2>L1 gap census (D3 cross-cutter). This cycle dispatches the **top fan-out picks against both**, all of which are clean, prerequisite-free abstractor work:

1. **R1 (LEAD):** the two standalone L4>L3 dissolution themes for `iterate-while` + `iterate-while-with-prev` (paired). I decide **abstractor (route b)**, not lifter — rationale in Open questions. The substantive body content (unpruned + pruned forms, the §3.8 collapse rule, Condition-5 selection) already exists firm at `krylov-step-typed-wrapper-dissolution.md:158-200`; the deliverable extracts it into two dedicated, layer-coherent L4>L3 chapters and re-points the two firm L4 entries' "standalone theme pending" deferrals.
2. **L2>L1 foundation hygiene:** the two genuine L2>L1 gaps the c046 census found — `ksp_solve` (rank-1, driver tier) + `krylov-step` (rank-2, kernel tier). Both are firm L2 entries with non-identity L2↔L1 rotations and no dedicated `L2-L1/*` theme; `krylov-step` additionally resolves the dangling `L2/krylov-step.md:121` forward-ref.
3. **R2 prerequisite tee-up:** a `layer-intro-author` dispatch to **anchor the `solve-monad` outer-driver L4 vocabulary** (`restart_cycle` / `solve_loop` / the `Outcome`-sum termination) as an L4-row surface in `L4/index.md`. This is the verified gating item for the R2 `L4/ksp_solve.md` cap (deferred to cycle-048): both `L3/ksp_solve` and `L3/eigsolve` reference an unanchored `solve-monad` outer-driver row (`L4/index.md:47`). Teeing it up this cycle lets cycle-048 author `L4/ksp_solve.md` against firm vocabulary.

R3 (`L4/eigsolve.md`) and R5 (`L4/orthogonalize.md`) are NOT dispatched — R3 sequences after R2 (cycle-048+, shares `solve-monad`), R5 is marginal/deferred per the survey. The R2 cap itself (`L4/ksp_solve.md`) is HELD to cycle-048 behind its `solve-monad` prerequisite (D5 this cycle).

## Dispatches

**D1 — (`abstractor`, `book/src/L4-L3/iterate-while-dissolution.md` — standalone L4>L3 dissolution theme for the `iterate-while` combinator; deps: none)**
- Author a dedicated L4>L3 theme that extracts the **trajectory-keeping unpruned form** + the **§3.8-pruned form** + the demand-pruning collapse rule (Law 1's L3-side image) from the shared sub-component `book/src/L4-L3/krylov-step-typed-wrapper-dissolution.md:158-200` (the body content is already firm — this is extraction-into-a-layer-coherent-home, not fresh derivation). LHS = L4 `iterate-while` Form A (firm `book/src/L4/iterate-while.md`, Law 1 §3.8 `l4_calculus.md:186-213`); RHS = the L3 `iterate_while_L3` / `iterate_while_L3_pruned` tail-recursion (the `:164-171` / `:176-184` forms). Narrate forward L4→L3 per the high→low discipline. Re-anchor `book/src/L4/iterate-while.md`'s "standalone L4>L3 theme pending per OQ `iterate-while-l3-rendering-trajectory-accumulation-gap`" §"Lowers to" deferral (the `L4/index.md:54` dep-map cell carries the same pending note) to cite this new dedicated theme. **DUAL-REGISTRATION:** D1 adds its OWN `L4-L3/index.md` themes-table ROW + its own SUMMARY.md line; the `L4-L3/index.md` §Working-Notes carries NO consolidated firm-count tally (verified — themes-table only), so there is no tally to defer this cycle.
- **rationale:** R1 LEAD pick (survey top fan-out, LOW cost); serves `l4-l3-coverage-and-l4-expansion` + closes OQ `iterate-while-l4-l3-standalone-theme-warranted-lifter-vs-abstractor` (route-b realization). The loop combinators are the calculus's universal iteration vocabulary; every downstream burn iterative component consumes this dissolution.

**D2 — (`abstractor`, `book/src/L4-L3/iterate-while-with-prev-dissolution.md` — standalone L4>L3 dissolution theme for the `iterate-while-with-prev` combinator; deps: D1 [forward-reference ordering — shares the trajectory-keeping content + the collapse rule; D1 establishes the dedicated-theme home pattern D2 parallels])**
- Sister theme to D1, specialised for `iterate-while-with-prev` (Form B, the first-iteration-unrolled `iterate_while_with_prev` of `krylov-step-typed-wrapper-dissolution.md:150-154`). LHS = L4 `iterate-while-with-prev` (firm `book/src/L4/iterate-while-with-prev.md`, Law 2 / degeneracy-to-`iterate-while` Law 1); RHS = the bootstrap-then-tail-recursive L3 form (the additional `prev` positional dissolution parallel to D1's). Re-anchor `book/src/L4/iterate-while-with-prev.md`'s standalone-theme deferral (`L4/index.md:55` dep-map cell) to cite this new theme. **DUAL-REGISTRATION:** D2 adds its OWN `L4-L3/index.md` themes-table ROW + SUMMARY.md line (no consolidated tally to defer).
- **rationale:** R1 paired pick; the Form-B parallel of D1. Resolves the parallel Law-2 standalone-theme gap. Sequenced after D1 (wave-2) for forward-reference cleanliness — D2 may cite D1's dedicated home as the companion theme.

**D3 — (`abstractor`, `book/src/L2-L1/ksp-solve-outer-driver-unfold.md` — L2>L1 lowering theme for `ksp_solve`; SOLE `L2-L1/index.md` consolidated-tally owner this cycle; deps: none)**
- Author the dedicated L2>L1 theme for the `ksp_solve` opacity-uncollapse rotation. The L2 chapter's §"Lowers from" (`book/src/L2/ksp_solve.md:155-157`) already narrates the non-identity rotation in-line (L1 opacity opened at L2 — the `krylov-step` kernel + convergence-test fold become visible; the absorbed `krylov-method` axis re-surfaces as the L2 solver-method loop-shaping axis) but DEFERS the firming-evidence / theme content to a dedicated theme that does not exist. LHS = L2 `ksp_solve` (firm); RHS = L1 `ksp_solve` (firm). Slug `ksp-solve-outer-driver-unfold` parallels the existing UPWARD `L3-L2/ksp-solve-outer-driver` (firm c021), making the asymmetry-closing explicit (the L3>L2 edge is themed, the L2>L1 edge now is too). Narrate forward L2→L1 per high→low. **DUAL-REGISTRATION + COUNT-OWNERSHIP:** D3 adds its OWN `L2-L1/index.md` themes-table ROW + SUMMARY.md line, AND — as SOLE count-owner this cycle — writes the consolidated §Working-Notes firm-count tally bump (**19 firm + 1 partly-constructive → 21 firm + 1 partly-constructive**, accounting for BOTH D3 and D4 landings) + the cohort-growth-log line for the two new themes. D4 DEFERS the tally/growth-log to D3.
- **rationale:** c046 census rank-1 (driver tier, widest downstream-reuse: consumed by `eigsolve`, `divfree-projector`, `incremental-least-squares`). Closes OQ `ksp-solve-l2-l1-theme-gap`; jointly with D4 closes `residual-l2-l1-gap-audit`. Foundation hygiene under `foundation_solidity`.

**D4 — (`abstractor`, `book/src/L2-L1/krylov-step-kernel-defusion.md` — L2>L1 lowering theme for `krylov-step`; deps: none [parallel-safe with D3 — distinct theme file + distinct table row + distinct bullet; defers ONLY the shared tally to D3])**
- Author the dedicated L2>L1 theme for the `krylov-step` kernel de-fusion. The L2 chapter has NO §"Lowers from" section at all, and variant-axis 6 (`book/src/L2/krylov-step.md:121`) explicitly defers the in-place-buffer specialisation "to the L2>L1 lowering" — a dangling forward-ref to a file that does not exist. Substantive content = the de-fusion of the per-step kernel into its seven L1 leaves (`apply_linop`, `axpy`, `axpby`, `axpbypcz`, `dot`, `nrm2`, `scal`, `book/src/L2/krylov-step.md:96`) under a fold + the in-place→out-of-place buffer rotation. LHS = L2 `krylov-step` (firm); RHS = the seven L1 leaves. Treat the §"L2 vs L1 distinction" (`:129-132`) as CONTEXT, not as the lowering (it is conceptual, not a forward-narrated rewrite — c046 caveat). Slug `krylov-step-kernel-defusion`. **DUAL-REGISTRATION:** D4 adds its OWN `L2-L1/index.md` themes-table ROW + SUMMARY.md line; **DEFERS the consolidated §Working-Notes firm-count tally + cohort-growth-log to D3** (the sole count-owner this cycle).
- **rationale:** c046 census rank-2 (kernel tier; `krylov-step` is the kernel half folded by `ksp_solve`, `chebyshev-iteration`, transitively `eigsolve`). Closes OQ `krylov-step-l2-l1-theme-gap` + resolves the dangling `:121` forward-ref so the planner can drop the perpetual re-flag; jointly with D3 closes `residual-l2-l1-gap-audit`.

**D5 — (`layer-intro-author`, `book/src/L4/index.md` §"Queued at L4" + the `solve-monad` outer-driver L4-vocabulary anchor — the R2 PREREQUISITE; deps: none)**
- Anchor the `solve-monad` outer-driver vocabulary (`restart_cycle` / `solve_loop` outer drivers + the `Outcome`-sum termination that lifts `ksp_solve`'s soft-fail `Bool` to a sum type per `book/src/L3/ksp_solve.md:160` / `book/src/concepts/solve-monad.md`) as an explicit L4-row surface, flipping the `L4/index.md:47` "referenced from concept pages but not yet anchored as L4 rows (potential cycle-009+ harvester scope)" deferral into a firmed (or `rough-in`) L4 vocabulary entry — so the R2 `L4/ksp_solve.md` cap (cycle-048) and the R3 `L4/eigsolve.md` cap (cycle-048+) have a firm `solve-monad` outer-driver surface to lift against. Scope this as the L4-vocabulary survey/anchor the survey flagged "straddles `layer-intro-author` concept-page + `harvester` L4-row authority" (OQ `l4-ksp-solve-eigsolve-caps-gated-on-solve-monad-outer-driver-vocabulary` + `l4-native-combinator-denominator-completeness-survey`); route `layer-intro-author` because the deliverable is the L4 Part-overview vocabulary surface + dep-map (not a single operator harvest). Author the `restart_cycle`/`solve_loop`/`Outcome` rows under the L4 strawman conventions (`book/src/design/l4_calculus.md`); cite the concept page + the two L3 driver consumers as the first-two-consumers evidence. Do NOT author `L4/ksp_solve.md` or `L4/eigsolve.md` this cycle (those are the cycle-048 R2/R3 caps gated on this).
- **rationale:** the verified R2/R3 prerequisite. The survey + OQ both flag that the highest-fan-out L4 *expansion* pick may be anchoring this vocabulary itself; teeing it up unblocks the whole missing-L4-driver-half of the Krylov + eigen chains for cycle-048. Serves `l4-l3-coverage-and-l4-expansion`. **FLOOR-LANDING-REANCHOR note:** anchoring the `solve-monad` ROWS does NOT land an `L4/ksp_solve.md` or `L4/eigsolve.md` CAP, so the multiple `L3/eigsolve.md` "no firm L4 eigsolve exists / L4/eigsolve.md not yet authored" assertions (`:19,:34,:78,:166,:172,:203,:214`) and the `L3/ksp_solve.md` solve-monad-surface references do NOT go stale this cycle — they assert the *cap chapter* is unauthored, which remains true. The re-anchor obligation lands with the cycle-048 cap dispatch, not D5. (D5 MAY note in the new `solve-monad` rows that the cap consumers are forthcoming.)

## Overlap analysis

Pairwise (5 dispatches → 10 pairs):

- **D1 × D2** — both land themes into `book/src/L4-L3/` (distinct new files `iterate-while-dissolution.md` / `iterate-while-with-prev-dissolution.md`) and both append a distinct ROW to the `L4-L3/index.md` themes-table + a distinct SUMMARY.md line. Distinct files + distinct table rows + distinct SUMMARY lines = NOT overlapping at the operational level. The `L4-L3/index.md` §Working-Notes carries NO consolidated firm-count tally (verified), so there is no shared aggregate either. The only coupling is content: D2 cites D1's dedicated home as the companion theme (forward-reference) → sequence D2 after D1 (wave ordering), but this is forward-ref ordering, not a write conflict. **NOT a region conflict; sequenced for forward-reference cleanliness.**
- **D1 × D3, D1 × D4** — different layers (L4-L3 vs L2-L1), different files, different indices. **No overlap → parallel.**
- **D1 × D5** — D1 touches `L4/iterate-while.md` §"Lowers to" + the `L4/index.md:54` dep-map iterate-while cell (re-anchor the standalone-theme deferral). D5 touches `L4/index.md` §"Queued at L4" (`:47`) + adds new `solve-monad` rows to the L4 dep-map table. Both touch `L4/index.md` but **distinct regions**: D1 edits the existing `iterate-while` row's L3-lowering cell (`:54`); D5 edits the §"Queued at L4" prose (`:47`) and APPENDS new `restart_cycle`/`solve_loop` rows. Distinct anchors. Per the conflict-tolerance philosophy (distinct regions of the same index file, additive), this is **parallel-safe** — but to be safe against the §"Queued at L4" prose both potentially touching, I sequence D5 in wave-1 and D1 in wave-1 too; the integrator's serial per-report re-read-before-edit handles the two distinct-region edits to `L4/index.md` cleanly (canonical "two additive/distinct-region edits to the same index" pattern). Mark **PARALLEL** (when in doubt, parallel; false-sequentialization is the worse error).
- **D2 × D3, D2 × D4** — different layers, different files/indices. **No overlap → parallel.**
- **D2 × D5** — D2 touches `L4/iterate-while-with-prev.md` + `L4/index.md:55` dep-map cell; D5 touches `L4/index.md:47` + new rows. Same distinct-region argument as D1 × D5. **PARALLEL.**
- **D3 × D4** — both land L2>L1 themes (distinct new files `ksp-solve-outer-driver-unfold.md` / `krylov-step-kernel-defusion.md`), both append a distinct ROW to the `L2-L1/index.md` themes-table + a distinct SUMMARY.md line + a distinct §Vocabulary-cohort bullet. Those are anchor-distinct, parallel-safe. **The ONE shared mutable aggregate** is the `L2-L1/index.md` §Working-Notes consolidated firm-count tally (19→21) + the cohort-growth-log — governed by the count-ownership + dual-registration convention. **Partition stated explicitly in the scopes:** D3 (sole count-owner) writes the consolidated tally + growth-log accounting for BOTH landings; D4 writes ONLY its own row + bullet + SUMMARY line and DEFERS the tally to D3. With that partition, **NOT a region conflict → parallel** (the convention is what makes them parallel-safe).
- **D3 × D5, D4 × D5** — different layers (L2-L1 vs L4), different files/indices. **No overlap → parallel.**

No two dispatches modify the same operator entry or rewrite the same theme body. The only shared aggregate (the `L2-L1/index.md` tally) is partitioned to a single owner (D3).

## Sequencing schedule

- **Wave 1 (parallel):** D1, D3, D4, D5. All four are prerequisite-free; D3 is the sole `L2-L1/index.md` tally-owner (D4 defers to it); D1 and D5 touch distinct regions of `L4/index.md` (handled by serial per-report re-read at integration).
- **Wave 2 (after D1's report lands):** D2 — sequenced after D1 only for forward-reference cleanliness (D2 cites D1's dedicated theme as its companion; the per-report integrator wires the live link once D1's file is on disk). No artifact-region conflict; the wave split is purely forward-reference ordering, NOT a second finalize (there is exactly ONE `integrator-finalize` at cycle end).

Integration pipeline unchanged: 5 specialized dispatches → 5 critics → repairers (as needed) → `integrator-per-report` ×5 (serial) → ONE `integrator-finalize` (rebuild book + commit + push + housekeeping). The book is NOT rebuilt between waves.

## Deliverable-presence verification

All five dispatches resolve to named `book/src/` artifact paths, so each carries the four-step check with literal pasted command output.

**D1 — `book/src/L4-L3/iterate-while-dissolution.md` (NEW standalone theme):**
1. File existence: `ls book/src/L4-L3/iterate-while-dissolution.md` → `ls: cannot access 'book/src/L4-L3/iterate-while-dissolution.md': No such file or directory`. **ABSENT — open.** (The `L4-L3/` dir holds only `fgmres-inner-loop-iterate-while-migration.md`, `gmres-inner-loop-iterate-while-migration.md`, `index.md`, `krylov-step-typed-wrapper-dissolution.md` — confirmed via `ls book/src/L4-L3/`.)
2. Maturity: N/A (target absent). Deps firm: `book/src/L4/iterate-while.md` `## Status` reads ``firm`` (pasted: "`firm` — small-step semantics inherited verbatim from the L4 strawman §3.7 …"); `book/src/L4-L3/krylov-step-typed-wrapper-dissolution.md` `## Status` reads ``firm``. The body content to extract is at `:158-200` (read + confirmed: unpruned `:164-171`, pruned `:176-184`, collapse rule `:188-198`).
3. OQ-ledger RESOLVED-grep: `grep 'iterate-while-l4-l3-standalone.*RESOLVED\|iterate-while-l4-l3-standalone.*CLOSED' scaffolding/open-questions.md` → no matches. The slug `iterate-while-l4-l3-standalone-theme-warranted-lifter-vs-abstractor` is OPEN at `open-questions.md:170` (cycle-046 New-intake, *Trigger: cycle-047 planner picks R1 + decides realization* — fired this cycle).
4. Structural-block: none. Both L4 caps firm; the survey ranks R1 as the cleanest, lowest-cost, prerequisite-free L4-layer pick. **Open by construction-of-gap (the c046 survey is the immediately-prior-cycle justification).**

**D2 — `book/src/L4-L3/iterate-while-with-prev-dissolution.md` (NEW standalone theme):**
1. File existence: `ls book/src/L4-L3/iterate-while-with-prev-dissolution.md` → `ls: cannot access … No such file or directory`. **ABSENT — open.**
2. Maturity: N/A (absent). Dep firm: `book/src/L4/iterate-while-with-prev.md` `## Status` reads ``firm`` (pasted: "`firm` — small-step semantics derived from the strawman §3.7 form by adding the bootstrap step and the closure-threaded `prev` parameter …").
3. OQ-ledger RESOLVED-grep: same as D1 (paired under the same OQ `iterate-while-l4-l3-standalone-theme-warranted-lifter-vs-abstractor`, `open-questions.md:170`, OPEN).
4. Structural-block: none. **Open by construction-of-gap (c046 survey).**

**D3 — `book/src/L2-L1/ksp-solve-outer-driver-unfold.md` (NEW L2>L1 theme):**
1. File existence: `ls book/src/L2-L1/ksp-solve-outer-driver-unfold.md` → `No such file or directory`; `ls book/src/L2-L1/ | grep -i 'ksp\|krylov'` → `(no ksp/krylov L2-L1 theme files)`. **ABSENT — open.** (c046 census confirmed the 20 L2-L1 theme files contain no `ksp_solve` or `krylov-step` subject theme.)
2. Maturity: N/A (absent). Dep firm: `book/src/L2/ksp_solve.md` `## Status` reads ``firm`` (pasted: "`firm` — the convergence-test fold of the `krylov-step` kernel is the canonical L2 form … Both the L2↔L1 and L3↔L2 rotations are **non-identity**"). The non-identity rotation is in-chapter at `:155-157`.
3. OQ-ledger RESOLVED-grep: `grep 'ksp-solve-l2-l1-theme-gap.*RESOLVED\|ksp-solve-l2-l1-theme-gap.*CLOSED' scaffolding/open-questions.md` → no matches. The slug `ksp-solve-l2-l1-theme-gap` is OPEN at `open-questions.md:182` (cycle-046 New-intake, "RANK FIRST").
4. Structural-block: none. Genuine-missing-theme gap (c046 census; the `apply_linop` no-L2-by-design precedent explicitly does NOT transfer — firm L2 entry exists). **Open.**

**D4 — `book/src/L2-L1/krylov-step-kernel-defusion.md` (NEW L2>L1 theme):**
1. File existence: `ls book/src/L2-L1/krylov-step-kernel-defusion.md` → `No such file or directory`; grep (above) → no ksp/krylov L2-L1 theme. **ABSENT — open.**
2. Maturity: N/A (absent). Dep firm: `book/src/L2/krylov-step.md` `## Status` reads ``firm`` (pasted: "`firm` — signature is the canonical fold-kernel shape; algebraic laws are reduced to the one non-trivial property (the demand-pruning law) …"). The dangling forward-ref is at `:121`.
3. OQ-ledger RESOLVED-grep: `grep 'krylov-step-l2-l1-theme-gap.*RESOLVED\|krylov-step-l2-l1-theme-gap.*CLOSED' scaffolding/open-questions.md` → no matches. The slug `krylov-step-l2-l1-theme-gap` is OPEN at `open-questions.md:183` (cycle-046 New-intake, "RANK SECOND").
4. Structural-block: none. Genuine-missing-theme gap. **Open.**

**D5 — `book/src/L4/index.md` §"Queued at L4" + new `solve-monad` outer-driver L4-vocabulary rows (EDIT/anchor, not a new file):**
1. File existence: `ls book/src/L4/index.md` → present (the L4 Part-overview). The `solve-monad` CAP rows (`L4/ksp_solve.md`, `L4/eigsolve.md`) are ABSENT (`ls book/src/L4/ksp_solve.md book/src/L4/eigsolve.md` → both `No such file or directory`; `L4/` dir holds only `chebyshev.md`, `index.md`, `iterate-while.md`, `iterate-while-with-prev.md`, `krylov-step.md`) — confirming the outer-driver vocabulary is unanchored. The `solve-monad` CONCEPT page exists (`ls book/src/concepts/solve-monad.md` → present).
2. Maturity: the deferral to anchor is on-disk at `book/src/L4/index.md:47` (pasted: "the `solve-monad`'s `restart_cycle` and `solve_loop` outer drivers are referenced from concept pages but not yet anchored as L4 rows (potential cycle-009+ harvester scope; not blocking forward-frontier work)"). The L4 rows do NOT exist (the dep-map table at `:53-56` lists only `krylov-step` / `iterate-while` / `iterate-while-with-prev` / `chebyshev`). So the anchor deliverable is genuinely absent. NOT a no-op.
3. OQ-ledger RESOLVED-grep: `grep 'l4-ksp-solve-eigsolve-caps-gated.*RESOLVED\|solve-monad-outer-driver.*RESOLVED' scaffolding/open-questions.md` → no matches. The slug `l4-ksp-solve-eigsolve-caps-gated-on-solve-monad-outer-driver-vocabulary` is OPEN at `open-questions.md:171`; `l4-native-combinator-denominator-completeness-survey` OPEN at `:173`.
4. Structural-block: none on the vocabulary-anchor itself (the gate is the OTHER direction — the R2 cap is blocked ON this, so anchoring it is the unblocking move). The cap chapters (`L4/ksp_solve.md`, `L4/eigsolve.md`) are NOT proposed this cycle (HELD to c048 behind this anchor). **Open.**

**STOP-PROPOSING negative-list cross-check:** none of D1–D5 names `lu_solve`, `back_solve`, `ls-update-column`, or the 4 NLEPS atoms; `apply_nonlinear_pencil` stays HELD (not touched). All clear.

## Open questions / caveats

- **R1 lifter-vs-abstractor decision (OQ `iterate-while-l4-l3-standalone-theme-warranted-lifter-vs-abstractor`): I chose ABSTRACTOR (route b), two dispatches.** Rationale: route (a) — a pure lifter re-anchor of the two L4 entries' §"Lowers to" directly to the collapse-rule — would NOT create the dedicated standalone L4>L3 chapters the layer-coherence argument wants; an L3 reader of the `iterate-while` combinator would still find no dedicated L4>L3 home (only a sub-component buried inside `krylov-step-typed-wrapper-dissolution`). The survey ranks R1 as theme-authoring and flags the lifter alternative as "lighter but possibly layer-incoherent." Per "Identity-lowerings still require both L levels" / the layer-coherence discipline and the just-completed L3>L2-coverage precedent (each edge gets its own themed home), the dedicated-chapter realization is correct. The cost stays LOW because the body content is already firm at `krylov-step-typed-wrapper-dissolution.md:158-200` — the abstractor extracts + re-homes it, not re-derives it. If the c047 abstractor finds the extraction is genuinely a pure re-anchor with zero new derivation (a degenerate theme), it may flag back that route (a) was sufficient; I judge route (b) the layer-coherent call and the integrator can collapse to a thinner theme if the body is truly identical.
- **R2 `solve-monad` prerequisite is REAL and is the gating item — confirmed, not just flagged.** I verified both `L3/ksp_solve.md` (`:142,:160`) and `L3/eigsolve.md` (`:19,:34,:78,:166,:172,:203,:214`) reference the `solve-monad` outer-driver / `Outcome`-sum surface as the (future, unanchored) L4 home, and `L4/index.md:47` confirms `restart_cycle`/`solve_loop` are unanchored. The R2 cap (`L4/ksp_solve.md`) is therefore correctly HELD to cycle-048 behind D5's anchor. D5 routes `layer-intro-author` because the deliverable is the L4 Part-overview vocabulary surface, but the survey noted it "straddles layer-intro-author + harvester" — if the `solve-monad` rows want genuine per-operator harvest depth (not just vocabulary anchoring), the c047 layer-intro-author may flag that a follow-up harvester pass is warranted before the c048 cap. I judge the vocabulary-anchor (rows + dep-map + first-two-consumers framing) sufficient to unblock the c048 cap; deeper harvest can ride the cap itself.
- **`residual-l2-l1-gap-audit` denominator: I use the c046 CENSUS gap-set of 2 (`ksp_solve` + `krylov-step`), NOT the c046 dispatch-framing's 1.** The c046 integrator-signals + OQ `residual-l2-l1-gap-audit-planner-undercount` (`open-questions.md:184`) flag the benign one-gap undercount; the independent census is authoritative. D3 + D4 close the audit jointly. No further L2>L1 gap remains (the census swept all 22 L2 chapters; 20 of 22 covered, `deflate` partly-constructive has its theme, the 2 gaps are D3/D4).
- **L4-L3/index.md count-ownership:** I verified `L4-L3/index.md` §Working-Notes carries NO consolidated firm-count tally (only the themes-table), so D1+D2 landing two themes there does NOT trigger the count-ownership partition — each just appends its own row. This is unlike `L2-L1/index.md`, whose §Working-Notes DOES carry a "firm N → M" tally + cohort-growth-log (hence D3 is sole tally-owner there). If the c047 layer-intro-author or a producer later adds a consolidated count to `L4-L3/index.md`, future cycles must assign an owner; this cycle there is nothing to partition there.
- **Meta-cadence note (for the batch-14 meta-phase after c048):** the c046 survey found the L4 layer is "mostly intentionally complete" (13 of 18 L3 ops carry a CONFIRMED-NOT-NEEDED no-L4-by-design verdict). After this cycle's R1 themes + cycle-048's R2/R3 caps land, the L4 frontier may be substantially exhausted (only R5 `orthogonalize` marginal + any `l4-native-combinator` survey residue remain). The meta-phase should assess whether the "uniform climb resumes UPWARD to L4" frontier is near-complete and what the next forward direction is (L4-native combinator completeness survey per OQ `l4-native-combinator-denominator-completeness-survey`, or a width/depth consolidation pass) — I flag this now since the friction-ledger / priorities reshape for batch-15 is a meta-phase pass, not mine.

## priorities.md handling

Per the cycle-planner write-authority (I co-own the plan), I leave `priorities.md` for the integrator-finalize to update at cycle-end (the established recent convention — c044/c045/c046 planners wrote the active head into their CYCLE.md and let the finalize strike landed items). The CYCLE-047 active head is the `## Dispatches` + `## Goals` above. The integrator-finalize should, at cycle-end: (i) **strike `erasure-scope-taxonomy-concept-page`** from the Backlog High tier (LANDED c046 — `book/src/concepts/erasure-scope.md` firm, per c046 integrator-signals:40); (ii) mark `residual-l2-l1-gap-audit` CLOSING once D3+D4 land (both L2>L1 gaps themed); (iii) advance `l4-l3-coverage-and-l4-expansion` (R1 themes landed; R2 prerequisite teed up, R2/R3 caps carry to c048).
