---
verifies: ../CYCLE.md
critiqued_at: 2026-05-30T22:35:00Z
critic_version: 1
checks:
  citation-validity: pass
  surface-or-evidence: pass
  rotation-quality: pass
  variant-axis-coverage: pass
  cross-reference-integrity: warning
  edge-label-fidelity: pass
  plan-kind-consistency: pass
  skill-uptake-survey: pass
repaired_at: 2026-05-30T23:10:00Z
repairer_version: 1
repairs:
  citation-validity: not-needed
  surface-or-evidence: not-needed
  rotation-quality: not-needed
  variant-axis-coverage: not-needed
  cross-reference-integrity: repaired
  edge-label-fidelity: not-needed
  plan-kind-consistency: not-needed
  skill-uptake-survey: not-needed
overall_status: ready
follow_up_agent: null
---

# META: verification of L1>L0 theme sketch — reciprocal-elementwise-product-mutation-rotation (cycle-034 D1)

## Critique

### Checks run

**citation-validity** — PASS. Mechanical scan with `tools/citecheck/citecheck.py --scan` returned `43 ok, 0 failing (43 citations checked)`. Independent `--anchor` re-runs on the nine load-bearing pinpoint citations all confirmed the producer's self-verification block: `ComplexVector::Reciprocal` at `vector.cpp:248` within 248-261; `BaseDiagonalOperator<Operator>::Mult` at `operator.cpp:479` within 478-487; `BaseDiagonalOperator<ComplexOperator>::Mult` at `:490` within 489-507; `MultHermitianTranspose` at `:548` within 545-568; `Apply(const Vector` at `jacobi.cpp:31` within 30-39; `Apply(const ComplexVector` at `:42` within 41-69; `Apply(dinv, x, y)` at `:103` within 99-104. Manual `awk`-on-disk re-read of the complex-reciprocal body (`vector.cpp:248-261`) confirmed the report's exact line-attributions: `s = 1/(XR²+XI²)` at `:257`, `XR[i] *= s` at `:258`, `XI[i] *= -s` at `:259` — byte-identical to the report's quoted body. Manual re-read of the three elementwise-product L0 surface forms (`operator.cpp:478-487` Y[i]=D[i]*X[i] at :486; `:489-507` complex pair at :504-505; `:545-568` conjugate-variant sign-flipped pair at :564-565; `jacobi.cpp:30-39` real Apply Y[i]=DI[i]*X[i] at :38; `jacobi.cpp:41-69` complex Apply with `if constexpr (!Transpose)` at :52 and `else` at :61) all match verbatim. Supporting citations (`jacobi.cpp:16` SPD precondition, `vector.hpp:20` Vector alias, `vector.hpp:107-108` declaration, `jacobi.hpp:43` MultTranspose→Mult one-liner, `operator.hpp:279` BaseDiagonalOperator MultTranspose→Mult inside class starting at :257, `chebyshev.cpp:178,241`, `bilinearform.cpp:278`) all verified on disk. The report's `verified_against:` claim of `tools/citecheck/citecheck.py --anchor` invocations checks out — the producer self-verification was real, not asserted.

**surface-or-evidence** — PASS. The proposal is a new L1>L0 theme (new chapter file), which is *not* a refinement of an existing operator/theme — therefore the surface-OR-evidence criterion applies in its rewrite-surface form: the entire `## L0 form (RHS)` section (file:CYCLE.md lines 91-407) is on the L1>L0 rewrite surface with per-claim citations. No uncited algebraic claims surfaced in the read. The two algebraic claims that are NOT purely structural (the complex closed-form `1/z = z̄/|z|²` recognition rule in sub-pattern A; the conjugation sub-axis as kernel-template choice in sub-pattern B) are both cited to L1/reciprocal law 5 + `vector.cpp:257-259` and to L1/elementwise_product Variant axes + `operator.cpp:545-568,564-565` respectively.

**rotation-quality** — PASS. The theme correctly narrates the lowering forward (L_{n+1} → L_n): the L1 pure-value-return form (`result = reciprocal(x)` / `y = elementwise_product(a, b)`) is rewritten into an L0 in-place mutation (`x.Reciprocal()` self-overwrite for A; `y = a ⊙ b` output-arg write for B). The rotation IS strictly more compact / more abstract at L1: the L1 form erases (per the report's explicit enumeration at lines 156-181 for A and 335-376 for B) the receiver-self-overwrite mutation idiom, the `forall_switch` device dispatch, the `s = 1/|z|²` intermediate-factoring trick, the per-element kernel templating, the conjugation kernel-template selection, and the implicit non-aliasing precondition. The lowering is structurally faithful and semantically preserving (the `s` factoring is algebraically identical to the unfused form; the kernel choice between `Mult` and `MultHermitianTranspose` realises the conjugation sub-axis verbatim). The composite-vs-split decision is explicitly justified at lines 728-745 with three load-bearing factors (shared rewrite class + co-occurrence in `JacobiSmoother::SetOperator` + boilerplate-duplication avoidance) and cites the `ksp-solve-mutation-rotation` thin-theme precedent.

**variant-axis-coverage** — PASS. Three variant axes are explicitly enumerated at lines 603-635: sub-pattern selection (A/B), element-type (real/complex, with both paths cited for both sub-patterns), and conjugation (sub-axis on B complex only, with `Mult` vs. `MultHermitianTranspose` kernel-template choice). Non-axes are explicitly enumerated at lines 636-651 with rationale: zero-guard policy (recorded as L1 partiality, not an axis), operator-action-vs-free-binary (recovered via `elementwise_product` law 9, not an axis), in-place-vs-out-of-place (the rewrite itself, not an axis). The dead-code consumer-transpose branch (`jacobi.cpp:61-69`) is correctly flagged as a recognition-rule caveat, not a hidden axis. The four consumer sites for sub-pattern A (`jacobi.cpp:80`, `chebyshev.cpp:178`, `chebyshev.cpp:241`, `bilinearform.cpp:278`) are all enumerated under `## Verified-against`.

**cross-reference-integrity** — WARNING. All referenced live links resolve on disk: the L1 anchors (`book/src/L1/reciprocal.md`, `book/src/L1/elementwise_product.md`), all L1-L0 sibling references (`dot-mutation-rotation`, `scal-mutation-rotation`, `normalize-mutation-rotation`, `apply-linop-mutation-rotation`, `jacobi-smoother-mutation-rotation`, `assemble-diagonal-mutation-rotation`, `chebyshev-smoother-mutation-rotation`), and the L1 firm-on-positive-structure precedents (`apply_linop`, `apply_nonlinear_pencil`, `jacobi-smoother`, `chebyshev-smoother`) all exist. The build-readiness fence guard PASSES: the report has 8 fence markers (4 pairs, even parity), the firm body inside the `new:` fence (lines 29-787) contains `## Status`, `## Slug`, `## L1 form`, `## L0 form`, `## Applicability conditions`, `## Justification kind`, `## Verified-against`, `## Variant axes`, `## Open questions / caveats` all INSIDE the fence; the inner code blocks (e.g., the C++ kernel quotations at lines 110-112, 127-140, 222-231, etc.) use 4-space-indented code, NOT nested ```text fences — no fence-truncation defect. The `edit:book/src/L1-L0/index.md` block (lines 789-794) correctly targets the position after line 42 (`jacobi-smoother-mutation-rotation` row) and before line 43 (`minres-iteration` row) — verified on disk. **The warning**: the `edit:book/src/SUMMARY.md` block (lines 796-801) misstates the SUMMARY ordering: it asserts "the existing alphabetical ordering of L1-L0 chapters places `reciprocal-elementwise-product-mutation-rotation` after `orthogonalize-mutation-rotation` (`o`) and before `scal-mutation-rotation` (`s`); insert there". The actual SUMMARY ordering (book/src/SUMMARY.md lines 95-120) is NOT alphabetical — it is integration-cycle-chronological (e.g., `apply-linop` :96 precedes `eigsolve` :98 precedes `dot` :107 precedes `assemble-diagonal` :111). `orthogonalize-mutation-rotation` is at :100; `scal-mutation-rotation` is at :110 — they are NOT adjacent (8 chapters between them: `bicgstab`, `minres`, `triangular-solve-obstruction`, `chebyshev-smoother`, `jacobi-smoother`, `divfree-projector`, `dot`, `nleps-deflated-residual`, `nrm2`). The report's "insert there" instruction is ambiguous / mis-stated; an integrator following it literally would face an under-determined insertion point. The recommended convention (e.g., append at end of L1-L0 Part, or insert after `jacobi-smoother-mutation-rotation` to mirror the index.md placement) is not specified.

**edge-label-fidelity** — PASS. The slug `reciprocal-elementwise-product-mutation-rotation` and the chapter location `book/src/L1-L0/` correctly mark the L1>L0 lowering direction. The prose narrates forward from L1 (LHS) to L0 (RHS) throughout — both sub-patterns are framed as "the L1 value ... lowers to ... L0 in-place ..." (e.g., lines 102-106 for A; lines 215-219 for B). The dep-map index row uses `firm` correctly. No edge-mismatch detected.

**plan-kind-consistency** — PASS. Status `firm` is justified: both sub-patterns lower into positively-cited L0 sites with no negative-anchor reconstruction; the canonical-vs-consumer-duplicate equivalence (B.1 ↔ B.3 forward branch) is byte-level identity modulo variable rename, which IS firm-grade structural recognition. The firm-on-positive-structure escape for the absent dedicated test (`test-reciprocal.cpp` / `test-elementwise-product.cpp` / `test-diagonal-operator.cpp` — all absent under `reference/palace/test/unit/`) is correctly invoked at lines 676-688: every sub-rule is a syntactic identity on fully-specified positive source, so the missing test does not gate firm-ness. This matches the precedent invocations the report cites (apply_linop, apply_nonlinear_pencil, jacobi-smoother, chebyshev-smoother). The dead-code consumer-transpose branch is correctly flagged as a *caveat (not a status reduction)* at lines 691-706, NOT as a `partly-constructive` qualification — the canonical `MultHermitianTranspose` IS live, only one consumer's copy of it is dead, and the report routes the audit follow-up to D2 (which has independently confirmed template-deadness). Three OQs are filed and two are resolved in the proposed `open-questions.md` edits; the resolution claims align with the OQ-ledger active-block entry at line 830 (the composite candidate WAS routed to abstractor as cycle-034 plan #1, and this dispatch IS that delivery).

**skill-uptake-survey** — PASS. The producer explicitly invoked `tools/citecheck/citecheck.py --anchor` nine times (logged at lines 869-877 under `## Producer self-verification`) — direct, correct invocation of the cycle-024 mechanical citation-check skill. The producer also implicitly satisfied the `proposed-changes-fence-encloses-full-body-guard` skill: the firm body is inside the `new:` fence, no nested ```text fences (the C++ kernels are 4-space-indented instead). The `upgrade-plain-text-ref-to-live-link-when-target-on-disk` skill is correctly satisfied: every plain-text-looking reference whose target is on disk is rendered as a live link (`./jacobi-smoother-mutation-rotation.md`, `../L1/reciprocal.md`, etc.); the only plain-text-by-design references are to the lowering-verifier-on-jacobi-smoother audit (in-flight D2 report — not yet on disk in `book/`) and to `safe_reciprocal` (no L0 anchor — speculative). Telemetry only; not blocking.

### Issues found

1. **SUMMARY.md insertion-point instruction is mis-stated** (`cross-reference-integrity` warning; severity: low-moderate). At CYCLE.md lines 796-801, the `edit:book/src/SUMMARY.md` block asserts the existing L1-L0 SUMMARY ordering is alphabetical and instructs insertion "after `orthogonalize-mutation-rotation` and before `scal-mutation-rotation`". The actual SUMMARY ordering at `book/src/SUMMARY.md:95-120` is integration-cycle-chronological, not alphabetical; `orthogonalize-mutation-rotation` (:100) and `scal-mutation-rotation` (:110) are NOT adjacent — 8 chapters separate them. An integrator following the instruction literally would face an under-determined insertion point. **Where**: `reports/.../CYCLE.md:796-801` (the `edit:book/src/SUMMARY.md` proposed-changes block). **Recommended repair scope**: rephrase the insertion instruction to either (a) append at the end of the L1-L0 Part chapter list, or (b) mirror the index.md placement (after `jacobi-smoother-mutation-rotation` row, before `minres-iteration` row) — line 105 in SUMMARY.md — so the index.md and SUMMARY.md insertion points are co-located. This is a mechanical surface-pointer correction, not a content issue.

(No other issues found. Citation evidence is exceptionally strong — 43 scan-clean citations, 9 anchor-verified pinpoints all independently re-verified, the complex-reciprocal closed-form body and the elementwise-product L0 surface forms checked verbatim against `awk`-on-disk reads. The firm status is well-justified by the firm-on-positive-structure escape; the composite-vs-split decision is well-defended; the variant-axis coverage is exhaustive; the dead-code caveat is consistent with D2's parallel verdict.)

## Repair

### Fixes attempted

- **Finding**: `cross-reference-integrity` warning — `edit:book/src/SUMMARY.md` block at CYCLE.md:796-801 misstates the existing SUMMARY ordering as alphabetical when it is integration-cycle-chronological; the named insertion-point anchors (`orthogonalize-mutation-rotation` :100 and `scal-mutation-rotation` :110) are NOT adjacent (8 chapters between them), leaving the instruction under-determined for the integrator.
- **Decision**: repaired.
- **Action**: Rewrote the SUMMARY.md `edit:` instruction at `CYCLE.md:796-801` in place. Verified the actual current state of `book/src/SUMMARY.md`: the L1>L0 Part runs from :92 to :121 with integration-cycle-chronological ordering. The `jacobi-smoother-mutation-rotation` row sits at :105 and is followed by `divfree-projector-mutation-rotation` at :106. Chose repair option (b) from the critic's recommended menu — mirror the `index.md` insertion position (which CYCLE.md:790 already places after the `jacobi-smoother-mutation-rotation` row at `index.md:42`) so the SUMMARY.md and index.md insertion sites are **co-located alongside the closest semantic sibling** (the jacobi-smoother lowering — they co-occur via `JacobiSmoother::SetOperator`, per the composite-vs-split justification at CYCLE.md:728-745). The new instruction is unambiguous: insert immediately after the `jacobi-smoother-mutation-rotation` row (SUMMARY.md:105) and before the `divfree-projector-mutation-rotation` row (:106). The chapter row body itself is unchanged. The repair touched **only** the surface-pointer prose around the row body; no theme body, citations, or proposed-changes fences were altered. Follows the `summary-md-surgical-insert` skill convention of naming a real adjacent anchor row by slug.
- **Rationale for "repaired" (in scope)**: Mechanical surface-pointer correction. The misnomer ("alphabetical") and the wrong adjacent-anchor pair are both directly contradicted by the current `book/src/SUMMARY.md:92-121` content; the repaired anchor is read directly off disk and matches what is verifiable today. No content authoring required.

### Unrepairable findings

None.

## Suggested resolution

`overall_status: ready`. The remaining 7 critic checks PASSED outright; the single WARNING (SUMMARY.md insertion pointer) is now mechanically repaired against on-disk evidence. The integrator can apply the corrected `edit:book/src/SUMMARY.md` instruction surgically: insert the new chapter row between the existing `jacobi-smoother-mutation-rotation` row (:105) and the `divfree-projector-mutation-rotation` row (:106), co-located with the matching `edit:book/src/L1-L0/index.md` insertion at `index.md:42→43`. The theme body, all 43 citations, and the proposed-changes fences are untouched.
