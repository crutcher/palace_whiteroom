---
agent: cycle-planner
invoked_at: 2026-06-07T134107Z
scope: cycle-126 dispatch plan (batch-40 CLOSER, position 3/3; batch-40 meta fires AFTER this finalize)
status: pending
---

# Cycle 126 dispatch plan

## Goals selected this cycle

c126 is the **batch-40 BATCH-CLOSER** (cycles 124/125/126; the batch-40 meta-phase fires after c126's finalize, aggregating all three). Per the c126 mandate, this cycle lands the **cleanly-closeable first L4 step of the ASK-2 "A" matrix-free deepening** plus the **owed kernel-impl re-audit** — leaving a clean state the meta can reshape into the batch-41 "A then B" head. The two picks: **(1) the L4 `mk_matrix_free_operator` cap as a `roadmap_goal`** with its pull-chain wired (the firm `L4/fe_assemble` spine consumer pulls it to a feature root; it down-links to the firm `L2/matrix-free-operator-apply` combinator) — this deepens the matrix-free layer to L4, the burn/GPU backend-lowering entry point, WITHOUT opening the sprawling multi-cycle "build the L4 backend-lowering surface" front (that continues into batch-41); **(2) the owed lowering-verifier empirical-match re-audit** of the now-firm `libceed-quadrature-kernel-impl` (the `test-libceed.cpp:284` empirical anchor flagged `empirical-anchor-confirmed-deferred` since c124, owed now the impl is firm — DIRECTIVE-3 impl-realizes-API integrity). Both are bounded, clean-gated, and disjoint. The residual RE tail is already burned down (RE3/RE6 discharged c124; RE4 consumer-gated with no batch-40 consumer) — no RE work this cycle. Excluded picks (with disposition) below.

## Dispatches

### D1 (`abstractor`, HIGH fan-out for the cap, WAVE-1) — L4 `mk_matrix_free_operator` backend-lowering operator-constructor cap (roadmap_goal) + the pull-chain

- **agent**: abstractor
- **scope**: Author `book/src/L4/mk_matrix_free_operator.md` as a **`status: roadmap_goal` (rank 0)** L4 backend-lowering operator-constructor chapter — the speculative L4 op deferred from c125 D2 (OQ `mk_matrix_free_operator-l4-backend-lowering-placeholder`). Signature sketch (from the OQ): `mk_matrix_free_operator :: FESpace -> WeakFormTerm -> GeomFactors -> LinearOperator (Tensor[(N: ...)])` — the L4 constructor whose **apply lowers to the tensor-contraction graph** `A = Gᵀ ∘ B_𝒟ᵀ ∘ D ∘ B_𝒟 ∘ G` (the firm `L2/matrix-free-operator-apply` combinator). It builds the matrix-free (un-materialized) `ceed::Operator` representation that `fe_assemble`'s `assembly-representation: partial matrix-free ceed::Operator` variant axis selects (`L4/fe_assemble.md:16,:166`). **Wire the pull-chain (the key clean-gate requirement — do NOT strand it):**
  - **Inbound (the pull to a feature root):** add a faithful **`reference`-class** down-edge from the firm `L4/fe_assemble` → `L4/mk_matrix_free_operator` (kind: `references`/`constructs-via` — abstractor states the chosen label). `fe_assemble` is the firm assemble-half backend-lowering surface that reaches the feature root via its 7 feature-column inbound edges; the matrix-free representation IS the constructive interior of fe_assemble's per-term `assemble_term` leaf under the matrix-free dispatch (`UseFullAssembly` false). NOTE per the graded-stack §1g cap: a `roadmap_goal` may rest on anything and be referenced by a firm node via a `reference` edge (navigational, free, NOT rank-constrained) — so the `fe_assemble → mk_matrix_free_operator` edge is `reference` (a firm node may navigationally reference a roadmap_goal; it must NOT carry a blocking `depends-on` to a rank-0 node, which would violate well-foundedness). Add the §"Downward / lowers-to" down-link from `mk_matrix_free_operator` → `L2/matrix-free-operator-apply` (the firm combinator its apply lowers to — `reference`-class `lowers-to`/`lifts` annotation; the L4 op is the operator-constructor surface, the L2 combinator is the apply-chain composition).
  - **L0 source anchors** (codemap-confirmed; the producer on-disk-confirms, END lines are HINTS): the matrix-free operator wrapper `palace/fem/libceed/operator.hpp:32` (`class Operator : public palace::Operator` — the `ceed::Operator` matrix-free wrapper) + `:48` (`AddSubOperator`) + `:81-82` (`CeedOperatorFullAssemble` — the matrix-free→CSR materialization, the alternative full representation); the dispatch selector `palace/fem/bilinearform.cpp:118` / `:143` (`UseFullAssembly` — order-threshold dispatch between partial matrix-free and full materialized). Cite these as the construction site; the L4 form is representation-agnostic (the variant axis absorbs the partial/full choice, inherited from `L4/fe_assemble.md:166` + `L1/fe_assemble.md:182-187`).
  - **Maturity discipline:** land it `roadmap_goal` (NOT firm) — the L4 backend-lowering feature surface that would make it a firm composition-root is the batch-41 "A" deep work; landing it firm now would over-claim (the OQ is explicit: the pull-chain is partial — `fe_assemble` references it, but the dedicated L4 backend-lowering feature column does not exist yet). The `roadmap_goal` carries intent + the `mk_matrix_free_operator-l4-backend-lowering-placeholder` provenance + the declared deps (the L2 combinator + the substrate). Clean-gate: if the abstractor judges the construction algebra is fully and positively sourced AND the apply-lowers-to-L2 correspondence is a syntactic-identity composition fact, it MAY note a promotion condition, but DEFAULT to roadmap_goal for the cap this cycle (do not force firm).
  - **SUMMARY + index:** register the new chapter in `book/src/SUMMARY.md` (alpha position within the L4 Part's appropriate by-kind group — abstractor inserts, does not append) + add its `L4/index.md` dep-map row (own row + own §Vocabulary-cohort bullet if the index carries one). D1 SOLE-OWNS the `L4/index.md` edits this cycle (no co-dispatched L4-index writer).
- **deps**: none (WAVE-1). Forward-references the firm `L2/matrix-free-operator-apply` (already on disk, c125) + the firm `L4/fe_assemble` (already on disk) — both live, no sibling-dispatch slug coordination needed.
- **rationale**: ASK-2 "A" direction first L4 step (priorities batch-40 item-2 deepening / the `mk_matrix_free_operator-l4-backend-lowering-placeholder` OQ). Deepens the matrix-free layer to L4 (the burn/GPU backend-lowering entry point) as a bounded, single-chapter cap with a wired (not stranded) pull-chain — exactly the "land the cleanly-closeable first L4 step, NOT a sprawling front" mandate. The c126-integrator/finalize materializes the live links; the meta reshapes the remaining L4-depth work into the batch-41 "A" head.

### D2 (`lowering-verifier`, MEDIUM fan-out, WAVE-1) — owed empirical-match re-audit of the now-firm `libceed-quadrature-kernel-impl`

- **agent**: lowering-verifier
- **scope**: Re-audit the DIRECTIVE-3 impl-realizes-API correspondence for `book/src/L1/libceed-quadrature-kernel-impl.md` now that the impl is **firm** (c125 D1). The existing `verified_against:` block (`:232+`) carries the `test-libceed.cpp:284` row at verdict **`empirical-anchor-confirmed-deferred`** (`:261-264`) — the deferred FIRMING-audit target the c124 D2 STRUCTURAL audit explicitly owed ("re-audit owed by lowering-verifier as the impl firms (empirical-match target test-libceed.cpp:284 below)"). **Upgrade the empirical anchor to an `empirical-match` (or `supports`-with-empirical-confirmation) verdict:** confirm on-disk `reference/palace/test/unit/test-libceed.cpp` that `TestCeedOperatorFullAssemble` (`:284`) constructs the libCEED matrix-free assembly and asserts (`:298`) `mat_diff MaxNorm < 1.0e-12 * max(mat_ref MaxNorm, 1.0)` against the MFEM reference — i.e. the matrix-free operator-application impl matches the reference assembly to 1e-12, the empirical evidence that the constructive `A = Gᵀ B_𝒟ᵀ D B_𝒟 G` impl realizes the opaque kernel-api faithfully. Append a NEW dated `verified_against:` row (do NOT rewrite the c124 STRUCTURAL rows — append-after at EOF) recording the firming-audit verdict + the empirical-match confirmation, and confirm: (a) the `realizes-kernel-api` edge stays **`reference`-class** (NOT depends-on — a mis-typing is the DIRECTIVE-3 defect to catch); (b) the kernel-api surface `book/src/L1-L0/fe-assemble-libceed-boundary-obstruction.md` stays `obstruction (opaque-library-ownership)` / role-label `kernel-api`, UNDOWNGRADED; (c) `L4/fe_assemble` stays firm. Closes the deferred-empirical-match half of the kernel-impl audit owed since c124.
- **deps**: none (WAVE-1). Touches ONLY `book/src/L1/libceed-quadrature-kernel-impl.md` (append a verified_against row) — disjoint from D1.
- **rationale**: The owed lowering-verifier re-audit (priorities batch-40 standing gate "Kernel-API/impl integrity"; integrator-signals c125 carry item-3). Cheap, owed, and DIRECTIVE-3-mandated (the impl-realizes-API correspondence must be re-confirmed as the impl firms). A clean batch-closer pick that discharges an explicit owed-audit.

## Overlap analysis

- **D1 ↔ D2**: NON-OVERLAPPING → PARALLEL. D1 creates `book/src/L4/mk_matrix_free_operator.md` and edits `book/src/L4/fe_assemble.md` (one inbound `reference` down-edge in the frontmatter `edges:` block + a §Downward link line), `book/src/L4/index.md` (own dep-map row, D1 sole-owner), and `book/src/SUMMARY.md` (one L4-Part insert). D2 appends ONE `verified_against:` row to `book/src/L1/libceed-quadrature-kernel-impl.md`. No shared file, no shared operator entry, no shared SUMMARY region (D1 = L4 Part; D2 touches no SUMMARY). No consolidated-tally collision (D1 SOLE-OWNS the `L4/index.md` row/count this cycle — no other co-dispatched L4-index writer). Genuinely disjoint.
- No cross-report forward-reference between D1 and D2 (D1's down-link target `L2/matrix-free-operator-apply` and pull source `L4/fe_assemble` are both already-on-disk firm chapters, NOT sibling-dispatch outputs) — no canonical-slug coordination needed.

## Sequencing schedule

- **WAVE 1 (parallel)**: D1, D2.
- ONE `integrator-finalize` at cycle end (rebuild book + commit + push + housekeeping). No wave-2 (no forward-reference ordering dependency between the two dispatches — both reference only already-on-disk firm chapters).

## Deliverable-presence verification

Per the mandatory pre-dispatch four-step check (paste-inline evidence):

**D1 — `book/src/L4/mk_matrix_free_operator.md`** (open by construction — a new L4 chapter; verified genuinely-not-yet-present):
1. File existence: `ls book/src/L4/mk_matrix_free_operator.md` → `ls: cannot access 'book/src/L4/mk_matrix_free_operator.md': No such file or directory` (does NOT exist — open).
2. Maturity: N/A (file does not exist).
3. OQ-ledger RESOLVED-grep: `grep 'mk_matrix_free_operator.*RESOLVED\|mk_matrix_free_operator.*CLOSED' scaffolding/open-questions.md` → `no matches (open)`. The driving OQ `mk_matrix_free_operator-l4-backend-lowering-placeholder` (open-questions.md:2031) is OPEN and explicitly routes the cap to "c126 / batch-41 L4 matrix-free dispatch should land it as a `roadmap_goal`."
4. Structural-block check: NOT blocked. The graded-stack §1g cap PERMITS a roadmap_goal (rank-0) chapter resting on anything; the pull-chain gate (firm `L4/fe_assemble` references it `reference`-class; it down-links the firm `L2/matrix-free-operator-apply`) is satisfiable on disk — `ls book/src/L2/matrix-free-operator-apply.md` exists (`rank: firm`, c125 D2) and `book/src/L4/fe_assemble.md` exists (`firmness: firm`, with the `assembly-representation: partial matrix-free ceed::Operator` variant axis at :16/:166 that this op makes structural). `grep -c "mk_matrix_free" book/src/L4/index.md` → `0` (not yet listed — open). Landing roadmap_goal (NOT firm) is the correct maturity (the dedicated L4 backend-lowering feature column does not exist; firm would over-claim).

**D2 — `book/src/L1/libceed-quadrature-kernel-impl.md`** (verified present + firm + audit-owed):
1. File existence: file EXISTS (`grep` returned content; `rank: firm` at :19).
2. Maturity: `rank: firm` (read on disk, :19) — the impl firmed c125 D1. The proposed deliverable (an `empirical-match` re-audit verdict) is ABOVE the on-disk audit state: the `test-libceed.cpp:284` row is currently `verdict: empirical-anchor-confirmed-deferred` (:262), explicitly flagged as "the empirical-match target for the FIRMING audit ... owed now that the impl is firm." NOT a no-op — the audit upgrade is genuinely owed.
3. OQ-ledger RESOLVED-grep: the audit is tracked as the standing DIRECTIVE-3 integrity gate (priorities batch-40 standing gate "Kernel-API/impl integrity" + integrator-signals c125 carry item-3), not a discrete OQ slug — `grep` for `libceed-quadrature-kernel-impl.*RESOLVED` → no closure (the firming re-audit is unrun).
4. Structural-block check: NOT blocked. The impl is firm and positively sourced; the empirical anchor `test-libceed.cpp:284`/`:298` exists on disk (cited in the deferred row). DIRECTIVE-3 expressly mandates the lowering-verifier audit the impl-realizes-API correspondence; no methodology gate blocks it.

STOP-PROPOSING negative-list check: neither D1 (`mk_matrix_free_operator`) nor D2 (`libceed-quadrature-kernel-impl` audit) matches any disqualified slug (`lu_solve`/`back_solve`/`ls-update-column`/`nleps_deflated_*`/`nleps_jacobian_action`/`nleps_eigenvalue_correction`). Both clear.

## Excluded candidates (with disposition)

- **"Matrix-free as first-class L1" (the L1 leg of the matrix-free deepening)** — EXCLUDED (no-op / already landed). The L1 leg is already covered: `book/src/L1/libceed-quadrature-kernel-impl.md` (firm, c125 D1) IS the L1 matrix-free operator-application chapter, the four element-local substrate ops (`element_restrict`/`basis_apply`/`quad_point_contract`/`geom_factor_build`) are all firm (c124/c125), and sum-factorization is already classified as a transparent-trick sub-axis (`libceed-quadrature-kernel-impl.md:115`) + geometry-factor application is the firm `L1/geom_factor_build`. A separate "matrix-free L1" dispatch would re-author existing firm content. The matrix-free deepening's genuinely-open frontier is UPWARD (L4, D1), not at L1.
- **L2/index prose-vs-dep-map-row firm-count gap reconcile** — DEFERRED to the batch-40 meta (explicit meta-territory). The integrator-finalize c125 signal flagged this as "a count-reconcile hygiene candidate **for the meta**"; `L2/index.md:95` prose says `23 firm` while the dep-map TABLE firm-row count diverged (~19-20, pre-existing, surfaced by D2). A consolidated-tally / count-prose reconcile that sums over the whole cohort is exactly the batch-level housekeeping the meta-phase owns (my role-spec: I do NOT do batch-level intake migration / count reconcile; the meta fires immediately after this finalize). Leaving it to the meta avoids a low-value dispatch in the closer and keeps the count-owner single (the meta).
- **`record-MultigridConfig-needs-definition-home`** — DEFERRED (2nd-consumer gate not fired; confirmed c125 — exactly 1 consumer on disk). Unchanged this cycle.
- **RE work** — none. RE3/RE6 discharged c124; RE4 is consumer-gated with no batch-40 consumer (priorities batch-40 standing gate). No RE fires or grounds this cycle by construction (D1 adds a `reference`-class edge from a firm node to a roadmap_goal — navigational, no reachability flip; D2 is an audit, no graph change).

## Open questions / caveats

- **`mk_matrix_free_operator` maturity judgment is delegated to the abstractor within a default.** I scope it as `roadmap_goal` (the OQ-mandated default + the no-over-claim safe choice for the batch-closer). If the abstractor finds the construction algebra fully positively-sourced AND the apply-lowers-to-L2 correspondence a clean syntactic-identity composition fact, it MAY record a promotion condition — but it should NOT force firm this cycle (the dedicated L4 backend-lowering feature column that would make it a firm composition-root is batch-41 "A" work). Flagging for the critic/meta: confirm the roadmap_goal disposition is the right cap maturity, or whether a firm-on-positive-structure cap is warranted (a batch-41-meta codification question if it recurs).
- **The `fe_assemble → mk_matrix_free_operator` edge MUST be `reference`-class, not `depends-on`.** A firm node carrying a blocking `depends-on` to a rank-0 roadmap_goal would VIOLATE well-foundedness (`rank(fe_assemble)=firm > rank(mk_matrix_free_operator)=0`). The pull-to-root is via `reference` (navigational, free, NOT rank-constrained) — stated explicitly in D1's scope. The c126 critic / linter should confirm `rank_violations: 0` HOLDS on the landed tree (the reference edge does not constrain rank).
- **Batch-40 carryover for the meta (NOT this cycle's work):** the L2/index count reconcile (above), the `graded-stack-baseline-exceptions.md` RE3/RE11/RE6 update (meta write-territory, carried from c124, still un-updated per the c125 finalize), the "deleted-slug frontmatter-edge sweep" codification (owed, did not recur c125/c126 as no deletions), and the batch-41 "A then B" head reshape. These are the meta's standing close-out; I note them so the meta's intake is primed.
