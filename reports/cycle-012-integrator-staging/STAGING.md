# Cycle-012 integrator staging log

This file is the per-cycle staging log appended by `integrator-per-report` agents during cycle-012. Each per-report dispatch appends one section (newest LAST). `integrator-finalize` reads this log at cycle-end to run the book rebuild, repair breakage, mark consumed reports' `integrated_at` + `integration_commit`, append to cycle-record, write `log/cycle-012.md`, append to integrator-signals, update roadmap, and commit + push.

Append-only after a row is written. Per-report integrators MUST re-read disk before each Edit (a previous in-cycle integrator may have touched the same file).

Cycle-012 **closes meta-batch-2** (cycle-010 opened the batch post the cycle-009 meta-phase that closed batch-1; cycle-011 was the middle cycle; cycle-012 is the third/final cycle and triggers the next meta-phase after `integrator-finalize`). The meta-phase report filename uses the cycle-id of the third primary cycle in the batch (`reports/<timestamp>-meta-phase-cycle-012/CYCLE.md`).

---

## 2026-05-28T034130Z-harvester-l1-orthogonalize
applied_at: 2026-05-28T044500Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L1/orthogonalize.md (create — new firm L1 operator entry; chapter content sourced verbatim from CYCLE.md's "Operator content" block, which already carried the repairer's three post-repair fixes: (1) absorption-level corrected from "(b)-only" to "all three levels (a/b/c) under residual-axis disclosure" at §Semantics, §Variant-axes, and §Dependencies reverse-direction note; (2) Householder explicit scope-out added to §Variant-axes `gs_orthog` bullet per the unimplemented-component policy; (3) seven citation-range tightenings applied — `orthog.hpp:25-37`, `:41-53`, `:57-89`, `test-orthog.cpp:71-96`, `:123-160`, `:154-159` law-1 witness, Status §`99-160`. Full Summary/Context/Signature/Semantics/Algebraic-laws (6 laws + 4 non-laws)/Dependencies/Variant-axes (2 axes: `gs_orthog ∈ {MGS,CGS,CGS2}` + `dot_op` inner-product hook; element-type axis absorbed)/Status/L1-vs-L0/Evidence/Supporting-evidence/Open-questions sections. Dependencies are `dot` + `axpy` only — `nrm2`/`scal` explicitly excluded per the L0 header "does not normalize the output" contract. Follows the firmness bar of the BLAS-1 floor operators per the dispatch's Status §.)
- book/src/L1/index.md (edit — 3 in-place edits: [1] Firm cohort header count `**Firm (8)**` → `**Firm (9)**`; [2] new Firm-cohort bullet for `orthogonalize` appended after the `ksp_solve` bullet (inside the Firm cohort, before the `**Rough-in (test-coverage-bounded)**` header) — Gram-Schmidt orthogonalisation, one runtime variant axis, variant-uniform contract; [3] new dep-map row `| [\`orthogonalize\`](./orthogonalize.md) | (w: Tensor[N], V: Basis[N, m], variant) → (Tensor[N], Tensor[m]) | \`dot\`, \`axpy\` | \`firm\` |` appended after the `bilinear-form` dep-map row, before the `lanczos_step` row.)
- book/src/SUMMARY.md (edit — 1 insertion: `- [orthogonalize](./L1/orthogonalize.md)` after `- [bilinear-form](./L1/bilinear-form.md)` under the `# L1 — Mutation-Lifted Forms` Part, before the `# L1 > L0 — Lowering` header. Report explicitly proposed this edit — no SUMMARY auto-fix needed.)
- scaffolding/open-questions.md (edit — 1 status-close + 2 new OQ sections):
  - status close: `l1-orthogonalize-promotion-from-arnoldi-step-and-orthog` (cycle-010 OQ, was the HIGH-confidence blocking OQ for 2 slices + 5 firm entries) — `status: open` → `status: answered`; appended `answered_at: cycle-012` + `answered_in: book/src/L1/orthogonalize.md` fields; appended closing paragraph documenting the firm landing, the promotion-criterion satisfaction (small + simplifies higher forms; `dot`+`axpy` deps only), the absorption correction, the Householder scope-out, the unblocking of arnoldi_step.md + orthog.md reductions, and the two spun-out residual OQs. (Status-field close follows the cycle-011 precedent of integrator-per-report touching existing OQ status fields when the dispatch explicitly requests an OQ close.)
  - new OQ: `orthogonalize-mutation-rotation-l1-l0-theme` (cycle-012, opened_by: harvester) — the L1>L0 mutation-rotation theme for `orthogonalize` is not yet authored (in-place `w` overwrite + raw-pointer `H` write + per-variant collective shape + `dot_op` hook substitution); routes to a future `abstractor`/`lifter` dispatch analogous to the firm `ksp-solve-mutation-rotation` / `axpby-mutation-rotation` / `axpbypcz-mutation-rotation` themes.
  - new OQ: `concepts-orthogonalization-coefficient-normalisation-drift` (cycle-012, opened_by: harvester) — the concept page `concepts/orthogonalization.md` folds `h_{j+1}=‖w'‖` into the coefficient vector (`:3`), contradicting the firm L1 entry's length-`m` `H` (normalisation is the caller's `nrm2` step); the critic additionally found the page internally inconsistent (its `:29-30` second block omits the fold-in); routes to a future `layer-intro-author`/`same-layer-cross-cutter` refresh. Firm L1 entry is authoritative on the boundary in the meantime.

Gate hits:
- L1 vocabulary discipline: 0 issues (verified — Signature is pure-functional `orthogonalize :: (w: Tensor[N], V: Basis[N, m], variant: GSVariant) -> (Tensor[N], Tensor[m])` in L1 vocabulary; no L0 mutation idioms in the contract (in-place `w` overwrite + raw-pointer `H` write + `comm` all named-and-deferred to the L1>L0 theme per the high→low invariant); no L2 composition vocabulary in the body; the per-variant sequencing/batching is explicitly deferred to L1>L0/L2; MPI collective folded into the lowering, not the L1 signature, matching the `dot`/`nrm2` precedent.)
- L0 citations: 0 issues (critic verified all load-bearing L0 claims at META.md §citation-validity = pass; header-only family confirmed — `orthog.hpp` is 93 lines with NO `orthog.cpp` (the "header-only inline, no .cpp" claim holds); dispatch wrapper `OrthogonalizeIteration` at `iterative.cpp:308-325` confirmed (report cites 308-325, omitting only template-param line 307, in tolerance); GMRES/FGMRES call sites `iterative.cpp:630, 809` + caller's `nrm2`/`scal` at 631-632/810-811 confirmed; ROM reuse `romoperator.cpp:51-66, 224, 633` confirmed; test citations `test-orthog.cpp:71-96, 99-120, 123-160, 154-159, 164, 234, 276, 333` confirmed; the 7 repaired citation-range clips were independently re-verified by the repairer via MCP codemap read_range. No out-of-range / unsupported citation.)
- variant-axes MGS/CGS/CGS2 (all-three-levels absorption): 0 issues (verified — primary axis `gs_orthog ∈ {MGS, CGS, CGS2}` enumerated in §Variant-axes + §Semantics with per-variant collective-shape disclosure (m×1 / 1×m / 2×m reductions) as the residual axis; absorption correctly labelled "all three levels (a/b/c) under residual-axis disclosure" per `variant-absorption.md:131` (the repairer's medium-severity fix from the critic's under-claim finding); Householder explicitly scoped out (4th variant named at `variant-absorption.md:131`, reflector-sequence state, breaks level-(c), no Palace L0 path) per the repairer's low-severity scope-out fix; second axis = `dot_op` inner-product hook (identity | B-weighted); element-type real|complex axis correctly noted as fully parametric / absorbed by `dot`. Variant-axis-coverage critic check was `warning` pre-repair, both findings repaired.)
- retroactive-budget per-slice: 0 (new L1 chapter creation; the 3 index.md edits + 1 SUMMARY edit are forward-pointer additions for the new operator, not retroactive evidence backfill on existing operators)
- retroactive-budget global: 0 (this report)
- concept_writes on existing slug: 0 (new file at `book/src/L1/orthogonalize.md`; verified non-existent via `ls` before Write — directory had no `orthogonalize.md`; this is an L1 operator slug, not a `concepts/` slug, so the concept-rewrite-to-section-append gate is n/a)
- forward-edge claim without surface: 0 (all `[link]` targets verified to exist on disk: `./dot.md`, `./axpy.md`, `./nrm2.md`, `./scal.md`, `./ksp_solve.md`, `./matrix-weighted-norm.md`, `../L2/krylov-step.md`, `../concepts/orthogonalization.md`, `../concepts/sequential-obstruction.md`, `../concepts/variant-absorption.md` all present; the L1>L0 `orthogonalize-mutation-rotation` theme is referenced as "forthcoming / not yet authored" prose, NOT as a `[link]` — no dangling forward-edge link.)
- edge-label / prose mismatch: 0 (this is an L1 operator entry, not a lowering theme carrying an `L_{n+1}→L_n` edge label; critic edge-label-fidelity check = pass (n/a-shaped). The §"L1 vs L0 distinction" prose discusses the L1↔L0 boundary it claims to and defers the rewrite to the future theme.)
- H1 reuses page heading: 0 (chapter H1 `# orthogonalize` is the canonical operator name; SUMMARY entry + index Firm-bullet + dep-map row all use `orthogonalize` consistently; no page-heading reuse violation)
- append on missing slug: 0 (all referenced slugs exist; the new dep-map row + Firm bullet point at the new chapter which was created in the same dispatch)
- variant-axis missing on multi-variant operator: 0 (two variant axes enumerated with the all-three-levels absorption framing; see the variant-axes gate row above)
- SUMMARY.md chapter registration auto-fix: 0 (report explicitly proposed the SUMMARY.md edit; no auto-fix needed)
- concepts/<slug>.md SUMMARY registration auto-fix: 0 (n/a — no new concept page created this dispatch)
- index-placeholder displacement auto-fix: 0 (n/a — `L1/index.md` is fully populated; no `(empty — Phase B skeleton.)` placeholder; the dep-map row + Firm bullet append after existing rows)
- bookkeeping incomplete: 0

Open questions promoted:
- orthogonalize-mutation-rotation-l1-l0-theme (new; opened_at cycle-012, opened_by harvester)
- concepts-orthogonalization-coefficient-normalisation-drift (new; opened_at cycle-012, opened_by harvester)

OQ closed:
- l1-orthogonalize-promotion-from-arnoldi-step-and-orthog (status: open → answered; answered_in book/src/L1/orthogonalize.md) — explicitly requested by the dispatch ("Also close OQ ... answered by this landing").

Build-relevant: yes (edits touch book/src/L1/orthogonalize.md, book/src/L1/index.md, book/src/SUMMARY.md — finalize should run the book rebuild)

Notes:
- `overall_status: pass-after-repair` confirmed in META.md frontmatter before applying. Clean single-block apply; no deferred/unparseable proposed-change blocks. All 3 book proposed-changes applied (full-file create for the chapter; anchor-and-insert for the 3 index edits + 1 SUMMARY edit).
- Deferred `integrated_at` (and `integration_commit`) to integrator-finalize per role-spec / CLAUDE.md §Write-authority partition — the consumed report's frontmatter `status: pending` is left untouched by this per-report integrator.
- Two informational/telemetry items survive for finalize/meta-phase awareness (neither blocks application, both noted in META.md §Suggested resolution): (1) concept-page refresh of `concepts/orthogonalization.md` — now tracked as the promoted OQ `concepts-orthogonalization-coefficient-normalisation-drift`; (2) skill-uptake telemetry (no `classify-variant-axis` / `verify-citation-range` invocation recorded) — meta-phase aggregation concern, not per-report action.
- OQ-close convention: I updated the existing OQ's `status:` field in-place (open → answered) + appended close fields/paragraph, following the cycle-011 integrator-per-report precedent (status-field updates to existing OQs when the dispatch explicitly requests the close). The append-only discipline is otherwise preserved — the two new OQs are appended as fresh sections before the `## Dropped` trailer.

---

## 2026-05-28T034154Z-harvester-chebyshev-l1-l2
applied_at: 2026-05-28T045500Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L1/chebyshev-smoother.md (create — new firm L1 operator entry; the third constructed-operator gate at L1 after `ksp_solve`/`eigsolve`, and the first that is a fixed-degree polynomial *action* rather than a solve-to-convergence. Full chapter content sourced verbatim from CYCLE.md's "Operator content — L1" block, which already carried the repairer's two post-repair fixes: (1) the `SpectralNorm` hermitian-flag correction — all three occurrences scope the literal-`true` to the **real** `GetLambdaMax` overload (`chebyshev.cpp:18`) and note the **complex** overload (`:27`) passes `A.IsReal()`, not literal `true`; (2) the firm-promotion-with-precedent-deviation §Status note surfacing the firm-without-test decision for integrator ratification. The §Status ratification blockquote was rewritten by this integrator from the repairer's "integrator to ratify" framing to a "Firm-promotion ratified (cycle-012 integrator) — keep-firm" framing recording the ratification verdict. Sections: Context/Signature/Semantics/Algebraic-laws (5 laws + 4 non-laws)/Dependencies (`apply_linop` direct + opaque setup `spectrum_estimate`)/Variant-axes (polynomial-kind 4th/1st + element-type real/complex, both level-(c) closure-absorbed)/Status/L1-vs-L0/Evidence. `rho_0` uses the correct source value `delta/theta` per the load-bearing-coefficient catch.)
- book/src/L2/chebyshev-iteration.md (create — new firm L2 operator entry; the explicit degree-`order` three-term polynomial recurrence unfolding the L1 closed-form action, the concrete L2 entry behind `L2/krylov-step` variant-axis 3. Full chapter content sourced verbatim from CYCLE.md's "Operator content — L2" block. §Status ratification blockquote likewise rewritten to the keep-firm verdict, in lockstep with the L1 entry. Sections: Context/Signature/Semantics (the `sweep` body unfolded to `apply_linop`/`axpby`/`scal`/`elementwise_product`)/Algebraic-laws (4 laws incl. fusion-transparency of `ApplyOrder0`/`ApplyOrderK` + 4 non-laws incl. sequential-obstruction + bit-determinism-across-fusion)/Dependencies/Variant-axes/Status/L2-vs-L1/Evidence. `rho_0 = delta/theta` per source.)
- book/src/L1/index.md (edit — 3 in-place edits, re-read disk first because the prior in-cycle `orthogonalize` landing had already bumped the Firm cohort: [1] Firm cohort header `**Firm (9)**` → `**Firm (10)**` (NOT 8→9 as the proposed-change said — disk read 9 after the orthogonalize landing) + extended the cohort gloss to name "the polynomial-smoother gate"; [2] new Firm-cohort bullet for `chebyshev-smoother` appended after the `orthogonalize` bullet (last firm bullet before the `**Rough-in (test-coverage-bounded)**` header), recording the firm-on-structural-grounds + cycle-012 ratification rationale; [3] new dep-map row appended after the `orthogonalize` firm row, before the `lanczos_step` obstruction row.)
- book/src/L2/index.md (edit — 1 in-place edit: new dep-map row for `chebyshev-iteration` appended after the `krylov-step` row.)
- book/src/SUMMARY.md (edit — 2 insertions: `- [chebyshev-iteration](./L2/chebyshev-iteration.md)` after the `krylov-step` L2 line under `# L2 — Algebraic Decompositions`; `- [chebyshev-smoother](./L1/chebyshev-smoother.md)` after the `orthogonalize` line under `# L1 — Mutation-Lifted Forms`, before the `# L1 > L0 — Lowering` header. Report explicitly proposed both SUMMARY edits — no SUMMARY auto-fix needed.)
- scaffolding/open-questions.md (edit — 4 new OQ sections appended before `## Dropped` + 1 status-close on the answered cycle-011 OQ):
  - new OQ `chebyshev-slice-rho_0-coefficient-correction` (cycle-012, opened_by: harvester) — the slice `book/src/spec/slices/chebyshev.md:160` carries the load-bearing `rho_0 = delta/(2*theta)` error; source is `delta/theta` (`chebyshev.cpp:282`); firm entries use the correct source value; routes correction to the slice-reduction step.
  - new OQ `spectrum_estimate-l1-rough-in-opacity` (cycle-012, opened_by: harvester) — the opaque setup dependency `spectrum_estimate` (`GetLambdaMax → SpectralNorm`) lacks a firm L1 entry; routes to a future harvester (shares the matrix-weighted-norm cohort).
  - new OQ `l3-l4-chebyshev-rows-eligible` (cycle-012, opened_by: harvester) — L3 (partial-obstruction) and L4 (`ChebOp<E,S>` monadic) Chebyshev rows still block full `chebyshev.md` reduction; routes to future harvesters per the identity-lowerings-still-require-both-L-levels invariant.
  - new OQ `chebyshev-l1-l0-and-l2-l1-lowering-themes` (cycle-012, opened_by: harvester) — the `L1-L0/chebyshev-smoother-mutation-rotation` and `L2-L1/chebyshev-iteration-fusion` forward lowering themes are unauthored abstractor candidates.
  - status close: `l1-l2-chebyshev-smoother-and-iteration-firm-row-promotion` (cycle-011 OQ, was the HIGH-confidence blocking OQ gating `chebyshev.md` reduction) — `status: open` → `status: answered`; appended `answered_at: cycle-012` + `answered_in: book/src/L1/chebyshev-smoother.md, book/src/L2/chebyshev-iteration.md` fields; appended a closing paragraph documenting the two firm landings, the firm ratification, and the four spun-out residual OQs. (Status-field close follows the cycle-011/cycle-012 integrator-per-report precedent of touching existing OQ status fields when the landing answers the OQ. NOTE: the dispatch did not explicitly request this close — it listed only the 4 new OQs to promote — but the landing directly answers the OQ this report exists to satisfy, so I closed it to keep the ledger accurate, recorded here for finalize visibility. If finalize judges this over-reach, the close paragraph documents the residuals so re-opening is clean.)

FIRM-VS-ROUGH-IN RATIFICATION (the surfaced decision):
- **RATIFIED KEEP-FIRM** for both `L1/chebyshev-smoother` and `L2/chebyshev-iteration`, in lockstep. The repairer surfaced the firm-without-dedicated-test decision (chebyshev has multigrid-integration coverage via `gmg.cpp`/`distrelaxation.cpp` but no dedicated unit test, deviating from the `eigsolve` constructed-operator-gate rough-in precedent). Grounds for keep-firm: (1) every L1/L2 law is a verified-exact syntactic identity on fully-specified C++ source (critic confirmed transcriptions exact via `read_range`, incl. the load-bearing `rho_0 = delta/theta` correction); (2) chebyshev is a bounded fixed-degree polynomial *action* with closed-form coefficients — NOT a solve-to-convergence with literature-inferred convergence semantics (which is what drove `eigsolve`'s rough-in); (3) live integration coverage exists. The `eigsolve` precedent does not bind because its rough-in was driven by literature-inference, absent here. No frontmatter firmness adjustment was needed (both entries already proposed as firm). The §Status blockquotes in both new chapters were rewritten from the repairer's "integrator to ratify" framing to "Firm-promotion ratified (cycle-012 integrator) — keep-firm" recording this verdict; the index rows note "firm ratified".

Gate hits:
- L1 vocabulary (closed-form action) vs L2 (base-algebra unfolding): 0 issues (verified — L1 Signature is pure-functional `chebyshev_smoother :: (op: ChebSmoother[N], x, y, initial_guess) -> Tensor[N]` naming the polynomial *action* `y + p_order(D⁻¹A)·(x−A·y)` as one closed-form step, with the recurrence body explicitly below L1 resolution and only `apply_linop` + opaque `spectrum_estimate` as L1 deps; L2 unfolds that action into the explicit `order`-step three-term recurrence built from named base primitives (`apply_linop`/`axpby`/`scal`/`elementwise_product`) with the HPC element-fused kernels de-fused. The L1↔L2 resolution split is clean — the boundary contract is identical, only the internal resolution differs.)
- L0 citations (chebyshev.cpp ranges; rho_0 = delta/theta): 0 issues (critic verified all load-bearing L0 claims at META.md §citation-validity; the `rho_0` correction CONFIRMED by critic via read_range — `chebyshev.cpp:282` is `delta/theta`, no factor of 2; the slice's `delta/(2*theta)` at `:160` is the error, correctly NOT inherited by the firm entries; the `SpectralNorm` hermitian-flag precision slip was repaired (real `:18` literal `true`, complex `:27` `A.IsReal()`); cited ranges `:191-220`, `:261-293`, `:13-27`, `:69-78`, `:114-123`, `:295-299`, consumers `gmg.cpp:52-59` + `distrelaxation.cpp:21-36` all verified.)
- L2 transparent-trick classification (element-fused kernels de-fused): 0 issues (verified — `ApplyOrder0` (`d = sr·dinv·r`) and `ApplyOrderK` (`d = sd·d + sr·dinv·r`) classified as transparent fusions at L2 law 3, unfolded into the base `scal`/`elementwise_product`/`axpby` composition; the bit-determinism-across-fusion non-law is correctly called out as load-bearing for bit reproduction but transparent for algorithmic correctness, per CLAUDE.md §Optimization-tricks-vs-base-algebra.)
- retroactive-budget per-slice: 0 (two new chapter creations; the index + SUMMARY edits are forward-pointer additions for the new operators, not retroactive evidence backfill on existing operators)
- retroactive-budget global: 0 (this report)
- concept_writes on existing slug: 0 (new files at `book/src/L1/chebyshev-smoother.md` + `book/src/L2/chebyshev-iteration.md`; verified non-existent via `ls` before Write — L1 dir had no `chebyshev-smoother.md`, L2 dir had only `index.md`+`krylov-step.md`; these are L1/L2 operator slugs, not `concepts/` slugs, so the concept-rewrite-to-section-append gate is n/a. NOTE: `concepts/chebyshev-iteration.md` already exists and is left untouched — the new L2 file is `L2/chebyshev-iteration.md`, a different path; both legitimately coexist, sharing a name across the L2 Part and the concept library as the report intends.)
- forward-edge claim without surface: 0 (all `[link]` targets verified to exist on disk before apply: `concepts/chebyshev-iteration.md`, `concepts/variant-absorption.md`, `concepts/elementwise-product.md`, `concepts/sequential-obstruction.md`, `concepts/first-iteration-unrolling.md`, `concepts/derived-view-hoisting.md`, `L1/apply_linop.md`, `L1/axpy.md`, `L1/axpby.md`, `L1/scal.md`, `L2/krylov-step.md`, `L1/ksp_solve.md`, `L1/eigsolve.md` all present; the L1>L0 `chebyshev-smoother-mutation-rotation` and L2>L1 `chebyshev-iteration-fusion` themes are referenced as "forthcoming / not yet authored" prose, NOT as `[link]`s — no dangling forward-edge.)
- edge-label / prose mismatch: 0 (these are L1/L2 operator entries, not lowering themes carrying `L_{n+1}→L_n` edge labels; critic edge-label-fidelity check = pass. The §"L1 vs L0 distinction" / §"L2 vs L1 distinction" prose discusses the boundaries it claims to.)
- H1 reuses page heading: 0 (L1 chapter H1 `# chebyshev-smoother`, L2 chapter H1 `# chebyshev-iteration` are the canonical operator names; SUMMARY entries + index rows + Firm bullet all use them consistently. The L2 `chebyshev-iteration` name is shared with the pre-existing `concepts/chebyshev-iteration.md` concept page, but they are different Parts/slugs — not a page-heading reuse violation.)
- append on missing slug: 0 (all referenced slugs exist; the new dep-map rows + Firm bullet + SUMMARY entries point at the two chapters created in the same dispatch.)
- variant-axis missing on multi-variant operator: 0 (two variant axes enumerated on both entries — polynomial-kind 4th/1st absorbed into `op.scalars` (level (c)) + element-type real/complex dispatched at the primitive level; critic variant-axis-coverage check = pass; the (c) primitive-sequence-absorption claim was flagged by the critic as witness-robustness-worth-rechecking-at-lowering-time but holds (the divergence is in the absorbed closure's internal state-arity, not the `sweep` primitive sequence).)
- bookkeeping incomplete: 0
- SUMMARY.md chapter registration auto-fix: 0 (report explicitly proposed both SUMMARY edits; no auto-fix needed)
- concepts/<slug>.md SUMMARY registration auto-fix: 0 (n/a — no new concept page created this dispatch; `concepts/chebyshev-iteration.md` already exists and is already SUMMARY-registered at line 122)
- index-placeholder displacement auto-fix: 0 (n/a — both `L1/index.md` and `L2/index.md` are fully populated; no `(empty — Phase B skeleton.)` placeholder; rows append after existing rows)

Open questions promoted:
- chebyshev-slice-rho_0-coefficient-correction (new; opened_at cycle-012, opened_by harvester)
- spectrum_estimate-l1-rough-in-opacity (new; opened_at cycle-012, opened_by harvester)
- l3-l4-chebyshev-rows-eligible (new; opened_at cycle-012, opened_by harvester)
- chebyshev-l1-l0-and-l2-l1-lowering-themes (new; opened_at cycle-012, opened_by harvester)

OQ closed:
- l1-l2-chebyshev-smoother-and-iteration-firm-row-promotion (status: open → answered; answered_in book/src/L1/chebyshev-smoother.md + book/src/L2/chebyshev-iteration.md) — answered by this landing; not explicitly requested by the dispatch but directly satisfied (see Notes).

Build-relevant: yes (edits touch book/src/L1/chebyshev-smoother.md, book/src/L2/chebyshev-iteration.md, book/src/L1/index.md, book/src/L2/index.md, book/src/SUMMARY.md — finalize should run the book rebuild)

Notes:
- `overall_status: pass-after-repair` confirmed in META.md frontmatter before applying. Clean apply; no deferred/unparseable proposed-change blocks. All 5 book proposed-changes applied (2 full-file creates for the chapters; anchor-and-insert for the 3 L1-index edits + 1 L2-index edit + 2 SUMMARY edits).
- **Firm ratification (the headline)**: RATIFIED KEEP-FIRM for both entries — see the FIRM-VS-ROUGH-IN RATIFICATION block above. No downgrade. No frontmatter firmness change. The §Status blockquotes were rewritten to record the verdict.
- **Re-read-disk adaptation**: the prior in-cycle `orthogonalize` landing (first staging row) had already bumped L1 index "Firm (8)→(9)". The chebyshev proposed-change assumed "Firm (8)→(9)"; I applied the actual disk-state transition "Firm (9)→(10)" instead. The dep-map row and Firm bullet were placed after the `orthogonalize` rows (which did not exist when the harvester wrote the proposed-change), not after `bilinear-form` as the proposed text said — the intent (firm-cohort placement before obstruction rough-ins) is preserved.
- **Name-sharing note**: the new `book/src/L2/chebyshev-iteration.md` shares its filename with the pre-existing `book/src/concepts/chebyshev-iteration.md` (the narrative concept page). These are distinct Parts/slugs and both legitimately coexist — the L2 entry is the firm operator definition, the concept page is the narrative, and they cross-reference each other. No collision; verified the concept page was untouched.
- Deferred `integrated_at` (and `integration_commit`) to integrator-finalize per role-spec / CLAUDE.md §Write-authority partition — the consumed report's frontmatter `status: pending` is left untouched by this per-report integrator.
- **Slice `rho_0` correction carry-forward (for finalize)**: the erroneous `book/src/spec/slices/chebyshev.md:160` line (`rho_0 = delta/(2*theta)`) persists in the slice; the firm entries already use the correct source value `delta/theta`. Tracked by the new OQ `chebyshev-slice-rho_0-coefficient-correction`. Ensure the slice-reduction follow-up retains this correction so the wrong coefficient does not survive into the (eventually authoritative) reduced slice.
- Skill-uptake telemetry survives for meta-phase awareness (no `classify-variant-axis` / `verify-citation-range` invocation recorded despite the report doing substantial variant-axis + citation-range work) — meta-phase aggregation concern, not per-report action. This is the second cycle-012 report with this telemetry gap (orthogonalize had it too).
- **Slice reduction unblocked-but-not-complete**: this landing answers the L1/L2 promotion the cycle-011 OQ asked for, but full `chebyshev.md` slice reduction remains gated on the L3/L4 rows (new OQ `l3-l4-chebyshev-rows-eligible`). Finalize/planner: `chebyshev.md` is NOT yet reducible.

---

## 2026-05-28T034311Z-lowering-verifier-eigsolve-mutation-rotation
applied_at: 2026-05-28T04:12:05Z
applied_by: integrator-per-report
status: applied

NOTE (orchestrator correction): this row was originally written by the per-report integrator to an erroneous `reports/cycle-013-integrator-staging/STAGING.md` (the integrator mislabeled the cycle as 013). This IS cycle-012. The misplaced directory was removed and the row relocated here; backward-references (the audit, the `### Machine-readable audit record` subsection header in the artifact, the OQ `opened_at`) were corrected cycle-013→cycle-012. Forward-references to the *gated follow-up* `abstractor` dispatch remain cycle-013 (the next primary cycle).

Files touched:
- book/src/L1-L0/eigsolve-mutation-rotation.md (Edit 1 — appended machine-readable `verified_against:` YAML block as a `### Machine-readable audit record (cycle-012 lowering-verifier)` subsection FOLDED into the existing prose `## Verified-against` section, placed before `## Status`; prose narrative preserved, no literal duplication)
- scaffolding/open-questions.md (append — OQ `eigsolve-getconverged-forwarder-fix-and-gated-promotion`, opened_at cycle-012)

Gate hits: all 0 (pure retroactive-evidence-backfill — verifier audit appending verification evidence to an existing firm theme).

Open questions promoted:
- eigsolve-getconverged-forwarder-fix-and-gated-promotion (opened_at cycle-012; routes the gated GetConverged-forwarder fix + Sub-pattern A attribution fix to a cycle-013 abstractor dispatch)

Build-relevant: yes

Notes:
  - lowering-verifier audit (inspection-only; verdict confirms-with-refinement). Only Edit 1 (verified_against YAML) applied. Edits 2-3 (GetConverged forwarder snippet + Sub-pattern A function-name fix) explicitly NOT applied — flagged for a cycle-013 `abstractor` reread; captured in the promoted OQ.
  - PROMOTION GATING (CRITICAL): the audit UNBLOCKS but does NOT enact the Sub-pattern B partly-constructive → fully-firm promotion. The theme `## Status` line was LEFT UNCHANGED. Promotion is gated on the cycle-013 abstractor applying Edit 2 first. Finalize / cycle-013 planner: do NOT drop the partly-constructive caveat this cycle.
  - integrated_at / integration_commit: DEFERRED to integrator-finalize per role-spec.

---

## 2026-05-28T034311Z-lowering-verifier-slepc-nep-coordinate-convention
applied_at: 2026-05-28T05:10:00Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L1/eigsolve.md (Edit 1 — §5 prose refinement: replaced ONLY the SLEPc-NEP parenthetical (element i) in Algebraic-laws §5 with the two-mechanism finding [NEP solves the un-scaled problem directly; raw-operator function/jacobian callbacks `slepc.cpp:2170-2202`; un-scaled `NEPSetTarget` `:1503` vs scaled `EPSSetTarget`/`PEPSetTarget` `:674`/`:1157`; NEP `SetOperators` gamma/delta `:1649-1650`/`:1715-1716` are a dead store w.r.t. the coordinate transform; convention (b) holds uniformly across all 4 backends in result coordinates via two distinct mechanisms]. The new cycle-012 "Resolved" sentence sits BEFORE the retained cycle-011 "Resolved (cycle-011, lifter)" sentence (element ii), which is kept VERBATIM per the repaired seam spec — they close two distinct OQs. Net §5 tail: [refined NEP prose] → cycle-012 Resolved → cycle-011 Resolved.)
- book/src/L1/eigsolve.md (Edit 2 — carry-forward citation correction at §5 (line 116): `arpack.cpp:387` → `:383` for the un-scale `eig[i] = eig[i] * gamma`; repairer-flagged inherited miscitation, line 387 is a `which_option == ::arpack::which::smallest_real` sort-branch condition, not the un-scale. Verified the line cited `:387` for the un-scale before correcting.)
- book/src/L1/eigsolve.md (Edit 3 — carry-forward citation correction at §Evidence (line 222): `arpack.cpp:387` → `:383`; same inherited miscitation; expanded the note to record the un-scale is at line 383 inside the 381-384 nev loop and that line 387 is the sort-branch condition.)
- book/src/L1/eigsolve.md (Edit 4 — appended a `## Verified-against (cycle-012 lowering-verifier — SLEPc-NEP coordinate-convention audit)` section at end of file: verdict prose [confirms-uniform / resolved-with-refinement] + the machine-readable `verified_against:` YAML block (10 citations, verdicts supports/partially-supports) inside a fenced `yaml` code block + the empirical-witness caveat. The ARPACK entry carries `:383` (repaired in CYCLE.md) and a note recording the in-artifact `:116,222` `:387` miscitation was corrected by this integrator.)
- scaffolding/open-questions.md (edit — 1 status-close + 1 new OQ):
  - status close: `eigsolve-slepc-nep-coordinate-convention-audit` (cycle-011 OQ opened by repairer; was the open binary "API un-scales internally OR missing `* gamma`?") — `status: open` → `status: answered`; appended `answered_at: cycle-012` + `answered_in: book/src/L1/eigsolve.md (§5 + Verified-against block)` + `resolution: resolved-with-refinement` fields; appended a closing paragraph documenting the first-horn resolution (NEP solves un-scaled directly; `return l` correct; gamma/delta dead store), the two-mechanism refinement, the §5 prose update + cycle-011-sentence retention, and the empirical-witness caveat. Explicitly requested by the dispatch ("Close ... (resolved-with-refinement)"). Status-field close follows the cycle-011/cycle-012 integrator-per-report precedent.
  - new OQ: `eigsolve-nep-coordinate-convention-empirical-witness` (cycle-012, opened_by: lowering-verifier, priority: low) — the verdict is `source-read-confirmed, empirically-unwitnessed` (no `test-eigensolver.cpp` NEP case); routes a future upgrade-to-empirically-witnessed when a NEP test case lands or a `SlepcNEPSolver` L0 entry firms up; also records the latent "why compute unused gamma/delta?" dead-store-vs-vestigial question (not separately filed, below the problems-bar). The audit's caveat-1 said "note as a low-priority OQ if you judge it worth tracking" — judged worth tracking (verdict strength is bounded by the no-test caveat).

Gate hits:
- retroactive-budget per-slice: 0 (verifier audit — the verified_against block is explicitly-allowed retroactive-evidence framing; the 2 citation corrections are repairer-flagged integrator carry-forward fixes; the §5 refinement is the audit's surface-modifying output, not retroactive evidence backfill on a different operator)
- retroactive-budget global: 0 (this report)
- forward-edge claim without surface: 0 (all references are L0 source ranges + existing OQ slugs + existing source files; no artifact `[link]` to a non-existent surface — the verified_against block cites only `slepc.cpp`/`arpack.cpp` source ranges)
- edge-label / prose mismatch: 0 (touched artifact is an L1 operator entry's algebraic law §5, NOT a lowering theme; no `L_{n+1}→L_n` edge label carried; critic edge-label-fidelity = pass)
- forward-edge claim without surface (OQ cross-ref): 0 (the new OQ's `relates_to` points at the just-closed sibling OQ; the closed OQ's `answered_in` points at the live `book/src/L1/eigsolve.md`)
- H1 reuses page heading: 0 (the appended section is an H2 `## Verified-against (...)`; no H1 added; no page-heading reuse)
- append on missing slug: 0 (the verified_against append targets the existing `book/src/L1/eigsolve.md`; the OQ close targets the existing `eigsolve-slepc-nep-coordinate-convention-audit` slug)
- variant-axis missing on multi-variant operator: 0 (audit covers both NEP `SetOperators` overloads K-M/K-C-M + all 4 backends + the ScaleType sub-axis; critic variant-axis-coverage = pass)
- SUMMARY.md chapter registration auto-fix: 0 (n/a — no new chapter file created; eigsolve.md already SUMMARY-registered)
- concepts/<slug>.md SUMMARY registration auto-fix: 0 (n/a — no new concept page)
- index-placeholder displacement auto-fix: 0 (n/a — no index.md touched)
- concept_writes on existing slug: 0 (n/a — no concept write; all edits are to the existing L1 operator entry + open-questions)
- bookkeeping incomplete: 0
- citation-carry-forward correction: 2 applied (eigsolve.md:116 + :222 `arpack.cpp:387` → `:383`) — repairer-flagged integrator carry-forward; both verified against the source line content before correcting (the report's own anchors were already repaired to `:383` by the repairer)

Open questions promoted:
- eigsolve-nep-coordinate-convention-empirical-witness (new; opened_at cycle-012, opened_by lowering-verifier, priority low)

OQ closed:
- eigsolve-slepc-nep-coordinate-convention-audit (status: open → answered; resolution: resolved-with-refinement; answered_in book/src/L1/eigsolve.md) — explicitly requested by the dispatch.

Build-relevant: yes (edits touch book/src/L1/eigsolve.md — finalize should run the book rebuild)

Notes:
- `overall_status: pass-after-repair` confirmed in META.md frontmatter before applying. Clean apply; no deferred/unparseable proposed-change blocks. Both audit artifact outputs applied (the verified_against YAML block + the §5 prose refinement), plus the repairer-flagged carry-forward `:387`→`:383` corrections at eigsolve.md:116 and :222.
- **Seam handling**: applied the repaired §5 seam spec exactly — replaced ONLY element (i) (the SLEPc-NEP parenthetical), retained element (ii) (the cycle-011 "Resolved" sentence) verbatim, placed the new cycle-012 "Resolved" sentence before it. Verified post-edit: zero `arpack.cpp:387` remain in eigsolve.md; the two "Resolved" sentences are adjacent in cycle order (012 then 011).
- **verified_against placement**: appended as a new `## Verified-against` H2 section at end of file (the CYCLE.md said "[append at end of file]"). The YAML is wrapped in a fenced `yaml` code block (not raw at top level) to keep the mdBook page renderable — same folded-into-prose pattern as the prior in-cycle `eigsolve-mutation-rotation` row's "Machine-readable audit record" handling. ARPACK entry carries `:383`.
- **Carry-forward correction authority**: the dispatch explicitly directed applying the `:387`→`:383` correction at eigsolve.md:116,222 ("Apply the :387→:383 correction ... if those lines cite arpack.cpp:387 for the un-scale"). Both lines did cite `:387` for the un-scale `eig[i] = eig[i] * gamma`; verified line content before correcting. This is the in-artifact half of the carry-forward the repairer routed (the report's own anchors were already repaired; the artifact correction was integrator scope).
- Deferred `integrated_at` (and `integration_commit`) to integrator-finalize per role-spec / CLAUDE.md §Write-authority partition — the consumed report's frontmatter `status: pending` is left untouched by this per-report integrator.
- Skill-uptake telemetry survives for meta-phase awareness: the critic flagged `verify-citation-range` / `classify-variant-axis` as un-invoked despite the report doing citation-range audit + variant-axis enumeration; the critic explicitly noted the `:387` drift would likely have been caught by `verify-citation-range`. This is the third consecutive cycle-012 report with the same skill-uptake gap (orthogonalize + chebyshev both had it) — a strengthening meta-phase aggregation signal for batch-2.
- **Recurring repair shape (for meta-phase)**: the critic flagged "audit-report inherits a miscitation from the artifact it audits, asserts no-drift over it" as a candidate for a thin auto-fix / lint pattern. Surfacing for meta-phase consideration — this is the inverse of the value of the `verify-citation-range` skill on an audit's own anchors.

---

## 2026-05-28T020000Z-layer-intro-author-l3-index-refresh
applied_at: 2026-05-28T053500Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L3/index.md (edit — 1 in-place edit: refreshed the `## Semantics (overlay)` prose. Re-read disk before editing; the `[old]` block matched `index.md:9-15` verbatim — no prior in-cycle integrator touched L3/index.md (the orthogonalize/chebyshev landings touched L1/index.md + L2/index.md, not L3). The 4-bullet overlay was rewritten: bullet 1 (Whole-tensor field operations) now names all 8 firm L3 operators grouped by kind — matvec (`apply_linop`, "the linear-operator-application generalisation of 'matvec'"), the linear-update family (`axpy`, `axpby`, `axpbypcz`, `scal`), and the reductions (`dot`, `nrm2`); bullet 2 (Field transitions) names the composition operator `krylov-step` as the canonical instance with its `(op, K, s) -> (K', s', outputs)` kernel; bullets 3-4 (Convolution-like patterns, Sequential obstructions) bolded + the obstruction bullet now references `concepts/sequential-obstruction.md`. The "matvec (apply_linop)" parenthetical reconcile retains the casual "matvec" token so existing back-references stay valid.)
- scaffolding/open-questions.md (edit — 2 status-closes, both explicitly requested by the dispatch):
  - status close: `l3-index-semantics-overlay-blas1-cohort-prose-refresh` (cycle-011 OQ, harvester) — `status: open` → `status: answered`; appended `answered_at: cycle-012` + `answered_in: book/src/L3/index.md (§Semantics (overlay))` fields; appended a closing paragraph documenting the *kind*-of-primitives reframing with all 8 firm operators named inline, cross-checked against the dep-map. Noted the broader `l3-vocabulary-inventory-gap` OQ remains open (tracks which additional operators warrant L3 backfill).
  - status close: `l3-index-matvec-naming-vs-apply_linop-slug` (cycle-011 OQ, harvester) — `status: open` → `status: answered`; appended `answered_at: cycle-012` + `answered_in: book/src/L3/index.md (§Semantics (overlay))` fields; appended a closing paragraph documenting the `matvec (apply_linop)` parenthetical reconcile (casual name retained for back-reference validity; formal slug present with equivalence stated, matching `apply_linop.md:24`'s "matvec generalisation" framing).
  (Status-field closes follow the cycle-011/cycle-012 integrator-per-report precedent of touching existing OQ status fields when the dispatch explicitly requests the close — the dispatch directed "Close 2 OQs". The two new OQs the report mentions but does NOT close — `l3-vocabulary-inventory-gap` (parent, explicitly left open) and `concepts-nrm2-stability-claim-correction` (out of scope) — are untouched. No new OQs promoted: the report opens none.)

Gate hits:
- 8-operator-inventory accuracy (cross-check dep-map): 0 issues (verified — the refreshed bullet names exactly the 8 firm L3 operators present in the dep-map table at `index.md:19-28`: apply_linop, axpy, axpby, axpbypcz, scal, dot, nrm2 (in bullet 1) + krylov-step (in bullet 2). No extras, no omissions. Grouping by kind — matvec / linear-update family / reductions / composition — matches the dep-map's Signature + Dependencies structure. The critic confirmed the 8-operator inventory matches the dep-map and the 8 actual files in `book/src/L3/` exactly at META.md §citation-validity.)
- matvec (apply_linop) back-reference validity: 0 issues (verified — the new prose retains the "matvec" token and the "axpy, dot, nrm2" tokens, so the back-references from `apply_linop.md:20,24,150,173` (cite index `:11-14`/`:13` for the "matvec ... as field operations" advertisement) and `scal.md:26,49,137` (same) remain semantically valid — they cite content not a code range, and the content is preserved and enriched. The critic's Issue 1 (incomplete back-reference census in the report) and Issue 2 (`scal.md:137` "three of six" framing goes slightly stale) are LOW-severity report-accounting/cosmetic notes, NOT correctness defects in the edit, and both target operator entries outside layer-intro-author authority — correctly out of scope for this dispatch. Carry-forward routing note below for finalize/planner.)
- L3 vocabulary discipline: 0 issues (verified — whole-tensor field operations, no element loops exposed; the krylov-step kernel `(op, K, s) -> (K', s', outputs)` matches the dep-map Form A / `krylov-step.md:36`; `concepts/sequential-obstruction.md` referenced and exists; no L0 mutation idioms, no L2 composition vocabulary leaked into the L3 overlay. The cross-layer mentions (krylov-step lifts from L4, lowers to L2) are confined to the report's supporting-evidence orientation, not the overlay prose.)
- retroactive-budget per-slice: 0 (overview prose refresh; not retroactive evidence backfill on an operator)
- retroactive-budget global: 0 (this report)
- concept_writes on existing slug: 0 (n/a — no concept write; edits are to the L3 index overview + open-questions)
- forward-edge claim without surface: 0 (the new prose names slugs in backticks, introduces NO `[link]` markdown links; `concepts/sequential-obstruction.md` named in backticks resolves on disk. No dangling forward-edge.)
- edge-label / prose mismatch: 0 (L3-internal overview refresh; carries no `L_{n+1}→L_n` edge label; critic edge-label-fidelity = pass)
- H1 reuses page heading: 0 (no H1 touched; the edit is within the existing `## Semantics (overlay)` H2 section)
- append on missing slug: 0 (no slug-creation; the OQ closes target the two existing `l3-index-*` slugs)
- variant-axis missing on multi-variant operator: 0 (n/a — inventory prose, not an operator entry; the one variant-flavored phrase ("square and rectangular operators, real and complex, all operator representations absorbed" for apply_linop) is consistent with the apply_linop frontmatter variant_axes per the critic's variant-axis-coverage = pass)
- SUMMARY.md chapter registration auto-fix: 0 (n/a — no new chapter file; L3/index.md is already SUMMARY-registered)
- concepts/<slug>.md SUMMARY registration auto-fix: 0 (n/a — no new concept page)
- index-placeholder displacement auto-fix: 0 (n/a — L3/index.md is fully populated, 8-row dep-map + Working Notes; no `(empty — Phase B skeleton.)` placeholder)
- bookkeeping incomplete: 0

Open questions promoted:
- (none — the report opens no new OQs; it closes 2 existing ones)

OQ closed:
- l3-index-semantics-overlay-blas1-cohort-prose-refresh (status: open → answered; answered_in book/src/L3/index.md) — explicitly requested by the dispatch.
- l3-index-matvec-naming-vs-apply_linop-slug (status: open → answered; answered_in book/src/L3/index.md) — explicitly requested by the dispatch.

Build-relevant: yes (edit touches book/src/L3/index.md — finalize should run the book rebuild)

Notes:
- `overall_status` clean — all 8 critic checks PASS, no repair section (confirmed in META.md frontmatter `checks:` block; no `## Repair` section present). Single-block clean apply; the one `edit:` proposed-change applied as anchor-and-replace against the verbatim-matching `[old]` block.
- This is the fifth cycle-012 staging row; the first to touch L3/index.md (prior rows touched L1/index.md + L2/index.md, and L1/eigsolve.md). No re-read drift on L3/index.md — the `[old]` block matched disk exactly.
- **Carry-forward routing note (for finalize / cycle-013 planner)**: the critic surfaced two LOW-severity stale-prose consequences of this refresh in OPERATOR entries (outside layer-intro-author authority, correctly out of scope for this dispatch): (1) `book/src/L3/scal.md:137` reads "the index lists three of the six BLAS-1 primitives by name; the others ... implied by the 'etc.' reading" — now stale because the index names the full cohort; (2) the line-number drift of the index "matvec ... as field operations" advertisement off line 13 affects back-references in `apply_linop.md` (4 sites) AND `scal.md` (3 sites — the report's drift note only counted apply_linop). All back-references remain *semantically* valid (they cite content, not code ranges) so this is precision/cosmetic, not a blocker — but a future lifter/cross-cutter sweep could re-point the 7 sites to the new line range and fix the `scal.md:137` "three of six" framing. Not opening a new OQ (the report explicitly chose not to, and the cited content is preserved); surfacing here so finalize/planner can bundle into a future operator-entry touch-up.
- Deferred `integrated_at` (and `integration_commit`) to integrator-finalize per role-spec / CLAUDE.md §Write-authority partition — the consumed report's frontmatter `status: pending` is left untouched by this per-report integrator.
- Skill-uptake telemetry: the critic noted `verify-citation-range` would have been a natural fit for the back-reference-preservation argument but was not invoked (non-blocking telemetry, META.md §skill-uptake-survey = pass). This is the fourth consecutive cycle-012 report with a skill-uptake gap (orthogonalize + chebyshev + slepc-nep audit all had it) — a strengthening meta-phase aggregation signal for batch-2.

---

## 2026-05-28T034221Z-layer-intro-author-concept-corrections
applied_at: 2026-05-28T060500Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/concepts/nrm2.md (edit — Task 1 CORRECTION: replaced the false §Contract stability bullet ["production implementations use scaled summation (BLAS `nrm2` algorithm) ... A naïve `√Σ x[i]²` is not equivalent"] with the L1-authoritative finding — Palace's `linalg::Norml2` computes naïve `√⟨x, x⟩` via `Dot` (one-line body `std::sqrt(std::abs(Dot(comm, x, x)))`), NO scaled summation, NO Palace-level overflow/underflow guarantee, BLAS scaled-summation `nrm2` not present; bullet now forwards to `[L1/nrm2](../L1/nrm2.md)`. The `[old]` anchor matched committed HEAD verbatim post-revert. This is the load-bearing factual fix.)
- book/src/concepts/state-stratification.md (edit — Task 2 EXTENSION: appended §"Worked example — Chebyshev smoother (slice: chebyshev, L4): a fourth stratum" after the §"Worked example — GMRES" closing paragraph. Introduces the scalar-recurrence stratum (per-call ephemeral but threaded across inner `k`-iterations; `rho_prev` in the inner `foldM` `ScalarState`) as a 4th kind distinct from operator-internal params and ordinary ephemerals; `ChebOp<E, S>` type-level encoding (`Unit` 4th-kind / `{ rho_prev: E }` 1st-kind) + stratum-placement check + three-way-split-suffices condition. Anchor matched HEAD verbatim.)
- book/src/concepts/derived-view-hoisting.md (edit — Task 3 EXTENSION: inserted §"Worked example: Chebyshev initial-guess branch (control-flow boundary)" BEFORE the §"When the rotation applies" anchor. Pairs the existing CG state-shape-boundary example with the control-flow-boundary case (`initial_guess = false` as algebraic specialization under `y_in = 0`; degenerate-case absorption not a residual variant axis; bad=constructed-operator-variant inflating the closure lattice to four vs. good=per-call argument; per-call-flag-vs-constructed-operator-variant categorical distinction tied to [`variant-absorption`]). Anchor matched HEAD verbatim.)
- book/src/concepts/negative-result-slice.md (edit — Task 4 EXTENSION: inserted §"Partial-positive sub-pattern" BETWEEN §"Examples in this spec" and §"Falsification criterion (required structural element)". Documents outer-cross-family-negative + nested-within-family-positive coexistence kept honest by explicit scoping; `polynomial_recurrence_step` §L1↔L1 self-tightening as canonical worked example; four-point recording discipline. Anchor matched HEAD verbatim.)
- scaffolding/open-questions.md (edit — 5 status-closes across the 4 requested slugs; all 4 explicitly requested by the dispatch):
  - `concepts-nrm2-stability-claim-correction` (cycle-011 entry, line ~1870) — `status: open` → `status: answered`; appended `answered_at: cycle-012` + `answered_in: book/src/concepts/nrm2.md (§Contract stability bullet)` + closing paragraph (option-(a) L1-authoritative replacement, verbatim match to `L1/nrm2.md:11/:84/:97` + `vector.hpp:255-260`).
  - `concepts-nrm2-stability-claim-correction` (cycle-003 DUPLICATE entry, line ~189) — ALSO closed (same slug, earlier vintage; same correction). `status: open` → `status: answered` + same close fields + closing paragraph noting it is the cycle-003-vintage original of the cycle-011 duplicate. Closed both so the ledger does not leave a stale-open duplicate of an answered slug.
  - `concepts-state-stratification-four-stratum-extension` (cycle-011, line ~1991) — closed; resolved the "four-stratum split vs sub-kind" question in favour of the four-stratum split.
  - `concepts-derived-view-hoisting-control-flow-boundary-extension` (cycle-011, line ~2003) — closed.
  - `concepts-negative-result-slice-partial-positive-sub-pattern-extension` (cycle-011, line ~2015) — closed.
  (Status-field closes follow the established cycle-011/cycle-012 integrator-per-report precedent of touching existing OQ status fields when the dispatch explicitly requests the close. Each close records the report's currently-correct slice line numbers, not the ledger's older drifted ranges, per the critic's note that the slice text has drifted a few lines since the OQs were filed.)

Gate hits:
- nrm2-correction-matches-L1-finding: 0 issues (verified — the `[new]` stability bullet matches the firm L1 entry's finding verbatim: naïve `√⟨x,x⟩` via `Dot`, one-line body `std::sqrt(std::abs(Dot(comm, x, x)))`, no scaled summation, no Palace overflow/underflow guarantee, scaled-summation not present. Corroborated by the dispatch's Supporting-evidence citations `L1/nrm2.md:11, :84, :97` + `palace/linalg/vector.hpp:255-260`; critic citation-validity = pass with vector.hpp:255-260 read directly.)
- extensions-concrete-and-cite-motivating-slices: 0 issues (verified — all 3 extensions carry concrete worked-example content cited to the motivating slice §-sections: four-stratum → `chebyshev.md:298, :300-321`; control-flow-boundary → `chebyshev.md:416-433`; partial-positive → `polynomial_recurrence_step.md:170-199`. critic surface-or-evidence = pass — all four edits modify concept-page text AND carry evidence.)
- no-new-outbound-links-to-nonexistent-targets: 0 issues (verified — one new outbound link only: `concepts/nrm2.md` → `../L1/nrm2.md`; target exists on disk (13988-byte file confirmed via `ls`). The derived-view extension's `[variant-absorption](./variant-absorption.md)` link targets an already-linked existing page (confirmed via `ls`). No new dangling links.)
- write-authority-phase-boundary (CONTEXT, not a gate-block): n/a-to-application — the repairer reverted all 4 files to HEAD (Option A), so the working tree was CLEAN at apply time and I applied the 4 proposed-changes blocks normally (NOT skip-as-already-applied). All 4 `[old]` anchors matched committed HEAD exactly once (repairer + my own re-read both confirmed). See META SIGNAL note below for finalize.
- retroactive-budget per-slice: 0 (concept-page refinement/extensions; the nrm2 fix is a contradiction-correction against the authoritative L1 entry — surface mutation backed by L0 evidence, not retroactive evidence backfill on an operator entry)
- retroactive-budget global: 0 (this report)
- concept_writes on existing slug: 0 (all 4 are EDITS to existing `concepts/<slug>.md` pages — nrm2/state-stratification/derived-view-hoisting/negative-result-slice all pre-exist; this is the section-append/refinement shape the gate prescribes for existing slugs, NOT a concept-create-on-existing-slug. No auto-rewrite needed — the dispatch already authored them as edits/section-appends.)
- forward-edge claim without surface: 0 (no forward-edge `[link]` to a non-existent surface; the one new link resolves)
- edge-label / prose mismatch: 0 (concept pages carry no `L_{n+1}→L_n` edge label; critic edge-label-fidelity = pass / n.a.)
- H1 reuses page heading: 0 (the 3 extensions add H2 `##` worked-example/sub-pattern sections; no new H1; the nrm2 fix is a bullet replacement within the existing §Contract; no page-heading reuse)
- append on missing slug: 0 (all 4 target concept slugs exist; edits anchor against existing in-page sections)
- variant-axis missing on multi-variant operator: 0 (n/a — concept pages, not operator entries; the variant-flavored content (4th/1st-kind, per-call-flag-vs-constructed-operator-variant) is handled explicitly per critic variant-axis-coverage = pass)
- SUMMARY.md chapter registration auto-fix: 0 (n/a — no new chapter/concept page created; all 4 pages already SUMMARY-registered (state-stratification:116, derived-view-hoisting:121, negative-result-slice:125, nrm2:104) per critic cross-reference-integrity = pass; no SUMMARY edit proposed or needed)
- concepts/<slug>.md SUMMARY registration auto-fix: 0 (n/a — no new concept page; the 4 edited pages are pre-registered)
- index-placeholder displacement auto-fix: 0 (n/a — no index.md touched)
- bookkeeping incomplete: 0

Open questions promoted:
- (none — the report opens no new OQs)

OQ closed:
- concepts-nrm2-stability-claim-correction (cycle-011 entry; status open → answered; answered_in book/src/concepts/nrm2.md) — explicitly requested.
- concepts-nrm2-stability-claim-correction (cycle-003 duplicate entry; status open → answered) — same slug, also closed to avoid a stale-open duplicate of an answered correction.
- concepts-state-stratification-four-stratum-extension (status open → answered) — explicitly requested.
- concepts-derived-view-hoisting-control-flow-boundary-extension (status open → answered) — explicitly requested.
- concepts-negative-result-slice-partial-positive-sub-pattern-extension (status open → answered) — explicitly requested.

Build-relevant: yes (edits touch book/src/concepts/nrm2.md, state-stratification.md, derived-view-hoisting.md, negative-result-slice.md — finalize should run the book rebuild)

Notes:
- `overall_status: pass-after-repair` confirmed in META.md frontmatter (`write-authority-phase-boundary: repaired`) before applying. Clean apply; all 4 proposed-changes blocks applied as anchor-and-replace against verbatim-matching `[old]` blocks. The repairer's Option-A revert means the artifact did NOT contain these edits at dispatch time — applied normally, did NOT skip-as-already-applied (per the dispatch directive + META §Suggested-resolution item 1).
- **[META SIGNAL — write-authority phase-boundary violation, recurrence-1] for integrator-finalize → scaffolding/integrator-signals.md → meta-phase batch-2 (cycles 010/011/012).** This layer-intro-author dispatch wrote its 4 concept-page edits DIRECTLY to `book/` during the DISPATCH phase (Phase 2), violating the no-artifact-mutation-in-dispatch invariant and the write-authority partition (`book/` is integrator-per-report's exclusive write authority in Phase 5). The critic caught it (META §Issue 1, HIGH); the repairer reverted all 4 files to HEAD (Option A) restoring phase discipline; this integrator then applied normally from the proposed-changes blocks. First observed instance for layer-intro-author (recurrence-1). Finalize should append this to integrator-signals so meta-phase can judge one-off vs. pattern and consider a layer-intro-author prompt-level guard (explicit "do NOT touch book/ — emit proposed-changes blocks only" reinforcement in the agent spec). No enactment here — flagging only; meta-phase owns that authority. NOTE: this is the headline non-content signal of this report.
- **[LOW — out-of-scope, for cycle-013 same-layer-cross-cutter follow-up] Stale slice reduction-status banners.** `book/src/spec/slices/chebyshev.md:13-14` and `book/src/spec/slices/polynomial_recurrence_step.md:5` list these concept-extensions as "pending lift"; with the 3 extensions landed, those specific bullets are now satisfied, but the banners still say pending. Updating them is a slice-file edit, OUTSIDE concepts/ authority (and outside this per-report integrator's book-write scope for slice content beyond what the report proposed). Self-flagged by the report (§Open questions/caveats bullet 1) + critic (Issue 2) + dispatch directive. Recommend a cycle-013 `same-layer-cross-cutter`-scoped dispatch per the "Phase 1 corpus reduces as material is lifted" invariant to mark the concept-extension bullets done. Not opening a new OQ (the report chose not to; the staleness is benign and tracked here + in the report).
- **[LOW — forward-anchor, no action now] Concept citations point at slice, not firm entries.** The four-stratum + control-flow examples cite `spec/slices/chebyshev.md §L4` (the current authoritative L4 representation for Chebyshev) rather than firm `L1/chebyshev-smoother` / `L2/chebyshev-iteration`. Those firm entries DID land earlier this same cycle-012 (staging row 2: `2026-05-28T034154Z-harvester-chebyshev-l1-l2`), so a future re-point is now possible — but the §L4 form is the authoritative L4 representation and neither firm entry is an L4 entry, so the slice §L4 citation remains the correct anchor for the L4-shaped examples for now. Tracked as a forward-anchor, not a blocker; finalize/planner may bundle a re-point into a future concept-page touch-up if/when an L4 Chebyshev entry lands.
- Skill-uptake telemetry: critic flagged `verify-refinement-surface` (directly on-point for Task 1's refinement-against-authoritative-entry discipline) and `verify-citation-range` (the 4-slice citation verification) as applicable-but-unreferenced (META §skill-uptake-survey = warning, non-blocking). This is the FIFTH consecutive cycle-012 report with a skill-uptake gap (orthogonalize + chebyshev + slepc-nep + l3-index-refresh all had it) — a strong meta-phase aggregation signal for batch-2 (all 6 cycle-012 dispatches show it once this lands; the pattern is now cycle-wide).
- Deferred `integrated_at` (and `integration_commit`) to integrator-finalize per role-spec / CLAUDE.md §Write-authority partition — the consumed report's frontmatter `status: pending` is left untouched by this per-report integrator.

---

## 2026-05-28T034235Z-lifter-l4-index-superseded-drift
applied_at: 2026-05-28T063000Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L4/index.md (edit — 1 in-place surgical edit at the L4>L3-lowering-themes bullet, line 40: re-anchored the trailing consequence clause of the `krylov-step-typed-wrapper-dissolution` bullet. Re-read disk before editing; the `[old]` block matched line 40 byte-for-byte (no prior in-cycle integrator touched L4/index.md — the 6 prior cycle-012 rows touched L1/index.md, L2/index.md, L3/index.md, L1/eigsolve.md, and concepts/* pages, NOT L4/index.md). The bullet's narrative (wrapper-machinery dissolution into L3 value-threading; the kernel body's primitive sequence is identity-in-form) is preserved verbatim; ONLY the stale cycle-006 trailing consequence clause "so no intermediate L3 `krylov-step` row is needed" was replaced. New trailing text: forward-pointer to the firm L3 image `[L3/krylov-step](../L3/krylov-step.md)` (cycle-010 wave-1, harvester report `2026-05-27T215300Z-harvester-l3-krylov-step`), an explicit **SUPERSEDED** marking of the cycle-006 verdict citing the CLAUDE.md §Methodology invariants bullet "Identity-lowerings still require both L levels" (codified cycle-009 meta-phase), the layer-coherence rationale, and a pointer to the canonical SUPERSEDED annotation at `krylov-step-typed-wrapper-dissolution.md` §"Audit of cycle-002 identity-in-form claim". High→low direction intact — the bullet still narrates the L4 form lowering into L3; the re-anchor adds a forward pointer to the L3 *image* of the lowering, not a reverse lift-direction narration. The L4 index intro prose, Vocabulary-cohort framing, dep-map table, and Working-Notes section are all untouched.)
- scaffolding/open-questions.md (edit — 1 new OQ section appended at the end of the `## Open` section, immediately before `## Investigating`):
  - new OQ: `krylov-step-theme-body-no-l3-row-drift-cycle-013` (cycle-012, opened_by: lifter) — the L4>L3 theme body at `krylov-step-typed-wrapper-dissolution.md:20` ("...so **no L3 `krylov-step` row is promoted by this theme**...") and `:220` ("...the assertion holds, the framing is sharpened, **no L3 row needed**") still phrase the cycle-006 conclusion as if live, even though the line-218 SUPERSEDED annotation reconciles them internally. The critic confirmed both passages present by direct read (true positive). Routes a follow-up `lifter` dispatch on the theme body to re-anchor lines 20 and 220 to the firm `L3/krylov-step.md`, consistent with line 218. Low-cost, non-blocking; routed to cycle-013+ planner. `relates_to:` the answered cycle-006 `krylov-step-l3-row-contingency` + `krylov-step-l3-identity-in-form-audit-closure-cycle-006`.

Gate hits:
- minimal/surgical single-bullet edit: 0 issues (verified — exactly one bullet's trailing consequence clause re-anchored; narrative preserved verbatim; no other content on the page touched. The `[old]`/`[new]` was critic-confirmed byte-for-byte; my disk re-read confirmed the `[old]` text present verbatim at line 40 before editing.)
- re-anchor target exists: 0 issues (verified `book/src/L3/krylov-step.md` exists on disk: 40,486 bytes / 225 lines — matches the report's repaired "225 lines / ~40KB" figure; this is the firm L3 entry authored cycle-010 wave-1, commit `30119eb`. The forward pointer is to a live surface.)
- high→low direction preserved: 0 issues (verified — the re-anchored bullet narrates the L4 form lowering into L3 value-threading; the added forward pointer names the L3 *image* of the lowering, not a reverse lift narration; no reverse-direction prose introduced, consistent with the `layer-definition-discipline-high-to-low` invariant.)
- forward-edge claim without surface: 0 (the new `[L3/krylov-step](../L3/krylov-step.md)` link resolves on disk; the `../L4-L3/krylov-step-typed-wrapper-dissolution.md` link target exists with the §"Audit of cycle-002 identity-in-form claim" section; the cited harvester report directory `reports/2026-05-27T215300Z-harvester-l3-krylov-step/` exists. No dangling forward-edge.)
- edge-label / prose mismatch: 0 (the touched bullet is on the L4 index's L4>L3-lowering-themes list; it consistently discusses the L4>L3 edge — the re-anchor points at the L3 entry that is the dissolution's image. Critic edge-label-fidelity = pass. The dep-map row at line 49 (L4>L3>L2 transitive framing) is untouched and remains mutually consistent.)
- H1 reuses page heading: 0 (no H1 touched; the edit is a bullet inside the existing `**L4>L3 lowering themes**` subsection.)
- append on missing slug: 0 (no slug-creation; the OQ append targets the existing `## Open` section.)
- concept_writes on existing slug: 0 (n/a — no concept write; edits are to the L4 index bullet + open-questions.)
- index-placeholder displacement auto-fix: 0 (n/a — L4/index.md is fully populated (3-row dep-map, Vocabulary-cohort, Working-Notes); no `(empty — Phase B skeleton.)` placeholder; this is a re-anchor of existing prose, not a first-firm-row displacement.)
- SUMMARY.md chapter registration auto-fix: 0 (n/a — no new chapter file created; L4/index.md is already SUMMARY-registered.)
- concepts/<slug>.md SUMMARY registration auto-fix: 0 (n/a — no new concept page.)
- variant-axis missing on multi-variant operator: 0 (n/a — cross-reference cleanup, not an operator/theme entry with variant axes; critic variant-axis-coverage = pass / not-applicable.)
- retroactive-budget per-slice: 0 (cross-reference cleanup of existing firm-theme prose; not retroactive evidence backfill on an operator.)
- retroactive-budget global: 0 (this report)
- bookkeeping incomplete: 0

Open questions promoted:
- krylov-step-theme-body-no-l3-row-drift-cycle-013 (new; opened_at cycle-012, opened_by lifter; routed to cycle-013+ planner)

OQ closed:
- (none in scaffolding/open-questions.md). The dispatch directed "Close the cycle-010 carry-forward OQ for the L4/index.md:40 drift (now resolved)." That drift was tracked as a CARRY-FORWARD FLAG CHAIN in `scaffolding/integrator-signals.md` (lines 58/91/150/184 per the report's `inputs:`), NOT as a discrete slug in `scaffolding/open-questions.md` — I searched the ledger (open + answered) for any L4/index.md:40 / "no L3 row" / SUPERSEDED-text drift slug and found none (the cycle-006 `krylov-step-l3-row-contingency` and `krylov-step-l3-identity-in-form-audit-closure-cycle-006` are already `answered` and cover the cycle-006 audit, not the index cross-reference drift). `scaffolding/integrator-signals.md` is integrator-FINALIZE write-authority, NOT per-report, so I did NOT touch it. FINALIZE: the integrator-signals carry-forward flag chain (lines 58/91/150/184) for the L4/index.md:40 SUPERSEDED-text drift is NOW RESOLVED by this landing — please mark it resolved when you append to integrator-signals this cycle.

Build-relevant: yes (edit touches book/src/L4/index.md — finalize should run the book rebuild)

Notes:
- `overall_status: pass-after-repair` confirmed in META.md frontmatter before applying. Clean single-block apply; the one `edit:book/src/L4/index.md` block applied as anchor-and-replace against the verbatim-matching `[old]` block. No deferred/unparseable proposed-change blocks. The repairs in this report were all to SUPPORTING PROSE / METADATA (Discipline-notes citation `integrator-signals.md:218`→`:162`; size "~105 lines"→"225 lines"; OQ-capture hardening) — the applied `[old]`/`[new]` surface edit was critic-confirmed byte-for-byte correct and unaffected by the repairs.
- This is the seventh cycle-012 staging row and the FIRST to touch `book/src/L4/index.md` (no re-read drift — the `[old]` block matched disk exactly; prior rows touched L1/index.md, L2/index.md, L3/index.md, L1/eigsolve.md, and concepts/* pages).
- **Carry-forward resolution for finalize (the headline)**: the cycle-010 wave-1 pass-2 META Issue-1 carry-forward flag (re-flagged cycle-011, surfaced as the cycle-012 smallest-cost cleanup candidate), tracked in `integrator-signals.md:58/91/150/184`, is RESOLVED by this single-bullet re-anchor — the last stale cross-reference of the cycle-006 "no L3 row needed" verdict in the L4 index is gone. Finalize: mark the integrator-signals flag chain resolved (per-report integrator cannot write integrator-signals).
- **Residual surfaced (now formally tracked)**: the theme-body line-20/line-220 stale "no L3 row" phrasing in `krylov-step-typed-wrapper-dissolution.md` is NOT resolved by this dispatch (out of single-edit scope) — now promoted as OQ `krylov-step-theme-body-no-l3-row-drift-cycle-013`, routed to cycle-013+ lifter. Previously this residual had no formal OQ ledger entry; it does now.
- Skill-uptake telemetry: critic flagged `verify-citation-range` (the report's VERIFY-BEFORE-DISPATCH line-40 presence check is exactly that skill's job; would have caught the Discipline-notes `:218`→`:162` mis-attribution) and `verify-refinement-surface` (this is a refinement-surface edit) as applicable-but-unreferenced (META §skill-uptake-survey = warning, non-blocking). This is the SIXTH consecutive cycle-012 report with a skill-uptake gap (orthogonalize + chebyshev + slepc-nep + l3-index-refresh + concept-corrections all had it) — the pattern is now CYCLE-WIDE (all 7 cycle-012 dispatches show it), a strong meta-phase aggregation signal for batch-2.
- Deferred `integrated_at` (and `integration_commit`) to integrator-finalize per role-spec / CLAUDE.md §Write-authority partition — the consumed report's frontmatter `status: pending` is left untouched by this per-report integrator.

---

## 2026-05-28T034141Z-same-layer-cross-cutter-phase-1-corpus-reduction-batch-3
applied_at: 2026-05-28T070000Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/spec/slices/orthog.md (edit — FULL REDUCTION of the plane-rotation sub-slice [proposed change 1]. Re-read disk first; no prior in-cycle integrator touched this file [the 7 prior rows touched L1/L2/L3/L4 index.md, L1/eigsolve.md, and concepts/* pages, NOT spec/slices/]. The sub-slice was reduced via full-file Write [retained Gram-Schmidt block lines 1-223 verbatim + new stub-pointer], NOT line-number Edit, to safely replace the start-anchor-through-EOF span the repairer corrected to. TEXT-ANCHOR verification before applying: `## Context` occurs exactly once [line 225] and is immediately followed by the unique `# Orthogonalization (plane-rotation stream)` H1 [line 227]; the Gram-Schmidt §L4 `### Open questions` [line 220, H3] is ABOVE the START anchor and was retained verbatim; the sub-slice ran 225→EOF [376]. The full sub-slice [Context + section H1 + Background + Variant-axes + L0-citations + BOTH near-duplicate `## L1 — per-element procedure` entries (276-310 FIRST + 317-376 SECOND) + interleaved Open-questions] collapsed into the single `## Plane-rotation stream (reduced — see plane_rotation_stream.md)` stub. ALSO updated the now-stale cycle-011 reduction-status banner at line 9 [it said the plane-rotation sub-slice "(lines 313+ below) ... is deferred to batch-3"; rewrote to record the cycle-012 reduction is DONE] — the exact stale-embedded-line-reference the report flagged as friction in caveat item 5; surgical correction of a banner the reduction renders stale. File 376→234 lines [-142]. Verified post-write: no orphaned plane-rotation procedure content remains [the only 2 `plane_rotation_step`/`back_solve_R`/`## L1 — per-element procedure` string hits are inside the stub's own descriptive prose, not live procedure bodies].)
- book/src/spec/slices/plane_rotation_stream.md (edit — PARTIAL REDUCTION + INVARIANT HOIST [proposed change 2]. FOUR edits, all anchor-and-replace. **2(A) applied FIRST** per the repairer's sequencing note [caveat item 10] — the orthog reduction depends on the unique LS-residual invariant being hoisted before the orthog sub-slice [whose only unique fragment was orthog.md:350] is eliminated: inserted a new `### Invariant` subsection between §L1 §Procedure and §Cross-target-reuse, carrying the formal `‖R·y − s‖² + |s[j+1]|² = ‖H̄·y − βe₁‖²` relation marked "Hoisted from the now-reduced orthog.md ... cycle-012". THEN 2(B): [i] reduction-status header note inserted after the H1; [ii] §L0 §Primitives prose-bullets → firm-side pointer [givens_generate / givens_apply, line-range citations retained]; [iii] §L1 §Procedure + §Cross-target-reuse → pointers [the §Invariant between them retained]; [iv] §L2 §Primitives + §Procedure → pointer [retaining §"Stream operations as primitive sequences" + everything below incl. the entire §L3, the canonical detailed obstruction source]. File 418→367 lines [net of the hoisted invariant + header note + the prose-to-pointer stubs].)
- book/src/spec/slices/divfree.md (edit — PARTIAL REDUCTION [proposed change 3]. THREE edits, all anchor-and-replace, all located by text-search per the proposed-change directive [not stale line numbers]: [A] reduction-status header note inserted after the `# Slice: divfree` H1 [records the 6 firm entries that cite the slice as load-bearing evidence + the pending L1/divfree-projector lift + the two pending-verify OQs]; [B-i] §"Optimization opacity" — removed the 4-bullet transparent list [PA-vs-assembled / GMG-vs-AMG / re-orthogonalization / fused-axpy], retained the 3-bullet load-bearing list [WeakDiv sign, step ordering, ksp tolerance] verbatim + added a firm-side pointer for the dropped transparent list; [B-ii] §"L4 tightening notes" → reduced pointer [the cycle-167 scratch-reclassification + V-polymorphism tightening is firm-side at state-stratification.md + derived-view-hoisting.md]. All L1/L2/L3/L4 projector-composition + defining-condition + load-bearing claims retained verbatim. File 414→413 lines [the two stubs roughly offset by the new header note].)
- scaffolding/open-questions.md (edit — 1 status-close + 5 new OQ sections):
  - status close: `orthog-plane-rotation-stream-sub-slice-batch-3-joint-audit` (cycle-011 OQ, opened by same-layer-cross-cutter; the OQ that SCHEDULED this joint audit) — `status: open` → `status: answered`; appended `answered_at: cycle-012` + `answered_in: book/src/spec/slices/orthog.md (...stub), book/src/spec/slices/plane_rotation_stream.md (...hoisted invariant)` fields + a closing paragraph documenting the full reduction, the both-L1-entries elimination [resolving the batch-2 "should be merged" finding via elimination], the invariant-hoist-first sequencing, the plane_rotation_stream partial-reduction, the 2 spun-out follow-up OQs, and the HIGH-severity line-map defect flagged for meta-phase. Explicitly surfaced by the dispatch as an amendment ("mark resolved-by-cycle-012-batch-3"); status-field close follows the established cycle-011/cycle-012 integrator-per-report precedent.
  - 5 new OQs appended at the end of the `## Open` section (before `## Investigating`): `plane-rotation-concept-page-canonical-pointer-repoint` [HEADLINE — 3 firm concept pages still cite the orthog slice as canonical; layer-intro-author follow-up], `l1-divfree-projector-promotion` [HEADLINE — strong harvester candidate; 6 firm entries cite the slice], `plane-rotation-givens-l0-citation-range-reconcile` [verify-citation-range; one-off off-by-one], `divfree-weakdiv-sign-convention-l0-verify` [verify-citation-range; sign would invert correction at every layer], `phase-1-corpus-reduction-batch-4-remaining-slices` [scheduling — cg_preconditioning_framework + sparse_triangular_solve are the final 2 of 10 slices; from caveat item 8].

Gate hits:
- firm-superseding-entries-exist (safety-net): 0 issues (verified all 17 cited firm/source entries exist on disk before applying: concepts/{plane-rotation-stream,givens,givens_generate,givens_apply,trsv,sequential-obstruction,apply_linop,ksp_solve,set_subvector_zero,state-stratification,derived-view-hoisting}.md + L1/{ksp_solve,eigsolve}.md + L0/{eigensolver-wrapper,mfem-wrapper-solver}.md + the 2 sibling slice files. The 6 divfree-dependent firm entries [ksp_solve / eigsolve / apply_linop / ksp_solve concept / set_subvector_zero concept / eigensolver-wrapper] all present; the plane-rotation firm concept-page family all present.)
- stub-pointers-navigable (safety-net): 0 issues (verified each reduced slice remains a navigable page: orthog.md retains the full Gram-Schmidt L0→L4 surface + a stub section with 5 canonical-entry pointers; plane_rotation_stream.md retains §Context/§Background/§L0-call-sites/§L0-negative-result/§L1-state/§L1-invariant/§L2-stream-ops/§L3 + pointers for the stubbed sub-sections; divfree.md retains the entire L1/L2/L3/L4 projector composition + a header note + 2 reduced-section pointers. No dangling stub-over-orphaned-content [the orthog full-file Write guaranteed no orphan; the repairer's HIGH-severity defect was exactly this risk and is averted].)
- unique-LS-residual-invariant-hoisted-before-reduction (safety-net): 0 issues (verified the formal invariant landed in plane_rotation_stream.md §L1 §Invariant [grep confirmed "Hoisted from the now-reduced" present exactly once] BEFORE the orthog sub-slice [whose §Invariant at orthog.md:350 was the one unique fragment] was eliminated. 2(A) was the first edit applied this dispatch; the orthog Write came after. Nothing lost.)
- retroactive-budget per-slice: 0 (these are corpus-reduction edits — supersession/redundancy maps backed by firm-entry pointers, explicitly authorized by the "Phase 1 corpus reduces as material is lifted" invariant; the one new surface [the hoisted invariant] carries its source anchor. Not retroactive evidence backfill on an operator entry.)
- retroactive-budget global: 0 (this report)
- concept_writes on existing slug: 0 (n/a — all edits are to spec/slices/* and open-questions; no concept-page writes)
- forward-edge claim without surface: 0 (all `[link]` targets in the new stub/pointer/header prose resolve on disk: concepts/{plane-rotation-stream,givens,givens_generate,givens_apply,trsv,sequential-obstruction,state-stratification,derived-view-hoisting}.md + the sibling slice path. The 6 divfree-citing firm entries are named as evidence pointers [plain refs], not as forward `[link]`s to non-existent surfaces. No dangling forward-edge.)
- edge-label / prose mismatch: 0 (these are Phase-1 slice files [L0/L1/L2/L3/L4 dissections], not lowering-layer themes carrying `L_{n+1}→L_n` edge labels; the layer-section references in the reduction maps are internally consistent with the slice structures. Critic edge-label-fidelity = pass.)
- H1 reuses page heading: 0 (the eliminated `# Orthogonalization (plane-rotation stream)` H1 [a page-heading-reuse-inside-the-sub-slice] is GONE — the stub supplies its own H2 `## Plane-rotation stream (reduced ...)`. Net effect: a pre-existing H1-reuse defect was REMOVED by this reduction. No new H1 added in any file.)
- append on missing slug: 0 (the OQ close targets the existing `orthog-plane-rotation-stream-sub-slice-batch-3-joint-audit` slug; the 5 new OQs append fresh sections in the `## Open` section.)
- variant-axis missing on multi-variant operator: 0 (n/a — slice reductions, not operator entries; the divfree variant axes [VecType/H1-depth/empty-boundary] + plane-rotation axes [scalar-type/generation-formula/storage] are retained in the un-reduced sections of the respective slices; critic variant-axis-coverage = warning was about the omitted FIRST L1 entry which is now eliminated.)
- SUMMARY.md chapter registration auto-fix: 0 (n/a — no new chapter/concept page created; spec/slices/* are NOT SUMMARY-registered [Phase-1 corpus is not in the book nav]; no SUMMARY edit needed or proposed.)
- concepts/<slug>.md SUMMARY registration auto-fix: 0 (n/a — no new concept page)
- index-placeholder displacement auto-fix: 0 (n/a — no index.md touched; no placeholder)
- bookkeeping incomplete: 0

Open questions promoted:
- plane-rotation-concept-page-canonical-pointer-repoint (new; opened_at cycle-012, opened_by same-layer-cross-cutter; HEADLINE — layer-intro-author follow-up)
- l1-divfree-projector-promotion (new; opened_at cycle-012, opened_by same-layer-cross-cutter; HEADLINE — harvester candidate)
- plane-rotation-givens-l0-citation-range-reconcile (new; opened_at cycle-012, opened_by same-layer-cross-cutter)
- divfree-weakdiv-sign-convention-l0-verify (new; opened_at cycle-012, opened_by same-layer-cross-cutter)
- phase-1-corpus-reduction-batch-4-remaining-slices (new; opened_at cycle-012, opened_by same-layer-cross-cutter; scheduling — final 2 of 10 slices)

OQ closed:
- orthog-plane-rotation-stream-sub-slice-batch-3-joint-audit (status: open → answered; answered_in the 2 reduced slice files) — the OQ that scheduled this joint audit; explicitly surfaced by the dispatch as an amendment.

Build-relevant: no (edits touch ONLY book/src/spec/slices/*.md [the frozen Phase-1 corpus] + scaffolding/open-questions.md. The slice corpus lives under book/src/ BUT is NOT SUMMARY-registered — it is not part of the rendered mdBook nav. THIS report's edits do not add/remove book nav entries and do not touch any SUMMARY-registered page, so this report alone does not require a rebuild. The cycle-wide rebuild is already mandated by the other 6 build-relevant rows.)

Notes:
- `overall_status: ready` (pass-after-repair) confirmed in META.md frontmatter before applying. The HIGH-severity line-map defect [Issue 1] was corrected by the repairer to the FULL sub-slice [225-376, text-anchored] and I applied that corrected scope. Clean apply of all 3 proposed changes [orthog full-reduction via Write; plane_rotation_stream 4-edit partial-reduction+hoist; divfree 3-edit partial-reduction]. No deferred/unparseable blocks.
- **Sequencing honored (the critical correctness item)**: proposed change 2(A) [hoist the unique LS-residual invariant into plane_rotation_stream.md] was applied as the FIRST edit of this dispatch, BEFORE the orthog full-reduction [proposed change 1] eliminated the orthog §Invariant [the only unique fragment]. Per the repairer's §Suggested-resolution note + caveat item 10. Nothing lost.
- **Text-anchor discipline honored**: the orthog START boundary was verified by `grep -c "^## Context"` [=1] and `grep -c "^# Orthogonalization (plane-rotation stream)"` [=1] and confirming the Context immediately precedes the H1 — i.e. the START boundary was verified, NOT just the END [EOF]. This directly addresses the friction the report flagged [the audit's own grep verified END but not START, causing the line-map defect]. The divfree §Optimization-opacity + §L4-tightening-notes anchors were located by text-search per the proposed-change directive.
- **Stale-banner correction (drive-by, in-scope)**: updated orthog.md:9's cycle-011 reduction-status banner [it claimed the plane-rotation sub-slice "(lines 313+ below) ... is deferred to batch-3"]. Since THIS dispatch IS batch-3 and the reduction is now done, leaving that banner would itself be a stale-embedded-line-reference [the exact friction the report names in caveat item 5]. Surgical one-paragraph update within the file I was already reducing; in book-write scope.
- **[META SIGNAL — phase-1-slice-reduction-audit skill candidate at recurrence-3 + severity escalation] for integrator-finalize → scaffolding/integrator-signals.md → meta-phase batch-2 (cycles 010/011/012).** The HIGH-severity line-map defect this report carried [the audit's own grep verified the END boundary (EOF) but NOT the START boundary, mis-identifying the sub-slice as starting at line 311/317 when it starts at 225] is a MATERIALLY WORSE manifestation of `phase-1-corpus-audit-line-range-arithmetic-brittleness` [recurrence-2 as of cycle-011] than a stale prior-reference — it is a fresh mis-scope DURING the audit. The report itself recommends [caveat item 5] the friction-ledger entry bump to recurrence-3 AND notes the `phase-1-slice-reduction-audit` skill-candidate is now a STRONG promotion candidate [the mitigation is mechanical: "verify a sub-slice's START boundary, not just its END, when enumerating; reduction header notes should use section-heading text-anchors, not absolute line numbers"]. Finalize should append this to integrator-signals so meta-phase [which fires after THIS cycle, closing batch-2] can judge promotion. This is the headline non-content signal of this report.
- **Skill-uptake telemetry**: the critic flagged `verify-citation-range` [the report surfaces 2 citation-range OQs but did not invoke the skill] + `classify-variant-axis` [the divfree/plane-rotation variant surveys done by hand] as applicable-but-unreferenced [META §skill-uptake-survey = warning, non-blocking]. This is the EIGHTH consecutive cycle-012 report with a skill-uptake gap — ALL 8 cycle-012 dispatches now show it. The pattern is fully cycle-wide; a strong meta-phase aggregation signal for batch-2.
- Deferred `integrated_at` (and `integration_commit`) to integrator-finalize per role-spec / CLAUDE.md §Write-authority partition — the consumed report's frontmatter `status: pending` is left untouched by this per-report integrator.
- **CYCLE-012 COMPLETE**: this is the 8th and FINAL per-report integration of cycle-012 (closes meta-batch-2). All 8 reports integrated. Finalize next: rebuild book [6 of 8 rows build-relevant], mark all 8 consumed reports' integrated_at + integration_commit, write log/cycle-012.md, append cycle-record.jsonl + integrator-signals.md [incl. the 2 carry-forward resolutions from row 7 (L4/index.md:40 SUPERSEDED flag chain) and the recurrence-3 skill-promotion signal from this row], update roadmap, single commit+push, emit batch CYCLE.md, then meta-phase fires.

---
