---
agent: layer-intro-author
invoked_at: 2026-06-01T21:07:00Z
scope: cycle-051 consolidated count-ownership — L3>L2 + L2>L1 firm tallies + degenerate-cohort discharge narrative (D5, sole count-owner; D1–D4 deferred all consolidated tallies)
status: integrated
integrated_at: 2026-06-01T22:14:50Z
integration_commit: PLACEHOLDER_SHA
integration_notes: "Applied clean by integrator-per-report (D5 of cycle-051, SOLE count-owner); finalized cycle-051. Consolidated tallies: L3>L2 firm 13->5, L2>L1 firm 17->10 (+1 partly-constructive), L3 firm UNCHANGED at 17 + 3 partial-obstruction; degenerate-cohort discharge narrative; serial-order + reconcile-on-C honored; finalize re-verified on-disk survivor counts (L3-L2 = 5, L2-L1 = 11 = 10 firm + 1 pc); build exit 0."
inputs:
  - reports/2026-06-01T210700Z-lifter-linear-combination-family-demotion/CYCLE.md (D1)
  - reports/2026-06-01T210700Z-lifter-inner-product-dot-demotion/CYCLE.md (D2)
  - reports/2026-06-01T210700Z-lifter-nrm2-consumer-demotion/CYCLE.md (D3)
  - reports/2026-06-01T210700Z-lifter-jacobi-divfree-demotion/CYCLE.md (D4)
  - book/src/L3-L2/index.md (consolidated L3>L2 firm tally + cohort-header + Working-Notes growth-log)
  - book/src/L2-L1/index.md (consolidated L2>L1 firm tally + cohort-growth-log head)
  - book/src/L3/index.md (degenerate-cohort discharge narrative; L3 firm count UNCHANGED at 17)
---

# CYCLE: L3>L2 + L2>L1 consolidated count tally (cycle-051 sole count-owner)

## Summary

I am the **sole consolidated-count owner** for the cycle-051 demotion wave. D1–D4 each removed only their OWN dep-map rows + SUMMARY lines + in-line notes and DEFERRED all consolidated tallies to me. This report writes the single authoritative post-cohort firm counts in the three indexes and reconciles the §Working-Notes degenerate-cohort discharge narrative.

**Recomputed post-cohort firm counts (enumerated below, not trusted blind):**

- **L3>L2 firm: 13 → 5.** Eight `-body-identity` themes deleted (D1: `scal`/`axpy`/`axpby`/`axpbypcz`-body = 4; D2: `dot`-body = 1; D3: `nrm2`-body = 1; D4: `jacobi-smoother`-body + `divfree-projector`-body = 2). Survivors (5): `krylov-step-body-identity`, `ksp-solve-outer-driver`, `orthogonalize-variant-split`, `eigsolve-opaque-eigen-iteration`, `chebyshev-nested-recurrence`. **Matches the dispatch-expected 5.**
- **L2>L1 firm: 17 → 10 (+ 1 partly-constructive UNCHANGED).** Seven `-leaf-identity` themes deleted (D1: `scal`/`axpy`/`axpby`/`axpbypcz`-leaf = 4; D2: `dot`-leaf = 1; D3: `nrm2`-leaf = 1; D4: `jacobi-smoother`-leaf = 1). `divfree-projector-leaf-identity` SURVIVES (D4 KEEP-substantive). Survivors (10 firm): `chebyshev-iteration-fusion`, `linear-combination-fold-specialization`, `inner-product-fold-specialization`, `orthogonalize-composition-lowering`, `gram-fold-specialization`, `eigsolve-spectral-transform-composition`, `divfree-projector-leaf-identity`, `incremental-least-squares-composition-lowering`, `ksp-solve-outer-driver-unfold`, `krylov-step-kernel-defusion`; + `deflate-composition-lowering` (partly-constructive, unchanged). **Matches the dispatch-expected 10.**
- **L3 firm: UNCHANGED at 17 firm + 3 partial-obstruction.** The four BLAS-1 leaves + `dot`/`nrm2` are re-expressed in place (D1/D2/D3), NOT deleted; the two combinators were already counted in the c050 17. No L3 operator count edit.

**Degenerate-cohort discharge.** The degenerate identity-in-named-terms cohort spanning the D8 −1-corrected **17 degenerate {body,leaf} pairs** is now **FULLY DISCHARGED**. Counting at the **theme** unit (the unit the breakdown enumerates), **23 themes demoted + 1 KEPT-substantive = 24 themes across both edges of the cohort**: c050 demoted 8 themes (4 pairs `assemble-diagonal`/`elementwise-product`/`reciprocal`/`normalize`, both edges); c051 demoted 15 themes (the `linear_combination` family `scal`/`axpy`/`axpby`/`axpbypcz` both edges = 8, `dot` both edges = 2, `nrm2` both edges = 2, `jacobi-smoother` both edges = 2, `divfree-projector`-body = 1) — total demoted 8 + 15 = 23. The lone KEPT theme is `divfree-projector-leaf-identity` (the genuine step-4 `Grad->AddMult` fusion rotation, the one edge of the `divfree-projector` pair that is NOT identity-in-named-terms). Equivalently at the **pair** unit: of the 17 degenerate pairs (the D8 −1-corrected denominator after `divfree-projector`'s leaf edge was reclassified substantive), all 16 fully-degenerate pairs demoted on both edges and the 17th (`divfree-projector`) demoted body-only with its leaf edge KEPT. The **refactor-pass theme-demotion sweep is COMPLETE** at the clean batch-15-meta stopping point. The leaf-CHAPTER deletions (the `L2/`/`L3/` operator chapters whose themes demoted) remain HELD/meta-gated under OQ `collapsed-leaf-disposition-convention-cohort-wide` — only the THEME files are deleted this cohort.

## Proposed changes

### A. `book/src/L3-L2/index.md` — consolidated L3>L2 firm tally + cohort-header reconcile

#### A.1 — reconcile the gutted BLAS-1-leaf cohort-header (D1/D2/D3 removed all six member bullets beneath it)

The §Vocabulary-cohort sub-header `*Identity-in-form BLAS-1-leaf body edges (cycle-041; FOLD-PARENTED …)*` (line 34) headed six bullets (`dot`/`axpy`/`axpby`/`axpbypcz`/`nrm2`/`scal`-body-identity, lines 36–41) that D1 (4) + D2 (1) + D3 (1) remove. Collapse the now-empty cohort-header to a single discharge note. **Anchor is the header line only — D1/D2/D3 own the member bullets; this targets the italic intro paragraph they leave behind.**

```edit:book/src/L3-L2/index.md
[old]*Identity-in-form BLAS-1-leaf body edges (cycle-041; FOLD-PARENTED — the L3>L2 thin-identity edges of the BLAS-1 leaves whose L2 floors landed cycle-041; the leaf-primitive analogue of `krylov-step-body-identity` with NO wrapper rotation — the leaf has no `(op, K, s)` tuple and no outer loop):*
[new]*Identity-in-form BLAS-1-leaf body edges (cycle-041; FOLD-PARENTED) — **ALL SIX DEMOTED cycle-051** (the refactor-pass enactment under the 2026-06-01 VOCABULARY-SHIFT REDIRECT). The scalar-weighted-sum members `scal`/`axpy`/`axpby`/`axpbypcz`-`body-identity` (D1) collapsed into the firm [`linear_combination`](../L3/linear_combination.md) combinator's §"Downward to L2" identity note (the four L3 leaves re-expressed to speak through the combinator as arity-1/2/2/3 specializations); `dot-body-identity` (D2) collapsed into the firm [`inner_product`](../L3/inner_product.md) combinator's §"Downward to L2" note (`dot` re-expressed as the Hermitian/symmetric specialization); `nrm2-body-identity` (D3) demoted to an in-line §"Downward to L2" CONSUMER note on `book/src/L3/nrm2.md` (`nrm2` = `√ ∘ abs ∘ inner_product` consumer at `y=x`, NOT a fold member — do-NOT-merge, so demoted onto its own entry, not absorbed into the combinator). Each was a degenerate identity-in-named-terms lowering (the §1d smell the redirect names — same signature / laws / variant profile across the edge); the theme files are deleted, the rotations captured in-line / through the combinator homes. No member bullets remain under this header:*
```

#### A.2 — consolidated L3>L2 firm tally (Working-Notes cohort-growth bullet)

**SERIAL-ORDERING DEPENDENCY — coordinate with D4.** D4 (jacobi/divfree report, change #6) rewrites this same Working-Notes bullet (originally line 61, `Cohort growth (firm 17 → 13; cycle-050 …)`) to a `firm 17 → 13 → 11` form accounting ONLY for the c050 4 + D4's own 2 L3>L2 deletions. That `11` is correct relative to D4's scope but is NOT the consolidated post-cohort count — it omits D1's 4 + D2's 1 + D3's 1 = 6 further L3>L2 deletions. As the sole count-owner I supersede it with the true consolidated **5**. The integrator applies D4 BEFORE D5 (serial), so my `[old]` below is **D4's `[new]` text verbatim** (the post-D4 on-disk state). If the integrator applies D5 before D4, this anchor will not match — apply D4 first.

```edit:book/src/L3-L2/index.md
[old]- **Cohort growth (firm 17 → 13 cycle-050 → 11 cycle-051; degenerate-theme demotion).** The refactor-pass enactment under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) **demoted six thin `-body-identity` L3>L2 themes to in-line §"Downward to L2" notes** on their L3 operator entries across two cycles. Cycle-050 (D3–D6): `assemble-diagonal-body-identity`, `elementwise-product-body-identity`, `reciprocal-body-identity`, `normalize-body-identity`. **Cycle-051 (D4): `jacobi-smoother-body-identity` + `divfree-projector-body-identity`** — the two gated constructed-operator-gate pairs the c049 D3 audit flagged "verify-body-before-demoting", both confirmed DEMOTE-OK by the D8 verify-body audit (`reports/2026-06-01T195100Z-cross-layer-cross-cutter-verify-divfree-jacobi/CYCLE.md`). Each was a degenerate identity-in-named-terms lowering (same signature / same laws / same variant profile across the edge — the §1d smell the redirect names), so its dedicated theme chapter was deleted and its load-bearing facts folded into an in-line note on the standalone L3 operator entry (none has a fold-parent, so no operator chapter collapses). **The L3>L2 rotation for each operator remains captured** (now as an in-line identity note, not a theme file) — the coverage-gap is unaffected in *kind*; only the firm-theme COUNT drops, **17 → 13 → 11** (6 thin-identity themes deleted; the 4 substantive themes + the remaining 7 thin-identity themes stay). The remaining cohort: thin `-body-identity` (7) — `krylov-step`, `dot`, `nrm2`, `scal`, `axpy`, `axpby`, `axpbypcz`; substantive / non-identity (4) — `ksp-solve-outer-driver`, `orthogonalize-variant-split`, `eigsolve-opaque-eigen-iteration`, `chebyshev-nested-recurrence`. *(Prior — coverage-gap history, retained:* cohort grew firm 2→5→10→14→15→17 across cycles 041–045; the `l3-l2-rotation-theme-coverage-gap` reached 17-of-18 effectively complete at cycle-045, the residual 1 being `apply_linop` (lowers L3→L1 directly, no L2 entry / no L3-L2 theme by design — the cycle-010 "CONFIRMED-NOT-NEEDED-WITH-CAVEAT"). The cycle-050/051 demotion does NOT re-open the coverage-gap: the demoted operators' rotations are captured in-line, not lost.)*
[new]- **Cohort growth (firm 17 → 13 cycle-050 → 5 cycle-051; CONSOLIDATED post-cohort count — degenerate-theme demotion COMPLETE).** The refactor-pass enactment under the 2026-06-01 VOCABULARY-SHIFT REDIRECT (`METHODOLOGY-REDIRECT.md`) demoted **twelve thin `-body-identity` L3>L2 themes** to in-line / combinator-home notes across two cycles. **Cycle-050 (4, D3–D6):** `assemble-diagonal-body-identity`, `elementwise-product-body-identity`, `reciprocal-body-identity`, `normalize-body-identity` (standalone / fused-composite floors, NO fold-parent — demoted to in-line §"Downward to L2" notes on their L3 entries). **Cycle-051 (8, D1–D4):** the `linear_combination` family `scal`/`axpy`/`axpby`/`axpbypcz`-body-identity (D1 — collapsed into the firm [`linear_combination`](../L3/linear_combination.md) combinator's §"Downward to L2" note, the four L3 leaves re-expressed through the combinator), `dot-body-identity` (D2 — collapsed into the firm [`inner_product`](../L3/inner_product.md) combinator's §"Downward to L2" note), `nrm2-body-identity` (D3 — demoted to an in-line CONSUMER note on `L3/nrm2.md`, do-NOT-merge), and the two gated constructed-operator-gate pairs `jacobi-smoother-body-identity` + `divfree-projector-body-identity` (D4 — DEMOTE-OK per the D8 verify-body audit `reports/2026-06-01T195100Z-cross-layer-cross-cutter-verify-divfree-jacobi/CYCLE.md`, demoted to in-line §"Downward to L2" notes on their L3 entries). Each was a degenerate identity-in-named-terms lowering (the §1d smell the redirect names); the theme files are deleted and the rotations captured in-line / through the combinator homes (no operator chapter collapses — the leaf CHAPTERS are re-expressed-in-place / retained, only the THEME files delete). **The L3>L2 rotation for each operator remains captured** — the coverage-gap is unaffected in *kind*; only the firm-theme COUNT drops, **17 → 13 → 5**. The **5 surviving firm L3>L2 themes** are all substantive or the lone multi-primitive body-identity: `krylov-step-body-identity` (the five-primitive-group body identity + wrapper-surface adjustments — the one identity-theme that survives, distinct from the demoted single-leaf/gate degenerates), `ksp-solve-outer-driver`, `orthogonalize-variant-split`, `eigsolve-opaque-eigen-iteration`, `chebyshev-nested-recurrence` (the four substantive / non-identity iteration-rotation themes across the four erasure-scope roots). *(Prior — coverage-gap history, retained:* cohort grew firm 2→5→10→14→15→17 across cycles 041–045; the `l3-l2-rotation-theme-coverage-gap` reached 17-of-18 effectively complete at cycle-045, the residual 1 being `apply_linop` (lowers L3→L1 directly, no L2 entry / no L3-L2 theme by design — the cycle-010 "CONFIRMED-NOT-NEEDED-WITH-CAVEAT"). The cycle-050/051 demotion does NOT re-open the coverage-gap: the demoted operators' rotations are captured in-line / through the combinator homes, not lost — the firm-theme count drop reflects only the *vehicle* change (theme file → in-line note / combinator §"Downward" note).)*
```

### B. `book/src/L2-L1/index.md` — consolidated L2>L1 firm tally (cohort-growth-log head)

The cohort-growth-log head bracket `**[cycle-050 DEMOTION — firm 21 → 17 …]**` (line 73 head) records the c050 count. The consolidated post-c051 count drops the 7 deleted `-leaf-identity` firm themes (D1: 4; D2: 1; D3: 1; D4: 1 — `divfree-projector-leaf-identity` SURVIVES). Update the head bracket to the consolidated `17 → 10`. **Anchor is the head bracket substring only** — D4 (jacobi/divfree report) edits the LATER "NOTE the cycle-050-vs-051 split" sentence within the same logical line; the two substrings do not overlap (this head precedes D4's sentence). D3 (OQ-1) explicitly deferred this count to me.

```edit:book/src/L2-L1/index.md
[old]- Cohort growth log (most-recent first): **[cycle-050 DEMOTION — firm 21 → 17 (= 17 firm + 1 partly-constructive; 4 thin `-leaf-identity` themes deleted)]** the 2026-06-01 vocabulary-shift-redirect refactor-pass demoted four degenerate identity-in-named-terms `-leaf-identity` L2>L1 themes to in-line §"Downward to L1" notes on their L2 operator entries — `assemble-diagonal-leaf-identity` (D3), `elementwise-product-leaf-identity` (D4), `reciprocal-leaf-identity` (D5), `normalize-leaf-identity` (D6); each was the L2>L1 thin-identity edge of a standalone leaf / gate / fused-composite floor with NO fold-parent (so no operator chapter collapses — only the thin theme files are deleted, their content folded into in-line notes), value-thread-isomorphic across the edge (same signature / laws / variant profile — the §1d smell the redirect names). The L2>L1 rotation for each remains captured (in-line, not a theme file); only the firm-theme COUNT drops.
[new]- Cohort growth log (most-recent first): **[cycle-051 DEMOTION — CONSOLIDATED firm 21 → 17 (cycle-050) → 10 (cycle-051) (= 10 firm + 1 partly-constructive; 11 thin `-leaf-identity` themes deleted across the two cycles)]** the 2026-06-01 vocabulary-shift-redirect refactor-pass demoted eleven degenerate identity-in-named-terms `-leaf-identity` L2>L1 themes to in-line / combinator-home notes. **Cycle-050 (4, D3–D6):** `assemble-diagonal-leaf-identity`, `elementwise-product-leaf-identity`, `reciprocal-leaf-identity`, `normalize-leaf-identity` (standalone leaf / gate / fused-composite floors, NO fold-parent — demoted to in-line §"Downward to L1" notes on their L2 entries). **Cycle-051 (7, D1–D4):** the `linear_combination` family `scal`/`axpy`/`axpby`/`axpbypcz`-`leaf-identity` (D1 — their L2>L1 edge absorbed into the KEPT `linear-combination-fold-specialization` fold-parent, the four L3 leaves re-expressed through the combinator), `dot-leaf-identity` (D2 — absorbed into the `inner_product` combinator homes + the KEPT `inner-product-fold-specialization`), `nrm2-leaf-identity` (D3 — demoted to an in-line §"Downward to L1" CONSUMER note on `book/src/L2/nrm2.md`, do-NOT-merge), and `jacobi-smoother-leaf-identity` (D4 — DEMOTE-OK per the D8 verify-body audit, demoted to an in-line §"Downward to L1" note on `book/src/L2/jacobi-smoother.md`). **`divfree-projector-leaf-identity` is NOT demoted — KEEP-substantive** (the lone genuine step-4 `Grad->AddMult` apply-accumulate fusion rotation; it stays a standalone theme). Each demoted theme was value-thread-isomorphic across the edge (the §1d smell the redirect names); the theme files are deleted, the rotations captured in-line / through the fold-parents (no operator chapter collapses — only the thin theme files delete). The L2>L1 rotation for each remains captured; only the firm-theme COUNT drops, **17 → 10** (+ 1 partly-constructive `deflate-composition-lowering`, unchanged). The **10 surviving firm L2>L1 themes**: `chebyshev-iteration-fusion`, `linear-combination-fold-specialization`, `inner-product-fold-specialization`, `orthogonalize-composition-lowering`, `gram-fold-specialization`, `eigsolve-spectral-transform-composition`, `divfree-projector-leaf-identity` (KEEP-substantive), `incremental-least-squares-composition-lowering`, `ksp-solve-outer-driver-unfold`, `krylov-step-kernel-defusion`.
```

### C. `book/src/L3/index.md` — degenerate-cohort discharge note (L3 firm count UNCHANGED at 17)

The L3 firm-operator count is UNCHANGED (the leaves are re-expressed in place, not deleted; the combinators were already counted in the c050 17). I do NOT touch the authoritative L3 count tally (line 63) — it stays `17 firm + 3 partial-obstruction`. I append a discharge note to the cycle-050 combinator bullet (line 65) recording that the cycle-051 leaf re-expression is now done and the theme-demotion sweep is complete. **Anchor is the tail clause of the line-65 cycle-050 bullet** (the "NOT yet re-expressed … that is cycle-051" forward-reference D1/D2 left for me to resolve at the consolidated level; D1/D2 resolved the per-leaf entries but deferred this index-level summary clause to the count-owner).

```edit:book/src/L3/index.md
[old]The four L3 BLAS-1 leaves + the `dot`/`nrm2` reductions are **NOT yet re-expressed through** these combinators (each still stands firm in its own right) — that leaf re-expression/slim is cycle-051, gated on the cohort-wide leaf-disposition convention (OQ `l2-l3-leaf-chapter-disposition-cohort-wide`). The L3>L2 edge of each combinator is a degenerate identity-in-named-terms in-line note (the pre-built home the four `{scal,axpy,axpby,axpbypcz}-body-identity` / the `dot-body-identity` themes demote into at cycle-051); the substantive rotation in each chain is the existing firm L2>L1 fold-specialization theme (`linear-combination-fold-specialization` / `inner-product-fold-specialization`, both KEPT cycle-049). This bullet's count update is folded into the authoritative tally above (firm 15 → 17).
[new]The four L3 BLAS-1 leaves (`scal`/`axpy`/`axpby`/`axpbypcz`) + the `dot` reduction were **re-expressed through these combinators cycle-051** (D1/D2 — `scal`/`axpy`/`axpby`/`axpbypcz` speak through [`linear_combination`](./linear_combination.md) as arity-1/2/2/3 specializations; `dot` is the Hermitian/symmetric specialization of [`inner_product`](./inner_product.md)); `nrm2` was demoted-as-CONSUMER (D3 — `√ ∘ abs ∘ inner_product` at `y=x`, NOT a fold member, do-NOT-merge — its identity note lands on its own entry, not absorbed into the combinator). Each L3 leaf chapter still stands firm in its own right (re-expressed in place, NOT deleted — the leaf-CHAPTER disposition remains HELD/meta-gated under OQ `l2-l3-leaf-chapter-disposition-cohort-wide` / `collapsed-leaf-disposition-convention-cohort-wide`; only the THEME files were deleted). The L3>L2 edge of each combinator is now a degenerate identity-in-named-terms in-line §"Downward to L2" note (the home the four `{scal,axpy,axpby,axpbypcz}-body-identity` / the `dot-body-identity` themes were demoted into cycle-051 — those theme files deleted, their identity-in-form content absorbed); the substantive rotation in each chain is the existing firm L2>L1 fold-specialization theme (`linear-combination-fold-specialization` / `inner-product-fold-specialization`, both KEPT cycle-049). With cycle-051 the **refactor-pass theme-demotion sweep is COMPLETE** (the degenerate identity-in-named-terms cohort fully discharged across the D8 −1-corrected 17 degenerate {body,leaf} pairs — 23 themes demoted + 1 KEPT-substantive `divfree-projector-leaf-identity`; equivalently, all 16 fully-degenerate pairs demoted both edges and the 17th `divfree-projector` pair demoted body-only with its leaf edge KEPT; consolidated counts at `L3-L2/index.md` / `L2-L1/index.md` §Working-Notes). This bullet's count update remains folded into the authoritative tally above — **L3 firm count is UNCHANGED at 17** (the leaf re-expression is in-place; the two combinators were counted at c050).
```

## Supporting evidence

- **Producer reports (provenance for each delta):**
  - D1 `reports/2026-06-01T210700Z-lifter-linear-combination-family-demotion/CYCLE.md` — deletes 8 themes (`{scal,axpy,axpby,axpbypcz}-body-identity` 4 L3>L2 + `{scal,axpy,axpby,axpbypcz}-leaf-identity` 4 L2>L1); re-expresses the 4 L3 leaves through `L3/linear_combination`; removes its own 4 L3>L2 rows + 4 L2>L1 rows + 8 SUMMARY lines.
  - D2 `reports/2026-06-01T210700Z-lifter-inner-product-dot-demotion/CYCLE.md` — deletes `dot-body-identity` (L3>L2) + `dot-leaf-identity` (L2>L1); re-expresses `L3/dot` through `L3/inner_product`; removes its own rows + SUMMARY lines.
  - D3 `reports/2026-06-01T210700Z-lifter-nrm2-consumer-demotion/CYCLE.md` — deletes `nrm2-body-identity` (L3>L2) + `nrm2-leaf-identity` (L2>L1); demotes-as-consumer onto the `nrm2` entries (NOT absorbed into `inner_product`); removes its own rows + SUMMARY lines; explicitly defers the tally (OQ-1).
  - D4 `reports/2026-06-01T210700Z-lifter-jacobi-divfree-demotion/CYCLE.md` — deletes `jacobi-smoother-body-identity` + `jacobi-smoother-leaf-identity` + `divfree-projector-body-identity` (3); KEEPS `divfree-projector-leaf-identity` (substantive fusion rotation); removes its own rows + SUMMARY lines + edits the L3-L2 line-43 header / line-61 growth-bullet / L2-L1 line-73 split-sentence locally (`17→13→11`) — which I supersede here at the consolidated level.
- **On-disk pre-cohort counts:** `L3-L2/index.md:61` (firm 17 → 13, c050); `L2-L1/index.md:73` (firm 21 → 17, c050); `L3/index.md:63` (L3 17 firm + 3 partial-obstruction, authoritative tally).
- **Row enumeration verifying the survivors** (counted directly off the on-disk theme tables): L3>L2 table rows 13–25 = 13 themes, minus 8 deletions = 5; L2>L1 table rows 13–30 = 18 (17 firm + 1 partly-constructive), minus 7 firm deletions = 10 firm + 1 partly-constructive.
- **D8 −1 denominator correction** (`L2-L1/index.md:73`, `L3-L2/index.md:63`): the degenerate-cohort denominator is 17 (not 18) because `divfree-projector-leaf-identity` is substantive — carried forward into the discharge narrative (16 demoted + 1 KEPT = 17).

## Open questions / caveats

- **SERIAL-ORDERING DEPENDENCY (change A.2) — D4 must be applied before D5.** D4 rewrites the L3-L2 Working-Notes cohort-growth bullet to a local `17 → 13 → 11` (correct only for c050 + D4's own 2 deletions). My A.2 `[old]` is **D4's `[new]` text verbatim**; the integrator-per-report serial order (D1→D2→D3→D4→D5) makes this match on-disk. If for any reason D5 is applied before D4, the A.2 anchor will fail to match — apply D4 first. The dispatch brief flagged this exact coordination (D4 "already adjusted some cohort-count narrative locally (`17→13→11`) — reconcile to the FINAL correct numbers"); the final consolidated L3>L2 firm count is **5**, not 11.
- **No collision on L2-L1 line 73.** My change B targets the head bracket `**[cycle-050 DEMOTION — firm 21 → 17 …]**` and its immediately-following demotion-narrative sentence (the four c050 demotions). D4 edits the LATER "NOTE the cycle-050-vs-051 split" sentence within the same logical line (a non-overlapping substring after the `[/cycle-050]` close is NOT in my `[old]`; my `[old]` ends before the "NOTE the cycle-050-vs-051 split" sentence). D3 (OQ-1) deferred this head count to me. The integrator should confirm B's `[old]` and D4's line-73 `[old]` are disjoint substrings (they are: my `[old]` is the head + the first c050-demotion sentence; D4's is the split-note sentence further along).
- **L2/index.md cohort-growth-log historical mentions (`:118`/`:121`/`:123`) NOT touched here.** D3 (OQ-1) flagged that `L2/index.md` carries bare-code-span historical mentions of `nrm2-*-identity` (and siblings) as firm themes — non-build-breaking (not live links) but now naming demoted slugs. These were NOT in my dispatch scope (my three files are `L3-L2/index.md`, `L2-L1/index.md`, `L3/index.md`). Recommend a batch-16 micro-sweep (or integrator-finalize bounded touch) to annotate them `(demoted cycle-051)` for narrative honesty; non-blocking.
- **Residual stale future-tense in `L3/linear_combination.md` (D1 OQ, NOT applied by D1 or D5).** D1 flagged §"Arity specializations"/§Status/§Dependencies/§Evidence forward-tense passages ("still exist firm as of this cycle / their collapse … is cycle-051 (gated …)") as non-build-breaking tense touches deferred to a follow-up sweep. These are operator-CHAPTER edits (not index tallies), so outside my count-owner scope; flagged for batch-16 or a dedicated micro-sweep. The only build-breaking link (`:111` → deleted `axpy-body-identity`) D1 handles in its change (e).
- **Leaf-CHAPTER disposition remains HELD/meta-gated.** This cohort deletes only THEME files; the `L2/`/`L3/` operator chapters whose themes demoted are re-expressed-in-place / retained. The cohort-wide leaf-chapter disposition (whether the `L2/dot.md` / `L2/scal.md` etc. floor chapters reduce-to-stub now that their themes are gone) is OQ `collapsed-leaf-disposition-convention-cohort-wide` / `l2-l3-leaf-chapter-disposition-cohort-wide` — a separate meta-gated decision, NOT enacted this cycle. The refactor-pass theme-demotion sweep is COMPLETE at this clean batch-15-meta stopping point.
