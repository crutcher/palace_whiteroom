---
agent: cycle-planner
invoked_at: 2026-06-07T203807Z
scope: cycle-134 dispatch plan
status: pending
---

# Cycle 134 dispatch plan

## Goals selected this cycle

Cycle-134 is the **MIDDLE (2/3) primary cycle of meta-batch-43** (cycles 133/134/135; the batch-43 meta fires after c135's finalize). The batch-43 LEAD is the human-chosen **(C) GATED sharding-MATH exploration**. The c133 OPENER ran the WAVE-1 **hard spine-non-destabilization PROBE** and it returned **CLEAR on BOTH arms** (D1 vertical = SPLIT-leaning-CLEAR: general decomposition-reduce abstraction CLEAR, Dörfler cross-rank candidate NO-CLEAR/stays-deferred; D2 lateral = ALL-GREEN ZERO-RED: every firm reduce/fold combinator is composable-by-`reference`, none re-rooted). The combined gate is CLEAR → **WAVE-2 is GREENLIT**.

Therefore c134 dispatches **WAVE-2: the `sharding-math-decomposition-abstraction-sketch`** — the LEAD. The sketch lands as a **single `roadmap_goal` (rank-0) L4 chapter** authoring the sub-domain decomposition-as-restriction-and-compose math, composing the EXISTING firm reduce roots **BY NAME via `reference`-class edges ONLY** (the hard tripwire from both probe arms). The MAINTENANCE FLOOR standing-hygiene audit runs alongside (re-baselining against the WAVE-2 landing per the standing caveat). MPI/distributed mechanics stay OUT (DIRECTIVE-1 boundary — cited as deferred-future mechanism, never lifted).

**One chapter, not a cohort** — see `## Open questions / caveats` for the rationale.

## Deliverable-presence verification

Per the MANDATORY pre-dispatch deliverable-presence check (paste-inline-evidence requirement, batch-10 strengthening).

**D1 — `sharding-math-decomposition-abstraction-sketch` (new L4 roadmap_goal chapter):** OPEN BY CONSTRUCTION (a fresh `roadmap_goal` sketch authored this cycle, no prior-cycle history — the WAVE-2 work was explicitly DEFERRED to c134 by the c133 plan + greenlit by the c133 finalize integrator-signals). Verified the slug is genuinely new and not already discharged:
- **(1) File existence** — `ls book/src/L4/<candidate>.md`:
  ```
  ls: cannot access 'book/src/L4/sharding-decompose-reduce.md': No such file or directory
  ls: cannot access 'book/src/L4/subdomain-decompose-reduce.md': No such file or directory
  ls: cannot access 'book/src/L4/domain-decompose-reduce.md': No such file or directory
  ```
  No sharding/decompose chapter anywhere on disk (`ls book/src/**/sharding*.md book/src/**/*decompos*.md` → none).
- **(2) Maturity** — N/A (file does not exist; lands fresh at `rank: roadmap_goal` (rank 0)).
- **(3) OQ-ledger RESOLVED-grep** — `grep -niE "sharding-math-decomposition-abstraction-sketch.*(RESOLVED|CLOSED)" scaffolding/open-questions.md` → `no matches (slug open)`. The probe-verdict OQs are **answered=greenlit** (`sharding-math-non-destabilization-probe-vertical-arm-verdict` :1992, `...-lateral-arm-verdict` :2004 — both answered CLEAR, NOT closing the sketch).
- **(4) Structural-block check** — NOT blocked. The hard spine-non-destabilization GATE (the only structural gate on this work) CLEARED both arms at c133 (integrator-signals c133 §Unblocked: "WAVE-2 is GREENLIT for c134"). The DIRECTIVE-1 MPI boundary remains in force but does NOT block the sketch (the sketch lifts the MATH only, cites MPI as deferred mechanism). The graded-stack baseline (`rank_violations=0`) is preserved BY CONSTRUCTION (rank-0 node wired by `reference`-class edges to firm roots — a `depends-on` from a firm node to the rank-0 node would be the RED outcome, which the dispatch scope FORBIDS).

**D2 — `maintenance-floor-standing-hygiene`:** OPEN BY CONSTRUCTION (the standing every-cycle floor audit; explicitly suggested by the c133 integrator-signals §Suggested-next-dispatches; audit-class, no fixed deliverable slug). The c133 D3 pass returned clean-bill on the held baseline; the c134 pass re-runs it AND re-baselines against any WAVE-2 reference-class landing (OQ `maintenance-floor-baseline-re-baseline-on-sharding-sketch-landing`).

**STOP-PROPOSING negative-list check:** neither dispatch matches any disqualified slug (`lu_solve`, `back_solve`, `ls-update-column`, `nleps_deflated_*`). N/A.

## Codemap path verification (OUT-of-scope MPI boundary + firm-root references)

Verified via `palace-codemap` (`list_files`) + on-disk (`ls book/src/`) before citing in the dispatch scope.

**OUT-of-scope MPI boundary paths (CITE-as-deferred-mechanism only, NEVER lift — DIRECTIVE-1):**
- `palace/linalg/rap.cpp`, `palace/linalg/rap.hpp` — CONFIRMED present (`ParOperator`/RAP parallel assembly).
- `palace/utils/geodata.cpp`, `palace/utils/geodata.hpp` — CONFIRMED present (mesh-distribution).
- `palace/utils/dorfler.cpp`, `palace/utils/dorfler.hpp` — CONFIRMED present (the Dörfler cross-rank bisection D1 found NO-CLEAR; stays deferred OQ).

**Firm-root chapters the sketch will REFERENCE (verified on disk, all firm):**
- `book/src/L4/domain_energy_reduce.md` — `rank: firm`. **THE structural precedent** (a firm domain-restricted reduce verb: per-domain `map`-collect with the partition-of-unity as a config-conditional NON-law variant axis). Carries `## Record definition`, the concatenation-homomorphism fold law (`:147-152`), and the partition-coverage precondition axis (`:16`, `:172-178`).
- `book/src/L4/inner_product.md` — `firmness: firm` (split-additivity / shape-concatenation monoid-homomorphism inherited from `L3/inner_product.md:233-240`).
- `book/src/L4/linear_combination.md` — `firmness: firm` (concatenation-homomorphism, `L3/linear_combination.md:103`).
- `book/src/L4/gram_reduce.md` — `rank: firm` (per-pair map-independence / list-homomorphism, `:119,158`).
- `book/src/L2/gram.md` — firm (concatenation block law, `:154-159`).

**SUMMARY wiring model:** existing `roadmap_goal` chapters live in their natural layer Part section (e.g. `L3/lanczos_step.md`, `L3/nleps-deflated-eigensolve.md` wired at `SUMMARY.md:129-130`), NOT a separate "future" Part. The new L4 sketch wires into the **L4 Part section near `domain_energy_reduce`** (`SUMMARY.md:72`).

## Dispatches

### D1 [LEAD] (`abstractor`, WAVE-1) — `sharding-math-decomposition-abstraction-sketch`

- **agent:** `abstractor`
- **scope:** Author **ONE new `roadmap_goal` (rank-0) L4 chapter** — canonical slug **`book/src/L4/sharding-decompose-reduce.md`** (the cycle-134 author picks the final filename; this is the canonical slug all references in this plan + the priorities block use) — sketching the **sharding-as-decomposition-abstraction MATH**: a sub-domain **restrict → per-sub-domain reduce/solve → homomorphic compose** combinator that generalizes the firm `domain_energy_reduce` precedent from a domain-energy *output reduction* to a general decomposition abstraction over a sub-domain index set. The math: `subdomain-reduce = reduce ∘ restrict-to-block`, where the firm reduce primitives' **split/concatenation monoid-homomorphism over the index set** (`inner_product` split-additivity `L3/inner_product.md:233-240`; `linear_combination` concatenation-hom `L3/linear_combination.md:103`; `gram`/`gram_reduce` concatenation-block/map-independence `L2/gram.md:154-159`, `L4/gram_reduce.md:119`) IS the decomposition algebra that makes the compose lossless. **Carry the partition-of-unity as an EXPLICIT config-conditional precondition / stated hypothesis (a NON-law)**, mirroring `domain_energy_reduce`'s `partition-coverage` variant axis (`L4/domain_energy_reduce.md:16,172-178`) — the abstraction holds the precondition, it does NOT assume it. Compose the firm roots **BY NAME**. Use `domain_energy_reduce` as the structural model (frontmatter `edges:` shape, `## Record definition`, variant-axes, fold-law sections).
- **HARD TRIPWIRE (from both probe arms — load-bearing):** the chapter's frontmatter `edges:` block lists the firm roots (`domain_energy_reduce`, `inner_product`, `linear_combination`, `gram_reduce`, and `L2/gram` as appropriate) under **`reference:` ONLY — NEVER `depends-on`**. A `depends-on` edge between a firm node and this rank-0 node manufactures `rank(firm)=3 > rank(roadmap_goal)=0`, the RED rank-violation the gate forbids (the rank-linter catches it). The graded-stack baseline `rank_violations=0` MUST hold. `status: roadmap_goal` / `rank: roadmap_goal` frontmatter; claim-free placeholder discipline (intent + pulled-by provenance + declared deps + accreting working context; links resolve natively).
- **DIRECTIVE-1 boundary:** the MPI/distributed mechanics stay OUT — `palace/linalg/rap.{cpp,hpp}` (`ParOperator`/RAP), `palace/utils/geodata.{cpp,hpp}` (distribution), `palace/utils/dorfler.{cpp,hpp}` (cross-rank bisection), MPI collectives are CITED as the **deferred-future implementation mechanism**, NEVER lifted. The Dörfler cross-rank bisection is recorded as NO-CLEAR / deferred (OQ `dorfler-cross-rank-bisection-distributed-note-deferred`) — name it as out-of-scope, do not sketch its math.
- **deps:** none (WAVE-1; the probe it depended on already landed at c133).
- **rationale:** the batch-43 LEAD, WAVE-2 of the (C) GATED sharding-MATH exploration. Greenlit by the c133 combined-CLEAR gate verdict. Opens the deferred sharding-math direction as a grounded exploratory surface composing the firm spine BY NAME. Plan-tag `sharding-math-exploration`. fan-out: opens the one substantial forward direction left (the (C) door) as a non-destabilizing exploratory chapter.

### D2 (`cross-layer-cross-cutter`, WAVE-1) — `maintenance-floor-standing-hygiene`

- **agent:** `cross-layer-cross-cutter`
- **scope:** AUDIT (NO book mutation unless a one-line stale token surfaces): (i) **RE-set re-check** — RE4 consumer-gated (no GMRES-variant column c134; flag if one surfaces), RE11 §2g escalate-guard (flag any count move vs the held baseline); (ii) **kernel-API/impl integrity** — the 3 `realizes-kernel-api` edges (libceed-quadrature-kernel-impl ↔ fe-assemble-libceed-boundary-obstruction; eigsolve-impl; triangular-solve) stay `reference`-class on disk, no impl→API edge mis-typed as `depends-on`; (iii) **semantic-surface liveness** — quick `book/src/semantics/index.md` path/anchor drift scan. **AND — the c134-specific duty (OQ `maintenance-floor-baseline-re-baseline-on-sharding-sketch-landing`):** RE-BASELINE the graded-stack counts against the **WAVE-2 reference-class roadmap_goal landing** — if D1 lands a new rank-0 node wired by `reference`-class edges, the held baseline counts (`files`, `typed`/`untyped`, `reachable`, `reference_reachable`, `detritus`/`true_detritus`, `promotion_frontier`) **MOVE BY DESIGN** — that is EXPECTED, NOT a regression; the hard invariants that MUST still hold are `rank_violations=0` and `unresolved_depends_on_targets=0`. Match the new node to the RE11 cohort disposition (a deliberate reference-only-reachable node) or surface it as a new RE for the batch-43 meta. Deliverable: a clean-bill audit (with the re-baselined dispositions explicitly stated) OR a flagged-residual list for the batch-43 meta.
- **deps:** none operationally — runs in parallel with D1 (see overlap analysis). The re-baseline DISPOSITION is authored against D1's landed node, but D2 produces an audit observation (not a book mutation that conflicts with D1's chapter), and the per-report integrators serialize so D2's audit reads the post-D1 tree.
- **rationale:** the standing every-cycle MAINTENANCE FLOOR (the surrounding steady-state), carried from the batch-43 head + suggested by the c133 integrator-signals. The c134 pass specifically discharges the standing re-baseline caveat opened by c133 D3. Plan-tag `graded-stack-hygiene`. fan-out: LOW/hygiene (keeps the health signals clean; converts the c133-opened re-baseline caveat into a recorded batch-43 disposition).

## Overlap analysis

**D1 ↔ D2:** D1 AUTHORS one new L4 chapter (`book/src/L4/sharding-decompose-reduce.md`) + wires it into `SUMMARY.md` (L4 Part section) + lands its `reference`-class edges. D2 is AUDIT-class — it READS the typed dependency graph + runs the graded-stack linter to re-baseline, and produces an observation CYCLE.md (NO `## Proposed changes` to chapters, except possibly a one-line stale-token fix which would be in a DISJOINT file from D1's new chapter). They do **NOT** modify the same operator entry, do NOT rewrite the same theme body, and do NOT both write `SUMMARY.md` content (D2 does not touch SUMMARY). D2's re-baseline observation is *about* D1's landed node but is authored in D2's own report file. The only shared touch-point is the **graded-stack baseline counts** — but that is a *derived measurement* D2 reads post-apply, not a mutable index both write (no consolidated-tally collision: D2 is the SOLE owner of the baseline-count disposition this cycle; D1 does not write any count). Per the conflict-tolerance philosophy (when in doubt, PARALLEL) and the genuinely-disjoint artifact regions → **PARALLEL**.

No consolidated-tally collision (D1 lands no firm-count; the only count owner is D2, sole). No floor→adjacent-entry re-anchor coupling (no L_n floor lands under a firm L_{n+1} entry — the new node is a fresh rank-0 sketch, not a floor under an existing firm parent). No cross-report forward-reference to a not-yet-existing slug (D2 does not reference D1's new chapter by slug in a way that requires a live link in the same wave — its re-baseline reads the node by graph membership; and the canonical slug `book/src/L4/sharding-decompose-reduce.md` is stated explicitly in BOTH this overlap section AND D1's scope, so no slug-divergence risk even if a reference is added). No DUAL-REGISTRATION partition needed (single producer of book content this cycle).

## Sequencing schedule

**WAVE 1 (parallel):** D1 + D2.

One wave. ONE `integrator-finalize` at cycle end (rebuild book + commit + push + housekeeping). The per-report integrators run serially (D1 then D2, or by readiness order); D2's audit reads the post-D1 tree so its re-baseline reflects the WAVE-2 landing — but this is per-report-integrator serialization, NOT a dispatch wave boundary (the book is not rebuilt between per-report applies). Both dispatches are dispatched in parallel in Phase 2.

## RE-recheck (batch-43, c134)

- **RE4** — consumer-gated; no GMRES-variant driven-solver column dispatched c134 → does NOT fire. (D2 watches.)
- **RE11** — the deliberate reference-only-reachable cohort, §2g-accounted. The WAVE-2 sketch lands a NEW `reference`-class-reachable rank-0 node → it joins the reference-only-reachable disposition class BY DESIGN. D2 re-baselines it into the RE11 cohort accounting (or surfaces it as a fresh RE for the batch-43 meta). The §2g escalate-guard MUST NOT fire on the EXPECTED count move (the re-baseline caveat `maintenance-floor-baseline-re-baseline-on-sharding-sketch-landing` pre-authorizes the move).
- **Hard invariants that MUST hold post-c134:** `rank_violations=0` (the `reference`-class-only tripwire guarantees it) and `unresolved_depends_on_targets=0` (the sketch's `reference:` targets are all on-disk firm chapters). The OTHER baseline counts MOVE BY DESIGN (a new file + new reference edges) — expected, not a regression.
- **No new RE *fires* as a discharge** this cycle (WAVE-2 is exploratory roadmap_goal authoring, not a consumer that discharges RE1/RE5/RE7/RE9).

## Standing-gate confirmations (batch-43)

- **DIRECTIVE-1 boundary (MPI/sharding OUT of active scope) — ACTIVELY GUARDED.** The (C) gate is FIRING for the sharding-**MATH** only. D1 CITES the MPI-associated version (`linalg/rap.{cpp,hpp}` `ParOperator`/RAP, `utils/geodata.{cpp,hpp}` distribution, `utils/dorfler.{cpp,hpp}` cross-rank bisection, MPI collectives) as the **deferred-future implementation mechanism**, and lifts NONE of it. Any proposed-change that lifts an MPI mechanic is a boundary violation — catch it at critique. Codemap-confirmed the boundary paths exist (so they are cited correctly, not invented).
- **The HARD spine-non-destabilization gate** cleared at c133 (both arms CLEAR) → WAVE-2 is licensed. The gate's residual constraint LIVES ON as the D1 tripwire: `reference`-class edges ONLY, no firm-node re-root/downgrade, `rank_violations=0` held.
- **Kernel-API/impl integrity (DIRECTIVE 3).** The 3 `realizes-kernel-api` edges stay `reference`-class on disk — D2 confirms (carried from c133 D3's INTACT finding).
- **No forced rectangular pull-up of the spine** (the redirect's no-forced-rectangular-pull-up still governs) — the sketch GENERALIZES existing firm vocabulary by reference; it does not mirror an existing entry at a new layer or force a thin connecting theme.

## Open questions / caveats

- **ONE roadmap_goal chapter, NOT a cohort — rationale.** Both probe arms converged on a SINGLE coherent abstraction: `subdomain-reduce = reduce ∘ restrict-to-block` over a sub-domain index set, with partition-of-unity as the precondition. The structural precedent (`domain_energy_reduce`) is a single chapter. A rank-0 speculative sketch should be conservative — authoring a cohort would over-commit speculative surface that the math does not (yet) distinguish. If the abstraction naturally bifurcates during authoring (e.g. a *restriction* combinator distinct from a *compose* combinator that each merit their own home), the abstractor MAY split into a small cohort — but the default and recommendation is **one chapter**. I flag this as the author's-judgment call within D1's scope; if it splits, the SUMMARY wiring + D2's re-baseline simply account for ≥1 new node (same disposition class).
- **The re-baseline is EXPECTED to move counts — this is the one cycle this batch where the held baseline legitimately changes.** I have pre-authorized this in D2's scope + the RE-recheck section so the next maintenance pass / batch-43 meta does NOT mis-read the move as a regression. The hard invariants (`rank_violations=0`, `unresolved=0`) are the real guard.
- **No methodology-adjustment pattern surfaced** that the friction-ledger lacks. The batch-43 process (gated-first probe → greenlit sketch) is running as designed.
- The IEEE-754 reduction-tree-pinning (the one genuine load-bearing numerical concern D2's c133 sibling flagged) stays DEFERRED — it is a numerical-trick surface, not a decomposition-abstraction concern; D1 should NOT attempt to pin it (note it as deferred if the math touches reduction ordering).
