---
agent: cycle-planner
invoked_at: 2026-06-03T154956Z
scope: cycle-078 dispatch plan
status: pending
---

# Cycle 078 dispatch plan

## Goals selected this cycle

Cycle-078 is the THIRD/FINAL primary cycle of meta-batch-24 (cycles 076/077/078; the batch-24 meta-phase fires AFTER this cycle's finalize as a separate dispatch). It honors the cycle-077 finalize signal: land the two deferred FEATURE-SURFACE SPINE columns directly into their by-kind groupings (the c076 reorg makes this clean) — the **energy-fields output-product column** (the cohort's 5th-of-5; gate VERIFIED OPEN below) and the **boundary-mode driver-leaf column** (the 6th driver-leaf column; gate CLOSED-RATIFIED c072) — plus the LOW **output-product↔driver cross-link wiring** pass over the 4 existing pairs (which has real work: the driven↔sparameters reciprocal up-link is missing, verified below). The reduce-verb coupled-column promotions stay DOUBLE-GATED (second gate = a dedicated reduction test) and are NOT dispatched. THREE dispatches, one parallel wave (the shared `feature/index.md` matrix + `SUMMARY.md` feature block is sole-owned by one cohort owner per the parallel-blind-shared-index guard; the two columns' group-intro pages + 3 chapter files each are disjoint).

## Deliverable-presence verification (paste-inline-evidence per pick)

**D1 — `energy-fields` output-product column (5-of-5).** Open by partial construction (new feature column, no prior-cycle chapter history); ABSENT-verified + gate-verified:
- File existence (all 3 ABSENT): `ls book/src/feature/energy-fields.{L4,L1,L0}.md` →
  `ls: cannot access 'book/src/feature/energy-fields.L4.md': No such file or directory` (and `.L1.md`, `.L0.md` likewise). All three ABSENT — open.
- Group-intro target EXISTS (D1 adds its bullet + alpha-row): `ls book/src/feature/output-product.md` → exists; `grep -c "energy-fields" book/src/feature/output-product.md` → `0` (no bullet yet — D1 adds the alpha-position bullet between `eigenfrequency-qfactor` and `inductance`).
- **GATE VERIFICATION (the cycle-077 signal asked to verify the constituent vocabulary is firm enough).** The energy-fields output product is `MeasureDomainFieldEnergy` (`postoperator.cpp:1021`, codemap-confirmed). Its constituents reduce a driver's field solution to a per-domain energy table:
  - per-domain energy form `GetDomainElectricFieldEnergy` (`domainpostoperator.cpp:255-275`, codemap-read): body is `it->second.first->Mult(E,D); dot = LocalDot(E,D); return 0.5*dot` — i.e. `½⟨E, M_i E⟩`, an SPD-weighted energy-form = `matrix-weighted-norm`-SQUARED. `L1/matrix-weighted-norm.md` status read from disk: `rough-in (test-coverage-bounded)` — well-anchored, rough-in.
  - per-domain participation `participation_ratio = energy_i / energy` (`postoperator.cpp:1038`, `:1066`). `L1/participation_ratio.md` status read from disk: `firm` (firm-on-positive-structure, landed c077 D4). The `participation_ratio.md` cohort note (lines 19-22) already names the eigenmode-participation siblings; the domain-energy ratio is a clean further witness of the same `energy/E_total` shape.
  - **Verdict: gate OPEN.** The constituent reduction is cleanly describable in firm (`participation_ratio`) + rough-in (`matrix-weighted-norm` energy-form) vocabulary — the same firm+rough-in mix every existing output-product column composes. The column lands at `seed` (consistent with all 4 existing output-product columns staying `seed` because a composed constituent is rough-in).
- Structural-block check: none. The c074/c075/c077 output-product columns are the working precedent; the by-kind grouping (c076) is settled; this lands directly in the output-product grouping (alpha-within-kind).

**D2 — `boundary-mode` driver-leaf column (6th driver).** Open by partial construction (new feature column, no prior-cycle chapter history); ABSENT-verified + gate CLOSED-RATIFIED:
- File existence (all 3 ABSENT): `ls book/src/feature/boundary-mode.{L4,L1,L0}.md` → `No such file or directory` for all three. Also `ls book/src/feature/*boundary*` / `*wave-port*` → none. ABSENT — open.
- Group-intro target EXISTS (D2 converts its planned-prose to a live bullet + adds alpha-row): `ls book/src/feature/driver-leaf.md` → exists; `grep -c "boundary-mode" book/src/feature/driver-leaf.md` → `1` (the line-13 "planned… lands here when its constituent vocabulary composes cleanly" prose — D2 converts to the live alpha-FIRST bullet `boundary-mode` and re-words the planned-prose line).
- GATE: CLOSED-RATIFIED c072 (per the active-head #6 + the cycle-077 signal). Source codemap-confirmed: `ProblemType::BOUNDARYMODE` at `main.cpp:276`, `return std::make_unique<BoundaryModeSolver>(...)` at `:277` (the 6th `ProblemType` branch, sibling of DRIVEN/EIGENMODE/ELECTROSTATIC/MAGNETOSTATIC/TRANSIENT). `BoundaryModeSolver` class `boundarymodesolver.hpp:15-28`; ctor `boundarymodesolver.cpp:78-82`; `BoundaryModeSolver::Solve` `boundarymodesolver.cpp:200-341` (codemap-confirmed). The `Solve` body composes: 2D-submesh boundary extraction → `BoundaryModeOperator` assemble (`:215-216`) → `ModeEigenSolver` GEP eigen-iteration (`:235`, `:267` `eig.Solve(omega, sigma)`) → postprocess — i.e. the same **opaque-library black-box** corner as the `eigenmode` driver column (the eigen-iteration is opaque-library-owned), composed over a boundary-extracted 2D submesh. Cleanly describable as a `seed` driver-leaf column.
- Structural-block check: none. The 5 driver-leaf columns (c072/c073) are the working precedent; lands directly in the driver-leaf grouping (alpha-FIRST: boundary-mode sorts before driven).

**D3 — `output-product-driver-cross-link-wiring` (LOW; #7).** EXISTS-and-has-real-work verified (this is a wiring pass over existing files, not a new-artifact pick — verify the drift it repairs is real):
- The 4 output-product↔driver pairs, driver-side up-link check (`grep -c` on the driver L4 chapter):
  - electrostatic.L4 → `capacitance`: `8` (up-linked OK)
  - magnetostatic.L4 → `inductance`: `8` (up-linked OK)
  - eigenmode.L4 → `eigenfrequency-qfactor`: `5` (up-linked OK)
  - **driven.L4 → `sparameters`: `0`** (MISSING — the driven driver does NOT cross-link UP to its sparameters output-product column; this is the c075-era drift the ratified convention's drift-guard exists to catch). Real work confirmed.
- Structural-block check: none. The cross-link convention was RATIFIED batch-23 (active-head decision #3). The pass is a thin reciprocal-link wiring + drift-guard sweep; primary work item is the missing driven→sparameters up-link.

(Skip-justification for the deeper four-step on D1/D2: open by construction — fresh feature columns with no prior-cycle chapter history; the maturity/OQ-grep/structural-block steps are recorded above where applicable. D3 is a wiring pass; the EXISTS-and-stale evidence is pasted above.)

## Dispatches

**D1 — `layer-intro-author` — energy-fields output-product feature column (5-of-5); COHORT OWNER of the shared `feature/index.md` matrix + `SUMMARY.md` `# Feature surfaces` block for BOTH new columns this cycle.**
- scope: author the NEW `book/src/feature/energy-fields.{L4,L1,L0}.md` output-product feature column (status `seed`), landing in the **output-product** by-kind grouping at the alpha-within-kind position (between `eigenfrequency-qfactor` and `inductance`). The physical product = the domain/field energy table (per-domain electric + magnetic field energies + per-domain participation ratios). L0 ground-truth: `MeasureDomainFieldEnergy` `palace/models/postoperator.cpp:1021` (per-domain loop `:1029-1075`); per-domain energy form `DomainPostOperator::GetDomainElectricFieldEnergy` `palace/models/domainpostoperator.cpp:255-275` (the `½⟨E,M_i E⟩` SPD energy-form); the participation `energy_i / energy` `postoperator.cpp:1038`. Composes: the **firm** [`participation_ratio`](../L1/participation_ratio.md) (the `energy_i/E_total` ratio — landed c077 D4) + the per-domain energy-form (rough-in [`matrix-weighted-norm`](../L1/matrix-weighted-norm.md)-squared). **AUTHOR JUDGMENT (do NOT force a rectangular verb):** the column MAY either (a) mint a thin per-column L4 reduction verb (e.g. `domain_energy_reduce`, rough-in — for symmetry with the cohort's verb-per-column shape), OR (b) compose `participation_ratio` + the energy-form directly without a fresh dedicated verb if that is the cleaner description. Choose by which reads cleaner; the redirect forbids minting a verb that adds vocabulary without simplifying. Stays `seed` (a composed constituent — the energy-form — is rough-in). **OWNERSHIP (parallel-blind-shared-index guard, c074/c075 precedent):** D1 SOLE-owns the `feature/index.md` matrix (adds BOTH new rows: its own energy-fields row under `*Output-product columns*`, AND the boundary-mode row under `*Driver-leaf columns*`) + the `SUMMARY.md` `# Feature surfaces` block (adds BOTH new columns' 3-level nested entries, each in its kind grouping at alpha position). D1 also updates the `feature/index.md` line-54 "Still planned" prose (both energy-fields + boundary-mode now authored) and adds its OWN bullet to the `feature/output-product.md` group-intro (alpha position; the "A further output product — energy/field measurements — is planned" line-12 prose → now-authored).
- deps: none (D2 defers the two shared-surface rows to D1; see overlap analysis).
- rationale: active-head #5 (MEDIUM); cycle-077 finalize signal (land the deferred energy-fields column directly in its by-kind grouping); completes the output-product cohort 4→5. fan-out: MEDIUM (completes the output-product half of the feature spine; the firm `participation_ratio` is the key already-firm constituent the gate-verification confirmed).

**D2 — `layer-intro-author` — boundary-mode driver-leaf feature column (6th driver); DEFERS the two shared-surface rows (`feature/index.md` matrix + `SUMMARY.md` block) to D1.**
- scope: author the NEW `book/src/feature/boundary-mode.{L4,L1,L0}.md` driver-leaf feature column (status `seed`), landing in the **driver-leaf** by-kind grouping at the alpha-within-kind position (alpha-FIRST: `boundary-mode` sorts before `driven`). The composition root for `BoundaryModeSolver` (`main.cpp:276` `ProblemType::BOUNDARYMODE` → `:277`). L0 ground-truth: `BoundaryModeSolver::Solve` `palace/drivers/boundarymodesolver.cpp:200-341` (the 2D-submesh boundary extraction `:42-55` helpers + `:215` `BoundaryModeOperator` assemble + `:235`/`:267` `ModeEigenSolver` GEP eigen-iteration `eig.Solve(omega, sigma)` + postprocess); class `boundarymodesolver.hpp:15-28`; ctor `boundarymodesolver.cpp:78-82`. Inputs = config (`iodata.solver.boundary_mode`, the 2D waveguide-mode analysis params `:203`); output = the driver's solution family (the converged propagation modes). Body = the composition of the assemble + the **opaque-library black-box** eigen-iteration corner (the SAME corner as the [`eigenmode`](./eigenmode.L4.md) driver — cross-reference it; the `ModeEigenSolver` GEP is opaque-library-owned), over a boundary-extracted 2D submesh (the distinguishing shape vs the 3D-domain eigenmode driver). **OWNERSHIP:** D2 authors ONLY its 3 chapter files + its OWN bullet in the `feature/driver-leaf.md` group-intro (convert the line-13 "A 6th co-equal leaf driver column — wave-port / boundary-mode … is planned" prose into the live alpha-FIRST bullet `boundary-mode`). D2 **DEFERS** its `feature/index.md` matrix row + its `SUMMARY.md` feature-block 3-level entry to D1 (the cohort owner this cycle) — do NOT touch `feature/index.md` matrix or the `SUMMARY.md` `# Feature surfaces` block.
- deps: none operationally (its files are disjoint from D1's chapter files + group-intro page); the shared-surface rows are deferred to D1 (which is the same-wave owner — the per-report integrator applies D1 and D2 serially in either order; D1 owns the shared surface).
- rationale: active-head #6 (MEDIUM); cycle-077 finalize signal (boundary-mode gate CLOSED-RATIFIED c072; land directly in the driver-leaf grouping); completes the driver-leaf cohort 5→6; named FEATURE-SURFACE SPINE scope (wave-port/boundary-mode). fan-out: MEDIUM.

**D3 — `lifter` — output-product↔driver cross-link wiring pass (#7, LOW).**
- scope: a standing-state reciprocal cross-link wiring pass over the 4 existing output-product↔driver pairs, enforcing the batch-23-ratified convention (each output-product column cross-links its producing driver AND each driver stage-3 cross-links UP to its output-product column). The verified drift to repair: **`driven.L4.md` does NOT up-link to `sparameters`** (`grep -c sparameters driven.L4.md` → `0`) — add the reciprocal up-link from the driven driver's stage-3 postprocess to [`sparameters`](./sparameters.L4.md) (mirroring the electrostatic→capacitance / magnetostatic→inductance / eigenmode→eigenfrequency-qfactor up-links that already exist). Audit the other 3 pairs for reciprocal completeness while there; do NOT manufacture links the convention does not call for. skill: `upgrade-plain-text-ref-to-live-link-when-target-on-disk` is NOT this (the targets are on disk; this is a missing-reciprocal-link add, not a plain-text upgrade) — this is a thin convention-drift-guard wiring edit.
- deps: none. DISJOINT from D1/D2 (D3 edits the EXISTING driver L4 chapters' stage-3 sections + possibly the existing output-product chapters' down-link sections; D1/D2 author NEW chapter files + the index/SUMMARY/group-intro surfaces. D3 does NOT touch the new energy-fields/boundary-mode files, the `feature/index.md` matrix, or `SUMMARY.md`).
- rationale: active-head #7 (LOW); convention ratified batch-23 decision #3; the driven↔sparameters reciprocal-link gap is real verified drift. fan-out: LOW (keeps the two-entry-point surfaces from drifting; closes the c075-era driven→sparameters omission).

## Overlap analysis

- **D1 ↔ D2:** D1 and D2 each author DISJOINT new chapter-file sets (`energy-fields.{L4,L1,L0}.md` vs `boundary-mode.{L4,L1,L0}.md`) and DISJOINT group-intro pages (`feature/output-product.md` vs `feature/driver-leaf.md`). The ONLY genuinely-shared surfaces are (a) `feature/index.md` matrix and (b) `SUMMARY.md` `# Feature surfaces` block — both are consolidated multi-column surfaces (the parallel-blind-shared-index guard applies). **Resolved per the c074/c075 precedent:** D1 SOLE-owns BOTH shared surfaces (writes both new rows / both new SUMMARY entries, each in its correct kind grouping at alpha position); D2 DEFERS its index row + SUMMARY entry to D1 and touches neither. With that partition the two are NON-overlapping → PARALLEL. (Each authors its own group-intro bullet + own chapter files — anchor-distinct, parallel-safe.)
- **D1 ↔ D3:** No overlap. D1 authors NEW output-product chapter files + the index/SUMMARY/output-product-group-intro surfaces; D3 edits EXISTING driver L4 chapters' stage-3 reciprocal links (+ possibly existing output-product chapters' down-link prose). The energy-fields column is NEW (no driver pairs it yet — energy-fields is a driver-agnostic field measurement, not one of the 4 pairs D3 wires), so D3 does not touch energy-fields. PARALLEL.
- **D2 ↔ D3:** No overlap. D2 authors NEW boundary-mode chapter files + the driver-leaf group-intro bullet; D3 edits the 4 EXISTING output-product↔driver pairs (driven/electrostatic/magnetostatic/eigenmode). boundary-mode is a NEW driver with no output-product pair yet, so D3 does not touch it. The driver-leaf group-intro page: D2 adds the boundary-mode bullet; D3 does not edit group-intro pages. PARALLEL.

No two dispatches modify the same operator entry, rewrite the same theme/chapter body, or co-author the same consolidated index/SUMMARY surface (the one shared surface is sole-owned by D1). All three are PARALLEL.

## Sequencing schedule

**Single wave (all parallel):** D1, D2, D3 dispatch together. No forward-reference ordering is required: D2's chapter cross-references to existing chapters (`eigenmode.L4.md`, the L4 calculus) resolve to on-disk files; D1's cross-references to `participation_ratio.md` / `matrix-weighted-norm.md` resolve on-disk; the only forward-reference between wave-mates is D2's deferred index/SUMMARY rows to D1, which is an OWNERSHIP partition (D1 writes them), not a wire-a-live-link-to-a-not-yet-landed-slug dependency — the per-report integrator applies the two reports serially (artifact writes serialize) and D1 carries both rows regardless of apply order. (There is exactly ONE `integrator-finalize` at cycle end; this single wave feeds the N per-report integrators serially, then finalize once.)

## Open questions / caveats

- **For the batch-24 meta-phase (fires AFTER this cycle's finalize as a separate dispatch — NOT cycle-078 dispatch work):**
  1. **Ratify the new `record` Kind value** in the `concepts/index.md` Kind-legend (introduced c077 D1; OQ `concepts-record-kind-needs-meta-ratification`) — in-use-now-pending-ratification across 7 record-definition concept pages.
  2. **The c077 D4 dispatch-phase write-partition leak** (the `combinator-miner` authored `book/src/L1/participation_ratio.md` directly to `book/` during the dispatch phase; the repairer recovered it clean via `revert-dispatch-phase-book-mutation` + `new:`-block repackage). This is the data-point the recurrence-4-re-weighed `dispatch-phase-book-mutation` friction watch named (re-escalate to the clean-tree gate only on TWO leaks in one batch OR a leak escaping to a commit). Neither trigger is met (one leak, caught + repaired clean), so the held NO-GO on the structural gate stands — surface as a recurrence data-point only.
- **Coupled-column re-check (NOT triggered c078):** the `eigenfreq_qfactor_reduce` / `sparameter_reduce` rough-in→firm promotions + the coupled eigenfrequency-qfactor / sparameters seed→promotions stay DOUBLE-GATED — c077 closed ONE gate each (the firm L1 home); the SECOND gate (a dedicated reduction test) is still open (OQs `…-status-promotion-double-gated`). Do NOT promote any reduce verb or seed column this cycle. Energy-fields lands `seed` for the same reason (its energy-form constituent is rough-in).
- **Energy-fields L4-verb judgment is left to the author** (D1): mint a thin `domain_energy_reduce` verb for cohort symmetry vs compose `participation_ratio` + the energy-form directly — chosen by which reads cleaner, NOT forced (the redirect forbids minting vocabulary that does not simplify). If the author finds energy-fields does NOT compose cleanly in existing vocabulary, that is a spine finding (surface as an OQ), not a forced land — but the gate-verification above indicates it does compose cleanly (firm `participation_ratio` + rough-in energy-form).
- **Record-definition cohort gaps NOT picked this cycle** (per the cycle-077 signal: lower fan-out than the two feature columns): `assembled-fe-covector-record-definition-home` (`Covector[N]`, c077 D5 — watch the ≥2-consumer promote-to-concepts bar), `step-outputs-breakdown-tag-enum-open` (c077 D2), the `DomainData`/`BoundaryData` unpinned structs (note: the energy-fields D1 chapter will reference `Measurement::DomainData{idx, energy_i, participation_ratio}` from `postoperator.cpp:1040` — if D1's signature names `DomainData`, D1 either defines it in-chapter (single-consumer `## Record definition`) or flags `record-DomainData-needs-definition-home` per the record-definition obligation). These are candidates for the batch-24 meta-phase intake→plan migration or a later cycle.
