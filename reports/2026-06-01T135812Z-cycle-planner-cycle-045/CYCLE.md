---
agent: cycle-planner
invoked_at: 2026-06-01T135812Z
scope: cycle-045 dispatch plan
status: pending
---

# Cycle 045 dispatch plan

THIRD/FINAL primary cycle of meta-batch-13 (cycles 043/044/045). The batch-13 meta-phase fires AFTER this cycle's `integrator-finalize`, as a separate dispatch (NOT run here).

## Goals selected this cycle

Complete the substantive `l3-l2-rotation-theme-coverage-gap` toward 18-of-18 by authoring the **two remaining substantive L3>L2 iteration-rotation themes** — `eigsolve` (opaque-library erasure root) and `chebyshev` (audit-first: dedicated substantive theme vs. in-line-sufficient) — mirroring the c044 D3 `orthogonalize-variant-split` substantive-theme precedent. Materialize the **erasure-scope/root taxonomy** (unconditional-Palace-authored / variant-conditional-Palace-authored / opaque-library-owned / in-house-numerical-stability) now that the substantive cohort reaches its full root-set, as a `L3-L2/index.md` taxonomy note + consolidated count refresh. This is the lead-frontier work the c044 carry-forward identified; it keeps the stack moving rectangular (foundation-first, `foundation_solidity` weight) by completing the L3→L2 connective tissue rather than growing L3 width.

## State verified (post-cycle-044)

L2 firm 21, L2>L1 firm 19, L3>L2 firm 15 (15 theme files on disk excl. index; `l3-l2-rotation-theme-coverage-gap` 15-of-18), L3 firm 15 + 3 partial-obstruction, L4 firm 4 (from `cycle-record.jsonl` `counts_after` c044). The two target slugs `chebyshev`/`eigsolve` L3>L2 are confirmed ABSENT (see verification below). `apply_nonlinear_pencil` is the only remaining (B)-candidate (HELD, folds into a future eigsolve-variant pass — NOT dispatched).

## Dispatches

### D1 — (`abstractor`) `eigsolve` substantive L3>L2 theme — SOLE row-author for its theme; sole count-mover for its own row
- **scope**: Author the NEW firm SUBSTANTIVE L3>L2 rotation theme `book/src/L3-L2/eigsolve-opaque-library-erasure.md` (slug under abstractor judgment; suggested `eigsolve-opaque-library-erasure` to name the erasure ROOT, in the substantive-theme naming family of `orthogonalize-variant-split` / `ksp-solve-outer-driver` — NOT a `-body-identity` slug). Narrate FORWARD: L3 `eigsolve` (firm `partial-obstruction`, c024) → L2 `eigsolve` (firm `apply_shift_invert = apply_linop ▷ ksp_solve ▷ scale_untransform [▷ project]`, c023). The **substantive content is the loop-erasure**: the L3 eigen-iteration loop is a witnessed `sequential-obstruction` rooted in **opaque-library-ownership** (SLEPc folds inside `EPSSolve(eps)` — codemap-confirmed `palace/linalg/slepc.cpp:694`; ARPACK folds inside the `naupd` RCI driver — codemap-confirmed `palace/linalg/arpack.cpp:318`, "Begin RCI loop" `:313`, callback-dispatched `:315-339`), with NO Palace-authored eigen-step kernel / eigen-iteration driver pair analogous to `(krylov-step, ksp_solve)`. The L3 obstruction-marker (`eigen_iterate` named-by-role + cited opaque-library `sequential-obstruction`) collapses to the L2 fold-by-role reference; the per-step body `apply_shift_invert` is identity-in-form across the edge (that body identity is the in-line non-adjacent annotation — DO NOT duplicate it; the theme covers ONLY the substantive loop-erasure half, exactly as `ksp-solve-outer-driver` covers the driver while `krylov-step-body-identity` covers the body). **Re-anchor the parent** `book/src/L3/eigsolve.md` stale "No `book/src/L3-L2/` theme file is created" assertions (frontmatter `lowers_to:` :7-8; §Downward :35; §"L3 vs L2 distinction" :205-210; and the body-prose :199) to the DUAL framing — exactly the c044 D4 re-anchor of `L3/orthogonalize.md:8` ("per-step body identity-in-form annotated in-line per cycle-012; the SUBSTANTIVE loop-structure ... is the dedicated `…` L3>L2 theme"). **DUAL-REGISTRATION:** D1 adds BOTH its own index-table ROW in `book/src/L3-L2/index.md` AND its own §Vocabulary-cohort BULLET (under the "Substantive / non-identity iteration-rotation themes" sub-grouping, lines ~54-57) AND its SUMMARY.md chapter line; D1 DEFERS the consolidated firm-count tally + the coverage-gap denominator to D3 (the count-owner). State in scope: "D1 adds its eigsolve row+bullet+SUMMARY line; does NOT touch the §Working-Notes consolidated firm-count (15→N) or the coverage-gap line — D3 owns those."
- **deps**: none (wave 1)
- **rationale**: The lead substantive frontier (c044 carry-forward §Unblocked + §Suggested-next-dispatches; OQ `remaining-substantive-l3-l2-rotations-chebyshev-eigsolve` part (a) — explicitly "the natural next substantive L3>L2 theme to author (a dedicated theme file ... NOT an in-line annotation, since the loop-erasure is substantive)"). Completes `l3-l2-rotation-theme-coverage-gap` by one and introduces the THIRD erasure ROOT (opaque-library) that the taxonomy (D3) names. HIGH fan-out under `foundation_solidity`.

### D2 — (`cross-layer-cross-cutter`, AUDIT-FIRST) `chebyshev` L3>L2 — dedicated substantive theme vs. in-line-sufficient; lands the theme IF warranted
- **scope**: AUDIT-FIRST dispatch (NOT a reflexive abstractor author) per the OQ trigger and the c036 D2 audit-first precedent. Question: does `chebyshev` warrant a DEDICATED substantive `book/src/L3-L2/<slug>.md` theme file, or is the existing in-line non-adjacent-identity annotation sufficient? **The tension is real and load-bearing:** `L3/chebyshev.md` (firm `partial-obstruction`, c013) currently asserts — frontmatter `lowers_to:` :5-6, §Downward :69-82, "Non-adjacent identity" :84-101, §"L3 vs L2 distinction" :459-474 — that NO `L3-L2/` theme file is created, on the cycle-012 `l3-l1-inline-identity-rotation-convention`. That convention covers the **body** rotation, which IS identity-in-form (the L3 tensor-field updates map line-for-line to the L2 `chebyshev-iteration` `sweep` body). BUT the **loop-structure obstruction** — the two nested `iterate_while_pure` folds (the `pc_it` outer Richardson sweep + the `k` inner recurrence, both witnessed `sequential-obstruction` per Phillips & Fischer 2022 §2, cited in the entry) — is NOT erased by the in-line body-identity annotation and is substantive iteration-rotation content the in-line convention does NOT cover. Per the `ksp-solve-outer-driver` precedent (body-identity in-line + substantive driver-theme separate = "the full per-solver L3>L2 story") and OQ `remaining-substantive-l3-l2-rotations-chebyshev-eigsolve` part (b) ("the rotation is substantive, which the in-line convention does NOT cover, so a dedicated file MAY be warranted as for `orthogonalize`"), the LIKELY verdict is WARRANTED → land a substantive theme `book/src/L3-L2/chebyshev-recurrence-erasure.md` (slug under producer judgment; substantive-theme family, NOT `-body-identity`) naming the FOURTH erasure root: **unconditional, in-house-numerical-stability-rooted** (distinct from ksp-solve's unconditional-Palace-authored-loop, orthogonalize's variant-conditional, eigsolve's opaque-library). Narrate the two-nested-fold erasure → L2 `chebyshev-iteration` fold-by-role; shadow the obstruction to its L2 non-law(s). **If WARRANTED + lands:** re-anchor the `L3/chebyshev.md` stale "no L3-L2 theme file" assertions to the dual framing (body-identity in-line + substantive loop-erasure theme), mirroring c044 D4 on orthogonalize; add its own index-table ROW + §Vocabulary-cohort BULLET (substantive sub-grouping) + SUMMARY.md line (DUAL-REGISTRATION); DEFER the consolidated tally to D3. **If NOT-warranted (in-line-sufficient):** observation-only, no `book/` mutation beyond confirming/tightening the in-line annotation, no new index row — record the verdict for the OQ closure; D3's tally then moves only by D1's +1.
- **deps**: none (wave 1) — distinct theme file + distinct parent L3 entry from D1; no shared region. (Both touch the L3-L2/index table + SUMMARY.md by APPEND of distinct rows/lines = non-overlapping at the operational level per the §Discipline distinct-row rule; the consolidated tally is D3's.)
- **rationale**: OQ `remaining-substantive-l3-l2-rotations-chebyshev-eigsolve` part (b) explicitly routes this as an audit ("audit whether `chebyshev` needs a dedicated theme file or stays in-line (same-layer-cross-cutter / cross-layer-cross-cutter)"). Audit-first framing is mandated by the role-spec audit-first-vs-reflexive-harvest bullet for a candidate sitting at a convention boundary (in-line non-adjacent-identity vs. substantive-theme). Completes the substantive cohort's root-set toward 18-of-18 (or confirms chebyshev is genuinely in-line-discharged, which itself closes the gap-denominator question). MEDIUM-HIGH fan-out under `foundation_solidity`.

### D3 — (`layer-intro-author`) erasure-scope/root taxonomy note + SOLE consolidated-count owner
- **scope**: Materialize the **substantive-erasure-scope/root taxonomy** D3-c044 surfaced and flagged for the meta-phase, now that the substantive cohort reaches its full root-set this cycle. Author a taxonomy note in `book/src/L3-L2/index.md` §Working-Notes (and/or a short §Vocabulary-cohort sub-grouping refinement) naming the erasure scopes/roots across the substantive L3>L2 cohort: **unconditional-Palace-authored-loop** (`ksp-solve-outer-driver` — the whole operator IS the loop) / **variant-conditional-Palace-authored** (`orthogonalize-variant-split` — substantive erasure confined to the MGS branch) / **opaque-library-owned** (`eigsolve`, D1 — the loop is library-owned, not Palace-authored) / **unconditional-in-house-numerical-stability** (`chebyshev`, D2 — IF D2 lands a theme; otherwise note chebyshev's obstruction is in-line-discharged and record the root for completeness). Decide concept-page-vs-index-note: prefer the `L3-L2/index.md` §Working-Notes taxonomy note this cycle (low cost, organizes the cohort in place); a dedicated `concepts/` page is optional/deferrable if the index note suffices (state the decision). **SOLE consolidated-count owner this cycle (count-ownership convention):** D3 writes the `L3-L2/index.md` §Working-Notes consolidated firm-count tally (15 → 16 if only D1 lands, → 17 if D1+D2 both land themes) AND the `l3-l2-rotation-theme-coverage-gap` denominator line (15-of-18 → 16-of-18 or 17-of-18), accounting for what D1/D2 actually landed. D1 and D2 add ONLY their own rows/bullets/SUMMARY lines and DEFER both the consolidated tally and the coverage-gap line to D3.
- **deps**: D1, D2 (wave 2) — D3's taxonomy must name what D1/D2 actually landed (the eigsolve root always; the chebyshev root conditionally on D2's verdict), and D3's consolidated count must reflect the actual landings (parallel-blind-shared-index-count-divergence avoidance).
- **rationale**: OQ `substantive-l3-l2-erasure-scope-taxonomy` (opened c044 D3; the abstractor explicitly did NOT name it — "concept-page / index-taxonomy authoring is layer-intro-author / cross-cutter territory") + the `L3-L2/index.md` §Working-Notes "First substantive theme for a partial-obstruction operator (taxonomy note)" bullet flagging the question. With eigsolve (opaque-library root) landing this cycle the cohort reaches its full root-set, so the taxonomy is now well-founded rather than premature. LOW cost, organizes the substantive cohort. Count-ownership per cycle-039 meta-phase convention (D3 = the layer-intro-author dispatch in the wave → preferred tally owner).

## Deliverable-presence verification (paste-inline-evidence, MANDATORY)

### D1 — `eigsolve` L3>L2 substantive theme
1. **File existence (target ABSENT):** `ls book/src/L3-L2/eigsolve*` →
   ```
   ls: cannot access 'book/src/L3-L2/eigsolve*': No such file or directory
   ```
   No `eigsolve` L3>L2 theme on disk. OPEN.
2. **Parent maturity (must be firm/partial-obstruction):**
   - `L3/eigsolve.md` §Status → ``` `partial-obstruction` — the per-step body … lifts cleanly … the eigen-iteration loop is a witnessed sequential-obstruction rooted in opaque-library-ownership — SLEPc folds inside EPSSolve(eps) (palace/linalg/slepc.cpp:694) … ``` (firm partial-obstruction; the substantive loop-erasure is real). 
   - `L2/eigsolve.md` §Status → ``` `firm` (cycle-023 wave-1 … the named shift-invert spectral-transform composition apply_linop ▷ ksp_solve …) ``` (firm RHS floor present).
   Both parents present + mature → the substantive theme has live LHS+RHS anchors.
3. **OQ-ledger RESOLVED/CLOSED grep:** `grep 'remaining-substantive-l3-l2-rotations-chebyshev-eigsolve' scaffolding/open-questions.md` → the OQ is OPEN (needs-more, opened c044 D3) and part (a) routes "author the eigsolve substantive L3>L2 theme (abstractor) as the next substantive L3>L2 frontier item." NOT closed; the dispatch IS the resolution.
4. **Structural-block check:** No methodology gate blocks it. The c012 non-adjacent-identity convention applies ONLY to the body (identity-in-form, in-line); the substantive loop-erasure is NOT covered by that convention and warrants a dedicated theme per the `ksp-solve-outer-driver` substantive-theme precedent (CLAUDE.md "Identity-lowerings still require both L levels" + the substantive-theme cohort). Codemap-confirmed anchors: `slepc.cpp:694` `EPSSolve(eps)`, `arpack.cpp:313` "Begin RCI loop" / `:318` `naupd`. **ALL CHECKS PASS — recruit.**

### D2 — `chebyshev` L3>L2 (audit-first)
1. **File existence (target ABSENT):** `ls book/src/L3-L2/chebyshev*` →
   ```
   ls: cannot access 'book/src/L3-L2/chebyshev*': No such file or directory
   ```
   No `chebyshev` L3>L2 theme on disk. OPEN (audit-first; lands a theme IFF warranted).
2. **Parent maturity:**
   - `L3/chebyshev.md` §Status → ``` `partial-obstruction` — the per-inner-step body lifts cleanly to a global … ``` (firm partial-obstruction; the two nested iterate_while_pure folds are the substantive obstruction).
   - `L2/chebyshev-iteration.md` §Status → ``` `firm` — the primitive composition is a direct transcription of both Mult2 … ``` (firm RHS floor present — the chebyshev L2 floor under the non-same-named `chebyshev-iteration` slug, c012).
   Both parents present + mature.
3. **OQ-ledger grep:** `grep 'remaining-substantive-l3-l2-rotations-chebyshev-eigsolve' scaffolding/open-questions.md` → OPEN; part (b) explicitly routes the chebyshev case as an AUDIT ("verify whether the in-line annotation is sufficient or a dedicated theme is warranted … audit whether chebyshev needs a dedicated theme file or stays in-line (same-layer-cross-cutter / cross-layer-cross-cutter)"). NOT closed; the audit IS the resolution.
4. **Structural-block check:** The current `L3/chebyshev.md` "no L3-L2 theme file" assertions (frontmatter :5-6, §Downward :69-82, :84-101, :459-474) are the in-line non-adjacent-identity convention applied to the BODY rotation (correct for the body). They do NOT block a dedicated theme for the SUBSTANTIVE loop-erasure (which the in-line convention does not cover). The audit is exactly the convention-boundary question the role-spec audit-first bullet mandates for. **ALL CHECKS PASS — recruit as AUDIT-FIRST.**

### D3 — `L3-L2/index.md` taxonomy note + count
- **Open by construction (no prior-cycle history):** the erasure-scope taxonomy note does not yet exist (OQ `substantive-l3-l2-erasure-scope-taxonomy` opened c044 D3, NOT yet materialized; the abstractor explicitly deferred authoring it to layer-intro-author/cross-cutter). The consolidated-count refresh is per-cycle index housekeeping owned by the layer-intro-author count-owner. Skip the four-step artifact check — explicit skip per the "open by construction" carve-out. (Confirmed not-yet-present: `grep -n "erasure-scope\|taxonomy" book/src/L3-L2/index.md` shows only the c044 §Working-Notes FLAG bullet routing the question to the meta-phase, not an authored taxonomy.)

### STOP-PROPOSING NEGATIVE LIST consult
None of `lu_solve` / `back_solve` / `ls-update-column` / `nleps_deflated_residual` / `nleps_deflated_solve` / `nleps_jacobian_action` / `nleps_eigenvalue_correction` appears in any dispatch scope. `apply_nonlinear_pencil` (the only remaining (B)-candidate) is NOT dispatched (HELD — folds into a future eigsolve-variant pass per the plan). PASS.

## Overlap analysis

- **D1 × D2:** NON-overlapping. Distinct theme files (`eigsolve-*` vs `chebyshev-*`), distinct parent L3 entries (`L3/eigsolve.md` vs `L3/chebyshev.md`) for the re-anchors. Both APPEND a distinct row to `book/src/L3-L2/index.md`'s theme table and a distinct line to `SUMMARY.md` — distinct-row/distinct-line appends are NOT overlapping at the operational level (§Discipline: "Two dispatches that append distinct rows to the same dep-map table are NOT overlapping → parallel"). Both also add a distinct §Vocabulary-cohort bullet under the same "Substantive / non-identity" sub-grouping — distinct bullets, non-overlapping. The ONE shared mutable derived value (the §Working-Notes consolidated firm-count tally + the coverage-gap denominator) is partitioned to D3 ONLY (count-ownership). → PARALLEL.
- **D1 × D3 / D2 × D3:** D3 reads what D1/D2 landed (which themes, which roots) to author the taxonomy + write the consolidated count. This is a forward-reference / count-ownership dependency, not a same-region write conflict — D1/D2 own their rows/bullets, D3 owns the tally + taxonomy note (disjoint regions of `index.md`). Sequenced (D3 in wave 2) so D3 sees the actual landings and the count is not authored blind. → SEQUENTIAL (D3 after D1/D2).
- **Count-ownership partition (explicit, per cycle-039 meta-phase / `parallel-blind-shared-index-count-divergence`):** D3 is the SOLE owner of the `L3-L2/index.md` consolidated firm-count tally (15 → 16 or 17) and the `l3-l2-rotation-theme-coverage-gap` denominator line. D1 and D2 each emit ONLY: (i) their own theme-table row, (ii) their own §Vocabulary-cohort bullet, (iii) their own SUMMARY.md chapter line, (iv) their own parent-L3-entry re-anchor — and DEFER the consolidated tally + coverage-gap line to D3. Stated inline in each dispatch scope.

## Sequencing schedule

- **Wave 1 (parallel):** D1 (`abstractor`, eigsolve substantive L3>L2 theme + parent re-anchor), D2 (`cross-layer-cross-cutter` audit-first, chebyshev L3>L2 decision + theme-if-warranted + parent re-anchor).
- **Wave 2 (after D1/D2 reports land):** D3 (`layer-intro-author`, erasure-scope taxonomy note + SOLE consolidated count/coverage-gap owner) — depends on D1/D2 so the taxonomy names the actual roots landed and the count reflects actual landings.

Per the §Discipline single-finalize rule: this wave structure orders the *dispatches*; there is exactly ONE `integrator-finalize` at cycle end (the book is NOT rebuilt between waves). Critics + repairers + `integrator-per-report` (serial, once per ready report) run after the dispatch reports, then the single `integrator-finalize`.

## Open questions / caveats

- **D2 verdict is genuinely open (audit-first).** The plan ASSUMES the likely-WARRANTED verdict (dedicated chebyshev theme) based on the `ksp-solve-outer-driver` substantive-theme precedent + OQ part (b)'s reasoning. If D2 returns IN-LINE-SUFFICIENT, that is a valid landing: no new index row, the in-line annotation is confirmed, the OQ closes on the "in-line covers it" disposition, and D3's tally moves only +1 (eigsolve). D3 must handle BOTH outcomes — it depends on D1/D2 precisely so the count + taxonomy reflect the actual verdict. Flagged so the orchestrator does not treat a D2 no-theme landing as a failed dispatch.
- **`l3-l2-rotation-theme-coverage-gap` denominator semantics for chebyshev.** The "18" denominator counts all 18 L3 entries. If D2 verdicts chebyshev as in-line-sufficient (no dedicated theme), the gap reaches 16-of-18 with chebyshev counted as "covered in-line" (like the BLAS-1 leaves' body-identity, but here for the obstruction). D3 should state the chebyshev disposition explicitly in the §Working-Notes so the denominator is not ambiguous. This interacts with the `chebyshev-floor-cohort-count-reconciliation` OQ already routed to the batch-13 meta-phase (the chebyshev naming-exception under the non-same-named `chebyshev-iteration` slug) — note for the batch-13 meta-phase (fires after this finalize): the chebyshev L3>L2 disposition + the chebyshev L2-floor naming-exception are two facets of the same "chebyshev is named differently at each layer" benign-asymmetry already recorded in OQ `chebyshev-smoother-l3-candidate-subsumed-closed`.
- **After cycle-045 the substantive L3>L2 cohort is complete (or one in-line-noted).** The next foundation frontier (c046+, post-batch-13-meta) is the L4→L3 / L2→L1 coverage edges (c044 §Unblocked "the next foundation tier") + resuming the uniform L0→L4 climb. The remaining (B)-candidate `apply_nonlinear_pencil` stays HELD (eigsolve-variant pass). No action needed this cycle; flagged for the batch-13 meta-phase reshape of the c046 active head.
- **Process signal already routed:** the `l3-leaf-cohort-l2-floor-reanchor-deferred-from-c043` observation (L2-floor landings should imply a same-cycle L3 re-anchor, not a deferred sweep) is a cycle-planner dispatch-design note candidate routed to the batch-13 meta-phase. This cycle's D1/D2 already bundle their parent-L3 re-anchors INTO the same dispatch (the substantive theme + its parent's stale "no L3-L2 theme file" re-anchor land together) — applying that signal's recommendation proactively. No separate lifter sweep needed for these two.

## Priorities.md update note

I am marking the c045 picks in `scaffolding/priorities.md` (the `l3-l2-rotation-theme-coverage-gap` High-fan-out item — appending the c045 eigsolve+chebyshev substantive slice as DISPATCHED) and noting the substantive-cohort taxonomy materialization. I am NOT doing the batch-level intake migration/compaction (meta-phase territory; fires after this finalize).
