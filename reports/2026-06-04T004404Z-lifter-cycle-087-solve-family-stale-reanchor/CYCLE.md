---
agent: lifter
invoked_at: 2026-06-04T004404Z
scope: L4 maturity re-anchor — solve_family rough-in (test-coverage-bounded) → firm residue sweep (the c086 firm-promotion left-behind references in the 5 files c086 did NOT touch)
status: integrated
integrated_at: 2026-06-04T005727Z
integration_commit: PLACEHOLDER_SHA
integration_notes: "cycle-087 (batch-27 position 3/3, the LAST primary cycle; the batch-27 meta-phase fires NEXT). Applied clean — a PURE maturity re-anchor (12 edits / 7 sites / 5 files) firming the stale solve_family `rough-in (test-coverage-bounded)` references the c086 firm-promotion sweep MISSED, incl. the load-bearing book/src/L4/index.md internal contradiction (solve_family in BOTH the firm AND rough-in cohort headers; firm-cohort entry body still asserting rough-in). ZERO operator status/count change (the maturity was already firm c086 D1), ZERO feature-column flip (electrostatic/magnetostatic STAY seed). cargo make book exit 0, zero build-repair, zero dead links. retroactive-budget global = 0. The DO-NOT-TOUCH-list divergence at index.md:47 was correctly APPLIED (critic-confirmed legitimate stale-correction). Promoted 1 dischargeable-at-next-meta flag + 1 NEW drift-pattern signal (firm-promotion-coupled-re-anchor-needs-whole-book-cross-reference-grep, routed ALSO as an integrator-signal) + 1 out-of-scope drive-by (feature/eigenfrequency-qfactor.L4.md:38 same drift class). counts_after UNCHANGED from c086: L4 firm 17 main / 21 grand, rough-in-test-coverage-bounded 0, feature columns 6 firm / 6 seed."
inputs:
  - book/src/L4/solve_family.md (the re-anchor SUBJECT — firmness: firm on disk, promoted c086 D1)
  - book/src/L4/index.md (the load-bearing internal-inconsistency fix — :57/:59 rough-in cohort, :47 firm-cohort stale status clause, :122 dep-map cell)
  - book/src/L4/frequency_sweep.md (:69, :506 contrast-prose)
  - book/src/L4-L3/solve-family-map-dissolution.md (:134, :140, :187 LHS-caveat narration)
  - book/src/feature/index.md (:68 own-constituent gate narration)
  - book/src/L4/fe_assemble.md (:171 contrast-prose)
---

# CYCLE: Re-anchor solve_family stale `rough-in (test-coverage-bounded)` references → `firm`

## Summary

Cycle-086 D1 promoted the L4 fixed-operator family-map combinator `solve_family` from `rough-in (test-coverage-bounded)` → **firm** (the firm-on-positive-structure / syntactic-identity escape; element-independence read off the const `BaseKspSolver::Mult` body, `palace/linalg/ksp.cpp:297-310`). `solve_family.md:4` is `firmness: firm` on disk, and the c086 finalize set the authoritative L4 counts to `L4_firm: 17`, `L4_rough_in: 1` (domain_energy_reduce ONLY), `L4_rough_in_test_coverage_bounded: 0` (solve_family was the SOLE member; now empty). The c086 grep-sweep was scoped to the L4-operator + feature-driver files and left a coherent residue of stale `solve_family` `rough-in (test-coverage-bounded)` references across 5 OTHER files — including a **load-bearing internal contradiction in `book/src/L4/index.md`**: `solve_family` is listed in BOTH the firm cohort AND the "Rough-in at L4" cohort, with a stale `(1 + 1 test-coverage-bounded)` count header that contradicts the on-disk `L4_rough_in_test_coverage_bounded: 0`. This is a **pure maturity re-anchoring pass** — every genuinely-stale `solve_family` maturity reference re-anchored to `firm`, the double-listing collapsed (solve_family kept ONLY in the firm cohort), the count header reconciled with on-disk counts. ZERO new files, ZERO operator status/count change to any OTHER operator, ZERO feature-column status flip (electrostatic/magnetostatic stay `seed` — their `gram_reduce` gate is unchanged), ZERO SUMMARY change.

**One divergence from the dispatch plan's DO-NOT-TOUCH list, flagged for the integrator (see §Discipline notes):** the plan listed `book/src/L4/index.md:47` as "correctly-firm → KEEP" on the assumption the firm-cohort entry "already says `*(firm; cycle-086 D1)*`". That assumption is **wrong on disk** — the `:47` firm-cohort entry body still asserts "Status `rough-in (test-coverage-bounded)` (structure firm; laws stated against strawman §3.7 but test-unconfirmed)". That is a genuine stale reference INSIDE the firm cohort (the `:32` cohort header narrates the promotion correctly, but the `:47` entry body contradicts it). I re-anchor `:47`'s status clause as a bounded, L0-evidenced prose-correction (the on-disk `firmness: firm` is the supporting evidence) — it is the same maturity-drift class as the other 5 sites, not a new content decision.

## Proposed changes

### 1. `book/src/L4/index.md` — the load-bearing internal-inconsistency fix

**1a. Collapse the "Rough-in at L4" cohort header + drop the solve_family rough-in bullet (`:57`/`:59`).** Re-anchor the count header from `(1 + 1 test-coverage-bounded)` to `(1)` (only `domain_energy_reduce` remains rough-in per on-disk `L4_rough_in: 1`, `L4_rough_in_test_coverage_bounded: 0`), drop the in-line "plus the fixed-operator family-map combinator `solve_family` (`rough-in (test-coverage-bounded)`)" clause, and remove the entire `:59` solve_family bullet (it is already correctly present in the firm/active-frontier cohort at `:71` as `*(firm; cycle-086 D1)*` — keeping both is the duplicate-listing inconsistency). The `:60` `domain_energy_reduce` bullet (the genuinely-remaining rough-in entry) is preserved unchanged.

```edit:book/src/L4/index.md
[old]: **Rough-in at L4 (1 + 1 test-coverage-bounded)** — the per-domain energy-table reduction verb (gated on its `matrix-weighted-norm` rough-in folded primitive + no per-domain test) plus the fixed-operator family-map combinator `solve_family` (`rough-in (test-coverage-bounded)`), both awaiting law confirmation. The eigenmode per-mode reduction [`eigenfreq_qfactor_reduce`](./eigenfreq_qfactor_reduce.md) **promoted to `firm` cycle-082** (firm-on-positive-structure escape; both folded per-mode primitives firm L1 — `eigenvalue-untransform` c080 + `participation_ratio` c077 — and the assembly carries no inner-product-axiom content), moving to the firm cohort above:

- [`solve_family`](./solve_family.md) *(rough-in (test-coverage-bounded))* — the fixed-operator **map-over-RHS-family outer-driver combinator** (capture the system operator `op` once / `SetOperators(*K,*K)` hoisted outside the loop, build the solver once, map the [`ksp_solve`](./ksp_solve.md) cap over the RHS family `[rhs_i]`, collect `[x_i]`). The **pure-`map` degenerate** of the strawman §3.7 [`iterate-while`](./iterate-while.md) family (each element independent, no carry; the trajectory IS the collected family — reusing the firm iterate-while vocabulary, the [`chebyshev`](./chebyshev.md) route), sitting one shell *above* the [`ksp_solve`](./ksp_solve.md) cap (`solve_loop` iterate-whiles over inner cycles; `solve_family` maps over an independent RHS family). It is a **combinator-as-entry**: the electrostatic terminal-boundary sweep and the magnetostatic surface-current sweep are **specialization notes re-expressing THROUGH it** (NOT two rectangular leaf chapters); they differ only in the absorbed family-index domain / RHS-construction / collection, all absorbed into `[Inputs]`. Status `rough-in (test-coverage-bounded)`: its structure is firm on two fixed-operator witnesses, but the load-bearing concatenation-homomorphism + operator-capture-once laws are stated against the strawman §3.7 family / `map` list-homomorphism algebra and are **test-unconfirmed** (the drivers are integration-level; no unit test exercises the family-map at the `Solve(mesh)` entry). Promotion route: a dedicated family-map test OR a strawman-derivation pass raising the list-homomorphism law confidence to `ksp_solve`-equivalent (a batch-17 lowering-verifier may promote it on the firm-on-positive-structure escape once the `KspSolver`-reuse statelessness is confirmed). **Scope: fixed-operator ONLY (2-of-5 pipelines** — electrostatic + magnetostatic; the driven pipeline rebuilds the operator per-frequency (`drivensolver.cpp:176-180`) and is the `per-element` superset `map_solve_over_(operator,rhs)_family`, batch-17-gated). Harvested cycle-055 D1 from the c054 combinator-miner mine.
- [`domain_energy_reduce`](./domain_energy_reduce.md) *(rough-in; cycle-079 D3)*
[new]: **Rough-in at L4 (1)** — the per-domain energy-table reduction verb (gated on its `matrix-weighted-norm` rough-in folded primitive + no per-domain test), awaiting law confirmation. The eigenmode per-mode reduction [`eigenfreq_qfactor_reduce`](./eigenfreq_qfactor_reduce.md) **promoted to `firm` cycle-082** (firm-on-positive-structure escape; both folded per-mode primitives firm L1 — `eigenvalue-untransform` c080 + `participation_ratio` c077 — and the assembly carries no inner-product-axiom content), and the fixed-operator family-map combinator [`solve_family`](./solve_family.md) **promoted to `firm` cycle-086** (the same firm-on-positive-structure / syntactic-identity escape — element-independence read off the const `BaseKspSolver::Mult` body, `palace/linalg/ksp.cpp:297-310`), both moving to the firm cohort above:

- [`domain_energy_reduce`](./domain_energy_reduce.md) *(rough-in; cycle-079 D3)*
```

**1b. Re-anchor the `:47` firm-cohort entry's stale status clause.** The `:47` entry sits in the FIRM cohort (its `:32` header narrates "cycle-086 promoted ... → `firm`") but the entry body still asserts solve_family is `rough-in (test-coverage-bounded)` — a load-bearing internal contradiction. Re-anchor the status clause to firm (this diverges from the plan's DO-NOT-TOUCH list — see §Discipline notes; it is the same maturity-drift class, L0-evidenced by `solve_family.md:4` `firmness: firm`):

```edit:book/src/L4/index.md
[old]: Load-bearing law: the **concatenation-homomorphism** `solve_family op (a ++ b) = solve_family op a ++ solve_family op b`, licensed by the **operator-capture-once / `SetOperators`-hoist** identity. Status `rough-in (test-coverage-bounded)` (structure firm; laws stated against strawman §3.7 but test-unconfirmed). **Scope: fixed-operator ONLY (2-of-5 pipelines** — electrostatic + magnetostatic; the driven pipeline breaks shared-operator-capture, `drivensolver.cpp:176-180`, and is the `per-element` superset `map_solve_over_(operator,rhs)_family`, batch-17-gated). Harvested cycle-055 D1 from the c054 combinator-miner mine.
[new]: Load-bearing law: the **concatenation-homomorphism** `solve_family op (a ++ b) = solve_family op a ++ solve_family op b`, licensed by the **operator-capture-once / `SetOperators`-hoist** identity. Status `firm` (cycle-086 D1; the firm-on-positive-structure / syntactic-identity escape — the concatenation-homomorphism and operator-capture-once laws are read-off identities and element-independence is read off the const `BaseKspSolver::Mult` body, `palace/linalg/ksp.cpp:297-310`, whose only cross-call state is two monotone telemetry counters). **Scope: fixed-operator ONLY (2-of-5 pipelines** — electrostatic + magnetostatic; the driven pipeline breaks shared-operator-capture, `drivensolver.cpp:176-180`, and is the `per-element` superset `map_solve_over_(operator,rhs)_family`, batch-17-gated). Harvested cycle-055 D1 from the c054 combinator-miner mine.
```

**1c. Re-anchor the `:122` dep-map TABLE status cell.** The dep-map row's status cell still reads `rough-in (test-coverage-bounded)`; re-anchor to `firm` matching the firm-cohort narration at `:71`:

```edit:book/src/L4/index.md
[old]: | `rough-in (test-coverage-bounded)` (harvested cycle-055 D1; structure firm on 2 fixed-operator witnesses electrostatic `palace/drivers/electrostaticsolver.cpp:30-90` + magnetostatic `palace/drivers/magnetostaticsolver.cpp:30-100`, laws stated against strawman §3.7 / map list-homomorphism but test-unconfirmed — drivers integration-level, no unit test; promotion = dedicated family-map test OR strawman-derivation pass. Scope: fixed-operator ONLY 2-of-5; driven `drivensolver.cpp:176-180` is the `per-element` superset, batch-17-gated) |
[new]: | `firm` (harvested cycle-055 D1; promoted `rough-in (test-coverage-bounded)` → `firm` cycle-086 D1 via the firm-on-positive-structure / syntactic-identity escape — structure firm on 2 fixed-operator witnesses electrostatic `palace/drivers/electrostaticsolver.cpp:30-90` + magnetostatic `palace/drivers/magnetostaticsolver.cpp:30-100`; the concatenation-homomorphism + map list-homomorphism laws are read-off identities and element-independence is read off the const `BaseKspSolver::Mult` body, `palace/linalg/ksp.cpp:297-310`. Scope: fixed-operator ONLY 2-of-5; driven `drivensolver.cpp:176-180` is the `per-element` superset, batch-17-gated) |
```

### 2. `book/src/L4/frequency_sweep.md` — contrast-prose

**2a. `:69` — the out-of-scope/batch-17-gating contrast.** The out-of-scope/batch-17-gating fact is unchanged, but `solve_family` is now firm; re-narrate the contrast so it reads "the firm `solve_family` entry records `per-element` as out-of-scope":

```edit:book/src/L4/frequency_sweep.md
[old]: is `fixed` (op captured once, hoisted), `frequency_sweep` is `per-element` (op
rebuilt per member, inside the map). This is the named `per-element` value that the
`solve_family` `rough-in (test-coverage-bounded)` entry records as out-of-scope and
batch-17-gated (`L4/solve_family.md:137,146,163`). It reuses the firm
[`iterate-while`](./iterate-while.md) family rather than introducing a new iteration
primitive — the same route [`chebyshev`](./chebyshev.md) and
[`solve_family`](./solve_family.md) took.
[new]: is `fixed` (op captured once, hoisted), `frequency_sweep` is `per-element` (op
rebuilt per member, inside the map). This is the named `per-element` value that the
firm [`solve_family`](./solve_family.md) entry records as out-of-scope and
batch-17-gated (`L4/solve_family.md:137,146,163`). It reuses the firm
[`iterate-while`](./iterate-while.md) family rather than introducing a new iteration
primitive — the same route [`chebyshev`](./chebyshev.md) and
[`solve_family`](./solve_family.md) took.
```

**2b. `:506` — the firm-vs-rough-in contrast block.** `solve_family`'s independence claim was DISCHARGED c086 (read off the const `BaseKspSolver::Mult` body) — both entries are now firm, so the *firm-vs-rough-in maturity* contrast is moot. Re-narrate to PRESERVE the load-bearing **fresh-vs-reused-operator** distinction (the substantive point: `frequency_sweep` rebuilds a fresh operator per member, `solve_family` reuses one shared capture) while dropping the now-false maturity contrast:

```edit:book/src/L4/frequency_sweep.md
[old]: so the absence of a dedicated driven-sweep unit test does NOT gate them, and the entry is
`firm` rather than `rough-in (test-coverage-bounded)`. (Contrast
[`solve_family`](./solve_family.md), which is `rough-in (test-coverage-bounded)` because
its load-bearing claim — that the `KspSolver` *reuse* across members carries no hidden
cross-member state — is test-unconfirmed; `frequency_sweep` rebuilds a *fresh* operator
and re-captures it per member, so the no-cross-member-state property is read directly off
the per-member `SetOperators(*A, *P)` rebuild, not assumed of a reused solver — the
firm-vs-rough-in distinction is in the operator-capture axis itself.)
[new]: so the absence of a dedicated driven-sweep unit test does NOT gate them, and the entry is
`firm`. (Contrast the **operator-capture axis** against [`solve_family`](./solve_family.md),
also `firm` (c086): `solve_family`'s load-bearing independence claim — that the `KspSolver`
*reuse* across members carries no hidden cross-member state — was ultimately discharged on
positive structure (read off the const `BaseKspSolver::Mult` body, `palace/linalg/ksp.cpp:297-310`,
whose only cross-call state is two monotone telemetry counters); `frequency_sweep` instead
rebuilds a *fresh* operator and re-captures it per member, so its no-cross-member-state
property is read directly off the per-member `SetOperators(*A, *P)` rebuild rather than off a
reused solver. The two entries are now equal in maturity (both firm); the load-bearing
distinction is the **fixed-shared-capture vs. fresh-per-member-rebuild** axis itself, not a
firm-vs-rough-in maturity gap.)
```

### 3. `book/src/L4-L3/solve-family-map-dissolution.md` — LHS-caveat narration (theme STAYS firm)

The theme is firm and stays firm; only its narration of the now-moot LHS caveat is stale.

**3a. `:134` — the §Speculative-L4-operators LHS-status mention:**

```edit:book/src/L4-L3/solve-family-map-dissolution.md
[old]: None. This theme lowers an already-authored L4 combinator ([`solve_family`](../L4/solve_family.md), firm-structure cycle-055 D1, status `rough-in (test-coverage-bounded)`) assembled from the already-firm [`ksp_solve`](../L4/ksp_solve.md) cap mapped via the already-firm [`iterate-while`](../L4/iterate-while.md) family. No new speculative operator is introduced.
[new]: None. This theme lowers an already-authored L4 combinator ([`solve_family`](../L4/solve_family.md), firm-structure cycle-055 D1, status `firm` since cycle-086 D1) assembled from the already-firm [`ksp_solve`](../L4/ksp_solve.md) cap mapped via the already-firm [`iterate-while`](../L4/iterate-while.md) family. No new speculative operator is introduced.
```

**3b. `:140` — the §Verified-against bullet describing the LHS §Status:**

```edit:book/src/L4-L3/solve-family-map-dissolution.md
[old]: §Variant axes (the operator-capture scope boundary, the absorbed family-index / collection-shape axes), §Status (the `rough-in (test-coverage-bounded)` caveat this theme's §Status reasons about).
[new]: §Variant axes (the operator-capture scope boundary, the absorbed family-index / collection-shape axes), §Status (`firm` since cycle-086 D1; previously the `rough-in (test-coverage-bounded)` caveat this theme's §Status reasons about — see the §Status note below on its anticipated resolution).
```

**3c. `:187` — the "On the inherited LHS test-coverage caveat" paragraph.** This paragraph EXPLICITLY PREDICTED its own resolution ("should a batch-17 lowering-verifier pass confirm the `KspSolver`-reuse carries no cross-element state (promoting the LHS cap to `firm`), this theme is unaffected — it is already firm on structure"). That promotion LANDED (c086). Re-narrate from "the LHS is rough-in, but this theme is firm anyway" to "the LHS firmed c086 exactly as anticipated" — PRESERVING the firm-on-structure reasoning (load-bearing, still correct), updating only the LHS-maturity premise:

```edit:book/src/L4-L3/solve-family-map-dissolution.md
[old]: **On the inherited LHS test-coverage caveat (reasoning, load-bearing).** The LHS [`solve_family`](../L4/solve_family.md) is status `rough-in (test-coverage-bounded)`: its *algebraic laws* (the concatenation-homomorphism and the underlying family-member independence) are stated against the strawman §3.7 family / the `map` list-homomorphism algebra but are **test-unconfirmed** (the drivers are integration-level; no unit test exercises the family-map's independence at the `Solve(mesh)` entry). This theme is **`firm` rather than inheriting that caveat** because the *rotation shape* — what this theme asserts — does **not depend on the independence law**. The theme asserts that the L4 map shell *dissolves into* the L3 accumulating-loop shape with the operator hoisted; that assertion is a **structural identity on the map-shell syntax**, read directly off positive source (the `SetOperators`-outside-the-`for` placement, the pre-sized `std::vector<Vector>`, the `step++` accumulator are all *present in the Palace source*, not reconstructed). The independence law is what licenses *reordering / parallelizing* the family (the cap's Law 1 consequence) — a property of the *family-map semantics*, not of the *dissolution shape*. Concretely: even if a future test were to reveal hidden cross-member state in the `KspSolver` reuse (which would demote the cap's independence law), the *dissolution itself* would still hold — the L4 map shell would still dissolve into the same L3 accumulating loop (the loop would simply not be safely reorderable, exactly as the L3 sequential `for` already is not exploited for parallelism). So the test-coverage gate sits on the **upstream cap's laws**, not on **this rotation's shape**; the rotation is firm. **Promotion-of-the-cap note**: should a batch-17 lowering-verifier pass confirm the `KspSolver`-reuse carries no cross-element state (promoting the LHS cap to `firm`), this theme is unaffected — it is already firm on structure; the cap promotion would only strengthen the family-map *semantics* this theme's RHS faithfully renders. (OQ `solve-family-map-dissolution-firm-on-structure-vs-lhs-test-coverage` records this scoping for the batch-17 verifier.)
[new]: **On the (former) inherited LHS test-coverage caveat (reasoning, load-bearing).** The LHS [`solve_family`](../L4/solve_family.md) firmed cycle-086 D1; it was previously status `rough-in (test-coverage-bounded)` because its *algebraic laws* (the concatenation-homomorphism and the underlying family-member independence) were stated against the strawman §3.7 family / the `map` list-homomorphism algebra but were then **test-unconfirmed** (the drivers are integration-level; no unit test exercises the family-map's independence at the `Solve(mesh)` entry). Even while that caveat stood, this theme was **`firm` rather than inheriting it** because the *rotation shape* — what this theme asserts — does **not depend on the independence law**. The theme asserts that the L4 map shell *dissolves into* the L3 accumulating-loop shape with the operator hoisted; that assertion is a **structural identity on the map-shell syntax**, read directly off positive source (the `SetOperators`-outside-the-`for` placement, the pre-sized `std::vector<Vector>`, the `step++` accumulator are all *present in the Palace source*, not reconstructed). The independence law is what licenses *reordering / parallelizing* the family (the cap's Law 1 consequence) — a property of the *family-map semantics*, not of the *dissolution shape*. Concretely: even had a future test revealed hidden cross-member state in the `KspSolver` reuse (which would have demoted the cap's independence law), the *dissolution itself* would still hold — the L4 map shell would still dissolve into the same L3 accumulating loop (the loop would simply not be safely reorderable, exactly as the L3 sequential `for` already is not exploited for parallelism). So the test-coverage gate sat on the **upstream cap's laws**, not on **this rotation's shape**; the rotation is firm. **Cap-promotion outcome (LANDED c086, exactly as anticipated)**: the cap promotion this paragraph predicted happened — cycle-086 D1 promoted the LHS cap to `firm` via the firm-on-positive-structure / syntactic-identity escape (element-independence read off the const `BaseKspSolver::Mult` body, `palace/linalg/ksp.cpp:297-310`, whose only cross-call state is two monotone telemetry counters — discharging the independence law on positive structure rather than awaiting a dedicated test). As predicted, this theme is **unaffected** — it was already firm on structure; the cap promotion only strengthens the family-map *semantics* this theme's RHS faithfully renders. (OQ `solve-family-map-dissolution-firm-on-structure-vs-lhs-test-coverage` recorded this scoping; it is now discharged — the LHS firmed and the theme's firm-on-structure reasoning held.)
```

### 4. `book/src/feature/index.md:68` — own-constituent gate narration (column STAYS seed)

`solve_family` is firm; the electrostatic/magnetostatic own-constituent gate is now `gram_reduce` ONLY. The columns STAY `seed` (the `gram_reduce` `rough-in (test-coverage-bounded)` gate is unchanged). Re-narrate to match the canonical narration already on `electrostatic.L4.md:56` ("the own-constituent gate has narrowed ... `solve_family` ... is now firm (c086)"):

```edit:book/src/feature/index.md
[old]:   - [`electrostatic`](./electrostatic.L4.md) + [`magnetostatic`](./magnetostatic.L4.md) — own `solve_family` + own `gram_reduce` are `rough-in (test-coverage-bounded)`.
[new]:   - [`electrostatic`](./electrostatic.L4.md) + [`magnetostatic`](./magnetostatic.L4.md) — own `gram_reduce` is `rough-in (test-coverage-bounded)` (own `solve_family` firmed c086; the own-constituent gate has narrowed to `gram_reduce` alone).
```

### 5. `book/src/L4/fe_assemble.md:171` — contrast-prose (fe_assemble STAYS firm)

`solve_family`'s independence claim was DISCHARGED c086 (the firm-on-positive-structure escape applied to it too). Re-narrate the contrast: PRESERVE `fe_assemble`'s own firm-on-positive-structure reasoning (the load-bearing point — term-independence is a positive structural fact off the `AddSubOperator` integrator-fold); update the now-equal-maturity `solve_family` sibling reference (its independence was *also* ultimately discharged on positive structure, so the once-test-bounded contrast is historical):

```edit:book/src/L4/fe_assemble.md
[old]: This is distinct from `solve_family`'s `rough-in (test-coverage-bounded)`: `solve_family`'s laws encode an *independence claim* (no hidden cross-element state in the `KspSolver` reuse) that integration-level-only coverage leaves test-unconfirmed; here the analogous claim — that the term contributions are independent (no cross-term coupling) — is a **positive structural fact** read directly off the L0 `AddSubOperator` integrator-fold (`palace/fem/bilinearform.cpp:77`: each `sub_op` is built and added independently; the composite action is their sum) and the firm L1 fold, not an unconfirmed runtime property. So the firm-on-positive-structure escape applies (the same reasoning that makes `fold_solve` firm and `L1/fe_assemble` firm), and the entry is `firm`.
[new]: Here `fe_assemble`'s analogous independence claim — that the term contributions are independent (no cross-term coupling) — is a **positive structural fact** read directly off the L0 `AddSubOperator` integrator-fold (`palace/fem/bilinearform.cpp:77`: each `sub_op` is built and added independently; the composite action is their sum) and the firm L1 fold, not an unconfirmed runtime property. So the firm-on-positive-structure escape applies (the same reasoning that makes `fold_solve` firm and `L1/fe_assemble` firm), and the entry is `firm`. (`solve_family`'s sibling independence claim — no hidden cross-element state in the `KspSolver` reuse — was *also* ultimately discharged on positive structure, promoting it to `firm` at c086 by reading off the const `BaseKspSolver::Mult` body; the two combinators are now equal in maturity, both firm on positive structure.)
```

## Discipline notes

This is a **pure maturity re-anchoring pass** — the deliverable IS the corrected citation/maturity prose. Each of the 5 files (6 sites + the divergence site) carried a `solve_family` `rough-in (test-coverage-bounded)` reference left behind by the c086 firm-promotion grep-sweep (which was scoped to the L4-operator + feature-driver files). No structural decomposition, signature, or rotation-direction is touched; the high→low theme direction in `solve-family-map-dissolution.md` is preserved (LHS = L4, RHS = L3, narrated forward); the `solve_family` SUBJECT file (`solve_family.md`) is NOT touched (it is already firm on disk and was the c086 D2 cleanup scope).

**Bounded prose-corrections recorded (CLAUDE.md "L0-evidence-driven prose correction is in-scope when bounded + evidenced + recorded"):**
- **`index.md:47` status clause re-anchor (the divergence from the plan's DO-NOT-TOUCH list).** The plan listed `:47` as "correctly-firm → KEEP" on the stated assumption that the firm-cohort entry "already says `*(firm; cycle-086 D1)*`". On-disk verification shows that assumption is WRONG: the `:47` firm-cohort entry body still reads "Status `rough-in (test-coverage-bounded)` (structure firm; laws stated against strawman §3.7 but test-unconfirmed)". The plan's `:32`/`:71` "correctly-firm" observation holds for the cohort HEADER (`:32`) and the active-frontier entry (`:71`), but NOT for the firm-cohort per-entry body at `:47`. This is a genuine internal contradiction (the `:32` header says "cycle-086 promoted ... → `firm`" two lines above an entry body that says it is `rough-in`), in the SAME maturity-drift class as the 5 plan-named sites. (i) Supported by the L0 citation `book/src/L4/solve_family.md:4` `firmness: firm` (read this dispatch) + the c086 finalize counts (`L4_rough_in_test_coverage_bounded: 0`, read from `scaffolding/cycle-record.jsonl` cycle-086); (ii) bounded — a status-clause re-anchor, NOT a re-architecture of the entry's decomposition/signature; (iii) recorded here. I corrected it rather than leaving the contradiction for the meta-phase to inherit, since "land-clean / self-consistent" is the cycle's explicit discipline. **Flagged for the integrator:** this `:47` edit is the ONE deviation from the dispatch plan's literal DO-NOT-TOUCH list — applying it is correct (the plan's KEEP rationale was based on an inaccurate on-disk assumption), but I surface it explicitly so the integrator does not treat it as an out-of-scope leak.
- **`index.md:57` count-header reconciliation.** Re-anchored `(1 + 1 test-coverage-bounded)` → `(1)`. This is a pure re-anchor against the on-disk authoritative count (`L4_rough_in: 1` = domain_energy_reduce only; `L4_rough_in_test_coverage_bounded: 0`), NOT a count-owner re-tally — the count was already established by the c086 finalize; I am only correcting a prose header that drifted from it. No count-owner decision is needed (the cohort membership is unambiguous on disk: solve_family is firm, domain_energy_reduce is the sole rough-in).

**The c057-meta index-cell guard (CLAUDE.md "When you flip a chapter/theme `## Status` ... update the matching index-table status cell in the SAME proposed-changes pass"):** this dispatch does NOT flip any `## Status` line (solve_family's `## Status` was already flipped to firm at c086). The `index.md:122` dep-map cell re-anchor (§1c) IS the lagging index-cell update that the c086 promotion left stale — exactly the desync the guard targets, here corrected one cycle late as the named land-clean residue. No NEW status flip is introduced this cycle, so no further index cell needs touching.

**No feature-column status flip:** electrostatic + magnetostatic STAY `seed` (their `gram_reduce` `rough-in (test-coverage-bounded)` own-constituent gate is unchanged and convergently blocked on the `matrix-weighted-norm` √-cascade, NO-GO-HELD). The `feature/index.md:68` edit (§4) narrates the *narrowed* gate (`gram_reduce` alone), it does NOT change any `status:` token.

## Supporting evidence

- **Re-anchor SUBJECT maturity:** `book/src/L4/solve_family.md:4` — `firmness: firm` (read this dispatch; promoted c086 D1).
- **Authoritative count state:** `scaffolding/cycle-record.jsonl` cycle-086 record — `L4_firm: 17`, `L4_rough_in: 1`, `L4_rough_in_test_coverage_bounded: 0` (the `:57` header reconciliation target; the c086 finalize `counts_after`).
- **L0 evidence cited in the re-anchors (verified this dispatch):** `reference/palace/palace/linalg/ksp.cpp:297-310` — the const `BaseKspSolver<OperType>::Mult` body; the only cross-call state is `ksp_mult++` (line 308) and `ksp_mult_it += ksp->GetNumIterations()` (line 309), two monotone telemetry counters — the positive-structure element-independence evidence that licensed the c086 firm-on-positive-structure promotion. (`get_symbol_def`/`read_range`-equivalent on-disk `sed` confirmed the anchor sits exactly on the const `Mult` body.)
- **Canonical re-narration pattern matched:** `book/src/feature/electrostatic.L4.md:56` + `magnetostatic.L4.md:56` (DO-NOT-TOUCH; already correct) — "the [`solve_family`](../L4/solve_family.md) per-terminal map is now **firm** (c086 ...), so the own-constituent gate has narrowed ... to ONE: firming `gram_reduce`". The `feature/index.md:68` re-narration (§4) is brought into alignment with these two already-correct sibling files.
- **All [old] anchors verified exact-match unique on-disk** (grep `-c` == 1 for each, this dispatch): `index.md` :57-header / :47-status-clause / :47-scope-clause / :59-italic-bullet-start / :122-cell-tail; `frequency_sweep.md` :69 / :506; `solve-family-map-dissolution.md` :134 / :140 / :187; `feature/index.md` :68; `fe_assemble.md` :171.

## Open questions / caveats

- **The `:47` DO-NOT-TOUCH divergence (flagged above in §Discipline notes) is the only point where the integrator should expect an edit the plan's literal exclusion list did not anticipate.** It is correct to apply (the plan's KEEP rationale rested on an inaccurate on-disk assumption — the firm-cohort entry body at `:47` was NOT already firm-narrated, only the `:32` header was). If the integrator prefers to be conservative and NOT apply the `:47` edit, the result would be a *surviving* internal contradiction (a firm-cohort entry asserting `rough-in`), which contradicts the cycle's land-clean / self-consistent goal — so applying it is the recommended disposition.
- **No abstractor reread is required.** Every site is a maturity-prose re-anchor with the firm structure/signature/rotation unchanged; the formalized `solve_family` signature (`solve_family :: OpParams -> [Inputs] -> [SimState]`) is identical to what every theme/contrast assumed (it firmed in place, the signature did not shift). This is pure rewriting, not authorship.
- **OQ `solve-family-map-dissolution-firm-on-structure-vs-lhs-test-coverage` is now dischargeable** (noted in the §3c re-narration): the batch-17 scoping it recorded resolved exactly as the theme's §Status predicted — the LHS cap firmed (c086) and the theme's firm-on-structure reasoning held. I do not close the OQ myself (append-only between meta-phases / meta-phase owns unify-close), but flag it here for the integrator's Open-questions promotion + the batch-27 meta-phase's intake→plan migration.
- **Drift-pattern signal for the batch-27 meta-phase (NOT actionable this cycle):** the c087 residue (this dispatch's 6 sites across 5 files, PLUS the `:47` site the c086 grep-sweep also missed INSIDE the same `index.md` it partially corrected) is the `floor-landing-implies-same-cycle-adjacent-entry-reanchor` friction in its L4-operator-FIRM-promotion form. The c086 sweep corrected `index.md`'s `:32`/`:71` cohort narration but missed `:47`/`:57`/`:59`/`:122` in the same file — evidence that a firm-promotion's coupled re-anchor should carry a **whole-`book/src/` cross-reference grep** (the cycle-planner already surfaced this candidate in the c087 plan §Open-questions; the `:47`-in-same-file miss strengthens it — the sweep missed sites in the very file it edited, not only sibling files).
