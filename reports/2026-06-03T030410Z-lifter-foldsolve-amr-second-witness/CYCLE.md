---
agent: lifter
invoked_at: 2026-06-03T030410Z
scope: L4 fold_solve — add AMR loop as 2nd state-generated schedule-source witness
status: pending
inputs:
  - book/src/L4/fold_solve.md
  - reference/palace/palace/drivers/basesolver.cpp:153-276 (BaseSolver::SolveEstimateMarkRefine)
integrated_at: 2026-06-03T214500Z
integration_commit: 03d43ae
integration_notes: "cycle-073 D6 (LOW observation). Applied clean — x4 additive AMR 2nd-state-generated-witness fold-in to firm L4/fold_solve.md (BaseSolver::SolveEstimateMarkRefine basesolver.cpp:153-276 folded onto the state-generated schedule-source value; witness 1->2). Entry stays firm; no status/signature/law change; '2-of-5 pipelines' Scope deliberately NOT incremented (AMR is a driver-agnostic wrapper). OQ datapoint discharges the c072-D2 pre-positioned fold-solve-state-generated-schedule-source-second-witness-amr-loop. Build exit 0, linkcheck2 clean."
---

# CYCLE: Re-anchor fold_solve — AMR loop as 2nd state-generated witness

## Summary

`book/src/L4/fold_solve.md` carries a `schedule-source` variant axis (`fixed-list | state-generated`) that is the entry's load-bearing axis. The `state-generated` value currently has exactly ONE witness (driven-PROM `SweepAdaptive`'s greedy / error-terminated march). This dispatch (cycle-073 D6, LOW, observation-routed fold-in) adds the **AMR refinement loop** (`BaseSolver::SolveEstimateMarkRefine`, `basesolver.cpp:153-276`) as the **2nd state-generated witness**, raising the state-generated witness count 1 → 2. The AMR loop is a Solve → Estimate → Mark → Refine `while` whose loop bound is derived from the recomputed error-indicator STATE (`err = indicators.Norml2(comm)` re-evaluated each iteration, `:267`; loop continues while `err >= refinement.tol`, `:190`) — the same `schedule-source = state-generated` shape as SweepAdaptive: the next iteration's input (the refined mesh) and the termination are both generated from accumulated error-indicator state, not consumed from a fixed list. This is a pure strengthening of the existing firm entry: it confirms the `schedule-source` axis is genuinely multi-witness on the state-generated side. It does NOT introduce a new combinator, does NOT change the entry's `firm` status, does NOT touch the signature / laws. Surgical: frontmatter axis line + §Variant axes row-1 + a §Specializations witness note + a §Evidence witness block.

## Proposed changes

Four surgical, citation-only additions to `book/src/L4/fold_solve.md` — each appends the AMR witness alongside the existing SweepAdaptive witness on the `state-generated` side of the `schedule-source` axis. No status / signature / law text changes.

### Edit 1 — frontmatter `schedule-source` axis (name the AMR witness alongside SweepAdaptive)

```edit:book/src/L4/fold_solve.md
[old]:  - schedule-source (fixed-list: the carry consumes a precomputed [Time] schedule, foldl over a uniform list — transient | state-generated: the carry GENERATES the next input + the loop bound from accumulated state, a greedy/error-terminated march — driven-PROM SweepAdaptive. THE load-bearing axis; the fixed-list form is the default surface, the state-generated form the recorded generalization)
[new]:  - schedule-source (fixed-list: the carry consumes a precomputed [Time] schedule, foldl over a uniform list — transient | state-generated: the carry GENERATES the next input + the loop bound from accumulated state, an error-terminated march — TWO witnesses: driven-PROM SweepAdaptive (greedy frequency-sampling) + the AMR Solve→Estimate→Mark→Refine loop (basesolver.cpp:190, error-indicator-terminated). THE load-bearing axis; the fixed-list form is the default surface, the state-generated form the recorded generalization, now twice-witnessed)
```

### Edit 2 — §Variant axes, item 1 (schedule-source) — add the AMR witness to the state-generated value

```edit:book/src/L4/fold_solve.md
[old]:1. **schedule-source** (`fixed-list | state-generated`) — **THE load-bearing axis**, and the map/fold-spine generalization boundary. `fixed-list` (the default surface, transient): the carry consumes a precomputed `[Time]` schedule, `foldl` over a uniform list (`transientsolver.cpp:35-36,77`). `state-generated` (driven-PROM SweepAdaptive): the carry **generates** the next input (`omega_star = FindMaxError(state)` `drivensolver.cpp:389`) AND the loop bound (`memory < convergence_memory` `:384,398`) from accumulated state — a greedy/error-terminated march. Both share the fold spine (carry threaded, prior output = next input); the difference is whether the schedule is a free `[Time]` input or a state-derived generation. **This entry's default signature is `fixed-list`** (the transient witness); the `state-generated` form is rendered through the §3.7 carry and recorded here, with the question of whether it warrants a dedicated chapter deferred to OQ `fold-solve-greedy-schedule-source-generalization` (batch-18).
[new]:1. **schedule-source** (`fixed-list | state-generated`) — **THE load-bearing axis**, and the map/fold-spine generalization boundary. `fixed-list` (the default surface, transient): the carry consumes a precomputed `[Time]` schedule, `foldl` over a uniform list (`transientsolver.cpp:35-36,77`). `state-generated` is now **twice-witnessed** (both: the carry **generates** the next input AND the loop bound from accumulated state, an error-terminated march; neither has a free `[Time]` input):
   - **driven-PROM SweepAdaptive** — next input `omega_star = FindMaxError(state)` (`drivensolver.cpp:389`), loop bound `memory < convergence_memory` (`:384,398`); the greedy frequency-sampling march.
   - **AMR Solve→Estimate→Mark→Refine** (`basesolver.cpp:153-276`, `BaseSolver::SolveEstimateMarkRefine`) — next input is the **refined mesh** generated from the prior step's error indicators (`GeneralRefinement(marked_elements, ...)` `:239`, where `marked_elements` is Dörfler-thresholded from the error-indicator state), and the loop bound is the recomputed **error indicator** (`err = indicators.Norml2(comm)` re-evaluated each iteration `:267`; `while (use_amr && !ExhaustedResources(it, ntdof) && err >= refinement.tol)` `:190`). The next input + termination are both state-generated, not a fixed list — the same state-generated shape as SweepAdaptive.

   Both state-generated witnesses share the fold spine (carry threaded, prior output = next input); the difference from `fixed-list` is whether the schedule is a free `[Time]` input or a state-derived generation. **This entry's default signature is `fixed-list`** (the transient witness); the `state-generated` form is rendered through the §3.7 carry and recorded here, with the question of whether the (now twice-witnessed) state-generated form warrants a dedicated chapter deferred to OQ `fold-solve-greedy-schedule-source-generalization` (batch-18+).
```

### Edit 3 — §Specializations — add the AMR witness note alongside the SweepAdaptive note

```edit:book/src/L4/fold_solve.md
[old]:Both sweeps share the **state-threaded fold spine** (carry threaded forward, prior output = next input, opaque per-step body). The difference is the **`schedule-source` axis**: transient consumes a fixed `[Time]`; SweepAdaptive generates `[omega_star_k]` from accumulated state with a state-derived termination.
[new]:- **AMR mesh-refinement loop (Solve→Estimate→Mark→Refine)** (`palace/drivers/basesolver.cpp`, `BaseSolver::SolveEstimateMarkRefine`, `:153-276`). A **2nd state-generated-schedule** witness (alongside SweepAdaptive) — the schedule (the sequence of refined meshes) is **generated from the carry**, not consumed from a fixed list. The seed `s0` is the initial solve + error estimate (`auto [indicators, ntdof] = Solve(mesh)` `:174`, `err = indicators.Norml2(comm)` `:175`). The loop `while (use_amr && !ExhaustedResources(it, ntdof) && err >= refinement.tol)` (`:190`) has a **state-derived loop bound** — the genuine termination criterion is the recomputed error indicator `err >= refinement.tol` (`err` re-evaluated each iteration from the new solve, `:267`), with resource caps `it >= refinement.max_it` (`:182`) and `ntdof > refinement.max_size` (`:184`) as bounds — AND a **state-derived per-step input**: the refined mesh `GeneralRefinement(marked_elements, ...)` (`:239`), where `marked_elements` is Dörfler-thresholded from the **current** error-indicator state. Each step reads the prior carry (mesh + error indicators) and produces the next (refined mesh + new solve `std::tie(indicators, ntdof) = Solve(mesh)` `:266`). This is the **2nd witness** that the `schedule-source = state-generated` value is genuinely multi-witness (not a SweepAdaptive singleton). The per-step body bottoms out in the opaque `Solve(mesh)` driver call (the same opaque-library quantification). AMR is currently unsupported for transient (`:157-162`).

Both the transient + the two state-generated sweeps share the **state-threaded fold spine** (carry threaded forward, prior output = next input, opaque per-step body). The difference is the **`schedule-source` axis**: transient consumes a fixed `[Time]`; SweepAdaptive generates `[omega_star_k]` and AMR generates `[refined_mesh_k]`, both from accumulated state with a state-derived termination.
```

### Edit 4 — §Evidence — add the AMR witness block alongside the SweepAdaptive witness block

```edit:book/src/L4/fold_solve.md
[old]:  - `palace/drivers/drivensolver.cpp:73` (`adaptive ? SweepAdaptive(space_op) : SweepUniform(space_op)` — the adaptive dispatch), `:231` (`ErrorIndicator DrivenSolver::SweepAdaptive(SpaceOperator &space_op) const` — the greedy march entry), `:241` (`offline_tol = iodata.solver.driven.adaptive_tol`), `:242` (`convergence_memory = iodata.solver.driven.adaptive_memory`), `:243` (`max_size_per_excitation = iodata.solver.driven.adaptive_max_size`), `:244` (`nprom_indices = iodata.solver.driven.prom_indices.size()` — the PROM greedy params), `:384` (`for (std::size_t it0 = it; it < max_size_per_excitation && memory < convergence_memory; it++)` — the state-derived loop bound), `:389` (`omega_star = prom_op.FindMaxError(excitation_idx)[0]` — the state-derived per-step input: next sample = argmax of current PROM error), `:398` (`memory = max_errors.back() < offline_tol ? memory + 1 : 0` — the state-derived termination counter update from per-step error).
[new]:  - `palace/drivers/drivensolver.cpp:73` (`adaptive ? SweepAdaptive(space_op) : SweepUniform(space_op)` — the adaptive dispatch), `:231` (`ErrorIndicator DrivenSolver::SweepAdaptive(SpaceOperator &space_op) const` — the greedy march entry), `:241` (`offline_tol = iodata.solver.driven.adaptive_tol`), `:242` (`convergence_memory = iodata.solver.driven.adaptive_memory`), `:243` (`max_size_per_excitation = iodata.solver.driven.adaptive_max_size`), `:244` (`nprom_indices = iodata.solver.driven.prom_indices.size()` — the PROM greedy params), `:384` (`for (std::size_t it0 = it; it < max_size_per_excitation && memory < convergence_memory; it++)` — the state-derived loop bound), `:389` (`omega_star = prom_op.FindMaxError(excitation_idx)[0]` — the state-derived per-step input: next sample = argmax of current PROM error), `:398` (`memory = max_errors.back() < offline_tol ? memory + 1 : 0` — the state-derived termination counter update from per-step error).
- **Fold witness 3 — AMR Solve→Estimate→Mark→Refine loop (positive, 2nd state-generated-schedule variant):**
  - `palace/drivers/basesolver.cpp:153` (`void BaseSolver::SolveEstimateMarkRefine(std::vector<std::unique_ptr<Mesh>> &mesh) const` — the AMR march entry, range `:153-276`), `:174` (`auto [indicators, ntdof] = Solve(mesh)` — the seed solve + error estimate), `:175` (`double err = indicators.Norml2(comm)` — the seed error-indicator state), `:182` (`ret |= (it >= refinement.max_it)` — the iteration resource cap), `:184` (`ret |= (refinement.max_size > 0 && ntdof > refinement.max_size)` — the DOF resource cap), `:190` (`while (use_amr && !ExhaustedResources(it, ntdof) && err >= refinement.tol)` — the state-derived loop bound: the genuine termination is the error indicator `err >= refinement.tol`), `:239` (`fine_mesh.GeneralRefinement(marked_elements, -1, refinement.max_nc_levels)` — the state-derived per-step input: the refined mesh generated from the Dörfler-marked error-indicator state), `:266` (`std::tie(indicators, ntdof) = Solve(mesh)` — the per-step solve advancing the carry), `:267` (`err = indicators.Norml2(comm)` — the recomputed error indicator that re-derives the loop bound each iteration). Each step reads the prior carry (mesh + error indicators) and produces the next (refined mesh + new solve) — the genuine state-generated fold. The per-step body bottoms out in the opaque `Solve(mesh)` driver call. (AMR currently unsupported for transient, `:157-162`.)
```

Note: the `state-generated` witness count goes 1 → 2 (the fold-witness numbering keeps witness 1 = transient fixed-schedule, witness 2 = SweepAdaptive; the new block is labelled "Fold witness 3" as the 3rd overall fold witness and the 2nd *state-generated* witness). The §Status "Scope" sentence ("witnessed by transient + driven-PROM SweepAdaptive (2-of-5 pipelines)") is left unchanged on purpose — the AMR loop is a `BaseSolver`-level driver-agnostic sweep that wraps *any* of the 4 AMR-eligible solve pipelines (it is not a 3rd pipeline). See §Open questions for why I did not edit the Scope pipeline-count sentence.

## Discipline notes

- **Pure strengthening, no status/signature/law change.** Per the dispatch scope, this is a fold-in confirming the `schedule-source` axis is genuinely multi-witness on the state-generated side. The entry stays `firm`; the signature default surface stays `fixed-list`; no law text changed. The 4 edits are additive citation surface only.
- **High→low discipline preserved.** All additions narrate the L4 fold combinator's state-generated schedule-source value and cite the L0 witness; no reverse (L0→L4 lift) prose entered the chapter body. The AMR loop's structural role (state-generated schedule) is described in the entry's own L4 vocabulary (carry-threaded fold, state-derived loop bound + per-step input), citing L0 for the witness.
- **The genuine termination criterion is the error-indicator state, not the resource caps.** `ExhaustedResources` (`:181-186`) holds `it >= max_it` (`:182`) and `ntdof > max_size` (`:184`) as resource ceilings; but the load-bearing `state-generated` property is the `err >= refinement.tol` continuation (`:190`) with `err` recomputed each iteration from the new solve (`:267`). I cited all three so the state-generated claim is anchored on the error-indicator criterion specifically (the resource caps alone would be a *bounded* schedule, not a state-generated one — it is the error-indicator recomputation that makes it state-generated).
- **2-witness datapoint for the standing OQ.** This is the requested datapoint for `fold-solve-greedy-schedule-source-generalization` (batch-23 meta-phase input): the state-generated schedule-source value now has 2 independent witnesses (SweepAdaptive greedy frequency-sampling + AMR mesh-refinement). Per the dispatch instruction I record the 2-witness count but do NOT propose the dedicated combinator — that is a future mine once a 3rd state-generated witness or a clear simplification appears. I updated the OQ reference in §Variant axes from "(batch-18)" to "(batch-18+)" to reflect that it remains open (a one-token bounded touch, not a re-architecture).
- **Citation self-verification (all on-disk via `tools/citecheck/citecheck.py --anchor`):** every `basesolver.cpp` pinpoint was confirmed against on-disk source before emit — `:153` (`SolveEstimateMarkRefine`, range END `:276` confirmed by direct `read_range` as the closing `}`), `:174`/`:266` (`Solve(mesh)`), `:175`/`:267` (`Norml2`), `:182` (`refinement.max_it`), `:184` (`refinement.max_size`), `:190` (`while (use_amr`), `:239` (`GeneralRefinement`). All `[ok]`. The codemap `get_symbol_def` reported the same `153-276` range; END-line `}` confirmed by direct read (the FE-source close-brace drift class does not apply here — `:276` is a single-line `}` confirmed on disk).

## Supporting evidence

- `book/src/L4/fold_solve.md` — the entry being strengthened (the `schedule-source` variant axis at frontmatter `:12`, §Variant axes item 1 `:150`, §Specializations `:115-117`, §Evidence `:175-176`).
- `reference/palace/palace/drivers/basesolver.cpp:153-276` — `BaseSolver::SolveEstimateMarkRefine`, the AMR Solve→Estimate→Mark→Refine loop (the 2nd state-generated witness).
- Provenance: thread-opened c057 D4 (1 witness), firmed c058 D1 (2 fold witnesses, 1 state-generated); this dispatch (c073 D6) adds the 2nd state-generated witness.

## Open questions / caveats

- **§Status "Scope" pipeline-count sentence intentionally NOT edited.** The Scope sentence says `fold_solve` is "witnessed by transient + driven-PROM SweepAdaptive (2-of-5 pipelines, the two state-threaded sweeps)". The AMR loop is **not a 3rd pipeline** — `SolveEstimateMarkRefine` is a `BaseSolver`-level driver-agnostic outer wrapper that itself calls the per-driver `Solve(mesh)` (it wraps electrostatic / magnetostatic / eigenmode / driven, NOT transient which is excluded `:157-162`). So editing the "2-of-5 pipelines" count would be wrong (AMR is a cross-driver sweep, not a pipeline). The witness count that genuinely moves is the **`schedule-source = state-generated` value's witness count (1 → 2)**, which is what this report's edits touch. If a reviewer wants the §Status text to also call out the AMR witness, that is a judgment beyond this surgical fold-in — flag for the owner. I deliberately left §Status unchanged to keep the edit surgical and avoid a count-ownership conflict.
- **The `fold-solve-greedy-schedule-source-generalization` OQ now has its 2nd state-generated witness** (datapoint recorded, combinator NOT proposed per scope). The remaining trigger for the dedicated-combinator mine is a 3rd state-generated witness OR a clear simplification a downstream PROM/AMR consumer pulls on. I am appending this 2-witness datapoint to `scaffolding/open-questions.md` (see below). NOTE: the two state-generated witnesses differ in carry shape (SweepAdaptive: growing reduced basis + error history; AMR: refined mesh + error indicators) and per-step body (PROM greedy sample vs. mesh refinement + full re-solve) — the shared invariant is purely the *state-generated schedule-source* property (next input + loop bound both derived from accumulated error state). Whether that shared property alone (across two otherwise-divergent carries) justifies a dedicated combinator, or remains a variant-axis value of `fold_solve`, is the open mine question — leaning toward variant-axis-value (the divergent carries argue against a single dedicated combinator), but that is the future mine's call.

### Open-questions ledger append (proposed; I will append to scaffolding/open-questions.md)

A datapoint append (not a new question) under the existing `fold-solve-greedy-schedule-source-generalization` entry:

> **2026-06-03 (cycle-073 D6 lifter):** 2nd state-generated `schedule-source` witness landed — the AMR `BaseSolver::SolveEstimateMarkRefine` loop (`basesolver.cpp:153-276`, error-indicator-terminated `:190`/`:267`, state-derived per-step refined-mesh input `:239`). The `state-generated` schedule-source value now has 2 independent witnesses (SweepAdaptive greedy frequency-sampling + AMR mesh-refinement). Combinator NOT proposed (per c073 D6 scope) — the two witnesses have divergent carry shapes (reduced-basis+error-history vs. refined-mesh+error-indicators) and divergent per-step bodies (PROM sample vs. mesh-refine + full re-solve); the shared invariant is only the state-generated schedule-source property. Dedicated-combinator mine deferred pending a 3rd witness or a downstream-consumer simplification.
