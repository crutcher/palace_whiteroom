---
agent: layer-intro-author
invoked_at: 2026-05-31T235349Z
scope: L3 index refresh — §Semantics-overlay taxonomy + §Working-Notes consolidated count tally (cycle-040 D2; SOLE OWNER of the L3/index count this cycle)
status: integrated
integrated_at: 2026-06-01T010000Z
integration_commit: 26b58f6
integration_notes: "Applied clean (D2/3, cycle-040). 4 surgical edits to book/src/L3/index.md — §Semantics-overlay shape (e) orthogonalize variant-conditional + fused-composite-obstruction-free (normalize) fold; c024/c037 snapshot relabels SUPERSEDED; c039 bullet rewrite + NEW cycle-040 authoritative tally 15 firm + 3 partial-obstruction. SOLE count-owner; internal consistency VERIFIED across dep-map / §Working-Notes / §Semantics-overlay (partial-obstruction rows chebyshev/eigsolve/orthogonalize). DISCHARGED 2 OQs. count-ownership partition held cleanly, no parallel-blind divergence."
inputs:
  - book/src/L3/index.md (current state — 15 firm + 2 partial-obstruction, four obstruction shapes (a)/(b)/(c)/(d))
  - reports/2026-05-31T235349Z-cycle-040-harvester-orthogonalize-L3/CYCLE.md (D1 — the third partial-obstruction, orthogonalize, `## Status: partial-obstruction`, deferred the tally to me)
  - .claude/agents/layer-intro-author.md §"count-ownership convention" + §"survey chapter firmness from on-disk ## Status"
  - cycle-039 meta-phase count-ownership convention (friction-ledger `parallel-blind-shared-index-count-divergence`)
  - OQ l3-index-fifth-obstruction-profile-fused-composite-obstruction-free (touch (a))
  - OQ l3-index-working-notes-stale-snapshot-compaction-candidate (touch (b))
  - OQ l3-cohort-growth-audit-c036-verdict (the (B) substantive partial-obstruction follow-on now landing)
---

# CYCLE: L3 index refresh — semantics-overlay taxonomy + consolidated count tally

## Summary

Cycle-040 D2 (wave-2). This dispatch refreshes the **§Semantics (overlay)** taxonomy and the
**§Working Notes** consolidated count in `book/src/L3/index.md` to account for D1's wave-1 landing
of `orthogonalize` as the **third L3 `partial-obstruction`** operator, and folds three
co-scheduled index touches. I am the **sole owner of the L3/index consolidated count tally this
cycle** per the cycle-039 count-ownership convention (D1 appended only its own dep-map row +
SUMMARY registration and deferred the tally entirely to me — confirmed in D1's §"Count tally
deferred" caveat).

Three changes, all surgical edits to `book/src/L3/index.md`:

1. **§Semantics-overlay taxonomy** — extend the obstruction-profile enumeration to add
   `orthogonalize` as a **NEW firm/partial-obstruction shape (e)**: a numerical-stability-rooted
   `partial-obstruction` sibling of `chebyshev`, but **distinguished by being
   variant-axis-conditional** (the obstruction lives on the MGS branch of the `gs_orthog` axis
   only; CGS/CGS2 lift cleanly to batched global statements). Neither precedent
   (`chebyshev`/`eigsolve`, both **unconditional**) exhibits a variant-conditional obstruction.
   Also fold the **fifth obstruction profile** `fused-composite-obstruction-free` (`normalize`
   exemplar — a fused composite carrying NO obstruction because both constituents `nrm2`+`scal`
   are individually obstruction-free; the cleanest end of the spectrum) as a one-clause mention
   in the taxonomy. (Closes OQ `l3-index-fifth-obstruction-profile-fused-composite-obstruction-free`.)
   This raises the §Semantics-overlay from **four** non-trivial obstruction shapes (a)/(b)/(c)/(d)
   to **five** (a)/(b)/(c)/(d)/(e) — adding shape (e), `orthogonalize`'s variant-conditional
   partial-obstruction — and additionally names the two obstruction-free-end profiles explicitly
   (the leaf / single-field-op gates, and the new `fused-composite-obstruction-free` profile).

2. **§Working-Notes stale-snapshot compaction** — relabel the older c024/c037/c039 inline
   running-count snapshots so the live count is unambiguous (the c024 "9 firm + 2", c037 "11 firm
   + 2", c039 "15 firm + 2" snapshots are now superseded; the live count is the single authoritative
   tally in change 3). The c024/c037 relabels are Change 2; the c039 relabel is folded into Change
   3's whole-bullet rewrite. (Closes OQ `l3-index-working-notes-stale-snapshot-compaction-candidate`.)

3. **COUNT-OWNERSHIP — the single authoritative consolidated tally** — author the cycle-040
   §Working-Notes bullet establishing **15 firm + 3 partial-obstruction** (the post-cohort total,
   accounting for D1's `orthogonalize` landing), and explicitly mark it as the sole authoritative
   count, with the prior per-cycle snapshots labelled superseded narrative.

## Supporting evidence

- **On-disk status survey (per role-spec "survey chapter firmness from the on-disk `## Status`").**
  D1's report authors `book/src/L3/orthogonalize.md` with `## Status: partial-obstruction`
  (D1 CYCLE.md, the `## Status` section: "`partial-obstruction` — the **per-step body** … lifts
  cleanly … the **MGS `j`-loop is a witnessed `sequential-obstruction`**"). The file does not yet
  exist on disk (D1's proposed-changes block creates it at integration); per the count-ownership
  instruction in my dispatch prompt and D1's explicit deferral, I take the status from D1's landed
  `## Status` line as authorised. The `[orthogonalize](./orthogonalize.md)` link resolves at
  integration when D1's file lands (both in D1's dep-map row and in my taxonomy prose).
- **Pre-D1 on-disk count (verified against `book/src/L3/index.md:59`, the current single
  authoritative tally bullet):** "L3 firm-operator count is now 15 firm + 2 `partial-obstruction`"
  — firm: `krylov-step` c010; `apply_linop` + `axpy` + `axpby` + `axpbypcz` + `dot` + `nrm2` +
  `scal` c011; `ksp_solve` c020; `assemble-diagonal` + `jacobi-smoother` c037; `reciprocal` +
  `elementwise_product` + `divfree-projector` c038; `normalize` c039 (15 firm) — partial-obstruction:
  `chebyshev` c013, `eigsolve` c024 (2 partial-obstruction).
- **Post-cohort count:** the firm cohort is unchanged at **15** (`orthogonalize` lands
  `partial-obstruction`, not firm — it does not increment the firm count); the partial-obstruction
  cohort goes **2 → 3** (`+ orthogonalize` c040). Post-cohort total: **15 firm + 3
  partial-obstruction**.
- **Obstruction-profile placement.** D1's `orthogonalize` is a **numerical-stability-rooted**
  `partial-obstruction` (the MGS serial recurrence buys roundoff-orthogonality the batched CGS
  form loses) — the same obstruction *root* as `chebyshev`, distinct from `eigsolve`'s
  opaque-library root. Its distinguishing feature (per D1 §Status + §Open-questions) is that the
  obstruction is **variant-conditional**: present on the MGS branch of the `gs_orthog` axis,
  absent on CGS/CGS2 (which lift to batched `H = Vᴴw` / `w' = w − VH` global statements). This is
  a genuinely new partial-obstruction sub-shape neither precedent exhibits — both `chebyshev` and
  `eigsolve` carry **unconditional** obstructions. It slots into the taxonomy as shape (e),
  between (b) `chebyshev` (unconditional numerical-stability) and (c) `eigsolve` (unconditional
  opaque-library).
- **Fifth obstruction profile (touch (a)).** `normalize` (c039, firm) is the
  `fused-composite-obstruction-free` exemplar: a fused composite (`nrm2 + scal`) carrying NO
  obstruction because both constituents are individually obstruction-free (`nrm2` is the clean
  parallel reduction; `scal` is embarrassingly parallel). It sits at the **cleanest end** of the
  obstruction-profile spectrum (cleaner even than the leaf gates, because it is a composite that
  nonetheless authors no loop) and is currently absent from the §Semantics-overlay enumeration.
  Folding it as a one-clause mention completes the spectrum's obstruction-free end.

## Proposed changes

### Change 1 — §Semantics-overlay obstruction-profile taxonomy

Adds `orthogonalize` as shape (e) (variant-conditional partial-obstruction) and folds the
`fused-composite-obstruction-free` (`normalize`) fifth profile as a one-clause mention. The
verbatim `[old]` is the current §Semantics "Sequential obstructions" bullet
(`book/src/L3/index.md:15`).

```edit:book/src/L3/index.md
[old]: - **Sequential obstructions** — explicit markers where global form is unavailable, with reason (recorded per `concepts/sequential-obstruction.md`). Four firm shapes coexist at L3, arranged on an obstruction-profile spectrum from obstruction-free through obstruction-carrying to obstruction-authoring: (a) the outer loop folding `krylov-step` does not lift, but the firm driver [`ksp_solve`](./ksp_solve.md) still renders it as an explicit value-threaded tail recursion (firm cycle-020); (b) [`chebyshev`](./chebyshev.md) (`partial-obstruction`) — body lifts, but the inner `k`-recurrence + outer `pc_it` Richardson sweep are sequential obstructions rooted in **numerical stability**; (c) [`eigsolve`](./eigsolve.md) (`partial-obstruction`) — body lifts, but the eigen-iteration loop is a sequential obstruction rooted in **opaque-library-ownership** (Palace authors no loop — SLEPc `EPSSolve` / ARPACK RCI own it); (d) [`divfree-projector`](./divfree-projector.md) (`firm`, cycle-038) — **obstruction-carrying by reference**: its own apply is a fixed obstruction-free straight-line composition (`WeakDiv → Z_{bdr_eff} → ksp_solve → Grad`) authoring no loop of its own, but step 3 invokes the firm-L3 [`ksp_solve`](./ksp_solve.md) whose outer fold IS a sequential-obstruction; the gate neither introduces a new obstruction nor erases the inner one (the inner iteration stays interior to `ksp_solve` per the [`nested-constructed-operator-gate`](../concepts/nested-constructed-operator-gate.md) fidelity rule). Profile (d) sits in the spectrum **between** the obstruction-free constructed-operator gates / leaves ([`jacobi-smoother`](./jacobi-smoother.md), [`apply_linop`](./apply_linop.md) — apply is a single field op, no loop of any kind) and the obstruction-authoring gates ((a)/(c): `ksp_solve` authors its own fold, `eigsolve` delegates to an opaque library loop). (b) and (c) are the two `partial-obstruction` operators (body-lifts-loop-doesn't, differing only in the *reason* the loop does not lift: numerical stability vs. opaque-library-ownership); (a) and (d) are `firm` (their obstruction is rendered or carried-by-reference, not a partial lift). The distinction across (a)/(c)/(d) is *whose* loop carries the obstruction: `ksp_solve` its own, `eigsolve` an opaque library's, `divfree-projector` its inner `ksp_solve` gate's.
[new]: - **Sequential obstructions** — explicit markers where global form is unavailable, with reason (recorded per `concepts/sequential-obstruction.md`). The L3 cohort arranges on an **obstruction-profile spectrum** from obstruction-free, through obstruction-carrying, to obstruction-authoring. At the **obstruction-free end** sit (i) the leaf / single-field-op gates ([`jacobi-smoother`](./jacobi-smoother.md), [`apply_linop`](./apply_linop.md), [`dot`](./dot.md), [`scal`](./scal.md), [`reciprocal`](./reciprocal.md), [`elementwise_product`](./elementwise_product.md) — apply is a single field op, no loop of any kind) and (ii) the **fused-composite-obstruction-free** profile, exemplified by [`normalize`](./normalize.md) (`firm`, cycle-039) — a fused composite (`nrm2 + scal`) that authors no loop because **both constituents are individually obstruction-free** (the `nrm2` reduction is parallel-clean, the `scal` rescale is embarrassingly parallel); the cleanest end of the spectrum (a composite, yet obstruction-free). Five non-trivial-obstruction shapes then coexist: (a) the outer loop folding `krylov-step` does not lift, but the firm driver [`ksp_solve`](./ksp_solve.md) still renders it as an explicit value-threaded tail recursion (firm cycle-020); (b) [`chebyshev`](./chebyshev.md) (`partial-obstruction`) — body lifts, but the inner `k`-recurrence + outer `pc_it` Richardson sweep are sequential obstructions rooted in **numerical stability**, **unconditional** (holds for every parameter value); (c) [`eigsolve`](./eigsolve.md) (`partial-obstruction`) — body lifts, but the eigen-iteration loop is a sequential obstruction rooted in **opaque-library-ownership** (Palace authors no loop — SLEPc `EPSSolve` / ARPACK RCI own it), also **unconditional**; (d) [`divfree-projector`](./divfree-projector.md) (`firm`, cycle-038) — **obstruction-carrying by reference**: its own apply is a fixed obstruction-free straight-line composition (`WeakDiv → Z_{bdr_eff} → ksp_solve → Grad`) authoring no loop of its own, but step 3 invokes the firm-L3 [`ksp_solve`](./ksp_solve.md) whose outer fold IS a sequential-obstruction; the gate neither introduces a new obstruction nor erases the inner one (the inner iteration stays interior to `ksp_solve` per the [`nested-constructed-operator-gate`](../concepts/nested-constructed-operator-gate.md) fidelity rule); (e) [`orthogonalize`](./orthogonalize.md) (`partial-obstruction`, cycle-040) — body lifts (one `dot` + one `axpy` against whole tensors, all variants), but the obstruction is **variant-conditional**: the MGS branch of the `gs_orthog` axis is a Palace-authored per-column `sequential-obstruction` rooted in **numerical stability** (the serial rank-1-projector chain buys the roundoff-orthogonality the batched form loses), while the CGS/CGS2 branches **lift cleanly** to the batched global statements `H = Vᴴw` / `w' = w − VH`. Shape (e) is the **variant-conditional** sibling of (b): same numerical-stability obstruction *root* as `chebyshev`, but where (b)/(c) carry **unconditional** obstructions (every parameter value), (e)'s partial-obstruction verdict **splits along the `gs_orthog` variant axis** — a structure neither (b) nor (c) exhibits. (b), (c), and (e) are the three `partial-obstruction` operators (body-lifts-loop-doesn't); (a) and (d) are `firm` (their obstruction is rendered or carried-by-reference, not a partial lift). The distinction across the obstruction-carrying shapes is *whose* loop carries the obstruction and on *what condition*: `ksp_solve` its own (always), `eigsolve` an opaque library's (always), `divfree-projector` its inner `ksp_solve` gate's (by reference), `chebyshev` its own recurrence/sweep (always, numerical-stability), `orthogonalize` its own MGS column-recurrence (**only on the MGS variant**, numerical-stability).
```

### Change 2 — §Working-Notes stale-snapshot compaction (c024 / c037 bullets)

Relabels the c024 / c037 inline running-count snapshots as **superseded** so the live count
is unambiguous (they remain as per-cycle landing narrative; the live count lives in the single
authoritative tally, change 3). Two surgical edits, one per stale snapshot's count clause. (The
c039 snapshot's compaction is folded into Change 3's whole-bullet rewrite, since Change 3 already
rewrites the c039 bullet — keeping it one edit avoids two sequential edits against the same source
bullet.)

The c024 bullet's trailing count snapshot (`book/src/L3/index.md:56`):

```edit:book/src/L3/index.md
[old]: This is the cycle-021 OQ `l3-eigsolve-linear-evp-has-no-krylov-step-kernel-analog` prediction enacted. **L3 firm-operator count now 9 firm + 2 `partial-obstruction`** (firm: `krylov-step` c010, `apply_linop` + `axpy` + `axpby` + `axpbypcz` + `dot` + `nrm2` + `scal` c011, `ksp_solve` c020; partial-obstruction: `chebyshev` c013, `eigsolve` c024).
[new]: This is the cycle-021 OQ `l3-eigsolve-linear-evp-has-no-krylov-step-kernel-analog` prediction enacted. *(Count snapshot at cycle-024: 9 firm + 2 `partial-obstruction` — **SUPERSEDED**; the live authoritative count is the cycle-040 tally below. This snapshot is kept only as the cycle-024 landing narrative.)*
```

The c037 bullet's trailing count snapshot (`book/src/L3/index.md:57`):

```edit:book/src/L3/index.md
[old]: Both are layer-coherence backfills per **Identity-lowerings still require both L levels**; the substantive rotations live at L1>L0 (`assemble-diagonal-mutation-rotation`, `jacobi-smoother-mutation-rotation` + `reciprocal-elementwise-product-mutation-rotation` sub-pattern B). **L3 firm-operator count now 11 firm + 2 `partial-obstruction`** (firm: `krylov-step` c010, `apply_linop` + `axpy` + `axpby` + `axpbypcz` + `dot` + `nrm2` + `scal` c011, `ksp_solve` c020, `assemble-diagonal` + `jacobi-smoother` c037; partial-obstruction: `chebyshev` c013, `eigsolve` c024). Four of the six (A) backfills remained after cycle-037 (`reciprocal`, `elementwise_product`, `normalize`, `divfree-projector`) under OQ `l3-cohort-growth-audit-c036-verdict` — three of which land cycle-038 (next bullet).
[new]: Both are layer-coherence backfills per **Identity-lowerings still require both L levels**; the substantive rotations live at L1>L0 (`assemble-diagonal-mutation-rotation`, `jacobi-smoother-mutation-rotation` + `reciprocal-elementwise-product-mutation-rotation` sub-pattern B). *(Count snapshot at cycle-037: 11 firm + 2 `partial-obstruction` — **SUPERSEDED**; the live authoritative count is the cycle-040 tally below. This snapshot is kept only as the cycle-037 landing narrative.)* Four of the six (A) backfills remained after cycle-037 (`reciprocal`, `elementwise_product`, `normalize`, `divfree-projector`) under OQ `l3-cohort-growth-audit-c036-verdict` — three of which land cycle-038 (next bullet).
```

### Change 3 — §Working-Notes c039 bullet rewrite + cycle-040 consolidated tally (the single authoritative count; SOLE OWNER this cycle)

A **single whole-bullet replacement** of the c039 §Working-Notes bullet (`book/src/L3/index.md:59`)
that (i) strips its self-claimed "single authoritative count tally" label and demotes its count
clause to a SUPERSEDED snapshot (the touch (b) compaction of the c039 snapshot — folded into this
one edit rather than a separate one to avoid two sequential edits against the same source bullet),
and (ii) appends the new cycle-040 tally bullet establishing the post-cohort **15 firm + 3
partial-obstruction** count as the single authoritative tally. The `[old]` is the entire c039
bullet verbatim.

```edit:book/src/L3/index.md
[old]: - **`normalize` landed cycle-039 — the c036 (A) cohort closer; consolidated L3 firm-count tally**: [`normalize`](./normalize.md) (the fused `nrm2 + scal` whole-tensor self-map) lands firm cycle-039, the **sixth and final (A) identity-in-form backfill** of the cycle-036 D2 audit verdict (`book/src/L3/index.md:44`), closing the **c036 (A) cohort at 6-of-6**: `assemble-diagonal` (c037) + `jacobi-smoother` (c037) + `reciprocal` (c038) + `elementwise_product` (c038) + `divfree-projector` (c038) + `normalize` (c039) all landed under OQ `l3-cohort-growth-audit-c036-verdict` (now CLOSED for the (A) cohort). **L3 firm-operator count is now 15 firm + 2 `partial-obstruction`** — firm: `krylov-step` c010; `apply_linop` + `axpy` + `axpby` + `axpbypcz` + `dot` + `nrm2` + `scal` c011; `ksp_solve` c020; `assemble-diagonal` + `jacobi-smoother` c037; `reciprocal` + `elementwise_product` + `divfree-projector` c038; `normalize` c039 — partial-obstruction: `chebyshev` c013, `eigsolve` c024. This bullet is the **single authoritative count tally**; the per-cycle running counts in the c024/c037/c038 bullets above are superseded snapshots (kept as narrative of what landed each cycle, not as the live count). The §Semantics-overlay taxonomy now enumerates **four** firm obstruction shapes (a)/(b)/(c)/(d), the fourth being `divfree-projector`'s obstruction-carrying-by-reference profile (OQ `l3-index-fourth-obstruction-profile-obstruction-carrying-by-reference` RESOLVED).
[new]: - **`normalize` landed cycle-039 — the c036 (A) cohort closer**: [`normalize`](./normalize.md) (the fused `nrm2 + scal` whole-tensor self-map) lands firm cycle-039, the **sixth and final (A) identity-in-form backfill** of the cycle-036 D2 audit verdict (`book/src/L3/index.md:44`), closing the **c036 (A) cohort at 6-of-6**: `assemble-diagonal` (c037) + `jacobi-smoother` (c037) + `reciprocal` (c038) + `elementwise_product` (c038) + `divfree-projector` (c038) + `normalize` (c039) all landed under OQ `l3-cohort-growth-audit-c036-verdict` (now CLOSED for the (A) cohort). *(Count snapshot at cycle-039: 15 firm + 2 `partial-obstruction` — **SUPERSEDED** by the cycle-040 tally below, which adds `orthogonalize` as the third `partial-obstruction`. Kept only as the cycle-039 landing narrative: it closed the c036 (A) cohort at 6-of-6. The §Semantics-overlay taxonomy at this point enumerated **four** non-trivial obstruction shapes (a)/(b)/(c)/(d); cycle-040 raises it to five — OQ `l3-index-fourth-obstruction-profile-obstruction-carrying-by-reference` remains RESOLVED.)*
- **`orthogonalize` landed cycle-040 — the first (B) substantive partial-obstruction; THIS BULLET IS THE SINGLE AUTHORITATIVE L3 COUNT TALLY**: [`orthogonalize`](./orthogonalize.md) (the variant-split Gram-Schmidt orthogonalize-against-basis) lands `partial-obstruction` cycle-040 (D1 wave-1), the **first (B) substantive member** of the cycle-036 D2 audit verdict (`book/src/L3/index.md:47`) to land — distinct from the six (A) identity-in-form backfills (closed c039). It is the **third L3 `partial-obstruction`** after `chebyshev` (c013) and `eigsolve` (c024): the per-step body lifts whole-tensor for all variants and CGS/CGS2 lift entirely to the batched `H = Vᴴw` / `w' = w − VH` global statements, but the **MGS `j`-loop is a witnessed numerical-stability `sequential-obstruction`** — and the distinguishing feature is that the obstruction is **variant-conditional** (present on the MGS branch of the `gs_orthog` axis, absent on CGS/CGS2), so the partial-obstruction verdict **splits along the variant axis**, a structure neither `chebyshev` nor `eigsolve` (both **unconditional**) exhibits. It is enumerated as **shape (e)** in the §Semantics-overlay obstruction-profile spectrum (the variant-conditional sibling of `chebyshev`'s numerical-stability shape (b)). **L3 firm-operator count is now 15 firm + 3 `partial-obstruction`** — firm (15): `krylov-step` c010; `apply_linop` + `axpy` + `axpby` + `axpbypcz` + `dot` + `nrm2` + `scal` c011; `ksp_solve` c020; `assemble-diagonal` + `jacobi-smoother` c037; `reciprocal` + `elementwise_product` + `divfree-projector` c038; `normalize` c039 — partial-obstruction (3): `chebyshev` c013, `eigsolve` c024, `orthogonalize` c040. This bullet is the **single authoritative count tally** (cycle-040 D2 layer-intro-author sole-owned it per the cycle-039 meta-phase count-ownership convention, friction-ledger `parallel-blind-shared-index-count-divergence`; D1 deferred the tally entirely); the per-cycle running counts in the c024/c037/c039 bullets above are **superseded snapshots** (kept as narrative of what landed each cycle, not as the live count). The §Semantics-overlay taxonomy now enumerates **five** non-trivial obstruction shapes (a)/(b)/(c)/(d)/(e) — the fifth being `orthogonalize`'s variant-conditional partial-obstruction profile — plus the obstruction-free-end `fused-composite-obstruction-free` profile (`normalize`), resolving OQ `l3-index-fifth-obstruction-profile-fused-composite-obstruction-free` and OQ `l3-index-working-notes-stale-snapshot-compaction-candidate`.
```

## Open questions / caveats

- **OQ `l3-index-fifth-obstruction-profile-fused-composite-obstruction-free` — CLOSABLE at
  integration.** Change 1 folds `normalize`'s `fused-composite-obstruction-free` profile as a
  one-clause mention at the obstruction-free end of the §Semantics-overlay spectrum (touch (a)).
- **OQ `l3-index-working-notes-stale-snapshot-compaction-candidate` — CLOSABLE at integration.**
  Change 2 relabels the c024/c037 inline count snapshots as SUPERSEDED; Change 3's whole-bullet
  rewrite relabels the c039 snapshot AND establishes the single authoritative cycle-040 tally
  (touch (b) + the count-ownership touch (c)).
- **NEW OQ candidate — `concepts/sequential-obstruction.md` variant-conditional sub-shape.** D1
  flagged (its §Open-questions point 2) that `orthogonalize`'s **variant-conditional**
  partial-obstruction is a genuinely new sub-shape that neither precedent exhibits, and that a
  future cross-cutter may want to **name the variant-split shape explicitly** across the L3
  obstruction taxonomy in `concepts/sequential-obstruction.md`. I have surfaced the shape in the
  §Semantics-overlay taxonomy (shape (e), explicitly tagged "variant-conditional"), but the
  concept-page write-up is **out of scope for this dispatch** (one concept page per invocation;
  this dispatch is the L3 *index* refresh, not the concept page). Recommend the planner route a
  future `layer-intro-author` dispatch (or a `same-layer-cross-cutter` observation) on
  `concepts/sequential-obstruction.md` to add a named "variant-conditional sequential-obstruction"
  sub-shape, citing `orthogonalize` as the canonical instance. Suggested OQ slug:
  `concepts-sequential-obstruction-variant-conditional-sub-shape` (carry-forward of D1's flag).
- **Count-ownership discharged.** I am the sole writer of the L3/index consolidated tally this
  cycle (cycle-040 D2). The post-cohort total (15 firm + 3 partial-obstruction) accounts for BOTH
  my own touches (the taxonomy/snapshot edits, which do not change counts) AND D1's `orthogonalize`
  landing (the +1 partial-obstruction). D1 appended only its dep-map row + SUMMARY registration and
  deferred the tally — confirmed in D1's §"Count tally deferred (count-ownership)" caveat. No
  parallel-blind count divergence: there is exactly one authoritative tally bullet (the cycle-040
  bullet authored in Change 3).
- **No firm-count change.** `orthogonalize` lands `partial-obstruction`, not `firm` — the firm
  count stays at 15. Only the partial-obstruction count increments (2 → 3). The §Vocabulary-cohort
  subsection is not present in this L3 index (the index uses the dep-map table + §Working-Notes
  narrative form); no cohort-split subsection edit is in scope.
- **Link resolution.** The `[orthogonalize](./orthogonalize.md)` live links in my Change 1
  taxonomy prose and Change 3 tally bullet resolve at integration when D1's `book/src/L3/orthogonalize.md`
  file lands (D1's proposed-changes block creates it; the integrator applies D1 and D2 in the same
  cycle, so the anchor exists before `cargo make book` runs). This is the standard same-cycle
  co-landing pattern (precedent: every cohort cycle where the index refresh and the operator entry
  co-land, e.g. c038 `divfree-projector` taxonomy shape (d) + its dep-map row).
