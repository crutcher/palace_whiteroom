# Cycle-017 integrator staging log

Per-report integration staging for cycle-017 (SECOND primary cycle of meta-batch-4).
One section per ready report, dispatched serially by the parent. Newest LAST, append-only.
integrator-finalize reads this log to run the book rebuild, mark `integrated_at`, write
`log/cycle-017.md`, append cycle-record + integrator-signals, update roadmap, and commit.

---

## combinator-miner-linear-combination-fold
applied_at: 2026-05-28T2345Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L2/index.md (edit ×1 — appended one rough-in dep-map row for the new L2 combinator `linear_combination :: [(Scalar, Tensor[N])] -> Tensor[N]` (≡ `foldl (\acc (a,t) -> acc + a·t) zeros pairs`), AFTER the `chebyshev-iteration` row [on-disk line 24, table end]. Dependencies cell lists the four L1 fixed-arity specializations [`scal` arity-1, `axpy` arity-2-coeff-1-fixed, `axpby` arity-2, `axpbypcz` arity-3], the `scalar-promotion` concept [element-type-axis sibling of this arity-axis unification], and the do-NOT-merge sibling fold `dot`. Status cell `(rough-in, proposed-by: combinator-miner:2026-05-28T223022Z)`. Forward-link `./linear_combination.md` is INTENTIONAL and does NOT yet exist — harvester creates the operator file [matches the existing krylov-step/chebyshev-iteration rough-in forward-reference pattern in the same table]. No new operator file created this report. No SUMMARY edit [dep-map row only, no chapter yet]. The OLD anchor string [chebyshev-iteration row + the `## Working Notes` heading immediately below] matched disk byte-for-byte on fresh re-read.)
- scaffolding/open-questions.md (append — 2 NEW OQ blocks before the `## Dropped` section: `linear-combination-harvester-formalization` [the harvester cycle-018+ formalization: author the operator chapter + laws + SUMMARY-register; permutation-invariance as exact-arithmetic law + IEEE non-law; pull test-vector.cpp per-arity assertions for empirical-match; the `L2-L1/linear-combination-fold-specialization` lowering theme is abstractor follow-up; arity>3 upside note] and `inner-product-fold-sibling-candidate` [`dot` is a DIFFERENT reduce-to-scalar fold; a sibling `inner_product` L2 combinator along the conjugation-convention axis is a SEPARATE future combinator-miner candidate; do-NOT-over-unify])

Gate hits:
- forward-edge-without-surface: 0 (this is a combinator/operator dep-map row, not a lowering-theme forward-edge claim; the rough-in carries evidence — the parametric-arity family with four operator-definition instances + ≥6 live L0 call sites — and modifies surface; the row is the surface)
- broken-link / cross-reference-integrity: 0 (the forward-link `./linear_combination.md` is the ESTABLISHED rough-in pattern, NOT a broken-link gate hit — the row intentionally points at a file harvester will create, exactly as the krylov-step/chebyshev-iteration rows forward-referenced their not-yet-firm chapters; all named slugs `scal`/`axpy`/`axpby`/`axpbypcz`/`scalar-promotion`/`dot` resolve; critic cross-reference-integrity = pass)
- edge-label-prose-mismatch: 0 (n/a — combinator/operator proposal, carries no `L_{n+1}>L_n` edge label; the L2>L1 lowering theme is correctly deferred to abstractor per OQ, no edge prose authored)
- H1-page-heading-reuse: 0 (no H1 touched; dep-map table-row append only)
- append-on-missing-slug: 0 (anchored on the existing `chebyshev-iteration` row + `## Working Notes` heading, matched disk byte-for-byte; no slug-create — the row IS the rough-in slug registration, and the operator file is harvester's, correctly absent)
- variant-axis-missing: 0 (n/a — combinator proposal, not a multi-variant operator entry; the report DID enumerate three orthogonal axes [output-aliasing in-place-vs-out-of-place, element-type real|complex w/ scalar-promotion sub-axis, fusion-order L0-detail] and correctly scoped aliasing+fusion-order as L2>L1/L1>L0 lowering concerns and element-type as inherited — critic variant-axis-coverage = pass)
- concept_writes-on-existing-slug: 0 (no concept page touched; `scalar-promotion` referenced as existing, not written)
- retroactive-budget: 0 per-slice / 0 this-report (single new dep-map row append + 2 OQ appends; no retroactive-edit slices)
- index-placeholder-displacement: 0 (the L2 dep-map table is firm content [krylov-step + chebyshev-iteration rows], NOT a `(empty — Phase B skeleton.)` placeholder; clean append-after the last row)
- SUMMARY-registration: n/a (no new chapter created — dep-map rough-in row only; the SUMMARY chapter entry for `linear_combination` lands when harvester creates the operator file, captured in the `linear-combination-harvester-formalization` OQ)
- plan-kind-consistency: 0 (declared kind rough-in; content shape matches — dep-map row only, signature flagged best-guess, formalization deferred; no firm-grade claims smuggled under the rough-in label — critic plan-kind-consistency = pass)
- variant-axis / bookkeeping: complete

Open questions promoted:
- linear-combination-harvester-formalization (NEW — harvester cycle-018+ candidate: author `book/src/L2/linear_combination.md` + laws + SUMMARY-register; permutation-invariance exact-arithmetic-law + IEEE non-law phrasing; pull test-vector.cpp per-arity assertions for the concatenation-law empirical-match; the `L2-L1/linear-combination-fold-specialization` lowering theme is abstractor follow-up; arity>3 upside note. opened_by combinator-miner, status open, non-blocking — folds report §Open questions 2/3/4/5)
- inner-product-fold-sibling-candidate (NEW — `dot` is a DIFFERENT reduce-to-scalar fold; a sibling `inner_product :: (Tensor[N], Tensor[N]) -> Scalar` L2 combinator along the conjugation-convention axis is a SEPARATE future combinator-miner candidate; do-NOT-over-unify into one mega-combinator. opened_by combinator-miner, status open, non-blocking — folds report §Open question 1)

Build-relevant: yes

Notes: Position 1 of 5 (FIRST per-report integrator of cycle-017). Created this staging dir + STAGING.md (template from cycle-016). Single rough-in dep-map row append to `book/src/L2/index.md` enacting the constructive half (prong b) of the human-raised OQ `blas1-variadic-linear-combination-fold-unification` (ledger line 2937, opened cycle-016) — `linear_combination` unifies the BLAS-1 scalar-weighted-sum cohort along the ARITY axis (the `scalar-promotion` concept already unified the same four along the ELEMENT-TYPE axis; this is the complementary arity-axis unification). The overall_status is `ready` (critic 7/8 pass + 1 skill-uptake-survey warning; repairer all not-needed — all flagged items are telemetry/informational, none a repairable defect). **Forward-link discipline:** the row points at `./linear_combination.md` which does NOT yet exist — this is the ESTABLISHED rough-in forward-reference pattern (krylov-step/chebyshev-iteration rows did the same before their chapters firmed), NOT a broken-link gate hit; harvester creates the operator file (tracked in the `linear-combination-harvester-formalization` OQ). **Companion provenance bullet DEFERRED:** the report offered an optional L2 "Working Notes" provenance bullet (mirroring the krylov-step precedent at index.md:30-37) as integrator's-discretion-or-defer-to-harvester — I DEFERRED it to harvester (dispatch scoped this report to the dep-map row only; the harvester-formalization pass is the natural home for the provenance bullet alongside the operator chapter). **Deferred to finalize:** `integrated_at` + `integration_commit` frontmatter on the consumed report (per role-spec / CLAUDE.md write-authority partition — per-report integrator does NOT touch report frontmatter; confirmed neither field present in CYCLE.md or META.md pre-apply). **For finalize / next-cycle planner:** (1) the human-raised parent OQ `blas1-variadic-linear-combination-fold-unification` (ledger 2937) is now half-enacted — prong (b) [dispatch combinator-miner at the BLAS-1 family → propose `linear_combination`] is DONE this report; prong (a) [extend the combinator-miner spec with a parametric/variadic-family detection mode] remains a batch-4 meta-phase item (the skill-uptake-survey warning this report carries is direct telemetry for it — the proposal worked in ad-hoc "explicit parametric-family mode" with no skill invocation cited); I did NOT flip the parent OQ's status (it is human-raised, only half-resolved, and frontmatter `status:` flips are finalize's per the append-only ledger discipline). (2) `linear-combination-harvester-formalization` is the cycle-018+ harvester dispatch; `inner-product-fold-sibling-candidate` is a SEPARATE future combinator-miner family (conjugation-convention axis). Skill-uptake telemetry: the report's shape IS a parametric/variadic-family unification (exactly the gap the parent OQ prong-(a) flags) and references no skill invocation (`classify-variant-axis` for the three-axis enumeration, `propose-rotation`/`verify-rotation-citation` for the rotation sketch all exist and would apply) despite substantive on-merits correctness — surface for the batch-4 meta-phase combinator-miner spec extension as continuing named-skill-by-slug-uptake-weakness telemetry (consistent with the entire cycle-016 batch).

---

## harvester-divfree-l1-citation-fix
applied_at: 2026-05-29T002600Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L1/divfree-projector.md (edit ×11 — eleven surgical citation-line-reference corrections to the FIRM L1 operator entry; 11 insertions / 11 deletions confirmed by `git diff --stat`. (1-4) apply `Mult(VecType &y)` close-brace `divfree.cpp:155-186`→`:155-187` at all FOUR occurrence sites [§Context L14, §Semantics L122, §Status L237, §Evidence L301] — each matched on full-line context since the bare substring recurs 4×; (5) CG rel-tol §Algebraic-laws idempotence caveat `divfree.cpp:140,142` / "rel-tol set at :140"→`:141-142` / "rel-tol set at :141" [`:140` is `SetInitialGuess(false)`, `SetRelTol(tol)` is `:141`, abs-tol `:142` unchanged]; (6) §Signature MPI prose MPI_Comm `divfree.cpp:62`→`:63`; (7) §Context psi/rhs scratch `divfree.hpp:55`→`:54` [`:55` was blank]; (8) §Evidence `class DivFreeSolver` `divfree.hpp:34`→`:33` [`:34` is the `{`]; (9) §Evidence member-fields range `divfree.hpp:40-55`→`:40-54` [tighten to last decl]; (10) §Evidence ksp-solver comment `divfree.hpp:51`→`:50` [`:50` is the quoted comment]; (11) §Context dangling inline pointer `(see Variant axes)`→`(see Signature, the y element type)` [the entry has NO `## Variant axes` heading; matches cycle-016 repairer's resolution of the L1>L0 theme twin pointer]. NO signature/semantics/law/status/variant-axis change; operator stays `firm`. All OLD anchors matched clean cycle-016 HEAD disk state on fresh re-read.)
- scaffolding/open-questions.md (append — one RESOLUTION note after the `divfree-l1-entry-apply-close-and-reltol-line-drift` OQ prose block [ledger ~L2871], recording RESOLVED-IN-FULL by these 11 corrections; append-only, did NOT flip the `status:` YAML field [deferred to finalize per write-authority partition])

Gate hits:
- retroactive-budget: 1-slice (this report) / 1 global — the 11 line-ref edits are all retroactive evidence-correction on ONE firm slice (divfree-projector); per-slice 1 (<3, no block), global 1 (<4, no block). NOTE: integrator-finalize sees the aggregate across all 5 reports — flagging the per-report count here for the global tally.
- concept_writes-on-existing-slug: 0 (no concept page touched)
- forward-edge-without-surface: 0 (no forward-edge claim; pure citation maintenance on existing surface)
- edge-label-prose-mismatch: 0 (n/a — single-layer L1 citation correction, no L_{n+1}>L_n edge label carried)
- H1-page-heading-reuse: 0 (no H1 touched; the `# divfree-projector` heading is untouched)
- append-on-missing-slug: 0 (n/a — no append/slug-create; in-place edits to an existing firm entry)
- variant-axis-missing: 0 (n/a — citation correction, not a new multi-variant operator; the VecType ∈ {Vector, ComplexVector} axis remains documented in §Signature, and correction #11 actually FIXES the dangling pointer that referenced it)
- index-placeholder-displacement: 0 (n/a — no index.md / dep-map touched; the dep-map row is explicitly unchanged per the report)
- SUMMARY-registration: 0 (n/a — no new chapter; the divfree-projector chapter already exists under the L1 Part)
- plan-kind-consistency: 0 at integration (declared kind "surgical citation correction; not re-formalization" matches the applied content shape — status stays firm, no signature/law/semantics change, operator content NOT re-emitted). NOTE the report's META carried a plan-kind-consistency FAIL at critique — but that FAIL was about the DELIVERY MECHANISM (harvester's dispatch-phase in-place `book/` edit), NOT the content; the repairer reverted the leak (`git checkout`) and the content was applied via the CORRECT proposed-changes channel this dispatch. No content-level kind inconsistency.
- broken-link / cross-reference-integrity: 0 (correction #11 RESOLVES a pre-existing dangling `(see Variant axes)` pointer → `(see Signature, ...)`; the `## Signature` heading exists. Net cross-reference-integrity IMPROVED by this apply.)
- variant-axis / bookkeeping: complete

Open questions promoted:
- (none NEW promoted — the report's only OQ action is CLOSING `divfree-l1-entry-apply-close-and-reltol-line-drift`, recorded as a RESOLUTION note in open-questions.md; status-field flip → `resolved` + `last_revisited: cycle-017` deferred to finalize per append-only discipline)

Build-relevant: yes

Notes: Position 2 of 5. **CRITICAL FOR FINALIZE — META-SIGNAL escalation to route to integrator-signals → batch-4 meta-phase:** this report's META.md carries a WRITE-AUTHORITY PHASE-BOUNDARY VIOLATION escalation. The harvester edited `book/src/L1/divfree-projector.md` IN-PLACE during the dispatch phase (Phase 2) — a violation of the CLAUDE.md write-authority partition (specialized dispatch agents write ONLY to `reports/<id>/`). The repairer REVERTED that leak via `git checkout` (META.md repair section, `revert-dispatch-phase-book-mutation` skill Option A; book/ confirmed clean before this dispatch) and set `overall_status: ready`. The 11 corrections survived in the CYCLE.md proposed-changes block and I applied them the CORRECT way this dispatch. **This is the THIRD distinct specialized agent to leak `book/` during dispatch (recurrence-3): cycle-008 `abstractor` → cycle-012 `layer-intro-author` → cycle-017 `harvester`.** The friction-ledger `specialized-agent-direct-write-to-book-during-dispatch` Watch clause fires at recurrence-3. The cycle-012 prompt-guard was applied ONLY to `.claude/agents/layer-intro-author.md`; the harvester (and the other 6 specialized specs) never got it. The META.md §"ESCALATION for the meta-phase" + §META-SIGNAL request the batch-4 meta-phase (after cycle-018) to: (1) enact the prompt-guard across ALL 8 specialized agent specs, (2) re-weigh an `integrator-per-report`/pre-dispatch clean-tree gate as a structural backstop. **integrator-finalize: carry this META-SIGNAL verbatim into `scaffolding/integrator-signals.md` so the recurrence-3 escalation reaches the batch-4 meta-phase.** The verbatim repairer META-SIGNAL line: `write-authority-phase-boundary-violation reverted (Option A); harvester wrote 1 file (book/src/L1/divfree-projector.md, 11 ins / 11 del) to book/ during dispatch; restored to HEAD; integrator-per-report applies normally from the CYCLE.md proposed-changes block (8 distinct [old] anchors, all present + full-line-unique in HEAD).` **Deferred to finalize:** (a) `integrated_at` + `integration_commit` frontmatter on the consumed report (per write-authority partition — per-report integrator does NOT touch report frontmatter; confirmed neither field present pre-apply); (b) formal OQ `status:` flip → `resolved` + `last_revisited: cycle-017` on the `divfree-l1-entry-apply-close-and-reltol-line-drift` YAML block (ledger ~L2862; I appended a RESOLUTION note, did not edit the YAML). **For finalize / next-cycle planner:** the out-of-OQ-scope `arpack.cpp`/`slepc.cpp` driver call-site line lists (§Evidence L306-308) were NOT re-audited (the report correctly scoped them out — a separate dispatch if drift is suspected in the dozen+ Krylov-kernel projection sites). Sibling open OQs on this same firm L1 entry remain OPEN (informational): `divfree-mult-doc-irrotational-vs-divfree-stale` (Palace-internal stale-comment inconsistency, NOT a defect), `divfree-closure-nesting-constructed-gate-carrying-constructed-gate` (structural cross-cutter note candidate).

---

## lifter-l3-l2-body-identity-cg-sweep
applied_at: 2026-05-29T010500Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L3-L2/krylov-step-body-identity.md (edit ×3 — three surgical citation-provenance re-anchors to the FIRM L3>L2 theme; 3 insertions / 3 deletions confirmed by `git diff --stat`. The theme's own dangling `cg.md` provenance pointer is re-anchored at all three sites to the in-document terminal firm home — THIS theme's OWN §Verified-against bullet 1, which physically carries the verbatim cycle-002 combinator-miner Claim-2 quote — with the dead slice ranges demoted to parenthetical "original pre-reduction slice range … lifted here per the cycle-009 corpus reduction" provenance annotations (cycle-013/014/015/016 convention). (1) §Applicability-conditions condition 3 [line 97], `cg.md:351-362` → "preserved verbatim in this theme's §Verified-against bullet 1 — the terminal firm home; original pre-reduction slice range `cg.md:351-362`, lifted here per the cycle-009 corpus reduction". (2) §Justification-kind empirical-match [line 107], `cg.md:341-362` → "the CG body-identity claim, now preserved verbatim in this theme's §Verified-against bullet 1 … original pre-reduction slice range `cg.md:341-362`, lifted there per the cycle-009 corpus reduction — and the live anchor `arnoldi_step.md:178-213`". (3) §Verified-against bullet 1 [line 125], `cg.md:341-362` → declared the terminal firm home (verbatim quote physically present), upstream `L4-L3/krylov-step-typed-wrapper-dissolution.md` + `L3/krylov-step.md:188` both named as cross-chain consensus pointing here, slice range demoted to a trailing parenthetical + "the `cg.md` slice is now a 165-line stub and that range no longer resolves". NO body-shape / signature / decomposition / status / variant-axis change; theme stays `firm`. The co-pointer `gmres.md:459-471` [§Verified-against bullet 4, live 671-line slice] and `arnoldi_step.md:178-213` / `:185-188` [live 302-line slice] correctly left untouched — out of scope for a cg.md sweep. All 3 OLD anchor strings matched disk byte-for-byte on fresh re-read (no in-cycle file overlap — reports 1+2 touched `book/src/L2/index.md` + `book/src/L1/divfree-projector.md`).)
- scaffolding/open-questions.md (append — one RESOLUTION note after the `l3-l2-body-identity-cg-md-citation-sweep` OQ prose block [ledger ~L2912], recording RESOLVED-IN-FULL by these 3 re-anchors; append-only, did NOT flip the `status:` YAML field [deferred to finalize per write-authority partition])

Gate hits:
- retroactive-budget: 1-slice (this report) / 2 global — the 3 citation re-anchor edits are all retroactive provenance-correction on ONE firm slice (krylov-step-body-identity); per-slice 1 (<3, no block), global tally now 2 (report-2 contributed 1 on divfree-projector + this report 1 on krylov-step-body-identity; <4, no block). NOTE: integrator-finalize sees the aggregate across all 5 reports — flagging the per-report + running-global count here.
- concept_writes-on-existing-slug: 0 (no concept page touched; `state-stratification`/`derived-view-hoisting`/etc. referenced as existing, unchanged)
- forward-edge-without-surface: 0 (no forward-edge claim; pure citation-provenance maintenance on the existing firm-theme surface)
- edge-label-prose-mismatch: 0 (the L3>L2 edge label is intact throughout; prose narrates the same L3→L2 body-identity edge; no edge-label or direction change)
- H1-page-heading-reuse: 0 (no H1 touched; the `# krylov-step-body-identity` heading is untouched)
- append-on-missing-slug: 0 (n/a — no append/slug-create; in-place edits to an existing firm theme entry)
- variant-axis-missing: 0 (n/a — citation re-anchor, not a new multi-variant operator; the §Applicability-conditions condition 4 six-axis profile is unchanged, and condition 3's variant-axis-orthogonal content is untouched in substance)
- index-placeholder-displacement: 0 (n/a — no index.md / dep-map touched)
- SUMMARY-registration: 0 (n/a — no new chapter; the krylov-step-body-identity chapter already exists under the L3-L2 Part)
- plan-kind-consistency: 0 (declared kind "pure provenance-pointer re-anchor; no body-shape/signature/decomposition/status change" matches the applied content shape exactly — three `edit:` blocks swapping a dangling `cg.md` range for the in-document terminal-home reference + parenthetical original-range note; status correctly held `firm`; critic plan-kind-consistency = pass)
- broken-link / cross-reference-integrity: 0 (the re-anchor is the load-bearing critic check = pass; the "terminal home is this entry's OWN §Verified-against" is NOT a self-referential dangle — the bullet holds the verbatim Claim-2 quote at line 125, genuine terminal content, and both `L3/krylov-step.md:188` + `L4-L3/krylov-step-typed-wrapper-dissolution.md:204` independently designate this §section as the lifted-in home; this sweep terminates the SECOND of the two hops the cycle-016 position-2 resolution flagged as two-hop-to-dangle)
- variant-axis / bookkeeping: complete

Open questions promoted:
- (none NEW promoted — the report's only OQ action is CLOSING `l3-l2-body-identity-cg-md-citation-sweep`, recorded as a RESOLUTION note in open-questions.md; status-field flip → `resolved` + `last_revisited: cycle-017` deferred to finalize per append-only discipline)

Build-relevant: yes

Notes: Position 3 of 5. Pure citation-provenance hygiene sweep on the FIRM L3>L2 theme `book/src/L3-L2/krylov-step-body-identity.md` — three dangling `cg.md:341-362` / `:351-362` provenance pointers (lines 97/107/125) re-anchored to the in-document terminal firm home (the theme's OWN §Verified-against bullet 1, which physically carries the verbatim cycle-002 combinator-miner Claim-2 quote) with the dead slice ranges demoted to parenthetical original-range annotations (cycle-013/014/015/016 lifted-evidence convention). This terminates the SECOND of the two hops the cycle-016 integrator-per-report position-2 resolution recorded as "two-hop-to-dangle" (`L2/krylov-step.md:138` already swept cycle-016; `L3-L2/krylov-step-body-identity.md:125` swept now) — the cg.md re-anchor chain through the krylov-step lowering stack is now fully terminated for this theme. **NOT a self-referential dangle** (critic's load-bearing finding): the §Verified-against bullet holds genuine terminal content (the verbatim quote), and both `L3/krylov-step.md:188` and `L4-L3/krylov-step-typed-wrapper-dissolution.md:204` independently name this §section as the lifted-in home — established claim-home semantics, not a relay to an empty pointer. The body-identity observation is a terminal *methodological* claim that does NOT lower to an L0 Palace source (the distinct CG primitive-sequence L0 home is `L1-L0/ksp-solve-mutation-rotation.md` Sub-pattern B, `iterative.cpp:360-486`, named at L2:138 — recorded in the report's working-notes lifting-note, NOT in chapter content per the high→low directive). Status stays `firm` (the dangling provenance never gated firm status, only citation hygiene). **Repairer note (no artifact impact):** the META repair section corrected 2 working-notes frontmatter line-pointers in the report itself (`book/src/L2/krylov-step.md` precedent anchor "lines 138/146 + line 9" → "lines 138/146"; the stale "+ line 9" removed at the `inputs:` frontmatter + §Supporting-evidence sites) — these are the report's own working notes, NOT `book/` edits, so zero artifact impact; the load-bearing precedent anchors (138, 146) were already correct. **Co-pointer left in scope-acknowledged-but-untouched (NOT a hidden omission):** `gmres.md:459-471` (§Verified-against bullet 4) resolves in the live 671-line GMRES slice (the `inner_loop` body) and is correct as-is; should `gmres.md` later be reduced, a parallel lifter sweep re-points it (not this dispatch's job — the report flagged this explicitly in §Open-questions "Co-pointer note"). **Deferred to finalize:** (a) `integrated_at` + `integration_commit` frontmatter on the consumed report (per write-authority partition — per-report integrator does NOT touch report frontmatter; confirmed neither field present in CYCLE.md or META.md pre-apply); (b) formal OQ `status:` flip → `resolved` + `last_revisited: cycle-017` on the `l3-l2-body-identity-cg-md-citation-sweep` YAML block (ledger line 2903; I appended a RESOLUTION note, did not edit the YAML). **For finalize / next-cycle planner:** the cg.md-reduction re-anchor chain for the krylov-step lowering stack (L4 swept cycle-016, L2 swept cycle-016, L3-L2 swept now) is fully terminated; no further cg.md dangling-pointer sweep is open on this stack. The remaining cg.md-related future work is purely the eventual slice-removal audit (the L2 entry's §Evidence lines 138/146 RETAIN live `cg.md:27-141`/`:86-106` citations per the cycle-016 position-3 resolution; those re-point to `concepts/first-iteration-unrolling.md` only when the cg.md stub is fully removed — tracked under the `phase-1-slice-reduction-audit` skill, NOT actionable now).

---

## lifter-l3-chebyshev-sibling-refresh
applied_at: 2026-05-29T013000Z
applied_by: integrator-per-report
status: applied

Files touched:
- book/src/L3/chebyshev.md (edit ×5 — pure same-shape prose vocabulary refresh of all five remaining `forM_`/`foldM` sibling mentions onto the firm `iterate_while_pure`/`iterate_while_pure_L3` family with step-count predicates, closing OQ `l3-chebyshev-sibling-formm-foldm-prose-sweep`. (1) §Context [on-disk lines 44-47 pre-edit] — the `krylov-step`-vs-`chebyshev` iteration-structure contrast sentence: `chebyshev`'s "bounded `forM_`/`foldM` ranges (rendered at L3 as tail recursions over static index ranges)" → "two nested **step-count-predicate** `iterate_while_pure` folds (outer `pc_it` Richardson sweep `s.it <= op.pc_it`, inner `k`-recurrence `c.k <= op.order - 1`), rendered at L3 as the `iterate_while_pure_L3` tail recursions over those static ranges"; SAME edit block disambiguates the `krylov-step` clause "predicate-driven" → "convergence-predicate-driven" [IN-SCOPE disambiguation — necessary because the refresh introduces "step-count-predicate" in the same sentence; restores the convergence-gated-vs-step-count distinction the entry draws elsewhere at §Context "no convergence test … not predicate-driven" + §Status "no convergence predicate"; critic confirmed in-scope, not scope-creep]. (2) §Upward wrapper-dissolution bullet [~53-54] — "the `forM_`/`foldM` binds → tail recursions over static ranges" → "the two nested `iterate_while_pure` folds → `iterate_while_pure_L3` tail recursions over their step-count predicates". (3) §Non-adjacent-identity caveat [~95-96] — "a property of the surrounding `forM_`/`foldM` ranges, not of the body" → "a property of the surrounding two nested `iterate_while_pure` folds (the `iterate_while_pure_L3` tail recursions over the `pc_it`/`k` step-count predicates), not of the body". (4) §"L3 vs L4 distinction" L4-bullet [~478-479] — "the two sequential obstructions surface as `forM_` (outer) and `foldM` (inner) binds" → "surface as two nested `iterate_while_pure` folds with step-count predicates (`iterate_while_pure` outer `pc_it` sweep, `iterate_while_pure` inner `k`-recurrence)". (5) §"L3 vs L4 distinction" L3-bullet [~483-484] — "the `forM_`/`foldM` binds are tail recursions over static ranges" → "the two nested `iterate_while_pure` folds are the `iterate_while_pure_L3` tail recursions over their step-count predicates". NO L3 semantics/structure/signature/status change — entry stays `partial-obstruction`; no new citations, no new markdown links introduced. All five `[old]` anchors matched disk byte-for-byte on fresh re-read (line numbers drifted +4 from the OQ's pre-cycle-016 listing, exactly as the OQ flagged and the dispatch noted; no in-cycle file overlap — cycle-017 reports 1-3 touched distinct files). POST-APPLY VERIFICATION: re-grep `forM_\|foldM` in `book/src/L3/chebyshev.md` returned ZERO residual mentions — the L3 entry is now fully consistent with the firm L4 sibling `book/src/L4/chebyshev.md`.)
- scaffolding/open-questions.md (append — one RESOLUTION note after the `l3-chebyshev-sibling-formm-foldm-prose-sweep` OQ prose block [ledger line 2937], recording RESOLVED-IN-FULL by these 5 refreshes + the in-scope disambiguation, with the post-apply zero-residual grep noted; append-only, did NOT flip the `status:` YAML field [deferred to finalize per write-authority partition])

Gate hits:
- retroactive-budget: 0-slice / 2 global — these 5 edits are a pure vocabulary REFRESH of present-tense own-rendering prose (tracking an already-firmed L4 re-anchor), NOT retroactive evidence-correction on a firm slice; per-slice 0 (<3, no block); the running global tally across reports 1-4 stays 2 (report-2 divfree-projector 1 + report-3 krylov-step-body-identity 1; this report contributes 0). NOTE: integrator-finalize sees the aggregate across all 5 reports — flagging the running-global count here (still <4, no block).
- concept_writes-on-existing-slug: 0 (no concept page touched)
- forward-edge-without-surface: 0 (no forward-edge claim; pure same-layer vocabulary refresh of existing surface; the `iterate_while_pure_L3` term named resolves to a real firm definition at `book/src/L4/iterate-while.md:193`)
- edge-label-prose-mismatch: 0 (the refreshed sentences preserve high→low direction throughout — L4 `iterate_while_pure` folds lowering FORWARD into L3 `iterate_while_pure_L3` tail recursions; the step-count predicates `s.it <= op.pc_it` / `c.k <= op.order - 1` match the firm L4 §"L4 > L3" verbatim per the critic's independent edge-label-fidelity verification; no edge-label or direction change)
- H1-page-heading-reuse: 0 (no H1 touched; the `# chebyshev` heading is untouched)
- append-on-missing-slug: 0 (n/a — no append/slug-create to the artifact; in-place prose edits to an existing partial-obstruction entry)
- variant-axis-missing: 0 (n/a — vocabulary refresh, not a new multi-variant operator; the two variant axes [polynomial-kind 4th/1st, element-type real/complex] in §Variant axes are untouched and the refresh is orthogonal — the loop vocabulary applies uniformly across both kinds)
- index-placeholder-displacement: 0 (n/a — no index.md / dep-map touched)
- SUMMARY-registration: 0 (n/a — no new chapter; the chebyshev chapter already exists under the L3 Part)
- plan-kind-consistency: 0 (declared kind "pure same-shape vocabulary refresh" matches the applied content shape exactly — 5 in-place code-span swaps + 1 in-sentence disambiguation, no semantics/decomposition/signature/status change; status held `partial-obstruction`; critic plan-kind-consistency = pass)
- broken-link / cross-reference-integrity: 0 (the 5 edits are plain code-span vocabulary swaps; no new `[...](...)` markdown links added, so none can dangle; the `iterate_while_pure_L3` term resolves to `book/src/L4/iterate-while.md:193`; critic cross-reference-integrity = pass)
- variant-axis / bookkeeping: complete

Open questions promoted:
- (none NEW promoted — the report's only OQ action is CLOSING `l3-chebyshev-sibling-formm-foldm-prose-sweep`, recorded as a RESOLUTION note in open-questions.md; status-field flip → `resolved` + `last_revisited: cycle-017` deferred to finalize per append-only discipline)

Build-relevant: yes

Notes: Position 4 of 5. Pure same-shape prose vocabulary refresh on `book/src/L3/chebyshev.md` — the COMPANION sibling sweep to the cycle-016 named-sentence refresh, closing OQ `l3-chebyshev-sibling-formm-foldm-prose-sweep` (opened cycle-016, ledger line 2928). All FIVE remaining `forM_`/`foldM` mentions refreshed onto the firm `iterate_while_pure`/`iterate_while_pure_L3` family with step-count predicates, matching the register of the cycle-016 canonical paragraph (`:236-241`) and the firm L4 §"L4 > L3" anchor (`book/src/L4/chebyshev.md:427-436`). **Post-apply re-grep confirmed ZERO residual `forM_`/`foldM` mentions** — the L3 entry is now fully consistent with its firm L4 sibling. The one in-scope `krylov-step` clause disambiguation ("predicate-driven" → "convergence-predicate-driven") was the critic-confirmed in-sentence disambiguation made necessary by the refresh itself (the refresh introduces "step-count-predicate" in the same sentence; without "convergence-" the two predicate kinds would collide), NOT scope-creep. **Historical-narrative asymmetry vs cycle-016 L4 sweep is CORRECT (critic-confirmed):** all 5 L3 sites are present-tense own-rendering (the L4 form the entry lowers from, or the L3 form's own loop structure); NONE sits in a "superseded"/"was rendered as"/"slice's rendering" provenance frame — so refresh-all-5 is right, correctly differing from the cycle-016 L4-side sweep which kept 4 INTENTIONAL provenance mentions. The cycle-016 OQ flagged that line numbers would drift +4 from its own listing (named-sentence edit's net effect); confirmed on fresh re-read (all 5 `[old]` anchors matched disk byte-for-byte). The pre-existing OQ for a dedicated thin L4>L3 chebyshev identity-theme (referenced at §Upward) is untouched — out of scope for a pure vocabulary refresh, remains open. **Deferred to finalize:** (a) `integrated_at` + `integration_commit` frontmatter on the consumed report (per write-authority partition — per-report integrator does NOT touch report frontmatter; confirmed neither field present in CYCLE.md or META.md pre-apply); (b) formal OQ `status:` flip → `resolved` + `answered_in` this CYCLE.md + `last_revisited: cycle-017` on the `l3-chebyshev-sibling-formm-foldm-prose-sweep` YAML block (ledger line 2928; I appended a RESOLUTION note, did not edit the YAML). **Skill-uptake telemetry (non-blocking, for finalize → integrator-signals → batch-4 meta-phase):** the report's META carries a skill-uptake-survey WARNING — it performs `verify-citation-range`-shaped citation self-verification (`iterate-while.md:193`/`:190` line callouts) but never names the `verify-citation-range` skill by slug; repairer ruled this not-needed (verification substance present, only the skill-slug back-reference absent; not a repairable defect). Continuing named-skill-by-slug-uptake-weakness telemetry consistent with the cycle-016/017 batch — surface for the batch-4 meta-phase. **For finalize / next-cycle planner:** the cycle-014 L4 re-anchor → cycle-016 named-sentence refresh → cycle-017 sibling sweep chain for `book/src/L3/chebyshev.md` is now FULLY TERMINATED (zero residual `forM_`/`foldM` in the L3 entry); the only chebyshev follow-up still open is the pre-existing dedicated-thin-L4>L3-identity-theme OQ (out of scope here; lowering-verifier candidate if it wants a dedicated audit anchor).

---

## cross-layer-cross-cutter-closure-nesting-gate
applied_at: 2026-05-29T015500Z
applied_by: integrator-per-report
status: applied

Files touched:
- scaffolding/open-questions.md (append ×2 — (1) an **[ANSWER]** note after the existing `divfree-closure-nesting-constructed-gate-carrying-constructed-gate` OQ prose block [ledger ~L2899, opened cycle-016]: the "first … does not recur" premise is REFUTED by the firm `eigsolve-mutation-rotation` theme [cycle-011, commit `8bb16b7`, five cycles before divfree's `b54ea1c`] — eigsolve's closure `E` carries TWO nested gates `E.linear : Solver[A]` + `E.projector : Maybe DivFreeSolver` [the divfree projector itself, so eigsolve ⊃ divfree ⊃ ksp transitively], and its core sub-pattern B lowers each `opInv->Mult` through the firm `ksp-solve-mutation-rotation` theme; divfree is the THIRD instance, `ksp_solve` `K.M⁻¹` a latent further site; ≥2-firm-instance bar cleared; cited the report + critic VERIFIED-SOUND verdict; `status:` flip → `answered` + `last_revisited: cycle-017` deferred to finalize. (2) a NEW OQ block `nested-constructed-operator-gate-concept-and-divfree-correction` [opened_at cycle-017, opened_by cross-layer-cross-cutter, status open] inserted before the `## Dropped` section, routing the two cycle-018 follow-ups: (a) `nested-constructed-operator-gate` concept page → cycle-018 `layer-intro-author` [full page-contents spec: name the shape, the cross-layer fidelity rule, instance index w/ three-deep transitivity, sibling relations, + the two confirm-before-cite caveats — eigsolve-is-clean-FIRM-despite-LinearSolveFailed + ksp_solve-latent-pending-L0-confirm]; (b) divfree-theme "first"/"no other op" correction at `book/src/L1-L0/divfree-projector-mutation-rotation.md:108-113` + `:457-464` [+ the ANSWERED OQ prose] → cycle-018 lifter/harvester scoped append-only dispatch. Both appends are append-only per write-authority partition; did NOT edit existing YAML/prose blocks.)

NO book/ changes applied — this is a READ-ONLY cross-layer audit (no `## Proposed changes` block in CYCLE.md; META confirms "read-only cross-layer audit with NO `book/` proposed-changes, `Build-relevant: no`").

Gate hits:
- retroactive-budget: 0-slice (this report) / 2 global — this report applies ZERO book/ edits (OQ-ledger appends only); the running global retroactive-budget tally across all 5 reports stays 2 (report-2 divfree-projector 1 + report-3 krylov-step-body-identity 1; reports 1/4/5 contribute 0). Well under the global ≥4 block threshold. NOTE for finalize: aggregate confirmed = 2 across the cycle.
- forward-edge-without-surface: 0 (n/a — read-only audit, no forward-edge claim, no surface mutation)
- edge-label-prose-mismatch: 0 (n/a — no L_{n+1}>L_n edge authored; the report is an L1↔L1>L0 same-direction cross-cut observation, no edge label carried)
- concept_writes-on-existing-slug: 0 (n/a — no concept page written this report; the recommended `nested-constructed-operator-gate.md` is routed to cycle-018 layer-intro-author, NOT created here; provisional slug, correctly absent)
- H1-page-heading-reuse: 0 (n/a — no book/ file touched)
- append-on-missing-slug: 0 (n/a — no book/ append; the OQ-ledger appends anchor on the existing `divfree-closure-nesting-…` block prose + the `inner-product-fold-sibling-candidate` block end, both matched disk byte-for-byte on fresh re-read)
- variant-axis-missing: 0 (n/a — read-only audit, not a multi-variant operator entry; the report's gate-vs-raw survey table is an exhaustive classification over the L1 op set, critic variant-axis-coverage = pass)
- index-placeholder-displacement: 0 (n/a — no index.md / dep-map touched)
- SUMMARY-registration: 0 (n/a — no new chapter created; the concept page is cycle-018 layer-intro-author work, its SUMMARY entry lands then)
- plan-kind-consistency: 0 (declared kind read-only cross-layer-cross-cutter observation; content shape matches — observes/refutes/recommends with no artifact mutation; critic plan-kind-consistency = pass)
- broken-link / cross-reference-integrity: 0 (the OQ-ledger ANSWER + NEW-OQ prose reference real artifact slugs [`eigsolve-mutation-rotation`, `divfree-projector-mutation-rotation`, `constructed-operator-factory`, `solver-as-operator`, `variant-absorption`, all confirmed by critic cross-reference-integrity = pass]; the forward-referenced `nested-constructed-operator-gate.md` is correctly flagged provisional in the NEW OQ; no markdown link added to book/ surface)
- variant-axis / bookkeeping: complete

Open questions promoted:
- divfree-closure-nesting-constructed-gate-carrying-constructed-gate (ANSWERED — premise REFUTED by the eigsolve precedent [cycle-011, `8bb16b7`]; recorded as an [ANSWER] note after the existing OQ prose; `status:` → `answered` + `last_revisited: cycle-017` deferred to finalize per append-only discipline)
- nested-constructed-operator-gate-concept-and-divfree-correction (NEW — opened_by cross-layer-cross-cutter, status open, non-blocking; routes the two cycle-018 follow-ups: (a) concept page → layer-intro-author; (b) divfree-theme "first"/"no other op" correction → lifter/harvester. Folds the report's repairer-added §"Follow-up dispatches to route" preamble bullets (a)+(b); the report's third preamble bullet (c) is the ANSWER above, not a separate open OQ)

Build-relevant: no

Notes: Position 5 of 5 — LAST per-report integrator of cycle-017. **Read-only cross-layer audit, zero book/ mutation** — the report has no `## Proposed changes` block (META Suggested-resolution: "read-only cross-layer audit with NO book/ proposed-changes … Build-relevant: no"). overall_status `ready` (critic 7/8 pass + 1 skill-uptake-survey warning [telemetry, `verify-citation-range` + `classify-variant-axis` implied-but-unnamed]; repairer all not-needed — the one repair was a mechanical relocation of the three follow-up recommendations from §Recommendation into §"Open questions / caveats" as a "Follow-up dispatches to route" preamble so this integrator's promotion step [role-spec step 6] would auto-promote them; no substantive content authored by repairer). **Verdict landed:** the audit's headline refutation is VERIFIED-SOUND — divfree's three "first"/"no other current L1 op" claims (`:108-113`, `:457-464`, + the OQ-ledger prose) are inaccurate against firm artifact (`eigsolve-mutation-rotation`, cycle-011 `8bb16b7`) authored five cycles before the divfree theme (`b54ea1c`); eigsolve carries TWO nested gates and its core sub-pattern B is exactly the nested-gate lowering. **OQ-ledger ANSWER + NEW-OQ are the entire deliverable of this report** (no surface change). **Deferred to finalize:** (a) `integrated_at` + `integration_commit` frontmatter on the consumed report (per write-authority partition — per-report integrator does NOT touch report frontmatter; confirmed neither field present in CYCLE.md or META.md pre-apply); (b) formal OQ `status:` flip → `answered` + `last_revisited: cycle-017` on the `divfree-closure-nesting-constructed-gate-carrying-constructed-gate` YAML block (ledger ~L2890; I appended an [ANSWER] note, did not edit the YAML). **For finalize / next-cycle planner — TWO cycle-018 dispatch candidates routed via the NEW OQ `nested-constructed-operator-gate-concept-and-divfree-correction`:** (1) `layer-intro-author` → author `book/src/concepts/nested-constructed-operator-gate.md` (≥2 firm instances cleared: eigsolve cycle-011 + divfree cycle-016; the page replaces the divfree theme's in-line nesting note; two confirm-before-cite caveats carried — eigsolve's `LinearSolveFailed` partly-constructive sub-part is orthogonal to the firm nesting, and the latent `ksp_solve` `K.M⁻¹`-as-`Solver` site needs L0 confirmation before being listed past "latent"); (2) lifter/harvester → scoped APPEND-ONLY correction of the three inaccurate divfree-theme "first" claims (the theme is append-only after `integrated_at:`; not a free edit). Concept-over-combinator is the recommended first home (documentation/fidelity shape, not a new evaluation primitive); re-route to combinator-miner only if a later cycle finds the nesting needs L4-level support. **Cross-cutting finalize note:** this is the FINAL row of cycle-017's staging log — finalize now has all 5 rows. Build-relevant tally across the cycle: reports 1-4 = yes (book/ touched), report 5 = no (OQ-ledger only) → book rebuild needed (reports 1-4 mutated `book/src/{L2/index,L1/divfree-projector,L3-L2/krylov-step-body-identity,L3/chebyshev}.md`). Global retroactive-budget aggregate = 2 (<4, no block). **Skill-uptake telemetry continues** (this report's skill-uptake-survey warning is the 4th in the cycle-016/017 batch carrying the named-skill-by-slug-uptake-weakness signal — surface for batch-4 meta-phase via integrator-signals).

---
