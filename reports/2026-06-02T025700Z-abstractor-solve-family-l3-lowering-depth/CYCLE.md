---
agent: abstractor
invoked_at: 2026-06-02T02:57:00Z
scope: L3 lowering-depth warrant call — solve_family (does the L3 family-solve loop warrant a firm L3/solve_family entry + an L3>L2 hop?)
status: pending
integrated_at: 2026-06-02T050000Z
integration_commit: PLACEHOLDER_SHA
integration_notes: "Applied cycle-057 (D1). NO-ENTRY warrant — no L3/solve_family entry; 5 forward-reference corrections re-point dangling promises of the absent slug onto the firm L4>L3 solve-family-map-dissolution theme as the authoritative L3-form home. No count change, no new file. WARRANT-FIRST anti-mirror discipline (the negative warrant is the recorded outcome). Build clean; no live link to the absent L3/solve_family.md created (stays code-span). OQ solve-family-l3-no-entry-warrant-record promoted."
inputs:
  - book/src/L4/solve_family.md (rough-in test-coverage-bounded; the L4 fixed-operator map-over-RHS-family combinator)
  - book/src/L4-L3/solve-family-map-dissolution.md (firm; the L4>L3 dissolution to the L3 explicit accumulating loop)
  - book/src/L3/ksp_solve.md (firm; the precedent — L3 driver carrying a genuine outer-loop sequential-obstruction)
  - book/src/L3/index.md (the L3 dep-map + count tally; SOLE count-owner this cycle)
  - book/src/L3-L2/index.md (the L3>L2 theme list; SOLE count-owner this cycle)
  - reference/palace/palace/drivers/electrostaticsolver.cpp:30-90 (fixed-operator witness)
  - reference/palace/palace/drivers/magnetostaticsolver.cpp:30-100 (fixed-operator witness)
  - reference/palace/palace/drivers/drivensolver.cpp:168-180 (per-element-operator scope-boundary witness)
---

# CYCLE: L3 lowering-depth warrant call — solve_family

## Summary

**Verdict: NO-ENTRY.** The L3 family-solve loop does NOT warrant a firm `book/src/L3/solve_family.md` entry, and there is consequently no `book/src/L3-L2/solve-family-step-fold-dissolution` theme. The lowering is **fully and concisely expressed by the firm L4>L3 `solve-family-map-dissolution` theme**; a separate L3 chapter would be a **mirror** of that theme's already-authored §"L3 form (RHS)" plus its already-drawn obstruction-contrast against `L3/ksp_solve` — exactly the degenerate "have both levels" rationale that the 2026-06-01 VOCABULARY-SHIFT REDIRECT supersedes.

The load-bearing judgment is the contrast the dispatch named. `L3/ksp_solve` (firm) earns its chapter because L3 is the iteration-rotation layer and `ksp_solve` carries a **first-class outer-loop `sequential-obstruction`** that is *rendered as an explicit tail recursion you cannot find anywhere else*, and whose erasure drives the substantive L3>L2 theme `ksp-solve-outer-driver`. The `solve_family` family loop is the **opposite on that axis**: it carries **NO sequential obstruction** (the family members are independent — embarrassingly-parallel, written sequentially), so L3's reason-to-exist produces a *negative* finding ("this loop lifts cleanly"), and that negative finding is **already stated in L3 vocabulary in the dissolution theme** as the very contrast that gives the theme its substance (`solve-family-map-dissolution.md:102, 117`). There is no L3-native iteration-rotation primitive to introduce, no obstruction to render, and no substantive L3>L2 erasure (a substantive L3>L2 theme exists only where an L3 obstruction shadows to an L2 non-law — `solve_family` has none). The vocabulary fails to shift between an `L3/solve_family` chapter and the dissolution theme's RHS; they would be the same content.

Five forward-reference corrections follow from the verdict (the L4 cap, the dissolution theme, and the L4 dep-map currently name a *pending* `L3/solve_family` as the dissolution target / firm L3 image): re-point them at the dissolution theme as the authoritative L3-form home — two edits to the L4 cap (`L4/solve_family.md` frontmatter `lowers_to:` + §"Lowers to"), two to the dissolution theme (`L4-L3/solve-family-map-dissolution.md` §does-NOT-cover + §Verified-against), and one to the L4 dep-map (`L4/index.md:80` L3-image cell; the cycle-057 D1 repair-pass fifth correction). These are proposed-changes (corrections), applied by the integrator.

## The warrant call (load-bearing)

### What an `L3/solve_family` chapter would contain — and where each piece already lives

| Candidate L3-chapter content | Already authored at | Genuinely new at L3? |
|---|---|---|
| The L3 explicit positional accumulating `for`-loop form (`solve_family_L3 :: (op, rhss) -> solutions`, operator hoisted, pre-sized `std::vector<Vector>`, `step++` collection) | `solve-family-map-dissolution.md:59-82` (§"L3 form (RHS)") | No — verbatim the dissolution theme's RHS |
| The "no sequential obstruction / embarrassingly-parallel-written-sequentially" characterization | `solve-family-map-dissolution.md:102` (§"What does NOT change") + `:117` (§Applicability cond. 2) | No — already drawn, explicitly contrasted against `L3/ksp_solve` |
| The map→for / capture-once→hoist / trajectory→pre-sized-collection rewrite narration | `solve-family-map-dissolution.md:86-96` (the three coordinated rewrites) | No — that IS the dissolution |
| Variant axes (operator-capture / family-index / element-type / collection-shape) | `L4/solve_family.md:133-140` (cap §Variant axes), transported by the theme | No — transported, not L3-specific |
| Laws (concatenation-homomorphism, capture-hoist, element-independence) | `L4/solve_family.md:84-101` (cap §Algebraic laws) | No — family-map semantics, a cap concern |

Every candidate piece already has a home. The only "L3 content" a chapter could add is the *negative finding* "the family loop lifts (no obstruction)" — and that finding is precisely the contrast that the dissolution theme already draws to give itself substance.

### Why `L3/ksp_solve` warrants a chapter but `L3/solve_family` does not — the obstruction axis

L3 is the iteration-rotation layer; the single most important thing it captures per operator is **whether the iteration lifts** (`L3/index.md:7`, `:15`, "negative L3 results are part of the deliverable").

- **`L3/ksp_solve` (firm):** the outer fold over `krylov-step` does **NOT** lift — each step reads scalars (`α`, `β`, …) the previous step produced; the trajectory is intrinsically sequential (`L3/ksp_solve.md:100-104`, the outer-loop `sequential-obstruction`). The L3 entry is the *only* place this obstruction is rendered as an explicit `iterate_while_L3` tail recursion. Its erasure at the L3>L2 hop is what makes `ksp-solve-outer-driver` a **substantive** theme (the obstruction shadows to the L2 fold non-laws, `L3-L2/index.md:14`). The chapter is load-bearing.
- **`L3/solve_family` (would-be):** the family map carries **NO** obstruction. The members are independent (no carry between solves); the loop is embarrassingly-parallel, written sequentially as a coding realization, NOT a `sequential-obstruction` (`solve-family-map-dissolution.md:102`, the theme's own verdict: "unlike the inner `L3/ksp_solve` outer-driver fold ... the *family* loop has no cross-member dependence — it is an embarrassingly-parallel sweep written sequentially, not a `sequential-obstruction`"). There is nothing to render and nothing to erase.

The `(ksp_solve obstruction, solve_family no-obstruction)` contrast is genuinely interesting L3 content — **but it is content of the dissolution theme**, which draws it explicitly (`solve-family-map-dissolution.md:102, 117`, `:146` cross-refs `L3/ksp_solve` §"Iteration-rotation marker"). Duplicating it into a standalone L3 chapter does not shift any vocabulary; it mirrors.

### Anti-mirror test (the redirect's §1d smell)

A degenerate identity-in-named-terms lowering is a smell: the vocabulary failed to shift. An `L3/solve_family` chapter + a thin `solve-family-step-fold-dissolution` L3>L2 theme would exhibit exactly the smell:

- The L3 chapter body would be value-thread-isomorphic to the L4 cap modulo the map→for syntactic rewrite — and that rewrite is *already* the dissolution theme's job (narrated L4→L3 in `solve-family-map-dissolution.md:84-96`). An L3 chapter narrating the same rewrite "in L3 vocabulary" is the mirror.
- A `solve-family-step-fold-dissolution` L3>L2 theme has **no substance to carry**: the four firm substantive L3>L2 themes (`ksp-solve-outer-driver`, `orthogonalize-variant-split`, `eigsolve-opaque-eigen-iteration`, `chebyshev-nested-recurrence`, `L3-L2/index.md:43-46`) each exist because an L3 `sequential-obstruction` shadows to an L2 non-law. `solve_family` has no obstruction to erase, so the only L3>L2 content would be a degenerate map→outer-driver-by-role identity-in-named-terms restatement — precisely the kind of thin theme being DEMOTED in the cycle-050/051 refactor pass (`L3-L2/index.md:26`, `:51`), not authored fresh.

This is the clean NO-ENTRY case the dispatch anticipated: the L4>L3 dissolution theme fully expresses the lowering; no separate L3 chapter is warranted.

### Why this is NOT a contradiction of "the family loop has L3-relevant structure"

The dissolution theme's §"What does NOT change" (`:102`) and §Applicability condition 2 (`:117`) ARE the L3-relevant structural statement (the no-obstruction / parallel-decomposable characterization). The redirect's bar is whether that structure needs a *separate chapter* — i.e., whether it is "a GENUINE concise L3 form with its own vocabulary." It is not: it has no own iteration-rotation vocabulary (the family-map collapses to a bare `for`-over-independent-members, which is the *absence* of an L3 iteration primitive, not one), and its content already lives — coherently, in L3 vocabulary — in the dissolution theme. Each layer staying internally coherent (the redirect's prescribed resolution for a degenerate case) is satisfied: an L3 reader following the firm `solve_family` cap's §"Lowers to" lands at the dissolution theme, where the L3 form and its no-obstruction character are stated in full.

## Proposed changes

The verdict creates no new `book/` files. Two forward-reference corrections follow from it — the L4 cap and the dissolution theme each name a *pending* `book/src/L3/solve_family.md` as the dissolution target / "firm L3 image"; with NO-ENTRY that file will never exist, so the references must re-point at the dissolution theme as the authoritative L3-form home (a live link to a never-to-exist `L3/solve_family.md` would be a `linkcheck2` build error if ever linked, and is a dangling promise as prose).

```edit:book/src/L4/solve_family.md
[in the frontmatter `lowers_to:` block, replace the line

  - book/src/L3/solve_family.md (the L3 explicit std::vector<Vector>-accumulating outer sweep; theme L4-L3/solve-family-map-dissolution — batch-17, pending; NOT this entry's job)

with

  - book/src/L4-L3/solve-family-map-dissolution.md (the L4>L3 dissolution to the L3 explicit std::vector<Vector>-accumulating outer sweep — the authoritative L3-form home; NO standalone L3/solve_family entry, per the cycle-057 D1 NO-ENTRY warrant: the family loop carries no sequential-obstruction, so the L3 form is fully + concisely expressed by the dissolution theme's §"L3 form (RHS)" — a separate L3 chapter would mirror it)
]
```

```edit:book/src/L4/solve_family.md
[in §"Lowers to" (currently ending the paragraph at `book/src/L4/solve_family.md:131`), replace the final sentence

The firm L3 image is the layer-coherent `L3/solve_family` (also batch-17; the dissolution target).

with

There is **no standalone `L3/solve_family` entry** (cycle-057 D1 NO-ENTRY warrant): the L4>L3 map-shell rotation is substantive, but the resulting L3 family loop carries **no `sequential-obstruction`** (the family members are independent — embarrassingly parallel, written sequentially), so L3's iteration-rotation content for the family shell is the *negative* finding "the loop lifts" — already stated, in L3 vocabulary, in the dissolution theme's §"L3 form (RHS)" + §"What does NOT change" (where it is explicitly contrasted against the obstruction-carrying [`L3/ksp_solve`](../L3/ksp_solve.md)). A separate L3 chapter would mirror the dissolution theme's RHS, not shift vocabulary (the 2026-06-01 VOCABULARY-SHIFT REDIRECT anti-mirror principle). The authoritative L3-form home for the family shell is therefore the dissolution theme itself; the per-member solve `ksp.Mult` delegates to the firm [`L3/ksp_solve`](../L3/ksp_solve.md), which DOES carry its own (per-solve outer-loop) obstruction.
]
```

```edit:book/src/L4-L3/solve-family-map-dissolution.md
[in the §"What this lowering does NOT cover" bullet on the L3>L2 hop (currently `book/src/L4-L3/solve-family-map-dissolution.md:109`), replace

- **The L3>L2 hop**, which (for the family shell) consolidates into the L2 outer-driver framing — a separate L3>L2 theme, batch-17-pending, gated on the L3 `solve_family` entry (itself batch-17). Not duplicated here.

with

- **The L3>L2 hop.** There is **no `L3-L2/solve-family-step-fold-dissolution` theme and no standalone `L3/solve_family` entry** (cycle-057 D1 NO-ENTRY warrant). The family loop carries **no `sequential-obstruction`** (independent members; the loop lifts), so it has no L3 obstruction to render as a distinct chapter and none to *erase* at an L3>L2 hop — the four firm substantive L3>L2 themes each exist because an L3 obstruction shadows to an L2 non-law, which the family shell does not have. A `solve_family` L3>L2 theme would be a degenerate identity-in-named-terms restatement (the §1d smell, the kind of thin theme being demoted in the cycle-050/051 refactor pass), not a substantive rotation. The L3 form of the family shell lives here (§"L3 form (RHS)"); the per-member solve's L3>L2 consolidation is the inner [`ksp-solve-driver-dissolution`](./ksp-solve-driver-dissolution.md) chain's concern.
]
```

```edit:book/src/L4-L3/solve-family-map-dissolution.md
[in §"Verified-against" → "L3 source (the RHS of this rewrite)" the bullet

- `book/src/L3/ksp_solve.md` (firm cycle-020) — the per-member solve target [`L3/ksp_solve`](../L3/ksp_solve.md) ...

is correct and unchanged; ADD immediately after it a NO-ENTRY provenance note bullet:

- **No `book/src/L3/solve_family.md`** — cycle-057 D1 warrant verdict NO-ENTRY: the family-map loop carries no `sequential-obstruction` (independent members; the loop lifts), so the L3 family-shell form is fully expressed by this theme's §"L3 form (RHS)"; a standalone L3 chapter would mirror it (2026-06-01 VOCABULARY-SHIFT REDIRECT anti-mirror principle). This theme is the authoritative L3-form home for the family shell.
]
```

```edit:book/src/L4/index.md
[in the `solve_family` L4 dep-map row's L3-lowering cell (currently `book/src/L4/index.md:80`), replace the substring

via the **substantive** L4>L3 theme `solve-family-map-dissolution` *(batch-17 / cycle-055 dispatch #2; pending)*; firm L3 image `L3/solve_family` *(batch-17; pending)*.

with

via the **substantive** L4>L3 theme [`solve-family-map-dissolution`](../L4-L3/solve-family-map-dissolution.md) (firm), which is the **authoritative L3-form home** for the family shell. There is **no standalone `L3/solve_family` entry** (cycle-057 D1 NO-ENTRY warrant): the family loop carries no `sequential-obstruction` (independent members; the loop lifts), so L3's iteration-rotation content is the *negative* finding "the loop lifts" — already stated in L3 vocabulary in the dissolution theme's §"L3 form (RHS)" + §"What does NOT change"; a separate L3 chapter would mirror it (2026-06-01 VOCABULARY-SHIFT REDIRECT anti-mirror principle).
]
```

(No `book/src/L3/index.md`, `book/src/L3-L2/index.md`, or `book/src/SUMMARY.md` row additions — NO-ENTRY adds no L3 operator and no L3>L2 theme. As SOLE count-owner of both indices this cycle, I confirm: **no count change** — L3 stays 17 firm + 3 partial-obstruction; L3>L2 stays 5 firm themes. See §Index count-ownership below.)

## Index count-ownership (SOLE owner this cycle)

I am the sole count-owner of `book/src/L3/index.md` and `book/src/L3-L2/index.md` this cycle (D2/D3/D4 do not touch them). The NO-ENTRY verdict means **no rows are added to either index and both running tallies are unchanged**:

- `book/src/L3/index.md` — count stays **17 firm + 3 partial-obstruction** (the single authoritative tally in the `orthogonalize`-landing bullet, `L3/index.md:63`, as last consolidated cycle-052 D4). No new operator row. No edit needed.
- `book/src/L3-L2/index.md` — count stays **5 firm themes** (`krylov-step-body-identity`, `ksp-solve-outer-driver`, `orthogonalize-variant-split`, `eigsolve-opaque-eigen-iteration`, `chebyshev-nested-recurrence`; `L3-L2/index.md:51`). No new theme row. No edit needed.

The promotion-time index-cell guard (cycle-056 D2 recommendation) is satisfied vacuously: no Status is set this dispatch, so no index cell changes.

## Supporting evidence

All L0 citations self-verified against on-disk source this dispatch via `tools/citecheck/citecheck.py --anchor` (all `[ok]`):

- `reference/palace/palace/drivers/electrostaticsolver.cpp:36` (`ksp.SetOperators(*K,*K)` — operator captured outside the loop), `:46` (`std::vector<Vector> V(n_step)` pre-sized family collection), `:60` (`for (const auto &[idx, data] : laplace_op.GetSources())` — the family sweep). Fixed-operator witness.
- `reference/palace/palace/drivers/magnetostaticsolver.cpp:47` (`std::vector<Vector> A(n_step)`), `:66` (`for (... : curlcurl_op.GetSurfaceCurrentOp())`). Fixed-operator witness.
- `reference/palace/palace/drivers/drivensolver.cpp:176` (`auto A = space_op.GetSystemMatrix(...)` operator rebuilt per-ω INSIDE the loop), `:180` (`ksp.SetOperators(*A,*P)` INSIDE the loop). The per-element-operator scope boundary — NOT `solve_family`; confirms the family-map is the fixed-operator shape only.
- `book/src/L4-L3/solve-family-map-dissolution.md:59-82` (§"L3 form (RHS)" — the L3 family-loop form already authored), `:102` + `:117` (the no-`sequential-obstruction` verdict, contrasted against `L3/ksp_solve`).
- `book/src/L3/ksp_solve.md:100-104` (the outer-loop `sequential-obstruction` — the load-bearing reason that entry warrants a chapter; the contrast case).
- `book/src/L3-L2/index.md:43-46` (the four firm substantive L3>L2 themes, each obstruction-driven — the structural reason a `solve_family` L3>L2 theme would have no substance).
- `book/src/L3/index.md:7, :15, :63` (L3 is the iteration-rotation layer; negative results are deliverables; the single authoritative count tally).

## Open questions / caveats

- **OQ (new, low-priority): `solve-family-l3-no-entry-warrant-record`.** Records the cycle-057 D1 NO-ENTRY verdict so a future planner does not re-propose an `L3/solve_family` backfill under a stale "Identity-lowerings still require both L levels" reading (that invariant is SUPERSEDED by the 2026-06-01 redirect; the anti-mirror principle governs). The L3 family-shell form lives in `L4-L3/solve-family-map-dissolution.md` §"L3 form (RHS)"; no standalone L3 chapter, no `L3-L2/solve-family-step-fold-dissolution` theme. Trigger to re-open: a witnessed fixed-operator family sweep whose family loop carries a genuine cross-member `sequential-obstruction` (none in the 2 fixed-operator pipelines; would require a 3rd probe finding a stateful/fold family shape — but a fold family shape would NOT be `solve_family` anyway, it would be the superset's sibling, per `L4/solve_family.md:146`).

- **Caveat — this verdict is scoped to the FIXED-operator family (`solve_family`), 2-of-5 pipelines.** The per-element-operator superset `map_solve_over_(operator,rhs)_family` (driven, `drivensolver.cpp:176-180`) is batch-17 future work (OQ `solve-family-general-operator-rhs-superset-probe`, `L4/solve_family.md:146`). Whether THAT superset warrants an L3 entry is a separate, later warrant call — it MAY differ, because the per-ω operator rebuild interacts with frequency-sweep ROM/PROM machinery that could carry its own structure. The NO-ENTRY call here does not prejudge the superset. (Transient and eigenmode remain unprobed for the family pattern entirely.)

- **Caveat — the per-member solve DOES have a firm L3 entry.** The NO-ENTRY call is about the *family shell* only. Each member solve `ksp.Mult` is the firm `L3/ksp_solve` (which carries its own per-solve outer-loop obstruction). The family shell wrapping independent invocations of an obstruction-carrying inner solve is itself obstruction-free — that nesting is exactly the `divfree-projector` "obstruction-carrying by reference" precedent shape (`L3/index.md:38`), except here the *shell* is a parallel map rather than a straight-line composition, so it does not even carry the inner obstruction by reference at the shell granularity (each member is independent). This is consistent, not contradictory: the obstruction lives at the per-member `ksp_solve`, has its firm L3 home, and the family shell adds none.
