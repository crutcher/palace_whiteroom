---
agent: cross-layer-cross-cutter
invoked_at: 2026-06-01T235200Z
scope: L1↔L2 cross-cut — magnetostatic outer-solve-sweep probe (2nd witness for the solve-family combinator single-witness gate)
status: pending
integrated_at: 2026-06-02T010500Z
integration_commit: 5e8e7a1
integration_notes: "cycle-053 D1 — APPLIED clean (observation-only, NO book mutation). THE HEADLINE RESULT: the magnetostatic 2nd-pipeline probe DISCHARGES the cycle-052-D6 single-witness gate — the fixed-operator parametric solve-sweep is now 2-of-N witnessed (electrostatic electrostaticsolver.cpp:111-137 + magnetostatic magnetostaticsolver.cpp:110-152) => the fixed-operator solve-family combinator is MINEABLE, queued for the cycle-054 combinator-miner (highest-fan-out batch-16 frontier item). Scope caveat: driven BREAKS shared-operator-capture (drivensolver.cpp:176/:180 SetOperators-inside-loop) — combinator is fixed-operator-only, general map_solve_over_(operator,rhs)_family is the superset. 3 OQs promoted. Build exit 0 (no book change)."
---

# CYCLE: Cross-layer observation — magnetostatic-solve-sweep-probe (single-witness gate discharge: CONFIRMS)

## Summary

Probing `MagnetostaticSolver::Solve` (`palace/drivers/magnetostaticsolver.cpp:21-109`) as the 2nd witness for the cycle-052 D6 electrostatic finding (`electrostatic-outer-terminal-sweep-needs-solve-family-combinator`), the magnetostatic pipeline exhibits the **same outer parametric solve-sweep shape, structurally identical at the skeleton level**: the curl-curl stiffness operator `K` is assembled **once** outside the loop (`:29`), the linear solver is constructed once and bound to that fixed `K` (`:34-35`), and the body loops over a **source-term family** (`GetSurfaceCurrentOp()`, the surface-current boundaries), building a per-source RHS and calling the **same `ksp.Mult` (`ksp_solve`) cap** against the **shared captured `K`**, collecting the per-source solutions into a solution family `A[step]` (`:67-99`). The collection is then reduced by an `O(n²)` energy-product double-loop into a dense inductance matrix `M` with an in-place LAPACK `Invert()` (`PostprocessTerminals`, `:110-205`) — the **same Gram/capacitance-family-reduction shape** as electrostatic's `PostprocessTerminals`. This **CONFIRMS** the outer-solve-sweep shape is now **2-of-N witnesses** (electrostatic terminals + magnetostatic source-boundaries), discharging the `electrostatic-solver-probe-findings-are-single-witness-generality-unverified` gate **for the shared-operator-capture solve-sweep specifically**. The combinator becomes **mineable** — I record the shape precisely below for a cycle-054 `combinator-miner` dispatch. Per dispatch contract + the redirect's observation-first rule, I author **NO `book/` entry**. One caveat carried forward: both confirmed witnesses are **operator-fixed** sweeps; whether driven (frequency-sweep) and transient (time-step) **keep `K` fixed or re-assemble per step** is the load-bearing open question for the *generality of the shared-operator-capture invariant* (they plausibly differ — frequency changes the system matrix), which bounds whether the mined combinator is `map-solve-over-rhs-family[fixed A]` or needs an operator-varying sibling.

## Observation kind

**Coverage gap** (confirmed cross-pipeline) — the outer parametric solve-sweep operator referenced by the electrostatic D6 finding has **no entry at any layer** in the shared spine, and is now confirmed to recur across ≥2 pipelines with the **same shared-operator-capture structure**. The gap is a finding *about the spine* (a missing driver-level combinator), confirmed mineable; the magnetostatic solver is the 2nd test-load witnessing it, exactly as the redirect intends.

## Specific finding

**Structural comparison, magnetostatic vs electrostatic, on the three probe questions:**

| Probe question | Electrostatic (D6, `electrostaticsolver.cpp`) | Magnetostatic (`magnetostaticsolver.cpp`) | Same shape? |
|---|---|---|---|
| **Is the operator fixed across the sweep (shared-operator-capture)?** | YES — `K = GetStiffnessMatrix()` assembled once at `:30`, *outside* the loop at `:60`; `ksp.SetOperators(*K,*K)` once at `:34-35`. | **YES** — `K = curlcurl_op.GetStiffnessMatrix()` assembled once at `:29`, *outside* the loop at `:67`; `ksp.SetOperators(*K,*K)` once at `:35`. The Curl matrix `Curl` (`:30`) is also captured once. **Operator does NOT change per step.** | **SAME** |
| **Is the inner solve the same `ksp_solve` cap?** | YES — `ksp.Mult(RHS, V[step])` at `:69` = `ksp_solve(K, RHS_idx)`. | **YES** — `ksp.Mult(RHS, A[step])` at `:77` = `ksp_solve(K, RHS_idx)`. Identical `KspSolver::Mult` cap, same fixed `K`. | **SAME** |
| **Is the collection the same "collect into a solution family" shape?** | YES — solutions stored in `std::vector<Vector>` `V`, indexed `V[step]` per terminal (`:43-44`, `:69`); reduced by `PostprocessTerminals` `O(n²)` energy-product → dense capacitance `C` + in-place `C.Invert()` (`:111-139`). | **YES** — solutions stored in `std::vector<Vector> A(n_step)` indexed `A[step]` per source (`:45`, `:77`); reduced by `PostprocessTerminals` `O(n²)` energy-product → dense inductance `M` + in-place `Minv.Invert()` (`:122-152`). | **SAME** |

**The sweep skeleton matches step-for-step.** Mapping the magnetostatic driver body against the same six-step decomposition D6 used:

| Driver step | Magnetostatic source | Maps to existing spine? (same verdict as D6) |
|---|---|---|
| 1. Assemble system operator `K = GetStiffnessMatrix()` | `:29`, def `curlcurloperator.cpp:171-210` | **NO** — `BilinearForm k(GetNDSpace()); k.AddDomainIntegrator<CurlCurlIntegrator>(muinv_func); k.Assemble(...)`. Same FE-assembly-from-integrators gap as D6 step 1 (here `CurlCurlIntegrator` instead of `DiffusionIntegrator`; the assembly *shape* is identical). |
| 2. Construct linear solver `ksp.SetOperators(*K,*K)` | `:34-35` | **CLEAN (construction-absorbed)** — folds into `ksp_solve`'s constructed-`Solver[A]` opaque argument. |
| 3. **Outer loop over sources** `for (const auto &[idx,data] : curlcurl_op.GetSurfaceCurrentOp())` | `:67-99` | **NO** — re-solves the *same* `K` against a *family* of RHS vectors, collecting per-member solutions `A[step]`. The **shared-operator parametric solve-sweep** — exactly D6's gap item 1. The loop variable family here is *surface-current boundaries*; D6's was *terminals*. Structurally identical map-over-RHS-family with shared-operator capture. |
| 4. Build per-source RHS `GetExcitationVector(idx, RHS)` | `:76`, def `curlcurloperator.cpp:212-236` | **NO** — `SumVectorCoefficient fb; surf_j_op.AddExcitationBdrCoefficients(idx,fb)` + `LinearForm rhs; rhs.AddBoundaryIntegrator(VectorFEBoundaryLFIntegrator(fb)); rhs.Assemble()` + prolongation-transpose + `SetSubVector(RHS, dbc_tdof_lists, 0.0)` (Dirichlet elimination). BC-elimination / excitation-construction gap — D6's gap item 4. (Construction differs in detail — surface boundary LinearForm here vs D6's `ProjectBdrCoefficient`+`EliminateRHS` — but both are "build a per-member excitation vector with essential-dof elimination"; the *role in the sweep* is identical.) |
| 5. Inner linear solve `ksp.Mult(RHS, A[step])` | `:77` | **CLEAN** — exactly `ksp_solve`. `A[step] = ksp_solve(K, RHS_idx)`. |
| 6a. Field recovery `Curl.Mult(A[step], B)` (`B = ∇×A`) | `:85` | **CLEAN** — `apply_linop` (non-accumulating `Mult` variant). `B = apply_linop(Curl, A[step])`. (D6's 6a was `Grad.AddMult(...,-1)` for `E=-∇V`, the accumulating variant; here it is the plain `Mult` variant — same `apply_linop` operator, different variant axis.) |
| 6b. Norm postprocess `Norml2(...)`, `MeasureAndPrintAll` | `:81-83`, `:91` | **CLEAN (leaf-level)** — `Norml2` is `nrm2`. |
| 7. **Inductance-matrix reduction** `PostprocessTerminals` | `:110-205` | **PARTIALLY** (same as D6 step 7) — per-entry kernel `M(i,j) = Dot(A_gf, M_mag·A_gf) / (Iᵢ·Iⱼ)` (`:125-138`) is **`bilinear-form`** `Aⱼᵀ K Aᵢ` (the magnetic energy product; comments at `:124`/`:131` confirm `Mᵢᵢ=(AᵢᵀKAᵢ)/Iᵢ²`, `Mᵢⱼ=(AⱼᵀKAᵢ)/(IᵢIⱼ)`). But the **`O(n²)` double-loop accumulation into dense `M`** + in-place `Minv.Invert()` (`:152`, LAPACK) have no spine vocabulary — the same **Gram/solution-family reduction** as electrostatic's capacitance matrix. (Magnetostatic adds a current-normalization `/(Iᵢ·Iⱼ)` scaling and a second `Mm` magnetizing-inductance matrix `:139-148`, but the *family-reduction structure* is the same shape.) |

**Maps cleanly (inner kernel + leaves):** steps 2, 5, 6a, 6b, and the `bilinear-form` kernel of step 7 — identical to the electrostatic clean set.

**Does NOT map (outer skeleton + two ends):** steps 1, 3, 4, and the reduction-structure of 7 — **the same four gaps as D6**, now confirmed cross-pipeline.

**Conclusion: CONFIRMS.** The outer parametric solve-sweep with shared-operator-capture is **2-of-N witnesses**. The differences between the two witnesses are all at the *leaf-content* level (curl-curl vs diffusion integrator; surface-current vs terminal excitation; `Mult` vs `AddMult` field recovery; inductance vs capacitance physical meaning of the reduction) — the **structural skeleton** (assemble-once → construct-solver-once → map-`ksp_solve`-over-RHS-family-against-fixed-operator → collect-solution-family → `O(n²)` energy-product reduce → invert) is **invariant across both**. This is exactly the cross-pipeline recurrence D6 hypothesized but could not confirm from one witness.

MPI/Par* note (per scope): `ParOperator`, `MultigridOperator`, `Mpi::Print`, `Mpi::GlobalSum/GlobalMin`, `linalg::Norml2(comm,...)`, `linalg::Dot<Vector>(comm,...)`, `GetComm()` are read single-rank; the `comm` argument and `Mpi::Print` logging are flag-once-and-skip distribution surface. `SetFromTrueDofs` / `GetProlongationMatrix()->AddMultTranspose` (`:212-236`, `:130-131`) are the single-rank prolongation/restriction identity at our scope.

## Recommendation

**Dispatch a `combinator-miner` (cycle-054) on the shared-operator parametric solve-sweep** — the single-witness gate is now discharged (2-of-N), so the combinator is mineable. The miner should:

1. **Mine the shape precisely** (recorded below) across the two confirmed witnesses (electrostatic + magnetostatic) and author the combinator entry — likely a clean L4 `map`-over-family with the **shared-operator capture** as the load-bearing structural feature (operator assembled *once*, outside the sweep; the family is the RHS/excitation set; `ksp_solve` is the mapped function). Per the redirect: **replace-and-propagate, not mine-and-strand** — wire it into both driver descriptions, do not leave it stranded.
2. **Before authoring the final signature, check whether driven + transient keep the operator fixed.** Both confirmed witnesses are operator-fixed (steady-state, RHS-family-only variation). The **driven** solver sweeps over *frequencies* — the system matrix is `(K + iωC - ω²M)`, which **changes per step** (operator is NOT shared-captured); the **transient** solver steps in *time* — the operator may be fixed (constant time-step, same Jacobian) or re-assembled. This determines whether the mined combinator is **`map_solve_over_rhs_family[fixed A]`** (the 2 confirmed witnesses) or whether a sibling **operator-varying sweep** (`map_solve_over_(operator,rhs)_family`) is the more general form with the fixed-operator case as a specialization. **2 witnesses suffice to mine the fixed-operator combinator**; a 3rd probe (driven, D-future) is needed only to establish whether the fixed-operator capture is the *general* invariant or a *steady-state specialization*. Recommend mining the fixed-operator combinator now (2 witnesses confirm it) and flagging the operator-varying generalization as the open frontier.
3. **Fold the solution-family reduction (step 7) into the same miner or a sibling `same-layer-cross-cutter` `gram`-unification probe** — D6's gap item 2 (`capacitance-reduction-may-be-gram-variant-axis-extension`) is now also 2-of-N witnessed (capacitance `C` + inductance `M`, both `O(n²)` energy-product matrices over a solution family with general matrix-weight, both LAPACK-inverted). The `gram`-unification hypothesis is correspondingly stronger.

The FE-assembly (gap item 3) and BC-elimination (gap item 4) gaps are also re-confirmed cross-pipeline but remain a **separate FE-assembly sub-spine thread** (per D6) — not folded into the solve-sweep combinator.

## Proposed-changes

NONE. No `book/` mutation proposed this cycle (observation-only 2nd-pipeline probe; the clean mappings are already firm, the confirmed gap is mineable but authoring is the combinator-miner's Phase-2 dispatch, not this probe's). The deliverable is the CONFIRMS finding + the OQ-ledger appends below (for `integrator-per-report` to promote).

## Supporting evidence

- `palace/drivers/magnetostaticsolver.cpp:21-109` — `MagnetostaticSolver::Solve`: **operator assembled once** `K = curlcurl_op.GetStiffnessMatrix()` `:29`, `Curl` captured `:30`, **solver constructed once** `ksp.SetOperators(*K,*K)` `:34-35`, source count `:40`, RHS/solution-family storage `std::vector<Vector> A(n_step)` `:44-45`, **outer source loop** `for (const auto &[idx,data] : curlcurl_op.GetSurfaceCurrentOp())` `:67-99`, per-source RHS build `:76`, **inner solve** `ksp.Mult(RHS, A[step])` `:77`, field recovery `B=∇×A` `Curl.Mult(A[step],B)` `:85`, norm print `:81-83`, reduction call `PostprocessTerminals(...,A,I_inc)` `:106`.
- `palace/drivers/magnetostaticsolver.cpp:110-205` — `PostprocessTerminals`: dense inductance `M(A.size())` `:122`, **`O(n²)` double loop** `:123-149`, diagonal kernel `M(i,i)=Dot(A_gf, M_mag·A_gf)/Iᵢ²` `:125-130`, off-diagonal `M(i,j)=Dot(...)/(IᵢIⱼ)` `:138`, in-place `Minv.Invert()` (LAPACK) `:152`, CSV output (root-only, flag-skip) `:154-203`.
- `palace/models/curlcurloperator.cpp:171-210` — `GetStiffnessMatrix`: `BilinearForm k(GetNDSpace()); k.AddDomainIntegrator<CurlCurlIntegrator>(muinv_func); k.Assemble(...)` — FE-assembly-from-integrators (gap item 3; curl-curl integrator analog of electrostatic's `DiffusionIntegrator`). Essential-dof handling `SetEssentialTrueDofs(dbc_tdof_lists[l], DIAG_ONE)` `:202`.
- `palace/models/curlcurloperator.cpp:212-236` — `GetExcitationVector`: `surf_j_op.AddExcitationBdrCoefficients(idx,fb)` `:218`, `LinearForm rhs; rhs.AddBoundaryIntegrator(VectorFEBoundaryLFIntegrator(fb)); rhs.Assemble()` `:229-233`, prolongation-transpose `:234`, Dirichlet elimination `SetSubVector(RHS, dbc_tdof_lists.back(), 0.0)` `:235` — BC-elimination / excitation-construction (gap item 4).
- Existing spine vocabulary (same as D6, confirmed covering the magnetostatic clean set):
  - `book/src/L4/ksp_solve.md` (firm) — covers step 5 (`ksp.Mult` against fixed `K`) verbatim; its `solve_loop` is the *inner* Krylov iteration, NOT the outer source sweep.
  - `book/src/L1/apply_linop.md` (firm) — covers step 6a (`Curl.Mult`, non-accumulating variant; D6 used the accumulating `AddMult` variant — same operator, different variant axis).
  - `book/src/L1/bilinear-form.md` (rough-in) — covers the step-7 per-entry kernel `Aⱼᵀ K Aᵢ` exactly (magnetic energy product = `Mᵢⱼ` modulo the `1/(IᵢIⱼ)` current normalization).
  - `book/src/L1/nrm2.md` / `book/src/L1/dot.md` (firm) — cover the `Norml2` / `Dot` leaves.
  - `book/src/L2/gram.md` (firm) — closest analog to the step-7 reduction structure; the `gram`-unification hypothesis is now 2-witness-stronger.
- Cross-witness reference: `reports/2026-06-01T223300Z-cross-layer-cross-cutter-electrostatic-solver-probe/CYCLE.md` (D6) — the electrostatic 1st witness this probe confirms.

## Open questions / caveats

To append to `scaffolding/open-questions.md` (intake; meta-phase migrates actionable items to the plan):

- `solve-family-combinator-confirmed-2-of-n-mine-now` — the shared-operator parametric solve-sweep (assemble-once → map-`ksp_solve`-over-RHS-family-against-fixed-operator → collect-solution-family) is **CONFIRMED 2-of-N witnesses** (electrostatic terminals `electrostaticsolver.cpp:60-89` + magnetostatic source-boundaries `magnetostaticsolver.cpp:67-99`), discharging the `electrostatic-solver-probe-findings-are-single-witness-generality-unverified` gate for the **fixed-operator** case. The combinator is mineable. Dispatch a cycle-054 `combinator-miner` to author it (likely L4 `map`-over-family with shared-operator capture as the load-bearing feature). Supersedes / closes the action half of `electrostatic-outer-terminal-sweep-needs-solve-family-combinator`.
- `solve-sweep-shared-operator-capture-invariant-needs-driven-transient-check` — both confirmed witnesses are **operator-fixed** sweeps (steady-state, RHS-family-only variation). The **driven** solver's system matrix `(K + iωC − ω²M)` **changes per frequency step** (operator NOT shared) and the **transient** solver may re-assemble per time-step. The combinator-miner should author the **fixed-operator** combinator from the 2 witnesses now, and flag whether the more general form is `map_solve_over_(operator,rhs)_family` with fixed-operator as a specialization. A 3rd probe (driven pipeline, `drivensolver.cpp`) is the next witness to establish the shared-operator-capture invariant's *scope*, NOT a precondition for mining the fixed-operator combinator.
- `inductance-capacitance-reduction-now-2-witness-gram-hypothesis` — the `O(n²)` solution-family energy-product reduction (capacitance `C`, electrostatic `:111-139` + inductance `M`, magnetostatic `:122-152`) is now 2-of-N witnessed: dense matrix over a solution family, per-entry `bilinear-form` kernel with general matrix-weight, LAPACK in-place invert. Strengthens D6's `capacitance-reduction-may-be-gram-variant-axis-extension` — a `same-layer-cross-cutter` `gram`-unification probe should now treat it as a 2-witness pattern (the magnetostatic adds a per-pair current-normalization scaling + a second `Mm` matrix, which are variant-axis details on the same reduction shape).

Caveats to verify before acting:
- The CONFIRMS verdict rests on **structural skeleton** identity (assemble-once / fixed-operator-capture / map-over-RHS-family / collect / reduce-and-invert); the *leaf contents* differ (curl-curl vs diffusion integrator, surface-current vs terminal excitation, `Mult` vs `AddMult` recovery, inductance vs capacitance). The combinator-miner must abstract the combinator over these leaf-content variant axes — confirm the abstraction does not flatten a load-bearing difference (e.g. the `Mult`/`AddMult` field-recovery variant is *outside* the sweep combinator and should not be folded into it).
- The shared-operator-capture invariant is confirmed *only* for the two steady-state pipelines. Do NOT over-claim it as the general solve-sweep invariant until driven/transient are probed — the fixed-operator combinator is firm at 2 witnesses, but its *universality* is not (the driven frequency-sweep is a near-certain operator-varying counter-shape). Author the fixed-operator combinator; flag the operator-varying generalization as open.
- `MeasureAndPrintAll` (`:91`), `CurlFluxErrorEstimator` (`:50-58`, `:94`) were not decomposed — postprocessing/error-estimation surface beyond the core solve skeleton; flagged out-of-probe-scope, not claimed clean or unclean (same boundary D6 drew).
