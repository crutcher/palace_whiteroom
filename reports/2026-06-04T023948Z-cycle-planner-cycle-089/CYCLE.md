---
agent: cycle-planner
invoked_at: 2026-06-04T023948Z
scope: cycle-089 dispatch plan
status: pending
---

# Cycle 089 dispatch plan

## Goals selected this cycle

The SECOND primary cycle of meta-batch-28 (cycles 088/089/090; the batch-28 meta-phase fires after cycle-090's finalize). **The central decision was whether to run the `matrix-weighted-norm-full-firm-cascade-wave` now** — the c088 norm-axiom probe landed as a DISCHARGE (partial — structure-side) and the OQ's outcome-(a) trigger FIRED, making the cascade a "live candidate." **My honest call: the full-firm cascade is NOT yet licensed — it is PREMATURE.** A "full-firm cascade" propagates a `firm` state; the verb `matrix-weighted-norm` is `rough-in (test-coverage-bounded)` on disk (line 110, verified) and did NOT flip. There is no firm state to propagate. The structure-side discharge closed ONE of three open items (the math validity of laws 4/6/7); the FP-side residue (`:69-70`) and the gate-(a) √-entry-point test remain, and the c088 probe judged the FP-side **test-bounded (conservative)**. The honest in-scope next step is the CHEAP gate-test of whether the verb can flip at all — a bounded `lowering-verifier` **FP-residue confidence pass** (the c088-probe-shaped analog for the FP-side), NOT the heavy ~30-file sweep. The cycle pushes: (D1) the FP-residue dischargeability probe — the gate-test that determines whether a firm flip + cascade is ever warranted; (D2) the c088-D2-flagged frontmatter-hygiene follow-up on the `eigenfrequency-qfactor` column `composes:` `seed` residue.

## Central judgment on the cascade-wave: NOT licensed yet (verb still rough-in → nothing firm to cascade)

The reasoning, with the load-bearing on-disk evidence:

1. **The verb did NOT flip.** `book/src/L1/matrix-weighted-norm.md:110` reads `rough-in (test-coverage-bounded)` (pasted in §Deliverable-presence D1). The c088 finalize signal confirms: "the verb STAYS `rough-in (test-coverage-bounded)` (NO firm flip), NO count change, NO column flip" (`integrator-signals.md:33`).

2. **A "full-firm cascade" has no firm state to propagate.** The plan's cascade-gate text (`priorities.md:41`) calls it "A full `firm` promotion of `matrix-weighted-norm` cascades a ~30-file re-anchor sweep." The cascade's precondition is a full-firm promotion *to propagate*. The structure-side discharge is NOT a promotion — the new OQ itself states the full-firm flip is gated on **"(i) resolving the FP-side residue (`:69-70`) via an inherited-from-`dot`/`apply_linop` confidence argument OR a dedicated √-entry-point test, and (ii) the ~30-file cross-reference re-anchor"** (`open-questions.md`, `matrix-weighted-norm-norm-axiom-laws-structure-side-discharged`). Item (i) is unresolved. Running the 30-file sweep now would force a firm flip the evidence does not support — the exact forcing the VOCABULARY-SHIFT REDIRECT forbids (advance a layer only when cleanly describable; what a solver/verb can't cleanly say is a finding, not a forcing).

3. **The discharge only REFINED the c080 ruling; it did not flip the verb.** The new OQ: the c088 discharge "REFINES (not overturns) the c080 D1 'escape INAPPLICABLE' ruling" — the laws don't NEED a positive L0 law site because they follow from the SPD structure, which has one. But this is the **structure-side** only. The c083 meta-phase narrowed the firm-on-positive-structure escape to "applies iff (i) ALL folded primitives firm AND (ii) bare scalar arithmetic, no axiom-needing law" (`open-questions.md:1096`). `matrix-weighted-norm`'s norm-axiom laws carry inner-product-structure axioms whose FP realization (strict-CS ULP failures, bit-determinism across `B` representations, `:69-70`) is a numerical claim, not an exact-arithmetic theorem.

4. **The trigger FIRED, but "trigger fired" ≠ "cascade licensed."** Outcome-(a) of the c088 probe fired, which makes the cascade a *candidate to weigh* (the standing gate said it "becomes the c089 candidate"). Weighing it on-disk, the candidate fails its own precondition: no firm state exists. The honest c089 move is the in-scope **gate-test for whether a firm flip is achievable at all** — not the cascade.

5. **The cheap in-scope gate-test IS available and is the highest-fan-out genuinely-open work.** The c088 probe discharged the structure-side via a literature anchor; its exact analog for the FP-side is a bounded `lowering-verifier` pass asking: do the FP sub-claims at `:69-70` inherit sufficient confidence from the now-firm `dot`+`apply_linop` (both `firm` on disk "modulo the explicitly-recorded floating-point caveats" — `dot.md:100`, `apply_linop.md:87`) to discharge WITHOUT a dedicated √-entry-point test? Two clean outcomes: **(a) DISCHARGE** → the FP-side inherits → the verb CAN flip firm → THEN the `full-firm-cascade-wave` becomes a worth-dedicating OWN-cycle structural wave for a dedicated cycle (NOT c090 — that is land-clean — so it would be a batch-29 LEAD); **(b) CONFIRM-CEILING** → the FP-side genuinely needs the √-entry-point test the corpus lacks (ZERO `Norml2` refs in `test/unit/`, verified c088) → records the ceiling cleanly, verb stays rough-in, cascade stays NO-GO-with-explicit-verdict. Either outcome is a clean spine finding, NOT a forcing.

**Conclusion: do NOT run the cascade this cycle.** Run the FP-residue dischargeability probe (D1) — the cheap gate-test that determines whether the verb can flip firm at all — plus the c088-flagged frontmatter-hygiene follow-up (D2). This is the same probe-before-the-heavy-wave discipline the batch-27 meta-phase used to carve the c088 structure-side probe out of the NO-GO-HELD heavy wave, now applied to the FP-side. HARD CONSTRAINT carried into D1: do NOT trigger the ~30-file cascade regardless of outcome — touch only the verb's own §Status; the cascade is a separate dedicated wave gated on outcome (a).

## Deliverable-presence verification (paste-inline-evidence; per the strengthened c036 batch-10 requirement)

### D1 — `matrix-weighted-norm-fp-residue-law-confidence-probe` (`lowering-verifier`; touches `book/src/L1/matrix-weighted-norm.md` ONLY)

1. **File existence:**
   `ls -la book/src/L1/matrix-weighted-norm.md` → `-rw-rw-r-- 1 crutcher crutcher 31297 Jun  3 19:26 book/src/L1/matrix-weighted-norm.md` (EXISTS).
2. **Maturity / already-discharged check:** `## Status` line on disk (`:110`): ``rough-in (test-coverage-bounded)` — signature and algebraic laws are well-anchored by the L0 source ... but no dedicated Palace test exercises the SPD-weighted overload at this exact entry point ...`. The FP-side is NOT discharged: the §Status gate-(c) bullet (rewritten c088) reads "What this discharge does **NOT** cover ... the **floating-point** sub-claims at 'Laws that do not hold' `:69-70` ... remain genuinely test-bounded ... So the structure-side of 4/6/7 is closed; the FP-side and the entry-point test are the residue keeping the verb at rough-in." The verb carries 6 `verified_against:` blocks; grep for an FP-side block: NONE — the only c088 note is the STRUCTURE-SIDE discharge (`matrix-weighted-norm.md:170`: "...FP sub-claims at :69-70 (ULP strict-CS, bit-determinism) NOT covered and stay test-bounded; verb stays rough-in"). The FP-residue confidence question is genuinely UN-audited on disk → dispatch is NOT a no-op.
3. **OQ-ledger RESOLVED-grep:** `grep -niE "^\s*[-*].*matrix-weighted-norm.*(RESOLVED|CLOSED)"` returns ONLY closing line-items for ADJACENT constituents (the L1>L0 *theme* resolved c026 `:443`; the verifier-followup c027 `:444`; the radicand-half PARTIALLY-advanced c080 `:1068`) and the c028 adjudication that EXPLICITLY says "RESOLVED-as-adjudication ... (STAYS OPEN as plan Backlog item ...) — both L1 operators STAY rough-in" (`:477`). The OPERATOR ENTRY's gate OQ `matrix-weighted-norm-and-bilinear-form-stay-rough-in-with-sharpened-per-operator-gates-c028` is OPEN (`:1042`: "STAYS `rough-in (test-coverage-bounded)`"). The c088 OQ `matrix-weighted-norm-norm-axiom-laws-structure-side-discharged` is `partially-answered (structure-side DISCHARGED; FP-side OPEN)`. No closing line-item for the FP-residue. → OPEN.
4. **Structural-block check:** the FP-residue probe is the in-scope analog of an assembly test (the same evidence-kind the c088 structure-side probe + the c082/c083 reduce-verb passes used). It is NOT structurally blocked: it is a law-confidence judgment a `lowering-verifier` is authorized to make on a `rough-in (test-coverage-bounded)` entry. NOT on the STOP-PROPOSING negative list (`priorities.md:370` — `lu_solve`/`back_solve`/`ls-update-column`/4 NLEPS atoms/`apply_nonlinear_pencil`-HELD/`polynomial_smoother`/`L3/solve_family`/`L2/fold_solve`/`L2/fe_assemble`/`map_solve`/`weak_form_term`; `matrix-weighted-norm` and FP-residue-probe are NOT listed). PASS.

**ALL 4 PASS → recruit D1.**

### D2 — `eigenfreq-qfactor-column-composes-frontmatter-seed-hygiene` (`lifter`; touches `book/src/feature/eigenfrequency-qfactor.{L4,L1}.md` frontmatter ONLY)

1. **File existence:** `ls book/src/feature/eigenfrequency-qfactor.L4.md book/src/feature/eigenfrequency-qfactor.L1.md` → both EXIST (no error).
2. **Maturity / already-discharged check:** the stale `seed` label is on disk at the `composes:` frontmatter: `eigenfrequency-qfactor.L4.md:7` → `  - book/src/feature/eigenmode.L4.md (seed — the producing driver column: supplies the converged eigenpair family)` and `eigenfrequency-qfactor.L1.md:7` → `  - book/src/feature/eigenmode.L1.md (seed — the producing driver column: supplies the converged EigResult)`. The referent `eigenmode` column flipped `seed → firm` at c085 (signal `integrator-signals.md:33` + c085 reshape `priorities.md:65`). These are stale-on-disk maturity REFERENCES (the referent's own frontmatter is firm). NOT already fixed → dispatch is NOT a no-op.
3. **OQ-ledger RESOLVED-grep:** the c088 D2 intake OQ `eigenfrequency-qfactor-column-composes-frontmatter-stale-seed-label` is `needs-more (NEWLY OPENED by D2 intake)` (`integrator-signals.md:46`) — explicitly flagged-not-fixed per the lifter's hard 3-prose-label constraint, queued as "A co-target for a follow-up frontmatter-hygiene pass." OPEN.
4. **Structural-block check:** pure frontmatter prose re-anchor of stale maturity labels; the referent is firm on disk. NOT structurally blocked, NOT on the STOP-PROPOSING list. This is the firm-promotion-coupled whole-book-grep residue the batch-27 meta-phase GO-codified (`firm-promotion-coupled-re-anchor-needs-whole-book-cross-reference-grep`). PASS.

**ALL 4 PASS → recruit D2.**

### Codemap anchor confirmation (D1's √-overload entry point, pre-supplied to avoid localization loop)

`mcp__palace-codemap__read_range palace/linalg/operator.cpp:599-619` confirmed on-disk this plan: the named √-overload entry point `linalg::Norml2(comm,x,B,Bx)` — real specialization `:599-607` (`B.Mult(x,Bx); double dot = Dot(comm,Bx,x); MFEM_ASSERT(dot > 0.0, ...); return std::sqrt(dot);` — the outer √ at `:606`) + complex `:609-619` (`std::sqrt(dot.real())` at `:618`). The ONLY runtime check is `MFEM_ASSERT(dot>0.0)` — the FP sub-claims (`:69-70`) are asserted NOWHERE, confirming they are genuinely test-bounded (the probe's central question). This is the named entry point both gate (a) and the FP-side residue concern; pre-supplied so D1 reads the cited lines rather than entering a localization loop.

## Dispatches

1. **agent:** `lowering-verifier`
   **scope:** `matrix-weighted-norm-fp-residue-law-confidence-probe` — a SCOPED FP-side law-confidence probe on `book/src/L1/matrix-weighted-norm.md`, the c088-structure-side-probe analog for the FP residue. Judge whether the floating-point sub-claims at "Laws that do not hold" `:69-70` (strict Cauchy–Schwarz failing by ULP-level amounts via compound `dot`+`apply_linop` non-associativity; bit-determinism across operator representations of `B`) **inherit sufficient confidence from the now-firm `dot` (`book/src/L1/dot.md:100` firm "modulo ... floating-point caveats") and `apply_linop` (`book/src/L1/apply_linop.md:87` firm "modulo ... floating-point caveats")** to be discharged WITHOUT a dedicated √-entry-point test — i.e. are the verb's FP caveats EXACTLY the union of the two firm dependencies' already-recorded FP caveats (no NEW FP claim the composition introduces), or does the outer `√` / the `Bx`-workspace composition smuggle in an FP property that only a √-entry-point test (`linalg::Norml2(comm,x,B,Bx)`, codemap-confirmed `palace/linalg/operator.cpp:599-619`; outer √ at `:606`/`:618`; only runtime check `MFEM_ASSERT(dot>0.0)`) could confirm? **TWO clean outcomes:** (a) DISCHARGE — the FP caveats are inherited verbatim from firm `dot`+`apply_linop` with nothing new at the `√`/composition boundary → append a `verified_against:` block recording the inheritance argument + narrow the §Status gate-(c) FP-side note → **THIS LICENSES a future firm flip; queue `matrix-weighted-norm-full-firm-cascade-wave` as a DEDICATED OWN-CYCLE batch-29 structural wave** (NOT c090 land-clean; NOT bundled). (b) CONFIRM-CEILING — the `√`/composition introduces an FP claim genuinely needing the absent √-entry-point test → record the explicit-verdict ceiling in §Status + a findings note, verb stays `rough-in (test-coverage-bounded)`, cascade stays NO-GO-with-verdict (the explicit ceiling is itself the load-bearing finding). **HARD CONSTRAINT: do NOT trigger or touch the ~30-file cascade regardless of outcome; touch ONLY `book/src/L1/matrix-weighted-norm.md`'s own §Status + its YAML `verified_against:` block.** Cites the c088 structure-side discharge (`matrix-weighted-norm-norm-axiom-laws-structure-side-discharged`) as the prior — this is its FP-side completion analog; cites the c083 two-condition narrowed escape rule (`open-questions.md:1096`: applies iff folded primitives firm AND bare arithmetic, no axiom-needing law) as the standard the FP-side must meet.
   **deps:** none.
   **rationale:** active-head #1's recommended c089 candidate, re-scoped from the (premature) full-firm cascade to its actual precondition gate-test. **fan-out: HIGH** — the convergent foundation-blocker for 5 of 6 stay-seed columns (`gram_reduce` → electrostatic/magnetostatic/capacitance/inductance; `domain_energy_reduce` → energy-fields). Outcome (a) is the ONLY in-scope path that licenses the eventual cascade + the downstream reduce-verb firmings; outcome (b) converts the cascade NO-GO from inertia-held to verdict-held. Either way it is the highest-fan-out genuinely-open cleanly-describable work this cycle (the bottom-up width frontier is otherwise gated on exactly this verb — `open-questions.md:1096` confirms `domain_energy_reduce`/`gram_reduce` stay gated behind firming `matrix-weighted-norm`). NOT a forcing; the redirect's "what a verb can't cleanly say is a finding" discipline.

2. **agent:** `lifter`
   **scope:** `eigenfreq-qfactor-column-composes-frontmatter-seed-hygiene` — re-anchor the 2 stale `seed` maturity labels in the `composes:` frontmatter that the c088 D2 lifter flagged-not-fixed (its hard 3-prose-label scope constraint excluded the frontmatter): `book/src/feature/eigenfrequency-qfactor.L4.md:7` `  - book/src/feature/eigenmode.L4.md (seed — ...)` → `(firm — ...)` and `book/src/feature/eigenfrequency-qfactor.L1.md:7` `  - book/src/feature/eigenmode.L1.md (seed — ...)` → `(firm — ...)`. The referent `eigenmode` column flipped `seed → firm` at c085 (its OWN-composition constituents firm); these `composes:` labels are stale REFERENCES to a now-firm sibling column. Pure frontmatter maturity re-anchor — ZERO operator status/count change, ZERO column-status flip (the eigenfrequency-qfactor column itself is already `firm` on disk c085), ZERO SUMMARY change. Per the batch-27 `firm-promotion-coupled-re-anchor-needs-whole-book-cross-reference-grep` discipline, the dispatch ALSO runs a whole-`book/src/feature/` grep for any OTHER `eigenmode.*seed` / `eigenfreq_qfactor_reduce.*rough-in` cross-reference residue in the column's frontmatter and re-anchors any found (the c088 D2 land-clean already cleaned the prose body labels; this closes the frontmatter residue).
   **deps:** none.
   **rationale:** the c088 D2 explicitly-flagged frontmatter-hygiene follow-up (`integrator-signals.md:46,51`; OQ `eigenfrequency-qfactor-column-composes-frontmatter-stale-seed-label`). **fan-out: LOW** (drift-guard hygiene; closes the last frontmatter residue of the c085 column flip). Disjoint from D1 (different files, no shared region).

## Overlap analysis

- **D1 ↔ D2:** D1 touches `book/src/L1/matrix-weighted-norm.md` ONLY (verb §Status + its YAML block). D2 touches `book/src/feature/eigenfrequency-qfactor.{L4,L1}.md` frontmatter ONLY. **Byte-disjoint, different directories (`L1/` vs `feature/`), no shared file, no shared operator name, no shared index/SUMMARY/tally** (D1 emits NO count/SUMMARY change per its HARD CONSTRAINT; D2 emits ZERO status/count/SUMMARY change). **NON-OVERLAPPING → parallel.** No shared running-count / consolidated-tally owner needed (neither touches a layer index aggregate). No cross-report forward-reference (neither references a slug the other lands). No floor-landing/adjacent-entry coupling (D1 explicitly does NOT flip the verb, so no adjacent re-anchor is triggered; if outcome (a), the cascade + its re-anchor is a SEPARATE future dedicated wave, not this cycle).

## Sequencing schedule

**Single wave (both parallel):**
- **Wave 1 (parallel):** D1 (`lowering-verifier`, FP-residue probe), D2 (`lifter`, frontmatter hygiene).

Then the standard post-dispatch pipeline: 2 critics (parallel) → repairers as needed → `integrator-per-report` ×2 (serial) → ONE `integrator-finalize`. No forward-reference ordering between the two dispatches (byte-disjoint).

## Open questions / caveats

- **The c089→c090→batch-29 dependency the DISCHARGE outcome opens.** If D1 returns outcome (a) DISCHARGE, the verb CAN flip firm — but c090 is the batch-28 LAND-CLEAN cycle (3rd-of-batch), NOT a place for a heavy structural wave. So a DISCHARGE outcome would queue the `matrix-weighted-norm-full-firm-cascade-wave` (firm flip + the ~30-file whole-book re-anchor) as a DEDICATED batch-29 structural wave — surfaced for the batch-28 meta-phase (fires after c090) to weigh as the batch-29 LEAD. I have appended this conditional candidate to the Backlog. If D1 returns outcome (b) CONFIRM-CEILING, the cascade stays NO-GO-with-explicit-verdict and the batch-29 frontier returns to the bottom-up width survey. The batch-28 meta-phase inherits whichever verdict c089 produces.
- **Deliberately TWO dispatches, single wave — NO manufactured third.** The bottom-up width frontier (active-head #2) is genuinely gated on D1's outcome: `open-questions.md:1096` confirms the remaining reduce-verb tail (`gram_reduce` A3 / `domain_energy_reduce` A4) "stays gated behind firming `matrix-weighted-norm`" because a reduction is as firm as its least-firm folded primitive, and the firm-on-positive-structure escape applies only when all folded primitives are firm. So there is no clean reduce-verb law-confidence pass available this cycle that is not downstream of D1 — manufacturing one would be a rectangular pull-up the redirect forbids. The honest plan is the gate-test (D1) + the flagged hygiene (D2).
- **Whether the FP-side inheritance argument is itself sufficient is the open methodological question D1 settles.** The c088 probe established the precedent that a structure-side law CAN be discharged by inheritance-from-structure without a positive test. Whether an FP-side caveat can be discharged by inheritance-from-firm-dependencies (vs. genuinely needing a composition-boundary test) is the analogous question one layer down — and is itself a finding the batch-28 meta-phase may want to generalize (does "firm modulo FP caveats" compose cleanly through `√`?). Flagged for the meta-phase regardless of D1's outcome.
