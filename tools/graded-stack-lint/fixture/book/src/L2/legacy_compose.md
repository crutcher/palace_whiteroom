---
kind: feature-surface
feature: legacy
level: L2
status: seed
composes:
  - book/src/L1/good_op.md (firm — a vocabulary op, becomes depends-on)
  - book/src/feature/sibling.L4.md (a sibling root, becomes a reference)
l0_ground_truth:
  - palace/foo/bar.cpp:10-20 (Foo::Bar — evidence cite, not a book node)
---
# legacy_compose (transitional dual-form seed)

Exercises the migration mapping AND the transitional dual-form `status: seed`
(legacy combined root marker). Root via status:seed; composes -> typed edges.
